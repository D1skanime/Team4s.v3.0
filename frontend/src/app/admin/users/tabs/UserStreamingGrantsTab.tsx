'use client'

interface Props {
  userId: number
}

export function UserStreamingGrantsTab({ userId: _userId }: Props) {
  return (
    <div>
      <p>Streaming-Grants sind in dieser Version noch nicht konfigurierbar.</p>
    </div>
  )
}
