// Stub — wird in Task 2 (Plan 70-05) vollstaendig implementiert.
// Dieser Stub stellt sicher dass StoryImageExtension.ts in Tests kompiliert.
'use client'

import { NodeViewWrapper } from '@tiptap/react'
import type { NodeViewProps } from '@tiptap/react'

// Minimaler Stub der den TypeScript-Import von StoryImageExtension.ts auflöst.
// Task 2 ersetzt diesen Stub durch die vollstaendige NodeView-Implementierung.
export function StoryImageNodeView(_props: NodeViewProps) {
  return <NodeViewWrapper />
}
