---
gsd_summary_version: 1.0
quick_id: 260603-l77
slug: inventory-doc-gaps
status: complete
date: 2026-06-03
---

# Quick Task 260603-l77 — Summary

## Was gemacht wurde

`docs/architecture/current-system-inventory.md` um die beim Code-Abgleich
gefundenen fehlenden Oberflächen und Funktionsbereiche ergänzt (reine additive
Doku-Änderung, kein Code berührt).

## Änderungen im Dokument

1. **Episoden-Admin-Verwaltung** — zwei neue Zeilen in der Haupttabelle
   („Current Route/API/DB Inventory"):
   - `Admin Episodenverwaltung` (UI `/admin/anime/[id]/episodes`,
     `/episodes/[episodeId]/edit`, `/episodes/[episodeId]/versions`,
     `/admin/episodes`; Episode-CRUD + Version-CRUD; DB owner `episodes`,
     `episode_titles`, `episode_version_episodes`, `episode_theme_overrides`).
   - `Admin Episoden-Import` (UI `/admin/anime/[id]/episodes/import`;
     episode-import context/preview/apply, editor-context, folder-scan).
2. **Neuer Abschnitt „Zusätzliche UI-Routen"** — `/`, `/login`,
   `/episodes/[id]`, `/watchlist`, `/me/contributions`, `/invitations/accept`,
   `/claim-invitations/accept`, `/manage/groups`, `/admin`, `/admin/profile`,
   `/admin/fansubs` (+create, +merge), `/admin/my-groups`, `/dev/ui-system`.
3. **Neuer Abschnitt „Next.js BFF / Route-Handler"** — `/api/v1/[...path]`,
   keycloak token/logout, admin/upload-cover, admin/asset-proxy,
   `/covers/[file]`, `/media/[...path]`.
4. **Neuer Abschnitt „Ergänzende Backend-Endpunkte"** — `GET /genres`,
   `/admin/genres`, `/admin/tags`, Fansub-Merge, Member-Requests-Review.
5. **„Important DB groups" erweitert** — Episode-Detail, Anime-Metadaten-
   Side-Tables, Genres/Tags, Streaming/Release-Variant-Graph, Audit-Tabellen,
   Legacy-Parallelwelt (doppelte media-Tabellen, Legacy users/roles/user_roles).
6. **„Known High-Risk Duplication Traps" erweitert** — Episoden-Surface,
   BFF-Bypass, Legacy-vs-kanonisch (streams/media), Member-Request vs Claim.

## Verifikation

- Markdown-Tabellen bleiben valide (gleiche Spaltenzahl je Tabelle).
- Deutsche Umlaute korrekt (CLAUDE.md Sprachregel).
- Grundlage: Explore-Agenten + direkte Glob-Verifikation der Pages/Handler
  gegen `frontend/src/app/`, `backend/cmd/server/*.go`, Migrationen, `api.ts`.

## Hinweis

Reine Lookup-Tabellen (`*_types`, `visibilities`, `relation_types`, …) wurden
bewusst nicht aufgenommen — das Dokument ist eine kuratierte
„nicht neu bauen"-Karte, kein vollständiger Schema-Dump.
