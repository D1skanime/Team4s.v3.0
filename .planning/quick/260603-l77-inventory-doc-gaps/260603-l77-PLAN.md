---
gsd_plan_version: 1.0
quick_id: 260603-l77
slug: inventory-doc-gaps
description: Ergänze current-system-inventory.md um fehlende Oberflächen und Funktionsbereiche
date: 2026-06-03
mode: quick
---

# Quick Task 260603-l77: Inventar-Doku-Lücken schließen

## Kontext

Beim Code-Abgleich von `docs/architecture/current-system-inventory.md` gegen die
tatsächliche Codebasis (Backend-Routen in `backend/cmd/server/main.go` +
`admin_routes.go`, Frontend-Pages unter `frontend/src/app/`, Next.js
Route-Handler, DB-Migrationen, API-Client) wurden mehrere reale Oberflächen und
Funktionsbereiche gefunden, die im Inventar fehlen. Das Dokument ist die
„nicht neu bauen"-Karte — fehlende Oberflächen sind genau das Risiko, das es
verhindern soll.

## Verifizierte Lücken (Ground-Truth aus dem Code)

1. **Episoden-Admin-Verwaltung** — komplette Oberfläche fehlt:
   - UI: `/admin/anime/[id]/episodes`, `/episodes/import`,
     `/episodes/[episodeId]/edit`, `/episodes/[episodeId]/versions`,
     `/admin/episodes`
   - API: `POST/PATCH/DELETE /admin/episodes`,
     `episode-import/context|preview|apply`,
     `episode-versions/:id/editor-context`, `/folder-scan`,
     Episode-Version-CRUD unter `/anime/:id/episodes/:episodeNumber/versions`
2. **Fehlende UI-Routen** (existieren, nicht gelistet): `/` (Home), `/login`,
   `/episodes/[id]` (öffentliches Episodendetail), `/watchlist`,
   `/me/contributions`, `/invitations/accept`, `/claim-invitations/accept`,
   `/manage/groups`, `/admin` (Dashboard), `/admin/profile`,
   `/admin/fansubs` (Liste), `/admin/fansubs/create`, `/admin/fansubs/merge`,
   `/admin/my-groups` (Liste), `/dev/ui-system`
3. **Next.js BFF/Route-Handler-Schicht** (komplett fehlend):
   `/api/v1/[...path]` (Backend-Proxy), `/api/auth/keycloak/token`,
   `/api/auth/keycloak/logout`, `/api/admin/upload-cover`,
   `/api/admin/asset-proxy`, `/covers/[file]`, `/media/[...path]`
4. **Fehlende Backend-Endpunkte**: `GET /genres` (öffentlich),
   `GET /admin/genres`, `GET /admin/tags`, Fansub-Merge
   (`/admin/fansubs/merge` + `/merge/preview`), Member-Requests-Review
   (`/admin/member-requests` + approve/reject)
5. **Fehlende DB-Tabellen**:
   - Audit: `admin_anime_mutation_audit`, `audit_logs`
   - Streaming: `release_streams`, `streams`, `stream_sources`, `stream_types`,
     `release_sources`, `release_variants`, `release_variant_episodes`
   - Titel: `anime_titles`, `episode_titles`
   - Genres/Tags: `genres`, `anime_genres`, `tags`, `anime_tags`
   - Notizen: `member_anime_notes`, `member_episode_notes`
   - Sonstige: `anime_source_links`, `media_external`,
     `episode_version_episodes`, `episode_theme_overrides`
   - Legacy-Parallelwelt: doppelte media-Tabellen in
     `backend/database/migrations/`, Legacy `users`/`roles`/`user_roles`

## Aufgabe

Ergänze `docs/architecture/current-system-inventory.md` um diese Bereiche.
Reine additive Doku-Änderung, keine Code-Berührung. Deutsche Umlaute korrekt.

### Task 1: Inventar-Dokument ergänzen

- **files:** `docs/architecture/current-system-inventory.md`
- **action:**
  - Neue Zeile(n) in „Current Route/API/DB Inventory" für die
    Episoden-Admin-Verwaltung (UI + API + DB owner episodes/episode_titles/
    episode_version_episodes).
  - Neuen Abschnitt „Zusätzliche UI-Routen (Einstieg, Auth-Flows, Listen)" mit
    den fehlenden Pages aus Lücke 2.
  - Neuen Abschnitt „Next.js BFF / Route-Handler" mit den Handlern aus Lücke 3.
  - Inventar/Checklisten um die fehlenden Backend-Endpunkte (Lücke 4) ergänzen.
  - „Important DB groups" um Audit-, Streaming-, Titel-, Genre/Tag-, Notiz- und
    Legacy-Parallel-Tabellen (Lücke 5) erweitern.
  - „Known High-Risk Duplication Traps" um passende Einträge ergänzen
    (z. B. Episoden-Admin nicht neu bauen, BFF-Proxy nicht umgehen).
- **verify:** Datei enthält alle genannten Routen/Tabellen; Markdown-Tabellen
  bleiben valide; korrekte Umlaute.
- **done:** Alle 5 Lückenbereiche im Dokument abgebildet.

## must_haves

- truths:
  - Episoden-Admin-Oberfläche ist im Inventar dokumentiert.
  - Next.js BFF/Route-Handler-Schicht ist im Inventar dokumentiert.
  - Fehlende DB-Tabellen sind in „Important DB groups" ergänzt.
- artifacts:
  - `docs/architecture/current-system-inventory.md` (erweitert)
