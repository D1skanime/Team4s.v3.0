---
phase: quick-260707-hx0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.tsx
  - frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/project.module.css
  - frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.test.tsx
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07]

must_haves:
  truths:
    - "Nicht-zugeordnete Folgen (has_own_contribution=false) erscheinen in keinem Filter-Modus mehr auf der Seite (D-01)."
    - "Der Filter bietet exakt drei Modi: Alle, Offen, Erledigt (D-02)."
    - "Im Modus Alle stehen offene Folgen vor erledigten Folgen (D-03)."
    - "Jede sichtbare Folge zeigt ein Status-Badge (Offen/Erledigt) aus @/components/ui (D-04)."
    - "Die Folgen-Nummer-Suche funktioniert in allen drei Modi, nicht nur im bisherigen 'all'-Modus (D-05)."
    - "Die SectionHeader-Beschreibung bezieht sich auf zugeordnete Folgen (offen/erledigt-Zaehlung), nicht mehr auf die Gesamtzahl aller Release-Versionen (D-06)."
    - "Leerzustaende fuer 'keine zugeordneten Folgen', 'Modus Offen leer' und 'Modus Erledigt leer' zeigen eigene, motivierende deutsche Texte mit korrekten Umlauten (D-07)."
  artifacts:
    - path: "frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.tsx"
      provides: "To-Do-Workspace-Filterlogik (assigned/open/done), Sortierung, Badge-Anzeige, angepasste Leerzustaende"
    - path: "frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/project.module.css"
      provides: "Ggf. angepasste Zeilen-/Badge-Abstaende fuer neuen Status-Badge"
    - path: "frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.test.tsx"
      provides: "Tests fuer neue Modi, Ausschluss nicht-zugeordneter Folgen, Sortierung, Leerzustaende"
  key_links:
    - from: "frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.tsx"
      to: "frontend/src/types/contributions.ts"
      via: "MeProjectReleaseVersion.has_own_contribution / has_own_notes / has_own_media"
      pattern: "has_own_contribution"
    - from: "frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.tsx"
      to: "@/components/ui"
      via: "Badge import fuer Offen/Erledigt-Status"
      pattern: "Badge"
---

<objective>
Baue die persoenliche Projekt-Detailseite `/me/projects/[animeId]/group/[fansubGroupId]` von einem "Meine Beitraege"/"Alle"-Umschalter zu einem echten To-Do-Workspace um: die Basisliste zeigt nur Folgen, denen der Nutzer zugeordnet ist (`has_own_contribution`), ein Segmented-Control filtert zwischen Alle/Offen/Erledigt, offene Folgen stehen im "Alle"-Modus zuerst, und jede Folge zeigt einen klaren Status-Badge.

Purpose: Der Nutzer soll auf einen Blick sehen, wo er noch etwas schreiben/hochladen muss, ohne durch Folgen zu scrollen, an denen er gar nicht mitwirkt.
Output: Aktualisierte `page.tsx` (Filterlogik, Sortierung, Badges, Leerzustaende, Header-Beschreibung), angepasstes `project.module.css` falls noetig, erweiterte `page.test.tsx`.

Reine Frontend-Aenderung. Kein Backend-/DB-Change. Alle benoetigten Flags (`has_own_contribution`, `has_own_notes`, `has_own_media`) liefert das Backend bereits ueber `getMyProjectDetail`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.tsx
@frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/project.module.css
@frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.test.tsx
@frontend/src/types/contributions.ts

<interfaces>
<!-- MeProjectReleaseVersion (frontend/src/types/contributions.ts, Zeilen 172-184) -->
```typescript
export interface MeProjectReleaseVersion {
  release_version_id: number
  episode_number: string
  episode_title: string | null
  episode_sort_index: number | null
  version: string
  title: string | null
  role_codes: string[]
  role_labels: string[]
  has_own_contribution: boolean
  has_own_notes: boolean
  has_own_media: boolean
}
```

<!-- Badge (frontend/src/components/ui/Badge.tsx) -->
```typescript
export type BadgeVariant = 'neutral' | 'success' | 'warning' | 'danger' | 'info' | 'muted'
export interface BadgeProps extends HTMLAttributes<HTMLSpanElement> {
  variant?: BadgeVariant
}
export function Badge({ variant = 'neutral', className, children, ...props }: BadgeProps)
```
Import via `import { Badge } from '@/components/ui'` (bereits im Barrel-Export `frontend/src/components/ui/index.ts` enthalten).

<!-- Aktuelle Struktur page.tsx (vor Aenderung) -->
- `type ReleaseFilterMode = 'mine' | 'all'` (Zeile 15) -> wird zu `'all' | 'open' | 'done'`
- `filterReleases(releases, mode, episodeQuery)` (Zeile 46-61) -> Basisfilter auf `has_own_contribution`, dann Modus-Filter, dann Suche, dann Sortierung
- `mode`-State default `'mine'` (Zeile 77) -> Default wird `'all'`
- Segmented-Control-Buttons (Zeile 220-241): Labels "Nur meine Beitraege"/"Alle" -> werden "Alle"/"Offen"/"Erledigt"
- Suchfeld nur bei `mode === 'all'` sichtbar (Zeile 243-256) -> soll in allen drei Modi sichtbar sein
- SectionHeader description (Zeile 216): `${filteredReleases.length} von ${project.release_versions.length} Versionen sichtbar` -> auf zugeordnete Folgen umstellen
- Release-Zeile (Zeile 260-284): `hasOwnArtifacts = release.has_own_notes || release.has_own media`; `release.has_own_contribution ? styles.releaseRow : styles.releaseRowMuted` -> da alle sichtbaren Folgen jetzt zugeordnet sind, wird die Muted-Unterscheidung durch Status-Badge ersetzt
- Leerzustand (Zeile 287-289): ein generischer Text `"Keine Release-Versionen fuer diesen Filter."` -> wird durch modus-spezifische Texte ersetzt
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Filterlogik, Sortierung und Modus-Umbau (Alle/Offen/Erledigt)</name>
  <files>frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.tsx, frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.test.tsx</files>
  <behavior>
    - Test: `filterReleases` (oder Page-Verhalten) schliesst eine Folge mit `has_own_contribution=false` in JEDEM der drei Modi ('all', 'open', 'done') aus.
    - Test: Modus `'open'` zeigt nur zugeordnete Folgen mit `has_own_notes=false && has_own_media=false`.
    - Test: Modus `'done'` zeigt nur zugeordnete Folgen mit `has_own_notes=true` ODER `has_own_media=true`.
    - Test: Modus `'all'` zeigt beide Gruppen, offene Folgen (nicht erledigt) stehen VOR erledigten Folgen in der gerenderten Reihenfolge; innerhalb jeder Gruppe bleibt die Eingabereihenfolge (Backend-Sortierung nach `episode_sort_index`) stabil erhalten.
    - Test: Die Folgen-Nummer-Suche filtert in allen drei Modi (nicht nur 'all').
  </behavior>
  <action>
    Aendere `ReleaseFilterMode` von `'mine' | 'all'` auf `'all' | 'open' | 'done'`.

    Baue `filterReleases` um: zuerst `assigned = releases.filter(r =&gt; r.has_own_contribution)`. Definiere `isDone(release) = release.has_own_notes || release.has_own_media`. Filtere je nach `mode`: `'open'` -&gt; `assigned.filter(r =&gt; !isDone(r))`; `'done'` -&gt; `assigned.filter(r =&gt; isDone(r))`; `'all'` -&gt; `assigned` komplett, aber sortiert mit offenen zuerst (stabiler Sort: z.B. `[...assigned].sort((a, b) => Number(isDone(a)) - Number(isDone(b)))`, JS-`sort` ist stabil und erhaelt die relative Reihenfolge innerhalb gleicher Gruppen). Wende die Episoden-Suche (bestehende `query`-Logik) danach unabhaengig vom Modus auf das gefilterte Ergebnis an (Suche entfernt sich aus dem `mode !== 'all'`-Gate).

    Passe den `mode`-State-Default in `MyProjectDetailPage` von `'mine'` auf `'all'` an. Entferne keine bestehenden Funktionen ohne Ersatz; `roleText`-Helper wurde bereits in einer frueheren Aufgabe entfernt und darf NICHT wieder eingefuehrt werden.

    Aktualisiere `page.test.tsx`: bestehende Tests, die sich auf `mode: 'mine'`/`'all'`-Button-Labels beziehen ("Nur meine Beitraege"), muessen auf die neuen Labels/Modi ("Alle", "Offen", "Erledigt") umgestellt werden (Task 2 fuehrt die Labels final ein, aber die Testerwartungen fuer Filterverhalten gehoeren logisch hierher). Ergaenze `has_own_contribution`/`has_own_notes`/`has_own_media`-Kombinationen in `makeRelease`-Aufrufen fuer jeden Testfall aus dem Behavior-Abschnitt.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/me/projects/[animeId]/group/[fansubGroupId]/page.test.tsx</automated>
  </verify>
  <done>Alle Filter-/Sortier-Tests aus dem Behavior-Abschnitt sind gruen; `filterReleases` liefert fuer 'open'/'done'/'all' korrekte, has_own_contribution-gefilterte Ergebnisse mit offene-zuerst-Sortierung im 'all'-Modus.</done>
</task>

<task type="auto">
  <name>Task 2: UI-Umbau — Segmented Control, Status-Badge, Header-Beschreibung, Leerzustaende</name>
  <files>frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.tsx, frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/project.module.css, frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.test.tsx</files>
  <action>
    Importiere `Badge` zusaetzlich aus `@/components/ui` (Barrel-Export vorhanden, kein neuer Pfad noetig).

    Segmented Control: ersetze die zwei Buttons ("Nur meine Beitraege"/"Alle") durch drei Buttons fuer die Modi `'all'`, `'open'`, `'done'` mit Labels "Alle", "Offen", "Erledigt" (korrekte Umlaute pruefen, hier keine noetig). Jeder Klick setzt `mode` und `setVisibleCount(20)` wie bisher.

    Suchfeld: entferne das `mode === 'all'`-Gate um das Suchfeld-Markup, sodass die Folgen-Nummer-Suche in allen drei Modi sichtbar und nutzbar ist.

    Release-Zeile: da nach Task 1 ausschliesslich zugeordnete Folgen (`has_own_contribution === true`) sichtbar sind, entferne die `releaseRow`/`releaseRowMuted`-Unterscheidung anhand `has_own_contribution` (immer `styles.releaseRow`, da der Muted-Zustand jetzt keine Bedeutung mehr hat) und ergaenze stattdessen einen `Badge`-Aufruf pro Zeile: `variant="success"` mit Text "Erledigt" wenn `isDone(release)`, sonst `variant="warning"` mit Text "Offen". Platziere den Badge sinnvoll in `styles.releaseMain` (z.B. neben oder unter `releaseLabel`). Der bestehende "Notizen & Medien"-Button bleibt unveraendert erhalten (weiterhin `hasOwnArtifacts` fuer `primary`/`secondary`-Variante); da alle sichtbaren Folgen zugeordnet sind, entfaellt die bisherige `release.has_own_contribution ? (...) : null`-Bedingung um den Button — der Button wird immer gerendert.

    SectionHeader-Beschreibung: berechne `assignedCount` (Anzahl zugeordneter Folgen vor Modus-Filterung, aber nach `has_own_contribution`-Filter), `openCount` und `doneCount` daraus. Ersetze die description durch einen Text im Format `"{openCount} offen · {doneCount} erledigt"` (Mittelpunkt-Zeichen `·` wie im Codebase ueblich, siehe `releaseLabel`-Helper). Bei Suche/weniger sichtbaren Eintraegen bleibt dies eine Gesamtzaehlung ueber alle zugeordneten Folgen (nicht nur die aktuell sichtbaren `visibleReleases`), da sie einen Ueberblick unabhaengig vom aktiven Filter geben soll.

    Leerzustaende (ersetze den einzelnen `styles.emptyText`-Absatz durch eine Fallunterscheidung):
    - Wenn `assignedCount === 0` (keine zugeordneten Folgen ueberhaupt): "Du bist noch keiner Folge in diesem Projekt zugeordnet."
    - Wenn `mode === 'open'` und die gefilterte (Such-)Liste leer ist, aber `assignedCount > 0`: "Alle deine Folgen sind erledigt."
    - Wenn `mode === 'done'` und die gefilterte (Such-)Liste leer ist: "Noch keine Folge erledigt."
    - Wenn eine aktive Episoden-Suche (`episodeQuery.trim() !== ''`) zu einem leeren Ergebnis fuehrt (in jedem Modus): bestehenden generischen Suchtext beibehalten, z.B. "Keine Release-Versionen fuer diesen Filter." (bestehendes Verhalten fuer den Such-Leerfall).
    Reihenfolge der Pruefung: zuerst `assignedCount === 0`, dann aktive Suche ohne Treffer, dann modus-spezifischer Text, sonst (Default-Fallback) bestehender generischer Text.

    CSS: pruefe `project.module.css` auf noetige Anpassungen fuer die Badge-Platzierung in `.releaseMain` (z.B. `display: flex; align-items: center; gap: 6px;` fuer eine Zeile, die Titel+Badge nebeneinander zeigt, oder Badge in eigener Zeile — entscheide nach bestehendem Grid-Layout von `.releaseMain`). Entferne `.releaseRowMuted`-Nutzung aus page.tsx, behalte die CSS-Klasse aber im Modul (kein toter Code-Zwang, geringes Risiko, falls anderswo referenziert — pruefe per Grep, ob `releaseRowMuted` noch irgendwo sonst importiert wird; falls nicht, kann die Klasse aus dem CSS-Modul entfernt werden).

    Halte `page.tsx` unter 450 Zeilen (aktuell ~305; neue Badge-Logik + Leerzustaende bleiben klar darunter).

    Aktualisiere `page.test.tsx` um: Test fuer Badge-Text "Offen"/"Erledigt" je Release-Zeile; Test dass eine nicht-zugeordnete Folge in keinem der drei Modi erscheint (`queryByText`/`queryByRole` fuer deren Label); Test fuer alle drei Leerzustands-Texte (keine Zuordnung, 'open' leer, 'done' leer); Test dass die SectionHeader-Beschreibung `"{openCount} offen · {doneCount} erledigt"` zeigt (nicht mehr die alte "X von Y Versionen sichtbar"-Form); Test dass Suche auch im 'open'/'done'-Modus sichtbar/nutzbar ist.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/me/projects/[animeId]/group/[fansubGroupId]/page.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Segmented Control zeigt Alle/Offen/Erledigt; jede sichtbare Folge hat einen Badge (Offen/Erledigt); Suche funktioniert in allen Modi; SectionHeader zeigt offen/erledigt-Zaehlung statt Gesamtzahl aller Versionen; alle drei modus-spezifischen Leerzustaende sind implementiert und getestet; tsc kompiliert fehlerfrei; page.tsx bleibt unter 450 Zeilen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend API -> Frontend | `getMyProjectDetail` liefert `has_own_contribution`/`has_own_notes`/`has_own_media` bereits serverseitig berechnet; Frontend filtert/sortiert nur, erstellt keine neue Autorisierungslogik. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-260707-hx0-01 | Information Disclosure | page.tsx Filterlogik | accept | Reine Anzeige-Filterung bestehender, bereits vom Backend autorisierter Daten (kein neuer Datenzugriff, keine neuen Felder); Backend-Autorisierung fuer `getMyProjectDetail` bleibt unveraendert und ist aus Scope dieser Aenderung. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/app/me/projects/[animeId]/group/[fansubGroupId]/page.test.tsx` — alle Tests gruen.
- `cd frontend && npx tsc --noEmit` — keine Typfehler.
- Manuelle Zeilenzahl-Pruefung: `page.tsx` bleibt unter 450 Zeilen.
- Kein Backend-Change, kein Docker-Rebuild durch den Executor (nur `docker restart team4sv30-frontend` durch den Orchestrator nach Abschluss, da Turbopack-HMR nicht zuverlaessig greift).
</verification>

<success_criteria>
- Nicht-zugeordnete Folgen erscheinen in keinem Filter-Modus mehr.
- Drei Filter-Modi (Alle/Offen/Erledigt) ersetzen den bisherigen Mine/All-Umschalter.
- Offene Folgen stehen im "Alle"-Modus vor erledigten Folgen.
- Jede sichtbare Folge zeigt einen Status-Badge aus `@/components/ui`.
- Folgen-Nummer-Suche funktioniert in allen drei Modi.
- SectionHeader-Beschreibung bezieht sich auf zugeordnete Folgen (offen/erledigt), nicht mehr auf die Gesamtzahl aller Release-Versionen.
- Drei spezifische Leerzustaende (keine Zuordnung / Offen leer / Erledigt leer) sind korrekt deutsch mit Umlauten formuliert.
- Alle Tests gruen, tsc fehlerfrei, `page.tsx` unter 450 Zeilen.
</success_criteria>

<output>
Create `.planning/quick/260707-hx0-meine-projekt-detailseite-als-to-do-work/260707-hx0-SUMMARY.md` when done
</output>
