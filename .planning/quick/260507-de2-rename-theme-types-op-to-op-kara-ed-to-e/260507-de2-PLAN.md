---
phase: quick-260507-de2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/0050_normalize_theme_types.up.sql
  - database/migrations/0058_rename_theme_types_kara.up.sql
  - database/migrations/0058_rename_theme_types_kara.down.sql
  - frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseSegments.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "DB theme_types-Tabelle hat name='OP Kara' fuer id=1, 'ED Kara' fuer id=3, 'Insert Kara' fuer id=5, 'Outro' bleibt unveraendert"
    - "Segment-Formular zeigt 'OP Kara', 'ED Kara', 'Insert Kara' als Dropdown-Optionen"
    - "Badge-Darstellung funktioniert weiterhin korrekt (substring-Logik auf 'OP', 'ED', 'INSERT' trifft unveraendert zu)"
  artifacts:
    - path: "database/migrations/0058_rename_theme_types_kara.up.sql"
      provides: "Live-DB-Migration die OP->OP Kara, ED->ED Kara, Insert->Insert Kara umbenennt"
    - path: "database/migrations/0058_rename_theme_types_kara.down.sql"
      provides: "Rollback-Migration"
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseSegments.ts"
      provides: "Aktualisierte Label-Strings fuer Segment-Dropdown"
  key_links:
    - from: "database/migrations/0058_rename_theme_types_kara.up.sql"
      to: "theme_types table"
      via: "UPDATE SET name"
      pattern: "UPDATE theme_types SET name"
    - from: "useReleaseSegments.ts"
      to: "GenericSegmentThemeOption.label"
      via: "label string literals"
      pattern: "Ending \\(ED\\)|Insert Song|Opening \\(OP\\)"
---

<objective>
Theme-Typen OP, ED und Insert in Datenbank und Frontend auf "OP Kara", "ED Kara" und "Insert Kara" umbenennen. Outro bleibt unveraendert.

Purpose: Korrekte Bezeichnung der Kara-Typen im gesamten Admin-Workflow — sowohl in der DB als auch in den UI-Labels.
Output: Migration 0058 (up/down), gepatchte Migration 0050, und angepasste Label-Strings in useReleaseSegments.ts.
</objective>

<execution_context>
@/home/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260507-de2-rename-theme-types-op-to-op-kara-ed-to-e/260507-de2-PLAN.md

Technische Ausgangslage:
- `database/migrations/0050_normalize_theme_types.up.sql` setzt aktuell `name='OP'` (id=1), `name='ED'` (id=3), `name='Insert'` (id=5), `name='Outro'` (id=6)
- `useReleaseSegments.ts` nutzt Labels `'Opening (OP)'`, `'Ending (ED)'`, `'Insert Song'` fuer den Segment-Dropdown
- Badge-Helper in `SegmenteTab.helpers.tsx` verwendet `.includes('OP')`, `.includes('ED')`, `.includes('INSERT')` — diese Substring-Checks funktionieren nach Umbenennung weiterhin korrekt, weil 'OP Kara'.toUpperCase().includes('OP') === true
- `GroupAssetsExperience.tsx` enthaelt `ending: 'Ending'`, `insert: 'Insert'` als Media-Type-Labels (nicht theme_type_name) — KEIN Change noetig
- Backend hat keine hardkodierten theme_type-Name-Strings — KEIN Change noetig
- Naechste freie Migrationsnummer: 0058
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0058 erstellen und 0050 aktualisieren</name>
  <files>
    database/migrations/0058_rename_theme_types_kara.up.sql
    database/migrations/0058_rename_theme_types_kara.down.sql
    database/migrations/0050_normalize_theme_types.up.sql
  </files>
  <action>
Drei Aenderungen:

**1. `database/migrations/0058_rename_theme_types_kara.up.sql` neu anlegen:**
```sql
-- Umbenennung der Theme-Typen: OP → OP Kara, ED → ED Kara, Insert → Insert Kara
-- Outro bleibt unveraendert.
UPDATE theme_types SET name = 'OP Kara'     WHERE id = 1;
UPDATE theme_types SET name = 'ED Kara'     WHERE id = 3;
UPDATE theme_types SET name = 'Insert Kara' WHERE id = 5;
```

**2. `database/migrations/0058_rename_theme_types_kara.down.sql` neu anlegen:**
```sql
-- Rollback: OP Kara → OP, ED Kara → ED, Insert Kara → Insert
UPDATE theme_types SET name = 'OP'     WHERE id = 1;
UPDATE theme_types SET name = 'ED'     WHERE id = 3;
UPDATE theme_types SET name = 'Insert' WHERE id = 5;
```

**3. `database/migrations/0050_normalize_theme_types.up.sql` anpassen:**
Die letzten drei UPDATE-Zeilen aendern:
- `UPDATE theme_types SET name = 'OP' WHERE id = 1;`  →  `UPDATE theme_types SET name = 'OP Kara' WHERE id = 1;`
- `UPDATE theme_types SET name = 'ED' WHERE id = 3;`  →  `UPDATE theme_types SET name = 'ED Kara' WHERE id = 3;`
- `UPDATE theme_types SET name = 'Insert' WHERE id = 5;`  →  `UPDATE theme_types SET name = 'Insert Kara' WHERE id = 5;`
- Die Zeile fuer `Outro` (id=6) bleibt unveraendert: `UPDATE theme_types SET name = 'Outro' WHERE id = 6;`

Migration 0050 aendern ist korrekt, da sie die Datenquelle fuer Fresh-Installs ist. Migration 0058 adressiert bestehende Live-DB-Eintraege.
  </action>
  <verify>
    <automated>grep -c "OP Kara\|ED Kara\|Insert Kara" /c/Users/admin/Documents/Team4s/database/migrations/0058_rename_theme_types_kara.up.sql && grep "OP Kara" /c/Users/admin/Documents/Team4s/database/migrations/0050_normalize_theme_types.up.sql</automated>
  </verify>
  <done>0058 up/down existieren mit korrekten UPDATE-Statements; 0050 zeigt 'OP Kara', 'ED Kara', 'Insert Kara'; Outro bleibt 'Outro'</done>
</task>

<task type="auto">
  <name>Task 2: Frontend-Labels in useReleaseSegments.ts aktualisieren</name>
  <files>
    frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseSegments.ts
  </files>
  <action>
In `useReleaseSegments.ts` die drei label-Strings in der `byKind.set(kind, { label: ... })`-Logik (ca. Zeile 63-70) aendern:

- `'Opening (OP)'` → `'OP Kara'`
- `'Ending (ED)'` → `'ED Kara'`
- `'Insert Song'` → `'Insert Kara'`
- `'Outro'` bleibt unveraendert

Nur die String-Literale aendern, keinerlei Logik-Aenderungen. Die kind-Variablen ('op', 'ed', 'insert', 'outro') und die Vergleiche bleiben unveraendert.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -c "error" || true</automated>
  </verify>
  <done>'OP Kara', 'ED Kara', 'Insert Kara' erscheinen als label-Werte in useReleaseSegments.ts; TypeScript-Kompilierung ohne neue Fehler; 'Outro' unveraendert</done>
</task>

</tasks>

<verification>
Nach beiden Tasks:
1. `grep "OP Kara\|ED Kara\|Insert Kara" database/migrations/0058_rename_theme_types_kara.up.sql` — zeigt 3 Treffer
2. `grep "Outro" database/migrations/0058_rename_theme_types_kara.up.sql` — kein Treffer (Outro wird nicht angefasst)
3. `grep "OP Kara\|ED Kara\|Insert Kara" frontend/src/app/admin/episode-versions/\[versionId\]/edit/useReleaseSegments.ts` — zeigt 3 Treffer
4. `grep "'OP'\|'ED'\|'Insert Song'\|'Ending (ED)'\|'Opening (OP)'" frontend/src/app/admin/episode-versions/\[versionId\]/edit/useReleaseSegments.ts` — kein Treffer
5. TypeScript-Build ohne neue Fehler
</verification>

<success_criteria>
- Migration 0058 up/down existieren und enthalten korrekte UPDATE-Statements fuer id=1,3,5
- Migration 0050 zeigt die neuen Namen (fuer Fresh-Installs)
- useReleaseSegments.ts zeigt 'OP Kara', 'ED Kara', 'Insert Kara' als Dropdown-Labels
- Badge-Helper-Tests in SegmenteTab.test.tsx bleiben unveraendert gruen (substring-Logik nicht betroffen)
- 'Outro' an keiner Stelle geaendert
</success_criteria>

<output>
Nach Abschluss: `.planning/quick/260507-de2-rename-theme-types-op-to-op-kara-ed-to-e/260507-de2-SUMMARY.md` erstellen mit ausgefuehrten Aenderungen und Commit-Hash.
</output>
