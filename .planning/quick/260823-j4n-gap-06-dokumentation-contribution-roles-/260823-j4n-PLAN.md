---
phase: quick-260823-j4n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/permissions/permissions.go
  - backend/internal/permissions/effective_rights_integration_test.go
  - .planning/phases/137-central-effective-rights-resolver-overrides/137-CONTEXT.md
  - .planning/phases/137-central-effective-rights-resolver-overrides/137-UAT.md
  - .planning/phases/137-central-effective-rights-resolver-overrides/137-12-SUMMARY.md
  - DECISIONS.md
autonomous: true
requirements: [GAP-06]
must_haves:
  truths:
    - "Jeder Leser von 137-CONTEXT.md, 137-UAT.md oder 137-12-SUMMARY.md sieht sofort, dass GAP-06 als Fall B entschieden ist -- Contribution Roles sind eine bewusste, override-blinde Ausnahme von D01 -- statt als offene Fall-C-Frage"
    - "Der Code-Kommentar direkt vor dem Contribution-Role-Fallback in CanForReleaseVersion() erklaert die Fall-B-Entscheidung mit Datum, Quelle (137-CONTEXT.md) und Testverweis, statt 'DECISION REQUIRED' zu behaupten"
    - "DECISIONS.md enthaelt einen datierten Eintrag (2026-08-23), der die Fall-B-Entscheidung fuer GAP-06 nachvollziehbar dokumentiert"
    - "go test ./internal/permissions/... ist weiterhin gruen; kein Testverhalten, keine Assertion und keine Kontrollfluss-Zeile wurde veraendert"
  artifacts:
    - path: "backend/internal/permissions/permissions.go"
      provides: "Kommentar vor Step 3 (ListActorContributionRolesForVersion) in CanForReleaseVersion() beschreibt Fall B als entschiedene, benannte Ausnahme statt offener Frage"
    - path: ".planning/phases/137-central-effective-rights-resolver-overrides/137-CONTEXT.md"
      provides: "D01-Abschnitt referenziert die Contribution-Role-Ausnahme als explizit benannten Fall B, damit kuenftige Audits GAP-06 nicht erneut als offen einstufen"
    - path: "DECISIONS.md"
      provides: "Neuer datierter Eintrag 2026-08-23 zu GAP-06 / Fall B"
  key_links:
    - from: "backend/internal/permissions/permissions.go (Step 3 comment)"
      to: ".planning/phases/137-central-effective-rights-resolver-overrides/137-CONTEXT.md (D01 exception)"
      via: "Kommentarverweis auf 137-CONTEXT.md und Entscheidungsdatum 2026-08-23"
      pattern: "137-CONTEXT\\.md"
    - from: "backend/internal/permissions/effective_rights_integration_test.go"
      to: "backend/internal/permissions/permissions.go"
      via: "Testname TestIntegrationCanForReleaseVersionContributionRoleFallbackNotBlockedByUserDeny bleibt Ankerpunkt fuer die dokumentierte Fall-B-Ausnahme"
      pattern: "TestIntegrationCanForReleaseVersionContributionRoleFallbackNotBlockedByUserDeny"
---

<objective>
GAP-06 aus `137-UAT.md` abschliessend dokumentieren. Der Nutzer hat am 2026-08-23 entschieden: Fall B gilt -- Contribution Roles bleiben eine bewusste, override-blinde Domaene. Ein gespeichertes `user_deny` schlaegt den Contribution-Role-Fallback in `CanForReleaseVersion()` Schritt 3 NICHT. `137-12-SUMMARY.md` hatte dies faelschlich als Fall C (offene Frage, "DECISION REQUIRED") dispositioniert -- diese Einstufung ist jetzt durch eine explizite Nutzerentscheidung ueberholt.

Diese Aufgabe ist AUSSCHLIESSLICH Dokumentation: keine Verhaltensaenderung, keine Migration, keine neue Capability, keine Aenderung an Testassertions oder Kontrollfluss. Nur Kommentare, Markdown-Dokumente und ein DECISIONS.md-Eintrag werden angepasst.

Purpose: Verhindert, dass kuenftige Audits/Executoren GAP-06 erneut als ungeklaert aufgreifen und ungewollt versuchen, den Contribution-Role-Fallback an D01-Precedence zu binden. Schafft eine einzige, konsistente Quelle der Wahrheit (Code-Kommentar + CONTEXT.md + DECISIONS.md + UAT/SUMMARY) fuer die Fall-B-Entscheidung.

Output: Aktualisierte Kommentare in `permissions.go` und dem Regressionstest, ein neuer benannter Ausnahme-Absatz in `137-CONTEXT.md` unter D01, ein GAP-06-Statusupdate in `137-UAT.md` und `137-12-SUMMARY.md`, ein neuer DECISIONS.md-Eintrag, sowie ein gruener `go test ./internal/permissions/...`-Lauf als Beleg, dass sich nichts am Verhalten geaendert hat.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/137-central-effective-rights-resolver-overrides/137-UAT.md
@.planning/phases/137-central-effective-rights-resolver-overrides/137-12-SUMMARY.md
@.planning/phases/137-central-effective-rights-resolver-overrides/137-CONTEXT.md
@backend/internal/permissions/permissions.go
@backend/internal/permissions/effective_rights_integration_test.go
@DECISIONS.md
</context>

<interfaces>
<!-- Exact locations to edit, extracted so the executor does not need to re-search. -->

**backend/internal/permissions/permissions.go, `CanForReleaseVersion()`, Step 3 comment block (currently lines ~619-630):**
Current text frames GAP-06 as "Fall C ... a genuinely unresolved design question" and points to `137-12-SUMMARY.md`'s "DECISION REQUIRED — Contribution Role vs User Deny" section. This must become: a named, deliberate exception, decided by the user on 2026-08-23, citing `137-CONTEXT.md`'s D01 exception paragraph and the regression test `TestIntegrationCanForReleaseVersionContributionRoleFallbackNotBlockedByUserDeny`. The actual code line right after the comment (`roleCodes, err := s.resolver.ListActorContributionRolesForVersion(ctx, actor.AppUserID, releaseVersionID)`) and everything below it in the function body must NOT change.

**backend/internal/permissions/permissions.go, `canForReleaseVersionGroupRole` doc comment (currently lines ~661-669):**
Contains the same stale framing ("Step 3 (contribution-role fallback) remains a separate, unchanged domain per the plan's minimal-edit scope"). This phrase does not mention Fall C directly but should be checked and, if it under-explains the now-decided exception, given a one-line clarifying update pointing at the Step-3 comment above rather than duplicating the full rationale. Do not rewrite this comment extensively -- it describes Step 2, not Step 3.

**backend/internal/permissions/effective_rights_integration_test.go, doc comment above `TestIntegrationCanForReleaseVersionContributionRoleFallbackNotBlockedByUserDeny` (currently lines ~227-243):**
Currently says "this is Fall C (an unresolved design question) ... Per 137-UAT.md's explicit instruction not to resolve this unilaterally, this test proves and locks TODAY'S ACTUAL, UNCHANGED behavior" and cites "DECISION REQUIRED — Contribution Role vs User Deny" in `137-12-SUMMARY.md`. Update to state this is now Fall B, a deliberate decided exception (2026-08-23), still locking the same behavior (a stored `user_deny` does not block Step 3) -- now BY DESIGN rather than pending decision. The test body (lines 244-263: resolver setup, assertions) must NOT change at all.

**.planning/phases/137-central-effective-rights-resolver-overrides/137-CONTEXT.md, `## D01 — Permission precedence` section (lines 101-141):**
Ends with: "The Review Delegation model itself remains specialized and is not migrated into the generic override table." Add a new named exception paragraph after this section (or as a clearly marked subsection immediately following D01, e.g. `### D01 exception — Contribution Roles (entschieden 2026-08-23)`), stating explicitly: Contribution Roles (the `CanForReleaseVersion()` Step 3 fallback via `ListActorContributionRolesForVersion`) are an intentionally standalone, override-blind domain. A stored `user_deny` from D01's precedence chain does NOT defeat this fallback. Reference GAP-06 (137-UAT.md) and the regression test name. This closes the ambiguity 137-12-SUMMARY.md left open.

**.planning/phases/137-central-effective-rights-resolver-overrides/137-UAT.md, `## GAP-06` section (lines 254-314, specifically the "Fall B" and "Fall C" subsections around lines 292-314):**
Add a clearly marked resolution note (e.g. `### Entscheidung (2026-08-23)`) directly after the Fall A/B/C analysis, stating Fall B was chosen by the user, without deleting the original Fall A/B/C analysis text (it stays as historical record of the investigation).

**.planning/phases/137-central-effective-rights-resolver-overrides/137-12-SUMMARY.md, `## DECISION REQUIRED — Contribution Role vs User Deny` section (lines 84-94):**
Add a resolution note directly after this section (e.g. `### Resolved (2026-08-23)`) stating the human decision was made: Fall B, contribution roles remain override-blind by design. Do not delete or rewrite the original "DECISION REQUIRED" analysis -- it documents the investigation that led to the decision.

**DECISIONS.md:**
Append a new dated entry at the end of the file (after the last existing `## 2026-08-10 ...` entry), following the file's established format (`## YYYY-MM-DD - Title`, then `### Decision`, `### Context`, `### Why This Won`, `### Consequences`, `### Follow-ups Required`). Title should reference GAP-06 and Phase 137.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update code comments in permissions.go and the regression test to reflect the Fall B decision</name>
  <files>backend/internal/permissions/permissions.go, backend/internal/permissions/effective_rights_integration_test.go</files>
  <action>
  In permissions.go, rewrite the Step-3 comment block inside CanForReleaseVersion() (directly above the `roleCodes, err := s.resolver.ListActorContributionRolesForVersion(...)` line). Replace the "Fall C / genuinely unresolved design question / DECISION REQUIRED" framing with a statement that this is a named, deliberate exception decided by the user on 2026-08-23: Contribution Roles are an intentionally standalone, override-blind domain per 137-CONTEXT.md's D01 exception paragraph; a stored user_deny from Step 2 does NOT block this fallback, by design. Reference 137-CONTEXT.md and the regression test TestIntegrationCanForReleaseVersionContributionRoleFallbackNotBlockedByUserDeny by name. Keep the factual description of what the code does (roleCodes lookup, roleAllows loop) accurate and unchanged in meaning.

  Check the nearby canForReleaseVersionGroupRole doc comment (~line 665) for the stale "remains a separate, unchanged domain per the plan's minimal-edit scope" phrase; if present, adjust the wording minimally so it does not contradict the now-decided exception (e.g. "remains, by decided exception, a separate override-blind domain -- see the Step-3 comment above"), without expanding this comment into a full re-explanation.

  In effective_rights_integration_test.go, rewrite the doc comment above TestIntegrationCanForReleaseVersionContributionRoleFallbackNotBlockedByUserDeny: replace "Fall C (an unresolved design question)" and the "DECISION REQUIRED" reference with "Fall B (a decided, named exception, 2026-08-23)". Keep the explanation of why this test differs from the test above it (populates contributionRoles vs. leaves it empty) unchanged. Do NOT touch the test function body (resolver setup, service call, assertions) at all -- only the comment block above the function signature.

  Do not modify any other code, imports, function signatures, or control flow in either file.
  </action>
  <verify>
    <automated>cd backend && go build ./internal/permissions/... && go vet ./internal/permissions/... && git diff internal/permissions/permissions.go internal/permissions/effective_rights_integration_test.go | grep -E '^\+' | grep -v '^\+\+\+' | grep -vE '^\+[[:space:]]*//' | grep -v '^\+$' | wc -l | grep -qx 0</automated>
  </verify>
  <done>Both files build and vet cleanly; the only added lines in the diff are comment lines (`//`) or blank lines -- no non-comment code line was added or changed, and no existing non-comment line was removed (git diff shows zero control-flow/test-assertion changes).</done>
</task>

<task type="auto">
  <name>Task 2: Update planning docs (137-CONTEXT.md, 137-UAT.md, 137-12-SUMMARY.md) and add a DECISIONS.md entry, then verify tests still pass</name>
  <files>.planning/phases/137-central-effective-rights-resolver-overrides/137-CONTEXT.md, .planning/phases/137-central-effective-rights-resolver-overrides/137-UAT.md, .planning/phases/137-central-effective-rights-resolver-overrides/137-12-SUMMARY.md, DECISIONS.md</files>
  <action>
  In 137-CONTEXT.md, add a new subsection directly after the existing `## D01 — Permission precedence` content (after its closing line about Review Delegation, before `## D02`), named e.g. `### D01 exception — Contribution Roles (entschieden 2026-08-23)`. State explicitly, in German matching the surrounding document's language and using correct Umlaute: Contribution Roles (CanForReleaseVersion() Step 3, ListActorContributionRolesForVersion) sind eine bewusste, override-blinde Ausnahme von D01's Precedence-Kette. Ein gespeichertes user_deny schlaegt diesen Fallback NICHT. Reference GAP-06 (137-UAT.md) and the regression test TestIntegrationCanForReleaseVersionContributionRoleFallbackNotBlockedByUserDeny by name, so future audits treat this as a named, closed exception rather than an unresolved gap.

  In 137-UAT.md's GAP-06 section, insert a new subsection immediately after the existing Fall A/B/C analysis (after Fall C's closing text, before the "Nicht im Scope" section or wherever the GAP-06 section ends), e.g. `### Entscheidung (2026-08-23)`. State: Nutzer hat Fall B gewaehlt -- Contribution Roles bleiben bewusst eigenstaendig und override-blind. Reference the updated 137-CONTEXT.md D01 exception and the code comment location. Do not delete or alter the original Fall A/B/C analysis text above it -- it remains as the historical investigation record.

  In 137-12-SUMMARY.md, insert a new subsection immediately after the existing `## DECISION REQUIRED — Contribution Role vs User Deny` section content, e.g. `### Resolved (2026-08-23) — Fall B`. State the human decision was made: contribution roles remain an intentionally separate, override-blind domain (option b from the original decision list), now recorded as a named exception in 137-CONTEXT.md's D01 section and in DECISIONS.md. Do not delete or rewrite the original "DECISION REQUIRED" text -- it stays as the record of the investigation that produced the decision.

  In DECISIONS.md, append one new entry at the end of the file following the established format (`## 2026-08-23 - <Title referencing GAP-06 / Phase 137 / Contribution Roles override-blind>`, then `### Decision`, `### Context`, `### Why This Won`, `### Consequences`, `### Follow-ups Required`). Content: Decision = Contribution Roles bleiben eine bewusst eigenstaendige, override-blinde Domaene (Fall B); ein gespeichertes user_deny schlaegt den Contribution-Role-Fallback in CanForReleaseVersion() Schritt 3 nicht. Context = GAP-06 aus 137-UAT.md, zuvor durch 137-12-SUMMARY.md als Fall C (offene Frage) dispositioniert. Why This Won = explizite Nutzerentscheidung am 2026-08-23, reine Dokumentationsklarstellung ohne Verhaltensaenderung noetig, da bestehendes Verhalten bereits der Fall-B-Erwartung entspricht. Consequences = 137-CONTEXT.md D01 traegt jetzt eine benannte Ausnahme; permissions.go-Kommentar und Regressionstest-Kommentar spiegeln die Entscheidung; GAP-06 gilt als final geschlossen. Follow-ups Required = keine (dokumentationsseitig abgeschlossen).

  After all four files are edited, run the backend permissions test suite to confirm zero behavior change.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/permissions/...</automated>
  </verify>
  <done>137-CONTEXT.md has a new D01 exception subsection naming the Contribution-Role/user_deny exception; 137-UAT.md's GAP-06 section has a dated resolution note (Fall B) without deleting the original analysis; 137-12-SUMMARY.md's DECISION REQUIRED section has a dated "Resolved" note without deleting the original text; DECISIONS.md has one new dated entry (2026-08-23) documenting the GAP-06/Fall B decision; `go test ./internal/permissions/...` passes with the same test count/behavior as before (no assertions changed).</done>
</task>

<task type="auto">
  <name>Task 3: Commit all documentation and comment changes</name>
  <files>backend/internal/permissions/permissions.go, backend/internal/permissions/effective_rights_integration_test.go, .planning/phases/137-central-effective-rights-resolver-overrides/137-CONTEXT.md, .planning/phases/137-central-effective-rights-resolver-overrides/137-UAT.md, .planning/phases/137-central-effective-rights-resolver-overrides/137-12-SUMMARY.md, DECISIONS.md</files>
  <action>
  Stage exactly the six files listed above (no other files) and create a single atomic commit. Commit message subject should reference GAP-06 and the Fall B decision, e.g. `docs(137): close GAP-06 as Fall B - contribution roles stay override-blind`. Body should note: user decision 2026-08-23, no behavior change, comment/doc updates only, cites the regression test name as the locked proof of current (and now permanently sanctioned) behavior.
  </action>
  <verify>
    <automated>git -C /home/d1sk/team4s status --short backend/internal/permissions/permissions.go backend/internal/permissions/effective_rights_integration_test.go .planning/phases/137-central-effective-rights-resolver-overrides/137-CONTEXT.md .planning/phases/137-central-effective-rights-resolver-overrides/137-UAT.md .planning/phases/137-central-effective-rights-resolver-overrides/137-12-SUMMARY.md DECISIONS.md | wc -l | grep -qx 0</automated>
  </verify>
  <done>A single new commit exists containing exactly these six files, with a message referencing GAP-06 and the Fall B decision; `git status --short` for these six paths is empty afterward.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan is documentation/comment-only. No new input source, no new trust boundary, no runtime code path is added or changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick260823-01 | Repudiation | GAP-06 decision provenance | accept | The decision is documented in four places (permissions.go comment, 137-CONTEXT.md, 137-UAT.md, DECISIONS.md) with a consistent date (2026-08-23), so the decision's origin cannot be silently disputed later; no code executes differently based on these docs. |
</threat_model>

<verification>
1. `cd backend && go build ./... && go vet ./internal/permissions/...` succeeds.
2. `cd backend && go test ./internal/permissions/...` passes with the same test count and outcomes as before this plan ran (no test added, none removed, none changed in behavior).
3. `git diff` on `backend/internal/permissions/permissions.go` and `backend/internal/permissions/effective_rights_integration_test.go` contains only comment-line (`//`) and blank-line changes -- zero non-comment diff lines.
4. `grep -n "Fall B" .planning/phases/137-central-effective-rights-resolver-overrides/137-CONTEXT.md .planning/phases/137-central-effective-rights-resolver-overrides/137-UAT.md .planning/phases/137-central-effective-rights-resolver-overrides/137-12-SUMMARY.md` returns at least one match in each file.
5. `grep -n "2026-08-23" DECISIONS.md` returns at least one match for the new entry.
6. A single git commit exists containing exactly the six files in `files_modified`.
</verification>

<success_criteria>
- GAP-06 is documented as resolved (Fall B) consistently across `permissions.go`, the regression test comment, `137-CONTEXT.md` (D01 exception), `137-UAT.md`, `137-12-SUMMARY.md`, and a new `DECISIONS.md` entry dated 2026-08-23.
- No production code control flow, no test assertion, and no database/migration changed.
- `go test ./internal/permissions/...` passes with identical behavior to before this plan.
- All six touched files are committed in a single atomic commit referencing GAP-06.
</success_criteria>

<output>
Create `.planning/quick/260823-j4n-gap-06-dokumentation-contribution-roles-/260823-j4n-SUMMARY.md` when done
</output>
