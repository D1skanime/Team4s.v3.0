---
phase: quick-260620-eaj
plan: 02
type: execute
wave: 2
depends_on: [260620-eaj-01]
files_modified:
  - frontend/src/components/contributions/ReportFormFehler.tsx
  - frontend/src/components/contributions/ReportFormStory.tsx
  - frontend/src/components/contributions/ReportFormMedia.tsx
  - frontend/src/components/contributions/RejectReasonModal.tsx
  - frontend/src/components/contributions/ReportModal.tsx
  - frontend/src/components/contributions/ProposalForm.tsx
autonomous: true
requirements: [eaj-scope]

must_haves:
  truths:
    - "ReportFormFehler, ReportFormStory, ReportFormMedia haben keine inline style= Attribute mehr außer legitimen Ausnahmen"
    - "RejectReasonModal Fehlermeldung verwendet kein inline style= mehr"
    - "ReportModal reviewHint-Paragraph entfernt oder auf max. 8 Wörter gekürzt"
    - "ProposalForm infoPanel-divs und warningPanel-div sind gekürzt oder entfernt"
    - "ProposalForm Scope-Button erklärender <span> ist gekürzt"
    - "Alle Änderungen nutzen @/components/ui Primitives wo verfügbar — kein neues natives Markup eingeführt"
    - "npx tsc --noEmit läuft ohne Fehler"
    - "ProposalForm.test + ReportModal.test + ContributionCard.test bleiben grün"
  artifacts:
    - path: frontend/src/components/contributions/ReportFormFehler.tsx
      provides: ReportFormFehler ohne inline style= auf form/p Elementen
    - path: frontend/src/components/contributions/ReportFormStory.tsx
      provides: ReportFormStory ohne inline style=
    - path: frontend/src/components/contributions/ReportFormMedia.tsx
      provides: ReportFormMedia ohne inline style= (außer legitimer file-input Ausnahme)
    - path: frontend/src/components/contributions/RejectReasonModal.tsx
      provides: RejectReasonModal mit CSS-Klasse statt inline style für Fehlertext
    - path: frontend/src/components/contributions/ReportModal.tsx
      provides: ReportModal ohne verbose reviewHint Paragraph
    - path: frontend/src/components/contributions/ProposalForm.tsx
      provides: ProposalForm mit gekürzten infoPanels und Scope-Button-Beschreibungen
  key_links:
    - from: frontend/src/components/contributions/ReportModal.tsx
      to: frontend/src/components/contributions/ReportFormFehler.tsx
      via: Component import
      pattern: "ReportFormFehler"
---

<objective>
Inline-Style-Migration und Copy-Verschlankung in den Report-Formularen, RejectReasonModal, ReportModal und ProposalForm.

Purpose: Inline style= Attribute durch CSS-Klassen ersetzen; erklärende Paragraphen kürzen oder entfernen; Primitive-Nutzung vervollständigen.
Output: 6 bereinigte Dateien. Kein neues natives Markup. Alle Tests grün.
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260620-eaj-member-contribution-ui-auf-globales-desi/260620-eaj-CONTEXT.md
@.planning/quick/260620-eaj-member-contribution-ui-auf-globales-desi/260620-eaj-01-SUMMARY.md

Verfügbare UI-Primitives in @/components/ui: Button, Badge, Card, FormField, Input, Select, Textarea, Modal, EmptyState, LoadingState, ErrorState, SectionHeader, PageHeader, Tabs, Drawer, YearPicker, Table, Pagination, ActionBar, Toolbar.
Kein Alert/InfoPanel/FileInput-Primitive vorhanden — div-Container mit CSS-Klassen sind dort erlaubt.
contributions.module.css hat 308 Zeilen — Klassen können dort ergänzt werden.

Entscheidungen (per CONTEXT.md):
- D-Copy: Erklärende Sätze raus, nur Labels/Aktionen behalten.
- D-Primitives: Volle Migration — kein handgebautes Markup für Primitiv-Typen.
- D-Umlaut: Korrekte Umlaute.
- D-Lines: ≤ 450 Zeilen pro Datei.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline styles in ReportFormFehler + ReportFormStory + ReportFormMedia → CSS-Klassen; erklärende Paragraphen kürzen</name>
  <files>
    frontend/src/components/contributions/ReportFormFehler.tsx
    frontend/src/components/contributions/ReportFormStory.tsx
    frontend/src/components/contributions/ReportFormMedia.tsx
    frontend/src/components/contributions/contributions.module.css
  </files>
  <action>
    Alle drei Formulare haben dasselbe Muster: `<form style={{ display: 'flex', flexDirection: 'column', gap: 16 }}>` und `<p role="alert" style={{ color: 'var(--button-danger-start)', fontSize: '0.875rem' }}>` und erklärende `<p style={{ fontSize: '0.875rem', color: 'var(--text-muted)' }}>` am Ende.

    **contributions.module.css:** Falls nicht vorhanden, folgende Klassen ergänzen:
    - `.reportForm { display: flex; flex-direction: column; gap: 16px; }` — für form-Container
    - `.fieldError { color: var(--button-danger-start); font-size: 0.875rem; }` — falls noch nicht vorhanden (prüfen, ggf. bereits definiert)
    - `.formNote { font-size: 0.875rem; color: var(--text-muted); }` — für untere Hinweis-Paragraphen

    **ReportFormFehler.tsx:**
    - `<form id="report-form-fehler" ... style={{ display: 'flex', flexDirection: 'column', gap: 16 }}>` → `className={styles.reportForm}` statt `style=`
    - `<p role="alert" style={{ color: 'var(--button-danger-start)', fontSize: '0.875rem' }}>` → `<p role="alert" className={styles.fieldError}>`
    - `<p style={{ fontSize: '0.875rem', color: 'var(--text-muted)' }}>Dein Vorschlag wird geprüft und kann bei Bedarf vom Team umgesetzt werden.</p>` → entfernen (erklärend, kein Mehrwert)

    **ReportFormStory.tsx:**
    - Dieselben inline styles wie oben ersetzen mit `styles.reportForm` / `styles.fieldError`
    - `<p style={{ fontSize: '0.875rem', color: 'var(--text-muted)' }}>Deine Story wird geprüft und kann nach Freigabe veröffentlicht werden.</p>` → entfernen

    **ReportFormMedia.tsx:**
    - `<form id="report-form-media" ... style={{ display: 'flex', flexDirection: 'column', gap: 16 }}>` → `className={styles.reportForm}`
    - `<p role="alert" style={{ color: 'var(--button-danger-start)', fontSize: '0.875rem' }}>` → `<p role="alert" className={styles.fieldError}>`
    - `<p style={{ fontSize: '0.875rem', color: 'var(--text-muted)' }}>Ausgewählt: ...</p>` → `<p className={styles.formNote}>Ausgewählt: ...</p>`
    - `<div role="status" style={{ fontSize: '0.875rem', color: 'var(--text-muted)' }}>` → `<div role="status" className={styles.formNote}>`
    - `<p style={{ fontSize: '0.875rem', color: 'var(--text-muted)' }}>Hochgeladene Medien sind zunächst nur intern sichtbar und werden vor der Veröffentlichung geprüft.</p>` → entfernen
    - Native `<input type="file">` mit eslint-disable-Kommentar bleibt unverändert (kein FileInput-Primitive verfügbar — Ausnahme korrekt dokumentiert).

    Import von `styles` ergänzen, falls in einer Datei noch nicht vorhanden (alle drei sollten `import styles from './contributions.module.css'` haben).

    Dateigröße nach Änderungen: ReportFormFehler ~135 Z., ReportFormStory ~120 Z., ReportFormMedia ~185 Z. — alle ≤ 450.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run src/components/contributions/ReportModal.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <done>
    ReportFormFehler, ReportFormStory, ReportFormMedia: kein inline style= außer native file-input Ausnahme.
    contributions.module.css: reportForm, fieldError, formNote Klassen vorhanden.
    TSC fehlerfrei. ReportModal.test grün.
  </done>
</task>

<task type="auto">
  <name>Task 2: RejectReasonModal + ReportModal + ProposalForm — inline styles entfernen, verbose Copy kürzen</name>
  <files>
    frontend/src/components/contributions/RejectReasonModal.tsx
    frontend/src/components/contributions/ReportModal.tsx
    frontend/src/components/contributions/ProposalForm.tsx
  </files>
  <action>
    **RejectReasonModal.tsx:**
    - Fehleranzeige `<p role="alert" style={{ color: 'var(--button-danger-start)', fontSize: '0.875rem', marginTop: 8 }}>` → ersetzen durch `<p role="alert" className={styles.fieldError} style={{ marginTop: 8 }}>` (oder styles.fieldError mit margin ergänzen — bevorzuge letzteres: styles.fieldError erhält `margin-top: 8px` in contributions.module.css).
    - `import styles from './contributions.module.css'` ergänzen, falls nicht vorhanden.
    - Alle anderen Elemente (Button, FormField, Modal, Textarea) bleiben unverändert.

    **ReportModal.tsx:**
    - `<p className={styles.reviewHint}>Dein Hinweis wird nach dem Absenden von Team4s geprüft.</p>` → entfernen. Der Modal-Kontext (Formulare mit "Vorschlag senden"-Button) ist selbsterklärend.
    - Alle Buttons, Modal, FormField bleiben unverändert.
    - Wenn `styles.reviewHint` danach nirgends mehr referenziert wird, muss die CSS-Klasse nicht entfernt werden (harmlos).

    **ProposalForm.tsx:**
    - Oberer `<div className={styles.infoPanel}>` ("Hier wird kein Credit direkt vergeben. Der Hinweis geht an die zuständige Gruppe. Danach wird entschieden, ob daraus ein bestätigter Eintrag wird.") → kürzen auf einen Satz: "Der Hinweis geht zur Prüfung an die zuständige Gruppe."
    - Scope-Button "Projekt insgesamt" `<span>Du sagst: Ich war in diesem Projekt dabei. Team4s lässt das prüfen.</span>` → entfernen. Nur `<strong>Projekt insgesamt</strong>` bleibt.
    - Scope-Button "Bestimmte Folgen / Release-Version" `<span>Noch nicht verfügbar: Dafür braucht Team4s zuerst eine echte Folgen- oder Release-Version-Auswahl.</span>` → kürzen auf `<span>Noch nicht verfügbar</span>`.
    - `<div role="status" className={styles.warningPanel}>Release-Version-spezifische Arbeit wird hier nicht als globaler Anime-Beitrag gespeichert.</div>` → entfernen (technischer Hinweis, kein UX-Wert für Nutzer).
    - Unterer `<div className={styles.infoPanel}>` ("Reagiert kein Gruppen-Leader binnen 90 Tagen, kannst du den Vorschlag selbst als unverifizierten historischen Eintrag öffentlich schalten.") → beibehalten, Text kürzen auf "Keine Reaktion nach 90 Tagen: Vorschlag kann selbst öffentlich geschaltet werden." (relevant für Nutzeraktion).

    Alle anderen Elemente (Modal footer, FormField, Select, Textarea, YearPicker, Button) bleiben vollständig unverändert.

    **Achtung ProposalForm.test.tsx:** Der Test prüft `expect(markup).toContain('90 Tagen')` — der 90-Tage-Hinweis bleibt erhalten, Test bleibt grün. Test prüft auch `expect(markup).toContain('Hier wird kein Credit direkt vergeben.')` — dieser String ändert sich. ProposalForm.test.tsx muss angepasst werden:
    - Zeile `expect(markup).toContain('Hier wird kein Credit direkt vergeben.')` → `expect(markup).toContain('Hinweis geht zur Prüfung an die zuständige Gruppe')`
    Test prüft außerdem `expect(markup).toContain('Noch nicht verfügbar')` — bleibt erhalten, da der Text im Button weiterhin "Noch nicht verfügbar" enthält.

    Dateigröße: RejectReasonModal ~97 Z., ReportModal ~165 Z., ProposalForm ~310 Z. — alle ≤ 450.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run src/components/contributions/ProposalForm.test.tsx src/components/contributions/ReportModal.test.tsx src/components/contributions/ContributionCard.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    RejectReasonModal: kein inline style= mehr; fieldError-Klasse genutzt.
    ReportModal: reviewHint-Paragraph entfernt.
    ProposalForm: oberer infoPanel gekürzt, Scope-Buttons ohne lange Erklärungs-Spans, warningPanel-Release-Hinweis entfernt.
    ProposalForm.test.tsx: angepasst an neue Copy, alle Tests grün.
    TSC fehlerfrei.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI only | Reine Copy- und CSS-Änderungen. Keine neuen Trust Boundaries eingeführt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-eaj-01 | Tampering | contributions.module.css | accept | Reine Style-Datei ohne Sicherheitsrelevanz |
</threat_model>

<verification>
Nach beiden Tasks:
- `cd C:/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit` — keine Fehler
- `npx vitest run src/components/contributions/` — alle Tests grün
- Kein neues inline style= in den geänderten Dateien (außer native file-input Ausnahme in ReportFormMedia)
- Alle deutschen Strings mit korrekten Umlauten
- Dateien ≤ 450 Zeilen
</verification>

<success_criteria>
- ReportFormFehler/Story/Media: form-Container und Fehlertexte nutzen CSS-Klassen statt inline styles
- RejectReasonModal: fieldError-Klasse statt inline color/fontSize
- ReportModal: reviewHint-Paragraph entfernt
- ProposalForm: infoPanels deutlich kürzer, Scope-Buttons ohne Erklärungs-Spans, Release-warningPanel entfernt
- ProposalForm.test.tsx angepasst und grün
- TSC fehlerfrei, alle Contribution-Tests grün
</success_criteria>

<output>
Create `.planning/quick/260620-eaj-member-contribution-ui-auf-globales-desi/260620-eaj-02-SUMMARY.md` when done
</output>
