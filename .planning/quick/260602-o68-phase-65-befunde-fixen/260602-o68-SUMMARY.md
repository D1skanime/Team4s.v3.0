---
quick_id: 260602-o68
slug: phase-65-befunde-fixen
status: complete
date: 2026-06-02
commit: working tree
---

# Quick Task 260602-o68: phase 65 befunde fixen

## Summary

Closed the actionable Phase 65 review findings in the working tree:

- `searchAnimeForProposal` now omits `include_disabled` deliberately and requests 8 results to match the UI cap.
- Proposal creation now explicitly verifies that `fansub_group_member_id` belongs to the submitted `fansub_group_id` before the repository insert.
- Proposal creation now accepts and guards optional `release_version_id` through the existing `GroupParticipatesInReleaseVersion` seam.
- Review reject keeps empty-body support but returns 400 for malformed JSON.
- Phase 65 repository German prose/error strings now use correct Umlaute.
- Contribution role labels now use canonical `role_definitions` seed codes.
- SelfPublish status invariant is covered by an executable regression test instead of a log-only test.
- Shared contribution contracts now match runtime error examples and include optional `release_version_id`.

## Checks

- `cd backend && go test ./internal/handlers -run "TestCreateProposal|TestSelfPublish|TestListMemberships|TestListProposals|TestConfirmProposal|TestRejectProposal" -count=1`
- `cd frontend && npx tsc --noEmit`
- `cd frontend && npx vitest run src/components/contributions/ProposalForm.test.tsx src/components/contributions/ReviewQueue.test.tsx`
- `node -e` YAML parse through `frontend/node_modules/js-yaml`
- `git diff --check`

## Remaining

Phase 65 browser UAT remains human-needed:

- Anime typeahead with real DB data.
- SelfPublish public anime-page visual distinction for unverified historical entries.
- Leader review queue UX.
- Member proposal modal flow.

No quick-task commit was created because the workspace already contained unrelated dirty files and an untracked Phase 68 artifact.
