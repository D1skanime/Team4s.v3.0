---
phase: quick-260805-7lu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/FocalCarousel.tsx
  - frontend/src/components/ui/FocalCarousel.test.tsx
autonomous: false
requirements: [260805-7lu-scope]

must_haves:
  truths:
    - "With prefers-reduced-motion: reduce, deliberate FocalCarousel arrow-button and carousel-level Arrow/Home/End keyboard navigation still glide smoothly to the requested card."
    - "During deliberate navigation the carousel is moving and exposes no active card; active emphasis and aria-current return only after the requested card is physically centered and settled."
    - "Reduced-motion behavior outside deliberate arrow/keyboard navigation is unchanged; fixed card geometry, vertical page gestures, pointer drag/momentum policy, wheel behavior, and nested-control keyboard isolation remain intact."
    - "Existing deferred activation, lazy loading, and responsive cached WebP delivery remain green for the member badge consumer."
  artifacts:
    - path: frontend/src/components/ui/FocalCarousel.tsx
      provides: Narrow V2 override for explicit arrow and keyboard programmatic navigation
    - path: frontend/src/components/ui/FocalCarousel.test.tsx
      provides: Reduced-motion smooth-navigation and inherited interaction regression coverage
  key_links:
    - from: frontend/src/components/ui/FocalCarousel.tsx
      to: focusItem
      via: arrow move and carousel-level ArrowLeft/ArrowRight/Home/End handlers share the deliberate navigation path
      pattern: "move|handleKeyDown|focusItem"
    - from: frontend/src/components/ui/FocalCarousel.tsx
      to: scheduleScrollSettle
      via: smooth navigation keeps pending target and delays active state until settle
      pattern: "pendingTargetIndexRef|scheduleScrollSettle"
---

<objective>
Make deliberate FocalCarousel arrow and keyboard navigation glide smoothly even when the browser reports reduced motion, without broadening the exception beyond that component-owned navigation path.

Purpose: Implement the user-approved V2 interaction after live inspection identified `prefers-reduced-motion: reduce` as the reason navigation jumped immediately.
Output: A narrow FocalCarousel implementation/test change, Docker-based regression and image-delivery proof, rebuilt live frontend, and blocking tablet UAT.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
<read_first>
@AGENTS.md
@.planning/STATE.md
@docs/engineering/implementation-contract.md
@docs/frontend/ui-system.md
@docs/agent-guidelines-ui.md
@frontend/src/components/ui/FocalCarousel.tsx
@frontend/src/components/ui/FocalCarousel.module.css
@frontend/src/components/ui/FocalCarousel.test.tsx
@frontend/src/components/profile/MemberBadgeChain.tsx
@frontend/src/components/profile/MemberBadgeChain.module.css
@frontend/src/components/profile/MemberBadgeChain.test.tsx
@frontend/src/components/fansubs/FansubProjectsGrid.tsx
@frontend/src/components/fansubs/__tests__/FansubProjectsGrid.test.tsx
@frontend/src/components/ui/ResponsiveImage.tsx
@frontend/src/components/ui/ResponsiveImage.test.tsx
@frontend/scripts/run-profile-image-probe.mjs
@frontend/scripts/verify-profile-image-delivery.mjs
@.planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-CONTEXT.md
@.planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-RESEARCH.md
@.planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-UI-SPEC.md
@.planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-06-PLAN.md
@.planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-06-SUMMARY.md
@.planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-12-PLAN.md
@.planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-12-SUMMARY.md
</read_first>

The user-selected V2 behavior is locked: override reduced motion only for deliberate FocalCarousel arrow-button and carousel-level keyboard navigation. Do not change the global accessibility preference, global CSS motion policy, pointer-drag reduced-motion policy, consumer-local image/loading behavior, or unrelated carousels. Preserve every unrelated dirty file; this is a shared worktree, so inspect `git status --short` before editing and never reset, overwrite, broad-format, or stage unrelated changes.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make deliberate navigation smooth under reduced motion</name>
  <files>frontend/src/components/ui/FocalCarousel.tsx, frontend/src/components/ui/FocalCarousel.test.tsx</files>
  <behavior>
    - With `matchMedia('(prefers-reduced-motion: reduce)').matches === true`, clicking either arrow calls the existing track `scrollTo` seam with `behavior: 'smooth'`, enters `data-navigation-state="moving"`, exposes no `aria-current`, and settles the requested centered card only after the existing 160 ms settle path.
    - Under the same reduced-motion condition, carousel-level ArrowLeft/ArrowRight/Home/End use the same smooth pending-target behavior; rapid deliberate commands continue from `pendingTargetIndexRef` rather than the stale settled index.
    - Keyboard events from nested interactive children remain ignored, and free wheel/pointer drag paths do not acquire the deliberate-navigation override.
    - The CSS contract still contains no card scale/proximity pumping and no global smooth-scroll declaration; active styling remains settled-only.
  </behavior>
  <action>
    Start by replacing the existing reduced-motion test that expects `behavior: 'auto'`/immediate activation with focused failing assertions for the locked V2 outcome, and add a reduced-motion keyboard case covering Arrow/Home/End plus the existing pending-target/settle semantics. In `FocalCarousel.tsx`, keep `reducedMotionRef`, its media-query listener, its preference-change cleanup/settling behavior, and its use in pointer-release momentum unchanged. Narrowly change the `focusItem` branch used by arrow `move()` and carousel-level `handleKeyDown()` so a valid deliberate programmatic target always sets `pendingTargetIndexRef`, enters moving state, calls `track.scrollTo({ left, behavior: 'smooth' })`, and schedules the existing settle timer even when reduced motion is reported. Do not add a prop, global override, CSS `scroll-behavior`, new timer/listener, consumer-local carousel, scale/transform animation, or alternate active-state path. Preserve clamping, rapid-click pending targets, physical-center reconciliation, quiet mode, deferred interaction, wheel behavior, vertical-intent escape, drag capture/momentum behavior, focus return, nested-interactive keyboard isolation, and all existing German labels.
  </action>
  <verify>
    <automated>cd /home/d1sk/team4s &amp;&amp; docker compose exec -T team4sv30-frontend npm test -- --run src/components/ui/FocalCarousel.test.tsx src/components/fansubs/__tests__/FansubProjectsGrid.test.tsx src/components/profile/MemberBadgeChain.test.tsx</automated>
  </verify>
  <done>Focused tests prove reduced-motion arrow and keyboard navigation glide through the shared pending-target/settle path, while geometry, active timing, gestures, drag, nested controls, deferred interaction, and both existing consumers retain their contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Run Docker regressions, rebuild, and deploy the frontend</name>
  <files>none (validation and deployment only)</files>
  <action>
    From `/home/d1sk/team4s`, recheck `git status --short` and review the exact diff for only the two declared FocalCarousel files. Run the focused carousel/consumer tests, frontend typecheck, frontend lint, and a clean production build inside Compose. Run the existing Phase-120 image-delivery probe with the checked-in avatar/hero fixtures and required `static-badge,profile-avatar,profile-hero,api-project,api-group` URL classes, widths `128,160,512,640`, alpha, cache-hit, and original-fallback gates so this interaction-only change cannot silently regress lazy/responsive WebP delivery. Then rebuild and force-recreate only `team4sv30-frontend`; do not restart databases, Redis, Keycloak, backend, or overwrite `.env`/`media`. Confirm the service is Up and the tunneled frontend responds before live review. Report pre-existing unrelated failures separately rather than editing outside scope.
  </action>
  <verify>
    <automated>cd /home/d1sk/team4s &amp;&amp; docker compose exec -T team4sv30-frontend npm test -- --run src/components/ui/FocalCarousel.test.tsx src/components/fansubs/__tests__/FansubProjectsGrid.test.tsx src/components/profile/MemberBadgeChain.test.tsx src/components/ui/ResponsiveImage.test.tsx &amp;&amp; docker compose exec -T team4sv30-frontend npm run typecheck &amp;&amp; docker compose exec -T team4sv30-frontend npm run lint &amp;&amp; docker compose run --rm --no-deps -T -e NODE_ENV=production -v /app/.next team4sv30-frontend npm run build &amp;&amp; docker compose -f docker-compose.yml run --rm --no-deps -T -e NODE_ENV=production -e PHASE120_IMAGE_PROBE=1 -e API_INTERNAL_URL=http://127.0.0.1:3101 -v "$PWD/frontend/test/fixtures/phase120-image-probe/avatar.png:/media/profile/phase120/avatar.png:ro" -v "$PWD/frontend/test/fixtures/phase120-image-probe/hero.png:/media/profile/phase120/hero.png:ro" team4sv30-frontend node scripts/run-profile-image-probe.mjs --next-port 3100 --fixture-port 3101 --verifier scripts/verify-profile-image-delivery.mjs --require-url-classes static-badge,profile-avatar,profile-hero,api-project,api-group --require-widths 128,160,512,640 --require-alpha --require-cache-hit --require-original-fallback &amp;&amp; docker compose build team4sv30-frontend &amp;&amp; docker compose up -d --force-recreate team4sv30-frontend &amp;&amp; docker compose ps team4sv30-frontend &amp;&amp; curl -fsS http://127.0.0.1:3000/ &gt;/dev/null &amp;&amp; git diff --check</automated>
  </verify>
  <done>All scoped interaction, consumer, type, lint, build, image-delivery, service-health, and whitespace gates pass against the rebuilt live frontend, with only the declared source/test diff.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve reduced-motion tablet navigation in the live shared flow</name>
  <files>none (human verification only)</files>
  <what-built>V2 deliberate FocalCarousel arrow/keyboard glide under the browser's real reduced-motion preference, deployed through the existing frontend Compose service.</what-built>
  <action>Use the shared Codex in-app browser and the user-visible profile navigation after Task 2 has rebuilt the frontend. Do not disable the browser's reduced-motion preference, substitute headless-only evidence, or self-approve the visual/gesture result.</action>
  <how-to-verify>
    1. In the shared Codex in-app browser, navigate visibly from `http://127.0.0.1:3300` to the populated public member profile (use `http://127.0.0.1:3300/members/csubs-leader` when that fixture remains available) and set the viewport to 768x1024.
    2. Confirm the browser still reports `matchMedia('(prefers-reduced-motion: reduce)').matches === true`; do not disable the preference to make the test pass.
    3. In a multi-card badge FocalCarousel, click Next/Previous and use carousel-level ArrowLeft/ArrowRight/Home/End. Each deliberate command must visibly glide to its target instead of jumping; rapid commands must continue toward the newly requested card.
    4. During the glide confirm no card scales, pumps, changes reserved size, or becomes active early. Only the centered/settled card receives active emphasis and the counter/arrow disabled state agrees with it afterward.
    5. Swipe vertically starting over the carousel and confirm the page scrolls normally. Then drag horizontally and use a horizontal trackpad/wheel gesture; drag/capture, rail scrolling, endpoint passthrough, and settling remain intact.
    6. Scroll away and back through deep badge sections. Confirm deferred activation/lazy loading remains stable, artwork geometry does not shift, and the network continues serving responsive WebP badge variants rather than eager full-size originals in normal operation.
  </how-to-verify>
  <verify>
    <automated>cd /home/d1sk/team4s &amp;&amp; docker compose exec -T team4sv30-frontend npm test -- --run src/components/ui/FocalCarousel.test.tsx src/components/profile/MemberBadgeChain.test.tsx &amp;&amp; docker compose ps team4sv30-frontend &amp;&amp; git diff --check</automated>
  </verify>
  <done>The user explicitly approves live 768x1024 reduced-motion arrow/keyboard glide with fixed geometry, settled-only emphasis, intact vertical/horizontal gestures, drag behavior, lazy loading, and responsive WebP delivery.</done>
  <resume-signal>Type "approved" or report the exact command, card, gesture, viewport, and observed failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser accessibility preference → carousel navigation | A user-agent preference normally suppresses motion; V2 permits a narrowly scoped exception only for explicit FocalCarousel arrow/keyboard commands. |
| Pointer/keyboard/wheel input → carousel state | Interaction events may change pending/settled card state and owned horizontal scroll position. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260805-01 | T | `FocalCarousel.focusItem` reduced-motion branch | mitigate | Tests require the exception only on the existing deliberate arrow/keyboard path; retain the media-query ref for drag/momentum and do not change global CSS/preferences. |
| T-260805-02 | D | rapid navigation and settle timer | mitigate | Reuse one pending target and one cleared/rescheduled settle timer; retain bounded target and physical-center reconciliation tests. |
| T-260805-03 | D | touch/pointer gesture routing | mitigate | Preserve `touch-action: pan-y`, vertical-intent escape, endpoint passthrough, drag capture, and settled-only active-state regressions. |
| T-260805-04 | I | responsive image delivery | accept | No image/API surface changes; existing consumer and deterministic WebP/cache/fallback probes remain mandatory. |
</threat_model>

<verification>
- Focused FocalCarousel, FansubProjectsGrid, MemberBadgeChain, and ResponsiveImage tests pass in the running frontend container.
- Typecheck, lint, isolated production build, deterministic Phase-120 image-delivery probe, `git diff --check`, and exact two-file diff review pass.
- `team4sv30-frontend` is rebuilt and force-recreated without touching other services or host-local runtime data.
- Blocking live 768x1024 UAT proves V2 under an actually true reduced-motion media query and checks fixed geometry, settled-only emphasis, gestures, drag, lazy loading, and responsive WebP delivery.
</verification>

<success_criteria>
- Reduced-motion arrow buttons and carousel-level Arrow/Home/End navigation use `behavior: 'smooth'` and the existing moving-to-settled state machine.
- No global accessibility preference, CSS motion policy, pointer-drag policy, consumer API, image seam, or domain behavior changes.
- Cards never scale or pump; active state appears only after centering/settling.
- Vertical page scrolling, horizontal wheel/trackpad motion, pointer drag, rapid commands, quiet mode, deferred activation, lazy loading, and responsive cached WebP delivery remain verified.
- Docker checks, frontend rebuild/deploy, service health, diff review, and live tablet approval complete successfully.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Task | Status | Notes
GOAL | — | Smooth explicit FocalCarousel arrow/keyboard navigation when reduced motion is true | 1-3 | COVERED | V2 implementation, deployment, and live proof
REQ | 260805-7lu-scope | Preserve geometry, settled emphasis, gestures, drag, lazy loading, and WebP delivery | 1-3 | COVERED | Automated regressions plus tablet UAT
RESEARCH | Phase 120 D-16/D-17 | Fixed geometry with no scale/size animation or layout shift | 1-3 | COVERED | CSS remains read-only and regressions/UAT pin behavior
RESEARCH | Phase 120 D-18 | Preserve shared FocalCarousel behavior/deferred SSR interaction seam | 1-2 | COVERED | Existing component and consumer tests reused
RESEARCH | Phase 120 D-19/D-20/D-21 | Lazy responsive cached WebP delivery and safe fallback | 2-3 | COVERED | Existing deterministic image probe and live network review
CONTEXT | V2 | Override reduced motion only for deliberate FocalCarousel arrow/keyboard navigation | 1-3 | COVERED | No broad preference or CSS override
CONTEXT | invariant | Cards do not scale/pump; active emphasis waits for centered/settled state | 1-3 | COVERED | Test and live UAT
CONTEXT | invariant | Vertical gestures and drag behavior remain intact | 1-3 | COVERED | Existing tests and live UAT
Deferred Ideas | — | None supplied | — | EXCLUDED | No deferred scope introduced
</source_audit>

<output>
After completion, create `.planning/quick/260805-7lu-make-focalcarousel-arrow-and-keyboard-na/260805-7lu-SUMMARY.md`.
</output>
