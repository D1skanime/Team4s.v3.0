---
phase: quick-260915-ro9
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/admin/anime/[id]/edit/formatEditLoadError.ts
  - frontend/src/app/admin/anime/[id]/edit/page.tsx
  - frontend/src/app/admin/anime/[id]/edit/page.test.tsx
  - frontend/src/app/admin/anime/page.tsx
  - frontend/src/app/admin/anime/page.test.tsx
autonomous: true
requirements: [QUICK-260915-RO9-01, QUICK-260915-RO9-02]

must_haves:
  truths:
    - "npx tsc --noEmit -p tsconfig.json reports 0 errors in the frontend container (the two named TS2344 errors on the edit and list admin-anime page files are gone, and no new type error is introduced)."
    - "frontend/src/app/admin/anime/[id]/edit/page.tsx exports only the default page component (plus any pre-existing Next.js-sanctioned export it already had); the pure formatEditLoadError helper lives in its own sibling file with byte-identical behavior and the same German error strings."
    - "frontend/src/app/admin/anime/page.tsx's default export satisfies the Next.js 16 PageProps contract without an optional-default-value workaround on the destructured props parameter; the created-anime highlight behavior driven by the ?created= search param is unchanged at runtime."
    - "/admin/anime and /admin/anime/1/edit both return HTTP 200 (an auth redirect/login prompt is acceptable; a 500 is not) after docker restart team4sv30-frontend."
  artifacts:
    - path: "frontend/src/app/admin/anime/[id]/edit/formatEditLoadError.ts"
      provides: "The extracted, unchanged formatEditLoadError(error: unknown): string helper, imported by page.tsx and by page.test.tsx"
    - path: "frontend/src/app/admin/anime/[id]/edit/page.tsx"
      provides: "Only the default AdminAnimeEditPage export — no extra named export breaking the generated Next.js page type"
    - path: "frontend/src/app/admin/anime/page.tsx"
      provides: "AdminAnimePage's props parameter has no default value; AdminAnimePageProps stays the same optional-searchParams shape already used elsewhere in this codebase (see interfaces block)"
  key_links:
    - from: "frontend/src/app/admin/anime/[id]/edit/page.tsx"
      to: "frontend/src/app/admin/anime/[id]/edit/formatEditLoadError.ts"
      via: "import { formatEditLoadError } from './formatEditLoadError'"
      pattern: "from \"\\./formatEditLoadError\""
    - from: "frontend/src/app/admin/anime/[id]/edit/page.test.tsx"
      to: "frontend/src/app/admin/anime/[id]/edit/formatEditLoadError.ts"
      via: "import { formatEditLoadError } from './formatEditLoadError'"
      pattern: "from '\\./formatEditLoadError'"
---

<objective>
Fix the two exact TypeScript errors `npx tsc --noEmit` reports for the admin-anime routes under Next.js 16's generated page-type contract, and nothing else.

1. `.next/dev/types/app/admin/anime/[id]/edit/page.ts(14,13): error TS2344` — `frontend/src/app/admin/anime/[id]/edit/page.tsx` exports a second, non-Next.js-sanctioned named export (`formatEditLoadError`) alongside its default page export. Next.js page files may only export a fixed set of identifiers (default, metadata, generateMetadata, dynamic, revalidate, etc.); the extra export breaks the generated page type. This is also documented in STATE.md as (plausibly) the root cause of a previously known admin-page production-build export blocker.
2. `.next/dev/types/app/admin/anime/page.ts(36,29): error TS2344` — `frontend/src/app/admin/anime/page.tsx`'s default export destructures its props parameter with a `= {}` default value, which makes the inferred parameter type `AdminAnimePageProps | undefined` and violates the Next.js 16 `PageProps` constraint.

Purpose: unblock a clean `tsc --noEmit` for these two routes without touching any other lint/type debt, and remove a specific, previously-flagged path to the admin-page production-build export blocker.
Output: `formatEditLoadError` moved to its own file with unchanged behavior; `AdminAnimePage`'s props signature corrected to the pattern already used elsewhere in this codebase (`members/ranking/page.tsx`); both route's tests updated to match; 0 TypeScript errors; both routes verified to answer HTTP 200/redirect (never 500) live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- Current frontend/src/app/admin/anime/[id]/edit/page.tsx (277 lines). The helper to move,
     unchanged, into a new sibling file frontend/src/app/admin/anime/[id]/edit/formatEditLoadError.ts:

       import { formatAdminError } from "../../utils/studio-helpers";

       export function formatEditLoadError(error: unknown): string {
         return formatAdminError(error, "Anime konnte nicht geladen werden.");
       }

     ("../../utils/studio-helpers" resolves the same way from the new file as it does from page.tsx
     today, since both files live directly in the [id]/edit/ directory: [id]/edit/ -> [id]/ -> anime/
     -> utils/studio-helpers.ts.)

     page.tsx currently: imports formatAdminError from "../../utils/studio-helpers" ONLY to build
     formatEditLoadError (grep confirms formatAdminError is not used anywhere else in page.tsx) — so
     that import line moves out of page.tsx entirely, replaced by
     `import { formatEditLoadError } from "./formatEditLoadError";`. The one call site inside
     AdminAnimeEditContent's loadAnime() effect (`setErrorMessage(formatEditLoadError(error));`) is
     unchanged. page.tsx keeps its `"use client";` directive and every other existing import/export;
     only `export function formatEditLoadError(...)` and the now-unused `formatAdminError` import are
     removed from it.

     The ONLY other consumer of formatEditLoadError in the repo is
     frontend/src/app/admin/anime/[id]/edit/page.test.tsx, line 9:
       import { formatEditLoadError } from './page'
     which becomes:
       import { formatEditLoadError } from './formatEditLoadError'
     No other change to that test file — the assertion at line 21 (exact German two-line message via
     formatAdminError) must keep passing unchanged, since the helper's implementation does not change. -->

<!-- Current frontend/src/app/admin/anime/page.tsx (83 lines):

       interface AdminAnimePageProps {
         searchParams?: Promise<{
           created?: string;
         }>;
       }

       export default async function AdminAnimePage({
         searchParams,
       }: AdminAnimePageProps = {}) {
         const resolvedSearchParams = searchParams ? await searchParams : undefined;
         ...
       }

     Fix: delete the `= {}` default value on the destructured parameter only. Do NOT change
     AdminAnimePageProps (searchParams stays an optional property — that part is already correct and
     matches this codebase's own established pattern for optional-searchParams async pages, e.g.
     frontend/src/app/members/ranking/page.tsx's RankingPageProps:
       interface RankingPageProps {
         searchParams?: Promise<{ page?: string | string[] }>;
       }
       export default async function MemberRankingPage({ searchParams }: RankingPageProps) {
         const resolved = ((await searchParams) ?? {}) as ResolvedRankingSearchParams;
       ). The function body's existing
     `const resolvedSearchParams = searchParams ? await searchParams : undefined;` line needs no change
     — it already tolerates an absent searchParams. Only the trailing `= {}` on the destructured
     parameter goes away.

     Because the parameter itself becomes required (no default), every call site that today calls
     `AdminAnimePage()` with zero arguments must instead pass an explicit props object, exactly as
     members/ranking/page.test.tsx's own tests already do for MemberRankingPage
     (`MemberRankingPage({ searchParams: Promise.resolve(searchParams) })`) — never by re-adding a
     default value to accommodate the tests.

     frontend/src/app/admin/anime/page.test.tsx currently calls `await AdminAnimePage()` with no
     arguments at two call sites (lines 59 and 82, inside `'renders the anime overview shell...'` and
     `'keeps Jellyfin intake controls out of the anime overview route'`). Change both to
     `await AdminAnimePage({ searchParams: Promise.resolve({}) })`. No other line in that test file
     changes. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract formatEditLoadError out of the edit page's export surface</name>
  <files>frontend/src/app/admin/anime/[id]/edit/formatEditLoadError.ts, frontend/src/app/admin/anime/[id]/edit/page.tsx, frontend/src/app/admin/anime/[id]/edit/page.test.tsx</files>
  <behavior>
    - formatEditLoadError, now imported from its new file, still turns an ApiError with status/code/detail
      into the same exact two-line German string the existing page.test.tsx assertion checks
      ("(500) Interner Serverfehler\nFehlende Spalte: anime.max_episodes") — byte-identical to today.
    - page.tsx's default export (AdminAnimeEditPage) and all its runtime behavior (loading state, error
      display, fansub loading, relation section, developer panel) are unchanged.
    - page.tsx has exactly one export: `export default function AdminAnimeEditPage`.
  </behavior>
  <action>
    Create frontend/src/app/admin/anime/[id]/edit/formatEditLoadError.ts per the first &lt;interfaces&gt;
    block: import formatAdminError from "../../utils/studio-helpers" and re-export
    formatEditLoadError(error: unknown): string with the identical body it has today in page.tsx. In
    page.tsx, delete the `export function formatEditLoadError` block and its now-unused
    `formatAdminError` import from "../../utils/studio-helpers", and add
    `import { formatEditLoadError } from "./formatEditLoadError";` in its place; the existing call site
    inside loadAnime()'s catch branch stays exactly as-is. Update page.test.tsx's import at line 9 from
    `import { formatEditLoadError } from './page'` to
    `import { formatEditLoadError } from './formatEditLoadError'` — no other line of that test file
    changes, including its `readFileSync(... 'page.tsx' ...)` assertions further down, which continue to
    read the (now-shorter) page.tsx and must keep passing unchanged.
  </action>
  <verify>
    <automated>docker exec -w /app team4sv30-frontend sh -c "npx vitest run 'src/app/admin/anime/[id]/edit/page.test.tsx'"</automated>
  </verify>
  <done>formatEditLoadError.ts exists and is the sole owner of the helper; page.tsx exports only the default page component; all page.test.tsx assertions pass unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correct the admin-anime list page's PageProps contract</name>
  <files>frontend/src/app/admin/anime/page.tsx, frontend/src/app/admin/anime/page.test.tsx</files>
  <behavior>
    - AdminAnimePage's inferred parameter type is AdminAnimePageProps (never `AdminAnimePageProps |
      undefined`), satisfying the generated Next.js 16 PageProps constraint.
    - Rendering with an empty resolved searchParams (Promise.resolve({})) behaves exactly as today's
      "no ?created= param" case: no highlighted anime, same markup asserted by the existing tests.
  </behavior>
  <action>
    In frontend/src/app/admin/anime/page.tsx, remove the `= {}` default value from the destructured
    `{ searchParams }: AdminAnimePageProps` parameter of the default-exported AdminAnimePage function,
    per the second &lt;interfaces&gt; block. Leave AdminAnimePageProps and the function body untouched
    (searchParams stays an optional property on the interface; the existing
    `searchParams ? await searchParams : undefined` guard already handles that). In
    frontend/src/app/admin/anime/page.test.tsx, change both zero-argument `await AdminAnimePage()`
    call sites (inside the "renders the anime overview shell..." and "keeps Jellyfin intake controls
    out of the anime overview route" tests) to
    `await AdminAnimePage({ searchParams: Promise.resolve({}) })`. No other test in this file changes.
  </action>
  <verify>
    <automated>docker exec -w /app team4sv30-frontend sh -c "npx vitest run src/app/admin/anime/page.test.tsx"</automated>
  </verify>
  <done>AdminAnimePage no longer has a default-valued props parameter; both updated test call sites pass; the other two tests in the file (untouched) still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Full targeted verification and live route check</name>
  <files></files>
  <action>
    Before any git add, run `git status --short` and confirm only this plan's five files_modified paths
    are touched — do not stage anything under admin/episode-versions, admin/anime/[id]/episodes/import,
    or any backend path (a parallel Phase 161 run may be active in the same working tree); stage only
    this plan's files by explicit path, never `git add -A`/`git add .`. Inside the team4sv30-frontend
    container (working dir /app), run `npx tsc --noEmit -p tsconfig.json` and confirm 0 errors (the two
    named TS2344 errors are gone, and no other error appears — do not fix any other pre-existing error
    if one is unexpectedly present; report it instead of touching unrelated files). Run
    `npx eslint` on exactly the five changed files and confirm no new errors/warnings. Then run
    `docker restart team4sv30-frontend`, wait for it to become healthy, and curl
    `http://192.168.235.196:3000/admin/anime` and `http://192.168.235.196:3000/admin/anime/1/edit`,
    confirming each returns HTTP 200 or an auth redirect (never a 500). If `.git/index.lock` exists at
    commit time, wait briefly and retry — never delete the lock file. Never use `git stash`, never push.
  </action>
  <verify>
    <automated>docker exec -w /app team4sv30-frontend npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>tsc reports 0 errors; scoped eslint on the five changed files is clean; both admin-anime routes return 200/redirect (never 500) against the restarted container; git status confirms no foreign-phase files were touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> /admin/anime, /admin/anime/[id]/edit | Already-authenticated-gated admin routes (PlatformAdminGate); this plan changes only export shape and prop-typing, not the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-260915-RO9-01 | Tampering | formatEditLoadError extraction | accept | Pure function move with unchanged implementation and an existing test asserting byte-identical output; no behavior change, no new trust boundary |
| T-260915-RO9-02 | Elevation of Privilege | AdminAnimePage props-signature change | accept | PlatformAdminGate wraps the page body unchanged; only the searchParams destructuring/typing changes, never an authorization check |
</threat_model>

<verification>
- Task 1's scoped vitest run for the edit route's page.test.tsx passes, including the unchanged
  byte-identical formatEditLoadError assertion.
- Task 2's scoped vitest run for the list route's page.test.tsx passes with both updated call sites.
- Task 3: `npx tsc --noEmit -p tsconfig.json` reports 0 errors; scoped eslint on the five changed files
  is clean; both live routes return 200/redirect (never 500) after `docker restart team4sv30-frontend`;
  `git status --short` confirms no files outside this plan's five files_modified were touched.
</verification>

<success_criteria>
- Both named TS2344 errors are gone from `npx tsc --noEmit` output, with no new type error introduced.
- frontend/src/app/admin/anime/[id]/edit/page.tsx exports only its default page component.
- frontend/src/app/admin/anime/page.tsx's default export satisfies the Next.js 16 PageProps contract
  without weakening AdminAnimePageProps or its runtime search-param behavior.
- No other lint/type error is fixed incidentally; no file outside this plan's declared scope is touched.
- /admin/anime and /admin/anime/1/edit both respond 200/redirect (never 500) live after a container
  restart.
</success_criteria>

<output>
Create `.planning/quick/260915-ro9-zwei-typescript-fehler-in-admin-anime-ro/260915-ro9-SUMMARY.md` when done
</output>
