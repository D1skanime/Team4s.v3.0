# Quick Plan

## Read first

- `AGENTS.md`
- `docs/engineering/implementation-contract.md`
- `docs/frontend/ui-system.md`
- `docs/agent-guidelines-ui.md`
- `frontend/src/app/admin/anime/[id]/episodes/import/page.tsx`
- `frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportEpisodeGroup.tsx`
- `frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportMappingRow.tsx`
- `frontend/src/components/ui/Input.tsx`
- `frontend/src/components/ui/Select.tsx`
- `frontend/src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.ts`

## Actions

1. In `page.tsx`, correct the German global skip-action label and keep the existing builder callbacks.
2. In `EpisodeImportEpisodeGroup.tsx`, replace title `Textarea` controls with the existing `Input`, add the extensible language `Select` with only `de`/„Deutsch“, and leave title persistence on `setEpisodeTitle`.
3. Remove the single-file header presentation so `EpisodeImportMappingRowCard` remains the only filename/path presentation.
4. In `page.module.css`, right-align the workbench actions, reduce title controls to normal field height, and preserve narrow-layout stacking.
5. Run typecheck, focused ESLint/tests, diff checks, update quick artifacts and `STATE.md`, then commit atomically.

## Acceptance criteria

- The three workbench actions are visually right-aligned on wide and intermediate layouts and remain usable on narrow layouts.
- The title label reads „Episodentitel“ and uses a normal single-line input.
- A language select is visible with „Deutsch“ as the only currently active option.
- Filename and display path appear in the lower mapping row, not duplicated in the episode header.
- Existing title state updates and mapping behavior remain covered by passing tests.
