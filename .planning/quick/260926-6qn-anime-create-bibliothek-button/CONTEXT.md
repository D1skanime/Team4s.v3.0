# Context

The previous copy cleanup removed the entire DiscoveryEntryCard, which also removed the required `Bibliothek durchsuchen` action. The explanatory card should stay gone, but the navigation action must remain available.

Decision: render the existing shared Button directly above the AniSearch/Jellyfin provider grid and keep the old card title/description removed.
