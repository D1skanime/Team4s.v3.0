# Context

The create source area had a library button isolated on the left and provider cards with different heights, leaving the Jellyfin side visually underfilled.

Decision: keep the existing provider controls, align the library action to the right above the source cards, and let both provider cards stretch to the same grid-row height.
