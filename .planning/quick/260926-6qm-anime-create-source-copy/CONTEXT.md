# Context

After the first create-page copy cleanup, the source area still repeated labels and helper text: the section heading, black AniSearch heading, provider path hint, Jellyfin search label, the Pflichtangaben eyebrow, and the adopted-assets explanation.

Decision: keep the library search button and all functional inputs/actions, while removing only redundant visible text. Add an accessible aria-label to the now-unlabeled Jellyfin search input.
