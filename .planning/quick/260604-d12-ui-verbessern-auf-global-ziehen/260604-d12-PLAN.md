---
quick_id: 260604-d12
slug: ui-verbessern-auf-global-ziehen
status: planned
---

# Quick Task 260604-d12: UI verbessern auf global ziehen

## Goal

Improve the AnimeOwnage/Fansub group history milestone UI in `/admin/fansubs/[id]/edit` by reusing the global Team4s UI primitives more consistently.

## Read First

- `docs/engineering/implementation-contract.md`
- `docs/frontend/ui-system.md`
- `docs/agent-guidelines-ui.md`
- `frontend/src/components/groups/GroupHistorySection.tsx`
- `frontend/src/components/groups/GroupHistoryForm.tsx`
- `frontend/src/components/groups/groups.module.css`
- `frontend/src/components/ui/FormField.tsx`
- `frontend/src/components/ui/YearPicker.tsx`
- `frontend/src/components/ui/Badge.tsx`
- `frontend/src/components/ui/LoadingState.tsx`

## Plan

1. Replace local milestone form label/error wrappers with global `FormField`.
2. Replace the free numeric year input with global `YearPicker` for the persisted year field.
3. Use global `Badge` and `LoadingState` in `GroupHistorySection`.
4. Adjust only local layout CSS needed for the history timeline composition.
5. Run focused frontend checks and `git diff --check`.

## Acceptance Criteria

- The year field is no longer a plain number input.
- Milestone event type labels render through global `Badge`.
- Loading state uses global `LoadingState`.
- No API helper, endpoint, DTO, or media ownership behavior changes.
