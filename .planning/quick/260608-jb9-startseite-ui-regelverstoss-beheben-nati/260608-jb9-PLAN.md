---
phase: quick-260608-jb9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/page.tsx
  - frontend/src/styles/globals.css
autonomous: true
requirements:
  - UI-PRIM-01
must_haves:
  truths:
    - "Die vier Navigations-Buttons rendern als `<a>`-Tags via Button-Primitiv (kein natives `<Link className=...>`)"
    - "Der Flex-Wrapper nutzt eine CSS-Klasse `.hero-actions` statt Inline-Styles"
    - "`.primary-link` und `.primary-link:hover` sind aus globals.css entfernt"
    - "Keine Inline-Style-Props auf der Seite nach dem Fix"
  artifacts:
    - path: frontend/src/app/page.tsx
      provides: Startseite mit Button-Primitiv-Links
      contains: "Button href"
    - path: frontend/src/styles/globals.css
      provides: CSS-Klasse .hero-actions
      contains: ".hero-actions"
  key_links:
    - from: frontend/src/app/page.tsx
      to: frontend/src/components/ui/Button.tsx
      via: "import { Button } from '@/components/ui'"
      pattern: "Button href="
---

<objective>
Den UI-Regelverstoß auf der Startseite beheben: vier native `<Link className="primary-link">`
durch das `Button`-Primitiv aus `@/components/ui` ersetzen und den Inline-Style-Flex-Wrapper
durch eine benannte CSS-Klasse (`.hero-actions`) ablösen.

Purpose: Einhaltung der CLAUDE.md-Pflicht, wonach jede user-facing UI die globalen Primitives
aus `@/components/ui` nutzen muss — kein Eigen-Markup für einen vorhandenen Primitiv-Typ.

Output: Bereinigte `page.tsx` ohne Inline-Styles und ohne `<Link className=...>`-Muster;
bereinigtes `globals.css` ohne die obsolete `.primary-link`-Regel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/quick/260608-jb9-startseite-ui-regelverstoss-beheben-nati/260608-jb9-PLAN.md
@frontend/src/app/page.tsx
@frontend/src/components/ui/Button.tsx
@frontend/src/styles/globals.css
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Button-Primitiv auf Startseite einsetzen und CSS aufräumen</name>
  <files>frontend/src/app/page.tsx, frontend/src/styles/globals.css</files>
  <action>
    In `frontend/src/app/page.tsx`:

    1. Den `import Link from 'next/link'` entfernen.
    2. Stattdessen `import { Button } from '@/components/ui'` hinzufügen.
    3. Den Inline-Style-Wrapper `<div style={{ display: 'flex', gap: '0.75rem', flexWrap: 'wrap' }}>` durch `<div className="hero-actions">` ersetzen.
    4. Jedes der vier `<Link href="..." className="primary-link">...</Link>` durch `<Button href="...">...</Button>` ersetzen:
       - `<Button href="/anime">Zur Anime-Liste</Button>`
       - `<Button href="/watchlist">Zur Watchlist</Button>`
       - `<Button href="/login">Anmelden</Button>`
       - `<Button href="/admin">Zu Admin</Button>`
    5. Keine weiteren Änderungen an `.page-shell` oder `.hero` — diese Layout-Klassen bleiben unberührt.

    In `frontend/src/styles/globals.css`:

    6. Die Regeln `.primary-link { ... }` und `.primary-link:hover { ... }` (Zeilen ~171–183) vollständig entfernen. Diese Klasse wird ausschließlich von `page.tsx` genutzt (grep bestätigt: nur 2 Treffer — page.tsx und globals.css selbst).
    7. Direkt nach dem `.hero { ... }`-Block (ca. Zeile ~161) die neue Klasse einfügen:

       .hero-actions {
         display: flex;
         gap: 0.75rem;
         flex-wrap: wrap;
         margin-top: var(--space-4);
       }

       Hinweis: `margin-top: var(--space-4)` übernimmt das bisherige `margin-top` aus `.primary-link`, da es nun auf dem Container statt auf den einzelnen Links sitzt.

    Keine Inline-Styles dürfen nach diesem Task noch auf der Seite verbleiben.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit --project tsconfig.json 2>&amp;1 | head -20</automated>
  </verify>
  <done>
    - `page.tsx` importiert `Button` aus `@/components/ui`, kein `Link`-Import mehr.
    - Alle vier Links sind `&lt;Button href="..."&gt;`-Elemente.
    - Kein `style={{...}}` auf der Seite.
    - `.hero-actions`-Klasse existiert in globals.css.
    - `.primary-link`- und `.primary-link:hover`-Regeln sind aus globals.css entfernt.
    - TypeScript-Compiler meldet keine Fehler in diesen Dateien.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Startseite mit vier Button-Primitiv-Links statt nativer Link-Klassen und
    CSS-Klasse `.hero-actions` statt Inline-Styles.
  </what-built>
  <how-to-verify>
    1. Dev-Server läuft auf http://127.0.0.1:3000 (Hot-Reload aktiv).
    2. Startseite http://127.0.0.1:3000 aufrufen.
    3. Prüfen: Vier Buttons "Zur Anime-Liste", "Zur Watchlist", "Anmelden", "Zu Admin" sind sichtbar und haben das primäre Button-Styling (blaue/akzentfarbene Hintergrundfarbe aus dem Design-System, nicht aus `.primary-link`).
    4. Jeden Button anklicken und prüfen, dass er zur richtigen Route navigiert (/anime, /watchlist, /login, /admin).
    5. Browser-DevTools → Elements: Buttons sind `<a>`-Tags (nicht `<button>`) mit Button-Design-System-Klassen. Kein `style`-Attribut auf dem Flex-Container.
  </how-to-verify>
  <resume-signal>Mit "approved" bestätigen oder Abweichungen beschreiben.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| keine | Reine Frontend-Styling-Änderung, keine Datengrenzen betroffen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-jb9-01 | Tampering | globals.css | accept | Nur CSS-Klassen-Umbenennung; kein sicherheitsrelevanter Pfad |
</threat_model>

<verification>
- TypeScript: `cd frontend && npx tsc --noEmit` — keine Fehler in page.tsx
- Grep-Check: `grep -r "primary-link" frontend/src/` gibt nur noch 0 Treffer (oder ausschließlich globals.css-Treffer, falls dort noch kommentierte Zeilen verbleiben)
- Grep-Check: `grep "style={{" frontend/src/app/page.tsx` gibt 0 Treffer
- Live: vier Buttons rendern korrekt auf http://127.0.0.1:3000
</verification>

<success_criteria>
- Keine `<Link className="primary-link">` mehr in page.tsx
- Keine Inline-Style-Props mehr auf der Startseite
- `.primary-link`-Regel in globals.css entfernt
- `.hero-actions`-Klasse in globals.css vorhanden
- Vier Button-Primitiv-Links navigieren korrekt
- TypeScript-Kompilierung fehlerfrei
</success_criteria>

<output>
Kein SUMMARY-File erforderlich (quick-Task). Commit-Message nach Abschluss:
`fix(startseite): Button-Primitiv statt nativer Link-Klassen, Inline-Styles entfernt`
</output>
