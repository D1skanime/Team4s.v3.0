---
phase: quick-260707-f3t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/models/member_profile.go
  - backend/internal/repository/member_profile_repository.go
  - backend/internal/repository/member_profile_repository_test.go
  - frontend/src/types/profile.ts
  - frontend/src/components/profile/RecentContributionsSection.tsx
  - frontend/src/components/profile/RecentContributionsSection.test.tsx
autonomous: true
requirements: [Q-01]
must_haves:
  truths:
    - "Der Fortschrittsbalken in Profil > Letzte Projekte zeigt eine absolute Metrik (worked/total Release-Versionen), nicht mehr eine relative Zahl die bei einem einzigen Projekt immer 100% ergibt (Q-01)"
    - "Reine Besetzung/Projektrolle ohne eigene Notiz oder eigenes Medium zaehlt NICHT als bearbeitet (Q-01)"
    - "Ein Member mit Notiz oder Medien-Upload auf 1 von 12 Release-Versionen sieht ca. 8% Balkenfuellung, nicht 100%"
  artifacts:
    - path: "backend/internal/models/member_profile.go"
      provides: "WorkedReleaseVersionCount + TotalReleaseVersionCount Felder auf MemberProfileRecentContribution"
    - path: "backend/internal/repository/member_profile_repository.go"
      provides: "loadRecentContributions liefert worked_release_version_count/total_release_version_count pro Projekt-Zeile"
    - path: "frontend/src/types/profile.ts"
      provides: "worked_release_version_count/total_release_version_count im MemberProfileRecentContribution Typ"
    - path: "frontend/src/components/profile/RecentContributionsSection.tsx"
      provides: "Balkenbreite = worked/total statt projectWorkUnits/maxWorkUnits"
  key_links:
    - from: "frontend/src/components/profile/RecentContributionsSection.tsx"
      to: "backend/internal/repository/member_profile_repository.go"
      via: "MemberProfileRecentContribution.worked_release_version_count / total_release_version_count aus /api/v1/... Profil-Response"
      pattern: "worked_release_version_count"
---

<objective>
Ersetze den irrefuehrenden relativen Fortschrittsbalken in der Profil-Sektion "Letzte Projekte"
(frontend/src/components/profile/RecentContributionsSection.tsx) durch eine echte absolute Metrik:
Anteil der Release-Versionen eines Projekts, bei denen der Member eine eigene Notiz ODER eigene
Medien hat. Reine Besetzung/Projektrolle zaehlt nicht als "gemacht" (Gamification-Anreiz).

Purpose: Der bisherige Balken (projectWorkUnits(item)/maxWorkUnits, basierend auf
release_version_count+episode_count) ist bei nur einem sichtbaren Projekt IMMER 100% und beruht
auf reinen Rollen-Credits statt auf konkreter Mitarbeit. Das taeuscht Fortschritt/Abschluss vor,
wo keiner ist.

Output: Backend liefert pro Projekt-Zeile `worked_release_version_count` (distinct Release-Versionen
mit eigener Notiz ODER eigenem Medien-Upload) und `total_release_version_count` (alle distinct
Release-Versionen des Projekts bei dieser Gruppe). Frontend rendert die Balkenbreite als
`worked/total * 100`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Aktuelles Model (backend/internal/models/member_profile.go, Z.54-67) -->
```go
type MemberProfileRecentContribution struct {
	ID                  int64    `json:"id"`
	AnimeTitle          string   `json:"anime_title"`
	AnimeID             int64    `json:"anime_id"`
	FansubGroupID       int64    `json:"fansub_group_id"`
	FansubGroupName     string   `json:"fansub_group_name"`
	FansubGroupNames    []string `json:"fansub_group_names"`
	RoleName            string   `json:"role_name"`
	RoleNames           []string `json:"role_names"`
	RoleLabel           string   `json:"role_label"`
	RoleLabels          []string `json:"role_labels"`
	ReleaseVersionCount int32    `json:"release_version_count"`
	EpisodeCount        int32    `json:"episode_count"`
}
```

<!-- Aktuelle Query backend/internal/repository/member_profile_repository.go Z.1093-1224
     (loadRecentContributions). project_rows exponiert bereits anime_id + fansub_group_id
     als Spalten (Z.1159-1176). Der finale SELECT (Z.1177-1192) liest daraus. -->

Aktuelle project_rows CTE (Z.1159-1176):
```sql
project_rows AS (
    SELECT
        anime_id AS id,
        anime_title,
        anime_id,
        fansub_group_id,
        (ARRAY_AGG(DISTINCT fansub_group_name ORDER BY fansub_group_name))[1] AS fansub_group_name,
        COALESCE(ARRAY_AGG(DISTINCT fansub_group_name ORDER BY fansub_group_name), ARRAY[]::text[]) AS fansub_group_names,
        (ARRAY_AGG(DISTINCT role_name ORDER BY role_name))[1] AS role_name,
        COALESCE(ARRAY_AGG(DISTINCT role_name ORDER BY role_name), ARRAY[]::text[]) AS role_names,
        (ARRAY_AGG(DISTINCT role_label ORDER BY role_label))[1] AS role_label,
        COALESCE(ARRAY_AGG(DISTINCT role_label ORDER BY role_label), ARRAY[]::text[]) AS role_labels,
        COUNT(DISTINCT release_version_id)::int AS release_version_count,
        COUNT(DISTINCT episode_id)::int AS episode_count,
        MAX(created_at) AS created_at
    FROM deduped
    GROUP BY anime_id, anime_title, fansub_group_id
)
SELECT
    id, anime_title, anime_id, fansub_group_id, fansub_group_name, fansub_group_names,
    role_name, role_names, role_label, role_labels, release_version_count, episode_count
FROM project_rows
ORDER BY created_at DESC
LIMIT 3
```

Scan-Reihenfolge im Go-Code (Z.1200-1218):
```go
var item models.MemberProfileRecentContribution
rows.Scan(
    &item.ID, &item.AnimeTitle, &item.AnimeID, &item.FansubGroupID,
    &item.FansubGroupName, &item.FansubGroupNames,
    &item.RoleName, &item.RoleNames, &item.RoleLabel, &item.RoleLabels,
    &item.ReleaseVersionCount, &item.EpisodeCount,
)
```

Aufrufer (zwei Stellen, memberID-only, publicOnly-Flag ist der bestehende zweite Parameter):
```go
base.RecentContributions, err = r.loadRecentContributions(ctx, base.MemberID, false)   // Z.83, eigenes Profil
profile.RecentContributions, loadErr = r.loadRecentContributions(ctx, row.memberID, true) // Z.507, oeffentliches Profil
```

<!-- Referenz-Definition "has_own_notes"/"has_own_media" aus
     backend/internal/repository/anime_contributions_member_project_repository.go Z.136-149
     (Projektansicht "starker blauer Button"). Dort wird has_own_media direkt mit appUserID
     verglichen (rvm.uploaded_by_user_id = $2), WEIL appUserID dort schon als Parameter vorliegt.
     In loadRecentContributions liegt appUserID NICHT vor -> ueber member_claims aufloesen,
     damit die Funktionssignatur (memberID, publicOnly) unveraendert bleibt: -->
```sql
EXISTS (
    SELECT 1 FROM release_version_notes rvn
    WHERE rvn.release_version_id = rv.id
      AND rvn.member_id = $1
      AND rvn.deleted_at IS NULL
) AS has_own_notes,
EXISTS (
    SELECT 1 FROM release_version_media rvm
    WHERE rvm.release_version_id = rv.id
      AND rvm.uploaded_by_user_id = $2   -- appUserID, dort bereits als Parameter vorhanden
      AND rvm.deleted_at IS NULL
) AS has_own_media
```

<!-- release_version_notes.deleted_at existiert (database/migrations/0064_release_version_notes.up.sql Z.18).
     release_version_media.deleted_at existiert (database/migrations/0059_release_version_media_schema.up.sql Z.16).
     member_claims (database/migrations/0081_historical_members_identity.up.sql Z.7-18):
       id, member_id, app_user_id, claim_status ('pending'|'verified'|'rejected'), verified_by, verified_at, created_at.
     -> app_user_id fuer einen Member ueber:
        SELECT mc.app_user_id FROM member_claims mc WHERE mc.member_id = $1 AND mc.claim_status = 'verified' -->

<!-- Aktuelles Frontend RecentContributionsSection.tsx (komplett, 137 Zeilen) -->
```tsx
type RecentContributionProject = MemberProfileRecentContribution & {
  fansub_group_names: string[]
  role_names: string[]
  role_labels: string[]
  release_version_count: number
  episode_count: number
}

function toRecentProjects(items: MemberProfileRecentContribution[]): RecentContributionProject[] {
  // ... merged-map ueber anime_id:fansub_group_id, akkumuliert release_version_count/episode_count via +=
}

function projectWorkUnits(item: RecentContributionProject): number {
  return Math.max(1, item.release_version_count + item.episode_count)
}

export function RecentContributionsSection({ items, canView, isPublicView = false }: RecentContributionsSectionProps) {
  const projects = toRecentProjects(items)
  const maxWorkUnits = Math.max(1, ...projects.map(projectWorkUnits))
  // ...
  const progressValue = Math.round((projectWorkUnits(item) / maxWorkUnits) * 100)
  // ...
  <div className={styles.projectProgress} aria-label={`Bearbeitungsumfang ${progressValue} Prozent`}>
    <span style={{ width: `${progressValue}%` }} />
  </div>
```

<!-- Aktueller Typ frontend/src/types/profile.ts Z.45-58 -->
```ts
export interface MemberProfileRecentContribution {
  id: number
  anime_title: string
  anime_id: number
  fansub_group_id: number
  fansub_group_name: string
  fansub_group_names?: string[]
  role_name: string
  role_names?: string[]
  role_label: string
  role_labels?: string[]
  release_version_count?: number
  episode_count?: number
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend — worked_release_version_count/total_release_version_count in loadRecentContributions</name>
  <files>backend/internal/models/member_profile.go, backend/internal/repository/member_profile_repository.go, backend/internal/repository/member_profile_repository_test.go</files>
  <behavior>
    Erweitere den source-invariant Test in member_profile_repository_test.go
    (TestMemberProfileRepositorySourceInvariants) um Assertions, die auf dem Query-Source-Text
    pruefen (Muster wie bestehende Zeilen 97-100), NICHT als Live-DB-Test:
    - Assert Query-Source enthaelt `worked_release_version_count` und `total_release_version_count`
      als Spaltennamen im finalen SELECT von project_rows.
    - Assert Query-Source enthaelt die has_own-Bedingung ueber `release_version_notes` UND
      `release_version_media` mit `deleted_at IS NULL` fuer beide.
    - Assert Query-Source enthaelt die member_claims-Aufloesung
      (`member_claims mc` mit `mc.claim_status = 'verified'`) fuer die Medien-Zuordnung
      (kein appUserID-Parameter in loadRecentContributions noetig).
    - Assert Scan-Reihenfolge in loadRecentContributions bindet exakt 2 neue Ziele
      (`&item.WorkedReleaseVersionCount`, `&item.TotalReleaseVersionCount`) nach den
      bestehenden Feldern.
    Fuehre den Test aus (`go test ./internal/repository/... -run TestMemberProfileRepositorySourceInvariants`)
    und bestaetige RED (neue Assertions schlagen fehl, weil die Query sie noch nicht enthaelt),
    bevor du implementierst.
  </behavior>
  <action>
    1. Model (backend/internal/models/member_profile.go, MemberProfileRecentContribution ~Z.54-67):
       Ergaenze zwei Felder nach EpisodeCount:
       `WorkedReleaseVersionCount int32 `json:"worked_release_version_count"``
       `TotalReleaseVersionCount int32 `json:"total_release_version_count"``

    2. Query in loadRecentContributions (member_profile_repository.go, ~Z.1093-1193):
       Die project_rows-CTE gruppiert bereits pro (anime_id, fansub_group_id) und hat Zugriff
       auf alle release_version_ids der deduped-CTE — die "worked/total" Metrik ist aber
       QUELLENUNABHAENGIG von der Rollen-Credit-Herkunft (release_member_roles vs.
       anime_contributions) und muss ALLE Release-Versionen der Gruppe/des Anime zaehlen,
       nicht nur die, zu denen ein Credit existiert. Daher zwei korrelierte Subqueries im
       finalen SELECT gegen project_rows (analog zur Diagnose), NICHT in der project_rows-CTE
       selbst, da project_rows nur ueber `deduped` (Credit-Zeilen) gruppiert:

       Finaler SELECT (ersetzt den bestehenden Block Z.1177-1192):
       ```
       SELECT
           id,
           anime_title,
           anime_id,
           fansub_group_id,
           fansub_group_name,
           fansub_group_names,
           role_name,
           role_names,
           role_label,
           role_labels,
           release_version_count,
           episode_count,
           (SELECT COUNT(DISTINCT rv.id) FROM release_versions rv
            JOIN release_version_groups rvg ON rvg.release_version_id = rv.id
            JOIN fansub_releases fr ON fr.id = rv.release_id
            JOIN episodes ep ON ep.id = fr.episode_id
            WHERE ep.anime_id = project_rows.anime_id
              AND rvg.fansub_group_id = project_rows.fansub_group_id)::int AS total_release_version_count,
           (SELECT COUNT(DISTINCT rv.id) FROM release_versions rv
            JOIN release_version_groups rvg ON rvg.release_version_id = rv.id
            JOIN fansub_releases fr ON fr.id = rv.release_id
            JOIN episodes ep ON ep.id = fr.episode_id
            WHERE ep.anime_id = project_rows.anime_id
              AND rvg.fansub_group_id = project_rows.fansub_group_id
              AND (
                EXISTS (
                  SELECT 1 FROM release_version_notes n
                  WHERE n.release_version_id = rv.id
                    AND n.member_id = $1
                    AND n.deleted_at IS NULL
                )
                OR EXISTS (
                  SELECT 1 FROM release_version_media m
                  WHERE m.release_version_id = rv.id
                    AND m.deleted_at IS NULL
                    AND m.uploaded_by_user_id IN (
                      SELECT mc.app_user_id FROM member_claims mc
                      WHERE mc.member_id = $1 AND mc.claim_status = 'verified'
                    )
                )
              ))::int AS worked_release_version_count
       FROM project_rows
       ORDER BY created_at DESC
       LIMIT 3
       ```
       `$1` ist bereits `memberID` (erster Query-Parameter), `$2` bleibt `publicOnly` — beide neuen
       Subqueries nutzen ausschliesslich `$1`, keine neuen Bind-Parameter noetig.

    3. Scan-Aufruf (~Z.1200-1218): Ergaenze nach `&item.EpisodeCount`:
       `&item.TotalReleaseVersionCount, &item.WorkedReleaseVersionCount,`
       (Reihenfolge MUSS exakt der SELECT-Spaltenreihenfolge entsprechen: total zuerst, dann worked,
       wie im SELECT oben).

    4. Kontextpruefung: member_profile_repository.go bleibt bewusst ueber dem 450-Zeilen-Limit
       (bereits 1378 Zeilen vor dieser Aenderung) — das ist eine bekannte Vorbedingung dieses Fixes,
       kein Split im Rahmen dieses Quick-Tasks (nur Query-Erweiterung, keine Struktur-Aenderung).

    5. Keine Aenderung an den beiden Aufrufstellen (Z.83, Z.507) noetig — Funktionssignatur bleibt
       `loadRecentContributions(ctx, memberID, publicOnly)`.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/repository/... -run TestMemberProfileRepositorySourceInvariants -v</automated>
  </verify>
  <done>go build erfolgreich, TestMemberProfileRepositorySourceInvariants gruen mit den neuen Assertions, Model und Query enthalten worked_release_version_count/total_release_version_count in korrekter Scan-Reihenfolge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frontend — absoluter Fortschrittsbalken (worked/total statt relativ)</name>
  <files>frontend/src/types/profile.ts, frontend/src/components/profile/RecentContributionsSection.tsx, frontend/src/components/profile/RecentContributionsSection.test.tsx</files>
  <behavior>
    Erweitere RecentContributionsSection.test.tsx:
    - Passe `makeContribution()` Default-Fixture um `worked_release_version_count: 1` und
      `total_release_version_count: 1` (damit bestehende Tests, die keine Balken-Prozentwerte
      pruefen, weiterhin gruen bleiben).
    - Neuer Test: Ein Projekt mit `worked_release_version_count: 1, total_release_version_count: 12`
      rendert eine Balkenbreite/aria-label von 8% (Math.round(1/12*100) = 8), NICHT 100%.
    - Neuer Test: Zwei Release-Version-Zeilen desselben Projekts mit identischem
      worked_release_version_count/total_release_version_count (Backend liefert diese Werte
      bereits projekt-aggregiert, nicht pro Zeile) werden beim Merge NICHT aufsummiert — der Wert
      wird einmal (vom ersten Vorkommen) uebernommen. Beispiel: zwei Rollen-Zeilen mit je
      worked=1/total=12 ergeben nach Merge weiterhin worked=1/total=12, nicht worked=2/total=24.
    - Neuer Test: `total_release_version_count: 0` rendert 0% (kein Division-durch-Null-Crash).
    Fuehre `npm test -- RecentContributionsSection` (Vitest) aus und bestaetige RED, bevor du
    RecentContributionsSection.tsx anpasst.
  </behavior>
  <action>
    1. Typ (frontend/src/types/profile.ts, MemberProfileRecentContribution Z.45-58): Ergaenze
       `worked_release_version_count?: number` und `total_release_version_count?: number`
       nach `episode_count?: number`.

    2. RecentContributionsSection.tsx:
       - `RecentContributionProject`-Type (Z.12-18): Ergaenze
         `worked_release_version_count: number` und `total_release_version_count: number`.
       - `toRecentProjects` (Z.27-62): Beim ERSTEN Vorkommen eines Projekts (im `else`-Zweig, Z.46-54)
         `worked_release_version_count: item.worked_release_version_count ?? 0` und
         `total_release_version_count: item.total_release_version_count ?? 0` uebernehmen.
         Im `existing`-Merge-Zweig (Z.37-44) die beiden Felder NICHT aufsummieren (im Unterschied
         zu release_version_count/episode_count, die weiterhin per `+=` akkumuliert werden) —
         die Backend-Werte sind bereits pro Projekt (anime_id+fansub_group_id) aggregiert, ein
         Aufsummieren ueber mehrere Rollen-Zeilen desselben Projekts wuerde falsch verdoppeln.
       - Entferne `projectWorkUnits` (Z.75-77) und die `maxWorkUnits`-Berechnung (Z.86) komplett —
         nicht mehr gebraucht.
       - Neue Hilfsfunktion `progressPercent(item: RecentContributionProject): number`:
         `return item.total_release_version_count > 0 ? Math.round((item.worked_release_version_count / item.total_release_version_count) * 100) : 0`.
       - Im Render (Z.96, 115): `progressValue` durch `progressPercent(item)` ersetzen.
       - aria-label (Z.115) aussagekraeftiger formulieren mit korrekten Umlauten, z.B.:
         `` `${item.worked_release_version_count} von ${item.total_release_version_count} Release-Versionen bearbeitet` ``
         (bei total=0: `Noch keine Release-Versionen vorhanden` als Fallback-Label, um "0 von 0" zu vermeiden).
    3. Keine Aenderung an `formatProjectStats` (release_version_count/episode_count-Anzeige bleibt
       als Zusatzinfo bestehen, ist von dieser Metrik unabhaengig).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/profile/RecentContributionsSection.test.tsx</automated>
  </verify>
  <done>Vitest gruen; Balken/aria-label zeigen worked/total-Verhaeltnis (z.B. 1/12 -> 8%), kein Fallback mehr auf "immer 100% bei einem Projekt"; kein Crash bei total=0; Merge summiert worked/total nicht ueber mehrere Rollen-Zeilen eines Projekts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend SQL query (member_id-scoped) | Neue Subqueries nutzen ausschliesslich den bereits authentifizierten `memberID` ($1) und `claim_status='verified'` — kein neuer Eingabe-Trust-Boundary-Uebertritt. |
| API Response -> Frontend | worked/total-Zahlen sind reine Integer-Aggregate ohne Freitext; kein XSS-Risiko. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260707f3t-01 | Information Disclosure | loadRecentContributions worked/total Subqueries | accept | Zaehlt nur aggregierte Counts fuer den bereits autorisierten eigenen memberID-Scope (kein Cross-Member-Leak, da $1 = eingeloggter/aufgerufener Member); keine PII in den neuen Feldern. |
| T-260707f3t-02 | Tampering | Frontend progressPercent Division | mitigate | Guard gegen total_release_version_count=0 verhindert NaN/Infinity-Rendering (Task 2, Schritt 2). |
</threat_model>

<verification>
1. `cd backend && go build ./... && go test ./internal/repository/... -run TestMemberProfileRepositorySourceInvariants -v` — gruen.
2. `cd frontend && npx vitest run src/components/profile/RecentContributionsSection.test.tsx` — gruen.
3. Manuelle Live-Pruefung (Orchestrator nach Backend-Rebuild + Frontend-Restart):
   Profil sheppert (member_id 2) unter :3000/me/profile aufrufen, Sektion "Letzte Projekte"
   zeigt fuer das Projekt mit 1 von 12 bearbeiteten Release-Versionen einen Balken bei ca. 8%,
   nicht 100%.
</verification>

<success_criteria>
- Backend liefert `worked_release_version_count`/`total_release_version_count` korrekt aggregiert
  pro Projekt (anime_id+fansub_group_id), basierend auf eigener Notiz ODER eigenem Medien-Upload,
  unabhaengig von reiner Rollen-Besetzung.
- Frontend-Balken zeigt eine absolute Prozentzahl (worked/total), die bei einem einzelnen sichtbaren
  Projekt NICHT automatisch 100% ist.
- Keine Regression bestehender Tests (release_version_count/episode_count-Anzeige, Merge-Logik,
  Public/Private-View-Unterscheidung).
- Deutsche user-facing Strings (aria-label) mit korrekten Umlauten.
</success_criteria>

<output>
Create `.planning/quick/260707-f3t-profil-letzte-projekte-fortschrittsbalke/260707-f3t-SUMMARY.md` when done
</output>
