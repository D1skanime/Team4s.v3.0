# Context

The Anime edit Basisdaten card forced six unrelated fields into one responsive grid row. On the current width, Titel EN wrapped onto a separate line and the field relationships were unclear.

Decision: preserve all existing controls and values, but group them by meaning: main title, classification selects, numeric season metadata, and language titles.
