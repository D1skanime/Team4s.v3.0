---
phase: quick-260812-kr1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/profile/MemberBadgeChain.module.css
  - frontend/src/components/profile/MemberBadgeChain.test.tsx
autonomous: false
requirements: [260812-kr1-scope]
must_haves:
  truths:
    - "Rollenfortschritt, Anime-Projekte, Punkte-Meilensteine und Mitgliedschaft zeigen Artwork, Titel, Status, Werte, Fortschritt und Tracks direkt auf der bestehenden hellblauen Sektionsfläche, ohne große weiße Hero-Innenkarte."
    - "Beiträge bleibt mit der bereits durch Commit 5d23e95f transparenten Haupt-Stage unverändert konform; kleine ausgewählte, gesperrte und besondere Statusflächen bleiben in allen Bereichen sichtbar."
    - "Carousel-, Track-, Thumbnail-, Button- und Tastaturinteraktionen sowie alle Daten, Schwellenwerte und Berechnungen bleiben unverändert."
    - "Die öffentliche Profilseite hat bei 390x844, 768x1024, 1024x768, 1440x900 und 1920x1080 keinen horizontalen Seitenüberlauf, keine Überlagerung und keine abgeschnittenen Inhalte."
  artifacts:
    - {path: "frontend/src/components/profile/MemberBadgeChain.module.css", provides: "eine profile-lokale, gemeinsame transparente Achievement-Stage-Regel mit minimalen bereichsspezifischen Overrides"}
    - {path: "frontend/src/components/profile/MemberBadgeChain.test.tsx", provides: "DOM-, CSS-, Interaktions- und Responsive-Regressionsverträge für alle fünf Zielbereiche"}
  key_links:
    - {from: "frontend/src/components/profile/MemberBadgeChain.tsx", to: "frontend/src/components/profile/MemberBadgeChain.module.css", via: "bestehende Klassen roleBadgeRow, animeProjectStage, pointsAchievementStage, contributionAchievementStage und membershipStage"}
    - {from: "frontend/src/components/profile/MemberBadgeChain.module.css", to: "frontend/src/components/ui/FocalCarousel.tsx", via: "nur bestehende Consumer-Klassen; Shared-Carousel bleibt unberührt"}
---

<objective>
Remove the large white inner Hero Card surfaces from the public profile achievement sections while preserving the outer section canvas and every useful content or interaction surface.

Purpose: End the nested “section → white card → content” appearance without redesigning achievements or changing behavior.
Output: Focused CSS/DOM regression contracts, one local transparent-stage treatment, five-size visual evidence, and a blocking human approval.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
<read_first>
- AGENTS.md
- docs/engineering/implementation-contract.md
- docs/frontend/ui-system.md
- docs/agent-guidelines-ui.md
- frontend/src/components/ui/Card.tsx
- frontend/src/components/ui/Card.module.css
- frontend/src/components/profile/MemberBadgeChain.tsx
- frontend/src/components/profile/MemberBadgeChain.module.css
- frontend/src/components/profile/MemberBadgeChain.test.tsx
- .planning/quick/260812-jtp-public-member-profile-vertical-spacing-r/260812-jtp-PLAN.md
- git commit 5d23e95f (contribution stage precedent and current baseline)
</read_first>

<interfaces>
Technical owners already mapped: `MemberBadgeChain.tsx` renders role Heroes through `Card.roleBadgeRow`, Anime-Projekte through `Card.animeProjectStage`, Punkte-Meilensteine through `Card.pointsAchievementStage`, Beiträge through `Card.contributionAchievementStage`, and Mitgliedschaft through `Card.membershipStage`. All surface ownership is local to `MemberBadgeChain.module.css`; no shared `Card` or `FocalCarousel` change is required.

Existing compliant precedent from 5d23e95f: `.contributionAchievementStage` retains its layout/padding but explicitly sets `border: 0`, `border-radius: 0`, `background: transparent`, and `box-shadow: none`; inactive contribution previews retain their separate compact behavior.

Protected small surfaces include progress tracks/remainder panels, role stages, selected/current chips, locked artwork/stages, milestone and duration tracks, founding-member panel, thumbnail buttons, focus rings, carousel controls and show-all buttons.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the five-section surface and interaction contract</name>
  <files>frontend/src/components/profile/MemberBadgeChain.test.tsx</files>
  <behavior>
    - A focused CSS contract fails unless the main wrappers for `roleBadgeRow`, `animeProjectStage`, `pointsAchievementStage`, `contributionAchievementStage`, and `membershipStage` all resolve to borderless, radius-free, transparent and shadow-free surfaces; it must also reject a later responsive rule that restores white Card chrome.
    - DOM tests prove the same existing titles, artwork slots, chips/states, values, progressbars, remainder copy, stage/thumbnail tracks, carousel regions/counters and “Alle Auszeichnungen … anzeigen” controls remain present where applicable.
    - Interaction tests keep selection preview/current/locked semantics and existing contribution/role carousel navigation behavior; no implementation logic changes are introduced to satisfy them.
    - Responsive CSS contracts preserve `min-width: 0`, bounded media, existing narrow stacking and container-query ownership, and do not introduce document-level horizontal clipping as a workaround.
  </behavior>
  <action>From `/home/d1sk/team4s`, first capture the exact baseline (`git status --short`, HEAD, working diff, cached diff, and blob hashes) for both files under this Quick's `evidence/incoming/`. The shared tree is dirty and `MemberBadgeChain.test.tsx` already contains unrelated unstaged work; preserve it byte-for-byte outside this Quick's exact hunks. Add one clearly named Quick 260812-kr1 test block using the established `memberBadgeChainCss` fixture and existing render helpers. Record the intentional RED result in `evidence/RED.txt`. Treat Beiträge as an already-compliant regression target from 5d23e95f, not as a fresh redesign. Do not alter snapshots or weaken/skip existing tests. Do not touch TSX, `Card`, `FocalCarousel`, `FansubProjectsGrid`, page/header code, backend, API, database, badge logic, thresholds, calculations, membership logic or assets.</action>
  <verify>
    <automated>cd /home/d1sk/team4s/frontend &amp;&amp; npm test -- --run src/components/profile/MemberBadgeChain.test.tsx</automated>
  </verify>
  <done>The new tests fail only because the four remaining large Hero wrappers still expose Card chrome, while contributions and all protected content/interaction contracts are explicitly represented.</done>
</task>

<task type="auto">
  <name>Task 2: Apply one local transparent Achievement-Stage treatment</name>
  <files>frontend/src/components/profile/MemberBadgeChain.module.css</files>
  <action>Implement the smallest CSS-only change in `MemberBadgeChain.module.css`. Reuse the contribution precedent by defining one final profile-local grouped surface rule for `.roleBadgeRow`, `.animeProjectStage`, `.pointsAchievementStage`, `.contributionAchievementStage`, and `.membershipStage` that removes only the large wrapper's `border`, `border-radius`, `background`, and `box-shadow`; set wrapper overflow to `visible` only where required to eliminate the frame without clipping focus/artwork. Place the rule after responsive variants so desktop, wide, tablet and mobile cannot restore Card chrome. Retain the existing grid/flex composition, padding or tokenized content spacing, max widths, artwork sizes, progress/remainder blocks, track separators, selected/current/locked treatments, founding-member panel, focus styles and carousel peeking. If a wrapper needs a narrowly different overflow rule, keep that exception adjacent and document why; do not create five separate redesigns. Leave `.chainCard` and the outer light-blue/sectional surface intact. Leave the already-compliant contribution inactive-preview rules from 5d23e95f unchanged. Use targeted hunks only and do not reformat the large CSS file.</action>
  <verify>
    <automated>cd /home/d1sk/team4s/frontend &amp;&amp; npm test -- --run src/components/profile/MemberBadgeChain.test.tsx &amp;&amp; npm run typecheck &amp;&amp; npm run lint -- --file src/components/profile/MemberBadgeChain.tsx &amp;&amp; cd /home/d1sk/team4s &amp;&amp; git diff --check</automated>
  </verify>
  <done>All five main Hero wrappers use the shared transparent treatment, Beiträge remains behaviorally unchanged, all focused tests/checks pass, and no protected small surface or interaction was removed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve the cardless Achievement sections at five exact sizes</name>
  <files>none (live verification and evidence only)</files>
  <what-built>The public profile Achievement Heroes now sit directly on the existing light-blue section canvas; only the redundant large white wrapper chrome is removed.</what-built>
  <action>Rebuild or force-recreate only `team4sv30-frontend` if required and confirm it is Up. Open the populated Sheppert public profile through the shared Codex in-app browser at `http://127.0.0.1:3300`. Capture full-page and focused Achievement screenshots under this Quick's `evidence/uat/` at exactly 390x844, 768x1024, 1024x768, 1440x900 and 1920x1080, plus a measurement manifest. Headless automation may support the review but cannot replace this live shared-flow checkpoint. Coordinate visually with the open spacing Quick `260812-jtp`: do not change its spacing scope, do not create or overwrite its evidence/SUMMARY/STATE row, and do not claim it complete.</action>
  <how-to-verify>
1. Inspect Rollenfortschritt, Anime-Projekte, Punkte-Meilensteine, Beiträge and Mitgliedschaft in the populated Sheppert profile at all five exact viewport sizes.
2. Confirm the outer light-blue/sectional canvas remains, while no large white rectangle, border, rounded frame or shadow surrounds the complete Hero content in any target section.
3. Confirm artwork, titles, chips (`Aktuell`, `Vorschau`, `Gesperrt`, `Erreicht`), values, percentages, remainder text, progress bars and all tracks/thumbnails remain readable and aligned.
4. Exercise role and contribution carousel arrows, swipe/drag where available, keyboard navigation and show-all buttons; exercise selectable earned stages and confirm locked stages remain non-interactive.
5. Confirm the small selected/current/locked/special surfaces and founding-member panel remain intentional and visually distinct; the requirement is not a fully cardless page.
6. Record `document.documentElement.scrollWidth &lt;= document.documentElement.clientWidth` at every size and inspect for overlap, clipping, letterwise wrapping and focus-ring clipping.
7. Compare against the incoming baseline and `5d23e95f`: Beiträge must not regress, `FocalCarousel`/`FansubProjectsGrid`/header specials must remain untouched, and the open JTP spacing work must remain independently resumable.
8. Inspect exact staged hunks before approval: only this Quick's CSS/test hunks may be staged; pre-existing dirty/index state must remain unchanged.
  </how-to-verify>
  <verify>
    <automated>cd /home/d1sk/team4s &amp;&amp; docker compose ps team4sv30-frontend &amp;&amp; for size in 390x844 768x1024 1024x768 1440x900 1920x1080; do test -f ".planning/quick/260812-kr1-ffentliche-profilseite-gro-e-wei-e-innen/evidence/uat/public-member-profile-inner-cards-${size}.png" || exit 1; grep -E "${size}.*(overflow.*PASS|scrollWidth.*clientWidth)" .planning/quick/260812-kr1-ffentliche-profilseite-gro-e-wei-e-innen/evidence/uat/MANIFEST.md || exit 1; done &amp;&amp; git diff --check</automated>
  </verify>
  <done>The user approves all five target sections at all five sizes, confirms only the redundant large white inner Cards disappeared, and all content, small semantic surfaces and interactions remain intact without horizontal overflow.</done>
  <resume-signal>Stop and wait for exact standalone `approved`; any other response is implementation feedback. Do not create SUMMARY.md, update STATE.md, commit code/docs, or finalize this Quick before that exact approval.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Dirty shared tree/index → Quick hunks | Existing profile tests, assets, FocalCarousel work and planning artifacts belong to other work and must not be overwritten or staged. |
| Large Card wrapper → small semantic surfaces | Removing wrapper chrome must not flatten selected, locked, progress, remainder, track or special surfaces. |
| Local BadgeChain CSS → shared UI primitives | A profile-only presentation change must not alter global `Card`, shared `FocalCarousel` or unrelated consumers. |
| Responsive component → document flow | Transparent wrappers must not cause clipping, overlap or horizontal page overflow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-kr1-01 | T | dirty worktree/index | mitigate | Incoming hashes/diffs, exact targeted patches, hunk-only staging, cached/working diff audit and fail-closed overlap handling. |
| T-kr1-02 | T | small semantic surfaces | mitigate | CSS/DOM tests explicitly retain status, locked, selected, track, remainder and special/founding surfaces. |
| T-kr1-03 | D | responsive profile flow | mitigate | Existing container ownership plus five exact viewport captures and scrollWidth/clientWidth measurements. |
| T-kr1-04 | R | visual acceptance | mitigate | Blocking exact `approved` checkpoint before summary/state/commit. |
| T-kr1-05 | I | public profile data | accept | Presentation-only CSS/test scope; no API, auth, backend, database or persisted-data mutation. |
| T-kr1-06 | E | shared component scope | mitigate | Explicitly prohibit edits to Card, FocalCarousel, FansubProjectsGrid, headers and non-profile consumers. |
</threat_model>

<verification>
Run the focused MemberBadgeChain suite, frontend typecheck, scoped lint and `git diff --check`. Perform live in-app-browser UAT on the populated Sheppert profile at 390x844, 768x1024, 1024x768, 1440x900 and 1920x1080 with explicit overflow measurements and interaction checks. Inherited failures must be recorded truthfully, never relabeled as PASS. Human approval is mandatory and blocks SUMMARY.md, STATE.md and all commits.
</verification>

<success_criteria>
- Large white Hero wrapper background, border, radius and shadow are absent from Rollenfortschritt, Anime-Projekte, Punkte-Meilensteine, Beiträge and Mitgliedschaft.
- The outer light-blue/sectional canvas, all content, progress/remainder information, tracks/thumbnails, small semantic surfaces and interactions remain intact.
- Beiträge preserves the already-compliant 5d23e95f implementation; already-compliant sections receive no redundant redesign.
- No horizontal page overflow, overlap or clipping appears at any of the five exact target sizes.
- No backend/API/DB/logic/threshold/calculation/membership/FocalCarousel/FansubProjectsGrid/header-special change occurs.
- Only exact Quick hunks are staged; all pre-existing dirty and indexed work remains unchanged.
- The open `260812-jtp` spacing Quick remains open and is neither duplicated nor falsely finalized.
- The user gives exact standalone `approved` before summary, state update or commit.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Task | Status | Notes
GOAL | none | Remove redundant large white Hero Cards while retaining outer section canvases | 1-3 | COVERED | Shared local CSS treatment plus live visual approval
REQ | kr1-01 | Cover Rollenfortschritt, Anime-Projekte, Punkte-Meilensteine, Beiträge and Mitgliedschaft | 1-3 | COVERED | Contributions is regression-only because 5d23e95f already complies
REQ | kr1-02 | Remove only main background, border, shadow, radius and frame | 1-2 | COVERED | Explicit grouped wrapper contract
REQ | kr1-03 | Retain all artwork, copy, values, progress, remainder, tracks, thumbnails and small surfaces | 1-3 | COVERED | DOM/CSS tests and UAT
REQ | kr1-04 | Retain carousel, buttons, selection, locked states and keyboard behavior | 1-3 | COVERED | Interaction regressions plus live exercise
REQ | kr1-05 | No overflow on mobile, tablet, desktop and wide desktop | 1-3 | COVERED | Five exact sizes and manifest
REQ | kr1-06 | Reuse central/local pattern with minimal section-specific CSS | 2 | COVERED | One grouped final profile-local rule; exceptions only if technically necessary
REQ | kr1-07 | Exclude backend/API/DB/logic/shared carousel/grid/header changes | 1-3 | COVERED | Explicit protected scope
CONTEXT | screenshot/PRD | Section is main canvas; Cards only separate genuinely distinct content | 1-3 | COVERED | Small semantic surfaces deliberately retained
CONTEXT | coordination | Respect open spacing Quick 260812-jtp and commit 5d23e95f | 1-3 | COVERED | Baseline, no duplicate contribution redesign, no false finalization
RESEARCH | existing code map | MemberBadgeChain CSS owns all target wrapper surfaces | 1-2 | COVERED | Level 0 discovery; no dependency or shared primitive needed
Deferred Ideas | explicit exclusions | Any broader profile redesign, spacing rewrite, shared carousel change or special header change | none | EXCLUDED | Must not be implemented
</source_audit>

<output>After exact standalone approval only, create `.planning/quick/260812-kr1-ffentliche-profilseite-gro-e-wei-e-innen/260812-kr1-SUMMARY.md`, update `.planning/STATE.md` Quick Tasks Completed, and commit only exact Quick hunks plus its artifacts. Until then, stop at the checkpoint.</output>
