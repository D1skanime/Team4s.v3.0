---
phase: quick-260901-la2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/handlers/phase136_narrow_role_defaults_enforcement_test.go
  - .planning/STATE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "TestPhase136NarrowRoleDefaultsSeedToHandlerContract reads database/migrations/0146_capability_policy_catalog.up.sql from the true repo root while every other read in the file keeps resolving relative to backend/"
    - "The misleadingly-named phase136RepositoryRoot helper no longer exists; a correctly-named phase136BackendRoot (unchanged logic) and a new phase136RepoRoot (one level higher) exist instead"
    - "STATE.md's frontmatter and Current Position / Session Continuity sections state 8 total phases with 7 completed and Phase 143 as the ready-to-plan current position, matching v1.4-MILESTONE-AUDIT.md's passed 7/7 status, with internally consistent (non-contradictory) plan counts"
  artifacts:
    - path: "backend/internal/handlers/phase136_narrow_role_defaults_enforcement_test.go"
      provides: "phase136BackendRoot (renamed, same logic) + new phase136RepoRoot helper, migration read repointed to phase136RepoRoot"
    - path: ".planning/STATE.md"
      provides: "corrected frontmatter progress counts and Current Position / Session Continuity narrative reflecting v1.4 complete + Phase 143 unplanned"
  key_links:
    - from: "TestPhase136NarrowRoleDefaultsSeedToHandlerContract migration read"
      to: "database/migrations/0146_capability_policy_catalog.up.sql (true repo root)"
      via: "filepath.Join(phase136RepoRoot(t), \"database/migrations/0146_capability_policy_catalog.up.sql\")"
      pattern: "phase136RepoRoot"
    - from: ".planning/STATE.md frontmatter progress"
      to: ".planning/v1.4-MILESTONE-AUDIT.md (status: passed, phases: 7/7)"
      via: "progress.completed_phases == 7 and Current Position names Phase 143"
      pattern: "completed_phases: 7"
---

<objective>
Two independent, mechanical fixes, each its own atomic commit:

1. Fix the misleadingly-named `phase136RepositoryRoot` test helper in `backend/internal/handlers/phase136_narrow_role_defaults_enforcement_test.go` — it actually resolves to `backend/`, not the repo root, which only stays invisible today because the local dev Docker Compose mount happens to place the true repo-root `database/migrations/` directory at both `/app/database/migrations` (docker-compose.yml line 165) and `/database/migrations` (docker-compose.override.yml). Outside that specific dev-container mount shape (plain `go test` on a checkout, or a differently-mounted container), the migration-file read in this test resolves under `backend/database/migrations/`, which does not contain `0146_capability_policy_catalog.up.sql` (only 3 unrelated files live there) — the test is one Docker-mount coincidence away from red. Renaming the helper and adding a correctly-scoped second helper for the one read that genuinely needs the repo root removes that fragility.
2. Correct `.planning/STATE.md`'s frontmatter and narrative sections, which have been stale since 2026-08-26 and now contradict `.planning/v1.4-MILESTONE-AUDIT.md` (status: passed, 7/7 phases, audited 2026-09-01) and `.planning/ROADMAP.md` (Phase 143 added 2026-09-01, not yet planned). The current `progress.completed_plans: 137` also exceeds `progress.total_plans: 83`, which is internally impossible.

Purpose: Remove a fragile, misleadingly-named test helper before Phase 143 work begins on top of it, and make STATE.md — the file every future GSD session reads first — truthful again.
Output: Two commits on `main`, in the order below.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/v1.4-MILESTONE-AUDIT.md
@.planning/ROADMAP.md

<!-- All Go tooling runs inside the running backend container (go is not on the host PATH). -->
<!-- Confirmed via docker compose ps: team4sv30-backend is already up. -->
</context>

<interfaces>
<!-- Ground truth already established during planning by actually reading/running things. Do not re-derive from scratch; re-run only to confirm nothing drifted since planning. -->

**Task 1 — verified root cause (docker inspect team4sv30-backend --format '{{json .Mounts}}'):**
- `docker-compose.yml:165` mounts `./database/migrations:/app/database/migrations:ro` (true repo root -> container's `/app/database/migrations`, overlaying whatever the image itself has baked in at that path).
- `docker-compose.override.yml` separately mounts `./database/migrations:/database/migrations:ro` (true repo root -> container's `/database/migrations`, an absolute root path).
- `backend/database/migrations/` (the actual on-disk directory `phase136RepositoryRoot(t)` + `"database/migrations/..."` would resolve to without either mount) contains only 3 files and does NOT contain `0146_capability_policy_catalog.up.sql` — confirmed via `ls backend/database/migrations/`.
- The true repo root `database/migrations/0146_capability_policy_catalog.up.sql` exists (243 lines) — confirmed via `git ls-files database/migrations/0146_capability_policy_catalog.up.sql`.
- `go test` run today inside `team4sv30-backend` currently PASSES only because of the `docker-compose.yml:165` mount coincidence described above, not because the helper's path logic is actually correct.

**Task 1 exact current file structure** (`backend/internal/handlers/phase136_narrow_role_defaults_enforcement_test.go`, verified line numbers as of planning time — locate by content if they've shifted):

```go
// line 21-30
func TestPhase136NarrowRoleDefaultsSeedToHandlerContract(t *testing.T) {
	root := phase136RepositoryRoot(t)
	read := func(path string) string {
		content, err := os.ReadFile(filepath.Join(root, path))
		require.NoError(t, err)
		return string(content)
	}

	migration := read("database/migrations/0146_capability_policy_catalog.up.sql")
	permissionsSource := read("internal/permissions/permissions.go")
```

```go
// line 68 (inside TestPhase136NarrowRoleDefaultsPatchClasses)
	source, err := os.ReadFile(filepath.Join(phase136RepositoryRoot(t), "internal/handlers/fansub_groups.go"))
```

```go
// line 91 (inside TestPhase136HistoryPatchAuthorizesBeforeEventSpecificProbes)
	source, err := os.ReadFile(filepath.Join(phase136RepositoryRoot(t), "internal/handlers/fansub_group_history_handler.go"))
```

```go
// line 130-135
func phase136RepositoryRoot(t testing.TB) string {
	t.Helper()
	_, filename, _, ok := runtime.Caller(0)
	require.True(t, ok)
	return filepath.Clean(filepath.Join(filepath.Dir(filename), "..", ".."))
}
```

Repo-wide grep confirms `phase136RepositoryRoot` is used ONLY in this one file (4 occurrences: lines 22, 68, 91, 130) — safe to rename without touching any other file.

**Task 2 — verified real counts** (from `find .planning/phases -iname "*-PLAN.md"` / `*-SUMMARY.md`, scoped to the current milestone v1.4's phases 136-143, matching how the existing `progress.total_phases: 7` / `completed_phases: 6` values were already scoped before going stale):

| Phase | PLAN.md count | SUMMARY.md count |
|---|---:|---:|
| 136 | 31 | 31 |
| 137 | 14 | 14 |
| 138 | 18 | 18 |
| 139 | 10 | 10 |
| 140 | 3 | 3 |
| 141 | 7 | 7 |
| 142 | 1 | 1 |
| 143 | 0 | 0 (not yet planned) |
| **Total** | **84** | **84** |

`total_plans: 84`, `completed_plans: 84` (every planned plan in v1.4 has a summary; Phase 143 has zero plans because it is not yet planned — this is not a contradiction, unlike the current `137 > 83`).

`progress.percent` tracks **phases** (confirmed: the current stale `total_phases:7`/`completed_phases:6` -> `percent:86` is `round(6/7*100)=86`, not a plans-based calc, since plans were already self-contradictory). New value: `round(7/8*100) = round(87.5) = 88`.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix the phase136 test's misleadingly-named repo-root helper</name>
  <files>backend/internal/handlers/phase136_narrow_role_defaults_enforcement_test.go</files>
  <action>
    Rename `phase136RepositoryRoot` to `phase136BackendRoot` everywhere in this file (the function definition at line 130 and all 3 call sites at lines 22, 68, 91) — do not change the function's body/logic; it must still return `filepath.Clean(filepath.Join(filepath.Dir(filename), "..", ".."))`, i.e. still resolves to `backend/`. Immediately after the renamed `phase136BackendRoot` function, add a new helper `phase136RepoRoot(t testing.TB) string` that returns one directory level higher than `phase136BackendRoot(t)` (`filepath.Clean(filepath.Join(phase136BackendRoot(t), ".."))`), with its own `t.Helper()` call. Then, in `TestPhase136NarrowRoleDefaultsSeedToHandlerContract`, change ONLY the migration-file read: replace the line `migration := read("database/migrations/0146_capability_policy_catalog.up.sql")` with a direct read that does not go through the `root`-scoped `read` closure — read `filepath.Join(phase136RepoRoot(t), "database/migrations/0146_capability_policy_catalog.up.sql")` via `os.ReadFile`, `require.NoError(t, err)`, and assign the string result to a variable named `migration` (keep the same variable name so every downstream reference in the function — `require.Contains(t, migration, ...)` — stays unchanged). Leave the `root := phase136BackendRoot(t)` line and the `read` closure exactly as they are (still used for `permissionsSource` and all the handler-file reads in the `cases` slice) — do not repoint any read other than the migration one. Leave lines 68 and 91 as plain renames (`phase136RepositoryRoot(t)` -> `phase136BackendRoot(t)`), no other change.
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-backend sh -lc "cd /app && /usr/local/go/bin/go test ./internal/handlers/ -run 'TestPhase136NarrowRoleDefaultsSeedToHandlerContract|TestPhase136NarrowRoleDefaultsPatchClasses|TestPhase136HistoryPatchAuthorizesBeforeEventSpecificProbes' -v" | grep -c '^--- FAIL'</automated>
  </verify>
  <done>The verify command's grep count is 0 (no FAIL lines); the same command's tail shows `ok  	team4s.v3/backend/internal/handlers`. `grep -c phase136RepositoryRoot backend/internal/handlers/phase136_narrow_role_defaults_enforcement_test.go` reports 0. `docker compose exec -T team4sv30-backend sh -lc "cd /app && /usr/local/go/bin/go build ./..."` succeeds with no output. Commit only this file with message `fix(136): resolve phase136 test migration read from true repo root, not backend/`.</done>
</task>

<task type="auto">
  <name>Task 2: Correct stale STATE.md frontmatter and Current Position / Session Continuity</name>
  <files>.planning/STATE.md</files>
  <action>
    Update the YAML frontmatter (lines 1-15) in place: set `status: ready_to_plan` (unchanged value, still accurate), `stopped_at: "Milestone v1.4 complete (7/7 phases passed) — Phase 143 ready to plan"`, `last_updated:` to the current UTC timestamp in the same `YYYY-MM-DDTHH:MM:SS.sssZ` format as the existing value (run `date -u +"%Y-%m-%dT%H:%M:%S.000Z"` to get it), `last_activity: 2026-09-01`, and replace the entire `progress:` block with `total_phases: 8`, `completed_phases: 7`, `total_plans: 84`, `completed_plans: 84`, `percent: 88` (these are the real current counts — see the interfaces block's verified table; do not invent different numbers). Then update the `## Current Position` section (currently `Phase: 142` / `Plan: Not started` / `Status: Ready to plan` / a `Last activity:` line naming quick task 260901-jk5) to: `Phase: 143`, `Plan: Not started`, `Status: Ready to plan`, and a `Last activity:` line reading `2026-09-01 - Milestone v1.4 audited and passed (41/41 requirements, 7/7 phases, see .planning/v1.4-MILESTONE-AUDIT.md); Phase 143 (Phase-142-Nacharbeit und Dashboard-Lane für abgelehnte Notizen) added to ROADMAP.md, not yet planned`. Also update the `**Current focus:**` line under `## Project Reference` (currently `Phase 142 — integrated security, fixtures & live release gate`) to `Phase 143 — Phase-142-Nacharbeit und Dashboard-Lane für abgelehnte Notizen (not yet planned)`, since it directly restates the same stale Phase-142-as-current claim the Current Position section is being corrected to fix. Finally, update the `## Session Continuity` section near the end of the file (currently `Last session: 2026-08-26T10:00:08.703Z` / `Stopped at: Completed 141-06-PLAN.md` / `Last activity: 2026-08-20 - Completed Phase 134 Plan 06...` / `Resume file: None`) to: `Last session:` set to the same current UTC timestamp used above, `Stopped at: Milestone v1.4 complete (7/7 phases passed) — Phase 143 ready to plan`, `Last activity: 2026-09-01 - Milestone v1.4 audit passed (41/41 requirements, 7/7 phases, .planning/v1.4-MILESTONE-AUDIT.md); Phase 143 added to roadmap, not yet planned`, `Resume file: None`. Do not touch any other section of STATE.md (the Decisions log, Roadmap Evolution list, or the plan-duration metrics table further down all stay exactly as they are).
  </action>
  <verify>
    <automated>grep -c "completed_phases: 7" .planning/STATE.md && grep -c "^Phase: 143$" .planning/STATE.md</automated>
  </verify>
  <done>Frontmatter reads `total_phases: 8` / `completed_phases: 7` / `total_plans: 84` / `completed_plans: 84` / `percent: 88` with no internal contradiction (completed_plans never exceeds total_plans). `## Current Position` names Phase 143 with status "Ready to plan". `## Session Continuity`'s `Stopped at:` / `Last activity:` lines both describe the actual v1.4-complete-Phase-143-unplanned state instead of the stale Phase-141/Phase-134 references. Commit only this file with message `docs: correct stale STATE.md frontmatter after v1.4 milestone completion`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan touches a test-only helper function and a planning-documentation file. No production code path, input boundary, or auth surface is created or altered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick260901la2-01 | Tampering | phase136 test helper path resolution | accept | Test-only code; a wrong path only fails/passes the test itself, never affects production request handling. Fix makes the test's own guarantee (migration content matches handler enforcement) robust to environment, which is a correctness improvement, not a new surface. |
| T-quick260901la2-02 | Repudiation | STATE.md progress/session narrative | accept | Pure documentation; no runtime or audit-log behavior depends on this file's content. |

No package installs in this plan (no Package Legitimacy Gate applicable).
</threat_model>

<verification>
After both commits land, from the repo root:

1. `docker compose exec -T team4sv30-backend sh -lc "cd /app && /usr/local/go/bin/go test ./internal/handlers/ -run TestPhase136 -v"` — all `TestPhase136*` subtests pass, zero `--- FAIL` lines.
2. `docker compose exec -T team4sv30-backend sh -lc "cd /app && /usr/local/go/bin/go build ./..."` — succeeds, no output.
3. `grep -c phase136RepositoryRoot backend/internal/handlers/phase136_narrow_role_defaults_enforcement_test.go` — reports 0.
4. `grep -E "total_phases|completed_phases|total_plans|completed_plans|percent" .planning/STATE.md | head -5` — shows `8`/`7`/`84`/`84`/`88`.
5. `grep -A2 "## Current Position" .planning/STATE.md` — shows `Phase: 143`.
6. `git log --oneline -2` — shows exactly two new commits in the specified order: the phase136 test fix, then the STATE.md correction.
</verification>

<success_criteria>
- Two atomic commits exist on `main`, each touching only the one file named in its task.
- `phase136RepositoryRoot` no longer exists anywhere in the codebase; `phase136BackendRoot` (unchanged logic) and `phase136RepoRoot` (one level higher) both exist in `phase136_narrow_role_defaults_enforcement_test.go`, and only the migration-file read uses `phase136RepoRoot`.
- All `TestPhase136*` tests in this file pass, and `go build ./...` stays clean.
- `.planning/STATE.md`'s frontmatter `progress` block is internally consistent (`completed_plans <= total_plans`, `completed_phases <= total_phases`) and matches `.planning/v1.4-MILESTONE-AUDIT.md`'s passed 7/7 status plus `.planning/ROADMAP.md`'s unplanned Phase 143.
- `.planning/STATE.md`'s `## Current Position` and `## Session Continuity` sections both name Phase 143 as the current, not-yet-planned position instead of stale Phase 141/142/134 references.
</success_criteria>

<output>
Create `.planning/quick/260901-la2-fix-phase136-test-repo-root-helper-and-c/260901-la2-SUMMARY.md` when done.
</output>
