---
phase: quick-260924-b7s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/models/episode_import.go
  - backend/internal/handlers/jellyfin_source_folder_list.go
  - backend/internal/handlers/jellyfin_folder_display_names.go
  - backend/internal/handlers/jellyfin_folder_display_names_test.go
  - backend/internal/handlers/admin_episode_import.go
  - frontend/src/types/episodeImport.ts
  - frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportFolderSelector.tsx
  - frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportFolderSelector.test.tsx
  - frontend/src/app/admin/anime/[id]/episodes/import/FansubGroupOriginHint.tsx
  - frontend/src/app/admin/anime/[id]/episodes/import/FansubGroupOriginHint.test.tsx
  - backend/internal/repository/episode_placeholder_title.go
  - backend/internal/repository/episode_placeholder_title_test.go
  - backend/internal/repository/episode_import_repository_apply.go
  - backend/internal/repository/episode_import_repository_anime_type.go
  - backend/internal/handlers/admin_content_handler.go
  - backend/internal/handlers/admin_episode_import_einteiler.go
  - backend/internal/handlers/admin_episode_import_einteiler_test.go
  - backend/internal/handlers/admin_episode_import_test.go
  - frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportMappingRow.tsx
  - frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportMappingRow.test.tsx
  - .planning/phases/167-fansub-gruppenerkennung-beim-import/167-UAT.md
autonomous: true
requirements: [QUICK-260924-B7S-01, QUICK-260924-B7S-02, QUICK-260924-B7S-03]

must_haves:
  truths:
    - "GAP-01: Jellyfin-Ordnerauswahl im Episoden-Import zeigt Ordnername/letztes Pfadsegment statt roher Jellyfin-IDs, markiert den Haupt-Ordner, zeigt den vollen Pfad als Tooltip, und faellt nur bei wirklich fehlendem Namen/Pfad auf die rohe ID zurueck -- mit genau EINEM gebuendelten Jellyfin-Call pro Kontext-Ladevorgang, nie einem Call pro Ordner."
    - "GAP-02: Eine Mapping-Zeile mit erkanntem, aber keiner Gruppe zugeordnetem Fansub-Kuerzel zeigt einen sichtbaren Hinweis mit dem erkannten Kuerzel, solange keine Gruppe ausgewaehlt ist -- ohne automatische Gruppenerstellung oder Zuordnung."
    - "GAP-03: Ein Einteiler-Anime (Film immer; OVA/ONA/Special/Bonus nur bei genau 1 kanonischer Episode) mit genau einer nummernlosen Datei erhaelt fuer diese Datei einen suggested-Vorschlag auf die einzige Episode mit sichtbarer Begruendung; bei 0 oder mindestens 2 nummernlosen Dateien oder mehr als 1 kanonischer Episode bleibt das Verhalten unveraendert."
    - "Alle drei GAPs sind nach der Ausfuehrung in 167-UAT.md von 'open' auf 'resolved' aktualisiert, mit Verweis auf diesen Plan."
    - "Backend- und Frontend-Container laufen nach dem Fix mit einem neueren Start-Zeitpunkt als vor der Ausfuehrung; keine Aenderung an der team4s_v2-Datenbank; kein git push."
  artifacts:
    - path: "backend/internal/handlers/jellyfin_folder_display_names.go"
      provides: "hydrateJellyfinFolderDisplayNames: EIN gebuendelter Jellyfin-Batch-Call fuer alle verbundenen Ordner-IDs, fail-open bei Jellyfin-Fehlern, Haupt-Ordner erhaelt zusaetzlich den lokal bekannten Pfad als Baseline, No-Op bei <=1 Ordner (kein Regressions-Call im Normalfall)."
    - path: "backend/internal/handlers/admin_episode_import_einteiler.go"
      provides: "applyEinteilerSuggestion: reine Funktion, die genau eine 'skipped'-Zeile bei genau 1 kanonischer Episode + Einteiler-Typ zu 'suggested' mit Begruendung macht und UnmappedEpisodes/UnmappedMediaItemIDs nachfuehrt."
    - path: "backend/internal/repository/episode_import_repository_anime_type.go"
      provides: "GetAnimeType(ctx, animeID) (string, error) auf *EpisodeImportRepository, genutzt zur Preview-Zeit."
    - path: "frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportFolderSelector.tsx"
      provides: "Options-Label aus folder_display_name/letztem Pfadsegment, Haupt-Ordner-Markierung, Tooltip mit vollem Pfad."
    - path: "frontend/src/app/admin/anime/[id]/episodes/import/FansubGroupOriginHint.tsx"
      provides: "Neuer Hinweis-Zustand E fuer erkanntes, aber unzugeordnetes Kuerzel."
    - path: ".planning/phases/167-fansub-gruppenerkennung-beim-import/167-UAT.md"
      provides: "GAP-01..GAP-03 von open auf resolved, mit Fix-Referenz."
  key_links:
    - from: "backend/internal/handlers/admin_episode_import.go (loadEpisodeImportContext)"
      to: "hydrateJellyfinFolderDisplayNames(...)"
      via: "eine zusaetzliche Zeile nach collectJellyfinFolderOptions(...)"
      pattern: "hydrateJellyfinFolderDisplayNames"
    - from: "backend/internal/handlers/admin_episode_import.go (PreviewEpisodeImport)"
      to: "applyEinteilerSuggestion(preview, animeType)"
      via: "h.episodeImportRepo.GetAnimeType(...) gefolgt von einem Aufruf vor c.JSON"
      pattern: "applyEinteilerSuggestion"
    - from: "frontend/src/app/admin/anime/[id]/episodes/import/FansubGroupOriginHint.tsx"
      to: "row.fansub_group_name"
      via: "neuer Zweig innerhalb des bisherigen fruehen Returns (kein origin, keine suggestions)"
      pattern: "keiner Gruppe zugeordnet"
---

<objective>
Drei Live-UAT-Befunde aus Phase 167 (Anime #7, ".hack//G.U. Returner", zwei verlinkte
Jellyfin-Ordner) beheben, dokumentiert in `167-UAT.md` als GAP-01..GAP-03:

- GAP-01: Die Jellyfin-Ordnerauswahl im Episoden-Import zeigt rohe Jellyfin-IDs statt Namen/Pfad.
- GAP-02: Ein erkanntes, aber keiner Gruppe zugeordnetes Fansub-Kuerzel ("GAX") ist im UI unsichtbar.
- GAP-03: Eine nummernlose Datei bei einem Einteiler-Anime (genau 1 kanonische Episode) landet
  unter "Ohne Episodenzuordnung" statt als Vorschlag auf die einzige Episode.

Purpose: Die drei Befunde sind Sichtbarkeits-/Vorschlags-Luecken in einer bereits abgeschlossenen
Phase (167), keine neuen Produktentscheidungen -- alle drei bleiben strikt vorschlagend/informativ,
nie automatisch (keine automatische Gruppenerstellung, keine automatische Zuordnung/Bestaetigung).

Output: Ordnerauswahl mit lesbaren Labels, sichtbarer Hinweis bei unzugeordnetem Kuerzel,
Einteiler-Vorschlag fuer die einzige Episode -- je mit Backend-/Frontend-Tests, Container neu
gebaut/neu gestartet, `167-UAT.md` auf resolved aktualisiert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/167-fansub-gruppenerkennung-beim-import/167-CONTEXT.md
@.planning/phases/167-fansub-gruppenerkennung-beim-import/167-UAT.md

<critical_gotcha>
The dev backend container (team4sv30-backend) runs Dockerfile.dev via air with docker compose
watch live-syncing ./backend into /app -- it DOES have the Go toolchain (go version works via
docker exec team4sv30-backend sh -c "go version"). BUT prior sessions hit a real gotcha (see
.planning/quick/260923-ed4-gap-24-anisearch-platzhaltertitel-episod/260923-ed4-SUMMARY.md): if
docker compose watch is not actively running, docker exec ... go test silently runs against the
container's stale build-time-baked copy of the source, not your just-edited host files, and shows
"no tests to run" for new files. Before trusting any docker exec team4sv30-backend ... go test
result, either confirm docker compose watch is running, or docker cp every edited/created backend
file into the container first. The final docker compose up -d --build team4sv30-backend step
(Task 6) performs the real, permanent rebuild regardless of this live-sync mechanism.
</critical_gotcha>

<interfaces>
Current models.JellyfinFolderOption (backend/internal/models/episode_import.go, ~131-143):
  type JellyfinFolderOption struct {
    JellyfinItemID string `json:"jellyfin_item_id"`
    IsMain         bool   `json:"is_main"`
  }
Used both by EpisodeImportContextResult.JellyfinFolders (episode_import.go) and by
AdminAnimeJellyfinProvenanceContext.Folders (admin_content.go, 165-07 edit-page folder list,
which shares the exact same underlying-ID/no-name problem but is OUT OF SCOPE for this
quick-task -- only the episode-import screen is in scope for GAP-01). Do not touch
AdminAnimeJellyfinProvenanceContext or AnimeJellyfinFolderList.tsx.

Current loadEpisodeImportContext (backend/internal/handlers/admin_episode_import.go, ~168-214):
  folderPath := normalizeStringPtr(derefString(source.FolderName))  -- already computed above
  allFolders := collectJellyfinFolderOptions(source.Source, source.SourceLinks, source.Source)
  displayFolders := allFolders
  if len(displayFolders) <= 1 { displayFolders = nil }  // D-14
  return models.EpisodeImportContextResult{ ..., FolderPath: folderPath, JellyfinFolders: displayFolders,
    JellyfinFoldersForOwnershipCheck: allFolders }, http.StatusOK, nil
The ONLY change here: one new line right after "allFolders := collectJellyfinFolderOptions(...)":
  allFolders = h.hydrateJellyfinFolderDisplayNames(c.Request.Context(), allFolders, folderPath)
Everything else in this function stays byte-identical.

backend/internal/handlers/jellyfin_source_batch.go already provides the batching primitive:
  func (h *AdminContentHandler) getJellyfinSourceItems(ctx context.Context, itemIDs []string) (map[string]jellyfinEpisodeItem, error)
jellyfinEpisodeItem (jellyfin_client.go) has fields ID, Name, Path (among others).
fetchJellyfinSourceBatch chunks at 100 IDs per /Items request and is proven request-count-bounded
by TestEpisodeImportSourceBatchBudget (admin_episode_import_test.go) -- reuse getJellyfinSourceItems
directly, do not write a second batching implementation. Note: it treats ANY missing/unexpected/
duplicate ID as a hard error (fail-closed) -- that is correct for its existing episode-source-
integrity callers, but for this new best-effort display hydration you must treat that error as
non-fatal (log and return folders unchanged), never propagate it to the caller.

backend/internal/repository/episode_placeholder_title.go, current (GAP-24, 165-UAT.md):
  func isEinteilerAnimeType(animeType string, totalEpisodeCount int) bool {
    switch animeType {
    case "film": return true
    case "ova", "ona", "special", "bonus": return totalEpisodeCount == 1
    default: return false
    }
  }
Rename this (and its single call site in episode_import_repository_apply.go:64, and its test in
episode_placeholder_title_test.go) to the exported IsEinteilerAnimeType -- same body, same doc
comment content, just exported so backend/internal/handlers can call it (handlers already imports
"team4s.v3/backend/internal/repository" in admin_episode_import.go for repository.ErrConflict). Do
not duplicate this switch statement in the handlers package.

backend/internal/handlers/admin_content_handler.go, adminEpisodeImportRepository interface (~156-162):
  type adminEpisodeImportRepository interface {
    GetJellyfinSourceBindings(ctx context.Context, itemIDs []string) (map[models.JellyfinSourceKey]models.JellyfinSourceSnapshot, error)
    Apply(ctx context.Context, input models.EpisodeImportApplyInput) (*models.EpisodeImportApplyResult, error)
    PreviewExistingCoverage(ctx context.Context, animeID int64) (models.EpisodeImportExistingCoverage, error)
    ResolveFansubGroupMatches(ctx context.Context, candidates []string) ([]models.FansubGroupMatch, error)
    SuggestSimilarFansubGroups(ctx context.Context, candidate string) ([]models.FansubGroupSuggestion, error)
  }
Add: GetAnimeType(ctx context.Context, animeID int64) (string, error)

backend/internal/handlers/admin_episode_import_test.go's episodeImportSourceRepoSpy is the ONLY
fake assigned to h.episodeImportRepo in the whole codebase (grep-verified) -- it is the only fake
that needs a new GetAnimeType method to keep compiling once the interface gains that method. A
trivial stub returning ("", nil) is enough; none of that file's existing tests exercise
PreviewEpisodeImport through the spy.

Current PreviewEpisodeImport (backend/internal/handlers/admin_episode_import.go, ~101-113):
  preview := buildEpisodeImportPreview(animeID, contextResult.AnimeTitle, ..., req.SeasonOffset)
  preview.Mappings = enrichEpisodeImportPreviewFansubData(c.Request.Context(), h.episodeImportRepo, preview.Mappings)
  c.JSON(http.StatusOK, gin.H{"data": preview})
Insert, between the enrichEpisodeImportPreviewFansubData line and the c.JSON line:
  if h.episodeImportRepo != nil {
    if animeType, typeErr := h.episodeImportRepo.GetAnimeType(c.Request.Context(), animeID); typeErr == nil {
      preview = applyEinteilerSuggestion(preview, animeType)
    }
  }
A GetAnimeType error (e.g. anime not found, DB hiccup) must never fail the whole preview response --
fail open by simply skipping the suggestion, exactly like the existing PreviewExistingCoverage guard
a few lines above this insertion point.

backend/internal/models/episode_import.go, EpisodeImportMappingRow's existing display-only fields
(fansub_group_match_origin, fansub_group_suggestions, release_version_source) are "never sent back
on apply" by convention/comment only -- Apply-side validation (buildEpisodeImportApplyPlan,
episode_import_repository.go) never reads them. Add SuggestionReason the same way: a new
"SuggestionReason *string" field with json tag suggestion_reason,omitempty, same "display-only"
comment style.

frontend/src/types/episodeImport.ts, current JellyfinFolderOption:
  export interface JellyfinFolderOption {
    jellyfin_item_id: string
    is_main: boolean
  }
Add: folder_display_name?: string | null; folder_path?: string | null
Also add to EpisodeImportMappingRow: suggestion_reason?: string | null (display-only, same style
as release_version_source).

frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportFolderSelector.tsx, current option
render:
  {folders.map((folder) => (
    <option key={folder.jellyfin_item_id} value={folder.jellyfin_item_id}>
      {folder.jellyfin_item_id}
    </option>
  ))}
The component doc comment currently says: "this component only receives Jellyfin item IDs, not
resolved folder paths, so the raw Jellyfin ID is rendered directly as the option label" -- that
sentence becomes false after this task and must be updated/removed.

frontend/src/app/admin/anime/[id]/episodes/import/FansubGroupOriginHint.tsx, current early return
(~42-47):
  const origin = row.fansub_group_match_origin ?? null
  const suggestions = row.fansub_group_suggestions ?? []
  if (!origin && suggestions.length === 0) { return null }
  const currentGroup = selectedFansubGroups[0] ?? null ...
row.fansub_group_name is ALREADY present on every row whose filename yielded a candidate group
string (set unconditionally by buildEpisodeImportPreview via importutil.DeriveFansubGroupName,
independent of whether a match/suggestion was found) -- no backend change needed for GAP-02, this
is frontend-only.

frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportMappingRow.tsx, existing sibling
hint pattern to mirror for suggestion_reason (~172-174):
  {(row.target_episode_numbers ?? []).length > 1 ? (
    <span className={styles.multiEpisodeHint}>Deckt {row.target_episode_numbers.length} Episoden ab</span>
  ) : null}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend -- batched Jellyfin folder name/path hydration (GAP-01)</name>
  <files>backend/internal/models/episode_import.go, backend/internal/handlers/jellyfin_folder_display_names.go, backend/internal/handlers/jellyfin_folder_display_names_test.go, backend/internal/handlers/admin_episode_import.go</files>
  <behavior>
    - Test A (single folder, no remote call): hydrateJellyfinFolderDisplayNames called with exactly one
      folder must return it unchanged and must NOT hit the Jellyfin httptest server at all (use a server
      handler that calls t.Fatal if invoked, proving zero network calls for the common single-folder case
      per D-14).
    - Test B (multiple folders, one batched call): 3 folders (1 main + 2 extra) called against an httptest
      /Items server that returns Name/Path for all 3 IDs in one response; assert the server received
      exactly 1 request (not 3), and every returned folder now has FolderDisplayName/FolderPath populated
      from the response.
    - Test C (Jellyfin error fails open): same 3-folder input, server returns HTTP 503 for /Items; assert
      hydrateJellyfinFolderDisplayNames returns the folders slice unchanged from its pre-call state (main
      folder still carries whatever local baseline path it was given, extra folders stay bare IDs) -- the
      function itself has no error return; the caller must never see a broken preview/context response
      because of a Jellyfin outage.
  </behavior>
  <action>
    In backend/internal/models/episode_import.go, add two new optional fields to JellyfinFolderOption:
    FolderDisplayName *string (json folder_display_name,omitempty) and FolderPath *string (json
    folder_path,omitempty), with a doc comment explaining they are best-effort, populated by
    hydrateJellyfinFolderDisplayNames (handlers package), and distinct from the existing (confusingly
    path-valued) AdminAnimeSyncSource.FolderName field elsewhere in this same file.

    Create backend/internal/handlers/jellyfin_folder_display_names.go with a method
    "func (h *AdminContentHandler) hydrateJellyfinFolderDisplayNames(ctx context.Context, folders
    []models.JellyfinFolderOption, knownMainFolderPath *string) []models.JellyfinFolderOption":
    1. If len(folders) <= 1, return folders unchanged immediately (zero remote calls -- matches D-14's
       "no visible new field in the regular case" and avoids an unnecessary Jellyfin round trip on every
       context load).
    2. Set the entry with IsMain == true's FolderPath from knownMainFolderPath when it is non-nil and
       non-empty (free local baseline, no Jellyfin call).
    3. If h.jellyfinBaseURL or h.jellyfinAPIKey is empty, return folders as-is (no remote call possible;
       mirrors the existing guard style in buildAnimeJellyfinContext, jellyfin_metadata_resync.go).
    4. Collect every folder's JellyfinItemID into one slice and call h.getJellyfinSourceItems(ctx, ids)
       (the existing batched-Items helper, jellyfin_source_batch.go -- see interfaces block). This is ONE
       call regardless of folder count.
    5. On any error from that call, log.Printf a non-fatal warning (same style as
       "episode import context jellyfin path resolve failed anime_id=%d path=%q: %v" in
       loadEpisodeImportContext) and return folders unchanged -- never propagate the error.
    6. On success, for each folder whose ID is present in the returned map, set FolderDisplayName from the
       item's Name (when non-empty) and FolderPath from the item's Path (when non-empty; if Path is empty
       but Name is not, also use Name as FolderPath so the frontend tooltip has something). Leave both nil
       for any folder ID absent from the map (fail-open per-folder, not per-whole-response).
    7. Return the updated slice.

    Wire this into loadEpisodeImportContext (admin_episode_import.go): insert exactly one line immediately
    after the existing "allFolders := collectJellyfinFolderOptions(source.Source, source.SourceLinks,
    source.Source)" statement: "allFolders = h.hydrateJellyfinFolderDisplayNames(c.Request.Context(),
    allFolders, folderPath)". Do not touch any other line in this function (see interfaces block) -- this
    file is already over the project's 450-line guideline and must not grow beyond this single wiring line
    for this task.
  </action>
  <verify>
    <automated>docker exec team4sv30-backend sh -c "cd /app && go build ./... && go vet ./... && go test ./internal/handlers/... -run TestHydrateJellyfinFolderDisplayNames -v -count=1"</automated>
  </verify>
  <done>go build/go vet clean; all 3 new test cases pass (no remote call for <=1 folder, exactly 1 batched request for multiple folders with names/paths populated, fail-open unchanged folders on a Jellyfin error); loadEpisodeImportContext gained exactly one new line.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend -- readable folder selector labels (GAP-01)</name>
  <files>frontend/src/types/episodeImport.ts, frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportFolderSelector.tsx, frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportFolderSelector.test.tsx</files>
  <action>
    In frontend/src/types/episodeImport.ts, add folder_display_name?: string | null and
    folder_path?: string | null to the JellyfinFolderOption interface (see interfaces block).

    In EpisodeImportFolderSelector.tsx, replace the option label logic: for each folder, compute the label
    as folder.folder_display_name?.trim() || lastPathSegment(folder.folder_path) || folder.jellyfin_item_id
    (a small local lastPathSegment(path?: string | null): string | null helper that splits on both / and \
    since Jellyfin paths in this deployment can be Windows-style, trims trailing separators, and returns
    the last non-empty segment or null). When folder.is_main is true, append " (Haupt-Ordner)" to that
    label. Set the option's title attribute to folder.folder_path ?? undefined so the full path shows as
    a native tooltip. Update the component's doc comment (it currently claims the component only ever
    receives raw IDs -- see interfaces block) to reflect the new fallback chain: display name, then last
    path segment, then raw ID.

    In EpisodeImportFolderSelector.test.tsx, add cases: (a) a folder with folder_display_name set renders
    that name as the option label; (b) a folder with only folder_path set (Windows-style, e.g.
    "D:\\Anime\\Show\\Extras") renders "Extras" as the label and the full path as the title attribute; (c)
    the main folder's label includes " (Haupt-Ordner)"; (d) a folder with neither folder_display_name nor
    folder_path still falls back to the raw jellyfin_item_id (this must keep the two existing tests in this
    file passing unchanged, since they already exercise exactly that no-metadata case).
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run 'src/app/admin/anime/[id]/episodes/import/EpisodeImportFolderSelector.test.tsx'"</automated>
  </verify>
  <done>All EpisodeImportFolderSelector.test.tsx cases pass, including the 4 new ones and the 3 pre-existing ones (single-folder no-render, no-folders no-render, main-folder-selected-by-default) unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend -- visible hint for a detected-but-unmatched fansub abbreviation (GAP-02)</name>
  <files>frontend/src/app/admin/anime/[id]/episodes/import/FansubGroupOriginHint.tsx, frontend/src/app/admin/anime/[id]/episodes/import/FansubGroupOriginHint.test.tsx</files>
  <action>
    This is frontend-only: row.fansub_group_name is already populated on the response for any row whose
    filename yielded a candidate group string, independent of whether a match or suggestion was found (see
    interfaces block) -- no backend change is needed or in scope here.

    In FansubGroupOriginHint.tsx, inside the existing early-return block ("if (!origin &&
    suggestions.length === 0) { ... }"), before returning null: if row.fansub_group_name?.trim() is
    non-empty AND selectedFansubGroups.length === 0 (no group chosen yet for this row -- once a group is
    picked, fansub_group_name gets overwritten to the selected group's name by the frontend's own
    normalization, so this guard is required to avoid a stale/misleading hint), render a span reusing the
    existing styles.originHint class (same visual style as the matched-origin hint, per the task's "reuse
    the same component/style" requirement) with the text: Kuerzel "${detectedRaw}" erkannt - keiner Gruppe
    zugeordnet. Nach der Zuweisung wird es als Alias gelernt. -- written with real German umlauts, curly
    quotes and an en-dash exactly as specified in the source task description. Only fall through to
    return null when this condition is not met, preserving the existing Zustand D behavior.

    In FansubGroupOriginHint.test.tsx, add a new test case ("Zustand E") asserting: given a row with
    fansub_group_match_origin: null, fansub_group_suggestions: [], fansub_group_name: 'GAX', and no
    selectedFansubGroups, the hint text containing "GAX" and "keiner Gruppe zugeordnet" is rendered. Add a
    second case proving the hint does NOT render when selectedFansubGroups is non-empty even with the same
    unmatched fansub_group_name (already-resolved-or-being-resolved row must not show a stale hint). The
    existing "Zustand D: renders nothing" test (no fansub_group_name set) must keep passing unchanged.
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run 'src/app/admin/anime/[id]/episodes/import/FansubGroupOriginHint.test.tsx'"</automated>
  </verify>
  <done>FansubGroupOriginHint.test.tsx has 2 new passing cases (Zustand E renders, and is suppressed once a group is selected) plus all pre-existing Zustand A-D cases unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Backend -- Einteiler suggestion for a single number-less file (GAP-03)</name>
  <files>backend/internal/repository/episode_placeholder_title.go, backend/internal/repository/episode_placeholder_title_test.go, backend/internal/repository/episode_import_repository_apply.go, backend/internal/repository/episode_import_repository_anime_type.go, backend/internal/handlers/admin_content_handler.go, backend/internal/handlers/admin_episode_import_einteiler.go, backend/internal/handlers/admin_episode_import_einteiler_test.go, backend/internal/handlers/admin_episode_import_test.go, backend/internal/handlers/admin_episode_import.go, backend/internal/models/episode_import.go</files>
  <behavior>
    Required test coverage (three cases against the new applyEinteilerSuggestion, called directly as a
    pure function -- same direct-call style as the existing buildEpisodeImportPreview tests in
    admin_episode_import_test.go, no HTTP/DB needed):
    - Case A: preview.CanonicalEpisodes has exactly one entry (EpisodeNumber: 1), preview.Mappings has
      exactly one row with Status "skipped" and no target/suggested numbers. Calling
      applyEinteilerSuggestion(preview, "ova") must change that row's Status to "suggested",
      TargetEpisodeNumbers and SuggestedEpisodeNumbers to [1], and set a non-nil SuggestionReason whose
      text is "Einziger Kandidat fuer die einzige Episode" (real umlaut in the actual German string, plain
      substitution only in this English plan prose); the row's MediaItemID must be removed from
      preview.UnmappedMediaItemIDs and episode 1 must be removed from preview.UnmappedEpisodes if present.
    - Case B: same single-canonical-episode setup, but preview.Mappings has TWO rows with Status "skipped".
      Calling applyEinteilerSuggestion(preview, "ova") must leave BOTH rows unchanged (still "skipped", no
      SuggestionReason, no target numbers) -- ambiguous candidates never get auto-suggested.
    - Case C: preview.CanonicalEpisodes has multiple entries (e.g. 12, simulating a TV series) and one
      "skipped" row. Calling applyEinteilerSuggestion(preview, "tv") must leave that row unchanged --
      behavior identical to before this task for any non-Einteiler or multi-episode anime.
    Also cover a "film" animeType case distinctly from "ova"/"ona"/"special"/"bonus" (film is always an
    Einteiler regardless of a hypothetical total-episode-count parameter, per IsEinteilerAnimeType's
    existing semantics -- but this function is gated by len(preview.CanonicalEpisodes) == 1 regardless of
    animeType, so a "film" preview with 2 canonical episodes must also leave rows unchanged like Case C).
  </behavior>
  <action>
    Step 1 (mechanical rename, no behavior change): in
    backend/internal/repository/episode_placeholder_title.go, rename isEinteilerAnimeType to the exported
    IsEinteilerAnimeType, keeping its body and doc comment as-is (just capitalize the identifier and adjust
    the doc comment's leading word accordingly). Update its one call site in
    episode_import_repository_apply.go (line ~64, "isEinteiler := isEinteilerAnimeType(animeType,
    totalEpisodeCount)") to the new exported name. Update episode_placeholder_title_test.go's
    TestIsEinteilerAnimeType to call the exported name too (same test cases, same table, no behavior
    change -- purely a rename so this file keeps compiling).

    Step 2: create backend/internal/repository/episode_import_repository_anime_type.go with a method
    "func (r *EpisodeImportRepository) GetAnimeType(ctx context.Context, animeID int64) (string, error)"
    that runs "SELECT type FROM anime WHERE id = $1" against r.db and returns the scanned string, mapping
    pgx.ErrNoRows to the package's existing ErrNotFound sentinel and wrapping any other error with context
    (animeID) -- follow the exact same structure as GetAnimeSyncSource's error handling in
    admin_content_sync.go for consistency (nil-repo/nil-db guard returning a descriptive error, same as
    every other method in episode_import_repository.go).

    Step 3: add "GetAnimeType(ctx context.Context, animeID int64) (string, error)" to the
    adminEpisodeImportRepository interface in admin_content_handler.go (see interfaces block for the exact
    current interface body). Add a matching stub method to episodeImportSourceRepoSpy in
    admin_episode_import_test.go (admin_episode_import_test.go already defines this struct -- add
    "func (r *episodeImportSourceRepoSpy) GetAnimeType(context.Context, int64) (string, error) { return "",
    nil }" alongside its other methods) so the file keeps compiling.

    Step 4: add SuggestionReason *string (json suggestion_reason,omitempty) to
    models.EpisodeImportMappingRow in episode_import.go, documented as display-only exactly like
    ReleaseVersionSource next to it.

    Step 5: create backend/internal/handlers/admin_episode_import_einteiler.go with a pure function
    "func applyEinteilerSuggestion(preview models.EpisodeImportPreviewResult, animeType string)
    models.EpisodeImportPreviewResult":
    - If len(preview.CanonicalEpisodes) != 1, return preview unchanged.
    - If repository.IsEinteilerAnimeType(animeType, 1) is false, return preview unchanged.
    - Collect the indices of every row in preview.Mappings whose Status equals
      models.EpisodeImportMappingStatusSkipped. If that count is not exactly 1, return preview unchanged.
    - Otherwise, on that single skipped row: set Status to models.EpisodeImportMappingStatusSuggested, set
      both TargetEpisodeNumbers and SuggestedEpisodeNumbers to a one-element slice containing
      preview.CanonicalEpisodes[0].EpisodeNumber, and set SuggestionReason to a pointer to the exact German
      string "Einziger Kandidat fuer die einzige Episode" written with the real ue-umlaut character (not
      the ASCII digraph) in the actual Go source.
    - Remove that row's MediaItemID from preview.UnmappedMediaItemIDs (rebuild the slice filtering it out)
      and remove the now-covered episode number from preview.UnmappedEpisodes (rebuild filtering it out) so
      both derived summary fields stay internally consistent with the new suggestion.
    - Return the mutated preview.

    Step 6: wire this into PreviewEpisodeImport (admin_episode_import.go) exactly as shown in the
    interfaces block -- one guarded block between the existing enrichEpisodeImportPreviewFansubData call
    and the final c.JSON call, fetching animeType via h.episodeImportRepo.GetAnimeType and failing open
    (skip the suggestion, never error the response) on any GetAnimeType error or when h.episodeImportRepo
    is nil.

    Step 7: create backend/internal/handlers/admin_episode_import_einteiler_test.go implementing Cases
    A-C plus the film-with-multiple-episodes case from the behavior block above, calling
    applyEinteilerSuggestion directly (no HTTP, no DB).
  </action>
  <verify>
    <automated>docker exec team4sv30-backend sh -c "cd /app && go build ./... && go vet ./... && go test ./internal/repository/... ./internal/handlers/... -run 'TestIsEinteilerAnimeType|TestApplyEinteilerSuggestion' -v -count=1"</automated>
  </verify>
  <done>go build/go vet clean across both packages; IsEinteilerAnimeType exported and used from both packages with zero duplicated switch logic; all 4 applyEinteilerSuggestion test cases pass (single-candidate suggestion with correct reason text and consistent Unmapped* fields, two-candidate no-op, multi-episode no-op, film-with-multiple-episodes no-op); episodeImportSourceRepoSpy still compiles and its existing tests still pass unmodified in behavior.</done>
</task>

<task type="auto">
  <name>Task 5: Frontend -- render the Einteiler suggestion reason (GAP-03)</name>
  <files>frontend/src/types/episodeImport.ts, frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportMappingRow.tsx, frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportMappingRow.test.tsx</files>
  <action>
    In frontend/src/types/episodeImport.ts, add suggestion_reason?: string | null to
    EpisodeImportMappingRow (display-only, same style as release_version_source next to it).

    In EpisodeImportMappingRow.tsx (the EpisodeImportMappingRowCard component), render row.suggestion_reason
    when present, next to the existing multi-episode hint span (see interfaces block for the exact sibling
    pattern at ~172-174): a conditional span with the reason text, reusing styles.multiEpisodeHint for
    visual consistency with that neighboring informational hint (no new CSS class required unless the
    executor judges the shared class visually wrong for this different meaning -- in that case add one
    minimal rule to page.module.css instead of inlining styles).

    In EpisodeImportMappingRow.test.tsx, add one test case rendering a row with
    suggestion_reason: 'Einziger Kandidat fuer die einzige Episode' (the real umlaut character in the
    actual test file, matching Task 4's backend string exactly) and asserting that text appears in the
    rendered output. Keep the two existing tests in this file passing unchanged.
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run 'src/app/admin/anime/[id]/episodes/import/EpisodeImportMappingRow.test.tsx'"</automated>
  </verify>
  <done>EpisodeImportMappingRow.test.tsx has a new passing case asserting the suggestion_reason text renders, plus both pre-existing tests unchanged.</done>
</task>

<task type="auto">
  <name>Task 6: Full verification, container rebuild/restart, and UAT gap closure</name>
  <files>.planning/phases/167-fansub-gruppenerkennung-beim-import/167-UAT.md</files>
  <action>
    Backend full suite: run
    "docker exec team4sv30-backend sh -c 'cd /app && go build ./... && go vet ./... && go test ./...'"
    (after confirming via the critical_gotcha note above that the container's live-synced source reflects
    every file this plan touched -- docker cp any file that is not yet reflected before trusting the
    result). Treat any NEW failure as a blocker for this plan; a pre-existing, independently-documented
    failure unrelated to any file this plan touched (cross-check via git diff --stat) is not a blocker but
    must be named explicitly in the final report, matching this project's established convention (see
    260923-ed4-SUMMARY.md's handling of pre-existing TestPhase128* DSN-gated failures).

    Frontend full suite + typecheck + lint, each inside the frontend container (not on host):
    "docker compose exec -T team4sv30-frontend sh -c 'cd /app && npm run test'",
    "docker compose exec -T team4sv30-frontend sh -c 'cd /app && npm run typecheck'",
    "docker compose exec -T team4sv30-frontend sh -c 'cd /app && npm run lint'". Same blocker rule as
    backend: any NEW failure in a file this plan touched blocks; pre-existing failures elsewhere must be
    named, not silently ignored.

    Record the CURRENT container start times before rebuilding:
    "docker inspect -f '{{.State.StartedAt}}' team4sv30-backend team4sv30-frontend". Then rebuild/restart:
    "docker compose up -d --build team4sv30-backend" followed by
    "curl -sf http://192.168.235.196:8092/health -o /dev/null -w '%{http_code}\n'" (expect 200), then
    "docker restart team4sv30-frontend" followed by "docker compose ps team4sv30-frontend" (expect "Up").
    Record the NEW container start times with the same docker inspect command and confirm both are strictly
    later than the pre-rebuild timestamps.

    Do not touch the team4s_v2 database in any way. Do not run git push. Stay on main (no branch, no
    worktree). Never use git stash, not even transiently, per CLAUDE.md.

    Finally, edit .planning/phases/167-fansub-gruppenerkennung-beim-import/167-UAT.md: change all three
    gap entries' "status: open" to "status: resolved", and append a "reason:"-adjacent note (or a short
    trailing artifacts/missing update, matching this project's existing UAT-gap-closure style seen in
    165-UAT.md) referencing this plan's identifier (quick-260924-b7s) and the specific files that closed
    each gap. Do not delete or rewrite the existing root_cause/artifacts/missing analysis prose -- only add
    the resolution note and flip the status field, mirroring how prior UAT gap closures in this repository
    have been recorded.
  </action>
  <verify>
    <automated>docker exec team4sv30-backend sh -c "cd /app && go build ./... && go vet ./..." && docker compose exec -T team4sv30-frontend sh -c "cd /app && npm run typecheck" && grep -c "status: resolved" /home/d1sk/team4s/.planning/phases/167-fansub-gruppenerkennung-beim-import/167-UAT.md</automated>
  </verify>
  <done>Backend go build/go vet/go test and frontend npm run test/typecheck/lint all reported (pass, or named pre-existing unrelated failures only); both containers rebuilt/restarted with strictly newer recorded start times than before; /health returns 200; team4s_v2 untouched; no git push; no git stash used; 167-UAT.md shows GAP-01, GAP-02, and GAP-03 all as status: resolved with a note pointing at this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin browser -> episode-import preview/context API | Authenticated admin-only surface (requireAdmin gate, unchanged by this plan); the folder-name hydration adds a new outbound call from the backend to the already-trusted Jellyfin instance, not a new inbound trust boundary. |
| Backend -> Jellyfin /Items API | Existing trust boundary, reused via the existing getJellyfinSourceItems batching primitive; no new credentials, no new endpoint pattern. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-QUICK260924-B7S-01 | Denial of Service (self-inflicted latency/API load) | hydrateJellyfinFolderDisplayNames | mitigate | Exactly one batched /Items call per context load regardless of folder count, early return with zero calls when <=1 folder (the default case) -- proven by Task 1's request-count assertions. |
| T-QUICK260924-B7S-02 | Denial of Service (availability regression from a flaky external dependency) | hydrateJellyfinFolderDisplayNames error path | mitigate | Any Jellyfin error is logged and swallowed, folders returned unchanged (fail-open) -- the episode-import context/preview endpoint must never 5xx because Jellyfin is briefly unreachable; proven by Task 1 Case C. |
| T-QUICK260924-B7S-03 | Tampering / Repudiation (silent auto-assignment) | applyEinteilerSuggestion | accept-by-design | The suggestion is never auto-confirmed -- Status stays "suggested", requiring the same explicit admin confirmation as every other suggested row before Apply persists anything; this is the explicit non-goal from the task description, not a gap. |

No package-manager installs are part of this plan; the Package Legitimacy Gate does not apply.
</threat_model>

<verification>
1. Task 1's automated Go test proves the batched, fail-open folder hydration (zero calls for <=1 folder, exactly 1 batched call for multiple folders, unchanged folders on a Jellyfin error).
2. Task 2's automated Vitest run proves the folder selector's label/title/fallback/main-folder-marking logic.
3. Task 3's automated Vitest run proves the unmatched-abbreviation hint appears only when appropriate.
4. Task 4's automated Go test proves the Einteiler suggestion rule's three required cases plus the film/multi-episode edge case.
5. Task 5's automated Vitest run proves the suggestion reason renders on the mapping row.
6. Task 6's full backend/frontend suites plus container rebuild/restart plus 167-UAT.md gap closure.
</verification>

<success_criteria>
- [ ] Folder selector on /admin/anime/{id}/episodes/import shows readable names/paths instead of raw Jellyfin IDs, with the main folder marked, for anime with more than one connected folder (e.g. anime #7).
- [ ] A detected-but-unmatched fansub abbreviation shows a visible hint on its row until a group is selected; no automatic group creation or assignment anywhere.
- [ ] A single-episode Einteiler anime's single number-less file is proposed (status "suggested", not auto-confirmed) as that episode's match, with a visible rationale; multi-file or multi-episode cases are unchanged.
- [ ] backend/internal/handlers/admin_episode_import.go and frontend's episodes/import/page.tsx gained no new logic beyond trivial one-line wiring calls -- all real logic lives in new/existing helper files.
- [ ] All new user-facing strings use real German umlauts, no ASCII substitutes.
- [ ] All new UI uses only @/components/ui primitives.
- [ ] All new tests actually execute the behavior under test (real component renders / real handler-adjacent pure-function calls), never source-grepping.
- [ ] 167-UAT.md's GAP-01, GAP-02, GAP-03 are all status: resolved, referencing this plan.
- [ ] Both containers rebuilt/restarted with confirmed newer start times; team4s_v2 untouched; no git push; no git stash used.
</success_criteria>

<output>
Create `.planning/quick/260924-b7s-phase-167-live-uat-fixes-gap-01-gap-03/260924-b7s-SUMMARY.md` when done
</output>
