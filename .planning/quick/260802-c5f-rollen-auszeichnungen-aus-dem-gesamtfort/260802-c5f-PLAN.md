---
phase: quick-rollen-auszeichnungen-aus-dem-gesamtfort
plan: 260802-c5f
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/profile/MemberBadgeChain.tsx
  - frontend/src/components/profile/MemberBadgeChain.test.tsx
  - frontend/src/app/members/[slug]/page.tsx
autonomous: true
requirements:
  - QUICK-260802-C5F
must_haves:
  truths:
    - "Der allgemeine Auszeichnungsfortschritt zählt weder Rollen-Einstiege noch Rollen-Volumenstufen im Zähler oder Nenner."
    - "Es erscheinen nur Fansubrollen, für die das Mitglied mindestens eine Rollen-Auszeichnung tatsächlich verdient hat; andere Rollen erscheinen nicht als gesperrte Zeilen."
    - "Jede sichtbare Rolle zeigt weiterhin ihre eigene vollständige Entwicklung aus Einstieg, Bronze, Silber, Gold und Platin mit erreichtem beziehungsweise noch nicht erreichtem Zustand."
    - "Die Rollen-Sektion trägt neutrale deutsche Bezeichnungen und nennt korrekt '1 ausgeübte Fansubrolle' beziehungsweise '{n} ausgeübte Fansubrollen'."
  artifacts:
    - path: "frontend/src/components/profile/MemberBadgeChain.tsx"
      provides: "Getrennte allgemeine Fortschrittsberechnung und verdienstbasierte Rollenanzeige"
    - path: "frontend/src/components/profile/MemberBadgeChain.test.tsx"
      provides: "Regressionstests für Zählertrennung, Rollenfilter, Stufenkette und Singular/Plural"
    - path: "frontend/src/app/members/[slug]/page.tsx"
      provides: "Öffentliches Profil ohne zuordnungsbasierte Aufweitung fremder Rollen"
  key_links:
    - from: "frontend/src/app/members/[slug]/page.tsx"
      to: "frontend/src/components/profile/MemberBadgeChain.tsx"
      via: "earnedBadges bleiben die einzige Quelle für sichtbare Rollen"
      pattern: "<MemberBadgeChain"
    - from: "frontend/src/components/profile/MemberBadgeChain.tsx"
      to: "frontend/src/components/profile/memberBadgeLabels.ts"
      via: "getMemberBadgePresentation gruppiert role_entry_* und role_volume_* nach roleCode"
      pattern: "getMemberBadgePresentation"
---

<objective>
Rollen-Auszeichnungen aus dem allgemeinen Gesamtfortschritt herauslösen und im öffentlichen Mitgliedsprofil nur tatsächlich verdiente Fansubrollen mit ihrer jeweiligen bestehenden Stufenentwicklung darstellen.

Purpose: Der allgemeine Fortschritt darf nicht durch fachfremde Rollenstufen verzerrt werden, und nicht ausgeübte Rollen dürfen nicht als Schloss-Sammlung erscheinen.
Output: Eine fokussierte Anpassung der bestehenden `MemberBadgeChain`-Komposition samt Regressionstests; keine API-, Backend-, Schema- oder Asset-Änderung.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
<read_first>
@AGENTS.md
@docs/engineering/implementation-contract.md
@docs/frontend/ui-system.md
@docs/agent-guidelines-ui.md
@frontend/src/components/profile/MemberBadgeChain.tsx
@frontend/src/components/profile/MemberBadgeChain.test.tsx
@frontend/src/components/profile/memberBadgeLabels.ts
@frontend/src/app/members/[slug]/page.tsx
@frontend/src/types/profile.ts
</read_first>

<interfaces>
Existing contracts to preserve:
- `MemberBadgeChain({ earnedBadges, catalog })` renders the public badge catalog and adds earned badges absent from the static catalog through `catalogWithEarnedBadges`.
- `getMemberBadgePresentation(badgeCode)` maps both `role_entry_<role>` and `role_volume_<role>_<tier>` to group `roles` plus a common `roleCode`.
- `buildMemberBadgeGroups` already merges all catalog items with the same role code into one row.
- `PUBLIC_MEMBER_BADGE_CATALOG` supplies each role's entry item; earned volume badges are merged at runtime.
- Role volume labels and thresholds remain owned by `memberBadgeLabels.ts`: Bronze 12+, Silber 108+, Gold 320+, Platin 510+.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the separated progress and earned-role behavior with component tests</name>
  <files>frontend/src/components/profile/MemberBadgeChain.test.tsx</files>
  <behavior>
    - A member with one earned general badge and earned role entry/volume badges sees general progress computed solely from non-role catalog items; role badges do not change its numerator or denominator (D-01).
    - An unearned catalog role is absent entirely: no role heading, row, badge label, lock icon, or `gesperrt` accessibility label for that foreign role (D-02).
    - An earned role shows exactly its existing entry plus Bronze/Silber/Gold/Platin progression; earned stages remain earned and later stages remain locked inside that same visible role (D-03).
    - One visible role renders `1 ausgeübte Fansubrolle`; two render `2 ausgeübte Fansubrollen`, using correct umlauts and neutral titles (D-04).
  </behavior>
  <action>Extend the existing `MemberBadgeChain` test suite before production changes. Use small injected catalogs containing a general badge plus complete role chains so expectations are deterministic. Assert the progress block by its accessible label/text, scope role-chain assertions to the roles group/list, and assert foreign role absence through both visible text and accessible locked labels. Preserve the existing artwork and carousel assertions; do not replace them with broad snapshots.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run src/components/profile/MemberBadgeChain.test.tsx</automated>
  </verify>
  <done>The new tests fail against the current implementation specifically because roles still influence total progress and assignment-derived/foreign roles can render as locked rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Separate general progress from earned role progress in the existing badge chain</name>
  <files>frontend/src/components/profile/MemberBadgeChain.tsx, frontend/src/app/members/[slug]/page.tsx</files>
  <behavior>
    - General progress is derived only from presentations whose group is not `roles`.
    - Visible role codes are derived only from earned `role_entry_*` or `role_volume_*` presentations, not memberships, project assignments, or the complete catalog.
    - Once a role is visible, its catalog row retains entry and all four volume tiers so the member's own next stages remain visible as locks.
  </behavior>
  <action>Refactor `MemberBadgeChain` without creating a second badge component. Per D-01, split the merged catalog into a non-role catalog used exclusively for `earnedCount`, total count, percentage, and the general progress block, and a role catalog filtered by role codes present in `earnedBadges`. Per D-02, remove the `relevantRoleCodes` prop and its union logic so membership/app-role/project association cannot reveal an unearned role as a locked row; update `frontend/src/app/members/[slug]/page.tsx` to remove the derived `relevantRoleCodes` array and prop wiring. Per D-03, retain `buildMemberBadgeGroups`, same-role merging, artwork resolution, and the complete entry/Bronze/Silber/Gold/Platin catalog items for each earned role—only whole foreign role rows disappear, not future stages of an earned role. Per D-04, render separate neutral German labels for the general progress area and roles area, plus the compact role count with exact singular/plural `1 ausgeübte Fansubrolle` / `{n} ausgeübte Fansubrollen`; use correct umlauts. Keep `FocalCarousel`, existing CSS classes, badge assets, `memberBadgeLabels.ts` thresholds, and non-role lock behavior unchanged. Do not modify APIs, DTOs, backend badge calculation, asset files, or add a parallel role-progress helper unless extraction is required to make the tested filtering pure.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run src/components/profile/MemberBadgeChain.test.tsx &amp;&amp; npm run typecheck &amp;&amp; npm run lint -- --quiet &amp;&amp; git diff --check</automated>
  </verify>
  <done>All component regressions pass; the public profile no longer supplies assignment-derived role codes; general progress excludes all role items; only earned roles appear and each keeps its own full five-step progression and correct German summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public profile API data → SSR profile page → badge component | Badge and role codes are server-provided strings rendered on a public surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-C5F-01 | Information Disclosure | `MemberBadgeChain.tsx` role filtering | mitigate | Derive visible roles only from public earned badges; do not expose membership/app-role/project associations as locked role hints. |
| T-C5F-02 | Tampering | Badge-code presentation lookup | accept | React escapes rendered strings and the existing defensive presentation fallback remains unchanged; this task adds no mutation or request path. |
| T-C5F-03 | Denial of Service | Catalog grouping/rendering | accept | Inputs remain the existing bounded profile badge/catalog arrays and no new network or recursive processing is introduced. |
</threat_model>

<verification>
1. Run the focused Vitest suite and confirm the new separation, filtering, five-stage role progression, and singular/plural cases pass.
2. Run frontend typecheck and lint to catch removal of the `relevantRoleCodes` prop and any stale page wiring.
3. Run `git diff --check` and review the diff to confirm only the three declared frontend files changed; do not stage or alter the pre-existing untracked `frontend/public/member-achievement-badges/role_volume_timer_*.png` files.
4. Live UAT at `http://127.0.0.1:3300/members/{slug}` with one profile having one earned role and another having multiple roles: verify the general fraction is unchanged by role tiers, foreign roles are absent, own future tiers remain locked, count grammar is correct, and all UI strings use proper umlauts.
</verification>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Rollen-Auszeichnungen aus Gesamtfortschritt lösen und nur echte Rollen zeigen | 260802-c5f | COVERED | Tasks 1–2
REQ | QUICK-260802-C5F | Vollständiger Nutzerauftrag | 260802-c5f | COVERED | Alle sechs Akzeptanzpunkte abgedeckt
RESEARCH | — | Bestehende MemberBadgeChain-/Presentation-Seams wiederverwenden | 260802-c5f | COVERED | Keine neue Abhängigkeit oder parallele UI
CONTEXT | D-01 | Rollen aus allgemeinem Fortschritt entfernen | 260802-c5f | COVERED | Task 1–2
CONTEXT | D-02 | Nur ausgeübte/verdiente Rollen; fremde Rollen ohne Schlösser | 260802-c5f | COVERED | Task 1–2
CONTEXT | D-03 | Eigene Entry/Bronze/Silber/Gold/Platin-Kette erhalten | 260802-c5f | COVERED | Task 1–2
CONTEXT | D-04 | Neutrale deutsche Titel und Rollenanzahl Singular/Plural | 260802-c5f | COVERED | Task 1–2
</source_audit>

<success_criteria>
- Der allgemeine Fortschrittszähler und -balken verwenden ausschließlich Nicht-Rollen-Auszeichnungen.
- Die Rollenfläche enthält keine nicht verdiente Rolle und keine Schlossdarstellung für fremde Rollen.
- Jede verdiente Rolle behält die bestehende fünfteilige Entwicklung Einstieg/Bronze/Silber/Gold/Platin.
- Die Rollen-Zusammenfassung ist grammatisch korrekt und alle deutschen UI-Texte enthalten korrekte Umlaute.
- Bestehende Artwork-, Carousel-, Beitragsfortschritts- und Badge-Gruppierungstests bleiben grün.
</success_criteria>

<output>
After completion, create `.planning/quick/260802-c5f-rollen-auszeichnungen-aus-dem-gesamtfort/260802-c5f-SUMMARY.md`.
</output>
