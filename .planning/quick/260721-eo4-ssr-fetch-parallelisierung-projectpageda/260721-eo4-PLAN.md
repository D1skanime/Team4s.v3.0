---
phase: quick-260721-eo4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/anime/[id]/group/[groupId]/projectPageData.ts
autonomous: true
requirements:
  - 260721-eo4-ssr-fetch-parallelisierung
user_setup: []

must_haves:
  truths:
    - "Rückgabeobjekt von loadPublicFansubProjectPageData ist bit-identisch zum Ist-Code (gleiche Felder, gleiche not-found/error-Pfade, gleiche Per-Branch-Fallbacks)."
    - "Phase A (getGroupDetail + getAnimeByID) läuft weiterhin zuerst und gated 404→not-found bzw. null/Fehler→error VOR jeglicher Phase-B-Arbeit."
    - "Die unabhängigen Phase-B-Fetches laufen nebenläufig in einem Promise.all statt seriell; keine Branch reißt bei Fehler eine andere mit."
    - "canonicalProjectPath und fansubProjectNavigation werden aus EINEM getPublicFansubProfileBySlug-Aufruf (identischer Slug) abgeleitet statt aus zwei getrennten Aufrufen."
    - "Der Release-Vorschau-Block bleibt intern seriell (release-list cursor → dann release-detail per ID) und konsumiert denselben canonicalProjectPath wie heute."
    - "Datei-Zeilenzahl steigt nicht über das bisherige Maß (aktuell 489); Ziel < 450."
  artifacts:
    - path: "frontend/src/app/anime/[id]/group/[groupId]/projectPageData.ts"
      provides: "Parallelisierte SSR-Ladefunktion loadPublicFansubProjectPageData (verhaltenserhaltend)"
      contains: "Promise.all"
  key_links:
    - from: "loadPublicFansubProjectPageData"
      to: "getPublicFansubProfileBySlug"
      via: "einmaliges Profil-Promise, von dem canonicalProjectPath UND fansubProjectNavigation abgeleitet werden"
      pattern: "getPublicFansubProfileBySlug"
---

<objective>
`loadPublicFansubProjectPageData` in `projectPageData.ts` führt aktuell die "Phase B"
(~9 SSR-Fetches) weitgehend seriell aus, obwohl fast alle voneinander unabhängig sind.
Diese Phase B wird nebenläufig gemacht — bei BIT-IDENTISCHEM beobachtbarem Verhalten.

Purpose: SSR-TTFB der öffentlichen Fansubprojekt-Detailseite senken, ohne funktionale,
UI-, API- oder Backend-Änderung. Reine Nebenläufigkeits-Umstrukturierung.

Output: Überarbeitete `projectPageData.ts` mit parallelisierter Phase B; gleiches
Rückgabeobjekt `LoadPublicFansubProjectPageDataResult`, gleiche Fallback-/Fehlerpfade.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@frontend/src/app/anime/[id]/group/[groupId]/projectPageData.ts

<constraints_from_claude_md>
- Datei-Zeilen-Limit 450 (CLAUDE.md). Die Datei liegt bereits bei 489 Zeilen —
  die Änderung darf sie NICHT über das bisherige Maß heben; Ziel ist eine Senkung
  unter 450 (weniger serielle Boilerplate → weniger Zeilen).
- Deutsche Umlaute nur in NEUEN user-facing Strings relevant — hier wird KEIN
  user-facing String erzeugt (reine Datenlade-Logik). Bestehende Kommentare/Strings
  nicht umformulieren.
- @/components/ui-Pflicht ist hier NICHT einschlägig (keine UI).
- Keine Änderung an `frontend/src/lib/api.ts` oder Backend.
</constraints_from_claude_md>

<current_dependency_structure>
Verifizierter Ist-Stand (projectPageData.ts, loadPublicFansubProjectPageData ab Z. 246):

PHASE A (Z. ~256, BLEIBT UNVERÄNDERT):
- `Promise.all([getGroupDetail(animeID, groupID), getAnimeByID(animeID)])`
- 404 (ApiError.status===404) → return {status:"not-found"}.
- catch (sonstiger Fehler) → errorMessage gesetzt.
- Danach: fehlt groupResponse ODER animeResponse → return {status:"error", animeID, groupID, message}.
- Erst danach: `group = groupResponse.data`, `anime = animeResponse.data`.
- Diese Gate-Logik MUSS unverändert und VOR Phase B bleiben.

PHASE B (Z. ~278–392, aktuell seriell — zu parallelisieren):
1. Profil-Fetch A (Z. 282): `getPublicFansubProfileBySlug(canonicalFansubSlug)`
   → `project = profile.data.projects.find(item => item.id === animeID && item.anime_slug?.trim())`
   → `canonicalProjectPath = project?.anime_slug ? buildPublicFansubProjectPath(canonicalFansubSlug, project.anime_slug) : null`.
   try/catch → canonicalProjectPath=null. Guard: nur wenn `canonicalFansubSlug` (=`group.fansub.slug?.trim()`) truthy.
2. getGroupAssets (Z. 291) → groupAssetsResponse; try/catch schluckt Fehler (bleibt null).
3. Promise.all([getGroupReleases(per_page:100), getAnimeFansubs]) (Z. 302) mit
   VERSCHACHTELTEM Fallback: bei Fehler nochmal NUR getGroupReleases (Z. 310), sonst
   releaseEpisodes/otherGroups/animeFansubRelations auf Defaults ([]/[]/null).
4. Release-Vorschau-Block (Z. 319–344): getGroupReleaseListCursor(limit:1, sort:"release_date")
   → DANN (intern seriell) getGroupReleaseDetail(latestRelease.id) mit eigenem try/catch→null
   → publicReleasePreviews via buildPublicReleasePreview. KONSUMIERT canonicalProjectPath (Z. 339).
   Eigenes try/catch → previews bleiben []. WICHTIG: Dieser Block ist im Ist-Code ein
   SEPARATES try/catch als die canonicalProjectPath-Ableitung (#1); ein Fehler beim
   release-list-Fetch DARF canonicalProjectPath NICHT verwerfen.
5. getGroupContributors (Z. 356) → contributorsData; try/catch → Default {team_members:[],external_contributors:[]}.
6. getGroupThemes (Z. 361) → themesData; try/catch → Default {themes:[]}.
7. getGroupReleaseMedia (Z. 366) → releaseMediaData; try/catch → Default {items:[]}.
8. getGroupProjectNote (Z. 371) → projectNotesHtml = response.data?.body_html?.trim() || null; try/catch → null.
9. Profil-Fetch B (Z. 380): `getPublicFansubProfileBySlug(fansubSlug)` (IDENTISCHER Slug
   wie #1) → `currentProject = data.projects.find(p => p.id === animeID)` (KEIN anime_slug-Guard)
   → fansubProjectNavigation via buildFansubProjectNavigation(currentAnimeID, currentAnimeSlug:
   currentProject?.anime_slug ?? null, currentFansubGroupID, currentFansubSlug, projects).
   try/catch → Default {previous:null, next:null}. Guard: nur wenn fansubSlug truthy.

Abhängigkeiten: #4 hängt von canonicalProjectPath (aus #1) ab. #1 und #9 nutzen
IDENTISCHEN Slug (`group.fansub.slug?.trim()`) und dasselbe Profil. Alle anderen
(#2,#3,#5,#6,#7,#8) sind untereinander unabhängig.

Return (ab Z. ~394): hasTeamContent, storyAvailable, hasReleases, hasThemes, hasMedia,
navigationGroups, breadcrumbItems und alle Hero/Poster/Style-Ableitungen UNVERÄNDERT.
</current_dependency_structure>

<interfaces>
Signaturen der genutzten api.ts-Funktionen (frontend/src/lib/api.ts) — NICHT ändern:
- getGroupDetail(animeID, groupID)
- getAnimeByID(animeID)
- getGroupAssets(animeID, groupID)
- getGroupReleases(animeID, groupID, { per_page })
- getAnimeFansubs(animeID)
- getGroupReleaseListCursor(animeID, groupID, { limit, sort })
- getGroupReleaseDetail(animeID, groupID, releaseVersionID)
- getGroupContributors(animeID, groupID)
- getGroupThemes(animeID, groupID)
- getGroupReleaseMedia(animeID, groupID)
- getGroupProjectNote(animeID, groupID)
- getPublicFansubProfileBySlug(slug)
Alle sind idempotente GETs; Reihenfolge der Effekte ist irrelevant, nur die
abgeleiteten Werte müssen identisch berechnet werden.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase B in projectPageData.ts parallelisieren (verhaltenserhaltend)</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/projectPageData.ts</files>
  <action>
Baue in `loadPublicFansubProjectPageData` nur die Phase B (aktuell Z. ~278–392) um.
Phase A (Z. 256–277, inkl. 404→not-found und null/Fehler→error Gate) bleibt Zeichen
für Zeichen unverändert und läuft weiterhin ZUERST. Auch der Return-Block ab Z. ~394
und alle abgeleiteten Flags/Styles bleiben unverändert.

Kern-Zusammenführung des doppelten Profil-Fetches:
Lege NACH dem Phase-A-Gate `canonicalFansubSlug = group.fansub.slug?.trim()` an und
erzeuge EIN gemeinsames Profil-Promise: falls `canonicalFansubSlug` truthy ist, ein
einzelner `getPublicFansubProfileBySlug(canonicalFansubSlug)`-Aufruf (als Promise, noch
nicht awaited), sonst `null`. Aus GENAU diesem Promise werden sowohl canonicalProjectPath
(#1) als auch fansubProjectNavigation (#9) abgeleitet — der zweite explizite Profil-Aufruf
entfällt (nicht nur via Next-Dedup). Bei fehlendem/null Slug bleibt canonicalProjectPath=null
und fansubProjectNavigation={previous:null,next:null} wie heute.

Faktorisiere eine kleine lokale Hilfsfunktion, die aus einem aufgelösten Profil den
canonicalProjectPath ableitet: gleiche find-Prädikat-Logik wie im Ist-Code
(item.id === animeID && item.anime_slug?.trim(), dann buildPublicFansubProjectPath(slug,
project.anime_slug), sonst null). Diese Hilfsfunktion wird von der canonical/navigation-Branch
UND intern von der Vorschau-Branch genutzt, damit der Wert bit-identisch berechnet wird.

Starte alle unabhängigen Branches in EINEM `Promise.all`. Jede Branch ist eine eigene
async-Closure, die ihr EIGENES try/catch behält und im Fehlerfall EXAKT den bisherigen
Fallback-Wert liefert (leere Arrays / null / Default-Objekte). KEINE Branch darf werfen,
sonst reißt Promise.all die anderen mit. Die Branches:
- Assets-Branch (#2): getGroupAssets → Response oder null.
- Releases-Branch (#3): Promise.all([getGroupReleases(per_page:100), getAnimeFansubs])
  MIT dem verschachtelten Fallback (bei Fehler nochmal nur getGroupReleases; bei erneutem
  Fehler Defaults []/[]/null). Liefert {releaseEpisodes, otherGroups, animeFansubRelations}.
- Profil-abgeleitete-Branch (#1+#9): `await profilePromise` (guarded), dann canonicalProjectPath
  über die Hilfsfunktion (eigenes try/catch→null) UND fansubProjectNavigation über
  buildFansubProjectNavigation mit den identischen Argumenten wie heute (eigenes try/catch→
  Default). Liefert {canonicalProjectPath, fansubProjectNavigation}. Der von HIER gelieferte
  canonicalProjectPath ist der maßgebliche Wert für das Return-Objekt.
- Vorschau-Branch (#4): `await profilePromise` (guarded) und canonicalProjectPath über
  dieselbe Hilfsfunktion in einem INNEREN try/catch→null ableiten; DANN in einem SEPARATEN
  try/catch getGroupReleaseListCursor(limit:1, sort:"release_date"), intern seriell gefolgt
  von getGroupReleaseDetail(latestRelease.id) (eigenes try/catch→null), dann
  buildPublicReleasePreview. Die Trennung der beiden try/catch ist Pflicht: ein Fehler beim
  release-list-Fetch darf die canonicalProjectPath-Ableitung nicht beeinflussen, genau wie
  im Ist-Code. Liefert publicReleasePreviews (Default []). Der intern verwendete
  canonicalProjectPath ist per Hilfsfunktion identisch zu dem der Profil-abgeleiteten-Branch.
- Contributors-Branch (#5), Themes-Branch (#6), ReleaseMedia-Branch (#7), ProjectNote-Branch
  (#8): jeweils der bisherige Einzel-Fetch mit try/catch und exakt bisherigem Default.

Der Vorschau-Block bleibt INTERN seriell (Detail braucht die release-id). Reihenfolge der
Effekte über die Branches ist egal; nur die abgeleiteten Werte müssen exakt gleich sein.

Kommentare: Nur WAS/WARUM der Parallelisierung beschreiben (z. B. "unabhängige SSR-Fetches
laufen nebenläufig"); KEINE allgemeine Performance-Behauptung. Bestehende deutsche
Kommentare/Strings nicht umformulieren.

Zeilenlimit: Prüfe VOR und NACH der Änderung `wc -l` der Datei. Die Datei liegt aktuell
bei 489 Zeilen (> 450). Der Umbau soll sie NICHT über 489 heben; Ziel ist Senkung < 450
durch Wegfall der seriellen Boilerplate. Falls sie nach dem Umbau dennoch über 489 läge,
wähle den minimal-invasiven Weg (kompaktere Branch-Closures), aber KEIN neuer Datei-Split
und keine funktionale Änderung.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && npx vitest run "src/app/anime/[id]/group/[groupId]/page.test.tsx"</automated>
    <automated>cd frontend && npm run lint</automated>
  </verify>
  <done>
- `npx tsc --noEmit` fehlerfrei.
- Bestehende Vitest-Suite für die Projektseite (page.test.tsx) grün.
- `npm run lint` ohne neue Fehler.
- Phase A (404→not-found, null/Fehler→error) unverändert und weiterhin vor Phase B.
- Phase-B-Fetches laufen in einem Promise.all; jede Branch mit eigenem try/catch und
  bisherigem Fallback; keine Branch reißt andere mit.
- Nur EIN getPublicFansubProfileBySlug-Aufruf; canonicalProjectPath und
  fansubProjectNavigation daraus abgeleitet.
- Vorschau-Block intern seriell und mit separatem try/catch von der canonicalProjectPath-
  Ableitung; nutzt denselben canonicalProjectPath.
- Rückgabeobjekt-Struktur und alle Flags/Styles identisch zum Ist-Code.
- Datei-Zeilenzahl ≤ 489 (Ziel < 450).
  </done>
</task>

</tasks>

<verification>
- Typecheck (`npx tsc --noEmit`) grün.
- Vitest-Regressionssuite der Projektseite grün (die einzige Suite, die
  `loadPublicFansubProjectPageData` importiert; sie prüft Export-Existenz + ProjectPage-
  Rendering, keine Fetch-Mocks — dient hier als Regressionsschutz gegen Signatur-/
  Strukturbruch).
- Lint grün.
- Manuelle Diff-Kontrolle: Phase-A-Block und Return-Block unverändert; nur Phase B
  umstrukturiert.
- LAUFZEIT-/TTFB-NACHMESSUNG (Live :3000) erfolgt EXTERN durch den Orchestrator NACH der
  Ausführung — der Executor führt sie NICHT durch, hinterlässt aber im SUMMARY einen
  expliziten Platzhalter/Hinweis dazu.
</verification>

<success_criteria>
- Verhaltenserhaltende Parallelisierung der Phase B in `loadPublicFansubProjectPageData`.
- Keine Änderung an api.ts, Backend, UI oder Rückgabekontrakt.
- Typecheck + Vitest-Projektseiten-Suite + Lint grün.
- Datei-Zeilenzahl nicht über bisherigem Maß (489); idealerweise < 450.
- SUMMARY enthält Hinweis, dass die TTFB-Nachmessung extern (Live :3000) erfolgt.
</success_criteria>

<output>
Create `.planning/quick/260721-eo4-ssr-fetch-parallelisierung-projectpageda/260721-eo4-SUMMARY.md` when done.
Im SUMMARY einen expliziten Platzhalter setzen: "TTFB-/Laufzeit-Nachmessung erfolgt extern
durch den Orchestrator (Live :3000) — nicht im Executor durchgeführt."
</output>
