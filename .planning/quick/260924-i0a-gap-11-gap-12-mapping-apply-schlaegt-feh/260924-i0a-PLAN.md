---
phase: quick-260924-i0a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/handlers/jellyfin_owned_sources.go
  - backend/internal/handlers/jellyfin_owned_sources_test.go
  - backend/internal/handlers/episode_version_source_hydration.go
  - backend/internal/handlers/admin_episode_import.go
  - backend/internal/handlers/admin_content_episode_version_editor_helpers.go
  - backend/internal/handlers/admin_episode_import_test.go
  - backend/internal/handlers/episode_version_source_hydration_test.go
  - backend/internal/handlers/admin_content_episode_version_editor_context_test.go
  - frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportApplyErrorAlert.tsx
  - frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportApplyErrorAlert.test.tsx
  - frontend/src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.ts
  - frontend/src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.test.tsx
  - frontend/src/app/admin/anime/[id]/episodes/import/page.tsx
  - frontend/src/app/admin/anime/[id]/episodes/import/page.module.css
  - .planning/phases/167-fansub-gruppenerkennung-beim-import/167-UAT.md
autonomous: true
requirements: [GAP-11, GAP-12]
user_setup: []

must_haves:
  truths:
    - "GAP-11: „Mapping anwenden\" (Episoden-Import-Apply) übernimmt erfolgreich eine Datei, die aus einem zusätzlich mit dem Anime verbundenen Jellyfin-Ordner stammt (nicht nur aus dem Haupt-Ordner), und legt Release/Version wie beim Haupt-Ordner-Fall korrekt an."
    - "GAP-11: Der Editor-Relink (Erstellen und Neu-Verknüpfen einer Episodenversion, episode_version_source_hydration.go) und die schreibgeschützte Datei-Voransicht im Editor (admin_content_episode_version_editor_helpers.go) akzeptieren ebenfalls Dateien aus jedem verbundenen Ordner, nicht nur dem Haupt-Ordner."
    - "GAP-11: Eine Datei, die zu KEINER mit dem Anime verbundenen Jellyfin-Serie/keinem verbundenen Ordner gehört, wird an allen drei Stellen weiterhin mit derselben Fehlermeldung abgelehnt (Fail-closed/IDOR-Schutz aus 165-04 bleibt unverändert wirksam)."
    - "GAP-11: Der Haupt-Ordner-Fall (ein einzelner verbundener Ordner) bleibt verhaltensgleich zu vorher, inklusive keiner zusätzlichen Jellyfin-Anfrage pro Datei."
    - "GAP-12: Schlägt „Mapping anwenden\" fehl, erscheint die Fehlermeldung zusätzlich unmittelbar unterhalb des Buttons, als role=\"alert\" ausgezeichnet, und die Seite scrollt/fokussiert dorthin."
    - "167-UAT.md dokumentiert GAP-11 und GAP-12 als eigene Einträge mit status: resolved und bleibt LF-only."
  artifacts:
    - path: "backend/internal/handlers/jellyfin_owned_sources.go"
      provides: "jellyfinOwnedSource, matchOwnedJellyfinSource, ownedJellyfinSourcesFromFolders, hydrateFansubFolderPathsForRelink -- die geteilte Mehrfach-Ordner-Besitzprüfung"
      contains: "func matchOwnedJellyfinSource"
    - path: "backend/internal/handlers/episode_version_source_hydration.go"
      provides: "resolveReviewedJellyfinSource akzeptiert jetzt owned []jellyfinOwnedSource statt eines einzelnen (seriesID, folder)-Paars"
      contains: "matchOwnedJellyfinSource(owned"
    - path: "backend/internal/handlers/admin_episode_import.go"
      provides: "rehydrateEpisodeImportSources baut die Besitz-Paare aus importContext.JellyfinFoldersForOwnershipCheck (bereits geladen, keine neue Anfrage)"
      contains: "ownedJellyfinSourcesFromFolders("
    - path: "backend/internal/handlers/admin_content_episode_version_editor_helpers.go"
      provides: "enrichEpisodeVersionSelectedFile baut dieselben Besitz-Paare über hydrateJellyfinFolderDisplayNames"
      contains: "ownedJellyfinSourcesFromFolders("
    - path: "frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportApplyErrorAlert.tsx"
      provides: "role=\"alert\"-Fehleranzeige direkt beim Apply-Button mit scrollIntoView+focus"
      contains: "role=\"alert\""
    - path: "frontend/src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.ts"
      provides: "applyErrorMessage-State, ausschließlich von applyMappings genutzt"
      contains: "applyErrorMessage"
    - path: ".planning/phases/167-fansub-gruppenerkennung-beim-import/167-UAT.md"
      provides: "GAP-11/GAP-12 als neue status: resolved Einträge"
      contains: "GAP-12"
  key_links:
    - from: "backend/internal/handlers/admin_episode_import.go rehydrateEpisodeImportSources"
      to: "backend/internal/handlers/jellyfin_owned_sources.go ownedJellyfinSourcesFromFolders"
      via: "ein Aufruf vor der Mapping-Schleife, aus dem bereits geladenen importContext"
      pattern: "ownedJellyfinSourcesFromFolders\\("
    - from: "backend/internal/handlers/episode_version_source_hydration.go resolveEpisodeVersionSource"
      to: "backend/internal/handlers/jellyfin_owned_sources.go hydrateFansubFolderPathsForRelink"
      via: "baut owned vor dem Aufruf von resolveReviewedJellyfinSource"
      pattern: "hydrateFansubFolderPathsForRelink\\("
    - from: "frontend/src/app/admin/anime/[id]/episodes/import/page.tsx"
      to: "frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportApplyErrorAlert.tsx"
      via: "rendert direkt unter dem workbenchActions-Button-Block mit builder.applyErrorMessage"
      pattern: "EpisodeImportApplyErrorAlert"
---

<objective>
GAP-11/GAP-12 (Phase 167, Live-UAT durch den Auftraggeber im Browser, 2026-09-24,
`/admin/anime/7/episodes/import`, Anime #7 „.hack//G.U. Returner" mit zwei verbundenen
Jellyfin-Ordnern -- Haupt-Ordner plus zusätzlich verbundener Ordner „dot hack gu returner",
Jellyfin-ID `c70e8b3d970a6c3952fa6289dec07744`, Pfad
`/media/Anime/OVA/Anime.OVA.Sub/dot_hack-G.U._Returner [GAX]`):

- **GAP-11 (Blocker) -- „Mapping anwenden" wird für Dateien aus dem zusätzlichen Ordner abgelehnt:**
  `resolveReviewedJellyfinSource` (`backend/internal/handlers/episode_version_source_hydration.go:16-33`)
  prüft `item.SeriesID`/`item.Path` bzw. `resolved.Snapshot.SourcePath` bislang ausschließlich gegen
  EIN einzelnes (seriesID, folder)-Paar -- den Haupt-Ordner. Aufrufer sind der Import-Apply-Pfad
  (`admin_episode_import.go:758`, `seriesID`/`folder` aus der ersten Jellyfin-Referenz plus
  `anime.FolderName`), der Editor-Relink (`episode_version_source_hydration.go:78`, dieselbe
  Herleitung aus `anime.Source`/`anime.SourceLinks`/`anime.FolderName`) und die schreibgeschützte
  Datei-Voransicht im Editor (`admin_content_episode_version_editor_helpers.go:146`). Für Dateien
  aus einem zusätzlich verbundenen Ordner (Phase 165 D-05/D-14) schlagen beide Prüfungen dadurch
  zwangsläufig fehl -- die GAP-07-Korrektur hatte nur die Vorschau (Kandidatenliste) auf den
  gewählten Ordner umgestellt, nicht diese drei Stellen.
- **GAP-12 -- Fehlermeldung ist praktisch unsichtbar:** Der Fehler aus `applyMappings` landet
  ausschließlich im allgemeinen `errorMessage`-State, der ganz oben auf der Seite gerendert wird --
  weit entfernt vom Button „Mapping anwenden", ohne `role="alert"`. Für den Auftraggeber sah ein
  fehlgeschlagener Klick dadurch aus, als würde er gar nichts tun.

Verbindliche Vorgaben (nicht neu verhandeln):
- Die Besitzprüfung akzeptiert JEDE mit dem Anime verbundene Jellyfin-Serie (`anime.source` plus
  alle `jellyfin:`-Einträge aus `anime_source_links`) und den zugehörigen Ordnerpfad. Fail-closed
  bleibt: Dateien, die zu KEINER verbundenen Serie/keinem verbundenen Ordner gehören, werden
  weiterhin abgelehnt (IDOR-Schutz aus 165-04 unverändert wirksam).
- Kein N+1: der Ordnerpfad zusätzlicher Ordner wird gebündelt (einmal pro Anfrage, nicht einmal
  pro Datei) ermittelt -- wiederverwendet wird entweder der bereits geladene
  `importContext.JellyfinFoldersForOwnershipCheck`-Snapshot (Import-Apply) oder eine neue, ebenso
  gebündelte Batch-Anfrage (Editor-Pfade).
- GAP-11/GAP-12 werden in `167-UAT.md` als `status: resolved` dokumentiert (LF-Zeilenenden
  beibehalten).
- Keine Datenänderung an `team4s_v2`; keine Migration von Bestandsdaten.
- Direkt auf `main`, kein `git stash`, kein `git push`, keine Datei über 450 Zeilen.
  `admin_episode_import.go` (bereits 783 Zeilen) darf NICHT wachsen -- die neue Logik lebt in
  `jellyfin_owned_sources.go`.
- GAP-12 nutzt ausschließlich `@/components/ui`-Primitives (CLAUDE.md
  Frontend-UI-Design-System-Regel).

Purpose: Der Admin kann Episoden-Dateien aus JEDEM verbundenen Jellyfin-Ordner zuverlässig
übernehmen (nicht nur aus dem Haupt-Ordner), und sieht einen fehlgeschlagenen Übernehmen-Versuch
sofort dort, wo er geklickt hat, statt einen stillen Fehlschlag zu vermuten.

Output: Neue geteilte Mehrfach-Ordner-Besitzprüfung (`jellyfin_owned_sources.go`), angewendet an
allen drei betroffenen Stellen (Import-Apply, Editor-Relink, Editor-Datei-Voransicht); neue
`EpisodeImportApplyErrorAlert.tsx` (role="alert", scroll+focus) verdrahtet über einen neuen
`applyErrorMessage`-State; `167-UAT.md` mit zwei neuen `status: resolved`-Einträgen; beide
Container neu gebaut/neu gestartet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/167-fansub-gruppenerkennung-beim-import/167-UAT.md
@backend/internal/handlers/episode_version_source_hydration.go
@backend/internal/handlers/admin_episode_import.go
@backend/internal/handlers/admin_content_episode_version_editor_helpers.go
@frontend/src/app/admin/anime/[id]/episodes/import/page.tsx
@frontend/src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.ts

<critical_gotcha>
Der Dev-Backend-Container (`team4sv30-backend`) läuft über `air` mit `docker compose watch`
live-sync von `./backend` nach `/app`. Läuft `docker compose watch` gerade NICHT im Hintergrund,
liefert `docker exec team4sv30-backend ... go test` stillschweigend Ergebnisse gegen eine veraltete,
zur Build-Zeit eingebackene Quellcode-Kopie. Für Go-Build/Vet/Test-Verifikation in diesem Plan
IMMER das Scratch-Container-Muster verwenden (mountet den Host-Ordner direkt, keine
Live-Sync-Unsicherheit):
`docker run --rm -v /home/d1sk/team4s/backend:/app -w /app -v gomodcache:/tmp/gomodcache -v
gocache:/tmp/gocache -e GOMODCACHE=/tmp/gomodcache -e GOCACHE=/tmp/gocache --network
team4s_default golang:1.25-alpine sh -c "go build ./... && go vet ./... && go test ./..."`.
Postgres-basierte Handler-Tests (`openEVECFixture`/`testsupport.OpenPhase117Postgres`) brauchen
`--network team4s_default`, damit sie die laufende Postgres-Instanz erreichen. `docker exec
team4sv30-backend` ist ausschließlich für den finalen, bereits neu gebauten Container (Task 4,
Health-Check) reserviert.

Der Frontend-Container (`team4sv30-frontend`) bind-mountet `./frontend:/app` und läuft mit
`npm run dev -- --webpack` (Fast Refresh) -- Host-Dateiänderungen sind sofort sichtbar,
`docker compose exec -T team4sv30-frontend sh -c "cd /app && ..."` liest also immer den aktuellen
Stand.
</critical_gotcha>

<interfaces>
Vollständiger Inhalt der neuen Datei `backend/internal/handlers/jellyfin_owned_sources.go`:

```go
package handlers

import (
	"context"
	"strings"

	"team4s.v3/backend/internal/models"
)

// jellyfinOwnedSource is one (seriesID, folderPath) pair an anime is allowed
// to accept a Jellyfin file's ownership from (GAP-11, 167-UAT.md). An anime
// can be connected to more than one Jellyfin folder (Phase 165 D-05/D-18) --
// resolveReviewedJellyfinSource now accepts a file if it matches ANY owned
// pair, not just the anime's single main folder. An empty SeriesID or
// FolderPath on a pair means "not checked on that dimension" (mirrors the
// pre-GAP-11 single-pair contract, where an empty seriesID/folder meant the
// caller had nothing to compare against).
type jellyfinOwnedSource struct {
	SeriesID   string
	FolderPath string
}

// matchOwnedJellyfinSource returns the first owned pair the given item
// satisfies. Fail-closed: an item whose SeriesID/Path doesn't satisfy ANY
// owned pair is rejected -- the IDOR protection from 165-04 is unchanged by
// this generalization, it just now checks against a SET of legitimate pairs
// instead of a single one.
func matchOwnedJellyfinSource(owned []jellyfinOwnedSource, seriesID, itemPath string) (jellyfinOwnedSource, bool) {
	for _, entry := range owned {
		if entry.SeriesID != "" && entry.SeriesID != seriesID {
			continue
		}
		if entry.FolderPath != "" && !jellyfinPathHasPrefix(itemPath, entry.FolderPath) {
			continue
		}
		return entry, true
	}
	return jellyfinOwnedSource{}, false
}

// ownedJellyfinSourcesFromFolders builds the owned-pair list for
// resolveReviewedJellyfinSource. The main (mainSeriesID, mainFolderPath) pair
// is ALWAYS included exactly as the caller already resolved it -- byte-
// identical to the pre-GAP-11 single-folder contract, including callers that
// resolve a main seriesID not literally derivable from source/source_links
// (e.g. resolveEpisodeImportSeriesByFolderPath's heuristic fallback, or a
// caller-supplied seriesID in a unit test). Every OTHER (non-main) folder in
// folders contributes an additional pair, using its own already-hydrated
// FolderPath (never re-derived from mainFolderPath).
func ownedJellyfinSourcesFromFolders(mainSeriesID string, mainFolderPath *string, folders []models.JellyfinFolderOption) []jellyfinOwnedSource {
	owned := []jellyfinOwnedSource{{SeriesID: strings.TrimSpace(mainSeriesID), FolderPath: normalizeJellyfinPath(mainFolderPath)}}
	for _, folder := range folders {
		if folder.IsMain {
			continue
		}
		owned = append(owned, jellyfinOwnedSource{SeriesID: strings.TrimSpace(folder.JellyfinItemID), FolderPath: normalizeJellyfinPath(folder.FolderPath)})
	}
	return owned
}

// hydrateFansubFolderPathsForRelink batched-populates FolderPath on every
// NON-main connected folder via one Jellyfin /Items call (never per-file --
// called once per relink/create request, regardless of how many folders are
// connected; zero calls when there is only one connected folder). Mirrors
// hydrateJellyfinFolderDisplayNames's fail-open, no-op-below-two-folders
// contract (jellyfin_folder_display_names.go), but as a free function so
// FansubHandler's resolveEpisodeVersionSource
// (episode_version_source_hydration.go) can reuse it too --
// hydrateJellyfinFolderDisplayNames itself stays bound to AdminContentHandler,
// unchanged.
func hydrateFansubFolderPathsForRelink(ctx context.Context, folders []models.JellyfinFolderOption, getItems func(context.Context, []string) (map[string]jellyfinEpisodeItem, error)) []models.JellyfinFolderOption {
	if len(folders) <= 1 || getItems == nil {
		return folders
	}
	ids := make([]string, 0, len(folders))
	for _, folder := range folders {
		if !folder.IsMain {
			ids = append(ids, folder.JellyfinItemID)
		}
	}
	if len(ids) == 0 {
		return folders
	}
	items, err := getItems(ctx, ids)
	if err != nil {
		return folders // fail-open: extra folders keep an empty FolderPath -> seriesID-only match in ownedJellyfinSourcesFromFolders
	}
	for i := range folders {
		if folders[i].IsMain {
			continue
		}
		item, ok := items[folders[i].JellyfinItemID]
		if !ok {
			continue
		}
		if path := strings.TrimSpace(item.Path); path != "" {
			folderPath := path
			folders[i].FolderPath = &folderPath
		}
	}
	return folders
}
```

Aktuelles `resolveReviewedJellyfinSource` (episode_version_source_hydration.go:16-34, VOLLSTÄNDIG,
wird ersetzt):
```go
func resolveReviewedJellyfinSource(item jellyfinEpisodeItem, itemID, sourceID, seriesID, folder string, stored *models.JellyfinSourceSnapshot) (resolvedJellyfinMediaSource, error) {
	fail := func(message string) (resolvedJellyfinMediaSource, error) {
		return resolvedJellyfinMediaSource{}, fmt.Errorf("%s", message)
	}
	if item.ID != itemID || item.Type != "Episode" || (seriesID != "" && strings.TrimSpace(item.SeriesID) != seriesID) || (folder != "" && !jellyfinPathHasPrefix(item.Path, folder)) {
		return fail("Die Jellyfin-Datei gehört nicht zur gespeicherten Anime-Zuordnung.")
	}
	resolved, err := resolveJellyfinMediaSourceSelection(item, sourceID, stored)
	if err != nil || (sourceID != "" && resolved.Snapshot.MediaSourceID != sourceID) {
		return fail("Die geprüfte Jellyfin-Quelle hat sich geändert. Bitte Vorschau neu laden.")
	}
	if folder != "" && !jellyfinPathHasPrefix(resolved.Snapshot.SourcePath, folder) {
		return fail("Die Jellyfin-Quelle gehört nicht zum gespeicherten Anime-Ordner.")
	}
	if !resolved.Snapshot.StreamsComplete && (stored == nil || !stored.StreamsComplete) {
		return fail("Die Jellyfin-Quelle enthält keine vollständigen Stream-Daten. Bitte Vorschau neu laden.")
	}
	return resolved, nil
}
```
Neue Version (ersetzt die obige 1:1, gleiche Fehlermeldungen/Reihenfolge, nur die
Besitzprüfung wird auf die Paar-Liste generalisiert):
```go
func resolveReviewedJellyfinSource(item jellyfinEpisodeItem, itemID, sourceID string, owned []jellyfinOwnedSource, stored *models.JellyfinSourceSnapshot) (resolvedJellyfinMediaSource, error) {
	fail := func(message string) (resolvedJellyfinMediaSource, error) {
		return resolvedJellyfinMediaSource{}, fmt.Errorf("%s", message)
	}
	if item.ID != itemID || item.Type != "Episode" {
		return fail("Die Jellyfin-Datei gehört nicht zur gespeicherten Anime-Zuordnung.")
	}
	match, ok := matchOwnedJellyfinSource(owned, strings.TrimSpace(item.SeriesID), item.Path)
	if !ok {
		return fail("Die Jellyfin-Datei gehört nicht zur gespeicherten Anime-Zuordnung.")
	}
	resolved, err := resolveJellyfinMediaSourceSelection(item, sourceID, stored)
	if err != nil || (sourceID != "" && resolved.Snapshot.MediaSourceID != sourceID) {
		return fail("Die geprüfte Jellyfin-Quelle hat sich geändert. Bitte Vorschau neu laden.")
	}
	if match.FolderPath != "" && !jellyfinPathHasPrefix(resolved.Snapshot.SourcePath, match.FolderPath) {
		return fail("Die Jellyfin-Quelle gehört nicht zum gespeicherten Anime-Ordner.")
	}
	if !resolved.Snapshot.StreamsComplete && (stored == nil || !stored.StreamsComplete) {
		return fail("Die Jellyfin-Quelle enthält keine vollständigen Stream-Daten. Bitte Vorschau neu laden.")
	}
	return resolved, nil
}
```

`resolveEpisodeVersionSource` (episode_version_source_hydration.go:57-65 und :78, restliche
Funktion UNVERÄNDERT): Zeile 57-61 aktuell
```go
	seriesID := jellyfinSeriesIDFromAnimeSource(anime.Source, anime.SourceLinks)
	folder := normalizeJellyfinPath(anime.FolderName)
	if seriesID == "" && folder == "" {
		return fail(409, "Der Anime hat keine überprüfbare Jellyfin-Zuordnung.")
	}
```
wird zu
```go
	seriesID := jellyfinSeriesIDFromAnimeSource(anime.Source, anime.SourceLinks)
	if seriesID == "" && normalizeJellyfinPath(anime.FolderName) == "" {
		return fail(409, "Der Anime hat keine überprüfbare Jellyfin-Zuordnung.")
	}
	folders := hydrateFansubFolderPathsForRelink(ctx, collectJellyfinFolderOptions(anime.Source, anime.SourceLinks, anime.Source), h.getJellyfinSourceItems)
	owned := ownedJellyfinSourcesFromFolders(seriesID, anime.FolderName, folders)
```
und Zeile 78 aktuell `resolved, err := resolveReviewedJellyfinSource(items[itemID], itemID, selector, seriesID, folder, stored)`
wird zu `resolved, err := resolveReviewedJellyfinSource(items[itemID], itemID, selector, owned, stored)`.
`collectJellyfinFolderOptions` ist bereits im `handlers`-Package definiert
(jellyfin_source_folder_list.go) und ohne Import nutzbar; `h.getJellyfinSourceItems` existiert
bereits als Methode auf `*FansubHandler` (jellyfin_source_batch.go:32).

`rehydrateEpisodeImportSources` (admin_episode_import.go:728-733 und :758, restliche Funktion
UNVERÄNDERT -- Netto-Zeilenänderung dieser Datei: 0, siehe Task 1 Aktion): Zeile 728-732 aktuell
```go
	seriesID := strings.TrimSpace(derefString(importContext.JellyfinSeriesID))
	folder := normalizeJellyfinPath(importContext.FolderPath)
	if seriesID == "" && folder == "" {
		return fail(http.StatusConflict, "Der Anime hat keine überprüfbare Jellyfin-Zuordnung.")
	}
```
wird zu
```go
	seriesID := strings.TrimSpace(derefString(importContext.JellyfinSeriesID))
	owned := ownedJellyfinSourcesFromFolders(seriesID, importContext.FolderPath, importContext.JellyfinFoldersForOwnershipCheck)
	if seriesID == "" && normalizeJellyfinPath(importContext.FolderPath) == "" {
		return fail(http.StatusConflict, "Der Anime hat keine überprüfbare Jellyfin-Zuordnung.")
	}
```
(gleiche 5 Zeilen wie vorher -- nur `folder :=` durch `owned := ownedJellyfinSourcesFromFolders(...)`
ersetzt und die Guard-Bedingung inline normalisiert). Zeile 758 aktuell
`resolved, err := resolveReviewedJellyfinSource(item, mapping.MediaItemID, mapping.MediaSourceID, seriesID, folder, stored)`
wird zu
`resolved, err := resolveReviewedJellyfinSource(item, mapping.MediaItemID, mapping.MediaSourceID, owned, stored)`
(gleiche Zeilenzahl). `importContext.JellyfinFoldersForOwnershipCheck` ist bereits von
`loadEpisodeImportContext` (Zeile 199-218, bereits vor `ApplyEpisodeImport`s Aufruf bei Zeile 145
ausgeführt) über `hydrateJellyfinFolderDisplayNames` gebündelt geladen -- KEINE neue
Jellyfin-Anfrage nötig.

`enrichEpisodeVersionSelectedFile` (admin_content_episode_version_editor_helpers.go:127-156,
VOLLSTÄNDIG, wird ersetzt):
```go
func (h *AdminContentHandler) enrichEpisodeVersionSelectedFile(
	ctx context.Context, version *models.EpisodeVersion, anime *models.AdminAnimeSyncSource, folder *string, seriesID string,
) (*models.EpisodeVersionMediaFile, bool) {
	if version == nil || anime == nil || !strings.EqualFold(strings.TrimSpace(version.MediaProvider), "jellyfin") || !h.ensureJellyfinConfiguredForEditor() {
		return nil, false
	}
	itemID := strings.TrimSpace(version.MediaItemID)
	if itemID == "" || (seriesID == "" && normalizeJellyfinPath(folder) == "") {
		return nil, false
	}
	folders := h.hydrateJellyfinFolderDisplayNames(ctx, collectJellyfinFolderOptions(anime.Source, anime.SourceLinks, anime.Source), folder)
	owned := ownedJellyfinSourcesFromFolders(seriesID, folder, folders)
	items, err := fetchJellyfinSourceBatch(ctx, h.httpClient, h.jellyfinBaseURL, h.jellyfinAPIKey, []string{itemID}, true)
	if err != nil {
		return nil, true
	}
	binding := version.JellyfinSource
	if binding == nil && version.MediaSourceID != nil {
		binding = &models.JellyfinSourceSnapshot{Version: 1, MediaSourceID: *version.MediaSourceID}
	}
	source, err := resolveReviewedJellyfinSource(items[itemID], itemID, strings.TrimSpace(derefString(version.MediaSourceID)), owned, binding)
	if err != nil {
		return nil, true
	}
	if version.DurationSeconds == nil {
		version.DurationSeconds = source.DurationSeconds
	}
	file := buildEpisodeVersionMediaFile(items[itemID], source, h.buildJellyfinEditorStreamURL)
	file.ChapterHints = &source.ChapterHints
	return &file, false
}
```
(einzige Änderungen ggü. dem Original: die alte `normalizedFolder := normalizeJellyfinPath(folder)`-Zeile
entfällt zugunsten von zwei neuen Zeilen, die `folders`/`owned` bauen; der finale Aufruf übergibt
`owned` statt `seriesID, normalizedFolder`).

Aktuelles `EpisodeImportApplyMappingRow`/`errorMessage`-Rendering (page.tsx:119-121, wird Task 3
angepasst):
```tsx
{builder.errorMessage ? (
  <div className={styles.error}>{builder.errorMessage}</div>
) : null}
```
und der Apply-Button-Block (page.tsx:220-245, `workbenchActions`-div, Task 3 fügt danach das neue
Alert-Element ein):
```tsx
<div className={styles.workbenchActions}>
  ...
  <button className={styles.primaryButton} type="button" disabled={!builder.canApply || builder.isApplying}
    onClick={() => void builder.applyMappings()}>
    {builder.isApplying ? "Wendet an..." : "Mapping anwenden"}
  </button>
</div>
```

`applyMappings` (useEpisodeImportBuilder.ts, aktuell, wird Task 3 angepasst):
```ts
async function applyMappings() {
  if (!animeID || !preview || !canApply) return
  setIsApplying(true)
  setErrorMessage(null)
  try {
    const response = await applyEpisodeImport(animeID, buildEpisodeImportApplyInput(animeID, preview, mappings))
    setApplyResult(response.data)
  } catch (error) {
    setErrorMessage(formatEpisodeImportError(error, 'Mapping konnte nicht angewendet werden.'))
  } finally {
    setIsApplying(false)
  }
}
```

`ErrorState` Primitive (frontend/src/components/ui/ErrorState.tsx, unverändert, wiederverwendet):
`export function ErrorState({ title, description, action }: ErrorStateProps)` -- exportiert über
`@/components/ui` (index.ts: `export * from './ErrorState'`).

Bestehender `scrollIntoView`-Test-Shim (Konvention, z. B.
`frontend/src/app/admin/roles/RolesClient.test.tsx:52-55`):
```ts
if (typeof Element.prototype.scrollIntoView !== 'function') {
  Element.prototype.scrollIntoView = vi.fn()
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GAP-11 -- geteilte Mehrfach-Ordner-Besitzprüfung an allen drei Stellen</name>
  <files>backend/internal/handlers/jellyfin_owned_sources.go, backend/internal/handlers/jellyfin_owned_sources_test.go, backend/internal/handlers/episode_version_source_hydration.go, backend/internal/handlers/admin_episode_import.go, backend/internal/handlers/admin_content_episode_version_editor_helpers.go</files>
  <behavior>
    - Test 1: `matchOwnedJellyfinSource` mit zwei Paaren (series-401/„/data/anime/main", series-402/„/data/anime/second") liefert für eine Datei aus dem Haupt-Ordner UND für eine Datei aus dem zweiten Ordner jeweils `ok=true` und das passende Paar; für eine fremde SeriesID und für die richtige SeriesID mit falschem Pfad jeweils `ok=false`.
    - Test 2: ein Paar mit leerem `FolderPath` matcht JEDEN Pfad derselben SeriesID (Wildcard nur auf der Pfad-Dimension, SeriesID bleibt Pflicht).
    - Test 3: `ownedJellyfinSourcesFromFolders` liefert für `folders=[{IsMain:true}]` genau ein Paar aus den explizit übergebenen `mainSeriesID`/`mainFolderPath` (der `FolderPath` des Haupt-Folder-Eintrags selbst wird ignoriert -- Haupt-Fall bleibt byte-identisch zum Vorher-Zustand).
    - Test 4: `ownedJellyfinSourcesFromFolders` liefert für `folders=[{IsMain:true}, {IsMain:false, JellyfinItemID:"series-402", FolderPath:"/data/anime/second"}]` zwei Paare -- das explizite Haupt-Paar plus ein Paar aus dem zweiten Folder-Eintrag.
    - Test 5: `ownedJellyfinSourcesFromFolders("", mainFolderPath, nil)` (keine jellyfin:-Referenzen auffindbar) liefert genau ein Paar `{FolderPath: normalisierter mainFolderPath}` -- der Legacy-Fallback.
    - Test 6: `hydrateFansubFolderPathsForRelink` ruft `getItems` NICHT auf, wenn `len(folders)<=1`.
    - Test 7: `hydrateFansubFolderPathsForRelink` ruft `getItems` genau einmal auf, NUR mit den Nicht-Haupt-Folder-IDs, und befüllt nur deren `FolderPath` (Haupt-Eintrag bleibt `nil`).
    - Test 8: `hydrateFansubFolderPathsForRelink` gibt bei einem `getItems`-Fehler die Folder-Liste unverändert zurück (fail-open).
  </behavior>
  <action>
Erstelle `backend/internal/handlers/jellyfin_owned_sources.go` exakt wie im `<interfaces>`-Block
gezeigt (Typ `jellyfinOwnedSource`, `matchOwnedJellyfinSource`, `ownedJellyfinSourcesFromFolders`,
`hydrateFansubFolderPathsForRelink`).

Erstelle `jellyfin_owned_sources_test.go` mit den acht Fällen aus `<behavior>` als Table-Driven-
bzw. Einzel-Tests (`TestMatchOwnedJellyfinSource`, `TestOwnedJellyfinSourcesFromFolders`,
`TestHydrateFansubFolderPathsForRelink`), reine In-Memory-Tests ohne Postgres/HTTP -- `getItems`
als lokale Closure gemockt, die aufgerufenen IDs in einer Variable festhalten und prüfen.

In `episode_version_source_hydration.go`: ersetze `resolveReviewedJellyfinSource` (Zeile 16-34)
1:1 durch die im `<interfaces>`-Block gezeigte neue Version (Signatur wechselt von
`(..., seriesID, folder string, ...)` zu `(..., owned []jellyfinOwnedSource, ...)`, gleiche
Fehlermeldungen/Reihenfolge). Ändere `resolveEpisodeVersionSource` exakt wie im
`<interfaces>`-Block gezeigt: die `seriesID`/`folder`/Guard-Zeilen (57-61) werden um den Aufbau von
`folders`/`owned` über `hydrateFansubFolderPathsForRelink`/`ownedJellyfinSourcesFromFolders`
ergänzt, und der `resolveReviewedJellyfinSource`-Aufruf (Zeile 78) übergibt `owned` statt
`seriesID, folder`. Der Rest der Datei (inkl. `hydrateEpisodeVersionCreate`/
`hydrateEpisodeVersionPatch`) bleibt unverändert.

In `admin_episode_import.go`: ändere `rehydrateEpisodeImportSources` NUR an den beiden im
`<interfaces>`-Block gezeigten Stellen (Zeile 728-732 und Zeile 758) -- Netto-Zeilenzahl der
kompletten Datei bleibt exakt gleich (0 Zeilen Differenz; die Datei ist bereits bei 783 Zeilen und
darf laut Vorgabe nicht wachsen). Keine sonstige Zeile in dieser Datei anfassen.

In `admin_content_episode_version_editor_helpers.go`: ersetze `enrichEpisodeVersionSelectedFile`
(Zeile 127-156) 1:1 durch die im `<interfaces>`-Block gezeigte neue Version. Der Rest der Datei
bleibt unverändert.
  </action>
  <verify>
    <automated>docker run --rm -v /home/d1sk/team4s/backend:/app -w /app -v gomodcache:/tmp/gomodcache -v gocache:/tmp/gocache -e GOMODCACHE=/tmp/gomodcache -e GOCACHE=/tmp/gocache --network team4s_default golang:1.25-alpine sh -c "go build ./... && go vet ./... && go test ./internal/handlers/... -run 'TestMatchOwnedJellyfinSource|TestOwnedJellyfinSourcesFromFolders|TestHydrateFansubFolderPathsForRelink' -v" && wc -l /home/d1sk/team4s/backend/internal/handlers/admin_episode_import.go</automated>
  </verify>
  <done>jellyfin_owned_sources.go exists with jellyfinOwnedSource/matchOwnedJellyfinSource/
  ownedJellyfinSourcesFromFolders/hydrateFansubFolderPathsForRelink; resolveReviewedJellyfinSource,
  resolveEpisodeVersionSource, rehydrateEpisodeImportSources and enrichEpisodeVersionSelectedFile
  all use the new owned-pair matcher instead of a single (seriesID, folder) pair; go build/go vet
  are clean; all eight new unit tests pass; admin_episode_import.go's line count is unchanged
  (still 783 lines, not larger).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GAP-11 -- Regressionsbeweis (zweiter Ordner erfolgreich, fremder Ordner weiter abgelehnt) an allen drei Stellen</name>
  <files>backend/internal/handlers/admin_episode_import_test.go, backend/internal/handlers/episode_version_source_hydration_test.go, backend/internal/handlers/admin_content_episode_version_editor_context_test.go</files>
  <behavior>
    - Test 1 (admin_episode_import_test.go, NEU): Anime 301 (openEVECFixture) bekommt per
      `INSERT INTO anime_source_links (anime_id, source) VALUES (301, 'jellyfin:series-402')`
      einen zweiten verbundenen Ordner. Szenario „second folder": die Datei (`item.SeriesID =
      "series-402"`, `item.Path`/`MediaSources[1].Path = "/data/anime/SecondFolder/one.mkv"`) wird
      per `ApplyEpisodeImport` erfolgreich übernommen (200, `spy.calls == 1`,
      `spy.received.MediaCandidates[0].Path` zeigt auf den zweiten Ordner). Szenario „foreign
      folder": dieselbe Datei mit `item.SeriesID = "foreign-series"` und Pfad außerhalb beider
      verbundener Ordner wird weiterhin mit 409 und der Meldung „gehört nicht zur gespeicherten
      Anime-Zuordnung" abgelehnt (`spy.calls == 0`). Szenario „main folder": die unveränderte
      Datei aus dem Haupt-Ordner bleibt weiterhin erfolgreich (Regression).
    - Test 2 (episode_version_source_hydration_test.go, NEU): dieselbe zweite Verbindung auf Anime
      301 (openVersionHydrationFixture), PATCH-Relink (`hydrationPatch`) auf Version 3301.
      Szenario „second folder": `item-b` mit `SeriesId: "series-402"`,
      `Path: "/data/anime/SecondFolder/b.webm"` wird erfolgreich verknüpft (200,
      `stored.Data.MediaSourceID == "source-b"`). Szenario „foreign folder": `SeriesId: "foreign"`
      bleibt bei 409. Die bereits bestehende `TestEpisodeVersionSourceHydrationEndpoints` bleibt
      dabei UNVERÄNDERT und beweist automatisch, dass der Haupt-Ordner-Fall (nur eine Verbindung)
      weiterhin exakt gleich funktioniert.
    - Test 3 (admin_content_episode_version_editor_context_test.go, NEU): dieselbe zweite
      Verbindung auf Anime 301 (openVersionHydrationFixture), `GetEpisodeVersionEditorContext` für
      Version 3301 (`evecChapterRequest`). Szenario „second folder": `item-a` liegt jetzt laut Mock
      in `SeriesId: "series-402"`/`Path: "/data/anime/SecondFolder/a.mkv"` -- `SelectedFile` ist
      NICHT nil und `JellyfinEnrichmentDegraded == false`. Szenario „foreign folder": `SeriesId:
      "foreign"` -- `SelectedFile` ist nil und `JellyfinEnrichmentDegraded == true` (fail-open
      degradiert, kein 5xx). Die bereits bestehenden Tests in dieser Datei (u.a.
      `TestEpisodeVersionEditorContextChapterFailuresRemainUsable`,
      `TestEpisodeVersionEditorContextSelectedFileAndContributor`) bleiben UNVERÄNDERT und
      beweisen den weiterhin korrekten Ein-Ordner-Fall.
  </behavior>
  <action>
In `admin_episode_import_test.go`: füge `TestApplyEpisodeImport_AcceptsFileFromAdditionalConnectedJellyfinFolder`
hinzu, table-driven über `[]string{"main folder", "second folder", "foreign folder"}`. Pro
Subtest: `pool := openEVECFixture(t)`, danach das `INSERT INTO anime_source_links`-Statement aus
`<behavior>`. `item := episodeImportServerItem("one")` als Basis (liefert bereits
`SeriesID: "series-401"`, Pfad im Haupt-Ordner, `MediaSources[1].ID == "source-one"` passend zu
`episodeImportReviewedRequest()`s `MediaSourceID: "source-one"`); für „second folder"/„foreign
folder" `item.SeriesID` und `item.Path`/`item.MediaSources[1].Path` wie in `<behavior>`
überschreiben. Der HTTP-Mock muss ZWEI Arten von Anfragen bedienen -- den Ordner-Hydrations-Batch
aus `loadEpisodeImportContext` (`Ids` enthält eine oder beide Folder-IDs, NIE „one") und die
Item-Anfrage für die eigentliche Mapping-Datei (`Ids == "one"`): parse `Ids` per
`strings.Split(r.URL.Query().Get("Ids"), ",")`; ist das erste Element ungleich `"one"`, antworte
mit den passenden Einträgen aus einer lokalen `map[string]jellyfinEpisodeItem{"series-401":
{ID:"series-401",Path:"/data/anime/FixtureFallback"}, "series-402":
{ID:"series-402",Path:"/data/anime/SecondFolder"}}` (nur die tatsächlich angefragten IDs
zurückgeben); sonst mit `jellyfinEpisodeListResponse{Items: []jellyfinEpisodeItem{item}}` (exakt
das bestehende Muster aus `TestEpisodeImportSourceApplyRevalidatesBeforeWrite`). Handler-Aufbau,
Request-Konstruktion und `ApplyEpisodeImport`-Aufruf exakt wie in
`TestEpisodeImportSourceApplyRevalidatesBeforeWrite` (Zeile ~605-680: `evecFixtureHandler`,
`episodeImportSourceRepoSpy`, `adminRoleCheckerStub{isAdmin: true}`, `episodeImportReviewedRequest()`,
`gin.CreateTestContext`, `h.ApplyEpisodeImport(c)`). Erwarteter Status: 200 für „main folder"/
„second folder", 409 für „foreign folder"; bei 200 `spy.calls == 1` und
`spy.received.MediaCandidates[0].Path == item.Path` prüfen, bei 409 `spy.calls == 0` und dass der
Response-Body die Meldung „gehört nicht zur gespeicherten Anime-Zuordnung" enthält.

In `episode_version_source_hydration_test.go`: füge `TestEpisodeVersionSourceHydrationSecondConnectedFolder`
hinzu, table-driven über `[]string{"second folder", "foreign folder"}`. Pro Subtest:
`pool := openVersionHydrationFixture(t)` plus dasselbe `INSERT INTO anime_source_links`-Statement.
HTTP-Mock analog zu Test 1 -- unterscheide über `strings.Split(Ids, ",")`: ein Batch mit einer
oder zwei der IDs `{"series-401","series-402"}` beantwortet den Ordner-Hydrations-Call von
`hydrateFansubFolderPathsForRelink` (liefert `{"Id":"series-402","Path":"/data/anime/SecondFolder"}`
für die angefragte(n) ID(s)); ein Batch mit genau `["item-b"]` beantwortet die eigentliche
Item-Anfrage mit `SeriesId`/`Path` je nach Szenario (siehe `<behavior>`) und `MediaSources: [{Id:
"source-b", Path: <gleicher Pfad>, Container: "webm", MediaStreams: [{Index:0,Type:"Video",
Codec:"vp9",Width:1280,Height:720}]}]` (identisch zum bestehenden „valid relink"-Fixture-Body in
`TestEpisodeVersionSourceHydrationEndpoints`). Handler via `NewFansubHandler(...)` exakt wie in
`TestEpisodeVersionSourceHydrationEndpoints` (Zeile 111), Request via
`hydrationPatch(h, body, true)` mit
`body = `{"title":"Relinked","media_provider":"jellyfin","media_item_id":"item-b","media_source_id":"source-b","stream_url":"https://attacker.invalid"}``.
Erwarteter Status: 200 für „second folder" (zusätzlich `stored.Data.MediaSourceID ==
hydrationText("source-b")` aus dem Response-Body prüfen, exakt wie im bestehenden „valid
relink"-Assert-Block), 409 für „foreign folder". `TestEpisodeVersionSourceHydrationEndpoints` UND
`TestEpisodeVersionSourceHydrationCreate` NICHT anfassen -- sie bleiben die
Haupt-Ordner-Regressionsbeweise für PATCH bzw. POST.

In `admin_content_episode_version_editor_context_test.go`: füge
`TestEpisodeVersionEditorContextSelectedFileFromAdditionalConnectedFolder` hinzu, table-driven
über `[]string{"second folder", "foreign folder"}`. Pro Subtest: `pool :=
openVersionHydrationFixture(t)` plus dasselbe `INSERT INTO anime_source_links`-Statement.
HTTP-Mock: `ids := strings.Split(r.URL.Query().Get("Ids"), ",")`; ist `len(ids)==1 &&
ids[0]=="item-a"`, antworte mit dem Item (`SeriesId`/`Path` je nach Szenario aus `<behavior>`,
`MediaSources: [{Id:"source-a", Path: <gleicher Pfad>, MediaStreams: []}]`, `TotalRecordCount:1`)
-- sonst (das deckt sowohl `resolveEpisodeVersionFolderPath`s Einzel-Anfrage `Ids=series-401` als
auch `hydrateJellyfinFolderDisplayNames`s Batch ab) antworte pro angefragter ID mit deren
bekanntem Pfad aus `{"series-401":"/data/anime/FixtureFallback","series-402":"/data/anime/SecondFolder"}`.
Handler via `evecFixtureHandler(pool, server.URL, "fixture-key")`, Request via
`evecChapterRequest(h, false, true)` (nicht-Contributor, authentifiziert -- exakt wie in
`TestEpisodeVersionEditorContextSelectedFileAndContributor`). Erwartung: „second folder" ->
`response.Data.SelectedFile` nicht nil UND `response.Data.JellyfinEnrichmentDegraded == false`;
„foreign folder" -> `response.Data.SelectedFile` nil UND `response.Data.JellyfinEnrichmentDegraded
== true` (fail-open, kein 5xx -- `rec.Code` bleibt 200). Bestehende Tests in dieser Datei NICHT
anfassen.
  </action>
  <verify>
    <automated>docker run --rm -v /home/d1sk/team4s/backend:/app -w /app -v gomodcache:/tmp/gomodcache -v gocache:/tmp/gocache -e GOMODCACHE=/tmp/gomodcache -e GOCACHE=/tmp/gocache --network team4s_default golang:1.25-alpine sh -c "go build ./... && go vet ./... && go test ./internal/handlers/... -run 'TestApplyEpisodeImport_AcceptsFileFromAdditionalConnectedJellyfinFolder|TestEpisodeVersionSourceHydrationSecondConnectedFolder|TestEpisodeVersionEditorContextSelectedFileFromAdditionalConnectedFolder|TestEpisodeImportSourceApplyRevalidatesBeforeWrite|TestEpisodeVersionSourceHydrationEndpoints|TestEpisodeVersionSourceHydrationCreate|TestEpisodeVersionEditorContextChapterFailuresRemainUsable|TestEpisodeVersionEditorContextSelectedFileAndContributor' -v</automated>
  </verify>
  <done>New second-connected-folder tests pass at all three call sites (import apply, editor
  relink PATCH, editor selected-file enrichment); the same three tests prove a genuinely foreign
  series/folder is still rejected (409 for apply/relink, degraded-but-200 for the read-only
  enrichment); every pre-existing test in these three files (main-folder regression proof) passes
  unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GAP-12 -- Apply-Fehler sichtbar und fokussiert beim Button</name>
  <files>frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportApplyErrorAlert.tsx, frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportApplyErrorAlert.test.tsx, frontend/src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.ts, frontend/src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.test.tsx, frontend/src/app/admin/anime/[id]/episodes/import/page.tsx, frontend/src/app/admin/anime/[id]/episodes/import/page.module.css</files>
  <behavior>
    - Test 1 (EpisodeImportApplyErrorAlert.test.tsx, NEU): `message={null}` rendert nichts
      (`container.firstChild === null`).
    - Test 2 (NEU): `message="Boom"` rendert ein Element mit `role="alert"`, das den Text „Boom"
      enthält (über `ErrorState`s `description`); `scrollIntoView` und `focus` werden beim ersten
      Rendern mit gesetzter Message aufgerufen (Shim aus `<interfaces>` -- `Element.prototype.
      scrollIntoView` fehlt in jsdom, per `vi.fn()` ersetzen).
    - Test 3 (NEU): wechselt die Message von `null` zu einem neuen String innerhalb desselben
      gemounteten Elements (`rerender`) -> `scrollIntoView`/`focus` werden erneut aufgerufen (jede
      NEUE Apply-Fehlermeldung soll erneut ins Sichtfeld scrollen, nicht nur beim ersten Mount).
    - Test 4 (useEpisodeImportBuilder.test.tsx, NEU): `applyMappings()` bei einem
      `mocks.apply.mockRejectedValueOnce(new Error('Boom'))` setzt
      `result.current.applyErrorMessage === 'Boom'` UND lässt `result.current.errorMessage`
      unverändert (`null`) -- Apply-Fehler landen NICHT mehr im allgemeinen, oben gerenderten
      Error-State.
    - Test 5 (NEU): ein erfolgreicher `loadPreview()`-Aufruf NACH einem vorherigen
      Apply-Fehlschlag setzt `applyErrorMessage` wieder auf `null` zurück (kein veralteter Alert
      nach einer neuen Vorschau).
  </behavior>
  <action>
Erstelle `EpisodeImportApplyErrorAlert.tsx` (neue Datei, "use client"): Props
`{ message: string | null }`. `useRef<HTMLDivElement>(null)`; `useEffect`, das bei jedem Wechsel
von `message` zu einem NICHT-null-Wert `ref.current?.scrollIntoView({ behavior: 'smooth', block:
'center' })` und `ref.current?.focus()` aufruft (Dependency-Array `[message]`). Gibt bei
`message == null` `null` zurück. Sonst: `<div ref={ref} role="alert" tabIndex={-1}
className={styles.applyErrorAlert}><ErrorState title="Mapping konnte nicht angewendet werden"
description={message} /></div>` -- `ErrorState` aus `@/components/ui` (siehe `<interfaces>`),
`styles` aus `./page.module.css` (kein neues CSS-Modul für eine einzelne Klasse). Kurzer
Docstring-Kommentar über der Funktion, der auf GAP-12 (167-UAT.md) verweist und die Root Cause
(Fehler nur oben, weit vom Button, ohne role="alert") in einem Satz nennt.

Erstelle `EpisodeImportApplyErrorAlert.test.tsx` mit den drei Fällen aus `<behavior>` (Test 1-3).
Test-Shim für `scrollIntoView` am Dateianfang ergänzen (siehe `<interfaces>`, gleiches Muster wie
`RolesClient.test.tsx`); `focus` auf `HTMLElement.prototype` ebenfalls per `vi.fn()`
spionieren/prüfen (`vi.spyOn(HTMLElement.prototype, 'focus')` reicht, kein globaler Ersatz nötig).
Rendering/Assertions über `@testing-library/react` `render`/`screen`/`rerender`
(`screen.getByRole('alert')`, `within(...).getByText('Boom')`).

In `useEpisodeImportBuilder.ts`: füge einen neuen State `applyErrorMessage` (analog zu
`errorMessage`, `useState<string | null>(null)`) hinzu, exportiere ihn im
`UseEpisodeImportBuilderState`-Interface und im Rückgabe-Objekt. In `applyMappings()` (aktuell
siehe `<interfaces>`): `setApplyErrorMessage(null)` statt `setErrorMessage(null)` beim Start; im
`catch`-Block `setApplyErrorMessage(formatEpisodeImportError(error, 'Mapping konnte nicht
angewendet werden.'))` statt `setErrorMessage(...)`. In `loadContext`/`loadPreview` (Beginn der
jeweiligen Funktion, dort wo bereits `setErrorMessage(null)` steht) zusätzlich
`setApplyErrorMessage(null)` setzen, damit ein alter Apply-Fehler nicht nach einer neuen
Kontext-/Vorschau-Ladung stehen bleibt. `applyRow` bleibt UNVERÄNDERT (schreibt weiterhin in
`errorMessage` -- GAP-12 betrifft laut Live-UAT explizit den „Mapping anwenden"-Bulk-Button, nicht
den Einzelzeilen-Übernehmen-Button).

In `useEpisodeImportBuilder.test.tsx`: ergänze die zwei neuen Fälle aus `<behavior>` (Test 4-5)
nach dem bestehenden Testmuster in dieser Datei (`renderHook`, `mocks.apply`, `waitFor`).

In `page.tsx`: importiere `EpisodeImportApplyErrorAlert` aus `./EpisodeImportApplyErrorAlert`. Füge
direkt NACH dem schließenden `</div>` des `workbenchActions`-Blocks (innerhalb desselben
`panelHeader`-Containers, siehe `<interfaces>`) `<EpisodeImportApplyErrorAlert
message={builder.applyErrorMessage} />` ein. Ergänze am bestehenden Top-Level-Fehler-Rendering
(Zeile ~119-121, siehe `<interfaces>`) `role="alert"` auf dem vorhandenen `<div
className={styles.error}>` (kleine, risikofreie Konsistenz-Verbesserung -- dieser Block zeigt nach
diesem Task nur noch Kontext-/Vorschau-Ladefehler, keine Apply-Fehler mehr, also keine doppelte
Anzeige).

In `page.module.css`: füge nach dem `.workbenchActions`-Block (siehe `<interfaces>`, Zeile ~222-229)
eine neue Regel `.applyErrorAlert { margin-top: 12px; width: 100%; }` hinzu (Abstand zum
Button-Block, volle Breite für die `ErrorState`-Karte).
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run 'src/app/admin/anime/[id]/episodes/import/EpisodeImportApplyErrorAlert.test.tsx' 'src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.test.tsx' && npm run typecheck" 2>&1 | tail -150</automated>
  </verify>
  <done>EpisodeImportApplyErrorAlert renders nothing for a null message, and a role="alert" element
  with the message text plus a scrollIntoView+focus call for every non-null message (including
  message changes, not just first mount) otherwise; applyMappings failures set applyErrorMessage
  (not errorMessage) and loading a new context/preview clears any stale apply error; page.tsx
  renders the new alert directly under the "Mapping anwenden" button; typecheck and the new/
  touched vitest files are clean.</done>
</task>

<task type="auto">
  <name>Task 4: 167-UAT.md GAP-11/GAP-12-Einträge, Vollverifikation, Container-Rebuild/Restart, Commit</name>
  <files>.planning/phases/167-fansub-gruppenerkennung-beim-import/167-UAT.md</files>
  <action>
1. Vollständige Backend-Verifikation im Scratch-Container (siehe `critical_gotcha`): `go build
   ./... && go vet ./... && go test ./...` -- alle Pakete grün, nicht nur die in Task 1/2
   betroffenen. Laufzeit für die SUMMARY notieren.

2. Vollständige Frontend-Verifikation im laufenden Dev-Container: `docker compose exec -T
   team4sv30-frontend sh -c "cd /app && npm run test"` (voller Testlauf), danach `npm run
   typecheck`, danach `npm run lint` -- jeweils eigener `docker compose exec`-Aufruf, Laufzeit
   notieren.

3. Vor dem Bearbeiten mit `file 167-UAT.md` und `cat -A
   .planning/phases/167-fansub-gruppenerkennung-beim-import/167-UAT.md | tail -5` bestätigen, dass
   die Datei reine LF-Zeilenenden verwendet (kein `^M`). Am Ende der bestehenden `## Gaps`-Liste
   (nach dem GAP-10-Eintrag) ZWEI neue Einträge im exakt gleichen Format wie die bestehenden
   `status: resolved`-Einträge anhängen (truth/status/reason/severity/root_cause/artifacts/
   missing/resolution, reale Umlaute, `reason:` referenziert "Live-UAT Auftraggeber, Anime #7,
   2026-09-24" und die zwei verbundenen Jellyfin-Ordner inkl. der zusätzlichen Ordner-ID
   `c70e8b3d970a6c3952fa6289dec07744` und dem Pfad
   `/media/Anime/OVA/Anime.OVA.Sub/dot_hack-G.U._Returner [GAX]`):
   - GAP-11 (severity: blocker): truth beschreibt, dass „Mapping anwenden", der Editor-Relink und
     die Editor-Datei-Voransicht Dateien aus JEDEM verbundenen Jellyfin-Ordner übernehmen, fremde
     Serien/Ordner aber weiterhin fail-closed abgelehnt werden; root_cause verweist auf
     `resolveReviewedJellyfinSource` (episode_version_source_hydration.go:16-33), das bislang nur
     EIN (seriesID, folder)-Paar prüfte, und die drei betroffenen Aufrufstellen
     (admin_episode_import.go:758, episode_version_source_hydration.go:78,
     admin_content_episode_version_editor_helpers.go:146); resolution nennt das neue
     `jellyfin_owned_sources.go` (`jellyfinOwnedSource`, `matchOwnedJellyfinSource`,
     `ownedJellyfinSourcesFromFolders`, `hydrateFansubFolderPathsForRelink`) und dass alle drei
     Stellen jetzt dieselbe Mehrfach-Ordner-Besitzprüfung nutzen, ohne zusätzliche
     Pro-Datei-Anfrage.
   - GAP-12 (severity: minor): truth beschreibt, dass ein fehlgeschlagenes „Mapping anwenden"
     zusätzlich unmittelbar beim Button als `role="alert"` erscheint und die Seite dorthin
     scrollt/fokussiert; root_cause verweist auf den bisherigen, ausschließlich oben gerenderten
     `errorMessage`-State ohne `role="alert"`; resolution nennt den neuen `applyErrorMessage`-State
     (useEpisodeImportBuilder.ts) und die neue `EpisodeImportApplyErrorAlert.tsx`.
   Nach dem Edit erneut `cat -A .../167-UAT.md | tail -60` prüfen -- keine `^M`-Zeichen. Write/Edit-
   Tool verwenden, kein Bash-Heredoc. Bestehende GAP-01..GAP-10-Einträge und die
   `## Tests`/`## Summary`-Abschnitte darüber NICHT verändern.

4. Backend-Rebuild (Fixes im laufenden Container wirksam machen, da Go-Code geändert wurde):
   `docker compose up -d --build team4sv30-backend`. Danach Health-Check: `docker compose ps
   team4sv30-backend` zeigt "Up", `curl -sf http://192.168.235.196:8092/health -o /dev/null -w
   '%{http_code}\n'` liefert `200`.

5. Frontend-Container neu starten: `docker restart team4sv30-frontend`. Danach `docker compose ps
   team4sv30-frontend` zeigt "Up". Vor und nach Schritt 4/5 jeweils `docker inspect -f
   '{{.State.StartedAt}}' team4sv30-backend team4sv30-frontend` notieren und in der SUMMARY
   auflisten (Startzeitpunkte + Laufzeit der Rebuild/Restart-Befehle selbst).

6. Gezielt committen (kein `git add -A`/`.`): alle in `files_modified` (Frontmatter dieses Plans)
   gelisteten Pfade explizit per Pfad zu `git add` hinzufügen, dann `git commit` mit einer
   Commit-Message, die auf GAP-11 und GAP-12 (Phase 167) verweist. Kein `git push`, kein `git
   stash`.
  </action>
  <verify>
    <automated>grep -c "GAP-11\|GAP-12" /home/d1sk/team4s/.planning/phases/167-fansub-gruppenerkennung-beim-import/167-UAT.md; file /home/d1sk/team4s/.planning/phases/167-fansub-gruppenerkennung-beim-import/167-UAT.md; docker compose ps team4sv30-backend team4sv30-frontend; curl -sf http://192.168.235.196:8092/health -o /dev/null -w '%{http_code}\n'</automated>
  </verify>
  <done>Backend go build/go vet/go test ./... and frontend npm run test/typecheck/lint all pass (or
  name pre-existing, unrelated failures only, with their commands' durations recorded); 167-UAT.md
  has two new GAP-11/GAP-12 entries, both status: resolved, file remains LF-only; backend container
  rebuilt via docker compose up -d --build with confirmed newer start time and a 200 from /health;
  frontend container restarted with confirmed newer start time; changes committed with explicit
  file paths (no git add -A), no git push, no git stash; no team4s_v2 data changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin-Browser -> Episode-Import-Apply/Editor-Relink-Endpoints (bereits admin-only geschützt) | Dieser Plan GENERALISIERT eine bestehende Besitz-/IDOR-Prüfung von „genau ein Haupt-Ordner" auf „jeder mit dem Anime verbundene Ordner" -- die Prüfung selbst (fail-closed gegen alles Fremde) bleibt inhaltlich unverändert, nur die Menge der akzeptierten Paare wächst auf die tatsächlich verbundenen Ordner. |
| Backend -> Jellyfin (Ordner-Pfad-Hydration) | Neue, aber gebündelte (nicht pro Datei) Jellyfin-Anfragen für zusätzliche Ordner; bereits etablierten Fail-open-Mustern (`hydrateJellyfinFolderDisplayNames`, GAP-01) folgend -- ein Jellyfin-Fehler degradiert die Anzeige/Zusatz-Pfad-Kenntnis, blockiert aber nie hart, außer die eigentliche Besitzprüfung selbst schlägt fehl. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-QUICK260924-I0A-01 | Spoofing / Information Disclosure (IDOR) | `matchOwnedJellyfinSource` (backend/internal/handlers/jellyfin_owned_sources.go) | mitigate | Ein Item muss zu MINDESTENS einem Paar aus der Menge der tatsächlich mit dem Anime verbundenen (SeriesID, FolderPath)-Paare passen, die ausschließlich aus `anime.source`/`anime_source_links` (server-seitig geladen) abgeleitet werden -- niemals aus Client-Eingaben. Eine Datei, die zu keiner verbundenen Serie/keinem verbundenen Ordner gehört, wird weiterhin mit 409 abgelehnt (Task 2 beweist dies explizit für alle drei Aufrufstellen). |
| T-QUICK260924-I0A-02 | Tampering | `EpisodeImportApplyErrorAlert.tsx` (Frontend, reine Anzeige) | accept | Rendert ausschließlich eine bereits vom vertrauenswürdigen Backend zurückgegebene Fehlermeldung; führt kein neues, ungeprüftes Client-Eingabefeld ein. |
| T-QUICK260924-I0A-03 | Denial of Service (unnötige Jellyfin-Last) | `hydrateFansubFolderPathsForRelink`/`ownedJellyfinSourcesFromFolders` | mitigate | Bündelt die Ordner-Pfad-Ermittlung auf EINE Anfrage pro Request (nicht pro Datei) und überspringt sie vollständig, wenn nur ein Ordner verbunden ist (Task 1 Tests 6-8 beweisen dies). |

Keine Package-Manager-Installationen sind Teil dieses Plans; das Package Legitimacy Gate ist nicht
anwendbar.
</threat_model>

<verification>
1. Task 1's neue Unit-Tests beweisen die Mehrfach-Ordner-Paar-Logik (Match, Wildcard-Pfad,
   Haupt-Ordner-Fallback, gebündelte/übersprungene Jellyfin-Anfrage) isoliert und schnell, ohne
   Postgres/HTTP.
2. Task 2's neue Postgres/HTTP-Regressionstests beweisen an allen drei betroffenen Stellen (Import-
   Apply, Editor-Relink, Editor-Datei-Voransicht), dass ein zweiter verbundener Ordner jetzt
   funktioniert UND ein fremder Ordner weiterhin fail-closed abgelehnt wird -- die dabei
   unverändert gelassenen Bestandstests beweisen die Haupt-Ordner-Regression.
3. Task 3's neue Vitest-Fälle beweisen die GAP-12-Sichtbarkeit (role="alert", Scroll/Fokus bei
   jeder neuen Fehlermeldung, kein Apply-Fehler mehr im allgemeinen Top-Error-State).
4. Task 4's Vollverifikation, Migration-freie 167-UAT.md-Aktualisierung und Container-
   Rebuild/Restart schließen den Plan ab.
</verification>

<success_criteria>
- [ ] „Mapping anwenden" übernimmt erfolgreich eine Datei aus einem zusätzlich verbundenen
      Jellyfin-Ordner (nicht nur dem Haupt-Ordner); derselbe Fix gilt für Editor-Relink (Create/
      Patch) und die Editor-Datei-Voransicht.
- [ ] Eine Datei aus einer nicht verbundenen Jellyfin-Serie/einem nicht verbundenen Ordner wird an
      allen drei Stellen weiterhin fail-closed abgelehnt (gleiche Fehlermeldung wie vorher).
- [ ] Der Haupt-Ordner-Fall (nur ein verbundener Ordner) bleibt byte-identisch zum
      Vorher-Zustand, inklusive keiner zusätzlichen Jellyfin-Anfrage pro Datei.
- [ ] Ein fehlgeschlagenes „Mapping anwenden" zeigt die Fehlermeldung unmittelbar beim Button,
      als `role="alert"` ausgezeichnet, mit automatischem Scroll/Fokus dorthin.
- [ ] Kein von diesem Plan berührtes Produktionsfile überschreitet 450 Zeilen;
      `admin_episode_import.go` ist nicht gewachsen (weiterhin 783 Zeilen).
- [ ] 167-UAT.md dokumentiert GAP-11/GAP-12 als status: resolved, Datei bleibt LF-only.
- [ ] Backend (`go build`/`go vet`/`go test ./...`) und Frontend (`npm run test`/`typecheck`/
      `lint`) sind vollständig grün oder nennen nur vorbestehende, unabhängige Fehler.
- [ ] Backend-Container per `docker compose up -d --build` neu gebaut, Frontend-Container per
      `docker restart` neu gestartet, beide mit bestätigt neueren Startzeitpunkten.
- [ ] Keine Datenänderung an `team4s_v2`; kein `git push`; kein `git stash`.
</success_criteria>

<output>
Create `.planning/quick/260924-i0a-gap-11-gap-12-mapping-apply-schlaegt-feh/260924-i0a-SUMMARY.md` when done
</output>
