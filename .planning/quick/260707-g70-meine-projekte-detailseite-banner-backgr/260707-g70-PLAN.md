---
task: 260707-g70
type: quick
autonomous: true
files_modified:
  - docker-compose.yml
  - frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.tsx
  - frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/project.module.css
  - frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.test.tsx
must_haves:
  truths:
    - "Backdrop-Bild auf /me/projects/[animeId]/group/[fansubGroupId] wird sichtbar gerendert statt des blauen Fallback-Gradients"
    - "Cover-Overlay zeigt keinen 'Projekt-Kopfbereich'-Text mehr"
    - "Anime-Titel und Gruppenname erscheinen nur einmal auf der Seite (PageHeader), nicht zusaetzlich im Cover"
    - "'Deine Projektrollen' ist als eigene Card unterhalb des Covers gut lesbar platziert"
    - "Pro Release-Zeile wird keine Rollen-Kurzzeile mehr angezeigt"
    - "Filter 'Nur meine Beitraege' zeigt nur Releases mit has_own_notes oder has_own_media, nicht alle projektweit zugeordneten"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Leerer MEDIA_PUBLIC_BASE_URL-Default, damit Backend relative Media-URLs liefert"
      contains: "MEDIA_PUBLIC_BASE_URL: ${MEDIA_PUBLIC_BASE_URL:-}"
    - path: "frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.tsx"
      provides: "Bereinigtes Cover ohne Duplikate/Eyebrow, eigene Rollen-Card, gefixte mine-Filterlogik, keine Rollenzeile pro Release"
    - path: "frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/project.module.css"
      provides: "Styles fuer die neue eigenstaendige Rollen-Card statt Glass-Panel im Cover-Overlay"
  key_links:
    - from: "frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.tsx"
      to: "filterReleases"
      via: "mode === 'mine' Zweig prueft has_own_notes || has_own_media"
      pattern: "has_own_notes[\\s\\S]{0,40}has_own_media"
---

<objective>
Fuenf gezielte Fixes auf der "Meine Projekte"-Detailseite (`/me/projects/[animeId]/group/[fansubGroupId]`):
Banner-Background-Anzeige (Media-URL-Konfiguration), Entfernen des Debug-Textes "Projekt-Kopfbereich",
Beseitigung doppelter Titel/Gruppenname samt Neuplatzierung des Rollen-Panels, Entfernen der
Rollen-Kurzzeile pro Release, und Korrektur des "Nur meine Beitraege"-Filters auf konkrete
Notiz-/Medien-Arbeit statt projektweiter Zuordnung.

Purpose: Admins/Fansub-Mitglieder sehen auf ihrer Projektseite ein korrekt geladenes Hintergrundbild,
keine doppelten/Debug-Texte, eine klar platzierte Rollenuebersicht, und einen Filter der tatsaechlich
nur Folgen mit eigener Arbeit zeigt.

Output: Aktualisierte page.tsx/project.module.css/page.test.tsx sowie eine korrigierte
docker-compose.yml-Zeile (Media-Base-URL).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- MeProjectReleaseVersion / MeProjectDetail aus frontend/src/types/contributions.ts -->
```typescript
export interface MeProjectReleaseVersion {
  release_version_id: number
  episode_number: string
  episode_title: string | null
  episode_sort_index: number | null
  version: string
  title: string | null
  role_codes: string[]
  role_labels: string[]
  has_own_contribution: boolean
  has_own_notes: boolean
  has_own_media: boolean
}

export interface MeProjectDetail {
  anime_id: number
  anime_title: string
  fansub_group_id: number
  fansub_group_name: string
  backdrop_url: string | null
  role_codes: string[]
  role_labels: string[]
  release_versions: MeProjectReleaseVersion[]
}
```

<!-- Card-Primitive aus frontend/src/components/ui/Card.tsx -->
```typescript
type CardVariant = 'default' | 'elevated' | 'interactive' | 'compact' | 'section' | 'flat' | 'nested' | 'nestedFlat'
export interface CardProps extends HTMLAttributes<HTMLDivElement> {
  variant?: CardVariant
  title?: string
  description?: string
  header?: ReactNode
  footer?: ReactNode
}
```
Card rendert optional `header`/`title`+`description` in einem `.cardHeader`-Block, danach `children`.
Bereits importiert in page.tsx: `Card` (fuer `.releaseSection`, `variant="section"`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Media-Base-URL relativ machen (Banner-Fix)</name>
  <files>docker-compose.yml</files>
  <action>
    In docker-compose.yml Zeile ~137 den Default-Wert von MEDIA_PUBLIC_BASE_URL von
    `http://127.0.0.1:8092` auf leer aendern: `MEDIA_PUBLIC_BASE_URL: ${MEDIA_PUBLIC_BASE_URL:-}`.
    Kein Go-Code aendern. Grund: Das Backend liefert dann relative Media-URLs (/media/...,
    /api/v1/media/files/...), die der bestehende Frontend-Proxy same-origin ausliefert;
    Port 8092 ist vom Browser nicht erreichbar (Backend-Hostport ist 18092 und cross-origin).
    Diese Aenderung wirkt app-weit (alle absoluten :8092-Media-URLs waren im Browser kaputt),
    ist aber im Scope dieser Datei minimal und risikofrei.
  </action>
  <verify>
    <automated>MISSING — kein automatisierter Test fuer docker-compose.yml Env-Defaults vorhanden; Orchestrator verifiziert nach Rebuild live per curl auf http://127.0.0.1:3000/media/...</automated>
  </verify>
  <done>Zeile lautet exakt `MEDIA_PUBLIC_BASE_URL: ${MEDIA_PUBLIC_BASE_URL:-}` (leerer Default statt http://127.0.0.1:8092).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Filter-Logik "Nur meine Beitraege" auf konkrete Arbeit umstellen</name>
  <files>frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.tsx, frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.test.tsx</files>
  <behavior>
    - Test: Bei mode 'mine' erscheinen nur Releases mit has_own_notes===true ODER has_own_media===true,
      auch wenn has_own_contribution bei allen Releases true ist (projektweite Zuordnung).
    - Bestehende Tests in page.test.tsx duerfen nicht brechen: Test 1 nutzt bereits
      makeRelease({ has_own_notes: true }) fuer die sichtbare Release (release_version_id 41) und
      eine zweite Release mit has_own_contribution:false (die ohnehin nie sichtbar war) — bleibt gruen.
      Ergaenze einen neuen Testfall: zwei Releases mit has_own_contribution:true, aber nur eine mit
      has_own_notes:true (die andere hat has_own_notes:false und has_own_media:false) — im mine-Modus
      (Default) darf nur die erste erscheinen, die zweite nicht.
  </behavior>
  <action>
    In page.tsx die Funktion `filterReleases` (Zeile ~53-66) im 'mine'-Zweig von
    `releases.filter((release) => release.has_own_contribution)` auf
    `releases.filter((release) => release.has_own_notes || release.has_own_media)` umstellen.
    Kommentar ergaenzen der erklaert warum (has_own_contribution ist projektweit fast immer true,
    "Nur meine Beitraege" soll konkrete Notiz-/Medienarbeit meinen).
    Button-Sichtbarkeit ("Notizen & Medien"-Button, Zeile ~285, gerendert bei
    `release.has_own_contribution`) NICHT anfassen — bleibt unveraendert fuer den 'all'-Filter,
    dort sind weiterhin alle Releases sichtbar und der Button folgt wie bisher has_own_contribution.
    Neuen Testfall in page.test.tsx ergaenzen (siehe behavior), der den mine-Filter mit
    has_own_notes/has_own_media statt has_own_contribution beweist.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run "src/app/me/projects/[animeId]/group/[fansubGroupId]/page.test.tsx"</automated>
  </verify>
  <done>filterReleases filtert im mine-Modus auf has_own_notes||has_own_media; neuer und alle bestehenden Tests in page.test.tsx sind gruen.</done>
</task>

<task type="auto">
  <name>Task 3: Cover bereinigen, Rollen-Card auslagern, Rollenzeile pro Release entfernen</name>
  <files>frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.tsx, frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/project.module.css</files>
  <action>
    In page.tsx:
    1. Im Cover-Overlay (Zeile ~206-223) den `<p className={styles.coverEyebrow}>Projekt-Kopfbereich</p>`
       sowie `<h2>{project.anime_title}</h2>` und `<p>{project.fansub_group_name}</p>` entfernen
       (Titel/Gruppenname stehen bereits im PageHeader oben, keine Duplikate mehr im Cover).
    2. Das Cover-Overlay-`<div>`-Rollen-Panel (`styles.rolePanel` mit "Deine Projektrollen" und
       `roleDetailList`) komplett aus dem `<section className={styles.cover}>` herausloesen und
       als eigene `Card`-Komponente NACH der `cover`-Section, VOR der `Card variant="section"`
       Release-Liste einfuegen. Nutze die bereits importierte `Card`-Primitive
       (`title="Deine Projektrollen"` als Card-Header) statt des bisherigen Glass-Overlay-Panels.
       Struktur beibehalten: `<div role="group" aria-label="Deine Projektrollen in diesem Projekt">`
       mit den `roleDetailRow`-Eintraegen (label + "Fuer das gesamte Projekt") bleibt inhaltlich
       identisch, nur der aeussere Container wechselt von `rolePanel`-div zu `Card`.
    3. Die `cover`-Section behaelt nur noch das Hintergrundbild (backgroundImage-Style) und ggf.
       ein minimales `aria-label` — kein sichtbarer Overlay-Textinhalt mehr noetig, da Titel/Gruppe/
       Rollen jetzt anderswo stehen. `coverOverlay`-div kann entfallen oder leer/strukturell auf
       Mindesthoehe reduziert werden (Entscheidung: Cover-Section beibehalten fuer visuelle Banner-
       Wirkung mit min-height, aber ohne Overlay-Textinhalte).
    4. In der Release-Liste (Zeile ~272-298) die Zeile `<small>{roleText(release)}</small>` entfernen.
       Die Hilfsfunktion `roleText` (Zeile ~46-51) danach entfernen, da ungenutzt (verhindert
       no-unused-vars Lint-Fehler).
    In project.module.css:
    - `.coverEyebrow`-Regel entfernen (ungenutzt nach Punkt 1).
    - `.rolePanel`, `.roleDetailList`, `.roleDetailRow` (und deren `strong`/`small`-Kindselektoren)
      von "Glass-Panel auf dunklem Cover-Overlay" auf normale Card-Inhalts-Optik umstellen
      (helle/neutrale Farben passend zum globalen Card-Stil statt `rgba(255,255,255,...)`-Weiss-auf-
      Dunkel-Werten, z. B. `var(--text-strong)`/`var(--text-muted)`/`var(--border-subtle)`/
      `var(--surface-muted)` analog zu den bereits im File verwendeten Design-Tokens).
    - `.cover`/`.coverOverlay` Regeln entsprechend vereinfachen (kein `justify-content: space-between`
      mehr fuer zwei Panels noetig, wenn nur noch Hintergrundbild bleibt); mobile Media-Query
      (Zeile ~236-273) auf die neue Struktur anpassen, keine toten Selektoren stehen lassen.
    Alle deutschen UI-Strings mit korrekten Umlauten (Fuer → "Für" bleibt bereits korrekt in
    "Für das gesamte Projekt" — keine ASCII-Ersatzform einfuehren).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run "src/app/me/projects/[animeId]/group/[fansubGroupId]/page.test.tsx" && npx tsc --noEmit -p .</automated>
  </verify>
  <done>
    Kein "Projekt-Kopfbereich"-Text mehr im Code; Anime-Titel/Gruppenname stehen nur noch im
    PageHeader, nicht mehr im Cover-Overlay; "Deine Projektrollen" ist eine eigenstaendige Card
    unterhalb des Covers; Release-Zeilen zeigen keine Rollen-Kurzzeile mehr; roleText ist entfernt;
    bestehende und neue Tests aus Task 2 bleiben gruen; tsc ohne Fehler; Datei bleibt unter 450 Zeilen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> Backend Media-URL | Backdrop-/Medien-URLs werden vom Backend generiert und im Browser als `<img>`/CSS-`background-image` geladen; falsch konfigurierte absolute Basis-URL fuehrt zu einem toten Link, kein Datenverlust-Risiko |
| Client -> Client-seitiger Filter | `filterReleases` filtert nur clientseitig Anzeige, keine Autorisierungsentscheidung; Daten fuer alle Releases sind bereits im geladenen `project`-Objekt vorhanden (kein neuer Datenzugriff) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260707g70-01 | Information Disclosure | docker-compose.yml MEDIA_PUBLIC_BASE_URL | accept | Aenderung liefert nur relative statt absolute URLs; keine neuen Daten offengelegt, im Gegenteil behebt sie einen bestehenden Cross-Origin-Broken-Link-Zustand |
| T-260707g70-02 | Tampering | filterReleases (Client-Filter) | accept | Reine Anzeige-Filterung ohne Sicherheitsfunktion; alle Releases sind bereits im geladenen Response-Payload enthalten, kein zusaetzlicher Datenzugriff wird freigeschaltet oder verborgen |
</threat_model>

<verification>
- `cd frontend && npx vitest run "src/app/me/projects/[animeId]/group/[fansubGroupId]/page.test.tsx"` gruen (inkl. neuem Filter-Testfall)
- `cd frontend && npx tsc --noEmit -p .` ohne Fehler
- docker-compose.yml Zeile ~137: `MEDIA_PUBLIC_BASE_URL: ${MEDIA_PUBLIC_BASE_URL:-}`
- Manuelle Grep-Pruefung: kein Vorkommen von "Projekt-Kopfbereich" mehr in page.tsx
- Manuelle Grep-Pruefung: `roleText` nicht mehr in page.tsx referenziert
</verification>

<success_criteria>
- Alle 5 Fixes sind im Code umgesetzt (Media-URL, Eyebrow-Text weg, keine Titel-Duplikate + Rollen-Card ausgelagert, keine Rollenzeile pro Release, mine-Filter auf has_own_notes||has_own_media).
- Bestehende Tests in page.test.tsx bleiben gruen, neuer Filtertest ist gruen.
- tsc kompiliert ohne Fehler.
- Deutsche UI-Strings verwenden korrekte Umlaute.
- page.tsx und project.module.css bleiben unter 450 Zeilen.
- Nach Ausfuehrung durch den Orchestrator: docker compose Backend-Rebuild + Frontend-Restart, danach Live-Verifikation dass das Backdrop-Bild auf http://127.0.0.1:3000/me/projects/1/group/... sichtbar ist.
</success_criteria>

<output>
Create `.planning/quick/260707-g70-meine-projekte-detailseite-banner-backgr/260707-g70-SUMMARY.md` when done
</output>
