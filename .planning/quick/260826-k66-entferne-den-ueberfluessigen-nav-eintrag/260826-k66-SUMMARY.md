---
phase: 260826-k66-entferne-den-ueberfluessigen-nav-eintrag
plan: 01
subsystem: ui
tags: [nextjs, react, admin-nav, vitest]

# Dependency graph
requires:
  - phase: 138
    provides: "/admin/role-capabilities redirects server-side to /admin/roles, where the Standardrechte-Matrix now lives as the 'Standardrechte' tab"
provides:
  - "AdminMainNav.tsx with a 5-entry ADMIN_NAV_LINKS array (Capabilities entry removed)"
  - "Test coverage asserting Capabilities is absent from the rendered nav"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - frontend/src/components/admin/AdminMainNav.tsx
    - frontend/src/components/admin/AdminMainNav.test.tsx

key-decisions:
  - "Removed only the Capabilities entry from ADMIN_NAV_LINKS; left belongsToRightsModule and the render loop untouched since both already derive purely from the array"
  - "Left frontend/src/app/admin/role-capabilities/page.tsx unmodified so legacy ?role=<code>&tab=<x> bookmarks keep redirecting to /admin/roles"

patterns-established: []

requirements-completed: []

# Metrics
duration: 5min
completed: 2026-08-26
---

# Quick Task 260826-k66: Entferne den überflüssigen Nav-Eintrag Summary

**Removed the duplicate "Capabilities" admin nav link from `ADMIN_NAV_LINKS` since it pointed to the same destination as "Rollen" (both resolve to `/admin/roles`).**

## Performance

- **Duration:** 5 min
- **Started:** 2026-08-26T14:28:00Z
- **Completed:** 2026-08-26T14:33:22Z
- **Tasks:** 1 completed
- **Files modified:** 2

## Accomplishments
- `ADMIN_NAV_LINKS` in `AdminMainNav.tsx` now has exactly 5 entries (Benutzer, Gruppen, Rollen, Claims, Änderungen) — the redundant Capabilities link is gone.
- `AdminMainNav.test.tsx` updated: the "renders the complete rights module navigation" test now iterates only the 5 remaining links and adds an explicit assertion that `screen.queryByRole('link', { name: 'Capabilities' })` is `null`.
- The two other existing test blocks (nested-route visibility, hidden-outside-rights-module) were left byte-identical and still pass.
- `frontend/src/app/admin/role-capabilities/page.tsx` (the legacy redirect target) was not touched — verified via `git diff --stat` showing only the two intended files changed.

## Task Commits

Each task was committed atomically:

1. **Task 1: Remove Capabilities nav entry and update test coverage** - `615365b2` (fix)

**Plan metadata:** (docs commit handled separately by orchestrator)

## Files Created/Modified
- `frontend/src/components/admin/AdminMainNav.tsx` - Removed the `{ label: 'Capabilities', href: '/admin/role-capabilities' }` entry from `ADMIN_NAV_LINKS`
- `frontend/src/components/admin/AdminMainNav.test.tsx` - Updated the link-presence test to iterate only 5 remaining links and added an explicit absence assertion for Capabilities

## Decisions Made
None beyond what the plan specified - followed the plan exactly.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

The admin rights-module nav now shows 5 links with no duplicate destination. `docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/admin/AdminMainNav.test.tsx"` passes 6/6. No follow-up work identified; optional manual UAT step (navigate to `http://127.0.0.1:3300/admin/roles` and confirm `/admin/role-capabilities?role=xyz` still redirects to `/admin/roles?role=xyz`) remains available but non-blocking.

---
*Phase: 260826-k66-entferne-den-ueberfluessigen-nav-eintrag*
*Completed: 2026-08-26*

## Self-Check: PASSED

- FOUND: frontend/src/components/admin/AdminMainNav.tsx
- FOUND: frontend/src/components/admin/AdminMainNav.test.tsx
- FOUND: frontend/src/app/admin/role-capabilities/page.tsx (confirmed untouched by commit 615365b2)
- FOUND: commit 615365b2 in git log
