---
phase: quick
plan: 260510-t7j
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/cmd/server/main.go
  - backend/internal/handlers/admin_content_release_version_media.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "/media responses enthalten X-Content-Type-Options: nosniff, Content-Security-Policy: default-src 'none', X-Frame-Options: DENY"
    - "JPEG/PNG/WebP Originals werden EXIF-frei gespeichert"
    - "Bilder mit mehr als 40 Mio. Pixeln werden vor dem Decode abgelehnt"
  artifacts:
    - path: "backend/cmd/server/main.go"
      provides: "Security-Header-Middleware fuer /media Route"
    - path: "backend/internal/handlers/admin_content_release_version_media.go"
      provides: "EXIF-Strip und Pixel-Bomb-Guard"
  key_links:
    - from: "router.Static"
      to: "Security-Header-Gruppe"
      via: "router.Group mit Middleware vor StaticFS"
    - from: "processOneRVMFile"
      to: "EXIF-Strip"
      via: "imaging.Save statt os.WriteFile fuer JPEG/PNG/WebP"
    - from: "processOneRVMFile"
      to: "Pixel-Check"
      via: "meta.Width * meta.Height > 40_000_000 nach Dimensions-Check"
---

<objective>
Drei Sicherheits-Fixes im Backend ohne neue Dependencies und ohne API-Aenderungen.

Purpose: Begrenze die Angriffsoberflaeche der /media-Auslieferung und des Bild-Upload-Pfads.
Output: Haerterer /media-Endpunkt (Security-Header), EXIF-freie Originale, Schutz vor Dekompression-Bombs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@backend/cmd/server/main.go
@backend/internal/handlers/admin_content_release_version_media.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Security-Header-Middleware fuer /media in main.go</name>
  <files>backend/cmd/server/main.go</files>
  <action>
Ersetze die Zeile

    router.Static("/media", cfg.MediaStorageDir)

durch eine Middleware-Gruppe mit anschliessender StaticFS-Auslieferung:

    mediaGroup := router.Group("/media")
    mediaGroup.Use(func(c *gin.Context) {
        c.Header("X-Content-Type-Options", "nosniff")
        c.Header("Content-Security-Policy", "default-src 'none'")
        c.Header("X-Frame-Options", "DENY")
        c.Header("Cache-Control", "private, no-transform")
        c.Next()
    })
    mediaGroup.StaticFS("", http.Dir(cfg.MediaStorageDir))

Fuer `http.Dir` muss "net/http" bereits importiert sein — es ist in main.go noch nicht importiert, also ergaenze den Import.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go build ./...</automated>
  </verify>
  <done>go build ./... laeuft fehlerfrei durch; /media-Routen liefern die vier Security-Header.</done>
</task>

<task type="auto">
  <name>Task 2: EXIF-Strip und Pixel-Bomb-Guard im Upload-Handler</name>
  <files>backend/internal/handlers/admin_content_release_version_media.go</files>
  <action>
**Aenderung A — Pixel-Bomb-Guard** (nach dem bestehenden Dimensions-Check, Zeile ~286):

Nach dem Block

    if meta.Width > rvmMaxImageWidth || meta.Height > rvmMaxImageHeight { ... }

fuege ein:

    // Dekompression-Bomb-Schutz: Pixelzahl-Limit 40 MP.
    // meta ist bereits bekannt (aus inspectRVMImage), kein zweites Decode noetig.
    if meta.Width*meta.Height > 40_000_000 {
        return rvmFileResult{ClientFileName: clientName, Status: "failed",
            ErrorCode: "IMAGE_TOO_MANY_PIXELS",
            Message:   "bild enthaelt zu viele pixel (max 40 MP)"}
    }

**Aenderung B — EXIF-Strip** (ersetze den `os.WriteFile(originalPath, data, ...)` Block, Zeile ~316):

Ersetze:

    if err := os.WriteFile(originalPath, data, 0o644); err != nil {
        _ = removeFileQuietly(originalPath)
        return rvmFileResult{ClientFileName: clientName, Status: "failed",
            ErrorCode: "STORAGE_FAILED", Message: "original konnte nicht gespeichert werden"}
    }

durch:

    // EXIF-Strip fuer JPEG, PNG und WebP: imaging.Save re-enkodiert das Bild
    // ohne Metadaten. GIF bleibt raw, weil imaging die Animation nicht erhalten kann.
    if mimeType == "image/gif" {
        if err := os.WriteFile(originalPath, data, 0o644); err != nil {
            _ = removeFileQuietly(originalPath)
            return rvmFileResult{ClientFileName: clientName, Status: "failed",
                ErrorCode: "STORAGE_FAILED", Message: "original (gif) konnte nicht gespeichert werden"}
        }
    } else {
        decoded, _, err := image.Decode(bytes.NewReader(data))
        if err != nil {
            _ = removeFileQuietly(originalPath)
            return rvmFileResult{ClientFileName: clientName, Status: "failed",
                ErrorCode: "IMAGE_DECODE_FAILED", Message: "original dekodieren fehlgeschlagen"}
        }
        if err := imaging.Save(decoded, originalPath); err != nil {
            _ = removeFileQuietly(originalPath)
            return rvmFileResult{ClientFileName: clientName, Status: "failed",
                ErrorCode: "STORAGE_FAILED", Message: "original (exif-strip) konnte nicht gespeichert werden"}
        }
    }

Keine neuen Imports noetig — `image`, `bytes`, `imaging` und `os` sind bereits importiert.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go build ./... && go test ./internal/handlers/... -count=1 -run ReleaseVersionMedia</automated>
  </verify>
  <done>Build und alle ReleaseVersionMedia-Tests gruен; kein Compile-Fehler.</done>
</task>

</tasks>

<verification>
cd C:/Users/admin/Documents/Team4s/backend && go build ./... && go test ./internal/handlers/... -count=1 -run ReleaseVersionMedia
</verification>

<success_criteria>
- go build ./... fehlerfrei
- Alle ReleaseVersionMedia-Tests bestehen
- /media-Gruppe in main.go hat Security-Header-Middleware vor StaticFS
- processOneRVMFile lehnt Bilder > 40 MP mit IMAGE_TOO_MANY_PIXELS ab
- JPEG/PNG/WebP Originals werden via imaging.Save EXIF-frei persistiert; GIF bleibt raw (Kommentar erklaert warum)
</success_criteria>

<output>
Nach Abschluss kein separates SUMMARY.md noetig — Quick-Task.
</output>
