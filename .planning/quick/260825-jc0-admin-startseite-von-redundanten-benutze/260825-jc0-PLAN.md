# Quick Task 260825-jc0: Admin start page navigation cleanup

**Status:** Complete

## Scope

Remove only the redundant global entry points for group, role, claim, and change
workspaces. Keep the central users-and-rights entry and all destination routes.

## Tasks

1. Add a focused admin start page regression test for retained and redundant links.
2. Remove the four redundant links from `frontend/src/app/admin/page.tsx`.
3. Run focused tests, typecheck, lint attempt, and diff validation.

## Acceptance Criteria

- `/admin` exposes Studio, users-and-rights, profile, separate episode mode, and Fansubs.
- `/admin/groups`, `/admin/roles`, `/admin/claims`, and `/admin/changes` remain implemented.
- No API, auth, routing, or Fansub CRUD code changes.