---
phase: quick-s7v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx
  - frontend/src/app/admin/users/tabs/userGroupRightsHelpers.ts
  - frontend/src/app/admin/users/tabs/CapabilityDetailRow.tsx
  - frontend/src/app/admin/users/tabs/CategoryTable.tsx
  - frontend/src/app/admin/users/tabs/GroupRolesSection.tsx
  - frontend/src/app/admin/users/tabs/GroupSection.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "UserGroupRightsTab.tsx is at or below 450 lines and remains the sole entry point / canonical user-in-group editor (UADM-01) and sole mutation-flow orchestrator."
    - "Every new sibling file is at or below 450 lines."
    - "All 9 existing UserGroupRightsTab.test.tsx tests pass with zero assertion changes."
    - "GuidedRevokeFlow, GuidedGrantFlow, CapabilityHistoryPanel, RoleAssignmentImpactModal, and ActivationStatusIndicator remain byte-for-byte unchanged and are still rendered from within the tab exactly as before (same props, same call sites)."
    - "No permission/resolver semantics, decisive-source labels, category ordering, or user-facing text changed — this is a pure structural move, not a rewrite."
    - "No native <select>/<input>/<textarea>/<button> elements are introduced anywhere in the split files; all interactive UI still goes through @/components/ui primitives."
    - "German umlauts remain correct (ä/ö/ü/Ä/Ö/Ü/ß) in every user-facing string carried over into the new files."
  artifacts:
    - path: "frontend/src/app/admin/users/tabs/userGroupRightsHelpers.ts"
      provides: "Pure helpers/derivations: sortCategories, groupStatesByCategory, decisiveSourceLabel, roleLabelFor, assignableFansubGroupRoles (plus private CATEGORY_ORDER/UNKNOWN_CATEGORY constants)"
      contains: "export function assignableFansubGroupRoles"
    - path: "frontend/src/app/admin/users/tabs/CapabilityDetailRow.tsx"
      provides: "Expanded capability detail/provenance row (revoke/grant/remove-override actions + embedded CapabilityHistoryPanel)"
      contains: "export function CapabilityDetailRow"
    - path: "frontend/src/app/admin/users/tabs/CategoryTable.tsx"
      provides: "Per-category capability table with expandable detail rows"
      contains: "export function CategoryTable"
    - path: "frontend/src/app/admin/users/tabs/GroupRolesSection.tsx"
      provides: "Group-membership role display/assign/revoke section (D-22)"
      contains: "export function GroupRolesSection"
    - path: "frontend/src/app/admin/users/tabs/GroupSection.tsx"
      provides: "Per-group card composing GroupRolesSection + category accordion of CategoryTable"
      contains: "export function GroupSection"
    - path: "frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx"
      provides: "Data loading (memberships/rights/matrix), flow-state orchestration (revoke/grant/roleAssignment), top-level render"
      contains: "export function UserGroupRightsTab"
  key_links:
    - from: "frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx"
      to: "frontend/src/app/admin/users/tabs/GroupSection.tsx"
      via: "import { GroupSection } from './GroupSection'"
      pattern: "from './GroupSection'"
    - from: "frontend/src/app/admin/users/tabs/GroupSection.tsx"
      to: "frontend/src/app/admin/users/tabs/CategoryTable.tsx"
      via: "import { CategoryTable } from './CategoryTable'"
      pattern: "from './CategoryTable'"
    - from: "frontend/src/app/admin/users/tabs/GroupSection.tsx"
      to: "frontend/src/app/admin/users/tabs/GroupRolesSection.tsx"
      via: "import { GroupRolesSection } from './GroupRolesSection'"
      pattern: "from './GroupRolesSection'"
    - from: "frontend/src/app/admin/users/tabs/CategoryTable.tsx"
      to: "frontend/src/app/admin/users/tabs/CapabilityDetailRow.tsx"
      via: "import { CapabilityDetailRow } from './CapabilityDetailRow'"
      pattern: "from './CapabilityDetailRow'"
    - from: "frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx"
      to: "frontend/src/app/admin/users/tabs/userGroupRightsHelpers.ts"
      via: "import { groupStatesByCategory } from './userGroupRightsHelpers' (used for defaultOpenIds)"
      pattern: "from './userGroupRightsHelpers'"
---

<objective>
Split `UserGroupRightsTab.tsx` (716 lines, ~59% over CLAUDE.md's 450-line modularity cap) into
cohesive sibling files in the same `frontend/src/app/admin/users/tabs/` directory, with **zero
behavior change**. This is a pure structural move: no permission behavior, resolver semantics,
user-facing text, or external props may change. `UserGroupRightsTab` stays the sole default
export, the canonical user-in-group editor (UADM-01), and the sole mutation entry point.

Purpose: Bring the file back under the project's 450-line modularity limit without touching any
logic, so the 9/9 existing test suite proves the split is behaviorally invisible.
Output: `UserGroupRightsTab.tsx` reduced to orchestration/data-loading only, plus 5 new sibling
files (`userGroupRightsHelpers.ts`, `CapabilityDetailRow.tsx`, `CategoryTable.tsx`,
`GroupRolesSection.tsx`, `GroupSection.tsx`), all ≤450 lines, all tests green, unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx
@frontend/src/app/admin/users/tabs/UserGroupRightsTab.test.tsx
</context>

<interfaces>
<!-- Exact decomposition map. Follow this literally — every named export below already exists in
UserGroupRightsTab.tsx today; this plan only moves each block to a new file and rewires imports.
Carry over existing JSDoc comments (D-11/D-12/D-13/D-22/D-18/D-20/D-21 references) unchanged onto
whichever file the commented code moves to — do not delete or paraphrase them. -->

**Confirmed:** `UserGroupRightsTab.test.tsx` only imports `{ UserGroupRightsTab }` from
`'./UserGroupRightsTab'` — no test imports any internal helper or sub-component by name, so no
test file edits are needed for this split.

1. `userGroupRightsHelpers.ts` (new, pure functions only, no JSX, no 'use client'):
   - Private module-scope constants: `CATEGORY_ORDER` (string array), `UNKNOWN_CATEGORY = 'sonstige'`.
   - `export function sortCategories(categories: string[]): string[]`
   - `export function groupStatesByCategory(states: EffectiveRightState[], actionMeta: Map<string, ActionEntry>): Map<string, EffectiveRightState[]>`
   - `export function decisiveSourceLabel(state: EffectiveRightState): string` (D-13 label mapping)
   - `export function roleLabelFor(roleCode: string, matrix: RoleCapabilityMatrix | null): string`
   - `export function assignableFansubGroupRoles(matrix: RoleCapabilityMatrix | null, alreadyHeld: string[]): RoleEntry[]` (D-22)
   - Imports needed: `type { ActionEntry, EffectiveRightState, RoleCapabilityMatrix, RoleEntry } from '@/types/admin-capability'`

2. `CapabilityDetailRow.tsx` (new, 'use client' not required unless it uses hooks — it doesn't):
   - `export function CapabilityDetailRow({ groupId, appUserId, label, state, onOpenRevoke, onOpenGrant })` — move verbatim, same prop shape.
   - Imports: `{ Button, TableCell, TableRow } from '@/components/ui'`, `{ CapabilityHistoryPanel } from './CapabilityHistoryPanel'`, `type { EffectiveRightState } from '@/types/admin-capability'`.

3. `CategoryTable.tsx` (new):
   - `export function CategoryTable({ groupId, appUserId, states, actionMeta, expandedRows, onToggleRow, onOpenRevoke, onOpenGrant })` — move verbatim.
   - Imports: `{ Fragment } from 'react'`, `{ Badge, Table, TableBody, TableCell, TableHead, TableHeaderCell, TableRow } from '@/components/ui'`, `{ decisiveSourceLabel } from './userGroupRightsHelpers'`, `{ CapabilityDetailRow } from './CapabilityDetailRow'`, `type { ActionEntry, EffectiveRightState } from '@/types/admin-capability'`.

4. `GroupRolesSection.tsx` (new, uses `useState` so needs `'use client'` at top since it's a client component tree — verify by checking whether the file needs the directive; Next.js only requires it at the actual entry boundary but since sibling tab files here are plain modules imported into a `'use client'` tree, check existing siblings like `CapabilityHistoryPanel.tsx` for whether they redeclare `'use client'` and match that convention exactly):
   - `export function GroupRolesSection({ membership, matrix, onOpenRoleAssignment })` — move verbatim (includes its internal `useState` for `selectedRoleCode`).
   - Imports: `{ useState } from 'react'`, `{ Badge, Button, SectionHeader, Select } from '@/components/ui'`, `{ resolveRoleLink } from '../resolveRoleLink'`, `{ assignableFansubGroupRoles, roleLabelFor } from './userGroupRightsHelpers'`, `type { AdminGroupMembershipSummary } from '@/types/admin-users'`, `type { RoleCapabilityMatrix } from '@/types/admin-capability'`.

5. `GroupSection.tsx` (new):
   - `export function GroupSection({ membership, appUserId, states, actionMeta, matrix, openCategoryIds, onOpenCategoryIdsChange, expandedRows, onToggleRow, onOpenRevoke, onOpenGrant, onOpenRoleAssignment })` — move verbatim.
   - Imports: `{ Accordion, Button, Card, EmptyState, SectionHeader } from '@/components/ui'`, `type { AccordionItemDef } from '@/components/ui'`, `{ categoryDisplayLabel } from '../../role-capabilities/capabilityCategories'`, `{ groupStatesByCategory, sortCategories } from './userGroupRightsHelpers'`, `{ CategoryTable } from './CategoryTable'`, `{ GroupRolesSection } from './GroupRolesSection'`, `type { AdminGroupMembershipSummary } from '@/types/admin-users'`, `type { ActionEntry, EffectiveRightState, RoleCapabilityMatrix } from '@/types/admin-capability'`.

6. `UserGroupRightsTab.tsx` (rewritten, shrinks to ~250-290 lines):
   - Keeps: `'use client'` directive, the `ActiveFlow` type, `Props` interface, `GroupRightsState`
     interface (these are private to the top-level component only — nothing else needs them, do
     NOT extract them), and the entire `export function UserGroupRightsTab({ userId })` body
     (loadData, all `useCallback`/`useMemo`/`useState`/`useEffect` hooks, all `handleOpen*`
     handlers, `handleMutated`, and the final JSX including the 3 flow modals) unchanged.
   - Removes: `GroupRolesSection`, `CapabilityDetailRow`, `CategoryTable`, `GroupSection`
     function definitions, and the now-unused helper functions (`sortCategories`,
     `groupStatesByCategory`, `decisiveSourceLabel`, `roleLabelFor`,
     `assignableFansubGroupRoles`) and their `CATEGORY_ORDER`/`UNKNOWN_CATEGORY` constants —
     these now live in the 5 new files.
   - Import changes: replace the current single big `@/components/ui` import with only what the
     remaining top-level render still needs (`EmptyState, ErrorState, LoadingState,
     SectionHeader`); drop `Accordion, Badge, Card, Select, Table, TableBody, TableCell,
     TableHead, TableHeaderCell, TableRow` and the `AccordionItemDef` type import (all moved to
     sub-files). Drop `categoryDisplayLabel` and `resolveRoleLink` imports (moved). Drop
     `CapabilityHistoryPanel` import (moved into `CapabilityDetailRow.tsx`). Drop `RoleEntry` from
     the `@/types/admin-capability` type import (only used by `assignableFansubGroupRoles`, now
     in the helpers file) — keep `ActionEntry, CapabilityOverrideMutationResult,
     EffectiveRightState, RoleCapabilityMatrix`. Add `import { GroupSection } from
     './GroupSection'` and `import { groupStatesByCategory } from './userGroupRightsHelpers'`
     (still needed inside `loadData` for the `defaultOpenIds` computation). Keep
     `GuidedGrantFlow`, `GuidedRevokeFlow`, `RoleAssignmentImpactModal` imports unchanged (still
     rendered at the bottom of the returned JSX exactly as before).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract pure helpers and leaf-level components (helpers, CapabilityDetailRow, CategoryTable)</name>
  <files>
    frontend/src/app/admin/users/tabs/userGroupRightsHelpers.ts,
    frontend/src/app/admin/users/tabs/CapabilityDetailRow.tsx,
    frontend/src/app/admin/users/tabs/CategoryTable.tsx
  </files>
  <action>
    Read the full current content of UserGroupRightsTab.tsx first. Create the three new files
    exactly per the `<interfaces>` decomposition above: `userGroupRightsHelpers.ts` gets
    `sortCategories`, `groupStatesByCategory`, `decisiveSourceLabel`, `roleLabelFor`,
    `assignableFansubGroupRoles`, and the private `CATEGORY_ORDER`/`UNKNOWN_CATEGORY` constants,
    moved verbatim (byte-identical function bodies and comments, only import paths change).
    `CapabilityDetailRow.tsx` gets the `CapabilityDetailRow` component moved verbatim.
    `CategoryTable.tsx` gets the `CategoryTable` component moved verbatim, importing
    `decisiveSourceLabel` from the new helpers file and `CapabilityDetailRow` from the new
    component file. Do NOT modify UserGroupRightsTab.tsx in this task — leave its existing
    definitions of these same blocks in place for now (temporary duplication is expected and
    resolved in Task 2). Do not introduce any new native `<select>/<input>/<textarea>/<button>`
    elements; every UI primitive stays exactly as it already is in the moved code. Preserve every
    German string and comment character-for-character, including umlauts.
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c 'cd /app && npx vitest run src/app/admin/users/tabs/ --reporter=basic'</automated>
  </verify>
  <done>
    The three new files exist, each well under 450 lines, compile cleanly as part of the project
    (no unused-import errors within themselves), and the full existing
    `src/app/admin/users/tabs/` test suite still passes unchanged (UserGroupRightsTab.tsx itself
    is untouched at this point, so this run is a baseline sanity check).
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract GroupRolesSection and GroupSection, rewire UserGroupRightsTab.tsx, verify full suite</name>
  <files>
    frontend/src/app/admin/users/tabs/GroupRolesSection.tsx,
    frontend/src/app/admin/users/tabs/GroupSection.tsx,
    frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx
  </files>
  <action>
    Create `GroupRolesSection.tsx` with the `GroupRolesSection` component moved verbatim
    (including its internal `useState` for `selectedRoleCode`), importing
    `assignableFansubGroupRoles` and `roleLabelFor` from `./userGroupRightsHelpers` and
    `resolveRoleLink` from `../resolveRoleLink`. Create `GroupSection.tsx` with the `GroupSection`
    component moved verbatim, importing `CategoryTable` from `./CategoryTable`,
    `GroupRolesSection` from `./GroupRolesSection`, and `groupStatesByCategory`/`sortCategories`
    from `./userGroupRightsHelpers`. Then rewrite `UserGroupRightsTab.tsx` per the `<interfaces>`
    file-6 description: delete the now-duplicated `GroupRolesSection`, `CapabilityDetailRow`,
    `CategoryTable`, `GroupSection` function bodies and the five now-unused helper functions plus
    their `CATEGORY_ORDER`/`UNKNOWN_CATEGORY` constants (all now live in the 5 sibling files from
    Task 1 and this task); trim its `@/components/ui` import down to only
    `EmptyState, ErrorState, LoadingState, SectionHeader`; drop the now-unused
    `categoryDisplayLabel`, `resolveRoleLink`, `CapabilityHistoryPanel`, and `RoleEntry` imports;
    add `import { GroupSection } from './GroupSection'` and
    `import { groupStatesByCategory } from './userGroupRightsHelpers'`. Keep the `ActiveFlow`
    type, `Props` interface, `GroupRightsState` interface, and the entire
    `UserGroupRightsTab({ userId })` function body (loadData, all hooks, all handlers, and the
    final JSX rendering `GroupSection` per membership plus the 3 flow modals
    `GuidedRevokeFlow`/`GuidedGrantFlow`/`RoleAssignmentImpactModal`) byte-identical to today —
    only their surrounding file and import locations change. Do not alter any prop passed into
    `GuidedRevokeFlow`, `GuidedGrantFlow`, `RoleAssignmentImpactModal`, `CapabilityHistoryPanel`,
    or `ActivationStatusIndicator` (the last is used inside those flow components, untouched by
    this plan). After the rewrite, run `wc -l` on all 6 affected files
    (`UserGroupRightsTab.tsx`, `userGroupRightsHelpers.ts`, `CapabilityDetailRow.tsx`,
    `CategoryTable.tsx`, `GroupRolesSection.tsx`, `GroupSection.tsx`) and record every line count
    for the summary.
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c 'cd /app && npx vitest run src/app/admin/users/tabs/ --reporter=basic'</automated>
  </verify>
  <done>
    All 6 affected files are each ≤450 lines (report exact `wc -l` counts in the summary);
    `UserGroupRightsTab.tsx` has zero leftover duplicate definitions; the full
    `src/app/admin/users/tabs/` vitest suite passes with the same 9/9 UserGroupRightsTab
    assertions unchanged, and no other test file in that directory regressed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → UserGroupRightsTab render tree | Pre-existing boundary, unchanged — this plan moves code between files but crosses no new trust boundary and adds no new input path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-s7v-01 | Tampering | File-split correctness | accept | Pure structural move with byte-identical logic; the existing 9/9 UserGroupRightsTab.test.tsx suite (unchanged assertions) is the regression gate — any accidental logic drift during the move fails these tests immediately. |
| T-s7v-02 | Information Disclosure | Mutation entry points (GuidedRevokeFlow/GuidedGrantFlow/RoleAssignmentImpactModal) | accept | This plan does not touch these components' internals or the authorization checks they trigger server-side; it only relocates the call sites that render them, with identical props. |
</threat_model>

<verification>
Run the single verification command after each task:
`docker compose exec -T team4sv30-frontend sh -c 'cd /app && npx vitest run src/app/admin/users/tabs/ --reporter=basic'`

After Task 2, additionally confirm via `wc -l` that every one of the 6 affected files is ≤450
lines, and diff-review that `UserGroupRightsTab.tsx`'s remaining `UserGroupRightsTab` function
body is unchanged from the original (only its file's surrounding imports and co-located
definitions changed).
</verification>

<success_criteria>
- `UserGroupRightsTab.tsx` is ≤450 lines and remains the sole exported entry point / mutation
  orchestrator for the tab (UADM-01).
- 5 new sibling files exist (`userGroupRightsHelpers.ts`, `CapabilityDetailRow.tsx`,
  `CategoryTable.tsx`, `GroupRolesSection.tsx`, `GroupSection.tsx`), each ≤450 lines.
- All 9 pre-existing `UserGroupRightsTab.test.tsx` tests pass with zero assertion changes; the
  full `src/app/admin/users/tabs/` suite has no new failures.
- Zero behavior change: no permission/resolver semantics, no user-facing text, no component props
  altered anywhere in the split.
- No native `<select>/<input>/<textarea>/<button>` introduced; global `@/components/ui` primitives
  used throughout, matching pre-split usage exactly.
- German umlauts remain correct in every carried-over user-facing string.
- Final summary reports exact `wc -l` line counts for all 6 affected files.
</success_criteria>

<output>
Create `.planning/quick/260823-s7v-split-frontend-src-app-admin-users-tabs-/260823-s7v-SUMMARY.md` when done
</output>
</content>
