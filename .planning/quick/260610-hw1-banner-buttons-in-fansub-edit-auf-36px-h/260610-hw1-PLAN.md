---
phase: 260610-hw1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/admin/MediaUpload.module.css
autonomous: true
requirements:
  - UI-ALIGN-01
must_haves:
  truths:
    - Banner-Buttons (Bearbeiten/Ersetzen/Löschen) sind 36px hoch (identisch mit Logo-Buttons)
    - Logo-Buttons behalten ihre 36px Höhe (keine Regression)
    - Beide Cards füllen weiterhin min-height 310px ohne Inhalt zu strecken
  artifacts:
    - path: frontend/src/components/admin/MediaUpload.module.css
      provides: align-content: start in gemeinsamer .cardLogo, .cardBanner-Regel
  key_links:
    - from: .cardLogo, .cardBanner
      to: Card (display:grid)
      via: align-content: start
      pattern: "align-content:\\s*start"
---

<objective>
Banner-Buttons in der Fansub-Edit-Seite auf 36px vereinheitlichen.

Purpose: Die Banner-Card erbt vom Grid-Container (Card = display:grid) das Default-Verhalten
align-content: stretch, das die .actions-Zeile auf 54px aufbläst. Die Logo-Card hat
align-content: start und ist korrekt. Fix: align-content: start in die gemeinsame
.cardLogo, .cardBanner-Regel verschieben, damit beide Cards das gleiche Verhalten haben.

Output: MediaUpload.module.css mit align-content: start in der gemeinsamen Regel,
entfernt aus dem separaten .cardLogo-Block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: align-content: start in gemeinsame Card-Regel verschieben</name>
  <files>frontend/src/components/admin/MediaUpload.module.css</files>
  <action>
    Zeilen 1-14 in MediaUpload.module.css neu schreiben:

    Vorher (Zeilen 1-14):
      .cardLogo,
      .cardBanner {
        min-height: 310px;
      }

      .cardLogo {
        width: 100%;
        justify-items: center;
        align-content: start;
      }

      .cardBanner {
        min-width: 0;
      }

    Nachher:
      .cardLogo,
      .cardBanner {
        min-height: 310px;
        align-content: start;
      }

      .cardLogo {
        width: 100%;
        justify-items: center;
      }

      .cardBanner {
        min-width: 0;
      }

    Nur diese drei Blöcke ändern. Kein anderer CSS-Code wird angefasst.
    align-content: start wandert von .cardLogo in die gemeinsame Regel.
    Alle weiteren Regeln ab Zeile 16 bleiben unverändert.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('frontend/src/components/admin/MediaUpload.module.css','utf8');const shared=c.match(/\.cardLogo,\s*\.cardBanner\s*\{[^}]*align-content:\s*start/);const logoOnly=c.match(/\.cardLogo\s*\{[^}]*align-content/);if(!shared)throw new Error('align-content:start nicht in gemeinsamer Regel');if(logoOnly)throw new Error('align-content noch in .cardLogo-Block');console.log('OK')"</automated>
    <human-check>
      Dev-Server :3000 offen. Seite /admin/fansubs/1/edit aufrufen (AnimeOwnage hat Logo + Banner).
      DevTools Console:
        document.querySelectorAll('[class*="actions"] button').forEach(b=>{const r=b.getBoundingClientRect();console.log(b.textContent.trim(),r.height+'px')})
      Erwartung: alle Buttons 36px hoch (Logo-Card und Banner-Card identisch).
    </human-check>
  </verify>
  <done>
    Logo-Buttons und Banner-Buttons sind beide 36px hoch.
    align-content: start steht nur noch in der gemeinsamen .cardLogo, .cardBanner-Regel.
    Kein anderer CSS-Code wurde verändert.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS-Modul → Browser | Rein visuell, keine Sicherheitsrelevanz |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-hw1-01 | Tampering | MediaUpload.module.css | accept | Reine Layoutkorrektur, keine Daten oder Auth betroffen |
</threat_model>

<verification>
Nach dem Edit:
1. Automated-Check (node-Snippet oben) muss "OK" ausgeben.
2. Manuell im Browser: alle Action-Buttons auf /admin/fansubs/1/edit haben 36px Höhe.
3. Kein anderer Block in MediaUpload.module.css verändert (git diff zeigt nur Zeilen 1-14).
</verification>

<success_criteria>
Banner-Buttons rendern 36px hoch — identisch mit Logo-Buttons.
align-content: start gilt für beide Cards über die gemeinsame Regel.
Keine visuellen Regressionen in der Logo-Card.
</success_criteria>

<output>
Create `.planning/quick/260610-hw1-banner-buttons-in-fansub-edit-auf-36px-h/260610-hw1-01-SUMMARY.md` when done
</output>
