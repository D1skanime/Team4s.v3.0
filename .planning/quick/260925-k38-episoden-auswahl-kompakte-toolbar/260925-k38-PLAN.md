# Quick Plan

## Read first

- `AGENTS.md`
- `frontend/src/components/episodes/EpisodesOverview/EpisodesOverview.tsx`
- `frontend/src/components/episodes/EpisodesOverview/EpisodesOverview.module.css`
- `frontend/src/app/admin/anime/[id]/episodes/page.tsx`

## Actions

1. Move the overview heading into the episode overview component beside the select-all control.
2. Constrain the checkbox to a compact 18px control and keep the label on one line.
3. Change the label to „Auswahl aufheben“ when all episodes are selected, preserve responsive stacking, run focused checks, and push.

## Acceptance criteria

- The select-all control is compact and visually aligned with „Episoden mit Versionen“.
- Selecting every episode changes the label to „Auswahl aufheben“.
- Bulk actions remain available only when episodes are selected.
