# Quick Plan

## Read first

- `AGENTS.md`
- `docs/engineering/implementation-contract.md`
- `docs/frontend/ui-system.md`
- `docs/agent-guidelines-ui.md`
- `frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportMappingRow.tsx`
- `frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportMappingRowGroupField.tsx`
- `frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportEpisodeGroup.tsx`
- `frontend/src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.ts`
- `frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.ts`

## Actions

1. Remove the row-level `display_path` rendering and its unused local CSS.
2. Add a focused `confirmMapping` mapping helper and expose it through the existing builder seam.
3. Add a per-row „Bestätigen“ action for suggested/conflict rows while retaining „Überspringen“ and confirmed-row apply behavior.
4. Keep the existing field order: Gruppe, Episode, Version, Aktionen.
5. Add a focused mapping-helper test, run typecheck/lint/import tests/diff checks, document and commit.

## Acceptance criteria

- `Anime.TV.Sub/07-Ghost` is no longer rendered in the lower mapping row.
- The label „Vorschlag“ is no longer rendered, while the row’s yellow status accent remains.
- Suggested rows expose „Bestätigen“ and „Überspringen“ actions.
- Group chips and group-scope controls remain functional.
- The existing mapping state and apply flow continue to pass tests.
