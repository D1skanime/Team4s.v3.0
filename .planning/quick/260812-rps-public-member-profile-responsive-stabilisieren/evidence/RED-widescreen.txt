
> team4s-frontend@0.1.0 test
> vitest run --run src/app/members/[slug]/page.test.tsx src/components/profile/MemberCurrentProjectsSection.test.tsx -t Quick 260812-rps widescreen


 RUN  v3.2.4 /app

 ❯ src/components/profile/MemberCurrentProjectsSection.test.tsx (8 tests | 1 failed | 7 skipped) 12ms
   ↓ MemberCurrentProjectsSection > aligns equal-height cards and the count/load footer with the project grid
   ↓ MemberCurrentProjectsSection > uses the responsive two-column project grid without overflow
   ↓ MemberCurrentProjectsSection > renders every known role with its global accent token, unknown roles as other, and Projektweit neutrally
   ↓ MemberCurrentProjectsSection > makes the whole existing Card the exact project link without an isolated arrow
   ↓ MemberCurrentProjectsSection > loads exactly the next six once while preserving the initial six and guarding in-flight clicks
   ↓ MemberCurrentProjectsSection > keeps load-more dormant and the geometry shell visible until the section is near the viewport
   ↓ Phase 120 RED: reserves project geometry while SSR cards remain readable
   × Quick 260812-rps widescreen project alignment > lets a single project consume the shared section width 11ms
     → expected '.section {\n  display: grid;\n  gap: …' to match /\.projectList\s*\{[^}]*grid-template…/s
 ❯ src/app/members/[slug]/page.test.tsx (22 tests | 1 failed | 18 skipped) 15ms
   ↓ MemberProfilePage Phase 99 route composition > lets every visible profile section consume the shared 1480px shell
   ↓ MemberProfilePage Phase 99 route composition > encodes Rhythm C geometry for 390, 768 and 1440 pixel layouts without overflow repairs
   ↓ MemberProfilePage Phase 99 route composition > keeps every post-hero outer band transparent without changing layout or inner surfaces
   ↓ MemberProfilePage Phase 99 route composition > opts only the role badge band into the centered visual-width breakout
   ↓ MemberProfilePage Phase 99 route composition > renders the locked Hero B and Rhythm C heading hierarchy in DOM order
   ↓ MemberProfilePage Phase 99 route composition > keeps empty earned-only and contribution sections out of the public outline
   ↓ MemberProfilePage Phase 99 route composition > Phase 120 RED: deduplicates metadata and page reads for the same slug and viewer token
   ↓ MemberProfilePage Phase 99 route composition > Phase 120 RED: isolates anonymous and owner-preview cache keys
   ↓ MemberProfilePage Phase 99 route composition > does not render the old public tab navigation labels as section navigation or fetch the old timeline
   ↓ MemberProfilePage Phase 99 route composition > passes badge progress through the existing route
   ↓ MemberProfilePage Phase 99 route composition > keeps the hidden-profile owner preview path intact
   ↓ Phase 124 deterministic points SSR projection > derives only the highest badge at 0 and forwards authoritative progress unchanged
   ↓ Phase 124 deterministic points SSR projection > derives only the highest badge at 734 and forwards authoritative progress unchanged
   ↓ Phase 124 deterministic points SSR projection > derives only the highest badge at 2500 and forwards authoritative progress unchanged
   ↓ Phase 124 deterministic points SSR projection > derives only the highest badge at 5000 and forwards authoritative progress unchanged
   ↓ Phase 124 deterministic points SSR projection > keeps initial point selection props-only during repeat server renders
   ↓ Phase 126 membership SSR projection > keeps membership visible at zero and below five
   ↓ Phase 127 RED SSR hero composition > Phase 127 RED page forwards public badges through one cached request and suppresses legacy Special
   ✓ Quick 260811-pqe responsive profile composition > pins distinct profile and history-aware contribution grids without global word breaking 2ms
   ✓ Quick 260811-rms widescreen profile balance > keeps profile columns balanced on widescreen and stacks them through tight tablet widths 1ms
   ✓ Quick 260812-rps full-page responsive contract > stacks constrained pairs before tablet portrait geometry fails without clipping the page 1ms
   × Quick 260812-rps widescreen UAT regression > keeps the shared section edges aligned and the profile pair near 60/40 10ms
     → expected '.page {\n  width: min(var(--public-pa…' to match /\.profilePair\s*\{[^}]*grid-template…/s

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 2 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  src/components/profile/MemberCurrentProjectsSection.test.tsx > Quick 260812-rps widescreen project alignment > lets a single project consume the shared section width
AssertionError: expected '.section {\n  display: grid;\n  gap: …' to match /\.projectList\s*\{[^}]*grid-template…/s

[32m- Expected:[39m 
/\.projectList\s*\{[^}]*grid-template-columns:\s*repeat\(auto-fit,\s*minmax\(min\(100%,\s*32rem\),\s*1fr\)\)/s

[31m+ Received:[39m 
".section {
  display: grid;
  gap: 14px;
}

.projectList {
  display: grid;
  grid-column: 1;
  grid-row: 2;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 12px;
  margin: 0;
  padding: 0;
  list-style: none;
}

.projectList > li {
  min-width: 0;
  height: 100%;
}
.projectSkeleton {
  z-index: 2;
  pointer-events: none;
  opacity: 1;
  visibility: visible;
}

.projectSkeleton[data-visible=\"false\"] {
  opacity: 0;
  visibility: hidden;
}

.skeletonCard {
  border-color: var(--border-subtle);
  background: var(--surface-card);
}

.skeletonCover {
  background: var(--surface-card-muted);
}

.skeletonBody > span {
  display: block;
  border-radius: 999px;
  background: var(--surface-sunken);
}

.skeletonTitle {
  width: min(72%, 240px);
  height: 18px;
}

.skeletonGroup {
  width: min(48%, 160px);
  height: 14px;
}

.skeletonChips {
  width: min(62%, 200px);
  height: 24px;
}

.projectLink {
  display: block;
  min-width: 0;
  height: 100%;
  color: inherit;
  text-decoration: none;
}

.projectCard {
  height: 100%;
  min-width: 0;
  min-height: 136px;
  display: grid;
  grid-template-columns: 90px minmax(0, 1fr);
  align-items: stretch;
  gap: 0;
  padding: 0;
  overflow: hidden;
}

.projectLink:hover strong,
.projectLink:focus-visible strong {
  text-decoration: underline;
}

.cover {
  width: 90px;
  aspect-ratio: 2 / 3;
  display: inline-flex;
  align-items: stretch;
  justify-content: center;
  overflow: hidden;
  background:
    linear-gradient(135deg, rgba(47, 95, 227, 0.16), rgba(47, 155, 121, 0.12)),
    rgba(248, 250, 252, 0.94);
  color: var(--color-primary, #2f5fe3);
  font-size: 1.2rem;
  font-weight: 900;
}

.cover img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.projectBody {
  min-width: 0;
  display: grid;
  align-content: center;
  gap: 12px;
  padding: 14px 16px;
}

.projectTitleRow {
  min-width: 0;
  display: grid;
  gap: 6px;
}

.projectTitleRow strong {
  color: var(--text-strong, #111827);
  font-size: 1.02rem;
  line-height: 1.25;
  overflow-wrap: anywhere;
}

.projectGroup {
  color: var(--text-secondary, #64748b);
  font-size: 0.88rem;
  font-weight: 700;
  overflow-wrap: anywhere;
}

.chipRow {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 7px;
  min-width: 0;
}

.chipRow span {
  display: inline-flex;
  align-items: center;
  gap: 5px;
}

.roleChip {
  --project-role-accent: var(--role-accent-default);
  border-color: color-mix(in srgb, var(--project-role-accent) 32%, transparent);
  background: color-mix(in srgb, var(--project-role-accent) 13%, var(--surface-card));
  color: color-mix(in srgb, var(--project-role-accent) 78%, var(--text-primary));
  box-shadow: 0 4px 10px color-mix(in srgb, var(--project-role-accent) 10%, transparent);
}

.roleChip[data-role-code=\"fansub_lead\"] { --project-role-accent: var(--role-accent-fansub-lead); }
.roleChip[data-role-code=\"project_lead\"] { --project-role-accent: var(--role-accent-project-lead); }
.roleChip[data-role-code=\"editor\"] { --project-role-accent: var(--role-accent-editor); }
.roleChip[data-role-code=\"translator\"] { --project-role-accent: var(--role-accent-translator); }
.roleChip[data-role-code=\"timer\"] { --project-role-accent: var(--role-accent-timer); }
.roleChip[data-role-code=\"typesetter\"] { --project-role-accent: var(--role-accent-typesetter); }
.roleChip[data-role-code=\"quality_checker\"] { --project-role-accent: var(--role-accent-quality-checker); }
.roleChip[data-role-code=\"encoder\"] { --project-role-accent: var(--role-accent-encoder); }
.roleChip[data-role-code=\"raw_provider\"] { --project-role-accent: var(--role-accent-raw-provider); }
.roleChip[data-role-code=\"designer\"] { --project-role-accent: var(--role-accent-designer); }
.roleChip[data-role-code=\"admin\"] { --project-role-accent: var(--role-accent-admin); }
.roleChip[data-role-code=\"other\"] { --project-role-accent: var(--role-accent-other); }

.projectFooter {
  grid-column: 1;
  grid-row: 3;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  color: var(--text-secondary, #64748b);
  font-size: 0.875rem;
  font-weight: 700;
  padding-top: var(--space-2);
  border-top: 1px solid var(--border-subtle);
}

.loadError {
  grid-column: 1;
  grid-row: 4;
  margin: 0;
  color: var(--color-danger, #9f1239);
  font-size: 0.875rem;
}

@media (max-width: 1100px) {
  .projectList {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (max-width: 720px) {
  .projectList {
    grid-template-columns: minmax(0, 1fr);
  }

  .projectFooter {
    align-items: stretch;
    flex-direction: column;
  }

  .projectCard {
    grid-template-columns: 68px minmax(0, 1fr);
    min-height: 102px;
  }

  .cover {
    width: 68px;
  }

  .projectBody {
    gap: 6px;
    padding: 10px;
  }
}
"

 ❯ src/components/profile/MemberCurrentProjectsSection.test.tsx:214:27
    212| describe('Quick 260812-rps widescreen project alignment', () => {
    213|   it('lets a single project consume the shared section width', () => {
    214|     expect(projectStyles).toMatch(/\.projectList\s*\{[^}]*grid-templat…
       |                           ^
    215|     expect(projectStyles).not.toMatch(/\.projectList\s*\{[^}]*repeat\(…
    216|   })

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/2]⎯

 FAIL  src/app/members/[slug]/page.test.tsx > Quick 260812-rps widescreen UAT regression > keeps the shared section edges aligned and the profile pair near 60/40
AssertionError: expected '.page {\n  width: min(var(--public-pa…' to match /\.profilePair\s*\{[^}]*grid-template…/s

[32m- Expected:[39m 
/\.profilePair\s*\{[^}]*grid-template-columns:\s*minmax\(0,\s*3fr\) minmax\(0,\s*2fr\)/s

[31m+ Received:[39m 
".page {
  width: min(var(--public-page-max-width), calc(100% - var(--public-page-gutter)));
  min-width: 0;
  margin: 0 auto;
  padding: 24px 0 48px;
}

.backLink a {
  color: var(--text-secondary);
  text-decoration: none;
}

.backLink a:hover {
  color: var(--text-primary);
}

.breadcrumb {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  min-width: 0;
  gap: 8px;
  margin: 0;
  color: var(--text-secondary);
  font-size: 14px;
}

.profileToolbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex-wrap: wrap;
  min-width: 0;
  gap: 16px;
  margin-bottom: 16px;
}

.toolbarActions {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  flex-wrap: wrap;
  min-width: 0;
  gap: 8px;
  margin-left: auto;
}

.editProfileLink {
  min-height: 44px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  padding: 0 16px;
  border: 1px solid var(--border-subtle);
  border-radius: 8px;
  background: var(--surface-card);
  box-shadow: var(--shadow-xs);
  color: var(--text-body);
  font-size: 14px;
  font-weight: 700;
  line-height: 1.5;
  text-align: center;
  text-decoration: none;
  white-space: normal;
  overflow-wrap: anywhere;
}

.editProfileLink:hover,
.editProfileLink:focus-visible {
  border-color: var(--border-strong);
  background: var(--surface-card-muted);
  color: var(--text-strong);
}

.breadcrumb a {
  text-decoration: none;
}

.errorBox {
  margin-top: 16px;
  border-radius: var(--radius-md);
  border: 1px solid var(--color-border);
  background: var(--color-white);
  padding: 16px;
  color: var(--text-primary);
}

.section {
  width: 100%;
  min-width: 0;
  margin: 0 auto 16px;
}

.rhythmBand {
  display: grid;
  gap: 24px;
  min-width: 0;
  padding: 32px;
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-lg);
}

.profileBand,
.projectsBand,
.badgesBand,
.contributionsBand {
  border: 0;
  border-radius: 0;
  background: transparent;
}

.badgesVisualBand {
  position: relative;
  left: 50%;
  width: min(calc(100vw - var(--public-page-gutter)), var(--content-width-visual), var(--content-width-visual-cap));
  transform: translateX(-50%);
}

@media (max-width: 1599px) {
  .badgesVisualBand {
    width: 100%;
    left: auto;
    transform: none;
  }
}

.projectsBand h2 {
  padding-bottom: 8px;
  border-bottom: 2px solid var(--ui-line);
}

.sectionPair {
  display: grid;
  align-items: start;
  gap: 16px;
  min-width: 0;
}

.sectionPair > * {
  min-width: 0;
}

.profilePair {
  grid-template-columns: minmax(0, 8fr) minmax(0, 5fr);
}

.contributionPairPresent {
  grid-template-columns: minmax(0, 3fr) minmax(20rem, 2fr);
}

.contributionPairEmpty {
  grid-template-columns: minmax(0, 3fr) minmax(14rem, 1fr);

  .contributionPairPresent,
  .contributionPairEmpty {
    grid-template-columns: minmax(0, 1fr);
  }
}

.sectionPlaceholder {
  margin: 0;
  color: var(--color-text-muted, #64748b);
  font-size: 0.9rem;
}

/* Legacy: profileGrid bleibt für OwnHiddenProfilePreview */
.profileGrid {
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
  gap: 20px;
  margin-top: 20px;
}

.storySection,
.fullWidthSection {
  grid-column: 1 / -1;
}

.storySection {
  min-width: 0;
}

.contentSection {
  min-width: 0;
}

.contentSection h2 {
  margin: 0 0 12px;
  font-size: 18px;
  line-height: 1.3;
}

@media (max-width: 1399px) {
  .profilePair {
    grid-template-columns: minmax(0, 1fr);
  }
}

@media (max-width: 1099px) {
  .rhythmBand {
    padding: 24px;
  }
}

@media (max-width: 760px) {
  .page {
    width: min(calc(100% - 24px), var(--public-page-max-width));
    padding-top: 16px;
  }

  .rhythmBand {
    padding: 24px 16px;
  }

  .profilePair,
  .contributionPairPresent,
  .contributionPairEmpty {
    grid-template-columns: minmax(0, 1fr);
  }

  .profileGrid {
    grid-template-columns: minmax(0, 1fr);
  }

  .profileToolbar {
    align-items: stretch;
    flex-direction: column;
  }

  .toolbarActions {
    justify-content: flex-start;
    margin-left: 0;
  }

  .editProfileLink {
    align-self: flex-start;
  }
}
"

 ❯ src/app/members/[slug]/page.test.tsx:560:30
    558| describe('Quick 260812-rps widescreen UAT regression', () => {
    559|   it('keeps the shared section edges aligned and the profile pair near…
    560|     expect(memberPageStyles).toMatch(/\.profilePair\s*\{[^}]*grid-temp…
       |                              ^
    561|     const visualBand = memberPageStyles.match(/\.badgesVisualBand\s*\{…
    562|     expect(visualBand).toContain('width: 100%;')

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/2]⎯


 Test Files  2 failed (2)
      Tests  2 failed | 3 passed | 25 skipped (30)
   Start at  12:51:31
   Duration  2.00s (transform 878ms, setup 0ms, collect 2.26s, tests 27ms, environment 959ms, prepare 130ms)


> team4s-frontend@0.1.0 test
> vitest run --run src/components/profile/MemberProfileHero.test.tsx -t Quick 260812-rps responsive Hero


 RUN  v3.2.4 /app

 ↓ src/app/fansubs/__tests__/publicProjectHeroMobileContract.test.ts (2 tests | 2 skipped)
 ↓ src/components/ui/ResponsiveImage.config.test.ts (5 tests | 5 skipped)
 ↓ src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/responsiveGalleryReveal.test.ts (4 tests | 4 skipped)
 ↓ src/components/ui/ResponsiveImage.test.tsx (1 test | 1 skipped)
 ↓ src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseDetailHero.test.tsx (7 tests | 7 skipped)
 ↓ src/components/ui/HeroMetrics.test.tsx (1 test | 1 skipped)
 ❯ src/components/profile/MemberProfileHero.test.tsx (20 tests | 1 failed | 19 skipped) 8ms
   ↓ MemberProfileHero — Memorial-Variante (Wave-0 RED, D-10) > rendert Gedenk-Sprache bei profile_status="memorial"
   ↓ MemberProfileHero — Memorial-Variante (Wave-0 RED, D-10) > unterdrückt Mengen-/Aktivitäts-Badges bei profile_status="memorial" (D-10)
   ↓ MemberProfileHero > locks the Hero B responsive geometry and local copy-zone treatment
   ↓ MemberProfileHero > Phase 120 RED: prioritizes both hero background and avatar with differentiated discovery
   ↓ MemberProfileHero > keeps the same prioritized centered Hero B contract for light artwork
   ↓ MemberProfileHero > keeps the same prioritized centered Hero B contract for dark artwork
   ↓ MemberProfileHero > keeps the complete Hero B fallback when member images are absent
   ↓ MemberProfileHero > composes the public Hero B copy in the locked semantic order without leaking source originals
   ↓ MemberProfileHero > shows the public fansub activity period without adding a separate card
   ↓ MemberProfileHero > shows a finished public fansub activity period
   ↓ MemberProfileHero > renders GIF avatars without Next image optimization so animation survives
   ↓ MemberProfileHero > shows the total points hero metric for a public profile with real points (D-02)
   ↓ MemberProfileHero > still shows the honest zero total points hero metric, never hidden (D-02)
   ↓ MemberProfileHero > never renders the total points hero metric on the own-profile edit view
   ↓ Phase 127 RED hero special awards > Phase 127 RED hero renders no gap then Historical alone with approved decorative artwork
   ↓ Phase 127 RED hero special awards > Phase 127 RED hero renders Allrounder alone with Hexagon fallback and no invented image
   ↓ Phase 127 RED hero special awards > Phase 127 RED hero renders both once in catalog order as a static nonfocusable list
   ↓ Phase 127 RED hero special awards > Phase 127 RED hero rejects Verified Founding role and unknown while Verifiziert stays once
   ↓ Phase 127 RED hero special awards > Phase 127 RED hero CSS normalizes slots and wraps without responsive overflow
   × Quick 260812-rps responsive Hero contract > keeps identity, status, points and metadata shrinkable with normal local wrapping 7ms
     → expected '.hero {\n  display: grid;\n  gap: 16p…' to match /\.membershipsList\s*\{[^}]*grid-temp…/s

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  src/components/profile/MemberProfileHero.test.tsx > Quick 260812-rps responsive Hero contract > keeps identity, status, points and metadata shrinkable with normal local wrapping
AssertionError: expected '.hero {\n  display: grid;\n  gap: 16p…' to match /\.membershipsList\s*\{[^}]*grid-temp…/s

[32m- Expected:[39m 
/\.membershipsList\s*\{[^}]*grid-template-columns:\s*repeat\(auto-fit,\s*minmax\(min\(100%,\s*18rem\),\s*1fr\)\)/s

[31m+ Received:[39m 
".hero {
  display: grid;
  gap: 16px;
  min-width: 0;
  margin-bottom: 24px;
}

.heroPanel {
  position: relative;
  isolation: isolate;
  max-width: 100%;
  min-width: 0;
  min-height: 230px;
  display: grid;
  grid-template-columns: 140px minmax(0, 1fr);
  align-items: end;
  gap: 24px;
  padding: 32px;
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-lg);
  background: color-mix(in srgb, var(--accent-deep) 82%, var(--text-strong));
  box-shadow: var(--shadow-sm);
  color: #fff;
  overflow: hidden;
}

.heroPanel::after {
  position: absolute;
  inset: 0;
  z-index: -1;
  content: \"\";
  pointer-events: none;
  background: linear-gradient(
    90deg,
    rgba(9, 15, 27, 0.72) 0%,
    rgba(9, 15, 27, 0.58) 34%,
    rgba(9, 15, 27, 0.22) 54%,
    rgba(9, 15, 27, 0.04) 65%,
    transparent 72%
  );
}

.heroBackdrop {
  position: absolute;
  inset: 0;
  z-index: -2;
  overflow: hidden;
}

.heroBackdrop img {
  width: 100%;
  height: 100%;
  display: block;
  object-fit: cover;
  object-position: center;
}

.heroAvatar {
  width: 140px;
  height: 140px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border: 4px solid rgba(255, 255, 255, 0.76);
  border-radius: 50%;
  background: color-mix(in srgb, var(--surface-card) 18%, transparent);
  box-shadow: 0 10px 30px rgba(9, 15, 27, 0.4);
  font-size: 3rem;
  font-weight: 700;
  overflow: hidden;
}

.heroAvatar img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.heroCopy {
  display: grid;
  align-content: end;
  gap: 8px;
  min-width: 0;
  max-width: min(100%, 760px);
  text-shadow: 0 2px 8px rgba(9, 15, 27, 0.82);
}

.heroTitle,
.heroCopy p {
  margin: 0;
}

.heroEyebrow {
  color: rgba(255, 255, 255, 0.9);
  font-size: 14px;
  font-weight: 700;
  letter-spacing: 0.1em;
  line-height: 1.5;
  text-transform: uppercase;
}

.heroTitle {
  min-width: 0;
  color: #fff;
  font-size: 2rem;
  font-weight: 700;
  line-height: 1.05;
  overflow-wrap: normal;
}

.heroTitleRow {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 8px;
  min-width: 0;
}

.heroStatusSurface {
  display: inline-flex;
  align-items: center;
  min-width: 0;
  padding: 4px 8px;
  border: 1px solid color-mix(in srgb, var(--surface-card) 46%, transparent);
  border-radius: 999px;
  background: color-mix(in srgb, var(--surface-card) 92%, transparent);
  text-shadow: none;
}

.heroPoints {
  width: max-content;
  max-width: 100%;
  padding: 8px 12px;
  border: 1px solid color-mix(in srgb, var(--surface-card) 42%, transparent);
  border-radius: 8px;
  background: color-mix(in srgb, var(--text-strong) 72%, transparent);
  text-shadow: none;
}

.heroPoints dt,
.heroPoints dd {
  color: #fff;
}

.heroBio {
  max-width: 760px;
  color: rgba(255, 255, 255, 0.92);
  line-height: 1.5;
  overflow-wrap: normal;
}

.heroMetaLine {
  display: inline-flex;
  width: fit-content;
  align-items: center;
  gap: 7px;
  color: rgba(255, 255, 255, 0.82);
  font-size: 14px;
  font-weight: 700;
  line-height: 1.5;
  overflow-wrap: normal;
}

.heroMetaLine svg {
  flex: 0 0 auto;
}

.heroSpecialAwardsList {
  display: flex;
  flex-wrap: wrap;
  min-width: 0;
  width: fit-content;
  max-width: 100%;
  gap: 8px;
  margin: 4px 0 0;
  padding: 0;
  list-style: none;
}

.heroSpecialAward {
  display: inline-flex;
  min-width: 0;
  align-items: center;
  gap: 8px;
  padding: 4px 10px 4px 4px;
  border: 1px solid color-mix(in srgb, var(--surface-card) 46%, transparent);
  border-radius: 999px;
  background: color-mix(in srgb, var(--text-strong) 76%, transparent);
  color: #fff;
  font-size: 0.85rem;
  font-weight: 700;
  line-height: 1.25;
  text-shadow: 0 1px 4px rgba(9, 15, 27, 0.9);
  overflow-wrap: anywhere;
}

.heroSpecialArtwork {
  width: 40px;
  height: 40px;
  flex: 0 0 40px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  border-radius: 50%;
  background: color-mix(in srgb, var(--surface-card) 94%, transparent);
  color: var(--color-primary, #2f5fe3);
  text-shadow: none;
}

.heroSpecialArtwork img {
  width: 100%;
  height: 100%;
  object-fit: contain;
}

.deferredActionWrap {
  position: relative;
  display: inline-flex;
}

.deferredActionReason {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}

.chipRow {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  align-items: center;
}

.heroActionButton {
  align-self: flex-start;
  white-space: normal;
}

.recentList,
.recentMediaGrid {
  display: grid;
  gap: 12px;
  margin: 0;
  padding: 0;
  list-style: none;
}

.recentMediaGrid {
  grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
}

.recentMediaCard,
.recentItemBody {
  display: grid;
  gap: 10px;
}

.recentItemBody {
  min-width: 0;
  align-content: start;
  align-items: start;
}

.recentContributionCard {
  display: grid;
  grid-template-columns: 74px minmax(0, 1fr);
  gap: 14px;
  align-items: center;
}

.recentContributionCover {
  width: 74px;
  aspect-ratio: 1;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border-radius: 8px;
  background:
    linear-gradient(135deg, rgba(47, 95, 227, 0.18), rgba(47, 155, 121, 0.14)),
    rgba(248, 250, 252, 0.92);
  color: var(--color-primary, #2f5fe3);
  font-size: 1.35rem;
  font-weight: 900;
}

.projectProgress {
  height: 7px;
  overflow: hidden;
  border-radius: 999px;
  background: rgba(100, 116, 139, 0.16);
}

.projectProgress span {
  display: block;
  height: 100%;
  min-width: 12%;
  border-radius: inherit;
  background: linear-gradient(90deg, var(--color-primary, #2f5fe3), var(--color-success, #2f9b79));
}

.projectActionRow {
  display: flex;
  justify-content: flex-start;
}

.recentItemBody span {
  color: var(--color-text-muted, #64748b);
  font-size: 0.9rem;
}

.recentItemBody strong {
  display: -webkit-box;
  max-width: 100%;
  overflow: hidden;
  color: var(--text-strong, #111827);
  line-height: 1.35;
  overflow-wrap: anywhere;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 3;
}

.recentMediaCard {
  grid-template-rows: auto auto;
  align-content: start;
  height: 100%;
  padding: 12px;
}

.recentMediaThumb {
  aspect-ratio: 16 / 9;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  border-radius: 8px;
  background: rgba(47, 95, 227, 0.1);
  color: var(--color-primary, #2f5fe3);
  font-weight: 800;
}

.recentMediaThumb img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.membershipsSection {
  display: grid;
  gap: 14px;
  min-width: 0;
}

.membershipsList {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 360px));
  justify-content: start;
  gap: 12px;
  margin: 0;
  padding: 0;
  list-style: none;
}

.membershipCard {
  min-width: 0;
  height: 100%;
  padding: 0;
  overflow: hidden;
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-lg);
  background: var(--surface-card);
  box-shadow: var(--shadow-sm);
}

.membershipLink {
  min-width: 0;
  height: 100%;
  padding: 14px;
  border-radius: inherit;
  display: grid;
  grid-template-columns: 52px minmax(0, 1fr);
  gap: 14px;
  align-items: center;
  color: inherit;
  text-decoration: none;
  font-size: 14px;
  font-weight: 400;
  line-height: 1.5;
}

.membershipLink:hover strong,
.membershipLink:focus-visible strong {
  text-decoration: underline;
}
.membershipLink:focus-visible {
  outline: 2px solid var(--focus-ring, var(--color-primary, #2f5fe3));
  outline-offset: -3px;
}


.membershipLogo {
  width: 52px;
  height: 52px;
  flex: 0 0 52px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  border: 1px solid rgba(255, 255, 255, 0.82);
  border-radius: 999px;
  background: rgba(47, 95, 227, 0.1);
  color: var(--color-primary, #2f5fe3);
  box-shadow: 0 8px 18px rgba(15, 23, 42, 0.12);
}

.membershipLogo img {
  width: 100%;
  height: 100%;
  border-radius: inherit;
  object-fit: cover;
}

.membershipName {
  display: grid;
  gap: 4px;
  min-width: 0;
}

.membershipName strong {
  font-weight: 700;
  overflow-wrap: normal;
  word-break: normal;
}

.membershipName span {
  color: var(--text-soft);
  font-size: 14px;
  font-weight: 400;
}

.membershipAction {
  grid-column: 2;
  justify-self: start;
  display: inline-flex;
  align-items: center;
  gap: 6px;
  color: var(--color-primary, #2f5fe3);
  font-size: 14px;
  font-weight: 700;
  white-space: nowrap;
}

.emptyText {
  margin: 0;
  color: var(--color-text-muted, #64748b);
}

@media (max-width: 1099px) {
  .heroPanel {
    min-height: 220px;
    grid-template-columns: 120px minmax(0, 1fr);
    padding: 24px;
  }

  .heroAvatar {
    width: 120px;
    height: 120px;
  }
}

@media (max-width: 760px) {
  .heroPanel {
    min-height: 0;
    grid-template-columns: minmax(0, 1fr);
    align-content: start;
    align-items: start;
    gap: 16px;
    padding: 16px;
  }

  .heroAvatar {
    width: 100px;
    height: 100px;
  }

  .heroCopy,
  .heroTitleRow,
  .heroBio,
  .heroMetaLine,
  .knownForBlock,
  .heroSpecialAwardsList {
    min-width: 0;
    max-width: 100%;
  }
}

@media (max-width: 1100px) {
  .membershipsList {
    grid-template-columns: repeat(2, minmax(0, 360px));
  }
}

@media (max-width: 680px) {
  .membershipsList {
    grid-template-columns: minmax(0, 1fr);
  }

  .recentMediaGrid {
    grid-template-columns: 1fr;
  }

  .recentContributionCard {
    grid-template-columns: 1fr;
  }

  .heroActionButton {
    width: 100%;
  }
}

/* ---- Memorial-Hero ---- */

.memorialNotice {
  margin: 0;
  padding: 8px 12px;
  border-left: 3px solid rgba(255, 255, 255, 0.4);
  color: rgba(255, 255, 255, 0.9);
  font-size: 0.95rem;
  font-style: italic;
}

.memorialBio {
  margin: 0;
  color: rgba(255, 255, 255, 0.72);
  font-size: 0.9rem;
}

/* ---- „Bekannt für\"-Block (D-03) ---- */

.knownForBlock {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-top: 4px;
}

.knownForItem {
  font-size: 0.85rem;
  color: rgba(255, 255, 255, 0.78);
  font-weight: 400;
  overflow-wrap: anywhere;
}

/* ---- Badge-Chips ---- */

.badgeChipsRow {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin: 12px 0 4px;
}

.badgeChip {
  white-space: nowrap;
}

/* ---- Rollen-Timeline ---- */

.roleTimelineSection {
  display: grid;
  gap: 14px;
  margin-top: 16px;
}

.roleTimelineList {
  list-style: none;
  margin: 0;
  padding: 0;
  display: grid;
  gap: 10px;
}

.roleTimelineEntry {
  --timeline-accent: var(--text-secondary);
  --timeline-bg: color-mix(in srgb, var(--timeline-accent) 12%, var(--surface-card-muted));
  display: grid;
  grid-template-columns: 110px minmax(0, 1fr);
  align-items: start;
  gap: 16px;
  padding: 15px 16px;
  border-color: color-mix(in srgb, var(--timeline-accent) 36%, transparent);
  border-left: 4px solid var(--timeline-accent);
  background:
    linear-gradient(135deg, color-mix(in srgb, var(--timeline-accent) 18%, var(--surface-card)) 0%, var(--timeline-bg) 48%, color-mix(in srgb, var(--timeline-accent) 10%, var(--surface-sunken)) 100%);
  box-shadow:
    inset 0 1px 0 color-mix(in srgb, var(--surface-card) 70%, transparent),
    0 8px 22px color-mix(in srgb, var(--timeline-accent) 10%, transparent);
}

.roleTimelineEntryHistory {
  --timeline-accent: var(--color-warning);
  --timeline-bg: color-mix(in srgb, var(--color-warning) 20%, var(--surface-card-muted));
}

.roleTimelineEntryAnime {
  --timeline-accent: var(--color-success);
  --timeline-bg: color-mix(in srgb, var(--color-success) 16%, var(--surface-card-muted));
}

.roleTimelineYear {
  width: fit-content;
  min-width: 88px;
  padding: 6px 8px;
  border: 1px solid color-mix(in srgb, var(--timeline-accent) 32%, transparent);
  border-radius: 999px;
  background: color-mix(in srgb, var(--timeline-accent) 16%, var(--surface-card));
  color: color-mix(in srgb, var(--timeline-accent) 72%, var(--text-primary));
  font-size: 0.78rem;
  font-weight: 800;
  line-height: 1;
  text-align: center;
}

.roleTimelineContent {
  min-width: 0;
  display: grid;
  gap: 6px;
}

.roleTimelineMeta {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 6px;
}

.roleTimelineGroup {
  font-size: 0.88rem;
  color: var(--text-secondary);
}

.roleTimelineRole {
  color: var(--text-primary);
  font-size: 1rem;
  line-height: 1.25;
}

.roleTimelineRole,
.roleTimelineGroup,
.roleTimelineAnime {
  overflow-wrap: anywhere;
}

.roleTimelineAnime {
  color: var(--text-secondary);
  font-size: 0.84rem;
}

.roleTimelineDisclaimer {
  margin: 10px 0 0;
  color: var(--text-secondary);
  font-size: 0.82rem;
}

@media (max-width: 640px) {
  .roleTimelineEntry {
    grid-template-columns: 1fr;
    gap: 10px;
  }

  .roleTimelineYear {
    min-width: 0;
  }
}
"

 ❯ src/components/profile/MemberProfileHero.test.tsx:398:17
    396|     expect(copyRule).toContain('max-width: min(100%, 760px);')
    397|     expect(css).toMatch(/\.(?:heroTitle|heroBio|heroMetaLine)\s*\{[^}]…
    398|     expect(css).toMatch(/\.membershipsList\s*\{[^}]*grid-template-colu…
       |                 ^
    399|     expect(css).not.toMatch(/\.(?:heroTitle|heroBio|heroMetaLine)\s*\{…
    400|   })

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed | 6 skipped (7)
      Tests  1 failed | 39 skipped (40)
   Start at  12:51:48
   Duration  2.07s (transform 487ms, setup 0ms, collect 1.70s, tests 8ms, environment 1.98s, prepare 480ms)

