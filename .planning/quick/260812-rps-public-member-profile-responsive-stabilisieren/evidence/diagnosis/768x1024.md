# 768x1024 diagnosis

- Route: http://172.17.0.1:3000/members/sheppert
- Exact viewport: 768x1024
- documentElement.clientWidth: 768
- documentElement.scrollWidth: 768
- documentElement.scrollHeight: 7077
- Page-level horizontal overflow after GREEN: none

The pre-change full-page screenshot attempt did not complete: both Playwright fullPage and direct CDP Page.captureScreenshot stalled after the DOM audit. The capture process was terminated and the frontend container was restarted to remove the leaked renderer processes. No image is claimed.

The owning static defects reproduced by RED were the page-global `overflow-wrap: anywhere`, contribution pairing that stacked only below 760px, and reusable carousel geometry coupled to a viewport media query. These were corrected at their page/component owners.

Offending owner rectangles after GREEN: none at the document boundary in the successful 768px audit.
