---
phase: quick-260915-m2h
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/models/episode_version.go
  - backend/internal/handlers/jellyfin_client.go
  - backend/internal/handlers/jellyfin_source_batch.go
  - backend/internal/handlers/jellyfin_media_source.go
  - backend/internal/handlers/jellyfin_media_source_test.go
  - backend/internal/handlers/jellyfin_source_batch_test.go
  - backend/internal/handlers/admin_content_episode_version_editor_helpers.go
  - backend/internal/handlers/admin_content_episode_version_editor_scan.go
  - backend/internal/handlers/admin_content_episode_version_editor_context_test.go
  - frontend/src/types/episodeVersion.ts
  - frontend/src/types/__tests__/episode-version-contract.test.ts
  - frontend/src/lib/api.episode-versions.test.ts
  - frontend/src/app/admin/episode-versions/[versionId]/edit/useEpisodeVersionEditor.ts
  - frontend/src/app/admin/episode-versions/[versionId]/edit/useEpisodeVersionEditor.test.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditorPage.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentBasicFieldsSection.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.helpers.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.module.css
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.test.tsx
  - shared/contracts/openapi.yaml
  - shared/contracts/admin-content.yaml
  - shared/contracts/episode-versions.yaml
  - frontend/src/app/me/releases/[versionId]/workspace/page.tsx
  - frontend/src/app/me/releases/[versionId]/workspace/page.test.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditor.module.css
autonomous: true
requirements: [QUICK-CHAPTER-HINTS, QUICK-SELECTED-FILE-SIZE]
must_haves:
  truths:
    - "On creating a segment for the persisted current release variant, authorized admins and contributors can select a proven chapter timestamp for Start or Ende."
    - "Chapter options display their original millisecond timestamp and name; explicit selection visibly rounds to the nearest whole second and changes form state only."
    - "The existing selected-file size display receives the chosen MediaSource Size, including valid alternate sources."
    - "Wrong-source, unavailable and stale chapters never become selectable; ordinary editor use and manual times survive provider failures."
    - "Opening the editor and choosing chapter hints perform no application writes or new SQL; enrichment shares at most one exact-file GET with duration."
  artifacts:
    - path: backend/internal/handlers/jellyfin_media_source.go
      provides: "Selected-source size and fail-closed chapter attribution"
    - path: backend/internal/handlers/admin_content_episode_version_editor_helpers.go
      provides: "Shared bounded read enrichment and explicit contributor-safe projection"
    - path: frontend/src/types/episodeVersion.ts
      provides: "Documented nullable selected_file and chapter_hints contract"
    - path: frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentBasicFieldsSection.tsx
      provides: "Existing Start/End controls extended with global Select chapter choices"
  key_links:
    - from: backend/internal/handlers/admin_content_episode_version_editor_helpers.go
      to: backend/internal/handlers/jellyfin_source_batch.go
      via: "One exact persisted item read, shared for size, duration and chapters"
    - from: backend/internal/handlers/jellyfin_media_source.go
      to: backend/internal/models/episode_version.go
      via: "Selected source Size and source-proven chapter_hints into EpisodeVersionMediaFile"
    - from: frontend/src/app/admin/episode-versions/[versionId]/edit/useEpisodeVersionEditor.ts
      to: frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentBasicFieldsSection.tsx
      via: "Persisted context and invalidation guard through Page, SegmenteTab and SegmentEditPanel"
---

<objective>
Make MKV chapter times available as deliberate Start/End choices when creating a segment on the current saved release variant, and populate its existing file-size display from Jellyfin.

Purpose: avoid manually searching for chapter times while keeping file ownership and existing segment timing explicit.
Output: additive selected-file read projection, bounded chapter choices, existing UI integration and focused regression coverage.

This is a separate follow-up after Phase 161 verification, whose gate evidence was captured at 64340e93; a subsequent Phase-161 hook save-reconciliation fix is still being completed. Wait for that fix and its focused gate before implementing this quick, then read the fresh hook and make targeted edits. It does not reopen Phase 161, import or relink media, rescan a library, change migrations, save snapshots, or alter application rows as part of verification. Unit/integration fixture writes are restricted to the existing isolated test database. No changes to render/cache timing, video formats, upload flows, permissions or episode assignments.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AI-HANDOFF.md
@docs/engineering/implementation-contract.md
@docs/api/api-contracts.md
@docs/frontend/auth-api-client.md
@docs/frontend/ui-system.md
@docs/agent-guidelines-ui.md
@docs/architecture/db-schema-fansub-domain.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-CHAPTER-FOLLOWUP-RESEARCH.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-09-PLAN.md
@.planning/STATE.md

Research is complete; use its provider schema and exact-item evidence without repeating discovery. Current verified variant 28 has Size 516683140 and starts 0, 17.934, 117.951, 1344.926 seconds. The user's 1298.047/1378.043 examples are separate test inputs, not asserted to belong to variant 28 or public release 48.

No quick CONTEXT.md or roadmap requirement assignment exists. Requirements above are local quick-task identifiers, not additions to REQUIREMENTS.md. Preserve ROADMAP.md and other agents' Phase-161 artifacts.

Working decisions for this quick:
- QD-01: Display original millisecond positions; explicit selection uses Math.round(milliseconds / 1000). The orchestrator announced this default after the optional precision question remained unanswered. It is a working default, not a claim of explicit user approval; apply any later user correction before implementation.
- QD-02: Hints are advisory only and available for creation from the saved current variant's file. No automatic title/type/end/episode inference and no automatic save.
- QD-03: Source identity is mandatory. Alternate B may expose B's Size but never borrow own Item A's chapters. Unknown, ambiguous or unsafe evidence fails closed.
- QD-04: Existing editor endpoint, provider source resolver, global Select, byte formatter, auth client and local timing helpers remain the owning seams.
- QD-05: Both authorized context branches receive safe hints. No additional provider IDs, private paths or stream URLs reach contributors.
- QD-06: Zero new SQL, at most one exact-file GET/context shared with duration, zero requests per chapter and zero additional scan GETs. No library-wide chapter loading.

<interfaces>
Existing contracts, extracted from the current code:
- EpisodeVersionEditorContext contains version, anime_title, selected_groups, date_neighbors, optional anime_folder_path and jellyfin_enrichment_degraded.
- EpisodeVersionMediaFile already has file_name, path, media_item_id, optional media_source_id/stream_url/video_quality/file_size_bytes.
- resolveJellyfinMediaSource(item jellyfinEpisodeItem, stored *models.JellyfinSourceSnapshot) returns resolvedJellyfinMediaSource; use version.JellyfinSource, or its existing MediaSourceID-only binding fallback.
- resolveReviewedJellyfinSource(item, itemID, sourceID, seriesID, folder, stored) enforces exact item/type/series/folder and source consistency. Reuse this existing ownership guard; do not call resolveEpisodeVersionSource, which loads additional SQL bindings.
- getJellyfinSourceItems(ctx, itemIDs) delegates to fetchJellyfinSourceBatch with exact requested-ID-set validation; jellyfinSourceFields is also used by series browsing.
- loadEpisodeVersionEditorContext currently resolves the version/anime/groups/folder and optionally looks up duration. loadEpisodeVersionContributorContext already loads version/anime/groups/neighbors separately and constructs safeVersion explicitly.
- getEpisodeVersionEditorContext(versionID) calls the central authorizedFetch boundary and returns the typed data envelope.
- useEpisodeVersionEditor already owns contextGeneration, loadedRouteVersionId, fileSelectionRevision and hasPendingFileSelection; reuse these for invalidation.
- SegmentEditPanel.FormState has startTime/endTime and sourceType/sourceRef. EMPTY_FORM.sourceType is none, meaning the existing current-release segment path.
- formatTimeInput(totalSeconds) and parseFlexibleTimeInput(value) live in SegmenteTab.helpers.tsx. Existing serialization and render timing use whole seconds.
- SelectProps extends SelectHTMLAttributes; import Select/FormField from @/components/ui.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the additive contract and prove selected-source metadata</name>
  <files>backend/internal/models/episode_version.go, backend/internal/handlers/jellyfin_client.go, backend/internal/handlers/jellyfin_source_batch.go, backend/internal/handlers/jellyfin_media_source.go, backend/internal/handlers/jellyfin_media_source_test.go, backend/internal/handlers/jellyfin_source_batch_test.go, frontend/src/types/episodeVersion.ts, shared/contracts/openapi.yaml, shared/contracts/admin-content.yaml</files>
  <read_first>
    backend/internal/models/episode_version.go
    backend/internal/handlers/jellyfin_client.go
    backend/internal/handlers/jellyfin_source_batch.go
    backend/internal/handlers/jellyfin_media_source.go
    backend/internal/handlers/jellyfin_media_source_test.go
    backend/internal/handlers/jellyfin_source_batch_test.go
    backend/internal/handlers/episode_version_source_hydration.go
    frontend/src/types/episodeVersion.ts
    shared/contracts/openapi.yaml
    shared/contracts/admin-content.yaml
    docs/api/api-contracts.md
    .planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-CHAPTER-FOLLOWUP-RESEARCH.md
  </read_first>
  <behavior>
    - Stored source B returns B.Size when sources are reordered. A's item-level chapters are unavailable if B.Path differs from Item.Path.
    - Exact own path must be nonempty, case-sensitive after existing normalization, and matched by exactly one source; missing/duplicate paths withhold chapters even when a stored ID resolves.
    - Missing/null Chapters is unavailable; a proven explicit [] is known empty. Missing/null/negative timestamps are invalid, timestamp zero is valid, and timestamps beyond the selected source's raw RunTimeTicks are discarded.
    - A valid list is stable-sorted by raw ticks, preserves named equal-time choices, and has at most 256 entries. An oversized list or a nonempty list with no valid entries is unavailable.
    - Original tick positions corresponding to 1298.047 and 1378.043 retain those millisecond values; positive Size survives, null/nonpositive Size remains unknown.
    - Chapter fields are requested only for the exact selected-item reader; default series/batch browse Fields and HTTP request counts remain unchanged.
  </behavior>
  <action>
    Define EpisodeVersionChapterHint with required name (string or null) and start_ms (nonnegative safe integer milliseconds) and extend EpisodeVersionMediaFile with chapter_hints, plus optional nullable EpisodeVersionEditorContext.selected_file using that existing file shape, per QD-01/03/04. Document omission as not requested (e.g. scans), null as unavailable/unsafe evidence, [] as proven no chapters, and a nonempty list as validated choices. At editor-context projection explicitly use null for unavailable hints; never turn missing data into []. Document that millisecond display uses ticks divided by 10000 with sub-millisecond remainder excluded only from this display DTO; preserve int64 ticks through source validation. This does not round to whole seconds on the server. Update both contract files and frontend types in this task, including contributor redaction and existing unchanged endpoint/auth/status envelope. Keep names as inert provider text, including null; names do not establish identity.

    Decode nullable Size on jellyfinMediaSource and nullable Chapters of nullable Name/StartPositionTicks on jellyfinEpisodeItem. Extend the existing resolved source with FileSizeBytes and validated chapter hints; use only the selected source's Size/runtime and the unique own-path proof above, per QD-03. Missing trustworthy raw runtime withholds nonempty hints rather than inventing an upper bound. Keep the global source resolver's existing selection/track semantics intact; compute chapter eligibility separately from source eligibility so valid B size can survive unavailable chapters. Extend the existing batch reader with an internal optional field-selection path, keeping existing wrappers/default Fields unchanged. Only the editor's one-item caller opts into Chapters; do not create another HTTP transport or add Chapters to the shared series-browse constant, per QD-06. Add failing behavioral tests first, then implement and make them pass.
  </action>
  <verify><automated>docker exec -w /app team4sv30-backend go test ./internal/handlers -run 'Test.*(JellyfinMediaSource|JellyfinSourceBatch|Chapter)' -count=1 -timeout=60s</automated></verify>
  <done>Documented selected_file/chapter_hints contracts exist; source A/B and null/empty/timestamp tests pass; chosen-source size maps without widening provider browsing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Populate one authorized selected-file context for both callers</name>
  <files>backend/internal/handlers/admin_content_episode_version_editor_helpers.go, backend/internal/handlers/admin_content_episode_version_editor_scan.go, backend/internal/handlers/admin_content_episode_version_editor_context_test.go, frontend/src/types/__tests__/episode-version-contract.test.ts, frontend/src/lib/api.episode-versions.test.ts</files>
  <read_first>
    backend/internal/handlers/admin_content_episode_version_editor.go
    backend/internal/handlers/admin_content_episode_version_editor_helpers.go
    backend/internal/handlers/admin_content_episode_version_editor_scan.go
    backend/internal/handlers/admin_content_episode_version_editor_context_test.go
    backend/internal/handlers/episode_version_source_hydration.go
    backend/internal/models/jellyfin_source_contract_test.go
    frontend/src/lib/api.ts
    frontend/src/lib/api.episode-versions.test.ts
    frontend/src/lib/api.auth-refresh.test.ts
    frontend/src/types/__tests__/episode-version-contract.test.ts
    docs/frontend/auth-api-client.md
  </read_first>
  <behavior>
    - Known and missing saved duration both use at most one exact-item GET for selected-file metadata; missing duration is filled from that same selected source.
    - Both authorized admin and contributor GET /api/v1/admin/episode-versions/{versionId}/editor-context return safe chapter hints and chosen-source size.
    - Contributor serialized selected_file has empty required path/media_item_id, no media_source_id/stream_url, and only safe display metadata/hints; existing safeVersion redactions stay in place.
    - 401/403/5xx/timeout/network errors, exact-ID mismatch and lost binding leave ordinary context usable, with selected_file unavailable and degraded true for attempted failures.
    - Ownership mismatch cannot expose metadata. No provider configuration means unavailable metadata without an attempted-failure flag.
    - Size can map in existing scan results from already loaded sources with zero extra GETs and no chapter request.
    - Missing/expired access token with valid refresh reaches the unchanged central editor API helper; response null/empty hints remain distinct, and denied callers retain existing authorization.
  </behavior>
  <action>
    Add a focused shared enrichment function in admin_content_episode_version_editor_helpers.go which consumes the already loaded version, anime source and existing resolved folder/series information, per QD-04/05/06. Reuse the selected source binding and resolveReviewedJellyfinSource; require an actual saved Jellyfin item and verifiable anime/series/folder ownership before exposing metadata. Do not call the full admin context from the contributor branch, reload version/anime, query source bindings, access the filesystem, or infer file identity from a title. Replace the admin duration-only enrichment call with one exact-file read carrying duration/size/chapters; contributor enrichment uses its already loaded unsanitized version internally and serializes through a separate explicit allowlist. The admin selected_file may reuse the existing admin scan projection; contributor required strings path/media_item_id are empty and provider selector/stream properties are omitted. Include only basename display metadata, positive file_size_bytes and chapter_hints for contributors. Expand the existing degraded flag documentation/behavior to cover failed optional selected-file reads without making the editor fail. Retain real persistence errors and endpoint permission behavior.

    Set selected_file.FileSizeBytes from the resolved source; map size also in buildEpisodeVersionMediaFiles without fetching again, per QD-03/06. Share a small projection helper only if it removes actual duplicate mapping, keeping contributor redaction explicit. Tests must compare A/B chapter arrays and bytes, use distinct admin/contributor actors, assert no additional repository/SQL work in the enrichment and count exact-item versus existing series requests. Use isolated integration fixtures, never live row changes. Extend frontend contract and real API-helper tests for the new fields and refresh-only sessions; the browser helper's transport implementation needs no new fetch or token logic.
  </action>
  <verify><automated>docker exec -e TEAM4S_PHASE117_TEST_DSN -w /app team4sv30-backend go test ./internal/handlers -run 'Test.*(EpisodeVersionEditorContext|EpisodeVersionContributorContext|EpisodeVersionMediaFiles)' -count=1 -timeout=60s &amp;&amp; docker exec -w /app team4sv30-frontend npm test -- src/types/__tests__/episode-version-contract.test.ts src/lib/api.episode-versions.test.ts src/lib/api.auth-refresh.test.ts</automated></verify>
  <done>Both authorized contexts expose proven hints without contributor secrets; selected source Size reaches the existing field; failures degrade; zero new SQL and stated provider-request budgets are proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire current-file chapter choices into the existing segment form</name>
  <files>frontend/src/app/admin/episode-versions/[versionId]/edit/useEpisodeVersionEditor.ts, frontend/src/app/admin/episode-versions/[versionId]/edit/useEpisodeVersionEditor.test.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditorPage.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentBasicFieldsSection.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.helpers.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.module.css, frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.test.tsx</files>
  <read_first>
    frontend/src/app/admin/episode-versions/[versionId]/edit/useEpisodeVersionEditor.ts
    frontend/src/app/admin/episode-versions/[versionId]/edit/useEpisodeVersionEditor.test.tsx
    frontend/src/app/admin/episode-versions/[versionId]/edit/episodeVersionEditorUtils.ts
    frontend/src/app/admin/episode-versions/[versionId]/edit/episodeVersionEditorUtils.test.ts
    frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditorPage.tsx
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentBasicFieldsSection.tsx
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.helpers.tsx
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.formHelpers.ts
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.module.css
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.test.tsx
    frontend/src/components/ui/Select.tsx
    docs/frontend/ui-system.md
    docs/agent-guidelines-ui.md
  </read_first>
  <behavior>
    - Initial selectedFile prefers real context.selected_file; missing enrichment retains buildFallbackMediaFile. Size displays through existing formatBytes.
    - Chapter choices use saved context independently of scanned/draft selectedFile and work with contributor-redacted provider identifiers.
    - Chapter labels show 00:21:38.047 and 00:22:58.043; selection writes 00:21:38 and 00:22:58 respectively. A .500 case rounds upward; zero remains selectable.
    - Changing one choice changes only that time field. Name/type/episode range/source remain unchanged; no create/update/render/upload call occurs until explicit existing Save.
    - Manual times, invalid range/order and 240-second/runtime validation continue to work. Null and [] render their documented different unavailable/empty states.
    - Unsaved applyFile, source-only selection, advanced provider/item/stream changes and route/context races invalidate hints immediately. A saved relink cannot revive old context hints; they remain unavailable until fresh context is loaded.
    - Only new current-file creation with sourceType none and no pending upload may use hints. Existing segments, shared base times/per-release overrides, library/theme sources and uploaded assets receive no current-file choices.
    - Changing the persisted variant while a drawer is open closes/resets that drawer so an old form cannot silently become another file's form.
  </behavior>
  <action>
    In useEpisodeVersionEditor prefer nextContext.selected_file over buildFallbackMediaFile at load, per QD-04. Expose chapter hints derived from the saved context only while its loaded route matches and no binding draft differs; use existing generation/revision state and compare provider/item/stream draft values, plus hasPendingFileSelection for source-only relinks. Clear/invalidate saved selected_file after an actual binding change and keep it invalid through failed/late saves until a fresh context arrives; metadata-only saves must not add provider reads. Do not let scans replace the authoritative chapter source. Pass this guarded nullable list through EpisodeVersionEditorPage, SegmenteTab and SegmentEditPanel to SegmentBasicFieldsSection; keep props optional for unrelated callers and use the existing context variant identity, not redacted provider fields. Reset the create drawer on variant changes. Per QD-02, gate hints to editingSegment === null, sourceType === none and no pendingUploadFile. This deliberately implements the requested creation context; do not apply current-file hints to existing shared bases, overrides or another playback origin.

    Add two global Select controls directly within the existing Start/End field groups, per QD-01/02. Their labels are "Kapitel als Start" and "Kapitel als Ende", with an unselected "Kapitel auswählen" option. Options contain the exact millisecond clock plus original chapter name (fallback "Kapitel N" for null/blank name). Extend the existing formatTimeInput helper with an optional millisecond display mode preserving its default callers; avoid a second time formatter. On explicit selection use Math.round(start_ms / 1000), then existing whole-second formatTimeInput and onFormChange for that field only. Show "Kapitelzeiten werden bei der Übernahme auf die nächste ganze Sekunde gerundet." next to the controls, meaning nearest second; prefer the unambiguous copy "Bei der Übernahme wird auf ganze Sekunden gerundet (z. B. 21:38.047 → 21:38)." Preserve existing runtime clamp/order/window validation and make any clamp visible. Do not silently choose the next chapter as end or classify titles. Missing/unsafe hints show a compact "Für diese Datei sind keine verlässlichen Kapitelzeiten verfügbar."; confirmed [] shows "Diese Datei enthält keine Kapitel."; excluded editing/playback contexts simply omit this creation-only aid.

    UI contract: keep all existing section order, panel width, tokens, typography, time inputs and explicit Save. Each chapter Select sits below its own time input, has an accessible label, and is constrained to its field width with min-width: 0. Stack field groups at narrow available panel widths; only preserve two columns where both controls and gap fit. Use a local inline-size container if a new responsive transition is required; no viewport assumption for the embedded drawer. Long German/chapter names must not widen the page. Verify 390x844, 768x1024 and 1440x900 plus an embedded narrow drawer and zoom. Add tests first in existing hook/segment suites; verify manual Save only through mocks or isolated fixtures, never by saving a test segment into application rows.
  </action>
  <verify><automated>docker exec -w /app team4sv30-frontend npm test -- 'src/app/admin/episode-versions/[versionId]/edit/useEpisodeVersionEditor.test.tsx' 'src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.test.tsx' 'src/app/admin/episode-versions/[versionId]/edit/episodeVersionEditorUtils.test.ts' src/lib/api.no-token-boundary.test.ts &amp;&amp; docker exec -w /app team4sv30-frontend npm run typecheck &amp;&amp; git diff --check</automated></verify>
  <done>Authorized users can deliberately choose exact-labeled chapter hints for new current-file segments, see selected-file size, and continue manual editing safely; race, source, rounding and no-auto-save tests pass with width-safe UI evidence.</done>
</task>

</tasks>

<dependency_graph>
All tasks are sequential within this single quick plan: Task 1 creates provider/DTO contracts; Task 2 needs those contracts and produces authorized editor responses; Task 3 consumes those responses and wires existing controls. No checkpoints. Do not edit application files concurrently with another implementation agent; preserve other agents' Phase-161 documentation.
</dependency_graph>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jellyfin response to chosen persisted variant | Upstream item/paths/chapters must establish the actual selected source |
| Authorized handler to contributor browser | Display metadata cannot expose administrative provider paths, selectors or URLs |
| Saved context to mutable browser form | Route changes, relinks and other playback origins invalidate chapter applicability |
| Auth session to editor API | Valid refresh session must survive absent/expired access token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-Q01 | T | Source resolver | mitigate | Exact requested item plus existing anime/folder guard; unique own-path chapter proof; selected B size independent of A chapters |
| T-Q02 | I | Contributor context | mitigate | Explicit allowlist, empty required provider strings, omit source selector/URL; assert serialized response contains no fixture secrets/private paths |
| T-Q03 | T | Editor hook/panel | mitigate | Existing generation and binding revision guards, clear stale hints after relink, creation-only current file, no shared-origin assumptions |
| T-Q04 | D | Provider enrichment | mitigate | At most one exact-file GET, no per-chapter requests, 256 chapter cap, existing timeout/transport and degraded response |
| T-Q05 | S/E | Protected editor | mitigate | Existing backend permissions and central refresh client; refresh-only UI/API regression and denied-caller test |
| T-Q06 | T | Chapter labels/selection | mitigate | Inert text and nullable positions; raw tick bounds; explicit nearest-second adoption, no inferred names/types or automatic writes |
</threat_model>

<source_audit>
| Source | ID | Scope item | Coverage |
|--------|----|------------|----------|
| GOAL | Quick user request | Chapter time assistance while creating segments | Tasks 1–3 |
| REQ | QUICK-CHAPTER-HINTS | Exact display, explicit adoption, manual save, both callers | Tasks 1–3 |
| REQ | QUICK-SELECTED-FILE-SIZE | Selected source bytes in existing display | Tasks 1–3 |
| RESEARCH | Source guard | Item chapters are not alternate-source chapters; unique path, exact ID/ownership | Tasks 1–2 |
| RESEARCH | Null/precision | Preserve milliseconds, valid zero, invalid bounds, explicit empty, bounded/sorted hints | Tasks 1 and 3 |
| RESEARCH | Reuse/budgets | Existing context/duration/source resolver, zero SQL, one file GET, no scan extra GET | Tasks 1–2 |
| RESEARCH | Failure/races/auth | Degraded ordinary editing, invalidation, sanitized contributor, refresh seam | Tasks 2–3 |
| CONTEXT | QD-01–QD-06 | Working decisions recorded in this plan; no quick CONTEXT.md supplied | Tasks reference QD IDs |
| Exclusion | Phase 161 / broader precision | Import/relink/rescan implementation, persisted millisecond/render/cache redesign, migrations and live data writes | Outside the explicitly assigned follow-up |
</source_audit>

<verification>
Run all commands in /home/d1sk/team4s on team4s-linux; inspect git status --short and docker compose ps before editing. Node/Go commands run in existing Compose containers. No local dependency installations or runtime resets.

Prepare TEAM4S_PHASE117_TEST_DSN only in memory using the established Phase-161 isolated test-database setup from 161-09-PLAN.md (team4s_phase117_test_161, unique guarded schemas). Never print/store its credentials. Required DB tests must run without skips. If a focused suite exceeds a minute, split its test filter rather than suppressing coverage.

Run each task's tests, then focused ESLint for the changed TS/TSX files, typecheck, Go build/vet for changed packages, the relevant broader tests and frontend build where feasible. Record pre-existing global failures separately using docs/audits/2026-09-15-jellyfin12/frontend-final-checks.json and backend-final-checks.json; do not claim a globally clean baseline. Review the scoped diff and git diff --check. No migrations, new repository queries or token-bearing component fetches.

Live in-app browser evidence: navigate from the user-visible admin/fansub release workspace to the current editor and Segmente, then Neues Segment hinzufügen. Exercise chapter Start/End choices without Save, observe precise labels, rounding note and existing size; close/cancel. Repeat for an authorized contributor where its existing navigation exposes Segmente. The exact current editor link is http://127.0.0.1:3300/admin/episode-versions/28/edit; use the existing shell/workspace navigation as the discoverability check rather than treating this direct link as proof. Test widths 390/768/1440, keyboard labels, zoom and no document overflow. Record any unavailable session as a concrete verification limitation, not an authorization redesign.
</verification>

<success_criteria>
All five must-have truths are evidenced; the 256 cap/null semantics and ownership boundary match backend/frontend/contracts; no chapter fetch on scans or public browsing; no automatic mutations; existing title and episode assignment semantics remain intact; only scoped file edits are included.
</success_criteria>

<output>
After implementation, create .planning/quick/260915-m2h-kapitelhilfe-und-dateigroesse-im-release/260915-m2h-SUMMARY.md with changed files, executed checks, browser evidence, measured request/SQL budgets and remaining pre-existing failures. Planning itself creates only this PLAN.md and performs no commit, STATE or ROADMAP edit.
</output>

## Execution amendments (2026-09-15)

- Consumer review during live verification identified the member workspace as a direct `SegmenteTab` caller. Its existing selected-file context is forwarded without a new request; focused RED/GREEN coverage accompanies the one-line wiring change.
- Live 390px inspection found a 723px document width even with the drawer closed. Bound the existing editor grid tracks/children and give the existing tab strip local scrolling. This is a narrow responsive prerequisite, not a structural redesign or global overflow-hiding solution.
- The existing focused episode-versions DSL is aligned with the additive chapter/size contract. No persisted model or database schema changes.
- Admin live checks and member-workspace surface checks passed; distinct contributor live UAT, browser zoom and normal dashboard discoverability remain explicitly limited as recorded in the summary. The optional precision question was unanswered; use the announced whole-second adoption default and preserve exact labels.
