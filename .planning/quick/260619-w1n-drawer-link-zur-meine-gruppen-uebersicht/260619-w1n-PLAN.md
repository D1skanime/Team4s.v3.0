---
phase: quick-260619-w1n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/components/layout/AppShell.test.tsx
autonomous: true
requirements: [W1N]
must_haves:
  truths:
    - "Eingeloggte Mitglieder mit mindestens einer Gruppen-Mitgliedschaft sehen im Drawer einen Link 'Gruppen-Übersicht' unter 'Mein Bereich'"
    - "Der Link führt auf /manage/groups"
    - "Mitglieder ohne Memberships sehen den Link nicht"
    - "Die bestehenden per-Gruppe-Edit-Links (/admin/fansubs/[id]/edit) im 'Meine Gruppen'-Abschnitt bleiben unverändert erhalten"
    - "Der Übersichts-Link wird korrekt als aktiv markiert wenn currentPath=/manage/groups"
  artifacts:
    - path: frontend/src/components/layout/AppShell.tsx
      provides: "Drawer-Nav mit Gruppen-Übersicht-Link in myItems"
    - path: frontend/src/components/layout/AppShell.test.tsx
      provides: "Test der neuen Link-Sichtbarkeit und des Href-Werts"
  key_links:
    - from: "AppShellNavGroups"
      to: "/manage/groups"
      via: "myItems-Eintrag mit memberships.length > 0 Bedingung"
      pattern: "manage/groups"
---

<objective>
"Gruppen-Übersicht"-Link im AppShell-Drawer unter "Mein Bereich" ergänzen, sichtbar fuer eingeloggte Nutzer mit mindestens einer Gruppen-Mitgliedschaft.

Purpose: Fansub-Leader und -Member koennen /manage/groups (user-facing Alias fuer /admin/my-groups) direkt aus dem Drawer erreichen, ohne globale Admin-Rechte zu benoetigen.
Output: AppShell.tsx mit neuem bedingtem myItems-Eintrag; AppShell.test.tsx mit Verifikationstest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gruppen-Übersicht-Link in AppShellNavGroups ergaenzen und testen</name>
  <files>
    frontend/src/components/layout/AppShell.tsx,
    frontend/src/components/layout/AppShell.test.tsx
  </files>
  <behavior>
    - Test A: Mitglied mit memberships sieht Link mit href=/manage/groups und Label "Gruppen-Übersicht"
    - Test B: Mitglied ohne memberships sieht keinen Link nach /manage/groups
    - Test C: currentPath=/manage/groups setzt aria-current="page" auf den Link
    - Test D: per-Gruppe-Edit-Link /admin/fansubs/42/edit bleibt weiterhin vorhanden (Regression)
  </behavior>
  <action>
    ZUERST den Test schreiben, dann implementieren.

    TEST (AppShell.test.tsx):
    Neuen describe-Block "Gruppen-Übersicht-Link" oder Ergaenzung im bestehenden describe-Block hinzufuegen.
    Test A: Rendere AppShell mit hasMemberProfile=true und memberships=[{fansub_group_id:5, fansub_group_name:'Test Sub', fansub_group_slug:'test-sub'}] ohne canAccessAdmin. Erwarte getByRole('link', {name:/Gruppen-Übersicht/i}) mit href=/manage/groups.
    Test B: Rendere AppShell ohne memberships (leer). Erwarte queryByRole('link', {name:/Gruppen-Übersicht/i}) === null.
    Test C: Rendere AppShell mit currentPath="/manage/groups" und memberships=[...]. Erwarte link.getAttribute('aria-current') === 'page'.
    Test D: Rendere AppShell mit memberships=[{fansub_group_id:42,...}]. Erwarte getByRole('link', {name:/Test Sub/i}).getAttribute('href') === '/admin/fansubs/42/edit'.

    DANN implementieren (AppShell.tsx, Funktion AppShellNavGroups, ~Zeile 109-112):
    In myItems bedingt einen Eintrag hinzufuegen: Wenn memberships.length > 0, fuege als letzten Eintrag in myItems hinzu:
      { label: 'Gruppen-Übersicht', href: '/manage/groups', icon: <Users size={17} />, current: isCurrent(currentPath, '/manage/groups') }

    Das Users-Icon ist bereits importiert (Zeile 17). Kein neuer Import noetig.

    Wichtig: memberships ist als Parameter in AppShellNavGroups bereits vorhanden (Zeile 93-100). Die Bedingung prueft memberships.length > 0 direkt in der myItems-Array-Konstruktion via spread oder Filter.

    Empfohlenes Muster fuer den bedingten Eintrag in myItems (kein Fenced-Code-Block, beschreibend):
    Definiere myItems als const mit den festen Eintraegen (Mein Profil/Mein Account, Meine Projekte), dann spread-appende bedingt den Gruppen-Übersicht-Eintrag: [...fixedItems, ...(memberships.length > 0 ? [uebersichtsItem] : [])].

    KEIN neues HTML-Markup erfinden. Den vorhandenen AppShellNavItemView-Renderer (Zeile 63-89) verwenden — er rendert automatisch ein Link-Element mit den richtigen CSS-Klassen aus dem NavItem-Objekt. Der neue Eintrag geht in myItems, nicht in einen separaten Block.

    Dateilaenge nach Aenderung: AppShell.tsx ist aktuell 373 Zeilen — bleibt weit unter 450.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/layout/AppShell.test.tsx --reporter=verbose 2>&amp;1 | tail -40</automated>
  </verify>
  <done>
    Alle AppShell-Tests bestehen. Link "Gruppen-Übersicht" mit href=/manage/groups erscheint fuer Mitglieder mit memberships unter "Mein Bereich". Mitglieder ohne memberships sehen ihn nicht. Per-Gruppe-Edit-Links (/admin/fansubs/[id]/edit) funktionieren weiterhin unveraendert.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-seitig | AppShell ist ein Client-Component; memberships kommen vom Backend ueber getOwnProfile() — bereits authentifiziert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-W1N-01 | Spoofing | /manage/groups Route | accept | Route selbst prueft Authentifizierung; Drawer-Link ist nur sichtbar wenn memberships vorhanden (Profile geladen = Auth vorhanden) |
| T-W1N-02 | Information Disclosure | memberships-Daten im Drawer | accept | Nur eigene Memberships werden angezeigt; Datenabruf via getOwnProfile() mit Auth-Token |
</threat_model>

<verification>
- npx vitest run src/components/layout/AppShell.test.tsx muss ohne Fehler durchlaufen
- Manuell im Dev-Browser (:3000): Als eingeloggtes Mitglied einer Gruppe den Drawer oeffnen → "Gruppen-Übersicht" unter "Mein Bereich" sehen → Klick fuehrt auf /manage/groups
- Als Mitglied ohne Gruppen: kein "Gruppen-Übersicht"-Link im Drawer
- Bestehende Gruppen-Edit-Links im "Meine Gruppen"-Abschnitt weiterhin vorhanden
</verification>

<success_criteria>
- Link "Gruppen-Übersicht" mit href=/manage/groups erscheint unter "Mein Bereich" fuer authentifizierte Nutzer mit memberships.length > 0
- Link ist unsichtbar wenn memberships leer
- Bestehende per-Gruppe-Links auf /admin/fansubs/[id]/edit bleiben unveraendert
- Alle bestehenden AppShell-Tests bestehen weiterhin (keine Regression)
- Neuer Test deckt alle vier Szenarien ab (sichtbar, unsichtbar, aktiv, Regression)
- Korrekte Umlaute: "Gruppen-Übersicht" (nicht "Gruppen-Uebersicht")
</success_criteria>

<output>
Erstelle `.planning/quick/260619-w1n-drawer-link-zur-meine-gruppen-uebersicht/260619-w1n-01-SUMMARY.md` nach Abschluss
</output>
