---
phase: 260610-f7n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.test.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css
autonomous: true
requirements:
  - migrate-members-table
  - migrate-invitations-table
must_haves:
  truths:
    - Mitglieder werden als Tabelle (Tabellenkopf + eine Zeile pro Mitglied) gerendert, nicht als Card-Stapel
    - Mitglieder-Tabelle hat Spalten: Mitglied (Avatar + Name), Rollen (Badges), Status (Badge), Seit (Datum), Aktionen
    - Einladungen werden als Tabelle gerendert (Spalten: Empfänger, Rollen, Status, Läuft ab, Aktion)
    - Leerzustand bei 0 Mitgliedern nutzt TableEmptyState, kein fansubEditMembershipEmpty-Div
    - Leerzustand bei 0 Einladungen nutzt TableEmptyState
    - SectionHeader mit Titel "Mitgliederübersicht" und Zähler-Badge ist sichtbar
    - Tabellen-Oberfläche nutzt tableSurfaceBPrevious + tableWrapHeaderLineWine (lokal in FansubEdit.module.css)
    - Aktions-Spalte bei canManageMembers: "Rollen bearbeiten"-Button öffnet Modal, "Deaktivieren"/"Reaktivieren"-Button funktioniert
    - Aktions-Spalte bei canCancelInvitation: "Einladung zurückziehen"-Button funktioniert
    - Kopfbereich, beide Modals, alle Handler, alle Capability-Guards sind unverändert
    - Alle Vitest-Tests in FansubAppMembersSection.test.tsx laufen durch
  artifacts:
    - path: frontend/src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.tsx
      provides: Migrierte Komponente mit Table-Pattern
    - path: frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css
      provides: tableSurfaceBPrevious + tableWrapHeaderLineWine + rowActions lokal definiert
  key_links:
    - from: FansubAppMembersSection.tsx
      to: "@/components/ui"
      via: "Table, TableHead, TableBody, TableRow, TableHeaderCell, TableCell, TableEmptyState, SectionHeader"
    - from: FansubAppMembersSection.tsx
      to: FansubEdit.module.css
      via: "styles.tableSurfaceBPrevious, styles.tableWrapHeaderLineWine"
---

<objective>
Den Collaboration-Tab-Bereich "Fansub Members" von Card-Stapeln auf das Showcase-Tabellen-Pattern migrieren.

Purpose: Konsistentes UI mit dem globalen Design-System (Showcase Section 06) für Admin-Operatoren.
Output: FansubAppMembersSection.tsx mit zwei Tabellen (Mitglieder + Einladungen), angepasste Tests, lokale CSS-Klassen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@frontend/src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.tsx
@frontend/src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.test.tsx
@frontend/src/components/ui/Table.tsx
@frontend/src/components/ui/SectionHeader.tsx
@frontend/src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.tsx

<interfaces>
<!-- Table-Primitive aus frontend/src/components/ui/Table.tsx -->
```typescript
// Varianten: 'default' | 'compact' | 'selectable' | 'withActions'
export function Table({ caption, containerClassName, variant, className, children, ...props }: TableProps)
export function TableHead(props: HTMLAttributes<HTMLTableSectionElement>)
export function TableBody(props: HTMLAttributes<HTMLTableSectionElement>)
export function TableRow(props: HTMLAttributes<HTMLTableRowElement>)
export function TableHeaderCell(props: ThHTMLAttributes<HTMLTableCellElement>)  // scope="col" default
export function TableCell(props: TdHTMLAttributes<HTMLTableCellElement>)
export function TableEmptyState({ colSpan, title, description }: TableEmptyStateProps)
// TableEmptyState rendert eine <tr><td colSpan={N}> mit title (strong) + description
```

<!-- SectionHeader aus frontend/src/components/ui/SectionHeader.tsx -->
```typescript
export function SectionHeader({ eyebrow, title, description, actions }: SectionHeaderProps)
// actions-Slot: beliebiges ReactNode, z.B. Badge variant="muted" als Zähler
```

<!-- Showcase-Pattern (page.tsx Zeilen 625-654) — verbindliche Vorlage -->
// Wrapper:
<div className={styles.tableSurfaceBPrevious}>
  <Table caption="..." variant="withActions" containerClassName={styles.tableWrapHeaderLineWine}>
    <TableHead><TableRow><TableHeaderCell>...</TableHeaderCell>...</TableRow></TableHead>
    <TableBody>
      {rows.map(row => (
        <TableRow key={row.id}>
          <TableCell>...</TableCell>
          <TableCell><div className={styles.rowActions}><Button ...>...</Button></div></TableCell>
        </TableRow>
      ))}
    </TableBody>
  </Table>
</div>

// SectionHeader-Zähler-Pattern (page.tsx Zeile 617-621):
<SectionHeader
  title="Mitgliederübersicht"
  actions={<Badge variant="muted">12 Einträge</Badge>}
/>
```

<!-- Benötigte CSS-Klassen aus page.module.css — müssen lokal in FansubEdit.module.css angelegt werden -->
// tableSurfaceBPrevious: padding:16px; border-radius:4px; border/background/shadow (Dunkel-Beige)
// tableWrapHeaderLineWine: background:transparent; border-color:transparent; box-shadow:none
// tableSurfaceBPrevious .tableWrapHeaderLineWine thead th: background:transparent; border-bottom:3px solid #82122c
// rowActions: display:flex; gap:8px; align-items:center
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS-Klassen für Tabellen-Oberfläche in FansubEdit.module.css hinzufügen</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css</files>
  <action>
Vor dem git-Commit prüfen: `git diff frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css` — falls fremde uncommitted Änderungen vorliegen, NUR die eigenen Klassen als isolierten Block anhängen, keinen fremden Stand verändern.

Folgende CSS-Klassen am Ende von FansubEdit.module.css hinzufügen (nur wenn sie dort noch nicht existieren — per grep prüfen):

```
.fansubEditTableSurface {
  padding: 16px;
  border-radius: 4px;
  border: 1px solid rgba(72, 58, 48, 0.06);
  border-top-color: rgba(255, 248, 240, 0.78);
  background-color: #ece4dc;
  background-image: linear-gradient(to bottom, #f7f1eb, #ece4dc);
  box-shadow:
    0 1px 1px rgba(42, 30, 21, 0.2),
    0 2px 3px rgba(42, 30, 21, 0.08);
}

.fansubEditTableWrapWine {
  background: transparent;
  border-color: transparent;
  box-shadow: none;
}

.fansubEditTableSurface .fansubEditTableWrapWine thead th {
  background: transparent;
  border-bottom: 3px solid #82122c;
}

.fansubEditTableRowActions {
  display: flex;
  gap: 8px;
  align-items: center;
}
```

Klassennamen sind bewusst lokal (fansubEditTable*) statt Showcase-Namen zu kopieren, um Kollisionen mit dem Showcase-Modul zu vermeiden.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Vier CSS-Klassen in FansubEdit.module.css vorhanden: fansubEditTableSurface, fansubEditTableWrapWine, fansubEditTableRowActions + Compound-Regel für thead th.</done>
</task>

<task type="auto">
  <name>Task 2: FansubAppMembersSection.tsx auf Tabellen-Pattern migrieren</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.tsx</files>
  <action>
Import-Zeile erweitern: `Table, TableHead, TableBody, TableRow, TableHeaderCell, TableCell, TableEmptyState, SectionHeader` aus `@/components/ui` hinzufügen (Badge, Button, Card, FormField, Input, Modal bleiben — Card wird noch für den äußeren Rahmen, die Modals und den Einladungs-Container genutzt).

Lucide-Importe: `Calendar`, `CheckCircle2`, `Clock` können entfernt werden (werden in der Tabellenansicht nicht mehr direkt genutzt). `Pencil` bleibt (Rollen-bearbeiten-Button). `UserPlus` bleibt (Kopfbereich).

**Mitglieder-Abschnitt** (ersetzt den Block ca. Zeile 445–538 im Original):

1. Vor der Tabelle `SectionHeader` rendern:
   ```
   title="Mitgliederübersicht"
   actions={<Badge variant="muted">{members.length} {members.length === 1 ? 'Mitglied' : 'Mitglieder'}</Badge>}
   ```
   (Den bisherigen `fansubEditBasicStatusCard`-Block im Kopfbereich NICHT entfernen — er zeigt Aktiv/Deaktiviert-Statistik und ist Teil des unveränderten Kopfbereichs.)

2. Leerzustand: Statt `fansubEditMembershipEmpty`-Div eine vollständige `<Table variant="withActions">` mit `<TableEmptyState colSpan={5} title="Noch keine Mitglieder in dieser Gruppe" description="Sobald jemand hinzugefügt wurde, erscheinen Rollen und Status hier in der Übersicht." />` rendern — kein separates Leer-Div mehr.

3. Tabellen-Wrapper und Kopf:
   ```tsx
   <div className={styles.fansubEditTableSurface}>
     <Table variant="withActions" caption="Mitglieder dieser Fansubgruppe" containerClassName={styles.fansubEditTableWrapWine}>
       <TableHead>
         <TableRow>
           <TableHeaderCell>Mitglied</TableHeaderCell>
           <TableHeaderCell>Rollen</TableHeaderCell>
           <TableHeaderCell>Status</TableHeaderCell>
           <TableHeaderCell>Seit</TableHeaderCell>
           {canManageMembers ? <TableHeaderCell>Aktionen</TableHeaderCell> : null}
         </TableRow>
       </TableHead>
       <TableBody>
         {members.length === 0
           ? <TableEmptyState colSpan={canManageMembers ? 5 : 4} title="Noch keine Mitglieder in dieser Gruppe" description="Sobald jemand hinzugefügt wurde, erscheinen Rollen und Status hier in der Übersicht." />
           : members.map((member) => { ... })
         }
       </TableBody>
     </Table>
   </div>
   ```

4. Zeilen-Inhalt pro Mitglied:
   - Spalte Mitglied: kleines Avatar-Bild (width=32 height=32) falls avatarUrl vorhanden, sonst Initialen-Kürzel (formatMemberInitials) als Span, dahinter `<strong>{fansubName}</strong>`.
   - Spalte Rollen: Badges mit getRoleClassName-Farben wie bisher; bei leeren Rollen `<span className={styles.fansubEditHint}>Keine</span>`.
   - Spalte Status: `<Badge variant={memberStatusVariant(member.status)}>{formatMemberStatusLabel(member.status)}</Badge>`.
   - Spalte Seit: `formatDateTime(member.created_at)`.
   - Spalte Aktionen (nur wenn canManageMembers):
     ```tsx
     <TableCell>
       <div className={styles.fansubEditTableRowActions}>
         <Button variant="ghost" size="sm" leftIcon={<Pencil size={14} aria-hidden="true" />}
           onClick={() => setRoleEditorMemberId((current) => (current === member.id ? null : member.id))}>
           Rollen bearbeiten
         </Button>
         <Button
           variant={member.status === 'active' ? 'ghost' : 'secondary'}
           size="sm"
           disabled={isStatusBusy}
           onClick={() => void handleToggleStatus(member)}>
           {isStatusBusy ? 'Bitte warten...' : member.status === 'active' ? 'Deaktivieren' : 'Reaktivieren'}
         </Button>
       </div>
     </TableCell>
     ```

**Einladungs-Abschnitt** (ersetzt den Card-Stapel ca. Zeile 563–617 innerhalb des `Card variant="nestedFlat"`):

Der äußere `Card variant="nestedFlat"` Rahmen mit Eyebrow „Einladungen" und Beschreibungstext bleibt bestehen. Nur der innere Inhalt ändert sich:

Statt `fansubEditMembershipEmpty`-Div und `fansubEditMembershipList` + `Card variant="compact"` pro Einladung:

```tsx
<div className={styles.fansubEditTableSurface}>
  <Table variant="withActions" caption="Offene Einladungen" containerClassName={styles.fansubEditTableWrapWine}>
    <TableHead>
      <TableRow>
        <TableHeaderCell>Empfänger</TableHeaderCell>
        <TableHeaderCell>Rollen nach Annahme</TableHeaderCell>
        <TableHeaderCell>Status</TableHeaderCell>
        <TableHeaderCell>Läuft ab</TableHeaderCell>
        {canCancelInvitation ? <TableHeaderCell>Aktion</TableHeaderCell> : null}
      </TableRow>
    </TableHead>
    <TableBody>
      {invitations.length === 0
        ? <TableEmptyState colSpan={canCancelInvitation ? 5 : 4} title="Keine offenen Einladungen" description="Neue Einladungen erscheinen hier bis zur Annahme, zum Ablauf oder zur Rücknahme." />
        : invitations.map((invitation) => {
            const invitedFansubName = invitation.member?.fansub_name?.trim()
            const invitationBusy = busyKey === `invitation:${invitation.id}`
            return (
              <TableRow key={invitation.id}>
                <TableCell>
                  <strong>{invitedFansubName || invitation.email}</strong>
                  {invitedFansubName ? <><br /><span>{invitation.email}</span></> : null}
                </TableCell>
                <TableCell>
                  <div className={styles.chipRow}>
                    {invitation.invited_role_codes.map((role) => (
                      <Badge key={`${invitation.id}-${role}`} variant="info"
                        className={styleNames(styles.fansubEditRoleBadge, getRoleClassName(role))}>
                        {formatRoleLabel(role)}
                      </Badge>
                    ))}
                  </div>
                </TableCell>
                <TableCell>
                  <Badge variant={invitationStatusVariant(invitation.status)}>
                    {formatInvitationStatusLabel(invitation.status)}
                  </Badge>
                </TableCell>
                <TableCell>{formatDateTime(invitation.expires_at)}</TableCell>
                {canCancelInvitation ? (
                  <TableCell>
                    <Button variant="ghost" size="sm" disabled={invitationBusy}
                      onClick={() => void handleCancelInvitation(invitation.id)}>
                      {invitationBusy ? 'Bitte warten...' : 'Einladung zurückziehen'}
                    </Button>
                  </TableCell>
                ) : null}
              </TableRow>
            )
          })
      }
    </TableBody>
  </Table>
</div>
```

**CSS-Klassen-Aufräumen:** Klassen, die ausschließlich von FansubAppMembersSection.tsx genutzt wurden, können entfernt werden — aber NUR nach grep-Bestätigung. Kein blindes Entfernen. Empfehlung: vorerst stehen lassen, da FansubEdit.module.css viele Komponenten teilt.

**Parallel-Writer-Disziplin:** GroupMembersTab.tsx und alle anderen Dateien im Ordner NICHT anfassen. Commits ausschließlich per Pfad: `git add frontend/src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.tsx frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run "src/app/admin/fansubs" 2>&1</automated>
  </verify>
  <done>
- Vitest-Lauf für src/app/admin/fansubs besteht ohne Fehler.
- TypeScript-Compilation ohne Fehler (tsc --noEmit).
- Mitglieder-Abschnitt rendert table-Element mit thead + tbody statt div.fansubEditMembershipList.
- Einladungs-Abschnitt rendert table-Element mit thead + tbody statt div.fansubEditMembershipList.
- SectionHeader mit Titel "Mitgliederübersicht" ist im JSX vorhanden.
  </done>
</task>

<task type="auto">
  <name>Task 3: Tests an Tabellenstruktur anpassen</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.test.tsx</files>
  <action>
Bestehende Tests prüfen, ob sie durch die Migration brechen, und falls ja, anpassen.

Die vier Tests decken ab:
1. `renders current app members with active roles` — prüft auf Text "Phase Admin", "Fansub-Lead", "Editing", "Aktiv". Diese Texte sind in der Tabellenstruktur weiterhin vorhanden — kein Anpassungsbedarf erwartet.
2. `adds a new member through candidate search` — interagiert mit Buttons/Inputs im Modal (unverändert). Kein Anpassungsbedarf erwartet.
3. `creates an invitation and shows the one-time invite link` — prüft "InviteeNick" und "invitee@example.local" als Text im DOM. In der Tabellen-Zelle sind beide weiterhin sichtbar. Kein Anpassungsbedarf erwartet.
4. `shows smtp error message on 502 invitation failure` — prüft Fehlermeldungs-Text (unverändert).

Falls nach der Migration ein Test bricht (z.B. weil ein Text nicht mehr via `getByText` gefunden wird, weil er jetzt in einer `<td>` statt einem `<strong>` direkt liegt): Selektor auf `screen.getByRole('cell', { name: /.../ })` oder `screen.getByText(...)` anpassen — nie Mock-Daten oder Mock-Logik verändern.

Nach allen Anpassungen: `cd frontend && npx vitest run "src/app/admin/fansubs"` muss grün durchlaufen.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run "src/app/admin/fansubs" 2>&1</automated>
  </verify>
  <done>Alle Tests in FansubAppMembersSection.test.tsx bestehen. Keine skipped/todo-Tests. Keine TypeScript-Fehler.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reine UI-Migration | Keine neuen Endpoints, keine neuen API-Calls, kein neuer Datenzugang |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-f7n-01 | Tampering | CSS-Klassennamen | accept | Lokale Klassennamen (fansubEditTable*) vermeiden Kollision mit Showcase-Modul; keine globalen Styles |
| T-f7n-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Pakete — ausschließlich bestehende UI-Primitives aus @/components/ui |
</threat_model>

<verification>
1. `cd frontend && npx vitest run "src/app/admin/fansubs"` — alle 4 Tests grün
2. `cd frontend && npx tsc --noEmit` — keine TypeScript-Fehler
3. Sichtprüfung auf :3000 — /admin/fansubs/[id]/edit?tab=collaboration zeigt Tabellen statt Card-Stapel
4. `git diff --name-only` enthält NUR: FansubAppMembersSection.tsx, FansubEdit.module.css, FansubAppMembersSection.test.tsx (und .planning-Artefakte) — GroupMembersTab.tsx und Migration-SQL unberührt
</verification>

<success_criteria>
- Mitglieder-Liste: HTML-Tabelle mit Spalten Mitglied / Rollen / Status / Seit / Aktionen
- Einladungs-Liste: HTML-Tabelle mit Spalten Empfänger / Rollen nach Annahme / Status / Läuft ab / Aktion
- Beide Leerzustände via TableEmptyState (kein fansubEditMembershipEmpty-Div mehr)
- SectionHeader "Mitgliederübersicht" mit Zähler-Badge vorhanden
- Alle 4 Vitest-Tests bestehen
- Kein TypeScript-Fehler
- GroupMembersTab.tsx und DB-Migrations-Dateien unverändert
</success_criteria>

<output>
Create `.planning/quick/260610-f7n-fansubappmemberssection-collaboration-ta/260610-f7n-SUMMARY.md` when done
</output>
