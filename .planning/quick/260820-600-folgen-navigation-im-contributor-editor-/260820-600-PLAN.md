---
phase: quick-260820-600
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/admin/episode-versions/[versionId]/edit/episodeNeighborNavigation.ts
  - frontend/src/app/admin/episode-versions/[versionId]/edit/episodeNeighborNavigation.test.ts
  - frontend/src/app/admin/episode-versions/[versionId]/edit/useEpisodeNeighborNavigation.ts
  - frontend/src/app/admin/episode-versions/[versionId]/edit/useEpisodeNeighborNavigation.test.ts
  - frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeNavigationControls.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeNavigationControls.test.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditorPage.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditor.module.css
  - frontend/src/app/admin/episode-versions/[versionId]/edit/page.test.tsx
autonomous: false
requirements: [PHASE-117-EPISODE-NAV]
must_haves:
  truths:
    - "Im Contributor-Editor-Header sieht der Admin/Contributor Buttons 'Vorherige Folge' und 'Naechste Folge', wenn eine Nachbar-Folge mit passender Gruppe+Version existiert"
    - "Klick auf 'Naechste Folge' navigiert direkt zur release_version_id der naechsten Folge derselben Gruppe+Version und behaelt den aktuell aktiven Tab bei"
    - "Auf der ersten Folge ist 'Vorherige Folge' nicht aktiv/nicht sichtbar, auf der letzten Folge ist 'Naechste Folge' nicht aktiv/nicht sichtbar"
    - "Fehlt fuer die Nachbar-Folge eine Version mit gleicher Gruppe+Version, wird in diese Richtung nicht navigiert (Button disabled/ausgeblendet statt kaputtem Link)"
  artifacts:
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/episodeNeighborNavigation.ts"
      provides: "Reine Resolutionslogik: aktuelle Episoden-Position, Nachbar-Episode, passende Zielversion pro Gruppe+Version"
      exports: ["resolveCurrentEpisodeIndex", "findMatchingVersionForEpisode", "computeNeighborNavigation"]
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/useEpisodeNeighborNavigation.ts"
      provides: "React-Hook, laedt GroupedEpisodes fuer den Anime und liefert prevTarget/nextTarget/currentPosition"
      exports: ["useEpisodeNeighborNavigation"]
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeNavigationControls.tsx"
      provides: "UI-Komponente mit den zwei Button-Primitives + Positionsanzeige, mobile-first"
      exports: ["EpisodeNavigationControls"]
  key_links:
    - from: "EpisodeVersionEditorPage.tsx"
      to: "useEpisodeNeighborNavigation.ts"
      via: "Hook-Aufruf mit animeId/versionId/groupId/releaseVersion"
      pattern: "useEpisodeNeighborNavigation\\("
    - from: "EpisodeNavigationControls.tsx"
      to: "/admin/episode-versions/{versionId}/edit"
      via: "router.push mit ?tab= Query-Param"
      pattern: "router\\.push\\(`/admin/episode-versions/"
    - from: "EpisodeVersionEditorPage.tsx tab buttons"
      to: "URL query param 'tab'"
      via: "handleTabChange synct activeTab in die URL"
      pattern: "handleTabChange"
---

<objective>
Phase-117-Feature: Im Contributor-Editor (`EpisodeVersionEditorPage.tsx`) zwei Navigations-Buttons einbauen, mit denen Contributor/Admin direkt zur Vorherigen/Naechsten Folge desselben Anime + derselben Fansub-Gruppe + derselben Release-Version springen koennen, ohne den Umweg ueber die Folgen-Liste. Der aktive Tab (Segmente/Media/Notizen) bleibt beim Sprung erhalten.

Purpose: Contributor bearbeiten typischerweise ein Segment/Media/Notizen-Muster ueber viele Folgen hinweg identisch (z. B. Karaoke-Timing pro Folge). Der Rueckweg zur Folgen-Liste bei jeder Folge kostet unnoetige Klicks und Kontext-Wechsel.

Output:
- Reine, testbare Resolutionslogik fuer Nachbar-Episode + passende Zielversion (`episodeNeighborNavigation.ts`)
- React-Hook, der `getGroupedEpisodes` laedt und die Resolutionslogik anwendet (`useEpisodeNeighborNavigation.ts`)
- UI-Komponente mit den zwei Buttons (@/components/ui Button) + Positionsanzeige (`EpisodeNavigationControls.tsx`)
- Wiring in `EpisodeVersionEditorPage.tsx`, inkl. URL-getriebenem `tab`-Query-Param, damit der aktive Tab beim Sprung erhalten bleibt
- Tests fuer Resolutionslogik, Hook und Komponente
- Live-UAT-Checkpoint fuer den User

**Hinweis zum 450-Zeilen-Limit:** `EpisodeVersionEditorPage.tsx` hat bereits vor diesem Plan 944 Zeilen (Pre-Existing-Verstoss, ausserhalb des Scopes dieser Quick-Task). Um das Limit nicht weiter zu verschaerfen, lebt die gesamte neue Fach-/Ladelogik in den drei neuen, eigenstaendigen Dateien (`episodeNeighborNavigation.ts`, `useEpisodeNeighborNavigation.ts`, `EpisodeNavigationControls.tsx`). In `EpisodeVersionEditorPage.tsx` selbst landet nur minimaler Wiring-Code (Hook-Aufruf, Komponenten-Render, Umbenennung der bestehenden Tab-Click-Handler auf eine neue `handleTabChange`-Funktion). Ein vollstaendiger Refactor der Datei auf <450 Zeilen ist NICHT Teil dieses Plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditorPage.tsx
@frontend/src/app/admin/episode-versions/[versionId]/edit/useEpisodeVersionEditor.ts
@frontend/src/types/episodeVersion.ts
@frontend/src/lib/api.ts
</context>

<interfaces>
Types/exports the executor needs directly, no exploration needed.

From frontend/src/types/episodeVersion.ts (relevant excerpt):
- `export interface EpisodeVersion { id: number; anime_id: number; episode_number: number; release_version?: string | null; fansub_groups?: FansubGroupSummary[]; ... }`
- `export interface GroupedEpisode { episode_number: number; episode_title?: string | null; default_version_id?: number | null; version_count: number; versions: EpisodeVersion[] }`
- `export interface GroupedEpisodesPayload { anime_id: number; episodes: GroupedEpisode[] }`
- `export interface GroupedEpisodesResponse { data: GroupedEpisodesPayload }`

From frontend/src/types/fansub.ts (relevant excerpt):
- `export interface FansubGroupSummary { id: number; slug: string; name: string; ... }`

From frontend/src/lib/api.ts:
- `export async function getGroupedEpisodes(animeID: number): Promise<GroupedEpisodesResponse>` — throws `ApiError` on non-OK response. Episodes are already returned in `episode_number` ascending order by the backend, but the resolution logic should defensively sort by `episode_number` ascending since prev/next depends on strict order.

From frontend/src/components/ui/Button.tsx:
- `type ButtonVariant = 'primary' | 'secondary' | 'ghost' | 'subtle' | 'danger' | 'success'`
- `type ButtonSize = 'sm' | 'md' | 'lg'`
- Props: `variant`, `size`, `loading`, `fullWidth`, `iconOnly`, `leftIcon`, `rightIcon`, `className`, `disabled`, `children`, plus either native `onClick`/`type="button"` props or `href` (anchor variant).
- Use `<Button type="button" variant="secondary" disabled={...} onClick={...}>...</Button>` for both nav buttons — navigation must go through `router.push` (not a plain anchor) because the target `?tab=` query string is built at click time from current component state.

From EpisodeVersionEditorPage.tsx (existing, already computed — reuse, do not recompute):
- `const segmentAnimeId = editor.contextData?.version.anime_id ?? null;`
- `const selectedGroups = editor.selectedGroups;` (type `FansubGroupSummary[]`)
- `const primaryGroup = selectedGroups[0] ?? null;`
- `const segmentGroupId = primaryGroup?.id ?? null;`
- `const segmentVersion: string | null = editor.contextData?.version.release_version?.trim() || "v1";`
- `const episodeNumber = version?.episode_number ?? null;`
- `editor.contextData?.version.id` is the current `release_version_id` used in the route `/admin/episode-versions/{versionId}/edit`.

Tab state (existing, to be adapted in Task 3):
- `type ActiveTab = "uebersicht" | "dateien" | "informationen" | "segmente" | "media" | "changelog" | "notizen";`
- `const ACTIVE_TABS: ActiveTab[] = [...]` (already exists, module-level constant)
- `const tabFromQuery = searchParams.get("tab");`
- `const [activeTab, setActiveTab] = useState<ActiveTab>(() => parseActiveTab(tabFromQuery));`
- `visibleActiveTab` clamps `activeTab` to `allowedTabs` for rendering.
- Every tab button currently calls `onClick={() => setActiveTab("segmente")}` etc. (7 occurrences across the admin tab group and the contributor tab group). These must be swapped for a new `handleTabChange("segmente")` call in Task 3 so the URL stays in sync with the active tab.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reine Nachbar-Episode-Resolutionslogik (TDD)</name>
  <files>frontend/src/app/admin/episode-versions/[versionId]/edit/episodeNeighborNavigation.ts, frontend/src/app/admin/episode-versions/[versionId]/edit/episodeNeighborNavigation.test.ts</files>
  <behavior>
    - `resolveCurrentEpisodeIndex(episodes: GroupedEpisode[], currentVersionId: number): number` — findet den Index der Episode, deren `versions[]` eine Version mit `id === currentVersionId` enthaelt. Gibt `-1` zurueck, wenn keine Episode passt.
    - `findMatchingVersionForEpisode(episode: GroupedEpisode, groupId: number | null, releaseVersion: string): EpisodeVersion | null` — normalisiert `releaseVersion` (`trim() || 'v1'`) und vergleicht gegen jede `version.release_version` (ebenso normalisiert). Bei `groupId != null` muss zusaetzlich `version.fansub_groups?.some(g => g.id === groupId)` zutreffen. Bei `groupId == null` reicht die Versions-Uebereinstimmung. Gibt `null` zurueck, wenn keine Version passt.
    - `computeNeighborNavigation(input: { episodes: GroupedEpisode[]; currentVersionId: number; groupId: number | null; releaseVersion: string }): { currentIndex: number; totalCount: number; prevVersionId: number | null; prevEpisodeNumber: number | null; nextVersionId: number | null; nextEpisodeNumber: number | null }` — sortiert `episodes` defensiv nach `episode_number` aufsteigend, ruft die beiden Funktionen oben auf, liefert fuer die Vorgaenger-/Nachfolger-Episode (falls vorhanden) die passende Zielversion via `findMatchingVersionForEpisode`. Ist die aktuelle Episode nicht auffindbar (`currentIndex === -1`), sind `prevVersionId`/`nextVersionId` beide `null` und `currentIndex` bleibt `-1`.
    - Testfaelle (Vitest, kein DOM noetig): (1) mittlere Folge mit passenden Nachbarn in beide Richtungen liefert korrekte `prevVersionId`/`nextVersionId`; (2) erste Folge liefert `prevVersionId === null`, `nextVersionId` gesetzt; (3) letzte Folge liefert `nextVersionId === null`; (4) Nachbar-Folge existiert, aber keine Version mit passender Gruppe+Version -> `null` fuer diese Richtung, andere Richtung bleibt unberuehrt; (5) `groupId == null` matched nur ueber `releaseVersion`; (6) `currentVersionId` nicht in `episodes` gefunden -> `currentIndex === -1`, beide Targets `null`; (7) `releaseVersion` leer/whitespace normalisiert auf `'v1'` und matched entsprechend.
  </behavior>
  <action>
    Neue Datei `episodeNeighborNavigation.ts` mit den drei oben spezifizierten reinen Funktionen (kein React, kein Fetch, keine Seiteneffekte). Importiere `EpisodeVersion`, `GroupedEpisode` aus `@/types/episodeVersion`. Schreibe zuerst die Tests in `episodeNeighborNavigation.test.ts` gemaess der Behavior-Liste (RED), dann die Implementierung (GREEN). Nutze fuer die Normalisierung eine kleine Helper-Funktion `normalizeVersion(value: string | null | undefined): string` die `value?.trim() || 'v1'` zurueckgibt (kann exportiert werden, falls Task 2 sie ebenfalls braucht).
  </action>
  <verify>
    <automated>cd /home/d1sk/team4s/frontend && npx vitest run "src/app/admin/episode-versions/[versionId]/edit/episodeNeighborNavigation.test.ts"</automated>
  </verify>
  <done>Alle 7 Testfaelle gruen, Implementierungsdatei exportiert `resolveCurrentEpisodeIndex`, `findMatchingVersionForEpisode`, `computeNeighborNavigation` ohne React-Importe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hook + UI-Komponente fuer die Navigations-Buttons</name>
  <files>frontend/src/app/admin/episode-versions/[versionId]/edit/useEpisodeNeighborNavigation.ts, frontend/src/app/admin/episode-versions/[versionId]/edit/useEpisodeNeighborNavigation.test.ts, frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeNavigationControls.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeNavigationControls.test.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditor.module.css</files>
  <behavior>
    Hook `useEpisodeNeighborNavigation(params: { animeId: number | null; currentVersionId: number | null; groupId: number | null; releaseVersion: string })`:
    - Test 1: `animeId == null` oder `currentVersionId == null` -> ruft `getGroupedEpisodes` nicht auf, gibt `{ isLoading: false, error: null, currentIndex: -1, totalCount: 0, prevVersionId: null, prevEpisodeNumber: null, nextVersionId: null, nextEpisodeNumber: null }` zurueck.
    - Test 2: waehrend des Ladens ist `isLoading === true`, alle Targets `null`.
    - Test 3: erfolgreiche Response -> `isLoading === false`, Ergebnis von `computeNeighborNavigation` wird durchgereicht.
    - Test 4: `getGroupedEpisodes` wirft -> `error` gesetzt (String), `isLoading === false`, Targets `null`.
    - Test 5: Re-Fetch bei Aenderung von `animeId` oder `currentVersionId` (Cleanup/Cancel-Flag analog zum bestehenden `useEffect`-Pattern in `EpisodeVersionEditorPage.tsx`, dort als `cancelled`-Variable sichtbar).

    Component `EpisodeNavigationControls`:
    - Props: `{ prevVersionId: number | null; prevEpisodeNumber: number | null; nextVersionId: number | null; nextEpisodeNumber: number | null; currentIndex: number; totalCount: number; isLoading: boolean; activeTab: string }`.
    - Test 1: `isLoading === true` -> beide Buttons `disabled`, kein Klick loest Navigation aus.
    - Test 2: `prevVersionId === null` -> "Vorherige Folge"-Button ist `disabled` (bleibt im DOM sichtbar, damit das Layout stabil bleibt).
    - Test 3: `nextVersionId === null` -> "Naechste Folge"-Button `disabled`.
    - Test 4: Klick auf "Naechste Folge" (wenn `nextVersionId` gesetzt und nicht `isLoading`) navigiert zu `/admin/episode-versions/{nextVersionId}/edit?tab={activeTab}` (via `useRouter().push`, gemockt in Test). Analog fuer "Vorherige Folge" mit `prevVersionId`.
    - Test 5: `currentIndex >= 0 && totalCount > 0` -> Positionstext "Folge {currentIndex + 1} von {totalCount}" wird gerendert; bei `currentIndex === -1` wird kein Positionstext gerendert.
  </behavior>
  <action>
    `useEpisodeNeighborNavigation.ts`: React-Hook nach dem Muster des bestehenden `useEffect`+`cancelled`-Patterns in `EpisodeVersionEditorPage.tsx` (Zeilen ca. 106-132). Importiert `getGroupedEpisodes` aus `@/lib/api` und die drei Funktionen aus `./episodeNeighborNavigation`. State: `isLoading`, `error`, sowie das `computeNeighborNavigation`-Ergebnis. Bei fehlendem `animeId`/`currentVersionId` sofort `isLoading=false` ohne Fetch-Aufruf.

    `EpisodeNavigationControls.tsx`: Client-Komponente (`"use client"`), importiert `Button` aus `@/components/ui/Button` und `useRouter` aus `next/navigation`. Rendert einen Flex-Container (CSS-Modul-Klasse `styles.episodeNavBar`) mit zwei `Button`-Elementen (`variant="secondary"`, `type="button"`) — Label "← Vorherige Folge" und "Nächste Folge →" (korrekte Umlaute, Unicode-Pfeilzeichen `←`/`→`, kein Icon-Font noetig). Bei Klick ruft die Komponente `router.push` mit dem Ziel-Pfad `/admin/episode-versions/{targetVersionId}/edit?tab={activeTab}` auf. Buttons sind `disabled`, wenn `isLoading` ODER die jeweilige `*VersionId` `null` ist. Zwischen den Buttons wird die Positionsanzeige "Folge X von Y" als `<span className={styles.episodeNavPosition}>` gerendert (nur wenn `currentIndex >= 0 && totalCount > 0`).

    CSS: Neue Klasse `.episodeNavBar` in `EpisodeVersionEditor.module.css` (nahe der bestehenden `.actionBar`-Definition, gleiche Konventionen): `display: flex; flex-wrap: wrap; align-items: center; gap: 12px; padding: 4px 0;` sowie `.episodeNavPosition` fuer den Positionstext (`font-size: 14px; color: #6b6b70;`). Im bestehenden `@media (max-width: 768px)`-Block ergaenzen: `.episodeNavBar { flex-direction: column; align-items: stretch; }` — Buttons stapeln auf schmaler Breite, kein horizontales Ueberlaufen (kein `overflow-x`, keine feste `min-width` groesser als der Viewport).

    Schreibe zuerst die Tests (RED) fuer Hook und Komponente, dann die Implementierung (GREEN). Fuer Router-Mocking in Komponenten-Tests: `vi.mock("next/navigation", () => ({ useRouter: () => ({ push: pushMock }) }))` lokal im neuen Testfile (analog zu bestehenden `next/navigation`-Mocks in `page.test.tsx`, ohne diese bestehende Datei zu beeinflussen — dort wird aktuell nur `useSearchParams` gemockt).
  </action>
  <verify>
    <automated>cd /home/d1sk/team4s/frontend && npx vitest run "src/app/admin/episode-versions/[versionId]/edit/useEpisodeNeighborNavigation.test.ts" "src/app/admin/episode-versions/[versionId]/edit/EpisodeNavigationControls.test.tsx"</automated>
  </verify>
  <done>Hook- und Komponenten-Tests gruen. Buttons zeigen korrekte deutsche Labels mit Umlauten, sind disabled statt kaputt bei fehlendem Nachbar-Ziel, mobile CSS-Klasse existiert und stapelt im 768px-Breakpoint.</done>
</task>

<task type="auto">
  <name>Task 3: Wiring in EpisodeVersionEditorPage.tsx, URL-getriebener Tab-State, Docker-Restart</name>
  <files>frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditorPage.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/page.test.tsx</files>
  <action>
    In `EpisodeVersionEditorPage.tsx`:
    1. Importiere `useRouter`, `usePathname` zusaetzlich zu `useSearchParams` aus `next/navigation`; importiere `useEpisodeNeighborNavigation` aus `./useEpisodeNeighborNavigation` und `EpisodeNavigationControls` aus `./EpisodeNavigationControls`.
    2. Ergaenze `const router = useRouter();` und `const pathname = usePathname();` (falls `usePathname` in diesem Client-Component-Kontext bereits verfuegbar; sonst `/admin/episode-versions/${version?.id}/edit` als Pfad-String bauen, `version.id` ist bereits vorhanden).
    3. Ersetze alle bisherigen `onClick={() => setActiveTab("<tab>")}`-Aufrufe (7 Stellen in den Admin- und Contributor-Tab-Buttons) durch `onClick={() => handleTabChange("<tab>")}`. Definiere `handleTabChange(tab: ActiveTab)`: ruft `setActiveTab(tab)` auf UND aktualisiert die URL per `router.replace` mit den bestehenden `searchParams` als Basis, `tab`-Param auf den neuen Wert gesetzt (`{ scroll: false }`), damit die URL als Quelle der Wahrheit dient und beim Reload/Copy-Paste erhalten bleibt.
    4. Rufe den Hook auf: `const neighborNav = useEpisodeNeighborNavigation({ animeId: segmentAnimeId, currentVersionId: version?.id ?? null, groupId: segmentGroupId, releaseVersion: segmentVersion ?? "v1" });` — direkt nach der bestehenden Berechnung von `segmentVersion`.
    5. Rendere `<EpisodeNavigationControls prevVersionId={neighborNav.prevVersionId} prevEpisodeNumber={neighborNav.prevEpisodeNumber} nextVersionId={neighborNav.nextVersionId} nextEpisodeNumber={neighborNav.nextEpisodeNumber} currentIndex={neighborNav.currentIndex} totalCount={neighborNav.totalCount} isLoading={neighborNav.isLoading} activeTab={visibleActiveTab} />` innerhalb von `<header className={styles.header}>`, direkt unterhalb des bestehenden `subtitle`-Absatzes (also noch im linken `<div>` des Headers, nach dem `version ? <p className={styles.subtitle}>...</p> : null`-Block) — NICHT als eigene neue Sektion, damit die Buttons im Blickfeld direkt neben Episode/Gruppe/Version-Kontext stehen. Nur rendern, wenn `version` vorhanden ist (`{version ? <EpisodeNavigationControls .../> : null}`).
    6. Passe `page.test.tsx` an: ergaenze im bestehenden `next/navigation`-Mock (Zeile ca. 29-31) `useRouter: () => ({ push: vi.fn(), replace: vi.fn() })` und ggf. `usePathname: () => "/admin/episode-versions/42/edit"`, damit bestehende Tests nicht brechen. Ergaenze einen `vi.mock("./useEpisodeNeighborNavigation", ...)`- und `vi.mock("./EpisodeNavigationControls", ...)`-Block analog zu den bestehenden Mocks fuer `useReleaseVersionMedia`/`SegmenteTab` (einfache Stub-Rueckgabe bzw. Stub-Komponente mit `data-testid="episode-navigation-controls"`), damit `page.test.tsx` weiterhin isoliert die Editor-Page testet ohne echten Netzwerk-Call.
    Fuehre danach `docker restart team4sv30-frontend` aus, damit die Live-Instanz auf :3000 den neuen Code servt.
  </action>
  <verify>
    <automated>cd /home/d1sk/team4s/frontend && npx vitest run "src/app/admin/episode-versions/[versionId]/edit/page.test.tsx"</automated>
  </verify>
  <done>`page.test.tsx` bleibt gruen mit den neuen Mocks. `docker restart team4sv30-frontend` erfolgreich ausgefuehrt. Datei `EpisodeVersionEditorPage.tsx` enthaelt nur Wiring-Code fuer die neue Navigation (kein zusaetzlicher Fach-Logik-Block), Tab-Klicks laufen ueber `handleTabChange`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live-UAT der Folgen-Navigation</name>
  <files>none — Live-Browser-Verifikation der in Task 1-3 gebauten Navigations-Buttons</files>
  <action>Alle automatisierten Tests aus Task 1-3 sind bereits gruen und der Frontend-Container wurde neu gestartet. Dieser Task fuehrt keine weitere Code-Aenderung aus, sondern praesentiert dem User die Live-Verifikationsschritte unten und wartet auf Bestaetigung.</action>
  <verify>
    <automated>curl -sS -o /dev/null -w "%{http_code}" http://192.168.235.196:3000 | grep -qE "^(200|30[0-9])$"</automated>
  </verify>
  <what-built>
    Zwei Navigations-Buttons ("← Vorherige Folge" / "Nächste Folge →") im Header des Contributor-Editors, die zur Nachbar-Folge derselben Gruppe+Version springen und den aktiven Tab beibehalten. Position "Folge X von Y" wird angezeigt. Erste/letzte Folge sowie fehlende Nachbar-Version deaktivieren den jeweiligen Button.
  </what-built>
  <how-to-verify>
    1. Im Browser (Tunnel `http://127.0.0.1:3300` oder direkt `http://192.168.235.196:3000`) als Admin oder Contributor einloggen.
    2. Zu einem Anime mit mehreren Folgen und mindestens einer Fansub-Gruppe/Version navigieren, eine mittlere Folge im Contributor-Editor (`/admin/episode-versions/{id}/edit`) oeffnen.
    3. Pruefen: "← Vorherige Folge" und "Nächste Folge →" sind sichtbar, zeigen "Folge X von Y" an.
    4. Auf einem der Tabs (Segmente/Media/Notizen) klicken, dann auf "Nächste Folge →" klicken — pruefen, dass der gleiche Tab auf der Zielseite aktiv bleibt und die URL `?tab=...` enthaelt.
    5. Zur ersten Folge der Gruppe/Version navigieren — "Vorherige Folge" muss disabled sein. Zur letzten Folge navigieren — "Naechste Folge" muss disabled sein.
    6. Auf einem schmalen Viewport (Handy-Breite, z. B. Browser-DevTools auf 375px) pruefen: Buttons stapeln untereinander, kein horizontales Scrollen auf der Seite.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <done>User bestaetigt alle 6 Verifikationsschritte als korrekt (oder listet konkrete Abweichungen).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> Backend API | `getGroupedEpisodes` liest authentifiziert ueber `authorizedFetch`; Antwortdaten (Episode-/Versions-Listen) werden nur zur Client-seitigen Navigation genutzt, nicht in HTML/DOM als Rohtext injiziert |
| Client Router | `router.push`/`router.replace`-Ziele werden ausschliesslich aus intern berechneten numerischen `versionId`-Werten und einem festen `tab`-Enum-Wert gebaut, keine Nutzereingabe fliesst ungefiltert in den Pfad |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|------------------|
| T-quick260820-01 | Tampering | `handleTabChange` URL-Sync | accept | `tab`-Wert wird ueber `parseActiveTab`/`ACTIVE_TABS`-Whitelist validiert bevor er in State/URL landet — bereits bestehendes Pattern, unveraendert wiederverwendet |
| T-quick260820-02 | Information Disclosure | `useEpisodeNeighborNavigation` Fehlerzustand | accept | Bei Fetch-Fehler wird nur ein generischer String im Hook-State gehalten, kein Rohfehlertext/Stacktrace im UI gerendert (Buttons bleiben einfach disabled) |
| T-quick260820-SC | Tampering | npm-Installs | accept | Keine neuen npm-Pakete in diesem Plan; ausschliesslich bestehende Dependencies (`next/navigation`, `@/components/ui/Button`, `@/lib/api`) werden verwendet |
</threat_model>

<verification>
- `cd /home/d1sk/team4s/frontend && npx vitest run "src/app/admin/episode-versions/[versionId]/edit/episodeNeighborNavigation.test.ts" "src/app/admin/episode-versions/[versionId]/edit/useEpisodeNeighborNavigation.test.ts" "src/app/admin/episode-versions/[versionId]/edit/EpisodeNavigationControls.test.tsx" "src/app/admin/episode-versions/[versionId]/edit/page.test.tsx"` — alle vier Testfiles gruen.
- `docker restart team4sv30-frontend` erfolgreich, danach Live-Check auf `http://192.168.235.196:3000` (oder Tunnel `http://127.0.0.1:3300`).
</verification>

<success_criteria>
- Contributor-Editor zeigt "← Vorherige Folge" / "Nächste Folge →" im Header, gebaut aus @/components/ui Button.
- Klick springt zur passenden `release_version_id` der Nachbar-Folge (gleicher Anime, gleiche Gruppe, gleiche Version) und behaelt den aktiven Tab per `?tab=`-Query-Param bei.
- Erste/letzte Folge sowie fehlende Nachbar-Version fuehren zu disabled Buttons statt kaputten Links.
- Alle neuen/angepassten Testfiles laufen gruen; keine ASCII-Ersatz-Umlaute im UI-Text.
- Live-UAT-Checkpoint durch den User bestaetigt.
</success_criteria>

<output>
Create `.planning/quick/260820-600-folgen-navigation-im-contributor-editor-/260820-600-SUMMARY.md` when done. Die SUMMARY muss den Bezug zu "Phase-117" (Contributor-Editor-Erweiterung) explizit benennen, auch wenn das Verzeichnis unter `.planning/quick/` liegt.
</output>
