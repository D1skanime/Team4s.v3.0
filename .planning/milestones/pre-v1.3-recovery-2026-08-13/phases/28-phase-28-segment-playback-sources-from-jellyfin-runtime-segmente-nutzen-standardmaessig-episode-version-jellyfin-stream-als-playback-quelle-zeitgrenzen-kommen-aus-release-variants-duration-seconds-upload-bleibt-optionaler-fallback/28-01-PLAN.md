---
phase: 28-segment-playback-sources-from-jellyfin-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/models/admin_anime_themes.go
  - backend/internal/repository/admin_content_anime_themes.go
  - backend/internal/repository/episode_version_repository.go
  - backend/internal/handlers/admin_content_anime_theme_segments.go
autonomous: true
requirements:
  - P28-SC1
  - P28-SC2
  - P28-SC3
  - P28-SC4
must_haves:
  truths:
    - "Segment playback resolves from the current episode-version release variant before any uploaded fallback is considered."
    - "theme_segment_playback_sources remains the authoritative resolved-playback table; source_type/source_ref/source_label remain operator-facing provenance and fallback inputs."
    - "Runtime validation is nullable-safe: known duration rejects impossible segment bounds, unknown duration still allows save."
  artifacts:
    - path: "backend/internal/repository/admin_content_anime_themes.go"
      provides: "Resolved playback snapshot joins, precedence rules, and duration-aware persistence for theme_segment_playback_sources"
    - path: "backend/internal/handlers/admin_content_anime_theme_segments.go"
      provides: "Request validation and editor-context plumbing for release-variant-aware segment writes"
  key_links:
    - from: "backend/internal/handlers/admin_content_anime_theme_segments.go"
      to: "backend/internal/repository/admin_content_anime_themes.go"
      via: "release_variant_id/editor playback context carried from the episode-version editor into segment create/update/list flows"
      pattern: "release_variant|playback|duration"
---

<objective>
Lock the backend playback-resolution contract before any UI copy or controls change.

Purpose: Phase 28 fails if the UI says "default episode-version playback" while the repository still writes fallback-only playback rows.
Output: Real release-variant snapshot joins, authoritative playback precedence, and backend validation against `release_variants.duration_seconds`.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-phase-28-segment-playback-sources-from-jellyfin-runtime-segmente-nutzen-standardmaessig-episode-version-jellyfin-stream-als-playback-quelle-zeitgrenzen-kommen-aus-release-variants-duration-seconds-upload-bleibt-optionaler-fallback/28-CONTEXT.md
@.planning/phases/28-phase-28-segment-playback-sources-from-jellyfin-runtime-segmente-nutzen-standardmaessig-episode-version-jellyfin-stream-als-playback-quelle-zeitgrenzen-kommen-aus-release-variants-duration-seconds-upload-bleibt-optionaler-fallback/28-RESEARCH.md
@backend/internal/repository/admin_content_anime_themes.go
@backend/internal/repository/episode_version_repository.go
@backend/internal/handlers/admin_content_anime_theme_segments.go
@backend/internal/models/admin_anime_themes.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve the current release variant and Jellyfin runtime inside the playback snapshot</name>
  <files>backend/internal/repository/admin_content_anime_themes.go, backend/internal/repository/episode_version_repository.go, backend/internal/models/admin_anime_themes.go</files>
  <read_first>
    - backend/internal/repository/admin_content_anime_themes.go
    - backend/internal/repository/episode_version_repository.go
    - .planning/phases/28-phase-28-segment-playback-sources-from-jellyfin-runtime-segmente-nutzen-standardmaessig-episode-version-jellyfin-stream-als-playback-quelle-zeitgrenzen-kommen-aus-release-variants-duration-seconds-upload-bleibt-optionaler-fallback/28-RESEARCH.md
    - .planning/phases/28-phase-28-segment-playback-sources-from-jellyfin-runtime-segmente-nutzen-standardmaessig-episode-version-jellyfin-stream-als-playback-quelle-zeitgrenzen-kommen-aus-release-variants-duration-seconds-upload-bleibt-optionaler-fallback/28-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `loadThemeSegmentPlaybackSnapshotTx(...)` resolves `playback_release_variant_id`, Jellyfin item identity, and `duration_seconds` for the current editor release variant instead of returning `NULL::INTEGER AS duration_seconds`.
    - Test 2: The join prefers the concrete release variant from the current episode-version editor context and does not fall back to anime-global heuristics when that context is present.
    - Test 3: If the current release variant has no Jellyfin-backed stream or runtime, the snapshot still returns segment provenance cleanly and marks runtime/playback fields null without failing the write.
  </behavior>
  <action>Extend the segment repository snapshot contract so list/create/update flows receive the current editor release-variant anchor explicitly. Thread that anchor from the handler layer into `loadThemeSegmentPlaybackSnapshotTx(...)`, then join `release_variants rv`, `release_versions rev`, `release_streams rs`, and `stream_sources ss` using the same release-native tables already used in `episode_version_repository.go`. Resolve `rv.id`, `rv.duration_seconds`, and the preferred Jellyfin stream source (`ss.provider_type = 'jellyfin'`, `ss.external_id`) for the exact current variant. If multiple stream sources are linked, prefer Jellyfin first with a deterministic `ORDER BY` and `LIMIT 1` instead of relying on arbitrary row order.</action>
  <acceptance_criteria>
    - `Select-String -Path backend/internal/repository/admin_content_anime_themes.go -Pattern "release_variants|release_streams|stream_sources|duration_seconds"` returns matches inside `loadThemeSegmentPlaybackSnapshotTx`.
    - `Select-String -Path backend/internal/repository/admin_content_anime_themes.go -Pattern "NULL::INTEGER AS duration_seconds"` returns no matches.
    - `Select-String -Path backend/internal/models/admin_anime_themes.go -Pattern "ReleaseVariant|PlaybackVariant|Duration"` returns matches for the new snapshot/input context fields.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/repository -count=1</automated>
  </verify>
  <done>The repository can derive the current release variant, Jellyfin source, and runtime for segment playback resolution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make episode-version playback the default resolved source and keep upload as explicit fallback</name>
  <files>backend/internal/repository/admin_content_anime_themes.go, backend/internal/models/admin_anime_themes.go</files>
  <read_first>
    - backend/internal/repository/admin_content_anime_themes.go
    - .planning/phases/28-phase-28-segment-playback-sources-from-jellyfin-runtime-segmente-nutzen-standardmaessig-episode-version-jellyfin-stream-als-playback-quelle-zeitgrenzen-kommen-aus-release-variants-duration-seconds-upload-bleibt-optionaler-fallback/28-CONTEXT.md
    - .planning/phases/28-phase-28-segment-playback-sources-from-jellyfin-runtime-segmente-nutzen-standardmaessig-episode-version-jellyfin-stream-als-playback-quelle-zeitgrenzen-kommen-aus-release-variants-duration-seconds-upload-bleibt-optionaler-fallback/28-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: A segment with no explicit fallback and a resolvable current Jellyfin stream writes `playback_source_kind = 'episode_version'`.
    - Test 2: `source_type = 'release_asset'` only yields `playback_source_kind = 'uploaded_asset'` when the operator explicitly selected the uploaded fallback and a real `source_ref`/asset is present.
    - Test 3: Missing stream plus missing explicit fallback removes the playback row rather than leaving stale playback metadata behind.
  </behavior>
  <action>Refactor `syncThemeSegmentPlaybackSourceTx(...)` so it uses one explicit precedence table: `release_asset with source_ref` -> `uploaded_asset`; otherwise `current release_variant + jellyfin stream` -> `episode_version`; otherwise explicit legacy `jellyfin_theme` -> `jellyfin_theme`; otherwise no playback row. Keep `source_type/source_ref/source_label` as operator-managed provenance/fallback inputs and do not overload them as the final playback truth. Ensure `playback_start_offset_seconds`, `playback_end_offset_seconds`, `playback_duration_seconds`, `playback_release_variant_id`, and `playback_jellyfin_item_id` are rebuilt from the snapshot every create/update/upload/delete path.</action>
  <acceptance_criteria>
    - `Select-String -Path backend/internal/repository/admin_content_anime_themes.go -Pattern "episode_version|uploaded_asset|jellyfin_theme"` returns matches inside `syncThemeSegmentPlaybackSourceTx`.
    - `Select-String -Path backend/internal/repository/admin_content_anime_themes.go -Pattern "playback_release_variant_id|playback_jellyfin_item_id|duration_seconds"` returns matches in the playback upsert.
    - `cd backend && go test ./internal/repository -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/repository -count=1</automated>
  </verify>
  <done>Playback rows now reflect the product rule: episode-version by default, uploaded asset only by explicit fallback choice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Backend validation for runtime-known and runtime-null segment saves</name>
  <files>backend/internal/handlers/admin_content_anime_theme_segments.go, backend/internal/repository/admin_content_anime_themes.go</files>
  <read_first>
    - backend/internal/handlers/admin_content_anime_theme_segments.go
    - backend/internal/repository/admin_content_anime_themes.go
    - .planning/phases/28-phase-28-segment-playback-sources-from-jellyfin-runtime-segmente-nutzen-standardmaessig-episode-version-jellyfin-stream-als-playback-quelle-zeitgrenzen-kommen-aus-release-variants-duration-seconds-upload-bleibt-optionaler-fallback/28-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: If runtime is known, `end_time` beyond `release_variants.duration_seconds` is rejected with a clear validation response instead of silently persisting an impossible range.
    - Test 2: `start_time >= end_time` remains rejected regardless of runtime availability.
    - Test 3: When runtime is null, valid manual `start_time`/`end_time` saves continue to succeed.
  </behavior>
  <action>Add one authoritative validation seam used by both create and patch in `admin_content_anime_theme_segments.go`. It should parse `start_time` and `end_time`, look up the current release-variant runtime from the same playback snapshot context, and return a concrete 400 validation error when `end_time` exceeds known runtime or `start_time >= end_time`. Keep nullable runtime as a normal path: only enforce the upper bound when `duration_seconds` exists. Re-load and return the hydrated segment DTO so frontend callers receive the updated `playback_*` fields immediately after save.</action>
  <acceptance_criteria>
    - `Select-String -Path backend/internal/handlers/admin_content_anime_theme_segments.go -Pattern "duration_seconds|runtime|end_time|start_time|badRequest"` returns matches.
    - `Select-String -Path backend/internal/handlers/admin_content_anime_theme_segments.go -Pattern "CreateAnimeSegment|UpdateAnimeSegment"` returns matches near the shared validation seam.
    - `cd backend && go test ./internal/handlers ./internal/repository -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/handlers ./internal/repository -count=1</automated>
  </verify>
  <done>Backend saves are now authoritative about runtime limits without breaking nullable-runtime segment editing.</done>
</task>

</tasks>

<verification>
Run targeted backend tests, then confirm by inspection that the playback snapshot no longer hardcodes null runtime and that the sync path writes `episode_version` by default when the current release variant has a Jellyfin stream.
</verification>

<success_criteria>
The backend can resolve current release-variant playback, persist default episode-version playback rows, and reject impossible timings when real runtime is known.
</success_criteria>

<output>
After completion, create `.planning/phases/28-phase-28-segment-playback-sources-from-jellyfin-runtime-segmente-nutzen-standardmaessig-episode-version-jellyfin-stream-als-playback-quelle-zeitgrenzen-kommen-aus-release-variants-duration-seconds-upload-bleibt-optionaler-fallback/28-01-SUMMARY.md`
</output>
