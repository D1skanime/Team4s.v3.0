---
phase: 94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/Switch.tsx
  - frontend/src/components/ui/Accordion.tsx
  - frontend/src/components/ui/index.ts
  - frontend/src/components/ui/ui.module.css
  - frontend/src/app/dev/ui-system/page.tsx
autonomous: true
requirements: [AC-8, D-11, D-12, D-14]
nyquist_compliant: true

must_haves:
  truths:
    - "Es existiert ein Switch-Primitiv unter @/components/ui mit role=switch und aria-checked (D-12)"
    - "Es existiert ein Accordion-Primitiv unter @/components/ui mit aria-expanded-Header-Toggle (D-12)"
    - "Beide Primitives sind über frontend/src/components/ui/index.ts exportiert"
    - "Switch- und Accordion-Touch-Ziele sind >= 44 px (D-14)"
  artifacts:
    - path: "frontend/src/components/ui/Switch.tsx"
      provides: "Globales Switch/Toggle-Primitiv"
      min_lines: 20
      contains: "role=\"switch\""
    - path: "frontend/src/components/ui/Accordion.tsx"
      provides: "Globales Accordion-Primitiv (Kategorien-Collapse)"
      min_lines: 20
      contains: "aria-expanded"
  key_links:
    - from: "frontend/src/components/ui/index.ts"
      to: "Switch.tsx, Accordion.tsx"
      via: "export * from"
      pattern: "export \\* from './(Switch|Accordion)'"
---

<objective>
Zwei fehlende globale UI-Primitives bauen, die die rollenbasierte Capability-UI (Plan 06) bei 390 px
benötigt: Switch (pro Capability) und Accordion (Kategorien-Collapse). Beide existieren noch NICHT in
@/components/ui und sind laut RESEARCH echte Neubauten. CLAUDE.md verbietet natives
<button>/<input type=checkbox> in Konsumenten — diese Primitives kapseln native Elemente an der
einzig erlaubten Stelle (unter frontend/src/components/ui/).

Purpose: Liefert die wiederverwendbaren Bausteine, bevor die abhängige Capability-UI sie konsumiert
(Interface-First). Vermeidet, dass Plan 06 native Elemente erfindet (Pitfall 5).
Output: Switch.tsx, Accordion.tsx, Barrel-Export, CSS, /dev/ui-system-Showcase-Einträge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-CONTEXT.md
@.planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-PATTERNS.md
@frontend/src/components/ui/Tabs.tsx
@frontend/src/components/ui/Card.tsx
@frontend/src/components/ui/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Switch-Primitiv unter @/components/ui</name>
  <files>frontend/src/components/ui/Switch.tsx, frontend/src/components/ui/index.ts, frontend/src/components/ui/ui.module.css</files>
  <read_first>
    - frontend/src/components/ui/Tabs.tsx ('use client'-Direktive, classNames-Import, ui.module.css-Import, exportiertes Props-Interface, role="tab"/aria-selected-Muster Z.34-46)
    - frontend/src/components/ui/index.ts (Barrel-Export-Reihenfolge)
    - frontend/src/components/ui/ui.module.css (vorhandene Klassen-/Token-Konventionen)
    - .planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-PATTERNS.md (Abschnitt Switch.tsx)
  </read_first>
  <behavior>
    - Test 1: Switch rendert ein Element mit role="switch" und aria-checked entsprechend dem checked-Prop.
    - Test 2: Klick ruft onCheckedChange mit dem invertierten Wert auf.
    - Test 3: disabled=true setzt aria-disabled und unterdrückt onCheckedChange.
    - Test 4: aria-label / label-Prop ist gesetzt (Accessibility, deutsche Labels).
  </behavior>
  <action>
    Erstelle Switch.tsx analog zu Tabs.tsx-Konventionen: 'use client'; import { classNames } from './classNames';
    import styles from './ui.module.css'. Exportiere ein SwitchProps-Interface mit mindestens:
    checked: boolean, onCheckedChange: (next: boolean) => void, disabled?: boolean, label?: string,
    aria-label-Unterstützung. Render: <button type="button" role="switch" aria-checked={checked}
    aria-disabled={disabled} ...>. onClick toggelt und ruft onCheckedChange(!checked); bei disabled No-op.
    Touch-Ziel >= 44 px über eine neue CSS-Klasse in ui.module.css sicherstellen (min-width/min-height 44px).
    Deutsche, umlautkorrekte Default-/Beispieltexte falls UI-Text auftaucht. Export in index.ts ergänzen
    (`export * from './Switch'`), alphabetische/bestehende Reihenfolge beibehalten. KEIN natives
    <input type=checkbox> in Konsumenten — hier (Primitiv-Definition) ist das native <button> erlaubt.
    Lege eine colocated Test-Datei Switch.test.tsx mit `// @vitest-environment jsdom` an, die die vier
    Behavior-Fälle prüft.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/ui/Switch.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Quelle: Switch.tsx enthält role="switch" und aria-checked.
    - Quelle: index.ts enthält `export * from './Switch'`.
    - Quelle: ui.module.css enthält eine Switch-Klasse mit min-height/min-width >= 44px.
    - Behavior: `npx vitest run src/components/ui/Switch.test.tsx` exit 0; Toggle/disabled/aria geprüft.
    - Quelle: keine ASCII-Umlaut-Ersetzungen in user-facing Strings (ESLint/Sichtprüfung).
  </acceptance_criteria>
  <done>Switch-Primitiv exportiert, getestet (4 Verhalten grün), Touch-Ziel >= 44 px, role=switch/aria-checked korrekt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Accordion-Primitiv unter @/components/ui + ui-system-Showcase</name>
  <files>frontend/src/components/ui/Accordion.tsx, frontend/src/components/ui/index.ts, frontend/src/components/ui/ui.module.css, frontend/src/app/dev/ui-system/page.tsx</files>
  <read_first>
    - frontend/src/components/ui/Tabs.tsx (useId/useState, classNames, ui.module.css, ARIA)
    - frontend/src/components/ui/Card.tsx (variant-Prop-Muster Z.6-14, Header/Body-Komposition)
    - frontend/src/app/dev/ui-system/page.tsx (Showcase-Struktur für Primitives)
    - .planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-PATTERNS.md (Abschnitt Accordion.tsx)
  </read_first>
  <behavior>
    - Test 1: Accordion-Item rendert einen Header-<button> mit aria-expanded entsprechend dem offenen Zustand.
    - Test 2: Klick auf Header toggelt aria-expanded und zeigt/versteckt den Body.
    - Test 3: Mehrere Items sind unabhängig auf-/zuklappbar (oder single-open je nach gewähltem Modus — dokumentieren).
    - Test 4: Header-Touch-Ziel >= 44 px (CSS-Klasse vorhanden).
  </behavior>
  <action>
    Erstelle Accordion.tsx analog Tabs.tsx/Card.tsx: 'use client'; classNames + ui.module.css.
    Exportiere AccordionProps und AccordionItemProps (oder eine items-Array-API mit {id, title, children}).
    Header ist ein <button type="button" aria-expanded={open} aria-controls={panelId}>; Body wird per
    State ein-/ausgeblendet. Verwende useId für stabile IDs. KEINE horizontale Tab-Leiste (Anti-Pattern
    bei 390 px laut RESEARCH). Touch-Ziel-CSS >= 44 px. Export in index.ts (`export * from './Accordion'`).
    Ergänze in /dev/ui-system/page.tsx je einen kompakten Showcase-Block für Switch und Accordion
    (deutsche, umlautkorrekte Demo-Labels). Lege Accordion.test.tsx (`// @vitest-environment jsdom`) mit
    den vier Behavior-Fällen an.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/ui/Accordion.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Quelle: Accordion.tsx enthält aria-expanded und einen Header-<button>.
    - Quelle: index.ts enthält `export * from './Accordion'`.
    - Quelle: /dev/ui-system/page.tsx referenziert Switch und Accordion.
    - Behavior: `npx vitest run src/components/ui/Accordion.test.tsx` exit 0; Toggle/Sichtbarkeit geprüft.
    - Quelle: ui.module.css enthält Accordion-Header-Klasse mit min-height >= 44px.
  </acceptance_criteria>
  <done>Accordion-Primitiv exportiert, getestet (4 Verhalten grün), im ui-system-Showcase sichtbar, kein horizontaler Scroll-Zwang.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Konsument-Komponente → UI-Primitiv | Primitiv kapselt native Elemente; Konsument darf keine eigenen nativen Elemente bauen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-05-A | Tampering | Natives <input type=checkbox> als Switch-Ersatz im Konsumenten | mitigate | Globales Switch-Primitiv bereitstellen; ESLint no-restricted-syntax greift bei nativen Elementen |
| T-94-SC | Tampering | npm-Installs | accept | Phase fügt KEINE Pakete hinzu (RESEARCH); kein Legitimacy-Gate nötig |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/ui/Switch.test.tsx src/components/ui/Accordion.test.tsx` grün.
- index.ts exportiert beide Primitives.
- `git diff --check` sauber.
</verification>

<success_criteria>
- Switch und Accordion existieren als globale Primitives, exportiert und getestet.
- Touch-Ziele >= 44 px (D-14), ARIA korrekt, keine nativen Elemente in künftigen Konsumenten nötig.
- Showcase in /dev/ui-system aktualisiert.
</success_criteria>

<output>
Create `.planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-05-SUMMARY.md` when done
</output>
