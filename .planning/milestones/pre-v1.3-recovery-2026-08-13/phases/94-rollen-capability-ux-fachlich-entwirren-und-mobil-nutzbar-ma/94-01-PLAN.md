---
phase: 94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/handlers/admin_capability_handler_test.go
  - backend/internal/repository/authz_capability_mutations_test.go
  - backend/internal/repository/role_definitions_context_test.go
autonomous: true
requirements: [AC-6, AC-7, AC-10, D-03, D-04, D-05, D-07]
nyquist_compliant: true

must_haves:
  truths:
    - "Ein roter Go-Test beweist: Grant an historischer Rolle (founder) liefert HTTP 422 role_not_assignable (D-05)"
    - "Ein roter Go-Test beweist: Revoke an historischer Rolle (co_leader) liefert HTTP 422 role_not_assignable (D-05)"
    - "Ein roter Go-Test beweist: Matrix-Response setzt assignable=true für App-Rollen und assignable=false für historische Rollen (D-04)"
    - "Ein roter Go-Test beweist: group_history-Read-Repo-Methode liefert nur kuratierte Hist-Rollen, nicht FANSUB_GROUP_ROLE_OPTIONS (D-07)"
  artifacts:
    - path: "backend/internal/handlers/admin_capability_handler_test.go"
      provides: "Assignable-Guard-Tests (Grant+Revoke 422) + Matrix-assignable-Anreicherungstest"
      contains: "AssignableGuard"
    - path: "backend/internal/repository/role_definitions_context_test.go"
      provides: "group_history-Read-Repo-Test (kuratierte Whitelist)"
      contains: "RoleDefinitionsContext"
  key_links:
    - from: "admin_capability_handler_test.go"
      to: "permissions.IsKnownFansubGroupRole"
      via: "Negativ-Rolle founder/co_leader, Positiv-Rolle fansub_lead"
      pattern: "role_not_assignable"
---

<objective>
Wave-0-Tests-First (Nyquist) für die Backend-Verhaltensgarantien dieser Phase. Dieser Plan erstellt
die ROTEN Go-Tests, bevor die Produktionsimplementierung (Plan 02/03) existiert. Die Tests definieren
das verbindliche Verhalten: Assignable-Guard (422 role_not_assignable), assignable-Anreicherung in der
Capability-Matrix und die kuratierte group_history-Read-Liste.

Purpose: Stellt sicher, dass jede nachfolgende Backend-Task gegen ein vorab definiertes, prüfbares
Verhalten implementiert wird (AC-10 Pflichtabdeckung). Verhindert Happy-Path-Overclaiming.
Output: Drei Go-Test-Dateien/-Erweiterungen, die initial ROT laufen (bzw. via skip-freier
Interface-Assertion kompilieren), und in Plan 02/03 grün werden.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-CONTEXT.md
@.planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-VALIDATION.md
@.planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-PATTERNS.md

<interfaces>
<!-- Aus dem Codebase extrahiert — Executor nutzt diese direkt, kein eigenes Stöbern nötig. -->

backend/internal/permissions/permissions.go:
  func FansubGroupRoles() []string            // 10 assignable App-Gruppenrollen
  func IsKnownFansubGroupRole(role string) bool
  // fansubGroupRoleCatalog enthält die 10 Codes; founder/leader/co_leader/admin/other/project_manager sind NICHT enthalten

backend/internal/repository/authz_capability_mutations.go:
  type CapabilityMatrixRoleEntry struct { RoleCode string `json:"role_code"`; LabelDE string `json:"label_de"`; Actions []CapabilityMatrixActionState `json:"actions"` }
  func (r *AuthzRepository) ListCapabilityMatrix(ctx context.Context) (*CapabilityMatrix, error)
  func (r *AuthzRepository) GrantRoleCapability(ctx, roleCode, actionCode string) error
  func (r *AuthzRepository) RevokeRoleCapability(ctx, roleCode, actionCode string) error

backend/internal/repository/hist_group_member_roles_repository.go:231:
  func (r *HistGroupMemberRolesRepository) RoleCodeExistsForContext(ctx, roleCode, contextName string) (bool, error)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Roter Go-Test für Assignable-Guard (Grant+Revoke 422) und Matrix-assignable-Anreicherung</name>
  <files>backend/internal/handlers/admin_capability_handler_test.go</files>
  <read_first>
    - backend/internal/handlers/admin_capability_handler_test.go (gesamtes Stub-/Context-Muster: stubCapabilityAuthzRepo, makeCapabilityTestContext, TestRevokeCapabilityLastActionGuard)
    - backend/internal/handlers/admin_capability_handler.go (GrantCapability ~Z.71, RevokeCapability ~Z.115, Lockout-Guard-Muster ~Z.138)
    - backend/internal/permissions/permissions.go (IsKnownFansubGroupRole, FansubGroupRoles, fansubGroupRoleCatalog ~Z.200)
    - .planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-PATTERNS.md (Abschnitt "Backend-Tests (AC 10)")
  </read_first>
  <action>
    Erweitere admin_capability_handler_test.go um Tests, deren Namen das Substring "AssignableGuard"
    enthalten (damit `go test -run AssignableGuard` sie selektiert):
    (1) TestGrantCapabilityAssignableGuardRejectsHistoricalRole — ruft GrantCapability mit roleCode="founder"
        auf; erwartet HTTP-Status 422 und body.Error.Code == "role_not_assignable". Body in
        struct{ Error struct{ Code string `json:"code"` } } parsen (Muster aus TestRevokeCapabilityLastActionGuard).
    (2) TestRevokeCapabilityAssignableGuardRejectsHistoricalRole — ruft RevokeCapability mit roleCode="co_leader"
        auf; erwartet 422 + "role_not_assignable" (Pitfall 4: Guard in BEIDEN Pfaden).
    (3) TestGrantCapabilityAssignableGuardAllowsAppRole — ruft GrantCapability mit roleCode="fansub_lead"
        (assignable) auf; erwartet, dass der Guard NICHT 422 liefert (Status != 422). Stub-Repo so
        konfigurieren, dass GrantRoleCapability nil zurückgibt.
    (4) TestListCapabilityMatrixAssignableEnrichment — baut eine CapabilityMatrix mit je einer App-Rolle
        (z.B. "fansub_lead") und einer historischen Rolle (z.B. "founder"); ruft ListCapabilityMatrix-Handler;
        erwartet im JSON-Response je RoleEntry ein assignable-Feld: true für "fansub_lead", false für "founder".
        Da der Stub die Matrix liefert und der Handler anreichert (Plan 02), prüft der Test das angereicherte
        JSON. Verwende permissions.IsKnownFansubGroupRole als Erwartungsorakel, KEINE hartkodierte Rollenliste.
    Negativ-/Positiv-Rollen ausschließlich über permissions ableiten: positives Beispiel via FansubGroupRoles()[0],
    negatives Beispiel "founder"/"co_leader" (nicht im Katalog). Tests müssen initial ROT sein (Guard/Anreicherung
    existiert noch nicht) — das ist beabsichtigt (Nyquist RED-Phase). KEINE Produktionsdatei in diesem Task ändern.
    Deutsche Fehlermeldungen mit korrekten Umlauten erwarten, wo geprüft.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/handlers/ -run AssignableGuard 2>&1 | grep -E "FAIL|--- FAIL|undefined|cannot" || echo "EXPECTED-RED-OR-COMPILE-GAP"</automated>
  </verify>
  <acceptance_criteria>
    - Quelle: admin_capability_handler_test.go enthält die vier Testfunktionen mit "AssignableGuard" im Namen (außer Matrix-Test, der "AssignableEnrichment" trägt).
    - Quelle: kein hartkodiertes Rollen-Whitelist-Array im Test; Erwartung leitet sich aus permissions.IsKnownFansubGroupRole / FansubGroupRoles() ab.
    - Behavior: `cd backend && go test ./internal/handlers/ -run AssignableGuard` schlägt fehl ODER meldet undefined-Symbol (RED), weil der Guard noch fehlt — Test ist syntaktisch valide Go.
    - Quelle: Body-Assertion prüft exakt den String "role_not_assignable" und HTTP 422.
  </acceptance_criteria>
  <done>Vier Tests vorhanden, kompilieren als valides Go bzw. melden nur den fehlenden Produktions-Guard, prüfen 422+role_not_assignable für Grant und Revoke sowie assignable-Anreicherung.</done>
</task>

<task type="auto">
  <name>Task 2: Roter Go-Test für group_history-Read-Repo-Methode (kuratierte Whitelist)</name>
  <files>backend/internal/repository/role_definitions_context_test.go, backend/internal/repository/authz_capability_mutations_test.go</files>
  <read_first>
    - backend/internal/repository/hist_group_member_roles_repository.go (RoleCodeExistsForContext ~Z.231, $N = ANY(contexts)-Mechanik)
    - backend/internal/repository/authz_capability_mutations.go (CapabilityMatrixRoleEntry ~Z.30, ListCapabilityMatrix-Query ~Z.59)
    - .planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-RESEARCH.md (Pattern 4, Pitfall 2 — Migration-0103-Drift, Whitelist founder/leader/co_leader/project_manager)
    - database/migrations/0085_role_definitions_seed.up.sql (Seed der Hist-Rollen)
  </read_first>
  <action>
    Erstelle role_definitions_context_test.go mit einem Test, dessen Name "RoleDefinitionsContext" enthält.
    Da diese Repo-Methode (z.B. ListGroupHistoryRoleDefinitions / ListByContext) in Plan 03 erst entsteht,
    verankere den Test als kompilierende Interface-/Verhaltensspezifikation analog zum bestehenden
    Repo-Test-Stil dieses Pakets (vgl. STATE-Decision "Wave-0-Repository-Tests: t.Skip() durch
    Interface-Assertion (var _) und echte GREEN-Tests ersetzt" — hier zunächst RED/kompiliergeprüft):
    (1) Definiere ein Interface-Assertion-Konstrukt (var _ interface{ ... }) ODER einen Methodenaufruf-Stub,
        der die erwartete Signatur der neuen Methode dokumentiert: liefert eine Liste von {Code, LabelDE, SortOrder}
        gefiltert auf 'group_history' = ANY(contexts) AND code = ANY(whitelist).
    (2) Test TestRoleDefinitionsContextWhitelistOnly — dokumentiert als Erwartung: das Ergebnis enthält
        ausschließlich Codes aus {"founder","leader","co_leader","project_manager"} und KEINEN Code aus
        permissions.FansubGroupRoles() (z.B. "translator"/"encoder" dürfen NICHT erscheinen, obwohl Migration 0103
        sie mit group_history markiert — Pitfall 2). Wo eine echte DB-Verbindung im Paket-Teststil nicht
        verfügbar ist, prüfe die Whitelist-Konstante und die Disjunktheit zu FansubGroupRoles() rein
        funktional (Set-Schnitt == leer), damit der Test ohne Live-DB deterministisch ist.
    Ergänze in authz_capability_mutations_test.go nur falls nötig einen kompilierenden Platzhalter für die
    assignable/contexts-Felder (additiv), damit Plan 04/02 dagegen implementieren. Test initial ROT bzw.
    bei Interface-Assertion kompiliergeprüft. KEINE Produktionsdatei ändern.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/repository/ -run RoleDefinitionsContext 2>&1 | grep -E "FAIL|undefined|cannot|ok" || echo "EXPECTED-RED-OR-COMPILE-GAP"</automated>
  </verify>
  <acceptance_criteria>
    - Quelle: role_definitions_context_test.go enthält Test mit "RoleDefinitionsContext" im Namen.
    - Quelle: Test referenziert die Whitelist {founder, leader, co_leader, project_manager} und prüft Disjunktheit zu permissions.FansubGroupRoles().
    - Behavior: `cd backend && go test ./internal/repository/ -run RoleDefinitionsContext` kompiliert (valides Go) und ist RED oder dokumentiert die fehlende Methode via undefined.
    - Quelle: Set-Schnitt-Assertion zwischen Whitelist und FansubGroupRoles() == leer ist explizit kodiert.
  </acceptance_criteria>
  <done>group_history-Read-Test vorhanden, prüft kuratierte Whitelist und Disjunktheit zum App-Rollen-Katalog, kompiliert als valides Go.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin-Browser → Capability-API | Untrusted role_code/action_code als Pfadparameter |
| Test-Harness → Produktionscode | Tests definieren die serverseitige Autorisierungs-Invariante (kein direkter Angriffsvektor, aber Sicherheits-Spezifikation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-01 | Elevation of Privilege | GrantCapability/RevokeCapability mit historischem role_code | mitigate | Wave-0-Test fixiert 422 role_not_assignable als Pflichtverhalten (Implementierung in Plan 02) |
| T-94-02 | Tampering | Frontend-Bypass eines Disabled-Switch | mitigate | Test prüft serverseitigen Guard, nicht nur UI-Disabled |
| T-94-SC | Tampering | npm/go Paket-Installs | accept | Phase fügt KEINE Pakete hinzu (RESEARCH: kein npm install / go get) — kein Legitimacy-Gate nötig |
</threat_model>

<verification>
- `cd backend && go test ./internal/handlers/ -run AssignableGuard` läuft (RED erwartet, valides Go).
- `cd backend && go test ./internal/repository/ -run RoleDefinitionsContext` läuft (RED/Interface-Assertion erwartet).
- `git diff --check` ohne Whitespace-Fehler (AC-11).
</verification>

<success_criteria>
- Drei Test-Artefakte existieren und kompilieren als valides Go.
- Tests fixieren 422 role_not_assignable (Grant+Revoke), assignable-Anreicherung und die kuratierte group_history-Whitelist.
- Keine Produktionsdatei verändert (reine Tests-First-Wave).
</success_criteria>

<output>
Create `.planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-01-SUMMARY.md` when done
</output>
