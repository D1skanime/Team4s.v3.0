---
phase: 66-claiming-verifizierung
plan: 02
type: execute
wave: 2
depends_on:
  - 66-01
files_modified:
  - backend/internal/repository/member_claims_repository.go
  - backend/internal/repository/member_claim_invitations_repository.go
autonomous: true
requirements:
  - P66-SC1
  - P66-SC2
must_haves:
  truths:
    - "MemberClaimsRepository stellt Nick-Suche, Claim-Einreichung, Verifizierung (mit FOR UPDATE + EXISTS-Check) und Ablehnung bereit"
    - "VerifyClaim setzt noindex=false in derselben Transaktion (D-12)"
    - "MemberClaimInvitationRepository erzeugt Token via crypto/rand+SHA-256, speichert nur token_hash, gibt rawToken zurück (D-04)"
    - "AcceptInvitation validiert Token, prüft Ablauf, setzt member_claims.claim_status='verified' + members.noindex=false in einer Transaktion"
    - "ClaimMutationError-Typ deckt already_verified/invitation_expired/invitation_used/invitation_cancelled ab"
  artifacts:
    - path: backend/internal/repository/member_claims_repository.go
      provides: "CRUD für member_claims: SearchMembers, SubmitClaim, VerifyClaim, RejectClaim, ListPendingClaims"
      exports:
        - NewMemberClaimsRepository
        - MemberClaimsRepository
    - path: backend/internal/repository/member_claim_invitations_repository.go
      provides: "Token-Logik für member_claim_invitations: CreateInvitation, AcceptInvitation, CancelInvitation"
      exports:
        - NewMemberClaimInvitationRepository
        - MemberClaimInvitationRepository
        - ClaimMutationError
  key_links:
    - from: "VerifyClaim"
      to: "members.noindex = false"
      via: "UPDATE members SET noindex = false WHERE id = claim.MemberID (in Transaktion)"
      pattern: "UPDATE members SET noindex"
    - from: "AcceptInvitation"
      to: "member_claims INSERT + member_claim_invitations UPDATE"
      via: "pgx.TxOptions{} Transaktion mit FOR UPDATE auf Invitation-Row"
      pattern: "BeginTx.*FOR UPDATE"
---

<objective>
Zwei neue Repository-Dateien für Claim-Logik anlegen. `member_claims_repository.go` übernimmt CRUD für `member_claims` (Nick-Suche, Self-Service-Einreichung, Verifizierung mit Invariante, Ablehnung, Pending-Listing). `member_claim_invitations_repository.go` kapselt Token-Erzeugung, -Hashing, Lazy-Expire und den transaktionalen Accept-Flow analog zu `fansub_group_invitations_repository.go`.

Purpose: Handler in Plan 03 brauchen fertige Repository-Methoden, bevor sie HTTP-Endpunkte implementieren können.
Output: Zwei Repository-Dateien, jeweils unter 450 Zeilen, compilierbar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/66-claiming-verifizierung/66-CONTEXT.md
@.planning/phases/66-claiming-verifizierung/66-RESEARCH.md
@.planning/phases/66-claiming-verifizierung/66-PATTERNS.md

<interfaces>
<!-- Aus backend/internal/repository/fansub_group_invitations_repository.go — Token-Erzeugungskonvention -->
Token-Erzeugung: crypto/rand 32 Bytes → base64.RawURLEncoding → rawToken; sha256.Sum256([]byte(rawToken)) → hex.EncodeToString → tokenHash (64 Zeichen).
Lazy-Expire: UPDATE SET status='expired' WHERE status='pending' AND expires_at <= NOW()
Terminal-State-Map: cancelled → 410, accepted → 409, expired → 410
FOR UPDATE in Transaktion: SELECT ... FROM member_claim_invitations WHERE token_hash=$1 FOR UPDATE

<!-- Aus backend/internal/handlers/contributions_me_handler.go (resolveVerifiedMemberID) -->
Verified-Read-Query:
  SELECT member_id FROM member_claims WHERE app_user_id=$1 AND claim_status='verified' ORDER BY verified_at DESC LIMIT 1

<!-- Aus backend/internal/repository/errors.go -->
package repository enthält: ErrNotFound, ErrConflict — diese Sentinel-Fehler existieren bereits.

<!-- Nick-Suche (RESEARCH.md §4, Muster nach fansub_group_app_members_repository.go) -->
SELECT m.id, m.nickname, m.display_name FROM members m
WHERE m.nickname ILIKE $1
  AND NOT EXISTS (SELECT 1 FROM member_claims mc WHERE mc.member_id = m.id AND mc.claim_status='verified')
ORDER BY m.nickname LIMIT 10

<!-- Verified-Invariante (RESEARCH.md §3 + PATTERNS.md) -->
Innerhalb Transaktion (BeginTx + Defer Rollback):
1. SELECT EXISTS(SELECT 1 FROM member_claims WHERE member_id=$1 AND claim_status='verified') — Existenz prüfen
2. Falls existiert: ClaimMutationError{Code: "already_verified", Message: "Dieser Member-Eintrag ist bereits verifiziert.", HTTPStatus: 409}
3. UPDATE member_claims SET claim_status='verified', verified_by=$2, verified_at=NOW(), verification_method='manual_review' WHERE id=$3
4. UPDATE members SET noindex=false WHERE id=(SELECT member_id FROM member_claims WHERE id=$3)
5. Commit

<!-- Konstruktor-Konvention -->
type MemberClaimsRepository struct { db *pgxpool.Pool }
func NewMemberClaimsRepository(db *pgxpool.Pool) *MemberClaimsRepository
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: member_claims_repository.go anlegen</name>
  <files>backend/internal/repository/member_claims_repository.go</files>
  <read_first>
    - backend/internal/repository/fansub_group_invitations_repository.go — Konstruktor, Fehlertyp-Muster, Transaktionsmuster
    - backend/internal/repository/errors.go — ErrNotFound, ErrConflict Definitionen
    - backend/internal/handlers/contributions_me_handler.go — resolveVerifiedMemberID Query (Z. 39–54) als Referenz-SQL
    - database/migrations/0081_historical_members_identity.up.sql — member_claims-Spalten: claim_status, app_user_id, member_id, verified_by, verification_method, verified_at, note
    - backend/internal/repository/fansub_group_app_members_repository.go — SearchCandidates-Muster für ILIKE-Abfrage
  </read_first>
  <action>
    Erstelle `backend/internal/repository/member_claims_repository.go` im package `repository`.

    Imports: context, errors, fmt, strings, github.com/jackc/pgx/v5, github.com/jackc/pgx/v5/pgxpool

    Struct und Konstruktor:
    - type MemberClaimsRepository struct { db *pgxpool.Pool }
    - func NewMemberClaimsRepository(db *pgxpool.Pool) *MemberClaimsRepository

    Typen:
    - MemberSearchResult { ID int64; Nickname string; DisplayName string }
    - MemberClaimRow { ID int64; AppUserID int64; MemberID int64; MemberNickname string; ClaimStatus string; Note sql.NullString; CreatedAt time.Time }
    - SubmitClaimInput { MemberID int64; AppUserID int64; Note string }

    Methoden:
    1. SearchHistoricalMembers(ctx, query string) ([]MemberSearchResult, error)
       Query: SELECT m.id, m.nickname, COALESCE(m.display_name, '') FROM members m WHERE m.nickname ILIKE $1 AND NOT EXISTS (SELECT 1 FROM member_claims mc WHERE mc.member_id=m.id AND mc.claim_status='verified') ORDER BY m.nickname LIMIT 10
       Parameter: "%" + strings.TrimSpace(query) + "%"

    2. SubmitClaim(ctx, input SubmitClaimInput) (*MemberClaimRow, error)
       INSERT INTO member_claims (app_user_id, member_id, claim_status, note) VALUES ($1,$2,'pending',$3) ON CONFLICT (member_id, app_user_id) DO UPDATE SET note=$3, updated_at=NOW() RETURNING id, app_user_id, member_id, claim_status, note, created_at
       Bei UNIQUE-Violation anderer Art: ErrConflict zurückgeben

    3. VerifyClaim(ctx, claimID int64, verifiedByAppUserID int64) error
       Transaktion (pgx.TxOptions{}):
       - SELECT member_id FROM member_claims WHERE id=$1 FOR UPDATE
       - Falls kein Row: ErrNotFound
       - SELECT EXISTS(SELECT 1 FROM member_claims WHERE member_id=$memberID AND claim_status='verified') → falls true: &ClaimMutationError{Code:"already_verified", Message:"Dieser Member-Eintrag ist bereits verifiziert.", HTTPStatus:409}
       - UPDATE member_claims SET claim_status='verified', verified_by=$2, verified_at=NOW(), verification_method='manual_review', updated_at=NOW() WHERE id=$1
       - UPDATE members SET noindex=false, updated_at=NOW() WHERE id=$memberID (per D-12)
       - Commit

    4. RejectClaim(ctx, claimID int64, verifiedByAppUserID int64) error
       UPDATE member_claims SET claim_status='rejected', verified_by=$2, verified_at=NOW(), updated_at=NOW() WHERE id=$1 AND claim_status='pending'
       rowsAffected==0 → ErrNotFound

    5. ListPendingClaimsForGroup(ctx, fansubGroupID int64) ([]MemberClaimRow, error)
       SELECT mc.id, mc.app_user_id, mc.member_id, m.nickname, mc.claim_status, mc.note, mc.created_at
       FROM member_claims mc JOIN members m ON m.id=mc.member_id
       JOIN hist_fansub_group_members hgm ON hgm.member_id=mc.member_id
       WHERE hgm.fansub_group_id=$1 AND mc.claim_status='pending'
       ORDER BY mc.created_at ASC

    6. GetMyClaim(ctx, appUserID int64) (*MemberClaimRow, error)
       SELECT mc.id, mc.app_user_id, mc.member_id, m.nickname, mc.claim_status, mc.note, mc.created_at
       FROM member_claims mc JOIN members m ON m.id=mc.member_id
       WHERE mc.app_user_id=$1 ORDER BY mc.created_at DESC LIMIT 1
       pgx.ErrNoRows → ErrNotFound

    ClaimMutationError in dieser Datei deklarieren (nicht in invitations_repo, damit kein Import-Zyklus):
    type ClaimMutationError struct { Code string; Message string; HTTPStatus int }
    func (e *ClaimMutationError) Error() string
    func AsClaimMutationError(err error) (*ClaimMutationError, bool)

    Datei-Länge: Ziel unter 400 Zeilen.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go test ./internal/repository/... -run TestMemberClaims 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/repository/... -run TestMemberClaims` läuft ohne Compile-Fehler (Stubs aus Wave 0 werden als SKIP gemeldet)
    - Datei enthält SearchHistoricalMembers, SubmitClaim, VerifyClaim, RejectClaim, ListPendingClaimsForGroup, GetMyClaim
    - VerifyClaim verwendet SELECT ... FOR UPDATE und prüft EXISTS vor dem UPDATE
    - VerifyClaim enthält UPDATE members SET noindex=false in derselben Transaktion
    - ClaimMutationError mit Code "already_verified" definiert
    - Datei unter 450 Zeilen (wc -l < 450)
  </acceptance_criteria>
  <done>member_claims_repository.go kompiliert; alle 6 Methoden vorhanden; Invariante per FOR UPDATE + EXISTS gesichert; Test-Stubs aus Wave 0 laufen durch (SKIP).</done>
</task>

<task type="auto">
  <name>Task 2: member_claim_invitations_repository.go anlegen</name>
  <files>backend/internal/repository/member_claim_invitations_repository.go</files>
  <read_first>
    - backend/internal/repository/fansub_group_invitations_repository.go — vollständiges Vorbild: Token-Erzeugung (Z. 482–494), Accept-Transaktion (Z. 239–358), Lazy-Expire (Z. 361–374), mapTerminalState (Z. 496–507), Fehlertyp
    - backend/internal/repository/member_claims_repository.go — ClaimMutationError-Import (soeben erstellt)
    - database/migrations/0092_member_claim_invitations.up.sql — Spaltennamen verifizieren
  </read_first>
  <action>
    Erstelle `backend/internal/repository/member_claim_invitations_repository.go` im package `repository`.

    Imports: context, crypto/rand, crypto/sha256, encoding/base64, encoding/hex, errors, fmt, strings, time, github.com/jackc/pgx/v5, github.com/jackc/pgx/v5/pgxpool

    Struct + Konstruktor:
    - type MemberClaimInvitationRepository struct { db *pgxpool.Pool; appPublicURL string }
    - func NewMemberClaimInvitationRepository(db *pgxpool.Pool, appPublicURL string) *MemberClaimInvitationRepository

    Private Hilfsfunktionen (package-level, analog zu fansub_group_invitations):
    - generateClaimInvitationToken() (rawToken string, tokenHash string, err error)
      Implementierung: crypto/rand 32 Bytes → base64.RawURLEncoding.EncodeToString → rawToken; sha256.Sum256 → hex.EncodeToString → tokenHash
    - hashClaimInvitationToken(raw string) string
      sha256.Sum256([]byte(strings.TrimSpace(raw))) → hex.EncodeToString
    - mapTerminalClaimInvitationState(status string) error
      "cancelled" → ClaimMutationError{Code:"invitation_cancelled", Message:"Diese Einladung wurde bereits zurückgezogen.", HTTPStatus:410}
      "accepted"  → ClaimMutationError{Code:"invitation_used", Message:"Diese Einladung wurde bereits verwendet.", HTTPStatus:409}
      "expired"   → ClaimMutationError{Code:"invitation_expired", Message:"Dieser Einladungslink ist abgelaufen.", HTTPStatus:410}
      default     → ClaimMutationError{Code:"invalid_invitation_state", Message:"Diese Einladung kann nicht mehr verwendet werden.", HTTPStatus:409}

    Typen:
    - MemberClaimInvitationRow { ID int64; MemberID int64; FansubGroupID int64; Status string; ExpiresAt time.Time; CreatedAt time.Time }
    - CreateInvitationResult { Invitation MemberClaimInvitationRow; InviteLink string } — InviteLink = "/claim-invitations/accept?token="+rawToken

    Methoden:
    1. expirePendingInvitations(ctx, memberID int64) error — private, lazy expire
       UPDATE member_claim_invitations SET status='expired', updated_at=NOW() WHERE member_id=$1 AND status='pending' AND expires_at <= NOW()

    2. CreateInvitation(ctx, memberID int64, fansubGroupID int64, createdByAppUserID int64) (*CreateInvitationResult, error)
       - expirePendingInvitations aufrufen
       - generateClaimInvitationToken() aufrufen
       - INSERT INTO member_claim_invitations (member_id, fansub_group_id, token_hash, status, expires_at, created_by_app_user_id) VALUES ($1,$2,$3,'pending',NOW()+interval '7 days',$4) RETURNING id, member_id, fansub_group_id, status, expires_at, created_at
       - InviteLink: "/claim-invitations/accept?token=" + rawToken (rawToken NIEMALS in DB)
       - Bei UNIQUE-Violation pending-per-member: ClaimMutationError{Code:"pending_invitation_exists", Message:"Es gibt bereits einen aktiven Einladungslink für diesen Member.", HTTPStatus:409}

    3. AcceptInvitation(ctx, tokenRaw string, acceptedByAppUserID int64) error
       Transaktion (pgx.TxOptions{}):
       - tokenHash := hashClaimInvitationToken(tokenRaw)
       - SELECT id, member_id, fansub_group_id, status, expires_at FROM member_claim_invitations WHERE token_hash=$1 FOR UPDATE
       - pgx.ErrNoRows → ErrNotFound (ungültiger Token)
       - Falls status != 'pending': mapTerminalClaimInvitationState(status)
       - Falls expires_at <= NOW(): UPDATE status='expired'; ClaimMutationError{Code:"invitation_expired",...}
       - expirePendingInvitations(ctx, memberID) — lazy expire NACH Token-Lookup
       - Invariante: SELECT EXISTS(SELECT 1 FROM member_claims WHERE member_id=$memberID AND claim_status='verified') → falls true: ClaimMutationError{Code:"already_verified", Message:"Du bist bereits einem historischen Eintrag zugeordnet.", HTTPStatus:409}
       - INSERT INTO member_claims (app_user_id, member_id, claim_status, verification_method, verified_by, verified_at) VALUES ($acceptedByAppUserID, $memberID, 'verified', 'invite_link', $acceptedByAppUserID, NOW()) ON CONFLICT (member_id, app_user_id) DO UPDATE SET claim_status='verified', verification_method='invite_link', verified_by=$acceptedByAppUserID, verified_at=NOW(), updated_at=NOW()
       - UPDATE members SET noindex=false, updated_at=NOW() WHERE id=$memberID (per D-12)
       - UPDATE member_claim_invitations SET status='accepted', accepted_by_app_user_id=$acceptedByAppUserID, accepted_at=NOW(), updated_at=NOW() WHERE id=$invitationID
       - Commit

    4. CancelInvitation(ctx, invitationID int64, cancelledByAppUserID int64) error
       UPDATE member_claim_invitations SET status='cancelled', cancelled_by_app_user_id=$2, cancelled_at=NOW(), updated_at=NOW() WHERE id=$1 AND status='pending'
       rowsAffected==0 → ErrNotFound

    5. ListInvitationsForMember(ctx, memberID int64) ([]MemberClaimInvitationRow, error)
       SELECT id, member_id, fansub_group_id, status, expires_at, created_at FROM member_claim_invitations WHERE member_id=$1 ORDER BY created_at DESC LIMIT 10

    Datei-Länge: Ziel unter 420 Zeilen.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go test ./internal/repository/... -run TestMemberClaimInvitation 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/repository/... -run TestMemberClaimInvitation` läuft ohne Compile-Fehler (Stubs SKIP)
    - Datei enthält generateClaimInvitationToken, hashClaimInvitationToken, mapTerminalClaimInvitationState
    - AcceptInvitation enthält FOR UPDATE, EXISTS-Check für already_verified, INSERT member_claims, UPDATE members SET noindex=false, UPDATE invitation status='accepted' — alle in einer Transaktion
    - CreateInvitation gibt rawToken im InviteLink zurück (nicht in DB)
    - Keine Klartext-Tokens in DB-Inserts (nur tokenHash wird gespeichert)
    - Datei unter 450 Zeilen
  </acceptance_criteria>
  <done>Beide Repository-Dateien kompilieren; Token-Logik korrekt (nur Hash in DB); AcceptInvitation ist vollständig transaktional mit Invariante; Test-Stubs laufen durch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Grenze | Beschreibung |
|--------|-------------|
| Repository → DB | Alle Schreiboperationen über pgxpool.Pool; Transaktionen mit BeginTx |
| Token-Generierung | Nur crypto/rand; nie math/rand oder Zeit-basierte Seeds |

## STRIDE Threat Register

| Threat ID | Kategorie | Komponente | Disposition | Mitigationsplan |
|-----------|-----------|-----------|-------------|-----------------|
| T-66-02-01 | Tampering | VerifyClaim — Race Condition | mitigate | SELECT member_id FOR UPDATE + EXISTS-Check in Transaktion; ErrConflict 409 bei bereits-verified |
| T-66-02-02 | Elevation of Privilege | AcceptInvitation — doppeltes Einlösen | mitigate | mapTerminalClaimInvitationState gibt 409 bei status='accepted'; FOR UPDATE verhindert parallele Einlösung |
| T-66-02-03 | Information Disclosure | Klartext-Token in DB | mitigate | generateClaimInvitationToken gibt rawToken + tokenHash zurück; nur tokenHash wird persistiert; rawToken nur im InviteLink-Response |
| T-66-02-04 | Tampering | Token-Brute-Force | mitigate | 32 Byte crypto/rand → 256 Bit Entropie; SHA-256 Hash in DB macht Reverse-Lookup unmöglich |
| T-66-02-05 | Tampering | noindex-Reset bei ungewollter Einlösung | mitigate | AcceptInvitation prüft Token-Ablauf vor noindex-Reset; Token ist nach Einlösung 'accepted' und nicht wiederverwendbar |
| T-66-02-SC | Tampering | keine npm/pip Installs | accept | Nur Go-stdlib-Pakete (crypto/rand, sha256, hex); keine neuen Abhängigkeiten |

</threat_model>

<verification>
1. `go test ./internal/repository/... -run TestMemberClaims` ohne Compile-Fehler; Stubs SKIP
2. `go test ./internal/repository/... -run TestMemberClaimInvitation` ohne Compile-Fehler; Stubs SKIP
3. `member_claims_repository.go` < 450 Zeilen
4. `member_claim_invitations_repository.go` < 450 Zeilen
5. VerifyClaim enthält "FOR UPDATE" und "noindex=false" (grep-Verifikation)
6. AcceptInvitation enthält "FOR UPDATE", "claim_status='verified'", "noindex=false", "status='accepted'" (grep)
7. Kein "rawToken" oder Klartext-Token in INSERT-Statements
</verification>

<success_criteria>
Beide Repository-Dateien kompilieren fehlerfrei. Die Claim-Invariante (nur 1 verified per member_id) ist durch SELECT FOR UPDATE + EXISTS gesichert. Token-Sicherheitsprinzip eingehalten: rawToken nie in DB. Test-Stubs aus Wave 0 laufen durch (SKIP).
</success_criteria>

<output>
Erstelle `.planning/phases/66-claiming-verifizierung/66-02-SUMMARY.md` wenn fertig.
</output>
