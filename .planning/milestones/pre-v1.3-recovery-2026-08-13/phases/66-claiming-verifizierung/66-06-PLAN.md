---
phase: 66-claiming-verifizierung
plan: 06
type: execute
wave: 5
depends_on:
  - 66-04
files_modified:
  - frontend/src/app/claim-invitations/accept/page.tsx
  - frontend/src/app/admin/my-groups/[id]/page.tsx
  - frontend/src/app/me/profile/components/MemberClaimSection.tsx
autonomous: true
requirements:
  - P66-SC1
  - P66-SC2
must_haves:
  truths:
    - "claim-invitations/accept zeigt Login-Link wenn nicht eingeloggt, sonst Einladung-annehmen-Button"
    - "Nach Einlösung: successBox + router.replace('/me/profile') (Token aus URL entfernt)"
    - "Token-Error-Fälle (abgelaufen, ungültig, bereits verifiziert) zeigen spezifische deutsche Meldungen"
    - "manage/groups/[id] zeigt Claim-Einladungsbereich mit Einladungslink-Generator pro historischem Member"
    - "manage/groups/[id] zeigt Claim-Queue-Tabelle mit Bestätigen/Ablehnen-Aktionen"
    - "Einladungslink wird nach Generierung angezeigt mit 'Link kopieren'-Button (1,5s Feedback)"
    - "me/profile zeigt Nick-Suche + Claim-einreichen-Bereich; bei 'kein Treffer': Neuanlage-Antrag stellen (D-03)"
    - "Neuanlage-Antrags-Bereich in manage/groups/[id] mit 'Neuanlage-Anträge (N)'-Section und Bestätigen/Ablehnen"
  artifacts:
    - path: frontend/src/app/claim-invitations/accept/page.tsx
      provides: "Einlösungs-Seite analog invitations/accept"
      contains: "AcceptClaimInvitation"
    - path: frontend/src/app/admin/my-groups/[id]/page.tsx
      provides: "Claim-Einladungsbereich, Claim-Queue und Neuanlage-Antrags-Queue in Gruppen-Detail"
      contains: "member-claims"
    - path: frontend/src/app/me/profile/components/MemberClaimSection.tsx
      provides: "Nick-Suche + Claim-Einreichen + Neuanlage-Antrag-stellen-Bereich (D-03)"
      contains: "MemberClaimSection"
  key_links:
    - from: "claim-invitations/accept/page.tsx"
      to: "api.ts acceptClaimInvitation"
      via: "import { acceptClaimInvitation } from '@/lib/api'"
      pattern: "acceptClaimInvitation"
    - from: "admin/my-groups/[id]/page.tsx"
      to: "api.ts generateClaimInvitation + listPendingMemberClaims + listMemberRequests"
      via: "import { generateClaimInvitation, listPendingMemberClaims, listMemberRequests, ... } from '@/lib/api'"
      pattern: "generateClaimInvitation"
    - from: "me/profile/components/MemberClaimSection.tsx"
      to: "api.ts searchHistoricalMembers + submitMemberClaim + submitMemberRequest"
      via: "import { searchHistoricalMembers, submitMemberClaim, submitMemberRequest } from '@/lib/api'"
      pattern: "searchHistoricalMembers"
---

<objective>
Drei UI-Flächen implementieren:

1. `/claim-invitations/accept`: Einlösungs-Seite analog zu `invitations/accept` — Login-Redirect, Einlösen-Button, Fehler-Mapping, router.replace nach Erfolg.

2. `manage/groups/[id]`: Gruppendetail-Erweiterung um Einladungslink-Generator, Claim-Queue (Bestätigen/Ablehnen) UND Neuanlage-Antrags-Queue (D-03) mit konkretem `<action>`-Block.

3. `me/profile/components/MemberClaimSection.tsx`: Nick-Suche + Claim-einreichen-Flow; bei "kein Treffer" Neuanlage-Antrag-stellen-Button (D-03). In `me/profile/page.tsx` in die Side-Column oder einen eigenen Bereich einbinden.

Purpose: Diese Flächen sind die konkreten Aktionspunkte für den Leader-Einladungs-Flow (P66-SC2), den Self-Service-Claim-Einlösungs-Flow (P66-SC1) und den Neuanlage-Antrag-Flow (D-03).
Output: 1 neue Seite, 1 neue Komponente, 1 erweiterte Seite; alle build-grün.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/66-claiming-verifizierung/66-CONTEXT.md
@.planning/phases/66-claiming-verifizierung/66-UI-SPEC.md
@.planning/phases/66-claiming-verifizierung/66-PATTERNS.md
@.planning/phases/66-claiming-verifizierung/66-04-SUMMARY.md

<interfaces>
<!-- Aus frontend/src/app/invitations/accept/page.tsx — exaktes Analog -->
'use client'
Suspense-Wrapper mit AcceptInvitationContent inner-Komponente
useSearchParams() → token := searchParams.get('token')
useAuthSession() → { hasAccessToken, isClientInitialized }
useState: isSubmitting, errorMessage, successMessage
handleAccept(): try { await acceptFansubInvitation({token}); setSuccessMessage(...) } catch (ApiError) { setErrorMessage(...) }
Login-Link: <Link href="/login">anmelden</Link>

Anpassungen für claim-invitations/accept:
- Statt acceptFansubInvitation: acceptClaimInvitation (aus api.ts, nach Plan 04)
- Login-Link mit return_to: href={`/login?return_to=/claim-invitations/accept?token=${token}`}
- Nach Erfolg: router.replace('/me/profile') (Token aus URL entfernen, Fallstrick 3)
- successMessage: "Dein Account ist jetzt als Mitglied verifiziert. Dein Profil ist ab sofort öffentlich indexierbar."
- Fehler-Mapping per ApiError.code: 'invitation_expired' → spezifische Message; 'invitation_used' → ...; 'already_verified' → ...

<!-- Aus frontend/src/app/admin/my-groups/[id]/page.tsx -->
Bestehende Struktur: SectionHeader, Table, TableHead, TableBody, TableRow, Badge, Button, etc. aus @/components/ui
readErrorMessage-Hilfsfunktion (Z. 39–43)
Bestehende Member-Daten: Zugriff auf hist_fansub_group_members (historische Mitglieder der Gruppe)
Erweiterungspunkte: Am Ende der Seitenstruktur neue Bereiche anhängen
Neue Imports aus lucide-react: Copy, Link2, UserCheck, UserX, FilePlus

<!-- Aus frontend/src/lib/api.ts (nach Plan 04) -->
generateClaimInvitation(fansubId: number, memberId: number): Promise<GenerateClaimInvitationResponse>
listPendingMemberClaims(fansubId: number): Promise<MemberClaimRow[]>
verifyMemberClaim(fansubId: number, claimId: number): Promise<void>
rejectMemberClaim(fansubId: number, claimId: number): Promise<void>
listMemberRequests(): Promise<MemberRequestRow[]>
approveMemberRequest(requestId: number, payload: { nickname: string }): Promise<void>
rejectMemberRequest(requestId: number): Promise<void>
searchHistoricalMembers(q: string): Promise<MemberSearchResult[]>
submitMemberClaim(payload: { member_id: number; note?: string }): Promise<MemberClaimRow>
submitMemberRequest(payload: { note?: string }): Promise<MemberRequestRow>

<!-- UI-SPEC.md §4 — Einladungslink-Anzeige -->
Nach Generierung: readonly input mit Link + "Link kopieren"-Button (navigator.clipboard.writeText → 1,5s "Kopiert!")
Einladungslink-Hilfstext: "Teile diesen Link direkt mit dem Mitglied (z. B. via Discord). Der Link läuft in 7 Tagen ab."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: claim-invitations/accept/page.tsx + MemberClaimSection anlegen</name>
  <files>
    frontend/src/app/claim-invitations/accept/page.tsx
    frontend/src/app/me/profile/components/MemberClaimSection.tsx
  </files>
  <read_first>
    - frontend/src/app/invitations/accept/page.tsx — vollständige Datei: exaktes Analog; alle State-Variablen, handleAccept-Muster, Suspense-Wrapper
    - frontend/src/lib/api.ts — acceptClaimInvitation, searchHistoricalMembers, submitMemberClaim, submitMemberRequest Signaturen (nach Plan 04)
    - frontend/src/lib/useAuthSession.ts — useAuthSession-Hook Signatur und hasAccessToken-Verwendung
    - frontend/src/app/me/profile/page.tsx — Side-Column-Struktur; wo MemberClaimSection eingebunden wird
    - frontend/src/types/profile.ts — MemberSearchResult, MemberClaimRow, MemberRequestRow (nach Plan 04)
  </read_first>
  <action>
    Erstelle `frontend/src/app/claim-invitations/accept/page.tsx` analog zu `frontend/src/app/invitations/accept/page.tsx`:

    Struktur:
    - 'use client'
    - Imports: Suspense, useRouter (next/navigation), useSearchParams, useMemo, useState
    - Imports: acceptClaimInvitation, ApiError from '@/lib/api'
    - Import: useAuthSession from '@/lib/useAuthSession'
    - Import: Link from 'next/link'

    Inner-Komponente AcceptClaimInvitationContent:
    - token: useMemo(() => searchParams.get('token') || '', [searchParams]).trim()
    - { hasAccessToken, isClientInitialized } = useAuthSession()
    - State: isSubmitting, errorMessage, successMessage

    handleAccept():
    - Guard: if (!token || !hasAccessToken) return
    - setIsSubmitting(true), clearMessages
    - await acceptClaimInvitation({ token })
    - setSuccessMessage('Dein Account ist jetzt als Mitglied verifiziert. Dein Profil ist ab sofort öffentlich indexierbar.')
    - router.replace('/me/profile')  ← Token aus URL entfernen
    - catch ApiError per error.reason_code oder error.code:
      'invitation_expired' → "Dieser Einladungslink ist abgelaufen. Bitte bitte deinen Leader, einen neuen Link zu erstellen."
      'invitation_used' → "Diese Einladung wurde bereits verwendet."
      'already_verified' → "Du bist bereits einem historischen Eintrag zugeordnet."
      default → error.message || "Aktion konnte nicht durchgeführt werden. Bitte versuche es erneut."

    Rendering:
    - Layout: <main style={{ maxWidth: 720, margin: '0 auto', padding: '48px 20px' }}>
    - <h1>Member-Claim-Einladung annehmen</h1>
    - Kein Token: Fehlerblock "Im Link fehlt ein gültiges Einladungs-Token."
    - Token + !isClientInitialized: Loading-State "Einladung wird geladen..."
    - Token + !hasAccessToken: Login-Link mit return_to:
      <Link href={`/login?return_to=${encodeURIComponent('/claim-invitations/accept?token=' + token)}`}>Anmelden oder registrieren</Link>
      Erklärungstext: "Bitte melde dich zuerst an oder erstelle einen Account — du wirst danach automatisch zurückgeleitet."
    - Token + hasAccessToken: Button "Einladung annehmen" (loading: "Einladung wird angenommen...") disabled={isSubmitting}
    - successMessage: grüner Infoblock + <Link href="/me/profile">Zum Profil</Link>
    - errorMessage: roter Fehlerblock

    Wrapper AcceptClaimInvitationPage:
    return <Suspense fallback={<main>Einladung wird geladen...</main>}><AcceptClaimInvitationContent /></Suspense>

    ---

    Erstelle `frontend/src/app/me/profile/components/MemberClaimSection.tsx`:
    'use client'
    Nick-Suche + Claim-einreichen + Neuanlage-Antrag-Flow (D-03).

    Props:
    type MemberClaimSectionProps = {
      currentClaim?: MemberClaimRow | null
      authToken?: string
      disabled?: boolean
    }

    State:
    - searchQuery: string — Sucheingabe
    - searchResults: MemberSearchResult[] — Suchtreffer
    - isSearching: boolean
    - selectedMember: MemberSearchResult | null — ausgewählter Treffer
    - claimNote: string — optionale Notiz
    - isSubmitting: boolean
    - errorMessage: string | null
    - successMessage: string | null
    - showRequestForm: boolean — wechselt in Neuanlage-Antrag-Modus (D-03)
    - requestNote: string

    Ablauf (Rendering per UI-SPEC.md §2):
    1. Falls currentClaim.claim_status === 'verified': kein Claim-Bereich, Member bereits verifiziert.
    2. Nick-Suche: <input type="search" placeholder="Nickname suchen..." onChange={(e) => handleSearch(e.target.value)} />
       handleSearch: debounce 300ms → searchHistoricalMembers(q, authToken) → setSearchResults
       Ergebnis-Liste: Treffer anklicken → setSelectedMember
    3. Ausgewählter Treffer: "Das bin ich" → selectedMember + <textarea> für Notiz + Button "Claim einreichen"
       handleSubmitClaim(): submitMemberClaim({member_id: selectedMember.id, note: claimNote}, authToken)
       Erfolg: setSuccessMessage("Dein Claim wurde eingereicht. Warte auf Bestätigung durch den Leader.")
    4. "Keinen passenden Eintrag gefunden?" → Button "Neuanlage beantragen" (D-03)
       Zeigt showRequestForm=true: <textarea placeholder="Optionale Notiz..." /> + Button "Antrag stellen"
       handleSubmitRequest(): submitMemberRequest({note: requestNote}, authToken)
       Erfolg: setSuccessMessage("Dein Neuanlage-Antrag wurde eingereicht. Leader/Admin prüfen deinen Antrag.")

    Alle Fehlermeldungen auf Deutsch mit korrekten Umlauten.
    Datei-Länge: Ziel unter 200 Zeilen.

    Ergänze außerdem me/profile/page.tsx (minimaler Eingriff):
    - Import { MemberClaimSection } aus './components/MemberClaimSection'
    - Falls kein verified claim: MemberClaimSection in Side-Column oder eigenen Abschnitt einbinden
    - currentClaim={myClaim} (State aus getMyMemberClaim API-Aufruf — bereits oder neu in page.tsx laden)
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | grep -i "accept\|MemberClaim" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` ohne Fehler in claim-invitations/accept/page.tsx und MemberClaimSection.tsx
    - claim-invitations/accept/page.tsx: handleAccept ruft acceptClaimInvitation auf; router.replace('/me/profile') nach Erfolg; Login-Link mit encodeURIComponent(return_to)
    - MemberClaimSection.tsx: searchHistoricalMembers aufgerufen; submitMemberClaim für Treffer; submitMemberRequest bei "kein Treffer" (D-03)
    - Kein 'use server' in accept/page.tsx; 'use client' vorhanden
    - Deutsche Fehlermeldungen mit korrekten Umlauten
    - MemberClaimSection.tsx unter 250 Zeilen
  </acceptance_criteria>
  <done>accept/page.tsx mit vollständigem Einlösungs-Flow; MemberClaimSection mit Nick-Suche, Claim-einreichen und Neuanlage-Antrag (D-03); TypeScript sauber.</done>
</task>

<task type="auto">
  <name>Task 2: manage/groups/[id] um Claim-Einladungsbereich, Claim-Queue und Neuanlage-Antrags-Queue erweitern</name>
  <files>frontend/src/app/admin/my-groups/[id]/page.tsx</files>
  <read_first>
    - frontend/src/app/admin/my-groups/[id]/page.tsx — vollständige Datei: bestehende Struktur, State-Variablen, API-Aufrufe, SectionHeader-Verwendung, historische Member-Daten (hist_fansub_group_members), readErrorMessage, UI-Komponenten (Table, Badge, Button, EmptyState, LoadingState)
    - frontend/src/lib/api.ts — generateClaimInvitation, listPendingMemberClaims, verifyMemberClaim, rejectMemberClaim, listMemberRequests, approveMemberRequest, rejectMemberRequest Signaturen (nach Plan 04)
    - frontend/src/types/profile.ts — MemberClaimRow, GenerateClaimInvitationResponse, MemberRequestRow (nach Plan 04)
  </read_first>
  <action>
    Ergänze `frontend/src/app/admin/my-groups/[id]/page.tsx` mit drei neuen Bereichen am Ende der Seitenstruktur.

    Neue Imports:
    - { Copy, Link2, UserCheck, UserX, FilePlus } aus 'lucide-react'
    - { generateClaimInvitation, listPendingMemberClaims, verifyMemberClaim, rejectMemberClaim, listMemberRequests, approveMemberRequest, rejectMemberRequest } aus '@/lib/api'
    - { GenerateClaimInvitationResponse, MemberClaimRow, MemberRequestRow } aus '@/types/profile'

    Neuer State:
    - pendingClaims: MemberClaimRow[] — initial leer, beim Laden gefüllt
    - memberRequests: MemberRequestRow[] — initial leer, beim Laden gefüllt (D-03)
    - generatedInvites: Record<number, GenerateClaimInvitationResponse> — member_id → Einladung
    - copyStates: Record<number, boolean> — member_id → isCopied (1,5s "Kopiert!"-Feedback)
    - claimQueueLoading: boolean
    - approveNicknames: Record<number, string> — requestId → Nickname-Input für Approve (D-03)

    Beim Laden der Seite (useEffect mit groupId):
    - listPendingMemberClaims(groupId) → setPendingClaims
    - listMemberRequests() → setMemberRequests (D-03 — globale Liste, nach Leader-Scope gefiltert)

    Handler handleGenerateInvitation(memberId: number):
    - await generateClaimInvitation(groupId, memberId)
    - setGeneratedInvites(prev => ({...prev, [memberId]: result.data}))

    Handler handleCopyLink(memberId: number, link: string):
    - navigator.clipboard.writeText(link)
    - setCopyStates(prev => ({...prev, [memberId]: true}))
    - setTimeout(() => setCopyStates(prev => ({...prev, [memberId]: false})), 1500)

    Handler handleVerifyClaim(claimId: number):
    - await verifyMemberClaim(groupId, claimId)
    - setPendingClaims(prev => prev.filter(c => c.id !== claimId))
    - catch: setErrorMessage(readErrorMessage(err, "Claim konnte nicht bestätigt werden."))

    Handler handleRejectClaim(claimId: number, memberNick: string):
    - if (!window.confirm(`Claim für "${memberNick}" ablehnen?`)) return
    - await rejectMemberClaim(groupId, claimId)
    - setPendingClaims(prev => prev.filter(c => c.id !== claimId))

    Handler handleApproveRequest(requestId: number) — D-03:
    - nickname = approveNicknames[requestId]?.trim()
    - if (!nickname) { setErrorMessage("Nickname für den neuen Eintrag ist erforderlich."); return }
    - if (!window.confirm(`Neuanlage-Antrag mit Nickname "${nickname}" bestätigen?`)) return
    - await approveMemberRequest(requestId, { nickname })
    - setMemberRequests(prev => prev.filter(r => r.id !== requestId))

    Handler handleRejectRequest(requestId: number) — D-03:
    - if (!window.confirm('Neuanlage-Antrag ablehnen?')) return
    - await rejectMemberRequest(requestId)
    - setMemberRequests(prev => prev.filter(r => r.id !== requestId))

    Rendering (per UI-SPEC.md §4) — drei neue Bereiche am Ende der Seitenstruktur:

    Bereich 1 — "Member-Claim-Einladungen":
    <SectionHeader title="Member-Claim-Einladungen" />
    Erklärungstext: "Generiere Einladungslinks für historische Mitglieder und teile sie direkt (z. B. via Discord)."
    Falls historische Member vorhanden (hist_fansub_group_members-Daten der Seite):
      Pro Member:
        <Toolbar> <span>{member.nickname}</span>
          <Button variant="secondary" onClick={() => handleGenerateInvitation(member.id)} leftIcon={<Link2 size={16} />}>
            Einladungslink generieren
          </Button>
        </Toolbar>
        Falls generatedInvites[member.id]:
          <Card variant="section">
            <label>Einladungslink (7 Tage gültig)</label>
            <div style={{display:'flex',gap:'8px',alignItems:'center'}}>
              <input type="text" readOnly value={generatedInvites[member.id].invite_link} style={{flex:1}} />
              <Button variant="secondary" onClick={() => handleCopyLink(member.id, generatedInvites[member.id].invite_link)} leftIcon={<Copy size={16} />}>
                {copyStates[member.id] ? 'Kopiert!' : 'Link kopieren'}
              </Button>
            </div>
            <p>Teile diesen Link direkt mit dem Mitglied (z. B. via Discord). Der Link läuft in 7 Tagen ab.</p>
          </Card>
    Falls keine historischen Member-Daten auf dieser Seite vorhanden: Erklärungstext statt Member-Liste.

    Bereich 2 — "Offene Claims ({pendingClaims.length})":
    <SectionHeader title={`Offene Claims (${pendingClaims.length})`} />
    {pendingClaims.length === 0 ? <EmptyState description="Keine offenen Claims." /> :
      <Table>
        <TableHead>App-User-ID | Beanspruchter Nick | Notiz | Eingereicht am | Aktionen</TableHead>
        <TableBody>
          {pendingClaims.map(claim => (
            <TableRow key={claim.id}>
              <td>{claim.app_user_id}</td>
              <td>{claim.member_nickname}</td>
              <td>{claim.note || '—'}</td>
              <td>{new Date(claim.created_at).toLocaleDateString('de-DE')}</td>
              <td>
                <Button size="sm" variant="success" onClick={() => handleVerifyClaim(claim.id)} leftIcon={<UserCheck size={16}/>}>Bestätigen</Button>
                <Button size="sm" variant="danger" onClick={() => handleRejectClaim(claim.id, claim.member_nickname)} leftIcon={<UserX size={16}/>}>Ablehnen</Button>
              </td>
            </TableRow>
          ))}
        </TableBody>
      </Table>}

    Bereich 3 — "Neuanlage-Anträge ({memberRequests.length})" — D-03:
    <SectionHeader title={`Neuanlage-Anträge (${memberRequests.length})`} />
    <p>Mitglieder, die keinen passenden historischen Eintrag gefunden haben, können hier eine Neuanlage beantragen.</p>
    {memberRequests.length === 0 ? <EmptyState description="Keine offenen Neuanlage-Anträge." /> :
      <Table>
        <TableHead>App-User-ID | Notiz | Eingereicht am | Nickname für Neuanlage | Aktionen</TableHead>
        <TableBody>
          {memberRequests.map(req => (
            <TableRow key={req.id}>
              <td>{req.app_user_id}</td>
              <td>{req.note || '—'}</td>
              <td>{new Date(req.created_at).toLocaleDateString('de-DE')}</td>
              <td>
                <input type="text" placeholder="Nickname eingeben..."
                  value={approveNicknames[req.id] || ''}
                  onChange={(e) => setApproveNicknames(prev => ({...prev, [req.id]: e.target.value}))} />
              </td>
              <td>
                <Button size="sm" variant="success" onClick={() => handleApproveRequest(req.id)} leftIcon={<FilePlus size={16}/>}>Anlegen</Button>
                <Button size="sm" variant="danger" onClick={() => handleRejectRequest(req.id)} leftIcon={<UserX size={16}/>}>Ablehnen</Button>
              </td>
            </TableRow>
          ))}
        </TableBody>
      </Table>}

    Halte Datei unter 450 Zeilen — bei Bedarf Handler-Logik in separate Hilfsfunktionen auslagern.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` ohne neue Fehler
    - Datei enthält SectionHeader "Member-Claim-Einladungen", "Offene Claims" und "Neuanlage-Anträge" (D-03)
    - handleGenerateInvitation ruft generateClaimInvitation auf
    - handleCopyLink setzt 1,5s "Kopiert!"-State via setTimeout
    - handleVerifyClaim und handleRejectClaim rufen verifyMemberClaim/rejectMemberClaim auf
    - handleApproveRequest und handleRejectRequest für D-03 Neuanlage-Antrags-Queue implementiert
    - Confirm-Dialog bei Ablehnen: window.confirm mit deutschem Text
    - Leere Zustände mit EmptyState "Keine offenen Claims." und "Keine offenen Neuanlage-Anträge."
    - Datei unter 450 Zeilen
  </acceptance_criteria>
  <done>manage/groups/[id] mit Einladungslink-Generator, Kopier-Button (1,5s-Feedback), Claim-Queue (Bestätigen/Ablehnen) und Neuanlage-Antrags-Queue (D-03); build grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Grenze | Beschreibung |
|--------|-------------|
| Browser → /claim-invitations/accept | Kein Auth-Token → Login-Redirect mit return_to; kein API-Call ohne Token |
| Leader → generateClaimInvitation | Frontend schickt nur authenticated requests via authorizedFetch |
| Admin → approveMemberRequest | Nickname-Eingabe vom Leader wird an Backend gesendet; Backend prüft Admin-Scope |
| Clipboard API | navigator.clipboard.writeText schreibt rawToken-URL in Clipboard — kein Server-Zugriff |

## STRIDE Threat Register

| Threat ID | Kategorie | Disposition | Mitigationsplan |
|-----------|-----------|-------------|-----------------|
| T-66-06-01 | Information Disclosure | mitigate | Token in URL-Parameter: nach Erfolg sofort router.replace('/me/profile') — Token nicht im Browser-Verlauf als aktiv markiert |
| T-66-06-02 | Spoofing | mitigate | Login-Link enthält return_to mit encodeURIComponent — verhindert Open-Redirect durch URL-Encoding |
| T-66-06-03 | Elevation of Privilege | mitigate | verifyMemberClaim/rejectMemberClaim-Aufrufe durch authorizedFetch — Backend prüft Leader-Berechtigung |
| T-66-06-04 | Tampering | accept | Confirm-Dialog für Ablehnen ist client-seitig — Backend blockiert unberechtigte Rejects via permissionSvc |
| T-66-06-05 | Tampering | mitigate | approveMemberRequest mit nickname-Pflichtfeld + Confirm-Dialog; Backend validiert Admin-Scope und führt INSERT in Transaktion aus |
| T-66-06-SC | Tampering | accept | Keine neuen npm-Pakete; lucide-react bestehend |

</threat_model>

<verification>
1. `npm run build` ohne neue Fehler
2. `npx tsc --noEmit` ohne neue Fehler in diesen 3 Dateien
3. Grep claim-invitations/accept/page.tsx: "router.replace" und "return_to" vorhanden
4. Grep my-groups/[id]/page.tsx: "generateClaimInvitation", "listPendingMemberClaims", "listMemberRequests" vorhanden
5. Grep my-groups/[id]/page.tsx: "Kopiert!" (1,5s-Feedback) und "Neuanlage-Anträge" vorhanden
6. Grep MemberClaimSection.tsx: "submitMemberRequest" (D-03) und "searchHistoricalMembers" vorhanden
</verification>

<success_criteria>
Frontend-Build grün. Einlösungs-Seite mit korrektem Login-Redirect und Token-aus-URL-Entfernung. Leader-Bereich mit Einladungslink-Generator, Claim-Queue und Neuanlage-Antrags-Queue (D-03). MemberClaimSection mit Self-Service-Claim und Neuanlage-Antrag-Button.
</success_criteria>

<output>
Erstelle `.planning/phases/66-claiming-verifizierung/66-06-SUMMARY.md` wenn fertig.
</output>
