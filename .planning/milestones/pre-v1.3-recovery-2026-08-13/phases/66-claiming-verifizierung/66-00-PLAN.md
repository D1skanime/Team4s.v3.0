---
phase: 66-claiming-verifizierung
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/internal/handlers/member_claims_handler_test.go
  - backend/internal/repository/member_claims_repository_test.go
  - backend/internal/repository/member_claim_invitations_repository_test.go
  - backend/internal/handlers/member_profile_noindex_test.go
  - frontend/src/components/profile/VerifiedBadge.test.tsx
autonomous: true
requirements:
  - P66-SC1
  - P66-SC2
  - P66-SC3
must_haves:
  truths:
    - "5 Test-Stub-Dateien existieren bevor Implementierungspläne (Wave 1+) laufen"
    - "Backend-Stubs kompilieren via go build ./internal/..."
    - "Frontend-Stub wird von Vitest erkannt (npm test -- --run VerifiedBadge gibt 'no tests' oder skip)"
  artifacts:
    - path: backend/internal/handlers/member_claims_handler_test.go
      provides: "Test-Stubs für P66-SC1 (Self-Service-Claim + VerifyClaim)"
      contains: "TestMemberClaim"
    - path: backend/internal/repository/member_claims_repository_test.go
      provides: "Test-Stubs für UNIQUE- und 1-verified-Invariante"
      contains: "TestMemberClaimsRepository"
    - path: backend/internal/repository/member_claim_invitations_repository_test.go
      provides: "Test-Stubs für Token-Hash, Ablauf, Lifecycle (P66-SC2)"
      contains: "TestMemberClaimInvitation"
    - path: backend/internal/handlers/member_profile_noindex_test.go
      provides: "Test-Stubs für noindex-PATCH (P66-SC3)"
      contains: "TestNoindex"
    - path: frontend/src/components/profile/VerifiedBadge.test.tsx
      provides: "Vitest-Stub für verified-Badge (P66-SC3)"
      contains: "VerifiedBadge"
  key_links:
    - from: "backend/internal/handlers/member_claims_handler_test.go"
      to: "backend/internal/handlers/member_claims_handler.go (Wave 3)"
      via: "package handlers — gleiche Package-Deklaration"
      pattern: "package handlers"
    - from: "frontend/src/components/profile/VerifiedBadge.test.tsx"
      to: "frontend/src/components/profile/VerifiedBadge.tsx (Wave 5)"
      via: "import { VerifiedBadge } from './VerifiedBadge'"
      pattern: "VerifiedBadge"
---

<objective>
Wave-0-Plan: 5 Test-Stub-Dateien anlegen, BEVOR die Implementierungspläne (Wave 1+) ausgeführt werden. Die Stubs definieren die Test-IDs und Erwartungen; sie schlagen zunächst fehl (RED), weil die zu testenden Einheiten noch nicht existieren. Wave 1+ Implementierungspläne bringen sie auf GREEN.

Purpose: Nyquist-Compliance erfordert, dass jede Implementierung einen vorab definierten automatisierten Test hat (VALIDATION.md nyquist_compliant: true).
Output: 5 Stub-Dateien, kompilierbar (Backend) / erkennbar (Frontend).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/66-claiming-verifizierung/66-CONTEXT.md
@.planning/phases/66-claiming-verifizierung/66-VALIDATION.md
@.planning/phases/66-claiming-verifizierung/66-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend-Test-Stubs anlegen (4 Dateien)</name>
  <files>
    backend/internal/handlers/member_claims_handler_test.go
    backend/internal/repository/member_claims_repository_test.go
    backend/internal/repository/member_claim_invitations_repository_test.go
    backend/internal/handlers/member_profile_noindex_test.go
  </files>
  <read_first>
    - backend/internal/handlers/app_auth_test.go — Konventionen für Handler-Tests im package handlers: imports, testing.T-Muster, Test-Funktions-Naming
    - backend/internal/repository/ — ein beliebiger *_test.go: Repository-Test-Konventionen, package-Deklaration
  </read_first>
  <action>
    Erstelle vier Go-Test-Stub-Dateien. Jede Datei enthält nur die Package-Deklaration, nötige Imports und leere t.Skip()-Test-Funktionen (RED: schlagen mit t.Skip fehl, werden nach Implementierung durch echte Assertions ersetzt).

    `backend/internal/handlers/member_claims_handler_test.go`:
    ```
    package handlers
    import "testing"
    func TestMemberClaimSearchMembers(t *testing.T)    { t.Skip("stub — wird in Wave 3 implementiert") }
    func TestMemberClaimSubmitClaim(t *testing.T)      { t.Skip("stub — wird in Wave 3 implementiert") }
    func TestVerifyClaimHandler(t *testing.T)          { t.Skip("stub — wird in Wave 3 implementiert") }
    func TestVerifyClaimAlreadyVerified(t *testing.T)  { t.Skip("stub — wird in Wave 3 implementiert") }
    ```

    `backend/internal/repository/member_claims_repository_test.go`:
    ```
    package repository
    import "testing"
    func TestMemberClaimsRepositorySearchHistoricalMembers(t *testing.T) { t.Skip("stub") }
    func TestMemberClaimsRepositorySubmitClaimUniqueInvariant(t *testing.T) { t.Skip("stub") }
    func TestMemberClaimsRepositoryVerifyClaimOneVerifiedInvariant(t *testing.T) { t.Skip("stub") }
    func TestMemberClaimsRepositoryRejectClaim(t *testing.T) { t.Skip("stub") }
    ```

    `backend/internal/repository/member_claim_invitations_repository_test.go`:
    ```
    package repository
    import "testing"
    func TestMemberClaimInvitationTokenHash(t *testing.T)      { t.Skip("stub") }
    func TestMemberClaimInvitationCreateAndAccept(t *testing.T){ t.Skip("stub") }
    func TestMemberClaimInvitationExpiry(t *testing.T)         { t.Skip("stub") }
    func TestMemberClaimInvitationCancelledState(t *testing.T) { t.Skip("stub") }
    ```

    `backend/internal/handlers/member_profile_noindex_test.go`:
    ```
    package handlers
    import "testing"
    func TestNoindexPatchOwnerOnly(t *testing.T)     { t.Skip("stub — wird in Wave 3 implementiert") }
    func TestNoindexPatchUnauthorized(t *testing.T)  { t.Skip("stub — wird in Wave 3 implementiert") }
    ```

    Keine Imports von noch nicht existierenden Packages — nur "testing". Keine echten Assertions (die Implementierungen existieren noch nicht).
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go build ./internal/handlers/... ./internal/repository/... 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - Alle 4 Dateien existieren mit korrekter package-Deklaration
    - `go build ./internal/handlers/... ./internal/repository/...` ohne Fehler
    - Dateien enthalten t.Skip()-Stubs, keine echten Implementierungs-Imports
    - Test-Funktionsnamen entsprechen VALIDATION.md Per-Task-Verification-Map (TestMemberClaim*, TestMemberClaimsRepository*, TestMemberClaimInvitation*, TestNoindex*)
  </acceptance_criteria>
  <done>4 Backend-Stubs existieren und kompilieren; go build grün.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend-Vitest-Stub für VerifiedBadge anlegen</name>
  <files>frontend/src/components/profile/VerifiedBadge.test.tsx</files>
  <read_first>
    - frontend/vitest.config.ts — Test-Konfig, um Glob-Pattern und Umgebungseinstellungen zu verstehen
    - frontend/src/components/ — ein beliebiger *.test.tsx: Import-Muster, describe/it-Struktur, Vitest-Imports
  </read_first>
  <action>
    Erstelle `frontend/src/components/profile/VerifiedBadge.test.tsx` als Vitest-Stub.

    Inhalt (Stub — VerifiedBadge.tsx existiert noch nicht, daher mit vi.mock oder conditional import):

    ```tsx
    import { describe, it } from 'vitest'

    // Stub: VerifiedBadge.tsx wird in Wave 5 (Plan 05) implementiert.
    // Dieser Test wird nach Implementierung mit echten Assertions ersetzt.

    describe('VerifiedBadge', () => {
      it.todo('zeigt CheckCircle-Icon wenn is_verified=true')
      it.todo('enthält aria-label "Verifiziertes Mitglied"')
      it.todo('rendert das "Verifiziert"-Label als Text')
    })
    ```

    Kein Import von './VerifiedBadge' — die Datei existiert noch nicht und würde den Build brechen. it.todo() markiert Tests als ausstehend ohne sie auszuführen.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npm test -- --run VerifiedBadge 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `VerifiedBadge.test.tsx` existiert mit describe('VerifiedBadge', ...) Block
    - `npm test -- --run VerifiedBadge` läuft ohne Fehler (todo-Tests werden als "skipped" oder "todo" gemeldet, kein Crash)
    - Kein Import von nicht-existierendem './VerifiedBadge'
  </acceptance_criteria>
  <done>VerifiedBadge.test.tsx existiert; Vitest erkennt die Datei ohne Build-Fehler.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Grenze | Beschreibung |
|--------|-------------|
| Test-Stubs | Nur package-deklarationen und t.Skip — kein produktiver Code; kein Sicherheitsrisiko |

## STRIDE Threat Register

| Threat ID | Kategorie | Komponente | Disposition | Mitigationsplan |
|-----------|-----------|-----------|-------------|-----------------|
| T-66-00-01 | Tampering | Test-Stubs mit echter Impl-Logik | mitigate | Stubs enthalten nur t.Skip() / it.todo() — keine Imports von Production-Code, der noch nicht existiert |
| T-66-00-SC | Tampering | keine npm/pip/cargo Installs | accept | Nur neue Testdateien; keine Paket-Installationen |

</threat_model>

<verification>
1. Alle 5 Stub-Dateien existieren in den korrekten Verzeichnissen
2. `cd backend && go build ./internal/handlers/... ./internal/repository/...` — EXIT 0
3. `cd frontend && npm test -- --run VerifiedBadge` — kein Fehler-Exit, nur todo-Reports
4. Keine Imports von nicht-existierenden Modulen in den Stubs
</verification>

<success_criteria>
5 Test-Stub-Dateien existieren. Backend-Build grün. Vitest erkennt den Frontend-Stub. Alle Stubs verwenden t.Skip() / it.todo() und importieren keine noch nicht existierenden Produktions-Module. Nyquist-Anforderung für Wave 0 erfüllt.
</success_criteria>

<output>
Erstelle `.planning/phases/66-claiming-verifizierung/66-00-SUMMARY.md` wenn fertig.
</output>
