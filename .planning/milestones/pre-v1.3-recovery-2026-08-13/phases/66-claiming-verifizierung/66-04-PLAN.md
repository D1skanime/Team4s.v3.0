---
phase: 66-claiming-verifizierung
plan: 04
type: execute
wave: 4
depends_on:
  - 66-03
files_modified:
  - backend/internal/repository/member_profile_repository.go
  - backend/internal/models/member_profile.go
  - backend/internal/repository/member_requests_repository.go
  - backend/internal/handlers/member_requests_handler.go
  - backend/cmd/server/main.go
  - backend/cmd/server/admin_routes.go
  - frontend/src/lib/api.ts
  - frontend/src/types/profile.ts
autonomous: true
requirements:
  - P66-SC1
  - P66-SC2
  - P66-SC3
must_haves:
  truths:
    - "GET /api/v1/members/:slug liefert noindex und is_verified im Response-Body"
    - "PublicMemberProfile-Model enthält Noindex bool und IsVerified bool"
    - "GetPublicMemberProfile() führt LEFT JOIN auf member_claims WHERE claim_status='verified' durch"
    - "POST /me/member-requests reicht Neuanlage-Antrag ein (member_claims mit member_id=NULL, D-03)"
    - "GET /admin/member-requests listet Neuanlage-Anträge für Leader/Admin"
    - "POST /admin/member-requests/:id/approve erstellt historischen Member + verknüpft Claim"
    - "POST /admin/member-requests/:id/reject lehnt Neuanlage-Antrag ab"
    - "Alle neuen Backend-Routen in main.go/admin_routes.go verdrahtet"
    - "Frontend-API-Funktionen und Typ-Erweiterungen bereit für Pläne 05 und 06"
  artifacts:
    - path: backend/internal/repository/member_profile_repository.go
      provides: "GetPublicMemberProfile mit noindex + is_verified im SELECT"
      contains: "is_verified"
    - path: backend/internal/models/member_profile.go
      provides: "PublicMemberProfile mit Noindex + IsVerified Feldern"
      contains: "IsVerified"
    - path: backend/internal/repository/member_requests_repository.go
      provides: "CRUD für Neuanlage-Anträge (D-03): Submit, List, Approve, Reject"
      exports:
        - NewMemberRequestsRepository
        - MemberRequestsRepository
    - path: backend/internal/handlers/member_requests_handler.go
      provides: "HTTP-Handler für Neuanlage-Antrag-Flow (D-03)"
      exports:
        - NewMemberRequestsHandler
        - MemberRequestsHandler
    - path: backend/cmd/server/main.go
      provides: "Repo/Handler-Konstruktoren und me/admin-Routen für Claims, Einladungen, noindex, Neuanlage-Anträge"
      contains: "NewMemberClaimsRepository"
    - path: backend/cmd/server/admin_routes.go
      provides: "Admin-Routen für Claim-Queue, Einladungslink und Neuanlage-Anträge"
      contains: "member-claims"
    - path: frontend/src/lib/api.ts
      provides: "API-Funktionen für alle Claim-/Einladungs-/noindex-/Neuanlage-Endpunkte"
      contains: "acceptClaimInvitation"
    - path: frontend/src/types/profile.ts
      provides: "Typerweiterungen für verified/noindex-Felder + MemberRequest-Typen"
      contains: "is_verified"
  key_links:
    - from: "frontend/src/app/claim-invitations/accept/page.tsx (Plan 06)"
      to: "frontend/src/lib/api.ts acceptClaimInvitation"
      via: "import { acceptClaimInvitation } from '@/lib/api'"
      pattern: "acceptClaimInvitation"
    - from: "frontend/src/app/members/[slug]/page.tsx (Plan 05)"
      to: "frontend/src/types/profile.ts PublicMemberProfileData"
      via: "profile.is_verified, profile.noindex"
      pattern: "is_verified.*noindex"
    - from: "frontend/src/app/me/profile/page.tsx (Plan 05)"
      to: "frontend/src/lib/api.ts patchNoindex"
      via: "import { patchNoindex } from '@/lib/api'"
      pattern: "patchNoindex"
---

<objective>
Zwei Kernlücken schließen und Neuanlage-Antrag-Flow (D-03) implementieren:

1. **Blocker 2**: `GetPublicMemberProfile()` um `m.noindex` und `is_verified` (via LEFT JOIN auf member_claims) erweitern; `PublicMemberProfile`-Model um beide Felder ergänzen — macht generateMetadata() und VerifiedBadge (Plan 05) funktionsfähig.

2. **D-03 Backend**: Neuanlage-Antrag-Flow — `member_claims` mit `member_id=NULL` für "kein Treffer in Nick-Suche"-Fall. Repository + Handler + Routen.

3. **Verdrahtung**: Alle Repos/Handler in main.go + admin_routes.go eintragen. Frontend-API-Client (api.ts) und Typen (profile.ts) bereitstellen.

Purpose: Plan 05 und 06 brauchen beide die is_verified/noindex-Response sowie den fertigen API-Client.
Output: 8 Dateien modifiziert, Backend-Build und TypeScript-Check grün.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/66-claiming-verifizierung/66-CONTEXT.md
@.planning/phases/66-claiming-verifizierung/66-RESEARCH.md
@.planning/phases/66-claiming-verifizierung/66-PATTERNS.md
@.planning/phases/66-claiming-verifizierung/66-03-SUMMARY.md

<interfaces>
<!-- PublicMemberProfile in backend/internal/models/member_profile.go -->
Aktuell: MemberID, AppUserID, FansubName, Bio, MemberStoryHTML, ActiveFromDate, ActiveUntilDate,
  IsCurrentlyActive, ProfileVisibility, Avatar, BackgroundImage, Memberships, RecentMedia, RecentContributions
Neu ergänzen:
  Noindex    bool `json:"noindex"`
  IsVerified bool `json:"is_verified"`

<!-- GetPublicMemberProfile in backend/internal/repository/member_profile_repository.go (Z. 343–465) -->
Aktuelles SELECT in candidates-CTE:
  m.id, au.id AS app_user_id, m.nickname, m.slogan, m.member_story_html, ...,
  profile_visibility, avatar_path, background_image_path, db_slug
Erweiterung:
  m.noindex — direkt aus members-Tabelle
  EXISTS(SELECT 1 FROM member_claims mc WHERE mc.member_id=m.id AND mc.claim_status='verified') AS is_verified
Scan-Zeilen und publicMemberProfileBaseRow-Struct entsprechend ergänzen.
Nach Scan: profile.Noindex = row.noindex; profile.IsVerified = row.isVerified

<!-- member_requests — Neuanlage-Antrag (D-03, Annahme A2) -->
Tabelle: member_claims (bestehend) mit member_id = NULL
INSERT INTO member_claims (app_user_id, member_id, claim_status, note)
  VALUES ($appUserID, NULL, 'pending', $note)
  — member_id=NULL bedeutet: Neuanlage-Antrag, kein existierender Member
List-Query für Admin: WHERE member_id IS NULL AND claim_status='pending'
Approve: INSERT INTO members (nickname, ...) → member_id erhalten →
  UPDATE member_claims SET member_id=$newMemberID, claim_status='verified', verification_method='admin_created', verified_by=$adminID, verified_at=NOW()
  UPDATE members SET noindex=false WHERE id=$newMemberID
Reject: UPDATE member_claims SET claim_status='rejected', verified_by=$adminID, verified_at=NOW() WHERE id=$requestID AND member_id IS NULL

<!-- Aus backend/cmd/server/main.go — Repo/Handler-Verdrahtungs-Konvention -->
memberClaimsRepo := repository.NewMemberClaimsRepository(dbPool)
memberClaimInvitationsRepo := repository.NewMemberClaimInvitationRepository(dbPool, cfg.AppPublicURL)
memberRequestsRepo := repository.NewMemberRequestsRepository(dbPool)
memberClaimsHandler := handlers.NewMemberClaimsHandler(memberClaimsRepo, permissionSvc, auditLogRepo)
memberClaimInvitationsHandler := handlers.NewMemberClaimInvitationsHandler(memberClaimInvitationsRepo, permissionSvc, auditLogRepo, cfg.AppPublicURL)
memberProfileNoindexHandler := handlers.NewMemberProfileNoindexHandler(memberClaimsRepo)
memberRequestsHandler := handlers.NewMemberRequestsHandler(memberRequestsRepo, permissionSvc, auditLogRepo)

Me-Routen (nach v1.GET("/me/memberships", ...)):
  v1.GET("/me/member-search", authMiddleware, memberClaimsHandler.SearchMembers)
  v1.GET("/me/member-claim", authMiddleware, memberClaimsHandler.GetMyClaim)
  v1.POST("/me/member-claims", authMiddleware, memberClaimsHandler.SubmitClaim)
  v1.POST("/me/member-requests", authMiddleware, memberRequestsHandler.SubmitRequest)
  v1.PATCH("/me/profile/noindex", authMiddleware, memberProfileNoindexHandler.PatchNoindex)
  v1.POST("/claim-invitations/accept", authMiddleware, memberClaimInvitationsHandler.AcceptClaimInvitation)

Admin-Routen (in registerAdminRoutes via adminRouteHandlers):
  struct-Felder: memberClaimsHandler, memberClaimInvitationsHandler, memberProfileNoindexHandler, memberRequestsHandler

<!-- Aus backend/cmd/server/admin_routes.go -->
v1.GET("/admin/fansubs/:id/member-claims", auth, deps.memberClaimsHandler.ListPendingClaimsForGroup)
v1.POST("/admin/fansubs/:id/member-claims/:claimId/verify", auth, deps.memberClaimsHandler.VerifyClaim)
v1.POST("/admin/fansubs/:id/member-claims/:claimId/reject", auth, deps.memberClaimsHandler.RejectClaim)
v1.POST("/admin/fansubs/:id/group-members/:memberId/claim-invitations", auth, deps.memberClaimInvitationsHandler.CreateClaimInvitation)
v1.POST("/admin/fansubs/:id/group-members/:memberId/claim-invitations/:invitationId/cancel", auth, deps.memberClaimInvitationsHandler.CancelClaimInvitation)
v1.GET("/admin/member-requests", auth, deps.memberRequestsHandler.ListRequests)
v1.POST("/admin/member-requests/:requestId/approve", auth, deps.memberRequestsHandler.ApproveRequest)
v1.POST("/admin/member-requests/:requestId/reject", auth, deps.memberRequestsHandler.RejectRequest)

<!-- Frontend API + Typen -->
patchNoindex(noindex: boolean, authToken?: string): Promise<void>
  PATCH /api/v1/me/profile/noindex, JSON body {noindex}
submitMemberRequest(payload: {note?: string}, authToken?: string): Promise<MemberRequestRow>
  POST /api/v1/me/member-requests
listMemberRequests(authToken?: string): Promise<MemberRequestRow[]>
  GET /api/v1/admin/member-requests
approveMemberRequest(requestId: number, payload: {nickname: string}, authToken?: string): Promise<void>
  POST /api/v1/admin/member-requests/:requestId/approve
rejectMemberRequest(requestId: number, authToken?: string): Promise<void>
  POST /api/v1/admin/member-requests/:requestId/reject

MemberRequestRow { id: number; app_user_id: number; claim_status: string; note?: string | null; created_at: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GetPublicMemberProfile + PublicMemberProfile-Model um noindex + is_verified erweitern</name>
  <files>
    backend/internal/models/member_profile.go
    backend/internal/repository/member_profile_repository.go
  </files>
  <read_first>
    - backend/internal/models/member_profile.go — PublicMemberProfile struct (Z. 148–163): Felder, JSON-Tags; hier Noindex bool + IsVerified bool ergänzen
    - backend/internal/repository/member_profile_repository.go — GetPublicMemberProfile (Z. 343–465): candidates-CTE, SELECT-Liste, Scan-Aufruf, publicMemberProfileBaseRow struct; findPublicMemberProfileByNormalizedSlug (Z. 467ff) — ebenfalls anpassen
    - database/migrations/0081_historical_members_identity.up.sql — members.noindex-Spalte: Typ, Default (true)
  </read_first>
  <action>
    Ergänze `backend/internal/models/member_profile.go`:
    - PublicMemberProfile struct um zwei neue Felder erweitern:
      Noindex    bool `json:"noindex"`
      IsVerified bool `json:"is_verified"`

    Ergänze `backend/internal/repository/member_profile_repository.go`:

    1. publicMemberProfileBaseRow (privater Struct für Scan) um zwei Felder erweitern:
       noindex    bool
       isVerified bool

    2. GetPublicMemberProfile — candidates-CTE erweitern:
       Füge in die SELECT-Liste des CTE hinzu:
         m.noindex,
         EXISTS(SELECT 1 FROM member_claims mc WHERE mc.member_id=m.id AND mc.claim_status='verified') AS is_verified
       Ergänze den äußeren SELECT entsprechend (noindex, is_verified weitergeben).
       Ergänze den Scan-Aufruf um &row.noindex und &row.isVerified am Ende.
       Nach Scan: profile.Noindex = row.noindex; profile.IsVerified = row.isVerified

    3. findPublicMemberProfileByNormalizedSlug (Fallback-Abfrage, Z. 467ff) — gleiche Erweiterung:
       SELECT-Liste um m.noindex und EXISTS(...) AS is_verified erweitern.
       Scan und Struct-Felder gleich behandeln.

    Kein Breaking Change: bestehende Profile-Felder bleiben unverändert; nur Erweiterung.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go build ./internal/repository/... ./internal/models/... 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/repository/... ./internal/models/...` ohne Fehler
    - PublicMemberProfile enthält Noindex bool und IsVerified bool (grep)
    - GetPublicMemberProfile-Query enthält "m.noindex" und "IS_VERIFIED" oder "is_verified" (grep, case-insensitive)
    - Scan-Aufruf in GetPublicMemberProfile enthält &row.noindex und &row.isVerified
    - findPublicMemberProfileByNormalizedSlug ebenfalls erweitert
  </acceptance_criteria>
  <done>PublicMemberProfile liefert noindex + is_verified; API /api/v1/members/:slug gibt beide Felder zurück. generateMetadata() (Plan 05) und VerifiedBadge funktionsfähig.</done>
</task>

<task type="auto">
  <name>Task 2: D-03 Neuanlage-Antrag Backend + Verdrahtung + Frontend-Client</name>
  <files>
    backend/internal/repository/member_requests_repository.go
    backend/internal/handlers/member_requests_handler.go
    backend/cmd/server/main.go
    backend/cmd/server/admin_routes.go
    frontend/src/lib/api.ts
    frontend/src/types/profile.ts
  </files>
  <read_first>
    - backend/internal/repository/member_claims_repository.go — member_claims Tabellenschema, SubmitClaim-Muster; Neuanlage nutzt dieselbe Tabelle mit member_id=NULL
    - backend/cmd/server/main.go — vollständige aktuelle Datei (Z. 344–403): bestehende Repos/Handler als Einfüge-Kontext
    - backend/cmd/server/admin_routes.go — vollständige aktuelle Datei: adminRouteHandlers-Struct, Ende der Route-Liste
    - backend/internal/handlers/member_claims_handler.go — Handler-Struct/Konstruktor-Muster als Vorbild
    - frontend/src/types/profile.ts — vollständige Datei: PublicMemberProfileData und MemberProfileData als Erweiterungspunkte
    - frontend/src/lib/api.ts — acceptFansubInvitation oder letzte Funktionen als Muster; authorizedFetch, withAuthHeader, parseApiErrorPayload, ApiError
  </read_first>
  <action>
    Erstelle `backend/internal/repository/member_requests_repository.go` im package `repository`:

    Wiederverwendung von member_claims (member_id=NULL = Neuanlage-Antrag):
    - type MemberRequestsRepository struct { db *pgxpool.Pool }
    - func NewMemberRequestsRepository(db *pgxpool.Pool) *MemberRequestsRepository
    - MemberRequestRow { ID int64; AppUserID int64; ClaimStatus string; Note sql.NullString; CreatedAt time.Time }
    - SubmitRequest(ctx, appUserID int64, note string) (*MemberRequestRow, error)
      INSERT INTO member_claims (app_user_id, member_id, claim_status, note)
      VALUES ($1, NULL, 'pending', $2)
      ON CONFLICT DO NOTHING RETURNING id, app_user_id, claim_status, note, created_at
      (member_id=NULL signalisiert Neuanlage-Antrag, Annahme A2 aus D-03)
    - ListPendingRequests(ctx) ([]MemberRequestRow, error)
      SELECT id, app_user_id, claim_status, note, created_at FROM member_claims
      WHERE member_id IS NULL AND claim_status='pending' ORDER BY created_at ASC
    - ApproveRequest(ctx, requestID int64, adminUserID int64, nickname string) error
      Transaktion:
      - Prüfe: SELECT id, app_user_id FROM member_claims WHERE id=$1 AND member_id IS NULL AND claim_status='pending' FOR UPDATE; pgx.ErrNoRows → ErrNotFound
      - INSERT INTO members (nickname, noindex) VALUES ($nickname, false) RETURNING id → newMemberID
      - UPDATE member_claims SET member_id=$newMemberID, claim_status='verified', verification_method='admin_created', verified_by=$adminUserID, verified_at=NOW(), updated_at=NOW() WHERE id=$requestID
      - Commit
    - RejectRequest(ctx, requestID int64, adminUserID int64) error
      UPDATE member_claims SET claim_status='rejected', verified_by=$adminUserID, verified_at=NOW(), updated_at=NOW()
      WHERE id=$1 AND member_id IS NULL AND claim_status='pending'
      rowsAffected==0 → ErrNotFound

    Erstelle `backend/internal/handlers/member_requests_handler.go` im package `handlers`:
    - type MemberRequestsHandler struct { requestsRepo *repository.MemberRequestsRepository; permissionSvc *permissions.Service; auditLogRepo *repository.AuditLogRepository }
    - func NewMemberRequestsHandler(requestsRepo *repository.MemberRequestsRepository, permissionSvc *permissions.Service, auditLogRepo *repository.AuditLogRepository) *MemberRequestsHandler

    Endpunkte (4 Methoden):
    1. SubmitRequest (POST /me/member-requests)
       Auth: requireMeIdentity
       Body: { note?: string }
       Aufruf: h.requestsRepo.SubmitRequest(ctx, identity.AppUserID, req.Note)
       Erfolg: 201 {"data": {id, claim_status, note, created_at}}

    2. ListRequests (GET /admin/member-requests)
       Auth: Admin-Check (permissionActorFromContext → Admin-Scope oder Leader-Scope)
       Aufruf: h.requestsRepo.ListPendingRequests(ctx)
       Response: {"data": [{id, app_user_id, claim_status, note, created_at}, ...]}

    3. ApproveRequest (POST /admin/member-requests/:requestId/approve)
       Auth: Admin-Check
       Body: { nickname: string } — Pflichtfeld (neuer Member braucht Nick)
       Validation: nickname TrimSpace leer → 400 "Nickname fehlt."
       Aufruf: h.requestsRepo.ApproveRequest(ctx, requestID, identity.AppUserID, req.Nickname)
       ErrNotFound → 404; Erfolg: 200 {"data": {"message": "Neuanlage-Antrag bestätigt."}}
       Audit-Log: EventType "member_request.approved"

    4. RejectRequest (POST /admin/member-requests/:requestId/reject)
       Auth: Admin-Check
       Aufruf: h.requestsRepo.RejectRequest(ctx, requestID, identity.AppUserID)
       ErrNotFound → 404; Erfolg: 200 {"data": {"message": "Neuanlage-Antrag abgelehnt."}}

    Ergänze `backend/cmd/server/main.go`:
    - memberClaimsRepo, memberClaimInvitationsRepo, memberRequestsRepo anlegen
    - memberClaimsHandler, memberClaimInvitationsHandler, memberProfileNoindexHandler, memberRequestsHandler anlegen
    - Me-Routen: /me/member-search, /me/member-claim, /me/member-claims, /me/member-requests, /me/profile/noindex (PATCH), /claim-invitations/accept
    - adminRouteHandlers-Struct um neue Handler erweitern und an registerAdminRoutes übergeben

    Ergänze `backend/cmd/server/admin_routes.go`:
    - adminRouteHandlers-Struct: memberClaimsHandler, memberClaimInvitationsHandler, memberRequestsHandler
    - Phase-66-Admin-Routen am Ende anhängen:
      GET  /admin/fansubs/:id/member-claims
      POST /admin/fansubs/:id/member-claims/:claimId/verify
      POST /admin/fansubs/:id/member-claims/:claimId/reject
      POST /admin/fansubs/:id/group-members/:memberId/claim-invitations
      POST /admin/fansubs/:id/group-members/:memberId/claim-invitations/:invitationId/cancel
      GET  /admin/member-requests
      POST /admin/member-requests/:requestId/approve
      POST /admin/member-requests/:requestId/reject

    Ergänze `frontend/src/types/profile.ts`:
    1. PublicMemberProfileData um: is_verified: boolean; noindex: boolean
    2. MemberProfileData um: is_verified: boolean; noindex: boolean; claim_status?: 'pending' | 'verified' | 'rejected' | null; claim_member_nick?: string | null
    3. Neue Typen am Ende:
       MemberSearchResult { id: number; nickname: string; display_name: string }
       MemberClaimRow { id: number; app_user_id: number; member_id: number; member_nickname: string; claim_status: string; note?: string | null; created_at: string }
       GenerateClaimInvitationResponse { id: number; member_id: number; status: string; expires_at: string; invite_link: string }
       MemberRequestRow { id: number; app_user_id: number; claim_status: string; note?: string | null; created_at: string }

    Ergänze `frontend/src/lib/api.ts` (am Ende, Muster: authorizedFetch + ApiError):
    1. acceptClaimInvitation(payload: { token: string }, authToken?: string): Promise<void>
       POST /api/v1/claim-invitations/accept
    2. getMyMemberClaim(authToken?: string): Promise<MemberClaimRow | null>
       GET /api/v1/me/member-claim; 404 → return null
    3. searchHistoricalMembers(q: string, authToken?: string): Promise<MemberSearchResult[]>
       GET /api/v1/me/member-search?q=encodeURIComponent(q); returns response.data
    4. submitMemberClaim(payload: { member_id: number; note?: string }, authToken?: string): Promise<MemberClaimRow>
       POST /api/v1/me/member-claims
    5. generateClaimInvitation(fansubId: number, memberId: number, authToken?: string): Promise<GenerateClaimInvitationResponse>
       POST /api/v1/admin/fansubs/${fansubId}/group-members/${memberId}/claim-invitations
    6. listPendingMemberClaims(fansubId: number, authToken?: string): Promise<MemberClaimRow[]>
       GET /api/v1/admin/fansubs/${fansubId}/member-claims; returns response.data
    7. verifyMemberClaim(fansubId: number, claimId: number, authToken?: string): Promise<void>
       POST /api/v1/admin/fansubs/${fansubId}/member-claims/${claimId}/verify
    8. rejectMemberClaim(fansubId: number, claimId: number, authToken?: string): Promise<void>
       POST /api/v1/admin/fansubs/${fansubId}/member-claims/${claimId}/reject
    9. patchNoindex(noindex: boolean, authToken?: string): Promise<void>
       PATCH /api/v1/me/profile/noindex, JSON body {noindex}
    10. submitMemberRequest(payload: { note?: string }, authToken?: string): Promise<MemberRequestRow>
        POST /api/v1/me/member-requests
    11. listMemberRequests(authToken?: string): Promise<MemberRequestRow[]>
        GET /api/v1/admin/member-requests; returns response.data
    12. approveMemberRequest(requestId: number, payload: { nickname: string }, authToken?: string): Promise<void>
        POST /api/v1/admin/member-requests/${requestId}/approve
    13. rejectMemberRequest(requestId: number, authToken?: string): Promise<void>
        POST /api/v1/admin/member-requests/${requestId}/reject
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go build ./cmd/server/... 2>&1 && cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./cmd/server/...` ohne Fehler
    - `npx tsc --noEmit` ohne neue Fehler in profile.ts oder api.ts
    - main.go enthält "NewMemberClaimsRepository", "NewMemberRequestsRepository", "memberProfileNoindexHandler"
    - admin_routes.go enthält "member-claims", "claim-invitations", "member-requests"
    - profile.ts: PublicMemberProfileData hat is_verified und noindex; MemberRequestRow exportiert
    - api.ts: patchNoindex, submitMemberRequest, listMemberRequests, approveMemberRequest, rejectMemberRequest exportiert
    - member_requests_repository.go unter 200 Zeilen; member_requests_handler.go unter 250 Zeilen
    - Alle api.ts-Funktionen nutzen authorizedFetch (kein nacktes fetch)
  </acceptance_criteria>
  <done>Backend-Build grün; TypeScript-Check ohne neue Fehler; alle 13 API-Funktionen exportiert; is_verified + noindex in Typen; Neuanlage-Antrag-Backend vollständig.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Grenze | Beschreibung |
|--------|-------------|
| Frontend → API | Alle API-Funktionen nutzen authorizedFetch mit Auth-Header |
| Route-Registrierung | Alle claim-bezogenen Routen hinter authMiddleware |
| Neuanlage-Antrag → members INSERT | ApproveRequest schreibt in members-Tabelle; nur Admin über permissionSvc |

## STRIDE Threat Register

| Threat ID | Kategorie | Komponente | Disposition | Mitigationsplan |
|-----------|-----------|-----------|-------------|-----------------|
| T-66-04-01 | Elevation of Privilege | /claim-invitations/accept ohne Auth | mitigate | Route registriert mit authMiddleware; Handler hat requireMeIdentity als Fallback |
| T-66-04-02 | Information Disclosure | api.ts ohne Auth-Header | mitigate | Alle neuen Funktionen nutzen authorizedFetch mit withAuthHeader — kein nacktes fetch |
| T-66-04-03 | Tampering | Falsche Typen für claim_status | mitigate | claim_status als Union-Typ ('pending' | 'verified' | 'rejected' | null) in TypeScript |
| T-66-04-04 | Elevation of Privilege | ApproveRequest: members INSERT ohne Admin-Check | mitigate | permissionActorFromContext + Admin-Scope Check vor ApproveRequest |
| T-66-04-05 | Tampering | noindex/is_verified im Public-Profile fehlen | mitigate | GetPublicMemberProfile SELECT erweitert; Scan und Model-Felder ergänzt — generateMetadata() und VerifiedBadge bauen darauf auf |
| T-66-04-SC | Tampering | keine npm Installs | accept | Nur Typ- und Funktionserweiterungen; keine neuen Pakete |

</threat_model>

<verification>
1. `go build ./cmd/server/...` ohne Fehler
2. `cd frontend && npx tsc --noEmit` ohne neue Fehler
3. Grep backend/internal/models/member_profile.go: "IsVerified" und "Noindex" in PublicMemberProfile
4. Grep backend/internal/repository/member_profile_repository.go: "is_verified" im SQL-Query
5. Grep admin_routes.go: "member-claims", "claim-invitations", "member-requests" vorhanden
6. Grep api.ts: "patchNoindex", "submitMemberRequest", "acceptClaimInvitation" vorhanden
7. Grep profile.ts: "is_verified" und "noindex" in PublicMemberProfileData; "MemberRequestRow" exportiert
</verification>

<success_criteria>
Backend-Build grün mit allen neuen Routen inkl. PATCH /me/profile/noindex. GetPublicMemberProfile liefert is_verified + noindex. Neuanlage-Antrag-Backend vollständig (D-03). Frontend TypeScript-Check ohne neue Fehler. 13 API-Funktionen und alle Typ-Erweiterungen bereit für Pläne 05 und 06.
</success_criteria>

<output>
Erstelle `.planning/phases/66-claiming-verifizierung/66-04-SUMMARY.md` wenn fertig.
</output>
