---
phase: 30-fansub-releases-api-endpunkte
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/cmd/server/admin_routes.go
  - backend/internal/handlers/admin_content_handler.go
  - backend/internal/handlers/admin_content_release_theme_assets.go
  - backend/internal/models/admin_release_theme_assets.go
  - backend/internal/models/episode_version.go
  - backend/internal/repository/admin_content_anime_themes.go
autonomous: true
requirements:
  - P30-SC1
  - P30-SC2
must_haves:
  truths:
    - "Releases become explicit admin-readable resources instead of being discovered only through episode-version or theme-asset side effects."
    - "Scoped release lookup for `fansub + anime` reuses the already-wired `anime_fansub_groups` reality."
    - "The phase does not treat `fansub_group_media` as the authoritative release/fansub media seam."
---

<objective>
Expose `fansub_releases` as explicit admin read/resolve endpoints so release identity stops hiding behind helper responses.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-fansub-releases-api-endpunkte/30-CONTEXT.md
@.planning/phases/30-fansub-releases-api-endpunkte/30-RESEARCH.md
@backend/cmd/server/admin_routes.go
@backend/internal/handlers/admin_content_handler.go
@backend/internal/handlers/admin_content_release_theme_assets.go
@backend/internal/repository/admin_content_anime_themes.go
@frontend/src/app/admin/fansubs/[id]/edit/ReleaseThemeAssetsSection.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define explicit admin release DTOs and repository reads</name>
  <read_first>
    - .planning/phases/30-fansub-releases-api-endpunkte/30-CONTEXT.md
    - .planning/phases/30-fansub-releases-api-endpunkte/30-RESEARCH.md
    - backend/internal/models/admin_release_theme_assets.go
    - backend/internal/models/episode_version.go
    - backend/internal/repository/admin_content_anime_themes.go
  </read_first>
  <action>Add dedicated admin release DTOs that describe release identity and scope clearly, for example release summary items and canonical-release responses. Extend the admin-content repository seam with explicit methods to list releases for a `fansub + anime` scope, resolve the canonical release for that scope, and fetch a release summary by `releaseId`. Keep the data grounded in `fansub_releases`, `release_versions`, `release_variants`, and `release_version_groups`, and make the payload rich enough that a UI no longer needs theme-asset responses just to learn which release it is touching.</action>
  <acceptance_criteria>
    - `Select-String -Path backend/internal/models/*.go -Pattern "AdminFansubRelease|CanonicalRelease"` returns matches.
    - `Select-String -Path backend/internal/repository/admin_content_anime_themes.go -Pattern "ListFansubAnimeReleases|GetCanonicalFansubAnimeReleaseSummary|GetAdminReleaseByID"` returns matches.
    - New DTOs surface release identity, episode/anime context, and scope metadata explicitly.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register explicit admin routes and handlers for release reads</name>
  <read_first>
    - backend/cmd/server/admin_routes.go
    - backend/internal/handlers/admin_content_handler.go
    - backend/internal/handlers/admin_content_release_theme_assets.go
    - .planning/phases/30-fansub-releases-api-endpunkte/30-RESEARCH.md
  </read_first>
  <action>Add concrete admin handlers and routes for explicit release reads, such as `GET /admin/fansubs/:id/anime/:animeId/releases`, `GET /admin/fansubs/:id/anime/:animeId/releases/canonical`, and `GET /admin/releases/:releaseId`. Reuse the existing admin authorization and route-parameter conventions from the fansub/theme handlers. Do not remove current theme-asset routes in this plan; instead, stop relying on them as the only way to discover `release_id`.</action>
  <acceptance_criteria>
    - `Select-String -Path backend/cmd/server/admin_routes.go -Pattern "/admin/fansubs/:id/anime/:animeId/releases|/admin/releases/:releaseId"` returns matches.
    - `Select-String -Path backend/internal/handlers/*.go -Pattern "ListFansubAnimeReleases|GetCanonicalFansubAnimeRelease|GetAdminRelease"` returns matches.
    - `cd backend && go test ./internal/handlers ./internal/repository -count=1` passes.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Prove that release identity is now available through explicit admin APIs and no longer needs to be inferred from helper-only theme-asset responses.
</verification>

<success_criteria>
Admins and follow-on UI code can load release identity and scope explicitly for a fansub-anime context through a clear backend contract.
</success_criteria>

<output>
After completion, create `.planning/phases/30-fansub-releases-api-endpunkte/30-01-SUMMARY.md`
</output>
