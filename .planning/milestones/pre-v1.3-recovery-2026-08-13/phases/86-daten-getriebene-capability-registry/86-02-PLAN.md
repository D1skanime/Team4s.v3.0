---
phase: 86-daten-getriebene-capability-registry
plan: 02
type: execute
wave: 2
depends_on:
  - 86-01
files_modified:
  - backend/internal/permissions/permissions.go
  - backend/internal/repository/authz_permissions.go
  - backend/cmd/server/main.go
  - backend/internal/permissions/capability_registry_test.go
autonomous: true
requirements:
  - D-04
  - D-05
  - D-06
  - D-10
  - D-11
must_haves:
  truths:
    - "D-04: permissions.go enthält CacheLoader-Interface + LoadCache(ctx, CacheLoader) error Methode auf *Service; paket-globale cacheMu sync.RWMutex + loadedCache map[string][]Action."
    - "D-05: RoleAllowsAction(role string, action Action) bool bleibt paket-globale Funktion mit unveränderter Signatur; AllowedActionsForRole(role string) []Action bleibt unverändert. Bestehende Aufrufer kompilieren ohne Änderung."
    - "D-06: roleAllows() liest aus loadedCache (RLock); Fallback auf roleMatrix wenn loadedCache == nil (Unit-Tests ohne DB). Kein DB-Roundtrip im Hot-Path."
    - "D-10: LoadCache prüft nach dem Load, dass alle 18 bekannten Action-Konstanten in einer expliziten allActions []Action Liste definiert sind und jede dieser Actions in action_definitions (= im Seed) vorhanden ist — geprüft durch Existenz in der CacheLoader-zurückgegebenen aggregierten Menge PLUS einer expliziten Ausnahmeliste für standalone-Actions (ActionFansubGroupInvitationsAccept). ActionFansubGroupInvitationsAccept ist valide (hat action_definitions-Eintrag) aber keinen role_capabilities-Eintrag — daher wird der Check NICHT gegen role_capabilities aggregiert, sondern gegen eine paket-interne allKnownActions []Action. Backend startet ohne Fehler."
    - "authz_permissions.go enthält LoadRoleCapabilities(ctx) (map[string][]Action, error) auf *AuthzRepository via pgx SELECT role_code, action_code FROM role_capabilities."
    - "main.go ruft permissionSvc.LoadCache(ctx, authzRepo) nach NewService und vor Router-Init auf; log.Fatalf bei Fehler."
    - "D-11: Wave-0-Tests TestRoleMatrixSeedParity, TestCacheLoadAndLookup, TestStartupConsistencyCheck sind nach diesem Plan GREEN."
    - "go build ./... und go test ./internal/permissions/... kompilieren und alle Tests sind grün."
  artifacts:
    - path: "backend/internal/permissions/permissions.go"
      provides: "CacheLoader-Interface, LoadCache-Methode, paket-globale Cache-Variable, roleAllows-Fallback"
      contains: "type CacheLoader interface"
    - path: "backend/internal/repository/authz_permissions.go"
      provides: "LoadRoleCapabilities Implementierung"
      exports: ["LoadRoleCapabilities"]
    - path: "backend/cmd/server/main.go"
      provides: "LoadCache-Aufruf beim Start"
      contains: "LoadCache"
  key_links:
    - from: "main.go permissionSvc.LoadCache()"
      to: "authzRepo.LoadRoleCapabilities()"
      via: "CacheLoader-Interface"
      pattern: "LoadCache\\(ctx"
    - from: "permissions.roleAllows()"
      to: "loadedCache"
      via: "cacheMu.RLock()"
      pattern: "loadedCache\\["
---

<objective>
permissions.go bekommt CacheLoader-Interface + LoadCache-Methode + paket-globale cacheMu/loadedCache-Variable. roleAllows() liest aus loadedCache (Fallback auf roleMatrix für Tests ohne DB). authz_permissions.go erhält LoadRoleCapabilities()-Methode. main.go verdrahtet LoadCache beim Start. Die Wave-0-Tests aus Plan 01 werden GREEN.

Purpose: Schließt D-04, D-05, D-06, D-10; der Übergang von hartkodierter roleMatrix auf DB-getriebenen Cache ist vollständig — öffentliche API stabil.
Output: permissions.go (erweitert), authz_permissions.go (neue Methode), main.go (LoadCache-Aufruf), capability_registry_test.go (Tests GREEN).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/86-daten-getriebene-capability-registry/86-RESEARCH.md
@.planning/phases/86-daten-getriebene-capability-registry/86-01-SUMMARY.md
@backend/internal/permissions/permissions.go
@backend/internal/permissions/permissions_test.go
@backend/internal/permissions/capability_registry_test.go
@backend/internal/repository/authz_permissions.go
@backend/cmd/server/main.go

<interfaces>
<!-- Aus backend/internal/repository/authz_permissions.go (Zeilen 1-10) -->
package repository

// AuthzRepository hat bereits Methoden ResolveFansubGroup, ResolveRelease, etc.
// Neue Methode LoadRoleCapabilities wird hinzugefügt:
// func (r *AuthzRepository) LoadRoleCapabilities(ctx context.Context) (map[string][]permissions.Action, error)

<!-- Aus backend/cmd/server/main.go (Zeilen 125-126) — Aktueller Konstruktor -->
authzRepo := repository.NewAuthzRepository(dbPool)
permissionSvc := permissions.NewService(authzRepo)
// Nach Plan 02: LoadCache-Aufruf hier einfügen, vor Router-Init

<!-- Ziel-Signatur in permissions.go nach Plan 02 -->
// Paket-globale Cache-Variable (neu):
var (
    cacheMu     sync.RWMutex
    loadedCache map[string][]Action  // nil = Fallback auf roleMatrix
)

// CacheLoader-Interface (neu):
type CacheLoader interface {
    LoadRoleCapabilities(ctx context.Context) (map[string][]Action, error)
}

// LoadCache Methode (neu auf *Service):
func (s *Service) LoadCache(ctx context.Context, loader CacheLoader) error

// roleAllows (modifiziert — liest loadedCache statt roleMatrix direkt):
func roleAllows(role string, action Action) bool
// bleibt paket-interne Funktion; liest loadedCache wenn != nil, sonst roleMatrix-Fallback

// RoleAllowsAction (STABILE SIGNATUR per D-05 — nicht ändern):
func RoleAllowsAction(role string, action Action) bool

// AllowedActionsForRole (STABILE SIGNATUR per D-05 — nicht ändern):
func AllowedActionsForRole(role string) []Action
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CacheLoader-Interface + LoadCache + roleAllows-Fallback in permissions.go</name>
  <files>
    backend/internal/permissions/permissions.go
    backend/internal/permissions/capability_registry_test.go
  </files>
  <read_first>
    - backend/internal/permissions/permissions.go — vollständig lesen (roleMatrix, roleAllows, RoleAllowsAction, AllowedActionsForRole, Service-Struct, NewService)
    - backend/internal/permissions/permissions_test.go — Test-Patterns (resolverStub, wie LoadCache bisher nicht aufgerufen wird)
    - backend/internal/permissions/capability_registry_test.go — die drei RED-Tests, die GREEN werden sollen
    - Research R-03 (in 86-RESEARCH.md) — empfohlene Cache-Variable + LoadCache-Logik
    - Research R-04 (in 86-RESEARCH.md) — Konsistenz-Check-Anforderungen für D-10
  </read_first>
  <behavior>
    - Nach diesem Task: TestCacheLoadAndLookup ist GREEN — svc.LoadCache(ctx, stub) befüllt loadedCache; RoleAllowsAction(RoleFansubLead, ActionFansubGroupEdit) == true.
    - Nach diesem Task: TestStartupConsistencyCheck ist GREEN — LoadCache gibt Fehler zurück wenn eine Action-Konstante fehlt.
    - Nach diesem Task: TestRoleMatrixSeedParity ist GREEN — stub gibt vollständige roleMatrix zurück, alle 18 Actions in aggregiertem Cache vorhanden.
    - Bestehende Tests in permissions_test.go bleiben GREEN — roleAllows fällt auf roleMatrix-Fallback wenn loadedCache == nil.
    - RoleAllowsAction(role, action) und AllowedActionsForRole(role) haben DIESELBE Signatur wie vor diesem Plan (D-05).
  </behavior>
  <action>
    Modifiziere backend/internal/permissions/permissions.go.

    Imports erweitern: "context", "sync" (für sync.RWMutex) hinzufügen falls nicht vorhanden.
    "slices" ist bereits importiert laut Code-Lektüre.

    Schritt 1 — Paket-globale Cache-Variable nach den bestehenden const-Blöcken (vor oder nach roleMatrix):
    Zwei paket-globale Variablen mit var-Block: cacheMu sync.RWMutex und loadedCache map[string][]Action (nil = nicht geladen).

    Schritt 2 — CacheLoader-Interface definieren (neues exportiertes Interface):
    type CacheLoader interface mit einer Methode LoadRoleCapabilities(ctx context.Context) (map[string][]Action, error).

    Schritt 3 — paket-interne allKnownActions []Action definieren (alle 18 Konstanten):
    var allKnownActions = []Action{
        ActionFansubGroupEdit, ActionFansubGroupLinksManage, ActionFansubGroupMembersView,
        ActionFansubGroupMembersManage, ActionFansubGroupInvitationsView,
        ActionFansubGroupInvitationsCreate, ActionFansubGroupInvitationsCancel,
        ActionFansubGroupInvitationsAccept,  // Standalone — in action_definitions aber nicht in role_capabilities
        ActionFansubGroupNotesWrite, ActionAnimeFansubProjectNotesWrite,
        ActionReleaseView, ActionReleaseVersionView, ActionReleaseVersionMediaView,
        ActionReleaseVersionMediaUpload, ActionReleaseVersionMediaUpdate,
        ActionReleaseVersionMediaDelete, ActionReleaseVersionMediaDeleteOwn,
        ActionReleaseVersionNotesWrite,
    }
    // standaloneActions sind Actions die in action_definitions existieren aber keinen role_capabilities-Eintrag haben
    var standaloneActions = []Action{ActionFansubGroupInvitationsAccept}

    Schritt 4 — LoadCache Methode auf *Service:
    func (s *Service) LoadCache(ctx context.Context, loader CacheLoader) error.
    Intern: loader.LoadRoleCapabilities(ctx) aufrufen, Fehler propagieren.
    D-10 Konsistenz-Check (exakt so, KEINE Alternative):
      - Baue eine Menge aller Actions die in irgendeiner Rolle im geladenen Cache vorkommen: seenActions := make(map[Action]bool); for _, actions := range m { for _, a := range actions { seenActions[a] = true } }.
      - Für jede Action in allKnownActions: prüfe ob seenActions[a] == true ODER ob slices.Contains(standaloneActions, a) == true.
      - Wenn weder noch: return fmt.Errorf("permission cache: Action %q fehlt in role_capabilities und ist keine standalone-Action — Startup abgebrochen", a).
      - Damit gilt: ActionFansubGroupInvitationsAccept ist valide (standaloneActions), das Backend startet. Eine fehlende nicht-standalone-Action bricht den Start ab (fail-closed).

    Schritt 5 — Cache nach Konsistenz-Check sperren und setzen: cacheMu.Lock(); loadedCache = m; cacheMu.Unlock().

    Schritt 6 — roleAllows() modifizieren (paket-intern, nicht exportiert):
    Am Anfang: cacheMu.RLock(); cache := loadedCache; cacheMu.RUnlock().
    Wenn cache != nil: return slices.Contains(cache[strings.TrimSpace(role)], action).
    Fallback: return slices.Contains(roleMatrix[strings.TrimSpace(role)], action) (bestehende Logik).

    Schritt 7 — AllowedActionsForRole() analog: cache-first, Fallback auf roleMatrix.
    Die Funktion hat heute Zugriff auf roleMatrix direkt — nach dem Umbau: cache-first, gleiches Fallback-Muster.

    Signatur RoleAllowsAction(role string, action Action) bool NICHT ändern (D-05).
    Signatur AllowedActionsForRole(role string) []Action NICHT ändern (D-05).

    Nach dem Umbau: capability_registry_test.go anpassen, falls das lokale CacheLoader-Interface dort als Typ-Alias definiert war — jetzt das exportierte permissions.CacheLoader nutzen (oder entfernen wenn es in permissions.go ist und der Test im selben Paket liegt, was er tut — kein Import-Alias nötig).
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go test ./internal/permissions/... -v -count=1 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    Alle Tests in permissions_test.go und capability_registry_test.go grün.
    TestCacheLoadAndLookup, TestStartupConsistencyCheck, TestRoleMatrixSeedParity: PASS.
    RoleAllowsAction und AllowedActionsForRole haben dieselbe Signatur wie vorher.
    go build ./internal/permissions/... kompiliert ohne Fehler.
    D-10-Check: ActionFansubGroupInvitationsAccept wird als standaloneAction behandelt → kein Startup-Fehler wenn role_capabilities keinen Eintrag dafür hat.
  </done>
</task>

<task type="auto">
  <name>Task 2: LoadRoleCapabilities in authz_permissions.go + LoadCache-Verdrahtung in main.go</name>
  <files>
    backend/internal/repository/authz_permissions.go
    backend/cmd/server/main.go
  </files>
  <read_first>
    - backend/internal/repository/authz_permissions.go — vollständig lesen (AuthzRepository-Struktur, bestehende Methoden ResolveFansubGroup, ResolveRelease etc.; Zeile 1 bis EOF)
    - backend/cmd/server/main.go Zeilen 120-160 — NewAuthzRepository + NewService + Routing-Init; LoadCache muss NACH NewService und VOR dem Router aufgerufen werden
    - Research R-03 Code-Beispiel (in 86-RESEARCH.md) — LoadRoleCapabilities pgx-Implementierung
  </read_first>
  <action>
    Schritt 1 — authz_permissions.go: Neue Methode LoadRoleCapabilities auf *AuthzRepository.
    Signatur: func (r *AuthzRepository) LoadRoleCapabilities(ctx context.Context) (map[string][]permissions.Action, error)
    Rückgabe-Typ ist map[string][]permissions.Action (Rolle → Action-Slice), wie CacheLoader-Interface verlangt.
    Implementierung via r.db.Query (pgx/v5): SELECT role_code, action_code FROM role_capabilities ORDER BY role_code, action_code.
    Scan in var role, action string. Aggregation: result[role] = append(result[role], permissions.Action(action)).
    Fehlerbehandlung: rows.Err() nach der Schleife prüfen (pgx-Konvention). fmt.Errorf("load role capabilities: %w", err) wrappen.

    AuthzRepository implementiert damit das permissions.CacheLoader-Interface implizit (Go duck-typing) — kein explizites Implements nötig, aber eine Compile-Zeit-Assertion kann als var _ permissions.CacheLoader = (*AuthzRepository)(nil) hinzugefügt werden.

    Datei nach Änderung: <=450 Zeilen prüfen (CLAUDE.md). Methode ist kurz (~20 Zeilen).

    Schritt 2 — main.go: LoadCache-Aufruf einfügen.
    Nach der Zeile permissionSvc := permissions.NewService(authzRepo) (Zeile 126 laut Research):
    ctx für LoadCache: context.Background() oder den bereits vorhandenen ctx aus der main-Funktion verwenden.
    Prüfen ob in main.go ein ctx := context.Background() oder context.WithTimeout bereits existiert — wenn ja, wiederverwenden; wenn nein, ctx := context.Background() direkt vor den Aufruf.
    Hinzufügen: if err := permissionSvc.LoadCache(ctx, authzRepo); err != nil { log.Fatalf("Capability-Registry laden fehlgeschlagen: %v", err) }
    Fehler-String auf Deutsch per CLAUDE.md-Anforderung (User-facing Go-Response-String).

    LoadCache-Aufruf muss VOR dem ersten Router-Init stehen. In main.go wird der Router nach allen Service-Konstruktionen gebaut — der Aufruf direkt nach NewService ist korrekt.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go build ./... 2>&amp;1 | head -20</automated>
  </verify>
  <done>
    authz_permissions.go enthält LoadRoleCapabilities-Methode.
    main.go hat LoadCache-Aufruf nach permissionSvc := permissions.NewService(authzRepo).
    go build ./... kompiliert ohne Fehler.
    go test ./internal/permissions/... -count=1 alle Tests grün.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → In-Memory-Cache | LoadRoleCapabilities liest role_capabilities; manipulierte DB = falscher Cache |
| Cache → roleAllows Hot-Path | loadedCache wird ohne Neuvalidierung direkt für Entscheidungen genutzt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-05 | Tampering | LoadRoleCapabilities SQL | mitigate | pgx prepared statements (kein String-Concat); action_code ist FK → nur gültige Werte möglich |
| T-86-06 | Tampering | loadedCache Cache-Poisoning beim Start | mitigate | D-10-Konsistenz-Check in LoadCache: alle 18 Action-Konstanten müssen im aggregierten Cache vorhanden sein (oder als standaloneAction deklariert); Fehler = log.Fatalf (fail-fast) |
| T-86-07 | Elevation of Privilege | platform_admin nicht in role_capabilities | mitigate | platform_admin hat keinen role_capabilities-Eintrag; bleibt actor.IsPlatformAdmin-Flag in canForContext/CanForReleaseVersion; kein Rollback-Risiko durch den Seed |
| T-86-08 | Information Disclosure | Stale Cache nach Migration ohne Neustart | accept | D-06: Matrix ändert sich nur per Migration; Neustart nach Migration ist Deployment-Prozess; kein Auto-Reload nötig; in DECISIONS.md dokumentieren |
| T-86-09 | Denial of Service | LoadCache-Fehler beim Start | mitigate | log.Fatalf verhindert Start mit inkonsistenter Registry (fail-closed, nicht fail-open) |
| T-86-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Packages |
</threat_model>

<verification>
Nach Task 1:
- go test ./internal/permissions/... -v -count=1 → alle Tests PASS inkl. TestCacheLoadAndLookup, TestStartupConsistencyCheck, TestRoleMatrixSeedParity.
- permissions.go enthält CacheLoader-Interface und LoadCache-Methode.
- roleAllows() liest loadedCache mit RLock; Fallback auf roleMatrix wenn nil.
- D-10-Check prüft gegen allKnownActions; ActionFansubGroupInvitationsAccept in standaloneActions → kein Startup-Fehler.

Nach Task 2:
- go build ./... kompiliert.
- authz_permissions.go enthält LoadRoleCapabilities-Methode.
- main.go hat permissionSvc.LoadCache(ctx, authzRepo) vor Router-Init.
- Compile-Zeit-Assertion var _ permissions.CacheLoader = (*AuthzRepository)(nil) schlägt nicht fehl.
</verification>

<success_criteria>
- permissions.go: CacheLoader-Interface, LoadCache(*Service)-Methode, cacheMu, loadedCache, allKnownActions, standaloneActions, roleAllows()-Fallback
- authz_permissions.go: LoadRoleCapabilities() via pgx SELECT auf role_capabilities
- main.go: LoadCache-Aufruf nach NewService, log.Fatalf bei Fehler
- go test ./internal/permissions/... → alle Tests PASS
- go build ./... → kompiliert
- RoleAllowsAction und AllowedActionsForRole Signaturen unverändert (D-05)
- D-10: ActionFansubGroupInvitationsAccept ist valide (standaloneAction) → Backend startet ohne Fehler wenn Seed korrekt
</success_criteria>

<output>
Erstelle .planning/phases/86-daten-getriebene-capability-registry/86-02-SUMMARY.md wenn fertig.
</output>
