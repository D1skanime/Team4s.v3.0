---
phase: 81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe
plan: 06
type: execute
wave: 6
depends_on:
  - 81-05
files_modified:
  - backend/internal/repository/fansub_repository_test.go
  - backend/internal/handlers/fansub_test.go
  - backend/internal/repository/episode_version_repository_write_helpers_test.go
autonomous: false
requirements:
  - P81-SC8

user_setup: []

must_haves:
  truths:
    - "go test ./backend/... — alle Tests grün (P81-SC8)"
    - "cd frontend && npx vitest run — alle Tests grün"
    - "Kein Test mehr referenziert FansubGroupTypeCollaboration oder collaboration_members (P81-SC3/SC8)"
    - "fansub_repository_test.go: ListCollaborationMembers-Invarianten-Fragment entfernt"
    - "fansub_test.go: Kollaborations-Typ-Tests entfernt"
    - "Live-Verify bestätigt: 2 Gruppen-Chips im Editor, 2 Junction-Zeilen in DB"
  artifacts:
    - path: "backend/internal/repository/fansub_repository_test.go"
      provides: "Bereinigter Test ohne ListCollaborationMembers-Fragment"
    - path: "backend/internal/handlers/fansub_test.go"
      provides: "Bereinigter Test ohne collaboration-Typ-Tests"
  key_links:
    - from: "go test ./backend/..."
      to: "Alle Backend-Tests"
      via: "go test Runner"
      pattern: "PASS"
    - from: "npx vitest run"
      to: "Alle Frontend-Tests"
      via: "Vitest Runner"
      pattern: "Tests passed"
---

<objective>
Cleanup-Test-Suite + Human-Verify (P81-SC8)

Zweck: Alle verbliebenen Tests, die noch auf das alte Kombigruppen-Verhalten festschreiben,
werden entfernt oder bereinigt. Danach werden alle Backend- und Frontend-Test-Suiten grün gefahren.
Abschließend bestätigt der Operator per Live-Verify auf Dev-Server :3000, dass zwei Gruppen-Chips
korrekt gespeichert werden und in der Ansicht erscheinen.

Output: Grüne Test-Suite; Human-Verify-Checkpoint für P21-SC3-Umkehr bestanden.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-CONTEXT.md
@.planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-RESEARCH.md
@.planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend-Test-Cleanup — Kollaborations-Tests entfernen/bereinigen</name>
  <files>
    backend/internal/repository/fansub_repository_test.go,
    backend/internal/handlers/fansub_test.go,
    backend/internal/repository/episode_version_repository_write_helpers_test.go
  </files>
  <read_first>
    - backend/internal/repository/fansub_repository_test.go (vollständig — TestFansubRepository_PublicProfileSourceInvariants-Bereich)
    - backend/internal/handlers/fansub_test.go (vollständig — FansubGroupTypeCollaboration und group_type: "collaboration" Stellen)
    - backend/internal/repository/episode_version_repository_write_helpers_test.go (nach Plan 01 angelegt)
    - .planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-RESEARCH.md §"Forschungsfrage 7: Test-Landschaft"
  </read_first>
  <behavior>
    - fansub_repository_test.go: TestFansubRepository_PublicProfileSourceInvariants enthält kein "ListCollaborationMembers"-Fragment mehr
    - fansub_test.go: Tests die FansubGroupTypeCollaboration oder group_type: "collaboration" nutzen sind entfernt
    - episode_version_repository_write_helpers_test.go: Alle GREEN (TestSyncEpisodeVersionSelectedGroups_WritesNJunctionRows, TestBuildAnimeFansubLinkGroupIDs_UsesOnlyMemberGroups)
    - go test ./backend/internal/repository/... ./backend/internal/handlers/... — alle PASS
    - go test ./backend/... — PASS (gesamt)
  </behavior>
  <action>
    In fansub_repository_test.go:
    Suche TestFansubRepository_PublicProfileSourceInvariants (L68 Bereich).
    Entferne jegliches Fragment das "ListCollaborationMembers(ctx, group.ID)" als Source-Scan-Assert enthält.
    Behalte alle anderen Invarianten-Checks in der Testfunktion die noch valide sind.

    In fansub_test.go:
    Suche Tests die FansubGroupTypeCollaboration (L34, L71, L82 Bereich) und group_type: "collaboration" nutzen.
    Entferne diese Tests vollständig.
    Behalte alle anderen fansub_test.go Tests die nicht auf Kollaborations-Typen basieren.

    In episode_version_repository_write_helpers_test.go:
    Prüfe ob TestBuildAnimeFansubLinkGroupIDs_UsesOnlyMemberGroups nach den Änderungen in Plan 03 GREEN ist.
    Falls noch RED: Korrigiere den Test-Assert auf die neue buildAnimeFansubLinkGroupIDs([]resolvedImportFansubGroup)-Signatur.
    Testfall prüft: 3 Inputs (ID 3+4+3 Duplikat) → 2 deduplizierte sortierte IDs.

    Nach allen Änderungen: go test ./backend/... muss vollständig PASS sein.
    Achtung: Kompilierungsfehler haben Priorität vor inhaltlichen Test-Failures — zuerst alles kompilieren lassen.
  </action>
  <verify>
    <automated>cd backend && go test ./... 2>&1 | tail -30</automated>
  </verify>
  <done>
    go test ./backend/... — PASS ohne Fehler.
    grep -r "FansubGroupTypeCollaboration" backend/internal/handlers/fansub_test.go — 0 Treffer.
    grep -r "ListCollaborationMembers" backend/internal/repository/fansub_repository_test.go — 0 Treffer.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 81 vollständig implementiert: N Fansub-Gruppen als gleichwertige Chips, keine Kombigruppen mehr.

    Backend: N-fach-Junction-Upsert (syncEpisodeVersionSelectedGroups → N Zeilen), json_agg-Aggregation
    im Lesepfad, Migrations 0101 + 0102, alle Kollaborations-Endpunkte entfernt.

    Frontend: ReleaseVersionFansubChips mit Badge-Primitiv, Typen bereinigt, alle collaboration-Zweige entfernt.

    Tests: go test ./backend/... grün; npx vitest run grün.
  </what-built>
  <how-to-verify>
    Voraussetzung: Dev-Server läuft auf :3000 (npm run dev in frontend/) + Backend auf :8092.

    Schritt 1 — Einloggen:
    Öffne http://localhost:3000 und logge dich als Admin ein.

    Schritt 2 — Zwei Gruppen an einer Release-Version zuweisen:
    Gehe zu einer beliebigen Episode-Version-Edit-Seite:
    http://localhost:3000/admin/episode-versions/[bekannte-ID]/edit
    Füge im Gruppen-Chips-Bereich zwei verschiedene bestehende Fansub-Gruppen hinzu
    (z. B. vorhandene Gruppen aus der Suche wählen).
    Speichern klicken.

    Schritt 3 — DB-Prüfung (zwei Junction-Zeilen, keine Kombigruppe):
    Im Backend-Container oder per psql:
    SELECT rvg.release_version_id, fg.name, fg.group_type
    FROM release_version_groups rvg
    JOIN fansub_groups fg ON fg.id = rvg.fansub_group_id
    WHERE rvg.release_version_id = [die-ID-aus-Schritt-2];
    Erwartet: genau 2 Zeilen, beide mit group_type='group' (kein 'collaboration').

    Schritt 4 — Release-Ansicht prüfen:
    Navigiere zu /anime/[id]/group/[groupId] oder zur Release-Version-Ansicht.
    Beide Gruppen erscheinen als separate Badge-Chips nebeneinander.
    KEIN "Gruppe A & Gruppe B" als zusammengesetzter Name.

    Schritt 5 — Gruppen-Profilseite prüfen:
    Öffne /fansubs/[slug-von-gruppe-A] — Release-Version erscheint dort.
    Öffne /fansubs/[slug-von-gruppe-B] — dieselbe Release-Version erscheint auch dort.
    Kooperations-Hinweis (Badge oder Text "Kooperation") sichtbar.
  </how-to-verify>
  <resume-signal>
    Tippe "approved" wenn alle 5 Schritte bestätigt.
    Oder beschreibe gefundene Probleme mit Schritt-Nummer und Fehlerbeschreibung.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin-UI → Backend PATCH | Zwei Gruppen-IDs werden per PATCH an syncEpisodeVersionSelectedGroups übergeben; werden gegen DB validiert |
| DB-Lesung → Frontend | json_agg gibt Array zurück; Frontend rendert Chips ohne server-seitige Rechte-Prüfung (öffentliche Daten) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-FIN-01 | Elevation of Privilege | PATCH-Endpunkt: authMiddleware | mitigate | authMiddleware bereits auf Editor-PATCH-Route gesetzt; unverändert durch diese Phase |
| T-81-FIN-02 | Tampering | Live-UAT bestätigt korrekte DB-Zeilen | mitigate | Human-Verify mit SQL-Query-Nachweis; kein automatischer Proxy für DB-Korrektheit |
</threat_model>

<verification>
1. go test ./backend/... — PASS
2. cd frontend && npx vitest run — PASS
3. grep -r "FansubGroupTypeCollaboration" backend/ — 0 Treffer (außer models/fansub.go ist leer)
4. grep -rn "group_type.*collaboration" frontend/src/ — 0 Treffer
5. Human-Verify: 2 Chips im Editor + 2 DB-Zeilen + Anzeige auf beiden Gruppen-Profilseiten
</verification>

<success_criteria>
- go test ./backend/... — alle Tests PASS
- npx vitest run — alle Tests PASS
- Kein Test mehr referenziert FansubGroupTypeCollaboration oder collaboration_members
- Human-Verify bestätigt: 2 Gruppen → 2 Badge-Chips → 2 DB-Zeilen → Anzeige auf beiden Gruppen-Seiten
- Phase 81 abgeschlossen: P21-SC3-Kombigruppen-Verhalten vollständig umgekehrt
</success_criteria>

<output>
Erstelle `.planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-06-SUMMARY.md` nach Abschluss.
</output>
