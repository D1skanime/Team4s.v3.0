# 04.1-02 Summary

## Outcome

Plan `04.1-02` is complete. The admin anime create, edit-load, and edit-save flows now preserve backend `error.code` and operator-facing `error.details` instead of collapsing failures into a generic message.

## Delivered

- `frontend/src/lib/api.ts`
  Central admin anime create and update clients now throw `ApiError` with populated `code` and `details`, aligned with the existing `getAnimeByID(...)` load seam.
- `frontend/src/lib/api/admin-anime-intake.ts`
  The intake/create seam now reuses the shared `parseApiErrorPayload(...)` parser and throws `ApiError` instead of raw `Error`.
- `frontend/src/app/admin/anime/create/page.tsx`
  Create-route error rendering now includes backend detail text when present.
- `frontend/src/app/admin/anime/[id]/edit/page.tsx`
  The edit-route load failure path now uses the same detail-aware formatting through an explicit helper seam.
- `frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts`
  Save and cover-follow-up patch failures now forward formatted backend details to the existing error callback.
- Regression tests added:
  - `frontend/src/lib/api.admin-anime.test.ts`
  - `frontend/src/app/admin/anime/[id]/edit/page.test.tsx`
  - expanded `frontend/src/app/admin/anime/create/page.test.tsx`
  - expanded `frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts`

## Verification

- `cd backend && go test ./internal/repository ./internal/handlers -count=1`
- `cd frontend && npm test -- src/lib/api.admin-anime.test.ts src/app/admin/anime/create/page.test.tsx src/app/admin/anime/[id]/edit/page.test.tsx src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts src/app/admin/anime/utils/studio-helpers.test.ts`
- `cd frontend && npm run build`
- `docker compose up -d --build team4sv30-backend team4sv30-frontend`
- Live runtime flow after rebuild:
  - create anime -> `201`
  - load created anime -> `200`
  - upload local cover -> `201`
  - save edited cover -> `200`
  - reload anime -> `200` with updated `cover_image`
  - delete anime -> `200` with `orphaned_local_cover_image`
  - delete uploaded cover file via `/api/admin/upload-cover` -> `200`

All commands passed.

## Notes

- Backend handler-side structured error responses were already in place and remained compatible with this slice.
- The focused frontend tests still emit expected debug `console` output from the edit cover hook; this is not a test failure.
- During live verification, the previous `PATCH /api/v1/admin/anime/:id` flat-schema failure disappeared after rebuilding the backend container, confirming the runtime had been behind the workspace state.
