---
phase: 41-globalen-tiptap-rich-text-editor-einfuehren
plan: "06"
type: execute
wave: 3
depends_on:
  - "41-03"
  - "41-04"
  - "41-05"
files_modified:
  - frontend/src/components/editor/RichTextEditor.test.tsx
  - frontend/src/components/editor/ColorTokenExtension.test.ts
autonomous: false
requirements:
  - TIPTAP-EDITOR-01
must_haves:
  truths:
    - "go test ./... läuft grün (Backend)"
    - "npm run typecheck läuft grün (Frontend)"
    - "npm run lint läuft grün (Frontend)"
    - "RichTextEditor-Render-Test grün"
    - "ColorTokenExtension-Allowlist-Test grün"
    - "TipTap-Backend-Service-Tests grün (aus Plan 41-02)"
  artifacts:
    - path: frontend/src/components/editor/RichTextEditor.test.tsx
      provides: "Render-Test für RichTextEditor"
      contains: "RichTextEditor"
    - path: frontend/src/components/editor/ColorTokenExtension.test.ts
      provides: "Allowlist-Test für COLOR_TOKENS"
      contains: "COLOR_TOKENS"
  key_links:
    - from: frontend/src/components/editor/RichTextEditor.test.tsx
      to: frontend/src/components/editor/RichTextEditor.tsx
      via: "import { RichTextEditor }"
      pattern: "RichTextEditor"
---

<objective>
Frontend-Tests für globale Editor-Komponenten + finaler Integrations-Check aller Pläne.

Purpose: Sicherstellen, dass RichTextEditor und ColorTokenExtension korrekt funktionieren; Gesamtverifikation der Phase.

Output: 2 Test-Dateien; Checkpoint für Browser-UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-globalen-tiptap-rich-text-editor-einfuehren/41-CONTEXT.md
@.planning/phases/41-globalen-tiptap-rich-text-editor-einfuehren/41-RESEARCH.md

<interfaces>
<!-- Test-Framework: Vitest 3 + React Testing Library (frontend/vitest.config.ts) -->
<!-- TipTap DOM-APIs: Tests müssen jsdom-Umgebung haben (vitest.config.ts) -->

<!-- ColorTokenExtension (aus Plan 41-04):
  export const COLOR_TOKENS = ['default', 'gray', 'red', 'orange', 'yellow', 'green', 'blue', 'purple'] as const
  export type ColorToken = typeof COLOR_TOKENS[number]
-->

<!-- RichTextEditor (aus Plan 41-04):
  Props: value, onChange, placeholder, helperText, mode, disabled, minHeight
  Rendern: EditorContent, Toolbar, optional shortnote-Hinweis
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Frontend-Tests für ColorTokenExtension und RichTextEditor anlegen</name>
  <read_first>
    - frontend/vitest.config.ts (Test-Konfiguration, jsdom-Setup prüfen)
    - frontend/src/components/editor/ColorTokenExtension.ts (Export-Surface)
    - frontend/src/components/editor/RichTextEditor.tsx (Render-Struktur)
    - frontend/src/components/editor/RichTextRenderer.tsx (Render-Struktur)
  </read_first>
  <files>
    frontend/src/components/editor/RichTextEditor.test.tsx,
    frontend/src/components/editor/ColorTokenExtension.test.ts
  </files>
  <behavior>
    ColorTokenExtension.test.ts:
    - COLOR_TOKENS enthält genau 8 Elemente
    - COLOR_TOKENS enthält 'default', 'gray', 'red', 'orange', 'yellow', 'green', 'blue', 'purple'
    - COLOR_TOKENS enthält KEIN '#' (keine Hex-Werte)
    - COLOR_TOKENS enthält KEIN 'pink', 'cyan', 'violet' (nicht in Palette)

    RichTextEditor.test.tsx:
    - RichTextEditor rendert ohne Crash (smoke test mit `render(<RichTextEditor value={null} onChange={() => {}} />)`)
    - RichTextEditor mit mode="shortnote" zeigt den Shortnote-Hinweistext
      (Text "2–5 Sätze reichen" oder "Release-Version" im Dokument)
    - RichTextEditor mit mode="longform" zeigt den Shortnote-Hinweis NICHT
    - RichTextRenderer mit bodyHtml=null rendert null (kein DOM-Element)
    - RichTextRenderer mit bodyHtml="<p>Test</p>" rendert ein div mit dem HTML
  </behavior>
  <action>
    **ColorTokenExtension.test.ts** (Pure Unit Test, kein DOM nötig):
    ```typescript
    import { describe, it, expect } from 'vitest'
    import { COLOR_TOKENS } from './ColorTokenExtension'

    describe('ColorTokenExtension', () => {
      it('enthält genau 8 Tokens', () => {
        expect(COLOR_TOKENS).toHaveLength(8)
      })
      it('enthält alle erlaubten Tokens', () => {
        expect(COLOR_TOKENS).toContain('default')
        expect(COLOR_TOKENS).toContain('red')
        expect(COLOR_TOKENS).toContain('purple')
      })
      it('enthält keine Hex-Werte', () => {
        COLOR_TOKENS.forEach(token => {
          expect(token).not.toMatch(/^#/)
        })
      })
      it('enthält kein nicht-erlaubtes Token', () => {
        expect(COLOR_TOKENS).not.toContain('pink')
        expect(COLOR_TOKENS).not.toContain('cyan')
      })
    })
    ```

    **RichTextEditor.test.tsx** (benötigt jsdom + @testing-library/react):
    ```typescript
    import { describe, it, expect, vi } from 'vitest'
    import { render, screen } from '@testing-library/react'
    import { RichTextEditor } from './RichTextEditor'
    import { RichTextRenderer } from './RichTextRenderer'
    ```

    TipTap verwendet DOM-APIs. Falls jsdom nicht alle ProseMirror-APIs unterstützt,
    MockEditor oder einfache Stub-Tests verwenden:
    - Smoke Test: render darf nicht werfen
    - Attribute-Test: mit getByRole/getByText die sichtbaren UI-Elemente prüfen

    Falls TipTap in jsdom-Tests crasht (ProseMirror DOM-Probleme sind bekannt),
    dann `vi.mock('@tiptap/react', ...)` mit einem minimalen Mock:
    ```typescript
    vi.mock('@tiptap/react', () => ({
      useEditor: vi.fn(() => null),
      EditorContent: ({ editor }: { editor: null }) => <div data-testid="editor">{String(editor)}</div>,
    }))
    ```
    In diesem Fall: Teste nur die Toolbar-Sichtbarkeit und den shortnote-Hinweis-Text
    ohne echten Editor-Inhalt.

    Wähle den Ansatz der funktioniert — kein "es könnte klappen" als Antwort.
    Prüfe in vitest.config.ts ob `environment: 'jsdom'` gesetzt ist.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run RichTextEditor ColorTokenExtension 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm test -- --run ColorTokenExtension` → alle Tests PASS
    - `cd frontend && npm test -- --run RichTextEditor` → alle Tests PASS (oder bekannte Skip wenn TipTap DOM-Problem)
    - `cd frontend && npm run typecheck` → kein Fehler
    - `cd backend && go test ./internal/services/... -run TestTipTap` → alle Tests PASS
    - `cd backend && go test ./...` → kein Fehler
  </acceptance_criteria>
  <done>
    ColorTokenExtension-Tests grün.
    RichTextEditor-Tests grün (mit oder ohne TipTap-Mock).
    Alle Backend-Tests grün.
    typecheck grün.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    TipTap Rich-Text-Editor für alle vier Textbereiche:
    - DB: 4 Migrations (0067-0070) mit body_json, body_text, editor_type, content_schema_version
    - Backend: TipTapService (ValidateJSON, RenderHTML, ExtractText, IsEmpty), Handler umgestellt, admin_content_fansub_notes.go gesplittet
    - Frontend: RichTextEditor + RichTextRenderer + ColorTokenExtension als globale Komponenten
    - Admin-UI: Fansub-Gruppentext, Member-Geschichten, Fansubprojekt-Text, Release-Notizen alle auf TipTap
  </what-built>
  <how-to-verify>
    Voraussetzung: Docker-Container neu bauen (Migrations + Go-Backend-Änderungen):
    ```
    docker compose down && docker compose up --build -d
    ```

    **Test 1 — Fansub-Gruppen-Text (longform)**
    1. Admin öffnen: /admin/fansubs/[id]/edit → Tab "Texte"
    2. "Neue Notiz" anlegen
    3. TipTap-Editor muss erscheinen (WYSIWYG, keine Textarea)
    4. Überschrift H2 eingeben, Fettdruck testen, Tabelle einfügen
    5. Farb-Palette öffnen: nur 7 Farb-Tokens sichtbar (gray, red, orange, yellow, green, blue, purple), KEINE Hex-Eingabe
    6. Speichern → Erfolg

    **Test 2 — Release-Version-Notizen (shortnote)**
    1. Admin öffnen: /admin/episode-versions/[versionId]/edit → Tab "Notizen"
    2. Für eine Rolle: TipTap-Editor mit Shortnote-Hinweis "2–5 Sätze reichen" sichtbar
    3. Rollenbezogene Hilfetexte (z.B. "Schreibe kurz, was an der Übersetzung besonders war") sichtbar
    4. Text eingeben, Speichern → Erfolg

    **Test 3 — Sicherheit: Keine Script-Injektion**
    1. In einem Textfeld via Browser-DevTools body_json manipulieren oder
       direkt API-Request mit {"type":"doc","content":[{"type":"script"}]} senden
    2. Backend muss mit 400 "nicht erlaubter Node-Typ" antworten

    **Test 4 — Leere Inhalte**
    1. Editor öffnen, nichts eingeben, Speichern
    2. Gespeichert werden darf (leere Notiz ist valid), body_text ist leer

    Tippe "approved" wenn alle 4 Tests bestanden, oder beschreibe Probleme.
  </how-to-verify>
  <resume-signal>Tippe "approved" oder beschreibe Probleme die behoben werden müssen.</resume-signal>
</task>

</tasks>

<verification>
```bash
# Backend full suite
cd backend && go test ./...

# Frontend full suite
cd frontend && npm run typecheck && npm run lint

# Spezifische TipTap-Tests
cd backend && go test ./internal/services/... -run TestTipTap -v
cd frontend && npm test -- --run ColorTokenExtension
```
</verification>

<success_criteria>
- cd backend && go test ./...: alle Tests grün
- cd frontend && npm run typecheck: kein Fehler
- cd frontend && npm run lint: kein Fehler
- Frontend-Tests für ColorTokenExtension: alle PASS
- Frontend-Tests für RichTextEditor: alle PASS (ggf. mit Mock)
- Browser-UAT: TipTap-Editor erscheint in allen vier Admin-Bereichen
- Browser-UAT: Farb-Palette zeigt nur Token-Namen, keine Hex-Eingabe
- Browser-UAT: Shortnote-Modus zeigt Hinweistext
- Browser-UAT: Phase-40-Hilfetexte in release_version_notes weiterhin sichtbar
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/41-globalen-tiptap-rich-text-editor-einfuehren/41-06-SUMMARY.md` erstellen.
</output>
