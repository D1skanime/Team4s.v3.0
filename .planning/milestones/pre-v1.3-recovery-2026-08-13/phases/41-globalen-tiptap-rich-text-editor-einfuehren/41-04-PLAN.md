---
phase: 41-globalen-tiptap-rich-text-editor-einfuehren
plan: "04"
type: execute
wave: 2
depends_on:
  - "41-01"
  - "41-02"
files_modified:
  - frontend/src/components/editor/RichTextEditor.tsx
  - frontend/src/components/editor/RichTextEditor.module.css
  - frontend/src/components/editor/RichTextRenderer.tsx
  - frontend/src/components/editor/RichTextRenderer.module.css
  - frontend/src/components/editor/ColorTokenExtension.ts
  - frontend/src/components/editor/index.ts
  - frontend/src/app/globals.css
autonomous: true
requirements:
  - TIPTAP-EDITOR-01
must_haves:
  truths:
    - "RichTextEditor ist eine 'use client'-Komponente mit TipTap useEditor"
    - "StarterKit deaktiviert codeBlock, code, strike, hardBreak explizit"
    - "ColorTokenExtension erlaubt nur Token-Namen (default/gray/red/orange/yellow/green/blue/purple), keine Hex-Werte"
    - "RichTextRenderer ist SSR-safe und rendert nur sanitisiertes bodyHtml"
    - "Toolbar enthält Paragraph/H1/H2/H3, Bold, Italic, Bullet, Ordered, Quote, Table, Farbe, HR, Undo, Redo"
    - "shortnote-Modus zeigt Hinweistext"
    - "npm run typecheck läuft grün"
  artifacts:
    - path: frontend/src/components/editor/RichTextEditor.tsx
      provides: "Globale Editor-Komponente"
      exports: ["RichTextEditor"]
    - path: frontend/src/components/editor/RichTextRenderer.tsx
      provides: "Globale Renderer-Komponente (SSR-safe)"
      exports: ["RichTextRenderer"]
    - path: frontend/src/components/editor/ColorTokenExtension.ts
      provides: "Custom TipTap Extension für Token-Farben"
      exports: ["ColorTokenExtension", "COLOR_TOKENS"]
    - path: frontend/src/components/editor/index.ts
      provides: "Re-Export aller Editor-Komponenten"
      contains: "RichTextEditor"
  key_links:
    - from: frontend/src/components/editor/RichTextEditor.tsx
      to: frontend/src/components/editor/ColorTokenExtension.ts
      via: "import ColorTokenExtension"
      pattern: "ColorTokenExtension"
    - from: frontend/src/app/globals.css
      to: "body_html output"
      via: ".color-token-red etc. CSS-Klassen"
      pattern: "color-token-"
---

<objective>
Globale TipTap-Komponenten erstellen: RichTextEditor (Admin-Eingabe) und RichTextRenderer (Public-/Preview-Ausgabe) sowie ColorTokenExtension für Token-basierte Farben.

Purpose: Diese Komponenten werden von allen vier Admin-UI-Bereichen in Plan 41-05 verwendet. Sie müssen vor der Integration existieren.

Output: frontend/src/components/editor/ mit 6 Dateien.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-globalen-tiptap-rich-text-editor-einfuehren/41-CONTEXT.md
@.planning/phases/41-globalen-tiptap-rich-text-editor-einfuehren/41-RESEARCH.md

<interfaces>
<!-- RichTextEditorProps (aus CONTEXT.md — verbindlich):
type RichTextEditorProps = {
  value: unknown | null
  onChange: (value: unknown) => void
  placeholder?: string
  helperText?: string
  mode?: "longform" | "shortnote"
  disabled?: boolean
  minHeight?: number
}
-->

<!-- RichTextRendererProps (aus CONTEXT.md — verbindlich):
type RichTextRendererProps = {
  bodyHtml?: string | null
  bodyJson?: unknown | null
  editorType?: string | null
  contentSchemaVersion?: number | null
}
-->

<!-- ColorToken-Palette (Allowlist): default, gray, red, orange, yellow, green, blue, purple -->

<!-- TipTap-Packages (nach npm install in Plan 41-05):
  @tiptap/react (useEditor, EditorContent)
  @tiptap/pm (ProseMirror transitive dep)
  @tiptap/starter-kit (StarterKit)
  @tiptap/extension-table, table-row, table-cell, table-header
  @tiptap/extension-text-style
  @tiptap/extension-color (NICHT verwenden — custom ColorTokenExtension stattdessen)
  @tiptap/extension-placeholder
  @tiptap/extension-character-count
-->

<!-- Bestehende globale CSS: frontend/src/app/globals.css -->
<!-- CSS Modules Pattern: frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TipTap npm-Packages installieren + ColorTokenExtension + index.ts</name>
  <read_first>
    - frontend/package.json (aktuelle Dependencies prüfen)
    - frontend/src/components/ (Verzeichnisstruktur)
    - .planning/phases/41-globalen-tiptap-rich-text-editor-einfuehren/41-RESEARCH.md (Pattern 2: ColorTokenExtension)
  </read_first>
  <files>
    frontend/src/components/editor/ColorTokenExtension.ts,
    frontend/src/components/editor/index.ts,
    frontend/package.json,
    frontend/package-lock.json
  </files>
  <action>
    **Schritt 1: TipTap installieren**
    ```bash
    cd frontend && npm install @tiptap/react @tiptap/pm @tiptap/starter-kit \
      @tiptap/extension-table @tiptap/extension-table-row \
      @tiptap/extension-table-cell @tiptap/extension-table-header \
      @tiptap/extension-text-style @tiptap/extension-color \
      @tiptap/extension-placeholder @tiptap/extension-character-count
    ```

    **Schritt 2: ColorTokenExtension.ts erstellen**

    ```typescript
    // frontend/src/components/editor/ColorTokenExtension.ts
    import TextStyle from '@tiptap/extension-text-style'

    export const COLOR_TOKENS = [
      'default', 'gray', 'red', 'orange', 'yellow', 'green', 'blue', 'purple'
    ] as const

    export type ColorToken = typeof COLOR_TOKENS[number]

    // ColorTokenExtension erweitert TextStyle um ein colorToken-Attribut.
    // Verwendet data-color-token-Attribut und CSS-Klasse statt inline-style,
    // damit bluemonday im Backend das HTML durchlässt (class + data-* erlaubt, style nicht).
    export const ColorTokenExtension = TextStyle.extend({
      addAttributes() {
        return {
          ...this.parent?.(),
          colorToken: {
            default: null,
            parseHTML: (element: HTMLElement) => element.getAttribute('data-color-token') ?? null,
            renderHTML: (attributes: Record<string, unknown>) => {
              const token = attributes.colorToken as string | null
              if (!token || token === 'default') return {}
              return {
                'data-color-token': token,
                class: `color-token-${token}`,
              }
            },
          },
        }
      },
    })
    ```

    **Schritt 3: index.ts erstellen**
    ```typescript
    // frontend/src/components/editor/index.ts
    export { RichTextEditor } from './RichTextEditor'
    export { RichTextRenderer } from './RichTextRenderer'
    export { ColorTokenExtension, COLOR_TOKENS } from './ColorTokenExtension'
    export type { ColorToken } from './ColorTokenExtension'
    ```
    (RichTextEditor und RichTextRenderer existieren erst nach Task 2 — TypeScript-Fehler
    beim Build bis dann erwartet; index.ts trotzdem jetzt anlegen)
  </action>
  <verify>
    <automated>cd frontend && ls node_modules/@tiptap/react 2>/dev/null && echo "tiptap installiert"</automated>
  </verify>
  <acceptance_criteria>
    - `ls frontend/node_modules/@tiptap/react` → Verzeichnis existiert
    - `ls frontend/node_modules/@tiptap/starter-kit` → Verzeichnis existiert
    - `cat frontend/src/components/editor/ColorTokenExtension.ts | grep "COLOR_TOKENS"` → findet Treffer
    - `cat frontend/src/components/editor/ColorTokenExtension.ts | grep "data-color-token"` → findet Treffer
    - Datei enthält KEIN `style=` im renderHTML (nur class und data-color-token)
  </acceptance_criteria>
  <done>
    TipTap-Packages installiert.
    ColorTokenExtension.ts existiert mit COLOR_TOKENS-Export und renderHTML ohne inline style.
    index.ts existiert mit den Exporten (RichTextEditor/Renderer werden in Task 2 erstellt).
  </done>
</task>

<task type="auto">
  <name>Task 2: RichTextEditor + RichTextRenderer + CSS erstellen</name>
  <read_first>
    - frontend/src/components/editor/ColorTokenExtension.ts (aus Task 1)
    - frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css (CSS-Muster)
    - frontend/src/app/globals.css (für color-token CSS-Klassen)
    - .planning/phases/41-globalen-tiptap-rich-text-editor-einfuehren/41-RESEARCH.md (Pattern 1 + 6)
  </read_first>
  <files>
    frontend/src/components/editor/RichTextEditor.tsx,
    frontend/src/components/editor/RichTextEditor.module.css,
    frontend/src/components/editor/RichTextRenderer.tsx,
    frontend/src/components/editor/RichTextRenderer.module.css,
    frontend/src/app/globals.css
  </files>
  <action>
    **RichTextEditor.tsx erstellen**

    Erste Zeile MUSS `'use client'` sein.

    ```typescript
    'use client'

    import { useEditor, EditorContent } from '@tiptap/react'
    import StarterKit from '@tiptap/starter-kit'
    import { Table } from '@tiptap/extension-table'
    import TableRow from '@tiptap/extension-table-row'
    import TableCell from '@tiptap/extension-table-cell'
    import TableHeader from '@tiptap/extension-table-header'
    import Placeholder from '@tiptap/extension-placeholder'
    import CharacterCount from '@tiptap/extension-character-count'
    import { ColorTokenExtension, COLOR_TOKENS } from './ColorTokenExtension'
    import styles from './RichTextEditor.module.css'

    type RichTextEditorProps = {
      value: unknown | null
      onChange: (value: unknown) => void
      placeholder?: string
      helperText?: string
      mode?: 'longform' | 'shortnote'
      disabled?: boolean
      minHeight?: number
    }
    ```

    Editor-Konfiguration (KRITISCH: StarterKit muss unerlaubte Extensions deaktivieren):
    ```typescript
    const editor = useEditor({
      extensions: [
        StarterKit.configure({
          codeBlock: false,
          code: false,
          strike: false,
          hardBreak: false,
        }),
        Table.configure({ resizable: false }),
        TableRow,
        TableCell,
        TableHeader,
        ColorTokenExtension,
        Placeholder.configure({ placeholder: placeholder ?? 'Hier schreiben...' }),
        CharacterCount,
      ],
      content: (value as object) ?? { type: 'doc', content: [] },
      editable: !disabled,
      onUpdate: ({ editor }) => {
        onChange(editor.getJSON())
      },
    })
    ```

    Wenn value sich von außen ändert (z.B. beim Laden): `editor?.commands.setContent(...)` in einem
    useEffect mit Abhängigkeit auf value, aber NUR wenn sich der externe Wert vom internen unterscheidet
    (Referenz-Check oder deep-equal). Vorsicht: Endlos-Loop vermeiden.

    **Toolbar** (als eigene interne Komponente `EditorToolbar`):
    Buttons für: Paragraph, H1, H2, H3, Bold, Italic, BulletList, OrderedList, Blockquote,
    InsertTable (3x3 Standard), HorizontalRule, Undo, Redo.
    Farb-Dropdown: Map über COLOR_TOKENS (ohne 'default'), zeigt Farbname.
    Beim Klick: `editor.chain().focus().setMark('textStyle', { colorToken: token }).run()`
    "Farbe entfernen"-Button: `editor.chain().focus().unsetMark('textStyle').run()`

    **Shortnote-Modus**: Unter dem Editor ein `<p>` mit dem Hilfetext:
    "Diese Notizen beschreiben die konkrete Release-Version. Schreibe kurz, was du in deiner Rolle gemacht hast oder was an dieser Ausgabe besonders war. 2–5 Sätze reichen."
    Nur wenn `mode === 'shortnote'` anzeigen.

    **helperText**: Wenn vorhanden, über dem Editor anzeigen.

    **Datei unter 450 Zeilen halten.** Toolbar als interne Komponente in derselben Datei ist ok,
    solange das Limit eingehalten wird.

    ---

    **RichTextRenderer.tsx erstellen** (KEIN 'use client' nötig):

    ```typescript
    import styles from './RichTextRenderer.module.css'

    type RichTextRendererProps = {
      bodyHtml?: string | null
      bodyJson?: unknown | null
      editorType?: string | null
      contentSchemaVersion?: number | null
    }

    export function RichTextRenderer({ bodyHtml }: RichTextRendererProps) {
      if (!bodyHtml?.trim()) return null
      return (
        <div
          className={styles.richTextOutput}
          dangerouslySetInnerHTML={{ __html: bodyHtml }}
        />
      )
    }
    ```

    SICHERHEITSINVARIANTE: dangerouslySetInnerHTML NUR mit body_html (serverseitig sanitisiert).
    Niemals body_json direkt rendern ohne serverseitiges Sanitizing.

    ---

    **CSS-Dateien erstellen**

    RichTextEditor.module.css: Styles für Editor-Container, Toolbar, Toolbar-Buttons
    (aktiver Zustand = is-active Klasse die TipTap setzt), Editor-Content-Bereich mit
    minHeight-Fallback, Tabellen-Styles im Editor.

    RichTextRenderer.module.css: Styles für .richTextOutput (Prose-Styles): h1/h2/h3,
    p-Abstände, ul/ol-Einrückung, blockquote-Stil, table responsive.

    **globals.css erweitern** — color-token CSS-Klassen hinzufügen (benötigt für body_html-Ausgabe):
    ```css
    /* TipTap Color Tokens — Anwendung in body_html public output */
    .color-token-gray   { color: #6b7280; }
    .color-token-red    { color: #dc2626; }
    .color-token-orange { color: #ea580c; }
    .color-token-yellow { color: #ca8a04; }
    .color-token-green  { color: #16a34a; }
    .color-token-blue   { color: #2563eb; }
    .color-token-purple { color: #9333ea; }
    ```
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 frontend/src/components/editor/RichTextEditor.tsx` → `'use client'`
    - `grep "codeBlock: false" frontend/src/components/editor/RichTextEditor.tsx` → findet Treffer
    - `grep "hardBreak: false" frontend/src/components/editor/RichTextEditor.tsx` → findet Treffer
    - `grep "ColorTokenExtension" frontend/src/components/editor/RichTextEditor.tsx` → findet Treffer
    - `grep "dangerouslySetInnerHTML" frontend/src/components/editor/RichTextRenderer.tsx` → findet Treffer
    - `grep "color-token-red" frontend/src/app/globals.css` → findet Treffer
    - `cd frontend && npm run typecheck` → kein Fehler
    - `wc -l frontend/src/components/editor/RichTextEditor.tsx` → unter 450
  </acceptance_criteria>
  <done>
    RichTextEditor.tsx mit 'use client', StarterKit-Konfiguration, Toolbar und shortnote-Modus.
    RichTextRenderer.tsx als SSR-safe Komponente.
    CSS-Dateien für Editor und Renderer.
    globals.css mit color-token-* Klassen.
    npm run typecheck: kein Fehler.
  </done>
</task>

</tasks>

<verification>
```bash
cd frontend && npm run typecheck
ls frontend/src/components/editor/
grep "codeBlock: false" frontend/src/components/editor/RichTextEditor.tsx
grep "color-token-red" frontend/src/app/globals.css
wc -l frontend/src/components/editor/RichTextEditor.tsx
```
</verification>

<success_criteria>
- frontend/src/components/editor/ enthält: RichTextEditor.tsx, RichTextEditor.module.css, RichTextRenderer.tsx, RichTextRenderer.module.css, ColorTokenExtension.ts, index.ts
- npm run typecheck: kein Fehler
- RichTextEditor.tsx: erste Zeile 'use client', enthält StarterKit.configure mit codeBlock/code/strike/hardBreak: false
- ColorTokenExtension.ts: verwendet data-color-token statt style=
- globals.css: enthält color-token-* Klassen für alle 7 Farb-Tokens (ohne default)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/41-globalen-tiptap-rich-text-editor-einfuehren/41-04-SUMMARY.md` erstellen.
</output>
