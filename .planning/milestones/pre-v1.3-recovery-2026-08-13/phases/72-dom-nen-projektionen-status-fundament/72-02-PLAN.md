---
phase: 72-dom-nen-projektionen-status-fundament
plan: 02
type: execute
wave: 2
depends_on: ["72-01"]
files_modified:
  - backend/internal/repository/domain_projection_repository.go
  - backend/internal/handlers/domain_projection_handler.go
  - backend/internal/repository/domain_projection_repository_test.go
  - backend/cmd/server/main.go
autonomous: true
requirements: [A, H]
must_haves:
  truths:
    - "Eine Projektion liefert für eine Fansub-Gruppe Mitglied, historisches Mitglied und Mitwirkenden als getrennte JSON-Arrays"
    - "Eine anime_contributions-Zeile erzeugt NIE einen Mitglieds-Eintrag in der Mitglieder-Menge"
    - "Eine Contribution kann gleichzeitig status='confirmed' und dispute_state='open' in der Projektion tragen"
    - "claimed/unclaimed wird derived aus member_claims.claim_status, nicht aus Contributions"
    - "D-01: die Projektion liest dispute_state als separate Dimension neben dem erhaltenen Content-status (confirmed + open gleichzeitig darstellbar)"
    - "D-02: 'Das war ich' ist Contribution-/Claim-getrennt — claimed wird aus member_claims derived, eine Contribution-Beteiligung erzeugt nie einen Mitglieds-/Claim-Eintrag"
    - "D-03: die Projektion liest Sichtbarkeit (visibilities) und Review-Status (review_statuses) als zwei getrennte Achsen"
    - "D-05: ausschließlich GET-Read-Projektion, keine Schreib-Endpunkte"
  artifacts:
    - path: "backend/internal/repository/domain_projection_repository.go"
      provides: "Read-Projektion mit getrennten Mengen members/historical/contributors + dispute_state/visibility/review-Felder"
      min_lines: 60
    - path: "backend/internal/handlers/domain_projection_handler.go"
      provides: "Dünner GET-Handler, gibt DTO direkt zurück (kein Envelope)"
      min_lines: 20
    - path: "backend/internal/repository/domain_projection_repository_test.go"
      provides: "Source-Fragment-Test (no-DB): drei getrennte FROM-Mengen + dispute_state-Isolation + claimed-derived"
      min_lines: 40
  key_links:
    - from: "domain_projection_repository.go"
      to: "hist_fansub_group_members / fansub_group_members / anime_contributions"
      via: "drei getrennte SELECT-Joins je Menge (keine UNION-Flachliste)"
      pattern: "FROM (fansub_group_members|hist_fansub_group_members|anime_contributions)"
    - from: "domain_projection_repository.go"
      to: "review_statuses / visibilities"
      via: "LEFT JOIN auf anime_contributions.review_status_id / visibility_id"
      pattern: "LEFT JOIN (review_statuses|visibilities)"
    - from: "main.go"
      to: "EXAKTE GET-Route: GET /api/v1/fansubs/:id/domain-projection"
      via: "v1.GET registration (GET-only, kein POST/PATCH/DELETE)"
      pattern: "\\.GET\\(\"/fansubs/:id/domain-projection\""
    - from: "domain_projection_handler.go"
      to: "Response-Envelope-Form: DIREKTES DTO, KEIN {\"data\":...}-Wrapper"
      via: "c.JSON(http.StatusOK, response)"
      pattern: "c\\.JSON\\(http\\.StatusOK, response\\)"
---

<objective>
Baue die kanonische Domänen-Lese-Projektion, die Gruppenmitglied vs. historisches Mitglied vs. externer Mitwirkender als drei getrennte, typisierte Mengen liefert (eine Beteiligung erzeugt NIE einen Mitglieds-Eintrag), inklusive der neuen Statusachsen `dispute_state`, Sichtbarkeit und Review aus Migration 0096.

Purpose: Erfüllt Success Criterion 1 (Trennung der Mengen) und liefert das Lese-Aggregat, das die Phasen 73/74/76/78/80 ohne doppelte DTO-Arbeit konsumieren.
Output: Neues Projektions-Repo + dünner GET-Handler + Wave-0-Test + GET-only Routen-Wiring.

GEPINNTER CONTRACT (verbindlich, Plan 04 spiegelt exakt dies — kein Beispiel):
- GET-Route: `GET /api/v1/fansubs/:id/domain-projection` (GET-only, D-05).
- Envelope: DIREKTES DTO `{ members: [...], historical: [...], contributors: [...] }`, KEIN `{"data":...}`-Wrapper (folgt dem benachbarten contributions_public_handler-Analog, RESEARCH Q3 → no-envelope).
- Status-/Sichtbarkeits-Felder kommen aus 0096 ausschließlich per FK-Lookup-Join (review_status_id → review_statuses.code, visibility_id → visibilities.name).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-dom-nen-projektionen-status-fundament/72-RESEARCH.md
@.planning/phases/72-dom-nen-projektionen-status-fundament/72-PATTERNS.md

<interfaces>
<!-- Kanonisches Projektions-Muster — Executor kopiert Struktur, baut keine Codebase-Erkundung -->
From backend/internal/repository/anime_contributions_public_repository.go (CANONICAL):
- const memberSlugExpr / memberDisplayExpr (Zeilen 11-18) — Slug/Display NICHT hand-rollen
- type PublicContributorRow { MemberDisplayName string `json:"member_display_name"`; MemberSlug *string `json:"member_slug"`; Roles []string `json:"roles"`; RoleLabels []string `json:"role_labels"`; StartedYear *int; EndedYear *int; IsVerified bool }
- Error-Wrapping: fmt.Errorf("context: %w", err) auf Query/Scan/rows.Err()

From backend/internal/repository/member_archive_repository.go:
- type MemberArchiveRepository struct { db *pgxpool.Pool }; func NewMemberArchiveRepository(db *pgxpool.Pool) *MemberArchiveRepository — Muster für NEUE eigene Repo-Struct+Konstruktor

From backend/internal/handlers/contributions_public_handler.go (exact analog):
- type ContributionsPublicHandler struct { repo *repository.AnimeContributionsRepository }
- thin method: parseAnimeID(c.Param), badRequest/internalError Helper (existieren bereits), c.JSON(http.StatusOK, response) — KEIN {"data":...}-Envelope bei Public-Reads (GEPINNT)

From backend/cmd/server/main.go (wiring, Zeilen ~364/397/403-405):
- repo := repository.New...(dbPool); handler := handlers.New...(repo); v1.GET("/...", handler.Method) — Dependencies manuell, kein DI-Container

<!-- no-DB Test-Pattern (load-bearing, kein t.Skip) -->
From backend/internal/repository/runtime_authority_test.go (CANONICAL no-DB Pattern):
- readRepositorySource(t, "domain_projection_repository.go") liefert den Quelltext der Repo-Datei; strings.ToLower + strings.Contains(fragment) je erwartetem SQL-Fragment, sonst t.Fatalf. Kein DB-Pool, kein Skip.

Quell-Tabellen-Trennung (RESEARCH §Membership vs. Contribution Separation):
- Gruppenmitglied (App): fansub_group_members + fansub_group_member_roles, status='active'
- Historisches Mitglied: hist_fansub_group_members + hist_group_member_roles, status IN ('historical','confirmed'), visibility='public' für public
- Mitwirkender: anime_contributions JOIN hist_fansub_group_members, is_public_on_anime_page / is_public_on_member_profile
- claimed/unclaimed: derived aus member_claims.claim_status='verified', NIE aus Contributions (Lock H)
- Rollen-Labels: COALESCE(rd.label_de, code) via LEFT JOIN role_definitions rd
- Neue Felder aus 0096 (FK-Lookup): anime_contributions.dispute_state, LEFT JOIN visibilities ON visibilities.id = anime_contributions.visibility_id, LEFT JOIN review_statuses ON review_statuses.id = anime_contributions.review_status_id
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 Source-Fragment-Test für Mengen-Trennung und dispute_state-Isolation (no-DB)</name>
  <files>backend/internal/repository/domain_projection_repository_test.go</files>
  <read_first>
    - backend/internal/repository/runtime_authority_test.go (VERBINDLICHES no-DB Muster: readRepositorySource + strings.Contains; KEIN DB-Pool, KEIN t.Skip)
    - .planning/phases/72-dom-nen-projektionen-status-fundament/72-RESEARCH.md (§Validation Architecture: Test-Map A/Sep, D-01)
  </read_first>
  <behavior>
    - Die Repo-Datei MUSS drei getrennte FROM-Mengen enthalten (members / historical / contributors), keine UNION-Flachliste.
    - Die Contributor-Query projiziert dispute_state UNABHÄNGIG vom Content-status (beide Spalten werden selektiert; status wird NICHT durch dispute überschrieben).
    - claimed wird aus member_claims.claim_status='verified' abgeleitet, nicht aus anime_contributions.
  </behavior>
  <action>
    Schreibe den Test VOR der Repo-Implementierung (RED — readRepositorySource schlägt fehl, weil die Datei fehlt). KEIN DB-Pool, KEIN t.Skip — load-bearing Source-Fragment-Test nach dem Muster `runtime_authority_test.go`.

    `TestProjectionSeparatesThreeSets` — `content := readRepositorySource(t, "domain_projection_repository.go")`; `normalized := strings.ToLower(content)`; assertiere per strings.Contains (sonst t.Fatalf):
      - "from fansub_group_members"
      - "from hist_fansub_group_members"
      - "from anime_contributions"
      - Negativ: KEIN "union" (die drei Mengen sind getrennte SELECTs, keine UNION-Flachliste).

    `TestProjectionDisputeStateIsolated` — derselbe Quelltext; assertiere:
      - "dispute_state" wird selektiert (z. B. "ac.dispute_state" oder "dispute_state").
      - "ac.status" / "status" wird ebenfalls selektiert (Content-status bleibt erhalten).
      - LEFT JOIN auf review_statuses und visibilities ist vorhanden ("left join review_statuses", "left join visibilities").

    `TestProjectionClaimedDerivedFromClaims` — assertiere:
      - "member_claims" und "'verified'" kommen im Quelltext vor (claimed leitet sich aus member_claims.claim_status='verified' ab).
      - Negativ: claimed wird NICHT aus anime_contributions abgeleitet (keine Konstruktion, die contribution-Existenz als claimed wertet — prüfe, dass das claimed-Feld im member-SELECT-Block via member_claims befüllt wird; assertiere "claim_status").

    Optional zusätzlich `TestProjectionHandlerHasNoEnvelope` — readBackendSource(t, filepath.Join("internal","handlers","domain_projection_handler.go")); assertiere "c.json(http.statusok, response)" und Negativ: kein "\"data\"" Wrapper-Fragment.

    Alle Fragmente sind kanonisch; Task 2 baut exakt diese SQL/Handler-Form.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/repository/... -run TestProjection</automated>
  </verify>
  <acceptance_criteria>
    - Quell-Assertion: Datei deklariert TestProjectionSeparatesThreeSets, TestProjectionDisputeStateIsolated, TestProjectionClaimedDerivedFromClaims.
    - Behavior-Assertion: VOR Task 2 hart RED (readRepositorySource → t.Fatalf), NACH Task 2 grün. KEIN t.Skip im Test.
    - Quell-Assertion: Tests prüfen drei getrennte FROM-Klauseln, dispute_state+status getrennt selektiert, member_claims-derived claimed, FK-Lookup-Joins.
  </acceptance_criteria>
  <done>Source-Fragment-Tests kompilieren, laufen ohne DB, sind load-bearing und sperren die Trennungs-/Isolations-/derived-Invarianten.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Projektions-Repo + dünner GET-Handler + Wiring</name>
  <files>backend/internal/repository/domain_projection_repository.go, backend/internal/handlers/domain_projection_handler.go, backend/cmd/server/main.go</files>
  <read_first>
    - backend/internal/repository/anime_contributions_public_repository.go (CANONICAL Projektions-Muster, DTO-Shape, Error-Wrapping)
    - backend/internal/repository/member_archive_repository.go (eigene Struct+Konstruktor für NEUES Repo)
    - backend/internal/handlers/contributions_public_handler.go (dünner Handler, parseAnimeID/badRequest/internalError, c.JSON ohne Envelope)
    - backend/cmd/server/main.go (Wiring-Zeilen ~364/397/403-405)
    - database/migrations/0096_v12_status_foundation.up.sql (FK-Spaltennamen aus Plan 01: visibility_id, review_status_id, dispute_state, profile_status)
  </read_first>
  <behavior>
    - GetFansubGroupDomainProjection(ctx, groupID) liefert Response mit getrennten Arrays members/historical/contributors.
    - Mitwirkenden-DTO enthält dispute_state ('none'|'open'|'resolved'), visibility (aus visibilities.name) und review_status (aus review_statuses.code).
    - Member-DTO enthält claimed (bool, derived aus member_claims) und memorial-Status (aus members.profile_status, nur Lesewert).
  </behavior>
  <action>
    Erstelle ein NEUES fokussiertes Repo (< 450 Zeilen, eigene Struct `DomainProjectionRepository` + `NewDomainProjectionRepository`) nach dem Muster von `member_archive_repository.go`, mit drei getrennten SELECT-Joins (KEINE UNION-Flachliste). DTO-Struct mit snake_case json-Tags: Response `{ members: [...], historical: [...], contributors: [...] }`. Contributor-Row trägt zusätzlich `dispute_state`, `visibility`, `review_status` — Werte aus 0096 via `LEFT JOIN visibilities ON visibilities.id = ac.visibility_id` (→ visibilities.name als `visibility`) und `LEFT JOIN review_statuses ON review_statuses.id = ac.review_status_id` (→ review_statuses.code als `review_status`). Der Content-`status` wird unverändert mitselektiert (NIE durch dispute überschrieben). Member-Row trägt `profile_status` (memorial-fähig, Lesewert) und `claimed` (derived aus member_claims.claim_status='verified'). Nutze memberSlugExpr/memberDisplayExpr und COALESCE(rd.label_de, code). Alle User-Eingaben als pgx-`$N`-Parameter, keine String-Konkatenation. Sicherheits-WHERE: public-Projektionsteile gaten auf is_public_*=true AND visibility public-äquivalent.

    Erstelle den dünnen Handler (`DomainProjectionHandler`, GET-Methode, parseGroupID/badRequest/internalError, `c.JSON(http.StatusOK, response)` DIREKT — KEIN Envelope, GEPINNT). Verdrahte in main.go: Repo konstruieren → Handler konstruieren → GET-only Route registrieren mit EXAKTEM Pfad `v1.GET("/fansubs/:id/domain-projection", handler.GetFansubGroupDomainProjection)`. KEINE Schreib-Routen (D-05). Halte die Datei unter 450 Zeilen (Pitfall 4).

    Der Source-Fragment-Test aus Task 1 prüft die drei FROM-Klauseln, dispute_state+status getrennt, FK-Lookup-Joins und (optional) das no-envelope-Handler-Fragment — schreibe Repo/Handler so, dass alle Fragmente exakt vorkommen.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/repository/... -run TestProjection</automated>
  </verify>
  <acceptance_criteria>
    - Quell-Assertion: domain_projection_repository.go deklariert DomainProjectionRepository + NewDomainProjectionRepository und ein Response-DTO mit json-Tags `members`/`historical`/`contributors`.
    - Quell-Assertion: Contributor-DTO hat Felder mit json-Tags `dispute_state`, `visibility`, `review_status`; Member-DTO hat `profile_status` und `claimed`. SQL nutzt FK-Lookup-Joins (review_statuses, visibilities) auf review_status_id/visibility_id.
    - Quell-Assertion: main.go registriert exakt `v1.GET("/fansubs/:id/domain-projection", ...)`; Handler antwortet mit direktem DTO (c.JSON(http.StatusOK, response), kein {"data"}).
    - Behavior-Assertion: `go build ./...` erfolgreich; TestProjection grün.
    - Negativ-Assertion: keine `.POST(`/`.PATCH(`/`.DELETE(`-Registrierung für diese Projektion in main.go; Datei < 450 Zeilen; keine UNION-Flachliste.
  </acceptance_criteria>
  <done>Drei Mengen werden getrennt projiziert mit Status-/Sichtbarkeits-/Review-/Konflikt-Feldern; GET-only unter exaktem Pfad verdrahtet; no-envelope; Build + Source-Fragment-Tests grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Public GET-Read | untrusted groupID-Param betritt die Projektions-Query |
| Projektion → Public-DTO | interne/unbestätigte Daten dürfen nicht in Public-Antwort gelangen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-02-01 | Tampering | groupID-Param in SQL | mitigate | Ausschließlich pgx-`$N`-Parameter; parseGroupID validiert int64 (ASVS V5) |
| T-72-02-02 | Information Disclosure | interne/unbestätigte Nennungen in Public-Read | mitigate | WHERE gated auf is_public_*=true AND visibility public; review_status/dispute_state nur wo fachlich gewollt exponiert (RESEARCH §Security Domain) |
| T-72-02-03 | Spoofing | Mitgliedschaft aus Contribution abgeleitet | mitigate | Drei getrennte Quell-Tabellen-SELECTs; Source-Fragment-Test sperrt drei FROM-Klauseln + verbietet UNION (Lock A) |
| T-72-02-SC | Tampering | npm/pip/cargo installs | accept | Keine Paket-Installs in dieser Phase |
</threat_model>

<verification>
- `cd backend && go build ./...` erfolgreich.
- `cd backend && go test ./internal/repository/... -run TestProjection` grün (no-DB Source-Fragment-Test, load-bearing).
- main.go registriert nur GET `/fansubs/:id/domain-projection` für die neue Projektion.
</verification>

<success_criteria>
- Projektion liefert members/historical/contributors getrennt; Contribution erzeugt keinen Mitglieds-Eintrag.
- dispute_state ist getrennt vom Content-status lesbar; claimed derived aus member_claims.
- Keine Schreib-Endpunkte; Datei < 450 Zeilen; gepinnte Route + no-envelope.
</success_criteria>

<output>
Create `.planning/phases/72-dom-nen-projektionen-status-fundament/72-02-SUMMARY.md` when done
</output>
