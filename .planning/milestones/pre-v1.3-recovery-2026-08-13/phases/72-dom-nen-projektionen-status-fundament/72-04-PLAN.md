---
phase: 72-dom-nen-projektionen-status-fundament
plan: 04
type: execute
wave: 3
depends_on: ["72-02", "72-03"]
files_modified:
  - shared/contracts/openapi.yaml
  - frontend/src/types/domain-projection.ts
  - frontend/src/types/media-ownership.ts
  - frontend/src/lib/api.ts
  - frontend/src/types/__tests__/v12-projection-contract.test.ts
autonomous: true
requirements: [K, A]
must_haves:
  truths:
    - "Jedes neue DTO-Feld aus Plan 02/03 ist in openapi.yaml dokumentiert"
    - "Jedes neue DTO-Feld hat einen 1:1 snake_case TypeScript-Spiegel"
    - "Frontend ruft die neuen Reads über api.ts-Clientfunktionen ab, nicht ad-hoc"
    - "Keine undokumentierten Response-Felder existieren in den neuen Typen"
    - "OpenAPI-Pfade und Envelope spiegeln EXAKT die gepinnten Plan-02/03-Contracts"
    - "D-05: nur Contract-/Typen-Abgleich für die Read-Projektionen, keine Schreib-Endpunkte dokumentiert oder verdrahtet"
  artifacts:
    - path: "frontend/src/types/domain-projection.ts"
      provides: "TS-Spiegel der Domänen-Projektions-DTOs (members/historical/contributors + dispute_state/visibility/review)"
      min_lines: 25
    - path: "frontend/src/types/media-ownership.ts"
      provides: "TS-Spiegel der Medien-Ownership-Projektions-DTOs"
      min_lines: 15
    - path: "frontend/src/lib/api.ts"
      provides: "Clientfunktionen für die neuen GET-Reads über apiClientFetch/getApiBaseUrl"
      contains: "getApiBaseUrl"
  key_links:
    - from: "frontend/src/lib/api.ts"
      to: "GEPINNTE Backend GET-Routen: /fansubs/:id/domain-projection und /media-ownership/:ownerType/:ownerId"
      via: "apiClientFetch / fetch über getApiBaseUrl"
      pattern: "fansubs/.*domain-projection|media-ownership"
    - from: "shared/contracts/openapi.yaml"
      to: "GEPINNTE Pfade /fansubs/{id}/domain-projection + /media-ownership/{ownerType}/{ownerId}"
      via: "paths"
      pattern: "/fansubs/\\{id\\}/domain-projection|/media-ownership/"
    - from: "shared/contracts/openapi.yaml + TS-Typen"
      to: "Envelope-Form: DIREKTES DTO, KEIN {\"data\":...}-Wrapper (mirror Plan 02/03)"
      via: "responses 200 content schema = direktes DTO"
      pattern: "dispute_state"
---

<objective>
Schließe den Contract-Slice (Lock K): dokumentiere alle in Plan 02/03 entstandenen Read-DTO-Felder in `openapi.yaml`, spiegele sie 1:1 als snake_case TypeScript-Typen, baue die `api.ts`-Clientfunktionen und sperre die Parität per Typ-Test.

Purpose: Erfüllt Entscheidung K (Contract/API-Disziplin, keine undokumentierten Felder, kein ad-hoc-Fetch) und macht das Phase-72-Fundament für die Frontend-Surfaces 73–80 konsumierbar.
Output: OpenAPI-Schemas/Pfade + zwei TS-Typdateien + api.ts-Clientfunktionen + Paritäts-Typ-Test.

GEPINNTER CONTRACT (mirror, NICHT neu erfinden — exakt aus Plan 02/03 übernommen):
- Pfad Domäne: `GET /api/v1/fansubs/{id}/domain-projection` → direktes DTO `{ members, historical, contributors }` (KEIN {"data"}-Envelope).
- Pfad Medien: `GET /api/v1/media-ownership/{ownerType}/{ownerId}` → direkte Row-Liste (KEIN {"data"}-Envelope).
- review_status-Werte = review_statuses.code aus 0096: in_review/approved/rejected/archived/removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-dom-nen-projektionen-status-fundament/72-RESEARCH.md
@.planning/phases/72-dom-nen-projektionen-status-fundament/72-PATTERNS.md
@.planning/phases/72-dom-nen-projektionen-status-fundament/72-02-PLAN.md
@.planning/phases/72-dom-nen-projektionen-status-fundament/72-03-PLAN.md

<executor_note>
GROSSE DATEIEN — minimale Append-Edits, kein Strukturumbau:
- `frontend/src/lib/api.ts` (~7700+ Zeilen) und `shared/contracts/openapi.yaml` (~9600+ Zeilen) sind sehr groß. NUR additive Append-Style-Ergänzungen (neue Schemas/Pfade bzw. zwei neue Clientfunktionen am Ende des passenden Abschnitts), KEINE Reorganisation bestehender Blöcke, KEINE Reformatierung. Nach JEDER Ergänzung sofort `npm run typecheck` + den Paritäts-Test laufen lassen, um Drift früh zu fangen. Bestehende Public-Read-Funktionen (z. B. getAnimeContributions) als Vorbild für Platzierung/Stil nutzen.
</executor_note>

<interfaces>
<!-- Contract-/Typ-Muster — Executor spiegelt die in Plan 02/03 gebauten DTOs -->
From shared/contracts/contributions.yaml (Pfad-/Schema-Muster):
- Path-Block: tags/summary/description/operationId/parameters (int64, minimum:1)/responses mit $ref + ErrorResponse
- Schema-Block: type object, required[...], properties mit nullable:true für Go *T
From frontend/src/types/contributions.ts (1:1 snake_case Spiegel):
- export interface PublicAnimeContribution { member_display_name: string; member_slug: string | null; roles: string[]; is_verified: boolean }
- Literal-Unions spiegeln Go CHECK: status: 'confirmed'|'proposed'|'draft'|'disputed'|'hidden'
  -> analog: dispute_state: 'none'|'open'|'resolved'; review_status: 'in_review'|'approved'|'rejected'|'archived'|'removed'
From frontend/src/lib/api.ts (Client-fn, ~Zeile 7706 getAnimeContributions):
- getApiBaseUrl() + shared error helpers (parseApiErrorPayload, ApiError); für authenticated reads apiClientFetch (~Zeile 1253); encodeURIComponent für slug-Params; KEIN ad-hoc Browser->Backend-Fetch (Lock K)

GEPINNTE Routen + Envelope (aus Plan 02/03, EXAKT spiegeln):
- GET /api/v1/fansubs/{id}/domain-projection  → 200: direktes DTO { members[], historical[], contributors[] } (kein {"data"})
- GET /api/v1/media-ownership/{ownerType}/{ownerId}  → 200: direkte Liste MediaOwnershipRow[] (kein {"data"})

Quelle der zu spiegelnden Felder = die in Plan 02/03 gebauten Go-DTOs:
- domain projection: members[], historical[], contributors[] (contributor: member_display_name, member_slug, roles, role_labels, dispute_state, visibility, review_status; member: profile_status, claimed)
- media ownership: owner_type, owner_id, media_category, visibility, review_status
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OpenAPI-Schemas + gepinnte Pfade für die neuen Reads</name>
  <files>shared/contracts/openapi.yaml</files>
  <read_first>
    - shared/contracts/contributions.yaml (Pfad- + Schema-Block-Muster)
    - shared/contracts/openapi.yaml (bestehende components/schemas + paths Struktur)
    - .planning/phases/72-dom-nen-projektionen-status-fundament/72-02-PLAN.md (DTO-Felder + gepinnte Route/Envelope Domäne)
    - .planning/phases/72-dom-nen-projektionen-status-fundament/72-03-PLAN.md (DTO-Felder + gepinnte Route/Envelope Medien)
  </read_first>
  <action>
    Append-Style: Ergänze in openapi.yaml unter components/schemas die Schemas, die exakt die Go-DTO-Felder aus Plan 02/03 spiegeln (kein Strukturumbau bestehender Blöcke). Für die Domänen-Projektion ein Response-Schema mit `members`/`historical`/`contributors` (jeweils array of object), wobei die Contributor-Objekt-Properties `member_display_name`, `member_slug` (nullable), `roles`, `role_labels`, `dispute_state` (enum none/open/resolved), `visibility` (string aus visibilities), `review_status` (enum in_review/approved/rejected/archived/removed) tragen, und das Member-Objekt `profile_status` (enum active/historical/memorial) und `claimed` (boolean). Für die Medien-Projektion ein Schema mit `owner_type`, `owner_id`, `media_category`, `visibility`, `review_status`.
    Ergänze die GEPINNTEN GET-Pfade EXAKT wie in Plan 02/03 registriert: `/fansubs/{id}/domain-projection` (parameter id int64 minimum:1) und `/media-ownership/{ownerType}/{ownerId}` (ownerType string, ownerId int64 minimum:1). responses 200 mit content = DIREKTES DTO-$ref (KEIN {"data"}-Wrapper — mirror der Plan-02/03-no-envelope-Entscheidung); 400/404 ErrorResponse. Felder mit Go *T als `nullable: true`.
    Nach der Ergänzung sofort YAML validieren (verify).
  </action>
  <verify>
    <automated>cd frontend && npx --yes @redocly/cli@latest lint ../shared/contracts/openapi.yaml || node -e "const y=require('js-yaml');const fs=require('fs');y.load(fs.readFileSync('../shared/contracts/openapi.yaml','utf8'));console.log('yaml-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Quell-Assertion: openapi.yaml enthält ein Domänen-Projektions-Response-Schema mit Properties members/historical/contributors und ein Medien-Schema mit owner_type/owner_id/media_category/visibility/review_status.
    - Quell-Assertion: Pfade `/fansubs/{id}/domain-projection` und `/media-ownership/{ownerType}/{ownerId}` existieren als GET; responses 200 referenzieren direktes DTO (kein {"data"}-Envelope).
    - Quell-Assertion: dispute_state-Enum (none/open/resolved) und review_status-Enum (in_review/approved/rejected/archived/removed) sind als Schema-Enums dokumentiert.
    - Behavior-Assertion: YAML parst fehlerfrei (Lint oder js-yaml load erfolgreich).
    - Negativ-Assertion: kein DTO-Feld aus Plan 02/03 fehlt im Contract; kein {"data"}-Wrapper für die neuen Reads.
  </acceptance_criteria>
  <done>Alle neuen Read-DTO-Felder + gepinnte GET-Pfade (no-envelope) sind in openapi.yaml dokumentiert; YAML valide.</done>
</task>

<task type="auto">
  <name>Task 2: TS-Typen + api.ts-Clientfunktionen + Paritäts-Typ-Test</name>
  <files>frontend/src/types/domain-projection.ts, frontend/src/types/media-ownership.ts, frontend/src/lib/api.ts, frontend/src/types/__tests__/v12-projection-contract.test.ts</files>
  <read_first>
    - frontend/src/types/contributions.ts (1:1 snake_case Spiegel + Literal-Union-Muster)
    - frontend/src/lib/api.ts (getAnimeContributions ~Zeile 7706: getApiBaseUrl, parseApiErrorPayload, ApiError, apiClientFetch ~Zeile 1253)
    - shared/contracts/openapi.yaml (die in Task 1 ergänzten Schemas + gepinnte Pfade — Single Source of Truth für Feldnamen und Routen)
  </read_first>
  <action>
    Erstelle `domain-projection.ts` und `media-ownership.ts` als 1:1 snake_case-Spiegel der OpenAPI-Schemas aus Task 1: nullable Go *T → `| null`; CHECK/Enum → Literal-Union (`dispute_state: 'none' | 'open' | 'resolved'`, `review_status: 'in_review' | 'approved' | 'rejected' | 'archived' | 'removed'`, `profile_status: 'active' | 'historical' | 'memorial'`). Response-Interface mit `members`/`historical`/`contributors` (Domäne, DIREKTES DTO ohne data-Wrapper) bzw. der Medien-Row-Liste (`MediaOwnershipRow[]`, direkt).
    Ergänze in api.ts (Append-Style, neben getAnimeContributions) Clientfunktionen, die die GEPINNTEN Routen exakt treffen: `getFansubGroupDomainProjection(groupID: number)` → `${getApiBaseUrl()}/fansubs/${groupID}/domain-projection`, und `getMediaOwnershipProjection(ownerType: string, ownerID: number)` → `${getApiBaseUrl()}/media-ownership/${encodeURIComponent(ownerType)}/${ownerID}`. Beide nach dem `getAnimeContributions`-Muster: Fehlerbehandlung über parseApiErrorPayload/ApiError, KEIN ad-hoc-Fetch außerhalb dieses Seams (Lock K), keine Token-/Cookie-Direktzugriffe. Antwort direkt als DTO parsen (kein {"data"}-Unwrap).
    Schreibe `v12-projection-contract.test.ts` (Vitest): assertiere per type-level + runtime-Shape-Check, dass die Literal-Unions exakt die OpenAPI-Enums abbilden und dass die Response-Interfaces die drei Mengen-Felder bzw. die fünf Medien-Achsen tragen (kein undokumentiertes Feld). Nutze einen kompakten Ansatz (z. B. `satisfies`/exhaustive-Union-Check), der bei abweichendem Feldsatz fehlschlägt.
    Nach jeder Ergänzung sofort typecheck + Paritäts-Test laufen lassen (executor_note).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run v12-projection-contract && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Quell-Assertion: domain-projection.ts exportiert ein Response-Interface mit members/historical/contributors; media-ownership.ts exportiert ein Row-Interface mit owner_type/owner_id/media_category/visibility/review_status.
    - Quell-Assertion: api.ts enthält zwei neue Clientfunktionen, die getApiBaseUrl() nutzen und exakt die gepinnten Pfade (/fansubs/${id}/domain-projection, /media-ownership/${ownerType}/${ownerId}) treffen; kein roher ad-hoc-Fetch außerhalb des Seams.
    - Behavior-Assertion: `npx vitest run v12-projection-contract` grün; `npm run typecheck` ohne Fehler.
    - Negativ-Assertion: Literal-Unions enthalten exakt die OpenAPI-Enum-Werte (keine zusätzlichen/fehlenden Werte); keine direkten Token-/Cookie-Zugriffe in den neuen Clientfunktionen; kein {"data"}-Unwrap.
  </acceptance_criteria>
  <done>TS-Typen spiegeln die Contracts 1:1; api.ts-Clientfunktionen treffen die gepinnten Routen über den zentralen Seam; Paritäts-Test + typecheck grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → Backend GET | Aufrufe müssen über den zentralen api.ts-Seam laufen (kein ad-hoc-Fetch) |
| Contract ↔ Runtime | undokumentierte Felder dürfen nicht stillschweigend exponiert werden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-04-01 | Information Disclosure | undokumentierte Response-Felder | mitigate | Jedes Feld in openapi.yaml + TS-Typ; Paritäts-Test sperrt Drift (Lock K) |
| T-72-04-02 | Tampering / Spoofing | ad-hoc Browser→Backend-Fetch, Token-Direktzugriff | mitigate | Clientfunktionen ausschließlich über getApiBaseUrl/apiClientFetch-Seam; keine Token-/Cookie-Lesungen im UI (Lock K) |
| T-72-04-03 | Tampering | Enum-Drift zwischen Go CHECK und TS-Union | mitigate | Literal-Unions exakt = OpenAPI-Enums = Go CHECK; Vitest-Paritäts-Test |
| T-72-04-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neuen Runtime-Pakete; `@redocly/cli` nur als optionales Lint-Tool im verify (Fallback js-yaml), kein Projekt-Dependency-Add |
</threat_model>

<verification>
- `cd frontend && npx vitest run v12-projection-contract` grün.
- `cd frontend && npm run typecheck` ohne Fehler.
- openapi.yaml parst fehlerfrei; jedes Plan-02/03-DTO-Feld dokumentiert; gepinnte Pfade + no-envelope gespiegelt.
</verification>

<success_criteria>
- Alle neuen Read-DTO-Felder dokumentiert (OpenAPI) und 1:1 als snake_case TS gespiegelt.
- api.ts-Clientfunktionen über den zentralen Seam, treffen die gepinnten Routen; kein ad-hoc-Fetch.
- Paritäts-Test + typecheck grün; keine undokumentierten Felder; kein {"data"}-Envelope eingeführt.
</success_criteria>

<output>
Create `.planning/phases/72-dom-nen-projektionen-status-fundament/72-04-SUMMARY.md` when done
</output>
