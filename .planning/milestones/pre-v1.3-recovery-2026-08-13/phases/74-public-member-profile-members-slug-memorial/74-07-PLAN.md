---
phase: 74-public-member-profile-members-slug-memorial
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/repository/anime_contributions_public_repository.go
  - backend/internal/repository/anime_contributions_public_member_test.go
  - frontend/src/types/contributions.ts
  - shared/contracts/contributions.yaml
autonomous: true
gap_closure: true
requirements: [C, K, D-06, D-07]
must_haves:
  truths:
    - "Die role_timeline eines Members enthält aktuelle App-Gruppenrollen als context='group_history', sauber getrennt von anime_contribution-Einträgen (GAP-3, D-06)"
    - "Member 3 (Ballelboy, app_user 2) = fansub_lead@AnimeOwnage liefert genau einen group_history-Timeline-Eintrag ohne Duplikat (GAP-3)"
    - "PublicMemberRoleEntry trägt ein notes-Feld (aus anime_contributions.note) als Detaildaten-Quelle für den Inline-Expand (GAP-2/D-07)"
    - "Der Contract (Backend-Struct, contributions.yaml, frontend type) ist Lock-K-konsistent erweitert (notes)"
  artifacts:
    - path: "backend/internal/repository/anime_contributions_public_repository.go"
      provides: "GetPublicMemberContributions mit 3. UNION-Branch (app group roles) + notes-Projektion in allen Branches; PublicMemberRoleEntry.Notes"
      contains: "fansub_group_member_roles"
    - path: "frontend/src/types/contributions.ts"
      provides: "PublicMemberRoleEntry.notes (string | null)"
      contains: "notes"
  key_links:
    - from: "GetPublicMemberContributions 3. UNION-Branch"
      to: "fansub_group_members + fansub_group_member_roles + role_definitions"
      via: "member->app_user-Auflösung (verified member_claims ODER members.user_id)"
      pattern: "fansub_group_member_roles"
---

<objective>
Backend-/Contract-Erweiterung für die Contributions-Timeline (GAP-3 + GAP-2-Datenquelle):

1. **GAP-3 (Datenquelle, Nutzer-Entscheidung = aufnehmen):** `GetPublicMemberContributions` liest aktuell nur `hist_group_member_roles` + `anime_contributions`. Aktuelle App-Gruppenrollen aus `fansub_group_members` (+ `fansub_group_member_roles.role`) fehlen — dadurch ist die Timeline für rein-App-aktive Member (z. B. Ballelboy) leer. Ein 3. UNION-Branch ergänzt sie als `context='group_history'`, sauber getrennt, ohne Duplikate zu hist-Einträgen.
2. **GAP-2 (Detaildaten):** `PublicMemberRoleEntry` trägt keine Notes. Für den Inline-Expand (D-07) wird `anime_contributions.note` als `notes` end-to-end (Lock K) ergänzt.

Purpose: Wave-1-Fundament. Ohne diese Datenquelle bleibt die Live-Timeline leer (GAP-3) und der Inline-Expand zeigt keine echten Detaildaten (GAP-2). Die Frontend-Verdrahtung (Plan 74-09) baut darauf auf.
Output: erweiterter Repo-Query + Struct, neuer/erweiterter Go-Test, erweiterter frontend type, aktualisierter Contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-CONTEXT.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-VERIFICATION.md

<interfaces>
<!-- Aus Codebase extrahiert — Executor nutzt diese direkt, keine Exploration nötig. -->

backend PublicMemberRoleEntry (anime_contributions_public_repository.go:71-82):
  FansubGroupName string  `json:"fansub_group_name"`
  FansubGroupSlug string  `json:"fansub_group_slug"`
  RoleCode        string  `json:"role_code"`
  RoleLabel       string  `json:"role_label"`
  Context         string  `json:"context"`
  AnimeTitle      *string `json:"anime_title"`
  AnimeID         *int64  `json:"anime_id"`
  StartedYear     *int    `json:"started_year"`
  EndedYear       *int    `json:"ended_year"`
  Status          string  `json:"status"`
  // NEU: Notes *string `json:"notes"`

frontend PublicMemberRoleEntry (frontend/src/types/contributions.ts:174-185):
  fansub_group_name / fansub_group_slug / role_code / role_label
  context: 'group_history' | 'anime_contribution'
  anime_title: string | null; anime_id: number | null
  started_year / ended_year: number | null; status: string
  // NEU: notes: string | null

member->app_user-Auflösung (Muster aus member_profile_repository.go GetPublicMemberProfile-CTE, Z. 364-392):
  COALESCE(claim_user.app_user_id, legacy_user.id)
    claim_user = verified member_claims (claim_status='verified', neuester verified_at)
    legacy_user = app_users.legacy_user_id = m.user_id

Schema-Fakten:
  fansub_group_members(app_user_id, fansub_group_id, status)
  fansub_group_member_roles(fansub_group_member_id, role)  -- role-Codes: fansub_lead, project_lead, translator, timer, typesetter, editor, encoder, raw_provider, quality_checker, designer (Migration 0074)
  role_definitions(code PRIMARY KEY, label_de NOT NULL)  -- ggf. ohne leadership-codes -> COALESCE-Fallback nötig
  anime_contributions.note TEXT NULL  -- Detail-Quelle für notes
  fansub_groups(id, name, slug)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 3. UNION-Branch (App-Gruppenrollen) + notes-Projektion in GetPublicMemberContributions</name>
  <files>backend/internal/repository/anime_contributions_public_repository.go</files>
  <read_first>
    - backend/internal/repository/anime_contributions_public_repository.go (Z. 71-88 Struct, Z. 290-377 GetPublicMemberContributions inkl. resolveExpr, timelineQuery, Scan-Loop)
    - backend/internal/repository/member_profile_repository.go (Z. 359-392 GetPublicMemberProfile-CTE: member->app_user-Auflösung via COALESCE(claim_user.app_user_id, legacy_user.id); Z. 916-954 loadMemberships joint fansub_group_members via app_user_id)
    - database/migrations/0074_expand_fansub_group_member_roles.up.sql (role-Code-Liste)
    - database/migrations/0085_role_definitions_seed.up.sql (role_definitions.code/label_de)
    - database/migrations/0086_anime_contributions.up.sql (note-Spalte)
  </read_first>
  <action>
    1. Struct `PublicMemberRoleEntry` (Z. 71-82) um `Notes *string \`json:"notes"\`` erweitern.
    2. In `GetPublicMemberContributions` (Z. 290-377): die beiden bestehenden SELECT-Branches um eine `notes`-Projektion ergänzen:
       - hist_group_member_roles-Branch: `NULL::text AS notes` (hist hat keine note).
       - anime_contributions-Branch: `ac.note AS notes`.
    3. **3. UNION-Branch (App-Gruppenrollen, GAP-3)** anhängen, VOR der `ORDER BY`-Klausel. Member->app_user wie im member_profile_repository-Muster auflösen: app_user_id = COALESCE((SELECT mc.app_user_id FROM member_claims mc WHERE mc.member_id=$1 AND mc.claim_status='verified' ORDER BY mc.verified_at DESC NULLS LAST, mc.id DESC LIMIT 1), (SELECT au.id FROM app_users au JOIN members m ON m.user_id=au.legacy_user_id WHERE m.id=$1)). Diesen Wert in der Query inline berechnen (z. B. via WITH-CTE `resolved_user AS (SELECT ... )` oder Subselects), damit `$1` weiterhin die memberID ist. Branch:
       SELECT fg.name, fg.slug, fgmr.role AS role_code, COALESCE(rd.label_de, fgmr.role) AS role_label, 'group_history'::text AS context, NULL::text AS anime_title, NULL::bigint AS anime_id, NULL::int AS started_year, NULL::int AS ended_year, COALESCE(fgm.status, 'active') AS status, NULL::text AS notes
       FROM fansub_group_members fgm
       JOIN fansub_groups fg ON fg.id = fgm.fansub_group_id
       JOIN fansub_group_member_roles fgmr ON fgmr.fansub_group_member_id = fgm.id
       LEFT JOIN role_definitions rd ON rd.code = fgmr.role
       WHERE fgm.app_user_id = (resolved app_user_id)
    4. **Duplikat-Schutz (GAP-3):** Der Branch darf keine Einträge erzeugen, die bereits über hist-Rollen sichtbar sind. Da hist_group_member_roles aus `hist_fansub_group_members` (member_id) stammt und der neue Branch aus `fansub_group_members` (app_user_id) — unterschiedliche Quelltabellen, unterschiedliche role-Spalten (role_code vs. role). Eine harte Dedup ist nicht zwingend, aber wenn dieselbe (group, role) sowohl als hist- als auch als app-Rolle existiert, NICHT doppelt zeigen: `AND NOT EXISTS (SELECT 1 FROM hist_group_member_roles hr JOIN hist_fansub_group_members h2 ON h2.id=hr.hist_fansub_group_member_id WHERE h2.member_id=$1 AND h2.fansub_group_id=fgm.fansub_group_id AND hr.role_code=fgmr.role AND hr.visibility='public')`.
    5. Scan-Loop (Z. 358-371) um `&e.Notes` erweitern (Reihenfolge an die SELECT-Spalten anpassen — alle drei Branches müssen identische Spaltenliste/-reihenfolge haben). `HasUnverified`-Logik unverändert lassen (group_history-App-Rollen mit status='active' gelten nicht als unverified — `e.Status != "confirmed"` würde sie fälschlich als unverified zählen; daher die bestehende Bedingung ggf. auf `e.Status != "confirmed" && e.Status != "active"` angleichen, konsistent zu MemberRoleTimeline.isUnverifiedEntry).
    Kein neues Modell, keine Migration (alle Tabellen/Spalten existieren). 450-Zeilen-Limit der Datei beachten.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go vet ./internal/repository/</automated>
  </verify>
  <acceptance_criteria>
    - PublicMemberRoleEntry hat Notes *string mit json-Tag "notes".
    - GetPublicMemberContributions enthält einen 3. UNION-Branch über fansub_group_members + fansub_group_member_roles mit context='group_history' und COALESCE(rd.label_de, fgmr.role).
    - member->app_user-Auflösung spiegelt das member_profile_repository-Muster (verified member_claims ODER members.user_id->app_users.legacy_user_id).
    - Alle drei Branches projizieren identische Spalten inkl. notes; Scan-Loop liest &e.Notes.
    - NOT-EXISTS-Dedup verhindert Doppelung gegen hist-Rollen.
    - `go build ./...` grün; Datei <= 450 Zeilen.
  </acceptance_criteria>
  <done>App-Gruppenrollen erscheinen als group_history in der Timeline; notes ist projiziert; Build grün.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Go-Test — group_history-Branch + notes (Testcase Member 3 / Ballelboy)</name>
  <files>backend/internal/repository/anime_contributions_public_member_test.go</files>
  <behavior>
    - Query-Fragment-Test: GetPublicMemberContributions-Quelltext enthält "fansub_group_members", "fansub_group_member_roles", "group_history", "COALESCE(rd.label_de" und "ac.note" (analog zum bestehenden Fragment-Test-Muster in member_claims_memorial_guard_test.go, falls keine DB im CI verfügbar).
    - Falls eine Test-DB/Fixture-Harness existiert (prüfen, ob andere repository-Tests gegen eine echte pgx-Verbindung laufen): Integrationstest, der für member_id=3 (app_user 2, fansub_lead@AnimeOwnage) mindestens einen Eintrag mit context='group_history', role_code='fansub_lead', fansub_group_name='AnimeOwnage' liefert und KEIN Duplikat (genau 1 solcher Eintrag).
  </behavior>
  <read_first>
    - backend/internal/repository/anime_contributions_public_repository.go (zu testende Funktion + Struct)
    - backend/internal/repository/member_claims_memorial_guard_test.go (Fragment-Test-Muster ohne DB)
    - Vorhandene repository-Tests prüfen: gibt es eine echte DB-Test-Harness? (z. B. *_test.go mit pgxpool/testcontainers) — falls ja, Integrationsvariante; falls nein, Fragment-Test.
  </read_first>
  <action>
    1. Bestimme, ob die repository-Test-Suite eine echte DB nutzt. Wenn JA: schreibe einen Integrationstest, der GetPublicMemberContributions(ctx, "ballelboy") aufruft und asserted: genau ein Eintrag mit Context=="group_history" && RoleCode=="fansub_lead" && FansubGroupName=="AnimeOwnage"; keine Duplikate.
    2. Wenn NEIN (kein DB-Harness, nur Unit/Fragment-Stil wie member_claims_memorial_guard_test.go): schreibe einen Quelltext-Fragment-Test, der die Pflicht-Fragmente in anime_contributions_public_repository.go assertiert (siehe behavior). Das sichert die Implementierung ohne DB ab und ist konsistent mit dem bestehenden Stub-Erzwingungs-Muster im Repo.
    3. Deutsche Testkommentare; korrekte Umlaute.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/repository/ -run PublicMemberContributions -v</automated>
  </verify>
  <acceptance_criteria>
    - Test existiert und ist GREEN gegen die Implementierung aus Task 1.
    - Test deckt den group_history-Branch (App-Gruppenrolle) und das Duplikat-Verbot ab (Integration) bzw. die Pflicht-Query-Fragmente (Fragment-Stil).
    - `go test ./internal/repository/ -run PublicMemberContributions` grün.
  </acceptance_criteria>
  <done>Regressionstest für GAP-3-Branch grün; Testcase Ballelboy/Member 3 abgedeckt.</done>
</task>

<task type="auto">
  <name>Task 3: Contract-/Type-Erweiterung notes (Lock K)</name>
  <files>frontend/src/types/contributions.ts, shared/contracts/contributions.yaml</files>
  <read_first>
    - frontend/src/types/contributions.ts (Z. 174-190 PublicMemberRoleEntry + PublicMemberContributionsResponse)
    - shared/contracts/contributions.yaml (Struktur/Konventionen; prüfen ob role_timeline-Schema dort fehlt)
    - backend/internal/repository/anime_contributions_public_repository.go (autoritative Feld-Reihenfolge/json-Tags nach Task 1)
  </read_first>
  <action>
    1. `frontend/src/types/contributions.ts`: `PublicMemberRoleEntry` um `notes: string | null` erweitern (an die Backend-json-Tags angeglichen). Bestehende Felder unverändert.
    2. `shared/contracts/contributions.yaml`: Falls dort ein PublicMemberRoleEntry-/member-contributions-Schema existiert, `notes` (string, nullable) ergänzen. Falls das Schema dort NICHT existiert (role_timeline ist aktuell nirgends formal kontraktiert), ein minimales `PublicMemberRoleEntry`-Schema + `PublicMemberContributionsResponse` (role_timeline, has_unverified) im selben Stil wie die übrigen contributions.yaml-Schemas nachtragen, inkl. notes und context-Enum [group_history, anime_contribution]. Lock K: Backend-Struct (Task 1) bleibt die Quelle der Wahrheit; der Contract spiegelt sie.
    3. `api.ts` bleibt unverändert (Shape erweitert sich additiv, getMemberContributions-Signatur unverändert).
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - PublicMemberRoleEntry (frontend type) hat notes: string | null.
    - contributions.yaml enthält das notes-Feld am member-role-timeline-Schema (ergänzt oder neu angelegt) mit context-Enum.
    - `npm run typecheck` grün.
  </acceptance_criteria>
  <done>Contract + frontend type tragen notes konsistent zum Backend; Lock K erfüllt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → Public-Contributions-Query | Nur öffentlich freigegebene Einträge (visibility='public', is_public_on_member_profile) dürfen in die Timeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-07-INFO | Information Disclosure | 3. UNION-Branch leakt nicht-öffentliche App-Gruppenrollen | mitigate | App-Gruppenrollen sind bereits über die memberships-Sektion öffentlich sichtbar (loadMemberships, public-Profil); kein neuer Sichtbarkeits-Scope. Keine internen Felder (note bleibt für anime_contribution nur bei is_public_on_member_profile=true projiziert) |
| T-74-07-INFO2 | Information Disclosure | anime_contributions.note enthält internen Text | mitigate | note nur im anime_contribution-Branch projiziert, der bereits durch is_public_on_member_profile=true gegated ist; group_history-Branch setzt notes=NULL |
| T-74-07-SC | Tampering | npm/go installs | accept | Keine neuen Pakete |
</threat_model>

<verification>
- `cd backend && go build ./... && go test ./internal/repository/ -run PublicMemberContributions` grün.
- `cd frontend && npm run typecheck` grün.
- Manuell/DB: Ballelboy (member 3) liefert einen group_history-Eintrag (live in Plan 74-11 verifiziert).
</verification>

<success_criteria>
GetPublicMemberContributions liefert App-Gruppenrollen als group_history (GAP-3, sauber getrennt, ohne Duplikate); notes ist end-to-end (Backend-Struct + Contract + frontend type) als Detaildaten-Quelle vorhanden (GAP-2); Go-Test grün.
</success_criteria>

<output>
Create `.planning/phases/74-public-member-profile-members-slug-memorial/74-07-SUMMARY.md` when done
</output>
</content>
</invoke>
