---
phase: 74-public-member-profile-members-slug-memorial
plan: 09
type: execute
wave: 2
depends_on: ["74-07"]
files_modified:
  - frontend/src/components/profile/MemberContributionFilters.tsx
  - frontend/src/components/profile/MemberContributionFilters.test.tsx
  - frontend/src/components/profile/MemberRoleTimeline.tsx
  - frontend/src/components/profile/MemberRoleTimeline.module.css
autonomous: true
gap_closure: true
requirements: [C, K, D-06, D-07, D-08]
must_haves:
  truths:
    - "MemberContributionFilters filtert nach Anime, Gruppe, Rolle, Zeitraum UND Status — alle clientseitig (useMemo, kein fetch) über @/components/ui Select (GAP-1, D-06)"
    - "Auf dem Happy-Path rendert die gefilterte Liste durch MemberRoleTimeline (inkl. EntryDetail-Inline-Expand), NICHT die eigene <li>-Liste (GAP-2, D-07)"
    - "Der Inline-Expand zeigt echte Detaildaten (notes + role_code als Subtype), Hauptrollen-Labels bleiben vereinfacht (D-07)"
    - "Filter-Optionen sind aus der geladenen role_timeline abgeleitet (keine Hardcodes)"
  artifacts:
    - path: "frontend/src/components/profile/MemberContributionFilters.tsx"
      provides: "5 clientseitige Filter (Anime/Gruppe/Rolle/Zeitraum/Status) + Durchreichen an MemberRoleTimeline"
      contains: "MemberRoleTimeline"
    - path: "frontend/src/components/profile/MemberContributionFilters.test.tsx"
      provides: "Test: alle 5 Filter reduzieren clientseitig; gefilterte Liste rendert über MemberRoleTimeline"
      contains: "Zeitraum"
  key_links:
    - from: "MemberContributionFilters.tsx"
      to: "MemberRoleTimeline.tsx"
      via: "props entries={filtered}"
      pattern: "MemberRoleTimeline"
    - from: "EntryDetail (MemberRoleTimeline)"
      to: "PublicMemberRoleEntry.notes / role_code"
      via: "Inline-Expand"
      pattern: "entry.notes"
---

<objective>
GAP-1 + GAP-2 (Frontend): Die Contributions-Sektion ist auf dem Happy-Path doppelt unvollständig:

- **GAP-1 (D-06):** `MemberContributionFilters` bietet nur Anime + Status. Gefordert: **Anime, Gruppe, Rolle, Zeitraum, Status** — alle clientseitig (useMemo, kein fetch), alle über `@/components/ui` Select.
- **GAP-2 (D-07):** Die Komponente rendert eine eigene, ärmere `<li>`-Liste und umgeht `MemberRoleTimeline` + `EntryDetail`; der Inline-Expand erscheint dadurch nur im leeren Empty-Branch. Fix: die gefilterte `PublicMemberRoleEntry[]` an `MemberRoleTimeline` durchreichen. Der Inline-Expand zeigt echte Detaildaten (`notes` aus 74-07 + `role_code` als Subtype), Hauptrollen-Labels bleiben vereinfacht.

Purpose: Schließt die zwei am stärksten live-widerlegten Gaps. Baut auf 74-07 auf (`notes` im DTO/Type). Macht den Happy-Path funktional: Filter + funktionierender Inline-Expand mit echten Daten.
Output: refaktorierte Filter-Komponente (5 Filter + Durchreichen), erweiterter EntryDetail (notes), Vitest, ggf. CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-CONTEXT.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-VERIFICATION.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-07-SUMMARY.md

<interfaces>
<!-- Aus Codebase + 74-07. -->

PublicMemberRoleEntry (frontend/src/types/contributions.ts, nach 74-07):
  fansub_group_name, fansub_group_slug, role_code, role_label
  context: 'group_history' | 'anime_contribution'
  anime_title: string | null; anime_id: number | null
  started_year: number | null; ended_year: number | null; status: string
  notes: string | null   // NEU aus 74-07

MemberRoleTimeline (MemberRoleTimeline.tsx:90):
  props: { entries: PublicMemberRoleEntry[]; hasUnverified: boolean; isVerified?: boolean }
  enthält bereits sortEntries, contextLabel, isUnverifiedEntry, EntryDetail (Inline-Expand, Z. 46-88)
  EntryDetail zeigt aktuell role_code + anime_title; "hasDetail"-Gate Z. 55-59

MemberContributionFilters (aktuell, MemberContributionFilters.tsx):
  props: { roleTimeline: ContributionFilterEntry[] }
  bietet nur animeFilter + statusFilter; rendert eigene <li>-Liste (Z. 108-134) -> ERSETZEN
  @/components/ui Select akzeptiert <option>-children (siehe bestehende Nutzung)

page.tsx (Z. 144-146): roleTimeline.length>0 ? <MemberContributionFilters roleTimeline={roleTimeline}/> : <MemberRoleTimeline entries={[]} .../>
  -> page.tsx bleibt unverändert; nur MemberContributionFilters wird umgebaut.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EntryDetail um notes erweitern (D-07 echte Detaildaten)</name>
  <files>frontend/src/components/profile/MemberRoleTimeline.tsx, frontend/src/components/profile/MemberRoleTimeline.module.css</files>
  <behavior>
    - Ein Eintrag mit notes != null/"" zeigt im aufgeklappten EntryDetail die Notiz an.
    - Ein anime_contribution-Eintrag zeigt weiterhin role_code (als Subtype-Hinweis) + anime_title.
    - Hauptrollen-Label (role_label) bleibt unverändert die primäre Anzeige (Z. 142); Subtypes nur im Expand.
    - "Details anzeigen"-Button erscheint, wenn notes ODER role_code ODER (anime_title bei anime_contribution) vorhanden ist.
  </behavior>
  <read_first>
    - frontend/src/components/profile/MemberRoleTimeline.tsx (Z. 46-88 EntryDetail + hasDetail-Gate; Z. 142 role_label-Anzeige)
    - frontend/src/components/profile/MemberRoleTimeline.module.css (expandDetail/detailSubtype/detailAnime-Klassen)
    - frontend/src/types/contributions.ts (PublicMemberRoleEntry.notes nach 74-07)
  </read_first>
  <action>
    1. `EntryDetail` (Z. 51-88): `hasDetail`-Gate um `Boolean(entry.notes && entry.notes.trim())` erweitern.
    2. Im aufgeklappten Bereich einen Notes-Block ergänzen: `{entry.notes && entry.notes.trim() ? <span className={timelineStyles.detailNotes}>Notiz: {entry.notes}</span> : null}`. Korrekte Umlaute, deutsche Labels.
    3. CSS: `.detailNotes`-Klasse analog zu detailSubtype/detailAnime in MemberRoleTimeline.module.css ergänzen (dezent, layoutstabil).
    4. role_label bleibt die Hauptanzeige (Z. 142) — keine Subtype-Promotion (D-07). role_code bleibt nur im Expand.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run MemberRoleTimeline && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - EntryDetail zeigt notes im Expand, wenn vorhanden; Button erscheint auch bei nur-notes-Detail.
    - role_label bleibt primäre Anzeige; Subtypes (role_code, notes, anime) nur im Expand.
    - .detailNotes-CSS vorhanden; `npx vitest run MemberRoleTimeline` + typecheck grün.
  </acceptance_criteria>
  <done>Inline-Expand zeigt echte Detaildaten (notes + role_code) — D-07 erfüllt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MemberContributionFilters — 5 clientseitige Filter + Durchreichen an MemberRoleTimeline (GAP-1/GAP-2)</name>
  <files>frontend/src/components/profile/MemberContributionFilters.tsx, frontend/src/components/profile/MemberContributionFilters.test.tsx</files>
  <behavior>
    - Anime-Filter: reduziert auf gewählte anime_id.
    - Gruppe-Filter: reduziert auf gewählten fansub_group_slug (Optionen aus role_timeline abgeleitet).
    - Rolle-Filter: reduziert auf gewählten role_code/role_label (Optionen aus role_timeline abgeleitet).
    - Zeitraum-Filter: reduziert auf einen Jahresbereich/Zeitfenster (z. B. abgeleitete Dekaden oder min/max started_year-Buckets; Einträge ohne Jahr werden bei aktivem Zeitraum-Filter ausgeblendet, bei "Alle" sichtbar).
    - Status-Filter: confirmed / unverified / alle (bestehende matchesStatus-Logik, group_history mit status='active' zählt als bestätigt).
    - Alle Filter kombinieren (UND) und sind rein clientseitig (useMemo) — KEIN fetch/API-Call.
    - Die gefilterte Liste wird an MemberRoleTimeline (entries) durchgereicht und mit dessen EntryDetail-Inline-Expand gerendert; KEINE eigene <li>-Liste mehr.
    - Empty-State, wenn der Filter alles ausblendet.
  </behavior>
  <read_first>
    - frontend/src/components/profile/MemberContributionFilters.tsx (komplette aktuelle Datei: ContributionFilterEntry, collectAnimeOptions, matchesStatus, eigene <li>-Liste Z. 108-134 -> ENTFERNEN)
    - frontend/src/components/profile/MemberRoleTimeline.tsx (props-Signatur entries/hasUnverified/isVerified)
    - frontend/src/components/profile/MemberContributionFilters.module.css (filterBar/filterLabel/filterSelect-Klassen wiederverwenden)
    - frontend/src/components/ui (Select — akzeptiert <option>-children; Pflicht, kein natives <select>)
    - frontend/src/types/contributions.ts (PublicMemberRoleEntry inkl. notes)
    - frontend/src/app/members/[slug]/page.tsx (Z. 144-146 Einhängung — unverändert lassen)
  </read_first>
  <action>
    1. Props-Typ vereinheitlichen: `MemberContributionFilters` nimmt `roleTimeline: PublicMemberRoleEntry[]` (statt des Mock-flexiblen ContributionFilterEntry; falls Tests den Mock-Typ brauchen, PublicMemberRoleEntry direkt verwenden und Test-Fixtures anpassen). Optional `isVerified?: boolean` durchreichen, falls page.tsx das liefern soll — page.tsx aktuell nicht; daher hasUnverified aus den gefilterten Einträgen ableiten und isVerified weglassen oder false. (page.tsx NICHT ändern.)
    2. Optionen ableiten (alle aus roleTimeline, useMemo): animeOptions (id->title), groupOptions (slug->name), roleOptions (role_code->role_label, dedupliziert), zeitraumOptions (aus started_year/ended_year abgeleitete Buckets, z. B. Dekaden oder konkrete Jahre; sinnvolle deutsche Labels „Alle Zeiträume", „2010–2019", …).
    3. State: animeFilter, groupFilter, roleFilter, zeitraumFilter, statusFilter (je 'all'-Default).
    4. `filtered` = useMemo über roleTimeline mit UND-Verknüpfung aller fünf Prädikate. matchesStatus beibehalten/erweitern (active==bestätigt). Zeitraum-Prädikat: Eintrag fällt in den gewählten Bereich, wenn started_year (oder ended_year) im Fenster liegt; ohne Jahr -> nur bei 'all' sichtbar.
    5. UI: fünf `@/components/ui` Select in der filterBar (Anime/Gruppe/Rolle/Zeitraum/Status), je mit <option>-children aus den abgeleiteten Optionen. Deutsche Labels, korrekte Umlaute. KEIN natives <select>.
    6. **Eigene <li>-Liste (Z. 108-134) ENTFERNEN.** Stattdessen: `<MemberRoleTimeline entries={filtered} hasUnverified={filtered.some(isUnverified)} />` rendern. Den bestehenden `export { MemberRoleTimeline }` beibehalten oder entfernen (Konsistenz prüfen).
    7. `MemberContributionFilters.test.tsx`: Vitest, der (a) alle fünf Filter clientseitig reduzieren lässt (je ein Filter-Interaktionstest), (b) prüft, dass die gefilterte Liste über MemberRoleTimeline rendert (z. B. „Details anzeigen"-Button vorhanden bei Einträgen mit Detail), (c) keinen fetch auslöst. Bestehende Tests anpassen, falls sie die alte <li>-Liste asserten.
    Constraints: nur @/components/ui, kein fetch (D-06), Datei <= 450 Zeilen (bei Bedarf Options-Helper in separate Datei auslagern — aber sparsam, nicht over-engineeren).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run MemberContributionFilters && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Fünf Filter (Anime/Gruppe/Rolle/Zeitraum/Status) als @/components/ui Select; Optionen aus role_timeline abgeleitet; rein clientseitig (kein fetch).
    - Gefilterte Liste rendert über MemberRoleTimeline (EntryDetail-Inline-Expand erreichbar); keine eigene <li>-Liste mehr.
    - MemberContributionFilters.test.tsx GREEN: jeder der fünf Filter reduziert; Durchreichen an MemberRoleTimeline verifiziert.
    - `npx vitest run MemberContributionFilters` + typecheck + lint grün; keine nativen Form-Elemente.
  </acceptance_criteria>
  <done>Happy-Path-Contributions vollständig filterbar (5 Filter) mit funktionierendem Inline-Expand über MemberRoleTimeline — GAP-1 + GAP-2 geschlossen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Filter-UI | Rein clientseitig auf bereits geladenen, öffentlichen Daten; kein neuer Datenpfad |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-09-INFO | Information Disclosure | Filter zeigt mehr als die Server-Projektion freigibt | mitigate | Filter arbeiten ausschließlich auf der bereits server-gegateten role_timeline (74-07); keine zusätzlichen Felder, kein eigener fetch |
| T-74-09-UI | Tampering | Natives <select> umgeht Design-System | mitigate | Ausschließlich @/components/ui Select; ESLint no-restricted-syntax greift |
| T-74-09-SC | Tampering | npm installs | accept | Keine neuen Pakete |
</threat_model>

<verification>
- `cd frontend && npx vitest run MemberContributionFilters MemberRoleTimeline && npm run typecheck && npm run lint` grün.
- Live (Plan 74-11): /members/ballelboy + /members/angeldust zeigen 5 Filter; Inline-Expand mit echten Daten funktioniert.
</verification>

<success_criteria>
GAP-1 + GAP-2 geschlossen: fünf clientseitige Filter über @/components/ui; gefilterte Liste rendert durch MemberRoleTimeline mit funktionierendem Inline-Expand (notes + role_code) auf dem Happy-Path.
</success_criteria>

<output>
Create `.planning/phases/74-public-member-profile-members-slug-memorial/74-09-SUMMARY.md` when done
</output>
</content>
