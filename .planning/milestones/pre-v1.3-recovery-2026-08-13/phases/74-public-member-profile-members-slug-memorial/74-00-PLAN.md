---
phase: 74-public-member-profile-members-slug-memorial
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/handlers/member_memorial_handler_test.go
  - backend/internal/repository/member_claims_memorial_guard_test.go
  - backend/internal/repository/badge_public_source_test.go
  - frontend/src/components/profile/MemberContributionFilters.test.tsx
  - frontend/src/components/profile/MemberProfileHero.test.tsx
  - frontend/src/components/profile/MemberStatusPill.test.tsx
  - frontend/src/components/profile/deriveKnownFor.test.ts
  - .planning/phases/74-public-member-profile-members-slug-memorial/74-MIGRATION-COLLISION-NOTE.md
autonomous: true
requirements: [C, J, "Badges-13", K, D-03, D-06, D-09, D-10, D-14, D-15, D-17]
must_haves:
  truths:
    - "Wave-0 Test-Stubs existieren und sind RED (failing), bevor Produktionscode entsteht (Nyquist Dimension 8)"
    - "Migrations-Nummern-Kollision 0096 (Phase 72 vs. hist_group_members_confirmation_audit) ist dokumentiert und vor Phase-74-Migrationen geklärt (D-15/K)"
    - "Der Claim-Block gegen ein Memorial-Profil schreibt in BEIDEN Pfaden (SubmitClaim + AcceptInvitation) ein denied-Audit-Ereignis — durch einen Wave-0-Stub vor Implementierung verankert (D-15)"
    - "Der read-only abgeleitete „Bekannt für“-Block (D-03) hat eine mechanisch testbare Ableitungsfunktion mit Wave-0-Stub (D-03)"
    - "Jeder nachgelagerte Plan kann sein automated-verify auf einen hier erzeugten Stub abstützen"
  artifacts:
    - path: "backend/internal/handlers/member_memorial_handler_test.go"
      provides: "RED-Stub: Nicht-Global-Admin → 403; Memorial-Set schreibt audit_logs (J/D-14, D-15)"
      contains: "func TestMemorial"
    - path: "backend/internal/repository/member_claims_memorial_guard_test.go"
      provides: "RED-Stub: SubmitClaim + AcceptInvitation gegen memorial → 409 UND denied-Audit-Action member_claim.memorial_blocked in BEIDEN Pfaden (J/D-17, D-15)"
      contains: "func TestClaim"
    - path: "backend/internal/repository/badge_public_source_test.go"
      provides: "RED no-DB Source-Fragment-Test: GetPublicMemberBadges Guard visibility='public' AND status='active' (Badges-13)"
      contains: "func TestPublicBadges"
    - path: "frontend/src/components/profile/MemberContributionFilters.test.tsx"
      provides: "RED-Stub: Client-Filter reduziert role_timeline ohne API-Call (C/D-06)"
    - path: "frontend/src/components/profile/MemberProfileHero.test.tsx"
      provides: "RED-Stub: Memorial-Variante rendert Gedenk-Sprache + unterdrückt Mengen-Badges (C/D-10)"
    - path: "frontend/src/components/profile/MemberStatusPill.test.tsx"
      provides: "RED-Stub: Pill rendert alle 5 Status + Tooltip (C/D-09)"
    - path: "frontend/src/components/profile/deriveKnownFor.test.ts"
      provides: "RED-Stub: mechanische „Bekannt für“-Ableitung (aktive Jahre/häufigste Rollen/bekannte Gruppen) aus role_timeline, read-only (D-03)"
  key_links:
    - from: "badge_public_source_test.go"
      to: "backend/internal/repository/badge_repository.go"
      via: "readRepositorySource Source-Fragment-Assertion"
      pattern: "visibility='public'"
    - from: "member_claims_memorial_guard_test.go"
      to: "backend/internal/repository/member_claims_repository.go + member_claim_invitations_repository.go"
      via: "readRepositorySource Source-Fragment-Assertion auf denied-Audit-Action"
      pattern: "member_claim.memorial_blocked"
---

<objective>
Wave 0: Erzeuge alle sieben in `74-VALIDATION.md` geforderten Test-Stubs (RED) und kläre die in `74-RESEARCH.md` §Migrations-Befund dokumentierte Migrations-Nummern-Kollision 0096, bevor irgendein Produktionscode in Phase 74 entsteht.

Purpose: Nyquist Dimension 8 verlangt, dass jeder nachgelagerte Task ein automated-verify hat. Diese Stubs sind die Verankerung. Insbesondere muss das denied-Audit für den Claim-Block (D-15) bereits in Wave 0 durch einen Stub erzwungen sein, damit der Executor es nicht weglassen kann; und die D-03-Ableitung („Bekannt für") braucht eine mechanisch testbare Funktion. Die Migrations-Kollision ist ein BLOCKER, der vor jeder additiven Migration (Plan 03) geklärt sein muss.
Output: 7 RED-Test-Dateien + eine Migrations-Kollisions-Notiz mit dem realen Stand und der Umnummerierungs-Anweisung für Phase 72.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-VALIDATION.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-RESEARCH.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend Wave-0 RED-Stubs (Memorial-Handler, Claim-Guard inkl. denied-Audit, Public-Badge-Quelle)</name>
  <read_first>
    - backend/internal/repository/audit_logs.go (AuditLogEntry-Felder Z. 11-23: ActorAppUserID, EventType, TargetType, TargetID, Action, Outcome, Payload — bestätigt für D-15)
    - backend/internal/handlers/platform_admin_authz.go (requirePlatformAdminIdentity Z. 15-73 — Gate-Signatur für 403-Test)
    - backend/internal/handlers/contribution_proposals_me_handler.go (Z. 25-53 Interface-Stub-Muster; Z. 229-361 Write-Kette)
    - backend/internal/repository/member_claims_repository.go (SubmitClaim Z. 107-134; ClaimMutationError Z. 90)
    - backend/internal/repository/member_claim_invitations_repository.go (AcceptInvitation Z. 104 — zweiter Claim-Pfad)
    - backend/internal/repository/badge_repository.go (GetMemberBadges Z. 114 — Quelle für no-DB Source-Fragment-Pattern)
    - .planning/phases/72-dom-nen-projektionen-status-fundament/72-01-PLAN.md (Phase-72 no-DB readRepositorySource-Muster Z. 33)
    - 74-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements)
  </read_first>
  <action>
    Erzeuge drei RED Go-Test-Dateien analog dem Phase-72 no-DB Source-Fragment-Muster und dem Interface-Stub-Muster aus contribution_proposals_me_handler.go:
    1. `member_memorial_handler_test.go` mit `TestMemorialSetterRejectsNonPlatformAdmin` (erwartet HTTP 403 wenn der Akteur kein Global Admin ist) und `TestMemorialSetterWritesAuditLog` (erwartet einen AuditLogEntry mit EventType "member_profile.memorial_set", TargetType "member", Outcome "allowed"). Nutze einen Stub-roleChecker (AppUserHasGlobalRole liefert false → 403; true → 200) und einen Audit-Stub, der geschriebene Entries sammelt. Der Test referenziert die noch nicht existierende `NewMemberMemorialHandler`-Konstruktor-Signatur — das macht ihn legitim RED.
    2. `member_claims_memorial_guard_test.go` mit DREI Assertionen, BEIDE Claim-Pfade abdeckend (Fallstrick 3):
       a. `TestClaimSubmitBlockedForMemorialProfile` und `TestClaimAcceptInvitationBlockedForMemorialProfile`: no-DB Source-Fragment-Assertion via `readRepositorySource("member_claims_repository.go")` und `("member_claim_invitations_repository.go")`, die in BEIDEN Dateien das Vorhandensein der Guard-Abfrage `profile_status`, des Codes `memorial_not_claimable` UND des HTTP-Status `409` prüft.
       b. `TestClaimBlockWritesDeniedAudit`: NEU (deckt D-15 für den Claim-Block). Source-Fragment-Assertion, die in BEIDEN Dateien (member_claims_repository.go + member_claim_invitations_repository.go) den denied-Audit-Action-Key `member_claim.memorial_blocked` UND den Outcome-String `denied` nachweist. Damit ist erzwungen, dass der Executor in Plan 02 Task 2 das denied-Audit in beiden Pfaden schreibt — er kann es nicht weglassen, ohne dass dieser Test RED bleibt. Aktuell RED, weil die Guards + das denied-Audit noch fehlen.
       Festzurrung der Audit-Architektur für diesen Stub: Der Audit-Action-Key MUSS im Repo-Quelltext (member_claims_repository.go / member_claim_invitations_repository.go) als String-Literal erscheinen — entweder weil das denied-Audit dort direkt geschrieben wird (AuditLogRepository via DI ins Repo injiziert) ODER weil das Repo das Block-Ereignis mit genau diesem Action-Key konstruiert/durchreicht. Diese Source-Fragment-Erwartung legt fest, dass Plan 02 Task 2 die offen gelassene Architekturentscheidung (Audit im Repo vs. Handler) so auflöst, dass `member_claim.memorial_blocked` im Repo-Quelltext steht. Der Stub-Test ist die autoritative Spezifikation dieses Schemas.
    3. `badge_public_source_test.go` mit `TestPublicBadgesSourceFiltersVisibility` als no-DB Source-Fragment-Test: liest badge_repository.go-Quelltext und assertiert, dass eine Methode `GetPublicMemberBadges` existiert und ihr SQL die Fragmente `visibility='public'` UND `status='active'` enthält (Badges-13, kein UI-Recompute). Aktuell RED, weil die Methode noch fehlt.
    Verwende `t.Skip` NICHT. Fehlende Symbole/Compile-Fehler sind das erwartete RED-Signal; falls Compile-Fehler den Package-Build anderer Tests blockiert, kapsele die noch-nicht-existierenden Symbol-Referenzen über die Source-Fragment-Lesung (string-basiert), sodass die Datei kompiliert und der Test inhaltlich RED ist.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/handlers/... -run Memorial; go test ./internal/repository/... -run "Claim|PublicBadges"</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/handlers/... -run Memorial` läuft (kompiliert) und ist RED (Assertions/Fehlen-Symbole nicht erfüllt).
    - `go test ./internal/repository/... -run "Claim|PublicBadges"` ist RED.
    - badge_public_source_test.go assertiert exakt die Strings `visibility='public'` und `status='active'`.
    - claims-Guard-Test prüft BEIDE Dateien (member_claims_repository.go + member_claim_invitations_repository.go) auf `memorial_not_claimable` UND `409`.
    - `TestClaimBlockWritesDeniedAudit` assertiert in BEIDEN Dateien den Action-Key `member_claim.memorial_blocked` UND den Outcome `denied` (D-15).
  </acceptance_criteria>
  <done>Drei Go-Test-Dateien existieren, kompilieren und sind nachweislich RED; sie decken J/D-14, D-15 (Memorial-Set UND Claim-Block-denied), J/D-17, Badges-13 ab.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend Wave-0 RED-Stubs (Filters, Memorial-Hero, Status-Pill, Bekannt-für-Ableitung) + Migrations-Kollisions-Notiz</name>
  <read_first>
    - frontend/vitest.config.ts (Test-Config + @-Alias)
    - frontend/src/components/profile/MemberBadgeChips.tsx (heutiges Client-Component-Test-bares Muster; Z. 28-31 visibility-Filter)
    - frontend/src/components/contributions/ProposalForm.test.tsx (bestehendes Vitest-Komponenten-Test-Muster im Repo)
    - 74-VALIDATION.md (Wave 0 Requirements + Per-Task Verification Map)
    - 74-CONTEXT.md §D-03 (Z. 59-61 — „Bekannt für" read-only abgeleitet aus häufigsten/bestätigten Rollen, aktiven Jahren, bekannten Gruppen; KEIN Schreib-Flow)
    - 74-RESEARCH.md §Migrations-Befund + §Fallstrick 1 (Kollision 0096, Phase 72 ≥0097 umnummerieren)
    - database/migrations/ (realer Stand: höchste belegte Nummer 0096_hist_group_members_confirmation_audit)
  </read_first>
  <action>
    Erzeuge vier RED Vitest-Dateien (referenzieren noch nicht existierende Komponenten/Funktionen → legitim RED):
    1. `MemberContributionFilters.test.tsx`: rendert die (noch fehlende) `MemberContributionFilters`-Komponente mit einer Mock-`role_timeline` und assertiert, dass eine Status-Filter-Auswahl die sichtbaren Einträge clientseitig reduziert OHNE einen fetch/API-Call (vi.spyOn auf global.fetch → not called). Deckt C/D-06.
    2. `MemberProfileHero.test.tsx`: rendert Hero mit `profile_status='memorial'` und assertiert, dass der Text "Dieses Profil wird als historisches Gedenkprofil geführt." erscheint und KEINE Mengen-/Gamification-Badge-Anzeige (z. B. kein Element mit Aktivitätsmetrik) gerendert wird. Deckt C/D-10.
    3. `MemberStatusPill.test.tsx`: parametrisierter Test über alle 5 Status (active, historical, unclaimed, claimed, memorial); assertiert sichtbares Label + tooltip/title-Attribut pro Status. Deckt C/D-09.
    4. `deriveKnownFor.test.ts` (NEU, deckt D-03 mechanisch): testet eine reine, noch nicht existierende Ableitungsfunktion `deriveKnownFor(input)` (z. B. in `frontend/src/components/profile/deriveKnownFor.ts`), die aus einer Mock-`role_timeline` mechanisch ableitet: aktive Jahre (min/max Jahr der Einträge), häufigste Rollen (Top-N nach Häufigkeit/bestätigten Einträgen), bekannte Gruppen (distinct Gruppen). Assertiere für eine fixe Eingabe das erwartete deterministische Ergebnis (z. B. aktive Jahre "2019–2023", Top-Rolle "Übersetzer"). KEIN fetch, KEIN Schreib-Flow, KEIN neues DB-Feld — rein abgeleitet aus bereits geladenen Daten (D-03 read-only). Aktuell RED, weil die Funktion fehlt.
    Alle Strings mit korrekten deutschen Umlauten (CLAUDE.md §Sprachqualität). Keine watch-mode Flags.
    Erzeuge zusätzlich `74-MIGRATION-COLLISION-NOTE.md` im Phase-Verzeichnis, das festhält: realer höchster Migrations-Stand = 0096_hist_group_members_confirmation_audit; Phase-72-Plan-01 belegt kollidierend 0096_v12_status_foundation; Anweisung: Phase 72 vor Execute auf ≥0097 umnummerieren; Phase 74 eigene Migration (Plan 03, Korrektur-Tabelle) nimmt die nächste freie Nummer NACH der Phase-72-Umnummerierung (frühestens 0098). Dieser Befund MUSS vor `go run ./cmd/migrate up` in Plan 03 geklärt sein.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run MemberContributionFilters MemberProfileHero MemberStatusPill deriveKnownFor</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run MemberContributionFilters MemberProfileHero MemberStatusPill deriveKnownFor` ist RED (Komponenten/Funktion fehlen).
    - MemberContributionFilters.test.tsx assertiert explizit, dass fetch NICHT aufgerufen wird.
    - MemberProfileHero.test.tsx assertiert den exakten String "Dieses Profil wird als historisches Gedenkprofil geführt.".
    - deriveKnownFor.test.ts assertiert deterministisches Ableitungs-Ergebnis (aktive Jahre + Top-Rolle) ohne fetch (D-03).
    - 74-MIGRATION-COLLISION-NOTE.md nennt 0096_hist_group_members_confirmation_audit als realen Stand und ≥0097 für Phase 72.
  </acceptance_criteria>
  <done>Vier Vitest-Stubs existieren und sind RED; die Migrations-Kollisions-Notiz dokumentiert den BLOCKER mit konkreter Umnummerierungs-Anweisung.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (keine neuen) | Wave 0 erzeugt nur Test-Code + Doku; keine neuen Laufzeit-Pfade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-00-MIG | Tampering | DB-Migrations-Reihenfolge | mitigate | Kollisions-Notiz erzwingt Phase-72-Umnummerierung vor jeder Phase-74-Migration (Plan 03) |
| T-74-00-REP | Repudiation | Claim-Block ohne Nachweis | mitigate | Wave-0-Stub erzwingt denied-Audit-Action `member_claim.memorial_blocked` in beiden Claim-Pfaden (D-15) |
| T-74-00-SC | Tampering | npm/go installs | accept | Keine neuen Pakete (RESEARCH §Package Legitimacy Audit: nicht anwendbar) |
</threat_model>

<verification>
- Alle 6 Backend/Frontend-Stubs RED: `cd backend && go test ./internal/handlers/... -run Memorial && go test ./internal/repository/... -run "Claim|PublicBadges"` + `cd frontend && npx vitest run MemberContributionFilters MemberProfileHero MemberStatusPill deriveKnownFor`.
- Claim-Guard-Stub deckt denied-Audit in beiden Pfaden ab.
- Migrations-Notiz existiert.
</verification>

<success_criteria>
7 Test-Stubs existieren und sind RED (inkl. denied-Audit-Claim-Block + D-03-Ableitung); Migrations-Kollision dokumentiert; nachgelagerte Pläne können ihre verify-Kommandos abstützen.
</success_criteria>

<output>
Create `.planning/phases/74-public-member-profile-members-slug-memorial/74-00-SUMMARY.md` when done
</output>
</content>
</invoke>
