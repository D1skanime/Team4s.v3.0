---
phase: 74-public-member-profile-members-slug-memorial
plan: 04
type: execute
wave: 3
depends_on: ["74-00", "74-01"]
files_modified:
  - frontend/src/app/members/[slug]/page.tsx
  - frontend/src/app/members/[slug]/page.module.css
  - frontend/src/components/profile/MemberSectionNav.tsx
  - frontend/src/components/profile/MemberSectionNav.module.css
  - frontend/src/components/profile/MemberStatusPill.tsx
  - frontend/src/components/profile/MemberProfileHero.tsx
  - frontend/src/components/profile/MemberProfileMemorialHero.tsx
  - frontend/src/components/profile/MemberGroupsHistorySection.tsx
  - frontend/src/components/profile/deriveKnownFor.ts
autonomous: true
requirements: [C, "Badges-13", D-01, D-02, D-03, D-04, D-05, D-09, D-10]
must_haves:
  truths:
    - "/members/[slug] ist eine kuratierte einspaltige Scroll-Seite mit Sticky-Anker-Nav (Desktop) / Chip-Leiste (Mobil) (D-01)"
    - "Sektions-Reihenfolge: Hero → Badges → Gruppen & Geschichte → Beiträge (D-02)"
    - "„Bekannt für“/Highlights werden read-only mechanisch aus vorhandenen Daten abgeleitet (häufigste/bestätigte Rollen, aktive Jahre, bekannte Gruppen) — KEIN neues DB-Feld, kein Schreib-Flow (D-03)"
    - "Status-Pill neben dem Nickname mit Tooltip für alle 5 Status (D-09)"
    - "Memorial = würdevolle Sonder-Hero mit Gedenk-Sprache; normale Aktivitätsmetriken unterdrückt; Contributions/Geschichte bleiben sichtbar (D-10)"
    - "Story rendert ausschließlich über RichTextRenderer (server-sanitiert); Empty-States als Platzhalter (D-04/D-05)"
    - "getMyBadges als Public-Badge-Quelle entfernt; Badges kommen aus response.data.public_badges (Fallstrick 2)"
  artifacts:
    - path: "frontend/src/components/profile/MemberSectionNav.tsx"
      provides: "Client Sticky-Nav + IntersectionObserver über 4 Sektionen, @/components/ui Button"
      contains: "IntersectionObserver"
    - path: "frontend/src/components/profile/MemberStatusPill.tsx"
      provides: "Pill + Tooltip für active/historical/unclaimed/claimed/memorial (D-09)"
      contains: "memorial"
    - path: "frontend/src/components/profile/deriveKnownFor.ts"
      provides: "Reine read-only Ableitung „Bekannt für“ (aktive Jahre/häufigste Rollen/bekannte Gruppen) aus role_timeline; kein Schreib-Flow (D-03)"
      contains: "deriveKnownFor"
    - path: "frontend/src/components/profile/MemberProfileMemorialHero.tsx"
      provides: "Würdevolle Memorial-Hero-Variante mit Gedenk-Sprache (D-10)"
      contains: "Gedenkprofil"
    - path: "frontend/src/app/members/[slug]/page.tsx"
      provides: "Server-Component Root-Orchestrator (≤150 Z.) mit Sektions-Reihenfolge D-02"
      min_lines: 60
  key_links:
    - from: "frontend/src/app/members/[slug]/page.tsx"
      to: "response.data.public_badges"
      via: "Public-Badge-Quelle statt getMyBadges (aus Plan 01 DTO)"
      pattern: "public_badges"
    - from: "MemberProfileHero.tsx"
      to: "MemberProfileMemorialHero.tsx"
      via: "if profile_status==='memorial' → Memorial-Variante"
      pattern: "memorial"
    - from: "MemberProfileHero.tsx"
      to: "deriveKnownFor.ts"
      via: "read-only „Bekannt für“-Ableitung aus role_timeline (D-03)"
      pattern: "deriveKnownFor"
---

<objective>
Baue die bestehende `/members/[slug]/page.tsx` (193 Z., Server Component) zu einer kuratierten einspaltigen Scroll-Seite mit Sticky-Anker-Nav um — exakt das Phase-73-Paradigma. Liefere Status-Pill (D-09), die read-only „Bekannt für"-Ableitung (D-03) und die würdevolle Memorial-Hero-Variante (D-10). Story bleibt über `RichTextRenderer` (D-04); Empty-States als Platzhalter (D-05).

Purpose: Erfüllt Erfolgskriterien für das dreistufige Public-Profil-Layout (Lock C, D-01/D-02/D-03). Konsumiert das in Plan 01 erweiterte DTO (profile_status + public_badges) und ersetzt die fehlerhafte Viewer-Badge-Quelle.
Output: Refactorierte Root-Seite + 4 neue/erweiterte Sektions-Komponenten + reine Ableitungsfunktion `deriveKnownFor`. Badge-Highlights + Contribution-Filter folgen in Plan 05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-RESEARCH.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-PATTERNS.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-VALIDATION.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-PATTERNS.md

<interfaces>
<!-- Aus 74-PATTERNS.md (verifiziert) + Plan-01-DTO -->

PublicMemberProfileData (nach Plan 01): + profile_status: 'active'|'historical'|'memorial'; + public_badges: { id; badge_code; badge_category }[]
Hinweis: claimed/unclaimed sind DERIVED (member_claims.claim_status='verified'), nicht in profile_status — die Status-Pill muss diese ableiten (D-09 listet 5 Status; nur 3 stehen in profile_status).

deriveKnownFor (Plan-00-Stub deriveKnownFor.test.ts spezifiziert die erwartete Signatur/Ausgabe): reine Funktion über role_timeline → { activeYears: string; topRoles: string[]; knownGroups: string[] } o. ä.; deterministisch, kein fetch, read-only (D-03).

Sticky-Nav-Muster (73-PATTERNS Z. 209-288, FansubSectionNav existiert noch nicht — vollständige Impl dort dokumentiert):
  'use client'; SECTION_IDS = ['identitaet','badges','geschichte','beitraege'];
  IntersectionObserver rootMargin '-20% 0px -70% 0px'; @/components/ui Button variant subtle/ghost; scrollIntoView smooth.
  CSS: position:sticky; top:var(--header-height,60px); overflow-x:auto (Desktop flex-row, Mobil Chip-Leiste).

page.tsx heute (Z. 79-147): cookies()-Token, Promise.allSettled-Fetch, Envelope 'data' in response; Story via RichTextRenderer Z. 173; getMyBadges-Block Z. 136-147 ENTFERNEN.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MemberStatusPill + deriveKnownFor (D-03) + Memorial-Hero-Variante (Hero-Erweiterung + Split)</name>
  <behavior>
    - MemberStatusPill rendert für alle 5 Status (active/historical/unclaimed/claimed/memorial) sichtbares Label + title/tooltip (Plan-00 MemberStatusPill.test.tsx grün).
    - deriveKnownFor leitet aus einer fixen role_timeline deterministisch aktive Jahre + häufigste Rollen + bekannte Gruppen ab, ohne fetch, read-only (Plan-00 deriveKnownFor.test.ts grün).
    - MemberProfileHero mit profile_status='memorial' rendert "Dieses Profil wird als historisches Gedenkprofil geführt." und unterdrückt Aktivitätsmetrik/Mengen-Badges (Plan-00 MemberProfileHero.test.tsx grün).
  </behavior>
  <read_first>
    - .planning/phases/72-dom-nen-projektionen-status-fundament/72-*-SUMMARY.md (Pre-flight: bestätigt, dass `members.profile_status` von Phase 72 live ist — die Status-Pill + Memorial-Hero hängen am gelieferten DTO-Feld)
    - frontend/src/components/profile/MemberProfileHero.tsx (heutiger Hero — Avatar/Name/Bio/Aktivität/VerifiedBadge)
    - frontend/src/components/contributions/VerifiedBadge.tsx (Badge-Anzeige-Analog für die Pill)
    - frontend/src/components/profile/MemberProfileHero.test.tsx + MemberStatusPill.test.tsx + deriveKnownFor.test.ts (Plan-00-Stubs — Ziel-Assertions)
    - frontend/src/components/ui/ (Badge, Card — Primitives-Pflicht)
    - 74-CONTEXT.md §D-03 (Z. 59-61 — read-only Ableitung, kein Schreib-Flow)
    - 74-RESEARCH.md §Muster 3 (Memorial-Hero) + §Fallstrick 5 (450-Split) + §Fallstrick 7 (Umlaute)
    - 74-PATTERNS.md §MemberProfileHero / MemberStatusPill
  </read_first>
  <action>
    0. PRE-FLIGHT-GATE (harte Abhängigkeit, analog Stop-Gate Plan 03 Task 1): Verifiziere ZUERST via `72-*-SUMMARY.md` (Status executed/shipped) und — falls erreichbar — `\d members`, dass `members.profile_status` live ausgeliefert wird (das DTO-Feld speist Status-Pill + Memorial-Hero). Falls NICHT live (Phase 72 nicht ausgeführt / Migrations-Kollision 0096 ungeklärt — siehe 74-MIGRATION-COLLISION-NOTE.md), STOPPE und melde den BLOCKER im SUMMARY.
    1. `deriveKnownFor.ts` (NEU, read-only D-03): reine Funktion `deriveKnownFor(roleTimeline)` → leitet mechanisch ab: aktive Jahre (min/max Jahr), häufigste/bestätigte Rollen (Top-N nach Häufigkeit), bekannte Gruppen (distinct). KEIN fetch, KEIN neues DB-Feld, KEIN Schreib-Flow (analog Phase 73 D-05). Erfüllt exakt die Plan-00 deriveKnownFor.test.ts-Erwartung.
    2. `MemberStatusPill.tsx` (NEU): nimmt `status: 'active'|'historical'|'unclaimed'|'claimed'|'memorial'`; rendert ein `@/components/ui` Badge mit deutschem Label + erklärendem title/Tooltip pro Status (D-09). Korrekte Umlaute. (active=„Aktiv", historical=„Historisch", unclaimed=„Nicht beansprucht", claimed=„Beansprucht", memorial=„Gedenkprofil".)
    3. `MemberProfileHero.tsx` (ERWEITERN): Status-Pill neben dem Nickname einbinden; „Bekannt für"-Block über `deriveKnownFor` (D-03 — read-only abgeleitet aus profile_status/memberships/role_timeline; KEIN neues Feld, kein Schreib-Flow); bei `profile_status==='memorial'` an `MemberProfileMemorialHero` delegieren.
    4. `MemberProfileMemorialHero.tsx` (NEU Split, wegen 450-Limit): würdevolle Sonder-Hero — Gedenk-Sprache „Dieses Profil wird als historisches Gedenkprofil geführt." statt Aktivitätsmetrik; KEINE Mengen-/Gamification-Anzeige; ruhige Darstellung. Contributions/Geschichte werden NICHT hier unterdrückt (bleiben in den Folge-Sektionen sichtbar, D-10).
    Beide Hero-Dateien ≤450 Zeilen. Nur `@/components/ui`-Primitives; keine nativen HTML-Form-/Button-Elemente.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run MemberStatusPill MemberProfileHero deriveKnownFor && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Pre-flight-Gate ausgeführt: `members.profile_status` live bestätigt (oder BLOCKER gemeldet).
    - Plan-00 MemberStatusPill.test.tsx + MemberProfileHero.test.tsx + deriveKnownFor.test.ts GREEN.
    - deriveKnownFor ist rein (kein fetch, kein Schreib-Flow); „Bekannt für"-Block im Hero speist sich daraus (D-03).
    - Memorial-Hero enthält exakt "Dieses Profil wird als historisches Gedenkprofil geführt.".
    - Status-Pill deckt alle 5 Status mit deutschen Labels + Tooltip.
    - Keine Datei > 450 Zeilen; keine nativen `<button>/<select>/<input>`.
  </acceptance_criteria>
  <done>Status-Pill + read-only „Bekannt für"-Ableitung + Memorial-Hero-Variante existieren und sind getestet; Hero-Erweiterung respektiert 450-Limit + UI-Primitives; Pre-flight-Gate passiert.</done>
</task>

<task type="auto">
  <name>Task 2: Root-Seite zu Sektions-Scroll-Seite + Sticky-Nav + Gruppen/Geschichte-Rahmen</name>
  <read_first>
    - frontend/src/app/members/[slug]/page.tsx (heute Z. 72-192 — cookies/allSettled/Envelope/Story/Owner-Affordances behalten; Grid Z. 170 + getMyBadges Z. 136-147 ersetzen)
    - .planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-PATTERNS.md (FansubSectionNav vollständige Impl Z. 209-288)
    - frontend/src/components/profile/MembershipsSection.tsx (für Gruppen-Rahmen)
    - frontend/src/components/editor/RichTextRenderer.tsx (Story-Rendering D-04)
    - frontend/src/components/ui/ (Button für Nav, EmptyState/Card für Platzhalter)
    - 74-RESEARCH.md §Muster 1/2 + §Fallstrick 2 (Public-Badge-Quelle) + §Fallstrick 8 (RichTextRenderer)
    - 74-PATTERNS.md §page.tsx + §MemberSectionNav + §MemberGroupsHistorySection
    - .planning/phases/74-public-member-profile-members-slug-memorial/74-CONTEXT.md §Reviewed Todos (2026-06-03-member-profil-ui-und-params-bug — beim Umbau mit aufgreifen)
  </read_first>
  <action>
    1. `MemberSectionNav.tsx` + `.module.css` (NEU): 1:1 Adaption des FansubSectionNav-Musters (73-PATTERNS Z. 209-288) mit SECTION_IDS ['identitaet','badges','geschichte','beitraege'] und deutschen Labels (Identität/Badges/Geschichte/Beiträge). 'use client', IntersectionObserver rootMargin '-20% 0px -70% 0px', `@/components/ui` Button variant subtle/ghost, scrollIntoView smooth. CSS: position:sticky; top:var(--header-height,60px); Desktop flex-row, Mobil overflow-x Chip-Leiste.
    2. `MemberGroupsHistorySection.tsx` (NEU): Server-Rahmen für Ebene 2 — bettet `MembershipsSection` (App-/historisch getrennt) + die Story über `RichTextRenderer` ein. Empty-State als Platzhalter (Card/EmptyState mit dezentem Hinweis), damit der Sektions-Anker stabil bleibt (D-05).
    3. `page.tsx` (REFACTOR, ≤150 Z., bleibt Server Component): cookies()-Token + Promise.allSettled-Fetch behalten; `getMyBadges`-Block (Z. 136-147) als Public-Badge-Quelle ENTFERNEN — Badges aus `response.data.public_badges`; Grid-Layout durch einspaltige Scroll-Sektionen mit stabilen `id`s in Reihenfolge D-02 ersetzen: `<MemberSectionNav/>`, dann Sektionen #identitaet (Hero), #badges (Platzhalter für Plan-05 MemberBadgeHighlights), #geschichte (MemberGroupsHistorySection), #beitraege (Platzhalter für Plan-05 Filter/Timeline). Owner-Affordances (OwnProfileEditLink/OwnHiddenProfilePreview) erhalten. Empty-States als Platzhalter (D-05). Beim Umbau den im Reviewed-Todo `2026-06-03-member-profil-ui-und-params-bug.md` beschriebenen Params-/UI-Bug mit aufgreifen, falls er die Sektions-Reihenfolge/`params`-Auflösung betrifft.
    `page.module.css`: Lesebreite + Sektions-Abstände analog fansubs.
    Nur `@/components/ui`-Primitives; korrekte Umlaute; kein 'use client' auf der Root; kein ad-hoc-Fetch (Lock K).
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck && npx vitest run MemberProfileHero MemberStatusPill</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx ≤150 Zeilen, bleibt Server Component (kein 'use client'), Sektions-Reihenfolge D-02 mit stabilen id-Ankern.
    - getMyBadges-Block entfernt; Badges aus response.data.public_badges.
    - MemberSectionNav nutzt IntersectionObserver + @/components/ui Button; sticky.
    - Story rendert via RichTextRenderer; Empty-States als sichtbare Platzhalter (D-05).
    - `npm run typecheck` grün.
  </acceptance_criteria>
  <done>Root-Seite ist Sektions-Scroll-Seite mit Sticky-Nav (D-01/D-02); Gruppen/Geschichte-Rahmen + Story vorhanden; Public-Badge-Quelle verdrahtet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /members/[slug] (SSR) | Öffentlicher Read; Token nur server-seitig via cookies() für Owner-Preview |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-04-XSS | Tampering | Story-HTML-Rendering | mitigate | Ausschließlich RichTextRenderer (server-sanitiert, D-04); kein dangerouslySetInnerHTML (Fallstrick 8) |
| T-74-04-INFO | Information Disclosure | Token-Handling im UI | mitigate | Token nur server-seitig via cookies(); kein Browser-Token-Zugriff (Lock A/K) |
| T-74-04-TAMP | Tampering | Badge-State im UI | mitigate | Badges aus DTO (Plan 01); kein UI-Recompute (Lock 13) |
| T-74-04-SC | Tampering | npm installs | accept | Keine neuen Pakete (RESEARCH §Package Legitimacy Audit n/a) |
</threat_model>

<verification>
- `cd frontend && npm run typecheck` grün.
- `cd frontend && npx vitest run MemberProfileHero MemberStatusPill deriveKnownFor` grün.
- Manuelle Sichtprüfung der Memorial-Würdigkeit + redaktionellen „Bekannt für"-Kuratierung erfolgt in der End-of-Phase-UAT (74-VALIDATION §Manual-Only).
</verification>

<success_criteria>
Dreistufige Scroll-Seite mit Sticky-Nav, Status-Pill, read-only „Bekannt für"-Ableitung (D-03), würdevoller Memorial-Hero, RichTextRenderer-Story, Empty-State-Platzhaltern; Public-Badge-Quelle aus DTO; Pre-flight-Gate auf profile_status erzwungen.
</success_criteria>

<output>
Create `.planning/phases/74-public-member-profile-members-slug-memorial/74-04-SUMMARY.md` when done
</output>
</content>
