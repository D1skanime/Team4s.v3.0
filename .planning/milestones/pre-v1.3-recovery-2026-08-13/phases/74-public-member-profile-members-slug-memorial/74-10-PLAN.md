---
phase: 74-public-member-profile-members-slug-memorial
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/repository/member_claims_repository.go
  - backend/internal/repository/member_claim_invitations_repository.go
  - backend/internal/repository/member_claim_guards.go
  - backend/internal/handlers/member_memorial_handler.go
  - backend/internal/repository/member_claims_memorial_guard_test.go
  - backend/internal/handlers/member_memorial_handler_test.go
autonomous: true
gap_closure: true
requirements: [J, K, D-10, D-13, D-14, D-15, D-17]
must_haves:
  truths:
    - "SubmitClaim lehnt Claims gegen memorial-Profile mit HTTP 409 + Code 'memorial_not_claimable' ab (GAP-6/7, D-17)"
    - "AcceptInvitation (zweiter Claim-Pfad) lehnt memorial-Profile ebenfalls mit 409 + 'memorial_not_claimable' ab (GAP-6/7, D-17)"
    - "Beide Ablehn-Pfade schreiben ein denied-Audit-Event 'member_claim.memorial_blocked' mit Outcome 'denied' (D-15)"
    - "Memorial-Setter (POST /admin/members/:id/memorial) ist Global-Admin-only und auditiert ('member_profile.memorial_set', allowed) ohne app_user-Account-Eingriff (D-13/D-14/D-15)"
    - "Mengen-/Gamification-Badges werden für memorial-Profile unterdrückt (D-10) — code-level abgesichert"
    - "Alle obigen Verhalten sind durch `go test` abgedeckt (keine Live-/Seed-Daten nötig)"
  artifacts:
    - path: "backend/internal/repository/member_claims_memorial_guard_test.go"
      provides: "Tests für beide Claim-Pfade + denied-Audit-Key"
      contains: "memorial_not_claimable"
    - path: "backend/internal/handlers/member_memorial_handler_test.go"
      provides: "Test: Global-Admin-Gate + allowed-Audit-Event"
      contains: "member_profile.memorial_set"
  key_links:
    - from: "SubmitClaim / AcceptInvitation"
      to: "member_claim_guards.go (assertMemberNotMemorial)"
      via: "profile_status-Check -> 409 memorial_not_claimable + denied-Audit"
      pattern: "memorial_not_claimable"
---

<objective>
GAP-6/7 (CODE-ONLY — die DB enthält 0 memorial/historical-Profile, daher kein Live-Test, sondern `go test`-Absicherung): Audit + ggf. Fix der Memorial-Schutzkette:

1. Claim-Sperre gegen memorial-Profile mit HTTP 409 + Code `memorial_not_claimable` in **beiden** Pfaden (SubmitClaim UND AcceptInvitation/zweiter Claim-Pfad, D-17).
2. denied-Audit-Event `member_claim.memorial_blocked` (Outcome `denied`) in beiden Pfaden (D-15).
3. Memorial-Setter (POST /admin/members/:id/memorial): Global-Admin-only, auditiert (`member_profile.memorial_set`, allowed), kein app_user-Account-Eingriff (D-13/D-14/D-15).
4. Unterdrückung der Mengen-/Gamification-Badges für memorial-Profile (D-10) — code-level.

Der Re-Audit-Befund: Code existiert bereits (member_claim_guards.go, member_claims_repository.go, member_claim_invitations_repository.go, member_memorial_handler.go) inkl. Tests (member_claims_memorial_guard_test.go, member_memorial_handler_test.go). **Standardfall: bestätigen via `go test` und Coverage nur dort ergänzen, wo eine Lücke besteht.** Fix-Tasks nur, wenn ein Test rot ist oder eine Verhaltenslücke nachweisbar ist.

Purpose: Schließt den verbleibenden offenen GAP-6/7 (vorher nie mit echten Daten geprüft). Wave 1, unabhängig — eigene Dateien, keine Überschneidung mit 74-07/74-08.
Output: grüne Go-Tests; ergänzte Coverage falls Lücken; Fixes nur bei nachgewiesenem Defekt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-CONTEXT.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-VERIFICATION.md

<interfaces>
<!-- Bestehende Implementierung (aus Codebase). -->

member_claim_guards.go (Z. 13-33): assertMemberNotMemorial — liest profile_status, gibt ClaimMutationError{Code:"memorial_not_claimable", HTTP 409} bei status=='memorial'.
member_claims_repository.go (Z. 120-145): SubmitClaim — profile_status-Check, denied-Audit "member_claim.memorial_blocked", Code "memorial_not_claimable".
member_claim_invitations_repository.go (Z. 187-211): AcceptInvitation — zweiter Claim-Pfad, gleicher Guard + denied-Audit.
member_memorial_handler.go (Z. 97-152): SetMemorial — requirePlatformAdminAppUserIdentity (Global Admin), SetMemorialStatus, Audit "member_profile.memorial_set"/allowed, KEIN app_user-UPDATE (D-13).
member_claims_memorial_guard_test.go: Fragment-Tests für beide Pfade + denied-Audit-Key.
member_memorial_handler_test.go: Global-Admin-Gate + allowed-Audit-Event.
MemberBadgeHighlights.tsx (Z. 48-67): isMemorial -> filtert Gamification-/Mengen-Badge-Kategorien (D-10) — Frontend-Suppression bereits vorhanden.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit + go test der gesamten Memorial-Schutzkette (beide Claim-Pfade, Setter, Audit)</name>
  <files>backend/internal/repository/member_claims_repository.go, backend/internal/repository/member_claim_invitations_repository.go, backend/internal/repository/member_claim_guards.go, backend/internal/handlers/member_memorial_handler.go</files>
  <read_first>
    - backend/internal/repository/member_claim_guards.go (Z. 13-33 assertMemberNotMemorial)
    - backend/internal/repository/member_claims_repository.go (Z. 120-145 SubmitClaim-Guard + Audit)
    - backend/internal/repository/member_claim_invitations_repository.go (Z. 187-211 AcceptInvitation-Guard + Audit)
    - backend/internal/handlers/member_memorial_handler.go (Z. 97-152 SetMemorial: Global-Admin-Gate, SetMemorialStatus, Audit, kein Account-Eingriff)
    - backend/internal/repository/member_memorial_repository.go (SetMemorialStatus schreibt nur members.profile_status)
  </read_first>
  <action>
    1. Bestehende Tests laufen lassen: `go test ./internal/repository/ ./internal/handlers/ -run 'Memorial|Claim'`. Ergebnis bewerten.
    2. **Audit gegen die Akzeptanzkriterien** (jeweils Code lesen, KEINE blinde Änderung):
       - SubmitClaim: profile_status-Check vorhanden? 409 + Code "memorial_not_claimable"? denied-Audit "member_claim.memorial_blocked" mit Outcome "denied"?
       - AcceptInvitation: derselbe Guard + denied-Audit (zweiter Pfad, Fallstrick 3)?
       - SetMemorial: Global-Admin-only (requirePlatformAdminAppUserIdentity, NICHT gruppen-capability)? Audit "member_profile.memorial_set"/allowed? KEIN UPDATE auf app_user/account (D-13)? SetMemorialStatus berührt nur members.profile_status?
    3. **Nur falls eine konkrete Lücke nachweisbar ist** (Test rot ODER fehlendes Verhalten): minimal fixen — Outcome-Wert, fehlender denied-Audit-Call, fehlender Guard im zweiten Pfad, versehentlicher Account-Eingriff. Sonst keine Code-Änderung (der Standardfall laut Re-Audit ist „bereits korrekt").
    4. Dokumentiere im SUMMARY pro Punkt: bestätigt / gefixt (mit Diff-Begründung).
  </action>
  <verify>
    <automated>cd backend && go test ./internal/repository/ ./internal/handlers/ -run "Memorial|Claim" -v</automated>
  </verify>
  <acceptance_criteria>
    - Beide Claim-Pfade (SubmitClaim, AcceptInvitation) lehnen memorial mit 409 + "memorial_not_claimable" ab.
    - Beide Pfade schreiben denied-Audit "member_claim.memorial_blocked" (Outcome "denied").
    - SetMemorial ist Global-Admin-only, auditiert (allowed), ohne app_user-Account-Eingriff (D-13).
    - `go test ... -run "Memorial|Claim"` grün; Fixes (falls nötig) sind minimal und begründet.
  </acceptance_criteria>
  <done>Memorial-Schutzkette code-level bestätigt (oder minimal gefixt) und grün getestet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Coverage-Lücken schließen (denied-Audit-Outcome + zweiter Pfad + D-10-Suppression)</name>
  <files>backend/internal/repository/member_claims_memorial_guard_test.go, backend/internal/handlers/member_memorial_handler_test.go</files>
  <behavior>
    - SubmitClaim gegen memorial -> ClaimMutationError mit Code "memorial_not_claimable" und HTTP-Status 409.
    - AcceptInvitation gegen memorial -> dieselbe Ablehnung (zweiter Pfad explizit getestet).
    - Beide schreiben ein Audit-Event mit EventType/Action "member_claim.memorial_blocked" und Outcome "denied" (nicht nur "allowed").
    - SetMemorial: nicht-Global-Admin -> abgelehnt (kein Memorial gesetzt); Global-Admin -> allowed-Audit "member_profile.memorial_set" + profile_status='memorial' in der Response, KEIN app_user-Account-Mutationsaufruf.
  </behavior>
  <read_first>
    - backend/internal/repository/member_claims_memorial_guard_test.go (bestehende Fragment-/Behavior-Tests; Outcome-"denied"-Assertion vorhanden?)
    - backend/internal/handlers/member_memorial_handler_test.go (Z. 47-113 Global-Admin-Gate + allowed-Audit)
    - backend/internal/repository/member_claim_guards.go / member_claims_repository.go / member_claim_invitations_repository.go (Verhalten, gegen das getestet wird)
  </read_first>
  <action>
    1. Prüfe die bestehenden Tests gegen die behavior-Liste. Ergänze NUR fehlende Assertions:
       - Falls die denied-Audit-**Outcome**="denied"-Prüfung fehlt (Re-Audit nannte das explizit) -> Assertion ergänzen, dass das Event Outcome "denied" trägt.
       - Falls der AcceptInvitation-Pfad nur als Fragment-Test, aber nicht verhaltensbezogen abgedeckt ist -> Verhaltenstest mit gestubbter DB/Audit ergänzen (analog zum SubmitClaim-Test).
       - Falls der Memorial-Handler-Test den „kein app_user-Account-Eingriff"-Aspekt (D-13) nicht absichert -> Assertion ergänzen (z. B. dass kein Account-Repo-Mutationsaufruf erfolgt / Stub nicht aufgerufen wird).
    2. D-10-Suppression (Mengen-/Gamification-Badges bei memorial): die Frontend-Suppression existiert bereits (MemberBadgeHighlights.test bzw. .tsx). Falls KEIN Test die Suppression abdeckt, einen knappen Vitest-Hinweis im SUMMARY vermerken — die Frontend-Test-Ergänzung gehört nicht in dieses backend-fokussierte Plan; nur dokumentieren, NICHT hier neue Frontend-Dateien anlegen (Wave/Ownership-Trennung).
    3. Keine Over-Coverage: nur die im Re-Audit benannten Lücken.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/repository/ ./internal/handlers/ -run "Memorial|Claim" -v</automated>
  </verify>
  <acceptance_criteria>
    - denied-Audit-Outcome "denied" ist in beiden Claim-Pfaden getestet.
    - AcceptInvitation-Memorial-Ablehnung ist verhaltensbezogen (nicht nur Fragment) abgedeckt.
    - Memorial-Handler-Test deckt Global-Admin-Gate + allowed-Audit + kein-Account-Eingriff (D-13) ab.
    - `go test ... -run "Memorial|Claim"` grün.
  </acceptance_criteria>
  <done>Coverage-Lücken der Memorial-Schutzkette geschlossen; alle Go-Tests grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Claim-/Memorial-Endpoints | Untrusted; Server-Autorität entscheidet (409-Sperre, Global-Admin-Gate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-10-EOP | Elevation of Privilege | Nicht-Global-Admin setzt memorial | mitigate | requirePlatformAdminAppUserIdentity-Gate; Test im member_memorial_handler_test.go |
| T-74-10-BYP | Tampering | Claim umgeht Memorial-Sperre über zweiten Pfad (AcceptInvitation) | mitigate | Guard in BEIDEN Pfaden + Verhaltenstest (Fallstrick 3) |
| T-74-10-REP | Repudiation | Memorial-Vorgänge/Ablehnungen nicht nachvollziehbar | mitigate | denied-Audit "member_claim.memorial_blocked"/Outcome "denied" + allowed-Audit "member_profile.memorial_set"; je Pfad getestet (D-15) |
| T-74-10-SC | Tampering | go installs | accept | Keine neuen Pakete |
</threat_model>

<verification>
- `cd backend && go test ./internal/repository/ ./internal/handlers/ -run "Memorial|Claim" -v` grün.
- SUMMARY dokumentiert pro Befund: bestätigt / gefixt.
</verification>

<success_criteria>
GAP-6/7 code-level abgeschlossen: beide Claim-Pfade sperren memorial (409 + Code + denied-Audit), Memorial-Setter Global-Admin-only + auditiert ohne Account-Eingriff, D-10-Suppression bestätigt — alles via `go test` abgesichert (keine Seed-/Live-Daten nötig).
</success_criteria>

<output>
Create `.planning/phases/74-public-member-profile-members-slug-memorial/74-10-SUMMARY.md` when done
</output>
</content>
