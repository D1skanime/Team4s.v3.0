---
phase: 74-public-member-profile-members-slug-memorial
plan: 01
type: execute
wave: 2
depends_on: ["74-00"]
files_modified:
  - shared/contracts/openapi.yaml
  - backend/internal/models/member_profile.go
  - backend/internal/repository/badge_repository.go
  - backend/internal/repository/member_profile_repository.go
  - backend/internal/handlers/app_public_profile.go
  - frontend/src/lib/api.ts
  - frontend/src/types/profile.ts
autonomous: true
requirements: [C, "Badges-13", K, D-09, D-11]
must_haves:
  truths:
    - "Das PublicMemberProfile-DTO trägt profile_status (D-09) und public_badges (D-11/Badges-13) — eingebettet, kein zweiter Endpoint"
    - "GetPublicMemberBadges selektiert ausschließlich visibility='public' AND status='active' — Badge-Service ist Quelle, kein UI-Recompute (Badges-13)"
    - "Contract-zuerst: OpenAPI + DTO + Repo + Handler + api.ts + Types bewegen sich gemeinsam (K)"
    - "Fremder Besucher von /members/[slug] erhält die public-Badges des angezeigten Members (nicht Viewer-Badges) (Fallstrick 2)"
  artifacts:
    - path: "backend/internal/models/member_profile.go"
      provides: "PublicMemberProfile + ProfileStatus string + PublicBadges []PublicMemberBadge"
      contains: "ProfileStatus"
    - path: "backend/internal/repository/badge_repository.go"
      provides: "GetPublicMemberBadges(memberID) mit visibility='public' AND status='active'"
      contains: "GetPublicMemberBadges"
    - path: "backend/internal/repository/member_profile_repository.go"
      provides: "profile_status in der candidates-CTE + Top-N public Badges via LATERAL"
      contains: "profile_status"
    - path: "shared/contracts/openapi.yaml"
      provides: "PublicMemberProfile-Schema um profile_status + public_badges erweitert"
      contains: "profile_status"
    - path: "frontend/src/types/profile.ts"
      provides: "PublicMemberProfileData + profile_status + public_badges Typen"
      contains: "profile_status"
  key_links:
    - from: "backend/internal/handlers/app_public_profile.go"
      to: "member_profile_repository.GetPublicMemberProfile"
      via: "Envelope { data: profile } inkl. profile_status + public_badges"
      pattern: "profile_status"
    - from: "frontend/src/lib/api.ts getMemberProfile"
      to: "PublicMemberProfileData.public_badges"
      via: "response.data.public_badges"
      pattern: "public_badges"
---

<objective>
Erweitere die member-zentrierte Read-Projektion: Das `PublicMemberProfile`-DTO trägt künftig `profile_status` (für Status-Pill D-09 und Memorial-Variante D-10) und eingebettete `public_badges` (Top-N-Quelle für D-11/Badges-13). Eine neue Repo-Methode `GetPublicMemberBadges` liefert ausschließlich `visibility='public' AND status='active'` — Badge-Service bleibt Quelle, das UI rechnet nichts neu.

Purpose: Schließt die Hauptlücke (heute zeigt die Seite Viewer-Badges via getMyBadges) und liefert das Status-Feld, auf dem die gesamte Frontend-Statusdarstellung aufsetzt. Phase 72 erweitert das member-zentrierte DTO NICHT (liefert nur das `members.profile_status`-Schema), daher macht Phase 74 diese DTO-Erweiterung selbst.
Output: Contract-first erweitertes DTO über alle 7 Lock-K-Schichten (OpenAPI → DTO → Repo → Handler → api.ts → Types → die Tests aus Plan 00).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-RESEARCH.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-PATTERNS.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-VALIDATION.md

<interfaces>
<!-- Verifiziert aus Quellcode; Executor nutzt diese Verträge direkt -->

backend/internal/models/member_profile.go (PublicMemberProfile-DTO ab Zeile 169) — erweitern um:
  ProfileStatus string              `json:"profile_status"`
  PublicBadges  []PublicMemberBadge `json:"public_badges"`
neuer Typ PublicMemberBadge: { ID int64; BadgeCode string; BadgeCategory string } mit json-Tags id/badge_code/badge_category

backend/internal/repository/badge_repository.go — bestehende GetMemberBadges (Z. 114) liefert ALLE active Badges (inkl. internal/hidden). NICHT wiederverwenden für Public-Quelle.

backend/internal/repository/member_profile_repository.go — GetPublicMemberProfile (Z. 345 ff., candidates-CTE ab Z. 359).

Phase-72-Schema (aus 72-01-PLAN.md, kommt VOR Phase 74 live):
  members.profile_status VARCHAR(20) NOT NULL DEFAULT 'active' CHECK IN ('active','historical','memorial')
  Hinweis: 'claimed'/'unclaimed' sind NICHT in profile_status — sie bleiben derived aus member_claims.claim_status='verified'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GetPublicMemberBadges-Repo-Methode + DTO-Erweiterung (Backend)</name>
  <behavior>
    - GetPublicMemberBadges(ctx, memberID) liefert nur Badges mit status='active' AND visibility='public', sortiert nach awarded_at.
    - PublicMemberProfile serialisiert profile_status + public_badges in JSON.
    - Source-Fragment-Test aus Plan 00 (badge_public_source_test.go) wird GREEN.
  </behavior>
  <read_first>
    - backend/internal/repository/badge_repository.go (GetMemberBadges Z. 114 — Spaltennamen badge_code/badge_category/visibility/status/awarded_at; pgx $N-Parametrisierung)
    - backend/internal/models/member_profile.go (PublicMemberProfile Z. 169 ff.; bestehende Sub-DTO-Konventionen z. B. MemberProfileMembership Z. 22)
    - backend/internal/repository/member_profile_repository.go (GetPublicMemberProfile Z. 345, candidates-CTE Z. 359 — Stelle für LATERAL-Join + m.profile_status)
    - backend/internal/repository/badge_public_source_test.go (Plan-00-Stub — Ziel-Assertions visibility='public' AND status='active')
    - 74-RESEARCH.md §Public-Badge-Quelle + Code-Beispiele (CTE-LATERAL)
    - 74-PATTERNS.md §member_profile_repository / badge_repository / models
  </read_first>
  <action>
    Backend (Lock K, gemeinsam):
    1. `models/member_profile.go`: Füge `PublicMemberBadge`-Struct hinzu (Felder ID int64 `json:"id"`, BadgeCode string `json:"badge_code"`, BadgeCategory string `json:"badge_category"`) und erweitere `PublicMemberProfile` um `ProfileStatus string `json:"profile_status"`` und `PublicBadges []PublicMemberBadge `json:"public_badges"``.
    2. `badge_repository.go`: Neue Methode `GetPublicMemberBadges(ctx, memberID int64) ([]models.PublicMemberBadge, error)`. SQL parametrisiert via $1: `SELECT id, badge_code, badge_category FROM member_badges WHERE member_id=$1 AND status='active' AND visibility='public' ORDER BY awarded_at`. Die exakten Strings `visibility='public'` und `status='active'` müssen wörtlich im Quelltext stehen (Plan-00 Source-Fragment-Test).
    3. `member_profile_repository.go`: Ergänze in der candidates-CTE von `GetPublicMemberProfile` die Spalte `m.profile_status` und scanne sie in `profile.ProfileStatus`. Füge einen `LEFT JOIN LATERAL` für die Top-N public Badges hinzu (json_agg über member_badges WHERE member_id=m.id AND status='active' AND visibility='public' ORDER BY awarded_at) und scanne das Aggregat in `profile.PublicBadges`. Falls die CTE-Erweiterung die Datei über 450 Zeilen treibt, lagere die Public-Badge-Projektion in eine Hilfsfunktion / separate Repo-Datei aus.
    Beachte 450-Zeilen-Limit pro Datei; deutsche Umlaute nur bei user-facing Strings (hier keine).
  </action>
  <verify>
    <automated>cd backend && go test ./internal/repository/... -run "PublicBadges" && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - badge_public_source_test.go (Plan 00) ist GREEN.
    - GetPublicMemberBadges-SQL enthält wörtlich `visibility='public'` und `status='active'`.
    - PublicMemberProfile hat json-Felder `profile_status` und `public_badges`.
    - `go build ./...` erfolgreich; keine Datei > 450 Zeilen.
  </acceptance_criteria>
  <done>Repo-Methode + DTO-Felder existieren; Public-Badge-Source-Test grün; profile_status wird aus der CTE projiziert.</done>
</task>

<task type="auto">
  <name>Task 2: Contract-Schichten verdrahten (OpenAPI + Handler + api.ts + Types)</name>
  <read_first>
    - shared/contracts/openapi.yaml (/api/v1/members/{slug} Z. 458; PublicMemberProfile-Schema ~Z. 7705 ff.)
    - backend/internal/handlers/app_public_profile.go (GetPublicMemberProfile Z. 28-61 — Envelope { data: profile } bzw. { visible:false, reason })
    - frontend/src/lib/api.ts (getMemberProfile Z. 2663 — authorizedFetch-Struktur, Rückgabe-Envelope)
    - frontend/src/types/profile.ts (bestehende PublicMemberProfileData-Definition)
    - 74-RESEARCH.md §Lock-K-Checkliste / Fallstrick 6
  </read_first>
  <action>
    Lock-K-Verdrahtung (gemeinsam, nicht nachträglich):
    1. `openapi.yaml`: Erweitere das `PublicMemberProfile`-Schema um `profile_status` (string, enum active/historical/memorial — Hinweis: claimed/unclaimed sind derived, nicht im Schema-Enum) und `public_badges` (array of object mit id/badge_code/badge_category). Beschreibung mit korrekten Umlauten.
    2. `app_public_profile.go`: Keine Logikänderung am Envelope nötig (das Repo füllt jetzt die neuen Felder); stelle sicher, dass das zurückgegebene Profil die neuen Felder durchreicht (gleiche `c.JSON(http.StatusOK, gin.H{"data": profile})`-Struktur). Falls der Handler heute getMyBadges/Viewer-Badges mischt, entferne diese Vermengung — Public-Badges kommen ausschließlich aus dem DTO.
    3. `frontend/src/types/profile.ts`: Erweitere `PublicMemberProfileData` um `profile_status: 'active' | 'historical' | 'memorial'` und `public_badges: { id: number; badge_code: string; badge_category: string }[]`.
    4. `frontend/src/lib/api.ts`: `getMemberProfile` gibt das erweiterte DTO durch (keine zweite Fetch-Funktion für Badges nötig — eingebettet). Stelle sicher, dass keine ad-hoc-Fetch-Abkürzung eingeführt wird (Lock K/A).
    Keine Token-/Cookie-Direktzugriffe; native HTML-Elemente nicht betroffen (kein UI hier).
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck && cd ../backend && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - openapi.yaml PublicMemberProfile-Schema enthält `profile_status` und `public_badges`.
    - `npm run typecheck` grün mit den neuen Type-Feldern.
    - `go build ./...` grün; Handler reicht profile_status + public_badges im `data`-Envelope durch.
    - Keine neue ad-hoc-Fetch-Funktion; getMemberProfile bleibt der einzige Read-Seam.
  </acceptance_criteria>
  <done>Alle 7 Lock-K-Schichten tragen profile_status + public_badges; typecheck + build grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/v1/members/:slug | Öffentlicher unauthentifizierter Read; liefert nur public-Badges + Status |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01-INFO | Information Disclosure | GetPublicMemberBadges | mitigate | SQL-Guard `visibility='public' AND status='active'` — internal/hidden Badges nie öffentlich; Source-Fragment-Test erzwingt Guard |
| T-74-01-TAMP | Tampering | Badge-State im UI | mitigate | UI rechnet Badge-State NICHT neu (Lock 13); DTO ist autoritative Quelle, Top-N nur Slicing |
| T-74-01-SC | Tampering | go/npm installs | accept | Keine neuen Pakete (RESEARCH §Package Legitimacy Audit n/a) |
</threat_model>

<verification>
- `cd backend && go test ./internal/repository/... -run PublicBadges` grün.
- `cd backend && go build ./...` grün.
- `cd frontend && npm run typecheck` grün.
- OpenAPI-Schema-Review: profile_status + public_badges vorhanden.
</verification>

<success_criteria>
PublicMemberProfile-DTO trägt profile_status + public_badges über alle Lock-K-Schichten; Public-Badges nur visibility='public'+status='active'; Viewer-Badge-Vermengung entfernt.
</success_criteria>

<output>
Create `.planning/phases/74-public-member-profile-members-slug-memorial/74-01-SUMMARY.md` when done
</output>
