---
phase: 82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/0104_members_backfill_and_fansub_group_members_member_id.up.sql
  - database/migrations/0104_members_backfill_and_fansub_group_members_member_id.down.sql
  - database/migrations/0105_anime_contributions_member_id.up.sql
  - database/migrations/0105_anime_contributions_member_id.down.sql
  - database/migrations/0106_fansub_group_member_roles_fk.up.sql
  - database/migrations/0106_fansub_group_member_roles_fk.down.sql
  - database/migrations/0107_fansub_group_default_crew.up.sql
  - database/migrations/0107_fansub_group_default_crew.down.sql
autonomous: true
requirements:
  - D-01
  - D-02
  - D-04
  - D-07
  - D-08

must_haves:
  truths:
    - "anime_contributions hat eine NOT NULL Spalte member_id BIGINT REFERENCES members(id) nach Migration 0105 (per D-01)"
    - "fansub_group_members hat eine nullable Spalte member_id BIGINT REFERENCES members(id) nach Migration 0104 (per D-02)"
    - "Für jeden App-Member ohne verknüpfte members-Zeile wurde eine neue Zeile in members eingefügt (per D-02)"
    - "fansub_group_member_roles hat FK auf role_definitions(code) statt hartkodiertem CHECK nach Migration 0106 (per D-08)"
    - "Tabelle fansub_group_default_crew existiert mit Unique UNIQUE(fansub_group_id, member_id, role_code) nach Migration 0107 (per D-04)"
    - "Backfill: alle bestehenden anime_contributions-Zeilen haben member_id != NULL"
  artifacts:
    - path: "database/migrations/0104_members_backfill_and_fansub_group_members_member_id.up.sql"
      provides: "members-Backfill + fansub_group_members.member_id Spalte"
    - path: "database/migrations/0105_anime_contributions_member_id.up.sql"
      provides: "anime_contributions.member_id Spalte + Backfill + neuer Unique-Key"
    - path: "database/migrations/0106_fansub_group_member_roles_fk.up.sql"
      provides: "CHECK → FK-Umstellung für fansub_group_member_roles.role"
    - path: "database/migrations/0107_fansub_group_default_crew.up.sql"
      provides: "neue Tabelle fansub_group_default_crew"
  key_links:
    - from: "anime_contributions.member_id"
      to: "members.id"
      via: "FK ON DELETE RESTRICT"
      pattern: "REFERENCES members\\(id\\)"
    - from: "fansub_group_member_roles.role"
      to: "role_definitions.code"
      via: "FK (statt CHECK)"
      pattern: "REFERENCES role_definitions\\(code\\)"
---

<objective>
Datenbankmigrationen 0104–0107 anlegen, die den Anker von `anime_contributions` von `hist_fansub_group_members.id` auf `members.id` umstellen, App-Member mit members-Zeilen versorgen, den Rollen-CHECK auf FK umstellen und die Standard-Team-Tabelle einführen.

Purpose: Alle nachfolgenden Backend- und Frontend-Änderungen bauen auf diesen Migrations auf. Ohne diese Grundlage können weder der neue Endpoint noch die UI-Umstellung implementiert werden.
Output: Vier UP/DOWN-Migrationsdateien, anwendbar mit dem bestehenden migrate-Tooling.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/PROJECT.md
@C:/Users/admin/Documents/Team4s/.planning/ROADMAP.md
@C:/Users/admin/Documents/Team4s/.planning/STATE.md
@C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-CONTEXT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-RESEARCH.md
@C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-PATTERNS.md

<interfaces>
<!-- Schlüsselschemas, die der Executor kennen muss -->

Aus database/migrations/0086_anime_contributions.up.sql (verifiziert):
  fansub_group_member_id BIGINT NOT NULL REFERENCES hist_fansub_group_members(id) ON DELETE RESTRICT
  UNIQUE NULLS NOT DISTINCT (fansub_group_id, anime_id, fansub_group_member_id, release_version_id)
  -- Constraint-Name: uq_anime_contribution_member (aus 0091)

Aus database/migrations/0073_fansub_group_app_memberships.up.sql:
  -- fansub_group_members hat KEIN member_id-Feld (zu ergänzen in 0104)

Aus database/migrations/0082_historical_fansub_group_members.up.sql:
  hist_fansub_group_members.member_id BIGINT NOT NULL REFERENCES members(id)
  -- Backfill-Join: ac.fansub_group_member_id → hfgm.id → hfgm.member_id

Aus database/migrations/0074_expand_fansub_group_member_roles.up.sql:
  -- CHECK constraint name: chk_fansub_group_member_roles_role
  -- Spalte role: VARCHAR(40)
  -- Codes: 'fansub_lead','project_lead','translator','timer','typesetter','editor','encoder','raw_provider','quality_checker','designer'

Aus database/migrations/0085_anime_contribution_roles_catalog.up.sql:
  -- role_definitions(code TEXT, ...) — alle obigen Codes existieren bereits
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0104 — members-Backfill + fansub_group_members.member_id</name>
  <files>
    database/migrations/0104_members_backfill_and_fansub_group_members_member_id.up.sql
    database/migrations/0104_members_backfill_and_fansub_group_members_member_id.down.sql
  </files>
  <read_first>
    - database/migrations/0073_fansub_group_app_memberships.up.sql (Schema von fansub_group_members)
    - database/migrations/0082_historical_fansub_group_members.up.sql (Schema von hist_fansub_group_members + members-Verknüpfung)
    - database/migrations/0044_add_db_schema_v2_target_tables.up.sql (members-Tabellen-Schema)
    - backend/internal/repository/fansub_group_app_members_repository.go (App-Member/members-Verknüpfung via Claims)
    - C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-RESEARCH.md (Abschnitt 3 — members-Backfill, Z. 266–285)
  </read_first>
  <action>
    UP-Migration erstellen mit folgendem Ablauf (alle Schritte in einer Transaktion):

    Schritt A — Neue members-Zeilen für App-Member ohne verknüpfte members-Zeile (per D-02):
    INSERT INTO members (nickname, created_at, updated_at) ... für jeden fansub_group_members-Eintrag, bei dem weder ein verified member_claim (mc.member_id) noch ein legacy user_id-Match (members.user_id via app_users.legacy_user_id) existiert. Nickname aus app_users.display_name.

    Schritt B — Neue Spalte zu fansub_group_members hinzufügen:
    ALTER TABLE fansub_group_members ADD COLUMN IF NOT EXISTS member_id BIGINT NULL REFERENCES members(id) ON DELETE RESTRICT;

    Schritt C — Backfill fansub_group_members.member_id aus member_claims (claim_status='verified', neueste verified_at) ODER members.user_id via legacy_user_id ODER aus dem in Schritt A angelegten Eintrag.
    UPDATE fansub_group_members fgm SET member_id = COALESCE(mc.member_id, m_legacy.id, m_new.id) FROM ...

    Schritt D — Index anlegen:
    CREATE INDEX IF NOT EXISTS idx_fansub_group_members_member_id ON fansub_group_members(member_id) WHERE member_id IS NOT NULL;

    DOWN-Migration: DROP INDEX, ALTER TABLE fansub_group_members DROP COLUMN member_id; (die in Schritt A angelegten members-Zeilen werden nicht rückgängig gemacht — irreversibel).

    Wichtig: Migration-Header-Kommentar mit Zweck und Abhängigkeit zu 0073/0082/0044 schreiben. Backfill-Kollisionsprüfung: COALESCE in definierter Reihenfolge (claim → legacy → new) verhindert doppelte Zuordnung.
  </action>
  <verify>
    <automated>cd "C:/Users/admin/Documents/Team4s" && docker compose exec backend /app/migrate up && docker compose exec db psql -U postgres -d team4s -c "\d fansub_group_members" | grep member_id</automated>
  </verify>
  <acceptance_criteria>
    - fansub_group_members hat Spalte member_id BIGINT (nullable)
    - SELECT COUNT(*) FROM fansub_group_members WHERE member_id IS NULL ergibt 0 (sofern App-Member vorhanden)
    - Index idx_fansub_group_members_member_id existiert
    - DOWN-Migration entfernt die Spalte wieder ohne Fehler
  </acceptance_criteria>
  <done>Spalte member_id in fansub_group_members, backgefüllt für alle bestehenden App-Member</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0105 — anime_contributions.member_id + Backfill + Unique-Key-Tausch</name>
  <files>
    database/migrations/0105_anime_contributions_member_id.up.sql
    database/migrations/0105_anime_contributions_member_id.down.sql
  </files>
  <read_first>
    - database/migrations/0086_anime_contributions.up.sql (Ist-Schema)
    - database/migrations/0091_anime_contributions_release_version.up.sql (Unique-Key uq_anime_contribution_member)
    - database/migrations/0082_historical_fansub_group_members.up.sql (member_id-Feld in hfgm)
    - C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-RESEARCH.md (Abschnitt 3 — Migrations-Reihenfolge, Z. 236–261)
    - C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-PATTERNS.md (0105-Patterns, Z. 74–113)
  </read_first>
  <action>
    UP-Migration mit allen Schritten in einer Transaktion:

    Schritt A — member_id nullable hinzufügen:
    ALTER TABLE anime_contributions ADD COLUMN IF NOT EXISTS member_id BIGINT NULL REFERENCES members(id) ON DELETE RESTRICT;

    Schritt B — Kollisionsprüfung vor Backfill (gibt Warnung/Fehler bei Duplikaten):
    RAISE EXCEPTION wenn SELECT COUNT(*) > 0 über die geplante Unique-Gruppe (fansub_group_id, anime_id, hfgm.member_id, release_version_id) mit HAVING COUNT(*) > 1 Treffer liefert. (RESEARCH Pitfall 3)

    Schritt C — Backfill member_id via JOIN auf hist_fansub_group_members:
    UPDATE anime_contributions ac SET member_id = hfgm.member_id FROM hist_fansub_group_members hfgm WHERE hfgm.id = ac.fansub_group_member_id;

    Schritt D — NOT NULL setzen:
    ALTER TABLE anime_contributions ALTER COLUMN member_id SET NOT NULL;

    Schritt E — Alten Unique-Key ablösen, neuen auf member_id anlegen:
    ALTER TABLE anime_contributions DROP CONSTRAINT IF EXISTS uq_anime_contribution_member;
    ALTER TABLE anime_contributions ADD CONSTRAINT uq_anime_contribution_member UNIQUE NULLS NOT DISTINCT (fansub_group_id, anime_id, member_id, release_version_id);

    Schritt F — fansub_group_member_id nullable machen (Übergangsphase — NICHT droppen, öffentliche Projektionen joinen noch darüber):
    ALTER TABLE anime_contributions ALTER COLUMN fansub_group_member_id DROP NOT NULL;

    DOWN-Migration: fansub_group_member_id NOT NULL setzen, Constraint zurücktauschen, member_id droppen.
  </action>
  <verify>
    <automated>cd "C:/Users/admin/Documents/Team4s" && docker compose exec db psql -U postgres -d team4s -c "SELECT COUNT(*) FROM anime_contributions WHERE member_id IS NULL;" && docker compose exec db psql -U postgres -d team4s -c "\d anime_contributions" | grep member_id</automated>
  </verify>
  <acceptance_criteria>
    - anime_contributions.member_id ist NOT NULL BIGINT REFERENCES members(id)
    - SELECT COUNT(*) FROM anime_contributions WHERE member_id IS NULL ergibt 0
    - Constraint uq_anime_contribution_member existiert auf (fansub_group_id, anime_id, member_id, release_version_id)
    - fansub_group_member_id ist nullable (NOT NULL Constraint entfernt)
    - DOWN-Migration kehrt alle Änderungen ohne Fehler um
  </acceptance_criteria>
  <done>anime_contributions ankert auf members.id per D-01; Backfill lückenlos</done>
</task>

<task type="auto">
  <name>Task 3: Migrationen 0106 (Rollen-FK) und 0107 (Standard-Team-Tabelle)</name>
  <files>
    database/migrations/0106_fansub_group_member_roles_fk.up.sql
    database/migrations/0106_fansub_group_member_roles_fk.down.sql
    database/migrations/0107_fansub_group_default_crew.up.sql
    database/migrations/0107_fansub_group_default_crew.down.sql
  </files>
  <read_first>
    - database/migrations/0074_expand_fansub_group_member_roles.up.sql (CHECK-Constraint, Spalte role VARCHAR(40))
    - database/migrations/0085_anime_contribution_roles_catalog.up.sql (role_definitions-Schema, code-Feld)
    - database/migrations/0082_historical_fansub_group_members.up.sql (Pattern für neue Tabellen mit FK auf fansub_groups + members)
    - C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-PATTERNS.md (0106-Pattern Z. 115–134, 0107-Pattern Z. 138–158)
  </read_first>
  <action>
    Migration 0106 (CHECK → FK für fansub_group_member_roles.role):
    UP:
    1. ALTER TABLE fansub_group_member_roles DROP CONSTRAINT IF EXISTS chk_fansub_group_member_roles_role;
    2. ALTER TABLE fansub_group_member_roles ALTER COLUMN role TYPE TEXT; (Typ angleichen, Pitfall 6)
    3. ALTER TABLE fansub_group_member_roles ADD CONSTRAINT fk_fansub_group_member_roles_role_code FOREIGN KEY (role) REFERENCES role_definitions(code) ON DELETE RESTRICT;
    DOWN: DROP CONSTRAINT fk_fansub_group_member_roles_role_code; ALTER TYPE zurück auf VARCHAR(40); CHECK-Constraint aus 0074 wiederherstellen.

    Migration 0107 (neue Tabelle fansub_group_default_crew per D-04):
    UP:
    CREATE TABLE IF NOT EXISTS fansub_group_default_crew (
      id               BIGSERIAL PRIMARY KEY,
      fansub_group_id  BIGINT NOT NULL REFERENCES fansub_groups(id) ON DELETE CASCADE,
      member_id        BIGINT NOT NULL REFERENCES members(id) ON DELETE CASCADE,
      role_code        TEXT NOT NULL REFERENCES role_definitions(code) ON DELETE RESTRICT,
      created_by       BIGINT NULL REFERENCES app_users(id) ON DELETE SET NULL,
      created_at       TIMESTAMPTZ NOT NULL DEFAULT NOW(),
      updated_at       TIMESTAMPTZ NOT NULL DEFAULT NOW(),
      CONSTRAINT uq_fansub_group_default_crew UNIQUE (fansub_group_id, member_id, role_code)
    );
    CREATE INDEX IF NOT EXISTS idx_fansub_group_default_crew_group ON fansub_group_default_crew(fansub_group_id);
    CREATE INDEX IF NOT EXISTS idx_fansub_group_default_crew_member ON fansub_group_default_crew(member_id);
    DOWN: DROP TABLE IF EXISTS fansub_group_default_crew;

    Beide Migrationen mit aussagekräftigem Header-Kommentar versehen (Zweck, Abhängigkeiten).
  </action>
  <verify>
    <automated>cd "C:/Users/admin/Documents/Team4s" && docker compose exec db psql -U postgres -d team4s -c "\d fansub_group_member_roles" | grep "fk_fansub" && docker compose exec db psql -U postgres -d team4s -c "\d fansub_group_default_crew"</automated>
  </verify>
  <acceptance_criteria>
    - fansub_group_member_roles hat FK fk_fansub_group_member_roles_role_code auf role_definitions(code)
    - fansub_group_member_roles hat KEINEN hartkodiertem CHECK-Constraint mehr für role
    - Tabelle fansub_group_default_crew existiert mit Spalten: id, fansub_group_id, member_id, role_code, created_by, created_at, updated_at
    - UNIQUE-Constraint uq_fansub_group_default_crew auf (fansub_group_id, member_id, role_code)
    - DOWN-Migrationen beider Dateien kehren ohne Fehler um
    - Backend kompiliert nach `docker compose build backend` fehlerfrei (Migration-Run ist die einzige Voraussetzung)
  </acceptance_criteria>
  <done>Rollen-FK umgestellt (D-08); Standard-Team-Tabelle bereit (D-04)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Migration-Tooling → Datenbank | Migrationen laufen mit DB-Owner-Rechten; fehlerhafte SQL-Statements können Daten korrumpieren |
| Backfill-Logik | COALESCE-Reihenfolge entscheidet, welche members-Zeile einem App-Member zugeordnet wird |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01-01 | Tampering | 0105 Backfill-Kollision | mitigate | RAISE EXCEPTION bei doppeltem (fansub_group_id, anime_id, member_id, release_version_id) vor Unique-Key-Anlage (RESEARCH Pitfall 3) |
| T-82-01-02 | Tampering | 0104 Backfill COALESCE-Reihenfolge | mitigate | Reihenfolge claim → legacy → new ist deterministisch; kein Zufall |
| T-82-01-03 | Denial of Service | 0106 FK-Drop ohne Backup | mitigate | DOWN-Migration stellt CHECK-Constraint vollständig wieder her |
| T-82-01-SC | Tampering | npm/pip/cargo installs | accept | keine Package-Installs in diesem Plan |
</threat_model>

<verification>
Nach Ausführung aller drei Tasks:
1. `docker compose exec backend /app/migrate up` läuft ohne Fehler durch alle vier Migrationen
2. `docker compose exec db psql -U postgres -d team4s -c "SELECT COUNT(*) FROM anime_contributions WHERE member_id IS NULL;"` → 0
3. `docker compose exec db psql -U postgres -d team4s -c "\d fansub_group_default_crew"` → Tabelle mit korrekten Spalten
4. `docker compose exec db psql -U postgres -d team4s -c "\d fansub_group_member_roles"` → FK statt CHECK
</verification>

<success_criteria>
- Alle vier UP-Migrationen (0104–0107) laufen fehlerfrei durch
- anime_contributions.member_id ist NOT NULL und vollständig backgefüllt
- fansub_group_members.member_id Spalte existiert und ist backgefüllt
- fansub_group_default_crew-Tabelle ist bereit für Plan 02 (Backend)
- fansub_group_member_roles.role hat FK auf role_definitions(code)
- DOWN-Migrationen aller vier Dateien kehren ohne Fehler um
</success_criteria>

<output>
Create `.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-01-SUMMARY.md` when done
</output>
