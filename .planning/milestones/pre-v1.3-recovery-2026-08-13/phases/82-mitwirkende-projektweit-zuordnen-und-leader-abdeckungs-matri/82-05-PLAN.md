---
phase: 82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri
plan: 05
type: execute
wave: 3
depends_on:
  - 82-04
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionModal.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/AnimeReleasesFilterBar.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/DefaultCrewManager.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-04
  - D-05
  - D-06
  - D-07
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13
  - D-14
  - D-15
  - D-16

must_haves:
  truths:
    - "MAIN_TABS in page.tsx enthält kein Element mit key='anime-projekte' mehr (per D-13)"
    - "parseMainTab('anime-projekte') gibt 'releases' zurück (per D-13)"
    - "SectionKey-Union-Typ enthält 'anime-projekte' nicht mehr (per D-13)"
    - "Filterchip-Reihe [Alle] [Mitwirkende fehlen] [Einblick fehlt] ist oberhalb der Projektliste gerendert (per D-11, D-12)"
    - "ProjectCockpitBadges-Komponente ist in die Projektkarten-Kopfzeile eingebaut (per D-11)"
    - "AnimeProjectNoteWorkspace-Komponente erscheint im aufgeklappten Projektbereich vor der Releases-Liste (per D-10)"
    - "CoverageMatrix-Komponente ist im releases-Tab gerendert und erhält roles + rows als Props (per D-07)"
    - "Button 'Standard-Team übernehmen' ist verdrahtet: ruft applyDefaultCrew(fansubId) auf und zeigt Ergebnis an (per D-04 — KEIN toter Button)"
    - "DefaultCrewManager-Komponente existiert für Stamm-Crew-Pflege mit listDefaultCrew + upsertDefaultCrewEntry + deleteDefaultCrewEntry (per D-04)"
    - "AnimeContributionModal.tsx nutzt member_id statt fansub_group_member_id (per D-01)"
    - "AnimeContributionModal role_codes kann >1 Eintrag enthalten; UI-Element für Mehrfachauswahl (Checkboxen oder Multi-Select via @/components/ui) ist vorhanden — keine 1:1-Beschränkung (per D-05)"
    - "openAnimeContributions in page.tsx ruft listUnifiedGroupMembers statt listGroupMembers auf (per D-02)"
    - "page.tsx-Zeilenanzahl nach allen Änderungen ≤ 450 ODER Filterchip-Logik ist in AnimeReleasesFilterBar.tsx ausgelagert und page.tsx Restgröße ist dokumentiert (CLAUDE.md)"
    - "AnimeContributionModal.tsx ≤ 450 Zeilen (CLAUDE.md)"
    - "DefaultCrewManager.tsx ≤ 450 Zeilen (CLAUDE.md)"
  artifacts:
    - path: "frontend/src/app/admin/fansubs/[id]/edit/page.tsx"
      provides: "Tab-Merge, Filterchips (via AnimeReleasesFilterBar), Badges, Einblick-Block, Matrix, Standard-Team-Button; MAIN_TABS ohne anime-projekte"
    - path: "frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionModal.tsx"
      provides: "member_id-Semantik; UnifiedGroupMember als Prop-Typ; Mehrfachrollen-UI"
    - path: "frontend/src/app/admin/fansubs/[id]/edit/AnimeReleasesFilterBar.tsx"
      provides: "Filterchip-Reihe ausgelagert; hält cockpitFilter-State"
    - path: "frontend/src/app/admin/fansubs/[id]/edit/DefaultCrewManager.tsx"
      provides: "Stamm-Crew-Pflege-UI; Standard-Team übernehmen-Button mit realem API-Call"
  key_links:
    - from: "page.tsx:parseMainTab"
      to: "releases"
      via: "anime-projekte case"
      pattern: "anime-projekte.*releases"
    - from: "page.tsx:openAnimeContributions"
      to: "api.ts:listUnifiedGroupMembers"
      via: "Promise.all"
      pattern: "listUnifiedGroupMembers\\("
    - from: "page.tsx releases-Tab"
      to: "AnimeProjectNoteWorkspace"
      via: "animeExpanded && ..."
      pattern: "AnimeProjectNoteWorkspace"
    - from: "DefaultCrewManager:applyDefaultCrew"
      to: "api.ts:applyDefaultCrew"
      via: "onClick Handler"
      pattern: "applyDefaultCrew\\("
---

<objective>
page.tsx chirurgisch umstellen: MAIN_TABS bereinigen, parseMainTab erweitern, Filterchips (in AnimeReleasesFilterBar.tsx auslagern um Zeilenlimit einzuhalten), Projektkarten-Kopfzeile um ProjectCockpitBadges erweitern, aufgeklappten Projektbereich um AnimeProjectNoteWorkspace erweitern, CoverageMatrix einbauen, Standard-Team-Button verdrahten. AnimeContributionModal auf member_id-Semantik, UnifiedGroupMember und explizite Mehrfachrollen-UI umstellen. DefaultCrewManager-Komponente für Stamm-Crew-Pflege anlegen.

Purpose: Das fertige Projekt-Cockpit wird für den Fansubleader sichtbar. Alle Entscheidungen D-01, D-02, D-04, D-05, D-07, D-10, D-11, D-12, D-13 landen in der Oberfläche. Kein toter Button.
Output: Vollständig verdrahtetes Cockpit im Tab „Anime & Veröffentlichungen"; tab=anime-projekte leitet auf releases um; Standard-Team übernehmen ist voll funktionsfähig.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-CONTEXT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-UI-SPEC.md
@C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-PATTERNS.md
@C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-04-SUMMARY.md

<interfaces>
<!-- Schlüssel-Stellen in page.tsx (RESEARCH-Befunde Z. 119–200) -->

MAIN_TABS (Z. 190–199) — zu entfernendes Element:
  { key: "anime-projekte", label: "Anime-Einblicke" }

SectionKey-Union (Z. 122–133) — "anime-projekte" entfernen

parseMainTab (Z. 201–204) — neuer Case:
  if (value === "anime-projekte") return "releases";

Access-Guard (Z. 238–240) — Case "anime-projekte" entfernen

Formular-Ausblendung (Z. 2266–2269) — activeMainTab !== "anime-projekte" entfernen

Projektkarte-Render (Z. 2706–3279):
  article .fansubEditAnimeReleaseCard
    div .fansubEditAnimeReleaseHeaderRow
      button (Klapptrigger)  ← unverändert lassen
      [NEU: ProjectCockpitBadges]
      Button "Mitwirkende"   ← unverändert lassen
      [NEU: Button "Einblick" / "Einblick bearbeiten"]
    {animeExpanded && (
      [NEU: AnimeProjectNoteWorkspace before releases]
      bestehende Releases-Liste
    )}

Render-Case anime-projekte (Z. 3281–3286) — entfernen

openAnimeContributions (Z. 1736–1752):
  ALT: listGroupMembers(fansubID)
  NEU: listUnifiedGroupMembers(fansubID) → UnifiedGroupMember[]

<!-- AnimeContributionModal.tsx Schlüssel-Stellen (PATTERNS Z. 449–506) -->
Member-Prop-Typ: members: HistFansubGroupMember[] → UnifiedGroupMember[]
State-Initialisierung: contribution.fansub_group_member_id → contribution.member_id
Upsert-Call: fansub_group_member_id: memberId → member_id: memberId
Member-Iteration: member.id → member.member_id als key
Rollen-Auswahl: role_codes string[] muss Mehrfachauswahl erlauben
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: page.tsx — Tab-Merge, Filterbar auslagern, Badges, Einblick-Block, Matrix verdrahten</name>
  <files>
    frontend/src/app/admin/fansubs/[id]/edit/page.tsx
    frontend/src/app/admin/fansubs/[id]/edit/AnimeReleasesFilterBar.tsx
  </files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx (vollständig lesen; Fokus auf Z. 122–133 SectionKey, Z. 190–204 MAIN_TABS+parseMainTab, Z. 238–240 Access-Guard, Z. 1736–1752 openAnimeContributions, Z. 2266–2269 Formular-Ausblendung, Z. 2706–3286 Releases-Tab-Render)
    - C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-PATTERNS.md (page.tsx-Patterns Z. 376–445)
    - C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-UI-SPEC.md (Filterchip-Reihe Z. 135–143, Projektkarte Z. 145–169, Tab-Routing Z. 222–226)
  </read_first>
  <action>
    AnimeReleasesFilterBar.tsx — neue Datei ('use client') auslagern:
    Grund: page.tsx ist bereits sehr groß (>3000 Zeilen). Filterchip-Logik + State muss ausgelagert werden, damit page.tsx das 450-Zeilen-Limit nicht durch die Ergänzungen weiter verletzt (CLAUDE.md). Die Ausgelagerte Datei hält den cockpitFilter-State und gibt activeFilter als Callback nach oben.
    Props: type AnimeReleasesFilterBarProps = { onFilterChange: (filter: 'all' | 'no-contributions' | 'no-note') => void; activeFilter: 'all' | 'no-contributions' | 'no-note' }
    Render: drei Button variant="ghost" size="sm" als Filterchips: "Alle", "Mitwirkende fehlen", "Einblick fehlt". Aktiver Chip erhält zusätzliche CSS-Klasse oder data-active-Attribut für Highlight. Chip "Offene Punkte" wird NICHT gerendert (D-12, UI-SPEC A5). Zielgröße ~30 Zeilen.

    Änderungen in page.tsx (chirurgisch, kein komplettes Rewrite):

    1. SectionKey-Union (Z. ~122–133): "anime-projekte" aus dem Union-Typ entfernen. Alle sectionOpenState-Initialisierungen mit "anime-projekte" ebenfalls entfernen.

    2. MAIN_TABS (Z. ~190–199): { key: "anime-projekte", label: "Anime-Einblicke" } entfernen.

    3. parseMainTab (Z. ~201–204): Case hinzufügen: if (value === "anime-projekte") return "releases"; — vor dem bestehenden MAIN_TABS.some()-Check einfügen.

    4. Access-Guard (Z. ~238–240): Case "anime-projekte" aus dem switch/if-Zweig entfernen.

    5. Formular-Ausblendung (Z. ~2266): activeMainTab !== "anime-projekte"-Check entfernen.

    6. Anime-Projekte-Render-Case (Z. ~3281–3286): Den Block { activeMainTab === "anime-projekte" ? <AnimeProjectNotesSection .../> : null } entfernen.

    7. Imports: ProjectCockpitBadges, AnimeProjectNoteWorkspace, CoverageMatrix, AnimeReleasesFilterBar importieren. listUnifiedGroupMembers aus @/lib/api importieren. AnimeProjectNotesSection-Import entfernen (kein Main-Tab mehr; prüfen ob noch anderweitig genutzt).

    8. openAnimeContributions (Z. ~1736–1752): Promise.all: listGroupMembers(fansubID) → listUnifiedGroupMembers(fansubID). Typen-Anpassung: contributionMembers-State-Typ von HistFansubGroupMember[] auf UnifiedGroupMember[] ändern.

    9. Filterchip-Reihe: cockpitFilter-State in page.tsx anlegen: const [cockpitFilter, setCockpitFilter] = useState<'all' | 'no-contributions' | 'no-note'>('all'). AnimeReleasesFilterBar als Komponente rendern: <AnimeReleasesFilterBar activeFilter={cockpitFilter} onFilterChange={setCockpitFilter} />.

    10. Projektkarte-Kopfzeile: ProjectCockpitBadges als additives Element einfügen. Props: contributionCount (aus geladenen Contributions wenn vorhanden, sonst 0), note={noteByAnimeId?.[animeId]} (lazy, undefined bis geladen). KEIN episodeCount-Prop (D-12, kein Fake-Status).

    11. Aufgeklappter Projektbereich: AnimeProjectNoteWorkspace mit fansubId={fansubId} animeId={animeId} expanded={animeExpanded} vor der Releases-Liste einbauen.

    12. CoverageMatrix: Oberhalb der Projektliste (unter Filterbar) oder in einem ausklappbaren Abschnitt CoverageMatrix mit roles={catalogRoles} rows={coverageRows} onCellClick={(animeId, roleCode) => openAnimeContributions(animeId)} rendern. catalogRoles und coverageRows aus State — catalogRoles aus der role_definitions-API (falls vorhanden) ODER aus ANIME_CONTRIBUTION_ROLES als Fallback (bereits in page.tsx); coverageRows werden aus den geladenen Contribution-Daten berechnet.

    13. Filterlogik: releaseGroups durch cockpitFilter filtern — 'no-contributions': nur Gruppen ohne Contributions (contributionCount === 0), 'no-note': nur Gruppen wo note === null (nach lazy load). Filter greift wenn Daten geladen sind.

    14. Datei-Limit page.tsx ZWINGEND prüfen: `wc -l frontend/src/app/admin/fansubs/[id]/edit/page.tsx` nach Änderungen ausführen. Wenn > 450 Zeilen: Weitere Auslagerung in AnimeReleasesContent.tsx oder CockpitContent.tsx — konkret: die komplette Renders-Schleife (releaseGroups.map) auslagern. Zeilen-Anzahl nach Auslagern in acceptance_criteria dokumentieren.
  </action>
  <verify>
    <automated>cd "C:/Users/admin/Documents/Team4s/frontend" && npm run typecheck 2>&1 | grep "page.tsx" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "'anime-projekte'" frontend/src/app/admin/fansubs/[id]/edit/page.tsx → maximal 1 Treffer (der neue Legacy-Redirect-Case in parseMainTab)
    - MAIN_TABS enthält kein { key: "anime-projekte" } Element mehr
    - parseMainTab('anime-projekte') === 'releases' (Logik verifizierbar aus Code)
    - ProjectCockpitBadges und AnimeProjectNoteWorkspace werden in der Projektkarte gerendert
    - CoverageMatrix wird im releases-Tab-Render mit roles-Prop gerendert
    - listUnifiedGroupMembers wird in openAnimeContributions aufgerufen
    - AnimeReleasesFilterBar.tsx existiert ≤ 450 Zeilen
    - page.tsx nach allen Änderungen: wc -l ergibt ≤ 450 Zeilen ODER erzwungene Auslagerung ist durchgeführt und Restgröße dokumentiert
    - Keine TypeScript-Fehler in page.tsx durch die Änderungen
    - KEIN episodeCount-Prop an ProjectCockpitBadges (grep "episodeCount" page.tsx → 0 Treffer)
  </acceptance_criteria>
  <done>Tab-Merge abgeschlossen; Cockpit-UI in page.tsx verdrahtet; Filterbar und CoverageMatrix eingebaut; Legacy-Routing funktioniert</done>
</task>

<task type="auto">
  <name>Task 2: AnimeContributionModal.tsx + DefaultCrewManager.tsx — member_id, Mehrfachrollen, Standard-Team</name>
  <files>
    frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionModal.tsx
    frontend/src/app/admin/fansubs/[id]/edit/DefaultCrewManager.tsx
  </files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionModal.tsx (vollständig; State-Initialisierung Z. 76–92, Member-Prop-Typ Z. 37, Upsert-Call Z. 187–197, Member-Iteration in Render, Rollen-Auswahl-UI)
    - frontend/src/types/fansub.ts (UnifiedGroupMember-Interface, DefaultCrewEntry, AnimeContribution.member_id — nach Plan 03)
    - C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-PATTERNS.md (AnimeContributionModal-Pattern Z. 449–506)
    - C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-UI-SPEC.md (Mitwirkenden-Zuordnung Z. 173–178, D-05 Mehrere Rollen pro Person)
  </read_first>
  <action>
    AnimeContributionModal.tsx — gezielte Anpassungen:

    1. Props-Typ: members: HistFansubGroupMember[] → members: UnifiedGroupMember[]. Import: UnifiedGroupMember aus @/types/fansub importieren. HistFansubGroupMember-Import entfernen wenn nicht mehr anderweitig benötigt.

    2. State-Initialisierung aus existingContributions (Z. ~76–92):
       - ids.add(contribution.fansub_group_member_id) → ids.add(contribution.member_id)
       - roles[contribution.fansub_group_member_id] → roles[contribution.member_id]
       - Alle anderen Stellen die contribution.fansub_group_member_id lesen → contribution.member_id

    3. Member-Iteration in der Render-Liste:
       - key={member.id} → key={member.member_id}
       - Vergleiche wie selectedMemberIds.has(member.id) → selectedMemberIds.has(member.member_id)
       - Anzeige: member.nickname oder member.display_name → member.display_name (UnifiedGroupMember hat display_name)
       - has_app_account-Badge: wenn member.source === 'app' → kleiner Badge "Konto" aus @/components/ui (optional, einfach halten)

    4. Upsert-Call (Z. ~187–197):
       - await upsertAnimeContribution(fansubId, animeId, { fansub_group_member_id: memberId, ... })
         → await upsertAnimeContribution(fansubId, animeId, { member_id: memberId, ... })
       - memberId kommt aus der Member-Schleife über member.member_id

    5. D-05 (Mehrere Rollen pro Person) — ZWINGEND, keine bedingte Formulierung:
       Das Modal MUSS mehrere Rollen pro Person zulassen. role_codes: string[] muss >1 Eintrag enthalten können.
       Implementierung: Für jede Person eine Gruppe von Checkboxen (eine pro ANIME_CONTRIBUTION_ROLE); jede Checkbox ist eine eigenständige Rolle; gecheckte Rollen ergeben role_codes[]. KEIN Single-Select, KEIN Dropdown mit Einfachauswahl. Verwende FormField + ggf. eigene Checkbox-Gruppe via @/components/ui; falls @/components/ui kein Checkbox-Primitiv hat, ist eine native <input type="checkbox"> innerhalb eines FormField erlaubt (da kein Primitiv-Äquivalent vorhanden). Die UI muss visuell klar machen, dass mehrere Rollen wählbar sind (Beschriftung "Rollen" über der Gruppe, nicht "Rolle").

    6. ANIME_CONTRIBUTION_ROLES (Z. ~21–23): statische Liste beibehalten; filtert bereits fansub_lead + founder heraus (D-09). Keine Umstellung auf API nötig für V1.

    7. Datei-Limit: wc -l nach Änderungen. Wenn > 450 Zeilen → AnimeContributionMemberCard in eigene Datei auslagern.

    DefaultCrewManager.tsx — neue Datei ('use client'):
    Zweck: Stamm-Crew-Pflege (D-04). Minimal über das Mitwirkenden-Muster; nutzt @/components/ui-Primitives und die fünf api.ts-Helper aus Plan 03.

    Props: fansubId: number, members: UnifiedGroupMember[]

    State: const [crew, setCrew] = useState<DefaultCrewEntry[]>([]) + const [loading, setLoading] = useState(false) + const [applying, setApplying] = useState(false) + const [applyResult, setApplyResult] = useState<string | null>(null)

    useEffect beim Mount: listDefaultCrew(fansubId) → setCrew(entries)

    Render:
    - SectionHeader "Standard-Team" aus @/components/ui
    - Tabelle der bestehenden Crew-Einträge: für jeden DefaultCrewEntry → Zeile mit member.display_name (aus members-Prop via member_id-Lookup), role_code als Badge, Button variant="ghost" size="sm" "Entfernen" → deleteDefaultCrewEntry(fansubId, entry.member_id, entry.role_code) → setCrew(crew.filter(...))
    - Formular zum Hinzufügen: Select (Mitglied aus members) + Select (Rolle aus ANIME_CONTRIBUTION_ROLES) + Button "Hinzufügen" → upsertDefaultCrewEntry → crew neu laden
    - Button variant="primary" size="sm" "Standard-Team übernehmen" → onClick: setApplying(true); applyDefaultCrew(fansubId); → setApplyResult(`${result.applied_count} Contributions angelegt`); setApplying(false). Loading-Feedback während applying. Fehlerbehandlung mit setApplyResult("Fehler: ...").
    - EmptyState wenn crew.length === 0: "Noch kein Standard-Team konfiguriert."
    - Datei-Limit: Zielgröße ~150–200 Zeilen, max 450.
    - KEINE nativen <select>/<button> für Elemente mit Primitiv-Äquivalent.
  </action>
  <verify>
    <automated>cd "C:/Users/admin/Documents/Team4s/frontend" && npm run typecheck 2>&1 | grep -E "AnimeContributionModal|DefaultCrewManager" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - AnimeContributionModal empfängt members: UnifiedGroupMember[] (kein HistFansubGroupMember[] mehr)
    - grep "fansub_group_member_id" frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionModal.tsx → 0 Treffer
    - State-Initialisierung und Upsert-Call nutzen contribution.member_id
    - Member-Iteration nutzt member.member_id als key und für Vergleiche
    - D-05 Mehrfachrollen: Rolle-Auswahl-UI ist eine Checkbox-Gruppe (NICHT ein Dropdown mit Einfachauswahl); grep "type=\"checkbox\"" AnimeContributionModal.tsx → mindestens 1 Treffer ODER grep "Checkboxen\|multi" → Kommentar im Code; role_codes[] kann >1 Eintrag enthalten (State-Typ ist string[])
    - AnimeContributionModal.tsx ≤ 450 Zeilen (wc -l ≤ 450)
    - ANIME_CONTRIBUTION_ROLES filtert weiterhin fansub_lead + founder heraus (D-09)
    - DefaultCrewManager.tsx existiert und exportiert eine Komponente
    - DefaultCrewManager ruft applyDefaultCrew auf onClick "Standard-Team übernehmen" (kein toter Button — grep "applyDefaultCrew" DefaultCrewManager.tsx → Treffer)
    - DefaultCrewManager.tsx nutzt ausschließlich @/components/ui-Primitives für Buttons und Selects
    - DefaultCrewManager.tsx ≤ 450 Zeilen
    - npm run typecheck ohne neue Fehler durch Modal und DefaultCrewManager
  </acceptance_criteria>
  <done>AnimeContributionModal nutzt member_id, UnifiedGroupMember und Checkbox-Mehrfachrollen; DefaultCrewManager mit verdrahtetem Standard-Team-übernehmen-Button</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Browser → /unified-members | Alle Memberdaten fließen durch den permission-gesicherten Endpoint |
| Browser → /default-crew/apply | Apply-Mutation muss permission-gesichert sein (Plan 82-02); Frontend sendet nur fansubId aus State |
| Filterchip-State | Clientseitige Filterung; Backend liefert immer alle Daten; kein Security-Risiko |
| Legacy-Redirect parseMainTab | Nur URL-Routing; keine Berechtigungsänderung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-05-01 | Elevation of Privilege | Legacy ?tab=anime-projekte Redirect | accept | parseMainTab leitet auf releases; keine erweiterten Rechte durch Redirect; Releases-Tab hat eigenen canUseMainTab-Guard |
| T-82-05-02 | Information Disclosure | openAnimeContributions ruft listUnifiedGroupMembers auf | mitigate | authorizedFetch mit Token; Backend permission-geblockt (Plan 82-02); Daten nur für berechtigte Leader |
| T-82-05-03 | Spoofing | member_id in AnimeContributionModal.upsert | mitigate | Backend Cross-Group-Guard (Plan 82-02) ist maßgebliche Sicherung; Frontend kann keine ID erfinden die der Guard durchlässt |
| T-82-05-04 | Elevation of Privilege | DefaultCrewManager.applyDefaultCrew — Contributions für fremde Gruppe | mitigate | fansubId kommt aus page.tsx-State (URL-Param, authentifiziert); Backend-Permission-Guard MembersManage (Plan 82-02) blockt Nicht-Leader |
| T-82-05-SC | Tampering | npm/pip/cargo installs | accept | keine Package-Installs in diesem Plan |
</threat_model>

<verification>
Nach beiden Tasks:
1. `npm run typecheck` → keine Fehler in page.tsx, AnimeContributionModal.tsx, DefaultCrewManager.tsx, AnimeReleasesFilterBar.tsx
2. `grep "'anime-projekte'" frontend/src/app/admin/fansubs/[id]/edit/page.tsx` → genau 1 Treffer (parseMainTab-Case)
3. `grep "listUnifiedGroupMembers" frontend/src/app/admin/fansubs/[id]/edit/page.tsx` → ≥ 1 Treffer
4. `grep "AnimeProjectNoteWorkspace" frontend/src/app/admin/fansubs/[id]/edit/page.tsx` → ≥ 1 Treffer
5. `grep "ProjectCockpitBadges" frontend/src/app/admin/fansubs/[id]/edit/page.tsx` → ≥ 1 Treffer
6. `grep "CoverageMatrix" frontend/src/app/admin/fansubs/[id]/edit/page.tsx` → ≥ 1 Treffer
7. `grep "fansub_group_member_id" frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionModal.tsx` → 0 Treffer
8. `grep "applyDefaultCrew" frontend/src/app/admin/fansubs/[id]/edit/DefaultCrewManager.tsx` → ≥ 1 Treffer
9. `grep "episodeCount" frontend/src/app/admin/fansubs/[id]/edit/page.tsx` → 0 Treffer
10. `wc -l frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionModal.tsx` → ≤ 450
11. `wc -l frontend/src/app/admin/fansubs/[id]/edit/DefaultCrewManager.tsx` → ≤ 450
</verification>

<success_criteria>
- tab=anime-projekte leitet sauber auf releases um (Legacy-Redirect)
- Filterchips sichtbar (via AnimeReleasesFilterBar); Badges in Projektkarte; Einblick-Block im aufgeklappten Bereich
- CoverageMatrix im releases-Tab mit katalog-getriebenen Spalten
- Standard-Team übernehmen ist verdrahtet und gibt Feedback
- AnimeContributionModal nutzt UnifiedGroupMember, member_id und Checkbox-Mehrfachrollen
- npm run typecheck fehlerfrei
- Alle neuen/geänderten Produktionsdateien ≤ 450 Zeilen; page.tsx ≤ 450 Zeilen oder dokumentierte Auslagerung
</success_criteria>

<output>
Create `.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-05-SUMMARY.md` when done
</output>
