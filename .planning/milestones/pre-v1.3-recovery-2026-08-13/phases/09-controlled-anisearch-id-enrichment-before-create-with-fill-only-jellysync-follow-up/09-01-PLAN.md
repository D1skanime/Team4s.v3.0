---
phase: 09-controlled-anisearch-id-enrichment-before-create-with-fill-only-jellysync-follow-up
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - shared/contracts/openapi.yaml
  - frontend/src/types/admin.ts
  - frontend/src/lib/api/admin-anime-intake.ts
  - frontend/src/lib/api/admin-anime-intake.test.ts
  - backend/internal/models/admin_content.go
  - backend/internal/handlers/admin_content_handler.go
  - backend/internal/handlers/admin_content_test.go
autonomous: true
requirements: [ENR-01, ENR-02]
must_haves:
  truths:
    - "The phase has explicit requirement IDs and traceability instead of a roadmap placeholder."
    - "The codebase exposes one exact-ID AniSearch create-time enrichment contract and no AniSearch search endpoint."
    - "Frontend and backend share the same request and response shapes for enrichment, duplicate redirect, and fill-only draft payloads."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 09 goal, requirement IDs, dependency, and plan inventory"
      contains: "ENR-01, ENR-02, ENR-03, ENR-04, ENR-05"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirement definitions and traceability rows for Phase 09"
      contains: "### Create-Time Enrichment"
    - path: "shared/contracts/openapi.yaml"
      provides: "Documented admin AniSearch create-time enrichment endpoint and response variants"
      contains: "/api/v1/admin/anime/enrichment/anisearch"
    - path: "frontend/src/types/admin.ts"
      provides: "Shared TypeScript request and response contracts for AniSearch create-time enrichment"
      contains: "AdminAnimeAniSearchEnrichment"
    - path: "backend/internal/models/admin_content.go"
      provides: "Go response models aligned with the enrichment contract"
      contains: "AniSearch"
  key_links:
    - from: "frontend/src/lib/api/admin-anime-intake.ts"
      to: "shared/contracts/openapi.yaml"
      via: "exact endpoint path and response shape"
      pattern: "admin/anime/enrichment/anisearch"
    - from: "backend/internal/handlers/admin_content_handler.go"
      to: "backend/internal/models/admin_content.go"
      via: "request and response struct alignment"
      pattern: "AniSearch"
---

<objective>
Resolve the Phase 09 roadmap and requirement-mapping debt, then define the exact create-time AniSearch contract that later plans will implement.

Purpose: Executors need stable requirement IDs and shared request/response contracts before backend and frontend work can proceed without interpretation.
Output: Updated roadmap and requirements traceability, plus documented backend/frontend AniSearch enrichment types and endpoint contract.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-controlled-anisearch-id-enrichment-before-create-with-fill-only-jellysync-follow-up/09-CONTEXT.md
@shared/contracts/openapi.yaml
@frontend/src/types/admin.ts
@frontend/src/lib/api/admin-anime-intake.ts
@backend/internal/models/admin_content.go
@backend/internal/handlers/admin_content_handler.go

<interfaces>
From `frontend/src/types/admin.ts`:
```ts
export interface AdminAnimeCreateRequest {
  title: string
  type: AnimeType
  content_type: ContentType
  status: AnimeStatus
  title_de?: string
  title_en?: string
  year?: number
  max_episodes?: number
  genre?: string
  description?: string
  cover_image?: string
  source?: string
  folder_name?: string
}
```

From `backend/internal/handlers/admin_content_handler.go`:
```go
type adminAnimeCreateRequest struct {
  Title       string  `json:"title"`
  TitleDE     *string `json:"title_de"`
  TitleEN     *string `json:"title_en"`
  Type        string  `json:"type"`
  ContentType string  `json:"content_type"`
  Status      string  `json:"status"`
  Year        *int16  `json:"year"`
  MaxEpisodes *int16  `json:"max_episodes"`
  Genre       *string `json:"genre"`
  Description *string `json:"description"`
  CoverImage  *string `json:"cover_image"`
  Source      *string `json:"source"`
  FolderName  *string `json:"folder_name"`
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve Phase 09 requirement mapping and roadmap debt</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>@.planning/ROADMAP.md @.planning/REQUIREMENTS.md @.planning/phases/09-controlled-anisearch-id-enrichment-before-create-with-fill-only-jellysync-follow-up/09-CONTEXT.md</read_first>
  <acceptance_criteria>Phase 09 has named requirement IDs mapped in both roadmap and requirements traceability; each ID reflects the locked decisions D-01 through D-25; no deferred AniSearch search/crawl ideas are introduced.</acceptance_criteria>
  <action>Add a `Create-Time Enrichment` requirement section to `.planning/REQUIREMENTS.md` with `ENR-01` through `ENR-05`, covering exact-ID AniSearch loading, rate limiting, duplicate redirect, strict `manual > AniSearch > Jellysync` merge priority, and resolvable-only relation import per D-01 through D-25. Update `.planning/ROADMAP.md` so Phase 09 no longer says `TBD`, lists the new requirement IDs, records the real phase goal, and keeps execution dependency on the verified Phase 07 seam rather than on unrelated Phase 08 replace/delete cleanup work.</action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path .planning/ROADMAP.md,.planning/REQUIREMENTS.md -Pattern 'ENR-01|ENR-02|ENR-03|ENR-04|ENR-05' | Measure-Object | % Count"</automated>
  </verify>
  <done>Roadmap and requirements both mention ENR-01 through ENR-05, and Phase 09 is executable instead of placeholder-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define the exact AniSearch create-time enrichment contract across docs and code</name>
  <files>shared/contracts/openapi.yaml, frontend/src/types/admin.ts, frontend/src/lib/api/admin-anime-intake.ts, frontend/src/lib/api/admin-anime-intake.test.ts, backend/internal/models/admin_content.go, backend/internal/handlers/admin_content_handler.go, backend/internal/handlers/admin_content_test.go</files>
  <read_first>@shared/contracts/openapi.yaml @frontend/src/types/admin.ts @frontend/src/lib/api/admin-anime-intake.ts @backend/internal/models/admin_content.go @backend/internal/handlers/admin_content_handler.go</read_first>
  <behavior>
    - Test 1: AniSearch enrichment is exposed as one exact-ID endpoint and client helper, not a free-text AniSearch search API.
    - Test 2: Backend create validation accepts normalized `anisearch:{id}` provenance alongside existing `jellyfin:{id}` provenance.
    - Test 3: The documented response contract can represent either a draft enrichment payload or a duplicate redirect target.
  </behavior>
  <acceptance_criteria>The OpenAPI contract, Go models, and TypeScript types all describe the same create-time AniSearch request/response shapes; backend validation explicitly normalizes `anisearch:` sources; no AniSearch search or crawl route is documented.</acceptance_criteria>
  <action>Add contract types for `AdminAnimeAniSearchEnrichmentRequest`, `AdminAnimeAniSearchEnrichmentResponse`, and a duplicate redirect payload to `frontend/src/types/admin.ts` and `backend/internal/models/admin_content.go`. Extend `backend/internal/handlers/admin_content_handler.go` and `backend/internal/handlers/admin_content_test.go` so create validation accepts `anisearch:{id}` in addition to `jellyfin:{id}`. Update `frontend/src/lib/api/admin-anime-intake.ts`, its test file, and `shared/contracts/openapi.yaml` to expose a single `POST /api/v1/admin/anime/enrichment/anisearch` helper returning either `mode=draft` or `mode=redirect` per D-01, D-04, D-05, D-09, and D-10. Do not add any AniSearch title search endpoint, fuzzy matching, or crawler contract.</action>
  <verify>
    <automated>cd backend && go test ./internal/handlers -run "TestValidateAdminAnimeCreateRequest_(AcceptsNormalizedAniSearchSource|RejectsMalformedAniSearchSource)" -count=1</automated>
  </verify>
  <done>The repo exposes one documented AniSearch create-time enrichment contract, `anisearch:` provenance is validated, and no search-style AniSearch API leaks into the contract surface.</done>
</task>

</tasks>

<verification>
Phase 09 traceability is in place, and the shared contract surface is ready for backend and frontend implementation without ad-hoc schema invention.
</verification>

<success_criteria>
- Phase 09 requirement IDs exist and are mapped in roadmap plus requirements docs.
- One exact-ID AniSearch enrichment endpoint is documented and typed on both backend and frontend.
- `anisearch:` provenance is treated as a valid create-time source string while free search remains absent.
</success_criteria>

<output>
After completion, create `.planning/phases/09-controlled-anisearch-id-enrichment-before-create-with-fill-only-jellysync-follow-up/09-01-SUMMARY.md`
</output>
