---
phase: 102-fansubprojekte-ui-schrittweise-verbessern
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/anime/[id]/group/[groupId]/page.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/projectPageData.ts
  - frontend/src/app/anime/[id]/group/[groupId]/ProjectPage.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/page.test.tsx
autonomous: false
requirements_addressed:
  - D-01
  - D-02
  - D-05
  - D-07
  - D-21
must_haves:
  truths:
    - "The technical route `/anime/[id]/group/[groupId]` still renders the same public Fansub project after extraction."
    - "Project data loading exists in one shared server-side module that later routes can reuse."
    - "No new public API, media endpoint, upload flow, or schema migration is introduced."
  artifacts:
    - path: frontend/src/app/anime/[id]/group/[groupId]/projectPageData.ts
      provides: "Shared project detail loader and typed loaded-data shape"
    - path: frontend/src/app/anime/[id]/group/[groupId]/ProjectPage.tsx
      provides: "Shared public Fansub project page composition"
    - path: frontend/src/app/anime/[id]/group/[groupId]/page.tsx
      provides: "Compatibility route wrapper"
  key_links:
    - from: frontend/src/app/anime/[id]/group/[groupId]/page.tsx
      to: frontend/src/app/anime/[id]/group/[groupId]/projectPageData.ts
      via: "load by numeric anime/group IDs"
      pattern: "loadPublicFansubProjectPageData"
    - from: frontend/src/app/anime/[id]/group/[groupId]/page.tsx
      to: frontend/src/app/anime/[id]/group/[groupId]/ProjectPage.tsx
      via: "shared render composition"
      pattern: "<ProjectPage"
---

<objective>
Extract the current public Fansub project detail page into reusable server-side composition without changing visible behavior.

Purpose: D-21 requires a pretty route, but D-07 forbids duplicating loaders and section composition. This plan creates the shared route substrate first so later plans can add `/fansubs/[fansubSlug]/fansubprojekt/[animeSlug]` without copy-paste.
Output: A shared loader module, shared page component, compatibility route wrapper, and focused tests proving the technical route still works.
</objective>

<context>
@.planning/phases/102-fansubprojekte-ui-schrittweise-verbessern/102-CONTEXT.md
@.planning/phases/102-fansubprojekte-ui-schrittweise-verbessern/102-RESEARCH.md
@.planning/phases/102-fansubprojekte-ui-schrittweise-verbessern/102-VALIDATION.md
@.planning/phases/102-fansubprojekte-ui-schrittweise-verbessern/102-PATTERNS.md
@docs/engineering/implementation-contract.md
@docs/api/api-contracts.md
@docs/architecture/db-schema-fansub-domain.md
</context>

<read_first>
- `frontend/src/app/anime/[id]/group/[groupId]/page.tsx` - current data loading, optional-section degradation, and section order.
- `frontend/src/app/anime/[id]/group/[groupId]/sections/HeroSection.tsx` - current hero props consumed by the page.
- `frontend/src/app/anime/[id]/group/[groupId]/page.test.tsx` - existing route/helper tests that must be adapted without preserving stale empty-summary behavior.
- `frontend/src/lib/api.ts` - existing public API helpers; do not add ad hoc fetches.
- `frontend/src/lib/groupNavigation.ts` - current same-Anime group navigation helper, useful only as an example of deterministic sorting style.
</read_first>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extract shared project page loader</name>
  <read_first>
  - `frontend/src/app/anime/[id]/group/[groupId]/page.tsx`
  - `frontend/src/lib/api.ts`
  - `frontend/src/types/group.ts`
  - `frontend/src/types/groupContributors.ts`
  </read_first>
  <files>frontend/src/app/anime/[id]/group/[groupId]/projectPageData.ts, frontend/src/app/anime/[id]/group/[groupId]/page.tsx, frontend/src/app/anime/[id]/group/[groupId]/page.test.tsx</files>
  <action>Create `projectPageData.ts` and move the current numeric route parsing, primary `getGroupDetail`/`getAnimeByID` loading, optional `getGroupReleases`, `getAnimeFansubs`, `getGroupContributors`, `getGroupProjectNote`, `getGroupThemes`, `getGroupReleaseMedia`, and `getGroupAssets` loading into exported typed helpers. Keep 404 behavior for invalid IDs and missing primary group/anime exactly as today. Keep optional section failures non-fatal. Update `page.tsx` so it only awaits params, calls the loader, handles `notFound`, and passes loaded data forward. Per D-07, reuse `frontend/src/lib/api.ts`; do not add a new fetch wrapper or API endpoint.</action>
  <verify>
    <automated>npm --prefix frontend run test -- src/app/anime/[id]/group/[groupId]/page.test.tsx</automated>
    <automated>npm --prefix frontend run typecheck</automated>
  </verify>
  <acceptance_criteria>
  - `projectPageData.ts` exports a loader whose name contains `loadPublicFansubProjectPageData`.
  - `page.tsx` imports the loader and no longer directly imports all public API helpers from `@/lib/api`.
  - Invalid numeric params still resolve through `notFound()` behavior.
  - No files under `backend/`, `database/`, or `shared/contracts/` are modified in this plan.
  </acceptance_criteria>
  <done>Shared loader exists, technical route delegates to it, tests pass, and no backend/contract/media files are touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract shared render composition</name>
  <read_first>
  - `frontend/src/app/anime/[id]/group/[groupId]/page.tsx`
  - `frontend/src/app/anime/[id]/group/[groupId]/sections/HeroSection.tsx`
  - `frontend/src/app/anime/[id]/group/[groupId]/sections/StorySection.tsx`
  - `frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.tsx`
  </read_first>
  <files>frontend/src/app/anime/[id]/group/[groupId]/ProjectPage.tsx, frontend/src/app/anime/[id]/group/[groupId]/page.tsx, frontend/src/app/anime/[id]/group/[groupId]/page.test.tsx</files>
  <action>Create `ProjectPage.tsx` as the single composition component for the existing page. Preserve current visible sections and old technical-route behavior in this extraction plan; do not remove `GroupSectionsNav`, `LatestReleaseSection`, `ThemesSection`, `MediaSection`, or the global empty summary until their dedicated cleanup plans. The only intended behavioral change is that the render tree is now reusable by the pretty route per D-21.</action>
  <verify>
    <automated>npm --prefix frontend run test -- src/app/anime/[id]/group/[groupId]/page.test.tsx</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
  - `ProjectPage.tsx` renders the existing `HeroSection`, team/story/releases/theme/media sections through props from the shared loader.
  - `page.tsx` renders `ProjectPage` and contains no duplicate section composition.
  - Tests assert the wrapper delegates to shared composition rather than encoding a second route implementation.
  </acceptance_criteria>
  <done>Shared composition exists, technical route renders through it, and focused tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live acceptance for extracted technical route</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/page.tsx, frontend/src/app/anime/[id]/group/[groupId]/ProjectPage.tsx, frontend/src/app/anime/[id]/group/[groupId]/projectPageData.ts</files>
  <action>Stop after the extraction and run the D-01 live acceptance check before any later slice starts. Present the automated check results and open `http://127.0.0.1:3000/anime/13/group/1` or the available seeded technical project route. The expected user-visible result is no deliberate visual/content change from before extraction; this checkpoint exists because D-01 requires acceptance after each implemented area before the next wave.</action>
  <what-built>Shared server-side loader and shared project-page composition for the existing technical Fansub project route.</what-built>
  <how-to-verify>
  1. Open `http://127.0.0.1:3000/anime/13/group/1` or the seeded equivalent.
  2. Confirm the Fansub project still renders through the technical route.
  3. Confirm no new route, media/upload behavior, or visible section redesign is introduced by this extraction slice.
  </how-to-verify>
  <resume-signal>Type `approved` or describe the route/content regression.</resume-signal>
  <verify>
    <automated>npm --prefix frontend run test -- src/app/anime/[id]/group/[groupId]/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
  - User acceptance is received before Plan 102-02 starts.
  - Any extraction regression found in the technical route is fixed and rechecked.
  </acceptance_criteria>
  <done>Technical-route extraction is accepted by the user or recorded as blocked.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public route params -> frontend server loader | Untrusted numeric route params become API requests. |
| frontend server loader -> public API | Public API responses are rendered into the public page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-01-01 | Tampering | `projectPageData.ts` param parsing | mitigate | Keep positive integer parsing and `notFound()` on invalid IDs. |
| T-102-01-02 | Information Disclosure | optional public data loads | mitigate | Reuse existing public API helpers and backend visibility filters; do not add client-only filtering. |
| T-102-01-03 | Tampering | media ownership | accept | This plan performs no media writes and introduces no upload/media endpoint. |
</threat_model>

<verification>
- `npm --prefix frontend run test -- src/app/anime/[id]/group/[groupId]/page.test.tsx`
- `npm --prefix frontend run typecheck`
- `git diff --check`
</verification>

<success_criteria>
- The technical project route remains available and render-compatible.
- Later plans can import one loader/composition instead of duplicating the existing page.
- `102-00-PLAN.md` remains unchanged.
</success_criteria>
