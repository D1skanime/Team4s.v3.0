---
phase: 102-fansubprojekte-ui-schrittweise-verbessern
plan: "05"
type: execute
wave: 5
depends_on:
  - "102-04"
files_modified:
  - backend/internal/repository/group_repository.go
  - backend/internal/repository/release_detail_public_repository.go
  - backend/internal/repository/group_repository_test.go
  - shared/contracts/openapi.yaml
  - frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.test.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/sections/OlderReleasesList.tsx
autonomous: false
requirements_addressed:
  - D-01
  - D-05
  - D-06
  - D-07
  - D-15
  - D-16
  - D-18
  - D-27
must_haves:
  truths:
    - "The public release section title is `Releases zum Fansub`."
    - "The standalone `Neuestes Release` block is not rendered."
    - "Public release labels do not expose raw imported/Jellyfin/file names such as `.mkv`."
  artifacts:
    - path: backend/internal/repository/group_repository.go
      provides: "Public group release list title/fallback query behavior"
    - path: backend/internal/repository/release_detail_public_repository.go
      provides: "Public release detail title/fallback query behavior"
    - path: shared/contracts/openapi.yaml
      provides: "Documented public release title semantics"
    - path: frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.tsx
      provides: "Conservative public release section composition"
  key_links:
    - from: backend/internal/repository/group_repository.go
      to: frontend/src/app/anime/[id]/group/[groupId]/sections/OlderReleasesList.tsx
      via: "public release title DTO"
      pattern: "Version"
    - from: frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.tsx
      to: frontend/src/app/anime/[id]/group/[groupId]/sections/OlderReleasesList.tsx
      via: "single conservative list"
      pattern: "Releases zum Fansub"
---

<objective>
Make the release section conservative and safe: retitle it, remove the special newest-release block, and enforce D-18 public title safety where current queries can leak raw names.

Purpose: D-15, D-16, D-18, and D-27 define a narrow release slice. The section should not be redesigned, but public labels must not expose physical/import names.
Output: Public release title safety in repository reads, release section retitle, latest block removal, and tests.
</objective>

<context>
@.planning/phases/102-fansubprojekte-ui-schrittweise-verbessern/102-CONTEXT.md
@.planning/phases/102-fansubprojekte-ui-schrittweise-verbessern/102-RESEARCH.md
@.planning/phases/102-fansubprojekte-ui-schrittweise-verbessern/102-PATTERNS.md
@docs/api/api-contracts.md
@docs/architecture/db-schema-fansub-domain.md
</context>

<read_first>
- `frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.tsx` - current latest + older release composition.
- `frontend/src/app/anime/[id]/group/[groupId]/sections/OlderReleasesList.tsx` - current `Weitere Releases` title and cursor list.
- `frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.test.tsx` - current latest-release mock expectations.
- `backend/internal/repository/group_repository.go` - current `COALESCE(rev.title, e.title)` public list title source.
- `backend/internal/repository/release_detail_public_repository.go` - public detail title fallback source.
- `docs/architecture/db-schema-fansub-domain.md` - release/version ownership rules.
- `shared/contracts/openapi.yaml` - public group release list/detail response title fields that must document the D-18 semantics.
</read_first>

<scope_note>
D-19 reduced member Basisdaten editing and D-20 version uniqueness are deliberately outside this public release-display cleanup. This plan must not implement, claim, or partially modify those admin/member mutation rules; it only handles D-18 public display title safety plus D-15/D-16/D-27 release-section presentation.
</scope_note>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Enforce public release-title fallback safety</name>
  <read_first>
  - `backend/internal/repository/group_repository.go`
  - `backend/internal/repository/release_detail_public_repository.go`
  - `backend/internal/repository/group_repository_test.go`
  - `shared/contracts/openapi.yaml`
  - `docs/architecture/db-schema-fansub-domain.md`
  </read_first>
  <files>backend/internal/repository/group_repository.go, backend/internal/repository/release_detail_public_repository.go, backend/internal/repository/group_repository_test.go, shared/contracts/openapi.yaml</files>
  <action>Add a narrow repository-level public title fallback that treats empty titles and raw technical filenames as not curated for public display. Public labels must use curated `release_versions.title` only when it is non-empty and does not match raw import patterns such as `.mkv`, `.mp4`, `.avi`, `.m2ts`, `.ass`, slash/backslash paths, or obvious bracketed file names. If no curated public name exists, build the fallback from episode title, Fansub group name, and the real `release_versions.version`, for example `Kanonenschuss (Honto) Version 1`. Apply the same helper/query expression to public group release list and public release detail reads. Update `shared/contracts/openapi.yaml` descriptions/examples for the affected public release list/detail `title` fields so the contract states that public titles are curated names or neutral fallbacks and never raw import/file names. Do not modify admin edit queries; D-18 allows admin UI to show physical/import names separately. D-19 and D-20 remain outside this plan.</action>
  <verify>
    <automated>cd backend; go test ./internal/repository -run "Test.*Public.*Release.*Title|Test.*Release.*Title.*Fallback"</automated>
    <automated>$contract = Get-Content -Raw shared/contracts/openapi.yaml; if ($contract -notmatch "raw import" -or $contract -notmatch "neutral fallback") { throw "OpenAPI release title fallback semantics missing" }</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
  - Backend test covers raw title `Vipers Creed. S01E02-CSubs.mkv` and expects a neutral fallback containing episode title, group name, and `Version 1`.
  - Backend test covers a curated title and expects the curated title unchanged.
  - OpenAPI public release `title` descriptions/examples document curated-or-neutral fallback semantics and forbid raw import/file names.
  - No migration file is added.
  - No admin handler or reduced Basisdaten mutation path is changed in this plan.
  </acceptance_criteria>
  <done>Public release-title reads block raw technical names and backend title fallback tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retitle release section and remove newest-release composition</name>
  <read_first>
  - `frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.tsx`
  - `frontend/src/app/anime/[id]/group/[groupId]/sections/OlderReleasesList.tsx`
  - `frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.test.tsx`
  - `frontend/src/components/ui/Button.tsx`
  </read_first>
  <files>frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.tsx, frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.test.tsx, frontend/src/app/anime/[id]/group/[groupId]/sections/OlderReleasesList.tsx</files>
  <action>Remove the standalone `LatestReleaseSection` render from the public project release section. Keep the existing cursor/list presentation conservative and retain `Mehr laden` as a normal global `Button`. Change the release section title to exact string `Releases zum Fansub`. Remove user-facing `Weitere Releases` and `Neuestes Release` from this page's release section. Do not redesign release cards, cooperation display, or version grouping beyond the title/latest-block cleanup per D-27.</action>
  <verify>
    <automated>npm --prefix frontend run test -- src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.test.tsx</automated>
    <automated>npm --prefix frontend run typecheck</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
  - Release test asserts exact heading `Releases zum Fansub`.
  - Release test asserts no `latest-release-mock` or `Neuestes Release` output.
  - Source grep in `frontend/src/app/anime/[id]/group/[groupId]/sections` does not find active user-facing `Weitere Releases`.
  - `OlderReleasesList` keeps cursor loading and scoped error text behavior.
  </acceptance_criteria>
  <done>Release section renders as `Releases zum Fansub`, no latest block is rendered, and frontend tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live acceptance for release section and public title safety</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.tsx, frontend/src/app/anime/[id]/group/[groupId]/sections/OlderReleasesList.tsx, backend/internal/repository/group_repository.go, backend/internal/repository/release_detail_public_repository.go, shared/contracts/openapi.yaml</files>
  <action>Stop after the release slice and perform D-01 live acceptance before Plan 102-06 starts. Verify that the visible release section is conservative, titled `Releases zum Fansub`, has no standalone newest-release block, and does not expose raw import/Jellyfin/file labels. If runtime data cannot demonstrate a raw-title case, record the backend test evidence and the live release labels that were inspected.</action>
  <what-built>D-18 public release-title fallback semantics, OpenAPI title documentation, and conservative release section retitle/latest-block removal.</what-built>
  <how-to-verify>
  1. Open the accepted pretty route or technical route at `http://127.0.0.1:3000`.
  2. Confirm the exact release heading `Releases zum Fansub`.
  3. Confirm `Neuestes Release` and `Weitere Releases` are absent.
  4. Inspect visible release labels and confirm they do not contain `.mkv`, `.mp4`, `.avi`, `.m2ts`, `.ass`, slash/backslash paths, or raw import/Jellyfin names.
  5. Confirm this slice did not add admin member Basisdaten editing or version uniqueness behavior.
  </how-to-verify>
  <resume-signal>Type `approved` or describe the release title/copy issue.</resume-signal>
  <verify>
    <automated>npm --prefix frontend run test -- src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.test.tsx</automated>
  </verify>
  <acceptance_criteria>
  - User acceptance is received before Plan 102-06 starts.
  - Live release labels or documented test evidence prove raw-title safety for this slice.
  </acceptance_criteria>
  <done>Release section/title-safety slice is accepted by the user or recorded as blocked.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release/version DB rows -> public DTO | Curated and physical/import release data can reach public reads. |
| public release DTO -> frontend list | Release title text is displayed on a public page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-05-01 | Information Disclosure | public release title queries | mitigate | Filter raw filename/import patterns and build neutral fallback from episode title, group name, version. |
| T-102-05-02 | Tampering | admin/member release Basisdaten | accept | No mutation path or permission path is touched in this read-only public display plan. |
| T-102-05-03 | Information Disclosure | release media ownership | accept | No media endpoints or media joins are added or changed. |
</threat_model>

<verification>
- `cd backend; go test ./internal/repository -run "Test.*Public.*Release.*Title|Test.*Release.*Title.*Fallback"`
- `npm --prefix frontend run test -- src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.test.tsx`
- `npm --prefix frontend run typecheck`
- `git diff --check`
</verification>

<success_criteria>
- The release section reads as `Releases zum Fansub` and remains structurally conservative.
- Raw physical/import names are blocked from public release labels.
- D-19/D-20 admin/member edit decisions are not claimed by this plan and remain outside Phase 102 public release-display cleanup.
</success_criteria>
