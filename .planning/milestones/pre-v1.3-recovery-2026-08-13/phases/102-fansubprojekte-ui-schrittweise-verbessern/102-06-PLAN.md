---
phase: 102-fansubprojekte-ui-schrittweise-verbessern
plan: "06"
type: execute
wave: 6
depends_on:
  - "102-05"
files_modified:
  - frontend/src/app/anime/[id]/group/[groupId]/ProjectPage.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/page.test.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/page.module.css
autonomous: false
requirements_addressed:
  - D-01
  - D-02
  - D-05
  - D-07
  - D-14
  - D-15
  - D-17
  - D-28
must_haves:
  truths:
    - "The section nav/jump list is gone from the Fansub project page."
    - "The global empty-area summary is gone."
    - "Standalone OP/ED/Middle and Medien sections are not rendered on this project page."
    - "No public API or media ownership structure is deleted."
  artifacts:
    - path: frontend/src/app/anime/[id]/group/[groupId]/ProjectPage.tsx
      provides: "Final flowing project-page section composition"
    - path: frontend/src/app/anime/[id]/group/[groupId]/page.test.tsx
      provides: "Source/behavior assertions for removed obsolete sections"
  key_links:
    - from: frontend/src/app/anime/[id]/group/[groupId]/ProjectPage.tsx
      to: frontend/src/app/anime/[id]/group/[groupId]/sections/ThemesSection.tsx
      via: "no standalone render"
      pattern: "ThemesSection"
    - from: frontend/src/app/anime/[id]/group/[groupId]/ProjectPage.tsx
      to: frontend/src/app/anime/[id]/group/[groupId]/sections/MediaSection.tsx
      via: "no standalone render"
      pattern: "MediaSection"
---

<objective>
Finish the page-flow cleanup by removing obsolete standalone project-page structures without deleting APIs or media/domain code.

Purpose: D-14, D-17, and D-28 define structural removals after the individual content slices are in place. This plan makes the page flow like the public Fansub profile while leaving later OP/ED/media integration to the release experience.
Output: Updated shared page composition and tests proving the removed copy/sections stay absent.
</objective>

<context>
@.planning/phases/102-fansubprojekte-ui-schrittweise-verbessern/102-CONTEXT.md
@.planning/phases/102-fansubprojekte-ui-schrittweise-verbessern/102-UI-SPEC.md
@.planning/phases/102-fansubprojekte-ui-schrittweise-verbessern/102-PATTERNS.md
@docs/architecture/db-schema-fansub-domain.md
</context>

<read_first>
- `frontend/src/app/anime/[id]/group/[groupId]/ProjectPage.tsx` - shared composition after Plans 01-05.
- `frontend/src/app/anime/[id]/group/[groupId]/page.tsx` - compatibility wrapper should stay minimal.
- `frontend/src/app/anime/[id]/group/[groupId]/GroupSectionsNav.tsx` - obsolete section navigation component for this page only.
- `frontend/src/app/anime/[id]/group/[groupId]/sections/ThemesSection.tsx` - standalone OP/ED/Middle section to unrender.
- `frontend/src/app/anime/[id]/group/[groupId]/sections/MediaSection.tsx` - standalone Medien section to unrender.
- `frontend/src/app/anime/[id]/group/[groupId]/page.test.tsx` - old `buildEmptyAreaLabels` tests to replace.
</read_first>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Remove obsolete project-page section surfaces</name>
  <read_first>
  - `frontend/src/app/anime/[id]/group/[groupId]/ProjectPage.tsx`
  - `frontend/src/app/anime/[id]/group/[groupId]/GroupSectionsNav.tsx`
  - `frontend/src/app/anime/[id]/group/[groupId]/sections/ThemesSection.tsx`
  - `frontend/src/app/anime/[id]/group/[groupId]/sections/MediaSection.tsx`
  </read_first>
  <files>frontend/src/app/anime/[id]/group/[groupId]/ProjectPage.tsx, frontend/src/app/anime/[id]/group/[groupId]/page.module.css</files>
  <action>Remove the project-page render/import of `GroupSectionsNav`. Remove the global empty summary render and any local `buildEmptyAreaLabels` use. Stop rendering standalone `ThemesSection` and `MediaSection` on the project page. Keep their files and public API helpers intact because D-28 removes them from this page only and later integration belongs to release experience work. Ensure missing optional sections are simply absent; do not add a new global empty state.</action>
  <verify>
    <automated>npm --prefix frontend run test -- src/app/anime/[id]/group/[groupId]/page.test.tsx</automated>
    <automated>npm --prefix frontend run typecheck</automated>
  </verify>
  <acceptance_criteria>
  - Source grep in `ProjectPage.tsx` finds no active `GroupSectionsNav`, `ThemesSection`, `MediaSection`, `emptySummary`, or `buildEmptyAreaLabels`.
  - User-facing copy `Weitere Bereiche sind noch nicht öffentlich befüllt` is absent from the project page source.
  - No files under `backend/`, `database/`, or `shared/contracts/` are modified in this plan.
  </acceptance_criteria>
  <done>Project page composition no longer renders section nav, global empty summary, standalone OP/ED/Middle, or standalone Medien.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace stale tests with absence assertions</name>
  <read_first>
  - `frontend/src/app/anime/[id]/group/[groupId]/page.test.tsx`
  - `frontend/src/app/anime/[id]/group/[groupId]/ProjectPage.tsx`
  </read_first>
  <files>frontend/src/app/anime/[id]/group/[groupId]/page.test.tsx</files>
  <action>Remove tests that enforce `buildEmptyAreaLabels` or the old global empty summary. Add tests or source assertions that the project page no longer imports/renders the section nav, standalone OP/ED/Middle, standalone Medien, or global empty summary. Keep tests focused; do not snapshot the full page.</action>
  <verify>
    <automated>npm --prefix frontend run test -- src/app/anime/[id]/group/[groupId]/page.test.tsx</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
  - Tests fail if `Weitere Bereiche sind noch nicht öffentlich befüllt`, `OP/ED/Middle`, or standalone `Medien` returns to the shared page composition.
  - Tests do not assert old section-nav labels `Geschichte`, `Beteiligte`, `Releases`, `OP/ED/Middle`, `Medien` as a jump list.
  </acceptance_criteria>
  <done>Stale tests are replaced by absence assertions for the new flowing page structure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live acceptance for final page-flow removals</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/ProjectPage.tsx, frontend/src/app/anime/[id]/group/[groupId]/page.test.tsx, frontend/src/app/anime/[id]/group/[groupId]/page.module.css</files>
  <action>Stop after the page-flow removal slice and perform D-01 live acceptance before final Plan 102-07 starts. Verify the project page now flows without the jump list, global empty summary, standalone OP/ED/Middle section, or standalone Medien section, while the accepted hero, story, member, and release slices still render.</action>
  <what-built>Final flowing Fansub project page composition without obsolete nav, empty-summary, OP/ED/Middle, or Medien standalone surfaces.</what-built>
  <how-to-verify>
  1. Open the accepted pretty route or technical route at `http://127.0.0.1:3000`.
  2. Confirm no section jump list with `Geschichte`, `Beteiligte`, `Releases`, `OP/ED/Middle`, `Medien` appears.
  3. Confirm no global empty summary text appears.
  4. Confirm standalone `OP/ED/Middle` and standalone `Medien` sections are absent.
  5. Confirm the accepted hero, story, members, and `Releases zum Fansub` sections still render when data exists.
  </how-to-verify>
  <resume-signal>Type `approved` or describe the missing/obsolete section issue.</resume-signal>
  <verify>
    <automated>npm --prefix frontend run test -- src/app/anime/[id]/group/[groupId]/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
  - User acceptance is received before Plan 102-07 starts.
  - Removed surfaces are checked live and accepted.
  </acceptance_criteria>
  <done>Page-flow removal slice is accepted by the user or recorded as blocked.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| optional public section data -> page composition | Optional data can be omitted without global summary. |
| media/theme public reads -> hidden page sections | Existing data sources remain reachable by their existing owners, but not as standalone project sections. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-06-01 | Information Disclosure | standalone theme/media sections | mitigate | Stop rendering these sections on the project page; do not add replacement media reads. |
| T-102-06-02 | Tampering | media ownership structures | accept | No media schema/API/delete changes; only page composition changes. |
| T-102-06-03 | Denial of Service | removed section nav | accept | Removing client navigation reduces surface; no runtime load is added. |
</threat_model>

<verification>
- `npm --prefix frontend run test -- src/app/anime/[id]/group/[groupId]/page.test.tsx`
- `npm --prefix frontend run typecheck`
- `git diff --check`
</verification>

<success_criteria>
- The project page flows through hero, story, members, and conservative release section without obsolete nav/summary/media/theme blocks.
- Removed standalone sections are not replaced by new API or media seams.
- Tests encode the new absence behavior.
</success_criteria>
