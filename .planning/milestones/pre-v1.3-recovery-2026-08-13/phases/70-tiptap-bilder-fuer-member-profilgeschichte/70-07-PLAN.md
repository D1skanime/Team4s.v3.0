---
phase: 70-tiptap-bilder-fuer-member-profilgeschichte
plan: 07
type: execute
wave: 5
depends_on: [70-03, 70-04, 70-05, 70-06]
files_modified:
  - .planning/ROADMAP.md
autonomous: false
requirements: [D-12, D-15, D-21, D-22, D-23, D-24]

must_haves:
  truths:
    - "go test ./... -count=1 und npm test vollstaendig gruen"
    - "Manueller Round-Trip bestätigt: Bild einfuegen, Breite/Ausrichtung setzen, Save, Reload — Bild, Breite, Ausrichtung identisch (D-21)"
    - "Cleanup-on-Save bestätigt: Bild entfernen + Save → Datei im /media-Store und media_assets-Zeile physisch weg (D-22)"
    - "IDOR bestätigt: fremde media_asset_id beim Save → 422 (D-23)"
    - "Sanitizing bestätigt: manipulierter body_html / fremde src / script-Tags werden verworfen (D-23)"
    - "Public-Profil bestätigt: Story-Bilder erscheinen korrekt in oeffentlichem Member-Profil (D-24)"
    - "/me/profile Lesemodus bestätigt: serverseitiges body_html enthaelt img-Tag mit korrekter Breite und Ausrichtung (D-12)"
    - "Upload-Fehler-Atomizitaet bestätigt: kein partielles body_json persistiert bei Batch-Upload-Fehler (D-06/D-07)"
    - "ROADMAP.md: SC1-Gap (kein Alt-Text) explizit dokumentiert, SC5 auf 'Cleanup-on-Save in Phase 70 umgesetzt' aktualisiert"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "SC1-Gap dokumentiert, SC5 als 'in Phase 70 implementiert' markiert"
      contains: "D-05"
  key_links:
    - from: "GET /api/v1/members/{slug}/profile"
      to: "body_html"
      via: "Gleiche serverseitige body_html wie /me/profile Lesemodus enthaelt img-Tags"
      pattern: "member_story_html"
    - from: "GET /api/v1/me/profile"
      to: "body_html"
      via: "/me/profile Lesemodus rendert serverseitig erzeugtes body_html mit img-Tags (D-12)"
      pattern: "member_story_html"
---

<objective>
Verifikations-Phase: Volle Test-Suite, manuelle UAT-Szenarien (Round-Trip, Cleanup, IDOR, Public-Profil, /me/profile Lesemodus, Upload-Fehler-Atomizitaet) und ROADMAP-Korrekturen (SC1-Gap, SC5-Override). Der Planer dokumentiert D-05 und D-13 als explizite Vertragsabweichungen in ROADMAP.md.

Purpose: Sicherstellen dass alle D-NN-Entscheidungen korrekt implementiert sind und ROADMAP-SC1/SC5 die User-Entscheidungen akkurat widerspiegeln.
Output: Gruene Test-Suiten, bestandene UAT-Szenarien, aktualisierte ROADMAP.md.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/ROADMAP.md
@C:/Users/admin/Documents/Team4s/.planning/phases/70-tiptap-bilder-fuer-member-profilgeschichte/70-CONTEXT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/70-tiptap-bilder-fuer-member-profilgeschichte/70-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Volle Test-Suite + ROADMAP.md-Korrekturen</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md — Phase 70 Success Criteria Section (lines 1438-1444): SC1 und SC5 lesen fuer exakte Aenderungsstellen
    - 70-CONTEXT.md — D-05 (Alt/Caption gestrichen), D-12 (/me/profile Lesemodus = Public-aehnliches Rendering aus serverseitigem body_html), D-13 (Cleanup-on-Save in Phase 70)
  </read_first>
  <action>
    Test-Suite ausfuehren und Ergebnisse dokumentieren:
    Backend: go test ./... -count=1
    Frontend: npm test

    Falls Tests fehlschlagen → Bugs identifizieren und beheben. Nicht in SUMMARY weitergehen bevor alle Tests gruen sind.

    ROADMAP.md-Korrekturen — Phase 70 Success Criteria:

    SC1 aktualisieren: Urspruenglicher Text "optional Alt-/Caption-Text" → ersetzen durch:
    "[Korrigiert D-05] `RichTextEditor` unterstuetzt fuer die Member-Profilgeschichte eine sichere Bild-Einfuegen-Aktion mit Datei-Upload. Alt-/Caption-Text wurde per User-Entscheidung (D-05) nicht implementiert — dokumentierter Contract-Gap, kandidiert fuer spaetere Barrierefreiheits-Erweiterung."

    SC5 aktualisieren: Urspruenglicher Text "Entfernen eines Bildes...physisches Cleanup verwaister Media-Assets ist bewusst separat geplant oder dokumentiert." → ersetzen durch:
    "[Ueberschrieben D-13] Cleanup-on-Save ist in Phase 70 implementiert: Beim Speichern der Geschichte wird jedes dereferenzierte Story-Bild sofort physisch aus Dateispeicher (/media) und DB (media_assets) entfernt. Urspruenglicher 'deferred'-Status per User-Entscheidung (D-13) ueberschrieben."

    Plans-Section in ROADMAP.md Phase 70 aktualisieren:
    - Plans: 7 plans
    - Plan-Liste mit allen 7 PLANs ergaenzen
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./... -count=1 2>&1 | tail -5 && cd C:/Users/admin/Documents/Team4s/frontend && npm test 2>&1 | tail -5 && grep -v '^#' C:/Users/admin/Documents/Team4s/.planning/ROADMAP.md | grep -c "D-05"</automated>
  </verify>
  <done>Backend-Test-Suite: PASS (keine Failures). Frontend-Test-Suite: PASS. ROADMAP.md enthaelt SC1-Gap-Dokumentation mit "D-05" und SC5-Update mit "D-13".</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Vollstaendige Phase-70-Implementierung:
    - Migration 0089 (owner_member_id auf media_assets)
    - Backend: Upload-Handler POST /api/v1/me/profile/story-images (MIME/Size/Pixel-Bomb/EXIF/Resize)
    - Backend: TipTap-Service Image-Node (Allowlist, Validator, Renderer, bluemonday-Policy)
    - Backend: IDOR-Check + Cleanup-on-Save in UpdateOwnProfile
    - Frontend: StoryImageExtension + NodeView (Resize, Ausrichtung L/M/R)
    - Frontend: RichTextEditor opt-in enableImages + Toolbar-Bild-Icon (via StoryImageToolbarButton)
    - Frontend: deferred Batch-Upload + pendingImages-State in page.tsx
  </what-built>
  <how-to-verify>
    **Voraussetzungen:** Docker-Compose laeuft (Postgres + Backend + Frontend). Migration 0089 wurde angewendet.

    **UAT-Szenario 1 — Round-Trip (D-21):**
    1. /me/profile aufrufen, als eingeloggter Member
    2. Geschichte bearbeiten: Bild-Icon in Toolbar klicken → JPG oder PNG auswaehlen
    3. Bild erscheint im Editor mit lokaler Vorschau (Object-URL)
    4. Resize-Ziehgriff nach links/rechts ziehen → Breite aendert sich sichtbar (in % der Spaltenbreite)
    5. Ausrichtungs-Buttons: Links / Mitte / Rechts setzen
    6. Speichern klicken → "Bild wird hochgeladen … N%" erscheint → Erfolg
    7. Seite neu laden → Bild erscheint mit gleicher Breite und Ausrichtung
    Erwartung: Bild, Breite und Ausrichtung nach Reload identisch zu vor dem Save

    **UAT-Szenario 2 — Cleanup-on-Save (D-22):**
    1. Bild in Geschichte einfuegen und speichern (Szenario 1)
    2. media_asset_id aus Response notieren oder SQL: SELECT id FROM media_assets WHERE owner_member_id = (deine member_id) ORDER BY id DESC LIMIT 1;
    3. Datei-Pfad notieren: storage_path aus media_assets
    4. Bild aus TipTap-Editor loeschen (Entfernen-Taste oder Backspace)
    5. Speichern klicken
    6. Pruefen: SELECT id FROM media_assets WHERE id = (notierte ID) → 0 Zeilen (DB-Zeile weg)
    7. Pruefen: Datei unter storage_path existiert nicht mehr im Dateisystem (docker exec oder Volume-Mount)
    Erwartung: Datei + DB-Zeile physisch entfernt; andere Bilder/Profilfelder unveraendert

    **UAT-Szenario 3 — IDOR (D-23):**
    1. Als Member A eine Story-Bild-ID (media_asset_id) von Member B herausfinden (z.B. SQL)
    2. PUT /api/v1/me/profile mit body_json das diese fremde ID als image-Node enthaelt
    3. Erwartung: 422 Unprocessable Entity mit Fehlermeldung "story-bild gehört nicht diesem profil"

    **UAT-Szenario 4 — Public-Profil (D-24):**
    1. Story mit Bild speichern (Szenario 1)
    2. Oeffentliches Member-Profil aufrufen: /members/{slug} oder GET /api/v1/members/{slug}/profile
    3. Erwartung: body_html enthaelt &lt;img src="/media/profile/.../story/..." style="width:N%" class="story-img-align-X"&gt;; Bild erscheint sichtbar auf der oeffentlichen Profilseite

    **UAT-Szenario 5 — Sanitizing (D-20/D-23):**
    1. Als eingeloggter Member manuell PUT /api/v1/me/profile mit manipuliertem body_json: image-node mit media_asset_id (eigenes Asset), aber body_html direkt manipuliert zu include &lt;script&gt;alert(1)&lt;/script&gt;
    2. Erwartung: Backend ignoriert Client-body_html (body_html wird serverseitig neu berechnet); kein script-Tag in der Antwort-body_html

    **UAT-Szenario 6 — Upload-Fehler-Atomizitaet (D-06/D-07):**
    1. /me/profile aufrufen, als eingeloggter Member
    2. Zwei oder mehr Bilder in den Editor einfuegen (noch nicht gespeichert, pending_key-Zustand)
    3. Story-Bild-Endpunkt temporaer deaktivieren: POST /api/v1/me/profile/story-images abfangen (z.B. mit Browser-DevTools → Network → "Block request URL" fuer den story-images-Pfad) ODER alternativ eine Datei mit ungueltiger MIME (z.B. umbenennte .gif) auswaehlen sodass das Backend 400 zurueckgibt
    4. Speichern klicken
    5. Erwartung (a): UI zeigt die Fehlermeldung aus UI-SPEC: "Mindestens ein Bild konnte nicht hochgeladen werden. Die Geschichte wurde nicht gespeichert. Bitte erneut versuchen." (kein generischer Fehler, kein leerer Zustand)
    6. Erwartung (b): Atomarer Abbruch — kein partielles body_json wurde persistiert: GET /api/v1/me/profile → member_story_json enthaelt NICHT die teil-hochgeladenen image-Nodes mit media_asset_id (body_json ist unveraendert gegenueber dem Zustand vor dem Speichern-Versuch)
    Nachweis: Prueft T-70-06-04 (accept) mit UAT-Beleg — kein partieller Save, da uploadPendingStoryImages bei Fehler wirft bevor updateOwnProfile aufgerufen wird

    **UAT-Szenario 7 — /me/profile Lesemodus (D-12):**
    1. Story mit Bild speichern (Szenario 1)
    2. /me/profile als eingeloggter Member aufrufen (Lesemodus, nicht Bearbeitungsmodus)
    3. Erwartung: Seite zeigt die Geschichte im Lesemodus (serverseitiges body_html, Public-aehnliches Rendering); das gespeicherte Story-Bild ist sichtbar mit der gespeicherten Breite (width_percent) und Ausrichtung (story-img-align-X-Klasse)
    4. Pruefen: GET /api/v1/me/profile response → member_story_html enthaelt &lt;img src="/media/profile/.../story/..." style="width:N%" class="story-img-align-X"&gt;
    Erwartung: /me/profile Lesemodus und Public-Profil rendern identisch aus demselben serverseitigen body_html
  </how-to-verify>
  <resume-signal>Tippe "approved" wenn alle 7 UAT-Szenarien bestätigt, oder beschreibe Probleme fuer Bugfixes</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT-Verifikation | Manueller Check bestätigt dass Security-Controls greifen (IDOR, Sanitizing, Cleanup) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-07-01 | Tampering | ROADMAP.md SC1/SC5-Korrekturen koennen zukuenftige Planer verwirren | mitigate | Explizite D-05/D-13-Referenzen in den aktualisierten SC-Texten; Contract-Gap klar als "bewusste Entscheidung" markiert |
| T-70-07-02 | Information Disclosure | UAT-Szenario 3 (IDOR) benoetigt echte Member-Daten | accept | Nur lokale Entwicklungsumgebung; keine Produktionsdaten involviert |
| T-70-07-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Packages |
</threat_model>

<verification>
Backend vollstaendig gruen:
`cd C:/Users/admin/Documents/Team4s/backend && go test ./... -count=1`

Frontend vollstaendig gruen:
`cd C:/Users/admin/Documents/Team4s/frontend && npm test`

ROADMAP-Korrekturen verifiziert:
`grep "D-05" C:/Users/admin/Documents/Team4s/.planning/ROADMAP.md`
`grep "D-13" C:/Users/admin/Documents/Team4s/.planning/ROADMAP.md`

Phase 70 Plans aktualisiert:
`grep "Plans:" C:/Users/admin/Documents/Team4s/.planning/ROADMAP.md | tail -1`

Human-UAT-Checkpoint bestätigt.
</verification>

<success_criteria>
- go test ./... -count=1 ohne Failures (alle Wave-0-Tests gruen)
- npm test ohne Failures
- ROADMAP.md SC1 enthaelt "D-05" und "Contract-Gap"
- ROADMAP.md SC5 enthaelt "D-13" und "in Phase 70 implementiert"
- ROADMAP.md Plans-Count auf 7 aktualisiert mit Plan-Liste
- UAT-Checkpoint von Nutzer bestätigt (alle 7 Szenarien):
  - Szenario 1: Round-Trip (D-21)
  - Szenario 2: Cleanup-on-Save (D-22)
  - Szenario 3: IDOR (D-23)
  - Szenario 4: Public-Profil (D-24)
  - Szenario 5: Sanitizing (D-20/D-23)
  - Szenario 6: Upload-Fehler-Atomizitaet (D-06/D-07, T-70-06-04 UAT-Nachweis)
  - Szenario 7: /me/profile Lesemodus (D-12)
</success_criteria>

<output>
Create `.planning/phases/70-tiptap-bilder-fuer-member-profilgeschichte/70-07-SUMMARY.md` when done
</output>
