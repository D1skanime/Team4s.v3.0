---
phase: 70-tiptap-bilder-fuer-member-profilgeschichte
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/0089_member_story_images.up.sql
  - database/migrations/0089_member_story_images.down.sql
autonomous: true
requirements: [D-03, D-08, D-13, D-22]

must_haves:
  truths:
    - "Spalte owner_member_id BIGINT REFERENCES members(id) ON DELETE SET NULL existiert in media_assets nach Migration"
    - "Index idx_media_assets_owner_member auf owner_member_id (WHERE NOT NULL) existiert"
    - "Bestehende media_assets-Zeilen erhalten NULL fuer owner_member_id (kein Backfill noetig)"
    - "Down-Migration entfernt Spalte und Index sauber"
    - "Migration laeuft idempotent (ADD COLUMN IF NOT EXISTS / CREATE INDEX IF NOT EXISTS)"
  artifacts:
    - path: "database/migrations/0089_member_story_images.up.sql"
      provides: "ALTER TABLE media_assets ADD COLUMN IF NOT EXISTS owner_member_id + Index"
      contains: "owner_member_id"
    - path: "database/migrations/0089_member_story_images.down.sql"
      provides: "DROP INDEX + DROP COLUMN"
      contains: "DROP COLUMN IF EXISTS owner_member_id"
  key_links:
    - from: "database/migrations/0089_member_story_images.up.sql"
      to: "media_assets Tabelle"
      via: "ALTER TABLE media_assets ADD COLUMN"
      pattern: "ALTER TABLE media_assets"
    - from: "database/migrations/0089_member_story_images.up.sql"
      to: "members(id)"
      via: "REFERENCES members(id) ON DELETE SET NULL"
      pattern: "REFERENCES members"
---

<objective>
Append-only DB-Migration 0089: Neue Spalte owner_member_id BIGINT auf der media_assets-Tabelle zur Member-Eigentuemer-Bindung von Story-Bildern. Diese Spalte ist die Grundlage fuer IDOR-Schutz (D-03), Pfad-Eigentuemer-Validierung (D-08) und Cleanup-on-Save (D-13/D-22).

Purpose: Ohne owner_member_id kann kein sicherer Eigentuemer-Check beim Speichern durchgefuehrt werden, und Cleanup-on-Save wuerde fremde Assets gefaehrden.
Output: Zwei SQL-Migrations-Dateien (up + down), idempotent, append-only, sauber reversibel.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/ROADMAP.md
@C:/Users/admin/Documents/Team4s/.planning/phases/70-tiptap-bilder-fuer-member-profilgeschichte/70-CONTEXT.md

<interfaces>
<!-- Bestehende Migration 0088 als exaktes Stil-Vorbild -->
<!-- Aus database/migrations/0088_anime_contributions_constraints.up.sql (gelesen in RESEARCH) -->

Pattern: Kommentar-Header, IF NOT EXISTS-Schutz, saubere Trennung von DDL-Statements.

<!-- media_assets-Tabellenstruktur (aus RESEARCH/PATTERNS) -->
Bestehende relevante Spalten:
- id BIGINT (PK)
- uploaded_by BIGINT FK auf users(id) — bestehende Upload-Attribution (NICHT fuer Member-Owner)
- file_path TEXT
- public_url TEXT
- storage_path TEXT
- mime_type TEXT
- size_bytes BIGINT
- width INT
- height INT

Neue Spalte: owner_member_id BIGINT REFERENCES members(id) ON DELETE SET NULL
→ NULL fuer alle bestehenden Assets (kein Backfill)
→ Wird nur fuer Story-Bilder gesetzt (nicht fuer Avatar/Background/andere)

<!-- Migration-Vorgaenger-Verifikation -->
Letzte verifizierte Migration: 0088_anime_contributions_constraints (Stand 2026-06-02)
Vor dem Anlegen pruefen: ls database/migrations/ | grep "^0089" → sollte leer sein
Falls weitere untracked Files zwischen 0088 und 0089 → Nummer anpassen
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrations-Dateien anlegen</name>
  <files>database/migrations/0089_member_story_images.up.sql, database/migrations/0089_member_story_images.down.sql</files>
  <read_first>
    - database/migrations/0088_anime_contributions_constraints.up.sql — exaktes Stil-Vorbild (Header-Kommentar, IF NOT EXISTS, Statement-Struktur)
    - database/migrations/ — Verzeichnisinhalt pruefen: sicherstellen dass keine Datei zwischen 0088 und 0089 existiert
  </read_first>
  <action>
    Verzeichnis database/migrations/ lesen und pruefen ob eine Datei mit Praefix 0089 bereits existiert oder ob zwischen 0088 und dem aktuellen Stand weitere untracked Migrations-Dateien liegen (analog Phase-55 D-13-Empfehlung). Falls 0089 bereits vergeben → Nummer erhoehen.

    Up-Migration (0089_member_story_images.up.sql):
    - Header-Kommentar mit Zweck (analog 0088)
    - ALTER TABLE media_assets ADD COLUMN IF NOT EXISTS owner_member_id BIGINT REFERENCES members(id) ON DELETE SET NULL;
    - Leerzeile
    - CREATE INDEX IF NOT EXISTS idx_media_assets_owner_member ON media_assets(owner_member_id) WHERE owner_member_id IS NOT NULL;

    Down-Migration (0089_member_story_images.down.sql):
    - Header-Kommentar "Rollback Migration 0089"
    - DROP INDEX IF EXISTS idx_media_assets_owner_member;
    - ALTER TABLE media_assets DROP COLUMN IF EXISTS owner_member_id;

    Hinweise:
    - ON DELETE SET NULL: Wenn ein Member geloescht wird, bleiben media_assets-Zeilen erhalten, owner_member_id wird NULL. Kein kaskadiertes Delete (Story-Assets gehoeren physisch dem System, auch wenn der Member weg ist).
    - Die Spalte wird NICHT fuer Avatar-, Background- oder andere bestehende Asset-Typen gesetzt — nur explizit beim Story-Bild-INSERT.
    - Kein Backfill bestehender Zeilen noetig (NULL ist korrekt fuer alte Assets).
  </action>
  <verify>
    <automated>ls C:/Users/admin/Documents/Team4s/database/migrations/0089_member_story_images.up.sql && grep -c "owner_member_id" C:/Users/admin/Documents/Team4s/database/migrations/0089_member_story_images.up.sql && grep -c "idx_media_assets_owner_member" C:/Users/admin/Documents/Team4s/database/migrations/0089_member_story_images.up.sql</automated>
  </verify>
  <done>Beide SQL-Dateien existieren. up.sql enthaelt "owner_member_id" und "idx_media_assets_owner_member". down.sql enthaelt "DROP COLUMN IF EXISTS owner_member_id". Beide Dateien sind valides SQL (kein Syntaxfehler sichtbar).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Migration 0089: ALTER TABLE media_assets ADD COLUMN owner_member_id BIGINT REFERENCES members(id) ON DELETE SET NULL + Partial-Index. Lokal noch nicht angewendet (Anwendung erfolgt in Plan 70-04 zusammen mit dem Handler).</what-built>
  <how-to-verify>
    1. Datei lesen: `cat database/migrations/0089_member_story_images.up.sql`
       Erwartung: ALTER TABLE media_assets ADD COLUMN IF NOT EXISTS owner_member_id BIGINT REFERENCES members(id) ON DELETE SET NULL; + CREATE INDEX IF NOT EXISTS idx_media_assets_owner_member
    2. Datei lesen: `cat database/migrations/0089_member_story_images.down.sql`
       Erwartung: DROP INDEX IF EXISTS idx_media_assets_owner_member; + ALTER TABLE media_assets DROP COLUMN IF EXISTS owner_member_id;
    3. Sicherstellen: Keine bestehende Datei mit Praefix 0089 war vorhanden (Migration-Nummer korrekt)
    4. Optional lokal testen: `cd backend && go run ./cmd/migrate up` — sollte ohne Fehler durchlaufen, Postgres-Container laeuft via docker-compose
  </how-to-verify>
  <resume-signal>Tippe "approved" wenn die Migration korrekt aussieht, oder beschreibe Probleme</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB-Migration → media_assets | Neue Spalte begruendet Eigentuemer-Beziehung; FK auf members(id) sichert referentielle Integritaet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-02-01 | Tampering | Migration loescht falsche Spalten in Down-Migration | mitigate | DROP COLUMN IF EXISTS ist idempotent; Down-Migration nur Rollback-Szenario; Reviewer prueft SQL vor Ausfuehren |
| T-70-02-02 | Elevation of Privilege | owner_member_id NULL fuer bestehende Assets erlaubt keinen falschen Owner-Check | accept | NULL-Check in Handler: nur explizit gesetzte owner_member_id wird fuer IDOR geprueft; NULL-Assets sind aeltere Nicht-Story-Assets und werden nicht im Story-Kontext referenced |
| T-70-02-SC | Tampering | npm/pip/cargo installs | accept | Keine Packages installiert in diesem Plan |
</threat_model>

<verification>
Migration-Dateien vorhanden und korrekt:
```
ls database/migrations/ | grep 0089
grep "owner_member_id" database/migrations/0089_member_story_images.up.sql
grep "ON DELETE SET NULL" database/migrations/0089_member_story_images.up.sql
grep "DROP COLUMN" database/migrations/0089_member_story_images.down.sql
```
</verification>

<success_criteria>
- 0089_member_story_images.up.sql und .down.sql existieren in database/migrations/
- up.sql: ALTER TABLE media_assets ADD COLUMN IF NOT EXISTS owner_member_id BIGINT REFERENCES members(id) ON DELETE SET NULL + Partial-Index
- down.sql: DROP INDEX + DROP COLUMN sauber reversibel
- Keine Datei-Nummer-Kollision mit bereits vorhandenen Migrations
- Human-Checkpoint bestaetigt korrekte SQL-Syntax
</success_criteria>

<output>
Create `.planning/phases/70-tiptap-bilder-fuer-member-profilgeschichte/70-02-SUMMARY.md` when done
</output>
