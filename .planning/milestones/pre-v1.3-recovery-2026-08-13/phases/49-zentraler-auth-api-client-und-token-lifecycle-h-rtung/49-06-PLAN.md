---
phase: 49-zentraler-auth-api-client-und-token-lifecycle-h-rtung
plan: 06
type: execute
wave: 5
depends_on:
  - 49-05
files_modified:
  - frontend/src/components/admin/MediaUpload.tsx
  - frontend/src/components/admin/MediaUpload.test.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseVersionMedia.ts
autonomous: true
requirements:
  - AUTH-API-CLIENT-01
must_haves:
  truths:
    - "Shared upload/media callers do not receive or forward authToken props."
    - "Upload UI gating uses token-free booleans and existing disabled/busy state."
    - "Progress callbacks, endpoint URLs, FormData fields, release media ownership, group media ownership, and release-version media ownership are preserved."
    - "The upload/media caller slice stops before touching more than 10 files."
  artifacts:
    - path: "frontend/src/components/admin/MediaUpload.tsx"
      provides: "Token-free group media upload component"
    - path: "frontend/src/components/admin/MediaUpload.test.tsx"
      provides: "Component regression coverage for token-free upload behavior"
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseVersionMedia.ts"
      provides: "Release-version media caller compatibility with central upload lifecycle"
  key_links:
    - from: "frontend/src/components/admin/MediaUpload.tsx"
      to: "frontend/src/lib/api.ts"
      via: "uploadFansubMedia and deleteFansubMedia without authToken arguments"
      pattern: "uploadFansubMedia|deleteFansubMedia"
    - from: "frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseVersionMedia.ts"
      to: "frontend/src/lib/api.ts"
      via: "uploadReleaseVersionMedia central lifecycle"
      pattern: "uploadReleaseVersionMedia"
---

<objective>
Migrate shared upload/media caller surfaces to token-free central auth usage after the helper contract split lands.

Purpose: Media upload flows are high-risk because they combine auth, progress, files, and release/group ownership. This plan removes token props without changing media semantics.
Output: token-free shared upload callers, focused component test coverage where needed, and `49-06-SUMMARY.md`.
</objective>

<context>
@.planning/phases/49-zentraler-auth-api-client-und-token-lifecycle-h-rtung/49-03-SUMMARY.md
@.planning/phases/49-zentraler-auth-api-client-und-token-lifecycle-h-rtung/49-05-SUMMARY.md
@.planning/phases/49-zentraler-auth-api-client-und-token-lifecycle-h-rtung/49-auth-api-client-inventory.md
@.planning/phases/49-zentraler-auth-api-client-und-token-lifecycle-h-rtung/49-auth-api-client-boundaries.md
@.planning/phases/49-zentraler-auth-api-client-und-token-lifecycle-h-rtung/49-RESEARCH.md
@.planning/phases/49-zentraler-auth-api-client-und-token-lifecycle-h-rtung/49-PATTERNS.md
@docs/architecture/db-schema-fansub-domain.md
@frontend/src/components/admin/MediaUpload.tsx
@frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseVersionMedia.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce upload/media split scope before edits</name>
  <files>frontend/src/components/admin/MediaUpload.tsx, frontend/src/components/admin/MediaUpload.test.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseVersionMedia.ts</files>
  <action>Before editing, confirm Plan 49-05 has completed and that this upload/media caller slice can be handled in the 3 listed files. If parent fansub/anime pages must change at the same time, leave those pages to Plans 49-07 through 49-09. If this slice needs more than 10 files, stop before source edits and write `49-06-SUMMARY.md` with `SPLIT_REQUIRED`. This implements D-02, D-06, and D-09 while preserving the 49-03 split constraint.</action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s && rg -n "\\bauthToken\\b|authToken\\s*[?:=]|authToken=\\{" frontend/src/components/admin/MediaUpload.tsx frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseVersionMedia.ts</automated>
  </verify>
  <done>Scope is confirmed within listed files, or the plan stops before edits with exact split evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Remove token props from shared media upload callers</name>
  <files>frontend/src/components/admin/MediaUpload.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseVersionMedia.ts</files>
  <action>Remove `authToken` props, locals, and helper arguments from `MediaUpload`; use existing `disabled`, `onBusyChange`, `hasAccessToken` ownership from parents, and local busy/error state instead. Keep the remote source public fetch in `MediaUpload` classified as public/no-auth and do not route it through protected auth. Confirm `useReleaseVersionMedia` remains token-free or update it only if Plan 49-05 changed helper signatures. Preserve upload progress, cancellation/error behavior, endpoint URLs, FormData fields, group media ownership through `fansub_group_media`, release-version media ownership, and release media boundaries. Do not attach release media directly to episodes.</action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck</automated>
  </verify>
  <done>Shared upload callers no longer accept or forward authToken values and still preserve progress plus existing ownership semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused upload caller regression coverage</name>
  <files>frontend/src/components/admin/MediaUpload.test.tsx</files>
  <action>Create or update focused `MediaUpload` tests only if no suitable test already exists. Cover token-free upload/delete calls, disabled-state gating, progress/busy callbacks, and preservation of public remote-source fetch behavior as no-auth. Keep test mocks aligned with central helper signatures from 49-05. Do not add visual redesign assertions or change CSS.</action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run test -- components/admin/MediaUpload.test.tsx</automated>
  </verify>
  <done>Focused tests prove shared upload callers work without authToken props and do not alter public/no-auth source fetch behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser file input to upload helper | User-selected files cross into authenticated upload helpers. |
| upload UI to central client | UI must not own bearer tokens while initiating protected uploads. |
| remote source preview fetch | Public remote URL fetch must stay unauthenticated and not leak auth headers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-06-01 | I | `MediaUpload` remote source fetch | mitigate | Preserve public/no-auth classification so bearer headers are not sent to remote source URLs. |
| T-49-06-02 | T | media upload callers | mitigate | Preserve FormData fields and media ownership routes exactly while removing token props. |
| T-49-06-03 | D | progress upload flow | mitigate | Preserve progress and busy callbacks so long uploads remain observable after central auth migration. |
</threat_model>

<verification>
Automated:
1. `cd C:/Users/admin/Documents/Team4s/frontend && npm run test -- components/admin/MediaUpload.test.tsx`
2. `cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck`
3. `cd C:/Users/admin/Documents/Team4s && git diff --check -- frontend/src/components/admin/MediaUpload.tsx frontend/src/components/admin/MediaUpload.test.tsx frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseVersionMedia.ts`

Static gate implications:
- `rg -n "\\bauthToken\\b|authToken\\s*[?:=]|authToken=\\{" frontend/src/components/admin/MediaUpload.tsx frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseVersionMedia.ts` should return zero production hits after this plan unless a documented boundary remains.
- Final 49-04 gates must still allow the public remote-source fetch only as public/no-auth.
</verification>

<success_criteria>
- Shared upload/media callers are token-free.
- Progress, busy state, errors, endpoint URLs, and FormData fields are preserved.
- Release, release-version, and group media ownership remain on the existing structures.
- No Jellyfin/streaming redesign, DB/schema change, backend permission change, or media ownership change is introduced.
- Scope remains at or below 10 touched files, or execution stops with a split summary before edits.
</success_criteria>

<output>
After completion, create `.planning/phases/49-zentraler-auth-api-client-und-token-lifecycle-h-rtung/49-06-SUMMARY.md`.
</output>
