---
phase: 49-zentraler-auth-api-client-und-token-lifecycle-h-rtung
plan: 05
type: execute
wave: 4
depends_on:
  - 49-02
  - 49-03
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/lib/api/admin-anime-intake.ts
  - frontend/src/lib/api.auth-refresh.test.ts
  - frontend/src/lib/api.admin-anime.test.ts
autonomous: true
requirements:
  - AUTH-API-CLIENT-01
must_haves:
  truths:
    - "Normal browser API helper contracts no longer require page-owned authToken arguments where central runtime auth is available."
    - "apiClientFetch remains the protected normal request gatekeeper and is not bypassed by helper modules."
    - "Upload helper retry behavior preserves Plan 49-02 unsafe no-replay decisions."
    - "The helper/API slice stops before touching more than 10 files."
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "Central helper signatures, apiClientFetch usage, and unsafe upload retry classification"
    - path: "frontend/src/lib/api/admin-anime-intake.ts"
      provides: "Token-free admin anime intake helper calls"
    - path: "frontend/src/lib/api.auth-refresh.test.ts"
      provides: "Central lifecycle regression coverage for helper behavior"
    - path: "frontend/src/lib/api.admin-anime.test.ts"
      provides: "Focused API/helper upload contract coverage"
  key_links:
    - from: "frontend/src/lib/api/admin-anime-intake.ts"
      to: "frontend/src/lib/api.ts"
      via: "apiClientFetch calls without authToken options"
      pattern: "apiClientFetch"
    - from: "frontend/src/lib/api.ts"
      to: "49-02-SUMMARY.md"
      via: "upload retry eligibility evidence"
      pattern: "no automatic retry|re-auth"
---

<objective>
Migrate the helper/API contract slice that Plan 49-03 could not safely include in one broad caller migration.

Purpose: Later caller plans need stable token-free helper signatures before pages, components, hooks, and upload callers can stop threading token-shaped values.
Output: helper/API contract changes, focused tests, and `49-05-SUMMARY.md`.
</objective>

<context>
@.planning/phases/49-zentraler-auth-api-client-und-token-lifecycle-h-rtung/49-03-SUMMARY.md
@.planning/phases/49-zentraler-auth-api-client-und-token-lifecycle-h-rtung/49-auth-api-client-inventory.md
@.planning/phases/49-zentraler-auth-api-client-und-token-lifecycle-h-rtung/49-auth-api-client-boundaries.md
@.planning/phases/49-zentraler-auth-api-client-und-token-lifecycle-h-rtung/49-RESEARCH.md
@.planning/phases/49-zentraler-auth-api-client-und-token-lifecycle-h-rtung/49-PATTERNS.md
@.planning/phases/49-zentraler-auth-api-client-und-token-lifecycle-h-rtung/49-02-SUMMARY.md
@docs/architecture/db-schema-fansub-domain.md
@frontend/src/lib/api.ts
@frontend/src/lib/api/admin-anime-intake.ts
@frontend/src/lib/api.auth-refresh.test.ts
@frontend/src/lib/api.admin-anime.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce helper/API split scope before edits</name>
  <files>frontend/src/lib/api.ts, frontend/src/lib/api/admin-anime-intake.ts, frontend/src/lib/api.auth-refresh.test.ts, frontend/src/lib/api.admin-anime.test.ts</files>
  <action>Before editing, compare this plan's file list with the 49-01 inventory rows needed for helper/API contract migration. If implementation needs more than these 4 files or more than 10 files total, stop and write `49-05-SUMMARY.md` with `SPLIT_REQUIRED` plus the exact extra files. Do not edit pages, components, hooks, streaming routes, backend code, database schema, or media ownership files in this plan. This implements D-01, D-03, D-06, and D-09 while honoring the 49-03 split result.</action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s && rg -n "authToken\\?: string|apiClientFetch\\([^)]*\\{\\s*[^}]*authToken|withAuthHeader\\(\\{\\}, authToken\\)" frontend/src/lib/api.ts frontend/src/lib/api/admin-anime-intake.ts</automated>
  </verify>
  <done>Scope is confirmed within 4 listed files, or the plan stops before source edits with a split summary.</done>
</task>

<task type="auto">
  <name>Task 2: Remove normal browser authToken helper signatures where safe</name>
  <files>frontend/src/lib/api.ts, frontend/src/lib/api/admin-anime-intake.ts</files>
  <action>Remove normal browser `authToken` parameters and `authToken` forwarding from helper functions that can rely on central runtime auth. Preserve `apiClientFetch` and `authorizedFetch` as the central request gatekeeper per D-03. Keep any remaining explicit-token seam only when it is a documented auth entrypoint, SSR/server boundary, streaming boundary, or internal compatibility path; add a short code comment only if the exception would otherwise look like normal browser token ownership. Do not alter endpoint URLs, request bodies, response parsing, Keycloak identity boundaries, Jellyfin/streaming routes, or backend permission behavior.</action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run test -- api.auth-refresh.test.ts api.admin-anime.test.ts</automated>
  </verify>
  <done>Normal helper calls covered by this slice no longer accept or forward page-owned authToken values, and protected helpers still route through apiClientFetch.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve unsafe upload no-replay behavior in helper tests</name>
  <files>frontend/src/lib/api.ts, frontend/src/lib/api.auth-refresh.test.ts, frontend/src/lib/api.admin-anime.test.ts</files>
  <action>Update focused tests to lock the Plan 49-02 upload retry evidence: automatic retry is allowed only for endpoints proven auth-before-persistence or idempotent; endpoints not proven safe must surface a re-auth/error result without replaying the upload body. Preserve progress callbacks and central-only bearer attachment expectations for upload helpers. Do not change FormData field names, release/group media ownership, release-version media ownership, or endpoint URLs.</action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s && git diff --check -- frontend/src/lib/api.ts frontend/src/lib/api/admin-anime-intake.ts frontend/src/lib/api.auth-refresh.test.ts frontend/src/lib/api.admin-anime.test.ts</automated>
  </verify>
  <done>Focused helper tests pass and prove the helper contract does not reintroduce unsafe upload replay or page-owned token parameters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to API helper | Untrusted UI calls enter protected API helper functions. |
| helper to central client | Helper functions must not attach or resolve bearer tokens outside `api.ts` central lifecycle. |
| upload helper to backend | File payloads cross from browser to upload endpoints and must not be replayed unless endpoint safety is proven. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-05-01 | S | `frontend/src/lib/api/admin-anime-intake.ts` | mitigate | Remove normal browser token parameters so callers cannot spoof auth by passing stale or copied token strings. |
| T-49-05-02 | T | `frontend/src/lib/api.ts` upload helpers | mitigate | Preserve no-replay behavior for unsafe upload endpoints and test that 401 handling does not duplicate persisted media. |
| T-49-05-03 | E | helper exception seams | mitigate | Any remaining explicit-token path must be documented as auth entrypoint, server, streaming, or internal compatibility rather than normal browser ownership. |
</threat_model>

<verification>
Automated:
1. `cd C:/Users/admin/Documents/Team4s/frontend && npm run test -- api.auth-refresh.test.ts api.admin-anime.test.ts`
2. `cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck`
3. `cd C:/Users/admin/Documents/Team4s && git diff --check -- frontend/src/lib/api.ts frontend/src/lib/api/admin-anime-intake.ts frontend/src/lib/api.auth-refresh.test.ts frontend/src/lib/api.admin-anime.test.ts`

Static gate implications:
- The final 49-04 static gate should show fewer `authToken?: string` and `apiClientFetch(... authToken)` hits in `frontend/src/lib`.
- Remaining helper-token hits must be documented boundary exceptions or owned by later split plans.
</verification>

<success_criteria>
- Helper/API contract migration stays within the 4 listed files or stops with a split summary before edits.
- Normal browser helper signatures covered by this plan no longer require authToken arguments.
- `apiClientFetch` remains the central protected request path.
- Unsafe upload endpoints retain no-automatic-replay behavior.
- No Jellyfin/streaming redesign, DB/schema change, backend permission change, release media ownership change, or group media ownership change is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/49-zentraler-auth-api-client-und-token-lifecycle-h-rtung/49-05-SUMMARY.md`.
</output>
