---
phase: 54-globale-nav-drawer-und-layout-verdrahtung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/components/layout/AppShell.module.css
  - frontend/src/components/layout/AppShell.test.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-14
  - D-15
  - D-16
  - D-18
  - D-19

must_haves:
  truths:
    - "Auf Mobile öffnet/schließt der Burger-Button einen echten Slide-over-Drawer"
    - "Der Drawer erscheint über dem Content (overlay, kein Inline-Panel)"
    - "Auf Desktop ist ein 16px Glasrand (Edge-Strip) am linken Rand sichtbar"
    - "Hover auf Edge-Strip öffnet den Drawer; Maus verlässt Drawer → schließt"
    - "ESC schließt den offenen Drawer von überall"
    - "Klick auf Backdrop schließt den Drawer auf Mobile"
    - "Focus-Trap hält Tab/Shift+Tab innerhalb des offenen Drawers"
    - "Anonym-Modus: Anmelden- und Registrieren-Buttons im Footer sichtbar"
    - "Eingeloggt-Modus: Avatar-Bild (oder Initialen-Fallback) + Name + E-Mail im Footer"
    - "canAccessAdmin=false → kein Verwaltungslink im Drawer"
    - "canAccessAdmin=true → Verwaltungslink sichtbar"
    - "Alle bestehenden 5 Tests bleiben grün"
  artifacts:
    - path: "frontend/src/components/layout/AppShell.tsx"
      provides: "Erweiterter AppShell mit drawerOpen State, Edge-Strip, Backdrop, Focus-Trap, Dual-State-Footer"
      contains: "drawerOpen"
    - path: "frontend/src/components/layout/AppShell.module.css"
      provides: "Slide-over CSS (.drawer, .drawerOpen, .edgeStrip, .drawerBackdrop, .userAvatarImg)"
      contains: "translateX"
    - path: "frontend/src/components/layout/AppShell.test.tsx"
      provides: "Tests für Slide-over, Edge-Strip, ESC, Backdrop, Dual-State, Avatar, Focus-Trap"
      exports: []
  key_links:
    - from: "Edge-Strip (div.edgeStrip)"
      to: "drawerOpen State"
      via: "onMouseEnter → setDrawerOpen(true)"
      pattern: "setDrawerOpen"
    - from: "Backdrop (button.drawerBackdropBtn)"
      to: "drawerOpen State"
      via: "onClick → setDrawerOpen(false)"
      pattern: "setDrawerOpen"
    - from: "document keydown listener"
      to: "drawerOpen State"
      via: "ESC → setDrawerOpen(false)"
      pattern: "Escape"
---

<objective>
AppShell.tsx von der statischen Desktop-Sidebar mit Inline-Mobile-Panel zu einem vollständigen Slide-over-Drawer-Navigationssystem umbauen.

Zweck: Alle Drawer-Mechanismus-Entscheidungen (D-01 bis D-09, D-14, D-15, D-16, D-18, D-19) landen in AppShell und AppShell.module.css. Der AppShellClientWrapper (Plan 02) kann danach auf die neuen Props aufsetzen.

Ausgabe: Erweitertes AppShell.tsx mit Slide-over-Logik, Edge-Strip, Glassmorphism-CSS, Dual-State-Footer, Avatar-Bild-Unterstützung und erweiterten Tests.
</objective>

<execution_context>
@C:\Users\admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/54-globale-nav-drawer-und-layout-verdrahtung/54-CONTEXT.md
@.planning/phases/54-globale-nav-drawer-und-layout-verdrahtung/54-RESEARCH.md
@.planning/phases/54-globale-nav-drawer-und-layout-verdrahtung/54-UI-SPEC.md
@.planning/phases/54-globale-nav-drawer-und-layout-verdrahtung/54-PATTERNS.md

<interfaces>
Bestehende AppShell-Typen (aus AppShell.tsx Zeilen 21–43):

type AppShellMode = 'authenticated' | 'anonymous'

type AppShellUser = {
  displayName?: string | null
  email?: string | null
  // Phase 54 NEU: avatarUrl hinzufügen
}

export interface AppShellProps {
  mode?: AppShellMode
  currentPath?: string
  user?: AppShellUser | null
  canAccessAdmin?: boolean
  children: ReactNode
}

Bestehender State (AppShell.tsx Zeile 119):
  const [mobileNavOpen, setMobileNavOpen] = useState(false)
  // → umbenennen zu drawerOpen, Desktop-Edge-Strip-Events hinzufügen

Bestehende Klassen in AppShell.module.css:
  .shell, .mobileHeader, .mobileNav, .mobileNavOpen, .sidebar, .brand,
  .brandMark, .userAvatar, .nav, .navGroup, .navGroupLabel, .navItem,
  .navItemCurrent, .navItemDisabled, .navIcon, .userFooter, .content

getFocusableElements aus mediaCropA11y:
  import { getFocusableElements } from '@/components/media/crop/mediaCropA11y'
  Gibt HTMLElement[] zurück — filtert disabled und aria-hidden aus.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Neue Tests für Drawer-Behavior in AppShell.test.tsx schreiben (Wave-0-Contracts)</name>
  <files>frontend/src/components/layout/AppShell.test.tsx</files>
  <read_first>
    frontend/src/components/layout/AppShell.test.tsx
    frontend/src/components/layout/AppShell.tsx
    .planning/phases/54-globale-nav-drawer-und-layout-verdrahtung/54-PATTERNS.md
  </read_first>
  <behavior>
    - Test A: Burger-Button öffnet Drawer (aria-expanded wird true, Drawer-Element wird sichtbar)
    - Test B: ESC-Taste schließt offenen Drawer (aria-expanded zurück zu false)
    - Test C: Backdrop-Klick schließt offenen Drawer
    - Test D: Anonym-Modus → Drawer-Footer enthält Link mit Text "Anmelden" und Link mit Text "Registrieren"
    - Test E: Eingeloggt-Modus mit avatarUrl → img-Element mit src='https://cdn.test.com/av.jpg' und alt='Avatar von Mika' vorhanden
    - Test F: Eingeloggt-Modus ohne avatarUrl → span mit erstem Buchstaben des displayName (Großbuchstabe) vorhanden; img NICHT vorhanden
    - Test G: canAccessAdmin=true → Nav-Link "Verwaltung" mit href="/admin" vorhanden
    - Test H: canAccessAdmin=false → kein Link mit href="/admin"
    - Test I (bestehend): 5 vorhandene Tests müssen weiterhin grün sein — nicht anfassen, nur sicherstellen
  </behavior>
  <action>
    Lies AppShell.test.tsx vollständig. Füge NACH den bestehenden 5 Tests neue describe-Blöcke ein (kein Überschreiben).

    Für Tests A–C: AppShell ohne mode-Prop rendern (default: authenticated), Burger-Button per getByRole('button', { name: /Navigation/i }) finden. Drawer öffnen via fireEvent.click(navButton). Drawer-Schließung per ESC: fireEvent.keyDown(document, { key: 'Escape' }). Backdrop-Schließung: Das zu testende Backdrop-Element wird mit getByRole('button', { name: /Drawer schließen/i }) gefunden.

    Für Test D: mode="anonymous" übergeben, Drawer öffnen, dann getByRole('link', { name: /Anmelden/i }) und getByRole('link', { name: /Registrieren/i }) assertieren.

    Für Test E: user={{ displayName: 'Mika', email: 'mika@example.com', avatarUrl: 'https://cdn.test.com/av.jpg' }}, Drawer öffnen, img per getByRole('img', { name: /Avatar von Mika/i }) finden, src assertieren.

    Für Test F: user={{ displayName: 'Mika', email: 'mika@example.com' }} (kein avatarUrl), Drawer öffnen, queryByRole('img') ist null; span mit Textinhalt 'M' vorhanden.

    Tests G/H: canAccessAdmin-Prop übergeben, queryByRole('link', { name: /Verwaltung/i }) assertieren.

    ACHTUNG: Diese Tests werden zunächst FEHLSCHLAGEN (AppShell hat die neue API noch nicht). Das ist erwartet — die Tests definieren den Vertrag. Führe sie danach mit dem verify-Befehl aus und bestätige das erwartete Fehlschlagen.

    Wichtig: Kein fenced code block in action — alle Identifikatoren sind oben benannt.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/layout/AppShell.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>Neue Testfälle existieren in AppShell.test.tsx; bestehende 5 Tests sind noch grün; neue Tests schlagen erwartungsgemäß fehl weil AppShell die neue API noch nicht hat</done>
</task>

<task type="auto">
  <name>Task 2: AppShell.tsx — Slide-over-Drawer, Edge-Strip, Dual-State, Avatar-Footer implementieren</name>
  <files>frontend/src/components/layout/AppShell.tsx</files>
  <read_first>
    frontend/src/components/layout/AppShell.tsx
    frontend/src/components/layout/AppShell.test.tsx
    frontend/src/components/media/crop/mediaCropA11y.ts
    .planning/phases/54-globale-nav-drawer-und-layout-verdrahtung/54-PATTERNS.md
  </read_first>
  <action>
    Lies AppShell.tsx (177 Zeilen) vollständig. Implementiere folgende Änderungen — NUR diese, nichts anderes:

    IMPORTS erweitern: useEffect, useRef, useCallback zu React-Importen hinzufügen. Import hinzufügen: getFocusableElements aus '@/components/media/crop/mediaCropA11y'. Import hinzufügen: resolveApiUrl aus '@/lib/api' — NICHT in AppShell.tsx nutzen, der Wrapper löst die URL auf; resolveApiUrl wird NUR im Wrapper aufgerufen. Stattdessen: avatarUrl-Prop direkt als string nutzen.

    TYPE-ERWEITERUNG: AppShellUser um Feld avatarUrl?: string | null erweitern.

    STATE: mobileNavOpen umbenennen zu drawerOpen. Zwei Refs hinzufügen: drawerRef = useRef<HTMLAMLElement>(null) für den aside-Drawer, triggerRef = useRef<HTMLButtonElement>(null) für den Burger-Button.

    USEEFFECT — ESC + Focus-Trap: Ein useEffect der auf [drawerOpen] läuft. Guard: if (!drawerOpen) return. Keydown-Listener auf document registrieren. Bei 'Escape': setDrawerOpen(false), triggerRef.current?.focus(). Bei 'Tab': getFocusableElements(drawerRef.current!) aufrufen; Shift+Tab auf erstem Element → last.focus(); Tab auf letztem Element → first.focus(). Cleanup: removeEventListener.

    ANONYMER FOOTER (neue Hilfsfunktion DrawerAnonymousFooter): footer-Element mit className={styles.anonFooter}. Zwei Links (next/link): href="/auth" className={styles.btnPrimary} Text "Anmelden", href="/auth/register" className={styles.btnSecondary} Text "Registrieren".

    EINGELOGGT FOOTER (bestehender Footer, erweitern): Falls user?.avatarUrl vorhanden: img-Tag mit src={user.avatarUrl} alt={`Avatar von ${user?.displayName || 'Mitglied'}`} className={styles.userAvatarImg} width={36} height={36}. Falls kein avatarUrl: bestehender span-Initialen-Fallback bleibt unverändert.

    RENDER-STRUKTUR überarbeiten:
    1. Desktop Edge-Strip (nur außerhalb mobile, via CSS versteckt): div mit className={styles.edgeStrip}, role="button", tabIndex={0}, aria-expanded={drawerOpen}, aria-controls="team4s-nav-drawer", aria-label="Navigation öffnen", onMouseEnter={() => setDrawerOpen(true)}, onKeyDown={(e) => { if (e.key === 'Enter') setDrawerOpen(true) }}, onFocus={() => setDrawerOpen(true)}.
    2. Backdrop (Mobile, nur wenn drawerOpen): button mit type="button" className={styles.drawerBackdrop}, aria-label="Drawer schließen", onClick={() => setDrawerOpen(false)}.
    3. aside-Element: id="team4s-nav-drawer", ref={drawerRef}, className={styles.drawer + (drawerOpen ? ' ' + styles.drawerOpen : '')}, onMouseLeave={() => setDrawerOpen(false)}, onBlur={(e) => { if (!e.currentTarget.contains(e.relatedTarget as Node)) setDrawerOpen(false) }}, aria-label="Team4s Navigation". Enthält: Branding-Header (Team4s Logotext wie bisher), Nav (AppShellNavGroups für eingeloggt; anonym: nur publicItems mit Anime-Link), Footer (DrawerAnonymousFooter wenn mode==='anonymous', eingeloggt Footer wenn mode==='authenticated').
    4. Burger-Button: ref={triggerRef} hinzufügen, aria-controls="team4s-nav-drawer" (nicht mehr team4s-mobile-nav).
    5. .content-div: {children} bleibt direkt darin.
    6. Die .shell-div bleibt als Grid-Container, aber .sidebar (statischer Desktop) wird entfernt — der Drawer übernimmt.
    7. .mobileNav/mobileNavOpen wird nicht mehr genutzt — der aside-Drawer übernimmt auch Mobile.

    ANONYME NAV-GRUPPEN: Für mode==='anonymous' eine neue AppShellAnonNavGroups-Funktion: publicItems enthält Anime entdecken (/anime), Fansub-Gruppen (disabled, badge 'bald', per D-02 aus RESEARCH: kein /fansubs-Link), Suche (disabled, badge 'bald', per D-09: keine /search-Route).

    KRITISCH: font-weight: 650 in JS-Code nicht nutzen (kein valider CSS-Wert in allen Browsern — CSS-Datei korrigiert das separat in Task 3). avatarUrl-Feld ist bereits aufgelöst wenn es in AppShell ankommt — kein resolveApiUrl in AppShell.tsx aufrufen.

    BESTEHENDE TESTS: Test 5 prüft queryByLabelText('Hauptnavigation mobil') — dieses Label auf dem nav-Element innen im Drawer bleibt bei mode=authenticated erhalten via aria-label="Hauptnavigation" (Desktop-Nav). Der Burger-Button behält sein aria-expanded/aria-controls-Pattern. Test 5 lautet: navButton aria-expanded="false" im geschlossenen Zustand — das bleibt korrekt weil drawerOpen initial false.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/layout/AppShell.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <done>
    Alle Tests in AppShell.test.tsx grün (alte 5 + neue A-I).
    AppShell.tsx enthält: drawerOpen State, drawerRef/triggerRef Refs, ESC-useEffect, Edge-Strip div, Backdrop button, aside#team4s-nav-drawer, DrawerAnonymousFooter, Avatar-img-Fallback-Logik, AppShellAnonNavGroups.
    Die .sidebar-aside ist entfernt (Drawer übernimmt).
  </done>
</task>

<task type="auto">
  <name>Task 3: AppShell.module.css — Slide-over, Edge-Strip, Backdrop, Avatar-Bild, font-weight-Fix</name>
  <files>frontend/src/components/layout/AppShell.module.css</files>
  <read_first>
    frontend/src/components/layout/AppShell.module.css
    frontend/src/styles/globals.css
    .planning/phases/54-globale-nav-drawer-und-layout-verdrahtung/54-UI-SPEC.md
    .planning/phases/54-globale-nav-drawer-und-layout-verdrahtung/54-PATTERNS.md
  </read_first>
  <action>
    Lies AppShell.module.css (200 Zeilen) vollständig. Implementiere folgende CSS-Änderungen:

    SHELL-GRID: .shell grid-template-columns von "minmax(220px, 260px) minmax(0, 1fr)" auf "minmax(0, 1fr)" ändern — kein statischer Sidebar-Spalten-Slot mehr. Das .content-div bekommt keine spezielle Behandlung mehr.

    SIDEBAR entfernen: .sidebar-Regeln bleiben als toter Code nur wenn nötig; besser: Kommentar "/* .sidebar durch .drawer ersetzt in Phase 54 */" + Klasse auskommentieren oder leer lassen. Die .sidebar-Regel wird nicht mehr gerendert (aside wurde entfernt).

    NEUES .drawer (Slide-over):
      position: fixed; top: 0; left: 0; height: 100vh; width: 260px;
      z-index: var(--z-drawer, 80); (kein Magic Number)
      transform: translateX(-100%);
      transition: transform 200ms ease-out;
      background: rgba(255, 255, 255, 0.85);
      backdrop-filter: blur(16px);
      border-right: 1px solid var(--color-border, rgba(100, 116, 139, 0.22));
      display: flex; flex-direction: column; gap: 22px; padding: 22px 18px;

    NEUES .drawerOpen: transform: translateX(0);

    NEUES .edgeStrip (Desktop-Glasrand):
      position: fixed; top: 0; left: 0; width: 16px; height: 100vh;
      z-index: var(--z-drawer, 80);
      background: rgba(255, 255, 255, 0.35); backdrop-filter: blur(8px);
      cursor: pointer; border: 0; padding: 0;
    .edgeStrip:focus-visible: outline: 3px solid rgba(47, 95, 227, 0.42); outline-offset: 2px;

    NEUES .drawerBackdrop (Mobile Overlay):
      position: fixed; inset: 0; z-index: 79; /* unter Drawer, über Content */
      background: rgba(0, 0, 0, 0.35); border: 0; cursor: pointer;

    NEUES .userAvatarImg:
      width: 36px; height: 36px; border-radius: 50%;
      object-fit: cover; flex: 0 0 auto;

    NEUES .anonFooter:
      padding-top: 16px; border-top: 1px solid var(--color-border, rgba(100, 116, 139, 0.22));
      display: flex; gap: 8px; flex-wrap: wrap;

    NEUES .btnPrimary:
      flex: 1; min-width: 0; padding: 8px 12px; border-radius: 8px;
      background: var(--color-primary, #2f5fe3); color: #fff;
      font-size: 0.9rem; font-weight: 700; text-align: center; text-decoration: none;

    NEUES .btnSecondary:
      flex: 1; min-width: 0; padding: 8px 12px; border-radius: 8px;
      border: 1px solid var(--color-border, rgba(100, 116, 139, 0.22));
      background: rgba(255, 255, 255, 0.86); color: var(--color-text, #172033);
      font-size: 0.9rem; font-weight: 700; text-align: center; text-decoration: none;

    FONT-WEIGHT FIX: .navItem font-weight: 650 → font-weight: 700. .mobileNavButton font-weight: 750 → font-weight: 700. (650 und 750 sind nicht valide CSS-Werte in allen Browsern.)

    NAV-ITEM MIN-HEIGHT: .navItem min-height: 38px → min-height: 40px (Vielfaches von 4, per UI-SPEC).

    MEDIA QUERY (@media max-width 860px):
      .edgeStrip: display: none; (kein Edge-Strip auf Mobile)
      .drawerBackdrop: sichtbar (kein spezifisches hide)
      .mobileHeader: bleibt wie bisher (display: flex)
      .mobileNav, .mobileNavOpen: können stehen bleiben als Dead Code (kein Schaden); alternativ: Klassen auskommentieren
      .sidebar: display: none; bleibt oder wird entfernt — sidebar existiert nicht mehr im DOM
      Auf Desktop (>860px): .mobileHeader bleibt display: none (bestehende Regel)

    BRANDING-HEADER im Drawer: .brand-Stil bleibt wie bisher (Logo-Text "T4" + Titel).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/layout/AppShell.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    AppShell.module.css enthält: .drawer, .drawerOpen, .edgeStrip, .drawerBackdrop, .userAvatarImg, .anonFooter, .btnPrimary, .btnSecondary.
    .navItem min-height: 40px. font-weight überall 700 statt 650/750.
    Alle Tests grün (gleiche Assertion-Basis: CSS-Klassen sind nur in CSS, nicht in Test-Assertions für Styles).
    Frontend TypeScript-Check: cd frontend && npx tsc --noEmit 2>&1 | grep -v "^$" | head -20
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Client → Nav-UI | Auth-State kommt aus useAuthSession (Cookie), nicht aus Props |
| API → Avatar-URL | Avatar-URL kommt über getOwnProfile (im Wrapper), nie als Token-Prop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Elevation of Privilege | AppShell canAccessAdmin-Prop | mitigate | canAccessAdmin wird aus Backend-Capabilities (account_global_roles) im Wrapper berechnet, nicht aus Keycloak-Claims; Route /admin bleibt server-seitig geschützt; Test G/H in AppShell.test.tsx verifiziert Capability-Gate |
| T-54-02 | Information Disclosure | AppShellProps | mitigate | Kein authToken-Prop in AppShellProps; avatarUrl ist eine öffentliche URL; Token-free UI Boundary per auth-api-client.md eingehalten |
| T-54-SC | Tampering | npm/pip installs | accept | Keine neuen Pakete in Phase 54 — nur projekt-eigene Utilities |
</threat_model>

<verification>
Nach allen 3 Tasks:

1. `cd frontend && npx vitest run src/components/layout/AppShell.test.tsx` — alle Tests grün
2. `cd frontend && npx vitest run` — gesamte Suite grün
3. `cd frontend && npx tsc --noEmit 2>&1 | head -20` — keine TypeScript-Fehler
4. AppShell.tsx enthält "drawerOpen" (grep bestätigt)
5. AppShell.module.css enthält "translateX" (grep bestätigt)
6. AppShell.module.css enthält "edgeStrip" (grep bestätigt)
</verification>

<success_criteria>
1. Alle 5 bestehenden AppShell-Tests grün
2. Neue Tests A–I grün: Slide-over, ESC, Backdrop, Dual-State, Avatar, Initialen-Fallback, canAccessAdmin
3. TypeScript-Compile ohne Fehler
4. AppShell.tsx hat drawerOpen State und useEffect für ESC/Focus-Trap
5. AppShell.module.css hat .drawer mit transform: translateX(-100%) und .edgeStrip mit width: 16px
6. font-weight 650 und 750 sind aus der CSS-Datei entfernt
</success_criteria>

<output>
Erstelle `.planning/phases/54-globale-nav-drawer-und-layout-verdrahtung/54-01-SUMMARY.md` wenn fertig
</output>
