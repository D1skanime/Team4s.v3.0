---
phase: 54-globale-nav-drawer-und-layout-verdrahtung
plan: 03
type: execute
wave: 2
depends_on:
  - 54-01
  - 54-02
files_modified:
  - frontend/src/app/layout.tsx
  - frontend/src/app/me/profile/page.tsx
autonomous: true
requirements:
  - D-11
  - D-12
  - D-13
  - D-10

must_haves:
  truths:
    - "Root-Layout layout.tsx bleibt Server Component (kein 'use client' hinzugefügt)"
    - "AppShellClientWrapper ist in layout.tsx importiert und wraps {children}"
    - "layout.tsx hat keinen direkten Drawer-State oder Client-Logik"
    - "me/profile/page.tsx rendert keinen eigenen AppShell-Wrapper mehr"
    - "AppShell-Import in me/profile/page.tsx ist entfernt"
    - "Auth-Seiten (/auth/*) bekommen Drawer automatisch durch Root-Layout"
  artifacts:
    - path: "frontend/src/app/layout.tsx"
      provides: "Root-Layout mit AppShellClientWrapper um children"
      contains: "AppShellClientWrapper"
    - path: "frontend/src/app/me/profile/page.tsx"
      provides: "Profil-Seite ohne eigenen AppShell-Wrapper"
  key_links:
    - from: "frontend/src/app/layout.tsx"
      to: "frontend/src/components/layout/AppShellClientWrapper.tsx"
      via: "import { AppShellClientWrapper } from '@/components/layout/AppShellClientWrapper'"
      pattern: "AppShellClientWrapper"
---

<objective>
Root-Layout-Integration: AppShellClientWrapper in layout.tsx einbauen, sodass alle Seiten automatisch die globale Nav-Drawer erhalten. Danach Doppel-Shell-Problem bereinigen: me/profile/page.tsx gibt seinen eigenen AppShell-Wrapper auf.

Zweck: Plan 01 und 02 haben AppShell und Wrapper fertiggestellt. Jetzt werden sie seitenweit aktiviert (D-11) und der einzige Konsument mit Eigeninkludierung bereinigt (D-12).

Ausgabe: layout.tsx mit AppShellClientWrapper, me/profile/page.tsx ohne AppShell-Wrapper.
</objective>

<execution_context>
@C:\Users\admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/54-globale-nav-drawer-und-layout-verdrahtung/54-CONTEXT.md
@.planning/phases/54-globale-nav-drawer-und-layout-verdrahtung/54-PATTERNS.md
@.planning/phases/54-globale-nav-drawer-und-layout-verdrahtung/54-01-SUMMARY.md
@.planning/phases/54-globale-nav-drawer-und-layout-verdrahtung/54-02-SUMMARY.md

<interfaces>
Aktuelles layout.tsx (Zeilen 1–32):
  Imports: Metadata, ReactNode, AuthSessionSwitchGuard, LocalhostCanonicalRedirect, globals.css
  Kein 'use client' — muss so bleiben
  {children} ist direkt in body → wird zu <AppShellClientWrapper>{children}</AppShellClientWrapper>

me/profile/page.tsx Eigeninkludierung (Zeile 287):
  return (
    <AppShell currentPath="/me/profile" user={shellUser} canAccessAdmin={canAccessAdmin(profile)}>
      <main className={styles.page}>...</main>
    </AppShell>
  )
  → AppShell-Wrapper entfernen, nur <main className={styles.page}>...</main> zurückgeben

Nach Bereinigung: AppShell-Import-Zeile aus page.tsx entfernen.
  canAccessAdmin-Hilfsfunktion und shellUser-Variable: prüfen ob sie noch genutzt werden;
  wenn nur für AppShell gebraucht → entfernen; wenn an anderer Stelle → behalten.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppShellClientWrapper in Root-Layout einbauen</name>
  <files>frontend/src/app/layout.tsx</files>
  <read_first>
    frontend/src/app/layout.tsx
    frontend/src/components/layout/AppShellClientWrapper.tsx
    .planning/phases/54-globale-nav-drawer-und-layout-verdrahtung/54-PATTERNS.md
  </read_first>
  <action>
    Lies layout.tsx vollständig (32 Zeilen).

    Änderung 1: Import hinzufügen nach den bestehenden Imports (nach LocalhostCanonicalRedirect-Import):
      import { AppShellClientWrapper } from '@/components/layout/AppShellClientWrapper'

    Änderung 2: In RootLayout() im body, {children} ersetzen durch:
      <AppShellClientWrapper>{children}</AppShellClientWrapper>

    NICHT ändern: 'use client' darf NICHT hinzugefügt werden. metadata-Export bleibt. HTML-lang="de" bleibt. LocalhostCanonicalRedirect und AuthSessionSwitchGuard bleiben als Geschwister-Elemente.

    Minimales Diff: Nur 2 Zeilen ändern (1 Import-Zeile hinzufügen, 1 JSX-Zeile ändern).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    layout.tsx enthält "AppShellClientWrapper" (grep bestätigt).
    layout.tsx hat kein 'use client' (grep -c "use client" layout.tsx ergibt 0).
    TypeScript kompiliert ohne Fehler.
  </done>
</task>

<task type="auto">
  <name>Task 2: Doppel-Shell aus me/profile/page.tsx entfernen</name>
  <files>frontend/src/app/me/profile/page.tsx</files>
  <read_first>
    frontend/src/app/me/profile/page.tsx
  </read_first>
  <action>
    Lies me/profile/page.tsx vollständig. Finde Zeile 287 (AppShell return-Wrapper).

    Schritt 1: AppShell-Wrapper entfernen. Die return-Anweisung der Komponente gibt aktuell zurück:
      return (
        <AppShell currentPath="/me/profile" user={shellUser} canAccessAdmin={canAccessAdmin(profile)}>
          <main className={styles.page}>
            {/* ... Seiteninhalt ... */}
          </main>
        </AppShell>
      )
    Ändere zu:
      return (
        <main className={styles.page}>
          {/* ... gleicher Seiteninhalt unverändert ... */}
        </main>
      )

    Schritt 2: AppShell-Import-Zeile aus den Imports entfernen (Zeile mit import { AppShell } from '@/components/layout/AppShell' oder ähnlich).

    Schritt 3: Prüfe ob shellUser und canAccessAdmin (Hilfsfunktion) noch an anderen Stellen in der Datei genutzt werden — wenn sie NUR für den AppShell-Wrapper existierten, dann diese Variablen/Funktionen ebenfalls entfernen. Wenn sie für eigene Profillogik genutzt werden (z.B. interne Avatar-Anzeige), behalten.

    Schritt 4: TypeScript-Compiler zeigt an ob ungenutzte Imports/Variablen verblieben sind.

    NICHT ändern: Alle anderen Profilseiten-Inhalte (Formulare, Daten, Hooks, API-Aufrufe für die eigene Profilseite). Die Seite selbst bleibt vollständig funktional — sie rendert nur nicht mehr ihren eigenen Shell-Wrapper.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    me/profile/page.tsx enthält kein AppShell-JSX-Element mehr (grep -c "AppShell" ergibt 0 oder nur import-freie Zeilen).
    TypeScript kompiliert ohne Fehler.
    Die Seite gibt direkt main zurück ohne AppShell-Wrapper.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Root-Layout-Integration abgeschlossen:
    - AppShellClientWrapper ist in layout.tsx eingebaut → alle Seiten erhalten globale Nav-Drawer
    - me/profile/page.tsx hat die Eigeninkludierung von AppShell entfernt
    - TypeScript kompiliert ohne Fehler (bereits automated verifiziert)
  </what-built>
  <how-to-verify>
    1. Frontend starten: cd frontend && npm run dev (oder Docker Compose, falls bevorzugt)
    2. Browser öffnen, http://localhost:3002 aufrufen
    3. Eingeloggt — /me/profile aufrufen: Nur EINE Navigation/Drawer sehen (kein doppelter Drawer)
    4. Nicht eingeloggt — /anime aufrufen: Drawer zeigt anonymen Modus (Anmelden/Registrieren-Buttons im Footer wenn Drawer geöffnet)
    5. /auth aufrufen: Drawer vorhanden (Root-Layout-Integration)
    6. Desktop (>860px): Edge-Strip am linken Rand sichtbar (16px Glasrand), Hover öffnet Drawer
    7. Mobile (≤860px): Burger-Button im Header sichtbar, Klick öffnet Slide-over-Drawer
    8. ESC drücken wenn Drawer offen: Drawer schließt sich
  </how-to-verify>
  <resume-signal>Tippe "approved" wenn alle Punkte korrekt, oder beschreibe gefundene Probleme</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Server Component → Client Component | layout.tsx bleibt Server Component; AppShellClientWrapper übernimmt Client-State |
| Alle Seiten → globale Shell | Root-Layout-Integration betrifft alle Routen inkl. /auth/* |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Elevation of Privilege | Globale Shell auf allen Seiten | mitigate | canAccessAdmin kommt aus Backend-Capabilities im Wrapper; Route /admin bleibt server-seitig geschützt; Shell zeigt nur Nav-Link |
| T-54-02 | Information Disclosure | layout.tsx Props | mitigate | Kein authToken in Root-Layout oder AppShellClientWrapper Props; Token-free UI Boundary bleibt eingehalten |
| T-54-SC | Tampering | npm installs | accept | Keine neuen Pakete |
</threat_model>

<verification>
Nach Task 1 + 2 (vor Checkpoint):

1. `grep -n "AppShellClientWrapper" frontend/src/app/layout.tsx` — Treffer in Import und JSX
2. `grep -c "'use client'" frontend/src/app/layout.tsx` — ergibt 0 (Root-Layout bleibt Server Component)
3. `grep -c "AppShell" frontend/src/app/me/profile/page.tsx` — ergibt 0 oder nur in Kommentaren
4. `cd frontend && npx tsc --noEmit 2>&1 | head -10` — keine TypeScript-Fehler
5. `cd frontend && npx vitest run 2>&1 | tail -20` — gesamte Test-Suite grün

Checkpoint (manuell):
- Kein doppelter Drawer auf /me/profile
- Edge-Strip sichtbar auf Desktop
- Anonymer Drawer-Footer auf nicht-eingeloggten Seiten
</verification>

<success_criteria>
1. layout.tsx importiert AppShellClientWrapper und wraps {children} damit
2. layout.tsx hat kein 'use client'
3. me/profile/page.tsx hat keinen AppShell-Wrapper mehr
4. TypeScript-Compile ohne Fehler
5. Gesamte Vitest-Suite grün
6. Manuell verifiziert: kein doppelter Drawer auf /me/profile
</success_criteria>

<output>
Erstelle `.planning/phases/54-globale-nav-drawer-und-layout-verdrahtung/54-03-SUMMARY.md` wenn fertig
</output>
