---
phase: 54-globale-nav-drawer-und-layout-verdrahtung
plan: 04
type: execute
wave: 2
depends_on:
  - 54-01
files_modified:
  - frontend/src/app/dev/ui-system/page.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-05
  - D-08

must_haves:
  truths:
    - "Die Playground-Seite /dev/ui-system hat eine neue Demo-Sektion für den Nav-Drawer"
    - "Demo zeigt AppShell im anonymen Modus (Anmelden/Registrieren-Footer sichtbar)"
    - "Demo zeigt AppShell im eingeloggten Modus (Avatar-Initialen-Fallback sichtbar)"
    - "Demo zeigt AppShell mit Avatar-Bild (avatarUrl gesetzt)"
    - "Demo-Toggle ermöglicht Wechsel zwischen anonym/eingeloggt ohne Seiten-Reload"
    - "Drawer-Demo isoliert von Root-Layout — kein doppelter Shell-Render"
  artifacts:
    - path: "frontend/src/app/dev/ui-system/page.tsx"
      provides: "Playground-Demo für AppShell Drawer"
      contains: "AppShell"
  key_links:
    - from: "frontend/src/app/dev/ui-system/page.tsx"
      to: "frontend/src/components/layout/AppShell"
      via: "import { AppShell } from '@/components/layout/AppShell'"
      pattern: "AppShell"
---

<objective>
Playground-Demo für den neuen AppShell-Drawer in /dev/ui-system ergänzen.

Zweck: Die Demo dient als isolierte Entwicklungs- und Überprüfungsfläche für alle Drawer-States (anonym/eingeloggt, mit/ohne Avatar, Edge-Strip). Parallel zu Plan 03 (keine Datei-Überschneidung).

Ausgabe: /dev/ui-system/page.tsx mit neuer "Nav Drawer — Globale Shell"-Demo-Sektion, Mode-Toggle und allen drei Drawer-States dargestellt.
</objective>

<execution_context>
@C:\Users\admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/54-globale-nav-drawer-und-layout-verdrahtung/54-CONTEXT.md
@.planning/phases/54-globale-nav-drawer-und-layout-verdrahtung/54-PATTERNS.md
@.planning/phases/54-globale-nav-drawer-und-layout-verdrahtung/54-01-SUMMARY.md

<interfaces>
Bestehende dev/ui-system/page.tsx Struktur (aus PATTERNS.md):
  - Datei ist sehr groß (~1100+ Zeilen)
  - Bestehende Card-Komponenten: Card, SectionHeader, Button, Badge usw.
  - Bestehende State-Hooks auf Seiten-Ebene (z.B. drawerOpen auf Zeile 166)
  - SectionHeader-Muster mit eyebrow (nummeriert), title, description
  - Letzte Demo-Sektion ist ca. Sektion 08 → neue Sektion wäre 09
  - Demo-Cards haben variant="section" auf Card

AppShell (nach Plan 01) exportierte Props:
  mode?: 'authenticated' | 'anonymous'
  currentPath?: string
  user?: { displayName?: string | null; email?: string | null; avatarUrl?: string | null } | null
  canAccessAdmin?: boolean
  children: ReactNode

Bestehender drawerOpen-State (Zeile 166):
  const [drawerOpen, setDrawerOpen] = useState(false)
  → Neuer shellDemoMode-State hinzufügen: useState<'anonymous' | 'authenticated'>('authenticated')
  → Neuer shellDemoAvatar-State: useState<boolean>(true)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppShell-Demo-Sektion in /dev/ui-system ergänzen</name>
  <files>frontend/src/app/dev/ui-system/page.tsx</files>
  <read_first>
    frontend/src/app/dev/ui-system/page.tsx
  </read_first>
  <action>
    Lies dev/ui-system/page.tsx vollständig. Identifiziere:
    - Den Import-Block oben (wo AppShell-Import hinzugefügt werden muss)
    - Den State-Block (wo shellDemoMode und shellDemoAvatar States hinzukommen)
    - Die letzte Card-Sektion (wo die neue Demo-Card am Ende des Seitenrumpfs eingefügt wird)
    - Die bestehende Nummerierung der SectionHeader eyebrow-Props (um die neue Nummer zu bestimmen)

    Änderung 1 — Import hinzufügen (nach bestehenden UI-Component-Imports, alphabetisch oder am Ende des import-Blocks):
      import { AppShell } from '@/components/layout/AppShell'

    Änderung 2 — States hinzufügen (nach bestehenden State-Hooks):
      const [shellDemoMode, setShellDemoMode] = useState<'anonymous' | 'authenticated'>('authenticated')
      const [shellDemoAvatar, setShellDemoAvatar] = useState(true)

    Änderung 3 — Neue Demo-Sektion am Ende des Seiten-JSX einfügen (vor dem schließenden Tag der äußersten Container-Komponente):

    Card-Sektion "Nav Drawer — Globale Shell" mit SectionHeader eyebrow-Nummer um 1 höher als die letzte bestehende Sektion. description: "AppShell mit Slide-over Drawer, Dual-State (anonym/eingeloggt), Edge-Strip und Glassmorphism."

    Innerhalb der Card:
    a) Mode-Toggle-Zeile: zwei Button-Elemente. Erster: onClick={() => setShellDemoMode('anonymous')} Text "Anonym anzeigen". Zweiter: onClick={() => setShellDemoMode('authenticated')} Text "Eingeloggt anzeigen". (Stil: variant="secondary" auf inaktivem, variant="primary" auf aktivem — oder einfach variant="secondary" für beide.)
    b) Avatar-Toggle: Button onClick={() => setShellDemoAvatar((v) => !v)} Text "Avatar-Bild " + (shellDemoAvatar ? "an" : "aus").
    c) Demo-Container: div mit position: relative, height: 500px, overflow: hidden, border: 1px solid var(--color-border), border-radius: 12px. Darin: AppShell-Komponente mit:
      - mode={shellDemoMode}
      - currentPath="/anime"
      - user={shellDemoMode === 'authenticated' ? { displayName: 'Demo Nutzer', email: 'demo@team4s.de', avatarUrl: shellDemoAvatar ? 'https://picsum.photos/seed/team4s/36/36' : undefined } : null}
      - canAccessAdmin={false}
      - Als children: div mit Text "Seiten-Inhalt hier" in zentrierter, gedimmter Darstellung.

    WICHTIG: AppShell ist auch auf /dev/ui-system seitenübergreifend durch Root-Layout eingebunden (Plan 03). Die Demo-AppShell rendert eine zweite isolierte Shell als visuelles Beispiel — das ist auf einer Demo-Seite bewusst und korrekt. Kein Versuch die Demo-AppShell aus dem Root-Layout auszublenden.

    Datei-Größen-Limit: CLAUDE.md schreibt 450 Zeilen vor. Prüfe ob page.tsx nach der Erweiterung über 450 Zeilen geht. Falls ja: Die Demo-Card-Sektion in eine eigene Komponente NavDrawerDemoSection auslagern und in page.tsx importieren.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    dev/ui-system/page.tsx enthält "AppShell" im Import.
    dev/ui-system/page.tsx enthält "shellDemoMode".
    TypeScript kompiliert ohne Fehler.
    Falls Datei-Limit: NavDrawerDemoSection als eigene Datei extrahiert.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Dev-Only Seite → Production | /dev/ui-system ist eine interne Demo-Seite; kein Security-Impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Elevation of Privilege | Demo canAccessAdmin | accept | Demo-AppShell hat canAccessAdmin={false}; keine echten Berechtigungen exponiert |
| T-54-02 | Information Disclosure | Demo user Props | accept | Demo nutzt Dummy-Daten (demo@team4s.de, picsum-URL); kein echter Token |
| T-54-SC | Tampering | npm installs | accept | Keine neuen Pakete |
</threat_model>

<verification>
Nach Task 1:

1. `grep -n "AppShell" frontend/src/app/dev/ui-system/page.tsx` — Treffer in Import und JSX
2. `grep -n "shellDemoMode" frontend/src/app/dev/ui-system/page.tsx` — Treffer in State-Deklaration und JSX
3. `cd frontend && npx tsc --noEmit 2>&1 | head -10` — keine TypeScript-Fehler
4. `cd frontend && npx vitest run 2>&1 | tail -10` — Suite grün

Manuell (optional):
- /dev/ui-system aufrufen, "Nav Drawer"-Sektion sehen
- Mode-Toggle zwischen Anonym und Eingeloggt wechseln
- Avatar-Toggle an/aus
</verification>

<success_criteria>
1. dev/ui-system/page.tsx importiert AppShell
2. Mode-Toggle (anonym/eingeloggt) wechselt Drawer-Footer sichtbar
3. Avatar-Toggle schaltet zwischen Bild und Initialen-Fallback
4. TypeScript-Compile ohne Fehler
5. Vitest-Suite bleibt grün
</success_criteria>

<output>
Erstelle `.planning/phases/54-globale-nav-drawer-und-layout-verdrahtung/54-04-SUMMARY.md` wenn fertig
</output>
