---
phase: 107-best-tigung-delegation-und-ablehnungslebenszyklus
plan: "04"
type: execute
wave: 3
depends_on: ["107-02"]
files_modified:
  - backend/internal/repository/review_delegation_repository.go
  - backend/internal/repository/review_delegation_repository_test.go
  - backend/internal/repository/review_audit_repository.go
  - backend/internal/repository/review_audit_repository_test.go
autonomous: true
requirements: [P107-SC1, P107-SC4, P107-SC6]
must_haves:
  truths:
    - "P107-SC1 / D-02: Grant/Revoke sperren die exakte Membership und liefern serverseitige Group/User/Member/Status-Snapshots für die Servicepolicy."
    - "P107-SC1 / D-03 / D-07: Grant ist idempotent und ohne Ablaufdatum; Grant/Revoke melden tatsächliche Zeilenänderung, und Revoke entfernt nur das künftige Recht, nie historische Entscheidungen oder Punkte."
    - "P107-SC1 / D-04: Das Repository erweitert die vorhandene Membership-ID und eröffnet keine zweite Mitglieder- oder Delegationsdomäne."
    - "P107-SC4 / D-11: Nur tatsächlich veränderte Grant/Revoke-Mutationen und spätere Decisions besitzen verpflichtende strukturierte Auditmethoden im Caller-Tx; No-op-Retries und Reads bleiben schreibfrei."
    - "P107-SC4 / D-12: Reject- und Override-Reason-Body können nur separat nach Zweck eingefügt und später gelöscht werden; kein strukturiertes Feld oder Log enthält sie."
    - "P107-SC4 / D-13: ActorKind unterscheidet app_user und system ohne Fake-Member."
  artifacts:
    - path: "backend/internal/repository/review_delegation_repository.go"
      provides: "Tx-gebundene Membership-Locks und idempotente Grant/Revoke-Mutationen mit Changed-Signal"
    - path: "backend/internal/repository/review_audit_repository.go"
      provides: "Typisierte immutable Audit- und separate Reason-Schreibseams"
  key_links:
    - from: "ReviewDelegationRepository.LockMembership"
      to: "fansub_group_members"
      via: "SELECT ... FOR UPDATE"
      pattern: "FOR UPDATE"
    - from: "ReviewAuditRepository.InsertReason"
      to: "review_reason_texts"
      via: "separate Methode ohne JSON-/Log-Kopie"
      pattern: "review_reason_texts"
---

<objective>
Die tx-gebundenen Delegations- und Audit-Repositories als enge, domänenneutrale Mutationsbausteine implementieren.

Purpose: Plan 06 kann Autorisierung, Grant/Revoke und unverzichtbares Audit in einer Transaktion orchestrieren.
Output: Getestete Membership-Locks, idempotente Grant-Mutationen, immutable Audit-Events und scrub-fähige Reason-Texte.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/107-best-tigung-delegation-und-ablehnungslebenszyklus/107-CONTEXT.md
@.planning/phases/107-best-tigung-delegation-und-ablehnungslebenszyklus/107-RESEARCH.md
@.planning/phases/107-best-tigung-delegation-und-ablehnungslebenszyklus/107-PATTERNS.md
@.planning/phases/107-best-tigung-delegation-und-ablehnungslebenszyklus/107-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Membership-gebundene Grant/Revoke-Repositories implementieren</name>
  <read_first>
    - AGENTS.md
    - docs/engineering/implementation-contract.md
    - .planning/phases/107-best-tigung-delegation-und-ablehnungslebenszyklus/107-CONTEXT.md
    - .planning/phases/107-best-tigung-delegation-und-ablehnungslebenszyklus/107-RESEARCH.md
    - .planning/phases/107-best-tigung-delegation-und-ablehnungslebenszyklus/107-PATTERNS.md
    - backend/internal/repository/authz_capability_mutations.go
    - backend/internal/repository/fansub_group_app_members_repository.go
    - backend/internal/repository/member_claims_role_activation_repository.go
    - backend/internal/repository/point_ledger_repository_test.go
    - database/migrations/0134_review_foundation.up.sql
  </read_first>
  <files>backend/internal/repository/review_delegation_repository.go, backend/internal/repository/review_delegation_repository_test.go</files>
  <behavior>
    - "LockMembership liefert Membership-ID, Group-ID, App-User-ID, Member-ID, Membership- und App-User-Status sowie verified-Claim-Status unter FOR UPDATE."
    - "GrantAction/RevokeAction geben `changed` aus `RowsAffected` zurück; Grant derselben Action und Revoke einer fehlenden Action liefern false."
    - "Grant/Revoke beginnen oder beenden keine Transaktion."
    - "Membership-Inaktivität entfernt die gespeicherte Grant-Zeile nicht."
  </behavior>
  <action>
Implementiere `ReviewDelegationRepository` auf caller-supplied `repository.DBTX`. `LockMembership` sperrt genau die adressierte `fansub_group_members`-Zeile `FOR UPDATE` und liefert alle Policy-Snapshots; keine separate Group-/Member-Lookup-Kopie. `GrantAction` schreibt `(fansub_group_member_id, action_code, granted_by_app_user_id, granted_at)` mit `ON CONFLICT DO NOTHING`; `RevokeAction` löscht nur genau dieses Paar. Beide Methoden geben `(changed bool, err error)` zurück und leiten `changed` ausschließlich aus `RowsAffected()==1` ab: wiederholter Grant beziehungsweise Revoke einer fehlenden Zeile sind erfolgreiche No-ops mit `changed=false`. Repositorymethoden validieren positive IDs und nichtleere Action-Codes, beginnen/committen/rollbacken aber nie.

Die Servicepolicy aus D-02 (same group, manager/platform, active+verified, kein Weiterdelegieren) bleibt bewusst in Plan 06; die Repositorytests beweisen dafür vollständige Snapshots und Lock-Ordering. Füge kein expires_at, assigned_to, claimed_by oder sonstigen Lifecycle hinzu.
  </action>
  <verify>
    <automated>cd backend; go test ./internal/repository -run 'TestPhase107ReviewDelegation.*(Lock|Grant|Revoke|Idempotent|Inactive)' -count=1</automated>
  </verify>
  <done>Grant/Revoke sind kleine caller-owned-Tx-Mutationen auf der kanonischen Membership, melden tatsächliche Änderung präzise und bewahren historische Daten außerhalb der aktiven Grant-Zeile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Typed Audit und getrennten Reason-Text implementieren</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/107-best-tigung-delegation-und-ablehnungslebenszyklus/107-CONTEXT.md
    - .planning/phases/107-best-tigung-delegation-und-ablehnungslebenszyklus/107-PATTERNS.md
    - backend/internal/repository/audit_logs.go
    - backend/internal/repository/point_ledger_repository.go
    - backend/internal/repository/errors.go
    - backend/internal/migrations/phase107_review_foundation_test.go
    - database/migrations/0134_review_foundation.up.sql
  </read_first>
  <files>backend/internal/repository/review_audit_repository.go, backend/internal/repository/review_audit_repository_test.go</files>
  <behavior>
    - "InsertEvent verlangt DB, typisierten EventCode, ActorKind, Target-Snapshots und occurred_at."
    - "ActorKind=system benötigt keine App-User-/Member-ID; app_user benötigt eine positive App-User-ID."
    - "InsertReason akzeptiert typed `reject|override` plus Unicode-nonblank Text und schreibt ausschließlich review_reason_texts."
    - "Readmethoden und fehlgeschlagene Mutationen erzeugen keine Auditzeile."
  </behavior>
  <action>
Definiere typed `ReviewAuditEventCode` mindestens für `delegation.granted`, `delegation.revoked`, `review.confirmed`, `review.rejected`, `review.override`, `review_credit.awarded`, `review_credit.reversed` sowie typed `ReviewAuditActorKind` `app_user|system`. `ReviewAuditRepository` nimmt caller-supplied DBTX und schreibt strukturierte Spalten in `review_audit_events`; nil Repository/DB ist `repository.ErrValidation`, niemals best-effort success. `InsertReason` verlangt `ReviewReasonKindReject|ReviewReasonKindOverride` und Unicode-nonblank Freitext, schreibt beides ausschließlich nach `review_reason_texts` und gibt den Text nicht in Fehlern oder Logs aus. Ein Plattform-Self-Reject kann deshalb getrennte Reason-Kinder für `review.rejected` und `review.override` besitzen. Optionales Lesen dient Tests/Verifikation und schreibt kein Audit.

Reserviere keine Cleanup-Implementierung und emittiere keine späteren Submit/Edit/Resubmit/Publish/Cleanup-Events. Das Schema kann spätere Codes aufnehmen, aber Phase 107 schreibt nur Foundation-Mutationen. Tests gegen disposable PostgreSQL beweisen Parent-Immutability, Reason-Delete ohne Parentänderung, kein Freitext in strukturierten Zeilen und null Audit bei Reads.

Entscheidungstraceability: mandatory Audit nur für tatsächliche Zustandsänderungen und kein Read-Audit implementiert D-11; strukturierte Kategorie sowie getrennte löschbare Reject-/Override-Gründe D-12; typed `system` ohne künstliches Member D-13.
  </action>
  <verify>
    <automated>cd backend; go test ./internal/repository -run 'TestPhase107ReviewAudit.*(Event|SystemActor|Reason|Immutable|ReadBoundary)' -count=1</automated>
  </verify>
  <done>Jede Foundation-Mutation kann im selben Tx verpflichtend strukturiert auditiert werden; Freitext bleibt gezielt scrub-fähig und system actors benötigen kein Fake-Member.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ReviewService -> delegation repository | Service autorisiert; Repository sperrt und mutiert nur exakt adressierte Membership/Action. |
| Mutation -> audit repository | Mutation und Audit müssen denselben Caller-Tx teilen. |
| Free text -> structured history | Reason-Body darf die löschbare Kindtabelle nicht verlassen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-14 | Tampering | Grant/Revoke | mitigate | Exakter Membership-FOR-UPDATE-Lock und Unique-Paar. |
| T-107-15 | Repudiation | Audit writes | mitigate | Pflichtfelder, typed Codes und Fehler statt best-effort. |
| T-107-16 | Information disclosure | Reason text | mitigate | Separates Insert, keine JSON-/Log-/Error-Kopie, gezielter DELETE. |
| T-107-17 | Spoofing | System actor | mitigate | Expliziter ActorKind statt erfundenem Member/App-User. |
</threat_model>

<verification>
- Repositorytests sind ohne DB opt-in kompilierbar und mit opt-in DB vollständig grün.
- `go vet ./internal/repository` ist grün.
- Kein Service-, Handler-, UI-, Cleanup- oder Domainadapter-Wiring wurde vorweggenommen.
</verification>

<success_criteria>
- Plan 06 kann Grant/Revoke plus Audit atomar ausführen.
- Strukturierte Historie ist unveränderlich; Reason-Freitext bleibt separat löschbar.
- Reads erzeugen kein Audit.
</success_criteria>

<output>
After completion, create `.planning/phases/107-best-tigung-delegation-und-ablehnungslebenszyklus/107-04-SUMMARY.md`
</output>
