---
phase: 69-fansub-contributions-contract-und-permission-haertung
plan: 05
type: execute
wave: 4
depends_on:
  - 69-03
  - 69-04
files_modified:
  - shared/contracts/fansubs.yaml
  - shared/contracts/admin-content.yaml
autonomous: true
requirements:
  - P69-SC9

must_haves:
  truths:
    - "shared/contracts/fansubs.yaml enthaelt Endpunkt-Definitionen fuer group-members (GET/POST/PATCH/DELETE) und member-roles (GET/POST/PATCH/DELETE)"
    - "shared/contracts/admin-content.yaml enthaelt Endpunkt-Definitionen fuer anime/:animeId/contributions (GET/POST/PATCH/DELETE)"
    - "Alle Contracts beschreiben {data: ...}-Envelope konsistent mit Backend-Verhalten nach Wave 2/3"
    - "Permission-Anforderungen (CanForFansubGroup) sind in auth.permission dokumentiert"
    - "Cross-Group-Guard und Member-Auto-Create sind in den Endpunkt-Notes dokumentiert"
  artifacts:
    - path: "shared/contracts/fansubs.yaml"
      provides: "8 neue Endpunkt-Eintraege fuer group-members und member-roles"
      contains: "fansub-group-members-list"
    - path: "shared/contracts/admin-content.yaml"
      provides: "4 neue Endpunkt-Eintraege fuer anime/:animeId/contributions"
      contains: "admin-fansub-anime-contributions-list"
  key_links:
    - from: "shared/contracts/fansubs.yaml — fansub-group-members-create"
      to: "POST /api/v1/admin/fansubs/:id/group-members"
      via: "request_body: CreateGroupMemberRequest (display_name, joined_year, left_year, status)"
    - from: "shared/contracts/admin-content.yaml — admin-fansub-anime-contributions-create"
      to: "POST /api/v1/admin/fansubs/:id/anime/:animeId/contributions"
      via: "ON CONFLICT DO UPDATE semantics; status aus Request"
---

<objective>
Fuegt OpenAPI-konforme YAML-Contract-Eintraege fuer die drei neuen Endpunkt-Gruppen ein:
- 8 Eintraege fuer group-members und member-roles in shared/contracts/fansubs.yaml
- 4 Eintraege fuer anime/:animeId/contributions in shared/contracts/admin-content.yaml

Purpose: Schliesst F9 (fehlende Contracts). Contracts sind die Wahrheitsquelle fuer kuenftige
Frontend/Backend-Entwicklung an diesen Endpunkten.
Output: Aktualisierte YAML-Vertragsdateien, konsistent mit dem nach Wave 2 implementierten Backend.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/PROJECT.md
@C:/Users/admin/Documents/Team4s/.planning/ROADMAP.md
@C:/Users/admin/Documents/Team4s/.planning/phases/69-fansub-contributions-contract-und-permission-haertung/69-CONTEXT.md
</context>

<interfaces>
<!-- Contract-Format-Vorlage aus admin-content.yaml und fansubs.yaml -->
<!-- auth-Block mit permission-Feld und Standard-Fehlercodes -->
<!-- response-Block mit status und type -->
<!-- request_body mit required, type, example -->

<!-- Backend-Endpunkte (nach Wave 2) -->
GET  /api/v1/admin/fansubs/:id/group-members
     -> 200 {data: HistGroupMemberDisplayRow[]} (ListByFansubGroupWithDisplay)
     permission: fansub_group.members.view

POST /api/v1/admin/fansubs/:id/group-members
     request: {display_name, joined_year?, left_year?, status, visibility?}
     -> 201 {data: HistGroupMemberDisplayRow} (CreateWithAutoMember)
     permission: fansub_group.members.manage

PATCH /api/v1/admin/fansubs/:id/group-members/:memberId
     request: {joined_year?, left_year?, status?, visibility?}
     -> 200 {data: HistGroupMemberRow}
     permission: fansub_group.members.manage

DELETE /api/v1/admin/fansubs/:id/group-members/:memberId
     -> 204
     permission: fansub_group.members.manage

GET  /api/v1/admin/fansubs/:id/member-roles?member_id=N
     -> 200 {data: HistGroupMemberRoleRow[]}
     permission: fansub_group.members.view
     note: member_id query param REQUIRED (400 ohne)

POST /api/v1/admin/fansubs/:id/member-roles
     request: {hist_fansub_group_member_id, role_code, started_year?, ended_year?, status, visibility?}
     -> 201 {data: HistGroupMemberRoleRow}
     permission: fansub_group.members.manage
     note: Cross-Group-Guard: member muss zur fansubgruppe gehoeren

PATCH /api/v1/admin/fansubs/:id/member-roles/:roleId
     request: {started_year?, ended_year?, status?, visibility?, source_note?}
     -> 200 {data: HistGroupMemberRoleRow}
     permission: fansub_group.members.manage

DELETE /api/v1/admin/fansubs/:id/member-roles/:roleId
     -> 204
     permission: fansub_group.members.manage

GET  /api/v1/admin/fansubs/:id/anime/:animeId/contributions
     -> 200 {data: AnimeContributionDisplayRow[]}
     permission: fansub_group.members.view

POST /api/v1/admin/fansubs/:id/anime/:animeId/contributions
     request: UpsertAnimeContributionRequest
     -> 201 {data: AnimeContributionDisplayRow}
     permission: fansub_group.members.manage
     semantics: ON CONFLICT (group, anime, member) DO UPDATE

PATCH /api/v1/admin/fansubs/:id/anime/:animeId/contributions/:contributionId
     request: {role_codes?, started_year?, ended_year?, note?, is_public_on_anime_page?, is_public_on_member_profile?, status?}
     -> 200 {data: AnimeContributionDisplayRow}
     permission: fansub_group.members.manage

DELETE /api/v1/admin/fansubs/:id/anime/:animeId/contributions/:contributionId
     -> 204
     permission: fansub_group.members.manage
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: fansubs.yaml — 8 Endpunkt-Eintraege fuer group-members und member-roles</name>
  <files>shared/contracts/fansubs.yaml</files>
  <action>
  Lies shared/contracts/fansubs.yaml vollstaendig um den bestehenden Stil zu verstehen.

  Fuege am Ende der endpoints-Liste (vor dem letzten Abschnitt falls vorhanden, sonst ans Ende)
  8 neue Eintraege hinzu. Orientiere dich am Format der bestehenden Eintraege.

  Gemeinsamer auth-Block fuer alle 8 Eintraege:
      auth:
        required: true
        header:
          name: Authorization
          format: Bearer <access token>
        unauthenticated_status: 401
        unauthenticated_response:
          error:
            message: "anmeldung erforderlich"
        forbidden_status: 403
        forbidden_response:
          error:
            message: "keine berechtigung"

  Eintraege in Reihenfolge:

  1. fansub-group-members-list (GET /api/v1/admin/fansubs/:id/group-members):
     permission: fansub_group.members.view
     response: 200, data: HistGroupMemberDisplayRow[]
     notes: Mitglieder angereichert mit display_name aus members-Tabelle

  2. fansub-group-members-create (POST /api/v1/admin/fansubs/:id/group-members):
     permission: fansub_group.members.manage
     request_body: display_name (required), joined_year (nullable int), left_year (nullable int),
                   status (required, enum: active/alumni), visibility (optional, enum: public/internal)
     response: 201, data: HistGroupMemberDisplayRow
     notes: Legt automatisch einen members-Eintrag an (nickname=display_name, user_id=null).
            Cross-Group-Schutz via Composite-FK in Migration 0088.
     errors: 409 wenn Mitglied bereits in der Gruppe

  3. fansub-group-members-update (PATCH /api/v1/admin/fansubs/:id/group-members/:memberId):
     permission: fansub_group.members.manage
     request_body: joined_year?, left_year?, status?, visibility? (alle optional/nullable)
     response: 200, data: HistGroupMemberRow
     errors: 404 wenn Eintrag nicht gefunden

  4. fansub-group-members-delete (DELETE /api/v1/admin/fansubs/:id/group-members/:memberId):
     permission: fansub_group.members.manage
     response: 204
     errors: 404 wenn Eintrag nicht gefunden

  5. fansub-member-roles-list (GET /api/v1/admin/fansubs/:id/member-roles):
     permission: fansub_group.members.view
     query_params: member_id (int64, REQUIRED)
     response: 200, data: HistGroupMemberRoleRow[]
     errors: 400 wenn member_id fehlt

  6. fansub-member-roles-create (POST /api/v1/admin/fansubs/:id/member-roles):
     permission: fansub_group.members.manage
     request_body: hist_fansub_group_member_id (required int64), role_code (required string),
                   started_year (nullable int), ended_year (nullable int),
                   status (required, enum: historical/confirmed),
                   visibility (optional, enum: public/internal), source_note (optional string)
     response: 201, data: HistGroupMemberRoleRow
     notes: Cross-Group-Guard: hist_fansub_group_member_id muss zur Fansub-Gruppe der Route gehoeren.
            role_code muss ein gueltiger group_history-Kontextcode sein.
     errors: 404 wenn Mitgliedschaft nicht gefunden; 422 wenn role_code unbekannt oder Cross-Group

  7. fansub-member-roles-update (PATCH /api/v1/admin/fansubs/:id/member-roles/:roleId):
     permission: fansub_group.members.manage
     request_body: started_year?, ended_year?, status?, visibility?, source_note? (alle optional)
     response: 200, data: HistGroupMemberRoleRow
     errors: 404 wenn Rolleneintrag nicht gefunden

  8. fansub-member-roles-delete (DELETE /api/v1/admin/fansubs/:id/member-roles/:roleId):
     permission: fansub_group.members.manage
     response: 204
     errors: 404 wenn Rolleneintrag nicht gefunden
  </action>
  <verify>
    <automated>grep -c "name: fansub-group-members\|name: fansub-member-roles" shared/contracts/fansubs.yaml</automated>
  </verify>
  <done>
  - 8 neue Eintraege in shared/contracts/fansubs.yaml
  - Alle haben auth-Block mit permission-Feld
  - member_id als required query param in fansub-member-roles-list
  - Auto-Create-Notiz in fansub-group-members-create
  - YAML syntaktisch korrekt (keine Einrueck-Fehler)
  </done>
</task>

<task type="auto">
  <name>Task 2: admin-content.yaml — 4 Eintraege fuer anime/:animeId/contributions</name>
  <files>shared/contracts/admin-content.yaml</files>
  <action>
  Lies shared/contracts/admin-content.yaml vollstaendig um den bestehenden Stil zu verstehen.

  Fuege am Ende der endpoints-Liste 4 neue Eintraege hinzu.

  Gemeinsamer auth-Block: analog zu Task 1 (Authorization Bearer, 401/403-Fehler).

  Eintraege in Reihenfolge:

  1. admin-fansub-anime-contributions-list
     (GET /api/v1/admin/fansubs/:id/anime/:animeId/contributions):
     permission: fansub_group.members.view
     response: 200, data: AnimeContributionDisplayRow[]
     notes: Beitraege angereichert mit member_display_name aus members-Tabelle

  2. admin-fansub-anime-contributions-create
     (POST /api/v1/admin/fansubs/:id/anime/:animeId/contributions):
     permission: fansub_group.members.manage
     request_body:
       fansub_group_member_id (required int64)
       role_codes (required string[], anime_contribution-Kontext-Codes)
       started_year (nullable int)
       ended_year (nullable int)
       note (nullable string)
       is_public_on_anime_page (required bool)
       is_public_on_member_profile (required bool)
       status (required string, enum: draft/proposed/confirmed/disputed/hidden)
     response: 201, data: AnimeContributionDisplayRow
     semantics: ON CONFLICT (fansub_group_id, anime_id, fansub_group_member_id) DO UPDATE —
                wiederholter POST fuer denselben Member aktualisiert statt einen Duplikat anzulegen.
     notes: Cross-Group-Guard: fansub_group_member_id muss zur Fansub-Gruppe gehoeren.
            Duplikat-Schutz via UNIQUE-Constraint (Migration 0088).
     errors: 404 wenn Gruppe/Anime/Mitglied nicht gefunden; 422 wenn Cross-Group oder ungueltiger role_code

  3. admin-fansub-anime-contributions-update
     (PATCH /api/v1/admin/fansubs/:id/anime/:animeId/contributions/:contributionId):
     permission: fansub_group.members.manage
     request_body:
       role_codes (optional string[])
       started_year (optional nullable int)
       ended_year (optional nullable int)
       note (optional nullable string)
       is_public_on_anime_page (optional bool)
       is_public_on_member_profile (optional bool)
       status (optional string, enum: draft/proposed/confirmed/disputed/hidden)
     response: 200, data: AnimeContributionDisplayRow
     errors: 404 wenn Beitrag nicht gefunden; 422 wenn ungueltiger role_code

  4. admin-fansub-anime-contributions-delete
     (DELETE /api/v1/admin/fansubs/:id/anime/:animeId/contributions/:contributionId):
     permission: fansub_group.members.manage
     response: 204
     errors: 404 wenn Beitrag nicht gefunden
  </action>
  <verify>
    <automated>grep -c "name: admin-fansub-anime-contributions" shared/contracts/admin-content.yaml</automated>
  </verify>
  <done>
  - 4 neue Eintraege in shared/contracts/admin-content.yaml
  - Alle haben auth-Block mit permission-Feld
  - Upsert-Semantik in admin-fansub-anime-contributions-create dokumentiert
  - Status-Enum vollstaendig (draft/proposed/confirmed/disputed/hidden)
  - YAML syntaktisch korrekt
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Contracts | YAML-Contracts sind Dokumentation, keine Laufzeitdurchsetzung; echte Absicherung erfolgt im Handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-69-12 | Information Disclosure | Contract beschreibt interne Fehlermeldungen | accept | Fehlermeldungen sind generisch (kein Stack-Trace); bewusste Design-Entscheidung |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | Keine Paketinstallation |
</threat_model>

<verification>
Nach Implementierung:
1. grep -c "name: fansub-group-members\|name: fansub-member-roles" shared/contracts/fansubs.yaml -- gibt 8 aus
2. grep -c "name: admin-fansub-anime-contributions" shared/contracts/admin-content.yaml -- gibt 4 aus
3. grep "member_id" shared/contracts/fansubs.yaml -- Treffer in fansub-member-roles-list (required query param)
4. grep "ON CONFLICT\|upsert\|DO UPDATE" shared/contracts/admin-content.yaml -- Treffer in contributions-create
5. grep "fansub_group.members" shared/contracts/fansubs.yaml -- mindestens 8 Treffer (je ein permission-Eintrag)
</verification>

<success_criteria>
- fansubs.yaml: 8 neue Eintraege fuer group-members (4) und member-roles (4)
- admin-content.yaml: 4 neue Eintraege fuer anime/:animeId/contributions
- Alle Contracts: {data: ...}-Envelope, permission-Felder, Cross-Group-Notizen
- Upsert-Semantik in contributions-create dokumentiert
- YAML syntaktisch korrekt
</success_criteria>

<output>
Erstelle .planning/phases/69-fansub-contributions-contract-und-permission-haertung/69-05-SUMMARY.md wenn fertig.
</output>
