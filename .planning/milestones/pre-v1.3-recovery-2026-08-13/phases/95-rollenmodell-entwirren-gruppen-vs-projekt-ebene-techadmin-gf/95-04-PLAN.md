---
phase: 95-rollenmodell-entwirren-gruppen-vs-projekt-ebene-techadmin-gf
plan: 04
type: execute
wave: 2
depends_on:
  - 95-01
files_modified:
  - backend/internal/handlers/admin_capability_handler.go
  - backend/internal/handlers/admin_capability_handler_test.go
autonomous: true
requirements:
  - D-15

must_haves:
  truths:
    - "D-15 (WR-01): admin_capability_handler.go hält Interface-Felder statt *repository.AuthzRepository"
    - "D-15: admin_capability_handler_test.go enthält keinen adminCapabilityHandlerWithStubs-Struct mehr (Z.506-626)"
    - "D-15: Tests konstruieren NewAdminCapabilityHandler direkt mit Interface-Stubs"
    - "GrantCapability-Test trifft den Produktions-Handler (nicht Stub-Kopie)"
    - "Audit-Log-Schreibung im Produktions-Handler bleibt erhalten (Divergenz zu bisherigem Stub)"
  artifacts:
    - path: "backend/internal/handlers/admin_capability_handler.go"
      provides: "Interface-Felder statt konkrete Repository-Typen; neue Interfaces capabilityPermissionSvc + capabilityAuditRepo"
    - path: "backend/internal/handlers/admin_capability_handler_test.go"
      provides: "Tests gegen Produktions-Handler; Stub-Struct adminCapabilityHandlerWithStubs entfernt"
  key_links:
    - from: "backend/internal/handlers/admin_capability_handler_test.go"
      to: "NewAdminCapabilityHandler"
      via: "Tests konstruieren Handler direkt mit Interface-Stubs"
      pattern: "NewAdminCapabilityHandler"
---

<objective>
Plan 04 (Wave 2): Interface-Refaktorierung WR-01 (D-15)

Der `AdminCapabilityHandler` hält heute konkrete `*repository.AuthzRepository`-Felder statt Interfaces.
Das zwingt Tests dazu, einen duplizierten Stub-Handler (`adminCapabilityHandlerWithStubs`, Z.506-626)
zu führen, der in Divergenz zum Produktions-Code steht (Audit-Log fehlt im Stub-Revoke, Z.620-625).

Dieser Plan: Handler bekommt Interface-Felder (`capabilityAuthzRepo`, `capabilityMutationRepo`,
neue `capabilityPermissionSvc`, `capabilityAuditRepo`), Tests nutzen direkt `NewAdminCapabilityHandler`.
Der Stub-Struct wird gelöscht.

Läuft parallel zu Plan 02 und Plan 03 (keine gemeinsamen Dateien).

Purpose: Testqualität-Fix — Tests sollen die echte Audit-Log-Schreibung abdecken (WR-01).
Output: 2 modifizierte Dateien, Stub-Struct entfernt, Tests gegen Produktions-Handler.
</objective>

<execution_context>
@C:\Users\admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/95-rollenmodell-entwirren-gruppen-vs-projekt-ebene-techadmin-gf/95-CONTEXT.md
@.planning/phases/95-rollenmodell-entwirren-gruppen-vs-projekt-ebene-techadmin-gf/95-RESEARCH.md
@.planning/phases/95-rollenmodell-entwirren-gruppen-vs-projekt-ebene-techadmin-gf/95-PATTERNS.md
@.planning/phases/95-rollenmodell-entwirren-gruppen-vs-projekt-ebene-techadmin-gf/95-01-SUMMARY.md

<interfaces>
<!-- admin_capability_handler.go aktueller Zustand (zu lesen vor Änderung): -->
<!-- Z.1-14: package + imports -->
<!-- Z.15-25: Interfaces capabilityAuthzRepo, capabilityMutationRepo (bereits deklariert!) -->
<!-- Z.28-32: Struct AdminCapabilityHandler mit *repository.AuthzRepository (zu ersetzen) -->
<!-- Z.35-45: Konstruktor NewAdminCapabilityHandler(authzRepo, permissionSvc, auditLogRepo) -->
<!-- Z.50-69: ListCapabilityMatrix — nutzt h.authzRepo.requirePlatformAdminIdentity -->
<!-- Z.110-123: GrantCapability — schreibt Audit-Log -->
<!-- Z.190-197: RevokeCapability — schreibt Audit-Log (dieser Pfad fehlt im Stub Z.620-625) -->

<!-- admin_capability_handler_test.go: -->
<!-- Z.1-505: Bestehende Tests (BLEIBEN ERHALTEN) -->
<!-- Z.506-626: adminCapabilityHandlerWithStubs (WIRD GELÖSCHT) -->
<!-- Stub-Felder: stubCapabilityAuthzRepo, stubCapabilityPermissionSvc, captureAuditLogRepo (BLEIBEN als separate Stubs) -->

<!-- main.go Z.412: handlers.NewAdminCapabilityHandler(authzRepo, permissionSvc, auditLogRepo) -->
<!-- Muss nach Refaktorierung angepasst werden (Interface-Parameter statt konkreter Typen — kompatibel da AuthzRepository Interfaces implementiert) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AdminCapabilityHandler auf Interface-Felder umstellen + Konstruktor anpassen</name>
  <files>
    backend/internal/handlers/admin_capability_handler.go
  </files>
  <read_first>
    - backend/internal/handlers/admin_capability_handler.go (vollständig — alle 200 Zeilen)
    - backend/internal/handlers/admin_capability_handler_test.go (Z.506-626 — Stub-Struct der entfernt wird; Z.1-50 für bestehende Stubs)
    - 95-PATTERNS.md Abschnitt "admin_capability_handler.go" (vollständige Interface-Felder-Vorlage)
  </read_first>
  <behavior>
    - Neue Interfaces `capabilityPermissionSvc` und `capabilityAuditRepo` werden direkt in der Handler-Datei deklariert (nach Z.25)
    - AdminCapabilityHandler hält 4 Interface-Felder statt konkrete Typen
    - Konstruktor nimmt 4 Interface-Parameter
    - Alle Handler-Methoden (ListCapabilityMatrix, GrantCapability, RevokeCapability) bleiben funktional identisch
    - main.go-Kompatibilität: *repository.AuthzRepository erfüllt beide Interfaces, *permissions.Service erfüllt capabilityPermissionSvc, *repository.AuditLogRepository erfüllt capabilityAuditRepo
  </behavior>
  <action>
    In `backend/internal/handlers/admin_capability_handler.go`:

    Schritt 1 — Zwei neue Interfaces nach den bestehenden (Z.25) ergänzen:
    `type capabilityPermissionSvc interface { ... }` — Methoden die der Handler nutzt (ReloadCache, CanForFansubGroup oder ähnliches — aus Handler-Code ableiten).
    `type capabilityAuditRepo interface { Write(ctx, AuditLogEntry) error }` — Signatur aus AuditLogRepository.

    Schritt 2 — Struct auf Interface-Felder umstellen (Z.28-32 ersetzen):
    `type AdminCapabilityHandler struct {`
    `    authzRepo    capabilityAuthzRepo`
    `    mutationRepo capabilityMutationRepo`
    `    permissionSvc capabilityPermissionSvc`
    `    auditLogRepo  capabilityAuditRepo`
    `}`

    Schritt 3 — Konstruktor anpassen (Z.35-45 ersetzen):
    Alle 4 Interface-Parameter, return unveränderter Struct-Literal.

    Schritt 4 — Alle Vorkommen von `h.authzRepo.` die durch Interface-Split getrennt wurden
    (z.B. wenn authzRepo bisher sowohl capabilityAuthzRepo als auch capabilityMutationRepo war)
    auf die korrekten Interface-Felder umleiten.

    Keine Logikänderungen in den Handler-Methoden — nur Feld-Typ-Anpassungen.

    `go build ./...` nach jeder Teiländerung prüfen.
  </action>
  <verify>
    <automated>
      cd "C:/Users/admin/Documents/Team4s/backend" && go build ./internal/handlers/... 2>&1 | head -20
    </automated>
  </verify>
  <done>
    `admin_capability_handler.go` kompiliert. Struct-Felder sind Interface-Typen.
    Konstruktor akzeptiert 4 Interface-Parameter.
    `grep -n "\*repository.AuthzRepository" backend/internal/handlers/admin_capability_handler.go` → 0 Treffer.
  </done>
</task>

<task type="auto">
  <name>Task 2: Stub-Struct entfernen + Tests auf NewAdminCapabilityHandler umstellen</name>
  <files>
    backend/internal/handlers/admin_capability_handler_test.go
    backend/cmd/server/main.go
  </files>
  <read_first>
    - backend/internal/handlers/admin_capability_handler_test.go (vollständig — Z.1-626)
    - backend/cmd/server/main.go (Z.412 — NewAdminCapabilityHandler-Aufruf)
    - 95-PATTERNS.md Abschnitt "admin_capability_handler_test.go" (neues Test-Konstruktionsmuster)
  </read_first>
  <action>
    In `backend/internal/handlers/admin_capability_handler_test.go`:

    Schritt 1 — `adminCapabilityHandlerWithStubs`-Struct und alle seine Methoden (Z.506-626)
    vollständig löschen. Keine anderen Zeilen berühren.

    Schritt 2 — Tests Z.1-505 die `adminCapabilityHandlerWithStubs` nutzen umschreiben:
    Statt `h := adminCapabilityHandlerWithStubs{...}` nun:
    `h := NewAdminCapabilityHandler(authzStub, authzStub, permStub, auditStub)`
    (Die bestehenden Stub-Typen `stubCapabilityAuthzRepo`, `stubCapabilityPermissionSvc`,
    `captureAuditLogRepo` BLEIBEN erhalten — nur der Doppel-Handler entfällt.)

    Schritt 3 — Falls Stubs neue Interfaces nicht vollständig implementieren:
    Fehlende Methoden in den bestehenden Stub-Typen ergänzen (kompakteste mögliche Implementierung).

    In `backend/cmd/server/main.go` Z.412:
    Prüfen ob der Konstruktor-Aufruf noch kompatibel ist. Da *repository.AuthzRepository
    `capabilityAuthzRepo` + `capabilityMutationRepo` erfüllt: ggf. Parameter anpassen
    (z.B. authzRepo zweimal übergeben wenn Handler 2 getrennte Interface-Felder erwartet).
  </action>
  <verify>
    <automated>
      cd "C:/Users/admin/Documents/Team4s/backend" && go build ./... 2>&1 | head -20 && go test ./internal/handlers/... -run "TestGrantCapability|TestRevokeCapability|TestListCapabilityMatrix" -v 2>&1 | tail -20
    </automated>
  </verify>
  <done>
    `admin_capability_handler_test.go` enthält keinen `adminCapabilityHandlerWithStubs`-Struct mehr.
    `grep -n "adminCapabilityHandlerWithStubs" backend/internal/handlers/admin_capability_handler_test.go` → 0 Treffer.
    Alle bestehenden Capability-Tests PASS.
    `go build ./...` grün.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-Stub → Produktions-Handler | Vorher: Stub-Divergenz (kein Audit-Log). Nachher: Produktions-Handler mit echter Audit-Log-Schreibung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-04-T | Tampering | Stub-Handler ohne Audit-Log-Schreibung | mitigate | D-15/WR-01: Stub-Struct entfernt; Tests treffen Produktions-Handler mit Audit-Log in GrantCapability (Z.114-123) und RevokeCapability (Z.190-197) |
| T-95-04-SC | Tampering | npm/pip/cargo installs | accept | Keine Paket-Installationen in diesem Plan |
</threat_model>

<verification>
Nach Abschluss beider Tasks:

1. `go build ./...` — grün
2. `grep -n "adminCapabilityHandlerWithStubs" backend/internal/handlers/admin_capability_handler_test.go` → 0 Treffer
3. `grep -n "\*repository.AuthzRepository" backend/internal/handlers/admin_capability_handler.go` → 0 Treffer
4. `go test ./internal/handlers/... -run TestGrantCapability` — PASS
5. `go test ./internal/handlers/... -run TestRevokeCapability` — PASS
</verification>

<success_criteria>
- AdminCapabilityHandler hält Interface-Felder, nicht *repository.AuthzRepository (per D-15)
- adminCapabilityHandlerWithStubs-Struct gelöscht (per D-15/WR-01)
- Alle bestehenden Capability-Tests PASS mit dem Produktions-Handler (per D-15)
- `go build ./...` grün ohne Kompilierfehler
</success_criteria>

<output>
Erstelle `.planning/phases/95-rollenmodell-entwirren-gruppen-vs-projekt-ebene-techadmin-gf/95-04-SUMMARY.md` nach Abschluss.
</output>
