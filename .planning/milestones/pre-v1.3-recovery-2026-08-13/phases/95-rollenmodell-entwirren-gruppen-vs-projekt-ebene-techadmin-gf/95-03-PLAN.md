---
phase: 95-rollenmodell-entwirren-gruppen-vs-projekt-ebene-techadmin-gf
plan: 03
type: execute
wave: 2
depends_on:
  - 95-01
files_modified:
  - backend/internal/repository/hist_group_member_roles_repository.go
  - backend/internal/handlers/fansub_hist_group_member_roles_handler.go
  - backend/internal/repository/fansub_group_app_members_repository.go
  - backend/internal/handlers/fansub_hist_group_member_roles_handler_test.go
  - backend/internal/repository/hist_group_member_roles_whitelist_test.go
  - backend/internal/repository/fansub_group_app_members_auto_archive_test.go
autonomous: true
requirements:
  - D-06
  - D-09
  - D-10
  - D-13
  - D-14

must_haves:
  truths:
    - "D-13 (CR-01): POST hist-member-role mit App-Rollen-Code z.B. 'translator' → 422 (Whitelist-Härtung)"
    - "D-14 (WR-02): GET hist-member-roles mit member_id die einer anderen Gruppe gehört → 422 (Cross-Group-Guard)"
    - "D-10: SetMemberRole(Enable=false) schreibt automatisch einen hist_group_member_roles-Eintrag (started_year aus created_at, ended_year=aktuell, status='ended') — fail-open wenn kein hist-Member-Link"
    - "D-09: Aktive und historische Rollen koexistieren ohne Verdrängung (Auto-Archiv ist zusätzlich, nicht ersetzend)"
    - "D-11: Auto-archivierte hist_group_member_roles-Einträge werden mit visibility='internal' angelegt; kein Member-/Public-Surface in dieser Phase."
    - "Security: IsGroupHistoryWhitelistRole ersetzt RoleCodeExistsForContext im Write-Pfad"
  artifacts:
    - path: "backend/internal/repository/hist_group_member_roles_repository.go"
      provides: "IsGroupHistoryWhitelistRole() Methode + aktualisierte Whitelist (von Plan 01)"
    - path: "backend/internal/repository/hist_group_member_roles_whitelist_test.go"
      provides: "Wave-0-Test TestGroupHistoryWhitelist (D-06)"
    - path: "backend/internal/repository/fansub_group_app_members_auto_archive_test.go"
      provides: "Wave-0-Test TestAutoArchive (D-10)"
    - path: "backend/internal/handlers/fansub_hist_group_member_roles_handler_test.go"
      provides: "Neue Tests CR-01 (TestCreateHistGroupMemberRoleWhitelistReject) + WR-02 (TestListHistGroupMemberRolesCrossGroupGuard)"
  key_links:
    - from: "backend/internal/handlers/fansub_hist_group_member_roles_handler.go"
      to: "hist_group_member_roles_repository.IsGroupHistoryWhitelistRole"
      via: "CreateHistGroupMemberRole Z.237 — ersetzt RoleCodeExistsForContext-DB-Abfrage"
      pattern: "IsGroupHistoryWhitelistRole"
    - from: "backend/internal/repository/fansub_group_app_members_repository.go"
      to: "hist_group_member_roles"
      via: "SetMemberRole(Enable=false) DELETE + fail-open INSERT (D-10)"
      pattern: "INSERT INTO hist_group_member_roles"
---

<objective>
Plan 03 (Wave 2): Security-Fixes CR-01/WR-02 + Auto-Archivierung D-10

Drei inhaltlich zusammengehörende Sicherheits- und Lifecycle-Änderungen am Backend:
1. CR-01 (D-13): Write-Bypass schließen — `CreateHistGroupMemberRole` validiert per
   `IsGroupHistoryWhitelistRole` statt per breitem DB-Context-Check.
2. WR-02 (D-14): Cross-Group-Scope-Leck schließen — `ListHistGroupMemberRoles` prüft
   FansubGroupID-Membership bevor es member_id-Daten liest.
3. D-10: Auto-Archivierung — `SetMemberRole(Enable=false)` schreibt fail-open einen
   `hist_group_member_roles`-Eintrag wenn ein hist-Member-Link für den App-User existiert.

Läuft parallel zu Plan 02 und Plan 04 (keine gemeinsamen Dateien).

Purpose: Plan 01 hat die Whitelist-Slice aktualisiert (D-06). Dieser Plan baut darauf auf:
IsGroupHistoryWhitelistRole liest dieselbe Slice, die Plan 01 auf kanonische Codes umgestellt hat.
Output: 3 modifizierte Go-Handler/Repository-Dateien + 3 neue/erweiterte Test-Dateien.
</objective>

<execution_context>
@C:\Users\admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/95-rollenmodell-entwirren-gruppen-vs-projekt-ebene-techadmin-gf/95-CONTEXT.md
@.planning/phases/95-rollenmodell-entwirren-gruppen-vs-projekt-ebene-techadmin-gf/95-RESEARCH.md
@.planning/phases/95-rollenmodell-entwirren-gruppen-vs-projekt-ebene-techadmin-gf/95-PATTERNS.md
@.planning/phases/95-rollenmodell-entwirren-gruppen-vs-projekt-ebene-techadmin-gf/95-01-SUMMARY.md

<interfaces>
<!-- hist_group_member_roles_repository.go aktueller Zustand (nach Plan 01): -->
<!-- groupHistoryDialogRoleWhitelist = ["founder","fansub_lead","co_leader","project_lead","techadmin","gfxler"] -->
<!-- Z.245-280: ListGroupHistoryRoleDefinitions — Muster für IsGroupHistoryWhitelistRole -->
<!-- Z.282: RoleCodeExistsForContext (wird im Handler durch Whitelist-Check ersetzt) -->

<!-- fansub_hist_group_member_roles_handler.go: -->
<!-- CreateHistGroupMemberRole Z.157-292 — vollständige Methode inkl. Z.237-250 (zu ersetzen) -->
<!-- ListHistGroupMemberRoles Z.134-152 (Cross-Group-Check vor Z.145 einfügen) -->
<!-- memberRow.FansubGroupID != fansubID — Zugriff auf struct-Feld nach GetByID -->
<!-- h.histMembersRepo.GetByID(ctx, memberID) → Analog zu CreateHistGroupMemberRole Z.213-235 -->

<!-- fansub_group_app_members_repository.go: -->
<!-- SetMemberRole Z.380-426 — Enable=false-Pfad Z.399-413: DELETE-Block -->
<!-- fansub_group_members: app_user_id, fansub_group_id -->
<!-- hist_fansub_group_members: member_id (via members-Tabelle, nicht direkt app_user_id) -->
<!-- member_claims: member_id + app_user_id (Brücke für A1-Lookup) -->
<!-- Linkage-Pfad: fgm.app_user_id → member_claims.app_user_id → member_claims.member_id → hist_fansub_group_members.member_id -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0-Tests (CR-01/WR-02/D-10/D-06) + IsGroupHistoryWhitelistRole Methode</name>
  <files>
    backend/internal/repository/hist_group_member_roles_repository.go
    backend/internal/repository/hist_group_member_roles_whitelist_test.go
    backend/internal/repository/fansub_group_app_members_auto_archive_test.go
    backend/internal/handlers/fansub_hist_group_member_roles_handler_test.go
  </files>
  <read_first>
    - backend/internal/repository/hist_group_member_roles_repository.go (vollständig — Whitelist Z.245-280, ListByMember, Interface-Typ)
    - backend/internal/handlers/fansub_hist_group_member_roles_handler_test.go (bestehende Tests — Muster für neue Tests)
    - backend/internal/repository/fansub_group_app_members_repository.go (Z.380-426 — SetMemberRole-Pfad für D-10)
    - 95-PATTERNS.md Abschnitt "hist_group_member_roles_repository.go" (IsGroupHistoryWhitelistRole-Signatur)
    - 95-VALIDATION.md Wave-0-Requirements (alle 5 Dateien die erstellt werden müssen)
  </read_first>
  <behavior>
    - TestGroupHistoryWhitelist: Prüft direkt groupHistoryDialogRoleWhitelist-Slice; "translator" → nicht in Whitelist; "fansub_lead"/"techadmin"/"gfxler" → in Whitelist
    - TestCreateHistGroupMemberRoleWhitelistReject: POST mit RoleCode="translator" → 422 Unprocessable
    - TestListHistGroupMemberRolesCrossGroupGuard: GET mit member_id die FansubGroupID=9 hat, aber Request für FansubID=1 → 422
    - TestAutoArchive: Unit-Test mit Mock/Stub; SetMemberRole(false) mit hist-Member-Link → INSERT in hist_group_member_roles; ohne hist-Link → kein Fehler (fail-open)
  </behavior>
  <action>
    Neue Methode `IsGroupHistoryWhitelistRole(code string) bool` in
    `backend/internal/repository/hist_group_member_roles_repository.go` nach ListGroupHistoryRoleDefinitions
    (nach Z.280) einfügen: schlanke Slice-Iteration über `groupHistoryDialogRoleWhitelist`, pure Go,
    kein DB-Aufruf, kein Error-Return.

    Neue Testdatei `backend/internal/repository/hist_group_member_roles_whitelist_test.go`:
    TestGroupHistoryWhitelist mit Subtests: "translator nicht in Whitelist", "fansub_lead in Whitelist",
    "techadmin in Whitelist", "gfxler in Whitelist", "leader NICHT mehr in Whitelist".
    Direkt gegen `IsGroupHistoryWhitelistRole` über eine HistGroupMemberRolesRepository-Instanz (nil-DB ist OK für reine Slice-Methode).

    In bestehende `backend/internal/handlers/fansub_hist_group_member_roles_handler_test.go`
    ergänzen (NICHT überschreiben): Tests `TestCreateHistGroupMemberRoleWhitelistReject` und
    `TestListHistGroupMemberRolesCrossGroupGuard` hinzufügen. Muster: gin.New()-Router,
    MockHistMembersRepo mit GetByID-Stub (gibt FansubGroupID=99 zurück wenn Mismatch simuliert),
    MockHistRolesRepo mit IsGroupHistoryWhitelistRole als einfache Wrapper-Delegation.

    Neue Testdatei `backend/internal/repository/fansub_group_app_members_auto_archive_test.go`:
    TestAutoArchive-Subtests: "Mit hist-Link → INSERT ausgeführt", "Ohne hist-Link → kein Fehler",
    "Doppel-Entzug → ON CONFLICT DO NOTHING, kein Fehler". Tests können mit sqlmock oder als
    Integrations-Skip-Test mit `t.Skip("requires DB")` markiert werden; das Wichtigste ist die
    Methodensignatur und das Verhalten, das verifiziert wird.
  </action>
  <verify>
    <automated>
      cd "C:/Users/admin/Documents/Team4s/backend" && go build ./internal/repository/... ./internal/handlers/... 2>&1 | head -20 && go test ./internal/repository/... -run TestGroupHistoryWhitelist -v 2>&1 | tail -15
    </automated>
  </verify>
  <done>
    `IsGroupHistoryWhitelistRole` in hist_group_member_roles_repository.go vorhanden.
    3 neue Test-Dateien existieren.
    `go build ./internal/repository/... ./internal/handlers/...` ohne Fehler.
    TestGroupHistoryWhitelist-Tests für "translator" liefern false, für "fansub_lead"/"techadmin"/"gfxler" true.
  </done>
</task>

<task type="auto">
  <name>Task 2: CR-01/WR-02 Handler-Fixes + D-10 Auto-Archivierung im Repository</name>
  <files>
    backend/internal/handlers/fansub_hist_group_member_roles_handler.go
    backend/internal/repository/fansub_group_app_members_repository.go
  </files>
  <read_first>
    - backend/internal/handlers/fansub_hist_group_member_roles_handler.go (vollständig — Z.134-152 ListByMember, Z.213-250 CreateHistGroupMemberRole)
    - backend/internal/repository/fansub_group_app_members_repository.go (Z.380-426 SetMemberRole, fansubGroupID-Parameter)
    - database/migrations/0082_historical_fansub_group_members.up.sql (hist_fansub_group_members-Schema: member_id FK auf members)
    - database/migrations/0081_historical_members_identity.up.sql (member_claims: member_id + app_user_id)
    - 95-PATTERNS.md Abschnitte "fansub_hist_group_member_roles_handler.go" und "fansub_group_app_members_repository.go"
    - 95-RESEARCH.md Muster 3/4 (CR-01/WR-02 Fix-Vorlagen) und Muster 2 (D-10 Auto-Archivierung mit hist-Linkage)
  </read_first>
  <action>
    CR-01-Fix in `fansub_hist_group_member_roles_handler.go` (D-13):
    In `CreateHistGroupMemberRole` Z.237-250: Den Block mit `h.rolesRepo.RoleCodeExistsForContext(ctx, req.RoleCode, "group_history")`
    (die DB-Abfrage) komplett ersetzen durch:
    `if !h.rolesRepo.IsGroupHistoryWhitelistRole(req.RoleCode) { c.JSON(422, ...) return }`
    Kein DB-Aufruf mehr. Fehlermeldung auf Deutsch: "ungültiger role_code für group_history-Kontext".

    WR-02-Fix in `fansub_hist_group_member_roles_handler.go` (D-14):
    In `ListHistGroupMemberRoles` NACH dem memberID-Parse (Z.143) und VOR `h.rolesRepo.ListByMember(ctx, memberID)` (Z.145):
    `memberRow, err := h.histMembersRepo.GetByID(c.Request.Context(), memberID)`
    — ErrNotFound → 404, Serverfehler → 500, `memberRow.FansubGroupID != fansubID` → 422.
    Fehlermeldungen auf Deutsch mit korrekten Umlauten.

    D-10 Auto-Archivierung in `fansub_group_app_members_repository.go`:
    Im `else`-Zweig von SetMemberRole (Enable=false, Z.399ff) VOR dem DELETE:
    `SELECT id, created_at FROM fansub_group_member_roles WHERE fansub_group_member_id=$1 AND role=$2`
    → roleCreatedAt speichern.
    NACH dem bestehenden DELETE (Fallstrick: DELETE-Block Z.407-413 bleibt unverändert, nur davor/danach erweitern):
    Fail-open-Lookup über 2-Hop-Join (Linkage A1-Korrektur — hist_fansub_group_members hat member_id,
    NICHT direkt app_user_id):
    `SELECT hfgm.id FROM hist_fansub_group_members hfgm
     JOIN member_claims mc ON mc.member_id = hfgm.member_id AND mc.claim_status='verified'
     JOIN fansub_group_members fgm ON fgm.app_user_id = mc.app_user_id
     WHERE fgm.id = $1 AND hfgm.fansub_group_id = $2 LIMIT 1`
    Wenn histMemberID gefunden: INSERT INTO hist_group_member_roles mit ON CONFLICT DO NOTHING.
    visibility = 'internal', status = 'ended', started_year = roleCreatedAt.Year(),
    ended_year = time.Now().Year().
    KEIN Fehler-Return wenn Lookup fehlschlägt (fail-open per D-10 + A1-Hinweis).
    Import `"time"` hinzufügen wenn noch nicht vorhanden.
  </action>
  <verify>
    <automated>
      cd "C:/Users/admin/Documents/Team4s/backend" && go build ./... 2>&1 | head -20 && go test ./internal/handlers/... -run "TestCreateHistGroupMemberRoleWhitelistReject|TestListHistGroupMemberRolesCrossGroupGuard" -v 2>&1 | tail -20 && go test ./internal/repository/... -run "TestAutoArchive" -v 2>&1 | tail -15
    </automated>
  </verify>
  <done>
    CR-01: `fansub_hist_group_member_roles_handler.go` enthält IsGroupHistoryWhitelistRole-Aufruf statt
    RoleCodeExistsForContext (kein DB-Aufruf mehr im Validierungspfad).
    WR-02: ListHistGroupMemberRoles enthält GetByID + FansubGroupID-Check vor ListByMember.
    D-10: SetMemberRole(false)-Pfad enthält pre-DELETE SELECT + post-DELETE fail-open INSERT.
    TestCreateHistGroupMemberRoleWhitelistReject → PASS (422 für 'translator').
    TestListHistGroupMemberRolesCrossGroupGuard → PASS (422 bei Cross-Group).
    `go build ./...` grün.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /admin/fansubs/:id/member-roles | Nur Fansub-Scope mit FansubGroupMembersManage |
| Client → GET /admin/fansubs/:id/hist-member-roles | Nur Fansub-Scope mit FansubGroupMembersView |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-03-T | Tampering | CreateHistGroupMemberRole Write-Bypass | mitigate | D-13/CR-01: IsGroupHistoryWhitelistRole-Check ersetzt breiten DB-Context-Check; 'translator' als hist-Rolle → 422; Whitelist enthält nur Gruppen-Positions-Codes |
| T-95-03-ID | Information Disclosure | ListHistGroupMemberRoles Cross-Group | mitigate | D-14/WR-02: histMembersRepo.GetByID + FansubGroupID-Vergleich vor ListByMember; member_id einer anderen Gruppe → 422 |
| T-95-03-EoP | Elevation of Privilege | Auto-Archiv-INSERT mit falscher Rolle | mitigate | D-10: INSERT übernimmt `role`-Parameter direkt aus SetMemberRole-Aufruf — dieser wurde bereits durch IsKnownFansubGroupRole validiert; ON CONFLICT DO NOTHING verhindert Duplikate |
| T-95-03-SC | Tampering | npm/pip/cargo installs | accept | Keine Paket-Installationen in diesem Plan |
</threat_model>

<verification>
Nach Abschluss beider Tasks:

1. `go build ./...` — grün
2. `go test ./internal/handlers/... -run TestCreateHistGroupMemberRoleWhitelistReject` — PASS
3. `go test ./internal/handlers/... -run TestListHistGroupMemberRolesCrossGroupGuard` — PASS
4. `go test ./internal/repository/... -run TestGroupHistoryWhitelist` — PASS
5. `grep -n "RoleCodeExistsForContext" backend/internal/handlers/fansub_hist_group_member_roles_handler.go` → 0 Treffer (ersetzt)
6. `grep -n "IsGroupHistoryWhitelistRole" backend/internal/handlers/fansub_hist_group_member_roles_handler.go` → 1 Treffer
7. `grep -n "hist_group_member_roles" backend/internal/repository/fansub_group_app_members_repository.go` → INSERT-Statement gefunden (D-10)
</verification>

<success_criteria>
- CR-01 geschlossen: POST mit App-Rollen-Code 'translator' als hist-Rolle → 422 (per D-13)
- WR-02 geschlossen: GET hist-member-roles für fremde member_id → 422 (per D-14)
- Auto-Archivierung vorhanden: SetMemberRole(false) schreibt hist-Eintrag wenn Link besteht (per D-10)
- Alle Tests grün, kein Kompilierfehler
</success_criteria>

<output>
Erstelle `.planning/phases/95-rollenmodell-entwirren-gruppen-vs-projekt-ebene-techadmin-gf/95-03-SUMMARY.md` nach Abschluss.
</output>
