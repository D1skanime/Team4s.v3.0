---
phase: 65-member-vorschlaege-review-queue
plan: 02
type: execute
wave: 2
depends_on:
  - 65-01
files_modified:
  - backend/internal/handlers/contribution_proposals_me_handler.go
  - backend/internal/handlers/contribution_proposals_me_test.go
  - backend/internal/handlers/contributions_me_handler.go
  - backend/cmd/server/main.go
autonomous: true
requirements:
  - P65-SC1
  - P65-SC3

must_haves:
  truths:
    - "POST /api/v1/me/contribution-proposals erstellt Vorschlag mit status='proposed' für verifiziertes Member"
    - "Fremde fansub_group_member_id wird mit 403 abgelehnt (D-03)"
    - "Duplikat (gleiche Gruppe+Anime+Member) → 409 mit deutschem Fehlertext (D-05)"
    - "0 Rollen → 422 mit deutschem Fehlertext (D-04)"
    - "POST /api/v1/me/anime-contributions/:cid/self-publish prüft 90-Tage serverseitig; Status bleibt 'proposed' (D-15)"
    - "GET /api/v1/me/memberships gibt aktive hist_fansub_group_members-Einträge des verifizierten Members zurück"
    - "GET /api/v1/me/anime-contributions gibt can_self_publish und review_note pro Eintrag zurück (D-08, D-12)"
    - "D-01: Ownership-Check in CreateProposal stellt sicher, dass fansub_group_member_id zur verifizierten Member-Identität des Aufrufers gehört (serverseitig via hist_fansub_group_members JOIN)"
    - "D-02: CreateProposal erzwingt Anime + mind. 1 Rolle als Pflichtfelder; note-Feld (Freitext) ist optional aber im Request-Typ vorhanden"
    - "D-11: SelfPublish-Handler delegiert 90-Tage-Check an Repository; Status bleibt 'proposed'; kein automatischer Übergang auf confirmed"
    - "D-14: created_by wird bei CreateProposal auf die App-User-ID des eingeloggten Members gesetzt; Audit-Log-Eintrag 'anime_contribution.proposed' nach jedem erfolgreichen Insert"
    - "D-19: Alle deutschen Fehlermeldungen im Handler nutzen korrekte Umlaute (ä, ö, ü, ß) — keine ASCII-Ersetzungen"
  artifacts:
    - path: "backend/internal/handlers/contribution_proposals_me_handler.go"
      provides: "ContributionProposalsMeHandler mit CreateProposal, SelfPublish, ListMemberships"
    - path: "backend/internal/handlers/contribution_proposals_me_test.go"
      provides: "Unit-Tests für alle P65-SC1 und P65-SC3 Behaviors"
    - path: "backend/internal/handlers/contributions_me_handler.go"
      provides: "ListMyAnimeContributions nutzt ListByMemberIDWithProposalFields statt ListByMemberID"
  key_links:
    - from: "backend/internal/handlers/contribution_proposals_me_handler.go"
      to: "backend/internal/repository/anime_contributions_proposal_repository.go"
      via: "proposalRepo Interface (für Stub-Tests)"
      pattern: "proposalRepo\\.CreateProposal"
    - from: "backend/internal/handlers/contributions_me_handler.go"
      to: "backend/internal/repository/anime_contributions_proposal_repository.go"
      via: "ListByMemberIDWithProposalFields liefert can_self_publish + review_note"
      pattern: "ListByMemberIDWithProposalFields"
    - from: "backend/cmd/server/main.go"
      to: "contribution_proposals_me_handler.go"
      via: "proposalsMeHandler registriert nach Zeile 385"
      pattern: "POST.*me/contribution-proposals"
---

<objective>
Neuer Handler contribution_proposals_me_handler.go liefert die drei Member-seitigen Endpunkte: POST /api/v1/me/contribution-proposals (Vorschlag einreichen, P65-SC1), POST /api/v1/me/anime-contributions/:cid/self-publish (90-Tage-Selbstschaltung, P65-SC3), und GET /api/v1/me/memberships (eigene hist_fansub_group_members-Datensätze mit fansub_group_member_id für ProposalForm). Außerdem wird contributions_me_handler.go umgestellt, damit GET /api/v1/me/anime-contributions die Felder can_self_publish und review_note enthält (D-08, D-12).

Purpose: Member-getriebene Proposal-Erstellung mit vollständiger Sicherheitslogik plus Datenpfad für ownGroups (fansub_group_member_id) und Proposal-Felder im Dashboard.
Output: Neuer Handler-Datei + Test-Datei + Anpassung contributions_me_handler.go + main.go-Routen-Verdrahtung.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-member-vorschlaege-review-queue/65-CONTEXT.md
@.planning/phases/65-member-vorschlaege-review-queue/65-RESEARCH.md
@.planning/phases/65-member-vorschlaege-review-queue/65-PATTERNS.md
@.planning/phases/65-member-vorschlaege-review-queue/65-01-SUMMARY.md
</context>

<interfaces>
<!-- Schlüssel-Contracts die der Executor braucht. -->

Aus backend/internal/handlers/contributions_me_handler.go:
  type ContributionsMeHandler struct { contributionsRepo *repository.AnimeContributionsRepository; groupRolesRepo *repository.HistGroupMemberRolesRepository; db *pgxpool.Pool }
  func requireMeIdentity(c *gin.Context) (middleware.AuthIdentity, bool)
  func notFound(c *gin.Context, msg string)
  func internalError(c *gin.Context, msg string)
  func badRequest(c *gin.Context, msg string)
  func (h *ContributionsMeHandler) resolveVerifiedMemberID(ctx context.Context, appUserID int64) (int64, error)
  func (h *ContributionsMeHandler) ListMyAnimeContributions(c *gin.Context)
    -- Zeile 93: h.contributionsRepo.ListByMemberID(ctx, memberID)
    -- DIESE ZEILE muss auf ListByMemberIDWithProposalFields umgestellt werden (BLOCKER 1-Fix)

Aus backend/internal/repository/anime_contributions_proposal_repository.go (aus Plan 65-01):
  type ProposalInput struct { FansubGroupMemberID int64; RoleCodes []string; Note *string; StartedYear, EndedYear *int; AppUserID int64 }
  func (r *AnimeContributionsRepository) CreateProposal(ctx, fansubGroupID, animeID int64, input ProposalInput) (*AnimeContributionRow, error)
  func (r *AnimeContributionsRepository) SelfPublish(ctx, contributionID, appUserID int64) error
  func (r *AnimeContributionsRepository) ListByMemberIDWithProposalFields(ctx, memberID int64) ([]AnimeContributionRow, error)
    -- liefert AnimeContributionRow mit den zusätzlichen Feldern CanSelfPublish bool und ReviewNote *string
  -- ErrConflict bei Duplikat/SelfPublish-vor-90-Tagen; ErrNotFound bei fehlendem Eintrag

Aus backend/internal/repository/anime_contributions_repository.go:
  type AnimeContributionRow struct { ID, FansubGroupID, AnimeID, FansubGroupMemberID int64; Status string; Note, ReviewNote *string; CanSelfPublish bool; ... }
  ErrConflict, ErrNotFound (package-level Sentinels)

Aus backend/cmd/server/main.go (Zeilen 345–385):
  animeContributionsRepo := repository.NewAnimeContributionsRepository(dbPool)
  contributionsMeHandler := handlers.NewContributionsMeHandler(...)
  -- nach Zeile 385 neue Routen einfügen

Aus backend/internal/handlers/fansub_anime_contributions_handler.go (Zeilen 193–208):
  -- Rollen-Validierungs-Pattern: RoleCodeExistsForContext(ctx, code, "anime_contribution")
  type rolesRepo interface { RoleCodeExistsForContext(ctx, code, context string) (bool, error) }

Aus backend/internal/handlers/admin_content_anime_relations_test.go:
  -- Test-Muster: gin.SetMode(gin.TestMode), httptest.NewRecorder(), gin.CreateTestContext, c.Set("auth_identity", ...)
  -- Stub-Structs implementieren Interface-Methoden inline
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: contributions_me_handler.go — ListByMemberIDWithProposalFields verdrahten</name>
  <files>backend/internal/handlers/contributions_me_handler.go</files>
  <read_first>
    - backend/internal/handlers/contributions_me_handler.go (vollständig, 329 Zeilen — Zeile 93 ist der Zielpunkt)
    - backend/internal/repository/anime_contributions_proposal_repository.go (ListByMemberIDWithProposalFields-Signatur aus Plan 65-01; gibt []AnimeContributionRow mit CanSelfPublish und ReviewNote zurück)
  </read_first>
  <action>
    In backend/internal/handlers/contributions_me_handler.go genau eine Änderung vornehmen:

    ListMyAnimeContributions (Zeile 93): Ersetze den Aufruf
      h.contributionsRepo.ListByMemberID(c.Request.Context(), memberID)
    durch
      h.contributionsRepo.ListByMemberIDWithProposalFields(c.Request.Context(), memberID)

    Der Rückgabetyp ist identisch ([]AnimeContributionRow), nur dass die Felder CanSelfPublish und ReviewNote jetzt befüllt sind und im JSON-Output erscheinen (json-Tags: "can_self_publish" und "review_note"). Kein weiterer Code ändert sich.

    Wichtig: AnimeContributionRow muss in Plan 65-01 (anime_contributions_proposal_repository.go) bereits um CanSelfPublish bool und ReviewNote *string erweitert worden sein. Falls diese Felder noch fehlen, muss der Executor zuerst den Struct in anime_contributions_repository.go um diese Felder ergänzen, bevor diese Task abgeschlossen wird — das ist das einzige Szenario, in dem eine zweite Datei (anime_contributions_repository.go) angefasst werden darf, aber nur mit den zwei neuen Feldern, nicht mit anderen Änderungen.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go build ./internal/handlers/ 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - go build ./internal/handlers/ läuft ohne Fehler
    - grep "ListByMemberIDWithProposalFields" backend/internal/handlers/contributions_me_handler.go ergibt genau 1 Treffer
    - grep "ListByMemberID\b" backend/internal/handlers/contributions_me_handler.go ergibt 0 Treffer (alte Methode komplett ersetzt)
    - AnimeContributionRow in anime_contributions_repository.go enthält die Felder CanSelfPublish und ReviewNote (grep "CanSelfPublish" ergibt Treffer)
  </acceptance_criteria>
  <done>contributions_me_handler.go kompiliert; ListMyAnimeContributions ruft ListByMemberIDWithProposalFields auf; JSON-Response enthält can_self_publish und review_note.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ContributionProposalsMeHandler — CreateProposal, SelfPublish, ListMemberships + Tests</name>
  <files>backend/internal/handlers/contribution_proposals_me_handler.go, backend/internal/handlers/contribution_proposals_me_test.go</files>
  <read_first>
    - backend/internal/handlers/contributions_me_handler.go (requireMeIdentity, resolveVerifiedMemberID, authorizeAnimeContributionOwner, Error-Pattern, Zeilen 1–329)
    - backend/internal/handlers/fansub_anime_contributions_handler.go (Rollen-Validierung Zeilen 193–208, ErrConflict-Mapping, Audit-Log-Pattern)
    - backend/internal/handlers/admin_content_anime_relations_test.go (Test-Muster)
    - backend/internal/repository/anime_contributions_proposal_repository.go (Interface-Methoden aus Plan 65-01)
  </read_first>
  <behavior>
    Tests (contribution_proposals_me_test.go) müssen alle ROT sein bevor der Handler geschrieben wird:
    - TestCreateProposal_RequiresAuth: ohne Auth-Identity → 401
    - TestCreateProposal_RequiresRole: role_codes=[] → 422 "Bitte wähle mindestens eine Rolle aus."
    - TestCreateProposal_DuplicateBlocked: proposalRepo gibt ErrConflict → 409 "für diese Kombination aus Gruppe, Anime und deiner Identität existiert bereits ein Beitrag"
    - TestCreateProposal_ForeignMembershipRejected: fansub_group_member_id gehört nicht zum verifizierten Member → 403 "keine Berechtigung"
    - TestCreateProposal_Success: korrekte Anfrage → 201, status='proposed'
    - TestSelfPublish_Before90Days: proposalRepo.SelfPublish gibt ErrConflict → 409 "Vorschlag kann noch nicht selbst veröffentlicht werden"
    - TestSelfPublish_NotOwner: Eintrag gehört anderem Member → 403
    - TestSelfPublish_Success: proposalRepo.SelfPublish gibt nil → 200
    - TestListMemberships_RequiresAuth: ohne Auth-Identity → 401
    - TestListMemberships_Success: gibt Liste von MembershipEntry-Objekten zurück → 200
  </behavior>
  <action>
    Neue Datei contribution_proposals_me_handler.go erstellen.

    Handler-Struct ContributionProposalsMeHandler mit:
    - proposalRepo ProposalRepository (Interface, damit Stub-Tests möglich — D-19 Pitfall 4)
    - rolesRepo RolesRepository (Interface)
    - db *pgxpool.Pool (für resolveVerifiedMemberID und Ownership-Check via direktem DB-Zugriff, analog ContributionsMeHandler)
    - auditLogRepo *repository.AuditLogRepository

    Interface-Definitionen direkt im Handler-File:
      type ProposalRepository interface { CreateProposal(ctx context.Context, fansubGroupID, animeID int64, input repository.ProposalInput) (*repository.AnimeContributionRow, error); SelfPublish(ctx context.Context, contributionID, appUserID int64) error; GetByID(ctx context.Context, id int64) (*repository.AnimeContributionRow, error) }
      type RolesRepository interface { RoleCodeExistsForContext(ctx context.Context, code, context string) (bool, error) }

    func NewContributionProposalsMeHandler(proposalRepo ProposalRepository, rolesRepo RolesRepository, db *pgxpool.Pool, auditLogRepo *repository.AuditLogRepository) *ContributionProposalsMeHandler

    CreateProposal (POST /api/v1/me/contribution-proposals):
    1. requireMeIdentity → 401 wenn nicht eingeloggt
    2. ShouldBindJSON für CreateProposalRequest{ FansubGroupID int64, AnimeID int64, FansubGroupMemberID int64, RoleCodes []string, Note *string, StartedYear *int, EndedYear *int }
    3. len(req.RoleCodes) == 0 → 422 "Bitte wähle mindestens eine Rolle aus." (D-04)
    4. resolveVerifiedMemberID via direktem DB-Aufruf (analog ContributionsMeHandler.resolveVerifiedMemberID) → 404 wenn kein verifizierter member_claim
    5. Eigentümer-Check: SELECT member_id FROM hist_fansub_group_members WHERE id=$req.FansubGroupMemberID; wenn member_id != resolvedMemberID → 403 "keine Berechtigung" (D-03)
    6. Jede RoleCode gegen rolesRepo.RoleCodeExistsForContext(ctx, code, "anime_contribution") → 422 bei ungültigem Code
    7. proposalRepo.CreateProposal(...) → ErrConflict → 409 "für diese Kombination aus Gruppe, Anime und deiner Identität existiert bereits ein Beitrag."; ErrNotFound → 404
    8. Audit: auditLogRepo.Write mit EventType "anime_contribution.proposed", TargetType "anime_contribution"
    9. Erfolg: 201 + AnimeContributionRow

    SelfPublish (POST /api/v1/me/anime-contributions/:contributionId/self-publish):
    1. requireMeIdentity → 401
    2. Parse contributionId aus Param → 400 bei ungültig
    3. resolveVerifiedMemberID → 404
    4. Eigentümer-Check über h.db.QueryRow (analog authorizeAnimeContributionOwner) → 403
    5. proposalRepo.SelfPublish(ctx, contributionID, appUserID) → ErrConflict → 409 "Vorschlag kann noch nicht selbst veröffentlicht werden. 90 Tage müssen seit Einreichung vergangen sein."; ErrNotFound → 404
    6. Audit: EventType "anime_contribution.self_published"
    7. Erfolg: 200

    ListMemberships (GET /api/v1/me/memberships):
    Gibt die hist_fansub_group_members-Einträge des verifizierten Members zurück.
    1. requireMeIdentity → 401
    2. resolveVerifiedMemberID → 404
    3. SELECT hfgm.id AS fansub_group_member_id, fg.id AS fansub_group_id, fg.name AS group_name
       FROM hist_fansub_group_members hfgm
       JOIN fansub_groups fg ON fg.id = hfgm.fansub_group_id
       WHERE hfgm.member_id = $1
       ORDER BY fg.name ASC
       direkt über h.db.Query (kein Repository-Aufruf nötig für diese einfache Query)
    4. Erfolg: 200 + { "data": [{ "fansub_group_member_id": N, "fansub_group_id": N, "group_name": "..." }] }
    Typ-Definition MembershipEntry{ FansubGroupMemberID, FansubGroupID int64, GroupName string } direkt in handler-Datei.

    Alle deutschen Fehlermeldungen mit korrekten Umlauten (D-19).

    Test-Datei contribution_proposals_me_test.go:
    - Stub-Structs inline (proposalRepoStub, rolesRepoStub) implementieren die Interfaces
    - gin.SetMode(gin.TestMode)
    - c.Set("auth_identity", middleware.AuthIdentity{AppUserID: 42, ...})
    - Alle 10 Test-Fälle aus den Behavior-Punkten oben
    - ListMemberships-Test kann mit einem DB-Mock oder indem die DB-Dependency durch einen zweiten Interface-Layer abstrahiert wird — falls DB-Mock zu komplex, ist ein Integrationstest-Hinweis in einem Kommentar akzeptabel, aber die anderen 9 Tests müssen grün sein

    Dateigrößen-Check: Beide Dateien müssen unter 450 Zeilen bleiben.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go test ./internal/handlers/ -run "TestCreateProposal|TestSelfPublish|TestListMemberships" -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - go test ./internal/handlers/ -run "TestCreateProposal|TestSelfPublish" läuft grün (mindestens 8 Tests bestehen)
    - Handler-Datei kompiliert ohne Fehler
    - grep "ListMemberships" backend/internal/handlers/contribution_proposals_me_handler.go ergibt mindestens 1 Treffer
    - grep "fansub_group_member_id" backend/internal/handlers/contribution_proposals_me_handler.go ergibt mindestens 1 Treffer (SQL-Query in ListMemberships)
    - grep "status.*confirmed" backend/internal/handlers/contribution_proposals_me_handler.go ergibt 0 Treffer in SelfPublish-Methode (Status bleibt 'proposed')
    - wc -l contribution_proposals_me_handler.go < 450
    - wc -l contribution_proposals_me_test.go < 450
    - Alle deutschen Fehlermeldungen nutzen korrekte Umlaute (ä, ö, ü, ß) — keine ae/oe/ue
  </acceptance_criteria>
  <done>Handler und Tests grün; CreateProposal, SelfPublish und ListMemberships implementiert; keine ASCII-Umlaut-Ersetzungen.</done>
</task>

<task type="auto">
  <name>Task 3: main.go — neue Me-Routen registrieren</name>
  <files>backend/cmd/server/main.go</files>
  <read_first>
    - backend/cmd/server/main.go (Zeilen 345–395, bestehende Routen-Registrierung für contributionsMeHandler)
    - backend/internal/handlers/contribution_proposals_me_handler.go (NewContributionProposalsMeHandler-Signatur aus Task 2)
  </read_first>
  <action>
    In backend/cmd/server/main.go nach Zeile 385 (letzter contributionsMeHandler-Routen-Block) einfügen:

    proposalsMeHandler := handlers.NewContributionProposalsMeHandler(
        animeContributionsRepo, histGroupMemberRolesRepo, dbPool, auditLogRepo,
    )
    v1.GET("/me/memberships", authMiddleware, proposalsMeHandler.ListMemberships)
    v1.POST("/me/contribution-proposals", authMiddleware, proposalsMeHandler.CreateProposal)
    v1.POST("/me/anime-contributions/:contributionId/self-publish", authMiddleware, proposalsMeHandler.SelfPublish)

    Wichtig: animeContributionsRepo und histGroupMemberRolesRepo sind bereits deklariert. auditLogRepo ist ebenfalls vorhanden. Keine neuen Konstruktor-Aufrufe oder Repository-Instanzen nötig.

    Routen-Reihenfolge: /me/anime-contributions/:contributionId/self-publish muss NACH den bestehenden /me/anime-contributions/:contributionId/confirm und /me/anime-contributions/:contributionId/reject stehen.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go build ./cmd/server/ 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - go build ./cmd/server/ läuft ohne Fehler
    - grep "me/contribution-proposals" backend/cmd/server/main.go ergibt mindestens 1 Treffer
    - grep "me/memberships" backend/cmd/server/main.go ergibt mindestens 1 Treffer
    - grep "self-publish" backend/cmd/server/main.go ergibt mindestens 1 Treffer
    - Bestehende Routen-Registrierungen sind unverändert
  </acceptance_criteria>
  <done>Backend kompiliert; GET /me/memberships, POST /me/contribution-proposals und POST /me/anime-contributions/:id/self-publish sind registriert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /api/v1/me/contribution-proposals | Untrusted Member-Input: fansub_group_member_id kann gefälscht sein |
| Browser → POST /api/v1/me/anime-contributions/:cid/self-publish | Untrusted Zeitpunkt: Frontend-Sichtbarkeit des Buttons ist nicht vertrauenswürdig |
| Browser → GET /api/v1/me/memberships | Authenticated: gibt nur Daten des eigenen Members zurück (resolveVerifiedMemberID bindet an Identität) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-02-01 | Spoofing | CreateProposal — fansub_group_member_id | mitigate | Schritt 5 im Handler: SELECT member_id FROM hist_fansub_group_members WHERE id=req.FansubGroupMemberID; wenn member_id ≠ resolvedMemberID → 403 (D-03 Identitäts-Spoofing verhindert) |
| T-65-02-02 | Elevation of Privilege | CreateProposal ohne Auth | mitigate | requireMeIdentity() als erster Handler-Schritt → 401; authMiddleware auf Route registriert |
| T-65-02-03 | Tampering | SelfPublish — 90-Tage-Gate im Frontend umgehen | mitigate | SelfPublish-Endpunkt re-validiert 90 Tage serverseitig via proposalRepo.SelfPublish (SELECT + UPDATE-Bedingung); Frontend-Sichtbarkeit allein ist untrusted |
| T-65-02-04 | Tampering | SelfPublish setzt Status 'confirmed' | mitigate | Handler und Repository: Status bleibt 'proposed'; is_verified bleibt false; nur Sichtbarkeitsflags + confirmed_by gesetzt (D-15) |
| T-65-02-05 | Repudiation | Proposal ohne Audit-Spur | mitigate | auditLogRepo.Write mit EventType "anime_contribution.proposed" / "anime_contribution.self_published" nach jeder erfolgreichen Mutation |
| T-65-02-06 | Information Disclosure | ListMemberships gibt mehr Daten als nötig | accept | Gibt nur ID, fansub_group_id und group_name zurück — keine sensiblen Felder; resolveVerifiedMemberID bindet Ausgabe strikt an den eigenen Member-Account |
| T-65-02-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Pakete — Slopcheck nicht zutreffend |
</threat_model>

<verification>
- go test ./internal/handlers/ -run "TestCreateProposal|TestSelfPublish" → mindestens 8 Tests grün
- go build ./cmd/server/ → erfolgreich
- grep "ListByMemberIDWithProposalFields" backend/internal/handlers/contributions_me_handler.go → 1 Treffer
- grep "me/memberships" backend/cmd/server/main.go → 1 Treffer
- Manuelle Überprüfung: keine deutschen ASCII-Umlaut-Ersetzungen in handler-Datei
- wc -l contribution_proposals_me_handler.go < 450
</verification>

<success_criteria>
- go build ./cmd/server/ und go test grün
- contributions_me_handler.go: ListMyAnimeContributions ruft ListByMemberIDWithProposalFields auf — can_self_publish und review_note im JSON-Response (P65-SC3, D-08)
- ListMemberships: gibt eigene hist_fansub_group_members mit fansub_group_member_id zurück (Datenpfad für ProposalForm.ownGroups)
- CreateProposal-Handler: Auth, Rollen-Check (min 1), Identitäts-Binding, Duplikat-Block (409), Audit
- SelfPublish-Handler: Auth, Ownership, 90-Tage-Re-Validierung serverseitig, Status bleibt 'proposed'
- Alle deutschen Strings korrekte Umlaute
</success_criteria>

<output>
Erstelle .planning/phases/65-member-vorschlaege-review-queue/65-02-SUMMARY.md wenn fertig.
</output>
