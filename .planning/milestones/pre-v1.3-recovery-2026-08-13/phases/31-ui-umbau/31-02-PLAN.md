---
phase: 31-ui-umbau
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01-PLAN.md
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css
  - frontend/src/app/admin/fansubs/[id]/edit/ReleaseThemeAssetsSection.tsx
  - frontend/src/lib/api.ts
autonomous: true
requirements:
  - P31-SC3
  - P31-SC4
must_haves:
  truths:
    - "Expanded releases show Theme/Segment cards, not generic logo/banner upload controls."
    - "Cards clearly distinguish inherited/global/admin values from release-specific values and missing values."
    - "Segment clicks route into a release-specific editor surface while preserving existing Theme/Segment seams."
---

<objective>
Populate expanded release rows with Theme-/Segment-Kontext cards that show what exists globally, what exists for this release, and what is missing.
</objective>

<context>
@.planning/phases/31-ui-umbau/31-CONTEXT.md
@.planning/phases/31-ui-umbau/31-RESEARCH.md
@frontend/src/app/admin/fansubs/[id]/edit/page.tsx
@frontend/src/app/admin/fansubs/[id]/edit/ReleaseThemeAssetsSection.tsx
@frontend/src/lib/api.ts
@backend/internal/repository/admin_content_anime_themes.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define frontend DTOs/helpers for release segment status</name>
  <read_first>
    - frontend/src/types/admin.ts
    - frontend/src/types/fansub.ts
    - frontend/src/lib/api.ts
    - backend/internal/models/admin_release_theme_assets.go
  </read_first>
  <action>Add or reuse frontend types that represent a release segment card with exact fields: `theme_id`, `theme_type_name`, `theme_title`, `status` where status is one of `global`, `release`, `missing`, optional `media_id`, optional `public_url`, and optional `source_label`. Prefer adapting existing admin theme/theme-asset response data before adding a new endpoint. If existing responses cannot produce this shape without extra backend work, add a narrow frontend mapper that emits the shape from available theme assets and marks unknowns as `missing` only when the theme exists but has no release asset.</action>
  <acceptance_criteria>
    - `frontend/src` contains the string `ReleaseSegmentStatus`.
    - `frontend/src` contains the string `global` and `release` and `missing` in the segment status context.
    - `cd frontend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render segment cards inside expanded release rows</name>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
    - frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css
    - frontend/src/app/admin/fansubs/[id]/edit/ReleaseThemeAssetsSection.tsx
  </read_first>
  <action>Replace the plan-01 placeholder shell with a grid of Theme-/Segment-Karten. Each card must show the segment type label, optional title, status badge, and whether an asset exists. Use German copy: `Global gesetzt`, `Release-spezifisch`, `Fehlt noch`. A clicked card should set local editor state for that release and segment; it must not upload immediately. Do not render release logo/banner controls.</action>
  <acceptance_criteria>
    - `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` contains `Global gesetzt`.
    - `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` contains `Release-spezifisch`.
    - `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` contains `Fehlt noch`.
    - `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` does not contain visible strings `Release Logo` or `Release Banner`.
    - `cd frontend && npm run build` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Use fansub 17 and release 92 as live UAT data. Expand `11eyes: Pink Phantasmagoria` release 92 and verify cards render with status badges. Confirm no generic release logo/banner UI appears.
</verification>

<success_criteria>
The expanded release row tells the admin what Theme-/Segment work exists or is missing for that exact release.
</success_criteria>

<output>
Create `.planning/phases/31-ui-umbau/31-02-SUMMARY.md`.
</output>
