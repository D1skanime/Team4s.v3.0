---
phase: 87-sichtbarkeits-steuerung-per-rolle-capability-pflege-ui
plan: 03
type: execute
wave: 3
depends_on:
  - 87-02
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/app/admin/role-capabilities/page.tsx
  - frontend/src/app/admin/role-capabilities/RoleCapabilityClient.tsx
  - frontend/src/app/admin/role-capabilities/RoleCapabilityTable.tsx
  - frontend/src/app/admin/role-capabilities/GrantCapabilityModal.tsx
  - frontend/src/app/admin/role-capabilities/RevokeCapabilityModal.tsx
  - frontend/src/app/admin/role-capabilities/RoleCapabilityClient.test.tsx
  - frontend/src/app/admin/page.tsx
autonomous: false
requirements:
  - D-04
  - D-05
  - D-06
  - D-07

must_haves:
  truths:
    - "D-04: Route /admin/role-capabilities existiert und ist durch PlatformAdminGate geschützt"
    - "D-05: Alle Komponenten importieren ausschließlich @/components/ui — kein natives button/select/input"
    - "D-05: Deutscher UI-Text mit korrekten Umlauten (Capability-Verwaltung, Entziehen, Vergeben)"
    - "D-06: Sofort-Wirksamkeits-Hinweis unter PageHeader sichtbar (kein Toast, dezenter p-Text)"
    - "D-07: HTTP-409-Antwort vom Entzugs-Endpunkt zeigt Inline-Fehler im Modal — kein Toast"
    - "D-04: Rollen×Actions-Matrix-Tabelle zeigt alle Rollen und alle Actions aus GET /admin/role-capabilities"
    - "Standalone-Actions zeigen read-only Badge Systemaktion — kein Button"
    - "Nav-Link Capability-Verwaltung auf /admin/page.tsx verlinkt auf /admin/role-capabilities"
    - "npm test RoleCapabilityClient grün"
  artifacts:
    - path: "frontend/src/app/admin/role-capabilities/page.tsx"
      provides: "Server Component mit PlatformAdminGate"
      min_lines: 10
    - path: "frontend/src/app/admin/role-capabilities/RoleCapabilityClient.tsx"
      provides: "CSR State-Management, Fetching, Modal-Orchestrierung"
    - path: "frontend/src/app/admin/role-capabilities/RoleCapabilityTable.tsx"
      provides: "Rollen×Actions-Tabelle mit Zell-Logik"
    - path: "frontend/src/app/admin/role-capabilities/GrantCapabilityModal.tsx"
      provides: "Vergabe-Modal mit Pending-State + Inline-Error"
    - path: "frontend/src/app/admin/role-capabilities/RevokeCapabilityModal.tsx"
      provides: "Entzugs-Modal mit 409-Lockout-Handling + Inline-Error"
    - path: "frontend/src/lib/api.ts"
      provides: "listRoleCapabilities, grantRoleCapability, revokeRoleCapability"
  key_links:
    - from: "RoleCapabilityClient.tsx handleRevokeConfirm"
      to: "revokeRoleCapability in api.ts"
      via: "ApiError status===409 → Inline-Lockout-Fehler im Modal"
      pattern: "ApiError.*409.*lockout"
    - from: "RoleCapabilityTable.tsx"
      to: "RoleCapabilityClient.tsx onGrant/onRevoke callbacks"
      via: "Props-Interface — keine direkten API-Aufrufe in Table-Komponente"
      pattern: "onGrant|onRevoke"
---

<objective>
Frontend-Pflege-UI: /admin/role-capabilities-Route, Rollen×Actions-Matrix-Tabelle, Vergabe/Entzugs-Modals, Nav-Link, api.ts-Funktionen und abschließende Human-Verify-Prüfung.

Purpose: D-04/D-05 erfüllen — Platform-Admin kann Capabilities pro Rolle vergeben/entziehen ohne Deploy. UI nutzt ausschließlich @/components/ui, zeigt Sofort-Wirksamkeits-Hinweis und behandelt Lockout-Guard-409 als Inline-Fehler statt Toast.
Output: Vollständige Pflege-UI + GREEN-Tests + Human-Verify-Checkpoint
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/87-sichtbarkeits-steuerung-per-rolle-capability-pflege-ui/87-CONTEXT.md
@.planning/phases/87-sichtbarkeits-steuerung-per-rolle-capability-pflege-ui/87-RESEARCH.md
@.planning/phases/87-sichtbarkeits-steuerung-per-rolle-capability-pflege-ui/87-UI-SPEC.md
@.planning/phases/87-sichtbarkeits-steuerung-per-rolle-capability-pflege-ui/87-VALIDATION.md

<interfaces>
<!-- TypeScript-Typen aus Plan 87-01 (frontend/src/types/admin-capability.ts) -->

```typescript
export interface RoleActionState {
  code: string
  label_de: string
  category: string
  granted: boolean
  standalone: boolean
}

export interface RoleEntry {
  role_code: string
  label_de: string
  actions: RoleActionState[]
}

export interface ActionEntry {
  code: string
  label_de: string
  category: string
  sort_order: number
}

export interface RoleCapabilityMatrix {
  roles: RoleEntry[]
  all_actions: ActionEntry[]
}
```

<!-- Aus frontend/src/app/admin/users/tabs/UserGlobalRolesTab.tsx (Muster-Vorlage) -->

Modal-Muster (analog — anpassen für Capability-Kontext):
```tsx
<Modal open={open} onClose={onClose} title="..." footer={...}>
  {mutationError && <p role="alert" style={{ color: 'var(--color-error)' }}>{mutationError}</p>}
</Modal>
```

Pending-Button-Muster:
```tsx
<Button variant="danger" onClick={onConfirm} disabled={isMutating}>
  {isMutating ? 'Wird verarbeitet …' : 'Capability entziehen'}
</Button>
```

LoadingState/ErrorState/EmptyState aus @/components/ui vorhanden.

<!-- api.ts API-Basis-Pattern (aus revokeAdminUserGlobalRole) -->

```typescript
const baseUrl = getBaseUrl()
const url = `${baseUrl}/api/v1/admin/...`
const response = await fetch(url, { method: 'DELETE', headers: { Authorization: getAuthHeader() } })
if (!response.ok) {
  const body = await response.json().catch(() => ({}))
  throw new ApiError(response.status, extractErrorMessage(body, 'Fallback-Fehler'))
}
```

<!-- Aus frontend/src/app/admin/page.tsx (Nav-Link-Kontext) -->

Bestehende Nav-Links im styles.actions-div:
- /admin/anime (Studio)
- /admin/users (Benutzer & Rechte)
- /manage/groups (Meine Gruppen)
- /me/profile (Mein Profil)

Neuer Link: /admin/role-capabilities mit Linktext "Capability-Verwaltung" als styles.button-Klasse.

<!-- PlatformAdminGate aus @/components/auth/PlatformAdminGate -->

```tsx
import { PlatformAdminGate } from '@/components/auth/PlatformAdminGate'
export default function Page() {
  return <PlatformAdminGate><RoleCapabilityClient /></PlatformAdminGate>
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: api.ts + Komponenten-Dateien + GREEN-Tests</name>
  <files>frontend/src/lib/api.ts, frontend/src/app/admin/role-capabilities/page.tsx, frontend/src/app/admin/role-capabilities/RoleCapabilityClient.tsx, frontend/src/app/admin/role-capabilities/RoleCapabilityTable.tsx, frontend/src/app/admin/role-capabilities/GrantCapabilityModal.tsx, frontend/src/app/admin/role-capabilities/RevokeCapabilityModal.tsx, frontend/src/app/admin/role-capabilities/RoleCapabilityClient.test.tsx</files>
  <behavior>
    - listRoleCapabilities(): GET /api/v1/admin/role-capabilities → Promise<RoleCapabilityMatrix>; wirft ApiError bei non-ok
    - grantRoleCapability(roleCode, actionCode): PUT /api/v1/admin/role-capabilities/:roleCode/:actionCode → Promise<void>
    - revokeRoleCapability(roleCode, actionCode): DELETE /api/v1/admin/role-capabilities/:roleCode/:actionCode → Promise<void>; bei 409 wirft ApiError mit status=409
    - page.tsx: Server Component, PlatformAdminGate wraps RoleCapabilityClient
    - RoleCapabilityClient: State LOADING → READY; loadData ruft listRoleCapabilities; pending-Modal-State (grantPending, revokePending); onGrant/onRevoke Callbacks an RoleCapabilityTable; Kategorie-Filter via Select (clientseitig, kein API-Aufruf); Sofort-Wirksamkeits-Hinweis als <p>; maximal 450 Zeilen
    - RoleCapabilityTable: nimmt roles: RoleEntry[], filteredCategory: string, onGrant(roleCode, actionCode), onRevoke(roleCode, actionCode); rendert Table mit TableHeaderCell pro Action-Spalte; pro Zelle: granted+!standalone → Badge "info" + Button "Entziehen" variant="danger" size="sm"; !granted+!standalone → Button "Vergeben" variant="secondary" size="sm"; standalone=true → Badge "muted" aria-label="Systemaktion — nicht über Rollen konfigurierbar"; maximal 450 Zeilen
    - GrantCapabilityModal: open, roleLabel, actionLabel, isMutating, mutationError, onConfirm, onClose; Inline-Error als <p role="alert">; Footer: Abbrechen + "Capability vergeben" primary; maximal 100 Zeilen
    - RevokeCapabilityModal: analog GrantCapabilityModal; Footer-Button variant="danger"; bei 409 im Client zeigt speziellen Lockout-Text; maximal 100 Zeilen
    - RoleCapabilityClient.test.tsx (GREEN): (1) zeigt LoadingState beim initialen Laden (mock fetch hängt), (2) nach resolvedData RoleCapabilityMatrix zeigt Tabelle mit ersten Rollen-label_de aus mock-data, (3) nach HTTP-409-Mock auf revoke zeigt Inline-Lockout-Fehlertext im Modal
  </behavior>
  <action>
    Ergänze in frontend/src/lib/api.ts drei neue Funktionen am Ende der Datei. Jede Funktion nutzt getBaseUrl(), getAuthHeader(), fetch, ApiError, extractErrorMessage — exakt analog zu revokeAdminUserGlobalRole (bestehende Vorlage in derselben Datei). Importiere RoleCapabilityMatrix aus @/types/admin-capability.

    listRoleCapabilities: GET, kein Body, Response-Body als RoleCapabilityMatrix parsen mit response.json().
    grantRoleCapability(roleCode: string, actionCode: string): PUT, kein Body, Content-Type: application/json, nur auf !response.ok Fehler werfen.
    revokeRoleCapability(roleCode: string, actionCode: string): DELETE, kein Body, Fehler wie oben.

    Erstelle page.tsx als 'use server' (keine Direktive nötig — App Router default). Importiere PlatformAdminGate und RoleCapabilityClient. Gibt PlatformAdminGate > RoleCapabilityClient zurück.

    Erstelle RoleCapabilityClient.tsx ('use client'). Importiere: useState, useCallback, useEffect von react; Typen aus @/types/admin-capability; UI-Primitives (Badge, Button, EmptyState, ErrorState, LoadingState, Modal, PageHeader, SectionHeader, Select) aus @/components/ui; die drei api.ts-Funktionen; ApiError aus @/lib/api. State: matrix (RoleCapabilityMatrix|null), isLoading, error, filteredCategory, pendingGrant ({roleCode,actionCode,roleLabel,actionLabel}|null), pendingRevoke analog, grantError, revokeError, isGranting, isRevoking. loadData via useCallback+useEffect. Kategorie-Filterung: leite uniqueCategories aus matrix?.all_actions ab. Sofort-Wirksamkeits-Hinweis: <p style={{color:'var(--color-text-secondary)',fontSize:'0.875rem',marginBottom:'var(--space-4)'}}> mit dem Copywriting-Contract-Text. Gibt RoleCapabilityTable an onGrant={openGrantModal} onRevoke={openRevokeModal}. Modale als GrantCapabilityModal + RevokeCapabilityModal eingebunden.

    Erstelle RoleCapabilityTable.tsx. Empfängt Props: roles, allActions (ActionEntry[]), filteredCategory, onGrant, onRevoke. Filtert Spalten nach filteredCategory (wenn nicht ""). Rendert Table, TableHead mit leerem Header für Rollen-Label + TableHeaderCell pro Action (category+label_de). Pro Rolle: TableRow, erster TableCell = role.label_de. Pro Action-Spalte: finde actions.find(a=>a.code===action.code), rendere entsprechende Zelle.

    Erstelle GrantCapabilityModal.tsx und RevokeCapabilityModal.tsx als eigene Dateien (je maximal 100 Zeilen). Props: open, roleLabel, actionLabel, isMutating, mutationError, onConfirm, onClose. Implementiere nach UI-SPEC Copywriting-Contract (korrekte Umlaute im Code als Strings, keine Code-Identifier).

    Befülle RoleCapabilityClient.test.tsx mit echten Tests (ersetzt Wave-0-RED-Stub). Nutze @vitest-environment jsdom Direktive. Mock global.fetch. Test 1: fetch hängt → LoadingState sichtbar. Test 2: fetch löst mit RoleCapabilityMatrix-Mock auf (1 Rolle "fansub_lead"/"Fansub-Lead", 2 Actions) → erwarte role.label_de in DOM. Test 3: revoke-fetch gibt 409 zurück → handleRevokeConfirm schlägt mit Inline-Lockout-Text fehl.

    Zeilenzahl-Check vor dem Schreiben: wenn RoleCapabilityClient > 450 Zeilen drohen, Kategorie-Filter-Logik in RoleCapabilityClient.helpers.ts auslagern.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/app/admin/role-capabilities/ 2>&1 | tail -20</automated>
  </verify>
  <done>npm test grün für alle RoleCapabilityClient-Tests; alle Komponenten-Dateien unter 450 Zeilen; tsc --noEmit fehlerfrei; @/components/ui-Imports für alle UI-Elemente (kein natives button/select/input)</done>
</task>

<task type="auto">
  <name>Task 2: Nav-Link + ESLint-Check + Build-Verifikation</name>
  <files>frontend/src/app/admin/page.tsx</files>
  <action>
    Lese frontend/src/app/admin/page.tsx vor der Änderung (Pflicht). Ergänze in der styles.actions-div-Sektion einen neuen Link direkt unter dem "Benutzer & Rechte"-Link:

    ```
    <Link href="/admin/role-capabilities" className={styles.button}>
      Capability-Verwaltung
    </Link>
    ```

    Linktext ist "Capability-Verwaltung" (korrekte Umlaute, per Copywriting-Contract). Klasse styles.button (primäre Action-Klasse, analog zu "Studio (Anime + Episoden)" und "Benutzer & Rechte").

    Danach: prüfe ob alle neuen Komponenten-Dateien ESLint-konform sind. Keine nativen select/input/button-Elemente in den neuen Dateien (CLAUDE.md-Pflicht). Führe ESLint-Check auf den neuen Dateien durch. Falls Fehler: beheben.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>npm run build fehlerfrei; /admin/page.tsx enthält Link auf /admin/role-capabilities mit Text "Capability-Verwaltung"; ESLint meldet keine no-restricted-syntax-Fehler in neuen Dateien</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Vollständige Capability-Pflege-UI unter /admin/role-capabilities:
    - Rollen×Actions-Matrix-Tabelle mit Vergeben/Entziehen-Buttons
    - Vergabe-Modal (Bestätigung + Pending-State)
    - Entzugs-Modal (Bestätigung + Lockout-Guard-409-Inline-Fehler)
    - Sofort-Wirksamkeits-Hinweis unter PageHeader
    - Standalone-Aktionen als read-only "Systemaktion"-Badge
    - Nav-Link "Capability-Verwaltung" auf /admin-Übersicht

    View-Enforcement (Backend):
    - GET /admin/fansubs/:id/group-members prüft ActionFansubGroupMembersView
    - GET /admin/fansubs/:id/unified-members prüft ActionFansubGroupMembersView
    - GET /admin/fansubs/:id/anime-coverage prüft ActionFansubGroupMembersView
  </what-built>
  <how-to-verify>
    Dev-Server: http://localhost:3000 (npm run dev im frontend-Ordner)

    1. Öffne http://localhost:3000/admin
       → Link "Capability-Verwaltung" im Actions-Bereich sichtbar

    2. Klicke auf "Capability-Verwaltung" → http://localhost:3000/admin/role-capabilities
       → Seitenheader "Capability-Verwaltung" (28px, semibold)
       → Sofort-Wirksamkeits-Hinweis unter Header: "Änderungen werden nach dem Cache-Reload..."
       → Rollen×Actions-Matrix lädt (Spinner sichtbar, dann Tabelle)
       → Tabelle zeigt Rollen (fansub_lead, translator, ...) als Zeilen
       → Tabelle zeigt Actions als Spalten-Header (release.view, fansub_group.edit, ...)

    3. Suche in der Tabelle nach ActionFansubGroupInvitationsAccept (standalone):
       → Zelle zeigt Badge "Systemaktion" (kein Button)

    4. Klicke "Vergeben" für eine nicht-zugewiesene Action einer Rolle:
       → Modal öffnet sich mit Titel "Capability vergeben"
       → Beschreibungstext enthält role.label_de und action.label_de
       → Klicke "Abbrechen" → Modal schließt

    5. Klicke "Entziehen" für eine zugewiesene Action:
       → Modal öffnet sich mit Titel "Capability entziehen"
       → Klicke "Capability entziehen" → Bestätigung ausstehend
       → Nach Erfolg: Tabelle aktualisiert sich

    6. View-Enforcement testen (Dev-API):
       Öffne Browser-DevTools → Network
       Navigiere zu einer Seite die GET /admin/fansubs/:id/group-members aufruft
       → Prüfe ob Platform-Admin-User einen 200 erhält, ein User ohne Mitgliedschaft einen 403
       (Alternativ: curl http://localhost:8092/api/v1/admin/fansubs/1/group-members mit Dev-Token vs. ohne Token → 401)
  </how-to-verify>
  <resume-signal>
    Tippe "approved" wenn alle Punkte bestätigt, oder beschreibe gefundene Probleme mit Punkt-Nummer (z.B. "Punkt 3 fehlgeschlagen: Badge nicht sichtbar")
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /admin/role-capabilities | PlatformAdminGate server-seitig + requirePlatformAdminIdentity backend-seitig (doppelter Schutz) |
| Frontend → api.ts → Backend | Authorization-Header bei allen 3 Capability-API-Aufrufen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-09 | Elevation of Privilege | Frontend zeigt Capability-UI ohne Auth-Check | mitigate | PlatformAdminGate auf page.tsx (Server Component); Backend-Gate zusätzlich in Handler |
| T-87-10 | Tampering | Doppelter Submit durch fehlendes disabled während API-Call | mitigate | isMutating-State disabled alle Buttons im Modal während Pending |
| T-87-11 | Elevation of Privilege | Lockout-Guard-Fehler wird vom Frontend als Erfolg interpretiert | mitigate | ApiError(status=409) wird als Inline-Fehler dargestellt, Modal bleibt offen — kein Auto-Reload |
| T-87-12 | Repudiation | Standalone-Aktion wird trotz read-only Badge klickbar | mitigate | standalone=true: kein Button, nur aria-label Badge — keine onClick-Handler |
| T-87-SC | Tampering | npm installs | accept | Keine neuen npm-Pakete; nur bestehende @/components/ui und react-Importe |
</threat_model>

<verification>
Nach Plan 03 muss gelten:
1. `npm test -- --run src/app/admin/role-capabilities/` → alle Tests PASS
2. `npm run build` → fehlerfrei (keine TypeScript-/ESLint-Fehler)
3. Human-Verify-Checkpoint approved (Rollen×Actions-Matrix, Modals, Sofort-Wirksamkeits-Hinweis, Nav-Link)
4. `grep -rn "native.*<button\|native.*<select\|native.*<input" frontend/src/app/admin/role-capabilities/` → keine Treffer
5. Alle Dateien ≤450 Zeilen: `wc -l frontend/src/app/admin/role-capabilities/*.tsx`
</verification>

<success_criteria>
- /admin/role-capabilities ist erreichbar für Platform-Admins (PlatformAdminGate)
- Rollen×Actions-Matrix lädt und zeigt alle Rollen und Actions aus der API
- Standalone-Aktionen zeigen "Systemaktion"-Badge, kein Button
- Vergabe-Modal und Entzugs-Modal funktionieren mit Pending-State und Inline-Fehler
- HTTP-409-Lockout wird als Inline-Fehler im Modal gezeigt (kein Toast)
- Nav-Link "Capability-Verwaltung" auf /admin-Übersicht vorhanden
- Human-Verify approved
</success_criteria>

<output>
Erstelle `.planning/phases/87-sichtbarkeits-steuerung-per-rolle-capability-pflege-ui/87-03-SUMMARY.md` wenn fertig.
</output>
