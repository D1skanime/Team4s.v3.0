---
phase: 32
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/cmd/server/admin_routes.go
  - backend/internal/handlers/admin_content_release_theme_assets.go
  - backend/internal/handlers/admin_content_fansub_releases_test.go
  - frontend/src/lib/api.ts
  - frontend/src/types/admin.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Direct release Theme asset upload writes to the existing release_theme_assets table."
    - "No new DB table or fansub_group_media runtime path is introduced."
    - "Upload uses the concrete release_id selected in the Fansub Release row, not a canonical fansub/anime fallback."
---

<objective>
Add the smallest missing direct release upload API so the Phase 32 drawer can save Theme assets for the exact selected release.
</objective>

<context>
@.planning/phases/32-fansub-release-side-drawer-aus-phase-31-edit-drawer-fuer-release-theme-assets-mit-vorhandenen-db-tabellen-und-apis-ohne-neue-datenmodelle-db-ui-differenzen-vor-umsetzung-diskutieren/32-CONTEXT.md
@.planning/phases/32-fansub-release-side-drawer-aus-phase-31-edit-drawer-fuer-release-theme-assets-mit-vorhandenen-db-tabellen-und-apis-ohne-neue-datenmodelle-db-ui-differenzen-vor-umsetzung-diskutieren/32-RESEARCH.md
@backend/internal/handlers/admin_content_release_theme_assets.go
@backend/internal/repository/admin_content_anime_themes.go
@backend/internal/models/admin_release_theme_assets.go
@frontend/src/lib/api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add direct release Theme asset upload route</name>
  <read_first>
    - backend/cmd/server/admin_routes.go
    - backend/internal/handlers/admin_content_release_theme_assets.go
    - backend/internal/repository/admin_content_anime_themes.go
    - backend/internal/models/admin_release_theme_assets.go
  </read_first>
  <action>Add handler method `UploadReleaseThemeAssetForRelease` in `backend/internal/handlers/admin_content_release_theme_assets.go`. It must handle `POST /api/v1/admin/releases/:releaseId/theme-assets`, require admin auth, parse `releaseId` route param, parse multipart form field `theme_id`, read multipart form field `file`, call the same `h.mediaService.SaveVideoUpload`, `h.mediaRepo.CreateMediaAsset`, and `h.themeRepo.CreateReleaseThemeAsset` sequence used by `UploadReleaseThemeAsset`, but pass `models.AdminReleaseThemeAssetCreateInput{ReleaseID: releaseID, ThemeID: themeID, MediaID: mediaAsset.ID}` directly. On create/link failure, call `h.mediaRepo.DeleteMediaAsset` and `removeFileQuietly` exactly like the existing upload cleanup path. Register the route in `backend/cmd/server/admin_routes.go` beside the existing `/admin/releases/:releaseid/theme-assets` list/delete routes.</action>
  <acceptance_criteria>
    - `backend/internal/handlers/admin_content_release_theme_assets.go` contains `func (h *AdminContentHandler) UploadReleaseThemeAssetForRelease`.
    - `backend/internal/handlers/admin_content_release_theme_assets.go` contains `AdminReleaseThemeAssetCreateInput{`.
    - `backend/internal/handlers/admin_content_release_theme_assets.go` contains `ReleaseID: releaseID`.
    - `backend/cmd/server/admin_routes.go` contains `POST("/admin/releases/:releaseid/theme-assets"`.
    - `Select-String -Path backend/internal/handlers/admin_content_release_theme_assets.go -Pattern "fansub_group_media"` returns no matches.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add API helper for release-scoped upload</name>
  <read_first>
    - frontend/src/lib/api.ts
    - frontend/src/types/admin.ts
  </read_first>
  <action>In `frontend/src/lib/api.ts`, add interface `AdminReleaseThemeAssetUploadForReleaseOptions` with fields `releaseID: number`, `themeID: number`, `file: File`, `authToken?: string`, and `onProgress?: (percent: number) => void`. Add exported function `uploadAdminReleaseThemeAssetForRelease(options: AdminReleaseThemeAssetUploadForReleaseOptions): Promise<AdminReleaseThemeAssetCreateResponse>` using `XMLHttpRequest` and endpoint `${API_BASE_URL}/api/v1/admin/releases/${options.releaseID}/theme-assets`. The multipart body must set `theme_id` and `file`. Keep the existing `uploadAdminReleaseThemeAsset` helper unchanged for current callers.</action>
  <acceptance_criteria>
    - `frontend/src/lib/api.ts` contains `interface AdminReleaseThemeAssetUploadForReleaseOptions`.
    - `frontend/src/lib/api.ts` contains `export async function uploadAdminReleaseThemeAssetForRelease`.
    - `frontend/src/lib/api.ts` contains `/api/v1/admin/releases/${options.releaseID}/theme-assets`.
    - `frontend/src/lib/api.ts` still contains `export async function uploadAdminReleaseThemeAsset(`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cover route and build checks</name>
  <read_first>
    - backend/internal/handlers/admin_content_fansub_releases_test.go
    - backend/cmd/server/admin_routes.go
    - backend/internal/handlers/admin_content_release_theme_assets.go
  </read_first>
  <action>Add or update handler tests so the direct route and handler method are source-verified. The test must check for `/admin/releases/:releaseid/theme-assets`, `UploadReleaseThemeAssetForRelease`, and the existing list/delete route strings. If a full multipart handler unit test is practical with the current stubs, add it; otherwise keep source-presence coverage and rely on repository tests for `CreateReleaseThemeAsset` behavior.</action>
  <acceptance_criteria>
    - `backend/internal/handlers/admin_content_fansub_releases_test.go` or a new handler test file contains `UploadReleaseThemeAssetForRelease`.
    - `backend/internal/handlers/admin_content_fansub_releases_test.go` or a new handler test file contains `/admin/releases/:releaseid/theme-assets`.
    - `cd backend && go test ./internal/handlers ./internal/repository -count=1` exits 0.
    - `cd frontend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
This plan passes when a direct upload helper and route exist for the exact selected release and no new data authority has been introduced.
</verification>

<success_criteria>
The frontend drawer can call one direct release-scoped upload helper and persist to `release_theme_assets` through existing repository code.
</success_criteria>
