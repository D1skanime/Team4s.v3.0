---
phase: 114-oeffentliche-fansub-gruppen-uebersicht
plan: 04
type: execute
wave: 3
depends_on: ["114-02", "114-03"]
files_modified: []
autonomous: false
requirements: [D-01, D-02, D-05]

must_haves:
  truths:
    - "A live visitor (anonymous or logged-in) can reach /fansubs via the AppShell nav and see the rendered directory (D-01, D-02)"
    - "The directory's Anime-Projekte figure for a sampled group matches that same group's own detail page, confirming the Plan 114-01 metric fix holds live (D-02/D-03 regression guard)"
    - "Round logos and initials fallbacks render correctly on real data (D-05)"
  artifacts: []
  key_links: []
---

<objective>
Close out Phase 114 with a live verification pass on `:3000`: confirm the nav entries built in
Plan 114-02, the directory page built in Plan 114-03, and the backend metric fix from Plan 114-01
all work together correctly against real running services, both anonymous and logged in.

Purpose: automated tests cover logic/rendering in isolation with mocked data; this plan is the
one place that proves the full stack (Docker backend + Docker frontend + real DB rows) agrees —
in particular the "Anime-Projekte" parity between the new directory and the existing detail page,
which is the exact bug (Pitfall 1, `114-RESEARCH.md`) this phase was designed to avoid.

Output: a confirmed-working live phase; no new code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-oeffentliche-fansub-gruppen-uebersicht/114-CONTEXT.md
@.planning/phases/114-oeffentliche-fansub-gruppen-uebersicht/114-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restart the frontend container and run an automated SSR structural check</name>
  <files></files>
  <read_first>
    frontend/src/app/fansubs/page.tsx (from Plan 114-03, to know what strings must appear in SSR HTML)
    frontend/src/components/layout/AppShell.tsx (from Plan 114-02, to know the nav label to check for)
  </read_first>
  <action>
    Restart the frontend dev container so the new `/fansubs` route and AppShell nav changes are
    served (Turbopack dev mode does not hot-reload across a restart boundary reliably for new
    routes; per project convention this requires `docker restart team4sv30-frontend`, no
    `--build` needed since source is volume-mounted). Confirm the real host port via `docker ps`
    (do not assume `.env` defaults). Then curl the live SSR HTML of `GET :{port}/fansubs` and
    confirm the response contains: the page title `Fansub-Gruppen`, all four exact column labels
    (`Fansub-Gruppe`, `Anime-Projekte`, `Release-Versionen`, `Mitglieder`), and at least one
    `href="/fansubs/` row link. Also curl `GET :{port}/anime` (or any anonymous-mode page) and
    confirm the initial HTML contains the nav text `Fansub-Gruppen` linked to `/fansubs`, proving
    the nav entry is present in the anonymous render path.
  </action>
  <acceptance_criteria>
    - `docker ps` shows the frontend container running with a recent restart timestamp
    - curl of /fansubs SSR HTML contains all 4 exact column labels and at least one /fansubs/ row link
    - curl of an anonymous-mode page's SSR HTML contains the nav text "Fansub-Gruppen" linked to /fansubs
  </acceptance_criteria>
  <verify>
    <automated>P=$(docker port team4sv30-frontend 3000 | head -1 | sed 's/.*://') && curl -s "http://localhost:${P}/fansubs" | grep -q 'Anime-Projekte' && curl -s "http://localhost:${P}/fansubs" | grep -q 'href="/fansubs/'</automated>
  </verify>
  <done>Frontend container is running fresh code; automated curl checks confirm the page and nav render structurally correct SSR HTML.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify the live Fansub-Gruppen nav entry and directory on :3000</name>
  <files></files>
  <read_first>
    .planning/phases/114-oeffentliche-fansub-gruppen-uebersicht/114-UI-SPEC.md
  </read_first>
  <what-built>
    The full Phase 114 stack: an activated "Fansub-Gruppen" nav entry (visible anonymous and
    logged in, Plan 114-02), a new `/fansubs` directory page listing every fansub group sorted by
    release-versions descending with round logo/initials cells (Plan 114-03), and a corrected
    "Anime-Projekte" backend metric that now excludes disabled-anime relations (Plan 114-01).
  </what-built>
  <action>
    Present the already-automated `/fansubs` directory and both AppShell nav states for human
    live verification on `:3000`; append only the observed outcomes, do not re-derive UI decisions
    already locked in `114-UI-SPEC.md`.
  </action>
  <how-to-verify>
    1. Open http://localhost:3000 in a browser WITHOUT logging in. Confirm a "Fansub-Gruppen" nav
       entry is visible in the drawer and clicking it navigates to /fansubs.
    2. On /fansubs, confirm: every fansub group is listed (compare row count to the total group
       count you'd expect); the table is sorted by Release-Versionen descending, with ties broken
       alphabetically by group name; each row shows either a round logo image or a two-letter
       initials placeholder (no broken images, no missing fallback); clicking a group name
       navigates to its existing /fansubs/[slug] detail page.
    3. Pick one group from the directory. Note its "Anime-Projekte" number on /fansubs, then open
       its detail page /fansubs/[slug] and confirm the "Anime-Projekte" stat shown there matches
       exactly (this is the Pitfall-1 regression check — the numbers must agree).
    4. Log in, then repeat step 1: confirm "Fansub-Gruppen" is ALSO visible and clickable in the
       authenticated nav drawer (not just anonymous).
    5. Resize the browser to a narrow/mobile width and confirm the table remains usable via
       horizontal scroll (no broken layout, no column loss).
  </how-to-verify>
  <acceptance_criteria>
    - Nav entry reachable and correctly routed in both anonymous and authenticated states
    - Directory row count, sort order, logo/initials rendering, and row navigation match the UI-SPEC
    - Sampled group's Anime-Projekte figure matches identically between /fansubs and /fansubs/[slug]
  </acceptance_criteria>
  <verify>
    <automated>docker ps --filter "name=team4sv30-frontend" --format "{{.Status}}"</automated>
  </verify>
  <done>Human confirms nav visibility (both auth states), directory rendering/sorting/logo behavior, row navigation, and Anime-Projekte parity all match expectations on live :3000.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — this plan performs verification only, no code changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-114-04-01 | (n/a — verification-only plan) | n/a | accept | No new code or surface is introduced by this plan; all threats relevant to Phase 114 are registered and dispositioned in Plans 114-01/02/03 |
</threat_model>

<verification>
Automated curl-based structural check (Task 1) plus human live browser confirmation (Task 2)
covering nav visibility (both auth states), directory rendering/sorting, round-logo/initials
fallback, row navigation, and the Anime-Projekte parity regression check.
</verification>

<success_criteria>
Phase 114 goal is live-confirmed: any visitor, anonymous or logged in, can discover and browse all
fansub groups from a nav entry, land on a correctly sorted, correctly metriced directory, and
navigate into any group's existing detail page.
</success_criteria>

<output>
Create `.planning/phases/114-oeffentliche-fansub-gruppen-uebersicht/114-04-SUMMARY.md` when done
</output>
