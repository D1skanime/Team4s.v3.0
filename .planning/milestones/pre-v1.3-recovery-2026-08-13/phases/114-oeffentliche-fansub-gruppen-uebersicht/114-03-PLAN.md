---
phase: 114-oeffentliche-fansub-gruppen-uebersicht
plan: 03
type: execute
wave: 2
depends_on: ["114-01"]
files_modified:
  - frontend/src/components/ui/AvatarStack.tsx
  - frontend/src/app/fansubs/page.tsx
  - frontend/src/app/fansubs/page.module.css
  - frontend/src/app/fansubs/page.test.tsx
autonomous: true
requirements: [D-02, D-04, D-05]

must_haves:
  truths:
    - "Any visitor can view /fansubs and see every fansub group listed, sorted by release-versions descending, group name ascending as tie-break (D-05)"
    - "Each row links to the existing /fansubs/[slug] detail page (D-02)"
    - "A group's row shows a round 32px logo, or an initials fallback when logo_url is absent, consistent with the round member-avatar convention (D-05)"
    - "The directory is built entirely from @/components/ui global primitives — no hand-built table/select/input/button (D-04)"
    - "All 500-capacity groups are fetched in one request (no silent 24-row truncation)"
  artifacts:
    - path: "frontend/src/app/fansubs/page.tsx"
      provides: "SSR async Server Component rendering the 4-column sortable directory"
      min_lines: 40
    - path: "frontend/src/app/fansubs/page.test.tsx"
      provides: "Render/sort/empty/error/link/no-fan-out regression coverage"
    - path: "frontend/src/app/fansubs/page.module.css"
      provides: "Colocated page-level layout and logo-cell styling"
    - path: "frontend/src/components/ui/AvatarStack.tsx"
      provides: "Exported initials() helper reused by the directory's logo-fallback cell"
  key_links:
    - from: "frontend/src/app/fansubs/page.tsx"
      to: "frontend/src/lib/api.ts getFansubList"
      via: "await getFansubList({ per_page: 500 })"
      pattern: "getFansubList\\(\\{\\s*per_page:\\s*500\\s*\\}\\)"
    - from: "frontend/src/app/fansubs/page.tsx"
      to: "/fansubs/[slug] detail route"
      via: "Link href={`/fansubs/${group.slug}`}"
      pattern: "fansubs/\\$\\{group\\.slug\\}"
    - from: "frontend/src/app/fansubs/page.tsx"
      to: "frontend/src/components/ui/AvatarStack.tsx initials()"
      via: "import { initials } from '@/components/ui'"
      pattern: "initials\\(group\\.name\\)"
---

<objective>
Build the new public fansub-group directory at `/fansubs` (CONTEXT D-02): a single SSR page
listing every fansub group with round logo/name, Anime-Projekte, Release-Versionen, and
Mitglieder, sorted by release-versions descending with name as the ascending tie-break, each row
linking to the existing `/fansubs/[slug]` detail page.

Purpose: the deactivated "Fansub-Gruppen" nav entry (activated in Plan 114-02) needs a real
destination, and the projects_count metric added in Plan 114-01 needs a consumer. This plan is
the actual product surface of Phase 114.

Output: `frontend/src/app/fansubs/page.tsx` (+ colocated CSS + test), plus a one-line export
change to `AvatarStack.tsx` so its `initials()` helper can be reused instead of duplicated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-oeffentliche-fansub-gruppen-uebersicht/114-CONTEXT.md
@.planning/phases/114-oeffentliche-fansub-gruppen-uebersicht/114-UI-SPEC.md
@frontend/src/app/members/ranking/page.tsx
@frontend/src/app/members/ranking/page.test.tsx
</context>

<interfaces>
<!-- getFansubList (no change, consumed as-is; includes projects_count after Plan 114-01) -->
From frontend/src/lib/api.ts (lines 1568-1593):
```ts
export async function getFansubList(
  params: FansubListParams = {},
): Promise<FansubGroupListResponse> {
  const API_BASE_URL = getApiBaseUrl();
  const query = buildFansubListQuery(params);
  const url = `${API_BASE_URL}/api/v1/fansubs${query ? `?${query}` : ""}`;
  const response = await fetch(url, { cache: "no-store" });
  // ... throws ApiError(response.status, ...) on !response.ok
  return response.json() as Promise<FansubGroupListResponse>;
}
```
`FansubListParams` (lines 470-475): `{ q?: string; status?: FansubStatus; page?: number; per_page?: number }`.

<!-- resolveApiUrl, used for the round logo src -->
From frontend/src/lib/api.ts (lines 347-367): `export function resolveApiUrl(value?: string): string`
— resolves relative/`/api/`-prefixed paths and passes through absolute http(s) URLs. Used
identically by the detail page for `group.banner_url`/`group.logo_url`.

<!-- FansubGroup shape (after Plan 114-01 adds projects_count) -->
From frontend/src/types/fansub.ts (lines 21-46), relevant fields for this page:
```ts
export interface FansubGroup {
  id: number;
  slug: string;
  name: string;
  logo_url?: string | null;
  projects_count: number;        // added by Plan 114-01
  anime_relations_count: number; // DO NOT use for "Anime-Projekte" — wrong semantics, see 114-RESEARCH.md Q2
  release_versions_count: number;
  members_count: number;
  // ...
}
export interface FansubGroupListResponse { data: FansubGroup[]; meta: PaginationMeta }
```

<!-- initials() pattern to export and reuse (frontend/src/components/ui/AvatarStack.tsx, lines 23-30) -->
```ts
function initials(label: string) {
  return label.trim().split(/\s+/).slice(0, 2).map((part) => part.charAt(0).toUpperCase()).join('') || '?'
}
```
Add `export` to this declaration. It becomes reachable via the `@/components/ui` barrel
(`frontend/src/components/ui/index.ts` already has `export * from './AvatarStack'`).

<!-- Round-image usage pattern (same file, lines 49-57) -->
```tsx
<Image src={item.imageUrl} alt="" width={32} height={32} className={styles.avatarStackImage} unoptimized />
```
Reuse this shape (`alt=""`, 32x32, `unoptimized`) but with a new local page-scoped CSS class, not
`styles.avatarStackImage` (that class is scoped to `ui.module.css`/`AvatarStack`).

<!-- @/components/ui primitives used on this page (real signatures, verified in-repo) -->
- `PageHeader`: `{ title, description?, eyebrow?, actions?, breadcrumbs? }`
- `Table`: `{ variant?: 'default'|'compact'|'selectable'|'withActions' }` — use `variant="selectable"`
- `TableHead`, `TableBody`, `TableRow`: plain section/row wrappers
- `TableHeaderCell`: `<th scope="col">` (scope defaults to `col`)
- `TableCell`: `<td>` wrapper
- `EmptyState`: `{ title, description? }`
- `ErrorState` + `getErrorStateCopy(error, { defaultTitle, defaultDescription })`: returns `{ title, description }`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write the failing page.test.tsx scaffold for /fansubs</name>
  <files>frontend/src/app/fansubs/page.test.tsx</files>
  <read_first>
    frontend/src/app/members/ranking/page.test.tsx (full file — exact mock/test scaffold to mirror)
    frontend/src/components/layout/AppShell.test.tsx (lines 24-29 — next/image mock pattern, needed here since this page also uses next/image)
    frontend/src/types/fansub.ts (FansubGroup shape, incl. projects_count from Plan 114-01)
    .planning/phases/114-oeffentliche-fansub-gruppen-uebersicht/114-UI-SPEC.md (Table Columns, Copywriting Contract sections — exact German strings)
  </read_first>
  <behavior>
    - Test 1: renders exactly the 4 column headers `Fansub-Gruppe`, `Anime-Projekte`,
      `Release-Versionen`, `Mitglieder` (exact German text, UI-SPEC "Table Columns").
    - Test 2: given 3 mock groups out of order, rows render in
      `release_versions_count` DESC / `name` ASC tie-break order (assert via row text order,
      e.g. `screen.getAllByRole('row')` or reading rendered name sequence).
    - Test 3: each group's name renders as a `<Link>` with `href="/fansubs/{slug}"`.
    - Test 4: a group with `logo_url` set renders an `<img>`; a group with `logo_url: null` renders
      an initials fallback (derived from the group name, e.g. two-letter uppercase).
    - Test 5: empty state — `getFansubList` resolves `{ data: [], meta: {...} }` → renders
      `Noch keine Fansub-Gruppen`.
    - Test 6: error state — `getFansubList` rejects → renders `Fehler beim Laden`.
    - Test 7: `getFansubList` is called exactly once per render, with `{ per_page: 500 }` (no
      per-row API fan-out — mirrors the SC-4 discipline already tested on the ranking page).
  </behavior>
  <action>
    Create frontend/src/app/fansubs/page.test.tsx mirroring
    frontend/src/app/members/ranking/page.test.tsx's structure: `// @vitest-environment jsdom`
    header; `vi.hoisted` for `getFansubListMock`; `vi.mock('next/link', ...)` passthrough anchor
    (same shape as the ranking test); `vi.mock('next/image', ...)` passthrough `<img>` (mirror
    AppShell.test.tsx's pattern, since this page renders next/image for the logo); `vi.mock('next/navigation', ...)`
    only if the page needs it (it does not take searchParams, so this may be omittable — include
    only if a compile/runtime error surfaces without it); `vi.mock('@/lib/api', ...)` exposing
    `ApiError` (same class shape as the ranking test), `getFansubList: getFansubListMock`, and
    `resolveApiUrl: (v: string) => v` (passthrough, since jsdom has no real API base URL). Write
    the 7 test cases from `<behavior>` above inside a `describe('FansubsPage (D-02/D-05)', ...)`
    block. Since `frontend/src/app/fansubs/page.tsx` does not exist yet, import it anyway
    (`import FansubsPage from './page'`) and run the suite — it MUST fail (module not found /
    every test red). Confirm this failure before moving to Task 2.
  </action>
  <acceptance_criteria>
    - frontend/src/app/fansubs/page.test.tsx exists with exactly the 7 test cases described above
    - Running `npx vitest run src/app/fansubs/page.test.tsx` fails (RED) because ./page has no implementation yet
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/app/fansubs/page.test.tsx</automated>
  </verify>
  <done>page.test.tsx exists with all 7 cases, confirmed red for the expected reason (missing implementation).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement the /fansubs directory page (export initials, page, CSS)</name>
  <files>frontend/src/components/ui/AvatarStack.tsx, frontend/src/app/fansubs/page.tsx, frontend/src/app/fansubs/page.module.css</files>
  <read_first>
    frontend/src/app/members/ranking/page.tsx (full file — the scaffold to adapt: fetch/try-catch, three-way ErrorState/EmptyState/Table branch, `export const dynamic = 'force-dynamic'`)
    frontend/src/app/members/ranking/page.module.css (`.page` wrapper class pattern)
    frontend/src/components/ui/AvatarStack.tsx (lines 23-30 initials, lines 49-57 Image usage)
    .planning/phases/114-oeffentliche-fansub-gruppen-uebersicht/114-UI-SPEC.md (exact copy strings, column order, spacing tokens, C-3 line-limit note)
  </read_first>
  <action>
    1. In frontend/src/components/ui/AvatarStack.tsx, add the `export` keyword to
       `function initials(label: string)` (line 23) — no other change to this file.
    2. Create frontend/src/app/fansubs/page.module.css with a `.page` class mirroring
       members/ranking/page.module.css's wrapper (`width: min(1280px, 100% - 48px); margin: 0
       auto; padding: 24px 0 48px;`), plus `.logoCell` (flex row, `gap: var(--space-2)`,
       `align-items: center`) and `.logoImage` (`border-radius: 50%`, `object-fit: cover`) classes
       for the round 32px logo — reuse existing `--space-*`/`--accent-*` custom properties from
       globals.css, do not invent new tokens (C-4).
    3. Create frontend/src/app/fansubs/page.tsx as an async Server Component with no props:
       `export const dynamic = 'force-dynamic'`; fetch inside try/catch exactly like the ranking
       page's `fetchError` pattern: `const result = await getFansubList({ per_page: 500 })`
       (per_page MUST be explicit — the backend default is 24, which would silently truncate).
       On success, build a sorted copy:
       `[...result.data].sort((a, b) => b.release_versions_count - a.release_versions_count || a.name.localeCompare(b.name, 'de'))`.
       Render `<PageHeader title="Fansub-Gruppen" description="Alle Fansub-Gruppen im Überblick — sortiert nach aktivsten Gruppen zuerst." />`.
       Three-way branch: `fetchError` → `<ErrorState {...getErrorStateCopy(fetchError, { defaultTitle: 'Fehler beim Laden', defaultDescription: 'Die Fansub-Gruppen konnten nicht geladen werden.' })} />`;
       `sorted.length === 0` → `<EmptyState title="Noch keine Fansub-Gruppen" description="Sobald Fansub-Gruppen angelegt sind, erscheinen sie hier in der Übersicht." />`;
       else → `<Table variant="selectable">` with a `<TableHead><TableRow>` of 4
       `<TableHeaderCell scope="col">` (`Fansub-Gruppe`, `Anime-Projekte`, `Release-Versionen`,
       `Mitglieder`) and a `<TableBody>` mapping `sorted` to `<TableRow key={group.id}>` with:
       cell 1 = round logo (`group.logo_url ? <Image src={resolveApiUrl(group.logo_url)} alt=""
       width={32} height={32} className={styles.logoImage} unoptimized /> : <span
       aria-hidden="true">{initials(group.name)}</span>`) plus `<Link
       href={`/fansubs/${group.slug}`}>{group.name}</Link>`, cell 2 = `{group.projects_count}`,
       cell 3 = `{group.release_versions_count}`, cell 4 = `{group.members_count}`. Import
       `initials` from `@/components/ui`, `resolveApiUrl`/`getFansubList` from `@/lib/api`,
       `Image` from `next/image`, `Link` from `next/link`.
    4. Re-run frontend/src/app/fansubs/page.test.tsx and confirm all 7 Task-1 cases pass (GREEN).
       Run `npm run typecheck` to confirm `projects_count` resolves cleanly from Plan 114-01's
       type addition. Check the final line count of page.tsx — if it approaches 450 lines (C-3),
       extract the row/logo-cell markup into a colocated `FansubDirectoryRow.tsx` before
       finishing; not expected to be necessary for a 4-column table.
  </action>
  <acceptance_criteria>
    - AvatarStack.tsx's `initials` function has the `export` keyword
    - page.tsx calls `getFansubList({ per_page: 500 })` exactly once, no per-row fetch
    - page.tsx sorts by release_versions_count desc, name asc (locale 'de') before rendering
    - Table renders 4 columns with exact German headers from the UI-SPEC
    - Round logo uses `alt=""`, 32x32, `unoptimized`; missing logo renders `initials(group.name)`
    - Every row's group name links to `/fansubs/{slug}`
    - `npx vitest run src/app/fansubs/page.test.tsx` fully green (all 7 cases)
    - `npm run typecheck` reports no new errors
    - frontend/src/app/fansubs/page.tsx stays at or below 450 lines
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/app/fansubs/page.test.tsx && npm run typecheck</automated>
  </verify>
  <done>/fansubs renders the 4-column sortable directory with round-logo/initials cells and working row links; all tests green; typecheck clean; file within the line limit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → SSR page → public GET /api/v1/fansubs | Already-public, unauthenticated, read-only list endpoint; page adds zero new input surface (no forms, no query params consumed) |
| Group-provided strings (name, logo_url) → rendered DOM | Data originates from admin-managed fansub_groups rows, not end-user input, but is rendered on a public page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-114-03-01 | Tampering (XSS via group name) | `<Link>{group.name}</Link>` / initials fallback text | mitigate | Rendered as plain JSX text content only (React auto-escapes text nodes); no `dangerouslySetInnerHTML` or raw HTML injection point is introduced anywhere on this page |
| T-114-03-02 | Tampering (malicious logo_url as image src) | `<Image src={resolveApiUrl(group.logo_url)} />` | accept | Same `resolveApiUrl` seam already used unmodified by the existing public `/fansubs/[slug]` detail page for `banner_url`/`logo_url` — no new trust boundary or handling introduced by this phase; `<img>`/`next/image` src attributes do not execute injected script in modern browsers even for unusual URL values |
| T-114-03-03 | Information Disclosure | projects_count/release_versions_count/members_count cells | accept | Aggregate counts only, already publicly exposed via the existing detail page for the same groups |
| T-114-03-SC | Tampering (supply chain) | npm installs | accept | No new package installs in this plan — N/A |
</threat_model>

<verification>
`cd frontend && npx vitest run src/app/fansubs/page.test.tsx && npm run typecheck` green.
</verification>

<success_criteria>
`/fansubs` renders every fansub group in one request, sorted release-versions-desc/name-asc, with
round-logo/initials cells and working row links to the existing detail route — built entirely
from `@/components/ui` primitives, fully regression-tested.
</success_criteria>

<output>
Create `.planning/phases/114-oeffentliche-fansub-gruppen-uebersicht/114-03-SUMMARY.md` when done
</output>
