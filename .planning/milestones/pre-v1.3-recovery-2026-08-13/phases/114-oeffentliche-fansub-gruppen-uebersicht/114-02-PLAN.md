---
phase: 114-oeffentliche-fansub-gruppen-uebersicht
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/components/layout/AppShell.test.tsx
autonomous: true
requirements: [D-01]

must_haves:
  truths:
    - "Logged-in users see an enabled 'Fansub-Gruppen' nav entry that links to /fansubs (D-01)"
    - "Anonymous users see an enabled 'Fansub-Gruppen' nav entry that links to /fansubs (D-01)"
    - "No other nav entry (Dashboard, Suche) is accidentally enabled as a side effect"
  artifacts:
    - path: "frontend/src/components/layout/AppShell.tsx"
      provides: "Fansub-Gruppen entries in both AppShellNavGroups.publicItems and AppShellAnonNavGroups.publicItems, both pointing at /fansubs"
    - path: "frontend/src/components/layout/AppShell.test.tsx"
      provides: "Regression tests pinning both nav arrays expose the enabled /fansubs entry"
  key_links:
    - from: "frontend/src/components/layout/AppShell.tsx (AppShellNavGroups + AppShellAnonNavGroups)"
      to: "/fansubs route (built in Plan 114-03)"
      via: "href: '/fansubs' nav item entries"
      pattern: "label: 'Fansub-Gruppen', href: '/fansubs'"
---

<objective>
Activate the currently-disabled "Fansub-Gruppen" AppShell nav entry so it is visible AND
clickable for both anonymous and authenticated users (CONTEXT D-01), pointing at the new `/fansubs`
directory built in Plan 114-03.

Purpose: `AppShell.tsx` has TWO structurally separate nav-item arrays. The anonymous array
(`AppShellAnonNavGroups.publicItems`, line ~189) has a disabled "Fansub-Gruppen" placeholder that
looks like a simple flip. The authenticated array (`AppShellNavGroups.publicItems`, lines
120-124) has NO "Fansub-Gruppen" entry at all today — it must be newly inserted, not toggled.
Editing only the anonymous array would leave logged-in users with zero nav path to `/fansubs`,
silently failing D-01's "sichtbar anonym UND eingeloggt" requirement.

Output: Both `publicItems` arrays expose an enabled, linked "Fansub-Gruppen" entry; a regression
test pins this for both render modes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-oeffentliche-fansub-gruppen-uebersicht/114-CONTEXT.md
</context>

<interfaces>
<!-- Current state of both nav arrays (verified in-repo, frontend/src/components/layout/AppShell.tsx) -->

AppShellNavGroups.publicItems (authenticated, lines 120-124) — NO Fansub-Gruppen entry today:
```tsx
const publicItems: AppShellNavItem[] = [
  { label: 'Anime entdecken', href: '/anime', icon: <Compass size={17} />, current: isCurrent(currentPath, '/anime') },
  { label: 'Rangliste', href: '/members/ranking', icon: <Trophy size={17} />, current: isCurrent(currentPath, '/members/ranking') },
  { label: 'Dashboard', icon: <LayoutDashboard size={17} />, disabled: true, badge: 'bald' },
]
```

AppShellAnonNavGroups.publicItems (anonymous, lines 186-191) — disabled placeholder:
```tsx
const publicItems: AppShellNavItem[] = [
  { label: 'Anime entdecken', href: '/anime', icon: <Compass size={17} />, current: isCurrent(currentPath, '/anime') },
  { label: 'Rangliste', href: '/members/ranking', icon: <Trophy size={17} />, current: isCurrent(currentPath, '/members/ranking') },
  { label: 'Fansub-Gruppen', icon: <Users size={17} />, disabled: true, badge: 'bald' },
  { label: 'Suche', icon: <Compass size={17} />, disabled: true, badge: 'bald' },
]
```

`Users` icon is already imported at line 16 (`import { ..., Users, ... } from 'lucide-react'`) —
no new import needed. `isCurrent(currentPath, href)` (lines 65-67) is the existing helper for the
`current` flag. `AppShellNavItemView` (lines 69-103) already renders any item with `href` set and
`disabled` unset as a real `<Link>` — no rendering-logic changes needed, only the two array edits.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — add failing tests pinning the enabled /fansubs nav entry in both arrays</name>
  <files>frontend/src/components/layout/AppShell.test.tsx</files>
  <read_first>
    frontend/src/components/layout/AppShell.tsx (full file — both publicItems arrays, AppShellNavItemView rendering branch lines 69-103)
    frontend/src/components/layout/AppShell.test.tsx (existing describe('AppShell', ...) block, mock scaffolding for next/link/next/navigation/next/image/useLogoutAuthSession, lines 1-38, existing test-case style lines 40-79)
  </read_first>
  <behavior>
    - Test A: render `<AppShell mode="authenticated" currentPath="/fansubs" user={{ displayName: 'Mika', email: 'mika@example.com' }}>`; assert `screen.getByRole('link', { name: /Fansub-Gruppen/i })` exists with `href === '/fansubs'`.
    - Test B: render `<AppShell mode="anonymous" currentPath="/fansubs">`; assert the same enabled link exists with `href === '/fansubs'`, and assert no `aria-disabled="true"` element with that label remains.
    - Both tests MUST fail before Task 2's implementation (authenticated array has no such entry at all; anonymous entry is currently `disabled: true` with no `href`).
  </behavior>
  <action>
    Add a new `describe('Fansub-Gruppen nav (D-01)', ...)` block to
    frontend/src/components/layout/AppShell.test.tsx (after the existing top-level test cases,
    following the same render/mock conventions already in the file — no new mocks needed, the file
    already mocks next/link, next/navigation, next/image, and useLogoutAuthSession). Add the two
    test cases described above. Run the test file and confirm both new cases fail (RED) for the
    reasons stated — do not proceed to Task 2 until this failure is confirmed.
  </action>
  <acceptance_criteria>
    - Two new test cases exist in AppShell.test.tsx asserting an enabled /fansubs link in authenticated mode and in anonymous mode
    - Running `npx vitest run src/components/layout/AppShell.test.tsx` shows exactly these 2 new cases failing, all pre-existing cases still passing
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/layout/AppShell.test.tsx</automated>
  </verify>
  <done>Two new RED test cases exist and are confirmed failing for the documented reason (missing/disabled nav entry), with no other test regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — insert/enable the Fansub-Gruppen entry in both publicItems arrays</name>
  <files>frontend/src/components/layout/AppShell.tsx</files>
  <read_first>
    frontend/src/components/layout/AppShell.tsx (AppShellNavGroups.publicItems, lines 120-124; AppShellAnonNavGroups.publicItems, lines 186-191)
  </read_first>
  <action>
    In `AppShellNavGroups`'s `publicItems` array (~lines 120-124), insert a new entry between the
    'Rangliste' entry and the disabled 'Dashboard' entry (Dashboard stays last/disabled — it is an
    unrelated, separately-deferred feature per CONTEXT.md's deferred list):
    `{ label: 'Fansub-Gruppen', href: '/fansubs', icon: &lt;Users size={17} /&gt;, current: isCurrent(currentPath, '/fansubs') }`.
    In `AppShellAnonNavGroups`'s `publicItems` array (~lines 186-191), replace the disabled
    'Fansub-Gruppen' placeholder entry with the same enabled shape — drop `disabled`/`badge`, add
    `href`+`current`: `{ label: 'Fansub-Gruppen', href: '/fansubs', icon: &lt;Users size={17} /&gt;, current: isCurrent(currentPath, '/fansubs') }`.
    Keep the 'Suche' entry in the anonymous array disabled (separate deferred feature, out of
    scope for this phase). Do not add a new import — `Users` is already imported at line 16.
    Re-run the full AppShell.test.tsx suite to confirm the two Task 1 cases now pass (GREEN) and
    no pre-existing case regressed (in particular the D-17 "exactly one Account navigation entry"
    case and the admin-capability-gating cases).
  </action>
  <acceptance_criteria>
    - AppShellNavGroups.publicItems contains an entry with label 'Fansub-Gruppen', href '/fansubs', no disabled/badge
    - AppShellAnonNavGroups.publicItems contains an entry with label 'Fansub-Gruppen', href '/fansubs', no disabled/badge
    - AppShellAnonNavGroups.publicItems still contains the 'Suche' entry with disabled: true (unchanged)
    - `npx vitest run src/components/layout/AppShell.test.tsx` is fully green (all cases, old and new)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/layout/AppShell.test.tsx</automated>
  </verify>
  <done>Both publicItems arrays expose an enabled 'Fansub-Gruppen' entry linking to /fansubs; full AppShell.test.tsx suite green, including the 2 new D-01 cases and all pre-existing cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client-side nav rendering | Purely presentational; static route strings, no user input, no new data fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-114-02-01 | Spoofing / Tampering | AppShellNavGroups / AppShellAnonNavGroups publicItems arrays | accept | Static, developer-authored href strings; no user-controlled input reaches these arrays; no new auth or data surface is introduced |
| T-114-02-SC | Tampering (supply chain) | npm installs | accept | No new package installs in this plan — N/A |
</threat_model>

<verification>
`cd frontend && npx vitest run src/components/layout/AppShell.test.tsx` fully green (all cases).
</verification>

<success_criteria>
Both authenticated and anonymous AppShell nav renders expose an enabled 'Fansub-Gruppen' entry
linking to /fansubs, pinned by regression tests, with zero pre-existing test regressions.
</success_criteria>

<output>
Create `.planning/phases/114-oeffentliche-fansub-gruppen-uebersicht/114-02-SUMMARY.md` when done
</output>
