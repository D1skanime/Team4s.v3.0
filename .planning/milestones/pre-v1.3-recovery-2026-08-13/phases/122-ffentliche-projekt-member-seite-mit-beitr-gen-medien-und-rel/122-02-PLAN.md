---
phase: 122-ffentliche-projekt-member-seite-mit-beitr-gen-medien-und-rel
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - backend/internal/handlers/project_member_public_handler.go
  - backend/internal/handlers/project_member_public_handler_test.go
  - backend/cmd/server/main.go
  - shared/contracts/openapi.yaml
autonomous: true
requirements: [D-08, D-09, D-10]

must_haves:
  truths:
    - "Die kombinierte Sicht wird ueber vier getrennte, oeffentliche Read-Endpunkte bereitgestellt; es gibt keine Riesen-Response mit allen Collections (D-08, Brief 21)"
    - "Routing folgt der bestehenden Konvention /anime/:id/group/:groupId/... (groupPublicHandler), NICHT einer neuen /fansubs/:groupId/...-Kette (Brief 21 erlaubt konventionskonformes Routing)"
    - "Unbekannter Anime/Gruppe/Member oder fehlende Projektbeziehung -> 404; niemals Redirect auf /members/:slug (D-10)"
    - "Notes/Media/Releases akzeptieren limit (geklammert) + cursor und liefern next_cursor; Summary ist eine eigene, kleine Response (D-08)"
    - "Endpunkte sind unauthenticated public (wie die uebrigen /anime/:id/group/:groupId/-Routen), liefern ausschliesslich oeffentliche Inhalte (D-09)"
  artifacts:
    - path: "backend/internal/handlers/project_member_public_handler.go"
      provides: "ProjectMemberPublicHandler mit GetSummary, GetNotes, GetMedia, GetReleases; 404-Gate via ResolveMemberRelation"
    - path: "backend/cmd/server/main.go"
      provides: "4 neue v1.GET-Routen unter /anime/:id/group/:groupId/members/:memberSlug[/notes|/media|/releases]"
    - path: "shared/contracts/openapi.yaml"
      provides: "OpenAPI-Pfade + Schemas (ProjectMemberSummary, ProjectMemberNote(s), ProjectMemberMedia, ProjectMemberRelease, Cursor-Envelope)"
  key_links:
    - from: "v1.GET /anime/:id/group/:groupId/members/:memberSlug"
      to: "ProjectMemberPublicHandler.GetSummary"
      via: "gin route registration neben bestehenden groupPublic-Routen (main.go ~Zeile 379-382)"
      pattern: "group/:groupId/members/:memberSlug"
    - from: "shared/contracts/openapi.yaml paths"
      to: "frontend/src/types + api.ts (Plan 122-03)"
      via: "npm run generate erzeugt TS-Typen aus OpenAPI"
      pattern: "/anime/\\{id\\}/group/\\{groupId\\}/members/\\{memberSlug\\}"
---

<objective>
Exponiert die Datenschicht aus 122-01 als vier getrennte oeffentliche Endpunkte und verankert sie im
kanonischen OpenAPI-Contract. Summary/Hero getrennt von den paginierten Notes/Media/Releases (D-08,
Brief 21). Route-Konvention folgt den bestehenden `groupPublicHandler`-Routen
(`/anime/:id/group/:groupId/...`), was der Brief in 21 ausdruecklich erlaubt.

Purpose: Der Brief verbietet clientseitiges Vollladen+Filtern und verlangt getrennte Collections mit
Cursor. Der Handler-Satz ist die oeffentliche Vertragsflaeche; ohne OpenAPI-Update koennen die
Frontend-Typen (122-03) nicht generiert werden (AGENTS.md API-Contract-Regel).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-ffentliche-projekt-member-seite-mit-beitr-gen-medien-und-rel/122-CONTEXT.md
@backend/internal/handlers/group_contributors_handler.go
@backend/internal/handlers/app_public_profile.go
@backend/cmd/server/main.go
@shared/contracts/openapi.yaml
</context>

<interfaces>
Handler-Muster: `groupPublicHandler` (GetGroupContributors, GetGroupReleaseListCursor,
GetGroupReleaseMedia) parst `:id` (animeID) + `:groupId` via strconv, ruft Repo, mappt 404 auf
gin JSON 404. Neu zusaetzlich `:memberSlug` (string, kein parse). Cursor/limit aus Query
(`c.Query("cursor")`, `c.Query("limit")`), limit -> clampCursorLimit im Repo.
ProjectMemberPublicRepository (122-01) liefert alle Daten. Response-Envelope fuer Listen:
`{ items: [...], next_cursor: string|null, has_more: bool }` (Muster GetGroupReleaseListCursor).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ProjectMemberPublicHandler (4 Methoden + 404-Gate)</name>
  <files>backend/internal/handlers/project_member_public_handler.go</files>
  <read_first>
    backend/internal/handlers/group_contributors_handler.go (kompletter Handler: param-parse, repo-call, 404-Mapping);
    backend/internal/handlers/app_public_profile.go (GetPublicMemberProfile: 404 fuer unbekannten member-slug).
  </read_first>
  <action>
    Handler-Struct mit *ProjectMemberPublicRepository. Alle vier Methoden: parse animeID/groupID
    (400 bei ungueltig), lese memberSlug; rufe ResolveMemberRelation -> bei exists=false 404
    ({"error":"..."}) und Ende. Danach:
    - GetSummary -> repo.GetSummary, 200 JSON.
    - GetNotes/GetMedia/GetReleases -> repo.ListXxx(cursor, limit), 200 mit
      { items, next_cursor, has_more }. Deutsche user-facing Fehlerstrings mit korrekten Umlauten.
    Keine Auth-Middleware (public, wie die anderen group-Routen).
  </action>
  <verify>go build ./... ; go vet ./backend/internal/handlers/</verify>
</task>

<task type="auto">
  <name>Task 2: Routen-Registrierung + Handler-Verdrahtung in main.go</name>
  <files>backend/cmd/server/main.go</files>
  <read_first>
    backend/cmd/server/main.go Zeilen 285-295 (Repo-Konstruktion) + 372-382 (group-public-Routen-Block).
  </read_first>
  <action>
    Konstruiere `projectMemberPublicRepo := repository.NewProjectMemberPublicRepository(dbPool)` und
    `projectMemberPublicHandler := handlers.NewProjectMemberPublicHandler(projectMemberPublicRepo)`
    im bestehenden Konstruktions-Block. Registriere direkt nach den group-public-Routen:
    `v1.GET("/anime/:id/group/:groupId/members/:memberSlug", projectMemberPublicHandler.GetSummary)`
    und `.../members/:memberSlug/notes|/media|/releases`. Reihenfolge/Gruppierung wie die
    Nachbar-Routen; keine Auth-Middleware.
  </action>
  <verify>go build ./... ; go run ./backend/cmd/server --help 2>/dev/null || true</verify>
</task>

<task type="auto">
  <name>Task 3: OpenAPI-Contract</name>
  <files>shared/contracts/openapi.yaml</files>
  <read_first>
    shared/contracts/openapi.yaml (bestehende /anime/{id}/group/{groupId}/contributors + /release-media
    Pfade als Muster fuer Pfad-Parameter, Cursor-Query, Response-Schemas).
  </read_first>
  <action>
    Ergaenze vier Pfade unter `/anime/{id}/group/{groupId}/members/{memberSlug}` (+ /notes, /media,
    /releases) mit Pfad-Parametern, `limit`/`cursor` Query-Params fuer die Listen, 404-Response und
    Response-Schemas: ProjectMemberSummary (hero + roles[] + counts{roles,notes,media,releases}),
    ProjectMemberNote, ProjectMemberMedia, ProjectMemberRelease, und ein generisches
    CursorPage-Envelope { items, next_cursor, has_more }. snake_case, konsistent mit bestehenden Schemas.
  </action>
  <verify>python3 -c "import yaml,sys; yaml.safe_load(open('shared/contracts/openapi.yaml')); print('openapi valid yaml')"</verify>
</task>

<task type="auto">
  <name>Task 4: Handler-Tests (200-Shapes + 404-Gate)</name>
  <files>backend/internal/handlers/project_member_public_handler_test.go</files>
  <read_first>
    backend/internal/handlers/group_contributors_handler_test.go (httptest+gin-Muster);
    backend/internal/handlers/app_public_profile_test.go (404-Assertions).
  </read_first>
  <action>
    Tests mit gin-Testkontext: (a) gueltige Kombination -> 200, Summary/Listen-Shapes korrekt;
    (b) unbekannter Member bzw. fehlende Projektbeziehung -> 404 (kein Redirect); (c) limit/cursor
    werden durchgereicht; (d) Listen enthalten { items, next_cursor, has_more }.
  </action>
  <verify>go test ./backend/internal/handlers/ -run ProjectMemberPublic -v 2>&1 | tail -40</verify>
</task>

</tasks>

<acceptance>
- Vier getrennte public Endpunkte erreichbar; Summary klein, Listen cursor-paginierter Envelope.
- 404 bei ungueltiger Kombination; nie Redirect auf /members/:slug.
- OpenAPI aktualisiert und valides YAML; bereit fuer npm run generate (122-03).
- go build/vet/test sauber.
</acceptance>
