---
phase: 122-ffentliche-projekt-member-seite-mit-beitr-gen-medien-und-rel
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - frontend/src/types/projectMember.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/fansubProjectRoutes.ts
  - frontend/src/lib/fansubProjectRoutes.test.ts
autonomous: true
requirements: [D-02, D-08]

must_haves:
  truths:
    - "Frontend-Typen entsprechen dem OpenAPI-Contract aus 122-02 (regeneriert, nicht handgeraten) — AGENTS.md API-Contract-Regel"
    - "Vier getrennte api.ts-Helper (Summary + cursor Notes/Media/Releases) existieren; keine Sammel-Funktion, die alles auf einmal laedt (D-08)"
    - "Ein Route-Helper baut /fansubs/[slug]/fansubprojekt/[animeSlug]/mitwirkende/[memberSlug] und wird von 122-04/122-05 wiederverwendet (D-02)"
    - "Cursor-Helper unterstuetzen AbortSignal (Request Cancellation) und werden bei Navigation abgebrochen (Brief 12/28)"
  artifacts:
    - path: "frontend/src/types/projectMember.ts"
      provides: "TS-Interfaces ProjectMemberSummary/Note/Media/Release + CursorPage<T> (aus __generated__ re-exportiert oder daran ausgerichtet)"
    - path: "frontend/src/lib/api.ts"
      provides: "getProjectMemberSummary, getProjectMemberNotes, getProjectMemberMedia, getProjectMemberReleases (cursor+limit+signal)"
    - path: "frontend/src/lib/fansubProjectRoutes.ts"
      provides: "buildPublicFansubProjectMemberPath(fansubSlug, animeSlug, memberSlug)"
  key_links:
    - from: "shared/contracts/openapi.yaml"
      to: "frontend/src/__generated__ + frontend/src/types/projectMember.ts"
      via: "npm run generate (backend muss laufen)"
      pattern: "ProjectMemberSummary"
    - from: "buildPublicFansubProjectMemberPath"
      to: "buildPublicFansubProjectPath"
      via: "haengt /mitwirkende/[memberSlug] an den bestehenden Projektpfad, encodeURIComponent"
      pattern: "/mitwirkende/"
---

<objective>
Legt die Frontend-Datenschicht an: regenerierte Typen aus dem 122-02-OpenAPI-Contract, vier getrennte
cursor-faehige api.ts-Helper und einen Route-Helper fuer die neue Projekt-Member-Route. Kein UI in
diesem Plan.

Purpose: 122-05..09 duerfen NICHT gegen ad-hoc fetch/handgeraten Typen arbeiten (AGENTS.md). Die
getrennten Helper erzwingen die unabhaengige Ladbarkeit der Collections (D-08) und die
Request-Cancellation-Anforderung (Brief 28).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-ffentliche-projekt-member-seite-mit-beitr-gen-medien-und-rel/122-CONTEXT.md
@frontend/src/lib/api.ts
@frontend/src/lib/fansubProjectRoutes.ts
@frontend/src/types/groupContributors.ts
@shared/contracts/openapi.yaml
</context>

<interfaces>
- `npm run generate` (im frontend/, backend auf :18092 erreichbar) erzeugt `frontend/src/__generated__/`
  aus OpenAPI. `projectMember.ts` richtet die konsumierten Interfaces daran aus (Muster
  groupContributors.ts: snake_case, matches backend DTOs).
- api.ts-Helper-Muster: `getGroupReleaseListCursor` (cursor+limit, resolvePublicApiUrl, gibt
  { items, next_cursor, has_more }). Neue Helper akzeptieren zusaetzlich `signal?: AbortSignal`.
- fansubProjectRoutes.ts: bestehende buildPublicFansubProjectPath / buildPublicFansubReleasePath.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: OpenAPI-Typen regenerieren + projectMember.ts</name>
  <files>frontend/src/types/projectMember.ts</files>
  <read_first>
    frontend/src/types/groupContributors.ts (DTO-Stil);
    shared/contracts/openapi.yaml (die 122-02-Schemas).
  </read_first>
  <action>
    `cd frontend && npm run generate` (backend muss laufen; falls nicht erreichbar, im Summary
    dokumentieren und Typen manuell am OpenAPI-Schema ausrichten). Lege `types/projectMember.ts` an,
    das ProjectMemberSummary/Note/Media/Release + CursorPage<T> exportiert (aus __generated__
    re-exportiert oder strukturgleich).
  </action>
  <verify>cd frontend && npm run typecheck 2>&1 | tail -15</verify>
</task>

<task type="auto">
  <name>Task 2: api.ts Cursor-Helper</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    frontend/src/lib/api.ts (getGroupReleaseListCursor + getGroupReleaseMedia: URL-Bau,
    resolvePublicApiUrl, Fehlerbehandlung/ApiError, evtl. bestehende signal-Nutzung).
  </read_first>
  <action>
    Vier Helper: `getProjectMemberSummary(animeID, groupID, memberSlug, signal?)` und
    `getProjectMemberNotes|Media|Releases(animeID, groupID, memberSlug, { cursor?, limit?, signal? })`
    gegen die 122-02-Routen. Listen geben CursorPage<T> zurueck. Kein Sammel-Endpoint. Kein Base64.
  </action>
  <verify>cd frontend && npm run typecheck 2>&1 | tail -15</verify>
</task>

<task type="auto">
  <name>Task 3: Route-Helper + Test</name>
  <files>frontend/src/lib/fansubProjectRoutes.ts, frontend/src/lib/fansubProjectRoutes.test.ts</files>
  <read_first>
    frontend/src/lib/fansubProjectRoutes.ts (buildPublicFansubProjectPath — encodeURIComponent, trim).
  </read_first>
  <action>
    `buildPublicFansubProjectMemberPath(fansubSlug, animeSlug, memberSlug)` =
    `${buildPublicFansubProjectPath(fansubSlug, animeSlug)}/mitwirkende/${encodeURIComponent(memberSlug.trim())}`.
    Test (falls fansubProjectRoutes.test.ts existiert, erweitern; sonst neu): korrekter Pfad,
    Encoding von Sonderzeichen im Slug.
  </action>
  <verify>cd frontend && npm run test -- fansubProjectRoutes 2>&1 | tail -20</verify>
</task>

</tasks>

<acceptance>
- npm run typecheck sauber; Route-Helper-Test gruen.
- Vier getrennte, cursor-faehige, abbrechbare api.ts-Helper vorhanden.
- Typen stammen aus dem OpenAPI-Contract, nicht ad hoc.
</acceptance>
