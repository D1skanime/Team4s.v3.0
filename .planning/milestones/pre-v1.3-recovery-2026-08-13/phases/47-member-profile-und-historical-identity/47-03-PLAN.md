---
phase: 47-member-profile-und-historical-identity
plan: "03"
type: execute
wave: 3
depends_on:
  - 47-02-PLAN.md
files_modified:
  - frontend/src/app
  - frontend/src/lib
  - frontend/src/types
autonomous: true
requirements:
  - MEMBER-PROFILE-01
must_haves:
  truths:
    - "The profile page is the canonical place for personal identity editing."
    - "Fansub admin pages may show membership context, but should not act as personal-profile editors."
    - "Keycloak account management must stay clearly separate in the UI."
---

<objective>
Implement the frontend profile experience: own profile page, avatar and story editing, memberships display, Keycloak account button, and cleanup of the misplaced profile affordance in fansub edit flows.

Purpose: Give users one clear place to maintain their historical identity without mixing it into group-admin screens.
</objective>

<context>
@.planning/phases/47-member-profile-und-historical-identity/47-02-PLAN.md
@.planning/phases/47-member-profile-und-historical-identity/47-CONTEXT.md
@frontend/src/app/admin/fansubs/[id]/edit/page.tsx
@frontend/src/app/admin/fansubs/[id]/members/page.tsx
@frontend/src/lib/api.ts
@frontend/src/types/fansubNotes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend frontend API/types for profile aggregate and capabilities</name>
  <read_first>
    - frontend/src/lib/api.ts
    - frontend/src/types
  </read_first>
  <action>
    Add or extend frontend types and client helpers for:
    - own profile read/update
    - avatar upload
    - profile capabilities
    - Keycloak account-link metadata
    - memberships and historical-credit read models

    Keep the contract clear that email is read-only and account security edits happen elsewhere.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck</automated>
  </verify>
  <done>
    - Frontend profile types match the backend aggregate contract.
    - The API seam clearly separates editable profile fields from read-only account fields.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the profile page and Keycloak account action</name>
  <read_first>
    - existing admin routing/navigation
    - existing avatar/media components
    - existing TipTap/story components where reusable
  </read_first>
  <action>
    Implement the profile page, preferably at `/admin/profile`, with:
    - avatar
    - fansub name
    - display name
    - bio
    - member story
    - active from/until
    - currently active flag
    - visibility
    - memberships and roles
    - historical credit section or prepared placeholder
    - `Accountdaten ändern` button when the Keycloak account URL is configured

    Reuse existing editor/media components where possible.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck && npm run lint</automated>
  </verify>
  <done>
    - Users have a dedicated profile page for personal archival identity editing.
    - Keycloak account management is presented as a separate action, not an inline Team4s form.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove or demote the misplaced profile affordance from fansub edit surfaces</name>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
    - related fansub admin navigation surfaces
  </read_first>
  <action>
    Update the fansub edit/member surfaces so they no longer behave like profile editors.

    Preferred outcome:
    - group-admin surfaces show membership context
    - personal profile editing happens only on `/admin/profile`
    - where useful, provide a `Profil ansehen` link instead of a misleading edit affordance
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run lint</automated>
  </verify>
  <done>
    - The profile boundary is clear in the UI.
    - Fansub edit pages no longer imply that personal identity is edited there.
  </done>
</task>

</tasks>

<verification>
Automated:
1. `cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck`
2. `cd C:/Users/admin/Documents/Team4s/frontend && npm run lint`

Behavioral:
1. The profile page loads and saves archival profile data.
2. The Keycloak account button is present only when configured.
3. Fansub edit surfaces no longer expose personal-profile editing in the wrong place.
</verification>

<success_criteria>
- The frontend has a dedicated profile route and clear Keycloak account action.
- The profile page reuses existing UI/media/editor seams where practical.
- The misplaced profile affordance is removed or reduced to a contextual profile link.
</success_criteria>
