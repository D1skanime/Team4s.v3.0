---
phase: 47-member-profile-und-historical-identity
plan: "01"
type: execute
wave: 1
depends_on:
  - ../46-fansub-group-invitations-und-join-requests-mvp/46-04-PLAN.md
files_modified:
  - backend/cmd/server
  - backend/internal/handlers
  - backend/internal/middleware
  - backend/internal/models
  - backend/internal/permissions
  - backend/internal/repository
  - database/migrations
  - docs
autonomous: true
requirements:
  - MEMBER-PROFILE-01
must_haves:
  truths:
    - "Phase 47 must distinguish Keycloak account data from Team4s historical profile data."
    - "Existing member/story/media structures should be reused wherever they fit."
    - "Historical credits may enrich profile output but may never become app-permission sources."
---

<objective>
Do the mandatory bestandsanalyse and establish the canonical profile foundation: validate the actual identity seam, decide whether profile fields belong on an existing member/profile table, and introduce only the minimal schema/repository additions required for the historical member-profile MVP.

Purpose: Phase 47 should not build a third identity model next to Keycloak and group membership.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/47-member-profile-und-historical-identity/47-CONTEXT.md
@.planning/phases/47-member-profile-und-historical-identity/47-RESEARCH.md
@database/migrations/0044_add_db_schema_v2_target_tables.up.sql
@database/migrations/0062_member_group_stories.up.sql
@frontend/src/app/admin/fansubs/[id]/edit/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyse user, member, story, media, and profile seams first</name>
  <read_first>
    - database/migrations
    - backend/internal/repository
    - frontend/src/lib/api.ts
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
  </read_first>
  <action>
    Perform the required Ist-Analyse and document:
    - `app_users`, `users`, `members`, and any bridges between them
    - current profile-like fields
    - avatar/media references and upload seams
    - membership and role tables
    - `member_group_stories` and other archival text/credit tables
    - audit seams
    - current auth/current-user/capability handling
    - where the fansub edit page currently implies member/profile actions

    Then decide:
    - which table is the canonical profile owner
    - whether `member_group_stories` can back the personal story field or needs a minimal adaptation
    - whether any migration is truly needed

    If the identity relationship between app-user and profile owner remains unclear, stop with a blocker.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s && git diff --check</automated>
  </verify>
  <done>
    - There is a documented profile-focused Ist-Analyse baseline.
    - Phase 47 has one explicit canonical profile owner seam.
    - Any migration need is justified rather than assumed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Establish the minimal profile data model and Keycloak-account separation</name>
  <read_first>
    - results of Task 1
    - backend/internal/models
    - backend/internal/repository
  </read_first>
  <action>
    Reuse an existing profile/member table if possible. Otherwise add only the smallest schema extension needed for:
    - `display_name`
    - `fansub_name`
    - avatar media reference
    - bio
    - member story linkage
    - active from/until
    - current active flag
    - profile visibility

    Also define the contract boundary:
    - email is read-only
    - keycloak subject is read-only
    - password/MFA remain Keycloak-only
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/models/... ./internal/repository/...</automated>
  </verify>
  <done>
    - The MVP profile model is explicit and minimal.
    - The Keycloak-vs-Team4s ownership boundary is encoded in the contract.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add repository/service seams for profile aggregate reads and avatar references</name>
  <read_first>
    - backend/internal/repository
    - backend/internal/permissions
  </read_first>
  <action>
    Introduce or extend repository/service helpers for:
    - loading own profile aggregate
    - updating allowed own-profile fields
    - reading memberships and roles for display
    - loading historical credits read-only where practical
    - setting/changing avatar media references through the existing media architecture
    - optional admin read/update path for other profiles if the runtime seam supports it cleanly
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/repository/... ./internal/permissions/...</automated>
  </verify>
  <done>
    - Repository/service seams exist for the profile aggregate and avatar reference flow.
    - Historical credit reads remain explicitly non-authz.
  </done>
</task>

</tasks>

<verification>
Automated:
1. `cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/repository/...`
2. `cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/permissions/...`
3. `cd C:/Users/admin/Documents/Team4s && git diff --check`

Targeted:
1. Verify the canonical profile owner seam is explicit.
2. Verify `member_group_stories` reuse/adaptation is a conscious decision.
3. Verify no new parallel identity model is introduced by accident.
</verification>

<success_criteria>
- Phase 47 starts with a documented seam analysis.
- The profile model is minimal and clearly separate from Keycloak account data.
- Repository/service seams exist for profile aggregate reads and avatar reference handling.
</success_criteria>
