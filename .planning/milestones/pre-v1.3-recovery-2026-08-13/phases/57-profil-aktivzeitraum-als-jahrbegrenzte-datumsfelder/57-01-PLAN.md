---
phase: 57-profil-aktivzeitraum-als-jahrbegrenzte-datumsfelder
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/0079_member_profile_activity_dates.up.sql
  - database/migrations/0079_member_profile_activity_dates.down.sql
  - backend/internal/models/member_profile.go
  - backend/internal/handlers/app_profile.go
  - backend/internal/handlers/app_auth_test.go
  - backend/internal/repository/member_profile_repository.go
  - backend/internal/repository/member_profile_repository_test.go
  - shared/contracts/openapi.yaml
autonomous: true
requirements:
  - MEMBER-PROFILE-ACTIVITY-PERIOD-DATE-01
must_haves:
  truths:
    - "D-01: members must get real DATE columns for the own-profile activity period."
    - "D-02: Selected years are normalized to YYYY-01-01; arbitrary month/day values are not accepted as product data."
    - "D-03: Existing active_from_year/active_until_year values are backfilled into date columns without editing historical migrations."
    - "D-04: Backend runtime, OpenAPI, frontend DTOs, and API helpers must describe the same profile activity contract."
    - "D-05: /me/profile remains token-free in UI code and uses the central API client."
    - "No profile story, avatar, membership, release, fansub, anime, or media ownership behavior belongs in this plan."
---

<objective>
Create the backend and contract foundation for date-backed profile activity periods while preserving existing profile data and avoiding destructive migration risk.
</objective>

<context>
@AGENTS.md
@.planning/phases/57-profil-aktivzeitraum-als-jahrbegrenzte-datumsfelder/57-CONTEXT.md
@.planning/phases/57-profil-aktivzeitraum-als-jahrbegrenzte-datumsfelder/57-RESEARCH.md
@docs/engineering/implementation-contract.md
@docs/api/api-contracts.md
@docs/frontend/auth-api-client.md
@database/migrations/0077_member_profiles_mvp.up.sql
@database/migrations/0078_member_profile_story_tiptap.up.sql
@backend/internal/models/member_profile.go
@backend/internal/handlers/app_profile.go
@backend/internal/repository/member_profile_repository.go
@shared/contracts/openapi.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration-chain preflight and date-column migration</name>
  <files>database/migrations/0079_member_profile_activity_dates.up.sql, database/migrations/0079_member_profile_activity_dates.down.sql</files>
  <read_first>
    - database/migrations/0077_member_profiles_mvp.up.sql
    - database/migrations/0078_member_profile_story_tiptap.up.sql
  </read_first>
  <action>
    Run `git status --short database/migrations` before creating migration files. If untracked migration files already exist, stop and report migration-chain risk.

    Add migration `0079_member_profile_activity_dates`:
    - Up: add nullable `members.active_from_date DATE` and `members.active_until_date DATE`.
    - Up: backfill from `active_from_year` and `active_until_year` with `make_date(year, 1, 1)` where the year is valid.
    - Up: add constraints for year-normalized dates and `active_until_date >= active_from_date` when both exist.
    - Down: drop only the constraints and new date columns.

    Do not edit old migrations. Do not drop old integer columns in this plan unless a separate destructive-cleanup decision is made.
  </action>
  <verify>
    <automated>git status --short database/migrations</automated>
    <automated>git diff -- database/migrations/0079_member_profile_activity_dates.up.sql database/migrations/0079_member_profile_activity_dates.down.sql</automated>
  </verify>
  <done>The new reversible migration exists and preserves old year data through date backfill.</done>
</task>

<task type="auto">
  <name>Task 2: Backend DTOs and repository use the date source</name>
  <files>backend/internal/models/member_profile.go, backend/internal/repository/member_profile_repository.go, backend/internal/repository/member_profile_repository_test.go</files>
  <read_first>
    - backend/internal/models/member_profile.go
    - backend/internal/repository/member_profile_repository.go
    - backend/internal/repository/member_profile_repository_test.go
  </read_first>
  <action>
    Add date-backed fields to `MemberProfile` and `MemberProfileUpdateInput`, for example `ActiveFromDate` / `ActiveUntilDate` serialized as `active_from_date` / `active_until_date`.

    Update `MemberProfileRepository` so:
    - profile reads prefer `m.active_from_date` / `m.active_until_date`;
    - old year columns may be converted to date fallback only for rows not yet backfilled;
    - profile updates write date fields as the source of truth;
    - validation rejects non-positive years, arbitrary month/day dates, and `until < from`;
    - `is_currently_active` behavior remains compatible with clearing `active_until_date`.

    Add or update focused repository tests/invariant tests that prove date fields are read/written, old year columns are not the new source, and no parallel profile table is introduced.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/repository</automated>
  </verify>
  <done>Repository reads and writes date-backed activity fields with year-normalized validation.</done>
</task>

<task type="auto">
  <name>Task 3: Handler request parsing and OpenAPI contract alignment</name>
  <files>backend/internal/handlers/app_profile.go, backend/internal/handlers/app_auth_test.go, shared/contracts/openapi.yaml</files>
  <read_first>
    - backend/internal/handlers/app_profile.go
    - backend/internal/handlers/app_auth_test.go
    - shared/contracts/openapi.yaml
    - docs/api/api-contracts.md
  </read_first>
  <action>
    Update `updateOwnProfileRequest` and `UpdateOwnProfile` to accept the new `active_from_date` and `active_until_date` fields. Preserve old year request fields only as an explicitly tested compatibility path if needed during frontend migration; otherwise reject undocumented year fields after frontend moves.

    Update `shared/contracts/openapi.yaml`:
    - `MemberProfile` response fields include `active_from_date` and `active_until_date` with `type: string`, `format: date`, nullable.
    - `UpdateMemberProfileRequest` accepts the same date fields.
    - descriptions state that values are year-limited and normalized to January 1.
    - validation errors include malformed/non-normalized dates and invalid date ranges.

    Update handler tests to cover valid year-normalized dates, invalid arbitrary dates, `until < from`, disabled users, and account fields still read-only.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/handlers ./internal/repository</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Backend and OpenAPI expose the same date-backed profile activity contract.</done>
</task>

</tasks>

<acceptance_criteria>

- New migration adds real `DATE` profile activity columns and backfills existing years.
- Backend uses the date fields as the profile activity source of truth.
- Non-year-normalized dates are rejected or explicitly documented; no hidden month/day semantics enter the product.
- OpenAPI matches backend request/response behavior.
- Existing auth/account/profile ownership boundaries remain unchanged.

</acceptance_criteria>

<recommended_checks>

- `git status --short database/migrations`
- `cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/handlers ./internal/repository`
- `cd C:/Users/admin/Documents/Team4s && git diff --check`

</recommended_checks>
