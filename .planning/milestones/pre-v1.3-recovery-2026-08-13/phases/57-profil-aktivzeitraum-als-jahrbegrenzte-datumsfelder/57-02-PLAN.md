---
phase: 57-profil-aktivzeitraum-als-jahrbegrenzte-datumsfelder
plan: "02"
type: execute
wave: 2
depends_on:
  - "57-01"
files_modified:
  - frontend/src/types/profile.ts
  - frontend/src/lib/api.ts
  - frontend/src/app/me/profile/page.tsx
  - frontend/src/app/me/profile/components/profileFormTypes.ts
  - frontend/src/app/me/profile/components/ProfileBasicsForm.tsx
  - frontend/src/app/me/profile/page.test.tsx
autonomous: true
requirements:
  - MEMBER-PROFILE-ACTIVITY-PERIOD-DATE-01
must_haves:
  truths:
    - "D-02: The visible profile activity controls must be year-limited, not free text."
    - "D-04: Frontend DTOs and update payloads must match the documented backend date contract."
    - "D-05: Protected profile UI must continue using hasAccessToken || hasRefreshToken and central API helpers."
    - "D-06: Any new German visible strings must use proper umlauts."
---

<objective>
Move `/me/profile` from year-number payload fields to the new date contract while keeping the visible controls limited to years.
</objective>

<context>
@AGENTS.md
@.planning/phases/57-profil-aktivzeitraum-als-jahrbegrenzte-datumsfelder/57-CONTEXT.md
@.planning/phases/57-profil-aktivzeitraum-als-jahrbegrenzte-datumsfelder/57-RESEARCH.md
@docs/engineering/implementation-contract.md
@docs/frontend/ui-system.md
@docs/agent-guidelines-ui.md
@docs/api/api-contracts.md
@docs/frontend/auth-api-client.md
@frontend/src/components/ui/Select.tsx
@frontend/src/app/me/profile/page.tsx
@frontend/src/app/me/profile/components/ProfileBasicsForm.tsx
@frontend/src/app/me/profile/page.test.tsx
@frontend/src/types/profile.ts
@frontend/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update frontend DTOs and date/year conversion helpers</name>
  <files>frontend/src/types/profile.ts, frontend/src/app/me/profile/page.tsx, frontend/src/app/me/profile/components/profileFormTypes.ts</files>
  <read_first>
    - frontend/src/types/profile.ts
    - frontend/src/app/me/profile/page.tsx
  </read_first>
  <action>
    Replace profile form state usage of `active_from_year` / `active_until_year` with `active_from_date` / `active_until_date` in the typed contract. Add small local helpers in `page.tsx` or a nearby profile helper file:
    - date string `YYYY-01-01` -> displayed year string
    - displayed year string -> normalized date string `YYYY-01-01`
    - empty year -> `null`

    Keep the helpers profile-local unless another page already needs the same date-year conversion. Do not add API calls or token handling.
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck</automated>
  </verify>
  <done>Frontend profile types and form state speak the new date contract.</done>
</task>

<task type="auto">
  <name>Task 2: Replace free numeric inputs with year-limited semantic controls</name>
  <files>frontend/src/app/me/profile/components/ProfileBasicsForm.tsx</files>
  <read_first>
    - frontend/src/components/ui/Select.tsx
    - frontend/src/components/ui/FormField.tsx
    - frontend/src/app/me/profile/components/ProfileBasicsForm.tsx
    - docs/agent-guidelines-ui.md
  </read_first>
  <action>
    Replace the current numeric/text-like `Input` controls for `Aktiv seit` and `Aktiv bis` with year-limited controls. Prefer the existing global `Select` primitive with a bounded year list and an empty option such as `Keine Angabe`.

    Preserve:
    - `Aktuell aktiv` checkbox behavior.
    - clearing/disabling `Aktiv bis` when currently active.
    - scoped error display.
    - stable layout dimensions.

    New user-facing text must use correct German umlauts, e.g. `Wähle ein Jahr` rather than ASCII replacement.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/app/me/profile/page.test.tsx</automated>
  </verify>
  <done>The profile form no longer offers free activity-year text/number fields.</done>
</task>

<task type="auto">
  <name>Task 3: Update profile tests for date payloads and protected-session behavior</name>
  <files>frontend/src/app/me/profile/page.test.tsx</files>
  <read_first>
    - frontend/src/app/me/profile/page.test.tsx
    - docs/frontend/auth-api-client.md
  </read_first>
  <action>
    Update fixtures from `active_from_year` / `active_until_year` to `active_from_date` / `active_until_date`.

    Add or update assertions that:
    - saving selected years sends `active_from_date: "YYYY-01-01"` and `active_until_date: "YYYY-01-01"`;
    - `is_currently_active=true` sends `active_until_date: null`;
    - arbitrary invalid year selection cannot be submitted;
    - profile still loads when access token is absent but refresh token exists;
    - Keycloak-return dirty-state tests still preserve unsaved activity-period edits.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/app/me/profile/page.test.tsx</automated>
    <automated>cd frontend && npm run typecheck</automated>
  </verify>
  <done>Profile frontend tests prove the new date contract and preserve auth/dirty-state behavior.</done>
</task>

</tasks>

<acceptance_criteria>

- `frontend/src/types/profile.ts` matches the OpenAPI date fields.
- `/me/profile` sends `active_from_date` and `active_until_date`, not undocumented year payloads.
- Activity period controls are year-limited and not free text.
- Existing protected-session and Keycloak-return refresh behavior remains covered.

</acceptance_criteria>

<recommended_checks>

- `cd C:/Users/admin/Documents/Team4s/frontend && npm run test -- --run src/app/me/profile/page.test.tsx`
- `cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck`
- `cd C:/Users/admin/Documents/Team4s && git diff --check`

</recommended_checks>
