---
phase: 109-ranglisten-und-punkteprojektionen
plan: 02
type: execute
wave: 2
depends_on: ["109-01"]
files_modified:
  - database/migrations/0139_member_point_totals.up.sql
  - database/migrations/0139_member_point_totals.down.sql
  - backend/internal/repository/member_point_totals_repository.go
autonomous: true
requirements: [GAM-01, GAM-02, GAM-03, GAM-04, GAM-05]

must_haves:
  truths:
    - "D-05: Die Netto-Gesamtsumme pro Member wird persistent in einer eigenen Tabelle gespeichert, nicht zur Anzeigezeit berechnet."
    - "D-06: Die gespeicherte Summe wird transaktional gemeinsam mit jedem Ledger-Insert fortgeschrieben (AFTER-INSERT-Trigger auf point_ledger_entries), inklusive Senkung bei Reversal-Zeilen; Retries derselben Buchung erhöhen die Summe nicht doppelt."
    - "D-01: Die Rangliste (ListRanking) liefert Member absteigend nach Netto-Punktsumme sortiert mit stabilem member_id-ASC-Tie-Break."
    - "D-02/D-03/D-04 werden respektiert: es entstehen keine neuen Punktbuchungen, keine Gruppen-/Kategorie-/Zeitraum-Schnitte und keine Aufschlüsselung, wofür Punkte vergeben wurden — nur die globale Allzeit-Summe."
    - "point_service.go, point_ledger_repository.go, point_rules_repository.go, review_service.go, alle Review-Repositories und 0131_member_point_foundation.up/down.sql bleiben byte-identisch unverändert; die bestehenden Boundary-Tests TestPointServicePhase106Boundary und TestPhase107ReviewServiceBoundary bleiben grün."
  artifacts:
    - path: "database/migrations/0139_member_point_totals.up.sql"
      provides: "Tabelle member_point_totals, AFTER-INSERT-Fortschreibungs-Trigger, Guard-Trigger gegen Direkt-Writes"
      contains: "CREATE TABLE member_point_totals"
    - path: "database/migrations/0139_member_point_totals.down.sql"
      provides: "Symmetrischer Rückbau von Tabelle, Trigger und Funktionen ohne point_ledger_entries anzufassen"
    - path: "backend/internal/repository/member_point_totals_repository.go"
      provides: "Reines Lese-Repository mit ListRanking(ctx, page) (member, display_name, slug, total_points)"
      exports: ["MemberPointTotalsRepository", "NewMemberPointTotalsRepository", "MemberPointRankingRow"]
  key_links:
    - from: "point_ledger_entries INSERT (award oder reversal)"
      to: "member_point_totals.total_points"
      via: "AFTER INSERT ROW TRIGGER point_ledger_apply_member_total → apply_point_ledger_entry_to_member_total()"
      pattern: "after insert on point_ledger_entries"
    - from: "backend/internal/repository/member_point_totals_repository.go"
      to: "member_point_totals JOIN members"
      via: "ListRanking SELECT ... ORDER BY total_points DESC, member_id ASC"
      pattern: "order by.*total_points desc.*member_id asc"
---

<objective>
GREEN: Implementiere Migration 0139 (Tabelle `member_point_totals`, `AFTER INSERT`-Fortschreibungs-
Trigger auf `point_ledger_entries`, Guard-Trigger gegen Direkt-Writes) und das reine
`MemberPointTotalsRepository` mit `ListRanking`, sodass alle in Plan 109-01 geschriebenen
Wave-0-Tests grün werden — ohne eine der Boundary-geschützten Dateien
(`point_service.go`, `point_ledger_repository.go`, `point_rules_repository.go`, `review_service.go`,
Review-Repositories, `0131_member_point_foundation.up/down.sql`) anzufassen.

Purpose: D-05/D-06 (persistierte, transaktional konsistente Summe) und D-01 (absteigend sortierte
Rangliste) exakt und ausschließlich über neue Dateien erfüllen, wie in 109-RESEARCH.md begründet.
Output: Lauffähige Migration 0139 (up/down) und ein produktionsreifes, reines Lese-Repository.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-ranglisten-und-punkteprojektionen/109-CONTEXT.md
@.planning/phases/109-ranglisten-und-punkteprojektionen/109-RESEARCH.md
@.planning/phases/109-ranglisten-und-punkteprojektionen/109-PATTERNS.md
@backend/internal/migrations/phase109_member_point_totals_test.go
@backend/internal/repository/member_point_totals_repository_test.go
@database/migrations/0131_member_point_foundation.up.sql
@database/migrations/0131_member_point_foundation.down.sql
@backend/internal/repository/member_archive_repository.go
@backend/internal/repository/anime_contributions_public_repository.go
</context>

<interfaces>
<!-- Bereits vorhandene Konstanten/Signaturen, die dieses Repository direkt wiederverwenden MUSS. -->

Aus `backend/internal/repository/anime_contributions_public_repository.go` (gleiches Package
`repository`, nicht neu definieren):
```
const memberSlugExpr = `NULLIF(LOWER(TRIM(BOTH '-' FROM REGEXP_REPLACE(TRIM(%s), '[^a-z0-9]+', '-', 'gi'))), '')`
const memberDisplayExpr = `COALESCE(NULLIF(TRIM(%s.display_name), ''), %s.nickname)`
```

Aus `backend/internal/repository/member_archive_repository.go` (Bounds-Check-Stil, wörtlich
übernehmen):
```go
type MemberArchiveRepository struct { db *pgxpool.Pool }
func NewMemberArchiveRepository(db *pgxpool.Pool) *MemberArchiveRepository { return &MemberArchiveRepository{db: db} }
// Bounds-Check: Seite >= 1 und <= 1000
if page < 1 { page = 1 }
if page > 1000 { page = 1000 }
offset := (page - 1) * archivePageSize
```

Erwartete Test-Signaturen aus `backend/internal/repository/member_point_totals_repository_test.go`
(Plan 109-01, bereits geschrieben — MÜSSEN exakt bedient werden):
```
NewMemberPointTotalsRepository(pool *pgxpool.Pool) *MemberPointTotalsRepository
(*MemberPointTotalsRepository).ListRanking(ctx context.Context, page int) ([]MemberPointRankingRow, int, error)
```

Plan 109-01 enthält außerdem `TestMemberPointRankingRowJSONFieldNames` — ein
`json.Marshal(MemberPointRankingRow{...})`-Assertion-Test, der prüft, dass die serialisierten Keys
snake_case sind (`member_id`, `display_name`, `slug`, `total_points`) und NICHT PascalCase
(`MemberID`, ...). `MemberPointRankingRow` MUSS daher explizite JSON-Struct-Tags tragen (siehe Task 2
unten) — ohne Tags würde `encoding/json` die Go-Feldnamen 1:1 verwenden und diesen Test sowie den
snake_case-Contract aus OpenAPI/Frontend (Plan 109-03) brechen.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0139 — Tabelle, Fortschreibungs-Trigger, Guard-Trigger</name>
  <files>database/migrations/0139_member_point_totals.up.sql, database/migrations/0139_member_point_totals.down.sql</files>
  <read_first>
    backend/internal/migrations/phase109_member_point_totals_test.go (die exakten Contract-Fragmente, die die Migration enthalten bzw. NICHT enthalten darf — verbindlich);
    database/migrations/0131_member_point_foundation.up.sql und .down.sql (Migrationsrahmen BEGIN/COMMIT, plpgsql-Funktions-/Trigger-Stil, Drop-Reihenfolge in der Down-Migration, und insbesondere den Exception-Text-Stil von `guard_point_ledger_mutation` — dort englisch: `'point ledger is append-only'`);
    .planning/phases/109-ranglisten-und-punkteprojektionen/109-RESEARCH.md Abschnitt "Pattern 1" und "Pattern 2" (SQL-Wortlaut für Tabelle, Fortschreibungs-Funktion/-Trigger und Guard-Funktion/-Trigger; der Exception-Text in Pattern 2 ist dort auf Englisch korrigiert — wörtlich übernehmen, nicht neu übersetzen).
  </read_first>
  <action>
    Prüfe unmittelbar vor dem Anlegen der Datei per Verzeichnislisting von `database/migrations`
    erneut, dass `0139` noch die nächste freie Nummer ist (Pitfall 4, parallele GSD-Läufer auf
    `main`); falls belegt, verwende konsistent die nächste freie Nummer und passe den bereits in
    Plan 109-01 geschriebenen Testdatei-Konstanten (`phase109MigrationName` etc.) entsprechend an,
    ohne deren Prüflogik zu ändern. Schreibe `0139_member_point_totals.up.sql` innerhalb von
    `BEGIN;`/`COMMIT;` mit exakt dieser Reihenfolge: (1) `CREATE TABLE member_point_totals` mit
    Spalten `member_id BIGINT PRIMARY KEY REFERENCES members(id)` (ohne `ON DELETE CASCADE`, analog
    0131), `total_points BIGINT NOT NULL DEFAULT 0`, `updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`;
    (2) `CREATE FUNCTION apply_point_ledger_entry_to_member_total() RETURNS trigger LANGUAGE plpgsql`
    mit `INSERT INTO member_point_totals (member_id, total_points, updated_at) VALUES (NEW.member_id,
    NEW.point_value, NOW()) ON CONFLICT (member_id) DO UPDATE SET total_points =
    member_point_totals.total_points + NEW.point_value, updated_at = NOW(); RETURN NULL;` (kein
    `INSERT INTO point_ledger_entries` in dieser Funktion — sie liest nur `NEW`, schreibt nur in die
    eigene Tabelle); (3) `CREATE TRIGGER point_ledger_apply_member_total AFTER INSERT ON
    point_ledger_entries FOR EACH ROW EXECUTE FUNCTION apply_point_ledger_entry_to_member_total()`;
    (4) `CREATE FUNCTION guard_member_point_totals_mutation() RETURNS trigger LANGUAGE plpgsql` mit
    `IF pg_trigger_depth() = 0 THEN RAISE EXCEPTION 'member_point_totals is maintained exclusively by
    the point_ledger_entries trigger'; END IF; RETURN COALESCE(NEW, OLD);` — der Exception-Text ist
    zwingend Englisch (analog `guard_point_ledger_mutation`-Text `'point ledger is append-only'` aus
    0131; ein deutscher Text mit ASCII-Ersetzung wie "ausschliesslich" statt "ausschließlich" verstößt
    gegen die CLAUDE.md-Sprachregel für user-facing/DB-Strings — Englisch löst das sauber auf, ohne
    Umlaut-Frage) (analog `guard_point_ledger_mutation`-Stil aus 0131, aber ohne dessen komplexes
    Feld-für-Feld-NULL-Nesting, da `member_point_totals` keine FK-ON-DELETE-SET-NULL-Spalten hat);
    (5) `CREATE TRIGGER member_point_totals_guard_direct_write BEFORE INSERT OR UPDATE OR DELETE ON
    member_point_totals FOR EACH ROW EXECUTE FUNCTION guard_member_point_totals_mutation()`. Schreibe
    `0139_member_point_totals.down.sql` innerhalb von `BEGIN;`/`COMMIT;` in exakt dieser Reihenfolge:
    `DROP TRIGGER IF EXISTS member_point_totals_guard_direct_write ON member_point_totals`, `DROP
    TRIGGER IF EXISTS point_ledger_apply_member_total ON point_ledger_entries` (nur den eigenen
    Trigger auf der fremden Tabelle entfernen — `point_ledger_entries` selbst NICHT droppen, das
    gehört Migration 0131), `DROP FUNCTION IF EXISTS guard_member_point_totals_mutation()`, `DROP
    FUNCTION IF EXISTS apply_point_ledger_entry_to_member_total()`, `DROP TABLE IF EXISTS
    member_point_totals`. Berühre in diesem Task keine andere Datei, insbesondere nicht
    `0131_member_point_foundation.up/down.sql`, `point_service.go`, `point_ledger_repository.go` oder
    `point_rules_repository.go`.
  </action>
  <acceptance_criteria>
    go test ./backend/internal/migrations/... -run TestPhase109Migration -v ist grün (Up-Contract,
    Down-Contract, Live-Up-Down-Up). go test ./backend/internal/services/... -run
    'TestPointServicePhase106Boundary|TestPhase107ReviewServiceBoundary' -v bleibt grün (0131 und die
    Service-Dateien wurden nicht verändert). git diff --stat zeigt für diesen Task ausschließlich die
    beiden neuen 0139-Dateien. Der Guard-Trigger-Exception-Text in
    `0139_member_point_totals.up.sql` ist Englisch (kein deutscher Text, keine ASCII-Ersetzung wie
    "ausschliesslich").
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/migrations/... -run TestPhase109Migration -v && go test ./internal/services/... -run "TestPointServicePhase106Boundary|TestPhase107ReviewServiceBoundary" -v</automated>
  </verify>
  <done>Migration 0139 existiert, alle Wave-0-Migrationstests aus Plan 109-01 sind grün, keine Boundary-geschützte Datei wurde verändert, Guard-Exception-Text ist Englisch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MemberPointTotalsRepository mit ListRanking</name>
  <files>backend/internal/repository/member_point_totals_repository.go</files>
  <behavior>
    - ListRanking(ctx, page) liefert Members mit mindestens einer Buchung (INNER JOIN member_point_totals→members), gefiltert auf members.profile_visibility='public', sortiert total_points DESC, member_id ASC.
    - Seiten-Bounds: page<1 wird zu 1, page>1000 wird zu 1000, feste Seitengröße als Package-Konstante (analog archivePageSize).
    - DisplayName/Slug werden über die bestehenden Konstanten memberDisplayExpr/memberSlugExpr abgeleitet, nicht neu implementiert.
    - Rückgabe ist [] (leerer Slice, kein nil) bei keinen Treffern; total spiegelt COUNT(*) unter demselben profile_visibility-Filter.
    - MemberPointRankingRow serialisiert über json.Marshal zu snake_case-Keys (member_id, display_name, slug, total_points), nicht PascalCase — verifiziert durch TestMemberPointRankingRowJSONFieldNames aus Plan 109-01.
  </behavior>
  <read_first>
    backend/internal/repository/member_point_totals_repository_test.go (Plan 109-01 — definiert die exakt zu erfüllenden Signaturen und Assertions, inklusive TestMemberPointRankingRowJSONFieldNames);
    backend/internal/repository/member_archive_repository.go (Bounds-Check-, Konstruktor- und Fehlerformat-Konvention `fmt.Errorf("<bereich>: <schritt>: %w", err)`, Query-Aufbau mit pgx-Parametern für LIMIT/OFFSET; siehe auch die JSON-Struct-Tags auf `ArchiveMemberRow` als Namensgebungs-Vorbild);
    backend/internal/repository/anime_contributions_public_repository.go (memberDisplayExpr/memberSlugExpr — direkt importieren, nicht duplizieren; Struct-Tag-Stil `json:"member_display_name"` etc. als Vorbild für snake_case-Tags).
  </read_first>
  <action>
    Lege `backend/internal/repository/member_point_totals_repository.go` in Package `repository` an.
    Definiere `MemberPointRankingRow` mit Feldern und expliziten, snake_case JSON-Struct-Tags (Pflicht,
    analog `ArchiveMemberRow` in `member_archive_repository.go` und den Struct-Tags in
    `anime_contributions_public_repository.go`):
    `MemberID int64 \`json:"member_id"\``, `DisplayName string \`json:"display_name"\``,
    `Slug *string \`json:"slug"\``, `TotalPoints int64 \`json:"total_points"\``. Die Tags sind
    zwingend, weil der Handler in Plan 109-03 diese Rows direkt über `c.JSON` serialisiert — ohne Tags
    würde `encoding/json` PascalCase-Feldnamen erzeugen und den snake_case-Contract aus OpenAPI und
    Frontend (Plan 109-03) brechen. Definiere `MemberPointTotalsRepository struct { db *pgxpool.Pool
    }` mit Konstruktor `NewMemberPointTotalsRepository(db *pgxpool.Pool) *MemberPointTotalsRepository`.
    Definiere Package-Konstante `memberRankingPageSize = 50`. Implementiere `func (r
    *MemberPointTotalsRepository) ListRanking(ctx context.Context, page int) ([]MemberPointRankingRow,
    int, error)`: klemme `page` exakt wie in `member_archive_repository.go` (page<1→1, page>1000→1000),
    berechne `offset := (page - 1) * memberRankingPageSize`. Führe zuerst eine COUNT-Query gegen
    `member_point_totals mpt JOIN members m ON m.id = mpt.member_id WHERE m.profile_visibility =
    'public'` aus (Sicherheitsgrenze T-109-02: keine nicht-öffentlichen Members in Total oder Liste).
    Führe danach die Haupt-Query aus: `SELECT m.id, <memberDisplayExpr>, <memberSlugExpr>,
    mpt.total_points FROM member_point_totals mpt JOIN members m ON m.id = mpt.member_id WHERE
    m.profile_visibility = 'public' ORDER BY mpt.total_points DESC, m.id ASC LIMIT $1 OFFSET $2` mit
    `memberRankingPageSize`/`offset` als pgx-Parameter (niemals String-Interpolation für LIMIT/OFFSET,
    T-109-01). Nutze `fmt.Sprintf(memberDisplayExpr, "m", "m")` und `fmt.Sprintf(memberSlugExpr,
    "m.nickname")` exakt wie in `member_archive_repository.go` Zeilen 180–181. Scanne in
    `[]MemberPointRankingRow` (initialisiert als leerer Slice, nie nil), umschließe jeden Fehler mit
    `fmt.Errorf("member point ranking: <schritt>: %w", err)`. Gib `(rows, total, nil)` zurück. Führe
    keine Schreibmethode in dieser Datei ein — das Repository ist rein lesend (die Summe wird
    ausschließlich vom Migrations-Trigger aus Task 1 fortgeschrieben, D-05/D-06).
  </action>
  <acceptance_criteria>
    go test ./backend/internal/repository/... -run TestMemberPointTotals -v ist vollständig grün
    (Concurrency, Retry-Idempotenz, Reversal, Ranking-Reihenfolge/Tie-Break, Page-Bounds). go test
    ./backend/internal/repository/... -run TestMemberPointRankingRowJSONFieldNames -v ist ebenfalls
    grün und bestätigt per json.Marshal-Assertion, dass die Response-Keys snake_case sind (member_id,
    display_name, slug, total_points) und NICHT PascalCase (MemberID, DisplayName, TotalPoints) —
    schließt die Verifikationslücke, dass Struct-Tags tatsächlich vorhanden sind, nicht nur die
    Go-Feldnamen selbst. Die Datei enthält keine INSERT/UPDATE/DELETE-Anweisung gegen
    member_point_totals oder point_ledger_entries.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/repository/... -run TestMemberPointTotals -v && go test ./internal/repository/... -run TestMemberPointRankingRowJSONFieldNames -v</automated>
  </verify>
  <done>Alle Wave-0-Repository-Tests aus Plan 109-01 sind grün (inklusive der JSON-Feldnamen-Assertion); ListRanking liefert korrekt sortierte, seitenweise, öffentlichkeitsgefilterte Ergebnisse mit snake_case JSON-Serialisierung.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| point_ledger_entries INSERT → member_point_totals Trigger | DB-interne Boundary, kein externer Input, aber architektonisch kritisch für D-06 (muss in derselben Transaktion feuern) |
| MemberPointTotalsRepository → PostgreSQL | Nur parametrisierte Queries, kein dynamisches SQL aus Nutzereingabe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-01 | Tampering | ListRanking `page`-Parameter | mitigate | Bounds-Check page<1→1, page>1000→1000 direkt im Repository; LIMIT/OFFSET ausschließlich als pgx-Parameter ($1,$2), keine String-Interpolation |
| T-109-02 | Information Disclosure | Ranking-Query gegen `members` | mitigate | Query filtert zwingend auf `m.profile_visibility = 'public'`; Rückgabe enthält nur member_id, display_name, slug, total_points — kein actor_app_user_id, keine internen Ledger-Felder |
| T-109-03 | Denial of Service | Ranking-Query | mitigate (strukturell) | Kein Runtime-`SUM` — `SELECT` gegen die per Trigger materialisierte, PK-indizierte Tabelle `member_point_totals` |
| T-109-04 | Tampering | Direkter Schreibzugriff auf member_point_totals außerhalb des Ledger-Triggers | mitigate | Guard-Trigger `member_point_totals_guard_direct_write` lehnt jede Mutation mit `pg_trigger_depth() = 0` ab |
| T-109-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Paketabhängigkeiten (nur SQL-Migration + bestehende pgx/pgxpool) |
</threat_model>

<verification>
`cd backend && go test ./internal/migrations/... ./internal/repository/... -run TestPhase109 -v`
sowie `go test ./internal/repository/... -run TestMemberPointTotals -v` müssen grün sein. Zusätzlich
`go test ./internal/repository/... -run TestMemberPointRankingRowJSONFieldNames -v` (snake_case
JSON-Contract-Assertion) und `go test ./internal/services/... -run
'TestPointServicePhase106Boundary|TestPhase107ReviewServiceBoundary' -v` als Regressionsschutz. Vor
Abschluss der Wave: `cd backend && go test ./... -v` vollständig grün.
</verification>

<success_criteria>
Migration 0139 existiert, ist idempotent auf/ab-migrierbar und schreibt die Summe transaktional mit
jedem Ledger-Insert fort (D-05/D-06), mit einem englischsprachigen Guard-Exception-Text. Der
Guard-Trigger nutzt einen Englisch-Text ohne Umlaut-/ASCII-Ersetzungsproblem.
MemberPointTotalsRepository liefert eine korrekt sortierte, seitenweise, öffentlichkeitsgefilterte
Rangliste (D-01) mit snake_case-JSON-Struct-Tags, verifiziert durch eine explizite
Serialisierungs-Assertion. Keine Boundary-geschützte Datei wurde verändert.
</success_criteria>

<output>
Create `.planning/phases/109-ranglisten-und-punkteprojektionen/109-02-SUMMARY.md` when done
</output>
</output>
