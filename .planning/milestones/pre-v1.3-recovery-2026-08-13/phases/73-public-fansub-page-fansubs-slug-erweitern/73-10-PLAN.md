---
phase: 73-public-fansub-page-fansubs-slug-erweitern
plan: "10"
type: execute
wave: 2
depends_on: ["07"]
gap_closure: true
files_modified:
  - frontend/src/app/fansubs/[slug]/page.tsx
  - frontend/src/components/fansubs/FansubHeroSection.tsx
  - frontend/src/components/fansubs/FansubContributorsSection.tsx
autonomous: true
requirements: [D-07]
must_haves:
  truths:
    - "Eine Gruppe mit group_type='collaboration' zeigt keine normale Profil-Seite — sie zeigt stattdessen eine Kollaboration-Darstellung mit Verweis auf die beteiligten Gruppen"
    - "Angeldust (ehemalig + extern) erhält in der Mitwirkenden-Sektion einen visuellen Hinweis wenn dieselbe Person auch im Team-Block erscheint"
  artifacts:
    - path: "frontend/src/app/fansubs/[slug]/page.tsx"
      provides: "Früher Typ-Check: group_type=collaboration rendert CollaborationView statt normalem Profil; vier nachgelagerte API-Aufrufe feuern nicht"
    - path: "frontend/src/components/fansubs/FansubHeroSection.tsx"
      provides: "collaboration_members in Props; rendert Gruppenlinks wenn group_type=collaboration"
    - path: "frontend/src/components/fansubs/FansubContributorsSection.tsx"
      provides: "Querverweis-Badge wenn Mitwirkender auch im Team erscheint"
  key_links:
    - from: "page.tsx"
      to: "FansubHeroSection"
      via: "group.group_type === 'collaboration' early-return"
    - from: "FansubContributorsSection"
      to: "DomainProjectionMemberRow[]"
      via: "members-Prop für Duplikat-Erkennung"
---

<objective>
Behebt zwei UAT-Findings:

UAT-16 (Kollaboration als Gruppe): "AnimeOwnage & Project Messiah" hat group_type='collaboration' und wird als vollwertige Gruppenprofilseite gerendert — leeres Team, leere Projekte, generischer Hero. FansubGroup.group_type und FansubGroup.collaboration_members sind bereits im Typ vorhanden. Fix: In page.tsx wird nach dem Laden geprüft ob group_type === 'collaboration'. Wenn ja, wird VOR den vier nachgelagerten API-Aufrufen ein early return gerendert. Normale Profil-Sektionen (Team, Projekte, Geschichte etc.) werden nicht gerendert.

UAT-5 (Angeldust-Duplikat): Angeldust erscheint als "Ehemalige Mitglieder" (DomainProjection.members) UND als "Externe Mitwirkende" (DomainProjection.contributors). Kein Querverweis. Fix: FansubContributorsSection erhält die members-Liste als optionalen Prop und zeigt bei Überschneidung (gleicher member_display_name) eine Badge "auch Mitglied" als Querverweis-Hinweis.

Wave 2 wegen page.tsx-Abhängigkeit: 73-07 (wave 1) ändert page.tsx (section id="gruppenleitung"); 73-10 ändert page.tsx ebenfalls (Kollaboration-Early-Return + teamMemberNames-Prop). Sequenzielle Ausführung verhindert Überschreibung.

Output: page.tsx mit Kollaboration-Early-Path; FansubHeroSection mit Kollaborations-Anzeige; FansubContributorsSection mit Duplikat-Querverweis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@frontend/src/app/fansubs/[slug]/page.tsx
@frontend/src/components/fansubs/FansubHeroSection.tsx
@frontend/src/components/fansubs/FansubContributorsSection.tsx
@frontend/src/types/fansub.ts
@frontend/src/types/domain-projection.ts

<!-- Lese auch die SUMMARY von 73-07 sobald verfügbar, um die aktuellen page.tsx-Zeilen zu kennen: -->
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-07-SUMMARY.md

<interfaces>
<!-- FansubGroup relevante Felder für diesen Plan: -->
export type FansubGroupType = "group" | "collaboration";

export interface FansubGroup {
  group_type: FansubGroupType;
  collaboration_members?: FansubGroupSummary[];  // beteiligten Gruppen bei group_type=collaboration
  // ... weitere Felder
}

export interface FansubGroupSummary {
  id: number;
  slug: string;
  name: string;
  logo_url?: string | null;
}

<!-- DomainProjectionMemberRow aus domain-projection.ts (für Querverweis): -->
<!-- Felder die für member_display_name-Matching relevant sind: -->
interface DomainProjectionMemberRow {
  member_display_name: string;
  // ... weitere Felder
}

<!-- FansubContributorsSection aktueller Props: -->
interface FansubContributorsSectionProps {
  contributors: DomainProjectionContributorRow[]
}

<!-- DomainProjectionContributorRow relevantes Feld: -->
interface DomainProjectionContributorRow {
  member_display_name: string;
  // ...
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: page.tsx + FansubHeroSection — Kollaboration-Darstellung</name>
  <files>frontend/src/app/fansubs/[slug]/page.tsx, frontend/src/components/fansubs/FansubHeroSection.tsx</files>
  <action>
ÄNDERUNG 1: page.tsx — Kollaboration-Zweig mit exaktem Einfügepunkt

Lies die aktuelle page.tsx nach dem 73-07-Merge. Suche die Zeile `const group = groupResponse.data`. Füge den Kollaboration-Check unmittelbar NACH dieser Zeile und unmittelbar VOR dem `const [...] = await Promise.allSettled([...])` Block ein, sodass die vier nachgelagerten API-Aufrufe (loadFansubProjects, getFansubContributions, getFansubGroupDomainProjection, getMediaOwnershipProjection) für Kollaborations-Gruppen nicht feuern:

  const group = groupResponse.data   // ← existierende Zeile

  // Kollaboration-Check — VOR allen nachgelagerten API-Aufrufen einfügen:
  if (group.group_type === 'collaboration') {
    return (
      <main className={styles.page}>
        <div className={styles.readingColumn}>
          <FansubHeroSection group={group} isCollaboration />
        </div>
      </main>
    )
  }

  const [...] = await Promise.allSettled([...])   // ← existierende Zeile (unverändert)

Dadurch laufen loadFansubProjects, getFansubContributions, getFansubGroupDomainProjection und getMediaOwnershipProjection bei Kollaborationen nicht.

ÄNDERUNG 2: FansubHeroSection — isCollaboration-Prop

Füge optionalen Prop `isCollaboration?: boolean` zu FansubHeroSectionProps hinzu.

Wenn isCollaboration === true:
- Zeige unter dem bestehenden Hero-Inhalt (Logo, Titel, Status, Fakten) einen Hinweis-Block:
  - Überschrift (p oder small): "Dies ist eine Kollaboration zwischen:"
  - Liste der beteiligten Gruppen aus group.collaboration_members (falls vorhanden):
    - Jede Gruppe als Link auf /fansubs/[slug]: `<Link href={'/fansubs/' + member.slug}>{member.name}</Link>`
    - Wenn collaboration_members leer oder undefined: "Keine Gruppenangaben hinterlegt."
  - Stil: dezenter Info-Kasten (Card variant="flat" oder inline-Styling), setzt sich optisch vom normalen Hero-Content ab.
- Wenn isCollaboration nicht gesetzt (Standardfall), keine Änderung am bisherigen Render.

Beachte: group.collaboration_members kann undefined sein — immer mit `group.collaboration_members ?? []` absichern.

Importiere Link aus 'next/link' in FansubHeroSection.tsx wenn noch nicht vorhanden.
Keine Änderungen an page.module.css nötig (existierende .heroContent/.heroCopy-Klassen reichen).

Dateigrößen: FansubHeroSection.tsx ist aktuell ca. 103 Zeilen. Mit dem Kollaboration-Block bleibt sie weit unter 450 Zeilen.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>page.tsx: group_type=collaboration führt zu early return VOR Promise.allSettled mit vereinfachtem Layout; FansubHeroSection.tsx: isCollaboration-Prop zeigt collaboration_members-Links; tsc fehlerfrei</done>
</task>

<task type="auto">
  <name>Task 2: FansubContributorsSection — Querverweis-Badge für Doppelmitglieder</name>
  <files>frontend/src/components/fansubs/FansubContributorsSection.tsx</files>
  <action>
Bearbeite FansubContributorsSection.tsx.

Füge optionalen Prop `teamMemberNames?: string[]` zu FansubContributorsSectionProps hinzu. Dieser enthält die member_display_name-Werte aller Team-Mitglieder (aktiv + ehemalig) für die Querverweis-Erkennung.

Wenn teamMemberNames befüllt ist und ein Contributor auch in teamMemberNames vorkommt (Vergleich über member_display_name, case-insensitiv), zeige hinter dem Namen einen kleinen Badge oder kursiven Hinweis: "auch Mitglied".

Implementierung:
1. Berechne `const teamSet = new Set((teamMemberNames ?? []).map(n => n.toLowerCase()))`.
2. Für jeden visibleContributor: `const isAlsoMember = teamSet.has(contributor.member_display_name.toLowerCase())`.
3. Wenn isAlsoMember: Zeige nach dem Namen eine `<Badge variant="neutral">auch Mitglied</Badge>` aus @/components/ui (oder wenn Badge kein 'neutral' hat: nutze variant="muted" oder inline-style mit gedämpfter Farbe — prüfe welche Badge-Varianten verfügbar sind).

In page.tsx: Übergib teamMemberNames an FansubContributorsSection. Berechne den Namen-Set aus domainProjection.members:
`const teamMemberNames = domainProjection.members.map(m => m.member_display_name)`

Füge das `teamMemberNames`-Prop in der JSX-Instanz von `<FansubContributorsSection>` in page.tsx hinzu:
`<FansubContributorsSection contributors={domainProjection.contributors} teamMemberNames={teamMemberNames} />`

Hinweis für Executor: Importiere Badge aus @/components/ui in FansubContributorsSection.tsx wenn noch nicht vorhanden. Prüfe welche Badge-Varianten in @/components/ui/Badge vorhanden sind (neutral, muted, info etc.) und wähle die passendste gedämpfte Variante.

Dateigrößen: FansubContributorsSection.tsx ist aktuell ca. 47 Zeilen. page.tsx nach 73-07 ca. 141 Zeilen. Beide weit unter 450 Zeilen.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>FansubContributorsSection.tsx: teamMemberNames-Prop, Querverweis-Badge bei Überschneidung; page.tsx: teamMemberNames aus domainProjection.members; tsc fehlerfrei</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| API → Frontend | collaboration_members aus Backend-Response; Links auf /fansubs/[slug] |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-10-01 | Information Disclosure | Kollaboration-Early-Return | accept | Spart überflüssige API-Aufrufe bei Kollaborationen; keine sensitiven Daten exponiert |
| T-73-10-02 | Elevation of Privilege | member_display_name-Matching | accept | String-Vergleich nur für UI-Badge, keine Berechtigungsentscheidungen |
</threat_model>

<verification>
1. /fansubs/animeownage-project-messiah laden (Kollaboration)
2. Seite zeigt Kollaborationshinweis "Dies ist eine Kollaboration zwischen:" mit Links auf /fansubs/animeownage und /fansubs/project-messiah (oder ähnliche Slugs)
3. Keine leeren Team/Projekte/Geschichte-Sektionen auf der Kollaborations-Seite
4. /fansubs/animeownage laden (normale Gruppe)
5. Mitwirkende-Sektion: Angeldust zeigt Badge "auch Mitglied" neben dem Eintrag
6. Normale Seite unverändert (keine Kollaborations-Anzeige bei group_type=group)
7. tsc --noEmit: 0 Fehler
</verification>

<success_criteria>
- /fansubs/[kollaboration-slug]: Zeigt Kollaborations-Hinweis mit beteiligten Gruppen-Links, kein normales Seiten-Layout
- /fansubs/animeownage: Angeldust hat "auch Mitglied"-Badge in der Mitwirkenden-Sektion
- Reguläre Gruppen: keine Regressions in Hero/Team/Projekten
- tsc --noEmit: 0 Fehler
</success_criteria>

<output>
Erstelle `.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-10-SUMMARY.md` nach Abschluss.
</output>
