---
phase: 73-public-fansub-page-fansubs-slug-erweitern
plan: "09"
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - frontend/src/components/fansubs/FansubStorySection.tsx
  - frontend/src/components/fansubs/FansubMediaSection.tsx
  - frontend/src/components/fansubs/FansubGroupMediaBlock.tsx
  - frontend/src/lib/fansub-summary.ts
autonomous: true
requirements: [D-02, D-11, D-15]
must_haves:
  truths:
    - "Geschichte-Sektion zeigt einen sinnvollen Empty-State wenn kein Story-Text vorhanden — nicht die Hero-Metazeile"
    - "Geschichte-Sektion Empty-State-Subtitle zeigt die buildFansubFactSummary-Metazeile als informative Kurzinfo statt sie als Hauptinhalt zu missbrauchen"
    - "Medien-Sektion zeigt 'Gruppenmedien'-Bereich nur wenn dort nicht-redundante Inhalte existieren ODER zeigt einen klaren Empty-State ohne Logo+Banner zu wiederholen"
  artifacts:
    - path: "frontend/src/components/fansubs/FansubStorySection.tsx"
      provides: "Story-Sektion zeigt Empty-State mit buildFansubFactSummary als Subtitle statt als Hauptinhalt"
    - path: "frontend/src/lib/fansub-summary.ts"
      provides: "buildFansubFactSummary wird nicht mehr als Story-Hauptinhalt missbraucht — nur als EmptyState-Subtitle"
    - path: "frontend/src/components/fansubs/FansubGroupMediaBlock.tsx"
      provides: "Fallback-Zweig (Logo/Banner) entfernt; bei publicGroupMedia.length === 0 sofort EmptyState"
  key_links:
    - from: "FansubStorySection"
      to: "buildFansubFactSummary"
      via: "EmptyState description-Prop (nicht als CollapsibleStory-Inhalt)"
    - from: "FansubGroupMediaBlock"
      to: "FansubMediaSection"
      via: "Entscheidung: Banner/Logo-Fallback entfernt aus Gruppenmedien-Block"
---

<objective>
Behebt zwei Minor-Findings der UAT:

UAT-8 (Geschichte-Redundanz): FansubStorySection nutzt buildFansubFactSummary als Story-Inhalt und zeigt damit "1999 bis 2022 • Deutschland • aktiv" — identisch mit der Hero-Metazeile. Das ist kein Story-Text. Fix: FansubStorySection zeigt keinen CollapsibleStory mehr (kein echtes description-Feld im Typ). Stattdessen zeigt sie den EmptyState mit buildFansubFactSummary als informativem Subtitle — so bleibt die Sektion nicht leer und die Fakten sind trotzdem auffindbar, aber es wird nicht mehr der Eindruck erweckt, es handle sich um einen echten Story-Text.

UAT-6 (Medien-Redundanz): Gruppenmedien-Block zeigt exakt dasselbe Logo + Banner wie der Hero, wenn keine anderen Medien vorhanden sind. Fix: Der Logo/Banner-Fallback in FansubGroupMediaBlock wird entfernt. Wenn keine freigegebenen Gruppenmedien vorhanden, zeigt der Block den bestehenden EmptyState ("Noch keine Medien hinterlegt"). Dieser Plan ist alleiniger Eigentümer von FansubGroupMediaBlock.tsx.

Output: FansubStorySection mit EmptyState-Subtitle statt Hero-Redundanz; FansubGroupMediaBlock ohne Logo/Banner-Fallback-Zweig.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@frontend/src/components/fansubs/FansubStorySection.tsx
@frontend/src/components/fansubs/FansubGroupMediaBlock.tsx
@frontend/src/components/fansubs/FansubMediaSection.tsx
@frontend/src/lib/fansub-summary.ts
@frontend/src/types/fansub.ts

<interfaces>
<!-- FansubGroup aus fansub.ts hat KEIN 'description'- oder 'story'-Feld. -->
<!-- Die einzigen textlichen Felder im Typ sind: name, slug, country, website_url, discord_url, irc_url -->
<!-- buildFansubFactSummary baut eine Metazeile aus founded_year/dissolved_year/country/status -->

<!-- DESIGN-ENTSCHEIDUNG: Da FansubGroup keinen story_text/description-Feld hat, -->
<!-- kann FansubStorySection keinen echten Story-Text zeigen. -->
<!-- Fix: FansubStorySection zeigt den EmptyState mit buildFansubFactSummary als Subtitle. -->
<!-- Beispiel: title="Noch keine Geschichte hinterlegt", description="1999 bis 2022 • Deutschland • aktiv" -->
<!-- CollapsibleStory wird nur angezeigt wenn ein zukünftiges description-Feld befüllt ist. -->
<!-- Da description aktuell nicht existiert, zeigt die Sektion immer den Empty-State. -->
<!-- buildFansubFactSummary bleibt importiert (als Subtitle-Wert), CollapsibleStory-Import entfernt. -->

<!-- FansubGroupMediaBlock: Aktueller Fallback-Zweig: -->
if (publicGroupMedia.length === 0 && (group.logo_url || group.banner_url)) {
  return <Image src={group.logo_url} ... /> + <Image src={group.banner_url} ... />
}
<!-- Dieser Zweig soll entfernt werden — führt direkt zum EmptyState. -->

<!-- HINWEIS: 73-06 berührt FansubGroupMediaBlock.tsx NICHT. -->
<!-- Dieser Plan (73-09) ist alleiniger Eigentümer von FansubGroupMediaBlock.tsx. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FansubStorySection — buildFansubFactSummary als EmptyState-Subtitle statt Story-Inhalt</name>
  <files>frontend/src/components/fansubs/FansubStorySection.tsx, frontend/src/lib/fansub-summary.ts</files>
  <action>
Bearbeite FansubStorySection.tsx:

Aktueller Stand: `const storyContent = buildFansubFactSummary(group)` wird als Inhalt für CollapsibleStory genutzt. Diese Metazeile ist identisch mit dem Hero und kein Story-Text.

Neuer Stand: Da FansubGroup kein story_text/description-Feld hat, zeigt die Sektion den EmptyState. Entferne den CollapsibleStory-Render-Zweig. Behalte den buildFansubFactSummary-Aufruf, aber nutze ihn als description-Prop des EmptyState:

  const factSummary = buildFansubFactSummary(group)

  return (
    <section id="geschichte" className={...}>
      <SectionHeader title="Geschichte" />
      <EmptyState
        title="Noch keine Geschichte hinterlegt"
        description={factSummary || "Die Gruppe hat bisher noch keine Beschreibung veröffentlicht."}
      />
    </section>
  )

Damit bleibt die buildFansubFactSummary-Metazeile ("1999 bis 2022 • Deutschland • aktiv") als informativer Subtitle sichtbar, wird aber nicht mehr als Story-Hauptinhalt missbraucht. Die Sektion ist klar als Empty-State erkennbar.

Entferne den Import von CollapsibleStory aus dieser Datei (nicht mehr genutzt).
Behalte den Import von buildFansubFactSummary aus @/lib/fansub-summary (weiterhin genutzt als Subtitle).
Behalte den Import von EmptyState und SectionHeader aus @/components/ui.
Behalte section id="geschichte".

Hinweis: buildFansubFactSummary.ts selbst NICHT ändern — die Funktion wird anderswo genutzt (z.B. FansubHeroSection). Nur der CollapsibleStory-Render-Zweig in FansubStorySection.tsx wird entfernt.

Bearbeite FansubMediaSection.tsx NICHT in diesem Task (separater Task 2 handhabt FansubGroupMediaBlock).
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>FansubStorySection.tsx: kein CollapsibleStory-Import mehr, kein CollapsibleStory-Render, zeigt EmptyState mit buildFansubFactSummary als description-Wert; tsc fehlerfrei</done>
</task>

<task type="auto">
  <name>Task 2: FansubGroupMediaBlock — Logo/Banner-Fallback entfernen</name>
  <files>frontend/src/components/fansubs/FansubGroupMediaBlock.tsx</files>
  <action>
Bearbeite FansubGroupMediaBlock.tsx. Dieser Plan ist alleiniger Eigentümer dieser Datei — 73-06 berührt sie nicht.

Entferne den mittleren Fallback-Zweig der Funktion, der Logo und Banner rendert wenn keine freigegebenen Gruppenmedien vorhanden sind:

Zu entfernender Code (ca. Zeilen 37–50):
  if (publicGroupMedia.length === 0 && (group.logo_url || group.banner_url)) {
    return (
      <div style={{ display: 'grid', gap: 12 }}>
        {group.logo_url ? ( <Image src={group.logo_url} ... /> ) : null}
        {group.banner_url ? ( <Image src={group.banner_url} ... /> ) : null}
      </div>
    )
  }

Nach Entfernung fällt der Code direkt auf den bestehenden EmptyState durch wenn publicGroupMedia.length === 0. Der EmptyState bleibt unverändert:
  <EmptyState variant="compact" title="Noch keine Medien hinterlegt" description="..." />

Prüfe nach Entfernung:
- Werden Image und group.logo_url/group.banner_url noch anderswo in der Datei verwendet?
- Wenn nicht mehr: Entferne den Image-Import und logo_url/banner_url aus dem Props-Pick.
- Aktuell ist Props-Typ: Pick<FansubGroup, 'id' | 'logo_url' | 'banner_url'>
- Nach Entfernung: Pick<FansubGroup, 'id'> (logo_url/banner_url nicht mehr nötig)

Wenn die Datei noch andere Image-Verwendungen für echte Medien-Rows hat (mediaRows-Rendering), belasse diese unverändert und prüfe ob dort object-fit korrekt gesetzt ist. Wenn eine mediaRow ein Banner-Bild rendert ohne explizites object-fit:contain, ergänze style={{ objectFit: 'contain', maxWidth: '100%' }} auf diesem Image.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>FansubGroupMediaBlock.tsx: Fallback-Zweig mit Logo/Banner entfernt; bei publicGroupMedia.length === 0 zeigt Block EmptyState; Props-Typ enthält keine ungenutzten logo_url/banner_url mehr; tsc fehlerfrei</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| keine neuen | Reine UI-Vereinfachung, kein neuer API-Aufruf, keine Auth-Grenze |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-09-01 | Information Disclosure | FansubGroupMediaBlock | accept | Entfernung des Fallbacks reduziert Datenmenge (sicherer) |
</threat_model>

<verification>
1. /fansubs/animeownage laden
2. Geschichte-Sektion: Zeigt Empty-State "Noch keine Geschichte hinterlegt" mit Subtitle "1999 bis 2022 • Deutschland • aktiv" (NICHT als Hauptinhalt wie bisher)
3. Geschichte-Sektion zeigt NICHT mehr die Metazeile als CollapsibleStory-Inhalt
4. Medien-Sektion → Gruppenmedien: Zeigt "Noch keine Medien hinterlegt" (NICHT Logo+Banner-Duplikat des Hero)
5. Hero-Sektion: Unverändert (Logo + Banner weiterhin sichtbar)
6. Keine JS-Konsolfehler
</verification>

<success_criteria>
- Geschichte-Sektion zeigt keine Hero-Metazeile mehr als CollapsibleStory-Hauptinhalt
- Geschichte-Sektion EmptyState-Subtitle enthält die buildFansubFactSummary-Kurzinfo
- Gruppenmedien zeigt keine doppelten Logo/Banner mehr
- Hero-Sektion zeigt unverändert Logo + Banner
- tsc --noEmit: 0 Fehler
</success_criteria>

<output>
Erstelle `.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-09-SUMMARY.md` nach Abschluss.
</output>
