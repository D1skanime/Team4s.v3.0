---
phase: 73-public-fansub-page-fansubs-slug-erweitern
plan: "06"
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - frontend/src/app/fansubs/[slug]/page.module.css
  - frontend/src/components/fansubs/FansubHeroSection.tsx
autonomous: true
requirements: [D-06]
must_haves:
  truths:
    - "Banner im Public Hero wird ohne horizontalen Overflow auf 375px Viewport gerendert (max-width: 100%)"
    - "Banner im Public Hero verwendet object-fit:contain + object-position:center top, keine Verzerrung"
  artifacts:
    - path: "frontend/src/app/fansubs/[slug]/page.module.css"
      provides: "heroBanner overflow:hidden + heroBannerImage object-position:center top"
      contains: "overflow: hidden"
  key_links:
    - from: "FansubHeroSection"
      to: "page.module.css .heroBannerImage"
      via: "styles.heroBannerImage className"
---

<objective>
Behebt Banner-Darstellungsfehler auf der öffentlichen Fansub-Seite: Mobile-Overflow (UAT-13) und falsche object-position im Hero-Banner.

Purpose: Das Hero-Banner überlauft auf schmalen Viewports wegen fehlendem overflow:hidden am .heroBanner-Container. Außerdem fehlt object-position:center top (entspricht dem Admin-Edit-Muster). Kein Eingriff in FansubGroupMediaBlock — dieses File gehört ausschließlich 73-09.

Output: page.module.css mit overflow:hidden in .heroBanner und object-position:center top in .heroBannerImage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/app/fansubs/[slug]/page.module.css
@frontend/src/components/fansubs/FansubHeroSection.tsx
@frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css

<interfaces>
<!-- Referenz-Behandlung aus FansubEdit.module.css (das Ziel-Muster): -->

.fansubEditBannerShell {
  position: relative;
  min-height: 180px;
  background: color-mix(in srgb, var(--bg-secondary) 94%, transparent);
  overflow: hidden;
  isolation: isolate;
}

.fansubEditBannerImageElement {
  display: block;
  height: 180px;
  width: auto;
  max-width: min(100%, 980px);  /* ← responsive cap */
  object-fit: contain;
  object-position: center top;
  filter: drop-shadow(0 12px 24px rgba(76, 54, 31, 0.08));
}

<!-- FansubHeroSection nutzt Next.js Image mit fill + className={styles.heroBannerImage} -->
<!-- FansubGroupMediaBlock.tsx ist NICHT in diesem Plan — gehört zu 73-09 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: heroBannerImage in page.module.css responsiv machen</name>
  <files>frontend/src/app/fansubs/[slug]/page.module.css</files>
  <action>
Bearbeite page.module.css im Hero-Banner-Bereich.

Aktueller Stand: .heroBannerImage setzt object-fit:contain und object-position:center, aber keine explizite Breitenbegrenzung. Da Next.js Image fill verwendet, füllt das Bild den Container — der Container .heroBanner hat zwar keine fixe Breite, aber durch fehlende overflow:hidden am Wrapper kann das Bild bei bestimmten Viewports überlaufen.

Änderungen an .heroBanner (die Wrapper-Div):
- Füge overflow: hidden hinzu (verhindert, dass ein zu breites fill-Bild ausbricht)

Änderungen an .heroBannerImage:
- object-position: center top statt object-position: center (entspricht dem Admin-Edit-Muster)

Bestehende sizes-Angabe in FansubHeroSection.tsx ist bereits korrekt ("(max-width: 767px) calc(100vw - 48px), 960px") — NICHT ändern.

Bestehende @media (max-width: 767px)-Overrides für .heroBanner und .heroBannerImage bleiben erhalten; füge dort min-height: 0 für .heroBanner NICHT ein (min-height:112px bleibt).

Keine Änderungen an FansubHeroSection.tsx nötig — die Komponente nutzt bereits fill und sizes korrekt.

NICHT berühren: FansubGroupMediaBlock.tsx — dieses File gehört ausschließlich zu Plan 73-09.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20 && grep -A 10 "\.heroBanner[^I]" frontend/src/app/fansubs/[slug]/page.module.css | grep "overflow" && grep -A 10 "\.heroBannerImage" frontend/src/app/fansubs/[slug]/page.module.css | grep "object-position"</automated>
  </verify>
  <done>page.module.css enthält overflow:hidden im .heroBanner-Block und object-position:center top im .heroBannerImage-Block; tsc fehlerfrei; grep-Assertions greifen</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| keine neuen | Reine CSS-Änderung, keine Daten-/Auth-Grenze |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-06-01 | Tampering | page.module.css | accept | CSS-only-Änderung; keine Daten- oder Auth-Pfade betroffen |
</threat_model>

<verification>
1. Dev-Server starten: `npm run dev` in frontend/
2. /fansubs/animeownage auf 375px Viewport öffnen (DevTools → iPhone SE)
3. Kein horizontaler Scroll vorhanden (scrollWidth === window.innerWidth)
4. Banner-Bild nicht gestaucht/verzerrt (object-fit:contain erkennbar, kein Abschnitt oben)
5. Keine Änderungen an der Medien-Sektion (gehört zu 73-09)
</verification>

<success_criteria>
- 375px Viewport: document.body.scrollWidth === document.body.clientWidth (kein horizontaler Overflow)
- Public Hero: Banner verzerrungsfrei mit korrektem object-position:center top
- page.module.css: .heroBanner enthält overflow:hidden; .heroBannerImage enthält object-position:center top
- tsc --noEmit: 0 Fehler
</success_criteria>

<output>
Erstelle `.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-06-SUMMARY.md` nach Abschluss.
</output>
