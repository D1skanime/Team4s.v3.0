---
phase: 73-public-fansub-page-fansubs-slug-erweitern
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/fansubs/FansubSectionNav.tsx
  - frontend/src/components/fansubs/FansubSectionNav.module.css
  - frontend/src/components/fansubs/__tests__/FansubTeamSection.test.tsx
  - frontend/src/components/fansubs/__tests__/FansubProjectsSection.test.tsx
  - frontend/src/components/fansubs/__tests__/FansubMediaSection.test.tsx
  - frontend/src/app/fansubs/__tests__/page.test.tsx
autonomous: true
requirements: [B, K]
must_haves:
  truths:
    - "Die Sektions-Navigation ist als eigenständige Client-Komponente mit IntersectionObserver implementiert"
    - "Desktop zeigt eine klebende Sektions-Nav mit Anker-Sprungmarken (Story · Höhepunkte · Projekte · Team · Mitwirkende · Medien …) (D-04)"
    - "Mobil rendert die Nav als klebende, horizontal scrollbare Chip-Leiste mit denselben Sprungmarken; Abschnitte stapeln responsiv einspaltig (D-04, D-06)"
    - "Sticky-Nav-Buttons nutzen Button-Primitive aus @/components/ui (keine nativen <button>)"
    - "Test-Scaffolds für alle kritischen Verhaltensregeln existieren als failing tests"
    - "Team/Contributor-Trennung, Badge-Logik, Verlinkung, Medien-Filterung und Graceful Degradation sind spezifiziert"
  artifacts:
    - path: "frontend/src/components/fansubs/FansubSectionNav.tsx"
      provides: "Client Component mit IntersectionObserver + Sticky-Nav + Mobil-Chip-Leiste"
      min_lines: 40
    - path: "frontend/src/components/fansubs/__tests__/FansubTeamSection.test.tsx"
      provides: "Tests für Team/Contributor-Trennung, Badge unbestätigt, Verlinkung"
      min_lines: 30
    - path: "frontend/src/app/fansubs/__tests__/page.test.tsx"
      provides: "Tests für Graceful Degradation (allSettled + Empty States)"
      min_lines: 20
  key_links:
    - from: "frontend/src/components/fansubs/FansubSectionNav.tsx"
      to: "section[id]-Elemente in page.tsx"
      via: "IntersectionObserver + document.getElementById"
      pattern: "SECTION_IDS.*geschichte|hoehepunkte|projekte"
    - from: "@/components/ui Button"
      to: "FansubSectionNav.tsx Nav-Chips"
      via: "import { Button } from '@/components/ui'"
      pattern: "Button.*variant.*ghost|subtle"
---

<objective>
Plan 01 legt die Ausführungsgrundlage für Phase 73: Die Client-Komponente `FansubSectionNav` wird
vollständig implementiert (D-04) und die Nyquist-Regel-konformen Test-Scaffolds für alle kritischen
Verhaltensregeln (D-07 Team/Contributor-Trennung, D-09 Badge, D-10 Verlinkung, D-13 Deep-Dive-Links,
D-12 Medien-Filterung, D-15 Graceful Degradation) werden als failing tests angelegt.

Purpose: Wave-1-Arbeit, die keine Phase-72-Daten braucht. Sticky-Nav kann sofort implementiert werden
(reine Browser-Interaktion). Test-Scaffolds geben Executors der späteren Waves eine präzise Definition
of Done per Requirement.
Output: FansubSectionNav.tsx (voll implementiert), FansubSectionNav.module.css, vier Test-Scaffold-Dateien.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-CONTEXT.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-RESEARCH.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-PATTERNS.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-UI-SPEC.md

<interfaces>
<!-- Aus frontend/src/components/fansubs/FansubProfileTabs.module.css (Auszug) -->
<!-- bestehende CSS-Tokens als Referenz für FansubSectionNav.module.css -->
<!-- .tabRow → als Basis für .sectionNav sticky row -->
<!-- position: sticky; top-Wert richtet sich nach globalem Header -->

<!-- Aus frontend/src/components/ui/Button.tsx (Signatur): -->
<!-- Props: variant: 'primary' | 'ghost' | 'subtle' | 'danger' | 'link', size: 'sm' | 'md' | 'lg' -->
<!-- Verwendung: <Button variant="ghost" size="sm" onClick={...}>{label}</Button> -->

<!-- Aus frontend/src/app/episodes/[id]/components/ScreenshotGallery/ScreenshotGallery.tsx -->
<!-- IntersectionObserver-Muster: useRef<IntersectionObserver | null>(null) -->
<!-- useEffect: observe bei Mount, disconnect bei Cleanup -->
<!-- rootMargin: '-20% 0px -70% 0px' für Section-Highlighting -->

<!-- Phase-72-Typen (nach Phase-72-Ausführung verfügbar): -->
<!-- import type { DomainProjectionResponse, DomainMemberRow, DomainHistoricalRow, DomainContributorRow } from '@/types/domain-projection' -->
<!-- import type { MediaOwnershipRow } from '@/types/media-ownership' -->
<!-- Phase-73-Tests importieren diese Typen; bei fehlendem Modul werden Tests zu Compile-Fehlern -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FansubSectionNav — Client-Komponente mit IntersectionObserver und CSS</name>
  <files>
    frontend/src/components/fansubs/FansubSectionNav.tsx,
    frontend/src/components/fansubs/FansubSectionNav.module.css
  </files>
  <read_first>
    - frontend/src/components/fansubs/FansubProfileTabs.module.css (CSS-Token-Konventionen, .tabRow als Referenz)
    - frontend/src/components/ui/Button.tsx (Props-Interface, Varianten ghost/subtle)
    - frontend/src/app/episodes/[id]/components/ScreenshotGallery/ScreenshotGallery.tsx (IntersectionObserver-Muster mit useRef)
    - frontend/src/app/fansubs/[slug]/page.module.css (bestehende CSS-Tokens, var(--color-white), var(--color-border))
    - .planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-UI-SPEC.md (Spacing Scale, Color, Interaktionsverträge Sticky-Nav)
    - .planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-PATTERNS.md (FansubSectionNav Pattern Abschnitt)
  </read_first>
  <action>
    Erstelle `FansubSectionNav.tsx` als `'use client'` React-Funktionskomponente mit der Signatur
    `export function FansubSectionNav(): JSX.Element`.

    SECTION_IDS als readonly const-Array mit den acht Werten in Reihenfolge (D-02):
    `'geschichte', 'hoehepunkte', 'projekte', 'team', 'mitwirkende', 'medien', 'timeline', 'deep-dive'`.

    SECTION_LABELS als Record mit deutschen Korrektschreibung (D-04, Copywriting per UI-SPEC):
    `{ 'geschichte': 'Geschichte', 'hoehepunkte': 'Höhepunkte', 'projekte': 'Projekte', 'team': 'Team',
    'mitwirkende': 'Mitwirkende', 'medien': 'Medien', 'timeline': 'Timeline', 'deep-dive': 'Mehr' }`.

    useState für `activeSection` (initialer Wert: `'geschichte'`).
    useRef<IntersectionObserver | null>(null) für den Observer.
    useEffect: erstellt IntersectionObserver mit `rootMargin: '-20% 0px -70% 0px'`; bei `entry.isIntersecting`
    setzt `setActiveSection(entry.target.id)` (Break nach erstem Match). Observiert alle Elemente mit
    document.getElementById(id) für jede ID aus SECTION_IDS. Cleanup via `observerRef.current?.disconnect()`.

    Render: `<nav className={styles.sectionNav} aria-label="Seitennavigation">` mit SECTION_IDS.map(),
    jeder Chip als `<Button key={id} variant={activeSection === id ? 'subtle' : 'ghost'} size="sm"
    aria-current={activeSection === id ? 'true' : undefined}
    onClick={() => document.getElementById(id)?.scrollIntoView({ behavior: 'smooth' })}>
    {SECTION_LABELS[id]}</Button>`.

    Imports: `'use client'`, `{ useEffect, useRef, useState } from 'react'`, `{ Button } from '@/components/ui'`,
    `styles from './FansubSectionNav.module.css'`. Kein nativer `<button>` — ausschließlich `Button`-Primitive (CLAUDE.md).

    Erstelle `FansubSectionNav.module.css` mit:
    - `.sectionNav`: `position: sticky; top: var(--header-height, 60px); z-index: 10; display: flex;
      flex-wrap: nowrap; overflow-x: auto; white-space: nowrap; gap: 8px; padding: 8px 16px;
      background: var(--color-white); border-bottom: 1px solid var(--color-border);`
    - `@media (min-width: 640px)`: keine Änderung nötig (Flex-Row funktioniert auf Desktop automatisch)
    - Scrollbar auf Mobil verstecken: `scrollbar-width: none;` und `&::-webkit-scrollbar { display: none; }`
    Keine Hex-Werte — ausschließlich CSS-Custom-Properties (var(--color-white), var(--color-border),
    var(--color-primary) aus bestehendem Design-System).
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck 2>&1 | grep -i "FansubSectionNav" || echo "no-typecheck-errors"</automated>
  </verify>
  <acceptance_criteria>
    - Quell-Assertion: `FansubSectionNav.tsx` beginnt mit `'use client'`; kein nativer `<button>` oder `<a>` als Nav-Chip — nur `Button`-Primitive.
    - Quell-Assertion: SECTION_IDS enthält exakt 8 Einträge in der Reihenfolge `['geschichte', 'hoehepunkte', 'projekte', 'team', 'mitwirkende', 'medien', 'timeline', 'deep-dive']`.
    - Quell-Assertion: SECTION_LABELS enthält `'Höhepunkte'` (nicht 'Hoehepunkte') und `'Mitwirkende'` (korrekte Umlaute, CLAUDE.md).
    - Quell-Assertion: `useRef<IntersectionObserver | null>(null)` vorhanden; `IntersectionObserver`-Cleanup via `disconnect()` im useEffect-Return.
    - Quell-Assertion: `aria-current={activeSection === id ? 'true' : undefined}` auf jedem Button.
    - Quell-Assertion: `.sectionNav` hat `position: sticky` und `overflow-x: auto` in der CSS-Datei; keine Hex-Farben.
    - Behavior-Assertion: `npm run typecheck` produziert keinen Fehler, der `FansubSectionNav` referenziert.
  </acceptance_criteria>
  <done>FansubSectionNav.tsx und FansubSectionNav.module.css existieren, compilieren fehlerfrei, verwenden ausschließlich Button-Primitive für Nav-Chips, korrekte deutsche Umlaute in allen Labels.</done>
</task>

<task type="auto">
  <name>Task 2: Test-Scaffolds für kritische Verhaltensregeln (Wave 0 — Nyquist)</name>
  <files>
    frontend/src/components/fansubs/__tests__/FansubTeamSection.test.tsx,
    frontend/src/components/fansubs/__tests__/FansubProjectsSection.test.tsx,
    frontend/src/components/fansubs/__tests__/FansubMediaSection.test.tsx,
    frontend/src/app/fansubs/__tests__/page.test.tsx
  </files>
  <read_first>
    - frontend/src/components/profile/MembershipsSection.test.tsx (Vitest-Muster für Server Components mit mock-Props)
    - frontend/src/app/fansubs/[slug]/page.tsx (bestehende Seite — Props-Shape für Mock-Daten)
    - .planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-RESEARCH.md (Abschnitt §Validierungsarchitektur — Test-Mapping Req-IDs)
    - .planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-PATTERNS.md (Phase-72-Typen: DomainMemberRow, DomainHistoricalRow, DomainContributorRow, MediaOwnershipRow)
  </read_first>
  <action>
    Lege vier Test-Scaffold-Dateien mit `describe`-Blöcken und `it.todo`-Einträgen an.
    Jeder Eintrag beschreibt exakt eine prüfbare Verhaltensregel. Die Tests sind zunächst `it.todo`
    — sie werden in späteren Waves implementiert wenn die Komponenten existieren.

    `FansubTeamSection.test.tsx` (Req B/D-07, D-09, D-10):
    - `it.todo('enthält KEINE Einträge aus contributors[] — Team-Block bleibt frei von Contributions')` 
    - `it.todo('historische Einträge mit claimed=false tragen Badge mit Text "unbestätigt"')`
    - `it.todo('historische Einträge mit claimed=true und member_slug gesetzt rendern Link auf /members/[slug]')`
    - `it.todo('historische Einträge mit member_slug=null rendern nur Text, keinen Link')`
    - `it.todo('Mitglieder mit profile_status="memorial" erscheinen im Gedenken-Block, nicht in Aktiv oder Ehemalig')`

    `FansubProjectsSection.test.tsx` (Req B/D-13):
    - `it.todo('Projektkarten verlinken auf /anime/[id]/group/[groupId], nicht auf /anime/[id]')`
    - `it.todo('zeigt Empty State wenn projects-Array leer ist')`

    `FansubMediaSection.test.tsx` (Req G/D-12):
    - `it.todo('rendert nur MediaOwnershipRows mit visibility="public" AND review_status="approved"')`
    - `it.todo('filtert Rows mit visibility="internal" aus der Darstellung heraus')`
    - `it.todo('filtert Rows mit review_status="in_review" aus der Darstellung heraus')`
    - `it.todo('zeigt Empty State wenn nach Filterung keine sichtbaren Medien verbleiben')`

    `page.test.tsx` (Req B/D-15 Graceful Degradation):
    - `it.todo('rendert Team-Abschnitt mit Empty State wenn getDomainProjection-Fetch fehlschlägt')`
    - `it.todo('rendert Medien-Abschnitt mit Empty State wenn getMediaOwnershipProjection-Fetch fehlschlägt')`
    - `it.todo('zeigt Fehlerseite wenn getFansubBySlug 404 zurückgibt')`

    Imports-Struktur: `import { describe, it } from 'vitest'` — weitere Imports kommen wenn die
    Komponenten in späteren Waves implementiert werden.
    Kein `import ... from 'react'` wenn nicht benötigt in den Scaffolds.
    Deutsche Testbeschreibungen mit korrekten Umlauten (CLAUDE.md).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose FansubTeamSection FansubProjectsSection FansubMediaSection 2>&1 | grep -E "todo|pass|fail" | head -30</automated>
  </verify>
  <acceptance_criteria>
    - Quell-Assertion: Alle vier Test-Dateien existieren mit mindestens einem `it.todo`-Eintrag je Datei.
    - Quell-Assertion: `FansubTeamSection.test.tsx` enthält `it.todo` für contributors-Trennung (D-07), Badge „unbestätigt" (D-09) und Verlinkung (D-10).
    - Quell-Assertion: `FansubProjectsSection.test.tsx` enthält `it.todo` für `/anime/[id]/group/[groupId]`-Link (D-13).
    - Quell-Assertion: `FansubMediaSection.test.tsx` enthält `it.todo` für `visibility='public'` AND `review_status='approved'`-Filter (D-12).
    - Quell-Assertion: `page.test.tsx` enthält `it.todo` für Graceful Degradation bei 404 und bei fehlschlagendem domain-projection-Fetch (D-15).
    - Behavior-Assertion: `npx vitest run FansubTeamSection FansubProjectsSection FansubMediaSection` läuft ohne Syntax-Fehler durch; alle Einträge als `todo` gelistet, keine `fail`-Ergebnisse.
  </acceptance_criteria>
  <done>Vier Test-Scaffold-Dateien mit präzisen it.todo-Einträgen für alle Req-B/G/K-Verhaltensregeln existieren; Vitest erkennt sie fehlerfrei als todo-Tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser-Scroll → DOM | IntersectionObserver liest DOM-Elemente; kein User-Input, kein API-Aufruf |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01-01 | Information Disclosure | FansubSectionNav (öffentliche Seite) | accept | Kein API-Aufruf, keine Daten exponiert; reine DOM-Navigation |
| T-73-01-02 | Tampering | native `<button>` statt Button-Primitive | mitigate | Alle Nav-Chips via `Button`-Primitive aus `@/components/ui`; ESLint no-restricted-syntax warnt bei nativem `<button>` (CLAUDE.md) |
| T-73-01-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neuen Pakete in diesem Plan; slopcheck nicht erforderlich |
</threat_model>

<verification>
- `npm run typecheck` läuft ohne Fehler die FansubSectionNav.tsx referenzieren.
- `npx vitest run FansubTeamSection FansubProjectsSection FansubMediaSection` schlägt nicht mit Syntax-Fehlern fehl; alle Einträge als todo.
- FansubSectionNav.tsx: kein nativer `<button>`; korrekte Umlaute in SECTION_LABELS; IntersectionObserver-Cleanup korrekt.
</verification>

<success_criteria>
- FansubSectionNav ist voll implementiert: IntersectionObserver, 8 Abschnitts-Chips, Button-Primitive, Sticky-CSS, Mobil-Overflow.
- Test-Scaffolds für D-07, D-09, D-10, D-12, D-13, D-15 existieren als Vitest-todo-Tests.
- Kein Build-Fehler oder TypeScript-Fehler durch die neuen Dateien.
</success_criteria>

<output>
Create `.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-01-SUMMARY.md` when done
</output>
