---
phase: 73-public-fansub-page-fansubs-slug-erweitern
plan: "12"
type: execute
wave: 4
depends_on: ["11"]
gap_closure: true
files_modified:
  - frontend/src/app/fansubs/[slug]/page.tsx
  - frontend/src/components/fansubs/FansubHeroSection.tsx
  - frontend/src/components/fansubs/GroupLeaderTimeline.tsx
autonomous: true
requirements: [D-07]
must_haves:
  truths:
    - "Kollaborations-Seite zeigt die beteiligten Gruppen als klickbare Links über den dedizierten API-Endpunkt"
    - "Gruppenleitung-Sektion zeigt Ballelboy (fansub_lead) auch wenn leader_timeline leer ist"
  artifacts:
    - path: "frontend/src/app/fansubs/[slug]/page.tsx"
      provides: "Kollaboration-Branch lädt collaboration-members via getCollaborationMembers; übergibt FansubGroupSummary[] an FansubHeroSection; Gruppenleitung-Fallback aus domainProjection.members"
    - path: "frontend/src/components/fansubs/FansubHeroSection.tsx"
      provides: "collaborationMembers-Prop (FansubGroupSummary[]) statt group.collaboration_members"
    - path: "frontend/src/components/fansubs/GroupLeaderTimeline.tsx"
      provides: "fallbackLeads-Prop für synthetische Lead-Einträge wenn entries leer"
  key_links:
    - from: "page.tsx getCollaborationMembers(group.id)"
      to: "FansubHeroSection collaborationMembers"
      via: "CollaborationMemberListResponse.data.map(m => m.member_group)"
    - from: "page.tsx domainProjection.members"
      to: "GroupLeaderTimeline fallbackLeads"
      via: "filter(m => m.roles.includes('fansub_lead'))"
---

<objective>
Behebt zwei verbleibende UAT-Befunde aus Round 2:

UAT-16 (minor): Kollaborations-Seite /fansubs/animeownage-project-messiah (id=92) zeigt "Keine Gruppenangaben hinterlegt." obwohl DB fansub_collaboration_members(92) -> [88,89] enthält. Root Cause: FansubHeroSection liest group.collaboration_members; der Slug-Endpunkt liefert dieses Feld nicht (omitempty). Der dedizierte Endpunkt GET /api/v1/fansubs/:id/collaboration-members existiert und gibt CollaborationMemberListResponse zurück. Fix: In page.tsx wird im Kollaboration-Early-Return-Branch zusätzlich getCollaborationMembers(group.id) aufgerufen. Das Ergebnis (member_group FansubGroupSummary[]) wird als neuer Prop collaborationMembers an FansubHeroSection übergeben. FansubHeroSection liest collaborationMembers statt group.collaboration_members.

UAT-7-Lead (minor): Gruppenleitung-Sektion leer trotz Ballelboy=fansub_lead. Root Cause: contributions.leader_timeline=[] vom Backend. Fix: Frontend-Fallback ohne Backend-Änderung — wenn leader_timeline leer ist, wird GroupLeaderTimeline ein fallbackLeads-Prop übergeben, das aus domainProjection.members mit Rolle 'fansub_lead' synthetisch abgeleitet wird. GroupLeaderTimeline zeigt fallbackLeads wenn entries leer ist.

Kollaboration-Early-Return-Verhalten (kein API-Spam für die vier regulären Endpunkte) aus 73-10 bleibt erhalten.

Output: page.tsx mit getCollaborationMembers-Aufruf und Lead-Fallback; FansubHeroSection mit collaborationMembers-Prop; GroupLeaderTimeline mit fallbackLeads-Prop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@frontend/src/app/fansubs/[slug]/page.tsx
@frontend/src/components/fansubs/FansubHeroSection.tsx
@frontend/src/components/fansubs/GroupLeaderTimeline.tsx
@frontend/src/types/fansub.ts
@frontend/src/types/domain-projection.ts
@frontend/src/types/contributions.ts

<interfaces>
<!-- getCollaborationMembers existiert bereits in api.ts (Zeile 5614): -->
export async function getCollaborationMembers(
  fansubID: number,
  authToken?: string,
): Promise<CollaborationMemberListResponse>

<!-- CollaborationMemberListResponse aus fansub.ts: -->
export interface CollaborationMemberListResponse {
  data: CollaborationMember[];
}
export interface CollaborationMember {
  collaboration_id: number;
  member_group_id: number;
  added_at: string;
  member_group?: FansubGroupSummary;
}
export interface FansubGroupSummary {
  id: number;
  slug: string;
  name: string;
  logo_url?: string | null;
}

<!-- FansubHeroSection aktueller Prop (FansubHeroSection.tsx): -->
interface FansubHeroSectionProps {
  group: FansubGroup
  isCollaboration?: boolean
}
<!-- Aktuell: (group.collaboration_members ?? []).map(member => ...) -->

<!-- DomainProjectionMemberRow aus domain-projection.ts: -->
export interface DomainProjectionMemberRow {
  member_display_name: string;
  roles: string[];       // enthält 'fansub_lead' wenn Rolle zutrifft
  role_labels: string[];
  member_slug: string | null;
  // ...
}

<!-- PublicFansubLeaderEntry aus contributions.ts (für GroupLeaderTimeline): -->
export interface PublicFansubLeaderEntry {
  member_display_name: string
  member_slug: string | null
  role_code: string
  role_label: string
  started_year: number | null
  ended_year: number | null
  status: string
}

<!-- GroupLeaderTimeline aktueller Prop: -->
interface GroupLeaderTimelineProps {
  entries: PublicFansubLeaderEntry[]
}

<!-- Aktuelles Kollaboration-Early-Return in page.tsx (aus 73-10): -->
if (group.group_type === 'collaboration') {
  return (
    <main className={styles.page}>
      <div className={styles.readingColumn}>
        <FansubHeroSection group={group} isCollaboration />
      </div>
    </main>
  )
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: page.tsx — Kollaboration-Branch lädt collaboration-members; Lead-Fallback</name>
  <files>frontend/src/app/fansubs/[slug]/page.tsx</files>
  <action>
Bearbeite frontend/src/app/fansubs/[slug]/page.tsx.

ÄNDERUNG 1 (UAT-16): Kollaboration-Branch erweitern.

Füge getCollaborationMembers zum Import aus '@/lib/api' hinzu (neben getFansubBySlug, etc.).

Suche den Kollaboration-Early-Return-Block (group.group_type === 'collaboration'). Erweitere ihn so, dass vor dem return der Endpunkt GET /api/v1/fansubs/:id/collaboration-members aufgerufen wird:

  if (group.group_type === 'collaboration') {
    let collaborationMembers: import('@/types/fansub').FansubGroupSummary[] = []
    try {
      const collabResponse = await getCollaborationMembers(group.id)
      collaborationMembers = collabResponse.data
        .map((m) => m.member_group)
        .filter((g): g is import('@/types/fansub').FansubGroupSummary => g != null)
    } catch {
      // Fallback: leere Liste; Hero zeigt "Keine Gruppenangaben hinterlegt."
    }
    return (
      <main className={styles.page}>
        <div className={styles.readingColumn}>
          <FansubHeroSection group={group} isCollaboration collaborationMembers={collaborationMembers} />
        </div>
      </main>
    )
  }

Wichtig: Der Aufruf getCollaborationMembers ist nur in diesem Branch — die vier regulären API-Aufrufe (loadFansubProjects, getFansubContributions, getFansubGroupDomainProjection, getMediaOwnershipProjection) feuern für Kollaborationen weiterhin NICHT.

Importiere FansubGroupSummary aus '@/types/fansub' am Dateikopf (zu den bestehenden fansub-Importen) wenn noch nicht vorhanden.

ÄNDERUNG 2 (UAT-7-Lead): Lead-Fallback aus domainProjection.

Suche die Zeile, die GroupLeaderTimeline rendert:
  <GroupLeaderTimeline entries={contributions?.leader_timeline ?? []} />

Ersetze sie durch:

  <GroupLeaderTimeline
    entries={contributions?.leader_timeline ?? []}
    fallbackLeads={domainProjection.members
      .filter((m) => m.roles.includes('fansub_lead'))
      .map((m) => ({
        member_display_name: m.member_display_name,
        member_slug: m.member_slug,
        role_code: 'fansub_lead',
        role_label: 'Fansub-Lead',
        started_year: null,
        ended_year: null,
        status: m.status,
      }))}
  />

Der Typ des gemappten Objekts entspricht PublicFansubLeaderEntry aus @/types/contributions.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>page.tsx: Kollaboration-Branch ruft getCollaborationMembers auf und übergibt collaborationMembers an FansubHeroSection; GroupLeaderTimeline erhält fallbackLeads aus domainProjection.members mit Rolle 'fansub_lead'; tsc fehlerfrei</done>
</task>

<task type="auto">
  <name>Task 2: FansubHeroSection + GroupLeaderTimeline — neue Props verdrahten</name>
  <files>frontend/src/components/fansubs/FansubHeroSection.tsx, frontend/src/components/fansubs/GroupLeaderTimeline.tsx</files>
  <action>
ÄNDERUNG 1: FansubHeroSection.tsx — collaborationMembers-Prop.

Füge optionalen Prop `collaborationMembers?: FansubGroupSummary[]` zu FansubHeroSectionProps hinzu:

  interface FansubHeroSectionProps {
    group: FansubGroup
    isCollaboration?: boolean
    collaborationMembers?: FansubGroupSummary[]
  }

Importiere FansubGroupSummary aus '@/types/fansub' (neben FansubGroup).

Im isCollaboration-Block: Ersetze `(group.collaboration_members ?? [])` durch `(collaborationMembers ?? [])`:

Vorher:
  {(group.collaboration_members ?? []).length > 0 ? (
    <ul ...>
      {(group.collaboration_members ?? []).map((member) => (

Nachher:
  {(collaborationMembers ?? []).length > 0 ? (
    <ul ...>
      {(collaborationMembers ?? []).map((member) => (

Alle anderen Zeilen im isCollaboration-Block bleiben unverändert (Link href, Badge, Fallback-Text).

ÄNDERUNG 2: GroupLeaderTimeline.tsx — fallbackLeads-Prop.

Füge optionalen Prop `fallbackLeads?: PublicFansubLeaderEntry[]` zu GroupLeaderTimelineProps hinzu:

  interface GroupLeaderTimelineProps {
    entries: PublicFansubLeaderEntry[]
    fallbackLeads?: PublicFansubLeaderEntry[]
  }

Ändere die Render-Logik: Wenn entries leer ist UND fallbackLeads vorhanden und nicht leer sind, rendere fallbackLeads als Einträge statt den Empty-State:

  const effectiveEntries = entries.length > 0 ? entries : (fallbackLeads ?? [])

  if (effectiveEntries.length === 0) {
    return (
      <section className={styles.timeline}>
        <h2 className={styles.heading}>Gruppenleitung</h2>
        <p className={styles.empty}>Noch keine Gruppenleitung eingetragen.</p>
      </section>
    )
  }

  return (
    <section className={styles.timeline}>
      <h2 className={styles.heading}>Gruppenleitung</h2>
      <ol className={styles.list}>
        {effectiveEntries.map((entry, index) => (
          // ... bestehender li-Inhalt unverändert
        ))}
      </ol>
    </section>
  )

Die li-Inhalte (year, separator, name, historicalLabel, roleLabel) bleiben exakt wie im Original. Nur die Map-Quelle wechselt von entries zu effectiveEntries.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>FansubHeroSection.tsx: collaborationMembers-Prop; Kollaboration-Block rendert collaborationMembers statt group.collaboration_members; GroupLeaderTimeline.tsx: fallbackLeads-Prop; effectiveEntries = entries || fallbackLeads; tsc fehlerfrei</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| API → Frontend | getCollaborationMembers: Backend-Response bestimmt welche Gruppen-Links angezeigt werden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-12-01 | Information Disclosure | getCollaborationMembers | accept | Endpunkt liefert nur öffentliche FansubGroupSummary-Daten (id, slug, name, logo_url); keine sensitiven Felder |
| T-73-12-02 | Denial of Service | Kollaboration-Branch API-Aufruf | accept | Einmaliger Aufruf nur im collaboration-Branch; reguläre Gruppen unverändert; try/catch verhindert Seitenabsturz bei API-Fehler |
| T-73-12-03 | Tampering | Lead-Fallback aus DomainProjection | accept | String-Vergleich 'fansub_lead' in roles-Array; nur für UI-Darstellung, keine Berechtigungsentscheidungen |
| T-73-12-SC | Tampering | npm-Installs | accept | Kein neues npm-Package installiert; ausschließlich bestehende Codebase-Seams genutzt |
</threat_model>

<verification>
Live-Checks gegen Dev-Server :3000:

UAT-16 Verifikation:
1. /fansubs/animeownage-project-messiah aufrufen (Kollaboration id=92)
2. Hero-Block zeigt "Dies ist eine Kollaboration zwischen:" gefolgt von klickbaren Gruppen-Links
3. API-Bestätigung: GET /api/v1/fansubs/92/collaboration-members → data enthält member_group-Objekte mit id 88 und 89
4. Links führen zu /fansubs/animeownage und dem zweiten Mitglieds-Slug
5. Keine weiteren Sektionen (kein Team, keine Projekte, keine Geschichte)

UAT-7-Lead Verifikation:
1. /fansubs/animeownage aufrufen (normale Gruppe)
2. Gruppenleitung-Sektion: Ballelboy erscheint als Lead-Eintrag (auch wenn leader_timeline leer ist)
3. Rollenlabel "Fansub-Lead" angezeigt
4. API-Bestätigung: GET /api/v1/fansubs/88/domain-projection → members enthält Ballelboy mit roles ['fansub_lead']

Regressions-Check:
5. /fansubs/animeownage: Normale Sektionen (Team, Projekte, Geschichte, Mitwirkende) unverändert
6. tsc --noEmit: 0 Fehler
</verification>

<success_criteria>
- /fansubs/animeownage-project-messiah zeigt klickbare Gruppenlinks (animeownage + zweite Gruppe) im Kollaboration-Block
- /fansubs/animeownage Gruppenleitung-Sektion: Ballelboy sichtbar mit Rolle Fansub-Lead
- Kein API-Spam: Kollaborations-Seite ruft nur getCollaborationMembers auf (nicht loadFansubProjects etc.)
- tsc --noEmit: 0 Fehler
</success_criteria>

<output>
Erstelle `.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-12-SUMMARY.md` nach Abschluss.
</output>
