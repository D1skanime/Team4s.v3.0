---
phase: 67-release-episode-credits
plan: 03
type: execute
wave: 2
depends_on: ["67-01"]
files_modified:
  - backend/internal/repository/anime_contributions_public_repository.go
  - backend/internal/repository/anime_contributions_public_versions_repository.go
  - backend/internal/repository/anime_contributions_public_versions_repository_test.go
autonomous: true
requirements: [P67-SC2]

must_haves:
  truths:
    - "Die oeffentliche Anime-Query liefert anime-weite Beitraege (release_version_id IS NULL) getrennt von versions-spezifischen Beitraegen."
    - "Versions-spezifische Beitraege erscheinen NICHT zusaetzlich im anime-weiten Block (keine Doppelanzeige)."
    - "Die Versions-Aufschluesselung ist nach Episode-Nr, dann Version sortiert."
    - "Die Versions-Ebene fuehrt dieselben Public-Filter (is_public_on_anime_page=true AND hfgm.visibility='public') wie die anime-weite Ebene."
  artifacts:
    - path: "backend/internal/repository/anime_contributions_public_versions_repository.go"
      provides: "Ebene-2-Aggregation (attachVersionBreakdowns) + Breakdown-DTOs"
      contains: "attachVersionBreakdowns"
    - path: "backend/internal/repository/anime_contributions_public_repository.go"
      provides: "Ebene-1-Filter release_version_id IS NULL + Aufruf attachVersionBreakdowns"
      contains: "release_version_id IS NULL"
  key_links:
    - from: "GetPublicAnimeContributions"
      to: "attachVersionBreakdowns"
      via: "Aufruf nach attachHiddenCounts (map-Merge ueber fansub_group_id)"
      pattern: "attachVersionBreakdowns"
---

<objective>
Die oeffentliche Anime-Contributions-Query um eine zweite Aggregations-Ebene erweitern: die bestehende Ebene-1-Query wird auf anime-weite Beitraege (release_version_id IS NULL) gefiltert, und eine neue Methode attachVersionBreakdowns (Two-Query-map-Merge wie attachHiddenCounts) haengt pro Gruppe die versions-spezifischen Beitraege an, sortiert Episode-Nr -> Version. Neue DTO-Felder spiegeln das Frontend.

Purpose: P67-SC2 Backend-Anteil (D-05, D-06, D-07). Verhindert Doppelanzeige (Pitfall 2) und Information Disclosure nicht-oeffentlicher Beitraege in der Versions-Ebene.
Output: erweiterte Ebene-1-Query, neue Versions-Aggregations-Datei + DTOs, Repo-Test fuer Ebenen-Trennung und Sortierung.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/67-release-episode-credits/67-CONTEXT.md
@.planning/phases/67-release-episode-credits/67-RESEARCH.md
@.planning/phases/67-release-episode-credits/67-PATTERNS.md

<interfaces>
GetPublicAnimeContributions (anime_contributions_public_repository.go:93-172):
  Ebene-1-WHERE Z. 115-117: WHERE ac.anime_id=$1 AND ac.is_public_on_anime_page=true AND hfgm.visibility='public'
  Sammelt groups []PublicAnimeContributionGroup + groupIndex map[int64]int; ruft attachHiddenCounts (Z. 167) auf.
attachHiddenCounts (Z. 176-204): Two-Query-Muster, zweite Query GROUP BY ac.fansub_group_id, Merge ueber groupIndex.
PublicAnimeContributionGroup (Z. 34-42): FansubGroupID, FansubGroupName, FansubGroupSlug, ActiveFromYear, ActiveUntilYear, Contributors []PublicContributorRow, HiddenContributorCount.
PublicContributorRow (Z. 23-31): MemberDisplayName, MemberSlug, Roles, RoleLabels, StartedYear, EndedYear, IsVerified.
memberSlugExpr / memberDisplayExpr: wiederverwendbare SQL-Ausdruecke (Z. 14, 18).
role_labels: COALESCE(rd.label_de, acr.role_code) via LEFT JOIN role_definitions rd (Muster Z. 108, 114).
Release-Hierarchie (0035): release_versions(id, release_id, version) -> fansub_releases(id, release_id?, episode_id) -> episodes(id, anime_id, episode_number VARCHAR, sort_index). NULL-safe Episode-Sortierung: COALESCE(ep.sort_index, 2147483647), ep.id.
anime_contributions_public_repository.go = 389 Zeilen -> nur minimaler Edit (Filter + ein Methodenaufruf); Aggregation in neue Datei (Pitfall 3).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ebene-2-Aggregation (neue Datei) + DTOs</name>
  <files>backend/internal/repository/anime_contributions_public_versions_repository.go, backend/internal/repository/anime_contributions_public_versions_repository_test.go</files>
  <read_first>
    - backend/internal/repository/anime_contributions_public_repository.go (attachHiddenCounts Z. 176-204, GetPublicAnimeContributions Ebene-1-Query, DTO-Structs, memberSlugExpr/memberDisplayExpr)
    - database/migrations/0035_add_release_tables.up.sql (release_versions/fansub_releases/episodes-Spalten, episode_number VARCHAR)
    - .planning/phases/67-release-episode-credits/67-PATTERNS.md (Abschnitt public_versions-Datei)
    - frontend/src/types/contributions.ts (Ziel-JSON-Form fuer ReleaseVersionBreakdown)
  </read_first>
  <behavior>
    - attachVersionBreakdowns sammelt nur Zeilen mit ac.release_version_id IS NOT NULL.
    - Es fuehrt dieselben Public-Filter wie Ebene 1 (is_public_on_anime_page=true AND hfgm.visibility='public').
    - Ergebnis pro Gruppe sortiert nach Episode (sort_index NULL-safe, dann episode_number/id) und dann version.
    - Merge ueber groupIndex: nur Gruppen, die bereits anime-weit existieren? Nein -> auch Gruppen, die NUR versions-spezifische Beitraege haben, muessen erscheinen (siehe action).
  </behavior>
  <action>
    Lege anime_contributions_public_versions_repository.go an (package repository). Definiere zwei DTOs (snake_case JSON, Spiegel zu contributions.ts):
      ReleaseVersionBreakdownGroup{ ReleaseVersionID int64 json:"release_version_id"; EpisodeNumber string json:"episode_number"; Version string json:"version"; Contributors []PublicContributorRow json:"contributors" }
    und ergaenze PublicAnimeContributionGroup (in anime_contributions_public_repository.go, Task 2) um VersionBreakdown []ReleaseVersionBreakdownGroup json:"version_breakdown".
    Implementiere Methode attachVersionBreakdowns(ctx, animeID int64, groups []PublicAnimeContributionGroup, groupIndex map[int64]int) error:
      - Eine Query, die je (fansub_group_id, release_version_id, episode_number, version, contributor) liefert, mit denselben JOINs/Filtern wie Ebene 1 PLUS JOIN release_versions rv ON rv.id=ac.release_version_id JOIN fansub_releases fr ON fr.id=rv.release_id JOIN episodes ep ON ep.id=fr.episode_id, WHERE ac.anime_id=$1 AND ac.is_public_on_anime_page=true AND hfgm.visibility='public' AND ac.release_version_id IS NOT NULL.
      - role_codes/role_labels per ARRAY_AGG + COALESCE(rd.label_de, ...) wie Ebene 1; memberDisplayExpr/memberSlugExpr wiederverwenden.
      - ORDER BY ac.fansub_group_id, COALESCE(ep.sort_index, 2147483647), ep.id, rv.version (D-07).
      - Im Go-Code pro fansub_group_id in den passenden group via groupIndex einsortieren; innerhalb der Gruppe nach (release_version_id) zu ReleaseVersionBreakdownGroup buendeln (Reihenfolge aus der Query beibehalten). Falls eine Gruppe nur versions-spezifische Beitraege hat und noch nicht in groups existiert, neue PublicAnimeContributionGroup anlegen und in groups + groupIndex aufnehmen (sodass solche Gruppen auf der Anime-Seite erscheinen).
    Datei deutlich < 450 Zeilen. Schreibe anime_contributions_public_versions_repository_test.go: Test-Name enthaelt "PublicAnimeContributions" (damit -run PublicAnimeContributions greift); pruefe Ebenen-Trennung (Ebene-2 nur IS NOT NULL) und Sortierreihenfolge (DB-gestuetzt falls Infrastruktur vorhanden, sonst Query-/Logik-Sicherung analog bestehender repo-Tests).
  </action>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/repository/... -run PublicAnimeContributions -count=1</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... erfolgreich.
    - attachVersionBreakdowns existiert; Query filtert ac.release_version_id IS NOT NULL und fuehrt is_public_on_anime_page=true AND hfgm.visibility='public'.
    - ORDER BY enthaelt COALESCE(ep.sort_index, 2147483647), ep.id, rv.version.
    - ReleaseVersionBreakdownGroup-DTO mit snake_case JSON-Tags.
    - Datei < 450 Zeilen.
  </acceptance_criteria>
  <done>Ebene-2-Aggregation kompiliert; Versions-Filter und Sortierung gesetzt; Test mit "PublicAnimeContributions"-Namen laeuft.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ebene-1-Filter + Verdrahtung attachVersionBreakdowns</name>
  <files>backend/internal/repository/anime_contributions_public_repository.go</files>
  <read_first>
    - backend/internal/repository/anime_contributions_public_repository.go (GetPublicAnimeContributions Z. 93-172, WHERE Z. 115-117, attachHiddenCounts-Aufruf Z. 167, PublicAnimeContributionGroup Z. 34-42)
    - .planning/phases/67-release-episode-credits/67-PATTERNS.md (Pitfall 2: Doppelanzeige)
  </read_first>
  <behavior>
    - Nach der Aenderung enthaelt der "Allgemein"-Block (Ebene 1) ausschliesslich Beitraege mit release_version_id IS NULL.
    - attachVersionBreakdowns wird genau einmal nach attachHiddenCounts aufgerufen.
  </behavior>
  <action>
    1. Ebene-1-WHERE in GetPublicAnimeContributions um "AND ac.release_version_id IS NULL" ergaenzen (verhindert Doppelanzeige, Pitfall 2).
    2. PublicAnimeContributionGroup um das Feld VersionBreakdown []ReleaseVersionBreakdownGroup json:"version_breakdown" erweitern (Definition lebt in der neuen Datei aus Task 1; hier nur das Struct-Feld).
    3. Direkt nach dem bestehenden attachHiddenCounts-Aufruf r.attachVersionBreakdowns(ctx, animeID, groups, groupIndex) aufrufen und bei Fehler zurueckgeben. Reihenfolge: erst attachHiddenCounts (bleibt gruppenweit, KEINE Trennung nach Ebene -> sonst Doppelzaehlung), dann attachVersionBreakdowns.
    attachHiddenCounts selbst NICHT veraendern. Datei bleibt < 450 Zeilen (nur additive Zeilen).
  </action>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/repository/... -run PublicAnimeContributions -count=1</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... erfolgreich.
    - Ebene-1-WHERE enthaelt "ac.release_version_id IS NULL".
    - attachVersionBreakdowns wird nach attachHiddenCounts aufgerufen.
    - attachHiddenCounts bleibt unveraendert (gruppenweite Zaehlung).
    - PublicAnimeContributionGroup hat das Feld version_breakdown.
  </acceptance_criteria>
  <done>Ebene-1 ist auf anime-weite Beitraege gefiltert und die Versions-Ebene wird angehaengt; keine Doppelanzeige.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public-API -> oeffentlicher Anime-Seiten-Konsument | Anonyme Leser sehen nur oeffentliche Beitraege |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-03-ID | Information Disclosure | Versions-Ebene-Query | mitigate | Ebene-2-Query fuehrt dieselben Public-Filter (is_public_on_anime_page=true AND hfgm.visibility='public') wie Ebene 1. |
| T-67-03-DBL | (Korrektheit) | Doppelanzeige Ebene 1/2 | mitigate | Ebene-1-Query um ac.release_version_id IS NULL ergaenzt; hidden-count bleibt gruppenweit (Pitfall 2). |
| T-67-03-SQLI | Tampering | animeID-Parameter | mitigate | Parametrisierte pgx-Query ($1). |
</threat_model>

<verification>
- `cd backend && go build ./...` gruen.
- `go test ./internal/repository/... -run PublicAnimeContributions -count=1` gruen.
- Manuell/Test: ein Member mit anime-weitem UND versions-spezifischem Eintrag erscheint nur einmal im "Allgemein"-Block.
</verification>

<success_criteria>
- P67-SC2 Backend-Anteil erfuellt: getrennte Ebenen, keine Doppelanzeige, Sortierung Episode -> Version, gleiche Public-Filter.
</success_criteria>

<output>
Create `.planning/phases/67-release-episode-credits/67-03-SUMMARY.md` when done
</output>
