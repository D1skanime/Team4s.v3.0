---
phase: 67-release-episode-credits
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/0090_anime_contributions_release_version.up.sql
  - database/migrations/0090_anime_contributions_release_version.down.sql
  - backend/internal/migrations/phase67_release_version_credits_test.go
autonomous: false
requirements: [P67-SC1]

must_haves:
  truths:
    - "Die Spalte release_version_id existiert nullable auf anime_contributions mit FK auf release_versions(id) ON DELETE SET NULL."
    - "Der UNIQUE-Constraint uq_anime_contribution_member umfasst release_version_id und nutzt NULLS NOT DISTINCT, sodass derselbe Member je Version genau einmal und anime-weit genau einmal eingetragen werden kann."
    - "Die Migration ist sauber up und down anwendbar (idempotente IF EXISTS/IF NOT EXISTS Form)."
  artifacts:
    - path: "database/migrations/0090_anime_contributions_release_version.up.sql"
      provides: "Additive Spalte + Index + erweiterter UNIQUE-Constraint"
      contains: "release_version_id"
    - path: "database/migrations/0090_anime_contributions_release_version.down.sql"
      provides: "Reversibles Down (Constraint auf 3-Spalten-Form, Index + Spalte drop)"
      contains: "uq_anime_contribution_member"
    - path: "backend/internal/migrations/phase67_release_version_credits_test.go"
      provides: "Migration-Contract-Test (string-contract)"
      contains: "TestPhase67ReleaseVersionCreditsMigrationContract"
  key_links:
    - from: "anime_contributions.release_version_id"
      to: "release_versions.id"
      via: "FOREIGN KEY ... ON DELETE SET NULL"
      pattern: "references release_versions\\(id\\) on delete set null"
---

<objective>
Migration 0090 fuegt anime_contributions die optionale Spalte release_version_id hinzu (FK -> release_versions, ON DELETE SET NULL), legt einen partiellen Index an und ersetzt den bestehenden UNIQUE-Constraint uq_anime_contribution_member (aus 0088) durch eine vierspaltige Variante mit NULLS NOT DISTINCT. Ein Migration-Contract-Test sichert die Form ab. Die Migration wird per Migrate-Binary angewendet, damit nachgelagerte Plaene gegen die reale Spalte arbeiten.

Purpose: P67-SC1 Schema-Fundament. Ohne diese Spalte und ohne den erweiterten UNIQUE-Constraint koennen versions-spezifische Beitraege nicht koexistieren (Pitfall 1: zweiter Eintrag wuerde auf den anime-weiten Eintrag upserten).
Output: Migrationspaar 0090 (up/down), Migration-Contract-Test, angewendete Migration auf der lokalen DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-release-episode-credits/67-CONTEXT.md
@.planning/phases/67-release-episode-credits/67-RESEARCH.md
@.planning/phases/67-release-episode-credits/67-PATTERNS.md

<interfaces>
Bestehender Constraint, der ersetzt wird (database/migrations/0088_anime_contributions_constraints.up.sql:21-23):
  ADD CONSTRAINT uq_anime_contribution_member UNIQUE (fansub_group_id, anime_id, fansub_group_member_id)
Bestehendes Down-Muster (0088 down): DROP CONSTRAINT IF EXISTS uq_anime_contribution_member; plus zugehoerige FK/Hilfs-Constraints.
Migrate-Binary: backend/cmd/migrate (Kommandos: up, down, status; Flags --dir, --database-url). Compose mountet ./database/migrations nach /app/database/migrations.
Hoechste vorhandene Migration auf Platte: 0089_anime_contributions_review_note (verifiziert) -> 0090 ist frei.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0090 (up/down) schreiben</name>
  <files>database/migrations/0090_anime_contributions_release_version.up.sql, database/migrations/0090_anime_contributions_release_version.down.sql</files>
  <read_first>
    - database/migrations/0088_anime_contributions_constraints.up.sql (Quelle des zu ersetzenden uq_anime_contribution_member)
    - database/migrations/0088_anime_contributions_constraints.down.sql (Down-Stil)
    - database/migrations/0089_anime_contributions_review_note.up.sql (ADD COLUMN IF NOT EXISTS Muster)
    - database/migrations/0086_anime_contributions.up.sql (Tabellen-Kommentar "historisches Faktenregister", FK-ON-DELETE-Muster)
    - .planning/phases/67-release-episode-credits/67-PATTERNS.md (Zielform unter "0090_...release_version.up.sql")
  </read_first>
  <action>
    Schreibe 0090_anime_contributions_release_version.up.sql mit drei Schritten (per D-01, Pitfall 1):
    1. ALTER TABLE anime_contributions ADD COLUMN IF NOT EXISTS release_version_id BIGINT NULL REFERENCES release_versions(id) ON DELETE SET NULL. ON DELETE SET NULL begruendet im Kommentar: historisches Faktum bleibt erhalten, faellt auf anime-weit zurueck.
    2. CREATE INDEX IF NOT EXISTS idx_anime_contributions_release_version ON anime_contributions(release_version_id) WHERE release_version_id IS NOT NULL.
    3. ALTER TABLE anime_contributions DROP CONSTRAINT IF EXISTS uq_anime_contribution_member; danach ADD CONSTRAINT uq_anime_contribution_member UNIQUE NULLS NOT DISTINCT (fansub_group_id, anime_id, fansub_group_member_id, release_version_id). NULLS NOT DISTINCT ist PG15+ (Projekt nutzt PG16).
    Schreibe das Down: DROP CONSTRAINT IF EXISTS uq_anime_contribution_member; ADD CONSTRAINT uq_anime_contribution_member UNIQUE (fansub_group_id, anime_id, fansub_group_member_id) (Rueckkehr zur 3-Spalten-Form aus 0088); DROP INDEX IF EXISTS idx_anime_contributions_release_version; ALTER TABLE anime_contributions DROP COLUMN IF EXISTS release_version_id.
    Den Composite-FK fk_anime_contributions_member_group aus 0088 NICHT anfassen (bleibt unveraendert). Keine deutschen user-facing Strings noetig (reines DDL); Kommentare auf Deutsch zulaessig.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const u=fs.readFileSync('database/migrations/0090_anime_contributions_release_version.up.sql','utf8').toLowerCase();const d=fs.readFileSync('database/migrations/0090_anime_contributions_release_version.down.sql','utf8').toLowerCase();const need=['add column if not exists release_version_id bigint null','references release_versions(id) on delete set null','drop constraint if exists uq_anime_contribution_member','unique nulls not distinct (fansub_group_id, anime_id, fansub_group_member_id, release_version_id)','idx_anime_contributions_release_version'];const miss=need.filter(s=>!u.includes(s));if(miss.length){console.error('UP fehlt:',miss);process.exit(1)}if(!d.includes('drop column if exists release_version_id')){console.error('DOWN fehlt drop column');process.exit(1)}console.log('OK migration form')"</automated>
  </verify>
  <acceptance_criteria>
    - UP enthaelt alle fuenf Pflicht-Token (Spalte, FK SET NULL, DROP+ADD UNIQUE NULLS NOT DISTINCT vierspaltig, Index).
    - DOWN dropt Constraint, stellt 3-Spalten-UNIQUE wieder her, dropt Index und Spalte.
    - Kein UNION/Trigger/CASCADE auf release_version_id (Anti-Pattern, Research).
  </acceptance_criteria>
  <done>Beide Migrationsdateien existieren und enthalten die geforderten DDL-Anweisungen in der korrekten Form.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration-Contract-Test phase67</name>
  <files>backend/internal/migrations/phase67_release_version_credits_test.go</files>
  <read_first>
    - backend/internal/migrations/phase61_contributions_model_test.go (exaktes Muster: readMigrationFile + assertContainsAll, strings.ToLower)
    - .planning/phases/67-release-episode-credits/67-PATTERNS.md (Test-Struktur unter phase67_release_version_credits_test.go)
  </read_first>
  <behavior>
    - up enthaelt: "add column if not exists release_version_id bigint null", "references release_versions(id) on delete set null", "drop constraint if exists uq_anime_contribution_member", "unique nulls not distinct (fansub_group_id, anime_id, fansub_group_member_id, release_version_id)", "idx_anime_contributions_release_version".
    - down enthaelt: "drop constraint if exists uq_anime_contribution_member", "drop column if exists release_version_id".
  </behavior>
  <action>
    Lege TestPhase67ReleaseVersionCreditsMigrationContract an. Nutze die in der Package bereits vorhandenen Helper readMigrationFile(t, name) und assertContainsAll(t, content, []string) (NICHT neu definieren). Lies 0090_..._release_version.up.sql und .down.sql via strings.ToLower und pruefe die Token aus dem behavior-Block. Package bleibt migrations.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/migrations/... -run Phase67 -count=1</automated>
  </verify>
  <acceptance_criteria>
    - go test ./internal/migrations/... -run Phase67 ist gruen.
    - Test referenziert exakt den Dateinamen 0090_anime_contributions_release_version.{up,down}.sql.
    - assertContainsAll/readMigrationFile werden wiederverwendet, nicht dupliziert.
  </acceptance_criteria>
  <done>Der Migration-Contract-Test laeuft gruen und sichert die Form aus Task 1 ab.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Migration 0090 anwenden und verifizieren</name>
  <what-built>Migration 0090 angewendet auf die lokale DB ueber das Migrate-Binary. Dies ist ein [BLOCKING] Schritt: Build/Type-Checks der nachgelagerten Plaene wuerden auch ohne reale Spalte gruen sein (False-Positive), daher MUSS die Migration vor jeglicher Repository-Verifikation laufen.</what-built>
  <how-to-verify>
    Der Executor wendet die Migration zunaechst automatisiert an (KEINE manuelle Arbeit, die Claude tun kann):
    1. Sicherstellen, dass die DB laeuft: `docker compose up -d team4sv30-db`.
    2. Migration anwenden ueber das Migrate-Binary analog zu fruehe Phasen, z. B. `docker compose run --rm team4sv30-backend ./migrate up --dir /app/database/migrations` ODER lokal `cd backend && go run ./cmd/migrate up --dir ../database/migrations --database-url "$DATABASE_URL"`. Die in fruehe Phasen genutzte Variante (Migrate-Service/Container) verwenden; Status pruefen mit `... status`.
    3. Verifizieren, dass die Spalte existiert: psql-Abfrage `\d anime_contributions` zeigt release_version_id und Constraint uq_anime_contribution_member umfasst vier Spalten.
    Der Mensch bestaetigt anschliessend nur, dass die Migration ohne Fehler durchlief (insbesondere kein UNIQUE-Kollisions-Fehler bei bestehenden Testdaten, A1).
  </how-to-verify>
  <resume-signal>Tippe "approved" wenn die Migration sauber angewendet ist, oder beschreibe den Fehler (z. B. ADD CONSTRAINT-Kollision -> Pre-Cleanup der Testdaten noetig).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DDL-Migration -> bestehende Daten | Aenderung eines UNIQUE-Constraints auf einer Tabelle mit (Test-)Daten |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01-DM | Denial of Service | ADD CONSTRAINT auf bestehende Zeilen | accept | A1: anime_contributions enthaelt nur Testdaten (User-bestaetigt). Bei realer Kollision schlaegt ADD CONSTRAINT fehl -> Checkpoint deckt das ab (Pre-Cleanup). |
| T-67-01-DL | Tampering | Datenverlust bei Versions-Loeschung | mitigate | ON DELETE SET NULL statt CASCADE: historisches Faktum bleibt erhalten, faellt auf anime-weit zurueck (Anti-Pattern CASCADE explizit vermieden). |
</threat_model>

<verification>
- `cd backend && go test ./internal/migrations/... -run Phase67 -count=1` gruen.
- Migration up und down laufen ohne Fehler durch (Checkpoint).
- `\d anime_contributions` zeigt release_version_id (nullable) + vierspaltigen uq_anime_contribution_member.
</verification>

<success_criteria>
- P67-SC1 Schema-Anteil erfuellt: nullable FK + erweiterter UNIQUE-Constraint + Index existieren in der DB.
- Migration ist reversibel und durch den Contract-Test gesichert.
</success_criteria>

<output>
Create `.planning/phases/67-release-episode-credits/67-01-SUMMARY.md` when done
</output>
