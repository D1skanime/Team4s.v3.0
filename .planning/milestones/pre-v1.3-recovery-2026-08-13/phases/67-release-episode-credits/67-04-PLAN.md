---
phase: 67-release-episode-credits
plan: 04
type: execute
wave: 3
depends_on: ["67-02", "67-03"]
files_modified:
  - backend/internal/handlers/fansub_contributions_release_versions_handler.go
  - backend/cmd/server/main.go
  - shared/contracts/openapi.yaml
  - frontend/src/types/contributions.ts
  - frontend/src/types/fansub.ts
  - frontend/src/lib/api.ts
  - frontend/src/components/anime/ReleaseVersionBreakdown.tsx
  - frontend/src/components/anime/ReleaseVersionBreakdown.module.css
  - frontend/src/components/anime/GroupContributionBlock.tsx
  - frontend/src/components/anime/GroupContributionBlock.test.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionModal.tsx
autonomous: false
requirements: [P67-SC2, P67-SC1]

must_haves:
  truths:
    - "Die oeffentliche Anime-Seite zeigt pro Gruppe zuerst die anime-weiten Beitraege und darunter eine aufklappbare Versions-Detailebene, wenn versions-spezifische Beitraege existieren."
    - "Die Versions-Ebene ist nach Episode-Nr -> Version sortiert und nutzt dieselben Rollen-Chips wie die anime-weite Ebene."
    - "Das Leader-Contribution-Formular bietet ein optionales, gruppen-gefiltertes Release-Version-Dropdown mit Leeroption '— anime-weit lassen —'."
    - "Ein 422 vom Backend (gruppen-fremde Version) wird im Formular als Feldfehler dargestellt."
  artifacts:
    - path: "frontend/src/components/anime/ReleaseVersionBreakdown.tsx"
      provides: "Aufklappbare Versions-Detailebene (Progressive Disclosure)"
      contains: "release_version_id"
    - path: "backend/internal/handlers/fansub_contributions_release_versions_handler.go"
      provides: "GET-Endpunkt fuer gruppen-gefilterte Release-Versionen (Dropdown-Daten)"
      contains: "ListGroupReleaseVersionsForAnime"
  key_links:
    - from: "AnimeContributionModal.tsx"
      to: "GET /admin/fansubs/:id/anime/:animeId/release-versions"
      via: "getFansubAnimeReleaseVersions (api.ts)"
      pattern: "getFansubAnimeReleaseVersions"
    - from: "GroupContributionBlock.tsx"
      to: "ReleaseVersionBreakdown.tsx"
      via: "group.version_breakdown gerendert"
      pattern: "version_breakdown|ReleaseVersionBreakdown"
---

<objective>
Frontend-Verdrahtung der Versions-Dimension plus der dafuer noetige Dropdown-Endpunkt: ein neuer GET-Endpunkt liefert die gruppen-gefilterten Release-Versionen (aus ListGroupReleaseVersionsForAnime), die zentrale api.ts bekommt den Fetch + erweiterten Upsert-Body, die Anime-Seite erhaelt eine aufklappbare Versions-Detailebene (ReleaseVersionBreakdown), und das Leader-Formular bekommt das optionale, gruppen-gefilterte Dropdown. OpenAPI wird mitgepflegt.

Purpose: P67-SC2 (Anzeige) und P67-SC1 (Eingabe-UI). Setzt D-05/06/07 (Anzeige), D-08/09/10 (Eingabe) und D-11 (Umlaute) um.
Output: Dropdown-Endpunkt + Route, api.ts-Erweiterungen, ReleaseVersionBreakdown-Komponente + GroupContributionBlock-Integration + Test, Leader-Dropdown, OpenAPI/Typen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/67-release-episode-credits/67-CONTEXT.md
@.planning/phases/67-release-episode-credits/67-RESEARCH.md
@.planning/phases/67-release-episode-credits/67-PATTERNS.md
@.planning/phases/67-release-episode-credits/67-UI-SPEC.md

<interfaces>
Backend (aus Plan 02/03):
  repository.ListGroupReleaseVersionsForAnime(ctx, fansubGroupID, animeID int64) ([]GroupReleaseVersionOption, error)
    GroupReleaseVersionOption{ ReleaseVersionID int64 json:"release_version_id"; EpisodeNumber string json:"episode_number"; Version string json:"version" }
  PublicAnimeContributionGroup hat jetzt version_breakdown []ReleaseVersionBreakdownGroup
    ReleaseVersionBreakdownGroup{ release_version_id; episode_number; version; contributors []PublicContributorRow }
  FansubAnimeContributionsHandler bereits gebaut (main.go:357); CanForFansubGroup(MembersView) als Permission-Muster.

main.go-Routen (Z. 361-371): registerAdminRoutes(v1, authMiddleware, adminRouteHandlers{...animeContributionsHandler...}). Bestehende Contribution-Admin-Routen liegen in registerAdminRoutes.

Frontend-Analoge:
  api.ts getAdminFansubAnimeReleases (Z. 3857): getApiBaseUrl() + authorizedFetch(..., {authToken, cache:"no-store"}) + parseApiErrorPayload/ApiError.
  api.ts upsertAnimeContribution (Z. 6818): POST, headers Content-Type json, JSON.stringify(body), Typ UpsertAnimeContributionRequest.
  fansub.ts UpsertAnimeContributionRequest (Z. 604-613): fansub_group_member_id, role_codes, started_year, ended_year, note, is_public_on_anime_page, is_public_on_member_profile, status.
  GroupContributionBlock.tsx (68 Z.): props {group, expanded, onToggle}; roleChip/toggleButton/historicalLabel CSS; deutsche Labels "Alle anzeigen"/"weitere nicht oeffentlich".
  AnimeContributionsSection.tsx: expandedGroupId useState<number|null>; rendert GroupContributionBlock mit expanded/onToggle.
  AnimeContributionModal.tsx (Z. 56-58): rolesByMemberId/visibilityByMemberId/statusByMemberId als Record<number, ...>; upsert-Aufruf Z. 143-151.
  contributions.ts: PublicAnimeContribution (Z. 3-11), AnimeContributionGroup (Z. 34-42), MeAnimeContribution (Z. 50-63).

CLAUDE.md: alle user-facing Strings korrekte deutsche Umlaute; max 450 Zeilen/Datei; api.ts zentral; Domain-Typen in frontend/src/types/.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dropdown-Endpunkt (Backend-Handler + Route) + OpenAPI + Typen + api.ts</name>
  <files>backend/internal/handlers/fansub_contributions_release_versions_handler.go, backend/cmd/server/main.go, shared/contracts/openapi.yaml, frontend/src/types/contributions.ts, frontend/src/types/fansub.ts, frontend/src/lib/api.ts</files>
  <read_first>
    - backend/cmd/server/main.go (Z. 357-379: Handler-Konstruktion + registerAdminRoutes)
    - backend/internal/handlers/fansub_anime_contributions_handler.go (ListAnimeContributions: Permission-Muster CanForFansubGroup(MembersView), parseFansubID/parseAnimeIDParam, gin.H{"data":...})
    - backend/internal/repository/anime_contributions_release_lookup_repository.go (ListGroupReleaseVersionsForAnime-Signatur)
    - frontend/src/lib/api.ts (getAdminFansubAnimeReleases Z. 3857, upsertAnimeContribution Z. 6818)
    - frontend/src/types/fansub.ts (UpsertAnimeContributionRequest Z. 604)
    - frontend/src/types/contributions.ts (PublicAnimeContribution, AnimeContributionGroup)
    - shared/contracts/openapi.yaml (bestehende contribution-DTOs als Muster)
  </read_first>
  <action>
    Backend:
    1. Neuer Handler fansub_contributions_release_versions_handler.go (package handlers): Methode auf einem geeigneten Receiver (entweder neuer ReleaseVersionsLookupHandler mit animeContributionsRepo + permissionSvc, oder Methode am bestehenden FansubAnimeContributionsHandler — falls letzteres dessen Datei das 450-Limit reisst, NEUE Datei verwenden). Endpunkt-Logik analog ListAnimeContributions: parseFansubID + parseAnimeIDParam, CanForFansubGroup(MembersView), dann r.ListGroupReleaseVersionsForAnime, Antwort c.JSON(200, gin.H{"data": items}). Deutsche Fehlertexte mit Umlauten.
    2. main.go: Route GET /admin/fansubs/:id/anime/:animeId/release-versions registrieren (in registerAdminRoutes-Naehe der uebrigen contribution-Admin-Routen), authMiddleware vorgeschaltet.
    OpenAPI:
    3. release_version_id (nullable integer) am Contribution-Upsert-Request und an der Public-Contribution-Group (version_breakdown-Array mit release_version_id/episode_number/version/contributors) ergaenzen; neuen GET-Endpunkt /admin/fansubs/{id}/anime/{animeId}/release-versions dokumentieren (data: Liste {release_version_id, episode_number, version}).
    Frontend-Typen:
    4. contributions.ts: ReleaseVersionBreakdown{ release_version_id:number; episode_number:string; version:string; contributors:PublicAnimeContribution[] } und AnimeContributionGroup += version_breakdown: ReleaseVersionBreakdown[]. MeAnimeContribution += release_version_id: number | null. Neuer Typ FansubAnimeReleaseVersionOption{ release_version_id:number; episode_number:string; version:string }.
    5. fansub.ts: UpsertAnimeContributionRequest += release_version_id: number | null. ZUSAETZLICH den Read-Typ AnimeContribution (fansub.ts:581, Quelle des Modal-States via existingContributions) += release_version_id: number | null, damit der gespeicherte Wert beim Bearbeiten initialisiert werden kann (B2/D-10-Roundtrip). Backend liefert das Feld ab Plan 02 in AnimeContributionRow.
    api.ts:
    6. Neuer GET getFansubAnimeReleaseVersions(fansubId, animeId, authToken?) — Skelett von getAdminFansubAnimeReleases kopieren (getApiBaseUrl + authorizedFetch cache:"no-store" + parseApiErrorPayload/ApiError), Pfad /api/v1/admin/fansubs/${fansubId}/anime/${animeId}/release-versions, Rueckgabe { data: FansubAnimeReleaseVersionOption[] }.
    7. upsertAnimeContribution-Body-Typ traegt release_version_id automatisch ueber fansub.ts (Aenderung in Punkt 5); keine Signaturaenderung noetig.
  </action>
  <verify>
    <automated>cd backend && go build ./... && cd ../frontend && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... erfolgreich; neuer GET-Endpunkt in main.go registriert mit authMiddleware.
    - npm run typecheck fehlerfrei.
    - Endpunkt-Handler nutzt CanForFansubGroup(MembersView) (kein ungeschuetzter Lookup).
    - OpenAPI enthaelt release_version_id am Upsert-Request, version_breakdown an der Public-Group und den neuen Lookup-Endpunkt.
    - Betroffene Backend-Dateien <= 450 Zeilen.
  </acceptance_criteria>
  <done>Dropdown-Endpunkt ist erreichbar und permission-geschuetzt; api.ts/Typen/OpenAPI tragen release_version_id und die Lookup-Form.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReleaseVersionBreakdown-Komponente + GroupContributionBlock-Integration + Test</name>
  <files>frontend/src/components/anime/ReleaseVersionBreakdown.tsx, frontend/src/components/anime/ReleaseVersionBreakdown.module.css, frontend/src/components/anime/GroupContributionBlock.tsx, frontend/src/components/anime/GroupContributionBlock.test.tsx</files>
  <read_first>
    - frontend/src/components/anime/GroupContributionBlock.tsx (komplett: roleChip/historicalLabel/toggleButton, Contributor-Rendering)
    - frontend/src/components/anime/GroupContributionBlock.module.css (Klassen block/header/groupName/roleChip/toggleButton/historicalLabel)
    - frontend/src/components/anime/AnimeContributionsSection.tsx (expandedGroupId/onToggle Client-State-Muster)
    - frontend/src/types/contributions.ts (AnimeContributionGroup.version_breakdown, ReleaseVersionBreakdown aus Task 1)
    - .planning/phases/67-release-episode-credits/67-UI-SPEC.md (Copywriting + Interaction Contract)
  </read_first>
  <behavior>
    - Wenn group.version_breakdown leer ist, rendert kein "Nach Release-Version"-Trigger.
    - Wenn version_breakdown Eintraege hat, erscheint zuerst der "Allgemein an der Serie beteiligt:"-Block (anime-weite contributors), darunter ein aufklappbarer Trigger; nach dem Aufklappen werden Episode·Version-Gruppen mit Contributors + Rollen-Chips gerendert.
    - Reihenfolge der version_breakdown-Eintraege bleibt wie von der API geliefert (Episode -> Version).
  </behavior>
  <action>
    1. ReleaseVersionBreakdown.tsx (Client-Komponente): Props { breakdown: ReleaseVersionBreakdown[] }. Eigener useState<boolean> open + Toggle-Button mit toggleButton-Styling, Chevron-Praefix "▸"/"▾", Label "Nach Release-Version", aria-expanded + aria-controls. Bei open: pro Eintrag ein Episode·Version-Kopf `Episode {episode_number} · {version}` und darunter die contributors mit identischem roleChip-/historicalLabel-Markup wie GroupContributionBlock (Contributor-Liste wiederverwenden, ggf. kleine interne Helper-Funktion). Korrekte deutsche Umlaute in allen Strings (UI-SPEC Copywriting).
    2. ReleaseVersionBreakdown.module.css colocated; nutzt dieselben Tokens/Abstaende (16px Einrueckung der Versions-Beitraege, 8px zwischen Episode·Version-Gruppen, 12px Abstand Allgemein->Trigger; Accent fuer Trigger/Chips). Keine neuen Tokens (UI-SPEC Spacing/Color).
    3. GroupContributionBlock.tsx: ueber der bestehenden Contributor-Liste das Label "Allgemein an der Serie beteiligt:" einfuegen (nur wenn version_breakdown nicht leer ist — sonst bleibt die heutige flache Darstellung). Unter der Contributor-Liste <ReleaseVersionBreakdown breakdown={group.version_breakdown} /> rendern, wenn group.version_breakdown?.length > 0.
    4. GroupContributionBlock.test.tsx (Vitest): test-name enthaelt "GroupContributionBlock"; rendert eine Group mit version_breakdown und assertet: "Allgemein an der Serie beteiligt:" sichtbar, Trigger "Nach Release-Version" vorhanden, nach Klick Episode·Version-Kopf + Contributor sichtbar; und ein Fall ohne version_breakdown -> kein Trigger.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- GroupContributionBlock</automated>
  </verify>
  <acceptance_criteria>
    - npm run test -- GroupContributionBlock gruen.
    - ReleaseVersionBreakdown rendert nur bei nicht-leerem breakdown; Progressive Disclosure ist reiner Client-State.
    - aria-expanded/aria-controls am Trigger gesetzt.
    - Alle user-facing Strings mit korrekten Umlauten; keine neuen Spacing/Color-Tokens.
    - Beide Komponentendateien <= 450 Zeilen.
  </acceptance_criteria>
  <done>Anime-Seite zeigt anime-weit zuerst, dann aufklappbar versions-spezifisch (P67-SC2); Komponententest gruen.</done>
</task>

<task type="auto">
  <name>Task 3: Leader-Formular — gruppen-gefiltertes Release-Version-Dropdown</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionModal.tsx</files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionModal.tsx (komplett: Record-State-Muster Z. 56-58, upsert-Aufruf Z. 143-151, Member-Rendering)
    - frontend/src/lib/api.ts (getFansubAnimeReleaseVersions aus Task 1)
    - frontend/src/components/ui/Select.tsx und FormField.tsx (Primitives)
    - frontend/src/types/fansub.ts (UpsertAnimeContributionRequest mit release_version_id)
    - .planning/phases/67-release-episode-credits/67-UI-SPEC.md (Formular-Copywriting + Interaction Contract D-08/09/10)
  </read_first>
  <behavior>
    - Beim Oeffnen/Gruppenkontext laedt das Modal die gruppen-gefilterten Release-Versionen via getFansubAnimeReleaseVersions.
    - Pro Member ein optionales Select mit Default-Leeroption "— anime-weit lassen —" (Wert => release_version_id NULL) und Optionen `Episode {episode_number} · {version}`.
    - Beim Speichern wird release_version_id (oder null) im Upsert-Body mitgesendet.
    - Beim Bearbeiten eines bestehenden Beitrags zeigt das Select den aktuellen Wert; Zuruecksetzen auf Leeroption entfernt die Zuordnung (D-10).
    - 422 vom Backend -> Feldfehler unter dem Select (FormField error), korrekte Umlaute.
  </behavior>
  <action>
    1. State releaseVersionByMemberId: Record<number, number | null> analog zu statusByMemberId einfuehren; aus den bestehenden Beitraegen initialisieren — Quelle ist existingContributions vom Typ AnimeContribution (fansub.ts:581), dessen release_version_id-Feld in Plan 04 Task 1 ergaenzt wird (NICHT MeAnimeContribution; das ist der Member-Read-Typ).
    2. Beim Mount/Gruppenkontext getFansubAnimeReleaseVersions(fansubId, animeId) laden und Optionen in lokalem State halten; Fehlerzustand mit Text "Release-Versionen konnten nicht geladen werden. Bitte spaeter erneut versuchen." (UI-SPEC).
    3. Pro Member ein FormField + Select mit Label "Release-Version (optional)", Hint "Leer lassen = die Gruppe war allgemein an der Serie beteiligt.", Leeroption "— anime-weit lassen —". Bestehende ui-Primitives (Select, FormField) wiederverwenden, nicht nachbauen.
    4. upsert-Aufruf (Z. 143-151) um release_version_id: releaseVersionByMemberId[memberId] ?? null erweitern.
    5. 422-Antwort (ApiError status 422) als Feldfehler unter dem Select rendern mit Text "Diese Gruppe war an der gewaehlten Release-Version nicht beteiligt. Bitte eine andere Version waehlen oder anime-weit lassen." (UI-SPEC; korrekte Umlaute).
    Datei <= 450 Zeilen halten; falls die Datei das Limit reisst, einen Teil (z. B. die Select-Zeile als kleine Sub-Komponente) auslagern.
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck && npm run test -- AnimeContributionModal 2>/dev/null || npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - npm run typecheck fehlerfrei.
    - getFansubAnimeReleaseVersions wird aufgerufen; Optionen kommen serverseitig gefiltert (kein Client-Filter).
    - upsert-Body enthaelt release_version_id (Wert oder null).
    - Default-Leeroption "— anime-weit lassen —"; Label/Hint/Fehlertext mit korrekten Umlauten.
    - Datei <= 450 Zeilen.
  </acceptance_criteria>
  <done>Leader kann release_version_id optional setzen/aendern/entfernen; gefiltertes Dropdown und 422-Feldfehler funktionieren (P67-SC1 Eingabe-UI).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Browser-UAT — Anime-Seiten-Aufschluesselung und Leader-Dropdown</name>
  <what-built>Vollstaendige Versions-Dimension end-to-end: Docker-Rebuild (Backend nach Migration+Code, Frontend), oeffentliche Anime-Seite mit aufklappbarer Versions-Ebene und Leader-Formular mit gruppen-gefiltertem Dropdown.</what-built>
  <how-to-verify>
    Der Executor baut zuerst automatisiert: `docker compose build team4sv30-backend team4sv30-frontend && docker compose up -d`. Danach Mensch-UAT:
    1. Anime-Seite: ein Anime mit anime-weiten UND versions-spezifischen Beitraegen oeffnen. Pruefen: pro Gruppe zuerst "Allgemein an der Serie beteiligt:", darunter Trigger "▸ Nach Release-Version"; aufklappen zeigt Episode·Version-Koepfe sortiert nach Episode-Nr -> Version; ein Member, der anime-weit UND versions-spezifisch eingetragen ist, erscheint NICHT doppelt im Allgemein-Block.
    2. Leader-Formular (/admin/fansubs/[id]/edit, Anime-Beitraege): Release-Version-Dropdown listet nur Versionen der gewaehlten Gruppe; "— anime-weit lassen —" speichert NULL; eine gueltige Version speichern und auf der Anime-Seite verifizieren; danach wieder auf anime-weit zuruecksetzen (D-10).
    3. Negativfall (falls reproduzierbar): Versuch, eine gruppen-fremde Version zu setzen, zeigt den 422-Feldfehler.
  </how-to-verify>
  <resume-signal>Tippe "approved" wenn beide Formulare und die Anime-Seiten-Aufschluesselung wie beschrieben funktionieren, oder beschreibe die Abweichung.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated Leader -> Dropdown-Lookup-API | Leader fragt gruppen-gefilterte Release-Versionen ab |
| Public-API -> anonymer Anime-Seiten-Leser | Versions-Ebene darf nur oeffentliche Beitraege zeigen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-04-AUTH | Elevation | Dropdown-Lookup-Endpunkt | mitigate | authMiddleware + CanForFansubGroup(MembersView) vorgeschaltet. |
| T-67-04-ID | Information Disclosure | Versions-Ebene auf Anime-Seite | mitigate | Daten kommen aus der Public-Query (Plan 03) mit Public-Filtern; Frontend rendert nur geliefertes version_breakdown. |
| T-67-04-CLIENTFILTER | Tampering | Dropdown-Optionen | mitigate | Filterung serverseitig via release_version_groups (kein Client-Filter, Anti-Pattern vermieden). |
| T-67-04-XSS | (Sicherheit) | gerenderte Strings | accept | React escaped Text-Knoten standardmaessig; keine dangerouslySetInnerHTML. |
</threat_model>

<verification>
- `cd backend && go build ./...` gruen; neuer Endpunkt registriert.
- `cd frontend && npm run typecheck` fehlerfrei.
- `npm run test -- GroupContributionBlock` gruen.
- Docker-Rebuild + Browser-UAT (Checkpoint) bestanden.
</verification>

<success_criteria>
- P67-SC2 erfuellt: Anime-Seite zeigt Contributions aufgeschluesselt nach Release-Version (aufklappbar, sortiert, anime-weit zuerst).
- P67-SC1 Eingabe-UI erfuellt: beide Eingabeflaechen koennen das optionale, gruppen-gefilterte release_version_id setzen/aendern/entfernen.
</success_criteria>

<output>
Create `.planning/phases/67-release-episode-credits/67-04-SUMMARY.md` when done
</output>
