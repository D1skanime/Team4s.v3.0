---
phase: 59-ffentliches-fansub-member-profil
plan: 06
type: execute
wave: 4
depends_on:
  - 59-04
  - 59-05
files_modified:
  - shared/contracts/openapi.yaml
autonomous: true
requirements:
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
must_haves:
  truths:
    - "GET /api/v1/members/{slug} ist in openapi.yaml dokumentiert mit PublicMemberProfileResponse-Schema"
    - "POST /api/v1/me/profile/background ist in openapi.yaml dokumentiert"
    - "PublicMemberProfileResponse unterscheidet visible=false von Profildaten"
  artifacts:
    - path: "shared/contracts/openapi.yaml"
      provides: "GET /api/v1/members/{slug} und POST /api/v1/me/profile/background dokumentiert"
  key_links:
    - from: "shared/contracts/openapi.yaml"
      to: "GET /api/v1/members/{slug}"
      via: "paths definition"
      pattern: "/members/{slug}"
---

<objective>
OpenAPI-Contract für die zwei neuen Endpoints (GET /api/v1/members/{slug} und POST /api/v1/me/profile/background) dokumentieren.

Purpose: Hält shared/contracts/openapi.yaml als Single-Source-of-Truth aktuell; verhindert Contract-Drift zwischen Backend-Implementation und Dokumentation.
Output: Zwei neue Endpoint-Definitionen + PublicMemberProfileResponse-Schema in openapi.yaml.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/PROJECT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/59-ffentliches-fansub-member-profil/59-CONTEXT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/59-ffentliches-fansub-member-profil/59-04-SUMMARY.md
@C:/Users/admin/Documents/Team4s/.planning/phases/59-ffentliches-fansub-member-profil/59-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: OpenAPI-Contract für beide neuen Endpoints</name>
  <files>shared/contracts/openapi.yaml</files>
  <read_first>
    - shared/contracts/openapi.yaml — bestehende Endpoint-Definitionen für /me/profile (GET, PATCH) und /me/profile/avatar (POST) als Muster für Struktur; bestehende Schema-Definitionen (components/schemas-Abschnitt)
    - docs/api/api-contracts.md — Contract-Workflow-Regeln
  </read_first>
  <action>
    Zwei neue Einträge im paths-Abschnitt:

    GET /api/v1/members/{slug}:
    - Parameter: slug (path, required, string)
    - Security: optional (bearerAuth, leer Array als Alternative — analog zu anderen public Endpoints)
    - Responses:
      - 200: oneOf[ { $ref: '#/components/schemas/PublicMemberProfileEnvelope' }, { $ref: '#/components/schemas/MemberProfileHidden' } ]
      - 404: { error: { message: string } }
    - Tag: "member-profiles" (neuer Tag oder bestehender)

    POST /api/v1/me/profile/background:
    - Security: bearerAuth (required)
    - requestBody: multipart/form-data mit background-Feld (type: string, format: binary)
    - Responses:
      - 200: { data: { $ref: '#/components/schemas/MemberProfile' } }
      - 400/401/500 analog zum Avatar-Endpoint

    Neue Schemas in components/schemas:
    - PublicMemberProfileEnvelope: { data: { $ref: '#/components/schemas/PublicMemberProfileData' } }
    - MemberProfileHidden: { visible: boolean (false), reason: string }
    - PublicMemberProfileData: properties member_id, fansub_name, bio, member_story_html, active_from_date, active_until_date, is_currently_active, profile_visibility, avatar (nullable), background_image (nullable, {public_url: string}), memberships (array), recent_media (array), recent_contributions (array) — kein display_name, email, keycloak_subject
    - MemberProfileMembership (falls noch nicht vorhanden): fansub_group_id, fansub_group_name, fansub_group_slug, app_member_roles

    MemberProfile-Schema: background_image-Feld hinzufügen falls noch nicht vorhanden.

    YAML-Format: Einrückung und Stil exakt wie bestehende Abschnitte — kein Tab, 2-Space-Einrückung.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s && grep -c "/members/{slug}" shared/contracts/openapi.yaml</automated>
  </verify>
  <done>grep "/members/{slug}" findet den neuen Endpoint. grep "MemberProfileHidden" findet das neue Schema. grep "me/profile/background" findet den Upload-Endpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contract → Consumer | YAML-Datei; kein Sicherheitsrisiko |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-11 | Information Disclosure | PublicMemberProfileData Schema | mitigate | Schema enthält explizit kein display_name/email/keycloak_subject (D-08) |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | Kein neues Paket |
</threat_model>

<verification>
1. grep "/members/{slug}" in openapi.yaml findet den Endpoint
2. grep "MemberProfileHidden" findet das Schema mit visible: false
3. grep "me/profile/background" findet den POST-Endpoint
4. PublicMemberProfileData-Schema enthält kein display_name
</verification>

<success_criteria>
- GET /api/v1/members/{slug} in openapi.yaml mit korrekten Responses (inkl. visible=false-Zweig)
- POST /api/v1/me/profile/background in openapi.yaml
- PublicMemberProfileData-Schema ohne Keycloak-Felder
- YAML-Syntax valide (korrekte Einrückung, keine Duplikat-Keys)
</success_criteria>

<output>
Create `.planning/phases/59-ffentliches-fansub-member-profil/59-06-SUMMARY.md` when done
</output>
