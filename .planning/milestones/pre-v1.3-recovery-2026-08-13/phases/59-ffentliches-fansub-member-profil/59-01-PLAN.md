---
phase: 59-ffentliches-fansub-member-profil
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/0080_member_profile_background.up.sql
  - database/migrations/0080_member_profile_background.down.sql
  - backend/internal/models/member_profile.go
  - frontend/src/types/profile.ts
autonomous: true
requirements:
  - D-09
  - D-08
must_haves:
  truths:
    - "Die Spalte background_media_id existiert auf der members-Tabelle"
    - "Das PublicMemberProfile-Go-Modell enthält keine Keycloak-Felder"
    - "Das TypeScript-Interface PublicMemberProfileData ist exportiert und enthält background_image"
  artifacts:
    - path: "database/migrations/0080_member_profile_background.up.sql"
      provides: "ALTER TABLE members ADD COLUMN background_media_id"
    - path: "backend/internal/models/member_profile.go"
      provides: "PublicMemberProfile struct + MemberProfileBgImage struct"
    - path: "frontend/src/types/profile.ts"
      provides: "PublicMemberProfileData interface + PublicMemberProfileResponse union type"
  key_links:
    - from: "database/migrations/0080_member_profile_background.up.sql"
      to: "members table"
      via: "ALTER TABLE"
      pattern: "ALTER TABLE members ADD COLUMN"
    - from: "backend/internal/models/member_profile.go"
      to: "PublicMemberProfile"
      via: "Go struct export"
      pattern: "type PublicMemberProfile struct"
---

<objective>
DB-Migration für Hintergrundbild-Spalte sowie Backend- und Frontend-Typdefinitionen für das öffentliche Profil anlegen.

Purpose: Schafft die Datenbasis für alle weiteren Pläne dieser Phase — Migration, Go-Modell und TypeScript-Interface müssen zuerst existieren.
Output: Migration 0080, erweitertes Go-Modell, neues TypeScript-Interface ohne Keycloak-Felder.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/PROJECT.md
@C:/Users/admin/Documents/Team4s/.planning/ROADMAP.md
@C:/Users/admin/Documents/Team4s/.planning/STATE.md

@C:/Users/admin/Documents/Team4s/.planning/phases/59-ffentliches-fansub-member-profil/59-CONTEXT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/59-ffentliches-fansub-member-profil/59-RESEARCH.md

<interfaces>
Aus database/migrations/0079_member_profile_activity_dates.up.sql (Referenz-Pattern):
  ALTER TABLE members ADD COLUMN IF NOT EXISTS active_from_date DATE;
  ALTER TABLE members ADD COLUMN IF NOT EXISTS active_until_date DATE;

Aus backend/internal/models/member_profile.go (bestehende Structs):
  type MemberProfile struct { ... } -- enthält AppUserID, DisplayName, Email, KeycloakSubject
  type MemberProfileAvatar struct { PublicURL string; SourceOriginalURL *string }
  type ProfileVisibility = string
  const ProfileVisibilityPublic = "public"
  const ProfileVisibilityMembersOnly = "members_only"
  type MemberProfileMembership struct { FansubGroupID, FansubGroupName, FansubGroupSlug, AppMemberRoles []string }
  type MemberProfileRecentMedia struct { ... }
  type MemberProfileRecentContribution struct { ... }

Aus frontend/src/types/profile.ts (bestehende Typen):
  export interface MemberProfileData { ... }  // enthält display_name, email, keycloak_subject
  export type ProfileVisibility = 'public' | 'members_only'
  export interface MemberProfileMembership { fansub_group_id, fansub_group_name, fansub_group_slug, app_member_roles }
  export interface MemberProfileRecentMedia { ... }
  export interface MemberProfileRecentContribution { ... }
  export type MemberProfileResponse = { data: MemberProfileData }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB-Migration 0080 — background_media_id auf members</name>
  <files>database/migrations/0080_member_profile_background.up.sql, database/migrations/0080_member_profile_background.down.sql</files>
  <read_first>
    - database/migrations/0079_member_profile_activity_dates.up.sql — ALTER TABLE members Muster
    - database/migrations/0079_member_profile_activity_dates.down.sql — DOWN-Muster
    - database/migrations/0077_member_profiles_mvp.up.sql — Referenz für members-Tabelle und media-FK-Muster
  </read_first>
  <action>
    UP-Migration: ALTER TABLE members ADD COLUMN IF NOT EXISTS background_media_id UUID REFERENCES media(id) ON DELETE SET NULL;

    DOWN-Migration: ALTER TABLE members DROP COLUMN IF EXISTS background_media_id;

    Kein Index nötig (nicht sortiert/gefiltert). Kein NOT NULL-Constraint — Spalte ist optional.
  </action>
  <verify>
    <automated>grep -c "background_media_id" C:/Users/admin/Documents/Team4s/database/migrations/0080_member_profile_background.up.sql</automated>
  </verify>
  <done>Beide Migrationsdateien existieren. UP enthält "ADD COLUMN IF NOT EXISTS background_media_id UUID REFERENCES media(id) ON DELETE SET NULL". DOWN enthält "DROP COLUMN IF EXISTS background_media_id".</done>
</task>

<task type="auto">
  <name>Task 2: Go-Modell PublicMemberProfile + MemberProfileBgImage</name>
  <files>backend/internal/models/member_profile.go</files>
  <read_first>
    - backend/internal/models/member_profile.go — vollständige Datei lesen; bestehende Structs, ProfileVisibility-Konstanten und MemberProfileAvatar-Struct
  </read_first>
  <action>
    Zwei neue Structs am Ende der Datei anhängen (nach bestehenden Typen):

    MemberProfileBgImage: Feld PublicURL string mit json-Tag "public_url".

    PublicMemberProfile: Felder MemberID int64, FansubName string, Bio *string, MemberStoryHTML *string, ActiveFromDate *string, ActiveUntilDate *string, IsCurrentlyActive bool, ProfileVisibility string, Avatar *MemberProfileAvatar, BackgroundImage *MemberProfileBgImage, Memberships []MemberProfileMembership, RecentMedia []MemberProfileRecentMedia, RecentContributions []MemberProfileRecentContribution.

    Alle Felder mit json-Tags versehen (snake_case). Pointer-Felder bekommen "omitempty". MemberID, FansubName, IsCurrentlyActive, ProfileVisibility sind keine Pointer.

    Wichtig: Kein AppUserID-, DisplayName-, Email-, KeycloakSubject-Feld im PublicMemberProfile-Struct — per D-08.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go build ./internal/models/...</automated>
  </verify>
  <done>go build ./internal/models/... ohne Fehler. grep "PublicMemberProfile" zeigt die neue Struct. grep "BackgroundImage" und grep "AppUserID" — letzteres darf im PublicMemberProfile-Struct nicht vorkommen.</done>
</task>

<task type="auto">
  <name>Task 3: TypeScript-Interface PublicMemberProfileData + PublicMemberProfileResponse</name>
  <files>frontend/src/types/profile.ts</files>
  <read_first>
    - frontend/src/types/profile.ts — vollständige Datei lesen; MemberProfileData-Interface (enthält alle Felder inkl. Keycloak), MemberProfileMembership, MemberProfileRecentMedia, MemberProfileRecentContribution, ProfileVisibility
  </read_first>
  <action>
    Am Ende der Datei zwei neue Exporte anhängen:

    PublicMemberProfileData interface — Felder: member_id: number, fansub_name: string, bio?: string | null, member_story_html?: string | null, active_from_date?: string | null, active_until_date?: string | null, is_currently_active: boolean, profile_visibility: ProfileVisibility, avatar?: { public_url: string; source_original_url?: string | null } | null, background_image?: { public_url: string } | null, memberships: MemberProfileMembership[], recent_media: MemberProfileRecentMedia[], recent_contributions: MemberProfileRecentContribution[].

    Kein display_name, email, keycloak_subject-Feld — per D-08.

    PublicMemberProfileResponse union type: { data: PublicMemberProfileData } | { visible: false; reason: string }

    Beide als named exports.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>tsc --noEmit ohne Fehler für die geänderte Datei. grep "PublicMemberProfileData" und grep "PublicMemberProfileResponse" zeigen beide Exporte. grep "display_name" in den neuen Interfaces nicht vorhanden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → Postgres | SQL wird direkt ausgeführt; kein Benutzereingabe-Risiko |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | Information Disclosure | PublicMemberProfile Go-Struct | mitigate | Kein AppUserID/Email/KeycloakSubject-Feld im Struct — D-08 durch Typ-Design erzwungen |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | Kein neues Paket in diesem Plan |
</threat_model>

<verification>
1. `go build ./internal/models/...` ohne Fehler
2. `npx tsc --noEmit` ohne Fehler
3. Beide Migrationsdateien 0080 vorhanden
4. PublicMemberProfile enthält kein AppUserID/Email-Feld
</verification>

<success_criteria>
- Migration 0080 up/down vorhanden und korrekt strukturiert
- PublicMemberProfile-Struct kompiliert ohne Fehler
- PublicMemberProfileData-Interface exportiert, ohne Keycloak-Felder
- PublicMemberProfileResponse union type exportiert
</success_criteria>

<output>
Create `.planning/phases/59-ffentliches-fansub-member-profil/59-01-SUMMARY.md` when done
</output>
