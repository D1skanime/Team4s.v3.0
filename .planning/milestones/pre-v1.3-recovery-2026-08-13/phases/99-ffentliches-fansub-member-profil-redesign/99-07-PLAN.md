---
phase: 99
plan: "07"
type: execute
wave: 5
depends_on: []
files_modified:
  - "backend/internal/repository/release_detail_public_repository.go"
  - "backend/internal/repository/release_detail_public_repository_test.go"
  - "backend/internal/handlers/group_contributors_handler.go"
  - "backend/cmd/server/main.go"
  - "shared/contracts/openapi.yaml"
  - "frontend/src/lib/api.ts"
  - "frontend/src/types/releaseDetail.ts"
autonomous: true
requirements_addressed:
  - "AO4-02"
  - "AO4-05"
requirements:
  - "Phase 99 Add-on 4 AO4-02, AO4-05"
must_haves:
  truths:
    - "AO4-02: Ein oeffentlicher Endpoint GET /api/v1/anime/:id/group/:groupId/releases/:releaseVersionId liefert zu einer release_version_id Kopf-Kennzahlen (Anzahl Bilder, Texte, Beteiligte, Veroeffentlichungsdatum), Beteiligte (Name+Rolle), Bilder und Texte."
    - "AO4-02: Aggregationseinheit ist release_versions (nicht fansub_releases); Bilder aus release_version_media, Texte aus release_version_notes, Beteiligte aus anime_contributions ueber release_version_id."
    - "AO4-02: Nur oeffentlich sichtbare, freigegebene Inhalte werden ausgeliefert (gleiche Sichtbarkeits-Gates wie die bestehenden Public-Reads)."
    - "AO4-05: Jedes Bild traegt seine category (screenshot|typesetting_karaoke|fun_outtake|other) im Payload, damit das Frontend die deutschen Typ-Labels rendern kann."
  artifacts:
    - path: "backend/internal/repository/release_detail_public_repository.go"
      provides: "Aggregierender Public-Release-Read"
      exports: ["GetPublicReleaseDetail"]
    - path: "frontend/src/lib/api.ts"
      provides: "getGroupReleaseDetail Client-Funktion"
      contains: "getGroupReleaseDetail"
    - path: "frontend/src/types/releaseDetail.ts"
      provides: "TS-Typen fuer das Release-Detail-Payload"
  key_links:
    - from: "backend/cmd/server/main.go"
      to: "GET /anime/:id/group/:groupId/releases/:releaseVersionId"
      via: "Route-Registrierung + Handler"
      pattern: "releases/:releaseVersionId"
---

<objective>
Baue den aggregierenden oeffentlichen Release-Endpoint (AO4-02). Er existiert noch nicht und ist die Datenquelle fuer die eingebettete Release-Vorschau (AO4-11) und die neue Release-Detailseite (AO4-15..20).

Purpose: Eine einzige `release_version_id` soll Kopf-Kennzahlen, Beteiligte, Bilder und Texte liefern, aggregiert ueber `release_versions`. Kein Missbrauch der Legacy-`/releases/:id/images`-Endpoints (die behandeln `:id` als episode_version_id).

Output: Neues Repository, Handler-Methode + Route, OpenAPI-Contract, `api.ts`-Funktion und TS-Typen.
</objective>

<context>
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON4-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggregierendes Public-Release-Repository</name>
  <files>backend/internal/repository/release_detail_public_repository.go, backend/internal/repository/release_detail_public_repository_test.go</files>
  <read_first>
    - `backend/internal/repository/release_version_media_repository.go:196-269` — `ListReleaseVersionMedia` (Bilder pro release_version_id, category, Sichtbarkeits-Join media_assets/visibilities/review_statuses, media_files fuer thumb/original).
    - `backend/internal/repository/group_release_media_repository.go` — bestehendes Public-Sichtbarkeits-Gate (v.name='public', rs.code='approved', ma.status='ready') und `publicMediaURLForPath`-Nutzung als Referenz.
    - `backend/internal/repository/release_version_notes_repository.go:13-116` — `ReleaseVersionNote`-Struct (member_id, role_id, body_html, visibility, status, created_at) und `ListReleaseVersionNotes`.
    - `backend/internal/repository/anime_contributions_release_lookup_repository.go` — Muster fuer release_version_id-gebundene Queries + `GroupParticipatesInReleaseVersion`.
    - `backend/internal/repository/group_themes_repository.go:108-111` — `publicMediaURLForPath(path, storageDir)` Nutzung.
  </read_first>
  <action>
    Erstelle `release_detail_public_repository.go` mit einem Repo-Struct (db-Pool + mediaStorageDir, Konstruktor analog `NewGroupThemesRepository`) und der Methode `GetPublicReleaseDetail(ctx, animeID, groupID, releaseVersionID int64)`. Ablauf:
    1. Ownership/Scope pruefen: die release_version muss zur Gruppe+Anime gehoeren (JOIN release_version_groups + fansub_releases + episodes auf anime_id/fansub_group_id). Trifft nichts zu, gib `ErrNotFound` zurueck.
    2. Kopf: Veroeffentlichungsdatum aus `release_versions.release_date` (Fallback `fansub_releases.release_date`), Episode-Nummer/Titel.
    3. Beteiligte: Name + Rolle aus `anime_contributions` WHERE release_version_id = $ (Rollen ueber die vorhandene contribution-role-Aufloesung; Rollen-Label deutsch wie in domain_projection). Nur oeffentliche Eintraege.
    4. Bilder: aus `release_version_media` mit demselben Sichtbarkeits-Gate wie `group_release_media_repository` (public/approved/ready), inkl. `category`, thumb+original-URL via `publicMediaURLForPath`. Sortierung `sort_order ASC, id ASC`.
    5. Texte: aus `release_version_notes` mit oeffentlichem Gate (bestaetige die exakten visibility/status-Literale aus dem notes-Repo bzw. der Migration; nutze das Gegenstueck zu 'oeffentlich'/freigegeben), Felder member_id/-name, role_label, body_html, created_at. Kein Body-Text-Leak interner Notizen.
    6. Kopf-Kennzahlen `images_count`, `notes_count`, `contributors_count` als COUNT der oeffentlich sichtbaren Mengen (nicht der geladenen Teilmenge).
    DTOs mit snake_case json-Tags. Halte die Datei <=450 Zeilen; lagere bei Bedarf Sub-Reads (z. B. Bilder/Texte/Beteiligte) in eine `release_detail_public_repository_helpers.go` aus.

    Test `release_detail_public_repository_test.go`: Empty-/NotFound-Fall (nicht existierende release_version_id liefert ErrNotFound ohne Panik) plus Source-Assertion der Sichtbarkeits-Gates.
  </action>
  <acceptance_criteria>
    - Exportierte Methode `GetPublicReleaseDetail(ctx, animeID, groupID, releaseVersionID int64) (*PublicReleaseDetail, error)` existiert.
    - DTO-Felder enthalten `images_count`, `notes_count`, `contributors_count`, `release_date`, `contributors`, `images` (mit `category`), `notes`.
    - Bild-Query enthaelt `release_version_media` und das Gate `v.name = 'public'`/`rs.code = 'approved'`/`ma.status = 'ready'`.
    - `cd backend; go build ./...` fehlerfrei; `go test ./internal/repository -run "ReleaseDetail"` gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend; go build ./...</automated>
    <automated>cd C:/Users/admin/Documents/Team4s/backend; go test ./internal/repository -run "ReleaseDetail"</automated>
  </verify>
  <done>Repository liefert aggregiertes, oeffentlich gegatetes Release-Detail-Payload.</done>
</task>

<task type="auto">
  <name>Task 2: Handler-Methode, Route und OpenAPI</name>
  <files>backend/internal/handlers/group_contributors_handler.go, backend/cmd/server/main.go, shared/contracts/openapi.yaml</files>
  <read_first>
    - `backend/internal/handlers/group_contributors_handler.go:12-103` — `GroupPublicHandler`-Struct/Konstruktor und die Muster von `GetGroupThemes`/`GetGroupReleaseMedia` (parseAnimeID/parseGroupID, internalError).
    - `backend/cmd/server/main.go:280-356` — Repo-/Handler-Konstruktion (`groupPublicHandler`) und Public-Route-Block `/anime/:id/group/:groupId/...` (Zeilen 350-356).
    - `shared/contracts/openapi.yaml` — bestehende Pfade `/anime/{id}/group/{groupId}/themes` und `/release-media` als Vorlage.
  </read_first>
  <action>
    Ergaenze `GroupPublicHandler` um ein Feld `releaseDetailRepo *repository.ReleaseDetailPublicRepository` und eine Builder-Methode `WithReleaseDetailRepo(repo) *GroupPublicHandler` (bestehende Konstruktor-Signatur unveraendert lassen, um Aufruf-Churn zu vermeiden). Neue Handler-Methode `GetGroupReleaseDetail(c *gin.Context)`: parse `id`, `groupId` und `releaseVersionId` (neuer Path-Param), rufe `GetPublicReleaseDetail`; bei `ErrNotFound` HTTP 404 mit generischer deutscher Meldung (keine technische Fehlermeldung, AO4-14-Geist), sonst 200 mit dem Payload.

    In `main.go`: Konstruiere `repository.NewReleaseDetailPublicRepository(dbPool, cfg.MediaStorageDir)`, haenge sie via `.WithReleaseDetailRepo(...)` an `groupPublicHandler` und registriere `v1.GET("/anime/:id/group/:groupId/releases/:releaseVersionId", groupPublicHandler.GetGroupReleaseDetail)` NACH der bestehenden `/releases`-Route (Gin: Literal- vor Param-Reihenfolge beachten, damit `/releases` und `/releases/:releaseVersionId` kollisionsfrei matchen).

    In `openapi.yaml`: neuen Pfad mit den Response-Feldern aus Task 1 dokumentieren (deutsche Beschreibungen).
  </action>
  <acceptance_criteria>
    - `grep -n "releases/:releaseVersionId" backend/cmd/server/main.go` liefert genau die neue GET-Route.
    - `GetGroupReleaseDetail` existiert in `group_contributors_handler.go` und liefert 404 bei ErrNotFound.
    - `openapi.yaml` enthaelt den Pfad `/anime/{id}/group/{groupId}/releases/{releaseVersionId}`.
    - `cd backend; go build ./...` fehlerfrei.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend; go build ./...</automated>
    <automated>cd C:/Users/admin/Documents/Team4s/backend; go vet ./internal/handlers ./cmd/server</automated>
  </verify>
  <done>Route registriert, Handler wired, Contract dokumentiert.</done>
</task>

<task type="auto">
  <name>Task 3: api.ts-Funktion und TS-Typen</name>
  <files>frontend/src/lib/api.ts, frontend/src/types/releaseDetail.ts</files>
  <read_first>
    - `frontend/src/lib/api.ts:6152-6192` — `getGroupThemes`/`getGroupReleaseMedia` als Vorlage (getApiBaseUrl, authorizedFetch/fetch, parseApiError, ApiError).
    - `frontend/src/types/releaseVersionMedia.ts:1-27` — `ReleaseVersionMediaCategory` + `CATEGORY_LABELS` (zur Wiederverwendung fuer Typ-Tags).
  </read_first>
  <action>
    Erstelle `frontend/src/types/releaseDetail.ts` mit Interfaces, die das Payload aus Task 1 exakt spiegeln (snake_case Felder): `ReleaseDetailResponse` mit `images_count`, `notes_count`, `contributors_count`, `release_date`, `contributors[]` (name, role_label, avatar_url), `images[]` (id, category als `ReleaseVersionMediaCategory`, thumbnail_url, original_url, author/uploader-Anzeige, caption), `notes[]` (id, member_name, role_label, body_html, created_at). Verwende den bestehenden `ReleaseVersionMediaCategory`-Typ fuer `category`.

    Ergaenze in `api.ts` `getGroupReleaseDetail(animeID, groupID, releaseVersionID)`, analog zu `getGroupReleaseMedia` (gleiche Fehlerbehandlung), URL `/api/v1/anime/${animeID}/group/${groupID}/releases/${releaseVersionID}`.
  </action>
  <acceptance_criteria>
    - `getGroupReleaseDetail` ist in `api.ts` exportiert und ruft den korrekten Pfad.
    - `releaseDetail.ts` nutzt `ReleaseVersionMediaCategory` fuer `images[].category`.
    - `cd frontend; npm run typecheck` fehlerfrei.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run typecheck</automated>
  </verify>
  <done>Client-Funktion und Typen bereit fuer AO4-11 und AO4-15..20.</done>
</task>

</tasks>

<verification>
- `go build`, `go vet`, `go test ReleaseDetail`, `npm run typecheck`.
- Backend nach Commit neu bauen (`docker compose up -d --build team4sv30-backend`) — neue Route erscheint sonst als 404.
- Live-Check mit Direct-Grant-Token gegen den Host-Backend-Port (Memory: Live-API-Verifikation per Token).
</verification>

<success_criteria>
- Ein Aufruf mit gueltiger release_version_id liefert Kennzahlen, Beteiligte, Bilder (mit category) und Texte; ungueltige ID liefert 404 ohne technische Meldung.
</success_criteria>

<output>
Erstelle `.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-07-SUMMARY.md` nach Abschluss.
</output>
