---
phase: 99
plan: "13"
type: execute
wave: 9
depends_on:
  - "99-08"
  - "99-12"
files_modified:
  - "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseGallery.tsx"
  - "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseGallery.module.css"
  - "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseNotesList.tsx"
  - "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseNotesList.module.css"
  - "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.tsx"
autonomous: true
requirements_addressed:
  - "AO4-18"
  - "AO4-19"
  - "AO4-05"
  - "AO4-21"
  - "AO4-22"
  - "AO4-23"
  - "AO4-24"
  - "AO4-25"
requirements:
  - "Phase 99 Add-on 4 AO4-18, AO4-19, AO4-05, AO4-21, AO4-22, AO4-23, AO4-24, AO4-25"
must_haves:
  truths:
    - "AO4-18: Die vollstaendige Bildergalerie erscheint als Grid; pro Bild sind Typ-Tag (AO4-05) und Autor-Chip sichtbar; Nachladen via Infinite Scroll (Cursor) plus 'Mehr laden'-Button."
    - "AO4-19: Die vollstaendige Textliste erscheint als Karten mit Avatar, Name, Zeitpunkt und Inhalt; Nachladen via Infinite Scroll (Cursor) plus 'Mehr laden'-Button."
    - "AO4-05: Typ-Tags stammen aus den vier bestehenden Enum-Werten (screenshot|typesetting_karaoke|fun_outtake|other) mit deutschen Labels; keine Datenmodell-Aenderung."
    - "AO4-21: Infinite Scroll wird nur fuer Galerie und Textliste genutzt."
    - "AO4-22: Galerie-Bilder und Avatare nutzen loading='lazy' und Skeleton/feste Aspect-Ratio-Boxen (kein Layout-Sprung)."
    - "AO4-23: Galerie-Bilder liefern srcset/sizes."
    - "AO4-24: Galerie und Textliste laden cursor-basiert (nicht offset)."
    - "AO4-25: Zusaetzlich zum automatischen Nachladen existiert ein 'Mehr laden'-Button."
  artifacts:
    - path: "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseGallery.tsx"
      provides: "Galerie-Grid mit Typ-Tag + Autor, Cursor-Infinite-Scroll"
      contains: "Mehr laden"
    - path: "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseNotesList.tsx"
      provides: "Textliste als Karten, Cursor-Infinite-Scroll"
      contains: "Mehr laden"
  key_links:
    - from: "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseGallery.tsx"
      to: "getGroupReleaseImages"
      via: "Cursor-Bild-Endpoint (AO4-24)"
      pattern: "getGroupReleaseImages"
    - from: "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseNotesList.tsx"
      to: "getGroupReleaseNotes"
      via: "Cursor-Text-Endpoint (AO4-24)"
      pattern: "getGroupReleaseNotes"
---

<objective>
Ergaenze die Release-Detailseite um die vollstaendige Bildergalerie (AO4-18) und Textliste (AO4-19), jeweils mit Cursor-Infinite-Scroll, Lazy Loading, Skeletons, srcset und 'Mehr laden'-Fallback.

Purpose: Vollstaendige, performante Darstellung aller Release-Bilder (mit Typ-Tag + Autor) und aller Textbeitraege (mit Autor + Zeitpunkt) ohne Layout-Spruenge.

Output: ReleaseGallery, ReleaseNotesList und Verdrahtung in der Detailroute.
</objective>

<context>
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON4-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReleaseGallery (Grid, Typ-Tag + Autor, Cursor-Infinite-Scroll)</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseGallery.tsx, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseGallery.module.css</files>
  <read_first>
    - `frontend/src/lib/api.ts` — `getGroupReleaseImages` (99-08) und `CursorPage<T>` aus `releaseDetail.ts`.
    - `frontend/src/types/releaseVersionMedia.ts:1-27` — `ReleaseVersionMediaCategory` + `CATEGORY_LABELS` (deutsche Typ-Labels: Screenshot/Typesetting/Fun/Sonstiges).
    - `frontend/src/types/releaseDetail.ts` — Bild-Item-Felder (id, category, thumbnail_url, original_url, author/uploader, caption).
    - Design-System `@/components/ui` (Button Pflicht fuer 'Mehr laden').
  </read_first>
  <action>
    Erstelle eine Client-Komponente ('use client') `ReleaseGallery`. Rendere ein Bild-Grid; erste Seite via Prop oder initialem `getGroupReleaseImages`-Aufruf, weitere Seiten via Cursor (`next_cursor`/`has_more`). Pro Bild: Vorschaubild in fester Aspect-Ratio-Box (`loading="lazy"`, `sizes`, `srcSet` fuer mobile Aufloesungen), sichtbarer Typ-Tag ueber `CATEGORY_LABELS[item.category]` (AO4-05) und ein Autor-Chip (Uploader/Autor-Name). Nachladen: automatisch per `IntersectionObserver` am Grid-Ende UND `@/components/ui` `Button` "Mehr laden" (AO4-25). Waehrend des Ladens Skeleton-Kacheln mit fester Groesse (kein Layout-Sprung, AO4-22). Nur Team4s-Tokens; keine nativen `<button>`; Umlaute korrekt; Datei <=450 Zeilen.
  </action>
  <acceptance_criteria>
    - Grid nutzt `getGroupReleaseImages` cursor-basiert; bei `has_more` sowohl IntersectionObserver-Auto-Load als auch sichtbarer "Mehr laden"-Button.
    - Pro Bild sichtbarer Typ-Tag aus `CATEGORY_LABELS` und Autor-Chip.
    - Bilder haben `loading="lazy"`, `sizes` und `srcSet`; feste Aspect-Ratio-Box im CSS.
    - `cd frontend; npm run typecheck` fehlerfrei.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run typecheck</automated>
  </verify>
  <done>Galerie zeigt alle Bilder mit Typ-Tag+Autor, laedt cursor-basiert nach.</done>
</task>

<task type="auto">
  <name>Task 2: ReleaseNotesList (Karten, Cursor-Infinite-Scroll)</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseNotesList.tsx, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseNotesList.module.css</files>
  <read_first>
    - `frontend/src/lib/api.ts` — `getGroupReleaseNotes` (99-08) und `CursorPage<T>`.
    - `frontend/src/types/releaseDetail.ts` — Text-Item-Felder (id, member_name, role_label, body_html, created_at).
    - Bestehende TipTap/HTML-Darstellung sanitized Body: vorhandene Notiz-Render-Muster (z. B. Member-Story/Notes-Komponenten) als Referenz fuer sicheres `dangerouslySetInnerHTML` auf bereits serverseitig sanitiztem `body_html`.
    - Design-System `@/components/ui`.
  </read_first>
  <action>
    Erstelle eine Client-Komponente ('use client') `ReleaseNotesList`. Rendere Textbeitraege als Karten: Avatar (Initiale-Placeholder falls kein Bild), Name, Rolle (`role_label`), Zeitpunkt (`created_at` in deutschem Format) und Inhalt (`body_html`, bereits serverseitig sanitized). Erste Seite via Prop/Initial-Aufruf, weitere via Cursor `getGroupReleaseNotes`. Nachladen: automatisch per `IntersectionObserver` UND `@/components/ui` `Button` "Mehr laden" (AO4-25). Skeleton-Karten fester Hoehe waehrend des Ladens (AO4-22). Avatare `loading="lazy"`. Nur Team4s-Tokens; Umlaute korrekt; Datei <=450 Zeilen.
  </action>
  <acceptance_criteria>
    - Karten zeigen Avatar, Name, Zeitpunkt und Inhalt; Nachladen via `getGroupReleaseNotes` (Cursor) mit Auto- und "Mehr laden"-Modus.
    - Skeleton-Platzhalter fester Hoehe verhindern Layout-Sprung.
    - `cd frontend; npm run typecheck` fehlerfrei.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run typecheck</automated>
  </verify>
  <done>Textliste zeigt alle Beitraege als Karten, laedt cursor-basiert nach.</done>
</task>

<task type="auto">
  <name>Task 3: Galerie und Textliste in die Detailroute einbinden</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.tsx</files>
  <read_first>
    - `frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.tsx` (aus 99-12) — Hero, ContributorsRow, ThemeTimeline und die reservierten Container/Sektionen fuer Galerie/Textliste.
  </read_first>
  <action>
    Binde `ReleaseGallery` und `ReleaseNotesList` in die in 99-12 reservierten Sektionen ein. Uebergib die aus dem aggregierenden Endpoint bereits geladene erste Seite (Bilder/Texte) als Initialdaten sowie die IDs (animeID, groupID, releaseVersionID) fuer das Cursor-Nachladen. Bei leerer Galerie/Textliste die jeweilige Sektion ausblenden (keine leeren Boxen). Reihenfolge auf der Detailseite: Hero -> Beteiligte -> Galerie -> Textliste -> OP/ED/Middle-Timeline (oder sinnvolle, konsistente Anordnung). Keine neuen Tokens; Umlaute korrekt.
  </action>
  <acceptance_criteria>
    - `page.tsx` rendert `ReleaseGallery` und `ReleaseNotesList` mit Initialdaten + IDs; leere Listen blenden die Sektion aus.
    - `cd frontend; npm run typecheck` fehlerfrei; falls Route-Test existiert, gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run typecheck</automated>
  </verify>
  <done>Detailseite zeigt vollstaendige Galerie und Textliste mit Infinite Scroll + Mehr laden.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`; Route baut.
- Live-Check via :3000 in 99-14: kein Layout-Sprung beim Nachladen; Typ-Tags + Autor pro Bild; Mehr-laden-Button vorhanden.
</verification>

<success_criteria>
- Vollstaendige Galerie mit Typ-Tag + Autor; vollstaendige Textliste mit Autor + Zeitpunkt; Infinite Scroll nur hier + Projektseiten-Liste; Lazy/Skeleton/srcset aktiv; Mehr-laden-Fallback vorhanden.
</success_criteria>

<output>
Erstelle `.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-13-SUMMARY.md` nach Abschluss.
</output>
