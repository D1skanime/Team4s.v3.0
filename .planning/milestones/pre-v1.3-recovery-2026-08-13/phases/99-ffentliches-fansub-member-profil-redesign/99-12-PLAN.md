---
phase: 99
plan: "12"
type: execute
wave: 8
depends_on:
  - "99-06"
  - "99-07"
files_modified:
  - "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.tsx"
  - "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.module.css"
  - "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseDetailHero.tsx"
  - "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ContributorsRow.tsx"
  - "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.tsx"
autonomous: true
requirements_addressed:
  - "AO4-15"
  - "AO4-16"
  - "AO4-17"
  - "AO4-20"
  - "AO4-23"
requirements:
  - "Phase 99 Add-on 4 AO4-15, AO4-16, AO4-17, AO4-20, AO4-23"
must_haves:
  truths:
    - "AO4-15: Es existiert eine neue eigenstaendige oeffentliche Route /anime/[id]/group/[groupId]/releases/[releaseVersionId], die den aggregierenden Endpoint (AO4-02) nutzt und von der Projektseite verlinkt ist."
    - "AO4-16: Der Hero der Release-Detailseite zeigt Anzahl Bilder, Texte, Beteiligte und das Veroeffentlichungsdatum."
    - "AO4-17: Beteiligte werden als horizontale Avatar-Reihe mit Name + Rolle dargestellt."
    - "AO4-20: Eine OP/ED/Middle-Timeline zeigt aus dem zugehoerigen Projekt Vorschaubild, Zeitcode (AO4-04) und Typ-Tag - ohne Video-Player."
    - "AO4-23: Hero-Bilder liefern srcset/sizes fuer mobile Aufloesungen."
  artifacts:
    - path: "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.tsx"
      provides: "Neue Release-Detailroute"
    - path: "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.tsx"
      provides: "OP/ED/Middle-Timeline ohne Player"
      contains: "start_time"
  key_links:
    - from: "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.tsx"
      to: "getGroupReleaseDetail"
      via: "Aggregierender Endpoint (AO4-02)"
      pattern: "getGroupReleaseDetail"
---

<objective>
Baue die neue Release-Detailseite (Route + Hero mit Kennzahlen + Beteiligten-Avatarreihe + OP/ED/Middle-Timeline). Galerie und Textliste folgen in 99-13.

Purpose: AO4-15 verlangt eine eigenstaendige Seite pro Release (nicht die alte episodes/[id]?releaseId=-Umleitung), gespeist vom aggregierenden Endpoint.

Output: Route-Shell, Hero, ContributorsRow, ThemeTimeline.
</objective>

<context>
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON4-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route-Shell und Hero mit Kennzahlen</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.tsx, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.module.css, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseDetailHero.tsx</files>
  <read_first>
    - `frontend/src/app/anime/[id]/group/[groupId]/page.tsx:22-52` — Muster fuer Server-Route: params await, Number.parseInt-Guard, notFound(), ApiError-Handling.
    - `frontend/src/lib/api.ts` — `getGroupReleaseDetail` (99-07) und `getAnimeByID`/`getGroupDetail` fuer Breadcrumb/Kontext.
    - `frontend/src/types/releaseDetail.ts` — DTO-Felder (`images_count`, `notes_count`, `contributors_count`, `release_date`).
    - `frontend/src/components/navigation/Breadcrumbs` — Breadcrumb-Nutzung wie in HeroSection.
    - Design-System `@/components/ui` (Pflicht).
  </read_first>
  <action>
    Erstelle die Server-Route `page.tsx` fuer `releases/[releaseVersionId]`. Parse `id`, `groupId`, `releaseVersionId` (Number-Guard, sonst `notFound()`). Lade via `getGroupReleaseDetail`; bei ApiError 404 `notFound()`, bei anderen Fehlern generische, nutzerverstaendliche Meldung (kein technischer Text, AO4-14-Geist). Rendere `ReleaseDetailHero` mit Titel, Veroeffentlichungsdatum und den drei Kennzahlen (Bilder/Texte/Beteiligte) sowie Breadcrumb/Zurueck-Link zur Projektseite. Hero-Bild (falls vorhanden, z. B. erstes Galeriebild oder Poster) mit `loading`/`sizes`/`srcSet` (AO4-23). Reserviere im Layout Platzhalter fuer die spaeter (99-13) folgende Galerie/Textliste (Container-IDs oder klar getrennte Sektionen). Nur Team4s-Tokens; `@/components/ui`-Primitives; Umlaute korrekt; Dateien <=450 Zeilen.
  </action>
  <acceptance_criteria>
    - Route-Datei existiert unter `releases/[releaseVersionId]/page.tsx` und ruft `getGroupReleaseDetail`.
    - Hero zeigt `images_count`, `notes_count`, `contributors_count` und `release_date`.
    - Hero-Bild hat `sizes` und (bei mehreren Aufloesungen) `srcSet`.
    - `cd frontend; npm run typecheck` fehlerfrei; `next build`/`typecheck` erkennt die Route.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run typecheck</automated>
  </verify>
  <done>Detailroute existiert mit Hero-Kennzahlen und ist verlinkbar.</done>
</task>

<task type="auto">
  <name>Task 2: ContributorsRow (Beteiligte als Avatar-Reihe)</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ContributorsRow.tsx</files>
  <read_first>
    - `frontend/src/types/releaseDetail.ts` — `contributors[]` (name, role_label, avatar_url) aus 99-07.
    - `frontend/src/components/fansubs/FansubTeamSection.tsx` — bestehendes Avatar-/Mitglieder-Darstellungsmuster als Stil-Referenz.
    - Design-System `@/components/ui`.
  </read_first>
  <action>
    Erstelle `ContributorsRow`, die die Beteiligten aus dem Release-Detail-Payload als horizontale Avatar-Reihe rendert: pro Beteiligtem Avatar (Bild oder Initiale-Placeholder), Name und Rolle (`role_label`). Avatare `loading="lazy"` mit fester Groesse (kein Layout-Sprung). Horizontale Reihe, die auf Mobile (<=390px) ohne Seiten-Overflow scrollt. Leerfall: Sektion ausblenden. Nur Team4s-Tokens; Umlaute korrekt.
  </action>
  <acceptance_criteria>
    - Komponente rendert pro Beteiligtem Name + `role_label` + Avatar/Initiale.
    - Avatare haben `loading="lazy"` und feste Dimensionen; horizontale, mobil-scrollbare Reihe.
    - Leere Beteiligtenliste blendet die Sektion aus.
    - `cd frontend; npm run typecheck` fehlerfrei.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run typecheck</automated>
  </verify>
  <done>Beteiligte erscheinen als Avatar-Reihe mit Name + Rolle.</done>
</task>

<task type="auto">
  <name>Task 3: ThemeTimeline (OP/ED/Middle ohne Player)</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.tsx</files>
  <read_first>
    - `frontend/src/types/groupContributors.ts` — Theme-Typ inkl. `start_time`/`end_time` (aus 99-06) und `type`/`assets`.
    - `frontend/src/lib/api.ts:6152-6171` — `getGroupThemes` (liefert die Projekt-Themes fuer die Timeline).
    - `frontend/src/app/anime/[id]/group/[groupId]/sections/ThemesSection.tsx` — bestehendes Themes-Vorschau-Muster (Typ-Tag, Thumbnail) als Referenz.
  </read_first>
  <action>
    Erstelle `ThemeTimeline`, die die Projekt-Themes (via `getGroupThemes(animeID, groupID)`) als Timeline rendert: pro Eintrag Vorschaubild (`assets[0].thumbnail_url`, `loading="lazy"`, feste Aspect-Ratio), Typ-Tag (OP/ED/MIDDLE mit deutscher Beschriftung) und Zeitcode aus `start_time`/`end_time` (als HH:MM:SS-Spanne; fehlt der Zeitcode, Zeitcode weglassen ohne Platzhalter). KEIN Video-Player, keine Abspielsteuerung - nur Vorschau-Darstellung. Leerfall (keine Themes): Sektion ausblenden. Nur Team4s-Tokens; `@/components/ui`-Primitives; Umlaute korrekt.
  </action>
  <acceptance_criteria>
    - Komponente rendert pro Theme Thumbnail + Typ-Tag + Zeitcode (falls vorhanden), ohne `<video>`/Player-Element.
    - `grep -n "video\|player\|Player" ThemeTimeline.tsx` liefert keinen echten Video-Player (nur ggf. unkritische Bezeichner).
    - Zeitcode nutzt `start_time`/`end_time` aus dem Theme-Payload.
    - `cd frontend; npm run typecheck` fehlerfrei.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run typecheck</automated>
  </verify>
  <done>OP/ED/Middle-Timeline zeigt Vorschau + Zeitcode + Typ ohne Player.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`; Route baut.
- Live-Check via :3000 in 99-14.
</verification>

<success_criteria>
- Detailseite erreichbar, Hero mit Kennzahlen, Beteiligte als Avatar-Reihe, OP/ED/Middle-Timeline ohne Player.
</success_criteria>

<output>
Erstelle `.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-12-SUMMARY.md` nach Abschluss.
</output>
