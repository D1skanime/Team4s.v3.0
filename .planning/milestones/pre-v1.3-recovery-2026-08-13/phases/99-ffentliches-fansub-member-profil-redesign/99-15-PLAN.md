---
phase: 99
plan: "15"
type: execute
wave: 11
depends_on: []
files_modified:
  - "backend/internal/models/fansub.go"
  - "backend/internal/repository/fansub_repository.go"
  - "backend/internal/repository/fansub_repository_test.go"
  - "shared/contracts/openapi.yaml"
  - "frontend/src/types/fansub.ts"
autonomous: true
requirements_addressed:
  - "AO5-01"
  - "AO5-02"
requirements:
  - "Phase 99 Add-on 5 Public-DTO (Community-Links + Medien-Felder + Soft-Delete-Bugfix)"
must_haves:
  truths:
    - "AO5-01: Die öffentliche Profil-Antwort für /fansubs/[slug] enthält ein neues Feld community_links mit allen fansub_group_links (link_type, name, url) der Gruppe, befüllt über ListGroupLinks."
    - "AO5-02: PublicFansubMediaItem trägt title, description und category; der Public-Media-Query selektiert fgm.title, fgm.description, fgm.category."
    - "AO5-02: Soft-gelöschte Medien erscheinen nicht mehr im Public-Profil, weil der Public-Media-Query um AND fgm.deleted_at IS NULL ergänzt ist."
  artifacts:
    - path: "backend/internal/repository/fansub_repository.go"
      provides: "GetPublicProfileBySlug befüllt CommunityLinks; listPublicFansubMedia selektiert Titel/Beschreibung/Kategorie und filtert deleted_at"
      contains: "fgm.deleted_at IS NULL"
    - path: "backend/internal/models/fansub.go"
      provides: "CommunityLinks-Feld + Media-Felder title/description/category"
      contains: "CommunityLinks"
    - path: "frontend/src/types/fansub.ts"
      provides: "TS-Typen für community_links + Media-Felder"
      contains: "community_links"
  key_links:
    - from: "GetPublicProfileBySlug"
      to: "ListGroupLinks"
      via: "resp.CommunityLinks = links"
      pattern: "ListGroupLinks"
    - from: "listPublicFansubMedia SELECT/Scan"
      to: "PublicFansubMediaItem.Title/Description/Category"
      via: "fgm.title, fgm.description, fgm.category"
      pattern: "fgm\\.category"
---

<objective>
Erweitere den öffentlichen Fansub-Profil-Pfad (DTO) additiv: Community-Links werden ins Public-Profil geladen (AO5-01), Medien-Items tragen Titel/Beschreibung/Kategorie und soft-gelöschte Medien werden ausgeblendet (AO5-02). Kein Migrations-, kein Datenmodell-Umbau — nur additive DTO-Felder + ein WHERE-Bugfix.

Purpose: Zwei bereits vorhandene Datenquellen (fansub_group_links, fansub_group_media.title/description/category) sind im Public-Profil nicht sichtbar; die Frontend-Sektionen (99-16/99-17) brauchen diese Felder im Contract, bevor sie gebaut werden.

Output: Erweiterte Go-Models + Repository-Query, angepasster OpenAPI-Contract, konsistente TS-Typen, Source-Assertion-Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON5-CONTEXT.md
@CLAUDE.md

<interfaces>
Bestehende Go-Strukturen (backend/internal/models/fansub.go):
- PublicFansubProfileResponse (:64): Felder Group, Story, Projects, History, Media — Add-on 5 ergänzt CommunityLinks.
- PublicFansubMediaItem (:104): ID, MediaType, Caption, MimeType, ThumbnailURL, OriginalURL — Add-on 5 ergänzt Title, Description, Category.
- FansubGroupLink (:144): ID, GroupID, LinkType, Name(*string), URL, CreatedAt — existiert bereits, wird wiederverwendet.

Bestehende Repository-Funktionen (backend/internal/repository/fansub_repository.go):
- GetPublicProfileBySlug (:244) — Orchestrator, hier CommunityLinks befüllen.
- listPublicFansubMedia (:388) — SELECT :390-396, WHERE :404-410, Scan :424-433.
- ListGroupLinks (:610) — liefert []models.FansubGroupLink sortiert (link_type, created_at, id).

TS (frontend/src/types/fansub.ts):
- PublicFansubMediaItem (:171), PublicFansubProfile (:180), FansubGroupLink (:12) existieren bereits.

DB-Kontext: fansub_group_media hat title TEXT, description TEXT, category VARCHAR(40) NOT NULL DEFAULT 'other' (CHECK: gallery, history_screenshot, old_website, forum, irc_chat, event_meeting, artwork_fanart, other), deleted_at TIMESTAMPTZ (Migration 0109).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Go-Models + Public-Media-Query + Community-Links + Soft-Delete-Bugfix</name>
  <files>backend/internal/models/fansub.go, backend/internal/repository/fansub_repository.go, backend/internal/repository/fansub_repository_test.go</files>
  <read_first>
    - backend/internal/models/fansub.go (PublicFansubProfileResponse :64-71, PublicFansubMediaItem :104-111, FansubGroupLink :144-151) — Ziel-Strukturen.
    - backend/internal/repository/fansub_repository.go (GetPublicProfileBySlug :244-282, listPublicFansubMedia :388-446, ListGroupLinks :610-646, scanFansubGroupLink :1334-1348) — Quell-Query + Scan.
    - backend/internal/repository/fansub_repository_test.go (TestFansubRepository_PublicProfileSourceInvariants :45-77) — Source-Assertion-Muster (kein DB-Harness; Tests prüfen SQL-/Codefragmente per strings.Contains).
    - database/migrations/0109_fansub_group_media_management.up.sql (Spalten title/description/category/deleted_at + category-CHECK :23-32).
  </read_first>
  <behavior>
    - GetPublicProfileBySlug befüllt resp.CommunityLinks über ListGroupLinks(ctx, group.ID) (Init als leerer Slice, nie nil).
    - listPublicFansubMedia liefert für ein Medium title/description (nullable) und category (non-null) mit; Titel und Beschreibung bleiben *string, category ist string.
    - Ein soft-gelöschtes Medium (deleted_at gesetzt) wird nicht mehr zurückgegeben.
    - TestFansubRepository_PublicProfileSourceInvariants schlägt fehl, wenn eines der neuen Fragmente fehlt.
  </behavior>
  <action>
    In models/fansub.go: PublicFansubProfileResponse (:64) um Feld `CommunityLinks []FansubGroupLink `json:"community_links"`` ergänzen. PublicFansubMediaItem (:104) um `Title *string `json:"title,omitempty"``, `Description *string `json:"description,omitempty"`` und `Category string `json:"category"`` ergänzen.

    In fansub_repository.go GetPublicProfileBySlug (:250-255): CommunityLinks im Initial-Struct als `make([]models.FansubGroupLink, 0)` setzen; nach dem Media-Block (:279) `links, err := r.ListGroupLinks(ctx, group.ID)` aufrufen und `resp.CommunityLinks = links` zuweisen (Fehler wie die anderen Sub-Loader zurückgeben). Da die Gruppe über GetGroupBySlug bereits existiert, liefert ListGroupLinks kein ErrNotFound.

    In listPublicFansubMedia: SELECT-Liste (:390-396) um `fgm.title`, `fgm.description`, `fgm.category` erweitern. WHERE (:404-410) um `AND fgm.deleted_at IS NULL` ergänzen (Bugfix — ehemals public/approved, jetzt soft-gelöschte Medien dürfen nicht mehr erscheinen). Bestehende Filter (ma.status='ready', v.name='public', rs.code='approved', Logo/Banner-Ausschluss) unverändert lassen. Im Scan (:424-433) die drei neuen Spalten in `&item.Title`, `&item.Description`, `&item.Category` mappen (Reihenfolge SELECT = Scan beachten; media_type bleibt aus mt.name, category ist separat).

    In fansub_repository_test.go TestFansubRepository_PublicProfileSourceInvariants die Fragment-Liste (:52-67) erweitern um: `"fgm.deleted_at IS NULL"`, `"fgm.title"`, `"fgm.description"`, `"fgm.category"`, `"r.ListGroupLinks(ctx, group.ID)"` und `"CommunityLinks"`.

    Backend nach Codeänderung neu bauen mit `docker compose up -d --build team4sv30-backend`, damit die Route live ist (stale Docker-Backend liefert sonst alten DTO).
  </action>
  <acceptance_criteria>
    - `cd backend; go build ./...` fehlerfrei.
    - `cd backend; go test ./internal/repository -run PublicProfile` grün (die erweiterten Fragmente werden gefunden).
    - `grep -n "AND fgm.deleted_at IS NULL" backend/internal/repository/fansub_repository.go` liefert genau die Zeile im Public-Media-WHERE.
    - `grep -n "resp.CommunityLinks" backend/internal/repository/fansub_repository.go` zeigt die Zuweisung aus ListGroupLinks.
    - `grep -c -e "fgm.title" -e "fgm.description" -e "fgm.category" backend/internal/repository/fansub_repository.go` bestätigt die drei SELECT-Spalten.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend; go build ./... && go test ./internal/repository -run PublicProfile</automated>
  </verify>
  <done>Go-Models tragen CommunityLinks + Media-Felder; Public-Media-Query selektiert title/description/category und filtert deleted_at; GetPublicProfileBySlug befüllt CommunityLinks; Source-Tests grün; Backend neu gebaut.</done>
</task>

<task type="auto">
  <name>Task 2: OpenAPI-Contract + TS-Typen konsistent nachziehen</name>
  <files>shared/contracts/openapi.yaml, frontend/src/types/fansub.ts</files>
  <read_first>
    - shared/contracts/openapi.yaml (PublicFansubMediaItem :10326-10345, PublicFansubProfile :10346-10371) — Ziel-Schemas; im Contract existiert bisher KEIN FansubGroupLink-Schema.
    - frontend/src/types/fansub.ts (FansubGroupLink :12-19, PublicFansubMediaItem :171-178, PublicFansubProfile :180-186) — Ziel-Typen.
    - backend/internal/models/fansub.go (nach Task 1 geänderte JSON-Tags) — Feldnamen als Source of Truth für den Contract.
  </read_first>
  <action>
    In openapi.yaml PublicFansubMediaItem (:10326): unter properties `title` (type string, nullable true), `description` (type string, nullable true) und `category` (type string) ergänzen; `category` zusätzlich in `required` aufnehmen (Backend liefert immer einen Wert, Default 'other').

    Ein neues Schema `FansubGroupLink` unter components/schemas anlegen (analog zur Go-Struktur FansubGroupLink): properties id (integer int64), group_id (integer int64), link_type (string, enum website/discord/twitter/github/irc), name (string, nullable true), url (string), created_at (string, format date-time); required [id, group_id, link_type, url, created_at].

    In PublicFansubProfile (:10346) unter properties `community_links` als array mit items $ref auf FansubGroupLink ergänzen und in `required` aufnehmen (Backend liefert immer mindestens einen leeren Array).

    In frontend/src/types/fansub.ts: PublicFansubMediaItem (:171) um `title?: string | null`, `description?: string | null`, `category: string` erweitern. PublicFansubProfile (:180) um `community_links: FansubGroupLink[]` erweitern (FansubGroupLink ist bereits in derselben Datei definiert).
  </action>
  <acceptance_criteria>
    - `cd frontend; npm run typecheck` fehlerfrei.
    - `grep -n "community_links" shared/contracts/openapi.yaml frontend/src/types/fansub.ts` zeigt Treffer in beiden Dateien.
    - `grep -n "FansubGroupLink:" shared/contracts/openapi.yaml` zeigt das neue Contract-Schema.
    - `grep -c -e "title" -e "description" -e "category" shared/contracts/openapi.yaml` bestätigt die neuen Media-Properties im PublicFansubMediaItem-Block (visuell verifiziert im geänderten Abschnitt).
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run typecheck</automated>
  </verify>
  <done>OpenAPI trägt title/description/category am PublicFansubMediaItem, ein FansubGroupLink-Schema und community_links am PublicFansubProfile; TS-Typen sind konsistent; typecheck grün.</done>
</task>

</tasks>

<verification>
- `go build ./...` + `go test ./internal/repository -run PublicProfile` grün.
- `npm run typecheck` grün.
- Grep-Gates: `AND fgm.deleted_at IS NULL`, `resp.CommunityLinks`, `community_links` (Contract + TS), `FansubGroupLink:` (Contract).
- Backend via `docker compose up -d --build team4sv30-backend` neu gebaut, damit /public-profile live die neuen Felder liefert (finaler Live-Check erfolgt im UAT-Plan 99-18).
</verification>

<success_criteria>
- Public-Profil-DTO enthält community_links und Medien-Titel/Beschreibung/Kategorie; soft-gelöschte Medien sind aus dem Public-Query ausgeschlossen; Contract und TS-Typen konsistent.
</success_criteria>

<output>
Erstelle `.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-15-SUMMARY.md` nach Abschluss.
</output>
