---
phase: 99
plan: "22"
type: execute
wave: 16
depends_on:
  - "99-19"
files_modified:
  - "frontend/src/components/fansubs/FansubHistorySection.tsx"
  - "frontend/src/components/fansubs/FansubCommunityLinksSection.tsx"
  - "frontend/src/components/fansubs/FansubCommunityLinksSection.module.css"
  - "frontend/src/components/fansubs/FansubPublicSections.module.css"
  - "frontend/src/components/fansubs/__tests__/FansubHistorySection.test.tsx"
  - "frontend/src/components/fansubs/__tests__/FansubCommunityLinksSection.test.tsx"
autonomous: true
requirements_addressed:
  - "AO6-09"
  - "AO6-10"
  - "AO6-04"
requirements:
  - "Phase 99 Add-on 6 CONTEXT AO6-09 (Meilensteine farblich abheben)"
  - "Phase 99 Add-on 6 CONTEXT AO6-10 (Community-Links als einheitliche Chips)"
  - "Phase 99 Add-on 6 CONTEXT AO6-04 (ruhiges Layout, ein Header pro Sektion)"
must_haves:
  truths:
    - "[AO6-09] Die Meilenstein-/Erfolge-Einträge (FansubHistorySection) heben sich farblich ab (Akzent-Block bzw. linke Akzent-Kante) — ausschließlich über bestehende Team4s-Tokens, keine Ad-hoc-Farbwerte."
    - "[AO6-10] Jeder Community-Link erscheint als EIN klickbarer Chip je link_type (Icon + Label/Name in einem <a>); externe Links tragen rel='noreferrer noopener'."
    - "[AO6-04] History- und Community-Sektion haben je genau einen klaren Header; keine überdimensionierten leeren Kacheln."
  artifacts:
    - path: "frontend/src/components/fansubs/FansubHistorySection.tsx"
      provides: "Meilenstein-Liste mit Akzent-Hervorhebung"
      contains: "historyEntry"
    - path: "frontend/src/components/fansubs/FansubCommunityLinksSection.tsx"
      provides: "Community-Chips (ein Chip je Link)"
      contains: "rel=\"noreferrer noopener\""
  key_links:
    - from: "frontend/src/components/fansubs/FansubHistorySection.tsx"
      to: "FansubPublicSections.module.css Akzent-Klasse"
      via: "className mit border-left/Akzent-Token"
      pattern: "history"
    - from: "frontend/src/components/fansubs/FansubCommunityLinksSection.tsx"
      to: "externe URL"
      via: "a target=_blank rel=noreferrer noopener"
      pattern: "noreferrer noopener"
---

<objective>
Meilensteine farblich abheben (AO6-09) und Community-Links als einheitliche, klickbare Chips verfeinern (AO6-10) — im ruhigen Variante-B-Layout mit je einem klaren Sektions-Header (AO6-04). Ausschließlich Team4s-Tokens.

Purpose: Erfolge/Historie sollen visuell als Meilensteine lesbar sein; Community-Links müssen einheitlich und eindeutig klickbar sein.
Output: Überarbeitete History- und Community-Sektion + CSS (nur Tokens) + Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON6-CONTEXT.md
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-16-SUMMARY.md

<interfaces>
<!-- Aktueller Stand (read-only verifiziert). -->

FansubHistorySection.tsx: rendert `<section id="erfolge">` + `<SectionHeader title="Historie & Erfolge" />` + `<ol class=historyList>`; je Eintrag Card variant='flat' mit historyEntry (Jahr strong + Titel + note). Klassen in FansubPublicSections.module.css: historyList, historyEntry, historyYear, historyNote.
FansubCommunityLinksSection.tsx: `<section id="community">` + `<SectionHeader title="Community & Links" />` + `<ul class=chipList>`; je Link ein `<a target=_blank rel="noreferrer noopener">` mit Badge (Icon + getFansubLinkTypeLabel(link_type)) + optionalem name (styles.chipName). Icons LINK_TYPE_ICONS (lucide). Klassen in FansubCommunityLinksSection.module.css: chipList, chip, chipBadge, chipName.
fansub-labels.ts: getFansubLinkTypeLabel (deutsche Labels).
Verfügbare Tokens (aus vorhandenem CSS): --accent-primary, --accent-primary-strong, --text-primary, --text-secondary, --text-muted, --bg-hover, --color-border.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Meilensteine farblich abheben (AO6-09)</name>
  <files>frontend/src/components/fansubs/FansubHistorySection.tsx, frontend/src/components/fansubs/FansubPublicSections.module.css, frontend/src/components/fansubs/__tests__/FansubHistorySection.test.tsx</files>
  <read_first>
    - frontend/src/components/fansubs/FansubHistorySection.tsx (gesamt)
    - frontend/src/components/fansubs/FansubPublicSections.module.css:64-86, 175-181 (history-Klassen + Media-Query)
  </read_first>
  <behavior>
    - Jeder Meilenstein-Eintrag trägt eine Akzent-Hervorhebung (linke Akzent-Kante bzw. Akzent-Block), umgesetzt mit einem CSS-Token (var(--accent-primary)) — keine literalen Hex/rgb-Farbwerte im CSS neu.
    - Bei leerer history-Liste rendert die Sektion weiterhin null.
    - Der bestehende Test (Rendering von Jahr/Titel/Note, Empty-State) bleibt grün.
  </behavior>
  <action>
    FansubHistorySection.tsx: den Meilenstein-Einträgen eine Akzent-Klasse geben (z.B. styles.milestoneEntry am Card/li), die eine linke Akzent-Kante (border-left) in var(--accent-primary) und dezenten Akzent-Hintergrund/Padding setzt. Header 'Historie & Erfolge' bleibt einzeln (AO6-04 ok). Struktur (ol/li/Card) beibehalten.

    FansubPublicSections.module.css: neue Klasse milestoneEntry ergänzen — border-left: 3px solid var(--accent-primary); padding-left via Token-basierten px-Wert; optional background var(--bg-hover) für den Block. Nur bestehende Tokens, KEINE neuen Farbliterale. Bestehende history-Klassen unverändert lassen.

    Test FansubHistorySection.test.tsx: eine Assertion ergänzen, die belegt, dass die Akzent-Klasse (className enthält 'milestone' bzw. der Klassenname-Fragment) im gerenderten Markup vorkommt; Empty-State-Test beibehalten.
  </action>
  <acceptance_criteria>
    - `grep -n "milestone" frontend/src/components/fansubs/FansubHistorySection.tsx` und `grep -n "milestone" frontend/src/components/fansubs/FansubPublicSections.module.css` vorhanden.
    - `grep -n "var(--accent-primary)" frontend/src/components/fansubs/FansubPublicSections.module.css` vorhanden; `grep -nE "#[0-9a-fA-F]{3,6}|rgb\(" frontend/src/components/fansubs/FansubPublicSections.module.css` liefert keine NEU eingeführten Farbliterale (nur Tokens).
    - `cd frontend && npx vitest run src/components/fansubs/__tests__/FansubHistorySection.test.tsx` grün.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/fansubs/__tests__/FansubHistorySection.test.tsx</automated>
  </verify>
  <done>Meilensteine visuell abgesetzt (Akzent-Kante/Block via Token); Test grün; keine neuen Farbliterale.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Community-Links als einheitliche Chips verfeinern (AO6-10/04)</name>
  <files>frontend/src/components/fansubs/FansubCommunityLinksSection.tsx, frontend/src/components/fansubs/FansubCommunityLinksSection.module.css, frontend/src/components/fansubs/__tests__/FansubCommunityLinksSection.test.tsx</files>
  <read_first>
    - frontend/src/components/fansubs/FansubCommunityLinksSection.tsx (gesamt)
    - frontend/src/components/fansubs/FansubCommunityLinksSection.module.css (gesamt)
  </read_first>
  <behavior>
    - Je Link genau EIN klickbarer Chip (ein <a>-Element) mit Icon + Name/Label; kein doppeltes klickbares Element.
    - Anzeigetext: link.name falls gesetzt, sonst getFansubLinkTypeLabel(link_type); Icon je link_type.
    - Externe Links tragen target='_blank' rel='noreferrer noopener'.
    - Leere Liste → null.
  </behavior>
  <action>
    FansubCommunityLinksSection.tsx: sicherstellen, dass pro Link EIN einheitlicher Chip als `<a target="_blank" rel="noreferrer noopener">` gerendert wird, der Icon + Anzeigetext (name bevorzugt, sonst deutsches Label) in einem konsistenten Chip vereint. Falls das aktuelle Markup Label UND name getrennt zeigt und das uneinheitlich wirkt, auf eine klare Regel vereinheitlichen (name als primärer Text, Label als Fallback), Icon links. @/components/ui Badge/SectionHeader weiter nutzen; kein natives button/interaktives Zweitelement. Header 'Community & Links' bleibt einzeln.

    FansubCommunityLinksSection.module.css: Chip-Layout auf einheitliche Größe/Abstände justieren (inline-flex, gap, Padding, Hover via Token), nur Team4s-Tokens.

    Test FansubCommunityLinksSection.test.tsx: bestehende Fälle (Leerfall, sichere Link-Attribute) beibehalten; Assertion ergänzen/prüfen, dass je Link genau ein <a> mit rel='noreferrer noopener' existiert und der Anzeigetext der name-bevorzugt-Regel folgt.
  </action>
  <acceptance_criteria>
    - `grep -c "rel=\"noreferrer noopener\"" frontend/src/components/fansubs/FansubCommunityLinksSection.tsx` >= 1.
    - `grep -Ev '^\s*//' frontend/src/components/fansubs/FansubCommunityLinksSection.tsx | grep -c "<button"` = 0.
    - `cd frontend && npx vitest run src/components/fansubs/__tests__/FansubCommunityLinksSection.test.tsx && npm run typecheck` grün.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/fansubs && npm run typecheck</automated>
  </verify>
  <done>Ein einheitlicher, klickbarer Chip je Community-Link mit sicheren rel-Attributen; Tests grün.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run typecheck` und `npx vitest run src/components/fansubs` grün.
- Live: `docker restart team4sv30-frontend`, harter Reload `/fansubs/c-subs` (:3000) — Meilensteine mit Akzent-Kante abgesetzt; Community-Links als einheitliche Chips; je Sektion ein Header.
</verification>

<success_criteria>
- Meilensteine heben sich farblich ab (nur Tokens).
- Community-Links sind einheitliche, sichere Chips.
- Je Sektion ein klarer Header, ruhiges Layout.
</success_criteria>

<output>
Create `.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-22-SUMMARY.md` when done
</output>
