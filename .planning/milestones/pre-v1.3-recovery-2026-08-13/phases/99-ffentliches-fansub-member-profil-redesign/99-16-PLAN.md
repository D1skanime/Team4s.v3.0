---
phase: 99
plan: "16"
type: execute
wave: 12
depends_on:
  - "99-15"
files_modified:
  - "frontend/src/lib/fansub-labels.ts"
  - "frontend/src/lib/__tests__/fansub-labels.test.ts"
  - "frontend/src/components/fansubs/FansubCommunityLinksSection.tsx"
  - "frontend/src/components/fansubs/FansubCommunityLinksSection.module.css"
  - "frontend/src/components/fansubs/FansubMediaSection.tsx"
  - "frontend/src/components/fansubs/FansubGroupMediaBlock.tsx"
  - "frontend/src/components/fansubs/FansubPublicSections.module.css"
  - "frontend/src/components/fansubs/__tests__/FansubCommunityLinksSection.test.tsx"
  - "frontend/src/components/fansubs/__tests__/FansubGroupMediaBlock.test.tsx"
autonomous: true
requirements_addressed:
  - "AO5-05"
  - "AO5-06"
  - "AO5-08"
requirements:
  - "Phase 99 Add-on 5 daten-getriebene Sektionen (Community-Links + Medien) + deutsche Enum-Labels"
must_haves:
  truths:
    - "AO5-08: Zentrale Label-Maps übersetzen category (gallery→Galerie, history_screenshot→Historische Screenshots, old_website→Alte Website, forum→Forum, irc_chat→IRC-Chat, event_meeting→Event / Treffen, artwork_fanart→Artwork / Fanart, other→Sonstiges) und link_type (website→Webseite, discord→Discord, irc→IRC, twitter→Twitter/X, github→GitHub) mit Fallback für unbekannte Werte."
    - "AO5-05: Eine Community-Links-Sektion rendert die community_links als extern verlinkte Chips je link_type mit deutschem Label und optionalem name, ausschließlich über @/components/ui-Primitives, und rendert null bei leerer Liste."
    - "AO5-06: Die Medien-Sektion zeigt je Medium Thumbnail, Titel, Beschreibung und einen Typ-Tag (deutsches category-Label); Bilder nutzen lazy loading + Skeleton/feste Aspect-Ratio (kein Layout-Sprung) + sizes; nur Team4s-Tokens."
  artifacts:
    - path: "frontend/src/lib/fansub-labels.ts"
      provides: "getFansubMediaCategoryLabel + getFansubLinkTypeLabel + Label-Maps"
      contains: "Galerie"
    - path: "frontend/src/components/fansubs/FansubCommunityLinksSection.tsx"
      provides: "Community-Links-Chips-Sektion"
      min_lines: 20
    - path: "frontend/src/components/fansubs/FansubGroupMediaBlock.tsx"
      provides: "Medien-Grid mit Titel/Beschreibung/Typ-Tag + lazy/skeleton/sizes"
      contains: "loading"
  key_links:
    - from: "FansubGroupMediaBlock"
      to: "getFansubMediaCategoryLabel"
      via: "Typ-Tag Badge aus item.category"
      pattern: "getFansubMediaCategoryLabel"
    - from: "FansubCommunityLinksSection"
      to: "getFansubLinkTypeLabel"
      via: "Chip-Label aus link.link_type"
      pattern: "getFansubLinkTypeLabel"
---

<objective>
Baue die zwei daten-getriebenen Sektionen für /fansubs/[slug], die die in 99-15 ergänzten DTO-Felder konsumieren: eine Community-Links-Sektion (AO5-05) und eine aufgewertete Medien-Sektion mit Titel/Beschreibung/Typ-Tag und sauberem Ladeverhalten (AO5-06), gestützt auf zentrale deutsche Enum-Label-Maps (AO5-08). Verdrahtung in die Seite selbst erfolgt in 99-17.

Purpose: Community-Links und Medien-Kontext (Titel/Beschreibung/Kategorie) sind vorhanden, aber unsichtbar bzw. roh; diese Sektionen machen sie sichtbar, konsistent und barrierearm.

Output: fansub-labels.ts + neue Community-Links-Sektion + aufgewertete Medien-Sektion, jeweils testgesichert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON5-CONTEXT.md
@CLAUDE.md

<interfaces>
Nach 99-15 verfügbare TS-Typen (frontend/src/types/fansub.ts):
- PublicFansubMediaItem: id, media_type, caption?, mime_type, thumbnail_url?, original_url?, title?, description?, category.
- FansubGroupLink: id, group_id, link_type (website|discord|twitter|github|irc), name?, url, created_at.
- PublicFansubProfile: ... community_links: FansubGroupLink[].

Bestehende Komponenten (werden erweitert):
- FansubMediaSection.tsx — wrappt FansubGroupMediaBlock in Card + SectionHeader (id="medien").
- FansubGroupMediaBlock.tsx — rendert media in styles.mediaItemGrid; nutzt next/image (unoptimized), resolveApiUrl, EmptyState; label = caption || media_type.

Verfügbare @/components/ui-Primitives: Badge, Button, Card, EmptyState, SectionHeader, LoadingState (u.a.). Icons: lucide-react.
Bestehende CSS-Klassen (FansubPublicSections.module.css): mediaGrid, mediaItemGrid, mediaImage, mediaLabel, mediaLabelWithImage, inlineLink, sectionTitle.
Muster für externe Links im Hero: heroLink mit target="_blank" rel="noreferrer" (page.module.css).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zentrale deutsche Enum-Label-Maps (AO5-08)</name>
  <files>frontend/src/lib/fansub-labels.ts, frontend/src/lib/__tests__/fansub-labels.test.ts</files>
  <read_first>
    - .planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON5-CONTEXT.md (AO5-08 exakte Label-Werte :39-41).
    - frontend/src/types/fansub.ts (FansubGroupLinkType :5-10) — erlaubte link_type-Werte.
    - database/migrations/0109_fansub_group_media_management.up.sql (category-CHECK :23-32) — erlaubte category-Werte.
  </read_first>
  <behavior>
    - getFansubMediaCategoryLabel('gallery') === 'Galerie'; ('history_screenshot') === 'Historische Screenshots'; ('old_website') === 'Alte Website'; ('forum') === 'Forum'; ('irc_chat') === 'IRC-Chat'; ('event_meeting') === 'Event / Treffen'; ('artwork_fanart') === 'Artwork / Fanart'; ('other') === 'Sonstiges'.
    - getFansubLinkTypeLabel('website') === 'Webseite'; ('discord') === 'Discord'; ('irc') === 'IRC'; ('twitter') === 'Twitter/X'; ('github') === 'GitHub'.
    - Unbekannter Wert (z.B. 'unbekannt') → Fallback 'Sonstiges' (category) bzw. der Rohwert (link_type), kein Crash.
  </behavior>
  <action>
    Lege frontend/src/lib/fansub-labels.ts an mit zwei konstanten Maps FANSUB_MEDIA_CATEGORY_LABELS (Record<string,string>) und FANSUB_LINK_TYPE_LABELS (Record<FansubGroupLinkType,string>) mit exakt den Werten aus AO5-08 (korrekte deutsche Umlaute, Slash-Schreibweise 'Twitter/X', 'Event / Treffen', 'Artwork / Fanart'). Exportiere zwei Helper getFansubMediaCategoryLabel(category: string): string (Fallback 'Sonstiges') und getFansubLinkTypeLabel(linkType: string): string (Fallback = Rohwert). Keine JSX, reine Datenlogik.
    Lege den Vitest-Unit-Test an, der beide Helper für alle Enum-Werte plus je einen unbekannten Wert prüft.
  </action>
  <acceptance_criteria>
    - `cd frontend; npx vitest run src/lib/__tests__/fansub-labels.test.ts` grün.
    - `cd frontend; npm run typecheck` fehlerfrei.
    - `grep -c -e "Galerie" -e "Historische Screenshots" -e "Twitter/X" frontend/src/lib/fansub-labels.ts` bestätigt korrekte Umlaut-/Slash-Labels.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npx vitest run src/lib/__tests__/fansub-labels.test.ts</automated>
  </verify>
  <done>fansub-labels.ts exportiert beide Label-Maps und Helper mit Fallback; Unit-Test grün.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Community-Links-Sektion (AO5-05)</name>
  <files>frontend/src/components/fansubs/FansubCommunityLinksSection.tsx, frontend/src/components/fansubs/FansubCommunityLinksSection.module.css, frontend/src/components/fansubs/__tests__/FansubCommunityLinksSection.test.tsx</files>
  <read_first>
    - frontend/src/components/fansubs/FansubHeroSection.tsx (:95-103) — Muster externer Link (target/_blank, rel="noreferrer") + Badge-Nutzung.
    - frontend/src/lib/fansub-labels.ts (aus Task 1) — getFansubLinkTypeLabel.
    - frontend/src/types/fansub.ts (FansubGroupLink :12-19).
    - CLAUDE.md (Design-System-Pflicht: nur @/components/ui, keine nativen Elemente; Tokens statt Ad-hoc-Farben).
  </read_first>
  <behavior>
    - Bei leerer Links-Liste rendert die Komponente null (kein leerer Sektionsrahmen).
    - Bei vorhandenen Links rendert sie einen SectionHeader ('Community & Links') und je Link ein extern verlinktes Chip mit deutschem link_type-Label und, falls name gesetzt, dem name als Zusatztext.
    - Jeder Chip-Link trägt target="_blank" und rel="noreferrer noopener".
  </behavior>
  <action>
    Erstelle FansubCommunityLinksSection.tsx (Props: links: FansubGroupLink[]). Bei links.length === 0 → return null. Sonst Section mit id="community" und SectionHeader title="Community & Links". Rendere je Link ein Chip: umschließendes <a href={link.url} target="_blank" rel="noreferrer noopener"> um ein @/components/ui-Primitive (Badge oder Button size="sm" variant nach Discretion) mit Icon aus lucide-react je link_type (z.B. Globe/Webseite, MessageCircle/Discord, Hash/IRC, Twitter, Github) und Text = getFansubLinkTypeLabel(link.link_type); optionalen link.name als sekundären Text anhängen. KEINE nativen <button>/<select>/<input>. Ordne die Chips in einer flex-wrap-Liste (neues CSS-Modul FansubCommunityLinksSection.module.css, nur Team4s-Tokens, keine Ad-hoc-Farben). Icon-Wahl/Chip-Variante ist Discretion.
    Schreibe den Test (Testing Library / vitest): (a) leere Liste → Container leer/null; (b) zwei Links (website mit name, discord ohne name) → deutsche Labels sichtbar, name sichtbar, beide <a> mit target="_blank" und rel enthält "noreferrer".
  </action>
  <acceptance_criteria>
    - `cd frontend; npx vitest run src/components/fansubs/__tests__/FansubCommunityLinksSection.test.tsx` grün.
    - `grep -c -e "<select" -e "<input" -e "<textarea" -e "<button" frontend/src/components/fansubs/FansubCommunityLinksSection.tsx` ergibt 0 (nur @/components/ui-Primitives).
    - `grep -n "rel=" frontend/src/components/fansubs/FansubCommunityLinksSection.tsx` zeigt rel mit "noreferrer".
    - Datei ≤450 Zeilen (`wc -l` prüfen).
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npx vitest run src/components/fansubs/__tests__/FansubCommunityLinksSection.test.tsx</automated>
  </verify>
  <done>Community-Links-Sektion rendert extern verlinkte Chips mit deutschem Label + optionalem name über @/components/ui, null bei Leerliste; Test grün.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Medien-Sektion mit Titel/Beschreibung/Typ-Tag + lazy/skeleton/sizes (AO5-06)</name>
  <files>frontend/src/components/fansubs/FansubMediaSection.tsx, frontend/src/components/fansubs/FansubGroupMediaBlock.tsx, frontend/src/components/fansubs/FansubPublicSections.module.css, frontend/src/components/fansubs/__tests__/FansubGroupMediaBlock.test.tsx</files>
  <read_first>
    - frontend/src/components/fansubs/FansubGroupMediaBlock.tsx (komplett) — bestehende Render-Logik (next/image unoptimized, resolveApiUrl, isImage, label).
    - frontend/src/components/fansubs/FansubMediaSection.tsx (komplett) — Wrapper.
    - frontend/src/components/fansubs/FansubPublicSections.module.css (mediaGrid :88, mediaItemGrid :94, mediaImage :100-106, mediaLabel* :108-115).
    - frontend/src/lib/fansub-labels.ts (aus Task 1) — getFansubMediaCategoryLabel.
    - frontend/src/types/fansub.ts (PublicFansubMediaItem nach 99-15 mit title/description/category).
  </read_first>
  <behavior>
    - Jede Medien-Kachel zeigt: Thumbnail (falls Bild), Titel (item.title, Fallback caption || media_type), Beschreibung (item.description, nur wenn gesetzt) und einen Typ-Tag mit getFansubMediaCategoryLabel(item.category).
    - Bilder tragen loading="lazy" und sizes; der Bildplatz hat eine feste Aspect-Ratio (kein Layout-Sprung beim Nachladen); ein Skeleton/Platzhalter mit gleicher Aspect-Ratio erscheint solange kein Bild vorhanden ist.
    - Bei leerer Medien-Liste bleibt das bisherige EmptyState-Verhalten erhalten.
  </behavior>
  <action>
    In FansubGroupMediaBlock.tsx: pro Medium Titel-Priorität item.title?.trim() || item.caption?.trim() || item.media_type; Beschreibung item.description nur rendern, wenn nicht leer; Typ-Tag als @/components/ui Badge mit getFansubMediaCategoryLabel(item.category). Bild: next/image behalten, `loading="lazy"` und `sizes` (z.B. "(max-width: 640px) 100vw, 220px") ergänzen; feste Aspect-Ratio über die vorhandene .mediaImage-Klasse (aspect-ratio 16/9 ist gesetzt) sicherstellen und einen Skeleton-Platzhalter (CSS, neutrale Token-Fläche mit gleicher aspect-ratio) für den Ladezeitraum bzw. Nicht-Bild-Fälle einführen. srcset ergänzt next/image selbst nicht bei unoptimized → wo ein Original vorhanden ist, `sizes` setzen und thumbnail_url als src, original_url als optionales größeres Quellattribut nutzen (Discretion; kein Layout-Sprung ist das harte Kriterium). KEINE Ad-hoc-Farben, nur Team4s-Tokens im CSS-Modul.
    In FansubMediaSection.tsx: SectionHeader-Titel auf "Medien" belassen; die Grid-/Kachelstruktur so anpassen, dass sie mit den anderen Sektionen konsistent ist (nutzt Task-3-CSS aus FansubPublicSections.module.css). Falls die Liste sehr lang wird, ist ein "Mehr anzeigen"-Umschalter ab N Medien optional (Discretion) — KEIN Infinite Scroll.
    Schreibe/erweitere den Test für FansubGroupMediaBlock: (a) Medium mit title+description+category='gallery' → 'Galerie'-Typ-Tag, Titel und Beschreibung sichtbar; (b) Bild-Item hat loading="lazy" und sizes-Attribut; (c) leere Liste → EmptyState.
  </action>
  <acceptance_criteria>
    - `cd frontend; npx vitest run src/components/fansubs/__tests__/FansubGroupMediaBlock.test.tsx` grün.
    - `grep -n "loading=\"lazy\"" frontend/src/components/fansubs/FansubGroupMediaBlock.tsx` und `grep -n "sizes" frontend/src/components/fansubs/FansubGroupMediaBlock.tsx` liefern Treffer.
    - `grep -n "getFansubMediaCategoryLabel" frontend/src/components/fansubs/FansubGroupMediaBlock.tsx` zeigt Nutzung des Kategorie-Labels.
    - `grep -c -e "aspect-ratio" frontend/src/components/fansubs/FansubPublicSections.module.css` bestätigt feste Aspect-Ratio (Skeleton + Bild).
    - Beide geänderten .tsx-Dateien ≤450 Zeilen.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npx vitest run src/components/fansubs/__tests__/FansubGroupMediaBlock.test.tsx; npm run typecheck</automated>
  </verify>
  <done>Medien-Kacheln zeigen Titel/Beschreibung/Typ-Tag; Bilder lazy + sizes + feste Aspect-Ratio + Skeleton; kein Layout-Sprung; nur Team4s-Tokens; Test grün.</done>
</task>

</tasks>

<verification>
- Vitest der drei neuen/erweiterten Testdateien grün; `npm run typecheck` grün.
- Grep-Gates: keine nativen Formular-Elemente in FansubCommunityLinksSection; loading="lazy" + sizes im Medien-Block; getFansubMediaCategoryLabel/getFansubLinkTypeLabel verdrahtet.
- Datei-Zeilenlimit ≤450 für alle .tsx.
- Live-Sichtprüfung folgt im UAT-Plan 99-18.
</verification>

<success_criteria>
- Zentrale deutsche Label-Maps vorhanden; Community-Links-Sektion und aufgewertete Medien-Sektion (Titel/Beschreibung/Typ-Tag, lazy/skeleton/sizes) existieren und sind testgesichert, ausschließlich über @/components/ui + Team4s-Tokens.
</success_criteria>

<output>
Erstelle `.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-16-SUMMARY.md` nach Abschluss.
</output>
