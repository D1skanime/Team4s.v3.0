---
phase: 99
plan: "24"
type: execute
wave: 17
depends_on:
  - "99-19"
  - "99-22"
files_modified:
  - "frontend/src/components/fansubs/FansubGroupMediaBlock.tsx"
  - "frontend/src/components/fansubs/FansubMediaSection.tsx"
  - "frontend/src/components/fansubs/FansubPublicSections.module.css"
  - "frontend/src/components/fansubs/__tests__/FansubGroupMediaBlock.test.tsx"
autonomous: true
requirements_addressed:
  - "AO6-11"
  - "AO6-04"
requirements:
  - "Phase 99 Add-on 6 CONTEXT AO6-11 (Medien: gekappte Vorschau + Überlauf + 2-Zeilen-Snippet)"
  - "Phase 99 Add-on 6 CONTEXT AO6-04 (ein klarer Header pro Sektion)"
must_haves:
  truths:
    - "[AO6-11] Die Medien-Vorschau ist auf 5 Thumbnails begrenzt; gibt es mehr, ist die letzte sichtbare Kachel eine Überlauf-Kachel '+X weitere', und ein 'Alle N anzeigen'-Button erweitert das Grid inline batchweise (kein Auto-Scroll)."
    - "[AO6-11] Jedes Medium zeigt Thumbnail, Titel, deutschen Typ-Tag (aus category) und ein 2-Zeilen-Beschreibungs-Snippet (line-clamp: 2); Bilder laden lazy mit Skeleton (feste Aspect-Ratio) und sizes."
    - "[AO6-11] Jedes Thumbnail ist ein anklickbares Element (Trigger-Seam onSelect(index)) für die spätere Lightbox — umgesetzt über das @/components/ui Button-Primitive, kein natives button."
    - "[AO6-04] Die Medien-Sektion hat genau einen klaren Header ('Medien'), ohne redundanten 'Gruppenmedien'-Zwischentitel."
  artifacts:
    - path: "frontend/src/components/fansubs/FansubGroupMediaBlock.tsx"
      provides: "Client-Medien-Grid: 5er-Vorschau, Überlauf-Kachel, Alle-anzeigen, Snippet-Clamp, klickbare Trigger"
      contains: "use client"
  key_links:
    - from: "frontend/src/components/fansubs/FansubGroupMediaBlock.tsx"
      to: "onSelect(index) Trigger-Seam"
      via: "@/components/ui Button je Thumbnail"
      pattern: "onSelect"
    - from: "frontend/src/components/fansubs/FansubPublicSections.module.css"
      to: "2-Zeilen-Snippet"
      via: "-webkit-line-clamp: 2 auf mediaDescription"
      pattern: "line-clamp"
---

<objective>
Medien-Sektion skalierbar machen (AO6-11): Vorschau auf 5 Thumbnails begrenzen mit „+X weitere"-Überlaufkachel und „Alle N anzeigen"-Inline-Erweiterung; je Medium Titel, deutscher Typ-Tag und 2-Zeilen-Beschreibungs-Snippet; lazy + Skeleton + sizes. Thumbnails als klickbare Trigger (Seam für die Lightbox in 99-25). Ein klarer Header (AO6-04).

Purpose: Bei 40 Medien darf die Seite nicht überladen sein; die Vorschau muss dicht bleiben und den Lightbox-Einstieg vorbereiten.
Output: Client-Medien-Block mit Vorschau-Kappung + Snippet + Trigger-Seam + CSS + Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON6-CONTEXT.md
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-16-SUMMARY.md

<interfaces>
<!-- Aktueller Stand (read-only verifiziert). -->

frontend/src/types/fansub.ts — PublicFansubMediaItem { id; media_type; caption?; mime_type; thumbnail_url?; original_url?; title?; description?; category }.
FansubGroupMediaBlock.tsx (aktuell Server): props { media }. Je Item Card variant='section' mit mediaThumbFrame (aspect-ratio 16/9) → next/image fill + sizes (MEDIA_IMAGE_SIZES='(max-width: 640px) 100vw, 220px') + loading='lazy' + unoptimized, sonst mediaThumbSkeleton. mediaCardBody: Titel (title→caption→media_type) + Badge getFansubMediaCategoryLabel(category) + optional mediaDescription. isImage prüft mime_type startsWith 'image/'. resolveApiUrl aus @/lib/api. EmptyState bei leer.
FansubMediaSection.tsx: `<section id="medien">` + SectionHeader title='Medien' + compactStack mit h3 'Gruppenmedien' (redundanter Zwischentitel → AO6-04) + FansubGroupMediaBlock.
FansubPublicSections.module.css: mediaItemGrid, mediaCard, mediaThumbFrame, mediaThumbSkeleton, mediaCardBody, mediaCardHeader, mediaLabel, mediaDescription (aktuell ohne line-clamp).
@/components/ui: Button (variant ghost/subtle für Alle-anzeigen und als Thumbnail-Trigger — kein natives button), Badge, Card, EmptyState.
CLAUDE.md: klickbare Elemente über @/components/ui Button; ESLint no-restricted-syntax warnt bei nativem <button>.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Medien-Grid als Client — 5er-Vorschau, Überlaufkachel, Alle-anzeigen, Snippet-Clamp, Trigger-Seam (AO6-11)</name>
  <files>frontend/src/components/fansubs/FansubGroupMediaBlock.tsx, frontend/src/components/fansubs/FansubPublicSections.module.css, frontend/src/components/fansubs/__tests__/FansubGroupMediaBlock.test.tsx</files>
  <read_first>
    - frontend/src/components/fansubs/FansubGroupMediaBlock.tsx (gesamt)
    - frontend/src/components/fansubs/FansubPublicSections.module.css:94-159 (media-Klassen)
    - frontend/src/components/fansubs/__tests__/FansubGroupMediaBlock.test.tsx (gesamt)
  </read_first>
  <behavior>
    - Bei ≤5 Medien werden alle gezeigt, keine Überlaufkachel.
    - Bei >5 Medien werden 5 Kacheln gezeigt, wobei die 5. eine Überlauf-Kachel '+X weitere' ist (X = Gesamt − 4 sichtbare Vorschaubilder); zusätzlich ein 'Alle N anzeigen'-Button.
    - Klick auf 'Alle N anzeigen' (oder Überlaufkachel) erweitert das Grid inline batchweise bis alle Medien sichtbar sind (kein Auto-Scroll, kein Nachladen).
    - Jedes Bild-Thumbnail ist ein klickbares Element, das onSelect(globalIndex) aufruft (Seam für Lightbox).
    - Jede Karte zeigt Titel, deutschen Typ-Tag (getFansubMediaCategoryLabel), und (falls description) ein auf 2 Zeilen geklemmtes Snippet.
    - Leere Liste → EmptyState.
  </behavior>
  <action>
    FansubGroupMediaBlock.tsx zu 'use client' machen. Props erweitern zu { media: PublicFansubMediaItem[]; onSelect?: (index: number) => void }. Konstante PREVIEW_LIMIT=5, MEDIA_BATCH (z.B. 10). State visibleCount initial PREVIEW_LIMIT. Rendering: die ersten visibleCount Medien im mediaItemGrid; wenn media.length > visibleCount, statt der letzten sichtbaren Karte eine Überlaufkachel '+X weitere' (X = media.length − (PREVIEW_LIMIT−1)) rendern, deren Klick visibleCount um MEDIA_BATCH (bzw. auf media.length) erhöht; zusätzlich unter dem Grid ein @/components/ui Button 'Alle N anzeigen' (N = media.length), der visibleCount = media.length setzt. Wenn alle sichtbar, Überlaufkachel/Button ausblenden.

    Thumbnails klickbar machen: den Bild-Frame in ein @/components/ui Button (variant ghost, className um Padding/Buttonoptik zu neutralisieren, siehe /dev/ui-system) wrappen, das bei Klick onSelect?.(globalIndex) mit dem Index im vollständigen media-Array aufruft; aria-label = Titel des Mediums. KEIN natives button. Für Nicht-Bild-Medien (kein image/* mime) den bestehenden 'Medium öffnen'-Link beibehalten (kein Lightbox-Trigger).

    Titel/Typ-Tag/Snippet: Titel (title→caption→media_type) und Badge getFansubMediaCategoryLabel(category) bleiben; mediaDescription rendert das Snippet; die Klasse mediaDescription um 2-Zeilen-Clamp erweitern.

    FansubPublicSections.module.css: mediaDescription um display:-webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden ergänzen. Klassen mediaOverflowTile (Überlaufkachel, 16:9-Fläche, zentrierter '+X weitere'-Text via Token) und mediaShowAll (Button-Ausrichtung) ergänzen. Nur Team4s-Tokens. Bestehende mediaThumbFrame/-Skeleton/sizes/lazy beibehalten.

    Test FansubGroupMediaBlock.test.tsx: bestehende Fälle (Titel/Beschreibung/Typ-Tag, lazy+sizes, Empty-State) beibehalten und ergänzen: bei 6 Medien wird eine '+2 weitere'-Überlaufkachel und ein 'Alle 6 anzeigen'-Button gerendert; nach Klick auf 'Alle anzeigen' sind alle 6 Kacheln sichtbar; onSelect wird mit dem korrekten Index aufgerufen (Klick auf ein Thumbnail).
  </action>
  <acceptance_criteria>
    - `grep -n "use client" frontend/src/components/fansubs/FansubGroupMediaBlock.tsx` vorhanden; `grep -n "onSelect" frontend/src/components/fansubs/FansubGroupMediaBlock.tsx` vorhanden.
    - `grep -Ev '^\s*//' frontend/src/components/fansubs/FansubGroupMediaBlock.tsx | grep -c "<button"` = 0 (nur @/components/ui Button).
    - `grep -n "line-clamp: 2" frontend/src/components/fansubs/FansubPublicSections.module.css` vorhanden.
    - `grep -n "weitere" frontend/src/components/fansubs/FansubGroupMediaBlock.tsx` und `grep -n "anzeigen" frontend/src/components/fansubs/FansubGroupMediaBlock.tsx` vorhanden.
    - Datei ≤450 Zeilen; `cd frontend && npx vitest run src/components/fansubs/__tests__/FansubGroupMediaBlock.test.tsx` grün.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/fansubs/__tests__/FansubGroupMediaBlock.test.tsx</automated>
  </verify>
  <done>5er-Vorschau mit '+X weitere'-Kachel und 'Alle N anzeigen'; 2-Zeilen-Snippet; lazy/Skeleton/sizes; klickbare Trigger via Button-Primitive; Tests grün.</done>
</task>

<task type="auto">
  <name>Task 2: Medien-Sektion — redundanten Zwischentitel entfernen (AO6-04)</name>
  <files>frontend/src/components/fansubs/FansubMediaSection.tsx</files>
  <read_first>
    - frontend/src/components/fansubs/FansubMediaSection.tsx (gesamt)
  </read_first>
  <action>
    FansubMediaSection.tsx: den redundanten h3-Zwischentitel 'Gruppenmedien' entfernen, sodass die Sektion genau einen klaren Header ('Medien' via SectionHeader) trägt und direkt das FansubGroupMediaBlock zeigt. Prop-Durchreichung von media (und ggf. eines onSelect-Platzhalters, falls die Sektion den Lightbox-State noch nicht besitzt — der Lightbox-State wird in 99-25 ergänzt; hier keine Lightbox verdrahten, nur den Zwischentitel entfernen und die bestehende Übergabe beibehalten).
  </action>
  <acceptance_criteria>
    - `grep -c "Gruppenmedien" frontend/src/components/fansubs/FansubMediaSection.tsx` = 0.
    - `grep -n "SectionHeader title=\"Medien\"" frontend/src/components/fansubs/FansubMediaSection.tsx` vorhanden.
    - `cd frontend && npm run typecheck` grün.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/fansubs && npm run typecheck</automated>
  </verify>
  <done>Medien-Sektion hat genau einen Header; kein 'Gruppenmedien'-Zwischentitel mehr.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run typecheck` und `npx vitest run src/components/fansubs` grün.
- Live: `docker restart team4sv30-frontend`, harter Reload `/fansubs/c-subs` (:3000) — Vorschau auf 5 begrenzt, '+X weitere'/'Alle anzeigen' erweitert inline; je Medium Titel + Typ-Tag + 2-Zeilen-Snippet; lazy/Skeleton; ein 'Medien'-Header. Klick auf Thumbnail ruft onSelect (noch ohne sichtbare Lightbox — Verdrahtung folgt in 99-25).
</verification>

<success_criteria>
- Vorschau auf 5 begrenzt mit '+X weitere' + 'Alle anzeigen' (inline, kein Auto-Scroll).
- Titel + Typ-Tag + 2-Zeilen-Snippet je Medium; lazy/Skeleton/sizes.
- Klickbare Thumbnail-Trigger (Button-Primitive) als Lightbox-Seam.
- Ein klarer Medien-Header.
</success_criteria>

<output>
Create `.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-24-SUMMARY.md` when done
</output>
