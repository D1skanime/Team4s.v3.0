---
phase: 99
plan: "14"
type: execute
wave: 10
depends_on:
  - "99-09"
  - "99-10"
  - "99-11"
  - "99-13"
files_modified:
  - "frontend/src/app/anime/[id]/group/[groupId]/page.module.css"
  - "frontend/src/app/fansubs/[slug]/page.module.css"
  - "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.module.css"
autonomous: false
requirements_addressed:
  - "AO4-01"
  - "AO4-14"
  - "AO4-21"
  - "AO4-22"
requirements:
  - "Phase 99 Add-on 4 Integration + UAT"
must_haves:
  truths:
    - "AO4-01: Die Mitglieder-/Release-/Projekt-Kennzahlen sind ueber Fansub-Gruppenseite, Projektseite und Release-Detailseite konsistent; kein ' 0 Mitglieder' bei real vorhandenem aktivem oeffentlichem Mitglied."
    - "AO4-14: Keine technische Fehlermeldung erscheint im oeffentlichen UI der drei Seiten."
    - "AO4-21/AO4-22: Nachladen (Infinite Scroll + Mehr laden) erzeugt keinen Layout-Sprung; Lazy Loading/Skeletons aktiv; Mobile <=390px und Desktop ohne horizontalen Bruch."
  artifacts:
    - path: "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.module.css"
      provides: "Scoped responsive Polish falls UAT Layout-Probleme findet"
  key_links:
    - from: "drei oeffentliche Seiten"
      to: "Live-Browser-UAT :3000"
      via: "Kennzahlen-Konsistenz + kein Layout-Sprung"
      pattern: "Mitglieder"
---

<objective>
Verifiziere Add-on 4 ganzheitlich: Kennzahlen-Konsistenz ueber die drei Seiten, keine technischen Fehlermeldungen, kein Layout-Sprung beim Nachladen, Mobile + Desktop. Nur scoped CSS-Polish bei gefundenen Problemen.

Purpose: Die Success Criteria (99-ADDON4-CONTEXT) verlangen konsistente Kennzahlen und sauberes Ladeverhalten ueber alle Seiten. Backend- und Frontend-Aenderungen muessen live zusammenspielen.

Output: Dokumentierte UAT-Ergebnisse, ggf. scoped CSS-Korrekturen, Nutzer-Freigabe.
</objective>

<context>
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON4-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fokussierte Integrationschecks + Kennzahlen-Konsistenz</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/page.module.css, frontend/src/app/fansubs/[slug]/page.module.css, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.module.css</files>
  <read_first>
    - Alle SUMMARYs 99-05 bis 99-13 (soweit vorhanden) fuer die geaenderten Flaechen.
    - `backend/internal/repository/group_repository.go` + `fansub_repository.go` — korrigierte Mitglieder-Zaehlung (99-05).
    - `frontend/src/app/fansubs/[slug]/page.tsx` — `heroStats` 'Mitglieder' (visibleTeamCount) und `frontend/src/app/anime/[id]/group/[groupId]/sections/HeroSection.tsx` — `group.stats.member_count`.
    - `frontend/package.json` — Skripte `test`, `typecheck`, `lint`.
  </read_first>
  <action>
    Baue das Backend neu (`docker compose up -d --build team4sv30-backend`), damit die korrigierten Kennzahlen (99-05) live sind. Fuehre die fokussierten Checks aus: relevante Backend-Repository-Tests (GroupStats/Members/ReleaseDetail/Cursor) und Frontend-Tests der geaenderten Flaechen plus `npm run typecheck`. Pruefe Kennzahlen-Konsistenz: fuer eine reale Gruppe/Projekt muss die 'Mitglieder'-Zahl auf Fansub-Gruppenseite (`heroStats`), Projektseite (`group.stats.member_count`) und - wo angezeigt - Release-Detailseite identisch sein und der Team-Listen-Laenge entsprechen. Verifiziere per Live-API-Token (Keycloak Direct-Grant, siehe Memory) die neuen Endpunkte (`/releases/:releaseVersionId`, `/release-list`, `/releases/:id/images`, `/releases/:id/notes`) auf HTTP 200 und plausible Kennzahlen. Behebe nur durch Add-on 4 verursachte Fehler.
  </action>
  <acceptance_criteria>
    - `cd backend; go test ./internal/repository -run "GroupStats|Members|ReleaseDetail|Cursor"` gruen (oder dokumentierte, unabhaengige Vorbestands-Fehler).
    - `cd frontend; npm run typecheck` fehlerfrei.
    - Kennzahlen-Konsistenz dokumentiert: identische Mitgliederzahl ueber die Seiten fuer eine reale Gruppe.
    - Neue Endpunkte liefern live HTTP 200 (Token-Verifikation dokumentiert).
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend; go test ./internal/repository -run "GroupStats|Members|ReleaseDetail|Cursor"</automated>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run typecheck</automated>
  </verify>
  <done>Automatisierte Checks gruen, Kennzahlen-Konsistenz und Endpunkt-Erreichbarkeit belegt.</done>
</task>

<task type="auto">
  <name>Task 2: Live-Browser-Verifikation Mobile + Desktop mit scoped Polish</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/page.module.css, frontend/src/app/fansubs/[slug]/page.module.css, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.module.css</files>
  <read_first>
    - `.claude/projects/.../memory` Live-Testing-Hinweis: :3000 = Dev (Turbopack), HMR greift NICHT: nach Frontend-Edit `docker restart team4sv30-frontend` + Strg+F5; echte Host-Ports via `docker ps`.
    - Die drei Seiten-Routen und ihre neuen Sektionen/Komponenten aus 99-09 bis 99-13.
  </read_first>
  <action>
    Starte/nutze den Dev-Server auf :3000. Oeffne fuer eine reale Gruppe mit Daten: Fansub-Gruppenseite (`/fansubs/[slug]`), Projektseite (`/anime/[id]/group/[groupId]`) und eine Release-Detailseite (`/anime/[id]/group/[groupId]/releases/[releaseVersionId]`). Gib die exakten URLs im Summary an. Teste bei <=390px und Desktop:
    - Kennzahlen konsistent, kein '0 Mitglieder' bei realem Mitglied;
    - keine technische Fehlermeldung (kein 'Subgroups-Ordner'-Text, kein Backend-Jargon);
    - neuestes Release eingebettet mit Vorschau + Detail-Link; Detailseite mit Galerie (Typ-Tag+Autor), Textliste, OP/ED/Middle-Timeline ohne Player;
    - Infinite Scroll + 'Mehr laden' funktionieren; kein Layout-Sprung beim Nachladen (Skeletons/feste Aspect-Ratio);
    - kein horizontaler Overflow auf Mobile.
    Bei Layout-Problemen nur scoped CSS-Edits in den gelisteten `page.module.css`-Dateien (keine Redesigns, keine neuen Tokens, keine Contract-Aenderungen).
  </action>
  <acceptance_criteria>
    - Mobile <=390px und Desktop ohne Clipping/Overlap/horizontalen Bruch (dokumentiert mit URLs + Notizen/Screenshots).
    - Nachladen ohne Layout-Sprung; Mehr-laden-Button sichtbar/funktional.
    - Etwaige Korrekturen nur in den gelisteten CSS-Dateien.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run typecheck</automated>
  </verify>
  <done>Mobile/Desktop-UAT bestanden oder scoped CSS-Korrekturen umgesetzt und geprueft.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Nutzer-Freigabe fuer die drei oeffentlichen Seiten</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.tsx</files>
  <read_first>
    - Die im Task 2 dokumentierten Live-URLs.
  </read_first>
  <action>Pausiere nach der automatisierten Mobile/Desktop-Verifikation, damit der Nutzer die drei Seiten live im Browser prueft. Markiere Add-on 4 erst als abgeschlossen, wenn der Nutzer freigibt oder die gemeldeten scoped Probleme behoben sind.</action>
  <what-built>Fansub-Gruppenseite, Projektseite und die neue Release-Detailseite wurden ueberarbeitet: konsistente Kennzahlen, keine technischen Fehlermeldungen, eingebettetes neuestes Release, neue Release-Detailseite mit Galerie/Textliste/Timeline, Cursor-Infinite-Scroll + Mehr laden, Lazy/Skeleton/srcset.</what-built>
  <how-to-verify>
    1. Oeffne die vom Executor genannten Live-URLs auf :3000 (nach `docker restart team4sv30-frontend` + Strg+F5).
    2. Pruefe konsistente Mitgliederzahlen ueber alle drei Seiten (kein '0' bei realem Mitglied).
    3. Bestaetige: keine technische Fehlermeldung, eingebettetes neuestes Release mit Detail-Link, Detailseite mit Galerie (Typ-Tag+Autor), Textliste, OP/ED/Middle-Timeline ohne Player.
    4. Teste Nachladen (Scroll + 'Mehr laden') ohne Layout-Sprung, auf Desktop und Mobilbreite.
  </how-to-verify>
  <acceptance_criteria>
    - Nutzer gibt frei ("approved") oder alle gemeldeten Punkte sind in scoped Dateien behoben und re-verifiziert.
  </acceptance_criteria>
  <resume-signal>Schreibe "approved" oder beschreibe die konkreten Probleme.</resume-signal>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run typecheck</automated>
  </verify>
  <done>Nutzer bestaetigt die drei Seiten live oder alle Add-on-4-Punkte sind behoben.</done>
</task>

</tasks>

<verification>
- Backend-/Frontend-Fokustests, `npm run typecheck`.
- Live-UAT :3000 Mobile <=390px + Desktop.
- Grep-Gate 'Subgroups-Ordner' bleibt leer.
</verification>

<success_criteria>
- Kennzahlen konsistent, keine technischen Meldungen, kein Layout-Sprung, Nutzer-Freigabe erteilt.
</success_criteria>

<output>
Erstelle `.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-14-SUMMARY.md` nach Abschluss.
</output>
