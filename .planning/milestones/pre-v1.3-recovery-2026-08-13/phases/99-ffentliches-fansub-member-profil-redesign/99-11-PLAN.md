---
phase: 99
plan: "11"
type: execute
wave: 8
depends_on:
  - "99-07"
  - "99-08"
  - "99-10"
files_modified:
  - "frontend/src/app/anime/[id]/group/[groupId]/page.tsx"
  - "frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.tsx"
  - "frontend/src/app/anime/[id]/group/[groupId]/sections/LatestReleaseSection.tsx"
  - "frontend/src/app/anime/[id]/group/[groupId]/sections/LatestReleaseSection.module.css"
  - "frontend/src/app/anime/[id]/group/[groupId]/sections/OlderReleasesList.tsx"
  - "frontend/src/app/anime/[id]/group/[groupId]/sections/OlderReleasesList.module.css"
autonomous: true
requirements_addressed:
  - "AO4-11"
  - "AO4-12"
  - "AO4-13"
  - "AO4-21"
  - "AO4-22"
  - "AO4-23"
  - "AO4-25"
requirements:
  - "Phase 99 Add-on 4 AO4-11, AO4-12, AO4-13, AO4-21, AO4-22, AO4-23, AO4-25"
must_haves:
  truths:
    - "AO4-11: Das neueste Release ist auf der Projektseite eingebettet mit Titel, Datum, Kennzahlen (Bilder/Texte/Beteiligte-Anzahl), 3-4 Bild-Vorschauen (+X-Kachel fuer den Rest), 2 Text-Vorschauen, Beteiligten-Avataren und Link 'Vollstaendiges Release ansehen' zur Release-Detailseite."
    - "AO4-12: Aeltere Releases erscheinen als kompakte Liste (Name, Bild-/Text-Anzahl, Link), kein Vorschau-Overload; Nachladen via Infinite Scroll (Cursor) plus 'Mehr laden'-Button."
    - "AO4-13: Reihenfolge Hero -> Aktionen -> Geschichte (nur mit Inhalt) -> Neuestes Release -> Weitere Releases -> Sammel-Hinweis fuer leere Sektionen -> Mehr entdecken."
    - "AO4-21: Infinite Scroll wird nur fuer die aeltere Release-Liste genutzt, nicht auf der Hauptseite selbst."
    - "AO4-22: Vorschaubilder nutzen loading='lazy' und Skeleton/feste Aspect-Ratio-Boxen gegen Layout-Spruenge."
    - "AO4-23: Vorschaubilder liefern srcset/sizes, damit mobile Clients keine Desktop-Aufloesung laden."
    - "AO4-25: Zusaetzlich zum automatischen Nachladen existiert ein manueller 'Mehr laden'-Button."
  artifacts:
    - path: "frontend/src/app/anime/[id]/group/[groupId]/sections/LatestReleaseSection.tsx"
      provides: "Eingebettete Neuestes-Release-Vorschau"
    - path: "frontend/src/app/anime/[id]/group/[groupId]/sections/OlderReleasesList.tsx"
      provides: "Infinite-Scroll-Liste aelterer Releases"
      contains: "Mehr laden"
  key_links:
    - from: "frontend/src/app/anime/[id]/group/[groupId]/sections/LatestReleaseSection.tsx"
      to: "getGroupReleaseDetail"
      via: "Aggregierender Public-Release-Endpoint (AO4-02)"
      pattern: "getGroupReleaseDetail"
    - from: "frontend/src/app/anime/[id]/group/[groupId]/sections/OlderReleasesList.tsx"
      to: "getGroupReleaseListCursor"
      via: "Cursor-Nachladen (AO4-24)"
      pattern: "getGroupReleaseListCursor"
---

<objective>
Baue die neuestes-Release-Vorschau (AO4-11), die kompakte Liste aelterer Releases mit Cursor-Infinite-Scroll (AO4-12) und ordne die Projektseite gemaess AO4-13.

Purpose: Die Projektseite soll das aktuellste Release direkt zeigen und aeltere Releases leichtgewichtig nachladen, mit Link auf die neue Detailseite.

Output: Zwei neue Sektions-Komponenten, angepasste ReleasesSection und neu geordnete page.tsx.
</objective>

<context>
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON4-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: LatestReleaseSection (eingebettetes neuestes Release)</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/sections/LatestReleaseSection.tsx, frontend/src/app/anime/[id]/group/[groupId]/sections/LatestReleaseSection.module.css</files>
  <read_first>
    - `frontend/src/app/anime/[id]/group/[groupId]/page.tsx:58-118` — `releaseEpisodes` (aus `getGroupReleases`, aufsteigend nach episode_number) und die bestehende Sektions-Komposition.
    - `frontend/src/lib/api.ts` — `getGroupReleaseDetail` (aus 99-07) und Rueckgabetyp aus `frontend/src/types/releaseDetail.ts`.
    - `frontend/src/types/releaseVersionMedia.ts:14-19` — `CATEGORY_LABELS` fuer Typ-Anzeige.
    - Design-System: `@/components/ui` (Pflicht), Showcase `/dev/ui-system`.
  </read_first>
  <action>
    Erstelle `LatestReleaseSection`, die zum neuesten Release (hoechste episode_number bzw. juengster Eintrag aus `releaseEpisodes`) via `getGroupReleaseDetail(animeID, groupID, releaseVersionID)` das aggregierte Payload laedt und rendert: Titel, Veroeffentlichungsdatum, Kennzahlen `images_count`/`notes_count`/`contributors_count`, 3-4 Bild-Vorschauen mit einer "+X"-Kachel fuer den Rest (X = images_count - angezeigte), 2 Text-Vorschauen (Autor + gekuerzter Inhalt), Beteiligten-Avatare (Name), und einen Link "Vollstaendiges Release ansehen" auf `/anime/${animeID}/group/${groupID}/releases/${releaseVersionID}` (Detailseite aus 99-12). Bild-Vorschauen: `loading="lazy"`, feste Aspect-Ratio-Box (Skeleton-Platzhalter) und `srcset`/`sizes` (AO4-22/23). KEIN Infinite Scroll hier (AO4-21). Nutze `@/components/ui`-Primitives (Button/Card etc.), keine nativen `<button>`; nur Team4s-Tokens; korrekte Umlaute. Datei <=450 Zeilen.
  </action>
  <acceptance_criteria>
    - Komponente ruft `getGroupReleaseDetail` und rendert Kennzahlen, 3-4 Bilder mit +X-Kachel, 2 Texte, Beteiligten-Avatare und den Detail-Link mit korrektem Pfad.
    - Vorschaubilder haben `loading="lazy"`, `sizes` und (bei mehreren Aufloesungen) `srcSet`; feste Aspect-Ratio-Box im CSS.
    - Kein `IntersectionObserver`/Infinite-Scroll in dieser Komponente.
    - `cd frontend; npm run typecheck` fehlerfrei.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run typecheck</automated>
  </verify>
  <done>Neuestes Release ist eingebettet und verlinkt auf die Detailseite.</done>
</task>

<task type="auto">
  <name>Task 2: OlderReleasesList (Cursor-Infinite-Scroll + Mehr laden)</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/sections/OlderReleasesList.tsx, frontend/src/app/anime/[id]/group/[groupId]/sections/OlderReleasesList.module.css</files>
  <read_first>
    - `frontend/src/lib/api.ts` — `getGroupReleaseListCursor` (aus 99-08) und `CursorPage<T>` aus `releaseDetail.ts`.
    - `frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.tsx` — bestehendes leichtgewichtiges Release-Listen-Markup als Stil-Referenz.
    - Design-System `@/components/ui` (Button Pflicht fuer 'Mehr laden').
  </read_first>
  <action>
    Erstelle eine Client-Komponente ('use client') `OlderReleasesList`, die aeltere Releases (alle ausser dem neuesten) kompakt listet: pro Eintrag Name/Episode, Bild-Anzahl, Text-Anzahl und Link zur jeweiligen Release-Detailseite. Erste Seite via Prop oder initialem `getGroupReleaseListCursor`-Aufruf; weitere Seiten via Cursor (`next_cursor`/`has_more`). Automatisches Nachladen per `IntersectionObserver` am Listenende UND ein manueller `@/components/ui` `Button` "Mehr laden" als Fallback (AO4-25). Waehrend des Ladens Skeleton-Platzhalter mit fester Hoehe (kein Layout-Sprung, AO4-22). Kein Vorschau-Overload (keine Bild-Galerie pro Eintrag). Nur Team4s-Tokens; Umlaute korrekt; Datei <=450 Zeilen.
  </action>
  <acceptance_criteria>
    - Komponente nutzt `getGroupReleaseListCursor` und rendert bei `has_more` sowohl automatisches Nachladen (IntersectionObserver) als auch einen sichtbaren "Mehr laden"-Button.
    - Jeder Eintrag zeigt Bild-/Text-Anzahl und einen Link auf `.../releases/${releaseVersionID}`.
    - Kein per-Eintrag-Bildergrid (kompakte Liste).
    - `cd frontend; npm run typecheck` fehlerfrei; falls Test-Harness vorhanden, ein Render-/Logik-Test fuer 'Mehr laden'.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run typecheck</automated>
  </verify>
  <done>Aeltere Releases laden per Cursor nach, mit Auto- und Mehr-laden-Modus.</done>
</task>

<task type="auto">
  <name>Task 3: page.tsx neu ordnen und Sektionen verdrahten</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/page.tsx, frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.tsx</files>
  <read_first>
    - `frontend/src/app/anime/[id]/group/[groupId]/page.tsx:102-119` — aktuelle Render-Reihenfolge (Hero, GroupSectionsNav, Story, Team, Releases, Themes, Media, Backlinks).
    - Ergebnisse aus Task 1/2 dieses Plans (neue Sektionskomponenten) und AO4-07-Muster des Sammel-Hinweises aus `fansubs/[slug]/page.tsx`.
  </read_first>
  <action>
    Ordne die Projektseite gemaess AO4-13: Hero -> Aktionen -> Geschichte (nur bei Inhalt) -> Neuestes Release (`LatestReleaseSection`) -> Weitere Releases (`OlderReleasesList`) -> Sammel-Hinweis fuer leere Sektionen -> Mehr entdecken (Backlinks). Ersetze bzw. verdrahte die bestehende `ReleasesSection`-Nutzung so, dass nicht mehr die generische 5er-Liste dominiert, sondern das neueste Release eingebettet und der Rest als `OlderReleasesList` erscheint. Geschichte nur bei vorhandenem Inhalt rendern (analog `hasStoryContent`). Fuehre einen einzigen Sammel-Hinweis fuer tatsaechlich leere Bereiche ein (kein Einzel-Rendern leerer Sektionen), konsistent mit AO4-07. Reiche die neueste `releaseVersionID` und die uebrigen Releases an die neuen Komponenten durch. Keine neuen Farb-/CSS-Tokens; `@/components/ui`-Primitives; Umlaute korrekt.
  </action>
  <acceptance_criteria>
    - `page.tsx` rendert in der Reihenfolge Hero -> Aktionen -> Geschichte(bedingt) -> LatestReleaseSection -> OlderReleasesList -> Sammel-Hinweis -> Backlinks.
    - Kein separates Rendern leerer Sektionen ausserhalb eines Sammel-Hinweises.
    - `cd frontend; npm run typecheck` fehlerfrei; `npm run test -- src/app/anime` gruen falls Tests existieren.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run typecheck</automated>
  </verify>
  <done>Projektseite folgt AO4-13 mit eingebettetem neuestem und nachladbaren aelteren Releases.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`, ggf. `npm run test -- src/app/anime`.
- Live-Check via :3000 in 99-14 (nach Edit `docker restart team4sv30-frontend` + Strg+F5); kein Layout-Sprung beim Nachladen.
</verification>

<success_criteria>
- Neuestes Release eingebettet mit Vorschau + Detail-Link; aeltere Releases als kompakte Cursor-Liste; korrekte Reihenfolge; Infinite Scroll nur an der aelteren Liste; Mehr-laden-Fallback vorhanden.
</success_criteria>

<output>
Erstelle `.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-11-SUMMARY.md` nach Abschluss.
</output>
