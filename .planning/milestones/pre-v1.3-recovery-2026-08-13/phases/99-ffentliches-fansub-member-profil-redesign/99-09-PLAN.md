---
phase: 99
plan: "09"
type: execute
wave: 7
depends_on: []
files_modified:
  - "frontend/src/app/fansubs/[slug]/page.tsx"
  - "frontend/src/components/fansubs/FansubHeroSection.tsx"
  - "frontend/src/components/fansubs/FansubStorySection.tsx"
autonomous: true
requirements_addressed:
  - "AO4-06"
  - "AO4-07"
requirements:
  - "Phase 99 Add-on 4 AO4-06, AO4-07"
must_haves:
  truths:
    - "AO4-06: Die Fansub-Gruppenseite scrollt durchgehend in der Reihenfolge Hero -> Laufende Projekte -> Team -> Geschichte -> Erfolge; Kennzahlen (Projekte/Releases/Mitglieder) erscheinen als kompakte Stat-Zeile im Hero statt als separate Leerkarten."
    - "AO4-07: Leere Sektionen (Externe Mitwirkende, Medien, Gruppenleitung, Mehr) werden nicht einzeln gerendert, sondern in genau einem Sammel-Hinweis am Ende und nur falls tatsaechlich etwas fehlt."
    - "AO4-07: Die Geschichte wird nur gerendert, wenn Text vorhanden ist."
  artifacts:
    - path: "frontend/src/app/fansubs/[slug]/page.tsx"
      provides: "Durchgehender Scroll, Stat-Zeile, Sammel-Hinweis"
      contains: "emptyAreaLabels"
  key_links:
    - from: "frontend/src/app/fansubs/[slug]/page.tsx"
      to: "FansubHeroSection stats"
      via: "heroStats mit Projekte/Release-Versionen/Mitglieder"
      pattern: "heroStats"
---

<objective>
Verifiziere und finalisiere die oeffentliche Fansub-Gruppenseite gegen AO4-06/AO4-07. Der aktuelle Stand (`fix(99): streamline public fansub group page`) setzt Reihenfolge, Stat-Zeile und Sammel-Hinweis bereits weitgehend um; dieser Plan schliesst verbleibende Luecken und sichert das Verhalten mit Tests ab.

Purpose: Kein separater Leerkarten-Wildwuchs, konsistente Kennzahlen im Hero, ein einziger Sammel-Hinweis fuer leere Bereiche.

Output: Bestaetigte/nachgezogene Komposition plus Regressionstest.
</objective>

<context>
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON4-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reihenfolge, Stat-Zeile und Sammel-Hinweis pruefen/nachziehen</name>
  <files>frontend/src/app/fansubs/[slug]/page.tsx, frontend/src/components/fansubs/FansubHeroSection.tsx, frontend/src/components/fansubs/FansubStorySection.tsx</files>
  <read_first>
    - `frontend/src/app/fansubs/[slug]/page.tsx` — aktuelle Komposition: `heroStats` (Anime-Projekte/Release-Versionen/Mitglieder), `buildEmptyAreaLabels`, bedingtes Rendern (`profile.projects.length > 0`, `hasTeam`, `storyAvailable`, `hasHistory`, `emptyAreaLabels`).
    - `frontend/src/components/fansubs/FansubHeroSection.tsx` — Darstellung der Stat-Zeile.
    - `frontend/src/components/fansubs/FansubTeamSection.tsx:14` — `countVisibleTeamMembers` (Mitglieder-Zaehlung = aktive/oeffentliche Mitglieder).
    - `frontend/src/components/fansubs/FansubStorySection.tsx` — Story-Rendering; `hasStoryContent`-Gate in page.tsx.
  </read_first>
  <action>
    Pruefe die Komposition gegen AO4-06/07. Falls bereits konform: keine funktionale Aenderung, nur Test ergaenzen. Falls Abweichungen bestehen, ziehe nach:
    - Render-Reihenfolge exakt Hero -> Laufende Projekte -> Team -> Geschichte -> Erfolge (dann Sammel-Hinweis).
    - `heroStats` enthaelt genau die drei Kennzahlen Projekte, Release-Versionen, Mitglieder; keine separate Leerkarte fuer diese Werte.
    - `buildEmptyAreaLabels` bleibt die einzige Quelle des Sammel-Hinweises; keine der Leer-Sektionen (Externe Mitwirkende, Medien, Gruppenleitung, Mehr) wird einzeln gerendert.
    - Geschichte nur bei `hasStoryContent`.
    Keine neuen CSS-Klassen/Farbwerte ausserhalb des Tokensystems; globale `@/components/ui`-Primitives fuer jede neue user-facing UI. Korrekte Umlaute.
  </action>
  <acceptance_criteria>
    - `page.tsx` rendert die Sektionen in der genannten Reihenfolge (Source-Assertion) und enthaelt genau einen `emptyAreaLabels`-Sammel-Hinweis.
    - Kein separater Render-Block fuer eine der vier Leer-Sektionen ausserhalb von `buildEmptyAreaLabels`.
    - `cd frontend; npm run typecheck` fehlerfrei.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run typecheck</automated>
  </verify>
  <done>Gruppenseite erfuellt AO4-06/07 nachweislich.</done>
</task>

<task type="auto">
  <name>Task 2: Regressionstest fuer Reihenfolge und Leerfall-Sammel-Hinweis</name>
  <files>frontend/src/app/fansubs/[slug]/page.tsx</files>
  <read_first>
    - `frontend/src/components/fansubs/__tests__/` — vorhandene Test-Muster fuer Fansub-Komponenten (Setup, Mocks, jsdom-Direktive).
    - `frontend/vitest.config.ts` — Pfad-Alias `@` und Test-Umgebung.
  </read_first>
  <action>
    Ergaenze einen fokussierten Test (co-located oder unter `__tests__`), der die Datenlogik absichert: Bei einem Profil mit Projekten aber ohne Team/Story/History erscheint der Sammel-Hinweis mit den passenden Labels, und Team/Story/History-Bloecke werden NICHT gerendert. Falls das Rendern der Server-Komponente im Test unpraktisch ist, teste stattdessen `buildEmptyAreaLabels`/`hasStoryContent` als exportierte Hilfsfunktionen (bei Bedarf export ergaenzen) mit klaren Eingabe->Ausgabe-Faellen (z. B. leere projects -> Label "Laufende Projekte" enthalten; Story mit body_html -> hasStoryContent true).
  </action>
  <acceptance_criteria>
    - Neuer Test deckt mindestens: leere Bereiche -> korrekte Labels im Sammel-Hinweis; Story mit Inhalt -> true, ohne Inhalt -> false.
    - `cd frontend; npm run test -- src/app/fansubs` laeuft gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run test -- src/app/fansubs</automated>
  </verify>
  <done>Leerfall- und Reihenfolge-Verhalten ist testgesichert.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`, fokussierter Fansub-Test.
- Live-Check via :3000 spaeter in 99-14 (Dev-Modus: nach Edit `docker restart team4sv30-frontend` + Strg+F5).
</verification>

<success_criteria>
- Durchgehender Scroll mit korrekter Reihenfolge, Stat-Zeile im Hero, ein Sammel-Hinweis, Geschichte nur mit Text.
</success_criteria>

<output>
Erstelle `.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-09-SUMMARY.md` nach Abschluss.
</output>
