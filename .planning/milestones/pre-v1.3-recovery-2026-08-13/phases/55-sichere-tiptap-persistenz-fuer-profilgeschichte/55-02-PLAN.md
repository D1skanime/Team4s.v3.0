---
phase: 55-sichere-tiptap-persistenz-fuer-profilgeschichte
plan: 02
type: execute
wave: 2
depends_on:
  - 55-01
files_modified:
  - frontend/src/types/profile.ts
  - frontend/src/lib/api.ts
  - frontend/src/app/me/profile/page.tsx
  - frontend/src/app/me/profile/page.test.tsx
  - frontend/src/app/me/profile/components/ProfileStoryCard.tsx
  - frontend/src/app/me/profile/components/profileFormTypes.ts
autonomous: true
requirements:
  - MEMBER-PROFILE-STORY-RICH-TEXT-01

must_haves:
  truths:
    - "D-14: Nach erfolgreichem Speichern erscheint die Profilgeschichte nicht dauerhaft als Editor"
    - "D-15: Standardzustand ist Lesemodus mit gerendertem Rich Text wie spaetere Public-Darstellung"
    - "D-16: Editor, Toolbar, Tabs und Bearbeitungscontrols erscheinen nur im expliziten Bearbeitungsmodus"
    - "D-17: Bearbeitungsmodus startet ueber einen klaren Bearbeiten-Button"
    - "D-18: Nach Save kehrt die Story-Card in den Lesemodus zurueck und zeigt den gespeicherten Zustand"
    - "D-19: Der falsche Plain-Text-Speicherhinweis wird entfernt"
    - "D-20: Keine breite Profil-Hub-UX-Neugestaltung; nur Story-Card-Verhalten und Story-Texte"
    - "D-25: Geschuetzte Profilansicht und Speichern funktionieren mit Refresh-Session auch ohne Access Token"
    - "D-26: UI-Code bleibt tokenfrei und nutzt frontend/src/lib/api.ts"
    - "D-27: Keycloak-Return-Refresh ueberschreibt ungespeicherte Profilgeschichte nicht"
    - "D-28: Dirty-State erkennt Rich-Text-Aenderungen und setzt nach erfolgreichem Save zurueck"
  artifacts:
    - path: "frontend/src/app/me/profile/components/ProfileStoryCard.tsx"
      provides: "Story-Lese-/Bearbeitungsmodus mit Bearbeiten-Button"
      contains: "RichTextRenderer"
    - path: "frontend/src/app/me/profile/page.tsx"
      provides: "Rich-Text-Form-State und Save-Payload ohne Plain-Text-Konvertierungsverlust"
      contains: "member_story_json"
    - path: "frontend/src/app/me/profile/page.test.tsx"
      provides: "Regressionen für Rich-Text-Erhalt, Lesemodus, Edit-Modus und Dirty-State"
      contains: "Meine Fansub-Geschichte Editor"
---

<objective>
Frontend-Profilgeschichte auf den neuen TipTap-Contract umstellen und den gewünschten Lesemodus/Bearbeitungsmodus umsetzen.

Zweck: Nach Plan 01 existiert der Backend-/Contract-Speicherpfad. Plan 02 entfernt die lokale Plain-Text-Konvertierung aus `/me/profile`, nutzt die neuen DTO-Felder und stellt sicher, dass nach dem Speichern ein gerenderter Zustand sichtbar ist.
</objective>

<execution_context>
@C:\Users\admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/55-sichere-tiptap-persistenz-fuer-profilgeschichte/55-CONTEXT.md
@docs/frontend/auth-api-client.md
@docs/api/api-contracts.md
@shared/contracts/openapi.yaml

<interfaces>
Plan 01 muss abgeschlossen sein und den finalen Profil-Story-Feldnamen in OpenAPI/Backend festgelegt haben. Dieser Plan muss exakt diese Namen übernehmen.

Aktueller Fehler:
- `frontend/src/app/me/profile/page.tsx` hat `richTextFromPlainText` und `richTextToPlainText`.
- `handleSubmit` sendet `member_story: richTextToPlainText(form.memberStory) || null`.
- Dadurch verschwinden H1-H3, Farben, Tabellen und andere TipTap-Strukturen.

Gewünschter Zustand:
- Form-State hält TipTap JSON aus dem API-Feld.
- Save sendet TipTap JSON.
- Lesemodus rendert `member_story_html`.
- Bearbeiten-Button zeigt Editor.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Frontend-Tests für Rich-Text-Erhalt und Story-Lesemodus schreiben</name>
  <files>frontend/src/app/me/profile/page.test.tsx</files>
  <read_first>
    frontend/src/app/me/profile/page.test.tsx
    frontend/src/app/me/profile/page.tsx
    frontend/src/app/me/profile/components/ProfileStoryCard.tsx
    frontend/src/components/editor/RichTextRenderer.tsx
  </read_first>
  <action>
    Ergänze Tests vor der Umsetzung:
    - Profil lädt mit `member_story_json` und `member_story_html`; initial ist kein Editor sichtbar, sondern gerenderter Rich Text.
    - Klick auf `Bearbeiten` zeigt den RichTextEditor.
    - Ändern der Story setzt Dirty-State und aktiviert Speichern.
    - Save sendet das TipTap JSON im neuen Contract-Feld und sendet nicht mehr nur `member_story` als Plain-Text-Konvertierung.
    - Nach erfolgreichem Save wird wieder Lesemodus angezeigt.
    - Access Token fehlt, Refresh-Session vorhanden: Profil lädt weiterhin und Story-Lesemodus funktioniert.
    - Keycloak-Return-Refresh überschreibt eine ungespeicherte Rich-Text-Story nicht.

    Nutze den bestehenden `RichTextEditor`-Mock und ergänze einen `RichTextRenderer`-Mock, der bodyHtml sichtbar ausgibt. Die Tests dürfen zunächst rot sein, bis Tasks 2-4 umgesetzt sind.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run "src/app/me/profile/page.test.tsx"</automated>
  </verify>
  <done>Tests beschreiben Rich-Text-Erhalt, Lesemodus/Editmodus und Auth-Refresh-Session-Verhalten.</done>
</task>

<task type="auto">
  <name>Task 2: Profil-DTOs und API-Helper auf Rich-Text-Vertrag angleichen</name>
  <files>frontend/src/types/profile.ts, frontend/src/lib/api.ts</files>
  <read_first>
    frontend/src/types/profile.ts
    frontend/src/lib/api.ts
    shared/contracts/openapi.yaml
  </read_first>
  <action>
    Übernehme exakt die in Plan 01/OpenAPI festgelegten Felder in `frontend/src/types/profile.ts`.

    Mindestanforderung:
    - Response enthält TipTap JSON für Bearbeitung.
    - Response enthält serverseitig sanitisiertes HTML für Anzeige.
    - Plain Text bleibt als Kompatibilitäts-/Preview-Feld erhalten.
    - Update-Payload akzeptiert TipTap JSON.

    `frontend/src/lib/api.ts` darf keine neue geschützte Fetch-Seam einführen. `getOwnProfile` und `updateOwnProfile` bleiben zentrale API-Helfer. Falls Mapping nötig ist, zentral dort lösen, nicht in Page-Code.
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck</automated>
  </verify>
  <done>Frontend-Typen und API-Helper matchen OpenAPI und Backend-Vertrag.</done>
</task>

<task type="auto">
  <name>Task 3: ProfileStoryCard auf Lesemodus/Bearbeitungsmodus umbauen</name>
  <files>frontend/src/app/me/profile/components/ProfileStoryCard.tsx</files>
  <read_first>
    frontend/src/app/me/profile/components/ProfileStoryCard.tsx
    frontend/src/components/editor/RichTextEditor.tsx
    frontend/src/components/editor/RichTextRenderer.tsx
    frontend/src/components/ui
  </read_first>
  <action>
    Baue `ProfileStoryCard` so um, dass sie zwei Zustände unterstützt:
    - Lesemodus: `RichTextRenderer` rendert serverseitig sanitisiertes HTML; Toolbar/Tabs/Editor sind nicht sichtbar.
    - Bearbeitungsmodus: `RichTextEditor` wird angezeigt; Änderung ruft `onChange`.

    Füge einen klaren `Bearbeiten`-Button hinzu. Verwende vorhandene UI-Komponenten (`Button`, `FormField`, `SectionHeader` o. ä.) statt lokaler Sondercontrols, sofern passend.

    Entferne den falschen Hinweis, dass die Geschichte weiter als Plain Text gespeichert wird.

    Führe keine breite Profil-Hub-UX-Neugestaltung durch. Keine neuen Activity-/Media-Flächen.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run "src/app/me/profile/page.test.tsx"</automated>
  </verify>
  <done>Story-Card zeigt nach Save/Load gerenderten Rich Text und den Editor nur nach `Bearbeiten`.</done>
</task>

<task type="auto">
  <name>Task 4: Profilseite Save-/Load-/Dirty-State auf TipTap JSON umstellen</name>
  <files>frontend/src/app/me/profile/page.tsx, frontend/src/app/me/profile/components/profileFormTypes.ts</files>
  <read_first>
    frontend/src/app/me/profile/page.tsx
    frontend/src/app/me/profile/components/profileFormTypes.ts
    frontend/src/app/me/profile/components/ProfileStoryCard.tsx
    frontend/src/lib/api.ts
    frontend/src/types/profile.ts
  </read_first>
  <action>
    Entferne die verlustreiche `richTextToPlainText`-Save-Konvertierung für die Profilgeschichte. Behalte höchstens eine kleine Fallback-Funktion, um alte Plain-Text-Antworten in ein minimales TipTap-Doc zu verwandeln, falls der Backend-Contract dies für Kompatibilität verlangt.

    `toFormState` muss `member_story_json` aus der API übernehmen. Wenn kein JSON existiert, darf aus `member_story`/Plain Text ein minimales TipTap-Doc entstehen.

    `handleSubmit` muss TipTap JSON im neuen Update-Feld senden. Keine H1-/Farb-/Tabellen-Information darf absichtlich zu Plain Text reduziert werden.

    Nach erfolgreichem Save:
    - `applyProfile` synchronisiert die Response.
    - Dirty-State wird zurückgesetzt.
    - Story-Card kehrt in den Lesemodus zurück.

    Keycloak-Return-Refresh darf bei dirty Form weiterhin nicht synchronisieren.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run "src/app/me/profile/page.test.tsx"</automated>
    <automated>cd frontend && npm run typecheck</automated>
  </verify>
  <done>Profilseite speichert TipTap JSON, lädt es wieder und schützt Dirty-State beim Refresh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| UI Editor -> API | Frontend sendet JSON, nicht HTML |
| API Response -> Renderer | Renderer zeigt nur serverseitig sanitisiertes HTML |
| Auth session -> protected page | UI nutzt `hasAccessToken || hasRefreshToken`, nicht Access Token allein |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-FE-01 | Data Loss | `handleSubmit` | mitigate | TipTap JSON direkt senden; Tests prüfen Tabellen/Farben/Headings bleiben im Payload |
| T-55-FE-02 | XSS | `RichTextRenderer` | mitigate | Nur serverseitiges `member_story_html` an Renderer geben; kein clientseitiges HTML aus Editorzustand rendern |
| T-55-FE-03 | Spoofing/Auth | protected profile UI | mitigate | `hasAccessToken || hasRefreshToken`; zentrale API-Seam refreshen lassen |
</threat_model>

<verification>
1. `cd frontend && npm run test -- --run "src/app/me/profile/page.test.tsx"`
2. `cd frontend && npm run typecheck`
3. `git diff --check`
4. Manuelle Diff-Prüfung: kein ad hoc geschützter `fetch`, kein Token-Prop, kein direkter Keycloak-Refresh im UI.
</verification>

<success_criteria>
1. Rich-Text-Strukturen werden im Frontend-Payload erhalten.
2. Profilgeschichte zeigt nach Save/Reload Lesemodus mit `RichTextRenderer`.
3. Editor erscheint nur nach `Bearbeiten`.
4. Der alte Plain-Text-Defer-Hinweis ist weg.
5. Dirty-State und Keycloak-Return-Refresh schützen ungespeicherte Rich-Text-Änderungen.
6. Frontend-Tests und Typecheck sind grün.
</success_criteria>

<output>
Erstelle `.planning/phases/55-sichere-tiptap-persistenz-fuer-profilgeschichte/55-02-SUMMARY.md` wenn fertig.
</output>
