---
phase: 50-platform-admin-boundaries-und-contributor-scope-governance
plan: "02"
type: execute
wave: 2
depends_on:
  - 50-01-PLAN.md
files_modified:
  - backend/cmd/server
  - backend/internal/handlers
  - backend/internal/middleware
  - backend/internal/models
  - backend/internal/permissions
  - backend/internal/repository
autonomous: true
requirements:
  - PLATFORM-SCOPE-01
must_haves:
  truths:
    - "Backend authorization remains the source of truth; frontend guards are UX only."
    - "Global admin endpoints must require platform_admin."
    - "Group-management endpoints must resolve the requested group and check actor capabilities for that group."
    - "Direct URL/API access to foreign groups must be denied for fansub leads."
    - "Non-platform users must not be able to mutate canonical fansub identity fields such as name, slug, global status, merge, or delete."
---

<objective>
Harden backend route authorization and capability responses for the platform-admin versus group-scoped management split.

Purpose: direct URL/API access must be safe before the frontend is rearranged.
</objective>

<context>
@.planning/phases/50-platform-admin-boundaries-und-contributor-scope-governance/50-CONTEXT.md
@.planning/phases/50-platform-admin-boundaries-und-contributor-scope-governance/50-RESEARCH.md
@.planning/phases/50-platform-admin-boundaries-und-contributor-scope-governance/50-01-PLAN.md
@backend/cmd/server/admin_routes.go
@backend/internal/handlers/platform_admin_authz.go
@backend/internal/handlers/app_auth.go
@backend/internal/permissions/permissions.go
@backend/internal/repository/fansub_group_app_members_repository.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assert platform-admin-only backend gates for global admin operations</name>
  <read_first>
    - backend/cmd/server/admin_routes.go
    - backend/internal/handlers/admin_content_authz.go
    - backend/internal/handlers/fansub_admin.go
    - backend/internal/handlers/platform_admin_authz.go
  </read_first>
  <action>
    Verify and, where necessary, tighten backend handlers for global operations:
    - global fansub list/create/update/delete/merge
    - global anime create/edit/delete/enrichment/asset search
    - episode mapping/import/admin episode operations

    These operations must require `platform_admin` and must not treat
    `fansub_lead` as a global admin role.

    Do not weaken existing platform-admin checks.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/handlers -run "Test.*PlatformAdmin|Test.*Admin"</automated>
  </verify>
  <done>
    - Global backend admin operations are explicitly platform-admin only.
    - Tests or source-level assertions cover representative non-platform denial.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add or adapt group-scoped management backend seams</name>
  <read_first>
    - backend/internal/handlers/app_profile.go
    - backend/internal/handlers/app_auth.go
    - backend/internal/handlers/fansub_group_members.go
    - backend/internal/handlers/admin_content_fansub_group_notes.go
    - backend/internal/permissions/permissions.go
  </read_first>
  <action>
    Add or adapt backend seams needed by the new non-admin lead workspace:
    - group detail read for an authenticated actor
    - group edit capabilities for own group
    - member/invitation/note/asset/release capabilities for own group
    - release-version media and note capabilities for release versions connected
      to the actor's authorized fansub group/release context
    - denial for foreign group IDs

    Prefer wrapping/reusing existing app-member and capability handlers instead
    of introducing parallel membership logic.

    Ensure request DTOs and handlers distinguish group-scoped editable fields
    from platform-admin-only canonical fields. A fansub lead must not be able to
    update canonical Fansubgruppenname, slug, global status, merge/delete state,
    or any other destructive identity field through the scoped route or by
    direct API request.

    If a selected existing endpoint path remains under `/api/v1/admin/...` for
    compatibility, document that backend auth is group-capability based and not
    route-name based. Do not rely on the route name as the security boundary.

    If the existing release-version editor/API remains under an admin-named path
    for compatibility, ensure non-platform contributors can access only the
    scoped media/notes behavior for authorized release versions. All other
    release-version tabs, canonical episode/anime mapping behavior, and
    platform-admin operations must stay denied by backend checks.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/handlers ./internal/permissions ./internal/repository</automated>
  </verify>
  <done>
    - A fansub lead can load/manage their own group through backend capabilities.
    - An authorized contributor can manage release-version media/notes only for
      their own release context.
    - The same lead is denied for a foreign group.
    - The same contributor is denied for foreign release versions and
      platform-admin-only release-version fields.
    - The same lead is denied when attempting to mutate canonical group identity fields.
    - Platform admins keep global override where intended.
  </done>
</task>

</tasks>
