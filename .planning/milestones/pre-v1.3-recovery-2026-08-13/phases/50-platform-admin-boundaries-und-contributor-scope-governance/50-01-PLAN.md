---
phase: 50-platform-admin-boundaries-und-contributor-scope-governance
plan: "01"
type: execute
wave: 1
depends_on:
  - ../49-zentraler-auth-api-client-und-token-lifecycle-haertung/49-04-PLAN.md
files_modified:
  - docs
  - backend/internal/handlers
  - backend/internal/permissions
  - frontend/src/app
autonomous: true
requirements:
  - PLATFORM-SCOPE-01
must_haves:
  truths:
    - "The `/admin` namespace is reserved for platform_admin users only."
    - "Fansub leads must work through a group-scoped non-admin management route."
    - "The current `/admin/my-groups` placeholder is not the target lead workspace."
    - "Multi-group users need a real non-admin group chooser before entering a scoped workspace."
    - "Public group pages and authenticated group management are separate product surfaces."
    - "Canonical Fansubgruppen-Stammdaten such as the editable Fansubgruppenname field are platform-admin only."
---

<objective>
Establish the Phase 50 contract before changing runtime behavior: document the route taxonomy, permission boundary, group-management route direction, and deprecation path for `/admin/my-groups`.

Purpose: make the product/security boundary explicit so implementation does not become another patchwork of hidden buttons.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/50-platform-admin-boundaries-und-contributor-scope-governance/50-CONTEXT.md
@.planning/phases/50-platform-admin-boundaries-und-contributor-scope-governance/50-RESEARCH.md
@docs/operations/keycloak-auth-foundation-phase43.md
@docs/architecture/fansub-member-management.md
@docs/architecture/db-schema-fansub-domain.md
@backend/internal/handlers/platform_admin_authz.go
@backend/internal/permissions/permissions.go
@frontend/src/app/admin/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the platform-admin and group-management route contract</name>
  <read_first>
    - frontend/src/app/admin/page.tsx
    - frontend/src/app/admin/fansubs/page.tsx
    - frontend/src/app/admin/my-groups/page.tsx
    - frontend/src/app/fansubs/[slug]/page.tsx
  </read_first>
  <action>
    Document the route ownership contract:
    - `/admin/*` is platform-admin only.
    - Global fansub management, anime creation, anime editing, episode mapping/import, and merge/delete flows are platform-admin only.
    - Fansub lead management moves to a new non-admin route such as `/manage/fansubs/:id` or an equivalent route chosen in this task.
    - `/manage/groups` or an equivalent non-admin entry point handles users with one or multiple manageable groups.
    - `/admin/my-groups` is retired or redirected into the non-admin management entry point.
    - Release-version editing remains split: platform admins retain the full
      editor, while authorized fansub contributors may only enter the scoped
      release-version media/notes surface needed for their own release context.
    - Public group pages remain separate and must not include management access.

    Update docs or phase-local notes with the final route naming decision and rationale.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s && git diff --check</automated>
  </verify>
  <done>
    - The route taxonomy is explicit.
    - The future lead route is named and separated from `/admin`.
    - The multi-group chooser behavior is documented.
    - `/admin/my-groups` has a documented disposition.
  </done>
</task>

<task type="auto">
  <name>Task 2: Inventory global admin surfaces and group-scoped surfaces</name>
  <read_first>
    - frontend/src/app/admin
    - frontend/src/lib/api.ts
    - backend/cmd/server/admin_routes.go
    - backend/internal/handlers
  </read_first>
  <action>
    Produce a compact inventory of:
    - platform-admin pages/routes
    - group-scoped management pages/routes
    - release-version media/notes routes reachable by authorized contributors
    - public group pages
    - the non-admin group chooser or management entry route
    - API endpoints already protected by platform-admin guard
    - API endpoints that require group capability checks
    - UI links that currently send non-platform users into global admin surfaces

    Flag any route where frontend and backend authorization language disagree.
    Explicitly identify destructive or canonical fansub fields that must remain
    platform-admin only, including at least the editable `Fansubgruppenname`
    field and the slug field, which must not be visible in lead workspaces.
    Explicitly identify release-version editor tabs that must remain
    platform-admin only versus tabs that may be exposed to authorized
    contributors, with `Medien` and `Notizen` as the intended contributor-safe
    working set.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s && git diff --check</automated>
  </verify>
  <done>
    - There is a route/API inventory for Phase 50 execution.
    - Every global admin page and group-scoped page has an owner.
    - Release-version tabs are classified as platform-admin-only or
      contributor-safe.
    - Known frontend/backend mismatches are listed before code changes.
    - Canonical fansub identity fields are classified as platform-admin only.
  </done>
</task>

</tasks>
