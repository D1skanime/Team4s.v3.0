---
phase: 07-generic-upload-and-linking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/models/anime_assets.go
  - backend/internal/repository/anime_assets.go
  - backend/internal/repository/anime_test.go
  - backend/internal/handlers/admin_content_anime_assets.go
  - backend/internal/handlers/admin_content_test.go
  - backend/internal/handlers/media_upload.go
  - shared/contracts/openapi.yaml
autonomous: true
requirements:
  - UPLD-01
  - UPLD-02
  - UPLD-03
must_haves:
  truths:
    - "Admins can route every supported anime asset upload through the existing `/api/v1/admin/upload` seam instead of introducing slot-specific upload endpoints."
    - "The reusable V2 linking path supports `cover`, `banner`, `logo`, `background`, and `background_video` with the correct singular-versus-collection semantics."
    - "Uploaded assets are linked to the correct anime and slot only when the media reference and media type match the intended target."
    - "The active admin upload and linking endpoints are documented in `shared/contracts/openapi.yaml` so the contract stops drifting from the real implementation."
  artifacts:
    - path: "backend/internal/repository/anime_assets.go"
      provides: "Reusable V2 asset-linking helpers for all in-scope anime asset kinds"
    - path: "backend/internal/handlers/admin_content_anime_assets.go"
      provides: "Admin HTTP contract for assigning or adding uploaded anime assets to each supported slot"
    - path: "shared/contracts/openapi.yaml"
      provides: "Documented admin upload and asset-linking contract for the supported anime asset set"
  key_links:
    - from: "backend/internal/handlers/admin_content_anime_assets.go"
      to: "backend/internal/repository/anime_assets.go"
      via: "admin asset assign, add, clear, and remove calls"
      pattern: "AssignManual|AddManual|Clear|Remove"
    - from: "backend/internal/repository/anime_assets.go"
      to: "backend/internal/handlers/media_upload.go"
      via: "shared asset-type vocabulary between upload and linking"
      pattern: "normalizeUploadAssetType|assignManualSingularAssetV2|addManualBackgroundV2"
    - from: "shared/contracts/openapi.yaml"
      to: "backend/internal/handlers/admin_content_anime_assets.go"
      via: "documented request and response shapes for the active admin contract"
      pattern: "/api/v1/admin/upload|/api/v1/admin/anime/{id}/assets/"
---

<objective>
Generalize the backend anime asset-linking contract so all Phase 7 asset types can consume the verified upload seam through one reusable V2 path.

Purpose: turn the current cover/banner/background mix into a coherent backend contract for `cover`, `banner`, `logo`, `background`, and `background_video`.
Output: repository helpers, admin handlers, contract docs, and tests that align the upload seam with slot-aware V2 linking.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-generic-upload-and-linking/07-CONTEXT.md
@.planning/phases/07-generic-upload-and-linking/07-RESEARCH.md
@.planning/phases/07-generic-upload-and-linking/07-VALIDATION.md
@.planning/phases/06-provisioning-and-lifecycle-foundations/06-UAT.md
@backend/internal/handlers/media_upload.go
@backend/internal/handlers/admin_content_anime_assets.go
@backend/internal/repository/anime_assets.go
@backend/internal/models/anime_assets.go
@shared/contracts/openapi.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing backend coverage for generic anime asset linking across singular and collection slots</name>
  <files>backend/internal/repository/anime_test.go, backend/internal/handlers/admin_content_test.go</files>
  <read_first>
    - backend/internal/repository/anime_assets.go
    - backend/internal/handlers/admin_content_anime_assets.go
    - backend/internal/handlers/media_upload.go
    - .planning/phases/07-generic-upload-and-linking/07-CONTEXT.md
    - .planning/phases/07-generic-upload-and-linking/07-RESEARCH.md
    - .planning/phases/07-generic-upload-and-linking/07-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: singular-slot linking can assign uploaded media to `cover`, `banner`, `logo`, and `background_video` through one V2-style repository path.
    - Test 2: collection-slot linking can add a `background` without replacing the existing ordered background list.
    - Test 3: linking fails when the media reference does not exist or does not match the required media type for the slot.
    - Test 4: admin handler routes reject invalid anime IDs and return explicit German operator messages for unsupported slot operations.
    - Test 5: upload alias normalization still recognizes `poster`, `banner`, `logo`, `gallery`, `background`, `video`, and `background_video` without creating a second upload seam.
  </behavior>
  <action>Create focused failing tests before implementation. In `backend/internal/repository/anime_test.go`, add V2-path tests that call the current anime asset repository against `banner`, `logo`, `background`, and `background_video`, asserting that singular slots replace the active link while backgrounds remain additive and ordered. In `backend/internal/handlers/admin_content_test.go`, add admin route tests for the new or generalized asset endpoints, covering success and clear validation failures. Add at least one assertion that a wrong media type cannot be linked into the wrong slot, for example a `background` media asset into a `logo` slot or a `poster` into `background_video`.</action>
  <acceptance_criteria>
    - Repository tests cover at least one successful link for `logo` and one for `background_video`.
    - Repository tests cover additive `background` behavior rather than singular replacement.
    - Handler tests cover at least one invalid anime ID and one media mismatch failure with an explicit response message.
    - At least one test asserts upload alias normalization accepts every supported Phase 7 asset label.
    - The new tests fail against any implementation that keeps Phase 7 limited to only cover/banner/background.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/repository -run "Test.*Anime.*(Logo|Background|Video|Asset).*" -count=1</automated>
    <automated>cd backend && go test ./internal/handlers -run "Test.*Admin.*Anime.*Asset.*" -count=1</automated>
  </verify>
  <done>A red-green safety net exists for the full backend asset set and the intended slot semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the reusable backend linking contract for all supported anime asset types</name>
  <files>backend/internal/models/anime_assets.go, backend/internal/repository/anime_assets.go, backend/internal/repository/anime_test.go, backend/internal/handlers/admin_content_anime_assets.go, backend/internal/handlers/admin_content_test.go, backend/internal/handlers/media_upload.go</files>
  <read_first>
    - backend/internal/models/anime_assets.go
    - backend/internal/repository/anime_assets.go
    - backend/internal/handlers/admin_content_anime_assets.go
    - backend/internal/handlers/media_upload.go
    - .planning/phases/07-generic-upload-and-linking/07-CONTEXT.md
    - .planning/phases/07-generic-upload-and-linking/07-RESEARCH.md
  </read_first>
  <action>Refactor the anime asset backend so the supported Phase 7 slots all consume one V2-style linking vocabulary. Extend `backend/internal/repository/anime_assets.go` to expose reusable helpers for singular slot assignment and clearing across `poster`, `banner`, `logo`, and `background_video`, while keeping `background` as an ordered additive collection. Add or generalize admin handler routes in `backend/internal/handlers/admin_content_anime_assets.go` so `cover`, `banner`, `logo`, and `background_video` use the same request body shape `{ \"media_id\": string }` and `backgrounds` continues using create/delete semantics. Update `backend/internal/handlers/media_upload.go` only as needed so alias normalization and asset-type names remain aligned with the full supported set and there is still only one upload seam at `/api/v1/admin/upload`.</action>
  <acceptance_criteria>
    - `backend/internal/repository/anime_assets.go` contains reusable V2 helpers that support `poster`, `banner`, `logo`, and `background_video` as singular asset types.
    - `backend/internal/repository/anime_assets.go` keeps `background` as the additive ordered collection type.
    - `backend/internal/handlers/admin_content_anime_assets.go` exposes admin routes or route branches for `logo` and `background_video` in the same style as `cover` and `banner`.
    - `backend/internal/handlers/media_upload.go` still uses one upload endpoint and recognizes every Phase 7 asset alias.
    - `cd backend && go test ./internal/repository ./internal/handlers -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/repository -run "Test.*Anime.*(Logo|Background|Video|Asset).*" -count=1</automated>
    <automated>cd backend && go test ./internal/handlers -run "Test.*Admin.*Anime.*Asset.*" -count=1</automated>
    <automated>cd backend && go test ./internal/repository ./internal/handlers -count=1</automated>
  </verify>
  <done>The backend now exposes one reusable V2 linking contract for every in-scope anime asset slot.</done>
</task>

<task type="auto">
  <name>Task 3: Sync the active admin upload and linking contract into OpenAPI</name>
  <files>shared/contracts/openapi.yaml</files>
  <read_first>
    - shared/contracts/openapi.yaml
    - backend/internal/handlers/media_upload.go
    - backend/internal/handlers/admin_content_anime_assets.go
    - frontend/src/lib/api.ts
    - .planning/phases/07-generic-upload-and-linking/07-RESEARCH.md
  </read_first>
  <action>Update `shared/contracts/openapi.yaml` so it documents the real Phase 7 admin contract instead of drifting behind the implementation. Add or refresh the schema for `POST /api/v1/admin/upload` with multipart fields `entity_type`, `entity_id`, `asset_type`, and `file`, and document the currently supported anime asset labels `poster`, `banner`, `logo`, `background`, and `background_video`. Add the admin anime asset-linking endpoints used by the frontend, including singular-slot assignment routes and the additive background route, with request bodies containing `media_id` and operator-safe error responses matching the handler layer.</action>
  <acceptance_criteria>
    - `shared/contracts/openapi.yaml` contains `POST /api/v1/admin/upload`.
    - The upload contract documents the supported Phase 7 asset labels including `logo` and `background_video`.
    - The file documents the admin anime asset-linking endpoints exercised by the frontend.
    - The documented request shape for asset-linking calls includes `media_id`.
  </acceptance_criteria>
  <verify>
    <automated>Get-Content shared/contracts/openapi.yaml | Select-String -Pattern '/api/v1/admin/upload','background_video','logo','media_id'</automated>
  </verify>
  <done>The repo’s documented admin contract matches the upload and linking endpoints the application actually uses.</done>
</task>

</tasks>

<verification>
Run the backend repository and handler suites, then manually upload and link at least one `logo` and one `background_video` asset for a real anime to confirm the resulting asset URLs resolve through the existing V2 path rather than a slot-specific fallback.
</verification>

<success_criteria>
Phase 7 has a backend contract that lets every supported anime asset type use the same upload seam and a consistent V2 linking path.
</success_criteria>

<output>
After completion, create `.planning/phases/07-generic-upload-and-linking/07-01-SUMMARY.md`
</output>
