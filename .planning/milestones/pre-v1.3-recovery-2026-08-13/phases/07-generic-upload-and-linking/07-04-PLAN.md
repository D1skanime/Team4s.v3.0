---
phase: 07-generic-upload-and-linking
plan: 04
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - frontend/src/app/admin/anime/create/page.tsx
  - frontend/src/app/admin/anime/create/createAssetUploadPlan.ts
  - frontend/src/app/admin/anime/create/createAssetUploadPlan.test.ts
  - frontend/src/app/admin/anime/create/page.test.tsx
  - frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx
  - frontend/src/app/admin/anime/components/ManualCreate/ManualCreateAssetUploadPanel.tsx
autonomous: true
requirements:
  - UPLD-01
  - UPLD-02
  - UPLD-03
gap_closure: true
must_haves:
  truths:
    - "Admins can stage and then upload/link non-cover manual assets from the create workflow through the existing Phase 07 upload seam."
    - "Create no longer stops at `cover`; `banner`, `logo`, `background`, and `background_video` are reachable without leaving the supported V2 path."
    - "Post-create asset linking keeps the supported slot semantics: singular replacement for `cover`, `banner`, `logo`, `background_video`, and additive insertion for `background`."
  artifacts:
    - path: "frontend/src/app/admin/anime/create/page.tsx"
      provides: "Slimmed create route that delegates staged asset config and post-create orchestration instead of growing the 919-line page further"
    - path: "frontend/src/app/admin/anime/create/createAssetUploadPlan.ts"
      provides: "Extracted staged-asset config and post-create upload/link orchestration for create"
    - path: "frontend/src/app/admin/anime/components/ManualCreate/ManualCreateAssetUploadPanel.tsx"
      provides: "Extracted operator-facing staging controls for cover, banner, logo, background, and background_video"
    - path: "frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx"
      provides: "Manual create workspace shell that hosts the extracted asset-upload panel"
    - path: "frontend/src/app/admin/anime/create/page.test.tsx"
      provides: "Regression coverage for staged non-cover assets and post-create upload/link behavior"
  key_links:
    - from: "frontend/src/app/admin/anime/create/page.tsx"
      to: "frontend/src/app/admin/anime/create/createAssetUploadPlan.ts"
      via: "delegated staged asset config and post-create upload orchestration"
      pattern: "buildCreateAssetUploadPlan|uploadCreatedAnimeAssets"
    - from: "frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx"
      to: "frontend/src/app/admin/anime/components/ManualCreate/ManualCreateAssetUploadPanel.tsx"
      via: "delegated staging controls"
      pattern: "ManualCreateAssetUploadPanel"
    - from: "frontend/src/app/admin/anime/create/createAssetUploadPlan.ts"
      to: "frontend/src/lib/api.ts"
      via: "uploadAdminAnimeMedia and typed slot link helpers"
      pattern: "uploadAdminAnimeMedia|assignAdminAnime.*Asset|addAdminAnimeBackgroundAsset"
    - from: "frontend/src/app/admin/anime/create/page.test.tsx"
      to: "frontend/src/app/admin/anime/create/createAssetUploadPlan.ts"
      via: "helper-level coverage for staged asset uploads after create"
      pattern: "uploadCreatedAnimeAssets"
---

<objective>
Close the Phase 07 create-route reachability gap by letting admins stage and then upload/link non-cover manual assets through the existing V2 upload seam after the anime record is created.

Purpose: satisfy the verifier's failed truth on create without expanding scope into Phase 08 cleanup semantics.
Output: create-route asset staging, post-create upload/link helpers, and focused regression coverage for non-cover manual assets.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-generic-upload-and-linking/07-CONTEXT.md
@.planning/phases/07-generic-upload-and-linking/07-RESEARCH.md
@.planning/phases/07-generic-upload-and-linking/07-VERIFICATION.md
@.planning/phases/07-generic-upload-and-linking/07-02-SUMMARY.md
@frontend/src/app/admin/anime/create/page.tsx
@frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx
@frontend/src/lib/api.ts

<interfaces>
From frontend/src/app/admin/anime/create/page.tsx:
```ts
export function stageManualCreateCover(file: File): { draftValue: string; previewUrl: string }
export async function uploadCreatedAnimeCover(animeID: number, file: File, authToken?: string): Promise<string>
```

From frontend/src/types/admin.ts:
```ts
export type AdminAnimeAssetKind = 'cover' | 'banner' | 'logo' | 'background' | 'background_video'
```

From frontend/src/lib/api.ts:
```ts
export async function uploadAdminAnimeMedia(options: AdminAnimeMediaUploadOptions): Promise<AdminMediaUploadResponse>
export async function assignAdminAnimeBannerAsset(animeID: number, mediaID: string, authToken?: string): Promise<void>
export async function assignAdminAnimeLogoAsset(animeID: number, mediaID: string, authToken?: string): Promise<void>
export async function assignAdminAnimeBackgroundVideoAsset(animeID: number, mediaID: string, authToken?: string): Promise<void>
export async function addAdminAnimeBackgroundAsset(animeID: number, mediaID: string, authToken?: string): Promise<AdminAnimeBackgroundAssetResponse>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing create-route regression coverage for staged non-cover assets</name>
  <files>frontend/src/app/admin/anime/create/createAssetUploadPlan.test.ts, frontend/src/app/admin/anime/create/page.test.tsx</files>
  <read_first>
    - frontend/src/app/admin/anime/create/page.tsx
    - frontend/src/app/admin/anime/create/page.test.tsx
    - frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx
    - .planning/phases/07-generic-upload-and-linking/07-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: create-page markup exposes staged manual controls for `banner`, `logo`, `background`, and `background_video`, not only `cover`.
    - Test 2: post-create helper logic uploads and links the staged non-cover assets through the typed Phase 07 helpers.
    - Test 3: `background` remains additive while the other non-cover slots stay singular.
  </behavior>
  <action>Add failing tests before implementation. Keep the rendered-surface assertions in `frontend/src/app/admin/anime/create/page.test.tsx` so the create page proves it exposes staging controls for `Banner`, `Logo`, `Background`, and `Background-Video`. Add a new focused helper suite in `frontend/src/app/admin/anime/create/createAssetUploadPlan.test.ts` that pins the extracted staged-asset config and post-create uploader contract: `banner`, `logo`, and `background_video` must call singular asset-link helpers, while `background` must call the additive background helper. Do not weaken the existing `cover` expectations.</action>
  <acceptance_criteria>
    - The create-page test suite contains assertions for all four non-cover manual asset kinds.
    - `createAssetUploadPlan.test.ts` exists and fails against the current cover-only orchestration.
    - At least one helper test distinguishes additive `background` linking from singular-slot linking.
    - The tests fail against the current cover-only implementation.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- src/app/admin/anime/create/createAssetUploadPlan.test.ts</automated>
    <automated>cd frontend && npm test -- src/app/admin/anime/create/page.test.tsx</automated>
  </verify>
  <done>The create route has a failing regression net that captures the current non-cover reachability gap.</done>
</task>

<task type="auto">
  <name>Task 2: Implement staged create-route upload and linking for non-cover manual assets</name>
  <files>frontend/src/app/admin/anime/create/page.tsx, frontend/src/app/admin/anime/create/createAssetUploadPlan.ts, frontend/src/app/admin/anime/create/createAssetUploadPlan.test.ts, frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx, frontend/src/app/admin/anime/components/ManualCreate/ManualCreateAssetUploadPanel.tsx, frontend/src/app/admin/anime/create/page.test.tsx</files>
  <read_first>
    - frontend/src/app/admin/anime/create/page.tsx
    - frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx
    - frontend/src/app/admin/anime/create/page.test.tsx
    - frontend/src/lib/api.ts
    - .planning/phases/07-generic-upload-and-linking/07-02-SUMMARY.md
    - .planning/phases/07-generic-upload-and-linking/07-VERIFICATION.md
  </read_first>
  <action>Generalize the create-route post-create uploader instead of keeping the current `uploadCreatedAnimeCover` one-off, but do not center the new logic in the already-919-line `frontend/src/app/admin/anime/create/page.tsx`. Explicitly extract staged-asset config and post-create orchestration into `frontend/src/app/admin/anime/create/createAssetUploadPlan.ts`. That module should own the asset-kind config, file staging helpers, and the generalized `uploadCreatedAnimeAssets` flow that uploads via `uploadAdminAnimeMedia` and links via the existing typed helpers, preserving additive `background` semantics and singular-slot behavior for the other asset kinds. Also extract the visible staging controls into `frontend/src/app/admin/anime/components/ManualCreate/ManualCreateAssetUploadPanel.tsx`; keep `ManualCreateWorkspace.tsx` as a shell that renders the new panel and passes callbacks/state through. Reduce `create/page.tsx` to wiring state into those extracted modules plus the existing create submit flow. Keep Phase 08 behavior out of scope: no remove/cleanup redesign, only upload-and-link reachability.</action>
  <acceptance_criteria>
    - `createAssetUploadPlan.ts` owns the extracted asset-kind config and generalized `uploadCreatedAnimeAssets` orchestration instead of leaving those branches inline in `create/page.tsx`.
    - `ManualCreateAssetUploadPanel.tsx` owns the visible staging controls for all supported manual asset kinds.
    - `create/page.tsx` stages optional manual files for `banner`, `logo`, `background`, and `background_video` by delegating to the extracted helper/component rather than growing the 919-line page further.
    - `ManualCreateWorkspace.tsx` becomes a shell that hosts the extracted staging panel.
    - `cd frontend && npm test -- src/app/admin/anime/create/createAssetUploadPlan.test.ts src/app/admin/anime/create/page.test.tsx` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- src/app/admin/anime/create/createAssetUploadPlan.test.ts</automated>
    <automated>cd frontend && npm test -- src/app/admin/anime/create/createAssetUploadPlan.test.ts src/app/admin/anime/create/page.test.tsx</automated>
    <automated>Get-Content frontend/src/app/admin/anime/components/ManualCreate/ManualCreateAssetUploadPanel.tsx | Select-String -Pattern 'Banner','Logo','Background-Video'</automated>
    <automated>Get-Content frontend/src/app/admin/anime/create/createAssetUploadPlan.ts | Select-String -Pattern 'background_video','assignAdminAnimeLogoAsset','addAdminAnimeBackgroundAsset','uploadCreatedAnimeAssets'</automated>
  </verify>
  <done>The create workflow can stage and then upload/link non-cover assets through the same Phase 07 seam that already works for cover.</done>
</task>

</tasks>

<verification>
Run the create-page test file, then create a new anime locally and verify you can stage at least one `banner` and one `logo` before save, submit the create form, and see those assets linked on the newly created record without any fallback to legacy slot-specific behavior.
</verification>

<success_criteria>
The verifier's create-route gap is closed: create exposes non-cover manual upload/link controls and uses the existing Phase 07 typed seam after the anime is created.
</success_criteria>

<output>
After completion, create `.planning/phases/07-generic-upload-and-linking/07-04-SUMMARY.md`
</output>
