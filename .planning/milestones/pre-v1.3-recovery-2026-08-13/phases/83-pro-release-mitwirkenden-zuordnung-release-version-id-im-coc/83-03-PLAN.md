---
phase: 83
plan: 03
type: execute
wave: 2
depends_on: [83-02]
files_modified:
  - backend/internal/repository/release_version_notes_repository.go
autonomous: true
requirements: [D-13]

must_haves:
  truths:
    - "per D-13: `GetMemberRolesForVersion` liest aus `anime_contributions` + `anime_contribution_roles` statt aus der Legacy-Tabelle `release_member_roles`"
    - "per D-02: Fallback-Logik ist dieselbe wie in `ListActorContributionRolesForVersion` — versions-spezifisch zuerst, dann anime-weit"
    - "`loadValidMemberRoleKeysForVersion` und `MemberRoleForVersion`-Struct sind auf `role_code` (string) statt `role_id` (int64) umgestellt"
    - "Notizen-/Media-Maske kann nach diesem Plan denselben aufgelösten Mitwirkenden-Satz anzeigen wie der Permission-Check"
  artifacts:
    - path: "backend/internal/repository/release_version_notes_repository.go"
      provides: "Umgebautes GetMemberRolesForVersion + loadValidMemberRoleKeysForVersion auf anime_contributions"
      contains: "anime_contributions"
  key_links:
    - from: "release_version_notes_repository.go GetMemberRolesForVersion"
      to: "anime_contributions + anime_contribution_roles"
      via: "SQL JOIN release_version_groups WHERE release_version_id = $1"
      pattern: "anime_contributions.*release_version_id"
---

<objective>
D-13-Konsistenz: `GetMemberRolesForVersion` von Legacy-Tabelle `release_member_roles` auf `anime_contributions` migrieren.

Purpose: Ohne diesen Umbau zeigt die Notizen-/Media-Maske einen veralteten/falschen Mitwirkenden-Satz. Nach Plan 02 steuert `anime_contributions` die Rechte — die Anzeige muss denselben Satz liefern. Das ist eine kritische Konsistenz-Anforderung (D-13), kein optionales Cleanup.

Output: `GetMemberRolesForVersion` liest aus `anime_contributions` (zweistufige Auflösung analog Plan 02). `MemberRoleForVersion`-Struct und `loadValidMemberRoleKeysForVersion` sind angepasst.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-CONTEXT.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-RESEARCH.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-PATTERNS.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-02-SUMMARY.md

<interfaces>
<!-- Aus backend/internal/repository/release_version_notes_repository.go (342 Zeilen) -->
<!--
// MemberRoleForVersion Struct (Z. 38–44) — UMSTELLEN:
type MemberRoleForVersion struct {
  MemberID   int64  // bleibt
  MemberName string // bleibt
  RoleID     int64  // ENTFERNEN → durch RoleCode string ersetzen
  RoleName   string // ENTFERNEN → nicht mehr in neuer Query
  RoleLabel  string // bleibt (aus role_definitions.label_de)
}
-->
<!--
// GetMemberRolesForVersion (Z. 115–150) — UMBAUEN:
// Legacy Query: JOIN release_member_roles + contributor_roles (release_id basiert)
// Neue Query: JOIN anime_contributions + anime_contribution_roles + release_version_groups (version_id basiert, zweistufig)
-->
<!--
// loadValidMemberRoleKeysForVersion (Z. 180–200) — UMBAUEN:
// Erzeugt heute eine Map von (memberID, roleID) Keys
// Nach Umbau: Map von (memberID, roleCode) Keys
-->
<!--
// releaseVersionMemberRoleKey (Z. 170–178) — UMSTELLEN:
// Heute: fmt.Sprintf("%d:%d", memberID, roleID)
// Nach: fmt.Sprintf("%d:%s", memberID, roleCode)
-->

<!-- Aus 83-PATTERNS.md Abschnitt release_version_notes_repository.go (Z. 222–281) -->
<!-- Neue Query-Muster und Scan-Muster sind dokumentiert -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MemberRoleForVersion-Struct + GetMemberRolesForVersion auf anime_contributions umstellen</name>
  <files>backend/internal/repository/release_version_notes_repository.go</files>
  <read_first>
    - backend/internal/repository/release_version_notes_repository.go (vollständig: MemberRoleForVersion-Struct Z. 38–44, GetMemberRolesForVersion Z. 115–155, loadValidMemberRoleKeysForVersion Z. 180–210, releaseVersionMemberRoleKey Z. 170–178)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-PATTERNS.md (Abschnitt release_version_notes_repository.go Z. 222–281)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-RESEARCH.md (Pattern 4 — GetMemberRolesForVersion Migration Z. 284–313, Pitfall 3 Z. 362–372)
    - database/migrations/0085_anime_contribution_roles_catalog.up.sql (role_definitions.label_de, role_definitions.code)
    - database/migrations/0086_anime_contributions.up.sql (anime_contributions Basisschema)
    - database/migrations/0091_anime_contributions_release_version.up.sql (release_version_id-Spalte)
  </read_first>
  <behavior>
    - Test (Unit): `GetMemberRolesForVersion` für eine release_version_id mit versions-spezifischen anime_contributions gibt Members+RoleCodes zurück
    - Test (Unit): `GetMemberRolesForVersion` für eine release_version_id ohne override gibt Fallback anime-weite Members+RoleCodes zurück
    - Test (Unit): `GetMemberRolesForVersion` für eine release_version_id ohne jede Contribution gibt leere Liste zurück
  </behavior>
  <action>
**Schritt 1 — `MemberRoleForVersion`-Struct anpassen:**
Felder `RoleID int64` und `RoleName string` entfernen (aus Legacy `contributor_roles`). Neues Feld `RoleCode string` hinzufügen. `RoleLabel string` bleibt (kommt jetzt aus `role_definitions.label_de`).

Struct nach Umbau:
- `MemberID int64`
- `MemberName string`
- `RoleCode string`
- `RoleLabel string`

**Schritt 2 — `GetMemberRolesForVersion` Query ersetzen:**
Die alte Query (JOIN `release_member_roles` → `contributor_roles`) durch eine zweistufige Query ersetzen:

Schritt 2a — versions-spezifisch:
```sql
SELECT DISTINCT ac.member_id, m.nickname AS member_name,
       acr.role_code, rd.label_de AS role_label
FROM anime_contributions ac
JOIN members m ON m.id = ac.member_id
JOIN anime_contribution_roles acr ON acr.anime_contribution_id = ac.id
JOIN role_definitions rd ON rd.code = acr.role_code
WHERE ac.release_version_id = $1
  AND ac.fansub_group_id IN (
      SELECT fansub_group_id FROM release_version_groups WHERE release_version_id = $1
  )
ORDER BY rd.label_de ASC, m.nickname ASC
```

Schritt 2b — Fallback (nur wenn 2a leer): anime-weite Contributions für dasselbe Anime:
```sql
SELECT DISTINCT ac.member_id, m.nickname AS member_name,
       acr.role_code, rd.label_de AS role_label
FROM anime_contributions ac
JOIN members m ON m.id = ac.member_id
JOIN anime_contribution_roles acr ON acr.anime_contribution_id = ac.id
JOIN role_definitions rd ON rd.code = acr.role_code
JOIN release_versions rv ON rv.id = $1
JOIN fansub_releases fr ON fr.id = rv.release_id
WHERE ac.release_version_id IS NULL
  AND ac.anime_id = fr.anime_id
  AND ac.fansub_group_id IN (
      SELECT fansub_group_id FROM release_version_groups WHERE release_version_id = $1
  )
ORDER BY rd.label_de ASC, m.nickname ASC
```

Scan-Anpassung: Statt `&mr.RoleID, &mr.RoleName` jetzt `&mr.RoleCode, &mr.RoleLabel` in `rows.Scan(...)`.

**Schritt 3 — `releaseVersionMemberRoleKey` anpassen:**
Key-Format von `fmt.Sprintf("%d:%d", memberID, roleID)` auf `fmt.Sprintf("%d:%s", memberID, roleCode)` umstellen.

**Schritt 4 — `loadValidMemberRoleKeysForVersion` anpassen:**
Statt `MemberRoleForVersion.RoleID` jetzt `MemberRoleForVersion.RoleCode` für Key-Generierung verwenden. Die Funktion verbleibt strukturell gleich, nur der Key ändert sich.

Kein Umbau von `BulkUpsertReleaseVersionNotes` nötig — die Funktion prüft per Key-Lookup, ob ein Member-Role-Eintrag existiert. Der Key-Wechsel von ID auf Code ist transparent.

HINWEIS: `release_member_roles`-Tabelle bleibt bestehen (keine Migration, kein DROP) — nur die Lesepfade im Repository werden umgestellt.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/repository/... -run TestGetMemberRolesForVersion -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    `GetMemberRolesForVersion` und `loadValidMemberRoleKeysForVersion` lesen aus `anime_contributions`. `go build ./...` kompiliert ohne Fehler. Alle betroffenen Tests sind grün.
  </done>
  <acceptance_criteria>
    - `MemberRoleForVersion`-Struct enthält `RoleCode string` statt `RoleID int64` + `RoleName string`
    - `GetMemberRolesForVersion`-Query enthält JOIN auf `anime_contributions` und `anime_contribution_roles` — KEIN JOIN mehr auf `release_member_roles`
    - `releaseVersionMemberRoleKey` verwendet `%d:%s` statt `%d:%d`
    - `cd backend && go build ./...` exit 0
    - `go test ./backend/internal/repository/... -run TestGetMemberRolesForVersion` → PASS oder bestehende Tests passen zu neuem Struct
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Notizen-/Media-Maske → Mitwirkenden-Liste | Nur autorisierte Personen aus aufgelöstem Contribution-Satz sollen sichtbar sein |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | Information Disclosure | `GetMemberRolesForVersion` | mitigate | Query ist auf `release_version_groups` des Releases gefiltert — keine Mitglieder fremder Gruppen sichtbar (D-12) |
| T-83-03 | Tampering | `loadValidMemberRoleKeysForVersion` Key-Wechsel | accept | Key-Format-Wechsel ID→Code ändert keine Rechte; `BulkUpsertReleaseVersionNotes` prüft nur Existenz im aufgelösten Satz |
| T-83-LEGACY | Information Disclosure | `release_member_roles`-Tabelle verbleibt | accept | Tabelle wird nicht mehr gelesen; kein DROP in dieser Phase (kein Schema-Risiko). Alte Daten in der Tabelle sind nach Umbau unsichtbar — kein Datenleck |
| T-83-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Pakete |
</threat_model>

<verification>
- `cd backend && go build ./...` → exit 0
- `GetMemberRolesForVersion`-Implementierung enthält keinen String `release_member_roles` mehr
- `go test ./backend/internal/repository/... -run TestGetMemberRolesForVersion -v` → PASS
</verification>

<success_criteria>
D-13-Konsistenz hergestellt: Notizen-/Media-Maske liest denselben aufgelösten Mitwirkenden-Satz wie `CanForReleaseVersion`. `release_member_roles`-Legacy-Join ist entfernt. Backend kompiliert vollständig.
</success_criteria>

<output>
Create `.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-03-SUMMARY.md` when done.
</output>
