---
phase: 83
plan: 07
type: execute
wave: 5
depends_on: [83-05, 83-06]
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionModal.tsx
  - frontend/src/components/anime/ReleaseVersionBreakdown.tsx
  - backend/internal/repository/anime_contributions_member_repository.go
autonomous: true
requirements: [D-14, D-15, D-16]

must_haves:
  truths:
    - "per D-14: Kein natives <select> ohne FormField-Wrapper in AnimeContributionModal.tsx"
    - "per D-14: ReleaseVersionBreakdown.tsx verwendet ausschließlich @/components/ui-Primitives (kein natives <select>/<input>/<textarea>)"
    - "per D-16 (Constraint-Verifikation): Hard-DELETE für anime_contributions ist dokumentiert als Risiko/Folgearbeit im Repository-File"
    - "npm run lint meldet keine no-restricted-syntax-Fehler in den geänderten Dateien"
  artifacts:
    - path: "frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionModal.tsx"
      provides: "D-14-Konformes natives <select> → Select+FormField Migration"
    - path: "frontend/src/components/anime/ReleaseVersionBreakdown.tsx"
      provides: "D-14-konforme Primitives-Angleichung"
    - path: "backend/internal/repository/anime_contributions_member_repository.go"
      provides: "D-16-Constraint-Dokumentation als Kommentar zur Delete-Methode"
      contains: "D-16"
  key_links:
    - from: "AnimeContributionModal.tsx"
      to: "@/components/ui Select + FormField"
      via: "import { Select, FormField } from '@/components/ui'"
      pattern: "FormField.*Select|Select.*FormField"
---

<objective>
UI-Konsolidierung (D-14 gefoldete Todos): natives `<select>` in `AnimeContributionModal.tsx` durch `Select`+`FormField` ersetzen; `ReleaseVersionBreakdown.tsx` an `@/components/ui`-Primitives angleichen. D-16-Soft-Delete-Lücke im Repository-File dokumentieren.

Purpose: D-14 ist ein hartes Constraint für die gesamte Phase 83. Zwei Dateien aus gefoldeten Todos haben noch Primitive-Verstöße. Dieser Plan schließt die offenen D-14-Todos ab. Außerdem wird D-16 (Soft-Delete-Lücke) als Constraint/Risiko im betroffenen Repository-File dokumentiert (kein Vollausbau nötig).

Output: Beide Frontend-Dateien ESLint-konform. `npm run lint` meldet keine no-restricted-syntax-Fehler. D-16-Kommentar im Backend-Repository.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-CONTEXT.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-RESEARCH.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-PATTERNS.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-06-SUMMARY.md

<interfaces>
<!-- Aus 83-PATTERNS.md Abschnitt AnimeContributionModal.tsx (Z. 669–726) -->
<!--
// Zu ersetzendes Muster (Z. ~230–253):
// <Select value={...} onChange={(event) => { const memberId = Number(event.currentTarget.value) ... }}>
//   <option value="">...</option>
//   {members.map((m) => <option key={m.member_id} value={m.member_id}>{m.display_name}</option>)}
// </Select>
//
// Das Problem: fehlendes FormField-Wrapper
// Die Lösung: <FormField label="Person"><Select ...>...</Select></FormField>
-->

<!-- Aus 83-RESEARCH.md Pitfall 7 (Z. 393–396) -->
<!--
// Select-Primitive aus @/components/ui kapselt bereits ein natives <select>
// <option>-Kinder in Select sind erlaubt und kein CLAUDE.md-Verstoß
// Das Problem war: FEHLENDES FormField-Wrapper
-->

<!-- Aus frontend/src/components/ui/index.ts (verifiziert) -->
<!-- FormField, Select sind exportiert -->

<!-- D-16: Soft-Delete-Lücke (RESEARCH.md Pitfall 5, Z. 380–386) -->
<!--
// anime_contributions_member_repository.go enthält Hard-DELETE (DELETE FROM anime_contributions WHERE id = ...)
// Datei existiert unter: backend/internal/repository/anime_contributions_member_repository.go
// Kein deleted_at-Feld in anime_contributions — D-16 verlangt keinen Vollausbau, nur Dokumentation
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AnimeContributionModal.tsx — natives <select> ohne FormField → Select+FormField</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionModal.tsx</files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionModal.tsx (vollständig: Zielstelle des nativen <select>-Verstoßes ~Z. 230–253 laut RESEARCH.md, Imports-Block, bestehende Select-Nutzung, Gesamtstruktur)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-PATTERNS.md (Abschnitt AnimeContributionModal.tsx Z. 669–726)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-RESEARCH.md (Pitfall 7 Z. 393–396)
    - frontend/src/components/ui/index.ts (FormField-Props prüfen: label, für korrekte Verwendung)
  </read_first>
  <action>
Minimale chirurgische Änderung: Nur das eine native `<select>` ersetzen, das KEINEN `FormField`-Wrapper hat.

NICHT ersetzen: `<option>`-Kinder innerhalb von `Select` (sind erlaubt, da `Select` ein natives `<select>` kapselt).

Die spezifische Änderung (Zeile ~230–253):
Den problematischen `<Select>`-Block (ohne umgebenden `FormField`) in einen `<FormField label="Person">`-Wrapper einschließen. Der äußere Wrapper ist `<FormField label="Person">`, der innere `<Select>` und seine `<option>`-Kinder bleiben strukturell unverändert.

Import-Ergänzung prüfen: Falls `FormField` noch nicht importiert ist, in den bestehenden `@/components/ui`-Import einfügen.

Placeholder-Text auf korrekten Umlaut prüfen: `'Person aus der Gruppe wählen …'` (C-UMLAUTE).

Alle anderen `onChange`-Handler und Zustandsvariablen bleiben unverändert.

Datei-Größe: ~318 Zeilen aktuell. Diese Änderung fügt nur ~2 Zeilen hinzu (FormField-Tags). Unter 450 Zeilen.
  </action>
  <verify>
    <automated>npm --prefix frontend run lint -- --max-warnings=0 2>&1 | grep -i "AnimeContributionModal\|no-restricted" | head -10</automated>
  </verify>
  <done>
    `AnimeContributionModal.tsx` enthält keinen `<Select>` ohne `FormField`-Wrapper mehr. `npm run lint` meldet keinen no-restricted-syntax-Fehler für diese Datei.
  </done>
  <acceptance_criteria>
    - `AnimeContributionModal.tsx` enthält `<FormField` mit einem `<Select>` als Kind für den Member-Picker
    - Kein `<Select` ohne umgebenden `<FormField` in der Datei (außer in den nativen select-kapselnden Primitives selbst)
    - `npm --prefix frontend run lint` gibt keinen `no-restricted-syntax`-Fehler für `AnimeContributionModal.tsx`
    - `npm --prefix frontend run typecheck` exit 0
    - `AnimeContributionModal.tsx` ≤ 322 Zeilen
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ReleaseVersionBreakdown.tsx — Primitives/Tokens + D-16-Dokumentation in Repository</name>
  <files>
    frontend/src/components/anime/ReleaseVersionBreakdown.tsx,
    backend/internal/repository/anime_contributions_member_repository.go
  </files>
  <read_first>
    - frontend/src/components/anime/ReleaseVersionBreakdown.tsx (vollständig: aktuelle Implementierung, native HTML-Elemente die durch Primitives ersetzt werden müssen, bestehende Imports)
    - backend/internal/repository/anime_contributions_member_repository.go (vollständig: Delete-Methode identifizieren — Hard-DELETE-Zeile, Funktions-Signatur und Kommentar-Block als Einfügepunkt für D-16-Dokumentation; Pfad ist verifiziert: backend/internal/repository/anime_contributions_member_repository.go)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-CONTEXT.md (D-14: Angleichung ReleaseVersionBreakdown.tsx an Primitives/Tokens)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-RESEARCH.md (D-16: Hard-DELETE-Verifikation für anime_contributions Z. 381–409, Pitfall 5 Z. 380–386)
    - frontend/src/components/ui/index.ts (verfügbare Primitives: Badge, Button, Card etc.)
  </read_first>
  <action>
**Teil 1 — ReleaseVersionBreakdown.tsx angleichen (D-14):**

Datei vollständig lesen. Native HTML-Elemente prüfen:
- Natives `<button>` → `Button`-Primitive aus `@/components/ui` ersetzen
- Natives `<select>` → `Select`+`FormField` ersetzen
- Natives `<input>` → `Input`-Primitive ersetzen (falls vorhanden)
- Benennung: Falls "Beiträge"-String als user-facing Text vorhanden → in "Mitwirkende" umbenennen (D-15)
- CSS-Token-Angleichung: Falls Hard-coded px-Werte → `--space-*`-Tokens verwenden (D-14 Spirits)

Nur genuine Verstöße gegen D-14 beheben — KEINE vollständige Umschreibung. Das Ziel ist ESLint-Konformität und Primitives-Compliance, nicht optisches Redesign.

WICHTIG: Datei-Größe beachten. Falls `ReleaseVersionBreakdown.tsx` nach Umbau > 450 Zeilen → Größe explizit prüfen und im Summary vermerken.

**Teil 2 — D-16-Constraint-Dokumentation in anime_contributions_member_repository.go:**

Datei lesen um die Delete-Methode (Hard-DELETE) zu lokalisieren. Zur Delete-Methode einen Kommentar-Block hinzufügen (direkt über der Funktions-Signatur oder als ersten Kommentar im Funktions-Body):

Der Kommentar muss enthalten: Verweis auf D-16, Hinweis auf Hard-DELETE (kein Soft-Delete), Hinweis auf fehlendes deleted_at-Feld in anime_contributions, Folgearbeit-Kennzeichnung.

Beispiel-Form (Wortlaut anpassen an echte Funktions-Signatur nach dem Lesen):
- Kommentar-Zeile 1: Delete entfernt eine Contribution physisch (Hard-DELETE, kein Soft-Delete).
- Kommentar-Zeile 2: CONSTRAINT D-16: Leader-Löschen soll langfristig Soft-Delete nutzen (deleted_at-Feld).
- Kommentar-Zeile 3: Da anime_contributions kein deleted_at-Schema-Slot hat, bleibt dies Folgearbeit.
- Kommentar-Zeile 4: Audit-Logs für gelöschte Contributions enthalten keine Tombstone-Records.

Kein anderer Code in der Datei anfassen — nur der Kommentar-Block.
  </action>
  <verify>
    <automated>npm --prefix frontend run lint 2>&1 | grep -v "^#" | grep -c "no-restricted-syntax" ; echo "Expected: 0 new violations"</automated>
  </verify>
  <done>
    `ReleaseVersionBreakdown.tsx` ist ESLint-konform. D-16-Risiko ist als Kommentar im Repository-File dokumentiert. `npm run lint` zeigt keine neuen no-restricted-syntax-Verletzungen.
  </done>
  <acceptance_criteria>
    - `npm --prefix frontend run lint` gibt keinen `no-restricted-syntax`-Fehler für `ReleaseVersionBreakdown.tsx`
    - Falls "Beiträge" in `ReleaseVersionBreakdown.tsx` als user-facing String vorhanden war → durch "Mitwirkende" ersetzt (D-15)
    - `backend/internal/repository/anime_contributions_member_repository.go` enthält Kommentar mit Strings "D-16" und "Soft-Delete" und "Folgearbeit"
    - `npm --prefix frontend run typecheck` exit 0
    - `cd backend && go build ./...` exit 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| D-16 Soft-Delete-Lücke | Contributor-Deletes sind unwiderruflich bis Folgearbeit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-D16 | Tampering | `anime_contributions` Hard-DELETE | accept | D-16 fordert keinen Vollausbau in Phase 83. Risiko als Kommentar in `anime_contributions_member_repository.go` dokumentiert. Folgearbeit: `deleted_at`-Spalte + Soft-Delete-Migration in eigener Phase |
| T-83-UI14 | Tampering | natives `<select>` in Contribution-UI | mitigate | `FormField`+`Select` ersetzen natives `<select>` in `AnimeContributionModal.tsx`; ESLint-Verifikation via `npm run lint` |
| T-83-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Pakete |
</threat_model>

<verification>
- `npm --prefix frontend run lint` → 0 neue no-restricted-syntax-Fehler in `AnimeContributionModal.tsx` und `ReleaseVersionBreakdown.tsx`
- `npm --prefix frontend run typecheck` → exit 0
- `cd backend && go build ./...` → exit 0
- `backend/internal/repository/anime_contributions_member_repository.go` enthält String "D-16"
</verification>

<success_criteria>
UI-Konsolidierung abgeschlossen: `AnimeContributionModal.tsx` und `ReleaseVersionBreakdown.tsx` sind ESLint-konform und nutzen ausschließlich `@/components/ui`-Primitives. D-16-Soft-Delete-Lücke ist als Constraint-Kommentar in `anime_contributions_member_repository.go` dokumentiert. Alle D-01..D-16 sind abgedeckt.
</success_criteria>

<output>
Create `.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-07-SUMMARY.md` when done.
</output>
