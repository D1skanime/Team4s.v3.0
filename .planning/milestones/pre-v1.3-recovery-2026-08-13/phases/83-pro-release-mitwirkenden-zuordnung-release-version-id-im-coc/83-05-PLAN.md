---
phase: 83
plan: 05
type: execute
wave: 4
depends_on: [83-04]
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/ReleaseContributionDrawer.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/ReleaseContributionDrawer.test.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/ContributorAvatar.tsx
  - frontend/src/lib/api.ts
  - frontend/src/types/fansub.ts
  - frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css
autonomous: true
requirements: [D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-15]

must_haves:
  truths:
    - "per D-06/D-07: ReleaseContributionDrawer öffnet als rechter Drawer mit vorbefüllter Rollen-Liste (geerbtes Projektteam beim Öffnen)"
    - "per D-07: Pro Zeile: Rolle + Avatar (ContributorAvatar) + Person + Zeilenaktionen (Rolle ändern ⋯, Entfernen Trash2)"
    - "per D-06/C-STAGED: Drawer ist staged — nur explizites Speichern/Abbrechen, kein Auto-Save"
    - "per D-12: Person-Picker zeigt nur Mitglieder der aktuellen Fansub-Gruppe"
    - "per D-09: Mehrere Personen pro Rolle erlaubt — stagedRows kann mehrere Einträge mit demselben role_code halten; kein Zwang zur Exklusivität beim Hinzufügen/Umhängen"
    - "per D-15: Alle UI-Texte verwenden 'Mitwirkende' (nicht 'Beiträge')"
    - "per D-14/C-UI-PRIMITIVES: Ausschließlich @/components/ui-Primitives — kein natives <select>/<input>/<button>"
    - "per C-UMLAUTE: Alle deutschen Strings mit korrekten Umlauten (ä/ö/ü/ß)"
    - "Rollen-Picker: Statische Liste aus role_definitions-Katalog (Migration 0085) — bewusste Abweichung, da kein GET /api/v1/admin/role-definitions-Endpoint existiert (VERIFIED: Backend-Routen-Scan)"
    - "Vitest-Tests für ReleaseContributionDrawer sind grün"
  artifacts:
    - path: "frontend/src/app/admin/fansubs/[id]/edit/ReleaseContributionDrawer.tsx"
      provides: "Override-Drawer-Komponente (staged, Drawer-Primitive)"
    - path: "frontend/src/app/admin/fansubs/[id]/edit/ContributorAvatar.tsx"
      provides: "Mini-Avatar-Komponente (Initialen + optionales Bild)"
    - path: "frontend/src/app/admin/fansubs/[id]/edit/ReleaseContributionDrawer.test.tsx"
      provides: "Vitest-Tests für Drawer (D-06..D-08)"
    - path: "frontend/src/lib/api.ts"
      provides: "listEffectiveContributionsForVersion Helper"
    - path: "frontend/src/types/fansub.ts"
      provides: "EffectiveContributionRow + EffectiveContributionsResponse Typen"
  key_links:
    - from: "ReleaseContributionDrawer.tsx"
      to: "api.ts listEffectiveContributionsForVersion"
      via: "listEffectiveContributionsForVersion(releaseVersionId, fansubId)"
      pattern: "listEffectiveContributionsForVersion"
    - from: "ReleaseContributionDrawer.tsx"
      to: "api.ts upsertAnimeContribution + deleteAnimeContribution"
      via: "handleSave → Promise.all([deletes, upserts])"
      pattern: "upsertAnimeContribution|deleteAnimeContribution"
---

<objective>
Override-Drawer + API-Helper: `ReleaseContributionDrawer.tsx` (staged Editor), `ContributorAvatar.tsx` (Mini-Avatar), `listEffectiveContributionsForVersion` in `api.ts`, Typen in `fansub.ts`.

Purpose: Plan 05 liefert die Drawer-Komponente und ihre Abhängigkeiten. Plan 06 verdrahtet sie in `page.tsx`. Diese Trennung hält `page.tsx` (>3200 Zeilen) unberührt und ist durch das 450-Zeilen-Limit vorgeschrieben.

Output: Vollständiger Drawer mit Rollen-Liste, Zeilenaktionen, staged Speichern/Abbrechen. Vitest-Tests grün.

Rollen-Picker — bewusste Abweichung vom Live-Katalog-Endpoint: Es existiert kein `GET /api/v1/admin/role-definitions?context=anime_contribution`-Endpoint im Backend (VERIFIED: Routen-Scan ergab keinen Treffer). Die Rollen-Liste wird daher als statische Map aus den bekannten `role_definitions`-Einträgen (Migration 0085, Spalten `code` + `label_de`) im Drawer definiert. Dies ist eine bewusst getrackte Abweichung: neue Katalog-Rollen erscheinen erst nach Frontend-Redeploy — akzeptabel für die aktuelle Admin-Nutzung, da der Katalog stabil ist. Folgearbeit: Endpoint anlegen + Drawer auf dynamischen Abruf umstellen.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-CONTEXT.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-RESEARCH.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-PATTERNS.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-UI-SPEC.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-04-SUMMARY.md

<interfaces>
<!-- Aus frontend/src/components/ui/Drawer.tsx (verifiziert) -->
<!--
// Drawer Props: open: boolean, onClose: () => void, title: string, description?: string, children: ReactNode, footer?: ReactNode
// Rendert null wenn open=false
-->

<!-- Aus frontend/src/lib/api.ts (relevante bestehende Helpers) -->
<!--
// upsertAnimeContribution(fansubId, animeId, payload) — akzeptiert release_version_id im payload
// deleteAnimeContribution(fansubId, animeId, contributionId) — löscht nach contribution_id
// listUnifiedGroupMembers(fansubId) → UnifiedGroupMember[] (member_id, display_name, source, group_roles[])
// authorizedFetch, parseApiErrorPayload, ApiError, getApiBaseUrl — alle vorhanden
-->

<!-- Aus frontend/src/types/fansub.ts -->
<!--
// AnimeContribution interface enthält release_version_id?: number
// UnifiedGroupMember: member_id, display_name, source, has_app_account, group_roles[]
-->

<!-- Aus frontend/src/components/ui/index.ts (verifiziert) -->
<!--
// Button, Drawer, EmptyState, LoadingState, ErrorState, Select, FormField, Badge — alle vorhanden
// Button variant: "primary" | "ghost" | "danger" | "secondary" | "subtle"
// Button props: loading?: boolean, iconOnly?: boolean, size?: "sm" | "md"
-->

<!-- Aus 83-UI-SPEC.md -->
<!--
// ContributorAvatar: 32px Kreis, --surface-sunken bg, --text-soft text, --radius-md
// Copywriting: "Mitwirkende" (Drawer title), "Rollen für {Release}" (description),
//   "Rolle / Person hinzufügen", "Speichern", "Abbrechen", "Noch keine Mitwirkenden"
//   "Mitwirkende entfernen" (aria-label), "Rolle ändern" (aria-label)
-->

<!-- Rollen-Picker: Statische Liste (kein Live-Endpoint verfügbar) -->
<!-- Bekannte role_definitions aus Migration 0085 (code → label_de): -->
<!-- translator → "Übersetzer", timer → "Timer", typesetter → "Typesetter", -->
<!-- editor → "Editor", encoder → "Encoder", raw_provider → "Raw-Provider", -->
<!-- quality_checker → "Qualitätsprüfer", designer → "Designer" -->
<!-- TRACKED DEVIATION: kein GET /api/v1/admin/role-definitions-Endpoint vorhanden -->
<!-- Folgearbeit: Endpoint anlegen + Drawer auf dynamischen Katalog-Abruf umstellen -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Typen (fansub.ts) + API-Helper (api.ts) + ContributorAvatar</name>
  <files>
    frontend/src/types/fansub.ts,
    frontend/src/lib/api.ts,
    frontend/src/app/admin/fansubs/[id]/edit/ContributorAvatar.tsx,
    frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css
  </files>
  <read_first>
    - frontend/src/types/fansub.ts (Ende der Datei — bestehende Interfaces prüfen, AnimeContribution, UnifiedGroupMember)
    - frontend/src/lib/api.ts (listUnifiedGroupMembers Z. ~7284 als Fetch-Pattern-Vorlage; upsertAnimeContribution für release_version_id-Support; api.ts-Ende für Einfügepunkt)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-PATTERNS.md (Abschnitt api.ts Z. 773–826, ContributorAvatar Z. 621–663)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-UI-SPEC.md (Avatar-Tokens Z. 171–173, Copywriting)
    - frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css (Ende — bestehende CSS-Klassen prüfen, Einfügepunkt für neue)
  </read_first>
  <action>
**1. `frontend/src/types/fansub.ts` — neue Interfaces anfügen:**
`EffectiveContributionRow` mit Feldern: `contribution_id: number`, `member_id: number`, `member_display_name: string`, `member_avatar_url?: string | null`, `role_codes: string[]`.
`EffectiveContributionsResponse` mit Feldern: `data: EffectiveContributionRow[]`, `meta: { is_override: boolean; source: 'release_version' | 'anime_default' }`.

**2. `frontend/src/lib/api.ts` — neuen Helper `listEffectiveContributionsForVersion` anfügen:**
JSDoc-Kommentar: Aufgelöster Mitwirkenden-Satz für eine Release-Version (Override oder Projekt-Default, D-02). Endpoint: GET /api/v1/admin/release-versions/:versionId/contributions/effective.

Signatur: `async function listEffectiveContributionsForVersion(releaseVersionId: number, fansubGroupId: number, authToken?: string): Promise<EffectiveContributionsResponse>`.

Pattern: Identisch zu `listUnifiedGroupMembers` (authorizedFetch + parseApiErrorPayload + ApiError). URL: `${API_BASE_URL}/api/v1/admin/release-versions/${releaseVersionId}/contributions/effective?fansub_group_id=${fansubGroupId}`.

Import-Ergänzung in `api.ts`: `EffectiveContributionsResponse` aus `../types/fansub` importieren (falls api.ts Typen importiert — sonst lokale Interface-Definition).

**3. `ContributorAvatar.tsx` — neue Datei anlegen:**
`'use client'`. Props: `name: string`, `avatarUrl?: string | null`. Initialen aus den ersten zwei Wörtern des Namens (je erster Buchstabe, uppercase). Wenn avatarUrl: `<img src={avatarUrl} alt={name} className={styles.contributorAvatar} />`. Sonst: `<span className={styles.contributorAvatarInitials} aria-label={name}>{initials}</span>`.

**4. `FansubEdit.module.css` — CSS-Klassen anfügen:**
Neue Klassen am Ende (keine bestehenden ändern): `.contributorAvatar` (32px, border-radius var(--radius-md), object-fit cover, flex-shrink 0), `.contributorAvatarInitials` (32px, inline-flex, align-items center, justify-content center, border-radius var(--radius-md), background var(--surface-sunken), color var(--text-soft), font-size 12px, user-select none, flex-shrink 0), `.contributionRow` (flex, align-items center, gap var(--space-2), padding var(--space-2) 0, border-bottom 1px solid var(--border-subtle)), `.contributionRowActions` (flex, margin-left auto, gap var(--space-1)), `.contributionRoleLabel` (font-size 14px, font-weight 700, color var(--text-primary), min-width 80px), `.contributionMemberName` (font-size 16px, flex 1).
  </action>
  <verify>
    <automated>npm --prefix frontend run typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>
    `EffectiveContributionRow` + `EffectiveContributionsResponse` in `fansub.ts`. `listEffectiveContributionsForVersion` in `api.ts`. `ContributorAvatar.tsx` existiert. CSS-Klassen in `FansubEdit.module.css`. TypeScript kompiliert ohne Fehler.
  </done>
  <acceptance_criteria>
    - `frontend/src/types/fansub.ts` enthält `EffectiveContributionRow` mit Feld `contribution_id: number`
    - `frontend/src/lib/api.ts` enthält `listEffectiveContributionsForVersion` als exportierte Funktion
    - `ContributorAvatar.tsx` exportiert `function ContributorAvatar`; rendert `<img>` wenn avatarUrl vorhanden, sonst `<span>` mit Initialen
    - `FansubEdit.module.css` enthält `.contributorAvatar` und `.contributorAvatarInitials` mit `--radius-md`, `--surface-sunken`, `--text-soft`
    - `npm --prefix frontend run typecheck` exit 0 (oder keine neuen Fehler durch diese Datei)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReleaseContributionDrawer.tsx + Vitest-Tests</name>
  <files>
    frontend/src/app/admin/fansubs/[id]/edit/ReleaseContributionDrawer.tsx,
    frontend/src/app/admin/fansubs/[id]/edit/ReleaseContributionDrawer.test.tsx
  </files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionModal.tsx (vollständig: Staged-State-Pattern Z. 44–47, 135–187; Person-Picker; Error-Handling Z. 182–187)
    - frontend/src/components/ui/Drawer.tsx (Props-Struktur: open, onClose, title, description, children, footer)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-PATTERNS.md (Abschnitt ReleaseContributionDrawer.tsx Z. 480–610)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-UI-SPEC.md (Interaction Contract Z. 162–173, Copywriting Z. 127–157, Destruktive Aktionen Z. 155–157)
    - frontend/src/app/admin/fansubs/[id]/edit/page.test.tsx (Test-Struktur + Mocking-Pattern für Vitest)
  </read_first>
  <behavior>
    - Test: Drawer rendert nicht wenn open=false
    - Test: Drawer rendert Titel "Mitwirkende" wenn open=true
    - Test: Wenn contributionsResult.data leer → EmptyState mit "Noch keine Mitwirkenden" sichtbar
    - Test: Wenn contributionsResult.data hat Einträge → Rollen-Liste rendert Member-Namen
    - Test: "Entfernen"-Button (Trash2) klicken entfernt Zeile aus staged-Liste (kein API-Call)
    - Test: "Abbrechen"-Button schließt Drawer via onClose
    - Test: Kein natives <select> in der Komponente (ESLint/Code-Assertion)
  </behavior>
  <action>
**ReleaseContributionDrawer.tsx — Props-Interface:**
`open: boolean`, `fansubId: number`, `animeId: number`, `releaseVersionId: number`, `releaseTitle: string`, `onClose: () => void`, `onSaved: () => void`.

**State:** `stagedRows: EffectiveContributionRow[]`, `removedIds: number[]`, `addedRows: Array<{memberId: number, roleCodes: string[]}>`, `loading: boolean`, `saving: boolean`, `error: string | null`, `members: UnifiedGroupMember[]`, `isOverride: boolean`, `addingRow: boolean`, `newMemberId: number | null`, `newRoleCode: string | null`.

**Beim Öffnen (useEffect auf `open`):** Paralleles Laden via `Promise.all([listUnifiedGroupMembers(fansubId), listEffectiveContributionsForVersion(releaseVersionId, fansubId)])`. Fehlerbehandlung mit `setError`.

**Rollen-Liste (UI-SPEC Interaction Contract):**
`Drawer` mit `title="Mitwirkende"`, `description=\`Rollen für ${releaseTitle}\``. Kein separater "Vom Projekt geerbtes Team"-Block (D-07, C-NO-DIFF). Pro `stagedRows`-Eintrag: `div.contributionRow` mit `[roleLabel] · [ContributorAvatar] [memberName] · [⋯ Rolle ändern] [Trash2 Entfernen]`. Button-Varianten: ghost+iconOnly+size="sm" für Rolle ändern; danger+iconOnly+size="sm" für Entfernen. "Entfernen" entfernt Zeile aus `stagedRows` und fügt contribution_id zu `removedIds` hinzu (staged — kein API-Call). Button variant="secondary" + Plus-Icon: "Rolle / Person hinzufügen" → setzt addingRow=true. EmptyState wenn `stagedRows.length === 0 && !addingRow`.

**Rollen-Picker — statische Liste (bewusste Abweichung, kein Live-Endpoint):**
Da kein `GET /api/v1/admin/role-definitions`-Endpoint im Backend existiert (VERIFIED: Routen-Scan), wird eine statische Konstante `ANIME_CONTRIBUTION_ROLES` im Drawer definiert — abgeleitet aus Migration 0085 (`role_definitions`-Einträge mit `contexts @> 'anime_contribution'`):
`{ code: 'translator', label: 'Übersetzer' }`, `{ code: 'timer', label: 'Timer' }`, `{ code: 'typesetter', label: 'Typesetter' }`, `{ code: 'editor', label: 'Editor' }`, `{ code: 'encoder', label: 'Encoder' }`, `{ code: 'raw_provider', label: 'Raw-Provider' }`, `{ code: 'quality_checker', label: 'Qualitätsprüfer' }`, `{ code: 'designer', label: 'Designer' }`.
Kommentar: `// STATISCHE LISTE — kein /role-definitions-Endpoint vorhanden. Folgearbeit: Katalog-Endpoint + dynamischer Abruf.`

**Footer (staged — C-STAGED):** Button variant="ghost" → "Abbrechen"; Button variant="primary" loading={saving} → "Speichern".

**handleSave-Algorithmus (Pitfall 4):**
1. `await Promise.all(removedIds.map(id => deleteAnimeContribution(fansubId, animeId, id)))` — löscht GENAU die versions-spezifischen IDs
2. `await Promise.all(addedRows.map(r => upsertAnimeContribution(fansubId, animeId, {member_id: r.memberId, role_codes: r.roleCodes, release_version_id: releaseVersionId})))` 
3. Bei Fehler: `setError("Speichern fehlgeschlagen. Bitte Eingaben prüfen und erneut versuchen.")`
4. Erfolg: `onSaved()`, `onClose()`

**Person-Picker:** Optionen aus `members` (UnifiedGroupMember — D-12). Bereits in `stagedRows` enthaltene member_ids ausfiltern. Select+FormField (D-14).

**Datei-Größe:** Ziel ~200–280 Zeilen. Wenn >300 Zeilen → Hilfsfunktionen in ReleaseContributionDrawer.helpers.ts auslagern.

**ReleaseContributionDrawer.test.tsx:** `@vitest-environment jsdom` Direktive. Mock für `listEffectiveContributionsForVersion`, `listUnifiedGroupMembers`, `upsertAnimeContribution`, `deleteAnimeContribution` (alle via `vi.mock('@/lib/api', ...)`). Mindestens 5 Testfälle (gemäß Behavior-Block oben). Testing-Library: `render`, `screen`, `fireEvent` oder `userEvent`.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- --reporter=dot ReleaseContributionDrawer 2>&1 | tail -20</automated>
  </verify>
  <done>
    `ReleaseContributionDrawer.tsx` und `ReleaseContributionDrawer.test.tsx` existieren. Alle Vitest-Tests grün. `npm --prefix frontend run lint` meldet kein natives `<select>/<input>/<textarea>` in der neuen Datei.
  </done>
  <acceptance_criteria>
    - `ReleaseContributionDrawer.tsx` enthält keinen nativen `<select>`, `<input>`, `<textarea>` oder `<button>` (Primitive-Pflicht D-14)
    - `ReleaseContributionDrawer.tsx` enthält Konstante `ANIME_CONTRIBUTION_ROLES` mit statischer Rollen-Liste UND Kommentar "kein /role-definitions-Endpoint vorhanden" (bewusste Abweichung dokumentiert)
    - Alle deutschen Strings enthalten korrekte Umlaute: "Mitwirkende", "Übersetzer", "Qualitätsprüfer", "Rollen für", "Speichern", "Abbrechen", "Noch keine Mitwirkenden", "Mitwirkende entfernen" (C-UMLAUTE)
    - Kein "Beiträge"-String in der Datei (C-NAMING D-15)
    - `npm --prefix frontend run test -- --reporter=dot ReleaseContributionDrawer` → alle Tests PASS
    - `ReleaseContributionDrawer.tsx` ≤ 320 Zeilen
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → Backend (Speichern) | release_version_id muss korrekt gesetzt sein beim Upsert |
| Person-Picker (D-12) | Nur Gruppen-Members erscheinen als Optionen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-CLIENTSIDE | Elevation of Privilege | Frontend-Picker | accept | Person-Picker zeigt nur `listUnifiedGroupMembers(fansubId)` — Gruppen-Scope bereits im API-Call. Backend validiert Ownership unabhängig |
| T-83-PITFALL4 | Tampering | handleSave DELETE-Logik | mitigate | `deleteAnimeContribution` nutzt `contribution_id` aus `EffectiveContributionRow` — löscht GENAU versions-spezifische IDs (Pitfall 4 aus RESEARCH.md) |
| T-83-UI | Tampering | release_version_id in Upsert | mitigate | `upsertAnimeContribution` sendet `release_version_id: releaseVersionId` (aus Props) — nicht frei eingegeben |
| T-83-STATICROLES | Tampering | Statische Rollen-Liste | accept | Katalog-Rollen aus Migration 0085 — stabil; neue Rollen erscheinen erst nach Frontend-Redeploy. Folgearbeit getrackt: Live-Endpoint anlegen |
| T-83-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen npm-Pakete |
</threat_model>

<verification>
- `npm --prefix frontend run test -- --reporter=dot ReleaseContributionDrawer` → alle Tests PASS
- `npm --prefix frontend run lint` → kein no-restricted-syntax Fehler in neuen Dateien
- `npm --prefix frontend run typecheck` → exit 0
- `ReleaseContributionDrawer.tsx` enthält String "Mitwirkende" (Drawer title, D-15)
- `ReleaseContributionDrawer.tsx` enthält Kommentar über statische Rollen-Liste (Abweichung getrackt)
</verification>

<success_criteria>
Override-Drawer vollständig: staged Editor mit Rollen-Liste, ContributorAvatar, Zeilenaktionen, Person-/Rollen-Picker (statisch, bewusste Abweichung dokumentiert), Speichern/Abbrechen. Alle Vitest-Tests grün. Keine UI-Primitives-Verstöße.
</success_criteria>

<output>
Create `.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-05-SUMMARY.md` when done.
</output>
