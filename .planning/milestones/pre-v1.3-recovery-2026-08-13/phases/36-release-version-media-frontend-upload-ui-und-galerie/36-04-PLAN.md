---
phase: 36-release-version-media-frontend-upload-ui-und-galerie
plan: 04
type: execute
wave: 4
depends_on:
  - 36-01-PLAN.md
  - 36-02-PLAN.md
  - 36-03-PLAN.md
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/page.test.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/page.test.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.test.tsx
autonomous: false
requirements:
  - RVM-FRONTEND-01
must_haves:
  truths:
    - "Automated tests confirm the drawer summary renders the 'Media verwalten' CTA and does NOT render upload controls."
    - "Automated tests confirm the editor Media/Assets tab is reachable and renders the context card with fansub/release context."
    - "A media-section error (e.g. failed fetch) does not blank the rest of the editor page — other tabs remain functional."
    - "No production file created or modified in Phase 36 exceeds 450 lines."
  artifacts:
    - path: "frontend/src/app/admin/fansubs/[id]/edit/page.test.tsx"
      provides: "Drawer summary regression: CTA present, upload controls absent"
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/page.test.tsx"
      provides: "Editor Media/Assets tab: reachable, context card, error isolation"
---

<objective>
Close Phase 36 with regression coverage for both host surfaces (drawer summary and editor Media/Assets tab) and an explicit line-count verification that no file exceeds the 450-line project guardrail.

Purpose: Proves the two-host navigation model is test-backed and that scope did not creep past the modularity constraint.
Output: Drawer test, editor page test, 450-line check, phase summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-release-version-media-frontend-upload-ui-und-galerie/36-CONTEXT.md
@frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditorPage.tsx
@frontend/src/app/admin/fansubs/[id]/edit/page.tsx
@frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.test.tsx
@frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drawer regression tests and editor page tests</name>
  <files>
    frontend/src/app/admin/fansubs/[id]/edit/page.test.tsx,
    frontend/src/app/admin/episode-versions/[versionId]/edit/page.test.tsx
  </files>
  <read_first>
    - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.test.tsx (test style reference — use same Vitest + RTL setup)
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx (how ReleaseVersionMediaDrawerSummary is imported and when it renders)
    - frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditorPage.tsx (how 'media' tab renders)
    - frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.tsx (what to expect inside the media tab)
  </read_first>
  <action>
**Step A — Create `frontend/src/app/admin/fansubs/[id]/edit/page.test.tsx`:**

Mock `useReleaseVersionMedia` to return `{ items: [], isLoading: false, error: null, reload: vi.fn(), uploadItems: [], startUpload: vi.fn(), retryUpload: vi.fn(), clearUploadQueue: vi.fn() }`.
Mock `getReleaseVersionMedia` to return an empty list.

Test T1 — Drawer summary renders CTA:
- Render `ReleaseVersionMediaDrawerSummary` with `versionId=1`, `fansubName="SubGroup"`, `releaseVersionLabel="v1"`
- Assert: element with text "Media verwalten" is in the document
- Assert: the CTA href contains `/admin/episode-versions/1/edit`

Test T2 — Drawer summary has NO upload controls:
- Same render as T1
- Assert: `container.querySelector('input[type="file"]')` returns null (no file input)
- Assert: no element with text matching `/hochladen|Datei auswählen/i` from an upload action button exists

**Step B — Create `frontend/src/app/admin/episode-versions/[versionId]/edit/page.test.tsx`:**

Mock `useEpisodeVersionEditor` to return minimal contextData:
```typescript
contextData: {
  version: { id: 42, anime_id: 1, release_version: 'v1', episode_number: 1 },
  selected_groups: [{ id: 10, name: 'SubGroup' }],
  anime_title: 'Test Anime',
}
```
Mock `useReleaseVersionMedia` to return empty items.

Test T3 — Media/Assets tab button exists:
- Render `EpisodeVersionEditorPage`
- Assert: `getByRole('button', { name: 'Media / Assets' })` is in the document

Test T4 — Media/Assets tab shows context card:
- Render `EpisodeVersionEditorPage`, click "Media / Assets" tab button
- Assert: element containing "SubGroup" (fansub group name) is visible
- Assert: element containing "v1" (release version) is visible

Test T5 — Media section error does not blank editor:
- Render `EpisodeVersionEditorPage` with `useReleaseVersionMedia` mocked to return `error: "API Fehler"`
- Click "Media / Assets" tab button
- Assert: text "API Fehler" is visible in the media section
- Assert: `getByRole('button', { name: 'Informationen' })` and `getByRole('button', { name: 'Segmente' })` are STILL present (editor shell is not blanked)

**Step C — Run and confirm:**
All 5 tests pass: `cd frontend && npx vitest run page.test`
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npx vitest run page.test 2>&1 | tail -15</automated>
  </verify>
  <done>
    - `page.test.tsx` exists in the fansub edit directory with T1 (CTA present) and T2 (upload controls absent)
    - `page.test.tsx` exists in the episode-version edit directory with T3 (tab button), T4 (context card), T5 (error isolation)
    - All 5 tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — two-host navigation flow and 450-line guardrail check</name>
  <files>none</files>
  <action>Human verifies the end-to-end release-version-media flow across both host surfaces and confirms no production file exceeds 450 lines. No code changes made in this task.</action>
  <verify>
    <automated>echo "checkpoint task — human verification required"</automated>
  </verify>
  <done>Human approves all 7 steps below and line-count check confirms no file exceeds 450 lines.</done>
  <what-built>
    Phase 36 complete: shared release-version-media seam (types, API helpers, hook), compact drawer summary with "Media verwalten" CTA, full Media/Assets tab in the episode-version editor with context card + upload composer + categorized gallery + detail panel. Regression tests cover drawer CTA, editor tab, context card, and error isolation.
  </what-built>
  <how-to-verify>
    1. Open `/admin/fansubs/[id]/edit` for a fansub with releases. Click the "Edit" button on a release row to open the drawer. Confirm the drawer shows media counts and a "Media verwalten" button — no file picker visible.
    2. Click "Media verwalten". Confirm it navigates to `/admin/episode-versions/[versionId]/edit/` and lands on the "Media / Assets" tab.
    3. On the Media/Assets tab, confirm you see: (a) a context card with fansub group name and release version, (b) a category dropdown above the file input, (c) the file input and upload button are disabled until a category is selected.
    4. Select category "Release-Screenshot". Confirm the preview toggle appears. Change to "Spaßbild / Outtake". Confirm the preview toggle disappears.
    5. Upload one image. Confirm per-file progress row appears. After upload completes, confirm the image appears in the gallery under the correct category section.
    6. Click the gallery card. Confirm the detail panel opens with caption, sort-order fields and no category edit control.
    7. Confirm the other tabs (Informationen, Segmente, Dateien) still work without errors after interacting with the Media/Assets tab.

    Run the automated line-count check:
    `wc -l frontend/src/app/admin/episode-versions/\[versionId\]/edit/ReleaseVersionMediaSection.tsx frontend/src/app/admin/episode-versions/\[versionId\]/edit/ReleaseVersionMediaGallery.tsx frontend/src/app/admin/episode-versions/\[versionId\]/edit/ReleaseVersionMediaDetailPanel.tsx frontend/src/app/admin/episode-versions/\[versionId\]/edit/useReleaseVersionMedia.ts frontend/src/app/admin/fansubs/\[id\]/edit/ReleaseVersionMediaDrawerSummary.tsx`

    Confirm no file exceeds 450 lines.
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 verification steps pass and no file exceeds 450 lines. Describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Automated:
1. `cd C:/Users/admin/Documents/Team4s/frontend && npx vitest run ReleaseVersionMediaSection page.test`
2. `cd C:/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit`
3. Line-count check on all Phase-36 new production files (none may exceed 450 lines)

Manual: Human verifies the two-host navigation flow end-to-end (drawer CTA → editor tab → upload → gallery → detail panel).
</verification>

<success_criteria>
- Automated tests cover drawer-CTA presence, upload-controls-absent-from-drawer, editor tab reachability, context card content, and media-error isolation
- No production file in Phase 36 exceeds 450 lines (confirmed by wc -l check)
- Human verification passes all 7 steps
- `npx tsc --noEmit` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/36-release-version-media-frontend-upload-ui-und-galerie/36-04-SUMMARY.md`
</output>
