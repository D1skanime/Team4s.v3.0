---
phase: 36-release-version-media-frontend-upload-ui-und-galerie
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - frontend/src/lib/api.ts
  - database/migrations/0060_seed_image_media_type.up.sql
  - database/migrations/0060_seed_image_media_type.down.sql
autonomous: true
requirements:
  - RVM-FRONTEND-01
gap_closure: true

must_haves:
  truths:
    - "Nach docker-compose up --build startet das System ohne manuelle Migration — Migration 0059 ist automatisch angewendet."
    - "Ein Upload aus der Media/Assets-Ansicht sendet Dateien unter dem Schlüssel 'files[]' und erreicht den Backend-Handler ohne 400-Fehler."
    - "Der Backend-Handler kann Uploads erfolgreich persistieren, weil der Medientyp 'image' in der media_types-Tabelle vorhanden ist."
---

<objective>
Drei gezielte Bug-Fixes schließen die zwei UAT-Blocker aus Test 1 und Test 5 von Phase 36.

Purpose: Die Upload-Strecke ist in drei unabhängigen Stellen gebrochen. Jeder Fix adressiert einen konkreten Fehler ohne neue Features oder Architekturarbeit.

Output:
- docker-compose.yml: Backend-Service führt `./migrate up && ./server` statt nur `./server` aus.
- frontend/src/lib/api.ts: `uploadReleaseVersionMedia` sendet Dateien unter `files[]` statt `files`.
- Migration 0060: Seed-Insert für `image`-Zeile in `media_types`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-release-version-media-frontend-upload-ui-und-galerie/36-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto-Migration beim Container-Start aktivieren</name>
  <files>docker-compose.yml</files>
  <read_first>docker-compose.yml (Zeilen 27-71 — vollständige Backend-Service-Definition lesen, bevor etwas geändert wird)</read_first>
  <action>
In `docker-compose.yml` im Service `team4sv30-backend` eine `command`-Zeile hinzufügen, die Migrationen vor dem Server ausführt.

Direkt nach dem `depends_on`-Block (nach Zeile 70, vor dem Frontend-Service) folgende Zeile einfügen:

```yaml
    command: ["sh", "-c", "./migrate up && ./server"]
```

Einrückung: 4 Leerzeichen (konsistent mit den anderen Service-Properties wie `ports:`, `volumes:`, `depends_on:`).

Der Block `team4sv30-backend` soll danach so enden:
```yaml
    depends_on:
      team4sv30-db:
        condition: service_healthy
      team4sv30-redis:
        condition: service_started
    command: ["sh", "-c", "./migrate up && ./server"]
```

Keine anderen Änderungen in der Datei vornehmen.
  </action>
  <verify>
    <automated>grep -n "command.*migrate up.*server" /c/Users/admin/Documents/Team4s/docker-compose.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep "command.*migrate up.*server" docker-compose.yml` gibt eine Zeile zurück.
    - Die Zeile steht im `team4sv30-backend`-Service-Block.
    - Kein anderer Service hat eine `command`-Zeile erhalten.
    - Die YAML-Einrückung ist konsistent mit anderen Properties desselben Service-Blocks (4 Leerzeichen).
  </acceptance_criteria>
  <done>docker-compose.yml enthält `command: ["sh", "-c", "./migrate up && ./server"]` im Backend-Service. Nach `docker-compose up --build` werden ausstehende Migrationen automatisch angewendet.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend-Feldname von 'files' auf 'files[]' korrigieren</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>frontend/src/lib/api.ts (Zeilen 3095-3115 — die uploadReleaseVersionMedia-Funktion und den FormData-Block lesen)</read_first>
  <action>
In `frontend/src/lib/api.ts` exakt Zeile 3109 ändern.

Aktueller Wert:
```typescript
      body.append('files', file)
```

Neuer Wert:
```typescript
      body.append('files[]', file)
```

Nur diese eine Zeile ändern. Der Backend-Handler `UploadReleaseVersionMedia` liest den Multipart-Key `form.File["files[]"]` — der Schlüssel im Frontend muss exakt übereinstimmen.

Keine weiteren Änderungen in der Datei vornehmen.
  </action>
  <verify>
    <automated>grep -n "body.append('files\[\]'" /c/Users/admin/Documents/Team4s/frontend/src/lib/api.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "body.append('files\[\]'" frontend/src/lib/api.ts` gibt genau eine Zeile zurück.
    - `grep "body.append('files'," frontend/src/lib/api.ts` gibt keine Zeile zurück (alter Wert ist entfernt).
    - Die Datei ist syntaktisch valides TypeScript (kein Build-Fehler durch diese Änderung).
  </acceptance_criteria>
  <done>frontend/src/lib/api.ts sendet Dateien unter dem Schlüssel `files[]`. Der Backend-Handler empfängt die Dateien korrekt und gibt nicht mehr 400 `keine dateien hochgeladen` zurück.</done>
</task>

<task type="auto">
  <name>Task 3: Migration 0060 — Medientyp 'image' seeden</name>
  <files>
    database/migrations/0060_seed_image_media_type.up.sql
    database/migrations/0060_seed_image_media_type.down.sql
  </files>
  <read_first>database/migrations/0028_seed_media_types.up.sql (bestehende Seed-Migration als Pattern-Referenz lesen, um Stil und INSERT-Form zu übernehmen)</read_first>
  <action>
Zwei neue Migrationsdateien anlegen.

**Datei 1: `database/migrations/0060_seed_image_media_type.up.sql`**

Inhalt:
```sql
-- Migration 0060: Seed image media type for release-version media assets
INSERT INTO media_types (name) VALUES ('image') ON CONFLICT (name) DO NOTHING;
```

**Datei 2: `database/migrations/0060_seed_image_media_type.down.sql`**

Inhalt:
```sql
DELETE FROM media_types WHERE name = 'image';
```

Hinweise:
- Dateinamen exakt wie oben — das `migrate`-Tool erwartet `{nummer}_{beschreibung}.up.sql` / `.down.sql`.
- `ON CONFLICT (name) DO NOTHING` sorgt dafür, dass die Migration idempotent ist (Mehrfachausführung sicher).
- Keine weiteren Änderungen in anderen Dateien.
  </action>
  <verify>
    <automated>ls /c/Users/admin/Documents/Team4s/database/migrations/0060_seed_image_media_type.up.sql /c/Users/admin/Documents/Team4s/database/migrations/0060_seed_image_media_type.down.sql</automated>
  </verify>
  <acceptance_criteria>
    - `database/migrations/0060_seed_image_media_type.up.sql` existiert.
    - `database/migrations/0060_seed_image_media_type.down.sql` existiert.
    - `grep "INSERT INTO media_types.*image" database/migrations/0060_seed_image_media_type.up.sql` gibt eine Zeile zurück.
    - `grep "ON CONFLICT.*DO NOTHING" database/migrations/0060_seed_image_media_type.up.sql` gibt eine Zeile zurück.
    - `grep "DELETE FROM media_types WHERE name = 'image'" database/migrations/0060_seed_image_media_type.down.sql` gibt eine Zeile zurück.
  </acceptance_criteria>
  <done>Migration 0060 existiert mit Up- und Down-Skript. Nach `./migrate up` ist `image` in `media_types` vorhanden und `CreateMediaAssetWithStatusTx` kann den Upload-Record persistieren.</done>
</task>

</tasks>

<verification>
Alle drei Fixes sind unabhängig und können parallel verifiziert werden:

1. `grep "command.*migrate up.*server" docker-compose.yml` — gibt exakt eine Zeile im Backend-Service zurück.
2. `grep "body.append('files\[\]'" frontend/src/lib/api.ts` — gibt exakt eine Zeile zurück; `body.append('files',` existiert nicht mehr.
3. Beide Dateien `0060_seed_image_media_type.up.sql` und `.down.sql` existieren mit korrektem Inhalt.

End-to-end-Verifikation nach Ausführung aller drei Tasks:
- `docker-compose up --build` startet ohne manuellen `migrate`-Aufruf.
- `GET /api/v1/admin/release-versions/41/media` antwortet mit 200 (kein `db_schema_mismatch`).
- Ein authentifizierter Upload von der `/admin/episode-versions/41/edit?tab=media`-Seite kehrt mit einer Gallery-Card zurück (kein 400, kein `DB_FAILED`).
</verification>

<success_criteria>
- UAT Test 1 besteht: `docker-compose up --build` ohne manuelle Migration, GET media-Endpoint antwortet 200.
- UAT Test 5 besteht: Upload aus der UI sendet `files[]`, Backend persistiert den Asset-Record mit Medientyp `image`, Gallery-Card erscheint nach Upload.
- UAT Test 6 (blocked) wird durch Test-5-Fix entsperrt und kann erneut getestet werden.
</success_criteria>

<output>
Nach Abschluss `.planning/phases/36-release-version-media-frontend-upload-ui-und-galerie/36-05-SUMMARY.md` erstellen.
</output>
