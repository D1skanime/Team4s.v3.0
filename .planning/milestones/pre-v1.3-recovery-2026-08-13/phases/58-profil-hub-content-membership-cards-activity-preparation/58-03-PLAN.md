---
phase: 58-profil-hub-content-membership-cards-activity-preparation
plan: 03
type: execute
wave: 2
depends_on: [58-01]
files_modified:
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/components/layout/AppShellClientWrapper.tsx
autonomous: true
requirements: [P58-SC1, P58-SC2, P58-SC3, P58-SC4, P58-SC5]

must_haves:
  truths:
    - "App-Drawer zeigt unter 'Mein Bereich' einen dynamischen Abschnitt 'Meine Gruppen'"
    - "Jede Gruppe erscheint als klickbarer Link mit Users-Icon und Gruppenname"
    - "Klick navigiert zu /admin/fansubs/[fansub_group_id]/edit"
    - "AppShellClientWrapper übergibt memberships aus getOwnProfile() an AppShell"
    - "Statischer disabled-Eintrag 'Meine Gruppen' mit badge='bald' ist entfernt"
    - "Wenn keine Mitgliedschaften vorhanden sind, erscheint kein 'Meine Gruppen'-Abschnitt"
  artifacts:
    - path: frontend/src/components/layout/AppShell.tsx
      provides: AppShellProps.memberships; dynamischer Gruppen-Abschnitt im Drawer
    - path: frontend/src/components/layout/AppShellClientWrapper.tsx
      provides: WrapperProfile.memberships; Profile-Extraktion mit memberships-Feld
  key_links:
    - from: AppShellClientWrapper.tsx setProfile
      to: AppShell memberships prop
      via: memberships aus profile.memberships ?? []
      pattern: "memberships.*d\\.memberships"
    - from: AppShell Drawer
      to: /admin/fansubs/[id]/edit
      via: memberships.map href="/admin/fansubs/${m.fansub_group_id}/edit"
      pattern: "admin/fansubs.*fansub_group_id.*edit"
---

<objective>
App-Drawer um einen dynamischen „Meine Gruppen"-Abschnitt erweitern. AppShellClientWrapper leitet memberships aus dem getOwnProfile()-Response an AppShell weiter. Der statische disabled-Eintrag wird durch eine echte Liste ersetzt.

Purpose: Mitgliedschaftsnavigation aus der Profilseite herausnehmen (D-01) und als Drawer-Abschnitt nutzbar machen (D-02).
Output: Erweiterter AppShell mit memberships-Prop; AppShellClientWrapper mit memberships-Weitergabe.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.planning/phases/58-profil-hub-content-membership-cards-activity-preparation/58-CONTEXT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/58-profil-hub-content-membership-cards-activity-preparation/58-PATTERNS.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/frontend/src/components/layout/AppShell.tsx
@C:/Users/admin/Documents/Team4s/frontend/src/components/layout/AppShellClientWrapper.tsx
@C:/Users/admin/Documents/Team4s/frontend/src/types/profile.ts

<interfaces>
<!-- Aus 58-PATTERNS.md — AppShell-Erweiterung -->
// Neuer Typ in AppShell.tsx:
type AppShellMembership = {
  fansub_group_id: number
  fansub_group_name: string
  fansub_group_slug: string
}

// AppShellProps Erweiterung:
export interface AppShellProps {
  // ...bestehende Felder...
  memberships?: AppShellMembership[]  // NEU — optional, default []
}

// Statischer Eintrag (ERSETZEN per D-02):
{ label: 'Meine Gruppen', icon: <Users size={17} />, disabled: true, badge: 'bald' }
// → durch dynamischen Abschnitt ersetzen

// AppShellClientWrapper WrapperProfile Erweiterung:
interface WrapperProfile {
  // ...bestehende Felder...
  memberships?: Array<{
    fansub_group_id: number
    fansub_group_name: string
    fansub_group_slug: string
  }>
}

// Gruppen-Link-Ziel (verifiziert: Route existiert als /admin/fansubs/[id]/edit):
// href="/admin/fansubs/${m.fansub_group_id}/edit"

<!-- Route-Verifikation: frontend/src/app/admin/fansubs/[id]/edit/page.tsx existiert -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AppShell um memberships-Prop und dynamischen Gruppen-Abschnitt erweitern</name>
  <files>
    frontend/src/components/layout/AppShell.tsx
  </files>
  <read_first>
    - frontend/src/components/layout/AppShell.tsx — vollständige Datei lesen; insbesondere AppShellProps Interface (Zeilen ~40–46), myItems-Array (Zeilen ~88–92), NavGroup-Render-Struktur und CSS-Klassen (navGroup, navGroupLabel, navItem, navIcon) identifizieren
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx — erste Zeile lesen um zu bestätigen dass die Route existiert
  </read_first>
  <behavior>
    - Test 1: Wenn memberships prop leer oder nicht übergeben, erscheint kein "Meine Gruppen"-Abschnitt im Drawer
    - Test 2: Wenn memberships 2 Einträge hat, erscheinen 2 Links mit den Gruppennamen
    - Test 3: Link-Ziel ist "/admin/fansubs/[fansub_group_id]/edit"
    - Test 4: AppShellProps akzeptiert memberships?: AppShellMembership[] ohne TypeScript-Fehler
  </behavior>
  <action>
    In frontend/src/components/layout/AppShell.tsx:

    Neuen lokalen Typ AppShellMembership direkt über AppShellProps definieren: { fansub_group_id: number; fansub_group_name: string; fansub_group_slug: string }.

    AppShellProps um neues optionales Feld memberships?: AppShellMembership[] erweitern.

    In der Komponenten-Funktion (oder in AppShellNavGroups falls die Gruppen-Abschnitte dort gerendert werden) den statischen disabled-Eintrag { label: 'Meine Gruppen', icon: <Users size={17} />, disabled: true, badge: 'bald' } aus der myItems-Liste entfernen.

    An derselben Position im Render-Output einen bedingten Abschnitt einfügen: wenn memberships && memberships.length > 0, dann einen div-Block mit Klasse styles.navGroup (oder äquivalente existierende Klasse); darin einen p-Tag mit Klasse styles.navGroupLabel und Text "Meine Gruppen"; darunter für jede Membership einen Link (next/link) mit href="/admin/fansubs/${m.fansub_group_id}/edit"; Link-Inhalt: span mit styles.navIcon + Users size={17} Icon (lucide-react), span mit m.fansub_group_name.

    Wenn memberships undefined oder leer, den ganzen Abschnitt nicht rendern (kein leeres nav-Element).

    Den "Meine Beiträge"-Eintrag ({ label: 'Meine Beiträge', icon: <Compass size={17} />, disabled: true, badge: 'bald' }) unverändert lassen — bleibt disabled in Phase 58.

    Alle Strings auf korrekte Umlaute prüfen (D-17).
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>npm run typecheck fehlerfrei; AppShellProps hat memberships-Feld; statischer "Meine Gruppen"-disabled-Eintrag nicht mehr im myItems-Array; dynamischer Gruppen-Abschnitt gerendert wenn memberships vorhanden</done>
</task>

<task type="auto">
  <name>Task 2: AppShellClientWrapper memberships aus getOwnProfile() weiterreichen</name>
  <files>
    frontend/src/components/layout/AppShellClientWrapper.tsx
  </files>
  <read_first>
    - frontend/src/components/layout/AppShellClientWrapper.tsx — vollständige Datei lesen; WrapperProfile Interface (Zeilen ~12–17), setProfile()-Aufruf (Zeilen ~45–51), AppShell-Render mit Props (Zeilen ~73–79) identifizieren
    - frontend/src/types/profile.ts — MemberProfileMembership Interface lesen (Felder: fansub_group_id, fansub_group_name, fansub_group_slug vorhanden prüfen)
  </read_first>
  <action>
    In frontend/src/components/layout/AppShellClientWrapper.tsx:

    WrapperProfile-Interface um memberships-Feld erweitern: memberships?: Array<{ fansub_group_id: number; fansub_group_name: string; fansub_group_slug: string }>.

    Im setProfile()-Aufruf (nach canAdmin) memberships aus dem Profil-Response extrahieren: memberships: d.memberships ?? [] — dies adressiert Pitfall 3 aus RESEARCH.md.

    Im AppShell-JSX das neue memberships-Prop übergeben: memberships={activeProfile?.memberships ?? []}.

    Nur die drei Felder fansub_group_id, fansub_group_name, fansub_group_slug aus MemberProfileMembership übernehmen — kein Logo-Feld nötig (Pitfall 4: Fallback-Icon im Drawer reicht, per D-02).

    Kein zweiter API-Call für Memberships — ausschließlich aus dem bereits vorhandenen getOwnProfile()-Result extrahieren (Pitfall 5).
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>
    npm run build fehlerfrei;
    grep -n "memberships" frontend/src/components/layout/AppShellClientWrapper.tsx findet mindestens 2 Treffer (WrapperProfile-Erweiterung und setProfile-Aufruf);
    grep -n "memberships" frontend/src/components/layout/AppShell.tsx findet AppShellProps-Eintrag und Render-Logik
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend Drawer → Admin-Route | Link-Ziel /admin/fansubs/[id]/edit ist durch bestehende Admin-Auth-Middleware geschützt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-05 | Elevation of Privilege | Drawer-Gruppen-Links | accept | /admin/fansubs/[id]/edit ist durch Backend-Permission-Engine geschützt (Phase 44); Link im Drawer ist kein Bypass |
| T-58-06 | Information Disclosure | memberships in AppShellClientWrapper | accept | Daten kommen aus authentifiziertem getOwnProfile()-Call; kein Cross-User-Risiko |
| T-58-SC | Tampering | npm installs | accept | Keine neuen Pakete |
</threat_model>

<verification>
- npm run build schlägt nicht fehl
- AppShell rendert "Meine Gruppen"-Abschnitt wenn memberships nicht leer
- AppShell rendert keinen "Meine Gruppen"-Abschnitt wenn memberships leer/undefined
- AppShellClientWrapper extrahiert memberships aus d.memberships und gibt sie weiter
- Link-Ziel ist /admin/fansubs/${fansub_group_id}/edit
- Kein zweiter API-Call in AppShellClientWrapper
</verification>

<success_criteria>
- Drawer zeigt echte Gruppen-Links statt disabled-Platzhalter
- Link-Ziel ist /admin/fansubs/[id]/edit
- AppShellClientWrapper extrahiert memberships aus bestehendem getOwnProfile()-Call
- npm run build fehlerfrei
</success_criteria>

<output>
Create `.planning/phases/58-profil-hub-content-membership-cards-activity-preparation/58-03-SUMMARY.md` when done
</output>
