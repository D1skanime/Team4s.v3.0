---
phase: 58-profil-hub-content-membership-cards-activity-preparation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/models/member_profile.go
  - backend/internal/repository/member_profile_repository.go
  - frontend/src/types/profile.ts
  - shared/contracts/openapi.yaml
autonomous: true
requirements: [P58-SC1, P58-SC2, P58-SC3, P58-SC4, P58-SC5]

must_haves:
  truths:
    - "GET /api/v1/me/profile gibt recent_media (max. 3 Einträge) zurück"
    - "GET /api/v1/me/profile gibt recent_contributions (max. 3 Einträge) zurück"
    - "recent_media-Einträge enthalten id, category, thumbnail_url, anime_title"
    - "recent_contributions-Einträge enthalten id, anime_title, anime_id, fansub_group_name, role_name, role_label"
    - "loadRecentMedia filtert auf uploaded_by_user_id = appUserID (nicht memberID)"
    - "loadRecentContributions sortiert nach created_at DESC LIMIT 3"
  artifacts:
    - path: backend/internal/models/member_profile.go
      provides: MemberProfileRecentMedia und MemberProfileRecentContribution Structs; MemberProfile-Felder RecentMedia und RecentContributions
    - path: backend/internal/repository/member_profile_repository.go
      provides: loadRecentMedia und loadRecentContributions Methoden; GetOwnProfile ruft beide auf
    - path: frontend/src/types/profile.ts
      provides: MemberProfileRecentMedia und MemberProfileRecentContribution Interfaces; MemberProfileData erweitert
  key_links:
    - from: member_profile_repository.go GetOwnProfile
      to: loadRecentMedia
      via: base.RecentMedia, err = r.loadRecentMedia(ctx, appUserID)
      pattern: "loadRecentMedia.*appUserID"
    - from: member_profile_repository.go GetOwnProfile
      to: loadRecentContributions
      via: base.RecentContributions, err = r.loadRecentContributions(ctx, base.MemberID)
      pattern: "loadRecentContributions.*MemberID"
---

<objective>
Backend-Profil-Aggregat und Typen um zwei neue Arrays erweitern: recent_media (3 neueste release_version_media-Uploads des angemeldeten Members) und recent_contributions (3 neueste release_member_roles-Einträge des Members). Frontend-Types und OpenAPI-Contract parallel aktualisieren.

Purpose: Legt die Datenbasis, auf der Plan 02 die neuen Section-Komponenten aufbauen kann.
Output: Erweitertes MemberProfile-Modell in Go und TypeScript; neue Repository-Queries; OpenAPI-Felder dokumentiert.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.planning/phases/58-profil-hub-content-membership-cards-activity-preparation/58-CONTEXT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/58-profil-hub-content-membership-cards-activity-preparation/58-RESEARCH.md
@C:/Users/admin/Documents/Team4s/.planning/phases/58-profil-hub-content-membership-cards-activity-preparation/58-PATTERNS.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/ROADMAP.md
@C:/Users/admin/Documents/Team4s/backend/internal/models/member_profile.go
@C:/Users/admin/Documents/Team4s/backend/internal/repository/member_profile_repository.go
@C:/Users/admin/Documents/Team4s/frontend/src/types/profile.ts
@C:/Users/admin/Documents/Team4s/shared/contracts/openapi.yaml

<interfaces>
<!-- Aus backend/internal/models/member_profile.go (bestehende Structs als Muster) -->
// MemberProfileCredit (Muster für neue Structs):
type MemberProfileCredit struct {
    FansubGroupID   int64  `json:"fansub_group_id"`
    FansubGroupName string `json:"fansub_group_name"`
    RoleName        string `json:"role_name"`
    RoleLabel       string `json:"role_label"`
    ReleaseCount    int32  `json:"release_count"`
}

// MemberProfile (Erweiterungs-Target):
type MemberProfile struct {
    // ...bestehende Felder...
    Memberships       []MemberProfileMembership `json:"memberships"`
    HistoricalCredits []MemberProfileCredit     `json:"historical_credits"`
    // NEU: RecentMedia und RecentContributions
}

<!-- Aus backend/internal/repository/member_profile_repository.go (Aufruf-Muster in GetOwnProfile) -->
func (r *MemberProfileRepository) GetOwnProfile(ctx context.Context, appUserID int64) (*models.MemberProfile, error) {
    // ...bestehend...
    base.Memberships, err = r.loadMemberships(ctx, base.MemberID, appUserID)
    if err != nil { return nil, err }
    base.HistoricalCredits, err = r.loadHistoricalCredits(ctx, base.MemberID)
    if err != nil { return nil, err }
    // NEU: loadRecentMedia und loadRecentContributions hier anhängen
}

<!-- Aus database/migrations/0044_add_db_schema_v2_target_tables.up.sql — release_member_roles -->
// release_member_roles HAT created_at (TIMESTAMPTZ NOT NULL DEFAULT NOW())
// PRIMARY KEY (release_id, member_id, role_id)

<!-- Aus backend/internal/repository/release_version_media_repository.go (Thumbnail-JOIN-Muster) -->
// Thumbnail: LEFT JOIN media_files mf_thumb ON mf_thumb.media_id = rvm.media_asset_id AND mf_thumb.variant = 'thumb'
// uploaded_by_user_id referenziert users.id — NICHT members.id

<!-- Aus frontend/src/types/profile.ts (bestehende Interface-Muster) -->
export interface MemberProfileCredit {
  fansub_group_id: number
  fansub_group_name: string
  role_name: string
  role_label: string
  release_count: number
}
export interface MemberProfileData {
  // ...
  memberships: MemberProfileMembership[]
  historical_credits: MemberProfileCredit[]
  // NEU: recent_media und recent_contributions
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Go-Modelle und Repository-Queries für recent_media und recent_contributions</name>
  <files>
    backend/internal/models/member_profile.go
    backend/internal/repository/member_profile_repository.go
  </files>
  <read_first>
    - backend/internal/models/member_profile.go — vollständige aktuelle Struct-Definitionen und MemberProfile-Felder lesen
    - backend/internal/repository/member_profile_repository.go — GetOwnProfile, loadHistoricalCredits (Zeilen ~560–600), publicURLForPath lesen
    - backend/internal/repository/release_version_media_repository.go — Thumbnail-JOIN-Muster (LEFT JOIN media_files ... variant = 'thumb'), uploaded_by_user_id-Kontext lesen
  </read_first>
  <behavior>
    - Test 1 (loadRecentMedia): Für User mit 5 Uploads gibt die Methode exakt 3 Einträge zurück, sortiert nach created_at DESC
    - Test 2 (loadRecentMedia): Für User ohne Uploads gibt die Methode einen leeren Slice zurück (kein Fehler)
    - Test 3 (loadRecentMedia): Filtert nur auf uploaded_by_user_id = appUserID — andere User sehen diese Medien nicht
    - Test 4 (loadRecentContributions): Für Member mit 5 Beiträgen gibt die Methode exakt 3 Einträge zurück
    - Test 5 (loadRecentContributions): Für Member ohne Beiträge gibt die Methode einen leeren Slice zurück
  </behavior>
  <action>
    In backend/internal/models/member_profile.go zwei neue Structs direkt nach MemberProfileCredit hinzufügen:

    MemberProfileRecentMedia mit Feldern: ID int64 (json:"id"), Category string (json:"category"), ThumbnailURL string (json:"thumbnail_url,omitempty"), AnimeTitle string (json:"anime_title").

    MemberProfileRecentContribution mit Feldern: ID int64 (json:"id"), AnimeTitle string (json:"anime_title"), AnimeID int64 (json:"anime_id"), FansubGroupName string (json:"fansub_group_name"), RoleName string (json:"role_name"), RoleLabel string (json:"role_label").

    Im MemberProfile-Struct zwei neue Felder nach HistoricalCredits anhängen: RecentMedia []MemberProfileRecentMedia (json:"recent_media") und RecentContributions []MemberProfileRecentContribution (json:"recent_contributions").

    In backend/internal/repository/member_profile_repository.go:

    Neue Methode loadRecentMedia(ctx context.Context, appUserID int64) ([]models.MemberProfileRecentMedia, error) hinzufügen. Query-Struktur:
    - SELECT rvm.id, rvm.category, COALESCE(mf_thumb.path, ''), a.title
    - FROM release_version_media rvm
    - JOIN release_versions rv ON rv.id = rvm.release_version_id
    - JOIN fansub_releases fr ON fr.id = rv.release_id
    - JOIN anime a ON a.id = fr.anime_id
    - LEFT JOIN media_files mf_thumb ON mf_thumb.media_id = rvm.media_asset_id AND mf_thumb.variant = 'thumb'
    - WHERE rvm.uploaded_by_user_id = $1 AND rvm.deleted_at IS NULL
    - ORDER BY rvm.created_at DESC LIMIT 3
    - Im Scan: item.ThumbnailURL = r.publicURLForPath(rawPath) per D-05 (Pitfall 1: appUserID nicht memberID)
    - Fehlerbehandlung wie loadHistoricalCredits: fmt.Errorf("load recent media for user %d: %w", appUserID, err)

    Neue Methode loadRecentContributions(ctx context.Context, memberID int64) ([]models.MemberProfileRecentContribution, error) hinzufügen. Query-Struktur:
    - SELECT rmr.release_id, a.title, a.id, fg.name, cr.name, cr.label
    - FROM release_member_roles rmr
    - JOIN contributor_roles cr ON cr.id = rmr.role_id
    - JOIN fansub_releases fr ON fr.id = rmr.release_id
    - JOIN anime a ON a.id = fr.anime_id
    - JOIN release_versions rv ON rv.release_id = rmr.release_id
    - JOIN release_version_groups rvg ON rvg.release_version_id = rv.id
    - JOIN fansub_groups fg ON fg.id = rvg.fansub_group_id
    - WHERE rmr.member_id = $1
    - ORDER BY rmr.created_at DESC LIMIT 3
    - Hinweis: release_member_roles hat created_at TIMESTAMPTZ (Migration 0044 bestätigt), kein Fallback auf id DESC nötig
    - Für ID im Scan COALESCE-fähige Spalte nutzen: da (release_id, member_id, role_id) composite PK ist, kann rmr.release_id als Proxy für item.ID verwendet werden — oder die Query mit ROW_NUMBER() OVER (ORDER BY rmr.created_at DESC) als id umschreiben; einfacher: item.ID wird als rmr.release_id gesetzt (int64)
    - Fehlerbehandlung: fmt.Errorf("load recent contributions for member %d: %w", memberID, err)

    In GetOwnProfile nach dem bestehenden HistoricalCredits-Aufruf einhängen:
    base.RecentMedia, err = r.loadRecentMedia(ctx, appUserID)
    if err != nil { return nil, err }
    base.RecentContributions, err = r.loadRecentContributions(ctx, base.MemberID)
    if err != nil { return nil, err }
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go build ./internal/... 2>&1</automated>
  </verify>
  <done>go build ./internal/... schlägt nicht fehl; MemberProfileRecentMedia und MemberProfileRecentContribution sind exportiert; GetOwnProfile ruft loadRecentMedia und loadRecentContributions auf; keine pgx-Fehler beim Kompilieren</done>
</task>

<task type="auto">
  <name>Task 2: TypeScript-Types und OpenAPI-Contract erweitern</name>
  <files>
    frontend/src/types/profile.ts
    shared/contracts/openapi.yaml
  </files>
  <read_first>
    - frontend/src/types/profile.ts — bestehende MemberProfileCredit, MemberProfileData Interfaces vollständig lesen
    - shared/contracts/openapi.yaml — MemberProfileData-Schema-Abschnitt suchen und lesen
  </read_first>
  <action>
    In frontend/src/types/profile.ts zwei neue Interfaces direkt nach MemberProfileCredit einfügen:

    MemberProfileRecentMedia: { id: number; category: string; thumbnail_url?: string | null; anime_title: string; }

    MemberProfileRecentContribution: { id: number; anime_title: string; anime_id: number; fansub_group_name: string; role_name: string; role_label: string; }

    Im MemberProfileData-Interface zwei neue Felder nach historical_credits ergänzen:
    recent_media: MemberProfileRecentMedia[];
    recent_contributions: MemberProfileRecentContribution[];

    In shared/contracts/openapi.yaml im MemberProfileData-Schema (bzw. äquivalentem Schema-Eintrag) folgende neuen Properties hinzufügen:
    - recent_media: array of { id: integer, category: string, thumbnail_url: string (nullable), anime_title: string }
    - recent_contributions: array of { id: integer, anime_title: string, anime_id: integer, fansub_group_name: string, role_name: string, role_label: string }
    Beide Arrays sind optional (keine required-Markierung nötig) und können leer sein.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>npm run typecheck schlägt nicht fehl; MemberProfileRecentMedia und MemberProfileRecentContribution sind aus @/types/profile exportiert; MemberProfileData.recent_media und MemberProfileData.recent_contributions sind typisiert</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend → DB | loadRecentMedia filtert auf uploaded_by_user_id = authenticated appUserID |
| Backend → DB | loadRecentContributions filtert auf member_id = authenticated memberID |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Information Disclosure | loadRecentMedia | mitigate | Query explizit auf uploaded_by_user_id = $appUserID einschränken; Backend-Test verifiziert Isolation (Pitfall 1) |
| T-58-02 | Information Disclosure | loadRecentContributions | accept | member_id kommt aus GetOwnProfile (authenticated context); kein Cross-User-Risiko |
| T-58-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Pakete in Phase 58 |
</threat_model>

<verification>
- go build ./internal/... schlägt nicht fehl
- npm run typecheck schlägt nicht fehl
- MemberProfileData in profile.ts hat recent_media und recent_contributions
- GetOwnProfile in member_profile_repository.go ruft loadRecentMedia und loadRecentContributions auf
- loadRecentMedia verwendet WHERE rvm.uploaded_by_user_id = $1 (nicht member_id)
- loadRecentContributions verwendet ORDER BY rmr.created_at DESC LIMIT 3
</verification>

<success_criteria>
- Go-Backend kompiliert ohne Fehler
- TypeScript-Typprüfung schlägt nicht fehl
- Profil-Aggregat enthält recent_media und recent_contributions als typisierte Arrays
- OpenAPI-Contract dokumentiert neue Felder
</success_criteria>

<output>
Create `.planning/phases/58-profil-hub-content-membership-cards-activity-preparation/58-01-SUMMARY.md` when done
</output>
