---
phase: 116-personalisiertes-dashboard
plan: 04
type: execute
wave: 2
depends_on: ["116-01"]
files_modified:
  - frontend/src/app/me/dashboard/components/AttentionSection.tsx
  - frontend/src/app/me/dashboard/components/AttentionSection.module.css
  - frontend/src/app/me/dashboard/components/AttentionSection.test.tsx
  - frontend/src/app/me/dashboard/components/DashboardMetrics.tsx
  - frontend/src/app/me/dashboard/components/DashboardMetrics.test.tsx
autonomous: true
requirements: [D-02, D-03]

must_haves:
  truths:
    - "AttentionSection renders one Card-per-row list of recently-assigned contributions (D-02), each linking to resolveWorkspaceHref(contribution), with a 'Neu' Badge only when isRecentlyAssigned is true, and a compact EmptyState when nothing qualifies"
    - "DashboardMetrics renders exactly 5 HeroMetrics tiles in the fixed order Punkte, Badges, Projekte, Hochgeladene Bilder, Geschriebene Beiträge, sourced only from OwnDashboardData (never getMyBadges() or previous_contributions_count)"
    - "Every rendered primitive comes from @/components/ui (Card, SectionHeader, HeroMetrics, Badge, EmptyState) -- no handbuilt card/table/select markup (CLAUDE.md global UI-system mandate)"
  artifacts:
    - path: "frontend/src/app/me/dashboard/components/AttentionSection.tsx"
      provides: "D-02 'Braucht deine Aufmerksamkeit' section component"
    - path: "frontend/src/app/me/dashboard/components/DashboardMetrics.tsx"
      provides: "D-03 five-tile HeroMetrics section component"
  key_links:
    - from: "frontend/src/app/me/dashboard/components/AttentionSection.tsx"
      to: "frontend/src/app/me/dashboard/components/attentionHelpers.ts (Plan 116-01)"
      via: "isRecentlyAssigned(contribution.created_at, ATTENTION_WINDOW_DAYS), resolveWorkspaceHref(contribution)"
      pattern: "isRecentlyAssigned\\(|resolveWorkspaceHref\\("
    - from: "frontend/src/app/me/dashboard/components/DashboardMetrics.tsx"
      to: "frontend/src/types/dashboard.ts (Plan 116-01)"
      via: "props typed as OwnDashboardData"
      pattern: "OwnDashboardData"
---

<objective>
Build the two simplest Phase 116 section components: "Braucht deine Aufmerksamkeit" (D-02) and
"Deine Zahlen" (D-03). Both are pure presentation over already-defined data contracts and pure
helper functions from Plan 116-01 — no new business logic, only UI composition against the global
design system per the approved UI-SPEC.

Purpose: these two sections are the top of the page (per UI-SPEC's fixed section order) and have
the least design ambiguity — Attention reuses the exact Card-list pattern already proven in
`MembershipsSection.tsx`, and Metrics is a single `HeroMetrics` call with 5 fixed items. Building
them first, isolated from the other three (heavier) sections, keeps this plan inside a safe context
budget while producing two fully-working, independently reviewable dashboard sections.

Output: `AttentionSection.tsx` and `DashboardMetrics.tsx`, each with a green test suite, each
composed exclusively from `@/components/ui` primitives per CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-personalisiertes-dashboard/116-CONTEXT.md
@.planning/phases/116-personalisiertes-dashboard/116-UI-SPEC.md
@.planning/phases/116-personalisiertes-dashboard/116-PATTERNS.md
</context>

<interfaces>
<!-- Contracts this plan consumes, already fixed by Plan 116-01 -- do not redefine. -->

From `frontend/src/app/me/dashboard/components/attentionHelpers.ts` (Plan 116-01):
```typescript
export const ATTENTION_WINDOW_DAYS = 14
export function isRecentlyAssigned(createdAt: string, windowDays: number): boolean
export function resolveWorkspaceHref(contribution: Pick<MeAnimeContribution, 'release_version_id' | 'anime_id' | 'fansub_group_id'>): string
```

From `frontend/src/types/dashboard.ts` (Plan 116-01):
```typescript
export interface OwnDashboardData {
  has_member_profile: boolean
  total_points: number
  badges_count: number
  projects_count: number
  images_count: number
  contributions_count: number
  role_volume: OwnDashboardRoleVolumeEntry[]
  category_progress: OwnDashboardCategoryProgress[]
}
```

`frontend/src/components/profile/MembershipsSection.tsx` (full file, 92 lines) — the Card-list
pattern to replicate structurally for `AttentionSection` (list of `<Card variant="interactive">`
wrapping a `<Link>`, `ArrowRight` icon with `aria-hidden="true"`, empty-state branch):
```tsx
<ul className={styles.list}>
  {items.map((item) => (
    <li key={item.id}>
      <Card variant="interactive" className={styles.itemCard}>
        <Link className={styles.itemLink} href={resolveWorkspaceHref(item)}>
          ...
        </Link>
      </Card>
    </li>
  ))}
</ul>
```

`frontend/src/components/ui/HeroMetrics.tsx` (full file, exact signature to use unmodified):
```typescript
export interface HeroMetricItem { label: string; value: ReactNode }
export interface HeroMetricsProps { items: HeroMetricItem[]; ariaLabel: string; className?: string }
export function HeroMetrics({ items, ariaLabel, className }: HeroMetricsProps)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AttentionSection.tsx — "Braucht deine Aufmerksamkeit" (D-02)</name>
  <files>frontend/src/app/me/dashboard/components/AttentionSection.tsx, frontend/src/app/me/dashboard/components/AttentionSection.module.css, frontend/src/app/me/dashboard/components/AttentionSection.test.tsx</files>
  <read_first>
    frontend/src/components/profile/MembershipsSection.tsx (full file — Card/Link/EmptyState composition pattern, ArrowRight aria-hidden usage)
    frontend/src/components/profile/profile.module.css (membershipCard/membershipLink/membershipName/membershipAction class shapes — port the equivalent layout, do not import this file directly; this plan creates its own colocated AttentionSection.module.css)
    frontend/src/app/me/dashboard/components/attentionHelpers.ts (isRecentlyAssigned, resolveWorkspaceHref, ATTENTION_WINDOW_DAYS from Plan 116-01)
    frontend/src/types/contributions.ts (MeAnimeContribution, post-116-01 with created_at/confirmed_at)
    frontend/src/components/ui/index.ts (confirms Card, SectionHeader, Badge, EmptyState are all exported from '@/components/ui')
  </read_first>
  <behavior>
    - Given `contributions` sorted by the component descending by `created_at`, renders one `<Card variant="interactive">` row per item wrapped in a `<Link href={resolveWorkspaceHref(item)}>`.
    - Each row shows the anime/release title (bold) and, when `isRecentlyAssigned(item.created_at, ATTENTION_WINDOW_DAYS)` is true, a `<Badge variant="info">Neu</Badge>`; when false, no badge is rendered for that row.
    - Given an empty `contributions` array, renders `<EmptyState variant="compact" title="Nichts Neues im Moment" description="Du hast in den letzten 14 Tagen keine neuen Projekt- oder Release-Zuweisungen erhalten." />` and zero `<Card>` elements.
    - The trailing `ArrowRight` icon has `aria-hidden="true"`.
  </behavior>
  <action>
    Create `AttentionSection.tsx` accepting `{ contributions: MeAnimeContribution[] }`, sorting a
    local copy descending by `created_at` (fall back to `confirmed_at` only if `created_at` is
    ever absent — it will not be, per Plan 116-01), and rendering per the behavior bullets using
    `SectionHeader title="Braucht deine Aufmerksamkeit"`, `Card variant="interactive"`, `Badge
    variant="info"`, `EmptyState variant="compact"` (all from `@/components/ui`), `Link` from
    `next/link`, and `ArrowRight` from `lucide-react` with `aria-hidden="true"` (mirroring
    `MembershipsSection.tsx` exactly). Add a colocated `AttentionSection.module.css` with only the
    row-layout classes needed (list reset, flex row for title/badge/arrow, gap using the existing
    `--space-3`/`--space-4` tokens per UI-SPEC — introduce zero new color values). Import
    `isRecentlyAssigned`, `resolveWorkspaceHref`, `ATTENTION_WINDOW_DAYS` from
    `./attentionHelpers` — do not reimplement this logic inline. Create the test file covering all
    four behavior bullets with fixture contributions (mixed old/new `created_at`, one with
    `release_version_id` set and one `null` to also exercise the href branch indirectly).
  </action>
  <acceptance_criteria>
    - AttentionSection renders EmptyState with the exact copy above when contributions is empty
    - Rows sort descending by created_at and show 'Neu' Badge only for items within the 14-day window
    - Every row Link href matches resolveWorkspaceHref(item) exactly
    - `cd frontend && npx vitest run src/app/me/dashboard/components/AttentionSection.test.tsx` is fully green
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/app/me/dashboard/components/AttentionSection.test.tsx</automated>
  </verify>
  <done>AttentionSection.tsx renders a correctly-sorted, correctly-badged, correctly-linked list or the exact D-02 empty state, fully covered by a green test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DashboardMetrics.tsx — 5 Kennzahlen (D-03)</name>
  <files>frontend/src/app/me/dashboard/components/DashboardMetrics.tsx, frontend/src/app/me/dashboard/components/DashboardMetrics.test.tsx</files>
  <read_first>
    frontend/src/components/ui/HeroMetrics.tsx (full file — exact HeroMetricItem/HeroMetricsProps signature)
    frontend/src/components/profile/MemberProfileHero.tsx (lines 176-178 only — existing HeroMetrics usage precedent: `<HeroMetrics items={[{ label: 'Punkte', value: totalPoints }]} ariaLabel="Mitglied-Punktzahl" />`)
    frontend/src/types/dashboard.ts (OwnDashboardData, post-116-01)
  </read_first>
  <behavior>
    - Given `{ total_points: 1234, badges_count: 7, projects_count: 3, images_count: 42, contributions_count: 5 }`, renders exactly 5 HeroMetrics items in this fixed order: Punkte, Badges, Projekte, Hochgeladene Bilder, Geschriebene Beiträge.
    - `total_points` above 999 is formatted with `toLocaleString('de-DE')` (renders `1.234`); values under 1000 render as plain integers.
    - Given all-zero `OwnDashboardData` fields, still renders all 5 tiles with value `0` — no empty state branch exists for this section (RESEARCH: every value always has a sensible zero default).
  </behavior>
  <action>
    Create `DashboardMetrics.tsx` accepting `{ data: OwnDashboardData }`, rendering
    `SectionHeader title="Deine Zahlen"` directly above a single `HeroMetrics` call (no wrapping
    `Card` — HeroMetrics already owns its tile styling per UI-SPEC Section 2) with `ariaLabel`
    describing the metric group (e.g. `"Deine Kennzahlen"`). Build the `items` array in the fixed
    order above, formatting `total_points` via `data.total_points.toLocaleString('de-DE')` and the
    other four as plain numbers. Do not import or reference `getMyBadges()` or
    `previous_contributions_count` anywhere in this file (RESEARCH Pitfalls 1/2 — this component
    only ever reads from the `OwnDashboardData` prop). Create the test file covering all three
    behavior bullets.
  </action>
  <acceptance_criteria>
    - Exactly 5 HeroMetrics items render in the fixed order Punkte/Badges/Projekte/Hochgeladene Bilder/Geschriebene Beiträge
    - total_points formats with toLocaleString('de-DE') when >= 1000
    - All-zero input still renders all 5 tiles with value 0, no empty-state branch
    - `cd frontend && npx vitest run src/app/me/dashboard/components/DashboardMetrics.test.tsx` is fully green
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/app/me/dashboard/components/DashboardMetrics.test.tsx</automated>
  </verify>
  <done>DashboardMetrics.tsx renders exactly the 5 fixed D-03 tiles from OwnDashboardData with correct German number formatting, fully covered by a green test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None (this plan) | Pure presentational components rendering already-fetched, already-typed props; no direct network calls, no user input parsing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-116-04-01 | Tampering | AttentionSection href construction | accept | resolveWorkspaceHref (Plan 116-01) builds hrefs only from server-supplied numeric IDs already present in the fetched contribution object; target routes own their own access checks (documented no-new-risk in 116-RESEARCH.md Security Domain) |
| T-116-04-SC | Tampering (supply chain) | npm installs | accept | No new package installs in this plan |
</threat_model>

<verification>
`cd frontend && npx vitest run src/app/me/dashboard/components/AttentionSection.test.tsx src/app/me/dashboard/components/DashboardMetrics.test.tsx && npx tsc --noEmit`
</verification>

<success_criteria>
Both AttentionSection and DashboardMetrics render correctly against the OwnDashboardData/
MeAnimeContribution contracts using only @/components/ui primitives, matching the UI-SPEC copy and
layout exactly, each with a fully green test suite.
</success_criteria>

<output>
Create `.planning/phases/116-personalisiertes-dashboard/116-04-SUMMARY.md` when done
</output>
