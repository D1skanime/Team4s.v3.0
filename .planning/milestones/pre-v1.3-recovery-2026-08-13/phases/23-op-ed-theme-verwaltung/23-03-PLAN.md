---
phase: 23-op-ed-theme-verwaltung
plan: 03
type: execute
wave: 2
depends_on:
  - 23-01-PLAN.md
  - 23-02-PLAN.md
files_modified:
  - backend/internal/handlers/media_upload.go
  - backend/internal/models/admin_release_theme_assets.go
  - backend/internal/repository/admin_content_anime_themes.go
  - backend/internal/handlers/admin_content_handler.go
  - backend/internal/handlers/admin_content_release_theme_assets.go
  - backend/cmd/server/admin_routes.go
  - backend/cmd/server/main.go
  - frontend/src/types/admin.ts
  - frontend/src/lib/api.ts
  - frontend/src/app/admin/fansubs/[id]/edit/FansubOpEdSection.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/FansubOpEdSection.module.css
  - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
autonomous: true
requirements:
  - P23-SC1

must_haves:
  truths:
    - "Admin sieht auf der Fansub-Edit-Seite einen Abschnitt 'OP/ED Videos' mit Anime-Auswahl"
    - "Nach Anime-Auswahl wird pro Release ein ausklappbarer Bereich mit Theme-Videos und Upload-Dialog gezeigt"
    - "Upload eines Videos + Theme-Auswahl legt automatisch einen fansub_releases-Eintrag an (ON CONFLICT DO NOTHING) und speichert einen release_theme_assets-Eintrag"
    - "GET /api/v1/admin/releases/:releaseId/theme-assets gibt alle Theme-Videos fuer das Release zurueck"
    - "DELETE /api/v1/admin/releases/:releaseId/theme-assets/:themeId entfernt den Eintrag"
    - "go build ./... und npm run build schlagen nicht fehl"
  artifacts:
    - path: "backend/internal/models/admin_release_theme_assets.go"
      provides: "AdminReleaseThemeAsset, AdminReleaseThemeAssetCreateInput structs"
      exports: ["AdminReleaseThemeAsset", "AdminReleaseThemeAssetCreateInput"]
    - path: "backend/internal/handlers/admin_content_release_theme_assets.go"
      provides: "ListReleaseThemeAssets, UploadReleaseThemeAsset, DeleteReleaseThemeAsset handler methods"
    - path: "frontend/src/app/admin/fansubs/[id]/edit/FansubOpEdSection.tsx"
      provides: "Anime-Selector + per-Release Upload-Dialog fuer OP/ED Videos"
  key_links:
    - from: "backend/internal/handlers/admin_content_release_theme_assets.go"
      to: "backend/internal/repository/admin_content_anime_themes.go"
      via: "h.themeRepo.ListReleaseThemeAssets / CreateReleaseThemeAsset / DeleteReleaseThemeAsset"
      pattern: "themeRepo\\..*ReleaseThemeAsset"
    - from: "frontend/src/app/admin/fansubs/[id]/edit/FansubOpEdSection.tsx"
      to: "/api/v1/admin/releases/:releaseId/theme-assets"
      via: "uploadAdminReleaseThemeAsset, getAdminReleaseThemeAssets, deleteAdminReleaseThemeAsset in api.ts"
      pattern: "admin/releases/.*theme-assets"
    - from: "backend/internal/handlers/admin_content_release_theme_assets.go"
      to: "fansub_releases table"
      via: "INSERT ... ON CONFLICT DO NOTHING beim ersten Upload fuer Gruppe+Anime"
      pattern: "ON CONFLICT DO NOTHING"
---

<objective>
Video-Upload und Theme-Zuweisung fuer Fansub-Releases implementieren. Der Admin oeffnet die Fansub-Edit-Seite, waehlt ein Anime aus der Gruppe heraus, sieht alle Releases und kann pro Release ein OP/ED-Video hochladen und einem Theme zuweisen. fansub_releases-Eintraege werden automatisch angelegt.

Purpose: Schliesst die Video-Seite der Zwei-Konzept-Architektur ab — Theme-Definitionen liegen am Anime (Plan 02), Videos liegen an Fansub-Releases.
Output: 3 Backend-Endpoints, Repository-Methoden, mediaService-Verdrahtung, Frontend-Sektion auf der Fansub-Edit-Seite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-op-ed-theme-verwaltung/23-CONTEXT.md
@.planning/phases/23-op-ed-theme-verwaltung/23-01-SUMMARY.md
@.planning/phases/23-op-ed-theme-verwaltung/23-02-SUMMARY.md

Referenz-Implementierungen — vor dem Schreiben lesen:
@backend/internal/handlers/admin_content_handler.go
@backend/internal/handlers/admin_content_anime_themes.go
@backend/internal/handlers/fansub_media_upload.go
@backend/internal/services/media_service.go
@backend/internal/repository/admin_content_anime_themes.go
@backend/internal/repository/media_repository.go
@backend/cmd/server/admin_routes.go
@backend/cmd/server/main.go
@frontend/src/app/admin/fansubs/[id]/edit/page.tsx
@frontend/src/lib/api.ts
@frontend/src/types/admin.ts

<interfaces>
<!-- normalizeUploadAssetType in backend/internal/handlers/media_upload.go —
     uploadAssetTypeAliases map: theme_video muss eingetragen werden. -->
```go
// Bestehende Map in media_upload.go — "theme_video" hinzufuegen:
uploadAssetTypeAliases = map[string]string{
    "poster":           "cover",
    "cover":            "cover",
    "banner":           "banner",
    "logo":             "logo",
    "gallery":          "background",
    "background":       "background",
    "video":            "background_video",
    "background_video": "background_video",
    "theme_video":      "theme_video",  // NEU
}
```

<!-- DB-Schema fuer die relevanten Tabellen: -->
```sql
-- fansub_releases: wird auto-angelegt beim ersten Upload
CREATE TABLE fansub_releases (
    id              BIGSERIAL PRIMARY KEY,
    fansub_group_id BIGINT NOT NULL REFERENCES fansub_groups(id),
    anime_id        BIGINT NOT NULL REFERENCES anime(id),
    UNIQUE (fansub_group_id, anime_id)
);

-- release_theme_assets: Verknuepfung Release <-> Theme <-> Video
CREATE TABLE release_theme_assets (
    release_id BIGINT NOT NULL REFERENCES fansub_releases(id) ON DELETE CASCADE,
    theme_id   BIGINT NOT NULL REFERENCES themes(id) ON DELETE CASCADE,
    media_id   BIGINT NOT NULL REFERENCES media_assets(id) ON DELETE CASCADE,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    PRIMARY KEY (release_id, theme_id, media_id)
);
```

<!-- MediaRepository (nicht MediaUploadRepository) — wird von FansubHandler verwendet.
     AdminContentHandler bekommt dieselbe Instanz via main.go. -->
```go
// backend/internal/repository/media_repository.go — relevante Methoden:
func (r *MediaRepository) CreateMediaAsset(ctx context.Context, input models.MediaAssetCreateInput) (*models.MediaAsset, error)
func (r *MediaRepository) DeleteMediaAsset(ctx context.Context, id int64) error

// models.MediaAssetCreateInput:
type MediaAssetCreateInput struct {
    Filename    string
    StoragePath string
    PublicURL   string
    MimeType    string
    SizeBytes   int64
}
```

<!-- MediaService.SaveUpload — Bild-only. Fuer Videos SaveVideoUpload hinzufuegen.
     Muster aus SaveUpload ableiten; detectMimeType und buildFilename existieren bereits. -->
```go
// backend/internal/services/media_service.go — bestehende Hilfsfunktionen:
func detectMimeType(data []byte) string          // uses mimetype.Detect
func buildFilename(kind models.MediaKind, ext string) string  // "theme_video_<hex8>.mp4"
// MediaSaveResult.CreateInput ist models.MediaAssetCreateInput — direkt nutzbar.
```

<!-- adminThemeRepository interface in admin_content_handler.go — 3 Methoden anhaengen: -->
```go
// In der bestehenden adminThemeRepository interface:
ListReleaseThemeAssets(ctx context.Context, releaseID int64) ([]models.AdminReleaseThemeAsset, error)
CreateReleaseThemeAsset(ctx context.Context, input models.AdminReleaseThemeAssetCreateInput) (*models.AdminReleaseThemeAsset, error)
DeleteReleaseThemeAsset(ctx context.Context, releaseID int64, themeID int64) error
```

<!-- Fansub-Edit-Seite ist 597 Zeilen — UEBERSCHREITET 450 Zeilen bereits.
     Der neue Abschnitt muss als eigene Komponente FansubOpEdSection.tsx extrahiert werden.
     page.tsx importiert nur die Komponente und gibt fansubID + authToken weiter. -->

<!-- anime_fansub_groups Abfrage fuer Releases-Tab:
     SELECT DISTINCT a.id, a.title FROM anime_fansub_groups afg
     JOIN anime a ON a.id = afg.anime_id
     WHERE afg.fansub_group_id = ?
     ORDER BY a.title ASC
     Endpoint: GET /api/v1/admin/fansubs/:fansubId/anime — neu registrieren. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — theme_video registrieren, SaveVideoUpload, Modelle, Repository-Methoden, Handler, Routen</name>
  <files>
    backend/internal/handlers/media_upload.go,
    backend/internal/models/admin_release_theme_assets.go,
    backend/internal/services/media_service.go,
    backend/internal/repository/admin_content_anime_themes.go,
    backend/internal/handlers/admin_content_handler.go,
    backend/internal/handlers/admin_content_release_theme_assets.go,
    backend/cmd/server/admin_routes.go,
    backend/cmd/server/main.go
  </files>
  <action>
**Schritt 1 — `backend/internal/handlers/media_upload.go`** (bestehend modifizieren):

In `uploadAssetTypeAliases` den Eintrag `"theme_video": "theme_video"` hinzufuegen. Keine weiteren Aenderungen an dieser Datei.

**Schritt 2 — `backend/internal/models/admin_release_theme_assets.go`** (neue Datei):

```go
package models

import "time"

// AdminReleaseThemeAsset beschreibt ein Theme-Video das einer Fansub-Release zugeordnet ist.
type AdminReleaseThemeAsset struct {
    ReleaseID     int64     `json:"release_id"`
    ThemeID       int64     `json:"theme_id"`
    ThemeTypeName string    `json:"theme_type_name"`
    ThemeTitle    *string   `json:"theme_title"`
    MediaID       int64     `json:"media_id"`
    PublicURL     string    `json:"public_url"`
    MimeType      string    `json:"mime_type"`
    SizeBytes     int64     `json:"size_bytes"`
    CreatedAt     time.Time `json:"created_at"`
}

// AdminReleaseThemeAssetCreateInput wird beim Anlegen benoetigt.
type AdminReleaseThemeAssetCreateInput struct {
    ReleaseID int64
    ThemeID   int64
    MediaID   int64
}
```

**Schritt 3 — `backend/internal/services/media_service.go`** (bestehend modifizieren):

Existierende Datei lesen. `SaveVideoUpload` nach `SaveUpload` einfuegen. Erlaubte MIME-Typen und Extensions:

| MIME | Extension |
|------|-----------|
| video/mp4 | mp4 |
| video/webm | webm |
| video/x-matroska | mkv |
| video/x-msvideo | avi |
| video/quicktime | mov |

Maximale Dateigroesse: 500 MB. Kein Dimensions-Decode (Width/Height nil). Dateiname: `buildFilename(models.MediaKindThemeVideo, ext)`. Speicherpfad: `filepath.Join(s.storageDir, filename)`. Public-URL: `fmt.Sprintf("%s/api/v1/media/files/%s", s.publicBaseURL, url.PathEscape(filename))`. Rueckgabe: `*MediaSaveResult` (CreateInput befuellt, GIFLargeHint false). Bei leerem Byte-Slice oder MIME-Fehler: `*MediaValidationError` zurueckgeben.

`models.MediaKindThemeVideo` muss vorher in `backend/internal/models/media.go` als Konstante eingetragen werden (`MediaKind = "theme_video"`). Datei lesen und Konstante in den bestehenden `const`-Block einfuegen.

**Schritt 4 — `backend/internal/repository/admin_content_anime_themes.go`** (bestehend modifizieren):

Datei lesen. Am Ende drei neue Methoden auf `*AdminContentRepository` anhaengen.

`GetOrCreateFansubRelease` — Hilfsmethode fuer auto-create:
```go
func (r *AdminContentRepository) GetOrCreateFansubRelease(ctx context.Context, fansubGroupID int64, animeID int64) (int64, error) {
    var id int64
    err := r.db.QueryRow(ctx, `
        INSERT INTO fansub_releases (fansub_group_id, anime_id)
        VALUES ($1, $2)
        ON CONFLICT (fansub_group_id, anime_id) DO UPDATE SET fansub_group_id = EXCLUDED.fansub_group_id
        RETURNING id
    `, fansubGroupID, animeID).Scan(&id)
    return id, err
}
```

`ListReleaseThemeAssets(ctx, releaseID int64) ([]models.AdminReleaseThemeAsset, error)`:
- Existenz des Release pruefen via `SELECT EXISTS(...)`, sonst `ErrNotFound`
- JOIN query: `release_theme_assets rta JOIN themes t JOIN theme_types tt JOIN media_assets ma`
- Spalten: release_id, theme_id, tt.name, t.title, media_id, ma.public_url, ma.mime_type, ma.size_bytes, rta.created_at
- ORDER BY rta.created_at ASC

`CreateReleaseThemeAsset(ctx, input models.AdminReleaseThemeAssetCreateInput) (*models.AdminReleaseThemeAsset, error)`:
- INSERT INTO release_theme_assets (release_id, theme_id, media_id) VALUES ($1, $2, $3)
- Bei pgconn.PgError Code "23505" (PK conflict) oder "23503" (FK violation): `ErrConflict`
- Danach ListReleaseThemeAssets aufrufen und den neuen Eintrag (ThemeID+MediaID Match) zurueckgeben

`DeleteReleaseThemeAsset(ctx, releaseID int64, themeID int64) error`:
- DELETE FROM release_theme_assets WHERE release_id=$1 AND theme_id=$2
- Bei RowsAffected() == 0: `ErrNotFound`

Auch `ListFansubAnime` hinzufuegen fuer den neuen Anime-Liste-Endpoint:
```go
func (r *AdminContentRepository) ListFansubAnime(ctx context.Context, fansubGroupID int64) ([]models.AdminFansubAnimeEntry, error)
// SELECT DISTINCT a.id, a.title FROM anime_fansub_groups afg
// JOIN anime a ON a.id = afg.anime_id
// WHERE afg.fansub_group_id = $1 ORDER BY a.title ASC
```

`AdminFansubAnimeEntry` als minimales Struct in `admin_release_theme_assets.go` erganzen:
```go
type AdminFansubAnimeEntry struct {
    ID    int64  `json:"id"`
    Title string `json:"title"`
}
```

**Schritt 5 — `backend/internal/handlers/admin_content_handler.go`** (bestehend modifizieren):

Datei lesen. Folgende Aenderungen:

a) `adminThemeRepository`-Interface um 5 neue Methoden erweitern:
```go
GetOrCreateFansubRelease(ctx context.Context, fansubGroupID int64, animeID int64) (int64, error)
ListFansubAnime(ctx context.Context, fansubGroupID int64) ([]models.AdminFansubAnimeEntry, error)
ListReleaseThemeAssets(ctx context.Context, releaseID int64) ([]models.AdminReleaseThemeAsset, error)
CreateReleaseThemeAsset(ctx context.Context, input models.AdminReleaseThemeAssetCreateInput) (*models.AdminReleaseThemeAsset, error)
DeleteReleaseThemeAsset(ctx context.Context, releaseID int64, themeID int64) error
```

b) `AdminContentHandler`-Struct um zwei neue Felder erweitern:
```go
mediaRepo    *repository.MediaRepository
mediaService *services.MediaService
```

c) `NewAdminContentHandler` unveraendert lassen. Stattdessen eine `WithMediaDeps`-Methode hinzufuegen (Muster: `FansubHandler.WithMedia`):
```go
func (h *AdminContentHandler) WithMediaDeps(repo *repository.MediaRepository, svc *services.MediaService) *AdminContentHandler {
    h.mediaRepo = repo
    h.mediaService = svc
    return h
}
```

**Schritt 6 — `backend/internal/handlers/admin_content_release_theme_assets.go`** (neue Datei):

4 Handler-Methoden auf `*AdminContentHandler`. Datei muss unter 450 Zeilen bleiben.

`ListFansubAnime` — GET /api/v1/admin/fansubs/:fansubId/anime:
```go
func (h *AdminContentHandler) ListFansubAnime(c *gin.Context) {
    if _, ok := h.requireAdmin(c); !ok { return }
    fansubID, err := strconv.ParseInt(c.Param("fansubId"), 10, 64)
    if err != nil || fansubID <= 0 { badRequest(c, "ungueltige fansub id"); return }
    items, err := h.themeRepo.ListFansubAnime(c.Request.Context(), fansubID)
    if errors.Is(err, repository.ErrNotFound) { c.JSON(404, gin.H{"error": gin.H{"message": "keine anime gefunden"}}); return }
    if err != nil { writeInternalErrorResponse(c, "interner serverfehler", err, "Anime-Liste konnte nicht geladen werden."); return }
    c.JSON(200, gin.H{"data": items})
}
```

`ListReleaseThemeAssets` — GET /api/v1/admin/releases/:releaseId/theme-assets:
- requireAdmin + themeRepo nil guard
- releaseID aus Param parsen
- themeRepo.ListReleaseThemeAssets aufrufen
- ErrNotFound → 404, sonstige Fehler → writeInternalErrorResponse
- Ergebnis: 200 `{"data": items}` (leere Liste = `[]`, nie null)

`UploadReleaseThemeAsset` — POST /api/v1/admin/fansubs/:fansubId/anime/:animeId/theme-assets:
- requireAdmin + themeRepo nil + mediaRepo nil + mediaService nil guards
- fansubID, animeID aus Params parsen
- theme_id aus `c.PostForm("theme_id")` als int64 parsen; > 0 pruefen
- `c.FormFile("file")` holen; Open + `io.ReadAll` zum Einlesen
- `h.mediaService.SaveVideoUpload(fileHeader.Filename, data)` aufrufen — `*services.MediaValidationError` → 400
- `h.mediaRepo.CreateMediaAsset(ctx, saveResult.CreateInput)` aufrufen — ergibt `*models.MediaAsset` mit Integer-ID
- `h.themeRepo.GetOrCreateFansubRelease(ctx, fansubID, animeID)` aufrufen — ergibt releaseID
- `h.themeRepo.CreateReleaseThemeAsset(ctx, models.AdminReleaseThemeAssetCreateInput{ReleaseID: releaseID, ThemeID: themeID, MediaID: asset.ID})` aufrufen
- ErrConflict → 409 `{"error": {"code": "already_assigned", "message": "theme bereits zugewiesen"}}`
- ErrNotFound → 404
- Erfolg → 201 `{"data": asset}`
- Bei Fehlern NACH mediaRepo.CreateMediaAsset: mediaRepo.DeleteMediaAsset aufrufen + Datei entfernen (h.mediaService hat StoragePath in saveResult.CreateInput.StoragePath)

`DeleteReleaseThemeAsset` — DELETE /api/v1/admin/releases/:releaseId/theme-assets/:themeId:
- requireAdmin + themeRepo nil guard
- releaseID + themeID aus Params parsen
- themeRepo.DeleteReleaseThemeAsset aufrufen
- ErrNotFound → 404, Erfolg → 204

WICHTIG: `UploadReleaseThemeAsset` laeuft ueber `/api/v1/admin/fansubs/:fansubId/anime/:animeId/theme-assets` (nicht ueber `:releaseId`), weil der Client fansubID + animeID kennt und die releaseID automatisch ermittelt wird. Das ListReleaseThemeAssets und DeleteReleaseThemeAsset nutzen weiterhin `:releaseId` direkt.

**Schritt 7 — `backend/cmd/server/main.go`** (bestehend modifizieren):

Datei lesen. Die Stelle finden wo `adminContentHandler` erstellt wird. `.WithMediaDeps(mediaRepo, mediaService)` anfuegen. Dabei die gleichen Instanzen nutzen die bereits fuer den FansubHandler erstellt werden — KEINE neuen Instanzen anlegen.

**Schritt 8 — `backend/cmd/server/admin_routes.go`** (bestehend modifizieren):

Nach den Theme-Segment-Routen anhaengen:

```go
// Fansub Releases / OP-ED Videos
v1.GET("/admin/fansubs/:fansubId/anime", auth, deps.adminContentHandler.ListFansubAnime)
v1.POST("/admin/fansubs/:fansubId/anime/:animeId/theme-assets", auth, deps.adminContentHandler.UploadReleaseThemeAsset)
v1.GET("/admin/releases/:releaseId/theme-assets", auth, deps.adminContentHandler.ListReleaseThemeAssets)
v1.DELETE("/admin/releases/:releaseId/theme-assets/:themeId", auth, deps.adminContentHandler.DeleteReleaseThemeAsset)
```

Verifizierung: `cd /c/Users/admin/Documents/Team4s/backend && go build ./...`
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go build ./... 2>&1</automated>
  </verify>
  <done>
    - `go build ./...` exitiert 0 ohne Fehler
    - `admin_content_release_theme_assets.go` existiert mit 4 Handler-Methoden
    - `adminThemeRepository`-Interface enthaelt alle neuen Methoden
    - `AdminContentHandler` hat `mediaRepo` und `mediaService` Felder
    - `WithMediaDeps` wird in `main.go` aufgerufen
    - 4 neue Routen in `admin_routes.go` eingetragen
    - `theme_video` in `uploadAssetTypeAliases` eingetragen
    - Alle neuen Dateien unter 450 Zeilen
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend — Typen, API-Helpers, FansubOpEdSection-Komponente, Fansub-Edit-Seite verdrahten</name>
  <files>
    frontend/src/types/admin.ts,
    frontend/src/lib/api.ts,
    frontend/src/app/admin/fansubs/[id]/edit/FansubOpEdSection.tsx,
    frontend/src/app/admin/fansubs/[id]/edit/FansubOpEdSection.module.css,
    frontend/src/app/admin/fansubs/[id]/edit/page.tsx
  </files>
  <action>
**Schritt 1 — `frontend/src/types/admin.ts`** (bestehend modifizieren):

Datei lesen. Am Ende anhaengen (nach den Phase-23-Plan-02-Typen falls vorhanden):

```typescript
// --- Release Theme Assets (Phase 23 Plan 03) ---

export interface AdminFansubAnimeEntry {
  id: number
  title: string
}

export interface AdminFansubAnimeListResponse {
  data: AdminFansubAnimeEntry[]
}

export interface AdminReleaseThemeAsset {
  release_id: number
  theme_id: number
  theme_type_name: string
  theme_title: string | null
  media_id: number
  public_url: string
  mime_type: string
  size_bytes: number
  created_at: string
}

export interface AdminReleaseThemeAssetsResponse {
  data: AdminReleaseThemeAsset[]
}

export interface AdminReleaseThemeAssetCreateResponse {
  data: AdminReleaseThemeAsset
}
```

**Schritt 2 — `frontend/src/lib/api.ts`** (bestehend modifizieren):

Datei lesen. Bestehende Imports + Typen verstehen. Vier neue Hilfsfunktionen anhaengen:

```typescript
// Gibt alle Anime zurueck fuer die eine Fansub-Gruppe in anime_fansub_groups steht
export async function getAdminFansubAnime(
  fansubID: number,
  authToken?: string,
): Promise<AdminFansubAnimeListResponse>
// GET /api/v1/admin/fansubs/${fansubID}/anime

// Gibt alle Theme-Videos fuer ein Release zurueck
export async function getAdminReleaseThemeAssets(
  releaseID: number,
  authToken?: string,
): Promise<AdminReleaseThemeAssetsResponse>
// GET /api/v1/admin/releases/${releaseID}/theme-assets

// Laedt ein Video hoch und verknuepft es mit einem Theme
export async function uploadAdminReleaseThemeAsset(options: {
  fansubID: number
  animeID: number
  themeID: number
  file: File
  authToken?: string
  onProgress?: (percent: number) => void
}): Promise<AdminReleaseThemeAssetCreateResponse>
// POST /api/v1/admin/fansubs/${fansubID}/anime/${animeID}/theme-assets
// Multipart-Felder: theme_id (String), file (File)
// XHR-Pattern von uploadFansubMedia uebernehmen fuer onProgress-Unterstuetzung

// Entfernt eine Theme-Video-Verknuepfung
export async function deleteAdminReleaseThemeAsset(
  releaseID: number,
  themeID: number,
  authToken?: string,
): Promise<void>
// DELETE /api/v1/admin/releases/${releaseID}/theme-assets/${themeID}
```

Fuer `uploadAdminReleaseThemeAsset` das XHR-Muster aus `uploadFansubMedia` 1:1 uebernehmen (FormData, XHR, onprogress, resolve/reject). FormData-Felder: `theme_id` als `String(options.themeID)` und `file` als `options.file`.

Fuer die anderen drei Funktionen das fetch-Muster aus bestehenden GET/DELETE-Funktionen uebernehmen (inkl. Auth-Header, getApiBaseUrl(), Fehlerbehandlung via ApiError).

**Schritt 3 — `frontend/src/app/admin/fansubs/[id]/edit/FansubOpEdSection.tsx`** (neue Datei):

`'use client'`-Komponente unter 250 Zeilen. Props:

```typescript
interface FansubOpEdSectionProps {
  fansubID: number
  authToken: string | null
}
```

Interner Zustand:
- `animeList: AdminFansubAnimeEntry[]` — geladen beim Mount via `getAdminFansubAnime`
- `selectedAnimeID: number | null`
- `releaseAssets: Record<number, AdminReleaseThemeAsset[]>` — Key = releaseID, geladen on-demand
- `themes: AdminAnimeTheme[]` — geladen wenn animeID ausgewaehlt wird (getAdminAnimeThemes aus Plan 02)
- `uploadState: Record<number, { themeID: number; file: File | null; progress: number; busy: boolean; error: string | null }>` — Key = releaseID (wird benoetigt weil pro Release ein eigener Upload-Dialog)

WICHTIG: In V1 kennt die Fansub-Seite keine Release-IDs direkt — sie kommen aus einem `fansub_releases`-Eintrag der beim Upload auto-erstellt wird. Die Komponente zeigt daher pro Anime aus `animeList` EINEN kombinierten Upload-Bereich (kein separater Release-Selector). Die Upload-Anfrage geht an `uploadAdminReleaseThemeAsset({ fansubID, animeID: selectedAnimeID, themeID, file })`. Nach erfolgreichem Upload: `getAdminReleaseThemeAssets(result.data.release_id)` aufrufen und Assets in `releaseAssets[result.data.release_id]` speichern.

Rendering:
1. Anime-Dropdown (`<select>`) mit `animeList` oder "(Kein Anime verfuegbar)" wenn leer
2. Nach Anime-Wahl: Themes-Dropdown + Datei-Input + Upload-Button
3. Hochgeladene Assets als Liste mit Dateiname/MIME und Delete-Button
4. Fortschrittsanzeige als `<progress value={progress} max={100} />`
5. Fehlermeldung inline unter dem Upload-Formular

Beim Delete: `deleteAdminReleaseThemeAsset(asset.release_id, asset.theme_id)` aufrufen und Asset aus dem lokalen State entfernen.

Datei-Input: `accept="video/*"`.

**Schritt 4 — `frontend/src/app/admin/fansubs/[id]/edit/FansubOpEdSection.module.css`** (neue Datei):

Minimale Styles. Klassen: `.section`, `.animeSelect`, `.uploadForm`, `.formRow`, `.progressWrap`, `.assetList`, `.assetRow`, `.assetMeta`, `.errorBox`. Variablen und Basisstile an bestehende `FansubEdit.module.css` anlehnen (Datei lesen).

**Schritt 5 — `frontend/src/app/admin/fansubs/[id]/edit/page.tsx`** (bestehend modifizieren):

Datei lesen. Die Seite ist 597 Zeilen und ueberschreitet bereits 450 Zeilen. Keine weiteren substantiellen Bloecke hinzufuegen — stattdessen nur:

1. `FansubOpEdSection` importieren: `import { FansubOpEdSection } from './FansubOpEdSection'`
2. Nach dem `</form>`-Tag und vor `</section>` einen neuen Block einfuegen:

```tsx
<div className={styles.fansubEditOpEdWrapper}>
  <FansubOpEdSection fansubID={fansubID} authToken={authToken} />
</div>
```

3. In `FansubEdit.module.css` die Klasse `.fansubEditOpEdWrapper` hinzufuegen (padding-top: 1.5rem; border-top: 1px solid var(--border); falls Variable vorhanden, sonst #e5e7eb).

Die Komponente ist bewusst ausserhalb des `<form>`-Elements platziert um Konflikte mit dem Fansub-Speichern-Button zu vermeiden.

Importpfade pruefen: `AdminFansubAnimeEntry`, `AdminReleaseThemeAsset` kommen aus `@/types/admin`. API-Funktionen aus `@/lib/api`.

Verifizierung: `cd /c/Users/admin/Documents/Team4s/frontend && npm run build 2>&1 | tail -30`
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `npm run build` exitiert 0 ohne TypeScript-Fehler
    - `AdminReleaseThemeAsset`, `AdminFansubAnimeEntry` in admin.ts exportiert
    - `getAdminFansubAnime`, `getAdminReleaseThemeAssets`, `uploadAdminReleaseThemeAsset`, `deleteAdminReleaseThemeAsset` in api.ts exportiert
    - `FansubOpEdSection.tsx` existiert unter 250 Zeilen
    - `FansubOpEdSection.module.css` existiert
    - Fansub-Edit-Seite zeigt die OP/ED-Sektion (durch Import + Einbindung)
    - Keine Produktionsdatei ueberschreitet 450 Zeilen
  </done>
</task>

</tasks>

<verification>
1. `cd /c/Users/admin/Documents/Team4s/backend && go build ./...` — exitiert 0
2. `cd /c/Users/admin/Documents/Team4s/frontend && npm run build` — exitiert 0
3. Smoke: `curl -s -H "Authorization: Bearer {token}" http://localhost:8092/api/v1/admin/fansubs/1/anime` — gibt `{"data":[...]}` oder leere Liste zurueck
4. Smoke: `curl -s -H "Authorization: Bearer {token}" http://localhost:8092/api/v1/admin/releases/1/theme-assets` — gibt `{"data":[]}` oder 404 zurueck
5. Browser: `/admin/fansubs/{id}/edit` oeffnen — "OP/ED Videos"-Abschnitt sichtbar am Ende der Seite
6. Anime aus Dropdown waehlen — Theme-Dropdown und Datei-Upload erscheinen
7. Video hochladen + Theme waehlen — Asset erscheint in der Liste
8. Asset loeschen — aus der Liste entfernt
</verification>

<success_criteria>
- Backend kompiliert sauber; 4 neue Endpunkte erreichbar
- fansub_releases wird beim ersten Upload per ON CONFLICT DO NOTHING auto-angelegt
- release_theme_assets-Eintrag korrekt verknuepft (release_id → theme_id → media_id)
- Frontend baut sauber; Fansub-Edit-Seite zeigt OP/ED-Upload-Workflow
- Upload end-to-end funktioniert in laufender Docker-Umgebung
- Keine Produktionsdatei ueberschreitet 450 Zeilen
</success_criteria>

<output>
Nach Abschluss `.planning/phases/23-op-ed-theme-verwaltung/23-03-SUMMARY.md` erstellen.
</output>
