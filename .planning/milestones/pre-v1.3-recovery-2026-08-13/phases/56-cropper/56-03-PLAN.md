---
phase: 56-cropper
plan: "03"
type: execute
wave: 2
depends_on:
  - 56-01
files_modified:
  - frontend/src/components/admin/MediaUpload.tsx
  - frontend/src/components/admin/MediaUpload.test.tsx
  - frontend/src/components/admin/MediaUpload.module.css
autonomous: true
requirements:
  - MEDIA-CROPPER-01
must_haves:
  truths:
    - "D-03: Fansub group media remains owned by MediaUpload, uploadFansubMedia, deleteFansubMedia, fansub_group_media, and fansub_groups.logo_id/banner_id."
    - "D-04: The phase must not combine profile media and fansub group media into one domain flow."
    - "D-14: Fansub group logo crop sends only the cropped logo file through the existing fansub media upload helper."
    - "D-15: Banner upload stays unchanged unless there is already a real crop contract for it."
    - "D-16: SVG logo handling must be explicit."
    - "Do not change release, release-version, anime, or episode media flows."
---

<objective>
Migrate the fansub group logo cropper inside `MediaUpload` to the shared Team4s cropper while preserving group-media upload, progress, validation, delete, and banner behavior.
</objective>

<context>
@AGENTS.md
@.planning/phases/56-cropper/56-CONTEXT.md
@docs/architecture/db-schema-fansub-domain.md
@docs/engineering/implementation-contract.md
@docs/frontend/auth-api-client.md
@frontend/src/components/media/crop/Team4sCropper.tsx
@frontend/src/components/admin/MediaUpload.tsx
@frontend/src/components/admin/MediaUpload.test.tsx
@frontend/src/lib/api.ts
@frontend/src/types/fansub.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline logo crop UI with the shared cropper</name>
  <files>frontend/src/components/admin/MediaUpload.tsx, frontend/src/components/admin/MediaUpload.module.css</files>
  <read_first>
    - frontend/src/components/admin/MediaUpload.tsx
    - frontend/src/components/media/crop/Team4sCropper.tsx
    - frontend/src/lib/api.ts
  </read_first>
  <action>
    Remove the inline logo cropper state and canvas export code from `MediaUpload` where it duplicates `Team4sCropper`. Keep existing dropzone, validation, upload progress, busy state, delete action, warning/error UI, and `uploadFansubMedia` calls.

    For raster logos, open `Team4sCropper` with 1:1/circular preview and 512x512 PNG output, then pass the cropped file to the existing `submitUpload` function.

    Preserve banner upload exactly unless implementation finds an already documented banner-crop contract.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck</automated>
  </verify>
  <done>
    `MediaUpload` uses the shared cropper for logo crop and keeps its original upload/delete ownership.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolve SVG logo behavior explicitly</name>
  <files>frontend/src/components/admin/MediaUpload.tsx, frontend/src/components/admin/MediaUpload.test.tsx</files>
  <read_first>
    - frontend/src/components/admin/MediaUpload.tsx
    - frontend/src/types/fansub.ts
  </read_first>
  <action>
    Decide and implement explicit SVG behavior. Preferred narrow path: keep SVG on the existing direct upload path without opening the raster cropper, because canvas-rasterizing SVG can have security and fidelity implications. If SVG direct upload stays allowed, tests must assert SVG does not open the cropper and still calls `uploadFansubMedia`.

    If implementation chooses to reject SVG for logo replacement, update visible validation text and tests, but do not make that policy change silently.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm test -- --run src/components/admin/MediaUpload.test.tsx</automated>
  </verify>
  <done>
    SVG logo behavior is deliberate, tested, and documented by code/tests.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update fansub media crop tests</name>
  <files>frontend/src/components/admin/MediaUpload.test.tsx</files>
  <read_first>
    - frontend/src/components/admin/MediaUpload.test.tsx
    - frontend/src/components/media/crop/Team4sCropper.test.tsx
  </read_first>
  <action>
    Update tests to cover raster logo crop apply, cancel, upload success, upload failure, progress reset, current-logo edit path, SVG behavior, and banner no-crop behavior. Mock `Team4sCropper` only at the integration boundary if needed.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm test -- --run src/components/admin/MediaUpload.test.tsx</automated>
  </verify>
  <done>
    Fansub media tests prove logo crop uses the shared adapter and still uploads through the existing fansub helper.
  </done>
</task>

</tasks>

<acceptance_criteria>

- `MediaUpload` no longer contains its own canvas crop/export implementation for logo crop.
- Raster logo crop uses `Team4sCropper`.
- SVG logo behavior is explicit and tested.
- Banner upload behavior is unchanged.
- `uploadFansubMedia` remains the only fansub group media upload transport.
- No release/anime/episode media flow is touched.

</acceptance_criteria>

<recommended_checks>

- `cd C:/Users/admin/Documents/Team4s/frontend && npm test -- --run src/components/admin/MediaUpload.test.tsx`
- `cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck`
- `cd C:/Users/admin/Documents/Team4s && git diff --check`

</recommended_checks>
