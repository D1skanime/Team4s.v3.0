---
phase: 56-cropper
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/components/media/crop/Team4sCropper.tsx
  - frontend/src/components/media/crop/Team4sCropper.module.css
  - frontend/src/components/media/crop/Team4sCropper.test.tsx
autonomous: true
requirements:
  - MEDIA-CROPPER-01
must_haves:
  truths:
    - "D-01: The shared cropper is a UI/client export primitive, not a media ownership layer."
    - "D-05: The new shared cropper lives under a neutral media/component location."
    - "D-06: The in-house cropper should not receive another geometry patch as the primary fix; replace it because it does not perform the required functions well enough."
    - "D-07: No cropper dependency is locked yet; react-advanced-cropper is only a prior research starting point."
    - "D-08: Compare maintained cropper libraries against Team4s' real required functions before changing dependencies."
    - "D-09: If a candidate cannot satisfy parity, keyboard/touch, and responsive requirements, reject it and document the different library chosen."
    - "D-17: Cropper controls must work with mouse, touch/pointer, keyboard, zoom control, cancel/ESC, and apply."
    - "D-18: Dialog focus must be trapped or handled through existing modal primitives, with visible focus states."
    - "D-19: German user-facing strings introduced by this phase must use proper umlauts."
    - "D-20: The implementation must avoid broad visual redesign; only cropper surfaces and their immediate controls are in scope."
    - "No upload endpoint, API helper, backend handler, or media table changes belong in this plan."
---

<objective>
Select and introduce the shared cropper dependency, then build the neutral Team4s cropper adapter that later profile and fansub flows can consume without inheriting third-party API details.
</objective>

<context>
@AGENTS.md
@.planning/phases/56-cropper/56-CONTEXT.md
@.planning/phases/56-cropper/56-RESEARCH.md
@docs/engineering/implementation-contract.md
@docs/frontend/ui-system.md
@docs/agent-guidelines-ui.md
@frontend/package.json
@frontend/src/components/media/crop/AvatarCropDialog.tsx
@frontend/src/components/media/crop/AvatarCropDialog.test.tsx
@frontend/src/components/media/crop/mediaCropMath.ts
@frontend/src/components/media/crop/mediaCropA11y.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm and record the cropper dependency decision</name>
  <files>frontend/package.json, frontend/package-lock.json, .planning/phases/56-cropper/56-RESEARCH.md</files>
  <read_first>
    - .planning/phases/56-cropper/56-RESEARCH.md
    - frontend/package.json
  </read_first>
  <action>
    Re-check the current package state and choose a different maintained cropper library than the current in-house foundation. Do not assume `react-advanced-cropper`; treat it as one candidate from prior research. Compare candidates against the documented required functions: fixed aspect/circle use, preview/export/display parity, mobile/touch, keyboard/a11y options, zoom/reset/apply/cancel, canvas/Blob/File export, package health, and adapter complexity. Update `56-RESEARCH.md` with the selected library and why before installing it.

    Add only the chosen cropper dependency. Do not add a second image editing library. Keep dependency changes limited to frontend package files.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm install</automated>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm ls --depth=0</automated>
  </verify>
  <done>
    The chosen library is installed and the decision is traceable in Phase 56 research.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the neutral Team4s cropper adapter</name>
  <files>frontend/src/components/media/crop/Team4sCropper.tsx, frontend/src/components/media/crop/Team4sCropper.module.css</files>
  <read_first>
    - frontend/src/components/media/crop/AvatarCropDialog.tsx
    - frontend/src/components/media/crop/AvatarCropDialog.module.css
    - frontend/src/components/ui/Button.tsx
  </read_first>
  <action>
    Create a domain-neutral cropper component under `frontend/src/components/media/crop`. The component should accept a `File`, title/labels, aspect ratio, output dimensions, output filename, output MIME type, optional circular preview mode, and `onCancel`/`onApply(croppedFile)` callbacks.

    Keep all third-party cropper imports inside this adapter. Domain components must not call library methods directly. Export a compact TypeScript prop type for later reuse.

    Use existing UI primitives for actions where practical. German visible strings must use correct umlauts. The component must provide cancel, reset, zoom, and apply controls with stable dimensions and no layout shift.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck</automated>
  </verify>
  <done>
    A reusable Team4s cropper component exists and hides third-party API details from domain callers.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add focused adapter tests</name>
  <files>frontend/src/components/media/crop/Team4sCropper.test.tsx</files>
  <read_first>
    - frontend/src/components/media/crop/AvatarCropDialog.test.tsx
    - frontend/src/app/me/profile/page.test.tsx
  </read_first>
  <action>
    Add tests for opening/rendering, cancel, reset, apply producing a `File`, disabled/applying state, and accessible labels. Mock the third-party cropper narrowly if jsdom cannot exercise the real canvas path, but keep at least one test covering Team4s' Blob/File conversion seam.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm test -- --run src/components/media/crop/Team4sCropper.test.tsx</automated>
  </verify>
  <done>
    The shared adapter has regression coverage before domain flows migrate.
  </done>
</task>

</tasks>

<acceptance_criteria>

- The dependency decision is explicit and traceable.
- A single shared Team4s cropper adapter exists under `frontend/src/components/media/crop`.
- Third-party cropper API details are not leaked into profile or admin flows.
- The adapter can export a cropped `File` suitable for existing upload helpers.
- No backend, API contract, media table, or upload endpoint changes were introduced.
- Adapter tests pass.

</acceptance_criteria>

<recommended_checks>

- `cd C:/Users/admin/Documents/Team4s/frontend && npm test -- --run src/components/media/crop/Team4sCropper.test.tsx`
- `cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck`
- `cd C:/Users/admin/Documents/Team4s && git diff --check`

</recommended_checks>
