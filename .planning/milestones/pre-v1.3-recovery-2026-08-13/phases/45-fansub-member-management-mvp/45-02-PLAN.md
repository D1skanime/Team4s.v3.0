---
phase: 45-fansub-member-management-mvp
plan: "02"
type: execute
wave: 2
depends_on:
  - 45-01-PLAN.md
files_modified:
  - backend/internal/handlers
  - backend/internal/models
  - backend/internal/permissions
  - backend/internal/repository
  - database/migrations
  - docs
autonomous: true
requirements:
  - FANSUB-MEMBER-MGMT-01
must_haves:
  truths:
    - "All member-management endpoints must enforce the Phase-44 permission engine."
    - "Self-lockout prevention is a backend invariant and returns 409 Conflict."
    - "Audit should reuse the existing generic seam where available and stay minimal otherwise."
---

<objective>
Implement the backend MVP for fansub-group member management: protected reads, app-user search, add-member flow, role mutation, activation toggles, self-lockout prevention, capability extension, and audit logging.

Purpose: This is the slice where the permission-aware member-management surface becomes real and enforceable.
</objective>

<context>
@.planning/phases/45-fansub-member-management-mvp/45-01-PLAN.md
@.planning/phases/45-fansub-member-management-mvp/45-CONTEXT.md
@.planning/phases/45-fansub-member-management-mvp/45-RESEARCH.md
@backend/internal/handlers/fansub_group_members.go
@backend/internal/permissions
@frontend/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace legacy member endpoints with permission-engine-backed member-management routes</name>
  <read_first>
    - backend/internal/handlers/fansub_group_members.go
    - backend/internal/handlers/fansub_admin.go
    - backend/internal/permissions
  </read_first>
  <action>
    Rework the fansub member routes so they operate on the canonical app-user membership model and enforce:
    - `fansub_group.members.view` for list/read
    - `fansub_group.members.manage` for add/update/deactivate/reactivate/remove-role flows

    Each route should:
    - pass the narrowest truthful group/member identifiers into the permission engine
    - reuse the repository/service helpers from Plan 45-01
    - reject unknown roles
    - reject duplicate active membership
    - map self-lockout prevention failures to `409 Conflict`
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/handlers/... -run "Test.*FansubMember|Test.*Members|Test.*Conflict"</automated>
  </verify>
  <done>
    - Member list and mutations are backend-protected by central permissions.
    - The routes no longer depend on raw `requireAdmin(...)` semantics.
    - Business conflicts are returned with the correct status.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add app-user search and extend fansub-group capabilities</name>
  <read_first>
    - backend/internal/handlers
    - backend/internal/permissions
    - frontend/src/lib/api.ts
  </read_first>
  <action>
    Add the backend surfaces needed by the UI:
    - app-user search by display name/email for the add-member flow
    - fansub-group capabilities extended with `canViewMembers` and `canManageMembers`

    Rules:
    - capability fields must map to central permission actions
    - capability handlers must not implement their own role logic
    - if the actor cannot read the group context, the capability endpoint returns 403
    - user search should return only the minimal identity data required for selection
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/handlers/... -run "Test.*Capabilities|Test.*UserSearch"</automated>
  </verify>
  <done>
    - The frontend has a minimal app-user search surface.
    - Fansub-group capabilities expose the two member-management booleans via the central engine.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire audit for member and role mutations</name>
  <read_first>
    - backend/internal/repository
    - existing audit seam from earlier phases
  </read_first>
  <action>
    Reuse the available generic audit seam and record:
    - member added
    - role added
    - role removed
    - member deactivated
    - member reactivated
    - blocked self-lockout attempt

    If no suitable generic seam exists, add only the smallest compatible extension after confirming project convention from Plan 45-01.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/repository/... ./internal/handlers/...</automated>
  </verify>
  <done>
    - Membership mutations and blocked self-lockout attempts are auditable.
    - No second parallel audit model was introduced.
  </done>
</task>

</tasks>

<verification>
Automated:
1. `cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/handlers/...`
2. `cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/repository/...`
3. `cd C:/Users/admin/Documents/Team4s/backend && go build ./...`

Behavioral:
1. Unauthorized user gets 403 on member list.
2. Allowed group manager can add an existing app user with valid roles.
3. Unknown roles and duplicate active memberships are rejected.
4. Self-lockout attempts return 409 and are audited.
</verification>

<success_criteria>
- Backend member-management endpoints exist and are permission-engine-backed.
- Capabilities expose `canViewMembers` and `canManageMembers`.
- App-user search is available for the add-member flow.
- Audit covers the required mutation families and blocked self-lockout attempts.
</success_criteria>
