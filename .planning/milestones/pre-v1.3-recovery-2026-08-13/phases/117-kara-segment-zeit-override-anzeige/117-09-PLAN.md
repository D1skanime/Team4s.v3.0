---
phase: 117-kara-segment-zeit-override-anzeige
plan: 09
type: execute
wave: 7
depends_on: ["117-06", "117-07", "117-08"]
files_modified: []
autonomous: false
requirements: [D-01, D-02, D-03]

must_haves:
  truths:
    - "Ein Zeit-Override fuer eine einzelne Folge veraendert nachweislich NUR den abgeleiteten Render-Cache-Clip, nicht die Episoden-Quelldatei/den release_variant-Datensatz (D-01, VALIDATION.md Manual-Only-Verification)"
    - "Die volle Backend- und Frontend-Testsuite ist gruen nach allen acht Plaenen (117-01 bis 117-08) zusammen"
    - "Alle drei UI-SPEC-Surfaces (Override-Eingabe, Entdopplungs-Indikator, oeffentliche Timeline-Entdopplung) sind live am :3000-Dev-Server visuell bestaetigt"
  artifacts: []
  key_links: []
---

<objective>
Schließt Phase 117 mit einer vollständigen Regressionsprüfung und der laut VALIDATION.md nicht
automatisierbaren Live-Verifikation ab: Kein-Re-Encode-Beweis (D-01) und reale
Mehr-Folgen-Entdopplung (D-02) an echten Daten, plus visuelle Abnahme aller drei UI-SPEC-Surfaces.

Purpose: VALIDATION.md führt zwei Verifikationen explizit als "Manual-Only" (kein Unit-Test
praktikabel): den Negativbeweis "kein Re-Encode" und die reale Nachbar-Folgen-Konstellation für die
öffentliche Entdopplung. Dieser Plan bündelt beide gemäß dem projektweiten Muster
("Live-UAT über Port 3000", siehe Projekt-Memory) und läuft nach allen Implementierungswellen.
Output: Grüne Gesamt-Testsuite, dokumentiertes Live-UAT-Ergebnis, Phase-117-Abschluss.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-kara-segment-zeit-override-anzeige/117-VALIDATION.md
@.planning/phases/117-kara-segment-zeit-override-anzeige/117-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Volle Backend- und Frontend-Regressionssuite</name>
  <files></files>
  <read_first>
    .planning/phases/117-kara-segment-zeit-override-anzeige/117-VALIDATION.md (Abschnitt "Sampling
    Rate" — "Before /gsd:verify-work: Beide Suiten müssen grün sein").
  </read_first>
  <action>
    Führe `cd backend && go build ./... && go vet ./... && go test ./internal/...` aus und behebe
    jeden Fehlschlag, der durch die Änderungen aus 117-01 bis 117-08 verursacht wurde (nicht
    vorbestehende, phasenfremde Fehlschläge — diese im Executor-Summary dokumentieren, nicht
    reparieren). Führe `cd frontend && npm run typecheck && npm test` aus und behebe entsprechend.
    Stelle sicher, dass der Backend-Container für den Live-UAT-Schritt (Task 2) aktuell ist: `docker
    compose up -d --build team4sv30-backend` (Docker-Rebuild ist laut Projekt-Memory zwingend, bevor
    neue Go-Routen live erreichbar sind).
  </action>
  <acceptance_criteria>
    - `go test ./internal/...` (in `backend/`) ist vollständig grün.
    - `npm test` (in `frontend/`) ist vollständig grün.
    - `docker ps` zeigt den Backend-Container mit einem Image-Build-Zeitstempel nach dem letzten
      Commit dieser Phase.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./... && go test ./internal/... && cd ../frontend && npm run typecheck && npm test</automated>
  </verify>
  <done>Beide Testsuiten grün; Backend-Container neu gebaut und aktuell.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-UAT — Kein-Re-Encode-Beweis, Entdopplung, alle drei UI-SPEC-Surfaces</name>
  <read_first>
    .planning/phases/117-kara-segment-zeit-override-anzeige/117-VALIDATION.md Abschnitt "Manual-Only Verifications" (Kein-Re-Encode-Beweis, oeffentliche Entdopplung an echten Daten);
    .planning/phases/117-kara-segment-zeit-override-anzeige/117-UI-SPEC.md (alle drei Surface-Vertraege fuer die visuelle Abnahme).
  </read_first>
  <what-built>
    Vollständiges Zielmodell D-03 (geteiltes, pro Release-Version zuweisbares Kara-Segment),
    Per-Folge-Zeit-Override ohne Re-Encode (D-01), entdoppelte Anzeige in Admin und öffentlich (D-02).
  </what-built>
  <how-to-verify>
    1. Docker-Backend neu gebaut (`docker restart team4sv30-frontend` nach jeder Frontend-Änderung,
       kein `--build` nötig — Live-Testing-Konvention laut Projekt-Memory), Dev-Server auf :3000.
    2. Admin-Editor öffnen für ein Anime mit einem OP/ED, das über mehrere Folgen läuft (oder ein
       Testszenario: ein Kara anlegen, über den neuen Zuweisungs-Endpunkt einer zweiten Folge
       zuweisen). Prüfen: Segment-Tabelle zeigt "Geteiltes Segment"-Badge, Zuweisungsliste öffnet
       über den DisclosureIndicator, zeigt beide Folgen.
    3. Für eine der zugewiesenen Folgen im Segment-Editor-Panel den Override-Switch aktivieren, eine
       abweichende Start-/Endzeit setzen, speichern. Prüfen: "Zeit hier überschrieben"-Badge
       erscheint in der Tabelle; die ANDERE zugewiesene Folge zeigt weiterhin die Basis-Zeit.
    4. Kein-Re-Encode-Beweis (D-01): vor dem Speichern des Overrides `docker exec team4sv30-backend
       stat /path/zur/episoden-quelldatei` (Pfad aus `release_variants`/`media_storage_dir`
       ermitteln) notieren (mtime/Größe); nach dem Speichern erneut prüfen — mtime/Größe UNVERÄNDERT.
       Nur ein neuer `theme_segment_render_cache`-Eintrag (Status `queued`→`ready`) darf entstehen.
    5. Öffentliche Release-Detailseite für die Span-Start-Folge öffnen: Kara erscheint mit "Gilt auch
       für Folge {von}–{bis}"-Badge. Die Folgeepisode (ohne Override oder mit Override, aber
       DEMSELBEN Kara) öffnen: Kara erscheint dort NICHT erneut in der Timeline.
    6. Mobile-Viewport-Check für alle drei Surfaces (Override-Panel, Segment-Tabelle,
       öffentliche Timeline) — keine abgeschnittenen Badges/Buttons.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <acceptance_criteria>
    - Kein-Re-Encode-Beweis dokumentiert: mtime/Groesse der Episoden-Quelldatei sind vor und nach dem Override-Speichern identisch.
    - Ein geteiltes Kara erscheint auf der oeffentlichen Release-Detailseite nachweislich nur auf der Span-Start-Folge, nicht auf der Folgeepisode mit demselben Kara.
    - Alle drei UI-SPEC-Surfaces (Override-Eingabe, Entdopplungs-Indikator, oeffentliche Timeline-Entdopplung) sind auf Desktop- und Mobile-Viewport ohne abgeschnittene Elemente bestaetigt.
  </acceptance_criteria>
  <files></files>
  <action>Fuehre die sechs Schritte aus how-to-verify manuell am laufenden Dev-Server (:3000) durch und dokumentiere das Ergebnis.</action>
  <verify>
    <human-check>Alle sechs Schritte aus how-to-verify sind bestaetigt; kein Re-Encode nachweisbar, Entdopplung sichtbar, alle drei UI-SPEC-Surfaces mobil und desktop korrekt.</human-check>
  </verify>
  <done>Live-UAT fuer D-01/D-02/D-03 bestaetigt oder Abweichungen dokumentiert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Live-UAT -> Produktions-nahe Dev-Umgebung | Nur lokale Docker-Umgebung, kein neuer Boundary gegenüber den Vorgänger-Plänen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-117-09-01 | Repudiation (fehlende Nachvollziehbarkeit) | Kein-Re-Encode-Beweis nur manuell dokumentiert | accept | VALIDATION.md klassifiziert diesen Fall explizit als Manual-Only (Negativbeweis an der ffmpeg-/Dateisystem-Grenze); Ergebnis wird im Executor-Summary/SUMMARY.md dauerhaft festgehalten |
| T-117-09-SC | Tampering | keine neuen Paket-Installationen | accept | Reiner Verifikations-/Regressions-Plan ohne Code-Änderung |
</threat_model>

<verification>
Volle Backend- und Frontend-Testsuite grün; menschliche Live-UAT-Bestätigung für D-01/D-02/D-03 gemäß
VALIDATION.md Manual-Only-Verifications-Tabelle.
</verification>

<success_criteria>
Phase 117 ist vollständig implementiert, regressionsgetestet und live bestätigt: geteiltes,
zuweisbares Kara (D-03), Per-Folge-Zeit-Override ohne Re-Encode (D-01), entdoppelte Anzeige in Admin
und öffentlich (D-02).
</success_criteria>

<output>
Create `.planning/phases/117-kara-segment-zeit-override-anzeige/117-09-SUMMARY.md` when done
</output>
