---
phase: 117-kara-segment-zeit-override-anzeige
plan: 05
type: execute
wave: 5
depends_on: ["117-04"]
files_modified:
  - backend/internal/handlers/admin_content_anime_theme_segment_assignments.go
  - backend/cmd/server/admin_routes.go
  - shared/contracts/admin-content.yaml
  - backend/internal/handlers/admin_content_anime_theme_segment_assignments_test.go
autonomous: true
requirements: [D-01, D-03]

must_haves:
  truths:
    - "Ein Admin mit release_version.segments.manage kann ein geteiltes Kara explizit einer weiteren Release-Version zuweisen und wieder entziehen (D-03)"
    - "Ein Admin kann fuer eine einzelne zugewiesene Folge eine abweichende Zeit setzen und wieder entfernen, ohne ein neues Segment anzulegen (D-01)"
    - "Jeder neue Schreibpfad ist durch dieselbe Capability release_version.segments.manage gegated wie die bestehenden Segment-Endpunkte (V4 Access Control)"
    - "Die vier neuen Admin-CRUD-Endpunkte sind im OpenAPI-Contract dort dokumentiert, wo bereits verwandte Admin-Anime-Endpunkte liegen (shared/contracts/admin-content.yaml, konsistent mit PATTERNS.md, Nyquist-Fix W3)"
  artifacts:
    - path: "backend/internal/handlers/admin_content_anime_theme_segment_assignments.go"
      provides: "AssignAnimeSegment / UnassignAnimeSegment / UpsertAnimeSegmentEpisodeOverride / DeleteAnimeSegmentEpisodeOverride Handler"
  key_links:
    - from: "POST .../segments/:segmentId/assignments"
      to: "requireSegmentManage(c, releaseVersionID)"
      via: "identisches Capability-Gate wie ListAnimeSegments/CreateAnimeSegment"
      pattern: "requireSegmentManage"
    - from: "PUT .../assignments/:releaseVersionId/override"
      to: "validateSegmentTimes"
      via: "Wiederverwendung der bestehenden 4-Minuten-Fenster-/Start-vor-Ende-Validierung"
      pattern: "validateSegmentTimes"
    - from: "vier neue Endpunkte"
      to: "shared/contracts/admin-content.yaml"
      via: "additive Endpunkt-/Type-Eintraege im bestehenden admin-anime-theme-create-Format (Zeilen 381-408), NICHT episode-versions.yaml (Nyquist-Fix W3)"
      pattern: "admin-segment-assign"
---

<objective>
Fügt vier neue Admin-Endpunkte hinzu, die Plan 117-01 bis 117-04 fachlich nutzbar machen: Kara einer
weiteren Release-Version zuweisen/entziehen (D-03), Per-Version-Zeit-Override setzen/entfernen (D-01).
Beide Schreibpfade gehen zwingend über die bestehende Capability `release_version.segments.manage`
und die bestehende `validateSegmentTimes`-Validierung — keine neue Autorisierungs- oder
Validierungslogik.

Purpose: Ohne diese Endpunkte bleiben die Backend-Wellen 117-01 bis 117-04 unerreichbar für die
Admin-UI (Welle 6). Dieser Plan ist die letzte reine Backend-Welle vor dem Frontend.

**Nyquist-Fix W3 (Plan-Checker):** Die vier neuen Endpunkte werden im OpenAPI-Contract additiv in
`shared/contracts/admin-content.yaml` dokumentiert (nicht in `episode-versions.yaml`). Das folgt
PATTERNS.md ("Contracts under `shared/contracts/`... extend it additively... segments already live
in `admin-content.yaml`") UND der tatsächlichen Domänen-Grenze der beiden Dateien: `admin-content.yaml`
dokumentiert bereits die verwandten Admin-CRUD-Schreibpfade unter `/api/v1/admin/anime/:id/...`
(z. B. `admin-anime-theme-create`, `POST /api/v1/admin/anime/:id/themes`, Zeilen 381-408) — exakt
dieselbe Namensraum-Familie wie die neuen `/api/v1/admin/anime/:id/segments/:segmentId/assignments...`-
Endpunkte. `episode-versions.yaml` dokumentiert dagegen ausschließlich die NICHT-Admin,
öffentlich/Session-erreichbaren Playback-Endpunkte `/api/v1/segments/:id/grant|render|stream`
(Zeilen 204-278) — ein anderer Endpunkt-Namensraum und eine andere Zielgruppe (App-Session statt
Admin-Capability). Keine der beiden Dateien dokumentiert die admin CRUD-Segment-Endpunkte
(`/api/v1/admin/anime/:id/segments*`) bereits vollständig — das ist eine vorbestehende Lücke, die
dieser Plan NICHT rückwirkend schließt, sondern nur additiv um die vier NEUEN Zuweisungs-/Override-
Endpunkte im Format von `admin-content.yaml` ergänzt.

Output: Neue fokussierte Handler-Datei (bestehende `admin_content_anime_theme_segments.go` bleibt bei
846 Zeilen unverändert, keine weitere Vergrößerung per CLAUDE.md 450-Zeilen-Regel), vier neue Routen,
OpenAPI-Contract-Eintrag in `admin-content.yaml`, Handler-Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-kara-segment-zeit-override-anzeige/117-UI-SPEC.md
@backend/internal/handlers/admin_content_anime_theme_segments.go
@shared/contracts/admin-content.yaml
</context>

<interfaces>
Wiederverwendung, KEIN Duplikat:
`func (h *AdminContentHandler) requireSegmentManage(c *gin.Context, releaseVariantID int64) bool`
(admin_content_anime_theme_segments.go:159-186) — trotz des Parameternamens `releaseVariantID` ist
der tatsächlich erwartete Wert eine `release_versions.id` (verifiziert über
`permissions.CanForReleaseVersion` -> `AuthzRepository.ResolveReleaseVersion`,
`backend/internal/repository/authz_permissions.go:72-85`, JOIN auf
`release_version_groups.release_version_id`). Neue Endpunkte übergeben hier direkt die echte
`release_version_id` aus dem URL-Pfad bzw. Body — KEINE neue Variante dieser Funktion nötig.
`func validateSegmentTimes(startTime, endTime *string, durationSeconds *int32) string`
(admin_content_anime_theme_segments.go:33-57).
`func parseClockToSeconds(raw *string) *int32` (admin_content_anime_theme_segments.go:61-85).
Aus Plan 117-02/117-03: `AssignThemeSegmentToReleaseVersion`, `UnassignThemeSegmentFromReleaseVersion`,
`UpsertThemeSegmentEpisodeOverride`, `DeleteThemeSegmentEpisodeOverride`,
`GetSegmentReleaseDuration(ctx, animeID, fansubGroupID, version, startEpisode, endEpisode)`.

Aus `shared/contracts/admin-content.yaml` (Formatvorbild für die neuen Endpunkt-Einträge, Zeilen
381-408, `admin-anime-theme-create`): `name`, `method`, `path`, `auth.required`/`auth.header`/
`auth.notes`, `path_params`, `query_params` (optional), `request_body.type`, `response.status`/
`response.type` — dasselbe Schema für alle vier neuen Endpunkte übernehmen.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Assignment- und Override-Endpunkte + Routing</name>
  <files>backend/internal/handlers/admin_content_anime_theme_segment_assignments.go, backend/cmd/server/admin_routes.go</files>
  <read_first>
    backend/internal/handlers/admin_content_anime_theme_segments.go Zeilen 1-186 (Imports,
    `validateSegmentTimes`, `parseClockToSeconds`, `requireSegmentManage`, `parseReleaseVariantIDQuery`
    als Stilvorbild für Query-/Path-Parsing) und Zeilen 328-486 (`UpdateAnimeSegment` — Vorbild für
    Fehler-Envelope-Shape `gin.H{"error": gin.H{"message": ...}}` und
    `errors.Is(err, repository.ErrConflict)`-Handling);
    backend/cmd/server/admin_routes.go Zeilen 116-125 (bestehende Segment-Routen — neue Routen direkt
    darunter registrieren, gleiches `auth`-Middleware-Muster).
  </read_first>
  <behavior>
    - `POST /api/v1/admin/anime/:id/segments/:segmentId/assignments` mit Body `{release_version_id}`:
      fehlende/ungültige `release_version_id` -> 400; fehlende Capability -> 403 (via
      `requireSegmentManage`); Erfolg -> 201 mit dem aktualisierten `AdminThemeSegment` (inkl.
      `assigned_release_version_ids`).
    - `DELETE /api/v1/admin/anime/:id/segments/:segmentId/assignments/:releaseVersionId`: Erfolg -> 200
      mit aktualisiertem Segment; nicht existierende Zuweisung -> 404.
    - `PUT /api/v1/admin/anime/:id/segments/:segmentId/assignments/:releaseVersionId/override` mit
      Body `{start_time, end_time}`: läuft durch `validateSegmentTimes` mit der Laufzeit DIESER
      spezifischen Release-Version (`GetSegmentReleaseDuration` mit dem Segment-Episodenbereich als
      Filter); ungültiger Zeitbereich -> 400 mit derselben deutschen Fehlermeldung wie beim
      Basis-Zeitbereich; Zeit außerhalb der Zuweisung (FK-Verletzung) -> 409; Erfolg -> 200.
    - `DELETE .../override`: Erfolg -> 200 mit aktualisiertem Segment (Override entfernt, Basis-Zeit
      greift wieder); kein Override vorhanden -> 404.
  </behavior>
  <action>
    Erzeuge `backend/internal/handlers/admin_content_anime_theme_segment_assignments.go` (Paket
    `handlers`, gleicher Empfänger `*AdminContentHandler`). Definiere
    `adminAnimeSegmentAssignmentRequest{ReleaseVersionID int64 \`json:"release_version_id"\`}` und
    `adminAnimeSegmentOverrideRequest{StartTime string \`json:"start_time"\`, EndTime string
    \`json:"end_time"\`}`. Implementiere `AssignAnimeSegment(c *gin.Context)`: parse `id`/`segmentId`
    aus dem Pfad, binde `adminAnimeSegmentAssignmentRequest`, rufe `h.requireSegmentManage(c,
    req.ReleaseVersionID)` auf, rufe `h.themeRepo.AssignThemeSegmentToReleaseVersion(ctx, segmentID,
    req.ReleaseVersionID)`, bei Erfolg lade das Segment neu über `GetAnimeSegmentByID(ctx, animeID,
    segmentID, req.ReleaseVersionID)` (Signatur aus Plan 117-04 Task 1) und antworte 201 mit `gin.H{"data":
    segment}`. Implementiere `UnassignAnimeSegment(c *gin.Context)` analog mit
    `releaseVersionId`-Pfadparameter, ruft `h.themeRepo.UnassignThemeSegmentFromReleaseVersion`, 200
    bei Erfolg, 404 bei `repository.ErrNotFound`. Implementiere `UpsertAnimeSegmentEpisodeOverride(c
    *gin.Context)`: parse `releaseVersionId` aus dem Pfad, `requireSegmentManage(c,
    releaseVersionID)`, binde `adminAnimeSegmentOverrideRequest`, lade das Segment für
    Episodenbereich (`StartEpisode`/`EndEpisode`), ermittle die Laufzeit über
    `GetSegmentReleaseDuration(ctx, animeID, *segment.FansubGroupID, segment.Version,
    derefEpisodeOrZero(segment.StartEpisode), derefEpisodeOrZero(segment.EndEpisode))`, validiere per
    `validateSegmentTimes(&req.StartTime, &req.EndTime, duration)` — bei Fehlermeldung 400 mit
    derselben Meldung. Rufe `h.themeRepo.UpsertThemeSegmentEpisodeOverride(ctx, ...)`; bei
    `errors.Is(err, repository.ErrConflict)` -> 409 `gin.H{"error": gin.H{"message": "release_version_id
    ist diesem Segment nicht zugewiesen", "code": "not_assigned"}}`. Nach Erfolg: rufe den in Plan
    117-04 Task 2 gebauten Invalidierungspfad für GENAU diese eine `releaseVersionID` auf (analog dem
    `UpdateAnimeSegment`-Muster, aber ohne Fan-Out — ein Override betrifft nur seine eigene
    Release-Version). Antworte 200 mit dem neu geladenen Segment. Implementiere
    `DeleteAnimeSegmentEpisodeOverride(c *gin.Context)` analog: `DeleteThemeSegmentEpisodeOverride`,
    danach ebenfalls Invalidierung für diese eine `releaseVersionID` (Basis-Zeit greift jetzt wieder,
    das ist eine Render-relevante Änderung), 200 bei Erfolg, 404 wenn kein Override existierte.
    Registriere in `backend/cmd/server/admin_routes.go` direkt unterhalb der bestehenden
    Segment-Routen (nach Zeile 125): `v1.POST("/admin/anime/:id/segments/:segmentId/assignments",
    auth, deps.adminContentHandler.AssignAnimeSegment)`,
    `v1.DELETE("/admin/anime/:id/segments/:segmentId/assignments/:releaseVersionId", auth,
    deps.adminContentHandler.UnassignAnimeSegment)`,
    `v1.PUT("/admin/anime/:id/segments/:segmentId/assignments/:releaseVersionId/override", auth,
    deps.adminContentHandler.UpsertAnimeSegmentEpisodeOverride)`,
    `v1.DELETE("/admin/anime/:id/segments/:segmentId/assignments/:releaseVersionId/override", auth,
    deps.adminContentHandler.DeleteAnimeSegmentEpisodeOverride)`.
  </action>
  <acceptance_criteria>
    - `go build ./...` kompiliert fehlerfrei.
    - `backend/internal/handlers/admin_content_anime_theme_segment_assignments.go` bleibt unter 300
      Zeilen.
    - Jede der vier Handler-Funktionen ruft `h.requireSegmentManage` als ERSTE
      Autorisierungsprüfung auf, bevor irgendeine DB-Mutation stattfindet (Grep-Nachweis:
      `requireSegmentManage` erscheint vor jedem `h.themeRepo.`-Mutationsaufruf in der Datei).
    - Override-Endpunkt gibt bei ungültigem Zeitbereich denselben Fehlertext wie
      `validateSegmentTimes` beim Basis-Zeitbereich zurück (keine abweichende Fehlermeldung für
      dieselbe Regel).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./...</automated>
  </verify>
  <done>Vier neue Endpunkte registriert, capability-gated, wiederverwenden validateSegmentTimes und die release-version-scoped Invalidierung.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Handler-Tests + OpenAPI-Contract-Eintrag in admin-content.yaml</name>
  <files>backend/internal/handlers/admin_content_anime_theme_segment_assignments_test.go, shared/contracts/admin-content.yaml</files>
  <read_first>
    backend/internal/handlers/segment_stream_test.go (Stil für Gin-Test-Context-Aufbau mit
    `gin.CreateTestContext`/`httptest.NewRequest`, Stub-Repository-Muster);
    shared/contracts/admin-content.yaml Zeilen 381-408 (`admin-anime-theme-create` — Formatvorbild:
    `name`/`method`/`path`/`auth`/`path_params`/`query_params`/`request_body`/`response` — DIESES
    Format für die vier neuen Endpunkte übernehmen, NICHT das `episode-versions.yaml`-Format der
    Public-Playback-Endpunkte, Nyquist-Fix W3) und Zeilen 1301-1340 (`types:`-Sektion,
    `AdminAnimeThemeCreateRequest`/`AdminAnimeThemeCreateResponse` als Formatvorbild für die zwei
    neuen Request-Types).
  </read_first>
  <behavior>
    - `AssignAnimeSegment` ohne Capability -> 403, mit Capability + gültigem Body -> 201.
    - `UpsertAnimeSegmentEpisodeOverride` mit `end_time` vor `start_time` -> 400 mit der bestehenden
      deutschen Validierungsmeldung.
    - `UpsertAnimeSegmentEpisodeOverride` gegen eine nicht zugewiesene `release_version_id` (Stub-Repo
      liefert `repository.ErrConflict`) -> 409.
    - `DeleteAnimeSegmentEpisodeOverride` gegen eine Release-Version ohne Override (Stub-Repo liefert
      `repository.ErrNotFound`) -> 404.
  </behavior>
  <action>
    Erzeuge `backend/internal/handlers/admin_content_anime_theme_segment_assignments_test.go` mit den
    vier Fällen aus dem `<behavior>`-Block, per Stub-Repository (gleiches Interface-Stub-Muster wie
    `segment_stream_test.go`). Ergänze in `shared/contracts/admin-content.yaml` unter `- name:`
    (Endpunkt-Liste, direkt nach dem `admin-anime-theme-create`-Eintrag, Zeile 408) vier neue Einträge
    (`name: admin-segment-assign`, `admin-segment-unassign`, `admin-segment-episode-override-upsert`,
    `admin-segment-episode-override-delete`) im bestehenden Format dieser Datei (`method`, `path`,
    `auth` mit `required: true`/`header`/`notes`, `path_params`, `request_body`, `response`), mit
    `path` `/api/v1/admin/anime/:id/segments/:segmentId/assignments` bzw.
    `.../assignments/:releaseVersionId` bzw. `.../assignments/:releaseVersionId/override`. Ergänze
    unter `types:` (nach `AdminAnimeThemeCreateResponse`, Zeile 1333) einen Eintrag
    `AdminThemeSegmentAssignmentRequest` (`release_version_id: int64`) und
    `AdminThemeSegmentEpisodeOverrideRequest` (`start_time: string`, `end_time: string`), analog zur
    vorhandenen `AdminAnimeThemeCreateRequest`-Type-Definition.
  </action>
  <acceptance_criteria>
    - `go test ./internal/handlers/... -run AnimeSegmentAssignment -v` ist grün.
    - Alle vier Verhaltensfälle aus `<behavior>` sind als eigene Testfunktionen/Subtests vorhanden.
    - `shared/contracts/admin-content.yaml` bleibt valides YAML (kein Parse-Fehler).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/handlers/... -run AnimeSegmentAssignment -v</automated>
  </verify>
  <done>Vier Handler-Tests grün; neue Endpunkte additiv im OpenAPI-Contract admin-content.yaml dokumentiert (nicht episode-versions.yaml).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin-Client -> neue Assignment/Override-Endpunkte | Authentifizierter Admin-Kontext, gescoped auf Fansub-Gruppen-Rollen via bestehende Permission Engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-117-05-01 | Elevation of Privilege | Vier neue Schreib-Endpunkte ohne eigene Autorisierung | mitigate | Jeder Endpunkt ruft `requireSegmentManage` (Capability `release_version.segments.manage`) als ersten Schritt auf — identisches Gate wie die bestehenden Segment-Endpunkte, keine neue Capability erfunden |
| T-117-05-02 | Tampering | Override-Zeit umgeht die 4-Minuten-Fenster-/Laufzeit-Validierung | mitigate | `validateSegmentTimes` wird 1:1 wiederverwendet, keine Parallel-Validierung |
| T-117-05-03 | Tampering | Override fuer nicht zugewiesene Release-Version | mitigate | DB-composite-FK (Plan 117-01) + Repository-Fehlerübersetzung auf `ErrConflict` -> 409, kein stiller Erfolg |
| T-117-05-SC | Tampering | keine neuen Paket-Installationen | accept | Nur Standardbibliothek + bestehende `gin`/`pgx`-Abhängigkeiten |
</threat_model>

<verification>
`go build ./...` fehlerfrei; `go test ./internal/handlers/... -run AnimeSegmentAssignment -v` grün;
`go test ./internal/handlers/... -run Segment -v` (bestehende Segment-Tests) bleibt grün.
</verification>

<success_criteria>
Vier neue, capability-gated Endpunkte existieren, wiederverwenden bestehende Validierung/Autorisierung
vollständig, sind additiv im OpenAPI-Contract `admin-content.yaml` dokumentiert (konsistent mit
PATTERNS.md und der bestehenden Admin-CRUD-Namensraum-Grenze, Nyquist-Fix W3), und sind
handler-getestet.
</success_criteria>

<output>
Create `.planning/phases/117-kara-segment-zeit-override-anzeige/117-05-SUMMARY.md` when done
</output>
