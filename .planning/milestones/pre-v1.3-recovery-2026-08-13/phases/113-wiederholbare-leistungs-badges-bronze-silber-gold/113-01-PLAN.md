---
phase: 113-wiederholbare-leistungs-badges-bronze-silber-gold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/repository/member_profile_contribution_badges_repository.go
  - backend/internal/repository/member_profile_contribution_badges_repository_test.go
  - backend/internal/repository/member_profile_repository.go
autonomous: true
requirements: [GAM-04]
user_setup: []

must_haves:
  truths:
    - "D-01: Live-Projektion — jeder Profil-Read aggregiert frisch; sinkt die Netto-Zahl unter eine Schwelle, verschwindet das Badge automatisch (kein persistierter Zustand)."
    - "D-02: Familie 1 zählt ein Projekt (anime_id, fansub_group_id) nur, wenn der Member auf JEDER ledger-erfassten release_version (≥1 awarded Credit von irgendwem) einen eigenen netto-awarded release_role_credit_lifecycles-Credit hat; Schwellen 1/5/15."
    - "D-03: Familie 2 (Chronist) zählt release_version_notes (member_id direkt, Pflicht-Kern) additiv plus anime_fansub_project_notes + fansub_group_notes via created_by_user_id→verified-member_claims-Seam, jeweils status='published' AND deleted_at IS NULL, visibility-unabhängig; Schwellen 10/50/150."
    - "D-04: Familie 3 (Bildarchivar) zählt COUNT(*) über release_version_media via Autor-Seam, nur deleted_at IS NULL, OHNE review_status/visibility-Join; Schwellen 10/50/150."
    - "GAM-04: Alle drei Familien werden als synthetische PublicMemberBadge{ID:0} emittiert, nie in member_badges persistiert; kein UpsertMemberBadge, kein INSERT."
    - "V4 Access Control: loadContributionBadges wird ausschließlich innerhalb von GetPublicMemberProfile aufgerufen, hinter dem bestehenden members_only-Visibility-Gate — kein neuer Einstiegspfad."
  artifacts:
    - path: "backend/internal/repository/member_profile_contribution_badges_repository.go"
      provides: "3 Schwellenfunktionen (highestContribProjectsTier/ChronicleTier/ArchivistTier) + loadContributionBadges"
      contains: "func (r *MemberProfileRepository) loadContributionBadges"
      min_lines: 90
    - path: "backend/internal/repository/member_profile_contribution_badges_repository_test.go"
      provides: "DB-freie Grenzwert-Unit-Tests pro Familie + optionaler Postgres-Integrationstest (SKIP ohne DSN)"
      contains: "func TestHighestContribProjectsTier"
    - path: "backend/internal/repository/member_profile_repository.go"
      provides: "≤5-Zeilen-Callsite: Aufruf von loadContributionBadges + Append an profile.PublicBadges"
      contains: "loadContributionBadges"
  key_links:
    - from: "GetPublicMemberProfile"
      to: "loadContributionBadges"
      via: "Aufruf direkt nach loadRoleVolumeBadges, Append an profile.PublicBadges"
      pattern: "loadContributionBadges\\(ctx"
    - from: "loadContributionBadges"
      to: "release_role_credit_lifecycles / release_version_notes / release_version_media"
      via: "parametrisierte pgx-Queries ($1 = memberID)"
      pattern: "lifecycle_status = 'awarded'"
---

<objective>
Baut die read-time Backend-Datenschicht für die drei abgeleiteten Badge-Familien (Familie 1
„Mitgetragene Projekte", Familie 2 „Chronist", Familie 3 „Bildarchivar"). Pro Familie eine
member-gefilterte SQL-Aggregation → reine Schwellenfunktion → höchste erreichte Stufe als
synthetisches `PublicMemberBadge{ID:0}`. Kein neuer Buchungspfad, keine Persistenz, kein neuer
Handler — reine Erweiterung des bestehenden `GetPublicMemberProfile`-Reads exakt nach dem
Phase-112-`loadRoleVolumeBadges`-Muster.

Purpose: Erfüllt GAM-04 + D-01…D-04 serverseitig; liefert dem Frontend fertige Badge-Codes
(`contribution_{family}_{tier}`), sodass dort nur Präsentation nötig ist.
Output: Neues Split-File mit Aggregations-Reads + Schwellenfunktionen, Unit-/Integrationstests,
≤5-Zeilen-Callsite in der bestehenden 1881-Zeilen-Repository-Datei.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-wiederholbare-leistungs-badges-bronze-silber-gold/113-CONTEXT.md
@.planning/phases/113-wiederholbare-leistungs-badges-bronze-silber-gold/113-RESEARCH.md
@.planning/phases/113-wiederholbare-leistungs-badges-bronze-silber-gold/113-PATTERNS.md

<interfaces>
<!-- Blueprint 1:1 adaptieren. Executor braucht KEINE Codebase-Exploration darüber hinaus. -->

Blueprint (kanonisches Vorbild, komplette Datei) — backend/internal/repository/member_profile_role_volume_repository.go:
```go
package repository

import (
	"context"
	"fmt"

	"team4s.v3/backend/internal/models"
)

func highestRoleVolumeTier(count int) string {
	switch {
	case count >= 510: return "platinum"
	case count >= 320: return "gold"
	case count >= 108: return "silver"
	case count >= 12:  return "bronze"
	default:           return ""
	}
}

func (r *MemberProfileRepository) loadRoleVolumeBadges(ctx context.Context, memberID int64) ([]models.PublicMemberBadge, error) {
	rows, err := r.db.Query(ctx, `SELECT role_code, COUNT(*) ... WHERE member_id = $1 AND lifecycle_status = 'awarded' GROUP BY role_code`, memberID)
	// ... rows.Scan(&roleCode, &count); if tier := highestRoleVolumeTier(count); tier != "" { append PublicMemberBadge{ID:0, ...} }
}
```

Transport-Typ — backend/internal/models/member_profile.go:187:
```go
type PublicMemberBadge struct {
	ID            int64  `json:"id"`
	BadgeCode     string `json:"badge_code"`
	BadgeCategory string `json:"badge_category"`
}
```

Bestehende Callsite (Anschlusspunkt) — member_profile_repository.go:535-539:
```go
volumeBadges, loadErr := r.loadRoleVolumeBadges(ctx, row.memberID)
if loadErr != nil { return nil, loadErr }
profile.PublicBadges = append(profile.PublicBadges, volumeBadges...)
```

Autor→Member-Seam (Pflicht Familie 3, optional Familie 2) — member_profile_repository.go:1379-1386:
```sql
WHERE <col>.uploaded_by_user_id IN (
    SELECT au.legacy_user_id
    FROM member_claims mc
    JOIN app_users au ON au.id = mc.app_user_id
    WHERE mc.member_id = $1 AND mc.claim_status = 'verified' AND au.legacy_user_id IS NOT NULL
)
```

Projekt-Release-Mengen-Definition (Familie-1-Basis) — member_profile_repository.go:1661-1666:
```sql
FROM release_versions rv
JOIN release_version_groups rvg ON rvg.release_version_id = rv.id
JOIN fansub_releases fr ON fr.id = rv.release_id
JOIN episodes ep ON ep.id = fr.episode_id
WHERE ep.anime_id = :anime_id AND rvg.fansub_group_id = :fansub_group_id
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split-File anlegen mit drei reinen Schwellenfunktionen + Grenzwert-Unit-Tests (Wave 0)</name>
  <files>backend/internal/repository/member_profile_contribution_badges_repository.go, backend/internal/repository/member_profile_contribution_badges_repository_test.go</files>
  <read_first>
    - backend/internal/repository/member_profile_role_volume_repository.go (Blueprint: Package, Imports, highestRoleVolumeTier)
    - backend/internal/repository/member_profile_repository_postgres_test.go (Zeilen um TestHighestRoleVolumeTier, ~228-242, als Test-Analog)
  </read_first>
  <behavior>
    - highestContribProjectsTier: 0→"", 1→"bronze", 4→"bronze", 5→"silver", 14→"silver", 15→"gold"
    - highestContribChronicleTier: 9→"", 10→"bronze", 49→"bronze", 50→"silver", 149→"silver", 150→"gold"
    - highestContribArchivistTier: 9→"", 10→"bronze", 49→"bronze", 50→"silver", 149→"silver", 150→"gold"
  </behavior>
  <action>
    Lege `member_profile_contribution_badges_repository.go` an (package `repository`, Imports `context`, `fmt`, `team4s.v3/backend/internal/models`).
    Definiere drei reine Schwellenfunktionen nach dem `highestRoleVolumeTier`-switch-Muster (absteigend, höchste Stufe zuerst, `""` = keine Stufe, Tier-Tokens intern-englisch bronze/silver/gold):
    `highestContribProjectsTier(count int) string` mit Schwellen 15/5/1 (D-02); `highestContribChronicleTier(count int) string` mit Schwellen 150/50/10 (D-03); `highestContribArchivistTier(count int) string` mit Schwellen 150/50/10 (D-04). KEIN platinum-Tier (Familien enden bei Gold).
    Lege `member_profile_contribution_badges_repository_test.go` an mit drei DB-freien Tests `TestHighestContribProjectsTier`, `TestHighestContribChronicleTier`, `TestHighestContribArchivistTier` (testify `require.Equal`), die die Grenzwerte oben abdecken.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/repository/... -run "TestHighestContrib" -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/repository/... -run "TestHighestContrib" -v` meldet 3 PASS, 0 FAIL.
    - Quelle enthält exakt `func highestContribProjectsTier`, `func highestContribChronicleTier`, `func highestContribArchivistTier`.
    - Kein `platinum` in `member_profile_contribution_badges_repository.go`.
    - Datei bleibt package `repository`.
  </acceptance_criteria>
  <done>Drei Schwellenfunktionen existieren, Grenzwert-Unit-Tests grün, DB-frei lauffähig.</done>
</task>

<task type="auto">
  <name>Task 2: Drei Aggregations-Reads + loadContributionBadges-Emission + ≤5-Zeilen-Callsite</name>
  <files>backend/internal/repository/member_profile_contribution_badges_repository.go, backend/internal/repository/member_profile_repository.go</files>
  <read_first>
    - backend/internal/repository/member_profile_role_volume_repository.go (loadRoleVolumeBadges: Query/Scan/append-Muster + ID:0-Emission)
    - backend/internal/repository/member_profile_repository.go (GetPublicMemberProfile um Zeile 535-539 = loadRoleVolumeBadges-Callsite; Autor-Seam :1379-1386; Projekt-Release-Count :1661-1666)
    - .planning/phases/113-wiederholbare-leistungs-badges-bronze-silber-gold/113-RESEARCH.md (Abschnitt „Recommended Familie-1 Query-Shape", Zeilen 204-229)
  </read_first>
  <action>
    Ergänze im Split-File die Methode `func (r *MemberProfileRepository) loadContributionBadges(ctx context.Context, memberID int64) ([]models.PublicMemberBadge, error)`. Initialisiere `items := make([]models.PublicMemberBadge, 0)`, aggregiere pro Familie via parametrisiertem `r.db.QueryRow(ctx, ..., memberID).Scan(&count)`, mappe über die Schwellenfunktion aus Task 1 und appende bei `tier != ""` ein `PublicMemberBadge{ID: 0, BadgeCode: "contribution_"+family+"_"+tier, BadgeCategory: "contribution"}`. Badge-Codes exakt: `contribution_projects_{tier}`, `contribution_chronicle_{tier}`, `contribution_archivist_{tier}`.
    Familie 3 (Bildarchivar, D-04): `SELECT COUNT(*) FROM release_version_media rvm WHERE rvm.deleted_at IS NULL AND rvm.uploaded_by_user_id IN (<Autor-Seam>)`. Den `review_statuses='approved'`/`visibilities='public'`-Join aus `loadLatestContributions` NICHT übernehmen (Anti-Pattern D-04).
    Familie 2 (Chronist, D-03): Pflicht-Kern `SELECT COUNT(*) FROM release_version_notes WHERE member_id = $1 AND status = 'published' AND deleted_at IS NULL`; additiv `anime_fansub_project_notes` + `fansub_group_notes` über den `created_by_user_id`-Autor-Seam mit denselben Gates (`status='published' AND deleted_at IS NULL`, visibility-unabhängig). Member-Story zählt NICHT.
    Familie 1 (Coverage, D-02): Query-Form aus RESEARCH „Recommended Familie-1 Query-Shape". `project_versions` = Release-Versionen je (anime_id, fansub_group_id), eingeschränkt auf Versionen mit ≥1 awarded Credit von IRGENDWEM (Landmine A1 — NICHT literal jede release_version); `member_covered` = release_versions mit ≥1 awarded Credit DES Members (`release_role_credit_lifecycles WHERE member_id=$1 AND lifecycle_status='awarded'`); zähle Projekte mit `COUNT(*) > 0 AND COUNT(*) = COUNT(*) FILTER (WHERE member covered)`.
    Wire die Callsite in `GetPublicMemberProfile` direkt NACH dem `loadRoleVolumeBadges`-Append (identische Fehler-/Append-Form, ≤5 Zeilen): `contributionBadges, loadErr := r.loadContributionBadges(ctx, row.memberID); if loadErr != nil { return nil, loadErr }; profile.PublicBadges = append(profile.PublicBadges, contributionBadges...)`. Keine weitere Logik in `member_profile_repository.go`.
    Alle Queries ausschließlich parametrisiert (pgx `$1`), kein String-Building (V5). Kein `UpsertMemberBadge`, kein `member_badges`-INSERT (GAM-04).
  </action>
  <verify>
    <automated>cd backend && go build ./... && go vet ./internal/repository/... && go test ./internal/repository/... -run "TestHighestContrib" -v</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` kompiliert fehlerfrei; `go vet ./internal/repository/...` ohne Befund.
    - Split-File enthält `func (r *MemberProfileRepository) loadContributionBadges` und emittiert `ID: 0` sowie die drei Badge-Code-Präfixe `contribution_projects_`, `contribution_chronicle_`, `contribution_archivist_`.
    - `member_profile_repository.go`-Diff ist ausschließlich der ≤5-Zeilen-Callsite (grep `loadContributionBadges` findet genau einen Aufruf; keine neue Query in dieser Datei).
    - Familie-3-Query enthält KEIN `review_status`/`visibilit`-Token; Familie-1-Query schränkt `project_versions` auf awarded-Credit-Versionen ein (grep `lifecycle_status = 'awarded'`).
    - Kein `UpsertMemberBadge`/`INSERT INTO member_badges` im Split-File.
  </acceptance_criteria>
  <done>loadContributionBadges liefert die drei Familien als ID:0-Badges; GetPublicMemberProfile hängt sie an PublicBadges an; Build + vet grün.</done>
</task>

<task type="auto">
  <name>Task 3: Postgres-Integrationstest (SKIP ohne DSN) für Live-Downgrade, Coverage, Notiz- und Media-Netto-Semantik</name>
  <files>backend/internal/repository/member_profile_contribution_badges_repository_test.go</files>
  <read_first>
    - backend/internal/repository/member_profile_repository_postgres_test.go (TestLoadPublicBadgesPostgresRoleVolume ~186-226: DSN-Gate/SKIP-Muster, Helper containsPublicBadge/postgresAwardInput/insertRoleEntryLifecycleRow)
    - backend/internal/repository/member_profile_contribution_badges_repository.go (loadContributionBadges als System under Test)
  </read_first>
  <action>
    Ergänze im Test-File einen Postgres-Integrationstest `TestLoadContributionBadgesPostgres`, der wie `TestLoadPublicBadgesPostgresRoleVolume` `TEAM4S_PHASE106_TEST_DSN` liest und bei fehlendem DSN via `t.Skip(...)` überspringt (SKIP ist KEIN FAIL). Nutze die vorhandenen Helper (`containsPublicBadge`, `postgresAwardInput`, `insertRoleEntryLifecycleRow`) wo möglich.
    Decke ab: (D-01) award N awarded Credits → Familie-1/2/3-Stufe im Ergebnis sichtbar; reverse unter die Schwelle (bzw. soft-delete) → Stufe verschwindet beim erneuten Read. (D-02) Vollabdeckung aller ledger-erfassten release_versions eines (anime,group) → Projekt zählt; eine Lücke → zählt nicht. (D-03) release_version_note `status='published'` zählt, `deleted_at IS NOT NULL` zählt nicht. (D-04) jede release_version_media-Row zählt (TOTAL), soft-delete raus, review/visibility ignoriert. Assertion via `containsPublicBadge(profile.PublicBadges, "contribution_..._<tier>")`.
    Kein persistiertes `member_badges`-Row wird erzeugt (GAM-04) — prüfe, dass die Badges nur read-time im DTO erscheinen.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/repository/... -run "TestLoadContributionBadgesPostgres" -v</automated>
  </verify>
  <acceptance_criteria>
    - Ohne gesetztes `TEAM4S_PHASE106_TEST_DSN` läuft der Test als SKIP (Output enthält `--- SKIP`), nicht als FAIL.
    - Test-Quelle enthält `func TestLoadContributionBadgesPostgres` und referenziert alle drei Badge-Code-Präfixe.
    - Kompiliert im Gesamtbuild (`go build ./...` grün) und läuft in `go test ./...` mit (SKIP zählt als bestanden).
  </acceptance_criteria>
  <done>Integrationstest existiert, SKIP-sicher ohne DSN, deckt D-01…D-04 + GAM-04 ab, kompiliert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP-Client → GetPublicMemberProfile | Untrusted `slug` (bereits validiert/normalisiert im Handler); löst intern die member_id auf |
| Repository → PostgreSQL | Aggregations-Queries mit member_id als einzigem Parameter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | Tampering | loadContributionBadges SQL (member_id) | mitigate | Ausschließlich parametrisierte pgx-Queries (`$1`), kein String-Building; member_id ist intern aufgelöste int64, kein direkter User-Input (V5) |
| T-113-02 | Information Disclosure | Aggregierte Contribution-Stufe im öffentlichen Profil | accept | Bewusst akzeptiert per D-03/D-04: nur die aggregierte Tier-Stufe wird exponiert, keine Rohzeilen/IDs/Inhalte. Badge = intrinsische Member-Leistung, nicht Sichtbarkeitspräferenz |
| T-113-03 | Elevation of Privilege | Umgehung des members_only-Visibility-Gates | mitigate | loadContributionBadges wird NUR innerhalb GetPublicMemberProfile aufgerufen, hinter dem bestehenden members_only-Gate (app_public_profile.go:51-58); kein neuer Handler/Endpunkt/Einstiegspfad (V4) |
| T-113-04 | Denial of Service | Teure Familie-1-Coverage-Aggregation | mitigate | Single-Profile-Scope, indizierte Joins (idx_release_role_credit_context, idx_rvm_public); niemals in Listen-/Ranking-Schleifen aufgerufen |
</threat_model>

<verification>
- `cd backend && go test ./internal/repository/... -run "TestHighestContrib" -v` → 3 PASS
- `cd backend && go build ./... && go vet ./internal/repository/...` → grün
- `cd backend && go test ./...` → grün (Integrationstest SKIP ohne DSN)
- grep-Gate: `member_profile_repository.go` enthält genau einen `loadContributionBadges`-Aufruf und keine neue Familien-Query
</verification>

<success_criteria>
- Neues Split-File mit 3 Schwellenfunktionen + loadContributionBadges (ID:0, nie persistiert).
- Callsite in GetPublicMemberProfile ≤5 Zeilen; member_profile_repository.go bleibt frei von Familien-Logik.
- Familie 1 nutzt ledger-erfasste Release-Menge (A1/D-02), Familie 3 ignoriert review/visibility (D-04), Familie 2 kombiniert release_version_notes + Seam-Tabellen (D-03).
- Alle SQL parametrisiert; kein member_badges-INSERT (GAM-04). Build/vet/Tests grün.
</success_criteria>

<output>
Create `.planning/phases/113-wiederholbare-leistungs-badges-bronze-silber-gold/113-01-SUMMARY.md` when done
</output>
