---
phase: 77-leader-workspace-public-preview-readiness
plan: 02
type: execute
wave: 1
depends_on: ["77-01"]
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/ReadinessTab.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/PublicPreviewPanel.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css
autonomous: true
requirements: [F, I, K]

must_haves:
  truths:
    - "ReadinessTab.tsx existiert, ≤ 450 Zeilen, exportiert ReadinessTab-Komponente"
    - "PublicPreviewPanel.tsx existiert, ≤ 450 Zeilen, rendert FansubProfileTabs + GroupLeaderTimeline im Fallback-Modus (D-01)"
    - "Readiness-Zähler aggregieren ausschließlich listGroupMembers, listPendingMemberClaims, getAdminFansubAnime (Lock K)"
    - "Claims/Contributions tragen Badge variant=info — kein warning/danger-Urteil (D-06)"
    - "Alle Sprungmarken nutzen router.replace mit ?tab= ohne neue Routing-Infrastruktur (D-04)"
    - "Alle user-facing Strings mit korrekten Umlauten; kein ASCII-Ersatz"
    - "Nur @/components/ui-Primitive — keine nativen button/select/input/textarea (C1)"
    - "PublicPreviewPanel enthält TODO(Phase 73)-Kommentar als dokumentierten Upgrade-Pfad (D-01)"
    - "Kriterium 'Gruppengeschichte' ist als informativer Prüf-Hinweis (variant=info, Sprungmarke) umgesetzt, NICHT als permanentes 'fehlt' — kein has_notes-DTO-Feld (Lock K, RESEARCH.md RESOLVED)"
    - "D-02: PublicPreviewPanel zeigt die exakte anonyme Besucher-Sicht (nur öffentlich + freigegeben); interne/in-Prüfung-Marker bewusst nicht in der Preview — was fehlt, sagt der Readiness-Check"
    - "D-05: Readiness ist reiner Leitfaden, kein Gate — kein neuer Publish-/Sichtbarkeits-Toggle, keine danger-Farbe, keine Block-Affordanz; Hinweistext 'Leitfaden, kein Freigabe-Schalter'"
    - "D-07: nur Reuse/Bündeln bestehender Kontext-Pflege über Sprungmarken auf die zuständigen Tabs (Gruppengeschichte/Anime-Einblicke/Release-Drawer) — keine neuen Editierfelder, kein neuer Schreibcode (Owner-Tabellen unverändert, Ownership-Matrix G)"
  artifacts:
    - path: "frontend/src/app/admin/fansubs/[id]/edit/ReadinessTab.tsx"
      provides: "Readiness-Checkliste + Preview-Einbettung, capability-gated"
      min_lines: 80
      exports: ["ReadinessTab"]
    - path: "frontend/src/app/admin/fansubs/[id]/edit/PublicPreviewPanel.tsx"
      provides: "Read-only Preview-Wrapper (Fallback auf FansubProfileTabs + GroupLeaderTimeline)"
      min_lines: 30
      exports: ["PublicPreviewPanel"]
    - path: "frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css"
      provides: "Neue readiness*-CSS-Klassen"
      contains: ".readinessTabRoot"
  key_links:
    - from: "ReadinessTab.tsx"
      to: "api.ts"
      via: "listGroupMembers, listPendingMemberClaims, getAdminFansubAnime"
      pattern: "listGroupMembers|listPendingMemberClaims|getAdminFansubAnime"
    - from: "ReadinessTab.tsx"
      to: "PublicPreviewPanel.tsx"
      via: "direkter Import in ReadinessTab"
      pattern: "import.*PublicPreviewPanel"
    - from: "ReadinessTab.tsx (members/projects state)"
      to: "PublicPreviewPanel.tsx"
      via: "Props members={members} projects={projects} aus listGroupMembers/getAdminFansubAnime"
      pattern: "members.*projects.*PublicPreviewPanel"
---

<objective>
Zwei neue Komponentendateien implementieren: `ReadinessTab.tsx` (Readiness-Checkliste + Preview-Einbettung) und `PublicPreviewPanel.tsx` (Read-only-Fallback auf bestehende öffentliche Fansub-Komponenten). Ergänzung neuer CSS-Klassen in `FansubEdit.module.css`.

Zweck: Die eigentliche Produktionslogik von Phase 77 — capability-gated, read-only, ohne neuen Backend-Endpunkt. Nach diesem Plan laufen die Wave-0-Tests aus Plan 01 grün.

Ausgabe: ReadinessTab.tsx, PublicPreviewPanel.tsx (beide ≤ 450 Zeilen), neue CSS-Klassen.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/77-leader-workspace-public-preview-readiness/77-CONTEXT.md
@.planning/phases/77-leader-workspace-public-preview-readiness/77-RESEARCH.md
@.planning/phases/77-leader-workspace-public-preview-readiness/77-UI-SPEC.md
@.planning/phases/77-leader-workspace-public-preview-readiness/77-PATTERNS.md
</context>

<interfaces>
<!-- Bestehende API-Seams (api.ts — verifiziert) -->

listGroupMembers(fansubId: number): Promise<{ data: FansubMember[] }>  -- api.ts Zeile 6989
listPendingMemberClaims(fansubId: number): Promise<MemberClaim[]>      -- api.ts Zeile 3442
getAdminFansubAnime(fansubId: number): Promise<{ data: AdminFansubAnimeEntry[] }>  -- api.ts Zeile 4148

<!-- FansubGroup-Felder (types/fansub.ts, verifiziert) -->
FansubGroup: { logo_url?: string|null, banner_url?: string|null, name, slug, status, country, founded_year, group_type }

<!-- FansubProfileTabs Props (FansubProfileTabs.tsx Zeilen 14–19, verifiziert) -->
interface FansubProfileTabsProps {
  group: FansubGroup
  members: FansubMember[]
  projects: AnimeListItem[]
}

<!-- GroupLeaderTimeline Props (GroupLeaderTimeline.tsx, verifiziert) -->
interface GroupLeaderTimelineProps {
  entries: PublicFansubLeaderEntry[]
}

<!-- buildFansubFactSummary (lib/fansub-summary.ts Zeile 23) -->
buildFansubFactSummary(group: FansubGroup): string | null
-- gibt Kurzbeschreibung aus Gründungsjahr/Land/Status zurück; null wenn keine Daten

<!-- Sprungmarken-Navigationsmuster (page.tsx Zeilen 1189–1207, verifiziert) -->
const next = new URLSearchParams(searchParams.toString())
next.set('tab', targetTab)
router.replace(`${pathname}?${next.toString()}`, { scroll: false })

<!-- CSS-Modul-Merge (GroupMembersTab.tsx Zeile 38, verifiziert) -->
import sharedStyles from '../../../admin.module.css'
import fansubEditStyles from './FansubEdit.module.css'
const styles = { ...sharedStyles, ...fansubEditStyles }
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ReadinessTab.tsx implementieren</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/ReadinessTab.tsx</files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/GroupMembersTab.tsx (vollständig — Analog-Datei; useCallback/useEffect/useState-Muster, ApiError-Handling, LoadingState/ErrorState-Nutzung, CSS-Modul-Merge, Zeilen 1–450)
    - .planning/phases/77-leader-workspace-public-preview-readiness/77-UI-SPEC.md (Checklisten-Item-Anatomie Zeilen 147–183, Copywriting-Contract Zeilen 189–214, Zustände Zeilen 222–231)
    - .planning/phases/77-leader-workspace-public-preview-readiness/77-PATTERNS.md (ReadinessTab.tsx-Sektion, Zeilen 25–177)
    - frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css (bestehende Klassen prüfen bevor neue hinzugefügt werden)
  </read_first>
  <behavior>
    - render mit group.logo_url=null → Listeneintrag „Logo vorhanden" mit Badge variant=warning sichtbar
    - render mit group.logo_url='https://...' → Listeneintrag „Logo vorhanden" mit Badge variant=success sichtbar
    - render mit listPendingMemberClaims resolved [claim1, claim2] → Text „Offene Claims: 2" sichtbar mit Badge variant=info (KEIN variant=warning)
    - render, Klick auf Sprungmarke für Logo → router.replace aufgerufen mit param enthält tab=media
    - render → kein native button-Element (ausschließlich Button aus @/components/ui)
    - render → keine Schreib-Inputs, kein type=submit, kein onSave-Callback
    - render → Kriterium „Gruppengeschichte prüfen" wird mit Badge variant=info und Sprungmarke auf notes-Tab angezeigt (KEIN variant=warning/„fehlt", da kein has_notes-DTO-Feld verfügbar ist — Lock K)
  </behavior>
  <action>
    Neue Datei `ReadinessTab.tsx` erstellen. Maximale Länge: 450 Zeilen.

    Datei-Kopf: `'use client'`. Imports: useCallback, useEffect, useState aus react; AlertCircle, ArrowRight, Check, Info aus lucide-react; Badge, Button, Card, EmptyState, ErrorState, LoadingState, SectionHeader aus @/components/ui; ApiError, getAdminFansubAnime, listGroupMembers, listPendingMemberClaims aus @/lib/api; buildFansubFactSummary aus @/lib/fansub-summary; type FansubGroup, type FansubMember aus @/types/fansub; type AnimeListItem aus @/types/anime; usePathname, useRouter, useSearchParams aus next/navigation; PublicPreviewPanel aus ./PublicPreviewPanel; sharedStyles aus ../../../admin.module.css; fansubEditStyles aus ./FansubEdit.module.css; const styles = { ...sharedStyles, ...fansubEditStyles }.

    Props: `interface ReadinessTabProps { fansubId: number; group: FansubGroup }`. Export: `export function ReadinessTab({ fansubId, group }: ReadinessTabProps)`.

    Interner Hook `useTabNavigation()` — erzeugt navigateToTab-Funktion per router.replace-Pattern aus PATTERNS.md Zeile 156–171; nutzt useRouter, usePathname, useSearchParams aus next/navigation.

    State: loading (boolean), error (string|null), memberCount (number, init 0), openClaimCount (number, init 0), animeCount (number, init 0), members (FansubMember[], init []), projects (AnimeListItem[], init []).

    loadCounts: useCallback + useEffect — Promise.all aus listGroupMembers(fansubId), listPendingMemberClaims(fansubId), getAdminFansubAnime(fansubId). Setzt memberCount, openClaimCount, animeCount, members, projects. Fehler via ApiError-Check.

    Readiness-Kriterien-Array (bewertbar, 7 Einträge aus UI-SPEC):
    1. logo — satisfied: Boolean(group.logo_url), targetTab: 'media', label: 'Logo vorhanden', hint: 'Im Medien-Tab ergänzen'
    2. banner — satisfied: Boolean(group.banner_url), targetTab: 'media', label: 'Banner vorhanden', hint: 'Im Medien-Tab ergänzen'
    3. description — satisfied: Boolean(buildFansubFactSummary(group)), targetTab: 'basic', label: 'Kurzbeschreibung vorhanden', hint: 'In Grunddaten ergänzen'
    4. members — satisfied: memberCount > 0, targetTab: 'mitglieder', label: 'Mitglieder eingetragen/geprüft', hint: 'Mitglieder prüfen'
    5. contributions — satisfied: animeCount > 0, targetTab: 'releases', label: 'Externe Mitwirkende geprüft', hint: 'In Anime & Veröffentlichungen prüfen'
    6. media — satisfied: animeCount > 0, targetTab: 'media', label: 'Medien korrekt kategorisiert', hint: 'Im Medien-Tab prüfen'
    7. preview — satisfied: true, targetTab: 'basic', label: 'Öffentliche Vorschau verfügbar', hint: 'Vorschau unten'

    Informative Prüf-Hinweis-Einträge (D-06, kein satisfied/unsatisfied, variant=info):
    8. story — label: 'Gruppengeschichte prüfen', targetTab: 'notes', hint: 'In der Gruppengeschichte ergänzen'
       BEGRÜNDUNG: FansubGroup-DTO enthält kein has_notes-Feld (Lock K — kein neuer Endpunkt). Deshalb wird die Gruppengeschichte als neutraler informativer Prüf-Hinweis mit Badge variant=info und Sprungmarke auf den Notes-Tab dargestellt, NICHT als bewertbares Kriterium mit „fehlt"-Urteil. Das entspricht dem mentalen Modell von D-06: offene Posten zur Kenntnisnahme, kein Urteil.
    9. claims — label: `Offene Claims: ${openClaimCount}`, targetTab: 'claims', hint: 'Claims einsehen'
    10. contributions-open — label: `Offene Vorschläge: ${animeCount > 0 ? '(pro Projekt verfügbar)' : '0'}`, targetTab: 'vorschlaege', hint: 'Vorschläge einsehen'
    Hinweis: Contributions-Gesamt ohne gruppen-weiten Endpunkt — vereinfacht auf animeCount-basierte Aussage (Lock K gewahrt; kein neuer Endpunkt nötig).

    Render-Struktur:
    - SectionHeader title="Veröffentlichung & Pflegezustand" description="So sieht die öffentliche Fansub-Seite für anonyme Besucher aus. Die Checkliste zeigt, was noch zu pflegen ist – sie blockiert nichts und schaltet nichts frei."
    - Hinweis-Text: "Hinweis: Die öffentliche Seite ist bereits live. Diese Übersicht ist ein Leitfaden, kein Freigabe-Schalter." (Badge variant=info oder Card)
    - Card variant=section: Überschrift „Bereitschaft", loading/error States, dann bewertbare Kriterien-Liste (Einträge 1–7) mit readinessList/readinessItem-Klassen
    - Separator (hr mit readinessSectionDivider)
    - Card variant=section: Überschrift „Offene Posten (zur Kenntnisnahme)", informative Hinweise (Einträge 8–10)
    - PublicPreviewPanel group={group} members={members} projects={projects} leaderTimeline={[]}

    Jedes bewertbare Readiness-Item (1–7): Badge variant=satisfied?'success':'warning' mit Icon Check/AlertCircle + aria-label „Status: erfüllt" / „Status: fehlt" + span label-Text + Button variant=subtle size=sm rightIcon=ArrowRight onClick=navigateToTab(item.targetTab) (nur wenn !satisfied).

    Jedes informative Item (8–10, inkl. story): Badge variant=info + Icon Info + aria-label „Information" + span label-Text + Button variant=subtle size=sm rightIcon=ArrowRight onClick=navigateToTab(item.targetTab). KEIN satisfied/unsatisfied-Urteil, KEIN aria-label „Status: fehlt" für diese Items.

    Verboten: native button/input/select/textarea. Kein onSave/onChange/onEdit-Prop. Kein iframe. Kein externer Link für Preview (D-01). Keine danger-Farbe (D-05). Kein variant=warning für das Story-Item.

    Wenn ReadinessTab zu groß wird (>400 Zeilen): ReadinessChecklist als eigene Datei auslagern analog zu RESEARCH.md Zeile 137–144.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/admin/fansubs/\\[id\\]/edit/ReadinessTab.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    - ReadinessTab.tsx existiert, ≤ 450 Zeilen, TypeScript kompiliert fehlerfrei
    - ReadinessTab.test.tsx-Tests grün (alle sechs Req-Cases)
    - Nur @/components/ui-Primitive — ESLint-Check: `cd frontend && npx eslint src/app/admin/fansubs/\\[id\\]/edit/ReadinessTab.tsx --no-warn-ignored 2>&1 | grep -i "restricted\|button\|input\|select"` liefert keine Treffer
    - 77-VALIDATION.md: `wave_0_complete: true` setzen, sobald die Wave-0-Tests (Plan 01) grün sind; `nyquist_compliant: true` setzen, sobald die volle Vitest-Suite grün ist.
  </done>
</task>

<task type="auto">
  <name>Task 2: PublicPreviewPanel.tsx implementieren + FansubEdit.module.css erweitern</name>
  <files>
    frontend/src/app/admin/fansubs/[id]/edit/PublicPreviewPanel.tsx
    frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css
  </files>
  <read_first>
    - frontend/src/components/fansubs/FansubProfileTabs.tsx (vollständig — Props-Schnittstelle Zeilen 14–19, read-only-Natur der Komponente, window.history.replaceState-Tab-Navigation ist workspace-unabhängig)
    - frontend/src/components/fansubs/GroupLeaderTimeline.tsx (vollständig — Props-Schnittstelle, ~47 Zeilen)
    - frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css (alle bestehenden Klassen lesen — neue .readiness*-Klassen ans Ende anfügen, keine bestehenden überschreiben)
    - .planning/phases/77-leader-workspace-public-preview-readiness/77-PATTERNS.md (PublicPreviewPanel.tsx-Sektion Zeilen 181–238, CSS-Sektion Zeilen 404–441)
    - .planning/phases/77-leader-workspace-public-preview-readiness/77-UI-SPEC.md (Zustände Zeilen 222–231, Copywriting-Contract: aria-label, Fallback-Hinweis-Text)
  </read_first>
  <action>
    Neue Datei PublicPreviewPanel.tsx erstellen.

    Datei-Kopf: `'use client'`. Imports: Badge, Card, EmptyState aus @/components/ui; FansubProfileTabs aus @/components/fansubs/FansubProfileTabs; GroupLeaderTimeline aus @/components/fansubs/GroupLeaderTimeline; type AnimeListItem aus @/types/anime; type FansubGroup, type FansubMember aus @/types/fansub; type PublicFansubLeaderEntry aus @/types/fansub (falls vorhanden — andernfalls inline interface).

    Props: `interface PublicPreviewPanelProps { group: FansubGroup; members: FansubMember[]; projects: AnimeListItem[]; leaderTimeline: PublicFansubLeaderEntry[] }`. Kein onSave/onChange/onEdit-Prop (D-01 read-only-Vertrag).

    Export: `export function PublicPreviewPanel({ group, members, projects, leaderTimeline }: PublicPreviewPanelProps)`.

    TODO-Kommentar (D-01 Upgrade-Pfad, Phase-73-Upgrade): Direkt vor dem return-Statement diesen Kommentar einfügen:
    // TODO(Phase 73): Sobald Phase 73 ausgeführt ist, FansubProfileTabs und GroupLeaderTimeline
    // durch die echten Section-Komponenten (FansubHeroSection, FansubStorySection, FansubHighlightsSection etc.)
    // aus frontend/src/app/fansubs/[slug]/sections/ ersetzen (D-01).
    // Dieser Fallback ist ein dokumentierter Übergangszustand bis Phase 73 liefert.

    Render: Card variant=flat aria-label="Öffentliche Vorschau (schreibgeschützt)":
    - Badge variant=info: „Vorschau im Übergangsmodus: Bis die neue öffentliche Seite ausgerollt ist, zeigt diese Vorschau die bestehende Darstellung der Fansub-Seite." (exakter String aus UI-SPEC Copywriting-Contract)
    - GroupLeaderTimeline entries={leaderTimeline} (leere Array-Default wenn leer)
    - FansubProfileTabs group={group} members={members} projects={projects}
    - Wenn members.length === 0 und projects.length === 0: EmptyState heading="Noch keine öffentlichen Inhalte" body="Sobald Logo, Beschreibung und Story gepflegt sind, erscheinen sie hier in der Besucher-Vorschau. Nutze die Sprungmarken oben, um zu starten."

    Wichtig: Keine Buttons mit Schreib-Aktionen, kein onSave-Callback, kein externer Link für Preview, kein iframe (D-01).

    FansubEdit.module.css erweitern: Neue .readiness*-Klassen ans Ende der Datei anfügen (NICHT bestehende Klassen verändern). Klassen aus PATTERNS.md Zeilen 410–441:
    - .readinessTabRoot: display grid, gap var(--space-6)
    - .readinessList: display grid, gap var(--space-3)
    - .readinessItem: display flex, align-items center, gap var(--space-2)
    - .readinessItemLabel: flex 1, font-size var(--text-body), color var(--text-primary)
    - .readinessSectionDivider: margin-block var(--space-4), border none, border-top 1px solid color-mix(in srgb, var(--text-secondary) 14%, transparent)

    Keine Hardcode-Pixelwerte — ausschließlich CSS-Custom-Properties (var(--space-*), var(--text-*), var(--text-primary)).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/admin/fansubs/\\[id\\]/edit/ 2>&1 | tail -20</automated>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - PublicPreviewPanel.tsx enthält `'use client'` am Anfang der Datei
    - PublicPreviewPanel.tsx rendert FansubProfileTabs und GroupLeaderTimeline read-only (keine onChange/onSubmit-Props werden übergeben)
    - PublicPreviewPanel.tsx enthält kein `<iframe`
    - Fallback-Hinweis nutzt Badge variant="info" mit deutschem Text und korrekten Umlauten
    - PublicPreviewPanel.tsx enthält TODO(Phase 73)-Kommentar, der das Ersetzen von FansubProfileTabs/GroupLeaderTimeline durch die echten Phase-73-Section-Komponenten nach deren Ausführung verankert (D-01)
    - FansubEdit.module.css enthält die neuen Readiness-/Preview-Klassen: .readinessTabRoot, .readinessList, .readinessItem, .readinessItemLabel, .readinessSectionDivider
    - TypeScript-Compile: `cd frontend && npx tsc --noEmit` ohne neue Fehler
    - `cd frontend && npx vitest run src/app/admin/fansubs/\\[id\\]/edit/` — alle Tests grün
  </acceptance_criteria>
  <done>
    - PublicPreviewPanel.tsx existiert, ≤ 200 Zeilen, TypeScript kompiliert fehlerfrei
    - PublicPreviewPanel.tsx enthält TODO(Phase 73)-Kommentar als dokumentierten Upgrade-Pfad (D-01)
    - FansubEdit.module.css enthält .readinessTabRoot, .readinessList, .readinessItem, .readinessItemLabel, .readinessSectionDivider — alle ohne Hardcode-Pixelwerte
    - Alle Tests in ReadinessTab.test.tsx grün (Plan 01 RED-Phase aufgelöst)
    - Alle bestehenden Tests im Verzeichnis weiterhin grün
    - TypeScript-Compile fehlerfrei
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Grenze | Beschreibung |
|--------|--------------|
| Browser → API | ReadinessTab ruft drei bestehende Admin-Endpunkte auf (authn durch FansubEditAccessGate bereits gesichert) |
| Preview-Rendering | PublicPreviewPanel rendert FansubProfileTabs — bestehende Komponente, keine neue Datenquelle |

## STRIDE Threat Register

| Threat ID | Kategorie | Komponente | Disposition | Mitigationsplan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-02-EoP | Elevation of Privilege | ReadinessTab — Tab-Zugang via URL ?tab=readiness | mitigate | Tab-Gating per canUseMainTab("readiness") in page.tsx (Plan 03); resolveMainTabForAccess fällt auf erlaubten Tab zurück wenn Capability fehlt [VERIFIED: page.tsx Zeilen 261–268] |
| T-77-03-ID | Information Disclosure | PublicPreviewPanel zeigt Gruppenbasisdaten | accept | FansubProfileTabs + GroupLeaderTimeline rendern nur Daten die im group-State (getFansubByID) bereits vorhanden sind; Assumption A1 aus RESEARCH.md — öffentliche Endpunkte geben keine nicht-öffentlichen Daten zurück |
| T-77-04-EoP | Elevation of Privilege | Sprungmarken-Navigation ?tab=claims | accept | Sprungmarken sind URL-Änderungen; resolveMainTabForAccess prüft Capability für jeden Tab-Zugriff; keine Capability-Eskalation durch Sprungmarken möglich [VERIFIED] |
| T-77-SC | Tampering | npm install | accept | Keine neuen Pakete installiert (RESEARCH.md bestätigt) |
</threat_model>

<verification>
cd frontend && npx vitest run src/app/admin/fansubs/\\[id\\]/edit/ReadinessTab.test.tsx 2>&1 | tail -5
Erwartetes Ergebnis: alle Tests grün (0 failed).

cd frontend && npx vitest run src/app/admin/fansubs/\\[id\\]/edit/ 2>&1 | tail -5
Erwartetes Ergebnis: alle Tests grün.

cd frontend && npx tsc --noEmit 2>&1 | head -20
Erwartetes Ergebnis: keine neuen TypeScript-Fehler.

powershell -Command "Get-Content 'frontend/src/app/admin/fansubs/[id]/edit/ReadinessTab.tsx' | Measure-Object -Line | Select-Object Lines"
Erwartetes Ergebnis: ≤ 450 Zeilen.

powershell -Command "Get-Content 'frontend/src/app/admin/fansubs/[id]/edit/PublicPreviewPanel.tsx' | Measure-Object -Line | Select-Object Lines"
Erwartetes Ergebnis: ≤ 450 Zeilen.
</verification>

<success_criteria>
- ReadinessTab.tsx: exportiert ReadinessTab-Komponente, ≤ 450 Zeilen, alle sechs Req-Cases aus Plan 01 grün
- PublicPreviewPanel.tsx: exportiert PublicPreviewPanel, rendert FansubProfileTabs + GroupLeaderTimeline mit Fallback-Badge, keine Schreib-Aktionen, enthält TODO(Phase 73)-Upgrade-Kommentar (D-01)
- FansubEdit.module.css: enthält .readinessTabRoot, .readinessList, .readinessItem, .readinessItemLabel, .readinessSectionDivider mit var(--space-*)-Tokens
- Alle bestehenden Tests in frontend/src/app/admin/fansubs/ bleiben grün
- TypeScript-Build: `cd frontend && npx tsc --noEmit` ohne neue Fehler
</success_criteria>

<output>
Erstelle `.planning/phases/77-leader-workspace-public-preview-readiness/77-02-SUMMARY.md` wenn fertig
</output>
