---
phase: 115-globale-suche-postgres-fts
plan: 05
type: execute
wave: 4
depends_on: ["115-04"]
files_modified:
  - frontend/src/types/search.ts
  - frontend/src/lib/api.ts
  - frontend/src/app/suche/useDebouncedSearch.ts
  - frontend/src/app/suche/useDebouncedSearch.test.tsx
  - frontend/src/components/layout/AppShell.tsx
autonomous: true
requirements: [D-08, D-11]
must_haves:
  truths:
    - "D-08: getSearch/getSearchSuggestions bauen die Query nur aus gesetzten Params (URL = Source of Truth) und reichen ein AbortController-signal durch"
    - "D-08: useDebouncedSearch debounced 250ms, bricht veraltete Requests via AbortController ab und spiegelt q/type/Filter/page in useSearchParams"
    - "D-11(1)/UI-SPEC: Der 'Suche'-Nav-Eintrag ist in BEIDEN Shell-Gruppen aktiv (anonym + eingeloggt), href '/suche', ohne badge/disabled"
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "getSearch + getSearchSuggestions + buildSearchQuery"
      contains: "getSearch"
    - path: "frontend/src/app/suche/useDebouncedSearch.ts"
      provides: "Debounce + AbortController + searchParams-Sync Hook"
    - path: "frontend/src/components/layout/AppShell.tsx"
      provides: "Aktiver Suche-Nav-Eintrag in beiden Nav-Gruppen"
  key_links:
    - from: "frontend/src/app/suche/useDebouncedSearch.ts"
      to: "getSearch (api.ts)"
      via: "AbortController-signal + debounced fetch"
      pattern: "AbortController"
    - from: "frontend/src/components/layout/AppShell.tsx"
      to: "/suche"
      via: "AppShellNavGroups + AppShellAnonNavGroups Eintrag"
      pattern: "/suche"
---

<objective>
Frontend-Datenschicht + Navigations-Aktivierung der Suche: die typisierten API-Helfer
(`getSearch`/`getSearchSuggestions`), der wiederverwendbare `useDebouncedSearch`-Hook (Debounce +
Request-Abbruch + URL-Suchzustand) und die Aktivierung des toten „Suche"-Nav-Eintrags in BEIDEN
AppShell-Gruppen (anonym + eingeloggt, D-11(1)).

Purpose: Contract-first-Grundlage, auf der die Such-UI (Plan 115-06/07) aufsetzt — die Komponenten
erhalten Hook + API fertig und müssen den Datenfluss nicht neu erfinden.
Output: types/search.ts + api.ts-Helfer + useDebouncedSearch(.test) + AppShell-Nav.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-globale-suche-postgres-fts/115-UI-SPEC.md
@.planning/phases/115-globale-suche-postgres-fts/115-PATTERNS.md
@.planning/phases/115-globale-suche-postgres-fts/115-04-SUMMARY.md

<interfaces>
API-Analog — frontend/src/lib/api.ts:
  getAnimeList+buildQuery (:483-499, :1420-1454), getFansubList+buildFansubListQuery (:470-475, :520-528).
  Muster: nur gesetzte Params via URLSearchParams; getApiBaseUrl(); ApiError bei !response.ok;
  ÖFFENTLICHER fetch (kein authorizedFetch — Suche ist öffentlich); AbortController-signal durchreichen.
  Response-Form = PaginatedSearchResponse aus openapi.yaml (Plan 04). Domain-Typen nach
  frontend/src/types/ (Konvention, vgl. AnimeListParams).

AppShell — frontend/src/components/layout/AppShell.tsx:
  AppShellAnonNavGroups (~:186-191): toter Eintrag { label:'Suche', icon:<Compass/>, disabled:true, badge:'bald' }
    → { label:'Suche', href:'/suche', icon:<Search size={17}/>, current: isCurrent(currentPath,'/suche') }
  AppShellNavGroups publicItems (~:120-124): KEIN Suche-Eintrag → neuen ergänzen (gleiche Shape).
  `Search` aus lucide-react importieren (ersetzt Compass-Platzhalter). Nav-Item-Shape unverändert:
  { label, href?, icon, current?, disabled?, badge? }.

useDebouncedSearch — KEIN Analog (RESEARCH Punkt 9). Neu: 250ms Debounce + AbortController
  (Abbruch veralteter Requests) + useSearchParams-Sync (q/type/Filter/page). Vitest Pflicht.
  Interaction Contract (UI-SPEC): Vorschläge ab ≥2 Zeichen; jeder Tastendruck bricht vorherigen ab.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Suchtypen + api.ts-Helfer (getSearch / getSearchSuggestions)</name>
  <files>frontend/src/types/search.ts, frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts (getAnimeList+buildQuery :483-499,:1420-1454; getFansubList :470-475)
    - frontend/src/types/ (bestehende Domain-Typ-Konvention, z. B. AnimeListParams)
    - shared/contracts/openapi.yaml (PaginatedSearchResponse/SearchResultItem aus Plan 04)
  </read_first>
  <behavior>
    - buildSearchQuery hängt nur gesetzte Params an (leere/undefined werden ausgelassen)
    - getSearch(params, signal) ruft GET /api/v1/search mit dem AbortController-signal und wirft ApiError bei !ok
    - getSearchSuggestions(q, signal) ruft GET /api/v1/search/suggestions und liefert gruppierte Vorschläge
  </behavior>
  <action>
    Erstelle frontend/src/types/search.ts: `SearchParams` (q, type, year_from, year_to, genre, tag,
    format, status, fansub_group, page, per_page, sort), `SearchResultItem` (Diskriminator
    anime|fansub + Felder gemäß openapi.yaml), `SearchResponse` (data + meta:PaginationMeta-Form),
    `SearchSuggestion`. Ergänze in api.ts `buildSearchQuery(params): string` (URLSearchParams, nur
    gesetzte), `getSearch(params, signal?)` und `getSearchSuggestions(q, signal?)` exakt nach dem
    getAnimeList-Muster (getApiBaseUrl, öffentlicher fetch mit `signal`, ApiError bei !ok). Deutsche
    Fehlertexte mit korrekten Umlauten, wo user-facing.
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function getSearch\|export async function getSearch" frontend/src/lib/api.ts` ≥ 1
    - `grep -c "getSearchSuggestions" frontend/src/lib/api.ts` ≥ 1
    - `grep -c "signal" frontend/src/lib/api.ts` zeigt signal-Durchreichung in getSearch (AbortController)
    - `cd frontend && npm run typecheck` fehlerfrei
  </acceptance_criteria>
  <done>Typisierte, signal-fähige Such-API-Helfer bauen die Query nur aus gesetzten Params.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useDebouncedSearch-Hook (250ms + AbortController + searchParams-Sync) + Test</name>
  <files>frontend/src/app/suche/useDebouncedSearch.ts, frontend/src/app/suche/useDebouncedSearch.test.tsx</files>
  <read_first>
    - frontend/src/app/anime/page.tsx (searchParams-Auflösung 17-58 — URL-Zustandsmuster)
    - frontend/vitest.config.ts (globals:true, jsdom pro Datei via @vitest-environment)
    - frontend/src/lib/api.ts (getSearch aus Task 1)
  </read_first>
  <behavior>
    - Eine Folge schneller Tastendrücke löst nur EINEN Request nach 250ms Stille aus (Debounce)
    - Ein neuer Aufruf bricht den vorherigen laufenden Request via AbortController ab (kein veraltetes Ergebnis)
    - q/type/Filter/page werden in useSearchParams gespiegelt; Reload stellt den Zustand her
    - Bei q-Länge < 2 wird kein Suggestions-Request ausgelöst
  </behavior>
  <action>
    Erstelle frontend/src/app/suche/useDebouncedSearch.ts: kapselt Debounce (250ms), AbortController
    (Abbruch veralteter Requests), und useSearchParams-Sync (q/type/Filter/page → URL als Source of
    Truth). Der Hook ruft getSearch/getSearchSuggestions (Task 1) und gibt Ergebnis/Loading/Error +
    Setter zurück. Mindestlänge q≥2 für Suggestions clientseitig respektieren (Server erzwingt es
    zusätzlich). Erstelle useDebouncedSearch.test.tsx (Vitest, @vitest-environment jsdom): Tests für
    Debounce-Zusammenfassung, Abbruch des vorherigen Requests, searchParams-Sync, <2-Zeichen-Guard.
    Datei ≤450 Zeilen.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/suche/useDebouncedSearch.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/app/suche/useDebouncedSearch.test.tsx` grün
    - `grep -c "AbortController" frontend/src/app/suche/useDebouncedSearch.ts` ≥ 1
    - `grep -c "useSearchParams\|URLSearchParams" frontend/src/app/suche/useDebouncedSearch.ts` ≥ 1
    - Test deckt explizit Abbruch UND Debounce UND <2-Zeichen-Guard ab
  </acceptance_criteria>
  <done>Wiederverwendbarer Hook debounced, bricht ab und synchronisiert die URL; per Vitest bewiesen.</done>
</task>

<task type="auto">
  <name>Task 3: AppShell-Nav-Aktivierung in beiden Gruppen (anonym + eingeloggt)</name>
  <files>frontend/src/components/layout/AppShell.tsx</files>
  <read_first>
    - frontend/src/components/layout/AppShell.tsx (AppShellAnonNavGroups ~:186-191, AppShellNavGroups ~:120-124, isCurrent-Helper)
    - .planning/phases/115-globale-suche-postgres-fts/115-UI-SPEC.md (Navigation Activation Contract)
  </read_first>
  <action>
    Aktiviere in AppShellAnonNavGroups den toten „Suche"-Eintrag: `disabled`+`badge` entfernen,
    `href:'/suche'` + `current: isCurrent(currentPath,'/suche')` setzen, Icon auf `Search` (size 17)
    umstellen. Ergänze in AppShellNavGroups (publicItems) einen NEUEN „Suche"-Eintrag mit gleicher
    Shape (`href:'/suche'`, `current: isCurrent(currentPath,'/suche')`, Icon `Search`). Importiere
    `Search` aus lucide-react (ersetzt Compass-Platzhalter, falls nicht mehr genutzt). Nav-Item-Shape
    unverändert. Label „Suche" (korrekte Umlaute nicht nötig, aber deutsche Copy). Datei bleibt ≤450
    Zeilen (aktuell 447 — ggf. Icon-Import ohne Zeilenzuwachs oder minimalen Split beachten).
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'/suche'" frontend/src/components/layout/AppShell.tsx` ≥ 2 (beide Nav-Gruppen)
    - `grep -c "badge: 'bald'" frontend/src/components/layout/AppShell.tsx` == 0 (toter Badge entfernt)
    - `wc -l frontend/src/components/layout/AppShell.tsx` ≤ 450
    - `cd frontend && npm run typecheck && npm run lint` fehlerfrei
  </acceptance_criteria>
  <done>„Suche" ist in anonymer UND eingeloggter Nav aktiv und verlinkt auf /suche.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/v1/search | Client-seitige Query-Bildung; Backend validiert/filtert (V4/V5 in Plan 04/03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-05-01 | Denial of Service | ungedrosselte Requests bei jedem Tastendruck | mitigate | 250ms Debounce + AbortController bricht veraltete Requests ab (D-08) |
| T-115-05-02 | Information Disclosure | Client vertraut Rohdaten | accept | Sichtbarkeit wird serverseitig erzwungen (Plan 03/04); Client rendert nur gelieferte Items |
| T-115-05-03 | Tampering | XSS über Suchbegriff | mitigate | React escaped Textknoten by default; kein dangerouslySetInnerHTML für q/Ergebnistext (Prüfung in Plan 06/07) |
| T-115-05-SC | Tampering | npm-Installs | mitigate | Keine neuen npm-Pakete; nur vorhandene Deps (Next 16, lucide-react) |
</threat_model>

<verification>
- `cd frontend && npm run typecheck && npm run lint` fehlerfrei.
- `cd frontend && npx vitest run src/app/suche/useDebouncedSearch.test.tsx` grün.
- AppShell zeigt „Suche" in beiden Nav-Gruppen (Grep-Gate); kein `badge:'bald'` mehr.
</verification>

<success_criteria>
Typisierte, signal-fähige Such-API-Helfer + ein getesteter Debounce/Abbruch/URL-Sync-Hook + der
„Suche"-Nav-Eintrag aktiv in anonymer und eingeloggter Shell. Keine nativen UI-Elemente eingeführt.
</success_criteria>

<output>
Create `.planning/phases/115-globale-suche-postgres-fts/115-05-SUMMARY.md` when done.
</output>
