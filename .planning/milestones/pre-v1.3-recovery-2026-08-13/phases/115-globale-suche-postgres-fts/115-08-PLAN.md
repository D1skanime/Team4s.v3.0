---
phase: 115-globale-suche-postgres-fts
plan: 08
type: execute
wave: 7
depends_on: ["115-04", "115-07"]
files_modified:
  - docs/performance/anime-search-query-plan-tracking.md
  - docs/search/meilisearch-dock-point.md
autonomous: false
requirements: [D-09, D-10, D-12]
must_haves:
  truths:
    - "D-09: EXPLAIN ANALYZE der Suchqueries belegt Index-Nutzung (Bitmap/Index Scan, kein Seq Scan) und wird als Baseline dokumentiert"
    - "D-12: Nach Code-Fix (Plan 01) + Re-Import findet die Live-Suche 'Koe no Katachi'/'Eiga Koe no Katachi' → 'A Silent Voice'"
    - "D-11/D-04/D-05: Live-Smoke belegt dissolved erscheint, disabled ausgeschlossen, Tippfehler/Alias/Akzent-Treffer, exakter Treffer nie durch Popularität verdrängt"
    - "D-10: Meilisearch-Andockpunkt ist DOKUMENTIERT (kein Code): Sync aus PostgreSQL, Erhalt von Sichtbarkeit, Wechsel-Messwerte"
  artifacts:
    - path: "docs/performance/anime-search-query-plan-tracking.md"
      provides: "EXPLAIN-ANALYZE-Baseline für /api/v1/search"
    - path: "docs/search/meilisearch-dock-point.md"
      provides: "Meilisearch-Provider-Andockpunkt-Dokumentation (D-10)"
  key_links:
    - from: "scripts/smoke-search.ps1"
      to: "/api/v1/search"
      via: "Keycloak-Direct-Grant Live-Smoke (D-04/D-05/D-11/D-12)"
      pattern: "api/v1/search"
---

<objective>
Absicherung + Abschluss: Performance-Verifikation der Suche per `EXPLAIN ANALYZE` (D-09),
Datenkorrektur per Re-Import für den D-12-Bestand, vollständiger Live-Smoke gegen das Docker-Backend
(D-04/D-05/D-11/D-12 End-to-End) und die reine DOKUMENTATION des späteren Meilisearch-Andockpunkts
(D-10, kein Code). Dies ist die Sub-Phase D aus RESEARCH Punkt 12.

Purpose: Die datenmodell-nahen Fähigkeiten (Persistenz, Ranking, Sichtbarkeit) sind mit dem
bestehenden Unit-Harness NICHT beweisbar (VALIDATION.md Manual-Only) — hier werden sie live verifiziert.
Output: Perf-Baseline-Doku + Meilisearch-Doku + bestätigter Live-UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-globale-suche-postgres-fts/115-RESEARCH.md
@.planning/phases/115-globale-suche-postgres-fts/115-VALIDATION.md
@.planning/phases/115-globale-suche-postgres-fts/115-04-SUMMARY.md
@.planning/phases/115-globale-suche-postgres-fts/115-07-SUMMARY.md

<interfaces>
Perf-Doku — docs/performance/anime-search-query-plan-tracking.md (Baseline 2026-03-03, ~13.351 Zeilen).
Smoke — scripts/smoke-search.ps1 (Plan 04): D-12/D-04/D-05/D-11-Assertions.
D-12-Datenkorrektur (Runtime State Inventory): reiner Code-Fix macht NEUE Speichervorgänge korrekt,
  repariert NICHT rückwirkend Anime, deren Romaji/Haupttitel nie in anime_titles landete →
  betroffene Test-Anime NEU importieren/anreichern (disponible Testdaten, kein Zwangs-Backfill).
D-10 (nur Doku): wo ein Meilisearch-Provider hinter dem SearchProvider-Interface (Plan 03) andockt;
  Sync aus PostgreSQL; Erhalt von Sichtbarkeit/Berechtigungen; Wechsel-Messwerte (Suchlatenz,
  #Dokumente, Tippfehlerqualität, Facetten-Kosten, PG-Ressourcen, Ranking-Aufwand).
Live-Umgebung: Container-Rebuild `docker compose up -d --build team4sv30-backend`; realer Port via
  `docker ps`; Bash-Sandbox erreicht Host-Ports NICHT → Live-Schritte im echten Terminal (Checkpoint).
Perf-Hinweis (Pitfall 6): gefühlte Langsamkeit NICHT vorschnell DB/Docker zuschreiben — erst prüfen,
  ob die Suchseite serielle SSR-Requests feuert und ob im Production-Build gemessen wird.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Meilisearch-Andockpunkt dokumentieren (D-10, nur Doku)</name>
  <files>docs/search/meilisearch-dock-point.md</files>
  <read_first>
    - backend/internal/models/search.go (SearchProvider-Interface aus Plan 03 — der Andockpunkt)
    - .planning/phases/115-globale-suche-postgres-fts/115-RESEARCH.md (D-10-Abschnitt)
  </read_first>
  <action>
    Erstelle docs/search/meilisearch-dock-point.md: beschreibe, wo ein späterer Meilisearch-Provider
    hinter dem `SearchProvider`-Interface (models/search.go) andockt (zweite Impl neben SearchRepository,
    keine Handler-/Frontend-Änderung); wie ein Sync aus PostgreSQL (Source of Truth, D-01) aussähe;
    wie Sichtbarkeit/Berechtigungen erhalten bleiben (dieselbe serverseitige Filterung wie Plan 03);
    und die konkreten Messwerte, die einen Wechsel/eine Ergänzung rechtfertigen (Suchlatenz,
    #Dokumente, Tippfehlerqualität, Facetten-Kosten, PG-Ressourcen, Ranking-Aufwand, gewünschte
    Search-as-you-type-Qualität). KEIN Code, keine Paket-/Service-Installation.
  </action>
  <verify>
    <automated>test -f docs/search/meilisearch-dock-point.md && grep -ci SearchProvider docs/search/meilisearch-dock-point.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/search/meilisearch-dock-point.md`
    - `grep -ci "SearchProvider" docs/search/meilisearch-dock-point.md` ≥ 1 (Andockpunkt benannt)
    - `grep -ci "Sichtbarkeit\|Messwert\|Latenz" docs/search/meilisearch-dock-point.md` ≥ 2
    - Keine Code-Datei/Migration/Paket in diesem Task angelegt (`git status` zeigt nur die .md)
  </acceptance_criteria>
  <done>Meilisearch-Andockpunkt vollständig dokumentiert, ausschließlich als Doku (D-10).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Live-UAT — D-12 Re-Import, Smoke-Suite, EXPLAIN-Baseline</name>
  <files>docs/performance/anime-search-query-plan-tracking.md, scripts/smoke-search.ps1</files>
  <action>Wende die Phasen-115-Backend-Aenderungen an (docker compose up -d --build team4sv30-backend), fuehre die D-12-Datenkorrektur per Re-Import der betroffenen Test-Anime durch, fahre scripts/smoke-search.ps1 vollstaendig gegen das Docker-Backend und ergaenze die EXPLAIN-ANALYZE-Baseline in docs/performance/anime-search-query-plan-tracking.md. Diese Schritte brauchen ein echtes Terminal (Bash-Sandbox erreicht Host-Ports nicht).</action>
  <what-built>
    D-12-Datenkorrektur per Re-Import + vollständiger Live-Smoke der Suche (Plan 04 smoke-search.ps1)
    gegen das Docker-Backend, plus EXPLAIN-ANALYZE-Perf-Baseline. Diese Schritte brauchen ein echtes
    Terminal (Bash-Sandbox erreicht Host-Ports nicht) und laufen gegen Live-Daten.
  </what-built>
  <how-to-verify>
    1. Backend-Container mit den Phasen-115-Änderungen bauen: `docker compose up -d --build team4sv30-backend`
       und realen Port via `docker ps` bestätigen; Migration 0140 als angewandt prüfen.
    2. D-12-Datenkorrektur: die betroffenen Test-Anime (z. B. „A Silent Voice") neu importieren/anreichern,
       damit Romaji/Japanisch jetzt in anime_titles landen. Danach:
       `SELECT title FROM anime_titles at JOIN title_types tt ON tt.id=at.title_type_id WHERE tt.name='romaji'`
       liefert Zeilen.
    3. Live-Smoke: `pwsh scripts/smoke-search.ps1` ausführen und bestätigen:
       - `GET /api/v1/search?q=Koe%20no%20Katachi` und `q=Eiga%20Koe%20no%20Katachi` enthalten „A Silent Voice" (D-12)
       - `q=Narotu` findet „Naruto"; `q=T4S` bzw. `q=team-4s` findet Team4s über Alias (D-04)
       - exakter Haupttitel steht auf Rang 1 trotz populärerem Teiltreffer (D-05)
       - dissolved-Gruppe erscheint; disabled-Anime fehlt ohne Admin-Token, erscheint mit Admin-Token (D-11)
       - q<2 → 400, q>100 → 400 (D-07)
    4. Perf: für die Anime-Titel- und Fansub-Alias-Query je `EXPLAIN ANALYZE` ausführen; `Bitmap Index
       Scan`/`Index Scan` auf einem 0140-Index bestätigen (kein Seq Scan auf der Suchspalte) und den
       Plan in docs/performance/anime-search-query-plan-tracking.md als datierte Baseline ergänzen.
    5. UI-Sichtprüfung auf :3000 (nach `docker restart team4sv30-frontend` + Strg+F5): Suchfeld als
       Anker, gruppierte Vorschläge, Tabs mit Trefferzahl, Filter-Chips, mobiler Drawer, Lade-/Empty-/
       Fehlerzustände, teilbarer URL-Zustand.
  </how-to-verify>
  <verify>
    <automated>MANUAL (Live-Terminal): pwsh scripts/smoke-search.ps1 besteht D-04/D-05/D-11/D-12/D-07; EXPLAIN ANALYZE zeigt Bitmap/Index Scan</automated>
  </verify>
  <resume-signal>„approved" wenn alle Live-Checks bestehen, sonst konkrete Abweichungen beschreiben (Gap-Closure).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live-Client → /api/v1/search | Öffentlicher Endpunkt unter Last (Smoke); Admin-Token nur für disabled-Override |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-08-01 | Denial of Service | teure Suchqueries live | mitigate | EXPLAIN-Nachweis der Index-Nutzung (D-09); Mindestlänge/per_page-Cap aus Plan 04 live bestätigt |
| T-115-08-02 | Information Disclosure | disabled-Leak live | mitigate | Smoke prüft: disabled fehlt ohne Admin-Token; nur mit Admin-Identität sichtbar (V4) |
| T-115-08-03 | Information Disclosure | Meilisearch-Doku enthält Secrets | accept | Reine Konzept-Doku ohne Zugangsdaten; kein Service-Setup |
| T-115-08-SC | Tampering | Re-Import/Doku | mitigate | Keine Paketinstallation; Re-Import nutzt bestehende Enrichment-Pfade (Plan 01) |
</threat_model>

<verification>
- Live-Smoke (smoke-search.ps1) besteht alle D-04/D-05/D-11/D-12/D-07-Assertions.
- EXPLAIN-ANALYZE-Baseline in der Perf-Doku zeigt Index-Nutzung (kein Seq Scan auf Suchspalte).
- Meilisearch-Doku existiert (D-10), kein Code.
- Human-Checkpoint „approved" (Live-UAT ist Pflicht — code-level reicht nicht, Memory „Gap-Fixes live UAT-en").
</verification>

<success_criteria>
Die Suche ist live verifiziert: „Koe no Katachi" → „A Silent Voice" (D-12), Tippfehler/Alias/Akzent-
Treffer (D-04), exakter Treffer nie durch Popularität verdrängt (D-05), dissolved erscheint / disabled
ausgeschlossen (D-11), Index-Nutzung per EXPLAIN belegt (D-09) — und der Meilisearch-Andockpunkt ist
dokumentiert (D-10). Human-Checkpoint bestätigt.
</success_criteria>

<output>
Create `.planning/phases/115-globale-suche-postgres-fts/115-08-SUMMARY.md` when done.
</output>
