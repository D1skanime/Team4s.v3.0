---
phase: 48-meine-gruppen-und-contributor-dashboard
plan: "01"
type: execute
wave: 1
depends_on:
  - ../47-member-profile-und-historical-identity/47-04-PLAN.md
files_modified:
  - backend/cmd/server
  - backend/internal/handlers
  - backend/internal/middleware
  - backend/internal/models
  - backend/internal/permissions
  - backend/internal/repository
  - docs
autonomous: true
requirements:
  - CONTRIBUTOR-DASHBOARD-01
must_haves:
  truths:
    - "Phase 48 must make existing group/release/media functions reachable and safe rather than rebuilding them."
    - "All contributor visibility and action state derives from canonical group membership and the permission engine."
    - "Historical credits may enrich the dashboard but may never grant rights."
---

<objective>
Do the mandatory seam analysis and establish the contributor-scoping strategy: validate the membership and capability runtime seams, decide which existing admin components can be reused safely, and define the minimal backend/frontend structure for the dashboard.

Purpose: Phase 48 should not accidentally turn into a second admin application or leak global-admin functionality.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/48-meine-gruppen-und-contributor-dashboard/48-CONTEXT.md
@.planning/phases/48-meine-gruppen-und-contributor-dashboard/48-RESEARCH.md
@frontend/src/app/admin/fansubs/[id]/edit/page.tsx
@frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNotesSection.tsx
@frontend/src/app/admin/fansubs/[id]/edit/NotesTab.tsx
@frontend/src/app/admin/fansubs/[id]/edit/ReleaseVersionMediaDrawerSummary.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyse contributor-facing reuse seams before changing routes</name>
  <read_first>
    - backend/internal/repository
    - backend/internal/permissions
    - frontend/src/lib/api.ts
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
  </read_first>
  <action>
    Perform the required Ist-Analyse and document:
    - canonical group membership and role source
    - available profile/current-user data
    - existing fansub/release/release-version/media/note endpoints
    - capability response shapes from prior phases
    - navigation/user-menu seams
    - which existing frontend sections are directly reusable, need wrapping, or are too admin-specific

    Then decide:
    - whether contributor detail should live on a new `/admin/my-groups/:id` route
    - which sections can be reused on day one
    - whether a lightweight `Meine Beteiligungen` read model is feasible without new heavy data work

    If runtime seams for membership/capabilities are missing or contradictory, stop with a blocker.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s && git diff --check</automated>
  </verify>
  <done>
    - There is a documented contributor-focused Ist-Analyse baseline.
    - Each major reused UI/API seam has an explicit reuse/wrap/block decision.
    - The contributor routing strategy is explicit before implementation starts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Define the canonical contributor dashboard read model</name>
  <read_first>
    - results of Task 1
    - backend/internal/models
    - backend/internal/repository
  </read_first>
  <action>
    Establish the minimal DTO/aggregate shape for:
    - `GET /api/me/fansub-groups`
    - contributor group detail
    - optional lightweight contributions read model

    Include at minimum:
    - group metadata
    - membership status
    - own role list
    - active from/until
    - summary counts where cheap
    - per-group capabilities

    Keep this read model focused on visibility and routing, not on recreating all domain data.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/models/... ./internal/repository/...</automated>
  </verify>
  <done>
    - The dashboard read model is explicit and capability-aware.
    - The model is designed around reuse rather than new domain invention.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add repository/service seams for contributor-scoped group and release reads</name>
  <read_first>
    - backend/internal/repository
    - backend/internal/permissions
  </read_first>
  <action>
    Introduce or extend repository/service helpers for:
    - loading the current actor's groups
    - loading contributor-safe group detail
    - loading anime/release/release-version summaries for one owned group
    - computing or attaching contributor capability sets
    - resolving coop release visibility from canonical release-version-group joins

    Keep scoping centralized so handlers do not rebuild ownership checks ad hoc.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/repository/... ./internal/permissions/...</automated>
  </verify>
  <done>
    - Repository/service seams exist for dashboard overview and contributor-safe group detail.
    - Coop-aware release scoping is centralized and testable.
  </done>
</task>

</tasks>

<verification>
Automated:
1. `cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/repository/...`
2. `cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/permissions/...`
3. `cd C:/Users/admin/Documents/Team4s && git diff --check`

Targeted:
1. Verify reused sections are classified before implementation.
2. Verify dashboard/group DTOs are built from canonical membership truth.
3. Verify no second admin stack is introduced.
</verification>

<success_criteria>
- Phase 48 starts with a documented seam and reuse analysis.
- The dashboard/group read models are explicit and minimal.
- Contributor-safe repository/service seams exist for own-group and coop-aware release reads.
</success_criteria>
