---
phase: 76-me-contributions-dashboard-registrierte-user-vorschl-ge
plan: 05
type: execute
wave: 5
depends_on:
  - 76-03
  - 76-04
files_modified:
  - frontend/src/app/me/contributions/page.tsx
  - frontend/src/components/contributions/ContributionCard.tsx
  - frontend/src/components/contributions/MyContributionsSection.tsx
  - frontend/src/components/contributions/MyProposalsSection.tsx
autonomous: false
requirements:
  - E
  - Runde 6
  - H
  - K

must_haves:
  truths:
    - "D-01: `/me/contributions` ist ein Aktions-/Arbeits-Surface (kein Sticky-Nav-Profil); bestehende `page.tsx`, `MyContributionsSection`, `MyProposalsSection`, `ContributionCard` werden erweitert/umgebaut, NICHT ersetzt"
    - "page.tsx rendert Sektionen in verbindlicher Reihenfolge: ContributionInbox → ContributionSummary → [Vorschlagen/Melden]-Button → gefilterte Mitwirkungen-Liste → gefilterte Vorschläge-Liste (D-02)"
    - "Primärer [Vorschlagen/Melden]-Button öffnet ReportModal (D-04/D-05 einziger Einstieg)"
    - "ContributionCard mode='pending' ist in Inbox verdrahtet mit onConfirm=bestätigen, onRejectWithReason=RejectReasonModal öffnen, onCorrect=ReportModal prefillType='fehler' prefillContributionId (D-08/D-09/D-10)"
    - "useMemo-Filter (ContributionFilters.applyFilters) filtert Mitwirkungen- und Vorschläge-Liste nach activeFilters-State (D-11)"
    - "getMyAnimeContributions lädt Daten einmalig; kein zweiter API-Call für Filterung (D-11)"
    - "page.tsx bleibt <450 Zeilen"
    - "Human-Verify bestätigt: Inbox, Summary, Filter, Modal, Reject-Modal im Browser funktionsfähig"
  artifacts:
    - path: "frontend/src/app/me/contributions/page.tsx"
      provides: "Vollständig umgebautes Dashboard mit allen Sektionen verdrahtet"
    - path: "frontend/src/components/contributions/ContributionCard.tsx"
      provides: "Erweiterung um onRejectWithReason + onCorrect Props"
    - path: "frontend/src/components/contributions/MyContributionsSection.tsx"
      provides: "Erweitert: nimmt gefilterte Liste entgegen statt alle Contributions"
    - path: "frontend/src/components/contributions/MyProposalsSection.tsx"
      provides: "Nimmt gefilterte Vorschläge-Liste entgegen"
  key_links:
    - from: "frontend/src/app/me/contributions/page.tsx"
      to: "frontend/src/components/contributions/ContributionInbox.tsx"
      via: "contributions={contributions} onConfirm={handleConfirm} onRejectWithReason={openRejectModal} onCorrect={openCorrectModal}"
      pattern: "ContributionInbox"
    - from: "frontend/src/app/me/contributions/page.tsx"
      to: "frontend/src/components/contributions/ContributionSummary.tsx"
      via: "contributions={contributions} activeFilters={activeFilters} onFilterChange={setActiveFilters}"
      pattern: "ContributionSummary"
    - from: "frontend/src/app/me/contributions/page.tsx"
      to: "frontend/src/lib/api.ts"
      via: "confirmAnimeContribution, rejectAnimeContributionWithReason"
      pattern: "confirmAnimeContribution|rejectAnimeContributionWithReason"
---

<objective>
Finale Verdrahtung: page.tsx wird zum voll funktionsfähigen Dashboard mit verbindlicher Sektionsreihenfolge (D-02), Bestätigen/Ablehnen/Korrigieren-Aktionen, client-seitiger Filterung und Human-Verify-Checkpoint.

Purpose: Alle in Plänen 01–04 erstellten Komponenten und Backend-Seams werden in der Seite zusammengeführt. Der Human-Verify-Checkpoint schließt die Phase ab.

Output: page.tsx vollständig umgebaut + ContributionCard erweitert + Human-Verify.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-CONTEXT.md
@.planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-UI-SPEC.md
@.planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-PATTERNS.md

<interfaces>
<!-- Aus frontend/src/app/me/contributions/page.tsx (78 Zeilen, bestehend) — zu erweitern: -->
```typescript
'use client'
import { useEffect, useState } from 'react'
import { MyContributionsSection } from '@/components/contributions/MyContributionsSection'
import { MyProposalsSection } from '@/components/contributions/MyProposalsSection'
import { Button, ErrorState, LoadingState } from '@/components/ui'
import { ApiError, getMyAnimeContributions } from '@/lib/api'
import { useAuthSession } from '@/lib/useAuthSession'
import type { MeAnimeContribution } from '@/types/contributions'
// Auth + Load-Pattern (Zeilen 18–81) bleibt strukturell erhalten
```

<!-- ContributionCard.tsx (aktuell 120 Zeilen) — neue Props: -->
```typescript
interface ContributionCardProps {
  // bestehend:
  contribution: MeAnimeContribution
  mode: 'confirmed' | 'pending' | 'proposal'
  onConfirm?: (id: number) => void
  onReject?: (id: number) => void           // bestehend
  onVisibilityChange?: (id: number, isPublic: boolean) => void
  // NEU Phase 76:
  onRejectWithReason?: (id: number) => void  // öffnet RejectReasonModal (D-09)
  onCorrect?: (id: number) => void           // öffnet ReportModal prefilled (D-10)
}
```

<!-- FilterState-Typ und applyFilters aus ContributionFilters.tsx (Plan 03): -->
```typescript
import { applyFilters, ContributionFilterState } from '@/components/contributions/ContributionFilters'
```

<!-- confirmAnimeContribution + rejectAnimeContributionWithReason aus api.ts: -->
```typescript
import { confirmAnimeContribution, rejectAnimeContributionWithReason, getMyAnimeContributions } from '@/lib/api'
```

<!-- Dashboard-Layout-Zielstruktur per UI-SPEC §Dashboard-Struktur: -->
<!-- <main> H1 "Meine Beiträge" + [Vorschlagen/Melden]-Button -->
<!--   1. ContributionInbox -->
<!--   2. ContributionSummary (activeFilters, onFilterChange) -->
<!--   3. MyContributionsSection (gefilterte confirmed+pending) -->
<!--   4. MyProposalsSection (gefilterte proposals) -->
<!-- ReportModal, RejectReasonModal als Overlays -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: page.tsx + ContributionCard + MyContributionsSection + MyProposalsSection verdrahten</name>
  <files>
    frontend/src/app/me/contributions/page.tsx,
    frontend/src/components/contributions/ContributionCard.tsx,
    frontend/src/components/contributions/MyContributionsSection.tsx,
    frontend/src/components/contributions/MyProposalsSection.tsx
  </files>
  <read_first>
    - frontend/src/app/me/contributions/page.tsx (vollständig; alle 78 Zeilen; bestehender Auth+Load-Pattern)
    - frontend/src/components/contributions/ContributionCard.tsx (vollständig; bestehende Props-Interface; mode="pending"-Block; bestehende Aktions-Buttons Zeilen 104–117)
    - frontend/src/components/contributions/MyContributionsSection.tsx (vollständig; 54 Zeilen; Props-Interface)
    - frontend/src/components/contributions/MyProposalsSection.tsx (vollständig; 275 Zeilen; Props-Interface; Status-Filter)
    - .planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-PATTERNS.md §page.tsx (Layout-Erweiterung + Auth+Lade-Pattern + Fehlerbehandlung)
    - .planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-PATTERNS.md §ContributionCard (Props-Erweiterungs-Pattern + Aktions-Buttons für D-08/D-09/D-10)
    - .planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-UI-SPEC.md §Zustände & Interaktion (vollständig)
  </read_first>
  <behavior>
    - ContributionCard.tsx: Ergänze Props `onRejectWithReason?: (id: number) => void` und `onCorrect?: (id: number) => void`; im mode="pending"-Aktions-Block (Zeilen 104–117): button „Das war ich" bleibt als `Button variant="success"` mit `onConfirm`; ersetze/ergänze `onReject` durch `onRejectWithReason` für button „Das war ich nicht" (`variant="danger"`); füge button „Details korrigieren" hinzu wenn `onCorrect` vorhanden (`variant="secondary" size="sm"`); Abstand zwischen Confirm/Reject mindestens `--space-2` (kein Fehlklick, UI-SPEC)
    - MyContributionsSection.tsx: Props: nimm `contributions: MeAnimeContribution[]` als fertiger Array (gefiltert von page.tsx); entferne eigene Filterung falls vorhanden; nutze Contributions direkt
    - MyProposalsSection.tsx: Props-Erweiterung für gefilterte Liste; kein Änderungsbedarf an der Filter-Logik falls sie schon Proposals filtert
    - page.tsx: Erweitere bestehende Datei (78 Zeilen → ca. 200 Zeilen; bleibt <450): (1) Zusatz-State: `activeFilters: ContributionFilterState`, `rejectModalOpenId: number | null`, `reportModalOpen: boolean`, `reportPrefillType: SuggestionType | null`, `reportPrefillId: number | null`; (2) useMemo `filteredContributions` und `filteredProposals` via `applyFilters`; (3) Handler: `handleConfirm(id)` = `confirmAnimeContribution(id)` + reload; `openRejectModal(id)` = `setRejectModalOpenId(id)`; `handleRejectWithReason(id, reason)` = `rejectAnimeContributionWithReason(id, reason)` + reload; `openCorrectModal(id)` = `setReportPrefillType('fehler')` + `setReportPrefillId(id)` + `setReportModalOpen(true)`; (4) Layout: `<main>` mit H1 „Meine Beiträge" + `Button variant="primary" size="md"` „Vorschlagen / Melden" (aria-label="Vorschlagen oder melden öffnen") → `setReportModalOpen(true)`; dann in verbindlicher Reihenfolge D-02: ContributionInbox → ContributionSummary → MyContributionsSection(filtered) → MyProposalsSection(filtered); (5) Overlays: `<RejectReasonModal open={rejectModalOpenId !== null} contributionId={rejectModalOpenId} onClose={...} onConfirm={handleRejectWithReason} />` und `<ReportModal open={reportModalOpen} onClose={...} onSuccess={reload} prefillType={reportPrefillType} prefillContributionId={reportPrefillId} />`; (6) Spacing-Wrapper: `<div style={{ display: 'flex', flexDirection: 'column', gap: 'var(--space-6)' }}>` für die drei Dashboard-Sektionen (UI-SPEC §Spacing Scale —space-6 = 32px für Block-Abstand)
    - Alle Imports typsicher; keine nicht-verwendeten Imports; CLAUDE.md-Check: page.tsx ≤450 Zeilen
  </behavior>
  <action>Erweitere page.tsx: State für Overlays + activeFilters, useMemo-Filter, Handler-Funktionen, Layout per D-02 Reihenfolge, Overlays ReportModal + RejectReasonModal. Erweitere ContributionCard um onRejectWithReason + onCorrect-Props mit korrekten Button-Varianten. Passe MyContributionsSection/MyProposalsSection für gefilterte Eingabe an.</action>
  <verify>
    <automated>cd frontend && npm test 2>&1 | tail -20</automated>
  </verify>
  <done>Alle Frontend-Tests grün (inkl. ContributionInbox + ContributionSummary aus Plan 01); page.tsx ≤450 Zeilen; TypeScript fehlerfrei; Layout-Reihenfolge D-02 eingehalten; ReportModal und RejectReasonModal als Overlays verdrahtet</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 76 ist vollständig implementiert: Dashboard `/me/contributions` mit Klärungs-Inbox (D-03), Summary-Aggregat + klickbaren Stat-Chip-Filtern (D-12), unified „Vorschlagen / Melden"-Modal (D-05/D-06) mit Typen Fehler/Story/Medien/Contribution/Claim, Pflicht-Begründungs-Modal bei „Das war ich nicht" (D-09), korrektem german copy (C3), Primitives-only UI (C1/C2).

    Backend: Migration 0097 (member_suggestions + member_reason auf anime_contributions), Reject-Endpoint mit Pflicht-member_reason, Suggestion-Endpoints POST/GET /me/suggestions, audit_logs-Eintrag je Submission.
  </what-built>
  <how-to-verify>
    **Voraussetzung:** Dev-Server :3000 läuft (`npm run dev` im frontend-Verzeichnis). Docker-Backend mit Migration 0097 appliziert (`cd backend && go run ./cmd/migrate/main.go up`).

    1. Navigiere zu `http://localhost:3000/me/contributions` (eingeloggt).

    2. **Dashboard-Layout (D-02):** Prüfe Reihenfolge: (a) „Offene Aktionen"-Karte zuoberst; (b) „Überblick & Filter"-Karte mit Stat-Chips darunter; (c) „Vorschlagen / Melden"-Button sichtbar; (d) Mitwirkungen-Liste und Vorschläge-Liste darunter.

    3. **Inbox (D-03):** Falls keine pending/disputed Contributions vorhanden → EmptyState „Keine offenen Aktionen" erscheint. Falls vorhanden: Bestätige-/Ablehnen-Buttons sichtbar. Klicke „Das war ich nicht" → Reject-Modal öffnet sich mit Pflicht-Textarea.

    4. **Reject-Modal (D-09):** Sende-Button ist deaktiviert, solange Textarea leer. Tippe weniger als 5 Zeichen → Senden immer noch deaktiviert. Tippe ≥5 Zeichen → Button aktiv. Sende → Toast „Widerspruch wurde gespeichert." erscheint.

    5. **Stat-Chip-Filter (D-12):** Klicke einen Status-Chip → Liste wird gefiltert. Erneuter Klick auf denselben Chip → Filter aufgehoben. Klicke „Filter zurücksetzen" wenn mehrere aktiv.

    6. **Melde-Modal (D-05/D-06):** Klicke „Vorschlagen / Melden" → Modal öffnet Typ-Auswahl. Wähle „Fehler / Korrektur melden" → Ziel-Select und Textarea erscheinen. Fülle aus und sende → Toast „Vorschlag zur Prüfung gesendet." Prüfe, dass Claim-Typ nur einen Hinweis + Weiterleitungs-Link zeigt (kein Formular).

    7. **UI-Primitive-Check (C1/C2):** Kein Eingabefeld sieht nach nativem Browser-Standard-Select aus; alle Buttons haben einheitliches Design. VisibilityDropdown ist Migration auf Select-Primitive erkennbar (einheitliches Aussehen).

    8. **Umlaut-Check (C3):** Titel „Meine Beiträge", „Überblick & Filter", „Offene Aktionen" etc. korrekt dargestellt (kein „Ubersicht", „Offene Aktionen" etc.).
  </how-to-verify>
  <resume-signal>Tippe "approved" wenn alle 8 Prüfpunkte bestanden, oder beschreibe konkrete Probleme.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /me/contributions | Authenticated route; useAuthSession Gate |
| page.tsx useMemo-Filter | Rein client-seitig; kein vertrauenswürdiger Sicherheits-Boundary — Zugriffssteuerung liegt im Backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-05-01 | Tampering | handleConfirm/handleRejectWithReason | mitigate | Backend-Handler prüft Ownership via `authorizeAnimeContributionOwner`; Frontend-State-Manipulation ändert nur die UI, nicht die DB |
| T-76-05-02 | Information Disclosure | Inbox-Render member_reason | mitigate | member_reason aus getMyAnimeContributions-Response wird nur im eigenen Dashboard angezeigt; kein Cross-User-Leak möglich da API Me-Endpoint ist |
| T-76-05-03 | Tampering | openCorrectModal prefillContributionId | accept | prefillContributionId ist nur UI-State für Vorausfüllung; Backend-Validierung im Suggestion-Submit prüft target_id unabhängig |
| T-76-05-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Pakete |
</threat_model>

<verification>
Nach Plan 05:
- `cd frontend && npm test` — vollständige Suite grün (inkl. alle Wave-0-Tests)
- `cd frontend && npx tsc --noEmit` — fehlerfrei
- `cd backend && go test ./internal/handlers/... -run TestRejectContributionRequiresReason` — grün
- `cd backend && go test ./internal/handlers/... -run TestSuggestionAudit` — grün
- page.tsx ≤450 Zeilen
- Human-Verify approved
</verification>

<success_criteria>
- `/me/contributions` zeigt vollständiges Dashboard mit Inbox → Summary → Listen
- Alle D-01 bis D-12-Entscheidungen sind implementiert
- Sämtliche Tests (Frontend + Backend) grün
- Human-Verify: alle 8 Prüfpunkte bestanden
</success_criteria>

<output>
Erstelle `.planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-05-SUMMARY.md` wenn fertig
</output>
