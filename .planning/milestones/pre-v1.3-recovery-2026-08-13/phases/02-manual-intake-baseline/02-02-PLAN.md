---
phase: 02-manual-intake-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/handlers/admin_content_anime_validation.go
  - backend/internal/handlers/admin_content_test.go
autonomous: true
requirements:
  - INTK-04
  - INTK-05
  - ASST-04
must_haves:
  truths:
    - "The authoritative create endpoint rejects manual anime creates that omit the cover."
    - "A manual anime can still be created without any Jellyfin linkage or source dependency."
    - "The existing cover upload result remains the accepted `cover_image` input for create."
  artifacts:
    - path: "backend/internal/handlers/admin_content_anime_validation.go"
      provides: "Phase 2 minimum create validation for title plus cover"
    - path: "backend/internal/handlers/admin_content_test.go"
      provides: "Regression coverage for cover-required create validation and manual-only create payloads"
  key_links:
    - from: "backend/internal/handlers/admin_content_anime.go"
      to: "backend/internal/handlers/admin_content_anime_validation.go"
      via: "validateAdminAnimeCreateRequest"
      pattern: "validateAdminAnimeCreateRequest"
    - from: "frontend/src/app/api/admin/upload-cover/route.ts"
      to: "backend/internal/handlers/admin_content_anime_validation.go"
      via: "returned file_name used as create cover_image"
      pattern: "CoverImage|cover_image"
---

<objective>
Tighten the backend create contract so Phase 2 manual intake is enforced server-side, not just by the UI.

Purpose: Prevent title-only record creation and keep manual intake independent from Jellyfin while reusing the existing cover upload result.
Output: Updated backend validation plus targeted tests for the title-plus-cover minimum contract.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-manual-intake-baseline/02-RESEARCH.md
@backend/internal/handlers/admin_content_anime.go
@backend/internal/handlers/admin_content_anime_validation.go
@backend/internal/handlers/admin_content_test.go
@frontend/src/app/api/admin/upload-cover/route.ts
@frontend/src/types/admin.ts

<interfaces>
From frontend/src/types/admin.ts:
```ts
export interface AdminAnimeCreateRequest {
  title: string
  type: AnimeType
  content_type: ContentType
  status: AnimeStatus
  title_de?: string
  title_en?: string
  year?: number
  max_episodes?: number
  genre?: string
  description?: string
  cover_image?: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing Go coverage for the Phase 2 create minimum contract</name>
  <files>backend/internal/handlers/admin_content_test.go</files>
  <read_first>
    - backend/internal/handlers/admin_content_test.go
    - backend/internal/handlers/admin_content_anime_validation.go
    - .planning/phases/02-manual-intake-baseline/02-RESEARCH.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <behavior>
    - Test 1: `validateAdminAnimeCreateRequest` rejects requests with `title` but no `cover_image`.
    - Test 2: `validateAdminAnimeCreateRequest` accepts a request with only the required `title` and `cover_image` plus existing enum defaults.
    - Test 3: The validated create input remains manual-only and does not require or derive any Jellyfin identifier.
  </behavior>
  <action>Extend `backend/internal/handlers/admin_content_test.go` with focused Phase 2 tests near the existing create-validation coverage. Add one regression that passes `title` with empty or whitespace `cover_image` and expects a blocking validation message, one regression that verifies `cover_image` survives normalization into `models.AdminAnimeCreateInput`, and one regression that proves the create validation contract remains satisfied without any Jellyfin field or source requirement per INTK-05.</action>
  <acceptance_criteria>
    - `backend/internal/handlers/admin_content_test.go` contains a test name matching `RequiresCover`.
    - `backend/internal/handlers/admin_content_test.go` contains a test case that asserts `CoverImage` is preserved on success.
    - No new test introduces Jellyfin fields into the create request struct.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/handlers -run 'TestValidateAdminAnimeCreateRequest' -count=1</automated>
  </verify>
  <done>The handler test suite contains explicit Phase 2 coverage for cover-required manual create behavior and for the still-manual, no-Jellyfin create contract.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce `title + cover` in backend create validation without broadening scope</name>
  <files>backend/internal/handlers/admin_content_anime_validation.go, backend/internal/handlers/admin_content_test.go</files>
  <read_first>
    - backend/internal/handlers/admin_content_anime_validation.go
    - backend/internal/handlers/admin_content_anime.go
    - backend/internal/handlers/admin_content_test.go
    - frontend/src/app/api/admin/upload-cover/route.ts
  </read_first>
  <action>Update `validateAdminAnimeCreateRequest` in `backend/internal/handlers/admin_content_anime_validation.go` so create fails unless both `title` and a normalized non-empty `cover_image` are present, per INTK-04 and ASST-04. Preserve the current request shape, allowed enums, audit path, and repository call in `CreateAnime`; do not add Jellyfin identifiers, provenance flags, or any alternate upload handling. Keep `cover_image` normalization compatible with the `file_name` returned by `/api/admin/upload-cover` so the manual draft UI can submit that exact value unchanged.</action>
  <acceptance_criteria>
    - `backend/internal/handlers/admin_content_anime_validation.go` returns a blocking validation message when `cover_image` is empty.
    - `backend/internal/handlers/admin_content_anime_validation.go` still returns `models.AdminAnimeCreateInput` with `CoverImage` populated from `cover_image`.
    - `backend/internal/handlers/admin_content_anime.go` still calls the existing repository `CreateAnime` path and does not introduce Jellyfin branching.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/handlers -run 'TestValidateAdminAnimeCreateRequest' -count=1</automated>
  </verify>
  <done>Server-side create validation now matches the Phase 2 minimum contract, and the backend remains a manual-only create path that accepts the current cover upload result.</done>
</task>

</tasks>

<verification>
Re-run the focused handler validation slice and confirm the updated validation still compiles against the unchanged create handler and request struct.
</verification>

<success_criteria>
The authoritative create endpoint rejects title-only anime creation, accepts the existing uploaded cover filename as `cover_image`, and does not require Jellyfin linkage for a valid manual create.
</success_criteria>

<output>
After completion, create `.planning/phases/02-manual-intake-baseline/02-02-SUMMARY.md`
</output>
