---
phase: 02-manual-intake-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/admin/anime/page.tsx
  - frontend/src/app/admin/anime/page.test.tsx
  - frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts
  - frontend/src/app/admin/anime/hooks/useManualAnimeDraft.test.ts
autonomous: true
requirements:
  - INTK-01
must_haves:
  truths:
    - "Admin sees an intake start screen at /admin/anime with one manual primary action and a reserved Jellyfin branch."
    - "The manual path communicates that title and cover are enough for the first saved anime."
    - "A reusable manual draft state contract exists before the create route is rewired."
  artifacts:
    - path: "frontend/src/app/admin/anime/page.tsx"
      provides: "Phase 2 intake choice entry with manual CTA and Jellyfin placeholder"
    - path: "frontend/src/app/admin/anime/page.test.tsx"
      provides: "Regression coverage for intake entry copy and links"
    - path: "frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts"
      provides: "Manual draft state contract for empty, incomplete, and ready states"
    - path: "frontend/src/app/admin/anime/hooks/useManualAnimeDraft.test.ts"
      provides: "Regression coverage for manual draft state derivation"
  key_links:
    - from: "frontend/src/app/admin/anime/page.tsx"
      to: "/admin/anime/create"
      via: "manual CTA href"
      pattern: "href=.*/admin/anime/create"
    - from: "frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts"
      to: "frontend/src/app/admin/anime/create/page.tsx"
      via: "exported draft state helper consumed by create route"
      pattern: "export function useManualAnimeDraft|export function resolveManualCreateState"
---

<objective>
Lock the Phase 2 intake entry contract and create the reusable manual draft state seam before the create page is rebuilt.

Purpose: Satisfy the explicit mode-choice requirement and prevent the create-route refactor from inventing draft-state semantics inline.
Output: A tested intake entry screen plus a dedicated manual draft hook/state helper for the later create workflow.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-manual-intake-baseline/02-RESEARCH.md
@.planning/phases/02-manual-intake-baseline/02-UI-SPEC.md
@.planning/phases/01-ownership-foundations/01-CONTEXT.md
@.planning/phases/01-ownership-foundations/01-03-SUMMARY.md
@frontend/src/app/admin/anime/page.tsx
@frontend/src/app/admin/anime/hooks/useAnimeEditor.ts
@frontend/src/app/admin/anime/types/admin-anime-editor.ts

<interfaces>
From frontend/src/app/admin/anime/types/admin-anime-editor.ts:
```ts
export type AnimeEditorContext = 'edit' | 'create'

export interface AnimeEditorControllerInput {
  isDirty: boolean
  isSubmitting: boolean
  onSubmit?: () => void
  formID?: string
  submitLabel?: string
  submitButtonType?: ButtonHTMLAttributes<HTMLButtonElement>['type']
  savedStateTitle?: string
  savedStateMessage?: string
  dirtyStateTitle?: string
  dirtyStateMessage?: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing frontend coverage for intake choice and manual draft states</name>
  <files>frontend/src/app/admin/anime/page.test.tsx, frontend/src/app/admin/anime/hooks/useManualAnimeDraft.test.ts, frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts</files>
  <read_first>
    - frontend/src/app/admin/anime/page.tsx
    - .planning/phases/02-manual-intake-baseline/02-UI-SPEC.md
    - .planning/phases/02-manual-intake-baseline/02-RESEARCH.md
    - .planning/phases/01-ownership-foundations/01-03-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: `/admin/anime` renders manual intake copy with exact primary CTA text `Neu manuell`.
    - Test 2: The entry screen renders a non-manual Jellyfin branch without exposing Phase 3 search UI.
    - Test 3: Manual draft state resolution returns `empty`, `incomplete`, and `ready` based on `title` and `cover_image`.
  </behavior>
  <action>Create `frontend/src/app/admin/anime/page.test.tsx` covering the Phase 2 entry-point copy and target hrefs per INTK-01. Create `frontend/src/app/admin/anime/hooks/useManualAnimeDraft.test.ts` describing the required draft state machine from `02-UI-SPEC.md`: no meaningful input -> `empty`, any manual input without both `title` and `cover_image` -> `incomplete`, and both present -> `ready`. Add a minimal exported contract file at `frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts` that declares the draft-state types and the helper under test so later tasks can build on a stable seam.</action>
  <acceptance_criteria>
    - `frontend/src/app/admin/anime/page.test.tsx` exists and contains `Neu manuell`.
    - `frontend/src/app/admin/anime/hooks/useManualAnimeDraft.test.ts` exists and contains assertions for `empty`, `incomplete`, and `ready`.
    - `frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts` exports the draft-state helper referenced by the new test file.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- src/app/admin/anime/page.test.tsx src/app/admin/anime/hooks/useManualAnimeDraft.test.ts</automated>
  </verify>
  <done>Two new Vitest files exist, they target the exact Phase 2 entry and draft-state contract, and the contract file exports the helper the create route will later import.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the Phase 2 intake choice screen and reusable manual draft-state helper</name>
  <files>frontend/src/app/admin/anime/page.tsx, frontend/src/app/admin/anime/page.test.tsx, frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts, frontend/src/app/admin/anime/hooks/useManualAnimeDraft.test.ts</files>
  <read_first>
    - frontend/src/app/admin/anime/page.tsx
    - frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts
    - .planning/phases/02-manual-intake-baseline/02-UI-SPEC.md
    - .planning/phases/02-manual-intake-baseline/02-RESEARCH.md
  </read_first>
  <action>Update `frontend/src/app/admin/anime/page.tsx` so the header and primary action implement the Phase 2 contract exactly: the manual branch is the primary CTA labeled `Neu manuell`, its supporting copy states that `title` and `cover` are enough for the first record, and the Jellyfin branch is rendered only as a reserved future option per INTK-01 without any search, preview, provenance, or relation UI. Implement `frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts` as the shared state resolver for `empty`, `incomplete`, and `ready`, driven by meaningful manual input and the minimum `title + cover` contract from `02-RESEARCH.md`. Keep the helper free of Jellyfin-specific fields and ready for direct consumption by the create page in the next wave.</action>
  <acceptance_criteria>
    - `frontend/src/app/admin/anime/page.tsx` contains the exact CTA label `Neu manuell`.
    - `frontend/src/app/admin/anime/page.tsx` contains copy mentioning `title` and `cover`.
    - `frontend/src/app/admin/anime/page.tsx` does not render Jellyfin search inputs or preview sections.
    - `frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts` exports the `empty`, `incomplete`, and `ready` state logic.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- src/app/admin/anime/page.test.tsx src/app/admin/anime/hooks/useManualAnimeDraft.test.ts</automated>
  </verify>
  <done>The intake entry point shows the approved manual start contract, the Jellyfin path stays visibly deferred, and the manual draft-state helper is ready for the create-route refactor.</done>
</task>

</tasks>

<verification>
Run the targeted entry and draft-state Vitest slice, then confirm the intake screen still uses the existing admin styling language rather than introducing a second surface.
</verification>

<success_criteria>
`/admin/anime` exposes the Phase 2 mode-choice contract for manual intake, and a tested manual draft-state helper exists for the create page without pulling any Phase 3 Jellyfin behavior into this phase.
</success_criteria>

<output>
After completion, create `.planning/phases/02-manual-intake-baseline/02-01-SUMMARY.md`
</output>
