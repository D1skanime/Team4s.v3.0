---
phase: 03-jellyfin-assisted-intake
plan: 05
type: execute
wave: 6
depends_on:
  - "03-04"
files_modified:
  - backend/internal/models/admin_jellyfin_intake.go
  - backend/internal/handlers/jellyfin_intake_preview.go
  - backend/internal/handlers/jellyfin_intake_preview_test.go
  - frontend/src/types/admin.ts
  - frontend/src/app/admin/anime/create/page.tsx
  - frontend/src/app/admin/anime/create/page.test.tsx
autonomous: true
requirements:
  - JFIN-04
  - JFIN-05
must_haves:
  truths:
    - "The initial Jellyfin-assisted draft title comes from the Jellyfin folder name, not the upstream display title."
    - "The folder-seeded title remains editable before save and is not silently overwritten during the same draft session."
    - "Phase 3 still exposes no deferred per-asset upload controls or anime-ID file naming UI while this title-seeding change lands."
  artifacts:
    - path: "backend/internal/models/admin_jellyfin_intake.go"
      provides: "Intake preview field for the Jellyfin folder-name title seed"
    - path: "backend/internal/handlers/jellyfin_intake_preview.go"
      provides: "Preview payload mapping that exposes folder-name title seed to the create flow"
    - path: "frontend/src/app/admin/anime/create/page.tsx"
      provides: "Draft hydration that initializes title from folder-name seed while preserving manual editability"
    - path: "frontend/src/app/admin/anime/create/page.test.tsx"
      provides: "Regression coverage for folder-name title initialization and editability"
  key_links:
    - from: "backend/internal/handlers/jellyfin_intake_preview.go"
      to: "frontend/src/types/admin.ts"
      via: "preview payload field for folder-name title seed"
      pattern: "folder.*name|title seed"
    - from: "frontend/src/app/admin/anime/create/page.tsx"
      to: "backend/internal/handlers/jellyfin_intake_preview.go"
      via: "hydrated draft title source"
      pattern: "folder.*name|title"
    - from: "frontend/src/app/admin/anime/create/page.tsx"
      to: "frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts"
      via: "editable title value seeded once from Jellyfin folder name"
      pattern: "title"
---

<objective>
Capture the clarified Phase 3 title-seeding rule added after 03-01 through 03-04.

Purpose: Make the execute step explicit that Jellyfin intake seeds the create draft title from the folder name, not the display title, while preserving operator edit control.
Output: One narrow follow-up plan for preview-contract and create-page title hydration only.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-jellyfin-assisted-intake/03-CONTEXT.md
@.planning/phases/03-jellyfin-assisted-intake/03-03-PLAN.md
@.planning/phases/03-jellyfin-assisted-intake/03-04-PLAN.md
@backend/internal/handlers/jellyfin_intake_preview.go
@frontend/src/types/admin.ts
@frontend/src/app/admin/anime/create/page.tsx

<interfaces>
Clarified locked decisions from `03-CONTEXT.md`:
```text
D-14: The draft title should be initialized from the Jellyfin folder name rather than the Jellyfin display title.
D-15: The folder-name title seed remains editable; the admin can still adjust the title manually before save.
```

Deferred scope that must stay excluded:
```text
Per-asset manual upload buttons that create contract-compliant asset folders or files named with the anime ID remain deferred.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock folder-name title seeding in backend and create-page tests</name>
  <files>backend/internal/handlers/jellyfin_intake_preview_test.go, frontend/src/app/admin/anime/create/page.test.tsx</files>
  <read_first>
    - .planning/phases/03-jellyfin-assisted-intake/03-CONTEXT.md
    - backend/internal/handlers/jellyfin_intake_preview.go
    - frontend/src/app/admin/anime/create/page.tsx
  </read_first>
  <behavior>
    - Test 1: Intake preview returns a dedicated folder-name seed field for the draft title per D-14 instead of forcing the UI to infer from display title.
    - Test 2: The hydrated create draft title uses the folder-name seed first and still allows manual edits afterward per D-15.
    - Test 3: The tests assert no deferred per-asset manual upload button copy or anime-ID file naming controls appear in this title-seeding slice.
  </behavior>
  <action>Add failing coverage that makes the title-source clarification executable. Extend the intake preview test to require a distinct folder-name title seed field. Extend the create-page test to assert the hydrated `title` input starts with the folder-name seed and changes when the admin types a replacement. Include a negative assertion that no deferred per-asset manual upload buttons or ID-based asset naming copy appear in this flow.</action>
  <acceptance_criteria>
    - `backend/internal/handlers/jellyfin_intake_preview_test.go` asserts a folder-name title seed field distinct from the display title.
    - `frontend/src/app/admin/anime/create/page.test.tsx` asserts the title input starts with folder-name seed content and changes when edited.
    - Tests assert the absence of deferred upload copy such as `anime ID`, `ID-basiert`, or `Aus Datei hochladen` in this Phase 3 draft path.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/handlers -run "Test.*Jellyfin.*IntakePreview" -count=1 && cd ../frontend && npm test -- src/app/admin/anime/create/page.test.tsx</automated>
  </verify>
  <done>The folder-name title source and editability rules are fixed in tests before implementation, and deferred upload controls remain explicitly excluded.</done>
</task>

<task type="auto">
  <name>Task 2: Expose folder-name title seed and hydrate the editable draft title once</name>
  <files>backend/internal/models/admin_jellyfin_intake.go, backend/internal/handlers/jellyfin_intake_preview.go, backend/internal/handlers/jellyfin_intake_preview_test.go, frontend/src/types/admin.ts, frontend/src/app/admin/anime/create/page.tsx, frontend/src/app/admin/anime/create/page.test.tsx</files>
  <read_first>
    - backend/internal/models/admin_jellyfin_intake.go
    - backend/internal/handlers/jellyfin_intake_preview.go
    - frontend/src/types/admin.ts
    - frontend/src/app/admin/anime/create/page.tsx
    - .planning/phases/03-jellyfin-assisted-intake/03-CONTEXT.md
  </read_first>
  <action>Add an explicit folder-name title seed field to the Phase 3 intake preview contract and frontend type definitions so the create route does not infer title source from the wrong upstream string. In the create-page hydration path, initialize the unsaved draft `title` from that folder-name seed per D-14, keep the existing input fully editable per D-15, and avoid overwriting later manual edits during the same draft session. Do not add per-asset manual upload buttons, anime-ID naming UI, or any other deferred asset-folder automation from later phases.</action>
  <acceptance_criteria>
    - `backend/internal/models/admin_jellyfin_intake.go` and `frontend/src/types/admin.ts` expose a folder-name title seed field for intake preview payloads.
    - `frontend/src/app/admin/anime/create/page.tsx` hydrates the editable title from the folder-name seed rather than the Jellyfin display title.
    - Manual title edits survive after hydration within the same draft session.
    - No new file in this plan adds per-asset manual upload buttons, anime-ID naming controls, or upload-folder provisioning language.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/handlers -run "Test.*Jellyfin.*IntakePreview" -count=1 && cd ../frontend && npm test -- src/app/admin/anime/create/page.test.tsx</automated>
  </verify>
  <done>Phase 3 execution now has explicit instructions for folder-name title initialization while deferred upload work stays out of scope.</done>
</task>

</tasks>

<verification>
Run the focused intake preview and create-page tests. The implemented flow should seed the title from the Jellyfin folder name, preserve manual editability, and keep deferred upload controls absent.
</verification>

<success_criteria>
The additive title-seeding clarifications are executable: folder name seeds the initial title, the title remains editable, and no deferred per-asset upload controls leak into Phase 3.
</success_criteria>

<output>
After completion, create `.planning/phases/03-jellyfin-assisted-intake/03-05-SUMMARY.md`
</output>
