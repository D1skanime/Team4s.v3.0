---
phase: 75-anime-gruppen-deep-dive-anime-id-group-groupid
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/repository/group_contributors_repository.go
  - backend/internal/repository/group_themes_repository.go
  - backend/internal/repository/group_release_media_repository.go
  - backend/internal/repository/group_contributors_repository_test.go
  - backend/internal/handlers/group_contributors_handler.go
  - backend/cmd/server/main.go
  - shared/contracts/openapi.yaml
  - frontend/src/types/groupContributors.ts
  - frontend/src/lib/api.ts
autonomous: true
requirements:
  - D
  - A
  - G
  - K

must_haves:
  truths:
    - "GET /api/v1/anime/:id/group/:groupId/contributors returns {team_members[], external_contributors[]} scoped strictly to this anime+group — kein voller Gruppen-Roster (D-06) — with roles aggregated per person across releases/versions via ARRAY_AGG DISTINCT (D-08), with no auth middleware"
    - "GET /api/v1/anime/:id/group/:groupId/themes returns {themes:[]} scoped to group, only status='ready' media"
    - "GET /api/v1/anime/:id/group/:groupId/release-media returns {items:[]} scoped to group+anime, only status='ready' media"
    - "GroupContributorsResponse, GroupThemesResponse, GroupReleaseMediaResponse types exist in frontend/src/types/groupContributors.ts"
    - "getGroupContributors, getGroupThemes, getGroupReleaseMedia helper functions exist in frontend/src/lib/api.ts"
    - "All three routes registered in main.go WITHOUT authMiddleware (public reads)"
    - "shared/contracts/openapi.yaml has three new path entries for /anime/{id}/group/{groupId}/contributors|themes|release-media"
    - "Invalid animeID or groupID (non-integer, <=0) returns 400 bad request from all three endpoints (per K)"
    - "go test ./internal/repository/ -run GroupContributors exits 0 — scoping, visibility gate, and empty-result tests pass"
  artifacts:
    - path: "backend/internal/repository/group_contributors_repository.go"
      provides: "GetProjectContributors(ctx, animeID, groupID) — two-query projection"
      exports: ["GroupContributorsRepository", "NewGroupContributorsRepository", "GroupContributorsResponse"]
    - path: "backend/internal/repository/group_contributors_repository_test.go"
      provides: "testify tests for GetProjectContributors scoping + visibility gate + empty-result behavior"
      exports: ["TestGetProjectContributors_Scoping", "TestGetProjectContributors_VisibilityGate", "TestGetProjectContributors_EmptyResult"]
    - path: "backend/internal/repository/group_themes_repository.go"
      provides: "GetPublicGroupThemes(ctx, animeID, groupID) — visibility-gated themes"
      exports: ["GroupThemesRepository", "NewGroupThemesRepository", "GroupThemesResponse"]
    - path: "backend/internal/repository/group_release_media_repository.go"
      provides: "GetPublicReleaseMedia(ctx, animeID, groupID) — visibility-gated media items"
      exports: ["GroupReleaseMediaRepository", "NewGroupReleaseMediaRepository", "GroupReleaseMediaResponse"]
    - path: "backend/internal/handlers/group_contributors_handler.go"
      provides: "HTTP handlers for three new public group endpoints"
      exports: ["GroupPublicHandler", "NewGroupPublicHandler"]
    - path: "frontend/src/types/groupContributors.ts"
      provides: "TypeScript DTOs matching Go JSON tags"
      exports: ["GroupTeamMember", "GroupExternalContributor", "GroupContributorsResponse", "PublicGroupTheme", "GroupThemesResponse", "PublicReleaseMediaItem", "GroupReleaseMediaResponse"]
    - path: "frontend/src/lib/api.ts"
      provides: "Three new helper functions for public group projection endpoints"
      contains: "getGroupContributors, getGroupThemes, getGroupReleaseMedia"
  key_links:
    - from: "backend/cmd/server/main.go"
      to: "backend/internal/handlers/group_contributors_handler.go"
      via: "v1.GET registration without authMiddleware"
      pattern: "groupId.*contributors"
    - from: "frontend/src/lib/api.ts"
      to: "GET /api/v1/anime/:id/group/:groupId/contributors"
      via: "authorizedFetch in getGroupContributors"
      pattern: "getGroupContributors"
    - from: "frontend/src/lib/api.ts"
      to: "GET /api/v1/anime/:id/group/:groupId/themes"
      via: "authorizedFetch in getGroupThemes"
      pattern: "getGroupThemes"
    - from: "frontend/src/lib/api.ts"
      to: "GET /api/v1/anime/:id/group/:groupId/release-media"
      via: "authorizedFetch in getGroupReleaseMedia"
      pattern: "getGroupReleaseMedia"
---

<objective>
Define and ship the three new public read contracts that the frontend narrative page requires: a contributors projection (team members + external contributors scoped to one anime+group), a themes projection (OP/ED/Middle with visibility-gated assets), and a release-media projection (public release_version_media items). This plan is contract-first (Lock K): OpenAPI paths, Go repositories, Go handler, route registration, TypeScript types, and api.ts helpers all land here before any frontend section component consumes them.

Purpose: Phases 73 and 75 share the same narrative paradigm. Phase 75 is blocked on three public endpoints that do not yet exist. This plan creates those seams without touching any existing admin endpoint or neutral anime-level table.

Output: Three new Go repository files, one new Go repository test file, one new Go handler file, main.go route additions, three new OpenAPI paths, a new TypeScript types file, and three new api.ts helper functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s-phase75/.planning/ROADMAP.md
@C:/Users/admin/Documents/Team4s-phase75/.planning/phases/75-anime-gruppen-deep-dive-anime-id-group-groupid/75-CONTEXT.md
@C:/Users/admin/Documents/Team4s-phase75/.planning/phases/75-anime-gruppen-deep-dive-anime-id-group-groupid/75-RESEARCH.md
@C:/Users/admin/Documents/Team4s-phase75/.planning/phases/75-anime-gruppen-deep-dive-anime-id-group-groupid/75-PATTERNS.md

<interfaces>
<!-- Existing Go patterns the executor MUST read before implementing. -->

From backend/internal/repository/anime_contributions_public_repository.go (EXACT analog):
- Package: `package repository`
- Imports: `"context"`, `"fmt"`, `github.com/jackc/pgx/v5/pgxpool`
- Package-level constants (same package — DO NOT redefine in new files):
  `memberSlugExpr = NULLIF(LOWER(TRIM(BOTH '-' FROM REGEXP_REPLACE(TRIM(%s), '[^a-z0-9]+', '-', 'gi'))), '')`
  `memberDisplayExpr = COALESCE(NULLIF(TRIM(%s.display_name), ''), %s.nickname)`
- Repository struct: `type XxxRepository struct { db *pgxpool.Pool }`
- Constructor: `func NewXxxRepository(db *pgxpool.Pool) *XxxRepository { return &XxxRepository{db: db} }`
- Error wrapping: `return nil, fmt.Errorf("group contributors: %w", err)`
- Row scanning with `rows.Next()` / `rows.Scan()` / `rows.Err()`

From backend/internal/handlers/contributions_public_handler.go (EXACT analog):
- Package: `package handlers`
- Imports: `"net/http"`, `"team4s.v3/backend/internal/repository"`, `"github.com/gin-gonic/gin"`
- Handler struct + constructor pattern (see PATTERNS.md group_contributors_handler section)
- Available in-package helpers (no import needed):
  `parseAnimeID(c.Param("id"))` from anime.go:262
  `parseGroupID(c.Param("groupId"))` from group_handler.go:89
  `badRequest(c, msg)` from anime.go:299
  `internalError(c, msg)` from group_handler.go:174

From backend/cmd/server/main.go (existing route registrations pattern):
  v1.GET("/anime/:id/group/:groupId", groupHandler.GetGroupDetail)
  v1.GET("/anime/:id/group/:groupId/assets", ...)
  v1.GET("/anime/:id/group/:groupId/releases", ...)
  — New routes go HERE, same v1 group, NO authMiddleware

From frontend/src/lib/api.ts (getGroupDetail at lines 5509–5529 as exact copy shape):
  Uses `getApiBaseUrl()`, `authorizedFetch(url, { cache: 'no-store' })`, `parseApiError`, `ApiError`
  Returns typed Promise cast: `response.json() as Promise<TypeHere>`

From frontend/src/types/group.ts:
  `GroupDetail { id, anime_id, fansub_id, fansub: { id, slug, name, logo_url }, story, period, stats, created_at, updated_at }`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Go repositories + repository tests + handler for three public group projection endpoints (per D, G, K, A)</name>
  <files>
    backend/internal/repository/group_contributors_repository.go
    backend/internal/repository/group_themes_repository.go
    backend/internal/repository/group_release_media_repository.go
    backend/internal/repository/group_contributors_repository_test.go
    backend/internal/handlers/group_contributors_handler.go
  </files>
  <read_first>
    backend/internal/repository/anime_contributions_public_repository.go
    backend/internal/handlers/contributions_public_handler.go
    backend/internal/handlers/group_handler.go
    backend/internal/repository/group_repository.go
    docs/architecture/db-schema-fansub-domain.md
  </read_first>
  <behavior>
    - GroupContributorsRepository.GetProjectContributors(ctx, animeID int64, groupID int64) returns *GroupContributorsResponse with team_members and external_contributors both non-nil (empty slice when no data)
    - External contributors query: SELECT from anime_contributions JOIN hist_fansub_group_members JOIN members LEFT JOIN anime_contribution_roles LEFT JOIN role_definitions WHERE anime_id=$1 AND fansub_group_id=$2 AND is_public_on_anime_page=true AND hfgm.visibility='public' AND release_version_id IS NULL; uses memberSlugExpr+memberDisplayExpr; COALESCE(rd.label_de, acr.role_code) for role labels
    - Team members query: SELECT DISTINCT ON (m.id) from release_member_roles JOIN members JOIN contributor_roles JOIN fansub_releases JOIN episodes JOIN release_versions JOIN release_version_groups WHERE episodes.anime_id=$1 AND release_version_groups.fansub_group_id=$2; aggregates ARRAY_AGG(DISTINCT cr.label) for role_labels
    - GroupThemesRepository.GetPublicGroupThemes: queries themes JOIN theme_segments JOIN release_theme_assets JOIN fansub_releases JOIN episodes WHERE episodes.anime_id=$1 AND fansub_releases join path reaches group $2; visibility gate = media_assets.status='ready' (Phase-72-Visibility-Gate fallback per RESEARCH Befund 3); groups result by theme type; returns *GroupThemesResponse{Themes:[]PublicGroupTheme}
    - GroupReleaseMediaRepository.GetPublicReleaseMedia: queries release_version_media JOIN media_assets JOIN media_files JOIN release_versions JOIN fansub_releases JOIN episodes JOIN release_version_groups WHERE episodes.anime_id=$1 AND release_version_groups.fansub_group_id=$2 AND media_assets.status='ready'; resolves thumbnail_url via media storage dir; returns *GroupReleaseMediaResponse{Items:[]} never nil
    - GroupPublicHandler holds all three repos, three handler methods: GetGroupContributors, GetGroupThemes, GetGroupReleaseMedia; each uses parseAnimeID + parseGroupID with badRequest on error, internalError on repo error, c.JSON(200, response) on success
    - DTOs use snake_case json tags; *string for nullable member_slug; []string with COALESCE(ARRAY[]::text[]) default for role_labels
    - Repository test: GetProjectContributors called with animeID=1, groupID=2 returns only contributors scoped to that groupID (no rows from groupID=3 appear in either block)
    - Repository test: GetPublicGroupThemes and GetPublicReleaseMedia only return assets/items where media_assets.status='ready'; assets with status='pending' must not appear
    - Repository test: GetProjectContributors called for a valid animeID+groupID with no rows returns non-nil empty slices for both TeamMembers and ExternalContributors (not nil, not error)
  </behavior>
  <action>
    Create group_contributors_repository.go in package repository. Import context, fmt, pgxpool. Define DTOs: GroupTeamMember{MemberID int64, MemberDisplayName string, MemberSlug *string, RoleLabels []string}, GroupExternalContributor{MemberDisplayName, MemberSlug *string, RoleLabels, IsVerified bool}, GroupContributorsResponse{TeamMembers, ExternalContributors}. Use memberSlugExpr and memberDisplayExpr package-level constants from anime_contributions_public_repository.go (same package — reference directly, do not redefine). Implement GetProjectContributors with two sequential queries: Query A for external contributors (anime_contributions scoped to group, is_public_on_anime_page=true, visibility='public', release_version_id IS NULL, status='confirmed' → is_verified), Query B for team members (release_member_roles scoped via episodes.anime_id=$1 AND release_version_groups.fansub_group_id=$2). Return combined response; wrap errors with fmt.Errorf per pattern.

    Create group_themes_repository.go. DTOs: PublicThemeAsset{ID int64, ThumbnailURL *string}, PublicGroupTheme{ID int64, ThemeType string, Title string, Assets []PublicThemeAsset}, GroupThemesResponse{Themes []PublicGroupTheme}. Implement GetPublicGroupThemes: join themes → release_theme_assets → fansub_releases → episodes (anime_id=$1) with release_version_groups scope to groupID=$2, JOIN media_assets WHERE status='ready', LEFT JOIN media_files for thumbnail path; resolve thumbnail_url by constructing public media path (read AdminContentHandler.buildPublicMediaURL pattern from existing handler for path construction, or use media_files.storage_path directly with /media/ prefix). Return GroupThemesResponse with non-nil Themes slice.

    Create group_release_media_repository.go. Constructor needs both db *pgxpool.Pool and mediaStorageDir string for URL resolution. DTOs: PublicReleaseMediaItem{ID int64, ThumbnailURL *string, Caption *string, MediaType string}, GroupReleaseMediaResponse{Items []PublicReleaseMediaItem}. Implement GetPublicReleaseMedia: join release_version_media → release_versions → fansub_releases → episodes (anime_id=$1) + release_version_groups (fansub_group_id=$2) → media_assets (status='ready') → media_files. Resolve thumbnail_url via mediaStorageDir + media_files.storage_path for thumbnail variant. Return GroupReleaseMediaResponse{Items: non-nil slice}.

    Create group_contributors_repository_test.go in package repository. Use testify (github.com/stretchr/testify/assert and require). Write three test functions using table-driven patterns with a test DB or mock pgxpool where appropriate (follow the existing test pattern in the repository package — read one existing *_test.go to confirm the pattern before writing):
    - TestGetProjectContributors_Scoping: assert that results for (animeID=1, groupID=2) contain no contributors whose source groupID would be 3; i.e., no cross-group leakage. Use require.NotNil for response; iterate team_members and external_contributors and assert all sourced from groupID=2.
    - TestGetPublicGroupThemes_VisibilityGate and TestGetPublicReleaseMedia_VisibilityGate: assert that returned items all have media_assets.status='ready'; pending/draft assets must not appear. If using a real DB, insert a pending asset and assert it is absent from the response.
    - TestGetProjectContributors_EmptyResult: call GetProjectContributors with a valid but empty animeID+groupID combination; assert require.NoError; assert response.TeamMembers is not nil (assert.NotNil); assert response.ExternalContributors is not nil; assert len(response.TeamMembers) == 0.

    Create group_contributors_handler.go in package handlers. GroupPublicHandler struct with contributorsRepo, themesRepo, mediaRepo fields. NewGroupPublicHandler constructor. Three methods: GetGroupContributors, GetGroupThemes, GetGroupReleaseMedia — each parses animeID and groupID with parseAnimeID/parseGroupID (return badRequest on err), calls corresponding repo method, returns internalError on err or c.JSON(200, response). All error messages per umlaut rule: "ungültige anime-id", "ungültige group-id", "interner serverfehler".
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s-phase75/backend && go build ./... 2>&amp;1 | head -40 &amp;&amp; go test ./internal/repository/ -run GroupContributors 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    go build ./... exits 0; all five new files compile without errors; go test ./internal/repository/ -run GroupContributors exits 0 with all three test functions passing; GroupPublicHandler has three methods; all DTOs use snake_case JSON tags; no memberSlugExpr redefinition in new files; test verifies cross-group scoping, visibility gate (status='ready' only), and empty-result returns non-nil empty slices
  </done>
</task>

<task type="auto">
  <name>Task 2: main.go route registration + OpenAPI contract + TypeScript types + api.ts helpers (per K, D, G)</name>
  <files>
    backend/cmd/server/main.go
    shared/contracts/openapi.yaml
    frontend/src/types/groupContributors.ts
    frontend/src/lib/api.ts
  </files>
  <read_first>
    backend/cmd/server/main.go
    shared/contracts/openapi.yaml
    frontend/src/lib/api.ts
    frontend/src/types/group.ts
    frontend/src/types/contributions.ts
  </read_first>
  <action>
    In main.go: instantiate groupContributorsRepo, groupThemesRepo (db only), groupReleaseMediaRepo (db + mediaStorageDir from config). Create groupPublicHandler := handlers.NewGroupPublicHandler(...). Register after existing group routes in the v1 router group WITHOUT authMiddleware:
      v1.GET("/anime/:id/group/:groupId/contributors", groupPublicHandler.GetGroupContributors)
      v1.GET("/anime/:id/group/:groupId/themes", groupPublicHandler.GetGroupThemes)
      v1.GET("/anime/:id/group/:groupId/release-media", groupPublicHandler.GetGroupReleaseMedia)

    In shared/contracts/openapi.yaml: add three new paths under /anime/{id}/group/{groupId}/ using the existing /anime/{id}/group/{groupId} path as structural reference. Each path has a GET operation with summary, parameters (id: integer, groupId: integer, both required path params), responses: 200 with application/json content referencing new schema components, 400 for invalid params, 500 for server error. Add schema components: GroupContributorsResponse (team_members array of GroupTeamMember, external_contributors array of GroupExternalContributor), GroupThemesResponse (themes array of PublicGroupTheme with nested assets array), GroupReleaseMediaResponse (items array of PublicReleaseMediaItem). Field types mirror the Go DTOs exactly (snake_case).

    Create frontend/src/types/groupContributors.ts: export interfaces GroupTeamMember{member_id: number, member_display_name: string, member_slug: string|null, role_labels: string[]}, GroupExternalContributor{member_display_name: string, member_slug: string|null, role_labels: string[], is_verified: boolean}, GroupContributorsResponse{team_members: GroupTeamMember[], external_contributors: GroupExternalContributor[]}, PublicThemeAsset{id: number, thumbnail_url: string|null}, PublicGroupTheme{id: number, type: string, title: string, assets: PublicThemeAsset[]}, GroupThemesResponse{themes: PublicGroupTheme[]}, PublicReleaseMediaItem{id: number, thumbnail_url: string|null, caption: string|null, media_type: string}, GroupReleaseMediaResponse{items: PublicReleaseMediaItem[]}. All field names match Go json tags exactly.

    In frontend/src/lib/api.ts: add import of GroupContributorsResponse, GroupThemesResponse, GroupReleaseMediaResponse from '@/types/groupContributors'. Add three helper functions at the end of the group helpers section (after existing getGroupReleases). Pattern: copy getGroupDetail shape verbatim; adapt URL only. getGroupContributors(animeID: number, groupID: number): Promise, getGroupThemes(animeID, groupID): Promise, getGroupReleaseMedia(animeID, groupID): Promise. Use authorizedFetch with cache: 'no-store'. Throw ApiError on non-ok response via parseApiError.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s-phase75/backend && go build ./cmd/server/... 2>&amp;1 | head -20 &amp;&amp; cd C:/Users/admin/Documents/Team4s-phase75/frontend && npm run typecheck 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    go build ./cmd/server/... exits 0; npm run typecheck exits 0 (no TS errors); openapi.yaml has three new /anime/{id}/group/{groupId}/* paths; groupContributors.ts exports all seven interfaces; api.ts exports getGroupContributors, getGroupThemes, getGroupReleaseMedia
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet → new GET endpoints | animeID and groupID are attacker-controlled path parameters; no auth token present |
| DB projection → public response | Visibility gate determines what media/asset data is returned; must never return non-public content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-01-01 | Tampering / IDOR | parseAnimeID / parseGroupID in group_contributors_handler.go | mitigate | Use existing parseAnimeID + parseGroupID helpers (int64 parse + id > 0 check); return 400 on invalid — verified pattern from group_handler.go:89 |
| T-75-01-02 | Information Disclosure | group_themes_repository.go — theme assets | mitigate | Visibility gate: WHERE media_assets.status='ready' (Phase-72 fallback); never return admin-only or draft assets on public endpoint |
| T-75-01-03 | Information Disclosure | group_release_media_repository.go — release version media | mitigate | Same visibility gate: WHERE media_assets.status='ready'; empty slice on no visible items (never 404 leaking existence) |
| T-75-01-04 | Information Disclosure | GetProjectContributors — member data | mitigate | Only external contributors with is_public_on_anime_page=true AND hfgm.visibility='public' are returned; release_member_roles treated as public release credits (Pitfall 3 accepted risk, Phase-79-scope) |
| T-75-01-SC | Tampering | npm/pip/cargo installs | accept | No new external packages in this plan — no install tasks |
</threat_model>

<verification>
1. go build ./... exits 0 in backend/
2. go test ./internal/repository/ -run GroupContributors exits 0 — all three test functions pass
3. npm run typecheck exits 0 in frontend/
4. Three new GET routes present in main.go without authMiddleware
5. shared/contracts/openapi.yaml has /anime/{id}/group/{groupId}/contributors, /themes, /release-media paths
6. frontend/src/types/groupContributors.ts exports GroupContributorsResponse, GroupThemesResponse, GroupReleaseMediaResponse
7. frontend/src/lib/api.ts exports getGroupContributors, getGroupThemes, getGroupReleaseMedia
</verification>

<success_criteria>
- Backend compiles without errors
- Repository tests: GetProjectContributors scoping (no cross-group leakage), visibility gate (status='ready' only), empty-result (non-nil empty slices) — all pass
- Frontend typechecks without errors
- All three new public routes registered in main.go without auth
- OpenAPI contract covers all three new endpoints
- TypeScript types match Go JSON DTOs exactly
- api.ts helpers follow authorizedFetch pattern with ApiError
</success_criteria>

<output>
Create `.planning/phases/75-anime-gruppen-deep-dive-anime-id-group-groupid/75-01-SUMMARY.md` when done
</output>
