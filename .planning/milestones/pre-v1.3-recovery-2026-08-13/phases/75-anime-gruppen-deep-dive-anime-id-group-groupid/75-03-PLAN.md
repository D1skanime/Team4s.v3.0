---
phase: 75-anime-gruppen-deep-dive-anime-id-group-groupid
plan: 03
type: execute
wave: 3
depends_on:
  - 75-01
  - 75-02
files_modified:
  - frontend/src/app/anime/[id]/group/[groupId]/page.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/sections/TeamSection.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/sections/ThemesSection.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/sections/MediaSection.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/sections/BacklinksSection.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/page.module.css
autonomous: true
requirements:
  - D
  - A
  - G
  - K

must_haves:
  truths:
    - "TeamSection renders two strictly separated blocks: Team-Beteiligte (release_member_roles) and Externe Mitwirkende (anime_contributions), never mixed (D-07)"
    - "Claimed members (member_slug != null) are rendered as <Link href=/members/[slug]>; unclaimed as plain <span> — no cursor/pointer (D-09)"
    - "ReleasesSection shows first 5 episodes as highlight cards with Button CTA 'Alle Releases ansehen' linking to /releases (D-10, D-03)"
    - "ThemesSection groups themes by type (Opening/Ending/Middle) and shows visible release_theme_assets tiles, sofern sichtbar/freigegeben — read-only, keine Player-/Timing-Editoren — with EmptyState when no themes (D-12, D-13)"
    - "MediaSection shows gallery of public release media items with EmptyState — section never hidden (D-14, D-15)"
    - "BacklinksSection renders links to /fansubs/[slug] and /anime/[id] (D-02 Rückverlinkung)"
    - "page.tsx calls getGroupContributors, getGroupThemes, getGroupReleaseMedia with graceful degradation — section shows EmptyState on fetch error (D-05)"
    - "All section anchor IDs present and stable (#team, #releases, #themes, #medien) — sticky-nav never links to missing section (D-04)"
    - "All user-facing strings use correct German umlauts (CLAUDE.md)"
    - "No native <button>/<input>/<select> outside @/components/ui; Card/Badge/Button/SectionHeader/EmptyState used per UI-SPEC"
    - "No file exceeds 450 lines (CLAUDE.md)"
    - "D-11 (version labels v1/v2, TV/BD) is a known gap deferred to /releases depth — EpisodeReleaseSummary carries no version_label field; no version label rendering attempted in ReleasesSection"
  artifacts:
    - path: "frontend/src/app/anime/[id]/group/[groupId]/sections/TeamSection.tsx"
      provides: "Two-block member display (team + external) with claim-based linking"
      exports: ["TeamSection"]
    - path: "frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.tsx"
      provides: "Highlight releases preview + CTA link to /releases"
      exports: ["ReleasesSection"]
    - path: "frontend/src/app/anime/[id]/group/[groupId]/sections/ThemesSection.tsx"
      provides: "OP/ED/Middle themes grouped by type with asset tiles"
      exports: ["ThemesSection"]
    - path: "frontend/src/app/anime/[id]/group/[groupId]/sections/MediaSection.tsx"
      provides: "Release-Einblicke gallery with EmptyState (always visible)"
      exports: ["MediaSection"]
    - path: "frontend/src/app/anime/[id]/group/[groupId]/sections/BacklinksSection.tsx"
      provides: "Deep-links to fansub page and anime page"
      exports: ["BacklinksSection"]
  key_links:
    - from: "frontend/src/app/anime/[id]/group/[groupId]/page.tsx"
      to: "getGroupContributors, getGroupThemes, getGroupReleaseMedia"
      via: "import from @/lib/api + try/catch secondary fetch"
      pattern: "getGroupContributors|getGroupThemes|getGroupReleaseMedia"
    - from: "frontend/src/app/anime/[id]/group/[groupId]/sections/TeamSection.tsx"
      to: "/members/[slug]"
      via: "next/link when member_slug != null"
      pattern: "member_slug.*Link|Link.*member_slug"
    - from: "frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.tsx"
      to: "/anime/[animeID]/group/[groupID]/releases"
      via: "Button href prop"
      pattern: "releases.*href|href.*releases"
---

<objective>
Wire the five remaining narrative sections (Team, Releases, Themes, Medien, Backlinks) with real data from the Plan-01 endpoints, and update page.tsx to call the three new API helpers with graceful degradation. This completes the narrative scroll-page defined by D-01 through D-15. After this plan the route `/anime/[id]/group/[groupId]` is a fully functional seven-section narrative page.

Purpose: The section scaffold from Plan 02 has empty placeholders for #team, #releases, #themes, #medien. This plan fills them with real components and real data fetches. The D-07 member/contributor separation, D-09 claim-based linking, D-10 highlights, D-12 themes grouping, D-14/D-15 media gallery, and D-02 backlinks all land here.

Output: Five new section component files, updated page.tsx wiring three new API calls, extended page.module.css with grid/gallery styles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s-phase75/.planning/ROADMAP.md
@C:/Users/admin/Documents/Team4s-phase75/.planning/phases/75-anime-gruppen-deep-dive-anime-id-group-groupid/75-CONTEXT.md
@C:/Users/admin/Documents/Team4s-phase75/.planning/phases/75-anime-gruppen-deep-dive-anime-id-group-groupid/75-RESEARCH.md
@C:/Users/admin/Documents/Team4s-phase75/.planning/phases/75-anime-gruppen-deep-dive-anime-id-group-groupid/75-PATTERNS.md
@C:/Users/admin/Documents/Team4s-phase75/.planning/phases/75-anime-gruppen-deep-dive-anime-id-group-groupid/75-UI-SPEC.md
@C:/Users/admin/Documents/Team4s-phase75/.planning/phases/75-anime-gruppen-deep-dive-anime-id-group-groupid/75-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 + Plan 02 that sections consume. -->

From frontend/src/types/groupContributors.ts (created in Plan 01):
  GroupTeamMember { member_id: number, member_display_name: string, member_slug: string|null, role_labels: string[] }
  GroupExternalContributor { member_display_name: string, member_slug: string|null, role_labels: string[], is_verified: boolean }
  GroupContributorsResponse { team_members: GroupTeamMember[], external_contributors: GroupExternalContributor[] }
  PublicGroupTheme { id: number, type: string, title: string, assets: PublicThemeAsset[] }
  PublicThemeAsset { id: number, thumbnail_url: string|null }
  GroupThemesResponse { themes: PublicGroupTheme[] }
  PublicReleaseMediaItem { id: number, thumbnail_url: string|null, caption: string|null, media_type: string }
  GroupReleaseMediaResponse { items: PublicReleaseMediaItem[] }

From frontend/src/types/group.ts:
  EpisodeReleaseSummary { id, episode_id?, episode_number, title?, has_op, has_ed, karaoke_count, insert_count, screenshot_count, thumbnail_url?, released_at? }
  NOTE: EpisodeReleaseSummary carries NO version label fields (no version_label, release_version, tv_bd_label, etc.).
  D-11 (v1/v2, TV/BD release version labels) CANNOT be rendered from this type — it is a known gap deferred to the /releases depth page. Do NOT attempt to render version labels in ReleasesSection.

UI Primitive variants (from UI-SPEC):
  Team-Beteiligte block: Card variant="elevated"
  Externe Mitwirkende block: Card variant="flat"
  Release highlight cards: Card variant="interactive"
  Theme cards: Card variant="section"
  Media gallery container: Card variant="section"
  Backlinks card: Card variant="flat"
  Inactive nav chip: Button variant="ghost", size="sm"
  Active nav chip: Button variant="subtle", size="sm"

Copywriting (correct umlauts required in production code):
  Section heading Team: "Beteiligte am Projekt"
  Block title team: "Team-Beteiligte"
  Block title external: "Externe Mitwirkende"
  Section heading Releases: "Releases & Versionen"
  Section heading Themes: "OP / ED / Middle"
  Theme type labels: "Opening", "Ending", "Middle"
  Section heading Media: "Release-Einblicke"
  Section heading Backlinks: "Mehr entdecken"
  CTA button: "Alle Releases ansehen"
  Deep-link group: "Zur Gruppenseite"
  Deep-link anime: "Zur Anime-Seite"
  Empty States (umlauts): see UI-SPEC Copywriting Contract table

Graceful degradation pattern (from page.tsx Plan 02 pattern):
  try { data = await getGroupContributors(animeID, groupID) } catch { /* empty default */ }
  Pass default empty response on error → section shows EmptyState (D-05)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TeamSection + ReleasesSection + BacklinksSection (per D-06, D-07, D-08, D-09, D-10, D-11, D-03)</name>
  <files>
    frontend/src/app/anime/[id]/group/[groupId]/sections/TeamSection.tsx
    frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.tsx
    frontend/src/app/anime/[id]/group/[groupId]/sections/BacklinksSection.tsx
    frontend/src/app/anime/[id]/group/[groupId]/sections/TeamSection.test.tsx
  </files>
  <read_first>
    frontend/src/types/groupContributors.ts
    frontend/src/types/group.ts
    frontend/src/components/ui/Card.tsx
    frontend/src/components/ui/Badge.tsx
    frontend/src/components/ui/SectionHeader.tsx
    frontend/src/components/ui/EmptyState.tsx
    frontend/src/components/ui/Button.tsx
    frontend/src/app/anime/[id]/group/[groupId]/page.module.css
  </read_first>
  <behavior>
    TeamSection tests:
    - Test 1: teamMembers and externalContributors are in separate DOM sections — querying for "Team-Beteiligte" and "Externe Mitwirkende" headings returns two distinct elements
    - Test 2: member with member_slug="test-slug" renders as <a href="/members/test-slug"> (link); member with member_slug=null renders as <span> with no href
    - Test 3: empty teamMembers array shows empty-state text "noch keine Team-Mitglieder" (case-insensitive); does NOT hide the section
    - Test 4: contribution entry with role_labels=["Timer"] shows the role label "Timer" in the external contributors block
  </behavior>
  <action>
    Create TeamSection.test.tsx first (Wave 0): import render from vitest + testing-library; mock @/components/ui primitives if needed (Card, Badge, SectionHeader, EmptyState as pass-through renders); write four failing tests per behavior block above.

    Create TeamSection.tsx: Props: { teamMembers: GroupTeamMember[]; externalContributors: GroupExternalContributor[] }. Outer div id="team" className={styles.teamSection}. SectionHeader title="Beteiligte am Projekt". Block 1 div className={styles.teamBlock}: h3 "Team-Beteiligte"; if empty → EmptyState variant="compact" title="Noch keine Team-Beteiligten" description="Für dieses Projekt sind noch keine Team-Mitglieder erfasst."; else div className={styles.personGrid} mapping teamMembers to Card variant="elevated" className={styles.personCard}: if member_slug → Link href={`/members/${m.member_slug}`}; else span; below name: div className={styles.roleTags} mapping role_labels to Badge variant="muted". Block 2 div className={styles.externalBlock}: lg-spacer, h3 "Externe Mitwirkende"; if empty → EmptyState variant="compact" title="Keine externen Mitwirkenden" description="Für dieses Projekt sind noch keine externen Mitwirkenden hinterlegt."; else Card variant="flat" className={styles.externalList} mapping externalContributors to div className={styles.externalRow}: link or span per member_slug, role Badge tags. Keep ≤120 lines.

    Create ReleasesSection.tsx: Props: { episodes: EpisodeReleaseSummary[]; animeID: number; groupID: number }. Outer div id="releases" className={styles.releasesSection}. SectionHeader title="Releases & Versionen". If empty → EmptyState variant="compact" title="Noch keine Releases" description="Für dieses Projekt sind noch keine öffentlichen Releases vorhanden." and still show the CTA. Else div className={styles.releaseGrid} mapping first 5 episodes (slice already done in page.tsx) to Card variant="interactive" className={styles.releaseCard}: paragraph episode title or "Episode {N}"; episode.released_at shown as secondary text if present; DO NOT render has_op/has_ed/karaoke_count flags (RESEARCH Befund 1 — dummy values). D-11 GAP (DEFERRED): EpisodeReleaseSummary carries no version_label, release_version, or tv_bd_label field — version labels (v1/v2, TV/BD) cannot be rendered from this type. Do not attempt to derive or display version label information; this is explicitly deferred to the /releases depth page where per-version data is available. After grid: div className={styles.releasesCta} with Button variant="primary" href={`/anime/${animeID}/group/${groupID}/releases`} "Alle Releases ansehen". Keep ≤100 lines.

    Create BacklinksSection.tsx: Props: { fansubSlug: string; animeID: number }. Div with SectionHeader title="Mehr entdecken". Card variant="flat" className={styles.backlinksCard} containing two Link elements: href={`/fansubs/${fansubSlug}`} with ExternalLink icon (lucide-react, size=16) "Zur Gruppenseite"; href={`/anime/${animeID}`} with ExternalLink "Zur Anime-Seite". Keep ≤50 lines.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s-phase75/frontend && npm test -- --reporter=verbose TeamSection 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    TeamSection.test.tsx: all 4 tests pass; TeamSection renders two distinct blocks (never merged); member with slug gets Link; member without slug gets span; ReleasesSection shows CTA button; ReleasesSection does NOT render version labels (D-11 deferred — EpisodeReleaseSummary has no version_label field, confirmed by reading types/group.ts before implementing); BacklinksSection has two links; no native button/select/input outside ui/; correct German umlauts in all strings
  </done>
</task>

<task type="auto">
  <name>Task 2: ThemesSection + MediaSection + page.tsx final wiring (per D-12, D-13, D-14, D-15, D-05)</name>
  <files>
    frontend/src/app/anime/[id]/group/[groupId]/sections/ThemesSection.tsx
    frontend/src/app/anime/[id]/group/[groupId]/sections/MediaSection.tsx
    frontend/src/app/anime/[id]/group/[groupId]/page.tsx
    frontend/src/app/anime/[id]/group/[groupId]/page.module.css
  </files>
  <read_first>
    frontend/src/app/anime/[id]/group/[groupId]/page.tsx
    frontend/src/app/anime/[id]/group/[groupId]/page.module.css
    frontend/src/types/groupContributors.ts
    frontend/src/components/ui/Card.tsx
    frontend/src/components/ui/SectionHeader.tsx
    frontend/src/components/ui/EmptyState.tsx
  </read_first>
  <action>
    Create ThemesSection.tsx: Props: { themes: PublicGroupTheme[] }. Outer div id="themes" className={styles.themesSection}. SectionHeader title="OP / ED / Middle". If themes empty → EmptyState variant="compact" title="Noch keine OP/ED/Middle" description="Für dieses Projekt sind noch keine Theme-Einblicke freigegeben.". Else: group themes by type using a local groupByType helper (const grouped = {OP:[], ED:[], MIDDLE:[]} pattern). Theme type label map: { OP: 'Opening', ED: 'Ending', MIDDLE: 'Middle' }. Render ['OP','ED','MIDDLE'] in order; skip groups with no themes. For each group: h3 with type label; map themes to Card variant="section" className={styles.themeCard}: div className={styles.themeHeader} with span themeTitle + Badge variant="muted" type label; if theme.assets.length > 0: div className={styles.assetGrid} mapping assets to div className={styles.assetTile} with img src={asset.thumbnail_url} alt={`OP-Asset: ${theme.title}`} (descriptive alt per A11y spec); skip tiles with null thumbnail_url. Read-only — NO player, no timing editors. Keep ≤120 lines. If file approaches 120 lines, extract groupByType to ThemesSection.helpers.ts.

    Create MediaSection.tsx: Props: { items: PublicReleaseMediaItem[] }. Outer div id="medien" className={styles.mediaSection}. SectionHeader title="Release-Einblicke". Section is ALWAYS visible regardless of items count (D-15). If items empty → EmptyState variant="compact" title="Noch keine Release-Einblicke" description="Für dieses Projekt sind bisher keine öffentlichen Medien freigegeben.". Else Card variant="section" with div className={styles.galleryGrid} mapping items to div className={styles.galleryTile}: img src={item.thumbnail_url} alt={item.caption ? `Release-Einblick: ${item.caption}` : 'Release-Einblick'} (correct umlauts) only if thumbnail_url not null; if item.caption: paragraph className={styles.galleryCaption}. Keep ≤80 lines.

    Update page.tsx: Replace the three stub variables (contributorsData, themesData, releaseMediaData) with real fetches. Add try/catch for each new fetch using the graceful degradation pattern: try { contributorsData = await getGroupContributors(animeID, groupID) } catch { /* default stays */ }. Same for getGroupThemes and getGroupReleaseMedia. Import getGroupContributors, getGroupThemes, getGroupReleaseMedia from '@/lib/api'. Import TeamSection, ReleasesSection, ThemesSection, MediaSection, BacklinksSection from './sections/*'. Replace placeholder section divs with real section components passing data as props: section id="team" → TeamSection teamMembers + externalContributors; section id="releases" → ReleasesSection episodes={releaseEpisodes.slice(0,5)} animeID groupID; section id="themes" → ThemesSection themes={themesData.themes}; section id="medien" → MediaSection items={releaseMediaData.items}; BacklinksSection fansubSlug={group.fansub.slug} animeID. Keep page.tsx ≤150 lines. The page.tsx structure produced by Plan 75-02 is the base — only the stub variable assignments (contributorsData, themesData, releaseMediaData) and the placeholder section divs (id="team", id="releases", id="themes", id="medien") are replaced; no other existing page.tsx code (params resolution, existing fetches, computed values, HeroSection, StorySection, GroupSectionsNav, imports already present) is altered.

    Extend page.module.css: Add .teamSection, .teamBlock, .externalBlock, .personGrid, .personCard, .roleTags, .externalList, .externalRow, .releasesSection, .releaseGrid, .releaseCard, .releasesCta, .themesSection, .themeCard, .themeHeader, .themeTitle, .assetGrid, .assetTile, .assetThumb, .mediaSection, .galleryGrid, .galleryTile, .galleryThumb, .galleryCaption, .backlinksSection, .backlinksCard, .deepLink CSS classes. Apply spacing scale per UI-SPEC: sections gap xl (32px), within-section lg (24px) above content, card padding md (16px), grid gap md (16px). Person grid: grid-template-columns repeat(auto-fill, minmax(200px, 1fr)). Gallery grid: grid-template-columns repeat(auto-fill, minmax(160px, 1fr)). Gallery thumb: aspect-ratio 16/9, object-fit cover, border-radius 8px. Keep ≤450 lines total across entire page.module.css.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s-phase75/frontend && npm run typecheck 2>&amp;1 | tail -20 &amp;&amp; npm run build 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    npm run typecheck exits 0; npm run build exits 0; page.tsx ≤150 lines; all five new section files exist; MediaSection section always present in DOM (never hidden); ThemesSection groups themes by OP/ED/Middle; page.tsx calls all three new api helpers with graceful try/catch; all German strings use correct umlauts; no file exceeds 450 lines; page.tsx structure produced by Plan 75-02 is preserved as the base — only the stub variable assignments and the placeholder section divs are replaced; no other existing page.tsx code is altered
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Vollständige narrative Scroll-Seite `/anime/[id]/group/[groupId]` mit sieben Abschnitten:
    Hero → Projektgeschichte → Beteiligte am Projekt (Team + Externe) → Releases & Versionen →
    OP / ED / Middle → Release-Einblicke → Mehr entdecken.
    Sticky-Nav mit fünf Chips (Geschichte · Beteiligte · Releases · OP/ED/Middle · Medien).
    Alle drei neuen Backend-Endpoints aktiv. Graceful Degradation — Empty States bei fehlenden Daten.
  </what-built>
  <how-to-verify>
    1. Dev-Server starten: `cd frontend && npm run dev` (Port 3000)
    2. Eine Anime-Gruppen-URL öffnen, z. B. `/anime/1/group/1` (oder ein existierendes Paar)
    3. Prüfen: Sieben Abschnitte sichtbar in der richtigen Reihenfolge (Hero → Geschichte → Beteiligte → Releases → OP/ED/Middle → Release-Einblicke → Mehr entdecken)
    4. Sticky-Nav vorhanden: fünf Chips; beim Scrollen wird der aktive Chip hervorgehoben; Klick auf Chip scrollt zum Anker
    5. Abschnitte mit leeren Daten: EmptyState-Platzhalter sichtbar — kein versteckter Abschnitt
    6. Beteiligte-Abschnitt: "Team-Beteiligte" und "Externe Mitwirkende" als getrennte Blöcke; geclaimte Member als Link, ungeclaimte als Text
    7. Releases: Highlight-Karten + "Alle Releases ansehen"-Button vorhanden
    8. OP/ED/Middle: Theme-Typen gruppiert (Opening/Ending/Middle); leerer Abschnitt zeigt EmptyState
    9. Release-Einblicke: Galerie oder EmptyState — Abschnitt nie ausgeblendet
    10. "Mehr entdecken": zwei Links (Zur Gruppenseite, Zur Anime-Seite) funktionieren
    11. Mobil (< 640px): Sticky-Nav als horizontale scrollbare Chip-Leiste
    12. Keine Console-Errors, keine 401-Fehler auf neuen Endpoints
    13. Alle deutschen Texte mit korrekten Umlauten (ä/ö/ü/ß) — KEINE ae/oe/ue/ss
  </how-to-verify>
  <resume-signal>„approved" eingeben oder Probleme beschreiben</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend API → Frontend Server Component | Three new public endpoints called from SSR; graceful degradation on error |
| Public page → /members/[slug] links | Only rendered when member_slug != null; slug comes from backend memberSlugExpr |
| Theme asset thumbnails | Only backend-filtered public assets reach ThemesSection; no client-side visibility logic needed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-03-01 | Information Disclosure | ThemesSection — asset thumbnails | mitigate | Backend already filters visibility='public'/status='ready' (Plan 01); frontend renders whatever backend returns — no additional frontend filter needed, but do NOT re-call admin endpoints |
| T-75-03-02 | Information Disclosure | MediaSection — release_version_media | mitigate | Same: backend-only visibility gate; MediaSection renders items[] directly from API response |
| T-75-03-03 | Tampering | page.tsx — animeID/groupID in URLs passed to child sections | mitigate | IDs are parsed as int in page.tsx (parseInt + notFound guard); never concatenate raw params into URLs — only use parsed numeric values |
| T-75-03-04 | Information Disclosure | Error messages in page.tsx catch blocks | mitigate | catch blocks set empty defaults (no error strings exposed to user); API errors do not surface stack traces (D-05/UI-SPEC Security Constraints) |
| T-75-03-SC | Tampering | npm installs | accept | No new external packages — no install tasks |
</threat_model>

<verification>
1. npm run typecheck exits 0 across all new section files
2. npm run build exits 0 — no build errors
3. TeamSection: two distinct blocks (team / external) confirmed by test
4. Member with slug → Link; without → span — confirmed by test
5. page.tsx calls getGroupContributors, getGroupThemes, getGroupReleaseMedia with try/catch
6. MediaSection always rendered in DOM (section id="medien" present regardless of items count)
7. No native button/input/select outside @/components/ui
8. All user-facing strings use correct German umlauts
9. No file exceeds 450 lines
10. page.tsx structure from Plan 75-02 preserved — only stubs and placeholder divs replaced
11. Human checkpoint: all seven sections visible and functional at `/anime/[id]/group/[groupId]`
</verification>

<success_criteria>
- All seven sections present at `/anime/[id]/group/[groupId]` (no missing sections, no hidden sections)
- D-07 member/contributor separation enforced at component level and confirmed by tests
- D-09 claim-based linking: slug → Link, null → span
- D-10 highlights + D-03 CTA to /releases working
- D-11 version labels explicitly documented as gap deferred to /releases depth (EpisodeReleaseSummary has no version_label field)
- D-14/D-15 media gallery always visible
- D-12/D-13 themes grouped by type, read-only
- Frontend builds and typechecks without errors
- Human UAT checkpoint passed
</success_criteria>

<output>
Create `.planning/phases/75-anime-gruppen-deep-dive-anime-id-group-groupid/75-03-SUMMARY.md` when done
</output>
