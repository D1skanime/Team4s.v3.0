---
phase: 101
plan: "02"
type: execute
wave: 1
depends_on:
  - "101-01"
files_modified:
  - "frontend/src/components/groups/GroupHistorySection.tsx"
  - "backend/internal/handlers/fansub_group_history_handler.go"
  - "backend/internal/repository/fansub_group_history_repository.go"
  - "backend/internal/handlers/fansub_group_history_handler_test.go"
autonomous: true
requirements_addressed:
  - "Phase 101 D-01"
  - "Phase 101 D-02"
must_haves:
  truths:
    - "Direct API writes cannot save history years before founded_year."
    - "Direct API writes cannot save history years after the current calendar year."
    - "Frontend save displays a local validation error before sending invalid years."
  artifacts:
    - path: "backend/internal/handlers/fansub_group_history_handler.go"
      provides: "Create/update year validation guard"
    - path: "backend/internal/repository/fansub_group_history_repository.go"
      provides: "Founded-year lookup or validation helper"
---

<objective>
Enforce the Phase 101 year rules at save time and verify the admin flow on the running app.
</objective>

<context>
@.planning/phases/101-meilensteine-zeitmanagement-und-anzeige-der-meilensteine-ver/101-CONTEXT.md
@.planning/phases/101-meilensteine-zeitmanagement-und-anzeige-der-meilensteine-ver/101-01-PLAN.md
@AGENTS.md
@docs/engineering/implementation-contract.md
@docs/api/api-contracts.md
@docs/frontend/auth-api-client.md
</context>

<read_first>
- `frontend/src/components/groups/GroupHistorySection.tsx` - current frontend save guards and `createGroupHistory` / `updateGroupHistory` payload construction.
- `frontend/src/lib/api.ts` - existing `createGroupHistory` and `updateGroupHistory` helpers; do not introduce ad hoc fetch calls.
- `backend/internal/handlers/fansub_group_history_handler.go` - current event type, single-use, and unlock guard sequence.
- `backend/internal/repository/fansub_group_history_repository.go` - current fansub history persistence and existing validation methods.
- `backend/internal/handlers/fansub_group_history_handler_test.go` if it exists; otherwise inspect nearby handler tests for pure guard testing style.
- `shared/contracts/openapi.yaml` - confirm no request/response shape changes are introduced.
</read_first>

<tasks>
<task id="T-101-02-01" type="execute">
<title>Add frontend pre-submit year validation</title>
<files>
- `frontend/src/components/groups/GroupHistorySection.tsx`
</files>
<action>
In `handleSubmit`, validate `yearValue` after parsing and before `createGroupHistory` / `updateGroupHistory`:

- If `yearValue !== null && foundedYear && yearValue < foundedYear`, set `saveError` to `Meilenstein-Jahr darf nicht vor dem Gründungsjahr liegen.` and return before the API call.
- If `yearValue !== null && yearValue > new Date().getFullYear()`, set `saveError` to `Meilenstein-Jahr darf nicht in der Zukunft liegen.` and return before the API call.
- Preserve the existing event-specific save guards and API helper usage.
</action>
<acceptance_criteria>
- `frontend/src/components/groups/GroupHistorySection.tsx` contains `Meilenstein-Jahr darf nicht vor dem Gründungsjahr liegen.`
- `frontend/src/components/groups/GroupHistorySection.tsx` contains `Meilenstein-Jahr darf nicht in der Zukunft liegen.`
- `frontend/src/components/groups/GroupHistorySection.tsx` still calls `createGroupHistory(` and `updateGroupHistory(` from `@/lib/api`.
</acceptance_criteria>
</task>

<task id="T-101-02-02" type="execute">
<title>Add backend founded/future year validation guard</title>
<files>
- `backend/internal/handlers/fansub_group_history_handler.go`
- `backend/internal/repository/fansub_group_history_repository.go`
</files>
<action>
Add backend validation for `groupHistoryCreateRequest.Year` and `groupHistoryPatchRequest.Year`:

- In `backend/internal/repository/fansub_group_history_repository.go`, add a small helper method such as `GetFansubFoundedYear(ctx context.Context, fansubGroupID int64) (*int, error)` that reads `founded_year` from `fansub_groups`.
- In `backend/internal/handlers/fansub_group_history_handler.go`, add a handler helper such as `validateGroupHistoryYear(c *gin.Context, fansubID int64, year *int) bool`.
- `validateGroupHistoryYear` returns true when `year == nil`.
- If founded year exists and `*year < *foundedYear`, return HTTP 422 with JSON error message `Meilenstein-Jahr darf nicht vor dem Gründungsjahr liegen.`
- If `*year > time.Now().Year()`, return HTTP 422 with JSON error message `Meilenstein-Jahr darf nicht in der Zukunft liegen.`
- Call the helper in `CreateGroupHistory` after request parsing and before `historyRepo.Create`.
- Call the helper in `UpdateGroupHistory` only when `req.Year != nil`; remember `req.Year` is `**int`, so explicit null must stay allowed.
- Add Go `time` import only in the handler file.
</action>
<acceptance_criteria>
- `backend/internal/handlers/fansub_group_history_handler.go` contains `validateGroupHistoryYear`.
- `backend/internal/handlers/fansub_group_history_handler.go` contains `time.Now().Year()`.
- `backend/internal/repository/fansub_group_history_repository.go` contains `GetFansubFoundedYear`.
- `backend/internal/handlers/fansub_group_history_handler.go` returns HTTP 422 for both invalid-year cases.
</acceptance_criteria>
</task>

<task id="T-101-02-03" type="tdd">
<title>Cover backend invalid year guards</title>
<files>
- `backend/internal/handlers/fansub_group_history_handler_test.go`
- `backend/internal/handlers/fansub_group_history_handler.go`
- `backend/internal/repository/fansub_group_history_repository.go`
</files>
<action>
Add focused tests for the backend guard. Prefer the existing handler test style if a `fansub_group_history_handler_test.go` already exists; otherwise create source-inspection tests that verify the guard strings, `time.Now().Year()`, and create/update call sites exist.

Coverage must include:

- create rejects `year < founded_year` with `Meilenstein-Jahr darf nicht vor dem Gründungsjahr liegen.`
- create rejects `year > current year` with `Meilenstein-Jahr darf nicht in der Zukunft liegen.`
- update with explicit `year: null` remains allowed by the guard path.
</action>
<acceptance_criteria>
- `backend/internal/handlers/fansub_group_history_handler_test.go` contains `TestGroupHistoryYearValidation`.
- `go test ./backend/internal/handlers ./backend/internal/repository` exits 0, or if the package path differs in this repo, the executed command covers both changed backend packages and exits 0.
</acceptance_criteria>
</task>

<task id="T-101-02-04" type="execute">
<title>Run checks and prepare live UAT on :3000</title>
<files>
- `.planning/phases/101-meilensteine-zeitmanagement-und-anzeige-der-meilensteine-ver/101-02-PLAN.md`
</files>
<action>
After implementation, run the focused checks and provide the user a test route:

- `npm --prefix frontend test -- GroupHistorySection.test.ts GroupHistoryForm.test.tsx`
- `npm --prefix frontend run typecheck`
- `go test ./backend/internal/handlers ./backend/internal/repository`
- `git diff --check`

Then confirm the live route for user UAT is `http://127.0.0.1:3000/admin/fansubs/{id}/edit` on the `Historie & Erfolge` / `Meilensteine` area.
</action>
<acceptance_criteria>
- Final implementation summary lists each command and whether it passed.
- User-facing UAT notes include: no founding year -> only locked `Gründung`; founded year -> `Gründung`, `Erstes Projekt`, `Erstes Release`; both first entries exist -> full catalog; year picker min/max.
</acceptance_criteria>
</task>
</tasks>

<threat_model>
## Threat Model

- Severity: Medium. Backend year validation must not reject explicit `null`, because existing optional timeline entries are valid.
- Severity: Medium. Validation must not attach history data to the wrong group; use the URL `fansubID` already validated by existing handlers.
- Severity: Low. Contract shape should not change; if a new error code field is introduced, `shared/contracts/openapi.yaml`, frontend DTOs, and api helpers must move together.
</threat_model>

<verification>
- `npm --prefix frontend test -- GroupHistorySection.test.ts GroupHistoryForm.test.tsx`
- `npm --prefix frontend run typecheck`
- `go test ./backend/internal/handlers ./backend/internal/repository`
- `git diff --check`
- Live UAT on `http://127.0.0.1:3000/admin/fansubs/{id}/edit`
</verification>

<success_criteria>
- Frontend and backend both enforce founded-year/current-year bounds.
- No ad hoc frontend fetch or auth handling is introduced.
- The existing `YearPicker` remains the only year control used for milestone timing.
- User can verify the staged selector behavior on port 3000.
</success_criteria>
