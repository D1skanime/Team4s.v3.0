---
phase: 16-hide-already-imported-anisearch-candidates-on-create
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - frontend/src/types/admin.ts
  - frontend/src/lib/api/admin-anime-intake.ts
  - frontend/src/lib/api.admin-anime.test.ts
  - frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts
  - frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts
  - frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx
  - frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.test.tsx
  - frontend/src/app/admin/anime/create/page.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "The create AniSearch chooser only renders candidates that are still valid for creating a new anime."
    - "When AniSearch found raw hits but all were hidden as already imported, the UI says so explicitly instead of pretending AniSearch returned zero hits."
    - "Exact-ID load and duplicate redirect behavior remain unchanged."
  artifacts:
    - path: "frontend/src/types/admin.ts"
      provides: "Typed AniSearch search response with filtered-existing metadata"
      contains: "filtered_existing_count"
    - path: "frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts"
      provides: "Create AniSearch search-state handling for hidden duplicates"
      contains: "isSearchingCandidates"
    - path: "frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx"
      provides: "Operator-visible filtered-empty messaging in the create AniSearch card"
      contains: "bereits"
  key_links:
    - from: "frontend/src/lib/api/admin-anime-intake.ts"
      to: "frontend/src/types/admin.ts"
      via: "typed AniSearch search response parsing"
      pattern: "AdminAnimeAniSearchSearchResponse"
    - from: "frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts"
      to: "frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx"
      via: "filtered candidate search feedback props"
      pattern: "filteredExistingCount"
---

<objective>
Update the create AniSearch UI and typed client so hidden duplicate candidates never appear as normal create hits and the empty-result state stays truthful.

Purpose: Once the backend filters duplicates, the frontend still needs to parse the widened response, preserve existing exact-ID behavior, and explain when AniSearch did find titles that were hidden because they already exist locally.
Output: Typed AniSearch search response handling, controller-level filtered-empty logic, and UI regressions for hidden duplicates plus the clarified copy.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/16-hide-already-imported-anisearch-candidates-on-create/16-CONTEXT.md
@.planning/phases/12-create-anisearch-intake-reintroduction-and-draft-merge-control/12-03-PLAN.md
@frontend/src/types/admin.ts
@frontend/src/lib/api/admin-anime-intake.ts
@frontend/src/lib/api.admin-anime.test.ts
@frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts
@frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts
@frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx
@frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.test.tsx
@frontend/src/app/admin/anime/create/page.test.tsx

<interfaces>
From `frontend/src/types/admin.ts`:
```ts
export interface AdminAnimeAniSearchSearchCandidate {
  anisearch_id: string
  title: string
  type: string
  year?: number
}

export interface AdminAnimeAniSearchSearchResponse {
  data: AdminAnimeAniSearchSearchCandidate[]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regressions for hidden duplicates and filtered-empty AniSearch feedback</name>
  <files>frontend/src/lib/api.admin-anime.test.ts, frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts, frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.test.tsx, frontend/src/app/admin/anime/create/page.test.tsx</files>
  <read_first>@frontend/src/lib/api.admin-anime.test.ts @frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts @frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.test.tsx @frontend/src/app/admin/anime/create/page.test.tsx</read_first>
  <acceptance_criteria>`frontend/src/lib/api.admin-anime.test.ts` asserts the AniSearch title-search helper parses `filtered_existing_count`; `frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts` contains a case where `data` is empty and `filtered_existing_count` is greater than zero, producing a message that mentions already-captured anime were hidden; `CreateAniSearchIntakeCard.test.tsx` or `page.test.tsx` asserts that hidden-duplicate copy renders without changing the existing exact-ID load or duplicate redirect CTA expectations.</acceptance_criteria>
  <action>Add frontend regressions before implementation. Extend the typed API helper tests to cover the widened AniSearch search response. Add a controller test where AniSearch title search returns `{ data: [], filtered_existing_count: 2 }` and assert the user-facing state does not say generic `Keine AniSearch-Treffer gefunden`, but instead mentions that two already captured anime were hidden. Add UI assertions proving the create card can render that hidden-duplicate message while still rendering the existing exact-ID controls and `Zum vorhandenen Anime wechseln` CTA for the explicit-load conflict state.</action>
  <verify>
    <automated>cd frontend && npm test -- src/lib/api.admin-anime.test.ts src/app/admin/anime/create/useAdminAnimeCreateController.test.ts src/app/admin/anime/create/CreateAniSearchIntakeCard.test.tsx src/app/admin/anime/create/page.test.tsx</automated>
  </verify>
  <done>The frontend regression suite describes the new hidden-duplicate contract before any production code changes land.</done>
</task>

<task type="auto">
  <name>Task 2: Wire filtered-result metadata through the create AniSearch client, controller, and card</name>
  <files>frontend/src/types/admin.ts, frontend/src/lib/api/admin-anime-intake.ts, frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts, frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx</files>
  <read_first>@frontend/src/types/admin.ts @frontend/src/lib/api/admin-anime-intake.ts @frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts @frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx</read_first>
  <acceptance_criteria>`frontend/src/types/admin.ts` models `filtered_existing_count` on the AniSearch search response; `useAdminAnimeCreateController.ts` stores or immediately interprets that field when title-search results are empty; `CreateAniSearchIntakeCard.tsx` renders copy that explicitly says already captured anime were hidden; the existing exact-ID `AniSearch laden` path, draft summary, and duplicate redirect state are unchanged.</acceptance_criteria>
  <action>Update the frontend AniSearch search DTO so `searchAdminAnimeCreateAniSearchCandidates(...)` returns both `data` and `filtered_existing_count`. In `useAdminAnimeCreateController.ts`, keep populating `aniSearchCandidates` from `response.data`, but when `response.data.length === 0` and `filtered_existing_count > 0`, set a dedicated message such as `Alle {count} gefundenen AniSearch-Treffer sind bereits als Anime erfasst und wurden ausgeblendet.` instead of the generic no-hits text. Pass any needed filtered-count or resolved message props into `CreateAniSearchIntakeCard.tsx`; keep the modal chooser behavior unchanged for non-empty candidate lists and do not alter the exact-ID `loadAniSearchDraftByID(...)` redirect behavior.</action>
  <verify>
    <automated>cd frontend && npm test -- src/lib/api.admin-anime.test.ts src/app/admin/anime/create/useAdminAnimeCreateController.test.ts src/app/admin/anime/create/CreateAniSearchIntakeCard.test.tsx src/app/admin/anime/create/page.test.tsx && npm run build</automated>
  </verify>
  <done>The create AniSearch flow stays duplicate-safe and operator-clear: no existing entries appear as fresh candidates, and filtered-empty searches explain why.</done>
</task>

</tasks>

<verification>
Plan 16-02 is complete when the typed client understands `filtered_existing_count`, the create AniSearch controller distinguishes filtered-empty from true no-hit states, and UI regressions pass without changing explicit-ID duplicate redirects.
</verification>

<success_criteria>
- The frontend never reintroduces hidden duplicate candidates through stale typing or fallback logic.
- Filtered-empty AniSearch searches show explicit already-imported messaging.
- Existing exact-ID load and duplicate redirect behavior remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/16-hide-already-imported-anisearch-candidates-on-create/16-02-SUMMARY.md`
</output>
