---
phase: 16-hide-already-imported-anisearch-candidates-on-create
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/services/anime_create_enrichment.go
  - backend/internal/services/anime_create_enrichment_test.go
  - backend/internal/handlers/admin_content_anime_enrichment_search.go
  - backend/internal/handlers/admin_content_test.go
  - backend/internal/models/admin_content.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "AniSearch create title search never returns a normal candidate when that candidate's `anisearch:{id}` source already exists on a local anime."
    - "Duplicate suppression happens in the backend AniSearch search seam, not only inside create-page rendering."
    - "The search response can distinguish between `AniSearch found nothing` and `AniSearch hits were filtered because they already exist locally`."
  artifacts:
    - path: "backend/internal/services/anime_create_enrichment.go"
      provides: "Duplicate-safe AniSearch search candidate shaping"
      contains: "SearchAniSearchCandidates"
    - path: "backend/internal/services/anime_create_enrichment_test.go"
      provides: "Service regressions for filtering already-imported AniSearch IDs"
      contains: "filtered"
    - path: "backend/internal/handlers/admin_content_anime_enrichment_search.go"
      provides: "HTTP response for filtered AniSearch create candidates"
      contains: "SearchAnimeCreateAniSearchCandidates"
  key_links:
    - from: "backend/internal/services/anime_create_enrichment.go"
      to: "backend/internal/repository/admin_content_anisearch.go"
      via: "source ownership lookup for candidate suppression"
      pattern: "ResolveAdminAnimeRelationTargetsBySources"
    - from: "backend/internal/handlers/admin_content_anime_enrichment_search.go"
      to: "backend/internal/models/admin_content.go"
      via: "search response envelope"
      pattern: "filtered_existing_count"
---

<objective>
Filter AniSearch create title-search results against existing local AniSearch ownership and lock the backend contract in tests before any UI changes.

Purpose: The user-facing bug starts with the backend returning raw AniSearch candidates that include entries already owned locally. Wave 1 fixes that at the authoritative source and exposes enough response detail for the UI to explain filtered-empty cases honestly.
Output: A duplicate-safe AniSearch search response, plus backend regression coverage for hidden existing IDs and filtered-result metadata.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/16-hide-already-imported-anisearch-candidates-on-create/16-CONTEXT.md
@backend/internal/handlers/admin_content_anime_enrichment_search.go
@backend/internal/services/anime_create_enrichment.go
@backend/internal/services/anime_create_enrichment_test.go
@backend/internal/repository/admin_content_anisearch.go
@backend/internal/models/admin_content.go

<interfaces>
From `backend/internal/services/anime_create_enrichment.go`:
```go
func (s *AnimeCreateEnrichmentService) SearchAniSearchCandidates(
  ctx context.Context,
  query string,
  limit int,
) ([]models.AdminAnimeAniSearchSearchCandidate, error)
```

From `backend/internal/repository/admin_content_anisearch.go`:
```go
func (r *AdminContentRepository) ResolveAdminAnimeRelationTargetsBySources(
  ctx context.Context,
  sources []string,
) ([]models.AdminAnimeSourceMatch, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock duplicate-safe AniSearch search behavior in service and handler regressions</name>
  <files>backend/internal/services/anime_create_enrichment_test.go, backend/internal/handlers/admin_content_test.go</files>
  <read_first>@backend/internal/services/anime_create_enrichment_test.go @backend/internal/handlers/admin_content_test.go @backend/internal/models/admin_content.go @.planning/phases/16-hide-already-imported-anisearch-candidates-on-create/16-CONTEXT.md</read_first>
  <acceptance_criteria>`backend/internal/services/anime_create_enrichment_test.go` contains a case where raw AniSearch search results include at least one locally owned `anisearch:{id}` and the returned candidate list omits that ID while preserving still-creatable IDs; `backend/internal/handlers/admin_content_test.go` asserts the HTTP response includes the filtered candidate list and a numeric `filtered_existing_count` when hidden duplicates were removed.</acceptance_criteria>
  <action>Add service and handler regressions before implementation. Create a service test where `stubAniSearchFetcher.SearchAnime(...)` returns at least two candidates, the stub repository resolves one matching `Source: "anisearch:{id}"`, and the service result only contains the still-new candidate. Add a handler-level JSON assertion that `GET /api/v1/admin/anime/enrichment/anisearch/search?q=...` returns `data` without the duplicate candidate and includes `filtered_existing_count: 1` when one result was hidden.</action>
  <verify>
    <automated>cd backend && go test ./internal/services ./internal/handlers -count=1</automated>
  </verify>
  <done>The regression suite proves already-imported AniSearch IDs are suppressed before the create UI renders them.</done>
</task>

<task type="auto">
  <name>Task 2: Implement backend filtering and filtered-result metadata on the AniSearch search seam</name>
  <files>backend/internal/services/anime_create_enrichment.go, backend/internal/handlers/admin_content_anime_enrichment_search.go, backend/internal/models/admin_content.go</files>
  <read_first>@backend/internal/services/anime_create_enrichment.go @backend/internal/repository/admin_content_anisearch.go @backend/internal/handlers/admin_content_anime_enrichment_search.go @backend/internal/models/admin_content.go</read_first>
  <acceptance_criteria>`backend/internal/services/anime_create_enrichment.go` resolves `anisearch:{id}` source ownership for the returned candidate batch and excludes matching IDs from the final `data` list; `backend/internal/models/admin_content.go` defines a response shape that includes `filtered_existing_count`; `backend/internal/handlers/admin_content_anime_enrichment_search.go` writes the new response envelope while keeping query validation and auth behavior unchanged.</acceptance_criteria>
  <action>Change `SearchAniSearchCandidates(...)` from a plain candidate slice mapper into a duplicate-aware search result builder. Construct source keys as literal `anisearch:{candidate.AniSearchID}` values, resolve them through the existing batched source lookup seam, drop any candidate whose source is already present locally, and count how many were removed. Add a typed response model in `backend/internal/models/admin_content.go` such as `AdminAnimeAniSearchSearchResult` plus `AdminAnimeAniSearchSearchResponse` with fields `data []AdminAnimeAniSearchSearchCandidate` and `filtered_existing_count int32`. Update the HTTP handler to serialize that response object without changing the route, query parameters, or existing validation messages.</action>
  <verify>
    <automated>cd backend && go test ./internal/services ./internal/handlers -count=1</automated>
  </verify>
  <done>The backend AniSearch title-search seam becomes the single source of truth for hiding already-imported create candidates and telling callers how many were suppressed.</done>
</task>

</tasks>

<verification>
Plan 16-01 is complete when backend AniSearch title search omits locally owned `anisearch:{id}` matches, reports how many candidates were filtered, and the service/handler regressions pass.
</verification>

<success_criteria>
- Duplicate AniSearch candidates are filtered before leaving the backend.
- The response contract exposes `filtered_existing_count` for honest UI feedback.
- Backend tests prove both mixed-result and filtered-empty scenarios.
</success_criteria>

<output>
After completion, create `.planning/phases/16-hide-already-imported-anisearch-candidates-on-create/16-01-SUMMARY.md`
</output>
