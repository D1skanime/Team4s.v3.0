---
phase: 05-relations-and-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/models/admin_content.go
  - backend/internal/repository/anime_relations_admin.go
  - backend/internal/repository/anime_relations_admin_test.go
  - backend/internal/handlers/admin_content_anime_relations.go
  - backend/internal/handlers/admin_content_anime_relations_test.go
  - frontend/src/types/admin.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/api.admin-anime.test.ts
autonomous: true
requirements:
  - RELA-01
  - RELA-02
  - RELA-03
  - RELA-04
  - RELA-05
  - RELA-06
  - RELA-07
  - RLY-01
must_haves:
  truths:
    - "Admins can load, search, create, update, and delete anime relations through authenticated admin endpoints instead of direct database work."
    - "The admin contract exposes only the approved V1 relation labels `Hauptgeschichte`, `Nebengeschichte`, `Fortsetzung`, and `Zusammenfassung` even though the database supports more raw relation types."
    - "Self-links, duplicate relations, missing targets, and unsupported labels fail with explicit operator-usable admin errors."
    - "All relation writes persist through the existing normalized `anime_relations` and `relation_types` tables rather than a second relation store."
  artifacts:
    - path: "backend/internal/repository/anime_relations_admin.go"
      provides: "Admin relation repository seam for list/search/create/update/delete against normalized relation tables"
    - path: "backend/internal/handlers/admin_content_anime_relations.go"
      provides: "Authenticated admin HTTP contract for relation list/search/create/update/delete with explicit validation failures"
    - path: "frontend/src/lib/api.ts"
      provides: "Typed admin client helpers for relation list/search/create/update/delete with structured ApiError transport"
  key_links:
    - from: "backend/internal/handlers/admin_content_anime_relations.go"
      to: "backend/internal/repository/anime_relations_admin.go"
      via: "admin relation list/search/create/update/delete calls"
      pattern: "ListAdminAnimeRelations|SearchAdminAnimeRelationTargets|CreateAdminAnimeRelation|UpdateAdminAnimeRelation|DeleteAdminAnimeRelation"
    - from: "backend/internal/repository/anime_relations_admin.go"
      to: "backend/internal/models/admin_content.go"
      via: "admin response and request DTOs for relations and target search rows"
      pattern: "AdminAnimeRelation|AdminAnimeRelationTarget"
    - from: "frontend/src/lib/api.ts"
      to: "backend/internal/handlers/admin_content_anime_relations.go"
      via: "typed admin fetch helpers for relation CRUD and target lookup"
      pattern: "getAdminAnimeRelations|searchAdminAnimeRelationTargets|createAdminAnimeRelation|updateAdminAnimeRelation|deleteAdminAnimeRelation"
---

<objective>
Create the backend and typed-client foundation for Phase 5 so relation editing has a reliable admin contract before any edit-route UI is built.

Purpose: close the missing authenticated relation data path, including perspective-aware reads, V1 label narrowing, and operator-safe validation failures.
Output: repository, handlers, DTOs, and API helpers for relation list/search/create/update/delete.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-reference-metadata-groundwork/05-CONTEXT.md
@.planning/phases/05-reference-metadata-groundwork/05-RESEARCH.md
@.planning/phases/05-reference-metadata-groundwork/05-VALIDATION.md
@.planning/phases/05-reference-metadata-groundwork/05-UI-SPEC.md
@database/migrations/0020_add_metadata_reference_tables.up.sql
@database/migrations/0021_add_normalized_metadata_tables.up.sql
@backend/internal/repository/anime_relations.go
@backend/internal/handlers/admin_content_anime.go
@frontend/src/lib/api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing coverage for admin relation CRUD, V1 label narrowing, and invalid mutation failures</name>
  <files>backend/internal/repository/anime_relations_admin_test.go, backend/internal/handlers/admin_content_anime_relations_test.go, frontend/src/lib/api.admin-anime.test.ts</files>
  <read_first>
    - backend/internal/repository/anime_relations.go
    - backend/internal/handlers/admin_content_anime.go
    - frontend/src/lib/api.ts
    - .planning/phases/05-reference-metadata-groundwork/05-CONTEXT.md
    - .planning/phases/05-reference-metadata-groundwork/05-RESEARCH.md
    - .planning/phases/05-reference-metadata-groundwork/05-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: listing admin relations returns entries from the current anime perspective rather than the raw public both-directions shape.
    - Test 2: target search returns existing anime rows suitable for live selection without exposing unsupported labels.
    - Test 3: create/update/delete succeed for the approved V1 labels only.
    - Test 4: self-link, duplicate, unsupported label, and missing target attempts fail with explicit admin errors.
    - Test 5: frontend API helpers preserve `ApiError.message`, `code`, or `details` for relation search and mutations.
  </behavior>
  <action>Create focused failing tests for the Phase 5 contract. In the repository and handler suites, pin the directional admin read shape, V1 label mapping, normalized-table persistence, and clear validation failures for create/update/delete. In `frontend/src/lib/api.admin-anime.test.ts`, pin the new relation client helpers so they preserve structured admin error payloads instead of collapsing them to generic fetch failures.</action>
  <acceptance_criteria>
    - Repository tests cover create, update, delete, self-link rejection, and duplicate rejection.
    - Handler tests cover relation list, target search, and at least one create failure with a German validation message.
    - Frontend API tests include one search failure and one mutation failure assertion with `ApiError.code` or `ApiError.details`.
    - The tests fail against any implementation that exposes labels outside the approved V1 set or writes to anything other than normalized relation tables.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/repository -run "Test.*AdminAnimeRelation.*" -count=1</automated>
    <automated>cd backend && go test ./internal/handlers -run "Test.*AdminAnimeRelation.*" -count=1</automated>
    <automated>cd frontend && npm test -- src/lib/api.admin-anime.test.ts</automated>
  </verify>
  <done>There is a red-green safety net for the authenticated relation contract, narrowed label set, and invalid mutation handling.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the admin relation repository, handlers, DTOs, and typed API helpers</name>
  <files>backend/internal/models/admin_content.go, backend/internal/repository/anime_relations_admin.go, backend/internal/repository/anime_relations_admin_test.go, backend/internal/handlers/admin_content_anime_relations.go, backend/internal/handlers/admin_content_anime_relations_test.go, frontend/src/types/admin.ts, frontend/src/lib/api.ts, frontend/src/lib/api.admin-anime.test.ts</files>
  <read_first>
    - backend/internal/models/admin_content.go
    - backend/internal/repository/anime_relations.go
    - backend/internal/handlers/admin_content_anime.go
    - frontend/src/types/admin.ts
    - frontend/src/lib/api.ts
    - .planning/phases/05-reference-metadata-groundwork/05-CONTEXT.md
    - .planning/phases/05-reference-metadata-groundwork/05-RESEARCH.md
    - .planning/phases/05-reference-metadata-groundwork/05-UI-SPEC.md
  </read_first>
  <action>Introduce a dedicated admin relation seam instead of mutating the public relation contract. In `backend/internal/repository/anime_relations_admin.go`, implement list/search/create/update/delete methods that operate on `anime_relations` plus `relation_types`, map the approved V1 labels to the existing normalized relation type names, preserve current-anime direction semantics, and reject self-links, duplicates, missing targets, and unsupported labels. In `backend/internal/handlers/admin_content_anime_relations.go`, expose authenticated admin endpoints for relation list, target search, create, update, and delete using the same operator-safe German error style as other admin content handlers. In `backend/internal/models/admin_content.go`, add narrow DTOs for relation rows, mutation requests, and target search results. In `frontend/src/types/admin.ts` and `frontend/src/lib/api.ts`, add typed helpers for the new endpoints with structured `ApiError` parsing so the edit route can surface failures clearly.</action>
  <acceptance_criteria>
    - The repository layer writes only to `anime_relations` and resolves relation type IDs from `relation_types`.
    - The handler layer exposes a relation list endpoint, a target-search endpoint, and create/update/delete endpoints guarded by admin auth.
    - The exposed admin contract allows only `Hauptgeschichte`, `Nebengeschichte`, `Fortsetzung`, and `Zusammenfassung`.
    - The frontend API client exports `getAdminAnimeRelations`, `searchAdminAnimeRelationTargets`, `createAdminAnimeRelation`, `updateAdminAnimeRelation`, and `deleteAdminAnimeRelation`.
    - All automated tests in this plan pass.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/repository -run "Test.*AdminAnimeRelation.*" -count=1</automated>
    <automated>cd backend && go test ./internal/handlers -run "Test.*AdminAnimeRelation.*" -count=1</automated>
    <automated>cd frontend && npm test -- src/lib/api.admin-anime.test.ts</automated>
  </verify>
  <done>The edit route now has a stable authenticated relation data contract built on the normalized relation tables.</done>
</task>

</tasks>

<verification>
Run the targeted backend and frontend relation contract tests, then manually confirm with a database-backed anime pair that a create and delete round-trip changes `anime_relations` without any direct SQL edits outside the application.
</verification>

<success_criteria>
Phase 5 has a trustworthy backend foundation: authenticated relation CRUD exists, the UI-safe V1 label set is enforced, invalid mutations fail clearly, and normalized tables remain the only persistence path.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reference-metadata-groundwork/05-01-SUMMARY.md`
</output>
