---
phase: 14-create-provider-search-separation-and-result-selection
plan: 03
type: execute
wave: 3
depends_on: [14-02]
files_modified:
  - frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts
  - frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts
  - frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx
  - frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.test.tsx
  - frontend/src/app/admin/anime/create/page.tsx
  - frontend/src/app/admin/anime/create/page.test.tsx
  - frontend/src/app/admin/anime/create/page.module.css
autonomous: true
requirements: []
must_haves:
  truths:
    - "AniSearch candidate selection happens through a popup chooser that lists title, type, year, and ID."
    - "Choosing a candidate reuses the existing exact-ID AniSearch load path rather than adding a second detail crawler."
    - "After candidate selection, the chosen record hydrates the draft directly and still shows explicit unsaved status."
  artifacts:
    - path: "frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx"
      provides: "AniSearch popup chooser and candidate-selection UI"
      contains: "Treffer laden"
    - path: "frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts"
      provides: "Selected-candidate handoff into the existing ID-based AniSearch draft load"
      contains: "loadAdminAnimeCreateAniSearchDraft"
    - path: "frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.test.tsx"
      provides: "Popup and candidate-selection regression coverage"
      contains: "Noch nichts gespeichert"
  key_links:
    - from: "frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx"
      to: "frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts"
      via: "candidate selection and existing ID-load action"
      pattern: "AniSearch"
    - from: "frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts"
      to: "frontend/src/lib/api/admin-anime-intake.ts"
      via: "reuse of exact-ID detail load after candidate selection"
      pattern: "loadAdminAnimeCreateAniSearchDraft"
---

<objective>
Finish Phase 14 by adding AniSearch candidate selection and wiring it into the existing ID-based AniSearch detail-load seam.

Purpose: This is the final user-facing step of the new AniSearch flow. Candidate search is already present; now the operator needs a controlled chooser that passes the selected candidate's ID into the existing draft-load path.
Output: Popup candidate chooser, selection handler, and regression coverage proving the selected AniSearch ID triggers the current detail loader and updates the draft with explicit unsaved feedback.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/phases/14-create-provider-search-separation-and-result-selection/14-CONTEXT.md
@.planning/phases/14-create-provider-search-separation-and-result-selection/14-UI-SPEC.md
@.planning/phases/14-create-provider-search-separation-and-result-selection/14-02-PLAN.md
@frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts
@frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx
@frontend/src/app/admin/anime/create/page.tsx
@frontend/src/app/admin/anime/create/page.module.css
@frontend/src/lib/api/admin-anime-intake.ts

<interfaces>
From `frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts`:
```ts
async function handleAniSearchDraftLoad() {
  const requestPayload = {
    anisearch_id: anisearchID,
    draft: ...
  }
  const response = await loadAdminAnimeCreateAniSearchDraft(requestPayload, runtimeAuthToken)
}
```

From `frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx`:
```tsx
interface CreateAniSearchIntakeCardProps {
  anisearchID: string
  result: CreateAniSearchDraftState | null
  onSubmit: () => void
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regressions for popup-based candidate selection and existing ID-loader reuse</name>
  <files>frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.test.tsx, frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts, frontend/src/app/admin/anime/create/page.test.tsx</files>
  <read_first>@frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.test.tsx @frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts @frontend/src/app/admin/anime/create/page.test.tsx @.planning/phases/14-create-provider-search-separation-and-result-selection/14-UI-SPEC.md</read_first>
  <acceptance_criteria>`CreateAniSearchIntakeCard.test.tsx` asserts popup candidate rows show title, type, year, and ID plus `Treffer laden` and `Abbrechen`; `useAdminAnimeCreateController.test.ts` contains a case proving selecting a candidate calls the existing exact-ID load path with the chosen candidate ID; `page.test.tsx` confirms candidate selection still results in unsaved draft feedback, not immediate persistence.</acceptance_criteria>
  <action>Add tests that describe the final AniSearch interaction contract before implementation: title search opens a popup chooser, candidate rows expose title/type/year/ID, selecting one candidate passes only that chosen AniSearch ID into the already-existing `loadAdminAnimeCreateAniSearchDraft(...)` path, and the resulting UI still says the draft is unsaved. Keep these regressions focused on popup behavior, ID-path reuse, and draft hydration; do not add edit-route or broader crawler assertions.</action>
  <verify>
    <automated>cd frontend && npm test -- src/app/admin/anime/create/CreateAniSearchIntakeCard.test.tsx src/app/admin/anime/create/useAdminAnimeCreateController.test.ts src/app/admin/anime/create/page.test.tsx</automated>
  </verify>
  <done>The regression suite proves candidate selection uses the existing ID-based load seam and keeps the create flow draft-first and unsaved.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the AniSearch popup chooser and hand selected IDs into the existing draft loader</name>
  <files>frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts, frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx, frontend/src/app/admin/anime/create/page.tsx, frontend/src/app/admin/anime/create/page.module.css</files>
  <read_first>@frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts @frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx @frontend/src/app/admin/anime/create/page.module.css @frontend/src/app/admin/anime/create/createPageHelpers.ts @.planning/phases/14-create-provider-search-separation-and-result-selection/14-UI-SPEC.md</read_first>
  <acceptance_criteria>`CreateAniSearchIntakeCard.tsx` renders a popup/modal chooser when AniSearch candidates exist; popup rows show title, type, year, and AniSearch ID; choosing one candidate triggers the existing `loadAdminAnimeCreateAniSearchDraft(...)` path with that selected ID; the popup can be cancelled without mutating the draft; successful selection still renders `Noch nichts gespeichert` or equivalent unsaved draft messaging.</acceptance_criteria>
  <action>Implement a popup/modal candidate chooser inside `CreateAniSearchIntakeCard.tsx` using the existing card as the owning source surface and the Phase 14 UI contract for spacing, radius, and copy. Extend `useAdminAnimeCreateController.ts` with the minimal state needed to open/close the candidate chooser, track the selected candidate, and call the existing `handleAniSearchDraftLoad` logic with the chosen candidate's AniSearch ID instead of inventing a second detail-fetch implementation. Update `page.tsx` and `page.module.css` only as needed to support popup rendering and source-local status transitions. Keep the exact-ID AniSearch path unchanged and available in parallel, and do not trigger detail load for every candidate returned by title search.</action>
  <verify>
    <automated>cd frontend && npm test -- src/app/admin/anime/create/CreateAniSearchIntakeCard.test.tsx src/app/admin/anime/create/useAdminAnimeCreateController.test.ts src/app/admin/anime/create/page.test.tsx && npm run build</automated>
  </verify>
  <done>The AniSearch title-search flow ends in a popup chooser whose selected candidate reuses the existing ID-based AniSearch draft loader and updates the draft safely.</done>
</task>

</tasks>

<verification>
Plan 14-03 is complete when AniSearch candidate selection happens through a popup, the chosen ID is routed through the existing exact-ID loader, and the create route still behaves as an unsaved draft-first flow.
</verification>

<success_criteria>
- AniSearch candidate selection is deliberate and popup-based.
- Candidate selection reuses the existing exact-ID AniSearch load path.
- Draft hydration and unsaved feedback remain explicit after selection.
</success_criteria>

<output>
After completion, create `.planning/phases/14-create-provider-search-separation-and-result-selection/14-03-SUMMARY.md`
</output>
