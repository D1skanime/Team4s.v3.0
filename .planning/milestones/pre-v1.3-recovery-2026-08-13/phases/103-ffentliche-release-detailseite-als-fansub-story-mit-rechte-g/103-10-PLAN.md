---
phase: 103
plan: "10"
type: execute
gap_closure: true
wave: 2
depends_on: ["103-06"]
files_modified:
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseGallery.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseGallery.module.css
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseGallery.test.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/responsiveGalleryReveal.ts
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/responsiveGalleryReveal.test.ts
  - frontend/src/components/fansubs/FansubMediaLightbox.tsx
  - frontend/src/components/fansubs/FansubMediaLightbox.test.tsx
requirements_addressed: [P103-UAT2]
must_haves:
  truths:
    - "All release-version images render in one responsive grid and open originals in the existing public lightbox."
    - "No zero-count reveal action appears; category/uploader and full description remain available."
    - "Collapsed reveal is exactly 6 desktop, 4 tablet, and 2 mobile from one breakpoint-aware state source."
---

<objective>Replace obsolete category chapters with one scalable release image grid while reusing/generalizing the established public Fansub lightbox and retaining release-version category pagination.</objective>

<read_first>
- `AGENTS.md`
- `.planning/debug/103-release-image-gallery.md`
- `docs/engineering/implementation-contract.md`
- `docs/frontend/ui-system.md`
- `frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseGallery.tsx`
- `frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseGallery.module.css`
- `frontend/src/types/releaseDetail.ts`
- `frontend/src/components/fansubs/FansubGroupMediaBlock.tsx`
- `frontend/src/components/fansubs/FansubMediaLightbox.tsx`
- `frontend/src/components/fansubs/FansubMediaLightbox.module.css`
- `frontend/src/types/fansub.ts`
- `frontend/src/lib/api.ts` (`getGroupReleaseImages`)
</read_first>

<tasks>
<task type="auto">
  <name>Task 1: Generalize the existing public image lightbox seam</name>
  <action>Extract the narrow image item shape needed by `FansubMediaLightbox` so Fansub and release images adapt into the same Modal/keyboard/focus/original-image behavior. Do not build a second release lightbox or change media ownership/API.</action>
  <verify>Shared lightbox tests cover original URL, full description, previous/next, Escape/close, counter, and both adapters.</verify>
</task>
<task type="auto">
  <name>Task 2: Render one responsive release grid with correct reveal state</name>
  <action>Flatten the stable deduplicated loaded items into one grid. Each semantic thumbnail button shows clamped caption plus category Badge and uploader metadata and opens the global-index lightbox. Implement `responsiveGalleryReveal.ts` as the single source of truth for named breakpoints and collapsed limits: desktop `6`, tablet `4`, mobile `2`. Its client hook owns `matchMedia`/resize subscription, current limit, and an `expanded` boolean. `ReleaseGallery` must use that one returned limit for the rendered slice, `remaining = max(0, total - visibleCount)`, button label, and button visibility; CSS may control grid columns but must not hide items with independent `nth-child` limits. While collapsed, viewport transitions recompute 6/4/2 and the remaining label immediately. Once expanded, resize never recollapses; reveal fetches every outstanding category cursor independently, merges/deduplicates into the flat list, and shows all loaded results. Render no action when remaining is zero, including exactly 6/4/2 totals.</action>
  <verify>`responsiveGalleryReveal.test.ts` drives `matchMedia`/resize transitions and asserts desktop 6, tablet 4, mobile 2, collapsed transitions update visible/remaining, and expanded state survives resize. `ReleaseGallery.test.tsx` covers exact 6/4/2 slices and labels, totals equal to limits produce zero remaining/no button, uneven outstanding category cursors all fetch independently, duplicate-free flat merge, and resize/expanded behavior. Also cover one grid, clickable original/full caption, category, and uploader.</verify>
</task>
</tasks>

<verification>
- commit each task atomically after its focused checks pass
- focused gallery/lightbox tests
- `npm --prefix frontend run typecheck`
- `npm --prefix frontend run build`
- `git diff --check`
- live browser at explicit desktop/tablet/mobile widths: verify exactly 6/4/2 collapsed items and matching remaining label, resize transitions, expanded state retention, outstanding-category fetch, lightbox keyboard navigation and focus return
</verification>

<success_criteria>One in-page grid uses one tested 6/4/2 responsive reveal state for slicing/counts/resize, preserves canonical release_version_media category cursors, never offers a zero action, and fully reuses the public lightbox experience.</success_criteria>
