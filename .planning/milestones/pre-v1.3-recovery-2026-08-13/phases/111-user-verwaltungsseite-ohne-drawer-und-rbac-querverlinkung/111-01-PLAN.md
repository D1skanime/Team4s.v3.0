---
phase: 111-user-verwaltungsseite-ohne-drawer-und-rbac-querverlinkung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/repository/authz_capability_mutations.go
  - backend/internal/handlers/admin_capability_handler.go
  - backend/internal/handlers/admin_capability_handler_test.go
  - shared/contracts/admin-capabilities.yaml
  - frontend/src/types/admin-capability.ts
autonomous: true
requirements: [D-05]

must_haves:
  truths:
    - "D-05: /admin/role-capabilities kann pro Rolle einen Impact-Count anzeigen, der ausschließlich globale Rollenzuweisungen zählt (platform_admin/content_admin/user)."
    - "Die drei globalen App-Rollen existieren nicht in role_definitions (Pitfall 1) — die API liefert sie trotzdem als eigene, nicht-editierbare Zeilen mit korrektem Zählwert."
  artifacts:
    - path: "backend/internal/repository/authz_capability_mutations.go"
      provides: "CountGlobalRoleAssignments() Aggregat-Query gegen app_user_global_roles"
    - path: "backend/internal/handlers/admin_capability_handler.go"
      provides: "ListCapabilityMatrix-Handler mergt 3 synthetische global_app_role-Zeilen mit global_assignment_count"
    - path: "frontend/src/types/admin-capability.ts"
      provides: "RoleEntry.global_assignment_count / RoleEntry.role_kind für Frontend-Konsumenten (Plan 111-05)"
  key_links:
    - from: "admin_capability_handler.go ListCapabilityMatrix"
      to: "authz_capability_mutations.go CountGlobalRoleAssignments"
      via: "capabilityMutationRepo interface"
      pattern: "CountGlobalRoleAssignments"
---

<objective>
Backend-Fundament für D-05 (RBAC Impact-Count): `GET /api/v1/admin/role-capabilities` liefert zusätzlich zu den bestehenden `role_definitions`-Zeilen drei synthetische, nicht-editierbare Zeilen für die globalen App-Rollen `platform_admin`/`content_admin`/`user` mit einem `global_assignment_count`-Feld (Anzahl aktiver Zuweisungen aus `app_user_global_roles`). Löst die in 111-RESEARCH.md Pitfall 1 dokumentierte Datenmodell-Lücke: diese drei Rollen kommen in `role_definitions` strukturell nie vor, daher kann `RoleMasterList` (Plan 111-05) ohne diese Erweiterung keinen Impact-Count anzeigen.

Purpose: Schafft den Daten-Contract (Go-Struct + OpenAPI + TS-Typ), auf dem Plan 111-05 (Frontend-Darstellung des Impact-Counts) aufbaut. Reine Backend-/Contract-Arbeit, keine UI-Änderung in diesem Plan.
Output: Erweiterte `CapabilityMatrixRoleEntry` (Go), erweitertes `RoleEntry` (TypeScript + OpenAPI), neue Repository-Methode `CountGlobalRoleAssignments`, Handler-Merge-Logik, grüner Go-Test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-user-verwaltungsseite-ohne-drawer-und-rbac-querverlinkung/111-CONTEXT.md
@.planning/phases/111-user-verwaltungsseite-ohne-drawer-und-rbac-querverlinkung/111-RESEARCH.md

<interfaces>
<!-- Aktueller Stand, den dieser Plan erweitert. Executor soll NICHT erneut suchen. -->

Aus `backend/internal/repository/authz_capability_mutations.go:29-37`:
```go
type CapabilityMatrixRoleEntry struct {
    RoleCode           string                        `json:"role_code"`
    LabelDE            string                        `json:"label_de"`
    Actions            []CapabilityMatrixActionState `json:"actions"`
    Assignable         bool                          `json:"assignable"`
    CapabilityEditable bool                          `json:"capability_editable"`
    Contexts           []string                      `json:"contexts,omitempty"`
}
```

Aus `backend/internal/handlers/admin_capability_handler.go:22-28` (Interface, das die Handler-Post-Processing-Loop bedient):
```go
type capabilityMutationRepo interface {
    ListCapabilityMatrix(ctx context.Context) (*repository.CapabilityMatrix, error)
    GrantRoleCapability(ctx context.Context, roleCode, actionCode string) error
    RevokeRoleCapability(ctx context.Context, roleCode, actionCode string) error
    CountRolesWithAction(ctx context.Context, actionCode string) (int64, error)
    LoadRoleCapabilities(ctx context.Context) (map[string][]permissions.Action, error)
}
```

Aus `backend/internal/handlers/admin_capability_handler.go:63-90` (bestehende Enrichment-Stelle, NACH der dieser Plan die Synthetic-Rows anhängt):
```go
func (h *AdminCapabilityHandler) ListCapabilityMatrix(c *gin.Context) {
    _, ok := requirePlatformAdminIdentity(c, h.authzRepo, "")
    if !ok { return }
    matrix, err := h.mutationRepo.ListCapabilityMatrix(c.Request.Context())
    // ... error handling ...
    for i := range matrix.Roles {
        matrix.Roles[i].Assignable = permissions.IsKnownFansubGroupRole(matrix.Roles[i].RoleCode)
        matrix.Roles[i].CapabilityEditable = permissions.IsCapabilityBearingRole(matrix.Roles[i].RoleCode)
    }
    c.JSON(http.StatusOK, matrix)
}
```

Aus `backend/internal/repository/admin_users_repository.go:192` (kanonische Liste der drei globalen Rollen, bereits im Code hartkodiert):
```go
AssignableRoles: []string{"platform_admin", "content_admin", "user"},
```

Aus `backend/internal/repository/admin_users_queries.go:51-54` (Sibling-Aggregat gegen dieselbe Tabelle, als SQL-Analog):
```sql
LEFT JOIN LATERAL (
    SELECT ARRAY_AGG(role ORDER BY role) AS roles
    FROM app_user_global_roles WHERE app_user_id = page.id
) roles ON true
```

Aus `frontend/src/app/admin/users/tabs/UserGlobalRolesTab.tsx:34-46` (Quelle der deutschen Labels — MUSS mit den neuen Backend-Labels synchron bleiben, Pitfall 2):
```typescript
function roleLabel(role: string): string {
  switch (role) {
    case 'platform_admin': return 'Plattform-Admin'
    case 'content_admin': return 'Content-Admin'
    case 'user': return 'Benutzer'
    default: return role
  }
}
```

Aus `frontend/src/types/admin-capability.ts:16-30` (TS-Typ, den dieser Plan erweitert):
```typescript
export interface RoleEntry {
  role_code: string;
  label_de: string;
  actions: RoleActionState[];
  assignable?: boolean;
  capability_editable?: boolean;
  contexts?: string[];
}
```

Aus `backend/internal/handlers/admin_capability_handler_test.go:22-64` (Test-Double-Muster, das erweitert wird):
```go
type stubCapabilityAuthzRepo struct {
    isPlatformAdmin       bool
    countRolesWithAction  int64
    countErr              error
    grantErr              error
    revokeErr             error
    matrixRoles           []repository.CapabilityMatrixRoleEntry
}
func (s *stubCapabilityAuthzRepo) ListCapabilityMatrix(_ context.Context) (*repository.CapabilityMatrix, error) {
    roles := s.matrixRoles
    if roles == nil { roles = []repository.CapabilityMatrixRoleEntry{} }
    return &repository.CapabilityMatrix{Roles: roles, AllActions: []repository.CapabilityMatrixActionEntry{}}, nil
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contracts erweitern — Go-Struct, OpenAPI, TypeScript-Typ synchron halten</name>
  <files>backend/internal/repository/authz_capability_mutations.go, shared/contracts/admin-capabilities.yaml, frontend/src/types/admin-capability.ts</files>
  <read_first>
    backend/internal/repository/authz_capability_mutations.go (CapabilityMatrixRoleEntry, Zeilen 29-37)
    shared/contracts/admin-capabilities.yaml (RoleEntry-Schema, Zeilen 276-297)
    frontend/src/types/admin-capability.ts (RoleEntry-Interface, Zeilen 16-30)
  </read_first>
  <action>
    Füge in `CapabilityMatrixRoleEntry` (authz_capability_mutations.go:30-37) zwei neue Felder hinzu: `GlobalAssignmentCount *int` mit Tag `json:"global_assignment_count,omitempty"` (Pointer, damit `null`/fehlend vom nicht-zählbaren Fall unterscheidbar bleibt — role_definitions-Zeilen bekommen NIE dieses Feld gesetzt) und `RoleKind string` mit Tag `json:"role_kind,omitempty"` (Wert `"global_app_role"` nur für die drei in Task 2 hinzugefügten synthetischen Zeilen, leerer String für alle bestehenden role_definitions-Zeilen). Kommentiere beide Felder mit einem Verweis auf Pitfall 1 (111-RESEARCH.md) und auf die beiden Gegenstücke (OpenAPI-Schema + TS-Interface), damit die drei Stellen synchron bleiben (Pitfall 2). Ergänze in `shared/contracts/admin-capabilities.yaml` im `RoleEntry`-Schema (nach `contexts`, Zeile 297) die zwei neuen optionalen Properties `global_assignment_count` (type integer, nullable: true, description referenziert die 3 globalen App-Rollen) und `role_kind` (type string, description: "global_app_role für die drei synthetischen globalen App-Rollen-Zeilen (platform_admin/content_admin/user), sonst leer/fehlend"). Ergänze in `frontend/src/types/admin-capability.ts` im `RoleEntry`-Interface (nach `contexts`, Zeile 29) `global_assignment_count?: number | null;` und `role_kind?: 'global_app_role' | string;` mit demselben Kommentarverweis.
  </action>
  <acceptance_criteria>
    grep -n "global_assignment_count" backend/internal/repository/authz_capability_mutations.go shared/contracts/admin-capabilities.yaml frontend/src/types/admin-capability.ts liefert in allen drei Dateien mindestens einen Treffer an der jeweiligen RoleEntry/CapabilityMatrixRoleEntry-Definitionsstelle.
    cd backend && go build ./... beendet ohne Fehler.
    cd frontend && npx tsc --noEmit zeigt keine neuen Fehler, die auf admin-capability.ts zurückgehen.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && cd ../frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Go-Struct, OpenAPI-Schema und TS-Interface tragen alle drei `global_assignment_count`/`role_kind`; Build und Typecheck grün.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CountGlobalRoleAssignments + Handler-Merge synthetischer globaler Rollen-Zeilen</name>
  <files>backend/internal/repository/authz_capability_mutations.go, backend/internal/handlers/admin_capability_handler.go, backend/internal/handlers/admin_capability_handler_test.go</files>
  <read_first>
    backend/internal/handlers/admin_capability_handler.go (capabilityMutationRepo-Interface Zeilen 22-28, ListCapabilityMatrix-Handler Zeilen 63-90)
    backend/internal/handlers/admin_capability_handler_test.go (stubCapabilityAuthzRepo Zeilen 22-64, TestListCapabilityMatrixAssignableEnrichment Zeilen 344-417)
    backend/internal/repository/authz_capability_mutations.go (ListCapabilityMatrix-Repo-Methode Zeilen 61-163)
    backend/internal/repository/admin_users_repository.go (Zeile 192, kanonische globale-Rollen-Liste)
    frontend/src/app/admin/users/tabs/UserGlobalRolesTab.tsx (Zeilen 34-46, deutsche Labels)
  </read_first>
  <behavior>
    - Test 1: `ListCapabilityMatrix`-Handler-Response enthält zusätzlich zu den role_definitions-Zeilen genau drei Zeilen mit `role_code` `platform_admin`/`content_admin`/`user`, `role_kind="global_app_role"`, `assignable=false`, `capability_editable=false`, `global_assignment_count` gleich dem Stub-Zählwert (inkl. 0 für eine im Stub fehlende Rolle).
    - Test 2: Eine bestehende `role_definitions`-Zeile (z. B. `fansub_lead`) behält `global_assignment_count` als `null`/abwesend und `role_kind=""` — kein Bruch von `TestListCapabilityMatrixAssignableEnrichment`.
  </behavior>
  <action>
    RED: Ergänze in `admin_capability_handler_test.go` das Feld `globalRoleCounts map[string]int` auf `stubCapabilityAuthzRepo` sowie die Methode `func (s *stubCapabilityAuthzRepo) CountGlobalRoleAssignments(_ context.Context) (map[string]int, error) { return s.globalRoleCounts, nil }`. Schreibe den neuen Test `TestListCapabilityMatrixIncludesSyntheticGlobalRoleEntries`: Stub mit `globalRoleCounts: map[string]int{"platform_admin": 3, "content_admin": 0}` (bewusst ohne `"user"`-Eintrag, um den Default-0-Fall zu prüfen) und einer normalen `role_definitions`-Zeile (`fansub_lead`); rufe `h.ListCapabilityMatrix(c)` auf; parse die JSON-Response und prüfe: Response enthält 4 Rollen (1 role_definitions + 3 synthetisch), die drei synthetischen Zeilen haben `role_kind="global_app_role"` und `global_assignment_count` 3/0/0, die `fansub_lead`-Zeile hat `role_kind` leer und `global_assignment_count` `null`. Lauf bestätigen: RED (Compile-Fehler, da `CountGlobalRoleAssignments` weder im Interface noch im Repository existiert).
    GREEN: Füge `func (r *AuthzRepository) CountGlobalRoleAssignments(ctx context.Context) (map[string]int, error)` in `authz_capability_mutations.go` hinzu — Query `SELECT role, COUNT(*) AS cnt FROM app_user_global_roles GROUP BY role`, baut `map[string]int{role: count}`. Erweitere das `capabilityMutationRepo`-Interface in `admin_capability_handler.go` um dieselbe Methodensignatur. Im `ListCapabilityMatrix`-Handler (nach der bestehenden Assignable/CapabilityEditable-Schleife, Zeile 87): rufe `counts, err := h.mutationRepo.CountGlobalRoleAssignments(ctx)` auf; bei Fehler nur loggen und mit leerer Map fortfahren (fail-open, analog zum bestehenden ReloadCache-Fail-Safe-Muster); baue eine lokale Konstante/Map `globalRoleLabels := map[string]string{"platform_admin": "Plattform-Admin", "content_admin": "Content-Admin", "user": "Benutzer"}` (synchron zu `UserGlobalRolesTab.tsx roleLabel()`, Kommentar mit Gegenverweis); für jede der drei Rollen in fester Reihenfolge `["platform_admin", "content_admin", "user"]` einen `repository.CapabilityMatrixRoleEntry` mit `Actions: []repository.CapabilityMatrixActionState{}`, `Assignable: false`, `CapabilityEditable: false`, `RoleKind: "global_app_role"`, `GlobalAssignmentCount:` Pointer auf `counts[role]` (Go-Zero-Value 0 wenn Key fehlt) bauen und diese drei VOR `matrix.Roles` voranstellen (`matrix.Roles = append(syntheticRoles, matrix.Roles...)`), sodass die globalen App-Rollen konsistent am Anfang der Liste erscheinen (RESEARCH Open Question 2). Lauf bestätigen: GREEN, und `TestListCapabilityMatrixAssignableEnrichment` bleibt grün.
  </action>
  <acceptance_criteria>
    <automated>cd backend && go test ./internal/handlers/... -run TestListCapabilityMatrixIncludesSyntheticGlobalRoleEntries -v</automated>
    cd backend && go test ./internal/handlers/... -run TestListCapabilityMatrixAssignableEnrichment -v bleibt grün (keine Regression).
    cd backend && go build ./... beendet ohne Fehler.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/handlers/... -run "TestListCapabilityMatrixIncludesSyntheticGlobalRoleEntries|TestListCapabilityMatrixAssignableEnrichment" -v</automated>
  </verify>
  <done>ListCapabilityMatrix-Response enthält die 3 synthetischen global_app_role-Zeilen mit korrektem global_assignment_count; bestehender Enrichment-Test bleibt grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin-Client → `GET /api/v1/admin/role-capabilities` | Bereits hinter `requirePlatformAdminIdentity`; dieser Plan ändert nur die Response-Form, keine neue Eingabefläche |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01 | Information Disclosure | Neues `global_assignment_count`-Feld in bestehendem Endpoint | accept | Endpoint ist bereits Platform-Admin-only (`requirePlatformAdminIdentity`, unverändert); die Zahl aggregiert dieselben Daten, die auf `/admin/users` mit `global_role`-Filter ohnehin einsehbar sind — kein neues Datenleck. |
| T-111-02 | Tampering | Neue Aggregat-Query `CountGlobalRoleAssignments` (kein User-Input) | accept | Query hat keine Parameter (`GROUP BY` ohne WHERE-Bedingung aus Request-Daten) — keine Injection-Fläche. |

Keine neuen high-severity Threats — bestehendes `requirePlatformAdminIdentity`-Gate greift unverändert auf diesem Endpoint (V4 Access Control, siehe 111-RESEARCH.md Security Domain).
</threat_model>

<verification>
- `cd backend && go build ./...` erfolgreich.
- `cd backend && go test ./internal/handlers/... ./internal/repository/...` grün.
- `cd frontend && npx tsc --noEmit` zeigt keine neuen Fehler in `admin-capability.ts`.
- Nach Docker-Rebuild (`docker compose up -d --build team4sv30-backend`): `GET /api/v1/admin/role-capabilities` (mit Platform-Admin-Token, siehe `reference_live_api_token_verify`-Memory) liefert im `roles`-Array drei Einträge mit `role_kind="global_app_role"`.
</verification>

<success_criteria>
- `CapabilityMatrixRoleEntry`/`RoleEntry` (Go + TS + OpenAPI) tragen `global_assignment_count` und `role_kind`.
- `ListCapabilityMatrix`-Handler liefert 3 synthetische globale-App-Rollen-Zeilen mit korrektem Zählwert aus `app_user_global_roles`.
- Bestehende Tests (`TestListCapabilityMatrixAssignableEnrichment`, `TestGrantCapabilityRequiresPlatformAdmin`, etc.) bleiben grün.
</success_criteria>

<output>
Create `.planning/phases/111-user-verwaltungsseite-ohne-drawer-und-rbac-querverlinkung/111-01-SUMMARY.md` when done
</output>
