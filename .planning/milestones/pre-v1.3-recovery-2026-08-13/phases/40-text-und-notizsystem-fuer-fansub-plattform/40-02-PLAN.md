---
phase: 40-text-und-notizsystem-fuer-fansub-plattform
plan_id: 40-02
title: DB Migration 0065 — contributor_roles Cleanup und 11 Kernrollen
wave: 1
depends_on: []
estimated_minutes: 15
autonomous: true
files_modified:
  - database/migrations/0065_seed_contributor_roles_kernrollen.up.sql
  - database/migrations/0065_seed_contributor_roles_kernrollen.down.sql

must_haves:
  truths:
    - "contributor_roles enthält nach Migration genau die 11 Kernrollen (lowercase)"
    - "Jede Kernrolle hat label und description entsprechend der CONTEXT.md-Rollentabelle"
    - "Alle FKs auf contributor_roles (release_member_roles, member_episode_notes) bleiben intakt"
  artifacts:
    - path: database/migrations/0065_seed_contributor_roles_kernrollen.up.sql
      provides: TRUNCATE + INSERT der 11 Kernrollen mit label und description
      contains: "TRUNCATE contributor_roles CASCADE"
  key_links:
    - from: release_member_roles
      to: contributor_roles.id
      via: role_id FK — CASCADE-Auswirkung beim TRUNCATE beachten
---

## Goal

Die bestehende `contributor_roles`-Tabelle enthält 6 veraltete Seeds (Translator, Timer, Typesetter, Encoder, QC, Karaoke als Test-/Platzhalter-Daten). Laut User-Entscheidung darf die Tabelle modifiziert/geleert werden. Diese Migration ersetzt alle bestehenden Seeds durch die 11 Kernrollen mit korrekten Labels und Descriptions.

**Wichtig:** `TRUNCATE contributor_roles CASCADE` löscht auch abhängige Zeilen in `release_member_roles` und `member_episode_notes`. Das ist bei dieser Tabelle mit Test-Daten akzeptiert (User-Entscheidung: "seeds are test data only").

Die `contributor_roles`-Tabelle hat aktuell nur `id`, `name`, `created_at` — kein `label`- oder `description`-Feld. Die Migration muss diese Spalten zuerst hinzufügen.

## Context

- `40-CONTEXT.md` — vollständige Rollentabelle mit Labels, Hilfetexten und Placeholders
- `RESEARCH.md` — Befund: 6 alte Seeds, Tabellenschema (`id`, `name`, `created_at`)
- `database/migrations/0044_add_db_schema_v2_target_tables.up.sql` — ursprüngliche contributor_roles-Definition

## Tasks

### Task 1: Migration 0065 — contributor_roles erweitern und neu seedden

Erstelle `database/migrations/0065_seed_contributor_roles_kernrollen.up.sql`:

Die Tabelle `contributor_roles` hat derzeit nur `id BIGSERIAL`, `name VARCHAR(80)`, `created_at`. Zuerst `label` und `description` ergänzen, dann TRUNCATE CASCADE, dann 11 Kernrollen einfügen.

```sql
-- Spalten label und description hinzufügen falls nicht vorhanden
ALTER TABLE contributor_roles
    ADD COLUMN IF NOT EXISTS label VARCHAR(100) NOT NULL DEFAULT '',
    ADD COLUMN IF NOT EXISTS description TEXT NOT NULL DEFAULT '';

-- Test-Daten ersetzen: Tabelle leeren (CASCADE löscht auch release_member_roles, member_episode_notes)
TRUNCATE contributor_roles CASCADE;

-- 11 Kernrollen einfügen (lowercase name, korrektes Label, Hilfetext aus CONTEXT.md)
INSERT INTO contributor_roles (name, label, description) VALUES
    ('translator',     'Übersetzung',      'Schreibe kurz, was an der Übersetzung dieser Version besonders war: Dialogstil, Begriffe, Songtexte, Schilder...'),
    ('editor',         'Editing',          'Schreibe kurz, was du sprachlich verbessert hast: Lesbarkeit, Stil, Charakterstimmen...'),
    ('timer',          'Timing',           'Schreibe kurz, was am Timing besonders war: Dialog-Timing, Karaoke-Timing, Lesbarkeit...'),
    ('typesetter',     'Typesetting / FX', 'Schreibe kurz, was du visuell umgesetzt hast: Signs, Overlays, Fonts, Karaoke-FX...'),
    ('encoder',        'Encoding',         'Schreibe kurz, was an dieser technischen Ausgabe besonders war: 8bit/10bit, MP4/MKV...'),
    ('raw_provider',   'Raw-Bereitstellung', 'Schreibe kurz, was zur Quelle wichtig ist: Herkunft, Qualität, Probleme, bessere Quelle...'),
    ('quality_checker','Qualitätsprüfung', 'Schreibe kurz, worauf bei der Prüfung geachtet wurde: Übersetzung, Timing, Encoding...'),
    ('project_lead',   'Projektleitung',   'Schreibe kurz, warum diese Version veröffentlicht wurde: Koordination, Ziel, Re-Release...'),
    ('designer',       'Design',           'Schreibe kurz, welche visuellen Elemente du beigesteuert hast: Banner, Logos, Vorschaubilder...'),
    ('admin',          'Administration',   'Schreibe kurz, was organisatorisch wichtig war: Archivierung, Upload, Metadaten...'),
    ('other',          'Sonstiges',        'Nutze dieses Feld nur wenn der Beitrag nicht zu den Standardrollen passt.');
```

Erstelle `database/migrations/0065_seed_contributor_roles_kernrollen.down.sql`:

Die DOWN-Migration kann die Kernrollen nicht zuverlässig rückgängig machen (CASCADE-Löschungen in abhängigen Tabellen wären weg). DOWN ist daher ein No-Op mit Kommentar:

```sql
-- DOWN: Nicht reversibel — TRUNCATE CASCADE hat abhängige Zeilen in release_member_roles
-- und member_episode_notes gelöscht. Kein automatisches Rollback möglich.
-- Für manuelle Wiederherstellung: backup einspielen.
SELECT 1; -- No-op placeholder
```

## Verification

Migration-Datei existiert:

```bash
ls database/migrations/0065_seed_contributor_roles_kernrollen.up.sql
```

Nach Ausführung:

```sql
SELECT COUNT(*) FROM contributor_roles;
-- Erwartetes Ergebnis: 11

SELECT name, label FROM contributor_roles ORDER BY id;
-- Erwartete Ausgabe: translator, editor, timer, typesetter, encoder, raw_provider,
-- quality_checker, project_lead, designer, admin, other

SELECT column_name FROM information_schema.columns
WHERE table_name = 'contributor_roles' AND column_name IN ('label', 'description');
-- Ergebnis: 2 Zeilen
```

## Output

Nach Abschluss `.planning/phases/40-text-und-notizsystem-fuer-fansub-plattform/40-02-SUMMARY.md` erstellen.
