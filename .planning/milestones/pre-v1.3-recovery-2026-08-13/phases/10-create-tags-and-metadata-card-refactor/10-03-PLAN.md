---
phase: 10-create-tags-and-metadata-card-refactor
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - frontend/src/app/admin/anime/create/page.tsx
  - frontend/src/app/admin/anime/create/page.test.tsx
  - frontend/src/app/admin/anime/components/CreatePage/AnimeCreateGenreField.tsx
  - frontend/src/app/admin/anime/components/CreatePage/AnimeCreateTagField.tsx
  - frontend/src/app/admin/anime/components/CreatePage/AnimeCreateTokenField.tsx
  - frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx
  - frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.module.css
  - frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts
  - frontend/src/app/admin/anime/utils/anime-helpers.ts
  - frontend/src/lib/api/admin-anime-intake.ts
  - frontend/src/types/admin.ts
autonomous: true
requirements: [TAG-02, TAG-03, TAG-04, TAG-05]
must_haves:
  truths:
    - "The create route shows tags as a dedicated visible card rather than a hidden payload-only state."
    - "Manual entry, comma-separated input, and click-based suggestions all converge into one tag-token state."
    - "The create-page metadata area is refactored so no single page file remains above the 700-line guardrail."
    - "Newly extracted sections and non-obvious helpers contain short purpose comments."
  artifacts:
    - path: "frontend/src/app/admin/anime/components/CreatePage/AnimeCreateTagField.tsx"
      provides: "Dedicated tags card for create metadata"
      contains: "Ausgewaehlte Tags"
    - path: "frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx"
      provides: "Refactored metadata layout hosting genre card, tags card, and description card"
      contains: "Genre, Tags und Beschreibung"
    - path: "frontend/src/app/admin/anime/create/page.tsx"
      provides: "Refactored create route orchestration under the line-count guardrail"
      contains: "getAdminTagTokens"
    - path: "frontend/src/app/admin/anime/utils/anime-helpers.ts"
      provides: "Shared normalization helpers used by genre and tag inputs"
      contains: "splitTagTokens"
    - path: "frontend/src/app/admin/anime/create/page.test.tsx"
      provides: "Regression coverage for visible tags card and tag payload submission"
      contains: "Tags"
  key_links:
    - from: "frontend/src/app/admin/anime/create/page.tsx"
      to: "frontend/src/app/admin/anime/components/CreatePage/AnimeCreateTagField.tsx"
      via: "dedicated tag card props and event handlers"
      pattern: "AnimeCreateTagField"
    - from: "frontend/src/app/admin/anime/create/page.tsx"
      to: "frontend/src/lib/api/admin-anime-intake.ts"
      via: "load tag tokens through the dedicated admin tags helper"
      pattern: "getAdminTagTokens"
    - from: "frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx"
      to: "frontend/src/app/admin/anime/components/CreatePage/AnimeCreateGenreField.tsx"
      via: "parallel metadata-card presentation"
      pattern: "AnimeCreateGenreField"
---

<objective>
Implement the create-route tags card and refactor the create metadata area so genres, tags, and description remain clear, testable, and within the page-size guardrail.

Purpose: The normalized backend work only becomes operator-usable once `/admin/anime/create` exposes a deliberate tags surface that matches the UI contract and no longer buries metadata logic in one oversized page.
Output: Dedicated create tags UI, tag-token fetching and hydration, shared token helpers where needed, regression tests, and a create page split under 700 lines.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-create-tags-and-metadata-card-refactor/10-CONTEXT.md
@.planning/phases/10-create-tags-and-metadata-card-refactor/10-UI-SPEC.md
@frontend/src/app/admin/anime/create/page.tsx
@frontend/src/app/admin/anime/components/CreatePage/AnimeCreateGenreField.tsx
@frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx
@frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts
@frontend/src/app/admin/anime/utils/anime-helpers.ts
@frontend/src/lib/api/admin-anime-intake.ts
@frontend/src/types/admin.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing frontend coverage for visible tags card and tag payload flow</name>
  <files>frontend/src/app/admin/anime/create/page.test.tsx</files>
  <read_first>@frontend/src/app/admin/anime/create/page.tsx @frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx @.planning/phases/10-create-tags-and-metadata-card-refactor/10-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: the create metadata area renders a dedicated `Tags` card alongside the existing genre controls.
    - Test 2: entering comma-separated tags and clicking suggestions results in one deduped selected tag list.
    - Test 3: provider-hydrated tags appear in the same selected-tag region as manual tags.
    - Test 4: the final create payload includes `tags: [...]` and does not collapse tags into the `genre` string.
  </behavior>
  <acceptance_criteria>`frontend/src/app/admin/anime/create/page.test.tsx` fails until the UI renders `Tags`, `Ausgewaehlte Tags`, and a tag-aware create payload with `tags: [...]`; the tests assert that duplicate tag input only yields one selected chip and that the create payload still keeps `genre` separate.</acceptance_criteria>
  <action>Add create-page tests that render the metadata workspace, assert a dedicated `Tags` heading exists, add tags via both manual input and suggestion click, and verify the selected tag chip row is labeled `Ausgewaehlte Tags`. Add a submit-path assertion that the outgoing payload contains `tags: ['Classic', 'Mecha']` or the chosen normalized values, while `genre` remains the comma-joined genre string. Add a provider-hydration test using the existing Jellyfin draft hydration path to prove imported tags populate the same tag token state rather than a separate preview-only UI.</action>
  <verify>
    <automated>cd frontend && npm test -- src/app/admin/anime/create/page.test.tsx</automated>
  </verify>
  <done>Frontend tests describe the required visible tags experience, payload shape, and hydration behavior before implementation starts.</done>
</task>

<task type="auto">
  <name>Task 2: Build the tags card and refactor the create metadata area under the line guardrail</name>
  <files>frontend/src/app/admin/anime/create/page.tsx, frontend/src/app/admin/anime/components/CreatePage/AnimeCreateGenreField.tsx, frontend/src/app/admin/anime/components/CreatePage/AnimeCreateTagField.tsx, frontend/src/app/admin/anime/components/CreatePage/AnimeCreateTokenField.tsx, frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx, frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.module.css, frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts, frontend/src/app/admin/anime/utils/anime-helpers.ts, frontend/src/lib/api/admin-anime-intake.ts, frontend/src/types/admin.ts</files>
  <read_first>@frontend/src/app/admin/anime/create/page.tsx @frontend/src/app/admin/anime/components/CreatePage/AnimeCreateGenreField.tsx @frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx @frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts @frontend/src/app/admin/anime/utils/anime-helpers.ts @.planning/phases/10-create-tags-and-metadata-card-refactor/10-UI-SPEC.md</read_first>
  <acceptance_criteria>`frontend/src/app/admin/anime/create/page.tsx` is below 700 lines; a dedicated `AnimeCreateTagField` or equivalent extracted component exists; `ManualCreateWorkspace.tsx` contains the section title `Genre, Tags und Beschreibung`; the tags card renders `Noch keine Tags gesetzt.` and `Tag Vorschlaege`; `frontend/src/app/admin/anime/utils/anime-helpers.ts` exports a dedicated tag-splitting or shared token-splitting helper; `npm run build` completes successfully.</acceptance_criteria>
  <action>Refactor `frontend/src/app/admin/anime/create/page.tsx` by extracting tag-card rendering and any shared token-card behavior into focused components such as `AnimeCreateTagField.tsx` or a shared `AnimeCreateTokenField.tsx`, and move non-obvious orchestration into helpers or hooks so the page file drops below 700 lines. Keep short purpose comments on newly extracted sections and non-obvious helpers. In `ManualCreateWorkspace.tsx`, change the metadata section copy to match the UI-SPEC with the title `Genre, Tags und Beschreibung`, render genre and tags as sibling card surfaces, and keep description visually separate. In `frontend/src/app/admin/anime/utils/anime-helpers.ts`, add a dedicated `splitTagTokens` helper or a clearly named shared token helper so genre and tag normalization do not drift. In `frontend/src/lib/api/admin-anime-intake.ts`, load tag suggestions through `getAdminTagTokens`, and in the page orchestration maintain separate genre and tag suggestion state while allowing provider hydration to feed `createTagTokens`. Preserve the existing `tags` payload field on submit and do not collapse tags into the legacy `genre` string.</action>
  <verify>
    <automated>cd frontend && npm test -- src/app/admin/anime/create/page.test.tsx && npm run build</automated>
  </verify>
  <done>The create route exposes a real tags card, hydrates provider tags into the same selected token list, and the page-level refactor lands within the agreed line-count and documentation guardrails.</done>
</task>

</tasks>

<verification>
The create route treats tags as a first-class metadata surface instead of leftover hidden state. The UI matches the approved contract and the refactor reduces page sprawl rather than growing it further.
</verification>

<success_criteria>
- `/admin/anime/create` shows a dedicated visible tags card.
- Manual input, suggestions, and provider hydration converge into one tag token state.
- Submit payloads include `tags: [...]` independently of `genre`.
- `page.tsx` falls under 700 lines and extracted helpers/components are purpose-commented where needed.
</success_criteria>

<output>
After completion, create `.planning/phases/10-create-tags-and-metadata-card-refactor/10-03-SUMMARY.md`
</output>
