---
phase: 10-create-tags-and-metadata-card-refactor
plan: 04
type: execute
wave: 4
depends_on: [10-02, 10-03]
files_modified:
  - database/migrations/0042_add_tag_tables_forward_fix.up.sql
  - database/migrations/0042_add_tag_tables_forward_fix.down.sql
  - backend/internal/repository/admin_content_test.go
  - .planning/phases/10-create-tags-and-metadata-card-refactor/10-UAT.md
autonomous: true
gap_closure: true
requirements: [TAG-01]
must_haves:
  truths:
    - "Already-migrated databases gain the missing `tags` and `anime_tags` tables through a new forward migration instead of relying on edits to previously applied migrations."
    - "Anime create with tags no longer fails with a missing-schema 500 once the new migration is applied."
    - "The Phase 10 tag persistence code remains unchanged in behavior; the fix only restores the missing database contract for real runtimes."
  artifacts:
    - path: "database/migrations/0042_add_tag_tables_forward_fix.up.sql"
      provides: "Forward-safe creation of `tags` and `anime_tags` for databases that already applied earlier migration versions"
      contains: "CREATE TABLE IF NOT EXISTS tags"
    - path: "database/migrations/0042_add_tag_tables_forward_fix.up.sql"
      provides: "Forward-safe creation of the anime-to-tag junction table"
      contains: "CREATE TABLE IF NOT EXISTS anime_tags"
    - path: "backend/internal/repository/admin_content_test.go"
      provides: "Regression coverage proving tag persistence assumes normalized tables and remains aligned with the forward migration fix"
      contains: "normalizeTagList"
    - path: ".planning/phases/10-create-tags-and-metadata-card-refactor/10-UAT.md"
      provides: "Gap diagnosis and post-fix verification record for persisted tag save flow"
      contains: "Persisted Tags On Save"
  key_links:
    - from: "database/migrations/0042_add_tag_tables_forward_fix.up.sql"
      to: "database/migrations/0019_add_reference_data_tables.up.sql"
      via: "replays the missing `tags` table creation as a new forward migration for already-migrated databases"
      pattern: "CREATE TABLE IF NOT EXISTS tags"
    - from: "database/migrations/0042_add_tag_tables_forward_fix.up.sql"
      to: "database/migrations/0022_add_junction_tables.up.sql"
      via: "replays the missing `anime_tags` junction table creation as a new forward migration for already-migrated databases"
      pattern: "CREATE TABLE IF NOT EXISTS anime_tags"
    - from: "backend/internal/repository/admin_content.go"
      to: "database/migrations/0042_add_tag_tables_forward_fix.up.sql"
      via: "create persistence writes tags through `replaceAuthoritativeAnimeTags` and depends on the missing tables"
      pattern: "replaceAuthoritativeAnimeTags"
---

<objective>
Close the Phase 10 persisted-tag blocker by adding a new forward migration for the missing tag tables and validating the save flow against a real already-migrated runtime.

Purpose: The implementation code for tags is already working, but it was shipped on top of edited historical migrations. This plan restores the real database contract safely for environments that already applied migrations through 0041.
Output: A new forward migration, regression coverage that documents the contract, applied local runtime schema, and a re-run of the blocked UAT save test.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-create-tags-and-metadata-card-refactor/10-CONTEXT.md
@.planning/phases/10-create-tags-and-metadata-card-refactor/10-02-SUMMARY.md
@.planning/phases/10-create-tags-and-metadata-card-refactor/10-03-SUMMARY.md
@.planning/phases/10-create-tags-and-metadata-card-refactor/10-UAT.md
@database/migrations/0019_add_reference_data_tables.up.sql
@database/migrations/0019_add_reference_data_tables.down.sql
@database/migrations/0022_add_junction_tables.up.sql
@database/migrations/0022_add_junction_tables.down.sql
@backend/internal/repository/admin_content.go
@backend/internal/repository/admin_content_test.go
@backend/cmd/migrate/main.go
@docker-compose.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a forward migration that restores missing tag tables on already-migrated databases</name>
  <files>database/migrations/0042_add_tag_tables_forward_fix.up.sql, database/migrations/0042_add_tag_tables_forward_fix.down.sql</files>
  <read_first>
    - database/migrations/0019_add_reference_data_tables.up.sql
    - database/migrations/0019_add_reference_data_tables.down.sql
    - database/migrations/0022_add_junction_tables.up.sql
    - database/migrations/0022_add_junction_tables.down.sql
    - .planning/phases/10-create-tags-and-metadata-card-refactor/10-UAT.md
    - backend/internal/repository/admin_content.go
  </read_first>
  <behavior>
    - Test 1: a database that already shows migrations 1-41 as applied receives `tags` and `anime_tags` after applying the new migration.
    - Test 2: the migration is idempotent and safe if either table already exists.
    - Test 3: the down migration only removes the forward-fix tables and indexes introduced by 0042.
  </behavior>
  <action>Create `database/migrations/0042_add_tag_tables_forward_fix.up.sql` and `database/migrations/0042_add_tag_tables_forward_fix.down.sql`. In the up migration, explicitly create `tags` with `id BIGSERIAL PRIMARY KEY`, `name TEXT NOT NULL`, `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`, `updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`, plus `UNIQUE (name)` and an index `idx_tag_name` if it does not already exist. Also create `anime_tags` with `anime_id BIGINT NOT NULL REFERENCES anime(id) ON DELETE CASCADE`, `tag_id BIGINT NOT NULL REFERENCES tags(id) ON DELETE CASCADE`, `PRIMARY KEY (anime_id, tag_id)`, and supporting indexes for `anime_id` and `tag_id`, all guarded with `IF NOT EXISTS` or equivalent safe statements. In the down migration, drop `anime_tags` first and then `tags`, matching the forward-fix scope only. Do not edit historical migrations 0019 or 0022 again.</action>
  <acceptance_criteria>
    - `database/migrations/0042_add_tag_tables_forward_fix.up.sql` exists and contains `CREATE TABLE IF NOT EXISTS tags`.
    - `database/migrations/0042_add_tag_tables_forward_fix.up.sql` exists and contains `CREATE TABLE IF NOT EXISTS anime_tags`.
    - `database/migrations/0042_add_tag_tables_forward_fix.down.sql` exists and contains `DROP TABLE IF EXISTS anime_tags`.
    - `database/migrations/0042_add_tag_tables_forward_fix.down.sql` exists and contains `DROP TABLE IF EXISTS tags`.
    - No further changes are made to `0019_*` or `0022_*` migration files.
  </acceptance_criteria>
  <verify>
    <automated>Get-Content database/migrations/0042_add_tag_tables_forward_fix.up.sql | Select-String -Pattern 'CREATE TABLE IF NOT EXISTS tags','CREATE TABLE IF NOT EXISTS anime_tags','idx_tag_name'</automated>
    <automated>Get-Content database/migrations/0042_add_tag_tables_forward_fix.down.sql | Select-String -Pattern 'DROP TABLE IF EXISTS anime_tags','DROP TABLE IF EXISTS tags'</automated>
  </verify>
  <done>The repo contains a forward-safe migration that can repair already-migrated runtimes without rewriting history.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the repair migration locally and re-verify the blocked save flow</name>
  <files>backend/internal/repository/admin_content_test.go, .planning/phases/10-create-tags-and-metadata-card-refactor/10-UAT.md</files>
  <read_first>
    - backend/internal/repository/admin_content_test.go
    - backend/cmd/migrate/main.go
    - docker-compose.yml
    - .planning/phases/10-create-tags-and-metadata-card-refactor/10-UAT.md
  </read_first>
  <behavior>
    - Test 1: the local database reports migration 42 as applied after running the project migrator.
    - Test 2: `tags` and `anime_tags` exist in the running database container after the migration.
    - Test 3: the previously blocked Phase 10 UAT save scenario can be rerun and recorded in `10-UAT.md`.
  </behavior>
  <action>Keep production code changes minimal. If needed, extend `backend/internal/repository/admin_content_test.go` only with targeted assertions or comments that document the normalized tag persistence contract rather than reworking working repository logic. Then apply the new migration to the running local stack with the project migrator inside the backend container, using `/app/migrate up -dir /app/database/migrations -database-url postgres://team4s:team4s_dev_password@team4sv30-db:5432/team4s_v2?sslmode=disable`. After that, verify with `migrate status` and a direct `psql` table listing that migration 42, `tags`, and `anime_tags` are present. Finally, rerun the blocked browser save test for Phase 10 and update `10-UAT.md` with the final pass or any remaining issue, replacing the diagnosed blocker if fixed.</action>
  <acceptance_criteria>
    - `docker exec team4sv30-backend /app/migrate status ...` reports version `42` as `applied`.
    - `docker exec team4sv30-db psql -U team4s -d team4s_v2 -c "\\dt"` includes `tags` and `anime_tags`.
    - `go test ./internal/repository ./internal/handlers ./cmd/server -count=1` passes after the migration file is added.
    - `.planning/phases/10-create-tags-and-metadata-card-refactor/10-UAT.md` no longer ends with the unresolved missing-schema blocker once the rerun succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/repository ./internal/handlers ./cmd/server -count=1</automated>
    <automated>docker exec team4sv30-backend /app/migrate up -dir /app/database/migrations -database-url "postgres://team4s:team4s_dev_password@team4sv30-db:5432/team4s_v2?sslmode=disable"</automated>
    <automated>docker exec team4sv30-backend /app/migrate status -dir /app/database/migrations -database-url "postgres://team4s:team4s_dev_password@team4sv30-db:5432/team4s_v2?sslmode=disable"</automated>
    <automated>docker exec team4sv30-db psql -U team4s -d team4s_v2 -c "\dt tags" && docker exec team4sv30-db psql -U team4s -d team4s_v2 -c "\dt anime_tags"</automated>
  </verify>
  <done>The missing tag schema is repaired in the local runtime and the previously blocked persisted-tag save test is ready to pass or reveal only residual non-schema issues.</done>
</task>

</tasks>

<verification>
Run the backend test slice, apply the new migration against the running local database, then repeat the Phase 10 "Persisted Tags On Save" browser test until `10-UAT.md` records a final resolved result.
</verification>

<success_criteria>
The Phase 10 gap is closed without reopening scope: already-migrated databases receive `tags` and `anime_tags` through a new forward migration, and create-with-tags no longer fails because the schema is missing.
</success_criteria>

<output>
After completion, create `.planning/phases/10-create-tags-and-metadata-card-refactor/10-04-SUMMARY.md`
</output>
