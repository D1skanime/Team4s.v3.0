---
phase: 10-create-tags-and-metadata-card-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - shared/contracts/openapi.yaml
  - backend/cmd/server/admin_routes.go
  - backend/internal/models/admin_content.go
  - backend/internal/handlers/admin_content_handler.go
  - frontend/src/types/admin.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/api/admin-anime-intake.ts
autonomous: true
requirements: [TAG-01, TAG-02, TAG-03, TAG-04, TAG-05]
must_haves:
  truths:
    - "Phase 10 no longer relies on placeholder requirement text; tags and create-refactor work have explicit IDs and traceability."
    - "Backend, frontend, and OpenAPI all describe the same create-time tag payload and tag-token listing contract."
    - "The admin API surface distinguishes genre tokens and tag tokens as separate endpoints and separate response types."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 10 goal, dependency, success criteria, and named requirement IDs"
      contains: "TAG-01, TAG-02, TAG-03, TAG-04, TAG-05"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirement definitions and traceability rows for create tags and metadata refactor"
      contains: "### Create Tags And Metadata Refactor"
    - path: "shared/contracts/openapi.yaml"
      provides: "Documented request, patch, and token-listing contracts for tags"
      contains: "/api/v1/admin/tags"
    - path: "frontend/src/types/admin.ts"
      provides: "TypeScript request and response contracts for create tags and tag tokens"
      contains: "AdminTagTokensResponse"
    - path: "backend/internal/models/admin_content.go"
      provides: "Go models aligned with the tag-aware create and patch contract"
      contains: "Tags"
  key_links:
    - from: "frontend/src/lib/api/admin-anime-intake.ts"
      to: "shared/contracts/openapi.yaml"
      via: "exact tag token endpoint path and response shape"
      pattern: "/api/v1/admin/tags"
    - from: "backend/internal/handlers/admin_content_handler.go"
      to: "backend/internal/models/admin_content.go"
      via: "request and response struct alignment for tags"
      pattern: "Tags"
---

<objective>
Resolve the Phase 10 requirement-mapping gap and define one exact shared contract for normalized tags, tag token listing, and tag-aware anime create and patch payloads.

Purpose: Executors need stable requirement IDs and one shared frontend/backend contract before the schema, repository, and UI work can be implemented without ad-hoc naming drift.
Output: Updated roadmap and requirements traceability plus documented backend/frontend contracts for tag tokens and tag-aware anime create/edit payloads.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-create-tags-and-metadata-card-refactor/10-CONTEXT.md
@.planning/phases/10-create-tags-and-metadata-card-refactor/10-UI-SPEC.md
@shared/contracts/openapi.yaml
@backend/internal/models/admin_content.go
@backend/internal/handlers/admin_content_handler.go
@backend/cmd/server/admin_routes.go
@frontend/src/types/admin.ts
@frontend/src/lib/api/admin-anime-intake.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Formalize Phase 10 requirement IDs and roadmap traceability</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>@.planning/ROADMAP.md @.planning/REQUIREMENTS.md @.planning/phases/10-create-tags-and-metadata-card-refactor/10-CONTEXT.md @.planning/phases/10-create-tags-and-metadata-card-refactor/10-UI-SPEC.md</read_first>
  <acceptance_criteria>`.planning/REQUIREMENTS.md` contains a dedicated `### Create Tags And Metadata Refactor` section defining `TAG-01` through `TAG-05`; `.planning/ROADMAP.md` Phase 10 lists the same IDs in its `Requirements` line; the traceability table maps every `TAG-0X` row to Phase 10.</acceptance_criteria>
  <action>Add a new requirement section to `.planning/REQUIREMENTS.md` with `TAG-01` through `TAG-05` covering: normalized `tags` and `anime_tags` persistence analogous to genres, dedicated create-page tags card, provider-tag hydration into shared token state, create-page refactor under the 700-line page guardrail, and short purpose comments for newly extracted sections and helpers. Update `.planning/ROADMAP.md` so Phase 10 replaces the placeholder requirement text with `TAG-01, TAG-02, TAG-03, TAG-04, TAG-05`, keeps dependency on Phase 09, and keeps the success criteria aligned with the locked decisions and UI-SPEC.</action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path .planning/ROADMAP.md,.planning/REQUIREMENTS.md -Pattern 'TAG-01|TAG-02|TAG-03|TAG-04|TAG-05' | Measure-Object | % Count"</automated>
  </verify>
  <done>Phase 10 has named requirements and no longer depends on placeholder requirement text in planning artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define the shared API and type contract for tags and tag tokens</name>
  <files>shared/contracts/openapi.yaml, backend/cmd/server/admin_routes.go, backend/internal/models/admin_content.go, backend/internal/handlers/admin_content_handler.go, frontend/src/types/admin.ts, frontend/src/lib/api.ts, frontend/src/lib/api/admin-anime-intake.ts</files>
  <read_first>@shared/contracts/openapi.yaml @backend/internal/models/admin_content.go @backend/internal/handlers/admin_content_handler.go @backend/cmd/server/admin_routes.go @frontend/src/types/admin.ts @frontend/src/lib/api/admin-anime-intake.ts</read_first>
  <acceptance_criteria>`shared/contracts/openapi.yaml` documents `GET /api/v1/admin/tags`; `frontend/src/types/admin.ts` exports `TagToken` and `AdminTagTokensResponse` or an equivalently named dedicated tag-token response; `backend/internal/models/admin_content.go` carries `Tags []string` on create and patch models plus a tag-token response shape; `backend/cmd/server/admin_routes.go` registers the new admin tags route; `frontend/src/lib/api/admin-anime-intake.ts` exposes a helper that fetches tag tokens instead of reusing the genre helper.</acceptance_criteria>
  <action>Update `shared/contracts/openapi.yaml` to keep `tags?: string[]` on admin anime create and patch payloads and to document a dedicated `GET /api/v1/admin/tags` endpoint returning `{ data: [{ name, count }] }`. In `backend/internal/models/admin_content.go`, ensure both create and patch models expose tag fields and add a dedicated tag-token response model matching the documented payload. In `frontend/src/types/admin.ts`, add the matching `TagToken` and `AdminTagTokensResponse` types while keeping `tags?: string[]` in `AdminAnimeCreateRequest` and draft payload types. In `backend/cmd/server/admin_routes.go` and `backend/internal/handlers/admin_content_handler.go`, reserve the handler and route name `ListTagTokens`. In `frontend/src/lib/api.ts` and `frontend/src/lib/api/admin-anime-intake.ts`, add a typed `getAdminTagTokens({ query?, limit? }, authToken?)` helper that calls `/api/v1/admin/tags` with the same auth-header pattern as other admin metadata helpers.</action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path shared/contracts/openapi.yaml,backend/cmd/server/admin_routes.go,frontend/src/types/admin.ts,frontend/src/lib/api/admin-anime-intake.ts -Pattern '/api/v1/admin/tags|ListTagTokens|AdminTagTokensResponse' | Measure-Object | % Count"</automated>
  </verify>
  <done>The repository has one exact contract for tag-aware admin payloads and a separate tag-token endpoint ready for backend and frontend implementation.</done>
</task>

</tasks>

<verification>
Phase 10 has explicit requirement IDs and a stable tag contract before any schema or UI implementation begins. The documented API surface separates tags from genres while keeping both parallel in shape.
</verification>

<success_criteria>
- Phase 10 requirement IDs exist and are mapped in roadmap plus requirements docs.
- One dedicated admin tag-token endpoint is documented and typed on both backend and frontend.
- Create and patch payloads already reserve `tags` as a first-class field rather than a local UI-only detail.
</success_criteria>

<output>
After completion, create `.planning/phases/10-create-tags-and-metadata-card-refactor/10-01-SUMMARY.md`
</output>
