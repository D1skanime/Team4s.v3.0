---
phase: 97-revoke-rollen-lifecycle-uebergang
plan: 02
type: execute
wave: 2
depends_on:
  - 97-01
files_modified:
  - backend/internal/repository/hist_group_member_roles_repository.go
  - backend/internal/repository/hist_group_members_repository.go
  - backend/internal/handlers/fansub_hist_group_member_roles_handler.go
  - backend/internal/handlers/fansub_hist_group_members_handler.go
autonomous: true
requirements:
  - D-02
  - D-08

must_haves:
  truths:
    - "Go-Structs in hist_group_member_roles_repository.go verwenden StartedDate/EndedDate *time.Time statt StartedYear/EndedYear *int"
    - "Go-Structs in hist_group_members_repository.go verwenden JoinedDate/LeftDate *time.Time"
    - "Handler-DTOs akzeptieren started_date/ended_date als ISO-String (nicht mehr started_year/ended_year Int)"
    - "Handler parst ISO-Datum 'YYYY-MM-DD' via time.Parse und gibt 400 bei ungültigem Format"
    - "JSON-Responses liefern started_date/ended_date als ISO-8601-String"
    - "go build ./... und go test ./internal/repository/... ./internal/handlers/... sind fehlerfrei"
  artifacts:
    - path: backend/internal/repository/hist_group_member_roles_repository.go
      provides: "DATE-Typen in allen Structs und SQL-Queries"
      contains: "StartedDate *time.Time"
    - path: backend/internal/repository/hist_group_members_repository.go
      provides: "DATE-Typen in allen Structs und SQL-Queries"
      contains: "JoinedDate *time.Time"
    - path: backend/internal/handlers/fansub_hist_group_member_roles_handler.go
      provides: "ISO-String-DTOs + time.Parse-Validierung"
      contains: "started_date"
    - path: backend/internal/handlers/fansub_hist_group_members_handler.go
      provides: "ISO-String-DTOs für Mitglied-Beitrittsdaten"
      contains: "joined_date"
  key_links:
    - from: "backend/internal/handlers/fansub_hist_group_member_roles_handler.go"
      to: "backend/internal/repository/hist_group_member_roles_repository.go"
      via: "HistGroupMemberRoleInput.StartedDate *time.Time"
      pattern: "StartedDate"
    - from: "backend/internal/repository/hist_group_member_roles_repository.go"
      to: "hist_group_member_roles.started_date"
      via: "SQL INSERT/SELECT started_date"
      pattern: "started_date"
---

<objective>
Backend-Repository-Structs und Handler-DTOs auf DATE-Typen umstellen, nachdem die DB-Migration (Plan 01) die INT-Spalten durch DATE-Spalten ersetzt hat. Ohne diese Umstellung kompiliert der Backend-Code nicht mehr (alle SQL-Scans lesen nicht-existente Spalten).

Zweck: Stellt die vollständige Go-Kompilierbarkeit her und macht die neuen DATE-Felder über die bestehenden Admin-Endpunkte erreichbar. Voraussetzung für Plan 03 (Frontend-Typen).

Ausgabe: 4 Go-Dateien mit DATE-Typen; JSON-API liefert ISO-Datums-Strings statt Jahres-Integers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-CONTEXT.md
@.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-RESEARCH.md
@.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-PATTERNS.md
@.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-01-SUMMARY.md
</context>

<interfaces>
<!-- Ist-Stand der Structs (aus Codebase gelesen): -->

Aus hist_group_member_roles_repository.go (aktueller Stand — muss geändert werden):
```go
type HistGroupMemberRoleRow struct {
    ID                      int64
    HistFansubGroupMemberID int64
    RoleCode                string
    StartedYear             *int       // → StartedDate *time.Time
    EndedYear               *int       // → EndedDate *time.Time
    Status                  string
    Visibility              string
    ConfirmedBy             *int64
    ConfirmedAt             *time.Time
    SourceNote              *string
    CreatedBy               *int64
    CreatedAt               time.Time
    UpdatedAt               time.Time
}

type HistGroupMemberRoleInput struct {
    HistFansubGroupMemberID int64
    RoleCode                string
    StartedYear             *int     // → StartedDate *time.Time
    EndedYear               *int     // → EndedDate *time.Time
    Status, Visibility, SourceNote, CreatedBy ...
}

type HistGroupMemberRolePatchInput struct {
    StartedYear **int    // → StartedDate **time.Time
    EndedYear   **int    // → EndedDate **time.Time
    ...
}

type HistGroupMemberRoleDisplayRow struct {
    StartedYear *int `json:"started_year"`  // → StartedDate *time.Time `json:"started_date"`
    EndedYear   *int `json:"ended_year"`    // → EndedDate *time.Time `json:"ended_date"`
}
```

Aus member_claims_repository.go (VerifyClaim-Signatur — wird in Plan 04 erweitert):
```go
func (r *MemberClaimsRepository) VerifyClaim(ctx context.Context, fansubGroupID int64, claimID int64, verifiedByAppUserID int64) error
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hist_group_member_roles_repository.go — Structs und SQL auf DATE umstellen</name>
  <files>
    backend/internal/repository/hist_group_member_roles_repository.go
  </files>

  <read_first>
    - backend/internal/repository/hist_group_member_roles_repository.go — vollständige Datei (458 Zeilen); alle Struct-Definitionen, SQL-Queries, Scan-Aufrufe
    - .planning/phases/97-revoke-rollen-lifecycle-uebergang/97-PATTERNS.md — Pattern hist_group_member_roles_repository.go (Ziel-Structs, SQL-Anpassungen, COALESCE-Datum-Muster)
  </read_first>

  <behavior>
    - Alle *int-Felder StartedYear/EndedYear in HistGroupMemberRoleRow, HistGroupMemberRoleInput, HistGroupMemberRolePatchInput, HistGroupMemberRoleDisplayRow → *time.Time (StartedDate/EndedDate).
    - JSON-Tags in HistGroupMemberRoleDisplayRow: "started_year" → "started_date", "ended_year" → "ended_date".
    - Alle SQL-Strings: started_year → started_date, ended_year → ended_date.
    - ORDER BY COALESCE(r.started_year, 9999) → ORDER BY COALESCE(r.started_date, '9999-01-01'::date).
    - Scan-Aufrufe: Variablen-Typen von *int auf *time.Time anpassen.
    - Datei bleibt unter 450 Zeilen (aktuell 458 — Änderungen sind Ersetzungen; nötigenfalls kurze Kommentare kürzen).
  </behavior>

  <action>
    Alle Vorkommen von StartedYear/*int → StartedDate/*time.Time und EndedYear/*int → EndedDate/*time.Time in:
    - Struct-Definitionen (HistGroupMemberRoleRow, HistGroupMemberRoleInput, HistGroupMemberRolePatchInput, HistGroupMemberRoleDisplayRow)
    - SQL-Queries in ListByFansubGroup, ListByMember, GetByID, CreateHistGroupMemberRole, PatchHistGroupMemberRole (alle started_year/ended_year → started_date/ended_date)
    - COALESCE-Muster: COALESCE(r.started_year, 9999) → COALESCE(r.started_date, '9999-01-01'::date)
    - json-Tags aktualisieren: "started_year" → "started_date", "ended_year" → "ended_date"
    - Alle Scan-Aufrufe für diese Felder: &row.StartedYear → &row.StartedDate (type *time.Time)

    In PatchHistGroupMemberRole: Dynamischer SET-Aufbau für StartedDate/EndedDate statt StartedYear/EndedYear.
    Der if-Block prüft auf input.StartedDate != nil und appendet "started_date = $N", arg = *input.StartedDate.

    Datei-Limit: Wenn nach allen Änderungen > 450 Zeilen → kurze Leerzeilen-Blöcke oder redundante Kommentare kürzen.
  </action>

  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go build ./internal/repository/... 2>&amp;1</automated>
  </verify>

  <done>
    go build ./internal/repository/... fehlerfrei. Keine *int-Felder mehr für StartedYear/EndedYear in dieser Datei. JSON-Tags sind started_date/ended_date.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: hist_group_members_repository.go + Handler-DTOs auf DATE umstellen</name>
  <files>
    backend/internal/repository/hist_group_members_repository.go,
    backend/internal/handlers/fansub_hist_group_member_roles_handler.go,
    backend/internal/handlers/fansub_hist_group_members_handler.go
  </files>

  <read_first>
    - backend/internal/repository/hist_group_members_repository.go — vollständige Datei (418 Zeilen); JoinedYear/LeftYear *int, CreateWithAutoMember-TX-Muster
    - backend/internal/handlers/fansub_hist_group_member_roles_handler.go — vollständige Datei (473 Zeilen); Create/Patch-Request-Structs, Date-Parsing fehlt noch
    - backend/internal/handlers/fansub_hist_group_members_handler.go — vollständige Datei (365 Zeilen); Create/Patch-Request-Structs für Mitglied
    - .planning/phases/97-revoke-rollen-lifecycle-uebergang/97-PATTERNS.md — Handler-DTOs, Date-Parsing-Muster (time.Parse "2006-01-02"), Permission-Guard-Muster
  </read_first>

  <behavior>
    Repository hist_group_members_repository.go:
    - JoinedYear/LeftYear *int → JoinedDate/LeftDate *time.Time in HistGroupMemberRow, HistGroupMemberInput, HistGroupMemberPatchInput, HistGroupMemberDisplayRow.
    - json-Tags: "joined_year" → "joined_date", "left_year" → "left_date".
    - SQL-Queries: joined_year → joined_date, left_year → left_date. COALESCE-Datum-Muster analog.
    - CreateWithAutoMember-Transaktion: joined_date/left_date als *time.Time-Parameter.

    Handler fansub_hist_group_member_roles_handler.go:
    - histGroupMemberRoleCreateRequest: StartedYear *int → StartedDate *string (JSON: "started_date"); EndedYear *int → EndedDate *string (JSON: "ended_date").
    - histGroupMemberRolePatchRequest: analog (**string statt **int).
    - Date-Parsing-Hilfsfunktion parseOptionalDate(s *string) (*time.Time, error) anlegen: time.Parse("2006-01-02", *s); bei Fehler → error mit Meldung "Format YYYY-MM-DD erwartet".
    - Vor jedem Repository-Call: parsed StartedDate/EndedDate via parseOptionalDate; bei Fehler → 400 BadRequest mit deutschem Fehlertext "Ungültiges Datum — Format JJJJ-MM-TT erwartet."
    - Datei-Limit: aktuell 473 Zeilen — prüfen ob nach Änderungen ≤ 450; sonst parseOptionalDate in eigene Hilfsdatei fansub_hist_handler_helpers.go auslagern.

    Handler fansub_hist_group_members_handler.go:
    - histGroupMemberCreateRequest: JoinedYear *int → JoinedDate *string (JSON: "joined_date"); LeftYear *int → LeftDate *string (JSON: "left_date").
    - histGroupMemberPatchRequest: analog.
    - parseOptionalDate wiederverwenden (aus Roles-Handler oder Helpers-Datei).
    - Datei-Limit: aktuell 365 Zeilen — kein Split nötig.
  </behavior>

  <action>
    hist_group_members_repository.go: Alle JoinedYear/*int → JoinedDate/*time.Time, LeftYear/*int → LeftDate/*time.Time. SQL-Strings ersetzen. json-Tags aktualisieren. CreateWithAutoMember-TX übergibt time.Time-Parameter statt int.

    fansub_hist_group_member_roles_handler.go:
    - Request-Structs auf *string/*string umstellen (started_date, ended_date als JSON-Keys).
    - parseOptionalDate-Hilfsfunktion anlegen (Signatur: func parseOptionalDate(s *string) (*time.Time, error); verwendet time.Parse("2006-01-02", *s)).
    - Alle Handler-Methoden CreateHistGroupMemberRole + PatchHistGroupMemberRole: parseOptionalDate aufrufen, 400 bei Fehler.
    - Wenn Datei > 450 Zeilen: parseOptionalDate + eventuelle weitere Hilfsfunktionen in fansub_hist_handler_helpers.go auslagern.

    fansub_hist_group_members_handler.go:
    - Request-Structs auf *string/*string umstellen (joined_date, left_date als JSON-Keys).
    - parseOptionalDate importieren/wiederverwenden.
    - CreateHistGroupMember + PatchHistGroupMember: parseOptionalDate aufrufen.
  </action>

  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go build ./... 2>&amp;1 && go test ./internal/repository/... ./internal/handlers/... 2>&amp;1</automated>
  </verify>

  <done>
    go build ./... fehlerfrei. go test ./internal/repository/... ./internal/handlers/... fehlerfrei (keine Compilation-Fehler; bestehende Tests weiterhin grün). Keine *int-Felder mehr für Jahres-Felder in den 3 geänderten Dateien. JSON-Tags sind joined_date/left_date resp. started_date/ended_date.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| HTTP-Request → Handler | JSON-Body mit started_date/ended_date als String; Angreifer kann beliebige Strings senden |
| Handler → Repository | *time.Time nach validiertem Parse; keine SQL-Injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-02-01 | Tampering | Handler: Datums-String ohne Validierung | mitigate | parseOptionalDate via time.Parse("2006-01-02") — invalide Strings → 400 BadRequest mit deutschem Fehlertext |
| T-97-02-02 | Information Disclosure | JSON-Response mit internen Zeitstempeln | accept | created_at/updated_at waren schon vorher in Response; kein neues Expositionsrisiko durch DATE-Felder |
| T-97-02-03 | Elevation of Privilege | Jede Handler-Methode hat Permission-Guard | mitigate | CanForFansubGroup(ActionFansubGroupMembersManage) bleibt in allen Schreibpfaden erhalten (aus bestehendem Code kopiert) |
| T-97-02-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Packages; nur Go-Quelldatei-Änderungen |
</threat_model>

<verification>
Nach beiden Tasks:
1. `cd backend && go build ./... ` — 0 Fehler.
2. `cd backend && go test ./internal/repository/... ./internal/handlers/...` — alle Tests grün.
3. Struct-Check: `grep -n "StartedYear\|EndedYear\|JoinedYear\|LeftYear" backend/internal/repository/hist_group_member_roles_repository.go backend/internal/repository/hist_group_members_repository.go` — 0 Treffer.
4. JSON-Tag-Check: `grep -n "started_year\|ended_year\|joined_year\|left_year" backend/internal/repository/ backend/internal/handlers/` — 0 Treffer in geänderten Dateien.
5. Datei-Limit: `wc -l backend/internal/handlers/fansub_hist_group_member_roles_handler.go backend/internal/handlers/fansub_hist_group_members_handler.go backend/internal/repository/hist_group_member_roles_repository.go backend/internal/repository/hist_group_members_repository.go` — alle ≤ 450.
</verification>

<success_criteria>
- Alle Go-Structs für historische Rollen/Mitglieder verwenden *time.Time für Datum-Felder.
- Handler akzeptieren ISO-Datum-Strings, validieren via time.Parse, geben 400 bei ungültigem Format.
- JSON-API liefert started_date/ended_date/joined_date/left_date als ISO-8601-Strings.
- go build ./... und go test ./internal/... sind fehlerfrei.
- Alle 4 Dateien ≤ 450 Zeilen.
</success_criteria>

<output>
Erstelle `.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-02-SUMMARY.md` nach Abschluss.
</output>
