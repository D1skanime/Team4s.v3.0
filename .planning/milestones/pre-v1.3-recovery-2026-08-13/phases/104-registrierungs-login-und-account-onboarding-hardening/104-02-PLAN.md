---
phase: 104
plan: 02
type: implementation
wave: 2
depends_on: [104-01]
files_modified:
  - frontend/src/lib/keycloakAuth.ts
  - frontend/src/lib/keycloakAuth.test.ts
  - frontend/src/lib/registrationCompletion.ts
  - frontend/src/lib/registrationCompletion.test.ts
  - frontend/src/app/login/page.tsx
  - frontend/src/app/login/page.test.tsx
autonomous: true
requirements: [P104-REG-1, P104-AUTH-1]
must_haves:
  truths:
    - "D-01 D-02: successful registration auto-signs-in and always hands off to /me/profile."
    - "D-03 D-04: the exact neutral confirmation is backed by trusted one-shot state, not a query parameter."
    - "D-23: visible Anmelden and Registrieren CTAs reuse @/components/ui Button."
---

<objective>
Expose a direct German registration entry through the existing PKCE lifecycle and create a trusted one-shot registration-completion handoff for `/me/profile`.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Extend the single PKCE transaction with registration intent</name>
  <read_first>
    <file>AGENTS.md</file><file>104-CONTEXT.md</file><file>docs/frontend/auth-api-client.md</file>
    <file>frontend/src/lib/keycloakAuth.ts</file><file>frontend/src/lib/keycloakAuth.test.ts</file>
  </read_first>
  <files>frontend/src/lib/keycloakAuth.ts, frontend/src/lib/keycloakAuth.test.ts, frontend/src/lib/registrationCompletion.ts, frontend/src/lib/registrationCompletion.test.ts</files>
  <action>Extend `beginKeycloakLogin` with typed login/register intent using the existing state, verifier, callback exchange and safe-path validation. Bind the intent to the validated one-time PKCE transaction. After a successful registration exchange, create a short-lived session-scoped, consume-once completion marker via `registrationCompletion.ts`; an arbitrary URL/query value must not create it. Registration ignores arbitrary `next` and resolves to `/me/profile`; ordinary login keeps safe `next` behavior. Clear marker on cancellation or failure. Do not read auth cookies or refresh directly.</action>
  <verify>`cd frontend; npm test -- src/lib/keycloakAuth.test.ts src/lib/registrationCompletion.test.ts` covers register/login URL, mismatch, replay, failure, spoofed query and external next rejection.</verify>
  <acceptance_criteria>One PKCE/callback seam owns both intents; auto-login succeeds without a second login; only a validated registration callback can create the one-shot profile marker.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add visible global-Button login and registration actions</name>
  <read_first>
    <file>frontend/src/app/login/page.tsx</file><file>frontend/src/app/login/page.test.tsx</file>
    <file>frontend/src/components/ui/Button.tsx</file><file>docs/frontend/ui-system.md</file><file>docs/agent-guidelines-ui.md</file>
  </read_first>
  <files>frontend/src/app/login/page.tsx, frontend/src/app/login/page.test.tsx</files>
  <action>Render distinct German `Anmelden` and `Registrieren` CTAs through the existing `@/components/ui` Button. Use the intent-aware PKCE start seam, preserve busy/error/disabled states, and after callback exchange hand off registration to `/me/profile`. Do not add native/page-local button styling, a second callback, password rules, verified-email claims, Fansub assumptions or automatic roles.</action>
  <verify>`cd frontend; npm test -- src/app/login/page.test.tsx` proves Button reuse, both intents, callback ordering and errors; run typecheck and no-token-boundary test.</verify>
  <acceptance_criteria>Registration is discoverable from visible Team4s UI; both CTAs share the global Button seam; ordinary login and safe redirect behavior remain intact.</acceptance_criteria>
</task>
</tasks>

<verification>
- Focused keycloakAuth, completion-marker and login-page tests
- `cd frontend; npm run typecheck; npm test -- src/lib/api.no-token-boundary.test.ts`
- `git diff --check`
</verification>

<success_criteria>D-01 through D-04 and D-23 are executable through the existing authentication boundary.</success_criteria>
