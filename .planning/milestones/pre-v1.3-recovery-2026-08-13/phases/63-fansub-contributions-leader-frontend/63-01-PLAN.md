---
phase: 63-fansub-contributions-leader-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/fansub.ts
  - frontend/src/lib/api.ts
autonomous: true
requirements:
  - P63-SC1
  - P63-SC2
  - P63-SC3
  - P63-SC4
  - P63-SC5

must_haves:
  truths:
    - "TypeScript-Typen für hist_fansub_group_members, hist_group_member_roles und anime_contributions sind in fansub.ts exportiert"
    - "api.ts enthält Funktionen für alle Admin-Endpunkte der Gruppen-Mitglieder, Rollen und Contributions"
    - "Jede API-Funktion folgt dem bestehenden Muster (authFetch, ApiResponse<T>)"
  artifacts:
    - path: frontend/src/types/fansub.ts
      provides: "Neue Interfaces für GroupMember, GroupMemberRole, AnimeContribution"
      contains: "HistFansubGroupMember"
    - path: frontend/src/lib/api.ts
      provides: "API-Calls für /admin/fansubs/:id/group-members, /member-roles, /anime/:animeId/contributions"
      exports:
        - listGroupMembers
        - createGroupMember
        - updateGroupMember
        - deleteGroupMember
        - listMemberRoles
        - createMemberRole
        - updateMemberRole
        - deleteMemberRole
        - listAnimeContributions
        - upsertAnimeContribution
        - deleteAnimeContribution
  key_links:
    - from: frontend/src/types/fansub.ts
      to: frontend/src/lib/api.ts
      via: "API-Funktionen geben ApiResponse<HistFansubGroupMember[]> etc. zurück"
      pattern: "HistFansubGroupMember"
---

<objective>
API-Typen und api.ts-Erweiterungen für alle Phase-62-Endpunkte, die Phase 63 UI-Tabs brauchen.

Purpose: Ohne diese Typen und API-Calls können die UI-Komponenten in Wave 2 und 3 nicht implementiert werden. Diese Contracts definieren die Schnittstelle, gegen die alle drei Tabs entwickeln.

Output: Neue TypeScript-Interfaces in fansub.ts und neue API-Funktionen in api.ts — analog zu bestehenden Fansub-API-Patterns.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/63-fansub-contributions-leader-frontend/63-CONTEXT.md
@.planning/phases/62-fansub-contributions-admin-api/62-CONTEXT.md

<interfaces>
<!-- Bestehende Patterns aus frontend/src/lib/api.ts (Zeilen ~2871-3200) -->
<!-- Neue Funktionen MÜSSEN exakt diesem Stil folgen -->

Bestehender api.ts-Stil (Beispiel listFansubAppMembers):
```typescript
export async function listFansubAppMembers(
  fansubId: number,
): Promise<ApiResponse<FansubAppMemberListResponse>> {
  return authFetch<FansubAppMemberListResponse>(
    `${API_BASE_URL}/api/v1/admin/fansubs/${fansubId}/app-members`,
    { method: "GET" },
  );
}
```

Bestehende fansub.ts-Interfaces als Vorlage:
```typescript
export interface FansubAppMember {
  user_id: number;
  username: string;
  display_name: string | null;
  role: FansubGroupRoleCode;
  joined_at: string;
  is_active: boolean;
}
export interface FansubAppMemberListResponse {
  members: FansubAppMember[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Neue TypeScript-Interfaces in fansub.ts</name>
  <files>frontend/src/types/fansub.ts</files>
  <read_first>
    - frontend/src/types/fansub.ts — gesamte Datei lesen, um am Ende anfügen zu können ohne Duplikate
    - .planning/phases/62-fansub-contributions-admin-api/62-CONTEXT.md — Request/Response-Strukturen aus Abschnitt "Specific Ideas"
  </read_first>
  <action>
    Am Ende von frontend/src/types/fansub.ts folgende neue Interfaces anfügen (per D-03 Datei-Größenlimit: nur Interfaces, keine Implementierungslogik):

    1. HistFansubGroupMember — repräsentiert einen Eintrag in hist_fansub_group_members:
       Felder: id (number), fansub_group_id (number), display_name (string), joined_year (number | null), left_year (number | null), app_user_id (number | null), app_username (string | null), status ("active" | "alumni"), created_at (string)

    2. HistFansubGroupMemberListResponse — { members: HistFansubGroupMember[] }
    3. HistFansubGroupMemberResponse — { member: HistFansubGroupMember }
    4. CreateGroupMemberRequest — Felder: display_name (string), joined_year (number | null), left_year (number | null), app_user_id (number | null), status ("active" | "alumni")
    5. UpdateGroupMemberRequest — alle Felder optional außer status, gleiche Typen wie Create

    6. HistGroupMemberRole — repräsentiert einen Eintrag in hist_group_member_roles:
       Felder: id (number), fansub_group_member_id (number), member_display_name (string), role_code (string), role_label (string | null), started_year (number | null), ended_year (number | null), note (string | null), status ("historical" | "confirmed"), created_at (string)

    7. HistGroupMemberRoleListResponse — { roles: HistGroupMemberRole[] }
    8. HistGroupMemberRoleResponse — { role: HistGroupMemberRole }
    9. CreateMemberRoleRequest — Felder: fansub_group_member_id (number), role_code (string), started_year (number | null), ended_year (number | null), note (string | null), status ("historical" | "confirmed")
    10. UpdateMemberRoleRequest — alle Felder optional außer Typ-Konsistenz

    11. AnimeContribution — repräsentiert eine Contribution pro Anime-Mitglied-Kombination:
        Felder: id (number), fansub_group_member_id (number), member_display_name (string), anime_id (number), role_codes (string[]), started_year (number | null), ended_year (number | null), note (string | null), is_public_on_anime_page (boolean), is_public_on_member_profile (boolean), status ("draft" | "confirmed" | "hidden"), created_at (string)

    12. AnimeContributionListResponse — { contributions: AnimeContribution[] }
    13. AnimeContributionResponse — { contribution: AnimeContribution }
    14. UpsertAnimeContributionRequest — Felder: fansub_group_member_id (number), role_codes (string[]), started_year (number | null), ended_year (number | null), note (string | null), is_public_on_anime_page (boolean), is_public_on_member_profile (boolean), status ("draft" | "confirmed" | "hidden")

    Alle Interfaces werden am Dateiende angefügt — nicht in bestehende Blöcke eingebaut.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - fansub.ts kompiliert fehlerfrei
    - Alle 14 Interfaces sind exportiert
    - Keine bestehenden Interfaces verändert
  </done>
</task>

<task type="auto">
  <name>Task 2: API-Funktionen in api.ts für Gruppen-Mitglieder, Rollen, Contributions</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts — Zeilen 2860-3200 lesen (listFansubAppMembers-Block als Pattern-Referenz)
    - frontend/src/types/fansub.ts — Task-1-Interfaces (nach Task-1-Abschluss verfügbar)
  </read_first>
  <action>
    Am Ende von frontend/src/lib/api.ts folgende Funktionen anfügen, strikt analog zu listFansubAppMembers/createFansubAppMember (authFetch, API_BASE_URL, gleiche Fehlerbehandlung durch authFetch):

    Gruppen-Mitglieder (Endpunkt /api/v1/admin/fansubs/:id/group-members):
    - listGroupMembers(fansubId: number): Promise<ApiResponse<HistFansubGroupMemberListResponse>>
    - createGroupMember(fansubId: number, body: CreateGroupMemberRequest): Promise<ApiResponse<HistFansubGroupMemberResponse>>
    - updateGroupMember(fansubId: number, memberId: number, body: UpdateGroupMemberRequest): Promise<ApiResponse<HistFansubGroupMemberResponse>>
    - deleteGroupMember(fansubId: number, memberId: number): Promise<ApiResponse<void>>

    Mitglieder-Rollen (Endpunkt /api/v1/admin/fansubs/:id/member-roles):
    - listMemberRoles(fansubId: number): Promise<ApiResponse<HistGroupMemberRoleListResponse>>
    - createMemberRole(fansubId: number, body: CreateMemberRoleRequest): Promise<ApiResponse<HistGroupMemberRoleResponse>>
    - updateMemberRole(fansubId: number, roleId: number, body: UpdateMemberRoleRequest): Promise<ApiResponse<HistGroupMemberRoleResponse>>
    - deleteMemberRole(fansubId: number, roleId: number): Promise<ApiResponse<void>>

    Anime-Contributions (Endpunkt /api/v1/admin/fansubs/:id/anime/:animeId/contributions):
    - listAnimeContributions(fansubId: number, animeId: number): Promise<ApiResponse<AnimeContributionListResponse>>
    - upsertAnimeContribution(fansubId: number, animeId: number, body: UpsertAnimeContributionRequest): Promise<ApiResponse<AnimeContributionResponse>>
    - deleteAnimeContribution(fansubId: number, animeId: number, contributionId: number): Promise<ApiResponse<void>>

    Imports am Dateianfang NICHT berühren — alle neuen Typen sind bereits durch den bestehenden fansub.ts-Import verfügbar (der Import von fansub.ts besteht schon in api.ts).

    deleteGroupMember und deleteMemberRole und deleteAnimeContribution verwenden method: "DELETE", body: undefined.
    upsertAnimeContribution verwendet method: "POST" (POST auf /contributions, nicht PATCH, da kein contributionId beim Erstellen).
    updateGroupMember und updateMemberRole verwenden method: "PATCH".
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - api.ts kompiliert fehlerfrei
    - Alle 11 Funktionen sind exportiert
    - Jede Funktion folgt dem authFetch-Pattern ohne custom fetch-Logik
    - Imports am Dateianfang unverändert
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Frontend → Backend API | Alle neuen API-Calls gehen über authFetch mit Bearer-Token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | Tampering | api.ts neue Funktionen | accept | authFetch erzwingt Auth-Header; Backend validiert Leader-Berechtigung |
| T-63-SC | Tampering | npm installs | accept | Keine neuen npm-Pakete in diesem Plan |
</threat_model>

<verification>
- `npx tsc --noEmit` in frontend/ läuft ohne Fehler
- grep auf "HistFansubGroupMember" in fansub.ts findet mindestens 5 Treffer
- grep auf "listGroupMembers\|listMemberRoles\|listAnimeContributions" in api.ts findet je einen Treffer
</verification>

<success_criteria>
- Alle 14 neuen Interfaces in fansub.ts exportiert und typsicher
- Alle 11 neuen API-Funktionen in api.ts exportiert
- TypeScript-Kompilierung fehlerfrei
- Keine bestehenden Funktionen oder Interfaces verändert
</success_criteria>

<output>
Create `.planning/phases/63-fansub-contributions-leader-frontend/63-01-SUMMARY.md` when done
</output>
