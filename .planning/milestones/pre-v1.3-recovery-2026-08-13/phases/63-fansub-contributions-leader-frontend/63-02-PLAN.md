---
phase: 63-fansub-contributions-leader-frontend
plan: 02
type: execute
wave: 2
depends_on:
  - 63-01
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/GroupMembersTab.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/MemberRolesTab.tsx
autonomous: true
requirements:
  - P63-SC1
  - P63-SC2
  - P63-SC3

must_haves:
  truths:
    - "Tab-Leiste der Fansub-Edit-Seite hat zwei neue Einträge: Mitglieder und Rollen/Timeline"
    - "Mitglieder-Tab zeigt Liste aller Gruppenmitglieder mit Name, Zeitraum, Status"
    - "Mitglieder können ohne App-User angelegt werden; App-User-Verknüpfung ist optional"
    - "Rollen/Timeline-Tab zeigt alle Rolleneinträge gruppiert nach Mitglied"
    - "Leader-Zeiträume können mit started_year/ended_year pro Mitglied erfasst werden"
    - "Leerer Zustand beider Tabs zeigt erklärenden deutschen Text"
  artifacts:
    - path: frontend/src/app/admin/fansubs/[id]/edit/GroupMembersTab.tsx
      provides: "Mitglieder-Tab-Komponente"
      contains: "GroupMembersTab"
    - path: frontend/src/app/admin/fansubs/[id]/edit/MemberRolesTab.tsx
      provides: "Rollen/Timeline-Tab-Komponente"
      contains: "MemberRolesTab"
    - path: frontend/src/app/admin/fansubs/[id]/edit/page.tsx
      provides: "Erweiterte Tab-Leiste und Tab-Rendering"
      contains: "mitglieder"
  key_links:
    - from: frontend/src/app/admin/fansubs/[id]/edit/page.tsx
      to: GroupMembersTab.tsx
      via: "activeMainTab === 'mitglieder' ? <GroupMembersTab fansubId={fansubID} /> : null"
      pattern: "GroupMembersTab"
    - from: frontend/src/app/admin/fansubs/[id]/edit/page.tsx
      to: MemberRolesTab.tsx
      via: "activeMainTab === 'rollen' ? <MemberRolesTab fansubId={fansubID} /> : null"
      pattern: "MemberRolesTab"
---

<objective>
Mitglieder-Tab und Rollen/Timeline-Tab als neue Unterkomponenten der Fansub-Edit-Seite.

Purpose: Leader können historische Gruppenmitglieder und deren Rollen/Zeiträume pflegen. Das ist die Grundlage für die Anime-Contributions in Wave 3, die aus dieser Mitgliederliste schöpfen.

Output: Zwei neue Tab-Komponenten (GroupMembersTab, MemberRolesTab) und Integration in page.tsx-Tab-Leiste.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/63-fansub-contributions-leader-frontend/63-CONTEXT.md
@.planning/phases/63-fansub-contributions-leader-frontend/63-01-SUMMARY.md

<interfaces>
<!-- Aus page.tsx (Zeilen 108-184): SectionKey und MAIN_TABS -->
```typescript
type SectionKey =
  | "basic"
  | "media"
  | "links"
  | "collaboration"
  | "releases"
  | "anime-projekte"
  | "notes";
type MainTab = SectionKey;

const MAIN_TABS: Array<{ key: MainTab; label: string }> = [
  { key: "basic", label: "Grunddaten" },
  { key: "notes", label: "Gruppengeschichte" },
  { key: "media", label: "Medien" },
  { key: "collaboration", label: "Mitglieder" },
  { key: "releases", label: "Anime & Veröffentlichungen" },
  { key: "anime-projekte", label: "Anime-Einblicke" },
];
```

<!-- Aus page.tsx (Zeilen 3159-3165): Wie externe Tab-Komponenten gerendert werden -->
```tsx
{activeMainTab === "anime-projekte" ? (
  <AnimeProjectNotesSection fansubId={fansubID} />
) : null}
{activeMainTab === "notes" ? <NotesTab fansubId={fansubID} /> : null}
```

<!-- Aus FansubAppMembersSection.tsx: Pattern für Mitglieder-Sektion -->
Props: { fansubId: number }
Imports: Badge, Button, Card, Modal aus @/components/ui
API: listFansubAppMembers, searchFansubAppMemberCandidates aus @/lib/api
Styles: sharedStyles (admin.module.css) + fansubEditStyles (FansubEdit.module.css)

<!-- Neue Typen aus Plan 01 (fansub.ts): -->
HistFansubGroupMember, HistGroupMemberRole
CreateGroupMemberRequest, UpdateGroupMemberRequest
CreateMemberRoleRequest, UpdateMemberRoleRequest
listGroupMembers, createGroupMember, updateGroupMember, deleteGroupMember
listMemberRoles, createMemberRole, updateMemberRole, deleteMemberRole
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GroupMembersTab.tsx — Mitglieder anlegen und verwalten</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/GroupMembersTab.tsx</files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.tsx — vollständig lesen (Pattern für Listendarstellung, Modal-Aufbau, Fehlerbehandlung, Styles)
    - frontend/src/types/fansub.ts — HistFansubGroupMember und Create/Update-Request-Interfaces
    - frontend/src/lib/api.ts — letzte ~50 Zeilen (neue listGroupMembers etc. Funktionen)
  </read_first>
  <action>
    Neue Datei GroupMembersTab.tsx erstellen. Maximale Dateigröße: 450 Zeilen (per D-06 CLAUDE.md). Wenn die Implementierung mehr bräuchte, sind inline-Hilfsfunktionen zusammenzufassen oder eine GroupMemberForm-Unterkomponente auszulagern.

    Prop-Signatur: `{ fansubId: number }`

    Zustand:
    - members: HistFansubGroupMember[] (geladen via listGroupMembers)
    - loading, error (wie in FansubAppMembersSection)
    - modalOpen: boolean für "Mitglied hinzufügen"-Modal
    - editTarget: HistFansubGroupMember | null (null = Neu-Anlage, nicht null = Bearbeitung)
    - form-Felder: displayName, joinedYear, leftYear, appUserId (number | null), status

    Listendarstellung:
    - Pro Mitglied eine Zeile: Name/Nick, Zeitraum (joined_year – left_year oder "aktiv"), Status-Badge ("aktiv" / "alumnus"), App-User-Verknüpfung-Indikator
    - Bearbeiten-Button pro Zeile öffnet Modal mit Vorausfüllung
    - Löschen-Button pro Zeile mit Bestätigung (window.confirm)
    - Leerer Zustand: "Noch keine Mitglieder eingetragen. Füge das erste Mitglied hinzu."

    Modal "Mitglied hinzufügen / bearbeiten":
    - Input: Anzeigename (Freitext, Pflichtfeld, min 1 Zeichen)
    - YearSelectField (analog page.tsx Zeilen 832-861): Beitrittsjahr, Austrittsjahr (beide optional)
    - App-User verknüpfen: einfaches Zahlenfeld für app_user_id (optional), KEIN voller MemberSelector im MVP — Freitext-Zahl reicht hier, da Phase 63 kein eigenes UserSearch-Widget einführt
    - Status-Select: "aktiv" / "alumnus" (Wert: "active" / "alumni")
    - Speichern-Button ruft createGroupMember oder updateGroupMember auf
    - Fehleranzeige im Modal

    Styles: sharedStyles + fansubEditStyles (gleicher Merge wie FansubAppMembersSection.tsx Zeile 31).

    Alle user-facing Strings auf Deutsch mit korrekten Umlauten. Keine ASCII-Ersetzungen.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - GroupMembersTab.tsx existiert, kompiliert fehlerfrei
    - Unter 450 Zeilen
    - listGroupMembers wird beim Mount aufgerufen
    - createGroupMember/updateGroupMember werden beim Speichern aufgerufen
    - Leerer Zustand mit deutschem Text vorhanden
  </done>
</task>

<task type="auto">
  <name>Task 2: MemberRolesTab.tsx — Rollen/Timeline anlegen</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/MemberRolesTab.tsx</files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/GroupMembersTab.tsx — nach Task-1-Abschluss lesen um Stilkonsistenz sicherzustellen
    - frontend/src/types/fansub.ts — HistGroupMemberRole, CreateMemberRoleRequest
    - frontend/src/lib/api.ts — listMemberRoles, createMemberRole, updateMemberRole, deleteMemberRole
  </read_first>
  <action>
    Neue Datei MemberRolesTab.tsx erstellen. Maximale Dateigröße: 450 Zeilen.

    Prop-Signatur: `{ fansubId: number }`

    Zustand:
    - roles: HistGroupMemberRole[] (geladen via listMemberRoles)
    - members: HistFansubGroupMember[] (geladen via listGroupMembers — für Mitglied-Dropdown im Formular)
    - loading, error
    - modalOpen: boolean
    - editTarget: HistGroupMemberRole | null

    Listendarstellung (chronologisch, nicht als Tabelle — per CONTEXT.md Specific Ideas):
    - Pro Rolle eine Zeile im Stil: "2001–2005  Sora  Gründer/in, Leader"
    - Spalten: Zeitraum (started_year–ended_year), Mitglied-Name, Rolle, Status-Badge
    - Sortierung: nach started_year absteigend (neueste zuerst)
    - Leerer Zustand: "Noch keine Rolleneinträge vorhanden."
    - Bearbeiten- und Löschen-Button pro Zeile

    Modal "Rolle hinzufügen / bearbeiten":
    - Mitglied-Dropdown: aus members-Liste (value = member.id, label = member.display_name) — Pflichtfeld
    - Rollen-Input: Freitext für role_code (Pflichtfeld) — KEIN vordefiniertes Dropdown, da role_definitions backend-seitig validiert werden
    - Von-Jahr / Bis-Jahr: YearSelectField (beide optional)
    - Notiz: Input (optional)
    - Status-Select: "historisch" / "bestätigt" (Wert: "historical" / "confirmed")
    - Speichern ruft createMemberRole oder updateMemberRole auf
    - Fehleranzeige im Modal

    Styles: sharedStyles + fansubEditStyles (gleicher Merge-Stil).
    Alle user-facing Strings auf Deutsch mit korrekten Umlauten.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - MemberRolesTab.tsx existiert, kompiliert fehlerfrei
    - Unter 450 Zeilen
    - listMemberRoles und listGroupMembers werden beim Mount aufgerufen
    - Rollenzeilen sortiert nach started_year absteigend
    - Leerer Zustand mit deutschem Text vorhanden
  </done>
</task>

<task type="auto">
  <name>Task 3: page.tsx — Neue Tabs in SectionKey, MAIN_TABS und Rendering integrieren</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/page.tsx</files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx — Zeilen 108-184 (SectionKey, MainTab, MAIN_TABS) und Zeilen 3159-3165 (Tab-Rendering-Ende)
  </read_first>
  <action>
    Vier präzise Änderungen an page.tsx — keine Logik sonst berühren:

    1. SectionKey erweitern: | "mitglieder" | "rollen" zu den bestehenden Typen hinzufügen (Zeile ~108-115)

    2. MAIN_TABS erweitern: Nach dem "collaboration"-Eintrag zwei neue Einträge einfügen:
       { key: "mitglieder", label: "Mitglieder" }
       { key: "rollen", label: "Rollen/Timeline" }

    3. openSections initial state erweitern: mitglieder: true, rollen: true hinzufügen (Zeile ~941-951)

    4. Imports am Dateianfang: GroupMembersTab und MemberRolesTab importieren (analog zu NotesTab-Import Zeile ~84)

    5. Tab-Rendering am Dateiende: Nach dem `{activeMainTab === "notes" ? <NotesTab ... /> : null}`-Block zwei neue Blöcke anfügen:
       ```
       {activeMainTab === "mitglieder" ? <GroupMembersTab fansubId={fansubID} /> : null}
       {activeMainTab === "rollen" ? <MemberRolesTab fansubId={fansubID} /> : null}
       ```

    6. Die Bedingung in Zeile ~2131 (`activeMainTab !== "releases" && activeMainTab !== "anime-projekte" && activeMainTab !== "notes"`) muss auch "mitglieder" und "rollen" ausschließen, damit der Formular-Wrapper bei diesen Tabs nicht gerendert wird.

    WICHTIG: page.tsx ist bereits sehr groß. Nur die 6 genannten Stellen ändern. Keine Refaktorierungen.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - page.tsx kompiliert fehlerfrei
    - MAIN_TABS enthält "Mitglieder" und "Rollen/Timeline"
    - Beide Tab-Komponenten werden bei aktivem Tab gerendert
    - Bei "mitglieder" und "rollen" wird kein Formular-Wrapper gerendert
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Leader-Frontend → Admin-API | API-Calls über authFetch mit Bearer-Token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-02 | Elevation of Privilege | GroupMembersTab API-Calls | accept | Backend-Middleware prüft Leader-Berechtigung; Frontend zeigt nur bei hasFansubWorkspaceAccess=true |
| T-63-03 | Tampering | window.confirm vor deleteGroupMember | accept | Kein kritisches Asset; Backend-seitige Löschvalidierung vorhanden |
| T-63-SC | Tampering | npm installs | accept | Keine neuen npm-Pakete |
</threat_model>

<verification>
- `npx tsc --noEmit` fehlerfrei
- MAIN_TABS in page.tsx: grep findet "mitglieder" und "rollen"
- GroupMembersTab.tsx und MemberRolesTab.tsx existieren und sind je unter 450 Zeilen
- Leere-Zustands-Texte auf Deutsch vorhanden
</verification>

<success_criteria>
- Tab-Leiste zeigt "Mitglieder" und "Rollen/Timeline" neben bestehenden Tabs
- Mitglieder-Tab: Ladeliste, Hinzufügen-Modal, Bearbeiten/Löschen pro Zeile
- Rollen-Tab: Chronologische Liste, Modal mit Mitglied-Dropdown und Rollenfeldern
- Beide Tabs: Leerer Zustand mit deutschem erklärendem Text
- TypeScript fehlerfrei
</success_criteria>

<output>
Create `.planning/phases/63-fansub-contributions-leader-frontend/63-02-SUMMARY.md` when done
</output>
