---
phase: 20-release-native-episode-import-schema
plan: 02
type: backend
wave: 2
depends_on:
  - 20-01-PLAN.md
files_modified:
  - backend/internal/services/anisearch_episode_import.go
  - backend/internal/repository/episode_import_repository.go
  - backend/internal/handlers/admin_episode_import.go
  - backend/internal/models/episode_import.go
autonomous: true
requirements:
  - P20-SC2
  - P20-SC3
  - P20-SC4
  - P20-SC5
must_haves:
  truths:
    - "Episode import apply writes the normalized release graph, not only legacy episode_versions."
    - "AniSearch episode titles are persisted per language where available."
    - "Filler metadata is parsed, persisted, and returned through admin APIs."
---

<objective>
Move episode import apply semantics from legacy versions to the release-native graph.

Purpose: The schema only matters if the backend uses it as the authoritative write path.
Output: Repository and handler code that persists canonical episodes, multilingual titles, filler flags, release sources, releases, versions, variants, stream sources, streams, group joins, and coverage joins.
</objective>

<context>
@.planning/phases/20-release-native-episode-import-schema/20-CONTEXT.md
@.planning/phases/19-episode-import-operator-workbench/19-UAT.md
@backend/internal/services/anisearch_episode_import.go
@backend/internal/repository/episode_import_repository.go
@backend/internal/handlers/admin_episode_import.go
@backend/internal/models/episode_import.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parse and carry multilingual titles plus filler fields</name>
  <files>backend/internal/services/anisearch_episode_import.go, backend/internal/models/episode_import.go, backend/internal/services tests</files>
  <action>
    Extend the AniSearch import model so each canonical episode can carry `titles_by_language` and filler metadata. Preserve the display title fallback order German, English, Japanese, generated fallback.
  </action>
  <verify>
    <automated>Run focused AniSearch parser tests against 11eyes and Naruto-like episode fixtures, including a case with German/English/Japanese titles and filler metadata.</automated>
  </verify>
  <done>Preview data includes canonical titles and filler information before persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Persist canonical episode records and titles</name>
  <files>backend/internal/repository/episode_import_repository.go, backend/internal/repository/episode_import_repository_test.go</files>
  <action>
    Upsert `episodes` by `(anime_id, number, episode_type_id)`, fill number decimal/text/sort/filler fields, and upsert `episode_titles` for every parsed language. Keep `episodes.title` as a compatibility display cache while reads migrate.
  </action>
  <verify>
    <automated>Repository tests prove repeated import is idempotent and does not duplicate episodes or titles.</automated>
  </verify>
  <done>Canonical episode metadata is normalized and repeat-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Persist release-native media graph</name>
  <files>backend/internal/repository/episode_import_repository.go, backend/internal/repository/episode_import_repository_test.go</files>
  <action>
    For every confirmed file mapping, create/upsert:
    - `ReleaseSource`
    - `FansubRelease`
    - `ReleaseVersion`
    - `ReleaseVariant`
    - `ReleaseVersionGroup`
    - `StreamSource`
    - `ReleaseStream`
    - `ReleaseVariantEpisode`
    Derive source/group/version conservatively from file evidence and allow operator overrides from the preview/apply DTO.
  </action>
  <verify>
    <automated>Repository tests cover multiple files for episode 1, one file mapped to episodes 9 and 10, and repeated apply idempotency.</automated>
  </verify>
  <done>New imports can be queried entirely through the release graph.</done>
</task>

</tasks>

<success_criteria>
- Applying a preview writes normalized release rows for confirmed mappings.
- Filler and title metadata are persisted with canonical episodes.
- Multi-release and multi-episode-file scenarios are represented without legacy-only rows.
</success_criteria>

<output>
Create `20-02-SUMMARY.md` with query examples showing episode, title, release, variant, stream, and coverage rows.
</output>

