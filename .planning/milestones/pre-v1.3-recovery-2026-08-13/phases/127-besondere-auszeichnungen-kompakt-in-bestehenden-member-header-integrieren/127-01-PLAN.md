---
phase: 127-besondere-auszeichnungen-kompakt-in-bestehenden-member-header-integrieren
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/profile/MemberProfileHero.test.tsx
  - frontend/src/app/members/[slug]/page.test.tsx
  - frontend/src/components/profile/MemberBadgeChain.test.tsx
  - frontend/src/components/profile/badgeArtwork.test.ts
  - .planning/phases/127-besondere-auszeichnungen-kompakt-in-bestehenden-member-header-integrieren/evidence/phase127-red-gate.sh
autonomous: true
requirements: [D-04, D-05, D-06, D-07, D-12, D-13, D-14, D-15, D-16, D-18, D-19, D-20, D-21, D-22, D-27, D-29, D-30, D-31, D-32, D-33, D-34, D-35, D-36, D-37, D-41, D-42, D-43, D-44, D-45, D-46, D-47]
must_haves:
  truths:
    - Tests define zero, either one, both, Verified-once and Founding-Membership-only behavior before production changes.
    - Tests require SSR public_badges to reach the hero and require the legacy Special collection to be absent while five other groups remain.
  artifacts:
    - path: frontend/src/components/profile/MemberProfileHero.test.tsx
      provides: Header award, accessibility and responsive contracts
    - path: frontend/src/app/members/[slug]/page.test.tsx
      provides: SSR composition and exact-once integration contracts
    - path: frontend/src/components/profile/MemberBadgeChain.test.tsx
      provides: Legacy suppression and five-group regression contracts
    - path: frontend/src/components/profile/badgeArtwork.test.ts
      provides: Preservation oracle for every extracted mapping and fallback branch
    - path: .planning/phases/127-besondere-auszeichnungen-kompakt-in-bestehenden-member-header-integrieren/evidence/phase127-red-gate.sh
      provides: Fail-closed RED verifier
  key_links:
    - from: frontend/src/app/members/[slug]/page.test.tsx
      to: frontend/src/components/profile/MemberProfileHero.tsx
      via: profile.public_badges prop contract
    - from: frontend/src/components/profile/MemberBadgeChain.test.tsx
      to: data-badge-group
      via: special absent; roles/projects/points/contributions/membership retained
---

<objective>
Create the failing executable contract for compact header specials and all deduplication/regression boundaries before production edits.

Purpose: Make D-01–D-49 behavior mechanically unambiguous while preserving the dirty Phase-121–126 baseline.
Output: Focused RED tests plus a deterministic Phase-127 ownership manifest.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
<read_first>
@AGENTS.md
@docs/engineering/implementation-contract.md
@docs/frontend/ui-system.md
@docs/agent-guidelines-ui.md
@.planning/phases/127-besondere-auszeichnungen-kompakt-in-bestehenden-member-header-integrieren/127-PRD.md
@.planning/phases/127-besondere-auszeichnungen-kompakt-in-bestehenden-member-header-integrieren/127-CONTEXT.md
@.planning/phases/127-besondere-auszeichnungen-kompakt-in-bestehenden-member-header-integrieren/127-VALIDATION.md
@.planning/phases/127-besondere-auszeichnungen-kompakt-in-bestehenden-member-header-integrieren/127-PATTERNS.md:23
@frontend/src/components/profile/MemberProfileHero.test.tsx
@frontend/src/app/members/[slug]/page.test.tsx
@frontend/src/components/profile/MemberBadgeChain.test.tsx
@frontend/src/components/profile/memberBadgeLabels.test.ts
@frontend/src/components/profile/MemberBadgeChain.tsx:72
@.planning/phases/125-beitragsbadges-als-echtes-familien-carousel-mit-visuellen-st/125-UAT.md
@.planning/phases/126-mitgliedschaftsbadges-fachlich-trennen-responsive-membership-stage/126-UAT.md
</read_first>

Phase 125 and Phase 126 are incomplete and must stay reported as incomplete. PATTERNS lines 136–159 define exact test analogs; lines 195–207 define isolation.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Freeze incoming state and author the RED hero/page contracts</name>
  <files>frontend/src/components/profile/MemberProfileHero.test.tsx, frontend/src/app/members/[slug]/page.test.tsx</files>
  <behavior>
    - No approved special produces no wrapper/gap; Historical-only uses approved resolver artwork; Allrounder-only uses Hexagon; both render once in catalog order.
    - verified/founding/role/unknown codes are rejected; isVerified plus both specials produces one Verifiziert; Founding appears only in Membership.
    - Static list is named, ordered and nonfocusable; art is decorative; CSS source requires normalized slot, contain, flex-wrap, min-width/no overflow.
    - Page passes existing profile.public_badges to hero through the one cached SSR request and adds no request.
  </behavior>
  <action>Before editing, create `.planning/phases/127-besondere-auszeichnungen-kompakt-in-bestehenden-member-header-integrieren/evidence/127-01-incoming/` containing status, unstaged binary diff, cached diff, `git ls-files -s`, `git write-tree`, SHA-256 and byte copies for every authorized file plus MemberBadgeChain.tsx/module.css/memberBadgeLabels.ts and protected FocalCarousel files. Persist `BASE_INDEX_TREE`, baseline cached binary patch and indexed blob manifest in `127-01-MANIFEST.md`. Add the cases above per D-04–D-37. Create `phase127-red-gate.sh`: run only the two named test files into a durable raw log; require nonzero test exit; require the exact Phase-127 test titles listed in `<behavior>` to be the complete failing-test set; reject missing named failures, any additional/inherited failure, suite startup/import/transform/syntax/timeout/unhandled error, zero collected tests, or a passing suite. The wrapper itself exits 0 only for that exact RED state. For staging, create an exact patch, run `git apply --cached --check`, then apply transactionally; validate `git diff --cached "$BASE_INDEX_TREE" --binary`, path list, indexed blob IDs and hashes equal only the declared Phase-127 delta. Install an EXIT/error trap that runs `git read-tree "$BASE_INDEX_TREE"` on any mismatch, proving `git write-tree == BASE_INDEX_TREE`; this restores the index only and preserves worktree/untracked files. Never `git add <file>` or whole-file stage.</action>
  <verify><automated>bash .planning/phases/127-besondere-auszeichnungen-kompakt-in-bestehenden-member-header-integrieren/evidence/phase127-red-gate.sh hero-page</automated></verify>
  <done>New Phase-127 expectations fail specifically because hero integration is absent; incoming manifest proves exact ownership and preserves any pre-existing index.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Specify legacy suppression and unchanged five-group behavior</name>
  <files>frontend/src/components/profile/MemberBadgeChain.test.tsx, frontend/src/components/profile/badgeArtwork.test.ts</files>
  <behavior>
    - Historical/Allrounder/Verified combinations never render data-badge-group=special or its heading, skeleton, carousel, arrows or counter.
    - Roles, projects, points, all three contribution families, and membership keep their existing selectors, progress/preview and carousel ownership.
    - Every current artwork mapping/fallback (first contribution, veteran, productive tiers, versioned points, nine contributions, four memberships, Historical, generic contribution, role entry, timer role-volume, other role-volume, unknown) is preserved exactly by the future extraction.
  </behavior>
  <action>Replace only the obsolete positive Special consumer assertion identified at PATTERNS lines 153–159. Add `badgeArtwork.test.ts` as the RED contract importing the exact future `resolveBadgeArtwork` export and asserting every mapping/fallback branch enumerated above, including undefined unknown. Preserve dirty Phase-121–126 tests byte-for-byte outside the exact hunk. Extend the RED wrapper so `chain-artwork` accepts only the named missing-suppression and missing-module/export failures and rejects inherited/unexpected/suite/import failures other than the explicitly named not-yet-created `./badgeArtwork` module. Generate `127-01.patch`; exact allowed path set is MemberProfileHero.test.tsx, page.test.tsx, MemberBadgeChain.test.tsx, badgeArtwork.test.ts and evidence/phase127-red-gate.sh. Use the same baseline-tree transactional `git apply --cached` protocol and fail-safe `git read-tree` restoration; if ownership is inseparable, restore index, leave app work unstaged and report.</action>
  <verify><automated>bash .planning/phases/127-besondere-auszeichnungen-kompakt-in-bestehenden-member-header-integrieren/evidence/phase127-red-gate.sh chain-artwork</automated></verify>
  <done>The old Special expectation is a targeted failing suppression contract and all five retained presentation families have explicit regression assertions.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-127-01 | T | dirty worktree/index | mitigate | Incoming byte/hash/index manifest, exact patch staging, abort on inseparable ownership per D-45. |
| T-127-02 | I | public SSR data | mitigate | Tests require existing single cached request and prohibit new fetch/API per D-27/D-44/D-46. |
</threat_model>

<verification>RED failures are Phase-127-specific; `git diff --check`, cached-delta equality, exact path-set and protected-file hash equality pass.</verification>
<success_criteria>Contracts cover D-04–D-47 without production edits, predecessor completion is not claimed, and exact incoming state is recoverable.</success_criteria>
<output>After completion create `127-01-SUMMARY.md` without claiming Phase 125/126 complete.</output>
