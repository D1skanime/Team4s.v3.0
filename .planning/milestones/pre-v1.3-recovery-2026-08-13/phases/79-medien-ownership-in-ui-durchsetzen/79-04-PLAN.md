---
phase: 79-medien-ownership-in-ui-durchsetzen
plan: 04
type: execute
wave: 2
depends_on:
  - 79-01
  - 79-02
files_modified:
  - frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.helpers.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseVersionMedia.ts
  - frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinAssetUploadControls.tsx
autonomous: true
requirements:
  - G
  - I
  - K

must_haves:
  truths:
    - "D-07: ReleaseVersionMediaSection.tsx bindet MediaOwnershipContext mit ownerType='release_version', categoryMode='dropdown', statusPolicy='in_review' ein (Surface 4, Prozessmedien)"
    - "D-03: Surface 4 sendet Default reviewStatusCode='in_review', visibilityCode='private' an uploadReleaseVersionMedia"
    - "D-08: Kategorie-Dropdown in Surface 4 bleibt echtes Dropdown (ReleaseVersionMediaCategory) — bereits als Select-Primitive vorhanden"
    - "D-06: handleUploadClick in ReleaseVersionMediaSection.tsx blockiert wenn ownerCtx.ownerResolved=false"
    - "D-07: AnimeJellyfinAssetUploadControls.tsx bindet MediaOwnershipContext mit ownerType='anime', categoryMode='slot', statusPolicy='immediate' ein (Surface 2, Branding)"
    - "D-09: Surface 2 sendet visibilityCode='public', reviewStatusCode='approved' (Anime-Assets = Branding-Klasse)"
    - "useReleaseVersionMedia.ts startUpload-Signatur akzeptiert optionale visibilityCode/reviewStatusCode Parameter"
    - "CLAUDE.md 450-Zeilen-Limit: ReleaseVersionMediaSection.tsx ≤ 450 Zeilen nach Änderungen; falls Überschreitung → ReleaseVersionMediaSection.helpers.tsx enthält ausgelagerte Utilities"
  artifacts:
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.tsx"
      provides: "Surface 4 mit MediaOwnershipContext (volles Pflichtfeld-Formular)"
      contains: "MediaOwnershipContext"
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseVersionMedia.ts"
      provides: "Erweiterte startUpload-Signatur mit Status-Codes"
      contains: "visibilityCode"
    - path: "frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinAssetUploadControls.tsx"
      provides: "Surface 2 mit MediaOwnershipContext (Branding-Slot)"
      contains: "MediaOwnershipContext"
  key_links:
    - from: "ReleaseVersionMediaSection.tsx"
      to: "useReleaseVersionMedia.ts startUpload"
      via: "media.startUpload(..., ownerCtx.visibilityCode, ownerCtx.reviewStatusCode)"
      pattern: "startUpload"
    - from: "useReleaseVersionMedia.ts"
      to: "uploadReleaseVersionMedia in api.ts"
      via: "visibilityCode, reviewStatusCode Parameter weitergeben"
      pattern: "visibilityCode"
    - from: "AnimeJellyfinAssetUploadControls.tsx"
      to: "uploadAdminAnimeMedia in api.ts"
      via: "visibilityCode: ownerCtx.visibilityCode, reviewStatusCode: ownerCtx.reviewStatusCode"
      pattern: "reviewStatusCode"
---

<objective>
Surface 4 (Release-Version Process Media) und Surface 2 (Anime Assets) auf die gemeinsame MediaOwnershipContext-Komponente heben. Surface 4 ist der komplexeste Fall: sie nutzt das volle Pflichtfeld-Formular (categoryMode='dropdown', statusPolicy='in_review') über den useReleaseVersionMedia-Hook. Surface 2 (Anime-Assets) ist ein einfacher Branding-Slot (statusPolicy='immediate').

Purpose: Nach diesem Plan sind 4 von 5 Surfaces auf Owner-/Status-Erzwingung gehoben. Pläne 03 und 04 laufen in Wave 2 parallel (keine gemeinsamen Dateien).
Output: Erweiterte ReleaseVersionMediaSection.tsx + useReleaseVersionMedia.ts (Surface 4); AnimeJellyfinAssetUploadControls.tsx (Surface 2).
</objective>

<execution_context>
@C:\Users\admin\Documents\Team4s\.planning\phases\79-medien-ownership-in-ui-durchsetzen\79-UI-SPEC.md
@C:\Users\admin\Documents\Team4s\.planning\phases\79-medien-ownership-in-ui-durchsetzen\79-RESEARCH.md
@C:\Users\admin\Documents\Team4s\.planning\phases\79-medien-ownership-in-ui-durchsetzen\79-PATTERNS.md
@C:\Users\admin\Documents\Team4s\.planning\phases\79-medien-ownership-in-ui-durchsetzen\79-CONTEXT.md
@C:\Users\admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\admin\Documents\Team4s\.planning\ROADMAP.md

<interfaces>
<!-- Aus Plan 79-01: MediaOwnershipContext -->
MediaOwnershipContextProps { ownerType, ownerID, ownerLabel, categoryMode, categoryOptions?, categoryValue?, statusPolicy, disabled?, onContextChange }
MediaOwnershipContextValue { ownerResolved: boolean; visibilityCode: string; reviewStatusCode: string; categoryValue: string }

<!-- Aus 79-PATTERNS.md ReleaseVersionMediaSection.tsx + useReleaseVersionMedia.ts -->
Bestehender selectedCategory-State (Z. 80–85 der Section):
  const [selectedCategory, setSelectedCategory] = useState<ReleaseVersionMediaCategory | ''>('')

Bestehender handleUploadClick (Z. 185–197):
  async function handleUploadClick() {
    if (!selectedCategory || selectedFiles.length === 0) return
    await media.startUpload(selectedCategory, selectedFiles, defaultCaption, isPreviewCandidate)
    setSelectedFiles([])
  }

useReleaseVersionMedia.ts bestehende startUpload-Signatur (Z. 41–46):
  startUpload: (
    category: ReleaseVersionMediaCategory, files: File[],
    defaultCaption?: string, isPreviewCandidate?: boolean,
  ) => Promise<void>

Kategorie-Optionen (UI-SPEC Kategorie-Optionen release_version_media):
  [{value:'screenshot',label:'Screenshot'}, {value:'typesetting_karaoke',label:'Typesetting / Karaoke'}, {value:'fun_outtake',label:'Fun / Outtake'}, {value:'other',label:'Sonstiges'}]

<!-- Aus 79-PATTERNS.md AnimeJellyfinAssetUploadControls.tsx -->
Transport: uploadAdminAnimeMedia({ animeID, assetType, file, onProgress })
Props: animeID: number, animeTitle?: string (oder ähnlich für ownerLabel)
Kategorie: assetType als Slot (poster/banner/logo/background_video) — read-only
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface 4 — useReleaseVersionMedia.ts startUpload-Erweiterung</name>
  <files>
    frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseVersionMedia.ts
  </files>
  <read_first>
    - frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseVersionMedia.ts — vollständig lesen (startUpload-Funktion, uploadReleaseVersionMedia-Aufruf, Hook-Interface)
    - frontend/src/lib/api.ts — Z. 6245–6285 (uploadReleaseVersionMedia nach 79-02-Erweiterung: visibilityCode/reviewStatusCode in Options)
    - .planning/phases/79-medien-ownership-in-ui-durchsetzen/79-PATTERNS.md — Abschnitt useReleaseVersionMedia.ts
  </read_first>
  <action>
    startUpload-Signatur um zwei optionale Parameter erweitern (am Ende der Parameterliste):
    visibilityCode?: string und reviewStatusCode?: string.

    Im Funktionskörper von startUpload: uploadReleaseVersionMedia-Aufruf um diese Werte ergänzen:
    uploadReleaseVersionMedia({ ..., visibilityCode, reviewStatusCode })

    Das Hook-Interface (return type) entsprechend aktualisieren wenn startUpload dort typisiert ist.

    Keine anderen Teile des Hooks ändern. Bestehende Aufrufstellen in der Section bleiben kompatibel (optionale Parameter).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    useReleaseVersionMedia.ts startUpload-Signatur hat visibilityCode? und reviewStatusCode? als optionale Parameter.
    uploadReleaseVersionMedia erhält diese Werte wenn übergeben.
    TypeScript kompiliert ohne Fehler.
  </done>
</task>

<task type="auto">
  <name>Task 2: Surface 4 — ReleaseVersionMediaSection.tsx: MediaOwnershipContext (volles Pflichtfeld-Formular) + 450-Zeilen-Check</name>
  <files>
    frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.tsx,
    frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.helpers.tsx
  </files>
  <read_first>
    - frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.tsx — vollständig lesen; Zeilenzahl ermitteln (wc -l oder Dateilänge aus Read-Ausgabe ablesen — letzter Zeilenindex ist die aktuelle Länge). Falls die Datei bereits ≥ 420 Zeilen hat: Split VOR den neuen Änderungen planen (MediaOwnershipContext-Einbindung fügt ca. 25–35 Zeilen hinzu). Auszulagernde Logik für ReleaseVersionMediaSection.helpers.tsx: CATEGORY_OPTIONS-Konstante, CATEGORY_LABELS-ähnliche Hilfsfunktionen, reine Daten-Utilities — keine State-/Hook-Logik auslagern.
    - frontend/src/components/admin/media/MediaOwnershipContext.tsx — aus Plan 79-01 erzeugt; Props-Interface
    - .planning/phases/79-medien-ownership-in-ui-durchsetzen/79-UI-SPEC.md — Surface-Matrix Zeile 4: ownerType='release_version', categoryMode='dropdown', statusPolicy='in_review', volle Formular-Last
    - .planning/phases/79-medien-ownership-in-ui-durchsetzen/79-PATTERNS.md — Abschnitt ReleaseVersionMediaSection.tsx
  </read_first>
  <action>
    Schritt 0 — Zeilen-Check und ggf. vorab splitten:
    Aktuelle Zeilenzahl von ReleaseVersionMediaSection.tsx feststellen. Falls die Datei nach Hinzufügen von MediaOwnershipContext (ca. +30 Zeilen) über 450 Zeilen käme:
    Erstelle frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.helpers.tsx und lagere darin aus:
    - Die CATEGORY_OPTIONS-Konstante (oder äquivalente Kategorie-Daten)
    - Reine Utility-Funktionen ohne State-Abhängigkeit (z.B. Kategorie-Label-Helfer, Formatierungsfunktionen)
    ReleaseVersionMediaSection.tsx importiert diese aus der helpers-Datei.
    Kein State, keine Hooks, keine JSX in helpers.tsx auslagern.

    Schritt 1 — MediaOwnershipContext einbinden:
    MediaOwnershipContext und MediaOwnershipContextValue aus '@/components/admin/media/MediaOwnershipContext' importieren.
    State hinzufügen: const [ownerCtx, setOwnerCtx] = useState<MediaOwnershipContextValue | null>(null)

    Kategorie-Optionen als Konstante (in helpers.tsx oder direkt in Section falls Platz reicht):
    const CATEGORY_OPTIONS = [
      { value: 'screenshot', label: 'Screenshot' },
      { value: 'typesetting_karaoke', label: 'Typesetting / Karaoke' },
      { value: 'fun_outtake', label: 'Fun / Outtake' },
      { value: 'other', label: 'Sonstiges' },
    ]
    (Exakte Strings aus UI-SPEC Kategorie-Optionen)

    Im JSX: MediaOwnershipContext VOR dem Upload-Bereich einbinden:
    - ownerType="release_version"
    - ownerID={versionId} (aus Props — versionId identifiziert den Owner-Kontext eindeutig)
    - ownerLabel: Release-Version-Label aus verfügbarem Kontext (z.B. 'Release-Version' als Fallback wenn kein Name-Prop vorhanden; idealerweise z.B. `Version ${versionId}`)
    - categoryMode="dropdown" (D-08: RVM hat echtes Kategorie-Dropdown)
    - categoryOptions={CATEGORY_OPTIONS}
    - statusPolicy="in_review" (D-03: Prozessmedien-Default)
    - disabled={media.isUploading}
    - onContextChange={(ctx) => { setOwnerCtx(ctx); wenn ctx.categoryValue gesetzt ist, setSelectedCategory(ctx.categoryValue as ReleaseVersionMediaCategory) }}

    WICHTIG: Da MediaOwnershipContext das Kategorie-Dropdown übernimmt (categoryMode='dropdown'), bestehenden Kategorie-Select-Block im JSX der Section ENTFERNEN oder ausblenden (Dopplung vermeiden). Der Zustand selectedCategory wird durch den MediaOwnershipContext-Callback befüllt.

    handleUploadClick erweitern:
    - D-06-Guard: wenn !ownerCtx?.ownerResolved → setError('Upload nicht möglich: Kein gültiger Owner-Kontext.') und return
    - media.startUpload-Aufruf: + ownerCtx?.visibilityCode, ownerCtx?.reviewStatusCode als letzte Parameter

    Schritt 2 — Abschluss-Check:
    wc -l ReleaseVersionMediaSection.tsx → muss ≤ 450 sein.
    Falls helpers.tsx erstellt: wc -l ReleaseVersionMediaSection.helpers.tsx prüfen (kein Limit-Problem zu erwarten).

    Alle user-facing Strings deutsch. Keine nativen Form-Elemente einführen.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20 && wc -l src/app/admin/episode-versions/*/edit/ReleaseVersionMediaSection.tsx</automated>
  </verify>
  <done>
    ReleaseVersionMediaSection.tsx rendert MediaOwnershipContext mit ownerType='release_version', categoryMode='dropdown', statusPolicy='in_review'.
    handleUploadClick prüft ownerCtx.ownerResolved.
    media.startUpload erhält visibilityCode und reviewStatusCode aus ownerCtx.
    Kein doppeltes Kategorie-Dropdown (MediaOwnershipContext übernimmt die Kategorie-Auswahl für Surface 4).
    ReleaseVersionMediaSection.tsx ≤ 450 Zeilen (wc -l bestätigt).
    Falls helpers.tsx erstellt: enthält nur Konstanten/Utilities, kein State.
    TypeScript kompiliert ohne Fehler.
  </done>
</task>

<task type="auto">
  <name>Task 3: Surface 2 — AnimeJellyfinAssetUploadControls.tsx: MediaOwnershipContext (Branding-Slot)</name>
  <files>
    frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinAssetUploadControls.tsx
  </files>
  <read_first>
    - frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinAssetUploadControls.tsx — vollständig lesen (Props, Upload-Handler, uploadAdminAnimeMedia-Aufruf, JSX-Struktur)
    - .planning/phases/79-medien-ownership-in-ui-durchsetzen/79-UI-SPEC.md — Surface-Matrix Zeile 2: ownerType='anime', categoryMode='slot', statusPolicy='immediate'
    - .planning/phases/79-medien-ownership-in-ui-durchsetzen/79-RESEARCH.md — Surface 2 Befunde (animeID Prop, assetType als Slot-Kategorie)
  </read_first>
  <action>
    MediaOwnershipContext und MediaOwnershipContextValue aus '@/components/admin/media/MediaOwnershipContext' importieren.

    Die Komponente verwaltet mehrere Asset-Typen (poster/banner/logo/background_video). Eine MediaOwnershipContext-Instanz pro Upload-Aktion wäre aufwändig. Stattdessen: ownerCtx als gemeinsamer State für den gesamten Upload-Bereich (alle Slots teilen denselben animeID-Owner-Kontext).

    State: const [ownerCtx, setOwnerCtx] = useState<MediaOwnershipContextValue | null>(null)

    Im JSX: Eine MediaOwnershipContext-Instanz für den gesamten Anime-Asset-Upload-Block:
    - ownerType="anime"
    - ownerID={animeID}
    - ownerLabel: Anime-Titel aus Props (wenn vorhanden) oder `Anime ${animeID}`; Format: 'Upload für: Anime «{titel}» · Owner-Typ: Anime' wird in MediaOwnershipContext gerendert
    - categoryMode="slot" (D-08: Anime-Assets sind Slots — read-only)
    - categoryValue: Slot-Name des aktuell aktiven/fokussierten Upload-Slots (kann als generischer 'anime-asset' gesetzt werden, da der eigentliche Slot-Typ im assetType-Parameter des Handlers steckt)
    - statusPolicy="immediate" (D-09: Anime-Assets = Branding-Klasse; Assumption A2 bestätigt)
    - onContextChange={setOwnerCtx}

    Jeden Upload-Handler (handleCoverUpload, handleBannerUpload, handleLogoUpload etc.) erweitern:
    - D-06-Guard: wenn !ownerCtx?.ownerResolved → setError('Upload nicht möglich: Kein gültiger Owner-Kontext.') und return
    - uploadAdminAnimeMedia-Aufruf: + visibilityCode: ownerCtx.visibilityCode, reviewStatusCode: ownerCtx.reviewStatusCode

    Alle Strings deutsch mit Umlauten. Keine nativen Form-Elemente einführen.
    Dateigröße ≤ 450 Zeilen prüfen.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20 && npx vitest run src/components/admin/media/ --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>
    AnimeJellyfinAssetUploadControls.tsx rendert MediaOwnershipContext mit ownerType='anime', statusPolicy='immediate'.
    Alle Upload-Handler prüfen ownerCtx.ownerResolved.
    uploadAdminAnimeMedia erhält visibilityCode+reviewStatusCode.
    TypeScript kompiliert ohne Fehler.
    Bestehende Tests (media/) grün.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Grenze | Beschreibung |
|--------|-------------|
| versionId → ownerID (Surface 4) | kommt aus Route-Param; Backend CanForReleaseVersion ist Authority |
| animeID → ownerID (Surface 2) | kommt aus Route-Param; Backend Anime-Admin-Gate ist Authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-04-01 | Broken Access Control (Lock I) | Surface 4 — versionId als ownerID | mitigate | Backend CanForReleaseVersion-Gate bleibt unveränderter Authority-Punkt; Frontend ownerID ist rein informativ |
| T-79-04-02 | Tampering | Surface 4 reviewStatusCode='approved' im Frontend schickbar | mitigate | Backend-Handler Whitelist (Plan 79-02); Prozessmedien-Handler setzt 'in_review' als Default nur wenn leer; ein bewusst gesendetes 'approved' wird vom Backend akzeptiert — serverseitige Capability-Prüfung ist Guard (kein öffentlicher Endpoint ohne Auth) |
| T-79-04-03 | Broken Access Control | Surface 2 — Anime-Assets direkt freigegeben (immediate) | accept | D-09 definiert Anime-Assets als Branding-Klasse; Anime-Upload erfordert Admin-Capability (bestehende Gate); SC2 gewahrt weil Owner-Kontext zwingend ist |
| T-79-04-04 | Tampering | categoryValue aus MediaOwnershipContext → setSelectedCategory | mitigate | categoryValue ist restricted auf CATEGORY_OPTIONS.values; TypeScript-Cast zu ReleaseVersionMediaCategory; Backend CHECK-Constraint auf category-Spalte (Migration 0059) ist letzter Guard |
  | T-79-04-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Pakete |
</threat_model>

<verification>
TypeScript (beide neuen Surfaces):
cd frontend && npx tsc --noEmit

Test-Suite:
cd frontend && npx vitest run src/components/admin/media/ --reporter=verbose

Zeilen-Check Surface 4:
wc -l frontend/src/app/admin/episode-versions/*/edit/ReleaseVersionMediaSection.tsx
→ ≤ 450

Integritätsprüfung:
grep -l "MediaOwnershipContext" \
  frontend/src/app/admin/episode-versions/*/edit/ReleaseVersionMediaSection.tsx \
  frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinAssetUploadControls.tsx
→ beide Dateien gelistet

Kategorie-Check Surface 4:
grep "categoryMode.*dropdown" frontend/src/app/admin/episode-versions/*/edit/ReleaseVersionMediaSection.tsx
→ 1 Treffer
</verification>

<success_criteria>
1. ReleaseVersionMediaSection.tsx rendert MediaOwnershipContext mit categoryMode='dropdown', statusPolicy='in_review'.
2. handleUploadClick prüft ownerCtx.ownerResolved; media.startUpload erhält visibilityCode+reviewStatusCode.
3. useReleaseVersionMedia.ts startUpload hat optionale visibilityCode/reviewStatusCode Parameter; übergibt sie an uploadReleaseVersionMedia.
4. AnimeJellyfinAssetUploadControls.tsx rendert MediaOwnershipContext mit ownerType='anime', statusPolicy='immediate'.
5. Alle Upload-Handler in Surface 2 prüfen ownerCtx.ownerResolved.
6. ReleaseVersionMediaSection.tsx ≤ 450 Zeilen; falls Split: helpers.tsx enthält nur Konstanten/Utilities.
7. npx tsc --noEmit ohne Fehler.
8. cd frontend && npx vitest run src/components/admin/media/ grün.
</success_criteria>

<output>
Erstelle .planning/phases/79-medien-ownership-in-ui-durchsetzen/79-04-SUMMARY.md wenn fertig.
</output>
