---
phase: 79-medien-ownership-in-ui-durchsetzen
plan: 03
type: execute
wave: 2
depends_on:
  - 79-01
  - 79-02
files_modified:
  - frontend/src/components/admin/MediaUpload.tsx
  - frontend/src/components/admin/MediaUploadCore.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/ReleaseThemeAssetsSection.tsx
autonomous: true
requirements:
  - G
  - I
  - K

must_haves:
  truths:
    - "D-07: MediaUpload.tsx bindet MediaOwnershipContext mit ownerType='fansub_group', ownerID=fansubID, statusPolicy='immediate' ein (Surface 1, Branding)"
    - "D-06: MediaUpload.tsx blockiert uploadFansubMedia wenn ownerResolved=false; keine Upload-Anfrage bei fehlendem Owner"
    - "D-09: Surface 1 sendet visibilityCode='public', reviewStatusCode='approved' an uploadFansubMedia (Branding-Slot)"
    - "CLAUDE.md 450-Zeilen-Limit: MediaUpload.tsx ≤ 450 Zeilen nach Split; MediaUploadCore.tsx existiert"
    - "D-07: ReleaseThemeAssetsSection.tsx bindet MediaOwnershipContext mit ownerType='release_theme', statusPolicy='immediate' ein (Surface 3)"
    - "CLAUDE.md UI-Primitives: ReleaseThemeAssetsSection.tsx enthält KEIN natives '<select' — migriert zu Select aus @/components/ui"
    - "D-06: ReleaseThemeAssetsSection.tsx blockiert Upload wenn fansubID oder animeID fehlt"
  artifacts:
    - path: "frontend/src/components/admin/MediaUpload.tsx"
      provides: "Surface 1 Koordinator mit MediaOwnershipContext (≤ 450 Zeilen)"
      min_lines: 50
    - path: "frontend/src/components/admin/MediaUploadCore.tsx"
      provides: "Ausgelagerter Upload-Kern von MediaUpload.tsx"
      min_lines: 100
    - path: "frontend/src/app/admin/fansubs/[id]/edit/ReleaseThemeAssetsSection.tsx"
      provides: "Surface 3 mit MediaOwnershipContext + native-select-Migration"
      contains: "MediaOwnershipContext"
  key_links:
    - from: "MediaUpload.tsx"
      to: "MediaOwnershipContext"
      via: "import MediaOwnershipContext from '@/components/admin/media/MediaOwnershipContext'"
      pattern: "MediaOwnershipContext"
    - from: "MediaUpload.tsx"
      to: "uploadFansubMedia in api.ts"
      via: "visibilityCode und reviewStatusCode aus ownerCtx"
      pattern: "visibilityCode"
    - from: "ReleaseThemeAssetsSection.tsx"
      to: "MediaOwnershipContext"
      via: "import MediaOwnershipContext"
      pattern: "MediaOwnershipContext"
---

<objective>
Surface 1 (Fansub Branding/MediaUpload.tsx) und Surface 3 (Release Theme Assets) auf die gemeinsame MediaOwnershipContext-Komponente heben. Gleichzeitig wird MediaUpload.tsx auf das 450-Zeilen-Limit gesplittet (CLAUDE.md) und die nativen `<select>`/`<input>`-Elemente in ReleaseThemeAssetsSection.tsx werden auf globale Primitives migriert.

Purpose: Erste zwei Surfaces produzieren nach diesem Plan keine owner-losen/status-losen Medien mehr. Der Split von MediaUpload.tsx erfüllt CLAUDE.md Modularity-Constraint.
Output: Gesplittete MediaUpload.tsx (≤450Z) + MediaUploadCore.tsx; ReleaseThemeAssetsSection.tsx mit MediaOwnershipContext und global-UI-Primitives.
</objective>

<execution_context>
@C:\Users\admin\Documents\Team4s\.planning\phases\79-medien-ownership-in-ui-durchsetzen\79-UI-SPEC.md
@C:\Users\admin\Documents\Team4s\.planning\phases\79-medien-ownership-in-ui-durchsetzen\79-RESEARCH.md
@C:\Users\admin\Documents\Team4s\.planning\phases\79-medien-ownership-in-ui-durchsetzen\79-PATTERNS.md
@C:\Users\admin\Documents\Team4s\.planning\phases\79-medien-ownership-in-ui-durchsetzen\79-CONTEXT.md
@C:\Users\admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\admin\Documents\Team4s\.planning\ROADMAP.md

<interfaces>
<!-- Aus 79-01 PLAN: MediaOwnershipContext wird bereitgestellt -->
MediaOwnershipContextProps {
  ownerType: 'fansub_group' | 'anime' | 'release_theme' | 'release_version' | 'member'
  ownerID: number | null
  ownerLabel: string
  categoryMode: 'slot' | 'dropdown'
  categoryValue?: string
  statusPolicy: 'immediate' | 'in_review'
  disabled?: boolean
  onContextChange: (ctx: MediaOwnershipContextValue) => void
}
MediaOwnershipContextValue {
  ownerResolved: boolean; visibilityCode: string; reviewStatusCode: string; categoryValue: string
}

<!-- Aus 79-PATTERNS.md MediaUpload.tsx -->
Props-Interface (Z. 24–33):
  interface MediaUploadProps {
    type: FansubMediaKind  // 'logo' | 'banner'
    fansubID: number
    groupName: string
    value: EditableMediaValue | null
    disabled?: boolean
    onBusyChange?: (isBusy: boolean) => void
    onChange: (nextValue: EditableMediaValue | null) => void
  }

submitUpload (Z. 235–271): ruft uploadFansubMedia({ fansubID, kind, file, onProgress }) auf
— nach Phase 79: uploadFansubMedia({ fansubID, kind, file, visibilityCode: ownerCtx.visibilityCode, reviewStatusCode: ownerCtx.reviewStatusCode, onProgress })

<!-- Aus 79-PATTERNS.md ReleaseThemeAssetsSection.tsx -->
Natives <select> Z. 119 → Select-Primitive
Natives <input type="file"> Z. 131 → hidden file input bleibt nativ (kein ui-Primitive für file); sichtbare Fläche nutzt Button

handleUpload (Z. 92–112): ruft uploadAdminReleaseThemeAsset({ fansubID, animeID, themeID, file, onProgress }) auf
— nach Phase 79: + visibilityCode, reviewStatusCode aus ownerCtx

Bekannte Props von ReleaseThemeAssetsSection: fansubID: number, animeID: number, fansubName: string (oder äquivalent für ownerLabel)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MediaUpload.tsx splitten → MediaUploadCore.tsx auslagern</name>
  <files>
    frontend/src/components/admin/MediaUpload.tsx,
    frontend/src/components/admin/MediaUploadCore.tsx
  </files>
  <read_first>
    - frontend/src/components/admin/MediaUpload.tsx — vollständig lesen (540 Zeilen); Koordinator-Logik Z. 1–203, Upload-Kern Z. 204–540
    - .planning/phases/79-medien-ownership-in-ui-durchsetzen/79-PATTERNS.md — Abschnitt MediaUpload.tsx (Split)
  </read_first>
  <action>
    Lies MediaUpload.tsx vollständig. Extrahiere die Dropzone/Upload-Kern-Logik (alles ab ca. Z. 204 — Dropzone-Markup, Preview-Render, Fortschrittsanzeige, Action-Buttons, Datei-Hover-Overlay) in eine neue Datei frontend/src/components/admin/MediaUploadCore.tsx.

    MediaUploadCore.tsx:
    - 'use client' Direktive
    - Props-Interface MediaUploadCoreProps definieren — alle Props, die der Kern benötigt (value, disabled, isBusy, progress, error, warning, onFileSelected, onDeleteRequest, type)
    - Rendert das Dropzone-Markup, die Preview-Card, die Aktions-Buttons, den Fortschrittsbalken und Fehler-/Warn-Anzeigen
    - Importiert benötigte Primitives aus '@/components/ui' (Button, Badge etc.)
    - Importiert Lucide-Icons nach Bedarf

    MediaUpload.tsx (Koordinator, nach Split ≤ 450 Zeilen):
    - Behält alle Props (MediaUploadProps Interface unverändert)
    - Behält State-Management (setBusyAction, setProgress, setError, setWarning, value-State)
    - Importiert MediaUploadCore und rendert ihn
    - Hält ownerCtx-State für die MediaOwnershipContext-Integration (nächster Schritt) vor — State-Variable ownerCtx vom Typ MediaOwnershipContextValue | null, Default null
    - submitUpload bleibt in MediaUpload.tsx als Koordinator-Logik

    Kein funktionales Verhalten ändern — nur Extraktion. Alle bestehenden Props und Verhaltensweisen bleiben identisch. Keine Logik entfernen.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20 && wc -l src/components/admin/MediaUpload.tsx</automated>
  </verify>
  <done>
    MediaUpload.tsx ≤ 450 Zeilen (wc -l gibt ≤ 450 aus).
    MediaUploadCore.tsx existiert und wird von MediaUpload.tsx importiert.
    npx tsc --noEmit ohne Fehler in diesen Dateien.
    Kein UI-Verhalten geändert.
  </done>
</task>

<task type="auto">
  <name>Task 2: Surface 1 — MediaOwnershipContext in MediaUpload.tsx einbinden</name>
  <files>
    frontend/src/components/admin/MediaUpload.tsx
  </files>
  <read_first>
    - frontend/src/components/admin/MediaUpload.tsx — aktuellen Zustand nach Task 1 lesen
    - frontend/src/components/admin/media/MediaOwnershipContext.tsx — Interface und Rückgabewert aus Plan 79-01
    - .planning/phases/79-medien-ownership-in-ui-durchsetzen/79-UI-SPEC.md — Surface-Matrix Zeile 1: ownerType='fansub_group', categoryMode='slot', statusPolicy='immediate'
  </read_first>
  <action>
    MediaOwnershipContext aus '@/components/admin/media/MediaOwnershipContext' importieren.
    MediaOwnershipContextValue-Typ aus '@/components/admin/media/MediaOwnershipContext' importieren.

    State hinzufügen: const [ownerCtx, setOwnerCtx] = useState<MediaOwnershipContextValue | null>(null)

    Im JSX: MediaOwnershipContext VOR dem MediaUploadCore-Block einbinden mit Props:
    - ownerType="fansub_group" (per D-05/UI-SPEC Surface 1)
    - ownerID={fansubID} (aus Props)
    - ownerLabel={groupName ? `Gruppe «${groupName}»` : ''} (UI-SPEC Copywriting: 'Upload für: Gruppe «{name}» · Owner-Typ: Gruppe' wird in MediaOwnershipContext selbst gerendert)
    - categoryMode="slot" (D-08: Branding-Slot ist read-only)
    - categoryValue={type} (z.B. 'logo' oder 'banner' — aus Props)
    - statusPolicy="immediate" (D-09: Branding-Slot sofort sichtbar)
    - disabled={disabled}
    - onContextChange={setOwnerCtx}

    submitUpload erweitern: Wenn !ownerCtx?.ownerResolved, setError mit 'Upload nicht möglich: Kein gültiger Owner-Kontext.' aufrufen und return. Sonst:
    uploadFansubMedia({ fansubID, kind: type, file, visibilityCode: ownerCtx.visibilityCode, reviewStatusCode: ownerCtx.reviewStatusCode, onProgress: setProgress })

    MediaUploadCore: disabled-Prop ergänzen wenn ownerCtx?.ownerResolved === false (blockiert visuell).

    Alle user-facing Strings deutsch mit Umlauten. Kein neues natives Form-Element einführen.
    MediaUpload.tsx MUSS nach dieser Änderung ≤ 450 Zeilen bleiben.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20 && npx vitest run src/components/admin/media/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    MediaUpload.tsx ≤ 450 Zeilen.
    MediaOwnershipContext wird in MediaUpload.tsx mit korrekten Props gerendert.
    submitUpload prüft ownerCtx.ownerResolved vor dem Upload-Call.
    uploadFansubMedia erhält visibilityCode und reviewStatusCode aus ownerCtx.
    Bestehende Tests grün.
  </done>
</task>

<task type="auto">
  <name>Task 3: Surface 3 — ReleaseThemeAssetsSection.tsx: native-select-Migration + MediaOwnershipContext</name>
  <files>
    frontend/src/app/admin/fansubs/[id]/edit/ReleaseThemeAssetsSection.tsx
  </files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/ReleaseThemeAssetsSection.tsx — vollständig lesen (alle Zeilen, insb. Z. 1–50 Imports, Z. 90–150 handleUpload + JSX mit nativem select/input)
    - .planning/phases/79-medien-ownership-in-ui-durchsetzen/79-PATTERNS.md — Abschnitt ReleaseThemeAssetsSection.tsx (native select Z. 119 + input Z. 131)
    - .planning/phases/79-medien-ownership-in-ui-durchsetzen/79-UI-SPEC.md — Surface-Matrix Zeile 3: ownerType='release_theme', categoryMode='slot', statusPolicy='immediate'
    - .planning/phases/79-medien-ownership-in-ui-durchsetzen/79-CONTEXT.md — D-09: Release-Theme-Assets = Branding-ähnlich → immediate
  </read_first>
  <action>
    1. Native-select-Migration (CLAUDE.md Pflicht):
    Zeile 119 (natives <select> für theme-Auswahl): Ersetzen durch Select-Primitive aus '@/components/ui'.
    Import Select (und FormField falls nicht vorhanden) aus '@/components/ui' hinzufügen.
    <Select value={String(themeID ?? '')} onChange={(e) => setThemeID(Number(e.target.value))}>
    Bestehende <option>-Elemente bleiben erhalten, nur der Wrapper ändert sich.

    2. Native-input-Sonderfall: <input type="file"> (Z. 131) bleibt hidden (kein @/components/ui-Primitive für file inputs). Sichtbare Trigger-Fläche muss ein Button-Primitive sein (prüfen ob schon vorhanden, sonst ergänzen).

    3. MediaOwnershipContext einbinden:
    State: const [ownerCtx, setOwnerCtx] = useState<MediaOwnershipContextValue | null>(null)
    Import: MediaOwnershipContext, MediaOwnershipContextValue aus '@/components/admin/media/MediaOwnershipContext'

    Im JSX: MediaOwnershipContext VOR dem Upload-Formular-Block einbinden:
    - ownerType="release_theme" (UI-SPEC Surface 3)
    - ownerID={fansubID} (aus Props — Release-Theme-Context wird über fansubID identifiziert)
    - ownerLabel: z.B. aus fansubName-Prop oder ähnlichem; falls kein Name-Prop: 'Release «{animeID} · {fansubID}»' (UI-SPEC Copywriting)
    - categoryMode="slot" (theme_type ist read-only)
    - categoryValue: aktuell gewählter Theme-Typ-Label (z.B. 'Opening' oder Themes-Array-Entry-Bezeichnung)
    - statusPolicy="immediate" (D-09: Theme-Assets sind Branding-ähnlich)
    - disabled={busy}
    - onContextChange={setOwnerCtx}

    handleUpload: D-06-Guard — wenn !ownerCtx?.ownerResolved → setError('Upload nicht möglich: Kein gültiger Owner-Kontext.') und return.
    uploadAdminReleaseThemeAsset-Aufruf: + visibilityCode: ownerCtx.visibilityCode, reviewStatusCode: ownerCtx.reviewStatusCode

    Kein natives <select> verbleibend nach Migration. Alle Strings deutsch mit Umlauten.
    Dateigröße nach Änderungen ≤ 450 Zeilen prüfen; falls über Limit: helper-Funktion auslagern.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20 && grep -c "&lt;select" src/app/admin/fansubs/[id]/edit/ReleaseThemeAssetsSection.tsx || grep -c "<select" src/app/admin/fansubs/\\[id\\]/edit/ReleaseThemeAssetsSection.tsx</automated>
  </verify>
  <done>
    ReleaseThemeAssetsSection.tsx enthält kein natives '<select' mehr.
    MediaOwnershipContext wird mit ownerType='release_theme', statusPolicy='immediate' eingebunden.
    handleUpload prüft ownerCtx.ownerResolved vor dem Aufruf.
    uploadAdminReleaseThemeAsset erhält visibilityCode und reviewStatusCode.
    npx tsc --noEmit ohne Fehler in dieser Datei.
    Dateigröße ≤ 450 Zeilen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Grenze | Beschreibung |
|--------|-------------|
| MediaOwnershipContext onContextChange → submitUpload | ownerCtx-Werte aus Frontend-State; Backend validiert unabhängig |
| fansubID Prop → MediaOwnershipContext ownerID | kommt aus Page-Context (Route-Param); ist vertrauenswürdig für UI, nicht für Backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-03-01 | Broken Access Control | Surface 1 — fansubID als ownerID | mitigate | Backend CanForFansubGroup-Gate bleibt alleinige Authority (Plan 79-02 verändert das nicht); Frontend ownerID-Anzeige ist rein informativ |
| T-79-03-02 | Tampering | ownerCtx.visibilityCode aus Frontend-State | mitigate | Backend Whitelist-Validierung (Plan 79-02, T-79-02-01); Branding-Handler überschreibt mit 'public'/'approved' |
| T-79-03-03 | Visibility-Tampering | statusPolicy='immediate' auf Branding → immer öffentlich | accept | D-09 definiert das als fachliches Soll; SC2 bleibt gewahrt weil Owner-Kontext zwingend ist (D-06) |
| T-79-03-04 | DoS | Upload ohne ownerCtx.ownerResolved | mitigate | D-06-Guard in submitUpload/handleUpload verhindert API-Call; Backend hat eigene Capability-Gate |
  | T-79-03-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Pakete |
</threat_model>

<verification>
TypeScript:
cd frontend && npx tsc --noEmit

Test-Suite:
cd frontend && npx vitest run src/components/admin/media/ --reporter=verbose

Zeilen-Check:
wc -l frontend/src/components/admin/MediaUpload.tsx
→ ≤ 450

Native-Select-Prüfung (keine nativen selects außer in @/components/ui selbst):
grep -n "<select" frontend/src/app/admin/fansubs/*/edit/ReleaseThemeAssetsSection.tsx
→ kein Treffer

Integritätsprüfung MediaOwnershipContext-Einbindung:
grep -l "MediaOwnershipContext" frontend/src/components/admin/MediaUpload.tsx frontend/src/app/admin/fansubs/*/edit/ReleaseThemeAssetsSection.tsx
→ beide Dateien gelistet
</verification>

<success_criteria>
1. MediaUpload.tsx ≤ 450 Zeilen; MediaUploadCore.tsx existiert.
2. MediaUpload.tsx importiert und rendert MediaOwnershipContext mit ownerType='fansub_group', statusPolicy='immediate'.
3. submitUpload in MediaUpload.tsx prüft ownerCtx.ownerResolved; uploadFansubMedia erhält visibilityCode+reviewStatusCode.
4. ReleaseThemeAssetsSection.tsx enthält kein natives '<select' mehr.
5. ReleaseThemeAssetsSection.tsx rendert MediaOwnershipContext mit ownerType='release_theme', statusPolicy='immediate'.
6. handleUpload in ReleaseThemeAssetsSection.tsx prüft ownerCtx.ownerResolved.
7. npx tsc --noEmit ohne Fehler.
8. npx vitest run src/components/admin/media/ grün.
</success_criteria>

<output>
Erstelle .planning/phases/79-medien-ownership-in-ui-durchsetzen/79-03-SUMMARY.md wenn fertig.
</output>
