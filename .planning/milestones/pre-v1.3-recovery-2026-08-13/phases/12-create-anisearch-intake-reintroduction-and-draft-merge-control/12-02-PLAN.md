---
phase: 12-create-anisearch-intake-reintroduction-and-draft-merge-control
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts
  - frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts
  - frontend/src/app/admin/anime/create/createPageHelpers.ts
  - frontend/src/lib/api/admin-anime-intake.ts
  - frontend/src/types/admin.ts
autonomous: true
requirements: [ENR-03, ENR-04]
must_haves:
  truths:
    - "A duplicate AniSearch ID in create moves the operator to the existing anime instead of leaving them on a broken create draft."
    - "Both load orders enforce manual > AniSearch > Jellyfin inside the create draft."
    - "Provisional lookup text can be replaced by AniSearch until the operator makes a real manual edit."
  artifacts:
    - path: "frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts"
      provides: "AniSearch-aware create draft orchestration and duplicate redirect handling"
      contains: "AniSearch"
    - path: "frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts"
      provides: "Controller regressions for both load orders and duplicate redirect state"
      contains: "Jellyfin"
    - path: "frontend/src/lib/api/admin-anime-intake.ts"
      provides: "Create AniSearch helper consumed by the controller"
      contains: "loadAdminAnimeCreateAniSearchDraft"
  key_links:
    - from: "frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts"
      to: "frontend/src/lib/api/admin-anime-intake.ts"
      via: "explicit AniSearch draft load action"
      pattern: "loadAdminAnimeCreateAniSearchDraft"
    - from: "frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts"
      to: "frontend/src/app/admin/anime/create/createPageHelpers.ts"
      via: "redirect and precedence helpers"
      pattern: "AniSearch"
---

<objective>
Extend the create controller to understand AniSearch draft loads, duplicate redirects, and explicit origin precedence without regressing the verified Jellyfin workflow.

Purpose: The riskiest part of Phase 12 is draft ownership drift. This plan centralizes the merge rules and redirect behavior before the page renders new UI.
Output: AniSearch-aware create controller state/actions plus regression coverage for both load orders, duplicate redirects, and provisional-title replacement.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-create-anisearch-intake-reintroduction-and-draft-merge-control/12-CONTEXT.md
@.planning/phases/12-create-anisearch-intake-reintroduction-and-draft-merge-control/12-RESEARCH.md
@.planning/phases/09-controlled-anisearch-id-enrichment-before-create-with-fill-only-jellysync-follow-up/09-UAT.md
@.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-VERIFICATION.md
@.planning/phases/12-create-anisearch-intake-reintroduction-and-draft-merge-control/12-01-SUMMARY.md
@frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts
@frontend/src/app/admin/anime/create/createPageHelpers.ts
@frontend/src/lib/api/admin-anime-intake.ts
@frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts

<interfaces>
From `frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts`:
```ts
export function useAdminAnimeCreateController() {
  // current create route orchestrates Jellyfin search/review, manual draft state,
  // staged assets, and final save through one controller hook
}
```

From `frontend/src/app/admin/anime/create/createPageHelpers.ts`:
```ts
export function resolveJellyfinPreviewBaseDraft(...)
export function resolveSourceActionState(title: string)
```

From `frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts`:
```ts
export type ManualAnimeDraftValues = { ... }
export function hydrateManualDraftFromJellyfinPreview(...)
export function buildManualCreateDraftSnapshot(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drive create-controller AniSearch precedence from regression tests</name>
  <files>frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts</files>
  <read_first>@frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts @frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts @.planning/phases/09-controlled-anisearch-id-enrichment-before-create-with-fill-only-jellysync-follow-up/09-UAT.md</read_first>
  <acceptance_criteria>`frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts` contains grep-verifiable cases for `jellyfin -> anisearch`, `anisearch -> jellyfin`, duplicate redirect, preserved manual edits, and provisional lookup text replacement; those cases fail until controller state and handlers are implemented.</acceptance_criteria>
  <action>Expand the controller test file from Plan 12-01 so it drives the full D-05 through D-12 behavior: AniSearch overwrites Jellyfin-derived metadata, later Jellyfin remains fill-only relative to AniSearch-owned values, manual edits stay authoritative, provisional lookup text is replaceable until the operator edits the field for real, and duplicate redirects produce immediate edit-route handoff instead of generic error text. Keep tests controller-focused rather than page-focused so this plan can prove state correctness before UI wiring.</action>
  <verify>
    <automated>cd frontend && npm test -- src/app/admin/anime/create/useAdminAnimeCreateController.test.ts</automated>
  </verify>
  <done>The controller regression suite fully specifies the locked precedence and duplicate rules before production logic is changed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AniSearch controller state, duplicate redirects, and source ownership rules</name>
  <files>frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts, frontend/src/app/admin/anime/create/createPageHelpers.ts, frontend/src/lib/api/admin-anime-intake.ts, frontend/src/types/admin.ts</files>
  <read_first>@frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts @frontend/src/app/admin/anime/create/createPageHelpers.ts @frontend/src/lib/api/admin-anime-intake.ts @frontend/src/types/admin.ts</read_first>
  <acceptance_criteria>`frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts` exposes AniSearch input/state, draft-result state, conflict redirect state, and a load handler wired to the existing backend seam; controller logic applies `manual > AniSearch > Jellyfin` in both load orders and clears stale AniSearch state on retry/discard paths; no edit-route refactor logic is added.</acceptance_criteria>
  <action>Extend `useAdminAnimeCreateController.ts` with explicit AniSearch state slices and helper functions that reuse the Plan 12-01 helper/API seam. Normalize create duplicate results into an immediate redirect to the existing edit route per D-10 through D-12 instead of inventing a second conflict policy. Preserve the verified Jellyfin preview behavior, but update hydration so AniSearch-owned values survive later Jellyfin previews per D-07 and D-08 while real manual edits still win per D-05 and D-06. Use narrow helper functions or small local modules only if needed to keep the hook readable and under the project file-budget expectations. Do not add free-text AniSearch search, browse results, or hidden placeholder logic.</action>
  <verify>
    <automated>cd frontend && npm test -- src/app/admin/anime/create/useAdminAnimeCreateController.test.ts src/lib/api.admin-anime.test.ts</automated>
  </verify>
  <done>The create controller can load AniSearch by ID, redirect duplicates into edit, preserve manual authority, and keep AniSearch ahead of Jellyfin in both load orders.</done>
</task>

</tasks>

<verification>
This plan is complete when the controller test suite proves both load orders and duplicate redirects, and the production controller exposes the new AniSearch state/actions without adding a second workflow or backend policy.
</verification>

<success_criteria>
- Duplicate AniSearch IDs leave create via the existing edit route.
- Manual edits remain authoritative, AniSearch beats Jellyfin, and later Jellyfin stays fill-only relative to AniSearch-owned values.
- The controller exposes all state needed for the UI plan without page-level orchestration drift.
</success_criteria>

<output>
After completion, create `.planning/phases/12-create-anisearch-intake-reintroduction-and-draft-merge-control/12-02-SUMMARY.md`
</output>
