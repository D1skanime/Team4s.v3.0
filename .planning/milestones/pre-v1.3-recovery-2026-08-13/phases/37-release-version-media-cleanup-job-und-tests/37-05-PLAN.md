---
phase: 37-release-version-media-cleanup-job-und-tests
plan: 05
type: execute
wave: 5
depends_on:
  - 37-01-PLAN.md
  - 37-04-PLAN.md
files_modified:
  - backend/cmd/server/main.go
  - backend/internal/repository/release_version_media_cleanup.go
  - backend/internal/services/release_version_media_cleanup.go
  - backend/internal/services/release_version_media_cleanup_test.go
  - .planning/phases/37-release-version-media-cleanup-job-und-tests/37-UAT.md
  - .planning/phases/37-release-version-media-cleanup-job-und-tests/37-VERIFICATION.md
autonomous: true
requirements:
  - RVM-CLEANUP-01
gap_closure: true
must_haves:
  truths:
    - "Ein periodischer Cleanup-Job gilt erst dann als verifiziert, wenn ein echter Docker-Lauf DB-Zeilen UND Dateien entfernt; grüne Unit-Tests allein reichen nicht."
    - "Soft-deleted Release-Version-Media darf nur dann hart gelöscht werden, wenn die SQL-Selektion und der Runtime-Guard weiterhin bestätigen, dass kein aktiver Fremdbezug auf dasselbe media_asset besteht."
    - "Die Runtime-Seam für den Cleanup-Loop muss beobachtbar und testbar sein; ein stiller Hintergrund-Loop ohne nachweisbare Zyklen ist für diese Phase unzureichend."
---

<objective>
Schließe den Live-Gap aus `37-UAT.md`: Der Release-Version-Media-Cleanup muss im Docker-Runtimepfad nachweisbar zyklisch laufen und echte Soft-Delete-Kandidaten sowohl aus Postgres als auch vom Storage-Volume entfernen.

Purpose: Phase 37 ist funktional fast fertig, aber die zentrale Produktbehauptung "periodischer Cleanup läuft live" ist noch nicht bewiesen. Dieser Gap-Plan repariert oder verhärtet genau diese Runtime-Seam und dokumentiert den Beweis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-release-version-media-cleanup-job-und-tests/37-CONTEXT.md
@.planning/phases/37-release-version-media-cleanup-job-und-tests/37-UAT.md
@.planning/phases/37-release-version-media-cleanup-job-und-tests/37-VERIFICATION.md
@backend/cmd/server/main.go
@backend/internal/repository/release_version_media_cleanup.go
@backend/internal/services/release_version_media_cleanup.go
@backend/internal/services/release_version_media_cleanup_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the periodic cleanup runner observable, testable, and effective</name>
  <read_first>
    - .planning/phases/37-release-version-media-cleanup-job-und-tests/37-UAT.md
    - backend/cmd/server/main.go
    - backend/internal/services/release_version_media_cleanup.go
    - backend/internal/repository/release_version_media_cleanup.go
    - backend/internal/services/release_version_media_cleanup_test.go
  </read_first>
  <action>
Reproduce the narrow runtime gap from `37-UAT.md` and fix the smallest seam that prevents eligible soft-delete candidates from being removed after a real scheduler interval. If the anonymous ticker loop in `main.go` is too opaque to prove, extract the scheduling logic into a testable helper that accepts context and a tick seam, then wire `main.go` through that helper. Add low-noise cycle-level observability so a Docker run can prove that a cleanup cycle started and finished without relying on guesswork. Keep the existing best-effort semantics: no server crash on cleanup failure, no destructive delete when another active relation still references the asset.

Target behaviors to lock:
- repeated ticks invoke cleanup deterministically
- eligible soft-delete candidates actually reach the destructive path when `RunOnce` executes
- the runtime seam leaves enough evidence to distinguish "cycle never fired" from "cycle fired but skipped/failed"
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/repository/... ./internal/services/... -count=1</automated>
  </verify>
  <done>
    - The periodic cleanup scheduling path is encapsulated enough to be unit-tested or otherwise deterministically exercised.
    - `backend/internal/services/release_version_media_cleanup_test.go` includes at least one test for repeated scheduled invocation and one test that proves eligible soft-delete candidates reach the hard-delete path.
    - The runtime cleanup seam emits cycle-level evidence that can be seen in Docker logs without flooding normal server output.
    - `go test ./internal/repository/... ./internal/services/... -count=1` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-run Docker-backed cleanup proof and reconcile phase evidence</name>
  <read_first>
    - .planning/phases/37-release-version-media-cleanup-job-und-tests/37-UAT.md
    - .planning/phases/37-release-version-media-cleanup-job-und-tests/37-VERIFICATION.md
    - backend/cmd/server/main.go
    - backend/internal/services/release_version_media_cleanup.go
  </read_first>
  <action>
Run a real Docker-backed smoke verification after Task 1. Create or reuse an eligible soft-delete candidate on release-version media, restart or rebuild the backend as needed, wait through the real cleanup interval, and verify two things with timestamps: the candidate rows disappear from `release_version_media` / `media_assets` / `media_files`, and the corresponding files disappear from the backend storage volume. Update `37-UAT.md` with the exact live evidence and reconcile `37-VERIFICATION.md` so it no longer overstates the phase if the runtime proof was previously missing.
  </action>
  <verify>
    <automated>git diff --check -- .planning/phases/37-release-version-media-cleanup-job-und-tests/37-UAT.md .planning/phases/37-release-version-media-cleanup-job-und-tests/37-VERIFICATION.md</automated>
  </verify>
  <done>
    - `37-UAT.md` records a fresh Docker-backed cleanup run with absolute timestamps and explicit DB/filesystem outcomes.
    - `37-VERIFICATION.md` is consistent with the new live result instead of claiming an unconditional pass while UAT still shows a runtime gap.
    - The final evidence clearly separates automated proof from live Docker proof.
    - `git diff --check -- ...37-UAT.md 37-VERIFICATION.md` exits 0.
  </done>
</task>

</tasks>

<verification>
This gap-plan is only complete when both layers are true:

1. `cd backend && go test ./internal/repository/... ./internal/services/... -count=1`
2. Docker-backed live proof:
   - backend container restarted or rebuilt on the new code
   - at least one eligible soft-delete candidate created
   - after one real cleanup interval, the candidate is gone from `release_version_media`, `media_assets`, and `media_files`
   - the corresponding `original` and `thumb` files are gone from the backend storage volume
   - Docker logs show cycle-level cleanup evidence for the observed run
</verification>

<success_criteria>
- The periodic cleanup loop is no longer a black box; its cycles are provable in tests and visible in runtime logs.
- The Phase-37 live gap is closed with real Docker evidence, not only source inspection or unit tests.
- UAT and VERIFICATION artifacts tell the same story about Phase 37.
</success_criteria>

<output>
Nach Abschluss `.planning/phases/37-release-version-media-cleanup-job-und-tests/37-05-SUMMARY.md` erstellen.
</output>
