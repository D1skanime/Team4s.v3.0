---
phase: 20.1-db-schema-v2-physical-cutover
plan: 01
type: database
wave: 1
depends_on: []
files_modified:
  - docs/architecture/db-schema-v2.md
  - database/migrations
  - backend/internal
  - scripts
autonomous: true
requirements:
  - P20.1-SC1
  - P20.1-SC2
  - P20.1-SC3
must_haves:
  truths:
    - "The documented DB Schema v2 is treated as the authoritative contract before writing migrations."
    - "The execution begins from a controlled local reset because old episode data is disposable test data."
    - "The migration plan explicitly distinguishes target episode identity tables from legacy episode-version tables."
---

<objective>
Create the schema cutover inventory and safe local reset baseline.

Purpose: Before creating broad schema migrations, execution must know exactly what exists, what is missing, and what can be deleted.
Output: Schema audit notes, local reset command/script, and a migration checklist mapped to `docs/architecture/db-schema-v2.md`.
</objective>

<context>
@docs/architecture/db-schema-v2.md
@.planning/phases/20.1-db-schema-v2-physical-cutover/20.1-CONTEXT.md
@database/migrations
@DECISIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate live-vs-target schema inventory</name>
  <files>database/migrations, docs/architecture/db-schema-v2.md, optional scripts/schema-audit tooling</files>
  <action>
    Compare the live migrated local schema against every table, column, foreign key, unique constraint, and index documented in `docs/architecture/db-schema-v2.md`. Produce a short audit artifact listing: already present, missing, divergent, and legacy-to-delete.
  </action>
  <verify>
    <automated>Run the audit from a clean local DB and include row/table output in `20.1-01-SUMMARY.md`.</automated>
  </verify>
  <done>The executor has an exact migration checklist instead of guessing from memory.</done>
</task>

<task type="auto">
  <name>Task 2: Add controlled local reset for disposable test data</name>
  <files>scripts or backend/dev tooling, STATUS.md if command documentation changes</files>
  <action>
    Add a local-only reset command/script that clears anime/import/media test state as needed for schema cutover validation. The script must be clearly unsafe for production and must run only against local Docker/dev settings.
  </action>
  <verify>
    <automated>Run reset locally, then query counts for anime/import/release/media tables and record the result.</automated>
  </verify>
  <done>The old test anime state can be removed reproducibly before destructive legacy table removal.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the deletion boundary</name>
  <files>20.1-01-SUMMARY.md, DECISIONS.md if needed</files>
  <action>
    Document the exact tables allowed to be dropped: `episode_version_episodes`, `episode_version_images`, and `episode_versions`. Also document tables that must not be dropped: `episodes`, `episode_titles`, `episode_types`, `languages`, release graph tables, and media tables.
  </action>
  <verify>
    <manual>The summary makes it impossible to confuse target episode identity with legacy episode-version persistence.</manual>
  </verify>
  <done>Deletion scope is explicit before migrations are written.</done>
</task>

</tasks>

<success_criteria>
- Target-vs-live schema inventory exists.
- Local reset is reproducible.
- Legacy deletion scope is explicit and limited to old release-bearing episode tables.
</success_criteria>

<output>
Create `20.1-01-SUMMARY.md`.
</output>

