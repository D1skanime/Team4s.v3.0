---
phase: 20.1-db-schema-v2-physical-cutover
plan: 03
type: migration
wave: 3
depends_on:
  - 20.1-02-PLAN.md
files_modified:
  - database/migrations
  - backend/internal
  - frontend/src
autonomous: true
requirements:
  - P20.1-SC3
  - P20.1-SC5
must_haves:
  truths:
    - "Legacy episode-version tables are removed after the target schema exists."
    - "Code no longer depends on `episode_versions`, `episode_version_images`, or `episode_version_episodes`."
    - "Target episode identity tables are preserved."
---

<objective>
Drop the legacy episode-version table family and remove code dependencies.

Purpose: If the old tables remain usable, future work can accidentally keep writing to them. The cutover should make that impossible.
Output: Drop migration, repository/API updates, and tests proving the app no longer requires the legacy tables.
</objective>

<context>
@docs/architecture/db-schema-v2.md
@.planning/phases/20.1-db-schema-v2-physical-cutover/20.1-CONTEXT.md
@.planning/phases/20.1-db-schema-v2-physical-cutover/20.1-02-SUMMARY.md
@backend/internal
@frontend/src
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop legacy episode-version tables</name>
  <files>database/migrations</files>
  <action>
    Add a migration that drops legacy tables in dependency order: `episode_version_episodes`, `episode_version_images`, then `episode_versions`. Include a down migration only if project migration conventions require one, and make clear that old test data is intentionally discarded.
  </action>
  <verify>
    <automated>Run migrations on local DB and query information_schema to confirm those tables are absent while `episodes`, `episode_titles`, and release graph tables remain present.</automated>
  </verify>
  <done>Legacy persistence tables are gone from the local schema.</done>
</task>

<task type="auto">
  <name>Task 2: Remove repository and API dependencies on dropped tables</name>
  <files>backend/internal, frontend/src/types if contracts change</files>
  <action>
    Search for `episode_versions`, `episode_version_images`, and `episode_version_episodes` references. Replace read/write paths with release-native equivalents or temporarily return clear not-implemented errors only for surfaces that Phase 20 will rebuild immediately.
  </action>
  <verify>
    <automated>Backend tests and frontend typecheck do not fail due to missing legacy tables or stale types.</automated>
  </verify>
  <done>The app can compile and boot without the old tables.</done>
</task>

<task type="auto">
  <name>Task 3: Update test fixtures away from legacy tables</name>
  <files>backend tests, frontend tests, test fixtures</files>
  <action>
    Rewrite test setup that inserted into legacy episode-version tables so it uses target schema tables or is moved into Phase 20 import-specific tests.
  </action>
  <verify>
    <automated>Targeted backend tests pass and no test fixture references dropped table names.</automated>
  </verify>
  <done>Tests encode the new schema direction instead of keeping legacy alive.</done>
</task>

</tasks>

<success_criteria>
- Legacy episode-version tables are absent.
- Code and tests no longer require those tables.
- Canonical target episode/release tables remain intact.
</success_criteria>

<output>
Create `20.1-03-SUMMARY.md`.
</output>

