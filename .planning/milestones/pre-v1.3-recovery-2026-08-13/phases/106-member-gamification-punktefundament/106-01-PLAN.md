---
phase: 106-member-gamification-punktefundament
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/testsupport/phase106_postgres.go
  - backend/internal/testsupport/phase106_postgres_test.go
  - backend/internal/migrations/phase106_member_points_test.go
autonomous: true
requirements: [GAM-01, GAM-02, GAM-03, GAM-04, GAM-05]
must_haves:
  truths:
    - "GAM-01 / D-01 / D-02: Der Migrationsvertrag verlangt einen Member und erlaubt einen fehlenden App-User, ohne Claim- oder Profil-Voraussetzung."
    - "GAM-02 / D-04: Echte PostgreSQL-Verträge verlangen append-only Awards, genau ein direktes Storno und unveränderte Originalzeilen; eine verschachtelte optionale Kontextreferenz darf nur dann non-null→null werden, wenn ihr OLD-Parent durch die echte FK-Löschung nicht mehr existiert."
    - "GAM-03 / D-04: Regel- und Ledger-Snapshots sind versioniert und weder Punktwerte noch Idempotenzschlüssel kommen aus einem externen Request."
    - "GAM-04 / D-06 / D-07: Genau fansub_work und platform_contribution sind erlaubt; Profilpflege und Badges bleiben getrennt."
    - "GAM-05 / D-03 / D-05 / D-08 / D-09 / D-10: Tests bleiben auf Phase-106-Artefakte beschränkt und verdrahten keine späteren Consumer."
  artifacts:
    - path: "backend/internal/testsupport/phase106_postgres.go"
      provides: "Opt-in PostgreSQL-16-Testfixture mit hartem Testdatenbank-Guard und exklusivem Schema"
    - path: "backend/internal/migrations/phase106_member_points_test.go"
      provides: "Kompilierbare Up-/Down-/Constraint-/Append-only-/Reversal-Verträge"
  key_links:
    - from: "backend/internal/migrations/phase106_member_points_test.go"
      to: "backend/internal/testsupport/phase106_postgres.go"
      via: "dedizierte Testdatenbank und isoliertes Schema für reales Up -> Down -> Up"
      pattern: "OpenPhase106Postgres"
---

<objective>
Eine sichere und an jedem Task-Punkt kompilierbare Nyquist-Wave-0 für Migration und Datenbankinvarianten anlegen.

Purpose: Die irreversiblen Schema-, Append-only-, Storno- und Testdatenbank-Grenzen werden vor der Implementierung ausführbar festgeschrieben, ohne Tests für noch nicht existierende Go-Typen in andere Pakete zu legen.
Output: Ein hart geschütztes opt-in PostgreSQL-Harness und erwartungsgemäß rote, aber kompilierbare Migrationstests.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/106-member-gamification-punktefundament/106-CONTEXT.md
@.planning/phases/106-member-gamification-punktefundament/106-RESEARCH.md
@.planning/phases/106-member-gamification-punktefundament/106-PATTERNS.md
@.planning/phases/106-member-gamification-punktefundament/106-VALIDATION.md
@.planning/notes/260722-member-gamification-DECISION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dedizierte opt-in PostgreSQL-Testdatenbank hart absichern</name>
  <read_first>
    - AGENTS.md
    - docs/engineering/implementation-contract.md
    - docs/architecture/db-schema-fansub-domain.md
    - .planning/phases/106-member-gamification-punktefundament/106-CONTEXT.md
    - .planning/phases/106-member-gamification-punktefundament/106-RESEARCH.md
    - .planning/phases/106-member-gamification-punktefundament/106-PATTERNS.md
    - docker-compose.yml
    - backend/go.mod
    - backend/internal/migrations/runner.go
  </read_first>
  <files>backend/internal/testsupport/phase106_postgres.go, backend/internal/testsupport/phase106_postgres_test.go</files>
  <action>
Create package `testsupport` with `OpenPhase106Postgres(t *testing.T) *pgxpool.Pool` and `ApplySQLFile`. Read only `TEAM4S_PHASE106_TEST_DSN`; when absent call `t.Skip` and never fall back to `DATABASE_URL`, `.env`, or an application pool. Parse the DSN with existing pgx APIs and reject before connecting unless the database name matches `team4s_phase106_test_[a-z0-9]+`; explicitly reject `team4s_v2`, `postgres`, template databases, an empty database name, and any DSN whose runtime `current_database()` differs from the parsed guarded name. Create a random schema matching `phase106_[a-z0-9_]+`, quote it through `pgx.Identifier`, configure every connection with a search path containing only that schema, and assert `current_schemas(false)` does not include `public`. Cleanup closes the scoped pool and drops only that sanitized schema; never issue `DROP DATABASE`, `TRUNCATE`, `DROP SCHEMA public`, or touch application schemas.

Inside the schema create only FK prerequisites `members`, `app_users`, `fansub_groups`, and `release_versions`. Unit-test environment selection, accepted/rejected database names, schema-name validation, and the rule that `DATABASE_URL` alone still skips. Add a source guard that rejects executable `public.`-qualified targets in Phase-106 fixture/migration SQL. Use only pgx/v5, stdlib, and existing testify; add no dependency. This implements the opt-in isolation required by GAM-05 and preserves all domain/media ownership per D-09.
  </action>
  <verify>
    <automated>cd backend; go test ./internal/testsupport -run 'TestPhase106.*(DSN|DatabaseGuard|Schema|Public)' -count=1</automated>
  </verify>
  <done>Die Fixture kompiliert ohne laufende DB, akzeptiert ausschließlich ausdrücklich erzeugte Phase-106-Testdatenbanken und kann nur ihr eigenes Schema verändern.</done>
</task>

<task type="auto">
  <name>Task 2: Kompilierbare Migrations-, Append-only- und Reversal-Verträge rot schreiben</name>
  <read_first>
    - AGENTS.md
    - docs/engineering/implementation-contract.md
    - docs/architecture/db-schema-fansub-domain.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/106-member-gamification-punktefundament/106-CONTEXT.md
    - .planning/phases/106-member-gamification-punktefundament/106-RESEARCH.md
    - .planning/phases/106-member-gamification-punktefundament/106-PATTERNS.md
    - backend/internal/testsupport/phase106_postgres.go
    - backend/internal/migrations/phase103_release_playback_entitlements_test.go
    - backend/internal/migrations/release_content_source_groups_test.go
    - backend/internal/migrations/runner.go
    - database/migrations/0108_capability_registry.up.sql
    - database/migrations/0128_group_history_single_use_events.up.sql
    - database/migrations/0129_release_playback_entitlements.up.sql
  </read_first>
  <files>backend/internal/migrations/phase106_member_points_test.go</files>
  <action>
Create independently compilable tests with distinct names: `TestPhase106MigrationUpContract`, `TestPhase106MigrationDownContract`, `TestPhase106MigrationLiveUpDownUp`, `TestPhase106LedgerAppendOnly`, `TestPhase106ReversalCrossRowInvariants`, and `TestPhase106MigrationBoundary`. Static Up checks require rules before ledger, unique `(rule_code, rule_version)`, positive rule version/value, exact categories `fansub_work` and `platform_contribution`, no productive rule INSERT, immutable-rule and ledger-mutation trigger/function definitions, required Member, optional Actor/group/release-version with only those optional FKs using `ON DELETE SET NULL`, non-cascading Member/rule/reversal identity, rule/code/version/category/value snapshots, separate `effective_at`/`recorded_at`, unique idempotency key, coupled award/reversal checks, self-reference rejection, and a partial unique direct-reversal index. Require a BEFORE INSERT validator that branches by entry kind: awards must exactly match the referenced `point_rules` row for ID/code/version/category/positive value; reversals must exactly inherit the award's immutable semantic snapshot and use its exact negated value. Require the ledger UPDATE guard to retain `pg_trigger_depth() > 1`, at least one exact non-null→null transition among `actor_app_user_id`, `fansub_group_id`, and `release_version_id`, and `IS NOT DISTINCT FROM OLD` for every optional context column not being nulled plus every other ledger column. Additionally require the matching OLD parent to be absent for every column that is nulled: `NOT EXISTS (SELECT 1 FROM app_users WHERE id = OLD.actor_app_user_id)`, `NOT EXISTS (SELECT 1 FROM fansub_groups WHERE id = OLD.fansub_group_id)`, and `NOT EXISTS (SELECT 1 FROM release_versions WHERE id = OLD.release_version_id)`. If any changed column still has a live parent, the guard must raise the append-only exception; direct UPDATE and every DELETE remain rejected. Static Down checks require removal of all validation/immutability mechanisms before ledger then rules, with no unrelated object.

Live tests use only `OpenPhase106Postgres`, apply Up -> Down -> Up, insert explicit test rules, and prove PostgreSQL rejects UPDATE/DELETE on `point_rules` plus every direct UPDATE/DELETE on `point_ledger_entries`, including direct `UPDATE ... SET fansub_group_id = NULL`. Delete each optional parent type in isolated fixtures and prove its real nested FK action alone nulls the matching actor/group/release-version column after that OLD parent has disappeared, without changing any other ledger field; Member/rule/original deletion remains blocked. Inside the disposable Phase-106 database, deliberately install a foreign test trigger plus test function whose outer statement performs exactly one otherwise shape-valid nested context mutation, for example `fansub_group_id` non-null→null while that referenced `fansub_groups` parent still exists. Execute the outer statement and prove `TestPhase106LedgerAppendOnly/nested_context_null_with_live_parent` receives the append-only error; this is not a mixed or immutable-column mutation. Register cleanup immediately and drop that test trigger/function explicitly (also via `t.Cleanup` on failure) so neither can escape the isolated schema. Retain the existing test-only outer-trigger cases for an immutable ledger field or mixed context change as separate defenses. Direct SQL award inserts with a valid Rule ID but wrong rule code, wrong version, wrong category, or wrong positive value must fail. Direct SQL must also prove a reversal can reference only an award and must inherit Member, source type/key, rule ID/code/version/category, nullable group/release context, and the exact negated point value; self-reference, reversal-of-reversal, duplicate direct reversal, wrong sign, blank reason, and every mismatched inherited field fail. Keep the durable boundary test limited to the Phase-106 migration and fixture files: forbid media/upload/crop/thumbnail/relation/cleanup/review/capability/badge mutations and `public.` targets there, but do not scan `main.go`, handlers, or future Phase-107/108 consumer files.
  </action>
  <verify>
    <automated>cd backend; $output = go test ./internal/migrations -run 'TestPhase106MigrationUpContract' -count=1 2&gt;&amp;1; if ($LASTEXITCODE -eq 0) { throw 'Wave-0 migration contract must be RED before the Up migration exists' }; if (($output -join "`n") -notmatch '0131_member_point_foundation') { $output; throw 'unexpected Wave-0 failure' }</automated>
  </verify>
  <done>Der Datenbankvertrag kompiliert eigenständig und ist ausschließlich wegen der noch fehlenden Phase-106-Migration rot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process -> PostgreSQL cluster | Tests dürfen nur eine explizit erzeugte `team4s_phase106_test_*`-Datenbank und darin ein zufälliges Schema verwenden. |
| SQL writer -> ledger | PostgreSQL muss UPDATE/DELETE und ungültige Cross-Row-Stornos auch bei direktem SQL abweisen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-01 | Tampering | Test fixture | mitigate | DSN-Namensguard, Runtime-Datenbankprüfung, exklusiver Search-Path und kein `public`-Fallback. |
| T-106-02 | Tampering | point_ledger_entries | mitigate | Live-Verträge weisen direkte UPDATE/DELETE ab; eine verschachtelte exakt geformte non-null→null-Kontextmutation scheitert bei noch vorhandenem OLD-Parent, während echte `ON DELETE SET NULL`-Aktionen nach nachgewiesenem Parent-Wegfall bei sonst null-sicher unveränderter Zeile gelingen. |
| T-106-03 | Repudiation | Award/Reversal | mitigate | Direkte SQL-Tests verlangen Rule-ID/Code/Version/Kategorie/Wert-Gleichheit für Awards sowie award-only Referenz und vollständige geerbte Identität/Snapshots mit exakter Negation für Stornos. |
| T-106-04 | Denial of service | Future consumer evolution | mitigate | Dauerhafte Boundary-Scans prüfen nur Phase-106-Artefakte und blockieren keine legitime Phase-107/108-Verdrahtung. |
</threat_model>

<verification>
- `cd backend; go test ./internal/testsupport -count=1` ist ohne DB grün.
- Der erwartete RED-Test kompiliert und scheitert nur an der fehlenden Up-Migration.
- Kein Verify-Befehl setzt eine DSN auf `team4s_v2` oder einen anderen normalen Entwicklungsdatenbanknamen.
- `git diff --check` besteht.
</verification>

<success_criteria>
- Wave 1 enthält keine Tests, die erst in späteren Plänen entstehende Repository-/Service-Typen referenzieren.
- Die Testfixture ist opt-in und besitzt einen doppelten Guard vor falscher Datenbank- oder Schema-Nutzung.
- Append-only, die an den nachgewiesenen OLD-Parent-Wegfall gebundene FK-Kontextnullung sowie Award-Rule- und Cross-Row-Stornoinvarianten sind als echte PostgreSQL-Verträge festgelegt.
- Alle D-01 bis D-10 sind entweder im Fundament geprüft oder für ihre benannte spätere Phase geschützt.
</success_criteria>

<output>
After completion, create `.planning/phases/106-member-gamification-punktefundament/106-01-SUMMARY.md`
</output>
