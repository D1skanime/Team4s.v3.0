---
phase: 106-member-gamification-punktefundament
plan: "03"
type: execute
wave: 3
depends_on: ["106-01", "106-02"]
files_modified:
  - backend/internal/repository/audit_logs.go
  - backend/internal/repository/point_rules_repository.go
  - backend/internal/repository/point_rules_repository_test.go
  - backend/internal/repository/point_ledger_repository.go
  - backend/internal/repository/point_ledger_repository_test.go
autonomous: true
requirements: [GAM-02, GAM-03, GAM-04, GAM-05]
must_haves:
  truths:
    - "GAM-02 / D-04: Identische Award- und Reversal-Retries liefern dieselbe persistierte Zeile; semantische Abweichungen scheitern geschlossen."
    - "GAM-02: Parallele Awards oder Stornos werden durch PostgreSQL auf genau eine fachliche Buchung begrenzt."
    - "GAM-03 / D-04: Das Rule-Repository liest nur einen expliziten RuleRef aus Code und Version, ohne Latest-/Activation-Semantik."
    - "GAM-04 / D-02 / D-08: Repositorydaten bewahren Kategorie, Source, Snapshot und effective/recorded time ohne Content-/Hash-Bewertung."
    - "GAM-05 / D-03 / D-05 / D-06 / D-07 / D-09 / D-10: Repositorys bleiben intern und von späteren Review-, Badge-, Retention- und Media-Flows getrennt."
  artifacts:
    - path: "backend/internal/repository/audit_logs.go"
      provides: "Gemeinsame DBTX-Naht mit Exec und QueryRow"
    - path: "backend/internal/repository/point_rules_repository.go"
      provides: "Read-only Lookup exakt nach Code und Version"
      exports: ["NewPointRulesRepository"]
    - path: "backend/internal/repository/point_ledger_repository.go"
      provides: "Insert-first Award und Reversal mit Retry-Mismatch-Prüfung"
      exports: ["NewPointLedgerRepository"]
  key_links:
    - from: "backend/internal/repository/point_ledger_repository.go"
      to: "point_ledger_entries.idempotency_key"
      via: "INSERT ON CONFLICT DO NOTHING RETURNING plus vollständiger Vergleich"
      pattern: "ON CONFLICT.*DO NOTHING"
    - from: "backend/internal/repository/point_ledger_repository.go"
      to: "point_ledger_entries.reverses_entry_id"
      via: "INSERT SELECT aus dem Original und Konfliktauflösung nach Key/Original"
      pattern: "reverses_entry_id"
---

<objective>
Read-only Rule-Zugriff und race-sichere, append-only Award-/Reversal-Operationen auf der vorhandenen DBTX-Naht implementieren.

Purpose: Repositorys kapseln PostgreSQL-Arbitration und stabile Retry-/Fehlersemantik, während das Schema alle kritischen Invarianten auch bei direktem SQL schützt.
Output: Transaktionsbindbare Rule- und Ledger-Repositorys mit kompilierbaren Task-lokalen Tests und einem echten PostgreSQL-Wave-Gate.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/106-member-gamification-punktefundament/106-CONTEXT.md
@.planning/phases/106-member-gamification-punktefundament/106-RESEARCH.md
@.planning/phases/106-member-gamification-punktefundament/106-PATTERNS.md
@.planning/phases/106-member-gamification-punktefundament/106-VALIDATION.md
@.planning/phases/106-member-gamification-punktefundament/106-01-SUMMARY.md
@.planning/phases/106-member-gamification-punktefundament/106-02-SUMMARY.md
@.planning/notes/260722-member-gamification-DECISION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DBTX erweitern und expliziten RuleRef-Lookup samt Tests implementieren</name>
  <read_first>
    - AGENTS.md
    - docs/engineering/implementation-contract.md
    - docs/architecture/db-schema-fansub-domain.md
    - .planning/phases/106-member-gamification-punktefundament/106-CONTEXT.md
    - .planning/phases/106-member-gamification-punktefundament/106-RESEARCH.md
    - .planning/phases/106-member-gamification-punktefundament/106-PATTERNS.md
    - database/migrations/0131_member_point_foundation.up.sql
    - backend/internal/repository/audit_logs.go
    - backend/internal/repository/errors.go
    - backend/internal/repository/authz_permissions.go
  </read_first>
  <files>backend/internal/repository/audit_logs.go, backend/internal/repository/point_rules_repository.go, backend/internal/repository/point_rules_repository_test.go</files>
  <behavior>
    - "GetByRef(ctx, code, version) returns exactly that immutable rule."
    - "Unknown references wrap ErrNotFound; blank code or nonpositive version wraps ErrValidation."
    - "No latest, active, scheduled, update, delete, or upsert API exists."
  </behavior>
  <action>
Extend the existing exported `repository.DBTX` in `audit_logs.go` with `QueryRow(ctx context.Context, sql string, args ...any) pgx.Row`; do not create `PointDBTX`, request `Query`, or change AuditLogRepository behavior. Create `PointRule`, `PointRulesRepository{db DBTX}`, `NewPointRulesRepository`, `WithDB`, and exact `GetByRef(ctx, code, version)`. Query equality on both `rule_code` and `rule_version`; validate input; translate `pgx.ErrNoRows` to wrapped `ErrNotFound`; preserve errors with `%w`. There is no ORDER BY-version or catalog mutation path, enforcing the resolved Phase-106 RuleRef decision and D-04.

Create only `point_rules_repository_test.go` in this task. Use a small DBTX/Row fake or opt-in fixture for exact lookup, validation, not-found wrapping, and a source contract forbidding Latest/Active/Schedule/Update/Delete/Upsert. Do not create or compile `point_ledger_repository_test.go` until Task 2 creates all of its production types.
  </action>
  <verify>
    <automated>cd backend; go test ./internal/repository -run 'TestPointRules.*(Validation|ExactRef|NotFound|Boundary)' -count=1</automated>
  </verify>
  <done>Der exakte read-only RuleRef-Lookup und seine Tests kompilieren unabhängig vom noch nicht angelegten Ledger-Repository.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Idempotente Awards und idempotente referenzielle Stornos implementieren</name>
  <read_first>
    - AGENTS.md
    - docs/engineering/implementation-contract.md
    - docs/architecture/db-schema-fansub-domain.md
    - .planning/phases/106-member-gamification-punktefundament/106-CONTEXT.md
    - .planning/phases/106-member-gamification-punktefundament/106-RESEARCH.md
    - .planning/phases/106-member-gamification-punktefundament/106-PATTERNS.md
    - database/migrations/0131_member_point_foundation.up.sql
    - backend/internal/repository/audit_logs.go
    - backend/internal/repository/errors.go
    - backend/internal/repository/point_rules_repository.go
    - backend/internal/repository/anime_contributions_proposal_repository.go
    - backend/internal/repository/anime_contributions_proposal_merge_repository.go
  </read_first>
  <files>backend/internal/repository/point_ledger_repository.go, backend/internal/repository/point_ledger_repository_test.go</files>
  <behavior>
    - "Identical award and reversal retries return the persisted row; a mismatch returns ErrConflict without mutation."
    - "InsertReversal derives inherited identity/snapshot/context from one locked award and never accepts caller copies of them."
    - "Concurrent awards/reversals leave exactly one semantic row each, including a retry after a simulated lost response."
  </behavior>
  <action>
Define `PointLedgerEntry` for every ledger column and `PointAwardInput` for server-derived award data. Implement `InsertAward` as `INSERT ... ON CONFLICT (idempotency_key) DO NOTHING RETURNING`; after empty RETURNING load by key and compare kind, Member, Actor, source, rule ID/code/version/category/value, group/release context, and effective time. Exclude only recorded time. Return the existing row only for an identical retry; mismatches wrap `ErrConflict`. The migration's BEFORE INSERT validator independently rejects any award snapshot that does not exactly match the referenced `point_rules` row.

Define a deliberately narrow `PointReversalInput{OriginalEntryID, ActorAppUserID, IdempotencyKey, EffectiveAt, Reason}`. Do not accept caller-provided Member, source, rule, value, or group/release copies. Keep `GetForUpdate` for service-level validation. Implement `InsertReversal` as one append-only `INSERT ... SELECT` from an original award, copying its Member/source/rule ID/code/version/category/context and negating its value. Use `ON CONFLICT DO NOTHING RETURNING` so both the idempotency-key constraint and direct-reversal partial unique constraint arbitrate races. On empty RETURNING, load the original plus the existing row by idempotency key and/or `reverses_entry_id`, compare the full derived row and all reversal command fields, and return that same row for an identical retry. A different reason, Actor, effective time, key, original, inherited category, or inherited value is `ErrConflict`; missing/non-award originals are stable not-found/validation errors. This covers a committed write whose response was lost as well as parallel retries.

Create repository tests in the same task so all referenced types already exist. Fast fake/static tests cover validation, SQL shape, sentinels, and forbidden UPDATE/DELETE. Opt-in PostgreSQL tests use the guarded fixture and barriers immediately before competing calls. Prove identical and mismatched award retries, parallel awards, identical reversal retry after success, parallel reversals, mismatch conflicts, reversal-of-reversal rejection, and forced rollback. Direct SQL trigger coverage remains owned by 106-01/02. Add no aggregate, list, update, delete, handler, review, badge, or media operation.
  </action>
  <verify>
    <automated>cd backend; go test ./internal/repository -run 'TestPointLedger.*(Validation|SQLContract|RetryComparison|ErrorMapping)' -count=1</automated>
  </verify>
  <done>Repositorytests und Implementierung kompilieren gemeinsam; identische Award-/Storno-Retries konvergieren und Mismatches scheitern geschlossen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository input -> PostgreSQL | Inputs werden validiert; geerbte Stornofelder werden nicht vom Caller akzeptiert. |
| Concurrent calls -> unique constraints | Insert-first-Pfade müssen auf dieselbe persistierte Zeile konvergieren. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-09 | Tampering | InsertAward | mitigate | Vollständiger semantischer Vergleich einschließlich Rule-Kategorie nach Idempotenzkonflikt; DB-Trigger validiert jeden neuen Award gegen `point_rules`. |
| T-106-10 | Tampering | InsertReversal | mitigate | Minimaler Input und `INSERT SELECT` verhindern Caller-manipulierte geerbte Felder. |
| T-106-11 | Repudiation | Lost-response retry | mitigate | Nach Konflikt vorhandenes Storno laden, vollständig vergleichen und identisch zurückgeben. |
| T-106-12 | Denial of service | Concurrent retry | mitigate | PostgreSQL Unique/Partial-Unique plus echte Barrier-Tests. |
</threat_model>

<verification>
- Fast checks: `cd backend; go test ./internal/repository -run 'TestPoint(Rules|Ledger).*(Validation|ExactRef|NotFound|Boundary|SQLContract|RetryComparison|ErrorMapping)' -count=1`.
- Disposable DB gate: `docker compose up -d team4sv30-db; $dbName = 'team4s_phase106_test_' + [guid]::NewGuid().ToString('N'); try { docker compose exec -T team4sv30-db createdb -U team4s $dbName; if ($LASTEXITCODE -ne 0) { throw 'create test database failed' }; $port = if ($env:POSTGRES_PORT) { $env:POSTGRES_PORT } else { '5433' }; $env:TEAM4S_PHASE106_TEST_DSN = "postgres://team4s:team4s_dev_password@127.0.0.1:$port/$dbName?sslmode=disable"; Push-Location backend; try { go test ./internal/repository -run 'TestPointLedgerPostgres.*(Award|Reversal|Concurrent|LostResponse|Rollback)' -count=1; if ($LASTEXITCODE -ne 0) { throw 'phase 106 repository live tests failed' } } finally { Pop-Location } } finally { Remove-Item Env:TEAM4S_PHASE106_TEST_DSN -ErrorAction SilentlyContinue; if ($dbName -like 'team4s_phase106_test_*') { docker compose exec -T team4sv30-db dropdb -U team4s --force $dbName } }`.
- `cd backend; go vet ./internal/repository` und `git diff --check` bestehen.
</verification>

<success_criteria>
- Rule lookup requires explicit code plus version and exposes no latest/activation policy.
- Award and reversal retries are both idempotent under concurrency and lost responses.
- Reversal callers cannot supply inherited identity, source, rule, value, or context.
- Existing DBTX is reused; no second DB abstraction or later-phase consumer is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/106-member-gamification-punktefundament/106-03-SUMMARY.md`
</output>
