---
phase: 01-ownership-foundations
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - database/migrations/0035_add_admin_anime_mutation_audit.up.sql
  - database/migrations/0035_add_admin_anime_mutation_audit.down.sql
  - backend/cmd/server/main.go
  - backend/internal/handlers/admin_content_anime.go
  - backend/internal/handlers/admin_content_authz.go
  - backend/internal/handlers/media_upload.go
  - backend/internal/repository/admin_content.go
  - backend/internal/repository/admin_content_test.go
  - backend/internal/repository/media_upload.go
  - backend/internal/handlers/admin_content_test.go
autonomous: true
requirements:
  - RLY-03
must_haves:
  truths:
    - "Phase 1 anime editor mutations only run with a trusted authenticated admin identity."
    - "Anime save and cover-upload/remove operations leave durable actor attribution in the database instead of transient logs only."
    - "Unauthenticated or actor-less admin anime mutation requests fail clearly instead of using dev fallback identities."
  artifacts:
    - path: "database/migrations/0035_add_admin_anime_mutation_audit.up.sql"
      provides: "Durable audit storage for admin anime mutations"
    - path: "backend/internal/handlers/admin_content_authz.go"
      provides: "Strict acting-admin resolution without dev bypass"
    - path: "backend/internal/handlers/media_upload.go"
      provides: "Authenticated upload actor capture for Phase 1 cover handling"
  key_links:
    - from: "backend/cmd/server/main.go"
      to: "backend/internal/handlers/admin_content_authz.go"
      via: "auth middleware on admin anime and upload routes"
      pattern: "CommentAuthMiddlewareWithState"
    - from: "backend/internal/handlers/admin_content_anime.go"
      to: "database/migrations/0035_add_admin_anime_mutation_audit.up.sql"
      via: "repository-backed audit write with actor ID"
      pattern: "actor|audit"
---

<objective>
Make Phase 1 anime mutations durably attributable to a real acting admin user ID.

Purpose: `RLY-03` is structural scope in this phase, not cleanup. The current route auth bypasses and dev fallback identity make any audit claim untrustworthy.
Output: Strict admin auth enforcement, durable audit persistence, and regression tests for save and cover mutation paths.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-ownership-foundations/01-CONTEXT.md
@.planning/phases/01-ownership-foundations/01-RESEARCH.md
@backend/cmd/server/main.go
@backend/internal/handlers/admin_content_anime.go
@backend/internal/handlers/admin_content_authz.go
@backend/internal/handlers/media_upload.go
@backend/internal/repository/admin_content.go
@backend/internal/repository/media_upload.go

<interfaces>
From backend/internal/handlers/admin_content_authz.go:
```go
func (h *AdminContentHandler) requireAdmin(c *gin.Context) (middleware.AuthIdentity, bool)
```

From backend/internal/handlers/admin_content_anime.go:
```go
func (h *AdminContentHandler) CreateAnime(c *gin.Context)
func (h *AdminContentHandler) UpdateAnime(c *gin.Context)
```

From backend/internal/repository/media_upload.go:
```go
type MediaUploadRepo interface {
  CreateMediaAsset(ctx context.Context, asset *models.UploadMediaAsset) error
  DeleteMediaAsset(ctx context.Context, id string) error
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add durable audit storage and repository writes for Phase 1 anime mutations</name>
  <files>database/migrations/0035_add_admin_anime_mutation_audit.up.sql, database/migrations/0035_add_admin_anime_mutation_audit.down.sql, backend/internal/repository/admin_content.go, backend/internal/repository/admin_content_test.go</files>
  <read_first>
    - database/migrations/0034_add_release_decomposition_tables.up.sql
    - backend/internal/repository/admin_content.go
    - backend/internal/repository/admin_content_test.go
    - .planning/phases/01-ownership-foundations/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Admin anime create/update writes persist an audit row containing actor user ID, mutation kind, anime ID, and enough payload detail to trace the change later.
    - Test 2: Cover-removal mutations performed through the Phase 1 editor path also persist actor attribution.
    - Test 3: Audit persistence occurs in the same logical write path as the domain mutation, not as best-effort logging after the fact.
  </behavior>
  <action>Create migration `0035_add_admin_anime_mutation_audit` for a durable audit table dedicated to admin anime mutations. Use explicit columns for `id`, `anime_id`, `actor_user_id`, `mutation_kind`, `request_payload`, and timestamps; keep naming concrete and repo-local rather than generic event-bus abstractions. Extend the admin content repository so anime create/update operations write both the domain mutation and its audit record in one transaction. Include concrete mutation kinds for the Phase 1 surface, at minimum `anime.create`, `anime.update`, and `anime.cover.remove`, per D-05 and D-06.</action>
  <acceptance_criteria>
    - `Test-Path database/migrations/0035_add_admin_anime_mutation_audit.up.sql` returns true.
    - `Select-String -Path database/migrations/0035_add_admin_anime_mutation_audit.up.sql -Pattern "actor_user_id|mutation_kind|request_payload"` returns matches.
    - `Select-String -Path backend/internal/repository/admin_content.go -Pattern "anime\\.create|anime\\.update|anime\\.cover\\.remove|actor"` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/repository -run 'TestAdminContentRepository' -count=1</automated>
  </verify>
  <done>Anime create/update/remove-cover operations persist actor-backed audit records in the database, not just log lines.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Require authenticated admin identity on anime and upload routes and propagate actor IDs</name>
  <files>backend/cmd/server/main.go, backend/internal/handlers/admin_content_anime.go, backend/internal/handlers/admin_content_authz.go, backend/internal/handlers/media_upload.go, backend/internal/repository/media_upload.go, backend/internal/handlers/admin_content_test.go</files>
  <read_first>
    - backend/cmd/server/main.go
    - backend/internal/handlers/admin_content_authz.go
    - backend/internal/handlers/admin_content_anime.go
    - backend/internal/handlers/media_upload.go
    - backend/internal/repository/media_upload.go
    - .planning/phases/01-ownership-foundations/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `POST /api/v1/admin/anime`, `PATCH /api/v1/admin/anime/:id`, `POST /api/v1/admin/upload`, and `DELETE /api/v1/admin/media/:id` reject requests without a real auth context.
    - Test 2: Successful anime mutations receive the authenticated actor ID from `requireAdmin` and pass it into the repository audit path.
    - Test 3: Successful admin uploads persist `uploaded_by` from the same authenticated actor path instead of silent defaults or missing identity.
  </behavior>
  <action>Remove the dev-bypass behavior from `backend/internal/handlers/admin_content_authz.go` and treat missing auth identity as an explicit unauthorized response. Re-enable `middleware.CommentAuthMiddlewareWithState` on the Phase 1 admin anime and upload routes in `backend/cmd/server/main.go`. Update admin anime handlers to pass actor identity into repository audit writes, and update `backend/internal/handlers/media_upload.go` plus `backend/internal/repository/media_upload.go` so upload/delete paths capture the authenticated actor for media asset attribution. Keep the change scoped to the admin anime foundation surface; do not redesign global authz beyond what Phase 1 exposes.</action>
  <acceptance_criteria>
    - `Select-String -Path backend/cmd/server/main.go -Pattern "/admin/anime|/admin/upload|CommentAuthMiddlewareWithState"` shows the middleware is active on those routes.
    - `Select-String -Path backend/internal/handlers/admin_content_authz.go -Pattern "dev mode bypass"` returns no matches.
    - `Select-String -Path backend/internal/handlers/media_upload.go -Pattern "uploaded_by|AuthIdentity|unauthorized|nicht angemeldet"` returns matches proving actor capture and rejection handling exist.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/handlers ./internal/repository -run 'TestAdminContent|TestMediaUpload' -count=1</automated>
  </verify>
  <done>Phase 1 anime and upload mutations require trusted auth, propagate actor IDs into persistence, and fail closed when the actor is missing.</done>
</task>

</tasks>

<verification>
Run the targeted handler/repository slice and confirm route wiring no longer comments out auth middleware. Audit migration files must exist before tests are considered complete.
</verification>

<success_criteria>
Admin anime save and cover-related mutations are durably attributable to authenticated admins, with no route-level or handler-level fallback identities left in the Phase 1 surface.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ownership-foundations/01-02-SUMMARY.md`
</output>
