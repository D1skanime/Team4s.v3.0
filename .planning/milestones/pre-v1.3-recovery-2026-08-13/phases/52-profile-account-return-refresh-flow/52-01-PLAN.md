---
phase: 52-profile-account-return-refresh-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/admin/profile/page.tsx
  - frontend/src/app/admin/profile/page.test.tsx
autonomous: true
requirements:
  - AUTH-PROFILE-ACCOUNT-RETURN-01
must_haves:
  truths:
    - "D-02 is respected: profile UI uses refreshActiveAuthSession(), not direct token or Keycloak helper access."
    - "D-03 is respected: account cards are refreshed through getOwnProfile() after /api/v1/me session refresh."
    - "D-04 is respected: return refresh does not overwrite unsaved Team4s profile form edits."
  artifacts:
    - path: "frontend/src/app/admin/profile/page.test.tsx"
      provides: "Regression coverage for Keycloak return refresh and dirty-form preservation"
  key_links:
    - from: "frontend/src/lib/api.ts refreshActiveAuthSession"
      to: "frontend/src/app/admin/profile/page.tsx return refresh"
      via: "central auth/API seam"
      pattern: "refreshActiveAuthSession|getOwnProfile"
---

<objective>
Add the profile-page return-refresh mechanics and focused regression tests so Team4s can refresh account cards after the user returns from Keycloak without violating auth boundaries or resetting unsaved profile edits.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/52-profile-account-return-refresh-flow/52-CONTEXT.md
@.planning/phases/52-profile-account-return-refresh-flow/52-RESEARCH.md
@docs/frontend/auth-api-client.md
@docs/engineering/implementation-contract.md
@frontend/src/app/admin/profile/page.tsx
@frontend/src/app/admin/profile/page.test.tsx
@frontend/src/lib/api.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Add failing profile return-refresh regressions</name>
  <files>frontend/src/app/admin/profile/page.test.tsx</files>
  <read_first>
    - frontend/src/app/admin/profile/page.test.tsx
    - frontend/src/lib/api.ts
    - docs/frontend/auth-api-client.md
  </read_first>
  <action>
    Extend the profile page test mocks to include `refreshActiveAuthSession`. Add focused tests that prove: clicking the Keycloak account link marks the handoff, returning focus calls `refreshActiveAuthSession()` then `getOwnProfile()`, changed account display/email renders `Accountdaten aktualisiert.`, unchanged account data produces no success message, and an edited Team4s form field such as `Anzeigename` is not overwritten by the return refresh.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run test -- --run "src/app/admin/profile/page.test.tsx"</automated>
  </verify>
  <done>
    Tests fail for the current implementation for the return-refresh behavior, not because of unrelated setup errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement guarded return refresh in the profile page</name>
  <files>frontend/src/app/admin/profile/page.tsx</files>
  <read_first>
    - frontend/src/app/admin/profile/page.tsx
    - frontend/src/lib/api.ts
    - docs/frontend/auth-api-client.md
  </read_first>
  <action>
    Import and use `refreshActiveAuthSession()` from the central API module. Refactor the initial profile load into a local helper that can either sync the form or preserve current form state. Track whether the Keycloak account link was opened, whether the form is dirty, and whether a return refresh is already running. Add focus/visibility listeners that run only after the Keycloak handoff and only when the Team4s tab becomes active again.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run test -- --run "src/app/admin/profile/page.test.tsx"</automated>
  </verify>
  <done>
    Return refresh updates account-card data via existing helpers and does not reset dirty Team4s profile form fields.
  </done>
</task>

<task type="auto">
  <name>Task 3: Keep auth boundary and race behavior clean</name>
  <files>frontend/src/app/admin/profile/page.tsx, frontend/src/app/admin/profile/page.test.tsx</files>
  <read_first>
    - frontend/src/lib/api.no-token-boundary.test.ts
    - frontend/src/lib/api.auth-refresh.test.ts
  </read_first>
  <action>
    Ensure the implementation does not introduce token props, direct token reads, direct Keycloak helper calls, or ad hoc fetches. Add a regression assertion if useful to prove duplicate focus events do not run parallel refreshes.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm test -- --run src/lib/api.no-token-boundary.test.ts src/lib/api.auth-refresh.test.ts</automated>
  </verify>
  <done>
    Existing auth boundary tests still pass, and the profile page stays inside the central auth/API seam.
  </done>
</task>

</tasks>
