---
phase: 46-fansub-group-invitations-und-join-requests-mvp
plan: "03"
type: execute
wave: 3
depends_on:
  - 46-02-PLAN.md
files_modified:
  - frontend/src/app
  - frontend/src/lib
  - frontend/src/types
autonomous: true
requirements:
  - FANSUB-INVITES-01
must_haves:
  truths:
    - "The frontend may only consume invitation capabilities, never infer roles."
    - "The existing fansub members/admin surface is the preferred MVP anchor."
    - "The create flow must surface the raw invite token once without storing it client-side as durable permission state."
---

<objective>
Connect the invitation MVP to the admin UI and accept flow: open invitations list, invitation creation/cancellation, and authenticated invitation acceptance with clear status UX.

Purpose: Give operators and invitees the thinnest useful UI on top of the secured backend lifecycle.
</objective>

<context>
@.planning/phases/46-fansub-group-invitations-und-join-requests-mvp/46-02-PLAN.md
@.planning/phases/46-fansub-group-invitations-und-join-requests-mvp/46-CONTEXT.md
@frontend/src/app/admin/fansubs/[id]/members/page.tsx
@frontend/src/lib/api.ts
@frontend/src/types/fansub.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend frontend API/types for invitation lifecycle and capability fields</name>
  <read_first>
    - frontend/src/lib/api.ts
    - frontend/src/types/fansub.ts
  </read_first>
  <action>
    Add or extend the frontend contract for:
    - fansub-group invitation capabilities
    - list invitations
    - create invitation
    - cancel invitation
    - accept invitation

    Include typed support for:
    - invite email
    - invited role codes
    - expiry metadata
    - invitation status
    - one-time raw token returned at creation time
    - capability flags `canViewInvitations`, `canCreateInvitation`, `canCancelInvitation`
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck</automated>
  </verify>
  <done>
    - Frontend types and client helpers match the invitation backend contract.
    - Invitation capabilities are first-class in the fansub-group client seam.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend the fansub members/admin surface with open invitations management</name>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/members/page.tsx
  </read_first>
  <action>
    Reuse the existing fansub members/admin area and add a minimal invitation section that:
    - loads invitation capabilities
    - shows open invitations only when allowed
    - shows create-invitation controls only when allowed
    - allows cancellation only when allowed
    - displays the newly created raw invite token/link once in a clear copy/share surface
    - displays status, email, invited roles, expiry, and creator information without leaking any token material

    Prefer existing inline panel or lightweight dialog patterns over a new UI framework.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck && npm run lint</automated>
  </verify>
  <done>
    - Operators can manage open invitations from the existing fansub admin surface.
    - All visible actions are capability-driven.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add an authenticated invitation-accept experience with clear error states</name>
  <read_first>
    - touched frontend routes/helpers
    - existing error handling patterns
  </read_first>
  <action>
    Add the thinnest useful accept-invitation UX for logged-in users:
    - token intake or invite-link landing
    - invitation preview/confirmation if needed by the chosen backend contract
    - accept action using the authenticated app-user session
    - clear handling for `401`, `403`, `404`, and `409`
    - clear handling for expiry (`410` or project-convention equivalent) and email mismatch

    If join requests are only preparatory in backend scope, reflect that as a deliberate omission rather than a half-implemented UI.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run lint</automated>
  </verify>
  <done>
    - Invitees have a minimal authenticated acceptance path.
    - Error states for invalid/expired/used invites are understandable and scoped.
  </done>
</task>

</tasks>

<verification>
Automated:
1. `cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck`
2. `cd C:/Users/admin/Documents/Team4s/frontend && npm run lint`

Behavioral:
1. Invitation controls are hidden or disabled when capabilities are false.
2. The create flow exposes the raw token once.
3. Invalid or expired invitation acceptance shows understandable feedback.
</verification>

<success_criteria>
- The invitation UI is capability-driven and anchored to existing fansub admin surfaces.
- Create/cancel/list/accept flows are reachable without client-side role logic.
- Error handling is clear for the important invitation states.
</success_criteria>
