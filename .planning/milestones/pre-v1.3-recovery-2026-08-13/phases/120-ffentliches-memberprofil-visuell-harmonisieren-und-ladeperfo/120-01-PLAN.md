---
phase: 120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-01-PHASE119-BASELINE.patch
  - .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-01-PHASE119-AUTHORIZATION.json
  - .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/verify-overlap-chain.mjs
  - .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/verify-overlap-chain.test.mjs
  - .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/evidence/120-01-initial-overlap.json
  - .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-01-SUMMARY.md
autonomous: false
requirements: [D-14]
must_haves:
  truths:
    - "Phase 119 remains blocked unless separately approved; Phase 120 may begin only from an explicitly authorized byte snapshot."
    - "Every dirty Phase-119 overlap file has an initial SHA-256 entry and every later edit must extend that file's immutable predecessor chain."
    - "A mismatch, missing predecessor, reused output path, absent authorization ID or attempted auto-refresh aborts before an edit."
  artifacts:
    - {path: .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/verify-overlap-chain.mjs, provides: "begin/check/finish per-file chain validator"}
    - {path: .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/evidence/120-01-initial-overlap.json, provides: "immutable initial SHA-256 for every authorized overlap file"}
    - {path: .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-01-PHASE119-AUTHORIZATION.json, provides: "explicit input authorization ID bound to HEAD/status/diff/baseline"}
  key_links:
    - {from: evidence/120-01-initial-overlap.json, to: every later overlap evidence manifest, via: "per-file predecessor id and SHA-256"}
---
<objective>Capture and authorize the exact dirty Phase-119 input, then establish an immutable per-file authorization chain that survives intervening edits without silently accepting them.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/ROADMAP.md
@.planning/phases/119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha/119-05-SUMMARY.md
@.planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-CONTEXT.md
@AGENTS.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Capture Phase-119 bytes and implement the per-file chain validator</name>
  <files>.planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-01-PHASE119-BASELINE.patch, .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/verify-overlap-chain.mjs, .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/verify-overlap-chain.test.mjs, .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-01-SUMMARY.md</files>
  <read_first>
    .planning/phases/119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha/119-05-SUMMARY.md
    frontend/src/components/ui/FocalCarousel.tsx
    frontend/src/components/ui/FocalCarousel.module.css
    frontend/src/components/ui/FocalCarousel.test.tsx
    frontend/src/components/profile/MemberBadgeChain.tsx
    frontend/src/components/profile/MemberBadgeChain.module.css
    frontend/src/components/profile/MemberBadgeChain.test.tsx
    frontend/src/components/profile/LatestContributionsSection.tsx
    frontend/src/components/profile/LatestContributionsSection.test.tsx
  </read_first>
  <behavior>check rejects changed/missing files and wrong predecessor hashes, and its final-audit form checks every initial path while applying explicit latest-manifest overrides; begin records immutable before hashes and actual HEAD in a uniquely named hidden evidence receipt; finish refuses overwrite, verifies and removes only that receipt, and writes predecessor id/hash plus before/after SHA-256 per file; no command refreshes authorization.</behavior>
  <action>Per D-14, do not edit/reset/stage Phase-119 production, tests or badge assets. Capture host `git rev-parse HEAD`, exact sorted `git status --short`, SHA-256 of status and complete binary diff, ROADMAP 3/5 state, and a binary-safe patch including untracked Phase-119 inputs. The overlap set is exactly the eight named TS/TSX/CSS files above plus every dirty `frontend/public/member-achievement-badges/*` path found in the captured status. Implement `verify-overlap-chain.mjs` with commands `check`, `begin`, `finish`, and `init`: all require `--authorization`; `check/begin` accept repeatable `--expect path=manifest`; `check` also accepts `--initial manifest` plus repeatable `--latest path=manifest` for the final audit, verifies every path in the initial manifest, and uses a latest override only for that exact path; `begin` additionally requires `--plan`, `--task`, `--output` and writes a uniquely named hidden receipt under repository `evidence/`; `finish` requires that receipt and a new evidence output, refuses an existing output, recomputes current bytes, records schemaVersion, authorizationId, plan/task, input HEAD, finish HEAD, predecessor manifest id/hash, beforeSha256 and afterSha256 per file plus canonical manifestDigest, and deletes only its matching receipt. `init` may run only after explicit approval and writes the initial manifest once. Reject missing files, duplicate paths, unrelated overrides, predecessor entries not matching the same path, authorization/digest mismatch, absent HEAD, modified bytes, output reuse and any `--refresh`/accept behavior. Add Node tests for all rejection paths, a valid two-transition chain and a full initial-plus-latest audit. Run through the frontend Compose image with repo root mounted at `/workspace`; never use host node/npm.</action>
  <verify><automated>docker compose run --rm --no-deps -T -v "$PWD:/workspace" -w /workspace team4sv30-frontend node --test .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/verify-overlap-chain.test.mjs &amp;&amp; test -s .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-01-PHASE119-BASELINE.patch &amp;&amp; git diff --check</automated></verify>
  <done>The exact unresolved input and a tested fail-closed per-file chain tool exist; no approval is inferred.</done>
</task>
<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Authorize the captured bytes and emit the immutable initial manifest</name>
  <files>.planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-01-PHASE119-AUTHORIZATION.json, .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/evidence/120-01-initial-overlap.json, .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-01-SUMMARY.md</files>
  <read_first>
    .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-01-PHASE119-BASELINE.patch
    .planning/phases/119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha/119-05-SUMMARY.md
  </read_first>
  <decision>May Phase 120 extend this exact unresolved Phase-119 byte snapshot?</decision>
  <action>Present HEAD, status/diff/baseline digests, overlap paths and focused suite results. `resolve-119` stops with no authorization or initial manifest. `authorize-snapshot` creates a never-refreshed authorization JSON with a unique authorizationId, actor/time, captured HEAD and digests, then runs `init` once with every exact overlap path to produce `evidence/120-01-initial-overlap.json`. Immediately run `check` for every path against that manifest. Any mismatch stops; never rewrite either artifact.</action>
  <options>
    <option id="resolve-119"><name>Resolve Phase 119 first</name><pros>Starts from an independently completed predecessor.</pros><cons>Phase 120 remains blocked.</cons></option>
    <option id="authorize-snapshot"><name>Authorize exact snapshot</name><pros>Allows a byte-audited continuation without falsely completing Phase 119.</pros><cons>Any overlap-byte change must have an explicit manifest transition.</cons></option>
  </options>
  <verify><automated>docker compose run --rm --no-deps -T -v &quot;$PWD:/workspace&quot; -w /workspace team4sv30-frontend node .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/verify-overlap-chain.mjs check --authorization .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-01-PHASE119-AUTHORIZATION.json --initial .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/evidence/120-01-initial-overlap.json &amp;&amp; git diff --check</automated></verify>
  <done>Only explicit authorization produces the initial per-file chain root and completes Plan 01.</done>
  <resume-signal>Select `resolve-119` or `authorize-snapshot`; do not continue without one.</resume-signal>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Disposition | Mitigation Plan |
|---|---|---|---|
| T-120-01 | Tampering/repudiation | mitigate | Immutable per-file predecessor hashes, actual-byte recomputation and explicit owner authorization. |
| T-120-02 | Information disclosure | transfer | No profile execution; later auth-keyed tests remain blocking. |
| T-120-03 | SSRF | transfer | No optimizer work in preflight. |
| T-120-04 | Elevation | transfer | No auth behavior changes. |
| T-120-05 | Upload renderer | accept | No media mutation. |
| T-120-06 | Availability | mitigate | Focused inherited suites and fail-closed chain tests. |
</threat_model>
<verification>Snapshot, helper tests, explicit decision and initial per-file manifest are all required.</verification>
<success_criteria>Every downstream overlap edit has a cryptographically exact, non-refreshable chain root.</success_criteria>
<source_coverage>GOAL dependency safety covered; REQ none; RESEARCH predecessor gate covered; CONTEXT D-14 covered; deferred none.</source_coverage>
<output>After authorization create 120-01-SUMMARY.md; keep Phase 119 formally blocked.</output>
