---
phase: 29-fansub-group-model-normalization-and-generic-links
plan: 03
type: execute
wave: 3
depends_on:
  - 29-01-PLAN.md
  - 29-02-PLAN.md
files_modified:
  - database/migrations/0055_fansub_legacy_cleanup_boundary.up.sql
  - database/migrations/0055_fansub_legacy_cleanup_boundary.down.sql
  - backend/internal/repository/fansub_repository.go
  - backend/internal/repository/fansub_repository_test.go
  - DECISIONS.md
  - STATUS.md
  - CONTEXT.md
  - WORKING_NOTES.md
  - TODO.md
autonomous: true
requirements:
  - P29-SC5
must_haves:
  truths:
    - "Legacy duplicate fields have an explicit compatibility boundary and are no longer silently treated as equal product truth."
    - "Cleanup is additive-safe first: no hard drop lands without verification that active CRUD paths now use the canonical model."
    - "Handoff and verification docs must say exactly which fields remain compatibility-only after Phase 29."
---

<objective>
Close Phase 29 with a verified cleanup boundary so the new canonical model does not leave ambiguous duplicate-field semantics behind.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-fansub-group-model-normalization-and-generic-links/29-CONTEXT.md
@.planning/phases/29-fansub-group-model-normalization-and-generic-links/29-RESEARCH.md
@.planning/phases/29-fansub-group-model-normalization-and-generic-links/29-VALIDATION.md
@backend/internal/repository/fansub_repository.go
@backend/internal/repository/fansub_repository_test.go
@DECISIONS.md
@STATUS.md
@CONTEXT.md
@WORKING_NOTES.md
@TODO.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Codify the legacy cleanup boundary in migration and repository behavior</name>
  <read_first>
    - backend/internal/repository/fansub_repository.go
    - backend/internal/repository/fansub_repository_test.go
    - .planning/phases/29-fansub-group-model-normalization-and-generic-links/29-RESEARCH.md
    - .planning/phases/29-fansub-group-model-normalization-and-generic-links/29-CONTEXT.md
  </read_first>
  <action>Add one cleanup-boundary migration and repository alignment step that makes the remaining duplicate fansub fields explicit. Prefer compatibility-safe actions such as backfill, comments, view/projection alignment, or dropping only unused duplicate references like alias-side `group_id` when tests prove the active code no longer depends on them. Do not hard-drop `closed_year` or `history_description` unless verification proves no active surface still reads them; if they remain, mark them clearly as transitional and stop using them in live write paths. Mirror the durable outcome into `DECISIONS.md` so the canonical-vs-transitional split does not have to be rediscovered later.</action>
  <acceptance_criteria>
    - `Select-String -Path database/migrations/0055_fansub_legacy_cleanup_boundary.up.sql -Pattern "group_id|closed_year|history_description|COMMENT|DROP|UPDATE"` returns matches.
    - `Select-String -Path backend/internal/repository/fansub_repository.go -Pattern "closed_year|history_description|website_url|discord_url|irc_url"` shows only deliberate compatibility handling, not new primary write logic.
    - `cd backend && go test ./internal/repository -count=1` passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Refresh handoff and verification docs for the canonical fansub baseline</name>
  <read_first>
    - DECISIONS.md
    - STATUS.md
    - CONTEXT.md
    - WORKING_NOTES.md
    - TODO.md
    - .planning/phases/29-fansub-group-model-normalization-and-generic-links/29-VALIDATION.md
  </read_first>
  <action>Update `DECISIONS.md`, `STATUS.md`, `CONTEXT.md`, `WORKING_NOTES.md`, and `TODO.md` so the next session can tell at a glance which fansub fields are canonical now, which link seam is authoritative, whether collaboration-member admin is live, and which legacy fields still remain transitional after Phase 29. Add the exact verification commands that proved the new baseline, plus any remaining cleanup follow-up if a hard field drop was intentionally deferred.</action>
  <acceptance_criteria>
    - `Select-String -Path DECISIONS.md -Pattern "fansub|generic link|collaboration|legacy"` returns matches.
    - `Select-String -Path STATUS.md -Pattern "fansub|generic link|collaboration|legacy"` returns matches.
    - `Select-String -Path CONTEXT.md -Pattern "fansub|generic link|collaboration|legacy"` returns matches.
    - `Select-String -Path WORKING_NOTES.md -Pattern "fansub|generic link|collaboration|legacy"` returns matches.
    - `Select-String -Path TODO.md -Pattern "fansub|cleanup|legacy"` returns matches.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run the targeted repository tests, then verify the handoff docs explicitly name the canonical fansub contract, the authoritative generic link seam, and any deferred cleanup fields.
</verification>

<success_criteria>
Phase 29 ends with a documented and verified cleanup boundary instead of a half-migrated fansub model.
</success_criteria>

<output>
After completion, create `.planning/phases/29-fansub-group-model-normalization-and-generic-links/29-03-SUMMARY.md`
</output>
