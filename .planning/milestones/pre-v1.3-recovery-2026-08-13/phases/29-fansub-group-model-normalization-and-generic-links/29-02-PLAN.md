---
phase: 29-fansub-group-model-normalization-and-generic-links
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01-PLAN.md
files_modified:
  - frontend/src/types/fansub.ts
  - frontend/src/lib/api.ts
  - frontend/src/app/admin/fansubs/create/page.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css
  - backend/internal/handlers/fansub_collaborations.go
  - backend/internal/repository/fansub_repository.go
autonomous: true
requirements:
  - P29-SC3
  - P29-SC4
must_haves:
  truths:
    - "Fansub admin create/edit surfaces manage community links as generic rows rather than only three fixed URL inputs."
    - "Collaboration membership is visible and explicitly manageable when `group_type='collaboration'`."
    - "The default fansub list remains focused on real groups even while collaboration detail becomes administrierbar."
---

<objective>
Move the fansub admin UI onto the canonical generic-link and explicit-collaboration model after the backend contract is ready.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-fansub-group-model-normalization-and-generic-links/29-CONTEXT.md
@.planning/phases/29-fansub-group-model-normalization-and-generic-links/29-RESEARCH.md
@frontend/src/types/fansub.ts
@frontend/src/lib/api.ts
@frontend/src/app/admin/fansubs/create/page.tsx
@frontend/src/app/admin/fansubs/[id]/edit/page.tsx
@frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css
@backend/internal/handlers/fansub_collaborations.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace fixed community-link inputs with generic link rows</name>
  <read_first>
    - frontend/src/types/fansub.ts
    - frontend/src/lib/api.ts
    - frontend/src/app/admin/fansubs/create/page.tsx
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
    - .planning/phases/29-fansub-group-model-normalization-and-generic-links/29-RESEARCH.md
  </read_first>
  <action>Update `frontend/src/types/fansub.ts`, `frontend/src/lib/api.ts`, `frontend/src/app/admin/fansubs/create/page.tsx`, and `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` so community links are edited as a collection of rows with `link_type`, optional `name`, and `url`. Support exactly the DB-defined types `website`, `discord`, `twitter`, `github`, and `irc`. Replace the fixed three-field block with add/remove row controls, preserve absolute-URL validation, and add typed API helpers for listing, creating, patching, and deleting generic fansub links. Keep the UI copy honest: this is a generic community-links editor, not just three special URL slots.</action>
  <acceptance_criteria>
    - `Select-String -Path frontend/src/app/admin/fansubs/create/page.tsx -Pattern "twitter|github|link_type|links"` returns matches.
    - `Select-String -Path frontend/src/app/admin/fansubs/[id]/edit/page.tsx -Pattern "twitter|github|link_type|links"` returns matches.
    - `Select-String -Path frontend/src/lib/api.ts -Pattern "links|link_type|fansub"` returns matches for generic fansub-link API helpers.
    - `cd frontend && npm.cmd run build` passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add explicit collaboration-member management to the fansub edit page</name>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
    - frontend/src/lib/api.ts
    - frontend/src/types/fansub.ts
    - backend/internal/handlers/fansub_collaborations.go
    - backend/internal/repository/fansub_repository.go
    - .planning/phases/29-fansub-group-model-normalization-and-generic-links/29-CONTEXT.md
  </read_first>
  <action>Add an edit-page section that appears for `group_type='collaboration'` and lists current member groups, supports adding one existing member group, and removing one member group through the existing collaboration endpoints. Use the existing collaboration helpers in `frontend/src/lib/api.ts`, but make the read helper auth-capable if the page flow needs it. Reuse existing fansub list/search helpers for the picker and exclude the current collaboration record plus collaboration-type rows from candidate selection so the UI only offers real member groups. Keep this workflow explicit and separate from the ordinary group profile form so real-group management and collaboration wiring do not blur together.</action>
  <acceptance_criteria>
    - `Select-String -Path frontend/src/app/admin/fansubs/[id]/edit/page.tsx -Pattern "collaboration|member_group|group_type"` returns matches.
    - `Select-String -Path backend/internal/handlers/fansub_collaborations.go -Pattern "ListCollaborationMembers|AddCollaborationMember|RemoveCollaborationMember"` returns matches.
    - `cd frontend && npm.cmd run build` passes.
    - `cd backend && go test ./internal/handlers ./internal/repository -count=1` passes.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Build the frontend, run targeted backend tests for collaboration handlers/repository seams, and manually confirm that one collaboration fansub can show, add, and remove member groups while generic link rows round-trip through save/reload.
</verification>

<success_criteria>
Fansub admin create/edit now manages generic links and explicit collaboration membership through first-class UI flows.
</success_criteria>

<output>
After completion, create `.planning/phases/29-fansub-group-model-normalization-and-generic-links/29-02-SUMMARY.md`
</output>
