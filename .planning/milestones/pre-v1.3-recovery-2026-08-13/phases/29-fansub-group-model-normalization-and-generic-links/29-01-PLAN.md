---
phase: 29-fansub-group-model-normalization-and-generic-links
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/db-schema-v2.md
  - database/migrations/0054_fansub_group_links_contract.up.sql
  - database/migrations/0054_fansub_group_links_contract.down.sql
  - backend/cmd/server/admin_routes.go
  - backend/internal/models/fansub.go
  - backend/internal/handlers/fansub_requests.go
  - backend/internal/handlers/fansub_groups.go
  - backend/internal/repository/fansub_repository.go
  - frontend/src/types/fansub.ts
autonomous: true
requirements:
  - P29-SC1
  - P29-SC2
must_haves:
  truths:
    - "Fansub group profile writes run against one canonical group model and do not introduce new product dependencies on `closed_year`, `history_description`, or alias-side `group_id`."
    - "Generic community links are managed through `fansub_group_links` with explicit `link_type` values instead of expanding fixed per-column link semantics."
    - "Transition compatibility may remain readable, but the long-term write contract for links is `fansub_group_links`."
---

<objective>
Lock the backend and schema contract for canonical fansub groups plus generic links before the admin UI is changed.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-fansub-group-model-normalization-and-generic-links/29-CONTEXT.md
@.planning/phases/29-fansub-group-model-normalization-and-generic-links/29-RESEARCH.md
@docs/architecture/db-schema-v2.md
@backend/cmd/server/admin_routes.go
@backend/internal/models/fansub.go
@backend/internal/handlers/fansub_requests.go
@backend/internal/handlers/fansub_groups.go
@backend/internal/repository/fansub_repository.go
@frontend/src/types/fansub.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the canonical fansub-group contract and compatibility boundary</name>
  <read_first>
    - docs/architecture/db-schema-v2.md
    - .planning/phases/29-fansub-group-model-normalization-and-generic-links/29-CONTEXT.md
    - .planning/phases/29-fansub-group-model-normalization-and-generic-links/29-RESEARCH.md
    - backend/internal/models/fansub.go
    - frontend/src/types/fansub.ts
  </read_first>
  <action>Update `docs/architecture/db-schema-v2.md`, `backend/internal/models/fansub.go`, and `frontend/src/types/fansub.ts` so the canonical fansub-group contract includes the profile core (`slug`, `name`, `description`, `history`, `status`, `group_type`, `country`, `founded_year`, `dissolved_year`, media IDs/URLs), plus a generic `links` collection shaped around `fansub_group_links`. Add explicit DTOs such as `FansubGroupLink`, `FansubGroupLinkCreateInput`, and `FansubGroupLinkPatchInput`. Document and model `closed_year`, `history_description`, and alias-side `group_id` as compatibility/cleanup fields rather than current product truth. Keep fixed `website_url` / `discord_url` / `irc_url` readable only if existing callers still need transition compatibility, but do not add any new product write paths on those fields.</action>
  <acceptance_criteria>
    - `Select-String -Path docs/architecture/db-schema-v2.md -Pattern "fansub_group_links|closed_year|history_description|compatibility"` returns matches.
    - `Select-String -Path backend/internal/models/fansub.go -Pattern "FansubGroupLink|Links"` returns matches.
    - `Select-String -Path frontend/src/types/fansub.ts -Pattern "FansubGroupLink|links"` returns matches.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add backend generic-link CRUD on top of fansub_group_links</name>
  <read_first>
    - backend/cmd/server/admin_routes.go
    - backend/internal/handlers/fansub_requests.go
    - backend/internal/handlers/fansub_groups.go
    - backend/internal/repository/fansub_repository.go
    - .planning/phases/29-fansub-group-model-normalization-and-generic-links/29-RESEARCH.md
    - database/migrations/0044_add_db_schema_v2_target_tables.up.sql
  </read_first>
  <action>Create an additive migration and backend contract for generic fansub links. Register concrete admin routes in `backend/cmd/server/admin_routes.go` for list/create/update/delete link operations under the fansub admin seam, for example `/admin/fansubs/:id/links` plus `/admin/fansubs/:id/links/:linkId`. Add request DTOs in `fansub_requests.go`, handler methods in `fansub_groups.go`, and repository CRUD functions in `fansub_repository.go` that work directly against `fansub_group_links` using `group_id`, `link_type`, optional `name`, and `url`. If compatibility projection is needed, map the first `website`, `discord`, and `irc` rows back onto legacy response fields for old callers, but keep the primary write path on the generic table. Enforce the DB-defined link types `website`, `discord`, `twitter`, `github`, and `irc` exactly.</action>
  <acceptance_criteria>
    - `Select-String -Path backend/cmd/server/admin_routes.go -Pattern "fansubs/:id/links"` returns matches.
    - `Select-String -Path backend/internal/repository/fansub_repository.go -Pattern "fansub_group_links|link_type|INSERT INTO|UPDATE|DELETE"` returns matches.
    - `Select-String -Path backend/internal/handlers/fansub_requests.go -Pattern "link_type|url|group_id|FansubGroupLink"` returns matches.
    - `Select-String -Path database/migrations/0054_fansub_group_links_contract.up.sql -Pattern "fansub_group_links|website|discord|twitter|github|irc"` returns matches.
    - `cd backend && go test ./internal/handlers ./internal/repository -count=1` passes.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Confirm the canonical data contract is documented and typed, then prove generic link CRUD uses `fansub_group_links` directly while compatibility-only fields remain transitional.
</verification>

<success_criteria>
The backend and schema expose one canonical fansub-group profile plus generic link CRUD without deepening legacy duplicate-field dependence.
</success_criteria>

<output>
After completion, create `.planning/phases/29-fansub-group-model-normalization-and-generic-links/29-01-SUMMARY.md`
</output>
