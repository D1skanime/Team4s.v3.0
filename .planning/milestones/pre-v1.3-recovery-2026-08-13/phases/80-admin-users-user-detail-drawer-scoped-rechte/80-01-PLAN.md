---
phase: 80-admin-users-user-detail-drawer-scoped-rechte
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/models/admin_users.go
  - backend/internal/repository/authz.go
  - frontend/src/types/admin-users.ts
  - frontend/src/components/auth/PlatformAdminGate.tsx
autonomous: true
requirements:
  - Entscheidung I
  - Entscheidung K
  - Entscheidung H

must_haves:
  truths:
    - "Backend-DTOs AdminUserListItem, AdminUserOverview, AdminUserGlobalRoles, AdminUserMemberClaims, AdminUserGroupMemberships, AdminUserGroupRights, AdminUserContributions, AdminUserMedia, AdminUserAudit sind als Go-Structs definiert und exportiert"
    - "RevokeAppUserGlobalRole und CountActivePlatformAdmins existieren in authz.go mit Last-Admin-Guard-Logik"
    - "frontend/src/types/admin-users.ts exportiert alle korrespondierenden TypeScript-Interfaces ohne Duplikation aus auth.ts"
    - "PlatformAdminGate prüft hasAccessToken || hasRefreshToken statt nur hasAccessToken"
    - "Kein natives HTML-Element in PlatformAdminGate (UI-Primitive-Pflicht)"
    - "Kein new external package"
  artifacts:
    - path: backend/internal/models/admin_users.go
      provides: "Go-DTOs für alle Phase-80-Endpunkte"
      exports:
        - AdminUserListItem
        - AdminUserListParams
        - AdminUserListResult
        - AdminUserOverview
        - AdminUserGlobalRolesResult
        - AdminUserMemberClaimsResult
        - AdminUserGroupMembershipsResult
        - AdminUserGroupRightsResult
        - AdminUserContributionsResult
        - AdminUserMediaResult
        - AdminUserAuditResult
    - path: backend/internal/repository/authz.go
      provides: "RevokeAppUserGlobalRole + CountActivePlatformAdmins (Last-Admin-Guard)"
      contains: "RevokeAppUserGlobalRole"
    - path: frontend/src/types/admin-users.ts
      provides: "TypeScript-Interfaces für alle Phase-80-Responses"
      exports:
        - AdminUserListItem
        - AdminUserListParams
        - AdminUserListResponse
        - AdminUserOverviewResponse
        - AdminUserGlobalRolesResponse
        - AdminUserContributionItem
        - AdminUserContributionsResponse
  key_links:
    - from: frontend/src/types/admin-users.ts
      to: backend/internal/models/admin_users.go
      via: "Jedes json-Tag im Go-Struct hat ein korrespondierendes TS-Interface-Feld"
      pattern: "AdminUserListItem"
    - from: backend/internal/repository/authz.go
      to: app_user_global_roles
      via: "DELETE WHERE app_user_id=$1 AND role=$2"
      pattern: "RevokeAppUserGlobalRole"
---

<objective>
Typ-Fundament und Kleinfix für Phase 80.

Erstellt alle Go-DTOs in einem neuen Model-File, ergänzt authz.go um
RevokeAppUserGlobalRole und CountActivePlatformAdmins (Last-Admin-Guard), definiert
die korrespondierenden TypeScript-Interfaces in admin-users.ts und behebt den
PlatformAdminGate-Bug (hasRefreshToken fehlte).

Purpose: Dieses Fundament muss vor allen anderen Plänen stehen, weil Repository,
Handler und Frontend-Komponenten von den Typ-Definitionen abhängen. Die Interfaces
dienen als Kontrakt zwischen Wave-0-Tests (Plan 80-02) und der Implementierung (Plan 80-03).

Output: 4 Dateien — 2 geändert (authz.go, PlatformAdminGate.tsx), 2 neu
(admin_users.go, admin-users.ts).
</objective>

<execution_context>
@C:\Users\admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\admin\Documents\Team4s\.planning\ROADMAP.md
@C:\Users\admin\Documents\Team4s\.planning\phases\80-admin-users-user-detail-drawer-scoped-rechte\80-CONTEXT.md
@C:\Users\admin\Documents\Team4s\.planning\phases\80-admin-users-user-detail-drawer-scoped-rechte\80-RESEARCH.md
@C:\Users\admin\Documents\Team4s\.planning\phases\80-admin-users-user-detail-drawer-scoped-rechte\80-PATTERNS.md

<interfaces>
<!-- Aus backend/internal/models/app_auth.go (Basis-Pattern für neue DTOs): -->
<!--
type AppUser struct {
    ID          int64      `json:"id"`
    Email       string     `json:"email"`
    DisplayName string     `json:"display_name"`
    Status      string     `json:"status"`
    LastLoginAt *time.Time `json:"last_login_at,omitempty"`
    CreatedAt   time.Time  `json:"created_at"`
    UpdatedAt   time.Time  `json:"updated_at"`
    GlobalRoles []string   `json:"global_roles,omitempty"`
}
const AppUserStatusActive   = "active"
const AppUserStatusDisabled = "disabled"
const AppUserStatusPending  = "pending"
const AppGlobalRolePlatformAdmin = "platform_admin"
const AppGlobalRoleContentAdmin  = "content_admin"
const AppGlobalRoleUser          = "user"
-->

<!-- Aus backend/internal/repository/authz.go (Basis-Pattern für neue Methoden): -->
<!--
func (r *AuthzRepository) AssignAppUserGlobalRole(ctx context.Context, appUserID int64, roleName string) error {
    // INSERT INTO app_user_global_roles (app_user_id, role) VALUES ($1, $2) ON CONFLICT DO NOTHING
}
func (r *AuthzRepository) AppUserHasGlobalRole(ctx context.Context, appUserID int64, roleName string) (bool, error) {
    // SELECT EXISTS(SELECT 1 FROM app_user_global_roles WHERE app_user_id=$1 AND role=$2)
}
-->

<!-- Aus frontend/src/types/auth.ts (Import-Basis): -->
<!--
export interface AppUser {
  id: number
  email: string
  display_name: string
  status: string
  global_roles: string[]
  last_login_at: string | null
  created_at: string
  updated_at: string
}
-->

<!-- Aus frontend/src/components/auth/PlatformAdminGate.tsx (zu fixende Zeile): -->
<!--
const { hasAccessToken, isClientInitialized } = useAuthSession();
// Zeile mit hasAccessToken-only-Check:
if (!hasAccessToken) { setIsLoading(false); ... return }
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Go-DTOs für alle Phase-80-Endpunkte definieren</name>
  <files>backend/internal/models/admin_users.go</files>
  <read_first>
    backend/internal/models/app_auth.go (Basis-Struct AppUser, Status-Konstanten, Rollen-Konstanten — alle wiederverwenden, nicht duplizieren)
    backend/internal/models/admin_release_theme_assets.go (Zeile 51: HasOverride bool — Muster für Conflict-Flag)
    .planning/phases/80-admin-users-user-detail-drawer-scoped-rechte/80-PATTERNS.md (Abschnitt admin_users.go)
    .planning/phases/80-admin-users-user-detail-drawer-scoped-rechte/80-RESEARCH.md (Endpoint-Empfehlungen, Abschnitt Recommended Project Structure)
  </read_first>
  <behavior>
    - AdminUserListItem enthält alle D-05-Aggregate (member_profile_id, member_profile_name, group_membership_count, leader_context_count, open_claims_count, open_contributions_count, total_contributions_count, media_upload_count, release_scope_count, conflict_count, last_activity_at string nullable) plus AppUser-Basisfelder (id, email, display_name, status, global_roles)
    - AdminUserListParams: q, status, global_role string; has_conflicts bool; sort string; limit, offset int
    - AdminUserListResult: Data []AdminUserListItem; Meta struct { Total, Limit, Offset int }
    - AdminUserOverview: Alle Summary-Counts plus ConflictDetails []AdminConflict
    - AdminConflict: Type string (D-17/D-18-Typen), Message string
    - AdminUserGlobalRolesResult: Roles []string; AssignableRoles []string
    - AdminUserMemberClaimsResult: MemberProfile *AdminMemberProfileSummary; Claims []AdminClaimSummary
    - AdminUserGroupMembershipsResult: Memberships []AdminGroupMembershipSummary
    - AdminUserGroupRightsResult: GroupRights []AdminGroupRightsSummary
    - AdminUserContributionsResult: ProjectDefaults []AdminContributionItem; ReleaseOverrides []AdminContributionItem; OpenDisputes []AdminContributionItem; LegacyHistorical []AdminContributionItem
    - AdminContributionItem: ContributionID int64; FansubGroupID int64; FansubGroupName string; AnimeID int64; AnimeTitle string; ReleaseVersionID *int64; ContributionType string ("project_default"/"release_override"); DisputeState string; RoleCodes []string
    - AdminUserMediaResult: MediaItems []AdminMediaItemSummary
    - AdminUserAuditResult: Entries []AdminAuditEntry
    - Alle Status-Konstanten aus app_auth.go importieren, nicht kopieren (gleiche package models)
  </behavior>
  <action>
    Neue Datei package models. Importiert "time". Definiert alle oben genannten Structs mit json-Tags.
    Conflict-Typen als String-Konstanten: AdminConflictTypeOpenClaim = "open_claim_with_profile",
    AdminConflictTypeMemberWithoutRole = "member_without_role",
    AdminConflictTypeMediaWithoutScope = "media_without_scope",
    AdminConflictTypeOpenDispute = "open_dispute",
    AdminConflictTypeInvalidReleaseOverride = "invalid_release_override",
    AdminConflictTypeOverrideContradiction = "override_contradiction",
    AdminConflictTypeMediaWithoutContributionRights = "media_without_contribution_rights".
    Kein Code für AppUser oder Status-Konstanten kopieren — aus gleichem package models direkt nutzbar.
    Datei darf 450 Zeilen nicht überschreiten; falls nötig, schwere Sub-DTOs in admin_users_detail.go auslagern.
  </action>
  <verify>
    <automated>cd C:\Users\admin\Documents\Team4s\backend && go build ./internal/models/... 2>&1</automated>
  </verify>
  <done>
    go build ./internal/models/... ist fehlerfrei.
    grep -c "AdminUserListItem" backend/internal/models/admin_users.go gibt >= 1.
    grep -c "AdminConflictType" backend/internal/models/admin_users.go gibt >= 5.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RevokeAppUserGlobalRole + CountActivePlatformAdmins in authz.go</name>
  <files>backend/internal/repository/authz.go</files>
  <read_first>
    backend/internal/repository/authz.go (vollständige Datei lesen: AssignAppUserGlobalRole-Pattern Zeilen ~138-157, AppUserHasGlobalRole, Struct AuthzRepository)
    .planning/phases/80-admin-users-user-detail-drawer-scoped-rechte/80-PATTERNS.md (Abschnitt authz.go)
    database/migrations/0072_keycloak_app_users_foundation.up.sql (PK-Schema app_user_global_roles)
  </read_first>
  <behavior>
    - RevokeAppUserGlobalRole(ctx, appUserID int64, roleName string) error: validiert appUserID > 0 und roleName != "", führt DELETE FROM app_user_global_roles WHERE app_user_id=$1 AND role=$2 aus; fehlendes Ergebnis ist kein Fehler (idempotent)
    - CountActivePlatformAdmins(ctx) (int, error): SELECT COUNT(*) FROM app_user_global_roles agr JOIN app_users au ON au.id=agr.app_user_id WHERE agr.role='platform_admin' AND au.status='active'
    - Kein Last-Admin-Guard in Repository (Guard-Logik gehört in den Handler)
  </behavior>
  <action>
    Neue Methoden am Ende der Datei anhängen, nachdem AssignAppUserGlobalRole. Pattern aus PATTERNS.md Abschnitt authz.go 1:1 übernehmen.
    ImportBlock nicht verändern (context, fmt, strings bereits vorhanden).
    Datei-Zeilenzahl nach Änderung prüfen: maximal 450 Zeilen. Falls > 450, neue Datei authz_revoke.go anlegen (gleiche package repository, keine neue Imports nötig außer context/fmt/strings).
  </action>
  <verify>
    <automated>cd C:\Users\admin\Documents\Team4s\backend && go build ./internal/repository/... 2>&1</automated>
  </verify>
  <done>
    go build ./internal/repository/... fehlerfrei.
    grep -c "RevokeAppUserGlobalRole" backend/internal/repository/authz.go gibt >= 1.
    grep -c "CountActivePlatformAdmins" backend/internal/repository/authz.go gibt >= 1.
  </done>
</task>

<task type="auto">
  <name>Task 3: TypeScript-Interfaces admin-users.ts + PlatformAdminGate-Bugfix</name>
  <files>
    frontend/src/types/admin-users.ts
    frontend/src/components/auth/PlatformAdminGate.tsx
  </files>
  <read_first>
    frontend/src/types/auth.ts (AppUser-Interface — importieren, nicht duplizieren)
    frontend/src/components/auth/PlatformAdminGate.tsx (vollständige Datei lesen, hasAccessToken-Verwendung identifizieren)
    .planning/phases/80-admin-users-user-detail-drawer-scoped-rechte/80-PATTERNS.md (Abschnitte admin-users.ts und PlatformAdminGate.tsx)
    .planning/phases/80-admin-users-user-detail-drawer-scoped-rechte/80-UI-SPEC.md (Tabellenspaltendefinition — alle Felder müssen im Interface vertreten sein)
  </read_first>
  <action>
    admin-users.ts NEU: Exportiert AdminUserListItem (alle D-05-Felder), AdminUserListParams, AdminUserListResponse (data: AdminUserListItem[], meta: {total, limit, offset}), AdminUserOverviewResponse (summary_counts, conflict_details: AdminConflictDetail[]), AdminConflictDetail (type: string, message: string), AdminUserGlobalRolesResponse (roles: string[], assignable_roles: string[]), AdminUserMemberClaimsResponse (member_profile: AdminMemberProfileSummary | null, claims: AdminClaimSummary[]), AdminUserGroupMembershipsResponse (memberships: AdminGroupMembershipSummary[]), AdminUserGroupRightsResponse (group_rights: AdminGroupRightsSummary[]), AdminUserContributionsResponse (project_defaults, release_overrides, open_disputes, legacy_historical — je AdminContributionItem[]), AdminUserMediaResponse (media_items: AdminMediaItemSummary[]), AdminUserAuditResponse (entries: AdminAuditEntry[]).
    Jedes json-Tag aus den Go-DTOs (Task 1) hat ein korrespondierendes TS-Feld mit gleichem Namen.
    AdminContributionItem: contribution_id, fansub_group_id, fansub_group_name, anime_id, anime_title, release_version_id: number|null, contribution_type: 'project_default'|'release_override', dispute_state: string, role_codes: string[].
    Kein natives HTML in dieser Datei (nur Interfaces).

    PlatformAdminGate.tsx BUGFIX (per RESEARCH.md Pitfall 5):
    - useAuthSession-Destructuring: hasRefreshToken zusätzlich zu hasAccessToken extrahieren
    - Jede Stelle mit if (!hasAccessToken) auf if (!hasAccessToken && !hasRefreshToken) umstellen
    - Kein weiterer inhaltlicher Eingriff in die Datei
  </action>
  <verify>
    <automated>cd C:\Users\admin\Documents\Team4s\frontend && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>
    npm run typecheck gibt 0 Fehler.
    grep -c "AdminUserListItem" frontend/src/types/admin-users.ts gibt >= 1.
    grep -c "hasRefreshToken" frontend/src/components/auth/PlatformAdminGate.tsx gibt >= 1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Grenze | Beschreibung |
|--------|-------------|
| Typ-Definitions-Konsistenz | Go-Struct-json-Tags müssen mit TS-Interface-Feldern übereinstimmen; Drift führt zu Runtime-Deserialisierungsfehlern |
| PlatformAdminGate Session-Boundary | Refresh-Token-only-Session darf nicht als ausgeloggt behandelt werden |

## STRIDE Threat Register

| Threat ID | Kategorie | Komponente | Disposition | Mitigationsplan |
|-----------|-----------|------------|-------------|-----------------|
| T-80-01-01 | Tampering | admin_users.go — Conflict-Type-Konstanten | mitigate | Nur bekannte Conflict-Types als String-Konstanten; Handler validiert Type-Wert vor DB-Query (Phase 80-03) |
| T-80-01-02 | Spoofing | PlatformAdminGate.tsx — hasRefreshToken | mitigate | Gate prüft hasAccessToken \|\| hasRefreshToken; Backend bleibt autoritär über requirePlatformAdminIdentity |
| T-80-01-SC | Tampering | npm/pip/cargo Installs | accept | Keine neuen externen Pakete in diesem Plan — kein Slopcheck nötig |
</threat_model>

<verification>
Nach Plan 80-01:
- go build ./internal/models/... und go build ./internal/repository/... sind fehlerfrei
- npm run typecheck ist fehlerfrei
- grep -c "AdminUserListItem" backend/internal/models/admin_users.go gibt >= 1
- grep -c "RevokeAppUserGlobalRole" backend/internal/repository/authz.go gibt >= 1
- grep -c "hasRefreshToken" frontend/src/components/auth/PlatformAdminGate.tsx gibt >= 1
- Kein Duplicate: AppUser-Struct/Status-Konstanten tauchen NICHT in admin_users.go auf (gleiche package)
</verification>

<success_criteria>
Go-DTOs, TypeScript-Interfaces und der PlatformAdminGate-Bugfix sind im Codebase
und kompilieren/typechecken ohne Fehler. Plan 80-02 (Wave-0-Tests) und Plan 80-03
(Backend-Implementierung) können auf diesen Typen aufsetzen ohne eigene Typ-Definitionen
zu erfinden.
</success_criteria>

<output>
Erstelle `.planning/phases/80-admin-users-user-detail-drawer-scoped-rechte/80-01-SUMMARY.md` wenn fertig.
</output>
