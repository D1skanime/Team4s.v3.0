---
phase: 94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma
plan: 02
type: execute
wave: 2
depends_on: ["94-01"]
files_modified:
  - backend/internal/handlers/admin_capability_handler.go
  - backend/internal/repository/authz_capability_mutations.go
autonomous: true
requirements: [AC-6, AC-7, D-03, D-04, D-05]
nyquist_compliant: true

must_haves:
  truths:
    - "Grant an einer historischen/nicht-assignable Rolle wird serverseitig mit HTTP 422 role_not_assignable blockiert (D-05)"
    - "Revoke an einer historischen/nicht-assignable Rolle wird serverseitig mit HTTP 422 role_not_assignable blockiert (D-05)"
    - "Die Capability-Matrix-Response enthält pro Rolle assignable (true für App-Rollen, false für historische) und contexts (D-04)"
    - "Die Assignable-Wahrheitsquelle ist permissions.IsKnownFansubGroupRole — keine neue Spalte/Migration (D-03)"
  artifacts:
    - path: "backend/internal/handlers/admin_capability_handler.go"
      provides: "Assignable-Guard in GrantCapability und RevokeCapability + Matrix-assignable-Anreicherung"
      contains: "role_not_assignable"
    - path: "backend/internal/repository/authz_capability_mutations.go"
      provides: "CapabilityMatrixRoleEntry um Assignable/Contexts erweitert + contexts in Query"
      contains: "Contexts"
  key_links:
    - from: "admin_capability_handler.go"
      to: "permissions.IsKnownFansubGroupRole"
      via: "Guard vor DB-Mutation in beiden Pfaden + Anreicherung der Matrix"
      pattern: "IsKnownFansubGroupRole"
    - from: "authz_capability_mutations.go ListCapabilityMatrix"
      to: "role_definitions.contexts"
      via: "rd.contexts im SELECT/Scan"
      pattern: "contexts"
---

<objective>
Den serverseitigen Assignable-Guard (D-05) und die Assignability-/Kontext-Metadaten (D-04) im Backend
implementieren, sodass die in Plan 01 fixierten Tests grün werden. Grant UND Revoke blockieren jede
Mutation an rein historischen (nicht-assignable) Rollen mit HTTP 422 role_not_assignable. Die
Capability-Matrix liefert pro Rolle assignable + contexts, damit das Frontend (Plan 06) Badges/Disabled
ableiten kann. Wahrheitsquelle ist der vorhandene Katalog (permissions.IsKnownFansubGroupRole) — kein
Schema-Change (D-03).

Purpose: Schließt die zentrale Privilege-Escalation-Lücke (T-94-01): historische Rollen dürfen keine
App-Capabilities erhalten. Macht die Plan-01-Tests grün.
Output: Guard in beiden Mutationspfaden, angereichertes Matrix-DTO.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-CONTEXT.md
@.planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-RESEARCH.md
@.planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-PATTERNS.md

<interfaces>
backend/internal/handlers/admin_capability_handler.go:
  func (h *AdminCapabilityHandler) GrantCapability(c *gin.Context)   // roleCode/actionCode aus c.Param; Mutation Z.85
  func (h *AdminCapabilityHandler) RevokeCapability(c *gin.Context)  // Lockout-Guard Z.138 (Status 409, code lockout_guard)
  func (h *AdminCapabilityHandler) ListCapabilityMatrix(c *gin.Context) // matrix, _ := h.authzRepo.ListCapabilityMatrix(...); c.JSON(200, matrix) Z.56-63
  // permissions ist bereits importiert (Z.7); permissions.IsKnownFansubGroupRole verfügbar

backend/internal/repository/authz_capability_mutations.go:
  type CapabilityMatrixRoleEntry struct { RoleCode string; LabelDE string; Actions []CapabilityMatrixActionState }  // Z.30
  // ListCapabilityMatrix-Query: CROSS JOIN role_definitions rd; SELECT rd.code, rd.label_de ... (Z.59-74)
  // Kommentar Z.52: "Repository darf nicht vom permissions-Paket abhängen"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CapabilityMatrixRoleEntry um Assignable/Contexts erweitern + contexts in Query</name>
  <files>backend/internal/repository/authz_capability_mutations.go</files>
  <read_first>
    - backend/internal/repository/authz_capability_mutations.go (CapabilityMatrixRoleEntry Z.30, ListCapabilityMatrix-Query Z.58-74, Scan-/Aggregations-Block Z.89-142, Kommentar Z.52 "darf nicht von permissions abhängen")
    - .planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-PATTERNS.md (Abschnitt authz_capability_mutations.go)
  </read_first>
  <action>
    Erweitere CapabilityMatrixRoleEntry additiv um ein Feld Assignable bool mit JSON-Tag "assignable" und
    ein Feld Contexts vom Typ []string mit JSON-Tag "contexts,omitempty". Erweitere die
    ListCapabilityMatrix-Query um rd.contexts im SELECT; scanne rd.contexts (TEXT[]) in ein []string (pgx
    unterstützt []string-Scan für TEXT[]) und reiche es in einer roleContexts-Map (analog zur vorhandenen
    roleLabels-Map) durch; setze Contexts beim Aufbau der roles-Liste (Z.135-142). Assignable im Repository
    NICHT setzen/berechnen — das Repo darf nicht von permissions abhängen (Kommentar Z.52); Assignable
    bleibt im Repo der Go-Zero-Wert false und wird im Handler (Task 2) angereichert. Reihenfolge und
    Bestandsverhalten der Matrix bleiben unverändert.
  </action>
  <verify>
    <automated>cd backend && go build ./internal/repository/ && go test ./internal/repository/ -run CapabilityMatrix</automated>
  </verify>
  <acceptance_criteria>
    - Quelle: CapabilityMatrixRoleEntry enthält Felder Assignable (json "assignable") und Contexts (json "contexts,omitempty").
    - Quelle: ListCapabilityMatrix-Query enthält rd.contexts im SELECT und scannt es in die Rolle.
    - Quelle: authz_capability_mutations.go importiert NICHT das permissions-Paket; Assignable wird hier nicht gesetzt.
    - Behavior: `cd backend && go build ./internal/repository/` exit 0.
  </acceptance_criteria>
  <done>Matrix-DTO um Assignable/Contexts erweitert, contexts wird aus role_definitions geladen, Repo bleibt permissions-frei.</done>
</task>

<task type="auto">
  <name>Task 2: Assignable-Guard (Grant+Revoke 422) + Matrix-assignable-Anreicherung im Handler</name>
  <files>backend/internal/handlers/admin_capability_handler.go</files>
  <read_first>
    - backend/internal/handlers/admin_capability_handler.go (GrantCapability Z.71-108, RevokeCapability Z.115-171, Lockout-Guard-Muster Z.138-146, ListCapabilityMatrix Z.50-64)
    - backend/internal/handlers/admin_capability_handler_test.go (die in Plan 01 erstellten AssignableGuard-/AssignableEnrichment-Tests als Zielspezifikation)
    - .planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-PATTERNS.md (Abschnitt admin_capability_handler.go — exakter Guard-Block)
  </read_first>
  <action>
    (1) Guard in GrantCapability: direkt nach der roleCode/actionCode-Leerprüfung (nach Z.83), VOR der
        DB-Mutation, einfügen: wenn nicht permissions.IsKnownFansubGroupRole(roleCode), dann
        c.JSON mit http.StatusUnprocessableEntity und gin.H{"error": gin.H{"code":"role_not_assignable",
        "message":"Diese Rolle ist eine historische bzw. nicht-zuweisbare Rolle und kann keine
        Berechtigungen erhalten."}} senden und return. Umlaute korrekt.
    (2) Guard in RevokeCapability: an der analogen Stelle (nach der Leerprüfung Z.127, VOR dem Lockout-Guard
        Z.130) denselben Block einfügen (Pitfall 4: Guard in BEIDEN Pfaden). Status 422, code
        role_not_assignable (nicht 409 — kollidiert mit lockout_guard).
    (3) Matrix-Anreicherung in ListCapabilityMatrix: nach dem erfolgreichen ListCapabilityMatrix-Aufruf,
        VOR c.JSON, über matrix.Roles iterieren und
        matrix.Roles[i].Assignable = permissions.IsKnownFansubGroupRole(matrix.Roles[i].RoleCode) setzen.
    permissions ist bereits importiert (Z.7) — kein neuer Import. Keine weiteren Verhaltensänderungen.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/handlers/ -run AssignableGuard && go test ./internal/handlers/ -run AssignableEnrichment</automated>
  </verify>
  <acceptance_criteria>
    - Quelle: admin_capability_handler.go enthält den role_not_assignable-Guard-Block in GrantCapability UND RevokeCapability, jeweils VOR der DB-Mutation.
    - Quelle: Beide Guard-Blöcke nutzen http.StatusUnprocessableEntity (422) und code "role_not_assignable".
    - Quelle: ListCapabilityMatrix setzt matrix.Roles[i].Assignable via permissions.IsKnownFansubGroupRole vor c.JSON.
    - Behavior: `cd backend && go test ./internal/handlers/ -run AssignableGuard` exit 0 (Plan-01-Tests grün).
    - Behavior: `cd backend && go test ./internal/handlers/ -run AssignableEnrichment` exit 0.
  </acceptance_criteria>
  <done>Guard in Grant und Revoke aktiv (422 role_not_assignable), Matrix liefert assignable korrekt, Plan-01-Tests grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin-Browser → Capability-Mutations-API | Untrusted roleCode als Pfadparameter; historischer roleCode darf keine Capability erhalten |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-01 | Elevation of Privilege | GrantCapability/RevokeCapability mit historischem role_code (founder/co_leader) | mitigate | permissions.IsKnownFansubGroupRole-Guard VOR DB-Mutation in beiden Pfaden → 422 role_not_assignable |
| T-94-03 | Information Disclosure | contexts-Feld leakt interne Rollen-Kontexte | accept | contexts stammt aus role_definitions.contexts, ist Platform-Admin-gated (requirePlatformAdminIdentity); keine sensiblen Daten |
| T-94-SC | Tampering | go get / npm install | accept | Phase fügt KEINE Pakete hinzu (RESEARCH) |
</threat_model>

<verification>
- `cd backend && go test ./internal/handlers/ -run AssignableGuard` exit 0.
- `cd backend && go test ./internal/handlers/ -run AssignableEnrichment` exit 0.
- `cd backend && go test ./internal/repository/ -run CapabilityMatrix` exit 0.
- `cd backend && go build ./...` exit 0.
- Nach Backend-Änderung: `docker compose up -d --build team4sv30-backend` für Live-Verfügbarkeit (Backend :8092).
- `git diff --check` sauber.
</verification>

<success_criteria>
- Guard blockiert Grant und Revoke an historischen Rollen mit 422 role_not_assignable.
- Matrix-Response trägt assignable + contexts pro Rolle.
- Keine neue Migration/Spalte (D-03 eingehalten); Repo bleibt permissions-frei.
</success_criteria>

<output>
Create `.planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-02-SUMMARY.md` when done
</output>
