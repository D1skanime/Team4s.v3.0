---
phase: 94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma
plan: 04
type: execute
wave: 2
depends_on: ["94-01"]
files_modified:
  - shared/contracts/admin-capabilities.yaml
  - frontend/src/types/admin-capability.ts
  - frontend/src/lib/api.ts
  - frontend/src/app/admin/role-capabilities/RoleCapabilityClient.test.tsx
autonomous: true
requirements: [AC-1, AC-3, AC-6, AC-11, D-04, D-06, D-07]
nyquist_compliant: true

must_haves:
  truths:
    - "admin-capabilities.yaml, admin-capability.ts, api.ts und die Test-Fixture sind synchron um assignable/contexts erweitert — keine stillen Breaking Changes (D-06, AC-11)"
    - "Die neuen Felder assignable/contexts sind additiv-optional (required-Liste NICHT erweitert) (D-06)"
    - "Es existiert ein api.ts-Helper listGroupHistoryRoleDefinitions(fansubId) für den group_history-Read (D-07)"
    - "Die Test-Fixture sampleMatrix enthält mindestens eine assignable=false-Rolle (D-04 Frontend-Vorbereitung)"
  artifacts:
    - path: "frontend/src/types/admin-capability.ts"
      provides: "RoleEntry um assignable?/contexts? erweitert + RoleDefinitionOption-Typ"
      contains: "assignable"
    - path: "shared/contracts/admin-capabilities.yaml"
      provides: "RoleEntry-Schema additiv um assignable/contexts + role_not_assignable-Fehlercode dokumentiert"
      contains: "assignable"
    - path: "frontend/src/lib/api.ts"
      provides: "Helper listGroupHistoryRoleDefinitions"
      contains: "listGroupHistoryRoleDefinitions"
  key_links:
    - from: "api.ts listGroupHistoryRoleDefinitions"
      to: "GET /admin/fansubs/:id/role-definitions"
      via: "fetch + data-Envelope-Unwrap (Muster listMemberRoles)"
      pattern: "role-definitions"
    - from: "admin-capability.ts RoleEntry"
      to: "admin-capabilities.yaml RoleEntry"
      via: "synchrone additive Felder assignable/contexts"
      pattern: "assignable"
---

<objective>
Den Contract-Synchron-Task (D-06, Pitfall 1) ausführen: die vier Artefakte für die API-Shape-Änderung
in EINEM Plan additiv und synchron erweitern — Contract (YAML), Frontend-Typ, api.ts-Helper und
Test-Fixture. Außerdem den Frontend-Konsum für den neuen group_history-Read-Endpunkt (Plan 03)
bereitstellen: neuer Typ RoleDefinitionOption + api.ts-Helper.

Purpose: Verhindert das Auseinanderdriften von Contract/Typen/api.ts/Tests (AC-11). Neue Felder
additiv-optional, damit Bestands-Consumer nicht brechen.
Output: Synchrone assignable/contexts-Felder, role_not_assignable-Fehlercode dokumentiert,
group_history-Helper + Typ, erweiterte Test-Fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-CONTEXT.md
@.planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-RESEARCH.md
@.planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-PATTERNS.md

<interfaces>
frontend/src/types/admin-capability.ts:
  export interface RoleEntry { role_code: string; label_de: string; actions: RoleActionState[] }  // Z.16
  export interface RoleCapabilityMatrix { roles: RoleEntry[]; all_actions: ActionEntry[] }

frontend/src/lib/api.ts:
  listRoleCapabilities()  // Z.9189 — Matrix generisch deserialisiert; parseApiErrorPayload Z.843; ApiError(status,msg,null,code,details) trägt code
  listMemberRoles(...)    // Z.7979 — GET, data-Envelope-Unwrap (Helper-Vorbild)

shared/contracts/admin-capabilities.yaml:
  RoleEntry:  // Schema ~Z.208 — required: [role_code, label_de, actions]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: assignable/contexts synchron in Contract + Frontend-Typ + Test-Fixture</name>
  <files>shared/contracts/admin-capabilities.yaml, frontend/src/types/admin-capability.ts, frontend/src/app/admin/role-capabilities/RoleCapabilityClient.test.tsx</files>
  <read_first>
    - shared/contracts/admin-capabilities.yaml (RoleEntry-Schema ~Z.208, required-Liste)
    - frontend/src/types/admin-capability.ts (RoleEntry Z.16, RoleActionState, RoleCapabilityMatrix)
    - frontend/src/app/admin/role-capabilities/RoleCapabilityClient.test.tsx (Fixture sampleMatrix Z.14-51, 409-Mock-Muster Z.82-85)
    - .planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-PATTERNS.md (Abschnitte admin-capabilities.yaml, admin-capability.ts, Shared Patterns "Contract+Typen+api.ts+Test synchron")
  </read_first>
  <action>
    (1) admin-capabilities.yaml: RoleEntry additiv erweitern um property assignable (type boolean, description
        "Ob die Rolle Capabilities erhalten darf (App-Gruppenrolle).") und contexts (type array, items type
        string, description "role_definitions.contexts (für Kontext-Badges)."). required-Liste NICHT erweitern
        (assignable/contexts bleiben optional → rückwärtskompatibel). Dokumentiere den Guard-Fehlercode
        role_not_assignable (HTTP 422) bei den Grant/Revoke-Endpunkten (responses 422 mit error.code
        role_not_assignable). Optional: ein Response-Schema für GET .../role-definitions (Liste aus
        {code, label_de, sort_order}).
    (2) admin-capability.ts: RoleEntry um assignable?: boolean und contexts?: string[] erweitern (optional).
        Neuen Typ RoleDefinitionOption { code: string; label_de: string; sort_order: number } exportieren.
    (3) RoleCapabilityClient.test.tsx: Fixture sampleMatrix so erweitern, dass mindestens eine Rolle
        assignable: false trägt (historische Rolle) und die übrigen assignable: true. Bestehende Tests
        müssen weiterhin grün bleiben (additive Felder). Noch KEINE neue 422-Verhaltensassertion hier — die
        kommt in Plan 06 mit dem UI-Umbau; in DIESEM Task nur die Fixture additiv anpassen, damit sie
        contract-konform ist.
    Alle user-facing/Beschreibungstexte mit korrekten Umlauten.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/admin/role-capabilities && npx tsc --noEmit 2>&1 | grep -E "admin-capability|RoleCapabilityClient" || echo "NO-TS-ERRORS-FOR-TOUCHED"</automated>
  </verify>
  <acceptance_criteria>
    - Quelle: admin-capabilities.yaml RoleEntry enthält assignable und contexts als properties; required-Liste unverändert (kein assignable/contexts darin).
    - Quelle: admin-capabilities.yaml dokumentiert role_not_assignable (422) bei Grant/Revoke.
    - Quelle: admin-capability.ts RoleEntry hat assignable?: boolean und contexts?: string[]; RoleDefinitionOption exportiert.
    - Quelle: RoleCapabilityClient.test.tsx-Fixture enthält mindestens eine assignable:false-Rolle.
    - Behavior: `cd frontend && npx vitest run src/app/admin/role-capabilities` exit 0 (Bestandstests bleiben grün).
  </acceptance_criteria>
  <done>Contract, Typ und Fixture synchron additiv erweitert; Fehlercode dokumentiert; Bestandstests grün.</done>
</task>

<task type="auto">
  <name>Task 2: api.ts-Helper listGroupHistoryRoleDefinitions</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts (listMemberRoles ~Z.7979 als GET-/data-Envelope-Vorbild; listRoleCapabilities ~Z.9189; ApiError/parseApiErrorPayload Z.843)
    - frontend/src/types/admin-capability.ts (RoleDefinitionOption — aus Task 1)
    - .planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-PATTERNS.md (Abschnitt api.ts)
  </read_first>
  <action>
    Füge in api.ts einen Helper listGroupHistoryRoleDefinitions(fansubId: number | string): Promise<RoleDefinitionOption[]>
    hinzu, analog zum listMemberRoles-Muster: GET auf /admin/fansubs/{fansubId}/role-definitions?context=group_history,
    zentrale Auth/API-Client-Seam verwenden (kein direkter fetch ohne den vorhandenen Client-Wrapper),
    data-Envelope auspacken und als RoleDefinitionOption[] typisieren. Fehlercode-Durchreichung über den
    bestehenden ApiError-Pfad (keine Sonderbehandlung nötig). Importiere RoleDefinitionOption aus
    @/types/admin-capability. Keine Änderung an listRoleCapabilities/grant/revoke nötig (Matrix wird generisch
    deserialisiert; assignable/contexts kommen additiv durch).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -E "api.ts|listGroupHistoryRoleDefinitions" || echo "NO-TS-ERRORS-FOR-API"</automated>
  </verify>
  <acceptance_criteria>
    - Quelle: api.ts enthält listGroupHistoryRoleDefinitions mit GET auf /admin/fansubs/{id}/role-definitions und data-Envelope-Unwrap.
    - Quelle: Rückgabetyp ist RoleDefinitionOption[] (importiert aus @/types/admin-capability).
    - Quelle: Helper nutzt die zentrale API-Client-Seam (kein roher fetch ohne Auth-Wrapper).
    - Behavior: `cd frontend && npx tsc --noEmit` meldet keine neuen Fehler in api.ts.
  </acceptance_criteria>
  <done>api.ts-Helper für group_history-Rollen vorhanden, typkonform, nutzt zentrale Client-Seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contract ↔ Runtime ↔ Frontend | Drift zwischen Contract/Typen/Client erzeugt stille Inkonsistenz (D-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-06 | Tampering | Stille Breaking Changes durch nicht-synchrone Contract-Erweiterung | mitigate | Vier Artefakte in EINEM Plan additiv-optional erweitern (required unverändert); Bestandstests als Regressionsgate |
| T-94-SC | Tampering | npm install | accept | Phase fügt KEINE Pakete hinzu (RESEARCH) |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/app/admin/role-capabilities` exit 0 (Bestandstests grün).
- `cd frontend && npx tsc --noEmit` ohne neue Fehler in den berührten Dateien.
- assignable/contexts additiv-optional (required-Liste in YAML unverändert).
- `git diff --check` sauber (AC-11).
</verification>

<success_criteria>
- Contract, Frontend-Typ, api.ts und Test-Fixture sind synchron und additiv erweitert.
- group_history-Helper + RoleDefinitionOption-Typ vorhanden.
- Keine Breaking Changes; Bestands-Consumer unverändert lauffähig.
</success_criteria>

<output>
Create `.planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-04-SUMMARY.md` when done
</output>
