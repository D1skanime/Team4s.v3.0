---
phase: 94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma
plan: 03
type: execute
wave: 2
depends_on: ["94-01"]
files_modified:
  - backend/internal/repository/hist_group_member_roles_repository.go
  - backend/internal/handlers/fansub_hist_group_member_roles_handler.go
  - backend/cmd/server/admin_routes.go
autonomous: true
requirements: [AC-1, AC-3, D-02, D-07]
nyquist_compliant: true

must_haves:
  truths:
    - "Ein neuer Read-Endpunkt liefert die kuratierte group_history-Rollenliste (Gründer/in, Gruppenleitung, Co-Leitung, Projektmanagement) (D-07, AC-1/3)"
    - "Die Liste stammt aus role_definitions WHERE 'group_history' = ANY(contexts) AND code IN Whitelist — NICHT aus FANSUB_GROUP_ROLE_OPTIONS (D-07)"
    - "Die Liste enthält keine aktiven App-Gruppenrollen (translator/encoder etc.) trotz Migration-0103-Drift (Pitfall 2)"
    - "Der Endpunkt ist mit CanForFansubGroup(MembersView) autorisiert (Auth-Gate)"
  artifacts:
    - path: "backend/internal/repository/hist_group_member_roles_repository.go"
      provides: "Repo-Methode ListGroupHistoryRoleDefinitions (kuratierte Whitelist)"
      contains: "group_history"
    - path: "backend/internal/handlers/fansub_hist_group_member_roles_handler.go"
      provides: "Read-Handler ListGroupHistoryRoleDefinitions mit CanForFansubGroup-Gate"
      contains: "ListGroupHistoryRoleDefinitions"
    - path: "backend/cmd/server/admin_routes.go"
      provides: "Route GET /admin/fansubs/:id/role-definitions"
      contains: "role-definitions"
  key_links:
    - from: "admin_routes.go"
      to: "histGroupMemberRolesHandler.ListGroupHistoryRoleDefinitions"
      via: "v1.GET-Registrierung im Fansub-Scope"
      pattern: "role-definitions"
    - from: "ListGroupHistoryRoleDefinitions handler"
      to: "CanForFansubGroup ActionFansubGroupMembersView"
      via: "Auth-Gate vor DB-Read"
      pattern: "ActionFansubGroupMembersView"
---

<objective>
Einen neuen schlanken Read-Endpunkt GET /api/v1/admin/fansubs/:id/role-definitions?context=group_history
bauen, der die fachlich kuratierte historische Rollenliste aus role_definitions liefert (D-07). DB ist
Single Source of Truth — keine driftende Frontend-Konstante. Wegen Migration-0103-Drift (alle App-Rollen
tragen zusätzlich group_history) wird die Liste über eine explizite Whitelist kuratiert
(founder, leader, co_leader, project_manager — Pitfall 2). Macht den Plan-01-Repo-Test grün.

Purpose: Liefert die korrekte Rollenquelle für den historischen Dialog (Plan 08), getrennt von den
aktiven App-Gruppenrollen. Auth-Gate analog ListHistGroupMemberRoles, da der Dialog im Fansub-Edit-Kontext lebt.
Output: Repo-Methode, Read-Handler, Route. (api.ts-Helper und Frontend-Typ liegen in Plan 04.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-CONTEXT.md
@.planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-RESEARCH.md
@.planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-PATTERNS.md

<interfaces>
backend/internal/repository/hist_group_member_roles_repository.go:
  type HistGroupMemberRolesRepository struct { ... }
  func (r *HistGroupMemberRolesRepository) RoleCodeExistsForContext(ctx, roleCode, contextName string) (bool, error)  // Z.231 — $N = ANY(contexts)-Mechanik

backend/internal/handlers/fansub_hist_group_member_roles_handler.go:
  type FansubHistGroupMemberRolesHandler struct { rolesRepo; permissionSvc *permissions.Service; auditLogRepo }
  func (h *FansubHistGroupMemberRolesHandler) ListHistGroupMemberRoles(c *gin.Context)  // Z.73 — Gate-Muster CanForFansubGroup + writePermissionDenied + c.JSON(200, gin.H{"data": items})
  // Helper: permissionActorFromContext(c), parseFansubID(c.Param("id")), permissions.ActionFansubGroupMembersView

backend/cmd/server/admin_routes.go:
  v1.GET("/admin/fansubs/:id/member-roles", auth, deps.histGroupMemberRolesHandler.ListHistGroupMemberRoles)  // Z.193 — Vorbild
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repo-Methode ListGroupHistoryRoleDefinitions (kuratierte Whitelist)</name>
  <files>backend/internal/repository/hist_group_member_roles_repository.go</files>
  <read_first>
    - backend/internal/repository/hist_group_member_roles_repository.go (RoleCodeExistsForContext Z.231 — Query-/Scan-Muster, $N = ANY(contexts))
    - backend/internal/repository/role_definitions_context_test.go (Plan-01-Test: erwartete Signatur und Whitelist-Disjunktheit zu FansubGroupRoles())
    - .planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-RESEARCH.md (Code Examples — group_history Read-Query; Pitfall 2)
    - database/migrations/0085_role_definitions_seed.up.sql (Seed der Hist-Rollen-Codes founder/leader/co_leader/project_manager)
  </read_first>
  <action>
    Füge eine Methode ListGroupHistoryRoleDefinitions(ctx context.Context) hinzu, die eine Liste von
    Strukturen mit den Feldern Code (json "code"), LabelDE (json "label_de"), SortOrder (json "sort_order")
    liefert. Definiere den Rückgabe-Typ (z.B. RoleDefinitionOption) im repository-Paket. Query kuratiert
    (Pitfall 2): SELECT code, label_de, sort_order FROM role_definitions WHERE 'group_history' = ANY(contexts)
    AND code = ANY($1) ORDER BY sort_order, code — mit $1 = der expliziten Whitelist
    founder, leader, co_leader, project_manager. Lege die Whitelist als benannte Paket-Variable an (z.B.
    groupHistoryDialogRoleWhitelist), damit der Plan-01-Test sie referenzieren kann. Kommentiere die
    Begründung der Whitelist (Migration 0103 markiert ALLE App-Rollen mit group_history, daher würde naives
    ANY(contexts) auch translator/encoder liefern). Keine permissions-Abhängigkeit im Repo nötig — die
    Whitelist ist eine reine Code-Konstante.
  </action>
  <verify>
    <automated>cd backend && go build ./internal/repository/ && go test ./internal/repository/ -run RoleDefinitionsContext</automated>
  </verify>
  <acceptance_criteria>
    - Quelle: hist_group_member_roles_repository.go enthält ListGroupHistoryRoleDefinitions mit der kuratierten Query (code = ANY($1)).
    - Quelle: Eine benannte Whitelist-Variable mit genau founder, leader, co_leader, project_manager existiert.
    - Quelle: Rückgabe-Typ trägt JSON-Tags code/label_de/sort_order.
    - Behavior: `cd backend && go test ./internal/repository/ -run RoleDefinitionsContext` exit 0 (Plan-01-Test grün).
  </acceptance_criteria>
  <done>Repo-Methode liefert kuratierte group_history-Liste; Plan-01-Test grün; keine App-Rollen enthalten.</done>
</task>

<task type="auto">
  <name>Task 2: Read-Handler ListGroupHistoryRoleDefinitions + Route-Registrierung</name>
  <files>backend/internal/handlers/fansub_hist_group_member_roles_handler.go, backend/cmd/server/admin_routes.go</files>
  <read_first>
    - backend/internal/handlers/fansub_hist_group_member_roles_handler.go (ListHistGroupMemberRoles Z.73-115 — vollständiges Gate-/Response-Muster: permissionActorFromContext, parseFansubID, CanForFansubGroup(ActionFansubGroupMembersView), auditPermissionDenied, writePermissionDenied, c.JSON(200, gin.H{"data": items}))
    - backend/cmd/server/admin_routes.go (Z.193 member-roles-Registrierung als exaktes Vorbild)
    - .planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-PATTERNS.md (Abschnitt Neuer Read-Endpunkt + admin_routes.go)
  </read_first>
  <action>
    (1) Handler: Füge ListGroupHistoryRoleDefinitions(c *gin.Context) zu FansubHistGroupMemberRolesHandler
        hinzu — exakt das Gate-Muster aus ListHistGroupMemberRoles übernehmen: identity/actor via
        permissionActorFromContext; fansubID via parseFansubID(c.Param("id")); CanForFansubGroup mit
        permissions.ActionFansubGroupMembersView; bei Fehler writePermissionInternalError; bei nicht erlaubt
        auditPermissionDenied (event z.B. "role_definitions.list.denied") + writePermissionDenied. Danach
        h.rolesRepo.ListGroupHistoryRoleDefinitions(c.Request.Context()) aufrufen; bei Fehler internalError;
        sonst c.JSON(http.StatusOK, gin.H{"data": items}). Optionalen Query-Param context (default
        "group_history") nur akzeptieren/ignorieren — die kuratierte Liste ist fix group_history.
    (2) Route: In admin_routes.go analog Z.193 registrieren:
        v1.GET("/admin/fansubs/:id/role-definitions", auth, deps.histGroupMemberRolesHandler.ListGroupHistoryRoleDefinitions).
        Platzierung im selben Block wie die übrigen /admin/fansubs/:id/member-roles-Routen.
    Deutsche Fehlermeldungen mit korrekten Umlauten.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go vet ./internal/handlers/ ./cmd/server/</automated>
  </verify>
  <acceptance_criteria>
    - Quelle: fansub_hist_group_member_roles_handler.go enthält ListGroupHistoryRoleDefinitions mit CanForFansubGroup(ActionFansubGroupMembersView)-Gate und c.JSON(200, gin.H{"data": ...}).
    - Quelle: admin_routes.go registriert GET /admin/fansubs/:id/role-definitions auf den neuen Handler.
    - Behavior: `cd backend && go build ./...` exit 0.
    - Quelle: Fehlermeldungen verwenden korrekte Umlaute (keine ASCII-Ersetzungen).
  </acceptance_criteria>
  <done>Read-Endpunkt registriert und auth-gated; liefert kuratierte group_history-Liste als data-Envelope; Build grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin-Browser → role-definitions-Read-API | Untrusted fansub id als Pfadparameter; Read muss autorisiert sein |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-04 | Information Disclosure | Ungeschützter Read der Rollendefinitionen | mitigate | CanForFansubGroup(ActionFansubGroupMembersView)-Gate vor DB-Read (analog ListHistGroupMemberRoles) |
| T-94-05-B | Elevation of Privilege | Naives ANY(contexts) liefert App-Rollen in den Hist-Dialog → falscher Rollenkontext | mitigate | Kuratierte Whitelist founder/leader/co_leader/project_manager statt ANY(contexts) (Pitfall 2) |
| T-94-SC | Tampering | go get / npm install | accept | Phase fügt KEINE Pakete hinzu (RESEARCH) |
</threat_model>

<verification>
- `cd backend && go test ./internal/repository/ -run RoleDefinitionsContext` exit 0.
- `cd backend && go build ./...` exit 0; `go vet ./internal/handlers/ ./cmd/server/` ohne Fehler.
- Nach Routen-Änderung: `docker compose up -d --build team4sv30-backend` (neue Route erscheint sonst nicht; Backend :8092).
- Live-Check (optional, nach Rebuild): GET /api/v1/admin/fansubs/:id/role-definitions liefert nur Hist-Rollen.
- `git diff --check` sauber.
</verification>

<success_criteria>
- Neuer Read-Endpunkt liefert kuratierte group_history-Rollen, autorisiert via CanForFansubGroup(MembersView).
- Keine aktiven App-Gruppenrollen in der Liste (Pitfall 2 vermieden).
- Keine neue Migration; Build und Plan-01-Repo-Test grün.
</success_criteria>

<output>
Create `.planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-03-SUMMARY.md` when done
</output>
