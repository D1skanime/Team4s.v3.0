---
phase: 94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/GroupMembersTab.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/GroupHistRoleDialog.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/FansubAppMemberEditorPanel.tsx
autonomous: true
requirements: [D-07, D-10]
nyquist_compliant: true

must_haves:
  truths:
    - "GroupMembersTab.tsx ist <= 450 Zeilen nach Extraktion des historischen Rollen-Dialogs (Pitfall 3)"
    - "FansubAppMembersSection.tsx ist <= 450 Zeilen nach Extraktion des Mitglieder-Editor-Panels (Pitfall 3)"
    - "Das Verhalten der beiden Mitglieder-Oberflächen ist nach dem Split unverändert (reine Extraktion)"
  artifacts:
    - path: "frontend/src/app/admin/fansubs/[id]/edit/GroupHistRoleDialog.tsx"
      provides: "Extrahierter historischer Rollen-Dialog (Vorbereitung für D-07/D-09 in Plan 08)"
      min_lines: 30
    - path: "frontend/src/app/admin/fansubs/[id]/edit/FansubAppMemberEditorPanel.tsx"
      provides: "Extrahiertes aktives Mitglieder-Editor-Panel (Vorbereitung für D-10 in Plan 08)"
      min_lines: 30
  key_links:
    - from: "GroupMembersTab.tsx"
      to: "GroupHistRoleDialog.tsx"
      via: "Import der extrahierten Dialog-Komponente"
      pattern: "GroupHistRoleDialog"
    - from: "FansubAppMembersSection.tsx"
      to: "FansubAppMemberEditorPanel.tsx"
      via: "Import des extrahierten Editor-Panels"
      pattern: "FansubAppMemberEditorPanel"
---

<objective>
Die beiden überlangen Mitglieder-Komponenten (GroupMembersTab.tsx = 1209 Z., FansubAppMembersSection.tsx
= 1064 Z.) ZUERST in Unterkomponenten aufteilen, bevor in Plan 08 die fachlichen Änderungen (D-07/D-09
Rollenquelle, D-10 Label) erfolgen. CLAUDE.md erzwingt <= 450 Zeilen; Pitfall 3 verlangt Split-First
als eigenen Task. Dieser Plan ändert KEINE Semantik — reine, verhaltensneutrale Extraktion.

Purpose: Hält das 450-Zeilen-Limit ein und isoliert die in Plan 08 zu ändernden Stellen (historischer
Rollen-Dialog, aktives Editor-Panel) in eigene Dateien, sodass Plan 08 klein und fokussiert bleibt.
Output: Zwei neue Unterkomponenten plus die verkleinerten Eltern-Dateien, Verhalten identisch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-CONTEXT.md
@.planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-PATTERNS.md
@frontend/src/app/admin/fansubs/[id]/edit/GroupMembersTab.tsx
@frontend/src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Historischen Rollen-Dialog aus GroupMembersTab.tsx extrahieren</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/GroupMembersTab.tsx, frontend/src/app/admin/fansubs/[id]/edit/GroupHistRoleDialog.tsx</files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/GroupMembersTab.tsx (vollständig; insbesondere der historische Rollen-Dialog mit Select-Primitiv Z.1085-1105 und die zugehörige roleForm-State-/Handler-Logik)
    - frontend/src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.tsx (als Extraktions-Vorbild im selben Ordner: wie Section-/Modal-Komponenten geschnitten sind, gemeinsames styles-Modul)
    - .planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-PATTERNS.md (Abschnitt GroupMembersTab.tsx)
  </read_first>
  <action>
    Extrahiere den historischen Rollen-Dialog (Modal mit roleForm, Select-Primitiv und Speichern/Abbrechen)
    aus GroupMembersTab.tsx in eine neue Datei GroupHistRoleDialog.tsx im selben Ordner. Übergib State und
    Handler als typisierte Props (z.B. open, onClose, roleForm, setRoleForm, onSubmit, isSaving, Fehlerzustand
    sowie die Rollenoptionen-Quelle als Prop, damit Plan 08 die group_history-Liste injizieren kann — in
    diesem Plan bleibt die bestehende FANSUB_GROUP_ROLE_OPTIONS-Quelle unverändert durchgereicht). Das
    Select-Primitiv aus @/components/ui beibehalten (D-08 ist erfüllt). Importiere die neue Komponente in
    GroupMembersTab.tsx und ersetze den Inline-Block durch das Element GroupHistRoleDialog. KEINE Verhaltens-,
    Text- oder Rollenquellen-Änderung — reines Verschieben. Beide Dateien danach <= 450 Zeilen. Falls
    GroupMembersTab.tsx weiterhin > 450 Zeilen ist, extrahiere zusätzlich einen weiteren kohäsiven Block
    (z.B. die historische Mitglieder-Tabelle) in eine eigene Datei, bis das Limit eingehalten ist.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -E "GroupMembersTab|GroupHistRoleDialog" || echo "NO-TS-ERRORS-FOR-TOUCHED-FILES"</automated>
  </verify>
  <acceptance_criteria>
    - Quelle: GroupHistRoleDialog.tsx existiert und enthält das Select-Primitiv aus @/components/ui.
    - Quelle: GroupMembersTab.tsx importiert und rendert GroupHistRoleDialog statt des Inline-Blocks.
    - Quelle: `wc -l GroupMembersTab.tsx` <= 450 und `wc -l GroupHistRoleDialog.tsx` <= 450.
    - Behavior: `npx tsc --noEmit` meldet keine neuen Fehler in den berührten Dateien.
    - Quelle: keine Änderung an Rollenquelle/Labels (git diff zeigt nur Verschiebung, kein neuer user-facing String).
  </acceptance_criteria>
  <done>Historischer Rollen-Dialog ausgelagert, GroupMembersTab.tsx <= 450 Z., Verhalten identisch, Select-Primitiv beibehalten.</done>
</task>

<task type="auto">
  <name>Task 2: Aktives Mitglieder-Editor-Panel aus FansubAppMembersSection.tsx extrahieren</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.tsx, frontend/src/app/admin/fansubs/[id]/edit/FansubAppMemberEditorPanel.tsx</files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.tsx (vollständig; insbesondere das Mitglieder-Editor-Panel mit den Rollen-Sektionen Z.1005-1033 und die FANSUB_GROUP_ROLE_OPTIONS-Nutzung Z.860/915/1008)
    - .planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-PATTERNS.md (Abschnitt FansubAppMembersSection.tsx)
  </read_first>
  <action>
    Extrahiere das aktive Mitglieder-Editor-Panel (Editor-Modal/Section mit den aktiven Rollen-Sektionen und
    Medienrechten) aus FansubAppMembersSection.tsx in FansubAppMemberEditorPanel.tsx im selben Ordner. State
    und Handler als typisierte Props durchreichen. Bestehende FANSUB_GROUP_ROLE_OPTIONS-Nutzung bleibt für
    aktive Rollen unverändert (D-01). KEINE Label- oder Verhaltensänderung in DIESEM Plan — die Umbenennung
    "Rollen" -> "Aktive Rechte" (D-10) erfolgt erst in Plan 08. Importiere und rendere das Panel aus der
    Eltern-Datei. Beide Dateien danach <= 450 Zeilen; falls die Eltern-Datei noch > 450 Z. ist, weiteren
    kohäsiven Block (z.B. Einladungs-Modal) in eine eigene Datei auslagern, bis das Limit hält.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -E "FansubAppMembersSection|FansubAppMemberEditorPanel" || echo "NO-TS-ERRORS-FOR-TOUCHED-FILES"</automated>
  </verify>
  <acceptance_criteria>
    - Quelle: FansubAppMemberEditorPanel.tsx existiert und kapselt die aktiven Rollen-Sektionen.
    - Quelle: FansubAppMembersSection.tsx importiert und rendert FansubAppMemberEditorPanel.
    - Quelle: `wc -l FansubAppMembersSection.tsx` <= 450 und `wc -l FansubAppMemberEditorPanel.tsx` <= 450.
    - Behavior: `npx tsc --noEmit` meldet keine neuen Fehler in den berührten Dateien.
    - Quelle: git diff zeigt keine neuen/geänderten user-facing Labels (reine Extraktion).
  </acceptance_criteria>
  <done>Aktives Editor-Panel ausgelagert, FansubAppMembersSection.tsx <= 450 Z., Verhalten/Labels identisch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Eltern-Komponente → extrahierte Unterkomponente | Props-Kontrakt; keine neue Vertrauensgrenze, reine Strukturierung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-07 | Tampering | Verhaltensdrift beim Extrahieren (versehentliche Logikänderung) | mitigate | Reine Verschiebung; tsc --noEmit + git diff-Review; keine Logik-/Textänderung in diesem Plan |
| T-94-SC | Tampering | npm-Installs | accept | Keine neuen Pakete (RESEARCH) |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` ohne neue Fehler in den vier berührten Dateien.
- Alle vier Dateien <= 450 Zeilen.
- `git diff --check` sauber; Diff zeigt nur Strukturverschiebung, keine Semantik-/Textänderung.
</verification>

<success_criteria>
- GroupMembersTab.tsx und FansubAppMembersSection.tsx sind <= 450 Zeilen.
- Historischer Rollen-Dialog und aktives Editor-Panel sind in eigene Dateien isoliert.
- Verhalten und user-facing Texte unverändert (Plan 08 nimmt die fachlichen Änderungen vor).
</success_criteria>

<output>
Create `.planning/phases/94-rollen-capability-ux-fachlich-entwirren-und-mobil-nutzbar-ma/94-07-SUMMARY.md` when done
</output>
