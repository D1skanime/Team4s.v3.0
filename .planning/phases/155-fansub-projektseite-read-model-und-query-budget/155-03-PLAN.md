---
phase: 155-fansub-projektseite-read-model-und-query-budget
plan: 03
type: execute
wave: 2
depends_on: ["155-01"]
files_modified:
  - backend/internal/repository/group_contributors_repository_test.go
autonomous: true
requirements: [P155-03, P155-04]

must_haves:
  truths:
    - "GetProjectContributors issues the SAME number of SQL queries whether a project has 0 contributors or 30-50 contributors with many roles/notes/media each"
    - "This constant-query-budget claim is proven by a real, running test — not by reading the repository's own source text"
  artifacts:
    - path: "backend/internal/repository/group_contributors_repository_test.go"
      provides: "A new constant-query-budget test using real seeded small (1-2) vs. large (30-50) contributor counts"
  key_links:
    - from: "backend/internal/repository/group_contributors_repository_test.go"
      to: "backend/internal/repository/fansub_project_resolver_query_budget_test.go"
      via: "reuse of openPhase155Postgres/mustExecPhase155 DSN scaffold created in Plan 155-01"
      pattern: "openPhase155Postgres\\(t\\)"
---

<objective>
Lock in, with a real running test, the negative finding RESEARCH.md already confirmed by direct
SQL inspection: `GroupContributorsRepository.GetProjectContributors` already issues exactly two
fixed SQL queries (one for external contributors, one for team members, both single `GROUP BY`
aggregations) regardless of contributor count — there is no per-member request/query fan-out
today. This plan does not change the repository's SQL; it proves the existing behavior with a
30-50-contributor synthetic seed, satisfying Auftrag §12's explicit contributor-load-test mandate
and P155-04's "load test with 30-50 contributors proves no detail data is loaded afterward"
requirement.

Purpose: Workstream B's CONTEXT.md guidance explicitly allows a documented negative finding
instead of a rewrite — but that finding must be measured and locked in as a regression gate, not
just asserted in prose. The existing `TestGetProjectContributors_EmptyResult`/`_Scoping` tests in
this file currently call `setupTestRepo(t)`, which unconditionally calls `t.Skip(...)` (see
`test_helpers.go:14-17`) — they have never actually run against a real database. This plan's new
test must NOT depend on that placeholder.
Output: One new test function proving constant query count across contributor-count scale, using
a real, DSN-gated Postgres database.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-fansub-projektseite-read-model-und-query-budget/155-CONTEXT.md
@.planning/phases/155-fansub-projektseite-read-model-und-query-budget/155-RESEARCH.md
@.planning/phases/155-fansub-projektseite-read-model-und-query-budget/155-PATTERNS.md

<interfaces>
From backend/internal/repository/group_contributors_repository.go (full file, already read in
full during planning) — the two queries to keep unmodified: `externalQuery` (lines 64-86,
`anime_contributions` + `members` + role aggregation) and `teamQuery` (lines 116-135,
`release_member_roles` + `members` + role aggregation, `DISTINCT ON (m.id)`). Both filter on
`WHERE ... anime_id = $1 AND ... fansub_group_id = $2` and both are single-shot `GROUP BY`
queries — confirm this stays true after seeding, do not add per-row follow-up queries.

From backend/internal/repository/test_helpers.go (lines 12-17) — `setupTestRepo` is a permanent
skip placeholder. Do not use it for the new test.

From backend/internal/repository/fansub_project_resolver_query_budget_test.go (created in Plan
155-01, Task 1) — reuse its `openPhase155Postgres(t) (*pgxpool.Pool, *queryCounter)` and
`mustExecPhase155(t, pool, sql)` helpers directly (same package, same file's exported-within-package
functions are visible here without import). Do not redeclare a second `TEAM4S_PHASE155_TEST_DSN`
scaffold.

From backend/internal/repository/fansub_public_profile_query_budget_test.go (lines 30-74, the
`seedPhase152PublicProfileQueryBudgetGroup` seeding-helper shape) — mirror this row-seeding style
(namespaced IDs, idempotent lookup-row inserts) for seeding 30-50 distinct `members` +
`release_member_roles`/`anime_contributions` rows scoped to one anime+group.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Constant-query-budget test for GetProjectContributors at 30-50 contributor scale</name>
  <files>backend/internal/repository/group_contributors_repository_test.go</files>
  <read_first>
    backend/internal/repository/group_contributors_repository.go (full file — externalQuery and teamQuery shapes, table names, required columns),
    backend/internal/repository/group_contributors_repository_test.go (full file — existing test naming/structure conventions to match, even though the two existing tests are currently skipped placeholders),
    backend/internal/repository/fansub_project_resolver_query_budget_test.go (the openPhase155Postgres/mustExecPhase155 scaffold created in Plan 155-01 — read it to get the exact function signatures before calling them here),
    database/migrations/ (grep for `CREATE TABLE members`, `CREATE TABLE release_member_roles`, `CREATE TABLE anime_contributions`, `CREATE TABLE contributor_roles`, `CREATE TABLE role_definitions` to confirm required NOT NULL columns before writing seed INSERTs)
  </read_first>
  <behavior>
    - A project with 2 seeded team-member contributors (via release_member_roles) and 2 seeded external contributors (via anime_contributions) yields a total query count for one GetProjectContributors call.
    - The SAME project shape scaled to 30 team-member contributors and 20 external contributors (50 total, each with at least one role, matching Auftrag §12's "many participations, several notes, many media uploads" framing as far as this repository's own query is concerned — notes/media volume does not affect this specific query since GetProjectContributors does not join notes/media tables at all, which is itself part of what this test proves) yields an IDENTICAL query count to the small case.
  </behavior>
  <action>
    Add a new test function `TestGetProjectContributorsQueryBudgetIsConstantAt30To50Contributors`
    to `group_contributors_repository_test.go`. Use `pool, counter := openPhase155Postgres(t)` (the
    scaffold from Plan 155-01) rather than `setupTestRepo`. Seed two anime/group pairs with
    namespaced IDs (e.g. `groupID := int64(1550100)` for small, `int64(1550200)` for large,
    mirroring the `groupID*1000 + i` convention already used in
    `fansub_public_profile_query_budget_test.go`): for each, insert one `fansub_groups` row, one
    `anime` row, the required `contributor_roles`/`role_definitions` lookup rows (idempotent, `ON
    CONFLICT DO NOTHING`, matching the existing convention for shared lookup tables), then for the
    small case 2 `members` + 2 `release_member_roles` (+ the `fansub_releases`/`episodes`/
    `release_versions`/`release_version_groups` chain each role needs to join through) + 2
    `members` + 2 `anime_contributions` rows; for the large case, 30 team-member members/roles +
    20 external-contributor members/contributions (50 total). Call
    `NewGroupContributorsRepository(pool).GetProjectContributors(ctx, animeID, groupID)` for both,
    resetting `counter` immediately before each call. Assert with `require.Len` that the small case
    returns exactly 2 team members + 2 external contributors and the large case returns exactly 30
    + 20, proving the seed landed correctly. Assert `require.Equal(t, smallQueryCount,
    largeQueryCount, "...")` proving the query count is IDENTICAL regardless of the 25x contributor
    volume difference. Log both counts via `t.Logf` for visibility in CI output, matching the
    logging style already used in `TestFansubPublicProfileQueryBudgetIsConstant`. Do not pin an
    exact numeric constant if the measured count for this repository is already documented
    elsewhere as "2" in RESEARCH.md — assert it explicitly equals 2 as well (both branches), since
    RESEARCH.md's own live-EXPLAIN finding already establishes this exact number as ground truth.
  </action>
  <acceptance_criteria>
    - `grep -c "func TestGetProjectContributorsQueryBudgetIsConstantAt30To50Contributors" backend/internal/repository/group_contributors_repository_test.go` equals 1
    - The new test does NOT call `setupTestRepo`
    - When `TEAM4S_PHASE155_TEST_DSN` is set to a dedicated `team4s_phase155_test` database, the test passes and both the small and large query counts equal 2
  </acceptance_criteria>
  <verify>
    <automated>docker run --rm --network team4s_default -v /home/d1sk/team4s:/workspace -w /workspace/backend golang:1.25-alpine go test ./internal/repository/... -run 'TestGetProjectContributorsQueryBudgetIsConstantAt30To50Contributors' -v -count=1</automated>
  </verify>
  <done>A real, seeded, 30-50-contributor test proves GetProjectContributors's query count does not grow with contributor volume, without depending on the always-skipping setupTestRepo placeholder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds a test only; no new production code path or trust boundary is introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-06 | N/A (test-only change) | group_contributors_repository_test.go | accept | No production code changes in this plan; existing GetProjectContributors visibility filters (is_public_on_anime_page, profile_visibility='public') are exercised as-is by the seed data, not altered |
</threat_model>

<verification>
- `go build ./...` unaffected (test-only file change).
- New test passes against a dedicated `team4s_phase155_test` database when `TEAM4S_PHASE155_TEST_DSN` is set; skips cleanly (does not fail) when unset, per the established convention.
</verification>

<success_criteria>
- The negative finding "contributor projection is already bounded/join-arm" is now a regression-locked, real-database-backed test, not just a documented claim.
</success_criteria>

<output>
Create `.planning/phases/155-fansub-projektseite-read-model-und-query-budget/155-03-SUMMARY.md` when done
</output>
