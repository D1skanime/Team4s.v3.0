---
phase: 155-fansub-projektseite-read-model-und-query-budget
plan: 05
type: execute
wave: 4
depends_on: ["155-04"]
files_modified:
  - frontend/src/app/anime/[id]/group/[groupId]/ProjectPage.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/sections/HeroSection.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/GroupAssetShowcase.tsx
  - frontend/src/components/groups/GroupAssetsExperience.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.test.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/page.test.tsx
autonomous: true
requirements: [P155-08, P155-09, P155-10, P155-11]

must_haves:
  truths:
    - "The project page's Releases stat and episode-asset cards render correctly using only data the loader now provides (releaseVersionCount and groupAssetsResponse), with no separate releaseEpisodes fetch anywhere in the render tree"
    - "The visible information architecture (Hero, episode asset showcase, Releases section gating) is pixel/behavior-identical to before this plan, only the data source changed"
  artifacts:
    - path: "frontend/src/components/groups/GroupAssetsExperience.tsx"
      provides: "Episode-to-release mapping read directly from its own episodes prop (release_id/episode_id/title), no separate releaseEpisodes prop"
    - path: "frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.tsx"
      provides: "hasReleases: boolean prop replacing the episodes: EpisodeReleaseSummary[] prop"
  key_links:
    - from: "frontend/src/app/anime/[id]/group/[groupId]/sections/HeroSection.tsx"
      to: "frontend/src/app/anime/[id]/group/[groupId]/sections/ProjectStats.tsx"
      via: "releaseCount prop now sourced from data.releaseVersionCount, not releaseEpisodes.length"
      pattern: "releaseCount=\\{releaseVersionCount\\}"
    - from: "frontend/src/components/groups/GroupAssetsExperience.tsx"
      to: "frontend/src/types/groupAsset.ts#GroupEpisodeAssets"
      via: "direct field access on the episodes prop (release_id, episode_id, title)"
      pattern: "episode\\.release_id"
---

<objective>
Finish removing `releaseEpisodes` from the render tree entirely (it no longer exists on
`PublicFansubProjectPageData` after Plan 155-04) by wiring every consumer that used to read it to
its correct new source: `HeroSection`/`ProjectStats` reads the new `releaseVersionCount` number,
`GroupAssetShowcase`/`GroupAssetsExperience` reads the `release_id`/`episode_id`/`title` fields
that already live on its own `episodes: GroupEpisodeAssets[]` prop (populated server-side by
`GetGroupAssets` itself, per RESEARCH.md's evidenced duplication finding), and `ReleasesSection`
takes an explicit `hasReleases: boolean` instead of an `episodes` array whose only use was a
`.length === 0` gate.

Purpose: P155-10/P155-11 require the visible page to render identically while removing the
now-nonexistent data dependency; CLAUDE.md's `--skip-ui` phase boundary means no visual/markup
change is authorized here beyond the prop-source swap.
Output: Five component files + two test files updated so `npx tsc --noEmit` and the full frontend
suite pass with zero references to `releaseEpisodes` anywhere in this directory tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-fansub-projektseite-read-model-und-query-budget/155-CONTEXT.md
@.planning/phases/155-fansub-projektseite-read-model-und-query-budget/155-RESEARCH.md
@.planning/phases/155-fansub-projektseite-read-model-und-query-budget/155-PATTERNS.md

<interfaces>
From frontend/src/app/anime/[id]/group/[groupId]/ProjectPage.tsx (full file, 56 lines, already
read in full during planning): current shape passes `releaseEpisodes={data.releaseEpisodes}` into
`HeroSection` (line 35) and wraps `<ReleasesSection episodes={data.releaseEpisodes} .../>` in a
`{data.hasReleases ? (...) : null}` ternary (lines 43-52).

From frontend/src/app/anime/[id]/group/[groupId]/sections/HeroSection.tsx (full file, 169 lines,
already read in full): `releaseEpisodes: EpisodeReleaseSummary[]` prop (line 33) is used at line
130 as `releaseCount={releaseEpisodes.length}` (passed into `ProjectStats`) and passed through
unchanged at line 163 into `<GroupAssetShowcase releaseEpisodes={releaseEpisodes} .../>`.

From frontend/src/app/anime/[id]/group/[groupId]/GroupAssetShowcase.tsx (full file, 28 lines) —
purely a thin prop-passthrough wrapper around `GroupAssetsExperience`.

From frontend/src/components/groups/GroupAssetsExperience.tsx (lines 1-140, already read in full):
`releaseEpisodes: EpisodeReleaseSummary[]` prop (line 19) feeds
`releaseByEpisode = new Map(releaseEpisodes.map((release) => [release.episode_number, release]))`
(lines 70-73), then per-episode lookup at line 134:
`const release = releaseByEpisode.get(episode.episode_number) || null` followed by
`release?.title`/`release?.episode_id`/`release?.id` usage (lines 135-139). Since `episodes:
GroupEpisodeAssets[]` (the OTHER, already-present prop, from `@/types/groupAsset`) already carries
`release_id`/`episode_id`/`title` directly on each entry (confirmed server-side: `group_assets_handler.go`'s
`buildGroupAssetsPayload` attaches these three fields per episode by internally calling
`GetGroupReleases` itself), the `releaseByEpisode` Map and the `releaseEpisodes` prop are both
removable — read `episode.release_id`/`episode.episode_id`/`episode.title` directly instead.

From frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.tsx (full file, 65
lines, already read in full): `episodes: EpisodeReleaseSummary[]` prop (line 11), guard `if
(episodes.length === 0) return null` (line 34) — this is the ENTIRE use of `episodes` in this
component; nothing else reads it.

From frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.test.tsx (full file, 80
lines, already read in full) — three `it()` blocks construct `<ReleasesSection episodes={...}
.../>`; each needs its `episodes` prop replaced with an equivalent `hasReleases` boolean.

From frontend/src/app/anime/[id]/group/[groupId]/page.test.tsx (lines 159-298, already read in
full) — the `describe('HeroSection navigation (102-03)')` block renders `<HeroSection ...
releaseEpisodes={[]} />` four times; each call site needs `releaseEpisodes={[]}` replaced with
`releaseVersionCount={0}` (or an equivalent non-zero value if a specific test asserts on the
rendered count — none currently do, per the file's full content already read).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProjectPage.tsx + HeroSection.tsx — wire releaseVersionCount, drop releaseEpisodes</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/ProjectPage.tsx, frontend/src/app/anime/[id]/group/[groupId]/sections/HeroSection.tsx, frontend/src/app/anime/[id]/group/[groupId]/page.test.tsx</files>
  <read_first>
    frontend/src/app/anime/[id]/group/[groupId]/ProjectPage.tsx,
    frontend/src/app/anime/[id]/group/[groupId]/sections/HeroSection.tsx,
    frontend/src/app/anime/[id]/group/[groupId]/page.test.tsx (lines 159-298)
  </read_first>
  <action>
    In `HeroSection.tsx`, replace the `releaseEpisodes: EpisodeReleaseSummary[]` prop with
    `releaseVersionCount: number` in `HeroSectionProps` and the destructured function params.
    Change line 130's `releaseCount={releaseEpisodes.length}` to `releaseCount={releaseVersionCount}`.
    Remove the `releaseEpisodes` pass-through into `<GroupAssetShowcase .../>` (current line 163) —
    `GroupAssetShowcase` no longer accepts this prop after Task 2 in this plan. Remove the
    `EpisodeReleaseSummary` type import if nothing else in the file references it.
    In `ProjectPage.tsx`, change the `<HeroSection .../>` call to pass
    `releaseVersionCount={data.releaseVersionCount}` instead of `releaseEpisodes={data.releaseEpisodes}`.
    Simplify the `ReleasesSection` render from `{data.hasReleases ? (<ReleasesSection
    episodes={data.releaseEpisodes} .../>) : null}` to an unconditional
    `<ReleasesSection hasReleases={data.hasReleases} publicReleasePreviews={data.publicReleasePreviews}
    animeID={data.animeID} groupID={data.groupID} canonicalProjectPath={data.canonicalProjectPath}
    releaseBackdropUrl={data.heroBackdropUrl} />` (moving the gate inside the component, per Task 3
    of this plan — ReleasesSection already self-gates via its own `hasReleases` prop check, making
    the outer ternary redundant).
    In `page.test.tsx`'s `describe('HeroSection navigation (102-03)')` block (lines 159-298),
    replace every `releaseEpisodes={[]}` call-site prop with `releaseVersionCount={0}` — do not
    change any other prop or assertion in these four `it()` blocks.
  </action>
  <acceptance_criteria>
    - `grep -c "releaseEpisodes" frontend/src/app/anime/[id]/group/[groupId]/ProjectPage.tsx frontend/src/app/anime/[id]/group/[groupId]/sections/HeroSection.tsx frontend/src/app/anime/[id]/group/[groupId]/page.test.tsx` sums to 0
    - `grep -c "releaseVersionCount" frontend/src/app/anime/[id]/group/[groupId]/sections/HeroSection.tsx` is at least 2
    - `npx vitest run` for `page.test.tsx`'s `HeroSection navigation (102-03)` describe block passes unchanged (same assertions, new prop)
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit && npx vitest run 'src/app/anime/[id]/group/[groupId]/page.test.tsx'"</automated>
  </verify>
  <done>ProjectStats receives its release count from data.releaseVersionCount everywhere; no file in this task still references releaseEpisodes.</done>
</task>

<task type="auto">
  <name>Task 2: GroupAssetShowcase.tsx + GroupAssetsExperience.tsx — read release fields off the existing episodes prop</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/GroupAssetShowcase.tsx, frontend/src/components/groups/GroupAssetsExperience.tsx</files>
  <read_first>
    frontend/src/app/anime/[id]/group/[groupId]/GroupAssetShowcase.tsx (full file),
    frontend/src/components/groups/GroupAssetsExperience.tsx (lines 1-140),
    frontend/src/types/groupAsset.ts (full file — GroupEpisodeAssets.release_id/episode_id/title)
  </read_first>
  <action>
    In `GroupAssetShowcase.tsx`, remove the `releaseEpisodes: EpisodeReleaseSummary[]` prop from
    `GroupAssetShowcaseProps` and the `releaseEpisodes={releaseEpisodes}` pass-through to
    `GroupAssetsExperience`. Remove the now-unused `EpisodeReleaseSummary` import if present.
    In `GroupAssetsExperience.tsx`, remove the `releaseEpisodes: EpisodeReleaseSummary[]` prop from
    `GroupAssetsExperienceProps` and its destructured parameter. Remove the `releaseByEpisode`
    `useMemo` (lines 70-73) entirely. Change the per-episode lookup at line 134 from
    `const release = releaseByEpisode.get(episode.episode_number) || null` to read directly off the
    already-present `episode` object: replace `release?.title`, `release?.episode_id`, `release?.id`
    with `episode.title`, `episode.episode_id`, `episode.id` — noting that `GroupEpisodeAssets.id`
    does not exist as a field name; use `episode.release_id` wherever the old code used `release.id`
    (the release-version identifier), since `GroupEpisodeAssets`'s field for this is named
    `release_id`, not `id`. Remove the `EpisodeReleaseSummary` import if no longer referenced.
    Verify no other line in this file still references `releaseEpisodes` or `releaseByEpisode`.
  </action>
  <acceptance_criteria>
    - `grep -c "releaseEpisodes\|releaseByEpisode" frontend/src/app/anime/[id]/group/[groupId]/GroupAssetShowcase.tsx frontend/src/components/groups/GroupAssetsExperience.tsx` sums to 0
    - `grep -c "episode.release_id\|episode.episode_id" frontend/src/components/groups/GroupAssetsExperience.tsx` is at least 2
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit"</automated>
  </verify>
  <done>GroupAssetsExperience builds its episode->release detail link (title/episode_id/release_id) directly from its own episodes prop; no separate releaseEpisodes prop or Map exists anywhere in this component pair.</done>
</task>

<task type="auto">
  <name>Task 3: ReleasesSection.tsx — episodes array to hasReleases boolean</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.tsx, frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.test.tsx</files>
  <read_first>
    frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.tsx (full file),
    frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.test.tsx (full file)
  </read_first>
  <action>
    In `ReleasesSection.tsx`, replace the `episodes: EpisodeReleaseSummary[]` field in
    `ReleasesSectionProps` with `hasReleases: boolean`; remove the `EpisodeReleaseSummary` type
    import (from `@/types/group`) since it becomes unused. Change the destructured parameter list
    and the guard from `if (episodes.length === 0) return null` to `if (!hasReleases) return null`.
    No other line in this component reads `episodes` — leave everything else (the
    `PublicReleaseBlock`/`OlderReleasesList`/CTA rendering) unchanged.
    In `ReleasesSection.test.tsx`, remove the `makeEpisode` helper and the `EpisodeReleaseSummary`
    import (both become unused). Update the three `it()` blocks: `episodes={[]}` becomes
    `hasReleases={false}`; the two `episodes={[makeEpisode(...)]}`/`episodes={[makeEpisode(...),
    makeEpisode(...)]}` call sites become `hasReleases={true}` (the specific episode IDs/numbers
    passed to `makeEpisode` were never asserted on by these tests — confirm this by re-reading the
    three `it()` bodies before deleting `makeEpisode`, and only proceed if no assertion in the file
    depends on episode-specific data).
  </action>
  <acceptance_criteria>
    - `grep -c "episodes" frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.tsx` equals 0
    - `grep -c "hasReleases" frontend/src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.tsx` is at least 2
    - `npx vitest run 'src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.test.tsx'` passes with the same three test descriptions and assertions as before (only the prop changed)
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit && npx vitest run 'src/app/anime/[id]/group/[groupId]/sections/ReleasesSection.test.tsx'"</automated>
  </verify>
  <done>ReleasesSection takes a plain hasReleases boolean; its three existing tests pass with the new prop shape and identical assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure prop-source refactor of already-public, already-rendered data; no new data crosses any boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-08 | N/A (render-only refactor) | HeroSection/GroupAssetsExperience/ReleasesSection | accept | No new data exposure; release_id/episode_id/title were already present in groupAssetsResponse's public payload before this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- Full frontend suite for this directory tree (`page.test.tsx`, `ReleasesSection.test.tsx`) passes with the same test descriptions/assertions as before, only prop shapes changed.
- No `releaseEpisodes` reference remains anywhere under `frontend/src/app/anime/[id]/group/[groupId]/` or `frontend/src/components/groups/`.
</verification>

<success_criteria>
- The visible project page renders identically (Hero stats, episode asset cards, Releases section gating) using only data sourced from the Plan 155-04 loader contract.
- Zero remaining references to the removed `releaseEpisodes` prop/type usage in this render subtree.
</success_criteria>

<output>
Create `.planning/phases/155-fansub-projektseite-read-model-und-query-budget/155-05-SUMMARY.md` when done
</output>
