---
phase: 155-fansub-projektseite-read-model-und-query-budget
plan: 06
type: execute
wave: 4
depends_on: ["155-01", "155-04"]
files_modified:
  - frontend/src/lib/fansubProjectNavigation.ts
  - frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/page.tsx
  - frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/page.test.tsx
  - frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/mitwirkende/[memberSlug]/page.tsx
  - frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/mitwirkende/[memberSlug]/page.test.tsx
  - frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/releases/[releaseVersionId]/page.tsx
  - frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/releases/[releaseVersionId]/page.test.tsx
autonomous: true
requirements: [P155-01, P155-02, P155-05, P155-06, P155-11]

must_haves:
  truths:
    - "All three pretty routes (project page, project-member page, release-detail page) resolve groupSlug+animeSlug via the new resolver instead of loading the full public fansub profile"
    - "Each route's existing 404 behavior for an unknown group or unmatched project is preserved exactly (same notFound() trigger, same conditions)"
    - "The project page passes the resolver's canonicalProjectPath and navigation into loadPublicFansubProjectPageData's precomputed param, eliminating that loader's own internal profile fetch for this route"
    - "The project-member and release-detail pages continue to render their existing, unchanged content — only the ID-resolution call changed"
  artifacts:
    - path: "frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/page.tsx"
      provides: "resolveFansubProject-based resolution feeding loadPublicFansubProjectPageData's precomputed param"
    - path: "frontend/src/lib/fansubProjectNavigation.ts"
      provides: "buildFansubProjectNavigation accepting the resolver's narrow FansubProjectNavigationEntry[] as well as the full PublicFansubProject[] it already accepted"
  key_links:
    - from: "frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/page.tsx"
      to: "frontend/src/lib/api.ts#resolveFansubProject"
      via: "await resolveFansubProject(fansubSlug, animeSlug) replacing getPublicFansubProfileBySlug"
      pattern: "resolveFansubProject\\("
    - from: "frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/mitwirkende/[memberSlug]/page.tsx"
      to: "frontend/src/lib/api.ts#resolveFansubProject"
      via: "await resolveFansubProject(fansubSlug, animeSlug) replacing getPublicFansubProfileBySlug"
      pattern: "resolveFansubProject\\("
    - from: "frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/releases/[releaseVersionId]/page.tsx"
      to: "frontend/src/lib/api.ts#resolveFansubProject"
      via: "await resolveFansubProject(slug, animeSlug) replacing getPublicFansubProfileBySlug"
      pattern: "resolveFansubProject\\("
---

<objective>
Wire the Plan 155-01 resolver into all three pretty routes, per the operator-locked scope
extension (155-CONTEXT.md "Resolved Open Questions" #1, citing Auftrag §1/§20): the project page,
the project-member page, and the release-detail page ALL currently run the identical
`getPublicFansubProfileBySlug(slug)` + `.projects.find(...)` pattern purely to resolve
`animeSlug -> {animeID, groupID}`. Replace that call with `resolveFansubProject` in all three,
preserving each route's existing error-handling shape and downstream call
(`loadPublicFansubProjectPageData`, `getProjectMemberSummary`, `buildPublicFansubProjectPath`)
byte-for-byte otherwise. No other change to the project-member or release-detail pages' rendered
content is authorized by this decision.

Purpose: P155-01/P155-02 require the full public profile to no longer be the resolution mechanism
for any of these three routes; P155-05/P155-06 require the drill-down chain to keep working
exactly as today, just resolved through the leaner path.
Output: Three route files updated, `fansubProjectNavigation.ts`'s input type widened to accept the
resolver's narrow projection, and route-level tests proving the resolver call replaces the profile
call while preserving 404 semantics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-fansub-projektseite-read-model-und-query-budget/155-CONTEXT.md
@.planning/phases/155-fansub-projektseite-read-model-und-query-budget/155-RESEARCH.md
@.planning/phases/155-fansub-projektseite-read-model-und-query-budget/155-PATTERNS.md

<interfaces>
From frontend/src/lib/api.ts (Plan 155-01, Task 3) — `resolveFansubProject(groupSlug: string,
animeSlug: string): Promise<FansubProjectResolutionResponse>` where
`FansubProjectResolutionResponse = { data: { group_id: number; anime_id: number; anime_slug:
string; projects: { id: number; title: string; anime_slug: string }[] } }`. On a non-2xx response
it throws `ApiError` with `.status` set (same throw shape as `getPublicFansubProfileBySlug`).

From frontend/src/app/anime/[id]/group/[groupId]/projectPageData.ts (Plan 155-04, Task 2) —
`loadPublicFansubProjectPageData({animeID, groupID, precomputed?})` where `precomputed` is
`{canonicalProjectPath: string | null; fansubProjectNavigation: FansubProjectNavigation}`.

From frontend/src/lib/fansubProjectNavigation.ts (full file, already read in full during
planning): `buildFansubProjectNavigation({currentAnimeID, currentAnimeSlug,
currentFansubGroupID, currentFansubSlug, projects})` — `projects` is currently typed
`PublicFansubProject[]` (imported from `@/types/fansub`) but the function body only ever reads
`.id`, `.title`, `.anime_slug` off each entry (see `compareProjects` line 28-32 and
`toNavigationItem` line 34-42) — it is safe to narrow this parameter's type to the resolver's
`FansubProjectNavigationEntry` shape (`{id, title, anime_slug}`) since `PublicFansubProject`
structurally satisfies it (a wider type is always assignable where the narrower one is expected),
so the numeric route's existing call site (passing full `PublicFansubProject[]`) keeps
type-checking with zero changes.

From frontend/src/lib/fansubProjectRoutes.ts (full file, already read in full) —
`buildPublicFansubProjectPath(fansubSlug, animeSlug)` is a pure string-builder, already exported,
reuse directly (do not have the resolver compute this path server-side).

Current content of all three route files was read in full during planning (each ~35-58 lines);
each follows this exact shape today (only the specific downstream call after project resolution
differs):
  try { profileResponse = await getPublicFansubProfileBySlug(fansubSlug) }
  catch (error) { if (error instanceof ApiError && error.status === 404) return notFound(); throw error }
  const project = profile(.data).projects.find((item) => item.anime_slug?.trim() === animeSlug)
  if (!project) return notFound()
  -- project page then calls loadPublicFansubProjectPageData({animeID: project.id, groupID: profile.group.id})
  -- member page then calls getProjectMemberSummary(project.id, profile.group.id, memberSlug)
  -- release page then calls buildPublicFansubProjectPath(profile.data.group.slug, project.anime_slug)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project page — resolver wiring + precomputed navigation + narrow navigation type</name>
  <files>frontend/src/lib/fansubProjectNavigation.ts, frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/page.tsx, frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/page.test.tsx</files>
  <read_first>
    frontend/src/lib/fansubProjectNavigation.ts (full file),
    frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/page.tsx (full file, 51 lines),
    frontend/src/app/anime/[id]/group/[groupId]/projectPageData.ts (as modified by Plan 155-04 — re-read after that plan lands to get the exact `precomputed` param shape/name),
    frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/releases/[releaseVersionId]/page.test.tsx (full file — the existing `vi.hoisted`/`vi.mock('@/lib/api', ...)` pattern to mirror for this new test file)
  </read_first>
  <action>
    In `frontend/src/lib/fansubProjectNavigation.ts`, change the `BuildFansubProjectNavigationInput.projects`
    field type from `PublicFansubProject[]` to a new, narrower type
    `FansubProjectNavigationEntry[]` imported from `@/types/fansub` (defined in Plan 155-01), and
    change `toNavigationItem`'s parameter type from `PublicFansubProject` to
    `FansubProjectNavigationEntry` to match. Remove the `PublicFansubProject` import if nothing
    else in the file uses it. This is a type-only widening — `PublicFansubProject` already has all
    three fields `FansubProjectNavigationEntry` requires, so the numeric legacy route's existing
    call site (which still passes full `PublicFansubProject[]` from a public-profile response)
    continues to type-check with zero source changes there.
    In `frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/page.tsx`, replace the
    `getPublicFansubProfileBySlug(fansubSlug)` + `.projects.find(...)` block with:
    `const resolution = await resolveFansubProject(fansubSlug, animeSlug)` wrapped in the SAME
    `try { ... } catch (error) { if (error instanceof ApiError && error.status === 404) return
    notFound(); throw error }` shape as today. Then build
    `const canonicalProjectPath = buildPublicFansubProjectPath(fansubSlug, resolution.data.anime_slug)`
    and `const fansubProjectNavigation = buildFansubProjectNavigation({ currentAnimeID:
    resolution.data.anime_id, currentAnimeSlug: resolution.data.anime_slug, currentFansubGroupID:
    resolution.data.group_id, currentFansubSlug: fansubSlug, projects: resolution.data.projects
    })`. Call `loadPublicFansubProjectPageData({ animeID: resolution.data.anime_id, groupID:
    resolution.data.group_id, precomputed: { canonicalProjectPath, fansubProjectNavigation } })`.
    Update imports: drop `getPublicFansubProfileBySlug`, add `resolveFansubProject` and
    `buildFansubProjectNavigation` (from `@/lib/fansubProjectNavigation`) to the existing import
    from `@/lib/api`/new import line; `buildPublicFansubProjectPath` is likely already imported
    transitively via `projectPageData` re-export (`export { buildPublicFansubProjectPath }` at
    `projectPageData.ts` line 105) — import it directly from `@/lib/fansubProjectRoutes` instead if
    that re-export is removed by Plan 155-04 (check before assuming it still exists). The
    `result.status === "error"`/`"not-found"` handling below stays byte-identical.
    Create `frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/page.test.tsx` (a NEW file —
    none exists today for this specific route) mirroring the
    `releases/[releaseVersionId]/page.test.tsx` file's `vi.hoisted` + `vi.mock('@/lib/api', ...)` +
    `vi.mock('next/navigation', ...)` pattern, additionally mocking
    `@/app/anime/[id]/group/[groupId]/projectPageData`'s `loadPublicFansubProjectPageData` export.
    Write: (1) a test asserting that on a 404 from `resolveFansubProject`, the route calls
    `notFound()` and never calls `loadPublicFansubProjectPageData`; (2) a success-path test
    asserting `loadPublicFansubProjectPageData` is called with `precomputed.canonicalProjectPath`
    equal to `buildPublicFansubProjectPath(fansubSlug, resolution.data.anime_slug)`'s expected
    value for a realistic fixture; (3) a test confirming `getPublicFansubProfileBySlug` (if still
    mocked/imported) is never invoked by this route file.
  </action>
  <acceptance_criteria>
    - `grep -c "getPublicFansubProfileBySlug" frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/page.tsx` equals 0
    - `grep -c "resolveFansubProject" frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/page.tsx` is at least 1
    - `grep -c "precomputed" frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/page.tsx` is at least 1
    - The new `page.test.tsx` passes and explicitly asserts non-invocation of `getPublicFansubProfileBySlug` and non-invocation of `loadPublicFansubProjectPageData` on the 404 path
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit && npx vitest run 'src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/page.test.tsx'"</automated>
  </verify>
  <done>The project pretty route resolves via resolveFansubProject only, feeds precomputed navigation into the shared loader, and a new route-level test proves the profile call is gone while 404 behavior is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Project-member page — resolver wiring, no other behavior change</name>
  <files>frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/mitwirkende/[memberSlug]/page.tsx, frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/mitwirkende/[memberSlug]/page.test.tsx</files>
  <read_first>
    frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/mitwirkende/[memberSlug]/page.tsx (full file, 58 lines),
    frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/mitwirkende/[memberSlug]/page.test.tsx (full file — currently tests ONLY the ProjectMemberPage component, does not import or exercise this route file at all; do not remove any existing test, only ADD new ones)
  </read_first>
  <action>
    In `page.tsx`, replace the `getPublicFansubProfileBySlug(fansubSlug)` + `.projects.find(...)`
    block with `const resolution = await resolveFansubProject(fansubSlug, animeSlug)` under the
    same try/catch-404-else-throw shape as today. Replace `getProjectMemberSummary(project.id,
    profile.group.id, memberSlug)` with `getProjectMemberSummary(resolution.data.anime_id,
    resolution.data.group_id, memberSlug)`, keeping its own separate try/catch-404 block
    unchanged. Update the `<ProjectMemberPage .../>` prop values that previously read from
    `profile`/`project`: `groupName` requires a display name the resolver does NOT provide (the
    resolver's contract is intentionally narrow — group_id/slug/anime identity only, no group
    name) — check `ProjectMemberSummary`/`getProjectMemberSummary`'s response shape for whether
    `groupName` can instead be sourced from `summary` (the project-member detail response); if not
    available there either, keep a SEPARATE minimal call to resolve the display name only if
    strictly required for rendering (e.g. `getGroupDetail(resolution.data.anime_id,
    resolution.data.group_id)` is already a lean, existing, single-purpose endpoint — prefer this
    over reintroducing any profile-shaped call). Set `animeTitle` similarly: check whether
    `summary` already carries an anime title; if not, `getGroupDetail`'s paired `getAnimeByID`-free
    minimal title source may not exist — in that case, keep the anime title lookup narrowly scoped
    (do not fall back to the full profile just to get a title). Set `animeID`/`groupID` from
    `resolution.data.anime_id`/`resolution.data.group_id`. Set `groupSlug={fansubSlug}` (already
    known from the route param, no lookup needed). Set `projectPath` using
    `buildPublicFansubProjectPath(fansubSlug, resolution.data.anime_slug)` (imported from
    `@/lib/fansubProjectRoutes`) instead of the current inline template-literal construction.
    Add new tests to the existing `page.test.tsx` (a new `describe('ProjectMemberRoute resolver
    wiring')` block, additive — do not touch the existing `describe('ProjectMemberPage')` block)
    using `vi.mock('@/lib/api', ...)` to stub `resolveFansubProject`/`getProjectMemberSummary`/
    (and whatever minimal display-name lookup was added above): (1) a 404-from-resolver test
    asserting `notFound()` fires and `getProjectMemberSummary` is never called; (2) a success-path
    test asserting `getProjectMemberSummary` is called with the resolver's `anime_id`/`group_id`,
    not any value derived from a full profile object; (3) an explicit assertion that
    `getPublicFansubProfileBySlug` is never imported/called by this route.
  </action>
  <acceptance_criteria>
    - `grep -c "getPublicFansubProfileBySlug" frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/mitwirkende/[memberSlug]/page.tsx` equals 0
    - `grep -c "resolveFansubProject" frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/mitwirkende/[memberSlug]/page.tsx` is at least 1
    - The existing `describe('ProjectMemberPage')` block in page.test.tsx is unmodified and still passes
    - The new resolver-wiring tests pass and assert non-invocation of getPublicFansubProfileBySlug
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit && npx vitest run 'src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/mitwirkende/[memberSlug]/page.test.tsx'"</automated>
  </verify>
  <done>The project-member pretty route resolves via resolveFansubProject only; its rendered content and existing component-level tests are completely unchanged; new tests prove the resolver wiring and the absence of the old profile call.</done>
</task>

<task type="auto">
  <name>Task 3: Release-detail page — resolver wiring, existing tests rewritten to mock resolveFansubProject</name>
  <files>frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/releases/[releaseVersionId]/page.tsx, frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/releases/[releaseVersionId]/page.test.tsx</files>
  <read_first>
    frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/releases/[releaseVersionId]/page.tsx (full file, 35 lines),
    frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/releases/[releaseVersionId]/page.test.tsx (full file, 52 lines — every existing test mocks getPublicFansubProfileBySlug via mocks.getProfile; all three must be rewritten to mock resolveFansubProject instead, with an updated resolved fixture shape)
  </read_first>
  <action>
    In `page.tsx`, replace `profile = await getPublicFansubProfileBySlug(slug.trim())` +
    `profile.data.projects.find(...)` with `const resolution = await resolveFansubProject(slug.trim(),
    animeSlug.trim())` under the same try/catch-404-else-throw shape as today. Replace the
    `<ReleaseDetailPageContent animeID={project.id} groupID={profile.data.group.id}
    canonicalProjectPath={buildPublicFansubProjectPath(profile.data.group.slug, project.anime_slug)}
    .../>` props with `animeID={resolution.data.anime_id} groupID={resolution.data.group_id}
    canonicalProjectPath={buildPublicFansubProjectPath(slug.trim(), resolution.data.anime_slug)}`.
    Update the `@/lib/api` import to drop `getPublicFansubProfileBySlug` and add
    `resolveFansubProject`.
    Rewrite `page.test.tsx`: change `mocks.getProfile` to `mocks.resolveProject` (or keep the
    variable name but repoint what it mocks), update the `vi.mock('@/lib/api', ...)` block to
    export `resolveFansubProject: mocks.resolveProject` instead of `getPublicFansubProfileBySlug:
    mocks.getProfile`. Update all three `mockResolvedValue`/`mockResolvedValueOnce` fixture shapes
    from `{ data: { group: { id: 4, slug: 'c-subs' }, projects: [{ id: 9, anime_slug:
    'vipers-creed' }] } }` to the resolver's shape `{ data: { group_id: 4, anime_id: 9, anime_slug:
    'vipers-creed', projects: [] } }` (for the "mismatched project slug" test, change the mock to
    return `anime_slug: 'other'`, matching the resolver's own not-matching-slug not-found
    semantics — if the resolver itself would 404 for a mismatched slug rather than returning a
    resolution with the WRONG anime_slug, rewrite that specific test to mock `resolveProject`
    rejecting with `new ApiError(404)` instead, since the resolver's contract does not return a
    partially-matching project the way the old `.find()` pattern could). Keep all three test
    names/expectations about `animeID`/`groupID`/`releaseVersionID`/`canonicalProjectPath` intact,
    only changing how those values are produced.
  </action>
  <acceptance_criteria>
    - `grep -c "getPublicFansubProfileBySlug" frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/releases/[releaseVersionId]/page.tsx frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/releases/[releaseVersionId]/page.test.tsx` sums to 0
    - `grep -c "resolveFansubProject" frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/releases/[releaseVersionId]/page.tsx` is at least 1
    - All three existing test names in page.test.tsx still exist and pass (same behavioral coverage: resolves IDs, forwards Kara deep-link, rejects mismatched slug)
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit && npx vitest run 'src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/releases/[releaseVersionId]/page.test.tsx'"</automated>
  </verify>
  <done>The release-detail pretty route resolves via resolveFansubProject only; all three pre-existing behavioral tests (ID resolution, Kara deep-link forwarding, mismatched-slug rejection) pass against the new mock shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> pretty route -> resolver endpoint | groupSlug/animeSlug path segments are the same untrusted input these routes already forwarded to getPublicFansubProfileBySlug; no new input shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-09 | Information Disclosure | Release-detail page's mismatched-slug handling | mitigate | Rewritten test (Task 3) confirms a mismatched project slug still yields the same notFound() outcome via the resolver's own not-found semantics, not by leaking a partially-matched project |
| T-155-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` passes for all three route files.
- Existing behavioral test coverage for the project-member and release-detail routes is preserved (same test names/expectations, updated mocks).
- New coverage exists for the project page route (previously untested at the route level).
- No route in this plan calls `getPublicFansubProfileBySlug` anymore.
</verification>

<success_criteria>
- All three pretty routes resolve `groupSlug + animeSlug` through the Plan 155-01 resolver.
- Project-member and release-detail pages render byte-identical content to before this plan — only their ID-resolution call changed.
- The project page's canonicalProjectPath/navigation flow through loadPublicFansubProjectPageData's `precomputed` param, eliminating that loader's internal profile fetch for this route.
</success_criteria>

<output>
Create `.planning/phases/155-fansub-projektseite-read-model-und-query-budget/155-06-SUMMARY.md` when done
</output>
