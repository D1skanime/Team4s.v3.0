---
phase: 155-fansub-projektseite-read-model-und-query-budget
plan: 07
type: execute
wave: 5
depends_on: ["155-01", "155-02", "155-03", "155-04", "155-05", "155-06"]
files_modified:
  - frontend/src/lib/fansubProjectNavigation.test.ts
  - docs/audits/2026-09-11-fansub-project-performance/REPORT.md
  - docs/audits/2026-09-11-fansub-project-performance/TABLES.md
  - docs/audits/2026-09-11-fansub-project-performance/REPRODUCE.md
  - docs/audits/2026-09-11-fansub-project-performance/VALIDATION.md
autonomous: true
requirements: [P155-04, P155-11, P155-12, P155-13, P155-14, P155-15]

must_haves:
  truths:
    - "Previous/Next project navigation is proven correct at both list boundaries (current is first, current is last) and with German-locale accented/case-varying titles"
    - "A full backend + frontend regression run is green after all six preceding plans have landed"
    - "A before/after audit document exists under docs/audits/ documenting requests, queries, payload size, and TTFB for the project page, following the established docs/audits/2026-09-09-public-member-performance/ structure"
    - "The audit explicitly documents the negative/deferred findings this phase intentionally left in place (ThemesSection/MediaSection component retention, GetGroupReleases's own internal redundant GetGroupDetail call, PublicReleaseBlock/OlderReleasesList non-linked contributor names)"
  artifacts:
    - path: "docs/audits/2026-09-11-fansub-project-performance/REPORT.md"
      provides: "Before/after narrative with measured request/query/payload numbers for the project page"
    - path: "docs/audits/2026-09-11-fansub-project-performance/TABLES.md"
      provides: "Exact measured numbers table (requests, queries, payload bytes, TTFB where reproducible)"
  key_links:
    - from: "docs/audits/2026-09-11-fansub-project-performance/REPRODUCE.md"
      to: "frontend/scripts/audit-public-member-performance.mjs"
      via: "AUDIT_ROUTES env var pointed at the project page route"
      pattern: "AUDIT_ROUTES=fansubs/"
---

<objective>
Close Workstream F: prove the Previous/Next navigation edge cases the operator's locked decision
(keep sorting in JS) depends on, run the full backend and frontend regression suites against the
fully-landed phase, and produce the mandated before/after audit document reusing the existing
Playwright/CDP measurement script — mirroring `docs/audits/2026-09-09-public-member-performance/`'s
structure exactly, per CONTEXT.md's explicit "prüfen und wiederzuverwenden" instruction.

Purpose: Auftrag §11/§12/§17/§19 require a documented before/after measurement, edge-case
coverage (list-boundary previous/next, previous project at list start, next project at list end),
and full green tests before the phase can be considered done. This is the phase's closing plan —
it runs only after all functional changes (155-01 through 155-06) are in place.
Output: Three new frontend edge-case tests, one full regression run (backend + frontend), and a
four-file audit document set under `docs/audits/2026-09-11-fansub-project-performance/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-fansub-projektseite-read-model-und-query-budget/155-CONTEXT.md
@.planning/phases/155-fansub-projektseite-read-model-und-query-budget/155-RESEARCH.md
@.planning/phases/155-fansub-projektseite-read-model-und-query-budget/155-PATTERNS.md
@docs/audits/2026-09-09-public-member-performance/REPORT.md
@docs/audits/2026-09-09-public-member-performance/REPRODUCE.md

<interfaces>
From frontend/src/lib/fansubProjectNavigation.test.ts (full file, already read in full during
planning) — the existing `project(overrides)` fixture helper and three existing `it()` blocks
(covering mid-list navigation, single-project list, and a Beta/Beta tie-breaker). Missing: an
explicit "current is the FIRST item in a 3+ item list" case (previous must be null, next non-null)
and an explicit "current is the LAST item" case, plus a case with genuinely accented titles (e.g.
"Änderung" vs "Zeta" vs an ASCII-adjacent title) proving `localeCompare('de', {sensitivity:
'base'})` orders diacritics the way German readers expect, not raw code-point order (Pitfall 2's
exact concern — this is a pure-function test, no live Postgres collation involved, since the
locked decision keeps this sort in JS).

From frontend/scripts/audit-public-member-performance.mjs (lines 1-40, already read) — reads
`AUDIT_OUT`, `AUDIT_BASE` (defaults to `http://127.0.0.1:3000`), `AUDIT_LABEL`, `AUDIT_ROUTES`
(comma-separated relative paths), `AUDIT_REPEATS` from env vars; fully generic over route path,
no code change needed — point `AUDIT_ROUTES` at the dev database's one existing fansub
group/project pretty-route path (confirm the exact slug/anime_slug via
`docker exec team4sv30-db psql -U ... -c "SELECT fg.slug, a.slug FROM anime_fansub_groups afg JOIN fansub_groups fg ON fg.id=afg.fansub_group_id JOIN anime a ON a.id=afg.anime_id LIMIT 5;"`
or equivalent, per RESEARCH.md's Environment Availability note that dev DB has exactly 1 fansub
group and 1 project).

From docs/audits/2026-09-09-public-member-performance/REPORT.md and REPRODUCE.md (read for
structure) — the four-file convention (`REPORT.md`, `TABLES.md`, `REPRODUCE.md`, `VALIDATION.md`)
and the "measured numbers first, explicit non-claims" narrative style to mirror.

RESEARCH.md's own "Code Examples" section (already documents the exact BEFORE code, verified
against commit 934c0037) is the authoritative source for this plan's "Vorher" numbers — do not
re-derive them by checking out or reverting code; cite RESEARCH.md's already-verified findings
directly (2 full public-profile loads across page.tsx+projectPageData.ts on the project page
alone, an identical pattern on the project-member and release-detail pages, the per_page:100 +
duplicate-retry release call, the two dead themes/media fetches, and
`fansub_public_profile_query_budget_test.go`'s already-measured
`phase152PublicProfileConstantQueryBudget = 8` as the per-full-profile-load query cost).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Previous/Next navigation boundary + German-locale edge case tests</name>
  <files>frontend/src/lib/fansubProjectNavigation.test.ts</files>
  <read_first>
    frontend/src/lib/fansubProjectNavigation.ts (full file),
    frontend/src/lib/fansubProjectNavigation.test.ts (full file)
  </read_first>
  <behavior>
    - Given a 3-project list where the current project is alphabetically first after German-locale sorting, previous is null and next points at the second-sorted project.
    - Given a 3-project list where the current project is alphabetically last after German-locale sorting, next is null and previous points at the second-to-last-sorted project.
    - Given titles "Änderung", "Zeta", "Zwiebel" with the current project being "Zeta", the sort order places "Änderung" before both "Zeta" and "Zwiebel" (German localeCompare base-sensitivity diacritic-aware ordering, not raw UTF-16 code-point ordering, which would place "Z" before "Ä").
  </behavior>
  <action>
    Add three new `it()` blocks to the existing `describe('buildFansubProjectNavigation')` block in
    `fansubProjectNavigation.test.ts`, reusing the existing `project(overrides)` helper: (1)
    "Previous Project am Listenanfang" — construct a 3-project list, set `currentAnimeSlug` to the
    slug that sorts first, assert `result.previous` is `null` and `result.next` is non-null with
    the correct `href`; (2) "Next Project am Listenende" — same list, current project set to the
    slug that sorts last, assert `result.next` is `null` and `result.previous` is non-null; (3) an
    explicit diacritic-ordering test using titles that differ specifically by a leading accented
    character, asserting the computed order via `previous`/`next` hrefs matches
    `["Änderung", "Zeta", "Zwiebel"].sort((a,b) => a.localeCompare(b, 'de', {sensitivity:'base'}))`'s
    actual JS output (compute and assert against that same comparator's real behavior in the test
    itself, not a hardcoded assumed order, in case this environment's ICU data sorts differently
    than expected).
  </action>
  <acceptance_criteria>
    - `npx vitest run 'src/lib/fansubProjectNavigation.test.ts'` passes with 6 total `it()` blocks (3 existing + 3 new)
    - The new diacritic test asserts against a real `localeCompare` computation performed in the test, not a hardcoded assumed order
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run 'src/lib/fansubProjectNavigation.test.ts'"</automated>
  </verify>
  <done>Previous/Next boundary and German-locale diacritic ordering are explicitly test-covered for the unchanged JS comparator the resolver's narrow projection now feeds.</done>
</task>

<task type="auto">
  <name>Task 2: Full regression suite — backend and frontend</name>
  <files></files>
  <read_first>
    .planning/phases/155-fansub-projektseite-read-model-und-query-budget/155-VALIDATION.md
  </read_first>
  <action>
    Run the full backend suite: `docker run --rm --network team4s_default -v
    /home/d1sk/team4s:/workspace -w /workspace/backend golang:1.25-alpine sh -c "go build ./... &&
    go vet ./... && go test ./..."`. Run the full frontend suite: `docker compose exec -T
    team4sv30-frontend sh -c "cd /app && npx tsc --noEmit && npx vitest run && npx eslint ."`.
    Capture both full outputs (redirect to temp log files under `/tmp` if useful input for the
    audit report's VALIDATION.md in Task 3). If any pre-existing, phase-unrelated failure is found
    (matching the established precedent of documenting-not-silently-fixing unrelated debt, e.g.
    Phase 152's `TestPointServicePhase106Boundary` precedent), record it explicitly as pre-existing
    rather than claiming it as fixed or silently ignoring it. If any failure IS caused by this
    phase's own changes (Plans 155-01 through 155-06), fix it directly before proceeding to Task 3
    — do not write the audit report against a red suite.
  </action>
  <acceptance_criteria>
    - Backend: `go build ./...`, `go vet ./...`, and `go test ./...` all report success (exit code 0)
    - Frontend: `npx tsc --noEmit`, `npx vitest run`, and `npx eslint .` all report success (exit code 0), or any failures are explicitly identified as pre-existing and unrelated to this phase's files_modified lists across Plans 155-01 through 155-06
    - `git status --short` shows no unrelated dirty files left over from the measurement run
  </acceptance_criteria>
  <verify>
    <automated>docker run --rm --network team4s_default -v /home/d1sk/team4s:/workspace -w /workspace/backend golang:1.25-alpine sh -c "go build ./... && go vet ./... && go test ./..." && docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit && npx vitest run && npx eslint ."</automated>
  </verify>
  <done>Full backend and frontend suites are green (or every remaining failure is documented as pre-existing and unrelated to this phase).</done>
</task>

<task type="auto">
  <name>Task 3: Before/after audit report reusing the existing Playwright measurement script</name>
  <files>docs/audits/2026-09-11-fansub-project-performance/REPORT.md, docs/audits/2026-09-11-fansub-project-performance/TABLES.md, docs/audits/2026-09-11-fansub-project-performance/REPRODUCE.md, docs/audits/2026-09-11-fansub-project-performance/VALIDATION.md</files>
  <read_first>
    docs/audits/2026-09-09-public-member-performance/REPORT.md (full structure/narrative style to mirror),
    docs/audits/2026-09-09-public-member-performance/REPRODUCE.md (full structure to mirror),
    frontend/scripts/audit-public-member-performance.mjs (env vars this task drives),
    .planning/phases/155-fansub-projektseite-read-model-und-query-budget/155-RESEARCH.md (Code Examples and Summary sections — the authoritative source for this report's "Vorher" numbers)
  </read_first>
  <action>
    Confirm the running dev stack is up (`docker compose ps`) and identify the dev DB's one
    existing fansub-group/project slug pair (per the Environment Availability note in
    RESEARCH.md). Run the existing audit script against the AFTER (fully-landed) code:
    `AUDIT_OUT=/tmp/phase155-project-audit AUDIT_BASE=http://127.0.0.1:3000
    AUDIT_LABEL=phase155-after AUDIT_ROUTES=fansubs/<slug>/fansubprojekt/<anime_slug>
    node frontend/scripts/audit-public-member-performance.mjs` (adjust the exact invocation to
    match how the script is actually run elsewhere — check
    `docs/audits/2026-09-09-public-member-performance/REPRODUCE.md` for the precise command form,
    including whether it runs inside or outside the frontend container). Record the measured
    request count, request list (grouped by endpoint), total payload bytes, and TTFB (if
    reproducible) for the project page route.
    Create `docs/audits/2026-09-11-fansub-project-performance/REPORT.md` mirroring
    `docs/audits/2026-09-09-public-member-performance/REPORT.md`'s opening framing (bold one-line
    finding, measured-numbers-first structure). Document, per Auftrag §19: Vorher (cite
    RESEARCH.md's already-verified code facts — 2 full public-profile loads on the project page
    alone [outer `page.tsx` + inner `projectPageData.ts`'s `profilePromise`], an identical
    2-load pattern independently existing on the project-member and release-detail pages before
    this phase, the `per_page:100` release call plus its duplicate nested-catch retry, the two
    dead `getGroupThemes`/`getGroupReleaseMedia` fetches) vs. Nachher (the actual measured AFTER
    numbers from this task's live run: 1 resolver call replacing each page's profile load, 0
    themes/media requests, 1 release-count request replacing the per_page:100 call, reduced
    request count, reduced payload). Explicitly document, as deliberate non-fixes (not silent
    omissions): `GetGroupReleases`'s own internal redundant `GetGroupDetail` call remains
    unfixed (out of scope, shared by other consumers — see Plan 155-02's Task 1 action note);
    `ThemesSection.tsx`/`MediaSection.tsx` remain as unrendered, untouched components (CONTEXT.md's
    explicit "kein stiller Komponenten-Löschzug" instruction); `PublicReleaseBlock`'s contributor
    row and `OlderReleasesList.rows.tsx`'s release rows render non-interactive avatar initials, not
    member links, so P155-05's canonical-route rule does not apply there today (nothing to
    redirect).
    Create `TABLES.md` with the exact measured before/after numbers in table form (requests,
    queries — cross-reference Plan 155-01/155-02/155-03's pinned query-budget constants — payload
    bytes, TTFB where reproducible). Create `REPRODUCE.md` documenting the exact command(s) used to
    reproduce this measurement, following the sibling audit's file/command-listing style. Create
    `VALIDATION.md` summarizing Task 1's edge-case test results, Task 2's full-suite result, and
    Plan 155-03's 30-50-contributor query-budget test result (cross-reference, do not re-run here)
    as the evidence trail for this phase's Abnahmekriterien.
  </action>
  <acceptance_criteria>
    - All four files exist under `docs/audits/2026-09-11-fansub-project-performance/`
    - `REPORT.md` contains both a "Vorher" and "Nachher" section with concrete numbers, not just prose claims
    - `REPORT.md` explicitly lists the three deliberate non-fixes named above
    - `TABLES.md` contains a table with at least: request count before/after, themes/media request count before/after, release-related request count before/after
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/audits/2026-09-11-fansub-project-performance/REPORT.md && test -f docs/audits/2026-09-11-fansub-project-performance/TABLES.md && test -f docs/audits/2026-09-11-fansub-project-performance/REPRODUCE.md && test -f docs/audits/2026-09-11-fansub-project-performance/VALIDATION.md</automated>
  </verify>
  <done>A four-file audit document set exists, reusing the existing Playwright measurement script, documenting concrete before/after numbers and the phase's deliberate non-fixes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is measurement and documentation only; no production code path changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-10 | N/A (documentation/measurement only) | docs/audits/2026-09-11-fansub-project-performance/ | accept | Read-only Playwright measurement against the local dev stack; no write/mutation endpoints are exercised |
| T-155-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- `npx vitest run 'src/lib/fansubProjectNavigation.test.ts'` passes with 6 tests.
- Full backend (`go build`, `go vet`, `go test`) and frontend (`tsc`, `vitest`, `eslint`) suites are green.
- Four audit files exist with concrete measured numbers and explicit deliberate-non-fix documentation.
- `git status --short` is clean (working tree, per Auftrag's Abnahmekriterien "Working Tree ist sauber") after this plan's own commits.
</verification>

<success_criteria>
- Previous/Next navigation is proven correct at both list boundaries and under German-locale diacritic sorting.
- The phase's full regression suite is green.
- A before/after audit document, reusing existing measurement tooling, documents the phase's request/query/payload reduction and explicitly names every deliberate non-fix.
</success_criteria>

<output>
Create `.planning/phases/155-fansub-projektseite-read-model-und-query-budget/155-07-SUMMARY.md` when done
</output>
