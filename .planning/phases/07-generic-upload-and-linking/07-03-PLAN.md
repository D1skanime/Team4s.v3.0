---
phase: 07-generic-upload-and-linking
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinMetadataSection.tsx
  - frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinMetadataSection.helpers.test.ts
  - frontend/src/app/admin/anime/components/AnimePatchForm/AnimePatchForm.tsx
  - frontend/src/app/admin/anime/components/AnimePatchForm/AnimeCoverField.tsx
  - frontend/src/app/admin/anime/[id]/edit/page.test.tsx
autonomous: true
requirements:
  - UPLD-01
  - UPLD-02
  - UPLD-03
gap_closure: true
must_haves:
  truths:
    - "Admins can manually upload and link `logo` and `background_video` from the active edit UI instead of seeing them as provider-only."
    - "The edit route exposes the Phase 07 asset-kind mutation seam for non-cover assets instead of leaving it reachable only through cover-specific wiring."
    - "Edit-route asset controls keep singular-slot replacement for `cover`, `banner`, `logo`, and `background_video`, while `background` remains additive."
  artifacts:
    - path: "frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinMetadataSection.tsx"
      provides: "Reachable manual upload controls and provenance copy for all supported edit-route asset kinds"
    - path: "frontend/src/app/admin/anime/components/AnimePatchForm/AnimePatchForm.tsx"
      provides: "Edit form wiring to the generic asset-kind upload-and-link seam from Phase 07-02"
    - path: "frontend/src/app/admin/anime/[id]/edit/page.test.tsx"
      provides: "Regression coverage proving edit UI exposes non-cover manual controls"
  key_links:
    - from: "frontend/src/app/admin/anime/components/AnimePatchForm/AnimePatchForm.tsx"
      to: "frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts"
      via: "uploadAndLinkAsset"
      pattern: "uploadAndLinkAsset"
    - from: "frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinMetadataSection.tsx"
      to: "frontend/src/lib/api.ts"
      via: "typed assign/add helpers for logo, background, and background_video"
      pattern: "assignAdminAnimeLogoAsset|assignAdminAnimeBackgroundVideoAsset|addAdminAnimeBackgroundAsset"
    - from: "frontend/src/app/admin/anime/[id]/edit/page.test.tsx"
      to: "frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinMetadataSection.tsx"
      via: "source-string and rendered-copy assertions"
      pattern: "Logo hochladen|Background-Video hochladen|provider-only"
---

<objective>
Close the Phase 07 edit-route reachability gap by exposing the existing generic upload-and-link seam in the visible admin UI for every supported manual asset kind.

Purpose: fix the verifier's failed truth without replanning backend contracts that already passed.
Output: edit-route UI wiring and regression coverage for `logo`, `background_video`, and the existing non-cover asset controls.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-generic-upload-and-linking/07-CONTEXT.md
@.planning/phases/07-generic-upload-and-linking/07-RESEARCH.md
@.planning/phases/07-generic-upload-and-linking/07-VERIFICATION.md
@.planning/phases/07-generic-upload-and-linking/07-02-SUMMARY.md
@frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinMetadataSection.tsx
@frontend/src/app/admin/anime/components/AnimePatchForm/AnimePatchForm.tsx
@frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts
@frontend/src/lib/api.ts

<interfaces>
From frontend/src/types/admin.ts:
```ts
export type AdminAnimeAssetKind = 'cover' | 'banner' | 'logo' | 'background' | 'background_video'
export type AdminAnimeUploadAssetType = 'poster' | 'banner' | 'logo' | 'background' | 'background_video'
```

From frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts:
```ts
const ANIME_ASSET_MUTATION_CONFIG: Record<AdminAnimeAssetKind, AnimeAssetMutationConfig>
// includes cover, banner, logo, background, background_video

return {
  submit,
  uploadAndLinkAsset,
  uploadAndSetCover,
}
```

From frontend/src/lib/api.ts:
```ts
export async function uploadAdminAnimeMedia(options: AdminAnimeMediaUploadOptions): Promise<AdminMediaUploadResponse>
export async function assignAdminAnimeLogoAsset(animeID: number, mediaID: string, authToken?: string): Promise<void>
export async function assignAdminAnimeBackgroundVideoAsset(animeID: number, mediaID: string, authToken?: string): Promise<void>
export async function addAdminAnimeBackgroundAsset(animeID: number, mediaID: string, authToken?: string): Promise<AdminAnimeBackgroundAssetResponse>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing edit-route regression coverage for non-cover manual asset reachability</name>
  <files>frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinMetadataSection.helpers.test.ts, frontend/src/app/admin/anime/[id]/edit/page.test.tsx</files>
  <read_first>
    - frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinMetadataSection.tsx
    - frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinMetadataSection.helpers.test.ts
    - frontend/src/app/admin/anime/[id]/edit/page.test.tsx
    - frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts
    - .planning/phases/07-generic-upload-and-linking/07-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: edit-route helper copy no longer describes `logo` or `background_video` as provider-only when provider data exists.
    - Test 2: edit-route source or rendered output exposes manual controls for `Logo hochladen` and `Background-Video hochladen`.
    - Test 3: the `background` path remains additive and is still described as append-only rather than singular replacement.
  </behavior>
  <action>Add failing tests that pin the verifier's failed truth before implementation. Extend `AnimeJellyfinMetadataSection.helpers.test.ts` to assert `summarizeAssetSlotDecision('logo', ...)` and `summarizeAssetSlotDecision('background_video', ...)` describe explicit manual replacement instead of provider-only behavior. Extend `frontend/src/app/admin/anime/[id]/edit/page.test.tsx` with source-string or rendered-copy assertions that the edit surface contains manual upload labels for `Logo` and `Background-Video`, and that no active edit copy still says `provider-only` for those asset kinds. Treat the current background upload path as additive, not a singular slot.</action>
  <acceptance_criteria>
    - At least one failing assertion covers `logo` manual reachability.
    - At least one failing assertion covers `background_video` manual reachability.
    - At least one assertion confirms `background` copy still implies additive behavior.
    - The tests fail against the current implementation that labels `logo` and `background_video` as provider-only.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinMetadataSection.helpers.test.ts src/app/admin/anime/[id]/edit/page.test.tsx</automated>
  </verify>
  <done>Edit-route regression tests prove the current UI gap before code changes land.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the generic asset-kind mutation seam into the reachable edit UI</name>
  <files>frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinMetadataSection.tsx, frontend/src/app/admin/anime/components/AnimePatchForm/AnimePatchForm.tsx, frontend/src/app/admin/anime/components/AnimePatchForm/AnimeCoverField.tsx, frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinMetadataSection.helpers.test.ts, frontend/src/app/admin/anime/[id]/edit/page.test.tsx</files>
  <read_first>
    - frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinMetadataSection.tsx
    - frontend/src/app/admin/anime/components/AnimePatchForm/AnimePatchForm.tsx
    - frontend/src/app/admin/anime/components/AnimePatchForm/AnimeCoverField.tsx
    - frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts
    - frontend/src/lib/api.ts
    - .planning/phases/07-generic-upload-and-linking/07-02-SUMMARY.md
    - .planning/phases/07-generic-upload-and-linking/07-VERIFICATION.md
  </read_first>
  <action>Reuse the existing Phase 07-02 asset-kind seam instead of inventing new slot-specific edit mutations. In `AnimePatchForm.tsx`, expose `uploadAndLinkAsset` for reachable non-cover controls so the shared edit form can invoke the same config-driven mutation path for `banner`, `logo`, `background`, and `background_video`; keep `cover` support intact. Update `AnimeCoverField.tsx` only as needed to rename or broaden the operator-facing asset panel without reintroducing legacy cover-only wording. In `AnimeJellyfinMetadataSection.tsx`, expand `UploadTarget`, file inputs, progress labels, success/error copy, and action badges so `logo` and `background_video` have manual upload buttons and no longer present as provider-only. Preserve singular replacement for `cover`, `banner`, `logo`, and `background_video`, and preserve additive behavior for `background`.</action>
  <acceptance_criteria>
    - `AnimeJellyfinMetadataSection.tsx` contains manual upload triggers for `logo` and `background_video`.
    - `AnimeJellyfinMetadataSection.tsx` no longer contains active copy marking `logo` or `background_video` as `provider-only`.
    - `AnimePatchForm.tsx` invokes the generic `uploadAndLinkAsset` seam for at least one non-cover asset kind instead of only `uploadAndSetCover`.
    - Targeted edit tests pass with the new helper-copy expectations.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinMetadataSection.helpers.test.ts src/app/admin/anime/[id]/edit/page.test.tsx</automated>
    <automated>Get-Content frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinMetadataSection.tsx | Select-String -Pattern 'Logo hochladen','Background-Video hochladen'</automated>
    <automated>Get-Content frontend/src/app/admin/anime/components/AnimePatchForm/AnimePatchForm.tsx | Select-String -Pattern 'uploadAndLinkAsset'</automated>
  </verify>
  <done>The edit route exposes the already-built generic asset mutation seam for the full supported manual asset set.</done>
</task>

</tasks>

<verification>
Run the targeted edit-route frontend tests, then open an existing anime in the admin edit UI and verify `logo` and `background_video` each have a visible manual upload control alongside the existing cover/banner/background controls.
</verification>

<success_criteria>
The verifier's edit-route reachability gap is closed: `logo` and `background_video` are no longer provider-only and the generic Phase 07 seam is visible in the active admin edit UI.
</success_criteria>

<output>
After completion, create `.planning/phases/07-generic-upload-and-linking/07-03-SUMMARY.md`
</output>
