---
phase: 07-generic-upload-and-linking
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - frontend/src/types/admin.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/api.admin-anime.test.ts
  - frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts
  - frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts
  - frontend/src/app/admin/anime/components/AnimePatchForm/AnimeCoverField.tsx
  - frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx
  - frontend/src/app/admin/anime/create/page.test.tsx
  - frontend/src/app/admin/anime/[id]/edit/page.test.tsx
autonomous: true
requirements:
  - UPLD-01
  - UPLD-02
  - UPLD-03
must_haves:
  truths:
    - "The browser client can upload every supported anime asset type through one typed helper instead of one cover-only branch and several missing cases."
    - "Create and edit flows describe and consume the verified V2 upload seam rather than legacy `frontend/public/covers` behavior."
    - "The client keeps the singular-versus-background semantics visible without creating separate upload stacks per asset type."
    - "Frontend API and hook layers preserve structured upload and linking failures so operators can recover from slot-specific problems."
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "Typed upload and asset-linking helpers for the complete Phase 7 asset set"
    - path: "frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts"
      provides: "Reusable edit-route upload/link mutations based on asset kind instead of only cover"
    - path: "frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx"
      provides: "Create-route asset workflow copy aligned with the generic V2 upload seam"
  key_links:
    - from: "frontend/src/lib/api.ts"
      to: "backend/internal/handlers/admin_content_anime_assets.go"
      via: "typed asset-linking and delete calls per supported slot"
      pattern: "assignAdminAnime|deleteAdminAnime|addAdminAnimeBackgroundAsset"
    - from: "frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts"
      to: "frontend/src/lib/api.ts"
      via: "generic upload+link mutation flow keyed by asset kind"
      pattern: "uploadAdminAnimeMedia|assignAdminAnimeCoverAsset"
    - from: "frontend/src/app/admin/anime/components/AnimePatchForm/AnimeCoverField.tsx"
      to: "frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts"
      via: "operator-facing upload copy and mutation invocation"
      pattern: "onUploadFile|onRemoveCover"
---

<objective>
Generalize the frontend upload and linking workflow so create and edit can consume the full Phase 7 asset set through the verified V2 seam.

Purpose: remove the remaining cover-first assumptions from typed APIs, mutation hooks, and operator copy.
Output: client helpers, hook refactors, and UI/test updates for the supported anime asset types.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-generic-upload-and-linking/07-CONTEXT.md
@.planning/phases/07-generic-upload-and-linking/07-RESEARCH.md
@.planning/phases/07-generic-upload-and-linking/07-VALIDATION.md
@frontend/src/types/admin.ts
@frontend/src/lib/api.ts
@frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts
@frontend/src/app/admin/anime/components/AnimePatchForm/AnimeCoverField.tsx
@frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing frontend coverage for the full Phase 7 asset set and generic upload/link mutations</name>
  <files>frontend/src/lib/api.admin-anime.test.ts, frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts, frontend/src/app/admin/anime/create/page.test.tsx, frontend/src/app/admin/anime/[id]/edit/page.test.tsx</files>
  <read_first>
    - frontend/src/lib/api.ts
    - frontend/src/types/admin.ts
    - frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts
    - frontend/src/app/admin/anime/components/AnimePatchForm/AnimeCoverField.tsx
    - frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx
    - .planning/phases/07-generic-upload-and-linking/07-CONTEXT.md
    - .planning/phases/07-generic-upload-and-linking/07-RESEARCH.md
    - .planning/phases/07-generic-upload-and-linking/07-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: the typed upload helper accepts `poster`, `banner`, `logo`, `background`, and `background_video`.
    - Test 2: the edit mutation layer can upload and link a non-cover singular slot without a new slot-specific helper function.
    - Test 3: the background flow remains additive instead of overwriting a singular slot.
    - Test 4: create/edit UI copy no longer claims that active upload writes to `frontend/public/covers`.
    - Test 5: structured `ApiError.code` or `ApiError.details` still survive upload/link failures for the new asset kinds.
  </behavior>
  <action>Extend `frontend/src/lib/api.admin-anime.test.ts` to pin the expanded upload helper and new asset-linking calls for `logo` and `background_video`. Extend `frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts` so the edit mutation hook proves it can dispatch upload-plus-link behavior by asset kind rather than only by cover. Update the create and edit page tests to assert that user-facing asset help text points to the generic V2 seam and not to `frontend/public/covers`.</action>
  <acceptance_criteria>
    - Frontend API tests fail if `uploadAdminAnimeMedia` still rejects `logo` or `background_video`.
    - Mutation-hook tests fail if non-cover asset uploads still require dedicated hardcoded helper functions.
    - At least one component test asserts the legacy `frontend/public/covers` copy is gone.
    - At least one test asserts a structured upload or linking error remains available as `ApiError.code` or `ApiError.details`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- src/lib/api.admin-anime.test.ts</automated>
    <automated>cd frontend && npm test -- src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts</automated>
    <automated>cd frontend && npm test -- src/app/admin/anime/create/page.test.tsx src/app/admin/anime/[id]/edit/page.test.tsx</automated>
  </verify>
  <done>The frontend has a failing safety net for the full asset vocabulary, generic mutations, and corrected operator copy.</done>
</task>

<task type="auto">
  <name>Task 2: Implement generic client asset helpers and update create/edit flows to match the V2 seam</name>
  <files>frontend/src/types/admin.ts, frontend/src/lib/api.ts, frontend/src/lib/api.admin-anime.test.ts, frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts, frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts, frontend/src/app/admin/anime/components/AnimePatchForm/AnimeCoverField.tsx, frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx, frontend/src/app/admin/anime/create/page.test.tsx, frontend/src/app/admin/anime/[id]/edit/page.test.tsx</files>
  <read_first>
    - frontend/src/types/admin.ts
    - frontend/src/lib/api.ts
    - frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts
    - frontend/src/app/admin/anime/components/AnimePatchForm/AnimeCoverField.tsx
    - frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx
    - backend/internal/handlers/admin_content_anime_assets.go
    - .planning/phases/07-generic-upload-and-linking/07-CONTEXT.md
    - .planning/phases/07-generic-upload-and-linking/07-RESEARCH.md
  </read_first>
  <action>Expand `frontend/src/lib/api.ts` so `uploadAdminAnimeMedia` accepts the full typed Phase 7 asset set `poster | banner | logo | background | background_video`, and add typed client helpers for any new backend asset-linking endpoints introduced in Plan 01. Refactor `frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts` so upload-plus-link behavior is driven by an asset-kind config instead of a cover-only `uploadCoverFile` function; preserve singular-slot replacement for `cover`, `banner`, `logo`, and `background_video`, and preserve additive behavior for `background`. Update `frontend/src/app/admin/anime/components/AnimePatchForm/AnimeCoverField.tsx` and `frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx` so their copy explicitly describes the generic V2 upload seam and no longer references `frontend/public/covers` as active behavior.</action>
  <acceptance_criteria>
    - `frontend/src/lib/api.ts` exports a typed upload helper that accepts `poster`, `banner`, `logo`, `background`, and `background_video`.
    - The edit mutation hook no longer relies on a dedicated cover-only upload helper for the generalized asset workflow.
    - User-facing copy in create/edit asset areas no longer says uploads write to `frontend/public/covers`.
    - `cd frontend && npm test -- src/lib/api.admin-anime.test.ts src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts src/app/admin/anime/create/page.test.tsx src/app/admin/anime/[id]/edit/page.test.tsx` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- src/lib/api.admin-anime.test.ts</automated>
    <automated>cd frontend && npm test -- src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts</automated>
    <automated>cd frontend && npm test -- src/app/admin/anime/create/page.test.tsx src/app/admin/anime/[id]/edit/page.test.tsx</automated>
  </verify>
  <done>The frontend now speaks the full Phase 7 asset vocabulary and points operators at the real V2 upload seam.</done>
</task>

</tasks>

<verification>
Run the targeted frontend tests, then manually use the local admin UI to upload at least one `banner` and one `logo` through the create or edit surface and confirm the resulting asset URLs resolve after refresh without any legacy cover-directory wording.
</verification>

<success_criteria>
Phase 7 has a frontend workflow that consumes the same V2 upload seam for all supported anime asset types instead of preserving a cover-only special case.
</success_criteria>

<output>
After completion, create `.planning/phases/07-generic-upload-and-linking/07-02-SUMMARY.md`
</output>
