---
phase: 03-jellyfin-assisted-intake
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - frontend/src/app/admin/anime/create/page.tsx
  - frontend/src/app/admin/anime/create/page.test.tsx
  - frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx
  - frontend/src/app/admin/anime/components/ManualCreate/JellyfinDraftAssets.tsx
  - frontend/src/app/admin/anime/components/ManualCreate/JellyfinDraftAssets.test.tsx
  - frontend/src/app/admin/anime/utils/jellyfin-intake-type-hint.ts
  - frontend/src/app/admin/anime/utils/jellyfin-intake-type-hint.test.ts
  - frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts
  - frontend/src/types/admin.ts
autonomous: true
requirements:
  - INTK-03
  - JFIN-04
  - JFIN-05
  - JFIN-06
must_haves:
  truths:
    - "After selecting a Jellyfin candidate, admins land in the same shared create draft already prefilled and still unsaved."
    - "The draft shows Jellyfin-provided metadata, asset-slot candidates, and a visibly reasoned advisory type suggestion that remains editable."
    - "Admins can remove unwanted imported draft assets or cancel the Jellyfin-assisted draft entirely without creating any Team4s anime."
  artifacts:
    - path: "frontend/src/app/admin/anime/create/page.tsx"
      provides: "Shared create route with title-adjacent Jellyfin and AniSearch triggers plus draft hydration"
    - path: "frontend/src/app/admin/anime/components/ManualCreate/JellyfinDraftAssets.tsx"
      provides: "Draft-level asset review and removal surface for imported Jellyfin assets"
    - path: "frontend/src/app/admin/anime/utils/jellyfin-intake-type-hint.ts"
      provides: "UI formatting helper for advisory type suggestion and reasoning"
    - path: "frontend/src/app/admin/anime/create/page.test.tsx"
      provides: "Regression coverage for prefilled draft, cancel-without-persist, and placeholder AniSearch button"
  key_links:
    - from: "frontend/src/app/admin/anime/create/page.tsx"
      to: "frontend/src/app/admin/anime/hooks/useJellyfinIntake.ts"
      via: "candidate search and preview hydration"
      pattern: "useJellyfinIntake"
    - from: "frontend/src/app/admin/anime/create/page.tsx"
      to: "frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx"
      via: "shared create draft composition"
      pattern: "ManualCreateWorkspace"
    - from: "frontend/src/app/admin/anime/components/ManualCreate/JellyfinDraftAssets.tsx"
      to: "frontend/src/types/admin.ts"
      via: "draft asset-slot rendering"
      pattern: "background_video|banner|logo|background|cover"
---

<objective>
Hydrate the shared create draft from a chosen Jellyfin candidate and let operators review or reject the imported draft before any save.

Purpose: Fulfill the core Phase 3 workflow without forking the create experience: one draft surface, one save bar, and explicit operator control over metadata and assets.
Output: The shared `/admin/anime/create` route with title-adjacent source actions, Jellyfin draft hydration, draft-level asset removal, advisory type hint display, and cancel-without-persist behavior.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-jellyfin-assisted-intake/03-CONTEXT.md
@.planning/phases/03-jellyfin-assisted-intake/03-UI-SPEC.md
@.planning/phases/03-jellyfin-assisted-intake/03-02-PLAN.md
@.planning/phases/02-manual-intake-baseline/02-03-SUMMARY.md
@frontend/src/app/admin/anime/create/page.tsx
@frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx
@frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts
@frontend/src/app/admin/anime/components/shared/AnimeEditorShell.tsx

<interfaces>
Existing create seam:
```ts
export function resolveManualCreateState(input: ManualAnimeDraftInput): ManualAnimeDraftState
export function useAnimeEditor(context: AnimeEditorContext, input: AnimeEditorControllerInput): AnimeEditorController
```

This plan should keep the shared create route and save bar, then layer Jellyfin draft state on top.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing create-route coverage for Jellyfin-assisted draft hydration and safe cancellation</name>
  <files>frontend/src/app/admin/anime/create/page.test.tsx, frontend/src/app/admin/anime/components/ManualCreate/JellyfinDraftAssets.test.tsx, frontend/src/app/admin/anime/utils/jellyfin-intake-type-hint.test.ts</files>
  <read_first>
    - frontend/src/app/admin/anime/create/page.tsx
    - frontend/src/app/admin/anime/create/page.test.tsx
    - .planning/phases/03-jellyfin-assisted-intake/03-CONTEXT.md
    - .planning/phases/03-jellyfin-assisted-intake/03-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: The title field renders adjacent `Jellyfin Sync` and `AniSearch Sync` buttons, both disabled until the title is meaningful per D-01 and D-02.
    - Test 2: `AniSearch Sync` stays placeholder-only in Phase 3 and performs no data import per D-03.
    - Test 3: Choosing a Jellyfin candidate hydrates the same shared create draft immediately, without creating an anime first per D-10.
    - Test 4: Draft asset removal happens only after hydration, inside the draft surface, not on candidate cards per D-11.
    - Test 5: Canceling or leaving the Jellyfin-assisted draft does not call create and leaves no hidden persistence per INTK-03.
    - Test 6: The advisory type suggestion renders reasons and does not lock the editable type field per D-13 and D-14.
  </behavior>
  <action>Add failing tests for the Phase 3 create route and the new draft asset review surface. Cover the adjacent button cluster, title gating, placeholder-only AniSearch action, compact-review-to-draft handoff, asset removal from the draft, and cancel-without-create semantics. Add tests for deterministic empty-slot states and removal affordance copy so missing assets and post-import removal are visible and operator-safe. Required Phase 3 copy in draft assets: missing slots must use explicit placeholder text such as `Kein Jellyfin-Banner gefunden` or equivalent per slot, and imported slots must expose remove copy like `Aus Entwurf entfernen` with helper text clarifying that the anime remains unsaved until the central save bar is used.</action>
  <acceptance_criteria>
    - `frontend/src/app/admin/anime/create/page.test.tsx` asserts the two adjacent source buttons and disabled-until-meaningful-title behavior.
    - `frontend/src/app/admin/anime/create/page.test.tsx` asserts the AniSearch control is placeholder-only and does not fetch data.
    - `frontend/src/app/admin/anime/components/ManualCreate/JellyfinDraftAssets.test.tsx` asserts both explicit empty-slot placeholders and `Aus Entwurf entfernen` copy.
    - Tests assert canceling the draft does not call `createAdminAnime`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- src/app/admin/anime/create/page.test.tsx src/app/admin/anime/components/ManualCreate/JellyfinDraftAssets.test.tsx src/app/admin/anime/utils/jellyfin-intake-type-hint.test.ts</automated>
  </verify>
  <done>The create-route test slice captures the Phase 3 source button cluster, draft hydration, advisory type hint, draft-only asset removal, explicit placeholder states, and no-hidden-persistence cancellation contract.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Jellyfin draft hydration into the shared create page and draft asset review</name>
  <files>frontend/src/app/admin/anime/create/page.tsx, frontend/src/app/admin/anime/create/page.test.tsx, frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx, frontend/src/app/admin/anime/components/ManualCreate/JellyfinDraftAssets.tsx, frontend/src/app/admin/anime/components/ManualCreate/JellyfinDraftAssets.test.tsx, frontend/src/app/admin/anime/utils/jellyfin-intake-type-hint.ts, frontend/src/app/admin/anime/utils/jellyfin-intake-type-hint.test.ts, frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts, frontend/src/types/admin.ts</files>
  <read_first>
    - frontend/src/app/admin/anime/create/page.tsx
    - frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx
    - frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts
    - frontend/src/app/admin/anime/hooks/useJellyfinIntake.ts
    - .planning/phases/03-jellyfin-assisted-intake/03-UI-SPEC.md
  </read_first>
  <action>Extend the existing create route instead of creating a second wizard. Add the title-adjacent `Jellyfin Sync` and placeholder-only `AniSearch Sync` controls per D-01 through D-03. Wire `Jellyfin Sync` into the Phase 3 intake hook from `03-02`: compact chooser first, rich review second, then immediate draft hydration in the same create page per D-04, D-07, and D-10. Keep the central save bar as the only persistence mechanism per D-12. Add draft-level imported asset review for cover/logo/banner/background/background_video with explicit per-slot placeholder states and `Aus Entwurf entfernen` copy; do not add these controls to candidate cards. Show the advisory type suggestion next to the editable type field, including confidence tone and plain-language reasons from path/name context, but keep the field fully editable. Preserve `resolveManualCreateState` as the readiness seam so the draft remains unsaved until explicit create.</action>
  <acceptance_criteria>
    - `frontend/src/app/admin/anime/create/page.tsx` renders adjacent `Jellyfin Sync` and `AniSearch Sync` controls near the title field.
    - The create route uses the same `ManualCreateWorkspace` rather than a second create shell.
    - `frontend/src/app/admin/anime/components/ManualCreate/JellyfinDraftAssets.tsx` exists and renders all five asset slots with explicit empty-state copy.
    - The type field remains editable after hydration and shows reasoned suggestion UI beside it.
    - No file in this plan persists a draft before the main create submit path runs.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- src/app/admin/anime/create/page.test.tsx src/app/admin/anime/components/ManualCreate/JellyfinDraftAssets.test.tsx src/app/admin/anime/utils/jellyfin-intake-type-hint.test.ts</automated>
  </verify>
  <done>The shared create route now supports Jellyfin-assisted draft hydration, draft-only asset removal, advisory type reasoning, placeholder AniSearch UI, and cancel-without-persist semantics while still relying on the central save bar.</done>
</task>

</tasks>

<verification>
Run the focused create-route Jellyfin draft tests. Manual smoke after execution should confirm candidate selection opens the same draft immediately, imported assets can be removed from the draft only, and leaving the page without save creates nothing.
</verification>

<success_criteria>
Operators can search Jellyfin, review a candidate, land in the shared prefilled draft, remove unwanted imported assets there, and cancel safely without any hidden persistence.
</success_criteria>

<output>
After completion, create `.planning/phases/03-jellyfin-assisted-intake/03-03-SUMMARY.md`
</output>
