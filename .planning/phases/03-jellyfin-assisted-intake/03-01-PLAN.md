---
phase: 03-jellyfin-assisted-intake
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/cmd/server/main.go
  - backend/internal/handlers/jellyfin_search.go
  - backend/internal/handlers/jellyfin_intake_preview.go
  - backend/internal/handlers/jellyfin_intake_preview_test.go
  - backend/internal/handlers/jellyfin_search_test.go
  - backend/internal/handlers/jellyfin_intake_helpers.go
  - backend/internal/models/admin_jellyfin_intake.go
autonomous: true
requirements:
  - JFIN-01
  - JFIN-02
  - JFIN-04
  - JFIN-05
  - JFIN-06
must_haves:
  truths:
    - "Jellyfin intake search returns compact candidates the create flow can rank and review without persisting anything."
    - "Each candidate exposes enough evidence for operator confidence: title, Jellyfin ID, full path, parent or library context, type hint, and poster/banner/logo/background previews."
    - "Selecting one candidate can load a draft-only preview payload with metadata, asset candidates, and a visibly reasoned advisory type suggestion."
  artifacts:
    - path: "backend/internal/handlers/jellyfin_search.go"
      provides: "Expanded Jellyfin intake search response with compact-first ranking data"
    - path: "backend/internal/handlers/jellyfin_intake_preview.go"
      provides: "Draft-only Jellyfin intake preview endpoint keyed by Jellyfin candidate selection"
    - path: "backend/internal/models/admin_jellyfin_intake.go"
      provides: "Phase 3 search and preview response structs for intake evidence and asset candidates"
    - path: "backend/internal/handlers/jellyfin_intake_preview_test.go"
      provides: "Coverage for preview-only payload mapping, asset slots, and advisory type reasoning"
  key_links:
    - from: "backend/internal/handlers/jellyfin_search.go"
      to: "backend/internal/models/admin_jellyfin_intake.go"
      via: "search response mapping"
      pattern: "AdminJellyfinIntake"
    - from: "backend/internal/handlers/jellyfin_intake_preview.go"
      to: "backend/internal/handlers/jellyfin_intake_helpers.go"
      via: "type-hint and asset-evidence mapping"
      pattern: "type hint|asset"
    - from: "backend/cmd/server/main.go"
      to: "backend/internal/handlers/jellyfin_intake_preview.go"
      via: "admin route registration"
      pattern: "jellyfin.*intake.*preview"
---

<objective>
Define the backend intake contracts that let Phase 3 search Jellyfin, inspect candidates, and hydrate a preview-only create draft.

Purpose: Establish the draft-safe data shape first so later frontend plans can reuse one stable search and preview seam instead of inventing payloads in the UI.
Output: Tested Jellyfin intake search and preview endpoints with rich candidate evidence, advisory type reasoning, and asset candidate payloads.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-jellyfin-assisted-intake/03-CONTEXT.md
@.planning/phases/03-jellyfin-assisted-intake/03-RESEARCH.md
@backend/internal/handlers/jellyfin_search.go
@backend/internal/handlers/jellyfin_preview.go
@backend/internal/handlers/jellyfin_resolution.go
@frontend/src/types/admin.ts
@frontend/src/lib/api.ts

<interfaces>
From frontend/src/types/admin.ts:
```ts
export interface AdminJellyfinSeriesSearchItem {
  jellyfin_series_id: string
  name: string
  production_year?: number
  path?: string
}

export interface AdminAnimeJellyfinPreviewResult {
  anime_id: number
  jellyfin_series_id: string
  jellyfin_series_name: string
  jellyfin_series_path?: string
  episodes: AdminAnimeJellyfinPreviewEpisode[]
}
```

Existing backend seam to evolve:
```go
func (h *AdminContentHandler) SearchJellyfinSeries(c *gin.Context)
func (h *AdminContentHandler) PreviewAnimeFromJellyfin(c *gin.Context)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the Phase 3 Jellyfin intake contracts in backend tests and response structs</name>
  <files>backend/internal/models/admin_jellyfin_intake.go, backend/internal/handlers/jellyfin_intake_preview_test.go, backend/internal/handlers/jellyfin_search_test.go, backend/internal/handlers/jellyfin_intake_helpers.go</files>
  <read_first>
    - .planning/phases/03-jellyfin-assisted-intake/03-CONTEXT.md
    - .planning/phases/03-jellyfin-assisted-intake/03-RESEARCH.md
    - .planning/phases/03-jellyfin-assisted-intake/03-UI-SPEC.md
    - backend/internal/handlers/jellyfin_search.go
    - backend/internal/handlers/jellyfin_preview.go
    - frontend/src/types/admin.ts
  </read_first>
  <behavior>
    - Test 1: Search results rank stronger title or folder matches first per D-05 and D-06 while still returning multiple manual choices.
    - Test 2: Every candidate includes title, Jellyfin ID, full path, parent or library context, advisory type hint, and poster/banner/logo/background preview references per D-08 and D-09.
    - Test 3: Intake preview returns draft metadata candidates for description, year, genres or tags, AniDB ID, asset slots, and a reasoned type suggestion without creating a Team4s anime.
    - Test 4: Type suggestion remains advisory only and includes plain-language reasons from folder or name context per D-13 and D-14.
    - Test 5: Missing assets return explicit empty-slot payloads instead of omitted keys so the draft can render deterministic placeholders.
  </behavior>
  <action>Create the Phase 3 intake response structs in `backend/internal/models/admin_jellyfin_intake.go` and add failing handler tests before implementation. The search contract must stay compact-first for the initial chooser, but each result still needs enough evidence for the later rich-review step: title, Jellyfin ID, full path, parent/library context, compact confidence signal, advisory type hint, and preview URLs or proxy references for poster/banner/logo/background per D-04, D-08, and D-09. The preview contract must be draft-only: no `anime_id` dependency, no persistence, and no episode-sync statistics reused from the existing persisted-anime preview. Include explicit asset-slot objects for cover/logo/banner/background/background_video with `present`, `kind`, `source`, and placeholder-ready empty states so Phase 3 can show missing-slot copy deterministically.</action>
  <acceptance_criteria>
    - `backend/internal/models/admin_jellyfin_intake.go` defines separate Phase 3 search and preview structs instead of reusing the persisted-anime preview result.
    - `backend/internal/handlers/jellyfin_search_test.go` asserts candidate evidence includes Jellyfin ID, full path, parent/library context, and preview references.
    - `backend/internal/handlers/jellyfin_intake_preview_test.go` asserts the preview payload contains metadata plus all five asset slots, including empty-slot representations.
    - Tests assert the type suggestion includes at least one human-readable reason string.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/handlers -run "Test.*Jellyfin.*(Search|IntakePreview)" -count=1</automated>
  </verify>
  <done>The backend has failing then passing tests that define the full Phase 3 intake evidence contract, including compact-first search data, rich candidate evidence, advisory type reasoning, and placeholder-ready asset slots.</done>
</task>

<task type="auto">
  <name>Task 2: Implement draft-safe Jellyfin search enrichment and the new intake preview endpoint</name>
  <files>backend/cmd/server/main.go, backend/internal/handlers/jellyfin_search.go, backend/internal/handlers/jellyfin_intake_preview.go, backend/internal/handlers/jellyfin_intake_preview_test.go, backend/internal/handlers/jellyfin_search_test.go, backend/internal/handlers/jellyfin_intake_helpers.go, backend/internal/models/admin_jellyfin_intake.go</files>
  <read_first>
    - backend/internal/handlers/jellyfin_search.go
    - backend/internal/handlers/jellyfin_preview.go
    - backend/internal/handlers/jellyfin_resolution.go
    - backend/internal/handlers/jellyfin_client.go
    - backend/cmd/server/main.go
    - .planning/phases/03-jellyfin-assisted-intake/03-CONTEXT.md
  </read_first>
  <action>Extend `SearchJellyfinSeries` into a Phase 3 intake search seam instead of a persisted-anime sync helper. Keep the request compact and title-anchored per D-01 and D-04, but enrich each result with explicit evidence fields and stable preview references for poster/banner/logo/background. Add `backend/internal/handlers/jellyfin_intake_preview.go` for a draft-only preview endpoint keyed by a chosen Jellyfin candidate, not by an existing anime record. Map upstream Jellyfin metadata into editable create-draft values for description, year, genre/tag string, AniDB ID when available, asset-slot candidates, and advisory type suggestion reasons from path/name context per D-13 and D-14. Reuse backend-mediated proxy or safe URL generation only; do not require browser-direct Jellyfin access. Register the new route in `backend/cmd/server/main.go`. Keep all behavior preview-only: no anime insert, no audit write beyond normal request logs, no hidden persistence before save.</action>
  <acceptance_criteria>
    - `backend/internal/handlers/jellyfin_intake_preview.go` exists and does not call anime create or sync mutation paths.
    - `backend/cmd/server/main.go` registers a dedicated Phase 3 intake preview route.
    - Search results expose path and Jellyfin ID plus library/parent context and preview references.
    - Preview payload includes `description`, `year`, `genre` or tags, `anidb_id` when available, asset candidates, and advisory type reasoning.
    - No file in this plan reuses the existing persisted-anime preview response struct for intake payloads.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/handlers -run "Test.*Jellyfin.*(Search|IntakePreview)" -count=1</automated>
  </verify>
  <done>The backend now exposes a Phase 3-specific Jellyfin intake search plus preview contract that later plans can consume without touching persistence before explicit save.</done>
</task>

</tasks>

<verification>
Run the focused Jellyfin handler slice. If package-wide handler tests remain blocked by the known external compile issue, keep verification scoped to the touched handler tests and document the blocker in the summary.
</verification>

<success_criteria>
Phase 3 has a stable backend intake contract: compact-first candidate search, rich evidence for operator review, draft-only preview hydration data, and advisory type reasoning with explicit empty-slot asset states.
</success_criteria>

<output>
After completion, create `.planning/phases/03-jellyfin-assisted-intake/03-01-SUMMARY.md`
</output>
