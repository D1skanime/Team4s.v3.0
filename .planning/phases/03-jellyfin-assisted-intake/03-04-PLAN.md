---
phase: 03-jellyfin-assisted-intake
plan: 04
type: execute
wave: 5
depends_on:
  - "03-03"
files_modified:
  - frontend/src/types/admin.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/api/admin-anime-intake.ts
  - frontend/src/app/admin/anime/create/page.tsx
  - frontend/src/app/admin/anime/create/page.test.tsx
  - backend/internal/handlers/admin_content_anime_validation.go
  - backend/internal/handlers/admin_content_test.go
autonomous: true
requirements:
  - INTK-03
  - JFIN-04
must_haves:
  truths:
    - "Saving a Jellyfin-assisted draft still happens only through the explicit create action."
    - "The saved anime can carry forward its Jellyfin linkage for later phases without introducing hidden persistence during preview."
    - "Canceling or abandoning the draft still creates nothing."
  artifacts:
    - path: "frontend/src/types/admin.ts"
      provides: "Explicit create payload support for optional Jellyfin linkage fields"
    - path: "backend/internal/handlers/admin_content_anime_validation.go"
      provides: "Server-side validation and normalization for explicit Jellyfin linkage on create"
    - path: "backend/internal/handlers/admin_content_test.go"
      provides: "Regression coverage for manual-only create plus optional Jellyfin-assisted create linkage"
  key_links:
    - from: "frontend/src/app/admin/anime/create/page.tsx"
      to: "frontend/src/lib/api/admin-anime-intake.ts"
      via: "explicit create submit"
      pattern: "createAdminAnime|createAdminAnimeFromJellyfinDraft"
    - from: "frontend/src/lib/api/admin-anime-intake.ts"
      to: "backend/internal/handlers/admin_content_anime_validation.go"
      via: "AdminAnimeCreateRequest"
      pattern: "source|folder_name|jellyfin"
    - from: "backend/internal/handlers/admin_content_anime_validation.go"
      to: "existing source/folder_name seams"
      via: "normalized create input"
      pattern: "source|folder_name"
---

<objective>
Finish Phase 3 by making the explicit create action capable of persisting Jellyfin linkage only when the operator actually saves the draft.

Purpose: Preserve the Phase 3 no-hidden-persistence rule while future-proofing saved records for Phase 4 provenance and resync work.
Output: Optional Jellyfin linkage fields in the create payload, backend validation, and regression coverage proving that preview remains non-persistent until explicit save.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-jellyfin-assisted-intake/03-RESEARCH.md
@.planning/phases/03-jellyfin-assisted-intake/03-03-PLAN.md
@.planning/phases/02-manual-intake-baseline/02-02-SUMMARY.md
@frontend/src/types/admin.ts
@frontend/src/lib/api.ts
@frontend/src/lib/api/admin-anime-intake.ts
@frontend/src/app/admin/anime/create/page.tsx
@backend/internal/handlers/admin_content_anime_validation.go
@backend/internal/repository/admin_content_sync.go

<interfaces>
Current create contract:
```ts
export interface AdminAnimeCreateRequest {
  title: string
  type: AnimeType
  content_type: ContentType
  status: AnimeStatus
  title_de?: string
  title_en?: string
  year?: number
  max_episodes?: number
  genre?: string
  description?: string
  cover_image?: string
}
```

Existing backend/source seam:
```go
func (r *AdminContentRepository) GetAnimeSyncSource(ctx context.Context, animeID int64) (*models.AdminAnimeSyncSource, error)
```

Phase 3 client placement constraint:
```text
frontend/src/lib/api/admin-anime-intake.ts = main new intake/create client implementation
frontend/src/lib/api.ts = thin export/compat layer only if needed
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing coverage for explicit-save Jellyfin linkage without breaking manual create</name>
  <files>frontend/src/app/admin/anime/create/page.test.tsx, backend/internal/handlers/admin_content_test.go</files>
  <read_first>
    - frontend/src/app/admin/anime/create/page.test.tsx
    - backend/internal/handlers/admin_content_test.go
    - backend/internal/handlers/admin_content_anime_validation.go
    - .planning/phases/03-jellyfin-assisted-intake/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Saving a Jellyfin-assisted draft posts optional linkage fields only on explicit create, never during preview or candidate selection.
    - Test 2: Canceling the hydrated draft still never posts create.
    - Test 3: Manual-only create still works unchanged when no Jellyfin source is present.
    - Test 4: Backend validation accepts normalized optional linkage fields for Jellyfin-assisted create and rejects malformed values.
  </behavior>
  <action>Add failing frontend and backend coverage for the explicit-save contract. The frontend test should assert that no create call is made until the save bar submits and that the eventual payload includes the selected Jellyfin linkage fields only after explicit save. The backend tests should preserve the existing manual-only create path while adding coverage for optional normalized Jellyfin linkage data. Use the existing source seams rather than inventing a second linkage model.</action>
  <acceptance_criteria>
    - Frontend tests distinguish preview/hydration from explicit save.
    - Backend tests cover both manual create and Jellyfin-assisted create payloads.
    - Tests reject malformed linkage values but allow absent linkage for manual-only create.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- src/app/admin/anime/create/page.test.tsx && cd ../backend && go test ./internal/handlers -run "Test.*AdminAnimeCreate.*|Test.*ValidateAdminAnimeCreateRequest.*" -count=1</automated>
  </verify>
  <done>The red-green test slice proves that preview stays non-persistent, explicit save can carry Jellyfin linkage, and manual-only create remains intact.</done>
</task>

<task type="auto">
  <name>Task 2: Wire optional Jellyfin linkage through the explicit create path only</name>
  <files>frontend/src/types/admin.ts, frontend/src/lib/api.ts, frontend/src/lib/api/admin-anime-intake.ts, frontend/src/app/admin/anime/create/page.tsx, frontend/src/app/admin/anime/create/page.test.tsx, backend/internal/handlers/admin_content_anime_validation.go, backend/internal/handlers/admin_content_test.go</files>
  <read_first>
    - frontend/src/types/admin.ts
    - frontend/src/app/admin/anime/create/page.tsx
    - frontend/src/lib/api/admin-anime-intake.ts
    - backend/internal/handlers/admin_content_anime_validation.go
    - backend/internal/repository/admin_content_sync.go
  </read_first>
  <action>Extend the explicit create payload with optional Jellyfin linkage fields using the existing source seams, not a new persistence model. Put any new Phase 3 frontend create helper implementation in `frontend/src/lib/api/admin-anime-intake.ts`; `frontend/src/lib/api.ts` may only add a thin export or compatibility shim if absolutely needed because it already violates the file-budget rule. The recommended shape is to carry a normalized `source` value such as `jellyfin:{series_id}` plus a stored `folder_name` or equivalent path context only when a Jellyfin candidate has been chosen and the admin explicitly saves. Keep preview, candidate review, and draft hydration non-persistent. Preserve the Phase 2 manual-only path when no source is selected. Update backend validation to normalize and forward the optional linkage fields without weakening the existing title-plus-cover contract. Do not introduce any automatic save on candidate selection, search, or page navigation.</action>
  <acceptance_criteria>
    - `frontend/src/types/admin.ts` documents optional linkage fields on `AdminAnimeCreateRequest`.
    - `frontend/src/lib/api/admin-anime-intake.ts` contains the main Phase 3 create helper implementation for Jellyfin-assisted draft save.
    - `frontend/src/lib/api.ts` is untouched or changed only as a thin export or compatibility layer.
    - `frontend/src/app/admin/anime/create/page.tsx` sends linkage fields only during explicit create submit.
    - `backend/internal/handlers/admin_content_anime_validation.go` keeps `title + cover` required and normalizes optional linkage values.
    - Manual create with no Jellyfin selection still posts the same minimal contract as Phase 2.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- src/app/admin/anime/create/page.test.tsx && cd ../backend && go test ./internal/handlers -run "Test.*AdminAnimeCreate.*|Test.*ValidateAdminAnimeCreateRequest.*" -count=1</automated>
  </verify>
  <done>Phase 3 ends with a future-safe explicit save path: nothing persists before save, but saved Jellyfin-assisted anime can carry source linkage forward for Phase 4.</done>
</task>

</tasks>

<verification>
Run the focused frontend create test and backend create-validation slice. The final manual smoke check should confirm that candidate selection and draft hydration stay non-persistent, and only clicking the central create action writes a new anime with optional Jellyfin linkage.
</verification>

<success_criteria>
The Phase 3 workflow stays preview-only until save, yet an explicitly saved Jellyfin-assisted anime can preserve its source linkage for later provenance and resync behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/03-jellyfin-assisted-intake/03-04-SUMMARY.md`
</output>
