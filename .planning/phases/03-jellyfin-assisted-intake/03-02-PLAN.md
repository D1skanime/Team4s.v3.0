---
phase: 03-jellyfin-assisted-intake
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - frontend/src/types/admin.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/api/admin-anime-intake.ts
  - frontend/src/app/admin/anime/hooks/useJellyfinIntake.ts
  - frontend/src/app/admin/anime/hooks/internal/useJellyfinIntakeImpl.ts
  - frontend/src/app/admin/anime/hooks/useJellyfinIntake.test.ts
autonomous: true
requirements:
  - JFIN-01
  - JFIN-02
  - JFIN-06
must_haves:
  truths:
    - "Admins can start from a compact title-adjacent Jellyfin chooser that stays gated until the title input is meaningful."
    - "The frontend keeps Jellyfin search and intake preview calls typed and draft-safe before any shared create-page hydration begins."
    - "The intake hook exposes advisory type hints and compact candidate evidence without auto-selecting a match."
  artifacts:
    - path: "frontend/src/types/admin.ts"
      provides: "Phase 3 intake search and preview types shared by hook and UI plans"
    - path: "frontend/src/lib/api/admin-anime-intake.ts"
      provides: "Main typed Phase 3 search and intake-preview client helpers"
    - path: "frontend/src/app/admin/anime/hooks/useJellyfinIntake.ts"
      provides: "Public intake hook seam for compact search and candidate review state"
    - path: "frontend/src/app/admin/anime/hooks/internal/useJellyfinIntakeImpl.ts"
      provides: "Internal intake orchestration for search, review-open, and preview hydration"
  key_links:
    - from: "frontend/src/app/admin/anime/hooks/internal/useJellyfinIntakeImpl.ts"
      to: "frontend/src/lib/api/admin-anime-intake.ts"
      via: "typed search and preview API calls"
      pattern: "searchAdminJellyfinIntakeCandidates|previewAdminAnimeFromJellyfinIntake"
    - from: "frontend/src/app/admin/anime/hooks/useJellyfinIntake.ts"
      to: "frontend/src/app/admin/anime/hooks/internal/useJellyfinIntakeImpl.ts"
      via: "public hook re-export"
      pattern: "useJellyfinIntake"
    - from: "frontend/src/types/admin.ts"
      to: "frontend/src/lib/api/admin-anime-intake.ts"
      via: "request and response typing"
      pattern: "AdminAnimeJellyfinIntake|AdminJellyfin"
---

<objective>
Build the typed frontend intake seam that turns a title query into compact Jellyfin candidates and a preview-ready selection state.

Purpose: Isolate the API typing and hook orchestration from the richer candidate-card UI so the phase stays within scope and downstream plans can depend on a stable intake seam.
Output: Shared intake types, dedicated API client helpers, and a public `useJellyfinIntake` hook with focused test coverage.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-jellyfin-assisted-intake/03-CONTEXT.md
@.planning/phases/03-jellyfin-assisted-intake/03-UI-SPEC.md
@.planning/phases/03-jellyfin-assisted-intake/03-01-PLAN.md
@.planning/phases/02-manual-intake-baseline/02-03-SUMMARY.md
@frontend/src/app/admin/anime/hooks/useJellyfinSync.ts
@frontend/src/app/admin/anime/hooks/internal/useJellyfinSyncImpl.ts
@frontend/src/lib/api.ts
@frontend/src/lib/api/admin-anime-intake.ts
@frontend/src/types/admin.ts

<interfaces>
Current frontend seams to extend:
```ts
export async function searchAdminJellyfinIntakeCandidates(query: string, params?: { limit?: number }, authToken?: string)
export async function previewAdminAnimeFromJellyfinIntake(payload: AdminAnimeJellyfinIntakePreviewRequest, authToken?: string)
export { useJellyfinSync } from './internal/useJellyfinSyncImpl'
```

Implement these in `frontend/src/lib/api/admin-anime-intake.ts`. `frontend/src/lib/api.ts` may only act as a thin re-export or compatibility layer if the executor needs one; it must not contain the main new Phase 3 client implementation because the file already exceeds the project file-size limit.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing frontend coverage for typed intake search and compact-to-review hook behavior</name>
  <files>frontend/src/app/admin/anime/hooks/useJellyfinIntake.test.ts</files>
  <read_first>
    - .planning/phases/03-jellyfin-assisted-intake/03-UI-SPEC.md
    - .planning/phases/03-jellyfin-assisted-intake/03-01-PLAN.md
    - frontend/src/app/admin/anime/hooks/internal/useJellyfinSyncImpl.ts
    - frontend/src/app/admin/anime/create/page.test.tsx
  </read_first>
  <behavior>
    - Test 1: The intake hook keeps Jellyfin search unavailable until the title input is meaningful per D-02.
    - Test 2: Compact results can open a review state instead of skipping directly to draft hydration per D-04 and D-10.
    - Test 3: The hook surfaces compact candidate evidence and advisory type hint data without hidden selection or asset deselection per D-08, D-11, and D-16.
  </behavior>
  <action>Create failing tests for the new intake hook seam only. Use the repo's existing frontend test style, not new libraries. Cover title gating, typed search state, compact-to-review transitions, and the no-hidden-selection rule so the later UI plan can build on a fixed hook contract.</action>
  <acceptance_criteria>
    - `frontend/src/app/admin/anime/hooks/useJellyfinIntake.test.ts` exists and covers title gating plus compact-to-review transitions.
    - Tests assert the hook exposes advisory type-hint data and does not auto-hydrate the draft on the first compact result.
    - Tests assert no candidate-level asset deselection behavior is present in the hook contract.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- src/app/admin/anime/hooks/useJellyfinIntake.test.ts</automated>
  </verify>
  <done>The intake hook contract is locked before implementation, with focused coverage for title gating, compact search, and explicit review-step transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the typed intake client and hook seam</name>
  <files>frontend/src/types/admin.ts, frontend/src/lib/api.ts, frontend/src/lib/api/admin-anime-intake.ts, frontend/src/app/admin/anime/hooks/useJellyfinIntake.ts, frontend/src/app/admin/anime/hooks/internal/useJellyfinIntakeImpl.ts, frontend/src/app/admin/anime/hooks/useJellyfinIntake.test.ts</files>
  <read_first>
    - frontend/src/types/admin.ts
    - frontend/src/lib/api.ts
    - frontend/src/lib/api/admin-anime-intake.ts
    - frontend/src/app/admin/anime/hooks/internal/useJellyfinSyncImpl.ts
    - .planning/phases/03-jellyfin-assisted-intake/03-UI-SPEC.md
  </read_first>
  <action>Implement a dedicated Phase 3 intake hook and typed API client wrappers around the backend search and preview contracts from `03-01`, but put the real client implementation in `frontend/src/lib/api/admin-anime-intake.ts` to avoid deepening the existing `frontend/src/lib/api.ts` monolith. `frontend/src/lib/api.ts` may only add a thin re-export or compatibility shim if the executor needs it. Build the compact-first chooser state per D-04 through D-06 and D-16 through D-17: title-adjacent search gating, compact result ranking metadata, explicit review-open transitions, and advisory type hint data with reasons. Do not implement the rich candidate cards, Jellyfin-only CSS modules, or candidate-review rendering in this plan; that UI work is split into the next plan. Do not add hidden auto-selection, asset deselection, or any AniSearch data calls.</action>
  <acceptance_criteria>
    - `frontend/src/lib/api/admin-anime-intake.ts` contains the main Phase 3 search and intake-preview client helpers.
    - `frontend/src/lib/api.ts` is untouched or changed only as a thin export or compatibility layer.
    - `frontend/src/app/admin/anime/hooks/useJellyfinIntake.ts` exports the public hook seam.
    - The hook exposes compact search results, review-open state, and advisory type-hint data without rendering concerns embedded in the client layer.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- src/app/admin/anime/hooks/useJellyfinIntake.test.ts</automated>
  </verify>
  <done>The frontend now has a stable typed intake seam for Jellyfin search and preview orchestration, ready for a separate candidate-card UI plan.</done>
</task>

</tasks>

<verification>
Run the focused hook test slice. The resulting seam should enforce title gating, support compact-first search into explicit review-open state, and keep all behavior draft-safe.
</verification>

<success_criteria>
Phase 3 has a stable typed intake hook and API layer that later UI and create-page plans can consume without reopening transport or state-contract questions.
</success_criteria>

<output>
After completion, create `.planning/phases/03-jellyfin-assisted-intake/03-02-SUMMARY.md`
</output>
