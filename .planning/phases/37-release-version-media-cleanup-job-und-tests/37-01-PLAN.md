---
phase: 37-release-version-media-cleanup-job-und-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/repository/media_repository.go
  - backend/internal/services/release_version_media_cleanup.go
  - backend/internal/services/release_version_media_cleanup_test.go
  - backend/cmd/server/main.go
autonomous: true
requirements:
  - RVM-CLEANUP-01
must_haves:
  truths:
    - "Release-version media cleanup stays on the existing media_assets/media_files seam and does not invent a parallel media store."
    - "Cleanup never makes half-finished assets public again; stale processing assets move to failed or deleted states only."
    - "Physical delete for soft-deleted assets only happens when no other relation still references the same media asset."
---

<objective>
Build the periodic cleanup foundation for release-version media so stale processing uploads, orphan staging files, missing media files, and expired soft-deletes are detected and reconciled safely.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-release-version-media-schema-foundation/34-CONTEXT.md
@backend/internal/handlers/fansub_media_helpers.go
@backend/internal/handlers/media_upload_storage.go
@backend/internal/repository/admin_content_test.go
@backend/internal/repository/media_repository.go
@backend/cmd/server/main.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add repository seams for stale-processing, missing-file, and soft-delete cleanup selection</name>
  <read_first>
    - backend/internal/repository/media_repository.go
    - backend/internal/repository/admin_content_test.go
    - .planning/phases/34-release-version-media-schema-foundation/34-CONTEXT.md
  </read_first>
  <action>Add the smallest repository methods needed for cleanup scans on the shared media seam: select release-version media assets stuck in `processing` beyond 30 minutes (hardcoded per D-05 — no env-variable needed for V1), select `media_files` rows whose physical file is missing, and select soft-deleted `release_version_media` rows eligible for physical cleanup. Reuse the existing status columns from Migration 0059 and ensure queries respect `deleted_at IS NULL` versus retention windows. Add tests or source-text assertions that prove soft-deleted rows are only selected when no other relation still references the same `media_asset_id`.</action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/repository/... -count=1</automated>
  </verify>
  <done>
    - `backend/internal/repository/media_repository.go` contains repository methods for stale processing assets, missing-file detection, and soft-delete cleanup candidates.
    - Candidate queries mention `release_version_media`, `media_assets.status`, and `media_files.status`.
    - A test in `backend/internal/repository/admin_content_test.go` or a new repository test file asserts that shared assets are not selected for physical delete while another relation still exists.
    - `go test ./internal/repository/... -count=1` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the cleanup service and wire a periodic runner</name>
  <read_first>
    - backend/cmd/server/main.go
    - backend/internal/handlers/fansub_media_helpers.go
    - backend/internal/handlers/media_upload_storage.go
    - backend/internal/repository/media_repository.go
  </read_first>
  <action>Create a dedicated cleanup service that performs four passes: mark stale `processing` assets as failed and remove their staging files; detect staging files with no DB entry and delete or quarantine them; mark missing physical files as `media_files.status='missing'` and escalate the asset to `failed` when no ready variant remains; and physically delete expired soft-deleted assets only after a no-other-reference check. Integrate the service into the server with the smallest project-owned periodic runner, preferably a ticker-based background loop in existing startup code if no scheduler abstraction already exists.</action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/services/... ./cmd/server/... -count=1</automated>
  </verify>
  <done>
    - `backend/internal/services/release_version_media_cleanup.go` exists and exposes a service or runner entrypoint — stays under 450 lines per CLAUDE.md; if all 4 passes don't fit, extract a helper file `release_version_media_cleanup_helpers.go`.
    - `backend/cmd/server/main.go` starts the cleanup job through a bounded periodic runner instead of an ad-hoc one-off call.
    - Cleanup code reuses `removeFileQuietly`-style semantics or an equivalent project-owned helper for physical deletes.
    - `backend/internal/services/release_version_media_cleanup_test.go` covers stale processing, orphan staging, missing file, and soft-delete retention paths.
    - `go test ./internal/services/... ./cmd/server/... -count=1` exits 0.
  </done>
</task>

</tasks>

<verification>
Run `cd backend && go test ./internal/repository/... ./internal/services/... ./cmd/server/... -count=1` and verify the cleanup service never selects shared media assets for destructive cleanup while still referenced elsewhere.
</verification>

<success_criteria>
Phase 37 cleanup is grounded when the project has one concrete periodic cleanup seam that can reconcile stale release-version media without violating shared-media reference safety.
</success_criteria>

<output>
Create `.planning/phases/37-release-version-media-cleanup-job-und-tests/37-01-SUMMARY.md`.
</output>
