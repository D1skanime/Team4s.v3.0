---
phase: 37-release-version-media-cleanup-job-und-tests
plan: 03
type: execute
wave: 3
depends_on:
  - 37-02-PLAN.md
files_modified:
  - frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.test.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/releaseVersionMediaUpload.test.ts
  - frontend/src/app/admin/episode-versions/[versionId]/edit/page.test.tsx
autonomous: true
requirements:
  - RVM-CLEANUP-01
must_haves:
  truths:
    - "The frontend only reports success after the backend returns a ready result; optimistic UI must not pretend broken uploads succeeded."
    - "Preview affordances stay hidden or disabled for fun_outtake and other, but the backend remains authoritative."
    - "Per-file retry and gallery refresh are first-class behaviors, not incidental side effects of a full-page reload."
---

<objective>
Lock the release-version-media frontend with focused tests for the category-first upload flow, per-file retry/error behavior, preview affordances, and gallery refresh after upload or delete.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/36-release-version-media-frontend-upload-ui-und-galerie/36-CONTEXT.md
@frontend/src/app/admin/episode-versions/[versionId]/edit/page.test.tsx
@frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.test.tsx
@frontend/src/app/admin/fansubs/[id]/edit/ReleaseThemeAssetsSection.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add category-first upload and retry-state tests</name>
  <read_first>
    - frontend/src/app/admin/episode-versions/[versionId]/edit/page.test.tsx
    - frontend/src/app/admin/fansubs/[id]/edit/ReleaseThemeAssetsSection.test.tsx
    - frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.tsx
  </read_first>
  <action>Create a dedicated release-version-media frontend test surface that verifies category selection is required before upload, multiple files render independent progress and status rows, partial failure leaves successful files visible, and failed files expose a retry path without resetting the whole queue. Reuse the existing component-testing style from `ReleaseThemeAssetsSection.test.tsx` and the editor page tests instead of inventing a new test harness.</action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npx vitest run "src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.test.tsx" "src/app/admin/episode-versions/[versionId]/edit/page.test.tsx"</automated>
  </verify>
  <done>
    - `frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.test.tsx` exists or the equivalent page test contains category-required, per-file status, and retry assertions.
    - At least one test covers partial success where one file succeeds and one fails.
    - Tests assert that no global success state appears when only a subset of files is ready.
    - The targeted Vitest command exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add preview-visibility and gallery-refresh regressions</name>
  <read_first>
    - frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.test.tsx
    - frontend/src/app/admin/episode-versions/[versionId]/edit/page.test.tsx
    - .planning/phases/36-release-version-media-frontend-upload-ui-und-galerie/36-CONTEXT.md
  </read_first>
  <action>Add tests that prove the preview toggle only appears for `screenshot` and `typesetting_karaoke`, that upload success refreshes or merges gallery state without a full page reload, and that delete removes the asset from the visible gallery after the backend soft-delete succeeds. Where possible, assert that backend error codes are surfaced as readable UI copy rather than swallowed into generic toasts.</action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npx vitest run "src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.test.tsx" "src/app/admin/episode-versions/[versionId]/edit/page.test.tsx" && npx tsc --noEmit</automated>
  </verify>
  <done>
    - Tests exist for preview visibility, gallery update after upload, and visible removal after delete.
    - At least one test verifies backend error text or code mapping is shown near the relevant file row or gallery item.
    - The touched release-version-media Vitest suites exit 0.
    - `npx tsc --noEmit` exits 0.
  </done>
</task>

</tasks>

<verification>
Run the targeted Vitest suite for the release-version editor media section and then `cd frontend && npx tsc --noEmit`; confirm category gating, retry, preview visibility, and gallery refresh all stay covered.
</verification>

<success_criteria>
The frontend upload/gallery contract is regression-protected and stays honest about per-file outcomes.
</success_criteria>

<output>
Create `.planning/phases/37-release-version-media-cleanup-job-und-tests/37-03-SUMMARY.md`.
</output>
