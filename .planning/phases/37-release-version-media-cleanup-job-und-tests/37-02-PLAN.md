---
phase: 37-release-version-media-cleanup-job-und-tests
plan: 02
type: execute
wave: 2
depends_on:
  - 37-01-PLAN.md
files_modified:
  - backend/internal/handlers/admin_content_release_version_media_test.go
  - backend/internal/repository/release_version_media_repository_test.go
autonomous: true
requirements:
  - RVM-CLEANUP-01
must_haves:
  truths:
    - "Backend tests prove that a file is only considered successful when original, thumbnail, relation row, and ready statuses all exist together."
    - "Animated GIF originals stay animated while thumbnails may be static first-frame derivatives."
    - "A partial multi-file upload reports per-file success or failure and does not collapse into one batch-wide result."
---

<objective>
Finish the backend regression suite for release-version media uploads so the upload contract, validation rules, GIF behavior, preview constraints, and partial-failure handling stay locked before production hardening.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/34-release-version-media-schema-foundation/34-CONTEXT.md
@backend/internal/handlers/media_upload_test.go
@backend/internal/handlers/admin_content_release_theme_assets_test.go
@backend/internal/repository/media_repository.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add upload validation and GIF regression coverage</name>
  <read_first>
    - backend/internal/handlers/media_upload_test.go
    - backend/internal/handlers/admin_content_release_theme_assets_test.go
    - backend/internal/handlers/admin_content_release_version_media.go
  </read_first>
  <action>Create or extend a release-version-media backend test file that covers valid JPEG, PNG, WebP, and animated GIF uploads; SVG rejection; wrong MIME or magic bytes rejection; file-size and dimension limits; GIF frame-count limits; and the rule that the original GIF remains animated while the thumbnail is a static derivative. Reuse the current multipart/test-router patterns from `media_upload_test.go` rather than inventing a separate harness.</action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/handlers/... ./internal/services/... -run ReleaseVersionMedia -count=1</automated>
  </verify>
  <done>
    - `backend/internal/handlers/admin_content_release_version_media_test.go` contains named tests for JPEG, PNG, WebP, GIF, invalid MIME/signature, and SVG rejection.
    - At least one test asserts thumbnail generation succeeds without mutating the stored original GIF format.
    - Tests cover machine-readable error codes such as invalid category or invalid preview category usage where the implementation exposes them.
    - `go test ./internal/handlers/... ./internal/services/... -run ReleaseVersionMedia -count=1` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add partial-failure, preview, delete, reorder, and cleanup-facing backend checks</name>
  <read_first>
    - backend/internal/handlers/admin_content_release_version_media_test.go
    - backend/internal/repository/release_version_media_repository_test.go
    - .planning/ROADMAP.md
  </read_first>
  <action>Extend backend coverage to prove partial multi-file uploads return isolated results, preview is rejected for `fun_outtake` and `other`, category changes are blocked on PATCH, soft delete removes assets from list/public-facing queries, and reorder is constrained to one release-version scope. Add at least one cleanup-facing test that verifies DB or storage failures do not leave a `ready` asset visible after a broken upload path.</action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/... -count=1</automated>
  </verify>
  <done>
    - Tests exist for partial multi-file success/failure, preview rejection, category-change rejection, soft delete visibility, and reorder scope validation.
    - At least one test proves that a DB/storage/thumbnail failure cannot leave a `status='ready'` asset visible.
    - Backend tests name or assert the relevant error codes instead of only generic HTTP 500 behavior where the implementation returns structured errors.
    - `go test ./internal/... -count=1` exits 0.
  </done>
</task>

</tasks>

<verification>
Run the targeted release-version-media backend suite plus `cd backend && go test ./internal/... -count=1`; confirm the tests document JPEG/PNG/WebP/GIF success, invalid-file rejection, preview rules, partial failures, and cleanup-safe failure behavior.
</verification>

<success_criteria>
The backend upload flow is locked down by executable tests before frontend and cleanup hardening rely on it.
</success_criteria>

<output>
Create `.planning/phases/37-release-version-media-cleanup-job-und-tests/37-02-SUMMARY.md`.
</output>
