---
phase: 45-fansub-member-management-mvp
plan: "01"
type: execute
wave: 1
depends_on:
  - ../44-app-permission-engine-fuer-fansub-release-und-media-kontexte/44-04-PLAN.md
files_modified:
  - backend/cmd/server
  - backend/internal/handlers
  - backend/internal/middleware
  - backend/internal/models
  - backend/internal/permissions
  - backend/internal/repository
  - database/migrations
  - docs
autonomous: true
requirements:
  - FANSUB-MEMBER-MGMT-01
must_haves:
  truths:
    - "Phase 45 builds on the app-user and permission foundations from Phases 43 and 44 rather than extending the legacy fansub_members seam."
    - "Only app-user-based group role assignments may grant app permissions; release/member credit tables never do."
    - "No new parallel membership or audit model should be introduced when an existing suitable seam already exists."
    - "If the executing branch does not expose the expected Phase-43/44 seams, this phase must stop with a blocker."
---

<objective>
Do the mandatory bestandsanalyse and establish the canonical Phase-45 foundation: confirm the app-user/group-membership seam, decide what existing member/admin code can be reused, and introduce only the minimal schema/repository additions truly required for app-user-based group membership management.

Purpose: Phase 45 should not accidentally build a second member model on top of the visible legacy `fansub_members` flow.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/45-fansub-member-management-mvp/45-CONTEXT.md
@.planning/phases/45-fansub-member-management-mvp/45-RESEARCH.md
@.planning/phases/44-app-permission-engine-fuer-fansub-release-und-media-kontexte/44-CONTEXT.md
@docs/architecture/db-schema-fansub-domain.md
@backend/internal/handlers/fansub_group_members.go
@backend/internal/repository/fansub_repository.go
@frontend/src/app/admin/fansubs/[id]/members/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyse schema and code seams before touching member management</name>
  <read_first>
    - database/migrations
    - backend/internal/middleware
    - backend/internal/permissions
    - backend/internal/handlers/fansub_group_members.go
    - frontend/src/app/admin/fansubs/[id]/members/page.tsx
  </read_first>
  <action>
    Perform the required Ist-Analyse and document the reality the phase will build on.

    Concretely inspect:
    - `app_users`, Keycloak mapping fields, global app-role tables
    - `fansub_groups`, `fansub_group_members`, `fansub_group_member_roles` or equivalent
    - existing audit structure
    - existing owner/status/created/updated fields
    - the CurrentUser/auth middleware seam
    - the Phase-44 permission package and current fansub-group capabilities
    - the visible legacy `fansub_members` handler/repository/frontend flow

    Then decide:
    - which existing structures will be reused
    - whether the visible `fansub_members` flow will be replaced, bridged, or explicitly left as a separate contributor/public seam
    - whether any migration is actually needed

    If the app-user/group-membership seam is missing or ambiguous, stop with a blocker instead of improvising.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s && git diff --check</automated>
  </verify>
  <done>
    - There is a documented Ist-Analyse baseline.
    - Phase 45 has one canonical group-membership source for app permissions.
    - The plan knows whether `fansub_members` is legacy-only or needs an explicit non-auth separation note.
  </done>
</task>

<task type="auto">
  <name>Task 2: Establish the canonical backend member-management model and minimal schema delta</name>
  <read_first>
    - results of Task 1
    - backend/internal/models
    - backend/internal/repository
  </read_first>
  <action>
    Formalize the canonical data model for the MVP:
    - app user identity
    - group membership row with active/inactive state
    - assigned role rows within the central role list

    Reuse existing tables if they already fit.
    If a schema gap remains, add only minimal migrations needed for:
    - membership status / activation tracking
    - role assignment linkage
    - audit compatibility

    Be explicit that:
    - unknown roles are rejected
    - duplicate active group membership is not allowed
    - deactivated memberships grant no permissions
    - Keycloak roles are never mutated here
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/models/... ./internal/repository/...</automated>
  </verify>
  <done>
    - The canonical backend member-management model is clear and app-user-based.
    - Any migration introduced is minimal and justified by the analysis.
    - No permission-bearing logic depends on `fansub_members.handle/role`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add repository/service seams for membership and self-lockout evaluation</name>
  <read_first>
    - backend/internal/repository
    - backend/internal/permissions
  </read_first>
  <action>
    Introduce or extend repository/service helpers for:
    - list group members with app-user identity and active roles
    - search app users by display name/email
    - create membership with initial roles
    - add/remove roles
    - activate/deactivate membership
    - evaluate whether a proposed mutation would remove the last active `fansub_lead` or the last active managing member

    Keep the self-lockout rule centralized so handlers can map it cleanly to `409 Conflict`.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/repository/... ./internal/permissions/...</automated>
  </verify>
  <done>
    - Repository/service seams exist for all Phase-45 mutation primitives.
    - Self-lockout checks can be reused by every relevant mutation path.
  </done>
</task>

</tasks>

<verification>
Automated:
1. `cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/repository/...`
2. `cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/permissions/...`
3. `cd C:/Users/admin/Documents/Team4s && git diff --check`

Targeted:
1. Verify the execution branch really exposes the app-user and permission seams Phase 45 expects.
2. Verify the canonical member-management source is not the legacy `fansub_members` table.
3. Verify any new migration is minimal and directly tied to an identified schema gap.
</verification>

<success_criteria>
- Phase 45 begins with a documented bestandsanalyse.
- The app-user/group-membership source of truth is explicit and non-legacy.
- Minimal repository/service seams exist for member management and self-lockout evaluation.
- The phase will not silently proceed on the wrong identity/membership seam.
</success_criteria>
