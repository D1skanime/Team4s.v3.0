---
phase: 45-fansub-member-management-mvp
plan: "04"
type: execute
wave: 4
depends_on:
  - 45-03-PLAN.md
files_modified:
  - backend/internal/handlers
  - backend/internal/repository
  - backend/internal/permissions
  - frontend/src/app
  - frontend/src/lib
  - frontend/src/types
  - docs
autonomous: true
requirements:
  - FANSUB-MEMBER-MGMT-01
must_haves:
  truths:
    - "Permissioned member management is a security-sensitive flow and needs explicit regression coverage."
    - "Self-lockout protection is only real when it is tested on negative paths."
    - "The final implementation report must begin with a short Ist-Analyse."
---

<objective>
Close Phase 45 with regression coverage, concise developer documentation, live verification, and a clean handoff into Phase 46 for invitations and join requests.

Purpose: Member management changes both permissions and operator workflows; it needs proof, not just implementation.
</objective>

<context>
@.planning/phases/45-fansub-member-management-mvp/45-CONTEXT.md
@.planning/phases/45-fansub-member-management-mvp/45-RESEARCH.md
@.planning/phases/45-fansub-member-management-mvp/45-01-PLAN.md
@.planning/phases/45-fansub-member-management-mvp/45-02-PLAN.md
@.planning/phases/45-fansub-member-management-mvp/45-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the required backend test matrix</name>
  <read_first>
    - backend/internal/handlers
    - backend/internal/repository
    - backend/internal/permissions
  </read_first>
  <action>
    Add backend tests for at least:
    - user without permission cannot see members
    - `fansub_lead` of own group sees members
    - `fansub_lead` can add an existing app user to own group
    - `fansub_lead` cannot manage another group
    - `designer` cannot manage members
    - unknown role rejected
    - duplicate active membership rejected
    - deactivated membership loses permissions
    - last active `fansub_lead` cannot be removed
    - last active manager role cannot be removed
    - self-deactivation blocked when it would leave no manager
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./... && go build ./...</automated>
  </verify>
  <done>
    - The critical positive and negative member-management paths are covered.
    - Self-lockout protections have explicit regression tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add thin frontend regression coverage for capability behavior and conflict UX</name>
  <read_first>
    - touched frontend tests/helpers from Plan 45-03
  </read_first>
  <action>
    Add the thinnest useful frontend coverage for:
    - `canViewMembers`/`canManageMembers` UI gating
    - app-user add flow visibility
    - `401`, `403`, and `409` error presentation on the touched screen
    - absence of hard-coded role-name checks in touched helpers where testable
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck && npm run lint</automated>
  </verify>
  <done>
    - Capability-gated member-management UI has regression protection.
    - Conflict/error feedback has at least basic automated coverage where practical.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write docs, verify live behavior, and prepare the Phase-46 handoff</name>
  <read_first>
    - docs/architecture/db-schema-fansub-domain.md
    - touched backend/frontend member-management files
  </read_first>
  <action>
    Add concise developer documentation that explains:
    - app-user members vs legacy `fansub_members` / contributor data
    - Keycloak identity vs Team4s group roles
    - the two relevant permissions
    - self-lockout protection rules
    - capability usage in the admin UI

    Then complete a live verification checklist that proves:
    - own-group member visibility
    - cross-group denial
    - add existing app user
    - role add/remove
    - deactivate/reactivate
    - self-lockout conflict behavior
    - audit output presence

    The final implementation write-up for the phase must start with a short Ist-Analyse section that summarizes:
    - relevant DB tables found
    - relevant columns found
    - auth/user middleware found
    - permission/admin seams found
    - existing API/frontend conventions found
    - which structures were reused
    - whether new migrations were needed or not

    Close with a narrow Phase-46 handoff focused on invitations and join requests.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./... && go build ./...</automated>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck && npm run lint</automated>
    <automated>cd C:/Users/admin/Documents/Team4s && git diff --check</automated>
  </verify>
  <done>
    - Docs explain the MVP and its boundaries clearly.
    - Live verification proves both enforcement and operator UX.
    - The final closeout starts with the requested Ist-Analyse and ends with a clean Phase-46 suggestion.
  </done>
</task>

</tasks>

<verification>
Automated:
1. `cd C:/Users/admin/Documents/Team4s/backend && go test ./...`
2. `cd C:/Users/admin/Documents/Team4s/backend && go build ./...`
3. `cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck`
4. `cd C:/Users/admin/Documents/Team4s/frontend && npm run lint`
5. `cd C:/Users/admin/Documents/Team4s && git diff --check`

Live:
1. Verify a same-group manager can view and manage members.
2. Verify a cross-group or under-privileged user gets capability-false UI and 403 API behavior.
3. Verify self-lockout attempts return 409 with a clear message.
4. Verify deactivated members lose permission-derived access.
5. Verify audit entries exist for the required mutations and blocked conflicts.
</verification>

<success_criteria>
- Backend and frontend regressions cover the required member-management MVP cases.
- Developer docs explain the architecture, legacy separation, and self-lockout rules.
- The phase ends with a clear handoff to Phase 46: Invitations & Join Requests.
</success_criteria>
