---
phase: 45-fansub-member-management-mvp
plan: "03"
type: execute
wave: 3
depends_on:
  - 45-02-PLAN.md
files_modified:
  - frontend/src/app
  - frontend/src/lib
  - frontend/src/types
autonomous: true
requirements:
  - FANSUB-MEMBER-MGMT-01
must_haves:
  truths:
    - "The frontend may only consume capabilities, never infer role permissions directly."
    - "The MVP should reuse the existing fansub members page/section where practical."
    - "401, 403, and 409 must produce understandable user-facing feedback."
---

<objective>
Refit the fansub admin UI into a minimal member-management MVP: capability-driven member list, app-user search and add flow, role edit/deactivate actions, and clear error handling.

Purpose: Give operators a usable UI for the new backend capabilities without introducing client-side auth logic.
</objective>

<context>
@.planning/phases/45-fansub-member-management-mvp/45-02-PLAN.md
@.planning/phases/45-fansub-member-management-mvp/45-CONTEXT.md
@frontend/src/app/admin/fansubs/[id]/members/page.tsx
@frontend/src/lib/api.ts
@frontend/src/types/fansub.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update API client/types for app-user-based member management</name>
  <read_first>
    - frontend/src/lib/api.ts
    - frontend/src/types/fansub.ts
  </read_first>
  <action>
    Replace or extend the legacy member DTOs and API helpers so the frontend can:
    - fetch fansub-group capabilities with `canViewMembers` / `canManageMembers`
    - list members with app-user identity, status, active roles, and timestamps
    - search existing app users
    - add a selected app user with at least one role
    - edit roles and activation state

    Keep the contract thin and aligned with the new backend endpoints.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck</automated>
  </verify>
  <done>
    - Frontend types and API helpers match the backend MVP contract.
    - Capability and member DTOs no longer assume the old `handle`/single-`role` model.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rework the fansub members page into a capability-driven management surface</name>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/members/page.tsx
  </read_first>
  <action>
    Reuse the existing route/page shell, but refit it to:
    - load capabilities first
    - only show the members view when `canViewMembers`
    - only show add/edit/deactivate controls when `canManageMembers`
    - provide an app-user search-and-select flow
    - display active roles, membership status, created/updated timestamps, and available actions

    Keep the UI intentionally MVP-sized:
    - one clear "Mitglieder & Rollen" surface
    - no hard-coded role-name checks
    - no speculative client-side self-lockout prediction
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck && npm run lint</automated>
  </verify>
  <done>
    - The touched UI only depends on capabilities, not role names.
    - Operators can complete the main member-management flows from one fansub-group page.
  </done>
</task>

<task type="auto">
  <name>Task 3: Improve 401/403/409 UX for the touched flows</name>
  <read_first>
    - frontend/src/lib/api.ts
    - touched fansub admin page/components
  </read_first>
  <action>
    Ensure the touched UI shows understandable German messages for:
    - missing authentication (`401`)
    - insufficient permissions (`403`)
    - business conflicts like self-lockout or duplicate active membership (`409`)

    Also make sure capabilities are reloaded after relevant mutations so button visibility and actions stay in sync.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run lint</automated>
  </verify>
  <done>
    - The MVP surfaces clear and specific error feedback.
    - Capability-driven affordances stay aligned after mutations.
  </done>
</task>

</tasks>

<verification>
Automated:
1. `cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck`
2. `cd C:/Users/admin/Documents/Team4s/frontend && npm run lint`

Behavioral:
1. `canViewMembers=false` hides or blocks the members view.
2. `canManageMembers=false` hides or disables mutation controls.
3. `409` conflicts show meaningful feedback without relying on role-name logic.
</verification>

<success_criteria>
- The members UI is capability-driven and app-user-based.
- The add-member and role-management flows are usable with the backend MVP.
- 401/403/409 handling is clear and scoped to the touched page.
</success_criteria>
