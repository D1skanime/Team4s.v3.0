---
phase: 80-admin-users-user-detail-drawer-scoped-rechte
plan: 02
type: tdd
wave: 1
depends_on:
  - 80-01
files_modified:
  - backend/internal/repository/admin_users_repository_test.go
  - backend/internal/handlers/admin_users_handler_test.go
  - frontend/src/lib/api.admin-users.test.ts
  - frontend/src/components/auth/PlatformAdminGate.test.tsx
  - frontend/src/app/admin/users/page.test.tsx
  - frontend/src/app/admin/users/UserDetailDrawer.test.tsx
autonomous: true
requirements:
  - Entscheidung I
  - Entscheidung H
  - Entscheidung K

must_haves:
  truths:
    - "admin_users_repository_test.go enthält Tests für Page-First-CTE-Logik, member_id-Anker-Priorität und Conflict-Count-Aggregation"
    - "admin_users_handler_test.go enthält Tests für Platform-Admin-Gate (403 bei Nicht-Admin), Rollen-Mutation-Audit, Last-Admin-Guard (409) und Status-Änderungs-Audit"
    - "api.admin-users.test.ts prüft Query-Serialisierung von AdminUserListParams und Fehlerbehandlung bei non-200-Responses"
    - "PlatformAdminGate.test.tsx enthält Regression für hasRefreshToken-only-Session (zeigt Admin-UI, kein Logout-Flash)"
    - "page.test.tsx prüft Tabellenspalten, Filter-Elemente und Drawer-Öffnungs-Pfad"
    - "UserDetailDrawer.test.tsx prüft Tab-Reihenfolge (9 Tabs), Lazy-Load-Verhalten und scoped-read-only-Sections"
    - "Alle Tests beginnen als RED und schlagen fehl (Wave-0-Kontrakt)"
  artifacts:
    - path: backend/internal/repository/admin_users_repository_test.go
      provides: "RED-Tests für Repository-Aggregat-Logik"
      contains: "MemberIDAnchor"
    - path: backend/internal/handlers/admin_users_handler_test.go
      provides: "RED-Tests für Handler-Gates und Mutations"
      contains: "AdminUsers"
    - path: frontend/src/lib/api.admin-users.test.ts
      provides: "RED-Tests für API-Helper"
    - path: frontend/src/app/admin/users/page.test.tsx
      provides: "RED-Tests für Listenoberfläche"
    - path: frontend/src/app/admin/users/UserDetailDrawer.test.tsx
      provides: "RED-Tests für Drawer + Tabs"
    - path: frontend/src/components/auth/PlatformAdminGate.test.tsx
      provides: "Regression: hasRefreshToken-only-Session"
  key_links:
    - from: backend/internal/repository/admin_users_repository_test.go
      to: backend/internal/repository/admin_users_repository.go
      via: "Methoden ListAdminUsersPage, GetUserOverview, ListUserContributions (noch nicht implementiert)"
      pattern: "AdminUsersRepository"
    - from: backend/internal/handlers/admin_users_handler_test.go
      to: backend/internal/handlers/admin_users_handler.go
      via: "requirePlatformAdminIdentity Gate + Audit-Write"
      pattern: "AdminUsersHandler"
---

<objective>
Wave-0-Testgerüst für Phase 80 (alle Tests beginnen als RED).

Erstellt die vollständigen Testdateien für Repository-, Handler- und Frontend-Schichten.
Die Tests legen die Erwartungen fest, bevor die Implementierung in Plan 80-03 beginnt.

Purpose: Nyquist-Pflicht (VALIDATION.md). TDD sichert, dass der Platform-Admin-Gate,
der Last-Admin-Guard und der member_id-Anker korrekt implementiert werden.
Alle RED-Tests werden durch Plan 80-03 auf GREEN gebracht.

Output: 6 neue Testdateien.
</objective>

<execution_context>
@C:\Users\admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\admin\Documents\Team4s\.planning\phases\80-admin-users-user-detail-drawer-scoped-rechte\80-VALIDATION.md
@C:\Users\admin\Documents\Team4s\.planning\phases\80-admin-users-user-detail-drawer-scoped-rechte\80-RESEARCH.md
@C:\Users\admin\Documents\Team4s\.planning\phases\80-admin-users-user-detail-drawer-scoped-rechte\80-PATTERNS.md
@C:\Users\admin\Documents\Team4s\.planning\phases\80-admin-users-user-detail-drawer-scoped-rechte\80-UI-SPEC.md

<interfaces>
<!-- Aus backend/internal/handlers/fansub_media_review_handler.go (Interface-Pattern für Tests): -->
<!--
type fansubMediaReviewRepo interface {
    ListFansubGroupMedia(ctx context.Context, ...) (...)
}
// Test-Mock-Pattern:
type mockFansubMediaReviewRepo struct { ... }
func (m *mockFansubMediaReviewRepo) ListFansubGroupMedia(...) { ... }
-->

<!-- Aus backend/internal/handlers/platform_admin_authz.go: -->
<!--
func requirePlatformAdminIdentity(c *gin.Context, authzRepo authzRoleChecker, scope string) (PlatformAdminIdentity, bool)
// Gibt false + schreibt 403 wenn kein Platform-Admin
-->

<!-- Aus frontend/src/components/auth/PlatformAdminGate.test.tsx (bestehende Datei): -->
<!-- Lesepflicht: vollständige Datei, um bestehende Test-Struktur nicht zu zerstören -->

<!-- Wave-0-Teststruktur (analog zu backend/internal/handlers/contribution_review_handler_test.go): -->
<!--
func TestAdminUsersHandler_ListUsers_RequiresPlatformAdmin(t *testing.T) {
    // setup gin router, mock authzRepo ohne platform_admin-Rolle
    // assert 403
}
func TestAdminUsersHandler_RevokeGlobalRole_LastAdminGuard(t *testing.T) {
    // CountActivePlatformAdmins gibt 1 zurück → erwartet 409
}
-->
</interfaces>
</context>

<feature>
  <name>Wave-0-Testgerüst Phase 80</name>
  <files>
    backend/internal/repository/admin_users_repository_test.go,
    backend/internal/handlers/admin_users_handler_test.go,
    frontend/src/lib/api.admin-users.test.ts,
    frontend/src/components/auth/PlatformAdminGate.test.tsx,
    frontend/src/app/admin/users/page.test.tsx,
    frontend/src/app/admin/users/UserDetailDrawer.test.tsx
  </files>
  <behavior>
    RED-Phase-Tests gemäss VALIDATION.md Per-Task Verification Map (80-W0-01 bis 80-W0-10):

    Backend Repository-Tests (admin_users_repository_test.go):
    - TestAdminUsersRepository_ListAdminUsersPage_PageFirstCTE: prüft, dass Aggregate als LATERAL berechnet werden (Erwartung: Methode ListAdminUsersPage existiert, Query-Struktur mit Page-CTE)
    - TestAdminUsersRepository_MemberIDAnchor_CanonicalFirst: prüft WHERE ac.member_id = $1 (nicht fansub_group_member_id) bei Contribution-Aggregation
    - TestAdminUsersRepository_ConflictCount_D17_D18: prüft, dass conflict_count aus D-17-Typen (Open Claim + Profil, Mitglied ohne Rolle, Media ohne Scope, Dispute) und D-18-Typen (ungültige Release-Version, Default/Override-Widerspruch, Media ohne Contribution-Rechte) zusammengesetzt wird
    Teststruktur: Kompilier-Tests mit Interface-Mocks (kein Live-DB nötig); schlagen fehl weil AdminUsersRepository noch nicht existiert.

    Backend Handler-Tests (admin_users_handler_test.go):
    - TestAdminUsersHandler_ListUsers_NonPlatformAdmin_Returns403: gin-TestRouter, authzRepo-Mock ohne platform_admin → HTTP 403
    - TestAdminUsersHandler_AssignGlobalRole_AuditsAllowed: authzRepo-Mock mit platform_admin → erwartet AuditLogEntry mit EventType="app_user_global_role.assigned" und Outcome="allowed"
    - TestAdminUsersHandler_RevokeGlobalRole_LastAdminGuard_Returns409: CountActivePlatformAdmins gibt 1 → HTTP 409; kein Audit-Write bei Ablehnung
    - TestAdminUsersHandler_UpdateUserStatus_Disable_AuditsAllowed: Status "disabled" → AuditLogEntry EventType="app_user_status.disabled"
    Teststruktur: Kompilier-Tests; schlagen fehl weil admin_users_handler.go noch nicht existiert.

    Frontend API-Tests (api.admin-users.test.ts):
    - listAdminUsersPage_serializes_all_params: prüft URL-Query-String für alle AdminUserListParams-Felder (q, status, global_role, has_conflicts=true, sort, limit, offset)
    - listAdminUsersPage_throws_ApiError_on_non200: mockFetch 403 → ApiError geworfen
    - getAdminUserOverview_returns_typed_response: mockFetch mit AdminUserOverviewResponse-Shape → kein Typecheck-Fehler
    Teststruktur: vitest + vi.spyOn(global, 'fetch'); schlagen fehl weil Helper-Funktionen noch nicht in api.ts existieren.

    PlatformAdminGate-Regression (PlatformAdminGate.test.tsx):
    - renders_admin_content_with_refresh_token_only: useAuthSession gibt { hasAccessToken: false, hasRefreshToken: true, isClientInitialized: true } → Children rendern (kein Logout-State)
    Liest bestehende Datei zuerst, ergänzt den Test ohne bestehende Struktur zu beschädigen.

    Frontend Page-Tests (page.test.tsx — neue Datei):
    - renders_table_with_all_required_columns: prüft Spaltenköpfe gemäss UI-SPEC (Benutzer, Status, Globale Rollen, Member-Profil, Gruppen, Leader-Kontext, Offene Claims, Beiträge, Medienuploads, Letzte Aktivität, Konflikte)
    - renders_filter_elements: prüft Select für Accountstatus, Select für Globale Rolle, Suchfeld
    - clicking_row_opens_drawer: simuliert Klick auf Tabellenzeile → Drawer-State wird gesetzt
    Teststruktur: @testing-library/react; schlagen fehl weil Komponenten noch nicht existieren.

    Frontend Drawer-Tests (UserDetailDrawer.test.tsx — neue Datei):
    - renders_nine_tabs: prüft Tab-Labels gemäss UI-SPEC (Übersicht, Globale Rollen, Member-Profil & Claims, Gruppenmitgliedschaften, Gruppenrechte, Beiträge, Medien, Audit, Streaming)
    - tabs_lazy_load_on_first_activation: Tab-Daten-API-Call nur beim ersten Aktivieren (nicht beim Rendern aller Tabs)
    - scoped_tabs_have_no_mutation_controls: Gruppenrechte-/Contributions-/Medien-Tab hat keinen Button für Rolle vergeben oder Status ändern
    Teststruktur: @testing-library/react mit vi.mock('@/lib/api'); schlagen fehl weil Komponenten noch nicht existieren.
  </behavior>
  <implementation>
    RED-Phase: Nur Testdateien anlegen. KEIN Produktionscode ändern.
    Test-Dateien müssen kompilieren (go vet / tsc), aber die Test-Assertions schlagen fehl
    weil die gemockten Interfaces/Komponenten noch fehlen.
    Für Go-Tests: Interface-Definitionen als lokale Typen im Testfile, falls AdminUsersRepository
    noch nicht existiert. Compile-Fehler bei fehlenden Methoden sind akzeptabel und erwartet.
    Für Frontend-Tests: vi.mock-Paths setzen, Imports auf nicht-existierende Dateien erlaubt
    wenn tsconfig-Paths konfiguriert.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Grenze | Beschreibung |
|--------|-------------|
| Test-Mock-Isolation | Mock-AuthzRepos dürfen nicht echte Platform-Admin-Identitäten leaken |
| Wave-0-RED-Kontrakt | Tests müssen fehlschlagen — kein Silently-Passing durch leere Mocks |

## STRIDE Threat Register

| Threat ID | Kategorie | Komponente | Disposition | Mitigationsplan |
|-----------|-----------|------------|-------------|-----------------|
| T-80-02-01 | Elevation of Privilege | admin_users_handler_test.go — NonPlatformAdmin-Test | mitigate | Test prüft explizit HTTP 403 bei fehlendem platform_admin; acceptiert keine 200 |
| T-80-02-02 | Denial of Service | LastAdminGuard-Test | mitigate | Test prüft HTTP 409 bei CountActivePlatformAdmins=1; verhindert Implementierungs-Fehler |
| T-80-02-SC | Tampering | npm/pip/cargo Installs | accept | Keine neuen externen Pakete — kein Slopcheck nötig |
</threat_model>

<verification>
RED-Phase-Prüfung:

Backend (erwartet Compiler-Fehler oder fehlschlagende Tests wegen fehlender Implementierung):
```
cd C:\Users\admin\Documents\Team4s\backend && go test ./internal/handlers -run AdminUsers -count=1 2>&1 | tail -20
cd C:\Users\admin\Documents\Team4s\backend && go test ./internal/repository -run AdminUsers -count=1 2>&1 | tail -20
```

Frontend (erwartet Fehler wegen fehlender Komponenten):
```
cd C:\Users\admin\Documents\Team4s\frontend && npm test -- src/lib/api.admin-users.test.ts 2>&1 | tail -20
cd C:\Users\admin\Documents\Team4s\frontend && npm test -- src/components/auth/PlatformAdminGate.test.tsx 2>&1 | tail -10
```

Ziel: Alle Testdateien vorhanden, go vet / tsc kann die Testdateien ohne Syntaxfehler parsen.
</verification>

<success_criteria>
Alle 6 Wave-0-Testdateien existieren und beschreiben die Erwartungen klar.
Plan 80-03 (Backend-Implementierung) kann gegen diese Tests implementieren und sie auf GREEN bringen.
</success_criteria>

<output>
Erstelle `.planning/phases/80-admin-users-user-detail-drawer-scoped-rechte/80-02-SUMMARY.md` wenn fertig.
</output>
