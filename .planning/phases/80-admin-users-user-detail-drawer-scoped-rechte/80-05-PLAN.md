---
phase: 80-admin-users-user-detail-drawer-scoped-rechte
plan: 05
type: execute
wave: 4
depends_on:
  - 80-04
files_modified:
  - frontend/src/app/admin/users/tabs/UserOverviewTab.tsx
  - frontend/src/app/admin/users/tabs/UserGlobalRolesTab.tsx
  - frontend/src/app/admin/users/tabs/UserClaimsTab.tsx
  - frontend/src/app/admin/users/tabs/UserMembershipsTab.tsx
  - frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx
  - frontend/src/app/admin/users/tabs/UserContributionsTab.tsx
  - frontend/src/app/admin/users/tabs/UserMediaTab.tsx
  - frontend/src/app/admin/users/tabs/UserAuditTab.tsx
autonomous: false
requirements:
  - Entscheidung I
  - Entscheidung H
  - Entscheidung J
  - Entscheidung K

must_haves:
  truths:
    - "UserOverviewTab zeigt Summary-Stat-Grid (alle 7 Counts) + Accountstatus mit Deaktivieren-/Reaktivieren-Mutation + Konfliktliste"
    - "UserGlobalRolesTab ermöglicht Rolle vergeben (Modal + Select) und Rolle entziehen (Bestätigungs-Modal) mit Last-Admin-Guard-Fehlermeldung inline im Modal"
    - "UserClaimsTab zeigt Member-Profil-Abschnitt (D-J: profile_status read-only, Gedenkprofil korrekt angezeigt) und Claims & Einladungen read-only"
    - "UserMembershipsTab zeigt Gruppenmitgliedschaften read-only mit Deep-Link 'In Gruppe öffnen' nach /admin/fansubs/[id]/edit"
    - "UserGroupRightsTab zeigt scoped Rechte read-only + Hinweistext + Deep-Link 'Gruppe bearbeiten'"
    - "UserContributionsTab gruppiert Beiträge in 4 Sektionen (D-13): Projektweite Beiträge, Release-spezifische Overrides, Offene/strittige, Historisch/Legacy — leere Gruppen ausgeblendet"
    - "UserMediaTab zeigt release-version-scoped Medien mit Berechtigung-Badge (aktiv/fehlt) und Deep-Link nach /me/releases/[versionId]/workspace"
    - "UserAuditTab zeigt Audit-Timeline mit Erlaubt/Verweigert-Badge"
    - "Streaming-Tab ist Stub (kein eigenständiges Tab-File nötig — inline im UserDetailDrawer.tsx als Einzeiler)"
    - "Alle 8 Dateien <= 450 Zeilen"
    - "Kein natives select/input/textarea/button — ausschliesslich @/components/ui-Primitives"
    - "Alle UI-Texte deutsch mit korrekten Umlauten"
  artifacts:
    - path: frontend/src/app/admin/users/tabs/UserOverviewTab.tsx
      provides: "Übersicht: Summary, Konfliktliste, Accountstatus-Mutation"
      contains: "updateAdminUserStatus"
    - path: frontend/src/app/admin/users/tabs/UserGlobalRolesTab.tsx
      provides: "Globale Rollen Assign/Revoke mit Modal"
      contains: "assignAdminUserGlobalRole"
    - path: frontend/src/app/admin/users/tabs/UserContributionsTab.tsx
      provides: "Phase-83-aware Contributions in 4 Gruppen"
      contains: "project_default"
    - path: frontend/src/app/admin/users/tabs/UserClaimsTab.tsx
      provides: "Member-Profil-Status (inkl. Gedenkprofil) + Claims read-only"
      contains: "Gedenkprofil"
  key_links:
    - from: frontend/src/app/admin/users/tabs/UserOverviewTab.tsx
      to: frontend/src/lib/api.ts
      via: "getAdminUserOverview(userId) und updateAdminUserStatus(userId, status)"
      pattern: "getAdminUserOverview"
    - from: frontend/src/app/admin/users/tabs/UserGlobalRolesTab.tsx
      to: frontend/src/lib/api.ts
      via: "assignAdminUserGlobalRole, revokeAdminUserGlobalRole, getAdminUserGlobalRoles"
      pattern: "revokeAdminUserGlobalRole"
    - from: frontend/src/app/admin/users/tabs/UserContributionsTab.tsx
      to: frontend/src/lib/api.ts
      via: "getAdminUserContributions(userId) — gibt AdminUserContributionsResponse zurück"
      pattern: "getAdminUserContributions"
---

<objective>
Tab-Implementierungen + Human-Verify.

Implementiert alle 8 Tab-Komponenten für den User-Detail-Drawer. Jede Datei hat ihre
eigene Lazy-Load-Logik (Lade/Fehler/EmptyState-Triade + useEffect beim Mount).
Danach manuelles UAT durch den Entwickler.

Purpose: Erst nach diesem Plan ist die gesamte /admin/users-Oberfläche funktional.
Die Tab-Trennung in 8 Dateien sichert das 450-Zeilen-Limit und macht jeden Tab
unabhängig testbar.

Output: 8 neue Tab-Dateien.
</objective>

<execution_context>
@C:\Users\admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\admin\Documents\Team4s\.planning\phases\80-admin-users-user-detail-drawer-scoped-rechte\80-UI-SPEC.md
@C:\Users\admin\Documents\Team4s\.planning\phases\80-admin-users-user-detail-drawer-scoped-rechte\80-PATTERNS.md
@C:\Users\admin\Documents\Team4s\.planning\phases\80-admin-users-user-detail-drawer-scoped-rechte\80-CONTEXT.md

<interfaces>
<!-- Aus frontend/src/app/admin/fansubs/[id]/edit/ContributionsReviewSection.tsx (Lade/Fehler/Empty-Triade): -->
<!--
'use client'
import { useCallback, useEffect, useState } from 'react'
import { Badge, Button, Card, EmptyState, ErrorState, LoadingState, SectionHeader, Table, ... } from '@/components/ui'
const [data, setData] = useState<T | null>(null)
const [isLoading, setIsLoading] = useState(true)
const [error, setError] = useState<string | null>(null)
const loadData = useCallback(async () => {
  try { setIsLoading(true); setError(null); const resp = await apiCall(userId); setData(resp) }
  catch (err) { setError(err instanceof ApiError ? err.message : 'Daten konnten nicht geladen werden.') }
  finally { setIsLoading(false) }
}, [userId])
useEffect(() => { void loadData() }, [loadData])
if (isLoading) return <LoadingState>Daten werden geladen …</LoadingState>
if (error) return <ErrorState><Button onClick={loadData}>Erneut versuchen</Button></ErrorState>
if (!data) return <EmptyState>Keine Einträge vorhanden.</EmptyState>
-->

<!-- Aus frontend/src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.tsx (Mutation+Modal-Pattern): -->
<!--
const [isModalOpen, setIsModalOpen] = useState(false)
const [isMutating, setIsMutating] = useState(false)
const [mutationError, setMutationError] = useState<string | null>(null)
// Button öffnet Modal; Modal hat inline error state + Bestätigungs-Button
<Modal open={isModalOpen} onClose={() => setIsModalOpen(false)} title="...">
  {mutationError && <p className="text-error">{mutationError}</p>}
  <Button variant="destructive" onClick={handleConfirm} disabled={isMutating}>
    {isMutating ? 'Wird verarbeitet …' : 'Bestätigung'}
  </Button>
</Modal>
-->

<!-- Aus frontend/src/types/admin-users.ts (aus Plan 80-01 — Tab-Response-Typen): -->
<!--
export interface AdminUserOverviewResponse { summary_counts: {...}; conflict_details: AdminConflictDetail[] }
export interface AdminUserGlobalRolesResponse { roles: string[]; assignable_roles: string[] }
export interface AdminUserContributionsResponse {
  project_defaults: AdminContributionItem[];
  release_overrides: AdminContributionItem[];
  open_disputes: AdminContributionItem[];
  legacy_historical: AdminContributionItem[];
}
export interface AdminContributionItem {
  contribution_id: number; fansub_group_id: number; fansub_group_name: string;
  anime_id: number; anime_title: string; release_version_id: number|null;
  contribution_type: 'project_default'|'release_override'; dispute_state: string; role_codes: string[];
}
export interface AdminUserMemberClaimsResponse {
  member_profile: AdminMemberProfileSummary | null;
  claims: AdminClaimSummary[];
}
export interface AdminUserAuditResponse { entries: AdminAuditEntry[] }
-->

<!-- Aus frontend/src/lib/api.ts (aus Plan 80-03): -->
<!--
export async function getAdminUserOverview(userId: number): Promise<AdminUserOverviewResponse>
export async function getAdminUserGlobalRoles(userId: number): Promise<AdminUserGlobalRolesResponse>
export async function assignAdminUserGlobalRole(userId: number, role: string): Promise<void>
export async function revokeAdminUserGlobalRole(userId: number, role: string): Promise<void>
export async function updateAdminUserStatus(userId: number, status: string): Promise<void>
export async function getAdminUserMemberClaims(userId: number): Promise<AdminUserMemberClaimsResponse>
export async function getAdminUserGroupMemberships(userId: number): Promise<AdminUserGroupMembershipsResponse>
export async function getAdminUserGroupRights(userId: number): Promise<AdminUserGroupRightsResponse>
export async function getAdminUserContributions(userId: number): Promise<AdminUserContributionsResponse>
export async function getAdminUserMedia(userId: number): Promise<AdminUserMediaResponse>
export async function getAdminUserAudit(userId: number): Promise<AdminUserAuditResponse>
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sechs read-only Tabs + UserContributionsTab (D-13)</name>
  <files>
    frontend/src/app/admin/users/tabs/UserClaimsTab.tsx
    frontend/src/app/admin/users/tabs/UserMembershipsTab.tsx
    frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx
    frontend/src/app/admin/users/tabs/UserContributionsTab.tsx
    frontend/src/app/admin/users/tabs/UserMediaTab.tsx
    frontend/src/app/admin/users/tabs/UserAuditTab.tsx
  </files>
  <read_first>
    frontend/src/app/admin/fansubs/[id]/edit/ContributionsReviewSection.tsx (Lade/Fehler/Empty-Triade vollständig lesen)
    frontend/src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.tsx (List-Pattern + SectionHeader-Nutzung)
    .planning/phases/80-admin-users-user-detail-drawer-scoped-rechte/80-UI-SPEC.md (je Tab: Copywriting-Contract + Abschnitte read-only, D-13, D-15)
    frontend/src/components/ui/index.ts (Barrel-Datei: SectionHeader, Badge-Varianten, Card verfügbar?)
  </read_first>
  <action>
    Alle 6 Dateien folgen dem Lade/Fehler/Empty-Triade-Pattern aus ContributionsReviewSection.tsx.
    Props für alle: { userId: number }.

    UserClaimsTab.tsx: getAdminUserMemberClaims(userId). Zwei Sektionen via SectionHeader: "Member-Profil" (member_profile.profile_status als Badge — 'active'=success, 'memorial'=muted mit Text "Gedenkprofil"; kein Member-Profil = EmptyState "Kein Member-Profil verknüpft.") und "Claims & Einladungen" (Tabelle: claim_status als Badge "Verifiziert"/"Offen"/"Abgelehnt"). Kein Edit-Button, kein Verify-Button. (D-J: profile_status read-only)

    UserMembershipsTab.tsx: getAdminUserGroupMemberships(userId). Tabelle: Gruppenname, Rolle, Status. Letzte Spalte: Button/Link "In Gruppe öffnen" → /admin/fansubs/[id]/edit in neuem Tab (target="_blank").

    UserGroupRightsTab.tsx: getAdminUserGroupRights(userId). Hinweistext "Gruppenrechte können in der jeweiligen Gruppenansicht bearbeitet werden." via SectionHeader oder Card. Pro Gruppe: Link "Gruppe bearbeiten" → /admin/fansubs/[id]/edit. Kein Edit-Control für Rechte. (D-03)

    UserContributionsTab.tsx: getAdminUserContributions(userId). Vier Sektionen mit SectionHeader:
    - "Projektweite Beiträge (Standard)": project_defaults (release_version_id IS NULL); Spalten: Anime-Titel, Gruppe, Rollen
    - "Release-spezifische Overrides": release_overrides (release_version_id gesetzt); Spalten: Anime-Titel, Release-Version, Rollen; Link "Release-Version öffnen" → /admin/fansubs/[id]/edit oder release-version-Seite
    - "Offene / strittige Beiträge": open_disputes; Badge "Offen" pro Dispute
    - "Historisch / Legacy": legacy_historical; Hinweis: historisch via fansub_group_member_id
    Leere Sektionen ausblenden (kein SectionHeader wenn count=0). (D-13)

    UserMediaTab.tsx: getAdminUserMedia(userId). Gruppiert nach Anime/Release-Version (SectionHeader je Gruppe). Pro Medien-Item: Typ, Hochgeladen-Datum, Berechtigung-Badge (success "Berechtigung aktiv" / warning "Berechtigung fehlt"). Link "Arbeitsfläche öffnen" → /me/releases/[versionId]/workspace in neuem Tab. (D-15)

    UserAuditTab.tsx: getAdminUserAudit(userId). Tabelle: Zeitstempel, EventType, Action, Outcome-Badge (success "Erlaubt" / danger "Verweigert"), Payload-Kurzanzeige.

    Jede Datei <= 450 Zeilen. Falls nötig Helper in eigene -helpers.tsx auslagern.
  </action>
  <verify>
    <automated>cd C:\Users\admin\Documents\Team4s\frontend && npm run typecheck 2>&1 | tail -5</automated>
    <automated>cd C:\Users\admin\Documents\Team4s\frontend && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <done>
    npm run typecheck fehlerfrei.
    npm run lint ohne Fehler (ggf. native-select-Warnung ausgeschlossen da keine nativen Elemente).
    grep -rn "Gedenkprofil" frontend/src/app/admin/users/tabs/UserClaimsTab.tsx gibt >= 1.
    grep -rn "project_default" frontend/src/app/admin/users/tabs/UserContributionsTab.tsx gibt >= 1.
    grep -rn "Arbeitsfläche öffnen" frontend/src/app/admin/users/tabs/UserMediaTab.tsx gibt >= 1.
    Alle 6 Dateien: wc -l gibt jeweils <= 450.
  </done>
</task>

<task type="auto">
  <name>Task 2: UserOverviewTab + UserGlobalRolesTab (Mutations + Modals)</name>
  <files>
    frontend/src/app/admin/users/tabs/UserOverviewTab.tsx
    frontend/src/app/admin/users/tabs/UserGlobalRolesTab.tsx
  </files>
  <read_first>
    frontend/src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.tsx (Mutation+Modal-Pattern vollständig lesen — Zeilen 1–80+)
    .planning/phases/80-admin-users-user-detail-drawer-scoped-rechte/80-UI-SPEC.md (Drawer-Übersicht-Tab-Layout, Drawer-Globale-Rollen-Tab-Abschnitt, Drawer-Accountstatus-Abschnitt, Mutation-Feedback)
    frontend/src/types/admin-users.ts (AdminUserOverviewResponse, AdminConflictDetail, AdminUserGlobalRolesResponse)
  </read_first>
  <action>
    UserOverviewTab.tsx: getAdminUserOverview(userId). Zeigt:
    a) Stat-Grid (Card oder kompakte Tabelle): globale_rollen, gruppen, offene_claims, offene_beiträge, release_arbeitsflächen, medien, konflikte — Zahlen aus summary_counts (UI-SPEC Übersicht-Tab Layout).
    b) Accountstatus-Abschnitt: Status-Badge (success=Aktiv, warning=Ausstehend, danger=Deaktiviert). Wenn status='active': Button "Konto deaktivieren" → öffnet Bestätigungs-Modal (Text "Das Konto von {display_name} wird deaktiviert. Der Benutzer verliert den Plattformzugang sofort.", Bestätigungs-Button "Jetzt deaktivieren", Abbruch "Abbrechen"). Wenn status='disabled': Button "Konto reaktivieren" → direkte Mutation ohne Modal (nicht destruktiv). Toast bei Erfolg. Inline-Fehler im Modal.
    c) Wenn conflict_details.length > 0: Sektionsheader "Erkannte Konflikte" + Badge-Zahl, dann bulleted Liste mit Konflikt-Messages aus UI-SPEC Copywriting Contract.
    Last-Admin-Guard: updateAdminUserStatus(userId, 'disabled') gibt 409 → inline Fehler "Das Konto des letzten aktiven Plattform-Admins kann nicht deaktiviert werden."
    Datei <= 450 Zeilen; Stat-Grid als eigene Helper-Komponente wenn nötig.

    UserGlobalRolesTab.tsx: getAdminUserGlobalRoles(userId). Zeigt:
    a) Sektionsheader "Aktive Rollen" + Tabelle aktiver Rollen (Badge pro Rolle + "Rolle entziehen"-Button pro Zeile).
    b) Button "Rolle vergeben" → öffnet Modal mit Select (aus assignable_roles) + Bestätigungs-Button.
    Entzug-Modal: "Soll die Rolle „{rolle}" von {display_name} entzogen werden?", Bestätigung "Rolle entziehen", Abbruch "Abbrechen". Inline-Fehler bei Last-Admin-Guard: "Die letzte Plattform-Admin-Rolle kann nicht entzogen werden."
    Vergabe-Erfolg: Toast "Rolle wurde erfolgreich vergeben." + Tab neu laden.
    Entzug-Erfolg: Toast "Rolle wurde erfolgreich entzogen." + Tab neu laden.
    Leerzustand: "Diesem Benutzer sind keine globalen Rollen zugewiesen."
    Datei <= 450 Zeilen.
  </action>
  <verify>
    <automated>cd C:\Users\admin\Documents\Team4s\frontend && npm run typecheck 2>&1 | tail -5</automated>
    <automated>cd C:\Users\admin\Documents\Team4s\frontend && npm test -- src/app/admin/users 2>&1 | tail -15</automated>
  </verify>
  <done>
    npm run typecheck fehlerfrei.
    npm test -- src/app/admin/users zeigt alle Tests PASS.
    grep -rn "Konto deaktivieren" frontend/src/app/admin/users/tabs/UserOverviewTab.tsx gibt >= 1.
    grep -rn "Rolle vergeben" frontend/src/app/admin/users/tabs/UserGlobalRolesTab.tsx gibt >= 1.
    grep -rn "letzte Plattform-Admin-Rolle" frontend/src/app/admin/users/tabs/UserGlobalRolesTab.tsx gibt >= 1.
    Beide Dateien: wc -l gibt jeweils <= 450.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Vollständige /admin/users-Route mit Listentabelle, Filtern, Pagination und User-Detail-Drawer mit 9 Tabs.
    Editierbar: Globale Rollen (Vergabe/Entzug) und Accountstatus (Deaktivieren/Reaktivieren).
    Read-only: Claims, Mitgliedschaften, Gruppenrechte, Contributions (D-13 gruppiert), Medien (D-15 release-version-scoped), Audit.
    Platform-Admin-Gate auf allen Backend-Endpunkten und im Frontend.
    Last-Admin-Guard für Revoke und Disable.
    Audit-Einträge für alle Mutations.
  </what-built>
  <how-to-verify>
    Voraussetzung: Dev-Server läuft auf http://127.0.0.1:3000

    1. Als Plattform-Admin öffne http://127.0.0.1:3000/admin/users
       → Erwartung: Tabelle mit mindestens einem User, alle 11 Spalten sichtbar.

    2. Suche nach bekanntem User (Name oder E-Mail eingeben)
       → Erwartung: Tabellenliste filtert in Echtzeit (Debounce ~300ms).

    3. Klicke auf eine Tabellenzeile
       → Erwartung: Rechter Drawer öffnet mit 9 Tabs.

    4. Öffne Tab "Globale Rollen" und versuche eine Rolle zuzuweisen/zu entziehen
       → Erwartung: Modal erscheint, Bestätigung schreibt Audit, Toast zeigt Erfolg.

    5. Öffne Tab "Beiträge"
       → Erwartung: Vier Gruppen (Projektweite Beiträge, Release-Overrides, Offene Disputes, Historisch) werden angezeigt; leere Gruppen ausgeblendet.

    6. Öffne Tab "Medien"
       → Erwartung: Mediauploads mit Berechtigung-Badge (aktiv/fehlt) und Deep-Link "Arbeitsfläche öffnen".

    7. Öffne Tab "Audit"
       → Erwartung: Audit-Timeline mit Einträgen (nach Mutation aus Schritt 4 muss ein Eintrag sichtbar sein).

    8. Als normaler User (ohne platform_admin) öffne /admin/users
       → Erwartung: Zugang verweigert, kein Daten-Flash.

    9. Prüfe Last-Admin-Guard: Versuche dem einzigen Plattform-Admin die Rolle zu entziehen
       → Erwartung: Inline-Fehler "Die letzte Plattform-Admin-Rolle kann nicht entzogen werden." im Modal.
  </how-to-verify>
  <resume-signal>
    "approved" wenn alle 9 Schritte bestätigt — oder beschreibe gefundene Abweichungen.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Grenze | Beschreibung |
|--------|-------------|
| UserGlobalRolesTab → POST/DELETE /api/v1/admin/users/:id/global-roles/:role | Mutation-Endpunkte brauchen Platform-Admin-Gate (Backend-Seite, Plan 80-03) |
| UserOverviewTab → PUT /api/v1/admin/users/:id/status | Status-Disable muss Last-Admin-Guard auslösen |

## STRIDE Threat Register

| Threat ID | Kategorie | Komponente | Disposition | Mitigationsplan |
|-----------|-----------|------------|-------------|-----------------|
| T-80-05-01 | Elevation of Privilege | UserGroupRightsTab — keine Edit-Controls | mitigate | Scoped-Rechte sind ausschliesslich read-only + Deep-Link; D-03 explizit einhalten; Acceptance-Criteria grep-Gate |
| T-80-05-02 | Denial of Service | UserGlobalRolesTab — letzten Admin sperren via Revoke | mitigate | Inline-Fehler aus Backend-409 in Modal; kein automatisches Modal-Schliessen bei Fehler |
| T-80-05-03 | Information Disclosure | UserContributionsTab — zeigt Legacy-Contributions ohne Member-ID | mitigate | Legacy-Gruppe ist explizit als "Historisch / Legacy" markiert; kein Recht wird daraus abgeleitet (D-04) |
| T-80-05-04 | Repudiation | UserAuditTab — Audit nach Mutation aus Tab 2 fehlt | mitigate | Audit-Tab zeigt alle AuditLogEntries mit actor_app_user_id; UAT-Schritt 7 verifiziert dies live |
| T-80-05-SC | Tampering | npm/pip/cargo Installs | accept | Keine neuen externen Pakete — kein Slopcheck nötig |
</threat_model>

<verification>
Phase-80-Abschluss-Verifikation:

Backend:
- go test ./internal/handlers -run AdminUsers -count=1: PASS
- go test ./internal/repository -run "AdminUsers|MemberIDAnchor|Authz" -count=1: PASS
- go build ./...: fehlerfrei

Frontend:
- npm test -- src/app/admin/users: alle Tests PASS
- npm run typecheck: fehlerfrei
- npm run lint: keine Fehler
- Zeilen-Limit: wc -l frontend/src/app/admin/users/tabs/*.tsx gibt jeweils <= 450

Manuell (UAT):
- Alle 9 UAT-Schritte aus checkpoint:human-verify bestätigt
- Audit-Eintrag nach Rollen-/Status-Mutation via Audit-Tab sichtbar
- Non-Admin-Denial ohne Daten-Flash bestätigt
</verification>

<success_criteria>
Die gesamte /admin/users-Oberfläche ist produktionsfertig:
1. Liste mit 11 Aggregat-Spalten, Server-seitiger Pagination, Suche und 3 Filtern.
2. User-Detail-Drawer mit 9 Tabs (lazy-geladen), modularen Tab-Dateien (je <= 450 Z.).
3. Editierbar: Globale Rollen (Assign/Revoke + Last-Admin-Guard) und Accountstatus (Disable/Reactivate + Last-Admin-Guard).
4. Read-only mit Deep-Links: Claims, Mitgliedschaften, Gruppenrechte, Contributions (D-13 Phase-83-aware), Medien (D-15), Audit.
5. Alle Mutations auditiert, Platform-Admin-Gate auf jedem Backend-Endpunkt.
6. UAT vom Entwickler bestätigt (checkpoint:human-verify approved).
</success_criteria>

<output>
Erstelle `.planning/phases/80-admin-users-user-detail-drawer-scoped-rechte/80-05-SUMMARY.md` wenn fertig.
</output>
