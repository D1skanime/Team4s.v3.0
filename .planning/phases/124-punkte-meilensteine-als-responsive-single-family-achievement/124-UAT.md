# Phase 124 UAT Evidence

## Checkpoint status

**BLOCKED at Plan 124-03 Task 2 evidence capture.**

Technical gate green after SSR correction. Live navigation reached the real public profile and terminal Stage. Eight screenshots are absent because IAB and supporting Playwright capture timed out/reset, including orchestrator retry. Approval was not requested.

- Route: http://127.0.0.1:3300/members/csubs-leader
- Navigation: Hauptnavigation -> Rangliste -> CSubs Leader
- Observed: 2'733 Punkte, Archiv-Legende, six stations
- 124-REPORT.md: absent

## Verification

Focused suite: **5 files passed; 182 passed; 1 skipped.** All 15 page tests passed, including SSR cases 0, 734, 2500, and 5000. FocalCarousel and FansubProjectsGrid passed. Existing FocalCarousel act(...) warnings remain.

- Full suite: five unrelated release-media/gallery failures.
- Typecheck: existing generated .next route-prop errors and known MemberBadgeChain.test.tsx:941 error.
- Lint: 0 errors, 326 existing warnings.
- git diff --check: passed.
- Plan 124-03 changed no app or protected consumer file.

## Live evidence

Public route reached through visible Rangliste navigation. Terminal Stage showed 2'733 points, Archiv-Legende, and six stations. Focused tests verify preview, ARIA, contained artwork CSS, local overflow, focus, and reduced motion. Visual viewport review remains incomplete.

Missing: points-390.png, points-768.png, points-1024.png, points-1440.png, points-1920.png, points-2560.png, points-preview.png, points-max.png.

Capture attempts: IAB multi-viewport and single-view timed out/reset; supporting Playwright navigation timed out despite HTTP 200; orchestrator retry timed out/reset.

## Resume condition

Resume Task 2 when screenshot export is stable. Capture and inspect all eight images before presenting the human checkpoint. This was the pre-approval resume condition; the later explicit approval accepts the missing evidence.

## Mobile scrollbar UAT retry — 2026-08-11

Focused regression rerun after the scrollbar fix: **182 passed, 1 skipped**; git diff --check passed. Source and contract verification confirm:

- pointsStageTrack retains overflow-x: auto, overscroll-behavior-inline: contain, and scroll-snap-type: x proximity.
- Firefox scrollbar suppression uses scrollbar-width: none.
- WebKit suppression uses .pointsStageTrack::-webkit-scrollbar { display: none; }.
- The 390px rule retains six fixed 112px columns, so the local track remains wider than its viewport and later stages are intended to remain reachable; a following thumbnail may be clipped as the scroll affordance.
- Desktop retains six minmax(132px, 1fr) columns with no arrow controls or outer FocalCarousel wrapper.
- Focused tests preserve the no-outer-carousel and protected shared-consumer contracts.

Live retry result: **blocked**. The in-app browser reported unavailable. A supporting Playwright run through the container gateway timed out before returning scrollLeft, overflow geometry, or screenshots. Therefore visual scrollbar absence, actual scrollLeft change, later-stage reachability, 390px document overflow, and desktop no-overflow geometry are not claimed as live-verified. At retry time, approval could not yet be requested.


## Human approval

- Exact signal: approved
- Recorded: 2026-08-11T11:10:21+00:00
- Decision: the user explicitly authorized closing Phase 124.
- Accepted limitation: automated screenshot capture and remaining live runtime geometry/interaction capture were unavailable.

The eight named screenshots still do not exist and are not claimed as evidence. Approval accepts that limitation alongside the recorded technical results, live route observation, terminal Stage observation, source contracts, and failed capture attempts.

Plan 124-03 approval gate is satisfied. Formal downstream closure may proceed separately; this artifact does not itself create 124-REPORT.md or execute Plan 124-04.
