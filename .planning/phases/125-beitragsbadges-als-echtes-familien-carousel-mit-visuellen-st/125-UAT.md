# Phase 125 UAT Evidence Ledger

Date: 2026-08-11
Plan: 125-03
Status: blocked before human approval

## Protected dirty-worktree boundary (D-39)

- `git diff --cached --name-only`: empty before evidence work.
- `git diff --check`: PASS.
- No application source, `FocalCarousel` file, Contribution PNG, ROADMAP, backend/API/DB file, dependency file, or migration was staged or committed by Plan 125-03.
- The Plan 125-02 implementation remains present and unstaged in `MemberBadgeChain.tsx`, `MemberBadgeChain.module.css`, and `memberBadgeLabels.ts` because its hunks overlap protected Phase-121/123/124 work.
- The six pre-existing modified Contribution PNGs and all other user-owned dirty changes remain untouched.
- No backend/API/DB/dependency change, second carousel, or inner gesture engine was introduced.

## Automated regression matrix

### Full frontend suite

Command: `docker compose exec -T team4sv30-frontend npm test`

Result: FAIL ? 230 test files passed, 5 failed, 1 skipped; 1683 tests passed, 7 failed, 1 skipped, 3 todo.

The two expected stale protected assertions are:

1. `MemberBadgeChain Phase 119 collection cards > renders independent family cards with authoritative progressbar values and exact copy`
2. `Phase 120 Task 2: keeps SSR carousel content while expensive listeners remain dormant`

Five additional pre-existing/out-of-scope failures occurred in `ResponsiveImage.config.test.ts`, `ReleaseVersionNotesTab.test.tsx`, `admin/fansubs/[id]/edit/page.test.tsx`, and two `ReleaseGallery.test.tsx` cases. They prevent a clean full-suite gate and were not changed.

### Focused Phase 125/shared suite

Command: `docker compose exec -T team4sv30-frontend npx vitest run src/components/profile/memberBadgeLabels.test.ts src/components/profile/MemberBadgeChain.test.tsx src/components/ui/FocalCarousel.test.tsx src/app/members/[slug]/page.test.tsx`

Result: FAIL ? 3 files passed, 1 failed; 201 tests passed, 2 known stale Phase-119/120 assertions failed, 1 skipped. Phase-125 label, carousel, profile, geometry, preview, accessibility, keyboard, Home/End, expanded-mode, and shared `FocalCarousel` coverage otherwise passed.

### Typecheck

Command: `docker compose exec -T team4sv30-frontend npm run typecheck`

Result: FAIL ? existing generated `.next/dev/types` route-prop constraints plus the existing `MemberBadgeChain.test.tsx:1011` `badgeProgress` prop typing error. No Phase-125 production source error was identified.

### Lint and build

- Lint was started but its SSH wrapper was interrupted while the host was saturated; duplicate executor-owned lint processes were stopped. No truthful complete lint result is available for this run. Plan 125-02 recorded 0 errors and 326 pre-existing warnings, but that result is not claimed as a Plan 125-03 rerun.
- Build did not start after the interrupted chained wrapper. No build pass is claimed.

### Backend visibility gate

Command: `docker compose exec -T team4sv30-backend go test ./internal/repository ./internal/handlers`

Result: FAIL ? repository passed; handlers failed because `/shared/contracts/openapi.yaml` is unavailable inside the backend container. This is an environment/mount failure, not a visibility assertion failure. No backend files were changed.

## Live shared-flow review

- Browser: Codex in-app browser.
- Route/navigation: `http://127.0.0.1:3300/` ? visible `Rangliste` navigation ? `CSubs Leader` row ? `http://127.0.0.1:3300/members/csubs-leader`.
- Observed: the public profile loaded; Beitr?ge exposes Anime-Projekte, Chronikpflege, and Bildarchivpflege families; each family exposes Bronze/Silber/Gold tier controls; `Alle Auszeichnungen in Beitr?ge anzeigen` is present.
- The seeded profile showed terminal Gold contribution states, including 50 Anime-Projekte and 150 Chronikbeitr?ge.
- This confirms intended-route discoverability and live rendering only. It does not substitute for the missing viewport/state screenshots or device touch observations.

## Missing required evidence / blockers

- Required six viewport screenshots (390?844, 768?1024, 1024?768, 1440?900, 1920?1080, 2560?1440): not persisted. The in-app-browser screenshot batch timed out and reset its session before files were produced.
- Required five state screenshots (Projects, Chronicle, Archivist, earned preview, expanded): not captured.
- Zero/below-Bronze, Bronze, Silber, Gold, and above-Gold live fixtures were not all available through the observed seeded profile.
- Real smartphone touch observations: not performed; requires a human on physical hardware.
- Real tablet touch observations: not performed; requires a human on physical hardware.
- Lint and build reruns remain incomplete.
- Full frontend and backend matrices are not green for the documented unrelated/environment failures.

deviations: unresolved
open_issues: screenshot evidence, real-device touch evidence, lint/build reruns, full-suite failures, backend contract mount

## Human verification still required

On a real smartphone and a real tablet, open `http://127.0.0.1:3300/members/csubs-leader` through the visible ranking route. Swipe free card space between families, tap earned tier thumbnails, vertically scroll, and confirm that thumbnail taps never change family and carousel drags never fire a tier click. On tablet also verify stable height, tier row, neighbor visibility, `Alle anzeigen`, and no overflow. Record device model, OS/browser, viewport/orientation, state, and observation.

