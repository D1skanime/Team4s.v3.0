---
phase: 116-personalisiertes-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/dashboard.ts
  - frontend/src/types/contributions.ts
  - frontend/src/components/profile/memberBadgeLabels.ts
  - frontend/src/components/profile/memberBadgeLabels.test.ts
  - frontend/src/app/me/dashboard/components/attentionHelpers.ts
  - frontend/src/app/me/dashboard/components/attentionHelpers.test.ts
autonomous: true
requirements: [D-02, D-04]

must_haves:
  truths:
    - "A shared OwnDashboardData/OwnDashboardResponse TS contract exists that every later dashboard plan (backend, api.ts, components) imports without redefining shapes (D-03/D-04 foundation)"
    - "isRecentlyAssigned(created_at, windowDays=14) correctly classifies 'neu' assignments per D-02 Variante A, and resolveWorkspaceHref picks the release-version workspace when release_version_id is set, else the project workspace"
    - "memberBadgeLabels.ts exposes reusable next-threshold helpers for point milestones and role-volume tiers so D-04's category-progress table never redefines these thresholds (RESEARCH Anti-Pattern: no new threshold values in Phase 116)"
  artifacts:
    - path: "frontend/src/types/dashboard.ts"
      provides: "OwnDashboardData, OwnDashboardRoleVolumeEntry, OwnDashboardCategoryProgress, OwnDashboardResponse interfaces"
    - path: "frontend/src/app/me/dashboard/components/attentionHelpers.ts"
      provides: "isRecentlyAssigned, resolveWorkspaceHref, ATTENTION_WINDOW_DAYS"
    - path: "frontend/src/components/profile/memberBadgeLabels.ts"
      provides: "exported POINT_MILESTONES/ROLE_VOLUME_TIER_THRESHOLDS plus resolveNextPointMilestone/resolveNextRoleVolumeThreshold helpers"
  key_links:
    - from: "frontend/src/app/me/dashboard/components/attentionHelpers.ts"
      to: "frontend/src/types/contributions.ts (MeAnimeContribution.created_at/confirmed_at)"
      via: "additive TS field read by isRecentlyAssigned"
      pattern: "created_at: string"
---

<objective>
Establish the shared, additive TypeScript contracts and pure helper functions that every other
Phase 116 plan (backend aggregation, frontend data layer, five section components) imports
directly instead of re-deriving. This is the Wave-0/interface-first foundation: no UI, no backend
change — only type definitions and pure, isolated-testable functions.

Purpose: prevents the "scavenger hunt" anti-pattern (later plans exploring the codebase to invent
the dashboard DTO shape or re-implement badge thresholds) and locks in D-02's "neu"-window logic
and D-04's threshold-reuse requirement before any component exists.

Output: `frontend/src/types/dashboard.ts` (new contract), additive `created_at`/`confirmed_at` on
`MeAnimeContribution`, additive exports + two new pure helper functions on
`frontend/src/components/profile/memberBadgeLabels.ts`, and a new
`frontend/src/app/me/dashboard/components/attentionHelpers.ts` pure-function module — all fully
unit-tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-personalisiertes-dashboard/116-CONTEXT.md
@.planning/phases/116-personalisiertes-dashboard/116-RESEARCH.md
@.planning/phases/116-personalisiertes-dashboard/116-PATTERNS.md
@.planning/phases/116-personalisiertes-dashboard/116-UI-SPEC.md
</context>

<interfaces>
<!-- Exact current state, verified in-repo. Executor should use these directly. -->

Current `MeAnimeContribution` (frontend/src/types/contributions.ts:75-108) — does NOT yet declare
`created_at`/`confirmed_at`, although the backend JSON already includes both fields today
(`AnimeContributionRow` in backend/internal/repository/anime_contributions_inputs.go:11-32,
`CreatedAt time.Time` json:"created_at", `ConfirmedAt *time.Time` json:"confirmed_at"):
```typescript
export interface MeAnimeContribution {
  id: number
  anime_id: number
  anime_title?: string
  fansub_group_id: number
  fansub_group_member_id: number
  status: 'confirmed' | 'proposed' | 'draft' | 'disputed' | 'hidden'
  role_codes: string[]
  // ... unrelated existing fields unchanged ...
  release_version_id: number | null
}
```

Current `memberBadgeLabels.ts` private constants that must become exported (lines verified):
- `const POINT_MILESTONES` (line 226) — descending array of `{ threshold: number; badge_code: string }`, lowest threshold is 1.
- `const ROLE_VOLUME_TIER_THRESHOLDS` (line 157) — `Record<RoleVolumeTier, number>` = `{ bronze: 12, silver: 108, gold: 320, platinum: 510 }`.
- `type RoleVolumeTier` (line 148) is currently NOT exported either — must become exported so the
  new helper's return type is usable from other modules.
- `deriveMilestoneBadge(totalPoints)` (line 237) already exists and stays unchanged — reuse it
  inside the new `resolveNextPointMilestone` helper rather than reimplementing "current badge".
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared dashboard DTO contract + additive contribution timestamp fields</name>
  <files>frontend/src/types/dashboard.ts, frontend/src/types/contributions.ts</files>
  <read_first>
    frontend/src/types/contributions.ts (full MeAnimeContribution interface, lines 75-108, and the MeAnimeContributionsResponse wrapper at line 176)
    backend/internal/repository/anime_contributions_inputs.go (AnimeContributionRow struct, lines 11-32 — confirms created_at/confirmed_at already serialize today)
    frontend/src/types/profile.ts (MemberProfileMembership interface, lines 13-23 — naming/style convention for flat additive DTOs to mirror)
  </read_first>
  <behavior>
    - No runtime behavior (pure type declarations) — verification is `tsc --noEmit` passing plus a
      type-only compile-check test asserting the new interfaces are importable and structurally
      match the documented shape.
  </behavior>
  <action>
    Create `frontend/src/types/dashboard.ts` exporting exactly:
    `OwnDashboardRoleVolumeEntry` (`role_code: string`, `count: number`);
    `OwnDashboardCategoryProgress` (`family: 'contribution_projects' | 'contribution_chronicle' | 'contribution_archivist'`,
    `current_tier: string`, `current_count: number`, `next_threshold: number | null`);
    `OwnDashboardData` (`has_member_profile: boolean`, `total_points: number`, `badges_count: number`,
    `projects_count: number`, `images_count: number`, `contributions_count: number`,
    `role_volume: OwnDashboardRoleVolumeEntry[]`, `category_progress: OwnDashboardCategoryProgress[]`);
    `OwnDashboardResponse` (`data: OwnDashboardData`). Add short purpose comments referencing D-03/D-04
    and noting that `category_progress` intentionally excludes point-milestone and role-volume rows —
    those are computed client-side in Plan 116-05 from `total_points`/`role_volume` plus the
    exported `memberBadgeLabels.ts` threshold helpers (Task 2), never duplicated server-side.
    Then additively extend `MeAnimeContribution` in `frontend/src/types/contributions.ts` with
    `created_at: string` and `confirmed_at?: string | null`, with a comment citing D-02/Pattern 3
    and the exact backend source field. Do not touch any other field on this interface.
  </action>
  <acceptance_criteria>
    - `frontend/src/types/dashboard.ts` exports OwnDashboardRoleVolumeEntry, OwnDashboardCategoryProgress, OwnDashboardData, OwnDashboardResponse
    - `MeAnimeContribution` in contributions.ts contains `created_at: string` and `confirmed_at?: string | null` with all pre-existing fields unchanged
    - `cd frontend && npx tsc --noEmit` passes with zero new errors
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Both files compile clean; the dashboard DTO contract exists and MeAnimeContribution carries the two additive timestamp fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Export badge thresholds + next-threshold helpers in memberBadgeLabels.ts (D-04)</name>
  <files>frontend/src/components/profile/memberBadgeLabels.ts, frontend/src/components/profile/memberBadgeLabels.test.ts</files>
  <read_first>
    frontend/src/components/profile/memberBadgeLabels.ts (full file — POINT_MILESTONES lines 223-233, deriveMilestoneBadge lines 235-240, ROLE_VOLUME_TIER_THRESHOLDS/LABELS/TIERS lines 144-176, resolveRoleVolumePresentation lines 178-206)
    frontend/src/components/profile/memberBadgeLabels.test.ts (existing describe blocks/import style, lines 1-40 — additive test style to follow, no new mocks needed)
  </read_first>
  <behavior>
    - `resolveNextPointMilestone(0)` returns `{ currentBadge: null, nextThreshold: 1 }`.
    - `resolveNextPointMilestone(50)` returns `{ currentBadge: { badge_code: 'point_milestone_active', ... }, nextThreshold: 200 }`.
    - `resolveNextPointMilestone(2500)` returns `{ currentBadge: { badge_code: 'point_milestone_legend', ... }, nextThreshold: null }` (highest tier, no next).
    - `resolveNextRoleVolumeThreshold(0)` returns `{ currentTier: '', nextThreshold: 12, nextTierLabel: 'Bronze' }`.
    - `resolveNextRoleVolumeThreshold(12)` returns `{ currentTier: 'bronze', nextThreshold: 108, nextTierLabel: 'Silber' }`.
    - `resolveNextRoleVolumeThreshold(510)` returns `{ currentTier: 'platinum', nextThreshold: null, nextTierLabel: null }`.
  </behavior>
  <action>
    Add the `export` keyword to the existing `POINT_MILESTONES` const (line 226) and
    `ROLE_VOLUME_TIER_THRESHOLDS` const (line 157) — no value changes, additive export only.
    Export the existing `RoleVolumeTier` type (line 148) the same way. Add two new exported pure
    functions in the same file: `resolveNextPointMilestone(totalPoints: number)` — sorts
    `POINT_MILESTONES` ascending by threshold internally (the source array is stored descending),
    finds the smallest threshold strictly greater than `totalPoints` for `nextThreshold`, and reuses
    the existing `deriveMilestoneBadge(totalPoints)` unchanged for `currentBadge` (do not
    reimplement "which milestone is currently held"). `resolveNextRoleVolumeThreshold(count: number)`
    — iterates the four tiers in ascending order (`bronze`, `silver`, `gold`, `platinum`), sets
    `currentTier` to the highest tier whose threshold `count` meets or exceeds, and `nextThreshold`/
    `nextTierLabel` to the next tier's threshold/German label (reusing the existing
    `ROLE_VOLUME_TIER_LABELS` map) or `null` if `platinum` is already reached. Add a short comment
    on both functions referencing D-04 and stating they exist specifically so
    `CategoryProgressTable.tsx` (Plan 116-05) never redefines these threshold values.
    Add new test cases to `memberBadgeLabels.test.ts` covering all six behavior bullets above,
    following the existing file's describe/it style (no new mocks required — pure functions only).
  </action>
  <acceptance_criteria>
    - `POINT_MILESTONES`, `ROLE_VOLUME_TIER_THRESHOLDS`, `RoleVolumeTier` are exported from memberBadgeLabels.ts
    - `resolveNextPointMilestone` and `resolveNextRoleVolumeThreshold` are exported and match all six behavior cases exactly
    - `cd frontend && npx vitest run src/components/profile/memberBadgeLabels.test.ts` is fully green including the new cases, with zero regressions on pre-existing cases
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/profile/memberBadgeLabels.test.ts</automated>
  </verify>
  <done>Both threshold constants plus RoleVolumeTier are exported, the two next-threshold helpers exist and pass all new test cases, and every pre-existing test in the file still passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: attentionHelpers.ts — isRecentlyAssigned / resolveWorkspaceHref (D-02)</name>
  <files>frontend/src/app/me/dashboard/components/attentionHelpers.ts, frontend/src/app/me/dashboard/components/attentionHelpers.test.ts</files>
  <read_first>
    frontend/src/types/contributions.ts (MeAnimeContribution shape, post-Task-1 with created_at/confirmed_at)
    frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.tsx (confirms this is a real, reachable route for the anime-level workspace target — read the route segment names only, no need to read full file body)
    frontend/src/app/me/releases/[versionId]/workspace/page.tsx (confirms this is a real, reachable route for the release-version-level workspace target — read the route segment names only)
  </read_first>
  <behavior>
    - `isRecentlyAssigned('2026-07-20T00:00:00Z', 14)` at a mocked "now" of `2026-07-28` returns `true` (8 days old, within window).
    - `isRecentlyAssigned('2026-07-01T00:00:00Z', 14)` at the same mocked "now" returns `false` (27 days old, outside window).
    - `resolveWorkspaceHref({ release_version_id: 62, anime_id: 5, fansub_group_id: 9 })` returns `'/me/releases/62/workspace'`.
    - `resolveWorkspaceHref({ release_version_id: null, anime_id: 5, fansub_group_id: 9 })` returns `'/me/projects/5/group/9'`.
    - `ATTENTION_WINDOW_DAYS` equals `14`.
  </behavior>
  <action>
    Create `frontend/src/app/me/dashboard/components/attentionHelpers.ts` exporting
    `ATTENTION_WINDOW_DAYS = 14` (named constant with a comment citing D-02/CONTEXT.md Claude's
    Discretion, not a magic number), `isRecentlyAssigned(createdAt: string, windowDays: number): boolean`
    (age-in-ms comparison against `Date.now()`, exactly the Pattern-3 formula from 116-RESEARCH.md),
    and `resolveWorkspaceHref(contribution: Pick<MeAnimeContribution, 'release_version_id' | 'anime_id' | 'fansub_group_id'>): string`
    (release_version_id present -> `/me/releases/${release_version_id}/workspace`; else ->
    `/me/projects/${anime_id}/group/${fansub_group_id}`, exactly Pattern 2 from 116-RESEARCH.md).
    Create the companion test file mocking `Date.now`/`vi.setSystemTime` for a fixed "now" and
    covering all five behavior bullets above.
  </action>
  <acceptance_criteria>
    - Both functions and the constant are exported from attentionHelpers.ts
    - All five behavior cases pass exactly as specified
    - `cd frontend && npx vitest run src/app/me/dashboard/components/attentionHelpers.test.ts` is fully green
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/app/me/dashboard/components/attentionHelpers.test.ts</automated>
  </verify>
  <done>attentionHelpers.ts exists with both pure functions and the named window constant, fully covered by a green test file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None (this plan) | Pure type declarations and pure client-side functions; no network I/O, no user input, no persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-116-01-01 | Tampering | attentionHelpers.ts / memberBadgeLabels.ts pure functions | accept | No external input surface; inputs are already-typed values from trusted internal callers, no user-controlled parsing |
| T-116-01-SC | Tampering (supply chain) | npm installs | accept | No new package installs in this plan |
</threat_model>

<verification>
`cd frontend && npx tsc --noEmit` clean, plus both new/modified test files fully green:
`npx vitest run src/components/profile/memberBadgeLabels.test.ts src/app/me/dashboard/components/attentionHelpers.test.ts`.
</verification>

<success_criteria>
The dashboard DTO contract, the additive contribution timestamp fields, the exported badge-threshold
helpers, and the D-02 attention pure functions all exist, are fully unit-tested, and compile clean —
so every later Phase 116 plan can import them directly instead of re-deriving shapes or thresholds.
</success_criteria>

<output>
Create `.planning/phases/116-personalisiertes-dashboard/116-01-SUMMARY.md` when done
</output>
