---
phase: 116-personalisiertes-dashboard
plan: 06
type: execute
wave: 3
depends_on: ["116-01", "116-02", "116-03", "116-04", "116-05"]
files_modified:
  - frontend/src/app/me/dashboard/page.tsx
  - frontend/src/app/me/dashboard/page.module.css
  - frontend/src/app/me/dashboard/page.test.tsx
autonomous: false
requirements: [D-01, D-07, D-09]

must_haves:
  truths:
    - "/me/dashboard renders for every authenticated user regardless of has_member_profile/has_project_assignments -- no router.replace/redirect gate exists anywhere in page.tsx (D-09, explicit contrast to /me/contributions)"
    - "page.tsx contains zero editable form controls, buttons that mutate data, or upload widgets -- it only composes the five read-only sections and renders links/numbers (D-01)"
    - "All three data sources (getOwnProfile, getMyAnimeContributions, getOwnDashboard) load via a single Promise.all, never sequentially (Pattern 1, avoids the known SSR-request-fan-out regression class)"
    - "The five sections render in the fixed UI-SPEC order: AttentionSection, DashboardMetrics, CategoryProgressTable, MyGroupsSection, QuickLinksSection, all inside a PageHeader-topped layout using only @/components/ui primitives (D-07)"
  artifacts:
    - path: "frontend/src/app/me/dashboard/page.tsx"
      provides: "The /me/dashboard route composing all five Phase-116 sections"
  key_links:
    - from: "frontend/src/app/me/dashboard/page.tsx"
      to: "frontend/src/lib/api.ts (getOwnDashboard, Plan 116-03)"
      via: "Promise.all([getOwnProfile(), getMyAnimeContributions(), getOwnDashboard()])"
      pattern: "Promise\\.all\\(\\["
    - from: "frontend/src/components/layout/AppShell.tsx (Dashboard nav entry, Plan 116-03)"
      to: "frontend/src/app/me/dashboard/page.tsx"
      via: "href '/me/dashboard' now resolves to a real route"
      pattern: "me/dashboard"
---

<objective>
Compose the five already-built, already-tested Phase 116 section components into the real
`/me/dashboard` route, wired to the three real data sources via one parallel fetch — and prove,
with a live human check, that the whole page works end to end for a real logged-in user with no
eligibility redirect anywhere on the page (D-09).

Purpose: this is the final wiring wave. Every other artifact (contracts, backend endpoint, api
client, nav entry, five section components) already exists and is independently tested; this plan's
only new risk is the composition itself — getting the parallel-fetch shape right, keeping the page
file thin (450-line limit — composition only, no inline section logic), and confirming the
previously-dead nav entry now leads somewhere real and correct.

Output: a working `/me/dashboard` page, its test file, and a completed live-verification checkpoint
on `:3000` covering every UI-SPEC section, every empty state, and the D-02 direct-link
no-403 guarantee.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-personalisiertes-dashboard/116-CONTEXT.md
@.planning/phases/116-personalisiertes-dashboard/116-UI-SPEC.md
@.planning/phases/116-personalisiertes-dashboard/116-VALIDATION.md
@.planning/phases/116-personalisiertes-dashboard/116-PATTERNS.md
</context>

<interfaces>
<!-- All five props contracts this page composes against, fixed by Plans 116-01/04/05. -->

```typescript
// AttentionSection.tsx (Plan 116-04)
function AttentionSection(props: { contributions: MeAnimeContribution[] }): JSX.Element
// DashboardMetrics.tsx (Plan 116-04)
function DashboardMetrics(props: { data: OwnDashboardData }): JSX.Element
// CategoryProgressTable.tsx (Plan 116-05)
function CategoryProgressTable(props: { data: OwnDashboardData }): JSX.Element
// MyGroupsSection.tsx (Plan 116-05)
function MyGroupsSection(props: { memberships: MemberProfileMembership[] }): JSX.Element
// QuickLinksSection.tsx (Plan 116-05)
function QuickLinksSection(): JSX.Element
```

`frontend/src/app/me/contributions/page.tsx` (lines 1-95, the pattern to explicitly NOT copy) —
`isEligibleForContributions` + `router.replace('/me/profile')` (lines 42-44, 80-86). Every line of
that eligibility-redirect logic must be ABSENT from this plan's `page.tsx`.

`frontend/src/app/me/profile/page.tsx` (lines 1-60, 209-219 region) — the session-gate
(`useAuthSession` -> `hasAccessToken || hasRefreshToken` -> `isClientInitialized`) and
`Promise.all([...])` loader-callback shape to replicate structurally, substituting this page's own
three data sources.

`frontend/src/lib/api.ts` — `getOwnProfile()`, `getMyAnimeContributions()` (existing),
`getOwnDashboard()` (Plan 116-03) — all three called in one `Promise.all`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: page.tsx — compose the five sections behind one parallel fetch, no eligibility gate</name>
  <files>frontend/src/app/me/dashboard/page.tsx, frontend/src/app/me/dashboard/page.module.css, frontend/src/app/me/dashboard/page.test.tsx</files>
  <read_first>
    frontend/src/app/me/contributions/page.tsx (full file — the eligibility-redirect pattern that must NOT appear here, plus its Promise.all/error-handling shape as a structural analog)
    frontend/src/app/me/profile/page.tsx (lines 1-60, 160-230 — useAuthSession session-gate and LoadingState/ErrorState usage pattern)
    frontend/src/lib/api.ts (getOwnProfile, getMyAnimeContributions, getOwnDashboard signatures)
    frontend/src/components/ui/index.ts (confirms PageHeader, LoadingState, ErrorState are exported)
  </read_first>
  <behavior>
    - Given a logged-in user with `has_member_profile: false` and `has_project_assignments: false`, the page still renders fully (no `router.replace` call ever fires) — every section renders its own empty state instead.
    - Given all three fetches resolve, the page renders one `PageHeader` (Eyebrow "Mein Bereich", Titel "Dashboard") followed by the five sections in exact order: AttentionSection, DashboardMetrics, CategoryProgressTable, MyGroupsSection, QuickLinksSection.
    - Given `isClientInitialized` is false or the fetch is in flight, renders `LoadingState` with the exact UI-SPEC copy ("Dein Dashboard wird geladen" / "Kennzahlen, Fortschritt und Gruppen werden zusammengestellt.").
    - Given any of the three fetches rejects, renders `ErrorState` with the exact UI-SPEC copy ("Dashboard konnte nicht geladen werden" / retry copy) instead of a partial/crashed page.
    - The three fetches (`getOwnProfile`, `getMyAnimeContributions`, `getOwnDashboard`) are invoked inside a single `Promise.all([...])`, never sequentially awaited one after another.
  </behavior>
  <action>
    Write the test file first (RED): assert no `router.replace`/`router.push` call ever occurs
    regardless of eligibility fields (mock `next/navigation`'s `useRouter` and assert it is never
    invoked for navigation), assert all five section components render in DOM order, assert the
    loading/error states render the exact UI-SPEC copy, and assert the three API functions are each
    called exactly once inside what resolves as a single microtask batch (mock all three, assert
    call order/count, not a specific implementation detail of Promise.all itself). Confirm these
    fail against a stub/empty page.tsx for the right reasons. Then implement `page.tsx` as a client
    component: `useAuthSession()` for the session gate (identical to `me/profile/page.tsx`), a
    `loadDashboard` callback that runs `Promise.all([getOwnProfile(), getMyAnimeContributions(),
    getOwnDashboard()])` and stores each result in local state, `useEffect` to trigger it once
    `isClientInitialized && hasAuthSession` is true, `LoadingState`/`ErrorState` branches with the
    exact copy from the UI-SPEC Copywriting Contract, and — once loaded — a `PageHeader` followed by
    the five sections in the fixed order, passing each section exactly the props shown in this
    plan's `<interfaces>` block. Do NOT add any eligibility check, redirect, or router navigation
    call anywhere in this file — this is the single most important behavioral contrast with
    `me/contributions/page.tsx` and is the explicit subject of this task's first behavior bullet.
    Add `page.module.css` containing only section-spacing rules per the UI-SPEC Spacing Scale
    (`--space-6` between sections, `--space-7` between PageHeader and the first section) — no new
    color values. Keep `page.tsx` itself under the 450-line CLAUDE.md limit by containing only
    fetch orchestration and section composition — if it approaches the limit, extract the
    loader callback into a colocated helper file rather than inlining more JSX.
  </action>
  <acceptance_criteria>
    - No call to router.replace/router.push exists anywhere in page.tsx for any eligibility condition
    - All five sections render in the fixed order with the props specified in this plan's interfaces block
    - LoadingState/ErrorState render the exact UI-SPEC copy
    - getOwnProfile/getMyAnimeContributions/getOwnDashboard are all invoked inside one Promise.all, not sequential awaits
    - `cd frontend && npx vitest run src/app/me/dashboard/page.test.tsx` is fully green
    - `cd frontend && npx tsc --noEmit` passes
    - `wc -l frontend/src/app/me/dashboard/page.tsx` reports 450 lines or fewer
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/app/me/dashboard/page.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>/me/dashboard renders for every authenticated user with no redirect gate, composing all five sections behind one parallel fetch, fully covered by a green test and under the line-count limit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-verify /me/dashboard end to end on :3000</name>
  <files></files>
  <read_first>
    .planning/phases/116-personalisiertes-dashboard/116-UI-SPEC.md
    .planning/phases/116-personalisiertes-dashboard/116-VALIDATION.md
  </read_first>
  <what-built>
    A working `/me/dashboard` route composing five sections — "Braucht deine Aufmerksamkeit" (D-02),
    "Deine Zahlen" (D-03), "Fortschritt je Kategorie" (D-04), "Meine Gruppen" (D-05), and
    "Schnellzugriffe" (D-06) — plus the previously-dead "Dashboard" nav entry now enabled in "Mein
    Bereich" and pointing at this route, plus the new read-only `GET /api/v1/me/dashboard` backend
    aggregation feeding it.
  </what-built>
  <action>
    Rebuild both containers so the new backend route and frontend build are live
    (`docker compose up -d --build team4sv30-backend`, then `docker restart team4sv30-frontend`),
    then present the already-automated `/me/dashboard` page for human live verification on `:3000`;
    append only the observed outcomes, do not re-derive UI decisions already locked in
    `116-UI-SPEC.md`.
  </action>
  <how-to-verify>
    1. After the rebuild/restart, hard-refresh (Strg+F5) on `:3000` — per CLAUDE.md, Turbopack HMR
       is unreliable for this stack.
    2. Log in as a member with at least one confirmed anime contribution and at least one group
       membership. Open the nav drawer: confirm "Dashboard" now appears in "Mein Bereich" (next to
       "Mein Profil"), enabled, no "bald" badge.
    3. Click it, land on `/me/dashboard`. Confirm all five sections render in order: Aufmerksamkeit
       -> Deine Zahlen (5 tiles: Punkte/Badges/Projekte/Hochgeladene Bilder/Geschriebene Beiträge)
       -> Fortschritt je Kategorie (table) -> Meine Gruppen -> Schnellzugriffe (5 tiles, first one
       accented).
    4. If any recent (within 14 days) contribution/release assignment exists, confirm it shows a
       "Neu" badge in "Braucht deine Aufmerksamkeit" and clicking it opens the correct workspace
       (release workspace if it has a release_version_id, else the project workspace) WITHOUT a 403.
    5. Click a "Meine Gruppen" entry: confirm it opens the public `/fansubs/[slug]` profile (not an
       admin edit route, no 403) even for a member without group-edit rights.
    6. Log in (or reuse the same session) as a user with NO member profile / no contributions: open
       `/me/dashboard` directly by URL. Confirm the page still renders (no redirect to
       `/me/profile`) and each section shows its friendly empty state instead of crashing or
       blanking.
    7. Confirm the "Suche" quick-access tile is either a working link to `/suche` (if Phase 115 has
       shipped that route by now) or a visibly disabled "bald" tile — never a dead link that 404s.
    8. Resize to a narrow/mobile viewport and confirm the layout stacks vertically without
       overflow/clipping.
  </how-to-verify>
  <acceptance_criteria>
    - Dashboard nav entry reachable, correctly grouped, and correctly routed for authenticated users
    - All five sections render in the fixed order with correct data, empty states, and the D-02 "Neu" badge behavior
    - Direct-link navigation from Aufmerksamkeit and Meine Gruppen never returns a 403
    - No-member-profile users see the page render with graceful empty states instead of any redirect
    - Suche tile is never a dead link to a 404 route
  </acceptance_criteria>
  <verify>
    <automated>docker ps --filter "name=team4sv30-frontend" --format "{{.Status}}"</automated>
  </verify>
  <done>Human confirms nav activation, all five sections, the D-02 direct-link no-403 guarantee, the D-09 no-redirect graceful-empty behavior, and the defensive Suche tile all match expectations on live :3000.</done>
  <resume-signal>Type "approved" once all 8 steps check out, or describe exactly which step failed and what you saw.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client -> three parallel GETs on page load | Same existing authenticated-fetch boundary as every other /me/* page; no new trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-116-06-01 | Information Disclosure | page.tsx composing all five sections | accept | Every section receives only data the authenticated user's own three GETs already returned; no cross-user data ever enters this page |
| T-116-06-02 | Elevation of Privilege | Direct-link "workspace" hrefs (AttentionSection) and "Meine Gruppen" hrefs (MyGroupsSection) | accept | Dashboard only builds URLs; target routes (/me/releases/[id]/workspace, /me/projects/[animeId]/group/[fansubGroupId], /fansubs/[slug]) own their own access checks unchanged by this phase (documented in 116-RESEARCH.md Security Domain) |
| T-116-06-SC | Tampering (supply chain) | npm installs | accept | No new package installs in this plan |
</threat_model>

<verification>
`cd frontend && npx vitest run src/app/me/dashboard/page.test.tsx && npx tsc --noEmit && npm run test`
(full suite, confirming zero regressions across the whole Phase 116 change set), followed by the
blocking human-verify checkpoint on `:3000`.
</verification>

<success_criteria>
`/me/dashboard` is live, reachable from the newly-activated nav entry, renders all five D-02..D-06
sections in the fixed order for every authenticated user with no eligibility redirect, gracefully
empty for users with no member profile, and has passed a live human verification covering every
UI-SPEC section, empty state, and the D-02/D-05 no-403 direct-link guarantee.
</success_criteria>

<output>
Create `.planning/phases/116-personalisiertes-dashboard/116-06-SUMMARY.md` when done
</output>
