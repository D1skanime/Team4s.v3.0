---
phase: 32
plan: 02
type: execute
wave: 2
depends_on:
  - 32-01-PLAN.md
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css
  - frontend/src/lib/api.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "The release row Edit button opens a right Side Drawer."
    - "The drawer edits Theme asset assignments only; it does not edit Anime or timeline timings."
    - "Upload/delete controls call release-scoped Theme asset APIs and refresh the selected release cards."
---

<objective>
Replace the prepared inline segment editor with a Side Drawer that lets an admin view and edit existing release Theme asset assignments for the selected release.
</objective>

<context>
@.planning/phases/32-fansub-release-side-drawer-aus-phase-31-edit-drawer-fuer-release-theme-assets-mit-vorhandenen-db-tabellen-und-apis-ohne-neue-datenmodelle-db-ui-differenzen-vor-umsetzung-diskutieren/32-CONTEXT.md
@.planning/phases/32-fansub-release-side-drawer-aus-phase-31-edit-drawer-fuer-release-theme-assets-mit-vorhandenen-db-tabellen-und-apis-ohne-neue-datenmodelle-db-ui-differenzen-vor-umsetzung-diskutieren/32-RESEARCH.md
@frontend/src/app/admin/fansubs/[id]/edit/page.tsx
@frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css
@frontend/src/lib/api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add release drawer state and open behavior</name>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
    - frontend/src/lib/api.ts
  </read_first>
  <action>In `page.tsx`, add state `drawerRelease: AdminFansubRelease | null` and use existing `selectedReleaseSegment` for the selected Theme card. Add function `openReleaseDrawer(release: AdminFansubRelease)` that sets `drawerRelease`, expands the release if not expanded, and calls `loadReleaseSegmentCards(release)`. Change the release row `Edit` button from `onClick={() => toggleRelease(release)}` to `onClick={() => openReleaseDrawer(release)}`. Keep the right-side chevron calling `toggleRelease(release)` for preview open/close. Do not add any `Anime bearbeiten` link or button.</action>
  <acceptance_criteria>
    - `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` contains `drawerRelease`.
    - `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` contains `openReleaseDrawer`.
    - `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` contains `onClick={() => openReleaseDrawer(release)}`.
    - `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` still contains `onClick={() => toggleRelease(release)}` for the chevron.
    - `Select-String -LiteralPath 'frontend/src/app/admin/fansubs/[id]/edit/page.tsx' -Pattern 'Anime bearbeiten'` returns no matches.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render Side Drawer with release context and timeline cards</name>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
    - frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css
  </read_first>
  <action>Add a right Side Drawer rendered when `drawerRelease` is not null. Use CSS classes `fansubEditReleaseDrawerOverlay`, `fansubEditReleaseDrawer`, `fansubEditReleaseDrawerHeader`, `fansubEditReleaseDrawerTabs`, `fansubEditReleaseDrawerBody`, `fansubEditReleaseDrawerDropzone`, and `fansubEditReleaseDrawerFooter`. Header copy must use `Release bearbeiten`, `Episode {episode_number}`, and the episode title if present. Do not display `Release #{release_id}` as primary title. Inside the drawer, render tab buttons `Details`, `Theme Assets`, and `Historie`; only `Theme Assets` needs functional content in this phase. Show the same Timeline preview card data as the expanded row and let OP/ED/IN buttons set `selectedReleaseSegment` without allowing start/end time edits.</action>
  <acceptance_criteria>
    - `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` contains `Release bearbeiten`.
    - `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` contains `Theme Assets`.
    - `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` contains `fansubEditReleaseDrawer`.
    - `frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css` contains `.fansubEditReleaseDrawer`.
    - `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` contains no input bound to `start_time` or `end_time`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire upload/delete controls to existing release Theme asset APIs</name>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
    - frontend/src/lib/api.ts
    - frontend/src/types/admin.ts
  </read_first>
  <action>Import `uploadAdminReleaseThemeAssetForRelease` and existing `deleteAdminReleaseThemeAsset`. In the drawer, when a `selectedReleaseSegment` belongs to `drawerRelease`, show current status, Theme title, source label, and current media URL if `media_id`/`public_url` exists. For `status === 'global'` or locked Jellyfin source, render disabled copy `Global/Jellyfin gesetzt - keine Fansub-Ueberschreibung in diesem Schritt`. For `status === 'missing'` or `status === 'release'`, render a file input/dropzone that calls `uploadAdminReleaseThemeAssetForRelease({ releaseID: drawerRelease.release_id, themeID: selectedReleaseSegment.card.theme_id, file, authToken })`. On success, clear upload progress and reload cards for that release by invalidating `releaseSegmentCards[releaseID]` before calling `loadReleaseSegmentCards(drawerRelease)`. If `status === 'release'` and `media_id` exists, render `Asset entfernen` that calls `deleteAdminReleaseThemeAsset(drawerRelease.release_id, theme_id, media_id, authToken)` and reloads cards.</action>
  <acceptance_criteria>
    - `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` contains `uploadAdminReleaseThemeAssetForRelease`.
    - `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` contains `deleteAdminReleaseThemeAsset`.
    - `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` contains `Global/Jellyfin gesetzt`.
    - `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` contains `Asset entfernen`.
    - `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` contains `releaseSegmentCards`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Verify and deploy</name>
  <read_first>
    - .planning/phases/32-fansub-release-side-drawer-aus-phase-31-edit-drawer-fuer-release-theme-assets-mit-vorhandenen-db-tabellen-und-apis-ohne-neue-datenmodelle-db-ui-differenzen-vor-umsetzung-diskutieren/32-VALIDATION.md
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
    - backend/internal/handlers/admin_content_release_theme_assets.go
  </read_first>
  <action>Run the Phase 32 validation commands. If backend changed, build and deploy backend plus frontend; otherwise build and deploy frontend only. After deploy, verify `http://127.0.0.1:3002/admin/fansubs/17/edit` returns status code 200. Record the outcome in `32-UAT.md` with exact commands run and whether upload was manually tested or left for browser UAT.</action>
  <acceptance_criteria>
    - `cd backend && go test ./internal/handlers ./internal/repository -count=1` exits 0.
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `docker compose build team4sv30-backend team4sv30-frontend` exits 0 if backend changed.
    - `docker compose up -d team4sv30-backend team4sv30-frontend` exits 0 if backend changed.
    - `.planning/phases/32-fansub-release-side-drawer-aus-phase-31-edit-drawer-fuer-release-theme-assets-mit-vorhandenen-db-tabellen-und-apis-ohne-neue-datenmodelle-db-ui-differenzen-vor-umsetzung-diskutieren/32-UAT.md` contains `/admin/fansubs/17/edit`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
This plan passes when the row `Edit` button opens the drawer, Theme asset upload/delete uses release-scoped APIs, and the UI does not expose Anime editing or timeline timing edits.
</verification>

<success_criteria>
Admins can open a Side Drawer for a concrete Fansub release and manage the release Theme asset for missing/release-specific OP/ED/IN context using existing DB tables.
</success_criteria>
