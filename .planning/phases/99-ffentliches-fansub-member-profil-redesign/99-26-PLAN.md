---
phase: 99
plan: "26"
type: execute
wave: 19
depends_on:
  - "99-19"
  - "99-20"
  - "99-21"
  - "99-22"
  - "99-23"
  - "99-24"
  - "99-25"
files_modified:
  - "frontend/src/app/fansubs/[slug]/page.module.css"
autonomous: false
requirements_addressed:
  - "AO6-01"
  - "AO6-02"
  - "AO6-03"
  - "AO6-04"
  - "AO6-05"
  - "AO6-06"
  - "AO6-07"
  - "AO6-08"
  - "AO6-09"
  - "AO6-10"
  - "AO6-11"
  - "AO6-12"
requirements:
  - "Phase 99 Add-on 6 CONTEXT — Success Criteria (alle AO6-*)"
must_haves:
  truths:
    - "[AO6-01/AO6-06] Live auf :3000 zeigen Projekt-Karten den Anime-Banner (Fallback Cover); bei vielen Projekten Lazy-Karussell mit „weitere anzeigen", keine eigene Seite."
    - "[AO6-02] Die im Admin gesetzte Medien-Reihenfolge wird öffentlich übernommen."
    - "[AO6-03/AO6-05] Mehrere veröffentlichte Geschichts-Blöcke werden alle angezeigt (Titel + Clamp), keiner verschluckt."
    - "[AO6-07/AO6-08] Aktive Mitglieder mit öffentlichem Profil sind klickbar → /members/[slug], Liste zweispaltig, historische einklappbar mit Rolle + Zeitraum."
    - "[AO6-09] Meilensteine heben sich farblich ab; [AO6-10] Community-Links sind einheitliche Chips."
    - "[AO6-11/AO6-12] Medien: Vorschau auf 5 begrenzt + „+X weitere"/„Alle anzeigen", Titel + Typ-Tag + 2-Zeilen-Beschreibung; Lightbox öffnet Original mit Weiter/Zurück durch alle + vollem Text."
    - "[AO6-04] Keine überdimensionierten leeren Kacheln, keine Doppel-Header; alles Team4s-Tokens + @/components/ui; ≤390px und Desktop ohne Clipping/Overflow."
  artifacts:
    - path: "frontend/src/app/fansubs/[slug]/page.module.css"
      provides: "Optionale responsive Politur der Seite, falls Live-UAT Mobile-Overflow zeigt"
  key_links:
    - from: "frontend/src/app/fansubs/[slug]/page.tsx"
      to: "Browser-UAT :3000"
      via: "lokale /fansubs/c-subs Route mit echten C-Subs-Daten"
      pattern: "Hero.*Geschichte.*Projekte.*Team.*Medien"
---

<objective>
Finaler Live-UAT der öffentlichen Fansub-Profilseite `/fansubs/[slug]` für ALLE Add-on-6-Success-Criteria auf :3000 (Mobile ≤390px + Desktop) mit echten C-Subs-Daten. Skalierungsnahe Fälle (viele Projekte/Medien/Mitglieder) so gut wie mit Seed-Daten möglich prüfen, sonst ehrlich vermerken. Nur bei konkretem Befund gezielte Politur an page.module.css.

Purpose: Add-on 6 ist ein Design-/Skalierungs-Polish — die Abnahme ist eine visuelle/funktionale Live-Verifikation, nicht nur code-seitig.
Output: Verifikationsprotokoll + ggf. gezielte responsive Fixes + Nutzer-Freigabe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON6-CONTEXT.md
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-17-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Voller Code-Gate-Lauf + Live-Seite vorbereiten</name>
  <files>frontend/src/app/fansubs/[slug]/page.module.css</files>
  <read_first>
    - .planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON6-CONTEXT.md (Success Criteria)
    - frontend/src/app/fansubs/[slug]/page.module.css (bestehende responsive Grids/Breakpoints)
  </read_first>
  <action>
    Vollständige Gate-Läufe im Frontend ausführen: `npm run typecheck` und `npx vitest run src/app/fansubs src/components/fansubs` — beide müssen grün sein (sonst blockiert der UAT). Frontend-Container neu starten (`docker restart team4sv30-frontend`, Host-Port via `docker ps` bestätigen — Dev-Server :3000, HMR greift NICHT). Danach `/fansubs/c-subs` mit hartem Reload (Strg+F5) öffnen und für die Verifikation in Task 2 vorbereiten. In diesem Task NUR dann an page.module.css editieren, wenn ein konkreter Mobile-Overflow-/Clipping-Befund vorliegt — keine spekulativen Änderungen (bestehende auto-fit/minmax-Grids + 767px-Query nicht ohne Befund anfassen).
  </action>
  <acceptance_criteria>
    - `cd frontend && npm run typecheck` fehlerfrei.
    - `cd frontend && npx vitest run src/app/fansubs src/components/fansubs` grün.
    - `/fansubs/c-subs` liefert HTTP 200 (kein Fehler-/Leerzustand im HTML); Host-Port aus `docker ps` dokumentiert.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run typecheck && npx vitest run src/app/fansubs src/components/fansubs</automated>
  </verify>
  <done>Alle Code-Gates grün; Live-Seite auf :3000 erreichbar und für die visuelle Abnahme bereit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Add-on 6 Design-/Skalierungs-Polish der öffentlichen Fansub-Profilseite `/fansubs/[slug]`: Projekt-Banner + Lazy-Karussell (AO6-01/06), Medien-Reihenfolge nach Admin-sort_order (AO6-02), mehrere Geschichts-Blöcke mit Titel+Clamp (AO6-03/05), zweispaltiges klickbares Team mit historischem Zeitraum + Einklappen (AO6-07/08), farbliche Meilensteine (AO6-09), einheitliche Community-Chips (AO6-10), gekappte Medien-Vorschau mit Snippet (AO6-11) und Bild-Lightbox (AO6-12), im ruhigen Variante-B-Layout ohne Doppel-Header/Riesen-Leerkacheln (AO6-04).
  </what-built>
  <how-to-verify>
    Host-Port des Frontends via `docker ps` bestätigen (Dev :3000). `/fansubs/c-subs` mit hartem Reload öffnen und prüfen — jeweils Desktop UND Mobile ≤390px (DevTools-Gerätebreite):

    1. [AO6-01/06] Projekte: laufende Projekte als 16:9-Banner-Karten (Anime-Banner; wenn kein Banner → Cover; wenn beides fehlt → Platzhalter, kein Layout-Sprung). Abgeschlossene/archivierte im horizontalen Karussell: Pfeil-Buttons links/rechts funktionieren, Tastatur (Tab auf Pfeile + Enter, ←/→ Scroll), bei vielen Projekten „X weitere anzeigen"-Endkachel blendet restliche inline ein (keine neue Seite, kein Endlos-Scroll).
    2. [AO6-02] Medien-Reihenfolge entspricht der im Admin gesetzten Sortierung (falls prüfbar mit Seed-Daten; sonst ehrlich vermerken).
    3. [AO6-03/05] Mehrere veröffentlichte Geschichts-Blöcke: alle sichtbar, jeder mit eigenem Titel + eigenem „Mehr/Weniger anzeigen"-Clamp; keiner verschluckt.
    4. [AO6-07/08] Team zweispaltig (Desktop) / einspaltig (≤390px); aktive Mitglieder mit öffentlichem Profil sind klar als Link erkennbar (Akzent + Chevron, Hover) und führen zu /members/[slug]; nicht-öffentliche bleiben neutral; historische Mitglieder zeigen Rolle + Zeitraum („JJJJ–JJJJ"/„seit JJJJ"), lange historische Liste einklappbar.
    5. [AO6-09] Meilensteine farblich abgesetzt (Akzent-Kante/Block). [AO6-10] Community-Links als einheitliche, klickbare Chips (Icon + Name/Label).
    6. [AO6-11] Medien: höchstens 5 Vorschaubilder, „+X weitere"-Überlaufkachel + „Alle N anzeigen" erweitert inline; je Medium Titel + deutscher Typ-Tag + 2-Zeilen-Beschreibungs-Snippet; lazy/Skeleton, kein Layout-Sprung.
    7. [AO6-12] Klick auf ein Thumbnail öffnet die Lightbox: Originalbild groß, voller Titel + Beschreibung, Zähler „n / N"; Weiter/Zurück und ←/→ blättern durch ALLE Medien; Esc schließt; Fokus kehrt zum Thumbnail zurück.
    8. [AO6-04] Gesamteindruck: keine überdimensionierten leeren Kacheln, keine Doppel-Header (ein klarer Header je Sektion), konsistente Kachelgrößen/Abstände; ≤390px und Desktop ohne horizontales Scrollen/Clipping/Überlappung.

    Skalierungsnahe Fälle (20 Mitglieder / 30 Projekte / 40 Medien) so gut wie mit vorhandenen Seed-Daten möglich prüfen; wo Seed-Daten nicht ausreichen, im SUMMARY ehrlich als „nicht live prüfbar mit aktuellem Datensatz" vermerken.
  </how-to-verify>
  <resume-signal>Antworte „freigegeben" oder beschreibe konkrete Befunde (Sektion + Gerät + Problem) für gezielte Nacharbeit.</resume-signal>
</task>

</tasks>

<verification>
- Code-Gates grün (typecheck + vitest über src/app/fansubs + src/components/fansubs).
- Live-Verifikation aller acht AO6-Kriterien-Gruppen auf Desktop und ≤390px durch den Nutzer bestätigt; gemeldete Befunde behoben oder als datenbedingt-nicht-prüfbar dokumentiert.
</verification>

<success_criteria>
- Alle AO6-Success-Criteria live bestätigt (oder Rest ehrlich dokumentiert).
- Kein Mobile-Overflow/Clipping; keine Doppel-Header/Riesen-Leerkacheln.
- Nutzer-Freigabe erteilt.
</success_criteria>

<output>
Create `.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-26-SUMMARY.md` when done
</output>
