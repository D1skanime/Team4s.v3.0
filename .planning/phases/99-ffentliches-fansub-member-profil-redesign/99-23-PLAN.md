---
phase: 99
plan: "23"
type: execute
wave: 16
depends_on:
  - "99-19"
files_modified:
  - "frontend/src/components/fansubs/FansubProjectsSection.tsx"
  - "frontend/src/components/fansubs/FansubProjectBannerCard.tsx"
  - "frontend/src/components/fansubs/FansubProjectsCarousel.tsx"
  - "frontend/src/components/fansubs/FansubProjectsSection.module.css"
  - "frontend/src/components/fansubs/__tests__/FansubProjectsSection.test.tsx"
  - "frontend/src/components/fansubs/__tests__/FansubProjectsCarousel.test.tsx"
autonomous: true
requirements_addressed:
  - "AO6-06"
  - "AO6-01"
  - "AO6-04"
requirements:
  - "Phase 99 Add-on 6 CONTEXT AO6-06 (Projekte: Banner-Karten + Lazy-Karussell)"
  - "Phase 99 Add-on 6 CONTEXT AO6-01 (Anime-Banner in Projekt-Karten, Fallback cover_image)"
  - "Phase 99 Add-on 6 CONTEXT AO6-04 (ein klarer Header pro Sektion)"
must_haves:
  truths:
    - "[AO6-01] Jede Projekt-Karte zeigt den Anime-Banner (project.banner_url), mit Fallback auf cover_image und Platzhalter wenn beides fehlt; Bilder laden lazy mit sizes."
    - "[AO6-06] Laufende Projekte erscheinen als volle 16:9-Banner-Karten (Titel-Overlay + Status-Pill); abgeschlossene/archivierte Projekte als horizontales Karussell mit scroll-snap, Pfeil-Buttons und Tastaturbedienung/A11y."
    - "[AO6-06] Das Karussell zeigt initial eine begrenzte Zahl Karten mit Skeleton-Platzhaltern beim Laden und eine 'X weitere anzeigen'-Endkachel, die restliche Karten inline aus dem bereits geladenen Datensatz einblendet (kein Auto-Infinite-Scroll, keine eigene Seite)."
    - "[AO6-04] Die Projekte-Sektion hat genau einen klaren Header, keine 'Laufende Projekte / Laufend'-Doppelung."
  artifacts:
    - path: "frontend/src/components/fansubs/FansubProjectBannerCard.tsx"
      provides: "16:9-Banner-Projektkarte mit Titel-Overlay + Status-Pill, lazy/sizes/Fallback"
      min_lines: 25
    - path: "frontend/src/components/fansubs/FansubProjectsCarousel.tsx"
      provides: "Client-Karussell: scroll-snap, Pfeile, Tastatur/A11y, Skeleton, 'X weitere anzeigen'"
      contains: "scroll"
  key_links:
    - from: "frontend/src/components/fansubs/FansubProjectBannerCard.tsx"
      to: "project.banner_url ?? project.cover_image"
      via: "resolveApiUrl auf Banner mit Cover-Fallback"
      pattern: "banner_url"
    - from: "frontend/src/components/fansubs/FansubProjectsSection.tsx"
      to: "FansubProjectsCarousel"
      via: "abgeschlossene/archivierte Buckets ins Karussell"
      pattern: "FansubProjectsCarousel"
---

<objective>
Projekte-Sektion neu gestalten (AO6-06/01): laufende Projekte als volle 16:9-Banner-Karten mit Titel-Overlay + Status-Pill, abgeschlossene/archivierte als horizontales Lazy-Karussell (scroll-snap, Pfeile, Tastatur/A11y, Skeleton, „X weitere anzeigen"-Endkachel). Anime-Banner (project.banner_url) mit Cover-Fallback. Ein klarer Header (AO6-04).

Purpose: Bei 30 Projekten muss die Sektion visuell führen (Banner) und dicht/skalierbar bleiben (Karussell mit manueller Enthüllung), ohne eigene Unterseite.
Output: Server-Sektion + Banner-Karte + Client-Karussell + eigenes CSS-Modul + Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON6-CONTEXT.md

<interfaces>
<!-- Aktueller Stand (read-only verifiziert). banner_url stammt aus Plan 99-19. -->

frontend/src/types/fansub.ts — PublicFansubProject { id; title; type; status; year?; cover_image?; max_episodes?; banner_url?: string|null (99-19) }.
frontend/src/components/fansubs/FansubProjectsSection.tsx (aktuell): props { projects, groupId }. Bucketing resolveProjectBucket: status==='ongoing'→ongoing, status==='done'→completed, sonst archived. Rendert SectionHeader title='Laufende Projekte' + je Bucket h3 (Laufend/Abgeschlossen/Archiviert) + cardGrid mit Link `/anime/${item.id}/group/${groupId}` (Card variant='interactive', Titel + Jahr). projectBucketOrder/projectBucketLabel vorhanden.
Bild-URL-Auflösung: resolveApiUrl aus @/lib/api (siehe FansubHeroSection/FansubGroupMediaBlock). next/image mit fill + sizes + loading='lazy' + unoptimized (Muster FansubGroupMediaBlock).
@/components/ui: Badge (Status-Pill), Button (Pfeile/Endkachel, variant ghost/subtle), Card, SectionHeader. Icons lucide-react (ChevronLeft/ChevronRight).
Status-Labels deutsch: Laufend/Abgeschlossen/Archiviert (projectBucketLabel bereits vorhanden).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FansubProjectBannerCard — 16:9-Banner + Titel-Overlay + Status-Pill + Fallback (AO6-01/06)</name>
  <files>frontend/src/components/fansubs/FansubProjectBannerCard.tsx, frontend/src/components/fansubs/FansubProjectsSection.module.css, frontend/src/components/fansubs/__tests__/FansubProjectsSection.test.tsx</files>
  <read_first>
    - frontend/src/components/fansubs/FansubProjectsSection.tsx (gesamt)
    - frontend/src/components/fansubs/FansubGroupMediaBlock.tsx (next/image fill+sizes+lazy-Muster)
    - frontend/src/types/fansub.ts:152-160
  </read_first>
  <behavior>
    - Bei gesetztem banner_url rendert die Karte ein 16:9-Bild aus resolveApiUrl(banner_url); ohne banner_url Fallback auf cover_image; ohne beides ein Skeleton/Platzhalter derselben 16:9-Fläche (kein Layout-Sprung).
    - Bild-Tag trägt loading='lazy' und ein sizes-Attribut.
    - Die Karte zeigt Titel als Overlay-Leiste und eine Status-Pill (deutsches Label) und verlinkt zu /anime/[id]/group/[groupId].
  </behavior>
  <action>
    FansubProjectBannerCard.tsx NEU (präsentational, kann Server-Komponente sein): props { project: PublicFansubProject; groupId: number; statusLabel: string }. Rendert einen Link zu `/anime/${project.id}/group/${groupId}` mit einem 16:9-Frame (position relative, aspect-ratio 16/9). Bildquelle = resolveApiUrl(project.banner_url || project.cover_image || ''); wenn vorhanden next/image fill + sizes (z.B. '(max-width: 640px) 100vw, 480px') + loading='lazy' + unoptimized, sonst Skeleton-Div derselben Fläche. Titel in einer Overlay-Leiste unten (Gradient/Token-Hintergrund) und eine @/components/ui Badge als Status-Pill (statusLabel). alt-Text = project.title.

    FansubProjectsSection.module.css NEU: Klassen bannerCard, bannerFrame (aspect-ratio 16/9, overflow hidden, radius), bannerImage (object-fit cover), bannerSkeleton, bannerOverlay (Titel-Leiste, Token-Hintergrund), bannerTitle, statusPill-Positionierung. Nur Team4s-Tokens.

    FansubProjectsSection.test.tsx: (RED zuerst) Test, dass eine Banner-Karte für ein laufendes Projekt mit banner_url das lazy-Bild rendert und die Status-Pill zeigt; Fallback-Test (kein banner_url, cover_image gesetzt → Bild aus cover_image); Platzhalter-Test (weder banner_url noch cover_image → Skeleton).
  </action>
  <acceptance_criteria>
    - `grep -n "banner_url" frontend/src/components/fansubs/FansubProjectBannerCard.tsx` und `grep -n "cover_image" frontend/src/components/fansubs/FansubProjectBannerCard.tsx` vorhanden (Banner mit Cover-Fallback).
    - `grep -n "loading=\"lazy\"" frontend/src/components/fansubs/FansubProjectBannerCard.tsx` und `grep -n "sizes" frontend/src/components/fansubs/FansubProjectBannerCard.tsx` vorhanden.
    - `grep -n "aspect-ratio" frontend/src/components/fansubs/FansubProjectsSection.module.css` vorhanden.
    - `cd frontend && npx vitest run src/components/fansubs/__tests__/FansubProjectsSection.test.tsx` grün.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/fansubs/__tests__/FansubProjectsSection.test.tsx</automated>
  </verify>
  <done>Banner-Projektkarte mit 16:9, Titel-Overlay, Status-Pill, Banner→Cover→Skeleton-Fallback, lazy/sizes; Tests grün.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FansubProjectsCarousel (client) + Sektion verdrahten, Header entdoppeln (AO6-06/04)</name>
  <files>frontend/src/components/fansubs/FansubProjectsCarousel.tsx, frontend/src/components/fansubs/FansubProjectsSection.tsx, frontend/src/components/fansubs/FansubProjectsSection.module.css, frontend/src/components/fansubs/__tests__/FansubProjectsCarousel.test.tsx, frontend/src/components/fansubs/__tests__/FansubProjectsSection.test.tsx</files>
  <read_first>
    - frontend/src/components/fansubs/FansubProjectsSection.tsx (gesamt, Bucketing)
    - frontend/src/components/fansubs/FansubProjectBannerCard.tsx (aus Task 1)
  </read_first>
  <behavior>
    - Das Karussell rendert eine horizontale, scroll-snap-fähige Bahn mit Pfeil-Buttons (links/rechts) und ist per Tastatur bedienbar (Buttons fokussierbar, aria-label 'Vorherige Projekte'/'Weitere Projekte').
    - Initial werden höchstens N Karten gezeigt; gibt es mehr, erscheint eine 'X weitere anzeigen'-Endkachel, die per Klick die restlichen bereits geladenen Karten inline einblendet (kein Netzwerk-Nachladen, kein Auto-Scroll).
    - Die Bahn hat eine A11y-Rolle/aria-label; Skeleton-Platzhalter bis Karten sichtbar/geladen sind.
    - FansubProjectsSection: laufende Projekte → Banner-Karten; abgeschlossene+archivierte → Karussell; genau ein SectionHeader.
  </behavior>
  <action>
    FansubProjectsCarousel.tsx NEU ('use client'): props { items: PublicFansubProject[]; groupId: number }. State visibleCount (initial CAROUSEL_INITIAL, z.B. 8). Rendert eine scroll-Bahn (className carouselTrack, CSS scroll-snap-type: x mandatory, overflow-x auto) mit FansubProjectBannerCard je sichtbarem Item (snap-align start). Pfeil-Buttons als @/components/ui Button (variant ghost) mit ChevronLeft/ChevronRight + aria-label; Klick scrollt die Bahn um eine Kartenbreite (ref.scrollBy). Wenn items.length > visibleCount, eine Endkachel als @/components/ui Button 'Noch X anzeigen' (dynamische Restzahl) rendern, die visibleCount auf items.length setzt (inline-Enthüllung). Skeleton-Platzhalter (feste 16:9-Fläche) via IntersectionObserver/lazy — mindestens ein Skeleton-Fallback-Zustand. Bahn trägt role='region' aria-label='Projekt-Karussell'. Kein natives button (nur @/components/ui Button); kein Auto-Infinite-Scroll. Datei ≤450 Zeilen.

    FansubProjectsSection.tsx: umbauen — SectionHeader auf einen klaren Titel 'Projekte' (statt 'Laufende Projekte', vermeidet 'Laufende Projekte / Laufend'-Doppel). ongoing-Bucket über FansubProjectBannerCard als volle Banner-Karten (Grid/Stack) rendern; completed+archived zusammen (oder je Gruppe) an FansubProjectsCarousel übergeben. statusLabel je Karte aus projectBucketLabel ableiten. Leere Buckets auslassen. groupId weiterreichen.

    FansubProjectsSection.module.css: Klassen carouselTrack, carouselArrow, carouselSkeleton, moreTile ergänzen (scroll-snap, gap, Pfeil-Positionierung), nur Tokens.

    Tests: FansubProjectsCarousel.test.tsx (RED zuerst) — mehr Items als CAROUSEL_INITIAL zeigt 'weitere anzeigen'-Endkachel; Klick blendet restliche ein; Pfeil-Buttons mit aria-label vorhanden. FansubProjectsSection.test.tsx erweitern: ongoing→Banner-Karten, completed/archived→Karussell, genau ein SectionHeader (kein 'Laufende Projekte'-String mehr als Section-Titel + 'Laufend'-Subtitel-Doppel).
  </action>
  <acceptance_criteria>
    - `grep -n "use client" frontend/src/components/fansubs/FansubProjectsCarousel.tsx` vorhanden; `grep -n "scroll-snap-type" frontend/src/components/fansubs/FansubProjectsSection.module.css` vorhanden.
    - `grep -n "aria-label" frontend/src/components/fansubs/FansubProjectsCarousel.tsx` vorhanden; `grep -Ev '^\s*//' frontend/src/components/fansubs/FansubProjectsCarousel.tsx | grep -c "<button"` = 0.
    - `grep -n "FansubProjectsCarousel" frontend/src/components/fansubs/FansubProjectsSection.tsx` und `grep -n "FansubProjectBannerCard" frontend/src/components/fansubs/FansubProjectsSection.tsx` vorhanden.
    - Alle geänderten Dateien ≤450 Zeilen (`awk 'END{exit(NR>450)}' <datei>`).
    - `cd frontend && npx vitest run src/components/fansubs/__tests__/FansubProjectsCarousel.test.tsx src/components/fansubs/__tests__/FansubProjectsSection.test.tsx && npm run typecheck` grün.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/fansubs && npm run typecheck</automated>
  </verify>
  <done>Laufende Projekte als Banner-Karten, restliche als A11y-Karussell mit scroll-snap/Pfeilen/Skeleton/„weitere anzeigen"; ein Header; Tests grün.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run typecheck` und `npx vitest run src/components/fansubs` grün.
- Live: `docker restart team4sv30-frontend`, harter Reload `/fansubs/c-subs` (:3000) — laufende Projekte als Banner-Karten (Anime-Banner bzw. Cover-Fallback), abgeschlossene/archivierte im horizontalen Karussell mit Pfeilen + Tastatur; bei vielen Projekten „weitere anzeigen"-Endkachel; keine eigene Unterseite. Skalierungsnähe mit vorhandenen Seed-Daten prüfen, sonst im SUMMARY ehrlich vermerken.
</verification>

<success_criteria>
- Projekt-Karten zeigen Anime-Banner (Fallback Cover), lazy geladen.
- Laufend = Banner-Karten; Abgeschlossen/Archiviert = A11y-Karussell mit scroll-snap, Pfeilen, Skeleton, manueller „weitere anzeigen"-Enthüllung.
- Ein klarer Header, keine eigene Seite, kein Auto-Infinite-Scroll.
</success_criteria>

<output>
Create `.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-23-SUMMARY.md` when done
</output>
