---
phase: 99
plan: "20"
type: execute
wave: 16
depends_on:
  - "99-19"
files_modified:
  - "frontend/src/types/fansub.ts"
  - "frontend/src/app/fansubs/[slug]/page.tsx"
  - "frontend/src/app/fansubs/__tests__/pageHelpers.test.tsx"
  - "frontend/src/app/fansubs/__tests__/page.test.tsx"
  - "frontend/src/components/fansubs/FansubStorySection.tsx"
  - "frontend/src/components/fansubs/FansubStoryBlock.tsx"
  - "frontend/src/components/fansubs/FansubStorySection.module.css"
  - "frontend/src/components/fansubs/FansubStoryBlock.module.css"
  - "frontend/src/components/fansubs/__tests__/FansubStorySection.test.tsx"
  - "frontend/src/components/fansubs/__tests__/FansubStoryBlock.test.tsx"
autonomous: true
requirements_addressed:
  - "AO6-03"
  - "AO6-05"
  - "AO6-04"
requirements:
  - "Phase 99 Add-on 6 CONTEXT AO6-03 (Frontend-Migration story→stories[])"
  - "Phase 99 Add-on 6 CONTEXT AO6-05 (mehrere Geschichts-Blöcke gerendert, Titel+Clamp)"
  - "Phase 99 Add-on 6 CONTEXT AO6-04 (ein klarer Header pro Sektion)"
must_haves:
  truths:
    - "[AO6-03] Der TS-Typ PublicFansubProfile trägt stories: PublicFansubStory[]; kein Frontend-Konsument (page.tsx, Komponenten, Tests) referenziert mehr profile.story (Singular). Typecheck + Vitest grün."
    - "[AO6-05] Alle veröffentlichten Geschichts-Blöcke aus stories[] werden gestapelt gerendert (Reihenfolge nach sort_order), jeder mit eigenem Titel und eigenem Clamp (Mehr/Weniger anzeigen); kein Block wird verschluckt."
    - "[AO6-04] Die Geschichte-Sektion hat genau EINEN Sektions-Header (SectionHeader 'Geschichte'), ohne zusätzliches Eyebrow/Doppel-Titel."
  artifacts:
    - path: "frontend/src/components/fansubs/FansubStoryBlock.tsx"
      provides: "Client-Block-Komponente: ein Geschichts-Block mit Titel + Clamp-Umschalter"
      min_lines: 30
    - path: "frontend/src/components/fansubs/FansubStorySection.tsx"
      provides: "Server-Wrapper: ein SectionHeader + Map über stories[] auf FansubStoryBlock"
      contains: "stories"
    - path: "frontend/src/app/fansubs/[slug]/page.tsx"
      provides: "Migrierte Konsumenten (hasStoriesContent, buildEmptyAreaLabels, Rendering) auf stories[]"
      contains: "profile.stories"
  key_links:
    - from: "frontend/src/app/fansubs/[slug]/page.tsx"
      to: "FansubStorySection stories={profile.stories}"
      via: "Prop-Übergabe des Story-Arrays"
      pattern: "stories=\\{profile\\.stories\\}"
    - from: "frontend/src/components/fansubs/FansubStorySection.tsx"
      to: "FansubStoryBlock"
      via: "map über stories[]"
      pattern: "FansubStoryBlock"
---

<objective>
Frontend-Migration der breaking DTO-Änderung `story` → `stories[]` (AO6-03) und Rendering mehrerer Geschichts-Blöcke (AO6-05): TS-Typ + alle Konsumenten in `page.tsx` + Tests umstellen, und die Geschichte-Sektion so umbauen, dass jeder veröffentlichte Block mit eigenem Titel und eigenem Clamp erscheint. Ein klarer Sektions-Header (AO6-04).

Purpose: Nach Plan 99-19 liefert das DTO `stories[]`; ohne diese Migration zeigt kein Konsument mehr Inhalte an. Mehrere Geschichts-Blöcke dürfen nicht mehr verschluckt werden.
Output: Migrierter Typ + Seite + Sektion + neue per-Block-Client-Komponente, typecheck- und vitest-grün.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON6-CONTEXT.md
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-17-SUMMARY.md

<interfaces>
<!-- Aktueller Stand (read-only verifiziert). Executor migriert diese Stellen. -->

frontend/src/types/fansub.ts:183-190 — PublicFansubProfile { group; story: PublicFansubStory | null; projects; history; media; community_links } → story wird zu `stories: PublicFansubStory[]`.
frontend/src/types/fansub.ts:144-150 — PublicFansubStory { id; title; body_html; body_text; updated_at? } (unverändert).

page.tsx Konsumenten:
- hasStoryContent(story) :35-39 — prüft Boolean(story && (title|body_html|body_text)).
- buildEmptyAreaLabels :41-64 — Zeile 50 `if (!hasStoryContent(profile.story)) labels.push('Geschichte')`.
- :122 `const storyAvailable = hasStoryContent(profile.story)`; :134-138 rendert `<FansubStorySection group story={profile.story} />` nur wenn storyAvailable.

FansubStorySection.tsx (aktuell 'use client', Clamp-Muster mit ResizeObserver): nimmt ein einzelnes `story` und rendert SectionHeader 'Geschichte' + einen Block mit Clamp. Clamp-Klassen in FansubStorySection.module.css (storyContentClamped/storyContentExpanded/toggle). RichTextRenderer aus @/components/editor/RichTextRenderer, Button/SectionHeader aus @/components/ui, sharedStyles aus FansubPublicSections.module.css (sectionTitle/bodyText).

Tests, die migriert werden müssen:
- pageHelpers.test.tsx:21-28 (emptyProfile.story:null → stories:[]), :48-59 (hasStoryContent-Fälle), :120 (story-Fixture).
- page.test.tsx:27-43 (Reihenfolgen-Assertion referenziert <FansubStorySection>).
- FansubStorySection.test.tsx:21-38 (story-Fixture + story={null}).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TS-Typ + page.tsx-Konsumenten auf stories[] migrieren (AO6-03)</name>
  <files>frontend/src/types/fansub.ts, frontend/src/app/fansubs/[slug]/page.tsx, frontend/src/app/fansubs/__tests__/pageHelpers.test.tsx</files>
  <read_first>
    - frontend/src/types/fansub.ts:144-190
    - frontend/src/app/fansubs/[slug]/page.tsx:31-64, 104-140
    - frontend/src/app/fansubs/__tests__/pageHelpers.test.tsx (gesamt)
  </read_first>
  <behavior>
    - hasStoriesContent([]) === false; hasStoriesContent([{title:'',body_html:'',body_text:''}]) === false; hasStoriesContent([{title:'X',body_html:'',body_text:''}]) === true.
    - buildEmptyAreaLabels pusht 'Geschichte' genau dann, wenn kein Block Inhalt hat (stories leer oder alle Blöcke leer).
    - Bestehende buildEmptyAreaLabels-Fälle (Projekte/Team/Community 'Mehr') bleiben unverändert grün.
  </behavior>
  <action>
    In fansub.ts das Feld `story: PublicFansubStory | null;` in PublicFansubProfile ersetzen durch `stories: PublicFansubStory[];`.

    In page.tsx: `hasStoryContent(story)` umbenennen/umbauen zu `hasStoriesContent(stories: ...['data']['stories'])`, das `stories.some((s) => Boolean(s.title?.trim() || s.body_html?.trim() || s.body_text?.trim()))` zurückgibt. Zeile 50 auf `if (!hasStoriesContent(profile.stories)) labels.push('Geschichte')` umstellen. Zeile 122 `storyAvailable = hasStoriesContent(profile.stories)`. Rendering (Zeile 134-138): `<FansubStorySection group={group} stories={profile.stories} />` statt `story={profile.story}`. Export von hasStoriesContent beibehalten (Tests importieren es).

    In pageHelpers.test.tsx: alle Fixtures von `story: null` auf `stories: []` bzw. `stories: [{...}]` umstellen; die hasStoryContent-Testfälle auf hasStoriesContent mit Array-Eingaben umschreiben (leeres Array, Array mit leerem Block, Array mit Titel-Block, Array mit body_html-Block). Import in Zeile 3 auf hasStoriesContent anpassen.
  </action>
  <acceptance_criteria>
    - `grep -c "profile.story\b" frontend/src/app/fansubs/[slug]/page.tsx` = 0 (kein Singular-Zugriff mehr); `grep -n "profile.stories" frontend/src/app/fansubs/[slug]/page.tsx` vorhanden.
    - `grep -n "stories: PublicFansubStory\[\]" frontend/src/types/fansub.ts` vorhanden.
    - `cd frontend && npx vitest run src/app/fansubs/__tests__/pageHelpers.test.tsx` grün.
    - `cd frontend && npm run typecheck` fehlerfrei.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/app/fansubs/__tests__/pageHelpers.test.tsx && npm run typecheck</automated>
  </verify>
  <done>Typ und alle page.tsx-Konsumenten nutzen stories[]; Helper-Tests grün; kein profile.story-Zugriff mehr.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FansubStorySection (Wrapper) + FansubStoryBlock (per-Block-Clamp) rendern alle Blöcke (AO6-05/04)</name>
  <files>frontend/src/components/fansubs/FansubStorySection.tsx, frontend/src/components/fansubs/FansubStoryBlock.tsx, frontend/src/components/fansubs/FansubStorySection.module.css, frontend/src/components/fansubs/FansubStoryBlock.module.css, frontend/src/components/fansubs/__tests__/FansubStorySection.test.tsx, frontend/src/components/fansubs/__tests__/FansubStoryBlock.test.tsx, frontend/src/app/fansubs/__tests__/page.test.tsx</files>
  <read_first>
    - frontend/src/components/fansubs/FansubStorySection.tsx (gesamt, aktuelles Clamp-Muster)
    - frontend/src/components/fansubs/FansubStorySection.module.css (Clamp-Klassen)
    - frontend/src/components/fansubs/__tests__/FansubStorySection.test.tsx
    - frontend/src/app/fansubs/__tests__/page.test.tsx:27-48
  </read_first>
  <behavior>
    - FansubStorySection mit stories=[A,B] rendert genau einen SectionHeader 'Geschichte' und je einen Block für A und B in Array-Reihenfolge (jeder Block-Titel im Markup vorhanden).
    - FansubStorySection mit stories=[] rendert null.
    - Blöcke ohne jeglichen Inhalt (kein Titel/HTML/Text) werden übersprungen.
    - FansubStoryBlock rendert seinen Titel + Inhalt und (im Client) einen Umschalter nur bei Overflow.
  </behavior>
  <action>
    FansubStorySection.tsx zu einer Server-Wrapper-Komponente machen (kein 'use client'), Prop-Signatur `{ group: FansubGroup; stories: PublicFansubStory[] }`. Rendert `<section id="geschichte">` mit EINEM `<SectionHeader title="Geschichte" />` (kein Eyebrow), dann map über stories.filter(Block hat Inhalt) auf `<FansubStoryBlock key={s.id} story={s} />` mit einem Stack-Container (gap via FansubStorySection.module.css). Bei leerer/gefilterter Liste null zurückgeben.

    FansubStoryBlock.tsx NEU ('use client'): übernimmt das bestehende Clamp-Muster aus der alten FansubStorySection (useRef/useState isExpanded/isOverflowing, ResizeObserver + window.resize measureOverflow, RichTextRenderer für body_html sonst body_text-Absatz, @/components/ui Button variant='subtle' size='sm' mit 'Mehr anzeigen'/'Weniger anzeigen'). Zeigt den Block-Titel als `<h3 className={sharedStyles.sectionTitle}>` (aus FansubPublicSections.module.css, nur lesen). Clamp-Klassen nach FansubStoryBlock.module.css verschieben (storyContentClamped/storyContentExpanded/toggle) oder aus FansubStorySection.module.css re-exportieren — Ownership: neue Klassen in FansubStoryBlock.module.css.

    page.test.tsx: die Reihenfolgen-Assertion beibehalten (referenziert `<FansubStorySection`), sicherstellen dass der Test weiterhin auf `<FansubStorySection` prüft; einen Fall ergänzen, der belegt, dass die Seite `stories={profile.stories}` übergibt (regex `stories=\{profile\.stories\}`).

    FansubStorySection.test.tsx: Fixtures auf `stories`-Prop umstellen — ein Test mit zwei Blöcken (beide Titel im Markup, Reihenfolge), ein Test mit `stories={[]}` → kein Markup/null.
    FansubStoryBlock.test.tsx NEU: Block mit Titel+body_html rendert Titel und Inhalt; leerer Block rendert null.

    Umlaute korrekt (Geschichte, Mehr anzeigen, Weniger anzeigen). Nur @/components/ui-Primitive (Button, SectionHeader) — kein natives button. Beide Dateien ≤450 Zeilen.
  </action>
  <acceptance_criteria>
    - `grep -n "FansubStoryBlock" frontend/src/components/fansubs/FansubStorySection.tsx` vorhanden; `grep -c "use client" frontend/src/components/fansubs/FansubStorySection.tsx` = 0 (Wrapper ist Server-Komponente).
    - `grep -n "use client" frontend/src/components/fansubs/FansubStoryBlock.tsx` vorhanden; `grep -Ev '^\s*//' frontend/src/components/fansubs/FansubStoryBlock.tsx | grep -c "<button" ` = 0 (kein natives button).
    - `grep -n "SectionHeader title=\"Geschichte\"" frontend/src/components/fansubs/FansubStorySection.tsx` vorhanden; kein `eyebrow` im Geschichte-Header.
    - `cd frontend && npx vitest run src/components/fansubs/__tests__/FansubStorySection.test.tsx src/components/fansubs/__tests__/FansubStoryBlock.test.tsx src/app/fansubs/__tests__/page.test.tsx` grün.
    - `cd frontend && npm run typecheck` grün.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/fansubs src/app/fansubs && npm run typecheck</automated>
  </verify>
  <done>Mehrere Geschichts-Blöcke werden gestapelt mit je eigenem Titel + Clamp gerendert; ein Sektions-Header; alle betroffenen Tests grün.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run typecheck` und `npx vitest run src/app/fansubs src/components/fansubs` grün.
- Live: `docker restart team4sv30-frontend`, harter Reload `/fansubs/c-subs` auf :3000 (Host-Port via `docker ps`) — mehrere Geschichts-Blöcke sichtbar (falls Seed-Daten mehrere public/published Notes haben), jeder mit Titel + eigenem Mehr/Weniger-Umschalter; genau ein 'Geschichte'-Header.
</verification>

<success_criteria>
- Kein Konsument referenziert profile.story (Singular); TS/Vitest grün.
- Alle veröffentlichten Geschichts-Blöcke werden mit Titel + Clamp angezeigt, keiner verschluckt.
- Ein klarer Sektions-Header ohne Doppelung.
</success_criteria>

<output>
Create `.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-20-SUMMARY.md` when done
</output>
