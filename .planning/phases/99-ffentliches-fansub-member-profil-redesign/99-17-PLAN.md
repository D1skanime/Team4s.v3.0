---
phase: 99
plan: "17"
type: execute
wave: 13
depends_on:
  - "99-16"
files_modified:
  - "frontend/src/app/fansubs/[slug]/page.tsx"
  - "frontend/src/app/fansubs/[slug]/page.module.css"
  - "frontend/src/components/fansubs/FansubStorySection.tsx"
  - "frontend/src/components/fansubs/FansubStorySection.module.css"
  - "frontend/src/components/fansubs/FansubPublicSections.module.css"
  - "frontend/src/app/fansubs/__tests__/page.test.tsx"
  - "frontend/src/app/fansubs/__tests__/pageHelpers.test.tsx"
autonomous: true
requirements_addressed:
  - "AO5-03"
  - "AO5-04"
  - "AO5-07"
requirements:
  - "Phase 99 Add-on 5 Seiten-Komposition (Reihenfolge + Geschichte-Clamp + visuelle Politur)"
must_haves:
  truths:
    - "AO5-03: /fansubs/[slug] rendert die Sektionen in der Reihenfolge Hero → Geschichte → Laufende Projekte → Team → Erfolge → Medien; die Community-Links-Sektion und die Medien-Sektion aus 99-16 sind eingebunden."
    - "AO5-03: buildEmptyAreaLabels führt 'Medien' nur bei leerer Medienliste und 'Mehr' nur, wenn weder website_url noch community_links vorhanden sind."
    - "AO5-04: Die Gruppen-Geschichte ist über einen 'Mehr anzeigen'/'Weniger anzeigen'-Umschalter einklappbar/kürzbar (Clamp), analog zum Member-Profil-Story-Clamp."
    - "AO5-07: Kachelgrößen und Abstände sind über Projekte/Team/Erfolge/Medien konsistent; keine überdimensionierten Texte; nur Team4s-Tokens, keine neuen Ad-hoc-Farbwerte."
  artifacts:
    - path: "frontend/src/app/fansubs/[slug]/page.tsx"
      provides: "Neue Sektions-Reihenfolge + eingebundene Community-/Medien-Sektionen + angepasstes buildEmptyAreaLabels"
      contains: "FansubMediaSection"
    - path: "frontend/src/components/fansubs/FansubStorySection.tsx"
      provides: "Client-Clamp mit Umschalter"
      contains: "use client"
  key_links:
    - from: "page.tsx"
      to: "FansubCommunityLinksSection + FansubMediaSection"
      via: "Render von community_links und media in neuer Reihenfolge"
      pattern: "FansubMediaSection"
    - from: "buildEmptyAreaLabels"
      to: "community_links + website_url"
      via: "'Mehr' nur ohne beide"
      pattern: "community_links"
---

<objective>
Setze die öffentliche Fansub-Profilseite final zusammen: neue Sektions-Reihenfolge inkl. Einbindung der in 99-16 gebauten Community-Links- und Medien-Sektion (AO5-03), einklappbare Gruppen-Geschichte (AO5-04) und konsistente visuelle Politur aller befüllten Sektionen (AO5-07).

Purpose: Die Seite ist unvollständig (zwei Datenquellen unsichtbar), in falscher Reihenfolge und optisch grob (riesige Kacheln, nicht kürzbare Texte). Dieser Plan behebt Komposition, Lesbarkeit und Konsistenz.

Output: Neu komponierte page.tsx, Clamp-fähige FansubStorySection, polierte Sektions-/Seiten-CSS, aktualisierte Source-Assertion-Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON5-CONTEXT.md
@CLAUDE.md

<interfaces>
Nach 99-16 verfügbar:
- FansubCommunityLinksSection (Props: links: FansubGroupLink[]) — rendert null bei Leerliste.
- FansubMediaSection (Props: media: PublicFansubMediaItem[]) — aufgewertete Medien-Sektion.
- profile.community_links (FansubGroupLink[]) und profile.media (mit title/description/category) aus dem DTO (99-15).

Bestehende Seite (frontend/src/app/fansubs/[slug]/page.tsx):
- Reihenfolge aktuell (:125-161): Hero → Projekte → Team → Geschichte → Erfolge → Sammelhinweis (Medien NICHT gerendert).
- buildEmptyAreaLabels (:39-62): u.a. 'Medien' bei profile.media.length===0, 'Mehr' bei !profile.group.website_url.
- Layout-Klassen (page.module.css): readingColumn (schmale Lesespalte), gridSection (breite Grid-Zone), sectionSpacing (margin-top:32px).
- FansubStorySection (Server-Komponente) nutzt aktuell FansubPublicSections.module.css.

Clamp-Muster (Vorlage): frontend/src/components/profile/MemberStorySection.tsx (use client, useRef+useState, ResizeObserver, Button variant="subtle" size="sm", 'Mehr lesen'/'Weniger anzeigen') + MemberStorySection.module.css (storyContentClamped max-height + mask-image, storyContentExpanded).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Neue Sektions-Reihenfolge + Community-/Medien-Sektion einbinden + buildEmptyAreaLabels (AO5-03)</name>
  <files>frontend/src/app/fansubs/[slug]/page.tsx, frontend/src/app/fansubs/__tests__/page.test.tsx, frontend/src/app/fansubs/__tests__/pageHelpers.test.tsx</files>
  <read_first>
    - frontend/src/app/fansubs/[slug]/page.tsx (komplett) — Render-Reihenfolge :125-161, buildEmptyAreaLabels :39-62, heroStats :115-119.
    - frontend/src/app/fansubs/__tests__/page.test.tsx — Source-Assertion-Muster (indexOf-Reihenfolge, Sammelhinweis-Zählung).
    - frontend/src/app/fansubs/__tests__/pageHelpers.test.tsx — Pure-Function-Tests zu buildEmptyAreaLabels/hasStoryContent.
    - frontend/src/components/fansubs/FansubCommunityLinksSection.tsx + FansubMediaSection.tsx (aus 99-16) — Props-Signaturen.
    - .planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON5-CONTEXT.md (AO5-03 :29).
  </read_first>
  <action>
    In page.tsx die Render-Reihenfolge auf Hero → Geschichte → Laufende Projekte → Team → Erfolge → Medien umstellen (kehrt AO4-06 bewusst um). Konkret: Hero (readingColumn) bleibt zuerst; danach Geschichte (storyAvailable-Gate); dann Projekte (gridSection); dann Team; dann Erfolge (History); dann Community-Links-Sektion (FansubCommunityLinksSection mit links={profile.community_links}) und die Medien-Sektion (FansubMediaSection mit media={profile.media}) am Seitenende. Community-/Medien-Sektionen nur rendern, wenn die jeweilige Liste nicht leer ist (Komponenten rendern selbst null, zusätzlich Gate im page-Layout für konsistenten Abstand). Platzierung von Community-Links vs. Medien: Community-Links vor Medien (Discretion für Reading- vs. Grid-Spalte, aber konsistente sectionSpacing).
    buildEmptyAreaLabels anpassen: 'Medien' weiterhin nur bei profile.media.length===0; 'Mehr' nur pushen, wenn WEDER profile.group.website_url NOCH profile.community_links.length>0 vorhanden ist (Signatur ggf. um community_links erweitern oder profile bereits enthält es). Bestehende Labels (Projekte/Team/Geschichte/Erfolge/Externe Mitwirkende/Gruppenleitung) unverändert lassen.
    page.test.tsx: Reihenfolgen-Assertion auf die neue Reihenfolge aktualisieren (indexOf Geschichte < Projekte < Team < Erfolge < Medien) und Vorkommen von FansubMediaSection/FansubCommunityLinksSection prüfen. pageHelpers.test.tsx: Fall ergänzen, dass 'Mehr' bei vorhandenen community_links (aber ohne website_url) NICHT im Ergebnis ist, und bei komplett fehlenden Links vorhanden ist.
    Keine nativen Formular-Elemente einführen.
  </action>
  <acceptance_criteria>
    - `cd frontend; npx vitest run src/app/fansubs` grün (page.test.tsx + pageHelpers.test.tsx).
    - `cd frontend; npm run typecheck` fehlerfrei.
    - `grep -n "FansubMediaSection" frontend/src/app/fansubs/[slug]/page.tsx` und `grep -n "FansubCommunityLinksSection" frontend/src/app/fansubs/[slug]/page.tsx` liefern Treffer.
    - `grep -n "community_links" frontend/src/app/fansubs/[slug]/page.tsx` zeigt die 'Mehr'-Logik.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npx vitest run src/app/fansubs; npm run typecheck</automated>
  </verify>
  <done>Seite rendert die neue Reihenfolge inkl. Community-/Medien-Sektion; buildEmptyAreaLabels behandelt 'Medien' und 'Mehr' korrekt; Tests grün.</done>
</task>

<task type="auto">
  <name>Task 2: Gruppen-Geschichte einklappbar (Clamp) machen (AO5-04)</name>
  <files>frontend/src/components/fansubs/FansubStorySection.tsx, frontend/src/components/fansubs/FansubStorySection.module.css</files>
  <read_first>
    - frontend/src/components/fansubs/FansubStorySection.tsx (komplett) — aktuelle Server-Render-Logik (title/bodyHtml/bodyText Gate).
    - frontend/src/components/profile/MemberStorySection.tsx (komplett) — Clamp-Referenzmuster (use client, useRef/useState, ResizeObserver, isOverflowing-Messung, Button-Umschalter).
    - frontend/src/components/profile/MemberStorySection.module.css (storyContentClamped mask-image + max-height, storyContentExpanded, toggle).
  </read_first>
  <action>
    Wandle FansubStorySection.tsx in eine Client-Komponente ('use client') um und übernimm das Clamp-Muster aus MemberStorySection: Content in einen ref-Container legen, per ResizeObserver + window-resize isOverflowing messen, Zustand isExpanded via useState; im geklappten Zustand die Clamp-Klasse (max-height + mask-image-Fade) anwenden, expandiert max-height:none. Toggle-Button ausschließlich über @/components/ui Button (variant="subtle" size="sm") mit Labels 'Mehr anzeigen' / 'Weniger anzeigen' (korrekte Umlaute); nur anzeigen, wenn isOverflowing. Bestehendes Rendering (Titel-h3, RichTextRenderer für bodyHtml, sonst bodyText-Absatz, Gate bei komplett leerem Story) beibehalten. Lege ein dediziertes FansubStorySection.module.css an (Clamp-Klassen, nur Team4s-Tokens; die bisherige Nutzung von FansubPublicSections.module.css für sectionTitle/bodyText kann bestehen bleiben oder in das neue Modul übernommen werden — keine Ad-hoc-Farben). Clamp-Zeilenzahl/max-height ist Discretion.
  </action>
  <acceptance_criteria>
    - `grep -n "use client" frontend/src/components/fansubs/FansubStorySection.tsx` zeigt die Client-Direktive in Zeile 1.
    - `grep -c -e "Mehr anzeigen" -e "Weniger anzeigen" frontend/src/components/fansubs/FansubStorySection.tsx` bestätigt beide Umschalter-Labels.
    - `grep -c -e "<button" -e "<select" -e "<input" -e "<textarea" frontend/src/components/fansubs/FansubStorySection.tsx` ergibt 0.
    - `cd frontend; npm run typecheck` fehlerfrei; `cd frontend; npx vitest run src/app/fansubs` bleibt grün.
    - Datei ≤450 Zeilen.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run typecheck; npx vitest run src/app/fansubs</automated>
  </verify>
  <done>Gruppen-Geschichte ist über @/components/ui-Umschalter einklappbar/kürzbar; Clamp nur bei Überlauf sichtbar; typecheck + Tests grün.</done>
</task>

<task type="auto">
  <name>Task 3: Visuelle Politur konsistenter Kacheln/Abstände (AO5-07)</name>
  <files>frontend/src/components/fansubs/FansubPublicSections.module.css, frontend/src/app/fansubs/[slug]/page.module.css</files>
  <read_first>
    - frontend/src/components/fansubs/FansubPublicSections.module.css (komplett) — cardGrid :30-34, mediaItemGrid :94-98, sectionTitle :6-12, historyList :64-86.
    - frontend/src/app/fansubs/[slug]/page.module.css (readingColumn :202-205, sectionSpacing :207-209, gridSection :211-214, Mobile-Media-Query :231-264).
    - CLAUDE.md (nur Team4s-Tokens; keine neuen Ad-hoc-Farbwerte/CSS-Klassen außerhalb des Tokensystems).
  </read_first>
  <action>
    Harmonisiere Kachelgrößen und Abstände über Projekte, Team, Erfolge und Medien: einheitliche minmax-Spaltenbreiten und gap-Werte für die Grids (cardGrid/mediaItemGrid und die Team-Grids), damit keine überdimensionierten, unstimmigen Kacheln entstehen; konsistente sectionSpacing zwischen allen Sektionen; Textgrößen (Titel/Body) im bestehenden Token-/Größensystem halten, keine überdimensionierten Blöcke. Ausschließlich vorhandene Team4s-Tokens (var(--...)) verwenden, KEINE neuen Hex-/rgb-Ad-hoc-Farbwerte und keine neuen Farbklassen. Falls Mobile ≤390px horizontalen Überlauf zeigt, in der bestehenden Media-Query begrenzen. Keine Änderung an .tsx-Struktur in diesem Task (nur CSS).
  </action>
  <acceptance_criteria>
    - `cd frontend; npm run typecheck` fehlerfrei; `cd frontend; npx vitest run src/app/fansubs` grün.
    - `grep -nE "#[0-9a-fA-F]{3,6}|rgb\(|rgba\(" frontend/src/components/fansubs/FansubPublicSections.module.css` führt keine NEU eingeführten Ad-hoc-Farbwerte (nur vorbestehende, dokumentierte Fälle) — Diff zeigt nur var(--...)-Ergänzungen.
    - `grep -c "var(--" frontend/src/components/fansubs/FansubPublicSections.module.css` steigt bzw. bleibt (Token-Nutzung).
    - Beide CSS-Dateien enthalten keine neuen Klassen mit fest kodierten Farben.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run typecheck; npx vitest run src/app/fansubs</automated>
  </verify>
  <done>Kacheln/Abstände über alle befüllten Sektionen konsistent, keine überdimensionierten Texte, ausschließlich Team4s-Tokens; Tests grün.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/app/fansubs` + `npm run typecheck` grün.
- Grep-Gates: FansubMediaSection/FansubCommunityLinksSection in page.tsx, community_links in 'Mehr'-Logik, 'use client' + beide Clamp-Labels in FansubStorySection, keine nativen Formular-Elemente, keine neuen Ad-hoc-Farben.
- Live-Sichtprüfung (Reihenfolge, Clamp, Kachel-Konsistenz, Mobile) im UAT-Plan 99-18.
</verification>

<success_criteria>
- Seite in korrekter Reihenfolge mit Community-Links- und Medien-Sektion; Geschichte einklappbar; Kacheln/Abstände konsistent; alles über @/components/ui + Team4s-Tokens.
</success_criteria>

<output>
Erstelle `.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-17-SUMMARY.md` nach Abschluss.
</output>
