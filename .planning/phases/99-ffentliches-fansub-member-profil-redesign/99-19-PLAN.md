---
phase: 99
plan: "19"
type: execute
wave: 15
depends_on: []
files_modified:
  - "backend/internal/models/fansub.go"
  - "backend/internal/repository/fansub_repository.go"
  - "backend/internal/repository/fansub_repository_test.go"
  - "shared/contracts/openapi.yaml"
  - "frontend/src/types/fansub.ts"
autonomous: true
requirements_addressed:
  - "AO6-01"
  - "AO6-02"
  - "AO6-03"
requirements:
  - "Phase 99 Add-on 6 CONTEXT AO6-01 (Anime-Banner in Projekt-Karten)"
  - "Phase 99 Add-on 6 CONTEXT AO6-02 (Medien-Reihenfolge sort_order)"
  - "Phase 99 Add-on 6 CONTEXT AO6-03 (mehrere Geschichts-Blöcke, DTO story→stories[])"
must_haves:
  truths:
    - "[AO6-01] Jede PublicFansubProject-Karte trägt ein banner_url-Feld, aufgelöst aus anime.banner_resolved_url bzw. banner_asset_id→media_files.path; ohne Banner ist das Feld null (Frontend fällt auf cover_image zurück)."
    - "[AO6-02] listPublicFansubMedia sortiert nach fgm.sort_order ASC, dann created_at/id — die im Admin gesetzte Reihenfolge wird öffentlich übernommen."
    - "[AO6-03] Das Public-DTO liefert stories[] (ALLE public+published+nicht-gelöschten fansub_group_notes, ORDER BY sort_order ASC, id ASC) statt eines einzelnen story-Objekts; kein Go-Konsument referenziert mehr resp.Story (Singular)."
  artifacts:
    - path: "backend/internal/models/fansub.go"
      provides: "PublicFansubProfileResponse.Stories ([]PublicFansubStory) + PublicFansubProject.BannerURL"
      contains: "Stories []PublicFansubStory"
    - path: "backend/internal/repository/fansub_repository.go"
      provides: "listPublicFansubStories (alle Blöcke), banner_url-Auflösung im Projekt-Query, sort_order-Order im Medien-Query"
      contains: "fgm.sort_order"
    - path: "shared/contracts/openapi.yaml"
      provides: "PublicFansubProfile.stories (Array) + PublicFansubProject.banner_url"
      contains: "stories"
  key_links:
    - from: "backend/internal/repository/fansub_repository.go"
      to: "models.PublicFansubProfileResponse.Stories"
      via: "GetPublicProfileBySlug befüllt resp.Stories aus listPublicFansubStories"
      pattern: "resp\\.Stories = "
    - from: "frontend/src/types/fansub.ts"
      to: "PublicFansubProject.banner_url"
      via: "additives TS-Feld spiegelt Go-DTO"
      pattern: "banner_url"
---

<objective>
Backend-Public-DTO für Add-on 6 erweitern: (1) Anime-Banner-URL je Projekt-Karte, (2) Medien-Reihenfolge folgt dem Admin-`sort_order`, (3) mehrere Geschichts-Blöcke — DTO-Feld `story` (einzeln) wird zu `stories[]` (Array). Additiv, KEINE Migration.

Purpose: Diese DTO-Basis ist Voraussetzung für die Frontend-Sektionen (Projekt-Banner-Karussell, mehrere Geschichts-Blöcke) der Wellen 16–18.
Output: Go-Model + Repository-Queries + OpenAPI + additives TS-Feld (`banner_url`); die breaking TS-Migration `story→stories` erfolgt bewusst erst im Frontend-Plan 99-20 (typecheck bleibt hier grün).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON6-CONTEXT.md
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-15-SUMMARY.md

<interfaces>
<!-- Aktueller Stand aus dem Code (read-only verifiziert). Executor nutzt diese Signaturen direkt. -->

backend/internal/models/fansub.go — aktuell:
- PublicFansubProfileResponse { Group; Story *PublicFansubStory `json:"story"`; Projects []PublicFansubProject; History; Media; CommunityLinks }
- PublicFansubProject { ID; Title; Type; Status; Year *int16; CoverImage *string `json:"cover_image,omitempty"`; MaxEpisodes *int16 }
- PublicFansubStory { ID; Title; BodyHTML; BodyText; UpdatedAt }

backend/internal/repository/fansub_repository.go:
- GetPublicProfileBySlug:244 — initialisiert resp, ruft getPublicFansubStory:291 (LIMIT 1), listPublicFansubProjects:318, listPublicFansubMedia:395 auf; Zeile 262 `resp.Story = story`.
- getPublicFansubStory:291 — SELECT ... FROM fansub_group_notes WHERE visibility='public' AND status='published' AND deleted_at IS NULL ORDER BY sort_order ASC, id ASC LIMIT 1; gibt bei ErrNoRows (nil,nil) zurück.
- listPublicFansubProjects:318 — SELECT a.id,a.title,a.type::text,a.status::text,a.year,a.cover_image,a.max_episodes FROM anime_fansub_groups afg JOIN anime a ...
- listPublicFansubMedia:395 — ORDER BY ma.created_at ASC, ma.id ASC (aktuell).

Banner-Auflösungs-Muster (Vorbild anime_assets.go:60-82 und admin_content_anime_themes.go:1885):
- Spalten: anime.banner_asset_id, anime.banner_resolved_url (0039_add_anime_asset_slots.up.sql:2-5).
- Auflösung: COALESCE(NULLIF(BTRIM(a.banner_resolved_url), ''), <media_files.path des banner_asset>).

DB-Fakten:
- fansub_group_media.sort_order INTEGER NOT NULL DEFAULT 0 (0109_fansub_group_media_management.up.sql:8).
- fansub_group_notes: title, body_html, sort_order, visibility, status, deleted_at (0061_fansub_group_notes.up.sql).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Go-Model + Repository — banner_url, sort_order-Order, stories[] (AO6-01/02/03)</name>
  <files>backend/internal/models/fansub.go, backend/internal/repository/fansub_repository.go</files>
  <read_first>
    - backend/internal/models/fansub.go (PublicFansubProfileResponse:64, PublicFansubProject:83, PublicFansubStory:74)
    - backend/internal/repository/fansub_repository.go:244-422 (GetPublicProfileBySlug, getPublicFansubStory, listPublicFansubProjects, listPublicFansubMedia)
    - backend/internal/repository/anime_assets.go:60-82 (Banner-Auflösungs-Join als Vorbild)
    - .planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON6-CONTEXT.md (AO6-01/02/03)
  </read_first>
  <action>
    AO6-03: In models.PublicFansubProfileResponse das Feld `Story *PublicFansubStory `json:"story"`` ersetzen durch `Stories []PublicFansubStory `json:"stories"``. In GetPublicProfileBySlug die Initialisierung um `Stories: make([]models.PublicFansubStory, 0)` ergänzen; `getPublicFansubStory` umbenennen/umbauen zu `listPublicFansubStories`, das ALLE Zeilen zurückgibt: gleiche WHERE-Klausel (visibility='public' AND status='published' AND deleted_at IS NULL), ORDER BY sort_order ASC, id ASC, OHNE LIMIT 1; Ergebnis als []models.PublicFansubStory (leerer Slice statt nil bei keinen Zeilen). Zeile 262 `resp.Story = story` ersetzen durch `resp.Stories = stories`. Prüfe per Grep, dass danach kein weiterer Go-Code auf `.Story` dieses Structs zugreift (einziger bekannter Konsument ist fansub_repository.go:262).

    AO6-02: In listPublicFansubMedia die ORDER-BY-Klausel von `ORDER BY ma.created_at ASC, ma.id ASC` ändern zu `ORDER BY fgm.sort_order ASC, ma.created_at ASC, ma.id ASC`.

    AO6-01: In models.PublicFansubProject ein Feld `BannerURL *string `json:"banner_url,omitempty"`` ergänzen. In listPublicFansubProjects den SELECT um eine aufgelöste Banner-Spalte erweitern: `COALESCE(NULLIF(BTRIM(a.banner_resolved_url), ''), (SELECT bmf.path FROM media_files bmf WHERE bmf.media_id = a.banner_asset_id AND (bmf.variant = 'original' OR bmf.variant IS NULL) AND bmf.status = 'ready' ORDER BY bmf.id ASC LIMIT 1)) AS banner_url`. Das Subquery (statt LEFT JOIN) verhindert Zeilen-Vervielfachung. Den Scan um `&project.BannerURL` erweitern (nullable *string). Fallback auf cover_image bleibt dem Frontend überlassen (banner_url ?? cover_image).
  </action>
  <acceptance_criteria>
    - `grep -n "Stories \[\]PublicFansubStory" backend/internal/models/fansub.go` findet das neue Feld; `grep -c "Story \*PublicFansubStory" backend/internal/models/fansub.go` liefert 0 für das Profile-Struct-Feld (PublicFansubStory-Definition selbst bleibt).
    - `grep -n "resp.Stories = stories" backend/internal/repository/fansub_repository.go` vorhanden; `grep -c "resp.Story = " backend/internal/repository/fansub_repository.go` = 0.
    - `grep -n "ORDER BY fgm.sort_order ASC" backend/internal/repository/fansub_repository.go` vorhanden.
    - `grep -n "banner_resolved_url" backend/internal/repository/fansub_repository.go` vorhanden; `grep -n "BannerURL" backend/internal/models/fansub.go` vorhanden.
    - `cd backend && go build ./...` fehlerfrei.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./internal/repository/ ./internal/models/</automated>
  </verify>
  <done>Go kompiliert; DTO trägt Stories[] + BannerURL; Medien-Query nach sort_order sortiert; nur ein Story-Loader liefert alle Blöcke.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OpenAPI + additives TS-Feld + Repo-Source-Invarianten (AO6-01/02/03)</name>
  <files>shared/contracts/openapi.yaml, frontend/src/types/fansub.ts, backend/internal/repository/fansub_repository_test.go</files>
  <read_first>
    - shared/contracts/openapi.yaml:10269-10404 (PublicFansubStory, PublicFansubProject, PublicFansubProfile)
    - frontend/src/types/fansub.ts:144-190 (PublicFansubStory, PublicFansubProject, PublicFansubProfile)
    - backend/internal/repository/fansub_repository_test.go:45-83 (TestFansubRepository_PublicProfileSourceInvariants Fragmentliste)
  </read_first>
  <behavior>
    - TestFansubRepository_PublicProfileSourceInvariants bleibt grün und pinnt die neuen SQL-Fragmente (`ORDER BY fgm.sort_order`, `banner_resolved_url`, `listPublicFansubStories`).
    - Der bestehende Frontend-Typecheck bleibt grün: `PublicFansubProject.banner_url` ist additiv/optional, `PublicFansubProfile.story` bleibt in diesem Plan unverändert (Migration erst in 99-20).
  </behavior>
  <action>
    OpenAPI (shared/contracts/openapi.yaml): In PublicFansubProject unter properties `banner_url: { type: string, nullable: true }` ergänzen. In PublicFansubProfile das `story`-Property (nullable oneOf PublicFansubStory) ersetzen durch `stories: { type: array, items: { $ref: '#/components/schemas/PublicFansubStory' } }`; im `required`-Array `story` durch `stories` ersetzen.

    TS (frontend/src/types/fansub.ts): In interface PublicFansubProject `banner_url?: string | null;` ergänzen (additiv). Die PublicFansubProfile-`story`-Zeile in DIESEM Plan NICHT ändern (bleibt `story: PublicFansubStory | null;`) — die breaking Umstellung auf `stories` migriert Plan 99-20 zusammen mit allen Frontend-Konsumenten, damit der Typecheck zwischenzeitlich grün bleibt.

    Repo-Test (fansub_repository_test.go): Die Fragmentliste in TestFansubRepository_PublicProfileSourceInvariants aktualisieren — Fragment für den Single-Story-Loader entfernen falls es den alten Funktionsnamen pinnt, und Fragmente `"ORDER BY fgm.sort_order ASC"`, `"banner_resolved_url"` sowie `"func (r *FansubRepository) listPublicFansubStories"` ergänzen. Bestehende Fragmente (FROM fansub_group_notes, visibility='public', status='published', deleted_at IS NULL, FROM anime_fansub_groups afg, FROM fansub_group_media fgm, rs.code='approved') beibehalten.
  </action>
  <acceptance_criteria>
    - `grep -n "banner_url" shared/contracts/openapi.yaml` und `grep -n "banner_url" frontend/src/types/fansub.ts` vorhanden.
    - `grep -n "stories:" shared/contracts/openapi.yaml` im PublicFansubProfile-Schema vorhanden; `grep -c "story:" shared/contracts/openapi.yaml` im PublicFansubProfile-Block = 0 (Property umbenannt).
    - `cd backend && go test ./internal/repository/ -run TestFansubRepository_PublicProfileSourceInvariants` grün.
    - `cd frontend && npm run typecheck` fehlerfrei (banner_url additiv, story unverändert).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/repository/ -run TestFansubRepository_PublicProfileSourceInvariants && cd ../frontend && npm run typecheck</automated>
  </verify>
  <done>OpenAPI trägt stories[] + banner_url; TS trägt additives banner_url; Repo-Source-Test pinnt die neuen SQL-Fragmente und ist grün; Frontend-Typecheck grün.</done>
</task>

</tasks>

<verification>
- `cd backend && go build ./... && go test ./internal/repository/ -run TestFansubRepository_PublicProfileSourceInvariants` grün.
- Backend live neu bauen: `docker compose up -d --build team4sv30-backend`, dann `/api/v1/fansub-slugs/c-subs/public-profile` (öffentlich) liefert `stories` als Array und Projekt-Objekte mit `banner_url` (null wenn kein Banner). Host-Port via `docker ps` bestätigen.
- `cd frontend && npm run typecheck` grün (Frontend-Konsumenten von `story` unverändert lauffähig; Migration folgt in 99-20).
</verification>

<success_criteria>
- Public-DTO liefert `stories[]` (alle public/published Blöcke) statt Einzel-`story`; kein Go-Konsument nutzt mehr `resp.Story`.
- Projekt-Objekte tragen `banner_url` (aufgelöst aus banner_resolved_url/banner_asset_id).
- Medien-Query respektiert `fgm.sort_order`.
- OpenAPI + additives TS-Feld konsistent; Repo-Source-Test grün; Live-Endpoint verifiziert.
</success_criteria>

<output>
Create `.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-19-SUMMARY.md` when done
</output>
