---
phase: 99
plan: "21"
type: execute
wave: 16
depends_on:
  - "99-19"
files_modified:
  - "frontend/src/components/fansubs/FansubTeamSection.tsx"
  - "frontend/src/components/fansubs/FansubTeamActiveGroup.tsx"
  - "frontend/src/components/fansubs/FansubTeamHistoricalGroup.tsx"
  - "frontend/src/components/fansubs/FansubTeamSection.module.css"
  - "frontend/src/components/fansubs/fansubTeamPeriod.ts"
  - "frontend/src/components/fansubs/__tests__/fansubTeamPeriod.test.ts"
  - "frontend/src/components/fansubs/__tests__/FansubTeamActiveGroup.test.tsx"
  - "frontend/src/components/fansubs/__tests__/FansubTeamHistoricalGroup.test.tsx"
autonomous: true
requirements_addressed:
  - "AO6-07"
  - "AO6-08"
  - "AO6-04"
requirements:
  - "Phase 99 Add-on 6 CONTEXT AO6-07 (Team zweispaltig, klickbar erkennbar, historisch einklappbar)"
  - "Phase 99 Add-on 6 CONTEXT AO6-08 (historische Mitglieder: Rolle + Zeitraum)"
  - "Phase 99 Add-on 6 CONTEXT AO6-04 (ein klarer Header pro Sektion)"
must_haves:
  truths:
    - "[AO6-07] Aktive Mitglieder mit öffentlichem Profil (member_slug != null) sind optisch klar als Link zu /members/[slug] erkennbar (Akzent-Name + Chevron-Icon, Hover); nicht-öffentliche Mitglieder bleiben neutral/nicht klickbar."
    - "[AO6-07] Die Mitgliederliste ist zweispaltig (2 Spalten Desktop, 1 Spalte Mobil); die historische Liste wird ab einer Schwelle (~8–10) eingeklappt mit 'X weitere anzeigen'."
    - "[AO6-08] Historische Mitglieder zeigen zusätzlich zur Rolle den Zeitraum: 'JJJJ–JJJJ' aus joined_year/left_year, 'seit JJJJ' ohne Enddatum; die 'unbestätigt'-Badge bleibt erhalten."
    - "[AO6-04] Die Team-Sektion hat genau EINEN klaren Header (SectionHeader ohne redundantes Eyebrow-Doppel)."
  artifacts:
    - path: "frontend/src/components/fansubs/fansubTeamPeriod.ts"
      provides: "reine Formatierungsfunktion Zeitraum aus joined_year/left_year"
      exports: ["formatMemberPeriod"]
    - path: "frontend/src/components/fansubs/FansubTeamHistoricalGroup.tsx"
      provides: "historische Liste mit Rolle+Zeitraum und Einklappen (client)"
      contains: "formatMemberPeriod"
  key_links:
    - from: "frontend/src/components/fansubs/FansubTeamActiveGroup.tsx"
      to: "/members/[slug]"
      via: "Link mit Akzent-Name + Chevron nur bei member_slug != null"
      pattern: "/members/"
    - from: "frontend/src/components/fansubs/FansubTeamHistoricalGroup.tsx"
      to: "formatMemberPeriod(joined_year, left_year)"
      via: "Zeitraum-Anzeige je historischem Eintrag"
      pattern: "formatMemberPeriod"
---

<objective>
Team-Sektion der öffentlichen Fansub-Profilseite ausbauen: zweispaltige dichte Liste, aktive Mitglieder mit öffentlichem Profil klar als Link erkennbar (Akzent + Chevron), historische Mitglieder mit Rolle + Zeitraum, und Einklappen der historischen Liste ab einer Schwelle. Ein klarer Sektions-Header (AO6-04).

Purpose: Bei 20 Mitgliedern muss die Liste dicht, navigierbar und einklappbar bleiben; historische Nennungen brauchen Zeitkontext.
Output: Überarbeitete Team-Komponenten + reine Zeitraum-Formatierung + Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON6-CONTEXT.md

<interfaces>
<!-- Aktueller Stand (read-only verifiziert). -->

frontend/src/types/domain-projection.ts:
- DomainProjectionMemberRow { member_display_name; member_slug: string|null; role_labels: string[]; profile_status; claimed; ... }
- DomainProjectionHistoricalRow { member_display_name; member_slug: string|null; role_labels: string[]; joined_year: number|null; left_year: number|null; profile_status; claimed; ... }

FansubTeamSection.tsx: rendert `<SectionHeader eyebrow="Fansub" title="Team & Mitglieder" />` (Eyebrow+Titel = Doppel → AO6-04), dann FansubTeamActiveGroup / FansubTeamHistoricalGroup / FansubTeamMemorialBlock.
FansubTeamActiveGroup.tsx:12-22 — renderMemberName: bei member_slug != null Link zu '/members/'+slug (className styles.memberName), sonst span. Grid styles.activeGrid.
FansubTeamHistoricalGroup.tsx:24-26 — renderRoles gibt nur role_labels.join(', ') zurück (kein Zeitraum). Rendert 'Ehemalige Mitglieder' (former=claimed) als Card + 'Historische Nennungen' (unconfirmed) mit Badge variant='muted' 'unbestätigt'. Liste styles.historicalList.
FansubTeamSection.module.css: activeGrid = repeat(auto-fit, minmax(220px,1fr)); memberName font-weight:700 color:--text-primary; historicalList flex column gap:8px; subgroupTitle. Tokens: --text-primary/-secondary/-muted, --accent-primary, --color-border, --bg-hover.

Icons: lucide-react (z.B. ChevronRight) — Muster wie FansubCommunityLinksSection.
@/components/ui: Button (variant subtle/ghost, size sm), Badge, Card, SectionHeader.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zeitraum-Formatierung + historische Liste (Rolle+Zeitraum, Einklappen) (AO6-08/07)</name>
  <files>frontend/src/components/fansubs/fansubTeamPeriod.ts, frontend/src/components/fansubs/__tests__/fansubTeamPeriod.test.ts, frontend/src/components/fansubs/FansubTeamHistoricalGroup.tsx, frontend/src/components/fansubs/__tests__/FansubTeamHistoricalGroup.test.tsx</files>
  <read_first>
    - frontend/src/components/fansubs/FansubTeamHistoricalGroup.tsx (gesamt)
    - frontend/src/types/domain-projection.ts:27-39
    - .planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON6-CONTEXT.md (AO6-08)
  </read_first>
  <behavior>
    - formatMemberPeriod(2018, 2020) === '2018–2020' (Halbgeviertstrich U+2013).
    - formatMemberPeriod(2018, null) === 'seit 2018'.
    - formatMemberPeriod(null, 2020) === 'bis 2020'.
    - formatMemberPeriod(null, null) === '' (leerer String, keine Anzeige).
    - FansubTeamHistoricalGroup zeigt je Eintrag role_labels UND (falls vorhanden) den Zeitraum; 'unbestätigt'-Badge bleibt bei unconfirmed.
    - Bei > Schwelle (Konstante, z.B. 9) Einträgen ist die Liste eingeklappt und ein 'X weitere anzeigen'-Umschalter blendet den Rest ein.
  </behavior>
  <action>
    fansubTeamPeriod.ts NEU: exportiere reine Funktion `formatMemberPeriod(joinedYear: number|null, leftYear: number|null): string` gemäß Behavior (Halbgeviertstrich '–', deutsche Präfixe 'seit'/'bis', leerer String bei beidem null).

    FansubTeamHistoricalGroup.tsx: renderRoles um den Zeitraum ergänzen — Rolle und formatMemberPeriod(member.joined_year, member.left_year) getrennt anzeigen (Zeitraum als sekundäre Meta, z.B. styles.memberPeriod), Zeitraum nur wenn nicht-leer. 'unbestätigt'-Badge (variant='muted') beibehalten. Für das Einklappen die Komponente zu 'use client' machen: eine Konstante HISTORICAL_COLLAPSE_THRESHOLD (z.B. 9); useState showAll; wenn Gesamtzahl der historischen Einträge > Schwelle, nur die ersten N rendern und einen @/components/ui Button (variant='subtle', size='sm') 'Noch X anzeigen' / 'Weniger anzeigen' zeigen (korrekte Umlaute, dynamische Restzahl). Kein natives button.
  </action>
  <acceptance_criteria>
    - `grep -n "export function formatMemberPeriod" frontend/src/components/fansubs/fansubTeamPeriod.ts` vorhanden.
    - `grep -n "formatMemberPeriod" frontend/src/components/fansubs/FansubTeamHistoricalGroup.tsx` vorhanden; `grep -n "use client" frontend/src/components/fansubs/FansubTeamHistoricalGroup.tsx` vorhanden.
    - `grep -Ev '^\s*//' frontend/src/components/fansubs/FansubTeamHistoricalGroup.tsx | grep -c "<button"` = 0.
    - `cd frontend && npx vitest run src/components/fansubs/__tests__/fansubTeamPeriod.test.ts src/components/fansubs/__tests__/FansubTeamHistoricalGroup.test.tsx` grün.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/fansubs/__tests__/fansubTeamPeriod.test.ts src/components/fansubs/__tests__/FansubTeamHistoricalGroup.test.tsx</automated>
  </verify>
  <done>Zeitraum-Formatierung getestet; historische Mitglieder zeigen Rolle+Zeitraum; lange Liste einklappbar; unbestätigt-Badge erhalten.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Aktive Mitglieder klickbar erkennbar + zweispaltig + Header entdoppelt (AO6-07/04)</name>
  <files>frontend/src/components/fansubs/FansubTeamActiveGroup.tsx, frontend/src/components/fansubs/FansubTeamSection.tsx, frontend/src/components/fansubs/FansubTeamSection.module.css, frontend/src/components/fansubs/__tests__/FansubTeamActiveGroup.test.tsx</files>
  <read_first>
    - frontend/src/components/fansubs/FansubTeamActiveGroup.tsx (gesamt)
    - frontend/src/components/fansubs/FansubTeamSection.tsx (gesamt)
    - frontend/src/components/fansubs/FansubTeamSection.module.css (gesamt)
  </read_first>
  <behavior>
    - Mitglied mit member_slug rendert einen Link zu /members/[slug] mit Akzent-Styling + einem Chevron-Icon (aria-hidden); Mitglied ohne slug rendert einen neutralen Namen ohne Link/Chevron.
    - Das aktive Grid zeigt 2 Spalten auf Desktop und 1 Spalte auf schmalen Viewports.
    - Die Team-Sektion rendert genau einen SectionHeader ohne redundantes Eyebrow.
  </behavior>
  <action>
    FansubTeamActiveGroup.tsx: renderMemberName so erweitern, dass der Link (member_slug != null) klar als solcher erkennbar ist — Akzent-Klasse (styles.memberLink, color var(--accent-primary)) plus lucide-react ChevronRight (size 14, aria-hidden='true') am Namensende und Hover-Unterstreichung; nicht-verlinkte Mitglieder behalten styles.memberName (neutral). Der bestehende Link-Pfad '/members/'+member_slug bleibt.

    FansubTeamSection.tsx: `<SectionHeader eyebrow="Fansub" title="Team & Mitglieder" />` auf einen einzelnen Header ohne Eyebrow reduzieren (title beibehalten, z.B. 'Team & Mitglieder').

    FansubTeamSection.module.css: activeGrid auf zweispaltig festlegen — grid-template-columns: repeat(2, minmax(0, 1fr)) mit gap; per @media (max-width: 640px) auf 1 Spalte. Neue Klassen memberLink (Akzentfarbe, inline-flex, gap, align-items center für Chevron) und memberPeriod (sekundäre Meta, --text-muted, font-size 13-14px). Nur Team4s-Tokens.
  </action>
  <acceptance_criteria>
    - `grep -n "ChevronRight" frontend/src/components/fansubs/FansubTeamActiveGroup.tsx` vorhanden; Link nur im member_slug-Zweig.
    - `grep -c "eyebrow" frontend/src/components/fansubs/FansubTeamSection.tsx` = 0.
    - `grep -n "repeat(2" frontend/src/components/fansubs/FansubTeamSection.module.css` vorhanden; `grep -n "max-width: 640px" frontend/src/components/fansubs/FansubTeamSection.module.css` vorhanden.
    - `grep -Ev '^\s*//' frontend/src/components/fansubs/FansubTeamActiveGroup.tsx | grep -c "<button"` = 0.
    - `cd frontend && npx vitest run src/components/fansubs/__tests__/FansubTeamActiveGroup.test.tsx && npm run typecheck` grün.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/fansubs && npm run typecheck</automated>
  </verify>
  <done>Aktive Mitglieder mit Profil klar klickbar (Akzent+Chevron), zweispaltiges Grid mit Mobil-Fallback, ein Sektions-Header; Tests grün.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run typecheck` und `npx vitest run src/components/fansubs` grün.
- Live: `docker restart team4sv30-frontend`, harter Reload `/fansubs/c-subs` (:3000, Host-Port via `docker ps`) — aktive Mitglieder mit Profil als Akzent-Link+Chevron; zweispaltig auf Desktop, einspaltig ≤390px; historische zeigen Rolle+Zeitraum; lange historische Liste einklappbar.
</verification>

<success_criteria>
- Aktive Mitglieder mit öffentlichem Profil sind klar klickbar → /members/[slug].
- Liste zweispaltig (Desktop) / einspaltig (Mobil); historische Liste einklappbar.
- Historische Mitglieder zeigen Rolle + Zeitraum; unbestätigt-Badge erhalten.
- Ein klarer Sektions-Header ohne Doppelung.
</success_criteria>

<output>
Create `.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-21-SUMMARY.md` when done
</output>
