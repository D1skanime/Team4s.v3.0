---
phase: 99
plan: "02"
type: execute
wave: 2
depends_on:
  - "99-01"
files_modified:
  - "frontend/src/app/members/[slug]/page.tsx"
  - "frontend/src/app/members/[slug]/page.module.css"
  - "frontend/src/components/profile/MemberProfileHero.tsx"
  - "frontend/src/components/profile/MemberBadgeChain.tsx"
  - "frontend/src/components/profile/MemberBadgeChain.module.css"
  - "frontend/src/components/profile/MemberBadgeChain.test.tsx"
  - "frontend/src/components/profile/MemberCurrentProjectsSection.tsx"
  - "frontend/src/components/profile/MemberCurrentProjectsSection.module.css"
  - "frontend/src/components/profile/profile.module.css"
autonomous: true
requirements_addressed:
  - "D-01"
  - "D-02"
  - "D-03"
  - "D-04"
  - "D-05"
  - "D-06"
  - "D-07"
  - "D-15"
  - "D-16"
  - "D-18"
  - "A-01"
  - "A-03"
  - "A-05"
requirements:
  - "Phase 99 CONTEXT D-01-D-07"
  - "Phase 99 CONTEXT D-15/D-16/D-18"
  - "Phase 99 CONTEXT A-01/A-03/A-05"
must_haves:
  truths:
    - "Public member page no longer renders old tab navigation or public role timeline."
    - "Hero, group membership, current project, and badge-chain sections render real public profile DTO data."
    - "Badge progress renders x von y and no tier marker appears without a real tier field."
  artifacts:
    - path: "frontend/src/app/members/[slug]/page.tsx"
      provides: "Single-scroll public route shell"
    - path: "frontend/src/components/profile/MemberCurrentProjectsSection.tsx"
      provides: "Current projects section"
    - path: "frontend/src/components/profile/MemberBadgeChain.tsx"
      provides: "Horizontal badge chain"
  key_links:
    - from: "frontend/src/app/members/[slug]/page.tsx"
      to: "frontend/src/types/profile.ts"
      via: "PublicMemberProfileData current_projects/public_badges"
      pattern: "current_projects|public_badges"
---

<objective>
Refactor the public `/members/[slug]` page into the top half of the locked single-scroll layout: Hero, Gruppenzugehörigkeit, Aktuelle Projekte, and Auszeichnungen.

Purpose: Remove the old public tab/timeline framing and render the new page from the additive Phase 99 DTO while preserving existing public profile visibility behavior and design-system conventions.

Output: Route composition and top-section components with passing tests for the staged top-shell/no-old-tabs behavior, hero semantics, multiple memberships, current projects, and badge chain/progress. Full D-01 section-order verification remains reserved for Plan 99-03 after the lower sections exist.
</objective>

<context>
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-CONTEXT.md
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-RESEARCH.md
@AGENTS.md
@docs/engineering/implementation-contract.md
@docs/frontend/ui-system.md
@docs/agent-guidelines-ui.md
@docs/api/api-contracts.md
</context>

<read_first>
- `frontend/src/app/members/[slug]/page.tsx` - current route imports `MemberSectionNav`, `getMemberContributions`, `MemberContributionFilters`, and `MemberRoleTimeline`; these are outdated for Phase 99 public page composition.
- `frontend/src/app/members/[slug]/page.module.css` - route-level layout and toolbar styles.
- `frontend/src/components/profile/MemberProfileHero.tsx` - avatar/name/verified/status/schwerpunkt rendering, memorial delegation, and current `roleTimeline` dependency.
- `frontend/src/components/profile/MembershipsSection.tsx` and `MembershipsSection.test.tsx` - existing clickable public group cards.
- `frontend/src/components/profile/MemberBadgeHighlights.tsx`, `MemberBadgeHighlights.module.css`, `memberBadgeLabels.ts` - existing badge labels and current grid/top-N behavior to replace for public page.
- `frontend/src/components/profile/RecentContributionsSection.tsx` - project card/progress pattern from existing real project DTOs.
- `frontend/src/types/profile.ts` - Plan 99-01 additive DTOs.
- Wave-0 tests: `frontend/src/app/members/[slug]/page.test.tsx`, `frontend/src/components/profile/MemberBadgeChain.test.tsx`, backend current-project tests. If `page.test.tsx` includes a full D-01 order test from Wave 0, stage it so Wave 2 runs only the top-shell/no-old-tabs assertions and Plan 99-03 enables the full order assertion once lower sections are wired.
</read_first>

<tasks>
<task type="auto">
  <name>Task 1: Replace old tab/timeline route frame with locked section shell</name>
  <files>frontend/src/app/members/[slug]/page.tsx, frontend/src/app/members/[slug]/page.module.css</files>
  <action>
    In `frontend/src/app/members/[slug]/page.tsx`, remove the public-page dependency on `MemberSectionNav`, `getMemberContributions`, `MemberContributionFilters`, and `MemberRoleTimeline`. Keep `getMemberProfile(slug, token || undefined)`, `OwnHiddenProfilePreview`, `OwnProfileEditLink`, and `CorrectionReportModal` behavior intact. The public route remains anonymous with optional owner preview token from the existing server boundary; do not add client-side token reads or a new auth helper.

    Compose the single-scroll public page shell in D-01 order using sections with stable accessible headings. This plan wires the first four sections only: Hero, Gruppenzugehörigkeit, Aktuelle Projekte, Auszeichnungen. Lower sections (`Letzte Beiträge`, `Fansub-Geschichte`, `Frühere Mitwirkungen`) are wired by Plan 99-03, which depends on this route shell. Do not render the old tab labels or a sticky tab nav. Do not add example or hard-coded cards; if a data array is empty, render either no section when D-13/D-14 requires omission or a scoped empty state only where the decision allows it.

    If `frontend/src/app/members/[slug]/page.test.tsx` was created in Plan 99-00 with one full section-order assertion, split or tag it before this verify runs: Wave 2 assertions cover "top-shell renders first four sections and no old tabs/timeline"; the full seven-section order assertion is enabled in Plan 99-03 after lower sections exist. Do not leave Wave 2 depending on lower-section components that are deliberately not implemented yet.

    Update CSS locally in `page.module.css` for the scrollable page rhythm and responsive section spacing using existing CSS variables/classes. Do not introduce new tokens, a UI framework, or broad unrelated visual redesign.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run test -- src/app/members/[slug]/page.test.tsx</automated>
  </verify>
  <done>The public route no longer renders the old tab/timeline frame and the staged page test passes for top-shell/no-old-tabs behavior without requiring Plan 99-03 lower sections.</done>
</task>

<task type="auto">
  <name>Task 2: Retain hero semantics and render group/current-project cards</name>
  <files>frontend/src/components/profile/MemberProfileHero.tsx, frontend/src/components/profile/MemberCurrentProjectsSection.tsx, frontend/src/components/profile/MemberCurrentProjectsSection.module.css, frontend/src/components/profile/profile.module.css, frontend/src/app/members/[slug]/page.tsx</files>
  <action>
    Update `MemberProfileHero.tsx` for D-02 without changing semantic data: avatar, name, verified status, profile status, and Schwerpunkt/known-for fields remain derived from real profile data. Render the verified status inline next to the name and keep the public visual direction aligned with the approved navy gradient header. Remove reliance on the old public `roleTimeline` fetch; derive public Schwerpunkt only from fields available in `PublicMemberProfileData` and `current_projects`, or omit that subline if there is no real data.

    Reuse `MembershipsSection` for D-03/A-05 where it already fits: clickable cards link to `/fansubs/${fansub_group_slug}`, support multiple memberships, use real logo URL/short fallback, role label, and joined-year/member-since where available. If `MembershipsSection` lacks joined-year display, extend it in-place or through a tiny prop-compatible addition; do not create a parallel group-card component for the same domain.

    Add `MemberCurrentProjectsSection.tsx` for D-04/D-05/A-01. It consumes `profile.current_projects` from Plan 99-01, renders one card per active Anime/project, includes real cover if provided, project title, real release-version chips/list from `release_versions`, project-level indicator only when `is_project_level` is true, and all role labels. Cards link to the public Anime/project detail route already used by the app; if the exact public group-specific route is present, prefer `/anime/{anime_id}/group/{fansub_group_id}`, otherwise use the existing public anime detail link pattern found in code. Do not link to `/me/projects` from the public page.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run test -- src/app/members/[slug]/page.test.tsx src/components/profile/MembershipsSection.test.tsx</automated>
  </verify>
  <done>Hero, membership, and current-project sections render real DTO data, no old role timeline is fetched, multiple group memberships are preserved, and current projects do not infer active/completed from UI assumptions.</done>
</task>

<task type="auto">
  <name>Task 3: Replace badge grid with public badge chain and progress</name>
  <files>frontend/src/components/profile/MemberBadgeChain.tsx, frontend/src/components/profile/MemberBadgeChain.module.css, frontend/src/components/profile/MemberBadgeChain.test.tsx, frontend/src/components/profile/memberBadgeLabels.ts, frontend/src/app/members/[slug]/page.tsx</files>
  <action>
    Implement `MemberBadgeChain` for D-06/D-07/A-03 using existing `PublicMemberBadge` and existing badge label/presentation data from `memberBadgeLabels.ts`. Export a public badge catalog from `memberBadgeLabels.ts` only if needed; keep it as presentational metadata, not new backend gamification logic.

    Render badges horizontally scrollable. Earned badges render normally from `profile.public_badges`; unearned catalog badges render disabled/locked using `lucide-react` lock icon or an accessible locked marker. Show progress as `x von y`. Because research found no generic badge `tier` or `level` field in schema/DTO, omit tier markers unless Plan 99-01 later exposes a real tier field. Do not invent points, rarity, levels, or additional badge gamification.

    Replace the old `MemberBadgeHighlights` usage on the public page with `MemberBadgeChain`. Do not break `MemberBadgeHighlights` if it is still used by `/me/profile` or other existing surfaces.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run test -- src/components/profile/MemberBadgeChain.test.tsx src/app/members/[slug]/page.test.tsx</automated>
  </verify>
  <done>Badge chain test passes: horizontal chain, locked unearned badges, `x von y` progress, and no invented tier marker.</done>
</task>
</tasks>

<verification>
- `cd frontend; npm run test -- src/app/members/[slug]/page.test.tsx src/components/profile/MemberBadgeChain.test.tsx src/components/profile/MembershipsSection.test.tsx`
- `cd frontend; npm run typecheck`
- `git diff --check`
</verification>

<success_criteria>
- Public profile route has no old tab navigation or public role-timeline dependency.
- Wave 2 verification does not require lower-section components that are scheduled for Plan 99-03.
- Hero semantics are retained and rearranged per D-02.
- Group memberships render as public navigation cards and support multiple memberships.
- Current projects use `current_projects` DTO data from existing member/role/release-version mappings.
- Badges render as a horizontal earned/locked chain with progress and no fake tier.
- All visible German UI text uses correct umlauts.
</success_criteria>

<notes>
This plan intentionally does not implement latest feed, story clamp, or previous-history reveal; those lower sections are Plan 99-03 and depend on this route shell. It must not add example lower-section content.
</notes>
