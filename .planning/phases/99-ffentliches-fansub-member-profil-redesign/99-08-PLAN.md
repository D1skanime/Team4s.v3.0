---
phase: 99
plan: "08"
type: execute
wave: 6
depends_on:
  - "99-05"
  - "99-07"
files_modified:
  - "backend/internal/repository/group_repository.go"
  - "backend/internal/repository/release_detail_public_repository.go"
  - "backend/internal/repository/release_detail_cursor_test.go"
  - "backend/internal/handlers/group_contributors_handler.go"
  - "backend/cmd/server/main.go"
  - "shared/contracts/openapi.yaml"
  - "frontend/src/lib/api.ts"
  - "frontend/src/types/releaseDetail.ts"
autonomous: true
requirements_addressed:
  - "AO4-03"
  - "AO4-24"
requirements:
  - "Phase 99 Add-on 4 AO4-03, AO4-24"
must_haves:
  truths:
    - "AO4-03: Cursor-basierte (Seek-)Pagination existiert ausschliesslich fuer drei nachladende Listen: vollstaendige Release-Liste, vollstaendige Bildergalerie, vollstaendige Textliste."
    - "AO4-03: Die bestehende Offset-Release-Liste (GetGroupReleases + releases/page.tsx) bleibt unveraendert und funktionsfaehig; der Cursor-Modus ist additiv."
    - "AO4-24: Die drei Nachlade-Listen liefern next_cursor/has_more statt page/offset; andere Listen (Members, Admin, Anime, Comments) bleiben Offset."
    - "AO4-03: Cursor-Basis Release-Liste ist der stabile Sortierschluessel (episode_number, rev.id); Bilder/Texte nutzen je (sort_order|created_at, id)."
  artifacts:
    - path: "backend/internal/repository/group_repository.go"
      provides: "Additive Cursor-Release-Liste"
      contains: "Cursor"
    - path: "backend/internal/handlers/group_contributors_handler.go"
      provides: "Cursor-Handler mit next_cursor/has_more"
      contains: "next_cursor"
  key_links:
    - from: "backend/internal/handlers/group_contributors_handler.go"
      to: "release-list / releases/:id/images / releases/:id/notes"
      via: "Cursor-Handler mit next_cursor/has_more"
      pattern: "next_cursor"
---

<objective>
Fuege cursor-basierte Pagination fuer genau die drei nachladenden Listen aus AO4-03 hinzu — additiv, ohne bestehende Offset-Endpunkte zu veraendern.

Purpose: Infinite Scroll + "Mehr laden" (AO4-11/12/18/19/25) braucht stabile Seek-Pagination. Offset-Umstellungen anderer Listen sind ausgeschlossen.

Output: Cursor-Query-Methoden (Release-Liste, Bilder, Texte), neue Handler/Routen, OpenAPI, api.ts-Funktionen und Cursor-Meta-Typen.
</objective>

<context>
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON4-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Cursor-Repository-Methoden + Test</name>
  <files>backend/internal/repository/group_repository.go, backend/internal/repository/release_detail_public_repository.go, backend/internal/repository/release_detail_cursor_test.go</files>
  <read_first>
    - `backend/internal/repository/group_repository.go:118-231` — bestehende Offset-`GetGroupReleases` und `buildReleasesWhere`; Sortierschluessel `ORDER BY CAST(e.episode_number AS INTEGER) ASC, rev.id ASC` (Zeile 172). NICHT veraendern, nur additiv erweitern.
    - `backend/internal/repository/release_detail_public_repository.go` (aus 99-07) — Bilder-/Texte-Reads, DTOs und Public-Gates, deren Cursor-Varianten hier ergaenzt werden.
    - `backend/internal/repository/anime_contributions_release_lookup_repository.go` — Muster fuer vollstaendig parametrisierte ($1/$2) Queries (SQL-Injection-Mitigation).
  </read_first>
  <action>
    Fuege drei Seek-Cursor-Reads hinzu (Cursor-Kodierung = Base64 des zusammengesetzten Schluessels, Claude's Discretion; Rueckgabe enthaelt neben den Items den `next_cursor` (nullable) und ein `has_more`-Flag — z. B. via eigenem Result-Struct pro Read):
    1. Release-Liste: neue Methode `GetGroupReleasesCursor(ctx, animeID, groupID, filter, cursor, limit)` in `group_repository.go`, additiv neben `GetGroupReleases`. WHERE reuse `buildReleasesWhere`; Seek-Bedingung `(CAST(e.episode_number AS INTEGER), rev.id) > (cursorEp, cursorId)` bei gleicher ORDER BY. `LIMIT limit+1` fuer has_more (Extra-Element nicht ausliefern, sondern nur als has_more-Signal + Cursor-Ursprung nutzen).
    2. Bildergalerie: `ListReleaseVersionImagesCursor(ctx, releaseVersionID, cursor, limit)` in `release_detail_public_repository.go`, Seek `(sort_order, id)`, gleiches Public-Gate wie 99-07.
    3. Textliste: `ListReleaseVersionNotesCursor(ctx, releaseVersionID, cursor, limit)`, Seek `(created_at, id)` oder `(sort_order, id)` — eindeutigen Schluessel waehlen, gleiches Public-Gate.
    Cursor-Encode/Decode-Helfer bereitstellen; ungueltiger/leerer Cursor startet bei der ersten Seite. Achte auf das 450-Zeilen-Limit (Cursor-Helfer ggf. in eigene Datei auslagern).

    Test `release_detail_cursor_test.go`: has_more/next_cursor-Semantik (bei `limit+1` Treffern ist has_more=true und das Extra-Element wird nicht ausgeliefert) sowie leere Liste liefert `has_more=false`, `next_cursor=null`; Round-Trip von Encode/Decode des Cursors.
  </action>
  <acceptance_criteria>
    - `GetGroupReleases` (Offset) bleibt unveraendert vorhanden; `GetGroupReleasesCursor`, `ListReleaseVersionImagesCursor`, `ListReleaseVersionNotesCursor` existieren zusaetzlich.
    - Jede Cursor-Methode liefert `next_cursor` und `has_more` (im Result-Struct).
    - `cd backend; go build ./...` fehlerfrei; `go test ./internal/repository -run "Cursor"` gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend; go build ./...</automated>
    <automated>cd C:/Users/admin/Documents/Team4s/backend; go test ./internal/repository -run "Cursor"</automated>
  </verify>
  <done>Drei Cursor-Repo-Methoden liefern Seek-Pagination mit next_cursor/has_more; Offset-Reads unangetastet.</done>
</task>

<task type="auto">
  <name>Task 1b: Cursor-Handler, Routen und OpenAPI-Contract</name>
  <files>backend/internal/handlers/group_contributors_handler.go, backend/cmd/server/main.go, shared/contracts/openapi.yaml</files>
  <read_first>
    - `backend/internal/handlers/group_contributors_handler.go` — Handler-Muster (parseAnimeID/parseGroupID, badRequest/internalError) + neue `GetGroupReleaseDetail`-Methode (99-07); Query-Param-Lesen via `c.Query`.
    - `backend/cmd/server/main.go:350-356` — Public-Route-Block `/anime/:id/group/:groupId/...`; neue `releases/:releaseVersionId`-Route aus 99-07 (Gin Literal-vor-Param beachten).
    - `shared/contracts/openapi.yaml` — bestehende Themes-/Release-Media-Pfade als Vorlage.
    - Die drei Cursor-Repo-Methoden aus Task 1a und deren Result-Structs.
  </read_first>
  <action>
    Ergaenze Handler-Methoden `GetGroupReleaseListCursor`, `GetGroupReleaseImages`, `GetGroupReleaseNotes` (parse `limit` mit Default/Max-Cap gegen Missbrauch, parse `cursor`; bei ungueltigem Cursor 400 oder stiller Neustart bei erster Seite). Verdrahte in `main.go` (falls `releaseDetailRepo` aus 99-07 noch nicht die Cursor-Methoden traegt: hier keine neue Repo-Konstruktion noetig, dieselbe Instanz nutzen) die drei Routen:
    - `v1.GET("/anime/:id/group/:groupId/release-list", ...)` (Cursor-Release-Liste, additiv; Offset-`/releases` bleibt).
    - `v1.GET("/anime/:id/group/:groupId/releases/:releaseVersionId/images", ...)`
    - `v1.GET("/anime/:id/group/:groupId/releases/:releaseVersionId/notes", ...)`
    Achte auf Gin-Literal-vor-Param-Reihenfolge, damit `/release-list` und `/releases/:releaseVersionId(/images|/notes)` kollisionsfrei matchen. OpenAPI: die drei neuen Pfade dokumentieren (query `cursor`, `limit`; response `items` + `next_cursor` + `has_more`).
  </action>
  <acceptance_criteria>
    - `main.go` enthaelt Routen `/release-list`, `/releases/:releaseVersionId/images`, `/releases/:releaseVersionId/notes`.
    - Jede Cursor-Handler-Response enthaelt die Felder `next_cursor` und `has_more`.
    - `openapi.yaml` dokumentiert die drei neuen Pfade mit `cursor`/`limit` und `next_cursor`/`has_more`.
    - `cd backend; go build ./...` fehlerfrei; `go vet ./internal/handlers ./cmd/server`.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend; go build ./...</automated>
    <automated>cd C:/Users/admin/Documents/Team4s/backend; go vet ./internal/handlers ./cmd/server</automated>
  </verify>
  <done>Drei Cursor-Routen registriert, Handler wired, Contract dokumentiert.</done>
</task>

<task type="auto">
  <name>Task 2: api.ts-Cursor-Funktionen und Meta-Typen</name>
  <files>frontend/src/lib/api.ts, frontend/src/types/releaseDetail.ts</files>
  <read_first>
    - `frontend/src/lib/api.ts:6083-6104` — `getGroupReleases` (Offset) als Struktur-Vorlage; bleibt unveraendert.
    - `frontend/src/types/releaseDetail.ts` (aus 99-07) — bestehende Item-Typen, die hier um eine generische Cursor-Page-Huelle ergaenzt werden.
  </read_first>
  <action>
    Ergaenze in `releaseDetail.ts` einen generischen Typ `CursorPage<T>` mit `items: T[]`, `next_cursor: string | null`, `has_more: boolean`. Fuege in `api.ts` drei neue Funktionen hinzu (Offset-`getGroupReleases` unveraendert lassen):
    - `getGroupReleaseListCursor(animeID, groupID, opts?: { cursor?: string; limit?: number })` → CursorPage von Release-Listeneintraegen.
    - `getGroupReleaseImages(animeID, groupID, releaseVersionID, opts?)` → CursorPage von Bildeintraegen.
    - `getGroupReleaseNotes(animeID, groupID, releaseVersionID, opts?)` → CursorPage von Texteintraegen.
    Query-Parameter `cursor`/`limit` nur setzen wenn vorhanden; Fehlerbehandlung wie `getGroupReleaseMedia`.
  </action>
  <acceptance_criteria>
    - `CursorPage<T>` mit `next_cursor`/`has_more`/`items` in `releaseDetail.ts` exportiert.
    - Drei Cursor-Funktionen in `api.ts` exportiert und rufen die neuen Pfade.
    - `getGroupReleases` (Offset) ist unveraendert vorhanden.
    - `cd frontend; npm run typecheck` fehlerfrei.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run typecheck</automated>
  </verify>
  <done>Cursor-Client-Funktionen und Meta-Typ bereit fuer Infinite Scroll.</done>
</task>

</tasks>

<verification>
- `go build`, `go vet`, `go test Cursor`, `npm run typecheck`.
- Backend nach Commit neu bauen; Offset-`releases/page.tsx` bleibt gruen (Regression pruefen).
</verification>

<success_criteria>
- Genau drei Cursor-Listen existieren; Offset-Endpunkte unveraendert; Meta liefert next_cursor/has_more.
</success_criteria>

<output>
Erstelle `.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-08-SUMMARY.md` nach Abschluss.
</output>
