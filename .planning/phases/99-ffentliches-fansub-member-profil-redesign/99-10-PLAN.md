---
phase: 99
plan: "10"
type: execute
wave: 7
depends_on: []
files_modified:
  - "frontend/src/app/anime/[id]/group/[groupId]/sections/HeroSection.tsx"
  - "frontend/src/components/groups/GroupAssetsExperience.tsx"
  - "frontend/src/components/groups/GroupEdgeNavigation.tsx"
  - "frontend/src/app/anime/[id]/group/[groupId]/page.module.css"
autonomous: true
requirements_addressed:
  - "AO4-08"
  - "AO4-09"
  - "AO4-10"
  - "AO4-14"
requirements:
  - "Phase 99 Add-on 4 AO4-08, AO4-09, AO4-10, AO4-14"
must_haves:
  truths:
    - "AO4-08: Der hartcodierte Text 'kein passender Subgroups-Ordner gefunden' erscheint nicht mehr im oeffentlichen UI (weder in HeroSection.tsx noch in GroupAssetsExperience.tsx); bei fehlendem Jellyfin-Ordner wird die Asset-Sektion ausgeblendet."
    - "AO4-14: Bei fehlendem Datensatz wird die Sektion ausgeblendet oder eine generische, nutzerverstaendliche Meldung gezeigt - nie technischer Text."
    - "AO4-09: Der Hero der Projektseite nutzt ein Verlaufs-Overlay statt Vollflaechen-Abdunklung; das Poster bleibt erkennbar; durchgehender Scroll."
    - "AO4-10: Die Projekt-Navigationspfeile sind mit 'Weitere Projekte von [Gruppe]' beschriftet statt unbeschriftet."
  artifacts:
    - path: "frontend/src/app/anime/[id]/group/[groupId]/sections/HeroSection.tsx"
      provides: "Kein Subgroups-Fehlertext, Asset-Sektion nur bei vorhandenen Assets"
    - path: "frontend/src/components/groups/GroupEdgeNavigation.tsx"
      provides: "Beschriftete Projekt-Navigation"
      contains: "Weitere Projekte von"
  key_links:
    - from: "frontend/src/app/anime/[id]/group/[groupId]/sections/HeroSection.tsx"
      to: "Asset-Sektion Sichtbarkeit"
      via: "Ausblenden statt Fehlertext bei !hasGroupFolder"
      pattern: "GroupAssetShowcase"
---

<objective>
Bereinige die Projektseite von geleakten technischen Fehlermeldungen (AO4-08/AO4-14), gib dem Hero ein Verlaufs-Overlay mit erkennbarem Poster (AO4-09) und beschrifte die Projekt-Navigation (AO4-10).

Purpose: Der oeffentliche Auftritt darf nie backend-nahe Texte zeigen; leere Bereiche werden ausgeblendet statt erklaert. Die Navigation soll verstaendlich sein.

Output: Angepasste HeroSection, GroupAssetsExperience, GroupEdgeNavigation und begleitendes CSS.
</objective>

<context>
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON4-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Technische Fehlermeldungen entfernen, Asset-Sektion ausblenden</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/sections/HeroSection.tsx, frontend/src/components/groups/GroupAssetsExperience.tsx</files>
  <read_first>
    - `frontend/src/app/anime/[id]/group/[groupId]/sections/HeroSection.tsx:48-152` — `hasGroupFolder`/`hasEpisodeAssets` und der Render-Zweig mit `styles.errorBox` (Zeilen 131-142) inkl. Text "kein passender Subgroups-Ordner gefunden" (:134).
    - `frontend/src/components/groups/GroupAssetsExperience.tsx:117-139` — zweite Kopie des Subgroups-Textes (:135) und der Episoden-Assets-Hinweis.
    - `backend/internal/handlers/group_assets_jellyfin.go:70` — Leerfall: Backend liefert still leeres Payload bei fehlendem Jellyfin-Ordner (kein Fehler).
  </read_first>
  <action>
    In `HeroSection.tsx`: entferne den Fehlertext-Zweig fuer `!hasGroupFolder` und `!hasEpisodeAssets`. Ersetze das Verhalten so, dass bei fehlendem Ordner ODER fehlenden Episoden-Assets die gesamte Asset-Sektion (`GroupAssetShowcase`) einfach nicht gerendert wird (Ausblenden statt Fehlertext). Ein echter Lade-/API-Fehler (`groupAssetsError`) darf weiterhin NICHT als technischer Text erscheinen; zeige entweder nichts oder eine generische, nutzerverstaendliche Meldung (kein Backend-/Jellyfin-Jargon, keine Wortteile "Subgroups-Ordner").

    In `GroupAssetsExperience.tsx`: entferne die zweite Kopie des Subgroups-Textes und den technischen Episoden-Assets-Hinweis (:134-139); bei Leerfall die betroffenen Bloecke ausblenden statt State-Box mit technischem Text. Korrekte Umlaute; keine neuen Farb-/CSS-Tokens.
  </action>
  <acceptance_criteria>
    - `grep -rn "Subgroups-Ordner" frontend/src` liefert KEINE Treffer mehr.
    - `HeroSection.tsx` rendert `GroupAssetShowcase` nur bei `hasGroupFolder && hasEpisodeAssets`; kein `errorBox` mit technischem Text im Leerfall.
    - `cd frontend; npm run typecheck` fehlerfrei.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s; rg -n "Subgroups-Ordner" frontend/src; if ($LASTEXITCODE -eq 0) { exit 1 } elseif ($LASTEXITCODE -eq 1) { exit 0 } else { exit $LASTEXITCODE }</automated>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run typecheck</automated>
  </verify>
  <done>Kein technischer Subgroups-Text mehr; Asset-Sektion blendet sich im Leerfall aus.</done>
</task>

<task type="auto">
  <name>Task 2: Hero-Verlaufs-Overlay und beschriftete Navigation</name>
  <files>frontend/src/components/groups/GroupEdgeNavigation.tsx, frontend/src/app/anime/[id]/group/[groupId]/page.module.css, frontend/src/app/anime/[id]/group/[groupId]/sections/HeroSection.tsx</files>
  <read_first>
    - `frontend/src/app/anime/[id]/group/[groupId]/page.module.css` — Hero-Klassen (`.hero`, `.heroWithBackdrop`, `.info`, `.poster`) und Overlay-Definition.
    - `frontend/src/app/anime/[id]/group/[groupId]/sections/HeroSection.tsx:120-128` — Render von `GroupEdgeNavigation` (bereits mit `group.fansub.name` im Scope).
    - `frontend/src/components/groups/GroupEdgeNavigation.tsx:28-116` — Props + unbeschriftete Chevron-Buttons (nur `aria-label`, kein sichtbares Label).
  </read_first>
  <action>
    AO4-09: Stelle sicher, dass der Hero-Backdrop ein Verlaufs-Overlay (linear-gradient) nutzt und keine flaechendeckende Abdunklung; das Poster (`.poster`) bleibt voll erkennbar. Falls das aktuelle Overlay bereits ein Verlauf ist, nur pruefen/leicht nachjustieren in `page.module.css` (keine neuen Farbwerte ausserhalb des Tokensystems; nutze vorhandene rgba-Overlay-Konvention). Kein separater "Tab"-Bruch; durchgehender Scroll.

    AO4-10: Ergaenze `GroupEdgeNavigation` um eine neue Prop `currentGroupName: string` und rendere ein sichtbares, dauerhaft angezeigtes Label "Weitere Projekte von {currentGroupName}" im Navigations-Overlay (zusaetzlich zu den bestehenden `aria-label`s). Reiche die Prop aus `HeroSection.tsx` mit `group.fansub.name` durch. Navigationsverhalten unveraendert. Korrekte Umlaute.
  </action>
  <acceptance_criteria>
    - `GroupEdgeNavigation.tsx` enthaelt den sichtbaren Text `Weitere Projekte von` und eine Prop `currentGroupName`.
    - `HeroSection.tsx` uebergibt `currentGroupName={group.fansub.name}`.
    - Hero-Overlay in `page.module.css` ist ein `linear-gradient` (keine Vollflaechen-Deckfarbe ueber dem Poster).
    - `cd frontend; npm run typecheck` fehlerfrei.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run typecheck</automated>
  </verify>
  <done>Hero zeigt Verlaufs-Overlay mit erkennbarem Poster; Navigation ist sichtbar beschriftet.</done>
</task>

</tasks>

<verification>
- Grep-Gate "Subgroups-Ordner" leer; `npm run typecheck`.
- Live-Check via :3000 in 99-14 (nach Edit `docker restart team4sv30-frontend` + Strg+F5).
</verification>

<success_criteria>
- Keine technische Fehlermeldung im oeffentlichen UI; Hero-Poster erkennbar; Navigation beschriftet.
</success_criteria>

<output>
Erstelle `.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-10-SUMMARY.md` nach Abschluss.
</output>
