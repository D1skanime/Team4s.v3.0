---
phase: 99
plan: "01"
type: execute
wave: 1
depends_on:
  - "99-00"
files_modified:
  - "backend/internal/models/member_profile.go"
  - "backend/internal/repository/member_profile_repository.go"
  - "backend/internal/repository/member_profile_repository_test.go"
  - "shared/contracts/openapi.yaml"
  - "frontend/src/types/profile.ts"
autonomous: true
requirements_addressed:
  - "D-03"
  - "D-04"
  - "D-05"
  - "D-08"
  - "D-09"
  - "D-10"
  - "D-14"
  - "D-15"
  - "D-17"
  - "D-18"
  - "D-19"
  - "A-01"
  - "A-02"
  - "A-04"
  - "A-05"
requirements:
  - "Phase 99 CONTEXT D-03-D-05"
  - "Phase 99 CONTEXT D-08-D-10"
  - "Phase 99 CONTEXT D-14-D-19"
  - "Phase 99 CONTEXT A-01/A-02/A-04/A-05"
must_haves:
  truths:
    - "GET /api/v1/members/:slug emits additive current_projects, latest_contributions, previous_contributions, and previous_contributions_count fields."
    - "Latest text contributions are public, published, non-empty release_version_notes rows only."
    - "Latest media contributions come from release_version_media/media_assets/media_files with real release_version_id and public/approved/ready filters."
  artifacts:
    - path: "backend/internal/models/member_profile.go"
      provides: "Public member profile DTO additions"
    - path: "backend/internal/repository/member_profile_repository.go"
      provides: "Public profile projection loaders"
    - path: "shared/contracts/openapi.yaml"
      provides: "Canonical public profile contract"
    - path: "frontend/src/types/profile.ts"
      provides: "Frontend DTO mirror"
  key_links:
    - from: "backend/internal/repository/member_profile_repository.go"
      to: "shared/contracts/openapi.yaml"
      via: "matching JSON field names"
      pattern: "current_projects|latest_contributions|previous_contributions"
---

<objective>
Extend the existing `GET /api/v1/members/:slug` public profile projection with contract-aligned additive DTO fields for current projects, latest public contributions, and collapsed previous contribution history.

Purpose: The public page must render real existing data without extra frontend fetch drift, example production content, a new endpoint family, or media ownership shortcuts.

Output: Backend models/repository, OpenAPI schema, frontend DTO types, and repository tests aligned around one additive public profile contract.
</objective>

<context>
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-CONTEXT.md
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-RESEARCH.md
@AGENTS.md
@docs/engineering/implementation-contract.md
@docs/api/api-contracts.md
@docs/architecture/db-schema-fansub-domain.md
</context>

<read_first>
- `backend/internal/models/member_profile.go` - current `PublicMemberProfile`, `MemberProfileRecentMedia`, `MemberProfileRecentContribution`, and `PublicMemberBadge` types.
- `backend/internal/repository/member_profile_repository.go` - `GetPublicMemberProfile`, `loadMemberships`, `loadPublicBadges`, `loadRecentMedia`, `loadRecentContributions`.
- `backend/internal/repository/anime_contributions_member_project_repository.go` - project/release-version aggregation pattern and `release_version_groups.fansub_group_id` usage.
- `backend/internal/repository/release_version_notes_repository.go` - note fields and deleted/status behavior.
- `backend/internal/repository/release_version_media_repository.go` and `backend/internal/repository/media_repository.go` - release-version-media ownership, ready file/status, visibility/review mappings.
- `database/migrations/0064_release_version_notes.up.sql`, `0059_release_version_media_schema.up.sql`, `0086_anime_contributions.up.sql`, `0091_anime_contributions_release_version.up.sql`, `0097_v12_status_foundation.up.sql`, `0105_anime_contributions_member_id.up.sql`, `0114_hist_roles_date_migration.up.sql`.
- `shared/contracts/openapi.yaml` - `PublicMemberProfileData`, `MemberProfileRecentMedia`, `MemberProfileRecentContribution`.
- `frontend/src/types/profile.ts` - frontend mirror DTOs.
- Wave-0 tests from Plan 99-00.
</read_first>

<tasks>
<task type="auto">
  <name>Task 1: Add additive public profile DTO contracts</name>
  <files>backend/internal/models/member_profile.go, shared/contracts/openapi.yaml, frontend/src/types/profile.ts</files>
  <action>
    Add explicit DTOs instead of overloading the old tab/timeline types.

    In Go, add public-profile DTO structs for:
    - `PublicMemberCurrentProject`: anime/project card data with `anime_id`, `anime_title`, optional `cover_url`, `fansub_group_id`, `fansub_group_name`, `roles`, `release_versions`, `is_project_level`, and period/status evidence fields needed by the UI.
    - `PublicMemberProjectReleaseVersion`: real `release_version_id`, label/version/title, episode number/title if available, and role labels for that version.
    - `PublicMemberLatestContribution`: discriminated `type` (`text` or `media`), `id`, `occurred_at`, `anime_id`, `anime_title`, real `release_version_id`, `release_version_label`, optional text preview/body HTML for text, optional `image_url`, `thumbnail_url`, `caption`, and category for media.
    - `PublicMemberPreviousContribution`: anime/group/role labels plus real period fields; no row may require displaying `ohne Jahr`.

    Add fields to `PublicMemberProfile`: `CurrentProjects []PublicMemberCurrentProject` with JSON `current_projects`, `LatestContributions []PublicMemberLatestContribution` with JSON `latest_contributions`, `PreviousContributions []PublicMemberPreviousContribution` with JSON `previous_contributions`, and `PreviousContributionsCount int` with JSON `previous_contributions_count`.

    Update `shared/contracts/openapi.yaml` in the same change with matching schemas and add the new fields to `PublicMemberProfileData`. Update `frontend/src/types/profile.ts` with equivalent TypeScript interfaces. Keep `recent_media` and `recent_contributions` additive/backward-compatible unless runtime code no longer emits them; do not change endpoint path or auth requirement. Do not update `shared/contracts/contributions.yaml` unless `/api/v1/members/:slug/contributions` behavior changes, which this plan should avoid.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s; rg -n "current_projects|latest_contributions|previous_contributions_count|PublicMemberLatestContribution" backend/internal/models/member_profile.go shared/contracts/openapi.yaml frontend/src/types/profile.ts</automated>
  </verify>
  <done>Backend model, OpenAPI schema, and frontend type definitions expose the same additive public profile fields and do not alter the separate contributions endpoint contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement repository projection with public filters and media ownership guards</name>
  <files>backend/internal/repository/member_profile_repository.go, backend/internal/repository/member_profile_repository_test.go</files>
  <action>
    Extend `GetPublicMemberProfile` to populate the new fields through focused private loader methods in `member_profile_repository.go`. Keep query code near the existing profile projection; do not introduce a parallel repository unless the file line-count pressure requires a small same-package helper file and the summary documents why.

    Current projects per D-04/D-05/A-01: derive from existing `anime_contributions` and `anime_contribution_roles` for the member, with `status='confirmed'` and `is_public_on_member_profile=true`. Treat active/current only through researched evidence from existing fields; research recommends `ended_year IS NULL` because no release-version status exists. Include real release-version rows through `release_versions`, `fansub_releases`, `episodes`, and `release_version_groups.fansub_group_id`. If a contribution is anime-wide with `release_version_id IS NULL`, return `is_project_level=true` and real aggregate/version list information; do not fabricate a release-version label.

    Latest contributions per D-08 through D-11/A-02/D-17: build one SQL-level union sorted by timestamp and `LIMIT 3`. Text source is `release_version_notes` filtered by `visibility='public'`, `status='published'`, `deleted_at IS NULL`, matching member, and usable non-empty `body_text` or `body_html`. Media source must use `release_version_media` joined to `media_assets` and `media_files`, require a real `release_version_id`, `rvm.deleted_at IS NULL`, public visibility via existing `visibilities`, approved review via `review_statuses.code='approved'`, `media_assets.status='ready'`, and at least one ready file variant. Do not read from or join `release_media` as a substitute. Use existing `publicURLForPath` URL normalization.

    Previous contributions per D-14/A-04: return only completed/historical project rows that have a real existing period/date field. Exclude rows with no period evidence from both entries and count; do not render or return a value that forces `ohne Jahr`. If the existing schema cannot identify previous/completed rows from period/status fields, stop and document the missing decision instead of adding a migration or status column.

    Memberships per D-03/A-05: keep using existing `loadMemberships`; do not add `LIMIT 1`. Confirm tests cover zero, one, and multiple memberships.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend; go test ./internal/repository -run "PublicMember(Profile|Latest|Previous|Current|Media)"</automated>
  </verify>
  <done>Repository tests from 99-00 pass for additive fields, current/previous semantics, latest public-only feed, media ownership, no `release_media` substitute, and multiple memberships.</done>
</task>

<task type="auto">
  <name>Task 3: Align handler serialization and contract checks</name>
  <files>backend/internal/models/member_profile.go, backend/internal/repository/member_profile_repository.go, shared/contracts/openapi.yaml, frontend/src/types/profile.ts</files>
  <action>
    Confirm `backend/internal/handlers/app_public_profile.go` can serialize the additive `PublicMemberProfile` fields without handler changes. Do not add mutation endpoints or authoring behavior. If the handler requires test updates, keep them within the existing public profile handler test file and only assert response shape for the new additive fields.

    Run contract drift checks by grepping the same field names in Go JSON tags, OpenAPI schema, and TypeScript types. Confirm frontend `getMemberProfile` in `frontend/src/lib/api.ts` does not need a path/signature/auth change; if no helper change is needed, leave it untouched. If runtime and contract disagree on a field name or enum, fix the stale side in this plan rather than teaching frontend components undocumented fallback shapes.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s; rg -n 'json:"current_projects|current_projects:|current_projects$|latest_contributions|previous_contributions_count' backend/internal/models/member_profile.go shared/contracts/openapi.yaml frontend/src/types/profile.ts</automated>
    <automated>cd C:/Users/admin/Documents/Team4s; git diff --check</automated>
  </verify>
  <done>Runtime DTO, OpenAPI, and frontend types are aligned; no new endpoint path, protected auth seam, upload flow, schema migration, or authoring surface was introduced.</done>
</task>
</tasks>

<verification>
- `cd backend; go test ./internal/repository -run "PublicMember(Profile|Latest|Previous|Current|Media)"`
- `cd backend; go test ./internal/handlers -run PublicMemberProfile` if handler tests are touched.
- `cd frontend; npm run typecheck`
- `git diff --check`
</verification>

<success_criteria>
- `GET /api/v1/members/:slug` returns real additive `current_projects`, `latest_contributions`, `previous_contributions`, and `previous_contributions_count` fields.
- Latest text rows are public + published + non-empty only.
- Latest media rows are public + approved + ready only, from `release_version_media` + `media_assets` + `media_files`, with real `release_version_id`.
- Previous rows have a real period/date and never force `ohne Jahr`.
- No schema migration is created; missing status/period semantics stop execution and are documented.
</success_criteria>

<notes>
This plan implements A-01, A-02, A-04, and A-05 using research evidence. Badge tier support A-03 is handled in frontend Plan 99-02 because research found no generic backend tier/level field. Deferred ideas remain out of scope.
</notes>
