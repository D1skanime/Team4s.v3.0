---
phase: 99
plan: "25"
type: execute
wave: 18
depends_on:
  - "99-24"
files_modified:
  - "frontend/src/components/fansubs/FansubMediaLightbox.tsx"
  - "frontend/src/components/fansubs/FansubMediaLightbox.module.css"
  - "frontend/src/components/fansubs/FansubGroupMediaBlock.tsx"
  - "frontend/src/components/fansubs/__tests__/FansubMediaLightbox.test.tsx"
autonomous: true
requirements_addressed:
  - "AO6-12"
requirements:
  - "Phase 99 Add-on 6 CONTEXT AO6-12 (Bild-Lightbox mit Weiter/Zurück, vollem Text, Tastatur, A11y)"
must_haves:
  truths:
    - "[AO6-12] Klick auf ein Medien-Thumbnail öffnet eine Lightbox (Client-Komponente), die das Originalbild (original_url) groß zeigt, mit vollem Titel + Beschreibung und einem Positions-Zähler 'n / N'."
    - "[AO6-12] Weiter/Zurück navigiert durch ALLE Medien der Gruppe (nicht nur die Vorschau); Tastatur: Esc schließt, ←/→ navigiert."
    - "[AO6-12] A11y: role='dialog' + aria-modal, Fokus-Falle und Fokus-Rückgabe an das auslösende Element; Weiter/Zurück-Buttons haben aria-labels."
  artifacts:
    - path: "frontend/src/components/fansubs/FansubMediaLightbox.tsx"
      provides: "Lightbox-Client-Komponente auf @/components/ui Modal, Originalbild + Navigation + Zähler + Tastatur"
      min_lines: 40
      contains: "aria-modal"
  key_links:
    - from: "frontend/src/components/fansubs/FansubGroupMediaBlock.tsx"
      to: "FansubMediaLightbox"
      via: "onSelect setzt aktiven Index → Lightbox öffnet mit vollständigem media[]"
      pattern: "FansubMediaLightbox"
    - from: "frontend/src/components/fansubs/FansubMediaLightbox.tsx"
      to: "original_url"
      via: "großes Originalbild aus resolveApiUrl(original_url)"
      pattern: "original_url"
---

<objective>
Bild-Lightbox (AO6-12): Klick auf ein Medien-Thumbnail öffnet eine barrierefreie Lightbox mit dem Originalbild, vollem Titel + Beschreibung, Positions-Zähler und Weiter/Zurück durch ALLE Medien der Gruppe. Tastatur (Esc/←/→), Fokus-Falle/-Rückgabe. Keine Backend-/Migrationsänderung (Felder liegen im DTO vor).

Purpose: Nutzer sollen Kontextmedien in voller Größe und mit vollem Text durchblättern können.
Output: Lightbox-Client-Komponente + Verdrahtung mit dem Medien-Grid + Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON6-CONTEXT.md
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-24-SUMMARY.md

<interfaces>
<!-- Aktueller Stand (read-only verifiziert). FansubGroupMediaBlock onSelect-Seam stammt aus 99-24. -->

frontend/src/types/fansub.ts — PublicFansubMediaItem { id; media_type; caption?; mime_type; thumbnail_url?; original_url?; title?; description?; category }.
FansubGroupMediaBlock.tsx (nach 99-24): 'use client', props { media; onSelect?(index) }, jedes Bild-Thumbnail ruft onSelect(globalIndex).

@/components/ui Modal (components/ui/Modal.tsx) — ModalProps { open: boolean; onClose: () => void; title: string; description?; children?; footer? }. Modal rendert bereits `<div role="dialog" aria-modal="true" aria-labelledby="ui-modal-title" onKeyDown={handleKeyDown}>`, behandelt Escape (onClose) und Tab-Fokus-Falle (getFocusableElements), fokussiert beim Öffnen den Close-Button und gibt Fokus beim Schließen an das zuvor fokussierte Element zurück (previouslyFocusedRef). Nutze Modal als Basis.
@/components/ui Button — variant primary/secondary/ghost/subtle/danger/success, size sm/md/lg. Für Weiter/Zurück variant ghost + aria-label.
Bild: next/image (unoptimized) oder <Image fill> in Frame; resolveApiUrl aus @/lib/api. Icons lucide-react ChevronLeft/ChevronRight.
CLAUDE.md: @/components/ui-Primitive Pflicht (Modal, Button), kein natives button; korrekte Umlaute; nur Team4s-Tokens.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FansubMediaLightbox — Originalbild, Navigation, Zähler, Tastatur, A11y (AO6-12)</name>
  <files>frontend/src/components/fansubs/FansubMediaLightbox.tsx, frontend/src/components/fansubs/FansubMediaLightbox.module.css, frontend/src/components/fansubs/__tests__/FansubMediaLightbox.test.tsx</files>
  <read_first>
    - frontend/src/components/ui/Modal.tsx (ModalProps, role/aria-modal, Escape/Tab-Handling, Fokus-Rückgabe)
    - frontend/src/components/fansubs/FansubGroupMediaBlock.tsx (onSelect-Seam aus 99-24)
    - frontend/src/types/fansub.ts:171-181
  </read_first>
  <behavior>
    - Bei open + gültigem Index zeigt die Lightbox das Originalbild aus resolveApiUrl(media[index].original_url), den vollen Titel und die volle Beschreibung sowie einen Zähler im Format 'n / N' (1-basiert).
    - Weiter/Zurück-Buttons ändern den Index; Navigation umfasst ALLE Einträge in media (Länge N). Verhalten an den Rändern deterministisch (Wrap-around ODER Deaktivierung am Rand — konsistent implementiert und getestet).
    - Tastatur: ArrowRight → nächstes, ArrowLeft → vorheriges; Escape schließt (über Modal onClose).
    - Markup trägt role='dialog' + aria-modal (via Modal); Weiter/Zurück-Buttons haben aria-label ('Nächstes Bild'/'Vorheriges Bild').
    - Bei geschlossenem Zustand (open=false) wird nichts gerendert.
  </behavior>
  <action>
    FansubMediaLightbox.tsx NEU ('use client'): props { media: PublicFansubMediaItem[]; index: number | null; onClose: () => void; onNavigate: (index: number) => void } ODER interner Index-State mit initialIndex — wähle die Variante, die die Fokus-Rückgabe/Steuerung am saubersten hält; empfohlen: gesteuert über index (null = geschlossen) + onNavigate. Baue die Lightbox auf @/components/ui Modal auf (open = index != null; onClose; title = aktueller Medientitel (title→caption→media_type)). Als children: einen Bild-Frame mit dem Originalbild (resolveApiUrl(original_url), next/image fill oder img, alt = Titel), die volle Beschreibung (description, ungeklemmt), den Positions-Zähler 'n / N', und zwei @/components/ui Button (variant ghost) mit ChevronLeft/ChevronRight + aria-label 'Vorheriges Bild'/'Nächstes Bild', die onNavigate(prev/next) aufrufen. Ergänze eine Tastatursteuerung für ArrowLeft/ArrowRight (useEffect mit window keydown, nur wenn geöffnet; Escape übernimmt Modal). Modal liefert role='dialog', aria-modal, Tab-Fokus-Falle und Fokus-Rückgabe — nicht neu erfinden. Randverhalten (Wrap oder Disable) konsistent umsetzen. Nur Team4s-Tokens; korrekte Umlaute.

    FansubMediaLightbox.module.css NEU: großer Bild-Frame (max-height Viewport-bezogen, object-fit contain), Navigations-/Zähler-Layout, Beschreibungs-Block. Nur Tokens.

    Test FansubMediaLightbox.test.tsx: (RED zuerst) mit media (N=3): open bei index=0 rendert Originalbild-URL, Titel, Beschreibung und Zähler '1 / 3'; Klick 'Nächstes Bild' ruft onNavigate mit 1; role='dialog'/aria-modal im Markup; bei index=null wird nichts gerendert; Rand-/Wrap-Verhalten wie implementiert testen.
  </action>
  <acceptance_criteria>
    - `grep -n "use client" frontend/src/components/fansubs/FansubMediaLightbox.tsx` vorhanden; `grep -n "original_url" frontend/src/components/fansubs/FansubMediaLightbox.tsx` vorhanden.
    - `grep -n "Modal" frontend/src/components/fansubs/FansubMediaLightbox.tsx` vorhanden (Basis-Primitive); `grep -Ev '^\s*//' frontend/src/components/fansubs/FansubMediaLightbox.tsx | grep -c "<button"` = 0.
    - `grep -nE "Nächstes Bild|Vorheriges Bild" frontend/src/components/fansubs/FansubMediaLightbox.tsx` vorhanden (aria-labels); Zähler-Format 'n / N' im Code.
    - `grep -nE "ArrowLeft|ArrowRight" frontend/src/components/fansubs/FansubMediaLightbox.tsx` vorhanden.
    - Datei ≤450 Zeilen; `cd frontend && npx vitest run src/components/fansubs/__tests__/FansubMediaLightbox.test.tsx` grün.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/fansubs/__tests__/FansubMediaLightbox.test.tsx</automated>
  </verify>
  <done>Lightbox zeigt Originalbild + vollen Text + Zähler, navigiert per Button und Tastatur durch alle Medien, aria-modal/Fokus-Falle via Modal; Tests grün.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lightbox mit dem Medien-Grid verdrahten (AO6-12)</name>
  <files>frontend/src/components/fansubs/FansubGroupMediaBlock.tsx, frontend/src/components/fansubs/__tests__/FansubMediaLightbox.test.tsx</files>
  <read_first>
    - frontend/src/components/fansubs/FansubGroupMediaBlock.tsx (nach 99-24, onSelect-Seam)
    - frontend/src/components/fansubs/FansubMediaLightbox.tsx (aus Task 1)
  </read_first>
  <behavior>
    - FansubGroupMediaBlock hält einen activeIndex-State (null = geschlossen); onSelect(index) setzt activeIndex; die Lightbox erhält das vollständige media[] und activeIndex und navigiert/schließt darüber.
    - Der Klick auf ein Thumbnail (auch eines, das erst nach 'Alle anzeigen' sichtbar ist) öffnet die Lightbox am korrekten globalen Index.
  </behavior>
  <action>
    FansubGroupMediaBlock.tsx: internen State activeIndex (number|null) ergänzen; die aus 99-24 vorhandene onSelect-Verdrahtung so nutzen, dass ein Thumbnail-Klick setActiveIndex(globalIndex) aufruft. Am Ende der Komponente `<FansubMediaLightbox media={media} index={activeIndex} onClose={() => setActiveIndex(null)} onNavigate={setActiveIndex} />` rendern. Sicherstellen, dass der übergebene Index sich auf das VOLLSTÄNDIGE media-Array bezieht (nicht auf die sichtbare Teilmenge), damit die Navigation alle Medien umfasst. Falls FansubGroupMediaBlock noch ein externes onSelect-Prop exponiert, das interne Öffnen der Lightbox als Default beibehalten.

    FansubMediaLightbox.test.tsx erweitern bzw. einen Integrationstest ergänzen (kann in derselben Datei bleiben): Rendern von FansubGroupMediaBlock mit >5 Medien, 'Alle anzeigen' klicken, ein spät sichtbares Thumbnail klicken → Lightbox öffnet mit dem korrekten Titel/Index dieses Mediums.
  </action>
  <acceptance_criteria>
    - `grep -n "FansubMediaLightbox" frontend/src/components/fansubs/FansubGroupMediaBlock.tsx` vorhanden; `grep -n "activeIndex" frontend/src/components/fansubs/FansubGroupMediaBlock.tsx` vorhanden.
    - `cd frontend && npx vitest run src/components/fansubs/__tests__/FansubMediaLightbox.test.tsx src/components/fansubs/__tests__/FansubGroupMediaBlock.test.tsx && npm run typecheck` grün.
    - FansubGroupMediaBlock.tsx ≤450 Zeilen (`awk 'END{exit(NR>450)}'`).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/fansubs && npm run typecheck</automated>
  </verify>
  <done>Thumbnail-Klick öffnet die Lightbox am korrekten globalen Index; Navigation umfasst alle Medien; Tests grün.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run typecheck` und `npx vitest run src/components/fansubs` grün.
- Live: `docker restart team4sv30-frontend`, harter Reload `/fansubs/c-subs` (:3000) — Klick auf ein Medien-Thumbnail öffnet die Lightbox mit Originalbild + vollem Titel/Beschreibung + Zähler; Weiter/Zurück und ←/→ blättern durch alle Medien; Esc schließt; Fokus kehrt zum Thumbnail zurück.
</verification>

<success_criteria>
- Lightbox zeigt Original + vollen Text + Zähler; Weiter/Zurück durch alle Medien; Esc/←/→; A11y (aria-modal, Fokus-Falle/-Rückgabe).
- Verdrahtet mit dem Medien-Grid am korrekten globalen Index.
</success_criteria>

<output>
Create `.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-25-SUMMARY.md` when done
</output>
