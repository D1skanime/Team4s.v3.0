---
phase: 99
plan: "18"
type: execute
wave: 14
depends_on:
  - "99-15"
  - "99-16"
  - "99-17"
files_modified:
  - "frontend/src/app/fansubs/[slug]/page.module.css"
  - "frontend/src/components/fansubs/FansubPublicSections.module.css"
  - "frontend/src/app/anime/[id]/group/[groupId]/page.module.css"
  - "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.module.css"
autonomous: false
requirements_addressed:
  - "AO5-01"
  - "AO5-02"
  - "AO5-03"
  - "AO5-04"
  - "AO5-05"
  - "AO5-06"
  - "AO5-07"
  - "AO5-08"
  - "AO4-01"
  - "AO4-14"
  - "AO4-21"
  - "AO4-22"
requirements:
  - "Phase 99 Add-on 5 Live-Akzeptanz + offene Add-on-4-Integration (ersetzt 99-14 als finaler UAT)"
must_haves:
  truths:
    - "AO5-01/AO5-05: Community-Links (Discord/IRC/Webseite/Twitter/X/GitHub) aus dem Fansub-Edit erscheinen live als Chips auf /fansubs/[slug]."
    - "AO5-02/AO5-06: Gruppen-Medien erscheinen live mit Thumbnail, Titel, Beschreibung und Typ-Tag; nur public/approved/nicht-gelöscht; soft-gelöschte Medien fehlen."
    - "AO5-03: Live-Reihenfolge ist Hero → Geschichte → Laufende Projekte → Team → Erfolge → Medien."
    - "AO5-04: Lange Gruppen-Geschichte ist live einklappbar/kürzbar (Umschalter funktioniert)."
    - "AO5-07: Kachelgrößen/Abstände live konsistent, keine überdimensionierten Texte; Mobile ≤390px ohne horizontalen Bruch."
    - "AO5-08: Typ-Tags und Link-Labels zeigen live die deutschen Enum-Labels; keine Roh-Codes, korrekte Umlaute."
    - "AO4-01/AO4-14/AO4-21/AO4-22: Offene Add-on-4-Integration bestätigt — konsistente Kennzahlen über die drei Seiten, keine technische Fehlermeldung, eingebettetes Release + Detailseite (Galerie/Textliste/OP-ED-Middle-Timeline), Infinite-Scroll nur an den definierten Stellen, Lazy/Skeleton + 'Mehr laden' ohne Layout-Sprung."
  artifacts:
    - path: "frontend/src/app/fansubs/[slug]/page.module.css"
      provides: "Scoped responsive Polish falls Live-UAT Layout-Probleme auf /fansubs/[slug] findet"
  key_links:
    - from: "/fansubs/[slug] + drei Add-on-4-Seiten"
      to: "Live-Browser-UAT :3000"
      via: "Mobile ≤390px + Desktop, echte Daten"
      pattern: "Mitglieder"
---

<objective>
Verifiziere Add-on 5 live auf /fansubs/[slug] UND die noch offene Add-on-4-Integration in einer gemeinsamen Live-UAT (Mobile ≤390px + Desktop), mit realen Daten auf :3000. Dieser Plan ERSETZT 99-14 als finalen UAT von Phase 99 — der Add-on-4-Integrationsscope aus 99-14 wird hier mitgeprüft; 99-14 wird nicht separat ausgeführt.

Purpose: Backend-DTO (99-15) und Frontend-Sektionen (99-16/99-17) müssen live zusammenspielen; zugleich ist die ganzheitliche Add-on-4-Integration (Kennzahlen-Konsistenz, kein Layout-Sprung, Release-Detailseite) noch nicht live abgenommen.

Output: Dokumentierte UAT-Ergebnisse mit Live-URLs, ggf. scoped CSS-Korrekturen, Nutzer-Freigabe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON5-CONTEXT.md
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-14-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend-Rebuild + automatisierte Regression + Live-API-Verifikation des Public-DTO</name>
  <files>frontend/src/app/fansubs/[slug]/page.module.css</files>
  <read_first>
    - .planning/phases/99-ffentliches-fansub-member-profil-redesign/99-15-SUMMARY.md, 99-16-SUMMARY.md, 99-17-SUMMARY.md — was tatsächlich geliefert wurde.
    - Memory-Hinweise: Backend ist Docker auf :8092 (neue Routen erst nach `docker compose up -d --build team4sv30-backend`); Live-API-Verifikation per Keycloak Direct-Grant-Token (csubs-leader/123, client team4s-frontend); echte Host-Ports via `docker ps` (aktuell KC/Backend teils abweichend von .env).
    - backend/internal/repository/fansub_repository.go (GetPublicProfileBySlug, listPublicFansubMedia) — Public-DTO-Quelle.
    - frontend/package.json — Skripte test, typecheck, lint.
  </read_first>
  <action>
    Baue das Backend neu (`docker compose up -d --build team4sv30-backend`), damit das erweiterte Public-DTO (community_links + Medien-Titel/Beschreibung/Kategorie + deleted_at-Filter) live ist. Führe die automatisierte Regression aus: `cd backend; go test ./internal/repository -run PublicProfile` und `cd frontend; npx vitest run src/app/fansubs src/components/fansubs src/lib/__tests__/fansub-labels.test.ts` plus `npm run typecheck` und `npm run lint`. Verifiziere per Live-API-Token (Keycloak Direct-Grant, siehe Memory) den Endpunkt `/api/v1/fansub-slugs/{slug}/public-profile` für eine reale Gruppe (z.B. c-subs): HTTP 200, Response enthält `community_links` (Array) und `media[]` mit `title`/`description`/`category`; ein bekanntes soft-gelöschtes Medium taucht NICHT auf. Dokumentiere echte Host-Ports (`docker ps`) und die verwendete slug/URL im Summary. Nur durch Add-on 5 verursachte Fehler beheben.
  </action>
  <acceptance_criteria>
    - `cd backend; go test ./internal/repository -run PublicProfile` grün.
    - `cd frontend; npm run typecheck` fehlerfrei und `npx vitest run src/app/fansubs src/components/fansubs src/lib/__tests__/fansub-labels.test.ts` grün.
    - Live-API-Antwort für die reale Gruppe enthält community_links und media[].category/title/description (Response-Auszug im Summary dokumentiert), soft-gelöschtes Medium fehlt.
    - Verwendete Host-Ports + slug/URL dokumentiert.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend; go test ./internal/repository -run PublicProfile</automated>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run typecheck; npx vitest run src/app/fansubs src/components/fansubs src/lib/__tests__/fansub-labels.test.ts</automated>
  </verify>
  <done>Backend neu gebaut; automatisierte Checks grün; Live-DTO liefert community_links + Medien-Felder, soft-gelöschte Medien ausgeblendet.</done>
</task>

<task type="auto">
  <name>Task 2: Live-Browser-UAT (Mobile ≤390px + Desktop) für Add-on 5 und offene Add-on-4-Integration</name>
  <files>frontend/src/app/fansubs/[slug]/page.module.css, frontend/src/components/fansubs/FansubPublicSections.module.css, frontend/src/app/anime/[id]/group/[groupId]/page.module.css, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.module.css</files>
  <read_first>
    - Memory-Hinweis Live-Testing: :3000 = Dev (Turbopack), HMR greift NICHT → nach jeder Frontend-Edit `docker restart team4sv30-frontend` + Strg+F5 (kein --build); echte Host-Ports via `docker ps`.
    - .planning/phases/99-ffentliches-fansub-member-profil-redesign/99-14-PLAN.md — der Add-on-4-Integrationsscope, der hier mitgeprüft wird.
    - .planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON5-CONTEXT.md (Success Criteria :82-93).
    - Die /fansubs/[slug]-Komposition aus 99-17 und die Sektionen aus 99-16.
  </read_first>
  <action>
    Nutze den Dev-Server auf :3000 (nach Backend-Rebuild + `docker restart team4sv30-frontend` + harter Reload). Gib die exakten Live-URLs im Summary an.

    Add-on 5 auf /fansubs/[slug] (reale Gruppe mit Community-Links + Medien, z.B. c-subs), bei ≤390px und Desktop prüfen:
    - Reihenfolge Hero → Geschichte → Laufende Projekte → Team → Erfolge → Medien;
    - Community-Links-Sektion zeigt Chips mit deutschen Labels (Discord, IRC, Webseite, Twitter/X, GitHub) + optionalem Namen, externe Links öffnen im neuen Tab;
    - Medien-Sektion zeigt Thumbnail, Titel, Beschreibung, deutschen Typ-Tag; kein Layout-Sprung beim Laden (Skeleton/feste Aspect-Ratio, lazy);
    - Geschichte einklappbar (Umschalter 'Mehr anzeigen'/'Weniger anzeigen' funktioniert);
    - konsistente Kacheln/Abstände, keine überdimensionierten Texte, korrekte Umlaute, keine technische Fehlermeldung, kein horizontaler Overflow.

    Offene Add-on-4-Integration auf den drei Seiten (Fansub-Gruppenseite, Projektseite `/anime/[id]/group/[groupId]`, Release-Detailseite `/anime/[id]/group/[groupId]/releases/[releaseVersionId]`):
    - Mitglieder-/Release-/Projekt-Kennzahlen konsistent über die Seiten, kein '0 Mitglieder' bei realem aktivem öffentlichem Mitglied;
    - keine technische Fehlermeldung (kein Backend-Jargon);
    - eingebettetes neuestes Release mit Vorschau + Detail-Link; Detailseite mit Galerie (Typ-Tag+Autor), Textliste, OP/ED/Middle-Timeline ohne Player;
    - Infinite Scroll nur an den definierten Stellen + 'Mehr laden' funktionieren, kein Layout-Sprung beim Nachladen.

    Bei Layout-Problemen ausschließlich scoped CSS-Edits in den gelisteten page.module.css / FansubPublicSections.module.css (keine Redesigns, keine neuen Tokens/Farben, keine Contract-Änderungen). Nach jeder CSS-Edit `docker restart team4sv30-frontend` + harter Reload und erneut prüfen.
  </action>
  <acceptance_criteria>
    - /fansubs/[slug]: Add-5-Kriterien (Reihenfolge, Community-Links, Medien mit Titel/Beschreibung/Typ-Tag, Clamp, deutsche Labels) live bestätigt auf Desktop UND ≤390px (dokumentiert mit URLs + Notizen/Screenshots).
    - Add-on-4-Integration: Kennzahlen konsistent, kein '0 Mitglieder' bei realem Mitglied, keine technische Fehlermeldung, Release-Einbettung + Detailseite (Galerie/Textliste/Timeline) vorhanden, Nachladen ohne Layout-Sprung.
    - Kein horizontaler Overflow bei ≤390px auf allen vier Seiten.
    - Etwaige Korrekturen ausschließlich in den gelisteten CSS-Dateien; `cd frontend; npm run typecheck` bleibt fehlerfrei.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run typecheck</automated>
  </verify>
  <done>Live-UAT für Add-on 5 und offene Add-on-4-Integration bestanden oder scoped CSS-Korrekturen umgesetzt und re-verifiziert.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Nutzer-Freigabe (finaler Phase-99-UAT, ersetzt 99-14)</name>
  <files>frontend/src/app/fansubs/[slug]/page.module.css</files>
  <read_first>
    - Die in Task 2 dokumentierten Live-URLs.
  </read_first>
  <action>Pausiere nach der automatisierten Live-Verifikation, damit der Nutzer /fansubs/[slug] und die drei Add-on-4-Seiten live im Browser prüft. Markiere Add-on 5 (und die offene Add-on-4-Integration) erst als abgeschlossen, wenn der Nutzer freigibt oder die gemeldeten scoped Probleme behoben sind. Dieser Checkpoint ersetzt den 99-14-Checkpoint.</action>
  <what-built>/fansubs/[slug] wurde vervollständigt und poliert: Community-Links-Sektion + Medien-Sektion (Titel/Beschreibung/Typ-Tag, lazy/skeleton), neue Reihenfolge (Hero → Geschichte → Projekte → Team → Erfolge → Medien), einklappbare Geschichte, konsistente Kacheln, deutsche Enum-Labels. Zusätzlich wurde die offene Add-on-4-Integration (Kennzahlen-Konsistenz, Release-Detailseite, Nachladen ohne Layout-Sprung) über die drei Seiten mitgeprüft.</what-built>
  <how-to-verify>
    1. Öffne die vom Executor genannten Live-URLs auf :3000 (nach `docker restart team4sv30-frontend` + Strg+F5).
    2. /fansubs/[slug]: Prüfe Reihenfolge Hero → Geschichte → Projekte → Team → Erfolge → Medien; Community-Links als Chips mit deutschen Labels + externem Öffnen; Medien mit Thumbnail/Titel/Beschreibung/Typ-Tag ohne Layout-Sprung; Geschichte-Umschalter; konsistente Kacheln; korrekte Umlaute; keine technische Fehlermeldung.
    3. Prüfe auf Mobilbreite (≤390px) keinen horizontalen Bruch.
    4. Add-on 4: Konsistente Mitgliederzahlen über die drei Seiten (kein '0' bei realem Mitglied), eingebettetes neuestes Release mit Detail-Link, Detailseite mit Galerie/Textliste/OP-ED-Middle-Timeline, Nachladen (Scroll + 'Mehr laden') ohne Layout-Sprung.
  </how-to-verify>
  <acceptance_criteria>
    - Nutzer gibt frei ("approved") oder alle gemeldeten Punkte sind in scoped CSS-Dateien behoben und re-verifiziert.
  </acceptance_criteria>
  <resume-signal>Schreibe "approved" oder beschreibe die konkreten Probleme.</resume-signal>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run typecheck</automated>
  </verify>
  <done>Nutzer bestätigt /fansubs/[slug] + Add-on-4-Integration live oder alle gemeldeten Punkte sind behoben.</done>
</task>

</tasks>

<verification>
- Backend-Rebuild + `go test ./internal/repository -run PublicProfile` + Frontend-Vitest + `npm run typecheck` + `npm run lint`.
- Live-API-Token-Check des Public-DTO (community_links + Medien-Felder + soft-delete).
- Live-UAT :3000 Mobile ≤390px + Desktop für /fansubs/[slug] und die drei Add-on-4-Seiten.
</verification>

<success_criteria>
- Alle Add-on-5-Success-Criteria live erfüllt; offene Add-on-4-Integration bestätigt; Nutzer-Freigabe erteilt (ersetzt 99-14).
</success_criteria>

<output>
Erstelle `.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-18-SUMMARY.md` nach Abschluss. Vermerke darin, dass dieser Plan 99-14 als finalen UAT ersetzt.
</output>
