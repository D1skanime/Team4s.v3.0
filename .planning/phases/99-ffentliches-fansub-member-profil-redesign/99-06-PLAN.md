---
phase: 99
plan: "06"
type: execute
wave: 5
depends_on: []
files_modified:
  - "backend/internal/repository/group_themes_repository.go"
  - "backend/internal/repository/group_themes_repository_test.go"
  - "frontend/src/types/groupContributors.ts"
  - "shared/contracts/openapi.yaml"
autonomous: true
requirements_addressed:
  - "AO4-04"
requirements:
  - "Phase 99 Add-on 4 AO4-04"
must_haves:
  truths:
    - "AO4-04: Das Public-DTO PublicGroupTheme liefert pro Theme die OP/ED/Middle-Zeitcodes (start_time/end_time) aus theme_segments, ohne Berechnung und ohne hartkodierte Platzhalter."
    - "AO4-04: Fehlen Zeitcodes in der DB, sind die Felder null (kein Fake-Wert), und die vorhandene Thumbnail-/Typ-Ausgabe bleibt unveraendert."
  artifacts:
    - path: "backend/internal/repository/group_themes_repository.go"
      provides: "PublicGroupTheme mit Zeitcode-Feldern + Join auf theme_segments"
      contains: "start_time"
  key_links:
    - from: "backend/internal/repository/group_themes_repository.go"
      to: "theme_segments.start_time/end_time"
      via: "LEFT JOIN theme_segments ts ON ts.theme_id = t.id"
      pattern: "theme_segments"
---

<objective>
Nimm die bereits in der DB vorhandenen OP/ED/Middle-Zeitcodes in das oeffentliche Themes-DTO auf. Der Public-Themes-Read liefert bislang nur Thumbnails, aber keine Zeitcodes.

Purpose: Die neue Release-Detailseite (AO4-20) zeigt eine OP/ED/Middle-Timeline mit Vorschaubild + Zeitcode + Typ-Tag, ohne Video-Player. Die Zeitcodes muessen aus `theme_segments.start_time`/`end_time` durchgereicht werden.

Output: Erweitertes `PublicGroupTheme`-DTO, angepasste Query, gespiegelter TS-Typ und OpenAPI-Eintrag.
</objective>

<context>
@.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-ADDON4-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PublicGroupTheme-DTO um Zeitcodes erweitern und Query joinen</name>
  <files>backend/internal/repository/group_themes_repository.go, backend/internal/repository/group_themes_repository_test.go</files>
  <read_first>
    - `backend/internal/repository/group_themes_repository.go` — DTO `PublicGroupTheme:34`, Query + Mapping `GetPublicGroupThemes:49-136` (Mapping `opening→OP`/`ending→ED`/`insert_song→MIDDLE`).
    - `database/migrations/0049_extend_theme_segments.up.sql` — `theme_segments` hat `theme_id`, `fansub_group_id`, `version`, `start_time`/`end_time` (Typ `interval`).
    - `backend/internal/repository/theme_segment_render_cache.go:223-224` — Referenz-Join `FROM theme_segments ts JOIN themes t ON t.id = ts.theme_id`.
    - Interval-Handling: Interval-Spalten muessen per `::text`-Cast gescannt werden (Phase-24-Muster), damit Go einen `HH:MM:SS`-String erhaelt.
    - `backend/internal/repository/group_contributors_repository_test.go:92-103` — vorhandenes Empty-Result-Test-Muster fuer `GetPublicGroupThemes` (als Vorlage; der neue Test gehoert aber in die themes-eigene Test-Datei).
  </read_first>
  <action>
    Ergaenze `PublicGroupTheme` um zwei nullable String-Felder `StartTime *string` (json `start_time`) und `EndTime *string` (json `end_time`). Erweitere die Query in `GetPublicGroupThemes` um einen `LEFT JOIN theme_segments ts ON ts.theme_id = t.id AND ts.fansub_group_id = $2` und selektiere `ts.start_time::text` sowie `ts.end_time::text`. Scanne beide in `sql.NullString`/`*string`. Setze die Felder pro Theme genau einmal (beim ersten Anlegen des Theme-Eintrags im `themeIndex`-Aggregat), nicht pro Asset-Zeile. Wenn kein Segment existiert, bleiben die Felder nil. Keine Berechnung, kein Default-Wert, kein Video-Player. Die bestehende Sichtbarkeits-Gate-Logik (ma.status='ready', v.name='public', rs.code='approved') und die Assets/Typ-Ausgabe bleiben unveraendert. Achte auf das 450-Zeilen-Limit; bei Bedarf Hilfsfunktion auslagern.

    Lege den Test in `backend/internal/repository/group_themes_repository_test.go` an (neue Datei, passend zur Logik-Datei `group_themes_repository.go`). Assertion (Source- oder DB-basiert): die Themes-Query joint `theme_segments` und `start_time` kommt im DTO-Scan vor; der Empty-Result-Fall bleibt fehlerfrei.
  </action>
  <acceptance_criteria>
    - `PublicGroupTheme` enthaelt exportierte Felder `StartTime *string` und `EndTime *string` mit json-Tags `start_time`/`end_time`.
    - Query enthaelt `LEFT JOIN theme_segments ts` und `ts.start_time::text`.
    - Der neue Test liegt in `backend/internal/repository/group_themes_repository_test.go` (nicht in group_contributors_repository_test.go).
    - `cd backend; go build ./...` fehlerfrei; `go test ./internal/repository -run "GetPublicGroupThemes"` gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend; go build ./...</automated>
    <automated>cd C:/Users/admin/Documents/Team4s/backend; go test ./internal/repository -run "GetPublicGroupThemes"</automated>
  </verify>
  <done>PublicGroupTheme liefert Zeitcodes aus theme_segments oder null; Test in themes-eigener Datei.</done>
</task>

<task type="auto">
  <name>Task 2: TS-Typ und OpenAPI-Contract spiegeln</name>
  <files>frontend/src/types/groupContributors.ts, shared/contracts/openapi.yaml</files>
  <read_first>
    - `frontend/src/types/groupContributors.ts` — bestehender `GroupThemesResponse`/Theme-Typ (Felder `id`, `type`, `title`, `assets`).
    - `shared/contracts/openapi.yaml` — bestehendes Schema fuer das Themes-Response (Pfad `/anime/{id}/group/{groupId}/themes`); ergaenze dort die neuen Felder analog zu bestehenden Themes-Feldern.
  </read_first>
  <action>
    Ergaenze im Theme-Typ in `groupContributors.ts` die optionalen Felder `start_time: string | null` und `end_time: string | null` passend zum Backend-DTO. Aktualisiere im OpenAPI-Contract das Theme-Schema um dieselben zwei nullable String-Felder (`start_time`, `end_time`) mit deutscher Beschreibung (z. B. "Startzeit des Segments im HH:MM:SS-Format, null wenn nicht hinterlegt"). Keine Aenderung an `getGroupThemes` in `api.ts` noetig (Funktion existiert bereits, gibt das Response-Objekt durch).
  </action>
  <acceptance_criteria>
    - Theme-Typ in `groupContributors.ts` enthaelt `start_time: string | null` und `end_time: string | null`.
    - `openapi.yaml` Themes-Schema enthaelt `start_time` und `end_time`.
    - `cd frontend; npm run typecheck` fehlerfrei.
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend; npm run typecheck</automated>
  </verify>
  <done>Frontend-Typ und Contract spiegeln die Zeitcode-Felder.</done>
</task>

</tasks>

<verification>
- `go build`, `go test` Themes, `npm run typecheck`.
- Backend nach Commit neu bauen (`docker compose up -d --build team4sv30-backend`).
</verification>

<success_criteria>
- Zeitcodes liegen im Public-Themes-Payload vor (oder null), bereit fuer die Release-Detail-Timeline (AO4-20).
</success_criteria>

<output>
Erstelle `.planning/phases/99-ffentliches-fansub-member-profil-redesign/99-06-SUMMARY.md` nach Abschluss.
</output>
