---
phase: 22-anime-edit-on-create-flow-foundation
plan: 01
type: frontend
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/admin/anime/create/page.tsx
  - frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts
  - frontend/src/app/admin/anime/create/createPageHelpers.ts
  - frontend/src/app/admin/anime/components/AnimeEditPage/AdminAnimeEditPageClient.tsx
  - frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx
  - frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts
  - frontend/src/types/admin.ts
autonomous: true
requirements:
  - P22-SC1
  - P22-SC2
must_haves:
  truths:
    - "/admin/anime/create and /admin/anime/[id]/edit render through one shared editor foundation rather than two divergent main form layouts."
    - "The shared foundation can hydrate existing anime data into editable create-style sections."
    - "The replacement does not regress the modern create route while enabling edit prefill."
---

<objective>
Define and introduce the shared create/edit workspace foundation that will become the primary editable anime surface.

Purpose: edit should stop being its own stale product. The shared foundation must carry the modern create experience into edit without breaking create-specific behavior.
Output: extracted shared workspace primitives, explicit mode handling, and edit-prefill support.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-anime-edit-on-create-flow-foundation/22-CONTEXT.md

Read these implementation references before editing:
@frontend/src/app/admin/anime/create/page.tsx
@frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts
@frontend/src/app/admin/anime/create/createPageHelpers.ts
@frontend/src/app/admin/anime/components/AnimeEditPage/AdminAnimeEditPageClient.tsx
@frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx
@frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts
@frontend/src/types/admin.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract a shared anime editor foundation from the create route</name>
  <read_first>
    - frontend/src/app/admin/anime/create/page.tsx
    - frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts
    - frontend/src/app/admin/anime/create/createPageHelpers.ts
    - frontend/src/app/admin/anime/components/AnimeEditPage/AdminAnimeEditPageClient.tsx
  </read_first>
  <action>
    Extract the create route's main editable structure into shared building blocks that can operate in `create` and `edit` modes. Preserve the existing section order and modern Phase-17 page model: source/context first, assets second, details third, and final review/save last. Introduce explicit mode props so create can keep create-only copy and submission behavior while edit can reuse the same structural foundation.
  </action>
  <acceptance_criteria>
    - frontend/src/app/admin/anime/create/page.tsx no longer contains the only copy of the main editable section layout
    - a shared create/edit workspace component or component set exists and is imported by the create route
    - the shared workspace exposes an explicit create-vs-edit mode instead of inferring route behavior from ad hoc conditions
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add edit-prefill support to the shared draft/state model</name>
  <read_first>
    - frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts
    - frontend/src/types/admin.ts
    - frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx
  </read_first>
  <action>
    Extend the shared draft/state model so the editor foundation can open from an existing anime record, not just from a blank create draft. The hydrated edit state must include title, title_de, title_en, type, content_type, status, year, max_episodes, description, genres, tags where supported, persisted assets, source, and folder_name. Keep the data shape explicit so edit mode does not need a second independent form state tree.
  </action>
  <acceptance_criteria>
    - frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts or a new shared draft helper contains a code path for hydrating an existing anime into the shared editor state
    - the edit route can provide existing anime values to the shared workspace without relying on the old AnimeEditWorkspace form fields
    - frontend/src/types/admin.ts contains any new shared editor-mode or hydration types required by both create and edit
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Keep create behavior intact while the shared foundation is introduced</name>
  <read_first>
    - frontend/src/app/admin/anime/create/page.tsx
    - frontend/src/app/admin/anime/create/createPageHelpers.ts
    - .planning/phases/17-anime-create-ux-ui-follow-through/17-05-PLAN.md
  </read_first>
  <action>
    Ensure the create route continues to behave as Phase 17 defined while moving onto the shared foundation. `Anime erstellen` remains the final create action, AniSearch stays the metadata source intake, Jellyfin stays the folder/source matcher first, and create-only redirect/success behavior remains create-only rather than leaking into edit mode.
  </action>
  <acceptance_criteria>
    - create route copy still contains `Anime erstellen`
    - create route still renders the AniSearch and Jellyfin create surfaces in the shared workspace
    - create redirect/success helpers remain available for create mode and are not required by edit mode
  </acceptance_criteria>
</task>

</tasks>

<verification>
Wave 1 is complete when a shared anime editor foundation exists, edit can hydrate into it, and create still follows the shipped Phase-17 contract.
</verification>

<success_criteria>
- Create and edit now share one core editing foundation.
- Existing anime data can be loaded into that foundation for edit mode.
- Create behavior remains stable while the shared foundation lands.
</success_criteria>

<output>
After completion, create `.planning/phases/22-anime-edit-on-create-flow-foundation/22-01-SUMMARY.md`.
</output>
