---
phase: 22-anime-edit-on-create-flow-foundation
plan: 02
type: fullstack
wave: 2
depends_on:
  - 22-01-PLAN.md
files_modified:
  - frontend/src/app/admin/anime/[id]/edit/page.tsx
  - frontend/src/app/admin/anime/components/AnimeEditPage/AdminAnimeEditPageClient.tsx
  - frontend/src/app/admin/anime/components/AnimeEditPage/AniSearchEnrichmentSection.tsx
  - frontend/src/app/admin/anime/hooks/useAniSearchEditEnrichment.ts
  - frontend/src/app/admin/anime/hooks/useJellyfinSync.ts
  - frontend/src/app/admin/anime/hooks/useAnimePatch.ts
  - backend/internal/handlers/admin_content_anime.go
autonomous: true
requirements:
  - P22-SC2
  - P22-SC3
  - P22-SC4
must_haves:
  truths:
    - "Edit mode exposes all key anime fields and assets through the shared workspace."
    - "AniSearch identity is visible but guarded once linked."
    - "Jellyfin relink/resync remains explicit and does not silently overwrite manual authority."
---

<objective>
Wire edit-specific identity and save rules onto the shared anime editor foundation.

Purpose: the shared UI is only correct if edit mode obeys the right identity rules. Existing anime must be editable from one consistent surface without allowing accidental AniSearch remapping or silent Jellyfin takeover.
Output: edit route on the shared workspace, guarded AniSearch identity display, and explicit Jellyfin relink/resync behavior.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-anime-edit-on-create-flow-foundation/22-CONTEXT.md

Read these implementation references before editing:
@frontend/src/app/admin/anime/[id]/edit/page.tsx
@frontend/src/app/admin/anime/components/AnimeEditPage/AdminAnimeEditPageClient.tsx
@frontend/src/app/admin/anime/components/AnimeEditPage/AniSearchEnrichmentSection.tsx
@frontend/src/app/admin/anime/hooks/useAniSearchEditEnrichment.ts
@frontend/src/app/admin/anime/hooks/useJellyfinSync.ts
@frontend/src/app/admin/anime/hooks/useAnimePatch.ts
@backend/internal/handlers/admin_content_anime.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount the edit route on the shared create-style workspace</name>
  <read_first>
    - frontend/src/app/admin/anime/[id]/edit/page.tsx
    - frontend/src/app/admin/anime/components/AnimeEditPage/AdminAnimeEditPageClient.tsx
    - frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx
  </read_first>
  <action>
    Replace the old edit-route main form mount with the shared create-style workspace from plan 22-01. Keep page-level navigation such as breadcrumbs and episode links if useful, but the primary editable content must now be the shared sectioned workspace rather than the old standalone `AnimeEditWorkspace` layout.
  </action>
  <acceptance_criteria>
    - frontend/src/app/admin/anime/components/AnimeEditPage/AdminAnimeEditPageClient.tsx no longer mounts the old standalone edit workspace as the primary edit surface
    - `/admin/anime/[id]/edit` renders the shared workspace component in edit mode
    - the edit route still exposes navigation back to episodes or public view where those links already existed
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Guard AniSearch identity on edit once a source is already linked</name>
  <read_first>
    - frontend/src/app/admin/anime/components/AnimeEditPage/AniSearchEnrichmentSection.tsx
    - frontend/src/app/admin/anime/hooks/useAniSearchEditEnrichment.ts
    - backend/internal/handlers/admin_content_anime.go
    - .planning/phases/22-anime-edit-on-create-flow-foundation/22-CONTEXT.md
  </read_first>
  <action>
    Change the edit AniSearch UX so an already linked AniSearch identity is displayed clearly but not treated as a freeform remapping field. The UI may still show provenance or allow refresh/reload semantics if supported, but it must not encourage changing an existing anime from `anisearch:{old}` to an unrelated `anisearch:{new}` through the normal edit flow without an explicit protected path.
  </action>
  <acceptance_criteria>
    - the edit route displays AniSearch linkage/provenance when present
    - edit mode does not render an unrestricted AniSearch-ID rewrite field for already linked anime
    - any save or enrichment path touched by the phase does not silently replace an existing AniSearch source with a different ID during normal editing
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Keep Jellyfin relink and resync explicit and operator-safe</name>
  <read_first>
    - frontend/src/app/admin/anime/hooks/useJellyfinSync.ts
    - frontend/src/app/admin/anime/components/AnimeEditPage/AdminAnimeEditPageClient.tsx
    - frontend/src/app/admin/anime/create/CreateJellyfinCard.tsx
    - frontend/src/app/admin/anime/hooks/useAnimePatch.ts
  </read_first>
  <action>
    Reuse or adapt the modern Jellyfin search/adopt patterns on edit so admins can explicitly re-search a Jellyfin series, relink the anime to a new Jellyfin source, and run resync actions from the shared workspace. Preserve the current product rule that manual values remain authoritative: Jellyfin actions may refresh suggestions and linkage context, but they must not silently wipe curated metadata or assets.
  </action>
  <acceptance_criteria>
    - edit mode contains a visible Jellyfin action path for search, relink, or resync
    - Jellyfin actions in edit are explicit button-driven actions rather than automatic background replacement
    - no touched save/sync path overwrites manual values without an operator-triggered action
  </acceptance_criteria>
</task>

</tasks>

<verification>
Wave 2 is complete when edit mode runs on the shared workspace and enforces the intended AniSearch-fixed / Jellyfin-resyncable identity model.
</verification>

<success_criteria>
- Edit uses the shared foundation and can save existing anime changes from one surface.
- AniSearch identity is visible and guarded once linked.
- Jellyfin relink/resync stays explicit and operator-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/22-anime-edit-on-create-flow-foundation/22-02-SUMMARY.md`.
</output>
