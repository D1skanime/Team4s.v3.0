---
phase: 106-medienkern-schema-legacy-abbau
plan: 08
type: execute
wave: 4
depends_on: [106-01, 106-02, 106-03, 106-04, 106-05, 106-06, 106-07]
files_modified: []
autonomous: false
requirements: [SC3, SC4]
must_haves:
  truths:
    - "SC4: `go build ./...` + `go vet ./...` sind gruen und die grep-Suite (`media-core-legacy-grep.ps1`) meldet KEINE verbleibenden Referenzen auf entfernte Symbole/Routen. Die Suite scopet `release_media` auf NON-TEST-Live-Code (die 6 intentional retained Test-Guards sind allowlisted) — kein False-Fail auf member_profile/theme_segment_render_cache/segment_validation/app_auth/group_contributors/runtime_authority-Tests."
    - "SC3: Die vollstaendige Migrationskette 1→n laeuft auf einer leeren DB fehlerfrei durch."
    - "SC3: `media-core-contract-check.ps1 -FailOnContractGaps` belegt gegen die frische DB: `media`/`media_variant` vorhanden, `release_media`/`anime.cover_image` abwesend, `media_assets`/`media_files` weiterhin vorhanden. (Ausnahme: bei 106-05 Endpoint-Option A bleibt `release_media` bewusst bestehen — dann meldet 106-05 zurueck und `release_media` wird NICHT als MUSS-abwesend gefuehrt.)"
    - "SC2 (D-03 live): Nach Backend-Rebuild liefert die alte `/covers/<file>`-URL am Dev-Server keinen Handler mehr (404)."
  artifacts: []
  key_links:
    - from: "scripts/media-core-contract-check.ps1"
      to: "frische DB nach Kettenlauf"
      via: "docker compose exec psql"
      pattern: "FailOnContractGaps"
---

<objective>
Phasen-Gate fuer 106: fuehrt die in 106-02 authored Gates gegen den finalen Zustand aus und belegt SC3 (Kette 1→n auf leerer DB + Contract-Check) und SC4 (`go build`/`go vet` gruen + grep-Suite leer). Enthaelt den unvermeidlichen Live-Checkpoint (Docker-Backend-Rebuild gegen frische DB + `/covers/`-404), da der Bash-Sandbox keine Host-Ports/Docker-DB erreicht (Projektgedaechtnis).

Purpose: Abschliessender Nachweis der vier Success Criteria vor `/gsd:verify-work`.
Output: Gate-Belege (kein Code-Artefakt) + SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-medienkern-schema-legacy-abbau/106-RESEARCH.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-VALIDATION.md

<interfaces>
<!-- Gate-Artefakte aus 106-01/106-02 + Migrations-Runner. -->
- scripts/media-core-legacy-grep.ps1 (SC4 grep-Gate, exit!=0 bei Rest-Referenz; release_media auf NON-TEST-Live-Code gescopet).
- scripts/media-core-contract-check.ps1 -FailOnContractGaps (SC3 Live-Contract-Gate, throw bei Verstoss).
- backend/internal/migrations/media_core_schema_test.go (SC1 Content-Test).
- Migrations-Runner: backend/cmd/migrate (up gegen leere DB; ResolveMigrationsDir → database/migrations).
- Backend-Rebuild (Projektgedaechtnis): `docker compose up -d --build team4sv30-backend`; DB-Identitaet team4s_v2/team4s.
- ENDPOINT-OPTION-A-KONDITIONAL (106-05): Falls 106-05 Option A gewaehlt hat (release_media-Drop zurueckgenommen), muss dieses Gate den angepassten Contract-Check-Zustand akzeptieren (release_media bewusst vorhanden) und der grep-Live-Suche den behaltenen ListReleaseAssets-Lesepfad allowlisten. 106-05-SUMMARY gibt die gewaehlte Option vor.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Statische Gates — Build, vet, Migrations-Test, grep-Suite</name>
  <files></files>
  <read_first>
    - scripts/media-core-legacy-grep.ps1 (Gate ausfuehren)
    - 106-VALIDATION.md „Per-Task Verification Map" (SC-Zuordnung)
    - 106-05-SUMMARY.md (gewaehlte Endpoint-Option A/B/C — beeinflusst release_media-Erwartung)
  </read_first>
  <action>Fuehre die statisch pruefbaren Gates im Working Tree aus: (1) `cd backend && go build ./... && go vet ./...` — muss gruen sein. (2) `cd backend && go test ./internal/migrations/... -run MediaCore` — Content-Test gruen. (3) `cd backend && go test ./internal/repository/... ./internal/handlers/...` — die angepassten Legacy-Test-Guards (runtime_authority, media_upload) gruen. (4) `powershell -File scripts/media-core-legacy-grep.ps1` — muss mit Exit 0 enden (keine Rest-Referenzen auf entfernte Symbole/Routen; die 6 intentional retained Test-Guards sind per Scope/Allowlist ausgenommen). Bei Treffern: welche Datei/welches Symbol noch offen ist im SUMMARY notieren und an den zustaendigen Removal-Plan zurueckmelden (Gate rot lassen, nicht kaschieren). Beruecksichtige die in 106-05 gewaehlte Endpoint-Option (bei Option A ist ein bewusst behaltener release_media-Lesepfad erwartet + allowlisted).</action>
  <acceptance_criteria>
    - `cd backend && go build ./... && go vet ./...` Exit 0.
    - `cd backend && go test ./internal/migrations/... ./internal/repository/... ./internal/handlers/...` gruen.
    - `powershell -File scripts/media-core-legacy-grep.ps1` Exit 0 (grep-Suite leer; Test-Guards nicht als Verstoss gewertet).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./... && go test ./internal/migrations/... && powershell -NoProfile -ExecutionPolicy Bypass -File ../scripts/media-core-legacy-grep.ps1 && echo STATIC-GATES-OK</automated>
  </verify>
  <done>Build/vet gruen, Migrations-/Legacy-Test-Guards gruen, grep-Suite leer ohne Test-Guard-False-Fail (SC4 belegt).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-Gate — Kette auf leerer DB + Contract-Check + /covers-404</name>
  <files></files>
  <action>Nach den gruenen statischen Gates (Task 1) die Live-Nachweise fuer SC3 + SC2-live durchfuehren: frische DB + volle Kette anwenden, Backend neu bauen, Contract-Check `-FailOnContractGaps` ausfuehren, `/covers/`-404 im Browser pruefen. Dieser Checkpoint existiert, weil Docker-Backend-Rebuild + frische Ziel-DB + Host-Port-Zugriff im Bash-Sandbox nicht erreichbar sind (Projektgedaechtnis) — die Schritte im how-to-verify sind die exakte Ausfuehrungsanleitung.</action>
  <what-built>
    Additive `media`/`media_variant`-Migration (0131) + vollstaendiger §6-Legacy-Abbau. Zu belegen sind SC3 (Kette 1→n auf leerer DB + Contract-Check) und der Live-Teil von SC2 (`/covers/`-404) — beides erfordert Docker-Backend-Rebuild und eine frische DB, die der Bash-Sandbox nicht erreicht.
  </what-built>
  <how-to-verify>
    1. Frische/leere Ziel-DB herstellen (lokaler Reset-Workflow) und die volle Kette anwenden:
       `docker compose exec team4sv30-backend /app/migrate up` (bzw. der etablierte `cmd/migrate up`-Aufruf gegen die leere DB). Erwartung: Kette 1→n laeuft fehlerfrei durch, keine Migration schlaegt fehl.
    2. Backend neu bauen: `docker compose up -d --build team4sv30-backend`; danach `/health` pruefen (200).
    3. Contract-Check ausfuehren: `powershell -File scripts/media-core-contract-check.ps1 -FailOnContractGaps`.
       Erwartung: kein throw; Report zeigt `media`/`media_variant` present, `release_media`/`anime.cover_image` absent, `media_assets`/`media_files` weiterhin present. (Bei 106-05 Option A: `release_media` bewusst present — Contract-Check entsprechend angepasst.)
    4. Live-`/covers/`-404: im Browser am Dev-Server `:3000` eine alte Cover-URL `/covers/<beliebiger-name>.jpg` aufrufen. Erwartung: 404 / kein Handler (Route entfernt).
    5. Optional Sanity: `POST /admin/upload` (generischer V2-Upload) funktioniert weiterhin wie vor der Phase (Auth-Verhalten unveraendert — Haertung erst Phase 108).
  </how-to-verify>
  <resume-signal>Antworte „approved" wenn Kette fehlerfrei, Contract-Check gruen und `/covers/` 404 liefert — oder beschreibe die Abweichung (fehlgeschlagene Migration / Contract-Gap / unerwartetes Cover-Serving).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Keine neue. Verifikation gegen frische DB; kein neuer Auth-/Input-Pfad.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-08-01 | Repudiation | Stale Docker-Backend maskiert Erfolg/Fehler | mitigate | Pflicht-Rebuild `docker compose up -d --build team4sv30-backend` vor Contract-Check + `/health`-Pruefung (Pitfall 6). |
| T-106-08-SC | Tampering | Package-Installs | accept | Keine Installs. |
Endpoint-Haertung (`POST /admin/upload`, `DELETE /admin/media/:id`) bleibt Phase 108 — hier nur Regressionscheck des unveraenderten Verhaltens.
</threat_model>

<verification>
- Task 1: statische Gates (build/vet/tests/grep) gruen; grep-Suite ohne Test-Guard-False-Fail.
- Task 2: Live-Kette + Contract-Check + `/covers/`-404 vom Nutzer bestaetigt.
</verification>

<success_criteria>
SC3 erfuellt: Kette 1→n auf leerer DB fehlerfrei + Contract-Check belegt Legacy-Freiheit bei intaktem media_assets/media_files. SC4 erfuellt: build/vet gruen + grep-Suite leer (Test-Guards korrekt ausgenommen).
</success_criteria>

<output>
Create `.planning/phases/106-medienkern-schema-legacy-abbau/106-08-SUMMARY.md` when done.
</output>
