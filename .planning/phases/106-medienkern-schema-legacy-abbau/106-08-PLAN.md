---
phase: 106-medienkern-schema-legacy-abbau
plan: 08
type: execute
wave: 5
depends_on: [106-01, 106-02, 106-03, 106-04, 106-05, 106-07, 106-09, 106-10]
files_modified: []
autonomous: false
requirements: [SC3, SC4]
must_haves:
  truths:
    - "SC4: `go build ./...` + `go vet ./...` sind gruen und die grep-Suite (`media-core-legacy-grep.ps1`) meldet KEINE verbleibenden Referenzen auf die in 106 entfernten Symbole/Routen (Exit 0)."
    - "SC4-Scope (Checker-Blocker 2, behoben): Die grep-Suite scannt AUSSCHLIESSLICH die vier Quell-Wurzeln `backend/internal`, `backend/cmd`, `frontend/src`, `shared/contracts`. `database/migrations/**` bleibt bewusst ausserhalb (Altmigrationen `0018_episode_version_images.*` und `0046_drop_legacy_episode_versions.*` bleiben laut D-01/A4 im Repo und tragen `episode_version_image` dauerhaft), ebenso `scripts/**` (`reset-local-schema-cutover-data.ps1`, `schema-v2-audit.ps1` — Phase-110-Scope, von keinem 106-Plan angefasst; ausserdem wuerden die Gate-Skripte ihre eigenen Suchbegriff-Literale treffen). Ohne dieses Scoping waere `Exit 0` per Konstruktion unerreichbar und dieses Gate unerfuellbar."
    - "cover_image-Statik-Scope: Die Spalten-Signatur-Suche laeuft NUR ueber `backend/internal` + `backend/cmd` mit `--include=*.go` — `frontend/src` ist ausgenommen, weil `anime.cover_image` dort der legitime TypeScript-DTO-Zugriff ist. `backend/internal/repository/anime_relations.go` trifft die Signatur `,\s*cover_image` nach 106-09 Task 2 nicht mehr, weil der Sub-Query-Ausgabename dort in `cover_url` umbenannt wurde."
    - "D-07 (LOCKED): Die grep-Suite sucht weder nach `release_media` noch nach `CreateReleaseMedia` — beide bleiben in Phase 106 bewusst im Code. Es gibt daher keine Allowlist, keine Test-Guard-Sonderbehandlung und keine SUMMARY-Konditionale in diesem Gate."
    - "D-08 (LOCKED, PO-Entscheid 2026-07-22): Plan 106-06 ist ERSATZLOS ENTFALLEN — der `asset_lifecycle`-Abbau wandert nach Phase 107, weil §6 ihn mit der erst dort entstehenden hash-basierten Ablage begruendet. Dieses Gate prueft entsprechend UMGEKEHRT: `asset_lifecycle` MUSS nach Phase 106 noch VORHANDEN sein — in Go (`services/asset_lifecycle_service.go`, `services/asset_lifecycle_errors.go`, `models/asset_lifecycle.go`, `repository/asset_lifecycle_audit.go`, `repository/asset_lifecycle_subjects.go`), im Contract (`openapi.yaml` genau 2 Treffer bei :1475/:1481) und als `provisioning`-Feld ueber alle drei Schichten (`models/media_upload.go:43`, `openapi.yaml:9058`, `frontend/src/types/admin.ts:691`). Ein Fehlen ist ein VORGRIFF auf Phase 107 und macht das Gate ROT. `asset_lifecycle` ist zugleich KEIN SC4-Suchbegriff mehr."
    - "D-09 (LOCKED, PO-Feststellung 2026-07-22): `media_assets`, `media_files`, `release_media` und `release_version_media` sind AKTIVE Infrastruktur. Dieses Gate belegt positiv, dass keine von ihnen und keiner ihrer Lese-/Schreibpfade in Phase 106 entfernt, geleert oder still umgebogen wurde — statisch (grep auf `CreateReleaseMedia`/`ListReleaseAssets`/`JOIN media_assets`) und live (Media-Assets-Sektion, Cover-Oberflaechen)."
    - "AGENTS.md „Auth Session Rules\" Z. 89/90 (Blocker 3 behoben): Phase 106 fasst Upload-Flows an, also traegt sie die Pflichtregression „Access-Token fehlt/abgelaufen + Refresh-Token gueltig → geschuetzte Upload-Aktion laeuft ueber die zentrale Refresh-Naht durch, ohne Logged-out-UI\". Sie ist in 106-07 Task 2 als automatisierter Test in `frontend/src/lib/api.auth-refresh.test.ts` implementiert und wird HIER als Phasen-Gate erneut ausgefuehrt — ein rotes Ergebnis blockiert den Phasenabschluss."
    - "Sauberkeits-Gate (Extra, Plan-Checker 2026-07-22): `git diff --check` laeuft als ABSCHLIESSENDER Schritt des statischen Gates und muss ohne Ausgabe/mit Exit 0 enden — er faengt Whitespace-Fehler und liegengebliebene Konfliktmarker (`<<<<<<<`, `=======`, `>>>>>>>`) ab, bevor die Phase schliesst."
    - "Blocker 2 (Plan-Checker 2026-07-22, behoben — CWD): Die grep-Suite wird AUS DEM REPO-ROOT aufgerufen (`powershell -NoProfile -ExecutionPolicy Bypass -File scripts/media-core-legacy-grep.ps1`), NICHT aus der `cd backend`-Kette heraus. Zusaetzlich traegt das Skript selbst seit 106-02 Task 2 einen `$PSScriptRoot`-Repo-Root-Guard plus harte Existenzpruefung seiner vier Scan-Wurzeln — beide Haelften zusammen schliessen aus, dass das SC4-Gate ueber nicht aufloesbare Pfade laeuft und dabei entweder hart rot wird oder still einen FALSE PASS liefert."
    - "SC3: Die vollstaendige Migrationskette 1→n laeuft auf einer leeren DB fehlerfrei durch."
    - "SC3: `media-core-contract-check.ps1 -FailOnContractGaps` belegt gegen die frische DB: `media`/`media_variant` vorhanden, `anime.cover_image` abwesend, `media_assets`/`media_files`/`release_media` weiterhin vorhanden (release_media als D-07-Drop-Guard)."
    - "SC2 (D-03, P-8 korrigiert): Der Nachweis fuer die Entfernung der Cover-Route ist die ABWESENHEIT DER HANDLER-DATEIEN `frontend/src/app/covers/[file]/route.ts` und `frontend/src/app/api/admin/upload-cover/route.ts` (statischer Datei-Existenzcheck in 106-02/106-08), NICHT ein Live-404. Begruendung: `frontend/public/covers/` existiert mit vier Dateien und wird von Next.js weiterhin statisch am Root bedient — `/covers/placeholder.jpg` liefert nach dem Handler-Delete weiterhin 200, und `/covers/<beliebiger-name>.jpg` liefert 404 sowohl VOR als auch NACH der Phase (der Handler gab bei fehlender Datei ebenfalls 404 zurueck, `route.ts:46`). Ein 404-Check kann in beiden Zustaenden dasselbe Ergebnis liefern und beweist damit nichts. `frontend/public/covers/` ist Phase-110-Scope."
    - "P-3/P-4 (SC4-Erreichbarkeit, RESEARCH-Refresh 2026-07-22): Das Gate kann Exit 0 nur erreichen, weil (a) Plan 106-03 Task 3 die Release-Images-Strecke aus `shared/contracts/openapi.yaml` entfernt (sonst matcht `listReleaseImages` case-insensitiv in der Scan-Wurzel `shared/contracts`) und (b) der wirkungslose snake_case-Term `episode_version_image` durch `EpisodeVersionImage` ersetzt wurde. Beide Korrekturen sind Voraussetzung dieses Gates."
    - "A4 (offene Annahme, hier geschlossen): Der Live-Checkpoint prueft per `\d anime` gegen die frische DB, dass die Spalte `slug` vorhanden ist. Nur dann sind die von 106-10 entfernten `HasSlug=false`-Legacy-Zweige tatsaechlich unerreichbar gewesen; andernfalls haette 106-10 lebende Pfade entfernt."
    - "P-6 (Konsequenz von 106-03): `ScreenshotGallery` bleibt als bewusst bis Phase 109 deferrierter Consumer der entfernten Route `/releases/:id/images` im Frontend stehen. Der Live-Checkpoint bestaetigt, dass die Screenshot-Sektion der Episoden-Detailseite sich verhaelt wie vor der Phase (Fehler-/Leerzustand, kein Layout-Bruch) — heute HTTP 500 aus dem Repo-Stub, danach HTTP 404, beides derselbe `!response.ok`-Zweig."
    - "cover_image-Live-Regression (Checker-Blocker, behoben): Nach angewendeter 0131 liefern die vier von den ehemaligen Rohspalten-Lesern bedienten OEFFENTLICHEN Oberflaechen weiterhin fehlerfrei Cover-Bilder - Watchlist, Anime-Relationen, Gruppenprojekte (`listPublicFansubProjects`) und Member-Profil. Ein uebersehener Leser faellt hier als HTTP 500 / leere Sektion auf, bevor die Phase schliesst."
    - "Migrations-Gate robust: Die 0131-Datei wird ueber ein Glob mit expliziter Existenzpruefung aufgeloest (`database/migrations/*_media_core_schema.up.sql` muss GENAU EINE Datei finden), nicht ueber eine hartkodierte Nummer. Ein `! grep -lq ... 0131_*.sql` auf einer fehlenden Datei liefert Exit 2 und damit negiert einen FALSE PASS - dieses Muster ist verboten."
    - "D-07-Live-Regression: `GET /releases/:id/assets` liefert am Dev-Server weiterhin Daten wie vor der Phase; die „Media Assets"-Sektion auf der Episoden-Detailseite ist unveraendert sichtbar."
  artifacts: []
  key_links:
    - from: "scripts/media-core-contract-check.ps1"
      to: "frische DB nach Kettenlauf"
      via: "docker compose exec psql"
      pattern: "FailOnContractGaps"
---

<objective>
Phasen-Gate fuer 106: fuehrt die in 106-02 authored Gates gegen den finalen Zustand aus und belegt SC3 (Kette 1→n auf leerer DB + Contract-Check) und SC4 (`go build`/`go vet` gruen + grep-Suite leer). Enthaelt den unvermeidlichen Live-Checkpoint (Docker-Backend-Rebuild gegen frische DB + `\d anime`-Schemapruefung (A4) + P-6-/D-07-Regressionschecks), da der Bash-Sandbox keine Host-Ports/Docker-DB erreicht (Projektgedaechtnis).

REVISION 2 (PO-Entscheid 2026-07-22 / CONTEXT.md D-08 + D-09): Plan 106-06 ist ersatzlos entfallen; `asset_lifecycle` bleibt in Phase 106 vollstaendig produktiv (Entfernung → Phase 107). Dieses Gate prueft die betroffenen Artefakte deshalb als ANWESEND (Drop-Guard) statt als abwesend, und alle frueher hier gefuehrten P-5-Assertionen auf die von 106-06 erzeugten Artefakte (`repository/media_upload_audit.go`, `repository/media_upload_subjects.go`, `LookupUploadSubject`, `media_upload.invalid_entity_id`) sind ENTFERNT — sie haetten auf nie erzeugte Dateien gezielt und das Gate deterministisch rot gemacht. Neu ausserdem: das AGENTS.md-pflichtige Upload-Auth-Regressionsgate (aus 106-07 Task 2) und ein abschliessender `git diff --check`.

REVISION (PO-Entscheid 2026-07-21, „Option A" / CONTEXT.md D-07): alle frueheren „je nach 106-05-Option"-Konditionale sind entfallen — der Zielzustand ist eindeutig: `release_media` bleibt in 106 bestehen (Lese- UND Schreibpfad), und das Gate prueft aktiv, dass es NICHT verschwunden ist.

Purpose: Abschliessender Nachweis der vier Success Criteria vor `/gsd:verify-work`.
Output: Gate-Belege (kein Code-Artefakt) + SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-medienkern-schema-legacy-abbau/106-RESEARCH.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-CONTEXT.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-VALIDATION.md

<interfaces>
<!-- Gate-Artefakte aus 106-01/106-02 + Migrations-Runner. -->
- scripts/media-core-legacy-grep.ps1 (SC4 grep-Gate, AUS DEM REPO-ROOT aufzurufen — traegt `$PSScriptRoot`-Guard + harte
  Wurzel-Existenzpruefung aus 106-02 Task 2; exit!=0 bei Rest-Referenz; kennt KEINEN release_media-Term;
  gescopet auf die vier Quell-Wurzeln backend/internal, backend/cmd, frontend/src, shared/contracts — siehe 106-02 Task 2).
- scripts/media-core-contract-check.ps1 -FailOnContractGaps (SC3 Live-Contract-Gate, throw bei Verstoss; release_media als erwartet-vorhanden).
- backend/internal/migrations/media_core_schema_test.go (SC1 Content-Test inkl. D-07-Negativ-Assertion auf release_media in 0131).
- Migrations-Runner: backend/cmd/migrate (up gegen leere DB; ResolveMigrationsDir → database/migrations).
- Backend-Rebuild (Projektgedaechtnis): `docker compose up -d --build team4sv30-backend`; DB-Identitaet team4s_v2/team4s.
- D-07 (LOCKED): `release_media` ist in Phase 106 unangetastet. Kein Plan liefert eine Option/Allowlist-Rueckmeldung;
  das Gate ist unbedingt formuliert.
- D-08 (LOCKED): `asset_lifecycle` ist in Phase 106 ebenfalls unangetastet (Plan 106-06 entfallen, Entfernung → Phase 107).
  Die betroffenen Dateien/Contract-Stellen werden hier als ANWESEND geguardet, nicht als abwesend gefordert.
- frontend/src/lib/api.auth-refresh.test.ts (aus 106-07 Task 2): traegt die AGENTS.md-pflichtige Upload-Auth-Regression
  (missing/expired Access-Token + gueltiger Refresh-Token). Ausfuehrung: `cd frontend && npx vitest run src/lib/api.auth-refresh.test.ts`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Statische Gates — Build, vet, Migrations-Test, grep-Suite, Auth-Regression, git diff --check</name>
  <files>scripts/media-core-legacy-grep.ps1, scripts/media-core-contract-check.ps1, frontend/src/lib/api.auth-refresh.test.ts, shared/contracts/openapi.yaml</files>
  <read_first>
    - scripts/media-core-legacy-grep.ps1 (Gate ausfuehren — READ-ONLY, dieses Gate aendert die Skripte nicht)
    - scripts/media-core-contract-check.ps1 (Gate-Artefakt, hier nur auf Existenz/Guards geprueft; Ausfuehrung in Task 2)
    - shared/contracts/openapi.yaml (Contract-Sauberkeits- und Drop-Guard-Assertionen)
    - frontend/src/lib/api.auth-refresh.test.ts (AGENTS.md-Auth-Regression aus 106-07 Task 2 — hier als Phasen-Gate erneut ausgefuehrt)
    - 106-VALIDATION.md „Per-Task Verification Map" (SC-Zuordnung)
    - 106-CONTEXT.md D-07 (release_media bleibt — kein Suchbegriff, kein Contract-Gap), D-08 (asset_lifecycle bleibt — Drop-Guard statt Abwesenheitsforderung), D-09 (aktive Infrastruktur)
  </read_first>
  <action>Fuehre die statisch pruefbaren Gates im Working Tree aus: (1) `cd backend && go build ./... && go vet ./...` — muss gruen sein. (2) `cd backend && go test ./internal/migrations/... -run MediaCore` — Content-Test gruen (inkl. D-07-Negativ-Assertion: 0131 enthaelt kein `release_media`). (3) `cd backend && go test ./internal/repository/... ./internal/handlers/...` — die angepassten Legacy-Test-Guards (runtime_authority, media_upload, release_assets) gruen. (4) `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/media-core-legacy-grep.ps1` — muss mit Exit 0 enden (keine Rest-Referenzen auf entfernte Symbole/Routen). CWD-PFLICHT (Blocker-2-Fix 2026-07-22): Dieser Aufruf laeuft AUS DEM REPO-ROOT, NICHT innerhalb der `cd backend`-Kette der uebrigen Schritte — die Scan-Wurzeln und Abwesenheitspfade der Suite sind repo-root-relativ, und PowerShell erbt das cwd des Aufrufers. Fuehre ihn deshalb als eigenstaendigen Befehl vor/nach der Go-Kette aus. Das Skript traegt zusaetzlich seit 106-02 Task 2 einen eigenen `$PSScriptRoot`-Guard; faellt einer der beiden Schutzmechanismen weg, ist das Gate ungueltig — dann 106-02 zurueckmelden, NICHT die Pfade im Aufruf zurechtbiegen. Falls das Skript einen Treffer in `database/migrations/**` oder `scripts/**` meldet, ist NICHT der Code, sondern der Scan-Scope defekt: diese Pfade sind laut 106-02 Task 2 bewusst ausserhalb (0018/0046 bleiben per D-01/A4; reset-/audit-Skripte sind Phase-110-Scope) — dann das Skript korrigieren statt die bewusst behaltenen Dateien anzufassen. (5) D-07-Statik-Regression: belege per grep, dass `CreateReleaseMedia` (repository/media_upload.go + handlers/media_upload_storage.go) und `ListReleaseAssets` (repository/episode_version_repository_read_helpers.go) weiterhin vorhanden sind. Die Medienkern-Migration NICHT ueber die hartkodierte Nummer 0131 adressieren (der D-02-Nummern-Guard in 106-01 kann auf 0132+ ausgewichen sein) und NICHT per `! grep -lq ... <glob>` pruefen - bei fehlender Datei liefert grep Exit 2, die Negation macht daraus einen FALSE PASS. Stattdessen: Datei ueber ein stabiles Namensglob aufloesen und ihre Existenz zuerst hart pruefen - `MIG=$(ls database/migrations/*_media_core_schema.up.sql)`; die Trefferzahl MUSS genau 1 sein, sonst Gate ROT. Erst danach `grep -q "release_media" "$MIG"` ausfuehren und einen Treffer als Verstoss werten. (6) cover_image-Statik: `grep -rnE "a\.cover_image|,[[:space:]]*cover_image|cover_image[[:space:]]*=|SET cover_image" backend/internal backend/cmd --include=*.go | grep -v "json:" | grep -v "applyString"` MUSS leer sein - das ist der statische Nachweis, dass 106-05/106-09/106-10 alle Rohspalten-Zugriffe geschlossen haben. (7) DATEI-ABWESENHEIT (P-8-Ersatz fuer den untauglichen Live-404-Check, plus P-4-Ergaenzung): pruefe hart, dass die folgenden Pfade NICHT mehr existieren — `frontend/src/app/covers/[file]/route.ts`, `frontend/src/app/api/admin/upload-cover/route.ts`, `backend/internal/handlers/episode_version_images_handler.go`, `backend/internal/repository/episode_version_image_repository.go`, `backend/internal/models/episode_version_image.go`, `backend/internal/handlers/media_upload_v2_compat.go`, sowie die Verzeichnisse `backend/database/migrations/` und `backend/cmd/migrate-covers/`. Gegenprobe: `frontend/public/covers/` existiert weiterhin (Phase-110-Scope) — das ist KEIN Verstoss. (8) CONTRACT-SAUBERKEIT (P-3, nach D-08 korrigiert): `grep -ci "listReleaseImages\|EpisodeVersionImage" shared/contracts/openapi.yaml` MUSS 0 sein, waehrend `grep -c "listReleaseAssets" shared/contracts/openapi.yaml` >= 1 bleibt (D-07-Endpoint). `asset_lifecycle` ist hier AUSDRUECKLICH NICHT MEHR Teil der Nullpruefung — der frueher hier stehende `asset_lifecycle`-Term gehoerte zum entfallenen Plan 106-06 und wuerde das Gate unter D-08 deterministisch rot machen.
(9) D-08-DROP-GUARD (ersetzt die entfallenen P-5-Assertionen): Belege POSITIV, dass Phase 106 den `asset_lifecycle`-Cluster NICHT vorweggenommen hat. Erwartet: `grep -c "asset_lifecycle" shared/contracts/openapi.yaml` == 2 (die Fehlercode-Beispiele :1475/:1481); `grep -c "provisioning" shared/contracts/openapi.yaml` >= 1; die Dateien `backend/internal/services/asset_lifecycle_service.go`, `backend/internal/services/asset_lifecycle_errors.go`, `backend/internal/models/asset_lifecycle.go`, `backend/internal/repository/asset_lifecycle_audit.go` und `backend/internal/repository/asset_lifecycle_subjects.go` EXISTIEREN weiterhin; `grep -q "WithLifecycleService" backend/cmd/server/main.go` liefert einen Treffer (Injektion intakt); `grep -q "Provisioning" backend/internal/models/media_upload.go` und `grep -q "provisioning" frontend/src/types/admin.ts` liefern Treffer (Feld ueber alle drei Schichten konsistent). Fehlt eines davon, hat ein Plan Phase-107-Scope vorweggenommen → Gate ROT, an den verursachenden Plan zurueckmelden, NICHT nachtraeglich legalisieren. ERSATZLOS GESTRICHEN sind die frueher hier stehenden P-5-Assertionen auf `repository/media_upload_audit.go`, `repository/media_upload_subjects.go`, `LookupUploadSubject` und `media_upload.invalid_entity_id` — diese Artefakte haette ausschliesslich der entfallene Plan 106-06 erzeugt; die Faehigkeiten (Entity-Existenzpruefung, Audit-Schreibung) leben unter D-08 unveraendert im `asset_lifecycle`-Namensraum weiter.
(10) AUTH-REGRESSION (AGENTS.md „Auth Session Rules" Z. 89/90, Blocker-3-Fix): `cd frontend && npx vitest run src/lib/api.auth-refresh.test.ts` — muss gruen sein, inklusive der beiden in 106-07 Task 2 ergaenzten Upload-Faelle (fehlendes bzw. abgelaufenes Access-Token bei gueltigem Refresh-Token laufen ueber die zentrale Refresh-Naht durch, ohne Logged-out-Zustand, ohne 401 an den Aufrufer). Rot = Phasenabschluss blockiert; NICHT durch Anpassen der Assertion gruen machen.
(11) SAUBERKEITS-GATE (Extra, ABSCHLIESSENDER Schritt): `git diff --check` im Repo-Root — muss ohne Ausgabe und mit Exit 0 enden. Er faengt Whitespace-Fehler und liegengebliebene Konfliktmarker ab. Dieser Schritt laeuft ZULETZT, nach allen anderen Gates, damit er den finalen Arbeitsbaum sieht.
Bei Treffern der grep-Suite: welche Datei/welches Symbol noch offen ist im SUMMARY notieren und an den zustaendigen Removal-Plan zurueckmelden (Gate rot lassen, nicht kaschieren).</action>
  <acceptance_criteria>
    - `cd backend && go build ./... && go vet ./...` Exit 0.
    - `cd backend && go test ./internal/migrations/... ./internal/repository/... ./internal/handlers/...` gruen.
    - `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/media-core-legacy-grep.ps1` — AUS DEM REPO-ROOT aufgerufen — Exit 0 (grep-Suite leer).
    - CWD-Kontrakt (Blocker-2-Fix): Der Aufruf steht NICHT in der `cd backend`-Kette, und die Suite traegt den `$PSScriptRoot`-Repo-Root-Guard aus 106-02 Task 2 — `grep -c "PSScriptRoot" scripts/media-core-legacy-grep.ps1` >= 1. Gegenprobe: aus `backend/` heraus aufgerufen (`powershell ... -File ../scripts/media-core-legacy-grep.ps1`) liefert die Suite denselben Exit-Code wie aus dem Repo-Root — ein abweichendes Ergebnis bedeutet fehlenden Guard und macht das Gate ungueltig.
    - Scope-Beleg: die grep-Suite nennt `backend/internal`, `backend/cmd`, `frontend/src` und `shared/contracts` als Wurzeln und scannt weder `database/migrations/**` noch `scripts/**` (Blocker-2-Fix; sonst waere Exit 0 unerreichbar).
    - D-07-Statik: `grep -q "CreateReleaseMedia" backend/internal/repository/media_upload.go` und `grep -q "ListReleaseAssets" backend/internal/repository/episode_version_repository_read_helpers.go` liefern Treffer.
    - Migrations-Aufloesung: `ls database/migrations/*_media_core_schema.up.sql | wc -l` == 1 (Datei existiert, Nummer egal); erst dann `grep -q "release_media" "$MIG"` -> KEIN Treffer.
    - cover_image-Statik: `grep -rnE "a\.cover_image|,[[:space:]]*cover_image|cover_image[[:space:]]*=|SET cover_image" backend/internal backend/cmd --include=*.go | grep -v "json:" | grep -v "applyString"` liefert KEINE Zeile.
    - P-8/P-4 Datei-Abwesenheit: `[ ! -f "frontend/src/app/covers/[file]/route.ts" ]`, `[ ! -f frontend/src/app/api/admin/upload-cover/route.ts ]`, `[ ! -f backend/internal/handlers/episode_version_images_handler.go ]`, `[ ! -f backend/internal/repository/episode_version_image_repository.go ]`, `[ ! -f backend/internal/models/episode_version_image.go ]`, `[ ! -f backend/internal/handlers/media_upload_v2_compat.go ]`, `[ ! -d backend/database/migrations ]`, `[ ! -d backend/cmd/migrate-covers ]` — alle erfuellt.
    - P-8 Gegenprobe (KEIN Verstoss): `[ -d frontend/public/covers ]` ist weiterhin wahr — der statische Ordner bleibt Phase-110-Scope und darf das Gate nicht rot machen.
    - P-3 Contract-Sauberkeit (nach D-08 korrigiert): `grep -ci "listReleaseImages\|EpisodeVersionImage" shared/contracts/openapi.yaml` == 0. `asset_lifecycle` ist NICHT Teil dieser Nullpruefung.
    - P-3/D-07 Contract-Gegenprobe: `grep -c "listReleaseAssets" shared/contracts/openapi.yaml` >= 1.
    - D-08-Drop-Guard Contract: `grep -c "asset_lifecycle" shared/contracts/openapi.yaml` == 2 UND `grep -c "provisioning" shared/contracts/openapi.yaml` >= 1 — die Contract-Oberflaeche des Clusters ist unveraendert (Entfernung erst Phase 107).
    - D-08-Drop-Guard Go: `[ -f backend/internal/services/asset_lifecycle_service.go ]`, `[ -f backend/internal/services/asset_lifecycle_errors.go ]`, `[ -f backend/internal/models/asset_lifecycle.go ]`, `[ -f backend/internal/repository/asset_lifecycle_audit.go ]`, `[ -f backend/internal/repository/asset_lifecycle_subjects.go ]` — alle erfuellt; zusaetzlich `grep -q "WithLifecycleService" backend/cmd/server/main.go` (Live-Verdrahtung intakt).
    - D-08-Drop-Guard Drei-Schichten-Konsistenz `provisioning`: `grep -q "Provisioning" backend/internal/models/media_upload.go` UND `grep -q "provisioning" shared/contracts/openapi.yaml` UND `grep -q "provisioning" frontend/src/types/admin.ts` liefern jeweils Treffer.
    - AGENTS.md-Auth-Gate (Z. 89/90): `cd frontend && npx vitest run src/lib/api.auth-refresh.test.ts` gruen; `grep -c "seedRuntimeSessionMissingAccessToken" frontend/src/lib/api.auth-refresh.test.ts` >= 2 und `grep -c "uploadAdminAnimeMedia" frontend/src/lib/api.auth-refresh.test.ts` >= 5 (die zwei neuen Upload-Faelle aus 106-07 Task 2 sind vorhanden).
    - Sauberkeits-Gate (Extra): `git diff --check` im Repo-Root liefert KEINE Ausgabe und Exit 0 (keine Whitespace-Fehler, keine Konfliktmarker). Laeuft als ABSCHLIESSENDER Schritt.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/media-core-legacy-grep.ps1 && (cd frontend && npx vitest run src/lib/api.auth-refresh.test.ts) && [ "$(grep -c 'seedRuntimeSessionMissingAccessToken' frontend/src/lib/api.auth-refresh.test.ts)" -ge 2 ] && [ "$(grep -c 'uploadAdminAnimeMedia' frontend/src/lib/api.auth-refresh.test.ts)" -ge 5 ] && cd backend && go build ./... && go vet ./... && go test ./internal/migrations/... && grep -q "CreateReleaseMedia" internal/repository/media_upload.go && grep -q "ListReleaseAssets" internal/repository/episode_version_repository_read_helpers.go && MIG=$(ls ../database/migrations/*_media_core_schema.up.sql) && [ "$(printf '%s\n' "$MIG" | wc -l)" = "1" ] && [ -f "$MIG" ] && ! grep -q "release_media" "$MIG" && [ -z "$(grep -rnE 'a\.cover_image|,[[:space:]]*cover_image|cover_image[[:space:]]*=|SET cover_image' internal cmd --include=*.go | grep -v 'json:' | grep -v 'applyString')" ] && [ ! -f "../frontend/src/app/covers/[file]/route.ts" ] && [ ! -f ../frontend/src/app/api/admin/upload-cover/route.ts ] && [ ! -f internal/handlers/episode_version_images_handler.go ] && [ ! -f internal/handlers/media_upload_v2_compat.go ] && [ ! -d database/migrations ] && [ ! -d cmd/migrate-covers ] && [ -d ../frontend/public/covers ] && [ "$(grep -ci 'listReleaseImages\|EpisodeVersionImage' ../shared/contracts/openapi.yaml)" = "0" ] && grep -q "listReleaseAssets" ../shared/contracts/openapi.yaml && [ "$(grep -c 'asset_lifecycle' ../shared/contracts/openapi.yaml)" = "2" ] && grep -q "provisioning" ../shared/contracts/openapi.yaml && grep -q "provisioning" ../frontend/src/types/admin.ts && grep -q "Provisioning" internal/models/media_upload.go && [ -f internal/services/asset_lifecycle_service.go ] && [ -f internal/services/asset_lifecycle_errors.go ] && [ -f internal/models/asset_lifecycle.go ] && [ -f internal/repository/asset_lifecycle_audit.go ] && [ -f internal/repository/asset_lifecycle_subjects.go ] && grep -q "WithLifecycleService" cmd/server/main.go && cd .. && [ -z "$(git diff --check)" ] && echo STATIC-GATES-OK</automated>
  </verify>
  <done>Build/vet gruen, Migrations-/Legacy-Test-Guards gruen, grep-Suite leer, Migrationsdatei per Glob eindeutig aufgeloest und release_media-frei, cover_image-Spalten-Statik leer, D-07-/D-08-/D-09-Statik belegt (release_media + asset_lifecycle + provisioning nachweislich unangetastet), AGENTS.md-Upload-Auth-Regression gruen, `git diff --check` sauber (SC4 belegt).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-Gate — Kette auf leerer DB + Contract-Check + A4-Schemapruefung + P-6-/D-07-Regression</name>
  <files></files>
  <action>Nach den gruenen statischen Gates (Task 1) die Live-Nachweise fuer SC3 + SC2-live + D-07-Regression durchfuehren: frische DB + volle Kette anwenden, Backend neu bauen, Contract-Check `-FailOnContractGaps` ausfuehren, `/covers/`-404 pruefen und die Release-Assets-Sektion gegenpruefen. Dieser Checkpoint existiert, weil Docker-Backend-Rebuild + frische Ziel-DB + Host-Port-Zugriff im Bash-Sandbox nicht erreichbar sind (Projektgedaechtnis) — die Schritte im how-to-verify sind die exakte Ausfuehrungsanleitung.</action>
  <what-built>
    Additive `media`/`media_variant`-Migration (0131) + §6-Legacy-Abbau ohne `release_media` (D-07, Option A). Zu belegen sind SC3 (Kette 1→n auf leerer DB + Contract-Check), die A4-Schemapruefung (`slug` vorhanden, `cover_image` abwesend) und die Regressionen P-6 (Screenshot-Sektion unveraendert) sowie D-07 (Release-Assets-Sektion unveraendert) — alles erfordert Docker-Backend-Rebuild und eine frische DB, die der Bash-Sandbox nicht erreicht.
  </what-built>
  <how-to-verify>
    1. Frische/leere Ziel-DB herstellen (lokaler Reset-Workflow) und die volle Kette anwenden:
       `docker compose exec team4sv30-backend /app/migrate up` (bzw. der etablierte `cmd/migrate up`-Aufruf gegen die leere DB). Erwartung: Kette 1→n laeuft fehlerfrei durch, keine Migration schlaegt fehl.
    2. Backend neu bauen: `docker compose up -d --build team4sv30-backend`; danach `/health` pruefen (200).
    3. Contract-Check AUS DEM REPO-ROOT ausfuehren: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/media-core-contract-check.ps1 -FailOnContractGaps` (das Skript traegt seit 106-02 Task 1 zusaetzlich einen `$PSScriptRoot`-Repo-Root-Guard, weil es `docker compose` und repo-root-relative Pfade nutzt).
       Erwartung: kein throw; Report zeigt `media`/`media_variant` present, `anime.cover_image` absent, und `media_assets`/`media_files`/`release_media` weiterhin present (release_media = D-07-Drop-Guard).
    4. `/covers/`-Nachweis (P-8 korrigiert — der frueher hier stehende 404-Check war KEIN Signal):
       a) Statisch (der eigentliche Beleg, bereits in Task 1 geprueft): die Handler-Dateien `frontend/src/app/covers/[file]/route.ts` und `frontend/src/app/api/admin/upload-cover/route.ts` existieren nicht mehr.
       b) Live-Gegenprobe (dokumentierend, KEIN Fail-Kriterium): `/covers/placeholder.jpg` am Dev-Server `:3000` liefert weiterhin 200 — die Datei kommt statisch aus `frontend/public/covers/` (4 Dateien, Phase-110-Scope). Das ist der ERWARTETE Zustand und kein Verstoss.
       c) Nicht mehr pruefen: `/covers/<beliebiger-name>.jpg` → 404. Dieses Ergebnis lieferte auch der alte Handler bei fehlender Datei (`route.ts:46`) und unterscheidet die beiden Zustaende daher nicht.
    4b. A4-Schemapruefung (schliesst die letzte offene Annahme): `docker compose exec -T team4sv30-db psql -U team4s -d team4s_v2 -c "\d anime"`.
       Erwartung: die Spalte `slug` IST vorhanden (nur dann waren die von 106-10 entfernten `HasSlug=false`-Legacy-Zweige tatsaechlich tot) UND die Spalte `cover_image` ist nach 0131 ABWESEND. Fehlt `slug`, ist das Gate ROT und 106-10 muss zurueckgemeldet werden.
    5. D-07-Regression: Episoden-Detailseite mit `?releaseId=<id>` am Dev-Server `:3000` oeffnen. Erwartung: die „Media Assets"-Sektion verhaelt sich exakt wie vor der Phase (keine leere Sektion, kein Fehler) — `GET /releases/:id/assets` liefert weiterhin. Ein Release-Upload landet weiterhin in der Sektion (Schreibpfad intakt).
    5b. P-6-Regression (ScreenshotGallery, bewusst bis Phase 109 deferrierter Consumer der entfernten Route `/releases/:id/images`): Episoden-Detailseite mit einer Release-Auswahl oeffnen und die Screenshot-Sektion ansehen. Erwartung: sie verhaelt sich exakt wie vor der Phase — Fehler-/Leerzustand mit der Meldung „Screenshots konnten nicht geladen werden.", KEIN Layout-Bruch, KEINE unbehandelte Exception in der Browser-Konsole. Hintergrund: das Backend-Repository war schon vor der Phase ein Dauer-Stub (HTTP 500 bei jedem Aufruf); nach dem Route-Delete kommt HTTP 404 — beides laeuft in denselben `!response.ok`-Zweig. Weicht die Darstellung ab, ist das eine sichtbare FE-Aenderung in einer Phase, die „kein Verhaltensumbau am Frontend" zusichert → Gate ROT und an Phase 109 zurueckmelden.
    6. cover_image-Live-Regression (PFLICHT, vier oeffentliche Oberflaechen - jede wurde vor der Phase von einem `a.cover_image`-Rohspalten-Leser bedient und wuerde bei einem uebersehenen Leser mit HTTP 500 antworten):
       a) WATCHLIST: als angemeldeter Nutzer die Watchlist-Seite oeffnen (Listenansicht mit mindestens einem Eintrag) und zusaetzlich einen Einzeleintrag abrufen. Erwartung: 200, Cover-Bilder werden angezeigt, kein `column a.cover_image does not exist` im Backend-Log (`docker compose logs --tail=100 team4sv30-backend`).
       b) ANIME-RELATIONEN: eine Anime-Detailseite mit gepflegten Relationen oeffnen. Erwartung: die Relations-Sektion rendert mit Cover-Bildern; zusaetzlich in der Admin-Oberflaeche die Relationsverwaltung + die Titel-Suche fuer Relationsziele oeffnen (deckt `anime_relations_admin.go` und `ResolveAdminAnimeRelationTargetsByTitles` ab).
       c) GRUPPENPROJEKTE: eine Fansub-Gruppenseite mit zugeordneten Anime oeffnen (`listPublicFansubProjects`). Erwartung: die Projektliste rendert mit Cover-Bildern.
       d) MEMBER-PROFIL: ein Member-Profil mit laufenden Projekten oeffnen (`loadCurrentProjects`, GROUP-BY-Fall). Erwartung: die Sektion „Aktuelle Projekte" rendert; insbesondere KEIN Postgres-Fehler `must appear in the GROUP BY clause`.
       e) Zusatzcheck Admin: die Fansub-Anime-Zuordnung im Admin (`ListFansubAnime`) oeffnen - Coveranzeige unveraendert.
       Bei einem Fehler auf einer dieser Oberflaechen: Gate ROT lassen und die betroffene Query an Plan 106-09 (Leser) bzw. 106-10 (Legacy-Zweige) zurueckmelden - NICHT die Migration zuruecknehmen.
    7. D-08-Regression (PFLICHT, nicht optional): `POST /admin/upload` (generischer V2-Upload) verhaelt sich EXAKT wie vor der Phase. Die 400er-Fehlercodes lauten unveraendert `asset_lifecycle.invalid_entity_type` / `asset_lifecycle.invalid_asset_type` — die frueher hier angekuendigte Namensraum-Migration nach `media_upload.*` gehoerte zum ENTFALLENEN Plan 106-06 und findet in Phase 106 NICHT statt (Verschiebung nach Phase 107). Ebenfalls unveraendert: die Entity-Existenzpruefung (Upload auf unbekannte `entity_id` → HTTP 400 `asset_lifecycle.invalid_entity_id`), die Audit-Zeile in `admin_anime_mutation_audit` (`mutation_kind = anime.asset_lifecycle.provision`, `actor_user_id` gesetzt) und das `provisioning`-Feld in der Upload-Response. Weicht eines davon ab, hat ein Plan Phase-107-Scope vorweggenommen → Gate ROT. Auth-Verhalten unveraendert — Endpoint-Haertung erst Phase 108.
    8. AGENTS.md-Auth-Regression, Live-Gegenprobe (Z. 89): Als angemeldeter Admin am Dev-Server `:3000` warten bis das Access-Token abgelaufen ist (oder es im Browser-Storage leeren, Refresh-Token belassen) und dann einen Admin-Medien-Upload starten. Erwartung: der Upload laeuft durch, OHNE dass die UI in einen Logged-out-Zustand faellt oder eine Neuanmeldung verlangt. Der automatisierte Nachweis liegt in Task 1 Schritt 10; dieser Schritt bestaetigt ihn am realen Stack.
  </how-to-verify>
  <resume-signal>Antworte „approved" wenn: der Upload-Endpoint unveraendert mit den `asset_lifecycle.*`-Codes, der Existenzpruefung, der Audit-Zeile und dem `provisioning`-Feld antwortet (D-08), der Admin-Upload bei abgelaufenem Access-Token ohne Logged-out-UI durchlaeuft (AGENTS.md Z. 89), Kette fehlerfrei, Contract-Check gruen, `\d anime` zeigt `slug` VORHANDEN und `cover_image` ABWESEND (A4), die Screenshot-Sektion unveraendert im Fehler-/Leerzustand rendert (P-6), die Release-Media-Assets-Sektion unveraendert funktioniert (D-07) UND alle vier oeffentlichen Cover-Oberflaechen (Watchlist, Anime-Relationen, Gruppenprojekte, Member-Profil) fehlerfrei rendern — oder beschreibe die Abweichung (fehlgeschlagene Migration / Contract-Gap / fehlende `slug`-Spalte / veraenderte Screenshot-Sektion / leere Media-Assets-Sektion / HTTP 500 oder GROUP-BY-Fehler auf einer der vier Oberflaechen). Der `/covers/`-Schritt ist bewusst KEIN Fail-Kriterium mehr (P-8).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Keine neue. Verifikation gegen frische DB; kein neuer Auth-/Input-Pfad.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-08-01 | Repudiation | Stale Docker-Backend maskiert Erfolg/Fehler | mitigate | Pflicht-Rebuild `docker compose up -d --build team4sv30-backend` vor Contract-Check + `/health`-Pruefung (Pitfall 6). |
| T-106-08-02 | Tampering | Unbemerkter `release_media`-Verlust (Migration oder Code) bricht `GET /releases/:id/assets` | mitigate | Statik-grep (Task 1) + Contract-Check-Present-Assertion + Live-Regressionsschritt 5 im Checkpoint. |
| T-106-08-03 | Denial of Service | Uebersehener `anime.cover_image`-Leser bricht nach dem DROP eine oeffentliche Oberflaeche (HTTP 500) | mitigate | Statik-Gate (Task 1, Schritt 6: Spalten-Signatur-grep leer) + verpflichtende Live-Verifikation der vier betroffenen Oberflaechen im Checkpoint-Schritt 6. |
| T-106-08-04 | Repudiation | FALSE PASS des Migrations-Gates, weil `! grep -lq` auf einer fehlenden Datei Exit 2 negiert | mitigate | Datei zuerst per Glob `*_media_core_schema.up.sql` aufloesen, Trefferzahl == 1 und `-f` hart pruefen, danach erst greppen. |
| T-106-08-05 | Repudiation | Unerfuellbares SC4-Gate: grep-Suite trifft bewusst behaltene Dateien (0018/0046, Phase-110-Skripte) → Gate wird als „bekannt rot" ignoriert und verliert seine Aussagekraft | mitigate | Scan auf vier Quell-Wurzeln gescopet (106-02 Task 2); Ausschlussgruende im Skript dokumentiert; Acceptance-Criterion prueft die Wurzelliste. |
| T-106-08-06 | Repudiation | P-8: Der `/covers/`-404-Live-Check kann in beiden Zustaenden dasselbe Ergebnis liefern (statisches `public/covers/`-Serving, Handler gab bei fehlender Datei ebenfalls 404) → ein gruenes Gate belegt nichts | mitigate | Live-404-Check als Fail-Kriterium gestrichen; Nachweis ist die Datei-Abwesenheit der beiden Route-Handler (Task 1 Schritt 7 + 106-02 Scope C); `public/covers/`-Existenz explizit als erwartet dokumentiert. |
| T-106-08-07 | Tampering | A4: Falls die Live-DB kein `anime.slug` traegt, hat 106-10 lebende `HasSlug=false`-Pfade entfernt — unbemerkt, weil Build und Unit-Tests das nicht sehen | mitigate | `\d anime`-Schemapruefung als Pflichtschritt 4b im Checkpoint; fehlende `slug`-Spalte → Gate ROT + Rueckmeldung an 106-10. |
| T-106-08-08 | Denial of Service | P-6: Der bis Phase 109 deferrierte `ScreenshotGallery`-Consumer koennte auf HTTP 404 anders reagieren als auf HTTP 500 → sichtbare FE-Aenderung entgegen der Phasenzusage | mitigate | Pflichtschritt 5b im Checkpoint prueft die Sektion visuell; Abweichung → Gate ROT + Rueckmeldung an Phase 109. |
| T-106-08-09 | Tampering | D-08-Vorgriff: ein Plan entfernt `asset_lifecycle` doch in Phase 106 → Entity-Existenzpruefung und Audit-Attribution (CLAUDE.md-Constraint) fallen weg, bevor der hash-basierte Ersatz existiert | mitigate | Umgekehrter Drop-Guard in Task 1 Schritt 9 (Dateien existieren, `WithLifecycleService` verdrahtet, openapi-Trefferzahl == 2, `provisioning` in allen drei Schichten) + Pflicht-Live-Regression Schritt 7 im Checkpoint. |
| T-106-08-10 | Spoofing / Denial of Service | AGENTS.md Z. 89: fehlendes/abgelaufenes Access-Token bei gueltigem Refresh-Token wird im Upload-Flow als „logged out" behandelt → Session-Verlust mitten im Admin-Upload | mitigate | Automatisiertes Gate in Task 1 Schritt 10 (`vitest run src/lib/api.auth-refresh.test.ts` inkl. der zwei Upload-Faelle aus 106-07 Task 2) + Live-Gegenprobe Schritt 8 im Checkpoint. |
| T-106-08-11 | Tampering | Liegengebliebene Konfliktmarker/Whitespace-Fehler gelangen unbemerkt in den Phasen-Abschluss | mitigate | `git diff --check` als abschliessender Schritt des statischen Gates (Task 1 Schritt 11); Ausgabe != leer ⇒ Gate ROT. |
| T-106-08-SC | Tampering | Package-Installs | accept | Keine Installs. |
Endpoint-Haertung (`POST /admin/upload`, `DELETE /admin/media/:id`) bleibt Phase 108 — hier nur Regressionscheck des unveraenderten Verhaltens. Die `asset_lifecycle`-Kontrollen (Entity-Existenzpruefung, Audit-Attribution) bleiben unter D-08 in Phase 106 vollstaendig aktiv; Phase 106 verschlechtert die Sicherheitslage dieses Endpoints damit nachweislich nicht.
</threat_model>

<verification>
- Task 1: statische Gates (build/vet/tests/grep) gruen + D-07-/D-08-/D-09-Statik belegt + AGENTS.md-Upload-Auth-Regression gruen + `git diff --check` sauber.
- Task 2: Live-Kette + Contract-Check + `\d anime`-Schemapruefung (A4) + Screenshot-Sektion (P-6) + Release-Assets-Regression (D-07) vom Nutzer bestaetigt; `/covers/` nur noch dokumentierend (P-8).
</verification>

<success_criteria>
SC3 erfuellt: Kette 1→n auf leerer DB fehlerfrei + Contract-Check belegt Zielschema und Legacy-Freiheit bei intaktem `media_assets`/`media_files`/`release_media`/`release_version_media` (D-07/D-09) und intaktem `asset_lifecycle`-Cluster (D-08). SC4 erfuellt: build/vet gruen + grep-Suite leer. Zusaetzlich erfuellt: AGENTS.md-Auth-Regression fuer Upload-Flows (Z. 89/90) und `git diff --check` sauber.
</success_criteria>

<output>
Create `.planning/phases/106-medienkern-schema-legacy-abbau/106-08-SUMMARY.md` when done.
</output>
