---
phase: 106-medienkern-schema-legacy-abbau
plan: 07
type: execute
wave: 2
depends_on: [106-01]
files_modified:
  - frontend/src/app/covers/[file]/route.ts
  - frontend/src/app/api/admin/upload-cover/route.ts
  - frontend/src/lib/api.ts
  - frontend/src/app/admin/anime/components/AdminAnimeOverviewClient.tsx
  - frontend/src/lib/api.auth-refresh.test.ts
autonomous: true
requirements: [SC2]
must_haves:
  truths:
    - "SC2/D-04 Cluster F: Der Cover-Serve-Route-Handler `frontend/src/app/covers/[file]/route.ts` und der Upload-Cover-Route-Handler `frontend/src/app/api/admin/upload-cover/route.ts` sind entfernt."
    - "SC2/D-03 (P-7, RESEARCH-Refresh 2026-07-22 praezisiert): Die EINZIGE `upload-cover`-Clientfunktion `deleteUploadedCoverFile` (`api.ts:5936-5957`, DELETE gegen `/api/admin/upload-cover`) ist entfernt und ihr Aufrufer in `AdminAnimeOverviewClient.tsx` bereinigt — `npm run typecheck` bleibt gruen."
    - "P-7 (Korrektur): Eine POST-Upload-Cover-Clientfunktion EXISTIERT NICHT. `grep -n \"upload-cover\" frontend/src/lib/api.ts` liefert genau EINEN Treffer (:5937, im DELETE-Body von `deleteUploadedCoverFile`). Die Vorfassung wies an, zusaetzlich eine \"zugehoerige POST-Upload-Clientfunktion\" zu entfernen — der Executor haette gesucht, nichts gefunden und im schlimmsten Fall eine benachbarte Cover-Funktion mitgeloescht. Es wird GENAU EINE Funktion entfernt. Der POST-Handler in `route.ts` faellt mit der Datei, hat aber keinen api.ts-Client."
    - "D-03-Grenze: NICHT-build-breaking `/covers/`-URL-Konsumenten (FansubVersionBrowser resolveLogoUrl, PublicReleaseBlock-Tests) bleiben unangetastet — Phase-109/110-Scope."
    - "AGENTS.md „Auth Session Rules\" Zeile 89 (PFLICHT, Blocker 3 behoben): Phase 106 fasst Upload-Flows an (106-04 verkleinert das Upload-DTO, 106-03 editiert den upload-nahen Contract, dieser Plan entfernt eine Admin-Upload-Clientfunktion). Damit MUSS die Phase eine Regressions-/Sicherheitspruefung fuer den Fall tragen: ACCESS-TOKEN FEHLT ODER IST ABGELAUFEN, REFRESH-TOKEN GUELTIG → die geschuetzte Upload-Aktion laeuft ueber die zentrale Refresh-Naht in `api.ts` durch, OHNE Logged-out-Zustand und OHNE 401 an den Aufrufer. Dieser Fall ist in `frontend/src/lib/api.auth-refresh.test.ts` heute NICHT abgedeckt (107-RESEARCH.md:90 fuehrt ihn als BLOCKED): vorhanden sind near-expiry-Preflight (`seedRuntimeSessionExpiringSoon`), Refresh-Fehlschlag, Progress-Erhalt und „kein Replay nach Upload-401\" — der Upload mit fehlendem/abgelaufenem Access-Token fehlt."
    - "AGENTS.md Zeile 90: Der Fall ist zusaetzlich im `<threat_model>` dieses Plans (T-106-07-02/-03) und in den Verifikationskriterien von 106-08 gefuehrt, damit `gsd-secure-phase` und `gsd-verify-work` die Regression greifen koennen."
    - "Test-Basis (kein neues Harness): Der Nachweis ERWEITERT die bestehende Suite `frontend/src/lib/api.auth-refresh.test.ts` und nutzt deren etablierte Helfer (`seedRuntimeSessionMissingAccessToken`, `seedRuntimeSessionExpiredAccessToken`, `freshKeycloakBundle`, `makeCurrentUserResponse`, `MockUploadXhr`-Muster, `refreshKeycloakTokenMock`). Es wird KEIN eigener Mock-Stack und KEINE zweite Testdatei erfunden."
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "api.ts ohne upload-cover-Clientfunktionen"
      contains: "export"
    - path: "frontend/src/lib/api.auth-refresh.test.ts"
      provides: "Upload-Auth-Regression: fehlendes/abgelaufenes Access-Token + gueltiges Refresh-Token laeuft ueber die zentrale Refresh-Naht durch (AGENTS.md Auth Session Rules)"
      contains: "uploadAdminAnimeMedia"
  key_links:
    - from: "frontend/src/app/admin/anime/components/AdminAnimeOverviewClient.tsx"
      to: "kein deleteUploadedCoverFile-Aufruf"
      via: "bereinigter Orphan-Cover-Cleanup-Pfad"
      pattern: "deleteUploadedCoverFile"
    - from: "frontend/src/lib/api.auth-refresh.test.ts"
      to: "uploadAdminAnimeMedia ueber die zentrale Refresh-Naht"
      via: "seedRuntimeSessionMissingAccessToken + refreshKeycloakTokenMock-Preflight"
      pattern: "seedRuntimeSessionMissingAccessToken"
---

<objective>
Entfernt die build-breaking Frontend-Cover-Legacy (Cluster F, D-03): die zwei Next-Route-Handler (`/covers/[file]`, `/api/admin/upload-cover`), die zugehoerigen api.ts-Clientfunktionen und ihren einzigen Aufrufer in `AdminAnimeOverviewClient.tsx`. Alle uebrigen `/covers/`-URL-Konsumenten (nicht build-breaking) bleiben bewusst fuer Phase 109/110.

REVISION (RESEARCH-Refresh 2026-07-22, Befund P-7): Die Vorfassung wies an, neben `deleteUploadedCoverFile` auch eine "zugehoerige POST-Upload-Cover-Clientfunktion" zu entfernen. Diese Funktion existiert nicht (verifiziert: genau ein `upload-cover`-Treffer in `api.ts`). Die Anweisung ist auf die eine real existierende Funktion praezisiert und um einen Nachbarschaftsguard ergaenzt, damit nicht ersatzweise eine fremde Cover-Funktion mitgeloescht wird.

REVISION 2 (Plan-Checker-Blocker 3, 2026-07-22 — AGENTS.md „Auth Session Rules\"): Phase 106 fasst Upload-Flows an, traegt aber bisher keine der von AGENTS.md Zeile 89/90 verlangten Auth-Regressionspruefungen. Der Fall „Access-Token fehlt/abgelaufen + Refresh-Token gueltig → geschuetzte Upload-Aktion laeuft durch, ohne Logged-out-UI\" ist in `api.auth-refresh.test.ts` heute NICHT abgedeckt (107-RESEARCH.md:90 = BLOCKED). Dieser Plan schliesst die Luecke als Task 2, weil er der einzige Frontend-beruehrende Plan der Phase ist und `frontend/src/lib/` bereits besitzt.

Purpose: SC2-Anteil (FE) — nur build-breaking Teile, `npm run typecheck` gruen halten (D-03); zusaetzlich die AGENTS.md-pflichtige Upload-Auth-Regression.
Output: geloeschte Route-Handler, bereinigte api.ts + Admin-Client, erweiterte Auth-Refresh-Testsuite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-medienkern-schema-legacy-abbau/106-RESEARCH.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-PATTERNS.md

<interfaces>
<!-- Verifizierte FE-Call-Sites (RESEARCH Cluster F + grep). -->
- frontend/src/app/covers/[file]/route.ts — selbststaendiger GET-Handler (liest public/covers/<file>); loeschen.
- frontend/src/app/api/admin/upload-cover/route.ts — POST/DELETE-Handler; loeschen.
- frontend/src/lib/api.ts:5936-5957 `export async function deleteUploadedCoverFile(fileName: string): Promise<void>` —
  ruft `fetch("/api/admin/upload-cover", { method: "DELETE", headers, body: JSON.stringify({ file_name: fileName }) })`
  und wirft bei `!response.ok` einen `ApiError` ueber `parseApiErrorPayload`. Das ist die EINZIGE zu entfernende Funktion.
- P-7 (VERIFIZIERT 2026-07-22): Eine POST-Upload-Cover-Clientfunktion EXISTIERT NICHT.
  `grep -n "upload-cover" frontend/src/lib/api.ts` → genau EIN Treffer (:5937, innerhalb `deleteUploadedCoverFile`).
  NICHT nach einer POST-Variante suchen und auf keinen Fall eine benachbarte Cover-Funktion "ersatzweise" mitloeschen.
  Direkt darueber und darunter stehen fremde Funktionen (u.a. `getAdminGenreTokens` ab :5959) — sie bleiben unveraendert.
- frontend/src/app/admin/anime/components/AdminAnimeOverviewClient.tsx:11 (Import) + :138 (`await deleteUploadedCoverFile(orphanedCoverImage)`) — Aufrufer/Import bereinigen.
- NICHT anfassen (D-03, Phase 109/110, nicht build-breaking):
  frontend/src/components/fansubs/FansubVersionBrowser.tsx:68 `resolveLogoUrl` gibt `/covers/${value}` zurueck;
  frontend/src/components/fansubs/__tests__/PublicReleaseBlock.test.tsx `/covers/placeholder.jpg`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cover-Route-Handler loeschen + api.ts-Client + Aufrufer bereinigen</name>
  <files>frontend/src/app/covers/[file]/route.ts, frontend/src/app/api/admin/upload-cover/route.ts, frontend/src/lib/api.ts, frontend/src/app/admin/anime/components/AdminAnimeOverviewClient.tsx</files>
  <read_first>
    - frontend/src/lib/api.ts:5925-5965 (Funktionsgrenzen von `deleteUploadedCoverFile` sowie die davor/danach stehenden fremden Funktionen als Abgrenzung — P-7: es gibt KEINE POST-Geschwister-Funktion)
    - frontend/src/app/admin/anime/components/AdminAnimeOverviewClient.tsx:1-20 + :125-150 (Import + Orphan-Cover-Cleanup-Nutzung)
    - frontend/src/app/covers/[file]/route.ts (bestaetigen: selbststaendig, kein Consumer importiert es)
  </read_first>
  <action>Loesche die Route-Handler-Dateien `frontend/src/app/covers/[file]/route.ts` und `frontend/src/app/api/admin/upload-cover/route.ts` (ggf. leer werdende Verzeichnisse `covers/[file]`, `api/admin/upload-cover` mit entfernen). Entferne in `frontend/src/lib/api.ts` GENAU EINE Funktion: `deleteUploadedCoverFile` (~:5936-5957, vom `export async function`-Kopf bis zur schliessenden Klammer). Sie ist der einzige `upload-cover`-Client (P-7, verifiziert: ein einziger `upload-cover`-Treffer in der Datei). Suche NICHT nach einer POST-Upload-Cover-Clientfunktion — es gibt keine — und loesche keine benachbarte Cover-Funktion ersatzweise; die direkt folgende `getAdminGenreTokens` (~:5959) und alles davor bleiben unveraendert. In `AdminAnimeOverviewClient.tsx` den Import (:11) und die Nutzung (~:138, Orphan-Cover-Cleanup) entfernen; falls dadurch eine lokale Variable/ein Zweig tot wird (z. B. `orphanedCoverImage`-Ermittlung), diesen Zweig sauber aufloesen, ohne andere Funktionalitaet zu aendern. NICHT anfassen: FansubVersionBrowser.tsx (`resolveLogoUrl`/`/covers/`) und die PublicReleaseBlock-Tests — D-03-Phase-109/110-Scope, nicht build-breaking. `npm run typecheck` gruen halten.</action>
  <acceptance_criteria>
    - `frontend/src/app/covers/[file]/route.ts` und `frontend/src/app/api/admin/upload-cover/route.ts` existieren nicht mehr.
    - `grep -rn "deleteUploadedCoverFile" frontend/src/lib frontend/src/app --include=*.ts --include=*.tsx` liefert KEINE Treffer.
    - `grep -rn "upload-cover" frontend/src/lib frontend/src/app --include=*.ts --include=*.tsx` liefert KEINE Treffer.
    - P-7-Umfangsgrenze: `git diff --stat frontend/src/lib/api.ts` zeigt ausschliesslich ENTFERNTE Zeilen und deren Anzahl liegt zwischen 20 und 26 (die eine Funktion inkl. umgebender Leerzeile) — ein groesserer Diff bedeutet, dass eine benachbarte Funktion mitgeloescht wurde.
    - P-7-Nachbarschaftsguard: `grep -c "export async function getAdminGenreTokens" frontend/src/lib/api.ts` == 1 (die direkt folgende Funktion ist unversehrt).
    - `frontend/src/components/fansubs/FansubVersionBrowser.tsx` ist unveraendert (enthaelt weiterhin `resolveLogoUrl`).
    - `cd frontend && npm run typecheck` gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run typecheck && [ ! -f "src/app/covers/[file]/route.ts" ] && [ ! -f src/app/api/admin/upload-cover/route.ts ] && ! grep -rq "deleteUploadedCoverFile\|upload-cover" src/lib src/app --include=*.ts --include=*.tsx && grep -q "export async function getAdminGenreTokens" src/lib/api.ts && echo CLUSTER-F-OK</automated>
  </verify>
  <done>Beide Cover-Route-Handler geloescht, api.ts-Clientfunktionen + Aufrufer entfernt, deferred /covers-Konsumenten unberuehrt, typecheck gruen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upload-Auth-Regression — fehlendes/abgelaufenes Access-Token + gueltiges Refresh-Token (AGENTS.md Auth Session Rules)</name>
  <files>frontend/src/lib/api.auth-refresh.test.ts</files>
  <read_first>
    - frontend/src/lib/api.auth-refresh.test.ts:1-140 (Harness-Kopf: `vi.mock('@/lib/keycloakAuth')`, `makeResponse`, `seedRuntimeSession`, `seedRuntimeSessionMissingAccessToken` :56-69, `seedRuntimeSessionExpiredAccessToken` :73-88, `seedRuntimeSessionExpiringSoon` :90-104, `freshKeycloakBundle`, `makeCurrentUserResponse`, `beforeEach`/`afterEach`)
    - frontend/src/lib/api.auth-refresh.test.ts:235-298 (die beiden bestehenden D-20-Tests fuer fehlendes bzw. abgelaufenes Access-Token — sie belegen das Muster fuer NICHT-Upload-Requests und sind die Vorlage)
    - frontend/src/lib/api.auth-refresh.test.ts:528-660 (die drei bestehenden Upload-Tests: „does not automatically replay unsafe upload requests after an upload 401", „preflights upload auth with refresh and preserves progress callbacks" mit der `MockUploadXhr`-Klasse, „rejects unsafe uploads before opening XHR when preflight refresh fails")
    - AGENTS.md Abschnitt „Auth Session Rules" (Zeilen 84-90) — Wortlaut der Pflichtpruefung
    - .planning/phases/107-vereinheitlichte-upload-pipeline-mediafileservice/107-RESEARCH.md (Zeile ~90: dieser Fall ist als BLOCKED/ungedeckt erfasst)
  </read_first>
  <behavior>
    - Test A („upload with a missing access token refreshes through the central seam"): `seedRuntimeSessionMissingAccessToken()` → `refreshKeycloakTokenMock` liefert `freshKeycloakBundle()`; `uploadAdminAnimeMedia({animeID, assetType:'poster', file})` mit dem bestehenden `MockUploadXhr`-Muster (status 201, JSON-Body wie im Preflight-Test) RESOLVED. Erwartungen: genau EIN `refreshKeycloakToken`-Aufruf mit `'refresh-token-1'`; die XHR traegt `Authorization: 'Bearer new-access-token'`; `getAuthSessionSnapshot()` ist danach `{hasAccessToken:true, hasRefreshToken:true}`; VOR dem Upload gilt `{hasAccessToken:false, hasRefreshToken:true}` — der fehlende Access-Token wird also nachweislich NICHT als Logged-out gewertet; es wird KEIN 401 an den Aufrufer durchgereicht.
    - Test B („upload with an already-expired access token refreshes through the central seam"): identisch, aber `seedRuntimeSessionExpiredAccessToken()`. Gleiche Erwartungen; zusaetzlich: `MockUploadXhr.instances` hat Laenge 1 (kein Doppel-Request, kein Replay).
    - Negativ-Abgrenzung (KEINE Verhaltensaenderung an bestehenden Tests): „does not automatically replay unsafe upload requests after an upload 401" bleibt woertlich unveraendert — ein Upload-401 wird weiterhin NICHT automatisch wiederholt. Die neuen Tests decken den PREFLIGHT-Fall ab, nicht den Replay-Fall.
  </behavior>
  <action>Erweitere AUSSCHLIESSLICH `frontend/src/lib/api.auth-refresh.test.ts` um die zwei in `<behavior>` beschriebenen Tests. Fuege sie im bestehenden `describe('authorized auth refresh flow', …)`-Block unmittelbar NACH dem Test „preflights upload auth with refresh and preserves progress callbacks" ein, damit die Upload-Faelle beieinanderstehen. Verwende AUSSCHLIESSLICH die bereits vorhandenen Helfer des Harness — `seedRuntimeSessionMissingAccessToken`, `seedRuntimeSessionExpiredAccessToken`, `freshKeycloakBundle`, `makeCurrentUserResponse`, `getAuthSessionSnapshot`, `refreshKeycloakTokenMock`, `uploadAdminAnimeMedia` — und kopiere die `MockUploadXhr`-Klasse aus dem Preflight-Test (Zeilen ~578-610) in den jeweiligen Testkoerper, wie es die bestehende Datei fuer jeden Upload-Test tut. Baue KEIN neues Mock-Framework, KEINE zweite Testdatei, KEINEN globalen Helfer. Aendere KEINEN Produktivcode: falls einer der beiden Tests rot ist, ist das ein ECHTER Auth-Defekt — Task rot lassen und melden, NICHT `api.ts` reparieren (Produktivaenderung an der Auth-Naht ist nicht Scope dieser reinen Legacy-/Schema-Phase). Aendere KEINEN bestehenden Test dieser Datei, insbesondere nicht den 401-Replay-Test. `npm run typecheck` muss gruen bleiben.</action>
  <acceptance_criteria>
    - `grep -c "seedRuntimeSessionMissingAccessToken" frontend/src/lib/api.auth-refresh.test.ts` >= 2 (Helfer-Definition + mindestens eine neue Upload-Nutzung).
    - `grep -c "seedRuntimeSessionExpiredAccessToken" frontend/src/lib/api.auth-refresh.test.ts` >= 2 (dito).
    - Die zwei neuen Tests referenzieren `uploadAdminAnimeMedia` — `grep -c "uploadAdminAnimeMedia" frontend/src/lib/api.auth-refresh.test.ts` >= 5 (drei bestehende Upload-Tests + zwei neue).
    - Der bestehende Replay-Guard ist unveraendert: `grep -c "does not automatically replay unsafe upload requests after an upload 401" frontend/src/lib/api.auth-refresh.test.ts` == 1.
    - `cd frontend && npx vitest run src/lib/api.auth-refresh.test.ts` gruen; die Ausgabe enthaelt beide neuen Testnamen.
    - Umfangsgrenze: `git diff --name-only frontend/src/lib/` listet in dieser Task AUSSCHLIESSLICH `frontend/src/lib/api.auth-refresh.test.ts` — insbesondere KEINE Aenderung an `frontend/src/lib/api.ts` (die Auth-Naht selbst ist nicht Scope dieser Phase).
    - `cd frontend && npm run typecheck` gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/api.auth-refresh.test.ts && npm run typecheck && [ "$(grep -c 'seedRuntimeSessionMissingAccessToken' src/lib/api.auth-refresh.test.ts)" -ge 2 ] && [ "$(grep -c 'seedRuntimeSessionExpiredAccessToken' src/lib/api.auth-refresh.test.ts)" -ge 2 ] && [ "$(grep -c 'uploadAdminAnimeMedia' src/lib/api.auth-refresh.test.ts)" -ge 5 ] && [ "$(grep -c 'does not automatically replay unsafe upload requests after an upload 401' src/lib/api.auth-refresh.test.ts)" = "1" ] && [ -z "$(git diff --name-only -- src/lib/ | grep -v 'api.auth-refresh.test.ts')" ] && echo AUTH-UPLOAD-REGRESSION-OK</automated>
  </verify>
  <done>Die von AGENTS.md Zeile 89 verlangte Regression ist automatisiert belegt: fehlendes UND abgelaufenes Access-Token mit gueltigem Refresh-Token lassen den geschuetzten Upload ueber die zentrale Refresh-Naht durchlaufen, ohne Logged-out-Zustand und ohne 401 an den Aufrufer; der bestehende 401-Replay-Guard ist unveraendert; kein Produktivcode angefasst.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
`frontend/src/lib/api.ts` ↔ Keycloak-Refresh-Naht ↔ `POST /api/admin/upload`: hier ueberquert Auth-State die Grenze zwischen Browser-Session und geschuetztem Admin-Endpoint. Phase 106 verschiebt diese Grenze nicht — sie wird hier ausschliesslich per Regressionstest abgesichert (AGENTS.md Auth Session Rules).

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-07-01 | Denial of Service | typecheck bricht durch verwaisten Aufrufer | mitigate | Aufrufer in AdminAnimeOverviewClient mit entfernt; `npm run typecheck` als Gate. |
| T-106-07-02 | Denial of Service / Spoofing | AGENTS.md Z. 89: Ein fehlendes oder abgelaufenes Access-Token bei gueltigem Refresh-Token wird von einer geschuetzten Upload-Aktion faelschlich als „logged out" behandelt → Admin verliert die Session mitten im Upload; im schlimmsten Fall Re-Login-Aufforderung als Phishing-Oberflaeche | mitigate | Task 2 fuegt zwei Regressionstests in `api.auth-refresh.test.ts` ein (missing + expired Access-Token, gueltiger Refresh): der Upload MUSS ueber die zentrale Refresh-Naht durchlaufen, genau EIN `refreshKeycloakToken`-Aufruf, XHR traegt das frische Bearer-Token, `getAuthSessionSnapshot()` bleibt aktiv. Gate ist automatisiert (`vitest run src/lib/api.auth-refresh.test.ts`) und zusaetzlich in 106-08 Task 1 als Phasen-Gate gespiegelt. |
| T-106-07-03 | Repudiation | Umgehung der zentralen Refresh-Naht: Testcode oder UI-Code liest Auth-Cookies direkt bzw. baut Bearer-Header selbst (AGENTS.md Z. 87) | mitigate | Die neuen Tests treiben ausschliesslich `uploadAdminAnimeMedia` und assertieren den Header, den `api.ts` selbst gesetzt hat; `refreshKeycloakToken` ist gemockt und wird nur ueber die Naht aufgerufen. Umfangsgrenze: `git diff --name-only frontend/src/lib/` darf in Task 2 nur die Testdatei listen — eine Aenderung an `api.ts` waere ein Scope-Verstoss und macht die Task rot. |
| T-106-07-SC | Tampering | Package-Installs | accept | Keine Installs. |
Der entfernte `/covers/`-Serve-Handler hatte einen Pfad-Traversal-Guard (`isSafeFileName`); mit Entfernung entfaellt die Flaeche ganz — keine Regression.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/api.auth-refresh.test.ts` gruen inkl. der beiden neuen Upload-Auth-Regressionstests (AGENTS.md Z. 89/90).
- `cd frontend && npm run typecheck` gruen.
- Beide Route-Handler-Dateien abwesend; `deleteUploadedCoverFile`/`upload-cover` in src/lib+src/app leer.
- FansubVersionBrowser/PublicReleaseBlock-Tests unveraendert (deferred).
</verification>

<success_criteria>
SC2-Teil (FE) erfuellt: build-breaking Cover-Legacy entfernt, typecheck gruen, D-03-Grenze gewahrt (Rest-`/covers/`-Konsumenten deferred). AGENTS.md-Auth-Pflicht erfuellt: fehlendes/abgelaufenes Access-Token bei gueltigem Refresh-Token laeuft im Upload-Flow nachweislich ueber die zentrale Refresh-Naht durch, ohne Logged-out-UI.
</success_criteria>

<output>
Create `.planning/phases/106-medienkern-schema-legacy-abbau/106-07-SUMMARY.md` when done. Dokumentiere explizit die zwei neuen Auth-Regressionstests (AGENTS.md Zeile 89/90) samt ihrer Testnamen, damit `gsd-secure-phase`/`gsd-verify-work` sie referenzieren koennen.
</output>
