---
phase: 106-medienkern-schema-legacy-abbau
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/media-core-contract-check.ps1
  - scripts/media-core-legacy-grep.ps1
autonomous: true
requirements: [SC3, SC4]
must_haves:
  truths:
    - "SC3: Ein an `scripts/schema-v2-audit.ps1` angelehnter Contract-Check belegt gegen die Live-DB, dass `media`/`media_variant` vorhanden und `release_media`/`anime.cover_image` abwesend sind."
    - "SC1-Negativ: Der Contract-Check schlaegt fehl, falls `media` eine der verbotenen Spalten caption/visibility_id/review_status_id/category/sort_order traegt."
    - "Grenzbefund A1: `media_assets`/`media_files` bleiben als erwartet-vorhanden gelistet (NICHT als legacy)."
    - "SC4: Eine grep-Suite meldet verbleibende Referenzen auf die entfernten Legacy-Symbole/Routen; Scope schliesst `*.exe`/`*.log`/`*.md`/`.planning`/`node_modules` aus (False-Positive-Schutz)."
    - "SC4/Blocker 2: Die `release_media`-Suche ist auf NON-TEST-Live-Code beschraenkt (Test-Guards bleiben intentional retained) und faengt dennoch Live-Code-Residuen (media_upload.go INSERT, ListReleaseAssets-Query, anime_assets-UNION, admin_content_anime_delete NOT-EXISTS). Die 6 bewusst behaltenen Test-Referenzen (app_auth_test, member_profile_repository_test, group_contributors_repository_test, runtime_authority_test, theme_segment_render_cache_test, segment_validation_test) sind explizit ausgenommen (Exclude `*_test.go` oder Allowlist). Zusaetzlich (106-05 Option A): sowohl der bewusst behaltene release_media READ-Pfad (`ListReleaseAssets` in episode_version_repository_read_helpers.go) ALS AUCH der WRITE-Pfad (`CreateReleaseMedia` Interface+Impl inkl. `INSERT INTO release_media` + `case \"release\"` in media_upload_storage.go:54) werden allowlisted (spiegelbildlich); unter Option B/C sind beide entfernt (0 Live-Treffer)."
    - "D-03-Scope: Die grep-Suite prueft KEINE blanket `/covers/`-URL-Strings (die in Phase-109-deferred FE wie FansubVersionBrowser + Tests weiterleben) — nur die entfernten Route-Handler-Dateien + `upload-cover`-Symbole."
  artifacts:
    - path: "scripts/media-core-contract-check.ps1"
      provides: "psql-basierter Schema-Contract-Check mit -FailOnContractGaps-Gate"
      contains: "FailOnContractGaps"
    - path: "scripts/media-core-legacy-grep.ps1"
      provides: "SC4-Rest-Referenz-Scan ueber entfernte Symbole/Routen (Doku/Binaries/Test-Guards ausgeschlossen)"
      contains: "cover_image"
  key_links:
    - from: "scripts/media-core-contract-check.ps1"
      to: "team4sv30-db (psql)"
      via: "docker compose exec Invoke-LocalPsql"
      pattern: "docker compose exec"
---

<objective>
Erstellt die zwei SC3/SC4-Gate-Skripte, die in Plan 106-08 als Phasen-Gate ausgefuehrt werden: (1) `media-core-contract-check.ps1` (Live-DB-Audit, Legacy-Freiheit + Zielschema), (2) `media-core-legacy-grep.ps1` (Rest-Referenz-Scan). Beide werden hier authored, nicht ausgefuehrt (die DB ist erst nach Plan 106-06 legacy-frei) — sie sind die praeregistrierten Gates.

Purpose: SC3-Contract-Check und SC4-grep-Beleg als reproduzierbare, versionierte Gates.
Output: zwei PowerShell-Skripte unter `scripts/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-medienkern-schema-legacy-abbau/106-RESEARCH.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-PATTERNS.md

<interfaces>
<!-- Analog: scripts/schema-v2-audit.ps1 (304 Z., reale Logik) — Struktur 1:1 uebernehmen, Listen tauschen. -->
- DB-Identitaets-Guard + psql-Helper (schema-v2-audit.ps1:8–22): `Invoke-LocalPsql` via
  `docker compose exec -T team4sv30-db psql -U team4s -d team4s_v2 ...`; harte Pruefung
  `current_database(),current_user == team4s_v2 / team4s`, sonst `throw`.
- Hashmap-Introspektion (:24–90): information_schema.tables/.columns + pg_constraint (pg_get_constraintdef).
- Present/Legacy-Assertion + Fail-Gate (:161–184/232–237/295–303): `$contractPassed = missing==0 && blocking==0`;
  `if ($FailOnContractGaps -and -not $contractPassed) { throw }`.
- WARNUNG: NICHT die `$targetTables`-Liste des v2-Audits wiederverwenden — sie erwartet `release_media` als vorhanden
  (wuerde den 106-Drop faelschlich als „missing" failen).
- BLOCKER-2-SCOPE (release_media grep): `release_media` lebt INTENTIONAL in 6 Test-Dateien weiter, die KEIN 106-Plan
  aendert — app_auth_test.go, member_profile_repository_test.go (:295 Negativ-Assertion), group_contributors_repository_test.go,
  runtime_authority_test.go (positive Assertion :161-163, umgestellt in 106-05), theme_segment_render_cache_test.go
  (:26 Input-String + :95/:97 forbidden-Needle-Liste, KEIN DB-SQL), segment_validation_test.go. Die grep-Suite darf diese
  NICHT als Verstoss werten (Exclude `*_test.go` fuer den release_media-Term, oder explizite Allowlist dieser Dateien),
  MUSS aber Live-Code-Residuen weiter fangen: media_upload.go INSERT (entfernt in 106-04), ListReleaseAssets-Query,
  anime_assets-UNION, admin_content_anime_delete NOT-EXISTS (entfernt/behandelt in 106-05).
- SCOPE-Klaerung (verifiziert): `frontend/src/components/fansubs/FansubVersionBrowser.tsx` konstruiert weiter
  `/covers/${value}` (Logo-Fallback) und Tests referenzieren `/covers/`-URLs — das ist NICHT build-breaking und
  laut D-03 Phase-109-deferred. Der 106-SC4-grep darf blanket `/covers/`-URL-Strings NICHT als Verstoss werten;
  das gehoert zum Phase-110-E2E-Gate (ROADMAP 110-SC4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contract-Check-Skript media-core-contract-check.ps1</name>
  <files>scripts/media-core-contract-check.ps1</files>
  <read_first>
    - scripts/schema-v2-audit.ps1 (komplett — Guard/Helper/Hashmaps/Fail-Gate spiegeln)
    - 106-PATTERNS.md Abschnitt „media-core-contract-check.ps1" (Anpassungsliste)
    - 106-RESEARCH.md Abschnitt „Contract-Check-Analyse (SC3)"
  </read_first>
  <action>Neues Skript analog `schema-v2-audit.ps1`, Struktur 1:1: `Invoke-LocalPsql`-Helper + harter `team4s_v2`/`team4s`-Identitaetsguard + Hashmap-Introspektion (`$tables`, `$columns`, `$constraints`). Param `-FailOnContractGaps`. Assertions: (a) MUSS-vorhanden — Tabelle `media` (Spalten kind/storage_key/content_hash/source/processing_status/owner_user_id/owner_member_id + CHECK-Constraints chk_media_kind/source/processing_status) und `media_variant` (`media_id`-FK mit `ON DELETE CASCADE` via pg_get_constraintdef + CHECK chk_media_variant_variant/status); (b) MUSS-abwesend — Tabelle `release_media`, Spalte `anime.cover_image`; (c) NEUE Negativ-Assertion (verbotene Medium-Spalten): Fail wenn `$columns["media"]` eine von caption/visibility_id/review_status_id/category/sort_order enthaelt; (d) `media_assets`/`media_files` bleiben als erwartet-vorhanden (NICHT in Legacy-Liste). `$contractPassed`-Gate + `throw` bei `-FailOnContractGaps`. Ziel < 450 Zeilen (Analog 304). Kein produktiver App-Code beruehrt. HINWEIS: Falls in 106-05 Endpoint-Option A gewaehlt wird (release_media-Drop zurueckgenommen), meldet 106-05 das zurueck — dann darf `release_media` NICHT als MUSS-abwesend gefuehrt werden. Default-Annahme dieses Skripts: release_media MUSS-abwesend (Option B/C).</action>
  <acceptance_criteria>
    - `scripts/media-core-contract-check.ps1` existiert, enthaelt `-FailOnContractGaps`-Param und den `team4s_v2`/`team4s`-Identitaetsguard.
    - Skript listet `release_media` als MUSS-abwesend und `anime.cover_image` als MUSS-abwesend.
    - Skript listet `media_assets` NICHT als Legacy-/zu-loeschen-Tabelle.
    - `powershell -NoProfile -Command "$null = [ScriptBlock]::Create((Get-Content -Raw scripts/media-core-contract-check.ps1))"` parst fehlerfrei (Syntax-Check).
    - Datei < 450 Zeilen.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -Command "$null=[ScriptBlock]::Create((Get-Content -Raw 'scripts/media-core-contract-check.ps1')); Write-Output PARSE-OK"</automated>
  </verify>
  <done>Contract-Check-Skript existiert, syntaktisch valide, korrekte Ziel-/Legacy-/Verboten-Listen.</done>
</task>

<task type="auto">
  <name>Task 2: SC4-grep-Suite media-core-legacy-grep.ps1</name>
  <files>scripts/media-core-legacy-grep.ps1</files>
  <read_first>
    - 106-RESEARCH.md Abschnitt „Wave 0 Gaps" (Symbol-/Routen-Liste)
    - 106-PATTERNS.md Abschnitt „Enrichment / Risiko-Befunde" (cover_image Spalte vs. DTO-Feld)
    - CLAUDE.md (grep-Scope-Hinweise, Docker-Backend)
  </read_first>
  <action>PowerShell-Skript, das ueber das Repo nach verbleibenden Legacy-Referenzen sucht und mit Exit-Code != 0 endet, falls Treffer bestehen. Suchbegriffe (entfernte Symbole/Routen): `SupportsLegacyUploadSchema`, `useLegacyUploadSchema`, `legacyUploadSchemaDetector`, `shouldUseAnimePosterPathFallback`, `asset_lifecycle`, `episode_version_image`, `ListReleaseImages`, `/releases/:id/images`, `CreateReleaseMedia`, `migrate-covers`, `upload-cover`, `deleteUploadedCoverFile`. ENTSCHEIDUNGS-HINWEIS: `CreateReleaseMedia` (und der Aufrufer `case \"release\"`) ist — wie `release_media` — an die 106-05-Option gekoppelt; unter Option A bleibt der Write-Path BEWUSST bestehen und MUSS dann allowlisted werden (kein False-Fail), unter Option B/C 0 Treffer erwartet.
    BLOCKER-2-SCOPING fuer den `release_media`-Term (word-boundary, NICHT `release_version_media`): Beschraenke die `release_media`-Suche auf NON-TEST-Live-Code — schliesse `*_test.go` aus (oder fuehre eine explizite Allowlist der 6 bewusst behaltenen Test-Dateien: app_auth_test.go, member_profile_repository_test.go, group_contributors_repository_test.go, runtime_authority_test.go, theme_segment_render_cache_test.go, segment_validation_test.go). So faellt die Suite NICHT auf intentional retained Test-Guards, faengt aber weiter Live-Code-Residuen (media_upload.go INSERT, ListReleaseAssets-Query in episode_version_repository_read_helpers.go, anime_assets-UNION, admin_content_anime_delete NOT-EXISTS). WICHTIG: Nach 106-05 Endpoint-Option A bleiben ZWEI release_media-Pfade BEWUSST bestehen — der READ-Pfad `ListReleaseAssets` (episode_version_repository_read_helpers.go, gemeldet von 106-05) UND der WRITE-Pfad `CreateReleaseMedia`/`INSERT INTO release_media` (media_upload.go) + `case \"release\"` (media_upload_storage.go:54, gemeldet von 106-04). In dem Fall muss die release_media-Live-Suche BEIDE Pfade allowlisten (kein False-Fail); Default (Option B/C): 0 Live-Treffer erwartet (Read- UND Write-Pfad entfernt).
    Fuer die entfernten Cover-Serve-/Upload-Routen pruefe die DATEI-ABWESENHEIT von `frontend/src/app/covers/[file]/route.ts` und `frontend/src/app/api/admin/upload-cover/route.ts` — NICHT einen blanket `/covers/`-URL-String-Scan (D-03: FansubVersionBrowser + Tests behalten `/covers/`-URLs bis Phase 109/110). Fuer `cover_image` MUSS zwischen Spalte und abgeleitetem DTO-Feld getrennt werden: suche nur nach den Spalten-Signaturen (`animeCoverImageSelectSQL`, `syncLegacyAnimeCoverImageV2`, `HasCoverImage`), NICHT pauschal `cover_image` (das JSON-DTO-Feld bleibt gueltig). Scope-Ausschluss (alle Terme): `*.exe`, `*.log`, `*.md`, `.git`, `node_modules`, `.planning/`. Ausgabe: pro Begriff Trefferliste; finales `exit 1` wenn irgendein Begriff noch matcht bzw. eine der Route-Handler-Dateien noch existiert.</action>
  <acceptance_criteria>
    - `scripts/media-core-legacy-grep.ps1` existiert und parst syntaktisch fehlerfrei.
    - Skript schliesst explizit `*.exe`, `*.log`, `*.md`, `.planning/`, `node_modules` aus.
    - Skript sucht `release_media` word-boundary-abgegrenzt gegen `release_version_media` UND scopet den Term auf NON-TEST-Live-Code (Exclude `*_test.go` oder Allowlist der 6 bekannten Test-Dateien).
    - Skript faengt Live-Code-Residuen (Nachweis: eine bewusst eingefuegte Live-`release_media`-Zeile wuerde als Treffer gemeldet).
    - Skript trennt `cover_image`-Spalte (verboten) von DTO-Feld (erlaubt) — kein pauschales `cover_image`.
    - Skript prueft Route-Handler-Datei-Abwesenheit statt blanket `/covers/`-URL-Scan.
    - Skript endet mit `exit 1` bei Resttreffern, `exit 0` bei Sauberkeit.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -Command "$null=[ScriptBlock]::Create((Get-Content -Raw 'scripts/media-core-legacy-grep.ps1')); Write-Output PARSE-OK"</automated>
  </verify>
  <done>grep-Suite existiert, korrekt gescopet (Doku/Binaries/Test-Guards aus, /covers-URL nicht blanket), Spalte-vs-DTO getrennt, faengt Live-Residuen, Exit-Code-Gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Keine. Read-only Introspektion (Contract-Check) bzw. statischer Repo-Scan (grep).

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-02-01 | Information Disclosure | psql gegen falsche DB | mitigate | Harter Identitaetsguard `team4s_v2`/`team4s` vom Analog uebernommen; `throw` bei Abweichung. |
| T-106-02-02 | Repudiation | grep-Suite false-fail auf intentional retained Test-Guards maskiert echten Zustand | mitigate | release_media-Term auf NON-TEST-Live-Code gescopet + Allowlist der 6 bekannten Test-Dateien; Live-Residuen weiter gefangen. |
| T-106-02-SC | Tampering | Package-Installs | accept | Keine Installs — kein Supply-Chain-Vektor. |
Keine neue Angriffsflaeche; Endpoint-Haertung erst Phase 108.
</threat_model>

<verification>
- Beide Skripte parsen fehlerfrei (ScriptBlock-Syntax-Check).
- Contract-Check-Skript enthaelt korrekte MUSS-vorhanden/MUSS-abwesend/Verboten-Listen.
- grep-Suite scope-korrekt (Doku/Binaries/Test-Guards aus, /covers-URL nicht blanket) und faengt Live-Residuen.
</verification>

<success_criteria>
SC3-Tooling: Contract-Check-Skript existiert (Ausfuehrung als Gate in 106-08). SC4-Tooling: grep-Suite existiert (Ausfuehrung als Gate in 106-08), release_media-Scope auf Live-Code beschraenkt.
</success_criteria>

<output>
Create `.planning/phases/106-medienkern-schema-legacy-abbau/106-02-SUMMARY.md` when done.
</output>
