---
phase: 106-medienkern-schema-legacy-abbau
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/media-core-contract-check.ps1
  - scripts/media-core-legacy-grep.ps1
autonomous: true
requirements: [SC3, SC4]
must_haves:
  truths:
    - "SC3: Ein an `scripts/schema-v2-audit.ps1` angelehnter Contract-Check belegt gegen die Live-DB, dass `media`/`media_variant` vorhanden und `anime.cover_image` abwesend sind."
    - "SC1-Negativ: Der Contract-Check schlaegt fehl, falls `media` eine der verbotenen Spalten caption/visibility_id/review_status_id/category/sort_order traegt."
    - "Grenzbefund A1: `media_assets`/`media_files` bleiben als erwartet-vorhanden gelistet (NICHT als legacy)."
    - "D-07 (LOCKED): `release_media` wird vom Contract-Check als ERWARTET-VORHANDEN gefuehrt (neben media_assets/media_files) — ein versehentlicher Drop in Phase 106 faellt damit als Contract-Gap auf. `release_media` ist NICHT MUSS-abwesend."
    - "SC4/D-07: Die grep-Suite kennt die Suchbegriffe `release_media` und `CreateReleaseMedia` GAR NICHT — sie werden in Phase 106 nicht entfernt, also ist die Suche danach falsch. Damit entfaellt auch jede Allowlist/Test-Guard-Sonderbehandlung fuer release_media."
    - "SC4: Die grep-Suite meldet verbleibende Referenzen auf die tatsaechlich in 106 entfernten Legacy-Symbole/Routen; sie scannt AUSSCHLIESSLICH die vier Quell-Wurzeln `backend/internal`, `backend/cmd`, `frontend/src`, `shared/contracts` (Checker-Blocker 2, behoben — ein Repo-Root-Scan konnte per Konstruktion nie Exit 0 liefern)."
    - "P-4 (RESEARCH-Refresh 2026-07-22, behoben): Der frueher gefuehrte Suchbegriff `episode_version_image` (snake_case) hat in den vier Scan-Wurzeln NULL Content-Treffer — Go/TS verwenden durchgaengig CamelCase. Er ist durch `EpisodeVersionImage` ERSETZT (verifizierte Treffer: `backend/cmd/server/main.go`(3), `handlers/episode_version_images_handler.go`(8), `models/episode_version_image.go`(9), `repository/episode_version_image_repository.go`(12), `shared/contracts/openapi.yaml`(4) — alle 106-03 zugeordnet). Ein Term, der per Konstruktion nie feuern kann, beweist nichts und wird nicht beibehalten."
    - "P-4-Ergaenzung: Zusaetzlich zur Symbolsuche prueft die Suite die DATEI-ABWESENHEIT der drei Go-Dateien der Strecke (`backend/internal/handlers/episode_version_images_handler.go`, `backend/internal/repository/episode_version_image_repository.go`, `backend/internal/models/episode_version_image.go`) — analog zur bereits etablierten Route-Handler-Abwesenheitsregel fuer `/covers/` und `upload-cover`."
    - "P-4/A1 (Case-Verhalten explizit): Die Suite laeuft CASE-INSENSITIV (PowerShell `Select-String`-Default) und dokumentiert das als bewusste Entscheidung im Skript-Kopf. Begruendung: (a) `shared/contracts/openapi.yaml` fuehrt `operationId: listReleaseImages` in Kleinschreibung des Anfangsbuchstabens — nur case-insensitiv erfasst; (b) `runtime_authority_test.go` normalisiert seine Assertion-Fragmente per `strings.ToLower`, Legacy-Symbole erscheinen dort also klein. Die Trefferlisten in diesem Plan wurden entsprechend mit `grep -rni` ermittelt (nicht mit case-sensitivem `grep`) — genau diese Diskrepanz war die Ursache von P-3 und P-4."
    - "P-3 (RESEARCH-Refresh 2026-07-22, BLOCKER behoben — Scope bleibt): `shared/contracts` BLEIBT SC4-Scan-Wurzel. Die zuvor unzugeordneten Contract-Treffer (`listReleaseImages` :6526, Pfad :6522, `$ref` :6563, Schemas :12612/:12655/:12663) sind per PO-Entscheid Plan 106-03 Task 3 zugewiesen, der die Release-Images-Strecke aus `openapi.yaml` entfernt. Die Alternative — `shared/contracts` aus dem Scan zu nehmen — wurde VERWORFEN, weil damit auch die `asset_lifecycle`-Contract-Pruefung von 106-06 verloren ginge."
    - "SC4/Scope-Begruendung (Checker-Blocker 2): `database/migrations/**` ist bewusst AUSSERHALB des Scans — die Altmigrationen `0018_episode_version_images.*` und `0046_drop_legacy_episode_versions.*` bleiben laut D-01/A4 unveraendert im Repo und tragen den Term `episode_version_image` dauerhaft. Ebenso ausserhalb: `scripts/**` — `reset-local-schema-cutover-data.ps1` und `schema-v2-audit.ps1` tragen `episode_version_image`, werden von KEINEM 106-Plan angefasst (Phase-110-Scope) und die Gate-Skripte selbst wuerden sich sonst selbst treffen."
    - "SC4/cover_image (Checker-Warning 2, behoben): Die grep-Suite MUSS `anime.cover_image`-Sauberkeit aktiv beweisen — sie scannt nach SQL-SPALTEN-SIGNATUREN (`a\\.cover_image`, `anime\\.cover_image`, `,\\s*cover_image`, `cover_image\\s*=`, `SET cover_image`) und schliesst dabei das legitime DTO-Feld aus (`json:\"cover_image\"`, `applyString(\"cover_image\"`). Ein blosser Ausschluss des Begriffs — wie in der Vorfassung — maskierte die acht lebenden Rohspalten-Leser und wird nicht wiederholt."
    - "SC4/cover_image-Scope (Checker-Blocker 1+2): Die Spalten-Signaturen laufen NUR ueber `backend/internal` + `backend/cmd` mit `--include=*.go`. `frontend/src` ist ausgenommen, weil dort `anime.cover_image` der legitime TS-DTO-Property-Zugriff ist (>15 lebende Treffer); `*.sql` ist ausgenommen, weil kein produktives SQL-File eine der Signaturen traegt (verifiziert) und die Altmigrationen bleiben."
    - "D-03-Scope: Die grep-Suite prueft KEINE blanket `/covers/`-URL-Strings (die in Phase-109-deferred FE wie FansubVersionBrowser + Tests weiterleben) — nur die entfernten Route-Handler-Dateien + `upload-cover`-Symbole."
  artifacts:
    - path: "scripts/media-core-contract-check.ps1"
      provides: "psql-basierter Schema-Contract-Check mit -FailOnContractGaps-Gate"
      contains: "FailOnContractGaps"
    - path: "scripts/media-core-legacy-grep.ps1"
      provides: "SC4-Rest-Referenz-Scan ueber die in 106 entfernten Symbole/Routen, gescopet auf die vier Quell-Wurzeln"
      contains: "cover_image"
  key_links:
    - from: "scripts/media-core-contract-check.ps1"
      to: "team4sv30-db (psql)"
      via: "docker compose exec Invoke-LocalPsql"
      pattern: "docker compose exec"
---

<objective>
Erstellt die zwei SC3/SC4-Gate-Skripte, die in Plan 106-08 als Phasen-Gate ausgefuehrt werden: (1) `media-core-contract-check.ps1` (Live-DB-Audit, Zielschema + Legacy-Freiheit), (2) `media-core-legacy-grep.ps1` (Rest-Referenz-Scan). Beide werden hier authored, nicht ausgefuehrt (die DB ist erst nach dem Kettenlauf in 106-08 im Zielzustand) — sie sind die praeregistrierten Gates.

REVISION (PO-Entscheid 2026-07-21, „Option A" / CONTEXT.md D-07): `release_media` wird in Phase 106 NICHT entfernt. Beide Skripte sind entsprechend UNBEDINGT formuliert — keine „Option A/B/C"-Konditionale, keine Rueckmeldungs-Abhaengigkeit von 106-04/106-05-SUMMARY. Der Contract-Check fuehrt `release_media` als erwartet-VORHANDEN (Drop-Guard), und die grep-Suite kennt die Begriffe `release_media`/`CreateReleaseMedia` ueberhaupt nicht mehr — womit auch die frueher noetige Test-Guard-Allowlist ersatzlos entfaellt.

REVISION 2 (Plan-Checker-Blocker 2, 2026-07-22): Die SC4-Suchlaeufe waren zuvor auf das Repo-Root gescopet und schlossen nur `*.exe`/`*.log`/`*.md`/`.git`/`node_modules`/`.planning/` aus. Damit blieben `database/migrations/**` und `scripts/**` im Scan — und beide tragen dauerhaft den Term `episode_version_image` (Altmigrationen 0018/0046 bleiben per D-01/A4; `reset-local-schema-cutover-data.ps1` und `schema-v2-audit.ps1` sind Phase-110-Scope und werden von keinem 106-Plan angefasst). Das Gate haette per Konstruktion nie Exit 0 erreichen koennen. Der Scan ist deshalb jetzt auf vier explizite QUELL-WURZELN gescopet.

REVISION 3 (RESEARCH-Refresh 2026-07-22, Befunde P-3 + P-4): (a) Der Suchbegriff `episode_version_image` hatte in den vier Wurzeln NULL Content-Treffer und wurde durch das CamelCase-Pendant `EpisodeVersionImage` ersetzt; zusaetzlich prueft die Suite jetzt die Datei-Abwesenheit der drei Go-Dateien der Strecke. (b) Das Case-Verhalten ist explizit festgelegt (case-insensitiv, `Select-String`-Default) statt implizit — die Diskrepanz zwischen case-sensitivem `grep` bei der Planung und case-insensitivem `Select-String` im Gate war die Ursache beider Blocker. (c) `shared/contracts` bleibt Scan-Wurzel; die zuvor unzugeordneten `listReleaseImages`-Treffer sind per PO-Entscheid Plan 106-03 Task 3 zugewiesen.

Purpose: SC3-Contract-Check und SC4-grep-Beleg als reproduzierbare, versionierte Gates.
Output: zwei PowerShell-Skripte unter `scripts/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-medienkern-schema-legacy-abbau/106-RESEARCH.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-PATTERNS.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-CONTEXT.md

<interfaces>
<!-- Analog: scripts/schema-v2-audit.ps1 (304 Z., reale Logik) — Struktur 1:1 uebernehmen, Listen tauschen. -->
- DB-Identitaets-Guard + psql-Helper (schema-v2-audit.ps1:8–22): `Invoke-LocalPsql` via
  `docker compose exec -T team4sv30-db psql -U team4s -d team4s_v2 ...`; harte Pruefung
  `current_database(),current_user == team4s_v2 / team4s`, sonst `throw`.
- Hashmap-Introspektion (:24–90): information_schema.tables/.columns + pg_constraint (pg_get_constraintdef).
- Present/Legacy-Assertion + Fail-Gate (:161–184/232–237/295–303): `$contractPassed = missing==0 && blocking==0`;
  `if ($FailOnContractGaps -and -not $contractPassed) { throw }`.
- HINWEIS zur `$targetTables`-Liste des v2-Audits: sie erwartet `release_media` als vorhanden — das ist unter D-07 KORREKT
  und darf uebernommen werden; `release_media` gehoert in die PRESENT-Liste, nicht in die Legacy-Liste.
- D-07 (LOCKED, CONTEXT.md): `release_media` und `CreateReleaseMedia` sind KEIN Gegenstand von Phase 106.
  Weder Contract-Check noch grep-Suite duerfen ihre Abwesenheit verlangen. Die frueher geplante Allowlist der
  6 „intentional retained" Test-Dateien (app_auth_test, member_profile_repository_test, group_contributors_repository_test,
  runtime_authority_test, theme_segment_render_cache_test, segment_validation_test) ist damit GEGENSTANDSLOS —
  ohne `release_media`-Suchbegriff gibt es nichts zu allowlisten. NICHT implementieren.
- SCOPE-Klaerung (verifiziert): `frontend/src/components/fansubs/FansubVersionBrowser.tsx` konstruiert weiter
  `/covers/${value}` (Logo-Fallback) und Tests referenzieren `/covers/`-URLs — das ist NICHT build-breaking und
  laut D-03 Phase-109-deferred. Der 106-SC4-grep darf blanket `/covers/`-URL-Strings NICHT als Verstoss werten;
  das gehoert zum Phase-110-E2E-Gate (ROADMAP 110-SC4).

<!-- Scope-Validierung 2026-07-22 (jeder Term real gegen den Baum geprueft, Blocker 2). -->
Treffer AUSSERHALB der vier Quell-Wurzeln, die dauerhaft bestehen bleiben und deshalb den Scan-Scope diktieren:
- `EpisodeVersionImage` (case-insensitiv, matcht damit auch die snake_case-Schreibweise `episode_version_images`)
  → `database/migrations/0018_episode_version_images.up.sql` + `.down.sql`,
  `database/migrations/0046_drop_legacy_episode_versions.up.sql` (:6) + `.down.sql` (:36) — bleiben per D-01/A4;
  `scripts/reset-local-schema-cutover-data.ps1` (:51), `scripts/schema-v2-audit.ps1` (:141) — Phase-110-Scope,
  von keinem 106-Plan in `files_modified`. Die Ausschlussbegruendung fuer `database/migrations/**` und `scripts/**`
  bleibt durch den Termwechsel (P-4) also UNVERAENDERT gueltig — der case-insensitive CamelCase-Term trifft dieselben
  Alt-Artefakte wie zuvor der snake_case-Term.
- `SET cover_image` → `scripts/remediate-cover-image.ps1` (:53/:54/:55) — wird zwar von 106-03 GELOESCHT,
  ist aber ohnehin ausserhalb der Wurzeln.
- Die Gate-Skripte selbst (`scripts/media-core-*.ps1`) tragen alle Suchbegriffe als Literale — ein Repo-Root-Scan
  wuerde sie selbst treffen. Wurzel-Scoping loest auch das.

Treffer INNERHALB der vier Wurzeln (Stand vor Ausfuehrung) — alle einem 106-Plan zugeordnet, Gate wird also erreichbar:
- `SupportsLegacyUploadSchema` → handlers/media_upload_v2_compat.go, handlers/media_upload_test.go, repository/media_upload.go (106-04)
- `useLegacyUploadSchema` → repository/media_upload.go (106-04)
- `legacyUploadSchemaDetector`, `shouldUseAnimePosterPathFallback` → handlers/media_upload_v2_compat.go, handlers/media_upload_image.go (106-04)
- `asset_lifecycle` → services/asset_lifecycle_errors.go(6), repository/asset_lifecycle_audit.go(2) (106-06), shared/contracts/openapi.yaml:1475/1481 (2 Treffer, 106-06)
- `EpisodeVersionImage` (CamelCase — ERSETZT den wirkungslosen snake_case-Term `episode_version_image`, P-4)
  → cmd/server/main.go(3), handlers/episode_version_images_handler.go(8), models/episode_version_image.go(9),
    repository/episode_version_image_repository.go(12) (106-03 Task 2), shared/contracts/openapi.yaml(4: :6563/:12612/:12655/:12663) (106-03 Task 3).
  Gegenprobe: `frontend/src` hat NULL Treffer — das FE nutzt die eigenen Typen `ScreenshotImage`/`ScreenshotImagesResponse`
  (`frontend/src/types/screenshotImage.ts`), die per D-03 bis Phase 109 bleiben. Kein False-Fail.
- `ListReleaseImages` → cmd/server/main.go(1), handlers/episode_version_images_handler.go(2) (106-03 Task 2),
  shared/contracts/openapi.yaml(1: `operationId: listReleaseImages` :6526 — case-insensitiver Treffer!) (106-03 Task 3)
- `/releases/:id/images` (Gin-Syntax) → cmd/server/main.go(1) (106-03 Task 2). Matcht das FE-Template-Literal
  `/api/v1/releases/${releaseId}/images` in ScreenshotGallery.tsx NICHT → kein False-Fail auf den per D-03/P-6
  bewusst bis Phase 109 deferrierten Consumer.
- `migrate-covers` → backend/cmd/migrate-covers/** (Verzeichnis wird von 106-03 geloescht)
- `upload-cover`, `deleteUploadedCoverFile` → frontend/src/lib/api.ts, frontend/src/app/admin/anime/components/AdminAnimeOverviewClient.tsx (106-07)
- `syncLegacyAnimeCoverImageV2`, `HasCoverImage` → repository/anime_assets.go, repository/anime_schema.go (106-05)
- `createAnimeLegacy` → repository/admin_content_anime_create_v2.go, repository/admin_content_anime_metadata.go (106-10)
- `buildAnimeListWhere(` → repository/anime.go (+ anime_test.go) (106-10). Das V2-Pendant `buildAnimeListWhereV2(`
  matcht diesen Term NICHT (das literale `(` folgt unmittelbar) und bleibt bestehen.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contract-Check-Skript media-core-contract-check.ps1</name>
  <files>scripts/media-core-contract-check.ps1</files>
  <read_first>
    - scripts/schema-v2-audit.ps1 (komplett — Guard/Helper/Hashmaps/Fail-Gate spiegeln)
    - 106-PATTERNS.md Abschnitt „media-core-contract-check.ps1" (Anpassungsliste)
    - 106-CONTEXT.md D-07 (release_media bleibt vorhanden)
    - 106-RESEARCH.md Abschnitt „Contract-Check-Analyse (SC3)"
  </read_first>
  <action>Neues Skript analog `schema-v2-audit.ps1`, Struktur 1:1: `Invoke-LocalPsql`-Helper + harter `team4s_v2`/`team4s`-Identitaetsguard + Hashmap-Introspektion (`$tables`, `$columns`, `$constraints`). Param `-FailOnContractGaps`. Assertions: (a) MUSS-vorhanden — Tabelle `media` (Spalten kind/storage_key/content_hash/source/processing_status/owner_user_id/owner_member_id + CHECK-Constraints chk_media_kind/source/processing_status) und `media_variant` (`media_id`-FK mit `ON DELETE CASCADE` via pg_get_constraintdef + CHECK chk_media_variant_variant/status); (b) MUSS-abwesend — GENAU EIN Eintrag: die Spalte `anime.cover_image`; (c) NEUE Negativ-Assertion (verbotene Medium-Spalten): Fail wenn `$columns["media"]` eine von caption/visibility_id/review_status_id/category/sort_order enthaelt; (d) ERWARTET-VORHANDEN (Drop-Guard, KEINE Legacy-Liste): `media_assets`, `media_files` UND `release_media` — letzteres gemaess D-07, damit ein versehentlicher `release_media`-Drop in Phase 106 als Contract-Gap auffaellt (Entfernung ist Phase-108-Scope). `$contractPassed`-Gate + `throw` bei `-FailOnContractGaps`. Ziel < 450 Zeilen (Analog 304). Kein produktiver App-Code beruehrt. Keine Konditionale/„je nach Option"-Logik — der Zielzustand ist eindeutig.</action>
  <acceptance_criteria>
    - `scripts/media-core-contract-check.ps1` existiert, enthaelt `-FailOnContractGaps`-Param und den `team4s_v2`/`team4s`-Identitaetsguard.
    - Skript listet `anime.cover_image` als MUSS-abwesend.
    - Skript listet `release_media`, `media_assets` und `media_files` als ERWARTET-VORHANDEN und NICHT als Legacy-/MUSS-abwesend-Eintrag (D-07).
    - `grep -n "release_media" scripts/media-core-contract-check.ps1` zeigt den Bezeichner ausschliesslich in der Present-/Erwartet-Liste (kein Legacy-/Absent-Kontext).
    - `powershell -NoProfile -Command "$null = [ScriptBlock]::Create((Get-Content -Raw scripts/media-core-contract-check.ps1))"` parst fehlerfrei (Syntax-Check).
    - Datei < 450 Zeilen.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -Command "$null=[ScriptBlock]::Create((Get-Content -Raw 'scripts/media-core-contract-check.ps1')); Write-Output PARSE-OK"</automated>
  </verify>
  <done>Contract-Check-Skript existiert, syntaktisch valide, korrekte Ziel-/Present-/Absent-/Verboten-Listen; release_media als Present-Guard.</done>
</task>

<task type="auto">
  <name>Task 2: SC4-grep-Suite media-core-legacy-grep.ps1</name>
  <files>scripts/media-core-legacy-grep.ps1</files>
  <read_first>
    - 106-RESEARCH.md Abschnitt „Wave 0 Gaps" (Symbol-/Routen-Liste)
    - 106-PATTERNS.md Abschnitt „Enrichment / Risiko-Befunde" (cover_image Spalte vs. DTO-Feld)
    - 106-CONTEXT.md D-07 (release_media/CreateReleaseMedia sind KEINE Suchbegriffe)
    - 106-02-PLAN.md `<interfaces>`-Abschnitt „Scope-Validierung 2026-07-22" (Term-fuer-Term-Trefferliste)
    - CLAUDE.md (grep-Scope-Hinweise, Docker-Backend)
  </read_first>
  <action>PowerShell-Skript, das die vier QUELL-WURZELN nach verbleibenden Legacy-Referenzen durchsucht und mit Exit-Code != 0 endet, falls Treffer bestehen.

SCAN-SCOPE A (Symbol-/Routen-Terme) — Wurzeln: `backend/internal`, `backend/cmd`, `frontend/src`, `shared/contracts`. Innerhalb dieser Wurzeln ausgeschlossen: `*.md`, `*.exe`, `*.log`, `node_modules`, `.git`. NICHT gescannt werden `database/migrations/**`, `backend/database/**`, `scripts/**`, `docs/**`, `.planning/**` und das Repo-Root — Begruendung MUSS als Kommentarblock im Skript stehen: die Altmigrationen `0018_episode_version_images.*` und `0046_drop_legacy_episode_versions.*` bleiben per D-01/A4 bewusst im Repo und tragen `episode_version_image` dauerhaft; `scripts/reset-local-schema-cutover-data.ps1` und `scripts/schema-v2-audit.ps1` tragen denselben Term, werden von keinem 106-Plan angefasst und gehoeren zum Phase-110-Scope; ausserdem wuerden die Gate-Skripte in `scripts/` ihre eigenen Suchbegriff-Literale treffen. Ein Repo-Root-Scan koennte deshalb per Konstruktion nie Exit 0 liefern (Checker-Blocker 2).

CASE-VERHALTEN (P-4/A1, VERBINDLICH): Der Scan laeuft CASE-INSENSITIV — also `Select-String` OHNE `-CaseSensitive` (PowerShell-Default). Das MUSS im Skript-Kopf als Kommentar ausdruecklich festgehalten werden, samt Begruendung: `shared/contracts/openapi.yaml` fuehrt `operationId: listReleaseImages` (kleiner Anfangsbuchstabe) und `runtime_authority_test.go` normalisiert seine Assertion-Fragmente per `strings.ToLower` — beide Treffer entgingen einem case-sensitiven Scan. Alle Trefferlisten in diesem Plan wurden mit `grep -rni` ermittelt.

VOLLSTAENDIGE Suchbegriff-Liste Scope A (genau die in Phase 106 entfernten Symbole/Routen, jeder Term case-insensitiv gegen den realen Baum validiert): `SupportsLegacyUploadSchema`, `useLegacyUploadSchema`, `legacyUploadSchemaDetector`, `shouldUseAnimePosterPathFallback`, `asset_lifecycle`, `EpisodeVersionImage`, `ListReleaseImages`, `/releases/:id/images`, `migrate-covers`, `upload-cover`, `deleteUploadedCoverFile`, `syncLegacyAnimeCoverImageV2`, `HasCoverImage`, `createAnimeLegacy`, `buildAnimeListWhere(` (Legacy-Where-Builder, in 106-10 entfernt; das literale `(` grenzt gegen das bleibende `buildAnimeListWhereV2(` ab).
ERSETZT UND VERBOTEN (P-4): Der snake_case-Term `episode_version_image` hat in den vier Wurzeln NULL Content-Treffer (Go/TS nutzen CamelCase; snake_case existiert nur in DATEINAMEN und in `database/migrations/0018|0046`, beide ausserhalb des Scans). Er darf NICHT in der Liste stehen — ein Term, der nie feuern kann, taeuscht Legacy-Freiheit vor, ohne sie zu belegen. Stattdessen `EpisodeVersionImage`. Das Skript scannt ausschliesslich DATEIINHALTE, keine Dateipfade; die Datei-Ebene wird stattdessen ueber die Abwesenheitspruefungen unten abgedeckt.
ACHTUNG: `animeCoverImageSelectSQL` ist KEIN Suchbegriff — der Helper BLEIBT bestehen (er ist der kanonische Ersatz und wird in 106-09 sogar auf weitere Consumer ausgerollt). Eine Suche danach waere ein garantierter False-Fail.
D-07 — NICHT SUCHEN: `release_media` und `CreateReleaseMedia` duerfen in dieser Suite NICHT als Suchbegriffe auftauchen. Beide bleiben in Phase 106 bewusst im Code (Entfernung = Phase 108); eine Suche danach waere ein garantierter False-Fail. Entsprechend gibt es KEINE Allowlist, KEINE `*_test.go`-Sonderbehandlung und KEINE word-boundary-Abgrenzung gegen `release_version_media` — der Term existiert schlicht nicht.

SCAN-SCOPE B (cover_image-SQL-Spalten-Signaturen) — ENGER: nur `backend/internal` + `backend/cmd`, nur `*.go`. Regex-Terme: `a\.cover_image`, `anime\.cover_image`, `,\s*cover_image`, `cover_image\s*=`, `SET cover_image`. Aus den Treffern die legitimen DTO-Vorkommen herausfiltern: Zeilen mit `json:"cover_image"`, `json:"cover_image,omitempty"`, `applyString("cover_image"` sowie Go-Feldnamen `CoverImage`. Warum enger als Scope A (MUSS als Kommentar im Skript stehen): in `frontend/src` ist `anime.cover_image` der legitime TypeScript-DTO-Property-Zugriff (>15 lebende Treffer in Seiten, Komponenten und Tests, D-03/Phase-109-deferred) — ein Frontend-Scan waere ein garantierter False-Fail; `*.sql` traegt verifiziert KEINE dieser Signaturen und ist daher unnoetig. Der Term darf NICHT wie in der Vorfassung komplett aus der Suite fallen, sonst beweist SC4 die Spalten-Sauberkeit nicht (Checker-Warning 2: genau dieser Ausschluss maskierte acht lebende Rohspalten-Leser).
HINWEIS zu `,\s*cover_image`: Der Term matcht auch Praefixe wie `, cover_image_url`. Kein 106-Plan darf einen SQL-Ausgabenamen einfuehren, der mit `cover_image` BEGINNT und komma-praefixiert in einer Spaltenliste steht — 106-09 Task 2 benennt den Sub-Query-Ausgabenamen in `anime_relations.go` deshalb in `cover_url` um. Ein Alias in der Form `<expr> AS cover_image` (nicht komma-praefixiert) ist erlaubt und wird bewusst NICHT getroffen.

DATEI-ABWESENHEITSPRUEFUNGEN (Scope C) — das Skript prueft, dass die folgenden Dateien NICHT mehr existieren; jede noch vorhandene Datei fuehrt zu `exit 1`:
- `frontend/src/app/covers/[file]/route.ts` und `frontend/src/app/api/admin/upload-cover/route.ts` (106-07). Fuer diese Routen NICHT einen blanket `/covers/`-URL-String-Scan verwenden (D-03: FansubVersionBrowser + Tests behalten `/covers/`-URLs bis Phase 109/110), und auch KEINEN Live-HTTP-404-Check — `frontend/public/covers/` wird von Next weiterhin statisch bedient (P-8), die Datei-Abwesenheit ist der einzige belastbare Nachweis.
- `backend/internal/handlers/episode_version_images_handler.go`, `backend/internal/repository/episode_version_image_repository.go`, `backend/internal/models/episode_version_image.go` (106-03, P-4-Ergaenzung).
- `backend/internal/handlers/media_upload_v2_compat.go` (106-04).
- `backend/database/migrations/` und `backend/cmd/migrate-covers/` als Verzeichnis-Abwesenheit (106-03).

Ausgabe: pro Begriff Trefferliste mit Datei:Zeile; finales `exit 1` wenn irgendein Begriff noch matcht bzw. eine der Route-Handler-Dateien noch existiert, sonst `exit 0`.</action>
  <acceptance_criteria>
    - `scripts/media-core-legacy-grep.ps1` existiert und parst syntaktisch fehlerfrei.
    - Scope A: Skript nennt die vier Wurzeln `backend/internal`, `backend/cmd`, `frontend/src`, `shared/contracts` explizit und scannt NICHT das Repo-Root.
    - Scope-Begruendung: `grep -c "database/migrations" scripts/media-core-legacy-grep.ps1` >= 1 (der Kommentarblock nennt die per D-01/A4 bewusst behaltenen Altmigrationen 0018/0046 als Ausschlussgrund).
    - `grep -c "schema-v2-audit\|reset-local-schema-cutover-data" scripts/media-core-legacy-grep.ps1` >= 1 (Phase-110-Skripte als Ausschlussgrund dokumentiert).
    - Skript schliesst innerhalb der Wurzeln `*.md`, `*.exe`, `*.log`, `node_modules` aus.
    - D-07: `grep -c "release_media\|CreateReleaseMedia" scripts/media-core-legacy-grep.ps1` == 0 (weder Suchbegriff noch Allowlist-Erwaehnung).
    - Skript enthaelt keine Allowlist von `*_test.go`-Dateien (gegenstandslos ohne release_media-Term).
    - P-4: `grep -c "EpisodeVersionImage" scripts/media-core-legacy-grep.ps1` >= 1 (CamelCase-Term vorhanden).
    - P-4: Der wirkungslose snake_case-Term steht NICHT mehr als Suchbegriff in der Term-Liste — `grep -c "'episode_version_image'\|\"episode_version_image\"" scripts/media-core-legacy-grep.ps1` == 0 (Erwaehnung im begruendenden Kommentarblock ist erlaubt und erwuenscht).
    - P-4/A1: Das Skript dokumentiert sein Case-Verhalten explizit im Kopf — `grep -ci "case-insensitiv\|CaseSensitive" scripts/media-core-legacy-grep.ps1` >= 1 — und verwendet `Select-String` OHNE `-CaseSensitive`.
    - P-4-Ergaenzung: Das Skript prueft die Datei-Abwesenheit der drei episode_version_image(s)-Go-Dateien — `grep -c "episode_version_images_handler.go" scripts/media-core-legacy-grep.ps1` >= 1.
    - P-3: `shared/contracts` ist weiterhin als Scan-Wurzel gelistet (NICHT aus dem Scope entfernt) — die Contract-Treffer sind stattdessen 106-03 Task 3 zugeordnet.
    - Scope B: Skript trennt `cover_image`-Spalte (verboten, ueber die Spalten-Signaturen `a\.cover_image`, `anime\.cover_image`, `,\s*cover_image`, `cover_image\s*=`, `SET cover_image`) von DTO-Feld (erlaubt, ausgefiltert ueber `json:"cover_image"` / `applyString("cover_image"` / `CoverImage`) — weder pauschales `cover_image` noch vollstaendiger Ausschluss des Terms.
    - Scope B laeuft nachweislich NUR ueber `backend/internal`/`backend/cmd` mit `*.go` — `frontend/src` ist fuer die Spalten-Signaturen ausgeschlossen (sonst False-Fail auf den TS-DTO-Zugriff `anime.cover_image`).
    - `grep -c "cover_image" scripts/media-core-legacy-grep.ps1` >= 1 (der Term ist praesent — die Vorfassung hatte ihn ausgeschlossen, wodurch SC4 die Spalten-Sauberkeit nicht bewies).
    - `grep -c "animeCoverImageSelectSQL" scripts/media-core-legacy-grep.ps1` == 0 (kein False-Fail auf den bleibenden Helper).
    - Skript prueft Route-Handler-Datei-Abwesenheit statt blanket `/covers/`-URL-Scan.
    - Skript endet mit `exit 1` bei Resttreffern, `exit 0` bei Sauberkeit.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -Command "$p='scripts/media-core-legacy-grep.ps1'; $null=[ScriptBlock]::Create((Get-Content -Raw $p)); if ((Select-String -Path $p -Pattern 'release_media|CreateReleaseMedia').Count -gt 0) { throw 'D-07 violation: release_media term present' }; if ((Select-String -Path $p -Pattern 'cover_image').Count -eq 0) { throw 'SC4 gap: cover_image not scanned' }; if ((Select-String -Path $p -Pattern 'animeCoverImageSelectSQL').Count -gt 0) { throw 'false-fail term animeCoverImageSelectSQL present' }; if ((Select-String -Path $p -Pattern 'EpisodeVersionImage' -CaseSensitive).Count -eq 0) { throw 'P-4: CamelCase term EpisodeVersionImage missing' }; if ((Select-String -Path $p -Pattern 'episode_version_images_handler.go').Count -eq 0) { throw 'P-4: file-absence check for episode_version_images_handler.go missing' }; foreach ($r in @('backend/internal','backend/cmd','frontend/src','shared/contracts','database/migrations')) { if ((Select-String -Path $p -Pattern ([regex]::Escape($r))).Count -eq 0) { throw ('scan-scope marker missing: ' + $r) } }; Write-Output PARSE-OK"</automated>
  </verify>
  <done>grep-Suite existiert, auf die vier Quell-Wurzeln gescopet (Altmigrationen + Phase-110-Skripte begruendet ausgeschlossen), cover_image-SPALTE aktiv gesucht (nur Go-Backend) und vom DTO-Feld getrennt, kein release_media/CreateReleaseMedia-Term, kein False-Fail auf animeCoverImageSelectSQL, Exit-Code-Gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Keine. Read-only Introspektion (Contract-Check) bzw. statischer Repo-Scan (grep).

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-02-01 | Information Disclosure | psql gegen falsche DB | mitigate | Harter Identitaetsguard `team4s_v2`/`team4s` vom Analog uebernommen; `throw` bei Abweichung. |
| T-106-02-02 | Repudiation | grep-Suite false-fail auf bewusst behaltenen release_media-Code maskiert den echten Zustand | mitigate | D-07: `release_media`/`CreateReleaseMedia` sind gar keine Suchbegriffe; Acceptance-Criterion prueft ihre Abwesenheit im Skript. |
| T-106-02-03 | Tampering | Unbemerkter `release_media`-Drop in Phase 106 bricht `GET /releases/:id/assets` | mitigate | Contract-Check fuehrt `release_media` als ERWARTET-VORHANDEN → Drop faellt als Contract-Gap auf. |
| T-106-02-04 | Repudiation | Unerreichbares Gate: Scan-Scope trifft bewusst behaltene Dateien (Altmigrationen 0018/0046, Phase-110-Skripte) → 106-08 kann nie gruen werden, das Gate wird deaktiviert oder ignoriert | mitigate | Scan auf vier Quell-Wurzeln gescopet; Ausschlussgruende als Kommentar im Skript + als Acceptance-Criteria verifiziert. |
| T-106-02-SC | Tampering | Package-Installs | accept | Keine Installs — kein Supply-Chain-Vektor. |
Keine neue Angriffsflaeche; Endpoint-Haertung erst Phase 108.
</threat_model>

<verification>
- Beide Skripte parsen fehlerfrei (ScriptBlock-Syntax-Check).
- Contract-Check-Skript enthaelt korrekte MUSS-vorhanden/MUSS-abwesend/Erwartet-vorhanden/Verboten-Listen (release_media als Present-Guard).
- grep-Suite scope-korrekt (vier Quell-Wurzeln; Altmigrationen/Phase-110-Skripte begruendet ausgeschlossen; /covers-URL nicht blanket) und OHNE release_media/CreateReleaseMedia-Term.
</verification>

<success_criteria>
SC3-Tooling: Contract-Check-Skript existiert (Ausfuehrung als Gate in 106-08), fuehrt `release_media` als vorhanden (D-07). SC4-Tooling: grep-Suite existiert (Ausfuehrung als Gate in 106-08), sucht ausschliesslich nach den tatsaechlich in 106 entfernten Symbolen/Routen und ist so gescopet, dass Exit 0 nach vollstaendigem Phasenlauf tatsaechlich erreichbar ist.
</success_criteria>

<output>
Create `.planning/phases/106-medienkern-schema-legacy-abbau/106-02-SUMMARY.md` when done.
</output>
