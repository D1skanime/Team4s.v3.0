---
phase: 106-medienkern-schema-legacy-abbau
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/migrations/media_core_schema_test.go
  - database/migrations/0131_media_core_schema.up.sql
  - database/migrations/0131_media_core_schema.down.sql
autonomous: true
requirements: [SC1, SC3]
must_haves:
  truths:
    - "SC1/D-06: Tabelle `media` existiert mit exaktem Spaltensatz aus DECISION §1 Ebene 1; alle 3 Enum-Felder als TEXT NOT NULL + benanntes CHECK."
    - "SC1/§1: `media` traegt KEINE der verbotenen Spalten caption/visibility/review_status/category/sort_order."
    - "SC1/D-06: Tabelle `media_variant` existiert mit `media_id → media(id) ON DELETE CASCADE` + CHECK auf variant/status."
    - "D-01/D-02: Neue append-only Migration Nummer 0131; vor Vergabe verifiziert, dass KEINE `0131_*`-Datei existiert (Nummern-Kollisions-Guard); Altmigrationen (inkl. 0018/0046) unberuehrt."
    - "SC2/D-07: UP dropt AUSSCHLIESSLICH `anime.cover_image`; DOWN rekonstruiert ausschliesslich `cover_image`. `release_media` wird in Phase 106 NICHT angefasst (PO-Entscheid 2026-07-21, „Option A") — Entfernung wandert nach Phase 108."
    - "D-07-Guard: 0131 (UP + DOWN) enthaelt den String `release_media` an KEINER Stelle; der Content-Test sichert das per Negativ-Assertion gegen versehentliche Wiedereinfuehrung ab."
    - "D-05: `content_hash` nullable, KEIN UNIQUE-Constraint in 0131."
    - "Grenzbefund A1: `media_assets`/`media_files` werden NICHT gedroppt."
  artifacts:
    - path: "database/migrations/0131_media_core_schema.up.sql"
      provides: "Additive media/media_variant-Anlage + einziger Legacy-DROP (anime.cover_image)"
      contains: "CREATE TABLE media"
    - path: "database/migrations/0131_media_core_schema.down.sql"
      provides: "Strukturelle Umkehr (cover_image rekonstruieren, media_variant/media droppen)"
      contains: "DROP TABLE IF EXISTS media"
    - path: "backend/internal/migrations/media_core_schema_test.go"
      provides: "String-Content-Assertion ueber 0131 UP/DOWN (positiv + Negativ-Assertion SC1 + D-07-Negativ-Assertion release_media)"
      contains: "func TestMediaCore"
  key_links:
    - from: "backend/internal/migrations/media_core_schema_test.go"
      to: "database/migrations/0131_media_core_schema.up.sql"
      via: "os.ReadFile relative Pfadpruefung"
      pattern: "0131_media_core_schema.up.sql"
---

<objective>
Legt das Medienkern-Zielschema additiv an: neue Tabellen `media` (globales physisches Medium) und `media_variant` (technische Ableitungen) per neuer append-only Migration `0131_media_core_schema`. Dieselbe Migration entfernt genau EINE DDL-Legacy-Struktur der §6-Liste: die `anime.cover_image`-Spalte. Die `release_media`-Junction bleibt gemaess D-07 (PO-Entscheid „Option A") in Phase 106 unangetastet und wird erst in Phase 108 entfernt. `media_assets`/`media_files` bleiben ebenfalls unangetastet (Grenzbefund A1).

Purpose: Fundament (SC1) fuer das gesamte Arbeitspaket 106–110 — ohne Verhaltensaenderung, reines Schema.
Output: `0131_*.up.sql`, `0131_*.down.sql`, `media_core_schema_test.go`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-medienkern-schema-legacy-abbau/106-RESEARCH.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-PATTERNS.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-CONTEXT.md
@.planning/notes/260721-medienmodell-neubau-architektur-DECISION.md

<interfaces>
<!-- Analog-Quellen — Executor spiegelt diese Muster 1:1, kein Codebase-Erkunden noetig. -->
- Enum/CHECK + FK-Analog (UP): database/migrations/0129_release_playback_entitlements.up.sql
  Muster: `col TEXT NOT NULL` + separater `CONSTRAINT chk_<table>_<col> CHECK (col IN ('kleinwert', ...))`;
  `... BIGINT NULL REFERENCES x(id) ON DELETE CASCADE|SET NULL`; `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`.
- Schlanke Umkehr-Analog (DOWN): database/migrations/0130_release_content_source_groups.down.sql (IF EXISTS-tolerant, umgekehrte Reihenfolge).
- Test-Analog (komplett, 36 Z.): backend/internal/migrations/release_content_source_groups_test.go
  Muster: `os.ReadFile("../../../database/migrations/NNNN_*.up/.down.sql")` + `strings.Contains`-Needles (positiv) + Negativ-Assertion.
- D-07 (LOCKED, CONTEXT.md): `release_media` ist KEIN Gegenstand von Phase 106. Weder UP noch DOWN duerfen den Bezeichner enthalten;
  es gibt in dieser Phase keine `release_media`-Rekonstruktionsdefinition und keinen `release_media`-DROP.
- D-02 Nummern-Guard: im Projekt gab es Kollisionen (0090 durch Phase 70 belegt → Phase 67 wich auf 0091 aus); zudem koennen parallele GSD-Schreiber auf `main` neue Migrationen anlegen. Vor dem Schreiben von 0131 MUSS verifiziert werden, dass keine `0131_*`-Datei bereits existiert.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Migrations-Content-Test fuer 0131 schreiben</name>
  <files>backend/internal/migrations/media_core_schema_test.go</files>
  <read_first>
    - backend/internal/migrations/release_content_source_groups_test.go (Struktur-Analog, komplett spiegeln)
    - 106-PATTERNS.md Abschnitt „media_core_schema_test.go" (positive + negative Needle-Liste)
    - 106-CONTEXT.md D-07 (release_media in 106 nicht angefasst)
    - DECISION §1 (exakter Spaltensatz + FORBIDDEN-Liste)
  </read_first>
  <behavior>
    - UP-positiv: enthaelt `CREATE TABLE media`, `content_hash`, `owner_user_id`, `owner_member_id`, jede der 5 CHECK-Klauseln (`chk_media_kind`, `chk_media_source`, `chk_media_processing_status`, `chk_media_variant_variant`, `chk_media_variant_status`), `CREATE TABLE media_variant`, `REFERENCES media(id) ON DELETE CASCADE`, `DROP COLUMN IF EXISTS cover_image`.
    - UP-negativ (SC1): enthaelt NICHT `caption`, `visibility`, `review_status`, `category`, `sort_order`, `UNIQUE (content_hash)`, und NICHT `DROP TABLE ... media_assets`/`DROP TABLE ... media_files`.
    - UP-negativ (D-07): enthaelt NICHT den String `release_media` (weder DROP noch sonstige Referenz).
    - DOWN-positiv: enthaelt `ADD COLUMN IF NOT EXISTS cover_image`, `DROP TABLE IF EXISTS media_variant`, `DROP TABLE IF EXISTS media`.
    - DOWN-negativ (D-07): enthaelt NICHT den String `release_media` (keine Rekonstruktion).
  </behavior>
  <action>Erstelle `media_core_schema_test.go` im Package `migrations`, exakt nach dem Analog `release_content_source_groups_test.go`. Eine Testfunktion `TestMediaCoreSchemaMigrationIsAdditiveAndReversible`. Lies `../../../database/migrations/0131_media_core_schema.up.sql` und `.down.sql` via `os.ReadFile`. Positive Needles per `strings.Contains` (Liste oben), Negativ-Assertionen per `strings.Contains == false` mit `t.Fatalf`. WICHTIG (D-07): Die Negativ-Assertion auf `release_media` gilt fuer UP UND DOWN — `release_media` ist in Phase 106 kein Gegenstand und darf in 0131 nirgends auftauchen; die Assertion ist der Guard gegen versehentliche Wiedereinfuehrung des urspruenglich geplanten DROPs. Der Test MUSS jetzt fehlschlagen (0131-Dateien existieren noch nicht → `os.ReadFile`-Fehler via `t.Fatal`).</action>
  <acceptance_criteria>
    - `backend/internal/migrations/media_core_schema_test.go` existiert, Package `migrations`, genau eine `Test...`-Funktion.
    - `cd backend && go test ./internal/migrations/... -run MediaCore` schlaegt fehl mit ReadFile-/fehlende-Datei-Fehler (RED bestaetigt).
    - Test enthaelt mindestens die 5 `chk_media_*`-Needles, die 5 verbotenen-Spalten-Negativ-Assertions und die `release_media`-Negativ-Assertion (UP + DOWN) als String-Literale.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/migrations/... -run MediaCore 2>&1 | grep -qiE "no such file|cannot find|0131" && echo RED-OK</automated>
  </verify>
  <done>Test kompiliert und schlaegt kontrolliert fehl, weil 0131 noch fehlt.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — Migration 0131 up/down anlegen</name>
  <files>database/migrations/0131_media_core_schema.up.sql, database/migrations/0131_media_core_schema.down.sql</files>
  <read_first>
    - database/migrations/0129_release_playback_entitlements.up.sql (Enum/CHECK/FK-Muster)
    - database/migrations/0130_release_content_source_groups.down.sql (Umkehr-Muster)
    - DECISION §1 Ebene 1 + Ebene 2 (exakter Spaltensatz)
    - 106-CONTEXT.md D-07 (release_media bleibt)
    - 106-RESEARCH.md Abschnitt „DROP-Reihenfolge & Down-Migration"
  </read_first>
  <action>
    SCHRITT 0 (D-02 Nummern-Guard, VOR dem Schreiben): Verifiziere, dass die Nummer 0131 frei ist — `ls database/migrations/0131_* 2>/dev/null` MUSS leer sein (keine bestehende `0131_*`-Datei). Falls belegt (paralleler GSD-Schreiber / Kollision), auf die naechste freie Nummer ab 0132 ausweichen und Dateinamen + Test-Pfad (`media_core_schema_test.go`) entsprechend anpassen; die gewaehlte Nummer im SUMMARY dokumentieren. Erst nach diesem Guard die Dateien schreiben.
    UP (`0131_media_core_schema.up.sql`):
    1. `CREATE TABLE media` mit Spalten in DECISION-§1-Reihenfolge: `id BIGSERIAL PRIMARY KEY, kind TEXT NOT NULL, storage_key TEXT NOT NULL, original_filename TEXT, mime_type TEXT, byte_size BIGINT, width INTEGER, height INTEGER, duration_seconds DOUBLE PRECISION, content_hash TEXT, source TEXT NOT NULL, source_ref TEXT, credit TEXT, rights_note TEXT, owner_user_id BIGINT NULL REFERENCES users(id) ON DELETE SET NULL, owner_member_id BIGINT NULL REFERENCES members(id) ON DELETE SET NULL, processing_status TEXT NOT NULL DEFAULT 'processing', created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`. Benannte CHECKs: `chk_media_kind CHECK (kind IN ('image','video','audio'))`, `chk_media_source CHECK (source IN ('upload','jellyfin','anilist','mal'))`, `chk_media_processing_status CHECK (processing_status IN ('processing','ready','failed'))`. Owner-FK-Verhalten = ON DELETE SET NULL (Owner optional, PO-Entscheid 3 / A2 — Begruendung im SUMMARY dokumentieren). KEINE Spalten caption/visibility/review_status/category/sort_order. KEIN UNIQUE auf content_hash (D-05).
    2. `CREATE TABLE media_variant`: `id BIGSERIAL PRIMARY KEY, media_id BIGINT NOT NULL REFERENCES media(id) ON DELETE CASCADE, variant TEXT NOT NULL, storage_key TEXT NOT NULL, width INTEGER, height INTEGER, byte_size BIGINT, mime_type TEXT, status TEXT NOT NULL DEFAULT 'ready'`. CHECKs `chk_media_variant_variant CHECK (variant IN ('original','thumbnail','preview'))`, `chk_media_variant_status CHECK (status IN ('ready','missing','failed'))`.
    3. Non-unique Indizes (D-05-Ermessen, struktureller Vorgriff Phase 107): `CREATE INDEX idx_media_content_hash ON media (content_hash) WHERE content_hash IS NOT NULL;` und `CREATE INDEX idx_media_variant_media_id ON media_variant (media_id);`
    4. Legacy-DROP-Anhang — GENAU EINE Anweisung: `ALTER TABLE anime DROP COLUMN IF EXISTS cover_image;`. NIEMALS `media_assets`/`media_files` droppen (A1). NIEMALS `release_media` droppen oder erwaehnen (D-07, PO-Entscheid Option A — Entfernung erfolgt in Phase 108); der Bezeichner `release_media` darf in dieser Datei nicht vorkommen.
    DOWN (`0131_media_core_schema.down.sql`), exakte Umkehr (IF EXISTS-tolerant, kein Datenerhalt):
    1. `ALTER TABLE anime ADD COLUMN IF NOT EXISTS cover_image TEXT;`
    2. `DROP TABLE IF EXISTS media_variant;` (FK-Kind zuerst)
    3. `DROP TABLE IF EXISTS media;`
    Keine `release_media`-Rekonstruktion (die Tabelle wurde nie gedroppt).
  </action>
  <acceptance_criteria>
    - Vor dem Schreiben belegt: keine bestehende `0131_*`-Datei (D-02-Guard); gewaehlte Nummer im SUMMARY notiert.
    - Beide Dateien existieren mit exakter Namenskonvention `0131_media_core_schema.up.sql`/`.down.sql` (bzw. der freien Nummer). Das Suffix `_media_core_schema` ist VERBINDLICH und darf sich auch bei einer Ausweichnummer NICHT aendern — nachgelagerte Gates (106-08) loesen die Datei ueber das Glob `database/migrations/*_media_core_schema.up.sql` auf.
    - `ls database/migrations/*_media_core_schema.up.sql | wc -l` == 1 und `ls database/migrations/*_media_core_schema.down.sql | wc -l` == 1 (genau eine Datei je Richtung — Vorbedingung fuer alle Folge-Greps).
    - `cd backend && go test ./internal/migrations/... -run MediaCore` ist GRUEN.
    - `grep -c 'chk_media' database/migrations/*_media_core_schema.up.sql` >= 5.
    - `grep -Ev '^--' database/migrations/*_media_core_schema.up.sql | grep -Eqi 'caption|visibility|review_status|category|sort_order'` liefert KEINE Treffer (Exit 1).
    - `grep -l 'release_media' database/migrations/*_media_core_schema.*.sql` liefert KEINE Datei (D-07). Vorher Existenz per `ls` belegen — ein negiertes `grep` auf eine fehlende Datei ist ein FALSE PASS.
    - UP enthaelt `DROP COLUMN IF EXISTS cover_image`; enthaelt NICHT `DROP TABLE media_assets`/`DROP TABLE media_files`/`release_media`.
  </acceptance_criteria>
  <verify>
    <automated>[ "$(ls database/migrations/*_media_core_schema.up.sql | wc -l)" = "1" ] && [ "$(ls database/migrations/*_media_core_schema.down.sql | wc -l)" = "1" ] && cd backend && go test ./internal/migrations/... -run MediaCore</automated>
  </verify>
  <done>0131-Nummer-Guard bestanden, 0131 up/down existieren, Content-Test gruen, keine verbotenen Medium-Spalten, media_assets/media_files/release_media unberuehrt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Keine neue Vertrauensgrenze. Reine DDL-Migration ohne neuen Auth-/Input-Pfad.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-01-01 | Tampering | Datenverlust bei falschem DROP | mitigate | DROP strikt auf `anime.cover_image` begrenzt; Content-Test asserted, dass `media_assets`/`media_files` NICHT gedroppt werden. |
| T-106-01-02 | Tampering | Migrations-Nummern-Kollision (0131 belegt durch parallelen Schreiber) ueberschreibt/kollidiert | mitigate | D-02-Guard vor dem Schreiben (`ls database/migrations/0131_*` leer), sonst Ausweichnummer + SUMMARY-Notiz. |
| T-106-01-03 | Tampering | Versehentlicher `release_media`-DROP bricht den lebenden Endpoint `GET /releases/:id/assets` | mitigate | D-07-Negativ-Assertion im Content-Test (UP + DOWN duerfen `release_media` nicht enthalten) + grep-Acceptance-Criterion. |
| T-106-01-SC | Tampering | Package-Installs | accept | Keine npm/go/pip-Installs in dieser Phase — kein Supply-Chain-Vektor. |
`content_hash` ist additiv/nullable; Endpoint-Haertung erst Phase 108. Kein High-Severity-Threat.
</threat_model>

<verification>
- 0131-Nummern-Guard bestanden (keine bestehende 0131_*-Datei).
- `cd backend && go test ./internal/migrations/... -run MediaCore` gruen.
- `cd backend && go build ./... && go vet ./...` gruen (Migrationsdatei bricht keinen Go-Code).
- Negativ-grep auf verbotene Spalten leer; Negativ-grep auf `release_media` in 0131 leer.
</verification>

<success_criteria>
SC1 erfuellt: `media`/`media_variant` per Migration angelegt, verbotene Spalten abwesend, `content_hash` ohne UNIQUE. SC2-Teil: `anime.cover_image` gedroppt, `release_media` per D-07 unangetastet. SC3-Teil: 0131 ist die naechste freie Kettennummer (append-only, Guard-verifiziert).
</success_criteria>

<output>
Create `.planning/phases/106-medienkern-schema-legacy-abbau/106-01-SUMMARY.md` when done.
</output>
