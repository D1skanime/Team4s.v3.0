---
phase: 106-medienkern-schema-legacy-abbau
plan: 06
type: execute
wave: 3
depends_on: [106-03, 106-04]
files_modified:
  - backend/internal/services/asset_lifecycle_service.go
  - backend/internal/services/asset_lifecycle_errors.go
  - backend/internal/services/asset_lifecycle_service_test.go
  - backend/internal/models/asset_lifecycle.go
  - backend/internal/repository/asset_lifecycle_audit.go
  - backend/internal/repository/asset_lifecycle_subjects.go
  - backend/internal/repository/asset_lifecycle_repository_test.go
  - backend/cmd/server/main.go
  - backend/internal/handlers/media_upload.go
  - backend/internal/handlers/media_upload_errors.go
  - backend/internal/handlers/media_upload_image.go
  - backend/internal/handlers/media_upload_video.go
  - backend/internal/handlers/media_upload_test.go
  - backend/internal/models/media_upload.go
  - shared/contracts/openapi.yaml
autonomous: true
requirements: [SC2]
must_haves:
  truths:
    - "SC2/D-04 Cluster G: `asset_lifecycle_service` (Service, Errors, Model, Repo + zugehoerige Tests) ist vollstaendig entfernt (Ordnerprovisionierung durch hash-basierte Ablage kuenftig ueberfluessig)."
    - "Pitfall 5: Der generische V2-Upload bleibt nach Entkopplung kompilierbar und funktional — der MediaUploadHandler traegt kein asset_lifecycle-Feld mehr."
    - "main.go konstruiert keinen AssetLifecycle-Service/-Repo mehr; keine ungenutzten Imports/Variablen."
    - "API-Contract-Treue (Checker-Warning 3): Die drei bisher ueber `services.AssetLifecycle*` ausgelieferten Fehlercodes werden NICHT neu erfunden, sondern definiert migriert: `asset_lifecycle.invalid_entity_type` -> `media_upload.invalid_entity_type`, `asset_lifecycle.invalid_asset_type` -> `media_upload.invalid_asset_type`, `asset_lifecycle.unsafe_path` -> `media_upload.unsafe_path`. Sie liegen damit im bereits etablierten `media_upload.*`-Namensraum (`media_upload.invalid_request`, `.missing_file`, `.open_failed`, `.validation_failed`, `.storage_prepare_failed`, `.invalid_format`, `.processing_failed`) und werden in `shared/contracts/openapi.yaml` mitgezogen."
    - "Der Speicherpfad ist eindeutig definiert: die BEREITS EXISTIERENDE nil-lifecycle-Fallback-Formel `filepath.Join(h.mediaStorageDir, req.EntityType, fmt.Sprintf(\"%d\", req.EntityID))` aus `ensureProvisioning` (media_upload.go:200) wird zur kanonischen Root-Pfad-Berechnung; `resolveUploadStoragePath` behaelt seine Traversal-Pruefung unveraendert."
    - "`go build ./... && go vet ./...` + Upload-Handler-Tests bleiben gruen."
  artifacts:
    - path: "backend/internal/handlers/media_upload.go"
      provides: "MediaUploadHandler ohne asset_lifecycle-Verdrahtung, V2-Upload intakt"
      contains: "MediaUploadHandler"
  key_links:
    - from: "backend/cmd/server/main.go"
      to: "kein AssetLifecycle-Service"
      via: "entfernte Konstruktion (:306-307) + Handler-Injektion"
      pattern: "AssetLifecycle"
---

<objective>
Entfernt den verzahnten `asset_lifecycle`-Cluster (Ordnerprovisionierung) vollstaendig und entkoppelt ihn vom `MediaUploadHandler`, sodass der generische V2-Upload in 106 weiter kompiliert und laeuft (Pitfall 5). Dieser Plan laeuft NACH 106-03 (main.go) und 106-04 (media_upload_test.go), da er dieselben Dateien beruehrt.

Purpose: SC2-Anteil — groesster verzahnter Backend-Cluster, Build-Gruenhaltung kritisch.
Output: 7 geloeschte asset_lifecycle-Dateien, entkoppeltes main.go + media_upload-Handler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-medienkern-schema-legacy-abbau/106-RESEARCH.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-PATTERNS.md

<interfaces>
<!-- Verifizierte Cluster-G-Verdrahtung (RESEARCH Cluster G + PATTERNS). -->
- Zu loeschen: services/asset_lifecycle_service.go(+_errors.go,+_service_test.go), models/asset_lifecycle.go,
  repository/asset_lifecycle_audit.go(+_subjects.go,+_repository_test.go).
- main.go:306 `NewAssetLifecycleRepository(...)`, :307 `NewAssetLifecycleService(...)` (Konstruktion entfernen)
  + Injektion in den MediaUploadHandler.
- handlers/media_upload.go: haelt ein asset_lifecycle-Service-Feld + nutzt es fuer Ordnerprovisionierung
  → Feld + Nutzung entfernen, V2-Upload-Pfad muss weiter kompilieren/laufen.
- handlers/media_upload_test.go: referenziert asset_lifecycle (Konstruktion/Mock) → mit anpassen.

<!-- Verifizierte Kopplungspunkte (Checker-Warning 3: hier war der Plan unterspezifiziert). -->
Fehlercode-Konstanten (services/asset_lifecycle_errors.go) und ihre Handler-Nutzung:
- `services.AssetLifecycleCodeInvalidEntityType` = "asset_lifecycle.invalid_entity_type" -> media_upload.go:108
- `services.AssetLifecycleCodeInvalidAssetType`  = "asset_lifecycle.invalid_asset_type"  -> media_upload.go:114
- `services.AssetLifecycleCodeUnsafePath`        = "asset_lifecycle.unsafe_path"         -> media_upload.go:212, :219
Fehlertyp: `services.AssetLifecycleError{Code, Message, Details map[string]any, Err}` mit `Error()`/`Unwrap()`;
ausgewertet in `mapUploadError` (media_upload.go:261-273) per `errors.As` + Detail-Extraktion `Details["folder"]`.
Contract-Oberflaeche: `shared/contracts/openapi.yaml:1475` (`invalidEntityType`) und `:1481` (`invalidAssetType`)
fuehren die alten Codes als Response-Beispiele; `:9044 AdminMediaUploadResponse` fuehrt `:9058 provisioning`.
Typ-Kopplung: `models.ProvisioningResult` (models/asset_lifecycle.go:20) wird verwendet in
`models.UploadResponse.Provisioning` (models/media_upload.go:43), `handlers/media_upload.go:194/:196`,
`handlers/media_upload_image.go:39/:102/:154`, `handlers/media_upload_video.go:30/:138`.
Speicherpfad: `resolveUploadStoragePath(provisioning.RootPath, ...)` (media_upload.go:157/:207) - der
nil-lifecycle-Fallback `filepath.Join(h.mediaStorageDir, req.EntityType, fmt.Sprintf("%d", req.EntityID))`
(media_upload.go:200) liefert bereits heute exakt denselben Root-Pfad und ist der vorgesehene Ersatz.
Frontend: `frontend/src/types/admin.ts:691` fuehrt `provisioning?:` OPTIONAL - Wegfall des Feldes ist nicht
build-breaking; das FE wird gemaess D-03 in Phase 109 bereinigt und hier NICHT angefasst.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: asset_lifecycle-Dateien loeschen + main.go entkoppeln</name>
  <files>backend/internal/services/asset_lifecycle_service.go, backend/internal/services/asset_lifecycle_errors.go, backend/internal/services/asset_lifecycle_service_test.go, backend/internal/models/asset_lifecycle.go, backend/internal/repository/asset_lifecycle_audit.go, backend/internal/repository/asset_lifecycle_subjects.go, backend/internal/repository/asset_lifecycle_repository_test.go, backend/cmd/server/main.go</files>
  <read_first>
    - backend/internal/services/asset_lifecycle_service.go (Konstruktor-Signatur + oeffentliche Methoden, um Nutzung im Handler zu verstehen)
    - backend/cmd/server/main.go:300-320 (AssetLifecycle-Konstruktion + Injektion)
    - 106-RESEARCH.md Cluster G „Planner-Hinweis" (Handler muss weiter kompilieren)
  </read_first>
  <action>Loesche die 7 asset_lifecycle-Dateien (services: `_service.go`, `_errors.go`, `_service_test.go`; models: `asset_lifecycle.go`; repository: `_audit.go`, `_subjects.go`, `_repository_test.go`). Entferne in `backend/cmd/server/main.go` die `NewAssetLifecycleRepository`/`NewAssetLifecycleService`-Konstruktion (~:306-307) und die Uebergabe des Service an den `MediaUploadHandler`-Konstruktor (Argument entfernen). Entferne dadurch ungenutzt gewordene Imports/Variablen. Noch NICHT den Handler selbst anpassen, falls das den Build bricht — Task 2 macht die Handler-Seite; arbeite so, dass nach Task 2 alles gruen ist. (Bei zirkulaerer Compile-Abhaengigkeit: Handler-Feld in Task 2 zuerst entfernen, dann main.go — Reihenfolge frei, Endzustand zaehlt.)</action>
  <acceptance_criteria>
    - Alle 7 asset_lifecycle-Dateien existieren nicht mehr.
    - `grep -n "AssetLifecycle" backend/cmd/server/main.go` liefert KEINE Treffer.
    - (Zwischenstand-Build darf noch rot sein, bis Task 2 den Handler entkoppelt — finaler Build in Task 2.)
  </acceptance_criteria>
  <verify>
    <automated>[ ! -f backend/internal/services/asset_lifecycle_service.go ] && [ ! -f backend/internal/models/asset_lifecycle.go ] && ! grep -q "AssetLifecycle" backend/cmd/server/main.go && echo FILES-DELETED-OK</automated>
  </verify>
  <done>asset_lifecycle-Dateien geloescht, main.go-Konstruktion/Injektion entfernt.</done>
</task>

<task type="auto">
  <name>Task 2: MediaUploadHandler entkoppeln - Fehlercodes migrieren, Speicherpfad fixieren</name>
  <files>backend/internal/handlers/media_upload.go, backend/internal/handlers/media_upload_errors.go, backend/internal/handlers/media_upload_image.go, backend/internal/handlers/media_upload_video.go, backend/internal/handlers/media_upload_test.go, backend/internal/models/media_upload.go, shared/contracts/openapi.yaml</files>
  <read_first>
    - backend/internal/handlers/media_upload.go:60-235 (Struct-Feld, WithLifecycleService, Upload-Flow, ensureProvisioning, resolveUploadStoragePath)
    - backend/internal/handlers/media_upload.go:255-290 (mapUploadError + writeUploadError - Ausgabeformat des Fehlerobjekts)
    - backend/internal/services/asset_lifecycle_errors.go (exakte Code-Strings + Fehlertyp-Struktur, VOR dem Loeschen in Task 1)
    - backend/internal/models/media_upload.go:35-50 (UploadResponse.Provisioning)
    - backend/internal/handlers/media_upload_image.go:35-160 und media_upload_video.go:25-145 (provisioning-Parameter + Response-Befuellung)
    - backend/internal/handlers/media_upload_test.go:340-520 (mockAssetLifecycleStore + WithLifecycleService-Setups)
    - shared/contracts/openapi.yaml:1455-1495 und :9040-9065 (Fehlercode-Beispiele + AdminMediaUploadResponse.provisioning)
    - 106-RESEARCH.md Pitfall 5
  </read_first>
  <action>ERSATZ STATT ERFINDUNG - die Fehlercodes sind API-Oberflaeche und duerfen nicht frei neu vergeben werden. (1) Lege `backend/internal/handlers/media_upload_errors.go` (Package `handlers`) an mit einem lokalen Fehlertyp `uploadError` (Felder `Code string`, `Message string`, `Details map[string]any`, `Err error`; Methoden `Error() string` liefert `Message`, `Unwrap() error` liefert `Err`) - strukturgleich zum bisherigen `services.AssetLifecycleError`, damit `mapUploadError` unveraendert per `errors.As` + `Details["folder"]` arbeiten kann. Definiere darin die drei Konstanten mit EXAKT diesen Werten: `uploadCodeInvalidEntityType = "media_upload.invalid_entity_type"`, `uploadCodeInvalidAssetType = "media_upload.invalid_asset_type"`, `uploadCodeUnsafePath = "media_upload.unsafe_path"`. Keine weiteren Codes erfinden; `asset_lifecycle.invalid_entity_id`, `.invalid_structure` und `.audit_failed` entfallen ersatzlos, weil ihre einzigen Erzeuger im geloeschten Service lagen. (2) In `media_upload.go` die Call-Sites :108 / :114 / :212 / :219 auf die neuen Konstanten und `&uploadError{...}` umstellen; `mapUploadError` von `*services.AssetLifecycleError` auf `*uploadError` umstellen; alle uebrigen `media_upload.*`-Codes bleiben unveraendert. (3) SPEICHERPFAD: `ensureProvisioning`, das Struct-Feld `lifecycle`, das Interface `mediaUploadLifecycle` und `WithLifecycleService` ersatzlos entfernen. Kanonisch wird die BEREITS EXISTIERENDE nil-Fallback-Formel: berechne im Upload-Flow `rootPath := filepath.Join(h.mediaStorageDir, req.EntityType, fmt.Sprintf("%d", req.EntityID))` und rufe `h.resolveUploadStoragePath(rootPath, req.AssetType, mediaID)` auf. `resolveUploadStoragePath` und `isUploadPathWithinBase` behalten ihre Traversal-Pruefung unveraendert - nur der Fehlertyp wechselt. (4) `models.ProvisioningResult` verschwindet mit Task 1; entferne daher das Feld `Provisioning *ProvisioningResult` aus `models.UploadResponse` (models/media_upload.go:43) sowie den `provisioning`-Parameter und die `Provisioning:`-Zuweisungen in `media_upload_image.go` (:39/:102/:154) und `media_upload_video.go` (:30/:138). Alle uebrigen Response-Felder (`id`, `status`, `files`, `url`) bleiben unveraendert. (5) CONTRACT NACHZIEHEN: in `shared/contracts/openapi.yaml` die Beispiel-Codes :1475 und :1481 auf `media_upload.invalid_entity_type` bzw. `media_upload.invalid_asset_type` aendern und die Property `provisioning` aus `AdminMediaUploadResponse` (:9058) entfernen. (6) In `media_upload_test.go` `mockAssetLifecycleStore` und alle `WithLifecycleService(services.NewAssetLifecycleService(...))`-Setups entfernen; Tests, die auf die alten Codes assertieren, auf die neuen `media_upload.*`-Codes umstellen (NICHT loeschen). NICHT ANFASSEN: `frontend/src/types/admin.ts:691` (`provisioning?` ist optional, nicht build-breaking, Phase-109-Scope gemaess D-03).</action>
  <acceptance_criteria>
    - `grep -rn "asset_lifecycle\|AssetLifecycle" backend/ --include=*.go` liefert KEINE Treffer.
    - `grep -rn "asset_lifecycle" shared/contracts/openapi.yaml` liefert KEINE Treffer.
    - `grep -c "media_upload.invalid_entity_type\|media_upload.invalid_asset_type\|media_upload.unsafe_path" backend/internal/handlers/media_upload_errors.go` == 3.
    - `grep -c "media_upload.invalid_entity_type" shared/contracts/openapi.yaml` >= 1 und `grep -c "media_upload.invalid_asset_type" shared/contracts/openapi.yaml` >= 1.
    - `grep -rn "ProvisioningResult\|ensureProvisioning\|WithLifecycleService\|mediaUploadLifecycle" backend/ --include=*.go` liefert KEINE Treffer.
    - `grep -q "filepath.Join(h.mediaStorageDir, req.EntityType" backend/internal/handlers/media_upload.go` liefert Treffer (kanonische Root-Pfad-Formel uebernommen).
    - `grep -q "isUploadPathWithinBase" backend/internal/handlers/media_upload.go` liefert Treffer (Traversal-Schutz erhalten).
    - `cd backend && go build ./... && go vet ./...` gruen.
    - `cd backend && go test ./internal/handlers/... -run MediaUpload` gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./... && go test ./internal/handlers/... -run MediaUpload && ! grep -rq "asset_lifecycle\|AssetLifecycle" internal/ cmd/ --include=*.go && ! grep -rq "ProvisioningResult\|ensureProvisioning\|WithLifecycleService" internal/ --include=*.go && ! grep -q "asset_lifecycle" ../shared/contracts/openapi.yaml && grep -q "media_upload.invalid_entity_type" ../shared/contracts/openapi.yaml && grep -q "isUploadPathWithinBase" internal/handlers/media_upload.go && echo CLUSTER-G-OK</automated>
  </verify>
  <done>Handler asset_lifecycle-frei, Fehlercodes definiert nach `media_upload.*` migriert und im OpenAPI-Contract nachgezogen, Speicherpfad auf die kanonische Fallback-Formel fixiert, Traversal-Schutz erhalten, V2-Upload intakt, Build + Upload-Tests gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Keine neue. Entfernung interner Ordnerprovisionierung; Upload-Endpunkt-Verhalten/Auth unveraendert (Phase 108).

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-06-01 | Denial of Service | V2-Upload bricht durch Entkopplung des Handlers | mitigate | Handler-Feld + Nutzung entfernt, direkter Pfad-Write bleibt; `go build` + Upload-Handler-Test als Gate. |
| T-106-06-02 | Tampering | Frei erfundene Fehlercodes beim Entkoppeln, stille API-Contract-Aenderung fuer Admin-UI-Konsumenten | mitigate | Codes sind im Plan namentlich festgelegt (`media_upload.invalid_entity_type`/`invalid_asset_type`/`unsafe_path`); grep-Acceptance auf Konstante UND OpenAPI-Beispiel; `asset_lifecycle`-Rest-grep muss auch im Contract leer sein. |
| T-106-06-03 | Elevation of Privilege | Wegfall der Pfad-Traversal-Pruefung beim Ersetzen der Provisionierung, Schreiben ausserhalb des Media-Roots | mitigate | `resolveUploadStoragePath`/`isUploadPathWithinBase` bleiben unveraendert und werden per grep-Acceptance nachgewiesen; nur der Fehlertyp wechselt. |
| T-106-06-SC | Tampering | Package-Installs | accept | Keine Installs. |
Kein neuer Angriffsvektor.
</threat_model>

<verification>
- `cd backend && go build ./... && go vet ./...` gruen.
- `cd backend && go test ./internal/handlers/... -run MediaUpload` gruen.
- grep leer fuer `asset_lifecycle`/`AssetLifecycle` in Go UND in `shared/contracts/openapi.yaml`.
- Fehlercodes `media_upload.invalid_entity_type|invalid_asset_type|unsafe_path` in Handler-Konstanten und Contract vorhanden.
- `isUploadPathWithinBase`-Traversal-Schutz erhalten.
</verification>

<success_criteria>
SC2-Teil erfuellt: Cluster G vollstaendig entfernt, MediaUploadHandler entkoppelt, V2-Upload funktional, Build + Tests gruen.
</success_criteria>

<output>
Create `.planning/phases/106-medienkern-schema-legacy-abbau/106-06-SUMMARY.md` when done.
</output>
