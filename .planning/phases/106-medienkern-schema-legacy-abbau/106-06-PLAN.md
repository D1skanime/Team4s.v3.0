---
phase: 106-medienkern-schema-legacy-abbau
plan: 06
type: execute
wave: 3
depends_on: [106-03, 106-04]
files_modified:
  - backend/internal/services/asset_lifecycle_service.go
  - backend/internal/services/asset_lifecycle_errors.go
  - backend/internal/services/asset_lifecycle_service_test.go
  - backend/internal/models/asset_lifecycle.go
  - backend/internal/repository/asset_lifecycle_audit.go
  - backend/internal/repository/asset_lifecycle_subjects.go
  - backend/internal/repository/asset_lifecycle_repository_test.go
  - backend/internal/models/media_upload_audit.go
  - backend/internal/repository/media_upload_subjects.go
  - backend/internal/repository/media_upload_audit.go
  - backend/internal/repository/media_upload.go
  - backend/cmd/server/main.go
  - backend/internal/handlers/media_upload.go
  - backend/internal/handlers/media_upload_errors.go
  - backend/internal/handlers/media_upload_image.go
  - backend/internal/handlers/media_upload_video.go
  - backend/internal/handlers/media_upload_test.go
  - backend/internal/models/media_upload.go
  - shared/contracts/openapi.yaml
autonomous: true
requirements: [SC2]
must_haves:
  truths:
    - "SC2/D-04 Cluster G: Der `asset_lifecycle`-NAMENSRAUM ist vollstaendig verschwunden — Service, Errors, Model, Repo-Dateien und die zugehoerigen Tests sind geloescht, `grep -ri asset_lifecycle` ist in Go UND im Contract leer."
    - "P-5 (RESEARCH-Refresh 2026-07-22 + PO-Entscheid): Der Phasenzweck ist die Entfernung der ORDNERPROVISIONIERUNG, NICHT der Validierungs- und Audit-Logik. Zwei Faehigkeiten des geloeschten Service werden deshalb MIGRIERT statt entfernt: (a) die Existenzpruefung der Ziel-Entity (`LookupAssetLifecycleSubject` → `SELECT id FROM anime WHERE id = $1`) und (b) die Audit-Schreibung nach `admin_anime_mutation_audit`. Ersatzlos entfaellt ausschliesslich `EnsureCanonicalLayout`/`ensureFolders` (`os.MkdirAll` des kanonischen Layouts) — unkritisch, weil `media_upload.go:164` ohnehin `os.MkdirAll(storagePath, 0755)` ausfuehrt."
    - "P-5/Audit (CLAUDE.md-Constraint „Admin-Aktionen brauchen Audit-Attribution nach User-ID\"): Die Audit-Zeile wird weiterhin geschrieben — `INSERT INTO admin_anime_mutation_audit (anime_id, actor_user_id, mutation_kind, request_payload)` mit `actor_user_id` aus `CommentAuthIdentityFromContext`. Nur der `mutation_kind`-Wert wandert mit dem Namensraum: `anime.asset_lifecycle.provision` -> `anime.media_upload.provision`. Historische Zeilen mit dem alten Wert bleiben in der Tabelle liegen (Tabelle wird nicht gedroppt) und sind im SUMMARY zu erwaehnen."
    - "P-5/Validierung: Ein Upload auf eine nicht existierende `entity_id` wird weiterhin mit HTTP 400 abgewiesen. Der Fehlercode wandert in den etablierten `media_upload.*`-Namensraum: `asset_lifecycle.invalid_entity_id` -> `media_upload.invalid_entity_id`. Ohne diese Migration wuerde 106 die Sicherheitslage verschlechtern, was ASVS V4 / §3 des Architekturentscheids explizit verbietet (Endpoint-Haertung ist Phase-108-Scope — 106 darf sie nicht vorwegnehmen, aber auch nicht untergraben)."
    - "Pitfall 5: Der generische V2-Upload bleibt nach Entkopplung kompilierbar und funktional — der MediaUploadHandler traegt kein asset_lifecycle-Feld mehr und keine `mediaUploadLifecycle`-Abstraktion."
    - "main.go konstruiert keinen AssetLifecycle-Service/-Repo mehr und ruft `WithLifecycleService` nicht mehr auf (:306-307 Konstruktion, :309-310 Injektion); keine ungenutzten Imports/Variablen."
    - "API-Contract-Treue (Checker-Warning 3 + P-5): Die Fehlercodes werden NICHT neu erfunden, sondern 1:1 in den `media_upload.*`-Namensraum migriert: `asset_lifecycle.invalid_entity_type` -> `media_upload.invalid_entity_type`, `.invalid_asset_type` -> `media_upload.invalid_asset_type`, `.unsafe_path` -> `media_upload.unsafe_path`, `.invalid_entity_id` -> `media_upload.invalid_entity_id`, `.audit_failed` -> `media_upload.audit_failed`. ERSATZLOS entfaellt nur `asset_lifecycle.invalid_structure` — sein einziger Erzeuger war die Ordner-Strukturpruefung in `ensureFolders`, die mit der Provisionierung faellt. Sie liegen damit im bereits etablierten `media_upload.*`-Namensraum (`media_upload.invalid_request`, `.missing_file`, `.open_failed`, `.validation_failed`, `.storage_prepare_failed`, `.invalid_format`, `.processing_failed`) und werden in `shared/contracts/openapi.yaml` mitgezogen."
    - "Der Speicherpfad ist eindeutig definiert: die BEREITS EXISTIERENDE nil-lifecycle-Fallback-Formel `filepath.Join(h.mediaStorageDir, req.EntityType, fmt.Sprintf(\"%d\", req.EntityID))` aus `ensureProvisioning` (media_upload.go:200) wird zur kanonischen Root-Pfad-Berechnung; `resolveUploadStoragePath` und `isUploadPathWithinBase` behalten ihre Traversal-Pruefung woertlich."
    - "`go build ./... && go vet ./...` + Upload-Handler-Tests bleiben gruen."
  artifacts:
    - path: "backend/internal/handlers/media_upload.go"
      provides: "MediaUploadHandler ohne asset_lifecycle-Verdrahtung, mit erhaltener Entity-Existenzpruefung und Audit-Schreibung, V2-Upload intakt"
      contains: "MediaUploadHandler"
    - path: "backend/internal/repository/media_upload_audit.go"
      provides: "Re-homed Audit-Schreibpfad nach admin_anime_mutation_audit (P-5)"
      contains: "admin_anime_mutation_audit"
    - path: "backend/internal/repository/media_upload_subjects.go"
      provides: "Re-homed Entity-Existenzpruefung (P-5)"
      contains: "SELECT id FROM anime WHERE id = $1"
  key_links:
    - from: "backend/cmd/server/main.go"
      to: "kein AssetLifecycle-Service"
      via: "entfernte Konstruktion (:306-307) + entfernte Injektion (:309-310)"
      pattern: "AssetLifecycle"
    - from: "backend/internal/handlers/media_upload.go (Upload)"
      to: "admin_anime_mutation_audit"
      via: "repo.RecordMediaUploadEvent — Audit-Attribution bleibt erhalten (P-5, CLAUDE.md)"
      pattern: "RecordMediaUploadEvent"
    - from: "backend/internal/handlers/media_upload.go (Upload)"
      to: "anime-Tabelle"
      via: "repo.LookupUploadSubject — Entity-Existenzpruefung bleibt erhalten (P-5)"
      pattern: "LookupUploadSubject"
---

<objective>
Entfernt den verzahnten `asset_lifecycle`-Cluster (Ordnerprovisionierung) und entkoppelt ihn vom `MediaUploadHandler`, sodass der generische V2-Upload in 106 weiter kompiliert und laeuft (Pitfall 5). Dieser Plan laeuft NACH 106-03 (main.go, openapi.yaml) und 106-04 (media_upload.go, media_upload_test.go), da er dieselben Dateien beruehrt.

REVISION (RESEARCH-Refresh 2026-07-22, Befund P-5 + PO-Entscheid): Die Vorfassung liess mit dem Service auch die ENTITY-EXISTENZPRUEFUNG und die AUDIT-SCHREIBUNG ersatzlos fallen — beides ist keine Ordnerprovisionierung und beides ist live verdrahtet (`main.go:309-310 WithLifecycleService`). Der Audit-Wegfall kollidiert direkt mit dem CLAUDE.md-Constraint „Admin-Aktionen brauchen Audit-Attribution nach User-ID"; der Wegfall der Existenzpruefung verschlechtert die Sicherheitslage eines Endpoints, der laut §3 des Architekturentscheids ohnehin noch kein Capability-Gate hat (Haertung erst Phase 108). PO-Entscheid: BEIDE Faehigkeiten werden aus dem `asset_lifecycle`-Namensraum HERAUSMIGRIERT statt geloescht. Ersatzlos faellt nur `EnsureCanonicalLayout`/`ensureFolders` — verifiziert unkritisch, weil `media_upload.go:164` ohnehin `os.MkdirAll(storagePath, 0755)` ausfuehrt.

Purpose: SC2-Anteil — groesster verzahnter Backend-Cluster, Build-Gruenhaltung kritisch, Verhaltens- und Auditparitaet gewahrt.
Output: 7 geloeschte asset_lifecycle-Dateien, 3 neue media_upload-Dateien (Modell, Subjects-Repo, Audit-Repo), entkoppeltes main.go + media_upload-Handler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-medienkern-schema-legacy-abbau/106-RESEARCH.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-PATTERNS.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-CONTEXT.md

<interfaces>
<!-- Verifizierte Cluster-G-Verdrahtung (RESEARCH-Refresh §1 Item G + §2 P-5). -->
ZU LOESCHEN (7 Dateien): services/asset_lifecycle_service.go, services/asset_lifecycle_errors.go,
services/asset_lifecycle_service_test.go, models/asset_lifecycle.go, repository/asset_lifecycle_audit.go,
repository/asset_lifecycle_subjects.go, repository/asset_lifecycle_repository_test.go.
- main.go:306 `NewAssetLifecycleRepository(...)`, :307 `NewAssetLifecycleService(...)` (Konstruktion entfernen)
  + :309-310 `.WithLifecycleService(...)`-Injektion in den MediaUploadHandler (MUSS mitfallen).
- handlers/media_upload.go:64-66 Feld `lifecycle mediaUploadLifecycle`, :62-64 Interface `mediaUploadLifecycle`,
  :85-88 `WithLifecycleService`, :185-205 `ensureProvisioning` → alle entfernen.
- handlers/media_upload_test.go: `mockAssetLifecycleStore` + `WithLifecycleService(...)`-Setups → mit anpassen.

<!-- P-5: WAS AUS DEM GELOESCHTEN CLUSTER GERETTET WIRD (verbindliche Zielnamen, NICHT improvisieren). -->
Drei Faehigkeiten des Service, getrennt bewertet:

| Faehigkeit | Heutige Fundstelle | Entscheid | Zielort nach 106-06 |
|------------|--------------------|-----------|---------------------|
| Ordnerprovisionierung `EnsureCanonicalLayout` / `ensureFolders` / `resolveCanonicalRoot` / `Phase6AnimeAssetLifecyclePolicy` / `models.ProvisioningResult` | asset_lifecycle_service.go:118-215, models/asset_lifecycle.go:1-26 | **ERSATZLOS ENTFERNEN** | — (`media_upload.go:164 os.MkdirAll(storagePath, 0755)` deckt die Verzeichniserstellung bereits ab — verifiziert) |
| Existenzpruefung der Ziel-Entity `LookupAssetLifecycleSubject` (`SELECT id FROM anime WHERE id = $1`, `pgx.ErrNoRows` → `ErrNotFound` → HTTP 400) | repository/asset_lifecycle_subjects.go:26-56, service :138-146 | **MIGRIEREN (P-5)** | `repository/media_upload_subjects.go` → `LookupUploadSubject` |
| Audit-Schreibung nach `admin_anime_mutation_audit` | repository/asset_lifecycle_audit.go:11-63, service :225-252 | **MIGRIEREN (P-5, CLAUDE.md-Constraint)** | `repository/media_upload_audit.go` → `RecordMediaUploadEvent` |

VERBINDLICHE ZIELNAMEN (kein Bezeichner darf `asset_lifecycle`/`AssetLifecycle` enthalten):

`backend/internal/models/media_upload_audit.go` (neu, Package `models`):
  type MediaUploadAuditEntry struct { ActorUserID int64; EntityType string; EntityID int64;
    AssetType string; Action string; Outcome string; Details map[string]any }
  — Feldsatz 1:1 aus `models.AssetLifecycleAuditEntry` (models/asset_lifecycle.go:28-36) uebernehmen,
    inkl. der bestehenden json-Tags, damit die Audit-Payload byte-gleich bleibt.

`backend/internal/repository/media_upload_subjects.go` (neu, Package `repository`):
  func normalizeUploadSubjectEntityType(entityType string) string        // strings.TrimSpace + ToLower
  func buildLookupUploadSubjectQuery(entityType string) (string, bool)   // "anime" → `SELECT id FROM anime WHERE id = $1`
  func (r *MediaUploadRepository) LookupUploadSubject(ctx context.Context, entityType string, entityID int64) (int64, error)
  — Logik 1:1 aus `LookupAssetLifecycleSubject` (asset_lifecycle_subjects.go:35-56): unbekannter entityType → `ErrNotFound`;
    `pgx.ErrNoRows` → `ErrNotFound`; sonstige Fehler gewrappt. Rueckgabe ist die gescannte `id` (der bisherige
    `models.AssetLifecycleSubject`-Wrapper faellt weg, weil `EntityType` beim Aufrufer bereits normalisiert vorliegt).

`backend/internal/repository/media_upload_audit.go` (neu, Package `repository`):
  const adminAnimeMutationKindMediaUploadProvision = "anime.media_upload.provision"
  func buildMediaUploadMutationKind(action string) string                 // "provision" → obige Konstante, sonst "anime.media_upload." + action
  func buildMediaUploadAuditPayload(entry models.MediaUploadAuditEntry) ([]byte, error)   // json.Marshal derselben 6 Keys wie heute
  func (r *MediaUploadRepository) RecordMediaUploadEvent(ctx context.Context, entry models.MediaUploadAuditEntry) error
  — Logik 1:1 aus `RecordAssetLifecycleEvent` (asset_lifecycle_audit.go:32-63): `ActorUserID <= 0` → Fehler;
    entityType != "anime" → Fehler; `INSERT INTO admin_anime_mutation_audit (anime_id, actor_user_id, mutation_kind, request_payload)`.
    EINZIGE inhaltliche Aenderung: der `mutation_kind`-Wert `anime.asset_lifecycle.provision` → `anime.media_upload.provision`.

INTERFACE-ERWEITERUNG `repository/media_upload.go` (`MediaUploadRepo`, :18-28 — nach dem 106-04-Shrink):
  LookupUploadSubject(ctx context.Context, entityType string, entityID int64) (int64, error)
  RecordMediaUploadEvent(ctx context.Context, entry models.MediaUploadAuditEntry) error
  → `MockMediaUploadRepository` in handlers/media_upload_test.go MUSS beide Methoden implementieren
    (Mock-Subject-Lookup: konfigurierbarer Satz existierender IDs; Mock-Audit: Aufzeichnung in einem Slice
    zur Assertion, dass die Audit-Zeile geschrieben wurde).

<!-- Verifizierte Kopplungspunkte (Checker-Warning 3). -->
Fehlercode-Konstanten (services/asset_lifecycle_errors.go:23-37) und ihre Handler-Nutzung:
- `AssetLifecycleCodeInvalidEntityType` = "asset_lifecycle.invalid_entity_type" -> media_upload.go:108
- `AssetLifecycleCodeInvalidAssetType`  = "asset_lifecycle.invalid_asset_type"  -> media_upload.go:114
- `AssetLifecycleCodeUnsafePath`        = "asset_lifecycle.unsafe_path"         -> media_upload.go:212, :219
- `AssetLifecycleCodeInvalidEntityID`   = "asset_lifecycle.invalid_entity_id"   -> service :141 (Existenzpruefung, P-5)
- `AssetLifecycleCodeAuditFailed`       = "asset_lifecycle.audit_failed"        -> service :245 (Audit-Fehler, P-5)
- `AssetLifecycleCodeInvalidStructure`  = "asset_lifecycle.invalid_structure"   -> service :181/:202 (NUR Ordnerstruktur → entfaellt)
Fehlertyp: `services.AssetLifecycleError{Code, Message, Details map[string]any, Err}` mit `Error()`/`Unwrap()`;
ausgewertet in `mapUploadError` (media_upload.go:260-271) per `errors.As` + Detail-Extraktion `Details["folder"]`,
Antwort immer HTTP 400; alles andere faellt auf HTTP 500 `media_upload.processing_failed`.
Contract-Oberflaeche: `shared/contracts/openapi.yaml:1475` (`invalidEntityType`) und `:1481` (`invalidAssetType`)
fuehren die alten Codes als Response-Beispiele; `:9044 AdminMediaUploadResponse` fuehrt `:9058 provisioning`.
ABGRENZUNG: Plan 106-03 (Welle 2) editiert in derselben Datei ausschliesslich die Release-Images-Bloecke
(:6522/:12612/:12655) — verschiedene Wellen, keine Kollision, kein gemeinsamer Bereich.
Typ-Kopplung: `models.ProvisioningResult` (models/asset_lifecycle.go:20) wird verwendet in
`models.UploadResponse.Provisioning` (models/media_upload.go:43), `handlers/media_upload.go:194/:196`,
`handlers/media_upload_image.go:39/:102/:154`, `handlers/media_upload_video.go:30/:138`.
Speicherpfad: `resolveUploadStoragePath(provisioning.RootPath, ...)` (media_upload.go:157/:207) — der
nil-lifecycle-Fallback `filepath.Join(h.mediaStorageDir, req.EntityType, fmt.Sprintf("%d", req.EntityID))`
(media_upload.go:200) liefert bereits heute exakt denselben Root-Pfad und ist der vorgesehene Ersatz.
Frontend: `frontend/src/types/admin.ts:691` fuehrt `provisioning?:` OPTIONAL — Wegfall des Feldes ist nicht
build-breaking; das FE wird gemaess D-03 in Phase 109 bereinigt und hier NICHT angefasst.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entity-Lookup + Audit aus dem asset_lifecycle-Namensraum herausmigrieren (P-5)</name>
  <files>backend/internal/models/media_upload_audit.go, backend/internal/repository/media_upload_subjects.go, backend/internal/repository/media_upload_audit.go, backend/internal/repository/media_upload.go</files>
  <read_first>
    - backend/internal/repository/asset_lifecycle_subjects.go (komplett, 57 Z. — `LookupAssetLifecycleSubject` inkl. `pgx.ErrNoRows`-Behandlung)
    - backend/internal/repository/asset_lifecycle_audit.go (komplett, 64 Z. — `RecordAssetLifecycleEvent`, Payload-Aufbau, INSERT-Statement)
    - backend/internal/models/asset_lifecycle.go:28-36 (`AssetLifecycleAuditEntry` — Feldsatz + json-Tags)
    - backend/internal/services/asset_lifecycle_service.go:118-160 und :225-252 (wo Lookup und Audit heute aufgerufen werden, mit welchen Argumenten und in welcher Reihenfolge)
    - backend/internal/repository/media_upload.go:15-45 (das nach 106-04 verkleinerte `MediaUploadRepo`-Interface + `MediaUploadRepository`-Struct)
    - 106-RESEARCH.md §2 P-5 (Verlustanalyse) + §11 Security Domain (ASVS V4)
  </read_first>
  <action>Lege die drei neuen Dateien exakt nach der Namenstabelle im `<interfaces>`-Abschnitt an — keine Namensimprovisation, kein Bezeichner darf `asset_lifecycle`/`AssetLifecycle` (auch nicht in Kommentaren) enthalten. (1) `backend/internal/models/media_upload_audit.go`: `MediaUploadAuditEntry` mit dem Feldsatz und den json-Tags von `models.AssetLifecycleAuditEntry`, damit die Audit-Payload byte-gleich bleibt. (2) `backend/internal/repository/media_upload_subjects.go`: `normalizeUploadSubjectEntityType`, `buildLookupUploadSubjectQuery` (nur `anime` → `SELECT id FROM anime WHERE id = $1`) und `(*MediaUploadRepository).LookupUploadSubject` — Logik 1:1 aus `LookupAssetLifecycleSubject` uebernehmen: unbekannter `entityType` → `ErrNotFound`, `pgx.ErrNoRows` → `ErrNotFound`, sonstige Fehler gewrappt; Rueckgabe ist die gescannte `id` (kein Subject-Wrapper mehr). (3) `backend/internal/repository/media_upload_audit.go`: `adminAnimeMutationKindMediaUploadProvision = "anime.media_upload.provision"`, `buildMediaUploadMutationKind`, `buildMediaUploadAuditPayload` und `(*MediaUploadRepository).RecordMediaUploadEvent` — Logik 1:1 aus `RecordAssetLifecycleEvent`, EINZIGE inhaltliche Aenderung ist der neue `mutation_kind`-Wert. Das INSERT-Statement (`INSERT INTO admin_anime_mutation_audit (anime_id, actor_user_id, mutation_kind, request_payload) VALUES ($1,$2,$3,$4)`) und die Guards (`ActorUserID <= 0`, entityType != "anime") bleiben woertlich. (4) Erweitere in `backend/internal/repository/media_upload.go` das Interface `MediaUploadRepo` um die beiden Methoden `LookupUploadSubject` und `RecordMediaUploadEvent`. Die alten `asset_lifecycle_*`-Dateien noch NICHT loeschen (das macht Task 2) — nach dieser Task koexistieren beide Implementierungen kurzzeitig, `go build` muss gruen sein.</action>
  <acceptance_criteria>
    - Die drei neuen Dateien existieren und enthalten KEINEN `asset_lifecycle`/`AssetLifecycle`-Bezeichner: `grep -ric "asset_lifecycle" backend/internal/models/media_upload_audit.go backend/internal/repository/media_upload_subjects.go backend/internal/repository/media_upload_audit.go` == 0.
    - `grep -q "SELECT id FROM anime WHERE id = \$1" backend/internal/repository/media_upload_subjects.go` → Treffer (Existenzpruefung erhalten, P-5).
    - `grep -q "INSERT INTO admin_anime_mutation_audit" backend/internal/repository/media_upload_audit.go` → Treffer (Audit-Schreibung erhalten, P-5 / CLAUDE.md).
    - `grep -q "anime.media_upload.provision" backend/internal/repository/media_upload_audit.go` → Treffer; `grep -c "anime.asset_lifecycle.provision" backend/internal/repository/media_upload_audit.go` == 0.
    - `grep -c "LookupUploadSubject\|RecordMediaUploadEvent" backend/internal/repository/media_upload.go` >= 2 (Interface erweitert).
    - `cd backend && go build ./...` gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && [ "$(grep -ric asset_lifecycle internal/models/media_upload_audit.go internal/repository/media_upload_subjects.go internal/repository/media_upload_audit.go | awk -F: '{s+=$2} END {print s}')" = "0" ] && grep -q "INSERT INTO admin_anime_mutation_audit" internal/repository/media_upload_audit.go && grep -q "FROM anime WHERE id" internal/repository/media_upload_subjects.go && grep -q "anime.media_upload.provision" internal/repository/media_upload_audit.go && grep -q "LookupUploadSubject" internal/repository/media_upload.go && echo P5-REHOME-OK</automated>
  </verify>
  <done>Entity-Existenzpruefung und Audit-Schreibung leben unter media_upload-neutralen Namen weiter; Interface erweitert; Build gruen.</done>
</task>

<task type="auto">
  <name>Task 2: asset_lifecycle-Dateien loeschen + main.go entkoppeln</name>
  <files>backend/internal/services/asset_lifecycle_service.go, backend/internal/services/asset_lifecycle_errors.go, backend/internal/services/asset_lifecycle_service_test.go, backend/internal/models/asset_lifecycle.go, backend/internal/repository/asset_lifecycle_audit.go, backend/internal/repository/asset_lifecycle_subjects.go, backend/internal/repository/asset_lifecycle_repository_test.go, backend/cmd/server/main.go</files>
  <read_first>
    - backend/internal/services/asset_lifecycle_service.go (Konstruktor-Signatur + oeffentliche Methoden, um Nutzung im Handler zu verstehen)
    - backend/cmd/server/main.go:300-320 (AssetLifecycle-Konstruktion :306-307 + Injektion `.WithLifecycleService(...)` :309-310)
    - backend/internal/repository/asset_lifecycle_repository_test.go (welche Testfaelle Lookup/Audit abdecken — sie werden in Task 3 in den Handler-Test uebertragen, NICHT ersatzlos verworfen)
    - 106-RESEARCH.md §1 Item G (Live-Verdrahtung) + §2 P-5
  </read_first>
  <action>Loesche die 7 asset_lifecycle-Dateien (services: `_service.go`, `_errors.go`, `_service_test.go`; models: `asset_lifecycle.go`; repository: `_audit.go`, `_subjects.go`, `_repository_test.go`). Der in Task 1 herausmigrierte Lookup- und Audit-Code ist zu diesem Zeitpunkt bereits vorhanden — es geht KEINE Faehigkeit verloren, nur die Ordnerprovisionierung. Entferne in `backend/cmd/server/main.go` die `NewAssetLifecycleRepository`/`NewAssetLifecycleService`-Konstruktion (~:306-307) UND die Injektionszeile `.WithLifecycleService(...)` (~:309-310) — letztere muss zwingend mitfallen, sonst bleibt ein Aufruf auf eine geloeschte Methode stehen. Entferne dadurch ungenutzt gewordene Imports/Variablen. Der Zwischenstand-Build darf rot sein, bis Task 3 die Handler-Seite entkoppelt; arbeite so, dass nach Task 3 alles gruen ist. (Bei zirkulaerer Compile-Abhaengigkeit: Handler-Feld in Task 3 zuerst entfernen, dann main.go — Reihenfolge frei, Endzustand zaehlt.)</action>
  <acceptance_criteria>
    - Alle 7 asset_lifecycle-Dateien existieren nicht mehr.
    - `grep -in "AssetLifecycle" backend/cmd/server/main.go` liefert KEINE Treffer.
    - `grep -c "WithLifecycleService" backend/cmd/server/main.go` == 0.
    - Die drei in Task 1 angelegten Dateien existieren weiterhin (kein versehentliches Mitloeschen der Migration).
    - (Zwischenstand-Build darf noch rot sein, bis Task 3 den Handler entkoppelt — finaler Build in Task 3.)
  </acceptance_criteria>
  <verify>
    <automated>[ ! -f backend/internal/services/asset_lifecycle_service.go ] && [ ! -f backend/internal/models/asset_lifecycle.go ] && [ ! -f backend/internal/repository/asset_lifecycle_audit.go ] && [ ! -f backend/internal/repository/asset_lifecycle_subjects.go ] && ! grep -qi "AssetLifecycle" backend/cmd/server/main.go && [ -f backend/internal/repository/media_upload_audit.go ] && [ -f backend/internal/repository/media_upload_subjects.go ] && echo FILES-DELETED-OK</automated>
  </verify>
  <done>asset_lifecycle-Dateien geloescht, main.go-Konstruktion UND -Injektion entfernt, die migrierten Faehigkeiten aus Task 1 unversehrt.</done>
</task>

<task type="auto">
  <name>Task 3: MediaUploadHandler entkoppeln — Fehlercodes migrieren, Existenzpruefung + Audit verdrahten, Speicherpfad fixieren</name>
  <files>backend/internal/handlers/media_upload.go, backend/internal/handlers/media_upload_errors.go, backend/internal/handlers/media_upload_image.go, backend/internal/handlers/media_upload_video.go, backend/internal/handlers/media_upload_test.go, backend/internal/models/media_upload.go, shared/contracts/openapi.yaml</files>
  <read_first>
    - backend/internal/handlers/media_upload.go:60-235 (Struct-Feld, Interface `mediaUploadLifecycle`, WithLifecycleService, Upload-Flow, ensureProvisioning, resolveUploadStoragePath)
    - backend/internal/handlers/media_upload.go:255-290 (mapUploadError + writeUploadError — Ausgabeformat und HTTP-Status-Zuordnung)
    - backend/internal/services/asset_lifecycle_errors.go (exakte Code-Strings + Fehlertyp-Struktur — VOR dem Loeschen in Task 2 lesen bzw. aus dem `<interfaces>`-Abschnitt uebernehmen)
    - backend/internal/services/asset_lifecycle_service.go:130-160 (Reihenfolge: assetType-Pruefung → Subject-Lookup → Audit) und :225-252 (recordAudit inkl. Fehlerpfad)
    - backend/internal/models/media_upload.go:35-50 (UploadResponse.Provisioning)
    - backend/internal/handlers/media_upload_image.go:35-160 und media_upload_video.go:25-145 (provisioning-Parameter + Response-Befuellung)
    - backend/internal/handlers/media_upload_test.go (mockAssetLifecycleStore + WithLifecycleService-Setups + MockMediaUploadRepository)
    - shared/contracts/openapi.yaml:1455-1495 und :9040-9065 (Fehlercode-Beispiele + AdminMediaUploadResponse.provisioning)
    - 106-RESEARCH.md Pitfall 5 + §2 P-5
  </read_first>
  <action>ERSATZ STATT ERFINDUNG — die Fehlercodes sind API-Oberflaeche und duerfen nicht frei neu vergeben werden.

(1) FEHLERTYP + CODES: Lege `backend/internal/handlers/media_upload_errors.go` (Package `handlers`) an mit einem lokalen Fehlertyp `uploadError` (Felder `Code string`, `Message string`, `Details map[string]any`, `Err error`; Methoden `Error() string` liefert `Message`, `Unwrap() error` liefert `Err`) — strukturgleich zum bisherigen `services.AssetLifecycleError`, damit `mapUploadError` unveraendert per `errors.As` + `Details["folder"]` arbeiten kann. Definiere darin FUENF Konstanten mit EXAKT diesen Werten: `uploadCodeInvalidEntityType = "media_upload.invalid_entity_type"`, `uploadCodeInvalidAssetType = "media_upload.invalid_asset_type"`, `uploadCodeUnsafePath = "media_upload.unsafe_path"`, `uploadCodeInvalidEntityID = "media_upload.invalid_entity_id"` (P-5), `uploadCodeAuditFailed = "media_upload.audit_failed"` (P-5). Keine weiteren Codes erfinden. ERSATZLOS entfaellt ausschliesslich `asset_lifecycle.invalid_structure` — sein einziger Erzeuger war die Ordner-Strukturpruefung in `ensureFolders`, die mit der Provisionierung faellt.

(2) CALL-SITES: In `media_upload.go` die Stellen :108 / :114 / :212 / :219 auf die neuen Konstanten und `&uploadError{...}` umstellen; `mapUploadError` von `*services.AssetLifecycleError` auf `*uploadError` umstellen (HTTP-400-Zuordnung und der 500er-Fallback `media_upload.processing_failed` bleiben unveraendert); alle uebrigen `media_upload.*`-Codes bleiben, wie sie sind.

(3) EXISTENZPRUEFUNG + AUDIT + SPEICHERPFAD (P-5, Kern dieser Task): Entferne `ensureProvisioning`, das Struct-Feld `lifecycle`, das Interface `mediaUploadLifecycle` und `WithLifecycleService`. Ersetze sie durch eine Handler-Methode `resolveUploadRoot(ctx context.Context, actorUserID int64, req models.UploadRequest) (string, error)`, die in dieser Reihenfolge arbeitet — sie spiegelt exakt den bisherigen Ablauf von `EnsureCanonicalLayout` abzueglich der Ordnererstellung:
   a) `if _, err := h.repo.LookupUploadSubject(ctx, req.EntityType, req.EntityID); errors.Is(err, repository.ErrNotFound) { return "", &uploadError{Code: uploadCodeInvalidEntityID, Message: "ungültige anime id", Details: map[string]any{"entity_id": req.EntityID}} } else if err != nil { return "", err }` — deutscher Meldungstext mit echten Umlauten (CLAUDE.md), Wortlaut 1:1 vom geloeschten Service uebernommen.
   b) `rootPath := filepath.Join(h.mediaStorageDir, req.EntityType, fmt.Sprintf("%d", req.EntityID))` — die bereits existierende kanonische Fallback-Formel.
   c) Audit-Zeile schreiben: `h.repo.RecordMediaUploadEvent(ctx, models.MediaUploadAuditEntry{ActorUserID: actorUserID, EntityType: req.EntityType, EntityID: req.EntityID, AssetType: req.AssetType, Action: "provision", Outcome: "success"})`. Schlaegt das fehl, `&uploadError{Code: uploadCodeAuditFailed, Message: "upload audit fehlgeschlagen", Details: map[string]any{"entity_id": req.EntityID, "entity_type": req.EntityType}, Err: err}` zurueckgeben — das entspricht dem heutigen Verhalten (Audit-Fehler war schon bisher ein harter Fehler mit HTTP 400).
   d) `rootPath` zurueckgeben; der Aufrufer ruft danach wie bisher `h.resolveUploadStoragePath(rootPath, req.AssetType, mediaID)`.
   `resolveUploadStoragePath` und `isUploadPathWithinBase` behalten ihre Traversal-Pruefung WOERTLICH — nur der Fehlertyp wechselt von `*services.AssetLifecycleError` auf `*uploadError`.

(4) PROVISIONING-TYP: `models.ProvisioningResult` verschwindet mit Task 2; entferne daher das Feld `Provisioning *ProvisioningResult` aus `models.UploadResponse` (models/media_upload.go:43) sowie den `provisioning`-Parameter und die `Provisioning:`-Zuweisungen in `media_upload_image.go` (:39/:102/:154) und `media_upload_video.go` (:30/:138). Alle uebrigen Response-Felder (`id`, `status`, `files`, `url`) bleiben unveraendert.

(5) CONTRACT NACHZIEHEN: in `shared/contracts/openapi.yaml` die Beispiel-Codes :1475 und :1481 auf `media_upload.invalid_entity_type` bzw. `media_upload.invalid_asset_type` aendern und die Property `provisioning` aus `AdminMediaUploadResponse` (:9058) entfernen. Ergaenze im `"400"`-Beispielblock desselben Endpoints ein weiteres Beispiel `invalidEntityId` mit `code: media_upload.invalid_entity_id` und `message: ungueltige anime id` — die Existenzpruefung bleibt erhalten (P-5) und der Contract muss sie fuehren. STRIKT NICHT ANFASSEN: die von Plan 106-03 (Welle 2) bereits entfernten Release-Images-Bloecke bzw. der lebende `/releases/{releaseId}/assets`-Pfad.

(6) TESTS: In `media_upload_test.go` `mockAssetLifecycleStore` und alle `WithLifecycleService(services.NewAssetLifecycleService(...))`-Setups entfernen. `MockMediaUploadRepository` um die zwei neuen Interface-Methoden erweitern: `LookupUploadSubject` (konfigurierbarer Satz existierender IDs, Default: die im Test verwendete ID existiert; unbekannte ID → `repository.ErrNotFound`) und `RecordMediaUploadEvent` (haengt den Eintrag an einen Slice an). Tests, die auf die alten Codes assertieren, auf die neuen `media_upload.*`-Codes umstellen (NICHT loeschen). Ergaenze zwei Regressionstests, die die aus `asset_lifecycle_repository_test.go` verlorene Abdeckung ersetzen: (i) Upload auf eine nicht existierende `entity_id` antwortet mit HTTP 400 und `code == "media_upload.invalid_entity_id"`; (ii) ein erfolgreicher Upload hat GENAU EINEN `RecordMediaUploadEvent`-Aufruf mit `ActorUserID` == der authentifizierten User-ID und `Action == "provision"` erzeugt.

NICHT ANFASSEN: `frontend/src/types/admin.ts:691` (`provisioning?` ist optional, nicht build-breaking, Phase-109-Scope gemaess D-03).</action>
  <acceptance_criteria>
    - `grep -rni "asset_lifecycle\|AssetLifecycle" backend/ --include=*.go` liefert KEINE Treffer.
    - `grep -ci "asset_lifecycle" shared/contracts/openapi.yaml` == 0.
    - Fehlercode-Migration vollstaendig: `grep -c "media_upload.invalid_entity_type\|media_upload.invalid_asset_type\|media_upload.unsafe_path\|media_upload.invalid_entity_id\|media_upload.audit_failed" backend/internal/handlers/media_upload_errors.go` == 5.
    - `grep -c "media_upload.invalid_structure" backend/internal/handlers/media_upload_errors.go` == 0 (der einzige ersatzlos entfallende Code).
    - Contract: `grep -c "media_upload.invalid_entity_type" shared/contracts/openapi.yaml` >= 1, `grep -c "media_upload.invalid_asset_type" shared/contracts/openapi.yaml` >= 1 UND `grep -c "media_upload.invalid_entity_id" shared/contracts/openapi.yaml` >= 1 (P-5: Existenzpruefung ist Contract-Oberflaeche).
    - P-5-Verdrahtung: `grep -c "LookupUploadSubject" backend/internal/handlers/media_upload.go` >= 1 UND `grep -c "RecordMediaUploadEvent" backend/internal/handlers/media_upload.go` >= 1.
    - `grep -rni "ProvisioningResult\|ensureProvisioning\|WithLifecycleService\|mediaUploadLifecycle" backend/ --include=*.go` liefert KEINE Treffer.
    - `grep -q "filepath.Join(h.mediaStorageDir, req.EntityType" backend/internal/handlers/media_upload.go` liefert Treffer (kanonische Root-Pfad-Formel uebernommen).
    - `grep -q "isUploadPathWithinBase" backend/internal/handlers/media_upload.go` liefert Treffer (Traversal-Schutz erhalten).
    - D-03-Grenze: `git diff --name-only frontend/` ist LEER.
    - `cd backend && go build ./... && go vet ./...` gruen.
    - `cd backend && go test ./internal/handlers/... -run MediaUpload` gruen — inkl. der zwei neuen Regressionstests (invalid_entity_id → 400; genau ein Audit-Event pro erfolgreichem Upload).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./... && go test ./internal/handlers/... -run MediaUpload && ! grep -rqi "asset_lifecycle" internal/ cmd/ --include=*.go && ! grep -rq "ProvisioningResult\|ensureProvisioning\|WithLifecycleService" internal/ --include=*.go && ! grep -qi "asset_lifecycle" ../shared/contracts/openapi.yaml && grep -q "media_upload.invalid_entity_type" ../shared/contracts/openapi.yaml && grep -q "media_upload.invalid_entity_id" ../shared/contracts/openapi.yaml && grep -q "LookupUploadSubject" internal/handlers/media_upload.go && grep -q "RecordMediaUploadEvent" internal/handlers/media_upload.go && grep -q "isUploadPathWithinBase" internal/handlers/media_upload.go && [ -z "$(git diff --name-only -- ../frontend/)" ] && echo CLUSTER-G-OK</automated>
  </verify>
  <done>Handler asset_lifecycle-frei; Fehlercodes definiert nach `media_upload.*` migriert und im Contract nachgezogen; Entity-Existenzpruefung und Audit-Schreibung nachweislich erhalten (P-5); Speicherpfad auf die kanonische Fallback-Formel fixiert; Traversal-Schutz erhalten; V2-Upload intakt; Build + Upload-Tests gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
`POST /api/admin/upload` (authentifiziert via `CommentAuthIdentityFromContext`, ohne Capability-Gate — Haertung Phase 108): untrusted `entity_type`/`entity_id`/`asset_type` + Datei-Upload ueberqueren hier die Grenze. Diese Grenze bleibt in 106 unveraendert; der Plan darf sie weder verschieben noch schwaechen.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-06-01 | Denial of Service | V2-Upload bricht durch Entkopplung des Handlers | mitigate | Handler-Feld + Nutzung entfernt, direkter Pfad-Write bleibt; `go build` + Upload-Handler-Test als Gate. |
| T-106-06-02 | Tampering | Frei erfundene Fehlercodes beim Entkoppeln, stille API-Contract-Aenderung fuer Admin-UI-Konsumenten | mitigate | Alle fuenf Codes im Plan namentlich festgelegt; grep-Acceptance auf Konstanten (== 5) UND auf die OpenAPI-Beispiele; `asset_lifecycle`-Rest-grep muss auch im Contract leer sein. |
| T-106-06-03 | Elevation of Privilege | Wegfall der Pfad-Traversal-Pruefung beim Ersetzen der Provisionierung, Schreiben ausserhalb des Media-Roots | mitigate | `resolveUploadStoragePath`/`isUploadPathWithinBase` bleiben woertlich und werden per grep-Acceptance nachgewiesen; nur der Fehlertyp wechselt. |
| T-106-06-04 | Tampering / Resource Abuse | P-5: Wegfall der Entity-Existenzpruefung → Upload auf beliebige `entity_id` legt `<media_root>/anime/<beliebig>/…` an und schreibt DB-Zeilen; ASVS V4 verschlechtert sich auf einem Endpoint ohne Capability-Gate | mitigate | Pruefung MIGRIERT statt entfernt (`LookupUploadSubject` in `media_upload_subjects.go`), Fehlercode `media_upload.invalid_entity_id`, Regressionstest (400 bei unbekannter ID) + grep-Acceptance auf die Verdrahtung im Handler. |
| T-106-06-05 | Repudiation | P-5: Wegfall der Audit-Schreibung → Admin-Upload ohne Attribution nach User-ID; direkter Verstoss gegen den CLAUDE.md-Constraint | mitigate | Audit-Seam MIGRIERT statt entfernt (`RecordMediaUploadEvent` in `media_upload_audit.go`), `actor_user_id` weiterhin aus `CommentAuthIdentityFromContext`; Regressionstest (genau ein Audit-Event je erfolgreichem Upload) + grep-Acceptance auf `INSERT INTO admin_anime_mutation_audit`. |
| T-106-06-06 | Repudiation | Audit-Kontinuitaetsbruch: `mutation_kind` wechselt von `anime.asset_lifecycle.provision` auf `anime.media_upload.provision`, Auswertungen ueber den alten Wert laufen ins Leere | accept | Bewusst akzeptiert — der alte Wert traegt den zu tilgenden Namensraum. Historische Zeilen bleiben erhalten (Tabelle wird nicht angefasst); der Wertwechsel ist im 106-06-SUMMARY zu dokumentieren, damit Auswertungen beide Werte kennen. |
| T-106-06-SC | Tampering | Package-Installs | accept | Keine Installs. |
Kein neuer Angriffsvektor; die Trust Boundary bleibt unveraendert und verliert keine Kontrolle.
</threat_model>

<verification>
- `cd backend && go build ./... && go vet ./...` gruen.
- `cd backend && go test ./internal/handlers/... -run MediaUpload` gruen (inkl. der zwei P-5-Regressionstests).
- grep (case-insensitiv) leer fuer `asset_lifecycle` in Go UND in `shared/contracts/openapi.yaml`.
- Fuenf `media_upload.*`-Fehlercodes in den Handler-Konstanten; `invalid_entity_type`/`invalid_asset_type`/`invalid_entity_id` im Contract.
- P-5-Erhalt positiv belegt: `SELECT id FROM anime WHERE id = $1`, `INSERT INTO admin_anime_mutation_audit`, `LookupUploadSubject`- und `RecordMediaUploadEvent`-Verdrahtung im Handler.
- `isUploadPathWithinBase`-Traversal-Schutz erhalten; `frontend/` unveraendert (D-03).
</verification>

<success_criteria>
SC2-Teil erfuellt: Der `asset_lifecycle`-Namensraum ist vollstaendig verschwunden und die Ordnerprovisionierung ersatzlos entfernt, waehrend Entity-Existenzpruefung und Audit-Attribution nachweislich erhalten bleiben (P-5). MediaUploadHandler entkoppelt, V2-Upload funktional, Build + Tests gruen.
</success_criteria>

<output>
Create `.planning/phases/106-medienkern-schema-legacy-abbau/106-06-SUMMARY.md` when done. Dokumentiere explizit: (a) welche der drei Service-Faehigkeiten entfernt und welche zwei migriert wurden (P-5) inkl. der neuen Bezeichner, (b) den `mutation_kind`-Wertwechsel `anime.asset_lifecycle.provision` -> `anime.media_upload.provision` und dass historische Audit-Zeilen mit dem alten Wert in `admin_anime_mutation_audit` liegen bleiben, (c) die vollstaendige Fehlercode-Mapping-Tabelle alt -> neu inkl. des ersatzlos entfallenen `asset_lifecycle.invalid_structure`.
</output>
