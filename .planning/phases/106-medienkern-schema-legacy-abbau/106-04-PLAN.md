---
phase: 106-medienkern-schema-legacy-abbau
plan: 04
type: execute
wave: 2
depends_on: [106-01]
files_modified:
  - backend/internal/repository/media_upload.go
  - backend/internal/handlers/media_upload_v2_compat.go
  - backend/internal/handlers/media_upload_image.go
  - backend/internal/models/media_upload.go
  - backend/internal/handlers/media_upload_test.go
autonomous: true
requirements: [SC2]
must_haves:
  truths:
    - "SC2/D-04 Cluster B: Legacy-Schema-Erkennung (`SupportsLegacyUploadSchema`, `useLegacyUploadSchema`) und alle `if useLegacy`-Dualbranches sind ersatzlos entfernt; der V2-Upload-Pfad bleibt funktional (Abloesung erst Phase 107)."
    - "SC2/D-04 Cluster B: `media_upload_v2_compat.go` (`legacyUploadSchemaDetector` + `shouldUseAnimePosterPathFallback`) ist geloescht und der Aufruf in `media_upload_image.go:91` aufgeloest."
    - "P-1 (RESEARCH-Refresh 2026-07-22, BLOCKER behoben): `UploadMediaAsset.ID` ist KEIN Legacy-Feld, sondern der OUT-PARAMETER des lebenden V2-Inserts (`repository/media_upload.go:222` `asset.ID = strconv.FormatInt(mediaID, 10)` nach `RETURNING id`). Es BLEIBT. Der DTO-Shrink beschraenkt sich auf die drei tatsaechlich toten UUID-Aera-Felder `EntityType`, `EntityID`, `AssetType` aus `UploadMediaAsset` — diese werden im V2-Pfad nirgends gelesen (der Handler uebergibt `req.EntityType`/`req.EntityID` separat aus `UploadRequest`)."
    - "P-1-Abgrenzung: `UploadRequest.EntityType/EntityID/AssetType` (Form-Binding + Speicherpfad-Formel) und `UploadMediaFile`/`UploadResponse` bleiben VOLLSTAENDIG unveraendert. Jede grep-Assertion auf `EntityType` muss deshalb auf den `UploadMediaAsset`-Struct-Body gescopet sein, nicht auf die ganze Datei."
    - "D-07 (LOCKED): Der `release_media`-SCHREIBPFAD bleibt VOLLSTAENDIG INTAKT — `CreateReleaseMedia` (Interface `media_upload.go:24` + Impl `:290` inkl. `INSERT INTO release_media` `:292`), der Live-Aufrufer `media_upload_storage.go:54` (`case \"release\"`), der Mock `media_upload_test.go:84` und der `release_media`-Eintrag der `DeleteMediaAsset`-Join-Liste `:373` werden in Phase 106 NICHT angefasst. Entfernung erfolgt in Phase 108 (ROADMAP 108-SC 3b)."
    - "Caller-Enumeration verifiziert (Guard gegen Kollateralschaden): `media_upload_storage.go:54` ist der EINZIGE Live-Aufrufer von `CreateReleaseMedia`; weitere Referenzen sind nur Interface `:24`, Impl `:290` und Test-Mock `:84`. Alle bleiben."
    - "`go build ./... && go vet ./...` und die angepassten Upload-Handler-Tests sind gruen; kein `undefined method` durch den Interface-Shrink (Cluster B) und kein `unknown field` durch den DTO-Shrink (Struct-Literal `media_upload_test.go:206`)."
  artifacts:
    - path: "backend/internal/repository/media_upload.go"
      provides: "V2-only Upload-Repository ohne Legacy-Dualpfad; release_media-Junction-Write unveraendert erhalten (D-07)"
      contains: "CreateMediaAsset"
    - path: "backend/internal/models/media_upload.go"
      provides: "UploadMediaAsset mit erhaltenem ID-Out-Parameter, ohne die drei toten UUID-Aera-Felder"
      contains: "ID         string"
  key_links:
    - from: "backend/internal/repository/media_upload.go:222 (createMediaAssetV2)"
      to: "backend/internal/handlers/media_upload_image.go:119 (CreateMediaFile.MediaID) + UploadResponse.ID"
      via: "asset.ID als Out-Parameter der BIGSERIAL-ID — MUSS erhalten bleiben (P-1)"
      pattern: "asset.ID = strconv.FormatInt"
    - from: "backend/internal/handlers/media_upload.go (V2-Upload)"
      to: "media_assets/media_files (lebend)"
      via: "V2-Pfad bleibt intakt"
      pattern: "CreateMediaAsset"
    - from: "backend/internal/handlers/media_upload_storage.go:54 (case \"release\")"
      to: "release_media-Junction (READ-Quelle von ListReleaseAssets)"
      via: "CreateReleaseMedia — unveraendert (D-07)"
      pattern: "case \"release\""
---

<objective>
Kollabiert den Legacy-Dual-Upload-Pfad (Cluster B) auf den V2-only-Pfad: Legacy-Schema-Erkennung, Compat-Shim und die toten UUID-Aera-DTO-Felder fallen ersatzlos. Der generische Upload bleibt in 106 funktional (V2 gegen `media_assets`) — die Abloesung durch den `MediaFileService` folgt in Phase 107.

REVISION 1 (PO-Entscheid 2026-07-21, „Option A" / CONTEXT.md D-07): Die frueher hier gefuehrte Kopplung an die A/B/C-Entscheidung aus 106-05 ENTFAELLT. Der `release_media`-SCHREIBPFAD wird in Phase 106 gar nicht angefasst und wandert samt Lesepfad nach Phase 108. Dieser Plan ist damit unbedingt, autonom und haengt nur noch an 106-01.

REVISION 2 (RESEARCH-Refresh 2026-07-22, Befund P-1 — BLOCKER): Die Vorfassung wies an, `ID string` aus `UploadMediaAsset` zu entfernen. Das haette den produktiven Upload gebrochen: `createMediaAssetV2` schreibt die neu vergebene BIGSERIAL-ID per `asset.ID = strconv.FormatInt(mediaID, 10)` (`repository/media_upload.go:222`) in genau dieses Feld zurueck, und der lebende Handler liest es dreifach weiter (`CreateMediaFile{MediaID: asset.ID}`, `createJoinTableEntryWithRepo(..., asset.ID)`, `UploadResponse.ID`). `ID` BLEIBT. Ausserdem fehlte die Anpassung des Struct-Literals in `media_upload_test.go:206` — sie ist jetzt Bestandteil von Task 2.

Purpose: SC2-Anteil — Compile-Level-Legacy im Upload-Repository entfernen, Upload lauffaehig halten (Pitfall 5-nah).
Output: bereinigtes `media_upload.go`, geloeschtes `media_upload_v2_compat.go`, praezise abgespecktes DTO, angepasster Test-Mock + Struct-Literal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-medienkern-schema-legacy-abbau/106-RESEARCH.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-PATTERNS.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-CONTEXT.md

<interfaces>
<!-- Verifizierte Call-Sites (RESEARCH-Refresh 2026-07-22, §1 Item A2/C + P-1). -->
CLUSTER B — zu entfernen:
- repository/media_upload.go: `SupportsLegacyUploadSchema`(:45/:47), `useLegacyUploadSchema`(:62),
  `if useLegacy`-Branches in CreateMediaAsset(:107/:111), CreateMediaFile(:139/:143),
  CreateAnimeMedia(:170/:180), `coerceMediaReference(mediaID, useLegacy)`(:243/:248).
- handlers/media_upload_v2_compat.go (22 Z.): `legacyUploadSchemaDetector`-Interface + `shouldUseAnimePosterPathFallback` (gibt immer false).
- handlers/media_upload_image.go:91: `usePathFallback, err := h.shouldUseAnimePosterPathFallback(ctx, req)` (Aufruf entfernen; Fallback immer false → Zweig aufloesen).
- handlers/media_upload_test.go:45: Mock `SupportsLegacyUploadSchema` (Test-Guard, muss mit dem Interface-Shrink fallen).

DTO-SHRINK `models/media_upload.go` — FELD FUER FELD (P-1, verbindlich; NICHT pauschal loeschen):

| Struct | Feld | Aktion | Begruendung (verifiziert am realen Baum) |
|--------|------|--------|------------------------------------------|
| `UploadMediaAsset` | `ID string` | **BEHALTEN** | Out-Parameter des V2-Inserts: `repository/media_upload.go:222` `asset.ID = strconv.FormatInt(mediaID, 10)` nach `RETURNING id`. Gelesen in `handlers/media_upload_image.go:119-142` (`CreateMediaFile{MediaID: asset.ID}`, `createJoinTableEntryWithRepo(..., asset.ID)`, `UploadResponse.ID`) und identisch in `media_upload_video.go:91-138`. Entfernen bricht den produktiven Upload. |
| `UploadMediaAsset` | `EntityType string` | **ENTFERNEN** | Im V2-Pfad nie gelesen/geschrieben; der Handler nutzt `req.EntityType` aus `UploadRequest`. |
| `UploadMediaAsset` | `EntityID int64` | **ENTFERNEN** | dito (`req.EntityID`). |
| `UploadMediaAsset` | `AssetType string` | **ENTFERNEN** | dito (`req.AssetType`); der V2-Insert nutzt `mediaTypeForUploadAsset(...)` → `MediaType`. |
| `UploadMediaAsset` | `Format`, `MimeType`, `UploadedBy`, `CreatedAt`, `FilePath`, `MediaType` | **BEHALTEN** | Alle Insert-Parameter von `createMediaAssetV2` (`:193-224`). |
| `UploadMediaFile` | alle Felder | **UNVERAENDERT** | Lebender V2-Varianten-Insert. |
| `UploadRequest` | `EntityType`, `EntityID`, `AssetType` | **UNVERAENDERT** | Gin-Form-Binding (`form:"entity_type"` etc.) + Speicherpfad-Formel. |
| `UploadResponse` | alle Felder | **UNVERAENDERT — in diesem Plan UND in ganz Phase 106** | D-08 (PO-Entscheid 2026-07-22): Der `asset_lifecycle`-Abbau (Cluster G) ist aus Phase 106 herausgenommen, Plan 106-06 ist ersatzlos entfallen. `Provisioning` bleibt also produktiv und ueber alle drei Schichten konsistent (`models/media_upload.go:43` → `shared/contracts/openapi.yaml:9058` → `frontend/src/types/admin.ts:691`); Entfernung erst Phase 107, dort gemeinsam ueber alle drei Schichten. |

FOLGEAENDERUNG (P-1-Zusatz, in Task 2): `handlers/media_upload_test.go:206-214` konstruiert
`models.UploadMediaAsset{ID: mediaID, EntityType: "anime", EntityID: 123, AssetType: "poster", Format: ..., MimeType: ..., CreatedAt: ...}`.
Die drei entfallenden Felder muessen dort mitgezogen werden (`ID` bleibt), sonst `unknown field`-Compile-Fehler.

D-07 — release_media-WRITE-PATH: VOLLSTAENDIG AUSSERHALB DIESES PLANS (LOCKED, CONTEXT.md D-07):
- Interface-Deklaration repository/media_upload.go:24 `CreateReleaseMedia(ctx, releaseID int64, mediaID string, sortOrder int) error` — BLEIBT.
- Impl repository/media_upload.go:290 inkl. `INSERT INTO release_media (release_id, media_id, sort_order)` :292 — BLEIBT.
- EINZIGER Live-Aufrufer handlers/media_upload_storage.go:53-54 `case "release": return repo.CreateReleaseMedia(...)` in `createJoinTableEntryWithRepo` — BLEIBT (Datei steht deshalb NICHT in files_modified).
- Test-Mock handlers/media_upload_test.go:84 `func (m *MockMediaUploadRepository) CreateReleaseMedia(...)` — BLEIBT (nur der Cluster-B-Mock `SupportsLegacyUploadSchema` faellt).
- `DeleteMediaAsset`-joinTables-Eintrag repository/media_upload.go:373 `"release_media"` — BLEIBT.
BEGRUENDUNG: Der Endpoint `GET /releases/:id/assets` liest diese Junction und hat einen lebenden FE-Consumer; ein Kappen des Schreibpfads in 106 wuerde neue Release-Uploads still nicht mehr im Frontend erscheinen lassen (Verstoss gegen die Phasengrenze „kein Verhaltensumbau an Upload/Frontend").

PATTERNS.md:241 flaggt `media_upload_storage.go:53` als compile-kritischen Caller — relevant nur, wenn `CreateReleaseMedia` aus dem Interface faellt. Genau das passiert in 106 NICHT; der Interface-Shrink betrifft ausschliesslich `SupportsLegacyUploadSchema`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Legacy-Dualpfad im Upload-Repository entfernen (Cluster B) + praeziser DTO-Shrink</name>
  <files>backend/internal/repository/media_upload.go, backend/internal/handlers/media_upload_v2_compat.go, backend/internal/handlers/media_upload_image.go, backend/internal/models/media_upload.go</files>
  <read_first>
    - backend/internal/repository/media_upload.go (komplett — Branch-Struktur der 3 Create-Methoden, `createMediaAssetV2` inkl. der Rueckschreib-Zeile `asset.ID = strconv.FormatInt(mediaID, 10)` bei ~:222, coerceMediaReference, die D-07-Marken :24/:290/:292/:373)
    - backend/internal/handlers/media_upload_v2_compat.go (komplett, 22 Z.)
    - backend/internal/handlers/media_upload_image.go:85-145 (shouldUseAnimePosterPathFallback-Aufruf UND die drei `asset.ID`-Weiterverwendungen — Beleg, dass `ID` lebt)
    - backend/internal/models/media_upload.go (komplett, 52 Z. — Feld-fuer-Feld-Tabelle im `<interfaces>`-Abschnitt gegenlesen)
    - 106-RESEARCH.md §2 Befund P-1 (ID ist Out-Parameter)
    - 106-CONTEXT.md D-07 (release_media-Write-Path bleibt)
  </read_first>
  <action>In `repository/media_upload.go`: entferne `SupportsLegacyUploadSchema` (Interface-Deklaration + Impl) und `useLegacyUploadSchema`. In `CreateMediaAsset`, `CreateMediaFile`, `CreateAnimeMedia` die `useLegacy, err := r.useLegacyUploadSchema(ctx)` + `if !useLegacy { ... }`-Verzweigung auf den V2-Zweig kollabieren (nur den `!useLegacy`/V2-Pfad behalten). Die Zeile `asset.ID = strconv.FormatInt(mediaID, 10)` in `createMediaAssetV2` (~:222) MUSS woertlich erhalten bleiben — sie ist der Rueckgabeweg der neuen BIGSERIAL-ID (P-1). `coerceMediaReference(mediaID string, useLegacy bool)` auf `coerceMediaReference(mediaID string)` vereinfachen — immer `strconv.ParseInt`-Pfad (V2), Legacy-`any`-Zweig raus; alle Aufrufer anpassen. Loesche `handlers/media_upload_v2_compat.go` komplett. In `handlers/media_upload_image.go:91` den `shouldUseAnimePosterPathFallback`-Aufruf entfernen und den `usePathFallback`-Zweig auf den `false`-Fall aufloesen (Fallback gab immer false zurueck); die `asset.ID`-Weiterverwendungen ab ~:119 bleiben unveraendert. In `models/media_upload.go` NUR die drei toten UUID-Aera-Felder `EntityType string`, `EntityID int64` und `AssetType string` aus `UploadMediaAsset` entfernen — arbeite dazu die Feld-fuer-Feld-Tabelle aus dem `<interfaces>`-Abschnitt ab. `ID string` BLEIBT (Out-Parameter, P-1). `UploadMediaFile`, `UploadRequest` und `UploadResponse` bleiben in diesem Plan vollstaendig unveraendert — insbesondere behaelt `UploadRequest` seine `EntityType`/`EntityID`/`AssetType`-Form-Bindings. STRIKT NICHT ANFASSEN (D-07): `CreateReleaseMedia` (Interface :24, Impl :290, INSERT :292) und der `"release_media"`-Eintrag der `DeleteMediaAsset`-joinTables-Slice (:373); `handlers/media_upload_storage.go` wird in diesem Plan ueberhaupt nicht editiert. Nach jedem Schritt `go build`.</action>
  <acceptance_criteria>
    - `grep -rn "SupportsLegacyUploadSchema\|useLegacyUploadSchema\|legacyUploadSchemaDetector\|shouldUseAnimePosterPathFallback" backend/ --include=*.go --exclude=*_test.go` liefert KEINE Treffer.
    - `backend/internal/handlers/media_upload_v2_compat.go` existiert nicht mehr.
    - P-1-Erhalt: `grep -c "asset.ID = strconv.FormatInt" backend/internal/repository/media_upload.go` == 1.
    - P-1-Erhalt: `sed -n '/type UploadMediaAsset struct/,/^}/p' backend/internal/models/media_upload.go | grep -c "ID  *string"` == 1.
    - P-1-Shrink (auf den Struct-Body gescopet, NICHT auf die ganze Datei — `UploadRequest` behaelt die Felder): `sed -n '/type UploadMediaAsset struct/,/^}/p' backend/internal/models/media_upload.go | grep -cE "EntityType|EntityID|AssetType"` == 0.
    - Gegenprobe: `sed -n '/type UploadRequest struct/,/^}/p' backend/internal/models/media_upload.go | grep -cE "EntityType|EntityID|AssetType"` == 3 (unveraendert).
    - `sed -n '/type UploadMediaAsset struct/,/^}/p' backend/internal/models/media_upload.go` enthaelt weiterhin `MediaType` und `FilePath`.
    - D-07: `grep -c "CreateReleaseMedia" backend/internal/repository/media_upload.go` liefert weiterhin >= 2 (Interface + Impl); `grep -q "INSERT INTO release_media" backend/internal/repository/media_upload.go` → Treffer; der `"release_media"`-joinTables-Eintrag ist weiterhin vorhanden.
    - `git diff --name-only backend/internal/handlers/media_upload_storage.go` ist LEER.
    - `cd backend && go build ./...` gruen (Test-Mock- und Struct-Literal-Anpassung folgen in Task 2).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && ! grep -rq "SupportsLegacyUploadSchema\|shouldUseAnimePosterPathFallback\|legacyUploadSchemaDetector" internal/ --include=*.go --exclude=*_test.go && grep -q "asset.ID = strconv.FormatInt" internal/repository/media_upload.go && [ "$(sed -n '/type UploadMediaAsset struct/,/^}/p' internal/models/media_upload.go | grep -cE 'EntityType|EntityID|AssetType')" = "0" ] && [ "$(sed -n '/type UploadRequest struct/,/^}/p' internal/models/media_upload.go | grep -cE 'EntityType|EntityID|AssetType')" = "3" ] && grep -q "INSERT INTO release_media" internal/repository/media_upload.go && echo CLUSTER-B-OK</automated>
  </verify>
  <done>V2-only Upload-Pfad; Legacy-Erkennung/Compat weg; DTO praezise abgespeckt (ID erhalten, drei tote Felder weg); Build gruen; release_media-Write-Path woertlich unveraendert.</done>
</task>

<task type="auto">
  <name>Task 2: Cluster-B-Test-Mock + UploadMediaAsset-Struct-Literal angleichen</name>
  <files>backend/internal/handlers/media_upload_test.go</files>
  <read_first>
    - backend/internal/handlers/media_upload_test.go:40-50 (Mock `SupportsLegacyUploadSchema`) UND :80-90 (Mock `CreateReleaseMedia` — nur ERSTERER faellt)
    - backend/internal/handlers/media_upload_test.go:198-220 (`TestMediaUploadHandler_Delete`, Struct-Literal `models.UploadMediaAsset{ID, EntityType, EntityID, AssetType, Format, MimeType, CreatedAt}` bei ~:206)
    - backend/internal/repository/media_upload.go (das nach Task 1 verkleinerte Interface)
    - backend/internal/models/media_upload.go (der nach Task 1 verkleinerte DTO)
    - 106-CONTEXT.md D-07
  </read_first>
  <action>Zwei gepaarte Anpassungen an `handlers/media_upload_test.go`: (1) INTERFACE-SHRINK: die Mock-Implementierung fuer `SupportsLegacyUploadSchema` (~:45) entfernen — sie paart mit dem Interface-Shrink aus Task 1. `MockMediaUploadRepository` muss danach das (kleinere) Interface weiterhin vollstaendig erfuellen. (2) DTO-SHRINK (P-1-Folgeaenderung, in der Vorfassung fehlend): im Struct-Literal `models.UploadMediaAsset{...}` in `TestMediaUploadHandler_Delete` (~:206-214) die drei entfallenden Feldzuweisungen `EntityType: "anime"`, `EntityID: 123` und `AssetType: "poster"` streichen. Die Zuweisung `ID: mediaID` BLEIBT (Feld existiert weiter, P-1) — ebenso `Format`, `MimeType`, `CreatedAt`. Der danebenstehende `storagePath := filepath.Join(tmpDir, "anime", "123", "poster", mediaID)` bleibt unveraendert (er baut den Pfad aus Literalen, nicht aus dem DTO). Pruefe zusaetzlich per grep, ob weitere `UploadMediaAsset{`-Literale in der Datei dieselben Felder setzen, und ziehe sie identisch mit. STRIKT BEHALTEN (D-07): die Mock-Methode `CreateReleaseMedia` (~:84) — sie bleibt Teil des Interfaces und ihr Entfernen wuerde den Compile brechen. Danach `go build ./... && go vet ./...` + `go test ./internal/handlers/... -run MediaUpload` gruen stellen.</action>
  <acceptance_criteria>
    - `grep -c "SupportsLegacyUploadSchema" backend/internal/handlers/media_upload_test.go` == 0.
    - `grep -c "CreateReleaseMedia" backend/internal/handlers/media_upload_test.go` >= 1 (D-07: Mock bleibt).
    - P-1: Kein `UploadMediaAsset`-Literal setzt noch die entfallenen Felder — `sed -n '/models.UploadMediaAsset{/,/^\t}/p' backend/internal/handlers/media_upload_test.go | grep -cE "EntityType:|EntityID:|AssetType:"` == 0.
    - P-1: `sed -n '/models.UploadMediaAsset{/,/^\t}/p' backend/internal/handlers/media_upload_test.go | grep -c "ID:"` >= 1 (ID-Zuweisung erhalten).
    - `cd backend && go build ./... && go vet ./...` gruen.
    - `cd backend && go test ./internal/handlers/... -run MediaUpload` gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./... && go test ./internal/handlers/... -run MediaUpload && ! grep -q "SupportsLegacyUploadSchema" internal/handlers/media_upload_test.go && grep -q "CreateReleaseMedia" internal/handlers/media_upload_test.go && [ "$(sed -n '/models.UploadMediaAsset{/,/^\t}/p' internal/handlers/media_upload_test.go | grep -cE 'EntityType:|EntityID:|AssetType:')" = "0" ] && echo CLUSTER-B-TAIL-OK</automated>
  </verify>
  <done>Cluster-B-Test-Mock entfernt, Struct-Literal auf den geschrumpften DTO angepasst (ID erhalten), release_media-Mock erhalten, Upload-Handler-Tests + Build/vet gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Keine neue. Der generische `/admin/upload`-Endpunkt bleibt unveraendert (kein Auth-Wechsel — Haertung erst Phase 108).

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-04-01 | Denial of Service | V2-Upload bricht durch zu breites Entfernen | mitigate | Nur Legacy-Zweige/Compat entfernen; `go build` + Upload-Handler-Test nach jedem Schritt; V2-DTO-Felder bleiben. |
| T-106-04-02 | Tampering | Stiller FE-Verlust neuer Release-Uploads durch Kappen des release_media-Write-Paths | mitigate | D-07: Write-Path in 106 gar nicht angefasst; positive grep-Acceptance-Criteria (`INSERT INTO release_media`, `CreateReleaseMedia`) sichern das Verbleiben ab; `media_upload_storage.go` nicht in files_modified. |
| T-106-04-03 | Denial of Service | `undefined method`-Compile-Break durch Mock/Interface-Drift | mitigate | Interface-Shrink (nur `SupportsLegacyUploadSchema`) und Mock-Anpassung in Task 2 gepaart; `go build` + `go test` als Gate. |
| T-106-04-04 | Denial of Service | P-1: Entfernen von `UploadMediaAsset.ID` kappt den Rueckgabeweg der BIGSERIAL-ID → `CreateMediaFile.MediaID` leer, Join-Table-Eintrag fehlerhaft, `UploadResponse.ID` leer (stiller Upload-Defekt ohne Compile-Fehler in den Repo-Schichten) | mitigate | Feld-fuer-Feld-Tabelle im Plan; positive Acceptance-Criteria auf `asset.ID = strconv.FormatInt` UND auf das Feld im Struct-Body; Struct-Body-gescopete Negativ-Assertion statt Datei-weitem grep. |
| T-106-04-SC | Tampering | Package-Installs | accept | Keine Installs. |
Kein neuer Angriffsvektor; `POST /admin/upload`-Auth unveraendert (Phase 108).
</threat_model>

<verification>
- `cd backend && go build ./... && go vet ./...` gruen.
- `cd backend && go test ./internal/handlers/... -run MediaUpload` gruen.
- grep leer fuer SupportsLegacyUploadSchema/useLegacyUploadSchema/legacyUploadSchemaDetector/shouldUseAnimePosterPathFallback.
- grep positiv fuer `asset.ID = strconv.FormatInt` und `ID string` im `UploadMediaAsset`-Body (P-1-Erhalt).
- grep positiv fuer `CreateReleaseMedia` + `INSERT INTO release_media` (D-07-Erhalt).
</verification>

<success_criteria>
SC2-Teil erfuellt: Cluster B (Dualpfad/Compat/Test-Mock) ersatzlos entfernt; DTO praezise um die drei toten UUID-Aera-Felder geschrumpft bei erhaltenem `ID`-Out-Parameter (P-1); V2-Upload funktional; `release_media`-Schreibpfad gemaess D-07 vollstaendig erhalten; Build + Tests gruen.
</success_criteria>

<output>
Create `.planning/phases/106-medienkern-schema-legacy-abbau/106-04-SUMMARY.md` when done. Dokumentiere explizit (a) dass `UploadMediaAsset.ID` gemaess P-1 erhalten blieb und warum, und (b) dass der release_media-Schreibpfad gemaess D-07 unangetastet blieb (Verweis auf Phase 108 / ROADMAP 108-SC 3b).
</output>
