---
phase: 106-medienkern-schema-legacy-abbau
plan: 04
type: execute
wave: 2
depends_on: [106-01]
files_modified:
  - backend/internal/repository/media_upload.go
  - backend/internal/handlers/media_upload_v2_compat.go
  - backend/internal/handlers/media_upload_image.go
  - backend/internal/models/media_upload.go
  - backend/internal/handlers/media_upload_test.go
autonomous: true
requirements: [SC2]
must_haves:
  truths:
    - "SC2/D-04 Cluster B: Legacy-Schema-Erkennung (`SupportsLegacyUploadSchema`, `useLegacyUploadSchema`) und alle `if useLegacy`-Dualbranches sind ersatzlos entfernt; der V2-Upload-Pfad bleibt funktional (Abloesung erst Phase 107)."
    - "SC2/D-04 Cluster B: `media_upload_v2_compat.go` (`legacyUploadSchemaDetector` + `shouldUseAnimePosterPathFallback`) ist geloescht und der Aufruf in `media_upload_image.go:91` aufgeloest."
    - "A3/OpenQ1: `models/media_upload.go` ist auf V2-Felder abgespeckt (Legacy-Felder `ID string`/`EntityType`/`EntityID`/`AssetType` weg); das DTO fuer den lebenden V2-Upload bleibt erhalten."
    - "D-07 (LOCKED): Der `release_media`-SCHREIBPFAD bleibt VOLLSTAENDIG INTAKT — `CreateReleaseMedia` (Interface `media_upload.go:24` + Impl `:290` inkl. `INSERT INTO release_media` `:292`), der Live-Aufrufer `media_upload_storage.go:54` (`case \"release\"`), der Mock `media_upload_test.go:84` und der `release_media`-Eintrag der `DeleteMediaAsset`-Join-Liste `:373` werden in Phase 106 NICHT angefasst. Entfernung erfolgt in Phase 108 (ROADMAP 108-SC 3b)."
    - "Caller-Enumeration verifiziert (Guard gegen Kollateralschaden): `media_upload_storage.go:54` ist der EINZIGE Live-Aufrufer von `CreateReleaseMedia`; weitere Referenzen sind nur Interface `:24`, Impl `:290` und Test-Mock `:84`. Alle bleiben."
    - "`go build ./... && go vet ./...` und die angepassten Upload-Handler-Tests sind gruen; kein `undefined method` durch den Interface-Shrink (Cluster B)."
  artifacts:
    - path: "backend/internal/repository/media_upload.go"
      provides: "V2-only Upload-Repository ohne Legacy-Dualpfad; release_media-Junction-Write unveraendert erhalten (D-07)"
      contains: "CreateMediaAsset"
  key_links:
    - from: "backend/internal/handlers/media_upload.go (V2-Upload)"
      to: "media_assets/media_files (lebend)"
      via: "V2-Pfad bleibt intakt"
      pattern: "CreateMediaAsset"
    - from: "backend/internal/handlers/media_upload_storage.go:54 (case \"release\")"
      to: "release_media-Junction (READ-Quelle von ListReleaseAssets)"
      via: "CreateReleaseMedia — unveraendert (D-07)"
      pattern: "case \"release\""
---

<objective>
Kollabiert den Legacy-Dual-Upload-Pfad (Cluster B) auf den V2-only-Pfad: Legacy-Schema-Erkennung, Compat-Shim und Legacy-DTO-Felder fallen ersatzlos. Der generische Upload bleibt in 106 funktional (V2 gegen `media_assets`) — die Abloesung durch den `MediaFileService` folgt in Phase 107.

REVISION (PO-Entscheid 2026-07-21, „Option A" / CONTEXT.md D-07): Die frueher hier gefuehrte Kopplung an die A/B/C-Entscheidung aus 106-05 ENTFAELLT. Der `release_media`-SCHREIBPFAD (`CreateReleaseMedia` Interface/Impl/INSERT, Live-Aufrufer `media_upload_storage.go:54 case "release"`, Mock, `DeleteMediaAsset`-Join) wird in Phase 106 gar nicht angefasst und wandert samt Lesepfad nach Phase 108. Dieser Plan ist damit wieder unbedingt, autonom und haengt nur noch an 106-01.

Purpose: SC2-Anteil — Compile-Level-Legacy im Upload-Repository entfernen, Upload lauffaehig halten (Pitfall 5-nah).
Output: bereinigtes `media_upload.go`, geloeschtes `media_upload_v2_compat.go`, abgespecktes DTO, angepasster Test-Mock.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-medienkern-schema-legacy-abbau/106-RESEARCH.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-PATTERNS.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-CONTEXT.md

<interfaces>
<!-- Verifizierte Call-Sites (RESEARCH B/D + grep + Caller-Enumeration). -->
CLUSTER B — zu entfernen:
- repository/media_upload.go: `SupportsLegacyUploadSchema`(:45/:47), `useLegacyUploadSchema`(:62),
  `if useLegacy`-Branches in CreateMediaAsset(:107/:111), CreateMediaFile(:139/:143),
  CreateAnimeMedia(:170/:180), `coerceMediaReference(mediaID, useLegacy)`(:243/:248).
- handlers/media_upload_v2_compat.go (22 Z.): `legacyUploadSchemaDetector`-Interface + `shouldUseAnimePosterPathFallback` (gibt immer false).
- handlers/media_upload_image.go:91: `usePathFallback, err := h.shouldUseAnimePosterPathFallback(ctx, req)` (Aufruf entfernen; Fallback immer false → Zweig aufloesen).
- models/media_upload.go (52 Z.): `UploadMediaAsset{ID string, EntityType, EntityID, AssetType,...}` — Legacy-Felder abspecken, V2-Felder MediaType/FilePath/Format/MimeType/UploadedBy behalten.
- handlers/media_upload_test.go:45: Mock `SupportsLegacyUploadSchema` (Test-Guard, muss mit dem Interface-Shrink fallen).

D-07 — release_media-WRITE-PATH: VOLLSTAENDIG AUSSERHALB DIESES PLANS (LOCKED, CONTEXT.md D-07):
- Interface-Deklaration repository/media_upload.go:24 `CreateReleaseMedia(ctx, releaseID int64, mediaID string, sortOrder int) error` — BLEIBT.
- Impl repository/media_upload.go:290 inkl. `INSERT INTO release_media (release_id, media_id, sort_order)` :292 — BLEIBT.
- EINZIGER Live-Aufrufer handlers/media_upload_storage.go:54 `case "release": return repo.CreateReleaseMedia(...)` in `createJoinTableEntryWithRepo` — BLEIBT (Datei steht deshalb NICHT in files_modified).
- Test-Mock handlers/media_upload_test.go:84 `func (m *MockMediaUploadRepository) CreateReleaseMedia(...)` — BLEIBT (nur der Cluster-B-Mock `SupportsLegacyUploadSchema` faellt).
- `DeleteMediaAsset`-joinTables-Eintrag repository/media_upload.go:373 `"release_media"` — BLEIBT.
BEGRUENDUNG: Der Endpoint `GET /releases/:id/assets` liest diese Junction und hat einen lebenden FE-Consumer; ein Kappen des Schreibpfads in 106 wuerde neue Release-Uploads still nicht mehr im Frontend erscheinen lassen (Verstoss gegen die Phasengrenze „kein Verhaltensumbau an Upload/Frontend").

PATTERNS.md:241 flaggt `media_upload_storage.go:54` als compile-kritischen Caller — relevant nur, wenn `CreateReleaseMedia` aus dem Interface faellt. Genau das passiert in 106 NICHT; der Interface-Shrink betrifft ausschliesslich `SupportsLegacyUploadSchema`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Legacy-Dualpfad im Upload-Repository entfernen (Cluster B)</name>
  <files>backend/internal/repository/media_upload.go, backend/internal/handlers/media_upload_v2_compat.go, backend/internal/handlers/media_upload_image.go, backend/internal/models/media_upload.go</files>
  <read_first>
    - backend/internal/repository/media_upload.go (komplett — Branch-Struktur der 3 Create-Methoden + coerceMediaReference + die D-07-Marken :24/:290/:292/:373)
    - backend/internal/handlers/media_upload_v2_compat.go (komplett, 22 Z.)
    - backend/internal/models/media_upload.go (komplett, 52 Z. — welche Felder V2 braucht)
    - 106-CONTEXT.md D-07 (release_media-Write-Path bleibt)
  </read_first>
  <action>In `repository/media_upload.go`: entferne `SupportsLegacyUploadSchema` (Interface-Deklaration + Impl) und `useLegacyUploadSchema`. In `CreateMediaAsset`, `CreateMediaFile`, `CreateAnimeMedia` die `useLegacy, err := r.useLegacyUploadSchema(ctx)` + `if !useLegacy { ... }`-Verzweigung auf den V2-Zweig kollabieren (nur den `!useLegacy`/V2-Pfad behalten). `coerceMediaReference(mediaID string, useLegacy bool)` auf `coerceMediaReference(mediaID string)` vereinfachen — immer `strconv.ParseInt`-Pfad (V2), Legacy-`any`-Zweig raus; alle Aufrufer anpassen. Loesche `handlers/media_upload_v2_compat.go` komplett. In `handlers/media_upload_image.go:91` den `shouldUseAnimePosterPathFallback`-Aufruf entfernen und den `usePathFallback`-Zweig auf den `false`-Fall aufloesen (Fallback gab immer false zurueck). In `models/media_upload.go` die Legacy-Felder `ID string`, `EntityType`, `EntityID`, `AssetType` aus `UploadMediaAsset` entfernen; V2-Felder (`MediaType`, `FilePath`, `Format`, `MimeType`, `UploadedBy`) behalten — das DTO bleibt fuer den lebenden V2-Upload bestehen (A3/OpenQ1: NICHT ganze Datei loeschen). STRIKT NICHT ANFASSEN (D-07): `CreateReleaseMedia` (Interface :24, Impl :290, INSERT :292) und der `"release_media"`-Eintrag der `DeleteMediaAsset`-joinTables-Slice (:373) — beide bleiben woertlich unveraendert; `handlers/media_upload_storage.go` wird in diesem Plan ueberhaupt nicht editiert. Nach jedem Schritt `go build`.</action>
  <acceptance_criteria>
    - `grep -rn "SupportsLegacyUploadSchema\|useLegacyUploadSchema\|legacyUploadSchemaDetector\|shouldUseAnimePosterPathFallback" backend/ --include=*.go --exclude=*_test.go` liefert KEINE Treffer.
    - `backend/internal/handlers/media_upload_v2_compat.go` existiert nicht mehr.
    - `models/media_upload.go` enthaelt `MediaType`/`FilePath`, aber NICHT mehr `EntityType`/`AssetType`.
    - D-07: `grep -c "CreateReleaseMedia" backend/internal/repository/media_upload.go` liefert weiterhin >= 2 (Interface + Impl); `grep -q "INSERT INTO release_media" backend/internal/repository/media_upload.go` → Treffer; der `"release_media"`-joinTables-Eintrag ist weiterhin vorhanden.
    - `git diff --name-only backend/internal/handlers/media_upload_storage.go` ist LEER.
    - `cd backend && go build ./...` gruen (Test-Mock-Anpassung folgt in Task 2).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && ! grep -rq "SupportsLegacyUploadSchema\|shouldUseAnimePosterPathFallback\|legacyUploadSchemaDetector" internal/ --include=*.go --exclude=*_test.go && grep -q "INSERT INTO release_media" internal/repository/media_upload.go && echo CLUSTER-B-OK</automated>
  </verify>
  <done>V2-only Upload-Pfad; Legacy-Erkennung/Compat weg; DTO abgespeckt; Build gruen; release_media-Write-Path woertlich unveraendert.</done>
</task>

<task type="auto">
  <name>Task 2: Cluster-B-Test-Mock angleichen + Upload-Tests gruen stellen</name>
  <files>backend/internal/handlers/media_upload_test.go</files>
  <read_first>
    - backend/internal/handlers/media_upload_test.go (Mock `SupportsLegacyUploadSchema` ~:45 UND Mock `CreateReleaseMedia` ~:84 — nur ERSTERER faellt)
    - backend/internal/repository/media_upload.go (das nach Task 1 verkleinerte Interface)
    - 106-CONTEXT.md D-07
  </read_first>
  <action>In `handlers/media_upload_test.go` die Mock-Implementierung fuer `SupportsLegacyUploadSchema` (~:45) entfernen — sie paart mit dem Interface-Shrink aus Task 1. Der Mock `MockMediaUploadRepository` muss danach das (kleinere) Interface weiterhin vollstaendig erfuellen. STRIKT BEHALTEN (D-07): die Mock-Methode `CreateReleaseMedia` (~:84) — sie bleibt Teil des Interfaces und ihr Entfernen wuerde den Compile brechen. Danach `go build ./... && go vet ./...` + `go test ./internal/handlers/... -run MediaUpload` gruen stellen.</action>
  <acceptance_criteria>
    - `grep -c "SupportsLegacyUploadSchema" backend/internal/handlers/media_upload_test.go` == 0.
    - `grep -c "CreateReleaseMedia" backend/internal/handlers/media_upload_test.go` >= 1 (D-07: Mock bleibt).
    - `cd backend && go build ./... && go vet ./...` gruen.
    - `cd backend && go test ./internal/handlers/... -run MediaUpload` gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./... && go test ./internal/handlers/... -run MediaUpload && ! grep -q "SupportsLegacyUploadSchema" internal/handlers/media_upload_test.go && grep -q "CreateReleaseMedia" internal/handlers/media_upload_test.go && echo CLUSTER-B-TAIL-OK</automated>
  </verify>
  <done>Cluster-B-Test-Mock entfernt, release_media-Mock erhalten, Upload-Handler-Tests + Build/vet gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Keine neue. Der generische `/admin/upload`-Endpunkt bleibt unveraendert (kein Auth-Wechsel — Haertung erst Phase 108).

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-04-01 | Denial of Service | V2-Upload bricht durch zu breites Entfernen | mitigate | Nur Legacy-Zweige/Compat entfernen; `go build` + Upload-Handler-Test nach jedem Schritt; V2-DTO-Felder bleiben. |
| T-106-04-02 | Tampering | Stiller FE-Verlust neuer Release-Uploads durch Kappen des release_media-Write-Paths | mitigate | D-07: Write-Path in 106 gar nicht angefasst; positive grep-Acceptance-Criteria (`INSERT INTO release_media`, `CreateReleaseMedia`) sichern das Verbleiben ab; `media_upload_storage.go` nicht in files_modified. |
| T-106-04-03 | Denial of Service | `undefined method`-Compile-Break durch Mock/Interface-Drift | mitigate | Interface-Shrink (nur `SupportsLegacyUploadSchema`) und Mock-Anpassung in Task 2 gepaart; `go build` + `go test` als Gate. |
| T-106-04-SC | Tampering | Package-Installs | accept | Keine Installs. |
Kein neuer Angriffsvektor; `POST /admin/upload`-Auth unveraendert (Phase 108).
</threat_model>

<verification>
- `cd backend && go build ./... && go vet ./...` gruen.
- `cd backend && go test ./internal/handlers/... -run MediaUpload` gruen.
- grep leer fuer SupportsLegacyUploadSchema/useLegacyUploadSchema/legacyUploadSchemaDetector/shouldUseAnimePosterPathFallback.
- grep positiv fuer `CreateReleaseMedia` + `INSERT INTO release_media` (D-07-Erhalt).
</verification>

<success_criteria>
SC2-Teil erfuellt: Cluster B (Dualpfad/Compat/DTO/Test-Mock) ersatzlos entfernt; V2-Upload funktional; `release_media`-Schreibpfad gemaess D-07 vollstaendig erhalten; Build + Tests gruen.
</success_criteria>

<output>
Create `.planning/phases/106-medienkern-schema-legacy-abbau/106-04-SUMMARY.md` when done. Dokumentiere explizit, dass der release_media-Schreibpfad gemaess D-07 unangetastet blieb (Verweis auf Phase 108 / ROADMAP 108-SC 3b).
</output>
