---
phase: 106-medienkern-schema-legacy-abbau
plan: 04
type: execute
wave: 2
depends_on: [106-01]
files_modified:
  - backend/internal/repository/media_upload.go
  - backend/internal/handlers/media_upload_v2_compat.go
  - backend/internal/handlers/media_upload_image.go
  - backend/internal/models/media_upload.go
  - backend/internal/handlers/media_upload_test.go
autonomous: true
requirements: [SC2]
must_haves:
  truths:
    - "SC2/D-04 Cluster B: Legacy-Schema-Erkennung (`SupportsLegacyUploadSchema`, `useLegacyUploadSchema`) und alle `if useLegacy`-Dualbranches sind entfernt; der V2-Upload-Pfad bleibt funktional (Ablosung erst Phase 107)."
    - "SC2/D-04 Cluster B: `media_upload_v2_compat.go` und der `shouldUseAnimePosterPathFallback`-Aufruf sind entfernt."
    - "A3/OpenQ1: `models/media_upload.go` wird auf V2-Felder abgespeckt (Legacy-Felder ID string/EntityType/EntityID/AssetType weg); das DTO fuer den lebenden V2-Upload bleibt erhalten."
    - "SC2/D-04 Cluster D-writes: `CreateReleaseMedia` (Interface + Impl + Tx) und der `release_media`-Eintrag in der `DeleteMediaAsset`-Join-Liste sind entfernt."
    - "`go build ./... && go vet ./...` und die angepassten Handler-Tests bleiben gruen."
  artifacts:
    - path: "backend/internal/repository/media_upload.go"
      provides: "V2-only Upload-Repository ohne Legacy-Dualpfad und ohne release_media-Junction-Write"
      contains: "CreateMediaAsset"
  key_links:
    - from: "backend/internal/handlers/media_upload.go (V2-Upload)"
      to: "media_assets/media_files (lebend)"
      via: "V2-Pfad bleibt intakt"
      pattern: "CreateMediaAsset"
---

<objective>
Kollabiert den Legacy-Dual-Upload-Pfad (Cluster B) auf den V2-only-Pfad und entfernt die schreibenden `release_media`-Junction-Seams (Cluster D-writes). Der generische Upload bleibt in 106 funktional (V2 gegen `media_assets`) — nur die tote Legacy-Schema-Erkennung und die `release_media`-Schreibpfade fallen.

Purpose: SC2-Anteil — Compile-Level-Legacy im Upload-Repository entfernen, Upload lauffaehig halten (Pitfall 5-nah).
Output: bereinigtes `media_upload.go`, geloeschtes `media_upload_v2_compat.go`, abgespecktes DTO, angepasste Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-medienkern-schema-legacy-abbau/106-RESEARCH.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-PATTERNS.md

<interfaces>
<!-- Verifizierte Call-Sites (RESEARCH B/D + grep). -->
- repository/media_upload.go: `SupportsLegacyUploadSchema`(:45/:47), `useLegacyUploadSchema`(:62),
  `if useLegacy`-Branches in CreateMediaAsset(:107/:111), CreateMediaFile(:139/:143),
  CreateAnimeMedia(:170/:180), `coerceMediaReference(mediaID, useLegacy)`(:243/:248),
  `CreateReleaseMedia`-Interface(:24) + Impl(:290) + DeleteMediaAsset-Join-Liste(:373 „release_media").
- handlers/media_upload_v2_compat.go (22 Z.): `legacyUploadSchemaDetector`-Interface + `shouldUseAnimePosterPathFallback` (gibt immer false).
- handlers/media_upload_image.go:91: `usePathFallback, err := h.shouldUseAnimePosterPathFallback(ctx, req)` (Aufruf entfernen; Fallback immer false → Zweig aufloesen).
- handlers/media_upload_test.go:45: Mock `SupportsLegacyUploadSchema`; Mock `CreateReleaseMedia` (test-guard).
- models/media_upload.go (52 Z.): `UploadMediaAsset{ID string, EntityType, EntityID, AssetType,...}` — Legacy-Felder abspecken, V2-Felder MediaType/FilePath/Format/MimeType/UploadedBy behalten.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Legacy-Dualpfad im Upload-Repository entfernen (Cluster B)</name>
  <files>backend/internal/repository/media_upload.go, backend/internal/handlers/media_upload_v2_compat.go, backend/internal/handlers/media_upload_image.go, backend/internal/models/media_upload.go</files>
  <read_first>
    - backend/internal/repository/media_upload.go (komplett — Branch-Struktur der 3 Create-Methoden + coerceMediaReference)
    - backend/internal/handlers/media_upload_v2_compat.go (komplett, 22 Z.)
    - backend/internal/models/media_upload.go (komplett, 52 Z. — welche Felder V2 braucht)
  </read_first>
  <action>In `repository/media_upload.go`: entferne `SupportsLegacyUploadSchema` und `useLegacyUploadSchema`. In `CreateMediaAsset`, `CreateMediaFile`, `CreateAnimeMedia` die `useLegacy, err := r.useLegacyUploadSchema(ctx)` + `if !useLegacy { ... }`-Verzweigung auf den V2-Zweig kollabieren (nur den `!useLegacy`/V2-Pfad behalten). `coerceMediaReference(mediaID string, useLegacy bool)` auf `coerceMediaReference(mediaID string)` vereinfachen — immer `strconv.ParseInt`-Pfad (V2), Legacy-`any`-Zweig raus; alle Aufrufer anpassen. Loesche `handlers/media_upload_v2_compat.go` komplett. In `handlers/media_upload_image.go:91` den `shouldUseAnimePosterPathFallback`-Aufruf entfernen und den `usePathFallback`-Zweig auf den `false`-Fall aufloesen (Fallback gab immer false zurueck). In `models/media_upload.go` die Legacy-Felder `ID string`, `EntityType`, `EntityID`, `AssetType` aus `UploadMediaAsset` entfernen; V2-Felder (`MediaType`, `FilePath`, `Format`, `MimeType`, `UploadedBy`) behalten — das DTO bleibt fuer den lebenden V2-Upload bestehen (A3/OpenQ1: NICHT ganze Datei loeschen). Nach jedem Schritt `go build`.</action>
  <acceptance_criteria>
    - `grep -rn "SupportsLegacyUploadSchema\|useLegacyUploadSchema\|legacyUploadSchemaDetector\|shouldUseAnimePosterPathFallback" backend/ --include=*.go` (ohne _test.go) liefert KEINE Treffer.
    - `backend/internal/handlers/media_upload_v2_compat.go` existiert nicht mehr.
    - `models/media_upload.go` enthaelt `MediaType`/`FilePath`, aber NICHT mehr `EntityType`/`AssetType`.
    - `cd backend && go build ./...` gruen (Test-Anpassung folgt in Task 2).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && ! grep -rq "SupportsLegacyUploadSchema\|shouldUseAnimePosterPathFallback\|legacyUploadSchemaDetector" internal/ --include=*.go --exclude=*_test.go && echo CLUSTER-B-OK</automated>
  </verify>
  <done>V2-only Upload-Pfad; Legacy-Erkennung/Compat weg; DTO abgespeckt; Build gruen.</done>
</task>

<task type="auto">
  <name>Task 2: release_media-Junction-Writes + Test-Mocks entfernen (Cluster D-writes)</name>
  <files>backend/internal/repository/media_upload.go, backend/internal/handlers/media_upload_test.go</files>
  <read_first>
    - backend/internal/repository/media_upload.go (Interface :24, CreateReleaseMedia :290, DeleteMediaAsset-Join-Liste :373)
    - backend/internal/handlers/media_upload_test.go (Mock-Methoden fuer SupportsLegacyUploadSchema + CreateReleaseMedia)
    - 106-PATTERNS.md „Cluster D — release_media junction" (Interface+Impl+Aufruf+Mock zusammen)
  </read_first>
  <action>In `repository/media_upload.go`: entferne die `CreateReleaseMedia`-Methode (Impl ~:290) UND ihre Interface-Deklaration (~:24) im `MediaUploadRepo`/`...Tx`-Interface. In der `DeleteMediaAsset`-`joinTables`-Slice (~:373) den Eintrag `"release_media"` entfernen (die anderen Join-Tabellen anime_media/episode_media/fansub_group_media bleiben — sie werden erst in Phase 108/110 abgeloest). In `handlers/media_upload_test.go` die Mock-Implementierungen fuer `SupportsLegacyUploadSchema` (~:45) und `CreateReleaseMedia` entfernen, damit der Mock weiter das (nun kleinere) Interface erfuellt; ggf. Testfaelle, die diese Methoden aufriefen, anpassen. `go build` + `go test ./internal/handlers/... -run MediaUpload` gruen.</action>
  <acceptance_criteria>
    - `grep -rn "CreateReleaseMedia" backend/ --include=*.go` liefert KEINE Treffer.
    - Die `DeleteMediaAsset`-Join-Liste enthaelt `release_media` nicht mehr (`grep -n "release_media" backend/internal/repository/media_upload.go` leer).
    - `cd backend && go build ./... && go vet ./...` gruen.
    - `cd backend && go test ./internal/handlers/... -run MediaUpload` gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./... && go test ./internal/handlers/... -run MediaUpload && ! grep -rq "CreateReleaseMedia" internal/ --include=*.go && ! grep -q "release_media" internal/repository/media_upload.go && echo D-WRITES-OK</automated>
  </verify>
  <done>release_media-Schreibpfade (Interface/Impl/Join/Mock) entfernt; Upload-Handler-Tests gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Keine neue. Der generische `/admin/upload`-Endpunkt bleibt unveraendert (kein Auth-Wechsel — Haertung erst Phase 108).

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-04-01 | Denial of Service | V2-Upload bricht durch zu breites Entfernen | mitigate | Nur Legacy-Zweige/Compat entfernen; `go build` + Upload-Handler-Test nach jedem Schritt; V2-DTO-Felder bleiben. |
| T-106-04-SC | Tampering | Package-Installs | accept | Keine Installs. |
Kein neuer Angriffsvektor; `POST /admin/upload`-Auth unveraendert (Phase 108).
</threat_model>

<verification>
- `cd backend && go build ./... && go vet ./...` gruen.
- `cd backend && go test ./internal/handlers/... -run MediaUpload` gruen.
- grep leer fuer SupportsLegacyUploadSchema/shouldUseAnimePosterPathFallback/CreateReleaseMedia.
</verification>

<success_criteria>
SC2-Teil erfuellt: Cluster B (Dualpfad/Compat/DTO) und Cluster D-writes (CreateReleaseMedia/Join/Mock) entfernt; V2-Upload funktional; Build + Tests gruen.
</success_criteria>

<output>
Create `.planning/phases/106-medienkern-schema-legacy-abbau/106-04-SUMMARY.md` when done.
</output>
