---
phase: 106-medienkern-schema-legacy-abbau
plan: 04
type: execute
wave: 3
depends_on: [106-01, 106-05]
files_modified:
  - backend/internal/repository/media_upload.go
  - backend/internal/handlers/media_upload_v2_compat.go
  - backend/internal/handlers/media_upload_image.go
  - backend/internal/models/media_upload.go
  - backend/internal/handlers/media_upload_test.go
  - backend/internal/handlers/media_upload_storage.go
autonomous: true
requirements: [SC2]
must_haves:
  truths:
    - "SC2/D-04 Cluster B (autonom, entscheidungs-UNABHAENGIG): Legacy-Schema-Erkennung (`SupportsLegacyUploadSchema`, `useLegacyUploadSchema`) und alle `if useLegacy`-Dualbranches sind entfernt; der V2-Upload-Pfad bleibt funktional (Ablosung erst Phase 107)."
    - "SC2/D-04 Cluster B: `media_upload_v2_compat.go` und der `shouldUseAnimePosterPathFallback`-Aufruf sind entfernt."
    - "A3/OpenQ1: `models/media_upload.go` wird auf V2-Felder abgespeckt (Legacy-Felder ID string/EntityType/EntityID/AssetType weg); das DTO fuer den lebenden V2-Upload bleibt erhalten."
    - "SC2/D-04 Cluster D-writes (ENTSCHEIDUNGS-GEKOPPELT an 106-05 A/B/C, `depends_on: [106-05]`): Der gesamte `release_media`-SCHREIBPFAD — `CreateReleaseMedia` (Interface `media_upload.go:24` + Impl `:290` mit `INSERT INTO release_media` `:292`), der EINZIGE Live-Aufrufer `media_upload_storage.go:54` (`case \"release\"` in `createJoinTableEntryWithRepo`), der Mock `media_upload_test.go:84` und der `release_media`-Eintrag der `DeleteMediaAsset`-Join-Liste `:373` — wird gemaess `106-05-SUMMARY` behandelt: bei Option A KOMPLETT BEHALTEN (Endpoint bleibt read+write funktional; neue Release-Uploads erscheinen weiter in der Episoden-Media-Assets-Sektion — kein stiller FE-Verlust), bei Option B/C ERSATZLOS ENTFERNT (inkl. `case \"release\"`-Zweig)."
    - "Caller-Enumeration verifiziert: `media_upload_storage.go:54` ist der EINZIGE Live-Aufrufer von `CreateReleaseMedia`; weitere Referenzen sind nur Interface `:24`, Impl `:290` und Test-Mock `:84`. `media_upload.go:292 INSERT INTO release_media` ist das im Impl entfernte Write-SQL (identisch)."
    - "`go build ./... && go vet ./...` und die angepassten Handler-Tests bleiben gruen — unabhaengig von der gewaehlten 106-05-Option (kein `undefined method`-Compile-Break durch verwaisten Caller)."
  artifacts:
    - path: "backend/internal/repository/media_upload.go"
      provides: "V2-only Upload-Repository ohne Legacy-Dualpfad; release_media-Junction-Write je 106-05-Option behalten (A) oder entfernt (B/C)"
      contains: "CreateMediaAsset"
  key_links:
    - from: "backend/internal/handlers/media_upload.go (V2-Upload)"
      to: "media_assets/media_files (lebend)"
      via: "V2-Pfad bleibt intakt"
      pattern: "CreateMediaAsset"
    - from: "backend/internal/handlers/media_upload_storage.go:54 (case \"release\")"
      to: "release_media-Junction (READ-Quelle von ListReleaseAssets)"
      via: "CreateReleaseMedia — Behandlung entscheidungs-gekoppelt an 106-05"
      pattern: "case \"release\""
---

<objective>
Kollabiert den Legacy-Dual-Upload-Pfad (Cluster B) auf den V2-only-Pfad und behandelt den schreibenden `release_media`-Junction-Seam (Cluster D-writes) SYMMETRISCH zur Lese-Seite: der Schreibpfad (`CreateReleaseMedia` Interface/Impl + der EINZIGE Live-Aufrufer `media_upload_storage.go:54 case "release"` + Mock + `DeleteMediaAsset`-Join) wird gemaess der 106-05 A/B/C-Entscheidung entweder KOMPLETT BEHALTEN (Option A) oder ERSATZLOS ENTFERNT (Option B/C). Der generische Upload bleibt in 106 funktional (V2 gegen `media_assets`) — die tote Legacy-Schema-Erkennung faellt unbedingt.

Purpose: SC2-Anteil — Compile-Level-Legacy im Upload-Repository entfernen, Upload lauffaehig halten (Pitfall 5-nah), und den write-seitigen `release_media`-Seam nicht unbedingt kappen (Write-Symmetrie zum Read-seitigen 106-05-01-Checkpoint) — sonst wuerden neue Release-Uploads unter Option A still nicht mehr im Frontend erscheinen und/oder `go build` bricht am verwaisten Caller.
Output: bereinigtes `media_upload.go`, geloeschtes `media_upload_v2_compat.go`, abgespecktes DTO, `release_media`-Write-Path je Option, angepasste Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-medienkern-schema-legacy-abbau/106-RESEARCH.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-PATTERNS.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-05-SUMMARY.md

<interfaces>
<!-- Verifizierte Call-Sites (RESEARCH B/D + grep + Caller-Enumeration). -->
- repository/media_upload.go: `SupportsLegacyUploadSchema`(:45/:47), `useLegacyUploadSchema`(:62),
  `if useLegacy`-Branches in CreateMediaAsset(:107/:111), CreateMediaFile(:139/:143),
  CreateAnimeMedia(:170/:180), `coerceMediaReference(mediaID, useLegacy)`(:243/:248).
- handlers/media_upload_v2_compat.go (22 Z.): `legacyUploadSchemaDetector`-Interface + `shouldUseAnimePosterPathFallback` (gibt immer false).
- handlers/media_upload_image.go:91: `usePathFallback, err := h.shouldUseAnimePosterPathFallback(ctx, req)` (Aufruf entfernen; Fallback immer false → Zweig aufloesen).
- models/media_upload.go (52 Z.): `UploadMediaAsset{ID string, EntityType, EntityID, AssetType,...}` — Legacy-Felder abspecken, V2-Felder MediaType/FilePath/Format/MimeType/UploadedBy behalten.

CLUSTER D-WRITES — VOLLSTAENDIGE CALLER-ENUMERATION (entscheidungs-gekoppelt an 106-05):
- Interface-Deklaration: repository/media_upload.go:24 `CreateReleaseMedia(ctx, releaseID int64, mediaID string, sortOrder int) error` (im `MediaUploadRepo`/`...Tx`-Interface).
- Impl: repository/media_upload.go:290 `func (r *MediaUploadRepository) CreateReleaseMedia(...)`; enthaelt bei :292 `INSERT INTO release_media (release_id, media_id, sort_order)` — DAS ist das Write-SQL (identisch mit dem im Impl entfernten Statement).
- EINZIGER Live-Aufrufer: handlers/media_upload_storage.go:54 `case "release": return repo.CreateReleaseMedia(ctx, entityID, mediaID, 0)` in `createJoinTableEntryWithRepo` (die anderen `case`-Zweige anime/episode/fansub bleiben unberuehrt — deren Junctions loesen erst Phase 108/110 ab).
- Mock/Test-Guard: handlers/media_upload_test.go:84 `func (m *MockMediaUploadRepository) CreateReleaseMedia(...)`.
- DeleteMediaAsset-Join-Liste: repository/media_upload.go:373 `"release_media"`-Eintrag (Cleanup-Join beim Media-Delete).
- KEIN weiterer Live-Caller (repo-weiter grep bestaetigt: nur :24/:290/:54/:84 + der Delete-Join).
- handlers/media_upload_test.go:45: Mock `SupportsLegacyUploadSchema` (Cluster-B-Test-Guard, UNBEDINGT entfernen).

PATTERNS.md:241 flaggt `media_upload_storage.go:54` explizit als compile-kritischen Cluster-D-Caller — er MUSS zusammen mit Interface+Impl behandelt werden, sonst `undefined method` nach Interface-Shrink.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Legacy-Dualpfad im Upload-Repository entfernen (Cluster B, autonom)</name>
  <files>backend/internal/repository/media_upload.go, backend/internal/handlers/media_upload_v2_compat.go, backend/internal/handlers/media_upload_image.go, backend/internal/models/media_upload.go</files>
  <read_first>
    - backend/internal/repository/media_upload.go (komplett — Branch-Struktur der 3 Create-Methoden + coerceMediaReference)
    - backend/internal/handlers/media_upload_v2_compat.go (komplett, 22 Z.)
    - backend/internal/models/media_upload.go (komplett, 52 Z. — welche Felder V2 braucht)
  </read_first>
  <action>Cluster B ist entscheidungs-UNABHAENGIG — immer ausfuehren. In `repository/media_upload.go`: entferne `SupportsLegacyUploadSchema` und `useLegacyUploadSchema`. In `CreateMediaAsset`, `CreateMediaFile`, `CreateAnimeMedia` die `useLegacy, err := r.useLegacyUploadSchema(ctx)` + `if !useLegacy { ... }`-Verzweigung auf den V2-Zweig kollabieren (nur den `!useLegacy`/V2-Pfad behalten). `coerceMediaReference(mediaID string, useLegacy bool)` auf `coerceMediaReference(mediaID string)` vereinfachen — immer `strconv.ParseInt`-Pfad (V2), Legacy-`any`-Zweig raus; alle Aufrufer anpassen. Loesche `handlers/media_upload_v2_compat.go` komplett. In `handlers/media_upload_image.go:91` den `shouldUseAnimePosterPathFallback`-Aufruf entfernen und den `usePathFallback`-Zweig auf den `false`-Fall aufloesen (Fallback gab immer false zurueck). In `models/media_upload.go` die Legacy-Felder `ID string`, `EntityType`, `EntityID`, `AssetType` aus `UploadMediaAsset` entfernen; V2-Felder (`MediaType`, `FilePath`, `Format`, `MimeType`, `UploadedBy`) behalten — das DTO bleibt fuer den lebenden V2-Upload bestehen (A3/OpenQ1: NICHT ganze Datei loeschen). NICHT anfassen in dieser Task: `CreateReleaseMedia` / `release_media`-Schreibpfad (das ist Task 2, entscheidungs-gekoppelt). Nach jedem Schritt `go build`.</action>
  <acceptance_criteria>
    - `grep -rn "SupportsLegacyUploadSchema\|useLegacyUploadSchema\|legacyUploadSchemaDetector\|shouldUseAnimePosterPathFallback" backend/ --include=*.go` (ohne _test.go) liefert KEINE Treffer.
    - `backend/internal/handlers/media_upload_v2_compat.go` existiert nicht mehr.
    - `models/media_upload.go` enthaelt `MediaType`/`FilePath`, aber NICHT mehr `EntityType`/`AssetType`.
    - `cd backend && go build ./...` gruen (Test-Anpassung + release_media-Write-Path folgen in Task 2).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && ! grep -rq "SupportsLegacyUploadSchema\|shouldUseAnimePosterPathFallback\|legacyUploadSchemaDetector" internal/ --include=*.go --exclude=*_test.go && echo CLUSTER-B-OK</automated>
  </verify>
  <done>V2-only Upload-Pfad; Legacy-Erkennung/Compat weg; DTO abgespeckt; Build gruen. release_media-Write-Path unberuehrt (Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: release_media-Write-Path je 106-05-Option + Cluster-B-Test-Mock (Cluster D-writes, entscheidungs-gekoppelt)</name>
  <files>backend/internal/repository/media_upload.go, backend/internal/handlers/media_upload_storage.go, backend/internal/handlers/media_upload_test.go</files>
  <read_first>
    - .planning/phases/106-medienkern-schema-legacy-abbau/106-05-SUMMARY.md (gewaehlte Endpoint-Option A/B/C — parametrisiert diese Task)
    - backend/internal/repository/media_upload.go (Interface :24, CreateReleaseMedia :290 mit INSERT :292, DeleteMediaAsset-Join-Liste :373)
    - backend/internal/handlers/media_upload_storage.go (createJoinTableEntryWithRepo, `case "release"` :54)
    - backend/internal/handlers/media_upload_test.go (Mock SupportsLegacyUploadSchema :45 + Mock CreateReleaseMedia :84)
    - 106-PATTERNS.md „Cluster D — release_media junction" (:241, media_upload_storage.go:54 als compile-kritisch)
  </read_first>
  <action>
    A) IMMER (Cluster-B-Tail, entscheidungs-UNABHAENGIG): In `handlers/media_upload_test.go` die Mock-Implementierung fuer `SupportsLegacyUploadSchema` (~:45) entfernen (paart mit Task-1-Interface-Shrink). Der Mock muss danach das (kleinere) Cluster-B-Interface weiter erfuellen.

    B) release_media-WRITE-PATH gemaess `106-05-SUMMARY`-Option:
    - **Option A gewaehlt (release_media retained):** ALLES BEHALTEN — `CreateReleaseMedia` (Interface :24 + Impl :290 inkl. INSERT :292), der Live-Aufrufer `media_upload_storage.go:54 case "release"`, der Mock `media_upload_test.go:84` und der `release_media`-Eintrag der `DeleteMediaAsset`-Join-Liste :373 bleiben UNVERAENDERT. Der Endpoint bleibt read+write funktional; neue Release-Uploads populieren weiter die `release_media`-Junction, die `ListReleaseAssets` liest (kein stiller FE-Verlust). Im SUMMARY dokumentieren, dass der Write-Path als bewusst-behalten allowlisted ist (Rueckmeldung an 106-02/08 grep-Gate: `CreateReleaseMedia` + `case "release"` + `INSERT INTO release_media` sind Option-A-Allowlist, spiegelbildlich zum Read-Path `ListReleaseAssets`).
    - **Option B/C gewaehlt (release_media removed/rewritten):** In `repository/media_upload.go` die `CreateReleaseMedia`-Methode (Impl ~:290 inkl. INSERT) UND ihre Interface-Deklaration (~:24) entfernen; in der `DeleteMediaAsset`-`joinTables`-Slice (~:373) den `"release_media"`-Eintrag entfernen (anime_media/episode_media/fansub_group_media bleiben). In `handlers/media_upload_storage.go` den `case "release": return repo.CreateReleaseMedia(...)`-Zweig (:54) entfernen (der `default: return nil` deckt „release" dann als No-op ab — kein 5. Case noetig). In `handlers/media_upload_test.go` den Mock `CreateReleaseMedia` (~:84) entfernen. `go build` + `go test`.

    Danach: `go build ./... && go vet ./...` + `go test ./internal/handlers/... -run MediaUpload` gruen — UNABHAENGIG von der Option (kein verwaister Caller → kein `undefined method`). Die gewaehlte Option und die (Option-A) Allowlist-Rueckmeldung im SUMMARY festhalten.
  </action>
  <acceptance_criteria>
    - `handlers/media_upload_test.go`: Mock `SupportsLegacyUploadSchema` entfernt (immer).
    - Option A: `CreateReleaseMedia` (Interface+Impl), `media_upload_storage.go:54 case "release"`, Mock :84 und der DeleteMediaAsset-`release_media`-Join sind BEWUSST BEHALTEN und im SUMMARY als allowlisted markiert; `grep -n "case \"release\"" backend/internal/handlers/media_upload_storage.go` liefert weiter Treffer.
    - Option B/C: `grep -rn "CreateReleaseMedia" backend/ --include=*.go` liefert KEINE Treffer; `grep -n "case \"release\"" backend/internal/handlers/media_upload_storage.go` leer; `grep -n "release_media" backend/internal/repository/media_upload.go` leer.
    - `cd backend && go build ./... && go vet ./...` gruen (beide Optionen).
    - `cd backend && go test ./internal/handlers/... -run MediaUpload` gruen (beide Optionen).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./... && go test ./internal/handlers/... -run MediaUpload && echo D-WRITES-OK</automated>
  </verify>
  <done>Cluster-B-Test-Mock entfernt; release_media-Write-Path (Interface/Impl/Caller :54/Mock/DeleteMediaAsset-Join) gemaess 106-05-Option behalten (A) oder ersatzlos entfernt (B/C); Upload-Handler-Tests + Build gruen; kein verwaister Caller.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Keine neue. Der generische `/admin/upload`-Endpunkt bleibt unveraendert (kein Auth-Wechsel — Haertung erst Phase 108).

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-04-01 | Denial of Service | V2-Upload bricht durch zu breites Entfernen | mitigate | Nur Legacy-Zweige/Compat entfernen; `go build` + Upload-Handler-Test nach jedem Schritt; V2-DTO-Felder bleiben. |
| T-106-04-02 | Tampering | Stiller FE-Verlust neuer Release-Uploads durch unbedingtes Kappen des Write-Paths unter Option A | mitigate | Write-Path (CreateReleaseMedia + case "release") an 106-05-Entscheidung gekoppelt (`depends_on: [106-05]`); unter Option A komplett behalten (Write-Symmetrie zum Read-Checkpoint). |
| T-106-04-03 | Denial of Service | `undefined method`-Compile-Break durch verwaisten Caller media_upload_storage.go:54 nach Interface-Shrink | mitigate | Caller in files_modified + Task-2-Action; Interface/Impl/Caller/Mock/Join immer gemeinsam behandelt (PATTERNS.md:241). |
| T-106-04-SC | Tampering | Package-Installs | accept | Keine Installs. |
Kein neuer Angriffsvektor; `POST /admin/upload`-Auth unveraendert (Phase 108).
</threat_model>

<verification>
- `cd backend && go build ./... && go vet ./...` gruen (beide 106-05-Optionen).
- `cd backend && go test ./internal/handlers/... -run MediaUpload` gruen.
- grep leer fuer SupportsLegacyUploadSchema/shouldUseAnimePosterPathFallback; `CreateReleaseMedia`/`case "release"` je Option behalten (A) oder leer (B/C).
</verification>

<success_criteria>
SC2-Teil erfuellt: Cluster B (Dualpfad/Compat/DTO) unbedingt entfernt; Cluster D-writes (CreateReleaseMedia/Caller :54/Join/Mock) gemaess 106-05-Option behalten (A) oder entfernt (B/C); V2-Upload funktional; kein verwaister Caller; Build + Tests gruen.
</success_criteria>

<output>
Create `.planning/phases/106-medienkern-schema-legacy-abbau/106-04-SUMMARY.md` when done. Dokumentiere die aus 106-05-SUMMARY uebernommene Option und — bei Option A — die Write-Path-Allowlist-Rueckmeldung an 106-02/08.
</output>
</content>
</invoke>
