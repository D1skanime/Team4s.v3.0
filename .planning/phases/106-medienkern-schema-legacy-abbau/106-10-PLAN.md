---
phase: 106-medienkern-schema-legacy-abbau
plan: 10
type: execute
wave: 4
depends_on: [106-05, 106-09]
files_modified:
  - backend/internal/repository/anime.go
  - backend/internal/repository/admin_content_sync.go
  - backend/internal/repository/admin_content_anime_create_v2.go
  - backend/internal/repository/admin_content_anime_metadata.go
  - backend/internal/repository/admin_content_anime_delete.go
  - backend/internal/repository/anime_test.go
  - backend/internal/repository/runtime_authority_test.go
autonomous: true
requirements: [SC2]
must_haves:
  truths:
    - "PO-Entscheid 2026-07-22 (LOCKED): `anime.cover_image` wird in Phase 106 entfernt. Weil 106-05 `HasCoverImage` aus der dynamischen Schema-Detektion streicht, sind die bisher ueber `HasSlug`/`useV2Schema` erreichbaren Legacy-Zweige nicht mehr nur tot, sondern DAUERHAFT DEFEKT (sie wuerden nach dem 0131-DROP in `column cover_image does not exist` laufen). Sie werden deshalb ersatzlos entfernt statt konserviert."
    - "Entfernt sind die permanent toten Legacy-Zweige: `anime.go` (List-Fallback :50, GetByID-Fallback :100, `buildAnimeListWhere`-HasCover-Zweige :364/:366), `admin_content_sync.go:27` (getAnimeSyncSource-Fallback), `admin_content_anime_create_v2.go:36/:41` (`createAnimeLegacy`), `admin_content_anime_metadata.go:395/:410` (Legacy-Update-Pfad), `admin_content_anime_delete.go:96` (`loadAnimeDeleteResult`-Fallback) und `:353` (`resolveOrphanedLocalCoverImage`-Fallback)."
    - "Verhaltenserhaltung: Der jeweils lebende V2-Pfad (`listV2`, `getByIDV2`, `getAnimeSyncSourceV2`, `createAnimeV2`, `loadAnimeDeleteResultV2`, `resolveOrphanedLocalCoverImageV2`, `buildAnimeListWhereV2`) bleibt unveraendert und wird nach dem Umbau unbedingt statt bedingt aufgerufen — kein Funktionsverlust, keine API-Aenderung."
    - "DTO-Ebene unberuehrt: die JSON-Felder `cover_image` in `models/anime.go`, `models/admin_content.go`, `models/watchlist.go`, `models/fansub.go` sowie `applyString(\"cover_image\", ...)` und die Audit-Payload-Schluessel bleiben bestehen."
    - "D-07 (LOCKED, nicht regressieren): Die `release_media`-Assertionen in `runtime_authority_test.go:156-167` bleiben unveraendert; nur die auf die entfernten `anime.go`-Legacy-Queries gerichteten Fragment-Assertionen werden gezogen."
    - "`go build ./... && go vet ./...` + Repository-/Handler-Tests bleiben gruen; keine ungenutzten Funktionen, Imports oder Variablen zurueckgelassen."
    - "SC4-Endgate-Scope (Blocker 1+2, 2026-07-22): Der repo-weite Signatur-grep in Task 2 laeuft NUR ueber `backend/internal` + `backend/cmd` mit `--include=*.go` (frontend traegt `anime.cover_image` als legitimen TS-DTO-Zugriff) und behaelt die VOLLE Signaturliste inkl. `,\s*cover_image`. Letzteres ist gueltig, weil 106-09 Task 2 den einzigen bewusst behaltenen komma-praefixierten Bezeichner — den Sub-Query-Ausgabenamen in `anime_relations.go` — in `cover_url` umbenannt hat. Identischer Scope und identische Signaturen wie 106-08 Task 1, 106-09 Task 3 und die VALIDATION.md-Sampling-Regel."
  artifacts:
    - path: "backend/internal/repository/anime.go"
      provides: "List/GetByID ohne Legacy-Fallback und ohne cover_image-Rohspalte"
      contains: "listV2"
    - path: "backend/internal/repository/admin_content_anime_delete.go"
      provides: "Delete-Pfad ohne useV2Schema-Dualbranch"
      contains: "loadAnimeDeleteResultV2"
  key_links:
    - from: "backend/internal/repository/anime.go (List, GetByID)"
      to: "listV2 / getByIDV2"
      via: "unbedingter Aufruf statt HasSlug-Gate"
      pattern: "listV2|getByIDV2"
---

<objective>
Entfernt die durch den `anime.cover_image`-Drop dauerhaft defekt gewordenen Legacy-Zweige hinter `HasSlug`/`useV2Schema`. Diese Zweige sind heute schon unerreichbar (die Live-Datenbank hat `slug`), aber solange sie im Code stehen, referenzieren sie eine Spalte, die es nach Migration 0131 nicht mehr gibt — Karteileichen mit garantiertem Laufzeitfehler.

Warum entfernen statt umschreiben: Der Zweig existiert nur, um ein Vor-V2-Schema zu bedienen, das im Projekt nirgends mehr existiert. Ein Umschreiben auf die abgeleitete Cover-Quelle waere Pflege von unerreichbarem Code. Der Ersatz (V2-Pfad) ist bereits der einzige tatsaechlich ausgefuehrte Pfad.

Sequenzierung: laeuft in Welle 4, also nach 106-05 (entfernt `HasCoverImage`) und 106-09 (stellt die lebenden Leser um) und VOR dem 106-08-Gate, in dem Migration 0131 erstmals angewendet wird.

Purpose: SC2-Anteil — Legacy ersatzlos entfernt, keine dauerhaft defekten Codepfade im Repository.
Output: 5 bereinigte Repository-Dateien + 2 nachgezogene Testdateien.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-medienkern-schema-legacy-abbau/106-RESEARCH.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-CONTEXT.md

<interfaces>
<!-- Verifizierte Gate-Struktur. Muster ueberall identisch: `if <gate> { return <V2-Pfad> }` gefolgt vom Legacy-Rumpf. -->

`anime.go`
- `:28 List` → `if schema.HasSlug { return r.listV2(ctx, filter, schema) }`, danach Legacy-Rumpf mit `buildAnimeListWhere` + Query `:50`.
- `:89 GetByID` → `if schema.HasSlug { return r.getByIDV2(...) }`, danach Legacy-Query `:100`.
- `:~330 buildAnimeListWhere` — NUR vom Legacy-List-Rumpf genutzt; enthaelt die HasCover-Zweige `:364/:366`. Das V2-Pendant heisst `buildAnimeListWhereV2` (nutzt `animeCoverAvailableSQL`) und BLEIBT.

`admin_content_sync.go:22` → `if schema.HasSlug { return r.getAnimeSyncSourceV2(...) }`, danach Legacy-Query `:27`.

`admin_content_anime_metadata.go`
- `:250` `useV2Schema, err := hasV2AnimeCreateSchema(ctx, tx)`; `:256 if useV2Schema { createAnimeV2 } else { createAnimeLegacy + syncAnimeSourceLinks + syncAuthoritativeAnimeMetadata + ... }`.
- `:395/:410` — Legacy-Update-Pfad (`cover_image = $N` Assignment + RETURNING-Liste).

`admin_content_anime_create_v2.go:29 createAnimeLegacy` — trotz Dateinamens der LEGACY-Insert (`INSERT INTO anime (... cover_image ...)`); wird ausschliesslich aus dem `else`-Zweig in `admin_content_anime_metadata.go:260` aufgerufen.

`admin_content_anime_delete.go`
- `:28` `useV2Schema, err := hasV2AnimeDeleteAssetSchema(ctx, tx)`; Weitergabe als Parameter an `:83 loadAnimeDeleteResult`, `:175 deleteAnimeAssociations`, `:336 resolveOrphanedLocalCoverImage`, `:70`-Zweig.
- `:96` Legacy-Query `SELECT id, title, cover_image FROM anime WHERE id = $1 FOR UPDATE`.
- `:353` Legacy-Query `SELECT COUNT(*) FROM anime WHERE cover_image = $1`.
- NICHT ANFASSEN (D-07): `:~247` und `:~280` `NOT EXISTS (SELECT 1 FROM release_media rm ...)`-Referenzguards vor `DELETE FROM media_assets` — die Junction lebt weiter und wird weiter beschrieben; ein Entfernen der Guards machte noch referenzierte media_assets loeschbar (Datenverlust).

Betroffene Test-Assertionen:
- `runtime_authority_test.go:11-30` `TestAnimeRepository_ReadPathUsesFlatColumnsWithNormalizedOverlay` — Fragmente `"max_episodes, genre, description, cover_image,"` und `"select id, %s as display_title, type, status, year, cover_image, max_episodes"` beschreiben GENAU die zu entfernenden Legacy-Queries.
- `runtime_authority_test.go:156-167` — `release_media`-Assertionen, BLEIBEN unveraendert (D-07).
- `anime_test.go:28-65` `TestBuildAnimeListWhere_QueryPositions` + `TestBuildAnimeListWhere_HasCoverFalse` — testen ausschliesslich die entfallende `buildAnimeListWhere`.
- `admin_content_anime_create_v2_test.go:45/:80/:115/:147` nutzen `animeV2SchemaInfo{HasSlug: true}` — betreffen den V2-Pfad und bleiben gruen; nur pruefen, nicht praeventiv aendern.

WICHTIG: `HasSlug` als FELD in `animeSchemaQuerier`/`animeV2SchemaInfo` bleibt bestehen — es gated weiterhin lebende Ausdruecke in `admin_content_anisearch.go:32/:76/:129`, `admin_content_jellyfin_intake.go:31`, `admin_content_anime_update_v2.go:125`, `admin_content_anime_metadata.go:318`. Nur die HIER gelisteten Dualbranches fallen. Ein Entfernen des Feldes ist NICHT Scope dieser Phase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Legacy-Lesepfade in anime.go und admin_content_sync.go entfernen</name>
  <files>backend/internal/repository/anime.go, backend/internal/repository/admin_content_sync.go, backend/internal/repository/runtime_authority_test.go, backend/internal/repository/anime_test.go</files>
  <read_first>
    - backend/internal/repository/anime.go:28-160 (List + GetByID inkl. beider Legacy-Rumpfe und deren Scan-Bloecke)
    - backend/internal/repository/anime.go:320-400 (buildAnimeListWhere komplett — HasCover-Zweige :364/:366)
    - backend/internal/repository/admin_content_sync.go:15-60 (Gate + Legacy-Query + Scan)
    - backend/internal/repository/runtime_authority_test.go:1-40 (Fragment-Assertionen auf die Legacy-Queries) und :150-175 (release_media-Assertionen, die bleiben)
    - backend/internal/repository/anime_test.go:25-65 (buildAnimeListWhere-Tests)
  </read_first>
  <action>In `anime.go`: In `List` den `if schema.HasSlug { return r.listV2(...) }`-Gate durch einen unbedingten `return r.listV2(ctx, filter, schema)` ersetzen und den gesamten nachfolgenden Legacy-Rumpf (Count-Query, `buildAnimeListWhere`-Aufruf, Legacy-Listen-Query `:50`, Scan-Schleife) loeschen. Analog in `GetByID`: unbedingter `return r.getByIDV2(ctx, id, includeDisabled, schema)`, Legacy-Query `:100` und deren Scan-Block loeschen. Danach `buildAnimeListWhere` loeschen — VORHER per `grep -rn "buildAnimeListWhere\b" backend/ --include=*.go` verifizieren, dass es ausser den zu loeschenden Tests keinen weiteren Aufrufer gibt (das V2-Pendant heisst `buildAnimeListWhereV2` und BLEIBT); falls doch ein Aufrufer existiert, die Funktion behalten und stattdessen nur die beiden HasCover-Zweige `:364/:366` auf `animeCoverAvailableSQL` umstellen und das im SUMMARY begruenden. In `admin_content_sync.go`: Gate `:22` durch unbedingten Aufruf von `getAnimeSyncSourceV2` ersetzen, Legacy-Query `:27` samt Scan loeschen. In beiden Dateien alle dadurch ungenutzt gewordenen Imports, Hilfsfunktionen und Variablen mitentfernen, bis `go vet` sauber ist. Tests nachziehen: in `runtime_authority_test.go` die zwei Fragmente `"max_episodes, genre, description, cover_image,"` und `"select id, %s as display_title, type, status, year, cover_image, max_episodes"` aus der `required`-Liste entfernen; die restlichen Fragmente derselben Assertion bleiben; die `release_media`-Assertionen bei `:156-167` NICHT anfassen (D-07). In `anime_test.go` die beiden Tests `TestBuildAnimeListWhere_QueryPositions` und `TestBuildAnimeListWhere_HasCoverFalse` entfernen, sofern `buildAnimeListWhere` geloescht wurde; die V2-Pendants bleiben.</action>
  <acceptance_criteria>
    - `grep -nE "cover_image" backend/internal/repository/anime.go backend/internal/repository/admin_content_sync.go` liefert KEINE Treffer.
    - `grep -rn "func buildAnimeListWhere(" backend/internal/repository/` liefert keinen Treffer ODER das SUMMARY begruendet den verbliebenen Aufrufer.
    - `grep -q "buildAnimeListWhereV2" backend/internal/repository/anime_v2.go` → Treffer (V2-Pendant unangetastet).
    - `grep -c "release_media" backend/internal/repository/runtime_authority_test.go` >= 1 (D-07-Assertionen erhalten).
    - `cd backend && go build ./... && go vet ./...` gruen.
    - `cd backend && go test ./internal/repository/...` gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./... && go test ./internal/repository/... && ! grep -qE "cover_image" internal/repository/anime.go && ! grep -qE "cover_image" internal/repository/admin_content_sync.go && grep -q "release_media" internal/repository/runtime_authority_test.go && echo LEGACY-READ-OK</automated>
  </verify>
  <done>Legacy-Lese-Fallbacks in anime.go und admin_content_sync.go entfernt, V2-Pfade unbedingt, Test-Assertionen gezogen, D-07-Assertionen erhalten, Build und Tests gruen.</done>
</task>

<task type="auto">
  <name>Task 2: Legacy-Schreib-/Loeschpfade in create/metadata/delete entfernen</name>
  <files>backend/internal/repository/admin_content_anime_create_v2.go, backend/internal/repository/admin_content_anime_metadata.go, backend/internal/repository/admin_content_anime_delete.go</files>
  <read_first>
    - backend/internal/repository/admin_content_anime_create_v2.go:29-80 (createAnimeLegacy komplett)
    - backend/internal/repository/admin_content_anime_metadata.go:240-300 (useV2Schema-Gate + else-Zweig mit createAnimeLegacy)
    - backend/internal/repository/admin_content_anime_metadata.go:350-440 (Legacy-Update-Assignments :395 + RETURNING :410 + Scan)
    - backend/internal/repository/admin_content_anime_delete.go:20-110 (useV2Schema-Ermittlung + Weitergabe + loadAnimeDeleteResult)
    - backend/internal/repository/admin_content_anime_delete.go:170-200 und :230-300 und :330-365 (deleteAnimeAssociations, release_media-Guards NICHT anfassen, resolveOrphanedLocalCoverImage)
  </read_first>
  <action>In `admin_content_anime_metadata.go`: das `hasV2AnimeCreateSchema`-Gate `:250` und den `else`-Zweig `:260ff` aufloesen — nur der `createAnimeV2`-Pfad bleibt; die im else-Zweig aufgerufenen Helfer (`syncAnimeSourceLinks`, `syncAuthoritativeAnimeMetadata`, Audit-Schreibung) NUR dann mitentfernen, wenn sie nach dem Umbau nachweislich keinen weiteren Aufrufer haben (`grep -rn "<name>(" backend/ --include=*.go` vorher pruefen) — im Zweifel behalten. Ebenso den Legacy-Update-Pfad `:395/:410` samt seiner Assignment-/RETURNING-/Scan-Bloecke entfernen; falls dieser Pfad ebenfalls hinter einem `useV2Schema`-Gate haengt, das Gate wie oben aufloesen. In `admin_content_anime_create_v2.go`: `createAnimeLegacy` vollstaendig loeschen (nach dem Metadata-Umbau ohne Aufrufer). In `admin_content_anime_delete.go`: `hasV2AnimeDeleteAssetSchema` und den `useV2Schema`-Parameter aus `loadAnimeDeleteResult`, `deleteAnimeAssociations`, `resolveOrphanedLocalCoverImage` und dem `:70`-Zweig entfernen; die jeweiligen `...V2`-Implementierungen werden unbedingt aufgerufen; die Legacy-Rumpfe `:96` (`SELECT id, title, cover_image ... FOR UPDATE`) und `:353` (`SELECT COUNT(*) FROM anime WHERE cover_image = $1`) loeschen. `hasV2AnimeDeleteAssetSchema`/`hasV2AnimeCreateSchema` mitentfernen, wenn ohne Aufrufer. STRIKT NICHT ANFASSEN (D-07): die beiden `NOT EXISTS (SELECT 1 FROM release_media rm WHERE rm.media_id = ma.id)`-Referenzguards vor `DELETE FROM media_assets` (~:247 und ~:280) — sie stehen in derselben Datei, gehoeren aber zur lebenden Junction und bleiben unveraendert. Alle DTO-Felder (`AdminAnimeCreateInput.CoverImage`, `AdminAnimeItem.CoverImage`, `AdminAnimeDeleteResult.OrphanedLocalCoverImage`) bleiben; nur SQL-Spalten-Zugriffe fallen. Ungenutzte Imports/Funktionen bereinigen bis `go vet` sauber ist. Datei-Zeilenlimit 450 beachten (alle drei Dateien schrumpfen).</action>
  <acceptance_criteria>
    - `grep -nE "cover_image" backend/internal/repository/admin_content_anime_create_v2.go backend/internal/repository/admin_content_anime_metadata.go backend/internal/repository/admin_content_anime_delete.go` liefert KEINE SQL-Spalten-Referenz mehr (DTO-/Go-Feldnamen wie `CoverImage` sind nicht betroffen).
    - `grep -rn "createAnimeLegacy" backend/ --include=*.go` liefert KEINE Treffer.
    - `grep -c "release_media" backend/internal/repository/admin_content_anime_delete.go` >= 2 (beide D-07-Referenzguards erhalten).
    - `grep -rn "useV2Schema" backend/internal/repository/admin_content_anime_delete.go backend/internal/repository/admin_content_anime_metadata.go` liefert KEINE Treffer.
    - `cd backend && go build ./... && go vet ./...` gruen.
    - `cd backend && go test ./internal/repository/... ./internal/handlers/...` gruen.
    - Repo-weiter Endnachweis (identischer Scope wie 106-08 Task 1 / 106-09 Task 3 — NUR `backend/internal` + `backend/cmd`, NUR `*.go`; `frontend/src`, `database/migrations/**` und `scripts/**` sind bewusst ausserhalb): `grep -rnE "a\.cover_image|,[[:space:]]*cover_image|cover_image[[:space:]]*=|SET cover_image" backend/internal backend/cmd --include=*.go | grep -v 'json:' | grep -v 'applyString'` liefert KEINE Treffer. Voraussetzung dafuer ist die `cover_url`-Umbenennung aus 106-09 Task 2 (Welle 3) — falls dort noch ein komma-praefixiertes `cover_image` in `anime_relations.go` steht, ist das an 106-09 zurueckzumelden und NICHT hier zu reparieren.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./... && go test ./internal/repository/... ./internal/handlers/... && ! grep -rq "createAnimeLegacy" internal/ --include=*.go && [ "$(grep -c 'release_media' internal/repository/admin_content_anime_delete.go)" -ge 2 ] && [ -z "$(grep -rnE 'a\.cover_image|,[[:space:]]*cover_image|cover_image[[:space:]]*=|SET cover_image' internal cmd --include=*.go | grep -v 'json:' | grep -v 'applyString')" ] && echo LEGACY-WRITE-OK</automated>
  </verify>
  <done>Legacy-Create/Update/Delete-Zweige entfernt, V2-Pfade unbedingt, release_media-Referenzguards erhalten, repo-weit keine cover_image-Spalten-Signatur mehr, Build und Tests gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin-UI → Admin-API (`/admin/anime` create/update/delete) | Untrusted Admin-Input trifft die umgebauten Schreib-/Loeschpfade; keine neue Parameter-Aufnahme, Auth-Verhalten unveraendert (Haertung erst Phase 108). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-10-01 | Denial of Service | Beim Aufloesen des Gates wird versehentlich der V2-Pfad statt des Legacy-Rumpfs entfernt → Anime-Liste/Detail/Create/Delete brechen | mitigate | Action benennt fuer jeden Zweig explizit die zu BEHALTENDE `...V2`-Funktion; Repository- und Handler-Tests als Gate; Live-Check der Admin-Anime-Oberflaeche im 106-08-Checkpoint. |
| T-106-10-02 | Tampering | Mitentfernen der `release_media`-Referenzguards in `admin_content_anime_delete.go` → noch referenzierte `media_assets` werden loeschbar (Datenverlust) | mitigate | Explizite NICHT-anfassen-Marke in der Action + Acceptance-Criterion `grep -c release_media >= 2` + `verify`-Gate. |
| T-106-10-03 | Repudiation | Entfernen der Audit-Schreibung zusammen mit dem Legacy-else-Zweig → Admin-Aktionen ohne Attribution | mitigate | Action verlangt Aufrufer-grep vor dem Entfernen jedes Helfers; im Zweifel behalten; Handler-Tests decken den Audit-Pfad. |
| T-106-10-04 | Denial of Service | Ungenutzte Funktion/Import bleibt zurueck → `go vet`/Build rot | mitigate | Bereinigungsauftrag in der Action + `go vet` als Acceptance-Criterion. |
| T-106-10-SC | Tampering | npm/pip/cargo installs | accept | Keine Installs — kein Supply-Chain-Vektor. |
</threat_model>

<verification>
- `cd backend && go build ./... && go vet ./...` gruen.
- `cd backend && go test ./internal/repository/... ./internal/handlers/...` gruen.
- Repo-weit keine `cover_image`-SQL-Spalten-Signatur mehr (DTO-Felder ausgenommen).
- `release_media`-Referenzguards in `admin_content_anime_delete.go` und `runtime_authority_test.go` nachweislich erhalten (D-07).
</verification>

<success_criteria>
SC2-Teil erfuellt: Alle dauerhaft toten `HasSlug`/`useV2Schema`-Legacy-Zweige mit `anime.cover_image`-Bezug sind ersatzlos entfernt, die V2-Pfade laufen unbedingt, und repo-weit existiert keine SQL-Referenz auf die gedroppte Spalte mehr.
</success_criteria>

<output>
Create `.planning/phases/106-medienkern-schema-legacy-abbau/106-10-SUMMARY.md` when done. Dokumentiere: (a) welche Gates aufgeloest und welche Funktionen geloescht wurden, (b) jede Entscheidung „Helfer behalten statt loeschen" samt Aufrufer-Nachweis, (c) den Beleg, dass die beiden `release_media`-Referenzguards und die `runtime_authority_test.go`-Assertionen unveraendert sind (D-07).
</output>
