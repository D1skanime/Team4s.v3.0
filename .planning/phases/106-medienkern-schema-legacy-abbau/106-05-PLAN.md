---
phase: 106-medienkern-schema-legacy-abbau
plan: 05
type: execute
wave: 2
depends_on: [106-01]
files_modified:
  - backend/internal/repository/anime_v2.go
  - backend/internal/repository/anime_assets.go
  - backend/internal/repository/anime_schema.go
  - backend/internal/repository/episode_version_repository_read_helpers.go
  - backend/internal/repository/admin_content_anime_delete.go
  - backend/internal/repository/runtime_authority_test.go
autonomous: true
requirements: [SC2]
must_haves:
  truths:
    - "PATTERNS-Enrichment/Cluster D-reads: Alle LESENDEN `release_media`-SQL-Call-Sites (admin_content_anime_delete NOT-EXISTS-Guards, anime_assets UNION-Zweig, episode_version_read_helpers FROM-Klausel) sind bereinigt — nach `DROP TABLE release_media` kein Laufzeit-SQL-Fehler."
    - "PATTERNS-Enrichment: Der Test-Guard `runtime_authority_test.go:161-163`, der den `from release_media rm`-Lesepfad POSITIV erzwingt, ist angepasst; `go test` bleibt gruen."
    - "SC2/D-04 Cluster E: Die `anime.cover_image`-Spalten-Lese-/Schreibpfade (animeCoverImageSelectSQL-Zweig, syncLegacyAnimeCoverImageV2, HasCoverImage-Guard + Schema-Detektion) sind entfernt."
    - "Pitfall 3: Das abgeleitete JSON-DTO-Feld `cover_image` bleibt (COALESCE ueber cover_resolved_url/poster/Jellyfin); cover_asset_id/cover_resolved_url/cover_source/cover_provider_key bleiben unberuehrt (Phase 108)."
    - "`go build ./... && go vet ./...` + Repository-Tests bleiben gruen."
  artifacts:
    - path: "backend/internal/repository/anime_v2.go"
      provides: "Cover-Select-SQL ohne cover_image-Spalten-Zweig (DTO-Feld weiter aufgeloest)"
      contains: "animeCoverImageSelectSQL"
  key_links:
    - from: "backend/internal/repository/runtime_authority_test.go"
      to: "kein release_media-Lesepfad-Zwang"
      via: "angepasste Assertion"
      pattern: "release_media"
---

<objective>
Bereinigt die von PATTERNS aufgedeckten LESENDEN `release_media`-Call-Sites (die `go build` gruen lassen, aber nach dem Drop zur Laufzeit brechen) inkl. des Test-Guards, der den Lesepfad erzwingt — und entfernt die `anime.cover_image`-Spalten-Zugriffe (Cluster E). Das abgeleitete DTO-Feld `cover_image` und der aktuelle `cover_asset_id`-Mechanismus bleiben unberuehrt.

Purpose: SC2-Anteil + Laufzeit-Korrektheit — verhindert „gruener Build, roter go test / Laufzeit-SQL-Fehler" nach dem release_media/cover_image-Drop aus 0131.
Output: bereinigte anime/read-helper/delete-Repos + angepasster Test-Guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-medienkern-schema-legacy-abbau/106-RESEARCH.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-PATTERNS.md

<interfaces>
<!-- Verifizierte Call-Sites (PATTERNS Enrichment + RESEARCH Cluster E). -->
- LESENDE release_media-SQL (brechen zur Laufzeit nach Drop):
  admin_content_anime_delete.go:247/280 → `AND NOT EXISTS (SELECT 1 FROM release_media rm WHERE rm.media_id = ma.id)`.
  anime_assets.go:539 → `UNION ALL SELECT 1 FROM release_media WHERE media_id = $1` (im „stillReferenced"-EXISTS).
  episode_version_repository_read_helpers.go:357-376 → Release-Assets-Lesehelfer `FROM release_media rm JOIN media_assets ma ...`.
- Test-Guard (POSITIV, erzwingt Lesepfad): runtime_authority_test.go:161-163 `strings.Contains(readHelpersContent, "from release_media rm")`.
- BLEIBEN gueltig (kein Handlungsbedarf, Forbidden-/Negativ-Assertions): member_profile_repository_test.go:295-296,
  theme_segment_render_cache_test.go:26/95/97 (SourceKind-Label + Forbidden-Liste, anderes Konzept).
- cover_image-Spalte (Cluster E): anime_v2.go:419 (animeCoverImageSelectSQL) + :443 (animeCoverAvailableSQL);
  anime_assets.go:349/386/428 (syncLegacyAnimeCoverImageV2), :516-524 (clear legacy cover), :539/:966-981/:1712;
  anime_schema.go:21 (`HasCoverImage bool`), :62-63 (Detektions-Case).
- BLEIBEN: cover_asset_id/cover_source/cover_resolved_url/cover_provider_key (aktueller Mechanismus, Phase 108).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lesende release_media-Call-Sites + Test-Guard bereinigen (Cluster D-reads)</name>
  <files>backend/internal/repository/admin_content_anime_delete.go, backend/internal/repository/anime_assets.go, backend/internal/repository/episode_version_repository_read_helpers.go, backend/internal/repository/runtime_authority_test.go</files>
  <read_first>
    - backend/internal/repository/episode_version_repository_read_helpers.go:340-395 (Lesehelfer + Caller-Kette pruefen)
    - backend/internal/repository/anime_assets.go:526-560 (stillReferenced-EXISTS mit UNION)
    - backend/internal/repository/admin_content_anime_delete.go:240-285 (beide NOT-EXISTS-Guards)
    - backend/internal/repository/runtime_authority_test.go:150-170 (positive release_media-Assertion)
    - 106-PATTERNS.md „Enrichment / Risiko-Befunde" (Handlungsbedarf-Beschreibung)
  </read_first>
  <action>Entferne in `admin_content_anime_delete.go` (~:247 und ~:280) je die Klausel `AND NOT EXISTS (SELECT 1 FROM release_media rm WHERE rm.media_id = ma.id)` — der Cleanup-Guard braucht release_media nicht mehr (Junction ist weg). In `anime_assets.go` (~:534-539) den `UNION ALL SELECT 1 FROM release_media WHERE media_id = $1`-Zweig aus dem `stillReferenced`-EXISTS entfernen; die uebrigen UNION-Zweige (anime_media/episode_media/fansub_group_media) bleiben. Fuer den Release-Assets-Lesehelfer in `episode_version_repository_read_helpers.go:357-376`: pruefe die Caller-Kette; da die einzige Datenquelle die gedroppte `release_media`-Junction ist, entferne den Lesehelfer (und einen nun toten Caller/DTO-Pfad, falls vorhanden) ODER — falls ein lebender Caller existiert, der ein leeres Ergebnis vertraegt — ersetze die Query so, dass sie nicht mehr `release_media` referenziert. Dokumentiere die gewaehlte Variante im SUMMARY. Passe `runtime_authority_test.go:161-163` an: die POSITIVE Assertion, dass der Lesehelfer `from release_media rm` enthaelt, entfernen bzw. auf den neuen (release_media-freien) Zustand umstellen. `go build` + `go test ./internal/repository/... -run RuntimeAuthority` gruen.</action>
  <acceptance_criteria>
    - `grep -rn "\brelease_media\b" backend/ --include=*.go | grep -v "release_version_media" | grep -v "_test.go"` liefert KEINE Treffer.
    - `runtime_authority_test.go` erzwingt keinen `from release_media rm`-Lesepfad mehr.
    - `cd backend && go build ./... && go vet ./...` gruen.
    - `cd backend && go test ./internal/repository/... -run RuntimeAuthority` gruen.
    - member_profile_repository_test.go / theme_segment_render_cache_test.go bleiben unveraendert und gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./... && go test ./internal/repository/... -run RuntimeAuthority && ! (grep -rn "\brelease_media\b" internal/ --include=*.go | grep -v "release_version_media" | grep -v "_test.go" | grep -q .) && echo D-READS-OK</automated>
  </verify>
  <done>Alle lesenden release_media-SQL-Stellen entfernt, Test-Guard umgestellt, Repository-Tests gruen.</done>
</task>

<task type="auto">
  <name>Task 2: anime.cover_image-Spalten-Zugriffe entfernen (Cluster E)</name>
  <files>backend/internal/repository/anime_v2.go, backend/internal/repository/anime_assets.go, backend/internal/repository/anime_schema.go</files>
  <read_first>
    - backend/internal/repository/anime_v2.go:410-450 (animeCoverImageSelectSQL + animeCoverAvailableSQL — welcher COALESCE-Zweig ist die Spalte)
    - backend/internal/repository/anime_assets.go:340-435 + :510-600 + :960-985 (syncLegacyAnimeCoverImageV2, HasCoverImage-Guard, clear legacy cover)
    - backend/internal/repository/anime_schema.go (komplett, 77 Z. — HasCoverImage-Feld + Detektions-Case)
    - 106-RESEARCH.md Cluster E + Pitfall 3 (DTO-Feld bleibt)
  </read_first>
  <action>In `anime_v2.go`: aus `animeCoverImageSelectSQL` (~:419) und `animeCoverAvailableSQL` (~:443) den `NULLIF(BTRIM(%s.cover_image), '')`-Zweig aus dem COALESCE entfernen; die uebrigen Quellen (`cover_resolved_url`, `poster.file_path`, Jellyfin-URL) bleiben — das JSON-DTO-Feld `cover_image` liefert weiter Werte (Pitfall 3, FE bleibt gruen). In `anime_assets.go`: entferne `syncLegacyAnimeCoverImageV2` und seine Aufrufe (~:349/:386/:428), den `HasCoverImage`-abhaengigen „clear legacy anime cover image"-Block (~:510-524) und die uebrigen cover_image-Spalten-Schreib-/Lesestellen (~:966-981; Kommentar :1712 bereinigen). In `anime_schema.go`: entferne das Feld `HasCoverImage bool` (:21) und den zugehoerigen dynamischen Spalten-Detektions-Case (:62-63). NICHT anfassen: `cover_asset_id`, `cover_source`, `cover_resolved_url`, `cover_provider_key`. `go build` + `go vet` + Repository-Tests gruen halten. Datei-Zeilenlimit beachten (anime_assets.go schrumpft durch Entfernen — gut).</action>
  <acceptance_criteria>
    - `grep -rn "cover_image" backend/internal/repository/anime_v2.go backend/internal/repository/anime_assets.go backend/internal/repository/anime_schema.go" liefert keine SQL-Spalten-Referenz mehr (nur ggf. das JSON-DTO-Feld an anderer Stelle, hier nicht).
    - `grep -rn "syncLegacyAnimeCoverImageV2\|HasCoverImage" backend/ --include=*.go` liefert KEINE Treffer.
    - `grep -rn "cover_asset_id\|cover_resolved_url" backend/internal/repository/anime_assets.go` liefert weiterhin Treffer (Mechanismus intakt).
    - `cd backend && go build ./... && go vet ./...` gruen.
    - `cd backend && go test ./internal/repository/... -run Anime` gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./... && go test ./internal/repository/... -run Anime && ! grep -rq "syncLegacyAnimeCoverImageV2\|HasCoverImage" internal/ --include=*.go && echo COVER-COL-OK</automated>
  </verify>
  <done>cover_image-Spalten-Zugriffe entfernt, DTO-Feld + cover_asset_id-Mechanismus intakt, Build + Tests gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Keine neue. Reine Repository-Query-/Schema-Detektions-Bereinigung.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-05-01 | Denial of Service | Laufzeit-SQL-Fehler durch verbleibende release_media-Reads nach Drop | mitigate | word-boundary-grep-Gate stellt Null-Rest sicher; RuntimeAuthority-Test gruen. |
| T-106-05-02 | Tampering | Versehentliches Entfernen des lebenden cover_asset_id-Mechanismus | mitigate | Explizite „BLEIBT"-Liste + grep-Assertion auf cover_asset_id/cover_resolved_url. |
| T-106-05-SC | Tampering | Package-Installs | accept | Keine Installs. |
Kein neuer Angriffsvektor.
</threat_model>

<verification>
- `cd backend && go build ./... && go vet ./...` gruen.
- `cd backend && go test ./internal/repository/...` gruen.
- word-boundary-grep `\brelease_media\b` (ohne _test/-release_version_media) leer; `syncLegacyAnimeCoverImageV2`/`HasCoverImage` leer.
</verification>

<success_criteria>
SC2-Teil erfuellt: lesende release_media-Call-Sites + Test-Guard bereinigt, cover_image-Spalten-Zugriffe entfernt, DTO-Feld + cover_asset_id intakt, Build + Tests gruen.
</success_criteria>

<output>
Create `.planning/phases/106-medienkern-schema-legacy-abbau/106-05-SUMMARY.md` when done.
</output>
