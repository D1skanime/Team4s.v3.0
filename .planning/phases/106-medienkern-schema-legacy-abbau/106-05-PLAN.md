---
phase: 106-medienkern-schema-legacy-abbau
plan: 05
type: execute
wave: 2
depends_on: [106-01]
files_modified:
  - backend/internal/repository/anime_v2.go
  - backend/internal/repository/anime_assets.go
  - backend/internal/repository/anime_schema.go
  - backend/internal/repository/episode_version_repository_read_helpers.go
  - backend/internal/repository/admin_content_anime_delete.go
  - backend/internal/repository/runtime_authority_test.go
  # Nur bei Entscheidungs-Option B/C beruehrt (siehe Task 1 checkpoint:decision):
  - backend/internal/handlers/release_assets_handler.go
  - backend/internal/handlers/release_assets_handler_test.go
  - backend/cmd/server/main.go
  # Nur bei Option A beruehrt (0131-DROP zuruecknehmen):
  - database/migrations/0131_media_core_schema.up.sql
  - database/migrations/0131_media_core_schema.down.sql
  - backend/internal/migrations/media_core_schema_test.go
autonomous: false
requirements: [SC2]
must_haves:
  truths:
    - "GRENZ-RISIKO (Blocker 1, entschieden per Task-1-Checkpoint): Der LIVE Public-Endpoint `GET /releases/:id/assets` (ListReleaseAssets) hat als EINZIGE Datenquelle die in 0131 gedroppte `release_media`-Junction UND einen lebenden Frontend-Consumer (`frontend/src/app/episodes/[id]/page.tsx` rendert via `getReleaseAssets` eine Media-Assets-Sektion). Er wird NICHT still geleert und NICHT still entfernt — die Behandlung ist eine bewusste Nutzer-Entscheidung (A/B/C)."
    - "Cluster D-reads (autonom, kein Live-Consumer): Die zwei UNKRITISCHEN lesenden `release_media`-Call-Sites (admin_content_anime_delete NOT-EXISTS-Guards :247/:280, anime_assets UNION-Zweig :539) sind ersatzlos bereinigt — nach `DROP TABLE release_media` kein Laufzeit-SQL-Fehler an diesen Stellen."
    - "PATTERNS-Enrichment: Der Test-Guard `runtime_authority_test.go` (positive `from release_media rm`-Lese-Assertion :161-163 UND, bei Option B/C, die Handler-Assertion :165-166) ist auf den gewaehlten Zielzustand umgestellt; `go test` bleibt gruen."
    - "SC2/D-04 Cluster E: Die `anime.cover_image`-Spalten-Lese-/Schreibpfade (animeCoverImageSelectSQL-Zweig, syncLegacyAnimeCoverImageV2, HasCoverImage-Guard + Schema-Detektion) sind entfernt."
    - "Pitfall 3: Das abgeleitete JSON-DTO-Feld `cover_image` bleibt (COALESCE ueber cover_resolved_url/poster/Jellyfin); cover_asset_id/cover_resolved_url/cover_source/cover_provider_key bleiben unberuehrt (Phase 108)."
    - "`go build ./... && go vet ./...` + Repository-Tests bleiben gruen."
  artifacts:
    - path: "backend/internal/repository/anime_v2.go"
      provides: "Cover-Select-SQL ohne cover_image-Spalten-Zweig (DTO-Feld weiter aufgeloest)"
      contains: "animeCoverImageSelectSQL"
  key_links:
    - from: "backend/internal/repository/runtime_authority_test.go"
      to: "kein release_media-Lesepfad-Zwang (Zielzustand gemaess Entscheidung)"
      via: "angepasste Assertion"
      pattern: "release_media"
---

<objective>
Bereinigt die von PATTERNS aufgedeckten LESENDEN `release_media`-Call-Sites und entfernt die `anime.cover_image`-Spalten-Zugriffe (Cluster E). Kern-Aenderung dieser Revision: Der Release-Assets-Lesehelfer `ListReleaseAssets` wird NICHT mehr ambig „entfernen ODER leer-neutern" behandelt — die Frontend-Consumer-Untersuchung (siehe Interfaces) hat einen LEBENDEN Public-Consumer gefunden, daher ist die Behandlung eine explizite Nutzer-Entscheidung (Task 1, checkpoint:decision). Das abgeleitete DTO-Feld `cover_image` und der aktuelle `cover_asset_id`-Mechanismus bleiben unberuehrt.

Purpose: SC2-Anteil + Laufzeit-Korrektheit OHNE stille Frontend-Verhaltensaenderung — verhindert „gruener Build, roter go test / Laufzeit-SQL-Fehler" nach dem release_media/cover_image-Drop, ohne die Phasengrenze „kein Verhaltensumbau an Frontend" zu verletzen.
Output: bereinigte anime/read-helper/delete-Repos + angepasster Test-Guard + dokumentierte Endpoint-Entscheidung.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-medienkern-schema-legacy-abbau/106-RESEARCH.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-PATTERNS.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-CONTEXT.md

<interfaces>
<!-- Verifizierte Call-Sites (PATTERNS Enrichment + RESEARCH Cluster E) + Frontend-Consumer-Untersuchung (Blocker 1). -->

FRONTEND-CONSUMER-UNTERSUCHUNG (Blocker 1 — Ergebnis: LEBENDER Consumer gefunden):
- `frontend/src/lib/api.ts:6405` → `export async function getReleaseAssets(releaseID)` ruft `GET ${API_BASE_URL}/api/v1/releases/${releaseID}/assets` (`authorizedFetch`, no-store).
- `frontend/src/app/episodes/[id]/page.tsx:104-120` → nutzt `getReleaseAssets(activeReleaseID)` bei gesetztem `?releaseId=`; :201-202 rendert `assets={releaseAssets}` in einer Media-Assets-Sektion (404 → leere Liste, sonst Fehlermeldung).
  => Der Endpoint ist NICHT tot/deferred. Ein Drop von `release_media` bricht ihn zur Laufzeit; stilles Leeren/Entfernen waere eine sichtbare FE-Verhaltensaenderung (verletzt Phasengrenze „kein Verhaltensumbau an Frontend" + D-03 „nur build-breaking FE").

VOLLE ENDPOINT-CHAIN (fuer Option B/C vollstaendig zu behandeln):
- Repo-Helfer: episode_version_repository_read_helpers.go:322 `func (r *EpisodeVersionRepository) ListReleaseAssets(...)`, Query :357-376 `FROM release_media rm JOIN media_assets ma ON ma.id = rm.media_id`.
- Handler: backend/internal/handlers/release_assets_handler.go:26-51 `ListReleaseAssets` → `episodeVersionRepo.ListReleaseAssets`.
- Route: backend/cmd/server/main.go:447 `v1.GET("/releases/:id/assets", releaseAssetsHandler.ListReleaseAssets)`.
- Handler-Test: backend/internal/handlers/release_assets_handler_test.go.
- Test-Guard runtime_authority_test.go: :160-163 positiv `from release_media rm` + `join media_assets ma on ma.id = rm.media_id`; :165-166 positiv `episodeversionrepo.listreleaseassets` (Handler-Wiring).

LESENDE release_media-SQL — UNKRITISCH (kein Live-Consumer, autonom in Task 2 bereinigbar):
- admin_content_anime_delete.go:247/280 → `AND NOT EXISTS (SELECT 1 FROM release_media rm WHERE rm.media_id = ma.id)` (Cleanup-Guard, Junction weg → Klausel entfallen).
- anime_assets.go:539 → `UNION ALL SELECT 1 FROM release_media WHERE media_id = $1` (im „stillReferenced"-EXISTS; uebrige UNION-Zweige anime_media/episode_media/fansub_group_media bleiben).

BLEIBEN gueltig (kein Handlungsbedarf — reine String-/Forbidden-Assertions, KEIN gegen die DB ausgefuehrtes SQL; Warning 3 verifiziert):
- theme_segment_render_cache_test.go:26 `invalid.SourceKind = "release_media"` (Validierungs-Input-String).
- theme_segment_render_cache_test.go:95/97 `"INSERT INTO release_media"`/`"REFERENCES release_media"` — Literale in einer `forbidden`-Needle-Liste (Negativ-Assertion gegen Quelltext-Inhalt, NICHT gegen die DB ausgefuehrt).
- member_profile_repository_test.go:295 `assert.False(strings.Contains(content, "FROM release_media"))` — Negativ-String-Assertion gegen Quelltext.
- Diese Dateien werden von KEINEM 106-Plan geaendert und bleiben nach dem Drop gruen.

cover_image-Spalte (Cluster E): anime_v2.go:419 (animeCoverImageSelectSQL) + :443 (animeCoverAvailableSQL);
anime_assets.go:349/386/428 (syncLegacyAnimeCoverImageV2), :516-524 (clear legacy cover), :539/:966-981/:1712;
anime_schema.go:21 (`HasCoverImage bool`), :62-63 (Detektions-Case).
BLEIBEN: cover_asset_id/cover_source/cover_resolved_url/cover_provider_key (aktueller Mechanismus, Phase 108).
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: ENTSCHEIDUNG — Behandlung des lebenden Endpoints /releases/:id/assets (release_media-Datenquelle)</name>
  <decision>
    Wie wird der lebende Public-Endpoint `GET /releases/:id/assets` behandelt, dessen EINZIGE Datenquelle die in Migration 0131 (Plan 106-01) gedroppte `release_media`-Junction ist und der einen lebenden Frontend-Consumer besitzt?
  </decision>
  <context>
    Blocker 1 (Checker): Der frueher ambige „Helfer entfernen ODER Query leer-neutern"-Text ist falsch — beide Varianten sind Verstoesse: Entfernen bricht den Build (Handler/Route/Handler-Test in keinem Plan gelistet), Leer-Neutern aendert still das Frontend-Verhalten (Media-Assets-Sektion auf der Episoden-Detailseite wird leer) und laesst einen Zombie-Endpoint zurueck (verletzt SC2 „ersatzlos entfernt" + Phasengrenze „kein Verhaltensumbau an Frontend").
    Frontend-Consumer-Untersuchung (Ergebnis): LEBENDER Consumer bestaetigt — `frontend/src/app/episodes/[id]/page.tsx` rendert via `getReleaseAssets` (`frontend/src/lib/api.ts:6405`) eine Media-Assets-Sektion. NICHT tot/deferred.
    Locked-Decision-Konflikt: D-04/DECISION §6 fordert `release_media` in 106 zu entfernen; D-03 + Phasengrenze fordern „kein Verhaltensumbau an Frontend". Bei diesem Endpoint kollidieren beide — daher Nutzer-Entscheidung noetig. Wichtig: 0131 ist zu diesem Zeitpunkt AUTHORED (Plan 01), aber noch NICHT angewandt (Live-Kette laeuft erst im Wave-4-Gate 106-08) — alle drei Optionen sind hier ohne Migrations-Rueckabwicklung ausfuehrbar.
  </context>
  <action>Praesentiere dem Nutzer die drei Optionen (A/B/C) mit den gelisteten Pros/Cons und der Planner-Empfehlung (Option A). Diese Task veraendert KEINEN Code — sie erfasst ausschliesslich die Entscheidung, die Task 2 parametrisiert. Blockiere, bis der Nutzer A, B oder C waehlt; halte die gewaehlte Option im SUMMARY fest und melde sie an 106-01/02/08 zurueck, falls Option A.</action>
  <options>
    <option id="option-a">
      <name>A — release_media-Drop in 106 zuruecknehmen (Endpoint + FE unveraendert, Removal nach Phase 108/110 verschieben)</name>
      <pros>Keine Frontend-Verhaltensaenderung; Phasengrenze „kein Verhaltensumbau an Frontend" strikt gewahrt; Endpoint bleibt live; sauberste Trennung — der Endpoint gehoert in die spaetere Medienmodell-Migration.</pros>
      <cons>Weicht von D-04/DECISION §6 ab (release_media wird erst spaeter statt in 106 entfernt); erfordert Edit von 0131 up/down (release_media-DROP + DOWN-Rekonstruktion entfernen) UND `media_core_schema_test.go` (positive release_media-DROP-Needle raus); SC2-Contract-Check (Plan 02/08) muss `release_media` dann NICHT als MUSS-abwesend fuehren.</cons>
    </option>
    <option id="option-b">
      <name>B — Endpoint-Query auf ueberlebendes Junction (release_version_media → media_assets) umschreiben (Endpoint + FE bleiben live, release_media-Drop bleibt)</name>
      <pros>release_media wird wie in D-04 gefordert gedroppt; Endpoint + FE-Media-Assets-Sektion bleiben funktional; rein Backend-Aenderung, FE unberuehrt.</pros>
      <cons>Semantik-Risiko: `release_version_media` traegt einen ANDEREN Asset-Satz (release_version-scoped) als `release_media` (release-scoped) — Datenparitaet muss VOR Adoption belegt werden, sonst versteckte Verhaltensaenderung; Query-Rewrite + Test-Guard-Umstellung (:161-163) noetig.</cons>
    </option>
    <option id="option-c">
      <name>C — Gesamten Endpoint-Chain + FE-Consumer jetzt entfernen (bewusste, sichtbare FE-Feature-Entfernung in 106)</name>
      <pros>Saubere ersatzlose Legacy-Entfernung (SC2 im Wortsinn); release_media-Drop bleibt; kein Zombie-Endpoint.</pros>
      <cons>Sichtbare Frontend-Verhaltensaenderung (Media-Assets-Sektion verschwindet) → verletzt D-03 + Phasengrenze; nur zulaessig, wenn der Nutzer die FE-Feature-Entfernung ausdruecklich in 106 zieht; beruehrt zusaetzlich `frontend/src/lib/api.ts` + `frontend/src/app/episodes/[id]/page.tsx`.</cons>
    </option>
  </options>
  <resume-signal>Waehle: option-a, option-b oder option-c. Die Wahl parametrisiert Task 2 (Ausfuehrungsdetails je Option unten). Empfehlung des Planners: option-a (kleinster Grenzverstoss, wahrt „kein Verhaltensumbau an Frontend"; Endpoint-Removal gehoert zur spaeteren Medienmodell-Migration).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Lesende release_media-Call-Sites + gewaehlte Endpoint-Behandlung + Test-Guard (Cluster D-reads)</name>
  <files>backend/internal/repository/admin_content_anime_delete.go, backend/internal/repository/anime_assets.go, backend/internal/repository/episode_version_repository_read_helpers.go, backend/internal/repository/runtime_authority_test.go, backend/internal/handlers/release_assets_handler.go, backend/internal/handlers/release_assets_handler_test.go, backend/cmd/server/main.go, database/migrations/0131_media_core_schema.up.sql, database/migrations/0131_media_core_schema.down.sql, backend/internal/migrations/media_core_schema_test.go</files>
  <read_first>
    - backend/internal/repository/episode_version_repository_read_helpers.go:315-395 (Lesehelfer + Caller-Kette)
    - backend/internal/repository/anime_assets.go:526-560 (stillReferenced-EXISTS mit UNION)
    - backend/internal/repository/admin_content_anime_delete.go:240-285 (beide NOT-EXISTS-Guards)
    - backend/internal/repository/runtime_authority_test.go:150-170 (positive release_media-Assertion :161-163 UND Handler-Assertion :165-166)
    - backend/internal/handlers/release_assets_handler.go (komplett) + backend/cmd/server/main.go:445-450 (Route :447)
    - 106-PATTERNS.md „Enrichment / Risiko-Befunde"
  </read_first>
  <action>
    A) IMMER (autonom, unabhaengig von der Entscheidung — kein Live-Consumer):
    - `admin_content_anime_delete.go` (~:247 und ~:280): je die Klausel `AND NOT EXISTS (SELECT 1 FROM release_media rm WHERE rm.media_id = ma.id)` entfernen (Junction weg → Guard unnoetig).
    - `anime_assets.go` (~:534-539): den `UNION ALL SELECT 1 FROM release_media WHERE media_id = $1`-Zweig aus dem `stillReferenced`-EXISTS entfernen; uebrige UNION-Zweige (anime_media/episode_media/fansub_group_media) bleiben.

    B) ENDPOINT-BEHANDLUNG gemaess Task-1-Entscheidung:
    - Option A (release_media-Drop zuruecknehmen): `ListReleaseAssets` (read_helpers) + Handler + Route + Handler-Test + runtime_authority-Assertions BLEIBEN unveraendert. Stattdessen Migration 0131 anpassen: `DROP TABLE IF EXISTS release_media CASCADE;` aus `0131_*.up.sql` entfernen UND die `release_media`-Rekonstruktion aus `0131_*.down.sql` entfernen; in `backend/internal/migrations/media_core_schema_test.go` die positive `DROP TABLE IF EXISTS release_media`-Needle (UP) und die `release_media`-Rekonstruktions-Needle (DOWN) entfernen. Im SUMMARY dokumentieren, dass release_media in Phase 108/110 (mit dem Endpoint) entfernt wird — Rueckmeldung an Plan 01/02/08 (Contract-Check darf release_media dann NICHT als MUSS-abwesend fuehren).
    - Option B (Query-Rewrite auf release_version_media): Datenparitaet zwischen `release_media` und `release_version_media`→`media_assets` PRUEFEN und im SUMMARY belegen; nur bei belegter Paritaet die Query in `ListReleaseAssets` (:357-376) auf die ueberlebende Junction umschreiben (kein `release_media` mehr). Handler/Route/Handler-Test BLEIBEN (Endpoint live). runtime_authority_test.go:161-163 auf den neuen (release_media-freien) Lesepfad umstellen; :165-166 (Handler-Wiring) BLEIBT. Bei nicht belegbarer Paritaet: Task rot lassen, an Entscheider zurueckmelden (NICHT still leeren).
    - Option C (Endpoint + FE entfernen): `ListReleaseAssets`-Helfer (read_helpers) + `release_assets_handler.go` + Route `main.go:447` + `release_assets_handler_test.go` entfernen; runtime_authority_test.go:161-163 UND :165-166 (Handler-Assertion) entfernen. Zusaetzlich FE-Consumer entfernen: `getReleaseAssets` in `frontend/src/lib/api.ts` und die Media-Assets-Sektion/`getReleaseAssets`-Nutzung in `frontend/src/app/episodes/[id]/page.tsx` (Build + `npm run typecheck` gruen halten). Im SUMMARY die bewusste FE-Verhaltensaenderung dokumentieren.

    Danach: `go build ./... && go vet ./...` + `go test ./internal/repository/... -run RuntimeAuthority` (+ bei B/C `go test ./internal/handlers/... -run ReleaseAssets`) gruen. Die konkret gewaehlte Option im SUMMARY festhalten.
  </action>
  <acceptance_criteria>
    - admin_content_anime_delete.go + anime_assets.go: kein `release_media`-Lesepfad mehr (unabhaengig von der Option).
    - `grep -rn "\brelease_media\b" backend/internal --include=*.go | grep -v "release_version_media" | grep -v "_test.go"` liefert bei Option B/C KEINE Treffer; bei Option A verbleibt ausschliesslich der bewusst behaltene `ListReleaseAssets`-Lesepfad in episode_version_repository_read_helpers.go (im SUMMARY als bewusst-behalten dokumentiert).
    - runtime_authority_test.go spiegelt den gewaehlten Zielzustand (Option A: unveraendert gruen; Option B: :161-163 umgestellt, :165-166 bleibt; Option C: beide entfernt).
    - `cd backend && go build ./... && go vet ./...` gruen.
    - `cd backend && go test ./internal/repository/... -run RuntimeAuthority` gruen (Option B/C zusaetzlich `-run ReleaseAssets` bei den Handlern).
    - member_profile_repository_test.go / theme_segment_render_cache_test.go bleiben unveraendert und gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./... && go test ./internal/repository/... -run RuntimeAuthority && echo D-READS-OK</automated>
  </verify>
  <done>Unkritische release_media-Reads entfernt, Endpoint gemaess Nutzer-Entscheidung behandelt (nicht still geleert), Test-Guard auf Zielzustand, Repository-Tests gruen.</done>
</task>

<task type="auto">
  <name>Task 3: anime.cover_image-Spalten-Zugriffe entfernen (Cluster E)</name>
  <files>backend/internal/repository/anime_v2.go, backend/internal/repository/anime_assets.go, backend/internal/repository/anime_schema.go</files>
  <read_first>
    - backend/internal/repository/anime_v2.go:410-450 (animeCoverImageSelectSQL + animeCoverAvailableSQL — welcher COALESCE-Zweig ist die Spalte)
    - backend/internal/repository/anime_assets.go:340-435 + :510-600 + :960-985 (syncLegacyAnimeCoverImageV2, HasCoverImage-Guard, clear legacy cover)
    - backend/internal/repository/anime_schema.go (komplett, 77 Z. — HasCoverImage-Feld + Detektions-Case)
    - 106-RESEARCH.md Cluster E + Pitfall 3 (DTO-Feld bleibt)
  </read_first>
  <action>In `anime_v2.go`: aus `animeCoverImageSelectSQL` (~:419) und `animeCoverAvailableSQL` (~:443) den `NULLIF(BTRIM(%s.cover_image), '')`-Zweig aus dem COALESCE entfernen; die uebrigen Quellen (`cover_resolved_url`, `poster.file_path`, Jellyfin-URL) bleiben — das JSON-DTO-Feld `cover_image` liefert weiter Werte (Pitfall 3, FE bleibt gruen). In `anime_assets.go`: entferne `syncLegacyAnimeCoverImageV2` und seine Aufrufe (~:349/:386/:428), den `HasCoverImage`-abhaengigen „clear legacy anime cover image"-Block (~:510-524) und die uebrigen cover_image-Spalten-Schreib-/Lesestellen (~:966-981; Kommentar :1712 bereinigen). In `anime_schema.go`: entferne das Feld `HasCoverImage bool` (:21) und den zugehoerigen dynamischen Spalten-Detektions-Case (:62-63). NICHT anfassen: `cover_asset_id`, `cover_source`, `cover_resolved_url`, `cover_provider_key`. `go build` + `go vet` + Repository-Tests gruen halten. Datei-Zeilenlimit beachten (anime_assets.go schrumpft durch Entfernen — gut).</action>
  <acceptance_criteria>
    - `grep -rn "cover_image" backend/internal/repository/anime_v2.go backend/internal/repository/anime_assets.go backend/internal/repository/anime_schema.go` liefert keine SQL-Spalten-Referenz mehr (nur ggf. das JSON-DTO-Feld an anderer Stelle, hier nicht).
    - `grep -rn "syncLegacyAnimeCoverImageV2\|HasCoverImage" backend/ --include=*.go` liefert KEINE Treffer.
    - `grep -rn "cover_asset_id\|cover_resolved_url" backend/internal/repository/anime_assets.go` liefert weiterhin Treffer (Mechanismus intakt).
    - `cd backend && go build ./... && go vet ./...` gruen.
    - `cd backend && go test ./internal/repository/... -run Anime` gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./... && go test ./internal/repository/... -run Anime && ! grep -rq "syncLegacyAnimeCoverImageV2\|HasCoverImage" internal/ --include=*.go && echo COVER-COL-OK</automated>
  </verify>
  <done>cover_image-Spalten-Zugriffe entfernt, DTO-Feld + cover_asset_id-Mechanismus intakt, Build + Tests gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Keine neue. Reine Repository-Query-/Schema-Detektions-Bereinigung; der Public-Endpoint `/releases/:id/assets` bleibt (Option A/B) bzw. wird bewusst entfernt (Option C) — keine neue Vertrauensgrenze.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-05-01 | Denial of Service | Laufzeit-SQL-Fehler durch verbleibende release_media-Reads nach Drop | mitigate | Unkritische Reads entfernt; der lebende ListReleaseAssets-Pfad wird per checkpoint:decision explizit behandelt (nicht still gebrochen); RuntimeAuthority-Test gruen. |
| T-106-05-02 | Tampering | Stille Frontend-Verhaltensaenderung durch Leer-Neutern des Endpoints | mitigate | checkpoint:decision verbietet stilles Neutern; Optionen wahren „kein Verhaltensumbau an Frontend" (A/B) bzw. machen die FE-Aenderung explizit (C). |
| T-106-05-03 | Tampering | Versehentliches Entfernen des lebenden cover_asset_id-Mechanismus | mitigate | Explizite „BLEIBT"-Liste + grep-Assertion auf cover_asset_id/cover_resolved_url. |
| T-106-05-SC | Tampering | Package-Installs | accept | Keine Installs. |
Kein neuer Angriffsvektor.
</threat_model>

<verification>
- `cd backend && go build ./... && go vet ./...` gruen.
- `cd backend && go test ./internal/repository/...` gruen (Option B/C zusaetzlich Handler-Tests).
- Endpoint-Behandlung entspricht der Task-1-Entscheidung (im SUMMARY dokumentiert); KEIN still geleerter Zombie-Endpoint.
- `syncLegacyAnimeCoverImageV2`/`HasCoverImage` grep leer.
</verification>

<success_criteria>
SC2-Teil erfuellt: unkritische release_media-Reads + Test-Guard bereinigt, der lebende `/releases/:id/assets`-Endpoint per Nutzer-Entscheidung behandelt (nicht still geleert/entfernt), cover_image-Spalten-Zugriffe entfernt, DTO-Feld + cover_asset_id intakt, Build + Tests gruen.
</success_criteria>

<output>
Create `.planning/phases/106-medienkern-schema-legacy-abbau/106-05-SUMMARY.md` when done. Dokumentiere die gewaehlte Endpoint-Option (A/B/C) und — bei Option A — die Rueckmeldung an Plan 01/02/08.
</output>
