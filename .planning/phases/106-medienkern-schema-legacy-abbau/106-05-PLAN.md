---
phase: 106-medienkern-schema-legacy-abbau
plan: 05
type: execute
wave: 2
depends_on: [106-01]
files_modified:
  - backend/internal/repository/anime_v2.go
  - backend/internal/repository/anime_assets.go
  - backend/internal/repository/anime_schema.go
  - backend/internal/repository/anime_test.go
  - backend/internal/repository/runtime_authority_test.go
autonomous: true
requirements: [SC2]
must_haves:
  truths:
    - "PO-Entscheid 2026-07-22 (LOCKED): `anime.cover_image` bleibt — anders als `release_media` (D-07) — im Abbau-Scope von Phase 106. Begruendung: ein produktiv erprobter Drop-in-Ersatz existiert bereits (`animeCoverImageSelectSQL`, COALESCE ueber `cover_resolved_url` / poster-`file_path` / Jellyfin-URL), die Umstellung ist mechanisch und verhaltenserhaltend und wirft keine Designfrage auf. `release_media` hatte diesen fertigen Ersatz nicht und brauchte zusaetzlich einen Datenparitaets-Entscheid — daher dort Verschiebung nach Phase 108."
    - "SC2/D-04 Cluster E: Die `anime.cover_image`-SPALTEN-Lese-/Schreibpfade (animeCoverImageSelectSQL-COALESCE-Zweig, animeCoverAvailableSQL-Zweig, syncLegacyAnimeCoverImageV2 + Aufrufer, HasCoverImage-Guard + dynamische Schema-Detektion) sind ersatzlos entfernt — nach `ALTER TABLE anime DROP COLUMN cover_image` (0131) kein Laufzeit-SQL-Fehler mehr."
    - "Checker-Warning 1 (behoben): Die Zeilen `anime_assets.go:349/:386/:966/:981` sind KEINE Legacy-Funktionen, sondern einzelne `cover_image`-Zuweisungen in LEBENDEN `UPDATE anime SET ...`-Statements. Dort faellt ausschliesslich die `cover_image`-Zeile; `cover_asset_id`, `cover_source`, `cover_resolved_url`, `cover_provider_key` und `updated_at` bleiben in allen vier Statements unveraendert bestehen. Vollstaendig entfernt werden nur `syncLegacyAnimeCoverImageV2` (:566-599) samt Aufruf (:428) und der `if schema.HasCoverImage`-Block (:516-524)."
    - "P-2 (RESEARCH-Refresh 2026-07-22, BLOCKER behoben): `backend/internal/repository/runtime_authority_test.go` ist WIEDER in `files_modified` — aber AUSSCHLIESSLICH fuer das eine Fragment `\"synclegacyanimecoverimagev2(ctx, tx, animeid, mediaid, schema)\"` (:120). Diese Datei assertiert POSITIV per `strings.Contains` ueber den lowercase-normalisierten Quelltext von `anime_assets.go`; sie bricht ohne Compile-Fehler, sobald das Symbol geloescht ist, und der Verify-Befehl dieses Plans (`go test ./internal/repository/... -run Anime`) trifft den Test `TestAnimeAssetCompatibilityUsesV2CoverHelpersWhenLegacySlotsAreGone` direkt."
    - "P-2-Abgrenzung (D-07 bleibt geschuetzt): Die frueher hier geforderte Byte-Identitaet der GESAMTEN `runtime_authority_test.go` ist auf die release_media-Assertionen VERENGT — `:156-167` (`from release_media rm`, `join media_assets ma on ma.id = rm.media_id`, `func (r *episodeversionrepository) listreleaseassets(`, `episodeversionrepo.listreleaseassets`) MUESSEN woertlich unveraendert bleiben und werden per positiver grep-Assertion belegt. Ebenso unangetastet bleiben die `cover_image`-Query-Fragmente :18/:21 — die zieht Plan 106-10 in Welle 4."
    - "P-2-Zusatzbefund: Fragment :124 `\"v2 remains authoritative once anime_media exists\"` verlangt den Kommentar-Satzanfang in `anime_assets.go:1710-1711`. Die Kommentar-Bereinigung ist deshalb auf den Teilsatz `und cover_image sync` (dt. `and cover_image sync`) verengt; der Satzanfang `V2 remains authoritative once anime_media exists` bleibt woertlich stehen."
    - "Pitfall 3: Das ABGELEITETE JSON-DTO-Feld `cover_image` bleibt bestehen (COALESCE ueber cover_resolved_url/poster.file_path/Jellyfin-URL); `cover_asset_id`/`cover_resolved_url`/`cover_source`/`cover_provider_key` bleiben unberuehrt (Phase 108)."
    - "D-07 (LOCKED): Dieser Plan fasst `release_media` NICHT an. Der Lesepfad `ListReleaseAssets` (episode_version_repository_read_helpers.go:322), `release_assets_handler.go`, die Route `main.go:447`, `release_assets_handler_test.go` und die `runtime_authority_test.go:156-167`-Assertionen bleiben unveraendert."
    - "D-07-Konsequenz (verifiziert): Die beiden lesenden `release_media`-Call-Sites `admin_content_anime_delete.go:247/:280` (NOT-EXISTS-Referenzguards vor `DELETE FROM media_assets`) und `anime_assets.go:~539` (UNION-Zweig im `stillReferenced`-EXISTS) BLEIBEN funktional erhalten — die Junction existiert weiter und wird weiter beschrieben (`CreateReleaseMedia`, 106-04 unveraendert); ein Entfernen der Guards wuerde noch referenzierte media_assets loeschbar machen (Datenverlust-Risiko)."
    - "Blocker 1 (Plan-Checker 2026-07-22, behoben): Die Umfangsgrenze `genau eine entfernte Zeile in runtime_authority_test.go` wird in TASK 1 geprueft (vor dem Task-Commit), nicht in Task 2. Der Executor committet nach jedem Task; ein `git diff HEAD`-Vergleich in Task 2 haette per Konstruktion 0 statt 1 geliefert und die Task deterministisch rot gemacht — mit der Anweisung, eine korrekte Aenderung zurueckzurollen. Task 2 haelt nur noch commit-unabhaengige Inhaltsassertionen."
    - "`go build ./... && go vet ./...` + Repository-Tests bleiben gruen."
  artifacts:
    - path: "backend/internal/repository/anime_v2.go"
      provides: "Cover-Select-SQL ohne cover_image-Spalten-Zweig (DTO-Feld weiter aufgeloest)"
      contains: "animeCoverImageSelectSQL"
    - path: "backend/internal/repository/anime_schema.go"
      provides: "Schema-Detektion ohne HasCoverImage-Feld/Case"
      contains: "func"
  key_links:
    - from: "backend/internal/repository/anime_v2.go (animeCoverImageSelectSQL)"
      to: "anime.cover_resolved_url / poster.file_path / Jellyfin-URL"
      via: "COALESCE ohne cover_image-Spaltenzweig"
      pattern: "COALESCE"
---

<objective>
Entfernt die `anime.cover_image`-SPALTEN-Zugriffe im Repository-Layer (Cluster E), passend zum `DROP COLUMN cover_image` in Migration 0131 (Plan 106-01). Das abgeleitete JSON-DTO-Feld `cover_image` und der aktuelle `cover_asset_id`-Mechanismus bleiben unberuehrt.

REVISION (PO-Entscheid 2026-07-21, „Option A" / CONTEXT.md D-07): Der frueher hier gefuehrte `checkpoint:decision` zur Behandlung des lebenden Endpoints `GET /releases/:id/assets` ENTFAELLT — die Entscheidung ist auf Planungsebene getroffen: `release_media` wird in Phase 106 gar nicht angefasst, die Entfernung wandert nach Phase 108 (ROADMAP 108-SC 3b). Damit entfaellt auch die zuvor geplante Bereinigung der zwei „unkritischen" release_media-Lesestellen: solange die Junction lebt und beschrieben wird, sind ihre Referenzguards korrekt und muessen bleiben. Uebrig bleibt der entscheidungs-unabhaengige cover_image-Spalten-Abbau. Der Plan ist damit wieder vollstaendig autonom.

REVISION 2 (RESEARCH-Refresh 2026-07-22, Befund P-2 — BLOCKER): Die Vorfassung hatte `runtime_authority_test.go` aus `files_modified` entfernt und in Task 2 jede Aenderung daran verboten — waehrend Task 1 die Funktion `syncLegacyAnimeCoverImageV2` loescht, die derselbe Test bei :120 POSITIV assertiert, und der Verify-Befehl `go test ./internal/repository/... -run Anime` genau diesen Test trifft. Der Plan konnte sein eigenes Gate nicht bestehen. Korrektur: die Datei ist wieder in `files_modified`, aber mit chirurgischem Umfang (GENAU EINE Slice-Zeile), und der D-07-Schutz ist von „ganze Datei byte-identisch" auf „release_media-Assertionen :156-167 woertlich unveraendert" verengt und per positiver grep-Assertion belegt.

Purpose: SC2-Anteil (Cluster E) + Laufzeit-Korrektheit nach dem cover_image-Spalten-Drop, ohne Frontend-Verhaltensaenderung.
Output: bereinigte anime_v2.go / anime_assets.go / anime_schema.go.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-medienkern-schema-legacy-abbau/106-RESEARCH.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-PATTERNS.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-CONTEXT.md

<interfaces>
<!-- Verifizierte Call-Sites (PATTERNS Enrichment + RESEARCH Cluster E). -->

cover_image-SPALTE (Cluster E — in diesem Plan zu entfernen):
- anime_v2.go:419 `animeCoverImageSelectSQL` — COALESCE mit `NULLIF(BTRIM(%s.cover_image), '')`-Zweig.
- anime_v2.go:443 `animeCoverAvailableSQL` — analoger Spaltenzweig.

PRAEZISE CALL-SITE-LISTE `anime_assets.go` (Checker-Warning 1, 2026-07-22 verifiziert — die Vorfassung
fasste :349/:386 falsch als „syncLegacyAnimeCoverImageV2 + Aufrufer" zusammen; tatsaechlich sind es
`cover_image`-Zuweisungen innerhalb LEBENDER UPDATE-Statements, deren Geschwister-Spalten BLEIBEN MUESSEN):

| Zeile | Kontext | Zu entfernen | Ausdruecklich BEHALTEN |
|-------|---------|--------------|------------------------|
| :349 | `AssignManualCover` (Legacy-Zweig), `UPDATE anime SET cover_asset_id=$2, cover_source='manual', cover_resolved_url=NULL, cover_provider_key=NULL, cover_image=$3, updated_at=NOW()` | NUR die Zeile `cover_image = $3,` + das dadurch ungenutzte `$3`-Argument (`normalizeNullableTrimmedString(coverPath)`) und die nur dafuer existierende `coverPath`-Vorabquery (:331-340, `SELECT path FROM media_files ...`) | `cover_asset_id`, `cover_source`, `cover_resolved_url`, `cover_provider_key`, `updated_at`, `ensureAnimeMediaAsset`-Aufruf, `ErrNotFound`-Behandlung |
| :386 | `ClearCoverWithResult` (Legacy-Zweig), `UPDATE anime SET cover_asset_id=NULL, cover_source=NULL, cover_resolved_url=NULL, cover_provider_key=NULL, cover_image=NULL, updated_at=NOW()` | NUR die Zeile `cover_image = NULL,` | alle uebrigen SET-Zuweisungen + `clearCoverV2`-Gate |
| :516-524 | `if schema.HasCoverImage { UPDATE anime SET cover_image = NULL, updated_at = NOW() }` („clear legacy anime cover image") | der GESAMTE `if schema.HasCoverImage`-Block inkl. Statement | direkt darueber `removeAnimeMediaLinksByType(...)` und direkt darunter der `stillReferenced`-EXISTS-Block mit dem `release_media`-UNION (~:539) |
| :566-599 | `func syncLegacyAnimeCoverImageV2(...)` inkl. `SET cover_image = $2` (:599) und `if !schema.HasCoverImage`-Guard (:573) | die GESAMTE Funktion | — |
| :428 | einziger Aufrufer `if err := syncLegacyAnimeCoverImageV2(ctx, tx, animeID, mediaID, schema); err != nil { ... }` in `assignManualCoverV2` | der gesamte Aufruf-Block | der uebrige Rumpf von `assignManualCoverV2` |
| :966 | Provider-CLEAR, `UPDATE anime SET cover_asset_id=NULL, cover_source=NULL, cover_resolved_url=NULL, cover_provider_key=NULL, cover_image=NULL, updated_at=NOW()` | NUR die Zeile `cover_image = NULL,` | alle uebrigen SET-Zuweisungen |
| :981 | Provider-APPLY, `UPDATE anime SET cover_asset_id=NULL, cover_source='provider', cover_resolved_url=$2, cover_provider_key=$3, cover_image=$2, updated_at=NOW()` | NUR die Zeile `cover_image = $2,` — die Argumente bleiben unveraendert, weil `$2` weiterhin von `cover_resolved_url` genutzt wird | alle uebrigen SET-Zuweisungen + beide Argumente |
| :1710-1711 | Kommentar `// V2 remains authoritative once anime_media exists, even if legacy slot columns` / `// still coexist for compatibility and cover_image sync.` | NUR den Teilsatz ` and cover_image sync` streichen (Satz endet dann nach `compatibility`) | Der Satzanfang `V2 remains authoritative once anime_media exists` MUSS woertlich stehen bleiben — `runtime_authority_test.go:124` assertiert genau dieses Fragment (P-2) |

- anime_schema.go:21 `HasCoverImage bool`; :62-63 dynamischer Spalten-Detektions-Case.

runtime_authority_test.go — GEZIELTE, MINIMALE ANPASSUNG (P-2, RESEARCH-Refresh 2026-07-22):
- `backend/internal/repository/runtime_authority_test.go:109-125` ist die `required`-String-Slice von
  `TestAnimeAssetCompatibilityUsesV2CoverHelpersWhenLegacySlotsAreGone`. Sie assertiert POSITIV per
  `strings.Contains(strings.ToLower(anime_assets.go), fragment)`.
- ZU ENTFERNEN: GENAU EIN Slice-Eintrag — die Zeile `"synclegacyanimecoverimagev2(ctx, tx, animeid, mediaid, schema)",` (:120).
- ZU BEHALTEN (alle uebrigen Eintraege derselben Slice, insbesondere):
  `"removeanimeposterassetsv2(ctx, tx, animeid, schema)"` (:119),
  `"loadv2animemediaidbyref(ctx, tx, trimmedmediaref, \"poster\")"` (:121),
  `"removeanimemedialinksbytype(ctx, tx, animeid, \"poster\")"` (:122),
  `"upsertanimemedialink(ctx, tx, animeid, mediaid, 0)"` (:123),
  `"v2 remains authoritative once anime_media exists"` (:124 — korrespondiert mit der verengten Kommentar-Bereinigung).
- NICHT ANFASSEN in derselben Datei:
  - :18/:21 `cover_image`-Query-Fragmente → Zustaendigkeit Plan 106-10 (Welle 4). Zwei Plaene, dieselbe Datei,
    verschiedene Wellen — im Wave-Graph dokumentiert, keine Intra-Wellen-Kollision.
  - :133-168 `TestReleaseRuntimeAuthorityUsesReleaseNativeTables` inkl. der release_media-Assertionen bei
    :156-167 → D-07 (LOCKED), MUESSEN woertlich unveraendert bleiben.

BLEIBEN unveraendert (NICHT anfassen):
- `cover_asset_id`, `cover_source`, `cover_resolved_url`, `cover_provider_key` — aktueller Mechanismus, Phase 108.
- Das ABGELEITETE JSON-DTO-Feld `cover_image` (aus COALESCE der uebrigen Quellen) — Pitfall 3, FE bleibt gruen.

D-07 / release_media — VOLLSTAENDIG AUSSERHALB DIESES PLANS (LOCKED, CONTEXT.md D-07):
- `episode_version_repository_read_helpers.go:322` `ListReleaseAssets` + Query :357-376 (`FROM release_media rm JOIN media_assets ma ...`) — UNVERAENDERT.
- `handlers/release_assets_handler.go`, Route `cmd/server/main.go:447`, `handlers/release_assets_handler_test.go` — UNVERAENDERT.
- `repository/runtime_authority_test.go:156-167` (positive `from release_media rm`- und Handler-Wiring-Assertionen) — UNVERAENDERT.
- `admin_content_anime_delete.go:~247` (`DELETE FROM media_assets ... AND NOT EXISTS (SELECT 1 FROM release_media rm WHERE rm.media_id = ma.id)`) und `~:280` (gleicher Guard in `deleteAnimeOwnedMediaByPathPrefixV2`) — UNVERAENDERT. VERIFIZIERT: beide Guards verhindern das Loeschen eines noch von `release_media` referenzierten `media_assets`-Eintrags; die Junction bleibt in 106 bestehen UND wird weiter beschrieben (106-04 laesst `CreateReleaseMedia` intakt) → die Guards sind weiterhin semantisch korrekt und notwendig.
- `anime_assets.go:~539` `UNION ALL SELECT 1 FROM release_media WHERE media_id = $1` im `stillReferenced`-EXISTS — UNVERAENDERT. VERIFIZIERT: gleiche Begruendung; der Zweig steht neben anime_media/episode_media/fansub_group_media und bleibt gueltig. ACHTUNG beim Editieren von `anime_assets.go` in Task 1: diese Zeile liegt nahe am `clear legacy cover`-Block (:516-524) — sie darf NICHT mitentfernt werden.
- theme_segment_render_cache_test.go, member_profile_repository_test.go, segment_validation_test.go, app_auth_test.go, group_contributors_repository_test.go — reine String-/Negativ-Assertionen, von keinem 106-Plan beruehrt.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: anime.cover_image-Spalten-Zugriffe entfernen (Cluster E)</name>
  <files>backend/internal/repository/anime_v2.go, backend/internal/repository/anime_assets.go, backend/internal/repository/anime_schema.go, backend/internal/repository/anime_test.go, backend/internal/repository/runtime_authority_test.go</files>
  <read_first>
    - backend/internal/repository/anime_v2.go:410-450 (animeCoverImageSelectSQL + animeCoverAvailableSQL — welcher COALESCE-Zweig ist die Spalte)
    - backend/internal/repository/anime_assets.go:340-435 + :510-600 + :960-985 (syncLegacyAnimeCoverImageV2, HasCoverImage-Guard, clear legacy cover, release_media-UNION bei ~:539 als NICHT-anfassen-Marke)
    - backend/internal/repository/anime_schema.go (komplett, 77 Z. — HasCoverImage-Feld + Detektions-Case)
    - backend/internal/repository/anime_test.go:100-150 (Fragment-Assertionen `"anime.cover_image"` in TestBuildAnimeListWhereV2_... und `"NULLIF(BTRIM(anime.cover_image), '')"` in TestAnimeCoverImageSelectSQL_... — beide muessen mitgezogen werden, sonst schlaegt `go test -run Anime` fehl)
    - backend/internal/repository/runtime_authority_test.go:105-131 (die `required`-Slice von `TestAnimeAssetCompatibilityUsesV2CoverHelpersWhenLegacySlotsAreGone` — Fragment :120 faellt, :119/:121/:122/:123/:124 bleiben) UND :133-168 (release_media-Assertionen :156-167, D-07-Sperrzone)
    - backend/internal/repository/anime_assets.go:1708-1713 (der Kommentar, dessen Satzanfang von Fragment :124 verlangt wird)
    - 106-CONTEXT.md D-07 (release_media bleibt vollstaendig unberuehrt)
    - 106-RESEARCH.md Cluster E + Pitfall 3 (DTO-Feld bleibt) + §2 Befund P-2 (positive String-Assertion)
  </read_first>
  <action>In `anime_v2.go`: aus `animeCoverImageSelectSQL` (~:419) und `animeCoverAvailableSQL` (~:443) den `NULLIF(BTRIM(%s.cover_image), '')`-Zweig aus dem COALESCE entfernen; die uebrigen Quellen (`cover_resolved_url`, `poster.file_path`, Jellyfin-URL) bleiben — das JSON-DTO-Feld `cover_image` liefert weiter Werte (Pitfall 3, FE bleibt gruen). In `anime_assets.go`: arbeite die PRAEZISE CALL-SITE-LISTE aus dem `<interfaces>`-Abschnitt Zeile fuer Zeile ab (Checker-Warning 1). WICHTIG — die Zeilen :349, :386, :966 und :981 sind KEINE Legacy-Funktionen, sondern einzelne `cover_image`-Zuweisungen INNERHALB lebender `UPDATE anime SET ...`-Statements: dort faellt AUSSCHLIESSLICH die `cover_image`-Zeile weg, waehrend die Geschwister-Zuweisungen `cover_asset_id`, `cover_source`, `cover_resolved_url`, `cover_provider_key` und `updated_at` unveraendert bestehen bleiben. Vollstaendig entfernt werden nur: die Funktion `syncLegacyAnimeCoverImageV2` (:566-599) samt ihrem einzigen Aufruf (:428) und der `if schema.HasCoverImage`-Block „clear legacy anime cover image" (:516-524). Bei :349 zusaetzlich das dadurch ungenutzte `$3`-Argument und die nur dafuer existierende `coverPath`-Vorabquery (:331-340) mitentfernen, damit `go vet` sauber bleibt; bei :981 bleiben die Argumente unveraendert, weil `$2` weiterhin von `cover_resolved_url` benoetigt wird. Kommentar-Bereinigung VERENGT (P-2): in `anime_assets.go:1710-1711` NUR den Teilsatz ` and cover_image sync` streichen — der Satzanfang `V2 remains authoritative once anime_media exists` MUSS woertlich stehen bleiben, weil `runtime_authority_test.go:124` genau dieses Fragment assertiert. WICHTIG (D-07): den `UNION ALL SELECT 1 FROM release_media WHERE media_id = $1`-Zweig im `stillReferenced`-EXISTS (~:539) NICHT entfernen — er liegt unmittelbar neben dem zu entfernenden Block und bleibt gueltig, weil `release_media` in Phase 106 bestehen bleibt und weiter beschrieben wird. In `anime_schema.go`: entferne das Feld `HasCoverImage bool` (:21) und den zugehoerigen dynamischen Spalten-Detektions-Case (:62-63). NICHT anfassen: `cover_asset_id`, `cover_source`, `cover_resolved_url`, `cover_provider_key`. `go build` + `go vet` + Repository-Tests gruen halten. Datei-Zeilenlimit beachten (anime_assets.go schrumpft durch Entfernen — gut). PFLICHT: `anime_test.go` mitziehen — aus `TestBuildAnimeListWhereV2_UsesPersistedFieldsAndFilters` das Fragment `"anime.cover_image"` und aus `TestAnimeCoverImageSelectSQL_UsesProviderFallbacks` das Fragment `"NULLIF(BTRIM(anime.cover_image), '')"` entfernen; alle uebrigen Fragmente (cover_resolved_url, poster.file_path, jellyfin) bleiben als Regressionsschutz stehen. Assertionen NICHT ersatzlos loeschen, nur die cover_image-Erwartung streichen. PFLICHT (P-2, BLOCKER-Fix): `runtime_authority_test.go` mitziehen — in der `required`-Slice von `TestAnimeAssetCompatibilityUsesV2CoverHelpersWhenLegacySlotsAreGone` (:109-125) GENAU EINEN Eintrag entfernen, naemlich die Zeile `"synclegacyanimecoverimagev2(ctx, tx, animeid, mediaid, schema)",` (:120). Alle uebrigen Eintraege derselben Slice bleiben stehen (insbesondere `"removeanimeposterassetsv2(...)"`, `"loadv2animemediaidbyref(...)"`, `"removeanimemedialinksbytype(...)"`, `"upsertanimemedialink(...)"` und `"v2 remains authoritative once anime_media exists"`). STRIKT NICHT ANFASSEN in derselben Datei: die `cover_image`-Query-Fragmente :18/:21 (Zustaendigkeit 106-10, Welle 4) und `TestReleaseRuntimeAuthorityUsesReleaseNativeTables` inkl. der release_media-Assertionen :156-167 (D-07, LOCKED). Ohne diese Anpassung kann der eigene Verify-Befehl dieses Plans (`go test ./internal/repository/... -run Anime`) nicht gruen werden.</action>
  <acceptance_criteria>
    - `grep -n "cover_image" backend/internal/repository/anime_v2.go backend/internal/repository/anime_assets.go backend/internal/repository/anime_schema.go` liefert keine SQL-Spalten-Referenz mehr.
    - `grep -rn "syncLegacyAnimeCoverImageV2\|HasCoverImage" backend/ --include=*.go` liefert KEINE Treffer.
    - `grep -rn "cover_asset_id\|cover_resolved_url" backend/internal/repository/anime_assets.go` liefert weiterhin Treffer (Mechanismus intakt).
    - Geschwister-Zuweisungen erhalten (Warning-1-Gate): `grep -c "cover_asset_id" backend/internal/repository/anime_assets.go` >= 4 und `grep -c "cover_provider_key" backend/internal/repository/anime_assets.go` >= 4 — die vier UPDATE-Statements (:349 AssignManualCover, :386 ClearCoverWithResult, :966 Provider-Clear, :981 Provider-Apply) haben jeweils NUR ihre `cover_image`-Zeile verloren.
    - `grep -c "cover_source" backend/internal/repository/anime_assets.go` >= 4 (dito).
    - `grep -c "release_media" backend/internal/repository/anime_assets.go` liefert weiterhin >= 1 (D-07: UNION-Zweig NICHT mitentfernt).
    - `git diff --name-only` listet ausschliesslich anime_v2.go / anime_assets.go / anime_schema.go / anime_test.go / runtime_authority_test.go — insbesondere KEINE Aenderung an episode_version_repository_read_helpers.go, admin_content_anime_delete.go, release_assets_handler*.go, main.go.
    - P-2-Umfangsgrenze in runtime_authority_test.go: `grep -c "synclegacyanimecoverimagev2" backend/internal/repository/runtime_authority_test.go` == 0 UND `grep -c "v2 remains authoritative once anime_media exists" backend/internal/repository/runtime_authority_test.go` == 1 UND `grep -c "removeanimeposterassetsv2" backend/internal/repository/runtime_authority_test.go` == 1.
    - P-2/D-07-Sperrzone unveraendert: `grep -c "from release_media rm" backend/internal/repository/runtime_authority_test.go` == 1 UND `grep -c "join media_assets ma on ma.id = rm.media_id" backend/internal/repository/runtime_authority_test.go` == 1 UND `grep -c "episodeversionrepo.listreleaseassets" backend/internal/repository/runtime_authority_test.go` == 1.
    - P-2/106-10-Abgrenzung: `grep -c "cover_image" backend/internal/repository/runtime_authority_test.go` >= 1 (die Fragmente :18/:21 bleiben hier stehen und werden erst von 106-10 gezogen).
    - P-2/Kommentar: `grep -c "cover_image sync" backend/internal/repository/anime_assets.go` == 0 UND `grep -c "V2 remains authoritative once anime_media exists" backend/internal/repository/anime_assets.go` == 1.
    - UMFANGSGRENZE `runtime_authority_test.go` (Blocker-1-Fix 2026-07-22 — aus Task 2 HIERHER verschoben): `git diff HEAD -U0 -- backend/internal/repository/runtime_authority_test.go` zeigt GENAU EINE entfernte Zeile, und diese enthaelt `synclegacyanimecoverimagev2`; keine weitere Zeile der Datei ist hinzugefuegt oder entfernt. Diese Assertion MUSS in DIESER Task laufen: der Executor committet unmittelbar nach jedem Task (task_commit_protocol), womit der Arbeitsbaum-Diff gegen HEAD in Task 2 zwangslaeufig leer und die Assertion dort deterministisch rot waere.
    - `cd backend && go build ./... && go vet ./...` gruen.
    - `cd backend && go test ./internal/repository/... -run Anime` gruen.
    - `grep -c "anime.cover_image" backend/internal/repository/anime_test.go` == 0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./... && go test ./internal/repository/... -run Anime && ! grep -rqi "syncLegacyAnimeCoverImageV2" internal/ --include=*.go && ! grep -rq "HasCoverImage" internal/ --include=*.go && grep -q "release_media" internal/repository/anime_assets.go && grep -q "from release_media rm" internal/repository/runtime_authority_test.go && grep -q "v2 remains authoritative once anime_media exists" internal/repository/runtime_authority_test.go && grep -q "V2 remains authoritative once anime_media exists" internal/repository/anime_assets.go && ! grep -q "cover_image sync" internal/repository/anime_assets.go && [ "$(git diff HEAD -U0 -- internal/repository/runtime_authority_test.go | grep -c '^-[^-]')" = "1" ] && git diff HEAD -U0 -- internal/repository/runtime_authority_test.go | grep '^-[^-]' | grep -q "synclegacyanimecoverimagev2" && echo COVER-COL-OK</automated>
  </verify>
  <done>cover_image-Spalten-Zugriffe entfernt, DTO-Feld + cover_asset_id-Mechanismus intakt, release_media-Referenzguards unberuehrt, Build + Tests gruen.</done>
</task>

<task type="auto">
  <name>Task 2: D-07-Regressionsnachweis — release_media-Pfade nachweislich unberuehrt (scoped, P-2)</name>
  <files></files>
  <read_first>
    - backend/internal/repository/runtime_authority_test.go:133-168 (`TestReleaseRuntimeAuthorityUsesReleaseNativeTables` — positive `from release_media rm`-Assertion + Handler-Wiring-Assertion; das ist die D-07-Sperrzone)
    - backend/internal/handlers/release_assets_handler_test.go (Handler-Test, muss weiter gruen sein)
    - 106-CONTEXT.md D-07
  </read_first>
  <action>Diese Task aendert KEINEN Produktivcode und KEINE release_media-Assertion — sie belegt, dass der release_media-Lese-/Schreibpfad nach Task 1 unveraendert und funktional ist (D-07-Guard gegen versehentliche Mitnahme). Fuehre die bestehenden Guard-Tests aus (`RuntimeAuthority` im Repository-Paket, `ReleaseAssets` im Handler-Paket) und belege per grep, dass `ListReleaseAssets`, `release_assets_handler.go` und die Route `main.go` unveraendert vorhanden sind. Falls einer dieser Nachweise fehlschlaegt: Task ROT lassen und die versehentliche Aenderung in Task 1 zurueckrollen — NICHT die Assertion anpassen. Diese Task hat bewusst KEINE `files_modified`. WICHTIG — VERENGUNG GEGENUEBER DER VORFASSUNG (P-2, RESEARCH-Refresh 2026-07-22): die frueher hier geforderte Byte-Identitaet der GESAMTEN `runtime_authority_test.go` war selbstwidersprechend, weil Task 1 in derselben Datei zwingend das Fragment `synclegacyanimecoverimagev2(...)` (:120) entfernen MUSS (sonst schlaegt der eigene `-run Anime`-Verify fehl). Der D-07-Schutz gilt deshalb jetzt SCOPED: unveraendert bleiben muessen ausschliesslich die release_media-/Release-Assets-Assertionen des Tests `TestReleaseRuntimeAuthorityUsesReleaseNativeTables` (:133-168, insbesondere :156-167) — nachgewiesen per POSITIVER grep-Assertion auf die exakten Fragmente statt per `git diff --stat`. BLOCKER-1-FIX (2026-07-22): Die frueher HIER stehende Diff-Umfangsgrenze (`git diff -U0 ... zeigt genau eine entfernte Zeile`) ist nach Task 1 VERSCHOBEN. Grund: der Executor committet unmittelbar nach jedem Task, also ist der Arbeitsbaum-Diff gegen HEAD zum Zeitpunkt dieser Task leer — die Assertion waere deterministisch rot gewesen und haette den Executor per Anweisung dazu gebracht, eine korrekte, zwingend erforderliche Aenderung aus Task 1 zurueckzurollen. Hier verbleiben ausschliesslich COMMIT-UNABHAENGIGE Zustandsassertionen auf den Dateiinhalt. Zur Abgrenzung: Plan 106-10 zieht spaeter die auf die entfernten `anime.go`-Legacy-Queries gerichteten `cover_image`-Fragmente :18/:21 derselben Datei (Welle 4) — die `release_media`-Assertionen bleiben auch dort unveraendert.</action>
  <acceptance_criteria>
    - `cd backend && go test ./internal/repository/... -run RuntimeAuthority` gruen.
    - `cd backend && go test ./internal/handlers/... -run ReleaseAssets` gruen.
    - `grep -q "func (r \*EpisodeVersionRepository) ListReleaseAssets" backend/internal/repository/episode_version_repository_read_helpers.go` → Treffer.
    - `grep -q "releases/:id/assets" backend/cmd/server/main.go` → Treffer.
    - D-07-Sperrzone (statt Byte-Identitaet der ganzen Datei — P-2): `grep -c "from release_media rm" backend/internal/repository/runtime_authority_test.go` == 1, `grep -c "join media_assets ma on ma.id = rm.media_id" backend/internal/repository/runtime_authority_test.go` == 1, `grep -c "func (r \*episodeversionrepository) listreleaseassets(" backend/internal/repository/runtime_authority_test.go` == 1, `grep -c "episodeversionrepo.listreleaseassets" backend/internal/repository/runtime_authority_test.go` == 1.
    - Umfangsgrenze der in Task 1 erlaubten Aenderung: bereits in TASK 1 verifiziert (Blocker-1-Fix — dort wird der Diff vor dem Task-Commit ausgewertet). Hier nur die commit-unabhaengige Zustandsgegenprobe: `grep -c "synclegacyanimecoverimagev2" backend/internal/repository/runtime_authority_test.go` == 0 UND `grep -c "removeanimeposterassetsv2" backend/internal/repository/runtime_authority_test.go` == 1 UND `grep -c "v2 remains authoritative once anime_media exists" backend/internal/repository/runtime_authority_test.go` == 1 — die `required`-Slice hat also genau den einen Eintrag verloren und sonst nichts.
    - `grep -c "cover_image" backend/internal/repository/runtime_authority_test.go` >= 1 (die :18/:21-Fragmente stehen in Welle 2 noch — sie gehoeren 106-10).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/repository/... -run RuntimeAuthority && go test ./internal/handlers/... -run ReleaseAssets && grep -q "ListReleaseAssets" internal/repository/episode_version_repository_read_helpers.go && grep -q "releases/:id/assets" cmd/server/main.go && grep -q "from release_media rm" internal/repository/runtime_authority_test.go && grep -q "episodeversionrepo.listreleaseassets" internal/repository/runtime_authority_test.go && grep -q "join media_assets ma on ma.id = rm.media_id" internal/repository/runtime_authority_test.go && ! grep -q "synclegacyanimecoverimagev2" internal/repository/runtime_authority_test.go && grep -q "removeanimeposterassetsv2" internal/repository/runtime_authority_test.go && grep -q "v2 remains authoritative once anime_media exists" internal/repository/runtime_authority_test.go && echo D-07-INTACT-OK</automated>
  </verify>
  <done>release_media-Lesepfad, Handler, Route und die release_media-Assertionen von runtime_authority_test.go nachweislich unveraendert und gruen (D-07 gewahrt); die einzige Aenderung an der Testdatei ist nachweislich die eine syncLegacyAnimeCoverImageV2-Fragmentzeile (P-2; die Diff-Umfangsgrenze liegt per Blocker-1-Fix in Task 1, hier steht die commit-unabhaengige Zustandsgegenprobe).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Keine neue. Reine Repository-Query-/Schema-Detektions-Bereinigung; der Public-Endpoint `/releases/:id/assets` bleibt unveraendert.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-05-01 | Denial of Service | Laufzeit-SQL-Fehler durch verbleibende `anime.cover_image`-Spalten-Reads nach dem 0131-DROP | mitigate | Alle Spalten-Call-Sites (anime_v2/anime_assets/anime_schema) entfernt; `go test ./internal/repository/... -run Anime` gruen. |
| T-106-05-02 | Tampering | Versehentliches Mitentfernen des `release_media`-UNION-Zweigs (~:539) beim Bereinigen des benachbarten cover-Blocks → noch referenzierte media_assets werden loeschbar (Datenverlust) | mitigate | Explizite NICHT-anfassen-Marke in Action + `grep -q release_media anime_assets.go` als Acceptance-Criterion + Task-2-Regressionsnachweis. |
| T-106-05-03 | Tampering | Versehentliches Entfernen des lebenden cover_asset_id-Mechanismus | mitigate | Explizite „BLEIBT"-Liste + grep-Assertion auf cover_asset_id/cover_resolved_url. |
| T-106-05-SC | Tampering | Package-Installs | accept | Keine Installs. |
Kein neuer Angriffsvektor; Endpoint-Haertung erst Phase 108.
</threat_model>

<verification>
- `cd backend && go build ./... && go vet ./...` gruen.
- `cd backend && go test ./internal/repository/... ./internal/handlers/...` gruen.
- `syncLegacyAnimeCoverImageV2`/`HasCoverImage` grep leer.
- `release_media`-Lesepfad + Referenzguards nachweislich unveraendert (D-07).
</verification>

<success_criteria>
SC2-Teil erfuellt: `anime.cover_image`-Spalten-Zugriffe entfernt, abgeleitetes DTO-Feld + `cover_asset_id`-Mechanismus intakt, `release_media` in Phase 106 nachweislich unberuehrt (D-07), Build + Tests gruen.
</success_criteria>

<output>
Create `.planning/phases/106-medienkern-schema-legacy-abbau/106-05-SUMMARY.md` when done. Dokumentiere explizit, dass `release_media` gemaess D-07 nicht angefasst wurde (Verweis auf Phase 108 / ROADMAP 108-SC 3b).
</output>
