---
phase: 106-medienkern-schema-legacy-abbau
plan: 03
type: execute
wave: 2
depends_on: [106-01]
files_modified:
  - backend/database/migrations/001_create_media_tables.up.sql
  - backend/database/migrations/001_create_media_tables.down.sql
  - backend/database/migrations/README.md
  - backend/internal/handlers/episode_version_images_handler.go
  - backend/internal/repository/episode_version_image_repository.go
  - backend/internal/models/episode_version_image.go
  - backend/cmd/server/main.go
  - backend/cmd/migrate-covers/
  - scripts/report-cover-image-state.ps1
  - scripts/remediate-cover-image.ps1
autonomous: true
requirements: [SC2]
must_haves:
  truths:
    - "SC2/D-04 Cluster A: Totes UUID-Parallelschema `backend/database/migrations/001_*` (nie ausgefuehrt) ist geloescht."
    - "SC2/D-04 Cluster C: episode_version_images Go-Code (Handler, Repo-Stub, Model) und Route `/releases/:id/images` sind entfernt; Altmigrationen 0018/0046 bleiben unberuehrt (A4/Pitfall 2)."
    - "SC2/D-04 Cluster E: `cmd/migrate-covers` und `report-/remediate-cover-image.ps1` sind entfernt."
    - "Pitfall 4: Lebender Public-Pfad `GetGroupReleaseImages` (main.go:358) bleibt unberuehrt."
    - "`go build ./... && go vet ./...` bleibt gruen."
  artifacts:
    - path: "backend/cmd/server/main.go"
      provides: "Server-Wiring ohne episode_version_images-Konstruktion/-Route"
      contains: "func main"
  key_links:
    - from: "backend/cmd/server/main.go"
      to: "kein episode_version_images-Handler"
      via: "entfernte Konstruktion + Route"
      pattern: "EpisodeVersionImages"
---

<objective>
Entfernt die drei bau-unkritischsten §6-Legacy-Cluster ohne Laufzeit-SQL-Risiko: (A) das tote UUID-Parallelschema `backend/database/migrations/001_*` (nie ausgefuehrt), (C) die `episode_version_images`-Go-Strecke inkl. Route `/releases/:id/images`, und die isolierten Cover-Ops-Werkzeuge (`cmd/migrate-covers`, `report-/remediate-cover-image.ps1`). Altmigrationen 0018/0046 bleiben unberuehrt (D-01/A4).

Purpose: SC2-Anteil — grosse, saubere Loeschungen, Build gruen halten.
Output: geloeschte Legacy-Dateien, bereinigtes `main.go`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-medienkern-schema-legacy-abbau/106-RESEARCH.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-PATTERNS.md

<interfaces>
<!-- Verifizierte Call-Sites (RESEARCH Legacy-Inventar A/C/E). -->
- main.go:88  → `NewEpisodeVersionImagesHandler(...)`-Konstruktion (entfernen).
- main.go:448 → `v1.GET("/releases/:id/images", <handler>.ListReleaseImages)` (Route entfernen).
- NICHT anfassen: main.go:358 `/anime/:id/group/:groupId/releases/:releaseVersionId/images`
  (GetGroupReleaseImages, group_contributors_handler.go:250 — lebender Public-Pfad ueber release_version_media).
- Dead 001 definiert ein anderes `media_assets(id VARCHAR(36),...)` als das lebende BIGSERIAL — reine Karteileiche.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Totes UUID-Schema + Cover-Ops-Werkzeuge loeschen</name>
  <files>backend/database/migrations/001_create_media_tables.up.sql, backend/database/migrations/001_create_media_tables.down.sql, backend/database/migrations/README.md, backend/cmd/migrate-covers/, scripts/report-cover-image-state.ps1, scripts/remediate-cover-image.ps1</files>
  <read_first>
    - backend/database/migrations/README.md (bestaetigen: totes, nie gemountetes Verzeichnis)
    - 106-RESEARCH.md Abschnitt „Zwei Migrationsverzeichnisse — Achtung" (runner.go liest nur database/migrations)
  </read_first>
  <action>Loesche das gesamte tote Verzeichnis `backend/database/migrations/` (die 3 Dateien `001_create_media_tables.up.sql`, `.down.sql`, `README.md`) — es wird vom Runner nie ausgefuehrt (bestaetigt: `ResolveMigrationsDir` sucht nur `database/migrations`). Loesche das gesamte Verzeichnis `backend/cmd/migrate-covers/` (eigenes `main`, isoliert). Loesche `scripts/report-cover-image-state.ps1` und `scripts/remediate-cover-image.ps1`. Keine Referenzen auf diese Artefakte im lebenden Code (verifiziert isoliert) — `go build` bleibt gruen.</action>
  <acceptance_criteria>
    - `backend/database/migrations/` existiert nicht mehr (oder ist leer/entfernt).
    - `backend/cmd/migrate-covers/` existiert nicht mehr.
    - `scripts/report-cover-image-state.ps1` und `scripts/remediate-cover-image.ps1` existieren nicht mehr.
    - `cd backend && go build ./... && go vet ./...` gruen.
    - `database/migrations/` (die AKTIVE Kette) ist unveraendert vorhanden.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./... && [ ! -d database/migrations ] && [ ! -d cmd/migrate-covers ] && echo DELETES-OK</automated>
  </verify>
  <done>Tote 001-Migration, migrate-covers-CLI und Cover-Ops-Skripte geloescht; Build gruen; aktive Kette intakt.</done>
</task>

<task type="auto">
  <name>Task 2: episode_version_images-Strecke + Route entfernen</name>
  <files>backend/internal/handlers/episode_version_images_handler.go, backend/internal/repository/episode_version_image_repository.go, backend/internal/models/episode_version_image.go, backend/cmd/server/main.go</files>
  <read_first>
    - backend/cmd/server/main.go (Zeilen um :88 Konstruktion und :448 Route)
    - backend/internal/handlers/episode_version_images_handler.go (bestaetigen: ListReleaseImages, keine anderen Consumer)
    - 106-PATTERNS.md „NICHT anfassen" (GetGroupReleaseImages-Verwechslungsschutz)
  </read_first>
  <action>Loesche die drei Dateien `episode_version_images_handler.go`, `episode_version_image_repository.go`, `episode_version_image.go`. Entferne in `backend/cmd/server/main.go` die `NewEpisodeVersionImagesHandler`-Konstruktion (~:88) und die Routenregistrierung `v1.GET("/releases/:id/images", ...ListReleaseImages)` (~:448) sowie zugehoerige Repo-/Handler-Variablen, die dadurch ungenutzt werden. NICHT anfassen: die Route `/anime/:id/group/:groupId/releases/:releaseVersionId/images` (:358, GetGroupReleaseImages) — anderer Handler, lebender Public-Pfad. Nach Entfernen `go build` + `go vet` gruen halten (ungenutzte Imports/Variablen mit entfernen).</action>
  <acceptance_criteria>
    - Die drei episode_version_image(s)-Dateien existieren nicht mehr.
    - `grep -rn "EpisodeVersionImages\|ListReleaseImages\|episode_version_image" backend/ --include=*.go` liefert KEINE Treffer.
    - `grep -n "/releases/:id/images" backend/cmd/server/main.go` liefert KEINE Treffer; `/releases/:releaseVersionId/images` (Gruppenpfad) bleibt vorhanden.
    - `cd backend && go build ./... && go vet ./...` gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./... && ! grep -rq "episode_version_image\|ListReleaseImages" internal/ cmd/ --include=*.go && echo CLUSTER-C-OK</automated>
  </verify>
  <done>episode_version_images Go-Code + Route entfernt, Gruppen-Bilderpfad intakt, Build gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Keine. Entfernung toter/legacy Server-Verdrahtung; keine neue Input-/Auth-Flaeche.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-03-01 | Denial of Service | Versehentliches Entfernen des lebenden Gruppen-Bilderpfads | mitigate | grep-Assertion trennt `/releases/:id/images` (entfernt) von `/releases/:releaseVersionId/images` (bleibt). |
| T-106-03-SC | Tampering | Package-Installs | accept | Keine Installs. |
Kein neuer Angriffsvektor; Endpoint-Haertung erst Phase 108.
</threat_model>

<verification>
- `cd backend && go build ./... && go vet ./...` gruen.
- grep leer fuer `episode_version_image`, `ListReleaseImages`, `EpisodeVersionImages`.
- Aktive `database/migrations/`-Kette unveraendert.
</verification>

<success_criteria>
SC2-Teil erfuellt: Cluster A + C + isolierte Cover-Ops-Werkzeuge entfernt, Build gruen, keine Rest-Referenzen.
</success_criteria>

<output>
Create `.planning/phases/106-medienkern-schema-legacy-abbau/106-03-SUMMARY.md` when done.
</output>
