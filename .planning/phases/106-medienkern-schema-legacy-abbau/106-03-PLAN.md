---
phase: 106-medienkern-schema-legacy-abbau
plan: 03
type: execute
wave: 2
depends_on: [106-01]
files_modified:
  - backend/database/migrations/001_create_media_tables.up.sql
  - backend/database/migrations/001_create_media_tables.down.sql
  - backend/database/migrations/README.md
  - backend/internal/handlers/episode_version_images_handler.go
  - backend/internal/repository/episode_version_image_repository.go
  - backend/internal/models/episode_version_image.go
  - backend/cmd/server/main.go
  - backend/cmd/migrate-covers/
  - scripts/report-cover-image-state.ps1
  - scripts/remediate-cover-image.ps1
  - shared/contracts/openapi.yaml
autonomous: true
requirements: [SC2, SC4]
must_haves:
  truths:
    - "SC2/D-04 Cluster A: Totes UUID-Parallelschema `backend/database/migrations/001_*` ist geloescht. VERSCHAERFUNG (RESEARCH-Refresh §1 Item A1): das Verzeichnis war nicht nur Karteileiche, sondern ueber `ResolveMigrationsDir` (`runner.go:52-75`, erster Kandidat `./database/migrations` RELATIV ZUM CWD) bei `cd backend && go run ./cmd/migrate up` versehentlich ausfuehrbar — die Loeschung beseitigt eine reale Fussangel."
    - "SC2/D-04 Cluster C: episode_version_images Go-Code (Handler, Repo-Stub, Model) und Route `/releases/:id/images` sind entfernt; Altmigrationen 0018/0046 bleiben unberuehrt (D-01/A4)."
    - "SC2/D-04 Cluster E: `cmd/migrate-covers` und `report-/remediate-cover-image.ps1` sind entfernt."
    - "P-3 (RESEARCH-Refresh 2026-07-22, BLOCKER behoben; PO-Entscheid: Contract in 106 mitziehen): Mit der Route faellt ihre OpenAPI-Beschreibung. `shared/contracts/openapi.yaml` verliert den Pfad `/api/v1/releases/{releaseId}/images` (:6522, inkl. `operationId: listReleaseImages` :6526 und dem `$ref` :6563) sowie die beiden ausschliesslich dort referenzierten Schemas `EpisodeVersionImage` (:12612) und `EpisodeVersionImagesResponse` (:12655). Ohne diesen Schritt kann das SC4-Gate in 106-08 per Konstruktion nie Exit 0 liefern, weil `shared/contracts` SC4-Scan-Wurzel ist und PowerShell `Select-String` case-insensitiv auf `listReleaseImages` matcht."
    - "P-3-Referenz-Guard (verifiziert 2026-07-22): `EpisodeVersionImage`/`EpisodeVersionImagesResponse` haben repo-weit in `shared/contracts/**` GENAU vier Vorkommen — :6563 ($ref im entfallenden Pfad), :12612 (Definition), :12655 (Definition), :12663 ($ref der einen Definition auf die andere). Kein anderer Pfad, kein anderes Contract-File referenziert sie. Beide Schemas sind damit vollstaendig verwaist und werden komplett entfernt."
    - "P-3-Abgrenzung: Der unmittelbar folgende Pfad `/api/v1/releases/{releaseId}/assets` (`operationId: listReleaseAssets`) sowie die Schemas `ReleaseAsset`/`ReleaseAssetsData` bleiben VOLLSTAENDIG unveraendert — sie beschreiben den lebenden D-07-Endpoint."
    - "P-6 (RESEARCH-Refresh 2026-07-22): `frontend/src/app/episodes/[id]/components/ScreenshotGallery/ScreenshotGallery.tsx:40` ist ein live gemounteter Consumer der hier entfernten Route (`page.tsx:205` rendert `<ScreenshotGallery releaseId=…/>`). Er wird in 106 BEWUSST NICHT angefasst (D-03: nur build-breaking FE; Loeschung der Komponente = Phase 109). Verhaltensneutral belegt: `EpisodeVersionImageRepository.GetByEpisodeVersionID` ist ein reiner Stub, der ausnahmslos `phase20ReleaseImportDeferred(...)` zurueckgibt → der Endpoint antwortet HEUTE SCHON bei JEDEM Aufruf mit HTTP 500; die Komponente setzt daraufhin ihren Fehlerzustand. Nach Entfernen der Route: HTTP 404 → `response.ok === false` → derselbe Fehlerzustand. Sichtbarer Effekt unveraendert."
    - "P-6-Entscheid (Planner, 2026-07-22): Route faellt in 106, Komponente bleibt bis 109. Die beiden vom Reviewer angebotenen Alternativen kollidieren jeweils mit einer LOCKED-Entscheidung: „Route bis 109 behalten\" widerspricht D-04 (Backend-Legacy-Abbau vollstaendig in 106, ausgenommen ist per D-07 ausschliesslich `release_media`) und wuerde die SC4-Terme `ListReleaseImages`/`EpisodeVersionImage` unzuordenbar machen; „Komponente in 106 mitloeschen\" widerspricht D-03 (nur build-breaking FE in 106, `ScreenshotGallery` ist nicht build-breaking). Die gewaehlte Variante verletzt keine der beiden und ist zusaetzlich verhaltensneutral — anders als bei `release_media`/D-07, wo der Endpoint echte Daten lieferte."
    - "Pitfall 4: Lebender Public-Pfad `GetGroupReleaseImages` (main.go:358, `/anime/:id/group/:groupId/releases/:releaseVersionId/images`) bleibt unberuehrt — anderer Handler, andere Route, andere Datenquelle."
    - "`go build ./... && go vet ./...` bleibt gruen."
  artifacts:
    - path: "backend/cmd/server/main.go"
      provides: "Server-Wiring ohne episode_version_images-Konstruktion/-Route"
      contains: "func main"
    - path: "shared/contracts/openapi.yaml"
      provides: "Contract ohne die entfernte Release-Images-Strecke, mit unveraendertem Release-Assets-Pfad"
      contains: "listReleaseAssets"
  key_links:
    - from: "backend/cmd/server/main.go"
      to: "kein episode_version_images-Handler"
      via: "entfernte Konstruktion + Route"
      pattern: "EpisodeVersionImages"
    - from: "shared/contracts/openapi.yaml"
      to: "entfernte Route /api/v1/releases/{releaseId}/images"
      via: "Contract-Synchronisierung (P-3, Pitfall 8)"
      pattern: "listReleaseImages"
---

<objective>
Entfernt die drei bau-unkritischsten §6-Legacy-Cluster ohne Laufzeit-SQL-Risiko: (A) das tote UUID-Parallelschema `backend/database/migrations/001_*`, (C) die `episode_version_images`-Go-Strecke inkl. Route `/releases/:id/images` UND ihrer OpenAPI-Beschreibung, und die isolierten Cover-Ops-Werkzeuge (`cmd/migrate-covers`, `report-/remediate-cover-image.ps1`). Altmigrationen 0018/0046 bleiben unberuehrt (D-01/A4).

REVISION (RESEARCH-Refresh 2026-07-22, Befunde P-3 + P-6):
- P-3 (BLOCKER, PO-Entscheid „Option A"): `shared/contracts/openapi.yaml` kommt in `files_modified`. Ein Contract, der einen entfernten Endpoint beschreibt, ist ein Drift-Defekt (Pitfall 8) — und blockiert hier zusaetzlich hart das SC4-Gate, weil `shared/contracts` Scan-Wurzel ist. `shared/contracts` wird ausdruecklich NICHT aus dem SC4-Scope genommen (sonst entfiele der Contract-Drift-Nachweis fuer die hier entfernte Release-Images-Strecke).
- P-6: `ScreenshotGallery` ist als bekannter, bewusst bis Phase 109 deferrierter Consumer dokumentiert (siehe must_haves). Keine FE-Aenderung in diesem Plan.

Purpose: SC2-Anteil — grosse, saubere Loeschungen inkl. Contract-Synchronisierung, Build gruen halten; SC4-Erreichbarkeit herstellen.
Output: geloeschte Legacy-Dateien, bereinigtes `main.go`, contract-synchrones `openapi.yaml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-medienkern-schema-legacy-abbau/106-RESEARCH.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-PATTERNS.md
@.planning/phases/106-medienkern-schema-legacy-abbau/106-CONTEXT.md

<interfaces>
<!-- Verifizierte Call-Sites (RESEARCH-Refresh 2026-07-22, §1 Item A1/B/I + §2 P-3/P-6). -->
GO-SEITE:
- main.go:87  → `NewEpisodeVersionImageRepository(...)`-Konstruktion (entfernen — die Vorfassung nannte nur :88).
- main.go:88  → `NewEpisodeVersionImagesHandler(...)`-Konstruktion (entfernen).
- main.go:448 → `v1.GET("/releases/:id/images", episodeVersionImagesHandler.ListReleaseImages)` (Route entfernen).
- NICHT anfassen: main.go:358 `/anime/:id/group/:groupId/releases/:releaseVersionId/images`
  (GetGroupReleaseImages, group_contributors_handler.go:250 — lebender Public-Pfad ueber release_version_media).
- NICHT anfassen: main.go `/releases/:id/assets` (ListReleaseAssets) — D-07-Endpoint, bleibt vollstaendig.
- Dead 001 definiert ein anderes `media_assets(id VARCHAR(36),...)` als das lebende BIGSERIAL — reine Karteileiche,
  ueber CWD-relative Migrationsaufloesung aber versehentlich ausfuehrbar (RESEARCH §1 A1 / §6 Runner-Reihenfolge).
- `backend/cmd/migrate-covers/` enthaelt VIER Dateien (`main.go`, `README.md`, `INVENTORY.md`, `test-migration.sh`) —
  das Verzeichnis-Delete deckt alle ab (auch die im Plan nicht einzeln gelisteten `.md`/`.sh`).

CONTRACT-SEITE (P-3) — exakte Blockgrenzen in `shared/contracts/openapi.yaml`:
- Pfad-Block `  /api/v1/releases/{releaseId}/images:` beginnt bei :6522 und endet unmittelbar VOR
  `  /api/v1/releases/{releaseId}/assets:` (~:6582). Er enthaelt `operationId: listReleaseImages` (:6526),
  die Parameter `releaseId`/`cursor`/`limit` und den `$ref` auf `EpisodeVersionImagesResponse` (:6563).
  → GESAMTER Pfad-Block entfaellt.
- Schema `    EpisodeVersionImage:` beginnt bei :12612 und endet unmittelbar VOR `    EpisodeVersionImagesResponse:` (:12655).
  → entfaellt (fuehrt u.a. das per §8 gestrichene Feld `caption`).
- Schema `    EpisodeVersionImagesResponse:` beginnt bei :12655 und endet unmittelbar VOR `    ReleaseAsset:` (~:12670).
  → entfaellt.
- REFERENZ-GUARD (verifiziert per `grep -rn "EpisodeVersionImage\|listReleaseImages\|releases/{releaseId}/images" shared/contracts/`):
  GENAU 6 Treffer, alle in `openapi.yaml` und alle innerhalb der drei oben genannten Bloecke
  (:6522, :6526, :6563, :12612, :12655, :12663). Kein weiteres Contract-File (`episode-versions.yaml` u.a.)
  referenziert sie. Beide Schemas sind nach Entfernen des Pfads vollstaendig verwaist.
- STRIKT BEHALTEN: `  /api/v1/releases/{releaseId}/assets:` (~:6582, `operationId: listReleaseAssets`),
  Schemas `ReleaseAsset` (~:12670) und `ReleaseAssetsData` — D-07-Endpoint.
- STRIKT NICHT ANFASSEN in diesem Plan UND in ganz Phase 106 (D-08, PO-Entscheid 2026-07-22):
  `shared/contracts/openapi.yaml:1475/:1481` (`asset_lifecycle.*`-Fehlercodes) und `:9058` (`provisioning`).
  Der frueher hier zustaendige Plan 106-06 ist ERSATZLOS ENTFALLEN — die `asset_lifecycle`-Entfernung wandert nach
  Phase 107 (dort macht der hash-basierte `storage_key` die Ordnerprovisionierung erst tatsaechlich ueberfluessig).
  Diese Contract-Stellen bleiben also nicht nur waehrend dieses Plans, sondern ueber die GESAMTE Phase 106 unveraendert;
  das `provisioning`-Feld bleibt ueber alle drei Schichten konsistent (`backend/internal/models/media_upload.go:43` →
  `shared/contracts/openapi.yaml:9058` → `frontend/src/types/admin.ts:691`). Dieser Plan editiert AUSSCHLIESSLICH die drei
  Release-Images-Bloecke.

FRONTEND (P-6) — BEWUSST NICHT IN DIESEM PLAN:
- `frontend/src/app/episodes/[id]/page.tsx:10` (Import) + `:205` (`<ScreenshotGallery releaseId={activeReleaseID} />`).
- `frontend/src/app/episodes/[id]/components/ScreenshotGallery/ScreenshotGallery.tsx:40`
  `fetch(resolvePublicApiUrl(\`/api/v1/releases/${releaseId}/images?…\`))` → bei `!response.ok` wird
  `Screenshots konnten nicht geladen werden.` als Fehlerzustand gesetzt (Zeile :42). Heute 500, danach 404 —
  identischer Zweig, identische Darstellung.
- `frontend/src/types/screenshotImage.ts` (`ScreenshotImage`, `ScreenshotImagesResponse`) — eigene FE-Typen,
  KEIN Bezug zum Contract-Schema-Namen `EpisodeVersionImage`; der SC4-Term `EpisodeVersionImage` trifft `frontend/src` daher nicht.
- Der SC4-Term `/releases/:id/images` (Gin-Syntax mit Doppelpunkt) matcht das FE-Template-Literal
  `/api/v1/releases/${releaseId}/images` NICHT → kein False-Fail.
- Entfernung von Komponente, Typen und Mount: Phase 109 (D-03, Deferred Ideas in 106-CONTEXT.md).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Totes UUID-Schema + Cover-Ops-Werkzeuge loeschen</name>
  <files>backend/database/migrations/001_create_media_tables.up.sql, backend/database/migrations/001_create_media_tables.down.sql, backend/database/migrations/README.md, backend/cmd/migrate-covers/, scripts/report-cover-image-state.ps1, scripts/remediate-cover-image.ps1</files>
  <read_first>
    - backend/database/migrations/README.md (bestaetigen: totes, nie gemountetes Verzeichnis)
    - backend/internal/migrations/runner.go:52-75 (`ResolveMigrationsDir` — Kandidatenreihenfolge, CWD-Abhaengigkeit)
    - 106-RESEARCH.md §6 „Migrations-Runner — Aufloesungsreihenfolge" + Pitfall 4 (zwei Migrationsverzeichnisse)
  </read_first>
  <action>Loesche das gesamte tote Verzeichnis `backend/database/migrations/` (die 3 Dateien `001_create_media_tables.up.sql`, `.down.sql`, `README.md`). Es wird im Docker-Pfad nie ausgefuehrt (`docker-compose.yml:151` mountet `./database/migrations`), waere aber bei einem lokalen `cd backend && go run ./cmd/migrate up` der ERSTE Kandidat von `ResolveMigrationsDir` und wuerde dann das tote UUID-Schema anwenden — die Loeschung schliesst diese Fussangel. Loesche das gesamte Verzeichnis `backend/cmd/migrate-covers/` (4 Dateien: `main.go`, `README.md`, `INVENTORY.md`, `test-migration.sh`; eigenes `main`, isoliert, kein Import aus dem Server-Binary). Loesche `scripts/report-cover-image-state.ps1` und `scripts/remediate-cover-image.ps1`. Keine Referenzen auf diese Artefakte im lebenden Code (verifiziert isoliert) — `go build` bleibt gruen. STRIKT NICHT ANFASSEN: die AKTIVE Kette `database/migrations/` im Repo-Root.</action>
  <acceptance_criteria>
    - `backend/database/migrations/` existiert nicht mehr.
    - `backend/cmd/migrate-covers/` existiert nicht mehr (inkl. `test-migration.sh` und den beiden `.md`).
    - `scripts/report-cover-image-state.ps1` und `scripts/remediate-cover-image.ps1` existieren nicht mehr.
    - `cd backend && go build ./... && go vet ./...` gruen.
    - `database/migrations/` (die AKTIVE Kette) ist unveraendert vorhanden — `ls database/migrations/*.sql | wc -l` > 0 und `git diff --name-only database/migrations/` ist LEER.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./... && [ ! -d database/migrations ] && [ ! -d cmd/migrate-covers ] && [ ! -f ../scripts/remediate-cover-image.ps1 ] && [ -d ../database/migrations ] && [ -z "$(git diff --name-only -- ../database/migrations/)" ] && echo DELETES-OK</automated>
  </verify>
  <done>Tote 001-Migration (inkl. CWD-Fussangel), migrate-covers-CLI und Cover-Ops-Skripte geloescht; Build gruen; aktive Kette nachweislich intakt.</done>
</task>

<task type="auto">
  <name>Task 2: episode_version_images-Strecke + Route entfernen</name>
  <files>backend/internal/handlers/episode_version_images_handler.go, backend/internal/repository/episode_version_image_repository.go, backend/internal/models/episode_version_image.go, backend/cmd/server/main.go</files>
  <read_first>
    - backend/cmd/server/main.go (Zeilen um :87 Repo-Konstruktion, :88 Handler-Konstruktion und :448 Route; zusaetzlich :358 und die `/releases/:id/assets`-Route als Schutzobjekte)
    - backend/internal/handlers/episode_version_images_handler.go (bestaetigen: `ListReleaseImages` ist der einzige registrierte Einstieg)
    - backend/internal/repository/episode_version_image_repository.go (bestaetigen: alle vier Methoden geben `phase20ReleaseImportDeferred(...)` zurueck → Endpoint ist heute schon ein Dauer-500)
    - 106-PATTERNS.md „NICHT anfassen" (GetGroupReleaseImages-Verwechslungsschutz)
    - 106-RESEARCH.md §2 P-6 (ScreenshotGallery als bewusst deferrierter Consumer)
  </read_first>
  <action>Loesche die drei Dateien `episode_version_images_handler.go`, `episode_version_image_repository.go`, `episode_version_image.go`. Entferne in `backend/cmd/server/main.go` die `NewEpisodeVersionImageRepository`-Konstruktion (~:87), die `NewEpisodeVersionImagesHandler`-Konstruktion (~:88) und die Routenregistrierung `v1.GET("/releases/:id/images", ...ListReleaseImages)` (~:448) sowie zugehoerige Variablen, die dadurch ungenutzt werden. NICHT anfassen: die Route `/anime/:id/group/:groupId/releases/:releaseVersionId/images` (:358, GetGroupReleaseImages — anderer Handler, lebender Public-Pfad) und die Route `/releases/:id/assets` (ListReleaseAssets — D-07). NICHT anfassen (D-03/P-6): `frontend/src/app/episodes/[id]/page.tsx` und `ScreenshotGallery.tsx` — der Consumer bleibt bewusst bis Phase 109 stehen; sein sichtbares Verhalten aendert sich nicht (heute HTTP 500 aus dem Repo-Stub, danach HTTP 404 — beide laufen in denselben `!response.ok`-Fehlerzweig). Nach Entfernen `go build` + `go vet` gruen halten (ungenutzte Imports/Variablen mit entfernen).</action>
  <acceptance_criteria>
    - Die drei episode_version_image(s)-Dateien existieren nicht mehr.
    - `grep -rni "EpisodeVersionImage\|ListReleaseImages\|episode_version_image" backend/internal backend/cmd --include=*.go` liefert KEINE Treffer (case-insensitiv, weil das SC4-Gate case-insensitiv laeuft).
    - `grep -n "/releases/:id/images" backend/cmd/server/main.go` liefert KEINE Treffer; `/releases/:releaseVersionId/images` (Gruppenpfad) und `/releases/:id/assets` (D-07) bleiben vorhanden.
    - D-03/P-6-Grenze: `git diff --name-only frontend/` ist LEER (kein FE-Eingriff in diesem Plan).
    - `cd backend && go build ./... && go vet ./...` gruen.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./... && ! grep -rqi "episode_version_image\|ListReleaseImages\|EpisodeVersionImage" internal/ cmd/ --include=*.go && grep -q "releases/:releaseVersionId/images" cmd/server/main.go && grep -q "releases/:id/assets" cmd/server/main.go && [ -z "$(git diff --name-only -- ../frontend/)" ] && echo CLUSTER-C-OK</automated>
  </verify>
  <done>episode_version_images Go-Code + Route entfernt, Gruppen-Bilderpfad und D-07-Assets-Route intakt, Frontend unberuehrt, Build gruen.</done>
</task>

<task type="auto">
  <name>Task 3: OpenAPI-Contract nachziehen — Release-Images-Strecke entfernen (P-3)</name>
  <files>shared/contracts/openapi.yaml</files>
  <read_first>
    - shared/contracts/openapi.yaml:6515-6600 (Pfad-Block `/api/v1/releases/{releaseId}/images` und die unmittelbar folgende `/assets`-Route als Abgrenzung)
    - shared/contracts/openapi.yaml:12605-12680 (Schemas `EpisodeVersionImage`, `EpisodeVersionImagesResponse` und das folgende `ReleaseAsset` als Abgrenzung)
    - 106-RESEARCH.md §2 P-3 + Pitfall 8 (API-Contract-Drift)
    - 106-03-PLAN.md `<interfaces>`-Abschnitt „CONTRACT-SEITE (P-3)" (exakte Blockgrenzen + Referenz-Guard)
  </read_first>
  <action>SCHRITT 0 — REFERENZ-GUARD VOR DEM LOESCHEN: Fuehre `grep -rn "EpisodeVersionImage\|EpisodeVersionImagesResponse\|listReleaseImages\|releases/{releaseId}/images" shared/contracts/` aus. Erwartet werden GENAU sechs Treffer, alle in `openapi.yaml` (:6522, :6526, :6563, :12612, :12655, :12663). Falls ein Treffer AUSSERHALB dieser drei Bloecke oder in einem anderen Contract-File liegt, ist das Schema NICHT verwaist: dann NUR den Pfad-Block plus die nachweislich unreferenzierten Schemas entfernen, das geteilte Schema stehen lassen und die Abweichung im SUMMARY explizit begruenden. SCHRITT 1: Entferne den kompletten Pfad-Block `  /api/v1/releases/{releaseId}/images:` (beginnt :6522, endet unmittelbar VOR `  /api/v1/releases/{releaseId}/assets:`) inklusive `operationId: listReleaseImages`, aller Parameter und aller Response-Definitionen. SCHRITT 2: Entferne unter `components.schemas` die beiden nun verwaisten Definitionen `    EpisodeVersionImage:` (beginnt :12612) und `    EpisodeVersionImagesResponse:` (beginnt :12655, endet unmittelbar VOR `    ReleaseAsset:`). Achte auf die YAML-Einrueckungsebene (4 Leerzeichen fuer Schema-Namen, 2 fuer Pfade) — es darf keine Leerzeile/Fragment-Zeile zurueckbleiben. STRIKT NICHT ANFASSEN: der Pfad `/api/v1/releases/{releaseId}/assets` (`operationId: listReleaseAssets`) und die Schemas `ReleaseAsset`/`ReleaseAssetsData` (D-07-Endpoint); ebenso NICHT die `asset_lifecycle`-Fehlercodes bei :1475/:1481 und die `provisioning`-Property bei :9058 — diese bleiben unter D-08 in der GESAMTEN Phase 106 unveraendert (Entfernung erst Phase 107; Plan 106-06 ist ersatzlos entfallen). SCHRITT 3: Belege per YAML-Parse, dass die Datei nach dem Eingriff weiterhin gueltig ist.</action>
  <acceptance_criteria>
    - `grep -rc "EpisodeVersionImage\|listReleaseImages\|releases/{releaseId}/images" shared/contracts/openapi.yaml` == 0 (auch case-insensitiv: `grep -ric` == 0).
    - D-07-Abgrenzung: `grep -c "listReleaseAssets" shared/contracts/openapi.yaml` >= 1 UND `grep -c "releases/{releaseId}/assets" shared/contracts/openapi.yaml` >= 1 UND `grep -c "ReleaseAsset:" shared/contracts/openapi.yaml` >= 1.
    - D-08-Drop-Guard: `grep -c "asset_lifecycle" shared/contracts/openapi.yaml` == 2 (unveraendert — die Codes bleiben in Phase 106 produktiv, Entfernung erst Phase 107) UND `grep -c "provisioning" shared/contracts/openapi.yaml` >= 1 (Feld bleibt ueber alle drei Schichten konsistent).
    - YAML bleibt parsbar: `python -c "import yaml,sys; yaml.safe_load(open('shared/contracts/openapi.yaml', encoding='utf-8')); print('YAML-OK')"` (oder ein aequivalenter Parser) laeuft fehlerfrei durch.
    - Keine verwaisten `$ref`: `grep -n '\$ref: "#/components/schemas/EpisodeVersionImage' shared/contracts/openapi.yaml` liefert KEINE Treffer.
    - `git diff --name-only shared/contracts/` listet ausschliesslich `shared/contracts/openapi.yaml`.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import yaml;yaml.safe_load(open('shared/contracts/openapi.yaml',encoding='utf-8'));print('YAML-OK')" && ! grep -qi "EpisodeVersionImage\|listReleaseImages\|releases/{releaseId}/images" shared/contracts/openapi.yaml && grep -q "listReleaseAssets" shared/contracts/openapi.yaml && [ "$(grep -c asset_lifecycle shared/contracts/openapi.yaml)" = "2" ] && echo CONTRACT-SYNC-OK</automated>
  </verify>
  <done>Contract beschreibt keinen entfernten Endpoint mehr; Release-Images-Pfad und die zwei verwaisten Schemas sind weg; D-07-Assets-Pfad und die D-08-Sperrzone (asset_lifecycle/provisioning) unveraendert; YAML valide; SC4-Exit-0 wird dadurch erreichbar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Keine. Entfernung toter/legacy Server-Verdrahtung + der zugehoerigen Contract-Beschreibung; keine neue Input-/Auth-Flaeche.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-03-01 | Denial of Service | Versehentliches Entfernen des lebenden Gruppen-Bilderpfads | mitigate | grep-Assertion trennt `/releases/:id/images` (entfernt) von `/releases/:releaseVersionId/images` (bleibt); positive Assertion auf den Gruppenpfad. |
| T-106-03-02 | Tampering | Versehentliches Entfernen des D-07-Endpoints `/releases/:id/assets` oder seiner Contract-Beschreibung, weil der Pfad-Block direkt daneben liegt | mitigate | Blockgrenzen im `<interfaces>`-Abschnitt zeilengenau benannt; positive Acceptance-Criteria auf `listReleaseAssets`, `releases/{releaseId}/assets`, `ReleaseAsset:` und die Go-Route. |
| T-106-03-03 | Repudiation | API-Contract-Drift: entfernter Endpoint bleibt in `openapi.yaml` beschrieben → falsche Client-Erwartung und unerreichbares SC4-Gate (Pitfall 8) | mitigate | Task 3 zieht den Contract synchron nach; grep-Acceptance == 0 fuer alle drei Bezeichner; YAML-Parse als Struktur-Gate. |
| T-106-03-04 | Tampering | Entfernen eines noch von anderen Pfaden genutzten Schemas (`EpisodeVersionImage`) → gebrochener Contract | mitigate | SCHRITT 0 Referenz-Guard mit erwarteter Trefferzahl 6 und explizitem Fallback („nur unreferenzierte Schemas entfernen, Abweichung begruenden"); Acceptance prueft zusaetzlich auf verwaiste `$ref`. |
| T-106-03-05 | Denial of Service | Sichtbarer FE-Bruch durch Wegfall der Route unter dem live gemounteten `ScreenshotGallery` (P-6) | accept | Verhaltensneutral belegt: das Repo ist ein Dauer-Stub (`phase20ReleaseImportDeferred` in allen vier Methoden) → heute HTTP 500, danach HTTP 404, beide im selben `!response.ok`-Zweig der Komponente. Komponentenentfernung ist Phase-109-Scope (D-03). Live-Bestaetigung im 106-08-Checkpoint. |
| T-106-03-SC | Tampering | Package-Installs | accept | Keine Installs. |
Kein neuer Angriffsvektor; Endpoint-Haertung erst Phase 108.
</threat_model>

<verification>
- `cd backend && go build ./... && go vet ./...` gruen.
- grep (case-insensitiv) leer fuer `episode_version_image`, `EpisodeVersionImage`, `ListReleaseImages` in `backend/internal`, `backend/cmd` UND `shared/contracts`.
- `openapi.yaml` parst als YAML; `listReleaseAssets`/`ReleaseAsset` und die D-08-Sperrzone (`asset_lifecycle` == 2 Treffer, `provisioning`) unveraendert.
- Aktive `database/migrations/`-Kette unveraendert; `frontend/` in diesem Plan unveraendert (D-03/P-6).
</verification>

<success_criteria>
SC2-Teil erfuellt: Cluster A + C + isolierte Cover-Ops-Werkzeuge entfernt, Build gruen, keine Rest-Referenzen in Go.
SC4-Teil erfuellt: Der Contract traegt die Release-Images-Strecke nicht mehr — damit sind die SC4-Terme `ListReleaseImages` und `EpisodeVersionImage` in allen vier Scan-Wurzeln vollstaendig einem Plan zugeordnet und Exit 0 wird erreichbar (P-3).
</success_criteria>

<output>
Create `.planning/phases/106-medienkern-schema-legacy-abbau/106-03-SUMMARY.md` when done. Dokumentiere: (a) das Ergebnis des Referenz-Guards aus Task 3 Schritt 0, (b) dass `ScreenshotGallery` als bewusst deferrierter Consumer stehen bleibt (P-6, D-03 → Phase 109) und warum das verhaltensneutral ist, (c) dass `/releases/:id/assets` und die `asset_lifecycle`-/`provisioning`-Contract-Stellen unangetastet blieben (D-07 bzw. D-08 → Phase 107).
</output>
