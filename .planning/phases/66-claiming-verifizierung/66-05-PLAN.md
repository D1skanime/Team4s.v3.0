---
phase: 66-claiming-verifizierung
plan: 05
type: execute
wave: 5
depends_on:
  - 66-04
files_modified:
  - frontend/src/app/me/profile/components/ClaimStatusCard.tsx
  - frontend/src/app/me/profile/page.tsx
  - frontend/src/components/profile/VerifiedBadge.tsx
  - frontend/src/components/profile/MemberProfileHero.tsx
  - frontend/src/components/profile/MemberRoleTimeline.tsx
  - frontend/src/app/members/[slug]/page.tsx
autonomous: true
requirements:
  - P66-SC1
  - P66-SC3
must_haves:
  truths:
    - "me/profile zeigt noindex-Toggle (Checkbox) und Claim-Status-Karte in der Side-Column"
    - "noindex-Toggle PATCH optimistisch; bei Fehler zurücksetzen + errorBox"
    - "Claim-Status-Karte zeigt EmptyState, Ausstehend-Badge oder Verifiziert-Badge je nach claim_status"
    - "VerifiedBadge erscheint neben Member-Namen im öffentlichen Profil wenn is_verified=true"
    - "MemberRoleTimeline zeigt (historisch) nur wenn !is_verified"
    - "members/[slug]/page.tsx exportiert generateMetadata() mit robots: {index: !noindex}"
  artifacts:
    - path: frontend/src/app/me/profile/components/ClaimStatusCard.tsx
      provides: "noindex-Toggle + Claim-Status-Anzeige Komponente"
      contains: "ClaimStatusCard"
    - path: frontend/src/components/profile/VerifiedBadge.tsx
      provides: "Häkchen-Badge Komponente (CheckCircle + Verifiziert)"
      contains: "VerifiedBadge"
    - path: frontend/src/app/members/[slug]/page.tsx
      provides: "generateMetadata mit robots-Meta-Tag"
      contains: "generateMetadata"
  key_links:
    - from: "me/profile/page.tsx"
      to: "ClaimStatusCard"
      via: "import { ClaimStatusCard } from './components/ClaimStatusCard'"
      pattern: "ClaimStatusCard"
    - from: "MemberProfileHero.tsx"
      to: "VerifiedBadge"
      via: "import { VerifiedBadge } from './VerifiedBadge'"
      pattern: "VerifiedBadge"
    - from: "members/[slug]/page.tsx generateMetadata"
      to: "profile.noindex"
      via: "getMemberProfile(slug) → data.noindex → robots"
      pattern: "generateMetadata"
---

<objective>
Eigenes Profil (`me/profile`) um noindex-Toggle und Claim-Status-Karte erweitern. VerifiedBadge-Komponente anlegen und in öffentlichem Profil einbinden. `(historisch)`-Logik in MemberRoleTimeline an is_verified koppeln. `generateMetadata()` in members/[slug]/page.tsx für robots-Meta-Tag ergänzen.

Purpose: Diese Plan liefert alle P66-SC3-sichtbaren Änderungen im Profil-Bereich: noindex-Steuerung, verified-Badge und robots-Meta-Tag.
Output: 6 Dateien (3 neue/erweiterte Komponenten + 3 Page-Erweiterungen).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/66-claiming-verifizierung/66-CONTEXT.md
@.planning/phases/66-claiming-verifizierung/66-UI-SPEC.md
@.planning/phases/66-claiming-verifizierung/66-PATTERNS.md
@.planning/phases/66-claiming-verifizierung/66-04-SUMMARY.md

<interfaces>
<!-- Aus frontend/src/types/profile.ts (nach Plan 04 erweitert) -->
PublicMemberProfileData { ..., is_verified: boolean, noindex: boolean }
MemberProfileData { ..., is_verified: boolean, noindex: boolean, claim_status?: 'pending'|'verified'|'rejected'|null, claim_member_nick?: string|null }
MemberSearchResult, MemberClaimRow (neu)

<!-- Aus frontend/src/lib/api.ts (nach Plan 04) -->
getMyMemberClaim, submitMemberClaim, searchHistoricalMembers: verfügbar
Für noindex-PATCH: bestehender PUT /api/v1/me/profile — in UpdateOwnProfile-Payload noindex-Feld hinzufügen

<!-- Aus frontend/src/app/me/profile/components/VisibilityCard.tsx — exaktes Analog für ClaimStatusCard -->
Props-Muster: { value, disabled, onChange: (updater) => void }
CSS: styles.radioGroup (fieldset), styles.radioCard (label), styles.mutedText
Checkbox-CSS: styles.checkboxControl

<!-- Aus frontend/src/app/me/profile/page.tsx -->
Side-Column: <aside className={styles.sideColumn}> ... </aside>
handleSubmit-Muster: setIsSaving(true) → API-Call → catch → setErrorMessage → finally setIsSaving(false)
Bestehende Card-Komponente: <Card variant="section"> ... </Card>
Noindex-Update: PUT /api/v1/me/profile kann noindex-Feld integrieren — ODER separater PATCH-Aufruf direkt in onNoindexChange

<!-- Aus frontend/src/components/profile/MemberProfileHero.tsx -->
Props Interface — isVerified prop hinzufügen: isVerified?: boolean
heroCopy-Block: <h2>{displayName} {isVerified && <VerifiedBadge />}</h2>
Benötigte Änderung: MemberProfileHero Props um isVerified: boolean erweitern; VerifiedBadge importieren

<!-- Aus frontend/src/components/profile/MemberRoleTimeline.tsx -->
Zeile 47 ca: const isHistorical = entry.status === 'historical'
Zeile 58 ca: {isHistorical && <span ...>(historisch)</span>}
Änderung: isVerified-Prop zum Timeline-Entry-Typ hinzufügen ODER als separate is_verified-Prop des ganzen Containers übergeben

<!-- generateMetadata Muster (RESEARCH.md §5) -->
export async function generateMetadata({ params }: MemberProfilePageProps): Promise<Metadata>
Aus getMemberProfile(slug) → response.data.noindex → robots: { index: !noindex, follow: !noindex }
Fallback bei Fehler: return {} (Browser-Default: index=true)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VerifiedBadge anlegen + MemberProfileHero und MemberRoleTimeline erweitern</name>
  <files>
    frontend/src/components/profile/VerifiedBadge.tsx
    frontend/src/components/profile/MemberProfileHero.tsx
    frontend/src/components/profile/MemberRoleTimeline.tsx
  </files>
  <read_first>
    - frontend/src/components/profile/MemberProfileHero.tsx — vollständige Datei: Props Interface, heroCopy-Block (h2), bestehende Icon-Imports
    - frontend/src/components/profile/MemberRoleTimeline.tsx — vollständige Datei: entry.status === 'historical'-Stelle, (historisch)-Rendering
    - frontend/src/app/me/profile/page.module.css — für CSS-Klassenreferenzen falls VerifiedBadge.module.css nicht vorhanden (dann inline-styles oder neue CSS-Datei)
  </read_first>
  <action>
    Erstelle `frontend/src/components/profile/VerifiedBadge.tsx` (per UI-SPEC.md §3, PATTERNS.md §VerifiedBadge):
    - 'use client' NICHT — reine Darstellungskomponente
    - Import: import { CheckCircle } from 'lucide-react'
    - Props: type VerifiedBadgeProps = { label?: string }
    - Rendering: <span style={{display:'inline-flex',alignItems:'center',gap:'4px',fontSize:'14px',fontWeight:700,color:'var(--color-success)'}} aria-label="Verifiziertes Mitglied"> <CheckCircle size={18} color="var(--color-success)" aria-hidden="true" /> <span>{label ?? 'Verifiziert'}</span> </span>
    - Keine eigene CSS-Datei nötig — inline-styles ausreichend für diese kleine Komponente

    Erweitere `MemberProfileHero.tsx`:
    - Props Interface um `isVerified?: boolean` ergänzen
    - Import { VerifiedBadge } aus './VerifiedBadge' hinzufügen
    - In heroCopy-Block: <h2>{displayName}{isVerified && <> <VerifiedBadge /></>}</h2>
    - Prop mit Default false; kein Breaking Change für bestehende Aufrufer

    Erweitere `MemberRoleTimeline.tsx`:
    - Komponente/Props um isVerified?: boolean oder isVerifiedMember?: boolean erweitern
    - (historisch)-Label: {isHistorical && !isVerified && <span className={styles.roleTimelineHistorical}> (historisch)</span>}
    - Default: isVerified = false — bestehende Aufrufer ohne Prop-Änderung unverändert
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` ohne neue Fehler in diesen 3 Dateien
    - VerifiedBadge.tsx exportiert VerifiedBadge-Komponente mit CheckCircle-Icon und aria-label="Verifiziertes Mitglied"
    - MemberProfileHero.tsx akzeptiert isVerified?: boolean und rendert VerifiedBadge conditional
    - MemberRoleTimeline.tsx rendert (historisch) nur wenn isHistorical && !isVerified
    - Kein Breaking Change für bestehende Aufrufer (alle neuen Props optional mit default false/undefined)
  </acceptance_criteria>
  <done>3 Dateien kompilieren; VerifiedBadge vorhanden; MemberProfileHero und MemberRoleTimeline backward-kompatibel erweitert.</done>
</task>

<task type="auto">
  <name>Task 2: ClaimStatusCard anlegen + me/profile und members/[slug] erweitern</name>
  <files>
    frontend/src/app/me/profile/components/ClaimStatusCard.tsx
    frontend/src/app/me/profile/page.tsx
    frontend/src/app/members/[slug]/page.tsx
  </files>
  <read_first>
    - frontend/src/app/me/profile/components/VisibilityCard.tsx — vollständige Datei: Props-Muster, fieldset/legend-Struktur, CSS-Klassen
    - frontend/src/app/me/profile/page.tsx — vollständige Datei: Side-Column-Block (aside.sideColumn), handleSubmit-Muster, isSaving-State, errorBox-Rendering
    - frontend/src/app/me/profile/page.module.css — checkboxControl, mutedText, successBox, errorBox CSS-Klassen
    - frontend/src/app/members/[slug]/page.tsx — vollständige Datei: MemberProfilePageProps Interface, bestehende getMemberProfile-Nutzung, Metadata-Import
    - frontend/src/lib/api.ts — searchHistoricalMembers, submitMemberClaim, getMyMemberClaim Signaturen (nach Plan 04)
  </read_first>
  <action>
    Erstelle `frontend/src/app/me/profile/components/ClaimStatusCard.tsx` (per UI-SPEC.md §1, PATTERNS.md §ClaimStatusCard):

    Props:
    type ClaimStatusCardProps = {
      noindex: boolean
      claimStatus?: 'pending' | 'verified' | 'rejected' | null
      claimMemberNick?: string | null
      disabled: boolean
      onNoindexChange: (noindex: boolean) => Promise<void>
    }

    Rendering:
    1. noindex-Toggle: <fieldset> <legend>Suchmaschinen-Sichtbarkeit</legend>
       <label className={styles.checkboxControl}>
         <input type="checkbox" checked={!noindex} disabled={disabled}
           onChange={(e) => void onNoindexChange(!e.target.checked)} />
         Mein Profil von Suchmaschinen indexieren lassen
       </label>
       <p className={styles.mutedText}>Wenn deaktiviert, wird dein Profil mit "noindex,nofollow" markiert.</p>
       </fieldset>

    2. Claim-Status-Anzeige:
       <fieldset> <legend>Verifizierter Member-Eintrag</legend>
         Falls !claimStatus oder claimStatus === null:
           <p className={styles.mutedText}>Du bist noch keinem historischen Member-Eintrag zugeordnet.</p>
           <button type="button" onClick={onBeginClaim} disabled={disabled}>Historischen Eintrag beanspruchen</button>
         Falls claimStatus === 'pending':
           Ausstehend-Badge + "Dein Claim für {claimMemberNick} wartet auf Bestätigung durch den Leader."
         Falls claimStatus === 'verified':
           Verifiziert-Badge + "Du bist als {claimMemberNick} verifiziert."
         Falls claimStatus === 'rejected':
           Abgelehnt-Badge + "Dein Claim wurde abgelehnt. Du kannst einen neuen Eintrag beanspruchen."
       </fieldset>

    V1-Scope: onBeginClaim als optionaler Prop (für Phase-66 genügt die Status-Anzeige; Self-Service-Claim-Trigger kann als TODO bleiben oder Link zu /me/claim führen).

    Ergänze `frontend/src/app/me/profile/page.tsx`:
    - Import ClaimStatusCard hinzufügen
    - handleNoindexChange async-Funktion: ruft bestehenden updateOwnProfile-API-Call mit { noindex: !checked } auf (oder direkten PATCH); bei Fehler: setErrorMessage, optimistischer UI-Rollback
    - In <aside className={styles.sideColumn}>: nach VisibilityCard eine neue <Card variant="section">-Instanz mit <ClaimStatusCard noindex={...} claimStatus={...} ... /> einfügen
    - profile.noindex und profile.claim_status aus API-Response lesen (nach Plan 04 vorhanden)
    - profile.is_verified an MemberProfileHero weitergeben (falls Hero auf dieser Seite gerendert wird)

    Ergänze `frontend/src/app/members/[slug]/page.tsx`:
    - 'import type { Metadata } from 'next'' am Anfang hinzufügen (falls nicht vorhanden)
    - generateMetadata Funktion exportieren (per RESEARCH.md §5):
      export async function generateMetadata({ params }): Promise<Metadata>
      Resolve params.slug → getMemberProfile(slug)
      Falls response.data.noindex === true: return { robots: { index: false, follow: false } }
      Sonst: return {} (Browser-Default: indexierbar)
      Bei Fetch-Fehler: return {} (kein Crash)
    - MemberProfileHero-Aufruf: isVerified={profile.is_verified} Prop hinzufügen
    - MemberRoleTimeline-Aufruf (falls vorhanden): isVerified={profile.is_verified} übergeben
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` ohne neue Fehler
    - ClaimStatusCard.tsx exportiert ClaimStatusCard; enthält noindex-Checkbox und Claim-Status-Anzeige
    - me/profile/page.tsx importiert und rendert ClaimStatusCard in Side-Column
    - members/[slug]/page.tsx exportiert generateMetadata-Funktion
    - generateMetadata gibt robots: { index: false, follow: false } zurück wenn noindex=true
    - MemberProfileHero erhält isVerified-Prop aus profile.is_verified
    - Fehlermeldungen auf Deutsch mit korrekten Umlauten (z. B. "Suchmaschinen-Sichtbarkeit")
  </acceptance_criteria>
  <done>me/profile mit ClaimStatusCard und noindex-Toggle; öffentliches Profil mit generateMetadata und VerifiedBadge; build grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Grenze | Beschreibung |
|--------|-------------|
| Browser → noindex-Toggle | Client-seitige Checkbox-Änderung → PATCH an Backend mit Auth-Token |
| SSR → generateMetadata | Server-seitig: liest noindex aus API, setzt robots-Header — kein Client-Zugriff nötig |

## STRIDE Threat Register

| Threat ID | Kategorie | Disposition | Mitigationsplan |
|-----------|-----------|-------------|-----------------|
| T-66-05-01 | Information Disclosure | mitigate | generateMetadata liest noindex aus API und setzt robots: {index: false, follow: false} — verhindert ungewollte Indexierung |
| T-66-05-02 | Spoofing | mitigate | noindex-Toggle geht über authorizedFetch mit Bearer-Token — kein unauthentifizierter PATCH möglich |
| T-66-05-03 | Information Disclosure | accept | robots-Meta-Tag ist nur ein Hinweis an kooperative Crawler (D-14); kein echter Zugriffsschutz — bewusst akzeptiertes Restrisiko |
| T-66-05-SC | Tampering | accept | Keine neuen npm-Pakete; nur lucide-react (bestehend) |

</threat_model>

<verification>
1. `npm run build` ohne neue Fehler
2. `npx tsc --noEmit` ohne neue Fehler
3. VerifiedBadge.tsx existiert mit CheckCircle-Icon
4. members/[slug]/page.tsx enthält "generateMetadata" (grep)
5. ClaimStatusCard.tsx enthält "checkboxControl" oder entsprechende Checkbox-Struktur
6. MemberProfileHero.tsx enthält "isVerified" und "VerifiedBadge"
</verification>

<success_criteria>
Frontend-Build grün. generateMetadata setzt robots-Meta-Tag korrekt. VerifiedBadge erscheint im öffentlichen Profil. me/profile zeigt noindex-Toggle und Claim-Status.
</success_criteria>

<output>
Erstelle `.planning/phases/66-claiming-verifizierung/66-05-SUMMARY.md` wenn fertig.
</output>
