---
phase: 66-claiming-verifizierung
plan: 01
type: execute
wave: 1
depends_on:
  - 66-00
files_modified:
  - database/migrations/0091_member_claim_invitations.up.sql
  - database/migrations/0091_member_claim_invitations.down.sql
autonomous: true
requirements:
  - P66-SC1
  - P66-SC2
must_haves:
  truths:
    - "Tabelle member_claim_invitations existiert in der Datenbank nach Migration"
    - "token_hash ist VARCHAR(64) mit UNIQUE-Index und CHECK char_length = 64"
    - "Unique-Partial-Index auf (member_id) WHERE status = 'pending' ist aktiv"
    - "Rollback via 0091.down.sql entfernt die Tabelle spurlos"
  artifacts:
    - path: database/migrations/0091_member_claim_invitations.up.sql
      provides: "CREATE TABLE member_claim_invitations mit Token-, Status-, FK-Schema"
      contains: "member_claim_invitations"
    - path: database/migrations/0091_member_claim_invitations.down.sql
      provides: "DROP TABLE member_claim_invitations"
      contains: "DROP TABLE"
  key_links:
    - from: "member_claim_invitations.member_id"
      to: "members(id)"
      via: "REFERENCES members(id) ON DELETE CASCADE"
      pattern: "REFERENCES members\\(id\\)"
    - from: "member_claim_invitations.created_by_app_user_id"
      to: "app_users(id)"
      via: "REFERENCES app_users(id) ON DELETE SET NULL"
      pattern: "REFERENCES app_users\\(id\\)"
---

<objective>
Migration 0091 anlegen: neue Tabelle `member_claim_invitations` als Grundlage für den Leader-Einladungslink-Flow (D-04, P66-SC2). Struktur analog zu `fansub_group_invitations` (Migration 0076), angepasst auf member_id-Kontext statt E-Mail-Kontext. Migration anwenden und Schema verifizieren.

Purpose: Ohne diese Tabelle kann der gesamte Invitation-Flow (Plan 02–04) nicht implementiert werden — sie ist der DB-Anker für Token-Hashing, Status-Lifecycle und Claim-Verknüpfung.
Output: Zwei Migrationsdateien (up/down), angewandte Migration in lokaler DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-claiming-verifizierung/66-CONTEXT.md
@.planning/phases/66-claiming-verifizierung/66-RESEARCH.md
@.planning/phases/66-claiming-verifizierung/66-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0091 up/down schreiben</name>
  <files>
    database/migrations/0091_member_claim_invitations.up.sql
    database/migrations/0091_member_claim_invitations.down.sql
  </files>
  <read_first>
    - database/migrations/0076_fansub_group_invitations.up.sql — exaktes Vorbild: Tabellenstruktur, CONSTRAINT-Namen, Index-Muster, Status-CHECK, token_hash-Length-CHECK
    - database/migrations/0081_historical_members_identity.up.sql — member_claims-Schema zum Verstehen der FK-Ziele (members, app_users)
    - Laufzeit-Bestätigung: `ls database/migrations/ | tail` — 0089 (anime_contributions_review_note) UND 0090 (member_story_images) sind BEREITS belegt; korrekte nächste Nummer ist 0091 (per ls verifiziert).
  </read_first>
  <action>
    Führe zuerst `ls database/migrations/ | tail` aus, um die aktuelle letzte Migrationsnummer zu bestätigen (sollte 0090_member_story_images sein). Die korrekte nächste Nummer ist 0091.

    Schreibe `database/migrations/0091_member_claim_invitations.up.sql` analog zu `0076_fansub_group_invitations.up.sql` mit diesen Anpassungen (per D-04, D-05, RESEARCH.md §1):

    Tabelle `member_claim_invitations`:
    - id BIGSERIAL PRIMARY KEY
    - member_id BIGINT NOT NULL REFERENCES members(id) ON DELETE CASCADE
    - fansub_group_id BIGINT NOT NULL REFERENCES fansub_groups(id) ON DELETE CASCADE (Leader-Kontext)
    - token_hash VARCHAR(64) NOT NULL
    - status VARCHAR(20) NOT NULL DEFAULT 'pending'
    - expires_at TIMESTAMPTZ NOT NULL
    - created_by_app_user_id BIGINT NULL REFERENCES app_users(id) ON DELETE SET NULL
    - accepted_by_app_user_id BIGINT NULL REFERENCES app_users(id) ON DELETE SET NULL
    - cancelled_by_app_user_id BIGINT NULL REFERENCES app_users(id) ON DELETE SET NULL
    - accepted_at TIMESTAMPTZ NULL
    - cancelled_at TIMESTAMPTZ NULL
    - created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    - updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
    - CONSTRAINT chk_member_claim_invitations_status CHECK (status IN ('pending', 'accepted', 'cancelled', 'expired'))
    - CONSTRAINT chk_member_claim_invitations_token_hash_length CHECK (char_length(token_hash) = 64)

    Indizes:
    - CREATE UNIQUE INDEX uq_member_claim_invitations_pending_member ON member_claim_invitations (member_id) WHERE status = 'pending' — nur ein offener Einladungslink pro Member gleichzeitig (A1)
    - CREATE UNIQUE INDEX uq_member_claim_invitations_token_hash ON member_claim_invitations (token_hash)
    - CREATE INDEX idx_member_claim_invitations_member_status ON member_claim_invitations (member_id, status, created_at DESC)
    - CREATE INDEX idx_member_claim_invitations_expires_at ON member_claim_invitations (expires_at)

    KEIN email/normalized_email/invited_role_codes-Feld — Einladung ist member_id-gebunden, nicht E-Mail-gebunden.

    Schreibe `database/migrations/0091_member_claim_invitations.down.sql`:
    DROP TABLE IF EXISTS member_claim_invitations;
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'database/migrations/0091_member_claim_invitations.up.sql' -Pattern 'member_claim_invitations','token_hash','char_length','uq_member_claim_invitations_pending_member' | Measure-Object -Line | Select-Object -ExpandProperty Lines"</automated>
  </verify>
  <acceptance_criteria>
    - `0091_member_claim_invitations.up.sql` enthält CREATE TABLE member_claim_invitations mit allen genannten Spalten
    - token_hash VARCHAR(64) und CHECK char_length(token_hash) = 64 vorhanden
    - UNIQUE INDEX auf (member_id) WHERE status = 'pending' vorhanden
    - UNIQUE INDEX auf (token_hash) vorhanden
    - REFERENCES members(id) ON DELETE CASCADE und REFERENCES fansub_groups(id) ON DELETE CASCADE vorhanden
    - app_users FKs mit ON DELETE SET NULL vorhanden
    - `0091_member_claim_invitations.down.sql` enthält DROP TABLE IF EXISTS member_claim_invitations
    - Keine email/normalized_email/invited_role_codes Spalten in der up.sql
    - Dateipfad ist 0091_... (nicht 0090_..., 0089_... oder andere Nummer)
  </acceptance_criteria>
  <done>Beide Migrationsdateien mit Nummer 0091 existieren und enthalten korrektes Schema per Vorbild 0076 mit member_id-Anpassungen.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0091 anwenden und Schema verifizieren</name>
  <files></files>
  <read_first>
    - backend/cmd/migrate/main.go oder docker-compose.yml — Migrations-Ausführungsweg ermitteln
    - database/migrations/0091_member_claim_invitations.up.sql — soeben erstellt, sicherstellen dass Datei korrekt ist
  </read_first>
  <action>
    Migration via `backend/cmd/migrate` im Docker-Compose-Kontext anwenden. Typischer Weg im Projekt:

    Option A (Docker): `docker compose run --rm migrate up` oder `docker compose exec backend migrate`
    Option B (direkt): `DATABASE_URL=... go run ./backend/cmd/migrate up`

    Ermittle den korrekten Weg aus `docker-compose.yml` oder `backend/cmd/migrate/main.go` und führe die Migration aus.

    Danach SQL-Verifikation: Prüfe über psql oder `docker compose exec db psql -U <user> -d <db> -c "\d member_claim_invitations"` dass:
    1. Tabelle existiert
    2. token_hash VARCHAR(64) korrekt
    3. Alle FK-Constraints sichtbar
    4. UNIQUE-Indizes angelegt

    Falls Docker nicht läuft: Starte via `docker compose up -d` und dann Migration.

    Syntaktischer Fallback (wenn Docker im Ausführungskontext nicht verfügbar):
    `python3 -c "import sqlparse; print('ok')" 2>/dev/null || psql --version 2>/dev/null || echo "Syntaxprüfung: Datei wurde per grep verifiziert (CREATE TABLE + alle Constraints vorhanden); Docker-Ausführung beim nächsten Compose-Start nötig."`
    In diesem Fall: Greife auf die vorherige grep-Verifikation (Task 1 verify) zurück und notiere im SUMMARY, dass `\d member_claim_invitations` beim nächsten `docker compose up` zu prüfen ist.
  </action>
  <verify>
    <automated>docker compose exec db psql -U postgres -d team4s -c "\d member_claim_invitations" 2>&1 || echo "Docker nicht verfügbar — syntaktische Verifikation via grep bereits in Task 1 erfolgt; Migration 0091 beim nächsten docker compose up anwenden."</automated>
  </verify>
  <acceptance_criteria>
    - `\d member_claim_invitations` zeigt alle Spalten (id, member_id, fansub_group_id, token_hash, status, expires_at, die drei *_app_user_id Spalten, timestamps) — ODER Docker-Fallback-Notiz im SUMMARY
    - CHECK Constraints sichtbar (status IN (...), char_length = 64)
    - Keine Migrationsfehler im Output
    - Migration-Tabelle zeigt 0091 als angewendet (oder Fallback-Notiz)
  </acceptance_criteria>
  <done>Migration 0091 fehlerfrei angewendet; `member_claim_invitations` existiert in der DB mit korrektem Schema — oder syntaktische Verifikation aus Task 1 bestätigt korrekte SQL-Struktur mit Fallback-Notiz.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Grenze | Beschreibung |
|--------|-------------|
| Migrations-Ausführung | Nur über Backend-CLI/Docker-Compose, nicht über fremde DB-Zugänge |

## STRIDE Threat Register

| Threat ID | Kategorie | Komponente | Disposition | Mitigationsplan |
|-----------|-----------|-----------|-------------|-----------------|
| T-66-01-01 | Tampering | Migration 0091 | mitigate | Migration wird nur durch `backend/cmd/migrate` ausgeführt; keine direkten DB-Writes durch Handler-Code; Down-Migration testet vollständigen Rollback |
| T-66-01-02 | Information Disclosure | token_hash-Schema | mitigate | VARCHAR(64) + CHECK-Constraint erzwingt nur SHA-256-Hashes; Klartext-Tokens landen nie in dieser Spalte — erzwungen durch 64-Zeichen-Grenze |
| T-66-01-SC | Tampering | keine npm/pip/cargo Installs | accept | Nur SQL-Migrationsdateien; kein Package-Manager involviert |

</threat_model>

<verification>
1. `0091_member_claim_invitations.up.sql` und `.down.sql` existieren in `database/migrations/`
2. Up-Migration enthält alle 13 Spalten, 2 CHECKs, 4 Indizes
3. Down-Migration enthält DROP TABLE IF EXISTS member_claim_invitations
4. Migration fehlerfrei angewendet (kein SQL-Fehler) — oder syntaktischer Fallback mit Notiz
5. `\d member_claim_invitations` zeigt korrektes Schema — oder Fallback-Notiz im SUMMARY
6. Migrationsnummer ist 0091 (0089 = anime_contributions_review_note, 0090 = member_story_images — beide belegt)
</verification>

<success_criteria>
Tabelle `member_claim_invitations` existiert in der lokalen DB (oder ist syntaktisch korrekt und bereit zur Ausführung) mit token_hash VARCHAR(64), UNIQUE-Index auf pending member_id, UNIQUE-Index auf token_hash, FKs auf members + fansub_groups + app_users. Migrationsnummer ist korrekt 0091.
</success_criteria>

<output>
Erstelle `.planning/phases/66-claiming-verifizierung/66-01-SUMMARY.md` wenn fertig.
</output>
