---
phase: 66-claiming-verifizierung
plan: 04
type: execute
wave: 4
depends_on:
  - 66-03
files_modified:
  - backend/cmd/server/main.go
  - backend/cmd/server/admin_routes.go
  - frontend/src/lib/api.ts
  - frontend/src/types/profile.ts
autonomous: true
requirements:
  - P66-SC1
  - P66-SC2
  - P66-SC3
must_haves:
  truths:
    - "Alle neuen Backend-Routen sind in main.go/admin_routes.go verdrahtet und erreichbar"
    - "Frontend-API-Funktionen acceptClaimInvitation, submitMemberClaim, searchHistoricalMembers, generateClaimInvitation, listPendingClaims, verifyMemberClaim, rejectMemberClaim existieren in api.ts"
    - "PublicMemberProfileData enthält is_verified und noindex"
    - "MemberProfileData enthält is_verified, noindex, claim_status, claim_member_nick"
    - "Backend-Build kompiliert nach Verdrahtung"
  artifacts:
    - path: backend/cmd/server/main.go
      provides: "Repo/Handler-Konstruktoren und me/admin-Routen für Claims und Einladungen"
      contains: "NewMemberClaimsRepository"
    - path: backend/cmd/server/admin_routes.go
      provides: "Admin-Routen für Claim-Queue und Einladungslink"
      contains: "member-claims"
    - path: frontend/src/lib/api.ts
      provides: "API-Funktionen für alle Claim-/Einladungs-Endpunkte"
      contains: "acceptClaimInvitation"
    - path: frontend/src/types/profile.ts
      provides: "Typerweiterungen für verified/noindex-Felder"
      contains: "is_verified"
  key_links:
    - from: "frontend/src/app/claim-invitations/accept/page.tsx (Plan 06)"
      to: "frontend/src/lib/api.ts acceptClaimInvitation"
      via: "import { acceptClaimInvitation } from '@/lib/api'"
      pattern: "acceptClaimInvitation"
    - from: "frontend/src/app/members/[slug]/page.tsx (Plan 05)"
      to: "frontend/src/types/profile.ts PublicMemberProfileData"
      via: "profile.is_verified, profile.noindex"
      pattern: "is_verified.*noindex"
---

<objective>
Backend-Handler und Repositories in `main.go` und `admin_routes.go` verdrahten. Neue API-Funktionen in `frontend/src/lib/api.ts` anlegen. TypeScript-Typen in `frontend/src/types/profile.ts` erweitern. Dieser Plan stellt die Verbindungsschicht bereit, auf der die Frontend-Pläne 05 und 06 aufbauen.

Purpose: Ohne Routen-Verdrahtung sind die Handler nicht erreichbar. Ohne api.ts-Funktionen können Frontend-Komponenten keine Anfragen stellen. Ohne Typ-Erweiterungen schlägt TypeScript in Plänen 05/06 fehl.
Output: Lauffähiges Backend mit allen neuen Routen; Frontend-Typen und API-Client bereit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/66-claiming-verifizierung/66-CONTEXT.md
@.planning/phases/66-claiming-verifizierung/66-RESEARCH.md
@.planning/phases/66-claiming-verifizierung/66-PATTERNS.md
@.planning/phases/66-claiming-verifizierung/66-03-SUMMARY.md

<interfaces>
<!-- Aus backend/cmd/server/main.go (Z. 344–391) — Repository/Handler-Verdrahtungs-Konvention -->
Neue Repos unter bestehende repos einreihen:
  memberClaimsRepo := repository.NewMemberClaimsRepository(dbPool)
  memberClaimInvitationsRepo := repository.NewMemberClaimInvitationRepository(dbPool, cfg.AppPublicURL)

Neue Handler:
  memberClaimsHandler := handlers.NewMemberClaimsHandler(memberClaimsRepo, permissionSvc, auditLogRepo)
  memberClaimInvitationsHandler := handlers.NewMemberClaimInvitationsHandler(memberClaimInvitationsRepo, permissionSvc, auditLogRepo, cfg.AppPublicURL)

Me-Routen (nach v1.GET("/me/memberships", ...)):
  v1.GET("/me/member-search", authMiddleware, memberClaimsHandler.SearchMembers)
  v1.GET("/me/member-claim", authMiddleware, memberClaimsHandler.GetMyClaim)
  v1.POST("/me/member-claims", authMiddleware, memberClaimsHandler.SubmitClaim)
  v1.POST("/claim-invitations/accept", authMiddleware, memberClaimInvitationsHandler.AcceptClaimInvitation)

Admin-Routen (in registerAdminRoutes via adminRouteHandlers):
  struct-Felder: memberClaimsHandler *handlers.MemberClaimsHandler; memberClaimInvitationsHandler *handlers.MemberClaimInvitationsHandler

<!-- Aus backend/cmd/server/admin_routes.go -->
Nach Phase-62-Routen (letzte: ...contributions/:contributionId):
  v1.GET("/admin/fansubs/:id/member-claims", auth, deps.memberClaimsHandler.ListPendingClaimsForGroup)
  v1.POST("/admin/fansubs/:id/member-claims/:claimId/verify", auth, deps.memberClaimsHandler.VerifyClaim)
  v1.POST("/admin/fansubs/:id/member-claims/:claimId/reject", auth, deps.memberClaimsHandler.RejectClaim)
  v1.POST("/admin/fansubs/:id/group-members/:memberId/claim-invitations", auth, deps.memberClaimInvitationsHandler.CreateClaimInvitation)
  v1.POST("/admin/fansubs/:id/group-members/:memberId/claim-invitations/:invitationId/cancel", auth, deps.memberClaimInvitationsHandler.CancelClaimInvitation)

<!-- Aus frontend/src/lib/api.ts — API-Funktions-Muster (acceptFansubInvitation Z. 3110–3142) -->
export async function acceptClaimInvitation(payload: {token: string}, authToken?: string): Promise<void>
export async function submitMemberClaim(payload: {member_id: number, note?: string}, authToken?: string): Promise<MemberClaimResponse>
export async function searchHistoricalMembers(q: string, authToken?: string): Promise<MemberSearchResult[]>
export async function generateClaimInvitation(fansubId: number, memberId: number, authToken?: string): Promise<GenerateClaimInvitationResponse>
export async function listPendingClaims(fansubId: number, authToken?: string): Promise<MemberClaimRow[]>
export async function verifyMemberClaim(fansubId: number, claimId: number, authToken?: string): Promise<void>
export async function rejectMemberClaim(fansubId: number, claimId: number, authToken?: string): Promise<void>
export async function getMyMemberClaim(authToken?: string): Promise<MemberClaimRow | null>

<!-- Aus frontend/src/types/profile.ts (PATTERNS.md Z. 729–749) -->
PublicMemberProfileData erweitern:
  is_verified: boolean
  noindex: boolean

MemberProfileData erweitern:
  is_verified: boolean
  noindex: boolean
  claim_status?: 'pending' | 'verified' | 'rejected' | null
  claim_member_nick?: string | null

Neue Interface-Typen in types/profile.ts oder einer neuen types/claims.ts:
  MemberSearchResult { id: number; nickname: string; display_name: string }
  MemberClaimRow { id: number; app_user_id: number; member_id: number; member_nickname: string; claim_status: string; note?: string; created_at: string }
  GenerateClaimInvitationResponse { id: number; member_id: number; status: string; expires_at: string; invite_link: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend-Routen verdrahten (main.go + admin_routes.go)</name>
  <files>
    backend/cmd/server/main.go
    backend/cmd/server/admin_routes.go
  </files>
  <read_first>
    - backend/cmd/server/main.go — vollständige aktuelle Datei lesen (Zeilen 344–403: bestehende Repos/Handler/Routen als Einfüge-Kontext)
    - backend/cmd/server/admin_routes.go — vollständige aktuelle Datei lesen (adminRouteHandlers-Struct, Ende der Route-Liste als Einfüge-Punkt)
    - backend/internal/handlers/member_claims_handler.go — exportierte Handler-Methoden-Namen
    - backend/internal/handlers/member_claim_invitations_handler.go — exportierte Handler-Methoden-Namen
    - backend/internal/repository/member_claims_repository.go — NewMemberClaimsRepository-Signatur
    - backend/internal/repository/member_claim_invitations_repository.go — NewMemberClaimInvitationRepository-Signatur
  </read_first>
  <action>
    Ergänze `backend/cmd/server/main.go`:

    1. Nach der proposalsMeHandler-Instantiierung (Zeile ~386) Repos und Handler anlegen:
       memberClaimsRepo := repository.NewMemberClaimsRepository(dbPool)
       memberClaimInvitationsRepo := repository.NewMemberClaimInvitationRepository(dbPool, cfg.AppPublicURL)
       memberClaimsHandler := handlers.NewMemberClaimsHandler(memberClaimsRepo, permissionSvc, auditLogRepo)
       memberClaimInvitationsHandler := handlers.NewMemberClaimInvitationsHandler(memberClaimInvitationsRepo, permissionSvc, auditLogRepo, cfg.AppPublicURL)

    2. Me-Routen nach v1.POST("/me/anime-contributions/:contributionId/self-publish", ...):
       v1.GET("/me/member-search", authMiddleware, memberClaimsHandler.SearchMembers)
       v1.GET("/me/member-claim", authMiddleware, memberClaimsHandler.GetMyClaim)
       v1.POST("/me/member-claims", authMiddleware, memberClaimsHandler.SubmitClaim)
       v1.POST("/claim-invitations/accept", authMiddleware, memberClaimInvitationsHandler.AcceptClaimInvitation)

    3. memberClaimsHandler und memberClaimInvitationsHandler an registerAdminRoutes übergeben — adminRouteHandlers-Struct und Übergabe-Aufruf anpassen.

    Ergänze `backend/cmd/server/admin_routes.go`:

    1. adminRouteHandlers-Struct um zwei neue Felder erweitern:
       memberClaimsHandler      *handlers.MemberClaimsHandler
       memberClaimInvitationsHandler *handlers.MemberClaimInvitationsHandler

    2. Am Ende von registerAdminRoutes (nach letzten Phase-62-Contributions-Routen) hinzufügen:
       // Phase 66: Member-Claiming und Einladungslinks
       v1.GET("/admin/fansubs/:id/member-claims", auth, deps.memberClaimsHandler.ListPendingClaimsForGroup)
       v1.POST("/admin/fansubs/:id/member-claims/:claimId/verify", auth, deps.memberClaimsHandler.VerifyClaim)
       v1.POST("/admin/fansubs/:id/member-claims/:claimId/reject", auth, deps.memberClaimsHandler.RejectClaim)
       v1.POST("/admin/fansubs/:id/group-members/:memberId/claim-invitations", auth, deps.memberClaimInvitationsHandler.CreateClaimInvitation)
       v1.POST("/admin/fansubs/:id/group-members/:memberId/claim-invitations/:invitationId/cancel", auth, deps.memberClaimInvitationsHandler.CancelClaimInvitation)
  </action>
  <verify>
    <automated>cd backend && go build ./cmd/server/... 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./cmd/server/...` ohne Fehler
    - main.go enthält "NewMemberClaimsRepository" und "NewMemberClaimInvitationRepository"
    - admin_routes.go enthält "member-claims" und "claim-invitations" Routen-Registrierungen
    - v1.POST("/claim-invitations/accept") in main.go registriert
    - adminRouteHandlers-Struct enthält memberClaimsHandler und memberClaimInvitationsHandler
  </acceptance_criteria>
  <done>Backend kompiliert fehlerfrei; alle 9 neuen Routen (4 me/ + 5 admin/) registriert.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend-Typen und API-Client ergänzen</name>
  <files>
    frontend/src/types/profile.ts
    frontend/src/lib/api.ts
  </files>
  <read_first>
    - frontend/src/types/profile.ts — vollständige Datei lesen: PublicMemberProfileData (Z. 119–138) und MemberProfileData (Z. 52–99) als Erweiterungspunkte identifizieren
    - frontend/src/lib/api.ts — acceptFansubInvitation-Funktion (letzte paar hundert Zeilen) als exaktes Muster für neue API-Funktionen; getApiBaseUrl, authorizedFetch, withAuthHeader, parseApiErrorPayload, ApiError importieren/verstehen
  </read_first>
  <action>
    Ergänze `frontend/src/types/profile.ts`:

    1. PublicMemberProfileData um zwei Felder erweitern:
       is_verified: boolean
       noindex: boolean

    2. MemberProfileData um vier Felder erweitern:
       is_verified: boolean
       noindex: boolean
       claim_status?: 'pending' | 'verified' | 'rejected' | null
       claim_member_nick?: string | null

    3. Neue Typen am Ende der Datei ergänzen:
       export interface MemberSearchResult { id: number; nickname: string; display_name: string }
       export interface MemberClaimRow { id: number; app_user_id: number; member_id: number; member_nickname: string; claim_status: string; note?: string | null; created_at: string }
       export interface GenerateClaimInvitationResponse { id: number; member_id: number; status: string; expires_at: string; invite_link: string }

    Ergänze `frontend/src/lib/api.ts` (am Ende, analog zu acceptFansubInvitation):

    Alle Funktionen nutzen: getApiBaseUrl(), authorizedFetch, withAuthHeader, parseApiErrorPayload, ApiError.

    1. acceptClaimInvitation(payload: { token: string }, authToken?: string): Promise<void>
       POST /api/v1/claim-invitations/accept, JSON body, throws ApiError on !response.ok

    2. getMyMemberClaim(authToken?: string): Promise<MemberClaimRow | null>
       GET /api/v1/me/member-claim
       404 → return null (statt throw)
       Anderer Fehler → throw ApiError

    3. searchHistoricalMembers(q: string, authToken?: string): Promise<MemberSearchResult[]>
       GET /api/v1/me/member-search?q=encodeURIComponent(q)
       Returns response.data (Array)

    4. submitMemberClaim(payload: { member_id: number; note?: string }, authToken?: string): Promise<MemberClaimRow>
       POST /api/v1/me/member-claims, JSON body

    5. generateClaimInvitation(fansubId: number, memberId: number, authToken?: string): Promise<GenerateClaimInvitationResponse>
       POST /api/v1/admin/fansubs/${fansubId}/group-members/${memberId}/claim-invitations

    6. listPendingMemberClaims(fansubId: number, authToken?: string): Promise<MemberClaimRow[]>
       GET /api/v1/admin/fansubs/${fansubId}/member-claims
       Returns response.data

    7. verifyMemberClaim(fansubId: number, claimId: number, authToken?: string): Promise<void>
       POST /api/v1/admin/fansubs/${fansubId}/member-claims/${claimId}/verify

    8. rejectMemberClaim(fansubId: number, claimId: number, authToken?: string): Promise<void>
       POST /api/v1/admin/fansubs/${fansubId}/member-claims/${claimId}/reject
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` ohne neue Fehler in profile.ts oder api.ts
    - profile.ts: PublicMemberProfileData hat is_verified und noindex
    - profile.ts: MemberProfileData hat is_verified, noindex, claim_status?, claim_member_nick?
    - profile.ts: MemberSearchResult, MemberClaimRow, GenerateClaimInvitationResponse exportiert
    - api.ts: acceptClaimInvitation, getMyMemberClaim, searchHistoricalMembers, submitMemberClaim, generateClaimInvitation, listPendingMemberClaims, verifyMemberClaim, rejectMemberClaim exportiert
    - Alle neuen api.ts-Funktionen nutzen authorizedFetch (kein nacktes fetch)
  </acceptance_criteria>
  <done>TypeScript-Typen und API-Client bereit; tsc ohne neue Fehler; alle 8 API-Funktionen exportiert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Grenze | Beschreibung |
|--------|-------------|
| Frontend → API | Alle API-Funktionen nutzen authorizedFetch mit Auth-Header |
| Route-Registrierung | Alle claim-bezogenen Routen hinter authMiddleware |

## STRIDE Threat Register

| Threat ID | Kategorie | Komponente | Disposition | Mitigationsplan |
|-----------|-----------|-----------|-------------|-----------------|
| T-66-04-01 | Elevation of Privilege | /claim-invitations/accept ohne Auth | mitigate | Route registriert mit authMiddleware; Handler hat zusätzlich requireMeIdentity als Fallback |
| T-66-04-02 | Information Disclosure | api.ts ohne Auth-Header | mitigate | Alle neuen Funktionen nutzen authorizedFetch mit withAuthHeader — kein nacktes fetch |
| T-66-04-03 | Tampering | Falsche Typen für claim_status | mitigate | claim_status als Union-Typ ('pending' | 'verified' | 'rejected' | null) in TypeScript — verhindert typo-bedingte falsche Werte |
| T-66-04-SC | Tampering | keine npm Installs | accept | Nur Typ- und Funktionserweiterungen; keine neuen Pakete |

</threat_model>

<verification>
1. `go build ./cmd/server/...` ohne Fehler
2. `cd frontend && npx tsc --noEmit` ohne neue Fehler
3. Grep admin_routes.go: "member-claims" und "claim-invitations" vorhanden
4. Grep api.ts: "acceptClaimInvitation" vorhanden
5. Grep profile.ts: "is_verified" und "noindex" in PublicMemberProfileData vorhanden
</verification>

<success_criteria>
Backend-Build grün mit allen neuen Routen. Frontend-TypeScript-Check ohne neue Fehler. Alle 8 API-Funktionen und alle Typ-Erweiterungen bereit für Pläne 05 und 06.
</success_criteria>

<output>
Erstelle `.planning/phases/66-claiming-verifizierung/66-04-SUMMARY.md` wenn fertig.
</output>
