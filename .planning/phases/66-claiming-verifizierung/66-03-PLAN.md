---
phase: 66-claiming-verifizierung
plan: 03
type: execute
wave: 3
depends_on:
  - 66-02
files_modified:
  - backend/internal/handlers/member_claims_handler.go
  - backend/internal/handlers/member_claim_invitations_handler.go
autonomous: true
requirements:
  - P66-SC1
  - P66-SC2
  - P66-SC3
must_haves:
  truths:
    - "GET /me/member-search?q=... gibt unverified historische Member zurück (Auth-geschützt)"
    - "POST /me/member-claims reicht Self-Service-Claim ein (P66-SC1)"
    - "GET /admin/member-claims?status=pending gibt Pending-Claims zurück (Leader/Admin)"
    - "POST /admin/member-claims/:id/verify bestätigt einen Claim (P66-SC1, per D-08)"
    - "POST /admin/member-claims/:id/reject lehnt einen Claim ab"
    - "POST /admin/fansubs/:id/group-members/:memberId/claim-invitations erzeugt Einladungslink (P66-SC2)"
    - "POST /claim-invitations/accept löst Einladung ein und setzt claim='verified' + noindex=false"
    - "Audit-Logs für alle Mutations-Aktionen"
  artifacts:
    - path: backend/internal/handlers/member_claims_handler.go
      provides: "Self-Service-Claim-Flow und Admin-Queue-Endpunkte"
      exports:
        - NewMemberClaimsHandler
        - MemberClaimsHandler
    - path: backend/internal/handlers/member_claim_invitations_handler.go
      provides: "Leader-Einladungslink-Generierung und Token-Einlösungs-Endpunkt"
      exports:
        - NewMemberClaimInvitationsHandler
        - MemberClaimInvitationsHandler
  key_links:
    - from: "MemberClaimsHandler.VerifyClaim"
      to: "repository.MemberClaimsRepository.VerifyClaim"
      via: "h.claimsRepo.VerifyClaim(ctx, claimID, actorAppUserID)"
      pattern: "claimsRepo\\.VerifyClaim"
    - from: "MemberClaimInvitationsHandler.AcceptInvitation"
      to: "repository.MemberClaimInvitationRepository.AcceptInvitation"
      via: "h.invitationsRepo.AcceptInvitation(ctx, token, identity.AppUserID)"
      pattern: "invitationsRepo\\.AcceptInvitation"
---

<objective>
Zwei neue Handler-Dateien implementieren. `member_claims_handler.go` stellt Self-Service-Claim-Endpunkte (Nick-Suche, Claim einreichen) und Admin-Queue-Endpunkte (Pending-Liste, Verify, Reject) bereit. `member_claim_invitations_handler.go` deckt Leader-Einladungslink-Generierung und den öffentlichen Einlösungs-Endpunkt ab.

Purpose: Diese Handler sind die HTTP-Schicht über den Repositories aus Plan 02 — sie prüfen Auth, Berechtigungen, binden Request-Bodies und geben strukturierte Responses zurück.
Output: Zwei Handler-Dateien, jeweils unter 450 Zeilen, compilierbar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/66-claiming-verifizierung/66-CONTEXT.md
@.planning/phases/66-claiming-verifizierung/66-RESEARCH.md
@.planning/phases/66-claiming-verifizierung/66-PATTERNS.md
@.planning/phases/66-claiming-verifizierung/66-01-SUMMARY.md
@.planning/phases/66-claiming-verifizierung/66-02-SUMMARY.md

<interfaces>
<!-- Aus backend/internal/handlers/contributions_me_handler.go -->
requireMeIdentity(c *gin.Context) (middleware.AuthIdentity, bool):
  Liest Auth-Identität, gibt 401 bei fehlendem Token.
  In member_claims_handler.go NICHT neu definieren — sie ist bereits package-level in contributions_me_handler.go.
  Stattdessen direkt aufrufen: identity, ok := requireMeIdentity(c)

<!-- Aus backend/internal/handlers/app_auth.go -->
permissionActorFromContext(c *gin.Context) (middleware.AuthIdentity, permissions.Actor, bool):
  Gibt Actor für permissionSvc.CanForFansubGroup zurück.
parseFansubID(param string) (int64, error): parst :id Parameter.

permissionSvc.CanForFansubGroup(ctx, actor, permissions.ActionFansubGroupInvitationsCreate, fansubID):
  Prüft Leader-Berechtigung. Gibt (PermissionResult, error) zurück.
  result.Allowed = false → 403

<!-- Aus backend/internal/repository/member_claims_repository.go (Plan 02) -->
SearchHistoricalMembers(ctx, q string) ([]MemberSearchResult, error)
SubmitClaim(ctx, SubmitClaimInput) (*MemberClaimRow, error)
VerifyClaim(ctx, claimID int64, verifiedByAppUserID int64) error
RejectClaim(ctx, claimID int64, verifiedByAppUserID int64) error
ListPendingClaimsForGroup(ctx, fansubGroupID int64) ([]MemberClaimRow, error)
GetMyClaim(ctx, appUserID int64) (*MemberClaimRow, error)

<!-- Aus backend/internal/repository/member_claim_invitations_repository.go (Plan 02) -->
CreateInvitation(ctx, memberID, fansubGroupID, createdByAppUserID int64) (*CreateInvitationResult, error)
AcceptInvitation(ctx, tokenRaw string, acceptedByAppUserID int64) error
ListInvitationsForMember(ctx, memberID int64) ([]MemberClaimInvitationRow, error)

<!-- Fehlerformat (projektweit) -->
c.JSON(http.StatusConflict, gin.H{"error": gin.H{"message": mutErr.Message, "reason_code": mutErr.Code}})
c.JSON(http.StatusUnauthorized, gin.H{"error": gin.H{"message": "anmeldung erforderlich"}})
c.JSON(http.StatusForbidden, gin.H{"error": gin.H{"message": "zugriff verweigert"}})
c.JSON(http.StatusInternalServerError, gin.H{"error": gin.H{"message": "interner serverfehler"}})

<!-- Audit-Log-Muster (aus app_auth.go Z. 420–434) -->
h.auditLogRepo.Write(ctx, repository.AuditLogEntry{
  ActorAppUserID: &identity.AppUserID,
  EventType:      "member_claim.verified",
  ScopeType:      permissions.ScopeTypeGroup,
  ScopeID:        &fansubGroupID,
  TargetType:     "member_claim",
  TargetID:       &claimID,
  Action:         "verify",
  Outcome:        "allowed",
  Payload:        map[string]any{"member_id": memberID},
})
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: member_claims_handler.go anlegen</name>
  <files>backend/internal/handlers/member_claims_handler.go</files>
  <read_first>
    - backend/internal/handlers/contributions_me_handler.go — requireMeIdentity, Handler-Struct, Konstruktor-Muster (Z. 1–35)
    - backend/internal/handlers/fansub_anime_contributions_handler.go — permissionActorFromContext, Leader-Berechtigungs-Check-Muster
    - backend/internal/handlers/app_auth.go — AuditLogEntry-Muster (Z. 420–434), parseFansubID
    - backend/internal/repository/member_claims_repository.go — Methoden-Signaturen, ClaimMutationError, AsClaimMutationError
    - backend/internal/permissions/permissions.go — ActionFansubGroupInvitationsCreate oder äquivalente Aktion für Leader-Verify
  </read_first>
  <action>
    Erstelle `backend/internal/handlers/member_claims_handler.go` im package `handlers`.

    Struct und Konstruktor:
    - type MemberClaimsHandler struct { claimsRepo *repository.MemberClaimsRepository; permissionSvc *permissions.Service; auditLogRepo *repository.AuditLogRepository }
    - func NewMemberClaimsHandler(claimsRepo *repository.MemberClaimsRepository, permissionSvc *permissions.Service, auditLogRepo *repository.AuditLogRepository) *MemberClaimsHandler

    Endpunkte (5 Handler-Methoden):

    1. SearchMembers (GET /me/member-search?q=...)
       Auth: requireMeIdentity — kein Token → 401
       Query-Param: q, min 1 Zeichen, sonst 400 "Suchbegriff fehlt."
       Aufruf: h.claimsRepo.SearchHistoricalMembers(ctx, q)
       Response: {"data": [{id, nickname, display_name}, ...]}

    2. SubmitClaim (POST /me/member-claims)
       Auth: requireMeIdentity
       Body: { member_id: int64, note?: string }
       Validation: member_id > 0 pflicht, sonst 400
       Aufruf: h.claimsRepo.SubmitClaim mit AppUserID aus identity
       ClaimMutationError → HTTPStatus + message + reason_code
       Bei Erfolg: 201 {"data": {id, member_id, claim_status, created_at}}

    3. GetMyClaim (GET /me/member-claim)
       Auth: requireMeIdentity
       Aufruf: h.claimsRepo.GetMyClaim(ctx, identity.AppUserID)
       ErrNotFound → 404 {"data": null}
       Erfolg: 200 {"data": {id, member_id, member_nickname, claim_status, created_at}}

    4. ListPendingClaimsForGroup (GET /admin/fansubs/:id/member-claims)
       Auth: permissionActorFromContext → permissionSvc.CanForFansubGroup mit ActionFansubGroupInvitationsCreate (Leader-Check)
       parseFansubID(c.Param("id"))
       Aufruf: h.claimsRepo.ListPendingClaimsForGroup(ctx, fansubID)
       Response: {"data": [{id, app_user_id, member_id, member_nickname, claim_status, note, created_at}, ...]}

    5. VerifyClaim (POST /admin/fansubs/:id/member-claims/:claimId/verify)
       Auth: permissionActorFromContext → CanForFansubGroup Leader-Check
       claimID aus c.Param("claimId") → strconv.ParseInt
       Aufruf: h.claimsRepo.VerifyClaim(ctx, claimID, identity.AppUserID)
       ClaimMutationError → HTTPStatus (409 bei already_verified)
       ErrNotFound → 404
       Erfolg: 200 {"data": {"message": "Claim erfolgreich bestätigt."}}
       Audit-Log: EventType "member_claim.verified", Action "verify"

    6. RejectClaim (POST /admin/fansubs/:id/member-claims/:claimId/reject)
       Auth: permissionActorFromContext → CanForFansubGroup Leader-Check
       claimID aus c.Param("claimId")
       Aufruf: h.claimsRepo.RejectClaim(ctx, claimID, identity.AppUserID)
       ErrNotFound → 404
       Erfolg: 200 {"data": {"message": "Claim abgelehnt."}}
       Audit-Log: EventType "member_claim.rejected", Action "reject"

    Datei-Länge: Ziel unter 350 Zeilen.
  </action>
  <verify>
    <automated>cd backend && go build ./internal/handlers/... 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/handlers/...` ohne Fehler
    - 6 Handler-Methoden vorhanden (SearchMembers, SubmitClaim, GetMyClaim, ListPendingClaimsForGroup, VerifyClaim, RejectClaim)
    - VerifyClaim und RejectClaim prüfen Leader-Berechtigung via permissionSvc
    - VerifyClaim schreibt Audit-Log
    - Fehlermeldungen auf Deutsch mit korrekten Umlauten
    - Datei unter 450 Zeilen
  </acceptance_criteria>
  <done>member_claims_handler.go kompiliert mit 6 Endpunkten; Auth- und Permission-Checks vorhanden; Audit-Logs für Mutations.</done>
</task>

<task type="auto">
  <name>Task 2: member_claim_invitations_handler.go anlegen</name>
  <files>backend/internal/handlers/member_claim_invitations_handler.go</files>
  <read_first>
    - backend/internal/handlers/app_auth.go — CreateFansubGroupInvitation-Muster (Z. 300–446): Permission-Check, Token-Response-Format, Audit-Log-Muster, appPublicURL-Nutzung
    - backend/internal/handlers/app_auth.go — AcceptFansubInvitation-Muster (Z. 520–600): Auth, Token aus Body, Repository-Call, Fehler-Mapping
    - backend/internal/repository/member_claim_invitations_repository.go — Signaturen: CreateInvitation, AcceptInvitation, CancelInvitation
    - backend/internal/repository/member_claims_repository.go — ClaimMutationError, AsClaimMutationError
  </read_first>
  <action>
    Erstelle `backend/internal/handlers/member_claim_invitations_handler.go` im package `handlers`.

    Struct und Konstruktor:
    - type MemberClaimInvitationsHandler struct { invitationsRepo *repository.MemberClaimInvitationRepository; permissionSvc *permissions.Service; auditLogRepo *repository.AuditLogRepository; appPublicURL string }
    - func NewMemberClaimInvitationsHandler(invitationsRepo *repository.MemberClaimInvitationRepository, permissionSvc *permissions.Service, auditLogRepo *repository.AuditLogRepository, appPublicURL string) *MemberClaimInvitationsHandler

    Endpunkte (3 Handler-Methoden):

    1. CreateClaimInvitation (POST /admin/fansubs/:id/group-members/:memberId/claim-invitations)
       Auth: permissionActorFromContext → permissionSvc.CanForFansubGroup mit ActionFansubGroupInvitationsCreate
       Parsen: fansubID (c.Param("id")), memberID (c.Param("memberId"))
       Aufruf: h.invitationsRepo.CreateInvitation(ctx, memberID, fansubGroupID, identity.AppUserID)
       ClaimMutationError → HTTPStatus (409 bei pending_invitation_exists)
       Erfolg: 201 {
         "data": {
           "id": ...,
           "member_id": ...,
           "status": "pending",
           "expires_at": ...,
           "invite_link": appPublicURL + result.InviteLink   ← absolute URL
         }
       }
       Audit-Log: EventType "member_claim_invitation.created", kein rawToken im Payload
       NOTE: appPublicURL strings.TrimRight(..., "/") + result.InviteLink

    2. AcceptClaimInvitation (POST /claim-invitations/accept)
       Auth: requireMeIdentity — kein Token → 401 "Bitte zuerst anmelden."
       Body: { token: string }
       Validation: token leer → 400 "Token fehlt."
       Aufruf: h.invitationsRepo.AcceptInvitation(ctx, token, identity.AppUserID)
       ClaimMutationError:
         "invitation_expired" → 410 mit message "Dieser Einladungslink ist abgelaufen. Bitte bitte deinen Leader, einen neuen Link zu erstellen."
         "invitation_used" → 409 mit message
         "invitation_cancelled" → 410 mit message
         "already_verified" → 409 mit message "Du bist bereits einem historischen Eintrag zugeordnet."
       ErrNotFound → 404 "Ungültiger Einladungslink. Bitte überprüfe den Link oder wende dich an deinen Leader."
       Erfolg: 200 {"data": {"message": "Dein Account ist jetzt verifiziert."}}
       Audit-Log: EventType "member_claim_invitation.accepted", kein token im Payload

    3. CancelClaimInvitation (POST /admin/fansubs/:id/group-members/:memberId/claim-invitations/:invitationId/cancel)
       Auth: permissionActorFromContext → CanForFansubGroup Leader-Check
       Parsen: fansubID, memberID (für Zugehörigkeitsprüfung — nicht nur invitationID), invitationID
       Aufruf: h.invitationsRepo.CancelInvitation(ctx, invitationID, identity.AppUserID)
       ErrNotFound → 404
       Erfolg: 200 {"data": {"message": "Einladung storniert."}}
       Audit-Log: EventType "member_claim_invitation.cancelled"

    Datei-Länge: Ziel unter 300 Zeilen.
  </action>
  <verify>
    <automated>cd backend && go build ./internal/handlers/... 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/handlers/...` ohne Fehler
    - CreateClaimInvitation prüft Leader-Berechtigung via permissionSvc
    - AcceptClaimInvitation ist auth-geschützt (requireMeIdentity)
    - AcceptClaimInvitation mapped ClaimMutationError korrekt auf HTTP-Status (410 für expired, 409 für used/already_verified)
    - Kein rawToken in Audit-Log-Payload
    - invite_link in Response ist absolute URL (appPublicURL + result.InviteLink)
    - Deutsche Fehlermeldungen mit korrekten Umlauten
    - Datei unter 450 Zeilen
  </acceptance_criteria>
  <done>member_claim_invitations_handler.go kompiliert; alle 3 Endpunkte implementiert; Token-Sicherheit (kein rawToken in Logs) eingehalten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Grenze | Beschreibung |
|--------|-------------|
| Browser → /claim-invitations/accept | Unauthentifizierte Anfragen werden durch requireMeIdentity blockiert |
| Leader → /admin/fansubs/:id/group-members/:memberId/claim-invitations | Nur Leader mit ActionFansubGroupInvitationsCreate können Tokens erstellen |
| Admin → /admin/fansubs/:id/member-claims/:id/verify | Nur Leader/Admin per permissionSvc |

## STRIDE Threat Register

| Threat ID | Kategorie | Komponente | Disposition | Mitigationsplan |
|-----------|-----------|-----------|-------------|-----------------|
| T-66-03-01 | Elevation of Privilege | AcceptClaimInvitation — unauthentifiziert | mitigate | requireMeIdentity gibt 401 zurück bevor AcceptInvitation aufgerufen wird |
| T-66-03-02 | Tampering | IDOR: Leader erstellt Einladung für fremden Member | mitigate | Backend prüft fansubGroupID bei CreateInvitation — Repository-Ebene validiert fansub_group_id = :fansubId (Fallstrick 5 aus RESEARCH.md) |
| T-66-03-03 | Information Disclosure | rawToken in Audit-Log | mitigate | Audit-Log-Payload enthält nur member_id, invitation_id — kein token-Feld |
| T-66-03-04 | Spoofing | Token-Injection in Response Body | mitigate | invite_link wird aus appPublicURL + result.InviteLink zusammengesetzt; rawToken aus Repository, nicht aus Request |
| T-66-03-05 | Elevation of Privilege | Claim-Verify ohne Leader-Auth | mitigate | VerifyClaim und RejectClaim nutzen permissionSvc.CanForFansubGroup; 403 bei fehlendem Recht |
| T-66-03-SC | Tampering | keine npm/pip Installs | accept | Nur Go-Handler; keine Paket-Installationen |

</threat_model>

<verification>
1. `go build ./internal/handlers/...` ohne Fehler
2. Grep: "requireMeIdentity" in AcceptClaimInvitation vorhanden
3. Grep: "CanForFansubGroup" in VerifyClaim, RejectClaim, CreateClaimInvitation vorhanden
4. Grep: kein "token" im Audit-Log Payload von AcceptClaimInvitation und CreateClaimInvitation
5. Beide Dateien unter 450 Zeilen
</verification>

<success_criteria>
Beide Handler-Dateien kompilieren fehlerfrei. Auth- und Permission-Guards auf allen Mutations-Endpunkten. Token-Sicherheitsprinzip eingehalten. Fehlermeldungen auf Deutsch.
</success_criteria>

<output>
Erstelle `.planning/phases/66-claiming-verifizierung/66-03-SUMMARY.md` wenn fertig.
</output>
