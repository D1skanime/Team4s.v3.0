---
phase: 31-ui-umbau
plan: 03
type: execute
wave: 3
depends_on:
  - 31-02-PLAN.md
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/ReleaseThemeAssetsSection.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css
  - frontend/src/lib/api.ts
  - backend/internal/handlers/admin_content_release_theme_assets.go
  - backend/internal/repository/admin_content_anime_themes.go
autonomous: true
requirements:
  - P31-SC4
  - P31-SC5
must_haves:
  truths:
    - "Segment editing reuses release/theme asset APIs instead of inventing a parallel upload model."
    - "Generic release-process media stays separate from OP/ED/Karaoke/Insert theme segment assets."
    - "The plan records what is only prepared structurally versus fully wired."
---

<objective>
Prepare the release-specific segment editor and media boundaries so admins can start editing missing release Theme-/Segment assets without confusing them with generic process media.
</objective>

<context>
@.planning/phases/31-ui-umbau/31-CONTEXT.md
@.planning/phases/31-ui-umbau/31-RESEARCH.md
@frontend/src/app/admin/fansubs/[id]/edit/page.tsx
@frontend/src/app/admin/fansubs/[id]/edit/ReleaseThemeAssetsSection.tsx
@frontend/src/lib/api.ts
@backend/internal/handlers/admin_content_release_theme_assets.go
@backend/internal/repository/admin_content_anime_themes.go
@backend/internal/repository/episode_version_repository.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire segment-card click into a release-specific edit surface</name>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
    - frontend/src/app/admin/fansubs/[id]/edit/ReleaseThemeAssetsSection.tsx
    - frontend/src/lib/api.ts
  </read_first>
  <action>When a segment card is clicked, open an inline editor panel below the expanded release or a small side panel scoped to the expanded release. The editor header must include release id, anime title, episode number, theme type, and status. The first wired action should reuse existing release theme asset upload/delete helpers for the selected `theme_id` and release context. If direct `release_id + theme_id` upload is not yet supported by the current helper signature, add the smallest compatible helper that still writes through the existing release theme asset backend path.</action>
  <acceptance_criteria>
    - `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` contains `selectedReleaseSegment`.
    - `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` contains visible copy `Segment bearbeiten`.
    - `frontend/src/lib/api.ts` contains no new endpoint path using `fansub_group_media`.
    - `cd frontend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Document and guard media boundary between theme assets and process media</name>
  <read_first>
    - backend/internal/repository/episode_version_repository.go
    - backend/internal/handlers/admin_content_release_theme_assets.go
    - frontend/src/types/mediaAsset.ts
    - .planning/phases/31-ui-umbau/31-CONTEXT.md
  </read_first>
  <action>Add explicit guardrails in code comments or tests where appropriate: OP/ED/Karaoke/Insert remain Theme-/Segment assets; generic process media uses `release_media` with types such as `image`, `gif`, `video`, `other` and must not be used for Theme segment replacement. If admin write APIs for generic process media are introduced in this plan, they must write to `media_assets`, `media_files`, and `release_media`, and they must not create or read `fansub_group_media` as authoritative runtime state.</action>
  <acceptance_criteria>
    - `Select-String -Path backend/internal/**/*.go,frontend/src/**/*.ts* -Pattern "fansub_group_media"` does not show a new authoritative admin release-media path.
    - `frontend/src/types/mediaAsset.ts` keeps release media types separate from theme extra types.
    - `cd backend && go test ./internal/handlers ./internal/repository ./internal/services -count=1` exits 0.
    - `cd frontend && npm run build` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Live UAT and handoff notes</name>
  <read_first>
    - .planning/phases/31-ui-umbau/31-CONTEXT.md
    - .planning/phases/31-ui-umbau/31-RESEARCH.md
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
  </read_first>
  <action>Run Docker deploy after tests pass. Verify `/admin/fansubs/17/edit` shows `11eyes`, `11eyes: Pink Phantasmagoria`, and `Release #92`; expanding release 92 shows segment cards; clicking a card opens the release-specific segment editor. Record exactly what is fully wired and what is still structural-only in `31-03-SUMMARY.md`.</action>
  <acceptance_criteria>
    - `docker compose build team4sv30-backend team4sv30-frontend` exits 0 if backend changed, otherwise `docker compose build team4sv30-frontend` exits 0.
    - `docker compose up -d team4sv30-backend team4sv30-frontend` or frontend-only equivalent exits 0.
    - `curl.exe -I --max-time 20 http://127.0.0.1:3002/admin/fansubs/17/edit` returns `HTTP/1.1 200 OK`.
    - `.planning/phases/31-ui-umbau/31-03-SUMMARY.md` contains `Release #92`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Phase 31 is verified only when the UI can locate and expand the real Strawhat OVA release and the code boundary between Theme segment assets and generic process media is preserved.
</verification>

<success_criteria>
Admins have a concrete release-context editing entrypoint, and future process-media/member-mapping work can attach to the expanded release UI without reworking the page again.
</success_criteria>

<output>
Create `.planning/phases/31-ui-umbau/31-03-SUMMARY.md` and update Phase 31 UAT notes.
</output>
