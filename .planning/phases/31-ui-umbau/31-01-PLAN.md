---
phase: 31-ui-umbau
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css
  - frontend/src/lib/api.ts
autonomous: true
requirements:
  - P31-SC1
  - P31-SC2
must_haves:
  truths:
    - "`Anime & Releases` is a real tab/section on the Fansub edit page, not a hidden debug-only panel."
    - "No prominent `Releases neu laden` button is added; refresh is automatic after relevant actions or hidden behind future small technical affordances."
    - "Release rows are expandable and stay grounded in Phase-30 release endpoints."
---

<objective>
Turn `/admin/fansubs/:id/edit` into a tabbed Fansub workspace where `Anime & Releases` is a first-class tab and each release row can be expanded.
</objective>

<context>
@.planning/phases/31-ui-umbau/31-CONTEXT.md
@.planning/phases/31-ui-umbau/31-RESEARCH.md
@frontend/src/app/admin/fansubs/[id]/edit/page.tsx
@frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css
@frontend/src/lib/api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert the Fansub edit surface to real top-level tabs</name>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
    - frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css
    - .planning/phases/31-ui-umbau/31-CONTEXT.md
  </read_first>
  <action>Introduce top-level tab state for `basic`, `tags`, `content`, `media`, `links`, `collaboration`, and `releases`. Render `Anime & Releases` as a tab in the page header/profile area, matching the provided UI direction. Keep existing form sections available under their existing labels. Move the current Anime/Releases block into the `releases` tab content. Do not add a visible `Releases neu laden` primary button.</action>
  <acceptance_criteria>
    - `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` contains a tab key or label `Anime & Releases`.
    - `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` does not contain the exact visible button text `Releases neu laden`.
    - `frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css` contains styles for the tab row and active tab state.
    - `cd frontend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make release rows expandable with local selection state</name>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
    - frontend/src/types/fansub.ts
    - frontend/src/lib/api.ts
  </read_first>
  <action>Add `expandedReleaseIds` state keyed by `release.release_id`. Render each release row with a chevron button that toggles expansion. In the expanded row, render a placeholder shell titled `Theme- und Segment-Kontext` with release identity, episode, version count, and a note that details load in the next plan. Keep existing `Versionen` and `Release ansehen` actions available. The expanded shell must be inside the release row/card rather than a global drawer.</action>
  <acceptance_criteria>
    - `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` contains `expandedReleaseIds`.
    - `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` contains visible copy `Theme- und Segment-Kontext`.
    - `frontend/src/app/admin/fansubs/[id]/edit/page.tsx` contains no component/state named `ReleaseDrawer`.
    - `cd frontend && npm run build` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run the frontend typecheck/build and open `/admin/fansubs/17/edit`; verify the page shows `Anime & Releases`, `11eyes`, `11eyes: Pink Phantasmagoria`, and expandable release rows including `Release #92`.
</verification>

<success_criteria>
The Fansub edit page becomes navigable for release work: a user can find the OVA release in context and expand it without leaving the page.
</success_criteria>

<output>
Create `.planning/phases/31-ui-umbau/31-01-SUMMARY.md`.
</output>
