---
phase: 01-ownership-foundations
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/cmd/server/main.go
  - backend/cmd/server/admin_routes.go
  - backend/cmd/server/bootstrap_helpers.go
  - backend/internal/handlers/media_upload.go
  - backend/internal/handlers/media_upload_image.go
  - backend/internal/handlers/media_upload_video.go
  - backend/internal/handlers/media_upload_storage.go
  - backend/internal/handlers/media_upload_test.go
autonomous: true
gap_closure: true
requirements:
  - RLY-04
must_haves:
  truths:
    - "Touched server bootstrap and media upload production files used by Phase 1 are all at or below 450 lines."
    - "Admin anime and upload routes remain fail-closed behind the same auth wiring after the split."
    - "Authenticated upload and delete behavior still preserves the verified actor attribution and operator-safe error behavior."
  artifacts:
    - path: "backend/cmd/server/admin_routes.go"
      provides: "Admin route registration extracted from main.go"
    - path: "backend/cmd/server/bootstrap_helpers.go"
      provides: "Bootstrap and environment helper functions extracted from main.go"
    - path: "backend/internal/handlers/media_upload_image.go"
      provides: "Image upload processing extracted from media_upload.go"
    - path: "backend/internal/handlers/media_upload_video.go"
      provides: "Video upload processing and thumbnail helpers extracted from media_upload.go"
    - path: "backend/internal/handlers/media_upload_storage.go"
      provides: "Storage path, join-table, and cleanup helpers extracted from media_upload.go"
  key_links:
    - from: "backend/cmd/server/admin_routes.go"
      to: "backend/internal/handlers/admin_content_authz.go"
      via: "auth middleware on admin anime and media routes"
      pattern: "CommentAuthMiddlewareWithState"
    - from: "backend/internal/handlers/media_upload.go"
      to: "backend/internal/handlers/media_upload_image.go"
      via: "Upload dispatch for image assets"
      pattern: "processImage"
    - from: "backend/internal/handlers/media_upload.go"
      to: "backend/internal/handlers/media_upload_video.go"
      via: "Upload dispatch for video assets"
      pattern: "processVideo"
---

<objective>
Close the remaining server/bootstrap and upload-handler modularity gaps for RLY-04 without reopening functional scope.

Purpose: Reduce the touched Phase 1 runtime files below the 450-line ceiling while preserving the verified auth wiring, upload actor attribution, audit safety, and operator-facing behavior.
Output: Smaller route/bootstrap and upload handler files with targeted regressions still passing.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-ownership-foundations/01-VERIFICATION.md
@.planning/phases/01-ownership-foundations/01-02-SUMMARY.md
@backend/cmd/server/main.go
@backend/internal/handlers/media_upload.go
@backend/internal/handlers/media_upload_test.go

<interfaces>
From backend/internal/handlers/media_upload.go:
```go
type MediaUploadHandler struct { /* existing fields stay unchanged */ }
func NewMediaUploadHandler(repo repository.MediaUploadRepoTx, storageDir, baseURL, ffmpegPath string) *MediaUploadHandler
func (h *MediaUploadHandler) Upload(c *gin.Context)
func (h *MediaUploadHandler) Delete(c *gin.Context)
```

From backend/cmd/server/main.go:
```go
func main()
func resolveReleaseGrantSecret(cfg config.Config) string
func bootstrapAdminRoleAssignments(ctx context.Context, db *pgxpool.Pool, logger *log.Logger, userIDs []int64) error
func corsMiddleware() gin.HandlerFunc
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split media upload handling into entrypoint, media-type processing, and storage helpers</name>
  <files>backend/internal/handlers/media_upload.go, backend/internal/handlers/media_upload_image.go, backend/internal/handlers/media_upload_video.go, backend/internal/handlers/media_upload_storage.go, backend/internal/handlers/media_upload_test.go</files>
  <behavior>
    - Test 1: Unauthenticated upload requests still fail closed.
    - Test 2: Authenticated upload requests still persist `uploaded_by` and keep existing image/video handling behavior.
    - Test 3: `media_upload.go` ends at or below 450 lines.
  </behavior>
  <action>Read first: `01-VERIFICATION.md`, `01-02-SUMMARY.md`, `backend/internal/handlers/media_upload.go`, and `backend/internal/handlers/media_upload_test.go`. Action: keep `Upload`, `Delete`, constructor, and request validation in `media_upload.go`; move image-specific processing into `media_upload_image.go`, video-specific processing and thumbnail helpers into `media_upload_video.go`, and shared storage/join-table/cleanup helpers into `media_upload_storage.go`. Preserve handler signatures, auth-first execution order, `uploaded_by` propagation, fallback thumbnail behavior, and response/error semantics so the already-verified authority and operator-safe behavior does not drift.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$files=@('backend/internal/handlers/media_upload.go','backend/internal/handlers/media_upload_image.go','backend/internal/handlers/media_upload_video.go','backend/internal/handlers/media_upload_storage.go'); $over=$files | Where-Object { (Get-Content $_).Count -gt 450 }; if ($over) { Write-Error ('Over limit: ' + ($over -join ', ')); exit 1 }; go test ./internal/handlers -run 'TestMediaUpload' -count=1"</automated>
  </verify>
  <done>Acceptance criteria: `media_upload.go` is <=450 lines, helper files are responsibility-scoped, and media upload tests still prove fail-closed auth plus authenticated actor attribution without any API or response regression.</done>
</task>

<task type="auto">
  <name>Task 2: Extract admin route wiring and bootstrap helpers out of server main</name>
  <files>backend/cmd/server/main.go, backend/cmd/server/admin_routes.go, backend/cmd/server/bootstrap_helpers.go</files>
  <action>Read first: `01-VERIFICATION.md`, `01-02-SUMMARY.md`, and `backend/cmd/server/main.go`. Action: extract admin route registration, especially the authenticated admin anime and media upload wiring, into `admin_routes.go`; extract bootstrap/env helper functions such as release secret resolution, admin bootstrap assignment helpers, and small environment checks into `bootstrap_helpers.go`. Keep runtime behavior unchanged: the same middleware must still guard the verified Phase 1 admin anime and media routes, and `main.go` should remain the startup composition root rather than absorbing more helper logic.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$files=@('backend/cmd/server/main.go','backend/cmd/server/admin_routes.go','backend/cmd/server/bootstrap_helpers.go'); $over=$files | Where-Object { (Get-Content $_).Count -gt 450 }; if ($over) { Write-Error ('Over limit: ' + ($over -join ', ')); exit 1 }; go test ./cmd/server ./internal/handlers -run 'TestMediaUpload' -count=1"</automated>
  </verify>
  <done>Acceptance criteria: `main.go` is <=450 lines, route/auth wiring still compiles from the extracted helpers, and the authenticated Phase 1 admin anime/upload routes remain registered exactly as verified.</done>
</task>

</tasks>

<verification>
Confirm all runtime files touched by this plan are <=450 lines and that the targeted handler/server compile-and-test slice still passes with the same authenticated upload behavior.
</verification>

<success_criteria>
RLY-04 is closed for the server and handler portion of Phase 1: no touched runtime production file in this plan exceeds 450 lines, and the verified upload/auth behavior remains intact.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ownership-foundations/01-05-SUMMARY.md`
</output>
