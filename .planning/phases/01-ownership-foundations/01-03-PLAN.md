---
phase: 01-ownership-foundations
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx
  - frontend/src/app/admin/anime/components/shared/AnimeEditorShell.tsx
  - frontend/src/app/admin/anime/components/shared/AnimeEditorShell.test.tsx
  - frontend/src/app/admin/anime/components/shared/AnimeOwnershipBadge.tsx
  - frontend/src/app/admin/anime/create/page.tsx
  - frontend/src/app/admin/anime/hooks/internal/useAnimePatchImpl.ts
  - frontend/src/app/admin/anime/hooks/useAnimeEditor.ts
  - frontend/src/app/admin/anime/types/admin-anime-editor.ts
  - frontend/src/app/admin/anime/utils/anime-editor-ownership.ts
  - frontend/src/app/admin/anime/utils/anime-editor-ownership.test.ts
autonomous: true
requirements:
  - INTK-06
  - RLY-04
must_haves:
  truths:
    - "Admin can open an existing anime in one ownership-aware editor surface with a single save bar."
    - "The editor shows lightweight ownership/source hints in Phase 1 without pulling in deferred full provenance or resync UI."
    - "The shared editor contract is reusable for current edit and future intake flows without leaving create/edit logic as separate monoliths."
  artifacts:
    - path: "frontend/src/app/admin/anime/components/shared/AnimeEditorShell.tsx"
      provides: "Reusable shared editor shell for edit now and intake flows later"
    - path: "frontend/src/app/admin/anime/components/shared/AnimeOwnershipBadge.tsx"
      provides: "Lightweight ownership visibility per D-03 and D-04"
    - path: "frontend/src/app/admin/anime/hooks/useAnimeEditor.ts"
      provides: "Stable shared editor hook boundary instead of route-local divergence"
    - path: "frontend/src/app/admin/anime/utils/anime-editor-ownership.test.ts"
      provides: "Regression tests for Phase 1 ownership labeling rules"
  key_links:
    - from: "frontend/src/app/admin/anime/components/shared/AnimeEditorShell.tsx"
      to: "frontend/src/app/admin/anime/hooks/useAnimeEditor.ts"
      via: "single editor state and save-bar contract"
      pattern: "submit|isDirty|isSubmitting"
    - from: "frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx"
      to: "frontend/src/app/admin/anime/components/shared/AnimeOwnershipBadge.tsx"
      via: "Phase 1 ownership hint rendering"
      pattern: "source|ownership"
    - from: "frontend/src/app/admin/anime/create/page.tsx"
      to: "frontend/src/app/admin/anime/components/shared/AnimeEditorShell.tsx"
      via: "shared editor primitives without full Phase 2 manual-flow expansion"
      pattern: "AnimeEditorShell"
---

<objective>
Extract the shared ownership-aware editor foundation and keep it modular enough for later intake reuse.

Purpose: Phase 1 must preserve one shared editor product surface, not let current edit and create pages continue diverging.
Output: A reusable editor shell, lightweight ownership hints, and frontend tests that keep the Phase 1 surface modular.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-ownership-foundations/01-CONTEXT.md
@.planning/phases/01-ownership-foundations/01-RESEARCH.md
@frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx
@frontend/src/app/admin/anime/hooks/internal/useAnimePatchImpl.ts
@frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts
@frontend/src/app/admin/anime/create/page.tsx
@frontend/src/lib/api.ts

<interfaces>
From frontend/src/app/admin/anime/hooks/internal/useAnimePatchImpl.ts:
```ts
export function useAnimePatch(
  authToken: string,
  onSuccess: (anime: AnimeDetail) => void,
  onError: (msg: string) => void,
  options?: { onRequest?: (value: string | null) => void; onResponse?: (value: string | null) => void },
): AnimePatchState & {
  isDirty: boolean
  submit: (animeID: number) => Promise<void>
  uploadAndSetCover: (file: File, animeID?: number | null) => Promise<void>
  resetFromAnime: (anime: AnimeDetail) => void
}
```

From frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx:
```ts
interface AnimeEditWorkspaceProps {
  anime: AnimeDetail
  authToken: string
  onSaved: (anime: AnimeDetail, message: string) => void
  onError: (message: string) => void
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract a shared anime editor contract and shell from the current edit surface</name>
  <files>frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx, frontend/src/app/admin/anime/components/shared/AnimeEditorShell.tsx, frontend/src/app/admin/anime/components/shared/AnimeEditorShell.test.tsx, frontend/src/app/admin/anime/hooks/internal/useAnimePatchImpl.ts, frontend/src/app/admin/anime/hooks/useAnimeEditor.ts, frontend/src/app/admin/anime/types/admin-anime-editor.ts, frontend/src/app/admin/anime/create/page.tsx</files>
  <read_first>
    - frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx
    - frontend/src/app/admin/anime/components/shared/AnimeEditorShell.tsx
    - frontend/src/app/admin/anime/hooks/internal/useAnimePatchImpl.ts
    - frontend/src/app/admin/anime/create/page.tsx
    - .planning/phases/01-ownership-foundations/01-CONTEXT.md
    - .planning/phases/01-ownership-foundations/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Existing anime edit still renders through one central save-bar workflow after the extraction.
    - Test 2: A dedicated frontend integration test renders both edit and create consumers through the same `AnimeEditorShell` and public `useAnimeEditor` contract.
    - Test 3: The create route can consume the new shared shell or shared form primitives without implementing the full Phase 2 manual-create workflow.
    - Test 4: The extraction reduces pressure on the current 546-line workspace and 575-line create page instead of adding more logic to either file, satisfying RLY-04.
  </behavior>
  <action>Create a reusable editor contract in `frontend/src/app/admin/anime/types/admin-anime-editor.ts` and a public hook boundary in `frontend/src/app/admin/anime/hooks/useAnimeEditor.ts` that wraps the existing patch state and single-save behavior from `useAnimePatch`. Move the shared layout and save-bar structure out of `AnimeEditWorkspace.tsx` into `components/shared/AnimeEditorShell.tsx`. Refactor `AnimeEditWorkspace.tsx` to compose the shell for existing-anime edit, and refactor `create/page.tsx` to adopt the same shell or shared form sections only where this can be done without implementing Phase 2 draft/save behavior yet. Add `frontend/src/app/admin/anime/components/shared/AnimeEditorShell.test.tsx` as the automated regression for this refactor: it must exercise the public `useAnimeEditor` hook through a small test harness and assert that both edit and create contexts render through `AnimeEditorShell` with the same save-bar contract. Preserve D-01, D-02, D-07, and D-08: one surface, one save model, different contexts rather than different products.</action>
  <acceptance_criteria>
    - `Test-Path frontend/src/app/admin/anime/components/shared/AnimeEditorShell.tsx` returns true.
    - `Test-Path frontend/src/app/admin/anime/components/shared/AnimeEditorShell.test.tsx` returns true.
    - `Test-Path frontend/src/app/admin/anime/hooks/useAnimeEditor.ts` returns true.
    - `Select-String -Path frontend/src/app/admin/anime/create/page.tsx -Pattern "AnimeEditorShell|useAnimeEditor"` returns matches.
    - `Select-String -Path frontend/src/app/admin/anime/components/shared/AnimeEditorShell.test.tsx -Pattern "useAnimeEditor|AnimeEditorShell|create|edit"` returns matches.
    - `(Get-Content frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx).Length -le 430`
    - `(Get-Content frontend/src/app/admin/anime/create/page.tsx).Length -le 450`
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/app/admin/anime/components/shared/AnimeEditorShell.test.tsx src/app/admin/anime/utils/anime-helpers.test.ts src/app/admin/anime/utils/studio-helpers.test.ts</automated>
  </verify>
  <done>The edit route uses extracted shared editor primitives, and the create route is aligned onto the same foundation seam without pulling Phase 2 workflow scope forward.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add lightweight ownership hints and frontend regressions for the shared editor</name>
  <files>frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx, frontend/src/app/admin/anime/components/shared/AnimeOwnershipBadge.tsx, frontend/src/app/admin/anime/utils/anime-editor-ownership.ts, frontend/src/app/admin/anime/utils/anime-editor-ownership.test.ts</files>
  <read_first>
    - frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx
    - frontend/src/lib/api.ts
    - .planning/phases/01-ownership-foundations/01-CONTEXT.md
    - .planning/phases/01-ownership-foundations/01-RESEARCH.md
    - .planning/phases/01-ownership-foundations/01-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: A manual anime shows a lightweight manual ownership label.
    - Test 2: A Jellyfin-linked anime shows a lightweight external/link hint without introducing deferred per-field provenance or resync preview UI.
    - Test 3: The ownership-label rules live in a small testable utility instead of being embedded ad hoc in the workspace component.
  </behavior>
  <action>Add `AnimeOwnershipBadge.tsx` and `anime-editor-ownership.ts` to derive simple Phase 1 labels from existing record signals such as Jellyfin/source linkage. Render the badge near the shared editor header or save bar so the admin sees ownership context while editing, per D-03. Do not add Phase 4 details like per-slot provenance, resync diffs, or full field-level ownership matrices. Keep the ownership derivation logic isolated and testable, and wire the badge through `AnimeEditWorkspace.tsx` using the shared shell.</action>
  <acceptance_criteria>
    - `Test-Path frontend/src/app/admin/anime/components/shared/AnimeOwnershipBadge.tsx` returns true.
    - `Select-String -Path frontend/src/app/admin/anime/utils/anime-editor-ownership.ts -Pattern "manual|jellyfin|source|linked"` returns matches.
    - `Select-String -Path frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx -Pattern "AnimeOwnershipBadge"` returns a match.
    - `npm run test -- src/app/admin/anime/utils/anime-editor-ownership.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- src/app/admin/anime/utils/anime-editor-ownership.test.ts</automated>
  </verify>
  <done>The shared editor shows simple ownership/source context that fits Phase 1 scope and is covered by frontend regression tests.</done>
</task>

</tasks>

<verification>
Confirm the shared shell exists, file sizes are under the project ceiling for touched production files, and the ownership badge stays lightweight rather than drifting into deferred provenance scope.
</verification>

<success_criteria>
Existing anime edit now sits on a reusable ownership-aware editor foundation with one save model, lightweight source visibility, and modular seams ready for later intake work.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ownership-foundations/01-03-SUMMARY.md`
</output>
