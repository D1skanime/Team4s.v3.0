---
phase: 145-mitgliedschafts-grundausstattung-in-die-rechte-registry-berf
plan: 04
type: execute
wave: 4
depends_on: ["145-03"]
files_modified: []
autonomous: false
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5, SC-6]

must_haves:
  truths:
    - "The full backend and frontend test/build/lint suites are green (or show only the pre-existing, already-triaged failure baseline) after all Phase 145 changes."
    - "A human has confirmed, in the live running application, that the pseudo-role appears first under Gruppenrollen, is capability-editable, and toggling one of its actions changes a real active member's effective rights."
  artifacts: []
  key_links: []
---

<objective>
Close Phase 145 with the full-suite regression gate 145-VALIDATION.md's Sampling Rate section
requires "before /gsd:verify-work", plus the one Manual-Only Verification 145-VALIDATION.md names
(end-to-end operator confidence that DB seed -> backend cache reload -> frontend matrix -> resolved
member rights all agree) that no isolated unit test can prove by itself.

Purpose: Plans 145-01 through 145-03 each verified their own slice in isolation; this plan is the
single point that (a) re-runs everything together to catch any interaction the isolated plans could
not see (the `loadedCache` package-global sharing risk flagged in 145-01's grounding_facts is the
most likely source of a cross-file regression), and (b) gets the one piece of proof — a human
actually operating the live UI against a real member — that no automated test can substitute for.

Output: a scoped regression-gate pass/fail report and a signed-off live UAT checkpoint, both recorded
in this plan's SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/145-mitgliedschafts-grundausstattung-in-die-rechte-registry-berf/145-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full regression gate across backend and frontend</name>
  <files></files>
  <read_first>.planning/phases/145-mitgliedschafts-grundausstattung-in-die-rechte-registry-berf/145-VALIDATION.md</read_first>
  <action>
  Run, in order, and record each result: (1) `docker compose exec team4sv30-backend go build ./...`;
  (2) `docker compose exec team4sv30-backend go vet ./...`; (3) `docker compose exec team4sv30-backend
  go test ./internal/permissions/... ./internal/repository/... ./internal/handlers/... -count=1` (the
  three packages this phase's `loadedCache`-sharing risk, flagged explicitly in Plan 145-01's
  grounding_facts, could reach); (4) `docker compose exec team4sv30-frontend npx tsc --noEmit`; (5)
  `docker compose exec team4sv30-frontend npx eslint .`; (6) `docker compose exec team4sv30-frontend
  npx vitest run`. For any RED result, determine whether the failing test/file was touched by any of
  Plans 145-01/02/03 (`git diff --stat` against this phase's base commit `b95135e2`) or references
  `loadedCache`/`RoleMembershipBaseline`/`group_member`/`reserved_baseline` — if so, it is this
  phase's regression and must be fixed by extending that test's own fixture with the
  `RoleMembershipBaseline` entry (mirroring the exact fix pattern already applied in Plan 145-01 Task
  2 to `roleMatrixStubData()`/`fullValidCacheData()`), not by weakening
  `validateMembershipBaselineRegistryPresence` or `IsMembershipBaselineAction`. If a RED result is
  unrelated to any phase-145 file or identifier (a pre-existing failure), record it as pre-existing
  baseline noise per this repo's established "Backend Gate Qualification" convention (see prior
  phases' SUMMARY files for the format) and do not attempt to fix it in this plan.

  Additionally run 145-VALIDATION.md's two Success Criterion 3 checks named in its own "Automated
  command" line (these are the phase's only checks that no earlier plan's task runs as a verify step,
  so this plan is their home): (7) the contract-file byte-diff check — confirm
  `shared/contracts/admin-capabilities.yaml`, `shared/contracts/openapi.yaml`, and
  `frontend/src/types/admin-capability.ts` are byte-identical to their state at this phase's base
  commit `b95135e2` (their provenance-enum sections must be untouched by this phase); (8) the
  untouched-file check — confirm `git diff --stat b95135e2..HEAD` prints nothing for
  `frontend/src/app/admin/users/tabs/GuidedRevokeFlow.tsx` and
  `frontend/src/app/admin/users/tabs/userGroupRightsHelpers.ts` (UI-SPEC's explicit "no change"
  contract for these two files). Both are folded into this task's `<verify>` command below.
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-backend go build ./... && docker compose exec -T team4sv30-backend go vet ./... && docker compose exec -T team4sv30-backend go test ./internal/permissions/... ./internal/repository/... ./internal/handlers/... -count=1 && diff <(git show b95135e2:shared/contracts/admin-capabilities.yaml) shared/contracts/admin-capabilities.yaml && diff <(git show b95135e2:shared/contracts/openapi.yaml) shared/contracts/openapi.yaml && diff <(git show b95135e2:frontend/src/types/admin-capability.ts) frontend/src/types/admin-capability.ts && git diff --stat b95135e2..HEAD -- frontend/src/app/admin/users/tabs/GuidedRevokeFlow.tsx frontend/src/app/admin/users/tabs/userGroupRightsHelpers.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 6 commands from the first paragraph of `<action>` have been run and their pass/fail status recorded.
    - Every RED result is triaged as either "phase-145 regression, fixed" or "pre-existing baseline noise, unrelated to this phase's files" — no unexplained red.
    - Zero RED results reference `RoleMembershipBaseline`, `group_member`, `reserved_baseline`, or `membershipBaselineActions` by the end of this task.
    - The contract-file byte-diff checks (`admin-capabilities.yaml`, `openapi.yaml`, `admin-capability.ts`) all report zero diff against the phase-145 base commit.
    - The `git diff --stat` check for `GuidedRevokeFlow.tsx` and `userGroupRightsHelpers.ts` prints nothing.
  </acceptance_criteria>
  <done>The full backend build/vet/test and frontend tsc/eslint/vitest gates have run, with every failure explicitly triaged as either fixed or pre-existing baseline noise, and Success Criterion 3's contract-file and untouched-file proof commands both pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live UAT — pseudo-role visible, editable, and its toggle changes a real member's effective rights</name>
  <files></files>
  <what-built>
    The three rights every active fansub-group member holds (view members, view group media, upload
    group media) now come from a reserved, non-assignable "Mitgliedschafts-Grundausstattung" pseudo-
    role stored in and editable through the same Capability Matrix admin UI as every other role,
    instead of a hardcoded Go list. Startup fails closed if that role's rights are ever missing from
    the database.
  </what-built>
  <action>
  Do not alter application code during this checkpoint. Use the live browser tunnel and real
  user-visible navigation only, exactly per `<how-to-verify>` below, and record the observed outcome
  of each of the 7 steps (including the toggle-off/toggle-on round trip) in this plan's SUMMARY.
  </action>
  <how-to-verify>
    1. Open `http://127.0.0.1:3300/admin/roles` (via the canonical SSH-tunnel path per CLAUDE.md).
    2. Confirm "Mitgliedschafts-Grundausstattung" is the FIRST row under "Gruppenrollen" (before any
       named role like Fansub-Leitung).
    3. Click it. Confirm the "Standardrechte" tab is active by default (not "Inhaber"), and clicking
       "Inhaber" shows only explanatory text, no table, no loading spinner stuck forever.
    4. On "Standardrechte", confirm exactly 3 toggleable actions are visible (grouped under
       "Gruppe"/"Gruppenmedien"): Mitglieder anzeigen, Gruppenmedien ansehen, Gruppenmedien hochladen
       — all currently ON.
    5. Open any other role (e.g. Fansub-Leitung) -> Standardrechte tab. Confirm those same 3 actions
       do NOT appear in its own list, and instead a sentence with a "Grundausstattung öffnen" button
       is shown; clicking it navigates back to the pseudo-role's own Standardrechte tab.
    6. Pick a real active member of any fansub group (via `/admin/users` or `/admin/fansubs/:id`
       group-member editor) and confirm their effective-rights view already shows these 3 actions as
       granted with a "Grundausstattung"/`membership_baseline` provenance label (unchanged copy).
    7. Back on the pseudo-role's Standardrechte tab, toggle OFF "Gruppenmedien hochladen", confirm
       through the impact-preview dialog. Re-check the same member's effective rights: that one action
       should now be gone/denied; the other two baseline actions remain granted. Toggle it back ON
       afterward to restore the pre-phase default state exactly (Success Criterion 2's "no more, no
       fewer" guarantee must hold after this manual toggle round-trip too).
  </how-to-verify>
  <verify>
    <automated>docker compose exec -T team4sv30-db psql -U team4s -d team4s_v2 -c "SELECT action_code FROM role_capabilities WHERE role_code = 'group_member' ORDER BY action_code;" | grep -c "fansub_group"</automated>
  </verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <done>User types "approved" after confirming all 7 steps, including the toggle-off/toggle-on round trip against a real member's effective rights, and the post-checkpoint DB state still shows exactly 3 role_capabilities rows for group_member.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human operator -> live production-shaped dev environment | The one place in this phase a real mutation (toggling the pseudo-role's `fansub_group_media.upload` grant) is performed against the live shared dev database, not a disposable test fixture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-09 | Repudiation | Manual toggle during live UAT (Task 2, step 7) | mitigate | The existing `role_capability.granted`/`role_capability.revoked` audit-log write (unchanged, pre-existing `AdminCapabilityHandler` behavior) records the human operator's UAT toggle exactly like any other admin mutation — no gap introduced by this checkpoint. |
| T-145-10 | Denial of Service | Live toggle-off during UAT temporarily removes a real right from every active member of every fansub group | accept | Step 7 explicitly requires toggling the action back ON before ending the checkpoint; the blast-radius warning copy added in Plan 145-03 ("wirken sich sofort auf alle aktiven Mitglieder aller Fansub-Gruppen aus") makes this consequence explicit to the operator performing it. |

</threat_model>

<verification>
- Task 1's full regression gate is green or fully triaged with zero unexplained red, including the two Success Criterion 3 proof commands (contract-file byte-diff, untouched-file check).
- Task 2's live UAT checkpoint is explicitly approved by the user, and the post-checkpoint database state still shows exactly 3 role_capabilities rows for group_member.
</verification>

<success_criteria>
- All six ROADMAP.md Phase 145 Success Criteria are proven true by the combination of Plans
  145-01/02/03's automated tests AND this plan's live human confirmation.
- No regression to any pre-existing green test anywhere in `backend/internal/permissions`,
  `backend/internal/repository`, `backend/internal/handlers`, or `frontend/src/app/admin/roles`.
</success_criteria>

<output>
Create `.planning/phases/145-mitgliedschafts-grundausstattung-in-die-rechte-registry-berf/145-04-SUMMARY.md` when done
</output>
