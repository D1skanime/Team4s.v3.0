---
phase: 47-member-profile-und-historical-identity
plan: "02"
type: execute
wave: 2
depends_on:
  - 47-01-PLAN.md
files_modified:
  - backend/internal/handlers
  - backend/internal/models
  - backend/internal/permissions
  - backend/internal/repository
  - database/migrations
  - docs
autonomous: true
requirements:
  - MEMBER-PROFILE-01
must_haves:
  truths:
    - "Users may edit only their own archival profile fields."
    - "Avatar upload must reuse the existing media architecture."
    - "Historical credit data remains read-only and never grants app rights."
---

<objective>
Implement the backend profile MVP: own-profile read/update, avatar upload, membership display, optional admin profile access, and audit logging.

Purpose: This is where the historical identity layer becomes a real backend contract instead of an implied future feature.
</objective>

<context>
@.planning/phases/47-member-profile-und-historical-identity/47-01-PLAN.md
@.planning/phases/47-member-profile-und-historical-identity/47-CONTEXT.md
@.planning/phases/47-member-profile-und-historical-identity/47-RESEARCH.md
@backend/internal/repository/member_group_stories_repository.go
@frontend/src/types/fansubNotes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement own-profile read/update endpoints and optional admin profile access</name>
  <read_first>
    - backend/internal/handlers
    - backend/internal/repository
    - backend/internal/permissions
  </read_first>
  <action>
    Implement the backend endpoints for:
    - `GET /api/me/profile`
    - `PUT /api/me/profile`

    Optionally, if the runtime identifier seam is clean:
    - admin read/update endpoints for another profile

    Rules:
    - authenticated user only
    - disabled users cannot update profile
    - own profile only for normal users
    - email and keycloak identifiers are read-only
    - group memberships and group roles are not editable through the profile endpoint
    - historical credit data is read-only enrichment only
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/handlers/... -run "Test.*Profile|Test.*Me"</automated>
  </verify>
  <done>
    - Own-profile backend endpoints exist and enforce the correct editing boundary.
    - Admin profile access is either implemented cleanly or explicitly deferred.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement avatar upload on top of the existing media stack</name>
  <read_first>
    - existing media upload handlers/services/repositories
    - profile aggregate seam from Plan 47-01
  </read_first>
  <action>
    Add avatar upload/update for the current user's profile using the existing media architecture.

    Requirements:
    - image files only
    - file-size limits follow project convention
    - profile stores a media asset reference
    - no blind physical deletion of previous avatar unless the existing media policy already defines it
    - no orphaned DB/file state
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/handlers/... -run "Test.*Avatar|Test.*Upload"</automated>
  </verify>
  <done>
    - Profile avatar upload reuses the current media pipeline.
    - Unsupported file types are rejected cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add profile capabilities, Keycloak account-link helper, and audit</name>
  <read_first>
    - backend/internal/permissions
    - existing capability and audit seams
  </read_first>
  <action>
    Add or extend the profile capability and helper seam for:
    - `canViewOwnProfile`
    - `canEditOwnProfile`
    - `canUploadOwnAvatar`
    - `canOpenKeycloakAccount`
    - `canViewMemberships`
    - `canViewHistoricalCredits`

    Also add a backend helper/config seam for the Keycloak Account Console URL and reuse the audit seam for:
    - profile changed
    - avatar uploaded/changed
    - visibility changed
    - admin changed another profile, if that path exists
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/permissions/... ./internal/handlers/... -run "Test.*Capabilities|Test.*Profile"</automated>
  </verify>
  <done>
    - Profile capabilities and Keycloak account-link support exist.
    - Audit captures the critical profile mutations through the existing seam.
  </done>
</task>

</tasks>

<verification>
Automated:
1. `cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/handlers/...`
2. `cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/repository/...`
3. `cd C:/Users/admin/Documents/Team4s/backend && go build ./...`

Behavioral:
1. Authenticated user can read/update own profile only.
2. Email/keycloak IDs are not writable through Team4s.
3. Avatar upload uses the existing media stack and rejects invalid types.
</verification>

<success_criteria>
- Backend profile endpoints exist and enforce the archival-data-only contract.
- Avatar upload is integrated via the existing media architecture.
- Capabilities and audit cover the key profile actions.
</success_criteria>
