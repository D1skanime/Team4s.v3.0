---
phase: 30-fansub-releases-api-endpunkte
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - backend/internal/handlers/admin_content_release_theme_assets.go
  - backend/internal/models/admin_release_theme_assets.go
  - backend/internal/repository/admin_content_anime_themes.go
  - frontend/src/lib/api.ts
  - frontend/src/app/admin/fansubs/[id]/edit/ReleaseThemeAssetsSection.tsx
  - frontend/src/types/fansub.ts
autonomous: true
requirements:
  - P30-SC3
  - P30-SC4
must_haves:
  truths:
    - "Theme-asset UX consumes explicit release context instead of using theme-assets responses as hidden release discovery."
    - "Release-adjacent media stays on the existing `media_assets` seam."
    - "Phase 30 does not convert `fansub_group_media` into an active product dependency."
---

<objective>
Adopt the explicit release API in the frontend and release-theme flow so the UI knows its release context without piggybacking on theme-asset side effects.
</objective>

<context>
@.planning/phases/30-fansub-releases-api-endpunkte/30-CONTEXT.md
@.planning/phases/30-fansub-releases-api-endpunkte/30-RESEARCH.md
@.planning/phases/30-fansub-releases-api-endpunkte/30-01-PLAN.md
@frontend/src/lib/api.ts
@frontend/src/app/admin/fansubs/[id]/edit/ReleaseThemeAssetsSection.tsx
@backend/internal/handlers/admin_content_release_theme_assets.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed frontend helpers for release context</name>
  <read_first>
    - frontend/src/lib/api.ts
    - frontend/src/types/fansub.ts
    - .planning/phases/30-fansub-releases-api-endpunkte/30-RESEARCH.md
  </read_first>
  <action>Add typed frontend API helpers and response types for the new release endpoints from Plan 01. Model release context explicitly, including canonical-release resolution and direct release detail loading, so consumers do not have to infer release identity from unrelated payloads.</action>
  <acceptance_criteria>
    - `Select-String -Path frontend/src/lib/api.ts -Pattern "getAdminFansubAnimeReleases|getAdminCanonicalFansubRelease|getAdminRelease"` returns matches.
    - `Select-String -Path frontend/src/types/fansub.ts -Pattern "AdminFansubRelease|canonical release|release_id"` returns matches.
    - `cd frontend && npx tsc --noEmit` passes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire ReleaseThemeAssetsSection to use explicit release context</name>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/ReleaseThemeAssetsSection.tsx
    - frontend/src/lib/api.ts
    - backend/internal/handlers/admin_content_release_theme_assets.go
  </read_first>
  <action>Update `ReleaseThemeAssetsSection` so it loads release context from the new explicit release endpoint(s) first, then uses theme-asset endpoints purely for theme-asset data. Preserve the current upload/delete behavior and error handling, but remove the hidden dependency on the theme-assets list response as the only source of `release_id` truth.</action>
  <acceptance_criteria>
    - `Select-String -Path frontend/src/app/admin/fansubs/[id]/edit/ReleaseThemeAssetsSection.tsx -Pattern "getAdminCanonicalFansubRelease|getAdminRelease"` returns matches.
    - The section no longer depends on theme-asset responses alone to discover `releaseID`.
    - `cd frontend && npm.cmd run build` passes.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Show that the fansub edit UI can resolve release context explicitly and still upload/list/delete theme assets against the existing release-media seam.
</verification>

<success_criteria>
Release-aware frontend flows use explicit release APIs, while theme-asset endpoints return to being about theme assets only.
</success_criteria>

<output>
After completion, create `.planning/phases/30-fansub-releases-api-endpunkte/30-02-SUMMARY.md`
</output>
