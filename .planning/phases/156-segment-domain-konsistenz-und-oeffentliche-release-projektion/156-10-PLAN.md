---
phase: 156-segment-domain-konsistenz-und-oeffentliche-release-projektion
plan: 10
type: execute
wave: 5
depends_on: ["156-01", "156-02", "156-03", "156-04", "156-05", "156-06", "156-07", "156-08", "156-09", "156-11"]
files_modified:
  - docs/audits/2026-09-XX-segment-domain-consistency/REPORT.md
  - docs/audits/2026-09-XX-segment-domain-consistency/REPRODUCE.md
  - docs/audits/2026-09-XX-segment-domain-consistency/TABLES.md
  - docs/audits/2026-09-XX-segment-domain-consistency/VALIDATION.md
autonomous: true
requirements: [P156-19]

must_haves:
  truths:
    - "The full backend test suite (go build, go vet, go test ./internal/repository/... ./internal/handlers/... ./internal/permissions/...) is green with TEAM4S_PHASE117_TEST_DSN set"
    - "The frontend ThemeTimeline Vitest suite and a full type-check are green"
    - "Migration 0161 (up and down) is verified against the live dev DB one final time in sequence with all seven prior backend plans applied"
    - "The Vorher/Nachher audit report follows the Phase 154/155 house style (REPORT/REPRODUCE/TABLES/VALIDATION four-file structure)"
    - "The working tree is clean (no stray uncommitted files) at phase close"
  artifacts:
    - path: "docs/audits/2026-09-XX-segment-domain-consistency/REPORT.md"
      provides: "Vorher/Nachher summary: additive-vs-reconciling assignments, suppressed-vs-shown release segments, label-heuristic-vs-role-code credits, range-vs-assignment-based project timeline"
  key_links: []
---

<objective>
Close Phase 156: run the complete backend + frontend test matrix one final time across everything
the prior nine plans built, verify the migration end-to-end against the live dev database, and
produce the Vorher/Nachher audit report documenting what changed and why -- following the exact
four-file structure (`REPORT.md`/`REPRODUCE.md`/`TABLES.md`/`VALIDATION.md`) Phase 154/155 already
established.

Purpose: P156-19 is explicit that a full green test matrix, verified migration/backfill, and a
documented before/after report are named phase deliverables, not optional cleanup. This is also
where the 156-VALIDATION.md TBD rows get their final sign-off check against real plan/task IDs.
Output: four audit documents plus a final green test run across the whole phase's backend and
frontend surfaces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-CONTEXT.md
@.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-VALIDATION.md
@docs/audits/2026-09-11-fansub-project-performance/REPORT.md
</context>

<interfaces>
<!-- Template structure to replicate, docs/audits/2026-09-11-fansub-project-performance/ -->
REPORT.md (headline summary + methodology + limits), REPRODUCE.md (exact commands run), TABLES.md
(raw before/after numbers), VALIDATION.md (checklist proving the report's claims are backed by
either direct code reads or actually-executed test commands, not inference).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Full backend + frontend test matrix, migration verification, gap check against Plans 156-01..09/11</name>
  <files>none (verification-only task, no source changes expected)</files>
  <read_first>.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-VALIDATION.md (Per-Task Verification Map -- confirm every TBD-* row now has a real plan reference), all nine prior plans' SUMMARY.md files once they exist (156-01 through 156-09, 156-11)</read_first>
  <action>
    Run, in order: (1) `docker run --rm -v /home/d1sk/team4s:/workspace -w /workspace/backend golang:1.25-alpine go build ./... && go vet ./...` (full build/vet, not scoped to one package). (2) Resolve `TEAM4S_PHASE117_TEST_DSN` from the running `team4sv30-backend` container's `DATABASE_URL` (swap DB name to the phase117 test schema per the established convention) and run `go test ./internal/repository/... ./internal/handlers/... ./internal/permissions/... -count=1` with that DSN set -- every test added across Plans 156-01 through 156-09 must be green, not skipped (confirm none report `--- SKIP` due to a missing DSN). (3) `docker compose up -d --build team4sv30-backend` to pick up all backend changes, then a smoke check: `curl -s http://192.168.235.196:8092/health` (or the project's established health endpoint) returns healthy. (4) `docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit -p tsconfig.json && npm run test -- ThemeTimeline"` -- both must be green. (5) Verify migration 0161 one final time against the live dev DB (not just the phase117 test schema): confirm it is already applied (from Plan 156-01) and re-run `migrate down` then `migrate up` once more in this final pass to prove end-to-end reversibility survives everything built on top of it in Plans 156-02 through 156-09. (6) Cross-check every row in `156-VALIDATION.md`'s "Per-Task Verification Map" table -- replace every remaining `TBD-*`/`TBD` placeholder with the real plan ID (e.g. `156-02` for the reconciliation row) and flip `Status` from `⬜ pending` to `✅ green` for every row now provable via the commands just run; if any row cannot be marked green, that is a genuine gap -- do not mark it green without a passing command backing it. (7) Confirm working tree cleanliness: `git status --short` shows no unexpected untracked/modified files outside what this phase's plans intentionally changed.
  </action>
  <acceptance_criteria>
    - `go build ./... && go vet ./...` exit code 0
    - `go test ./internal/repository/... ./internal/handlers/... ./internal/permissions/... -count=1` with `TEAM4S_PHASE117_TEST_DSN` set: all PASS, zero SKIP among Phase-156-added tests
    - `npx tsc --noEmit` and `npm run test -- ThemeTimeline`: exit code 0
    - `156-VALIDATION.md` has zero remaining `TBD-*` placeholder rows
    - `git status --short` shows only intentional Phase 156 changes
  </acceptance_criteria>
  <verify>
    <automated>docker run --rm -v /home/d1sk/team4s:/workspace -w /workspace/backend golang:1.25-alpine sh -c "go build ./... && go vet ./..."</automated>
  </verify>
  <done>Every backend and frontend test this phase added is green (not skipped), the migration round-trips cleanly one final time, 156-VALIDATION.md's TBD placeholders are all resolved, and the working tree is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Vorher/Nachher audit report (REPORT/REPRODUCE/TABLES/VALIDATION)</name>
  <files>docs/audits/2026-09-XX-segment-domain-consistency/REPORT.md, docs/audits/2026-09-XX-segment-domain-consistency/REPRODUCE.md, docs/audits/2026-09-XX-segment-domain-consistency/TABLES.md, docs/audits/2026-09-XX-segment-domain-consistency/VALIDATION.md</files>
  <read_first>docs/audits/2026-09-11-fansub-project-performance/REPORT.md (full file, house style to replicate exactly -- headline summary bolded, Methodik-und-Grenzen section, explicit "no speed claim without a measured constant" discipline), docs/audits/2026-09-11-fansub-project-performance/REPRODUCE.md, docs/audits/2026-09-11-fansub-project-performance/TABLES.md, docs/audits/2026-09-11-fansub-project-performance/VALIDATION.md, backend/internal/repository/segment_origin_query_budget_test.go (Plan 156-09's pinned query-budget constant, cite the real observed number)</read_first>
  <action>
    Create the directory `docs/audits/2026-09-XX-segment-domain-consistency/` (replace `XX` with the actual execution date) with four files matching the Phase 155 template's structure exactly. `REPORT.md`: bolded headline summary covering the four behavioral changes (additive -> reconciling assignments with the guard preserved; release page suppression removed, replaced with range/origin labels; label-substring credit heuristic -> stable role-code allow-list with dynamic origin-based projection; project-page range-based derivation -> assignment-based with bundled first-occurrence), a Methodik-und-Grenzen section stating explicitly which numbers are directly measured (Plan 156-09's pinned query-budget constant, cited by its real value) versus which are structural/qualitative claims (no invented performance percentages without a measured constant backing them, matching this phase's explicit "Beschleunigungsversprechen ohne Messbeleg" prohibition). `REPRODUCE.md`: the exact commands from Task 1, reproducible verbatim. `TABLES.md`: raw before/after facts -- e.g. "assignment reconciliation: additive-only (Quick-Task 260819-lm5) -> Soll-Ist sync (this phase)", "release-page suppression: `suppressSegmentsAlreadyVisibleOnPreviousEpisode` present -> deleted", "segment-credit source: current release's own contributors + label substring -> origin release's contributors + role-code allow-list", "project-timeline derivation: `start_episode`/`end_episode` range comparison -> `theme_segment_assignments` join + bundled first-occurrence", "new migration: 0161 (origin_release_version_id + index + backfill)", the Plan 156-09 query-budget constant value. `VALIDATION.md`: a checklist proving each REPORT.md claim traces to either a direct code read (cite file:line) or an actually-executed test/command from Task 1/REPRODUCE.md -- no claim without one of these two backings, matching this phase's evidentiary discipline.
  </action>
  <acceptance_criteria>
    - `docs/audits/2026-09-XX-segment-domain-consistency/` contains all four files: REPORT.md, REPRODUCE.md, TABLES.md, VALIDATION.md
    - every quantitative claim in REPORT.md (e.g. the Plan 156-09 query-budget constant) traces to a value actually observed in a prior plan's SUMMARY.md or test run, not invented
    - REPRODUCE.md's commands, when run, are the same commands Task 1 already executed successfully
    - VALIDATION.md has no unchecked claim without a code citation or executed-command reference
  </acceptance_criteria>
    <verify>
    <automated>ls /home/d1sk/team4s/docs/audits/2026-09-*-segment-domain-consistency/*.md 2>/dev/null | wc -l</automated>
  </verify>
  <done>Four audit documents exist, follow the established Phase 154/155 structure, and every claim in REPORT.md is backed by a code citation or an executed command in REPRODUCE.md -- no unproven acceleration or coverage claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new -- this plan is verification/documentation only | -- |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-18 | Repudiation | Audit report making unsubstantiated correctness/performance claims | mitigate | VALIDATION.md requires every REPORT.md claim to cite either a direct code read or an executed test command, matching the Phase 155 evidentiary template |
</threat_model>

<verification>
Full backend/frontend test matrix green; migration round-trips cleanly; 156-VALIDATION.md fully resolved; four audit documents complete and evidence-backed; working tree clean.
</verification>

<success_criteria>
Phase 156 closes with a fully green, non-skipped test matrix, a verified reversible migration, and a documented, evidence-backed Vorher/Nachher report matching house style.
</success_criteria>

<output>
Create `.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-10-SUMMARY.md` when done
</output>
