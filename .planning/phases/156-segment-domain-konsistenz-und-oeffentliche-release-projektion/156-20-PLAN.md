---
phase: 156-segment-domain-konsistenz-und-oeffentliche-release-projektion
plan: 20
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/internal/permissions/segment_credit_roles.go
  - backend/internal/permissions/segment_credit_roles_test.go
autonomous: true
requirements: [GAP-09]

must_haves:
  truths:
    - "permissions.SegmentCreditRoleCodes enthaelt exakt die acht Codes {translator, timer, karaoke_fx, typesetter, editor, quality_checker, encoder, designer} -- encoder und designer sind nicht mehr grundsaetzlich ausgeschlossen"
    - "permissions.SegmentCreditPreselectionRoleCodes ist eine neue, zweite Variable in derselben Datei mit exakt den alten sechs Codes {translator, timer, karaoke_fx, typesetter, editor, quality_checker} und ist per Test als echte (strikte) Teilmenge von SegmentCreditRoleCodes bewiesen"
    - "SegmentCreditLabelForRoles liefert fuer typesetter jetzt 'Karaoke-Typesetting' (nicht mehr 'Typesetting / Logo'), fuer encoder 'Karaoke-Encoding' und fuer designer 'Logo'; bei mehreren Rollen stehen encoder/designer wegen der festen Listenreihenfolge am Ende der kommagetrennten Ausgabe"
    - "Der alte, jetzt falsche Doku-Kommentar ueber SegmentCreditRoleCodes ('encoder bleibt der einzige ausgeschlossene Beitrags-relevante Rolle') existiert nicht mehr woertlich im Quelltext und ist durch eine GAP-09-korrekte Beschreibung ersetzt"
    - "TestSegmentCreditLabelCoverageIsDeckungsgleich beweist weiterhin die Deckungsgleichheit zwischen SegmentCreditRoleCodes und segmentCreditLabels, jetzt fuer alle acht Codes"
  artifacts:
    - path: "backend/internal/permissions/segment_credit_roles.go"
      provides: "SegmentCreditRoleCodes (8 Codes), SegmentCreditPreselectionRoleCodes (6 Codes, neu), segmentCreditLabels (8 Eintraege, typesetter umbenannt), SegmentCreditLabelForRoles (Verhalten unveraendert, neue Codes automatisch am Ende)"
  key_links:
    - from: "backend/internal/permissions/segment_credit_roles.go (SegmentCreditLabelForRoles)"
      to: "backend/internal/permissions/segment_credit_roles_test.go (TestSegmentCreditLabelForRoles/TestSegmentCreditLabelForRolesFixedOrder)"
      via: "direkter Funktionsaufruf innerhalb desselben Pakets, keine Indirektion"
      pattern: "SegmentCreditLabelForRoles\\(\\[\\]string\\{\"encoder\""
---

<objective>
Schliesst den zentralen Definitionsteil von `156-UAT.md` GAP-09 ("Encoding und Design als
Segment-Credit (nur manuell), Karaoke-Beschriftungen geschärft", Auftraggeber-Entscheidung im Chat,
2026-09-15). GAP-09 ersetzt **ausdrücklich** die bisherige Regel "Encoder erscheint niemals als
Segment-Credit" (`156-USER-REQUEST.md` "Nicht anzeigen: Encoding", GAP-01 Auftragspunkte 3/13,
Regressionsfall D). Designer war bislang mangels Freigabe komplett ausgeschlossen und wird jetzt
erstmals freigegeben.

`permissions.SegmentCreditRoleCodes` (`backend/internal/permissions/segment_credit_roles.go`)
steuert heute DREI Dinge zugleich aus einer einzigen Liste: den Filter der öffentlichen
Segment-Credit-Projektion, die Segment-Beschriftungen (`segmentCreditLabels`), und die
automatische Vorauswahl (konsumiert von `theme_segment_contributor_preselection.go`, Plan 156-21
in dieser Gap-Serie). GAP-09 trennt Punkt 3 (Vorauswahl) von den ersten beiden: encoder/designer
werden für Sichtbarkeit und Beschriftung freigegeben, aber NIE automatisch vorausgewählt. Dieser
Plan legt dafür die alleinige zentrale Definition an -- `SegmentCreditRoleCodes` waechst auf acht
Codes, eine neue, benannte zweite Liste `SegmentCreditPreselectionRoleCodes` traegt weiterhin nur
die alten sechs Codes. Beide Listen leben in DERSELBEN Datei im `permissions`-Paket (keine
Rollenliste ausserhalb dieses Pakets, keine Kopie in Frontend/Handler/SQL).

Dieser Plan aendert AUSSCHLIESSLICH die zentrale Definition + ihre eigenen Unit-Tests (DB-frei).
Die Konsumenten (Vorauswahl-Wiring, Migrations-Aequivalenztest, oeffentliche
Projektions-Integrationstests) folgen in Plan 156-21 (Wave 2, `depends_on: [156-20]`), das
Frontend-Textupdate in Plan 156-22 (Wave 1, unabhaengig).

Purpose: Eine einzige, korrekte, testbar deckungsgleiche Quelle fuer segmentrelevante Rollen UND
ihre Beschriftungen UND (getrennt) die Vorauswahl-Teilmenge -- kein Rollen-Duplikat, keine
drift-gefaehrdete zweite Kodierung.
Output: `segment_credit_roles.go` mit zwei Listen + erweitertem Label-Mapping; vollstaendig
korrigierte `segment_credit_roles_test.go`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-UAT.md

<interfaces>
<!-- Executor: use these EXACT identifiers/values. Do not reinvent them. -->

**Current full content of `backend/internal/permissions/segment_credit_roles.go`** (62 lines, read
this file directly before editing -- it is short enough for one Read call):
The file currently exports `var SegmentCreditRoleCodes = []string{RoleTranslator, RoleTimer,
RoleKaraokeFX, RoleTypesetter, RoleEditor, RoleQualityChecker}` (6 codes), an unexported
`segmentCreditLabels` map with 6 entries including `RoleTypesetter: "Typesetting / Logo"`, and
`SegmentCreditLabelForRoles(roleCodes []string) string` which iterates `SegmentCreditRoleCodes` IN
ORDER and joins matching labels with `", "`. `RoleEncoder = "encoder"` and `RoleDesigner =
"designer"` are ALREADY defined as constants in `backend/internal/permissions/permissions.go`
(lines 74/77) -- do not redeclare them, just reference them.

**Target new/changed declarations** (exact identifiers, exact order -- `SegmentCreditRoleCodes`'s
element order controls `SegmentCreditLabelForRoles`'s output order, so encoder/designer MUST be
appended at the end, not inserted alphabetically or anywhere else):

```go
var SegmentCreditRoleCodes = []string{RoleTranslator, RoleTimer, RoleKaraokeFX, RoleTypesetter, RoleEditor, RoleQualityChecker, RoleEncoder, RoleDesigner}

var SegmentCreditPreselectionRoleCodes = []string{RoleTranslator, RoleTimer, RoleKaraokeFX, RoleTypesetter, RoleEditor, RoleQualityChecker}

var segmentCreditLabels = map[string]string{
	RoleTranslator:     "Karaoke-Übersetzung",
	RoleTimer:          "Karaoke-Timing",
	RoleKaraokeFX:      "Karaoke-FX",
	RoleTypesetter:     "Karaoke-Typesetting",
	RoleEditor:         "Karaoke-Edit",
	RoleQualityChecker: "Karaoke-Qualitätsprüfung",
	RoleEncoder:        "Karaoke-Encoding",
	RoleDesigner:       "Logo",
}
```

`SegmentCreditLabelForRoles`'s function body is NOT changed -- it already iterates
`SegmentCreditRoleCodes` in fixed order and looks up `segmentCreditLabels[code]`, so extending
both vars/the map to 8 entries is sufficient; no new logic needed.

**Doc comment on `SegmentCreditRoleCodes`**: the current comment (lines 5-22) contains this
now-FALSE sentence that MUST be removed/replaced: "`encoder` remains the only deliberately
excluded contributor-relevant role -- an encoder must never appear as a segment credit, regardless
of any other role they hold (156-UAT.md)." Replace with a GAP-09-accurate description: encoder and
designer are segment-relevant like every other code in the list, visible ONLY when explicitly
selected as a `theme_segment_contributor` (same rule as every other role), and this rule
supersedes the old `156-USER-REQUEST.md`/GAP-01/Regressionsfall-D "encoder never" rule. Also add a
short paragraph documenting the NEW `SegmentCreditPreselectionRoleCodes` var directly above its
declaration: it exists because automatic preselection
(`ensureThemeSegmentContributorsPreselectedTx`, Plan 156-21) must keep excluding encoder/designer
even though the credit/label list no longer does -- name Plan 156-21 as the consumer, and name
`TestSegmentCreditPreselectionRoleCodes`'s subset proof.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the central role/label definition to GAP-09's eight codes, test-first</name>
  <files>backend/internal/permissions/segment_credit_roles_test.go, backend/internal/permissions/segment_credit_roles.go</files>
  <read_first>
    backend/internal/permissions/segment_credit_roles.go (the WHOLE file, 62 lines -- current
    `SegmentCreditRoleCodes`, `segmentCreditLabels`, `SegmentCreditLabelForRoles`, and every doc
    comment word for word);
    backend/internal/permissions/segment_credit_roles_test.go (the WHOLE file, 95 lines -- every
    existing test function, especially `TestSegmentCreditRoleCodes`'s `"never contains encoder"`
    subtest and `TestSegmentCreditLabelForRolesExcludesEncoder`, BOTH of which encode the now-FALSE
    premise and must be corrected, not silently left in place asserting something no longer true);
    backend/internal/permissions/permissions.go lines 68-81 (the `RoleEncoder = "encoder"` and
    `RoleDesigner = "designer"` constants this plan reuses -- do not redeclare them);
    `.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-UAT.md`
    GAP-09 section (lines 656-754, the full binding target behavior, points 1-3 and 6 specifically
    -- not just this plan's objective summary).
  </read_first>
  <behavior>
    - `SegmentCreditRoleCodes` contains exactly `{"translator", "timer", "karaoke_fx",
      "typesetter", "editor", "quality_checker", "encoder", "designer"}` (`ElementsMatch`) AND
      explicitly contains `"encoder"` and `"designer"` (no more `NotContains` assertion for
      encoder -- that premise is gone).
    - `SegmentCreditPreselectionRoleCodes` contains exactly the old six codes, never contains
      `"encoder"` or `"designer"`, and is a true (element-count-strictly-smaller) subset of
      `SegmentCreditRoleCodes` -- proven via `require.Subset` plus a length comparison, not by
      `ElementsMatch` alone (which would also pass for two identical slices).
    - `SegmentCreditLabelForRoles([]string{"typesetter"})` returns exactly `"Karaoke-Typesetting"`
      (not `"Typesetting / Logo"`).
    - `SegmentCreditLabelForRoles([]string{"encoder"})` returns exactly `"Karaoke-Encoding"`;
      `SegmentCreditLabelForRoles([]string{"designer"})` returns exactly `"Logo"`.
    - `SegmentCreditLabelForRoles([]string{"encoder", "translator"})` returns exactly
      `"Karaoke-Übersetzung, Karaoke-Encoding"` (translator first per fixed list order, encoder at
      the end -- 156-UAT.md GAP-09's literal Pflichttest "Person mit Übersetzung und Encoding
      ausgewählt").
    - `SegmentCreditLabelForRoles([]string{"raw_provider", "translator"})` returns exactly
      `"Karaoke-Übersetzung"` (raw_provider replaces encoder as this file's standing example of a
      role that is genuinely, permanently excluded -- 156-UAT.md GAP-09's literal Pflichttest "nur
      raw_provider ausgewählt -> erscheint nicht", proven here with `[]string{"raw_provider"}`
      alone returning `""`).
    - `TestSegmentCreditLabelCoverageIsDeckungsgleich` still passes unmodified in its assertion
      logic (it derives both sides from the package vars, so it needs no literal-list edits) and
      now proves coverage across all eight codes.
  </behavior>
  <action>
    In `segment_credit_roles_test.go`: replace `TestSegmentCreditRoleCodes`'s `"never contains
    encoder"` subtest with a `"contains encoder and designer as of 156-UAT.md GAP-09"` subtest
    (two `require.Contains` calls) and update the `"contains exactly the six segment-relevant role
    codes"` subtest's expected slice and name to the eight-element list. Add a new
    `TestSegmentCreditPreselectionRoleCodes` function proving `SegmentCreditPreselectionRoleCodes`
    per `<behavior>` above (exact six codes, excludes encoder/designer, true subset of
    `SegmentCreditRoleCodes` via `require.Subset` + `require.Less` on lengths). Update the
    `typesetter` case in `TestSegmentCreditLabelForRoles`'s table to expect `"Karaoke-Typesetting"`
    and add two new table rows for `encoder`/`designer`. Extend
    `TestSegmentCreditLabelForRolesFixedOrder` with the encoder-appended-at-end assertion from
    `<behavior>`. Replace `TestSegmentCreditLabelForRolesExcludesEncoder` (whose name and premise
    are now false) with `TestSegmentCreditLabelForRolesExcludesUnapprovedRoles`, asserting the
    `raw_provider` cases from `<behavior>`. Run `go test ./internal/permissions/... -v -count=1`
    inside the `golang:1.25-alpine` container (see `<verify>`) and confirm every NEW/changed
    assertion FAILS against the current, unmodified `segment_credit_roles.go` (RED) before editing
    that file. Then edit `segment_credit_roles.go`: replace the `SegmentCreditRoleCodes` literal,
    add the new `SegmentCreditPreselectionRoleCodes` var, extend `segmentCreditLabels` with the
    `encoder`/`designer` entries and the renamed `typesetter` entry, and replace the stale doc
    comment per `<interfaces>`'s "Doc comment on SegmentCreditRoleCodes" instructions -- do not
    change `SegmentCreditLabelForRoles`'s body. Re-run the full test file and confirm GREEN.
  </action>
  <acceptance_criteria>
    - `docker run --rm -v /home/d1sk/team4s:/workspace -w /workspace/backend golang:1.25-alpine go
      test ./internal/permissions/... -v -count=1` shows every test in
      `segment_credit_roles_test.go` PASS, including the new
      `TestSegmentCreditPreselectionRoleCodes` and `TestSegmentCreditLabelForRolesExcludesUnapprovedRoles`.
    - `grep -c "encoder.*must never appear as a segment credit" backend/internal/permissions/segment_credit_roles.go`
      returns `0`.
    - `grep -n "\"encoder\", \"designer\"\|RoleEncoder, RoleDesigner" backend/internal/permissions/segment_credit_roles.go`
      matches the `SegmentCreditRoleCodes` line (encoder/designer are the LAST two elements).
    - `grep -c "Karaoke-Typesetting" backend/internal/permissions/segment_credit_roles.go` returns
      at least `1`; `grep -c "Typesetting / Logo" backend/internal/permissions/segment_credit_roles.go`
      returns `0`.
    - `go build ./... && go vet ./...` succeed from `backend/` (run inside the same container or
      locally if a Go 1.25 toolchain is available).
    - `wc -l backend/internal/permissions/segment_credit_roles.go` reports 90 or fewer (well under
      the 450-line project cap).
  </acceptance_criteria>
  <verify>
    <automated>docker run --rm -v /home/d1sk/team4s:/workspace -w /workspace/backend golang:1.25-alpine go test ./internal/permissions/... -v -count=1</automated>
  </verify>
  <done>segment_credit_roles.go exports SegmentCreditRoleCodes (8 codes) and SegmentCreditPreselectionRoleCodes (6 codes, true subset), segmentCreditLabels covers all 8 codes with the GAP-09-confirmed labels (typesetter renamed, encoder/designer added), the stale doc comment is corrected, and every test in segment_credit_roles_test.go passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Package-internal constant/var definitions (no I/O) | This plan touches zero request/response paths, zero SQL, zero user input -- it is a pure Go constant/map/test change inside `permissions`, consumed only through already-existing, already-reviewed call sites in other files (unchanged by this plan). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-41 | Information Disclosure | `SegmentCreditRoleCodes` now includes `encoder`/`designer` | accept | This is the explicit, Auftraggeber-confirmed GAP-09 decision (2026-09-15) -- encoder/designer become visible ONLY when an admin explicitly selects them as a segment contributor (enforced in Plan 156-21's repository layer, not here); this plan only widens the central role catalog and label map, it does not itself expose any data. `member_slug` visibility (Auftragspunkt 13) is untouched by this plan. |
| T-156-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | N/A -- zero new dependencies; pure Go constant/map edits in an existing file, no new imports. |
</threat_model>

<verification>
- Task 1: `go test ./internal/permissions/... -v -count=1` green inside `golang:1.25-alpine`, covering every existing and new test in `segment_credit_roles_test.go`; `go build`/`go vet` clean; grep confirms the stale doc comment is gone and the label rename landed; file stays well under 450 lines.
- No DB/integration test is run or needed by this plan -- `permissions` package tests are pure unit tests.
</verification>

<success_criteria>
- `permissions.SegmentCreditRoleCodes` is the single central list of eight segment-relevant role codes, encoder/designer included, defined once.
- `permissions.SegmentCreditPreselectionRoleCodes` is the single central, smaller preselection-only list, proven to be a true subset of the credit list.
- `permissions.SegmentCreditLabelForRoles` returns the GAP-09-confirmed labels, including the renamed "Karaoke-Typesetting" and the new "Karaoke-Encoding"/"Logo", in fixed list order.
- No consumer file is touched by this plan -- Plan 156-21 wires `theme_segment_contributor_preselection.go` and the affected repository-level tests against these new identifiers.
</success_criteria>

<output>
Create `.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-20-SUMMARY.md` when done
</output>
