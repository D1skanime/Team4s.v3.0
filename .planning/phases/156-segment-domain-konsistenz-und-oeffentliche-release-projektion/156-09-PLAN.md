---
phase: 156-segment-domain-konsistenz-und-oeffentliche-release-projektion
plan: 09
type: execute
wave: 4
depends_on: ["156-04", "156-07"]
files_modified:
  - backend/internal/repository/segment_origin_query_budget_test.go
  - backend/internal/repository/segment_credit_role_filter_test.go
autonomous: true
requirements: [P156-16, P156-17]

must_haves:
  truths:
    - "loadReleaseSegments' bundled origin+credit load issues a constant number of queries regardless of how many segments a release has (no N+1 per segment, member, release, or credit)"
    - "The new index on origin_release_version_id is justified by a captured query-plan showing it is used, not added on speculation"
    - "The SegmentCreditRoleCodes filter has its own table-driven unit test proving encoder/quality_checker exclusion independent of any database"
  artifacts:
    - path: "backend/internal/repository/segment_origin_query_budget_test.go"
      provides: "queryCounter-based regression gate pinning loadReleaseSegments' query count as a named constant"
    - path: "backend/internal/repository/segment_credit_role_filter_test.go"
      provides: "table-driven unit test of the segment-credit role-code filtering logic"
  key_links:
    - from: "backend/internal/repository/segment_origin_query_budget_test.go"
      to: "backend/internal/repository/query_counter.go"
      via: "queryCounter as pgx.QueryTracer on a dedicated pgxpool.Pool"
      pattern: "queryCounter"
---

<objective>
Prove, with a pinned regression test, that Plan 156-07's origin-based bundled credit load does not
introduce N+1 behavior as segment/contributor counts grow (P156-16), and capture the query-plan
evidence for the one new index this phase adds (`idx_theme_segments_origin_release_version`,
migration 0161) rather than asserting its usefulness without proof (P156-17).

Purpose: CONTEXT.md is explicit that this phase claims no unproven performance wins and adds no
blind indexes -- this plan is where that discipline gets its evidence, following the exact
`queryCounter` pattern Phase 155 already established and proved reusable.
Output: a query-budget regression test with a small release (1 segment) and a larger release
(3+ segments, 2+ contributors each) asserting the SAME query count, plus a captured `EXPLAIN`
showing the new index in use, documented in this plan's SUMMARY for Plan 156-10's audit report to
cite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-CONTEXT.md
@.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-RESEARCH.md
@backend/internal/repository/query_counter.go
</context>

<interfaces>
<!-- Reusable tracer, query_counter.go (full file, already read this session) -->
```go
type queryCounter struct { queries atomic.Int64 }
func (c *queryCounter) TraceQueryStart(ctx context.Context, _ *pgx.Conn, _ pgx.TraceQueryStartData) context.Context { c.queries.Add(1); return ctx }
func (c *queryCounter) reset() { c.queries.Store(0) }
func (c *queryCounter) count() int { return int(c.queries.Load()) }
```

<!-- Phase-155 usage pattern to copy, fansub_project_resolver_query_budget_test.go:37-61,98-155 -->
```go
counter := &queryCounter{}
config.ConnConfig.Tracer = counter
// ... reset() immediately before the measured call, count() after ...
require.Equalf(t, smallCount, largeCount, "constant query budget violated: ...")
require.Equalf(t, phase155ProjectResolverConstantQueryBudget, largeCount, "... update only with an intentional, documented loader change")
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Query-budget regression test for the bundled origin+credit load</name>
  <files>backend/internal/repository/segment_origin_query_budget_test.go</files>
  <read_first>backend/internal/repository/query_counter.go (full file), backend/internal/repository/fansub_project_resolver_query_budget_test.go (full file, the exact two-assertion pattern to replicate), backend/internal/repository/release_detail_public_repository_helpers.go (post-156-07 loadReleaseSegments, to know its exact call shape)</read_first>
  <behavior>
    - Test 1: seed a release version with 1 segment (origin release version has 1 contributor) and a release version with 3 segments (each with a distinct origin release version, each origin having 2+ contributors) -- call `loadReleaseSegments` for each, `reset()` the counter immediately before each call, `count()` immediately after; the SMALL and LARGE call counts must be EQUAL (constant budget, independent of segment/contributor count)
    - Test 2: the measured count also equals a new named constant (e.g. `phase156SegmentOriginConstantQueryBudget`) declared in this test file, so a future accidental N+1 regression fails loudly with a clear "update only with an intentional, documented loader change" message, matching the Phase-155 convention exactly
  </behavior>
  <action>
    Create `backend/internal/repository/segment_origin_query_budget_test.go` following `fansub_project_resolver_query_budget_test.go`'s exact structure: open a `pgxpool.Pool` with `config.ConnConfig.Tracer = counter` (a fresh `&queryCounter{}`), skip cleanly if `TEAM4S_PHASE117_TEST_DSN` is unset (same convention as every other test in this table family). Seed two scenarios in the same test function or two `t.Run` subtests: (a) "small" -- one release version, one theme segment assigned to it, `origin_release_version_id` pointing at a release version with exactly one public contributor; (b) "large" -- one release version with THREE theme segments assigned to it, each with a DIFFERENT origin_release_version_id, each origin having at least two public contributors with different role codes. Call `repo.loadReleaseSegments(ctx, animeID, groupID, releaseVersionID, version, episodeNumber, nil)` for each scenario (the `contributors` parameter is now unused post-156-07's rewrite -- pass `nil`, confirm this compiles against the post-156-07 signature). `counter.reset()` immediately before each call, `counter.count()` immediately after. Assert `require.Equal(t, smallCount, largeCount, "constant query budget violated: bundled origin+credit load must not scale with segment/contributor count")`. Declare `const phase156SegmentOriginConstantQueryBudget = <observed value>` (fill in the actual number observed when the test is first run against a real DB, matching Phase-155's convention of pinning the literal observed count, not a guessed one) and assert `require.Equal(t, phase156SegmentOriginConstantQueryBudget, largeCount, "segment origin/credit query budget changed -- update only with an intentional, documented loader change")`.
  </action>
    <acceptance_criteria>
    - the 'small' (1 segment) and 'large' (3 segments, multiple contributors) scenarios issue the identical query count
    - the measured count equals the named pinned constant declared in the test file
    - the test skips cleanly (does not fail) when `TEAM4S_PHASE117_TEST_DSN` is unset
  </acceptance_criteria>
  <verify>
    <automated>docker run --rm -v /home/d1sk/team4s:/workspace -w /workspace/backend golang:1.25-alpine sh -c "go build ./... && go vet ./..."</automated>
  </verify>
  <done>The test compiles, and (once run against TEAM4S_PHASE117_TEST_DSN) proves the small and large scenarios issue the identical, pinned query count -- no N+1 as segment or contributor count grows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Table-driven unit test for the segment-credit role filter + index-plan evidence capture</name>
  <files>backend/internal/repository/segment_credit_role_filter_test.go</files>
  <read_first>backend/internal/permissions/permissions.go (post-156-01 SegmentCreditRoleCodes), backend/internal/repository/release_detail_public_repository_helpers.go (post-156-07, the exact filtering logic this test exercises -- whatever small exported/testable helper Plan 156-07 used to check role-code overlap)</read_first>
  <behavior>
    - Test 1: a contributor with `RoleCodes: ["translator"]` is included (in the allow-list)
    - Test 2: a contributor with `RoleCodes: ["encoder"]` is excluded
    - Test 3: a contributor with `RoleCodes: ["encoder", "translator"]` (mixed) is included -- ANY overlapping relevant role includes the contributor, documented as intentional per Plan 156-07's threat_model note (T-156-13)
    - Test 4: a contributor with `RoleCodes: []` (empty) is excluded
    - Test 5: `quality_checker` is excluded exactly like `encoder`
  </behavior>
  <action>
    Create `backend/internal/repository/segment_credit_role_filter_test.go` with a table-driven test exercising the exact filtering logic added in Plan 156-07's `loadReleaseSegments` (call the actual function/logic used there directly -- if Plan 156-07 inlined the filter rather than extracting a named helper, extract a small unexported testable function `isSegmentRelevantContributor(roleCodes []string) bool` as part of THIS task if it does not already exist, and update `loadReleaseSegments` to call it, so this negative case has a real, directly-executed unit test rather than a duplicated inline check). Cover all five behavior cases above via `require.Equal` against `permissions.SegmentCreditRoleCodes`. Additionally, capture the query-plan evidence for `idx_theme_segments_origin_release_version` (migration 0161): run `EXPLAIN (ANALYZE, BUFFERS) SELECT origin_release_version_id FROM theme_segments WHERE origin_release_version_id = <seeded id>` against the dev/test DB via `docker compose exec -T team4sv30-backend psql "$DATABASE_URL" -c "EXPLAIN (ANALYZE, BUFFERS) SELECT ..."` and record the plan output (showing `Index Scan using idx_theme_segments_origin_release_version` or equivalent) verbatim in this task's SUMMARY.md -- this is the P156-17 "neuer Index nur mit Query-Plan-Beleg" evidence; no source file changes are needed for the index itself, it already exists from migration 0161 (Plan 156-01).
  </action>
    <acceptance_criteria>
    - a contributor with an allow-listed role code is included
    - a contributor with only `encoder` or only `quality_checker` is excluded
    - a contributor with a mixed allow-listed + non-allow-listed role set is included (documented as intentional)
    - a contributor with an empty role-code list is excluded
    - the captured EXPLAIN output shows `idx_theme_segments_origin_release_version` is actually used, recorded in this task's SUMMARY.md
  </acceptance_criteria>
  <verify>
    <automated>docker run --rm -v /home/d1sk/team4s:/workspace -w /workspace/backend golang:1.25-alpine sh -c "go build ./... && go vet ./... && go test ./internal/repository/... -run TestSegmentCreditRoleFilter -v"</automated>
  </verify>
  <done>Table-driven unit test passes without any DB dependency (pure function), and the EXPLAIN output proving the origin index is actually used is captured for the audit report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new -- this plan is test-only, adding no new reachable code path | -- |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-17 | Denial of Service | Undetected future N+1 regression in the bundled origin+credit load | mitigate | Pinned constant-query-budget test fails loudly on any query-count change, forcing a deliberate update |
</threat_model>

<verification>
`go build`/`go vet` green; role-filter unit test passes with no DB dependency; query-budget test (once run against `TEAM4S_PHASE117_TEST_DSN`) proves constant query count between small and large scenarios; EXPLAIN output captured as index-usage evidence.
</verification>

<success_criteria>
No N+1 regression risk goes undetected, and the one new index this phase adds has documented query-plan proof of use, not speculation.
</success_criteria>

<output>
Create `.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-09-SUMMARY.md` when done
</output>
