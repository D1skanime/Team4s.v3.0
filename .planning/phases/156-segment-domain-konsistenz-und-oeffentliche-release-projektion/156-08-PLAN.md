---
phase: 156-segment-domain-konsistenz-und-oeffentliche-release-projektion
plan: 08
type: execute
wave: 4
depends_on: ["156-06", "156-07"]
files_modified:
  - frontend/src/types/releaseDetail.ts
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimelineSegmentDetails.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/releaseDetailPageData.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.test.tsx
autonomous: true
requirements: [P156-14, P156-15]

must_haves:
  truths:
    - "ThemeTimeline no longer derives segment type from its own label/string maps -- it renders the backend-canonical segment.type value"
    - "The OP/ED/INSERT/KARA classification decision itself is gone from the frontend; only a small presentational CSS-class/label lookup keyed off the already-canonical type may remain"
    - "Segment participant entries with a member_slug render as a link to the project-context member route, not plain text"
    - "ThemeTimeline.tsx and its new sibling file each stay at or below 450 lines"
  artifacts:
    - path: "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimelineSegmentDetails.tsx"
      provides: "SegmentDetails/SelectionSurface extracted, with member-link participant rendering"
  key_links:
    - from: "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimelineSegmentDetails.tsx"
      to: "frontend/src/lib/fansubProjectRoutes.ts"
      via: "projectPath + '/mitwirkende/' + encodeURIComponent(memberSlug), reusing buildPublicFansubProjectMemberPath's established suffix shape"
      pattern: "mitwirkende"
---

<objective>
Finish the frontend half of Workstream E/F2: `ThemeTimeline.tsx` holds its OWN type-classification
world (`TYPE_LABELS`, `TYPE_STYLE_KEYS` with variant keys like `'OP KARA'`) -- the frontend mirror of
the SQL `LIKE` heuristic Plan 156-06 already deleted on the backend. This plan deletes that
fachliche duplication and renders the backend-canonical `segment.type` directly, keeping only
presentational (CSS class/German label) mapping. It also wires the first production consumer of
`buildPublicFansubProjectMemberPath` (built in Phase 155, unused until now) so segment participants
with a `member_slug` link to the project-context member route instead of rendering as plain text.

Purpose: P156-15 requires ThemeTimeline to stop holding its own type world; P156-14 requires
segment-related member clicks to land on the project member route. The file is already at 396/450
lines, so this plan proactively splits it.
Output: `ThemeTimelineSegmentDetails.tsx` (new sibling), a slimmed `ThemeTimeline.tsx`, member links
on participants, updated Vitest coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-CONTEXT.md
</context>

<interfaces>
<!-- Type maps to DELETE (fachliche part), ThemeTimeline.tsx:31-59 -->
```ts
const TYPE_LABELS: Record<string, string> = { OP: 'Opening', ED: 'Ending', IN: 'Insert', MIDDLE: 'Middle', KARA: 'Karaoke', OTHER: 'Other' }
const TYPE_STYLE_KEYS: Record<string, keyof typeof styles> = { OP: 'typeOp', 'OP KARA': 'typeOp', ... }
```
<!-- Functions to extract verbatim, lines 143-195 -->
SegmentDetails({ segment, episodeNumber }), SelectionSurface({ segment, selected, playable, onSelect, episodeNumber })
<!-- Participant text to replace, line 161 -->
```tsx
{segment.participants.map((p) => `${p.name} · ${p.role_label}`).join(', ')}
```
<!-- Established conditional-Link STRUCTURE to copy (not its /members/ target), FansubContributorsSection.tsx:13-19 -->
```tsx
if (contributor.member_slug !== null) return <Link href={'/members/' + contributor.member_slug}>{name}</Link>
return <span>{name}</span>
```
<!-- releaseDetailPageData.tsx already has canonicalProjectPath in scope, passed to ReleaseDetailHero/ReleaseNavigation -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Split ThemeTimeline, drop the frontend type world, add project-context member links</name>
  <files>frontend/src/types/releaseDetail.ts, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.tsx, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimelineSegmentDetails.tsx, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/releaseDetailPageData.tsx</files>
  <read_first>frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.tsx (full file), frontend/src/lib/fansubProjectRoutes.ts (full file), frontend/src/components/fansubs/FansubContributorsSection.tsx:1-40 (conditional-Link structure, not its target path), frontend/src/types/releaseDetail.ts, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/releaseDetailPageData.tsx</read_first>
  <action>
    In `releaseDetail.ts`, add `role_codes: string[]` and `member_slug: string | null` to `PublicReleaseContributor` (mirrors Plan 156-05's backend JSON fields). Create `ThemeTimelineSegmentDetails.tsx`: move `SegmentDetails`/`SelectionSurface` (lines 143-195) verbatim, importing `styles from './ThemeTimeline.module.css'` and `Badge` from `@/components/ui`. Add prop `projectPath?: string | null` to `SegmentDetails`. Replace the plain-text participant line with a per-participant conditional render: when `projectPath` is truthy AND `participant.member_slug` is non-null, render `<Link href={projectPath + '/mitwirkende/' + encodeURIComponent(participant.member_slug)}>{participant.name}</Link>` followed by ` · ${participant.role_label}` as plain text; otherwise render the exact old plain-text line unchanged. Copy the conditional STRUCTURE from `FansubContributorsSection.tsx`'s `renderContributorName`, never its `/members/` target. Add `import Link from 'next/link'`. In `ThemeTimeline.tsx`: delete `TYPE_LABELS`, `TYPE_STYLE_KEYS`, `typeKey`, `segmentTypeLabel` entirely. Replace their one legitimate remaining use (mapping type to a CSS class for visual styling) with a small presentational-only lookup `SEGMENT_TYPE_STYLE_CLASS: Record<string, keyof typeof styles>` keyed by the four canonical codes (`OP`,`ED`,`INSERT`,`KARA`) plus a `typeOther` fallback -- this is allowed per CONTEXT.md ("rein darstellerische Zuordnungen duerfen im Frontend bleiben") because it maps an already-canonical value to a CSS class, it does not decide what that value is. For display text, keep a minimal local `SEGMENT_TYPE_DISPLAY_LABEL` map (German labels only, same four codes) if the backend sends short codes rather than ready-made German labels -- confirm the actual value shape against Plan 156-06's `CanonicalSegmentType` output before deciding whether this map is even needed. Import `SegmentDetails`/`SelectionSurface` from the new sibling file instead of defining them locally. Add a new `projectPath?: string | null` prop to `ThemeTimelineProps`, threaded to every `SelectionSurface`/`SegmentDetails` call site in this file. In `releaseDetailPageData.tsx`, pass `projectPath={canonicalProjectPath}` on the existing `<ThemeTimeline ... />` call. Confirm both files end at or below 450 lines.
  </action>
  <acceptance_criteria>
    - `grep -c "TYPE_LABELS\|TYPE_STYLE_KEYS" ThemeTimeline.tsx` returns 0
    - `grep -n "member_slug" ThemeTimelineSegmentDetails.tsx` shows a conditional Link render, not an unconditional one
    - `wc -l ThemeTimeline.tsx ThemeTimelineSegmentDetails.tsx` both <= 450
    - `npx tsc --noEmit` passes with no new errors in these files
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit -p tsconfig.json"</automated>
  </verify>
  <done>Type-classification logic is gone from the frontend, only presentational CSS/label mapping remains, participants with a member_slug render as project-context links, both files are within the line limit.</done>
</task>

<task type="auto">
  <name>Task 2: Update ThemeTimeline Vitest coverage for the split and member links</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.test.tsx</files>
  <read_first>frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.test.tsx (full file, current state)</read_first>
  <action>
    Update the existing test file's fixture segment data to include `role_codes`/`member_slug` on participants (matching the extended `PublicReleaseContributor` type). Add a case asserting that a participant WITH `member_slug` renders an `<a>`/Link pointing at `{projectPath}/mitwirkende/{memberSlug}` when `projectPath` is passed to `ThemeTimeline`, and a case asserting plain-text rendering (no link) when `projectPath` is omitted or `member_slug` is null. Add a case asserting the rendered type label/CSS class matches the already-canonical `segment.type` value from fixtures (e.g. `'OP'`), not a derived/heuristic value -- pass a fixture `type` value the OLD heuristic would have mis-handled (e.g. a value containing `'kara'` as a substring in an unrelated position) and confirm the component renders it as-is / via the small presentational map, not via any remaining string-matching logic. Update any existing assertions that referenced the deleted `TYPE_LABELS`/`segmentTypeLabel` behavior.
  </action>
    <acceptance_criteria>
    - a participant with a non-null `member_slug` and a `projectPath` prop renders a Link to `{projectPath}/mitwirkende/{memberSlug}`
    - a participant with `member_slug: null`, or no `projectPath` prop, renders plain text with no Link
    - a segment whose `type` value would have been mis-classified by the OLD heuristic renders correctly via the new presentational-only mapping
    - `npm run test -- ThemeTimeline` passes with zero failures
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npm run test -- ThemeTimeline"</automated>
  </verify>
  <done>Vitest suite passes, covering member-link presence/absence and canonical-type rendering without any residual heuristic assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Public release page -> member link rendering | `member_slug` already arrives pre-visibility-gated from the backend (Plan 156-05); this plan only renders it, adding no new exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-16 | Information Disclosure | Rendering a member link for a non-public profile | accept | `member_slug` is already `null` for non-public profiles at the backend (Plan 156-05's visibility-gated CASE); the frontend's conditional-render structure additionally never renders a Link when `member_slug` is null, so no path exists for this to leak |
</threat_model>

<verification>
`tsc --noEmit` clean; Vitest `ThemeTimeline` suite green; both files at or below 450 lines.
</verification>

<success_criteria>
ThemeTimeline renders backend-canonical segment types with only presentational local mapping, participants link to the project-context member route when a slug is available, and the file is split within the line budget.
</success_criteria>

<output>
Create `.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-08-SUMMARY.md` when done
</output>
