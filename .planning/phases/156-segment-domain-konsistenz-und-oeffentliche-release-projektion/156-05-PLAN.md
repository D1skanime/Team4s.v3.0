---
phase: 156-segment-domain-konsistenz-und-oeffentliche-release-projektion
plan: 05
type: execute
wave: 2
depends_on: ["156-01"]
files_modified:
  - backend/internal/repository/public_effective_contributors.go
  - backend/internal/repository/release_detail_public_repository.go
  - backend/internal/repository/public_effective_contributors_test.go
autonomous: true
requirements: [P156-08, P156-09]

must_haves:
  truths:
    - "loadPublicEffectiveContributors preserves each contributor's raw role_code set, not just the aggregated German label string"
    - "PublicReleaseContributor carries RoleCodes and a visibility-gated MemberSlug, sourced from the same one shared function that already feeds both the project page and the release page"
    - "No second contributor-loading function is created -- the existing batch-capable loadPublicEffectiveContributors is extended in place"
    - "member_slug respects existing profile visibility rules (only exposed when the member's profile is public), reusing the established CASE pattern verbatim"
  artifacts:
    - path: "backend/internal/repository/public_effective_contributors.go"
      provides: "publicContributorAccumulator.roleCodes map + role_codes SQL aggregate + member_slug column, still batch-loaded by []int64 release version IDs"
    - path: "backend/internal/repository/release_detail_public_repository.go"
      provides: "PublicReleaseContributor extended with RoleCodes []string and MemberSlug *string"
  key_links:
    - from: "backend/internal/repository/public_effective_contributors.go"
      to: "backend/internal/repository/release_detail_public_repository_helpers.go (Plan 156-07)"
      via: "PublicReleaseContributor.RoleCodes, filtered against permissions.SegmentCreditRoleCodes"
      pattern: "RoleCodes"
---

<objective>
Extend the ONE shared contributor-loading function (`loadPublicEffectiveContributors`) that already
feeds both the project page and the release page, so it stops throwing away `acr.role_code` after
aggregating it into a German label string, and starts exposing a visibility-gated `member_slug`.
This is the shared-load-point requirement for this phase: do not build a second loading function
for segment credits.

Purpose: Plan 156-07 needs stable role codes (not label substrings) to filter segment-relevant
contributors, and Plan 156-08 needs `member_slug` to build the project-context member link. Both
depend on this one function's output shape growing two fields, with its existing batch-by-ID
signature and precedence logic completely unchanged.
Output: `PublicReleaseContributor.RoleCodes`/`MemberSlug`, a `roleCodes` accumulator map alongside
the existing `roleLabels` map, and a unit/integration test proving both survive the aggregation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-CONTEXT.md
@.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-RESEARCH.md
@.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-PATTERNS.md
</context>

<interfaces>
<!-- Full current function already read this session, public_effective_contributors.go:43-124 -->
<!-- Accumulator struct to extend, lines 34-37 -->
```go
type publicContributorAccumulator struct {
	contributor PublicReleaseContributor
	roleLabels  map[string]struct{}
}
```

<!-- Existing role-label aggregate to add a sibling for, SQL lines 67-71 -->
```sql
COALESCE(
	ARRAY_AGG(DISTINCT COALESCE(rd.label_de, acr.role_code) ORDER BY COALESCE(rd.label_de, acr.role_code))
		FILTER (WHERE acr.role_code IS NOT NULL),
	ARRAY[]::text[]
) AS role_labels,
```

<!-- Aggregation loop to extend, lines 178-183 -->
```go
for _, roleLabel := range candidate.RoleLabels {
	roleLabel = strings.TrimSpace(roleLabel)
	if roleLabel != "" { accumulator.roleLabels[roleLabel] = struct{}{} }
}
```

<!-- Established member_slug visibility-gated CASE to copy verbatim, confirmed in group_contributors_repository.go:67/120, anime_contributions_public_repository.go:77/201, domain_projection_repository.go:103/195 -->
```sql
CASE WHEN m.profile_visibility = 'public' THEN m.public_slug ELSE NULL END AS member_slug
```

<!-- DTO to extend, release_detail_public_repository.go:34-40 -->
```go
type PublicReleaseContributor struct {
	FansubGroupID int64   `json:"fansub_group_id"`
	MemberID      int64   `json:"member_id"`
	Name          string  `json:"name"`
	RoleLabel     string  `json:"role_label"`
	AvatarURL     *string `json:"avatar_url"`
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve role codes + add visibility-gated member_slug through loadPublicEffectiveContributors</name>
  <files>backend/internal/repository/public_effective_contributors.go, backend/internal/repository/release_detail_public_repository.go, backend/internal/repository/public_effective_contributors_test.go</files>
  <read_first>backend/internal/repository/public_effective_contributors.go (full file, current state, already read this session), backend/internal/repository/release_detail_public_repository.go (PublicReleaseContributor struct, lines 34-40), backend/internal/repository/group_contributors_repository.go:60-70 (member_slug CASE, read-only reference for the exact SQL shape)</read_first>
  <behavior>
    - Test 1: a contributor with two roles (e.g. translator + timer) on a release version -- `PublicReleaseContributor.RoleCodes` contains both `"translator"` and `"timer"` (unique, order-independent) after aggregation, alongside the pre-existing joined `RoleLabel` string (unchanged behavior)
    - Test 2: a member with `profile_visibility='public'` and a non-empty `public_slug` -- `MemberSlug` is populated with that slug
    - Test 3: a member with `profile_visibility` != `'public'` -- `MemberSlug` is `nil`, even though the contributor itself is still returned (role/credit visibility and profile-slug visibility are independent gates)
    - Test 4: the function's batch signature (`releaseVersionIDs []int64`) and precedence/override resolution behavior (group-level override replacing anime-wide defaults) are unchanged -- existing behavior around `is_override`/`is_public` filtering is not touched by this plan
  </behavior>
  <action>
    In `release_detail_public_repository.go`, add two fields to `PublicReleaseContributor`: `RoleCodes []string \`json:"role_codes"\`` and `MemberSlug *string \`json:"member_slug"\`` (immediately after `RoleLabel`, following this struct's existing `json` tag naming convention). In `public_effective_contributors.go`: (1) add `roleCodes map[string]struct{}` to `publicContributorAccumulator`, alongside the existing `roleLabels` map. (2) Extend the SQL query's `SELECT` to add a sibling aggregate next to the existing `role_labels` one: `COALESCE(ARRAY_AGG(DISTINCT acr.role_code) FILTER (WHERE acr.role_code IS NOT NULL), ARRAY[]::text[]) AS role_codes`, and scan it into a new `RoleCodes []string` field on `publicContributionCandidate`. (3) Add the `member_slug` column to the same SELECT, joining `members m` (already joined in this query at `JOIN members m ON m.id = ac.member_id`) via `CASE WHEN m.profile_visibility = 'public' THEN m.public_slug ELSE NULL END AS member_slug` -- copy this CASE shape verbatim from the established pattern (`group_contributors_repository.go:67/120` etc.), do not invent a different visibility check; scan into a new `MemberSlug *string` field on `publicContributionCandidate`, and add it to the `GROUP BY` clause (`m.profile_visibility, m.public_slug` alongside the existing `m.nickname, m.display_name, member_avatar.file_path, v.name`). (4) In `resolvePublicEffectiveContributors`, when constructing a new accumulator, initialize `roleCodes: make(map[string]struct{})` and copy `candidate.MemberSlug` onto `accumulator.contributor.MemberSlug` the same way `AvatarURL` is already carried over (first non-nil wins is fine, `member_slug` for the same member/group pair will always be identical across candidate rows). (5) Add a sibling loop next to the existing `roleLabels` population loop (lines 178-183): iterate `candidate.RoleCodes`, trim, and populate `accumulator.roleCodes`. (6) When building the final `result` slice, add `roles := make([]string, 0, len(accumulator.roleCodes)); for code := range accumulator.roleCodes { roles = append(roles, code) }; sort.Strings(roles); accumulator.contributor.RoleCodes = roles` immediately alongside the existing `RoleLabel` join/sort logic. Do not change the function's public signature, its batch-loading shape, or its override/precedence resolution logic -- this task is purely additive to the per-contributor output shape.
  </action>
    <acceptance_criteria>
    - `PublicReleaseContributor.RoleCodes` contains all of a multi-role contributor's role codes (not just the joined label)
    - `PublicReleaseContributor.MemberSlug` is populated for a `profile_visibility='public'` member with a non-empty `public_slug`
    - `PublicReleaseContributor.MemberSlug` is nil for a non-public-profile member, even though the contributor itself is still returned
    - `loadPublicEffectiveContributors`'s existing batch signature, override precedence, and is_public filtering are unchanged (existing callers/tests for project + release page still pass)
  </acceptance_criteria>
  <verify>
    <automated>docker run --rm -v /home/d1sk/team4s:/workspace -w /workspace/backend golang:1.25-alpine sh -c "go build ./... && go vet ./..."</automated>
  </verify>
  <done>PublicReleaseContributor carries RoleCodes and a visibility-gated MemberSlug, both sourced from loadPublicEffectiveContributors' existing batch query, and (once run against a Postgres-backed test) the four behavior cases pass without any change to the function's existing batch/precedence contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Public release/project pages -> contributor data | Already-public-gated data (`is_public_on_anime_page=true AND visibility='public'`), extended with two new fields under the SAME existing gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-01 (shared with RESEARCH.md Security Domain) | Information Disclosure | member_slug exposing a non-public member's profile identifier | mitigate | Reuses the established `CASE WHEN m.profile_visibility = 'public' THEN m.public_slug ELSE NULL END` gate verbatim, same pattern already in production in three other repositories; dedicated test (Test 3) proves NULL on non-public profiles |
| T-156-12 | Information Disclosure | RoleCodes exposing role information beyond what the existing is_public/visibility gate already permits | accept | RoleCodes is derived from the exact same already-public-filtered candidate rows RoleLabel already exposes -- no new row reaches this function that wasn't already publicly visible via its label |
</threat_model>

<verification>
`go build`/`go vet` green; behavior tests (Postgres-backed) prove role-code preservation, visibility-gated member_slug, and unchanged batch/precedence behavior.
</verification>

<success_criteria>
The one shared contributor-loading function now exposes stable role codes and a visibility-respecting member slug, ready for Plan 156-07 (credit filtering) and Plan 156-08 (member link) to consume without a second loading function.
</success_criteria>

<output>
Create `.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-05-SUMMARY.md` when done
</output>
