---
phase: 156-segment-domain-konsistenz-und-oeffentliche-release-projektion
plan: 11
type: execute
wave: 3
depends_on: ["156-04"]
files_modified:
  - frontend/src/types/admin.ts
  - frontend/src/lib/api.ts
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx
autonomous: false
requirements: [P156-18]

must_haves:
  truths:
    - "An admin editing a shared segment can see its current origin (which assigned release version its credits come from) and correct it"
    - "Setting an origin is a single explicit action, never a step that blocks saving the segment itself"
    - "Only assigned release versions are offered as origin choices -- an admin cannot pick an unassigned one and hit a confusing 409"
    - "The new control uses only @/components/ui primitives (Select, no hand-built <select>)"
  artifacts:
    - path: "frontend/src/types/admin.ts"
      provides: "AdminThemeSegment.origin_release_version_id field"
    - path: "frontend/src/lib/api.ts"
      provides: "setAnimeSegmentOrigin(animeId, segmentId, releaseVersionId) calling PUT .../segments/:segmentId/origin"
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx"
      provides: "minimal origin display + correction control, gated to shared segments with >=1 assigned episode"
  key_links:
    - from: "frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx"
      to: "frontend/src/lib/api.ts"
      via: "onSetOrigin(releaseVersionID) prop calling setAnimeSegmentOrigin"
      pattern: "setAnimeSegmentOrigin"
---

<objective>
Give the existing admin segment editor a minimal, additive way to see and correct a segment's
origin release version (P156-18), without any visual redesign -- reusing the same assigned-episode
data the panel already renders as "Folge N" chips, and the global `@/components/ui` `Select`
primitive for the one new interactive element.

Purpose: Plan 156-04 built the validated backend endpoint; without a frontend surface, "administrativ
korrigierbar" (P156-06) has no admin-reachable path. CONTEXT.md is explicit this must not become a
UI redesign -- this plan adds exactly one small, optional control to the already-existing shared-
segment section of the panel.
Output: a new `origin_release_version_id` type field, a new API client function, and a compact
origin display + correction `Select` inside `SegmentEditPanel.tsx`, verified live in the browser.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-CONTEXT.md
@.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-PATTERNS.md
</context>

<interfaces>
<!-- Existing AdminThemeSegment type, frontend/src/types/admin.ts:833-891 (full struct already read this session) -->
<!-- assigned_episodes field already present, to reuse as the origin-choice source -->
```ts
assigned_episodes?: { release_version_id: number; episode_number: string; has_override: boolean }[]
```

<!-- Select primitive, frontend/src/components/ui/Select.tsx -->
```tsx
export interface SelectProps extends SelectHTMLAttributes<HTMLSelectElement> { invalid?: boolean }
export const Select = forwardRef<HTMLSelectElement, SelectProps>(...)
```

<!-- Existing sibling admin API function shape to copy, frontend/src/lib/api.ts:7057-7095 (assignAnimeSegment) -->
```ts
export async function assignAnimeSegment(animeId: number, segmentId: number, releaseVersionId: number): Promise<{ data: AdminThemeSegment }> {
  const response = await fetch(`${API_BASE_URL}/api/v1/admin/anime/${animeId}/segments/${segmentId}/assignments`, {
    method: 'POST', headers: { 'Content-Type': 'application/json' }, credentials: 'include',
    body: JSON.stringify({ release_version_id: releaseVersionId }),
  })
  ...
}
```

<!-- New backend endpoint this plan wires to (Plan 156-04) -->
```
PUT /api/v1/admin/anime/:id/segments/:segmentId/origin
Body: { "release_version_id": number }
Response: { "data": AdminThemeSegment }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Type field, API client function, and minimal origin control in SegmentEditPanel</name>
  <files>frontend/src/types/admin.ts, frontend/src/lib/api.ts, frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx</files>
  <read_first>frontend/src/types/admin.ts:833-891 (AdminThemeSegment struct), frontend/src/lib/api.ts:6938-7105 (createAnimeSegment/updateAnimeSegment/assignAnimeSegment -- the exact request/response pattern to copy), frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx (full file, current state, already read this session -- in particular the `isSharedSegment`/`findAssignedEpisodeNumber` usage around the existing override Switch block, lines 137-142 and 400-450), frontend/src/components/ui/Select.tsx (props shape)</read_first>
  <action>
    In `frontend/src/types/admin.ts`, add `origin_release_version_id?: number | null` to `AdminThemeSegment`, placed near `assigned_release_version_ids`/`is_shared` (matching this file's existing field grouping and comment style for Phase-117-era fields; add a short comment noting this is Phase 156's segment-origin). In `frontend/src/lib/api.ts`, add `export async function setAnimeSegmentOrigin(animeId: number, segmentId: number, releaseVersionId: number): Promise<{ data: AdminThemeSegment }>` immediately after `assignAnimeSegment` (copy its fetch/error-handling shape exactly: `PUT` method, `credentials: 'include'`, `body: JSON.stringify({ release_version_id: releaseVersionId })`, same non-2xx error-throwing convention already used by every sibling function in this file), targeting `${API_BASE_URL}/api/v1/admin/anime/${animeId}/segments/${segmentId}/origin`. In `SegmentEditPanel.tsx`: add two new props `onSetOrigin: (releaseVersionID: number) => void` and `isSettingOrigin: boolean`; place a new, small block immediately after the existing `isSharedSegment` Zeit-Override block (after line ~450, before the "Aktive Playback-Quelle" section), gated on `isSharedSegment && (editingSegment?.assigned_episodes?.length ?? 0) > 0`. Inside: a compact `FormField` labeled "Segment-Origin (Quelle der Credits)" wrapping a `Select` (from `@/components/ui`, NOT a native `<select>`) whose `<option>` list is built from `editingSegment.assigned_episodes` (`value={ep.release_version_id}`, label `Folge ${ep.episode_number}`), `value={editingSegment.origin_release_version_id ?? ''}`, `onChange={(e) => onSetOrigin(Number(e.target.value))}`, `disabled={isSettingOrigin}`. Add a short helper line of German copy under the Select using real umlauts: "Bestimmt, aus welcher zugewiesenen Folge die Credits dieses Segments stammen. Wird nicht automatisch mitgeaendert, wenn du den Bereich anpasst." Do not make this field required for saving the segment itself -- it is a separate, optional, explicit action, matching P156-18's "ohne unnoetige Pflichtinteraktion" requirement; do not add it to `saveDisabled`. In `SegmenteTab.tsx`, wire `onSetOrigin`/`isSettingOrigin` into the panel's existing render call: add a small async handler (mirroring the existing `onSaveOverride`/`onRemoveOverride` wiring pattern already in this file) that calls `setAnimeSegmentOrigin(animeId, editingSegment.id, releaseVersionID)` from `frontend/src/lib/api.ts`, tracks a local `isSettingOrigin` state, refreshes the segment list/editingSegment on success (reuse whatever reload mechanism the existing override save already uses, follow that exact pattern rather than inventing a new one), and surfaces a fetch error inline (reuse the existing `formError`/`overrideError`-style local error state pattern already present in this file).
  </action>
    <acceptance_criteria>
    - `AdminThemeSegment.origin_release_version_id` compiles as `number | null | undefined`
    - `setAnimeSegmentOrigin` sends a `PUT` request with `{ release_version_id }` body and `credentials: 'include'`, matching the sibling `assignAnimeSegment` pattern
    - the new origin Select only renders for `isSharedSegment && assigned_episodes.length > 0`
    - the origin control is never included in `saveDisabled`'s computation -- setting/changing origin is independent of the main Save action
    - the Select is sourced from `@/components/ui`, not a native `<select>` element (grep confirms no new native select/input/textarea introduced in the diff)
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit -p tsconfig.json" 2>&1 | grep -i "SegmentEditPanel\|SegmenteTab\|admin.ts\|lib/api.ts" ; echo "exit_check_done"</automated>
  </verify>
  <done>Type field, API function, and the new Select-based origin control all compile; the control only appears for shared segments with at least one assigned episode, never blocks the main Save action, and uses only @/components/ui primitives.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live verification -- admin origin-correction control</name>
  <files>none -- shared in-app browser verification only</files>
  <read_first>frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx (post-Task-1 state)</read_first>
  <what-built>A minimal origin-correction control in the admin segment editor: a Select (from @/components/ui) listing the segment's assigned episodes, letting an admin set/change which assigned release version supplies this segment's public credits.</what-built>
  <action>
    Via the SSH-tunnel path (`http://127.0.0.1:3300`, per CLAUDE.md), log in as a platform admin and walk through the five checks listed in `<how-to-verify>` below in order, on a real shared theme segment (assigned to 2+ release versions) in the dev dataset. Report the resume-signal only after all five checks pass.
  </action>
  <how-to-verify>
    1. Open `http://127.0.0.1:3300` (SSH tunnel) and navigate to Admin -> an anime with a shared theme segment (assigned to 2+ release versions) -> its episode-versions edit page -> open the segment editor for that segment.
    2. Confirm a new "Segment-Origin" field appears (only for shared segments with assigned episodes), listing the assigned "Folge N" options as choices.
    3. Change the selection to a different assigned episode; confirm it saves without requiring you to also press the main "Speichern" button, and without any error.
    4. Reopen the segment editor; confirm the newly-selected origin is shown as the current value (persisted).
    5. Confirm the main segment Save button still works normally and is never blocked by the origin field being empty/unset.
  </how-to-verify>
  <acceptance_criteria>
    - The Segment-Origin Select appears only for shared segments with assigned episodes
    - Changing the selection persists without requiring the main Save button
    - The main Save button remains functional and unblocked regardless of origin field state
  </acceptance_criteria>
  <verify>
    <automated>echo "This task is human-verification-only; a live admin UI flow has no automated equivalent per this phase's --skip-ui scope."</automated>
  </verify>
  <done>All five manual checks pass and the admin confirms via the resume-signal below.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin browser -> origin-correction request | Same existing admin-authenticated fetch pattern as every other segment-write call in `lib/api.ts`; no new trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-15 | Tampering | Frontend offering an unassigned release version as an origin choice | mitigate | The Select's option list is built exclusively from `editingSegment.assigned_episodes`, the same server-provided assigned-episode set already used for the existing override UI -- structurally cannot offer an unassigned choice |
</threat_model>

<verification>
TypeScript compiles cleanly; manual browser verification confirms the control is reachable, optional, and correctly scoped to shared/assigned segments.
</verification>

<success_criteria>
An admin can view and correct a segment's origin through one small, optional, primitive-based control, with no impact on the existing save flow.
</success_criteria>

<output>
Create `.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-11-SUMMARY.md` when done
</output>
