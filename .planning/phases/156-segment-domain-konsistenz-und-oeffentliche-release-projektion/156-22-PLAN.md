---
phase: 156-segment-domain-konsistenz-und-oeffentliche-release-projektion
plan: 22
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentContributorsField.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.test.tsx
autonomous: true
requirements: [GAP-09]

must_haves:
  truths:
    - "Der Beschreibungstext von 'Mitwirkende am Segment' (SegmentContributorsField.tsx) sagt ausdruecklich, dass Encoding und Design nicht automatisch vorausgewaehlt werden und bei Bedarf von Hand angehakt werden muessen"
    - "ThemeTimeline.test.tsx enthaelt keine 'Typesetting / Logo'-Fixture mehr -- jede Stelle, die vorher 'Typesetting / Logo' als segment_role_label erwartete, erwartet jetzt 'Karaoke-Typesetting'"
    - "SegmentContributorsField.tsx bleibt ausschliesslich auf globalen @/components/ui-Primitives aufgebaut -- keine neue Komponente, kein natives Formularelement"
    - "Frontend-Tests (vitest fuer ThemeTimeline und SegmenteTab, tsc --noEmit, eslint) sind fuer die geaenderten Dateien gruen"
  artifacts:
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentContributorsField.tsx"
      provides: "SectionHeader description prop mit dem GAP-09-Hinweis auf Encoding/Design"
  key_links:
    - from: "frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentContributorsField.tsx (SectionHeader description)"
      to: "@/components/ui SectionHeader"
      via: "description-Prop, keine neue Komponente"
      pattern: "nie automatisch vorausgew"
    - from: "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.test.tsx"
      to: "backend permissions.SegmentCreditLabelForRoles(['typesetter']) == 'Karaoke-Typesetting' (Plan 156-20)"
      via: "Fixture-String-Gleichheit, kein API-Call -- die Fixture spiegelt nur das erwartete Backend-Ergebnis"
      pattern: "Karaoke-Typesetting"
---

<objective>
Schliesst den Frontend-Teil von `156-UAT.md` GAP-09 ("Encoding und Design als Segment-Credit (nur
manuell), Karaoke-Beschriftungen geschärft", Auftraggeber-Entscheidung im Chat, 2026-09-15). Rein
textuelle Aenderungen, keine neue UI-Flaeche, kein neues Verhalten -- ein UI-SPEC wurde fuer diesen
Plan deshalb ausdruecklich NICHT erzeugt (per expliziter Anweisung fuer diesen Lauf: reine
Textaenderung an einer bestehenden Formularfeld-Beschreibung, kein neues UI-Element, keine neue
Layout-Aenderung).

Zwei unabhaengige, nicht ueberlappende Aenderungen:

1. **`SegmentContributorsField.tsx`** (156-UAT.md GAP-09 Punkt 5, "Editor-Hinweis"): der
   Beschreibungstext von "Mitwirkende am Segment" muss ausdruecklich sagen, dass Encoding und
   Design NICHT automatisch vorausgewaehlt werden und bei Bedarf von Hand angehakt werden muessen
   -- sonst keine UI-Aenderung.
2. **`ThemeTimeline.test.tsx`** (156-UAT.md GAP-09 Punkt 6, letzter Aufzaehlungspunkt): die
   `typesetter`-Beschriftung ist backend-seitig (Plan 156-20) von `"Typesetting / Logo"` auf
   `"Karaoke-Typesetting"` umbenannt -- die einzige Frontend-Testfixture, die den alten String noch
   hartcodiert (zwei Stellen in derselben Datei, `segments`-Fixture und ein spaeterer
   `describe`-Block), muss nachgezogen werden, sonst behauptet dieser Test weiterhin faelschlich
   den alten Wert.

Dieser Plan haengt NICHT von Plan 156-20/156-21 ab (kein gemeinsamer Dateibezug, reine
String-Fixture-Angleichung an ein bereits im GAP-09-Auftrag bestaetigtes Label) und kann parallel
zu Plan 156-20 in Wave 1 laufen.

Purpose: Der Admin-Editor kommuniziert das neue Vorauswahlverhalten korrekt; die Frontend-Testsuite
behauptet kein veraltetes Backend-Label.
Output: Aktualisierter Beschreibungstext, aktualisierte Testfixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-UAT.md

<interfaces>
<!-- Executor: use these EXACT strings. Do not reinvent them. -->

**Target 1 -- `SegmentContributorsField.tsx`, current `description` prop (line 39, inside the
`<SectionHeader level={3} title="Mitwirkende am Segment" description="..." />` call):**
```
"Nur Personen der gewählten Origin sind wählbar. Die Rolle stammt aus dem Origin-Release. Beim ersten Zuweisen einer gültigen Origin wird die Auswahl automatisch aus deren Mitwirkenden vorbefüllt und bleibt danach frei abwählbar."
```
**New `description` value (append exactly this sentence, keep everything before it byte-identical):**
```
"Nur Personen der gewählten Origin sind wählbar. Die Rolle stammt aus dem Origin-Release. Beim ersten Zuweisen einer gültigen Origin wird die Auswahl automatisch aus deren Mitwirkenden vorbefüllt und bleibt danach frei abwählbar. Encoding und Design werden dabei nie automatisch vorausgewählt und müssen bei Bedarf von Hand angehakt werden."
```
No other line in this 66-line file changes. `hasOrigin`/`candidates`/`onToggle`/`Switch`/
`EmptyState` usage is untouched -- this is a single-string edit inside one existing JSX attribute.

**Target 2 -- `ThemeTimeline.test.tsx`, two fixture literals + one regex, exact current text:**
  - Line 49 (the `segments` array's second entry, `theme_segment_id: 8`, "Starlight ED"):
    `segment_role_label: 'Typesetting / Logo'` -> `segment_role_label: 'Karaoke-Typesetting'`.
  - Line 285 (inside `describe('ThemeTimeline Phase 156-08 project-context member links', ...)`,
    the `"renders a participant with member_slug null as plain text..."` test): same literal,
    same replacement, inside a `participants` array spread override.
  - Line 289 (immediately after, in the SAME test): the assertion
    `expect(screen.getAllByText(/Noah.*Typesetting \/ Logo/).length).toBeGreaterThan(0)` -> the
    regex literal `/Noah.*Typesetting \/ Logo/` becomes `/Noah.*Karaoke-Typesetting/` (drop the
    now-unnecessary escaped slash since the new label contains no `/`).
  - `role_label: 'Typesetting'` (the RELEASE-level role label, e.g. lines 49/285) is UNCHANGED --
    only `segment_role_label` (the Karaoke/segment-specific label) and its matching regex change.
    `role_label` and `segment_role_label` are deliberately different fields (156-UAT.md GAP-06);
    do not conflate them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the GAP-09 editor hint and the stale Karaoke-Typesetting test fixture</name>
  <files>frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentContributorsField.tsx, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.test.tsx</files>
  <read_first>
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentContributorsField.tsx (the
    WHOLE file, 66 lines -- confirm the `description` prop's exact current text at line 39 matches
    `<interfaces>`'s "Target 1" before editing, since Plan 156-19 may have touched neighboring
    files but not this one's description text);
    frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.test.tsx
    lines 1-90 and 260-291 (the `segments` fixture array and the
    `describe('ThemeTimeline Phase 156-08 project-context member links', ...)` block -- confirm
    both `'Typesetting / Logo'` occurrences and the one regex occurrence match `<interfaces>`'s
    "Target 2" exactly before editing);
    `.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-UAT.md`
    GAP-09 points 3, 5, and 6 (lines 699-733) -- the confirmed label rename and the confirmed
    editor-hint wording requirement.
  </read_first>
  <action>
    In `SegmentContributorsField.tsx`, replace the `description` prop's string value on the
    `SectionHeader` call with `<interfaces>`'s "New `description` value" (append the new sentence,
    everything before it stays byte-identical -- do not touch `title`, `level`, or any other prop).
    In `ThemeTimeline.test.tsx`, apply `<interfaces>`'s "Target 2" three literal replacements
    (lines 49, 285, 289) exactly -- `'Typesetting / Logo'` becomes `'Karaoke-Typesetting'` in both
    fixture objects, and the regex `/Noah.*Typesetting \/ Logo/` becomes
    `/Noah.*Karaoke-Typesetting/`. Leave every `role_label: 'Typesetting'` occurrence (the
    release-level label, a DIFFERENT field) untouched. Run the full `ThemeTimeline` and
    `SegmenteTab` vitest suites plus `tsc --noEmit` and `eslint` on both changed files afterward.
  </action>
  <acceptance_criteria>
    - `grep -c "Encoding und Design werden dabei nie automatisch vorausgewählt"
      "frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentContributorsField.tsx"`
      returns `1`.
    - `grep -c "Typesetting / Logo"
      "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.test.tsx"`
      returns `0`.
    - `grep -c "Karaoke-Typesetting"
      "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.test.tsx"`
      returns at least `2`.
    - `docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run ThemeTimeline"`
      passes with zero failures.
    - `docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run SegmenteTab"`
      passes with zero failures (confirms the description-text change did not break any existing
      `SegmenteTab.test.tsx` assertion, none of which currently check this exact string per prior
      grep verification).
    - `docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit -p tsconfig.json"`
      exits 0.
    - `docker compose exec -T team4sv30-frontend sh -c "cd /app && npx eslint 'src/app/admin/episode-versions/[versionId]/edit/SegmentContributorsField.tsx' 'src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.test.tsx'"`
      exits 0 with zero warnings/errors.
    - `wc -l "frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentContributorsField.tsx"`
      reports 70 or fewer (well under the 450-line project cap).
    - `grep -n "ä\|ö\|ü\|ß" "frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentContributorsField.tsx"`
      still matches the pre-existing and the new German strings (e.g. "gewählten", "vorausgewählt")
      -- confirms no ASCII-substitution regression.
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit -p tsconfig.json && npx vitest run ThemeTimeline SegmenteTab"</automated>
  </verify>
  <done>SegmentContributorsField.tsx's description explicitly states encoder/designer are never auto-preselected and must be checked by hand; ThemeTimeline.test.tsx's typesetter fixture and regex match the GAP-09-confirmed "Karaoke-Typesetting" label instead of the superseded "Typesetting / Logo"; all touched-file tests, typecheck, and lint are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin-authenticated form description (static text) | `SegmentContributorsField.tsx`'s description prop is static, server-independent copy rendered inside the already-authenticated segment editor -- no new data flow, no new input, no new API call. |
| Public release page test fixture (test-only) | `ThemeTimeline.test.tsx`'s fixture literals are compile-time test data, never shipped to production -- updating them changes only what the test suite asserts, not runtime behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-44 | Tampering | `SegmentContributorsField.tsx` description string | accept | Pure static copy change inside an already-reviewed, already-authenticated admin form field; no new interactive element, no new data binding. |
| T-156-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | N/A -- zero new dependencies; two string edits in existing files. |
</threat_model>

<verification>
- Task 1: `npx vitest run ThemeTimeline SegmenteTab` green; `npx tsc --noEmit` and `npx eslint` clean on both touched files; grep confirms both target strings landed and the stale label is gone; `SegmentContributorsField.tsx` stays well under 450 lines.
</verification>

<success_criteria>
- The "Mitwirkende am Segment" description explicitly tells the admin that encoding/design require manual selection.
- No frontend test file still asserts the superseded "Typesetting / Logo" segment label.
- Frontend tests, typecheck, and lint pass for both touched files.
- No new UI component, no native form element, no layout change -- pure text edits, consistent with this plan's explicitly-skipped UI-SPEC gate.
</success_criteria>

<output>
Create `.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-22-SUMMARY.md` when done
</output>
