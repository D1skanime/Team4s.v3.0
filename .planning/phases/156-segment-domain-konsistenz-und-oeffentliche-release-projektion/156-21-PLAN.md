---
phase: 156-segment-domain-konsistenz-und-oeffentliche-release-projektion
plan: 21
type: tdd
wave: 2
depends_on: ["156-20"]
files_modified:
  - backend/internal/repository/theme_segment_contributor_preselection.go
  - backend/internal/repository/theme_segment_contributor_preselection_migration_test.go
  - backend/internal/repository/theme_segment_contributor_preselection_test.go
  - backend/internal/repository/segment_credit_role_filter_test.go
  - backend/internal/repository/release_detail_public_repository_segment_credits_test.go
  - backend/internal/repository/release_detail_public_repository_segment_contributor_subset_test.go
  - backend/internal/repository/theme_segment_contributors.go
  - backend/internal/models/admin_anime_themes.go
  - backend/internal/repository/theme_segment_contributors_integration_test.go
autonomous: true
requirements: [GAP-09]

must_haves:
  truths:
    - "Die automatische Vorauswahl (ensureThemeSegmentContributorsPreselectedTx) waehlt weiterhin NIE Encoder oder Designer vor -- sie referenziert jetzt permissions.SegmentCreditPreselectionRoleCodes statt permissions.SegmentCreditRoleCodes, bewiesen durch einen Testfall mit einem Designer-only-Mitglied zusaetzlich zum bestehenden Encoder-only-Fall"
    - "Der Migration-0165-Aequivalenztest vergleicht das SQL-Rollen-Array jetzt gegen permissions.SegmentCreditPreselectionRoleCodes statt gegen permissions.SegmentCreditRoleCodes; migration 0165s eigene up.sql/down.sql bleiben byte-identisch unveraendert"
    - "Ein explizit als Segment-Contributor ausgewaehlter Encoder erscheint auf der oeffentlichen Release-Seite als 'Karaoke-Encoding', ein explizit ausgewaehlter Designer als 'Logo' -- die alte 'Encoder nie'-Regel (Regressionsfall D) ist fachlich korrigiert, nicht geloescht"
    - "Ein explizit ausgewaehlter raw_provider erscheint weiterhin NIE als Segment-Credit, unabhaengig von Auswahl"
    - "hasAnySegmentRelevantRole (DB-freier Unit-Test) bestaetigt encoder und designer als jetzt segmentrelevant, raw_provider bleibt das Beispiel fuer eine dauerhaft ausgeschlossene Rolle"
    - "Alle betroffenen bestehenden Integrationstests in den neun veraenderten Dateien sind fachlich korrigiert (nicht geloescht) und laufen gruen gegen echtes Postgres"
  artifacts:
    - path: "backend/internal/repository/theme_segment_contributor_preselection.go"
      provides: "ensureThemeSegmentContributorsPreselectedTx, jetzt ueber permissions.SegmentCreditPreselectionRoleCodes gefiltert"
  key_links:
    - from: "backend/internal/repository/theme_segment_contributor_preselection.go"
      to: "backend/internal/permissions/segment_credit_roles.go (SegmentCreditPreselectionRoleCodes)"
      via: "Paket-Import + Schleife ueber permissions.SegmentCreditPreselectionRoleCodes"
      pattern: "permissions\\.SegmentCreditPreselectionRoleCodes"
    - from: "backend/internal/repository/release_detail_public_repository_segment_credits.go (unveraendert, liest permissions.SegmentCreditRoleCodes direkt)"
      to: "backend/internal/repository/release_detail_public_repository_segment_credits_test.go (Test5/Test5b)"
      via: "Integrationstest gegen echtes Postgres, kein Mock"
      pattern: "Karaoke-Encoding|Logo"
---

<objective>
Schliesst den Wiring- und Testkorrektur-Teil von `156-UAT.md` GAP-09 (Auftraggeber-Entscheidung im
Chat, 2026-09-15) -- Fortsetzung von Plan 156-20, das `permissions.SegmentCreditRoleCodes` bereits
auf acht Codes erweitert und die neue, kleinere `permissions.SegmentCreditPreselectionRoleCodes`
angelegt hat. Dieser Plan verdrahtet die drei betroffenen Konsumenten und korrigiert jeden
Bestandstest, der die jetzt falsche "Encoder nie"-Praemisse kodiert -- fachlich korrigiert, nicht
geloescht (156-UAT.md GAP-09 Punkt 6):

1. **Vorauswahl** (`theme_segment_contributor_preselection.go`,
   `ensureThemeSegmentContributorsPreselectedTx`) liest bisher `permissions.SegmentCreditRoleCodes`
   (jetzt 8 Codes inkl. encoder/designer) -- das wuerde nach Plan 156-20 versehentlich beide neuen
   Rollen automatisch vorauswaehlen. GAP-09 Punkt 2 verlangt ausdruecklich "Vorauswahl
   unveraendert": nur die alten sechs Codes. Umschwenken auf
   `permissions.SegmentCreditPreselectionRoleCodes`.
2. **Migration-0165-Aequivalenztest** (`theme_segment_contributor_preselection_migration_test.go`,
   `TestThemeSegmentContributorPreselectionMigrationRoleArrayMatchesGoSlice`) vergleicht das
   hartcodierte SQL-Array der bereits angewendeten Migration 0165 gegen
   `permissions.SegmentCreditRoleCodes` -- das waere nach Plan 156-20 sofort falsch (8 vs. 6
   Elemente). Migration 0165 selbst wird NICHT geaendert; nur der Testvergleich zeigt jetzt auf
   `permissions.SegmentCreditPreselectionRoleCodes`.
3. **Oeffentliche Projektion** (`release_detail_public_repository_segment_credits.go`) braucht
   KEINE Quelltextaenderung -- sie liest `permissions.SegmentCreditRoleCodes` bereits direkt als
   Paketvariable (verifiziert vor dieser Planung), Plan 156-20s Erweiterung wirkt sich automatisch
   aus. Was sich aendert, sind ausschliesslich die Tests, die die alte "Encoder/QC nie"-Fallmatrix
   kodieren.

Zusaetzlich: zwei Doku-Kommentare (`theme_segment_contributors.go`,
`ListThemeSegmentContributorCandidates`; `admin_anime_themes.go`,
`AdminThemeSegmentContributorCandidate`) sowie eine Testassertions-Meldung in
`theme_segment_contributors_integration_test.go` (Zeile 412,
`TestListThemeSegmentContributorCandidates`) behaupten woertlich, ein Encoder-only-Beitragender
"erscheine nie oeffentlich als Segment-Credit" -- das ist seit GAP-09 falsch und wird korrigiert
(reine Kommentar-/Meldungsaenderung, keine Verhaltensaenderung an
`ListThemeSegmentContributorCandidates`, die bereits ungefiltert war und bleibt, und keine
Aenderung der Zaehl-Assertion `require.Len(t, before, 2, ...)` selbst).

Purpose: Jeder Konsument der zentralen Rollendefinition zeigt konsistent das GAP-09-Verhalten;
kein stiller Bruch der Vorauswahl-Garantie; keine falschen Testaussagen im Bestand.
Output: Korrigierte Vorauswahl-Weiche, korrigierter Migrations-Aequivalenztest, korrigierte/
erweiterte Integrationstests, korrigierte Doku-Kommentare und Testassertions-Meldung.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-UAT.md
@.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-20-SUMMARY.md

<interfaces>
<!-- Executor: use these EXACT identifiers/diffs. Do not reinvent them. Plan 156-20 has already
landed permissions.SegmentCreditRoleCodes (8 codes) and permissions.SegmentCreditPreselectionRoleCodes
(6 codes, true subset) -- both exist and are green before this plan starts. -->

**Task 1 target: `theme_segment_contributor_preselection.go`** -- current lines 45-48:
```go
	relevant := make(map[string]struct{}, len(permissions.SegmentCreditRoleCodes))
	for _, code := range permissions.SegmentCreditRoleCodes {
		relevant[code] = struct{}{}
	}
```
Change BOTH occurrences of `permissions.SegmentCreditRoleCodes` on these two lines to
`permissions.SegmentCreditPreselectionRoleCodes`. Update the function's doc comment (lines 12-23,
starts "ensureThemeSegmentContributorsPreselectedTx fires GAP-07's preselection...") to name
GAP-09 and state explicitly that this now reads the smaller preselection-only list, not the full
credit list, so a future extension of `SegmentCreditRoleCodes` (e.g. a ninth segment-relevant role)
does NOT silently become auto-preselected without a deliberate second edit to
`SegmentCreditPreselectionRoleCodes`.

**Task 1 target: `theme_segment_contributor_preselection_migration_test.go`** -- current lines
27-45 (`TestThemeSegmentContributorPreselectionMigrationRoleArrayMatchesGoSlice`): change the
`require.ElementsMatch` call's second argument from `permissions.SegmentCreditRoleCodes` to
`permissions.SegmentCreditPreselectionRoleCodes`. Update the doc comment above the function (it
currently says the SQL array "must contain EXACTLY the same elements as
permissions.SegmentCreditRoleCodes's current value") to name `SegmentCreditPreselectionRoleCodes`
instead and note GAP-09 repointed this comparison. The literal `require.Contains` check for the
SQL text itself (`ARRAY['translator','timer','karaoke_fx','typesetter','editor','quality_checker']::text[]`)
and the migration file path (`migration0165Path`) are UNCHANGED -- migration 0165's own SQL is not
touched by GAP-09.

**Task 1 target: `theme_segment_contributor_preselection_test.go`** -- current lines 211-234 (the
subtest named `"Marker NULL und gueltige Origin: nur segment-relevante Rollen werden eingefuegt,
Marker wird gesetzt"`): add a `designerOnly := s.newMember(t)` alongside the existing
`encoderOnly := s.newMember(t)`, add `s.newContribution(t, designerOnly, &originID,
s.fansubGroupID, "designer")` alongside the existing encoder contribution, rename the subtest to
`"Marker NULL und gueltige Origin: nur segment-relevante Rollen werden eingefuegt, encoder/designer
nie (GAP-09), Marker wird gesetzt"`, and update the `require.Equal(t, 2, count, ...)` message to
mention both excluded roles. `translatorA`/`translatorB`/the final `require.ElementsMatch` and
`require.NotNil(t, s.marker(...))` assertions are otherwise unchanged -- only two new lines are
added (member + contribution) and the subtest name/comment are updated.

**Task 2 target: `segment_credit_role_filter_test.go`** (69 lines) -- FULL replacement of the
`tests` table-driven slice inside `TestSegmentCreditRoleFilter` (current lines 26-61). The
`"encoder-only is excluded"` case (want: `false`) is now FACTUALLY WRONG once
`permissions.SegmentCreditRoleCodes` includes encoder -- it MUST be changed to `want: true` with an
updated name/comment citing GAP-09, and the file needs a NEW case using `permissions.RoleRawProvider`
(want: `false`) as the standing example of a role that stays permanently excluded, replacing
encoder in that role. Also update the `"mixed encoder+translator is included"` case's role slice to
use `permissions.RoleRawProvider` instead of `permissions.RoleEncoder` (encoder alone is no longer a
surprising inclusion once it is unconditionally segment-relevant -- raw_provider mixed with
translator is the interesting "any overlapping relevant role wins" case now). Add a
`"designer-only is included (156-UAT.md GAP-09, new)"` case (want: `true`). Update the file's
top-of-file doc comment (lines 3-11) with a short paragraph naming GAP-09 and raw_provider as the
new standing excluded-role example.

**Task 2 target: `release_detail_public_repository_segment_credits_test.go`** (437 lines) -- THREE
changes:
  1. `newSegmentCreditsFixture`'s `role_definitions` seed INSERT (current lines 96-105) needs two
     more rows appended: `('designer', 'Design')` and `('raw_provider', 'Raw Provider')` (the
     `anime_contribution_roles.role_code` column has no FK to `role_definitions` in this local
     fixture, so this is not strictly required for the query to run, but keeps `RoleLabel`
     resolution realistic and consistent with the other six seeded roles).
  2. `Test3` (current lines 275-301, "Rollenkorrektur auf der Origin wirkt sich sofort auf den
     naechsten Aufruf aus (live)"): the `UPDATE anime_contribution_roles SET role_code = 'encoder'
     ...` on line 293 must become `role_code = 'raw_provider'` (encoder is no longer
     non-segment-relevant, so switching to it would no longer empty `Participants` -- the test's
     own premise requires a role that STAYS excluded). Update the inline comment above it (lines
     290-292) to say the role changes from `'translator'` (segmentrelevant) to `'raw_provider'`
     (permanently non-segment-relevant per GAP-09), not `'encoder'`.
  3. `Test5` (current lines 323-351, "Encoder erscheint nie, Quality-Checker nur wenn explizit
     ausgewaehlt und aufloesbar") is fully superseded and must be REPLACED (not deleted -- the
     assertions change, the test function itself goes away and two new ones take its place) with
     two new subtests proving 156-UAT.md GAP-09's Pflichttests: one proving encoder AND designer
     AND translator all appear (with `SegmentRoleLabel` `"Karaoke-Encoding"`/`"Logo"` respectively)
     when explicitly selected while a fourth, explicitly-selected `raw_provider` contributor never
     appears; a second proving encoder/designer do NOT appear when they hold the role on the
     Origin but are NOT explicitly selected (the "keine Auswahl = keine Credits" rule applies to
     them exactly like every other role). Rename the file's top-of-file doc comment bullet (lines
     15-17) that currently reads "ein Encoder erscheint NIE als Segment-Credit, auch nicht bei
     expliziter Auswahl; ein explizit ausgewaehlter Quality-Checker erscheint dagegen (korrigierte
     Regel, Plan 156-13, 156-UAT.md Auftragspunkt 17 -- die alte 'QC nie'-Annahme war falsch)" to
     describe the new GAP-09 rule instead (encoder/designer visible only when selected, raw_provider
     always excluded).

**Task 2 target: `release_detail_public_repository_segment_contributor_subset_test.go`** (284
lines) -- REPLACE the single `t.Run("D_encoder_is_origin_contributor_and_explicitly_selected_never_appears", ...)`
subtest (current lines 96-107) with FOUR subtests: `D_encoder_is_origin_contributor_and_explicitly_selected_appears_as_karaoke_encoding`
(encoder selected -> 1 participant, `SegmentRoleLabel == "Karaoke-Encoding"`),
`D2_designer_is_origin_contributor_and_explicitly_selected_appears_as_logo` (designer selected ->
1 participant, `SegmentRoleLabel == "Logo"`), `D3_encoder_and_designer_not_selected_do_not_appear_despite_segment_relevant_origin_role`
(both roles present on Origin, NEITHER `selectContributor`-ed -> empty Participants), and
`D4_raw_provider_explicitly_selected_never_appears` (raw_provider selected -> empty Participants).
Reuse `f.newReleaseVersion`/`f.newSegment`/`f.assignSegment`/`f.newContribution`/
`f.selectContributor`/`load(t)`/`findSegmentByID` exactly as every neighboring subtest in this file
already does -- no new fixture helpers needed.

**Task 3 target: `theme_segment_contributors.go`** -- the doc comment on
`ListThemeSegmentContributorCandidates` (current lines 145-151) contains the now-FALSE clause "auch
wenn diese nie oeffentlich als Segment-Credit erscheinen" (referring to an encoder-only
contributor). Replace with wording noting the candidate list stays unfiltered (e.g. `raw_provider`
is still referenceable there too) and that, since GAP-09 (2026-09-15), selecting an encoder or
designer here now DOES surface them publicly -- explicitly superseding the "Regressionsfall D"
reference in this same comment.

**Task 3 target: `admin_anime_themes.go`** -- the doc comment on
`AdminThemeSegmentContributorCandidate` (current lines 202-207) makes the same now-imprecise
"Regressionsfall D" reference (it does not claim "never public" as explicitly as the repository
comment, but still cites the superseded regression case as if it were the current rule). Add one
sentence noting GAP-09 (2026-09-15) supersedes Regressionsfall D: selecting encoder/designer here
now produces a real public segment credit.

**Task 3 target: `theme_segment_contributors_integration_test.go`** -- current line 412, inside
`TestListThemeSegmentContributorCandidates`'s subtest `"die Kandidatenliste enthaelt JEDEN
effektiven Origin-Contributor inkl. Encoder-only, mit korrektem Selected"`:
`require.Len(t, before, 2, "Encoder-only-Contributor muss in der Admin-Kandidatenliste erscheinen,
obwohl er nie oeffentlicher Segment-Credit wird")` asserts the pre-GAP-09 premise that GAP-09
explicitly supersedes (156-UAT.md GAP-09 point 6). Change ONLY the trailing message string to
wording that keeps the "the encoder-only contributor still appears in the admin candidate list"
fact but drops the false "never becomes a public segment credit" claim -- e.g. note that the
candidate list stays unfiltered and that an explicit selection would now make this contributor a
real public segment credit (exact German wording at the executor's discretion, per this plan's
Task 3 `<action>`). The `require.Len(t, before, 2, ...)` count itself, and every other assertion in
this subtest and file, are UNCHANGED. Also inspect the top-of-file doc comment bullet at line 16
("`ListThemeSegmentContributorCandidates` returns every effective Origin contributor, INCLUDING an
encoder-only one, with correct Selected flags before/after a write") -- it does not currently
repeat the "never public" claim explicitly, but correct it too if inspection shows otherwise.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repoint the preselection filter and its migration equivalence test at the new preselection-only list</name>
  <files>backend/internal/repository/theme_segment_contributor_preselection_test.go, backend/internal/repository/theme_segment_contributor_preselection.go, backend/internal/repository/theme_segment_contributor_preselection_migration_test.go</files>
  <read_first>
    backend/internal/repository/theme_segment_contributor_preselection.go (the WHOLE file, 105
    lines -- `ensureThemeSegmentContributorsPreselectedTx`'s full body, especially the `relevant`
    map construction at lines 45-48);
    backend/internal/repository/theme_segment_contributor_preselection_test.go (the WHOLE file, 353
    lines -- especially the subtest at lines 211-234 that this task extends, and every other
    subtest, which must keep passing unmodified since preselection's OWN behavior does not change,
    only which package var it reads);
    backend/internal/repository/theme_segment_contributor_preselection_migration_test.go (the WHOLE
    file, 321 lines -- `TestThemeSegmentContributorPreselectionMigrationRoleArrayMatchesGoSlice` at
    lines 27-45, and `TestThemeSegmentContributorPreselectionSQLGoEquivalence`'s
    `"Encoder-only wird von beiden Pfaden ausgeschlossen"` subtest at lines 315-320, which must
    stay green unmodified -- encoder is still excluded from BOTH the Go and SQL preselection paths,
    only the comparison target var changes);
    database/migrations/0165_theme_segment_contributor_preselection.up.sql (confirm the hardcoded
    `ARRAY['translator','timer','karaoke_fx','typesetter','editor','quality_checker']::text[]`
    literal is present and UNTOUCHED by this plan);
    backend/internal/permissions/segment_credit_roles.go (read AFTER confirming Plan 156-20 has
    landed -- `SegmentCreditRoleCodes` must already be 8 elements and
    `SegmentCreditPreselectionRoleCodes` must already exist with exactly the 6 pre-GAP-09 codes
    before this task's tests can pass).
  </read_first>
  <behavior>
    - `ensureThemeSegmentContributorsPreselectedTx` continues to preselect ONLY
      translator/timer/karaoke_fx/typesetter/editor/quality_checker contributors -- adding a
      `designer`-only contributor to the Origin alongside the existing
      translator/translator/encoder-only fixture in the extended subtest still yields `count == 2`
      (only the two translators), and neither the encoder-only nor the designer-only member appear
      in `GetThemeSegmentContributorMemberIDs`.
    - `TestThemeSegmentContributorPreselectionMigrationRoleArrayMatchesGoSlice` passes with its
      `require.ElementsMatch` second argument now `permissions.SegmentCreditPreselectionRoleCodes`
      (still exactly `{translator, timer, karaoke_fx, typesetter, editor, quality_checker}` after
      Plan 156-20 -- migration 0165's SQL literal is unchanged, so this proves equivalence against
      the CORRECT, smaller list post-GAP-09).
    - Every other existing subtest in all three files (the nil-origin no-op, the
      already-initialized no-op, the zero-qualified-contributor case, the inherited-default case,
      the group-override case, `TestEnsureThemeSegmentOriginAndContributors`'s two subtests, and
      `TestThemeSegmentContributorPreselectionMigration`'s full backfill/idempotency proof) passes
      completely unmodified -- none of their assertions reference `SegmentCreditRoleCodes` by name
      or depend on encoder/designer behavior.
  </behavior>
  <action>
    In `theme_segment_contributor_preselection_test.go`, apply the `<interfaces>` "Task 1 target"
    diff: add the `designerOnly` member/contribution to the named subtest, rename it, update its
    assertion message. Run the affected subtest (RED expected only if Plan 156-20 has NOT actually
    landed yet -- confirm Plan 156-20's SUMMARY exists and its acceptance criteria passed before
    starting; if `permissions.SegmentCreditPreselectionRoleCodes` does not compile, STOP and report
    a blocking dependency gap rather than improvising a local redefinition). Then edit
    `theme_segment_contributor_preselection.go` per `<interfaces>`'s exact two-line diff (both
    `permissions.SegmentCreditRoleCodes` occurrences in the `relevant` map construction become
    `permissions.SegmentCreditPreselectionRoleCodes`) and update its doc comment. Then edit
    `theme_segment_contributor_preselection_migration_test.go` per `<interfaces>`'s diff (repoint
    `TestThemeSegmentContributorPreselectionMigrationRoleArrayMatchesGoSlice`'s comparison target,
    update its doc comment; do not touch `migration0165Path`, `applyMigration0165`, or any SQL
    literal). Run all three files' tests together against real Postgres and confirm GREEN.
  </action>
  <acceptance_criteria>
    - `docker run --rm --network team4s_default -v /home/d1sk/team4s:/workspace -e
      TEAM4S_PHASE117_TEST_DSN -w /workspace/backend golang:1.25-alpine go test
      ./internal/repository/... -run
      'TestEnsureThemeSegmentContributorsPreselected|TestEnsureThemeSegmentOriginAndContributors|TestThemeSegmentContributorPreselectionMigration|TestThemeSegmentContributorPreselectionMigrationRoleArrayMatchesGoSlice|TestThemeSegmentContributorPreselectionSQLGoEquivalence'
      -v -count=1` shows every named subtest PASS against real Postgres.
    - `grep -n "permissions.SegmentCreditRoleCodes" backend/internal/repository/theme_segment_contributor_preselection.go`
      returns no matches; `grep -c "permissions.SegmentCreditPreselectionRoleCodes"
      backend/internal/repository/theme_segment_contributor_preselection.go` returns `2`.
    - `git diff --stat -- database/migrations/0165_theme_segment_contributor_preselection.up.sql
      database/migrations/0165_theme_segment_contributor_preselection.down.sql` shows NO output
      (both migration files byte-identical to before this plan).
    - `grep -n "permissions.SegmentCreditRoleCodes" backend/internal/repository/theme_segment_contributor_preselection_migration_test.go`
      returns no matches.
    - `go build ./... && go vet ./...` succeed from `backend/`.
  </acceptance_criteria>
  <verify>
    <automated>docker run --rm --network team4s_default -v /home/d1sk/team4s:/workspace -e TEAM4S_PHASE117_TEST_DSN -w /workspace/backend golang:1.25-alpine go test ./internal/repository/... -run 'TestEnsureThemeSegmentContributorsPreselected|TestEnsureThemeSegmentOriginAndContributors|TestThemeSegmentContributorPreselectionMigration|TestThemeSegmentContributorPreselectionMigrationRoleArrayMatchesGoSlice|TestThemeSegmentContributorPreselectionSQLGoEquivalence' -v -count=1</automated>
  </verify>
  <done>ensureThemeSegmentContributorsPreselectedTx and the migration 0165 equivalence test both read permissions.SegmentCreditPreselectionRoleCodes instead of permissions.SegmentCreditRoleCodes; migration 0165's own SQL files are byte-identical to before this plan; every preselection-related test passes against real Postgres, including a new designer-exclusion assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correct the public-projection and role-filter tests for GAP-09's new visibility rule</name>
  <files>backend/internal/repository/segment_credit_role_filter_test.go, backend/internal/repository/release_detail_public_repository_segment_credits_test.go, backend/internal/repository/release_detail_public_repository_segment_contributor_subset_test.go</files>
  <read_first>
    backend/internal/repository/segment_credit_role_filter_test.go (the WHOLE file, 69 lines);
    backend/internal/repository/release_detail_public_repository_segment_credits_test.go (the
    WHOLE file, 437 lines -- especially the top-of-file doc comment at lines 3-23,
    `newSegmentCreditsFixture`'s `role_definitions` seed at lines 96-105, `Test3` at lines 275-301,
    and `Test5` at lines 323-351);
    backend/internal/repository/release_detail_public_repository_segment_contributor_subset_test.go
    (the WHOLE file, 284 lines -- especially the `D_encoder_...` subtest at lines 96-107 and its
    neighboring subtests A/B/C for the exact fixture-helper call pattern to reuse);
    backend/internal/repository/release_detail_public_repository_segment_credits.go (the WHOLE
    file, 142 lines -- CONFIRM `applySegmentOriginCredits`/`hasAnySegmentRelevantRole` read
    `permissions.SegmentCreditRoleCodes` directly as a package var with no hardcoded role list of
    their own; this task must NOT modify this file, only the tests that exercise it);
    `.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-UAT.md`
    GAP-09's "Pflichttests" paragraph (lines 743-751) -- the literal list of mandatory test cases
    this task must cover.
  </read_first>
  <behavior>
    - `hasAnySegmentRelevantRole([]string{"encoder"}, ...)` and
      `hasAnySegmentRelevantRole([]string{"designer"}, ...)` both return `true`;
      `hasAnySegmentRelevantRole([]string{"raw_provider"}, ...)` returns `false`.
    - Against real Postgres: a segment whose Origin has an explicitly-selected encoder, designer,
      raw_provider, and translator shows exactly `{encoder, designer, translator}` in
      `Participants`, with `SegmentRoleLabel` `"Karaoke-Encoding"` for the encoder and `"Logo"` for
      the designer.
    - Against real Postgres: a segment whose Origin has encoder and designer contributors that are
      NOT explicitly selected shows an EMPTY `Participants` list (156-UAT.md's "keine Auswahl =
      keine Credits" rule applies identically to the two new roles).
    - `Test3`'s live-role-change proof still demonstrates a contributor disappearing from
      `Participants` after a role correction on the Origin, now using `raw_provider` as the
      target non-segment-relevant role instead of `encoder`.
    - Every one of subset-matrix cases A, B, C, E, F, G, H, I, J, and both `K_*` cases (everything
      EXCEPT the old single `D_*` case, which this task replaces with four) passes completely
      unmodified.
  </behavior>
  <action>
    Apply the `<interfaces>` "Task 2 target" diffs verbatim to all three files: rewrite
    `segment_credit_role_filter_test.go`'s test table and header comment; extend
    `release_detail_public_repository_segment_credits_test.go`'s role_definitions seed, retarget
    `Test3`'s role-change literal to `raw_provider`, and replace `Test5` with the two new GAP-09
    subtests plus the updated header comment bullet; replace
    `release_detail_public_repository_segment_contributor_subset_test.go`'s single `D_*` subtest
    with the four new `D`/`D2`/`D3`/`D4` subtests. Run
    `hasAnySegmentRelevantRole`-level assertions first (fast, no DB) to confirm the filter-level
    RED/GREEN transition, then run the two Postgres-backed integration test files. Do not modify
    `release_detail_public_repository_segment_credits.go` or
    `release_detail_public_repository_helpers.go` -- confirm via the `<read_first>` grep that no
    source change is needed there (Plan 156-20's extension of the package var is sufficient).
  </action>
  <acceptance_criteria>
    - `docker run --rm -v /home/d1sk/team4s:/workspace -w /workspace/backend golang:1.25-alpine go
      test ./internal/repository/... -run TestSegmentCreditRoleFilter -v -count=1` passes with the
      new `raw_provider`-only and `designer`-only cases both present in the `-v` output.
    - `docker run --rm --network team4s_default -v /home/d1sk/team4s:/workspace -e
      TEAM4S_PHASE117_TEST_DSN -w /workspace/backend golang:1.25-alpine go test
      ./internal/repository/... -run
      'TestReleaseDetailPublicSegmentOriginCredits|TestSegmentContributorSubsetMatrix' -v
      -count=1` shows every subtest PASS, including the two new `Test5`-replacement subtests and
      the four `D`/`D2`/`D3`/`D4` subtests.
    - `git diff --stat -- backend/internal/repository/release_detail_public_repository_segment_credits.go
      backend/internal/repository/release_detail_public_repository_helpers.go` shows NO output
      (both files byte-identical to before this plan).
    - `grep -c "encoder.*erscheint nie\|Encoder erscheint nie" backend/internal/repository/release_detail_public_repository_segment_credits_test.go`
      returns `0` (the old header-comment claim is gone).
    - `go build ./... && go vet ./...` succeed from `backend/`.
  </acceptance_criteria>
  <verify>
    <automated>docker run --rm --network team4s_default -v /home/d1sk/team4s:/workspace -e TEAM4S_PHASE117_TEST_DSN -w /workspace/backend golang:1.25-alpine go test ./internal/repository/... -run 'TestSegmentCreditRoleFilter|TestReleaseDetailPublicSegmentOriginCredits|TestSegmentContributorSubsetMatrix' -v -count=1</automated>
  </verify>
  <done>The public segment-credit projection is proven, against real Postgres, to show encoder/designer only when explicitly selected (with the correct GAP-09 labels) while raw_provider stays permanently excluded; the DB-free role filter unit test reflects the same rule; all pre-existing, unrelated subtests in the same three files are untouched and still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the two stale "encoder never public" doc comments and the stale candidate-list test assertion</name>
  <files>backend/internal/repository/theme_segment_contributors.go, backend/internal/models/admin_anime_themes.go, backend/internal/repository/theme_segment_contributors_integration_test.go</files>
  <read_first>
    backend/internal/repository/theme_segment_contributors.go lines 140-160 (the full
    `ListThemeSegmentContributorCandidates` doc comment and function signature);
    backend/internal/models/admin_anime_themes.go lines 195-215 (the full
    `AdminThemeSegmentContributorCandidate` doc comment and struct definition);
    backend/internal/repository/theme_segment_contributors_integration_test.go lines 1-26 (the
    top-of-file doc comment, whose bullet at line 16 says
    `ListThemeSegmentContributorCandidates` returns every effective Origin contributor "INCLUDING
    an encoder-only one" -- correct only if inspection shows it repeats the stale "never public"
    premise) and lines 380-430 (`TestListThemeSegmentContributorCandidates`'s subtest "die
    Kandidatenliste enthaelt JEDEN effektiven Origin-Contributor inkl. Encoder-only, mit korrektem
    Selected", especially the `require.Len(t, before, 2, "Encoder-only-Contributor muss in der
    Admin-Kandidatenliste erscheinen, obwohl er nie oeffentlicher Segment-Credit wird")` assertion
    at line 412 -- GAP-09 supersedes this message's premise: an explicitly-selected encoder-only
    contributor now DOES become a public segment credit);
    `.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-UAT.md`
    GAP-09 point 4 (Bestand) and the "Zielverhalten" intro -- confirms `ListThemeSegmentContributorCandidates`
    itself needs NO behavior change (it was already unfiltered before GAP-09), only its doc comment
    and this test's message text are stale;
    `.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-UAT.md`
    GAP-09 point 6 -- names `theme_segment_contributors_integration_test.go` explicitly as a file
    requiring a factual correction.
  </read_first>
  <action>
    Apply the `<interfaces>` "Task 3 target" comment rewrites verbatim to both files: correct
    `theme_segment_contributors.go`'s `ListThemeSegmentContributorCandidates` doc comment to remove
    the false "never public" claim and note GAP-09 (2026-09-15) makes an explicitly-selected
    encoder/designer a real public segment credit; correct `admin_anime_themes.go`'s
    `AdminThemeSegmentContributorCandidate` doc comment with the equivalent one-sentence addendum.
    Do not touch any executable code in either file.

    Additionally, in `theme_segment_contributors_integration_test.go`: correct the
    `require.Len(t, before, 2, ...)` assertion message at line 412 from "Encoder-only-Contributor
    muss in der Admin-Kandidatenliste erscheinen, obwohl er nie oeffentlicher Segment-Credit wird"
    to wording that keeps the "the encoder-only contributor still appears in the unfiltered admin
    candidate list" fact but drops the false "never becomes a public segment credit" premise --
    e.g. note that an explicit selection of this same contributor would now (per GAP-09) make him
    a real public segment credit. Exact German wording is at the executor's discretion, but the
    message MUST NOT claim an encoder-only contributor never becomes a public segment credit. Do
    NOT change the `require.Len(t, before, 2, ...)` count assertion itself, or any other assertion
    in this subtest or file -- the candidate-list behavior is unaffected by GAP-09, only public
    segment-credit visibility changed. Also inspect the top-of-file doc comment bullet at line 16
    and correct it too if, on inspection, it repeats the stale "never public" premise. Do not touch
    any executable code (SQL, Go logic, fixture setup) in this file -- message-string and comment
    changes only.
  </action>
  <acceptance_criteria>
    - `grep -rn "nie oeffentlich als Segment-Credit erscheinen\|nie öffentlich als Segment-Credit erscheinen"
      backend/internal/repository/theme_segment_contributors.go
      backend/internal/models/admin_anime_themes.go` returns no matches.
    - `grep -c "nie oeffentlicher Segment-Credit\|nie öffentlicher Segment-Credit"
      backend/internal/repository/theme_segment_contributors_integration_test.go` returns `0` (the
      stale GAP-09-superseded assertion message is gone).
    - `git diff -- backend/internal/repository/theme_segment_contributors.go` shows changes ONLY
      within comment lines (no `+`/`-` line touches a non-`//`-prefixed line other than blank
      context lines).
    - `git diff -- backend/internal/models/admin_anime_themes.go` shows changes ONLY within comment
      lines, same constraint.
    - `git diff -- backend/internal/repository/theme_segment_contributors_integration_test.go`
      shows the `require.Len(t, before, 2, ...)` call's `before`/`2` arguments UNCHANGED -- only
      the trailing message string literal (and, if applicable, the line-16 comment) differ; no
      other line in the file changes.
    - `docker run --rm --network team4s_default -v /home/d1sk/team4s:/workspace -e
      TEAM4S_PHASE117_TEST_DSN -w /workspace/backend golang:1.25-alpine go test
      ./internal/repository/... -run 'TestListThemeSegmentContributorCandidates|TestSetThemeSegmentContributors' -v -count=1`
      still passes unmodified (proves the behavior genuinely did not change; both tests live in
      the same file, so this run covers the corrected assertion message directly).
    - `go build ./... && go vet ./...` succeed from `backend/`.
  </acceptance_criteria>
  <verify>
    <automated>docker run --rm --network team4s_default -v /home/d1sk/team4s:/workspace -e TEAM4S_PHASE117_TEST_DSN -w /workspace/backend golang:1.25-alpine go test ./internal/repository/... -run 'TestListThemeSegmentContributorCandidates|TestSetThemeSegmentContributors' -v -count=1</automated>
  </verify>
  <done>Both doc comments accurately describe GAP-09's current rule instead of the superseded "encoder never public" claim; zero executable-code lines changed in either doc-comment file; the stale "encoder never becomes a public segment credit" assertion message in theme_segment_contributors_integration_test.go is corrected to reflect GAP-09 without changing the count assertion or any other test logic; the existing candidate-list and write-path tests still pass unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public release detail page (unauthenticated read) | `applySegmentOriginCredits` already gates on `is_public_on_anime_page`/`visibility_id`/`member_slug` visibility (Auftragspunkt 13, unchanged) -- this plan widens WHICH roles can appear once already-public, it does not touch the public/private gate itself. |
| Admin candidate selection (`theme_segment_contributors`, authenticated) | Already validated server-side against the effective Origin-contributor set (Plan 156-12); this plan changes neither the write validation nor the candidate list's own filtering (still intentionally unfiltered). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-42 | Information Disclosure | Public segment-credit projection now surfaces encoder/designer when selected | accept | Explicit, Auftraggeber-confirmed GAP-09 decision (2026-09-15) -- these are public-facing crew credits, not sensitive data, gated by the SAME already-existing `member_slug`/`is_public_on_anime_page` visibility checks as every other segment-credit role (Auftragspunkt 13, verified unchanged in Task 2's acceptance criteria via the `git diff --stat` on `release_detail_public_repository_segment_credits.go`/`_helpers.go`). |
| T-156-43 | Tampering | Preselection now reads a second, smaller package var (`SegmentCreditPreselectionRoleCodes`) | mitigate | Plan 156-20's `TestSegmentCreditPreselectionRoleCodes` already proves this list is a true subset of `SegmentCreditRoleCodes` at the source; Task 1 here additionally proves the WIRED consumer (`ensureThemeSegmentContributorsPreselectedTx`) actually reads the smaller list via a live designer-exclusion integration assertion, not just a source-level claim. |
| T-156-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | N/A -- zero new dependencies; pure Go test/wiring/comment edits in existing files, no new imports beyond the already-imported `permissions` package. |
</threat_model>

<verification>
- Task 1: preselection and migration-equivalence test suite green against real Postgres, byte-identical migration SQL files, `theme_segment_contributor_preselection.go` reads `SegmentCreditPreselectionRoleCodes` exactly twice.
- Task 2: role-filter unit test and both public-projection integration test files green against real Postgres; zero source changes to the two untouched repository files (`git diff --stat` empty); old false doc-comment claim gone.
- Task 3: two doc-comment-only diffs plus one corrected assertion-message diff in the candidate-list integration test, verified via `git diff` showing no behavior-changing line changes; existing candidate-list and write-path tests still green, proving zero behavior change.
- Full-file regression sweep (recommended final check, not a separate task): `docker run --rm --network team4s_default -v /home/d1sk/team4s:/workspace -e TEAM4S_PHASE117_TEST_DSN -w /workspace/backend golang:1.25-alpine go test ./internal/repository/... ./internal/permissions/... -count=1` with no `-run` filter, confirming zero regressions phase-wide.
</verification>

<success_criteria>
- Automatic preselection never selects encoder or designer, proven against real Postgres with both an encoder-only and a designer-only fixture.
- Migration 0165's equivalence test compares against the correct, smaller `SegmentCreditPreselectionRoleCodes` list; migration 0165's own SQL is untouched.
- The public segment-credit projection shows encoder ("Karaoke-Encoding") and designer ("Logo") exactly when explicitly selected, and never shows raw_provider regardless of selection -- proven against real Postgres, not just at the permissions-package unit-test level.
- Every corrected test documents WHY it changed (GAP-09 reference) rather than silently flipping an assertion.
- Two stale doc comments and one stale test-assertion message no longer claim encoder is permanently invisible in public.
- No production file outside the nine listed in `files_modified` needed a source change (confirmed via `git diff --stat` on the two files that were read but deliberately NOT modified).
</success_criteria>

<output>
Create `.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-21-SUMMARY.md` when done
</output>
