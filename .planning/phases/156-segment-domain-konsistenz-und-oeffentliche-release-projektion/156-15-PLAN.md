---
phase: 156-segment-domain-konsistenz-und-oeffentliche-release-projektion
plan: 15
type: execute
wave: 4
depends_on: ["156-14"]
files_modified:
  - .planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/deferred-items.md
autonomous: false
requirements: [P156-07, P156-08, P156-09, GAP-01, GAP-02]

must_haves:
  truths:
    - "Every previously-green Phase 156 backend/frontend test (156-01 through 156-11) is re-run and still green after GAP-01's changes, or any deviation is precisely documented -- no silent regression"
    - "GAP-02's full 14-item checklist (5 Origin + 9 Segment-Contributor items) is run as ONE bundled live-admin-UI verification pass, exactly as 156-UAT.md specifies"
    - "If no platform-admin session is available when this runs, the checkpoint is explicitly documented as still open -- never claimed as passed"
  artifacts:
    - path: ".planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/deferred-items.md"
      provides: "Updated entry recording the GAP-02 bundled checkpoint's actual outcome (passed with date, or still-open with the same recipe format as the existing 156-11 entry)"
  key_links:
    - from: "156-15-PLAN.md Task 2"
      to: "156-UAT.md GAP-02 section"
      via: "the checkpoint's <how-to-verify> steps are a literal copy of GAP-02's 14-item checklist, not a paraphrase"
      pattern: "GAP-02"
---

<objective>
Close out GAP-01 by proving nothing else in Phase 156 broke, then run the ONE bundled live-UAT
checkpoint 156-UAT.md's GAP-02 section calls for: the still-open Origin checkpoint from Plan
156-11 Task 2, combined with the new Segment-Contributor checks, in a single admin-browser pass.

Purpose: This is the phase's actual closeout gate for GAP-01/GAP-02 -- automated tests alone
cannot prove the admin UI is usable end-to-end, and 156-UAT.md is explicit that this checkpoint
must not be claimed as passed without a real session.

Output: A full backend+frontend regression confirmation and an updated `deferred-items.md` entry
recording GAP-02's actual, honest outcome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-UAT.md
@.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/deferred-items.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full Phase 156 regression re-run (156-01 through 156-14)</name>
  <files>.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/deferred-items.md</files>
  <read_first>
    .planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-01-SUMMARY.md
    through 156-11-SUMMARY.md (the exact test names/suites each plan established, to re-run by
    name, not guess at),
    .planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-10-PLAN.md
    (the prior full-regression-pass precedent -- same shape of command sequence to run again here)
  </read_first>
  <action>
    Confirm the next free migration number was not consumed by a concurrent phase
    (`ls database/migrations/ | sort | tail`) and that migration 0162 (Plan 156-12) is applied to
    the live `team4s_v2` database (`docker compose exec -T team4sv30-backend printenv
    DATABASE_URL` to confirm connectivity, then a direct row-count check on
    `theme_segment_contributors`).

    Inside a `golang:1.25-alpine` container on `team4s_default`, with `TEAM4S_PHASE117_TEST_DSN`
    derived from the LIVE backend container's `DATABASE_URL` (never `.env`, matching
    `^team4s_phase117_test_[a-z0-9]+$`):
    1. `go build ./... && go vet ./...` (whole backend)
    2. `go test ./internal/repository/... ./internal/handlers/... ./internal/permissions/... -count=1`
       -- every test named across 156-01 through 156-14's SUMMARY.md files must be present and
       green, not skipped (grep the output for `--- SKIP` on any Phase-156-named test; a skip due
       to a missing DSN is a genuine gap, not a pass). Explicitly confirm by name:
       `TestAssignThemeSegmentToEpisodeRange*`, `TestSetThemeSegmentOrigin*`,
       `TestSetThemeSegmentContributors*`, `TestListThemeSegmentContributorCandidates*`,
       `TestResolvePublicEffectiveContributors*`, `TestReleaseDetailPublicSegmentOriginCredits*`,
       `TestSegmentContributorSubsetMatrix*`, `TestSegmentCreditRoleFilter`,
       `TestSegmentCreditRoleCodes`, `TestLoadReleaseSegmentsQueryBudgetIsConstant`,
       `TestGroupRepositoryCursorTimeline*` (project-page timeline).
    3. `docker compose up -d --build team4sv30-backend` and confirm healthy.
    4. `docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit -p
       tsconfig.json && npx eslint . && npx vitest run"` -- full frontend suite, not scoped, plus
       the project's umlaut-check script (resolve its exact name from `package.json` at execution
       time).
    5. Re-verify migration reversibility end-to-end one more time for the WHOLE chain (0161 AND
       0162): `go run ./cmd/migrate down -steps 2` then `up`, confirm `theme_segments.
       origin_release_version_id` and `theme_segment_contributors` both disappear and reappear
       cleanly, with no data loss for pre-existing rows.

    Any test that fails and was NOT already documented as pre-existing/unrelated in a prior
    156-0X-SUMMARY.md (the ~49-failure Phase-128/134/member-claims baseline) is a genuine
    regression introduced by 156-12/13/14 -- fix it before proceeding to Task 2, per the Scope
    Boundary rule (this plan's own changes caused it, so it is this plan's job to fix).
  </action>
  <verify>
    <automated>docker run --rm --network team4s_default -v /home/d1sk/team4s:/workspace -w /workspace/backend -e TEAM4S_PHASE117_TEST_DSN golang:1.25-alpine go test ./internal/repository/... ./internal/handlers/... ./internal/permissions/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    Every Phase-156-named test listed above appears in the test output and passes. No new failure
    exists beyond the documented pre-existing baseline (compare the failure list against
    156-05/156-06/156-07/156-09-SUMMARY.md's documented ~49 pre-existing failures by name -- any
    name not in that list is new and must be fixed, not waved through). Frontend: `tsc`/`eslint`/
    full `vitest run`/umlaut-check all exit 0. Migration 0161+0162 round-trip is clean.
  </acceptance_criteria>
  <done>Full Phase 156 backend and frontend regression suites are green (or every remaining failure is confirmed pre-existing and unrelated by name), and migration 0161/0162 reversibility is proven end-to-end.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Bundled live-UAT -- Segment-Origin + Segment-Contributors (GAP-02)</name>
  <files>none -- shared in-app browser verification only</files>
  <read_first>
    .planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-UAT.md
    (GAP-02 section, the literal 14-item source list this checkpoint copies),
    .planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/deferred-items.md
    (156-11's still-open entry, the precedent for how to record an honest "still open" outcome)
  </read_first>
  <what-built>
    GAP-01's Segment-Contributor subset feature (Plans 156-12/13/14) is implemented,
    automated-tested, and wired into the admin segment editor next to the existing Segment-Origin
    control (Plan 156-11, whose own live-UAT checkpoint has remained open since 2026-09-11 -- see
    deferred-items.md). Per 156-UAT.md's GAP-02 section, both are verified together in ONE
    bundled pass now that GAP-01 has landed.
  </what-built>
  <action>
    Via the SSH-tunnel path (`http://127.0.0.1:3300`, per CLAUDE.md), log in as a platform admin
    and walk through all 14 checks listed in `<how-to-verify>` below, in order, on a real shared
    theme segment (assigned to 2+ release versions) in the dev dataset -- `theme_segment_id 3` is
    a ready-made dataset with 3 assignments. Report the resume-signal only after determining a
    definitive outcome for every item; if no admin session exists in this environment, do not
    attempt to guess or partially verify -- report that directly per `<resume-signal>`.
  </action>
  <how-to-verify>
    Open `http://127.0.0.1:3300` (SSH tunnel per CLAUDE.md) and log in as a platform admin.
    Navigate to an anime with a shared theme segment (assigned to 2+ release versions) -> its
    episode-versions edit page -> open the segment editor. `theme_segment_id 3` is a ready-made
    test dataset with 3 assignments in the dev database (per deferred-items.md).

    **Origin (5 items, from 156-11's original checklist):**
    1. Segment-Origin `Select` field appears (only for shared segments with >=1 assigned episode).
    2. Only assigned releases ("Folge N") are selectable.
    3. Changing the selection saves immediately, without the main "Speichern" button.
    4. Reopening the segment editor shows the persisted Origin.
    5. The main segment Save workflow still works normally.

    **Segment-Contributors (9 items, new for GAP-01):**
    6. Origin contributors are displayed in a "Mitwirkende am Segment" section, each with their
       current role.
    7. Multiple people can be selected.
    8. With 3 quality-checkers on the Origin, exactly one can be selected on its own.
    9. Saving and reopening the editor retains the selection.
    10. The change appears on the public release page.
    11. Non-selected quality-checkers do NOT appear on the public page.
    12. An editor can be selected and appears correctly.
    13. An encoder never appears as a segment credit, even if selected.
    14. Changing the Origin never leaves an inconsistent contributor state (a contributor invalid
        for the new Origin is silently and correctly removed, not shown as broken/leftover).
  </how-to-verify>
  <acceptance_criteria>
    - All 14 items are individually confirmed pass/fail -- no item is skipped or inferred from
      another
    - If confirmed, `deferred-items.md`'s 156-11 entry is updated to reflect the pass, dated
    - If no admin session is available, `deferred-items.md` gains a new dated entry documenting
      GAP-02 as still open, mirroring the 156-11 entry's exact format (symptom, why not fixed here,
      concrete recipe for the operator)
  </acceptance_criteria>
  <verify>
    <automated>echo "This task is human-verification-only; a live admin UI flow has no automated equivalent."</automated>
  </verify>
  <done>All 14 checklist items are confirmed pass, OR the checkpoint is explicitly documented as still open in deferred-items.md -- never silently skipped, never claimed as passed without a real session.</done>
  <resume-signal>
    Type "approved" if all 14 items pass. If ANY item fails, describe exactly which item and what
    happened. If no platform-admin credentials/session are available in this environment, respond
    "no admin session available" -- this task will then document the checkpoint as explicitly
    OPEN (not passed, not failed) in `deferred-items.md`, exactly mirroring how Plan 156-11's Task
    2 was handled, and will NOT claim GAP-02 as verified.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human operator attestation | The checkpoint's outcome is only as trustworthy as the honesty of what is recorded -- this plan's sole safeguard is textual: never claim "verified"/"passed" without an actual completed session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-31 | Repudiation | This plan's own closeout claim | mitigate | The checkpoint task's `<resume-signal>` explicitly requires "no admin session available" as a valid, non-failing outcome, and instructs the executor to update `deferred-items.md` with an honest OPEN status rather than a false PASS -- mirrors the exact precedent already established for Plan 156-11 Task 2 |
| T-156-32 | Tampering | Regression re-run (Task 1) | mitigate | Every test name is enumerated explicitly (not "run the suite and eyeball it") so a silently-skipped or silently-removed test cannot pass unnoticed |
</threat_model>

<verification>
1. Full backend test suite (all Phase-156-named tests enumerated in Task 1) green or precisely
   documented against the known pre-existing baseline
2. Full frontend suite (tsc/eslint/vitest/umlaut-check) green
3. Migration 0161+0162 round-trip clean
4. GAP-02's 14-item checklist run as one bundled pass, outcome honestly recorded (passed with
   date, or explicitly still-open)
</verification>

<success_criteria>
- No GAP-01 change silently regressed any already-shipped Phase 156 behavior
- `deferred-items.md` reflects the TRUE state of GAP-02's live-UAT checkpoint -- never an
  overstated "fully verified" claim while it remains unconfirmed
- Phase 156's closeout report (produced separately, not by this plan) can cite this plan's
  Task 1 output as its regression evidence and this plan's Task 2 outcome as its live-UAT status
</success_criteria>

<output>
Create `.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-15-SUMMARY.md` when done
</output>
