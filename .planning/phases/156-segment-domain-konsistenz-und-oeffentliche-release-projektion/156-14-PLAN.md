---
phase: 156-segment-domain-konsistenz-und-oeffentliche-release-projektion
plan: 14
type: execute
wave: 3
depends_on: ["156-13"]
files_modified:
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentAssetSection.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentOverrideField.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentPlaybackPreviewSection.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.test.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentContributorsField.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/useSegmentContributors.ts
  - frontend/src/lib/api/segment-contributors.ts
  - frontend/src/lib/api/segment-contributors.test.ts
  - frontend/src/types/admin.ts
autonomous: true
requirements: [P156-07, P156-08, P156-09, GAP-01]

must_haves:
  truths:
    - "SegmentEditPanel.tsx and SegmenteTab.tsx are at or under 450 lines BEFORE any GAP-01 feature code is added to them"
    - "An admin sees a 'Mitwirkende am Segment' section next to the Segment-Origin field, listing only Origin contributors with their current role, as a multi-select of PEOPLE (never roles)"
    - "All new UI elements use only @/components/ui primitives -- no native select/input/textarea/button for a type the design system already provides"
    - "All new German user-facing strings use correct umlauts (a/o/u-Umlaut, sharp-s), never ASCII substitutes"
    - "New API calls live in a dedicated frontend/src/lib/api/segment-contributors.ts module -- api.ts itself gains zero new lines for this feature"
  artifacts:
    - path: "frontend/src/lib/api/segment-contributors.ts"
      provides: "getThemeSegmentContributorCandidates + setThemeSegmentContributors, mirroring the admin-anime-intake.ts extraction precedent"
      exports: ["getThemeSegmentContributorCandidates", "setThemeSegmentContributors"]
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentContributorsField.tsx"
      provides: "The multi-select UI for Origin-contributor selection, built from @/components/ui primitives only"
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/useSegmentContributors.ts"
      provides: "Fetch/save state hook for the contributor picker, mirroring useSegmentOverrideHandlers"
  key_links:
    - from: "frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx"
      to: "frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentContributorsField.tsx"
      via: "rendered next to the existing Segment-Origin FormField, gated on isSharedSegment && editingSegment?.origin_release_version_id"
      pattern: "SegmentContributorsField"
    - from: "frontend/src/lib/api/segment-contributors.ts"
      to: "backend/internal/handlers/admin_content_anime_theme_segment_contributors.go"
      via: "apiClientFetch PUT/GET /api/v1/admin/anime/:id/segments/:segmentId/contributors"
      pattern: "segments/\\$\\{segmentId\\}/contributors"
---

<objective>
Give admins the "Mitwirkende am Segment" UI GAP-01 requires: a multi-select of the segment's
Origin-contributors (people only, never roles), wired to Plan 156-13's GET/PUT endpoints. Because
`SegmentEditPanel.tsx` (733 lines) and `SegmenteTab.tsx` (827 lines) already exceed the project's
450-line limit, this plan extracts focused sibling components FIRST -- per 156-UAT.md's explicit
blocking rule -- before adding any new feature code to either file.

Purpose: This is the only user-visible surface change in GAP-01 -- everything else (156-12,
156-13) is backend plumbing an admin cannot otherwise reach.

Output: Two files brought under the line limit via targeted extraction, a new dedicated API module
(not growing the already-10893-line `api.ts`), a new `SegmentContributorsField` component + hook,
and updated `SegmenteTab.test.tsx` fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-UAT.md
@frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx
@frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx
@frontend/src/lib/api/admin-anime-intake.ts
</context>

<interfaces>
<!-- Exact current shapes and precedents the executor needs. -->

`frontend/src/lib/api/admin-anime-intake.ts` (the EXTRACTION PRECEDENT to copy exactly -- a
dedicated feature module that imports from `@/lib/api`, never the other way around):
```ts
import { ApiError, apiClientFetch, parseApiErrorPayload } from '@/lib/api'

export async function someCall(...): Promise<SomeResponse> {
  const response = await apiClientFetch('/api/v1/admin/...', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify(payload) })
  if (!response.ok) {
    const parsed = await parseApiErrorPayload(response, `API request failed: ${response.status}`)
    throw new ApiError(response.status, parsed.message, null, parsed.code, parsed.details)
  }
  return response.json() as Promise<SomeResponse>
}
```
Consumers import this module directly (`@/lib/api/admin-anime-intake`), never via `api.ts`
re-export. `api.ts` gained zero lines for this precedent feature.

`frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.helpers.tsx`'s
`useSegmentOverrideHandlers` (the EXISTING hook-extraction precedent in THIS SAME directory --
mirror its shape for `useSegmentContributors`): a hook that owns its own `isSaving`/`error` state
and exposes handler functions, consumed in `SegmenteTab.tsx` as:
```ts
const { isSavingOverride, overrideError, handleSaveOverride, handleRemoveOverride, resetOverrideError } =
  useSegmentOverrideHandlers({ editingSegment, releaseVariantId: releaseVariantId ?? null, setSegmentOverride, removeSegmentOverride })
```

`SegmentEditPanel.tsx`'s existing stable JSX comment markers (use these to find extraction
boundaries -- do not rely on line numbers, which will have shifted since planning time):
- `{/* Per-Folge Zeit-Override (UI-SPEC Surface 1) — nur bei geteilten Segmenten */}` through its
  closing `{/* Segment-Origin ... */}` marker -- extract into `SegmentOverrideField.tsx`
- `{/* Resolved playback status when editing an existing segment */}` through
  `{/* Segmentstream preview */}`'s closing block -- extract into
  `SegmentPlaybackPreviewSection.tsx`
- `{/* Source type selector — Episode-Version/Jellyfin is default; upload is explicit fallback */}`
  through `{/* Segment-Asset-Sektion: nur bei release_asset */}`'s closing block (these two are
  coupled -- the source-type `<select>` controls whether the asset section renders) -- extract
  BOTH together into `SegmentAssetSection.tsx`

`Switch` (`@/components/ui`, already used in this exact file for the override toggle):
```tsx
export interface SwitchProps {
  checked: boolean
  onCheckedChange: (next: boolean) => void
  disabled?: boolean
  label?: string
}
```
The established multi-item toggle-list pattern (`ReviewDelegationSection.tsx`): one `<Switch
aria-label={label} checked={...} onCheckedChange={...} />` per row inside a `display: grid` list --
follow this shape for the contributor picker (one `Switch` per candidate row, label = display
name + current role).

`FormField`/`SectionHeader`/`EmptyState` (`@/components/ui`):
```ts
FormFieldProps { label?, htmlFor?, hint?, error?, required?, disabled?, children }
SectionHeaderProps { eyebrow?, title: string, level?: 2|3, description?, actions?, underline? }
EmptyStateProps { title: string, description?, action?, variant?: 'default'|'inline' }
```

`AdminThemeSegment` (`frontend/src/types/admin.ts`, current, relevant fields):
`id: number`, `origin_release_version_id?: number | null`, `is_shared: boolean`,
`assigned_episodes?: AdminThemeSegmentAssignmentEpisode[]`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: File-size extraction (BLOCKING -- must complete before Task 2/3's new code)</name>
  <files>
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx,
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentAssetSection.tsx,
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentOverrideField.tsx,
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentPlaybackPreviewSection.tsx
  </files>
  <read_first>
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx (full file, 733
    lines at planning time -- read it in full before extracting, the JSX comment markers in
    `<interfaces>` above are your extraction boundaries),
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.helpers.tsx (existing
    extraction precedent -- how a sibling file imports shared helpers/styles from this directory)
  </read_first>
  <action>
    Extract three new sibling components from `SegmentEditPanel.tsx`, following the exact
    boundaries in `<interfaces>`:
    - `SegmentOverrideField.tsx`: the entire `isSharedSegment ? (...) : null` per-folge
      time-override block (the `Switch` + conditional override sub-form). Props: `isSharedSegment:
      boolean`, `overrideEnabled: boolean`, `onOverrideToggle: (next: boolean) => void`,
      `currentEpisodeLabel: string`, `overrideStartTime: string`, `onOverrideStartTimeChange:
      (value: string) => void`, `computedOverrideEndTime: string | null`, `baseDurationSeconds:
      number | null`, `overrideDisplayError: string | null`, `currentReleaseHasOverride: boolean`,
      `onRemoveOverrideClick: () => void`, `isSavingOverride: boolean`. Keep all local derived-value
      computation (parsing, validation) in `SegmentEditPanel.tsx` -- this component is presentation
      only, matching the "props-in" pattern already used by `ThemeTimelineSegmentDetails.tsx`
      (Plan 156-08's precedent).
    - `SegmentPlaybackPreviewSection.tsx`: the "Resolved playback status" block AND the
      "Segmentstream preview" block together (they are adjacent and both read-only display).
      Props: `editingSegment: AdminThemeSegment | null`, `previewStreamHref: string | null`,
      `renderStatus: string`.
    - `SegmentAssetSection.tsx`: the source-type selector AND the entire release-asset upload/reuse
      block together (coupled by `formState.sourceType`). Props: `formState: FormState`,
      `onFormChange: (patch: Partial<FormState>) => void`, `editingSegment: AdminThemeSegment |
      null`, `isSaving: boolean`, `isUploading: boolean`, `isDeletingAsset: boolean`,
      `isLoadingReuseCandidates: boolean`, `isAttachingReuse: boolean`, `uploadError: string |
      null`, `reuseCandidates: AdminSegmentLibraryCandidate[]`, `reuseError: string | null`,
      `pendingUploadFile: File | null`, `onPendingUploadFileChange: (file: File | null) => void`,
      `onAssetUpload: (file: File) => void`, `onAssetDelete: () => void`,
      `onAttachReuseCandidate: (candidate: AdminSegmentLibraryCandidate) => void`. This is the
      largest extraction (~190 lines) -- if it alone does not bring `SegmentEditPanel.tsx` under
      450, also move the "Segmentstream preview" file-input's `fileInputRef`-dependent logic here
      rather than leaving a partial extraction.

    Update `SegmentEditPanel.tsx` to import and render all three new components in place of the
    extracted JSX, passing through the exact props listed above (compute the derived values it
    already computes today -- `overrideDisplayError`, `renderStatus`, etc. -- and pass them down,
    do not duplicate that logic into the new files).

    Verify with `wc -l` after every extraction step, not just at the end, to catch a
    miscalculated boundary early.
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit -p tsconfig.json"</automated>
  </verify>
  <acceptance_criteria>
    `wc -l frontend/src/app/admin/episode-versions/\[versionId\]/edit/SegmentEditPanel.tsx` reports
    <=450. Every extracted file is a plain, exported, props-typed React component (no default
    export, matching the project's named-export convention already used by
    `ThemeTimelineSegmentDetails.tsx`/`SegmentAssignmentsRow.tsx`). `docker compose exec -T
    team4sv30-frontend sh -c "cd /app && npx vitest run SegmenteTab"` passes with ZERO behavior
    change (this task is a pure refactor -- no new props, no new rendering logic beyond moving
    existing JSX). `git diff --stat` shows only the four listed files touched.
  </acceptance_criteria>
  <done>SegmentEditPanel.tsx is at or under 450 lines with identical rendered output and zero test regressions; three new focused sibling components exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dedicated segment-contributors API module + types</name>
  <files>
    frontend/src/lib/api/segment-contributors.ts,
    frontend/src/lib/api/segment-contributors.test.ts,
    frontend/src/types/admin.ts
  </files>
  <read_first>
    frontend/src/lib/api/admin-anime-intake.ts (the exact module shape to copy -- imports,
    error-handling pattern, export style),
    frontend/src/lib/api.admin-anime.test.ts (the exact test shape for a module under
    `frontend/src/lib/api/`, if it mocks `fetch`/`apiClientFetch` -- mirror its mocking approach)
  </read_first>
  <behavior>
    - `getThemeSegmentContributorCandidates(animeId, segmentId)` calls `GET
      /api/v1/admin/anime/${animeId}/segments/${segmentId}/contributors`, returns
      `{ data: AdminThemeSegmentContributorCandidate[], origin_release_version_id: number | null }`,
      throws `ApiError` with the parsed backend error code/message on a non-2xx response.
    - `setThemeSegmentContributors(animeId, segmentId, memberIds)` calls `PUT
      /api/v1/admin/anime/${animeId}/segments/${segmentId}/contributors` with body `{member_ids:
      memberIds}`, returns `{ data: AdminThemeSegment, contributors:
      AdminThemeSegmentContributorCandidate[] }`, throws `ApiError` on non-2xx (in particular
      surfacing `code: "segment_has_no_origin"` and `code: "member_not_origin_contributor"`
      unmodified from the backend, so the UI can branch on them if desired).
  </behavior>
  <action>
    Add to `frontend/src/types/admin.ts` (near the other `AdminThemeSegment*` types):
    ```ts
    export interface AdminThemeSegmentContributorCandidate {
      member_id: number
      name: string
      avatar_url: string | null
      role_label: string
      role_codes: string[]
      member_slug: string | null
      selected: boolean
    }
    export interface AdminThemeSegmentContributorCandidatesResponse {
      data: AdminThemeSegmentContributorCandidate[]
      origin_release_version_id: number | null
    }
    export interface AdminThemeSegmentContributorsSetResponse {
      data: AdminThemeSegment
      contributors: AdminThemeSegmentContributorCandidate[]
    }
    ```
    Create `frontend/src/lib/api/segment-contributors.ts` implementing both functions exactly per
    `<behavior>`, importing `ApiError, apiClientFetch, parseApiErrorPayload` from `@/lib/api` and
    the three new types from `@/types/admin` -- following `admin-anime-intake.ts`'s structure
    verbatim (same import grouping, same error-throw shape). Do NOT add anything to `api.ts`
    itself -- this is the entire point of the extraction precedent.

    Create `frontend/src/lib/api/segment-contributors.test.ts` (Vitest, mocking `fetch`/
    `apiClientFetch` the same way `api.admin-anime.test.ts` mocks `admin-anime-intake.ts`'s
    calls) proving: successful GET returns the parsed candidate list; successful PUT returns the
    reloaded segment + contributors; a 409 response with `code: "segment_has_no_origin"` throws an
    `ApiError` carrying that code; a 409 with `code: "member_not_origin_contributor"` throws an
    `ApiError` carrying that code; a network/non-2xx failure without a parseable body still throws
    a usable `ApiError` (mirrors the existing fallback message pattern in `admin-anime-intake.ts`).
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run segment-contributors"</automated>
  </verify>
  <acceptance_criteria>
    All 5 new Vitest cases pass. `grep -n "segments/.*contributors" frontend/src/lib/api.ts`
    returns zero matches (proves `api.ts` was not touched). `npx tsc --noEmit -p tsconfig.json`
    clean.
  </acceptance_criteria>
  <done>segment-contributors.ts exists with both functions, fully tested, and api.ts gained zero lines for this feature.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SegmentContributorsField component + useSegmentContributors hook + wiring</name>
  <files>
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentContributorsField.tsx,
    frontend/src/app/admin/episode-versions/[versionId]/edit/useSegmentContributors.ts,
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx,
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx,
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.test.tsx
  </files>
  <read_first>
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx (current
    `isSettingOrigin`/`originError`/`handleSetOrigin` wiring -- the Segment-Origin `Select` block
    in `SegmentEditPanel.tsx` this new field sits next to),
    frontend/src/components/ui/Switch.tsx and
    frontend/src/app/admin/users/tabs/ReviewDelegationSection.tsx (multi-toggle-list pattern to
    copy)
  </read_first>
  <behavior>
    `useSegmentContributors({ animeId, editingSegment })`:
    - re-fetches candidates via `getThemeSegmentContributorCandidates` whenever
      `editingSegment?.id` OR `editingSegment?.origin_release_version_id` changes
    - if `origin_release_version_id` is null/undefined, sets `candidates = []` without calling the
      API (matches the backend's own empty-when-no-origin contract, avoids a wasted request)
    - `toggleMember(memberId, next)`: computes the new full selected-member-ID set from the current
      `candidates` state (toggle `next` for `memberId`, keep everything else), calls
      `setThemeSegmentContributors(animeId, segmentId, newIds)`, and on success replaces
      `candidates` with the response's fresh candidate list (not an optimistic guess -- the backend
      is the source of truth for `Selected`/role display)
    - exposes `{ candidates, isLoading, isSaving, error, toggleMember }`

    `SegmentContributorsField`:
    - renders nothing (returns `null`) when `hasOrigin` is false -- do NOT render an empty
      "Mitwirkende" section with no explanation
    - renders an `EmptyState` (`variant="inline"`) when `hasOrigin` is true but `candidates.length
      === 0` and not loading (Origin has no resolvable contributors)
    - otherwise renders one `Switch` per candidate row (label: `${candidate.name} — ${candidate.role_label}`,
      `checked={candidate.selected}`, `onCheckedChange={(next) => onToggle(candidate.member_id,
      next)}`, `disabled={isSaving}`), inside a `SectionHeader` titled "Mitwirkende am Segment"
      (correct umlaut) with a `description` explaining "Nur Personen der gewählten Origin sind
      wählbar. Die Rolle stammt aus dem Origin-Release." (correct umlauts: gewählten, wählbar)
  </behavior>
  <action>
    Create `useSegmentContributors.ts` implementing the hook per `<behavior>`, following
    `useSegmentOverrideHandlers`'s file-local state-and-handlers shape (same directory,
    `SegmenteTab.helpers.tsx`, as the pattern to mirror -- this can be its own new file since it is
    a full custom hook, not a small helper function).

    Create `SegmentContributorsField.tsx` implementing the component per `<behavior>`, importing
    `Switch, SectionHeader, EmptyState` from `@/components/ui` only -- no native `<input
    type="checkbox">` or hand-rolled toggle markup.

    In `SegmentEditPanel.tsx`, render `<SegmentContributorsField candidates={...}
    isLoading={...} isSaving={...} error={...} hasOrigin={editingSegment?.origin_release_version_id
    != null} onToggle={onToggleContributor} />` immediately after the existing Segment-Origin
    `FormField`/`Select` block (same `isSharedSegment && (editingSegment?.assigned_episodes?.length
    ?? 0) > 0` gate, since a contributor selection is meaningless without an assignable Origin).

    In `SegmenteTab.tsx`, instantiate `useSegmentContributors({ animeId, editingSegment })` next to
    the existing `isSettingOrigin`/`originError` state, and pass its `candidates`/`isLoading`/
    `isSaving`/`error`/`toggleMember` through to `SegmentEditPanel` as the new
    `SegmentContributorsField`-related props (`onToggleContributor={(id, next) => void
    toggleMember(id, next)}`).

    Update `SegmenteTab.test.tsx`'s two direct `SegmentEditPanel` prop fixtures (the same two
    fixtures Plan 156-11 updated for `onSetOrigin`/`isSettingOrigin`) with the new required props
    for this task's additions, mirroring that exact precedent.

    Run the project's umlaut-check script (check `frontend/package.json`'s `scripts` for its exact
    name, e.g. `npm run check:umlaute` or similar) against every new/changed file in this task.
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit -p tsconfig.json && npx vitest run SegmenteTab segment-contributors"</automated>
  </verify>
  <acceptance_criteria>
    `grep -rn "<select\|<input\|<textarea\|<button" frontend/src/app/admin/episode-versions/\[versionId\]/edit/SegmentContributorsField.tsx`
    returns zero matches. `grep -rn "gewaehlten\|waehlbar\|gewahlt" frontend/src/app/admin/episode-versions/\[versionId\]/edit/SegmentContributorsField.tsx`
    (ASCII-substitute check) returns zero matches. ESLint introduces zero new warnings on changed
    files. Vitest: all `SegmenteTab`/`segment-contributors` suites green, including the two updated
    fixtures. The project's umlaut-check script passes on every file this task touched.
  </acceptance_criteria>
  <done>An admin can open a shared segment with an Origin set, see every Origin contributor with their current role, toggle any subset as segment-contributors, and have the selection persist via the new PUT endpoint -- all through @/components/ui primitives with correct German umlauts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser -> new frontend module | Renders server-supplied candidate data only; no client-side aggregation of contributor identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-28 | Tampering | `SegmentContributorsField`/native form elements | mitigate | Exclusive use of `@/components/ui` `Switch`/`SectionHeader`/`EmptyState` -- no hand-rolled `<input>`/`<button>` for a type the design system already provides, per CLAUDE.md's global UI rule |
| T-156-29 | Information Disclosure | `useSegmentContributors`'s candidate fetch | accept | Candidate visibility is entirely determined server-side (Plan 156-13's GET endpoint, same capability gate as every segment-write path) -- the frontend has no independent data source to leak |
| T-156-30 | Repudiation | `toggleMember`'s optimistic-vs-authoritative state | mitigate | The hook always replaces local state with the SERVER's response after a save, never an optimistic guess -- an admin never sees a selection state the backend does not actually hold |
</threat_model>

<verification>
1. `docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit -p tsconfig.json"`
2. `docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run SegmenteTab segment-contributors"`
3. `docker compose exec -T team4sv30-frontend sh -c "cd /app && npx eslint src/app/admin/episode-versions"`
4. `wc -l` on `SegmentEditPanel.tsx`/`SegmenteTab.tsx` <=450
5. `docker compose exec -T team4sv30-frontend sh -c "cd /app && npm run <project umlaut-check script>"` (resolve the exact script name from `package.json` at execution time)
</verification>

<success_criteria>
- Both oversized files are extracted under 450 lines BEFORE any GAP-01 code was added
- Admin can view and toggle Origin-contributor selection for a shared, Origin-set segment
- Zero native form elements introduced; zero ASCII-umlaut substitutions
- api.ts unchanged; new API calls live in the dedicated segment-contributors.ts module
</success_criteria>

<output>
Create `.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-14-SUMMARY.md` when done
</output>
