---
phase: 156-segment-domain-konsistenz-und-oeffentliche-release-projektion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/models/admin_anime_themes.go
  - backend/internal/repository/theme_segment_assignments.go
  - backend/internal/repository/theme_segment_assignments_integration_test.go
  - backend/internal/handlers/admin_content_handler.go
  - backend/internal/handlers/admin_content_anime_theme_segments.go
  - backend/internal/handlers/admin_content_anime_theme_segment_range_autoassign_test.go
autonomous: true
requirements: [P156-01, P156-02, P156-03]

must_haves:
  truths:
    - "theme_segment_assignments is the canonical release<->segment truth: shrinking a segment's episode range removes out-of-range, unprotected assignments; growing it adds missing ones"
    - "CRITICAL: an incomplete range (segmentID/animeID/fansubGroupID<=0 OR startEpisode/endEpisode<=0) is a no-op -- it NEVER deletes any assignment, and this has its own dedicated test (not incidental coverage inside a broader test)"
    - "An assignment with a live theme_segment_episode_overrides row survives an out-of-range shrink and is reported back as protected, not silently dropped"
    - "Deletion only ever touches assignments within the same anime+fansub_group+version domain as the segment being reconciled -- assignments belonging to other groups/versions are never touched"
    - "A range-shrink-only update (new assignments = 0, removed assignments > 0) still triggers the existing admin response reload, not just a range-growth update"
  artifacts:
    - path: "backend/internal/models/admin_anime_themes.go"
      provides: "ThemeSegmentAssignmentSyncResult{Added, Removed, ProtectedByOverride []int64} struct"
    - path: "backend/internal/repository/theme_segment_assignments.go"
      provides: "AssignThemeSegmentToEpisodeRange rewritten as insert-missing/delete-excess reconciliation, guard preserved verbatim"
    - path: "backend/internal/repository/theme_segment_assignments_integration_test.go"
      provides: "TestAssignThemeSegmentToEpisodeRangeGuardsInvalidRangeWithoutDBAccess extended for the new return shape + new shrink-removes/override-protection subtests replacing the old additive-only assertion"
  key_links:
    - from: "backend/internal/handlers/admin_content_anime_theme_segments.go"
      to: "backend/internal/repository/theme_segment_assignments.go"
      via: "AssignThemeSegmentToEpisodeRange(...) (*models.ThemeSegmentAssignmentSyncResult, error) call at both Create and Update call sites"
      pattern: "AssignThemeSegmentToEpisodeRange"
---

<objective>
Turn `AssignThemeSegmentToEpisodeRange` from a purely additive helper into the canonical Soll-Ist
reconciliation for `theme_segment_assignments` (P156-01/P156-02), while making the single most
dangerous failure mode of this phase structurally impossible to reintroduce: an incomplete range
must never be interpreted as "target set = empty" and wipe every assignment (P156-03's guard).
Legitimate per-episode time overrides must survive a range shrink instead of being silently
cascade-deleted.

Purpose: this function is the write-side source of truth every other surface in this phase reads
from (project timeline, release page, auto-assignment). Getting its reconciliation and its guard
right, with dedicated tests for both, is the precondition for everything downstream.
Output: reconciling `AssignThemeSegmentToEpisodeRange`, a typed sync-result struct, updated call
sites, and an integration test suite that proves shrink-removes, guard-blocks-wipe, and
override-protection as three independently named test cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-CONTEXT.md
@.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-RESEARCH.md
@.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-PATTERNS.md
</context>

<interfaces>
<!-- Guard to preserve VERBATIM, backend/internal/repository/theme_segment_assignments.go:84-86 -->
```go
if segmentID <= 0 || animeID <= 0 || fansubGroupID <= 0 || startEpisode <= 0 || endEpisode <= 0 {
	return nil, nil
}
```

<!-- Existing enumeration query to KEEP UNCHANGED, same file lines 93-97 (target set WITHIN the new range) -->
```sql
SELECT DISTINCT rev.id
FROM release_version_groups rvg
JOIN release_versions rev ON rev.id = rvg.release_version_id
	AND COALESCE(NULLIF(BTRIM(rev.version), ''), 'v1') = $3
JOIN fansub_releases fr ON fr.id = rev.release_id
JOIN episodes ep ON ep.id = fr.episode_id AND ep.anime_id = $1
WHERE rvg.fansub_group_id = $2
  AND COALESCE(ep.sort_index, CASE WHEN COALESCE(ep.episode_number, '') ~ '^[0-9]+$' THEN ep.episode_number::int ELSE NULL END) BETWEEN $4 AND $5
```

<!-- Insert-missing/delete-excess analog, admin_content_anime_themes.go:1279-1339 (unmodified, read-only reference) -->
```go
keptReleaseVersionIDs := make([]int64, 0, len(releaseVersionIDs))
for _, releaseVersionID := range releaseVersionIDs {
	kept, err := r.syncThemeSegmentPlaybackSourceForReleaseVersionTx(ctx, tx, segmentID, releaseVersionID)
	if err != nil { return err }
	if kept { keptReleaseVersionIDs = append(keptReleaseVersionIDs, releaseVersionID) }
}
if _, err := tx.Exec(ctx, `
	DELETE FROM theme_segment_playback_sources
	WHERE theme_segment_id = $1 AND NOT (release_version_id = ANY($2))
`, segmentID, keptReleaseVersionIDs); err != nil { ... }
```

<!-- adminThemeRepository interface entry to update, backend/internal/handlers/admin_content_handler.go:79 -->
```go
AssignThemeSegmentToEpisodeRange(ctx context.Context, segmentID int64, animeID int64, fansubGroupID int64, version string, startEpisode int, endEpisode int) ([]int64, error)
```

<!-- Handler call sites (BOTH), admin_content_anime_theme_segments.go:337-356 (Create) and :551-567 (Update) -->
```go
newlyAssigned, rangeErr := h.themeRepo.AssignThemeSegmentToEpisodeRange(
	c.Request.Context(), created.ID, animeID, *created.FansubGroupID, created.Version, *created.StartEpisode, *created.EndEpisode,
)
if rangeErr != nil {
	log.Printf(...)
} else if len(newlyAssigned) > 0 {
	// reload + fan-out
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconciling AssignThemeSegmentToEpisodeRange + typed sync result + integration tests</name>
  <files>backend/internal/models/admin_anime_themes.go, backend/internal/repository/theme_segment_assignments.go, backend/internal/repository/theme_segment_assignments_integration_test.go</files>
  <read_first>backend/internal/repository/theme_segment_assignments.go (full file, current state), backend/internal/repository/theme_segment_assignments_integration_test.go (full file, current state -- includes the "additive: a narrower range on a later call does not remove earlier assignments" subtest at lines 127-136 that this task REPLACES with the opposite behavior), backend/internal/models/admin_anime_themes.go (existing AdminThemeSegmentAssignment struct, lines 130-137), database/migrations/0142_theme_segment_episode_overrides.up.sql (composite FK ON DELETE CASCADE), backend/internal/repository/admin_content_anime_themes.go:1279-1339 (syncThemeSegmentPlaybackSourceTx reconciliation analog, read-only reference), database/migrations/0143_theme_segment_render_cache_release_version.up.sql (release_version_id column on render cache)</read_first>
  <behavior>
    - Test 1 (keep, extend): `TestAssignThemeSegmentToEpisodeRangeGuardsInvalidRangeWithoutDBAccess` -- for every invalid-input case (segmentID/animeID/fansubGroupID<=0, startEpisode/endEpisode<=0), the call returns `(nil, nil)` with the NEW return type too: `result` is nil, `err` is nil -- this is THE dedicated regression test for the phase's single most dangerous failure mode and must keep proving zero DB access (nil `db` field would panic if reached)
    - Test 2 (replace the old "additive" subtest): a range SHRINK (e.g. segment assigned across episodes 1-3, then re-synced with range 2-2) REMOVES the out-of-range, unprotected assignments (episodes 1 and 3) and returns them in `result.Removed`; the in-range assignment (episode 2) stays and is NOT in `result.Added` (already existed) or `result.Removed`
    - Test 3: a range GROW (segment re-synced with a wider range) adds the newly-covered episodes to `result.Added`, does not touch already-assigned episodes, and `result.Removed` is empty
    - Test 4: an assignment with a live `theme_segment_episode_overrides` row that falls OUTSIDE the new range is NOT deleted, appears in `result.ProtectedByOverride`, and does NOT appear in `result.Removed`
    - Test 5: deletion never touches an assignment belonging to a DIFFERENT anime/fansub_group/version domain than the segment being reconciled
  </behavior>
  <action>
    In `backend/internal/models/admin_anime_themes.go`, add a new exported struct near the existing `AdminThemeSegmentAssignment` struct: `ThemeSegmentAssignmentSyncResult` with three fields `Added []int64`, `Removed []int64`, `ProtectedByOverride []int64` (`json:"added"`, `json:"removed"`, `json:"protected_by_override"`, matching this file's snake_case convention). In `backend/internal/repository/theme_segment_assignments.go`, change `AssignThemeSegmentToEpisodeRange`'s return type from `([]int64, error)` to `(*models.ThemeSegmentAssignmentSyncResult, error)`. Preserve the existing guard verbatim as the first statement, still returning `(nil, nil)` -- this must run before ANY query, insert, or delete. Keep the existing target-set enumeration query and the existing insert-missing loop completely unchanged. After the insert loop, add the deletion side: (1) compute `domainReleaseVersionIDs` by re-running the SAME join-chain as the enumeration query but WITHOUT the `BETWEEN $4 AND $5` episode filter (every release_version_id belonging to this animeID+fansubGroupID+normalizedVersion, regardless of episode) -- this defines the domain deletion may ever touch. (2) Load `existingAssignmentIDs` for this segmentID (`SELECT release_version_id FROM theme_segment_assignments WHERE theme_segment_id=$1`). (3) Compute `deletionCandidates` = `existingAssignmentIDs` intersected with `domainReleaseVersionIDs`, minus `targetReleaseVersionIDs` -- assignments inside the domain but outside the new range. (4) Run the override-protection query from 156-PATTERNS.md Pattern 2 (`LEFT JOIN theme_segment_episode_overrides o ON o.theme_segment_id=tsa.theme_segment_id AND o.release_version_id=tsa.release_version_id WHERE tsa.theme_segment_id=$1 AND NOT (tsa.release_version_id = ANY($2)) AND o.id IS NULL`, `$2`=`targetReleaseVersionIDs`) restricted to `deletionCandidates` to split them into `toRemove` (no override) and `protectedByOverride` (has override). (5) Execute the actual DELETE, domain-scoped: `DELETE FROM theme_segment_assignments WHERE theme_segment_id=$1 AND release_version_id = ANY($domainReleaseVersionIDs) AND NOT (release_version_id = ANY($kept))` where `$kept` = `targetReleaseVersionIDs` UNION `protectedByOverride` -- the `AND release_version_id = ANY($domainReleaseVersionIDs)` clause is load-bearing: it is what stops this delete from ever touching an assignment in a different anime/group/version domain (P156-03), layered on top of the `NOT (id = ANY(kept))` shape borrowed from `syncThemeSegmentPlaybackSourceTx`. (6) Within the SAME transaction, clean up now-orphaned playback/render state for the removed release versions: `DELETE FROM theme_segment_playback_sources WHERE theme_segment_id=$1 AND release_version_id = ANY($toRemove)` and `DELETE FROM theme_segment_render_cache WHERE theme_segment_id=$1 AND release_version_id = ANY($toRemove)` -- both tables already have a `release_version_id` column (migrations 0054/0141/0143); this is the "gezielte Aufraeumarbeiten" CONTEXT.md calls for, done transactionally. (7) Commit and return `&models.ThemeSegmentAssignmentSyncResult{Added: newlyAssigned, Removed: toRemove, ProtectedByOverride: protectedByOverride}`. Update the function's doc comment: replace the "ADDITIV: bestehende Zuweisungen ausserhalb des Bereichs werden NICHT entfernt" sentence with a note that this is now a reconciling Soll-Ist-Synchronisation (Phase 156, supersedes Quick-Task 260819-lm5's additive design), and that the guard clause is the anchor for the "unvollstaendiger Bereich loescht nichts" requirement. In `theme_segment_assignments_integration_test.go`, REPLACE the `t.Run("additive: a narrower range on a later call does not remove earlier assignments", ...)` subtest (lines 127-136) with the shrink-removes behavior from Test 2 -- do not keep both assertions, they are now contradictory. Add the grow (Test 3), override-protection (Test 4), and cross-domain-safety (Test 5) subtests as new `t.Run` blocks, seeding via `pool.Exec` following this file's existing explicit-ID seeding style.
  </action>
    <acceptance_criteria>
    - `ThemeSegmentAssignmentSyncResult{Added, Removed, ProtectedByOverride []int64}` exists in models/admin_anime_themes.go
    - the guard clause runs and returns `(nil, nil)` before any `tx.Query`/`tx.Exec` call for every invalid-input case (proven by the nil-db-field no-panic test)
    - a seeded shrink scenario shows `result.Removed` contains exactly the out-of-range, unprotected release_version_ids and `result.Added` is empty
    - a seeded grow scenario shows `result.Added` contains exactly the newly-covered release_version_ids and `result.Removed` is empty
    - a seeded override-protected scenario shows the protected release_version_id in `result.ProtectedByOverride`, absent from `result.Removed`, and its `theme_segment_episode_overrides` row still exists after the call
    - a seeded cross-domain scenario shows an assignment belonging to a different anime/group/version is present before AND after the call (untouched)
  </acceptance_criteria>
  <verify>
    <automated>docker run --rm -v /home/d1sk/team4s:/workspace -w /workspace/backend golang:1.25-alpine sh -c "go build ./... && go vet ./..."</automated>
  </verify>
  <done>The function compiles with the new signature, the guard test still passes with zero DB access, and (once run against TEAM4S_PHASE117_TEST_DSN) shrink-removes, grow-adds, override-protection, and cross-domain-safety are each proven by their own named subtest.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the reconciling signature through the interface and both handler call sites</name>
  <files>backend/internal/handlers/admin_content_handler.go, backend/internal/handlers/admin_content_anime_theme_segments.go, backend/internal/handlers/admin_content_anime_theme_segment_range_autoassign_test.go</files>
  <read_first>backend/internal/handlers/admin_content_handler.go (adminThemeRepository interface, line 79), backend/internal/handlers/admin_content_anime_theme_segments.go (both call sites, CreateAnimeSegment lines 332-356 and UpdateAnimeSegment lines 543-567), backend/internal/handlers/admin_content_anime_theme_segment_range_autoassign_test.go (full file, current state -- the fake repo's AssignThemeSegmentToEpisodeRange method and all four existing tests reference the old []int64 shape)</read_first>
  <action>
    In `backend/internal/handlers/admin_content_handler.go`, update the `adminThemeRepository` interface's `AssignThemeSegmentToEpisodeRange` entry to return `(*models.ThemeSegmentAssignmentSyncResult, error)` instead of `([]int64, error)`. In `admin_content_anime_theme_segments.go`, update BOTH call sites (CreateAnimeSegment and UpdateAnimeSegment): rename the local variable from `newlyAssigned` to `rangeSync` (a `*models.ThemeSegmentAssignmentSyncResult`). Fix the reload condition per the known pitfall: change `else if len(newlyAssigned) > 0` to `else if rangeSync != nil && (len(rangeSync.Added) > 0 || len(rangeSync.Removed) > 0)` at BOTH call sites -- a range-shrink-only update (Added empty, Removed non-empty) must still trigger the reload, otherwise the response's `assigned_release_version_ids` goes stale. Update the fan-out call `h.resetAndQueueSegmentRenderForAssignments(c.Request.Context(), created.ID, newlyAssigned, nil, false)` to pass `rangeSync.Added` (fan-out semantics for newly-inserted assignments are unchanged; removed assignments already had their playback/render rows cleaned up transactionally inside the repository in Task 1). Extend the final JSON response at both call sites to surface the sync result for admin visibility (CONTEXT.md: override-protected exclusions must be "sichtbar gemeldet") -- change `c.JSON(http.StatusCreated, gin.H{"data": created})` to `c.JSON(http.StatusCreated, gin.H{"data": created, "range_sync": rangeSync})` (equivalent `http.StatusOK` change in Update); when `rangeSync` is nil, the field naturally serializes as JSON `null` -- existing frontend code that only reads `.data` is unaffected by this additive field. In `admin_content_anime_theme_segment_range_autoassign_test.go`, update the fake repo's `AssignThemeSegmentToEpisodeRange` method signature to return `(*models.ThemeSegmentAssignmentSyncResult, error)`, and its `rangeResult` field type from `[]int64` to `*models.ThemeSegmentAssignmentSyncResult`. Fix all four existing tests (`TestCreateAnimeSegment_RangeAutoAssignsAllEpisodesInRange`, `TestCreateAnimeSegment_RangeAutoAssignIdempotentSkipsReload`, `TestCreateAnimeSegment_RangeAutoAssignFailureIsNonFatal`, `TestUpdateAnimeSegment_RangeAutoAssignUsesEffectivePatchedValues`) to construct `rangeResult` as `&models.ThemeSegmentAssignmentSyncResult{Added: []int64{...}}` instead of a bare slice; `TestCreateAnimeSegment_RangeAutoAssignIdempotentSkipsReload`'s "no new assignments" case now sets `rangeResult: &models.ThemeSegmentAssignmentSyncResult{}` (empty struct, not nil) to keep proving the idempotent-skip-reload path with the new type.
  </action>
    <acceptance_criteria>
    - `adminThemeRepository` interface's `AssignThemeSegmentToEpisodeRange` entry returns `(*models.ThemeSegmentAssignmentSyncResult, error)`
    - both handler call sites reload when `len(rangeSync.Added) > 0 || len(rangeSync.Removed) > 0` (not just Added > 0)
    - the JSON response body includes a `range_sync` field alongside `data` at both Create (201) and Update (200) success paths
    - all four existing range-auto-assign handler tests pass against the new fake-repo signature (`go test ./internal/handlers/... -run RangeAutoAssign`)
  </acceptance_criteria>
  <verify>
    <automated>docker run --rm -v /home/d1sk/team4s:/workspace -w /workspace/backend golang:1.25-alpine sh -c "go build ./... && go vet ./... && go test ./internal/handlers/... -run 'RangeAutoAssign' -v"</automated>
  </verify>
  <done>Interface, both handler call sites, and the fake-repo test file all compile against the new return type; all four existing range-auto-assign handler tests pass unmodified in intent (idempotent-skip-reload, non-fatal-on-error, effective-patched-values), and the reload condition now also fires on shrink-only updates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin segment create/update request -> repository | Admin-authenticated input (episode range, group, version) reaches a function that can DELETE rows; must never over-delete on malformed/partial input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-03 | Tampering | `AssignThemeSegmentToEpisodeRange` reconciliation delete | mitigate | Domain-scoped `DELETE ... WHERE release_version_id = ANY($domainReleaseVersionIDs) AND NOT (... ANY($kept))`; guard clause runs before any DB access on incomplete input; dedicated no-DB-access test proves the guard structurally (nil db field would panic if reached) |
| T-156-04 | Tampering | Override-protected assignment silently deleted by a range shrink | mitigate | Pattern 2 `LEFT JOIN ... WHERE o.id IS NULL` excludes overridden rows from the delete candidate set before the DELETE runs; dedicated test (Test 4) |
| T-156-05 | Tampering | Cross-domain (different anime/group/version) assignment accidentally deleted | mitigate | `domainReleaseVersionIDs` computed via the same join-chain as the range enumeration, without the episode filter; DELETE is additionally scoped to this set; dedicated test (Test 5) |
| T-156-06 | Tampering | All new SQL uses string-concatenated identifiers | accept | All new queries use pgx parameterized placeholders (`$1`, `$2`, ...) exclusively, matching the existing file's convention; no deviation introduced |
</threat_model>

<verification>
`go build`/`go vet` green after both tasks; guard test proves zero-DB-access no-op on incomplete input; integration test suite (once run against `TEAM4S_PHASE117_TEST_DSN`) proves shrink-removes, grow-adds, override-protection-survives, and cross-domain-safety as four independently named subtests; handler tests green against the new interface shape.
</verification>

<success_criteria>
`AssignThemeSegmentToEpisodeRange` is a true Soll-Ist reconciliation, the guard against wiping on incomplete input is preserved and independently tested, override-protected assignments survive shrinks, and both admin call sites correctly reload on shrink-only changes.
</success_criteria>

<output>
Create `.planning/phases/156-segment-domain-konsistenz-und-oeffentliche-release-projektion/156-02-SUMMARY.md` when done
</output>
