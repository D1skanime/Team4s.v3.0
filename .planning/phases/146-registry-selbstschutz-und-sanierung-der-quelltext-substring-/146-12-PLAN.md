---
phase: 146
plan: 12
type: execute
wave: 3
depends_on: ["146-01", "146-02", "146-03"]
files_modified:
  - backend/internal/handlers/admin_content_release_version_media_test.go
autonomous: true
requirements: ["Criterion 5", "Criterion 6"]

must_haves:
  truths:
    - "admin_content_release_version_media_test.go's 17 handler-behavior claims (thumbnail/validation defaults, permission preservation, category/soft-delete/reorder/ownership, upload-constraint/capability exposure) are proven via real httptest calls against the actual handler, not by reading admin_content_release_version_media.go's source"
  artifacts:
    - path: "backend/internal/handlers/admin_content_release_version_media_test.go"
      provides: "real httptest proofs replacing all 17 functions' os.ReadFile-based assertions; the many already-compliant pure unit/httptest functions in this file (thumbnail generation, MIME/dimension validation via direct function calls) remain untouched"
  key_links:
    - from: "backend/internal/handlers/admin_content_release_version_media_test.go"
      to: "AdminContentHandler.UploadReleaseVersionMediaFile / PatchReleaseVersionMedia / etc."
      via: "real httptest calls with fake mediaRepo doubles and a real permissions.Service backed by a fake Resolver"
      pattern: "canMutateReleaseVersionMediaRelation"
---

<objective>
Remediate the second-largest of Phase 146's 20 locked security-relevant Block-2 files:
`admin_content_release_version_media_test.go` (727 lines, 40 test functions, 17 of which use
`os.ReadFile` on `admin_content_release_version_media.go` or a related file to claim handler
behavior from source text).

Purpose: closes Criteria 5/6 for this file. The other 23 functions in this file (GIF/PNG/JPEG
thumbnail generation, MIME/dimension/frame-count validation, result-shape assertions) already call
real functions directly (e.g. `TestGenerateGIFThumbnail`, `TestReleaseVersionMedia_AllowedMIMETypes`)
and are NOT the forbidden pattern — leave all of them untouched, touch only the 17 named below.
Output: all 17 `os.ReadFile`-based functions rewritten as real httptest calls against the actual
`AdminContentHandler` methods, with fake `mediaRepo` doubles and a real `permissions.Service`
backed by a fake `Resolver`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-CONTEXT.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-04-PLAN.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-PATTERNS.md
</context>

<interfaces>
Current state (re-read 2026-09-04, `backend/internal/handlers/admin_content_release_version_media_test.go`,
727 lines): the 17 functions using `os.ReadFile` on `admin_content_release_version_media.go` (or
a named sibling file) are, grouped by theme:

Group A — existence/permission-preservation (2 functions):
`TestUploadReleaseVersionMediaHandlerExists` (line 92), `TestPatchReleaseVersionMediaResponseKeepsActorPermissions` (line 109).

Group B — category/validation rejection (6 functions):
`TestReleaseVersionMedia_InvalidCategoryRejectsUpload` (line 279, reads the handler at both line
306 and 320), `TestReleaseVersionMedia_SVGRejectionByMIMEType` (line 314, reads at 329),
`TestReleaseVersionMedia_FileSizeRejection` (line 328, reads at 338 the category file too),
`TestReleaseVersionMedia_DimensionRejection` (line 337, reads at 347),
`TestReleaseVersionMedia_GIFFrameLimitRejection` (line 346, reads at 410),
`TestReleaseVersionMedia_PreviewRejectedForFunOuttake` (line 406, same read).

Group C — patch/category-change/soft-delete/reorder (4 functions):
`TestPatchReleaseVersionMediaAllowsCategoryChange` (line 430, reads at 431 and 442, the category
file, plus the repository at 455), `TestReleaseVersionMedia_SoftDeleteExcludesFromList` (line 454,
reads at 455 the repository), `TestReleaseVersionMedia_ReorderRequiresVersionOwnership` (line 474,
reads at 475 the reorder file and at 480 the repository),
`TestReleaseVersionMedia_BrokenUploadCannotLeaveReadyStatus` (line 493, reads at 494).

Group D — upload constraints and capability exposure (5 functions — CORRECTED, was
undercounted as 4 in an earlier revision of this plan; `TestReleaseVersionMedia_MaxFilesPerUpload`
was previously omitted, and its own `os.ReadFile` call at line 611 was wrongly attributed below to
the adjacent, separate `TestReleaseVersionMedia_UploadMissingCategoryError` function):
`TestReleaseVersionMedia_UploadMissingCategoryError` (line 589, reads at 601 — this function's
own, sole `os.ReadFile` call),
`TestReleaseVersionMedia_MaxFilesPerUpload` (line 608, reads at 611 — a SEPARATE function
immediately following `UploadMissingCategoryError`; it already has a real, non-source assertion
`assert.Equal(t, 20, rvmMaxFilesPerUpload, ...)` which must be KEPT unchanged, but its
`os.ReadFile`-based `strings.Contains(string(src), "TOO_MANY_FILES")` claim must become a real
triggered rejection),
`TestReleaseVersionMedia_CapabilitiesExposeOwnDelete` (line 692, reads at 693),
`TestReleaseVersionMedia_HandlerUsesContributorGroupMutationGuard` (line 706, reads at 707),
`TestReleaseVersionMedia_UploadReturnsAuthoritativeSourceRevision` (line 718, reads at 719).

Production handler: `AdminContentHandler` methods in `admin_content_release_version_media.go`
(upload/patch/list/reorder), `admin_content_release_version_media_category.go` (category
validation), `admin_content_release_version_media_reorder.go` (reorder), plus
`../repository/release_version_media_repository.go` for the repository-layer claims mixed into 2
of these functions. `TestReplaceReleaseVersionMediaFileRejectsNoAuth`
(`admin_content_release_version_media_replace_test.go`, remediated in Plan 146-08) is the exact
real-httptest-against-this-handler-family pattern to mirror.

`permissionSvc` on `AdminContentHandler` is a concrete `*permissions.Service` (not an interface)
and cannot be replaced with an arbitrary fake struct literal. Where a task in this plan needs to
control an allow/deny permission outcome, construct a real
`permissions.NewService(releasePermissionResolverStub{...})` — the existing, working pattern
already defined in `admin_content_fansub_releases_test.go` (a `releasePermissionResolverStub`
implementing `permissions.Resolver`: `ResolveFansubGroup`/`ResolveRelease`/
`ResolveReleaseVersion`/`ResolveReleaseVersionMedia`/`ListActorGroupRoles`/
`ListActorContributionRolesForVersion`) — rather than inventing a new fake `permissionSvc` type.
Most functions in this file only need a fake `mediaRepo` double; only reach for the resolver-stub
pattern where a function's claim is actually about permission gating.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remediate Group A (existence/permission-preservation) and Group B (category/validation rejection) — 8 functions</name>
  <files>backend/internal/handlers/admin_content_release_version_media_test.go</files>
  <read_first>backend/internal/handlers/admin_content_release_version_media_test.go, backend/internal/handlers/admin_content_release_version_media.go, backend/internal/handlers/admin_content_release_version_media_category.go, backend/internal/handlers/admin_content_fansub_releases_test.go</read_first>
  <behavior>
    - `TestUploadReleaseVersionMediaHandlerExists`: replace with a real httptest upload request
      through the actual handler (with a fake `mediaRepo`), asserting a real, non-404 response —
      the handler existing and being routable is proven by successfully invoking it, not by
      reading its source.
    - `TestPatchReleaseVersionMediaResponseKeepsActorPermissions`: real httptest PATCH request;
      assert the real JSON response includes the actor's permission-derived fields (e.g.
      `can_delete`/`can_edit`, whatever the real response DTO exposes) matching a real
      `permissions.Service` (backed by a fake `Resolver`, per the `<interfaces>` note) configured
      allow/deny state.
    - The 6 Group B functions: for each, fire a real httptest upload/patch request whose payload
      deliberately violates the claimed constraint (invalid category, disallowed MIME/SVG,
      oversized file, disallowed dimensions, over-limit GIF frame count, a `fun_outtake`/preview
      category on a context that forbids it) and assert the real handler returns the real
      rejection (4xx status + the expected stable error code in the JSON body) — proving each
      constraint by triggering it, not by grepping its validation source.
  </behavior>
  <action>
  Implement all 8 rewrites as real httptest calls against the real `AdminContentHandler` methods,
  using a shared fake `mediaRepo` double (struct literal, no mock framework) reused across this
  file's remaining tasks. For claims that are actually about permission gating (e.g.
  `PatchReleaseVersionMediaResponseKeepsActorPermissions`), construct `permissionSvc` as a real
  `permissions.NewService(releasePermissionResolverStub{...})`, mirroring
  `admin_content_fansub_releases_test.go`'s existing resolver-stub construction exactly — do not
  build a fake `permissionSvc` struct literal, since it is a concrete `*permissions.Service` type.
  Read `admin_content_release_version_media.go` and `admin_content_release_version_media_category.go`
  fully first for exact validation error codes/response shapes before writing assertions — many of
  these constraints likely already have their exact error-code constants defined near the pure
  validation-function tests already present and compliant elsewhere in this same file (e.g.
  `TestRVMValidCategories`, `TestImageExtFromMimeRVM`) — reuse those constants directly instead of
  re-deriving them.
  </action>
  <acceptance_criteria>
    - `grep -c "os.ReadFile" backend/internal/handlers/admin_content_release_version_media_test.go` is at most 11 after this task (8 of the original 19 os.ReadFile call sites removed)
    - `go test ./backend/internal/handlers/... -run "TestUploadReleaseVersionMediaHandlerExists|TestPatchReleaseVersionMediaResponseKeepsActorPermissions|TestReleaseVersionMedia_InvalidCategoryRejectsUpload|TestReleaseVersionMedia_SVGRejectionByMIMEType|TestReleaseVersionMedia_FileSizeRejection|TestReleaseVersionMedia_DimensionRejection|TestReleaseVersionMedia_GIFFrameLimitRejection|TestReleaseVersionMedia_PreviewRejectedForFunOuttake" -v` passes, 8/8 PASS
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/handlers/... -run "TestUploadReleaseVersionMediaHandlerExists|TestPatchReleaseVersionMediaResponseKeepsActorPermissions|TestReleaseVersionMedia_InvalidCategoryRejectsUpload|TestReleaseVersionMedia_SVGRejectionByMIMEType|TestReleaseVersionMedia_FileSizeRejection|TestReleaseVersionMedia_DimensionRejection|TestReleaseVersionMedia_GIFFrameLimitRejection|TestReleaseVersionMedia_PreviewRejectedForFunOuttake" -v</automated>
  </verify>
  <done>8 existence/permission/validation-rejection claims are proven via real httptest requests triggering the actual rejections.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remediate Group C (patch/category-change/soft-delete/reorder) — 4 functions</name>
  <files>backend/internal/handlers/admin_content_release_version_media_test.go</files>
  <read_first>backend/internal/handlers/admin_content_release_version_media_test.go, backend/internal/handlers/admin_content_release_version_media_reorder.go, backend/internal/repository/release_version_media_repository.go</read_first>
  <behavior>
    - `TestPatchReleaseVersionMediaAllowsCategoryChange`: real httptest PATCH changing a fake
      repo's seeded row's category; assert the fake repo received the new category value and the
      response reflects it.
    - `TestReleaseVersionMedia_SoftDeleteExcludesFromList`: real httptest DELETE (soft) followed by
      a real httptest LIST call against the same fake repo state; assert the deleted item is
      excluded from the list response.
    - `TestReleaseVersionMedia_ReorderRequiresVersionOwnership`: real httptest reorder request for
      a media item belonging to a DIFFERENT release version than the one in the request path;
      assert a real ownership-violation rejection.
    - `TestReleaseVersionMedia_BrokenUploadCannotLeaveReadyStatus`: real httptest upload request
      where the fake repo/service simulates a broken/incomplete upload; assert the real response
      status is never the "ready" state.
  </behavior>
  <action>
  Implement all 4 rewrites as real httptest calls against the real handler methods, reusing the
  shared fake `mediaRepo` double (and, where a claim concerns permission gating, the real
  `permissions.Service`-backed-by-resolver-stub construction) from Task 1. Read
  `admin_content_release_version_media_reorder.go` fully first for the reorder request/response
  shape.
  </action>
  <acceptance_criteria>
    - `grep -c "os.ReadFile" backend/internal/handlers/admin_content_release_version_media_test.go` is at most 6 after this task
    - `go test ./backend/internal/handlers/... -run "TestPatchReleaseVersionMediaAllowsCategoryChange|TestReleaseVersionMedia_SoftDeleteExcludesFromList|TestReleaseVersionMedia_ReorderRequiresVersionOwnership|TestReleaseVersionMedia_BrokenUploadCannotLeaveReadyStatus" -v` passes, 4/4 PASS
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/handlers/... -run "TestPatchReleaseVersionMediaAllowsCategoryChange|TestReleaseVersionMedia_SoftDeleteExcludesFromList|TestReleaseVersionMedia_ReorderRequiresVersionOwnership|TestReleaseVersionMedia_BrokenUploadCannotLeaveReadyStatus" -v</automated>
  </verify>
  <done>4 mutation/list-filtering/ownership claims are proven via real httptest requests against the fake repository state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remediate Group D (upload constraints and capability exposure) — 5 functions</name>
  <files>backend/internal/handlers/admin_content_release_version_media_test.go</files>
  <read_first>backend/internal/handlers/admin_content_release_version_media_test.go, backend/internal/handlers/admin_content_release_version_media.go</read_first>
  <behavior>
    - `TestReleaseVersionMedia_UploadMissingCategoryError`: real httptest upload request omitting
      the category field; assert the real rejection.
    - `TestReleaseVersionMedia_MaxFilesPerUpload`: KEEP the existing real, non-source assertion
      `assert.Equal(t, 20, rvmMaxFilesPerUpload, "max files per upload must be 20")` unchanged; then
      REPLACE the `os.ReadFile`-based `strings.Contains(string(src), "TOO_MANY_FILES")` claim with a
      real httptest upload request whose file count exceeds `rvmMaxFilesPerUpload`; assert the real
      handler response returns the real `TOO_MANY_FILES` rejection (4xx status + that exact error
      code in the JSON body), proving the limit is enforced by execution, not merely present in
      source.
    - `TestReleaseVersionMedia_CapabilitiesExposeOwnDelete`: real httptest list/detail request for
      a contributor who owns an item; assert the real response's capability field
      (`can_delete`/equivalent) is `true` for their own item and `false` for another
      contributor's item, using a real `permissions.Service` backed by a fake `Resolver`
      configured accordingly.
    - `TestReleaseVersionMedia_HandlerUsesContributorGroupMutationGuard`: real httptest mutation
      request from a contributor NOT in the owning group; assert a real rejection, proving the
      guard is load-bearing (not merely present in source).
    - `TestReleaseVersionMedia_UploadReturnsAuthoritativeSourceRevision`: real httptest upload
      request; assert the response's `source_revision` field matches the value the fake repo
      actually persisted, not a client-supplied one.
  </behavior>
  <action>
  Implement all 5 rewrites as real httptest calls, reusing the shared fake `mediaRepo` double (and
  the real `permissions.Service`-backed-by-resolver-stub construction where permission gating is
  the actual claim) from Task 1/2.
  </action>
  <acceptance_criteria>
    - `grep -c "os.ReadFile" backend/internal/handlers/admin_content_release_version_media_test.go` returns 0
    - `go test ./backend/internal/handlers/... -run "TestReleaseVersionMedia_UploadMissingCategoryError|TestReleaseVersionMedia_MaxFilesPerUpload|TestReleaseVersionMedia_CapabilitiesExposeOwnDelete|TestReleaseVersionMedia_HandlerUsesContributorGroupMutationGuard|TestReleaseVersionMedia_UploadReturnsAuthoritativeSourceRevision" -v` passes, 5/5 PASS
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/handlers/... -v</automated>
  </verify>
  <done>All 17 os.ReadFile-based functions in this file are gone; the file's other 23 already-compliant functions are untouched; the full package test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin/contributor browser -> release-version-media upload/patch/reorder/delete endpoints | Contributor-group-scoped mutation guard, category/MIME/dimension validation, and capability-exposure surface |
| Test suite -> false-assurance risk | 17 functions claiming handler-level validation, ownership, and capability behavior without ever executing the handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-18 | Repudiation (false assurance) | `admin_content_release_version_media_test.go` | mitigate | All 17 presence-based source assertions replaced with real httptest calls proving validation rejection, upload limits, ownership guards, and capability exposure by execution |
</threat_model>

<verification>
- `cd backend && go build ./...`
- `go test ./internal/handlers/... -run TestReleaseVersionMedia -v`
- `go test ./internal/handlers/... -v` (full package regression)
</verification>

<success_criteria>
All 17 os.ReadFile-based functions in this file are replaced with real, executed proofs; the
file's other 23 already-compliant functions remain untouched.
</success_criteria>

<output>
Create `.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-12-SUMMARY.md` when done
</output>
</output>
