---
phase: 146
plan: 05
type: execute
wave: 3
depends_on: ["146-01", "146-02", "146-03"]
files_modified:
  - backend/internal/repository/hist_group_member_roles_whitelist_test.go
  - backend/internal/repository/member_claims_repository_claim_activation_test.go
autonomous: true
requirements: ["Criterion 5", "Criterion 6"]

must_haves:
  truths:
    - "hist_group_member_roles_whitelist_test.go and member_claims_repository_claim_activation_test.go prove their catalog-guard claims via real Postgres calls, not source inspection; their legitimate absence checks are preserved unchanged (D-10, D-12)"
  artifacts:
    - path: "backend/internal/repository/hist_group_member_roles_whitelist_test.go"
      provides: "real-Postgres proof that RoleCodeExistsForContext is parameterized and that blank/invalid codes resolve neutrally"
    - path: "backend/internal/repository/member_claims_repository_claim_activation_test.go"
      provides: "real-Postgres proof that ResolvePendingRolesToActive joins role_definitions on assignable + fansub_group context"
  key_links:
    - from: "backend/internal/repository/hist_group_member_roles_whitelist_test.go"
      to: "HistGroupMemberRolesRepository.RoleCodeExistsForContext"
      via: "real Postgres call replacing the source-substring presence assertions"
      pattern: "RoleCodeExistsForContext"
---

<objective>
Remediate two of Phase 146's 20 locked security-relevant Block-2 files (per Plan 146-04's D-08
lock): `hist_group_member_roles_whitelist_test.go` and
`member_claims_repository_claim_activation_test.go`, closing Success Criteria 5/6 for these two
files.

Purpose: both files today prove security-relevant SQL-parameterization/catalog-guard claims by
reading their own production `.go` source and grep-matching fragments — CLAUDE.md's Teststil
convention (and Pitfall 5 in `146-RESEARCH.md`) forbids this for the presence half of these
assertions while explicitly preserving the legitimate absence-check half.
Output: both files' presence assertions replaced with real Postgres calls against the actual
repository methods; the absence checks in both files are untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-CONTEXT.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-04-PLAN.md
</context>

<interfaces>
Current state (re-read 2026-09-04):

`backend/internal/repository/hist_group_member_roles_whitelist_test.go` (full file, 46 lines):
`TestHistGroupMemberRolesUseCatalogContext` (lines 9-31): an absence loop over 3 forbidden
identifiers (`groupHistoryDialogRoleWhitelist`, `IsGroupHistoryWhitelistRole`,
`code = ANY($` — KEEP unchanged, legitimate) immediately followed by 2 presence assertions:
`strings.Contains(source, "$2 = ANY(contexts)")` and
`strings.Contains(source, "RoleCodeExistsForContext(ctx, code, \"group_history\")")` — REPLACE.
`TestHistGroupMemberRolesKeepNeutralInvalidBehavior` (lines 34-45): 2 presence assertions
(`if strings.TrimSpace(code) == ""` and `return false, nil`) claiming blank/invalid codes resolve
neutrally — REPLACE both with a real call.
The production method under test is `HistGroupMemberRolesRepository.RoleCodeExistsForContext(ctx
context.Context, roleCode string, contextName string) (bool, error)`
(`hist_group_member_roles_repository.go:320`).

`backend/internal/repository/member_claims_repository_claim_activation_test.go` (full file,
56 lines): `TestResolvePendingRolesToActive_ExistsOnRepository` (lines 12-23) is a `reflect`-based
signature check — NOT the forbidden pattern (no `os.ReadFile`), leave unchanged.
`TestResolvePendingRolesToActiveUsesCanonicalCatalog` (lines 26-40): 3 presence assertions
(`"JOIN role_definitions rd ON rd.code = r.role_code"`, `"'fansub_group' = ANY(rd.contexts)"`,
`"rd.assignable"`) — REPLACE; plus an absence check (`IsGroupHistoryWhitelistRole` /
`IsKnownFansubGroupRole` must not appear) — KEEP. `TestVerifyClaimActivatesRoles_*` (3 functions,
lines 46-56) are pre-existing `t.Skip("RED Wave 0 ...")` stubs from an EARLIER phase's Plan 04 —
they do not use `os.ReadFile`, are out of this plan's scope, and must not be touched (avoid scope
creep into unrelated pre-existing debt). The production method is
`MemberClaimsRepository.ResolvePendingRolesToActive(ctx, memberID, fansubGroupID, actorID int64)
error` (`member_claims_role_activation_repository.go`).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remediate hist_group_member_roles_whitelist_test.go</name>
  <files>backend/internal/repository/hist_group_member_roles_whitelist_test.go</files>
  <read_first>backend/internal/repository/hist_group_member_roles_whitelist_test.go, backend/internal/repository/hist_group_member_roles_repository.go, backend/internal/repository/membership_baseline_registry_test.go</read_first>
  <behavior>
    - `TestHistGroupMemberRolesUseCatalogContext`: keep the 3-item absence loop unchanged. Replace
      the 2 presence assertions with a real Postgres proof: seed at least 2 `role_definitions` rows
      (one with `group_history` in `contexts`, one without), call
      `NewHistGroupMemberRolesRepository(pool).RoleCodeExistsForContext(ctx, code, "group_history")`
      for both, and assert `true, nil` for the seeded-with-context row and `false, nil` for the
      other — a real, executed, parameterized-context proof.
    - `TestHistGroupMemberRolesKeepNeutralInvalidBehavior`: replace both presence assertions with
      real calls: `RoleCodeExistsForContext(ctx, "", "group_history")` must return `(false, nil)`
      (blank code), and `RoleCodeExistsForContext(ctx, "totally_invalid_code_xyz", "group_history")`
      must return `(false, nil)` (unknown code) — proving "neutral false, never an error" behavior
      by actually calling it.
  </behavior>
  <action>
  Implement both rewrites. Reuse `testsupport.OpenPhase145Postgres(t)` and the
  `phase145MigrationPath` pattern already established in `membership_baseline_registry_test.go`
  (same package) — declare a local copy of the helper in this file if it is not already accessible.
  </action>
  <acceptance_criteria>
    - `grep -A25 "func TestHistGroupMemberRolesUseCatalogContext" backend/internal/repository/hist_group_member_roles_whitelist_test.go | grep -c "strings.Contains(source"` returns 0
    - `TEAM4S_PHASE145_TEST_DSN=<local dsn> go test ./backend/internal/repository/... -run TestHistGroupMemberRoles -v` passes, 2/2 PASS, no SKIP
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && TEAM4S_PHASE145_TEST_DSN=$TEAM4S_PHASE145_TEST_DSN go test ./internal/repository/... -run TestHistGroupMemberRoles -v</automated>
  </verify>
  <done>Both functions prove RoleCodeExistsForContext's parameterization and neutral-false behavior via real Postgres calls; the sanctioned absence loop is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remediate member_claims_repository_claim_activation_test.go's presence assertions</name>
  <files>backend/internal/repository/member_claims_repository_claim_activation_test.go</files>
  <read_first>backend/internal/repository/member_claims_repository_claim_activation_test.go</read_first>
  <behavior>
    - `TestResolvePendingRolesToActive_ExistsOnRepository`: NO CHANGE (reflect-based, not the
      forbidden pattern).
    - `TestResolvePendingRolesToActiveUsesCanonicalCatalog`: keep the absence check
      (`IsGroupHistoryWhitelistRole`/`IsKnownFansubGroupRole` must not appear) unchanged. Replace
      the 3 presence assertions with a real Postgres proof: seed a `role_definitions` row that IS
      assignable with `fansub_group` context, and a second row that is either not assignable or
      lacks the `fansub_group` context; seed a pending `hist_group_member_roles`/equivalent input
      row referencing each; call `ResolvePendingRolesToActive(ctx, memberID, fansubGroupID,
      actorID)` and assert only the catalog-eligible role actually activates (verify via the
      resulting active-role state, e.g. a follow-up read), proving the JOIN/assignable/context
      filter behaviorally instead of by SQL-text grep.
    - `TestVerifyClaimActivatesRoles_*` (3 skipped functions): NO CHANGE — out of this plan's
      scope, unrelated pre-existing debt from an earlier phase's Plan 04.
  </behavior>
  <action>
  Implement the rewrite described above. If seeding the exact pending-role input shape requires
  reading additional repository methods beyond what's cited here, read
  `member_claims_role_activation_repository.go` fully first to get the real input/output types —
  do not guess field names.
  </action>
  <acceptance_criteria>
    - `grep -A20 "func TestResolvePendingRolesToActiveUsesCanonicalCatalog" backend/internal/repository/member_claims_repository_claim_activation_test.go | grep -c "strings.Contains(source, \"JOIN\|strings.Contains(source, \"'fansub_group'\|strings.Contains(source, \"rd.assignable\""` returns 0
    - `TEAM4S_PHASE145_TEST_DSN=<local dsn> go test ./backend/internal/repository/... -run TestResolvePendingRolesToActiveUsesCanonicalCatalog -v` passes, no SKIP
    - `go test ./backend/internal/repository/... -run "TestVerifyClaimActivatesRoles" -v` still shows 3 SKIP (unchanged, out of scope)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && TEAM4S_PHASE145_TEST_DSN=$TEAM4S_PHASE145_TEST_DSN go test ./internal/repository/... -run TestResolvePendingRolesToActive -v</automated>
  </verify>
  <done>The catalog-guard claim is proven by a real ResolvePendingRolesToActive call against seeded Postgres rows; the sanctioned absence check and the unrelated skipped tests are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test suite -> false-assurance risk | Repository-layer test files claiming role-catalog-guard behavior without ever executing the guarded query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-10 | Repudiation (false assurance) | `hist_group_member_roles_whitelist_test.go`, `member_claims_repository_claim_activation_test.go` | mitigate | Presence-based source assertions replaced with real Postgres calls exercising the actual catalog-guard SQL |
</threat_model>

<verification>
- `cd backend && go build ./...`
- `TEAM4S_PHASE145_TEST_DSN=<local dsn> go test ./internal/repository/... -run "TestHistGroupMemberRoles|TestResolvePendingRolesToActive" -v`
</verification>

<success_criteria>
Two more of the 20 locked security-relevant files prove their behavior via real Postgres calls,
not source inspection; their legitimate absence checks are preserved.
</success_criteria>

<output>
Create `.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-05-SUMMARY.md` when done
</output>
