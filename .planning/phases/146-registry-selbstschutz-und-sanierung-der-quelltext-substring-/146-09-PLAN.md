---
phase: 146
plan: 09
type: execute
wave: 3
depends_on: ["146-01", "146-02", "146-03"]
files_modified:
  - backend/internal/handlers/dashboard_me_handler_test.go
autonomous: true
requirements: ["Criterion 5", "Criterion 6"]

must_haves:
  truths:
    - "dashboard_me_handler_test.go proves the shared ownership-gate/IDOR-resistance and graceful-empty-state claims via real handler calls, not by grepping dashboard_me_handler.go/contributions_me_handler.go/me_identity_helpers.go (D-12)"
  artifacts:
    - path: "backend/internal/handlers/dashboard_me_handler_test.go"
      provides: "real httptest + real-Postgres proof that member_id is resolved only via resolveVerifiedMemberIDForAppUser, that attacker-supplied member_id inputs are ignored, and that the no-verified-claim path returns 200 with the empty state"
  key_links:
    - from: "backend/internal/handlers/dashboard_me_handler_test.go"
      to: "resolveVerifiedMemberIDForAppUser"
      via: "real Postgres call proving claim_status='verified' scoping"
      pattern: "resolveVerifiedMemberIDForAppUser"
---

<objective>
Remediate one more of Phase 146's 20 locked security-relevant Block-2 files:
`dashboard_me_handler_test.go`.

Purpose: closes Criteria 5/6 for this file. Four of its ten test functions today prove the D-08
IDOR-resistance regression (member_id resolved only from the verified identity, never from
query/param/body) and the D-09 graceful-empty-state regression by reading
`dashboard_me_handler.go`/`contributions_me_handler.go`/`me_identity_helpers.go` and grepping for
literal call/SQL fragments — exactly the kind of security-relevant claim CLAUDE.md's Teststil rule
requires a real, executed proof for.
Output: the 4 presence-based functions rewritten as real handler/Postgres calls; the 2 already-
compliant functions (`TestGetOwnDashboardRequiresAuth`, `TestEmptyOwnDashboardDataMatchesD09Contract`)
untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-CONTEXT.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-04-PLAN.md
</context>

<interfaces>
Current state (re-read 2026-09-04, `backend/internal/handlers/dashboard_me_handler_test.go`):

`ownDashboardLoader` interface (`dashboard_me_handler.go:20-21`):
`GetOwnDashboard(ctx context.Context, memberID int64) (*repository.OwnDashboardData, error)`.
`stubDashboardLoader` (this test file, lines 41-46) already implements it, currently used only for
the already-compliant `TestGetOwnDashboardRequiresAuth`
(`h := NewDashboardMeHandler(&stubDashboardLoader{}, nil)` — note: `db` is passed as `nil`).

`TestDashboardMeHandlerUsesSharedOwnershipGateHelper` (D-08 regression, presence+absence mixed):
claims `resolveVerifiedMemberIDForAppUser(c.Request.Context(), h.db, identity.AppUserID)` is the
sole memberID source and that `c.Query("member_id")`/`c.Param("member_id")`/
`c.PostForm("member_id")`/`ShouldBindJSON` are never used.

`TestDashboardMeHandlerGracefulEmptyStateInsteadOf403` (D-09 regression, presence+absence mixed):
claims `respondMemberProfileRequired(c)` is never called and `emptyOwnDashboardData()` +
`http.StatusOK` are used for the no-verified-claim path.

`TestContributionsMeHandlerDelegatesToSharedOwnershipGateHelper`: claims
`ContributionsMeHandler.resolveVerifiedMemberID` delegates via
`return resolveVerifiedMemberIDForAppUser(ctx, h.db, appUserID)`.

`TestMeIdentityHelpersDefinesSharedOwnershipGate`: claims
`resolveVerifiedMemberIDForAppUser(ctx context.Context, db *pgxpool.Pool, appUserID int64) (int64,
error)` is defined in `me_identity_helpers.go` and queries `claim_status = 'verified'`.

`resolveVerifiedMemberIDForAppUser` takes a real `*pgxpool.Pool`, not an interface — it cannot be
faked without a real database connection. This is exactly why the original tests used source
inspection. The remediation for these 3 functions therefore requires a REAL Postgres connection
(reuse whatever `testsupport.OpenXxxPostgres` fixture in this package/repo already provisions the
`members`/`member_claims` schema — grep `testsupport.Open` usage across `internal/handlers/*_test.go`
and `internal/repository/*_test.go` first to find the correct one; do not invent a new fixture if
an existing one already covers this schema, per RESEARCH.md's Don't-Hand-Roll guidance).
`TestGetOwnDashboardRequiresAuth` and `TestEmptyOwnDashboardDataMatchesD09Contract` do not use
`os.ReadFile` — leave both untouched.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remediate the D-08 IDOR-resistance and D-09 empty-state claims via real handler calls</name>
  <files>backend/internal/handlers/dashboard_me_handler_test.go</files>
  <read_first>backend/internal/handlers/dashboard_me_handler_test.go, backend/internal/handlers/dashboard_me_handler.go</read_first>
  <behavior>
    - `TestDashboardMeHandlerUsesSharedOwnershipGateHelper`: using a real Postgres connection,
      seed a verified `member_claims` row for a given `AppUserID` resolving to member X. Build a
      real `NewDashboardMeHandler(&stubDashboardLoader{...capturing the memberID argument
      GetOwnDashboard receives...}, realPool)`. Fire a real GET request carrying the authenticated
      identity for that `AppUserID` PLUS an attacker-supplied `?member_id=999999` query param (a
      different, unrelated member). Assert the stub's captured `memberID` argument equals member
      X's real resolved ID, never `999999` — proving IDOR resistance by execution, not grep.
    - `TestDashboardMeHandlerGracefulEmptyStateInsteadOf403`: same real-Postgres setup, but for an
      `AppUserID` with NO verified `member_claims` row. Fire a real GET request. Assert
      `recorder.Code == http.StatusOK` and the decoded JSON body's `has_member_profile == false`
      (matching `emptyOwnDashboardData()`'s shape) — proving the graceful-empty-state path by
      execution, never asserting `respondMemberProfileRequired` is absent from source (the
      behavioral 200-vs-403 outcome already proves that structurally).
    - `TestContributionsMeHandlerDelegatesToSharedOwnershipGateHelper`: real Postgres call —
      seed the same verified-claim row, call `ContributionsMeHandler`'s real request path for that
      `AppUserID`, and separately call `resolveVerifiedMemberIDForAppUser(ctx, pool, appUserID)`
      directly; assert both resolve to the identical memberID — proving delegation behaviorally
      (same resolved value via two call paths) rather than grepping for the delegating call's
      literal source text.
    - `TestMeIdentityHelpersDefinesSharedOwnershipGate`: real Postgres call — seed one
      `member_claims` row with `claim_status = 'pending'` and one with `claim_status = 'verified'`
      for two different `AppUserID`s; call `resolveVerifiedMemberIDForAppUser` for both; assert the
      pending one returns a not-found-style error/zero result and the verified one resolves to the
      correct memberID — proving the `claim_status = 'verified'` scoping by execution.
  </behavior>
  <action>
  Implement all four rewrites. First grep `internal/handlers/*_test.go` and
  `internal/repository/*_test.go` for existing `testsupport.Open*Postgres` fixture usage to find
  one that already provisions `members`/`member_claims` (reuse it, do not invent a new DSN-gated
  fixture). Read `dashboard_me_handler.go`, `contributions_me_handler.go`, and
  `me_identity_helpers.go` fully first for the exact types/signatures before writing the seeds and
  assertions.
  </action>
  <acceptance_criteria>
    - `grep -c "os.ReadFile" backend/internal/handlers/dashboard_me_handler_test.go` returns 0
    - `go test ./backend/internal/handlers/... -run "TestDashboardMeHandlerUsesSharedOwnershipGateHelper|TestDashboardMeHandlerGracefulEmptyStateInsteadOf403|TestContributionsMeHandlerDelegatesToSharedOwnershipGateHelper|TestMeIdentityHelpersDefinesSharedOwnershipGate" -v` passes, 4/4 PASS, no SKIP (with whichever DSN env var the reused fixture requires set locally)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/handlers/... -run TestDashboardMeHandler -v && go test ./internal/handlers/... -run TestGetOwnDashboardRequiresAuth -v</automated>
  </verify>
  <done>All 4 previously source-inspection-based functions now prove IDOR resistance and graceful-empty-state behavior via real handler/Postgres calls; the 2 already-compliant functions are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Authenticated member browser -> GET /me/dashboard, /me/contributions | Own-data-only endpoints; attacker-supplied member_id in query/param/body must never override the identity-resolved member |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-15 | Repudiation (false assurance) — the underlying IDOR-resistance guard itself is unchanged production code, already correct; only the TEST proving it was non-executing | `dashboard_me_handler_test.go` | mitigate | Real Postgres + httptest calls now prove attacker-supplied member_id query/param/body values are ignored and the authenticated identity's verified member is always used, by executing the resolution path with a deliberately conflicting query param |
</threat_model>

<verification>
- `cd backend && go build ./...`
- `go test ./internal/handlers/... -run TestDashboardMeHandler -v`
- `go test ./internal/handlers/... -v` (full package regression)
</verification>

<success_criteria>
One more of the 20 locked security-relevant files proves its IDOR-resistance and empty-state
claims via real handler/Postgres calls, not source inspection.
</success_criteria>

<output>
Create `.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-09-SUMMARY.md` when done
</output>
