---
phase: 146
plan: 03
type: execute
wave: 2
depends_on: ["146-01"]
files_modified:
  - backend/internal/handlers/admin_capability_handler.go
  - backend/internal/handlers/admin_capability_handler_test.go
autonomous: true
requirements: ["Criterion 1"]

must_haves:
  truths:
    - "Revoking one of the 3 baseline actions from group_member is rejected server-side (409) unconditionally, even when CountRolesWithAction reports 16+ other roles also grant the action — the existing per-role lockout guard is unchanged for every other role (D-02, D-03)"
    - "Granting a non-baseline action to group_member is rejected server-side (409) with a speaking German message, so the reserved role's registry state stays structurally limited to its 3 intended actions even if the frontend badge/filter is ever bypassed (D-16)"
    - "This plan closes Block 1 (Kriterien 1-4) before any Block 2 remediation plan begins, per D-01's required block ordering"
    - "The fix is a registry-consistency guard, not an access-control change — the platform-admin bypass performing the mutation is intentional and out of scope (D-13, D-14)"
  artifacts:
    - path: "backend/internal/handlers/admin_capability_handler.go"
      provides: "unconditional membership-baseline revoke guard + action-specific grant guard, deliberately not touching LoadCapabilityRoles per the D-17 trap"
    - path: "backend/internal/handlers/admin_capability_handler_test.go"
      provides: "httptest+fake-repo proof for both guards, real status code + response body assertions"
  key_links:
    - from: "backend/internal/handlers/admin_capability_handler.go"
      to: "permissions.MembershipBaselineActionCodes"
      via: "slices.Contains guard check in both RevokeCapability and GrantCapability"
      pattern: "permissions.RoleMembershipBaseline"
---

<objective>
Close ROADMAP.md Success Criterion 1: "Das Entfernen einer der drei Pflicht-Actions von der
reservierten Pseudo-Rolle wird im Mutationspfad serverseitig abgelehnt — nicht nur in der
Oberfläche — und ein Test spielt genau diesen Pfad durch, statt die Ablehnung aus dem Quelltext zu
erschließen. Der bestehende Lockout-Guard bleibt für alle anderen Rollen unverändert."

Plus the D-16 extension (post-research user decision, binding): the server also rejects GRANTING a
non-baseline action to `group_member`, not only revoking a baseline one — closing the adjacent gap
RESEARCH.md found (today nothing stops a platform admin from toggling e.g.
`release_version_media.delete` ON for the reserved role via the same admin UI).

Purpose: today `CountRolesWithAction` counts ALL roles granting an action (`145-REVIEW.md` CR-01)
— roughly 15 other roles also carry the 3 baseline actions, so the existing lockout guard never
fires for `group_member`, and the next backend start crash-loops in `LoadCache` /
`validateMembershipBaselineRegistryPresence` -> `log.Fatalf`. This is an operational-safety fix
(CONTEXT.md D-13/D-14 framing) — the platform-admin actor performing the edit is intentionally
unrestricted (Phase 136 Criterion 1); the fix is a registry-consistency guard, not an
access-control change.
Output: two new unconditional, action-specific mutation guards; two new httptest-based tests
proving both, per CLAUDE.md's Teststil convention (real call, real status code + body, no source
inspection).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-CONTEXT.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-RESEARCH.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-UI-SPEC.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-PATTERNS.md
</context>

<interfaces>
Current state (re-read 2026-09-04, `backend/internal/handlers/admin_capability_handler.go`):

`GrantCapability` (starting line 154): after the `roleCode`/`actionCode` empty check (line 163-166)
and the `IsCapabilityBearingRole` 422 block (lines 170-178), calls
`h.mutationRepo.GrantRoleCapability(c.Request.Context(), roleCode, actionCode)` at line 180.

`RevokeCapability` (starting line 219): after the same empty check and `IsCapabilityBearingRole`
422 block (lines 235-243), runs the existing D-07 lockout guard (lines 245-262:
`h.mutationRepo.CountRolesWithAction` then `if count <= 1 && !permissions.IsStandaloneAction(...)`
-> 409 `lockout_guard`), then calls `h.mutationRepo.RevokeRoleCapability` at line 264.

Both handlers use `permissions.IsCapabilityBearingRole` (package `permissions`, already imported)
and both already build `gin.H{"error": gin.H{"code": ..., "message": ...}}` JSON error bodies —
mirror this exact shape for the two new guards.

`permissions.MembershipBaselineActionCodes` (`[]permissions.Action`) and
`permissions.RoleMembershipBaseline` (`const = "group_member"`) exist per Plan 146-01 (this plan
depends on it) — the two new guards consume both.

`admin_capability_handler_test.go`: `stubCapabilityAuthzRepo` (fields include
`countRolesWithAction int64`), `stubCapabilityPermissionSvc`, `captureAuditLogRepo`,
`makeCapabilityTestContext(method, path, identity)` — all reusable unchanged. See
`TestRevokeCapabilityLastActionGuard` (lines 148-195) for the exact test-body shape to mirror.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the unconditional membership-baseline revoke guard (Criterion 1)</name>
  <files>backend/internal/handlers/admin_capability_handler.go</files>
  <read_first>backend/internal/handlers/admin_capability_handler.go</read_first>
  <behavior>
    - `RevokeCapability` called with `roleCode == "group_member"` and `actionCode` equal to one of
      `permissions.MembershipBaselineActionCodes` must return 409 with `error.code ==
      "membership_baseline_guard"` REGARDLESS of what `h.mutationRepo.CountRolesWithAction` would
      return (i.e. even when stubbed at 16, well above the lockout threshold of 1) — the guard
      fires BEFORE `CountRolesWithAction` is called.
    - `RevokeCapability` called for any other `roleCode`, or for `group_member` with a non-baseline
      `actionCode`, is unaffected — falls through unchanged to the existing D-07 lockout-guard
      block.
    - `h.mutationRepo.RevokeRoleCapability` must NOT be called when the new guard fires (no DB
      mutation, no audit-log write).
  </behavior>
  <action>
  In `RevokeCapability`, insert a new block immediately after the `IsCapabilityBearingRole` check
  (after line 243's closing brace) and before the existing `// D-07: Lockout-Guard` comment
  (line 245): if `roleCode == permissions.RoleMembershipBaseline` AND
  `slices.Contains(permissions.MembershipBaselineActionCodes, permissions.Action(actionCode))`,
  respond `c.JSON(http.StatusConflict, gin.H{"error": gin.H{"code": "membership_baseline_guard",
  "message": "Dieses Recht gehört zur Mitgliedschafts-Grundausstattung und kann nicht entzogen
  werden. Jedes aktive Mitglied benötigt es automatisch — die Änderung wurde nicht gespeichert."}})`
  and `return` — this exact message string is the locked Copywriting Contract from
  `146-UI-SPEC.md`, copy verbatim, do not paraphrase. Add `"slices"` to the file's import block if
  not already present. Do NOT modify `CountRolesWithAction`'s call, its SQL, or the existing
  lockout-guard block's logic — per D-02's "Bestandsschutz für alle anderen Rollen" requirement,
  add a second, independent, unconditional check, do not repurpose the existing one.
  </action>
  <acceptance_criteria>
    - `grep -n "membership_baseline_guard" backend/internal/handlers/admin_capability_handler.go` finds a match inside `RevokeCapability`, positioned before the `CountRolesWithAction` call in the same function
    - `cd backend && go build ./...` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./internal/handlers/...</automated>
  </verify>
  <done>RevokeCapability unconditionally rejects removing a baseline action from group_member, independent of CountRolesWithAction's result; every other role's revoke path is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the action-specific grant guard (D-16)</name>
  <files>backend/internal/handlers/admin_capability_handler.go</files>
  <read_first>backend/internal/handlers/admin_capability_handler.go</read_first>
  <behavior>
    - `GrantCapability` called with `roleCode == "group_member"` and `actionCode` NOT in
      `permissions.MembershipBaselineActionCodes` must return 409 with `error.code ==
      "membership_baseline_guard"`.
    - `GrantCapability` called with `roleCode == "group_member"` and `actionCode` IN
      `permissions.MembershipBaselineActionCodes` is unaffected (falls through to
      `GrantRoleCapability` unchanged — this is the only way the 3 baseline actions can be
      re-granted after a legitimate revoke-then-fix cycle).
    - `GrantCapability` for any other `roleCode` is completely unaffected.
    - Do NOT add `AND NOT reserved` (or any filter) to `LoadCapabilityRoles`
      (`backend/internal/repository/authz_permissions.go`) — that query feeds
      `capabilityRoleCatalog`/`IsCapabilityBearingRole`, shared by both Grant and Revoke; filtering
      it there would make `group_member` fail `IsCapabilityBearingRole` entirely and return the
      generic, wrong 422 ("Diese Beitrags- oder historische Rolle kann keine Standardrechte
      erhalten.") for BOTH directions — breaking Criterion 2's requirement that the 3 baseline rows
      stay interactive (D-17's documented trap). The new guard must be action-specific, added only
      inside `GrantCapability`.
  </behavior>
  <action>
  In `GrantCapability`, insert a new block immediately after the `IsCapabilityBearingRole` check
  (after line 178's closing brace) and before `h.mutationRepo.GrantRoleCapability` (line 180): if
  `roleCode == permissions.RoleMembershipBaseline` AND
  `!slices.Contains(permissions.MembershipBaselineActionCodes, permissions.Action(actionCode))`,
  respond `c.JSON(http.StatusConflict, gin.H{"error": gin.H{"code": "membership_baseline_guard",
  "message": "<German rejection message for the grant direction — Claude's Discretion per D-19's
  scope note, not locked by 146-UI-SPEC.md; state that the reserved role is limited to exactly the
  3 baseline rights and the change was not saved, with correct Umlaute>"}})` and `return`. Reuse the
  same `error.code` ("membership_baseline_guard") as Task 1's revoke guard so
  `RoleCapabilityImpactPreviewModal.tsx`'s existing generic `mutationError` rendering handles both
  directions with zero frontend code change — only the `message` text differs.
  </action>
  <acceptance_criteria>
    - `grep -c "membership_baseline_guard" backend/internal/handlers/admin_capability_handler.go` returns exactly 2 (one in RevokeCapability, one in GrantCapability)
    - `grep -B2 "membership_baseline_guard" backend/internal/handlers/admin_capability_handler.go | grep -c "!slices.Contains"` returns 1 (confirms the grant guard's inverted condition)
    - `cd backend && go build ./...` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./...</automated>
  </verify>
  <done>GrantCapability rejects assigning any non-baseline action to group_member; the 3 baseline actions remain grantable; LoadCapabilityRoles/IsCapabilityBearingRole are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove both guards via httptest + fake repository</name>
  <files>backend/internal/handlers/admin_capability_handler_test.go</files>
  <read_first>backend/internal/handlers/admin_capability_handler_test.go</read_first>
  <behavior>
    - Test A (`TestRevokeCapabilityMembershipBaselineGuard...`): stub `countRolesWithAction: 16`
      (well above the lockout threshold), `roleCode: "group_member"`,
      `actionCode: "fansub_group_media.upload"` (one of the 3 baseline codes). Call
      `h.RevokeCapability(c)`. Assert `rec.Code == http.StatusConflict` and the parsed JSON body's
      `error.code == "membership_baseline_guard"` (NOT `"lockout_guard"` — proves the new guard
      fires independent of the lockout count). Assert zero audit entries.
    - Test B (`TestGrantCapabilityMembershipBaselineGuard...`): `roleCode: "group_member"`,
      `actionCode` set to a non-baseline action code (e.g. `"release_version_media.delete"`). Call
      `h.GrantCapability(c)`. Assert `rec.Code == http.StatusConflict` and `error.code ==
      "membership_baseline_guard"`. Assert zero audit entries.
    - Test C (`TestGrantCapabilityMembershipBaselineAllowsBaselineAction...`): same as B but
      `actionCode` set to one of the 3 baseline codes; assert `rec.Code == http.StatusOK` (proves
      the grant guard does not block re-granting a baseline action).
  </behavior>
  <action>
  Add all three test functions to `admin_capability_handler_test.go`, mirroring
  `TestRevokeCapabilityLastActionGuard`'s (lines 148-195) exact structure: build the identity via
  `makeCapabilityTestContext`, set `gin.Params`, construct `stubCapabilityAuthzRepo` +
  `stubCapabilityPermissionSvc` + `captureAuditLogRepo`, call `NewAdminCapabilityHandler(...)`,
  invoke the handler method directly, assert `rec.Code` and unmarshal `rec.Body` into the same
  anonymous `{Error struct{Code string}}` shape already used in this file. No new stub types or
  mock framework needed.
  </action>
  <acceptance_criteria>
    - `cd backend && go test ./internal/handlers/... -run "TestRevokeCapabilityMembershipBaselineGuard|TestGrantCapabilityMembershipBaselineGuard|TestGrantCapabilityMembershipBaselineAllowsBaselineAction" -v` passes, 3/3 PASS
    - `grep -c "func Test.*MembershipBaseline" backend/internal/handlers/admin_capability_handler_test.go` returns 3
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/handlers/... -run MembershipBaseline -v</automated>
  </verify>
  <done>Both new guards are proven via real handler calls with status code + response body assertions, not source inspection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin browser -> role-capability mutation endpoints | Platform-admin-authenticated PUT/DELETE requests mutating role_capabilities rows, which feed effective-rights resolution for every active fansub-group member platform-wide |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-05 | Tampering (registry-consistency, not access-control — CONTEXT.md D-13/D-14; the platform-admin bypass performing the mutation is intentional and out of scope, Phase 136 Criterion 1) | `AdminCapabilityHandler.RevokeCapability` | mitigate | Unconditional membership-baseline guard rejects revoking any of the 3 baseline actions from `group_member`, independent of `CountRolesWithAction`, moving the defense to mutation time instead of only the startup fail-closed check |
| T-146-06 | Tampering (registry-consistency) | `AdminCapabilityHandler.GrantCapability` | mitigate | Action-specific guard rejects granting any non-baseline action to `group_member`, keeping the reserved role's registry state structurally limited to its 3 intended actions |
| T-146-07 | Denial of Service (crash-loop) | `cmd/server/main.go` startup `LoadCache` path | mitigate | Both new guards prevent the DB state that previously required only 2 admin-UI clicks to reach and that crashed the next backend start via `validateMembershipBaselineRegistryPresence` -> `log.Fatalf` |
</threat_model>

<verification>
- `cd backend && go build ./... && go vet ./...`
- `cd backend && go test ./internal/handlers/... -run MembershipBaseline -v`
- `cd backend && go test ./internal/handlers/... -v` (full package regression, confirms the existing lockout-guard/assignable-guard tests still pass unchanged)
</verification>

<success_criteria>
Criterion 1 (ROADMAP.md, quoted in Objective above) is true: revoking a baseline action from the
reserved role is server-side-rejected unconditionally, proven by a real mutation-path test; the
existing lockout guard is unchanged for all other roles. D-16's grant-direction extension is also
closed.
</success_criteria>

<output>
Create `.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-03-SUMMARY.md` when done
</output>
