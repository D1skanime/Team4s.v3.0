---
phase: 146
plan: 04
type: execute
wave: 3
depends_on: ["146-01", "146-02", "146-03"]
files_modified:
  - backend/internal/testquality/security_relevant_test_files.go
  - backend/internal/handlers/role_catalog_router_integration_test.go
  - backend/internal/repository/role_catalog_repository_test.go
  - backend/internal/repository/role_definitions_context_test.go
autonomous: true
requirements: ["Criterion 5", "Criterion 6", "Criterion 7"]

must_haves:
  truths:
    - "The 'security-relevant' file set for Criteria 5/6/7 is a single, frozen, documented list — the same list this plan's Go var exposes is what every later Block-2 plan and the Criterion-7 guard (Plan 146-13) consume (D-08)"
    - "role_catalog_router_integration_test.go, role_catalog_repository_test.go, and role_definitions_context_test.go's TestRoleDefinitionsContextQueryIsGeneric no longer claim behavior via os.ReadFile+strings.Contains on production Go source, while their legitimate absence checks are preserved unchanged (D-10)"
    - "This filter rule is identical to the one Plan 146-13's Criterion-7 ratchet guard enforces (D-09)"
  artifacts:
    - path: "backend/internal/testquality/security_relevant_test_files.go"
      provides: "SecurityRelevantTestFiles []string — the frozen 20-file definition, with the filter-rule reasoning as a doc comment"
  key_links:
    - from: "backend/internal/testquality/security_relevant_test_files.go"
      to: "backend/internal/testquality/source_substring_guard_test.go (Plan 146-13)"
      via: "shared package-level var, consumed by the ratchet guard's empty-intersection assertion"
      pattern: "SecurityRelevantTestFiles"
---

<objective>
Start Block 2 (Testsanierung, Criteria 5-8) per D-01's required ordering (after Block 1 waves 1-2
complete) and D-08's requirement that the FIRST Block-2 task lock the filter rule defining
"security-relevant" test files. Then remediate the 3 smallest of the 20 locked files.

Purpose: ROADMAP.md's own re-measurement found 20 candidate security-relevant files (name+header
keyword filter), not the original 17 — this plan makes the definitional call RESEARCH.md's Open
Question 1 flags as needing an explicit, documented decision, and records the reasoning so
Criterion 6's "höchstens 36 von 53" arithmetic (53 - 20 = 33 ≤ 36) is verifiable and Criterion 7's
guard (Plan 146-13) enforces the identical rule (D-09).

**Locked filter-rule decision (this plan, resolving RESEARCH.md's Open Question 1):**
"Security-relevant" = the 20-file name+header-keyword match from
`.planning/notes/2026-09-04-messung-substring-tests.md`
(`permission|authz|capability|role_capabilit|preview|403|forbidden|effective_right|whitelist|
delegation|role_catalog|reserved`, case-insensitive, matched against the relative path or the
first 4KB of file content) — adopted because it is reproducible via the committed measurement
script, cross-checks the roadmap's own 53-file/302-function totals exactly, and is the freshest
re-verified figure available. Within these 20 files, a test FUNCTION is a "violation" subject to
Criterion 5 remediation only if it uses `os.ReadFile` on a `.go` file to assert PRESENCE of a
pattern (via `strings.Contains`/`assert.Contains`/`require.Contains` without negation) as a
stand-in for behavior. Pure ABSENCE checks (`NotContains`, `assert.False(strings.Contains(...))`)
and files that are themselves the object under test (SQL migrations) remain compliant per
CLAUDE.md's own stated exceptions (D-10) — they require no code change, and do not count as
"reading .go source" for Criterion 6's count once verified to contain no PRESENCE-style
os.ReadFile assertion. This is the identical rule Plan 146-13's Criterion-7 guard enforces (D-09).
Output: the canonical `SecurityRelevantTestFiles` list (frozen input for every later Block-2 plan
and the Criterion-7 guard); 3 of the 20 files remediated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-CONTEXT.md
@.planning/notes/2026-09-04-messung-substring-tests.md
@.planning/notes/measure-substring-tests.py
</context>

<interfaces>
Current state (re-read 2026-09-04):

`backend/internal/handlers/role_catalog_router_integration_test.go` (full file, 18 lines):
`TestRoleDefinitionsRouterIsRegisteredExactlyOnceWithoutAuthMiddleware` reads
`../../cmd/server/main.go` and asserts (a) `strings.Count(text, "RegisterPublicRoleCatalogRoute(v1, roleCatalogHandler)") == 1`
and (b) the registration string is never immediately followed by `authMiddleware`. A REAL,
already-established replacement pattern exists in the same package:
`backend/internal/handlers/role_catalog_handler_test.go` lines 35-36 already call
`RegisterPublicRoleCatalogRoute(router.Group("/api/v1"), NewRoleCatalogHandler(repo))` on a real
`gin.New()` router and fire a real `httptest.NewRequest`.

`backend/internal/repository/role_catalog_repository_test.go` (full file, 29 lines):
`TestRoleCatalogRepositoryUsesBoundedPresentationProjection` reads `role_catalog_repository.go`
and asserts 9 SQL-column-fragment presence checks plus a 3-item forbidden-table absence loop. The
real production method is `RoleCatalogRepository.ListPublicRoleDefinitions(ctx, contextName)`
(`backend/internal/repository/role_catalog_repository.go`), returning `[]PublicRoleDefinition`
with fields `Code, LabelDE, Contexts, SortOrder, Assignable, ColorKey, IconKey,
OperativeCapabilityCount, HasOperativeCapabilities`. `testsupport.OpenPhase145Postgres(t)` already
builds the `role_definitions`/`role_capabilities` schema this method reads.

`backend/internal/repository/role_definitions_context_test.go` (full file, 45 lines): TWO
functions. `TestRoleDefinitionsContextKaraokeFXFollowsSeededContexts` reads a MIGRATION file
(`database/migrations/0146_capability_policy_catalog.up.sql`) — this is itself the object under
test (CLAUDE.md exception 2, SQL migrations) — leave this function completely unchanged, it is
already compliant. `TestRoleDefinitionsContextQueryIsGeneric` reads
`hist_group_member_roles_repository.go` and asserts the SQL fragment
`"WHERE code = $1 AND $2 = ANY(contexts)"` is present — this IS a violation (production Go source,
presence assertion). The real production method exercising this exact SQL is
`HistGroupMemberRolesRepository.RoleCodeExistsForContext(ctx, roleCode, contextName)`
(`hist_group_member_roles_repository.go:320`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create the frozen security-relevant file list (D-08 lock)</name>
  <files>backend/internal/testquality/security_relevant_test_files.go</files>
  <read_first>.planning/notes/2026-09-04-messung-substring-tests.md</read_first>
  <action>
  Create new package `testquality` at `backend/internal/testquality/security_relevant_test_files.go`.
  Declare `var SecurityRelevantTestFiles = []string{...}` containing exactly these 20 relative
  paths (relative to `backend/`, matching `measure-substring-tests.py`'s own `ROOT`-relative
  format):
  `internal/repository/release_version_media_repository_test.go`,
  `internal/handlers/admin_content_release_version_media_test.go`,
  `internal/handlers/admin_content_fansub_notes_test.go`,
  `internal/handlers/admin_content_release_theme_assets_test.go`,
  `internal/handlers/admin_content_release_version_media_replace_test.go`,
  `internal/repository/hist_group_member_roles_whitelist_test.go`,
  `internal/repository/member_archive_repository_test.go`,
  `internal/handlers/fansub_test.go`,
  `internal/repository/member_claims_repository_claim_activation_test.go`,
  `internal/repository/role_definitions_context_test.go`,
  `internal/handlers/role_catalog_router_integration_test.go`,
  `internal/repository/point_ledger_repository_test.go`,
  `internal/repository/role_catalog_repository_test.go`,
  `internal/services/release_crew_service_test.go`,
  `internal/handlers/admin_content_anime_project_notes_test.go`,
  `internal/handlers/dashboard_me_handler_test.go`,
  `internal/handlers/public_member_access_matrix_test.go`,
  `internal/repository/member_point_totals_repository_test.go`,
  `internal/repository/review_delegation_repository_test.go`,
  `internal/services/release_review_submission_test.go`.
  Add a doc comment above the var stating: this is the FROZEN definition of "security-relevant"
  for Phase 146 Criteria 5/6/7 (D-08), measured 2026-09-04 via
  `.planning/notes/measure-substring-tests.py`'s name+header-keyword filter; this list itself does
  not shrink or grow (it defines phase scope, not remediation progress) — what shrinks is the
  separate ratchet exception list in Plan 146-13's guard file. State the presence-vs-absence
  violation rule from this plan's Objective verbatim in the comment so future readers do not need
  to re-derive it.
  </action>
  <acceptance_criteria>
    - `cd backend && go build ./internal/testquality/...` succeeds
    - `grep -c '"internal/' backend/internal/testquality/security_relevant_test_files.go` returns exactly 20
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./internal/testquality/...</automated>
  </verify>
  <done>SecurityRelevantTestFiles exists as the single, documented, frozen 20-file definition every later plan and the Criterion-7 guard consume.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remediate role_catalog_router_integration_test.go and role_catalog_repository_test.go</name>
  <files>backend/internal/handlers/role_catalog_router_integration_test.go, backend/internal/repository/role_catalog_repository_test.go</files>
  <read_first>backend/internal/handlers/role_catalog_router_integration_test.go, backend/internal/repository/role_catalog_repository_test.go, backend/internal/handlers/role_catalog_handler_test.go</read_first>
  <behavior>
    - `role_catalog_router_integration_test.go`: rewrite
      `TestRoleDefinitionsRouterIsRegisteredExactlyOnceWithoutAuthMiddleware` to (a) build a real
      `gin.New()` router group, call `RegisterPublicRoleCatalogRoute` on it exactly once with a
      real or fake `publicRoleCatalogRepository`, fire a real `httptest.NewRequest(http.MethodGet,
      "/api/v1/role-definitions", nil)` WITHOUT an `Authorization` header, and assert the response
      is NOT 401/403 (proving the route is genuinely public, not source-inspected); (b) separately
      assert that calling `RegisterPublicRoleCatalogRoute` twice on the SAME router group panics
      (Gin's own duplicate-route panic) — a real, executable proof of "exactly once" instead of a
      `strings.Count` on main.go.
    - `role_catalog_repository_test.go`: replace the 9-fragment presence loop in
      `TestRoleCatalogRepositoryUsesBoundedPresentationProjection` with a real Postgres call: seed
      `role_definitions`/`role_capabilities` rows via `testsupport.OpenPhase145Postgres(t)`, call
      `NewRoleCatalogRepository(pool).ListPublicRoleDefinitions(ctx, "fansub_group")`, and assert
      the returned `[]PublicRoleDefinition` contains the expected seeded row(s) with correct
      `Code`/`LabelDE`/`Contexts`/`SortOrder`/`Assignable`/`ColorKey`/`IconKey`/
      `OperativeCapabilityCount` values — proving the projection is bounded and correct by
      actually calling it. Keep the existing forbidden-table absence loop (3 items:
      `user_group_capability_overrides`, `user_group_capability_override_history`,
      `app_user_global_roles`) unchanged — legitimate absence check per CLAUDE.md exception 1.
  </behavior>
  <action>
  Implement both rewrites exactly as described in `<behavior>`. For the router test, model the
  request/router construction on the already-existing real pattern in
  `role_catalog_handler_test.go` (same package, lines 35-36) rather than inventing a new one.
  </action>
  <acceptance_criteria>
    - `grep -c "os.ReadFile" backend/internal/handlers/role_catalog_router_integration_test.go` returns 0
    - `grep -n "os.ReadFile" backend/internal/repository/role_catalog_repository_test.go` shows only the sanctioned absence-loop usage remaining (`grep -A15 "os.ReadFile" backend/internal/repository/role_catalog_repository_test.go` confirms only the `forbidden` loop's `strings.Contains`, no bare presence assertions)
    - `cd backend && go test ./internal/handlers/... -run TestRoleDefinitionsRouterIsRegisteredExactlyOnceWithoutAuthMiddleware -v` passes
    - `TEAM4S_PHASE145_TEST_DSN=<local dsn> go test ./backend/internal/repository/... -run TestRoleCatalogRepositoryUsesBoundedPresentationProjection -v` passes, no SKIP
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/handlers/... -run RoleDefinitionsRouter -v</automated>
  </verify>
  <done>Both files prove their claims via real router/DB calls; only the sanctioned absence check remains as source inspection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remediate role_definitions_context_test.go's presence violation</name>
  <files>backend/internal/repository/role_definitions_context_test.go</files>
  <read_first>backend/internal/repository/role_definitions_context_test.go, backend/internal/repository/hist_group_member_roles_repository.go</read_first>
  <behavior>
    - `TestRoleDefinitionsContextKaraokeFXFollowsSeededContexts`: NO CHANGE — it reads a SQL
      migration file, which is itself the object under test (CLAUDE.md exception 2). Leave
      byte-identical.
    - `TestRoleDefinitionsContextQueryIsGeneric`: replace the single presence assertion
      (`strings.Contains(source, "WHERE code = $1 AND $2 = ANY(contexts)")`) with a real Postgres
      call proving the same genericity claim behaviorally: seed `role_definitions` rows including
      at least `karaoke_fx` with context `group_history` plus one other arbitrary code/context
      pair, call `NewHistGroupMemberRolesRepository(pool).RoleCodeExistsForContext(ctx, code,
      contextName)` for both a valid (code, context) pair (expect `true, nil`) and an invalid one
      (expect `false, nil`) — proving the query is genuinely parameterized/generic by exercising it
      with more than one code, not by reading its SQL text. This does not expand Criterion 3's
      locked scope (no filter-rule change to `RoleCodeExistsForContext`) — it is a test-quality
      rewrite only.
  </behavior>
  <action>
  Implement exactly the behavior above. Reuse `testsupport.OpenPhase145Postgres(t)` and the
  `phase145MigrationPath` helper pattern already established in
  `membership_baseline_registry_test.go` if this package-local file needs its own copy (declare
  locally if not already available in this file — mirror the existing per-file helper convention).
  </action>
  <acceptance_criteria>
    - `grep -n "os.ReadFile" backend/internal/repository/role_definitions_context_test.go` shows exactly ONE remaining call (the migration-file read inside `TestRoleDefinitionsContextKaraokeFXFollowsSeededContexts`, sanctioned)
    - `TEAM4S_PHASE145_TEST_DSN=<local dsn> go test ./backend/internal/repository/... -run TestRoleDefinitionsContextQueryIsGeneric -v` passes, no SKIP
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && TEAM4S_PHASE145_TEST_DSN=$TEAM4S_PHASE145_TEST_DSN go test ./internal/repository/... -run RoleDefinitionsContext -v</automated>
  </verify>
  <done>role_definitions_context_test.go's presence violation is replaced with a real, parameterized Postgres proof; the sanctioned migration-file absence check is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test suite -> false-assurance risk | Backend test files claiming security-relevant behavior without ever executing the code path under test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-08 | Repudiation (false assurance) | `role_catalog_router_integration_test.go`, `role_catalog_repository_test.go`, `role_definitions_context_test.go` | mitigate | Presence-based source-substring assertions replaced with real router/httptest/Postgres calls that actually execute the claimed behavior |
| T-146-09 | Tampering (definitional drift) | `SecurityRelevantTestFiles` | accept | The list is a plain Go var, editable by any future commit — accepted because Plan 146-13's ratchet guard is the actual enforcement mechanism (Criterion 7); this list only documents scope, per D-08 |
</threat_model>

<verification>
- `cd backend && go build ./...`
- `TEAM4S_PHASE145_TEST_DSN=<local dsn> go test ./internal/handlers/... ./internal/repository/... -run "RoleDefinitionsRouter|RoleCatalogRepository|RoleDefinitionsContext" -v`
</verification>

<success_criteria>
The D-08 filter-rule lock is documented and consumable by later plans; 3 of the 20 locked
security-relevant files no longer claim behavior via source-substring inspection.
</success_criteria>

<output>
Create `.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-04-SUMMARY.md` when done
</output>
