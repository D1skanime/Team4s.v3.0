---
phase: 146
plan: 07
type: execute
wave: 3
depends_on: ["146-01", "146-02", "146-03"]
files_modified:
  - backend/internal/handlers/fansub_test.go
  - backend/internal/repository/point_ledger_repository_test.go
autonomous: true
requirements: ["Criterion 5", "Criterion 6"]

must_haves:
  truths:
    - "fansub_test.go proves the group-edit permission guard on alias create/delete via a real httptest call, not by grepping the handler's own source"
    - "point_ledger_repository_test.go's SQL-contract claim is proven by the file's own existing real-Postgres award tests, with the presence-based grep removed"
  artifacts:
    - path: "backend/internal/handlers/fansub_test.go"
      provides: "real httptest proof that CreateFansubAlias/DeleteFansubAlias enforce the group-edit permission"
    - path: "backend/internal/repository/point_ledger_repository_test.go"
      provides: "presence-assertion removal, relying on the file's existing real Postgres award/reversal tests for behavioral proof"
  key_links:
    - from: "backend/internal/handlers/fansub_test.go"
      to: "FansubHandler.CreateFansubAlias / DeleteFansubAlias"
      via: "real httptest.NewRequest call with a permission-actor context"
      pattern: "CreateFansubAlias"
---

<objective>
Remediate two more of Phase 146's 20 locked security-relevant Block-2 files: `fansub_test.go` and
`point_ledger_repository_test.go`.

Purpose: `fansub_test.go`'s `TestFansubAliasMutationsUseGroupEditPermission` mixes a legitimate
absence check with 5 presence assertions claiming a permission guard exists in source, never
calling the handler (Pitfall 5 shape). `point_ledger_repository_test.go`'s
`TestPointLedgerSQLContract` similarly mixes a legitimate absence loop (forbidden UPDATE/DELETE
SQL) with a presence loop claiming specific SQL fragments exist — but this same file ALREADY has
real Postgres award/reversal/idempotency tests (`TestPointLedgerPostgresAwardRetryAndLostResponse`
and neighbors) that behaviorally prove the same contract; the presence loop is redundant, not just
non-compliant.
Output: `fansub_test.go` proves the group-edit guard via a real request;
`point_ledger_repository_test.go`'s presence loop is removed, its absence loop kept, its
behavioral proof left to the file's existing real-Postgres tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-CONTEXT.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-04-PLAN.md
</context>

<interfaces>
Current state (re-read 2026-09-04):

`backend/internal/handlers/fansub_test.go`:
`TestFansubAliasMutationsUseGroupEditPermission` (lines 284-312): reads
`fansub_group_aliases.go`, asserts 2 absence checks (`CreateFansubAlias`/`DeleteFansubAlias` must
NOT start with a `requireAdmin` gate — KEEP) followed by a 5-item presence loop
(`permissionactorfromcontext(c)`, `permissions.actionfansubgroupedit`, `canforfansubgroup`,
`fansub_group_alias.create.denied`, `fansub_group_alias.delete.denied`, all lowercased) — REPLACE.
Production: `FansubHandler.CreateFansubAlias(c *gin.Context)` (line 48) and
`DeleteFansubAlias(c *gin.Context)` (line 117), both in `fansub_group_aliases.go`; both call
`permissionActorFromContext(c)` (line 172) then
`h.permissionSvc.CanForFansubGroup(c.Request.Context(), actor, permissions.ActionFansubGroupEdit,
fansubID)` (line 177). Other functions in `fansub_test.go` do not use `os.ReadFile` — leave
untouched.

`backend/internal/repository/point_ledger_repository_test.go`:
`TestPointLedgerSQLContract` (lines 75-88): a 6-item presence loop (`"ON CONFLICT DO NOTHING"`,
`"INSERT INTO POINT_LEDGER_ENTRIES"`, `"INSERT"`, `"SELECT"`, `"FOR UPDATE"`,
`"REVERSAL_OF_ENTRY_ID"`) — REPLACE/REMOVE, followed by a 2-item forbidden-SQL absence loop
(`"UPDATE POINT_LEDGER_ENTRIES"`, `"DELETE FROM POINT_LEDGER_ENTRIES"`) — KEEP. This same file
already has real-Postgres tests at lines 149-245
(`TestPointLedgerPostgresAwardRetryAndLostResponse`, `TestPointLedgerPostgresConcurrentAward`,
`TestPointLedgerPostgresReversalRetryConcurrentAndLostResponse`,
`TestPointLedgerPostgresRollback`) that already exercise award/reversal/idempotency/FOR-UPDATE
locking behavior against a real database — these are the executed proof this file's SQL-contract
claim needs; the presence loop adds nothing beyond what those already prove.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remediate fansub_test.go's alias permission-guard claim</name>
  <files>backend/internal/handlers/fansub_test.go</files>
  <read_first>backend/internal/handlers/fansub_test.go, backend/internal/handlers/fansub_group_aliases.go</read_first>
  <behavior>
    - Keep the 2-item absence check (no `requireAdmin` gate on either handler) unchanged.
    - Replace the 5-item presence loop with two real `httptest` calls: (a) call
      `CreateFansubAlias` via a real `gin` request/router with a permission-actor context that
      `CanForFansubGroup(..., ActionFansubGroupEdit, ...)` denies (fake/stub permission service
      returning `Allowed: false`), assert the response is a 403-class rejection carrying the
      `fansub_group_alias.create.denied` error code in the JSON body; (b) same for
      `DeleteFansubAlias` with `fansub_group_alias.delete.denied`. If a permission-service fake
      that satisfies the handler's real interface does not already exist in this package, build a
      minimal struct-literal double mirroring `stubCapabilityAuthzRepo`'s style (no mock
      framework), implementing only what `permissionActorFromContext`/`CanForFansubGroup` need.
  </behavior>
  <action>
  Implement the two real httptest calls described above, replacing the 5-item presence loop.
  Read `fansub_group_aliases.go` fully first (both handler bodies) to get the exact request
  shape (route params, body) each handler expects before constructing the test requests.
  </action>
  <acceptance_criteria>
    - `grep -A30 "func TestFansubAliasMutationsUseGroupEditPermission" backend/internal/handlers/fansub_test.go | grep -c "strings.Contains(normalized, \"permissionactorfromcontext\|strings.Contains(normalized, \"canforfansubgroup\""` returns 0
    - `cd backend && go test ./internal/handlers/... -run TestFansubAliasMutationsUseGroupEditPermission -v` passes
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/handlers/... -run TestFansubAliasMutationsUseGroupEditPermission -v</automated>
  </verify>
  <done>The alias mutation permission guard is proven via real 403 responses carrying the expected error codes, not source inspection; the absence check is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove point_ledger_repository_test.go's redundant presence loop</name>
  <files>backend/internal/repository/point_ledger_repository_test.go</files>
  <read_first>backend/internal/repository/point_ledger_repository_test.go</read_first>
  <behavior>
    - Keep the 2-item forbidden-SQL absence loop unchanged (rename/relocate it into its own small
      test function if needed for clarity, but it may also stay inline in
      `TestPointLedgerSQLContract` — either is acceptable as long as the presence loop above it is
      gone).
    - Remove the 6-item presence loop entirely — do not replace it with a new assertion; the
      file's existing `TestPointLedgerPostgresAwardRetryAndLostResponse` /
      `TestPointLedgerPostgresConcurrentAward` /
      `TestPointLedgerPostgresReversalRetryConcurrentAndLostResponse` /
      `TestPointLedgerPostgresRollback` (all real-Postgres, already passing) already behaviorally
      prove the INSERT-only, ON-CONFLICT-idempotent, FOR-UPDATE-locked, reversal-linked contract
      this presence loop was redundantly re-asserting from source text.
  </behavior>
  <action>
  Delete the 6-item presence loop from `TestPointLedgerSQLContract` (or delete the whole function
  if the absence loop is moved into a new, separate `TestPointLedgerForbidsDirectMutationSQL`-style
  function name that better reflects what remains). Do not touch the 4 existing real-Postgres test
  functions.
  </action>
  <acceptance_criteria>
    - `grep -c "ON CONFLICT DO NOTHING\", \"INSERT INTO POINT_LEDGER_ENTRIES" backend/internal/repository/point_ledger_repository_test.go` returns 0
    - `go test ./backend/internal/repository/... -run TestPointLedger -v` still passes (all real-Postgres tests unaffected, using whatever DSN env var this file's tests already require)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/repository/... -run TestPointLedger -v</automated>
  </verify>
  <done>The redundant presence-based SQL-contract loop is gone; the sanctioned absence loop and all 4 real-Postgres behavioral tests remain intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin browser -> fansub alias mutation endpoints | Group-edit-permission-gated create/delete of fansub group aliases |
| Test suite -> false-assurance risk | Presence-based source assertions duplicating claims already behaviorally proven elsewhere |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-12 | Repudiation (false assurance) | `fansub_test.go` | mitigate | Real httptest calls now prove the group-edit permission guard rejects unauthorized alias mutation, instead of a source grep |
| T-146-13 | Repudiation (false assurance) | `point_ledger_repository_test.go` | mitigate | Redundant presence loop removed; the file's existing real-Postgres tests remain the sole, sufficient behavioral proof |
</threat_model>

<verification>
- `cd backend && go build ./...`
- `go test ./internal/handlers/... -run TestFansubAliasMutationsUseGroupEditPermission -v`
- `go test ./internal/repository/... -run TestPointLedger -v`
</verification>

<success_criteria>
Two more of the 20 locked security-relevant files no longer claim security-relevant behavior via
source-substring inspection.
</success_criteria>

<output>
Create `.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-07-SUMMARY.md` when done
</output>
