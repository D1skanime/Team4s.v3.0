---
phase: 146
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/permissions/permissions.go
  - backend/internal/repository/hist_group_member_roles_repository.go
  - backend/internal/repository/membership_baseline_registry_test.go
autonomous: true
requirements: ["Criterion 3", "Criterion 4"]

must_haves:
  truths:
    - "ListGroupHistoryRoleDefinitions never returns the reserved pseudo-role group_member, matching its three Phase-145 sibling queries (D-04)"
    - "The three membership-baseline action codes exist in exactly one Go source location; the migration seed and the startup validator can never silently drift apart from it (D-05)"
  artifacts:
    - path: "backend/internal/permissions/permissions.go"
      provides: "exported var MembershipBaselineActionCodes, single source for the 3 baseline action codes"
    - path: "backend/internal/repository/hist_group_member_roles_repository.go"
      provides: "ListGroupHistoryRoleDefinitions SQL predicate AND NOT rd.reserved"
    - path: "backend/internal/repository/membership_baseline_registry_test.go"
      provides: "real-Postgres proof for the 4th excluded query and the anti-drift assertion (D-12)"
  key_links:
    - from: "backend/internal/permissions/permissions.go"
      to: "validateMembershipBaselineRegistryPresence"
      via: "MembershipBaselineActionCodes var consumed instead of the old inline literal"
      pattern: "MembershipBaselineActionCodes"
    - from: "backend/internal/repository/hist_group_member_roles_repository.go"
      to: "role_definitions"
      via: "SQL predicate AND NOT rd.reserved"
      pattern: "AND NOT rd.reserved"
---

<objective>
Close ROADMAP.md Success Criteria 3 and 4 for Phase 146 Block 1 (Registry-Selbstschutz):
quote verbatim —

Criterion 3: "`ListGroupHistoryRoleDefinitions` trägt denselben `NOT reserved`-Filter wie seine
drei Geschwisterabfragen; ein Test belegt gegen echtes Postgres, dass die Pseudo-Rolle in keiner
der vier Abfragen auftaucht."

Criterion 4: "Die drei Baseline-Action-Codes haben eine einzige autoritative Quelle; die
verbleibenden Verwendungen leiten sich davon ab oder sind durch einen Test gegen Auseinanderdriften
gesichert."

Purpose: this is pure operational-safety/consistency work per CONTEXT.md D-13/D-14 — NOT an
access-control fix. The reserved pseudo-role `group_member` (Phase 145) is structurally supposed
to be excluded from every role-picker query and to have its 3 baseline action codes defined in
exactly one place; today one of the four sibling queries is missing the filter and the 3 codes
are triplicated (migration literal, Go validator literal, TS filter literal).
Output: `MembershipBaselineActionCodes` exported Go var (single source), the 4th query fixed,
extended real-Postgres test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-CONTEXT.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-PATTERNS.md
</context>

<interfaces>
Current state (re-read 2026-09-04, exact line numbers as of this pass — re-verify with the
executor's own Read before editing, other work may have landed):

`backend/internal/permissions/permissions.go`:
- Line 89: `const RoleMembershipBaseline = "group_member"`
- Lines 423-431 (`validateMembershipBaselineRegistryPresence`): the inline literal to extract is
  at line 425: `for _, a := range []Action{ActionFansubGroupMembersView, ActionFansubGroupMediaView, ActionFansubGroupMediaUpload} {`

`backend/internal/repository/hist_group_member_roles_repository.go`:
- Line 248: `func (r *HistGroupMemberRolesRepository) ListGroupHistoryRoleDefinitions(ctx context.Context) ([]RoleDefinitionOption, error) {`
- Line 253: `WHERE 'group_history' = ANY(rd.contexts)` (missing filter — this is the fix target)
- Line 297 (sibling, already correct): `WHERE ('fansub_group' = ANY(rd.contexts) OR 'group_history' = ANY(rd.contexts)) AND NOT rd.reserved`
- Line 320: `RoleCodeExistsForContext(ctx context.Context, roleCode string, contextName string) (bool, error)` exists — out of this plan's scope (RESEARCH.md Open Question 2 / D-04 locks scope to the 4-query ListGroupHistoryRoleDefinitions count only; do not touch RoleCodeExistsForContext here, it is remediated for test-quality reasons in a Block-2 plan, not for a filter change)

`backend/internal/repository/membership_baseline_registry_test.go`:
- Lines 137-169 (`TestMembershipBaselineMigrationSeedsExactlyThreeActionsAndPreservesEffectiveRights`):
  lines 165-169 currently assert the migration seed against a SECOND hardcoded `[]string{...}`
  literal — this is Criterion 4's anti-drift target.
- Lines 231-266 (`TestReservedPseudoRoleExcludedFromPickersAndMarkedInCapabilityMatrix`): asserts
  3 of the 4 sibling queries (ListCapabilityMatrix role_kind, ListFansubGroupRoleDefinitions,
  ListPublicRoleDefinitions) already exclude/mark the reserved role correctly. This is the exact
  test to extend with the 4th (`ListGroupHistoryRoleDefinitions`).
- `phase145MigrationPath(t, name)` (lines 24-31) resolves `database/migrations/<name>` relative
  to this file — reuse unchanged for `0160_membership_baseline_pseudo_role.up.sql`.
- `testsupport.OpenPhase145Postgres(t)` (SKIP-not-FAIL if `TEAM4S_PHASE145_TEST_DSN` unset) is the
  established fixture for this exact schema — reuse unchanged.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract MembershipBaselineActionCodes as the single Go source (Criterion 4)</name>
  <files>backend/internal/permissions/permissions.go</files>
  <read_first>backend/internal/permissions/permissions.go</read_first>
  <action>
  In `backend/internal/permissions/permissions.go`, add a new exported package-level var directly
  above `validateMembershipBaselineRegistryPresence` (currently starting at line 423), named
  `MembershipBaselineActionCodes`, typed `[]Action`, containing exactly
  `ActionFansubGroupMembersView, ActionFansubGroupMediaView, ActionFansubGroupMediaUpload` in that
  order. Add a doc comment stating it is the single Go source for the 3 membership-baseline
  action codes (Criterion 4, Phase 146), consumed by `validateMembershipBaselineRegistryPresence`
  and by the new mutation guards Plan 146-03 adds to `admin_capability_handler.go`.
  Change `validateMembershipBaselineRegistryPresence`'s loop (currently
  `for _, a := range []Action{ActionFansubGroupMembersView, ActionFansubGroupMediaView, ActionFansubGroupMediaUpload} {`
  at line 425) to `for _, a := range MembershipBaselineActionCodes {` — delete the inline literal,
  keep the rest of the function (the `slices.Contains` check and error message) byte-identical.
  Do not touch `RoleMembershipBaseline` (line 89) or any other function in this file.
  Note: `permissions.go` is already 928 lines, more than double CLAUDE.md's 450-line
  production-code cap — this is pre-existing debt unrelated to this phase. This task's small
  additive var/loop change does not remediate that debt; the file's size stays out of scope
  for this additive phase and should remain visible as carried-forward debt, not silently
  touched without acknowledgment.
  </action>
  <acceptance_criteria>
    - `grep -c "MembershipBaselineActionCodes" backend/internal/permissions/permissions.go` returns at least 2 (the var declaration + its use inside `validateMembershipBaselineRegistryPresence`)
    - `grep -n "ActionFansubGroupMembersView, ActionFansubGroupMediaView, ActionFansubGroupMediaUpload" backend/internal/permissions/permissions.go` shows the literal ONLY inside the new `MembershipBaselineActionCodes` var declaration, nowhere else in this file
    - `cd backend && go build ./...` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./internal/permissions/...</automated>
  </verify>
  <done>MembershipBaselineActionCodes is the sole literal source of the 3 baseline action codes in this file; validateMembershipBaselineRegistryPresence's fail-closed startup behavior is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add the missing NOT reserved filter to ListGroupHistoryRoleDefinitions (Criterion 3)</name>
  <files>backend/internal/repository/hist_group_member_roles_repository.go</files>
  <read_first>backend/internal/repository/hist_group_member_roles_repository.go</read_first>
  <action>
  In `ListGroupHistoryRoleDefinitions` (starting at line 248), change the SQL predicate on line 253
  from `WHERE 'group_history' = ANY(rd.contexts)` to
  `WHERE 'group_history' = ANY(rd.contexts) AND NOT rd.reserved` — mirroring the already-fixed
  sibling `ListFansubGroupRoleDefinitions` (line 297) exactly. Touch only this one predicate line;
  the `RoleDefinitionOption` struct, the `GROUP BY`/`ORDER BY` clauses, and the row-scan loop stay
  byte-identical. Do not modify `RoleCodeExistsForContext` (line 320) or `IsHistoricalMemberRoleCode`
  — out of Criterion 3's locked D-04 scope (they are remediated for test-quality reasons only, in
  a separate Block-2 plan, without a filter-rule change).
  </action>
  <acceptance_criteria>
    - `grep -A2 "func (r \*HistGroupMemberRolesRepository) ListGroupHistoryRoleDefinitions" backend/internal/repository/hist_group_member_roles_repository.go` shows `AND NOT rd.reserved` on the `WHERE` line
    - `cd backend && go build ./...` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./...</automated>
  </verify>
  <done>ListGroupHistoryRoleDefinitions' WHERE clause matches its 3 siblings' NOT reserved predicate exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend real-Postgres coverage for the 4th query and the anti-drift assertion</name>
  <files>backend/internal/repository/membership_baseline_registry_test.go</files>
  <read_first>backend/internal/repository/membership_baseline_registry_test.go</read_first>
  <behavior>
    - Extend `TestReservedPseudoRoleExcludedFromPickersAndMarkedInCapabilityMatrix` (or add a
      sibling `TestXxx` function in the same file) with a 4th assertion block: call
      `NewHistGroupMemberRolesRepository(pool).ListGroupHistoryRoleDefinitions(ctx)` against the
      same migrated pool used for the other 3 pickers in that test, and assert with
      `assert.NotEqual(t, "group_member", opt.Code, ...)` for every returned option — mirroring the
      existing `ListFansubGroupRoleDefinitions` assertion block in the same function exactly.
    - Change `TestMembershipBaselineMigrationSeedsExactlyThreeActionsAndPreservesEffectiveRights`'s
      lines 165-169 (currently comparing the migration-seeded `actionCodes` against a second
      hardcoded `[]string{"fansub_group.members.view", "fansub_group_media.upload",
      "fansub_group_media.view"}` literal) to instead build the expected slice from
      `permissions.MembershipBaselineActionCodes` (convert each `permissions.Action` to `string`)
      and compare with `assert.ElementsMatch`, so the migration seed is now anti-drift-tested
      against the single Go source from Task 1, not a 4th independent hardcoded copy.
  </behavior>
  <action>
  Implement exactly the two behavior changes above. Reuse `pool`, `ctx`, and the existing
  migration-apply call already present in each test function — do not create a new fixture or a
  new `phase145MigrationPath` helper (already defined at lines 24-31 in this file).
  </action>
  <acceptance_criteria>
    - `TEAM4S_PHASE145_TEST_DSN=<local dsn> go test ./internal/repository/... -run TestReservedPseudoRoleExcludedFromPickersAndMarkedInCapabilityMatrix -v` passes and its output does NOT print `SKIP`
    - `TEAM4S_PHASE145_TEST_DSN=<local dsn> go test ./internal/repository/... -run TestMembershipBaselineMigrationSeedsExactlyThreeActionsAndPreservesEffectiveRights -v` passes and does NOT print `SKIP`
    - `grep -n "permissions.MembershipBaselineActionCodes" backend/internal/repository/membership_baseline_registry_test.go` finds at least one match
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./internal/repository/...</automated>
  </verify>
  <done>All 4 role-picker queries are proven against real Postgres to exclude group_member; the migration seed is proven never to drift from permissions.MembershipBaselineActionCodes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin browser -> role-picker/history endpoints | Platform-admin-authenticated requests reading role catalogs used to populate assignment/history UI dropdowns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-01 | Tampering (registry-consistency, not access-control — see CONTEXT.md D-13/D-14) | `ListGroupHistoryRoleDefinitions` | mitigate | `AND NOT rd.reserved` predicate keeps the reserved pseudo-role out of the group-history role picker, consistent with its 3 sibling queries; proven with a real-Postgres test (Task 3) |
| T-146-02 | Repudiation (false assurance) | `membership_baseline_registry_test.go` | mitigate | Anti-drift assertion (Task 3) ties the migration seed to `permissions.MembershipBaselineActionCodes` so a future edit to one cannot silently diverge from the other without failing a real test |
</threat_model>

<verification>
- `cd backend && go build ./... && go vet ./...`
- `TEAM4S_PHASE145_TEST_DSN=<local dsn> go test ./internal/repository/... -run TestReservedPseudoRole -v` (no SKIP)
- `TEAM4S_PHASE145_TEST_DSN=<local dsn> go test ./internal/repository/... -run TestMembershipBaselineMigrationSeeds -v` (no SKIP)
</verification>

<success_criteria>
Criterion 3 and Criterion 4 (ROADMAP.md, quoted in Objective above) are both true: the 4th query
excludes the reserved role, proven against real Postgres; the 3 baseline action codes have one Go
source, and the migration seed is anti-drift-tested against it.
</success_criteria>

<output>
Create `.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-01-SUMMARY.md` when done
</output>
