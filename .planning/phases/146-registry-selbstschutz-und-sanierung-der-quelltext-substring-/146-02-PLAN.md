---
phase: 146
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/admin/roles/RoleCapabilityDetail.tsx
  - frontend/src/app/admin/roles/RoleCapabilityDetail.test.tsx
autonomous: true
requirements: ["Criterion 2"]

must_haves:
  truths:
    - "The capability matrix shows exactly 3 interactive, visibly-protected baseline rows for the reserved pseudo-role group_member, across all 8 accordion categories, not just the 2 opened during Phase 145 UAT (D-19)"
    - "Each of those 3 rows carries a Badge with Lock icon, visible text \"Geschützt\", and a screen-reader description wired via aria-describedby (D-06)"
    - "A rejected mutation attempt surfaces the backend's speaking German message through the existing, unchanged mutationError path — no code change needed in RoleCapabilityImpactPreviewModal.tsx (D-06)"
    - "This is a display/consistency correction, not a privilege-escalation fix — the platform-admin bypass performing the edit is intentional and out of scope (D-13, D-14)"
  artifacts:
    - path: "frontend/src/app/admin/roles/RoleCapabilityDetail.tsx"
      provides: "configurableActions filter fix (D-15) + Badge/Lock/aria-describedby per 146-UI-SPEC.md"
    - path: "frontend/src/app/admin/roles/RoleCapabilityDetail.test.tsx"
      provides: "rewritten '145-03' test (D-07) + new real-38-action-shape fixture test (D-19, D-18) proving exactly 3 switches across all categories"
  key_links:
    - from: "frontend/src/app/admin/roles/RoleCapabilityDetail.tsx"
      to: "membershipBaselineCodes"
      via: "configurableActions filter, both the reserved and non-reserved branches"
      pattern: "configurableActions = isReservedBaseline"
    - from: "frontend/src/app/admin/roles/RoleCapabilityDetail.tsx"
      to: "Switch aria-describedby"
      via: "baseline-protected-${action.code} id on the Badge, referenced by the Switch"
      pattern: "aria-describedby"
---

<objective>
Close ROADMAP.md Success Criterion 2: "Die Capability-Matrix weist diese drei Rechte sichtbar als
geschützt aus und zeigt bei einem Versuch eine sprechende deutsche Meldung mit korrekten Umlauten —
kein stiller Fehlschlag und kein roher Serverfehler."

Purpose: implements the locked `146-UI-SPEC.md` interaction contract on
`RoleCapabilityDetail.tsx`, AND fixes the prerequisite bug the UI-SPEC's "genau 3 Zeilen" premise
silently assumed but Phase-146 research found false: `ListCapabilityMatrix` is an unscoped CROSS
JOIN, so the reserved pseudo-role's `role.actions` array today carries all 38 catalog actions (34
live, unbadged, unprotected Switch rows plus the 3 intended baseline rows), not 3 — hidden during
Phase-145 UAT/unit-testing only because the Accordion lazy-mounts unopened categories (D-19). This
plan closes both: D-15 (filter fix, makes "3 rows" literally true) and the additive badge/lock
contract (Criterion 2 itself). Framing note: this is a display/consistency correction, not a
privilege-escalation fix — the platform-admin bypass performing the edit is intentional and out of
scope (CONTEXT.md D-13/D-14).
Output: `RoleCapabilityDetail.tsx` shows exactly 3 protected rows for the reserved role; test suite
proves it against the real 38-action shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-CONTEXT.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-UI-SPEC.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-PATTERNS.md
</context>

<interfaces>
Current state (re-read 2026-09-04, `frontend/src/app/admin/roles/RoleCapabilityDetail.tsx`):

```tsx
const membershipBaselineCodes = new Set([
  "fansub_group.members.view",
  "fansub_group_media.view",
  "fansub_group_media.upload",
])
...
const isEditable = role.capability_editable !== false
const isReservedBaseline = role.role_kind === 'reserved_baseline'
const configurableActions = isReservedBaseline
  ? role.actions
  : role.actions.filter((action) => !membershipBaselineCodes.has(action.code))
```
Row map (inside `accordionItems` useMemo): label `<span>` (lines ~89-97), then either a
`"Systemaktion"` span (`action.standalone`) or a `<Switch checked={action.granted}
disabled={!isEditable} aria-label={action.label_de} onCheckedChange={...} />` (lines ~109-120).
No `Badge`/`Lock` import currently exists in this file.

`RoleCapabilityDetail.test.tsx` fixtures: `assignableRole`, `historicalRole`,
`reservedBaselineRole` (3 actions only, all `granted: true`, categories `gruppe`/`gruppenmedien`).
`DetailHarness` accepts `initialOpen: string[]` (arbitrary category IDs) and holds
`openCategories` state — reuse unchanged for the new fixture test.
`Accordion` (`frontend/src/components/ui/Accordion.tsx`) lazy-mounts:
`isMounted = isOpen || keepMountedIds?.has(id)` — a category not in `initialOpen` renders zero
switches for that category.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix configurableActions filter (D-15) and add the protected badge (Criterion 2, UI-SPEC steps 1-6)</name>
  <files>frontend/src/app/admin/roles/RoleCapabilityDetail.tsx</files>
  <read_first>frontend/src/app/admin/roles/RoleCapabilityDetail.tsx</read_first>
  <behavior>
    - With `isReservedBaseline === true`, `configurableActions` must contain ONLY the 3 actions
      whose `code` is in `membershipBaselineCodes` (currently it passes `role.actions` through
      unfiltered — 38 entries in production).
    - With `isReservedBaseline === false`, `configurableActions` keeps its existing behavior
      (filters OUT `membershipBaselineCodes`) — unchanged.
    - For each row where `isReservedBaseline && membershipBaselineCodes.has(action.code)`, render a
      `Badge` between the label span and the Switch, and add `aria-describedby` to that row's
      `Switch`.
  </behavior>
  <action>
  Add two imports after the file's existing imports:
  `import { Badge } from '@/components/ui/Badge'` and `import { Lock } from 'lucide-react'`.
  Change the `configurableActions` ternary to:
  `isReservedBaseline ? role.actions.filter((action) => membershipBaselineCodes.has(action.code)) : role.actions.filter((action) => !membershipBaselineCodes.has(action.code))`.
  In the row-rendering map, between the label `<span>` and the `standalone`/`Switch` conditional,
  insert (exact JSX per `146-UI-SPEC.md` Interaction Contract step 2, verbatim, do not paraphrase
  the copy):
  `{isReservedBaseline && membershipBaselineCodes.has(action.code) && (<Badge variant="info" id={`baseline-protected-${action.code}`}><Lock size={16} aria-hidden="true" />{' '}Geschützt<span className="visually-hidden">{' '}– Teil der Mitgliedschafts-Grundausstattung. Entziehen wird serverseitig abgelehnt, da jedes aktive Mitglied dieses Recht automatisch benötigt.</span></Badge>)}`.
  Add `aria-describedby={isReservedBaseline && membershipBaselineCodes.has(action.code) ? `baseline-protected-${action.code}` : undefined}` to the existing `Switch` element, alongside its
  unchanged `aria-label={action.label_de}`.
  Do NOT change `disabled={!isEditable}`, `onRequestChange`, the accordion, categories, the top
  caution paragraph, or the "Grundausstattung öffnen" deep-link sentence — all byte-identical to
  today (UI-SPEC steps 4-6). Do NOT touch `RoleCapabilityImpactPreviewModal.tsx` (UI-SPEC step 6 —
  its existing `mutationError` render path already surfaces whatever message the backend returns).
  </action>
  <acceptance_criteria>
    - `grep -c "membershipBaselineCodes.has(action.code)" frontend/src/app/admin/roles/RoleCapabilityDetail.tsx` returns at least 3 (reserved-branch filter, Badge condition, aria-describedby condition)
    - `grep -n "Badge variant=\"info\"" frontend/src/app/admin/roles/RoleCapabilityDetail.tsx` finds exactly one match
    - `grep -n "import { Lock } from 'lucide-react'" frontend/src/app/admin/roles/RoleCapabilityDetail.tsx` finds exactly one match
    - `grep -n "disabled={!isEditable}" frontend/src/app/admin/roles/RoleCapabilityDetail.tsx` still finds exactly one match, unchanged
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -i "RoleCapabilityDetail" || true</automated>
  </verify>
  <done>The reserved pseudo-role renders exactly 3 configurableActions rows, each with the Badge/Lock/"Geschützt"/aria-describedby contract; every other role's behavior is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite the "keine Sonderbehandlung" test and add the real-38-action fixture proof (D-07, D-19)</name>
  <files>frontend/src/app/admin/roles/RoleCapabilityDetail.test.tsx</files>
  <read_first>frontend/src/app/admin/roles/RoleCapabilityDetail.test.tsx</read_first>
  <behavior>
    - Test 1 (rewrite of "145-03 ... keine Sonderbehandlung"): rename it to reflect the new,
      correct behavior (badge IS special treatment now); keep the existing assertions (3 switches,
      3 checked, hint-text paragraph present, no "Grundausstattung öffnen" link) since those still
      hold; ADD an assertion that `screen.getAllByText('Geschützt')` has length 3 (or an equivalent
      query proving the badge text renders exactly 3 times).
    - Test 2 (new, D-19): build a new fixture `reservedBaselineRoleFullCatalog` (or similarly named)
      with the real per-D-18 shape — 38 total actions for `group_member`: the existing 3 baseline
      actions (`granted: true`, `standalone: false`, categories `gruppe`/`gruppenmedien`) plus 34
      non-baseline actions spread across the other 6 categories from `146-CONTEXT.md`/RESEARCH.md
      (`gruppenseite`, `projekt`, `rechteverwaltung`, `release`, `review`,
      `veroeffentlichungen`), all `granted: false`, `standalone: false`, plus 1 standalone action
      (`fansub_group.invitations.accept`, `standalone: true`) in any category. Render via
      `DetailHarness` with `initialOpen` set to ALL 8 category IDs present in the fixture (to defeat
      the Accordion's lazy-mount). Assert `screen.getAllByRole('switch')` has length 3 (not 37) —
      proving Task 1's filter fix actually holds against the real action-count shape, not just the
      old 3-action fixture.
  </behavior>
  <action>
  Implement exactly the two behavior changes above in `RoleCapabilityDetail.test.tsx`. Reuse the
  existing `DetailHarness` component and `reservedBaselineRole` fixture where applicable; only add
  a new fixture constant for Test 2, do not mutate `reservedBaselineRole` itself (other tests
  depend on its exact 3-action shape).
  </action>
  <acceptance_criteria>
    - `cd frontend && npm run test -- RoleCapabilityDetail` passes, 0 failures
    - `grep -n "getAllByText('Geschützt')" frontend/src/app/admin/roles/RoleCapabilityDetail.test.tsx` finds at least one match
    - `grep -c "func Test\|it('" frontend/src/app/admin/roles/RoleCapabilityDetail.test.tsx` (adapted grep: `grep -c "  it("`) shows the file gained exactly one new test case relative to before this task (rewritten test does not count as new)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- RoleCapabilityDetail</automated>
  </verify>
  <done>The old "keine Sonderbehandlung" claim no longer exists in the suite; a real-action-count test proves exactly 3 switches render for the reserved role across all 8 categories.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin browser -> capability matrix UI | Platform-admin-authenticated React client rendering role.actions from a trusted API response |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-03 | Tampering (registry-consistency, not access-control — CONTEXT.md D-13/D-14) | `RoleCapabilityDetail.tsx` reserved-branch row filter | mitigate | `configurableActions` filter now scopes the reserved role's rendered rows to exactly its 3 intended baseline actions, matching its design intent, instead of exposing all 38 catalog actions as live unprotected switches |
| T-146-04 | Repudiation (false assurance) | `RoleCapabilityDetail.test.tsx` | mitigate | New fixture exercises the real 38-action shape across all 8 categories (not the old 3-action fixture that could not expose the bug), closing the UAT/unit-test blind spot D-19 documents |
</threat_model>

<verification>
- `cd frontend && npm run test -- RoleCapabilityDetail`
- `cd frontend && npx tsc --noEmit`
</verification>

<success_criteria>
Criterion 2 (ROADMAP.md, quoted in Objective above) is true: the capability matrix visibly marks
exactly 3 baseline rights as protected for the reserved role, across all categories.
</success_criteria>

<output>
Create `.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-02-SUMMARY.md` when done
</output>
