---
phase: 146
plan: 06
type: execute
wave: 3
depends_on: ["146-01", "146-02", "146-03"]
files_modified:
  - backend/internal/repository/member_archive_repository_test.go
  - backend/internal/repository/member_point_totals_repository_test.go
autonomous: true
requirements: ["Criterion 5", "Criterion 6"]

must_haves:
  truths:
    - "member_archive_repository_test.go and member_point_totals_repository_test.go prove their 'uses canonical stored slug, not a generated fallback' claim via a real Postgres call, not by grepping a SQL fragment out of the repository source"
  artifacts:
    - path: "backend/internal/repository/member_archive_repository_test.go"
      provides: "real-Postgres proof that the archive projection returns the stored public_slug"
    - path: "backend/internal/repository/member_point_totals_repository_test.go"
      provides: "real-Postgres proof that the ranking projection returns the stored public_slug"
  key_links:
    - from: "backend/internal/repository/member_archive_repository_test.go"
      to: "member_archive_repository.go"
      via: "real repository call against a seeded member row with a divergent nickname-derived vs. stored public_slug"
      pattern: "public_slug"
---

<objective>
Remediate two more of Phase 146's 20 locked security-relevant Block-2 files:
`member_archive_repository_test.go` and `member_point_totals_repository_test.go` — both share the
identical anti-pattern shape (a "uses canonical stored slug directly, no generated fallback"
claim proven by grepping the repository's own SQL text).

Purpose: closes Criteria 5/6 for these two files by replacing the presence half of each claim with
a real Postgres call proving the actual returned data uses the stored `public_slug`, not a
`regexp_replace`/nickname-derived/id-fallback value — the exact Phase-128 identity invariant
(`.planning/PROJECT.md`) these tests exist to protect, now proven by executing the query.
Output: both files' presence assertions replaced with real Postgres calls; their absence loops
(forbidden generated-slug fragments) are preserved unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-CONTEXT.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-04-PLAN.md
</context>

<interfaces>
Current state (re-read 2026-09-04):

`backend/internal/repository/member_archive_repository_test.go`:
`TestArchiveUsesCanonicalStoredMemberSlug` (lines 9-24): 1 presence assertion (`strings.Contains(
compact, "m.public_slug as slug")`) plus a 4-item forbidden-fragment absence loop
(`memberslugexpr`, `regexp_replace`, `coalesce(m.public_slug`, `id::text`) — KEEP the absence loop.
Production entry point: `MemberArchiveRepository.SearchMembers(...)` (`member_archive_repository.go:55`),
constructed via `NewMemberArchiveRepository(db *pgxpool.Pool)`. Other functions in this file
(`TestArchiveVisibilityFilter`, `TestArchivePaginationBounds`, `TestArchiveRoleFilter`) do NOT use
`os.ReadFile` — out of this task's scope, leave untouched.

`backend/internal/repository/member_point_totals_repository_test.go`:
`TestMemberPointTotalsRankingUsesCanonicalStoredSlug` (lines ~27-38): 1 presence assertion
(`require.Contains(t, compact, "m.public_slug as slug", ...)`) plus a 4-item forbidden-fragment
absence loop (identical shape to the archive file: `memberslugexpr`, `regexp_replace`,
`coalesce(m.public_slug`, `id::text`) — KEEP the absence loop. Production entry point:
`MemberPointTotalsRepository.ListRanking(ctx, page int) ([]MemberPointRankingRow, int, error)`
(`member_point_totals_repository.go:41`), constructed via
`NewMemberPointTotalsRepository(db *pgxpool.Pool)`. Other functions in this file already use a
real Postgres fixture (see `TestMemberPointTotalsPostgresConcurrentAwardsSumCorrectly` and
neighbors) — reuse whatever fixture/seeding helper those already establish rather than inventing a
new one.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remediate member_archive_repository_test.go</name>
  <files>backend/internal/repository/member_archive_repository_test.go</files>
  <read_first>backend/internal/repository/member_archive_repository_test.go, backend/internal/repository/member_archive_repository.go</read_first>
  <behavior>
    - Keep the 4-item forbidden-fragment absence loop unchanged.
    - Replace the single presence assertion with a real Postgres proof: seed a `members` row whose
      `public_slug` is deliberately different from what a nickname-derived/regex-generated slug
      would produce (e.g. nickname "Sör Ätzë" but a hand-set `public_slug` like
      `stable-archive-slug`), call `MemberArchiveRepository.SearchMembers(...)` with search
      parameters matching that row, and assert the returned row's slug field equals the stored
      `public_slug` exactly — proving the query selects the stored value directly, not a generated
      fallback, by actually running it.
  </behavior>
  <action>
  Implement the rewrite above. First read `member_archive_repository.go` fully to get
  `SearchMembers`'s exact parameter/return types before writing the seed + call — do not guess the
  signature. Reuse whatever real-Postgres fixture is already established for this package
  (`testsupport.OpenPhase145Postgres` or a broader existing member-schema fixture already used by a
  neighboring `_test.go` file in this package — grep for `testsupport.Open` in
  `internal/repository/*_test.go` to find the one that includes the `members` table with
  `public_slug`).
  </action>
  <acceptance_criteria>
    - `grep -A20 "func TestArchiveUsesCanonicalStoredMemberSlug" backend/internal/repository/member_archive_repository_test.go | grep -c "strings.Contains(compact"` returns 0
    - `<db-fixture-env-var>=<local dsn> go test ./backend/internal/repository/... -run TestArchiveUsesCanonicalStoredMemberSlug -v` passes, no SKIP
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/repository/... -run TestArchiveUsesCanonicalStoredMemberSlug -v</automated>
  </verify>
  <done>The archive projection's canonical-slug claim is proven by a real SearchMembers call against a seeded row; the sanctioned absence loop is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remediate member_point_totals_repository_test.go</name>
  <files>backend/internal/repository/member_point_totals_repository_test.go</files>
  <read_first>backend/internal/repository/member_point_totals_repository_test.go, backend/internal/repository/member_point_totals_repository.go</read_first>
  <behavior>
    - Keep the 4-item forbidden-fragment absence loop unchanged.
    - Replace the single presence assertion (`require.Contains(t, compact, "m.public_slug as
      slug", ...)`) with a real Postgres proof: reuse the same real-Postgres fixture already
      established elsewhere in this file (e.g. by `TestMemberPointTotalsPostgresConcurrentAwardsSumCorrectly`),
      seed a member with a deliberately-divergent nickname vs. stored `public_slug`, award at
      least one point-ledger entry for that member so it appears in the ranking, call
      `NewMemberPointTotalsRepository(pool).ListRanking(ctx, page)`, and assert the returned
      `MemberPointRankingRow`'s slug field equals the stored `public_slug` exactly.
  </behavior>
  <action>
  Implement the rewrite above. Read `member_point_totals_repository.go` fully first to confirm
  `MemberPointRankingRow`'s exact field name for the slug before writing the assertion.
  </action>
  <acceptance_criteria>
    - `grep -A15 "func TestMemberPointTotalsRankingUsesCanonicalStoredSlug" backend/internal/repository/member_point_totals_repository_test.go | grep -c "require.Contains(t, compact"` returns 0
    - `go test ./backend/internal/repository/... -run TestMemberPointTotalsRankingUsesCanonicalStoredSlug -v` passes, no SKIP (using whatever DSN env var this file's existing Postgres tests already require)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/repository/... -run TestMemberPointTotalsRankingUsesCanonicalStoredSlug -v</automated>
  </verify>
  <done>The ranking projection's canonical-slug claim is proven by a real ListRanking call against a seeded row; the sanctioned absence loop is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test suite -> false-assurance risk | Repository test files claiming public-identity (slug) correctness without executing the query that serves it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-11 | Repudiation (false assurance) | `member_archive_repository_test.go`, `member_point_totals_repository_test.go` | mitigate | Presence-based SQL-text assertions replaced with real Postgres calls proving the returned slug is the stored `public_slug`, protecting the Phase-128 canonical-identity invariant with an executed test |
</threat_model>

<verification>
- `cd backend && go build ./...`
- `go test ./internal/repository/... -run "TestArchiveUsesCanonicalStoredMemberSlug|TestMemberPointTotalsRankingUsesCanonicalStoredSlug" -v`
</verification>

<success_criteria>
Two more of the 20 locked security-relevant files prove their canonical-slug claim via real
Postgres calls, not source inspection.
</success_criteria>

<output>
Create `.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-06-SUMMARY.md` when done
</output>
