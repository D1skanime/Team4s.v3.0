---
phase: 146
plan: 13
type: execute
wave: 4
depends_on: ["146-04", "146-05", "146-06", "146-07", "146-08", "146-09", "146-10", "146-11", "146-12"]
files_modified:
  - backend/internal/testquality/source_substring_guard_test.go
  - .planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-SUBSTRING-TEST-REMAINDER.md
autonomous: true
requirements: ["Criterion 6", "Criterion 7", "Criterion 8"]

must_haves:
  truths:
    - "A go test-executed guard fails the build if any new file (outside the frozen, shrink-only exception list) adds an os.ReadFile+.go presence-assertion pattern, mirroring the identical filter rule from D-08 (D-09)"
    - "None of the 20 locked security-relevant files appear in the guard's exception list"
    - "At most 36 of the original 53 files still read .go source, and this is measured, not asserted"
    - "The 33-file (or fewer) remainder is documented with a named reason per file, not a silent gap (D-11)"
  artifacts:
    - path: "backend/internal/testquality/source_substring_guard_test.go"
      provides: "TestNoNewSourceSubstringTests — the frozen, shrink-only ratchet guard, modeled on LEGACY_NO_RESTRICTED_SYNTAX_FILES"
    - path: ".planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-SUBSTRING-TEST-REMAINDER.md"
      provides: "Criterion 8's named, per-file documented remainder"
  key_links:
    - from: "backend/internal/testquality/source_substring_guard_test.go"
      to: "backend/internal/testquality/security_relevant_test_files.go"
      via: "empty-intersection assertion between SecurityRelevantTestFiles and the guard's LegacyAllowedSubstringTestFiles"
      pattern: "SecurityRelevantTestFiles"
---

<objective>
Close ROADMAP.md Success Criteria 6, 7, and 8, completing Block 2 (Testsanierung) — this is the
final Phase 146 plan, run only after every Block-2 remediation plan (146-04 through 146-12) has
landed, since the ratchet guard's exception list must reflect the ACTUAL post-remediation file set,
not the pre-remediation one.

Criterion 6: "Messbar am Ende: höchstens 36 Testdateien lesen noch eine .go-Quelltextdatei ein (von
53), und keine der 17 [20, per D-08's locked count] sicherheitsrelevanten ist darunter."
Criterion 7: "Ein automatischer Guard verhindert Neuzugänge — eingefrorene, nur schrumpfende
Ausnahmeliste nach dem Vorbild von `LEGACY_NO_RESTRICTED_SYNTAX_FILES` in
`frontend/eslint.config.mjs`."
Criterion 8: "Der bewusst stehen gelassene Restbestand ist als benannte Schuld dokumentiert, mit
dem Grund je Datei — nicht als stillschweigende Lücke."

Purpose: without this plan, the Block-2 remediation work (146-04 through 146-12) has no regression
protection and no closing measurement — a future PR could reintroduce the anti-pattern in a
remediated file, or in any of the 33 untouched files, with nothing catching it. There is no CI
pipeline in this repo (confirmed by RESEARCH.md — no `.github/workflows/`, `.gitlab-ci.yml`,
`Makefile`, or `.golangci.yml`) — the guard must be a plain `go test`-executed Go test, run via
`go test ./...` like every other test in this codebase, not a lint rule or CI job.
Output: the ratchet guard test file; the Criterion 8 remainder documentation; a final measured
confirmation of the ≤36/53 figure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-CONTEXT.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-04-PLAN.md
@.planning/notes/2026-09-04-messung-substring-tests.md
@.planning/notes/measure-substring-tests.py
@frontend/eslint.config.mjs
</context>

<interfaces>
`frontend/eslint.config.mjs`'s `LEGACY_NO_RESTRICTED_SYNTAX_FILES` (lines ~28-111): the shape to
mirror in Go — a flat `[]string` of explicit relative paths (NOT a glob), preceded by a comment
block stating the measurement date, the exact count, and "RATCHET — this list may only shrink,
never grow; a new violation is a deliberate, reviewed decision, not a silent addition."

`backend/internal/testquality/security_relevant_test_files.go` (Plan 146-04) exports
`SecurityRelevantTestFiles []string` — the frozen 20-file definition. This plan's guard must
assert that none of those 20 paths appear in its own exception list.

**Post-remediation file set (as of Plans 146-04 through 146-12 landing):** the 20 locked
security-relevant files fall into two groups: (a) 16 files actively remediated (146-04 through
146-12, replacing `os.ReadFile`+presence-assertion patterns with real calls) — these must now have
ZERO remaining `os.ReadFile(...".go")` calls that back a presence-style assertion; (b) 4 files
needing NO code change because their only `os.ReadFile` usage was already a pure absence check
(CLAUDE.md exception 1), confirmed during research: `internal/services/release_crew_service_test.go`
(`TestReleaseCrewServiceHasNoDirectLedgerSQL`), `internal/handlers/admin_content_anime_project_notes_test.go`
(`TestAnimeProjectNoteHandlerOwnsNoLedgerPolicy`), `internal/repository/review_delegation_repository_test.go`
(`TestPhase107ReviewDelegationSourceBoundary`), `internal/services/release_review_submission_test.go`
(`TestReleaseSourceSubmitRequestSurfacesDoNotOwnReviewState`) — verify this is still true (re-run
`measure-substring-tests.py` or an equivalent grep) before writing the remainder doc; if any of the
four turns out to have drifted, treat it as newly in-scope for this plan's Task 1, do not silently
assume the research-time snapshot still holds.

The remaining 33 (or fewer, if any of the "already compliant" 4 needed touching) "Übrige" files
from `.planning/notes/2026-09-04-messung-substring-tests.md`'s measurement are the frozen ratchet
exception list content — re-measure before finalizing (files may have shifted since 2026-09-04 if
other phases touched them in parallel).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure and confirm the post-remediation state</name>
  <files>backend/internal/testquality/source_substring_guard_test.go</files>
  <read_first>.planning/notes/measure-substring-tests.py</read_first>
  <action>
  Run `.planning/notes/measure-substring-tests.py` (or an updated equivalent) against the current
  `backend/` tree. The raw script counts ANY `os.ReadFile(...".go")` occurrence, presence or
  absence, without distinguishing the two — before trusting its raw total, update the script
  (or apply a manual filter on top of its output) to EXCLUDE any file whose only remaining
  `os.ReadFile` call backs a sanctioned pure absence check (per the rule documented in
  `security_relevant_test_files.go`/Plan 146-04 — CLAUDE.md Teststil exception 1: an identifier
  asserted to NEVER appear anywhere in the file). Confirm: (a) none of the 20
  `SecurityRelevantTestFiles` paths still contain an `os.ReadFile(...".go")` call backing a
  PRESENCE-style assertion (`strings.Contains`/`assert.Contains`/`require.Contains` without
  negation) — spot-check any file the script still flags as "reads .go source" among the 20 to
  confirm its remaining usage is a pure absence check, per the rule locked in Plan 146-04; if a
  genuine violation remains, fix it directly in this task (a small, targeted follow-up
  remediation, not deferred) rather than shipping Criterion 6 false. (b) using the
  absence-check-excluding count from the updated script/filter, record the exact resulting total
  file count still reading `.go` source for a PRESENCE-style claim (target: ≤36 of 53). Do not
  proceed to Task 2 until (a) holds for all 20 files.
  </action>
  <acceptance_criteria>
    - Re-run of the measurement script (or equivalent grep-based check) shows 0 of the 20 `SecurityRelevantTestFiles` paths with a remaining presence-style `os.ReadFile(...".go")` assertion
    - The total remaining file count is ≤36
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go test ./... 2>&1 | tail -30</automated>
  </verify>
  <done>The post-remediation state is confirmed: zero presence violations remain among the 20 security-relevant files; the total remaining-file count is ≤36.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the Criterion 7 ratchet guard</name>
  <files>backend/internal/testquality/source_substring_guard_test.go</files>
  <read_first>backend/internal/testquality/security_relevant_test_files.go, frontend/eslint.config.mjs</read_first>
  <behavior>
    - `TestSecurityRelevantFilesNeverInLegacyExceptionList`: assert the intersection of
      `SecurityRelevantTestFiles` and `LegacyAllowedSubstringTestFiles` is empty.
    - `TestNoNewSourceSubstringTests`: walk `backend/**/*_test.go` (via `filepath.Walk` or
      `go/parser`+`go/ast`, per RESEARCH.md's Don't-Hand-Roll recommendation — regex mirroring
      `measure-substring-tests.py`'s proven logic is acceptable for velocity, AST is the more
      rigorous discretionary alternative). For each file containing an `os.ReadFile(...".go")` call
      backing a PRESENCE-style assertion (not a pure absence check, not a migration/self-target
      file), assert its relative path is a member of `LegacyAllowedSubstringTestFiles` — fail with
      a clear message naming the offending file and line if it is not. This is the actual
      enforcement: any NEW file added outside the frozen list fails `go test ./...` immediately.
  </behavior>
  <action>
  Create `backend/internal/testquality/source_substring_guard_test.go` in package `testquality`.
  Declare `var LegacyAllowedSubstringTestFiles = []string{...}` containing the current, re-measured
  (Task 1) list of remaining non-security files still legitimately reading `.go` source — mirror
  `LEGACY_NO_RESTRICTED_SYNTAX_FILES`'s exact comment shape: measurement date, exact count, "RATCHET
  — may only shrink, entries are deleted when a file is retrofitted, nothing is ever added silently
  — a genuine new need is a reviewed decision, not a silent edit." Implement both test functions
  described in `<behavior>`.
  </action>
  <acceptance_criteria>
    - `cd backend && go test ./internal/testquality/... -v` passes, both tests PASS
    - `grep -c "internal/" backend/internal/testquality/source_substring_guard_test.go` matches the Task-1-confirmed remaining-file count
    - Manually verified: temporarily adding a fake presence-style `os.ReadFile("x.go")` +
      `strings.Contains` pair to a scratch file OUTSIDE the exception list causes
      `TestNoNewSourceSubstringTests` to fail (confirm this behavior during implementation, then
      remove the scratch file — do not leave it committed)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/testquality/... -v</automated>
  </verify>
  <done>The ratchet guard exists, is proven to catch new violations, and confirms none of the 20 security-relevant files are in its exception list.</done>
</task>

<task type="auto">
  <name>Task 3: Document the named remainder (Criterion 8)</name>
  <files>.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-SUBSTRING-TEST-REMAINDER.md</files>
  <read_first>.planning/notes/2026-09-04-messung-substring-tests.md</read_first>
  <action>
  Create `146-SUBSTRING-TEST-REMAINDER.md` in the phase directory. List every file in
  `LegacyAllowedSubstringTestFiles` (Task 2) with a per-file reason. Group by shared reasoning to
  keep it scannable, but name every file explicitly (no "etc." or glob shorthand) — e.g.:
  "Non-security-relevant per the D-08 filter rule (does not match the
  permission/authz/capability/preview/403/forbidden/effective_right/whitelist/delegation/
  role_catalog/reserved keyword filter): [list]." Also record, as a named open item (not a gap,
  per RESEARCH.md Open Question 2): `RoleCodeExistsForContext`'s sibling
  `IsHistoricalMemberRoleCode` (`hist_group_member_roles_repository.go`) was NOT expanded into
  Criterion 3's scope (D-04 locks that criterion to exactly 4 named queries) — flag it here as a
  carried-forward, not-silently-lost item for a future phase, per `145-REVIEW.md` WR-01's broader
  recommendation. State the final measured numbers (X of 53 files remain, ≤36 required) and the
  date.
  </action>
  <acceptance_criteria>
    - The file exists and lists every path in `LegacyAllowedSubstringTestFiles` by exact relative path with a reason
    - `grep -c "^-\|^  -" .planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-SUBSTRING-TEST-REMAINDER.md` is at least equal to the Task-1-confirmed remaining-file count
  </acceptance_criteria>
  <verify>
    <automated>test -f ".planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-SUBSTRING-TEST-REMAINDER.md" && echo OK</automated>
  </verify>
  <done>Every remaining file is named with a documented reason; the RoleCodeExistsForContext-sibling open item is carried forward explicitly, not lost.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Future commits -> backend test suite | Any future PR adding a new `os.ReadFile`+presence-assertion test file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-19 | Repudiation (false assurance, regression) | `backend/internal/testquality/source_substring_guard_test.go` | mitigate | Frozen, shrink-only exception list + `go test`-executed scanner fails the build the moment a new file outside the list adds the forbidden pattern — the only enforcement mechanism available since no CI/lint config exists in this repo |
| T-146-20 | Repudiation (silent debt) | `146-SUBSTRING-TEST-REMAINDER.md` | accept | The remaining ~33 non-security files keep the anti-pattern as documented, named, reasoned debt — explicitly out of this phase's scope (Criteria 5/6 target only the 20 security-relevant files) |
</threat_model>

<verification>
- `cd backend && go build ./... && go test ./...`
- `go test ./internal/testquality/... -v`
- Manual: confirm `146-SUBSTRING-TEST-REMAINDER.md` names every remaining file
</verification>

<success_criteria>
Criteria 6, 7, and 8 (ROADMAP.md, quoted in Objective above) are all true: the ≤36/53 figure is
measured and confirmed with zero security-relevant files among the remainder; a `go test`-enforced,
frozen, shrink-only guard exists; the remainder is named and reasoned per file.
</success_criteria>

<output>
Create `.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-13-SUMMARY.md` when done
</output>
