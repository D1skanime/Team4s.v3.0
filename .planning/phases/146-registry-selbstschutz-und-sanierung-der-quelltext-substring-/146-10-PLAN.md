---
phase: 146
plan: 10
type: execute
wave: 3
depends_on: ["146-01", "146-02", "146-03"]
files_modified:
  - backend/internal/handlers/public_member_access_matrix_test.go
autonomous: true
requirements: ["Criterion 5", "Criterion 6"]

must_haves:
  truths:
    - "TestPhase128PublicMemberAccessMatrix proves the Vary/no-Vary-duplication, resolvePublicMemberAccess usage, and route registration claims via real requests through the real production handlers, not source grep"
  artifacts:
    - path: "backend/internal/handlers/public_member_access_matrix_test.go"
      provides: "real httptest calls against the actual app_public_profile.go / project_member_public_handler.go handlers and a real router build proving route registration"
  key_links:
    - from: "backend/internal/handlers/public_member_access_matrix_test.go"
      to: "app_public_profile.go / project_member_public_handler.go"
      via: "real httptest.NewRequest against the production handlers (not the file's own synthetic Reference-test router)"
      pattern: "resolvePublicMemberAccess"
---

<objective>
Remediate one more of Phase 146's 20 locked security-relevant Block-2 files:
`public_member_access_matrix_test.go`.

Purpose: closes Criteria 5/6 for this file. `TestPhase128PublicMemberAccessMatrix` reads
`public_member_access.go`, `app_public_profile.go`, `project_member_public_handler.go`, and
`cmd/server/main.go`, asserting via `require.Contains`/`require.NotContains` that
`writePublicMemberUnavailable`/`resolvePublicMemberAccess` are used, that per-handler
`c.Header("Vary", ...)` is not duplicated, and that 6 specific routes are registered with
`authOptionalMiddleware` — none of these claims are proven by executing the real handlers. Its
sibling `TestPhase128PublicMemberAccessMatrixReference` (same file) is ALREADY a real, compliant
httptest-based test (a synthetic reference router, no `os.ReadFile`) — reuse its request-building
scaffolding style, but this task must exercise the REAL production handlers, not a second
reimplementation.
Output: `TestPhase128PublicMemberAccessMatrix` rewritten to fire real requests through the real
production route registrations; `TestPhase128PublicMemberAccessMatrixReference` untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-CONTEXT.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-04-PLAN.md
</context>

<interfaces>
Current state (re-read 2026-09-04, `backend/internal/handlers/public_member_access_matrix_test.go`):

`TestPhase128PublicMemberAccessMatrix` (lines 85-121): 3 read targets —
`public_member_access.go` (asserts `writepublicmemberunavailable`/`resolvepublicmemberaccess`/
`c.header("vary", "authorization")` all present, lowercased-source match), a loop over
`app_public_profile.go` + `project_member_public_handler.go` (asserts both use
`resolvepublicmemberaccess`/`writepublicmemberunavailable` but must NOT contain their own
`c.header("vary"...)` call — i.e. Vary is set exactly once, centrally, not per-handler), and
`cmd/server/main.go` (asserts 6 specific route strings with `authoptionalmiddleware`).

`TestPhase128PublicMemberAccessMatrixReference` (lines 124-171) and its helpers
`servePhase128ReferenceRequest`/`writePhase128TestUnavailable`/`phase128MatrixLabel` (lines
173-241): a REAL, compliant, already-passing httptest-based test — builds a synthetic `gin.New()`
router per test case with a locally-defined handler closure that sets `Vary`/`Cache-Control`
headers and calls a `recordingPhase128AccessResolver`. This is a "reference" simulation, NOT a
call into the real production handlers — do not confuse it with what this task must build. Reuse
its REQUEST-BUILDING helpers (route/path templating, header injection) as a style reference only.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the presence-based handler/route claims with real request proofs</name>
  <files>backend/internal/handlers/public_member_access_matrix_test.go</files>
  <read_first>backend/internal/handlers/public_member_access_matrix_test.go, backend/internal/handlers/app_public_profile.go, backend/internal/handlers/project_member_public_handler.go, backend/internal/handlers/public_member_access.go, cmd/server/main.go</read_first>
  <behavior>
    - Vary/resolver/unavailable-path claims: build the REAL production routes (via whatever
      constructor/registration function `app_public_profile.go`/`project_member_public_handler.go`
      expose — read both files fully to find it) with a fake/stub access resolver, and fire real
      `httptest.NewRequest` calls for a public-anonymous case and a private-anonymous case. Assert
      the real response's `Vary` header equals `Authorization` exactly once (not duplicated), and
      that the private case returns the real neutral-404 body produced by
      `writePublicMemberUnavailable` — proving both claims by executing the real handlers, not by
      reading their source for `c.Header("Vary"...)` occurrences.
    - Route-registration claims: build the real router via whichever function
      `cmd/server/main.go` itself calls to register these routes (read `main.go` fully to identify
      it — if routes are registered directly inline in `main.go` with no separate registration
      function, extract the minimal real call sequence needed, or fire requests against a
      `gin.New()` router where you call the exact same registration statements main.go uses),
      firing an unauthenticated request against each of the 6 routes and asserting none returns
      401 (proving "optional auth, not required auth" by execution) instead of grepping for
      `authOptionalMiddleware` in main.go's text.
    - `TestPhase128PublicMemberAccessMatrixReference`: NO CHANGE.
  </behavior>
  <action>
  Implement the rewrite described above. If the real handlers require dependencies not easily
  fakeable (e.g. a concrete repository type rather than an interface), build the smallest viable
  fake satisfying only the interface the handler actually declares — read the handler constructor
  signatures fully first rather than guessing. If, after reading `main.go`, the 6 routes are wired
  through a single shared registration function, prefer calling that function directly over
  re-deriving `main.go`'s full router; if they are registered inline with no reusable function,
  note this in the task's SUMMARY as a discretionary limitation and instead assert the routes'
  real, already-tested handler functions individually accept unauthenticated requests (each
  handler already receives `middleware.AuthIdentity` optionally per the existing optional-auth
  pattern established in `public_member_access.go`).
  </action>
  <acceptance_criteria>
    - `grep -c "os.ReadFile" backend/internal/handlers/public_member_access_matrix_test.go` returns 0
    - `cd backend && go test ./internal/handlers/... -run TestPhase128PublicMemberAccessMatrix -v` passes, 2/2 PASS (both the rewritten test and the untouched Reference test)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/handlers/... -run TestPhase128PublicMemberAccessMatrix -v</automated>
  </verify>
  <done>The Vary-header, resolver-usage, neutral-404, and route-registration claims are all proven by real requests through the real production handlers/routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Anonymous/optionally-authenticated browser -> public member profile/project endpoints | Visibility-sensitive responses that must vary correctly on Authorization and never leak private-preview data via a duplicated or missing Vary header |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-16 | Information Disclosure (cache-poisoning risk if Vary is wrong — underlying production code unchanged, already correct; only the TEST was non-executing) | `public_member_access_matrix_test.go` | mitigate | Real requests through the real production handlers now prove the Vary header and neutral-404 unavailable path, instead of asserting their presence via source grep |
</threat_model>

<verification>
- `cd backend && go build ./...`
- `go test ./internal/handlers/... -run TestPhase128PublicMemberAccessMatrix -v`
</verification>

<success_criteria>
One more of the 20 locked security-relevant files proves its Vary/resolver/route-registration
claims via real requests, not source inspection.
</success_criteria>

<output>
Create `.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-10-SUMMARY.md` when done
</output>
