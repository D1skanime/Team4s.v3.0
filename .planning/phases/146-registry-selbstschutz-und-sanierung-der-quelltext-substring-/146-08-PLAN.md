---
phase: 146
plan: 08
type: execute
wave: 3
depends_on: ["146-01", "146-02", "146-03"]
files_modified:
  - backend/internal/handlers/admin_content_fansub_notes_test.go
  - backend/internal/handlers/admin_content_release_theme_assets_test.go
  - backend/internal/handlers/admin_content_release_version_media_replace_test.go
autonomous: true
requirements: ["Criterion 5", "Criterion 6"]

must_haves:
  truths:
    - "All three content-handler test files prove their permission-gate, repository-call, and rollback claims via real httptest calls with fake repositories, not by grepping their own handler source"
  artifacts:
    - path: "backend/internal/handlers/admin_content_fansub_notes_test.go"
      provides: "real httptest proof of route-context propagation into UpdateFansubGroupNote/DeleteFansubGroupNote/UpdateMemberGroupStory/DeleteMemberGroupStory"
    - path: "backend/internal/handlers/admin_content_release_theme_assets_test.go"
      provides: "real httptest proof of InsertMediaFile call count, rollback-on-error, permission gating, and the segment-lock error code"
    - path: "backend/internal/handlers/admin_content_release_version_media_replace_test.go"
      provides: "real httptest proof that ReplaceReleaseVersionMediaFile reuses the existing update-permission gate and calls the correct repository methods"
  key_links:
    - from: "backend/internal/handlers/admin_content_release_theme_assets_test.go"
      to: "AdminContentHandler.mediaRepo"
      via: "fake mediaRepo struct literal capturing InsertMediaFile/DeleteMediaAsset call counts"
      pattern: "InsertMediaFile"
---

<objective>
Remediate three more of Phase 146's 20 locked security-relevant Block-2 files, all in
`internal/handlers`, all currently proving handler behavior (permission gates, repository call
sequencing, rollback-on-error) by reading their own handler's `.go` source.

Purpose: closes Criteria 5/6 for these three files. All three handlers already accept their
dependencies (`mediaRepo`, `themeRepo`, note/story repositories, `permissionSvc`) as struct fields
on `AdminContentHandler`, directly settable via struct literal in-package (already the pattern
`admin_content_release_version_media_replace_test.go`'s existing, compliant
`TestReplaceReleaseVersionMediaFileRejectsNoAuth` uses) — real httptest + fake-repo remediation is
mechanical, no new test infrastructure needed.
Output: all three files' presence-based source assertions replaced with real handler calls
against fake repository/permission doubles, asserting real status codes and captured call data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-CONTEXT.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-04-PLAN.md
</context>

<interfaces>
Current state (re-read 2026-09-04):

`admin_content_fansub_notes_test.go` (51 lines, both functions 100% presence-based):
`TestAdminContentFansubNotes_ProjectNoteSourceInvariants` claims (via reading
`admin_content_anime_project_notes.go`) error mapping, read-vs-write access, German error message,
and the exact `projectNoteCreditSvc.Delete(...)` call shape.
`TestAdminContentFansubNotes_ScopedWriteSourceInvariants` claims (via reading
`admin_content_fansub_group_notes.go` and `admin_content_member_stories.go`) that
`UpdateFansubGroupNote(c.Request.Context(), noteID, fansubID, identity.UserID, repoReq)`,
`DeleteFansubGroupNote(c.Request.Context(), noteID, fansubID, identity.UserID)`,
`UpdateMemberGroupStory(...)`, `DeleteMemberGroupStory(...)` all receive the fansub route context.
Handlers: `AdminContentHandler.UpdateFansubGroupNote`/`DeleteFansubGroupNote`
(`admin_content_fansub_group_notes.go:140/201`),
`UpdateMemberGroupStory`/`DeleteMemberGroupStory` (`admin_content_member_stories.go:139/200`). The
neighboring, already-compliant `TestAnimeProjectNoteDeleteDelegatesOnce`
(`admin_content_anime_project_notes_test.go`) shows the exact real-call pattern to mirror: build a
`gin.Context` via `projectNoteHandlerContext(...)`, construct `&AdminContentHandler{permissionSvc:
..., projectNoteCreditSvc: stub}`, call the handler method, assert `recorder.Code` and
`stub.deleteCalls`/`stub.lastActor`.

`admin_content_release_theme_assets_test.go` (93 lines, all 4 functions 100% presence-based, read
via `readReleaseThemeHandlerSource`/`readMediaRepoSource` helpers): claims `InsertMediaFile` exists
on `MediaRepository` and is called >=2 times across `UploadReleaseThemeAsset`/
`UploadReleaseThemeAssetForRelease`; claims `DeleteMediaAsset` rollback follows a failed
`InsertMediaFile`; claims 3 specific `permissionSvc.CanForFansubGroup`/`CanForRelease` calls exist
in each of `UploadReleaseThemeAsset`/`UploadReleaseThemeAssetForRelease`/`DeleteReleaseThemeAsset`;
claims `HasReleaseAssetSegmentUploadBlockedForRelease` is checked >=2 times and
`theme_segment_upload_anchor_required` is a stable error code. `AdminContentHandler` fields
`themeRepo`, `mediaRepo`, `mediaService`, `permissionSvc` are all struct-settable (same package).

`admin_content_release_version_media_replace_test.go` (66 lines):
`TestReplaceReleaseVersionMediaFileRequiresUpdatePermission` (lines 22-38) is 100% presence-based
(6 assertions) claiming `ReplaceReleaseVersionMediaFile` reuses `ActionReleaseVersionMediaUpdate` +
`canMutateReleaseVersionMediaRelation`, calls `h.mediaRepo.ReplaceReleaseVersionMediaFile(...)` and
`h.mediaRepo.EnqueueReleaseVersionMediaFileDeleteJob(...)`, and bumps the review lifecycle via
`NewReleaseReviewLifecycleRepository(tx).SubmitMedia(...)` — REPLACE.
`TestReplaceReleaseVersionMediaFileRejectsNoAuth` (lines 45-66) is ALREADY a real httptest call
(no `os.ReadFile`) — NO CHANGE, this is the pattern to mirror for the rewrite above.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remediate admin_content_fansub_notes_test.go</name>
  <files>backend/internal/handlers/admin_content_fansub_notes_test.go</files>
  <read_first>backend/internal/handlers/admin_content_fansub_notes_test.go, backend/internal/handlers/admin_content_fansub_group_notes.go, backend/internal/handlers/admin_content_member_stories.go, backend/internal/handlers/admin_content_anime_project_notes_test.go</read_first>
  <behavior>
    - `TestAdminContentFansubNotes_ProjectNoteSourceInvariants`: rewrite as real httptest calls
      against the read (GET) and delete note handlers, using a fake `projectNoteCreditSvc` (or
      whatever the real interface is named) that records the exact arguments it received (noteID,
      animeID/fansubID, actor AppUserID), plus a case exercising the invalid anime/fansub context
      path asserting the real German 4xx response body.
    - `TestAdminContentFansubNotes_ScopedWriteSourceInvariants`: rewrite as real httptest calls
      against `UpdateFansubGroupNote`/`DeleteFansubGroupNote`/`UpdateMemberGroupStory`/
      `DeleteMemberGroupStory`, each with a fake repository capturing the exact
      (noteID/storyID, fansubID, actorID) triple it received, asserting the captured values match
      the route params + authenticated identity — proving route-context propagation by executing
      the handler, not by grepping the call-site source.
  </behavior>
  <action>
  Implement both rewrites, mirroring `admin_content_anime_project_notes_test.go`'s
  `TestAnimeProjectNoteDeleteDelegatesOnce`/`projectNoteHandlerContext` real-call shape exactly
  (fake repository struct literal, `&AdminContentHandler{...}` construction, real handler call,
  `recorder.Code` + captured-field assertions). Read both target handler files fully first to get
  each repository interface's exact method signature before building the fakes.
  </action>
  <acceptance_criteria>
    - `grep -c "os.ReadFile" backend/internal/handlers/admin_content_fansub_notes_test.go` returns 0
    - `cd backend && go test ./internal/handlers/... -run TestAdminContentFansubNotes -v` passes, 2/2 PASS
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/handlers/... -run TestAdminContentFansubNotes -v</automated>
  </verify>
  <done>Both functions prove route-context propagation and error mapping via real handler calls with fake repositories.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remediate admin_content_release_theme_assets_test.go</name>
  <files>backend/internal/handlers/admin_content_release_theme_assets_test.go</files>
  <read_first>backend/internal/handlers/admin_content_release_theme_assets_test.go, backend/internal/handlers/admin_content_release_theme_assets.go</read_first>
  <behavior>
    - `TestReleaseThemeAsset_InsertMediaFileCalled`: real httptest call to
      `UploadReleaseThemeAsset` (and `UploadReleaseThemeAssetForRelease`) with a fake `mediaRepo`
      that counts `InsertMediaFile` invocations; assert the count is >=1 per handler call (not a
      source `strings.Count`).
    - `TestReleaseThemeAsset_InsertMediaFileRollback`: real httptest call forcing the fake
      `mediaRepo.InsertMediaFile` to return an error; assert the fake `mediaRepo`'s
      `DeleteMediaAsset` was subsequently called with the same media asset ID — a real rollback
      proof.
    - `TestReleaseThemeAsset_UsesFansubPermissionsForUploadAndDelete`: real httptest calls to all
      3 handlers (`UploadReleaseThemeAsset`, `UploadReleaseThemeAssetForRelease`,
      `DeleteReleaseThemeAsset`) with a fake `permissionSvc` that denies
      `CanForFansubGroup`/`CanForRelease` for the relevant action; assert each returns a
      403-class rejection (proving the permission check is real and load-bearing, not merely
      present in source) — plus the existing 2-item absence check (no bare `requireAdmin` gate)
      kept unchanged.
    - `TestReleaseThemeAsset_BlocksNonAnchorReleaseAssetSegmentUploads`: real httptest call with a
      fake segment-lock checker reporting "blocked"; assert the real JSON response body contains
      the `theme_segment_upload_anchor_required` error code (not a source `strings.Count`).
  </behavior>
  <action>
  Implement all four rewrites. Build one small fake `mediaRepo` struct (call-count + error-inject
  fields, mirroring `stubCapabilityAuthzRepo`'s style — no mock framework) and one fake
  `permissionSvc` reused across the 4 test functions in this file. Read
  `admin_content_release_theme_assets.go` fully first for the exact `mediaRepo`/`themeRepo`/
  `mediaService`/`permissionSvc` interface method signatures `AdminContentHandler` expects.
  </action>
  <acceptance_criteria>
    - `grep -c "os.ReadFile" backend/internal/handlers/admin_content_release_theme_assets_test.go` returns 0
    - `cd backend && go test ./internal/handlers/... -run TestReleaseThemeAsset -v` passes, 4/4 PASS
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/handlers/... -run TestReleaseThemeAsset -v</automated>
  </verify>
  <done>All 4 functions prove their claims via real handler calls against fake repository/permission doubles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remediate admin_content_release_version_media_replace_test.go</name>
  <files>backend/internal/handlers/admin_content_release_version_media_replace_test.go</files>
  <read_first>backend/internal/handlers/admin_content_release_version_media_replace_test.go, backend/internal/handlers/admin_content_release_version_media_replace.go</read_first>
  <behavior>
    - `TestReplaceReleaseVersionMediaFileRequiresUpdatePermission`: rewrite as a real httptest call
      to `ReplaceReleaseVersionMediaFile` with a fake `mediaRepo` (capturing
      `ReplaceReleaseVersionMediaFile`/`EnqueueReleaseVersionMediaFileDeleteJob` call counts/args)
      and a fake `permissionSvc`: (a) case denying `ActionReleaseVersionMediaUpdate` via
      `canMutateReleaseVersionMediaRelation` — assert a 403-class rejection, proving the SAME gate
      as PATCH/DELETE is reused (no separate `ActionReleaseVersionMediaReplace` action exists to
      even reference); (b) case allowing it — assert 200 and that the fake repo's
      `ReplaceReleaseVersionMediaFile`/`EnqueueReleaseVersionMediaFileDeleteJob` were both called
      exactly once.
    - `TestReplaceReleaseVersionMediaFileRejectsNoAuth`: NO CHANGE — already compliant.
  </behavior>
  <action>
  Implement the rewrite above, mirroring `TestReplaceReleaseVersionMediaFileRejectsNoAuth`'s
  existing real `gin.New()` + `router.PUT(...)` + `httptest.NewRequest` construction pattern in
  this same file. Read `admin_content_release_version_media_replace.go` fully first for the exact
  `mediaRepo` interface method signatures and the transaction/review-lifecycle call shape before
  building the fake.
  </action>
  <acceptance_criteria>
    - `grep -c "os.ReadFile" backend/internal/handlers/admin_content_release_version_media_replace_test.go` returns 0
    - `cd backend && go test ./internal/handlers/... -run TestReplaceReleaseVersionMediaFile -v` passes, 2/2 PASS
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/handlers/... -run TestReplaceReleaseVersionMediaFile -v</automated>
  </verify>
  <done>The permission-reuse and repository-call-sequencing claims are proven via a real handler call with a fake repository/permission double.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin browser -> content-handler mutation endpoints | Fansub-group/release-scoped permission-gated note, story, theme-asset, and media-replace mutations |
| Test suite -> false-assurance risk | Handler test files claiming permission gates and rollback behavior without ever executing the handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-14 | Repudiation (false assurance) | `admin_content_fansub_notes_test.go`, `admin_content_release_theme_assets_test.go`, `admin_content_release_version_media_replace_test.go` | mitigate | Presence-based source assertions replaced with real httptest calls against fake repository/permission doubles, proving permission gates, rollback, and call sequencing by execution |
</threat_model>

<verification>
- `cd backend && go build ./...`
- `go test ./internal/handlers/... -run "TestAdminContentFansubNotes|TestReleaseThemeAsset|TestReplaceReleaseVersionMediaFile" -v`
</verification>

<success_criteria>
Three more of the 20 locked security-relevant files prove their behavior via real handler calls,
not source inspection.
</success_criteria>

<output>
Create `.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-08-SUMMARY.md` when done
</output>
