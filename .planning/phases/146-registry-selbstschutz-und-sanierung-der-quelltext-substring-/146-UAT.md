# Phase 146 — Live-Abnahme (UAT)

**Datum:** 2026-09-04
**Abgenommen von:** Nutzer, im Browser über den Tunnel `http://127.0.0.1:3300`
**Ergebnis:** angenommen

## Geprüfte Schritte

1. **Capability-Matrix, Rolle `group_member`, alle acht Kategorien aufgeklappt.**
   Erwartet und bestätigt: genau drei Rechte, jedes mit Schloss-Symbol und dem sichtbaren
   Hinweis „Geschützt". Keine weiteren Actions.

   Dieser Schritt war der eigentliche Prüfpunkt: Das `Accordion` mountet eingeklappte
   Kategorien nicht, weshalb die Abnahme der Phase 145 nur zwei von acht Kategorien sah und
   die 34 zusätzlich gerenderten, klickbaren Switches übersah.

2. **Versuch, eines der drei Grundrechte zu entziehen.**
   Erwartet und bestätigt: eine verständliche deutsche Meldung statt eines rohen
   Serverfehlers, und das Recht bleibt gesetzt.

   Vor dieser Phase hätte derselbe Klick die Zeile aus `role_capabilities` entfernt und den
   nächsten Backend-Start in eine Absturzschleife geschickt (`log.Fatalf` in
   `cmd/server/main.go`), reparierbar nur noch von Hand an der Datenbank.

## Zusätzlich in dieser Sitzung live abgenommen

**Verlinkung im Admin-Medien-Tab** (Quick-Fix `f6a24225`): Der Button „Release-Medien öffnen"
unter Admin → Benutzer → Medien führt jetzt in den Admin-Editor
(`/admin/episode-versions/<id>/edit`) statt in den Mitwirkenden-Bereich
(`/me/releases/<id>/workspace`), der ein verifiziertes Member-Profil voraussetzt, das ein
reiner Plattform-Admin nicht hat.

## Stand

Alle acht Erfolgskriterien der Phase sind verifiziert (`146-VERIFICATION.md`, 8/8, keine
Overrides) und jetzt zusätzlich live abgenommen. Backend und Frontend laufen mit dem
gebauten Stand.
