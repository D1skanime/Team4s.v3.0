---
phase: 146
plan: 11
type: execute
wave: 3
depends_on: ["146-01", "146-02", "146-03"]
files_modified:
  - backend/internal/repository/release_version_media_repository_test.go
autonomous: true
requirements: ["Criterion 5", "Criterion 6"]

must_haves:
  truths:
    - "release_version_media_repository_test.go's 11 repository-behavior claims and 3 misplaced handler-behavior claims are proven via real Postgres/httptest calls, not by reading production .go source files"
  artifacts:
    - path: "backend/internal/repository/release_version_media_repository_test.go"
      provides: "real-Postgres and real-httptest proofs replacing all 14 os.ReadFile call sites across 14 functions; pure compile-time signature checks (TestMediaRepositoryMethodSignatures, TestRVMCleanupRepositoryMutationMethodSignatures) remain untouched"
  key_links:
    - from: "backend/internal/repository/release_version_media_repository_test.go"
      to: "ReleaseVersionMediaRepository / MediaRepository"
      via: "real Postgres calls against seeded release_version_media / review-lifecycle rows"
      pattern: "release_version_media_review_lifecycle"
---

<objective>
Remediate the largest of Phase 146's 20 locked security-relevant Block-2 files:
`release_version_media_repository_test.go` (356 lines, 18 test functions, 14 of which use
`os.ReadFile` to claim repository/handler behavior from source text instead of executing it).

Purpose: closes Criteria 5/6 for this single file. Per RESEARCH.md's sampled evidence, this file's
anti-pattern is representative of the whole Block-2 remediation shape: presence-only source
assertions claiming SQL joins, transactional contracts, soft-delete filtering, and ownership
validation exist, none of which are exercised. 4 functions in this file
(`TestReleaseVersionMediaTypes`, `TestMediaRepositoryMethodSignatures`,
`TestRVMCleanupRepositoryMutationMethodSignatures`, and any other pure `var x *T; _ =
x.MethodName` compile-time existence check) do NOT use `os.ReadFile` and are explicitly NOT the
forbidden pattern (CLAUDE.md's Teststil rule targets source-substring BEHAVIORAL claims, not
method-existence checks) — leave these untouched.
Output: all 14 `os.ReadFile`-based functions rewritten as real Postgres/httptest calls against the
actual repository/handler methods they claim to test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-CONTEXT.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-04-PLAN.md
@.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-PATTERNS.md
</context>

<interfaces>
Current state (re-read 2026-09-04, `backend/internal/repository/release_version_media_repository_test.go`,
356 lines): the 14 functions using `os.ReadFile` (production file each reads, in parentheses) are:
`TestReleaseVersionMedia_ListIncludesOwnReviewLifecycle` (line 62, reads
`release_version_media_repository.go`, claims `release_version_media_review_lifecycle`/
`lifecycle.source_revision` SQL fragments exist);
`TestReleaseVersionMedia_UploadTransactionContract` (line 130, same production file);
`TestReleaseVersionMedia_SoftDeleteQueryExcludesDeletedRows` (line 151, same);
`TestReleaseVersionMedia_ReorderOwnershipValidationExists` (line 176, same);
`TestReleaseVersionMedia_ContributorGroupOwnershipResolverExists` (line 193, same);
`TestReleaseVersionMedia_CategoryChangeAllowed` (line 207, same);
`TestReleaseVersionMedia_PreviewEnforcementInRepository` (line 221, same);
`TestReleaseVersionMedia_PartialFailureIsolation` (line 231, reads
`../handlers/admin_content_release_version_media.go`);
`TestReleaseVersionMedia_HardDeleteTransactional` (line 250, reads
`release_version_media_cleanup.go`);
`TestReleaseVersionMedia_CleanupServicePassesExist` (line 267, reads
`../services/release_version_media_cleanup.go`);
`TestCreateMediaAsset_SubSelectVisibilityOnInput` (line 290, reads
`../repository/media_repository.go`);
`TestFansubMediaUploadHandler_BrandingDefaults` (line 306, reads
`../handlers/fansub_media_upload.go` — a handler-behavior claim misplaced in this repository test
file);
`TestRVMHandler_ProzessmedienDefaults` (line 328, reads
`../handlers/admin_content_release_version_media.go`, also handler-behavior);
`TestMemberMediaHandler_LockI_OwnerFromSession` (line 342, reads
`../handlers/member_media_upload.go`, also handler-behavior).
`testsupport.OpenPhase145Postgres` covers `role_definitions`/`role_capabilities` only — this
package's release-media schema is a DIFFERENT set of tables; grep this same file and its package
neighbors for an existing `testsupport.Open*Postgres` fixture that already provisions
`release_version_media`/review-lifecycle tables before assuming which one to reuse.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remediate the repository-layer read/query claims (5 functions)</name>
  <files>backend/internal/repository/release_version_media_repository_test.go</files>
  <read_first>backend/internal/repository/release_version_media_repository_test.go, backend/internal/repository/release_version_media_repository.go</read_first>
  <behavior>
    - `TestReleaseVersionMedia_ListIncludesOwnReviewLifecycle`: seed a `release_version_media` row
      plus its `release_version_media_review_lifecycle` row via real Postgres; call the real list
      method; assert the returned struct's review-lifecycle-derived field (e.g. `SourceRevision`)
      matches the seeded value.
    - `TestReleaseVersionMedia_UploadTransactionContract`: seed the preconditions for an upload
      write; call the real repository upload/insert method inside a transaction, force a
      mid-transaction failure path if the claim concerns atomicity, and assert no partial row
      exists afterward — a real transactional proof.
    - `TestReleaseVersionMedia_SoftDeleteQueryExcludesDeletedRows`: seed one soft-deleted and one
      live row; call the real list/query method; assert only the live row is returned.
    - `TestReleaseVersionMedia_ReorderOwnershipValidationExists`: call the real reorder method with
      a media row belonging to a different release version than the one in the request; assert a
      real ownership-validation error/rejection is returned (not merely "the validation function
      exists" per a source grep).
    - `TestReleaseVersionMedia_ContributorGroupOwnershipResolverExists`: call the real
      contributor-group ownership resolution path with a seeded row belonging to a specific group;
      assert the resolved group ID matches — a real resolution proof.
  </behavior>
  <action>
  Implement all 5 rewrites as real Postgres calls against the actual repository methods named in
  each function today. First identify (via grep on this same package, or on
  `testsupport/*.go`) an existing fixture already covering the `release_version_media`/review-
  lifecycle schema; reuse it. Read `release_version_media_repository.go` fully first for every
  method's exact signature before writing seeds/assertions — do not guess field or method names.
  </action>
  <acceptance_criteria>
    - `grep -c "os.ReadFile" backend/internal/repository/release_version_media_repository_test.go` is at most 9 after this task (5 of the original 14 removed)
    - `go test ./backend/internal/repository/... -run "TestReleaseVersionMedia_ListIncludesOwnReviewLifecycle|TestReleaseVersionMedia_UploadTransactionContract|TestReleaseVersionMedia_SoftDeleteQueryExcludesDeletedRows|TestReleaseVersionMedia_ReorderOwnershipValidationExists|TestReleaseVersionMedia_ContributorGroupOwnershipResolverExists" -v` passes, 5/5 PASS, no SKIP
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/repository/... -run "TestReleaseVersionMedia_ListIncludesOwnReviewLifecycle|TestReleaseVersionMedia_UploadTransactionContract|TestReleaseVersionMedia_SoftDeleteQueryExcludesDeletedRows|TestReleaseVersionMedia_ReorderOwnershipValidationExists|TestReleaseVersionMedia_ContributorGroupOwnershipResolverExists" -v</automated>
  </verify>
  <done>5 repository read/query claims are proven via real Postgres calls against seeded rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remediate the repository-layer mutation/service claims (6 functions)</name>
  <files>backend/internal/repository/release_version_media_repository_test.go</files>
  <read_first>backend/internal/repository/release_version_media_repository_test.go, backend/internal/repository/release_version_media_repository.go, backend/internal/repository/release_version_media_cleanup.go, backend/internal/services/release_version_media_cleanup.go, backend/internal/repository/media_repository.go</read_first>
  <behavior>
    - `TestReleaseVersionMedia_CategoryChangeAllowed`: real call changing a seeded row's category;
      assert the persisted row's category matches.
    - `TestReleaseVersionMedia_PreviewEnforcementInRepository`: real call attempting to mutate/read
      a preview-restricted row under conditions that should be rejected; assert the real rejection.
    - `TestReleaseVersionMedia_PartialFailureIsolation`: real call exercising a multi-item
      operation where one item fails; assert the successful items persisted and the failed one did
      not, via real Postgres state after the call (not a source claim about isolation).
    - `TestReleaseVersionMedia_HardDeleteTransactional`: real call to the hard-delete path; assert
      the row and its dependent rows are gone atomically (no partial delete state).
    - `TestReleaseVersionMedia_CleanupServicePassesExist`: real call into the cleanup service
      (`release_version_media_cleanup.go` service, not just the repository) proving it actually
      invokes the repository's cleanup method against seeded stale rows — assert the rows are
      gone/marked afterward.
    - `TestCreateMediaAsset_SubSelectVisibilityOnInput`: real call to `MediaRepository`'s
      `CreateMediaAsset` with a visibility-relevant input; assert the persisted row's visibility
      matches the expected sub-select-derived value.
  </behavior>
  <action>
  Implement all rewrites as real Postgres calls. Read each target production file fully first for
  exact method signatures. Reuse the fixture identified in Task 1 (do not create a second one).
  </action>
  <acceptance_criteria>
    - `grep -c "os.ReadFile" backend/internal/repository/release_version_media_repository_test.go` is at most 3 after this task
    - `go test ./backend/internal/repository/... -run "TestReleaseVersionMedia_CategoryChangeAllowed|TestReleaseVersionMedia_PreviewEnforcementInRepository|TestReleaseVersionMedia_PartialFailureIsolation|TestReleaseVersionMedia_HardDeleteTransactional|TestReleaseVersionMedia_CleanupServicePassesExist|TestCreateMediaAsset_SubSelectVisibilityOnInput" -v` passes, 6/6 PASS, no SKIP
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/repository/... -run "TestReleaseVersionMedia_CategoryChangeAllowed|TestReleaseVersionMedia_PreviewEnforcementInRepository|TestReleaseVersionMedia_PartialFailureIsolation|TestReleaseVersionMedia_HardDeleteTransactional|TestReleaseVersionMedia_CleanupServicePassesExist|TestCreateMediaAsset_SubSelectVisibilityOnInput" -v</automated>
  </verify>
  <done>6 repository mutation/service claims are proven via real Postgres calls against seeded rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remediate the 3 misplaced handler-behavior claims via real httptest calls</name>
  <files>backend/internal/repository/release_version_media_repository_test.go</files>
  <read_first>backend/internal/repository/release_version_media_repository_test.go, backend/internal/handlers/fansub_media_upload.go, backend/internal/handlers/admin_content_release_version_media.go, backend/internal/handlers/member_media_upload.go</read_first>
  <behavior>
    - `TestFansubMediaUploadHandler_BrandingDefaults`: real httptest call to the actual
      `fansub_media_upload.go` handler with an upload request omitting branding fields; assert the
      real persisted/returned media asset carries the expected default branding values.
    - `TestRVMHandler_ProzessmedienDefaults`: real httptest call to the actual
      `admin_content_release_version_media.go` handler; assert the real default "Prozessmedien"
      category/flag behavior in the response.
    - `TestMemberMediaHandler_LockI_OwnerFromSession`: real httptest call to the actual
      `member_media_upload.go` handler with a session-derived owner identity; assert the real
      persisted media asset's owner matches the session identity, not a request-supplied value
      (proving the "lock owner from session" claim by execution).
    These 3 functions test HANDLER behavior, not repository behavior — they remain in this file
    (do not relocate to a handlers package test file; out of this task's scope, avoid unrelated
    file churn) but must call the real handlers via `httptest`, with a fake repository double for
    each, mirroring the `stubCapabilityAuthzRepo`-style pattern already established elsewhere in
    this codebase.
  </behavior>
  <action>
  Implement all 3 rewrites as real httptest calls against the real handler functions, each with a
  minimal fake repository double satisfying only the interface the handler declares. Read all 3
  target handler files fully first for exact request/response shapes and repository interfaces.
  </action>
  <acceptance_criteria>
    - `grep -c "os.ReadFile" backend/internal/repository/release_version_media_repository_test.go` returns 0
    - `go test ./backend/internal/repository/... -run "TestFansubMediaUploadHandler_BrandingDefaults|TestRVMHandler_ProzessmedienDefaults|TestMemberMediaHandler_LockI_OwnerFromSession" -v` passes, 3/3 PASS
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/repository/... -run "TestFansubMediaUploadHandler_BrandingDefaults|TestRVMHandler_ProzessmedienDefaults|TestMemberMediaHandler_LockI_OwnerFromSession" -v</automated>
  </verify>
  <done>All 14 os.ReadFile call sites in this file are gone; the 3 handler-behavior claims are proven via real httptest calls; the pure compile-time signature-check functions remain untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Release-version-media upload/mutation/cleanup pipeline | Contributor/leader-scoped media upload, category change, soft/hard delete, review-lifecycle, and cleanup-service paths |
| Test suite -> false-assurance risk | 14 functions claiming transactional, ownership, and default-value behavior without ever executing the repository or handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-146-17 | Repudiation (false assurance) | `release_version_media_repository_test.go` | mitigate | All 14 presence-based source assertions replaced with real Postgres/httptest calls proving the actual transactional, ownership, soft-delete, and default-value behavior |
</threat_model>

<verification>
- `cd backend && go build ./...`
- `go test ./internal/repository/... -run TestReleaseVersionMedia -v`
- `go test ./internal/repository/... -run "TestFansubMediaUploadHandler_BrandingDefaults|TestRVMHandler_ProzessmedienDefaults|TestMemberMediaHandler_LockI_OwnerFromSession|TestCreateMediaAsset_SubSelectVisibilityOnInput" -v`
</verification>

<success_criteria>
All 14 os.ReadFile-based functions in this file are replaced with real, executed proofs;
compile-time signature checks are untouched.
</success_criteria>

<output>
Create `.planning/phases/146-registry-selbstschutz-und-sanierung-der-quelltext-substring-/146-11-SUMMARY.md` when done
</output>
