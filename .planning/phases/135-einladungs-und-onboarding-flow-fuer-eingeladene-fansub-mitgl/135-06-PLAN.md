---
phase: 135-einladungs-und-onboarding-flow-fuer-eingeladene-fansub-mitgl
plan: 06
type: execute
wave: 3
depends_on: ["135-05"]
files_modified:
  - frontend/src/app/claim-invitations/accept/page.tsx
  - frontend/src/app/claim-invitations/accept/page.test.tsx
autonomous: true
requirements: [D-09, D-07]

must_haves:
  truths:
    - "A cold, logged-out user following a claim-invite link now completes the same dual Anmelden/Registrieren + auto-return flow as an app-member invite, instead of the previously broken return_to redirect."
    - "claim-invitations/accept keeps its existing auto-redirect-to-/me/profile behavior on success."
  artifacts:
    - path: "frontend/src/app/claim-invitations/accept/page.tsx"
      provides: "D-09 shared-flow consumer #2"
  key_links:
    - from: "frontend/src/app/claim-invitations/accept/page.tsx"
      to: "frontend/src/components/auth/InviteAcceptFlow.tsx"
      via: "<InviteAcceptFlow ... /> composition"
      pattern: "InviteAcceptFlow"
---

<objective>
Complete D-09 by making `/claim-invitations/accept/page.tsx` the second consumer of the
`InviteAcceptFlow` component built in `135-05`. This also closes Pitfall 1/5's shared root cause
for this page specifically: its current `<Link href="/login?return_to=...">` never worked (wrong
param name, and any param is dropped across the Keycloak redirect regardless of name) -- the shared
component now routes through `135-01`'s validated `returnPath` sessionStorage mechanism instead.

Purpose: A claim-invite recipient without an account currently hits the exact same class of dead
end as the BLOCKER app-invite case; D-09 requires both invite types to share one onboarding flow.
Output: `claim-invitations/accept/page.tsx` becomes a thin composition of `InviteAcceptFlow`,
identical in shape to `invitations/accept/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/135-einladungs-und-onboarding-flow-fuer-eingeladene-fansub-mitgl/135-CONTEXT.md
@.planning/phases/135-einladungs-und-onboarding-flow-fuer-eingeladene-fansub-mitgl/135-RESEARCH.md
@.planning/phases/135-einladungs-und-onboarding-flow-fuer-eingeladene-fansub-mitgl/135-05-PLAN.md
</context>

<interfaces>
<!-- InviteAcceptFlow.tsx's props contract (built in 135-05) this page composes against: -->

```typescript
export type InviteAcceptFlowProps = {
  token: string
  title: string
  description: string
  loginPromptText: string
  loginHintEmail?: string
  onAccept: (token: string) => Promise<void>
  mapError: (error: unknown) => string
  successMessage: string
  afterAcceptRedirect?: string
  missingTokenText: string
}
```

<!-- acceptClaimInvitation signature, frontend/src/lib/api.ts lines 4063-4076: -->

```typescript
export async function acceptClaimInvitation(payload: { token: string }, authToken?: string): Promise<void>
```

<!-- Current claimInvitationErrorMessage to KEEP unchanged (frontend/src/app/claim-invitations/accept/page.tsx lines 10-26) -- already a correct friendly-mapping precedent covering invitation_expired/invitation_used/invitation_cancelled/already_verified. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite claim-invitations/accept/page.tsx onto the shared InviteAcceptFlow</name>
  <files>frontend/src/app/claim-invitations/accept/page.tsx</files>
  <read_first>
    frontend/src/app/claim-invitations/accept/page.tsx (current, full file, 91 lines -- to be replaced)
    frontend/src/components/auth/InviteAcceptFlow.tsx (post-135-05)
    frontend/src/lib/api.ts (acceptClaimInvitation signature, lines 4063-4076)
    frontend/src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.tsx (line ~322, confirms claim invitations carry no target email -- "teile sie direkt, zum Beispiel via Discord" -- so no loginHintEmail applies here, unlike invitations/accept)
  </read_first>
  <action>
    Rewrite `frontend/src/app/claim-invitations/accept/page.tsx`: keep the existing
    `claimInvitationErrorMessage` function (lines 10-26) exactly as-is (already correct). Replace
    the fallback `<main style={{...}}>Einladung wird geladen...</main>` with
    `<LoadingState title="Einladung wird geladen" />` (`@/components/ui`, D-07). Replace the entire
    `AcceptClaimInvitationContent` body with `<InviteAcceptFlow>` composition: `token` (unchanged
    trim/`useMemo` read), `title="Member-Claim-Einladung annehmen"`,
    `description="Verbinde deinen Team4s-Account mit einem historischen Fansub-Member-Eintrag."`,
    `loginPromptText="Bitte melde dich an oder erstelle einen Account. Danach kommst du automatisch
    zurueck."` (correct Umlaut: "zurück"), no `loginHintEmail` prop (claim invitations are generic
    shareable links with no target email, per `ClaimManagementPanel.tsx`'s own description),
    `onAccept={(tok) => acceptClaimInvitation({ token: tok })}`,
    `mapError={claimInvitationErrorMessage}`, `successMessage="Dein Account ist jetzt als Mitglied
    verifiziert. Dein Profil ist ab sofort oeffentlich indexierbar."` (correct Umlaut:
    "öffentlich"), `afterAcceptRedirect="/me/profile"` (preserves the existing immediate-redirect
    behavior), `missingTokenText="Im Link fehlt ein gueltiges Einladungs-Token."` (correct Umlaut:
    "gültiges"). Remove the now-unused local `useState`/`useRouter`/`useAuthSession` plumbing and
    the raw `<button>`/inline `style={{}}` markup entirely -- this closes Pitfall 1/5 (the
    `return_to`/`next` param mismatch and its loss across the Keycloak redirect) because the shared
    component now uses `beginKeycloakLogin({returnPath})` + `135-01`'s `consumeStoredReturnPath()`
    instead of the old `<Link href="/login?return_to=...">`.
  </action>
  <acceptance_criteria>
    - `grep -c 'return_to' frontend/src/app/claim-invitations/accept/page.tsx` returns 0
    - `grep -c '<button\|style={{' frontend/src/app/claim-invitations/accept/page.tsx` returns 0
    - `docker compose exec -T team4sv30-frontend npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend npx tsc --noEmit</automated>
  </verify>
  <done>claim-invitations/accept/page.tsx composes InviteAcceptFlow, contains no return_to/raw-button/inline-style remnants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Light page-level test for claim-invitations/accept</name>
  <files>frontend/src/app/claim-invitations/accept/page.test.tsx</files>
  <read_first>
    frontend/src/app/claim-invitations/accept/page.tsx (post-Task-1)
    frontend/src/app/login/page.test.tsx (mocking shape for @/lib/keycloakAuth, @/lib/useAuthSession, next/navigation)
  </read_first>
  <behavior>
    - Test: the rendered page shows the title "Member-Claim-Einladung annehmen".
    - Test: logged out, clicking "Anmelden" calls the mocked `beginKeycloakLogin` with an object whose `returnPath` contains the page's current token query string.
    - Test: logged in, triggering accept calls the mocked `acceptClaimInvitation` with `{token}` and, on success, `router.replace` is called with `/me/profile`.
  </behavior>
  <action>
    Create `frontend/src/app/claim-invitations/accept/page.test.tsx` mocking `@/lib/api`
    (`acceptClaimInvitation`, `ApiError`), `@/lib/keycloakAuth` (`beginKeycloakLogin`),
    `@/lib/useAuthSession` (`useAuthSession`), and `next/navigation` (`useSearchParams`,
    `useRouter`), implementing the three cases from `<behavior>` -- mirror
    `frontend/src/components/auth/InviteAcceptFlow.test.tsx`'s mocking shape (from `135-05`) since
    this page now renders the real `InviteAcceptFlow`.
  </action>
  <acceptance_criteria>
    - All 3 test cases pass
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend npm test -- --run src/app/claim-invitations/accept/page.test.tsx</automated>
  </verify>
  <done>New test file passes with 3 cases covering title, Anmelden returnPath, and accept+redirect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| claim-invitations/accept's returnPath, now routed through the shared `135-01` mechanism | Same as `135-01`/`135-05` -- no new boundary introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-06-01 | Tampering | claim-invitations/accept's returnPath | mitigate | Inherits `135-01`'s same-origin validation; closes the previously-unvalidated `?return_to=` URL param path entirely (it no longer exists in this file). |
</threat_model>

<verification>
`npx tsc --noEmit` and the new page test file (3 cases) both pass.
</verification>

<success_criteria>
Both invite types (`/invitations/accept`, `/claim-invitations/accept`) now share one working,
tested onboarding flow -- D-09 fully closed.
</success_criteria>

<output>
Create `.planning/phases/135-einladungs-und-onboarding-flow-fuer-eingeladene-fansub-mitgl/135-06-SUMMARY.md` when done
</output>
