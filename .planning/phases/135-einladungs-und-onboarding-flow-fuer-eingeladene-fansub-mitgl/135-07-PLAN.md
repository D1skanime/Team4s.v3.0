---
phase: 135-einladungs-und-onboarding-flow-fuer-eingeladene-fansub-mitgl
plan: 07
type: execute
wave: 5
depends_on: ["135-01", "135-02", "135-03", "135-04", "135-05", "135-06", "135-08"]
files_modified:
  - scripts/phase135-green-gate.sh
autonomous: false
requirements: [D-02, D-01, D-03, D-04, D-08, D-09]

must_haves:
  truths:
    - "A full automated gate (backend build/vet/test, frontend typecheck/lint/test/build, git diff --check) is green, or every failure is explicitly named as pre-existing and out of this phase's scope."
    - "Keycloak's team4s realm has registrationAllowed actually ON in the live, running instance (not just the tracked config file)."
    - "A real, cold, logged-out invitee completes an app-member invite round trip end-to-end (mail -> Registrieren -> auto-return -> auto-accept) with correct German copy and Umlaute throughout."
    - "A real, cold, logged-out claimant completes a claim-invite round trip end-to-end through the same shared flow."
  artifacts:
    - path: "scripts/phase135-green-gate.sh"
      provides: "One CI-style orchestration script for the phase's full automated suite"
  key_links:
    - from: "scripts/phase135-green-gate.sh"
      to: "backend/internal/handlers/app_auth.go, frontend/src/components/auth/InviteAcceptFlow.tsx"
      via: "go test ./... / npm test coverage of every plan's changed files"
      pattern: "PASS|FAIL"
---

<objective>
Run the phase's full automated gate and perform the live, human-verified UAT this phase's own
CONTEXT.md constraint requires ("Verifikation live auf :3000 mit echten Daten UND Mailpit :8025 --
Code-Level reicht nicht"). Confirms D-01 through D-09 all hold together in the real running stack,
not just in isolated unit/component tests.

Purpose: Automated tests prove each piece in isolation; this plan proves the end-to-end cold-invite
BLOCKER (Finding #10) is actually closed for a real user in a real browser against real Keycloak and
Mailpit instances.
Output: `scripts/phase135-green-gate.sh`; two completed human-verify checkpoints; the phase's final
sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/135-einladungs-und-onboarding-flow-fuer-eingeladene-fansub-mitgl/135-CONTEXT.md
@.planning/phases/135-einladungs-und-onboarding-flow-fuer-eingeladene-fansub-mitgl/135-RESEARCH.md
@.planning/phases/135-einladungs-und-onboarding-flow-fuer-eingeladene-fansub-mitgl/135-VALIDATION.md
</context>

<interfaces>
<!-- Precedent script shape from the prior phase's own green-gate script, .planning/phases/134-fixture-backed-verification-rollout/134-04-PLAN.md
     (scoped differently -- Phase 134 scoped to the member-profile surface; this phase follows
     135-VALIDATION.md's own "Full suite command" definition instead, which is the FULL unscoped
     suite, not a file-scoped subset): -->

```
scripts/phase134-green-gate.sh orchestrates go build ./..., go vet ./..., a scoped go test run,
npm run typecheck, npm run lint, a scoped vitest run, npm run build, and a repo-wide
git diff --check -- printing a single PASS/FAIL summary and a named KNOWN-DEFERRED vs
NEW-FAILURES partition.
```

<!-- 135-VALIDATION.md's own Full suite command definition to follow: -->

```
Full suite command: npm --prefix frontend run test  AND  go test ./... (from backend/)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build and run the full automated gate script</name>
  <files>scripts/phase135-green-gate.sh</files>
  <read_first>
    .planning/phases/134-fixture-backed-verification-rollout/134-04-PLAN.md (script shape/output-format precedent -- not literal reuse, Phase 134's file scope differs)
    .planning/phases/135-einladungs-und-onboarding-flow-fuer-eingeladene-fansub-mitgl/135-VALIDATION.md (Full suite command definition)
    frontend/package.json (typecheck/lint/test/build script names)
  </read_first>
  <action>
    Create `scripts/phase135-green-gate.sh` (bash, executable) that runs, in order, printing a
    `PASS`/`FAIL` line per step and a final single-line summary: (1)
    `docker compose exec -T team4sv30-backend go build ./...`; (2)
    `docker compose exec -T team4sv30-backend go vet ./...`; (3)
    `docker compose exec -T team4sv30-backend go test ./...` (full backend suite, per
    `135-VALIDATION.md`'s "Full suite command"); (4)
    `docker compose exec -T team4sv30-frontend npm run typecheck`; (5)
    `docker compose exec -T team4sv30-frontend npm run lint`; (6)
    `docker compose exec -T team4sv30-frontend npm test` (full frontend suite); (7)
    `docker compose exec -T team4sv30-frontend npm run build`; (8) `git diff --check`. The script
    must exit non-zero if any step fails, and must print exactly which step(s) failed by name in
    its final summary line. Run the script. Fix any regression it surfaces that is attributable to
    files this phase's own plans (`135-01` through `135-06`) touched. Pre-existing failures in files
    NOT touched by any Phase 135 plan (cross-check against each plan's `files_modified` list) are
    out of scope to fix here but MUST be named explicitly in the script's summary output, not
    silently ignored -- mirrors Phase 134's `KNOWN_DEFERRED` precedent.
  </action>
  <acceptance_criteria>
    - `scripts/phase135-green-gate.sh` exists, is executable, and runs all 8 steps in order
    - The script exits 0, or exits non-zero only for explicitly named, pre-existing, out-of-phase-scope failures
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase135-green-gate.sh</automated>
  </verify>
  <done>Full automated gate is green, or every non-green step is a named, pre-existing, out-of-scope exception.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-02 live Keycloak registrationAllowed check</name>
  <files>none (manual verification only, no file changes expected)</files>
  <action>
    Present the how-to-verify steps below to the user and wait for explicit confirmation (or a
    description of drift found) before proceeding to Task 3.
  </action>
  <what-built>D-02's decision is confirmed correct at the source-code level -- `infra/keycloak/realm-team4s.json` already has `registrationAllowed: true`. No code change was needed for D-02 itself; this checkpoint verifies the LIVE, running Keycloak instance actually matches the tracked file (Assumption A2 from 135-RESEARCH.md).</what-built>
  <how-to-verify>
    1. Open the Keycloak admin console (check `docker-compose.yml`/`.env` for the mapped port; typically reachable from the Windows SSH tunnel per CLAUDE.md's `http://127.0.0.1:3300` convention, or directly at `http://192.168.235.196:<keycloak-port>/admin/` on the Linux host).
    2. Log in as the Keycloak realm admin.
    3. Navigate to Realm settings for the `team4s` realm -> Login tab.
    4. Confirm "User registration" is ON.
    5. If it is OFF (drifted from the tracked `infra/keycloak/realm-team4s.json`), re-import the realm config or toggle it ON, and record the drift in this plan's SUMMARY.md.
  </how-to-verify>
  <resume-signal>Type "registrationAllowed confirmed ON" or describe the drift found and how it was resolved.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Cold-invite end-to-end round trip + mail content sign-off</name>
  <files>none (manual verification only, no file changes expected)</files>
  <action>
    Present the how-to-verify steps below to the user and wait for explicit "cold-invite round trip
    confirmed" (or a description of blocking issues/deviations) before the phase can be considered
    complete.
  </action>
  <what-built>The full cold-invite path: a context-rich invite mail (135-03), the shared dual Anmelden/Registrieren accept flow with persisted returnTo and auto-accept (135-01/135-05), and the same shared flow serving the claim-invite path too (135-06).</what-built>
  <how-to-verify>
    1. As an existing fansub group admin on `http://127.0.0.1:3300` (or `http://192.168.235.196:3000` per CLAUDE.md), create a new app-member invitation for a fresh test email address (e.g. `cold-invite-test@example.local`) not tied to any existing account.
    2. Open Mailpit at `:8025` and confirm the received mail: subject/body name the actual fansub group, the inviting admin's display name, one "Team4s ist..." sentence, and correct Umlaute (ä/ö/ü/Ä/Ö/Ü/ß, no ASCII substitutes, no mojibake such as "Ã¼").
    3. In a private/incognito browser tab (to guarantee a cold, logged-out session), open the mail's accept link.
    4. Confirm both "Anmelden" and "Registrieren" options are shown, with no Keycloak/architecture jargon anywhere in the visible copy.
    5. Click "Registrieren" and complete Keycloak self-registration. Note whether the email field was pre-filled with the invited address (confirms RESEARCH.md Assumption A1) or not (confirms only the server-side email_match fallback is active) -- either outcome is acceptable, but record which one occurred.
    6. Confirm the browser lands back on the original accept page automatically (not `/me/profile`) and the invitation auto-accepts without a second manual click, showing a friendly confirmation (no raw backend error text).
    7. Repeat steps 3-6 for a claim-invitation link generated via the now-wired `HistoricalMemberCard` button (135-04), against a second fresh test account, confirming it lands on `/me/profile` after acceptance per its existing redirect behavior.
  </how-to-verify>
  <resume-signal>Type "cold-invite round trip confirmed" plus any deviations found (e.g. "email prefill did not occur, relied on server-side rejection instead"), or describe blocking issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local dev-only orchestration script | No production exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-07-01 | Tampering | `scripts/phase135-green-gate.sh` | accept | Local dev-only orchestration script, matches Phase 134's `phase134-green-gate.sh` precedent; not part of any deployed surface. |
</threat_model>

<verification>
`bash scripts/phase135-green-gate.sh` exits 0 (or documents only pre-existing named exceptions);
both human-verify checkpoints explicitly confirmed.
</verification>

<success_criteria>
All of D-01 through D-09 are implemented and live-verified per CONTEXT.md; the full automated
suite is green; the cold-invite BLOCKER (Finding #10) is closed end-to-end for a real user.
</success_criteria>

<output>
Create `.planning/phases/135-einladungs-und-onboarding-flow-fuer-eingeladene-fansub-mitgl/135-07-SUMMARY.md` when done
</output>
