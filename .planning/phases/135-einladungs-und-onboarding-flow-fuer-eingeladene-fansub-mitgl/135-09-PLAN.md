---
phase: 135-einladungs-und-onboarding-flow-fuer-eingeladene-fansub-mitgl
plan: 09
type: execute
wave: 4
depends_on: ["135-08"]
files_modified:
  - infra/keycloak/realm-team4s.json
  - infra/keycloak/themes/team4s/login/register.ftl
  - infra/keycloak/themes/team4s/login/messages/messages_de.properties
autonomous: false
requirements: [D-14]

must_haves:
  truths:
    - "The Keycloak registration form collects a Fansubname as the primary identity field; first/last name are optional (not required)."
    - "A user can sign in with EITHER their email OR their Fansubname (Fansubname = KC username, loginWithEmailAllowed stays on)."
    - "Keycloak is authoritative for the Fansubname; it is carried in the OIDC token and seeds/updates Team4s members.nickname/fansub_name via JIT sync."
    - "The Fansubname is editable, with a single defined write-direction (KC master), and stays consistent with Team4s without dual-source drift."
  artifacts:
    - path: "infra/keycloak/realm-team4s.json"
      provides: "D-14 user-profile config (fansubname required, first/last optional) + token mapper"
  key_links:
    - from: "Keycloak OIDC token (fansubname claim)"
      to: "backend JIT identity sync -> members.nickname/fansub_name"
      via: "protocol mapper + AuthzRepository JIT seed"
      pattern: "fansub"
---

<objective>
Umbau der KC-Registrierung (D-14, Nutzer-bestaetigt): primaeres Feld ist der **Fansubname**, Vor-/
Nachname werden optional. Keycloak haelt den Fansubname autoritativ; er landet im Token und seedet
Team4s members.nickname/fansub_name. Motivation: Nutzer identifizieren sich ueber den Fansubname
(Login-Verwirrung "D1sk" vs. E-Mail); First/Last-Name verwirren und sind fachlich unnoetig.

REALITY / SCOPE (KC v26, Realm team4s -- an laufender Instanz 18081 pruefen, NICHT annehmen):
- KC 26 nutzt **Declarative User Profile**. First/Last-Name required-Status + neue Attribute werden
  dort konfiguriert (realm-team4s.json userProfile-Block bzw. Admin-API).
- KERN-ENTSCHEIDUNG (Task 1, an KC verifizieren): Wie wird der Fansubname zum Login-Identifier?
  Ziel steht (D-14-Praezisierung): Login mit E-Mail UND Fansubname -> Fansubname = KC **username**,
  loginWithEmailAllowed bleibt an. Task 1 klaert nur noch die Constraints (unique, erlaubter
  Zeichensatz, editUsername=ON fuer spaeteres Aendern, Kollision mit bestehenden usernames/E-Mails)
  und die Schreibrichtung -- nicht mehr das Ob. Ergebnis dokumentieren.
- Doppelquelle vermeiden: EINE Schreibrichtung (KC -> Team4s). Team4s-Profil-Editieren von
  fansub_name muss dann entweder nach KC zurueckschreiben ODER read-only-aus-KC sein -- in Task 1
  entscheiden + in der SUMMARY festhalten.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/135-einladungs-und-onboarding-flow-fuer-eingeladene-fansub-mitgl/135-CONTEXT.md
@.planning/phases/135-einladungs-und-onboarding-flow-fuer-eingeladene-fansub-mitgl/135-08-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: KC-26-User-Profile-Mechanik + Login-Identity klaeren (an laufender KC)</name>
  <files>none (Untersuchung/Entscheid; Umsetzung ab Task 2)</files>
  <action>
    An der laufenden KC (Realm team4s, http://127.0.0.1:18081, Admin-API) klaeren und dokumentieren:
    1. Declarative-User-Profile-Config des Realms lesen (Admin-API GET /admin/realms/team4s + users
       profile). Feststellen, wie firstName/lastName als required markiert sind.
    2. Entscheiden + festhalten: Fansubname = username (Login-mit-Fansubname; editUsername pruefen)
       ODER Attribut `fansubName` + E-Mail-Login. Empfehlung inkl. Begruendung + Konsequenzen fuer
       Login-UX.
    3. Schreibrichtung fuer Editierbarkeit festlegen (KC master; Team4s write-back vs read-only).
    Ergebnis dem Nutzer zur Bestaetigung vorlegen (blocking), bevor Task 2-4 umgesetzt werden.
  </action>
  <resume-signal>Bestaetige den gewaehlten Login-Identity-Mechanismus (username vs Attribut) + Schreibrichtung, oder beschreibe Anpassungen.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Realm User-Profile: Fansubname erfassen, First/Last optional</name>
  <files>infra/keycloak/realm-team4s.json</files>
  <read_first>infra/keycloak/realm-team4s.json (userProfile/attributes/required)</read_first>
  <action>
    Declarative User Profile so konfigurieren (per Task-1-Entscheid): Fansubname-Feld
    (username-basiert ODER Attribut `fansubName`) bei Registrierung erforderlich; firstName/lastName
    auf NICHT erforderlich. Realm-Export (realm-team4s.json) entsprechend aktualisieren, sodass ein
    Re-Import den Zustand reproduziert. Offene Registrierung ohne Invite bleibt funktionsfaehig.
  </action>
  <acceptance_criteria>
    - Registrierung verlangt Fansubname, nicht firstName/lastName.
    - realm-team4s.json bildet den Zustand ab (idempotenter Re-Import).
  </acceptance_criteria>
  <verify><manual>Registrierungs-Formular auf :3300 zeigt Fansubname (Pflicht), First/Last optional/entfernt.</manual></verify>
  <done>User-Profile erfasst Fansubname; Namensfelder optional.</done>
</task>

<task type="auto">
  <name>Task 3: register.ftl + Labels (Fansubname prominent, Namen optional)</name>
  <files>
    infra/keycloak/themes/team4s/login/register.ftl
    infra/keycloak/themes/team4s/login/messages/messages_de.properties
  </files>
  <read_first>infra/keycloak/themes/team4s/login/register.ftl (aus 135-08)</read_first>
  <action>
    Das team4s-register.ftl (135-08) so anpassen, dass das Fansubname-Feld klar beschriftet und
    prominent ist; Vor-/Nachname als optional gekennzeichnet oder ausgeblendet. Deutsche Labels mit
    korrekten Umlauten in messages_de.properties. Mit dem 135-08-Invite-Kontext + E-Mail-Lock
    koexistierend.
  </action>
  <acceptance_criteria>
    - Register-Seite zeigt "Fansubname" als Hauptfeld; First/Last optional/weg; korrekte Umlaute.
  </acceptance_criteria>
  <verify><manual>Live-Render der Register-Seite auf :3300.</manual></verify>
  <done>register.ftl fuehrt den Nutzer sauber zum Fansubname.</done>
</task>

<task type="auto">
  <name>Task 4: Token-Mapper + JIT-Sync (Fansubname -> members.nickname/fansub_name)</name>
  <files>infra/keycloak/realm-team4s.json</files>
  <read_first>
    infra/keycloak/realm-team4s.json (protocol mappers)
    backend JIT-Identity-Sync (AuthzRepository / SyncGlobalRolesFromKeycloak-Umfeld), app_auth.go identity
    backend/internal/handlers/app_profile.go (fansub_name Edit-Pfad)
  </read_first>
  <action>
    OIDC-Protocol-Mapper sicherstellen, der den Fansubname (username ODER fansubName-Attribut) als
    Claim in den Token legt. Backend-JIT/Identity-Seed so anpassen, dass beim Login members.nickname/
    fansub_name aus dem Fansubname gesetzt/aktualisiert wird (KC autoritativ). Editier-Pfad gemaess
    Task-1-Schreibrichtung konsistent machen (write-back nach KC ODER read-only-aus-KC). public_slug-
    Invarianten (unique/immutable) beachten.
  </action>
  <acceptance_criteria>
    - Token traegt den Fansubname-Claim.
    - Nach Login zeigt Team4s den Fansubname als Identitaet (kein First/Last-Fallback).
    - Editier-Verhalten entspricht der festgelegten Schreibrichtung, keine Drift.
  </acceptance_criteria>
  <verify><manual>Direct-Grant-Token inspizieren (Fansubname-Claim) + Team4s-Profil nach Login.</manual></verify>
  <done>Fansubname fliesst KC -> Token -> Team4s-Profil konsistent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Live-UAT Fansubname-Registrierung end-to-end</name>
  <files>none</files>
  <action>
    Vorlegen + bestaetigen lassen: neue Registrierung mit Fansubname (First/Last leer) -> Konto
    entsteht -> Login funktioniert (mit Fansubname bzw. E-Mail je Task-1-Entscheid) -> Team4s-Profil
    zeigt den Fansubname und ist gemaess Schreibrichtung editierbar.
  </action>
  <resume-signal>"Fansubname-Registrierung bestaetigt" plus etwaige Abweichungen.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|--------------|
| Nutzer-gewaehlter Fansubname (Registrierung) | Als Anzeige-/Identitaetswert behandeln; escapen (FTL ?html), Unique-/Slug-Invarianten serverseitig durchsetzen; keine Rechte aus dem Namen ableiten. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-09-01 | Spoofing | Fansubname als Login-Identity | mitigate | Unique-Constraint (KC username / members.public_slug), Kollisions-/Reserved-Checks; Impersonation ueber gleichlautende Namen verhindern. |
| T-135-09-02 | Tampering | fansubName-Attribut im Token | mitigate | Nur ueber KC gesetzt/gemappt; Backend vertraut signiertem Token, nicht Client-Eingaben; write-back nur ueber definierten Pfad. |
</threat_model>

<verification>
Registrierung erfasst Fansubname (First/Last optional); Token traegt ihn; Team4s-Profil zeigt/ediert
ihn konsistent (KC master); beide Human-Verify-Checkpoints bestaetigt.
</verification>

<success_criteria>
D-14 erfuellt: KC-Registrierung ist fansubname-zentriert, First/Last optional, Keycloak autoritativ,
Team4s spiegelt den Fansubname ohne Doppelquellen-Drift.
</success_criteria>

<output>
Create `.planning/phases/135-einladungs-und-onboarding-flow-fuer-eingeladene-fansub-mitgl/135-09-SUMMARY.md` when done
</output>
