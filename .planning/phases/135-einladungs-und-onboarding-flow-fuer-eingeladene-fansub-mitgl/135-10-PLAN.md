---
phase: 135-einladungs-und-onboarding-flow-fuer-eingeladene-fansub-mitgl
plan: 10
type: execute
wave: 4
depends_on: ["135-09"]
files_modified:
  - infra/keycloak/realm-team4s.json
  - infra/keycloak/themes/team4s/login/register.ftl
  - backend/internal/middleware/current_user_auth.go
  - frontend/src/app/admin/fansubs/[id]/edit/GroupMembersHistTable.tsx
autonomous: false
requirements: [D-15, D-16]

must_haves:
  truths:
    - "The Fansubname is displayed with its original case (e.g. 'Extro', 'D1sk') across the app, while the KC username stays lowercase for case-insensitive login."
    - "A group leader can see a pending historical-member self-claim on the historical member card and approve (verify) or reject it -- the backend routes/handlers/data already work; only the render is missing."
  artifacts:
    - path: "frontend/src/app/admin/fansubs/[id]/edit/GroupMembersHistTable.tsx"
      provides: "renders pendingClaimsByMember + onVerifyClaim/onRejectClaim in the HistoricalMemberCard"
  key_links:
    - from: "KC fansubName attribute (case-preserved)"
      to: "Team4s account_display_name / members.nickname"
      via: "OIDC claim + JIT/display derivation"
      pattern: "fansubName"
---

<objective>
Zwei Member-Onboarding-Fixes, gebuendelt (Nutzer: "gleichzeitig", KC-Sachen via GSD):

A) **Case-preserved Fansubname (D-15).** KC normalisiert `username` auf Kleinschreibung ("Extro"
   -> "extro"). Weil username=Fansubname, wird der Anzeigename klein. Fix: original geschriebenen
   Fansubname als eigenes KC-Attribut fuehren und fuer die ANZEIGE nutzen; username bleibt klein
   (Login). Ziel: "Extro"/"D1sk" ueberall angezeigt, Login case-insensitiv.

B) **Historisch-Mitglied Selbst-Claim Approval-Render (D-16, Finding #25).** Backend + Routen
   (admin_routes.go:225-227) + Permission + Daten funktionieren (GET /admin/fansubs/{id}/member-claims
   liefert den pending Claim). Die Frontend-Karte GroupMembersHistTable.tsx bekommt
   pendingClaimsByMember/onVerifyClaim/onRejectClaim durchgereicht, RENDERT sie aber nie (nur
   Typ-Deklarationen). Fix: Approval-UI in der HistoricalMemberCard rendern.

REALITY / OFFEN (Teil A, Task 1 an laufender KC 26 klaeren, NICHT annehmen):
- Wie wird `username = lowercase(fansubName)` in KC 26 abgeleitet? Optionen: register.ftl-JS fuellt
  ein (verstecktes) username-Feld case-normalisiert aus dem Fansubname-Feld; ODER KC-SPI; ODER
  Accept-Zeit-Normalisierung. Task 1 = blocking checkpoint, Mechanismus + Machbarkeit belegen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/135-einladungs-und-onboarding-flow-fuer-eingeladene-fansub-mitgl/135-CONTEXT.md
@.planning/phases/135-einladungs-und-onboarding-flow-fuer-eingeladene-fansub-mitgl/135-09-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: KC-26 username-aus-fansubName-Ableitung klaeren (Case-Preservation)</name>
  <files>none (Untersuchung; Umsetzung ab Task 2)</files>
  <action>
    An der laufenden KC (Realm team4s, 18081) klaeren + dokumentieren, wie ein case-preserved
    `fansubName`-Attribut neben einem klein-normalisierten `username` gefuehrt wird und wie der
    username automatisch = lowercase(fansubName) gesetzt wird (register.ftl-JS vs. SPI vs.
    Accept-Zeit). Empfehlung + Konsequenzen dem Nutzer vorlegen (blocking).
  </action>
  <resume-signal>Bestaetige den Ableitungs-Mechanismus, oder beschreibe Anpassungen.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: KC User-Profile: fansubName-Attribut (case-preserved) + Register-Feld</name>
  <files>infra/keycloak/realm-team4s.json, infra/keycloak/themes/team4s/login/register.ftl</files>
  <action>
    fansubName-Attribut (Original-Case) im User-Profile anlegen; register.ftl "Fansubname"-Feld
    darauf mappen + username = lowercase(fansubName) ableiten (per Task-1-Mechanismus); email bleibt
    (135-09-Invite-Prefill unangetastet). OIDC-Mapper: fansubName-Claim in den Token.
  </action>
  <acceptance_criteria>
    - Registrierung mit "Extro" -> KC fansubName="Extro", username="extro". Token traegt fansubName.
  </acceptance_criteria>
  <verify><manual>Registrierung live + Token-Claim pruefen.</manual></verify>
  <done>fansubName case-preserved erfasst + im Token.</done>
</task>

<task type="auto">
  <name>Task 3: Team4s liest fansubName fuer Anzeige</name>
  <files>backend/internal/middleware/current_user_auth.go</files>
  <action>
    DisplayName/members.nickname aus dem case-preserved fansubName-Claim ableiten (Fallback:
    preferred_username -> name -> email). members.nickname wird mit dem Original-Case gesetzt.
  </action>
  <acceptance_criteria>
    - account_display_name/members.nickname = "Extro"/"D1sk" (Case), Login weiter mit klein.
  </acceptance_criteria>
  <verify><manual>me/profile + Gruppen-/Release-Anzeige zeigen Original-Case.</manual></verify>
  <done>Anzeige case-preserved.</done>
</task>

<task type="auto">
  <name>Task 4: Frontend -- Selbst-Claim Approval in HistoricalMemberCard rendern (#25/D-16)</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/GroupMembersHistTable.tsx</files>
  <read_first>
    frontend/src/app/admin/fansubs/[id]/edit/GroupMembersHistTable.tsx (pendingClaimsByMember/onVerifyClaim/onRejectClaim sind bereits Props, ungenutzt)
    frontend/src/app/admin/fansubs/[id]/edit/useGroupMembersClaimActions.ts (Approve/Reject-Actions vorhanden)
  </read_first>
  <action>
    In der HistoricalMemberCard pro Member pendingClaimsByMember.get(member.id) rendern: den pending
    Claim (Antragsteller/Status) anzeigen + "Bestaetigen"/"Ablehnen"-Buttons (@/components/ui),
    onVerifyClaim(claim.id) / onRejectClaim(claim.id, member.nickname). Gated auf canCreateClaimInvitation-
    aequivalente Leiter-Capability. Analog zur vorhandenen pending-Invite-Zeile.
  </action>
  <acceptance_criteria>
    - Bei pending Claim erscheinen Bestaetigen/Ablehnen; Klick ruft die Actions; nach Erfolg verschwindet der Antrag.
  </acceptance_criteria>
  <verify><manual>Live: Extro-Selbst-Claim erscheint bei d1sk mit Approval; Verify verknuepft den Account.</manual></verify>
  <done>Selbst-Claim-Approval sichtbar + funktional.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Live-UAT beide Teile</name>
  <files>none</files>
  <action>
    Bestaetigen lassen: (A) Registrierung/Anzeige mit Original-Case Fansubname; (B) Historisch-
    Mitglied Selbst-Claim -> Leader bestaetigt -> Account verknuepft, Member klickbar.
  </action>
  <resume-signal>"135-10 bestaetigt" plus Abweichungen.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|--------------|
| fansubName (Nutzer-Eingabe, Anzeige) | escapen; Unique via username (lowercased) erzwungen; keine Rechte aus dem Namen. |
| Selbst-Claim Approval | Verify/Reject bleibt permission-gated (ActionFansubGroupHistoricalMembersLink); Frontend nur Anzeige. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-135-10-01 | Spoofing | fansubName-Anzeige | mitigate | Login-Identitaet bleibt der (unique) username; Anzeige rein kosmetisch. |
| T-135-10-02 | Elevation | Claim-Verify | accept | Serverseitige Permission unveraendert; Frontend rendert nur die schon erlaubte Aktion. |
</threat_model>

<verification>
Case-preserved Fansubname live in Registrierung + Anzeige; Selbst-Claim-Approval sichtbar+funktional;
beide Human-Verify-Checkpoints bestaetigt.
</verification>

<success_criteria>
D-15 (Fansubname Original-Case angezeigt, Login klein) + D-16 (Selbst-Claim-Approval gerendert) erfuellt.
</success_criteria>

<output>
Create `.planning/phases/135-einladungs-und-onboarding-flow-fuer-eingeladene-fansub-mitgl/135-10-SUMMARY.md` when done
</output>
