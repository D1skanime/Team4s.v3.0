# 130-05 — api.ts helper + Next.js route parse the three status branches identically (Wave 3)

**Requirement:** PMCT-03, PMCT-04, PMCT-08. **Depends on:** 130-04 (public TS types + error-envelope type).
**Goal:** The central API helper and the public member route/metadata parse the documented status contract identically — 200→`{data, viewer}`, neutral 404→notFound (hidden==missing, indistinguishable), 500→typed error — and `frontend/src/app/members/[slug]/page.tsx` (route, `generateMetadata`, PageProps async params, error/notFound branches, and the rendered profile components) compiles against the real route + contract with NO fallback fields and NO duplicate recent-data path.

**Read first:** `130-04-PLAN.md` (final TS types + `ApiErrorEnvelope`), `frontend/src/lib/api.ts` `getMemberProfile` (L3167–3190 — fetches `/api/v1/members/{slug}` with `cache:"no-store"`, throws `ApiError(status, message, null, code, details)` via `parseApiErrorPayload` on any non-ok, returns `PublicMemberProfileResponse`), `frontend/src/app/members/[slug]/page.tsx` (`getMemberProfileForRequest = cache(getMemberProfile)`, `isNotFoundError` → `error.status===404`, `permanentRedirect`, `notFound()`, `ErrorState`, `MemberProfileContent`), and the profile components under `frontend/src/app/members/[slug]/` + `frontend/src/components/profile/` that read `memberships` / `background_image`.

**Files**
- MODIFY `frontend/src/lib/api.ts` `getMemberProfile` (type-only alignment; likely minimal).
- MODIFY `frontend/src/app/members/[slug]/page.tsx` + its colocated `MemberProfileContent` and any colocated section components.
- MODIFY any `frontend/src/components/profile/*` that referenced the dropped `app_member_status`/`app_member_roles`/`historical_member_status` on memberships or `source_original_url` on the public background (retype fallout from 130-04).

**Steps**
1. **Helper — verify, do not rewrite the branch logic.** `getMemberProfile` ALREADY implements the contract: `response.ok`→returns `{data, viewer}` (`PublicMemberProfileResponse`); any non-ok (404 AND 500) throws `ApiError(status, message, null, code, details)` via `parseApiErrorPayload` — one path, no message-based branching, so hidden and missing (both 404) are indistinguishable. CONFIRM this is intact and that its return type matches the 130-04 types. If `parseApiErrorPayload`'s shape differs from `ApiErrorEnvelope`, reconcile to the one shared envelope type; otherwise leave the helper unchanged and say so.
2. **page.tsx (PMCT-08) — verify + de-fallback.** `page.tsx` already awaits async `params`, wraps the fetch in `cache(getMemberProfile)`, maps `isNotFoundError` (status 404) → `notFound()`, uses `permanentRedirect` for canonical slug and `ErrorState` for the failure branch — CONFIRM these three branches still hold against the retyped result. The real change: remove any fallback field access (defaulting a now-dropped membership/status field) and any duplicate recent-media/recent-contributions rendering path on the PUBLIC page (129 removed the data; delete any dead consumer still importing it). Ensure `generateMetadata` consumes the same typed result with no dropped-field access.
3. **Component fallout (the substantive work).** Update `MemberProfileContent` + profile components so they no longer read the dropped fields; public membership rendering uses only the allow-listed `PublicMemberMembership` fields; the background image uses only `public_url`. If any component still imports `MemberProfileRecentMedia`/`MemberProfileRecentContribution` for the PUBLIC surface, remove that usage (flag genuinely-dead files for 130-06).
4. Keep all user-facing German strings with correct umlauts (CLAUDE.md); use `@/components/ui` primitives for any touched UI (no hand-built native controls).

**Verification**
- `npm run -w frontend typecheck` and `npm run -w frontend lint` pass (no `any`, no dropped-field references).
- `npm run -w frontend test` green for touched profile/page/api tests (Vitest); update existing contract tests to the new shape.
- `grep -rn "app_member_status\|app_member_roles\|historical_member_status\|source_original_url" frontend/src/app/members frontend/src/components/profile` returns nothing on the public surface.
- Manual/route check deferred to Phase 134 live UAT bundle; here prove compile + unit-level parsing of all three branches (add a helper test asserting 404 and 500 map to the intended outcomes).

**Commit:** `feat(130-05): parse public member status contract identically in api helper and route`

**Notes/deviations to surface:** If the helper name differs from `getMemberProfile`, record the actual name. Live rendering of both reference profiles is intentionally NOT verified here (Phase 134). Do not add SSR-composition or race-safe-state changes (Phase 132) — this plan only aligns the contract parse and removes fallback/duplicate paths.
