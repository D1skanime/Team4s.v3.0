# 130-06 — Finish the Recent* removal: sweep genuine orphans, prove the rest retained (Wave 4)

**Requirement:** PMCT-07. **Depends on:** 130-02, 130-04, 130-05 (public structs/types/consumers finalized).
**Goal:** Complete the D-02 cleanup Phase 129 started — remove any GENUINELY orphaned recent-projection types/endpoints/components, and PROVE that everything retained is retained for a real reason (the internal edit surface still uses it). No public surface carries a recent-data path.

**Read first:** `130-CONTEXT.md` D-02 (129-06/07 already removed recent_* from the PUBLIC DTO/TS type and dropped the dead `/member-contributions` endpoint; 130 finishes verification + orphan removal), `129-CONTEXT.md` (129-D-11 delegated the finish here). Confirm current usage of `MemberProfileRecentMedia`/`MemberProfileRecentContribution` (Go + TS) and any lingering member-contributions endpoint/component references.

**Files**
- MODIFY only files proven dead by the grep audit below (candidates: orphaned model/TS types, dead route handlers, dead components). Likely NONE beyond stray references — this plan is verification-first, deletion-only-on-proof.

**Steps**
1. **Audit (Go).** `grep -rn "RecentMedia\|RecentContribution\|recent_media\|recent_contributions" backend/`. Confirm `MemberProfileRecentMedia`/`MemberProfileRecentContribution` are STILL referenced by the internal `MemberProfile` edit struct + its loader. If so → RETAIN them (they are not orphaned) and record the reference. Only if a type/field has ZERO remaining referrer, remove it.
2. **Audit (TS/frontend).** `grep -rn "RecentMedia\|RecentContribution\|recent_media\|recent_contributions\|member-contributions" frontend/src`. Confirm the internal `MemberProfileData` still uses the Recent* types (retain). Remove any component/import/route that is now dead on the PUBLIC surface (e.g. a leftover recent-contributions component no longer imported anywhere).
3. **Endpoint audit.** Confirm the dead member-contributions endpoint removed in 129-07 has no residual route registration in `backend/cmd/server/main.go`, no OpenAPI path, and no `api.ts` helper. Remove any residue found.
4. **Record the result** in the commit body / plan notes: list what was deleted and what was retained-with-reason, so the sweep is auditable (PMCT-07 assurance).

**Verification**
- `go build ./...` + `go test ./backend/...` green; `npm run -w frontend typecheck` + `npm run -w frontend test` green (nothing depended on a removed symbol).
- `grep -rn "recent_media\|recent_contributions" shared/contracts/openapi.yaml frontend/src/types/profile.ts` shows recent_* ONLY under the internal `MemberProfileData`/`MemberProfile` edit surface, NEVER under any `PublicMember*` type/schema.
- No dangling import or route references a removed symbol (`go vet`, eslint clean).

**Commit:** `chore(130-06): finish recent-projection cleanup; remove orphans, retain internal edit surface`

**Notes/deviations to surface:** Expected outcome is mostly VERIFICATION: `MemberProfileRecentMedia`/`MemberProfileRecentContribution` are legitimately retained because the internal edit surface consumes them — deleting them would break edit. State explicitly if nothing was deletable (that is a valid, complete result for PMCT-07), listing the retained-with-reason set.
