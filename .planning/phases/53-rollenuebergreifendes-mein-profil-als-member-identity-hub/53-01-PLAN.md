---
phase: 53-rollenuebergreifendes-mein-profil-als-member-identity-hub
plan: "01"
type: execute
wave: 1
depends_on:
  - ../47-member-profile-und-historical-identity
  - ../48-meine-gruppen-und-contributor-dashboard
  - ../52-profile-account-return-refresh-flow
files_modified:
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/components/layout/AppShell.module.css
  - frontend/src/components/layout/AppShell.test.tsx
  - frontend/src/app/me/profile/page.tsx
  - frontend/src/app/me/profile/components
  - frontend/src/app/me/profile/page.module.css
  - frontend/src/app/me/profile/page.test.tsx
  - frontend/src/app/admin/profile/page.tsx
  - frontend/src/app/admin/profile/page.test.tsx
  - frontend/src/app/admin/page.tsx
  - frontend/src/app/admin/my-groups/page.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.tsx
  - frontend/src/lib/profileLabels.ts
  - frontend/src/types/profile.ts
  - frontend/src/lib/api.ts
  - frontend/src/components/ui
  - shared/contracts/openapi.yaml
autonomous: true
requirements:
  - MEMBER-PROFILE-HUB-01
must_haves:
  truths:
    - "`/me/profile` is the target route for all signed-in users."
    - "`/admin/profile` must not remain a separate admin-profile world; it redirects or re-exports the neutral profile page."
    - "Phase 53A execution chooses internal re-export/transition wrapper for `/admin/profile`; redirect is only a documented fallback if the framework blocks the wrapper path."
    - "Keycloak owns login, e-mail, password, MFA, account status, and technical account security."
    - "Team4s owns fansub profile data, avatar, bio/story, memberships, Team4s roles, and contribution display."
    - "Historical credits are display/archive data only and never create permissions."
    - "Role kinds stay separated: platform role, group role, app role, historical credit role, release/project role."
    - "Missing or unclear visibility is not public."
    - "No mock data, invented fields, or fake contribution/member data is introduced."
    - "Existing UI/GDS components are used before adding page-local generic UI."
    - "OpenAPI documentation for GET/PUT /api/v1/me/profile and POST /api/v1/me/profile/avatar is mandatory, not optional."
    - "Sensitive account data must not be passed into future public-profile-ready components."
    - "The screenshot reference is a design direction, not permission to fake unsupported fields or hardcode a profile-only shell."
    - "D-01: `/me/profile` ist die Zielroute für alle eingeloggten User."
    - "D-02: `/admin/profile` bleibt nur Übergang: Redirect oder interner Re-Export, aber keine eigene Admin-Profilwelt."
    - "D-03: Neue Komponenten und sichtbare Texte werden rollenneutral benannt, z. B. `MyProfilePage`, `MemberProfileHero`, `MemberAvatarCard`, `AccountSecurityCard`."
    - "D-04: Normale Member dürfen im UI nicht das Gefühl bekommen, in einer Admin-Maske zu arbeiten."
    - "D-05: Keycloak besitzt Login, E-Mail, Passwort, MFA, technische Account-Sicherheit und technische Identität."
    - "D-06: Team4s besitzt Fansub-Name, Anzeigename, Avatar, Kurzbeschreibung, Fansub-Geschichte, Aktivitätszeitraum, Profil-Sichtbarkeit, Gruppen, Rollen und Beiträge im Team4s-Kontext."
    - "D-07: Frontend-Code darf keine Tokens lesen und keine Keycloak-Helfer direkt aufrufen. Rückkehr-Refresh läuft über `refreshActiveAuthSession()` und danach `getOwnProfile()`."
    - "D-08: Accountdaten bleiben read-only und dürfen nicht in Public-taugliche Komponenten geraten."
    - "D-09: OpenAPI für `GET /api/v1/me/profile`, `PUT /api/v1/me/profile` und `POST /api/v1/me/profile/avatar` ist in Phase 53 Pflicht, nicht optional."
    - "D-10: Runtime-Daten, Frontend-DTOs, API-Helfer und `shared/contracts/openapi.yaml` müssen gemeinsam bewegt werden."
    - "D-11: UI-Code darf keine undokumentierten Felder, Statuspfade oder Fallbacks aus ad hoc `fetch`-Antworten ableiten."
    - "D-12: Fehlende oder unklare Sichtbarkeit bedeutet nicht öffentlich."
    - "D-13: Aktuell bekannte Runtime-Werte sind `members_only | public`. Eine dritte Option wie `Für Gruppen sichtbar` darf nur mit DB-/Backend-/OpenAPI-/Public-Query-Contract umgesetzt werden."
    - "D-14: Die Referenz zeigt drei Sichtbarkeitsoptionen als Zielrichtung; falls der Contract in Phase 53 nicht erweitert wird, darf die UI nur die real unterstützten Optionen als aktiv nutzbar anzeigen."
    - "D-15: Der Profil-Aktivitätszeitraum meint den persönlichen allgemeinen Fansub-Aktivitätszeitraum, nicht Gruppenmitgliedschaft oder Projektbeteiligung."
    - "D-16: Aktiv seit/bis darf nicht aus Gruppenmitgliedschaften, historischen Credits oder Release-Rollen abgeleitet werden."
    - "D-17: Wenn Month/Year-Felder fehlen, gibt es nur zwei saubere Wege: reversible Migration plus Contract oder ehrliche year-only UI. Keine freie Texteingabe ohne Validierung."
    - "D-18: Rollenarten bleiben getrennt: Plattformrolle, Gruppenrolle, App-Rolle, historische Credit-Rolle und Release-/Projektrolle."
    - "D-19: Technische Codes werden zentral oder bewusst geteilt auf deutsche Labels gemappt; rohe Codes wie `platform_admin`, `quality_checker` oder `active` dürfen nicht unübersetzt im UI stehen."
    - "D-20: Historische Credits sind Archiv-/Darstellungsdaten und erzeugen niemals App-Berechtigungen."
    - "D-21: Phase 53 zeigt Beiträge zunächst ehrlich als Summary/Aggregate, wenn kein paginierter Detail-Endpunkt existiert. `/me/contributions` und detailreiche Beitragslisten bleiben vorbereitet oder deferred, außer sie werden explizit als eigener Contract umgesetzt."
    - "D-30: Die gelieferte Referenz ist Zielrichtung, nicht pixelgenaue Vorgabe."
    - "D-31: Zentrale Struktur der Referenz ist bindend als Planungsrichtung: bestehende Shell, Profil-Hero oben, Avatar prominent, Rollen/Gruppen als Chips, Basisdaten links, Avatar/Sichtbarkeit/Account rechts, Mitgliedschaften und Beiträge als eigene Bereiche."
    - "D-32: Keine neue Profile-only Sidebar hardcoden. Bestehende globale App-Shell/Navigation verwenden oder erweitern."
    - "D-33: Bestehende GDS-Komponenten wie `Card`, `Button`, `Badge`, `FormField`, `Input`, `Textarea`, `Select`, `PageHeader`, `SectionHeader`, `LoadingState`, `ErrorState`, `EmptyState`, `Modal` und `Drawer` werden vor page-lokalen Controls genutzt."
    - "D-34: Deutsche UI-Texte verwenden korrekte Umlaute."
    - "D-40: Phase 53A soll ein breiter Hub-Grundbau sein, nicht nur ein minimaler Routen-Slice. Die komplette Zielstruktur mit Hero, Basisdaten, Story, Avatar-Card, Sichtbarkeit, Account & Sicherheit, Mitgliedschaften und Beiträgen soll sichtbar angelegt werden."
    - "D-41: Alle Zielbereiche dürfen in 53A sichtbar sein, aber sie müssen ehrlich begrenzt sein: Empty State, deaktivierte Aktion oder klarer Hinweis, wenn Daten oder Contract fehlen. Keine Fake-Daten, keine erfundenen Felder und keine Links ins Leere."
    - "D-45: Die neue Shell ist global und wiederverwendbar, nicht profil-lokal. `/me/profile` ist der erste Consumer, aber die Shell soll perspektivisch für Dashboard, Verwaltung, Mein Bereich, Einstellungen und weitere App-Flächen nutzbar sein."
    - "D-46: Die Shell wird dual-state-fähig vorbereitet: `authenticated` wird in 53A produktiv genutzt, `anonymous`/Login/Registrieren werden architektonisch nicht verbaut, aber funktional auf später verschoben."
    - "D-47: Die eingeloggte Shell-Navigation soll referenznah sein: Public-Bereich, Dashboard, capability-gated Verwaltung, Mein Bereich mit Mein Profil/Meine Gruppen/Meine Beiträge, Einstellungen und User-Footer."
    - "D-48: 53A migriert nicht die ganze App auf die neue Shell. Nur `/me/profile` nutzt sie als erster Consumer; andere Seiten bleiben vorerst auf bestehender Struktur."
    - "D-49: Noch nicht existierende Shell-Ziele werden nicht als Fake-Routen verlinkt. Sie dürfen sichtbar vorbereitet werden, aber nur deaktiviert oder als Coming soon."
    - "D-50: Mobile-Shell-Härtung kommt in 53B. 53A muss Desktop/Tablet sauber und Mobile nicht kaputt liefern, aber ein finaler Drawer/Burger ist keine 53A-Pflicht."
    - "D-51: Parallelisierung erfolgt nur nach klarer File Ownership. Globale Shell ist ein eigener Block und darf nicht gleichzeitig mit konfliktierenden `/me/profile`-Grundlagen bearbeitet werden."
    - "D-52: `/me/profile` braucht einen Nicht-Admin-Einstieg. Admin-Kontext-Links allein reichen nicht; die globale Shell oder ein anderer auth-naher Einstieg muss normalen eingeloggten Membern den Weg zu `Mein Profil` geben."
    - "D-53: `frontend/src/app/me/profile/page.test.tsx` muss in 53A aktiv erstellt und mit Mindesttests befüllt werden. Eine leere/grüne Testdatei erfüllt die Route-Test-Anforderung nicht."
  artifacts:
    - path: "frontend/src/app/me/profile/page.tsx"
      provides: "Role-neutral own-profile route"
    - path: "frontend/src/app/admin/profile/page.tsx"
      provides: "Transition route without a separate admin profile implementation"
    - path: "shared/contracts/openapi.yaml"
      provides: "Documented runtime /api/v1/me/profile read/update/avatar contract"
  key_links:
    - from: "frontend/src/app/admin/profile/page.tsx"
      to: "frontend/src/app/me/profile/page.tsx"
      via: "internal re-export/transition wrapper"
      pattern: "MyProfilePage|/me/profile"
    - from: "backend/internal/repository/member_profile_repository.go"
      to: "frontend/src/types/profile.ts"
      via: "existing /api/v1/me/profile aggregate"
      pattern: "MemberProfileData|memberships|historical_credits"
---

<objective>
Phase 53A turns the current admin-looking profile screen into the first usable `/me/profile` Member Identity Hub foundation: correct route ownership, a reusable authenticated app shell as first consumer, real data sources, role-neutral component naming, GDS-based layout, visible honest hub sections, and honest membership/contribution display without changing deeper avatar, rich-text, visibility, or month-year contracts yet.
</objective>

<decision_coverage>

- D-01: `/me/profile` is the target route for all signed-in users.
- D-02: `/admin/profile` remains only a redirect or internal re-export, not a separate admin profile world.
- Execution decision for D-02: use internal re-export/transition wrapper by default; redirect requires a documented blocker and updated tests.
- D-03: Components and visible naming must be role-neutral.
- D-04: The UI must not feel like an admin-only backend form for normal members.
- D-05: Keycloak owns login, e-mail, password, MFA, technical account security, and technical identity.
- D-06: Team4s owns fansub profile data, avatar, bio/story, activity period, visibility, groups, roles, and contributions.
- D-07: Frontend code must not read tokens or call Keycloak helpers directly.
- D-08: Read-only account data must stay out of public-profile-ready components.
- D-09: OpenAPI documentation for own-profile read/update/avatar is mandatory.
- D-10: Runtime DTOs, frontend DTOs, API helpers, and OpenAPI move together.
- D-11: UI code must not infer undocumented response fields, status paths, or fallbacks.
- D-12: Missing or unclear visibility is not public.
- D-18: Role kinds remain separate.
- D-19: Technical role/status codes get German labels before user-facing display.
- D-20: Historical credits never grant permissions.
- D-21: Contributions stay honest aggregate/summary data unless a paginated contract is added.
- D-30: The screenshot is design direction, not pixel-perfect spec.
- D-31: The screenshot's information architecture guides the profile hub layout.
- D-32: No profile-only hardcoded shell/sidebar.
- D-33: Existing GDS components are used before page-local controls.
- D-34: German UI text uses correct umlauts.
- D-35: Save is active only for dirty Team4s profile fields.
- D-36: Keycloak-return refresh must not overwrite dirty Team4s profile fields.
- D-38: Aggregate profile reads must not pretend to have section-level partial failure boundaries.
- D-40: 53A builds the broad hub foundation, not only a minimal route.
- D-41: All target areas may be visible in 53A only with honest empty, disabled, or contract-missing states.
- D-45: The shell is global/reusable and `/me/profile` is only the first consumer.
- D-46: The shell is prepared for authenticated and future anonymous states.
- D-47: Authenticated shell navigation includes public area, dashboard, gated administration, Mein Bereich, settings, and user footer.
- D-48: 53A does not migrate the whole app to the new shell.
- D-49: Non-existing shell destinations must be disabled or Coming soon, not fake links.
- D-50: Final mobile shell hardening is 53B; 53A must not break mobile.
- D-51: Execution parallelizes only by clear file ownership; shell work is its own block.
- D-52: Normal signed-in members need a non-admin entry point to `/me/profile`.
- D-53: `frontend/src/app/me/profile/page.test.tsx` must be created and contain real route/state/no-admin-leak tests.

</decision_coverage>

<context_decision_index>

- D-01: `/me/profile` ist die Zielroute für alle eingeloggten User.
- D-02: `/admin/profile` bleibt nur Übergang: Redirect oder interner Re-Export, aber keine eigene Admin-Profilwelt.
- D-03: Neue Komponenten und sichtbare Texte werden rollenneutral benannt, z. B. `MyProfilePage`, `MemberProfileHero`, `MemberAvatarCard`, `AccountSecurityCard`.
- D-04: Normale Member dürfen im UI nicht das Gefühl bekommen, in einer Admin-Maske zu arbeiten.
- D-05: Keycloak besitzt Login, E-Mail, Passwort, MFA, technische Account-Sicherheit und technische Identität.
- D-06: Team4s besitzt Fansub-Name, Anzeigename, Avatar, Kurzbeschreibung, Fansub-Geschichte, Aktivitätszeitraum, Profil-Sichtbarkeit, Gruppen, Rollen und Beiträge im Team4s-Kontext.
- D-07: Frontend-Code darf keine Tokens lesen und keine Keycloak-Helfer direkt aufrufen. Rückkehr-Refresh läuft über `refreshActiveAuthSession()` und danach `getOwnProfile()`.
- D-08: Accountdaten bleiben read-only und dürfen nicht in Public-taugliche Komponenten geraten.
- D-09: OpenAPI für `GET /api/v1/me/profile`, `PUT /api/v1/me/profile` und `POST /api/v1/me/profile/avatar` ist in Phase 53 Pflicht, nicht optional.
- D-10: Runtime-Daten, Frontend-DTOs, API-Helfer und `shared/contracts/openapi.yaml` müssen gemeinsam bewegt werden.
- D-11: UI-Code darf keine undokumentierten Felder, Statuspfade oder Fallbacks aus ad hoc `fetch`-Antworten ableiten.
- D-12: Fehlende oder unklare Sichtbarkeit bedeutet nicht öffentlich.
- D-13: Aktuell bekannte Runtime-Werte sind `members_only | public`. Eine dritte Option wie `Für Gruppen sichtbar` darf nur mit DB-/Backend-/OpenAPI-/Public-Query-Contract umgesetzt werden.
- D-14: Die Referenz zeigt drei Sichtbarkeitsoptionen als Zielrichtung; falls der Contract in Phase 53 nicht erweitert wird, darf die UI nur die real unterstützten Optionen als aktiv nutzbar anzeigen.
- D-15: Der Profil-Aktivitätszeitraum meint den persönlichen allgemeinen Fansub-Aktivitätszeitraum, nicht Gruppenmitgliedschaft oder Projektbeteiligung.
- D-16: Aktiv seit/bis darf nicht aus Gruppenmitgliedschaften, historischen Credits oder Release-Rollen abgeleitet werden.
- D-17: Wenn Month/Year-Felder fehlen, gibt es nur zwei saubere Wege: reversible Migration plus Contract oder ehrliche year-only UI. Keine freie Texteingabe ohne Validierung.
- D-18: Rollenarten bleiben getrennt: Plattformrolle, Gruppenrolle, App-Rolle, historische Credit-Rolle und Release-/Projektrolle.
- D-19: Technische Codes werden zentral oder bewusst geteilt auf deutsche Labels gemappt; rohe Codes wie `platform_admin`, `quality_checker` oder `active` dürfen nicht unübersetzt im UI stehen.
- D-20: Historische Credits sind Archiv-/Darstellungsdaten und erzeugen niemals App-Berechtigungen.
- D-21: Phase 53 zeigt Beiträge zunächst ehrlich als Summary/Aggregate, wenn kein paginierter Detail-Endpunkt existiert. `/me/contributions` und detailreiche Beitragslisten bleiben vorbereitet oder deferred, außer sie werden explizit als eigener Contract umgesetzt.
- D-22: Avatar-Upload bleibt serverseitig autoritativ validiert.
- D-23: Erlaubt sind JPG, PNG und WEBP; SVG bleibt verboten, solange kein robustes Sanitizing-Konzept existiert.
- D-24: Das Profil-Avatar-Limit soll produktnah konkretisiert werden, z. B. 5 MB. Der aktuelle globale Bildpfad nutzt 50 MB und muss geprüft/angepasst werden.
- D-25: Avatar-Crop soll bestehende Projekt-Crop-Primitives aus `MediaUpload`, `mediaUploadCropMath.ts` und `mediaUploadA11y.ts` wiederverwenden oder die Abweichung dokumentieren.
- D-26: Varianten wie `avatar_256`, `avatar_96` und `avatar_48` werden entweder über bestehende `media_files.variant` umgesetzt oder bewusst als Follow-up markiert. Keine parallele Media-Logik.
- D-27: Die lange Fansub-Geschichte nutzt nur dann echte TipTap-Persistenz, wenn Backend-Validierung, Sanitizing, Speicherform und Contract bereit sind.
- D-28: Keine ungeprüfte HTML-Ausgabe; Links, Nodes und Marks brauchen Allowlist-Regeln.
- D-29: Wenn Phase 53 die sichere TipTap-Verdrahtung nicht vollständig schafft, bleibt Plain Text ehrlich sichtbar und Rich Persistence wird deferred.
- D-30: Die gelieferte Referenz ist Zielrichtung, nicht pixelgenaue Vorgabe.
- D-31: Zentrale Struktur der Referenz ist bindend als Planungsrichtung: bestehende Shell, Profil-Hero oben, Avatar prominent, Rollen/Gruppen als Chips, Basisdaten links, Avatar/Sichtbarkeit/Account rechts, Mitgliedschaften und Beiträge als eigene Bereiche.
- D-32: Keine neue Profile-only Sidebar hardcoden. Bestehende globale App-Shell/Navigation verwenden oder erweitern.
- D-33: Bestehende GDS-Komponenten wie `Card`, `Button`, `Badge`, `FormField`, `Input`, `Textarea`, `Select`, `PageHeader`, `SectionHeader`, `LoadingState`, `ErrorState`, `EmptyState`, `Modal` und `Drawer` werden vor page-lokalen Controls genutzt.
- D-34: Deutsche UI-Texte verwenden korrekte Umlaute.
- D-35: Speichern ist nur aktiv, wenn Team4s-Profilfelder dirty sind.
- D-36: Keycloak-Rückkehrrefresh und Avatar-Upload dürfen ungespeicherte Team4s-Profilfelder nicht überschreiben.
- D-37: Avatar-Änderung und Profilfeld-Speichern bleiben getrennt oder werden mit einem expliziten kombinierten Save-Contract umgesetzt.
- D-38: Partielle Fehler werden nur versprochen, wenn Datenquellen/API wirklich getrennt ladbar sind. Wenn `/me/profile` ein Aggregate bleibt, muss der Fehlerzustand ehrlich zum Aggregate passen.
- D-39: Mobile, Tastaturbedienung, sichtbare Fokuszustände, Dialog-Fokusführung, ESC-Schließen und keine Hover-only-Aktionen sind Akzeptanzkriterien.

</context_decision_index>

<context>
@AGENTS.md
@.planning/phases/53-rollenuebergreifendes-mein-profil-als-member-identity-hub/53-CONTEXT.md
@.planning/phases/53-rollenuebergreifendes-mein-profil-als-member-identity-hub/53-RESEARCH.md
@docs/engineering/implementation-contract.md
@docs/frontend/ui-system.md
@docs/agent-guidelines-ui.md
@docs/frontend/ui-inventory.md
@docs/api/api-contracts.md
@docs/frontend/auth-api-client.md
@docs/architecture/db-schema-fansub-domain.md
@docs/architecture/fansub-member-management.md
@frontend/src/app/admin/profile/page.tsx
@frontend/src/app/admin/profile/page.module.css
@frontend/src/app/admin/profile/page.test.tsx
@frontend/src/app/admin/my-groups/page.tsx
@frontend/src/app/manage/groups/page.tsx
@frontend/src/components/ui
@frontend/src/types/profile.ts
@frontend/src/types/fansub.ts
@frontend/src/lib/api.ts
@backend/internal/handlers/app_profile.go
@backend/internal/repository/member_profile_repository.go
@backend/internal/models/member_profile.go
@shared/contracts/openapi.yaml
</context>

<discovery>

## Ist-Zustand

- Formal planning registration: `gsd-sdk query init.plan-phase 53` returns `phase_found: true`, `phase_dir: .planning/phases/53-rollenuebergreifendes-mein-profil-als-member-identity-hub`, `phase_req_ids: MEMBER-PROFILE-HUB-01`, `has_context: true`, and `plan_count: 2`. Do not create a duplicate phase entry.
- Route: the runtime UI currently lives at `frontend/src/app/admin/profile/page.tsx` as `AdminProfilePage`. There is no `frontend/src/app/me/profile/page.tsx`.
- Existing non-admin route pattern: `frontend/src/app/manage/groups/page.tsx` already re-exports `../../admin/my-groups/page`, so transition routes are acceptable if ownership is explicit.
- API endpoints: backend routes exist for `GET /api/v1/me/profile`, `PUT /api/v1/me/profile`, and `POST /api/v1/me/profile/avatar`; frontend helpers are `getOwnProfile`, `updateOwnProfile`, and `uploadOwnProfileAvatar`.
- Data sources: `MemberProfileRepository` reads `app_users`, `app_user_global_roles`, `members`, `group_members`, `fansub_group_members`, `fansub_group_member_roles`, `release_member_roles`, `release_versions`, `release_version_groups`, `fansub_groups`, `contributor_roles`, `media_assets`, and `media_files`.
- Current UI issues: technical account fields are prominent, labels still show raw codes in places, active dates are free numeric inputs, visibility is a select, memberships/credits are debug-like lists, and the page imports admin styles directly.
- Current GDS pattern: `frontend/src/app/admin/my-groups/page.tsx` already uses `Button`, `Card`, `Badge`, `EmptyState`, `ErrorState`, `LoadingState`, `PageHeader`, `SectionHeader`, and `Toolbar`; `/admin/profile` should move toward that global component language.
- Current component boundary: the profile page is stateful and must remain an explicit Client Component or thin Server Component wrapper plus client leaf because it owns auth-session refresh, dirty state, form edits, and avatar upload state.
- Current navigation evidence: direct own-profile links exist in `frontend/src/app/admin/page.tsx`, `frontend/src/app/admin/my-groups/page.tsx`, and `frontend/src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.tsx`; executors must run a targeted link/navigation search before declaring `/me/profile` reachable.

## Zielarchitektur

- Target route: `/me/profile`.
- Transition route: `/admin/profile` uses an internal re-export or thin transition wrapper for the same `MyProfilePage`; it must not stay a separate implementation, and redirect is not the default execution path.
- Shell: add or extend a reusable app/authenticated shell seam under a shared location such as `frontend/src/components/layout/AppShell.tsx` after checking existing layout components. The shell is not profile-local and `/me/profile` is only its first production consumer.
- Non-admin entry: the shell or another auth-adjacent navigation seam must expose `Mein Profil` to normal signed-in members without relying on `/admin/*`.
- Component direction: `MyProfilePage`, `MemberProfileHero`, `ProfileBasicsForm`, `MembershipList`, `MembershipCard`, `ContributionSummaryCard`, `AccountSecurityCard`, `RoleChip`, `StatusBadge`.
- Component split: `page.tsx` is only the route/client orchestration shell. Section JSX belongs in `frontend/src/app/me/profile/components/` with focused components such as `MemberProfileHero`, `ProfileBasicsForm`, `ProfileStoryCard`, `MemberAvatarCard`, `ProfileVisibilityCard`, `MembershipList`, `ContributionSummaryCard`, and `AccountSecurityCard`. Split before `page.tsx` grows beyond roughly 450 lines.
- Data flow: 53A consumes the existing `GET /api/v1/me/profile` aggregate and existing mutation helpers. It must not invent fields when the DTO lacks them.
- Layout: profile hero first, then main column for basis/story/memberships/contributions and side column for avatar/visibility/account. In 53A the avatar/visibility cards may be visual organization only; deeper behavior lands in 53B.
- Design direction: use the supplied profile-hub screenshot as information architecture guidance: existing shell/sidebar if available, top actions, hero, chips, left main column, right avatar/visibility/account cards, membership cards, and contribution empty state. Adapt colors, spacing, buttons, cards, and form controls to the existing GDS instead of copying a page-local design system.
- Navigation direction: `Mein Profil` must be reachable for every signed-in user through shell/user-menu seams; admin-only menu items stay capability-gated. Known direct links should move toward `/me/profile`; if no pre-existing global shell exists, create the smallest reusable shell seam rather than hardcoding a profile sidebar.

## Datenquellen-Mapping

- Profil-Hero: `display_name`, `fansub_name`, `bio`, `avatar`, `active_from_year`, `is_currently_active`, `memberships`, `account_global_roles`; missing grouped role labels and display-ready group/logo data.
- Basisdaten: `display_name`, `fansub_name`, `bio`, `active_from_year`, `active_until_year`, `is_currently_active`; in 53A use constrained year/checkbox/select controls only where current contract supports them.
- Avatar: `avatar.public_url` via `members.avatar_media_id -> media_assets/media_files`; 53A only displays current avatar and the existing upload entry point if already present.
- Sichtbarkeit: `profile_visibility` supports `members_only | public`; 53A must not show a working third option unless the contract changes.
- Account & Sicherheit: `email`, `account_display_name`, `account_status`, `account_global_roles`, `keycloak_account_url`, `capabilities.can_open_keycloak_account`; all read-only.
- Mitgliedschaften: `memberships` from `group_members`, `fansub_group_members`, `fansub_group_member_roles`, `fansub_groups`; group logo is not currently in the profile DTO.
- Meine Beiträge: `historical_credits` is aggregated by group/role/release count; detailed Anime/Episode/Release-Version records are not yet available.
- Rich-Text-Bio: current profile page converts editor state to plain text; 53A may preserve existing behavior but must not claim rich-text persistence.

## Contract-/Backend-Lücken

- `shared/contracts/openapi.yaml` does not document the existing `/api/v1/me/profile` runtime API; Phase 53 must add GET/PUT profile and POST avatar documentation.
- `ProfileVisibility` only knows `public | members_only`.
- Activity period is year-only, not month/year.
- Profile rich text is plain-text converted.
- Avatar has no crop contract or variants.
- Group logo is missing in `MemberProfileMembership`.
- Contributions are aggregate-only, not paginated detail rows.

## Risiken / offene Fragen

- Sidebar/App-Shell reuse must be verified before adding route UI. If no global shell exists for `/me/*`, extend the existing shell seam or keep the current app layout; do not hardcode a profile-only sidebar.
- Membership and contribution sections must respect `can_view_memberships` and `can_view_historical_credits`; if capabilities are false or missing, render a conservative scoped state.
- Group logos and contribution detail rows are tempting because the screenshot shows richer cards, but they are not available in the current profile DTO. 53A may show fallback icons/summary only unless the contract is expanded.
- `/admin/profile` should stay test-friendly by re-exporting or wrapping `/me/profile`; a redirect is only acceptable if a framework blocker is documented and tests are updated accordingly.
- The current page already contains Keycloak-return dirty-state behavior from Phase 52; recomposition must preserve it.
- `bio` and `member_story` are separate profile concepts. 53A keeps `bio` as the short card/hero description and keeps `member_story` visible/editable in a separate `ProfileStoryCard` using the current plain-text persistence path.
- CSS ownership moves with the route: `/me/profile` owns its profile layout CSS under `/me/profile`. It must not import `admin/profile/page.module.css`; `/admin/profile` should only re-export or wrap.

</discovery>

<tasks>

<task type="auto">
  <name>Task 0: Create the reusable authenticated app shell entry for `/me/profile`</name>
  <files>frontend/src/components/layout/AppShell.tsx, frontend/src/components/layout/AppShell.module.css, frontend/src/components/layout/AppShell.test.tsx, frontend/src/app/me/profile/page.tsx</files>
  <read_first>
    - frontend/src/app/auth/page.tsx
    - frontend/src/app/admin/page.tsx
    - frontend/src/app/admin/my-groups/page.tsx
    - frontend/src/app/manage/groups/page.tsx
    - docs/frontend/ui-system.md
    - docs/agent-guidelines-ui.md
  </read_first>
  <action>
    Search for an existing global authenticated shell, user menu, sidebar, or layout component before creating anything. If none exists, create the smallest reusable `AppShell`-style component in a shared layout location, not under `/me/profile` and not under `/admin`. It must support an authenticated navigation model now and leave an explicit prop/API seam for future anonymous/login/register shell state without implementing those future flows. For 53A, wire only `/me/profile` as the first production consumer. Include referenznahe sections for public area, dashboard, capability-gated administration, `Mein Bereich`, settings, and user footer. Admin-only entries must be capability-gated or hidden. Existing but unavailable future targets such as `/me/groups` or `/me/contributions` must be disabled or marked Coming soon instead of linked as fake routes. Make `Mein Profil` reachable to a signed-in non-admin member through this shell.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run test -- --run "src/components/layout/AppShell.test.tsx"</automated>
    <manual>Confirm a normal signed-in member without admin capabilities has a visible path to `/me/profile`.</manual>
    <manual>Confirm `/me/profile` is the only page migrated to the new shell in 53A.</manual>
  </verify>
  <done>
    A reusable authenticated shell exists for `/me/profile`, is not profile-local, and provides the non-admin own-profile entry.
  </done>
</task>

<task type="auto">
  <name>Task 1: Register `/me/profile` as the role-neutral own-profile route</name>
  <files>frontend/src/app/me/profile/page.tsx, frontend/src/app/me/profile/components, frontend/src/app/me/profile/page.test.tsx, frontend/src/app/admin/profile/page.tsx, frontend/src/app/admin/profile/page.test.tsx, frontend/src/app/admin/page.tsx, frontend/src/app/admin/my-groups/page.tsx, frontend/src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.tsx</files>
  <read_first>
    - frontend/src/app/admin/profile/page.tsx
    - frontend/src/app/admin/profile/page.test.tsx
    - frontend/src/app/manage/groups/page.tsx
    - frontend/src/app/admin/my-groups/page.tsx
    - frontend/src/app/admin/page.tsx
    - frontend/src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.tsx
    - docs/frontend/auth-api-client.md
  </read_first>
  <action>
    Create the role-neutral `/me/profile` route and move/re-export the existing own-profile page behind neutral naming. Replace `AdminProfilePage` and any new `AdminProfile*` names with neutral names such as `MyProfilePage` or `MemberProfile*`. Make `/admin/profile` an internal re-export or thin transition wrapper around the same implementation; do not keep a duplicate admin profile implementation and do not choose redirect as the default path. Keep the stateful profile implementation as an explicit Client Component (`use client`) because it owns auth-session refresh, dirty state, form edits, and upload state; a Server Component may only be a thin wrapper. Actively create `frontend/src/app/me/profile/page.test.tsx` with assertions for render/load/error behavior, protected route behavior, no admin naming leaks, and route transition behavior. Audit `frontend/src/app/admin/profile/page.test.tsx` before reusing it so old `AdminProfilePage`/admin-specific assertions are removed or moved to the transition wrapper. Update known own-profile links in admin overview, my-groups, and fansub member edit helper text toward `/me/profile`; run a targeted search for `/admin/profile`, `Mein Profil`, user-menu, sidebar, and shell references before declaring navigation complete. Keep platform-admin-only admin navigation intact. Verify the route still uses the existing token-free auth/session boundary, does not read Keycloak tokens directly, and presents the same unauthenticated behavior as the previous protected own-profile route.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run test -- --run "src/app/me/profile/page.test.tsx"</automated>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run test -- --run "src/app/admin/profile/page.test.tsx"</automated>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && Select-String -Path "src/app/me/profile/page.tsx" -Pattern "'use client'|\"use client\""</automated>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck</automated>
    <manual>Run a targeted link search for `/admin/profile` and confirm own-profile entry points now use `/me/profile` or intentionally remain transitional.</manual>
    <manual>Open `/me/profile` without an active session and confirm the existing auth-required/login behavior appears; profile content must not render as public content.</manual>
  </verify>
  <done>
    `/me/profile` owns the profile UI, `/admin/profile` is transitional, and no duplicate admin-profile implementation remains.
  </done>
</task>

<task type="auto">
  <name>Task 2: Recompose the profile page with existing GDS components</name>
  <files>frontend/src/app/me/profile/page.tsx, frontend/src/app/me/profile/components, frontend/src/app/me/profile/page.module.css, frontend/src/app/admin/profile/page.module.css, frontend/src/components/ui</files>
  <read_first>
    - docs/frontend/ui-system.md
    - docs/agent-guidelines-ui.md
    - frontend/src/components/ui/Card.tsx
    - frontend/src/components/ui/Button.tsx
    - frontend/src/components/ui/Badge.tsx
    - frontend/src/components/ui/EmptyState.tsx
    - frontend/src/components/ui/FormField.tsx
    - frontend/src/app/admin/my-groups/page.tsx
  </read_first>
  <action>
    Recompose the page into a modern but scoped Member Identity Hub using global UI components first: `PageHeader`/hero, `Card`, `Button`, `Badge`, `FormField`, `Input`, `Textarea`, `Select`, `EmptyState`, `LoadingState`, and `ErrorState`. Use the screenshot reference for information hierarchy only: hero, chips, top actions, left main column, right side cards, membership cards, and contribution empty state. All target sections must exist in 53A: hero, basis data, `Meine Fansub-Geschichte`, avatar card, visibility card, account/security card, memberships, and contribution summary. Where data or contract is missing, render an honest empty, disabled, or unavailable state. Keep `page.tsx` as a thin orchestrator for loading/state/save handlers and move section JSX into `/me/profile/components` before the route file becomes a large blob. Move profile layout CSS to `/me/profile` ownership and remove any dependency where `/me/profile` imports `admin/profile/page.module.css`. Keep local CSS only for profile-specific layout such as hero/avatar grids. Do not create a local sidebar/app-shell and do not hardcode a one-off navigation shell. Avoid nested cards where a flat section or GDS variant is sufficient.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npx eslint "src/app/me/profile/page.tsx" "src/app/admin/profile/page.tsx"</automated>
    <manual>Confirm `page.tsx` remains a thin orchestrator and large section JSX lives in focused components, not one 600+ line route file.</manual>
    <manual>Desktop and mobile visual pass: no text overlap, no page-specific fake sidebar, no hover-only controls.</manual>
  </verify>
  <done>
    The first viewport clearly reads as `Mein Profil`, uses the existing UI system, and no longer feels like a raw admin/backend form.
  </done>
</task>

<task type="auto">
  <name>Task 3: Map real profile fields to semantic controls without inventing data</name>
  <files>frontend/src/app/me/profile/page.tsx, frontend/src/app/me/profile/components, frontend/src/types/profile.ts</files>
  <read_first>
    - frontend/src/types/profile.ts
    - backend/internal/models/member_profile.go
    - backend/internal/repository/member_profile_repository.go
    - docs/agent-guidelines-ui.md
  </read_first>
  <action>
    Build `ProfileBasicsForm` around only existing profile fields: Fansub-Name, Anzeigename, Aktiv seit, Aktuell aktiv, Aktiv bis, Kurzbeschreibung. Use a checkbox/switch-like control for `is_currently_active`, a constrained year input for existing year-only fields, and a character-counted short text control for `bio`. Keep `member_story` visually and structurally separate in `ProfileStoryCard`: label it `Meine Fansub-Geschichte`, preserve existing data, and use the current plain-text persistence behavior in 53A. Do not drop `member_story` and do not pretend rich formatting persists before 53B. Disable `Aktiv bis` while `is_currently_active` is true. If the current API shape cannot support a desired screenshot field, show a conservative empty/unavailable state rather than adding a fake field.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run test -- --run "src/app/admin/profile/page.test.tsx"</automated>
  </verify>
  <done>
    Persisted fields map to semantic controls and the request payload still matches `UpdateMemberProfileRequest`.
  </done>
</task>

<task type="auto">
  <name>Task 4: Separate account data, roles, memberships, and contribution summaries</name>
  <files>frontend/src/app/me/profile/page.tsx, frontend/src/lib/profileLabels.ts, frontend/src/types/profile.ts, frontend/src/types/fansub.ts</files>
  <read_first>
    - frontend/src/types/profile.ts
    - frontend/src/types/fansub.ts
    - frontend/src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.tsx
    - docs/architecture/fansub-member-management.md
    - docs/frontend/auth-api-client.md
  </read_first>
  <action>
    Create readable presentation helpers/components for role labels and status labels. Use `frontend/src/lib/profileLabels.ts` as the canonical home for profile-facing role/status/visibility label formatting when a shared helper is needed; reuse `FANSUB_GROUP_ROLE_OPTIONS` for group/app role labels where it fits instead of duplicating those mappings locally. Render platform roles, active group/app roles, and historical credit roles as distinct groups/chips. Move e-mail, login/account display name, status, and platform roles into `AccountSecurityCard`; never pass this card data into public-profile-ready components. Render memberships as cards with status and app roles, using logo fallback because group logo is not currently in the profile DTO. Render contributions as `Meine Beiträge` using existing aggregate `historical_credits`; if detailed data is missing, show an honest Summary/Empty State and a disabled/deferred `Alle Beiträge anzeigen` action rather than fake rows. Do not create `/me/contributions` or a detail-flow in 53A. Before using capability flags such as `can_view_memberships` or `can_view_historical_credits`, confirm they exist in `MemberProfileData`; if they are missing or false, render a conservative scoped state.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run test -- --run "src/app/admin/profile/page.test.tsx"</automated>
    <manual>Confirm no raw codes such as `platform_admin`, `members_only`, `quality_checker`, or `active` appear unlabelled in the user-facing UI.</manual>
    <manual>Confirm membership and contribution sections follow existing capability flags and do not expose data when capability state is missing or false.</manual>
  </verify>
  <done>
    User-facing profile sections are understandable, account data is isolated, and historical credits are not presented as permissions.
  </done>
</task>

<task type="auto">
  <name>Task 5: Align contracts when the existing profile API shape is touched</name>
  <files>shared/contracts/openapi.yaml, frontend/src/types/profile.ts, frontend/src/lib/api.ts, backend/internal/models/member_profile.go</files>
  <read_first>
    - docs/api/api-contracts.md
    - shared/contracts/openapi.yaml
    - frontend/src/lib/api.ts
    - frontend/src/types/profile.ts
    - backend/internal/handlers/app_profile.go
    - backend/internal/models/member_profile.go
  </read_first>
  <action>
    Document the existing runtime `GET /api/v1/me/profile`, `PUT /api/v1/me/profile`, and `POST /api/v1/me/profile/avatar` API in `shared/contracts/openapi.yaml` as part of 53A, even if no runtime field changes are made. Minimum contract content: operation IDs, request body schemas for update/avatar upload, response schemas covering every field used by `MemberProfileData`, documented 200 success responses, and documented auth/validation errors such as 400 and 401. If 53A changes any response field, request field, status handling, or helper behavior, update the shared contract and typed DTOs in the same change. Do not let page code infer undocumented response variants.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s && git diff --check -- shared/contracts/openapi.yaml frontend/src/types/profile.ts frontend/src/lib/api.ts</automated>
  </verify>
  <done>
    Runtime profile API, frontend DTOs, API helpers, and contract docs stay aligned or any remaining drift is explicitly recorded.
  </done>
</task>

</tasks>

<acceptance_criteria>

- `/me/profile` is the primary own-profile route.
- `/admin/profile` no longer has an independent admin-specific profile implementation and defaults to an internal re-export/transition wrapper.
- `/me/profile` has direct route tests for render/load/error behavior and no admin naming leaks.
- `frontend/src/app/me/profile/page.test.tsx` is newly created or actively populated and contains real assertions; a zero-test file does not satisfy 53A.
- A reusable authenticated app shell or auth-adjacent navigation seam gives normal signed-in members a non-admin path to `Mein Profil`.
- The shell is global/reusable, not profile-local, and only `/me/profile` is migrated to it in 53A.
- Future shell links with no real route are disabled or Coming soon, not fake links.
- `/me/profile` preserves the existing authenticated-user boundary.
- The client component boundary is verified, including a `use client` check or an equivalent thin wrapper/client-leaf test.
- `/me/profile` remains an explicit Client Component or a thin Server Component wrapper plus client leaf; dirty/auth/upload state must not move into an unsafe Server Component.
- `/me/profile` owns its profile CSS under `/me/profile`; it must not import `admin/profile/page.module.css`.
- `page.tsx` remains a thin orchestrator and section JSX is split into focused components before the file exceeds roughly 450 lines.
- Existing own-profile navigation entry points are updated to `/me/profile` or explicitly documented as transitional.
- The UI uses existing GDS/global components before local CSS.
- No persistent mock data, invented fields, or fake contribution rows are introduced.
- `shared/contracts/openapi.yaml` documents the runtime own-profile read/update/avatar API.
- `shared/contracts/openapi.yaml` includes request schemas, response schemas, and auth/validation error responses for the own-profile endpoints.
- Account & Sicherheit is read-only and Keycloak-owned.
- Team4s-owned fields remain editable only through the existing profile contract.
- `bio` stays the short description, while `member_story` remains visible/editable in a separate plain-text story card until safe TipTap persistence lands.
- Roles are separated by kind and displayed with German labels.
- Memberships are card-based and scoped to the user profile, not group management.
- Contributions are honest aggregate/empty-state data until a detail endpoint exists.
- Missing visibility/detail data is conservative and not public by default.
- Existing Keycloak-return dirty-state behavior survives the route/layout recomposition.

</acceptance_criteria>

<recommended_checks>

- `cd C:/Users/admin/Documents/Team4s/frontend && npm run test -- --run "src/app/admin/profile/page.test.tsx"`
- `cd C:/Users/admin/Documents/Team4s/frontend && npm run test -- --run "src/app/me/profile/page.test.tsx"`
- `cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck`
- `cd C:/Users/admin/Documents/Team4s/frontend && npm run lint`
- `cd C:/Users/admin/Documents/Team4s && git diff --check`
- Manual desktop/mobile UI check for `/me/profile` and `/admin/profile` transition behavior.

</recommended_checks>
