---
phase: 53-rollenuebergreifendes-mein-profil-als-member-identity-hub
plan: "02"
type: execute
wave: 2
depends_on:
  - 53-01
files_modified:
  - frontend/src/app/me/profile/page.tsx
  - frontend/src/app/me/profile/page.test.tsx
  - frontend/src/app/me/profile
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/components/layout/AppShell.module.css
  - frontend/src/components/media/crop  # optional: nur wenn Crop-Primitives aus admin verschoben werden
  - frontend/src/components/admin/MediaUpload.tsx
  - frontend/src/components/admin/mediaUploadCropMath.ts
  - frontend/src/components/admin/mediaUploadA11y.ts
  - frontend/src/components/editor/RichTextEditor.tsx
  - frontend/src/components/editor/RichTextRenderer.tsx
  - frontend/src/types/profile.ts
  - frontend/src/lib/api.ts
  - backend/internal/handlers/app_profile.go
  - backend/internal/repository/member_profile_repository.go
  - backend/internal/models/member_profile.go
  - backend/internal/services/tiptap_service.go
  - database/migrations
  - shared/contracts/openapi.yaml
autonomous: true
requirements:
  - MEMBER-PROFILE-HUB-01
must_haves:
  truths:
    - "Avatar upload remains server-validated; SVG is not allowed without a robust sanitizing strategy."
    - "Avatar crop reuses existing project crop math/a11y primitives before adding a new dependency."
    - "Rich text is validated and sanitized before render; no unchecked HTML is rendered."
    - "Visibility defaults conservatively: missing or unclear means not public."
    - "Month/year activity data is not invented from memberships or credits."
    - "Dirty profile fields survive account refresh or partial reloads."
    - "Partial failures are scoped to profile/account/memberships/contributions/avatar instead of crashing the whole page."
    - "Mobile and keyboard accessibility are verified for crop, visibility, forms, and dialogs."
    - "Any new visibility, month/year, rich-text, avatar variant, or contribution-detail behavior must be contract-backed."
    - "If a desired screenshot feature is not backed by runtime data, the UI must show an honest unavailable/deferred state."
    - "D-22: Avatar-Upload bleibt serverseitig autoritativ validiert."
    - "D-23: Erlaubt sind JPG, PNG und WEBP; SVG bleibt verboten, solange kein robustes Sanitizing-Konzept existiert."
    - "D-24: Phase 53 führt `maxAvatarSize = 5 * 1024 * 1024` (5 MB) als profilspezifische Konstante in `app_profile.go` ein. Das geteilte `maxImageSize` (50 MB) aus `media_upload.go` bleibt für alle anderen Upload-Pfade unverändert. Das ist die einzige Backend-Änderung für Avatar-Größe."
    - "D-25: Avatar-Crop soll bestehende Projekt-Crop-Primitives aus `MediaUpload`, `mediaUploadCropMath.ts` und `mediaUploadA11y.ts` wiederverwenden oder die Abweichung dokumentieren."
    - "D-26: Varianten wie `avatar_256`, `avatar_96` und `avatar_48` werden entweder über bestehende `media_files.variant` umgesetzt oder bewusst als Follow-up markiert. Keine parallele Media-Logik."
    - "D-27: Die lange Fansub-Geschichte nutzt nur dann echte TipTap-Persistenz, wenn Backend-Validierung, Sanitizing, Speicherform und Contract bereit sind."
    - "D-28: Keine ungeprüfte HTML-Ausgabe; Links, Nodes und Marks brauchen Allowlist-Regeln."
    - "D-29: Wenn Phase 53 die sichere TipTap-Verdrahtung nicht vollständig schafft, bleibt Plain Text ehrlich sichtbar und Rich Persistence wird deferred."
    - "D-35: Speichern ist nur aktiv, wenn Team4s-Profilfelder dirty sind."
    - "D-36: Keycloak-Rückkehrrefresh und Avatar-Upload dürfen ungespeicherte Team4s-Profilfelder nicht überschreiben."
    - "D-37: Avatar-Änderung und Profilfeld-Speichern bleiben getrennt oder werden mit einem expliziten kombinierten Save-Contract umgesetzt."
    - "D-38: Partielle Fehler werden nur versprochen, wenn Datenquellen/API wirklich getrennt ladbar sind. Wenn `/me/profile` ein Aggregate bleibt, muss der Fehlerzustand ehrlich zum Aggregate passen."
    - "D-39: Mobile, Tastaturbedienung, sichtbare Fokuszustände, Dialog-Fokusführung, ESC-Schließen und keine Hover-only-Aktionen sind Akzeptanzkriterien."
    - "D-42: `member_story` soll echte TipTap-Persistenz bekommen, aber strikt über den vorhandenen Phase-41-Stack. Vorhandene TipTap-Services, Validierung, Sanitizing, `body_json`/`body_html`/`body_text`-Pattern und `RichTextRenderer` werden wiederverwendet; kein zweiter TipTap-Service, kein neuer Renderer und keine parallele Sanitizing-Logik."
    - "D-43: Bestehende Plain-Text-`member_story`-Daten müssen weiter lesbar bleiben. 53B muss dafür Migration oder Legacy-Fallback vorsehen, bevor der Profil-Story-Pfad echtes TipTap als Standard nutzt."
    - "D-44: Avatar-Crop ist clientseitige Pre-Upload-Verarbeitung: `canvas.toBlob()` → neues `File`-Objekt → `uploadOwnProfileAvatar(croppedFile)`. Das Backend speichert das Ergebnis als `original.{ext}` — genau wie das bestehende Upload-System. Pre-Crop-Source-Retention (für zukünftigen Recrop) ist für Phase 53 kein Blocker; als bekannte Einschränkung dokumentieren."
    - "D-50: Mobile-Shell-Härtung kommt in 53B. 53A muss Desktop/Tablet sauber und Mobile nicht kaputt liefern, aber ein finaler Drawer/Burger ist keine 53A-Pflicht."
    - "D-51: Parallelisierung erfolgt nur nach klarer File Ownership. Globale Shell ist ein eigener Block und darf nicht gleichzeitig mit konfliktierenden `/me/profile`-Grundlagen bearbeitet werden; 53B-Backend-/DTO-/OpenAPI-/RichText-/Avatar-Contracts werden seriell oder explizit koordiniert umgesetzt."
    - "D-54: Avatar-Crop braucht Avatar-spezifische Geometrie: erzwungener 1:1-Crop, runde Vorschau und runde Canvas-/Masken-Ausgabe. Bestehende Crop-Math darf wiederverwendet werden, muss aber für Avatar-Geometrie erweitert und getestet werden."
    - "D-55: `/me/profile` darf nicht dauerhaft reusable Crop-Primitives aus `frontend/src/components/admin` importieren. Shared Crop-Code wird in einen media-/app-neutralen Ort verschoben oder eine kurze, bewusste Übergangskopplung wird dokumentiert."
    - "D-56: Avatar entfernen ist nicht implizit Teil von 53B. Ohne `DELETE`-/Remove-Contract darf kein produktiver Entfernen-Button erscheinen; der Remove-Endpunkt wird explizit deferred oder als eigener Contract umgesetzt."
    - "D-57: Contributions-Detail-Ausbau bleibt in Phase 53 fest deferred. Task 6 darf kein optionaler Umsetzungspfad für eine neue Detailroute sein; 53 zeigt Summary/Empty State und bereitet höchstens späteres Routing vor."
    - "D-58: Migrationen und Avatar-Validierung brauchen konkrete Guardrails: vor jeder neuen Migration aktuelle Nummerierung und untracked Migrationen prüfen; Avatar-Size-Verhalten wird bewusst geprüft und dokumentiert. Das generische 50-MB-Image-Limit darf für Profil-Avatare weiter gelten, wenn dies explizit akzeptiert ist und nicht versehentlich passiert."
  artifacts:
    - path: "shared/contracts/openapi.yaml"
      provides: "Expanded contract for avatar, rich text, visibility, and contribution details if runtime behavior changes"
    - path: "backend/internal/services/tiptap_service.go"
      provides: "Server-side TipTap validation/sanitizing reference"
    - path: "frontend/src/components/admin/MediaUpload.tsx"
      provides: "Existing crop UX/math/a11y analog to reuse or extract from"
  key_links:
    - from: "frontend/src/components/admin/mediaUploadCropMath.ts"
      to: "frontend/src/app/me/profile AvatarCropDialog"
      via: "shared crop geometry primitives"
      pattern: "computeCropMetrics|computeCropDrawRect|clampCropOffset"
    - from: "backend/internal/services/tiptap_service.go"
      to: "backend/internal/handlers/app_profile.go"
      via: "server-side validation and sanitized output"
      pattern: "ValidateJSON|RenderHTML|ExtractText"
---

<objective>
Phase 53B hardens the Member Identity Hub after the route/layout foundation: avatar crop and validation, month/year activity contract, safe TipTap rich text, visibility semantics, dirty-state behavior, partial error handling, mobile polish, and accessibility checks.
</objective>

<execution_order>

Although this plan is Wave 2, the implementation tasks are not all safe to run in parallel because several touch the same backend model, handler, repository, and OpenAPI files. Execute in this order unless a coordinator manually splits non-overlapping subchanges:

1. Task 1 avatar crop/upload contract decision.
2. Task 2 avatar validation/variant strategy using Task 1's contract.
3. Task 3a activity-period contract.
4. Task 3b visibility contract.
5. Task 4 rich text storage/rendering after model/contract shape is stable.
6. Task 5a dirty-state/scoped error tests.
7. Task 5b mobile/accessibility QA.
8. Task 6 fixed contribution-detail defer documentation.

</execution_order>

<decision_coverage>

- D-09: OpenAPI documentation expands for any avatar, rich-text, visibility, month/year, or contribution-detail contract changes.
- D-10: Runtime DTOs, frontend DTOs, API helpers, and OpenAPI move together.
- D-12: Missing or unclear visibility is not public.
- D-13: A third visibility option requires DB, backend, OpenAPI, and public-query semantics.
- D-14: The screenshot's three-option visibility UI is only enabled when real values support it.
- D-15: Profile activity period means personal general fansub activity period.
- D-16: Activity dates are not derived from groups, credits, or release roles.
- D-17: Month/year fields require a migration and contract, otherwise year-only remains honest.
- D-22: Avatar upload remains server-validated.
- D-23: JPG, PNG, and WEBP are allowed; SVG remains forbidden without robust sanitizing.
- D-24: Profile avatar size behavior must be consciously checked and documented; the existing generic 50 MB image limit is acceptable for avatars.
- D-25: Avatar crop reuses existing project crop primitives or documents a deviation.
- D-26: Avatar variants use `media_files.variant` or are deferred.
- D-27: Rich text gets true TipTap persistence only after backend validation, sanitizing, storage, and contract are ready.
- D-28: No unchecked HTML rendering; links, nodes, and marks need allowlists.
- D-29: If safe TipTap cannot be completed, plain text remains honest and rich persistence is deferred.
- D-35: Save is active only for dirty Team4s profile fields.
- D-36: Keycloak-return refresh and avatar upload must not overwrite dirty Team4s fields.
- D-37: Avatar change and profile-field save stay separate unless a combined contract is added.
- D-38: Partial failures are only promised where data/API boundaries support them.
- D-39: Mobile, keyboard access, visible focus, dialog focus, ESC close, and no hover-only actions are acceptance criteria.
- D-42: Profile `member_story` TipTap persistence must reuse the existing Phase 41 stack and must not duplicate services, renderers, or sanitizers.
- D-43: Existing plain-text `member_story` data must remain readable through migration or legacy fallback.
- D-44: Avatar crop is client-side pre-upload processing; backend already stores the cropped result as `original.{ext}`; pre-crop source retention is a documented future limitation, not a Phase 53 requirement.
- D-50: Final mobile shell hardening belongs in 53B.
- D-51: 53B backend/DTO/OpenAPI/RichText/avatar work must be serialized or explicitly coordinated.
- D-54: Avatar crop requires avatar-specific 1:1 and circular preview/output geometry tests.
- D-55: `/me/profile` must not permanently import shared crop primitives from `frontend/src/components/admin`.
- D-56: Avatar remove is deferred unless a DELETE/remove contract is implemented.
- D-57: Contributions detail expansion is fixed deferred in Phase 53.
- D-58: Migration numbering/untracked files and consciously documented avatar size behavior are required guardrails.

</decision_coverage>

<context>
@AGENTS.md
@.planning/phases/53-rollenuebergreifendes-mein-profil-als-member-identity-hub/53-CONTEXT.md
@.planning/phases/53-rollenuebergreifendes-mein-profil-als-member-identity-hub/53-RESEARCH.md
@docs/engineering/implementation-contract.md
@docs/frontend/ui-system.md
@docs/agent-guidelines-ui.md
@docs/frontend/ui-inventory.md
@docs/api/api-contracts.md
@docs/frontend/auth-api-client.md
@docs/architecture/db-schema-fansub-domain.md
@frontend/src/app/me/profile/page.tsx
@frontend/src/app/admin/profile/page.tsx
@frontend/src/app/admin/profile/page.test.tsx
@frontend/src/components/admin/MediaUpload.tsx
@frontend/src/components/admin/mediaUploadCropMath.ts
@frontend/src/components/admin/mediaUploadA11y.ts
@frontend/src/components/editor/RichTextEditor.tsx
@frontend/src/components/editor/RichTextRenderer.tsx
@frontend/src/types/profile.ts
@frontend/src/lib/api.ts
@backend/internal/handlers/app_profile.go
@backend/internal/repository/member_profile_repository.go
@backend/internal/models/member_profile.go
@backend/internal/services/tiptap_service.go
@shared/contracts/openapi.yaml
</context>

<discovery>

## 53B Ausgangslage

- Avatar: Das Upload-System ist bereits vollständig vorhanden. `uploadOwnProfileAvatar(file: File)` in `frontend/src/lib/api.ts` sendet FormData an `POST /api/v1/me/profile/avatar`; `UploadOwnProfileAvatar` in `backend/internal/handlers/app_profile.go` validiert via `detectAvatarImage()` und speichert `original.{ext}` unter `media/profile/{memberID}/avatar/{mediaID}/`. Was tatsächlich fehlt: (1) `AvatarCropDialog` als clientseitige Pre-Upload-Crop-UX (canvas-based, vor dem Upload); (2) `maxAvatarSize = 5 MB` als profilspezifische Konstante in `app_profile.go` statt dem geteilten 50-MB-Limit. Kein neues Upload-System bauen.
- Crop analog: `frontend/src/components/admin/MediaUpload.tsx` already contains project-owned crop UI, keyboard support, focus handling, and crop math helpers for group logo media. Prefer extracting/reusing focused primitives instead of adding `react-easy-crop` unless extraction is clearly worse.
- Rich text: `RichTextEditor` exists, but the profile page currently converts editor JSON to plain text before saving `member_story`. Backend `TipTapService` can validate JSON, render sanitized HTML, and extract text, but the profile API is not wired to it.
- Visibility: profile visibility has only `members_only` and `public`. The requested `Für Gruppen sichtbar` value needs a contract/DB decision before it is enabled.
- Activity period: profile fields are `active_from_year` and `active_until_year`. Month/year must not be derived from memberships or credits.
- Contributions: current `historical_credits` are aggregate-only. A detailed, paginated contributions endpoint does not exist.
- Membership card richness: current `MemberProfileMembership` lacks group logo data, so richer cards require a DTO/contract extension or fallback icons.
- OpenAPI: the existing own-profile endpoints are not documented in `shared/contracts/openapi.yaml`; 53A should add the baseline, and 53B must expand it for any avatar/rich-text/visibility/month-year/detail changes.

## 53B Target Contract Direction

- Avatar crop can be client-cropped to a safe raster file before upload, or backend can accept explicit crop metadata and generate variants. The plan must pick one contract and document why.
- Avatar validation must explicitly allow JPG, PNG, WEBP; reject SVG; document the accepted size behavior, including the existing 50 MB limit if kept; and keep server validation authoritative.
- Rich text should store TipTap JSON plus sanitized HTML/text only after validating allowed nodes/marks/links. If migration is required, add a new migration rather than editing historical migrations.
- Visibility should either stay limited to current values with clear UI copy, or add a third durable value such as `groups_visible` with defaults and public-query behavior. Do not show a fake working option.
- Month/year should be represented by explicit contract fields or a documented date encoding, not by guessing from group membership dates.
- Contributions should remain aggregate-only unless 53B explicitly adds a paginated detail endpoint and contract. Do not fake Anime/Episode/Release-Version rows from summary counts.

## Risiken / offene Fragen

- A three-option visibility UI (`Nur intern`, `Für Gruppen sichtbar`, `Öffentlich sichtbar`) matches the screenshot direction but cannot be fully interactive until the backend and public-query semantics support the third value.
- Month/year activity may need a migration. If no migration is added, the UI must stay year-only and clearly constrained.
- Rich text may require new durable fields for TipTap JSON and sanitized HTML/text. If those fields are not added, the editor must not pretend rich formatting persists.
- Existing `member_story` values are legacy plain text. TipTap persistence is a data migration or fallback-renderer problem, not only a schema problem; old stories must continue to render correctly.
- Avatar variants should reuse `media_files.variant`; if derivative generation is too large for this phase, document the follow-up instead of inventing a parallel avatar table.
- Das Backend speichert bereits `original.{ext}` — das hochgeladene (gecropte) Bild wird als neues Original gespeichert. Pre-Crop-Source-Retention (das Bild vor dem Crop) ist für Phase 53 kein Blocker; wenn spätere Phasen Recrop vom ungeschnittenen Original benötigen, ist eine separate Strategie nötig. Als bekannte Einschränkung dokumentieren.
- Existing crop math primitives may be mouse-oriented. Mobile support requires explicit pointer/touch handling (`pointerdown`/`pointermove`/`pointerup` or equivalent touch events), not only geometry reuse.
- Partial failures depend on API shape. If `/me/profile` remains one aggregate response, section-level partial failures may only apply to separate avatar/upload/save actions, not initial membership/contribution reads.
- Wave 2 touches overlapping files (`app_profile.go`, `member_profile.go`, repositories, DTOs, and `openapi.yaml`); tasks must be sequenced or coordinated to avoid merge conflicts.

## Security / Privacy Invariants

- Account data never goes into public-ready components.
- Public-profile-ready components receive only public-safe fields.
- Rich text never renders unsanitized HTML and never allows scripts, iframes, inline event handlers, or unsafe links.
- Historical credits remain read-only display data and do not grant capabilities.
- Frontend UI may use backend capability booleans, but backend remains authoritative for mutations.

</discovery>

<tasks>

<task type="auto">
  <name>Task 1: Decide and document the avatar crop/upload contract</name>
  <files>frontend/src/app/me/profile/page.tsx, frontend/src/components/media/crop, frontend/src/components/admin/mediaUploadCropMath.ts, frontend/src/components/admin/mediaUploadA11y.ts, backend/internal/handlers/app_profile.go, shared/contracts/openapi.yaml</files>
  <read_first>
    - frontend/src/components/admin/MediaUpload.tsx
    - frontend/src/components/admin/mediaUploadCropMath.ts
    - frontend/src/components/admin/mediaUploadA11y.ts
    - backend/internal/handlers/app_profile.go
    - backend/internal/repository/member_profile_repository.go
    - docs/api/api-contracts.md
  </read_first>
  <action>
    Das Avatar-Upload-System ist bereits vollständig vorhanden — kein neues Upload-System bauen. `uploadOwnProfileAvatar(file: File)` in `frontend/src/lib/api.ts` und `UploadOwnProfileAvatar` in `backend/internal/handlers/app_profile.go` sind fertig. Kein neuer Upload-Transport, kein Page-lokaler Upload-Helper, keine neue Media-Tabelle, kein neuer Storage-Pfad.

    Was zu bauen ist: `AvatarCropDialog` — eine clientseitige Pre-Upload-Crop-UX:
    1. Nutzer wählt Bild → Dialog öffnet mit Crop/Zoom-Ansicht
    2. Drag-to-position, Zoom-Slider, Circular-Preview
    3. `canvas.toBlob()` → neues `File`-Objekt erstellen
    4. `uploadOwnProfileAvatar(croppedFile)` aufrufen — fertig, keine Änderung am Upload-Pfad nötig

    Crop-Primitives: `mediaUploadCropMath.ts` und `mediaUploadA11y.ts` aus `frontend/src/components/admin/` wiederverwenden. Der `AvatarCropDialog` ist eine einzelne Dialog-Datei — diese transitionale Kopplung an `components/admin/` ist akzeptabel und stellt keine strukturelle Kopplung der gesamten Route dar (D-55). Optional: Primitives nach `frontend/src/components/media/crop` verschieben; wenn verschoben, in demselben Commit alle Imports in `frontend/src/components/admin/MediaUpload.tsx` auf den neuen Pfad aktualisieren. Der Move ist keine Phase-53-Pflicht.

    Avatar-Geometrie: 1:1-Crop-Constraint und Circular-Preview/Canvas-Mask ergänzen. Die bestehenden rechteckigen Crop-Primitives für Avatar-Kontext erweitern; diese Erweiterungen gezielt testen (1:1-Output, runde Maske).

    Touch/Pointer-Events: `pointerdown/pointermove/pointerup` neben Mouse-Events implementieren — Mobilbedienung muss funktionieren.

    Dialog-UX: Drag-to-Position, Touch-Drag, Zoom-Slider, Round-Preview, Small-Preview, Cancel, Apply, ESC-Schließen, Focus-Trap.

    Source-Retention: Das Backend speichert das hochgeladene Bild bereits als `original.{ext}`. Pre-Crop-Source-Retention ist für Phase 53 kein Blocker. Als bekannte Einschränkung dokumentieren: wenn spätere Phasen Recrop vom ungeschnittenen Original benötigen, ist eine separate Retention-Strategie erforderlich.

    Avatar-Upload von Profil-Felder-Speichern getrennt halten, außer ein expliziter Combined-Save-Contract wird hinzugefügt.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm test -- --run src/components/admin/mediaUploadCropMath.test.ts src/components/admin/mediaUploadA11y.test.ts src/components/media/crop/*.test.ts</automated>
    <manual>Avatar crop dialog is mouse, touch/pointer, and keyboard usable; ESC closes it and focus remains inside the dialog.</manual>
  </verify>
  <done>
    Avatar crop behavior is implemented or precisely contract-documented, and existing crop primitives are reused or a deviation is recorded.
  </done>
</task>

<task type="auto">
  <name>Task 2: Harden avatar validation and variant strategy</name>
  <files>backend/internal/handlers/app_profile.go, backend/internal/repository/member_profile_repository.go, backend/internal/models/member_profile.go, frontend/src/types/profile.ts, shared/contracts/openapi.yaml</files>
  <read_first>
    - backend/internal/handlers/app_profile.go
    - backend/internal/handlers/media_upload.go
    - backend/internal/handlers/app_auth_test.go
    - backend/internal/repository/member_profile_repository.go
    - backend/internal/models/media.go
  </read_first>
  <action>
    Die einzige Backend-Änderung für Avatar-Validierung in Phase 53: Konstante `maxAvatarSize = 5 * 1024 * 1024` (5 MB) in `backend/internal/handlers/app_profile.go` definieren und in `detectAvatarImage` statt `maxImageSize` verwenden. Das geteilte `maxImageSize` (50 MB aus `media_upload.go`) bleibt für alle anderen Upload-Pfade unverändert.

    SVG-Block prüfen: `allowedImageMimeTypes` in `media_upload.go` lesen. Wenn `image/svg+xml` nicht als erlaubter Typ eingetragen ist, ist SVG bereits geblockt — kein weiterer Handlungsbedarf. Falls SVG versehentlich erlaubt wäre, entfernen. Einen gezielten Test schreiben, der SVG-Upload-Ablehnung bestätigt.

    Kein neues Validierungssystem, kein neuer Upload-Pfad. `detectAvatarImage()` erledigt MIME-Typ-Prüfung und Bild-Dimensions-Validierung bereits.

    Entscheiden ob 53B `avatar_256`, `avatar_96`, `avatar_48`-Varianten jetzt erstellt oder als Follow-up dokumentiert. Varianten deferren, wenn derivative Generierung mehr als eine kleine, fokussierte Erweiterung von `media_files.variant` erfordert. Keine parallelen Media-Tabellen erfinden. Keinen produktiven „Bild entfernen"-Button hinzufügen, außer ein DELETE/Remove-Endpoint und OpenAPI-Contract sind implementiert; Avatar-Remove explizit als deferred dokumentieren.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/handlers ./internal/repository</automated>
    <automated>cd C:/Users/admin/Documents/Team4s && git diff --check -- backend/internal/handlers/app_profile.go backend/internal/repository/member_profile_repository.go shared/contracts/openapi.yaml</automated>
  </verify>
  <done>
    Avatar uploads are server-validated, unsafe types are rejected, and variant behavior is either implemented through existing media structures or explicitly deferred.
  </done>
</task>

<task type="auto">
  <name>Task 3a: Resolve the personal activity-period contract</name>
  <files>backend/internal/models/member_profile.go, backend/internal/repository/member_profile_repository.go, backend/internal/handlers/app_profile.go, frontend/src/types/profile.ts, frontend/src/app/me/profile/page.tsx, shared/contracts/openapi.yaml</files>
  <read_first>
    - backend/internal/models/member_profile.go
    - backend/internal/repository/member_profile_repository.go
    - database/migrations
    - frontend/src/types/profile.ts
    - docs/api/api-contracts.md
    - docs/agent-guidelines-ui.md
  </read_first>
  <action>
    For activity period, first inspect `database/migrations` for the current highest migration number and check `git status --short database/migrations` for untracked migration files. Stop and report a migration-chain risk if another untracked migration exists. Then check whether a DB migration already added month/date fields. If not, choose between:
    - keep year-only controls in 53B and document month/year as future contract work, or
    - add a reversible migration and contract fields for month/year, using a stable encoding such as `YYYY-MM` or date-first-of-month with UI month/year display.

    Reversible means a new migration with a safe down path, nullable/backfilled fields where old profiles exist, and no destructive conversion of existing year-only data without an explicit precondition check. Do not derive personal active period from group memberships or credits. If `is_currently_active` is true, disable or clear the editable active-until control according to the chosen contract.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/handlers ./internal/repository ./internal/migrations</automated>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck</automated>
  </verify>
  <done>
    Activity period UI cannot imply unsupported semantics, and any new durable fields are migration-safe and contract-backed.
  </done>
</task>

<task type="auto">
  <name>Task 3b: Resolve the profile visibility contract</name>
  <files>backend/internal/models/member_profile.go, backend/internal/repository/member_profile_repository.go, backend/internal/handlers/app_profile.go, frontend/src/types/profile.ts, frontend/src/app/me/profile/page.tsx, shared/contracts/openapi.yaml</files>
  <read_first>
    - backend/internal/models/member_profile.go
    - backend/internal/repository/member_profile_repository.go
    - backend/internal/handlers/app_profile.go
    - frontend/src/types/profile.ts
    - shared/contracts/openapi.yaml
    - docs/api/api-contracts.md
  </read_first>
  <action>
    Resolve visibility independently from activity-period work. If this task needs a migration, first inspect `database/migrations` for the current highest migration number and check `git status --short database/migrations` for untracked migration files; do not race Task 3a's migration number. Either keep the current `members_only | public` contract and hide or disable the screenshot's third option, or add a third durable value with backend validation, default/backfill semantics, OpenAPI docs, frontend DTOs, and public-query behavior. Missing/unknown visibility remains internal. Do not make `Für Gruppen sichtbar` clickable unless the backend contract and relevant public/member query semantics are complete.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/handlers ./internal/repository</automated>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck</automated>
  </verify>
  <done>
    Visibility UI matches the real contract, defaults conservatively, and never implies unsupported public/group semantics.
  </done>
</task>

<task type="auto">
  <name>Task 4: Wire profile rich text through safe TipTap storage/rendering</name>
  <files>backend/internal/services/tiptap_service.go, backend/internal/handlers/app_profile.go, backend/internal/repository/member_profile_repository.go, backend/internal/models/member_profile.go, database/migrations, frontend/src/components/editor/RichTextEditor.tsx, frontend/src/components/editor/RichTextRenderer.tsx, frontend/src/app/me/profile/page.tsx, shared/contracts/openapi.yaml</files>
  <read_first>
    - backend/internal/services/tiptap_service.go
    - backend/internal/services/tiptap_service_test.go
    - database/migrations
    - backend/internal/models/member_profile.go
    - backend/internal/repository/member_profile_repository.go
    - frontend/src/components/editor/RichTextEditor.tsx
    - frontend/src/components/editor/RichTextRenderer.tsx
    - frontend/src/app/admin/profile/page.tsx
    - docs/frontend/ui-system.md
  </read_first>
  <action>
    Replace the current profile-page plain-text conversion with a safe TipTap path only after the backend contract is ready. First inspect `database/migrations`, `member_profile.go`, and the repository to confirm the current storage shape of `member_story`; treat existing plain-text values as production data that must keep rendering. Before adding any schema migration, inspect the current highest migration number and `git status --short database/migrations`; coordinate with Task 3a/3b so migration numbers do not collide. Reuse the existing Phase 41 stack: `tiptap_service.go`, the existing validated/sanitized `body_json`/`body_html`/`body_text` pattern, and `RichTextRenderer`. Do not create a second TipTap service, a second renderer, or parallel sanitizer. Decide explicitly between a data migration from legacy plain text to valid TipTap JSON, dual fields with a legacy-text fallback renderer, or deferring rich persistence. If schema changes are required, add a new reversible migration before adjusting backend handlers; if a data migration is added, include rollback/default behavior for legacy text. Validate allowed TipTap JSON server-side, render sanitized HTML with the existing TipTap service, extract text for previews/search if needed, and store fields in a documented durable shape. Inspect `RichTextRenderer.tsx` before using it in the profile path and record whether it currently uses `dangerouslySetInnerHTML`; if it does, it may only receive sanitized backend output and must not render client-supplied or unchecked HTML. Keep the editor toolbar intentionally small for profile story use: paragraphs, headings, bold/italic, lists, quotes, links only if safe link handling is implemented, undo/redo.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/services ./internal/handlers ./internal/repository</automated>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm test -- --run src/components/editor/RichTextEditor.test.tsx</automated>
    <manual>Load or seed an existing plain-text `member_story` and confirm it renders correctly after the TipTap change or fallback path.</manual>
    <manual>Attempt a story payload containing `<script>alert(1)</script>`, iframe, inline event handlers, and unsafe links; rendered output must not contain executable script, iframes, inline events, or unsafe hrefs.</manual>
  </verify>
  <done>
    Profile story rich text is persisted and rendered only through the validated/sanitized TipTap path, or the phase explicitly defers rich persistence and keeps plain text honest.
  </done>
</task>

<task type="auto">
  <name>Task 5a: Add dirty-state, save-state, and scoped error tests</name>
  <files>frontend/src/app/me/profile/page.tsx, frontend/src/app/me/profile/page.test.tsx, frontend/src/app/admin/profile/page.test.tsx</files>
  <read_first>
    - frontend/src/app/me/profile/page.test.tsx
    - frontend/src/app/admin/profile/page.test.tsx
    - frontend/src/components/ui/ErrorState.tsx
    - frontend/src/components/ui/LoadingState.tsx
    - docs/agent-guidelines-ui.md
  </read_first>
  <action>
    Ensure save is enabled only when profile fields are dirty, warn on navigation with unsaved changes where feasible, and keep avatar changes distinct from profile-field changes unless a deliberate combined-save contract exists. Preserve Phase 52 Keycloak-return dirty-field protection. Scope loading and error states separately for profile/account, avatar, memberships, and contributions only where the API shape supports separate failure boundaries; otherwise keep aggregate errors honest and local. Add tests for save disabled/enabled state, dirty-field preservation after refresh, avatar upload error, aggregate profile error behavior, and partial contribution/membership failure only if the API is split.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run test -- --run "src/app/me/profile/page.test.tsx"</automated>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run test -- --run "src/app/admin/profile/page.test.tsx"</automated>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck</automated>
  </verify>
  <done>
    The profile hub is resilient to unsaved edits, Keycloak-return refresh, avatar-save separation, and honest aggregate/scoped failures.
  </done>
</task>

<task type="auto">
  <name>Task 5b: Verify mobile layout and accessibility</name>
  <files>frontend/src/app/me/profile/page.tsx, frontend/src/app/me/profile/page.module.css, frontend/src/components/layout/AppShell.tsx, frontend/src/components/layout/AppShell.module.css, frontend/src/components/ui/Modal.tsx, frontend/src/components/ui/Drawer.tsx</files>
  <read_first>
    - frontend/src/components/ui/Modal.tsx
    - frontend/src/components/ui/Drawer.tsx
    - frontend/src/components/ui/Button.tsx
    - frontend/src/components/ui/FormField.tsx
    - docs/agent-guidelines-ui.md
    - docs/frontend/ui-system.md
  </read_first>
  <action>
    Verify mobile layout separately from dirty-state logic: the 53A global shell gets its final mobile drawer/burger or equivalent authenticated mobile pattern here, profile sections collapse to one column, controls remain touch-sized, editor toolbar wraps without overlap, visibility controls are keyboard accessible, avatar crop dialog traps focus and closes with ESC, focus states are visible, and no interaction depends on hover only. Keep fixes scoped to existing GDS/layout primitives and the reusable shell; do not create a profile-only shell or new page-specific UI system.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run lint</automated>
    <manual>Manual UI pass on desktop, tablet, and mobile viewport for no overlap, no hover-only actions, visible focus states, keyboard operation, and usable dialog controls.</manual>
  </verify>
  <done>
    Mobile and accessibility behavior is verified independently from data/save behavior.
  </done>
</task>

<task type="auto">
  <name>Task 6: Defer contribution detail expansion without faking data</name>
  <files>backend/internal/repository/member_profile_repository.go, backend/internal/handlers/app_profile.go, backend/internal/models/member_profile.go, frontend/src/types/profile.ts, frontend/src/lib/api.ts, shared/contracts/openapi.yaml</files>
  <read_first>
    - backend/internal/repository/member_profile_repository.go
    - backend/internal/repository/contributor_dashboard_repository.go
    - backend/internal/repository/release_version_notes_repository.go
    - docs/architecture/db-schema-fansub-domain.md
  </read_first>
  <action>
    Keep Phase 53 fixed on aggregate `historical_credits` with an honest `Meine Beiträge` summary/empty state. Do not implement a paginated contributions endpoint or `/me/contributions` detail route in Phase 53. If a future detail design is useful, document a follow-up contract such as `GET /api/v1/me/contributions?limit=10&cursor=...`, but do not wire it now. `Alle Beiträge anzeigen` may only be disabled, Coming soon, or omitted unless a real route already exists. Never infer permissions or active roles from historical credits.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/repository ./internal/handlers</automated>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck</automated>
  </verify>
  <done>
    Contributions stay aggregate-only in Phase 53 and are honestly represented as summaries/empty states with no fake rows.
  </done>
</task>

</tasks>

<acceptance_criteria>

- Avatar crop supports drag, zoom, circular preview, cancel, and apply without breaking keyboard access.
- Avatar crop supports touch/pointer drag on mobile, not only mouse drag.
- Avatar crop enforces 1:1 avatar geometry and circular preview/output behavior with focused tests.
- `/me/profile` does not permanently import reusable crop primitives from `frontend/src/components/admin`; shared crop code is moved to a neutral location or the transition is explicitly documented.
- Pre-crop source retention is documented as a known future limitation; Phase 53 does not require storing the pre-crop original because the backend already stores the cropped result as `original.{ext}`.
- Avatar upload enforces server-side type, size, and image validity checks; SVG is rejected.
- Avatar size behavior is checked, documented, and tested; the existing generic 50 MB image limit may remain if accepted explicitly.
- Avatar remove is deferred unless a DELETE/remove endpoint and OpenAPI contract are implemented.
- Avatar variants are either implemented through `media_files` variants or explicitly deferred with a documented reason.
- Any new migration starts after checking migration numbering and untracked migration files.
- Activity period UI matches the real data contract and never derives personal active dates from memberships/credits.
- Any activity-period migration is reversible, non-destructive, and contract-backed.
- Visibility UI either matches the current two-value contract or ships a documented three-value contract with conservative defaults.
- Profile rich text is validated and sanitized server-side before render.
- TipTap persistence is preceded by an explicit DB schema check and `RichTextRenderer` security check.
- Existing plain-text `member_story` values still render correctly through a migration or legacy fallback path.
- Dirty-state behavior prevents accidental loss of unsaved Team4s profile edits.
- Partial failures are local to the failed section where the API is split; aggregate profile reads otherwise use honest aggregate error handling.
- Mobile profile layout is one-column and touch-friendly.
- Dialogs, controls, radios/selects, buttons, and editor/crop tools are keyboard accessible.
- Contributions remain aggregate-only in Phase 53; paginated detail expansion is deferred.
- 53B execution is serialized or explicitly coordinated because tasks edit overlapping backend/contract files.

</acceptance_criteria>

<recommended_checks>

- `cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/services ./internal/handlers ./internal/repository ./internal/migrations`
- `cd C:/Users/admin/Documents/Team4s/frontend && npm run test -- --run "src/app/me/profile/page.test.tsx"`
- `cd C:/Users/admin/Documents/Team4s/frontend && npm run test -- --run "src/app/admin/profile/page.test.tsx"`
- `cd C:/Users/admin/Documents/Team4s/frontend && npm test -- --run src/components/admin/mediaUploadCropMath.test.ts src/components/admin/mediaUploadA11y.test.ts src/components/editor/RichTextEditor.test.tsx`
- `cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck`
- `cd C:/Users/admin/Documents/Team4s/frontend && npm run lint`
- `cd C:/Users/admin/Documents/Team4s && git diff --check`
- Manual desktop/mobile UI check for `/me/profile`.
- Manual security check for rich-text XSS payloads and unsafe avatar files.

</recommended_checks>
