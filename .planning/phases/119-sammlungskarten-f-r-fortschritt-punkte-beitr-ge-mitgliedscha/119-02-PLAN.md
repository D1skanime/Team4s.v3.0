---
phase: 119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha
plan: "02"
type: execute
wave: 2
depends_on: [119-01]
files_modified:
  - backend/internal/models/member_profile.go
  - backend/internal/repository/member_profile_repository.go
  - backend/internal/repository/member_profile_progress_repository.go
  - backend/internal/repository/member_profile_repository_postgres_test.go
  - backend/internal/repository/member_profile_contribution_badges_repository_test.go
  - backend/internal/handlers/app_public_profile_test.go
  - shared/contracts/openapi.yaml
  - frontend/src/types/profile.ts
  - frontend/src/types/__tests__/v12-projection-contract.test.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-06, D-07, D-08]
must_haves:
  truths:
    - "Das bestehende sichtbarkeitsgeprüfte Memberprofil liefert exakte Rohwerte für Fortschritt, Punkte, drei Beitragsfamilien und Mitgliedschaft."
    - "Unterhalb der ersten Stufe bleibt ein Fortschrittswert eine Metrik und wird nicht als verdientes Badge ausgegeben."
    - "Go-Runtime, OpenAPI und TypeScript spiegeln denselben additiven badge_progress-Vertrag ohne neuen Endpoint oder API-Helfer."
  artifacts:
    - {path: backend/internal/models/member_profile.go, provides: "PublicMemberBadgeProgress and badge_progress field"}
    - {path: backend/internal/repository/member_profile_progress_repository.go, provides: "Exact project, points, contribution and membership projection on MemberProfileRepository"}
    - {path: shared/contracts/openapi.yaml, provides: "Canonical additive public profile contract"}
    - {path: frontend/src/types/profile.ts, provides: "Matching browser DTO"}
  key_links:
    - {from: backend/internal/repository/member_profile_repository.go, to: backend/internal/repository/member_profile_progress_repository.go, via: "GetPublicMemberProfile loads metrics after visibility gate"}
    - {from: backend/internal/repository/member_profile_progress_repository.go, to: backend/internal/repository/member_profile_contribution_badges_repository.go, via: "reuse loadContribProjectsCount/loadContribChronicleCount/loadContribArchivistCount"}
    - {from: shared/contracts/openapi.yaml, to: frontend/src/types/profile.ts, via: "v12 exact-key parity test"}
---

<objective>
Expose authoritative collection-family progress through the existing public member-profile response.

Purpose: Give the UI exact progress below, at and above badge thresholds without browser reconstruction, visibility leakage, a parallel endpoint or persistence.
Output: Tested additive Go repository projection and synchronized Go/OpenAPI/TypeScript contract.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha/119-CONTEXT.md
@.planning/phases/119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha/119-RESEARCH.md
@.planning/phases/119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha/119-PATTERNS.md
@.planning/phases/119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha/119-VALIDATION.md
@.planning/phases/119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha/119-01-SUMMARY.md
@AGENTS.md
@docs/engineering/implementation-contract.md
@docs/api/api-contracts.md
@docs/frontend/auth-api-client.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project exact family metrics inside the existing gated profile read</name>
  <files>backend/internal/models/member_profile.go, backend/internal/repository/member_profile_repository.go, backend/internal/repository/member_profile_progress_repository.go, backend/internal/repository/member_profile_repository_postgres_test.go, backend/internal/repository/member_profile_contribution_badges_repository_test.go</files>
  <read_first>
    backend/internal/models/member_profile.go
    backend/internal/repository/member_profile_repository.go
    backend/internal/repository/member_profile_repository_postgres_test.go
    backend/internal/repository/member_profile_contribution_badges_repository.go
    backend/internal/repository/member_profile_contribution_badges_repository_test.go
    backend/internal/services/badge_service.go
    backend/internal/repository/member_profile_role_volume_repository.go
    .planning/phases/118-rollenfortschritt-als-eigene-card-je-tats-chlich-ausge-bter-/118-01-SUMMARY.md
  </read_first>
  <action>Create models.PublicMemberBadgeProgress with stable `family`, `current_count`, nullable `next_threshold`, nonnegative `remaining_count`, nullable `next_tier`, and `complete`, and add non-nil `badge_progress` to PublicMemberProfile. Implement `loadBadgeProgress` on the existing MemberProfileRepository in `member_profile_progress_repository.go` solely as a line-limit extraction. Reuse `loadContribProjectsCount`, `loadContribChronicleCount`, `loadContribArchivistCount`; reuse/extract the confirmed distinct-anime SQL semantics from `BadgeService.computeProductiveTiers`; use existing total-points loading; compute membership as the greatest duration of one `hist_fansub_group_members` row using `COALESCE(left_date,CURRENT_DATE)` and never sum memberships or use profile dates. Emit stable families `progress`, `points`, `contribution_projects`, `contribution_chronicle`, `contribution_archivist`, `membership` even at zero so D-07/D-08 can show goals. Keep `founding_member` earned-only in public_badges and do not present it as an attainable next tier for non-founders. Call this loader only inside GetPublicMemberProfile after its existing visibility/owner gate; keep loadPublicBadges filtering `status='active' AND visibility='public'` unchanged. Add no handler, endpoint, repository type, migration, persisted row or duplicate counting SQL.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/repository -run 'Test.*(Badge|Progress|Membership|Contribution|PublicProfile)' -count=1 &amp;&amp; git diff --check</automated></verify>
  <done>All six family metrics are exact at zero/boundary/terminal fixtures, memberships are not summed, and earned badge visibility remains a separate truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Synchronize contracts and prove visibility minimization</name>
  <files>backend/internal/handlers/app_public_profile_test.go, shared/contracts/openapi.yaml, frontend/src/types/profile.ts, frontend/src/types/__tests__/v12-projection-contract.test.ts</files>
  <read_first>
    backend/internal/models/member_profile.go
    backend/internal/handlers/app_public_profile_test.go
    shared/contracts/openapi.yaml
    frontend/src/types/profile.ts
    frontend/src/types/__tests__/v12-projection-contract.test.ts
    frontend/src/lib/api.ts
    frontend/src/app/members/[slug]/page.tsx
    frontend/src/app/members/[slug]/page.test.tsx
    docs/api/api-contracts.md
    docs/frontend/auth-api-client.md
  </read_first>
  <action>Add PublicMemberBadgeProgress and PublicMemberProfileData.badge_progress to the canonical OpenAPI and TypeScript DTO with the exact Go names, required fields and nullable terminal fields. Extend the existing v12-projection-contract exact-key checks; do not create a second DTO family outside profile.ts and do not change `getMemberProfile`. Extend `TestGetPublicMemberProfile` cases to prove anonymous public metrics, no data/metrics for anonymous or other-user `members_only`, and owner-visible metrics through the existing optional SSR auth gate. This is a read-only public SSR path: do not invoke central refresh, add client auth gating, direct cookie reads, bearer construction or token props. If production changes become necessary in getMemberProfile/authorizedFetch/OwnHiddenProfilePreview, add the missing-access-token plus valid-refresh-token regression through the central API client before completing.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/handlers -run TestGetPublicMemberProfile -count=1 &amp;&amp; docker compose exec -T team4sv30-frontend npm test -- --run src/types/__tests__/v12-projection-contract.test.ts 'src/app/members/[slug]/page.test.tsx' &amp;&amp; docker compose exec -T team4sv30-frontend npm run typecheck &amp;&amp; git diff --check</automated></verify>
  <done>Runtime and three contract surfaces agree, and exact metrics cannot bypass existing public/members_only minimization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| database rows → MemberProfileRepository | Private contribution and membership facts become aggregate public metrics. |
| repository → existing public-profile visibility gate | Aggregates must not exist in hidden responses. |
| Go JSON → OpenAPI → TypeScript | Optional/nullable semantics must remain identical. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-119-01 | Information Disclosure | GetPublicMemberProfile/badge_progress | mitigate | HIGH gate: load only behind existing visibility/owner decision; handler tests cover public, anonymous hidden, other hidden and owner-visible responses. |
| T-119-02 | Tampering | PublicMemberBadgeProgress | mitigate | HIGH gate: server derives counts and terminal state; client supplies no metric and raw metrics never enter public_badges. |
| T-119-06 | Information Disclosure | membership metric | mitigate | Expose only aggregate maximum duration, never group names, dates or row-level membership data. |
| T-119-07 | Denial of Service | profile repository | mitigate | Reuse existing count loaders and one profile read; no API fan-out or per-stage queries. |
| T-119-04 | Elevation of Privilege | SSR optional auth | accept | No auth behavior changes; existing owner gate remains authoritative and public access invokes no client refresh seam. |

## Security Baseline and Blocking Gate
OWASP ASVS L1. Unresolved HIGH T-119-01 or T-119-02 blocks completion.
</threat_model>

<verification>Run focused repository, handler, contract and page tests, frontend typecheck and git diff --check. Inspect that no route/helper/migration/auth seam appears in the diff.</verification>
<success_criteria>D-01–D-04/D-06–D-08 receive authoritative exact metrics from one minimized public profile response.</success_criteria>
<decision_coverage>D-01,D-02,D-03,D-04,D-06,D-07,D-08: Tasks 1-2.</decision_coverage>
<source_coverage>GOAL: authoritative progress data for collection cards — covered. REQ: no formal IDs; D-IDs are coverage units — covered. RESEARCH: separate metrics, exact repository semantics, no endpoint/migration, contract parity — covered. CONTEXT: D-01–D-04/D-06–D-08 — covered; no deferred items.</source_coverage>
<output>After completion, create .planning/phases/119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha/119-02-SUMMARY.md</output>
