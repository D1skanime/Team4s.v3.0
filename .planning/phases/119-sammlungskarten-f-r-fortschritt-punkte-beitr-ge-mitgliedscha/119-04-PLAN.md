---
phase: 119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha
plan: "04"
type: execute
wave: 4
depends_on: [119-03]
files_modified: []
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-16]
must_haves:
  truths:
    - "Focused and full automated suites, typecheck, lint, production build and whitespace checks pass or unrelated pre-existing failures are evidenced separately."
    - "The user-visible `/members/{slug}` flow passes desktop, tablet, mobile, keyboard, pointer/touch/wheel and Reduced Motion review with no page overflow."
    - "Two grids remain open independently, special-award 0/1/many states are truthful, and FansubProjectsGrid remains unchanged."
  artifacts:
    - {path: .planning/phases/119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha/119-04-SUMMARY.md, provides: "Automated and live UAT evidence"}
  key_links:
    - {from: /members/{slug}, to: MemberBadgeChain, via: "visible app navigation and real public profile response"}
    - {from: MemberBadgeChain, to: FocalCarousel, via: "real responsive pointer/keyboard/grid interaction"}
---

<objective>
Prove the complete Phase-119 public collection-card experience through automated gates and live in-app browser UAT.

Purpose: Validate contract parity, visibility minimization, responsive composition, accessibility and shared-carousel consumer safety before phase completion.
Output: Green automated evidence and explicit human approval of the real user-visible route.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha/119-CONTEXT.md
@.planning/phases/119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha/119-UI-SPEC.md
@.planning/phases/119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha/119-VALIDATION.md
@.planning/phases/119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha/119-03-SUMMARY.md
@.planning/phases/118-rollenfortschritt-als-eigene-card-je-tats-chlich-ausge-bter-/118-04-SUMMARY.md
@.planning/quick/260803-be5-rollenbadges-visuell-vereinheitlichen-ka/260803-be5-SUMMARY.md
@AGENTS.md
@docs/frontend/ui-system.md
@docs/agent-guidelines-ui.md
@docs/frontend/auth-api-client.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full contract, security and build gates</name>
  <files></files>
  <read_first>
    .planning/phases/119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha/119-VALIDATION.md
    backend/internal/handlers/app_public_profile_test.go
    backend/internal/repository/member_profile_repository_postgres_test.go
    frontend/src/components/profile/MemberBadgeChain.test.tsx
    frontend/src/components/profile/memberBadgeLabels.test.ts
    frontend/src/components/ui/FocalCarousel.test.tsx
    frontend/src/components/fansubs/__tests__/FansubProjectsGrid.test.tsx
    frontend/src/types/__tests__/v12-projection-contract.test.ts
    frontend/src/app/members/[slug]/page.test.tsx
  </read_first>
  <action>Run the focused Phase-119 frontend tests, repository/handler tests and contract parity first; then full frontend/backend tests, typecheck, lint and production build from the canonical Compose services. Run git diff --check. Explicitly review HIGH T-119-01/T-119-02: exact metrics exist only inside the gated profile response, hidden profiles leak neither DTO nor metric, public_badges remains earned/public-only, and client data cannot fabricate `Aktuell`. Confirm no new migration, endpoint, helper, fetch/auth/token/carousel/badge-chain seam and that public-only SSR intentionally invokes no central refresh. This task is strictly validation-only: do not modify production or test source files. If a task-owned failure appears, record the exact command and failure in the executor summary and stop for a separately scoped corrective plan with explicit file ownership; list unrelated pre-existing failures separately with command and evidence.</action>
  <verify><automated>docker compose exec -T team4sv30-frontend npm test -- --run src/components/profile/memberBadgeLabels.test.ts src/components/profile/MemberBadgeChain.test.tsx src/components/ui/FocalCarousel.test.tsx src/components/fansubs/__tests__/FansubProjectsGrid.test.tsx src/types/__tests__/v12-projection-contract.test.ts 'src/app/members/[slug]/page.test.tsx' &amp;&amp; docker compose exec -T team4sv30-backend go test ./internal/repository ./internal/handlers -count=1 &amp;&amp; docker compose exec -T team4sv30-frontend npm test -- --run &amp;&amp; docker compose exec -T team4sv30-backend go test ./... &amp;&amp; docker compose exec -T team4sv30-frontend npm run typecheck &amp;&amp; docker compose exec -T team4sv30-frontend npm run lint &amp;&amp; docker compose exec -T team4sv30-frontend npm run build &amp;&amp; git diff --check</automated></verify>
  <done>All task-owned automated, static, build and HIGH security gates pass; any unrelated failure is clearly separated and does not mask Phase-119 evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify the real collection-card flow in the shared browser</name>
  <files></files>
  <what-built>Phase-119 family collection cards on the existing public member profile, using the Phase-118 role baseline and global FocalCarousel.</what-built>
  <action>Pause after Task 1 is green and conduct the numbered live verification in the shared Codex in-app browser. Do not replace visible navigation or human product-fit review with a headless run, and do not claim fixtures or motion states that were not actually observed.</action>
  <how-to-verify>
    1. In the Codex in-app browser, start at `http://127.0.0.1:3300`, use visible app navigation to a real `/members/{slug}` profile, and record the exact slug(s). Do not substitute a hidden direct route or headless-only run.
    2. At 1440×900 verify exact order `Rollen`, `Fortschritt`, `Punkte-Meilensteine`, `Beiträge`, `Mitgliedschaft`, `Besondere Auszeichnungen`; the profile uses the 1480px shell, neighbors are visible for multi-family categories, single-family categories have no arrows/dots/`1 von 1`, copy wraps and document scroll width equals viewport width.
    3. Navigate all three Beitragsfamilien through both endpoints with pointer, slow drag, fast swipe, wheel and Arrow/Home/End. Confirm `{position} von {total} Sammlungen`, stable physical centering and no trapped page scroll at endpoints.
    4. Select an older earned stage by pointer and Enter/Space. Confirm the hero changes to `Ausgewählt`, the real rank remains `Aktuell`, future stages remain `Gesperrt` and cannot receive focus, and family/data navigation resets selection.
    5. At 1024×768 and 390×844 verify approved card/hero proportions, 44×44 controls, long points strip scrolls only internally, `Aktuell` auto-centers on mount/family/threshold change, copy is not ellipsized and there is no page clipping/overflow.
    6. Open `Alle Auszeichnungen in ... anzeigen` in two categories simultaneously; close one with `Weniger anzeigen`. Confirm the other remains open, each toggle reports its own aria state/control, the closed toggle regains focus, and each badge occurs once in family order.
    7. Check live profiles/fixtures with no earned stage, exact threshold, terminal stage, and zero/one/multiple special awards. Confirm empty specials omit the section, one is a quiet card, many use carousel, specials have no locks/progress, separate memberships are not summed and non-founders are not offered `Gründungsmitglied`.
    8. Enable Reduced Motion and repeat carousel/stage selection: functionality remains, with no momentum, smooth auto-scroll or long glow/scale transition.
    9. Regress the Rollen area and the real public FansubProjectsGrid through visible navigation: first/last projects and show-all links work, and no badge collection counter appears there.
    Expected result: every D-01–D-16 state and UI-SPEC viewport/accessibility rule passes. Record unavailable fixtures honestly as automated-only evidence; do not claim unobserved live states.
  </how-to-verify>
  <verify><automated>docker compose exec -T team4sv30-frontend npm test -- --run src/components/profile/MemberBadgeChain.test.tsx src/components/ui/FocalCarousel.test.tsx src/components/fansubs/__tests__/FansubProjectsGrid.test.tsx &amp;&amp; git diff --check</automated></verify>
  <done>The user approves the real route after all nine live checks, or reports an exact reproducible route/viewport/input failure for correction.</done>
  <resume-signal>Type "approved" or describe the exact route, viewport and failing behavior.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| live public/members_only profile → browser DOM | Real visibility and minimized metrics must match automated fixtures. |
| user input → carousel/stage/grid state | Pointer, keyboard, focus and motion preferences must stay bounded and independent. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-119-01 | Information Disclosure | live public profile | mitigate | HIGH blocking handler tests plus live public/owner visibility review; exact metrics never appear outside a visible profile. |
| T-119-02 | Tampering | live progress UI | mitigate | HIGH blocking contract/resolver tests and live exact-boundary review; client never supplies earned state. |
| T-119-03 | Denial of Service | live FocalCarousel | mitigate | Endpoint pass-through, interruption, Reduced Motion and second-consumer checks. |
| T-119-09 | Elevation of Privilege | auth/session boundary | accept | Read-only public route has no protected action; optional owner SSR remains unchanged and central refresh is not invoked. |
| T-119-10 | Accessibility interaction | focus/motion/locks | mitigate | Blocking keyboard, focus-return, 44px target, lock tab-stop and Reduced Motion UAT. |

## Security Baseline and Blocking Gate
OWASP ASVS L1. Any unresolved HIGH T-119-01/T-119-02 or failed blocking UAT prevents completion.
</threat_model>

<verification>Automated Task 1 must pass before the live checkpoint. Human approval must cite the actual route and tested viewports; headless evidence does not replace Task 2.</verification>
<success_criteria>All D-01–D-16, approved UI-SPEC states, public visibility rules and shared-carousel regressions have automated evidence plus honest live UAT.</success_criteria>
<decision_coverage>D-01–D-16: Tasks 1-2.</decision_coverage>
<source_coverage>GOAL: production-ready family collection-card experience — covered. REQ: no formal IDs; D-01–D-16 are coverage units — covered. RESEARCH: full gates, visibility, exact metrics, responsive/live UAT, FansubProjectsGrid regression — covered. CONTEXT/UI-SPEC: every locked decision and manual matrix row — covered; no deferred items.</source_coverage>
<output>After approval, create .planning/phases/119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha/119-04-SUMMARY.md</output>
