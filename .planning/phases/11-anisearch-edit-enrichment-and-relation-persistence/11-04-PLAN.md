---
phase: 11-anisearch-edit-enrichment-and-relation-persistence
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/lib/api.admin-anime.test.ts
  - frontend/src/app/admin/anime/hooks/useAniSearchEditEnrichment.ts
  - frontend/src/app/admin/anime/hooks/useAniSearchEditEnrichment.test.ts
  - frontend/src/app/admin/anime/components/AnimeEditPage/AniSearchEnrichmentSection.tsx
  - frontend/src/app/admin/anime/components/AnimeEditPage/AniSearchEnrichmentSection.test.tsx
  - frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx
autonomous: true
gap_closure: true
requirements: [ENR-08]
must_haves:
  truths:
    - "Duplicate AniSearch ownership on edit returns redirect metadata all the way to the operator-facing edit flow."
    - "The edit AniSearch card shows an operator-usable conflict state with the existing anime title and a redirect action instead of only a thrown error message."
    - "Successful edit enrichment behavior from Plan 11-03 remains intact while the 409 path becomes first-class."
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "409 AniSearch conflict parsing for the edit enrichment helper"
      contains: "redirect_path"
    - path: "frontend/src/app/admin/anime/hooks/useAniSearchEditEnrichment.ts"
      provides: "Conflict-aware AniSearch edit hook state"
      contains: "existing_anime_id"
    - path: "frontend/src/app/admin/anime/components/AnimeEditPage/AniSearchEnrichmentSection.tsx"
      provides: "Operator-visible redirect action in the approved CSS-module edit card"
      contains: "Zum vorhandenen Anime wechseln"
  key_links:
    - from: "frontend/src/lib/api.ts"
      to: "backend/internal/handlers/admin_content_anime_enrichment_edit.go"
      via: "409 conflict payload parsing from the backend's data envelope"
      pattern: "redirect_path"
    - from: "frontend/src/app/admin/anime/hooks/useAniSearchEditEnrichment.ts"
      to: "frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx"
      via: "conflict result passed into the edit workspace"
      pattern: "existing_title"
    - from: "frontend/src/app/admin/anime/components/AnimeEditPage/AniSearchEnrichmentSection.tsx"
      to: ".planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-UI-SPEC.md"
      via: "existing CSS-module card and live-region contract"
      pattern: "AniSearch Daten laden"
---

<objective>
Close the remaining ENR-08 frontend gap by preserving edit-route AniSearch 409 redirect metadata through the shared helper and surfacing it in the existing edit UI.

Purpose: The backend already returns the correct duplicate-conflict payload. Operators still cannot act on it because the helper discards the backend's `data` envelope and the workspace only renders a generic message.
Output: Conflict-aware edit AniSearch helper, hook state, and edit-card UI that expose `redirect_path`, `existing_anime_id`, and `existing_title` without changing the approved workspace structure.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-VERIFICATION.md
@.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-UI-SPEC.md
@.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-03-SUMMARY.md
@frontend/src/lib/api.ts
@frontend/src/lib/api.admin-anime.test.ts
@frontend/src/app/admin/anime/hooks/useAniSearchEditEnrichment.ts
@frontend/src/app/admin/anime/components/AnimeEditPage/AniSearchEnrichmentSection.tsx
@frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx

<interfaces>
From `frontend/src/types/admin.ts`:
```ts
export interface AdminAnimeAniSearchEditConflictResult {
  mode: 'conflict'
  anisearch_id: string
  existing_anime_id: number
  existing_title: string
  redirect_path: string
}
```

From `frontend/src/lib/api.ts`:
```ts
export class ApiError extends Error {
  status: number
  retryAfterSeconds: number | null
  code: string | null
  details: string | null
}

export async function loadAdminAnimeEditAniSearchEnrichment(
  animeID: number,
  payload: AdminAnimeAniSearchEditRequest,
  authToken?: string,
): Promise<{ data: AdminAnimeAniSearchEditResult }>
```

From `frontend/src/app/admin/anime/hooks/useAniSearchEditEnrichment.ts`:
```ts
export function useAniSearchEditEnrichment({
  animeID,
  authToken,
  onRequest,
  onResponse,
}: UseAniSearchEditEnrichmentParams)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parse and expose edit AniSearch conflict metadata through the shared helper and hook</name>
  <files>frontend/src/lib/api.ts, frontend/src/lib/api.admin-anime.test.ts, frontend/src/app/admin/anime/hooks/useAniSearchEditEnrichment.ts, frontend/src/app/admin/anime/hooks/useAniSearchEditEnrichment.test.ts</files>
  <read_first>@.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-VERIFICATION.md @frontend/src/lib/api.ts @frontend/src/lib/api.admin-anime.test.ts @frontend/src/app/admin/anime/hooks/useAniSearchEditEnrichment.ts @frontend/src/app/admin/anime/hooks/useAniSearchEditEnrichment.test.ts</read_first>
  <acceptance_criteria>`loadAdminAnimeEditAniSearchEnrichment` parses `409 { data: { mode: 'conflict', anisearch_id, existing_anime_id, existing_title, redirect_path } }` from the backend and exposes those exact fields to callers; the hook stores a typed conflict result separately from generic error text; targeted tests cover both the success path and the real backend conflict envelope from `11-VERIFICATION.md`.</acceptance_criteria>
  <behavior>
    - Test 1: A 409 edit-enrichment response with a `data` envelope preserves `existing_anime_id`, `existing_title`, and `redirect_path` for the caller.
    - Test 2: A normal 200 edit-enrichment response still returns the existing draft result shape unchanged.
    - Test 3: The hook clears stale conflict state after a successful retry.
  </behavior>
  <action>Per ENR-08 and the failed truth in `11-VERIFICATION.md`, update the shared API seam instead of adding route-level fetches. Extend the `ApiError`/helper path in `frontend/src/lib/api.ts` so `loadAdminAnimeEditAniSearchEnrichment` recognizes the backend's real 409 body shape under `data`, preserves the backend error code, and attaches the parsed conflict object with the exact fields `mode`, `anisearch_id`, `existing_anime_id`, `existing_title`, and `redirect_path`. Then update `useAniSearchEditEnrichment.ts` to track this conflict result explicitly, clear it on new requests and successful loads, and continue forwarding request/response payloads to the developer panel. Do not change the success DTO shape, do not broaden error parsing for unrelated endpoints, and do not add a second AniSearch helper outside `frontend/src/lib/api.ts`.</action>
  <verify>
    <automated>cd frontend && npm test -- src/lib/api.admin-anime.test.ts src/app/admin/anime/hooks/useAniSearchEditEnrichment.test.ts</automated>
  </verify>
  <done>The edit AniSearch helper and hook preserve the backend's redirect metadata exactly, making the 409 path consumable by the existing edit workspace without affecting the successful draft-first behavior from Plan 11-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface the duplicate AniSearch redirect path in the existing edit workspace</name>
  <files>frontend/src/app/admin/anime/components/AnimeEditPage/AniSearchEnrichmentSection.tsx, frontend/src/app/admin/anime/components/AnimeEditPage/AniSearchEnrichmentSection.test.tsx, frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx</files>
  <read_first>@.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-UI-SPEC.md @.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-VERIFICATION.md @frontend/src/app/admin/anime/components/AnimeEditPage/AniSearchEnrichmentSection.tsx @frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx @frontend/src/app/admin/anime/components/AnimeEditPage/AniSearchEnrichmentSection.test.tsx</read_first>
  <acceptance_criteria>`AnimeEditWorkspace.tsx` passes the typed conflict result from the hook into `AniSearchEnrichmentSection`; the card renders the existing anime title plus a visible action labeled `Zum vorhandenen Anime wechseln` that links to `redirect_path`; the existing CSS-module card, live-region, and success/error rendering remain in place and success-path tests still pass.</acceptance_criteria>
  <behavior>
    - Test 1: When the hook reports a conflict, the card shows the duplicate title and a redirect action using `redirect_path`.
    - Test 2: When there is no conflict, the card continues to show the current success/error/empty states from Plan 11-03.
    - Test 3: The UI does not fall back to a generic `error.message`-only state for duplicate AniSearch conflicts.
  </behavior>
  <action>Wire the new conflict state through `AnimeEditWorkspace.tsx` into `AniSearchEnrichmentSection.tsx` without moving the card or changing the approved CSS-module admin system. Keep the existing German copy and live region, then add a dedicated duplicate-conflict branch that states the AniSearch ID is already linked to `existing_title` and offers a direct redirect action using `redirect_path`. Preserve the success summary and generic error branch for all other outcomes. Do not introduce a modal, toast-only flow, or a new page-level layout; the operator action must remain inside the existing AniSearch card per the approved UI-SPEC and the incremental gap-only scope.</action>
  <verify>
    <automated>cd frontend && npm test -- src/app/admin/anime/components/AnimeEditPage/AniSearchEnrichmentSection.test.tsx</automated>
  </verify>
  <done>The edit AniSearch card becomes operator-usable for duplicate conflicts: admins can see which existing anime owns the AniSearch ID and jump straight to that edit route from the current workspace.</done>
</task>

</tasks>

<verification>
Re-run the failed ENR-08 truth from `11-VERIFICATION.md`: the frontend must preserve the backend 409 `redirect_path` payload and render a redirectable conflict state in the edit AniSearch card.
</verification>

<success_criteria>
- Edit AniSearch 409 conflicts no longer stop at a generic error message.
- `redirect_path`, `existing_anime_id`, and `existing_title` survive the shared helper boundary into the edit UI.
- The existing Phase 11 edit AniSearch success path and CSS-module workspace stay unchanged outside the new conflict branch.
</success_criteria>

<output>
After completion, create `.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-04-SUMMARY.md`
</output>
