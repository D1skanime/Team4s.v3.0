---
phase: 11-anisearch-edit-enrichment-and-relation-persistence
plan: 03
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - frontend/src/types/admin.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/api.admin-anime.test.ts
  - frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts
  - frontend/src/app/admin/anime/hooks/useManualAnimeDraft.test.ts
  - frontend/src/app/admin/anime/hooks/useAniSearchEditEnrichment.ts
  - frontend/src/app/admin/anime/hooks/useAniSearchEditEnrichment.test.ts
  - frontend/src/app/admin/anime/components/AnimeEditPage/AniSearchEnrichmentSection.tsx
  - frontend/src/app/admin/anime/components/AnimeEditPage/AniSearchEnrichmentSection.test.tsx
  - frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx
  - frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.module.css
  - frontend/src/app/admin/anime/[id]/edit/page.tsx
  - frontend/src/app/admin/anime/[id]/edit/page.test.tsx
autonomous: true
requirements: [ENR-06, ENR-07]
must_haves:
  truths:
    - "Admins can load AniSearch from the edit route with an explicit ID and review the updated draft before saving."
    - "Protected fields remain untouched, while provisional lookup text is replaceable until the operator explicitly locks it."
    - "The AniSearch edit UI reports updated-field, protected-field, and relation counts without listing unresolved relations."
    - "The edit route keeps the existing CSS-module admin workspace and developer panel patterns."
  artifacts:
    - path: "frontend/src/app/admin/anime/components/AnimeEditPage/AniSearchEnrichmentSection.tsx"
      provides: "Dedicated AniSearch edit card"
      contains: "AniSearch Daten laden"
    - path: "frontend/src/app/admin/anime/hooks/useAniSearchEditEnrichment.ts"
      provides: "AniSearch edit request/lock orchestration"
      contains: "protected_fields"
    - path: "frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx"
      provides: "Edit workspace integration point above title fields"
      contains: "AniSearchEnrichmentSection"
    - path: "frontend/src/app/admin/anime/[id]/edit/page.tsx"
      provides: "Edit route success/error handling and developer panel continuity"
      contains: "Developer Panel"
  key_links:
    - from: "frontend/src/app/admin/anime/hooks/useAniSearchEditEnrichment.ts"
      to: "frontend/src/lib/api.ts"
      via: "edit enrichment POST helper"
      pattern: "enrichment/anisearch"
    - from: "frontend/src/app/admin/anime/components/AnimeEditPage/AniSearchEnrichmentSection.tsx"
      to: ".planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-UI-SPEC.md"
      via: "copy, fieldset, live-region, and placement contract"
      pattern: "Felder schuetzen"
    - from: "frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx"
      to: "frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts"
      via: "provisional-title replacement and draft hydration"
      pattern: "AniSearch"
---

<objective>
Integrate AniSearch enrichment into the edit route using the approved Phase 11 UI contract and the backend seam from Plan 11-02.

Purpose: Backend support alone is insufficient. Operators need a visible, explicit enrichment tool in the current edit workspace that preserves manual control and existing admin patterns.
Output: New AniSearch edit card, request hook, shared API helpers, and edit-route integration that keeps save semantics explicit.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-CONTEXT.md
@.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-UI-SPEC.md
@.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-02-PLAN.md
@frontend/src/types/admin.ts
@frontend/src/lib/api.ts
@frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx
@frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.module.css
@frontend/src/app/admin/anime/[id]/edit/page.tsx
@frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts
@frontend/src/app/admin/anime/hooks/internal/useAnimePatchImpl.ts
@frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts

<interfaces>
From `frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx`:
```tsx
export function AnimeEditWorkspace({
  anime,
  authToken,
  onSaved,
  onError,
  onRequest,
  onResponse,
}: AnimeEditWorkspaceProps)
```

From `frontend/src/lib/api.ts`:
```ts
export async function updateAdminAnime(
  animeID: number,
  payload: AdminAnimePatchRequest,
  authToken?: string,
): Promise<AdminAnimeUpsertResponse>
```

From `frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts`:
```ts
export function hydrateManualDraftFromAniSearchDraft(
  draft: ManualAnimeDraftValues,
  incoming: AdminAnimeCreateDraftPayload,
): ManualAnimeDraftValues
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared frontend AniSearch helpers and lock-aware draft orchestration</name>
  <files>frontend/src/types/admin.ts, frontend/src/lib/api.ts, frontend/src/lib/api.admin-anime.test.ts, frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts, frontend/src/app/admin/anime/hooks/useManualAnimeDraft.test.ts, frontend/src/app/admin/anime/hooks/useAniSearchEditEnrichment.ts, frontend/src/app/admin/anime/hooks/useAniSearchEditEnrichment.test.ts</files>
  <read_first>@frontend/src/types/admin.ts @frontend/src/lib/api.ts @frontend/src/lib/api.admin-anime.test.ts @frontend/src/app/admin/anime/hooks/useManualAnimeDraft.ts @frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts</read_first>
  <acceptance_criteria>`api.ts` exports an edit AniSearch helper that posts to `/api/v1/admin/anime/{id}/enrichment/anisearch`; `useAniSearchEditEnrichment.ts` sends `protected_fields`; `useManualAnimeDraft.test.ts` proves provisional title text is overwritten on first load when `title` is not protected; `api.admin-anime.test.ts` covers both success and `409 redirect_path` error propagation.</acceptance_criteria>
  <action>Implement the shared TypeScript contracts and API helper from Plan 11-01. Add `loadAdminAnimeEditAniSearchEnrichment(animeID, payload, authToken)` in `frontend/src/lib/api.ts`, preserving `ApiError` code/details on `409` conflict responses. Create `useAniSearchEditEnrichment.ts` to hold local AniSearch ID state, the explicit protected-field set, request/response capture for the existing developer panel, and a result formatter that emits updated/skipped/relation counts. Extend `useManualAnimeDraft.ts` with a helper dedicated to D-05 semantics so provisional lookup text is replaced by AniSearch unless `title` is in `protected_fields`; do not piggyback on generic dirty-state. Keep all API calls inside existing helpers and do not scatter raw `fetch` in route components.</action>
  <verify>
    <automated>cd frontend && npm test -- src/lib/api.admin-anime.test.ts src/app/admin/anime/hooks/useManualAnimeDraft.test.ts src/app/admin/anime/hooks/useAniSearchEditEnrichment.test.ts</automated>
  </verify>
  <done>The frontend has typed AniSearch edit helpers, explicit lock-aware request building, and regression coverage for provisional-title overwrite plus duplicate conflict propagation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build and integrate the AniSearch edit card inside the current admin workspace</name>
  <files>frontend/src/app/admin/anime/components/AnimeEditPage/AniSearchEnrichmentSection.tsx, frontend/src/app/admin/anime/components/AnimeEditPage/AniSearchEnrichmentSection.test.tsx, frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx, frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.module.css, frontend/src/app/admin/anime/[id]/edit/page.tsx, frontend/src/app/admin/anime/[id]/edit/page.test.tsx</files>
  <read_first>@.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-UI-SPEC.md @frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.tsx @frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditWorkspace.module.css @frontend/src/app/admin/anime/[id]/edit/page.tsx @frontend/src/app/admin/anime/[id]/edit/page.test.tsx</read_first>
  <acceptance_criteria>`AnimeEditWorkspace.tsx` renders `AniSearchEnrichmentSection` after `Basisdaten` and before `Titel und Struktur`; `AniSearchEnrichmentSection.tsx` contains the strings `AniSearch Daten laden`, `AniSearch laden`, and `Felder schuetzen`; the component uses a real `<fieldset>` with a visible `<legend>` plus a live region; `page.tsx` still retains the existing developer panel flow and does not introduce a new design system or page-level monolith.</acceptance_criteria>
  <action>Create `AniSearchEnrichmentSection.tsx` as a dedicated card that follows `11-UI-SPEC.md`: place it inside the existing edit workspace after the Basisdaten card, use the repo's CSS-module card/button/input classes, keep German copy from the UI spec, and wire the action row, protection checklist, helper text, loading state, and summary/error region exactly around the approved overwrite model. Integrate the hook from Task 1 so successful loads update the edit draft, write `source='anisearch:{id}'` into the pending patch values, refresh the relation section state when relations were auto-applied, and keep save explicit through the existing `useAnimePatch` submission flow. Maintain the current developer panel by forwarding AniSearch request/response payloads through `onRequest` and `onResponse`, and keep the page/component file size under the stated guardrails by extracting instead of inlining large new blocks.</action>
  <verify>
    <automated>cd frontend && npm test -- src/app/admin/anime/components/AnimeEditPage/AniSearchEnrichmentSection.test.tsx src/app/admin/anime/[id]/edit/page.test.tsx</automated>
  </verify>
  <done>The edit route shows a dedicated AniSearch enrichment card with lock controls, accessible status messaging, and draft-first integration that preserves the existing admin workspace, save flow, and developer panel.</done>
</task>

</tasks>

<verification>
Frontend verification is complete when the edit page exposes the AniSearch card in the approved position, helper/hook tests pass, and the route still uses the current CSS-module admin system rather than a new visual surface.
</verification>

<success_criteria>
- `/admin/anime/[id]/edit` exposes an explicit AniSearch card above the title fields.
- Operators can protect fields, load AniSearch data, and review the updated draft before saving.
- Provisional lookup text is replaceable until the field is explicitly protected.
- The existing developer panel and admin CSS-module workspace remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-03-SUMMARY.md`
</output>
