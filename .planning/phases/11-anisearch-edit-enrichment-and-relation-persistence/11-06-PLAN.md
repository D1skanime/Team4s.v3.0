---
phase: 11-anisearch-edit-enrichment-and-relation-persistence
plan: 06
type: execute
wave: 3
depends_on: [11-05]
files_modified:
  - frontend/src/app/admin/anime/create/page.tsx
  - frontend/src/app/admin/anime/create/createPageHelpers.ts
  - frontend/src/app/admin/anime/create/page.test.tsx
  - .planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-UAT.md
  - .planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-VERIFICATION.md
autonomous: true
gap_closure: true
requirements: [ENR-10]
must_haves:
  truths:
    - "The live create page no longer shows a disabled `AniSearch spaeter` control that suggests a reachable flow when none exists."
    - "Create-page regression coverage matches the actual reachable title-action UI instead of preserving the stale AniSearch placeholder."
    - "Phase 11 UAT and verification steps describe the current create-route behavior, so the diagnosed warning-visibility check is no longer blocked by unreachable UI."
  artifacts:
    - path: "frontend/src/app/admin/anime/create/page.tsx"
      provides: "Create-page title action row without the stale disabled AniSearch placeholder"
      contains: "Jellyfin suchen"
    - path: "frontend/src/app/admin/anime/create/createPageHelpers.ts"
      provides: "Current helper text for the reachable create-route source action state"
      contains: "Jellyfin"
    - path: "frontend/src/app/admin/anime/create/page.test.tsx"
      provides: "Regression coverage for the current create-page action row and helper copy"
      contains: "Jellyfin suchen"
    - path: ".planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-UAT.md"
      provides: "Human-verification wording aligned to the live create route"
      contains: "Create-route"
    - path: ".planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-VERIFICATION.md"
      provides: "Diagnosed gap narrative updated to the actual corrected behavior"
      contains: "AniSearch spaeter"
  key_links:
    - from: "frontend/src/app/admin/anime/create/page.tsx"
      to: "frontend/src/app/admin/anime/create/createPageHelpers.ts"
      via: "title action hint and source-action copy"
      pattern: "resolveSourceActionState"
    - from: "frontend/src/app/admin/anime/create/page.test.tsx"
      to: "frontend/src/app/admin/anime/create/page.tsx"
      via: "rendered create-page title actions"
      pattern: "Jellyfin suchen"
    - from: ".planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-UAT.md"
      to: ".planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-VERIFICATION.md"
      via: "same diagnosed create-page placeholder gap"
      pattern: "placeholder"
---

<objective>
Close the diagnosed Phase 11 create-page placeholder gap by removing the unreachable AniSearch placeholder from the live create UI, updating regression coverage, and aligning human verification to the behavior the product actually exposes.

Purpose: The current create page still advertises a disabled `AniSearch spaeter` action even though the live create route does not expose a reachable AniSearch intake seam. That stale control blocks the human-visible warning test and keeps product, tests, and verification out of sync.
Output: A create page whose visible actions match current reachable behavior, updated frontend tests that no longer enshrine the placeholder, and Phase 11 UAT/verification text that no longer asks humans to exercise an unreachable create-side AniSearch path.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-CONTEXT.md
@.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-UI-SPEC.md
@.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-UAT.md
@.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-VERIFICATION.md
@.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-05-SUMMARY.md
@frontend/src/app/admin/anime/create/page.tsx
@frontend/src/app/admin/anime/create/createPageHelpers.ts
@frontend/src/app/admin/anime/create/page.test.tsx

<interfaces>
From `frontend/src/app/admin/anime/create/createPageHelpers.ts`:
```ts
export function resolveSourceActionState(title: string) {
  return {
    canSync: boolean,
    helperText: string,
  };
}
```

From `frontend/src/app/admin/anime/create/page.tsx`:
```tsx
titleActions={
  <>
    <button className={createStyles.primaryAction} type="button">
      Jellyfin suchen
    </button>
    <button className={createStyles.secondaryAction} type="button" disabled>
      AniSearch spaeter
    </button>
  </>
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove the stale create-page AniSearch placeholder and lock the reachable UI instead</name>
  <files>frontend/src/app/admin/anime/create/page.tsx, frontend/src/app/admin/anime/create/createPageHelpers.ts, frontend/src/app/admin/anime/create/page.test.tsx</files>
  <read_first>@.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-UI-SPEC.md @frontend/src/app/admin/anime/create/page.tsx @frontend/src/app/admin/anime/create/createPageHelpers.ts @frontend/src/app/admin/anime/create/page.test.tsx</read_first>
  <acceptance_criteria>The create page no longer renders the disabled `AniSearch spaeter` button; the title-action row still uses the existing CSS-module action area and keeps `Jellyfin suchen` as the reachable action; helper copy no longer claims an AniSearch sync is coming from this surface; the updated page regression asserts the current live behavior and fails if the placeholder is reintroduced.</acceptance_criteria>
  <behavior>
    - Test 1: Static render of the create page shows `Jellyfin suchen` but not `AniSearch spaeter`.
    - Test 2: `resolveSourceActionState()` helper text describes the current Jellyfin-driven title action without promising a missing AniSearch control.
    - Test 3: Existing create-page smoke expectations still pass after the stale placeholder is removed.
  </behavior>
  <action>Per D-16 and the approved create touchpoint in `11-UI-SPEC.md`, keep the current admin CSS-module layout and title action area, but remove the disabled `AniSearch spaeter` secondary button so the live UI no longer advertises an unreachable path. Update `resolveSourceActionState()` helper text to describe only the reachable Jellyfin search behavior and remove the stale `AniSearch Sync kommt in Phase 4.` wording. Update `page.test.tsx` first so it stops locking the placeholder into place and instead proves the corrected action row and helper copy. Do not introduce a new create AniSearch panel, new button hierarchy, or layout redesign in this gap closure.</action>
  <verify>
    <automated>cd frontend && npm test -- src/app/admin/anime/create/page.test.tsx</automated>
  </verify>
  <done>The live create page and its regression tests now agree on the actual reachable UI: Jellyfin/manual create only, with no disabled AniSearch placeholder.</done>
</task>

<task type="auto">
  <name>Task 2: Align Phase 11 UAT and verification wording to the corrected create-route behavior</name>
  <files>.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-UAT.md, .planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-VERIFICATION.md</files>
  <read_first>@.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-UAT.md @.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-VERIFICATION.md @.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-05-SUMMARY.md</read_first>
  <acceptance_criteria>`11-UAT.md` and `11-VERIFICATION.md` no longer ask a human verifier to reach a create-side AniSearch path that is absent from the live create page. The diagnosed gap text is rewritten around the removed placeholder and the corrected product behavior, and the human verification note explicitly describes what can still be checked from the current create route without the stale button.</acceptance_criteria>
  <action>Rewrite the Phase 11 create-side human-verification language so it matches the corrected UI from Task 1. Keep this tied to ENR-10: the docs should preserve the already-implemented warning-before-redirect seam from 11-05, but stop instructing the verifier to click a create-page AniSearch control that does not exist. Update the diagnosed gap narrative in `11-VERIFICATION.md` so it records the stale placeholder as the prior issue and the corrected create-page surface as the fix. If a human-only AniSearch warning check is no longer reachable from the live UI, state that concretely instead of implying the missing control still exists.</action>
  <verify>
    <automated>if ((Select-String -Path 'frontend/src/app/admin/anime/create/page.tsx','frontend/src/app/admin/anime/create/createPageHelpers.ts','frontend/src/app/admin/anime/create/page.test.tsx','.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-UAT.md','.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-VERIFICATION.md' -Pattern 'AniSearch spaeter|AniSearch Sync kommt in Phase 4').Count -ne 0) { exit 1 }</automated>
  </verify>
  <done>Phase 11 verification artifacts now describe the same create-route behavior the operator sees, eliminating the diagnosed blocker caused by the stale placeholder narrative.</done>
</task>

</tasks>

<verification>
This gap is closed when the live create page no longer shows the disabled `AniSearch spaeter` affordance, the create-page test suite stops expecting it, and the Phase 11 UAT/verification text no longer tells humans to use an unreachable create AniSearch flow.
</verification>

<success_criteria>
- The create route’s visible actions match current reachable behavior and no longer advertise a dead AniSearch placeholder.
- Frontend regression coverage fails if `AniSearch spaeter` or the old placeholder copy is reintroduced.
- Phase 11 UAT and verification documents are consistent with the corrected create-page surface and the diagnosed gap is no longer reproducible from the live UI.
</success_criteria>

<output>
After completion, create `.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-06-SUMMARY.md`
</output>
