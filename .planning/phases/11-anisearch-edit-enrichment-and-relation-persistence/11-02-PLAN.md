---
phase: 11-anisearch-edit-enrichment-and-relation-persistence
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - backend/internal/models/admin_content.go
  - backend/internal/handlers/admin_content_handler.go
  - backend/internal/handlers/admin_content_anime.go
  - backend/internal/handlers/admin_content_anime_enrichment_edit.go
  - backend/internal/handlers/admin_content_anime_enrichment_edit_test.go
  - backend/cmd/server/admin_routes.go
  - backend/internal/services/anime_create_enrichment.go
  - backend/internal/services/anime_create_enrichment_test.go
  - backend/internal/repository/admin_content_anisearch.go
  - backend/internal/repository/admin_content_anime_create_v2.go
  - backend/internal/repository/admin_content_anime_update_v2.go
  - backend/internal/repository/anime_relations_admin.go
  - backend/internal/repository/anime_relations_admin_test.go
autonomous: true
requirements: [ENR-08, ENR-09, ENR-10]
must_haves:
  truths:
    - "Edit-route AniSearch enrichment returns a draft payload or a duplicate conflict, never a silent provenance reassignment."
    - "Edit-route AniSearch auto-applies only approved, locally resolvable relations without duplicating existing rows."
    - "Create-time AniSearch relations become durable after anime creation and relation follow-through failures downgrade to warnings instead of rolling back the anime."
    - "Both create and edit saves preserve `source='anisearch:{id}'` for later duplicate checks and source-first relation lookup."
  artifacts:
    - path: "backend/internal/handlers/admin_content_anime_enrichment_edit.go"
      provides: "Edit-route AniSearch enrichment HTTP contract"
      contains: "POST /admin/anime/:id/enrichment/anisearch"
    - path: "backend/internal/repository/anime_relations_admin.go"
      provides: "Idempotent AniSearch relation apply helper"
      contains: "ON CONFLICT"
    - path: "backend/internal/handlers/admin_content_anime.go"
      provides: "Create handler follow-through summary for AniSearch relation persistence"
      contains: "anisearch"
    - path: "backend/internal/repository/admin_content_anime_update_v2.go"
      provides: "Edit PATCH source persistence"
      contains: "source"
  key_links:
    - from: "backend/internal/handlers/admin_content_anime_enrichment_edit.go"
      to: "backend/internal/services/anime_create_enrichment.go"
      via: "shared AniSearch fetch and source-first relation resolution"
      pattern: "Enrich"
    - from: "backend/internal/handlers/admin_content_anime.go"
      to: "backend/internal/repository/anime_relations_admin.go"
      via: "post-create best-effort relation apply"
      pattern: "Apply"
    - from: "backend/internal/repository/admin_content_anime_update_v2.go"
      to: "backend/internal/repository/admin_content_anisearch.go"
      via: "persisted anisearch source for later duplicate lookup"
      pattern: "source"
---

<objective>
Implement the backend Phase 11 behavior: edit AniSearch enrichment, idempotent relation apply, and persisted create-time relation follow-through.

Purpose: The repo already has AniSearch fetch and create-draft shaping logic. This plan makes the missing backend seams durable and safe for edit-mode usage.
Output: New edit enrichment endpoint, idempotent relation-apply repository helper, create relation follow-through, and persisted AniSearch provenance on edit saves.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-CONTEXT.md
@.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-RESEARCH.md
@.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-01-PLAN.md
@backend/internal/services/anime_create_enrichment.go
@backend/internal/repository/admin_content_anisearch.go
@backend/internal/repository/admin_content_anime_create_v2.go
@backend/internal/repository/admin_content_anime_update_v2.go
@backend/internal/repository/anime_relations_admin.go
@backend/internal/handlers/admin_content_anime.go
@backend/internal/handlers/admin_content_handler.go
@backend/cmd/server/admin_routes.go

<interfaces>
From `backend/internal/services/anime_create_enrichment.go`:
```go
type AnimeCreateEnrichmentRepository interface {
	FindAnimeBySource(ctx context.Context, source string) (*models.AdminAnimeSourceMatch, error)
	ResolveAdminAnimeRelationTargetsByTitles(ctx context.Context, titles []string) ([]models.AdminAnimeRelationTitleMatch, error)
	ResolveAdminAnimeRelationTargetsBySources(ctx context.Context, sources []string) ([]models.AdminAnimeSourceMatch, error)
}
```

From `backend/internal/repository/anime_relations_admin.go`:
```go
func (r *AdminContentRepository) CreateAdminAnimeRelation(
	ctx context.Context,
	sourceAnimeID int64,
	targetAnimeID int64,
	relationLabel string,
) error
```

From `backend/internal/handlers/admin_content_anime.go`:
```go
func (h *AdminContentHandler) CreateAnime(c *gin.Context)
func (h *AdminContentHandler) UpdateAnime(c *gin.Context)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement idempotent AniSearch relation apply and create-time persisted follow-through</name>
  <files>backend/internal/models/admin_content.go, backend/internal/handlers/admin_content_anime.go, backend/internal/services/anime_create_enrichment.go, backend/internal/services/anime_create_enrichment_test.go, backend/internal/repository/admin_content_anime_create_v2.go, backend/internal/repository/anime_relations_admin.go, backend/internal/repository/anime_relations_admin_test.go</files>
  <read_first>@backend/internal/services/anime_create_enrichment.go @backend/internal/repository/admin_content_anime_create_v2.go @backend/internal/repository/anime_relations_admin.go @backend/internal/handlers/admin_content_anime.go @backend/internal/services/anime_create_enrichment_test.go</read_first>
  <acceptance_criteria>`anime_relations_admin.go` contains an idempotent apply helper using `ON CONFLICT DO NOTHING` or equivalent no-op semantics; `CreateAnime` returns `201` even when AniSearch relation follow-through warnings are present; create responses include an `anisearch` summary with applied and skipped counts; tests assert `anisearch:{id}` provenance and non-blocking warning behavior.</acceptance_criteria>
  <action>Add a repository helper named `ApplyAdminAnimeEnrichmentRelations` that accepts `sourceAnimeID int64` plus `[]models.AdminAnimeRelation`, inserts only approved relations, and no-ops duplicate rows instead of surfacing `ErrConflict` per D-07 and D-08. Extend create request/input handling so AniSearch-enriched create payloads can carry `relations`, `source`, and any needed `folder_name`, then update `CreateAnime` to run best-effort relation follow-through after the anime row is created. Return a response envelope that still uses `201 Created` with `data` plus `anisearch.relations_attempted`, `anisearch.relations_applied`, `anisearch.relations_skipped_existing`, and `anisearch.warnings`. Reuse the existing `AnimeCreateEnrichmentService.resolveRelations` source-first logic per D-09 and D-13; do not invent a second resolver or broaden the approved relation label set.</action>
  <verify>
    <automated>cd backend && go test ./internal/services ./internal/repository ./internal/handlers -run "TestAnimeCreateEnrichment|Test.*AniSearch.*Relation|TestCreateAnime" -count=1</automated>
  </verify>
  <done>Create-time AniSearch relations persist durably without duplicate rows, create remains non-blocking on relation follow-through issues, and the create response carries an AniSearch summary the frontend can surface later.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the edit AniSearch endpoint with override locks, duplicate conflict handling, and PATCH provenance support</name>
  <files>backend/internal/models/admin_content.go, backend/internal/handlers/admin_content_handler.go, backend/internal/handlers/admin_content_anime.go, backend/internal/handlers/admin_content_anime_enrichment_edit.go, backend/internal/handlers/admin_content_anime_enrichment_edit_test.go, backend/cmd/server/admin_routes.go, backend/internal/repository/admin_content_anisearch.go, backend/internal/repository/admin_content_anime_update_v2.go</files>
  <read_first>@backend/internal/handlers/admin_content_handler.go @backend/cmd/server/admin_routes.go @backend/internal/repository/admin_content_anisearch.go @backend/internal/repository/admin_content_anime_update_v2.go @.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-CONTEXT.md</read_first>
  <acceptance_criteria>`admin_routes.go` registers `POST /admin/anime/:id/enrichment/anisearch`; the handler returns `409` with `existing_anime_id` and `redirect_path` when `anisearch:{id}` is already claimed by a different anime; edit PATCH persistence supports `source` and `folder_name`; protected fields are skipped by explicit `protected_fields` membership instead of dirty-state heuristics.</acceptance_criteria>
  <action>Create `backend/internal/handlers/admin_content_anime_enrichment_edit.go` for `POST /admin/anime/:id/enrichment/anisearch`. The request must accept `anisearch_id`, a draft payload carrying the editable fields from the edit workspace, and `protected_fields []string`. The success response must return the next draft, `source='anisearch:{id}'`, `updated_fields`, `skipped_protected_fields`, `relations_applied`, and `relations_skipped_existing`. When the incoming AniSearch ID belongs to another anime, return `409 Conflict` with `existing_anime_id`, `existing_title`, and `redirect_path` rather than reassigning provenance. Persist relations immediately on successful enrichment per D-06, but keep metadata persistence draft-first by extending `updateAnimeV2` and the edit PATCH validation path to accept `source` and `folder_name` so the later explicit save writes AniSearch provenance durably. Do not auto-save title/description/type through the enrichment endpoint, and do not treat provisional lookup text as protected unless `title` appears in `protected_fields`.</action>
  <verify>
    <automated>cd backend && go test ./internal/handlers ./internal/repository -run "Test.*AniSearch.*Edit|Test.*UpdateAnime.*Source|Test.*Relation.*Apply" -count=1</automated>
  </verify>
  <done>The backend exposes the dedicated edit AniSearch endpoint, enforces duplicate safety and explicit locks, auto-applies resolvable relations idempotently, and lets the explicit edit save persist AniSearch provenance through the existing PATCH seam.</done>
</task>

</tasks>

<verification>
Backend verification is complete when targeted tests prove create follow-through is non-blocking, edit enrichment returns either a draft or a conflict redirect, and repeated AniSearch loads do not duplicate relations.
</verification>

<success_criteria>
- `POST /api/v1/admin/anime/:id/enrichment/anisearch` exists and is wired.
- Edit duplicate AniSearch IDs return `409` with redirect metadata to the existing anime.
- `source='anisearch:{id}'` can persist through the normal edit PATCH save.
- Create-time AniSearch relations are written durably with warning metadata instead of staying draft-only.
</success_criteria>

<output>
After completion, create `.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-02-SUMMARY.md`
</output>
