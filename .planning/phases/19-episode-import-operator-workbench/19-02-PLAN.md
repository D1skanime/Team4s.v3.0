---
phase: 19-episode-import-operator-workbench
plan: 02
type: feature
wave: 2
depends_on:
  - 19-01-PLAN.md
files_modified:
  - frontend/src/app/admin/anime/[id]/episodes/import/page.tsx
  - frontend/src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.ts
  - frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.ts
  - frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.test.ts
  - frontend/src/app/admin/anime/[id]/episodes/import/page.module.css
autonomous: true
requirements:
  - P19-SC1
  - P19-SC2
  - P19-SC3
  - P19-SC4
must_haves:
  truths:
    - "The operator can see which file is being mapped without opening dev tools or guessing from IDs."
    - "Batch or grouped controls reduce repetitive skip work for large libraries."
    - "The page stays compact and readable enough for real import sessions."
---

<objective>
Turn the episode-import page into a practical operator workbench.

Purpose: After the contract becomes version-friendly, the operator still needs a UI that shows files clearly, keeps source context visible, and offers fast ways to resolve many rows.
Output: A denser, readable import workbench with grouped/batch actions and clearer context.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-episode-import-operator-workbench/19-CONTEXT.md
@.planning/phases/18-episode-import-and-mapping-builder/18-UAT.md

Read these implementation references before editing:
@frontend/src/app/admin/anime/[id]/episodes/import/page.tsx
@frontend/src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.ts
@frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.ts
@frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.test.ts
@frontend/src/app/admin/anime/[id]/episodes/import/page.module.css
@frontend/src/types/episodeImport.ts
</context>

<feature>
  <name>Operator-readable workbench UI and practical bulk controls</name>
  <files>frontend/src/app/admin/anime/[id]/episodes/import/page.tsx, frontend/src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.ts, frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.ts, frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.test.ts, frontend/src/app/admin/anime/[id]/episodes/import/page.module.css</files>
  <behavior>
    - Mapping rows display canonical episode number/title together with readable Jellyfin file evidence.
    - The top context keeps AniSearch ID, Jellyfin series linkage, and folder path visible in a compact way.
    - The operator can bulk-skip, bulk-confirm, or otherwise resolve obvious rows without clicking every alternate release individually.
    - Real umlauts are used in operator-facing German copy.
  </behavior>
  <implementation>Prefer a compact grouped workbench over a flashy drag-and-drop interaction unless drag-and-drop is clearly the simpler path. Keep state logic in helpers/hooks rather than bloating the page component.</implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: Refactor the page into a readable mapping workbench</name>
  <files>frontend/src/app/admin/anime/[id]/episodes/import/page.tsx, frontend/src/app/admin/anime/[id]/episodes/import/page.module.css</files>
  <action>
    Rework the layout so source context, summary, canonical episodes, and Jellyfin candidates support actual mapping work instead of feeling like disconnected diagnostics. Make the mapping rows readable first and keep the density closer to the create page's polished admin surfaces.
  </action>
  <verify>
    <automated>cd frontend; npm test -- episodeImportMapping --run; if ($LASTEXITCODE -ne 0) { throw "frontend mapping tests must pass" }; npm run build</automated>
  </verify>
  <done>The page reads like an operator workbench rather than a debug screen.</done>
</task>

<task type="auto">
  <name>Task 2: Add practical grouped or batch resolution controls</name>
  <files>frontend/src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.ts, frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.ts, frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.test.ts, frontend/src/app/admin/anime/[id]/episodes/import/page.tsx</files>
  <action>
    Add one or more practical resolution controls that dramatically reduce repetitive `Überspringen` clicks for large candidate sets. This can be bulk-skip for alternates, per-episode grouping with quick actions, or a similarly pragmatic mechanism. Update helper tests to lock the chosen interaction.
  </action>
  <verify>
    <automated>cd frontend; npm test -- episodeImportMapping --run; if ($LASTEXITCODE -ne 0) { throw "grouped/batch mapping helper tests must pass" }</automated>
  </verify>
  <done>Large libraries can be resolved without dozens of repetitive single-row actions.</done>
</task>

</tasks>

<verification>
Wave 2 is complete when the page is readable in real use and the chosen bulk-resolution interaction is locked in helper tests and a frontend production build.
</verification>

<success_criteria>
- File evidence and canonical targets are readable in the same work surface.
- The operator no longer needs to resolve obvious alternates one row at a time.
- The page remains compact, stable, and production-build clean.
</success_criteria>

<output>
After completion, create `.planning/phases/19-episode-import-operator-workbench/19-02-SUMMARY.md`.
</output>
