---
phase: 19-episode-import-operator-workbench
plan: 01
type: feature
wave: 1
depends_on: []
files_modified:
  - backend/internal/models/episode_import.go
  - backend/internal/handlers/admin_episode_import.go
  - backend/internal/handlers/admin_episode_import_test.go
  - backend/internal/repository/episode_import_repository.go
  - backend/internal/repository/episode_import_repository_test.go
  - frontend/src/types/episodeImport.ts
autonomous: true
requirements:
  - P19-SC1
  - P19-SC2
  - P19-SC4
must_haves:
  truths:
    - "Mapping rows expose readable Jellyfin file evidence instead of opaque IDs as the primary operator label."
    - "Parallel releases of the same canonical episode are modelled as valid coexisting versions, not unresolved conflicts by default."
    - "Import context continues to show AniSearch source, Jellyfin series identity, and folder path honestly."
---

<objective>
Make the backend/import contract version-aware and operator-readable before further UI polish.

Purpose: The UI cannot become practical unless preview data distinguishes true conflicts from valid parallel releases and gives the frontend readable file evidence for each candidate.
Output: Updated preview/apply contracts, repository semantics, and tests that encode version-friendly mapping.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-episode-import-operator-workbench/19-CONTEXT.md
@.planning/phases/18-episode-import-and-mapping-builder/18-UAT.md

Read these implementation references before editing:
@backend/internal/models/episode_import.go
@backend/internal/handlers/admin_episode_import.go
@backend/internal/handlers/admin_episode_import_validation.go
@backend/internal/repository/episode_import_repository.go
@backend/internal/services/anisearch_episode_import.go
@frontend/src/types/episodeImport.ts
</context>

<feature>
  <name>Version-friendly preview contract and readable mapping evidence</name>
  <files>backend/internal/models/episode_import.go, backend/internal/handlers/admin_episode_import.go, backend/internal/handlers/admin_episode_import_test.go, backend/internal/repository/episode_import_repository.go, backend/internal/repository/episode_import_repository_test.go, frontend/src/types/episodeImport.ts</files>
  <behavior>
    - Preview rows expose readable primary labels such as `file_name`, `display_path`, and optional release-group/fansub hints.
    - Preview logic distinguishes true mapping conflicts from valid alternate versions of the same canonical episode.
    - Apply semantics preserve separate version rows when multiple files target the same canonical episode cleanly.
    - Import context returns linked AniSearch ID, resolved Jellyfin series ID, and folder path in a stable operator-facing DTO.
  </behavior>
  <implementation>Adjust DTOs and handler/repository semantics first. Keep compatibility with existing coverage persistence and do not expand the database to multilingual title storage in this wave.</implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: Extend preview DTOs and tests for readable file evidence</name>
  <files>backend/internal/models/episode_import.go, backend/internal/handlers/admin_episode_import.go, backend/internal/handlers/admin_episode_import_test.go, frontend/src/types/episodeImport.ts</files>
  <action>
    Add or rename fields in the shared import DTOs so the frontend receives operator-readable Jellyfin evidence for every mapping candidate. At minimum provide file name plus a path string that helps distinguish similar releases. Update handler tests so preview and context responses assert these fields and continue asserting normalized arrays and resolved folder-path linkage.
  </action>
  <verify>
    <automated>cd backend; go test ./internal/handlers -run "Test(PreviewEpisodeImport_SeparatesCanonicalEpisodesAndMediaCandidates|ResolveEpisodeImportSeriesByFolderPath_UsesStoredFolderPathFallback|FindJellyfinSeriesMatchesByPath_FiltersToExactFolder)" ; if ($LASTEXITCODE -ne 0) { throw "backend handler contract tests must pass" }; cd ..\frontend; npx tsc --noEmit --pretty false</automated>
  </verify>
  <done>Preview/context payloads give the frontend readable file evidence and still preserve the repaired folder-path fallback behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Teach repository/apply flow that parallel releases are valid versions</name>
  <files>backend/internal/repository/episode_import_repository.go, backend/internal/repository/episode_import_repository_test.go, backend/internal/handlers/admin_episode_import.go</files>
  <action>
    Refine import preview/apply semantics so multiple distinct files mapping cleanly to the same canonical episode are treated as valid separate versions instead of unresolved conflicts. Keep true ambiguity detection for rows that still need human resolution.
  </action>
  <verify>
    <automated>cd backend; go test ./internal/repository ./internal/handlers -run "Test(EpisodeImportApply_CreatesCoverageJoinRowsForMultiEpisodeMedia|ApplyEpisodeImport_RejectsUnconfirmedConflicts)" ; if ($LASTEXITCODE -ne 0) { throw "version-friendly apply semantics must be covered by repository/handler tests" }</automated>
  </verify>
  <done>Apply supports real multi-release libraries without forcing false conflict resolution row by row.</done>
</task>

</tasks>

<verification>
Wave 1 is complete when preview/apply tests prove that readable file evidence is present and multiple releases can coexist as separate versions without regressing the folder-path and coverage baseline.
</verification>

<success_criteria>
- Mapping data is readable enough for the frontend to identify real files.
- Clean alternate releases no longer block apply as conflicts by default.
- Import context exposes AniSearch ID, Jellyfin series linkage, and folder path consistently.
</success_criteria>

<output>
After completion, create `.planning/phases/19-episode-import-operator-workbench/19-01-SUMMARY.md`.
</output>
