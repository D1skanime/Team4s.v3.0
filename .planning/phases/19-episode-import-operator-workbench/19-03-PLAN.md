---
phase: 19-episode-import-operator-workbench
plan: 03
type: verify
wave: 3
depends_on:
  - 19-01-PLAN.md
  - 19-02-PLAN.md
files_modified:
  - .planning/phases/18-episode-import-and-mapping-builder/18-UAT.md
autonomous: true
requirements:
  - P19-SC5
must_haves:
  truths:
    - "The real-library import flow becomes end-to-end testable again."
    - "Blocked Phase-18 verification is explicitly resumed and either closed or narrowed to a smaller residual gap."
---

<objective>
Close the loop from blocked Phase-18 verification into a real end-to-end operator test.

Purpose: Phase 19 only counts if it makes the live import flow practical enough to verify on real anime data.
Output: Resumed UAT evidence showing whether the operator can preview, resolve mappings, and apply successfully on a real linked anime.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-episode-import-operator-workbench/19-CONTEXT.md
@.planning/phases/18-episode-import-and-mapping-builder/18-UAT.md
</context>

<feature>
  <name>Resume blocked Phase-18 verification against the hardened operator workbench</name>
  <files>.planning/phases/18-episode-import-and-mapping-builder/18-UAT.md</files>
  <behavior>
    - Re-run the blocked UAT on a real anime with linked folder path and real Jellyfin files.
    - Confirm that readable mapping rows, version-friendly logic, and batch controls make apply practically testable.
    - Record any residual gap precisely instead of leaving the phase in an ambiguous state.
  </behavior>
  <implementation>Do not broaden scope during verification. If a residual issue remains, capture it as a sharply bounded follow-up rather than quietly folding it into this phase.</implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: Resume and update live UAT evidence</name>
  <files>.planning/phases/18-episode-import-and-mapping-builder/18-UAT.md</files>
  <action>
    After implementation, rerun the blocked import verification on a real anime library. Update the existing Phase-18 UAT file with the real outcome, making it explicit whether the previous blocker is closed, downgraded, or still open for a narrower reason.
  </action>
  <verify>
    <manual>Run the real import flow on `/admin/anime/[id]/episodes/import`, use preview, resolve mappings with the new workbench, and attempt apply on a real linked anime. Capture the exact result in 18-UAT.md.</manual>
  </verify>
  <done>The previous verification stopper is replaced by real evidence instead of ambiguity.</done>
</task>

</tasks>

<verification>
Wave 3 is complete when blocked Phase-18 UAT is resumed and recorded with a clear pass, issue, or narrower blocked result.
</verification>

<success_criteria>
- Phase 19 is judged on live operator usability, not just local tests.
- The project has one honest UAT record instead of a silently abandoned blocker.
</success_criteria>

<output>
After completion, create `.planning/phases/19-episode-import-operator-workbench/19-03-SUMMARY.md`.
</output>
