---
phase: 73-public-fansub-page-fansubs-slug-erweitern
plan: 04
type: execute
wave: 3
depends_on: ["73-02"]
files_modified:
  - frontend/src/components/fansubs/FansubMediaSection.tsx
  - frontend/src/components/fansubs/FansubGroupMediaBlock.tsx
  - frontend/src/components/fansubs/FansubReleaseMediaBlock.tsx
  - frontend/src/components/fansubs/FansubMemberMediaBlock.tsx
autonomous: true
requirements: [B, G]
must_haves:
  truths:
    - "Medien-Abschnitt hat genau drei getrennte, fest beschriftete Bereiche ohne Vermischung der Quell-Tabellen (D-11)"
    - "Jeder Bereich filtert auf visibility=public AND review_status=approved vor dem Render (D-12)"
    - "Gruppenmedien-Bereich zeigt Fallback auf group.logo_url/banner_url wenn Phase-72-Mediaprojektion fehlt (A1)"
    - "Empty States mit Pflicht-Text erscheinen pro Bereich wenn keine sichtbaren Medien vorhanden (D-15)"
    - "Bereichs-Beschriftungen sind Gruppenmedien, Release-Einblicke, Team & Erinnerungen (Pflicht-Labels D-11)"
    - "Alle Dateien bleiben unter 450 Zeilen (CLAUDE.md)"
  artifacts:
    - path: "frontend/src/components/fansubs/FansubMediaSection.tsx"
      provides: "Medien-Abschnitts-Orchestrator — delegiert an drei Sub-Komponenten"
      min_lines: 30
    - path: "frontend/src/components/fansubs/FansubGroupMediaBlock.tsx"
      provides: "Gruppenmedien-Block (fansub_group_media/logo/banner)"
      min_lines: 30
    - path: "frontend/src/components/fansubs/FansubReleaseMediaBlock.tsx"
      provides: "Release-Einblicke-Block (release_version_media-Kontext)"
      min_lines: 25
    - path: "frontend/src/components/fansubs/FansubMemberMediaBlock.tsx"
      provides: "Team & Erinnerungen-Block (media_assets.owner_member_id)"
      min_lines: 25
  key_links:
    - from: "FansubGroupMediaBlock.tsx"
      to: "MediaOwnershipRow (ownerType='group')"
      via: "Props: { mediaRows: MediaOwnershipRow[]; group: FansubGroup }"
      pattern: "ownerType.*group|fansub_group_media"
    - from: "FansubMediaSection.tsx"
      to: "getMediaOwnershipProjection('group', group.id)"
      via: "Props: { mediaRows: MediaOwnershipRow[]; group: FansubGroup }"
      pattern: "MediaOwnershipRow"
    - from: "FansubGroupMediaBlock.tsx visibility filter"
      to: "nur visibility=public AND review_status=approved"
      via: "mediaRows.filter(r => r.visibility === 'public' && r.review_status === 'approved')"
      pattern: "visibility.*public.*review_status.*approved"
---

<objective>
Plan 04 implementiert die drei Medien-Ownership-Bereiche (D-11) als Sub-Komponenten von
FansubMediaSection. Dieser Plan läuft parallel zu Plan 03 (Wave 3) da beide keine gemeinsamen
Dateien berühren. Der Schwerpunkt liegt auf der strikten Ownership-Trennung (D-11), dem
Visibility/Review-Filter (D-12) und dem Fallback auf bestehende logo_url/banner_url für den
Fall dass Phase-72-Mediaprojektion nicht verfügbar ist (A1 — Graceful Degradation).

Purpose: Ownership-korrekte, visibility-gefilterte Medien-Darstellung in drei fest beschrifteten
Bereichen ohne Datenmischung.
Output: FansubMediaSection.tsx + 3 Sub-Komponenten-Dateien.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-CONTEXT.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-RESEARCH.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-PATTERNS.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-UI-SPEC.md

<interfaces>
<!-- Phase-72-Typ (aus frontend/src/types/media-ownership.ts, nach Phase-72-Ausführung): -->
<!-- interface MediaOwnershipRow {                                                          -->
<!--   owner_type: string         // 'group', 'member', 'release_version'                  -->
<!--   owner_id: number                                                                     -->
<!--   media_category: string     // Kategorie des Assets                                  -->
<!--   visibility: string         // 'public', 'registered', 'internal', ...               -->
<!--   review_status: string      // 'in_review', 'approved', 'rejected', 'archived', 'removed' -->
<!--   // Basis-Asset-Felder (id, url/path, etc.) — genaue Felder aus Phase-72-Plan-03      -->
<!-- }                                                                                       -->

<!-- FansubGroup (frontend/src/types/fansub.ts) — Fallback-Felder: -->
<!-- logo_url?: string | null; banner_url?: string | null -->

<!-- UI-Primitive: -->
<!-- Card: { variant: 'section' } für die drei Ownership-Bereich-Karten -->
<!-- SectionHeader, EmptyState (variant compact) -->
<!-- Image from 'next/image' für logo/banner Fallback (unoptimized für externe URLs) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FansubGroupMediaBlock und FansubMemberMediaBlock (D-11, D-12)</name>
  <files>
    frontend/src/components/fansubs/FansubGroupMediaBlock.tsx,
    frontend/src/components/fansubs/FansubMemberMediaBlock.tsx
  </files>
  <read_first>
    - frontend/src/components/fansubs/FansubProfileTabs.tsx (Zeilen 205–223 — bestehende banner_url/logo_url-Darstellung als Fallback-Muster)
    - frontend/src/types/fansub.ts (FansubGroup — logo_url, banner_url Felder)
    - frontend/src/components/ui/Card.tsx (variant section für Bereichs-Karten)
    - frontend/src/components/ui/EmptyState.tsx (variant compact)
    - .planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-UI-SPEC.md (Medien-Bereichs-Labels: Gruppenmedien, Team & Erinnerungen; Empty State Pflicht-Text)
    - .planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-PATTERNS.md (FansubMediaSection, FansubGroupMediaBlock Pattern-Abschnitt)
    - .planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-RESEARCH.md (A1 — Fallback-Strategie wenn ownerType=group nicht vom Endpoint aufgelöst wird)
  </read_first>
  <behavior>
    - FansubGroupMediaBlock filtert mediaRows auf ownerType='group' (oder ownerType='fansub_group') + visibility='public' + review_status='approved'
    - Wenn keine gefilterten Rows: Fallback auf group.logo_url / group.banner_url (A1-Graceful Degradation)
    - Wenn Fallback auch leer: EmptyState variant="compact" title="Noch keine Medien hinterlegt"
    - FansubMemberMediaBlock filtert mediaRows auf ownerType='member' + visibility + review_status
    - Abschnittsbezeichnung: "Gruppenmedien" (Block 1) und "Team & Erinnerungen" (Block 3)
  </behavior>
  <action>
    Erstelle `FansubGroupMediaBlock.tsx` — Props:
    `{ mediaRows: MediaOwnershipRow[]; group: Pick<FansubGroup, 'id' | 'logo_url' | 'banner_url'> }`.
    Keine Client-Direktive.

    Filterung (D-12): `const publicGroupMedia = mediaRows.filter(r =>
    (r.owner_type === 'group' || r.owner_type === 'fansub_group') &&
    r.visibility === 'public' && r.review_status === 'approved')`.

    Fallback (A1): wenn `publicGroupMedia.length === 0` und (`group.logo_url` oder `group.banner_url`):
    Zeige Fallback-Block mit `<Image>` für logo_url und/oder banner_url (unoptimized).

    Wenn `publicGroupMedia.length === 0` UND kein Fallback:
    `<EmptyState variant="compact" title="Noch keine Medien hinterlegt" description="Diese Gruppe hat bisher keine öffentlichen Medien bereitgestellt." />`.

    Sonst: rendern der media asset rows als Raster (jedes Item als `<Card variant="section">`).
    Da Phase-72-MediaOwnershipRow exakte Asset-Felder (url, path) erst nach Phase-72 bekannt sind,
    implementiere den Render-Block als konditionale Darstellung mit `r.media_category`-Label
    und einem Platzhalter-Rechteck wenn kein direktes url-Feld vorhanden. Executor soll die
    tatsächlichen Asset-URL-Felder aus der Phase-72-Plan-03-Implementierung lesen und anpassen.

    Bereichs-Überschrift NICHT hier rendern — wird von `FansubMediaSection.tsx` als `<Card variant="section">` mit eingebetteter Überschrift gerendert.
    Imports: `Image from 'next/image'`, `MediaOwnershipRow from '@/types/media-ownership'`,
    `FansubGroup from '@/types/fansub'`, `Card, EmptyState from '@/components/ui'`.

    Erstelle `FansubMemberMediaBlock.tsx` — Props: `{ mediaRows: MediaOwnershipRow[] }`.
    Filterung: `mediaRows.filter(r => r.owner_type === 'member' && r.visibility === 'public' && r.review_status === 'approved')`.
    Wenn leer: `<EmptyState variant="compact" title="Noch keine Medien hinterlegt" description="Diese Gruppe hat bisher keine öffentlichen Medien bereitgestellt." />`.
    Sonst: einfache Kachel-Liste `<Card variant="section">` pro Item.
    Imports: `MediaOwnershipRow from '@/types/media-ownership'`, `Card, EmptyState from '@/components/ui'`.
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck 2>&1 | grep -E "FansubGroupMedia|FansubMemberMedia" | head -10 || echo "typecheck-ok"</automated>
  </verify>
  <acceptance_criteria>
    - Quell-Assertion: `FansubGroupMediaBlock.tsx` enthält Filterbedingung mit `visibility === 'public'` AND `review_status === 'approved'` (D-12).
    - Quell-Assertion: Fallback-Logik prüft `group.logo_url || group.banner_url` wenn `publicGroupMedia.length === 0` (A1).
    - Quell-Assertion: `FansubMemberMediaBlock.tsx` enthält `owner_type === 'member'` im Filter.
    - Quell-Assertion: Empty State title ist exakt `"Noch keine Medien hinterlegt"` in beiden Dateien (UI-SPEC Copywriting).
    - Behavior-Assertion: `npm run typecheck` zeigt keine FansubGroupMedia/FansubMemberMedia-spezifischen Fehler.
  </acceptance_criteria>
  <done>FansubGroupMediaBlock mit A1-Fallback und FansubMemberMediaBlock mit D-12-Filter implementiert, compilierbar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FansubReleaseMediaBlock und FansubMediaSection — Orchestrator (D-11)</name>
  <files>
    frontend/src/components/fansubs/FansubReleaseMediaBlock.tsx,
    frontend/src/components/fansubs/FansubMediaSection.tsx
  </files>
  <read_first>
    - frontend/src/components/fansubs/FansubGroupMediaBlock.tsx (soeben erstellt — Props-Muster)
    - frontend/src/components/fansubs/FansubMemberMediaBlock.tsx (soeben erstellt — Props-Muster)
    - frontend/src/components/ui/Card.tsx (variant section für Bereichs-Cards)
    - frontend/src/components/ui/SectionHeader.tsx, EmptyState.tsx
    - .planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-UI-SPEC.md (Responsive Verhalten — Drei Bereiche: Desktop nebeneinander, Mobil gestapelt; Grid auto-fit minmax 220px 1fr)
    - .planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-PATTERNS.md (FansubMediaSection Pattern-Abschnitt)
    - .planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-CONTEXT.md (D-11: Pflicht-Labels Gruppenmedien / Release-Einblicke / Team & Erinnerungen)
  </read_first>
  <behavior>
    - FansubReleaseMediaBlock: filtert auf ownerType='release_version' + visibility='public' + review_status='approved'; Empty State wenn leer; Bereichs-Label "Release-Einblicke"
    - FansubMediaSection: Orchestrator mit section id="medien"; SectionHeader title="Medien"; 3-Spalten-Grid via CSS (Desktop nebeneinander, Mobil gestapelt); je ein Card variant="section" pro Bereich mit eingebetteter Bereichs-Überschrift; delegiert an drei Sub-Komponenten
  </behavior>
  <action>
    Erstelle `FansubReleaseMediaBlock.tsx` — Props: `{ mediaRows: MediaOwnershipRow[] }`.
    Filterung: `mediaRows.filter(r => r.owner_type === 'release_version' && r.visibility === 'public' && r.review_status === 'approved')`.
    Wenn leer: `<EmptyState variant="compact" title="Noch keine Medien hinterlegt" description="Diese Gruppe hat bisher keine öffentlichen Medien bereitgestellt." />`.
    Sonst: Kachel-Liste `<Card variant="section">` pro Item.
    Imports: `MediaOwnershipRow from '@/types/media-ownership'`, `Card, EmptyState from '@/components/ui'`.

    Erstelle `FansubMediaSection.tsx` — Props:
    `{ mediaRows: MediaOwnershipRow[]; group: FansubGroup }`.
    Keine Client-Direktive (Server Component).
    CSS-Grid-Layout für die drei Bereiche (inline style oder Klasse, muss auf Mobil stapeln):
    `display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 16px;` (UI-SPEC).
    Render:
    `<section id="medien">`, `<SectionHeader title="Medien" />`,
    `<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(220px, 1fr))', gap: '16px' }}>`.
    Drei `<Card variant="section">` mit eingebetteter Bereichs-Überschrift und delegiertem Block:
    - Erster Card: Überschrift `"Gruppenmedien"` (Pflicht-Label D-11), `<FansubGroupMediaBlock mediaRows={mediaRows} group={group} />`
    - Zweiter Card: Überschrift `"Release-Einblicke"` (Pflicht-Label D-11), `<FansubReleaseMediaBlock mediaRows={mediaRows} />`
    - Dritter Card: Überschrift `"Team & Erinnerungen"` (Pflicht-Label D-11), `<FansubMemberMediaBlock mediaRows={mediaRows} />`

    Bereichs-Überschriften als `<h3>` innerhalb der Card — NICHT als SectionHeader (die Bereichs-Überschriften sind Sub-Köpfe, nicht Haupt-Abschnittsköpfe).
    Wenn mediaRows komplett leer (Fallback-Szenario ohne Phase-72): Alle drei Blöcke trotzdem rendern,
    jeder zeigt seinen eigenen Empty State (D-15 — Anker stabil, Abschnitt sichtbar).
    Imports: `FansubGroupMediaBlock from './FansubGroupMediaBlock'`,
    `FansubReleaseMediaBlock from './FansubReleaseMediaBlock'`,
    `FansubMemberMediaBlock from './FansubMemberMediaBlock'`,
    `MediaOwnershipRow from '@/types/media-ownership'`, `FansubGroup from '@/types/fansub'`,
    `Card, SectionHeader from '@/components/ui'`.

    Zeilenlimit-Check: FansubMediaSection.tsx soll unter 100 Zeilen bleiben (reine Orchestrierung).
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck 2>&1 | grep -E "FansubReleaseMedia|FansubMediaSection" | head -10 || echo "typecheck-ok"</automated>
  </verify>
  <acceptance_criteria>
    - Quell-Assertion: `FansubReleaseMediaBlock.tsx` enthält `owner_type === 'release_version'` + `visibility === 'public'` + `review_status === 'approved'` im Filter (D-12).
    - Quell-Assertion: `FansubMediaSection.tsx` enthält genau drei Block-Komponenten: FansubGroupMediaBlock, FansubReleaseMediaBlock, FansubMemberMediaBlock — keine anderen Ownership-Bereiche.
    - Quell-Assertion: Bereichs-Überschriften im JSX sind exakt `"Gruppenmedien"`, `"Release-Einblicke"`, `"Team & Erinnerungen"` (D-11 Pflicht-Labels, korrekte Schreibweise).
    - Quell-Assertion: CSS-Grid mit `minmax(220px, 1fr)` vorhanden (Desktop nebeneinander, Mobil gestapelt per auto-fit).
    - Quell-Assertion: section id="medien" in FansubMediaSection.tsx.
    - Behavior-Assertion: `npm run typecheck` zeigt keine FansubReleaseMedia/FansubMediaSection-Fehler.
  </acceptance_criteria>
  <done>FansubReleaseMediaBlock und FansubMediaSection implementiert; drei Ownership-Bereiche mit korrekten Pflicht-Labels und D-12-Filter; compilierbar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend media-ownership API → Frontend | MediaOwnershipRow kommt ungefiltert; Phase 73 filtert Frontend-seitig auf visibility/review_status |
| group.logo_url / banner_url | Kommen aus FansubGroup-DTO (öffentlich); `<Image unoptimized>` rendert externe URLs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-04-01 | Information Disclosure | Medien-Render ohne Visibility-Filter | mitigate | `visibility='public' AND review_status='approved'` als Pflicht-Filter in allen drei Block-Komponenten (D-12); interne/in-review Inhalte werden nicht gerendert |
| T-73-04-02 | Information Disclosure | Datenmischung zwischen Ownership-Bereichen | mitigate | Strikte ownerType-Filter in jedem Block (`'group'`, `'release_version'`, `'member'`); keine geteilten Filter-Variablen zwischen Blocks |
| T-73-04-03 | Tampering | `<Image unoptimized>` für logo_url Fallback | accept | next/image mit unoptimized rendert externe URLs direkt; Domain ist Gruppenopinions-Daten (keine User-controlled URLs); Fallback nur für Gruppen-Assets aus der eigenen DB |
| T-73-04-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neuen Pakete; next/image ist bereits Teil von Next.js |
</threat_model>

<verification>
- `npm run typecheck` ohne Fehler für alle vier neuen Dateien.
- `npx vitest run FansubMediaSection` — nach Implementierung der it.todo-Tests: alle grün.
- FansubMediaSection.tsx: genau drei Bereichs-Blöcke; Pflicht-Labels "Gruppenmedien", "Release-Einblicke", "Team & Erinnerungen".
- Alle vier Dateien unter 450 Zeilen.
</verification>

<success_criteria>
- D-11 (drei getrennte Medien-Bereiche ohne Vermischung) durch separate Dateien mit ownerType-Filtern implementiert.
- D-12 (Visibility/Review-Filterung) in allen drei Block-Komponenten vor Render.
- A1-Fallback auf group.logo_url/banner_url in FansubGroupMediaBlock.
- D-15 (Empty States) pro Bereich.
- Alle vier Dateien unter 450 Zeilen.
</success_criteria>

<output>
Create `.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-04-SUMMARY.md` when done
</output>
