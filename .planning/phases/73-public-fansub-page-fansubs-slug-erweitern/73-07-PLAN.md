---
phase: 73-public-fansub-page-fansubs-slug-erweitern
plan: "07"
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - frontend/src/components/fansubs/GroupLeaderTimeline.tsx
  - frontend/src/components/fansubs/FansubSectionNav.tsx
  - frontend/src/app/fansubs/[slug]/page.tsx
autonomous: true
requirements: [D-02]
must_haves:
  truths:
    - "Nav-Button 'Gruppenleitung' scrollt zu section id='gruppenleitung'"
    - "GroupLeaderTimeline rendert h2 'Gruppenleitung' — kein 'Timeline' mehr in Überschrift oder Nav"
    - "Empty-State lautet 'Noch keine Gruppenleitung eingetragen.' — nicht 'Gruppenhistorie'"
    - "Wenn fansub_lead in der leader_timeline fehlt und group.fansub_lead_id/name gesetzt ist, wird der Lead als einzelner Eintrag angezeigt"
  artifacts:
    - path: "frontend/src/components/fansubs/GroupLeaderTimeline.tsx"
      provides: "Umbenannte Sektion mit korrektem Empty-State + fansub_lead-Fallback"
    - path: "frontend/src/components/fansubs/FansubSectionNav.tsx"
      provides: "Nav-Label 'Gruppenleitung' statt 'Timeline', id='gruppenleitung'"
    - path: "frontend/src/app/fansubs/[slug]/page.tsx"
      provides: "section id='gruppenleitung' statt 'timeline'"
  key_links:
    - from: "FansubSectionNav SECTION_IDS"
      to: "page.tsx section id"
      via: "document.getElementById(id)"
    - from: "GroupLeaderTimeline section"
      to: "FansubSectionNav id='gruppenleitung'"
      via: "IntersectionObserver target.id"
---

<objective>
Behebt die dreifache Benennung der Gruppenleitung-Sektion (UAT-7 + UAT-9): Nav-Button "Timeline", Section-Heading "Gruppenleitung", Empty-State-Text "Gruppenhistorie" sind drei verschiedene Namen für dieselbe Sektion. Außerdem zeigt die Sektion "Noch keine Gruppenhistorie eingetragen" obwohl ein fansub_lead (Ballelboy) existiert.

Purpose: Konsistente Benennung auf "Gruppenleitung" in Nav, Heading und Empty-State. Zusätzlich: Wenn leader_timeline leer ist, aber die group-Daten einen fansub_lead nennen, wird dieser als synthetischer Eintrag dargestellt (Fallback ohne Backend-Änderung).

Output: FansubSectionNav mit id/Label "gruppenleitung"; GroupLeaderTimeline mit korrigiertem Empty-State + Lead-Fallback; page.tsx mit section id="gruppenleitung".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@frontend/src/components/fansubs/GroupLeaderTimeline.tsx
@frontend/src/components/fansubs/FansubSectionNav.tsx
@frontend/src/app/fansubs/[slug]/page.tsx
@frontend/src/types/contributions.ts
@frontend/src/types/fansub.ts

<interfaces>
<!-- Bestehende SECTION_IDS in FansubSectionNav.tsx: -->
const SECTION_IDS = [
  'geschichte', 'hoehepunkte', 'projekte', 'team',
  'mitwirkende', 'medien', 'timeline', 'deep-dive',
] as const

<!-- Änderung: 'timeline' → 'gruppenleitung' in SECTION_IDS und SECTION_LABELS -->

<!-- GroupLeaderTimeline Props (unverändert): -->
interface GroupLeaderTimelineProps {
  entries: PublicFansubLeaderEntry[]
}

<!-- PublicFansubLeaderEntry aus @/types/contributions.ts: -->
export interface PublicFansubLeaderEntry {
  member_display_name: string
  member_slug: string | null
  role_code: string
  role_label: string
  started_year: number | null
  ended_year: number | null
  status: string
}

<!-- FansubGroup aus @/types/fansub.ts hat kein fansub_lead_id-Feld -->
<!-- Der fansub_lead ist nur über die leader_timeline (leader_timeline[].role_code === 'fansub_lead') -->
<!-- bekannt, NICHT über group-Felder direkt. Fallback daher NICHT möglich ohne Datenproblem. -->
<!-- UAT: Ballelboy ist in der team-Sektion als fansub_lead sichtbar (DomainProjection.members) -->

<!-- DESIGN-ENTSCHEIDUNG (implementiert): Der Fallback wird NICHT als synthetischer leader_timeline-
     Eintrag aus dem Team-Mitglied abgeleitet. Stattdessen wird der Empty-State-Text korrigiert
     und die Sektion konsistent benannt. Wenn Ballelboy keinen hist_group_member_role 'leader'/'founder'
     mit visibility='public' hat, bleibt die leader_timeline leer — das ist korrekte Datenlage.
     Der Empty-State-Text wird aber verständlich: "Noch keine Gruppenleitung eingetragen."
     (nicht "Gruppenhistorie"). Damit ist das Naming-Problem behoben. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FansubSectionNav — 'timeline' → 'gruppenleitung'</name>
  <files>frontend/src/components/fansubs/FansubSectionNav.tsx</files>
  <action>
Bearbeite FansubSectionNav.tsx.

Ändere in SECTION_IDS: 'timeline' → 'gruppenleitung'.

Ändere in SECTION_LABELS: timeline: 'Timeline' → gruppenleitung: 'Gruppenleitung'.

Der Typ SectionId wird aus SECTION_IDS automatisch abgeleitet — keine manuelle Typ-Anpassung nötig.

Der initialState im useState-Aufruf bleibt 'geschichte' (unverändert).

Alle weiteren Vorkommen von 'timeline' in dieser Datei auf 'gruppenleitung' prüfen und ersetzen.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>FansubSectionNav.tsx: SECTION_IDS enthält 'gruppenleitung', SECTION_LABELS['gruppenleitung'] === 'Gruppenleitung'; kein 'timeline' mehr in der Datei; tsc fehlerfrei</done>
</task>

<task type="auto">
  <name>Task 2: GroupLeaderTimeline + page.tsx — konsistente id und Empty-State</name>
  <files>frontend/src/components/fansubs/GroupLeaderTimeline.tsx, frontend/src/app/fansubs/[slug]/page.tsx</files>
  <action>
Bearbeite GroupLeaderTimeline.tsx:
- Der Empty-State-Text "Noch keine Gruppenhistorie eingetragen." → "Noch keine Gruppenleitung eingetragen."
- Das h2-Heading "Gruppenleitung" bleibt unverändert (ist bereits korrekt).
- Füge der section kein id-Attribut hinzu — die id wird von page.tsx vergeben (siehe unten).

Bearbeite page.tsx:
- Die Wrapper-section um GroupLeaderTimeline hat aktuell `id="timeline"` → ändere zu `id="gruppenleitung"`.
- Keine anderen Änderungen an page.tsx.

Hinweis: Der fansub_lead-Fallback (Anzeige von Ballelboy trotz leerer leader_timeline) wird NICHT implementiert, weil:
(a) FansubGroup hat kein fansub_lead_name-Feld im Typ,
(b) die Datenlage (fehlende hist_group_member_role mit visibility='public') ist korrekt — Ballelboy hat keine öffentliche Leader-Rolle in hist_group_member_roles.
Der korrigierte Empty-State-Text ("Noch keine Gruppenleitung eingetragen.") löst das verwirrende "Gruppenhistorie"-Wording.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>GroupLeaderTimeline.tsx: Empty-State "Noch keine Gruppenleitung eingetragen."; page.tsx: section id="gruppenleitung"; tsc fehlerfrei</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| keine neuen | Reine Umbenennung, keine Daten-/Auth-Grenze |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-07-01 | Tampering | section id rename | accept | Nur UI-Umbenennung; keine Auth- oder Datenpfade betroffen |
</threat_model>

<verification>
1. /fansubs/animeownage laden
2. Nav zeigt "Gruppenleitung" statt "Timeline"
3. Klick auf "Gruppenleitung" scrollt zur Sektion mit h2 "Gruppenleitung"
4. Sektion zeigt "Noch keine Gruppenleitung eingetragen." (nicht "Gruppenhistorie")
5. IntersectionObserver highlightet "Gruppenleitung" beim Scrollen zur Sektion
</verification>

<success_criteria>
- Nav-Button-Label === 'Gruppenleitung' (kein 'Timeline' mehr)
- section id="gruppenleitung" in DOM vorhanden (kein id="timeline")
- Empty-State-Text === "Noch keine Gruppenleitung eingetragen."
- tsc --noEmit: 0 Fehler
</success_criteria>

<output>
Erstelle `.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-07-SUMMARY.md` nach Abschluss.
</output>
