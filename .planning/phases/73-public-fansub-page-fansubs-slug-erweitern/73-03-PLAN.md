---
phase: 73-public-fansub-page-fansubs-slug-erweitern
plan: 03
type: execute
wave: 3
depends_on: ["73-01b", "73-02"]
files_modified:
  - frontend/src/components/fansubs/FansubTeamSection.tsx
  - frontend/src/components/fansubs/FansubTeamActiveGroup.tsx
  - frontend/src/components/fansubs/FansubTeamHistoricalGroup.tsx
  - frontend/src/components/fansubs/FansubTeamMemorialBlock.tsx
  - frontend/src/components/fansubs/FansubContributorsSection.tsx
  - frontend/src/components/fansubs/FansubTeamSection.module.css
autonomous: true
requirements: [B, C]
must_haves:
  truths:
    - "Team-Block enthält KEINE Einträge aus contributors[] — Separation per D-07/Entscheidung 3"
    - "Historische Einträge mit claimed=false tragen Badge variant=muted mit Text unbestätigt (D-09)"
    - "Historische Einträge mit member_slug gesetzt haben Link auf /members/[slug]; ohne slug nur Textdarstellung (D-10)"
    - "Mitglieder mit profile_status=memorial erscheinen ausschließlich in FansubTeamMemorialBlock"
    - "Aktive Mitglieder werden prominent in Card variant=elevated, Ehemalige in Card variant=flat gerendert"
    - "FansubContributorsSection filtert auf visibility=public AND review_status=approved (D-12)"
    - "Alle Dateien bleiben unter 450 Zeilen (CLAUDE.md)"
  artifacts:
    - path: "frontend/src/components/fansubs/FansubTeamSection.tsx"
      provides: "Team-Block-Orchestrator — delegiert an drei Sub-Komponenten"
      min_lines: 30
    - path: "frontend/src/components/fansubs/FansubTeamActiveGroup.tsx"
      provides: "Aktive App-Mitglieder als Avatar-Kachel-Raster (Card elevated)"
      min_lines: 30
    - path: "frontend/src/components/fansubs/FansubTeamHistoricalGroup.tsx"
      provides: "Ehemalige + Historische Nennungen mit Badge unbestätigt und Verlinkungslogik"
      min_lines: 40
    - path: "frontend/src/components/fansubs/FansubTeamMemorialBlock.tsx"
      provides: "Gedenken-Block (Card flat, kein Avatar, kein Badge)"
      min_lines: 20
    - path: "frontend/src/components/fansubs/FansubContributorsSection.tsx"
      provides: "Externe Mitwirkende — Visibility/Review-Filterung + Verlinkung"
      min_lines: 30
  key_links:
    - from: "FansubTeamSection.tsx props"
      to: "members[] und historical[] aus DomainProjectionResponse"
      via: "Props: { members: DomainMemberRow[]; historical: DomainHistoricalRow[] }"
      pattern: "DomainMemberRow|DomainHistoricalRow"
    - from: "FansubContributorsSection.tsx props"
      to: "contributors[] aus DomainProjectionResponse"
      via: "Props: { contributors: DomainContributorRow[] }"
      pattern: "DomainContributorRow"
    - from: "FansubTeamHistoricalGroup.tsx"
      to: "/members/[slug]"
      via: "Link href={'/members/' + member.member_slug} wenn member_slug !== null"
      pattern: "/members/"
---

<objective>
Plan 03 implementiert die Team & Mitglieder-Sektion (D-07/D-08/D-09/D-10) und die Externe
Mitwirkende-Sektion (D-07/D-12). Diese Abschnitte konsumieren Phase-72-DTOs
(`DomainMemberRow`, `DomainHistoricalRow`, `DomainContributorRow`). Da FansubTeamSection und
FansubMediaSection (Plan 04) beide auf Phase-72-Typen aufbauen aber unterschiedliche Dateien
berühren, laufen Plan 03 und Plan 04 in derselben Wave 3 parallel.

Purpose: Separation von Mitglieder- und Mitwirkenden-Daten (Entscheidung 3), korrekte Untergruppen-
Darstellung, Badge-Logik, Verlinkung historischer Mitglieder und Visibility-Filter für Contributors.
Output: FansubTeamSection + 3 Sub-Komponenten + FansubContributorsSection + 1 CSS-Datei.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-CONTEXT.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-RESEARCH.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-PATTERNS.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-UI-SPEC.md

<interfaces>
<!-- Phase-72-Typen (aus frontend/src/types/domain-projection.ts, nach Phase-72-Ausführung): -->
<!-- DomainProjectionResponse: { members: DomainMemberRow[]; historical: DomainHistoricalRow[]; contributors: DomainContributorRow[] } -->
<!--                                                                                                                                   -->
<!-- DomainMemberRow: {                                                                                                                 -->
<!--   member_display_name: string                                                                                                      -->
<!--   member_slug: string | null                                                                                                       -->
<!--   roles: string[]                                                                                                                  -->
<!--   role_labels: string[]                                                                                                            -->
<!--   profile_status: 'active' | 'historical' | 'memorial'                                                                            -->
<!--   claimed: boolean                                                                                                                 -->
<!-- }                                                                                                                                  -->
<!--                                                                                                                                   -->
<!-- DomainHistoricalRow: {                                                                                                             -->
<!--   member_display_name: string                                                                                                      -->
<!--   member_slug: string | null                                                                                                       -->
<!--   roles: string[]                                                                                                                  -->
<!--   role_labels: string[]                                                                                                            -->
<!--   profile_status: 'active' | 'historical' | 'memorial'                                                                            -->
<!--   claimed: boolean                                                                                                                 -->
<!-- }                                                                                                                                  -->
<!--                                                                                                                                   -->
<!-- DomainContributorRow: {                                                                                                            -->
<!--   member_display_name: string                                                                                                      -->
<!--   member_slug: string | null                                                                                                       -->
<!--   roles: string[]                                                                                                                  -->
<!--   role_labels: string[]                                                                                                            -->
<!--   dispute_state: 'none' | 'open' | 'resolved'                                                                                     -->
<!--   visibility: string                                                                                                               -->
<!--   review_status: string                                                                                                            -->
<!-- }                                                                                                                                  -->

<!-- UI-Primitive: Badge.tsx — variant: 'muted' für "unbestätigt" (D-09); 'neutral' für "In Erinnerung" (D-08) -->
<!-- UI-Primitive: Card.tsx — variant: 'elevated' für aktiv; 'flat' für ehemalig/memorial -->
<!-- UI-Primitive: SectionHeader, EmptyState (variant compact) -->

<!-- formatYearRange aus FansubProfileTabs.tsx (Zeilen 39-44): -->
<!-- function formatYearRange(sinceYear?: number | null, untilYear?: number | null): string -->
<!-- { if since && until: "since - until"; if since: "seit since"; if until: "bis until"; else "Zeitraum unbekannt" } -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FansubTeamSection + drei Sub-Komponenten (D-07, D-08, D-09, D-10)</name>
  <files>
    frontend/src/components/fansubs/FansubTeamSection.tsx,
    frontend/src/components/fansubs/FansubTeamActiveGroup.tsx,
    frontend/src/components/fansubs/FansubTeamHistoricalGroup.tsx,
    frontend/src/components/fansubs/FansubTeamMemorialBlock.tsx,
    frontend/src/components/fansubs/FansubTeamSection.module.css
  </files>
  <read_first>
    - frontend/src/components/fansubs/FansubProfileTabs.tsx (Zeilen 69–178 — bestehende membersByRole-Logik, formatYearRange, Rendering-Muster)
    - frontend/src/components/ui/Badge.tsx (variant-Interface — muted für unbestätigt, neutral für Gedenken)
    - frontend/src/components/ui/Card.tsx (variant-Interface — elevated, flat)
    - frontend/src/components/ui/SectionHeader.tsx (Props)
    - frontend/src/components/ui/EmptyState.tsx (Props, variant compact)
    - frontend/src/components/fansubs/FansubProfileTabs.module.css (memberGroup, memberCard, memberName CSS-Klassen)
    - .planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-UI-SPEC.md (Copywriting-Kontrakt: Untergruppen-Labels, Empty States)
    - .planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-PATTERNS.md (FansubTeamSection Pattern-Abschnitt)
  </read_first>
  <behavior>
    - FansubTeamSection empfängt members[] und historical[] — NIEMALS contributors[]
    - Aktive: members[] wo profile_status != 'memorial' → FansubTeamActiveGroup
    - Memorial: members[] wo profile_status = 'memorial' + historical[] wo profile_status = 'memorial' → FansubTeamMemorialBlock
    - Ehemalig/Historisch: historical[] wo profile_status != 'memorial' → FansubTeamHistoricalGroup
    - FansubTeamHistoricalGroup: claimed=false → Badge variant="muted" text="unbestätigt"; claimed=true + member_slug → Link; claimed=true ohne slug → nur Text
    - FansubTeamMemorialBlock: Card flat, kein Avatar, kein Badge (würdevoll), Badge variant="neutral" nur wenn Label nötig
    - Empty State: EmptyState variant="compact" title="Keine Mitglieder eingetragen" wenn alle Listen leer
  </behavior>
  <action>
    Erstelle `FansubTeamSection.module.css` mit folgenden Klassen:
    - `.teamSection`: kein extra Padding, setzt section-Rahmen
    - `.subgroupTitle`: `font-size: 18px; font-weight: 600; color: var(--text-primary); margin: 16px 0 8px;`
    - `.activeGrid`: `display: grid; grid-template-columns: repeat(auto-fill, minmax(180px, 1fr)); gap: 12px;`
    - `.historicalList`: `display: flex; flex-direction: column; gap: 8px;`
    - `.historicalEntry`: `display: flex; align-items: center; gap: 8px; color: var(--text-muted); font-size: 14px;`
    - `.memorialSection`: `border-top: 1px solid var(--color-border); padding-top: 16px; margin-top: 16px;`

    Erstelle `FansubTeamActiveGroup.tsx` — Props: `{ members: DomainMemberRow[] }`.
    Kein `'use client'` (Server Component). Renders: `<div className={styles.activeGrid}>` mit je einer
    `<Card variant="elevated" key={member.member_display_name}>` pro Mitglied.
    Inhalt der Karte: Anzeigename (fett), Rollen als kommagetrennte role_labels.
    Verlinkung (D-10): wenn `member.member_slug` → `<Link href={'/members/' + member.member_slug}>Name</Link>`;
    sonst `<span>Name</span>`.
    Wenn `members.length === 0` → gibt `null` zurück (kein Subsection-Text nötig — FansubTeamSection zeigt globalen Empty State).

    Erstelle `FansubTeamHistoricalGroup.tsx` — Props: `{ historical: DomainHistoricalRow[] }`.
    Filtert `memorial`-Einträge heraus (diese kommen in FansubTeamMemorialBlock): nur
    `historical.filter(m => m.profile_status !== 'memorial')`.
    Dann aufteilen: `former` (claimed=true) und `unconfirmed` (claimed=false).

    Für `former`-Gruppe: Untergruppen-Überschrift `"Ehemalige Mitglieder"` als kleines h3,
    jeder Eintrag als `<Card variant="flat">` mit Name + Rollen.
    Verlinkung: `member_slug` vorhanden → `<Link href={'/members/' + member.member_slug}>`.

    Für `unconfirmed`-Gruppe: Untergruppen-Überschrift `"Historische Nennungen"` als kleines h3,
    jeder Eintrag OHNE Card-Wrapper (gedämpfte Text-Zeile, D-09), Klasse `styles.historicalEntry`,
    Name als Text-Span (kein Link — ungeclaimt), `<Badge variant="muted">unbestätigt</Badge>`,
    role_labels kommagetrennt.
    Importiert `Badge, Card from '@/components/ui'` und `Link from 'next/link'`.

    Extrahiere `formatYearRange` aus FansubProfileTabs.tsx (Zeilen 39–44) als lokale Hilfsfunktion
    in `FansubTeamHistoricalGroup.tsx`; nutzt sie für Zeitraum-Anzeige neben den Einträgen.

    Erstelle `FansubTeamMemorialBlock.tsx` — Props: `{ memorial: Array<DomainMemberRow | DomainHistoricalRow> }`.
    Wenn `memorial.length === 0` → gibt `null` zurück.
    Render: `<div className={styles.memorialSection}>`, Überschrift `"In Erinnerung"` als h3,
    jeder Eintrag als `<Card variant="flat">` ohne Avatar-Bild, mit Name und role_labels.
    Kein Aktivitäts-Badge (würdevoller Block, D-08). Badge `variant="neutral"` nur wenn unbedingt nötig.
    Verlinkung wenn `member_slug` vorhanden.

    Erstelle `FansubTeamSection.tsx` — Props:
    `{ members: DomainMemberRow[]; historical: DomainHistoricalRow[] }`.
    Kein `contributors[]`-Prop — Compilezeitfehler wenn jemand contributors übergibt.
    Trennt memorial-Einträge: `memorialMembers = members.filter(m => m.profile_status === 'memorial')`.
    `activeMembers = members.filter(m => m.profile_status !== 'memorial')`.
    `historicalNonMemorial = historical.filter(h => h.profile_status !== 'memorial')`.
    `memorialHistorical = historical.filter(h => h.profile_status === 'memorial')`.
    `allMemorial = [...memorialMembers, ...memorialHistorical]`.

    Render: `<section id="team">`, `<SectionHeader eyebrow="Fansub" title="Team & Mitglieder" />`.
    Wenn alle Listen leer: `<EmptyState variant="compact" title="Keine Mitglieder eingetragen" description="Für diese Gruppe sind noch keine Mitglieder erfasst." />`.
    Sonst: `<FansubTeamActiveGroup members={activeMembers} />`,
    dann `<FansubTeamHistoricalGroup historical={historicalNonMemorial} />`,
    dann `<FansubTeamMemorialBlock memorial={allMemorial} />`.

    Zeilenlimit-Check: Jede der vier Dateien darf max. 450 Zeilen haben. Bei Überschreitung
    weitere Helper-Funktionen in eine lokale `.helpers.ts`-Datei auslagern.
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck 2>&1 | grep -E "FansubTeam" | head -20 || echo "typecheck-ok"</automated>
  </verify>
  <acceptance_criteria>
    - Quell-Assertion: `FansubTeamSection.tsx` Props-Interface enthält `members: DomainMemberRow[]` und `historical: DomainHistoricalRow[]` aber KEIN `contributors`-Feld.
    - Quell-Assertion: `FansubTeamHistoricalGroup.tsx` enthält `<Badge variant="muted">unbestätigt</Badge>` für `claimed=false`-Einträge.
    - Quell-Assertion: `FansubTeamHistoricalGroup.tsx` enthält `href={'/members/' + member.member_slug}` (oder Template-Literal-Äquivalent) mit Prüfung auf `member.member_slug !== null` vor dem Link.
    - Quell-Assertion: `FansubTeamMemorialBlock.tsx` enthält KEIN Avatar-`<img>` oder `<Image />`-Tag für memorial-Einträge.
    - Quell-Assertion: Alle vier TSX-Dateien haben weniger als 450 Zeilen.
    - Quell-Assertion: SectionHeader title="Team & Mitglieder" in FansubTeamSection.tsx (exakter Wortlaut, Kaufmanns-Und, kein Bindestrich).
    - Behavior-Assertion: `npm run typecheck` zeigt keine FansubTeam-spezifischen Fehler.
  </acceptance_criteria>
  <done>FansubTeamSection + 3 Sub-Komponenten implementiert, korrekte Separation, Badge-Logik, Verlinkungslogik, alle unter 450 Zeilen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FansubContributorsSection — externe Mitwirkende mit Visibility-Filter (D-07, D-12)</name>
  <files>
    frontend/src/components/fansubs/FansubContributorsSection.tsx
  </files>
  <read_first>
    - frontend/src/components/fansubs/FansubTeamHistoricalGroup.tsx (soeben erstellt — Verlinkungsmuster für member_slug, Badge-Import)
    - frontend/src/components/ui/Card.tsx (variant flat für Contributor-Liste)
    - frontend/src/components/ui/SectionHeader.tsx, EmptyState.tsx
    - .planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-PATTERNS.md (FansubContributorsSection Pattern-Abschnitt)
    - .planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-UI-SPEC.md (Copywriting: „Externe Mitwirkende" Abschnittskopf, Empty State)
    - .planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-CONTEXT.md (D-07: Contribution erscheint NIE im Team-Block; D-12: Filterung visibility + review_status)
  </read_first>
  <behavior>
    - FansubContributorsSection empfängt contributors[] — NIEMALS members[] oder historical[]
    - Filtert auf visibility='public' AND review_status='approved' vor Render (D-12)
    - Verlinkung wenn member_slug gesetzt; sonst nur Text (analog zu historischen Mitgliedern)
    - Empty State wenn nach Filterung kein Element übrig
    - SectionHeader title="Externe Mitwirkende" (Pflicht-Label D-02)
    - section id="mitwirkende"
  </behavior>
  <action>
    Erstelle `FansubContributorsSection.tsx` — Props: `{ contributors: DomainContributorRow[] }`.
    Kein `'use client'` (Server Component). Kein members[]- oder historical[]-Prop.

    Visibility/Review-Filter (D-12, Pflicht vor Render):
    `const visibleContributors = contributors.filter(c => c.visibility === 'public' && c.review_status === 'approved')`.

    Render: `<section id="mitwirkende">`, `<SectionHeader title="Externe Mitwirkende" />`.
    Wenn `visibleContributors.length === 0`:
    `<EmptyState variant="compact" title="Keine externen Mitwirkenden" description="Für diese Gruppe sind noch keine Mitwirkenden eingetragen." />`.
    Sonst: `<ul>` oder `<div className={styles.contributorList}>` mit je einem Eintrag als
    `<Card variant="flat" key={c.member_display_name}>`:
    - Name: `c.member_slug ? <Link href={'/members/' + c.member_slug}>{c.member_display_name}</Link> : <span>{c.member_display_name}</span>`
    - Rollen: `<span>{c.role_labels.join(', ')}</span>`
    CSS-Klassen aus `FansubTeamSection.module.css` (bestehend) oder inline style für den List-Container.
    Kein Avatar.

    Imports: `Link from 'next/link'`, `DomainContributorRow from '@/types/domain-projection'`,
    `Card, SectionHeader, EmptyState from '@/components/ui'`.
    Kein nativer `<button>`, `<input>`, `<select>`.
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck 2>&1 | grep -E "FansubContributors" | head -10 || echo "typecheck-ok"</automated>
  </verify>
  <acceptance_criteria>
    - Quell-Assertion: `FansubContributorsSection.tsx` Props-Interface enthält NUR `contributors: DomainContributorRow[]` — kein `members` oder `historical`.
    - Quell-Assertion: Filterbedingung `c.visibility === 'public' && c.review_status === 'approved'` ist vorhanden (exakte String-Werte, KEIN partial-match).
    - Quell-Assertion: Empty State title ist exakt `"Keine externen Mitwirkenden"` (UI-SPEC Copywriting).
    - Quell-Assertion: SectionHeader title ist exakt `"Externe Mitwirkende"` (Pflicht-Label, CONTEXT.md D-02).
    - Quell-Assertion: Verlinkung-Logik überprüft `c.member_slug !== null` vor `Link`-Render.
    - Behavior-Assertion: `npm run typecheck` zeigt keine FansubContributors-spezifischen Fehler.
  </acceptance_criteria>
  <done>FansubContributorsSection mit korrektem D-12-Filter und D-07-Separation implementiert, compilierbar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend domain-projection → Frontend | DomainProjectionResponse enthält Daten aus drei Quell-Tabellen; Phase-72-Backend verantwortet Filterung auf auth-context; Phase 73 filtert zusätzlich auf visibility/review_status für public render |
| contributor.member_slug → Link | Slug kommt aus DB; Next.js Link-href wird durch React escaped; kein Open-Redirect (relativer Pfad /members/[slug]) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-03-01 | Information Disclosure | FansubContributorsSection — visibility-Filter | mitigate | `visibility='public' AND review_status='approved'` als Pflicht-Filter vor Render (D-12); interne/in-review Medien werden nicht gerendert |
| T-73-03-02 | Information Disclosure | FansubTeamHistoricalGroup — unconfirmed entries | accept | Historische Nennungen ohne Claim sind absichtlich öffentlich sichtbar (gedämpft); nur Anzeigename und Rollen; kein PII |
| T-73-03-03 | Tampering | Vermischung von members[] und contributors[] | mitigate | Props-Interfaces sind strikt typisiert; TypeScript verhindert Übergabe von contributors[] an FansubTeamSection zur Compile-Zeit |
| T-73-03-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neuen Pakete; slopcheck nicht erforderlich |
</threat_model>

<verification>
- `npm run typecheck` ohne FansubTeam- oder FansubContributors-Fehler.
- `npx vitest run FansubTeamSection` — nach Implementierung der it.todo-Tests: alle grün.
- FansubTeamSection.tsx: kein `contributors`-Prop im Interface.
- FansubContributorsSection.tsx: Filterbedingung `visibility === 'public' && review_status === 'approved'` vorhanden.
- Alle fünf neuen Dateien unter 450 Zeilen.
</verification>

<success_criteria>
- D-07 (Separation members vs. contributors) durch Props-Interfaces erzwungen.
- D-08/D-09 (Untergruppen + Badge unbestätigt) in FansubTeamHistoricalGroup implementiert.
- D-10 (Verlinkung historisch) in FansubTeamHistoricalGroup und FansubContributorsSection implementiert.
- D-12 (Visibility-Filter) in FansubContributorsSection implementiert.
- Alle Pflicht-Labels aus UI-SPEC Copywriting-Kontrakt verwendet.
- Alle Dateien unter 450 Zeilen.
</success_criteria>

<output>
Create `.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-03-SUMMARY.md` when done
</output>
