---
phase: 73-public-fansub-page-fansubs-slug-erweitern
plan: "09"
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - frontend/src/components/fansubs/FansubStorySection.tsx
  - frontend/src/components/fansubs/FansubMediaSection.tsx
  - frontend/src/lib/fansub-summary.ts
autonomous: true
requirements: [D-02, D-11, D-15]
must_haves:
  truths:
    - "Geschichte-Sektion zeigt einen sinnvollen Empty-State wenn kein Story-Text vorhanden — nicht die Hero-Metazeile"
    - "Medien-Sektion zeigt 'Gruppenmedien'-Bereich nur wenn dort nicht-redundante Inhalte existieren ODER zeigt einen klaren Empty-State ohne Logo+Banner zu wiederholen"
  artifacts:
    - path: "frontend/src/components/fansubs/FansubStorySection.tsx"
      provides: "Story-Sektion nutzt echten story_text-Inhalt, fällt auf Empty-State zurück statt Metazeile"
    - path: "frontend/src/lib/fansub-summary.ts"
      provides: "buildFansubFactSummary wird nicht mehr als Story-Inhalt missbraucht"
    - path: "frontend/src/components/fansubs/FansubMediaSection.tsx"
      provides: "Gruppenmedien-Block nur bei nicht-redundantem Inhalt oder mit sauberem Empty-State"
  key_links:
    - from: "FansubStorySection"
      to: "group.description / group.story (wenn vorhanden)"
      via: "Direktfeld statt buildFansubFactSummary"
    - from: "FansubGroupMediaBlock"
      to: "FansubMediaSection"
      via: "Entscheidung: Banner/Logo-Fallback entfernt aus Gruppenmedien-Block"
---

<objective>
Behebt zwei Minor-Findings der UAT:

UAT-8 (Geschichte-Redundanz): FansubStorySection nutzt buildFansubFactSummary als Story-Inhalt und zeigt damit "1999 bis 2022 • Deutschland • aktiv" — identisch mit der Hero-Metazeile. Das ist kein Story-Text. Fix: FansubStorySection zeigt nur echten story_text (group.description oder ein dediziertes Textfeld). Wenn kein Text vorhanden, kommt ein aussagekräftiger Empty-State.

UAT-6 (Medien-Redundanz): Gruppenmedien-Block zeigt exakt dasselbe Logo + Banner wie der Hero, wenn keine anderen Medien vorhanden sind. Fix: Der Logo/Banner-Fallback in FansubGroupMediaBlock wird entfernt. Wenn keine freigegebenen Gruppenmedien (mediaRows mit owner_type=fansub_group, visibility=public, review_status=approved) vorhanden, zeigt der Block den bestehenden EmptyState ("Noch keine Medien hinterlegt").

Output: FansubStorySection mit korrekter Story-Logik; FansubGroupMediaBlock ohne Logo/Banner-Fallback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@frontend/src/components/fansubs/FansubStorySection.tsx
@frontend/src/components/fansubs/FansubGroupMediaBlock.tsx
@frontend/src/components/fansubs/FansubMediaSection.tsx
@frontend/src/lib/fansub-summary.ts
@frontend/src/types/fansub.ts

<interfaces>
<!-- FansubGroup aus fansub.ts hat KEIN 'description'- oder 'story'-Feld. -->
<!-- Die einzigen textlichen Felder im Typ sind: name, slug, country, website_url, discord_url, irc_url -->
<!-- buildFansubFactSummary baut eine Metazeile aus founded_year/dissolved_year/country/status -->

<!-- DESIGN-ENTSCHEIDUNG: Da FansubGroup keinen story_text/description-Feld hat, -->
<!-- kann FansubStorySection keinen echten Story-Text zeigen. -->
<!-- Fix: FansubStorySection zeigt DIREKT den Empty-State, wenn buildFansubFactSummary -->
<!-- das EINZIGE wäre, was gezeigt werden könnte (= keine echten Inhalte). -->
<!-- Die Metazeile wird NICHT mehr als "Inhalt" für CollapsibleStory missbraucht. -->
<!-- CollapsibleStory wird nur angezeigt, wenn ein zukünftiges description-Feld befüllt ist. -->
<!-- Da description aktuell nicht existiert, zeigt die Sektion immer den Empty-State. -->
<!-- Das ist korrekt und vermeidet die Hero-Redundanz. -->

<!-- FansubGroupMediaBlock: Aktueller Fallback-Zweig: -->
if (publicGroupMedia.length === 0 && (group.logo_url || group.banner_url)) {
  return <Image src={group.logo_url} ... /> + <Image src={group.banner_url} ... />
}
<!-- Dieser Zweig soll entfernt werden — führt direkt zum EmptyState. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FansubStorySection — buildFansubFactSummary nicht als Story-Inhalt nutzen</name>
  <files>frontend/src/components/fansubs/FansubStorySection.tsx, frontend/src/lib/fansub-summary.ts</files>
  <action>
Bearbeite FansubStorySection.tsx:

Aktueller Stand: `const storyContent = buildFansubFactSummary(group)` wird als Inhalt für CollapsibleStory genutzt. Diese Metazeile ist identisch mit dem Hero und kein Story-Text.

Neuer Stand: Da FansubGroup kein story_text/description-Feld hat, zeigt die Sektion immer den Empty-State. Entferne den buildFansubFactSummary-Aufruf und den CollapsibleStory-Render-Zweig. Die Sektion rendert ausschließlich den EmptyState mit dem bestehenden Text:
- title: "Noch keine Geschichte hinterlegt"
- description: "Die Gruppe hat bisher noch keine Beschreibung veröffentlicht."

Entferne den Import von buildFansubFactSummary und CollapsibleStory aus dieser Datei, da sie nicht mehr benötigt werden.

Behalte den Import von EmptyState und SectionHeader aus @/components/ui.
Behalte section id="geschichte".

Hinweis für Executor: buildFansubFactSummary.ts selbst NICHT ändern — die Funktion wird anderswo genutzt (z.B. FansubHeroSection). Nur den Import in FansubStorySection.tsx entfernen.

Bearbeite fansub-summary.ts NICHT (Datei ist korrekt, wird nur falsch verwendet).

Bearbeite FansubMediaSection.tsx NICHT in diesem Task (separater Task).
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>FansubStorySection.tsx: kein buildFansubFactSummary-Import mehr, kein CollapsibleStory-Render, zeigt immer EmptyState; tsc fehlerfrei</done>
</task>

<task type="auto">
  <name>Task 2: FansubGroupMediaBlock — Logo/Banner-Fallback entfernen</name>
  <files>frontend/src/components/fansubs/FansubGroupMediaBlock.tsx</files>
  <action>
Bearbeite FansubGroupMediaBlock.tsx.

Entferne den mittleren Fallback-Zweig der Funktion, der Logo und Banner rendert wenn keine freigegebenen Gruppenmedien vorhanden sind:

Zu entfernender Code (ca. Zeilen 37–50):
  if (publicGroupMedia.length === 0 && (group.logo_url || group.banner_url)) {
    return (
      <div style={{ display: 'grid', gap: 12 }}>
        {group.logo_url ? ( <Image src={group.logo_url} ... /> ) : null}
        {group.banner_url ? ( <Image src={group.banner_url} ... /> ) : null}
      </div>
    )
  }

Nach Entfernung fällt der Code direkt auf den bestehenden EmptyState durch wenn publicGroupMedia.length === 0. Der EmptyState bleibt unverändert:
  <EmptyState variant="compact" title="Noch keine Medien hinterlegt" description="..." />

Prüfe nach Entfernung:
- Werden Image und group.logo_url/banner_url noch anderswo in der Datei verwendet?
- Wenn nicht mehr: Entferne den Image-Import und das group.logo_url/group.banner_url aus dem Props-Pick.
- Aktuell ist Props-Typ: Pick<FansubGroup, 'id' | 'logo_url' | 'banner_url'>
- Nach Entfernung: Pick<FansubGroup, 'id'> (logo_url/banner_url nicht mehr nötig)

Hinweis: Task 73-06-Task-2 hat möglicherweise bereits resolveApiUrl hinzugefügt — falls ja, entferne diese Änderungen aus diesem Zweig mit, da der Zweig vollständig entfernt wird. Halte Änderungen minimal und prüfe Konfliktfreiheit mit 73-06.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>FansubGroupMediaBlock.tsx: Fallback-Zweig mit Logo/Banner entfernt; bei publicGroupMedia.length === 0 zeigt Block EmptyState; Props-Typ enthält keine ungenutzten logo_url/banner_url mehr; tsc fehlerfrei</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| keine neuen | Reine UI-Vereinfachung, kein neuer API-Aufruf, keine Auth-Grenze |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-09-01 | Information Disclosure | FansubGroupMediaBlock | accept | Entfernung des Fallbacks reduziert Datenmenge (sicherer) |
</threat_model>

<verification>
1. /fansubs/animeownage laden
2. Geschichte-Sektion: Zeigt Empty-State "Noch keine Geschichte hinterlegt" (NICHT die Metazeile "1999 bis 2022 • Deutschland • aktiv")
3. Medien-Sektion → Gruppenmedien: Zeigt "Noch keine Medien hinterlegt" (NICHT Logo+Banner-Duplikat des Hero)
4. Hero-Sektion: Unverändert (Logo + Banner weiterhin sichtbar)
5. Keine JS-Konsolfehler
</verification>

<success_criteria>
- Geschichte-Sektion zeigt keine Hero-Metazeile mehr als "Inhalt"
- Gruppenmedien zeigt keine doppelten Logo/Banner mehr
- Hero-Sektion zeigt unverändert Logo + Banner
- tsc --noEmit: 0 Fehler
</success_criteria>

<output>
Erstelle `.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-09-SUMMARY.md` nach Abschluss.
</output>
