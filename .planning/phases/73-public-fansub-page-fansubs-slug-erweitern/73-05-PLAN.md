---
phase: 73-public-fansub-page-fansubs-slug-erweitern
plan: 05
type: execute
wave: 4
depends_on: ["73-01b", "73-03", "73-04"]
files_modified:
  - frontend/src/app/fansubs/[slug]/page.tsx
  - frontend/src/app/fansubs/[slug]/page.module.css
  - frontend/src/components/fansubs/FansubProfileTabs.tsx
autonomous: false
requirements: [B, C, G, K]
must_haves:
  truths:
    - "page.tsx ist ≤ 150 Zeilen und orchestriert ausschließlich Datenfetch + Section-Rendering"
    - "Alle Phase-72-Fetches laufen via Promise.allSettled mit Graceful Degradation (leere Arrays bei Fehler)"
    - "Abschnittsreihenfolge entspricht exakt D-02: Hero → Geschichte → Höhepunkte → Projekte → Team → Mitwirkende → Medien → Timeline → Deep-Dive"
    - "FansubSectionNav ist die erste gerenderte Komponente nach <main>"
    - "FansubProfileTabs.tsx ist nach Extraktion aller Inhalte korrekt bereinigt"
    - "Altlasten Gruendung/Aufloesung in FansubProfileTabs.tsx sind korrigiert"
    - "Native <button>-Elemente in FansubProfileTabs.tsx (Zeilen 118–131) sind durch Button-Primitive aus @/components/ui ersetzt"
    - "Besucher sieht /fansubs/[slug] als kuratierte Erzählseite mit neun Abschnitten in der vorgeschriebenen Reihenfolge"
  artifacts:
    - path: "frontend/src/app/fansubs/[slug]/page.tsx"
      provides: "Root Server Component — paralleler Datenfetch + Section-Orchestrierung"
      min_lines: 60
    - path: "frontend/src/app/fansubs/[slug]/page.module.css"
      provides: "Erweitertes CSS mit readingColumn, section-Abständen, gridSection"
      min_lines: 30
    - path: "frontend/src/components/fansubs/FansubProfileTabs.tsx"
      provides: "Bereinigter Tab-Container — native button auf Button-Primitive migriert, Umlaut-Altlasten korrigiert"
  key_links:
    - from: "page.tsx"
      to: "getFansubGroupDomainProjection(group.id)"
      via: "import { getFansubGroupDomainProjection } from '@/lib/api'"
      pattern: "getFansubGroupDomainProjection"
    - from: "page.tsx"
      to: "getMediaOwnershipProjection('group', group.id)"
      via: "import { getMediaOwnershipProjection } from '@/lib/api'"
      pattern: "getMediaOwnershipProjection"
    - from: "page.tsx Promise.allSettled"
      to: "Graceful Degradation — fehlgeschlagene Phase-72-Fetches geben leere Arrays zurück"
      via: "result.status === 'fulfilled' ? result.value : { members: [], historical: [], contributors: [] }"
      pattern: "allSettled"
    - from: "FansubSectionNav SECTION_IDS"
      to: "section[id]-Attribute aller Section-Komponenten in page.tsx"
      via: "SECTION_IDS=['geschichte','hoehepunkte','projekte','team','mitwirkende','medien','timeline','deep-dive'] müssen exakt mit section id='...' in Render übereinstimmen"
      pattern: "id=\"geschichte\"|id=\"hoehepunkte\"|id=\"projekte\"|id=\"team\""
---

<objective>
Plan 05 ist der Abschluss-Plan: Die bestehende `page.tsx` wird zu einer schlanken Root-Server-
Komponente (≤ 150 Zeilen) umgebaut, die alle Daten parallel via `Promise.allSettled` fetcht und
die in Plans 01–04 erstellten Section-Komponenten in der vorgeschriebenen Reihenfolge (D-02)
zusammensetzt. `FansubProfileTabs.tsx` wird bereinigt (Umlaut-Fix + native `<button>` auf
Button-Primitive migriert). `page.module.css` erhält die neuen Layout-Klassen.

Purpose: Integriert alle vorherigen Wave-Komponenten in die lebende Public-Fansub-Seite;
liefert die vollständige kuratierte Erzählseite D-02.
Output: Fertige `/fansubs/[slug]` Seite, bereinigte FansubProfileTabs, vollständige Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-CONTEXT.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-RESEARCH.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-PATTERNS.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-UI-SPEC.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-01-SUMMARY.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-01b-SUMMARY.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-02-SUMMARY.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-03-SUMMARY.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-04-SUMMARY.md

<interfaces>
<!-- Bestehende page.tsx (frontend/src/app/fansubs/[slug]/page.tsx — 117 Zeilen) -->
<!-- Imports: Link, FansubProfileTabs, GroupLeaderTimeline, AnimeListItem, PublicFansubLeaderEntry -->
<!--          ApiError, getAnimeList, getFansubBySlug, getFansubContributions, getFansubMembers -->
<!--          buildFansubFactSummary, styles -->

<!-- api.ts-Funktionen (in Plan 73-01b angelegt, ggf. durch Phase-72-Plan-04 überschrieben): -->
<!-- getFansubGroupDomainProjection(groupID: number): Promise<DomainProjectionResponse> -->
<!-- getMediaOwnershipProjection(ownerType: string, ownerID: number): Promise<MediaOwnershipRow[]> -->

<!-- Section-Komponenten (aus Plans 01-04): -->
<!-- FansubSectionNav.tsx — import { FansubSectionNav } from '@/components/fansubs/FansubSectionNav' -->
<!-- FansubHeroSection.tsx — import { FansubHeroSection } from '@/components/fansubs/FansubHeroSection' -->
<!-- FansubStorySection.tsx — import { FansubStorySection } from '@/components/fansubs/FansubStorySection' -->
<!-- FansubHighlightsSection.tsx — import { FansubHighlightsSection } from '@/components/fansubs/FansubHighlightsSection' -->
<!-- FansubProjectsSection.tsx — import { FansubProjectsSection } from '@/components/fansubs/FansubProjectsSection' -->
<!-- FansubTeamSection.tsx — import { FansubTeamSection } from '@/components/fansubs/FansubTeamSection' -->
<!-- FansubContributorsSection.tsx — import { FansubContributorsSection } from '@/components/fansubs/FansubContributorsSection' -->
<!-- FansubMediaSection.tsx — import { FansubMediaSection } from '@/components/fansubs/FansubMediaSection' -->
<!-- FansubDeepDiveSection.tsx — import { FansubDeepDiveSection } from '@/components/fansubs/FansubDeepDiveSection' -->
<!-- GroupLeaderTimeline.tsx — bereits in page.tsx importiert -->

<!-- DomainProjectionResponse Leer-Fallback: { members: [], historical: [], contributors: [] } -->

<!-- FansubProfileTabs.tsx Altlasten (RESEARCH.md verifiziert): -->
<!-- Zeile 148: "Gruendung" → "Gründung" (user-facing String — CLAUDE.md Umlaut-Pflicht) -->
<!-- Zeile 150: "Aufloesung" → "Auflösung" (user-facing String — CLAUDE.md Umlaut-Pflicht) -->
<!-- Zeilen 118–131: native <button> → Button-Primitive aus @/components/ui (CLAUDE.md Primitive-Pflicht) -->

<!-- SECTION_IDS aus FansubSectionNav.tsx (Plan 73-01): -->
<!-- ['geschichte', 'hoehepunkte', 'projekte', 'team', 'mitwirkende', 'medien', 'timeline', 'deep-dive'] -->
<!-- Jede Section-Komponente MUSS exakt diese IDs als section id="..." tragen. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: page.tsx Umbau + page.module.css Erweiterung (D-01, D-02, D-04, D-15, K)</name>
  <files>
    frontend/src/app/fansubs/[slug]/page.tsx,
    frontend/src/app/fansubs/[slug]/page.module.css
  </files>
  <read_first>
    - frontend/src/app/fansubs/[slug]/page.tsx (vollständige aktuelle Datei — 117 Zeilen)
    - frontend/src/app/fansubs/[slug]/page.module.css (vollständige aktuelle CSS)
    - frontend/src/app/anime/[id]/page.tsx (Promise.allSettled-Muster, Zeilen 144–155 — Analog für parallelen Fetch)
    - frontend/src/lib/api.ts (verifizieren dass getFansubGroupDomainProjection + getMediaOwnershipProjection durch Plan 73-01b vorhanden sind)
    - frontend/src/components/fansubs/FansubSectionNav.tsx (SECTION_IDS-Array lesen — exakte IDs für section id="..." Alignment)
    - .planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-UI-SPEC.md (Spacing Scale, Responsive, Copywriting)
    - .planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-PATTERNS.md (page.tsx Pattern — paralleler Fetch-Shape, Slug-Validierung)
  </read_first>
  <action>
    SCHRITT 1 — api.ts-Existenzprüfung (kein Schreiben):
    Prüfe ob `getFansubGroupDomainProjection` und `getMediaOwnershipProjection` in
    `frontend/src/lib/api.ts` vorhanden sind. Plan 73-01b hat diese Funktionen definitiv angelegt —
    sie sollten bereits existieren. Wenn aus unerwartetem Grund eine Funktion fehlt, stoppe und
    melde den Befund (nicht: stillschweigend neu schreiben). Schreibe KEINEN Code in api.ts.
    Die Funktionen in api.ts werden ausschließlich durch Plan 73-01b oder Phase-72-Plan-04 verwaltet.

    SCHRITT 2 — page.tsx umschreiben (≤ 150 Zeilen):
    Importiere alle neun Section-Komponenten aus `@/components/fansubs/...`.
    Behalte `loadFansubProjects` (existierende Helper-Funktion), `FansubProfilePageProps`, Slug-Validierung.
    Ersetze sequentielle Fetches durch `Promise.allSettled`:
    Paralleles Fetching von projectsResult, contributionsResult, domainProjectionResult,
    mediaOwnershipResult. getFansubBySlug(slug) bleibt als einzelner Blocking-Fetch mit
    try/catch (404 führt zur Fehlerseite).
    Graceful Degradation für Phase-72-Fetches:
    domainProjection: status === 'fulfilled' ? value : { members: [], historical: [], contributors: [] }
    mediaRows: status === 'fulfilled' ? value : []

    Render-Reihenfolge exakt nach D-02 und exakt nach SECTION_IDS aus FansubSectionNav.tsx:
    main → FansubSectionNav → FansubHeroSection → FansubStorySection → FansubHighlightsSection
    → FansubProjectsSection → FansubTeamSection → FansubContributorsSection → FansubMediaSection
    → section id="timeline" mit GroupLeaderTimeline → FansubDeepDiveSection.

    WICHTIG — Section-ID-Alignment (gegen FansubSectionNav SECTION_IDS):
    Jede Section-Komponente trägt intern das section id="..." das dem SECTION_IDS-Eintrag entspricht.
    page.tsx umschließt GroupLeaderTimeline als: `<section id="timeline"><GroupLeaderTimeline ... /></section>`.
    Alle anderen IDs (geschichte, hoehepunkte, projekte, team, mitwirkende, medien, deep-dive) sind
    bereits in den jeweiligen Section-Komponenten (Plans 01–04) verankert.

    Breadcrumb-Nav aus der alten page.tsx kann entfernt werden (FansubSectionNav ersetzt sie).
    FansubProfileTabs wird NICHT mehr in der Render-Ausgabe verwendet.

    SCHRITT 3 — page.module.css erweitern (bestehende Klassen nicht entfernen):
    Ergänze folgende neue Klassen (append):
    `.readingColumn { width: min(780px, 100% - 48px); margin: 0 auto; }` (D-03 Lesebreite)
    `.sectionSpacing { margin-top: 32px; }` (xl = 32px zwischen Abschnitten, UI-SPEC)
    `.gridSection { width: 100%; }` (volle Seitenbreite für Projektraster und Medien)
    `.backLink { margin-top: 16px; font-size: 14px; color: var(--text-secondary); }` (wenn noch nicht vorhanden).
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck 2>&1 | head -30 && npx vitest run page 2>&1 | grep -E "pass|fail|todo" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Quell-Assertion: `page.tsx` hat weniger als 150 Zeilen nach dem Umbau.
    - Quell-Assertion: `Promise.allSettled` ist in page.tsx vorhanden mit mindestens den Calls `getFansubGroupDomainProjection` und `getMediaOwnershipProjection`.
    - Quell-Assertion: Graceful Degradation: `domainProjectionResult.status === 'fulfilled' ? domainProjectionResult.value : { members: [], historical: [], contributors: [] }`.
    - Quell-Assertion: Render enthält `<FansubSectionNav />` als erstes Kind von `<main>`.
    - Quell-Assertion: Reihenfolge der Section-Komponenten im JSX: FansubSectionNav → FansubHeroSection → FansubStorySection → FansubHighlightsSection → FansubProjectsSection → FansubTeamSection → FansubContributorsSection → FansubMediaSection → section id="timeline" → FansubDeepDiveSection.
    - Quell-Assertion: `page.module.css` enthält die Klasse `.readingColumn` mit `width: min(780px,`.
    - Quell-Assertion: `page.tsx` enthält `<section id="timeline">` als Wrapper um GroupLeaderTimeline.
    - Behavior-Assertion: `npm run typecheck` ohne Fehler in page.tsx oder page.module.css.
  </acceptance_criteria>
  <done>page.tsx als schlanker Orchestrator (≤ 150 Zeilen) mit Promise.allSettled, korrekter Abschnittsreihenfolge, Graceful Degradation und section id="timeline" implementiert.</done>
</task>

<task type="auto">
  <name>Task 2: FansubProfileTabs bereinigen — native button-Migration + Umlaut-Korrekturen</name>
  <files>
    frontend/src/components/fansubs/FansubProfileTabs.tsx
  </files>
  <read_first>
    - frontend/src/components/fansubs/FansubProfileTabs.tsx (vollständige Datei — speziell Zeilen 118–131 für native button, Zeile 148 für "Gruendung", Zeile 150 für "Aufloesung")
    - frontend/src/components/ui/Button.tsx (Props-Interface: variant, size, onClick — für Migration nativer button-Elemente)
  </read_first>
  <action>
    Behebe drei Altlasten in `FansubProfileTabs.tsx`:

    ALTLAST 1 — Native button-Migration (Zeilen 118–131):
    Lies den exakten Code der nativen `<button>`-Elemente in diesem Bereich.
    Ersetze jeden nativen `<button>` durch `<Button>` aus `@/components/ui`.
    Übertrage onClick, className (als variant/size wenn semantisch passend) und Textinhalt.
    Import ergänzen falls `Button` noch nicht importiert ist:
    `import { Button } from '@/components/ui'` — prüfe ob der Import bereits vorhanden ist.
    Kein nativer `<button>` darf nach dieser Änderung in FansubProfileTabs.tsx verbleiben
    (außer in Kommentaren oder innerhalb von @/components/ui-internen Wrappern, die nicht in
    dieser Datei liegen).

    ALTLAST 2 — Umlaut-Korrekturen in user-facing Strings (CLAUDE.md Pflicht):
    Zeile 148 (ungefähre Zeilennummer — Executor liest exakte Zeile): Ersetze `"Gruendung"` durch
    `"Gründung"` im JSX-Textknoten oder Label-String.
    Zeile 150 (ungefähre Zeilennummer): Ersetze `"Aufloesung"` durch `"Auflösung"`.
    Nur user-facing String-Inhalte (JSX-Text, Button-Labels, aria-labels, Placeholder) —
    keine Variablennamen oder CSS-Klassen ändern.

    Zeilenlimit-Check: FansubProfileTabs.tsx muss unter 450 Zeilen bleiben (CLAUDE.md).
    Prüfe die Zeilenzahl nach den Änderungen.
  </action>
  <verify>
    <automated>cd frontend && grep -n "Gruendung\|Aufloesung" src/components/fansubs/FansubProfileTabs.tsx || echo "no-ascii-umlauts" && grep -n "&lt;button" src/components/fansubs/FansubProfileTabs.tsx || grep -c "native-button-check" /dev/null 2>/dev/null; npm run typecheck 2>&1 | grep -E "FansubProfileTabs" | head -10 || echo "typecheck-ok"</automated>
  </verify>
  <acceptance_criteria>
    - Quell-Assertion: `grep -c "Gruendung\|Aufloesung" frontend/src/components/fansubs/FansubProfileTabs.tsx` gibt `0` zurück (keine ASCII-Umlaut-Ersetzungen mehr).
    - Quell-Assertion: `FansubProfileTabs.tsx` enthält `"Gründung"` und `"Auflösung"` als korrekte Umlaute in user-facing Strings.
    - Quell-Assertion: `grep -n "^[^/]*<button" frontend/src/components/fansubs/FansubProfileTabs.tsx` gibt keine Treffer zurück (keine nativen button-Elemente außerhalb von Kommentaren).
    - Quell-Assertion: `import.*Button.*@/components/ui` ist in FansubProfileTabs.tsx vorhanden (wenn nativer button migriert wurde).
    - Quell-Assertion: `wc -l frontend/src/components/fansubs/FansubProfileTabs.tsx` gibt einen Wert unter 450 aus.
    - Behavior-Assertion: `npm run typecheck` zeigt keine FansubProfileTabs-spezifischen Fehler.
  </acceptance_criteria>
  <done>FansubProfileTabs.tsx: keine ASCII-Umlaut-Ersetzungen in user-facing Strings; keine nativen button-Elemente; unter 450 Zeilen; typecheck grün.</done>
</task>

<task type="auto">
  <name>Task 3: Section-ID-Alignment verifizieren — SECTION_IDS vs. section[id]-Attribute</name>
  <files></files>
  <read_first>
    - frontend/src/components/fansubs/FansubSectionNav.tsx (SECTION_IDS-Array — exakte Werte)
    - frontend/src/components/fansubs/FansubStorySection.tsx (section id="geschichte")
    - frontend/src/components/fansubs/FansubHighlightsSection.tsx (section id="hoehepunkte")
    - frontend/src/components/fansubs/FansubProjectsSection.tsx (section id="projekte")
    - frontend/src/components/fansubs/FansubTeamSection.tsx (section id="team")
    - frontend/src/components/fansubs/FansubContributorsSection.tsx (section id="mitwirkende")
    - frontend/src/components/fansubs/FansubMediaSection.tsx (section id="medien")
    - frontend/src/app/fansubs/[slug]/page.tsx (section id="timeline" Wrapper um GroupLeaderTimeline)
    - frontend/src/components/fansubs/FansubDeepDiveSection.tsx (section id="deep-dive")
  </read_first>
  <action>
    Führe für jede der acht SECTION_IDS einen Grep-Check durch, der verifiziert dass das
    exakte `section id="..."` Attribut in der zugehörigen Datei vorhanden ist:

    `grep -n 'id="geschichte"' frontend/src/components/fansubs/FansubStorySection.tsx`
    `grep -n 'id="hoehepunkte"' frontend/src/components/fansubs/FansubHighlightsSection.tsx`
    `grep -n 'id="projekte"' frontend/src/components/fansubs/FansubProjectsSection.tsx`
    `grep -n 'id="team"' frontend/src/components/fansubs/FansubTeamSection.tsx`
    `grep -n 'id="mitwirkende"' frontend/src/components/fansubs/FansubContributorsSection.tsx`
    `grep -n 'id="medien"' frontend/src/components/fansubs/FansubMediaSection.tsx`
    `grep -n 'id="timeline"' frontend/src/app/fansubs/[slug]/page.tsx`
    `grep -n 'id="deep-dive"' frontend/src/components/fansubs/FansubDeepDiveSection.tsx`

    Wenn ein Grep keinen Treffer liefert: Öffne die entsprechende Datei und korrigiere die
    section id auf den exakten Wert aus SECTION_IDS. Das ist ein reines String-Alignment —
    keine Logic-Änderung.

    Hinweis: Der Hero-Abschnitt (FansubHeroSection) ist bewusst NICHT in SECTION_IDS — er ist
    kein Navigations-Anker. Der "history" hat section id="hero" (nicht in Nav).
  </action>
  <verify>
    <automated>cd frontend && for id in geschichte hoehepunkte projekte team mitwirkende medien deep-dive; do grep -rn "id=\"$id\"" src/components/fansubs/ src/app/fansubs/ 2>/dev/null || echo "MISSING: $id"; done && grep -n 'id="timeline"' src/app/fansubs/slug/page.tsx 2>/dev/null || grep -rn 'id="timeline"' src/app/fansubs/ 2>/dev/null || echo "CHECK timeline in page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Quell-Assertion: Für jede der acht IDs aus SECTION_IDS (`geschichte`, `hoehepunkte`, `projekte`, `team`, `mitwirkende`, `medien`, `timeline`, `deep-dive`) liefert `grep -rn "id=\"${ID}\""` in den fansubs-Komponenten oder page.tsx genau einen Treffer.
    - Quell-Assertion: Kein SECTION_ID-Eintrag ist ohne zugehöriges `section id="..."` in der Render-Ausgabe (IntersectionObserver würde sonst kein Element finden).
    - Behavior-Assertion: `npm run typecheck` bleibt ohne neue Fehler nach eventuellen String-Korrekturen.
  </acceptance_criteria>
  <done>Alle acht SECTION_IDS haben exakt passende section[id]-Attribute in den zugehörigen Komponenten; FansubSectionNav kann alle Abschnitte per document.getElementById korrekt observieren.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Die vollständige Public-Fansub-Seite `/fansubs/[slug]` wurde als kuratierte Erzählseite
    implementiert. Alle neun Abschnitte sind als Section-Komponenten vorhanden, die Sticky-Nav
    ist aktiv, und FansubProfileTabs wurde bereinigt (Umlaut-Altlasten + native button-Migration).
  </what-built>
  <how-to-verify>
    1. Dev-Server ist auf :3000 aktiv (MEMORY: Tests gegen :3000, nicht :3002).
    2. Öffne eine bekannte Fansub-Seite, z. B. http://localhost:3000/fansubs/[bekannter-slug].
    3. Prüfe Sticky-Nav: Bleibt die Nav oben beim Scrollen? Werden Chips beim Scrollen aktiviert?
    4. Prüfe Abschnittsreihenfolge: Geschichte → Höhepunkte → Projekte → Team → Mitwirkende → Medien → Timeline → Mehr
    5. Prüfe Team-Block: Gibt es einen Abschnitt „Team & Mitglieder"? Werden ggf. historische Nennungen mit „unbestätigt"-Badge angezeigt?
    6. Prüfe Medien-Block: Sind die drei Bereiche „Gruppenmedien", „Release-Einblicke", „Team & Erinnerungen" sichtbar?
    7. Prüfe Projektkarten: Verlinken sie auf `/anime/[id]/group/[groupId]` (Deep-Dive)?
    8. Prüfe leere Zustände: Für Gruppen ohne Daten erscheinen dezente Hinweise (nicht leere Seiten).
    9. Prüfe Responsivität: Auf Mobil (< 640px) alles einspalt? Sticky-Nav horizontal scrollbar?
    10. Führe `cd frontend && npm test` aus — alle Tests sollen grün oder todo sein, keine fails.
  </how-to-verify>
  <resume-signal>Eingabe "approved" wenn die Seite korrekt dargestellt wird, oder beschreibe gefundene Probleme für Reparatur.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSR Server → Public Client | page.tsx ist Server Component; keine Tokens/Cookies werden an Browser exponiert; alle Fetches server-seitig |
| slug URL-Parameter → API | slug.trim() bereits im bestehenden Code; getFansubBySlug(slug) übergibt zum Backend |
| Phase-72-Endpunkte → Frontend | allSettled degradiert graceful; 404/500 von Phase-72-Endpunkten → leere Arrays → EmptyState |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-05-01 | Information Disclosure | SSR-Fehlerbehandlung — Stack Trace in Response | mitigate | bestehende try/catch-Blöcke in page.tsx geben generische Fehlermeldung ("Fansub-Profil konnte nicht geladen werden."); keine technischen Details öffentlich sichtbar |
| T-73-05-02 | Information Disclosure | Phase-72-Daten ohne Visibility-Filter | mitigate | Filterung (visibility='public' AND review_status='approved') liegt in FansubContributorsSection, FansubGroupMediaBlock, FansubReleaseMediaBlock, FansubMemberMediaBlock — nicht in page.tsx; damit unabhängig von page.tsx-Änderungen |
| T-73-05-03 | Spoofing | ad-hoc-Fetch statt api.ts-Seam | mitigate | getFansubGroupDomainProjection + getMediaOwnershipProjection in api.ts (Lock K, angelegt durch Plan 73-01b); page.tsx importiert ausschließlich aus '@/lib/api' |
| T-73-05-04 | Tampering | FansubProfileTabs native button-Migration | mitigate | Task 2 migriert alle nativen button-Elemente (Zeilen 118–131) auf Button-Primitive aus @/components/ui; Acceptance-Criteria: grep liefert 0 native button-Treffer |
| T-73-05-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neuen Pakete in diesem Plan; slopcheck nicht erforderlich |
</threat_model>

<verification>
- `npm run typecheck` ohne Fehler nach allen Änderungen in page.tsx, page.module.css, FansubProfileTabs.tsx.
- `npm test` — alle Tests grün oder todo, keine fails.
- page.tsx hat unter 150 Zeilen.
- Grep auf page.tsx: `Promise.allSettled` vorhanden + `getFansubGroupDomainProjection` + `getMediaOwnershipProjection`.
- Grep auf FansubProfileTabs.tsx: kein "Gruendung" oder "Aufloesung" mehr; keine nativen `<button>` außerhalb von @/components/ui-Wrappern.
- Alignment-Check: Für jede der 8 SECTION_IDS liefert grep ein exaktes `section id="..."` in den zugehörigen Dateien.
- Human-UAT: Sticky-Nav aktiv, neun Abschnitte in korrekter Reihenfolge, Projekt-Links auf /group/, Empty States stabil.
</verification>

<success_criteria>
- Vollständige kuratierte Erzählseite mit allen neun Abschnitten in Reihenfolge D-02 live.
- page.tsx ≤ 150 Zeilen als reiner Orchestrator.
- Promise.allSettled mit Graceful Degradation für Phase-72-Fetches.
- FansubProfileTabs-Altlasten (Umlaute "Gründung"/"Auflösung" + native button) bereinigt.
- Alle acht SECTION_IDS haben passende section[id]-Attribute in den Section-Komponenten.
- Alle Tests grün oder todo (keine fails).
- Human-UAT bestanden.
</success_criteria>

<output>
Create `.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-05-SUMMARY.md` when done
</output>
