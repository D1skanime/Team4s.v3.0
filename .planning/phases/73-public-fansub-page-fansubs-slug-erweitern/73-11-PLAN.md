---
phase: 73-public-fansub-page-fansubs-slug-erweitern
plan: "11"
type: execute
wave: 3
depends_on: ["10"]
gap_closure: true
files_modified:
  - frontend/src/app/fansubs/[slug]/page.tsx
  - frontend/src/components/fansubs/FansubHighlightsSection.tsx
autonomous: true
requirements: [D-05]
must_haves:
  truths:
    - "Höhepunkte-Projektzähler und Projekte-Sektion stammen aus derselben Quelle (projects.length)"
    - "Badge 'auch Mitglied' erscheint bei Angeldust, weil historische Mitglieder im teamMemberNames-Set enthalten sind"
  artifacts:
    - path: "frontend/src/app/fansubs/[slug]/page.tsx"
      provides: "projects.length an FansubHighlightsSection; teamMemberNames aus members + historical"
    - path: "frontend/src/components/fansubs/FansubHighlightsSection.tsx"
      provides: "animeProjectCount-Prop statt contributions.anime_count als Projektzähler"
  key_links:
    - from: "page.tsx projects"
      to: "FansubHighlightsSection animeProjectCount"
      via: "projects.length prop"
    - from: "page.tsx domainProjection.historical"
      to: "FansubContributorsSection teamMemberNames"
      via: "historical.map(m => m.member_display_name) zusätzlich zu members"
---

<objective>
Behebt zwei verbleibende UAT-Befunde aus Round 2:

UAT-12 (major): Höhepunkte zeigt "2 Anime-Projekte", Projekte-Sektion "Noch keine Projekte". Root Cause: FansubHighlightsSection liest contributions.anime_count (=2, inkl. nicht-öffentlicher). Plan 73-08 wechselte von anime_relations_count auf anime_count — aber auch anime_count zählt nicht-öffentliche Beiträge. Die einzige konsistente Quelle ist projects.length (loadFansubProjects via getAnimeList, nur öffentliche Anime). Fix: page.tsx reicht projects.length als animeProjectCount-Prop an FansubHighlightsSection; FansubHighlightsSection liest diesen Prop statt contributions.anime_count.

UAT-5 (minor): Badge "auch Mitglied" fehlt bei Angeldust. Root Cause: teamMemberNames = domainProjection.members (nur aktive), Angeldust steht in domainProjection.historical. Fix: teamMemberNames = [...members, ...historical] (display names beider Listen).

Output: page.tsx mit korrektem animeProjectCount-Prop und erweitertem teamMemberNames; FansubHighlightsSection mit animeProjectCount-Prop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@frontend/src/app/fansubs/[slug]/page.tsx
@frontend/src/components/fansubs/FansubHighlightsSection.tsx
@frontend/src/types/domain-projection.ts

<interfaces>
<!-- Aktuelle FansubHighlightsSectionProps (FansubHighlightsSection.tsx): -->
interface FansubHighlightsSectionProps {
  group: FansubGroup
  contributions: Pick<PublicGroupContributionsResponse, 'anime_count' | 'member_count'> | null
}

<!-- Aktuelles computeHighlights: -->
{ label: 'Anime-Projekte', value: contributions?.anime_count ?? null },

<!-- Aktuelles page.tsx (nach 73-10): -->
const projects = resolveSettled<AnimeListItem[]>(projectsResult, [])
const domainProjection = ...  // { members, historical, contributors }
const teamMemberNames = domainProjection.members.map((m) => m.member_display_name)

<FansubHighlightsSection group={group} contributions={contributions} />
<FansubContributorsSection contributors={domainProjection.contributors} teamMemberNames={teamMemberNames} />

<!-- DomainProjectionHistoricalRow hat member_display_name: string -->
export interface DomainProjectionHistoricalRow {
  member_display_name: string;
  // ...
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FansubHighlightsSection — animeProjectCount-Prop einführen</name>
  <files>frontend/src/components/fansubs/FansubHighlightsSection.tsx</files>
  <action>
Bearbeite FansubHighlightsSection.tsx.

ÄNDERUNG 1: Props erweitern. Füge optionalen Prop `animeProjectCount?: number` zu FansubHighlightsSectionProps hinzu:

  interface FansubHighlightsSectionProps {
    group: FansubGroup
    contributions: Pick<PublicGroupContributionsResponse, 'anime_count' | 'member_count'> | null
    animeProjectCount?: number
  }

ÄNDERUNG 2: computeHighlights anpassen. Der erste Parameter erhält zusätzlich animeProjectCount. Ersetze die Anime-Projekte-Zeile in computeHighlights:

Vorher:
  { label: 'Anime-Projekte', value: contributions?.anime_count ?? null },

Nachher:
  { label: 'Anime-Projekte', value: animeProjectCount ?? null },

Die Funktion-Signatur wird zu:
  function computeHighlights(
    group: FansubGroup,
    contributions: FansubHighlightsSectionProps['contributions'],
    animeProjectCount?: number,
  ): Highlight[]

ÄNDERUNG 3: computeHighlights-Aufruf in der Render-Funktion aktualisieren:
  const highlights = computeHighlights(group, contributions, animeProjectCount)

Wenn animeProjectCount undefined ist, fällt der Wert auf null zurück und die Kachel wird weggelassen (filter entfernt null). Das ist korrekt als defensiver Fallback.

Wichtig: contributions-Prop NICHT entfernen — es wird noch für member_count gebraucht. Nur die Anime-Projekte-Zeile wechselt die Quelle.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>FansubHighlightsSection.tsx: animeProjectCount-Prop vorhanden; computeHighlights nutzt animeProjectCount für 'Anime-Projekte'-Kachel; tsc fehlerfrei</done>
</task>

<task type="auto">
  <name>Task 2: page.tsx — animeProjectCount und erweitertes teamMemberNames</name>
  <files>frontend/src/app/fansubs/[slug]/page.tsx</files>
  <action>
Bearbeite frontend/src/app/fansubs/[slug]/page.tsx.

ÄNDERUNG 1 (UAT-12): Übergib projects.length als animeProjectCount an FansubHighlightsSection. Die Variable projects ist bereits vorhanden (aus loadFansubProjects, über getAnimeList — nur öffentliche Anime). Ersetze:

  <FansubHighlightsSection group={group} contributions={contributions} />

durch:

  <FansubHighlightsSection group={group} contributions={contributions} animeProjectCount={projects.length} />

So kommen Höhepunkte-Zähler und Projekte-Sektion aus exakt derselben projects-Liste.

ÄNDERUNG 2 (UAT-5): Erweitere teamMemberNames um historische Mitglieder. Ersetze:

  const teamMemberNames = domainProjection.members.map((m) => m.member_display_name)

durch:

  const teamMemberNames = [
    ...domainProjection.members.map((m) => m.member_display_name),
    ...domainProjection.historical.map((m) => m.member_display_name),
  ]

Die Variable teamMemberNames wird bereits an FansubContributorsSection weitergereicht — diese Zeile bleibt unverändert. FansubContributorsSection baut daraus einen case-insensitiven Set und prüft ob ein Contributor in diesem Set ist. Durch das Hinzufügen von historical werden auch ehemalige Mitglieder erkannt.

Keine anderen Änderungen an page.tsx. Die übrigen Imports, der Kollaboration-Early-Return aus 73-10 und alle Sektions-JSX-Blöcke bleiben unverändert.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>page.tsx: FansubHighlightsSection erhält animeProjectCount={projects.length}; teamMemberNames enthält aktive + historische Mitglieder; tsc fehlerfrei</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| keine neuen | Reine Frontend-Logik-Korrekturen; kein neuer API-Aufruf |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-11-01 | Information Disclosure | animeProjectCount | accept | Zähler spiegelt nur öffentlich zugängliche Anime wider (getAnimeList ohne Auth-Filter); kein Datenleck |
| T-73-11-02 | Tampering | teamMemberNames-Erweiterung | accept | String-Vergleich nur für UI-Badge; keine Berechtigungsentscheidungen |
</threat_model>

<verification>
Live-Checks gegen Dev-Server :3000:

1. /fansubs/animeownage aufrufen
2. Höhepunkte-Sektion: "Anime-Projekte"-Kachel NICHT vorhanden (projects.length=0, Kachel weggelassen durch filter) — ODER der Zähler entspricht exakt der Anzahl Einträge in der Projekte-Sektion
3. Kein Widerspruch mehr zwischen Höhepunkte-Zähler und Projekte-Sektion
4. Mitwirkende-Sektion: Angeldust zeigt Badge "auch Mitglied"
5. API-Bestätigung: GET /api/v1/anime?fansub_id=88 → meta.total bestätigt dieselbe Zahl wie die Kachel (oder beide null/0)
</verification>

<success_criteria>
- Höhepunkte-Projektzähler zeigt exakt projects.length (Wert aus getAnimeList, öffentlich)
- Kein Widerspruch zwischen Höhepunkte-Kachel und Projekte-Sektion für /fansubs/animeownage
- Angeldust hat Badge "auch Mitglied" in der Mitwirkenden-Sektion
- tsc --noEmit: 0 Fehler
</success_criteria>

<output>
Erstelle `.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-11-SUMMARY.md` nach Abschluss.
</output>
