---
phase: 73-public-fansub-page-fansubs-slug-erweitern
plan: "06"
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - frontend/src/app/fansubs/[slug]/page.module.css
  - frontend/src/components/fansubs/FansubGroupMediaBlock.tsx
  - frontend/src/components/fansubs/FansubHeroSection.tsx
autonomous: true
requirements: [D-06]
must_haves:
  truths:
    - "Banner im Public Hero wird ohne horizontalen Overflow auf 375px Viewport gerendert (max-width: 100%)"
    - "Banner im Public Hero verwendet object-fit:contain + object-position:center top, keine Verzerrung"
    - "Banner in FansubGroupMediaBlock verwendet object-fit:contain, keine Verzerrung (kein object-fit:fill)"
    - "Alle Banner-URLs gehen durch resolveApiUrl, keine hardcodierten http://127.0.0.1-URLs"
  artifacts:
    - path: "frontend/src/app/fansubs/[slug]/page.module.css"
      provides: "heroBanner + heroBannerImage responsive CSS"
      contains: "max-width"
    - path: "frontend/src/components/fansubs/FansubGroupMediaBlock.tsx"
      provides: "Banner-Rendering mit contain + resolveApiUrl"
      contains: "resolveApiUrl"
  key_links:
    - from: "FansubHeroSection"
      to: "page.module.css .heroBannerImage"
      via: "styles.heroBannerImage className"
    - from: "FansubGroupMediaBlock"
      to: "resolveApiUrl"
      via: "import from @/lib/api"
---

<objective>
Behebt Banner-Darstellungsfehler auf der öffentlichen Fansub-Seite: Mobile-Overflow (UAT-13) und inkonsistente/verzerrte Banner-Darstellung (UAT-17).

Purpose: Das Gruppenbanner wird derzeit in drei verschiedenen Darstellungen gerendert. Ziel ist die Vereinheitlichung der Public-Oberflächen (Hero + Medien-Block) auf das bewährte Admin-Edit-Muster: object-fit:contain + object-position:center top + responsive max-width. Außerdem wird die absolute Loopback-URL in FansubGroupMediaBlock durch resolveApiUrl ersetzt.

Output: page.module.css mit responsivem heroBannerImage; FansubGroupMediaBlock mit contain + resolveApiUrl.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/app/fansubs/[slug]/page.module.css
@frontend/src/components/fansubs/FansubGroupMediaBlock.tsx
@frontend/src/components/fansubs/FansubHeroSection.tsx
@frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css

<interfaces>
<!-- Referenz-Behandlung aus FansubEdit.module.css (das Ziel-Muster): -->

.fansubEditBannerShell {
  position: relative;
  min-height: 180px;
  background: color-mix(in srgb, var(--bg-secondary) 94%, transparent);
  overflow: hidden;
  isolation: isolate;
}

.fansubEditBannerImageElement {
  display: block;
  height: 180px;
  width: auto;
  max-width: min(100%, 980px);  /* ← responsive cap */
  object-fit: contain;
  object-position: center top;
  filter: drop-shadow(0 12px 24px rgba(76, 54, 31, 0.08));
}

/* Mobile override (ab ca. Zeile 2641): */
.fansubEditBannerImageElement {
  width: 100%;
  max-width: 100%;
  height: auto;
}

<!-- FansubHeroSection nutzt Next.js Image mit fill + className={styles.heroBannerImage} -->
<!-- FansubGroupMediaBlock nutzt <Image src={group.banner_url} width={480} height={180} unoptimized /> -->
<!-- resolveApiUrl exportiert aus @/lib/api -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: heroBannerImage in page.module.css responsiv machen</name>
  <files>frontend/src/app/fansubs/[slug]/page.module.css</files>
  <action>
Bearbeite page.module.css im Hero-Banner-Bereich.

Aktueller Stand: .heroBannerImage setzt object-fit:contain und object-position:center, aber keine explizite Breitenbegrenzung. Da Next.js Image fill verwendet, füllt das Bild den Container — der Container .heroBanner hat zwar keine fixe Breite, aber durch fehlende overflow:hidden am Wrapper kann das Bild bei bestimmten Viewports überlaufen.

Änderungen an .heroBanner (die Wrapper-Div):
- Füge overflow: hidden hinzu (verhindert, dass ein zu breites fill-Bild ausbricht)

Änderungen an .heroBannerImage:
- object-position: center top statt object-position: center (entspricht dem Admin-Edit-Muster)

Bestehende sizes-Angabe in FansubHeroSection.tsx ist bereits korrekt ("(max-width: 767px) calc(100vw - 48px), 960px") — NICHT ändern.

Bestehende @media (max-width: 767px)-Overrides für .heroBanner und .heroBannerImage bleiben erhalten; füge dort min-height: 0 für .heroBanner NICHT ein (min-height:112px bleibt).

Keine Änderungen an FansubHeroSection.tsx nötig — die Komponente nutzt bereits fill und sizes korrekt.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>page.module.css enthält overflow:hidden in .heroBanner und object-position:center top in .heroBannerImage; keine TypeScript-Fehler</done>
</task>

<task type="auto">
  <name>Task 2: FansubGroupMediaBlock — object-fit:contain + resolveApiUrl</name>
  <files>frontend/src/components/fansubs/FansubGroupMediaBlock.tsx</files>
  <action>
Bearbeite FansubGroupMediaBlock.tsx.

Problem 1 — object-fit:FILL: Der aktuelle Code rendert das Banner mit `width={480} height={180} unoptimized` ohne object-fit-Überschreibung, was Next.js/Browser auf fill=stretch setzt (Verzerrung).

Problem 2 — absolute Loopback-URL: `group.logo_url` und `group.banner_url` können absolute http://127.0.0.1:8093/...-URLs enthalten (direkt aus der API), die im Browser nicht auflösbar sind. Die Hero-Sektion ruft resolveApiUrl auf — hier fehlt das.

Änderungen:
1. Importiere resolveApiUrl aus @/lib/api am Anfang der Datei.
2. Im Fallback-Render-Zweig (wenn publicGroupMedia.length === 0 und logo_url/banner_url vorhanden):
   - Berechne `const resolvedLogoUrl = resolveApiUrl(group.logo_url || '')` und `const resolvedBannerUrl = resolveApiUrl(group.banner_url || '')`.
   - Verwende resolvedLogoUrl bzw. resolvedBannerUrl als src statt der rohen group.*_url.
3. Für das Banner-Image: Ersetze `width={480} height={180}` durch ein responsives Layout:
   - Umhülle das Banner-Image in ein `<div>` mit `style={{ position: 'relative', width: '100%', aspectRatio: '16/4', overflow: 'hidden' }}`.
   - Nutze `fill` statt width/height auf dem Image.
   - Setze `style={{ objectFit: 'contain', objectPosition: 'center top' }}` auf dem Image.
   - Behalte `unoptimized` bei.
4. Für das Logo-Image: resolveApiUrl verwenden (width/height 160 bleibt unverändert).

Datei muss nach Änderung unter 450 Zeilen bleiben (aktuell 57 Zeilen — kein Problem).
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>FansubGroupMediaBlock importiert resolveApiUrl, rendert Banner mit object-fit:contain in responsivem Container, keine hardcodierten width=480-Attribute mehr; tsc fehlerfrei</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| API → Frontend | banner_url / logo_url können loopback-URLs sein (http://127.0.0.1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-06-01 | Information Disclosure | resolveApiUrl | accept | resolveApiUrl normalisiert Loopback-URLs serverseitig; keine Credentials in Media-URLs |
</threat_model>

<verification>
1. Dev-Server starten: `npm run dev` in frontend/
2. /fansubs/animeownage auf 375px Viewport öffnen (DevTools → iPhone SE)
3. Kein horizontaler Scroll vorhanden (scrollWidth === window.innerWidth)
4. Banner-Bild nicht gestaucht/verzerrt (object-fit:contain erkennbar)
5. /fansubs/animeownage → Medien-Sektion: Banner-Image ebenfalls nicht verzerrt
6. Keine JS-Konsolfehler wegen ungültiger Bild-URLs
</verification>

<success_criteria>
- 375px Viewport: document.body.scrollWidth === document.body.clientWidth (kein horizontaler Overflow)
- Public Hero + Medien-Block: Banner verzerrungsfrei (contain)
- Keine http://127.0.0.1-URLs in img src-Attributen im Medien-Block
- tsc --noEmit: 0 Fehler
</success_criteria>

<output>
Erstelle `.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-06-SUMMARY.md` nach Abschluss.
</output>
