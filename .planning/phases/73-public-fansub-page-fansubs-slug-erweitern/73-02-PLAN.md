---
phase: 73-public-fansub-page-fansubs-slug-erweitern
plan: 02
type: execute
wave: 2
depends_on: ["73-01"]
files_modified:
  - frontend/src/components/fansubs/FansubHeroSection.tsx
  - frontend/src/components/fansubs/FansubStorySection.tsx
  - frontend/src/components/fansubs/FansubHighlightsSection.tsx
  - frontend/src/components/fansubs/FansubProjectsSection.tsx
  - frontend/src/components/fansubs/FansubDeepDiveSection.tsx
autonomous: true
requirements: [B, C]
must_haves:
  truths:
    - "Hero-Abschnitt zeigt Logo, Gruppenname, Slug und Fact-Summary aus buildFansubFactSummary"
    - "Story-Abschnitt zeigt CollapsibleStory (Plaintext, kein HTML) oder Empty State wenn kein Story-Text vorhanden"
    - "Highlights-Abschnitt zeigt 3–4 Kennzahlen aus vorhandenen FansubGroup-Feldern ohne API-Write (D-05)"
    - "Projektkarten verlinken auf /anime/[id]/group/[groupId] (D-13) mit Card variant=interactive"
    - "Deep-Dive-Abschnitt zeigt externe Website-Link (target=_blank rel=noreferrer) und nutzt Card-Primitive"
    - "Alle Abschnitts-IDs und SectionHeader-Titel entsprechen exakt dem UI-SPEC Copywriting-Kontrakt"
    - "Empty States mit Pflicht-Text aus UI-SPEC in allen Abschnitten bei fehlenden Daten (D-15)"
  artifacts:
    - path: "frontend/src/components/fansubs/FansubHeroSection.tsx"
      provides: "Hero-Block: Logo, Gruppenname, Slug, Fact-Summary"
      min_lines: 20
    - path: "frontend/src/components/fansubs/FansubStorySection.tsx"
      provides: "Story-Block: CollapsibleStory oder Empty State"
      min_lines: 20
    - path: "frontend/src/components/fansubs/FansubHighlightsSection.tsx"
      provides: "Highlights-Block: berechnete Kennzahlen aus FansubGroup-Feldern"
      min_lines: 30
    - path: "frontend/src/components/fansubs/FansubProjectsSection.tsx"
      provides: "Projekte-Block: Bucket-Logik, Projektkarten, Deep-Dive-Links"
      min_lines: 60
    - path: "frontend/src/components/fansubs/FansubDeepDiveSection.tsx"
      provides: "Deep-Dive-Block: Website-Link, externe Links mit lucide-react Icon"
      min_lines: 20
  key_links:
    - from: "FansubProjectsSection.tsx"
      to: "/anime/[id]/group/[groupId]"
      via: "Next.js Link href"
      pattern: "/anime/\\$\\{item\\.id\\}/group/\\$\\{groupId\\}"
    - from: "FansubStorySection.tsx"
      to: "CollapsibleStory"
      via: "import { CollapsibleStory } from '@/components/groups/CollapsibleStory'"
      pattern: "CollapsibleStory"
    - from: "FansubHighlightsSection.tsx"
      to: "FansubGroup.anime_relations_count / release_versions_count / members_count"
      via: "Props destructuring"
      pattern: "anime_relations_count|release_versions_count|members_count"
---

<objective>
Plan 02 implementiert die fünf Abschnitts-Komponenten, die ausschließlich auf bereits vorhandenen
Daten (FansubGroup-DTO, AnimeListItem[], PublicGroupContributionsResponse) aufbauen — keine Phase-72-
Abhängigkeit. Die Bucket-Logik aus FansubProfileTabs wird extrahiert, alle Projekt-Links auf den
Deep-Dive-Pfad umgestellt (D-13), und Empty States mit exakten Pflicht-Texten aus dem UI-SPEC
Copywriting-Kontrakt werden implementiert (D-15).

Purpose: Liefert alle datenunabhängigen Abschnitte (kein Phase-72-Wait) als eigenständige,
testbare Komponenten ≤ 450 Zeilen je Datei (CLAUDE.md).
Output: Fünf fertige Server-Komponenten-Dateien, alle durch Typecheck verifiziert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-CONTEXT.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-RESEARCH.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-PATTERNS.md
@.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-UI-SPEC.md

<interfaces>
<!-- FansubGroup-Interface (frontend/src/types/fansub.ts, verifiziert) — Schlüsselfelder: -->
<!-- id: number; name: string; slug: string; website_url?: string | null -->
<!-- logo_url?: string | null; banner_url?: string | null -->
<!-- founded_year?: number | null; dissolved_year?: number | null -->
<!-- status: string; country?: string | null -->
<!-- anime_relations_count: number; release_versions_count: number; members_count: number -->

<!-- AnimeListItem (frontend/src/types/anime.ts) — Schlüsselfelder: -->
<!-- id: number; title: string; status: 'ongoing' | 'done' | 'announced' | ... -->
<!-- cover_url?: string | null; year?: number | null -->

<!-- PublicGroupContributionsResponse (frontend/src/types/contributions.ts): -->
<!-- leader_timeline: PublicFansubLeaderEntry[]; anime_count: number; member_count: number -->

<!-- buildFansubFactSummary (frontend/src/lib/fansub-summary.ts): -->
<!-- export function buildFansubFactSummary(group: FansubGroup): string | null -->

<!-- CollapsibleStory (frontend/src/components/groups/CollapsibleStory.tsx): -->
<!-- 'use client'; interface CollapsibleStoryProps { content: string } -->
<!-- THRESHOLD = 400 Zeichen; zeigt Plaintext, kein HTML, kein dangerouslySetInnerHTML -->

<!-- UI-Primitive aus @/components/ui (verifiziert): -->
<!-- SectionHeader: { eyebrow?: string; title: string; description?: string } -->
<!-- EmptyState: { variant: 'default' | 'withAction' | 'compact'; title: string; description: string } -->
<!-- Card: { variant: 'default' | 'flat' | 'elevated' | 'interactive' | 'section' } -->
<!-- Badge: { variant: 'neutral' | 'success' | 'warning' | 'danger' | 'info' | 'muted' } -->

<!-- Bucket-Logik aus FansubProfileTabs.tsx (Zeilen 46–60, zu extrahieren): -->
<!-- type ProjectBucketKey = 'ongoing' | 'completed' | 'archived' -->
<!-- projectBucketLabel: Record = { ongoing: 'Laufend', completed: 'Abgeschlossen', archived: 'Archiviert' } -->
<!-- resolveProjectBucket(status): 'ongoing' wenn 'ongoing', 'completed' wenn 'done', sonst 'archived' -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hero, Story und Highlights — drei deklarative Server-Komponenten</name>
  <files>
    frontend/src/components/fansubs/FansubHeroSection.tsx,
    frontend/src/components/fansubs/FansubStorySection.tsx,
    frontend/src/components/fansubs/FansubHighlightsSection.tsx
  </files>
  <read_first>
    - frontend/src/types/fansub.ts (vollständiges FansubGroup-Interface, Feldnamen und Typen)
    - frontend/src/lib/fansub-summary.ts (buildFansubFactSummary-Signatur)
    - frontend/src/components/groups/CollapsibleStory.tsx (Props-Interface, 'use client'-Marker)
    - frontend/src/components/ui/SectionHeader.tsx (Props-Interface)
    - frontend/src/components/ui/EmptyState.tsx (Props-Interface, variant-Typen)
    - frontend/src/components/ui/Card.tsx (Props-Interface, variant-Typen)
    - frontend/src/types/contributions.ts (PublicGroupContributionsResponse — anime_count, member_count)
    - .planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-UI-SPEC.md (Copywriting Contract — Pflicht-Texte für Empty States)
    - .planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-PATTERNS.md (FansubHeroSection, FansubStorySection, FansubHighlightsSection Pattern-Abschnitte)
  </read_first>
  <behavior>
    - FansubHeroSection({ group }): section id="hero", Slug als /{group.slug}, h1 Gruppenname, buildFansubFactSummary als Untertitel
    - FansubStorySection({ group }): section id="geschichte", SectionHeader title="Geschichte", CollapsibleStory wenn storyContent truthy; EmptyState variant="compact" title="Noch keine Geschichte hinterlegt" description="Die Gruppe hat bisher noch keine Beschreibung veröffentlicht." wenn null
    - FansubHighlightsSection({ group, contributions }): computeHighlights → max 4 Kennzahlen (Anime-Projekte, Release-Versionen, Mitglieder, Aktive Jahre); wenn alle null/0 → kein Empty State, Abschnitt auslassen; section id="hoehepunkte"
  </behavior>
  <action>
    Erstelle `FansubHeroSection.tsx` — Props: `{ group: FansubGroup }`. Keine Client-Direktive (Server Component).
    Markup: `<section id="hero" className={styles.hero}>`, `<p className={styles.slug}>/{group.slug}</p>`,
    `<h1 className={styles.title}>{group.name}</h1>`,
    `<p className={styles.subtitle}>{buildFansubFactSummary(group) || 'Keine Kurzbeschreibung vorhanden.'}</p>`.
    CSS-Klassen kommen aus bestehender `page.module.css` (`.hero`, `.slug`, `.title`, `.subtitle`) — keine eigene CSS-Datei nötig; die Section wird in page.tsx mit dem bestehenden `styles`-Import gerendert.
    Alternativ: eigene CSS-Datei, sofern das Markup in `page.module.css`-Klassen eingebettet bleibt.
    Import: `buildFansubFactSummary` von `@/lib/fansub-summary`; `FansubGroup` von `@/types/fansub`.

    Erstelle `FansubStorySection.tsx` — Props: `{ group: FansubGroup }`. Keine Client-Direktive.
    Importiert `CollapsibleStory` von `@/components/groups/CollapsibleStory`.
    Importiert `SectionHeader, EmptyState` von `@/components/ui`.
    `storyContent` wird vorläufig aus `buildFansubFactSummary(group)` abgeleitet (kein story-Feld im DTO, A3).
    Render: `<section id="geschichte">`, `<SectionHeader title="Geschichte" />`,
    dann `storyContent ? <CollapsibleStory content={storyContent} /> : <EmptyState variant="compact"
    title="Noch keine Geschichte hinterlegt" description="Die Gruppe hat bisher noch keine Beschreibung veröffentlicht." />`.

    Erstelle `FansubHighlightsSection.tsx` — Props: `{ group: FansubGroup; contributions: { anime_count: number; member_count: number } | null }`.
    Keine Client-Direktive.
    Hilfsfunktion `computeActiveYears(foundedYear, dissolvedYear)`: wenn kein `foundedYear` → null;
    sonst `(dissolvedYear ?? new Date().getFullYear()) - foundedYear`.
    Hilfsfunktion `computeHighlights(group, contributions)`: Array aus max. 4 Highlight-Objekten
    `{ label: string; value: number | string | null }`:
    `{ label: 'Anime-Projekte', value: group.anime_relations_count }`,
    `{ label: 'Release-Versionen', value: group.release_versions_count }`,
    `{ label: 'Mitglieder', value: group.members_count }`,
    `{ label: 'Aktive Jahre', value: computeActiveYears(group.founded_year, group.dissolved_year) }`.
    Filter: `.filter(h => h.value !== null && h.value !== 0)`.
    Wenn highlights.length === 0 → `return null` (kein DOM-Output, D-15 Sonderregel laut UI-SPEC).
    Sonst render: `<section id="hoehepunkte">`, `<SectionHeader title="Höhepunkte" />`,
    CSS-Grid-Raster `repeat(auto-fit, minmax(180px, 1fr))` via inline style oder CSS-Modul,
    jede Kennzahl als `<Card variant="section">` mit `value` (groß) + `label` (klein).
    Importiert `Card, SectionHeader` von `@/components/ui`.
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck 2>&1 | grep -E "FansubHero|FansubStory|FansubHighlights" || echo "typecheck-ok"</automated>
  </verify>
  <acceptance_criteria>
    - Quell-Assertion: `FansubHeroSection.tsx` hat section id="hero"; verwendet `buildFansubFactSummary`; kein `dangerouslySetInnerHTML`.
    - Quell-Assertion: `FansubStorySection.tsx` hat section id="geschichte"; importiert `CollapsibleStory`; Empty State title="Noch keine Geschichte hinterlegt" (exakter Wortlaut, korrekte Umlaute).
    - Quell-Assertion: `FansubHighlightsSection.tsx` hat section id="hoehepunkte" (Ankerpunkt ohne Umlaut, Sprungmarke korrekt per UI-SPEC); SECTION_LABELS enthält "Höhepunkte" als Anzeige-Label; `computeHighlights` gibt leeres Array zurück wenn alle Werte null/0; Komponente gibt `null` zurück wenn highlights.length === 0.
    - Quell-Assertion: Keine der drei Dateien enthält native `<button>`, `<input>`, `<select>` oder `<textarea>`.
    - Behavior-Assertion: `npm run typecheck` zeigt keine Fehler für FansubHero, FansubStory, FansubHighlights.
  </acceptance_criteria>
  <done>Drei Server-Komponenten implementiert, compilierbar, korrekte Pflicht-Texte, kein HTML-Inject, kein nativer primitiver Ersatz.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Projekte und Deep-Dive — Bucket-Logik, D-13-Link, externe Verlinkung</name>
  <files>
    frontend/src/components/fansubs/FansubProjectsSection.tsx,
    frontend/src/components/fansubs/FansubDeepDiveSection.tsx
  </files>
  <read_first>
    - frontend/src/components/fansubs/FansubProfileTabs.tsx (Zeilen 46–106 und 180–203 — Bucket-Logik, Projekt-Rendering, bestehender Link href, Altlast „Gruendung"/„Aufloesung" identifizieren)
    - frontend/src/types/anime.ts (AnimeListItem-Interface — id, title, status, cover_url, year)
    - frontend/src/types/fansub.ts (FansubGroup — id, website_url, slug)
    - frontend/src/components/ui/Card.tsx (variant interactive — hover-Effekt)
    - frontend/src/components/ui/SectionHeader.tsx (Props)
    - frontend/src/components/ui/EmptyState.tsx (Props, variant compact)
    - .planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-UI-SPEC.md (Copywriting Contract — Projektkarten-Empty State, Deep-Dive-Labels)
    - .planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-PATTERNS.md (FansubProjectsSection, FansubDeepDiveSection Pattern-Abschnitte)
  </read_first>
  <behavior>
    - FansubProjectsSection({ projects, groupId }): Bucket-Gruppen ongoing/completed/archived; leere Buckets ausblenden; Karten-Link auf /anime/${id}/group/${groupId} (D-13); Empty State wenn projects=[]
    - FansubDeepDiveSection({ group }): zeigt website_url wenn vorhanden als externen Link (target=_blank rel=noreferrer); zeigt SectionHeader title="Mehr"; keine Navigation-Links auf noch nicht existierende Routes
  </behavior>
  <action>
    Erstelle `FansubProjectsSection.tsx` — Props: `{ projects: AnimeListItem[]; groupId: number }`.
    Keine Client-Direktive (Server Component).

    Extrahiere die Bucket-Logik aus `FansubProfileTabs.tsx` direkt in diese Datei:
    `type ProjectBucketKey = 'ongoing' | 'completed' | 'archived'`
    `const projectBucketOrder: ProjectBucketKey[] = ['ongoing', 'completed', 'archived']`
    `const projectBucketLabel: Record<ProjectBucketKey, string> = { ongoing: 'Laufend', completed: 'Abgeschlossen', archived: 'Archiviert' }`
    `function resolveProjectBucket(status: AnimeListItem['status']): ProjectBucketKey { ... }` (wie in Vorlage).

    Projekte in Buckets gruppieren via `reduce`. Nur Buckets mit Inhalt rendern.
    Projektkartenlink (D-13): `<Link href={'/anime/' + item.id + '/group/' + groupId}>` — NICHT `/anime/${item.id}`.
    Jede Karte als `<Card variant="interactive">` (hover translateY(-2px), bestehender ui.module.css-Effekt).
    Empty State: `<EmptyState variant="compact" title="Noch keine Projekte" description="Diese Gruppe hat bisher keine öffentlich zugänglichen Anime-Projekte." />` wenn `projects.length === 0`.
    section id="projekte", `<SectionHeader title="Projekte" />`.
    Imports: `Link from 'next/link'`, `AnimeListItem from '@/types/anime'`, `Card, SectionHeader, EmptyState from '@/components/ui'`.

    Erstelle `FansubDeepDiveSection.tsx` — Props: `{ group: FansubGroup }`.
    Keine Client-Direktive.
    `<section id="deep-dive">`, `<SectionHeader title="Mehr" />`.
    Wenn `group.website_url`: `<Card variant="flat">` mit
    `<a href={group.website_url} target="_blank" rel="noreferrer" className={styles.externalLink}>
    <ExternalLink size={16} aria-hidden="true" /> Webseite besuchen</a>` — `ExternalLink` aus `lucide-react`.
    Wenn kein website_url: Abschnitt trotzdem rendern (D-15 — Anker stabil), aber kein Empty State text nötig,
    stattdessen compact placeholder „Keine weiteren Links verfügbar." als einfacher `<p>`.
    Imports: `ExternalLink from 'lucide-react'`, `Card, SectionHeader from '@/components/ui'`,
    `FansubGroup from '@/types/fansub'`.
    Kein nativer `<button>` — der `<a>`-Tag ist für externe Links korrekt (kein Button-Primitive nötig).

    Beim Lesen von `FansubProfileTabs.tsx`: Falls Altlasten „Gruendung" (Zeile 148) oder „Aufloesung" (Zeile 150) gefunden werden — diese Zeilen NICHT in `FansubProjectsSection.tsx` übernehmen; die FansubProfileTabs-Altlasten werden in Plan 05 korrigiert.
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck 2>&1 | grep -E "FansubProjects|FansubDeepDive" || echo "typecheck-ok"</automated>
  </verify>
  <acceptance_criteria>
    - Quell-Assertion: `FansubProjectsSection.tsx` hat section id="projekte"; Link-Attribut enthält `/group/` — Pattern: `href={.*\/group\/.*}` or `href={\`/anime/${...}/group/${...}\`}`.
    - Quell-Assertion: `FansubProjectsSection.tsx` importiert `AnimeListItem` und `Card, SectionHeader, EmptyState` aus `@/components/ui`; kein Fallback auf `/anime/${item.id}` ohne `/group/`-Suffix.
    - Quell-Assertion: Empty State title ist exakt `"Noch keine Projekte"` (korrekte Schreibweise per UI-SPEC).
    - Quell-Assertion: `FansubDeepDiveSection.tsx` hat `target="_blank"` und `rel="noreferrer"` am Website-Link; `ExternalLink` aus `lucide-react` importiert.
    - Quell-Assertion: Keine der beiden Dateien enthält `href={/anime/${item.id}}` ohne `/group/`-Suffix.
    - Behavior-Assertion: `npm run typecheck` zeigt keine Fehler für FansubProjects oder FansubDeepDive.
  </acceptance_criteria>
  <done>FansubProjectsSection mit korrektem D-13-Link und Bucket-Logik implementiert; FansubDeepDiveSection mit externem Link-Pattern; beide Dateien compilierbar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Component → API-Daten | Alle Props kommen von page.tsx-Fetch; keine direkten Browser-API-Aufrufe |
| Deep-Dive externe Links | group.website_url kommt aus Backend-DTO; target=_blank öffnet neue Browser-Tab |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-02-01 | Information Disclosure | FansubHighlightsSection — zeigt Kennzahlen | accept | Alle Felder (anime_relations_count etc.) sind öffentlich über getFansubBySlug exponiert; keine internen Daten |
| T-73-02-02 | Tampering | FansubDeepDiveSection — externes Link target=_blank | mitigate | `rel="noreferrer"` verhindert window.opener-Leakage; website_url kommt als DB-Wert, kein XSS durch React-Escaping |
| T-73-02-03 | Information Disclosure | CollapsibleStory content — kein HTML-Inject | mitigate | CollapsibleStory zeigt Plaintext; kein `dangerouslySetInnerHTML`; React escaped JSX-Strings automatisch |
| T-73-02-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neuen Pakete; `lucide-react` bereits in package.json; slopcheck nicht erforderlich |
</threat_model>

<verification>
- `npm run typecheck` produziert keine Fehler für FansubHeroSection, FansubStorySection, FansubHighlightsSection, FansubProjectsSection, FansubDeepDiveSection.
- Projektkarten-Links enthalten `/group/` im href-Attribut.
- Alle Empty-State-Texte entsprechen exakt dem UI-SPEC Copywriting-Kontrakt.
- Keine der fünf Dateien enthält native `<button>`, `<select>`, `<input>`, `<textarea>`.
</verification>

<success_criteria>
- Fünf Server-Komponenten-Dateien implementiert und compilierbar.
- D-13 (Deep-Dive-Link /anime/[id]/group/[groupId]) korrekt in FansubProjectsSection.
- D-05 (keine Writes, Highlights aus vorhandenen Daten) in FansubHighlightsSection.
- D-15 (Empty States mit Pflicht-Text) in allen fünf Komponenten.
- Korrekte deutsche Umlaute in allen user-facing Strings.
</success_criteria>

<output>
Create `.planning/phases/73-public-fansub-page-fansubs-slug-erweitern/73-02-SUMMARY.md` when done
</output>
