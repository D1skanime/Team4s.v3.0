---
phase: 97-revoke-rollen-lifecycle-uebergang
plan: 04
type: execute
wave: 4
depends_on:
  - 97-02
files_modified:
  - backend/internal/repository/member_claims_repository.go
autonomous: true
requirements:
  - D-05
  - D-06
  - D-07
  - D-08

must_haves:
  truths:
    - "VerifyClaim ruft nach erfolgreichem Commit ResolvePendingRolesToActive auf"
    - "Historische Rollen ohne ended_date werden als aktive fansub_group_member_roles übernommen"
    - "fansub_lead und founder sind aus der automatischen Aktivierung ausgeschlossen"
    - "Nur Rollencodes die IsGroupHistoryWhitelistRole bestehen werden aktiviert"
    - "ResolvePendingRolesToActive verwendet ON CONFLICT DO NOTHING (Idempotenz)"
    - "tenure_started_on wird beim Aktivieren der historischen Rolle auf time.Now gesetzt"
    - "canForContext liest weiterhin nur fansub_group_member_roles (keine Änderung — D-07)"
    - "go build ./... und go test ./internal/repository/... sind fehlerfrei"
  artifacts:
    - path: backend/internal/repository/member_claims_repository.go
      provides: "ResolvePendingRolesToActive + erweitertes VerifyClaim"
      contains: "ResolvePendingRolesToActive"
  key_links:
    - from: "backend/internal/repository/member_claims_repository.go"
      to: "hist_group_member_roles"
      via: "SELECT ended_date IS NULL WHERE role_code NOT IN ('fansub_lead','founder')"
      pattern: "ended_date IS NULL"
    - from: "backend/internal/repository/member_claims_repository.go"
      to: "fansub_group_member_roles"
      via: "INSERT ON CONFLICT DO NOTHING mit tenure_started_on"
      pattern: "tenure_started_on"
---

<objective>
Claim-Aktivierungslogik implementieren: Nach einem bestätigten Identitäts-Claim werden historische Rollen ohne Enddatum automatisch als aktive App-Rollen übernommen. Dies ist die Kern-Businesslogik von D-05 — der Übergang historisch→aktiv beim Claim-Flow.

Zweck: Ohne diese Logik müsste der Admin nach jedem Claim manuell alle aktiven Rollen neu zuweisen. D-06 (aktive Zuweisung) ist über den bestehenden SetRole-Pfad bereits erreichbar; diese Phase fügt nur die automatische Auflösung beim Claim hinzu. D-07 (Capability nur aktiv) braucht keine Code-Änderung — nur Verifikation.

Ausgabe: member_claims_repository.go mit neuer ResolvePendingRolesToActive-Methode + erweitertem VerifyClaim-Aufruf.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-CONTEXT.md
@.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-RESEARCH.md
@.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-PATTERNS.md
@.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-02-SUMMARY.md
</context>

<interfaces>
<!-- Schlüssel-Code aus Codebase gelesen: -->

Bestehende VerifyClaim-Signatur (member_claims_repository.go Z. 175):
```go
func (r *MemberClaimsRepository) VerifyClaim(ctx context.Context, fansubGroupID int64, claimID int64, verifiedByAppUserID int64) error
```

VerifyClaim-Transaktion endet bei Z. 234 mit:
```go
if err := tx.Commit(ctx); err != nil {
    return fmt.Errorf("verify member claim: commit: %w", err)
}
return nil
```

Bestehende IsGroupHistoryWhitelistRole (aus RESEARCH.md, VERIFIED: Codebase):
```go
repository.IsGroupHistoryWhitelistRole(code string) bool
// In hist_group_member_roles_repository.go — prüft ob Rollencode für hist-Kontext gültig ist
```

SetRole-Muster aus fansub_group_app_members_repository.go (Plan 01 angepasst):
```go
// Enable-INSERT (nach Plan 01 Update):
INSERT INTO fansub_group_member_roles
    (fansub_group_member_id, role, created_by_app_user_id, tenure_started_on, created_at)
VALUES ($1, $2, $3, $4, NOW())
ON CONFLICT (fansub_group_member_id, role) DO NOTHING
```

D-10 Auto-Archivierungs-Lookup (für memberID → fansub_group_member_id):
```go
// Lookup: fansub_group_members.id für (app_user_id, fansub_group_id)
SELECT id FROM fansub_group_members
WHERE app_user_id = $1 AND fansub_group_id = $2
```

ClaimMutationError-Muster (Z. 15–34):
```go
type ClaimMutationError struct { Code string; Message string; HTTPStatus int }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ResolvePendingRolesToActive implementieren + VerifyClaim erweitern</name>
  <files>
    backend/internal/repository/member_claims_repository.go
  </files>

  <read_first>
    - backend/internal/repository/member_claims_repository.go — vollständige Datei (421 Zeilen); VerifyClaim (Z. 175–238), Transaktion-Muster, ClaimMutationError, Imports
    - backend/internal/repository/hist_group_member_roles_repository.go — IsGroupHistoryWhitelistRole-Funktion (suchen per Grep); Whitelist-Konstante/Funktion-Signatur
    - backend/internal/repository/fansub_group_app_members_repository.go — Z. 399–406 (SetRole Enable INSERT nach Plan-01-Update mit tenure_started_on)
    - .planning/phases/97-revoke-rollen-lifecycle-uebergang/97-PATTERNS.md — member_claims_repository.go-Sektion (ResolvePendingRolesToActive Pseudocode, Fehlerbehandlungs-Muster)
  </read_first>

  <behavior>
    Neue Methode ResolvePendingRolesToActive(ctx, memberID, fansubGroupID, actorAppUserID int64) error:
    - Liest hist_group_member_roles WHERE ended_date IS NULL für diesen member+gruppe:
      SELECT r.role_code FROM hist_group_member_roles r
      JOIN hist_fansub_group_members hfgm ON hfgm.id = r.hist_fansub_group_member_id
      WHERE hfgm.member_id = $1
        AND hfgm.fansub_group_id = $2
        AND r.ended_date IS NULL
        AND r.role_code <> ALL(ARRAY['fansub_lead', 'founder'])
    - Für jeden gefundenen role_code: zusätzlich IsGroupHistoryWhitelistRole prüfen (defensiv).
    - Lookup fansub_group_members.id für den App-User der diesen Claim hat:
      SELECT fgm.id FROM fansub_group_members fgm
      JOIN member_claims mc ON mc.app_user_id = fgm.app_user_id
      WHERE mc.member_id = $1 AND fgm.fansub_group_id = $2 AND mc.claim_status = 'verified'
      LIMIT 1
    - Für jeden validen role_code: INSERT INTO fansub_group_member_roles (fansub_group_member_id, role, created_by_app_user_id, tenure_started_on, created_at) VALUES ($1, $2, $3, $4, NOW()) ON CONFLICT DO NOTHING.
    - tenure_started_on = time.Now().UTC().Truncate(24*time.Hour).
    - Fail-open: Fehler bei Lookup oder einzelnem INSERT werden geloggt, aber kein Fehler-Return — das Claim wurde bereits verifiziert.
    - Wenn kein fansub_group_member_id gefunden: Funktion returniert nil (kein aktiver App-User für diesen Member in dieser Gruppe → normal für rein historische Claims).

    VerifyClaim-Erweiterung:
    - Nach tx.Commit() (Z. 234) NICHT innerhalb der Transaktion, sondern als Post-Commit-Schritt:
      _ = r.ResolvePendingRolesToActive(ctx, memberID, fansubGroupID, verifiedByAppUserID)
    - Fehler aus ResolvePendingRolesToActive werden nur geloggt, nicht zurückgegeben (Claim war bereits erfolgreich committed).

    D-07-Verifikation (keine Code-Änderung nötig): canForContext in permissions.go liest nur fansub_group_member_roles. ResolvePendingRolesToActive schreibt dorthin → aktive Rollen werden korrekt in Capabilities aufgelöst. Kein Änderungsbedarf.

    Datei-Limit: member_claims_repository.go ist aktuell 421 Zeilen. ResolvePendingRolesToActive fügt ca. 40–50 Zeilen hinzu → ~465 Zeilen. Über 450-Limit → Methode in eigene Datei member_claims_role_activation_repository.go auslagern mit gleichem Receiver-Typ.
  </behavior>

  <action>
    Wenn member_claims_repository.go + neue Methode ≤ 450 Zeilen bleibt: Methode in derselben Datei anlegen.
    Wenn > 450 Zeilen: neue Datei backend/internal/repository/member_claims_role_activation_repository.go anlegen mit package repository-Header, gleichen Imports (context, time, log), Methode ResolvePendingRolesToActive als Methode auf *MemberClaimsRepository.

    Methoden-Rumpf:
    1. SELECT role_code FROM hist_group_member_roles JOIN hist_fansub_group_members mit ended_date IS NULL AND role_code <> ALL(ARRAY['fansub_lead','founder']).
    2. Für jeden role_code: IsGroupHistoryWhitelistRole-Check.
    3. SELECT fgm.id aus fansub_group_members JOIN member_claims (claim_status='verified').
    4. Falls kein fgm.id → return nil (kein Fehler).
    5. Für jeden validen role_code: INSERT mit ON CONFLICT DO NOTHING.
    6. Fail-open: log.Printf bei Fehlern, return nil.

    VerifyClaim-Erweiterung: Nach `return nil` am Ende von VerifyClaim, VOR dem letzten return nil: `_ = r.ResolvePendingRolesToActive(ctx, memberID, fansubGroupID, verifiedByAppUserID)` einfügen. Fehler nur loggen.

    D-06-Hinweis: Kein neuer Code für aktive Rollen zuweisen nötig — SetRole in fansub_group_app_members_repository.go ist der bestehende Pfad. ResolvePendingRolesToActive nutzt den gleichen Schreibpfad (gleiche Tabelle, gleiche INSERT-Logik).
  </action>

  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go build ./... 2>&amp;1 && go test ./internal/repository/... 2>&amp;1</automated>
  </verify>

  <done>
    go build ./... fehlerfrei. go test ./internal/repository/... fehlerfrei. member_claims_repository.go ≤ 450 Zeilen (oder Auslagerung in member_claims_role_activation_repository.go ≤ 450 Zeilen). ResolvePendingRolesToActive ist vorhanden. fansub_lead und founder sind in SQL-Excludes. VerifyClaim ruft ResolvePendingRolesToActive nach Commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Admin-Action (VerifyClaim) → DB | Claim-Bestätigung schreibt aktive Rollen; muss Gruppen-Scope respektieren |
| ResolvePendingRolesToActive | Liest hist-Tabelle, schreibt in aktive Rollen-Tabelle; keine Angreifer-Eingabe direkt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-04-01 | Elevation of Privilege | Claim-Aktivierung schreibt unkontrollierte Rollen | mitigate | SQL-Whitelist: AND role_code &lt;&gt; ALL(ARRAY['fansub_lead','founder']); IsGroupHistoryWhitelistRole als zweiter Guard |
| T-97-04-02 | Tampering | Cross-Group-Rollenzuweisung beim Claim | mitigate | JOIN prüft hfgm.fansub_group_id = $2 (fansubGroupID aus VerifyClaim-Aufruf) |
| T-97-04-03 | Elevation of Privilege | fansub_lead ohne Last-Active-Lead-Guard aktiviert | mitigate | fansub_lead explizit aus automatischem Flow ausgeschlossen (SQL AND role_code &lt;&gt; ALL(...)). Admin muss fansub_lead manuell über SetRole setzen. |
| T-97-04-04 | Tampering | Doppelte Rollen-Rows bei wiederholtem Claim-Verify | mitigate | ON CONFLICT DO NOTHING auf fansub_group_member_roles (UNIQUE auf fansub_group_member_id, role) |
| T-97-04-05 | Denial of Service | Fehler in ResolvePendingRolesToActive blockiert Claim-Verifikation | mitigate | Fail-open: Fehler werden nur geloggt, Claim-Commit ist bereits erfolgt; keine Fehler-Propagation |
| T-97-04-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Packages; nur Go-Änderung |
</threat_model>

<verification>
Nach Task 1:
1. `cd backend && go build ./...` — 0 Fehler.
2. `cd backend && go test ./internal/repository/...` — alle Tests grün.
3. `grep -n "ResolvePendingRolesToActive" backend/internal/repository/member_claims_repository.go backend/internal/repository/member_claims_role_activation_repository.go 2>/dev/null` — Methode gefunden.
4. `grep -n "fansub_lead\|founder" backend/internal/repository/member_claims*` — Ausschluss vorhanden.
5. `grep -n "ON CONFLICT DO NOTHING" backend/internal/repository/member_claims*` — vorhanden.
6. `grep -n "ResolvePendingRolesToActive" backend/internal/repository/member_claims_repository.go` — VerifyClaim ruft die Methode nach Commit auf.
7. D-07: `grep -rn "hist_group_member_roles" backend/internal/permissions/` — 0 Treffer (canForContext liest hist nicht).
8. Datei-Limit: `wc -l backend/internal/repository/member_claims_repository.go` ≤ 450.
</verification>

<success_criteria>
- ResolvePendingRolesToActive existiert als Methode auf MemberClaimsRepository.
- VerifyClaim ruft ResolvePendingRolesToActive nach Commit auf.
- fansub_lead und founder sind aus dem automatischen Aktivierungsflow ausgeschlossen.
- Nur Rollencodes, die IsGroupHistoryWhitelistRole bestehen, werden aktiviert.
- INSERT verwendet ON CONFLICT DO NOTHING und setzt tenure_started_on.
- Fail-open: Fehler in ResolvePendingRolesToActive blockieren keinen Claim.
- Alle Dateien ≤ 450 Zeilen. go build ./... und go test ./internal/repository/... grün.
</success_criteria>

<output>
Erstelle `.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-04-SUMMARY.md` nach Abschluss.
</output>
