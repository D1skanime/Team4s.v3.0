---
phase: 97-revoke-rollen-lifecycle-uebergang
plan: 01
type: execute
wave: 1
depends_on: [97-00]
files_modified:
  - database/migrations/0114_hist_roles_date_migration.up.sql
  - database/migrations/0114_hist_roles_date_migration.down.sql
  - database/migrations/0115_active_roles_tenure_date.up.sql
  - database/migrations/0115_active_roles_tenure_date.down.sql
  - backend/internal/repository/fansub_group_app_members_repository.go
autonomous: true
requirements:
  - D-02
  - D-04

must_haves:
  truths:
    - "hist_group_member_roles enthält started_date/ended_date als DATE-Spalten (keine started_year/ended_year INT-Spalten mehr)"
    - "hist_fansub_group_members enthält joined_date/left_date als DATE-Spalten (keine joined_year/left_year mehr)"
    - "fansub_group_member_roles enthält tenure_started_on DATE NULL"
    - "Bestandsdaten: jede Zeile mit started_year=2008 hat started_date=2008-01-01"
    - "D-10-Auto-Archivierung in SetRole(false) schreibt started_date/ended_date statt started_year/ended_year"
    - "go build ./... im backend ist fehlerfrei nach diesen Änderungen"
  artifacts:
    - path: database/migrations/0114_hist_roles_date_migration.up.sql
      provides: "INT→DATE-Migration für hist_group_member_roles + hist_fansub_group_members"
      contains: "MAKE_DATE"
    - path: database/migrations/0114_hist_roles_date_migration.down.sql
      provides: "Rück-Migration DATE→INT"
      contains: "EXTRACT(YEAR FROM"
    - path: database/migrations/0115_active_roles_tenure_date.up.sql
      provides: "tenure_started_on DATE NULL in fansub_group_member_roles"
      contains: "tenure_started_on"
    - path: backend/internal/repository/fansub_group_app_members_repository.go
      provides: "D-10-Auto-Archivierung mit DATE-Spalten"
      contains: "started_date"
  key_links:
    - from: "database/migrations/0114_hist_roles_date_migration.up.sql"
      to: "hist_group_member_roles.started_date"
      via: "ALTER TABLE ADD COLUMN + UPDATE MAKE_DATE"
      pattern: "MAKE_DATE.*started_year"
    - from: "backend/internal/repository/fansub_group_app_members_repository.go"
      to: "hist_group_member_roles"
      via: "INSERT with started_date/ended_date"
      pattern: "started_date.*ended_date"
---

<objective>
Schritt 1 des Rollen-Lifecycles: DB-Schema-Migration von Jahres-Integers auf tagesgenaue DATE-Spalten in beiden historischen Tabellen, plus Tenure-Startdatum in aktiven Rollen. Atomar mit der D-10-Auto-Archivierungs-Fix im Repository.

Zweck: Ohne diese Wave schlägt der Backend-Code nach Migration zur Laufzeit fehl (Pitfall D-10 in RESEARCH.md: started_year/ended_year existieren nach Migration nicht mehr). Alle nachfolgenden Waves bauen auf den neuen DATE-Spalten auf.

Ausgabe: 4 SQL-Migrations-Dateien + 1 Go-Repository-Fix. Kein Frontend-Change in dieser Wave.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-CONTEXT.md
@.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-RESEARCH.md
@.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrationen 0114 (INT→DATE hist-Tabellen) und 0115 (tenure_started_on) anlegen</name>
  <files>
    database/migrations/0114_hist_roles_date_migration.up.sql,
    database/migrations/0114_hist_roles_date_migration.down.sql,
    database/migrations/0115_active_roles_tenure_date.up.sql,
    database/migrations/0115_active_roles_tenure_date.down.sql
  </files>

  <read_first>
    - database/migrations/0082_historical_fansub_group_members.up.sql — exakter Constraint-Name chk_hist_fansub_group_members_years, Spalten joined_year/left_year
    - database/migrations/0083_hist_group_member_roles.up.sql — exakter Constraint-Name chk_hist_group_member_roles_years, Spalten started_year/ended_year
    - database/migrations/0112_role_model_cleanup.up.sql — kritisches Reihenfolge-Muster (DROP CONSTRAINT vor DROP COLUMN)
    - database/migrations/0113_fansub_lead_label_leader.up.sql — letzte Migration, bestätigt nächste freie Nummer 0114
    - .planning/phases/97-revoke-rollen-lifecycle-uebergang/97-PATTERNS.md — vollständiges Migrations-Template (Pattern 1 + Pattern 2)
  </read_first>

  <behavior>
    - Migration 0114 up: fügt started_date/ended_date DATE NULL zu hist_group_member_roles hinzu; mappt Bestandsdaten per MAKE_DATE(started_year, 1, 1) WHERE started_year IS NOT NULL; droppt Constraint chk_hist_group_member_roles_years; legt chk_hist_group_member_roles_dates CHECK (ended_date IS NULL OR started_date IS NULL OR ended_date >= started_date) an; droppt started_year/ended_year. Analog für hist_fansub_group_members (joined_date/left_date, Constraint chk_hist_fansub_group_members_years → chk_hist_fansub_group_members_dates). Ganzes File in einer BEGIN/COMMIT-Transaktion.
    - Migration 0114 down: stellt INT-Spalten zurück per EXTRACT(YEAR FROM date_col)::INT; stellt alte CHECK-Constraints wieder her; droppt DATE-Spalten. Beide Tabellen im selben File.
    - Migration 0115 up: ALTER TABLE fansub_group_member_roles ADD COLUMN IF NOT EXISTS tenure_started_on DATE NULL. Kein BEGIN/COMMIT nötig (DDL-Einzeloperation, aber konsistenzhalber wrap).
    - Migration 0115 down: ALTER TABLE fansub_group_member_roles DROP COLUMN IF EXISTS tenure_started_on.
  </behavior>

  <action>
    Datei 0114_hist_roles_date_migration.up.sql:
    Volle BEGIN/COMMIT-Transaktion. Reihenfolge exakt nach 97-PATTERNS.md Pattern 1 (aus RESEARCH.md übernommen, VERIFIED gegen 0082/0083):
    1. hist_group_member_roles: ADD COLUMN started_date DATE NULL, ended_date DATE NULL.
    2. UPDATE hist_group_member_roles SET started_date = MAKE_DATE(started_year, 1, 1) WHERE started_year IS NOT NULL.
    3. UPDATE hist_group_member_roles SET ended_date = MAKE_DATE(ended_year, 1, 1) WHERE ended_year IS NOT NULL.
    4. DROP CONSTRAINT IF EXISTS chk_hist_group_member_roles_years; ADD CONSTRAINT chk_hist_group_member_roles_dates CHECK (ended_date IS NULL OR started_date IS NULL OR ended_date >= started_date).
    5. DROP COLUMN IF EXISTS started_year, ended_year.
    Analog Teil 2 für hist_fansub_group_members mit joined_date/left_date und Constraint chk_hist_fansub_group_members_dates.
    Kommentar-Header "-- Kritische Reihenfolge: Constraints droppen VOR Spalten-Drop." analog zu 0112.

    Datei 0114 down: EXTRACT(YEAR FROM started_date)::INT als started_year zurück; analoges Vorgehen. Beide Tabellen, alte Constraints wieder herstellen.

    Datei 0115_active_roles_tenure_date.up.sql: ALTER TABLE fansub_group_member_roles ADD COLUMN IF NOT EXISTS tenure_started_on DATE NULL; (als BEGIN/COMMIT-Block).
    Datei 0115 down: ALTER TABLE fansub_group_member_roles DROP COLUMN IF EXISTS tenure_started_on;
  </action>

  <verify>
    <automated>
      cd C:/Users/admin/Documents/Team4s && docker compose exec -T team4sv30-postgres psql -U postgres -d team4s -c "\d hist_group_member_roles" 2>/dev/null | grep -E "started_date|ended_date" || echo "MIGRATION_NOT_RUN_YET_OK"
    </automated>
  </verify>

  <done>
    Alle 4 SQL-Dateien existieren. 0114 up enthält MAKE_DATE, chk_hist_group_member_roles_dates, chk_hist_fansub_group_members_dates. 0115 up enthält tenure_started_on. Down-Dateien enthalten EXTRACT(YEAR FROM. Kein SLOP, kein Leerzeile-only-File.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-10-Auto-Archivierung in fansub_group_app_members_repository.go auf DATE umstellen</name>
  <files>
    backend/internal/repository/fansub_group_app_members_repository.go
  </files>

  <read_first>
    - backend/internal/repository/fansub_group_app_members_repository.go — Zeilen 390–450 (SetRole mit D-10 INSERT, Ist-Stand: started_year/ended_year INT)
    - .planning/phases/97-revoke-rollen-lifecycle-uebergang/97-PATTERNS.md — Pattern fansub_group_app_members_repository.go (Ziel-Muster mit started_date/ended_date DATE + tenure_started_on)
  </read_first>

  <behavior>
    - Im D-10-Auto-Archivierungs-INSERT (Zeile ~443): started_year/ended_year durch started_date/ended_date ersetzen. Werte: startedDate = roleCreatedAt.Truncate(24*time.Hour), endedDate = time.Now().UTC().Truncate(24*time.Hour). SQL-Spalten: started_date, ended_date (kein started_year, kein ended_year).
    - Im SetRole Enable-INSERT (Zeile ~401): tenure_started_on = time.Now().UTC().Truncate(24*time.Hour) als $4-Parameter hinzufügen. SQL INSERT-Liste erweitern: (fansub_group_member_id, role, created_by_app_user_id, tenure_started_on, created_at).
    - Datei bleibt unter 810 Zeilen (aktuell 810 — Änderungen sind Ersetzungen, netto 0).
  </behavior>

  <action>
    In fansub_group_app_members_repository.go zwei Stellen ändern:

    Stelle 1 — SetRole Enable (Zeile ~401):
    INSERT INTO fansub_group_member_roles aktuell: (fansub_group_member_id, role, created_by_app_user_id, created_at) mit 3 Parametern.
    Ziel: (fansub_group_member_id, role, created_by_app_user_id, tenure_started_on, created_at) mit $4 = time.Now().UTC().Truncate(24*time.Hour). ON CONFLICT bleibt DO NOTHING.

    Stelle 2 — D-10 Auto-Archivierung (Zeile ~438–443):
    Vor dem INSERT: startedDate := roleCreatedAt.Truncate(24 * time.Hour); endedDate := time.Now().UTC().Truncate(24 * time.Hour).
    SQL-INSERT: Spalten started_date, ended_date (statt started_year, ended_year). Parameter $3 = startedDate, $4 = endedDate (statt .Year()-Aufruf). status='ended', visibility='internal' bleiben.

    Keine anderen Stellen ändern. go build ./... muss danach fehlerfrei sein (die Migrations-Spalten existieren nach Task 1-Migration in der DB).
  </action>

  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go build ./... 2>&amp;1</automated>
  </verify>

  <done>
    fansub_group_app_members_repository.go kompiliert fehlerfrei. started_year/ended_year kommen nicht mehr im D-10-INSERT vor. tenure_started_on wird im Enable-INSERT gesetzt. go build ./... = 0 Fehler.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Migration-Skript → PostgreSQL | SQL-DDL wird als Datenbankadmin ausgeführt; kein Angreifer-Kontakt |
| Go-Repository → DB | Parametrisierte Queries, kein Injection-Risiko |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01-01 | Tampering | Migration 0114 — DROP COLUMN Reihenfolge | mitigate | Constraint-Drop explizit vor Column-Drop; IF EXISTS auf allen DDL-Statements; BEGIN/COMMIT wrapping |
| T-97-01-02 | Tampering | D-10-Auto-Archivierung schreibt nach Migration auf nicht-existente Spalten | mitigate | started_year/ended_year → started_date/ended_date ATOMAR in dieser Wave (Task 2 = gleiche Wave wie Task 1) |
| T-97-01-SC | Tampering | npm/pip/cargo installs | accept | Keine Package-Installs in dieser Wave — nur SQL + Go-Quelldatei-Änderungen |
</threat_model>

<verification>
Nach Task 1 + Task 2:

1. `docker compose exec team4sv30-postgres psql -U postgres -d team4s -c "\d hist_group_member_roles"` — Spalten started_date DATE, ended_date DATE vorhanden; started_year/ended_year NICHT vorhanden.
2. `docker compose exec team4sv30-postgres psql -U postgres -d team4s -c "\d hist_fansub_group_members"` — joined_date DATE, left_date DATE vorhanden.
3. `docker compose exec team4sv30-postgres psql -U postgres -d team4s -c "\d fansub_group_member_roles"` — tenure_started_on DATE vorhanden.
4. `docker compose exec team4sv30-postgres psql -U postgres -d team4s -c "SELECT COUNT(*) FROM hist_group_member_roles WHERE started_year IS NOT NULL"` — Fehler (Spalte existiert nicht) bestätigt Drop.
5. Bestandsdaten-Check: `SELECT started_date FROM hist_group_member_roles LIMIT 5` — Werte sind '2008-01-01' o. ä. (kein NULL wo ursprünglich Jahr war).
6. `cd backend && go build ./...` — 0 Fehler.
</verification>

<success_criteria>
- hist_group_member_roles: started_date DATE, ended_date DATE; kein started_year, kein ended_year.
- hist_fansub_group_members: joined_date DATE, left_date DATE; kein joined_year, kein left_year.
- fansub_group_member_roles: tenure_started_on DATE NULL neu vorhanden.
- Bestandsdaten korrekt gemappt (YEAR → 1. Januar des Jahres).
- D-10-INSERT in fansub_group_app_members_repository.go schreibt started_date/ended_date.
- SetRole Enable schreibt tenure_started_on.
- go build ./... fehlerfrei.
</success_criteria>

<output>
Erstelle `.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-01-SUMMARY.md` nach Abschluss.
</output>
