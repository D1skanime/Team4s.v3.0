---
phase: 97-revoke-rollen-lifecycle-uebergang
plan: 05
type: execute
wave: 5
depends_on:
  - 97-03
  - 97-04
files_modified: []
autonomous: false
requirements:
  - D-04
  - D-06
  - D-07
  - D-08

must_haves:
  truths:
    - "DB enthält korrekte DATE-Spalten; Bestandsdaten auf 1. Januar gemappt"
    - "Admin-UI zeigt tagesgenaue Datumseingabe im Historischen-Mitglied-Dialog"
    - "canForContext liefert Capabilities nur aus fansub_group_member_roles (aktive Rollen)"
    - "Claim-Aktivierungslogik: historische Rolle ohne Enddatum → aktive Rolle nach Verify"
    - "Aktive Rollen-Zuweisung via Admin-UI (SetRole-Pfad) weiterhin funktional"
  artifacts:
    - path: .planning/phases/97-revoke-rollen-lifecycle-uebergang/97-05-SUMMARY.md
      provides: "UAT-Ergebnis und Phase-Close"
  key_links:
    - from: "Admin-Browser :3000"
      to: "/admin/fansubs/[id]/edit"
      via: "Historisches-Mitglied-Dialog mit Datumseingabe"
      pattern: "input[type=date]"
---

<objective>
Vollständige Live-Verifikation aller Phase-97-Deliverables: DB-Schema, Backend-Kompilierung, Frontend-Typen und Admin-Dialog-UI. Bestätigt, dass D-01 bis D-08 korrekt umgesetzt sind, bevor die Phase geschlossen wird.

Zweck: Sicherheitsnetz vor Phase-Close. Stellt sicher, dass kein stiller Regressions-Bug durch eine der vier Waves überlebt hat.

Ausgabe: SUMMARY.md mit UAT-Ergebnis; Phase als geschlossen markierbar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-CONTEXT.md
@.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-01-SUMMARY.md
@.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-02-SUMMARY.md
@.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-03-SUMMARY.md
@.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automatisierte Vollverifikation (Build + Tests + DB-Check)</name>
  <files></files>

  <read_first>
    - .planning/phases/97-revoke-rollen-lifecycle-uebergang/97-VALIDATION.md — Vollständige Test-Map, Wave-0-Requirements, Phase-Gate
  </read_first>

  <action>
    Führe alle automatisierten Gates der Phase aus:

    1. Backend-Build: cd backend && go build ./...
    2. Backend-Tests: cd backend && go test ./internal/repository/... ./internal/handlers/... ./internal/permissions/...
    3. Frontend-TypeCheck: cd frontend && npx tsc --noEmit
    4. Frontend-Tests: cd frontend && npm run test

    5. Docker-Rebuild (Backend + Frontend — Pflicht nach Go-Änderungen):
       docker compose up -d --build team4sv30-backend team4sv30-frontend

    6. DB-Schema-Verifikation nach Rebuild:
       docker compose exec team4sv30-postgres psql -U postgres -d team4s -c "\d hist_group_member_roles" — started_date/ended_date DATE vorhanden, kein started_year/ended_year
       docker compose exec team4sv30-postgres psql -U postgres -d team4s -c "\d hist_fansub_group_members" — joined_date/left_date DATE vorhanden
       docker compose exec team4sv30-postgres psql -U postgres -d team4s -c "\d fansub_group_member_roles" — tenure_started_on DATE vorhanden

    7. D-07-Verifikation: grep -rn "hist_group_member_roles" backend/internal/permissions/ — muss 0 Treffer haben (canForContext liest hist nicht)

    8. API-Smoke: curl-Test gegen :8092 (historische Rollen-Endpunkt) mit Bearer-Token — Response enthält started_date/ended_date als ISO-Strings statt Jahres-Integers.

    Alle Ergebnisse dokumentieren.
  </action>

  <verify>
    <automated>
      cd C:/Users/admin/Documents/Team4s/backend && go build ./... 2>&amp;1 &amp;&amp; go test ./internal/... 2>&amp;1 | tail -5
    </automated>
    <human-check>
      Docker-Rebuild abgeschlossen und :3000 erreichbar bestätigen.
    </human-check>
  </verify>

  <done>
    go build ./... fehlerfrei. go test fehlerfrei. tsc --noEmit fehlerfrei. npm run test fehlerfrei. DB hat DATE-Spalten. D-07: 0 hist_group_member_roles Treffer in permissions/.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 97 vollständig implementiert:
    - Wave 1: DB-Migrationen 0114/0115 (INT→DATE, tenure_started_on) + D-10-Auto-Archiv-Fix
    - Wave 2: Backend-Repository-Structs + Handler-DTOs auf DATE umgestellt
    - Wave 3: TypeScript-Typen + Admin-Dialog-UI mit Input type="date"
    - Wave 4: Claim-Aktivierungslogik (historische Rollen ohne Enddatum → aktive App-Rollen)
  </what-built>

  <how-to-verify>
    Bitte nach Strg+F5 auf :3000 (Docker-Prod-Build) folgende Schritte prüfen:

    **A — Datumseingabe im Dialog (D-02/D-03):**
    1. Öffne /admin/fansubs/[id]/edit für eine beliebige Fansub-Gruppe.
    2. Gehe zum Tab "Historische Mitglieder".
    3. Klicke auf "Historisches Mitglied bearbeiten" (oder anlegen).
    4. Erwartung: Felder "Beitrittsdatum" und "Austrittsdatum" sind Datumseingaben (kein Jahres-Dropdown), direkt im Dialog (nicht separate Rollen-Sektion).

    **B — Historische Rollen mit Datum (D-01/D-03):**
    1. Klicke auf "Historische Rolle bearbeiten/hinzufügen" für ein historisches Mitglied.
    2. Erwartung: Dialog zeigt Rollenauswahl (Dropdown) + "Rolle von" / "Rolle bis" als Datumseingaben.
    3. Gib ein Datum ein (z. B. "Rolle von: 2008-03-01") und speichere.
    4. Erwartung: Kein Fehler; Rolle wird mit Datum gespeichert.

    **C — Enddatum-Regel (D-04):**
    1. Trage eine historische Rolle ein OHNE Enddatum.
    2. Überprüfe in der Anzeige: Rolle wird als "aktiv" / ohne Enddatum angezeigt.
    3. Trage dieselbe Rolle mit Enddatum ein.
    4. Überprüfe: Rolle wird als "beendet" angezeigt.

    **D — Backend-API-Response (D-08):**
    Prüfe im Browser-Netzwerk-Tab den Response des historischen Rollen-Endpunkts:
    Erwartung: JSON enthält "started_date": "2008-01-01" (ISO-String), NICHT "started_year": 2008.

    Wenn ein Schritt nicht funktioniert: Fehlerbeschreibung + Screenshot angeben.
  </how-to-verify>

  <resume-signal>
    Schreibe "approved" wenn alle 4 Checks bestanden sind, oder beschreibe konkret welcher Check fehlschlägt.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Verifikations-Plan | Nur Lesezugriffe und bestehende Admin-Aktionen; kein neuer Code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-05-01 | Tampering | Verifikations-curl mit realem Bearer-Token | accept | Token ist kurzlebig (Direct-Grant, Dev-Only); kein Produktionsrisiko |
| T-97-05-SC | Tampering | npm/pip/cargo installs | accept | Keine Package-Installs in diesem Plan |
</threat_model>

<verification>
Automatisiert (Task 1):
- go build ./... grün
- go test ./internal/... grün
- tsc --noEmit grün
- npm run test grün
- DB: started_date DATE in hist_group_member_roles vorhanden
- D-07: 0 hist_group_member_roles Treffer in permissions/

Manuell (Task 2 Checkpoint):
- Dialog zeigt Input type="date" für Rollen- und Mitglieds-Daten
- Datums-Speichern funktioniert
- JSON-Response enthält ISO-Datum-Strings
</verification>

<success_criteria>
- Alle automatisierten Tests grün.
- Human-Verify: Dialog zeigt tagesgenaue Datumseingabe in Admin-UI.
- JSON-API liefert ISO-Datum-Strings.
- Kein Produktionsfehler nach Docker-Rebuild.
- Phase kann als abgeschlossen markiert werden.
</success_criteria>

<output>
Erstelle `.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-05-SUMMARY.md` nach Abschluss.
Aktualisiere `.planning/ROADMAP.md` — Phase 97 als completed markieren.
</output>
