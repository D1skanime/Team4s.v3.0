---
phase: 97-revoke-rollen-lifecycle-uebergang
plan: 03
type: execute
wave: 3
depends_on:
  - 97-02
files_modified:
  - frontend/src/types/fansub.ts
  - frontend/src/types/domain-projection.ts
  - frontend/src/types/contributions.ts
  - frontend/src/app/admin/fansubs/[id]/edit/GroupHistRoleDialog.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/GroupMemberFormModals.tsx
autonomous: true
requirements:
  - D-01
  - D-03

must_haves:
  truths:
    - "TypeScript-Typen für HistGroupMemberRole und HistFansubGroupMember verwenden string|null für Datums-Felder statt number|null"
    - "GroupHistRoleDialog zeigt Input type='date' aus @/components/ui statt YearPicker für Rollen-Start/-Ende"
    - "GroupMemberFormModals zeigt Input type='date' aus @/components/ui statt YearPicker für Beitritts-/Austrittsdatum"
    - "Ein Mitglied kann N historische Rollen eingeben (Dialog erlaubt mehrere Rollen-Einträge)"
    - "YearPicker-Import in GroupHistRoleDialog.tsx und GroupMemberFormModals.tsx ist entfernt"
    - "tsc --noEmit und npm run test im frontend sind fehlerfrei"
  artifacts:
    - path: frontend/src/types/fansub.ts
      provides: "Alle year-Felder auf date string|null umgestellt"
      contains: "started_date: string | null"
    - path: frontend/src/app/admin/fansubs/[id]/edit/GroupHistRoleDialog.tsx
      provides: "Datumseingabe per Input type='date' + N-Rollen-Support"
      contains: "type=\"date\""
    - path: frontend/src/app/admin/fansubs/[id]/edit/GroupMemberFormModals.tsx
      provides: "Datumseingabe per Input type='date' für Mitglied"
      contains: "joinedDate"
  key_links:
    - from: "frontend/src/app/admin/fansubs/[id]/edit/GroupHistRoleDialog.tsx"
      to: "frontend/src/types/fansub.ts"
      via: "RoleFormFields.startedDate: string"
      pattern: "startedDate"
    - from: "frontend/src/app/admin/fansubs/[id]/edit/GroupMemberFormModals.tsx"
      to: "frontend/src/types/fansub.ts"
      via: "MemberFormFields.joinedDate: string"
      pattern: "joinedDate"
---

<objective>
Frontend-Typen und Authoring-Dialoge auf tagesgenaue Datumseingabe umstellen. Die TypeScript-Interfaces spiegeln die neuen Backend-DTOs wider (string|null statt number|null). Die Rollen- und Mitglied-Dialoge erhalten Input type="date" aus @/components/ui.

Zweck: Vervollständigt D-03 (Rollenauswahl direkt im Dialog mit tagesgenauen Daten) und D-01 (N historische Rollen pro Person — Dialog darf nicht auf 1 Rolle pro Mitglied limitieren). Ohne diese Wave würde die Admin-UI mit Typfehlern oder falschen YearPicker-Feldern arbeiten.

Ausgabe: 5 Frontend-Dateien mit korrekten Typen und Datumseingabe-UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-CONTEXT.md
@.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-RESEARCH.md
@.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-PATTERNS.md
@.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-02-SUMMARY.md
</context>

<interfaces>
<!-- Bestehende Typ-Stellen (aus Codebase-Grep extrahiert): -->

frontend/src/types/fansub.ts (betroffen, Zeilenbereich ca. 563–644):
- HistFansubGroupMember: joined_year: number | null → joined_date: string | null
- HistFansubGroupMember: left_year: number | null → left_date: string | null
- CreateGroupMemberRequest: joined_year/left_year analog
- UpdateGroupMemberRequest: joined_year/left_year analog
- HistGroupMemberRole: started_year: number | null → started_date: string | null
- HistGroupMemberRole: ended_year: number | null → ended_date: string | null
- CreateMemberRoleRequest: started_year/ended_year analog
- UpdateMemberRoleRequest: started_year/ended_year analog

frontend/src/types/domain-projection.ts (Zeilen 34-35, 50-51):
- joined_year: number | null → joined_date: string | null
- left_year: number | null → left_date: string | null
- started_year: number | null → started_date: string | null
- ended_year: number | null → ended_date: string | null

frontend/src/types/contributions.ts (Zeilen 8-9, 23-24, 84-85, 127-128, 162-163, 215-216):
- started_year: number | null → started_date: string | null
- ended_year: number | null → ended_date: string | null

Globale @/components/ui Primitives vorhanden: Button, FormField, Input, Modal, Select, Textarea.
YearPicker ist in @/components/ui vorhanden — darf in GroupHistRoleDialog/GroupMemberFormModals nicht mehr importiert werden.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TypeScript-Typen in fansub.ts, domain-projection.ts, contributions.ts auf string|null umstellen</name>
  <files>
    frontend/src/types/fansub.ts,
    frontend/src/types/domain-projection.ts,
    frontend/src/types/contributions.ts
  </files>

  <read_first>
    - frontend/src/types/fansub.ts — vollständige Datei; alle Interfaces die year-Felder enthalten (ca. Z. 563–644)
    - frontend/src/types/domain-projection.ts — Zeilen 30–60 (joined_year/left_year/started_year/ended_year)
    - frontend/src/types/contributions.ts — vollständige Datei (alle 6 Stellen mit started_year/ended_year)
    - .planning/phases/97-revoke-rollen-lifecycle-uebergang/97-PATTERNS.md — frontend/src/types/fansub.ts-Sektion (genaue Auflistung aller 8 Interface-Änderungen)
  </read_first>

  <behavior>
    - fansub.ts: In allen 8 Interfaces (HistFansubGroupMember, CreateGroupMemberRequest, UpdateGroupMemberRequest, HistGroupMemberRole, CreateMemberRoleRequest, UpdateMemberRoleRequest sowie allfällige weitere) year-Felder umbenennen: joined_year → joined_date, left_year → left_date, started_year → started_date, ended_year → ended_date. Typen: number|null → string|null, number? → string|null (optional-Felder).
    - domain-projection.ts: Gleiche 4 Felder umstellen.
    - contributions.ts: Alle 6 Stellen started_year/ended_year → started_date/ended_date umstellen.
    - Keine anderen Interfaces oder Felder ändern. Keine neuen Imports nötig.
  </behavior>

  <action>
    fansub.ts: Alle `joined_year: number | null` → `joined_date: string | null`, `left_year: number | null` → `left_date: string | null`, `started_year: number | null` → `started_date: string | null`, `ended_year: number | null` → `ended_date: string | null`. Bei optional-Feldern (? Suffix): `started_year?: number | null` → `started_date?: string | null`.

    domain-projection.ts: 4 Felder analog.

    contributions.ts: 6 Vorkommnisse analog.

    Sicherheits-Check: Nicht versehentlich andere Interfaces (z. B. Anime-bezogene mit year-Feldern) ändern. Nur Fansub/Hist-Kontext-Interfaces betroffen.
  </action>

  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&amp;1 | head -30</automated>
  </verify>

  <done>
    tsc --noEmit: 0 Fehler (oder nur nicht mit dieser Phase zusammenhängende Pre-existing Fehler). Keine number|null-Felder mehr für year-Datums-Felder in den 3 Typ-Dateien.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GroupHistRoleDialog + GroupMemberFormModals YearPicker → Input type="date"</name>
  <files>
    frontend/src/app/admin/fansubs/[id]/edit/GroupHistRoleDialog.tsx,
    frontend/src/app/admin/fansubs/[id]/edit/GroupMemberFormModals.tsx
  </files>

  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/GroupHistRoleDialog.tsx — vollständige Datei (171 Zeilen); Import-Block, RoleFormFields-Typ, YearPicker-Blöcke Zeilen 131–151
    - frontend/src/app/admin/fansubs/[id]/edit/GroupMemberFormModals.tsx — vollständige Datei (221 Zeilen); Import-Block, MemberFormFields-Typ, YearPicker-Blöcke Zeilen 119–139
    - frontend/src/app/admin/fansubs/[id]/edit/GroupMembersTab.tsx — vollständige Datei (201 Zeilen); wie die Dialoge aufgerufen werden (Prop-Typen, Initialisierung der Form-Felder)
    - .planning/phases/97-revoke-rollen-lifecycle-uebergang/97-PATTERNS.md — GroupHistRoleDialog-Sektion (Ziel-Import-Block, RoleFormFields, Input type="date" Muster) + GroupMemberFormModals-Sektion
  </read_first>

  <behavior>
    GroupHistRoleDialog.tsx:
    - Import: YearPicker entfernen, Input hinzufügen (falls nicht bereits importiert).
    - RoleFormFields-Typ: startedYear: string → startedDate: string; endedYear: string → endedDate: string.
    - YearPicker-Blöcke für "Rolle von" und "Rolle bis" ersetzen durch FormField+Input type="date"-Blöcke (Muster aus PATTERNS.md). Label "Rolle von" / "Rolle bis". FormField hint für endedDate: "Leer lassen, wenn die Person weiterhin aktiv in dieser Funktion ist."
    - Formular-Initialisierung und Submit-Payload aktualisieren: startedDate/endedDate als ISO-String senden statt startedYear/endedYear als number.
    - Rollenauswahl (Select, historyRoleOptions) bleibt unverändert erhalten.
    - D-01-Support: Dialog darf nicht auf 1 Rolle pro Submit limitieren. Wenn aktuell bereits ein „Rolle hinzufügen"-Flow existiert (mehrere Rollen durch wiederholtes Öffnen des Dialogs), bleibt er erhalten. Falls der Dialog bisher nur eine Rolle erlaubt: keine Änderung am Flow nötig — das Schema unterstützt N Rollen durch wiederholtes Anlegen.

    GroupMemberFormModals.tsx:
    - Import: YearPicker entfernen.
    - MemberFormFields-Typ: joinedYear: string → joinedDate: string; leftYear: string → leftDate: string.
    - YearPicker-Blöcke für Beitrittsjahr/Austrittsjahr ersetzen durch FormField+Input type="date"-Blöcke. Labels "Beitrittsdatum" / "Austrittsdatum". Hints aus PATTERNS.md.
    - Formular-Initialisierung und Submit-Payload: joinedDate/leftDate als ISO-String.

    PFLICHT für beide Dateien:
    - `<Input type="date" ... />` aus @/components/ui — KEIN natives `<input type="date">` direkt.
    - aria-label auf jedem Input type="date".
    - id-Attribute eindeutig pro Feld.
  </behavior>

  <action>
    GroupHistRoleDialog.tsx:
    1. Import-Block: YearPicker entfernen, Input hinzufügen (alphabetisch sortiert zwischen FormField und Modal).
    2. RoleFormFields: startedYear → startedDate, endedYear → endedDate.
    3. Formular-Initialwert (useState/useEffect): '' für startedDate und endedDate (statt '').
    4. YearPicker-Block "Rolle von" (ca. Z. 131–139) ersetzen durch:
       <FormField label="Rolle von" htmlFor="member-role-started-date">
         <Input id="member-role-started-date" type="date" value={roleForm.startedDate} onChange={(e) => setRoleForm((f) => ({ ...f, startedDate: e.target.value }))} aria-label="Rolle von" />
       </FormField>
    5. YearPicker-Block "Rolle bis" (ca. Z. 141–151) ersetzen durch:
       <FormField label="Rolle bis" htmlFor="member-role-ended-date" hint="Leer lassen, wenn die Person weiterhin aktiv in dieser Funktion ist.">
         <Input id="member-role-ended-date" type="date" value={roleForm.endedDate} onChange={(e) => setRoleForm((f) => ({ ...f, endedDate: e.target.value }))} aria-label="Rolle bis" />
       </FormField>
    6. Submit-Payload: started_date: roleForm.startedDate || null, ended_date: roleForm.endedDate || null (statt started_year/ended_year als number).

    GroupMemberFormModals.tsx:
    1. Import: YearPicker entfernen.
    2. MemberFormFields: joinedYear → joinedDate, leftYear → leftDate.
    3. YearPicker-Block "Beitrittsjahr" ersetzen durch FormField+Input type="date" mit id="hist-member-joined-date", hint="Optionaler Startpunkt der Mitgliedschaft."
    4. YearPicker-Block "Austrittsjahr" ersetzen durch FormField+Input type="date" mit id="hist-member-left-date", hint="Leer lassen, wenn die Person weiterhin aktiv ist."
    5. Submit-Payload: joined_date: form.joinedDate || null, left_date: form.leftDate || null.
    6. GroupMembersTab.tsx: Alle Props/Initialisierungen die joinedYear/leftYear/startedYear/endedYear referenzieren auf die neuen Feldnamen umstellen.
  </action>

  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&amp;1 | head -30 && npm run test -- --reporter=verbose 2>&amp;1 | tail -20</automated>
  </verify>

  <done>
    tsc --noEmit fehlerfrei. npm run test grün. Kein YearPicker-Import in GroupHistRoleDialog.tsx oder GroupMemberFormModals.tsx. Beide Dateien enthalten Input type="date" (aus @/components/ui). Kein natives &lt;input type="date"&gt; direkt. Beide Dateien ≤ 450 Zeilen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Frontend-Form → API | Date-String wird als ISO 8601 gesendet; Backend validiert (Plan 02 parseOptionalDate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-03-01 | Tampering | Frontend sendet beliebigen Date-String | accept | Validierung liegt im Backend (parseOptionalDate → 400); Frontend-Input type="date" schränkt Browser-Eingabe ein |
| T-97-03-02 | Tampering | natives &lt;input type="date"&gt; statt @/components/ui Input | mitigate | Enforcement: ESLint warnt bei nativem input; CLAUDE.md-Regel; PATTERNS.md zeigt explizit das Pflicht-Muster |
| T-97-03-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Packages; nur TypeScript/TSX-Änderungen |
</threat_model>

<verification>
Nach beiden Tasks:
1. `cd frontend && npx tsc --noEmit` — 0 Fehler.
2. `npm run test` — alle Tests grün.
3. `grep -r "YearPicker" frontend/src/app/admin/fansubs/[id]/edit/GroupHistRoleDialog.tsx frontend/src/app/admin/fansubs/[id]/edit/GroupMemberFormModals.tsx` — 0 Treffer.
4. `grep -r "started_year\|ended_year\|joined_year\|left_year" frontend/src/types/fansub.ts frontend/src/types/domain-projection.ts frontend/src/types/contributions.ts` — 0 Treffer.
5. `grep -n "type=\"date\"" frontend/src/app/admin/fansubs/[id]/edit/GroupHistRoleDialog.tsx` — mindestens 2 Treffer (started_date + ended_date).
6. Datei-Limit: `wc -l frontend/src/app/admin/fansubs/[id]/edit/GroupHistRoleDialog.tsx frontend/src/app/admin/fansubs/[id]/edit/GroupMemberFormModals.tsx` — beide ≤ 450.
</verification>

<success_criteria>
- Alle TypeScript year-Felder in den 3 Typ-Dateien sind auf string|null umgestellt.
- GroupHistRoleDialog: Input type="date" für Rollen-Start/-Ende; kein YearPicker.
- GroupMemberFormModals: Input type="date" für Beitritts-/Austrittsdatum; kein YearPicker.
- tsc --noEmit und npm run test sind fehlerfrei.
- Beide Dialog-Dateien ≤ 450 Zeilen.
</success_criteria>

<output>
Erstelle `.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-03-SUMMARY.md` nach Abschluss.
</output>
