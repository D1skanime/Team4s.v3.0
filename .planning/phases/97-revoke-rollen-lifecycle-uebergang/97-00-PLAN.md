---
phase: 97-revoke-rollen-lifecycle-uebergang
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - backend/internal/repository/hist_group_member_roles_date_test.go
  - backend/internal/repository/member_claims_repository_claim_activation_test.go
  - frontend/src/app/admin/fansubs/[id]/edit/GroupHistRoleDialog.test.tsx
autonomous: true
requirements:
  - D-02
  - D-03
  - D-04
  - D-05

must_haves:
  truths:
    - "hist_group_member_roles_date_test.go existiert und testet D-02/D-04: DATE-Spalten-Erwartung + ended_date IS NULL = aktiv-Semantik (RED)"
    - "member_claims_repository_claim_activation_test.go existiert und testet D-05: hist ohne Enddatum → aktive Rolle; fansub_lead/founder ausgeschlossen (RED)"
    - "GroupHistRoleDialog.test.tsx existiert und testet D-03: Rollenauswahl + Input type=date statt YearPicker (RED)"
    - "go test ./internal/repository/... schlägt fehlerfrei ab, weil Implementierung noch fehlt — aber Compiler-Fehler gibt es nicht"
    - "npx vitest run existiert und erkennt GroupHistRoleDialog.test.tsx — Test schlägt wegen fehlendem Input type=date fehl (RED)"
  artifacts:
    - path: backend/internal/repository/hist_group_member_roles_date_test.go
      provides: "RED-Tests für D-02/D-04: DATE-Felder + Enddatum-Semantik"
      contains: "TestHistRoleEndDateRule"
    - path: backend/internal/repository/member_claims_repository_claim_activation_test.go
      provides: "RED-Tests für D-05: Claim-Aktivierung Whitelist + Ausschlüsse"
      contains: "TestVerifyClaimActivatesRoles"
    - path: frontend/src/app/admin/fansubs/[id]/edit/GroupHistRoleDialog.test.tsx
      provides: "RED-Tests für D-03: Datumseingabe-UI"
      contains: "type=\"date\""
  key_links:
    - from: "backend/internal/repository/hist_group_member_roles_date_test.go"
      to: "backend/internal/repository/hist_group_member_roles_repository.go"
      via: "StartedDate *time.Time — Struct-Feld nach Migration"
      pattern: "StartedDate"
    - from: "backend/internal/repository/member_claims_repository_claim_activation_test.go"
      to: "backend/internal/repository/member_claims_repository.go"
      via: "ResolvePendingRolesToActive — neue Methode"
      pattern: "ResolvePendingRolesToActive"
    - from: "frontend/src/app/admin/fansubs/[id]/edit/GroupHistRoleDialog.test.tsx"
      to: "frontend/src/app/admin/fansubs/[id]/edit/GroupHistRoleDialog.tsx"
      via: "Input type=date erwartet, YearPicker nicht vorhanden"
      pattern: "type.*date"
---

<objective>
Wave-0-Plan: RED-Test-Gerüste anlegen, bevor die Implementierung in den Wellen 1–4 läuft. Diese Tests sind bewusst ROT — sie beschreiben das Ziel-Verhalten, das noch nicht existiert. Die Wellen 1–4 machen sie grün.

Zweck: Nyquist-Compliance herstellen. Ohne Wave 0 kann der Checker nicht maschinell prüfen, ob D-02/D-03/D-04/D-05 korrekt implementiert sind. Die Tests dienen als verhaltensprüfende Sicherheitsnetz vor Phase-Close.

Ausgabe: 3 Testdateien (2 Go, 1 Vitest). Kein Produktionscode wird geändert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-CONTEXT.md
@.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-RESEARCH.md
@.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-PATTERNS.md
@.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-VALIDATION.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Go RED-Tests — DATE-Spalten (D-02/D-04) + Claim-Aktivierung (D-05)</name>
  <files>
    backend/internal/repository/hist_group_member_roles_date_test.go,
    backend/internal/repository/member_claims_repository_claim_activation_test.go
  </files>

  <read_first>
    - backend/internal/repository/hist_group_member_roles_repository.go — aktuelle Struct-Definitionen (StartedYear *int, EndedYear *int), IsGroupHistoryWhitelistRole-Signatur
    - backend/internal/repository/member_claims_repository.go — VerifyClaim-Signatur (Z. 175), ClaimMutationError-Typ, Package-Name
    - backend/internal/repository/fansub_group_app_members_repository.go — SetRole-Signatur (Zeile ~380), Package-Name
    - .planning/phases/97-revoke-rollen-lifecycle-uebergang/97-CONTEXT.md — D-02 (DATE-Spalten), D-04 (Enddatum-Regel), D-05 (Claim-Aktivierung: fansub_lead/founder ausgeschlossen)
  </read_first>

  <behavior>
    Datei hist_group_member_roles_date_test.go (D-02/D-04):
    - TestHistRoleStructHasDateFields: prüft per Compile-Zeit-Assertion, dass HistGroupMemberRoleRow.StartedDate vom Typ *time.Time ist (nicht *int). Verwendet reflect.TypeOf zur Laufzeit — schlägt nach Migration grün; schlägt vor Migration mit "StartedDate field not found" fehl.
    - TestHistRoleEndDateRule_NilEnddateIsActive: instantiiert eine HistGroupMemberRoleRow mit EndedDate = nil und prüft (per einfacher Bedingung), dass eine Funktion IsRoleActive(row) true zurückgibt.
    - TestHistRoleEndDateRule_SetEnddateIsHistorical: instantiiert HistGroupMemberRoleRow mit EndedDate = &someDate und prüft, dass IsRoleActive(row) false zurückgibt.
    - IsRoleActive ist eine einfache Hilfsfunktion im selben Package (package repository, nicht _test) — damit die Tests gegen echten Produktionscode testen, statt Mock-Logik.
    - Da IsRoleActive und die neuen Struct-Felder noch nicht existieren: Tests schlagen in RED-Phase mit Compile-Fehler fehl → korrekt für Wave 0.

    Datei member_claims_repository_claim_activation_test.go (D-05):
    - TestResolvePendingRolesToActive_ExistsOnRepository: prüft, dass MemberClaimsRepository eine Methode ResolvePendingRolesToActive(ctx, memberID, fansubGroupID, actorID int64) error hat — via interface-Assertion oder reflect.TypeOf.
    - TestVerifyClaimActivatesRoles_FansubLeadExcluded: Doku-Test (ohne echte DB), der das Ausschluss-Verhalten über einen kurzen Kommentar-basierten Expectations-Block beschreibt. Nutzt t.Skip("RED: Implementierung fehlt noch — wird in Plan 04 grün") bis zur Umsetzung.
    - TestVerifyClaimActivatesRoles_FounderExcluded: analog für founder. Nutzt t.Skip("RED: ...").
    - TestVerifyClaimActivatesRoles_NilEnddateRoleActivated: Doku-Test mit t.Skip für den Happy-Path (rolle ohne Enddatum wird aktiv).
    - Package: package repository_test, import-Pfad analog zu bestehenden Testdateien im selben Verzeichnis.
    - Die t.Skip-Einträge sorgen dafür, dass die Tests RUN (nicht als Compile-Fehler scheitern), aber als SKIPPED gemeldet werden — bis Plan 04 die echte Logik liefert, dann t.Skip entfernen.
  </behavior>

  <action>
    Datei backend/internal/repository/hist_group_member_roles_date_test.go:
    Package-Header: package repository_test. Imports: testing, reflect, time, plus der package-eigene Import-Pfad (wie in bestehenden Test-Dateien im Verzeichnis — per grep bestimmen).

    TestHistRoleStructHasDateFields: Erstelle ein HistGroupMemberRoleRow{} und prüfe per reflect.TypeOf(...).Field-Lookup, dass ein Feld "StartedDate" vom Kind "Ptr" (auf time.Time) existiert. Falls nicht: t.Fatal("HistGroupMemberRoleRow.StartedDate *time.Time nicht gefunden — D-02 nicht implementiert").

    TestHistRoleEndDateRule_NilEnddateIsActive und TestHistRoleEndDateRule_SetEnddateIsHistorical: Prüfen gegen eine Hilfsfunktion IsRoleActive(r HistGroupMemberRoleRow) bool, die in einer separaten Datei hist_group_member_roles_helpers.go (package repository) angelegt wird: gibt true zurück wenn EndedDate == nil, sonst false. Diese Hilfsdatei gehört zum Produktionscode — 8 Zeilen.

    Datei backend/internal/repository/member_claims_repository_claim_activation_test.go:
    Package-Header: package repository_test. Vier Testfunktionen wie im behavior-Block beschrieben. Die t.Skip-Einträge in den drei Implementations-Tests tragen eine lesbare Botschaft: "RED Wave 0 — wird in Plan 04 durch echte DB-Tests ersetzt". TestResolvePendingRolesToActive_ExistsOnRepository prüft ohne t.Skip per reflect ob die Methode vorhanden ist — schlägt vor Plan 04 mit "method not found" fehl.

    Neue Hilfsdatei backend/internal/repository/hist_group_member_roles_helpers.go (package repository, ≤15 Zeilen): func IsRoleActive(r HistGroupMemberRoleRow) bool { return r.EndedDate == nil }.
  </action>

  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/repository/... 2>&amp;1 | grep -E "FAIL|SKIP|ok|compile" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - hist_group_member_roles_date_test.go existiert mit package repository_test.
    - hist_group_member_roles_helpers.go existiert mit IsRoleActive (≤15 Zeilen).
    - member_claims_repository_claim_activation_test.go existiert mit 4 Testfunktionen.
    - go test ./internal/repository/... kompiliert (kein Syntax-Fehler). Tests schlagen mit FAIL/SKIP fehl (RED-Status), nicht mit Compiler-Fehler auf Testdatei-Seite.
    - TestHistRoleStructHasDateFields schlägt mit FAIL fehl (kein StartedDate-Feld in aktuellem Struct).
    - TestResolvePendingRolesToActive_ExistsOnRepository schlägt mit FAIL fehl (Methode noch nicht vorhanden).
    - Andere t.Skip-Tests werden als SKIP gemeldet.
  </acceptance_criteria>

  <done>
    3 Dateien existieren und kompilieren. go test zeigt FAIL oder SKIP für alle neuen Tests (RED-Status korrekt). Kein unerwarteter Compile-Fehler in bestehenden Dateien.
  </done>
</task>

<task type="tdd">
  <name>Task 2: Vitest RED-Test — GroupHistRoleDialog Datumseingabe (D-03)</name>
  <files>
    frontend/src/app/admin/fansubs/[id]/edit/GroupHistRoleDialog.test.tsx
  </files>

  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/GroupHistRoleDialog.tsx — vollständige Datei (171 Zeilen); aktueller Import-Block, RoleFormFields-Typ, YearPicker-Import
    - frontend/vitest.config.ts — Test-Setup (jsdom, path aliases)
    - frontend/src/app/admin/fansubs/[id]/edit/ — prüfe ob bereits eine .test.tsx existiert (Glob)
    - .planning/phases/97-revoke-rollen-lifecycle-uebergang/97-CONTEXT.md — D-03: Rolle direkt im Dialog wählbar, tagesgenau
  </read_first>

  <behavior>
    Testdatei frontend/src/app/admin/fansubs/[id]/edit/GroupHistRoleDialog.test.tsx:
    - Importiert GroupHistRoleDialog (default- oder named export, je nach Datei).
    - Test "rendert Input type=date für Rollen-Start": rendert die Komponente mit Minimal-Props (roleOptions=[], onSave=jest.fn(), onClose=jest.fn(), initialRole=null oder äquivalent). Prüft, dass ein input[type="date"] im gerenderten DOM vorhanden ist. Erwartet KEINEN YearPicker. Falls nach Wave 3 der Dialog noch keinen Input type=date hat: Test schlägt fehl (RED).
    - Test "rendert Input type=date für Rollen-Ende mit Hinweis": prüft zweites date-Input und das Hint-Label "Leer lassen, wenn die Person weiterhin aktiv" im DOM.
    - Test "enthält Rollenauswahl-Select": prüft, dass ein select-Element (aus dem bestehenden historyRoleOptions-Block) vorhanden ist — validiert D-01 (N Rollen: Dialog nicht auf 1 Rolle limitiert, da er mehrfach geöffnet werden kann).
    - Nutzt @testing-library/react render + screen.queryBy* (bereits im Projekt wenn vorhanden — per grep in package.json prüfen; falls nicht: screen.container.querySelector als Fallback).
    - Alle drei Tests sind initiell RED (kein Input type=date in GroupHistRoleDialog.tsx vor Plan 03).
  </behavior>

  <action>
    Testdatei anlegen unter frontend/src/app/admin/fansubs/[id]/edit/GroupHistRoleDialog.test.tsx.

    Prüfe zuerst, ob @testing-library/react in frontend/package.json vorhanden ist (grep "@testing-library"). Falls ja: import { render, screen } from '@testing-library/react'. Falls nein: Fallback auf container.querySelector via render aus vitest/react.

    Minimale Props für GroupHistRoleDialog bestimmen per Read der .tsx-Datei (Props-Interface lesen). Fehlende Required-Props mit sinnvollen Defaults befüllen (leere Arrays, no-op callbacks).

    Drei describe-Blöcke oder drei einzelne it()-Blöcke:
    1. "rendert Input type=date für 'Rolle von'": erwartet container.querySelector('input[type="date"]') nicht null.
    2. "rendert Input type=date für 'Rolle bis' mit Hinweis": erwartet zweites date-Input und Text "Leer lassen".
    3. "enthält Rollenauswahl-Select": erwartet container.querySelector('select') nicht null (für historyRoleOptions).

    Alle Tests stehen ohne vi.skip — sie sollen aktiv RED sein bis Plan 03 die echte Implementierung liefert.
  </action>

  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npx vitest run src/app/admin/fansubs 2>&amp;1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - GroupHistRoleDialog.test.tsx existiert mit mindestens 3 Testfällen.
    - npx vitest run src/app/admin/fansubs läuft ohne Syntax-/Import-Fehler auf der Testdatei selbst durch.
    - Alle 3 neuen Tests schlagen fehl (RED): input[type="date"] nicht gefunden, da GroupHistRoleDialog.tsx noch YearPicker nutzt.
    - Bestehende Tests (falls vorhanden) bleiben unberührt und bestehen weiterhin.
  </acceptance_criteria>

  <done>
    GroupHistRoleDialog.test.tsx existiert. npx vitest run zeigt 3 FAIL für die neuen Tests (RED-Status korrekt). Keine Compile-Fehler in der Testdatei.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Testdateien → Produktionscode | Testdateien lesen Produktions-Structs und -Methoden; kein Schreibzugriff auf DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-00-01 | Tampering | Wave-0-Tests überschreiben Produktionscode | accept | Tests liegen in _test.go- bzw. .test.tsx-Dateien; Go-Test-Compiler trennt Test- von Produktionscode |
| T-97-00-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Packages; nur Test-Dateien + eine kleine Helper-Datei |
</threat_model>

<verification>
Nach beiden Tasks:
1. `cd backend && go test ./internal/repository/... 2>&1 | head -30` — Ausgabe enthält FAIL oder SKIP für neue Tests; kein "build failed" auf Test-Seite.
2. `cd frontend && npx vitest run src/app/admin/fansubs 2>&1 | tail -20` — GroupHistRoleDialog.test.tsx erscheint mit 3 FAIL-Tests.
3. `ls backend/internal/repository/hist_group_member_roles_date_test.go backend/internal/repository/hist_group_member_roles_helpers.go backend/internal/repository/member_claims_repository_claim_activation_test.go frontend/src/app/admin/fansubs/*/edit/GroupHistRoleDialog.test.tsx` — alle 4 Dateien vorhanden.
4. `wc -l backend/internal/repository/hist_group_member_roles_helpers.go` — ≤15 Zeilen.
</verification>

<success_criteria>
- 3 neue Testdateien existieren (2 Go, 1 Vitest).
- Alle neuen Tests sind aktiv RED (schlagen fehl wegen fehlender Implementierung — nicht wegen Syntax-Fehlern in Testcode).
- go test ./internal/repository/... und npx vitest run kompilieren fehlerfrei.
- Nyquist-Compliance der Phase ist damit hergestellt: Wave-0-Tests decken D-02, D-03, D-04, D-05 ab.
- Keine Änderungen an Produktionscode (außer der ≤15-Zeilen-Hilfsdatei IsRoleActive).
</success_criteria>

<output>
Erstelle `.planning/phases/97-revoke-rollen-lifecycle-uebergang/97-00-SUMMARY.md` nach Abschluss.
</output>
