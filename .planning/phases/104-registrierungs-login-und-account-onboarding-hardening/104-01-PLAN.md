---
phase: 104
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - infra/keycloak/realm-team4s.json
  - infra/keycloak/themes/team4s/login/theme.properties
  - infra/keycloak/themes/team4s/login/messages/messages_de.properties
  - infra/keycloak/themes/team4s/login/resources/css/login.css
  - infra/keycloak/themes/team4s/login/resources/js/registration-validation.js
  - frontend/src/lib/keycloakRegistrationValidation.test.ts
  - docker-compose.yml
  - scripts/verify-keycloak-config.ps1
  - docs/operations/keycloak-auth-foundation-phase43.md
autonomous: true
requirements: [P104-REG-2, P104-ACCOUNT-2]
must_haves:
  truths:
    - "D-11 D-12 D-13 D-14: local password 123 remains valid, lockout stays disabled, Direct Grants remain enabled, and email verification stays disabled."
    - "D-16 D-21: the external Keycloak Account Console works in a new tab and all Keycloak user surfaces are German and Team4s-branded."
    - "D-22: editing one invalid registration field clears only that field's stale validation state; server validation remains authoritative."
---

<objective>
Fix the reproducible Keycloak Account Console 403 and version a minimal German Team4s theme, while preserving the deliberately permissive local test posture and repairing stale per-field registration validation.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Diagnose and fix the Account Console at its Keycloak owner</name>
  <read_first>
    <file>AGENTS.md</file><file>104-CONTEXT.md</file><file>104-RESEARCH.md</file>
    <file>infra/keycloak/realm-team4s.json</file><file>docker-compose.yml</file>
    <file>docs/operations/keycloak-auth-foundation-phase43.md</file>
    <file>frontend/src/app/me/profile/components/AccountSecurityCard.tsx</file>
  </read_first>
  <files>infra/keycloak/realm-team4s.json, docker-compose.yml, scripts/verify-keycloak-config.ps1, docs/operations/keycloak-auth-foundation-phase43.md</files>
  <action>Reproduce the 403 and record effective Keycloak 26 account/account-console client state, redirects, origins and logs before changing configuration. Apply only the evidence-backed realm/client fix. Document and script both fresh import and an idempotent controlled update for an existing local volume. Assert `registrationAllowed=true`, German locale/default locale, `verifyEmail=false`, empty/no password policy, `bruteForceProtected=false`, and `team4s-frontend.directAccessGrantsEnabled=true`. Do not add unique-email enforcement, role mappings, Team4s DB roles, Member records, memberships, contributions or projects. The Keycloak realm role `user` may remain or be removed only if mappings are proven unused; it must never become Team4s authorization (D-10, D-15).</action>
  <verify>Run `docker compose config`, parse the realm JSON, run `powershell -ExecutionPolicy Bypass -File scripts/verify-keycloak-config.ps1`, and prove both fresh/imported-realm paths open `/realms/team4s/account` with the same session and no 403.</verify>
  <acceptance_criteria>The Account Console 403 has a documented cause and reproducible configuration fix; password `123` works locally; no password policy, lockout, email verification, unique-email or automatic domain authorization is introduced; Direct Grants remain enabled only as documented local test support.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add the minimal German theme and isolated stale-validation repair</name>
  <read_first>
    <file>infra/keycloak/realm-team4s.json</file><file>docker-compose.yml</file>
    <file>docs/frontend/ui-system.md</file><file>frontend/vitest.config.ts</file><file>frontend/package.json</file>
  </read_first>
  <files>infra/keycloak/themes/team4s/login/theme.properties, infra/keycloak/themes/team4s/login/messages/messages_de.properties, infra/keycloak/themes/team4s/login/resources/css/login.css, infra/keycloak/themes/team4s/login/resources/js/registration-validation.js, frontend/src/lib/keycloakRegistrationValidation.test.ts, docker-compose.yml, scripts/verify-keycloak-config.ps1, docs/operations/keycloak-auth-foundation-phase43.md</files>
  <action>Mount a thin Keycloak-26 parent theme and set Team4s display branding plus German login, registration, password-reset and Account Console locale/copy. Do not display password requirements that do not exist and do not claim verified email. Inspect the live Keycloak 26 registration DOM, then wire `resources/js/registration-validation.js` through `theme.properties`; on input/change remove only the edited field's stale aria-invalid/message association, preserving sibling/global errors and the next server response. Put executable jsdom coverage at `frontend/src/lib/keycloakRegistrationValidation.test.ts`, importing/executing the infra theme script and covering username, password, confirmation, email, first name and last name. The only permitted template fallback is the exact optional file `infra/keycloak/themes/team4s/login/register.ftl`; keep it absent and unchanged unless captured DOM evidence proves a resource script cannot solve the issue, then add it to plan execution scope before editing and document why.</action>
  <verify>Run `cd frontend; npm test -- src/lib/keycloakRegistrationValidation.test.ts`, the config script, and live empty-submit/edit/re-submit checks for all six fields on Keycloak 26.</verify>
  <acceptance_criteria>German Team4s surfaces are visible; the focused test is discovered under `frontend/src/**`; only the edited field loses stale state; current server errors reappear on submit; `infra/keycloak/themes/team4s/login/register.ftl` remains absent/unchanged unless the documented DOM evidence requires that exact minimal fallback.</acceptance_criteria>
</task>
</tasks>

<verification>
- `docker compose config`
- `powershell -ExecutionPolicy Bypass -File scripts/verify-keycloak-config.ps1`
- `cd frontend; npm test -- src/lib/keycloakRegistrationValidation.test.ts`
- Fresh and existing-volume Account Console/UI checks; `git diff --check`
</verification>

<success_criteria>
- D-11 through D-16 and D-21 through D-22 are implemented without importing deferred production hardening.
- Account Console, German branding and isolated validation work reproducibly on Keycloak 26.
</success_criteria>
