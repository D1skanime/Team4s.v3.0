---
phase: 104
plan: 06
type: verification
wave: 6
depends_on: [104-05]
files_modified:
  - .planning/phases/104-registrierungs-login-und-account-onboarding-hardening/104-UAT.md
autonomous: false
requirements: [P104-UAT-1]
must_haves:
  truths:
    - "Live UAT verifies only visible consequences; automated provisioning/backend tests are the mandatory evidence for absent DB role/member/membership/contribution/project rows."
    - "Refresh-token resilience has mandatory deterministic automated evidence; live UAT covers natural expiry only with a documented finite-lifespan precondition and no storage/cookie manipulation."
    - "The only address-bar exception is the explicit /me/contributions resilience check."
---

<objective>
Prove the integrated anonymous, account-only, eligible-Member, Account Console, refresh and mobile experience through the shared in-app browser, starting from the homepage.
</objective>

<tasks>
<task type="checkpoint:human-verify">
  <name>Task 1: Run registration, auto-login and normal account-only UAT</name>
  <read_first>
    <file>AGENTS.md</file><file>104-CONTEXT.md</file><file>104-RESEARCH.md</file>
    <file>docs/frontend/auth-api-client.md</file><file>docs/operations/keycloak-auth-foundation-phase43.md</file>
  </read_first>
  <files>.planning/phases/104-registrierungs-login-und-account-onboarding-hardening/104-UAT.md</files>
  <action>Start exactly at `http://127.0.0.1:3000/` and use only visible UI. Record click, expected/actual result, intermediate state, screenshot/evidence and defect. Verify visible German global-Button login/register entry and German Team4s Keycloak registration/reset surfaces. Empty-submit then edit username, password, confirmation, email, first name and last name separately; only that stale field error changes and current server results return on submit. Register with local password `123`: it auto-signs-in, lands on `/me/profile`, and visibly shows the neutral active-account experience plus exactly `Dein Team4s-Konto wurde erstellt. Du bist jetzt angemeldet.` once until dismiss/page leave. Visible UI must make no Member/project claim, must omit `Meine Projekte`, and must present `Warst du als Fansubber aktiv?` as optional/unobtrusive. Cross-reference the mandatory automated provisioning/backend tests from Plans 01 and 04 as the evidence that registration created no DB global role, Member, membership, contribution or project rows; do not claim the UI proves database internals. Logout and normal re-login must not replay the notice or show contradictory/blank states.</action>
  <verify>The route is reproducible from the homepage without address-bar shortcuts, API/devtools calls or state injection; failures are blocking UAT gaps.</verify>
  <acceptance_criteria>D-01 through D-07 and visible consequences of D-10 through D-15 and D-18 through D-23 pass; automated backend/provisioning evidence proves absence of created authorization/domain rows; no password policy, lockout or email verification is claimed or introduced.</acceptance_criteria>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Verify authorization resilience, Account Console, eligible Member and mobile behavior</name>
  <read_first>
    <file>.planning/phases/104-registrierungs-login-und-account-onboarding-hardening/104-UAT.md</file>
    <file>frontend/src/app/me/contributions/page.tsx</file><file>frontend/src/components/layout/AppShell.tsx</file>
    <file>frontend/src/app/me/profile/components/AccountSecurityCard.tsx</file>
  </read_first>
  <files>.planning/phases/104-registrierungs-login-und-account-onboarding-hardening/104-UAT.md</files>
  <action>Continue normal journeys from the homepage through visible UI. The sole exception is explicitly typing `/me/contributions` in the address bar: authenticated account-only and verified-Member-without-assignment users redirect to `/me/profile`; anonymous remains login-gated; a verified Member with at least one real assignment sees `Meine Projekte` and the dashboard. Treat the deterministic `api.auth-refresh` and shell/session tests from Plan 03 as mandatory evidence for absent/expired access token plus valid refresh token. Live UI may additionally observe natural access-token expiry only when the operator documents before the run a finite Keycloak access-token lifespan, the start time and expected expiry window, then waits and navigates normally until the visible refresh consequence occurs. If that precondition is not configured, record the automated evidence and mark natural-expiry live observation not applicable; never inspect or mutate browser storage, cookies or tokens and never claim the UI manufactured the state. From Mein Account open Accountdaten verwalten in a new tab, verify no 403 and manage name/email/password in the German Account Console, then return and verify existing account refresh. Confirm `Account & Sicherheit` is absent. On mobile, first tap navigates/closes the drawer and logout fires once with pending state. Record empty pages/errors; headless evidence cannot replace live UAT for the other visible journeys.</action>
  <verify>Cross-check visible outcomes with automated results/logs only after UI observation; label the address-bar exception as route resilience, not discoverability.</verify>
  <acceptance_criteria>D-08, D-09, D-16 through D-19 and D-24 pass visibly; D-20 has mandatory deterministic automated evidence and optional honest natural-expiry UI evidence only under the documented precondition; Account Console has no 403; eligibility requires a real assignment; no false login, empty page or first-tap failure remains.</acceptance_criteria>
</task>
</tasks>

<verification>
- Run every focused test/check named in Plans 01-05, frontend typecheck/lint, relevant Go tests, Keycloak assertions, `docker compose config`, and `git diff --check`.
- Attach the Plan-01/04 automated provisioning/backend results proving no automatic DB role, Member, membership, contribution or project rows, and Plan-03 deterministic refresh-only session results; keep them distinct from visible UI observations.
- Complete `104-UAT.md` with D-01 through D-24 pass/fail evidence and exact visible navigation from the homepage.
- Confirm deferred password/lockout/email uniqueness/verification and unrelated public-copy/demo-data work were not implemented.
</verification>

<success_criteria>
- Visible registration, login, account-only, eligible-Member, Account Console, refresh and mobile flows pass.
- Any failure becomes a concrete gap; API or hidden-route tests do not substitute for UI evidence.
</success_criteria>
