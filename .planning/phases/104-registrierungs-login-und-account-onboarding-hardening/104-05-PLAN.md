---
phase: 104
plan: 05
type: implementation
wave: 5
depends_on: [104-04]
files_modified:
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/components/layout/AppShell.test.tsx
  - frontend/src/components/layout/AppShell.module.css
autonomous: true
requirements: [P104-NAV-1]
must_haves:
  truths:
    - "D-17: navigation contains only Mein Account; Account & Sicherheit is removed."
    - "D-24: drawer navigation and logout react on the first tap, close immediately, and expose pending state."
---

<objective>
Remove duplicate Account navigation and make mobile drawer link navigation, route changes and logout deterministic on the first interaction without weakening focus or keyboard behavior.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate Account navigation and centralize drawer close-on-navigation</name>
  <read_first>
    <file>AGENTS.md</file>
    <file>docs/frontend/ui-system.md</file>
    <file>docs/agent-guidelines-ui.md</file>
    <file>frontend/src/components/layout/AppShell.tsx</file>
    <file>frontend/src/components/layout/AppShell.test.tsx</file>
    <file>frontend/src/components/layout/AppShell.module.css</file>
  </read_first>
  <files>frontend/src/components/layout/AppShell.tsx, frontend/src/components/layout/AppShell.test.tsx, frontend/src/components/layout/AppShell.module.css</files>
  <action>Keep only the canonical visible `Mein Account` destination and remove `Account & Sicherheit`. Add one shared NavItem activation seam that closes the drawer before internal navigation; also close when `currentPath` changes. Do not alter the `has_project_assignments` eligibility from Plan 04 or duplicate handlers per link. Preserve Escape, backdrop, focus trap, focus restoration and desktop navigation behavior.</action>
  <verify>Add pointer/click tests proving first activation navigates and closes, currentPath change closes, Account link occurs once, and Escape/backdrop/focus tests remain green.</verify>
  <acceptance_criteria>One Account entry exists; no first-tap consumption; route changes cannot leave stale drawer open; accessibility behavior is preserved.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Make logout a single, observable transition</name>
  <read_first>
    <file>frontend/src/components/layout/AppShell.tsx</file>
    <file>frontend/src/lib/useAuthSession.ts</file>
    <file>frontend/src/lib/api.ts</file>
    <file>frontend/src/lib/keycloakAuth.ts</file>
  </read_first>
  <files>frontend/src/components/layout/AppShell.tsx, frontend/src/components/layout/AppShell.test.tsx</files>
  <action>Use the existing `useLogoutAuthSession` seam exactly once per action, disable repeated activation, close the drawer immediately, show a German logging-out state, and navigate only at the lifecycle point consistent with local cleanup and Keycloak logout initiation. Do not directly call Keycloak logout or read refresh tokens from AppShell.</action>
  <verify>Test rapid double activation invokes logout once, drawer closes, transition copy appears, successful logout navigates predictably and failure follows existing safe cleanup/error semantics.</verify>
  <acceptance_criteria>Logout never appears inert or fires twice; auth ownership stays centralized; mobile and keyboard interaction remain deterministic.</acceptance_criteria>
</task>

</tasks>

<verification>
- Focused AppShell tests including touch/pointer, route-change, focus and logout cases.
- Frontend typecheck/lint and no-token-boundary test.
- `git diff --check`.
</verification>

<success_criteria>
- Mobile navigation works on first tap and closes reliably.
- Account navigation is not duplicated.
- Logout is single-shot, understandable and centrally owned.
</success_criteria>
