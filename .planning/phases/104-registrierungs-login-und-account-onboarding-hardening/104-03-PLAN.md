---
phase: 104
plan: 03
type: implementation
wave: 3
depends_on: [104-02]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/lib/api.auth-cookie.test.ts
  - docs/frontend/auth-api-client.md
  - frontend/src/components/layout/AppShellClientWrapper.tsx
  - frontend/src/components/layout/AppShellClientWrapper.test.tsx
  - frontend/src/app/me/profile/page.tsx
  - frontend/src/app/me/profile/page.test.tsx
  - frontend/src/lib/api.auth-refresh.test.ts
autonomous: true
requirements: [P104-AUTH-2]
must_haves:
  truths:
    - "D-05 D-07: account-only is a normal state and the existing Fansubber claim flow is an unobtrusive optional section."
    - "D-18 D-19: auth/profile initialization is neutral; active-session profile failure offers Erneut versuchen and Abmelden, never Anmelden."
    - "D-20: access-token absent/expired plus valid refresh token remains active and refreshes only through the central client."
    - "HTTPS browser contexts write central auth cookies with Secure, while local http://127.0.0.1 remains functional without Secure; components never own cookies or tokens."
    - "D-03 D-04: profile displays the exact completion notice once, until dismiss or page leave."
---

<objective>
Converge shell/profile hydration into consistent session states and make `/me/profile` a complete account-only destination with trusted one-shot confirmation and optional Fansubber linking.
</objective>

<tasks>
<task type="auto">
  <name>Task 0: Harden central browser auth cookies by runtime protocol</name>
  <read_first>
    <file>AGENTS.md</file><file>104-CONTEXT.md</file><file>docs/frontend/auth-api-client.md</file>
    <file>frontend/src/lib/api.ts</file><file>frontend/src/lib/api.auth-refresh.test.ts</file><file>frontend/src/lib/api.no-token-boundary.test.ts</file>
  </read_first>
  <files>frontend/src/lib/api.ts, frontend/src/lib/api.auth-cookie.test.ts, docs/frontend/auth-api-client.md</files>
  <action>Keep cookie ownership inside the existing central `api.ts` auth/session seam. Make every central auth-cookie write and deletion append `Secure` when the actual browser context uses `window.location.protocol === 'https:'`; do not append it for plain `http:` including `http://127.0.0.1`, so local development continues to work. Apply the same protocol decision consistently to access-token, refresh-token and display-name cookies without introducing direct page/component cookie access, an httpOnly claim, BFF redesign or duplicated auth helper. Add deterministic jsdom unit coverage in `frontend/src/lib/api.auth-cookie.test.ts` that captures cookie assignments and proves HTTPS includes `Secure`, HTTP 127.0.0.1 omits it, existing `Path=/`, `Max-Age` and `SameSite=Lax` remain, and clear/logout writes follow the same rule. Document the browser-readable-cookie boundary and protocol-aware Secure behavior in the central auth guide.</action>
  <verify>`cd frontend; npm test -- src/lib/api.auth-cookie.test.ts src/lib/api.no-token-boundary.test.ts` deterministically covers HTTPS, local HTTP, normal writes and deletion; `rg "document.cookie" frontend/src/app frontend/src/components` finds no new ownership.</verify>
  <acceptance_criteria>HTTPS browser auth cookies carry Secure; local `http://127.0.0.1` auth remains usable without Secure; the central API seam remains the sole browser-cookie owner and no httpOnly/BFF redesign is implied.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 1: Project explicit initialization and profile-error states</name>
  <read_first>
    <file>AGENTS.md</file><file>104-CONTEXT.md</file><file>docs/frontend/auth-api-client.md</file>
    <file>frontend/src/lib/useAuthSession.ts</file><file>frontend/src/lib/api.ts</file>
    <file>frontend/src/components/layout/AppShellClientWrapper.tsx</file><file>frontend/src/components/layout/AppShellClientWrapper.test.tsx</file>
  </read_first>
  <files>frontend/src/components/layout/AppShellClientWrapper.tsx, frontend/src/components/layout/AppShellClientWrapper.test.tsx, frontend/src/lib/api.auth-refresh.test.ts</files>
  <action>Model client-uninitialized, anonymous, authenticated-profile-loading, authenticated-profile-error and ready states around `useAuthSession()` plus `getOwnProfile()`. Treat `hasAccessToken || hasRefreshToken` as active session. Render/pass one neutral loading projection until the account aggregate settles. For active-session profile errors expose German retry and the existing centralized logout action; never render login. Add the missing-access/expired-access plus valid-refresh regression and assert refresh occurs through `api.ts` only.</action>
  <verify>`cd frontend; npm test -- src/components/layout/AppShellClientWrapper.test.tsx src/lib/api.auth-refresh.test.ts` covers every state and no contradictory account/login UI.</verify>
  <acceptance_criteria>Shell and account page agree on auth state; refresh-only never looks logged out; retry and logout recover profile failure without direct token/Keycloak access.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Finish the neutral account profile and registration notice</name>
  <read_first>
    <file>frontend/src/app/me/profile/page.tsx</file><file>frontend/src/app/me/profile/page.test.tsx</file>
    <file>frontend/src/app/me/profile/components/MemberClaimSection.tsx</file><file>frontend/src/app/me/profile/components/ClaimStatusCard.tsx</file>
    <file>frontend/src/app/me/profile/components/AccountSecurityCard.tsx</file><file>frontend/src/lib/registrationCompletion.ts</file>
  </read_first>
  <files>frontend/src/app/me/profile/page.tsx, frontend/src/app/me/profile/page.test.tsx</files>
  <action>After own-profile hydration, consume the trusted completion marker and display exactly `Dein Team4s-Konto wurde erstellt. Du bist jetzt angemeldet.` as an accessible dismissible status. Keep it until dismiss or page leave; never recreate on remount/query/reload. Present account data as complete without Member assumptions. Place existing claim/search components below account data in an unobtrusive `Warst du als Fansubber aktiv?` section; no new API or mandatory onboarding. Keep the external Account Console link in `AccountSecurityCard` opening a new tab.</action>
  <verify>`cd frontend; npm test -- src/app/me/profile/page.test.tsx` covers exact copy, one-shot/dismiss/page-leave/spoof behavior, account-only rendering, optional claim section, retry/logout and Account Console target.</verify>
  <acceptance_criteria>The registration confirmation is accurate and one-shot; a normal account is useful; Fansubber linking is optional; active-session failures never ask the user to log in again.</acceptance_criteria>
</task>
</tasks>

<verification>
- Focused cookie, wrapper, profile and refresh tests
- `cd frontend; npm test -- src/lib/api.auth-cookie.test.ts src/lib/api.auth-refresh.test.ts src/lib/api.no-token-boundary.test.ts`
- `cd frontend; npm run typecheck; npm run lint; npm test -- src/lib/api.no-token-boundary.test.ts`
- `git diff --check`
</verification>

<success_criteria>D-03 through D-07 and D-18 through D-20 hold across callback, refresh, shell and profile.</success_criteria>
