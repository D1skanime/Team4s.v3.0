---
phase: 40-text-und-notizsystem-fuer-fansub-plattform
plan_id: 40-07
title: Frontend — TypeScript-Typen und API-Client-Funktionen für alle 4 Note-Typen
wave: 3
depends_on:
  - 40-05
  - 40-06
estimated_minutes: 30
autonomous: true
files_modified:
  - frontend/src/types/fansubNotes.ts
  - frontend/src/types/releaseVersionNotes.ts
  - frontend/src/lib/api.ts

must_haves:
  truths:
    - "TypeScript-Typen für alle 4 Note-Entitäten und ihre Request-Shapes existieren"
    - "API-Funktionen für alle Endpunkte aus Plan 40-05 und 40-06 sind in api.ts implementiert"
    - "Bulk-Save-Funktion akzeptiert BulkNoteInput[] und gibt ReleaseVersionNote[] zurück"
    - "Alle API-Funktionen gehen durch den bestehenden api.ts-Client (kein direktes fetch in Komponenten)"
  artifacts:
    - path: frontend/src/types/fansubNotes.ts
      provides: TypeScript-Typen für FansubGroupNote, MemberGroupStory, AnimeFansubProjectNote
      exports: ["FansubGroupNote", "MemberGroupStory", "AnimeFansubProjectNote", "CreateFansubGroupNoteRequest", "UpdateFansubGroupNoteRequest"]
    - path: frontend/src/types/releaseVersionNotes.ts
      provides: TypeScript-Typen für ReleaseVersionNote, MemberRoleForVersion, BulkNoteInput
      exports: ["ReleaseVersionNote", "MemberRoleForVersion", "BulkNoteInput"]
  key_links:
    - from: frontend/src/lib/api.ts
      to: /api/v1/admin/fansubs/:id/notes (und andere Endpunkte)
      via: Fetch-Calls in neuen API-Hilfsfunktionen
---

## Goal

TypeScript-Typen und API-Client-Funktionen für alle 4 Note-Typen erstellen. Diese werden von den UI-Komponenten in den Plänen 40-08, 40-09 und 40-10 importiert. Damit entfällt jede Exploration in den Komponenten-Plänen — die Interfaces sind hier definiert.

## Context

- `RESEARCH.md` — Frontend-API-Client-Pattern, TypeScript-Typen-Struktur
- `40-CONTEXT.md` — Feldliste aller 4 Note-Typen, Bulk-Save-Anforderung
- `frontend/src/lib/api.ts` — bestehende API-Client-Hilfsfunktionen (Muster für neue Funktionen)
- `frontend/src/types/` — bestehende Type-Dateien als Stil-Referenz
- Plan 40-05 SUMMARY — API-Endpunkte für Fansub-Notes
- Plan 40-06 SUMMARY — API-Endpunkte für release_version_notes

## Tasks

### Task 1: frontend/src/types/fansubNotes.ts

```typescript
export interface FansubGroupNote {
  id: number;
  fansubGroupId: number;
  title: string;
  bodyMarkdown: string;
  bodyHtml: string;
  visibility: 'public' | 'internal';
  status: 'draft' | 'published' | 'archived' | 'deleted';
  sortOrder: number;
  createdByUserId: number | null;
  updatedByUserId: number | null;
  createdAt: string;
  updatedAt: string | null;
  deletedAt: string | null;
}

export interface MemberGroupStory {
  id: number;
  fansubGroupId: number;
  memberId: number;
  roleId: number | null;
  title: string;
  bodyMarkdown: string;
  bodyHtml: string;
  visibility: 'public' | 'internal';
  status: 'draft' | 'published' | 'archived' | 'deleted';
  sortOrder: number;
  createdByUserId: number | null;
  updatedByUserId: number | null;
  createdAt: string;
  updatedAt: string | null;
  deletedAt: string | null;
}

export interface AnimeFansubProjectNote {
  id: number;
  animeId: number;
  fansubGroupId: number;
  title: string;
  bodyMarkdown: string;
  bodyHtml: string;
  visibility: 'public' | 'internal';
  status: 'draft' | 'published' | 'archived' | 'deleted';
  sortOrder: number;
  createdByUserId: number | null;
  updatedByUserId: number | null;
  createdAt: string;
  updatedAt: string | null;
  deletedAt: string | null;
}

export interface CreateFansubGroupNoteRequest {
  title: string;
  bodyMarkdown: string;
  visibility: 'public' | 'internal';
  status: 'draft' | 'published' | 'archived' | 'deleted';
  sortOrder?: number;
}

export interface UpdateFansubGroupNoteRequest {
  title?: string;
  bodyMarkdown?: string;
  visibility?: 'public' | 'internal';
  status?: 'draft' | 'published' | 'archived' | 'deleted';
  sortOrder?: number;
}

export interface CreateMemberGroupStoryRequest {
  memberId: number;
  roleId?: number | null;
  title: string;
  bodyMarkdown: string;
  visibility: 'public' | 'internal';
  status: 'draft' | 'published' | 'archived' | 'deleted';
  sortOrder?: number;
}

export interface UpdateMemberGroupStoryRequest {
  memberId?: number;
  roleId?: number | null;
  title?: string;
  bodyMarkdown?: string;
  visibility?: 'public' | 'internal';
  status?: 'draft' | 'published' | 'archived' | 'deleted';
  sortOrder?: number;
}

export interface UpsertAnimeFansubProjectNoteRequest {
  title?: string;
  bodyMarkdown: string;
  visibility: 'public' | 'internal';
  status: 'draft' | 'published' | 'archived' | 'deleted';
  sortOrder?: number;
}
```

### Task 2: frontend/src/types/releaseVersionNotes.ts

```typescript
export interface ReleaseVersionNote {
  id: number;
  releaseVersionId: number;
  memberId: number;
  roleId: number;
  title: string | null;
  bodyMarkdown: string;
  bodyHtml: string;
  visibility: 'public' | 'internal';
  status: 'draft' | 'published' | 'archived' | 'deleted';
  sortOrder: number;
  createdByUserId: number | null;
  updatedByUserId: number | null;
  createdAt: string;
  updatedAt: string | null;
  deletedAt: string | null;
}

export interface MemberRoleForVersion {
  memberId: number;
  memberName: string;
  roleId: number;
  roleName: string;
  roleLabel: string;
}

export interface BulkNoteInput {
  id: number;          // 0 = neu erstellen
  memberId: number;
  roleId: number;
  title?: string | null;
  bodyMarkdown: string;
  visibility: 'public' | 'internal';
  status: 'draft' | 'published' | 'archived' | 'deleted';
  sortOrder?: number;
}

export interface BulkUpsertReleaseVersionNotesRequest {
  notes: BulkNoteInput[];
}
```

### Task 3: API-Client-Funktionen in frontend/src/lib/api.ts

Füge die folgenden Funktionen in `api.ts` hinzu. Verwende die bestehenden Hilfsfunktionen (z.B. `apiFetch`, `getApiBase` o.ä. — bestehende Muster lesen und übernehmen, nicht neu erfinden).

**Fansub Group Notes:**
```typescript
export async function listFansubGroupNotes(fansubId: number): Promise<FansubGroupNote[]>
export async function createFansubGroupNote(fansubId: number, data: CreateFansubGroupNoteRequest): Promise<FansubGroupNote>
export async function updateFansubGroupNote(fansubId: number, noteId: number, data: UpdateFansubGroupNoteRequest): Promise<FansubGroupNote>
export async function deleteFansubGroupNote(fansubId: number, noteId: number): Promise<void>
```

**Member Group Stories:**
```typescript
export async function listMemberGroupStories(fansubId: number): Promise<MemberGroupStory[]>
export async function createMemberGroupStory(fansubId: number, data: CreateMemberGroupStoryRequest): Promise<MemberGroupStory>
export async function updateMemberGroupStory(fansubId: number, storyId: number, data: UpdateMemberGroupStoryRequest): Promise<MemberGroupStory>
export async function deleteMemberGroupStory(fansubId: number, storyId: number): Promise<void>
```

**Anime Fansub Project Notes:**
```typescript
export async function getAnimeFansubProjectNote(fansubId: number, animeId: number): Promise<AnimeFansubProjectNote | null>
export async function upsertAnimeFansubProjectNote(fansubId: number, animeId: number, data: UpsertAnimeFansubProjectNoteRequest): Promise<AnimeFansubProjectNote>
export async function deleteAnimeFansubProjectNote(fansubId: number, animeId: number, noteId: number): Promise<void>
```

**Release Version Notes:**
```typescript
export async function listReleaseVersionNotes(versionId: number): Promise<ReleaseVersionNote[]>
export async function getMemberRolesForVersion(versionId: number): Promise<MemberRoleForVersion[]>
export async function bulkUpsertReleaseVersionNotes(versionId: number, data: BulkUpsertReleaseVersionNotesRequest): Promise<ReleaseVersionNote[]>
export async function deleteReleaseVersionNote(versionId: number, noteId: number): Promise<void>
```

Für jede Funktion: Endpunkt-URL entspricht den Routen aus Plan 40-05 und 40-06 SUMMARY. Response-Extraktion: `response.data` (bestehende Konvention). Fehlerbehandlung: bestehende Error-Handling-Muster aus `api.ts` übernehmen.

## Verification

TypeScript-Kompilierung:

```bash
cd frontend && npx tsc --noEmit
```

Linting:

```bash
cd frontend && npx eslint src/types/fansubNotes.ts src/types/releaseVersionNotes.ts src/lib/api.ts --max-warnings 0
```

Import-Prüfung (keine zirkulären Imports):

```bash
cd frontend && npx tsc --noEmit 2>&1 | grep -i "circular\|error"
```

## Output

Nach Abschluss `.planning/phases/40-text-und-notizsystem-fuer-fansub-plattform/40-07-SUMMARY.md` erstellen.
