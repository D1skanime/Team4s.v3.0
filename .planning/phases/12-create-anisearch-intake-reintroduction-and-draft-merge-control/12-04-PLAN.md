---
phase: 12-create-anisearch-intake-reintroduction-and-draft-merge-control
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/cmd/server/admin_routes.go
  - backend/internal/handlers/admin_content_anime_enrichment_create.go
  - backend/internal/handlers/admin_content_test.go
  - frontend/src/lib/api.admin-anime.test.ts
autonomous: true
gap_closure: true
requirements: [ENR-02, ENR-03]
must_haves:
  truths:
    - "The create AniSearch exact-ID helper no longer 404s because the backend registers a real create-route enrichment endpoint."
    - "The create-route AniSearch endpoint returns the same draft-or-redirect envelope the frontend already consumes."
    - "Duplicate AniSearch IDs in create redirect to the existing edit route through the live backend seam, not only through frontend-only test doubles."
  artifacts:
    - path: "backend/cmd/server/admin_routes.go"
      provides: "Registered create-route AniSearch enrichment endpoint"
      contains: "/admin/anime/enrichment/anisearch"
    - path: "backend/internal/handlers/admin_content_anime_enrichment_create.go"
      provides: "Create-route AniSearch enrichment handler using the existing create enrichment service"
      contains: "LoadAnimeCreateAniSearchEnrichment"
    - path: "backend/internal/handlers/admin_content_test.go"
      provides: "Backend regression coverage for create AniSearch success and duplicate redirect"
      contains: "CreateAniSearch"
    - path: "frontend/src/lib/api.admin-anime.test.ts"
      provides: "Frontend regression that locks the exact backend seam path"
      contains: "/api/v1/admin/anime/enrichment/anisearch"
  key_links:
    - from: "backend/cmd/server/admin_routes.go"
      to: "backend/internal/handlers/admin_content_anime_enrichment_create.go"
      via: "registered create AniSearch POST route"
      pattern: "LoadAnimeCreateAniSearchEnrichment"
    - from: "backend/internal/handlers/admin_content_anime_enrichment_create.go"
      to: "backend/internal/services/anime_create_enrichment.go"
      via: "existing create enrichment service and duplicate redirect result"
      pattern: "Enrich("
    - from: "frontend/src/lib/api.admin-anime.test.ts"
      to: "backend/cmd/server/admin_routes.go"
      via: "exact create AniSearch endpoint path"
      pattern: "/admin/anime/enrichment/anisearch"
---

<objective>
Close the diagnosed Phase 12 create AniSearch 404 by wiring the missing backend create-route enrichment endpoint to the already-built AniSearch create service and locking the seam with backend/frontend regressions.

Purpose: The create AniSearch UI and controller are live, but browser UAT proved the backend route is missing. This gap closure restores the intended create-time enrichment path without reopening the broader Phase 12 UI work.
Output: Registered backend create AniSearch route, handler-level success/duplicate coverage, and frontend seam regression proving the exact endpoint remains stable.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-create-anisearch-intake-reintroduction-and-draft-merge-control/12-VERIFICATION.md
@.planning/phases/12-create-anisearch-intake-reintroduction-and-draft-merge-control/12-HUMAN-UAT.md
@.planning/phases/12-create-anisearch-intake-reintroduction-and-draft-merge-control/12-01-SUMMARY.md
@.planning/phases/12-create-anisearch-intake-reintroduction-and-draft-merge-control/12-02-SUMMARY.md
@.planning/phases/12-create-anisearch-intake-reintroduction-and-draft-merge-control/12-03-SUMMARY.md
@backend/cmd/server/admin_routes.go
@backend/internal/handlers/admin_content_anime_enrichment_edit.go
@backend/internal/handlers/admin_content_anime.go
@backend/internal/handlers/admin_content_test.go
@backend/internal/services/anime_create_enrichment.go
@backend/internal/models/admin_content.go
@frontend/src/lib/api/admin-anime-intake.ts
@frontend/src/lib/api.admin-anime.test.ts

<interfaces>
From `backend/internal/services/anime_create_enrichment.go`:
```go
func (s *AnimeCreateEnrichmentService) Enrich(
  ctx context.Context,
  req models.AdminAnimeAniSearchEnrichmentRequest,
) (any, error)
```

From `backend/internal/models/admin_content.go`:
```go
type AdminAnimeAniSearchEnrichmentRequest struct {
  AniSearchID string                       `json:"anisearch_id"`
  Draft       AdminAnimeCreateDraftPayload `json:"draft"`
}

type AdminAnimeAniSearchEnrichmentDraftResult struct { ... }
type AdminAnimeAniSearchEnrichmentRedirectResult struct { ... }
```

From `frontend/src/lib/api/admin-anime-intake.ts`:
```ts
export async function loadAdminAnimeCreateAniSearchDraft(
  payload: AdminAnimeAniSearchCreateRequest,
  authToken?: string,
): Promise<AdminAnimeAniSearchCreateResponse>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add backend create-route AniSearch endpoint regressions before wiring the route</name>
  <files>backend/internal/handlers/admin_content_test.go, frontend/src/lib/api.admin-anime.test.ts</files>
  <read_first>@.planning/phases/12-create-anisearch-intake-reintroduction-and-draft-merge-control/12-HUMAN-UAT.md @backend/internal/handlers/admin_content_test.go @frontend/src/lib/api.admin-anime.test.ts</read_first>
  <acceptance_criteria>`admin_content_test.go` contains backend-facing tests for create AniSearch success and duplicate redirect through a real HTTP route shape; `api.admin-anime.test.ts` keeps asserting the frontend helper calls `/api/v1/admin/anime/enrichment/anisearch`; the new backend tests fail until the route/handler exists.</acceptance_criteria>
  <behavior>
    - Test 1: `POST /api/v1/admin/anime/enrichment/anisearch` with a valid AniSearch request returns `200` and a `data.mode='draft'` payload.
    - Test 2: The same endpoint returns `409` with `existing_anime_id`, `existing_title`, and `redirect_path` when the AniSearch source already exists.
    - Test 3: The frontend helper regression continues to lock the exact create seam path so the 404 cannot silently return via path drift.
  </behavior>
  <action>Start from the diagnosed UAT failure, not from the happy-path UI. Extend the backend handler test suite with route-level coverage for the missing create AniSearch POST endpoint, using the same response envelope conventions as the edit endpoint and existing create-service result types. Keep the frontend API regression in place so the seam remains exact-ID create enrichment on `/api/v1/admin/anime/enrichment/anisearch`. Do not broaden this into browser tests or controller refactors; the purpose is to prove the backend seam itself exists and speaks the contract already consumed by the frontend.</action>
  <verify>
    <automated>cd backend && go test ./internal/handlers -run "TestLoadAnimeCreateAniSearchEnrichment" -count=1 && cd ../frontend && npm test -- src/lib/api.admin-anime.test.ts</automated>
  </verify>
  <done>The regression suite captures the exact create AniSearch backend seam and proves the current 404 would fail before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Register and implement the create AniSearch backend handler using the existing enrichment service</name>
  <files>backend/cmd/server/admin_routes.go, backend/internal/handlers/admin_content_anime_enrichment_create.go, backend/internal/handlers/admin_content_test.go</files>
  <read_first>@backend/cmd/server/admin_routes.go @backend/internal/handlers/admin_content_anime_enrichment_edit.go @backend/internal/services/anime_create_enrichment.go @backend/internal/models/admin_content.go</read_first>
  <acceptance_criteria>`admin_routes.go` registers `POST /admin/anime/enrichment/anisearch`; the new handler validates auth and request shape, delegates to the existing create enrichment service, returns `200 { data: draftResult }` for success and `409 { data: redirectResult }` for duplicates, and the backend regression tests pass without altering the edit-route contract.</acceptance_criteria>
  <behavior>
    - Success responses keep the existing `mode: 'draft'` create payload shape with `manual_fields_kept`, `filled_fields`, `filled_assets`, and `provider`.
    - Duplicate responses surface the redirect envelope expected by the frontend helper and controller.
    - Invalid request bodies or missing AniSearch IDs still return normal validation failures instead of silent empty drafts.
  </behavior>
  <action>Create a dedicated create-route AniSearch handler alongside the existing edit-route handler so the seam is explicit and maintainable. Reuse the already-shipped create enrichment service (`Enrich`) and existing models instead of inventing new DTOs. Register the route in `admin_routes.go`, mirror the project's auth/error-envelope conventions, and keep duplicate handling aligned with the edit route's response envelope while preserving the create-specific draft result shape. Do not refactor the service, change the frontend endpoint path, or mix this work with broader relation-persistence changes; this gap closes only the missing backend route and its contract proof.</action>
  <verify>
    <automated>cd backend && go test ./internal/handlers ./internal/services -count=1 && cd ../frontend && npm test -- src/lib/api.admin-anime.test.ts</automated>
  </verify>
  <done>The create AniSearch POST seam exists end-to-end, returns the contract Phase 12 already expects, and eliminates the live 404 that blocked browser verification.</done>
</task>

</tasks>

<verification>
Re-run the diagnosed Phase 12 UAT gap: `POST /api/v1/admin/anime/enrichment/anisearch` must no longer return `404`, valid AniSearch loads must return the draft envelope, and duplicate AniSearch IDs must return redirect metadata through the live backend seam.
</verification>

<success_criteria>
- The create AniSearch backend route exists at the exact path the frontend already calls.
- Success and duplicate responses use the existing create AniSearch draft/redirect contract without frontend path changes.
- Backend regressions fail if the route disappears or stops returning the expected envelope.
</success_criteria>

<output>
After completion, create `.planning/phases/12-create-anisearch-intake-reintroduction-and-draft-merge-control/12-04-SUMMARY.md`
</output>
