---
phase: 12-create-anisearch-intake-reintroduction-and-draft-merge-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/admin.ts
  - frontend/src/lib/api/admin-anime-intake.ts
  - frontend/src/lib/api.admin-anime.test.ts
  - frontend/src/app/admin/anime/create/createPageHelpers.ts
  - frontend/src/app/admin/anime/create/createAniSearchSummary.ts
  - frontend/src/app/admin/anime/create/createAniSearchSummary.test.ts
  - frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts
autonomous: true
requirements: [ENR-01, ENR-02]
must_haves:
  truths:
    - "The create route has one exact-ID AniSearch draft-load contract and no AniSearch search or browse surface."
    - "Create-side AniSearch results can be represented as either a draft update or a duplicate redirect to edit."
    - "The controller has executable regression coverage for the locked merge rules before UI wiring begins."
  artifacts:
    - path: "frontend/src/types/admin.ts"
      provides: "Create-side AniSearch request and response DTOs"
      contains: "AdminAnimeAniSearchCreate"
    - path: "frontend/src/lib/api/admin-anime-intake.ts"
      provides: "Exact-ID create AniSearch API helper"
      contains: "loadAdminAnimeCreateAniSearchDraft"
    - path: "frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts"
      provides: "Wave 0 create-controller precedence regressions"
      contains: "manual > AniSearch > Jellyfin"
    - path: "frontend/src/app/admin/anime/create/createAniSearchSummary.ts"
      provides: "Reusable summary builder for unsaved AniSearch draft feedback"
      contains: "not saved yet"
  key_links:
    - from: "frontend/src/lib/api/admin-anime-intake.ts"
      to: "frontend/src/types/admin.ts"
      via: "typed create AniSearch request and result parsing"
      pattern: "AdminAnimeAniSearchCreate"
    - from: "frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts"
      to: "frontend/src/app/admin/anime/create/createPageHelpers.ts"
      via: "controller-level precedence and redirect state assertions"
      pattern: "AniSearch"
---

<objective>
Define the create-side AniSearch DTOs, API helper, summary helper seam, and Wave 0 regression scaffolding before controller and UI implementation.

Purpose: Phase 12 is primarily a frontend reintegration. Executors need stable create-side contracts and tests that pin D-01 through D-18 before mutating controller state.
Output: Typed create AniSearch helper surface, summary helper seam, and failing-or-red-then-green create regression coverage for precedence, duplicate redirects, and unsaved summary rules.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-create-anisearch-intake-reintroduction-and-draft-merge-control/12-CONTEXT.md
@.planning/phases/12-create-anisearch-intake-reintroduction-and-draft-merge-control/12-RESEARCH.md
@.planning/phases/12-create-anisearch-intake-reintroduction-and-draft-merge-control/12-UI-SPEC.md
@.planning/phases/09-controlled-anisearch-id-enrichment-before-create-with-fill-only-jellysync-follow-up/09-01-PLAN.md
@.planning/phases/09-controlled-anisearch-id-enrichment-before-create-with-fill-only-jellysync-follow-up/09-02-PLAN.md
@.planning/phases/09-controlled-anisearch-id-enrichment-before-create-with-fill-only-jellysync-follow-up/09-UAT.md
@.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-03-SUMMARY.md
@.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-VERIFICATION.md
@frontend/src/types/admin.ts
@frontend/src/lib/api/admin-anime-intake.ts
@frontend/src/lib/api.admin-anime.test.ts
@frontend/src/app/admin/anime/create/createPageHelpers.ts
@frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts

<interfaces>
From `frontend/src/types/admin.ts`:
```ts
export interface AdminAnimeCreateDraftPayload {
  title: string
  title_de?: string
  title_en?: string
  type: AnimeType
  content_type: ContentType
  status: AnimeStatus
  year?: number
  max_episodes?: number
  genre?: string
  description?: string
  cover_image?: string
  source?: string
  folder_name?: string
  alt_titles?: AdminAnimeAltTitle[]
  tags?: string[]
  relations?: AdminAnimeRelation[]
  asset_suggestions?: AdminAnimeCreateDraftAssetSuggestions
}

export interface AdminAnimeCreateAniSearchSummary {
  source?: string
  relations_attempted: number
  relations_applied: number
  relations_skipped_existing: number
  warnings: string[]
}
```

From `frontend/src/lib/api/admin-anime-intake.ts`:
```ts
export async function searchAdminJellyfinIntakeCandidates(...)
export async function previewAdminAnimeFromJellyfinIntake(...)
export async function createAdminAnimeFromJellyfinDraft(...)
```

From `frontend/src/app/admin/anime/create/createPageHelpers.ts`:
```ts
export function buildCreateSuccessMessage(...)
export function resolveSourceActionState(title: string)
export function resolveJellyfinPreviewBaseDraft(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add create AniSearch contracts and Wave 0 regression scaffolds</name>
  <files>frontend/src/types/admin.ts, frontend/src/lib/api.admin-anime.test.ts, frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts, frontend/src/app/admin/anime/create/createAniSearchSummary.test.ts</files>
  <read_first>@frontend/src/types/admin.ts @frontend/src/lib/api.admin-anime.test.ts @frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts @.planning/phases/09-controlled-anisearch-id-enrichment-before-create-with-fill-only-jellysync-follow-up/09-UAT.md</read_first>
  <acceptance_criteria>`frontend/src/types/admin.ts` exports create-side AniSearch DTO names; `frontend/src/lib/api.admin-anime.test.ts`, `frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts`, and `frontend/src/app/admin/anime/create/createAniSearchSummary.test.ts` each contain at least one `AniSearch`-named assertion covering exact-ID load, duplicate redirect, unsaved summary copy, and both load orders for `manual > AniSearch > Jellyfin`.</acceptance_criteria>
  <action>Add TypeScript interfaces for create-side AniSearch request/result variants to `frontend/src/types/admin.ts`, aligned to the existing backend create enrichment seam per D-16 through D-18. Create or extend `frontend/src/lib/api.admin-anime.test.ts` with create AniSearch helper cases for exact-ID-only requests and redirect-vs-draft parsing per D-03, D-10, and D-11. Add `frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts` that pins both load orders from D-07 and D-08 plus the D-09 provisional-text rule. Add `frontend/src/app/admin/anime/create/createAniSearchSummary.test.ts` that asserts D-13 through D-15: updated-fields copy, relation notes, overwritten-Jellyfin note, preserved-manual note, and explicit `nothing is saved yet` wording.</action>
  <verify>
    <automated>cd frontend && npm test -- src/lib/api.admin-anime.test.ts src/app/admin/anime/create/useAdminAnimeCreateController.test.ts src/app/admin/anime/create/createAniSearchSummary.test.ts</automated>
  </verify>
  <done>Create AniSearch contracts and regression files exist, and the tests describe duplicate redirect handling, merge precedence, and unsaved-summary behavior before implementation completes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the create AniSearch helper and summary-builder seams</name>
  <files>frontend/src/lib/api/admin-anime-intake.ts, frontend/src/app/admin/anime/create/createPageHelpers.ts, frontend/src/app/admin/anime/create/createAniSearchSummary.ts, frontend/src/types/admin.ts</files>
  <read_first>@frontend/src/lib/api/admin-anime-intake.ts @frontend/src/app/admin/anime/create/createPageHelpers.ts @frontend/src/types/admin.ts @.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-03-SUMMARY.md</read_first>
  <acceptance_criteria>`frontend/src/lib/api/admin-anime-intake.ts` exports a single create AniSearch load helper targeting the existing exact-ID backend seam; `frontend/src/app/admin/anime/create/createAniSearchSummary.ts` exports reusable summary-formatting helpers that mention updated fields, relation notes, preserved manual values, overwritten Jellyfin values, and unsaved draft state; no new AniSearch search helper is introduced.</acceptance_criteria>
  <action>Add `loadAdminAnimeCreateAniSearchDraft(...)` to `frontend/src/lib/api/admin-anime-intake.ts`, using the existing create enrichment endpoint and `ApiError` parsing style rather than adding a second AniSearch client per D-16 and D-17. Create `frontend/src/app/admin/anime/create/createAniSearchSummary.ts` for draft-time AniSearch feedback text and lightweight summary shaping so later plans can render D-13 through D-15 near the source controls. Keep `createPageHelpers.ts` focused on persisted-create helpers; only export narrow bridge helpers there if the controller needs them. Do not add AniSearch title search, browse endpoints, or a second duplicate policy.</action>
  <verify>
    <automated>cd frontend && npm test -- src/lib/api.admin-anime.test.ts src/app/admin/anime/create/createAniSearchSummary.test.ts</automated>
  </verify>
  <done>The frontend has one typed create AniSearch helper plus a reusable summary builder for unsaved draft feedback, and the regression coverage for exact-ID parsing and summary copy passes.</done>
</task>

</tasks>

<verification>
The create AniSearch seam is contract-stable when the frontend exposes only one exact-ID helper, tests cover redirect/draft variants and load-order precedence, and summary-copy helpers exist before UI markup is introduced.
</verification>

<success_criteria>
- Create AniSearch DTOs and helper exports are present and aligned to the existing backend contract.
- Wave 0 tests exist for duplicate redirects, provisional lookup text, both merge orders, and unsaved summary messaging.
- No AniSearch search/browse helper or alternate duplicate policy is added.
</success_criteria>

<output>
After completion, create `.planning/phases/12-create-anisearch-intake-reintroduction-and-draft-merge-control/12-01-SUMMARY.md`
</output>
