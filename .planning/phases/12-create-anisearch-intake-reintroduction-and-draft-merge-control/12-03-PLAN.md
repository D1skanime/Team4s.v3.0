---
phase: 12-create-anisearch-intake-reintroduction-and-draft-merge-control
plan: 03
type: execute
wave: 3
depends_on: [12-02]
files_modified:
  - frontend/src/app/admin/anime/create/page.tsx
  - frontend/src/app/admin/anime/create/page.test.tsx
  - frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx
  - frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.module.css
  - frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx
  - frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.test.tsx
  - frontend/src/app/admin/anime/create/createAniSearchSummary.ts
autonomous: true
requirements: [ENR-01, ENR-05]
must_haves:
  truths:
    - "AniSearch is visibly available on the create route above Jellyfin with an explicit ID input and load action."
    - "After AniSearch loads, the operator sees a card-based unsaved summary with updated fields, relation notes, and provider-precedence reminders."
    - "AniSearch failures leave the draft usable, and duplicate ownership renders a direct redirect action instead of generic page error text."
  artifacts:
    - path: "frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx"
      provides: "Create-route AniSearch input, status, duplicate, and summary UI"
      contains: "AniSearch laden"
    - path: "frontend/src/app/admin/anime/create/page.tsx"
      provides: "Create page wiring that places AniSearch above the Jellyfin action seam"
      contains: "ManualCreateWorkspace"
    - path: "frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx"
      provides: "Title-action layout that can stack AniSearch above Jellyfin responsively"
      contains: "titleActions"
    - path: "frontend/src/app/admin/anime/create/page.test.tsx"
      provides: "Create-route regression coverage for visible AniSearch UI and summary states"
      contains: "AniSearch laden"
  key_links:
    - from: "frontend/src/app/admin/anime/create/page.tsx"
      to: "frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx"
      via: "titleActions composition above Jellyfin button"
      pattern: "CreateAniSearchIntakeCard"
    - from: "frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx"
      to: "frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts"
      via: "AniSearch input, submit, and status props"
      pattern: "anisearch"
---

<objective>
Reintroduce AniSearch as a visible create-route action and render the operator-safe status, summary, error, and duplicate states described by the Phase 12 UI contract.

Purpose: This is the user-visible completion of Phase 12. It must restore the AniSearch create seam without hiding behavior in generic page messages or regressing the existing create workspace.
Output: Visible create AniSearch card above Jellyfin, responsive layout updates, and page-level regressions covering the live operator flow.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-create-anisearch-intake-reintroduction-and-draft-merge-control/12-CONTEXT.md
@.planning/phases/12-create-anisearch-intake-reintroduction-and-draft-merge-control/12-RESEARCH.md
@.planning/phases/12-create-anisearch-intake-reintroduction-and-draft-merge-control/12-UI-SPEC.md
@.planning/phases/11-anisearch-edit-enrichment-and-relation-persistence/11-03-SUMMARY.md
@.planning/phases/12-create-anisearch-intake-reintroduction-and-draft-merge-control/12-02-SUMMARY.md
@frontend/src/app/admin/anime/create/page.tsx
@frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx
@frontend/src/app/admin/anime/components/AnimeEditPage/AniSearchEnrichmentSection.tsx
@frontend/src/app/admin/anime/create/createAniSearchSummary.ts

<interfaces>
From `frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx`:
```tsx
interface ManualCreateWorkspaceProps {
  titleActions?: ReactNode
  titleHint?: ReactNode
}
```

From `frontend/src/app/admin/anime/components/AnimeEditPage/AniSearchEnrichmentSection.tsx`:
```tsx
export function AniSearchEnrichmentSection({
  anisearchID,
  protectedFields,
  isLoading,
  conflictResult,
  statusMessage,
  errorMessage,
  onAniSearchIDChange,
  onProtectedFieldsChange,
  onSubmit,
}: AniSearchEnrichmentSectionProps)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add create-page regression coverage for the visible AniSearch intake flow</name>
  <files>frontend/src/app/admin/anime/create/page.test.tsx, frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.test.tsx</files>
  <read_first>@frontend/src/app/admin/anime/create/page.test.tsx @.planning/phases/12-create-anisearch-intake-reintroduction-and-draft-merge-control/12-UI-SPEC.md @frontend/src/app/admin/anime/components/AnimeEditPage/AniSearchEnrichmentSection.tsx</read_first>
  <acceptance_criteria>`frontend/src/app/admin/anime/create/page.test.tsx` asserts that AniSearch renders above `Jellyfin suchen`, exposes an explicit AniSearch ID field and `AniSearch laden` button, and shows unsaved summary/error/duplicate states; `frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.test.tsx` asserts the summary groups `Aktualisierte Felder`, `Relationen`, and `Entwurfsstatus` plus the duplicate redirect CTA.</acceptance_criteria>
  <action>Add UI regressions that enforce D-01 through D-04 and D-13 through D-15 against the current create route. Cover the mobile-safe stacked action area implicitly by asserting separate AniSearch and Jellyfin controls inside the same source-action cluster, plus the D-10 duplicate CTA label `Zum vorhandenen Anime wechseln`. Keep tests focused on reachable operator behavior; do not add assertions for deferred title search or any broader edit redesign.</action>
  <verify>
    <automated>cd frontend && npm test -- src/app/admin/anime/create/page.test.tsx src/app/admin/anime/create/CreateAniSearchIntakeCard.test.tsx</automated>
  </verify>
  <done>The create-page regression suite proves AniSearch is visibly restored, summary content is explicit and unsaved, and duplicate redirects remain operator-usable.</done>
</task>

<task type="auto">
  <name>Task 2: Build the create AniSearch card and wire it into the workspace above Jellyfin</name>
  <files>frontend/src/app/admin/anime/create/page.tsx, frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx, frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.module.css, frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx, frontend/src/app/admin/anime/create/createAniSearchSummary.ts</files>
  <read_first>@frontend/src/app/admin/anime/create/page.tsx @frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.tsx @frontend/src/app/admin/anime/components/ManualCreate/ManualCreateWorkspace.module.css @frontend/src/app/admin/anime/components/AnimeEditPage/AniSearchEnrichmentSection.tsx</read_first>
  <acceptance_criteria>`frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx` renders an AniSearch ID field, primary `AniSearch laden` action, inline helper line `Manuell > AniSearch > Jellyfin`, an `aria-live=\"polite\"` status region, and card branches for idle, loading, success, duplicate, and error; `frontend/src/app/admin/anime/create/page.tsx` composes that card directly above the existing Jellyfin button in `ManualCreateWorkspace`; duplicate state names the existing anime and links to edit; success state shows updated fields, relation notes, unsaved reminder, overwritten-Jellyfin note, and preserved-manual note when present.</acceptance_criteria>
  <action>Create `CreateAniSearchIntakeCard.tsx` as a focused create-route component that reuses the edit-card accessibility/status pattern but not the protected-field checkboxes. Wire it into `page.tsx` through `ManualCreateWorkspace.titleActions` so AniSearch appears above the current Jellyfin button per D-02 and D-19. Update `ManualCreateWorkspace.tsx` and its CSS module only as needed to support the stacked action cluster and mobile layout from the UI spec. Keep the existing top-level success box for post-create persistence feedback, but render draft-time AniSearch summary/error/conflict state locally in the new card. Do not bury duplicate redirects in `errorMessage`, do not add modal workflows, and do not restore any dead placeholder copy.</action>
  <verify>
    <automated>cd frontend && npm test -- src/app/admin/anime/create/page.test.tsx src/app/admin/anime/create/CreateAniSearchIntakeCard.test.tsx src/app/admin/anime/create/useAdminAnimeCreateController.test.ts && npm run build</automated>
  </verify>
  <done>The live create page shows a first-class AniSearch intake card above Jellyfin, operator-safe status stays local to the source controls, and the create route still builds successfully.</done>
</task>

</tasks>

<verification>
Phase 12 is user-visible when the create route exposes AniSearch above Jellyfin, duplicate redirects are in-card and actionable, and the summary card clearly says the draft changed but nothing is saved yet.
</verification>

<success_criteria>
- AniSearch is visibly present on `/admin/anime/create` with exact-ID input and a dedicated load button.
- Success, duplicate, and error feedback all render inline near the source controls rather than only in generic page messages.
- The UI preserves the locked merge hierarchy and relation/warning communication without adding deferred search or edit redesign work.
</success_criteria>

<output>
After completion, create `.planning/phases/12-create-anisearch-intake-reintroduction-and-draft-merge-control/12-03-SUMMARY.md`
</output>
