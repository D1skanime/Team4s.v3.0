---
phase: 152-public-fansub-gruppenseite-konsolidierung-und-modernisierung
plan: 10
type: execute
wave: 4
depends_on: ["152-09"]
files_modified: []
autonomous: false
requirements: [P152-14]

must_haves:
  truths:
    - "Every viewport in 320/390/520/768/1024/1440/1920/2560 has been visually inspected across Hero, Story, Projects, Team, History, Media with no horizontal overflow, no clipped badges, and an intact timeline."
    - "The two expected visual deltas (collapsed legendary emphasis glow; larger, uniform mobile badge size) are explicitly confirmed as correct, not silently accepted as unreviewed regressions."
    - "The temporary History fixture rows seeded by Plan 152-09 are removed after the visual pass, leaving the live database in its pre-152 data state."
    - "A human has given explicit sign-off after reviewing captured evidence, closing P152-14 and the phase."
  artifacts:
    - path: ".planning/phases/152-public-fansub-gruppenseite-konsolidierung-und-modernisierung/152-10-SUMMARY.md"
      provides: "Viewport screenshot evidence index + human sign-off record + fixture-cleanup confirmation"
  key_links:
    - from: "152-10-SUMMARY.md"
      to: "captured Playwright screenshots"
      via: "one screenshot set per viewport covering Hero/Story/Projects/Team/History/Media"
      pattern: "viewport"
---

<objective>
Capture and present visual evidence of the complete public fansub group page across all 8 required
viewports, obtain explicit human sign-off (including on the two deliberate visual deltas this phase
introduces), remove the temporary History fixture, and close the phase with independent final
verification. This is the phase's mandatory closing block (critical_sequencing_constraint #6),
depending on Plan 152-09's green regression gate and seeded fixture. Kept as its own plan (not merged
into 152-09) because it contains a `checkpoint:human-verify` gate, and "checkpoint + implementation" may
never share a plan.

Purpose: `152-USER-REQUEST.md`'s Definition of Done requires "mobile bis widescreen visuell geprüft" and
"unabhängige Abschlussverifikation PASS" before the phase can be considered complete; the phase's own
Emphasis Contract (UI-SPEC) and CSS consolidation (Plan 152-07 Task 2) both introduce visible,
DELIBERATE changes that must be confirmed correct by a human, not silently shipped.
Output: A full 8-viewport screenshot evidence set; explicit human approval; a database restored to its
pre-152-fixture state; phase-closing verification notes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-public-fansub-gruppenseite-konsolidierung-und-modernisierung/152-UI-SPEC.md
@.planning/phases/152-public-fansub-gruppenseite-konsolidierung-und-modernisierung/152-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture screenshot evidence across all 8 viewports</name>
  <files>N/A — evidence capture only, no source files modified</files>
  <read_first>
    frontend/scripts/capture-phase151-badge-evidence.mjs and
    frontend/scripts/phase151-badge-evidence-browser-private.mjs (the most recent Playwright
    viewport-matrix capture precedent in this codebase — `chromium` from the `playwright` package,
    per-viewport `page.setViewportSize`, `page.on('pageerror'|'console'|'requestfailed'|'response')`
    diagnostics, and screenshot-to-disk pattern this task's capture approach follows; do not build new
    generic capture infrastructure — write one small phase-152-scoped script or reuse Playwright
    directly against the running dev server, matching this file's diagnostics-wiring convention)
  </read_first>
  <action>
    Before capturing, per CONTEXT.md's operational note: `docker restart team4sv30-frontend` and warm
    the image cache with one curl against a History badge `/_next/image` URL (avoids the documented
    zombie-Chromium/stale-cache collector pitfall). For each of the 8 required viewport widths (320,
    390, 520, 768, 1024, 1440, 1920, 2560 — use a representative height per width, e.g. the same
    height-scaling convention as `phase151-badge-evidence-browser-private.mjs`'s `VIEWPORTS` list, or a
    fixed generous height like 2400 to capture the full scroll length in one shot), navigate Playwright
    to `http://192.168.235.196:3000/fansubs/<seeded-group-slug>`, wait for the page and its images to
    settle, and capture a full-page screenshot into a per-viewport output directory. Attach the same
    `pageerror`/`console`/`requestfailed`/`response>=400` diagnostics wiring as the Phase-151 precedent
    and record any browser errors as findings, not silent noise. Save all screenshots plus a findings
    log under a clearly-labeled output directory (e.g. `/tmp/team4s-152-visual-qa/`).
  </action>
  <verify>
    <automated>test -d /tmp/team4s-152-visual-qa && [ "$(find /tmp/team4s-152-visual-qa -name '*.png' | wc -l)" -ge 8 ]</automated>
  </verify>
  <done>At least 8 full-page screenshots exist (one per required viewport width), zero unhandled browser page/console/request errors were recorded, and a findings log accompanies the screenshot set.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual QA sign-off across all 8 viewports</name>
  <files>none (manual verification only, no file changes)</files>
  <action>
    Present the Task-1 screenshot evidence and the live page to the user for review against the
    checklist in how-to-verify below, and wait for explicit "approved" or a description of specific
    issues before proceeding to Task 3's fixture cleanup.
  </action>
  <what-built>
    The complete public fansub group page (`/fansubs/[slug]`) after Phase 152's consolidation: History
    badges now render through the shared Phase-151 `AchievementArtwork` slot (sharper, smaller, WebP
    -optimized, lazy-loaded); the two `legendary`-tier History events (`projects_500`,
    `releases_10000`) now share ONE token-driven glow treatment instead of two bespoke hardcoded ones;
    mobile/narrow-viewport badges now render at a fixed, larger 192px base size instead of the old
    viewport-shrinking clamp range; admin-authored free-text History titles render byte-for-byte
    unchanged; the Hero section may have switched to optimized image delivery (per Plan 152-05's
    documented decision); media thumbnail accessibility and page composition were hardened without
    visible change.
  </what-built>
  <how-to-verify>
    1. Review the Task 1 screenshot set (`/tmp/team4s-152-visual-qa/`) for all 8 viewports
       (320/390/520/768/1024/1440/1920/2560), or open `http://127.0.0.1:3300/fansubs/<seeded-group-slug>`
       live through the Windows SSH tunnel and resize the browser through each width.
    2. Confirm: no horizontal page overflow at any width; no clipped or overlapping badges/cards; the
       History timeline's left/right card alternation stays visually aligned at the desktop 3-column
       layout (≥1100px) and collapses cleanly to a single column below it; images are sharp, not
       blurry or pixelated, at every width.
    3. Confirm the two EXPECTED, DELIBERATE visual deltas look correct (not broken): (a) `projects_500`
       and `releases_10000` History entries now share one identical gold/violet/cyan glow treatment
       (previously two visually distinct bespoke glows) — confirm this reads as a coherent design
       choice, not a visual bug; (b) mobile/narrow-viewport badges are now uniformly ~192px (larger
       than before) — confirm they fit their card/timeline layout cleanly without overflow at 320px and
       390px specifically.
    4. Confirm the custom-titled History entry seeded by Plan 152-09 (e.g. "Projektor gekauft") renders
       exactly as typed, with no "Fansub-" prefix.
    5. Confirm Story/Projects/Team/Media sections are visually unaffected (this phase did not touch
       their layout) and that keyboard focus/tab order through the History section's "Weitere
       anzeigen"/"Weniger anzeigen" button still works.
  </how-to-verify>
  <verify>
    <human-check>User reviews the 8-viewport screenshot evidence and/or the live page, and explicitly types "approved" or describes specific issues.</human-check>
  </verify>
  <done>User has typed "approved" for all 8 viewports and both expected deltas, or listed specific viewport/section issues to route back through /gsd:plan-phase 152 --gaps.</done>
  <resume-signal>Type "approved" once all 8 viewports and both expected deltas are confirmed correct, or describe specific issues found (viewport + section + what's wrong) to route back to a gap-closure plan.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Remove the temporary History fixture and close the phase</name>
  <files>N/A — DB cleanup + phase-closing documentation only, no source files modified</files>
  <read_first>
    .planning/phases/152-public-fansub-gruppenseite-konsolidierung-und-modernisierung/152-09-SUMMARY.md
    (the exact seeded row IDs Plan 152-09 recorded — this task deletes precisely those rows and no
    others)
  </read_first>
  <action>
    After the Task-2 checkpoint is approved, delete exactly the `fansub_group_history` rows Plan
    152-09's SUMMARY recorded as seeded (via
    `docker compose exec -T team4sv30-db psql -U team4s -d team4s_v2 -c "DELETE FROM
    fansub_group_history WHERE id IN (<seeded ids>)"`), leaving the database in its pre-152 data state
    (per `152-CONTEXT.md`'s documented "Datenlage" baseline — one `founding` entry for the live group).
    Confirm via `SELECT COUNT(*) FROM fansub_group_history` that the count matches the pre-152 baseline.
    Re-run one final live smoke check: load `/fansubs/<slug>` and confirm the page still renders
    correctly with the fixture removed (the single remaining real History entry displays correctly).
    Record the human sign-off outcome, the final screenshot evidence index, and the restored-baseline
    confirmation in this plan's SUMMARY, closing P152-14 and the phase.
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-db psql -U team4s -d team4s_v2 -tAc "SELECT COUNT(*) FROM fansub_group_history"</automated>
  </verify>
  <done>Only the pre-152 baseline History rows remain in the database; a final live smoke check confirms the page renders correctly; the SUMMARY records the human sign-off, the evidence index, and the restored-baseline confirmation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Human reviewer -> live/screenshot evidence | Final human judgment gate before phase closure; no automated substitute for visual-appearance correctness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-152-10-01 | Tampering (leftover test data) | Temporary History fixture cleanup | mitigate | Task 2 deletes precisely the row IDs recorded by Plan 152-09, verified via a post-delete row-count check against the documented pre-152 baseline — no broad/unscoped DELETE |
</threat_model>

<verification>
8-viewport screenshot evidence captured with zero unhandled browser errors; explicit human "approved"
sign-off recorded; database row count restored to the documented pre-152 baseline; final live smoke
check passes.
</verification>

<success_criteria>
A human has visually confirmed the complete page at all 8 required viewports, explicitly signed off on
both deliberate visual deltas, and the database is restored to its pre-152 state — closing P152-14 and
the phase.
</success_criteria>

<output>
Create `.planning/phases/152-public-fansub-gruppenseite-konsolidierung-und-modernisierung/152-10-SUMMARY.md` when done
</output>
