---
phase: 152-public-fansub-gruppenseite-konsolidierung-und-modernisierung
plan: 07
type: execute
wave: 2
depends_on: ["152-01"]
files_modified:
  - frontend/src/lib/group-history-events.ts
  - frontend/src/components/fansubs/FansubHistorySection.tsx
  - frontend/src/components/fansubs/FansubPublicSections.module.css
  - frontend/src/components/fansubs/__tests__/FansubHistorySection.test.tsx
autonomous: true
requirements: [P152-02, P152-03, P152-04, P152-05, P152-10, P152-12, P152-13]

must_haves:
  truths:
    - "History badges render through the Phase-151 AchievementArtwork slot (next/image, lazy, WebP-capable) instead of a raw <img>."
    - "No CSS-declared history-badge geometry (--history-badge-size, its breakpoints, the releases_10000 special size) remains; AchievementArtwork's own 192/216/240px container-query steps are the only size authority."
    - "Two events (projects_500, releases_10000) share one generic 'legendary' emphasis treatment sourced from color-mix/var(--ach-color) tokens; no hardcoded hex remains in the emphasis blocks."
    - "Admin-authored free-text history titles render byte-for-byte unchanged; only the registry's static publicLabel gets the domain-consistency prefix."
    - "The timeline's spine year marker is aria-hidden; the card's own year stays in the accessible tree."
    - "FansubHistorySection.test.tsx asserts behavior (order, title, public label, freetext preservation, artwork presence, emphasis, accessibility) instead of CSS class-name substrings for the assertions that actually changed."
  artifacts:
    - path: "frontend/src/lib/group-history-events.ts"
      provides: "emphasis + publicLabel additive fields on GroupHistoryEventPresentation"
      contains: "emphasis: 'none' | 'legendary'"
    - path: "frontend/src/components/fansubs/FansubHistorySection.tsx"
      provides: "AchievementArtwork-based badge rendering, emphasis-driven wrapper class, publicLabel consumption, no publicDomainTerms"
      contains: "AchievementArtwork"
    - path: "frontend/src/components/fansubs/FansubPublicSections.module.css"
      provides: "History badge geometry fully delegated to AchievementArtwork; single historyTimelineEmphasisLegendary selector; dead classes removed"
      contains: "historyTimelineEmphasisLegendary"
    - path: "frontend/src/components/fansubs/__tests__/FansubHistorySection.test.tsx"
      provides: "Behavior-based rewrite + axe coverage"
      exports: ["describe('FansubHistorySection'"]
  key_links:
    - from: "frontend/src/components/fansubs/FansubHistorySection.tsx"
      to: "frontend/src/components/profile/AchievementArtwork.tsx"
      via: "direct descriptor { kind: 'direct', src: presentation.imageSrc }, size=hero, decorative"
      pattern: "kind: 'direct'"
    - from: "frontend/src/components/fansubs/FansubHistorySection.tsx"
      to: "frontend/src/lib/group-history-events.ts"
      via: "presentation.publicLabel / presentation.emphasis read directly, no runtime string rewrite"
      pattern: "presentation.publicLabel"
---

<objective>
Migrate `FansubHistorySection`'s badge rendering onto the shared Phase-151 `AchievementArtwork` slot
(A2), delete every History-specific badge-sizing CSS declaration in favor of `AchievementArtwork`'s own
container-query authority (A3), replace the hardcoded `eventType` if-chain and runtime
`publicDomainTerms` string-rewrite with additive, static registry fields (A4/A5), fix the duplicate-year
accessibility defect (D3), and rewrite the test file's class-name assertions into behavior assertions
with new axe coverage (D6/D4). A1 (Plan 152-01) MUST already be verified live before this plan starts —
it is this plan's sole cross-plan dependency (D09/critical_sequencing_constraint #1). All four touched
files are owned by this single plan/executor because they are edited by multiple workstreams in
sequence and D09 forbids splitting them across parallel-wave plans (constraint #3/#4).

Purpose: History badges currently render via a raw `<img>` with a bespoke, 15-declaration-site CSS
sizing chain (including a `releases_10000`-only size family and unexplained pixel shifts) and a hard
5-code `eventType` if-chain — a second, parallel badge-presentation system next to the one Phase 151
already built for member profiles (D03 forbids a second architecture). `publicDomainTerms` also mutates
ADMIN-authored free text through the same string-rewrite chain used for static labels — a real data
-integrity defect (D06 forbids this unconditionally).
Output: History badges rendered through `AchievementArtwork` (direct descriptor, no member badge-code
resolver, per D05); a CSS module with zero History-badge-geometry declarations; an additively-extended
registry (`emphasis`, `publicLabel`); a rewritten, behavior-asserting test file with axe coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-public-fansub-gruppenseite-konsolidierung-und-modernisierung/152-UI-SPEC.md
@.planning/phases/152-public-fansub-gruppenseite-konsolidierung-und-modernisierung/152-RESEARCH.md
@.planning/phases/152-public-fansub-gruppenseite-konsolidierung-und-modernisierung/152-PATTERNS.md
</context>

<interfaces>
<!-- AchievementArtwork's exact contract (Phase 151, do not modify) and the established embedding
     convention this plan replicates -- both already read in full during planning. -->

From frontend/src/components/profile/AchievementArtwork.tsx (props, confirmed at HEAD):
```tsx
export type AchievementArtworkDescriptor =
  { kind: 'direct'; src: string } | { kind: 'layered'; motifSrc: string; frameSrc: string }
export type AchievementArtworkProps = {
  descriptor: AchievementArtworkDescriptor
  badgeCode: string
  alt: string
  size: 'hero' | 'stage'
  decorative?: boolean
  priority?: boolean
  className?: string
}
```
Renders a `data-achievement-art={badgeCode}` attribute on its inner `<img>` (via `ResponsiveImage`) and
`data-achievement-slot`/`data-achievement-size` on its root `<span>` — these are the correct test hooks
(RESEARCH.md Pitfall 3 / AchievementArtwork.test.tsx precedent), not CSS module class names.

From frontend/src/components/profile/AchievementArtwork.module.css (`.container`, confirmed at HEAD):
```css
.container {
  container: achievement-card / inline-size;
  display: block; width: 100%; min-width: 0; max-width: 100%;
}
```
Consumed via `import artworkStyles from '@/components/profile/AchievementArtwork.module.css'` and
`<div className={artworkStyles.container}>` wrapping the full-width ancestor — per
`152-UI-SPEC.md`'s "Migration Contract" section, this wraps `.historyTimelinePair` (the whole grid row),
NOT `.historyTimelineBadge` alone (the badge box alone never crosses AchievementArtwork's 562px/658px
container-query thresholds and would permanently lock the badge at its smallest 192px step).

Current group-history-events.ts interface (51 lines, full file already read):
```ts
export interface GroupHistoryEventPresentation {
  value: string; label: string; category: GroupHistoryEventCategory; imageSrc: string
  tone: 'gold' | 'accent' | 'green' | 'pink' | 'muted' | 'blue' | 'violet' | 'red' | 'legendary'
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the registry and migrate FansubHistorySection to the AchievementArtwork slot</name>
  <files>frontend/src/lib/group-history-events.ts, frontend/src/components/fansubs/FansubHistorySection.tsx</files>
  <read_first>
    frontend/src/lib/group-history-events.ts (full file, 51 lines — the interface and all 23
    `GROUP_HISTORY_EVENT_OPTIONS` entries you extend additively)
    frontend/src/components/fansubs/FansubHistorySection.tsx (full file, 139 lines — every function
    (`achievementStyle`, `achievementEventStyle`, `historyTitle`, `publicDomainTerms`,
    `publicHistoryLabel`, `publicHistoryTitle`, `sortHistory`) and the full JSX render block, lines
    93-138, this task rewrites)
    frontend/src/components/groups/GroupHistoryForm.tsx (targeted, lines 1-10, 89-97, 123-175 — confirm
    it only reads `.value`/`.category`/`.tone`/`.imageSrc`/`.label`, never enumerates all keys, before
    trusting the additive-field-safety claim)
    152-UI-SPEC.md "Migration Contract — Artwork Slot Embedding" and "Emphasis Contract" and
    "Copywriting Contract" sections (binding, locked shapes — implement exactly what those sections
    show)
  </read_first>
  <behavior>
    - `getGroupHistoryEventPresentation('projects_500').emphasis === 'legendary'` and
      `getGroupHistoryEventPresentation('releases_10000').emphasis === 'legendary'`; every other of the
      21 remaining entries has `emphasis: 'none'`.
    - `getGroupHistoryEventPresentation(value).publicLabel` matches the exact per-value table in
      `152-UI-SPEC.md`'s Copywriting Contract for all 23 values (11 unchanged word-for-word, 12 gaining
      the "Fansub-" prefix — verify each row by running today's `publicDomainTerms(label)` against each
      admin `label` before deleting the function, per UI-SPEC's own regression-guard instruction; note
      UI-SPEC's summary sentence claims "14 unchanged / 9 changed" but the table itself (and a direct
      simulation of `publicDomainTerms`) shows 11 unchanged / 12 changed — trust the per-row table
      values, not the prose count).
    - An admin-authored custom title (e.g. `"Projektor gekauft"`, not equal to the event's registry
      `label` or `event_type`) renders byte-for-byte unchanged in the `<strong>` — no "Fansub-" prefix
      is ever applied to `item.title`.
    - The badge renders via `<AchievementArtwork descriptor={{ kind: 'direct', src:
      presentation.imageSrc }} badgeCode={item.event_type} alt="" size="hero" decorative />` wrapped in
      `artworkStyles.container` around `.historyTimelinePair`.
    - The axis-year `<span>` carries `aria-hidden="true"`; the card's own year `<span>` does not.
  </behavior>
  <action>
    In `group-history-events.ts`: add `emphasis: 'none' | 'legendary'` and `publicLabel: string` to
    `GroupHistoryEventPresentation`. Set `emphasis: 'legendary'` on exactly the `projects_500` and
    `releases_10000` entries (both already `tone: 'legendary'`); `'none'` on the other 21. Set
    `publicLabel` per the UI-SPEC Copywriting Contract table for all 23 entries (do not introduce a
    `'rare'` emphasis tier — no current entry needs it). Do not touch `value`/`label`/`category`/
    `imageSrc`/`tone` on any entry, and do not touch `GROUP_HISTORY_EVENT_VALUES`/
    `GROUP_HISTORY_EVENT_BY_VALUE`/`getGroupHistoryEventPresentation` (already correct, additive-safe).

    In `FansubHistorySection.tsx`: delete `achievementEventStyle` (lines 31-38), `publicDomainTerms`
    (lines 44-59), `publicHistoryLabel` (61-63), and `publicHistoryTitle` (65-71) entirely. Keep
    `achievementStyle` (lines 18-29, tone-driven, unchanged per PATTERNS.md — do not remove or alter
    it) and `historyTitle`/`sortHistory` unchanged. In the render `.map`, replace the removed
    `publicHistoryLabel(presentation)` call with a direct read of `presentation.publicLabel`, and
    replace `publicHistoryTitle(item, presentation)` with logic that mirrors its old branching intent
    but WITHOUT ever calling `publicDomainTerms` on `item.title`: if `historyTitle(item) ===
    item.event_type || historyTitle(item) === presentation.label` (meaning no real admin title was
    set, only the fallback), render `presentation.publicLabel`; otherwise render `historyTitle(item)`
    verbatim (the true admin-authored title, byte-for-byte, per D06/A5). Replace the `eventStyle`
    variable's source: instead of `achievementEventStyle(item.event_type)`, compute
    `presentation.emphasis === 'legendary' ? 'historyTimelineEmphasisLegendary' : null` and keep the
    existing `styles[eventStyle]` lookup pattern in the `<li>` className array (Task 2 creates this
    single new CSS class name). Add `data-emphasis={presentation.emphasis !== 'none' ?
    presentation.emphasis : undefined}` on the `<li>` as a semantic, non-CSS-coupled test hook (per
    RESEARCH.md's guidance to assert "presence of a semantic hook", not the CSS class string).

    Replace the badge-rendering block (current lines 114-118: the `.historyTimelinePair` div containing
    the axis-year span and the raw `<img>`-in-`.historyTimelineBadge`) with the exact structure shown
    in `152-UI-SPEC.md`'s "Migration Contract" code block: import `AchievementArtwork` from
    `@/components/profile/AchievementArtwork` and `artworkStyles` from
    `@/components/profile/AchievementArtwork.module.css`; wrap the ENTIRE `.historyTimelinePair` div
    (not just `.historyTimelineBadge`) in `<div className={artworkStyles.container}>`; add
    `aria-hidden="true"` to the axis-year `<span>` (currently unhidden, D3 fix); remove the
    `aria-hidden="true"` currently on the `.historyTimelineBadge` wrapper div (AchievementArtwork's own
    `decorative` prop now owns that semantics internally — keeping both would be redundant, not wrong,
    but UI-SPEC's locked shape omits it from the wrapper); replace the raw `<img
    src={presentation.imageSrc} alt="" className={styles.historyTimelineImage} />` with
    `<AchievementArtwork descriptor={{ kind: 'direct', src: presentation.imageSrc }}
    badgeCode={item.event_type} alt="" size="hero" decorative />`. Do not import or use
    `resolveBadgeArtwork`/`resolveLayeredRoleArtwork` (D05 — no member badge-code resolver adoption).
    Keep `INITIAL_VISIBLE_HISTORY = 6` and the expand/collapse `Button` block completely unchanged.
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit"</automated>
  </verify>
  <done>Registry has emphasis+publicLabel on all 23 entries matching the UI-SPEC table; FansubHistorySection.tsx renders badges via AchievementArtwork with a direct descriptor, no publicDomainTerms/achievementEventStyle remain, admin free text is never rewritten, axis-year is aria-hidden, data-emphasis reflects the registry field; tsc --noEmit is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Delete History-badge geometry CSS and collapse the emphasis blocks onto tokens</name>
  <files>frontend/src/components/fansubs/FansubPublicSections.module.css</files>
  <read_first>
    frontend/src/components/fansubs/FansubPublicSections.module.css (full file, 883 lines — this plan
    has already directly verified, beyond what 152-UI-SPEC.md/152-PATTERNS.md documented, that
    `--history-badge-size` has 5 CONSUMPTION sites, not 2: `.historyTimelineItem`'s
    `min-height: calc(var(--history-badge-size) + 8px)` at line 285, `.historyTimelinePair`'s
    `grid-template-columns` at line 291 AND its `min-height: var(--history-badge-size)` at line 294,
    and `.historyTimelineBadge`'s `width`/`height` at lines 302-303. UI-SPEC's Migration Contract only
    names the grid-template-columns and badge width/height sites — treat the `.historyTimelineItem`
    min-height (285) and `.historyTimelinePair` min-height (294) as a THIRD and FOURTH consumption site
    this task must also resolve, using the same UI-SPEC-locked reasoning (a fixed 240px value,
    AchievementArtwork's largest hero step) for consistency.
  </read_first>
  <action>
    Delete all 10 `--history-badge-size:` DECLARATION sites: line 281 (base, inside
    `.historyTimelineItem`), 485 (`@media min-width:1200px`), 496 (`@media min-width:1500px`), 512
    (`@media max-width:1100px`), 562 (`@media max-width:760px`), 614 (base, inside
    `.historyTimelineEventReleases10000`), 652/658/664/670 (the four `releases_10000`-specific media
    query overrides). For the four `releases_10000`-only media-query blocks (approx. lines 650-654,
    656-660, 662-666, 668-672) whose ENTIRE body is only that one declaration: delete the whole
    `@media {...}` block, not just the declaration inside it (an empty media block is dead weight).
    Resolve the 5 CONSUMPTION sites: `.historyTimelinePair`'s `grid-template-columns: minmax(0,1fr)
    var(--history-badge-size) minmax(0,1fr)` (line 291) becomes `grid-template-columns: minmax(0,1fr)
    240px minmax(0,1fr)`; `.historyTimelinePair`'s `min-height: var(--history-badge-size)` (line 294)
    becomes `min-height: 240px`; `.historyTimelineItem`'s `min-height: calc(var(--history-badge-size) +
    8px)` declaration (line 285) is REMOVED entirely (not replaced — `.historyTimelinePair`'s own fixed
    240px min-height already reserves row space, making this wrapper-level min-height redundant once
    the badge's actual size comes from `AchievementArtwork`); `.historyTimelineBadge`'s `width:
    var(--history-badge-size)` and `height: var(--history-badge-size)` (lines 302-303) are REMOVED
    entirely — the box's existing `display: grid; place-items: center` plus its existing
    `justify-self: center` (not `stretch`) already shrink-wraps it to its `AchievementArtwork` child's
    intrinsic size, so no replacement value is needed there. Delete `.historyTimelineImage` (lines
    346-351) entirely — this class targeted the raw `<img>` Task 1 removed and has zero remaining
    consumers. Delete the four `--history-image-x`/`--history-image-y` DECLARATION sites inside
    `.historyTimelineEventReleases500`/`Releases1000`/`Releases5000`/`Releases10000` (lines 598-616,
    per D08 "remove, don't relocate" — these are the achievement-specific pixel shifts A3 targets) —
    since `.historyTimelineImage`'s `transform: translate(var(--history-image-x,0),
    var(--history-image-y,0))` consumption is also deleted in the same step (it lived inside the
    just-removed `.historyTimelineImage` rule), no dangling reference remains. IMPORTANT: after
    deleting the pixel-shift declarations, the `.historyTimelineEventReleases500`/`Releases1000`/
    `Releases5000` selector blocks become fully empty — delete those three selector blocks entirely
    too (do not leave empty `{}` rule bodies).

    Replace `.historyTimelineEventProjects500`/`.historyTimelineEventReleases10000` (lines 572-648,
    both their `.historyTimelineBadge`, `.historyTimelineBadge::before`, and `.historyTimelineCard`
    sub-selectors) with ONE new selector set under `.historyTimelineEmphasisLegendary` (matching Task
    1's new class-name source), reusing the file's own established `.achLegendary` token triple
    (`--ach-color: #a16207`, `--ach-grad: linear-gradient(135deg, #fef3c7, #c4b5fd 52%, #67e8f9)`,
    `--ach-soft: #fef3c7`, already declared at lines 247-251, unchanged) via
    `color-mix(in srgb, var(--ach-color) N%, transparent)` / `var(--ach-grad)` / `var(--ach-soft)` in
    place of every hardcoded hex (`#7c3aed`, `#facc15`) and rgba derivative currently in those two
    blocks — collapsing two bespoke glow treatments into one shared one (Pattern 3). This is a
    deliberate, expected visible change: `releases_10000` and `projects_500` will render with an
    IDENTICAL gold/violet/cyan glow instead of two visually distinct bespoke palettes — flag this in
    the plan SUMMARY as an expected Visual QA delta, not a regression.

    Delete the 11 confirmed-dead classes and BOTH media-query blocks that contain only dead classes:
    `.achGrid`/`.ach`/`.ach::before`/`.medal`/`.achBody strong` inside `@media (min-width: 768px)`
    (lines 427-454) and the SAME dead-class set repeated inside a separate `@media (min-width: 1200px)`
    block (lines 456-477) — delete BOTH blocks in full (this is also C2's "merge duplicate breakpoint
    blocks" resolution: the duplication was between a dead block and the live History
    `@media (min-width: 1200px)` block at line 479, which stays). Also delete the remaining dead
    classes not yet covered: `.achImage`, `.achBody` (base, non-media-query declaration if present),
    `.achNote`, `.achType`, `.achYear`, `.historyEntry`, `.milestoneEntry`, `.projectYear` — grep-verify
    each is genuinely unreferenced via `styles[...]` dynamic lookup in
    `FansubHistorySection.tsx`/`FansubStoryBlock.tsx`/`FansubHighlightsSection.tsx`/
    `FansubContributorsSection.tsx`/`FansubDeepDiveSection.tsx`/`FansubGroupMediaBlock.tsx` (the 6
    components importing this CSS module) before deleting each one. Do NOT touch the confirmed-live
    tone classes (`achGold`/`achAccent`/`achGreen`/`achPink`/`achMuted`/`achBlue`/`achViolet`/`achRed`/
    `achLegendary`), the event classes still referenced by other card sub-selectors, or the
    `tagGallery`/`tagHistory`/`tagOldweb`/`tagForum`/`tagIrc`/`tagEvent`/`tagArtwork`/`tagOther` classes
    (Media block, out of this plan's scope).

    Expected mobile-viewport consequence (call out explicitly in the SUMMARY, do not silently ship):
    once History-specific size breakpoints are gone, mobile/narrow badges render at
    `AchievementArtwork`'s fixed 192px base hero step instead of the old mobile-shrinking clamp range
    (previously as small as ~108-180px at narrow widths) — badges will appear LARGER on mobile than
    before. This is the intended, directed outcome of A3 (delegate sizing entirely to
    `AchievementArtwork`, no family-specific breakpoint logic), not an oversight; confirm it looks
    correct (no overflow/clipping) during Wave-3 Visual QA.
  </action>
  <verify>
    <automated>grep -c "history-badge-size" frontend/src/components/fansubs/FansubPublicSections.module.css | grep -qx 0 && grep -c "history-image-x\|history-image-y" frontend/src/components/fansubs/FansubPublicSections.module.css | grep -qx 0</automated>
  </verify>
  <done>Zero remaining references to --history-badge-size or --history-image-x/y anywhere in the file; .historyTimelineImage and the 11 dead classes plus both dead-class-only media blocks are gone; the two emphasis blocks are collapsed into one token-driven .historyTimelineEmphasisLegendary selector with no hardcoded hex in the emphasis-specific rules; docker compose build team4sv30-frontend succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite FansubHistorySection.test.tsx to behavior assertions and add axe coverage</name>
  <files>frontend/src/components/fansubs/__tests__/FansubHistorySection.test.tsx</files>
  <read_first>
    frontend/src/components/fansubs/__tests__/FansubHistorySection.test.tsx (full file, 167 lines — the
    file this task rewrites; it currently mixes `renderToStaticMarkup` and `render`+`screen`)
    frontend/src/components/profile/AchievementArtwork.test.tsx (lines 1-46 — the `vi.mock('next/image',
    ...)` scaffold to add, and the `data-achievement-art`/`getByRole('img', {name})` assertion style)
    frontend/src/components/profile/MemberProfileHero.test.tsx (lines 165-174, axe pattern)
  </read_first>
  <behavior>
    - Order/title/public-label: given confirmed history entries, list items appear in the same sorted
      order as before; each item's accessible title text and public label are correct per the new
      registry-driven values (not asserted via `<li>` class-name substrings).
    - Freetext preservation (NEW, required by A5, does not exist today): an admin-authored title like
      `"Projektor gekauft"` (distinct from the event's registry label) renders exactly as
      `"Projektor gekauft"`, never `"Fansub-Projektor gekauft"`.
    - Artwork: the badge renders through the mocked `next/image`, queryable via
      `[data-achievement-art="<event_type>"]`, decorative (`getByRole('img', { hidden: true })` or
      `aria-hidden` on the artwork root) — not by asserting `AchievementArtwork.module.css` class names
      (that is Phase 151's own test surface).
    - Emphasis: `data-emphasis="legendary"` is present on the `projects_500` and `releases_10000` list
      items and absent (attribute not set, or `null`) on a plain `milestone`/`founding` item.
    - Accessibility: `expect(await axe(container)).toHaveNoViolations()` for a populated render; the
      axis-year `<span>` has `aria-hidden="true"` (assert via `getAttribute`, not a class check); the
      card's own year `<span>` does not carry `aria-hidden`.
    - Expand/collapse (lines 140-160) and empty-state (lines 162-166) tests: run them FIRST against the
      post-Task-1/Task-2 component, unmodified, and confirm they still pass — they already use
      role-based/behavior-based queries and RESEARCH.md's analysis found no reason they'd break; do not
      spend effort rewriting them unless they actually go red.
    - The `historyTimeline`/`historyTimelinePair`/`historyTimelineAxisYear` substring checks (original
      lines 47/52/53) and the `achGold` class check (original line 48, from `achievementStyle`, which
      Task 1 explicitly keeps unchanged): run the EXISTING assertions first against the migrated
      component before rewriting anything. `achievementStyle`'s tone-class output is not touched by
      this plan, so this specific assertion may continue to pass unmodified — only rewrite assertions
      that are empirically red, do not blanket-rewrite assertions that still pass.
  </behavior>
  <action>
    Add `vi.mock('next/image', () => ({ default: ({ alt, priority, unoptimized, ...props }) => { void
    priority; void unoptimized; return <img alt={alt} {...props} /> } }))` at the top of the file
    (mirrors `AchievementArtwork.test.tsx`'s exact mock, needed because `AchievementArtwork` now renders
    through `ResponsiveImage` -> `next/image`). Import `axe` from `jest-axe`.

    Run the CURRENT (pre-rewrite) test file against the Task-1/Task-2 code first — observe exactly which
    of the 6 pre-existing tests go red vs. stay green, and rewrite only the red ones plus add the new
    ones below (this file's own empirical result is the authoritative source for "what changed", not
    this plan's line-number predictions, which may be imprecise). Expected new/changed tests:
    (1) rewrite the "markiert besonders seltene Meilensteine" test (lines 85-138) to assert
    `data-emphasis="legendary"` presence on `projects_500`/`releases_10000` items and its absence on a
    control `milestone` item, replacing the `historyTimelineEventReleases500/1000/5000/10000`/
    `Projects500` substring assertions (those specific classes no longer exist post-Task-2 — the 3 mid
    -tier `releases_500/1000/5000` entries have `emphasis: 'none'` per the registry, so they get NO
    special wrapper class or attribute anymore, matching the Emphasis Contract's exact scope of "exactly
    the two `legendary`-tone entries", nothing broader);
    (2) add a NEW freetext-preservation test (admin title `"Projektor gekauft"` renders unchanged,
    A5's regression guard, does not exist in the file today);
    (3) add a NEW artwork-presence test asserting `[data-achievement-art]` renders with the expected
    `event_type` value and is decorative;
    (4) add a NEW axe test (`expect(await axe(container)).toHaveNoViolations()`) plus an explicit
    assertion that the axis-year span has `aria-hidden="true"` while the card's own year span does not.
    Keep the file's existing `history`/`countHistory` fixture-construction style (plain
    `PublicFansubHistory[]` arrays) rather than introducing a new fixture helper.
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/fansubs/__tests__/FansubHistorySection.test.tsx"</automated>
  </verify>
  <done>All tests in the rewritten file pass; freetext preservation, artwork presence, emphasis (data-emphasis on exactly 2 event types), and axe coverage are each proven by a dedicated assertion; no assertion depends on AchievementArtwork's or FansubPublicSections.module.css's internal CSS class names for artwork/geometry (only for the pre-existing, unchanged tone classes if those empirically still apply).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | Public, read-only, server-controlled History registry data rendered client-side; no new user input, no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-152-07-01 | Tampering (data-integrity, not injection) | `publicDomainTerms` removal | mitigate | Deleting the runtime string-rewrite chain that was incorrectly applied to admin-authored `item.title` closes a real data-fidelity defect (D06) — admin free text is now guaranteed byte-for-byte, proven by Task 3's new freetext-preservation test |
| T-152-07-02 | Information Disclosure | `AchievementArtwork` reuse for History | accept | No new asset path is introduced — `presentation.imageSrc` values are the same registry-controlled, already-public `/history-event-badges-transparent/*.png` paths as before; A1 (Plan 152-01) already scoped the one new `next.config.mjs` localPatterns entry narrowly |
</threat_model>

<verification>
`tsc --noEmit` and `docker compose build team4sv30-frontend` both clean. `vitest run
src/components/fansubs/__tests__/FansubHistorySection.test.tsx` green with the rewritten/added tests.
`vitest run` (full suite) has no NEW failures in `GroupHistoryForm`/`GroupHistorySection` tests (additive
registry fields, additive-safety already confirmed via `toMatchObject` usage during planning).
</verification>

<success_criteria>
History badges render through AchievementArtwork with zero achievement-specific CSS size/breakpoint
logic anywhere; admin free text is provably unchanged; the two legendary-tier events share one
token-driven emphasis treatment; the test file proves behavior, not CSS class strings, for everything
that actually changed.
</success_criteria>

<output>
Create `.planning/phases/152-public-fansub-gruppenseite-konsolidierung-und-modernisierung/152-07-SUMMARY.md` when done
</output>
