---
phase: 152-public-fansub-gruppenseite-konsolidierung-und-modernisierung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/next.config.mjs
autonomous: true
requirements: [P152-01]

must_haves:
  truths:
    - "A request to /_next/image for a file under /history-event-badges-transparent/** returns HTTP 200 with a WebP body instead of HTTP 400."
    - "The master PNG files under frontend/public/history-event-badges-transparent/ are byte-identical before and after this change (no bulk asset conversion)."
  artifacts:
    - path: "frontend/next.config.mjs"
      provides: "images.localPatterns entry for /history-event-badges-transparent/**"
      contains: "/history-event-badges-transparent/**"
  key_links:
    - from: "frontend/next.config.mjs"
      to: "Next.js built-in /_next/image optimizer"
      via: "images.localPatterns pathname glob match"
      pattern: "history-event-badges-transparent"
---

<objective>
Unblock the Next.js Image optimizer for the History-badge asset directory so Plan 152-07 (History
artwork-slot migration, A2) can render badges through `AchievementArtwork`/`ResponsiveImage`/
`next/image` without the page crashing.

Purpose: `next/image` on a path with no `images.localPatterns` match throws Next.js error E426 and
crashes the entire page render (confirmed precedent: `152-CONTEXT.md`'s "Bekannte Falle"). This is a
hard precondition (D09/critical_sequencing_constraint #1) that MUST land and be verified BEFORE any
plan touching `FansubHistorySection.tsx`'s image rendering begins.
Output: One additive `localPatterns` array entry; live-verified `/_next/image` 200/WebP response for
a History badge asset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-public-fansub-gruppenseite-konsolidierung-und-modernisierung/152-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the history-event-badges-transparent localPatterns entry and verify live</name>
  <files>frontend/next.config.mjs</files>
  <read_first>
    frontend/next.config.mjs (full file, 69 lines — the `images.localPatterns` array currently holds
    entries for `/__phase120-image-probe/alpha-badge.png`, `/member-achievement-badges/**`,
    `/covers/**`, `/media/anime/**`, `/media/profile/**`, `/media/release-version/**`, each following
    the `{ pathname, search: '' }` shape and the `T-143-07-01` narrow-glob idiom documented in the
    comment above the `/media/*` entries)
  </read_first>
  <action>
    Insert a new object `{ pathname: '/history-event-badges-transparent/**', search: '' }` into the
    `images.localPatterns` array in `frontend/next.config.mjs`, placed directly after the
    `/member-achievement-badges/**` entry (same image-class precedent per
    `152-RESEARCH.md`'s "Code Examples -> A1"). Do not touch `deviceSizes`, `imageSizes`, `formats`,
    `remotePatterns`, `dangerouslyAllowLocalIP`, or `qualities` — this is a single additive array
    entry, not a config rewrite (D02/D04). Do not add a blanket `/**` pattern or widen any existing
    entry.

    After editing, restart the frontend container (dev mode does not hot-reload
    `next.config.mjs`): run `docker restart team4sv30-frontend` and wait for it to report healthy
    (poll `docker compose ps team4sv30-frontend` or tail logs for the Next.js "Ready" line) before
    verifying.

    Verify with a live curl against the running dev server at `192.168.235.196:3000` (per
    `CLAUDE.md`'s canonical dev-environment ports) for an existing badge file, e.g. `founding.png`
    (confirmed present in `frontend/public/history-event-badges-transparent/`, 840090 bytes per the
    CONTEXT.md audit):
    `curl -sI 'http://192.168.235.196:3000/_next/image?url=%2Fhistory-event-badges-transparent%2Ffounding.png&w=256&q=75'`
    Confirm the response status line is `HTTP/1.1 200` (not 400) and the `content-type` header is
    `image/webp`. Also confirm a `srcset`-capable request works by requesting two different `w=`
    values (e.g. `w=256` and `w=640`) and confirming both return 200 with different byte sizes
    (`curl -s ... | wc -c` for each), demonstrating the optimizer generates distinct responsive
    variants rather than passing the file through unmodified.

    Finally, confirm the master PNG asset itself is untouched: compute
    `sha256sum frontend/public/history-event-badges-transparent/founding.png` before and after this
    task's edit+restart and confirm the hash is identical (proves this is purely a Next.js config
    change with zero asset mutation, matching D02/"no bulk conversion").
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" 'http://192.168.235.196:3000/_next/image?url=%2Fhistory-event-badges-transparent%2Ffounding.png&w=256&q=75' | grep -q '^200$'</automated>
  </verify>
  <done>
    `frontend/next.config.mjs`'s `images.localPatterns` array contains a
    `{ pathname: '/history-event-badges-transparent/**', search: '' }` entry; a live curl against
    `/_next/image?url=%2Fhistory-event-badges-transparent%2Ffounding.png&w=256&q=75` returns HTTP 200
    with `content-type: image/webp`; two different `w=` values produce differently-sized WebP
    responses (srcset capability proven); the master PNG's sha256 is unchanged from before the edit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client -> Next.js `/_next/image` optimizer | Unauthenticated public request supplies a `url` query param that the optimizer resolves against `images.localPatterns`/`remotePatterns` before fetching/transforming the referenced file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-152-01-01 | Information Disclosure | `frontend/next.config.mjs` `images.localPatterns` | mitigate | The new entry is a single narrow explicit glob (`/history-event-badges-transparent/**`), following the file's own established `T-143-07-01` narrowing precedent (explicit per-namespace allowlist, never a blanket `/**`). This cannot be used to optimize/serve arbitrary filesystem paths outside the one public badge-asset directory. |
| T-152-01-02 | Tampering | Master PNG assets under `frontend/public/history-event-badges-transparent/` | accept | This task only changes optimizer routing config; it performs zero writes to the asset directory. Verified via sha256 comparison in Task 1's acceptance criteria. |
</threat_model>

<verification>
Live curl smoke test (200 + `image/webp` + differing byte sizes across `w=` values) plus a sha256
diff of the master PNG proving zero asset mutation. No automated test file is warranted for a
single-line config array addition (per `152-VALIDATION.md`'s Per-Task Verification Map, P152-01 is
smoke-only, no existing test file target).
</verification>

<success_criteria>
`/_next/image` serves History-badge assets with HTTP 200 and WebP content; master PNGs are
byte-identical; no other `images` config keys were touched.
</success_criteria>

<output>
Create `.planning/phases/152-public-fansub-gruppenseite-konsolidierung-und-modernisierung/152-01-SUMMARY.md` when done
</output>
