---
phase: 152-public-fansub-gruppenseite-konsolidierung-und-modernisierung
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/fansubs/FansubHeroSection.tsx
autonomous: true
requirements: [P152-06, P152-10]

must_haves:
  truths:
    - "buildInitials's deliberate divergence from getMemberInitials is documented in code, not silently consolidated (C3)."
    - "If the live logo_url/banner_url shape matches an existing remotePattern (verified in Task 1), the Hero logo and banner are served through the Next.js Image optimizer instead of unoptimized, with no layout/crop regression (E1/E2)."
    - "The project-banner image path (E3) is confirmed already-adequate (lazy + correct sizes) and left unchanged, per the user's explicit analysis-only instruction."
  artifacts:
    - path: "frontend/src/components/fansubs/FansubHeroSection.tsx"
      provides: "buildInitials divergence comment; ResponsiveImage-based logo/banner rendering (if Task 1 confirms it is safe)"
      contains: "buildInitials"
  key_links:
    - from: "frontend/src/components/fansubs/FansubHeroSection.tsx"
      to: "frontend/src/components/ui/ResponsiveImage.tsx"
      via: "same optimizer-with-unoptimized-retry wrapper already used by MemberProfileHero.tsx for its backdrop/avatar images"
      pattern: "ResponsiveImage"
---

<objective>
Resolve C3 (document, don't silently merge, the `buildInitials`/`getMemberInitials` divergence) and
investigate/implement E1/E2 (Hero logo + banner Next Image optimization) if the live URL shape makes it
a clean, low-risk swap. Independent file (`page.module.css`-scoped, not `FansubPublicSections.module.css`),
independent of the History-artwork migration.

Purpose: `buildInitials` (group-name initials fallback) and `getMemberInitials`
(`fansubTeamInitials.ts`, person-name initials) look superficially identical but encode genuinely
different truncation rules (confirmed: single-word name -> 1 char vs. 2 chars; 3+-word name -> first-two
words vs. first+last word) — per RESEARCH.md Pitfall 5, USER-REQUEST C3 wants the divergence
DOCUMENTED, not silently merged (merging changes visible Hero fallback-avatar rendering and needs
explicit sign-off, not a default action). Separately, the Hero's logo/banner currently render via plain
`next/image` with a hardcoded `unoptimized` prop; live DB inspection (`logo_url`/`banner_url` values
shaped `/api/v1/media/files/<name>.png`, resolved by `resolveApiUrl` to an absolute URL matching
`next.config.mjs`'s already-configured `configuredApiMediaPatterns()` remotePattern) shows this can very
plausibly be a config-already-sufficient swap.
Output: A documenting code comment on `buildInitials`; Hero logo/banner migrated to
`ResponsiveImage` (the same optimizer-retry wrapper `MemberProfileHero.tsx` already uses for its own
backdrop/avatar) if Task 1's live verification confirms no regression, otherwise a documented
no-change decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-public-fansub-gruppenseite-konsolidierung-und-modernisierung/152-RESEARCH.md
</context>

<interfaces>
<!-- ResponsiveImage's exact contract and an already-live analog usage to replicate. -->

From frontend/src/components/ui/ResponsiveImage.tsx (full file, confirmed at HEAD):
```tsx
export type ResponsiveImageProps = Omit<ImageProps, 'src' | 'unoptimized'> & { src: string }
export function ResponsiveImage({ src, alt, onError, ...props }: ResponsiveImageProps) {
  // tries the Next optimizer first; on onError, retries once with unoptimized=true using the
  // SAME src (never swaps to a different display URL) -- geometry/props never change.
}
```

From frontend/src/components/profile/MemberProfileHero.tsx (lines 161-193, the exact analog pattern —
`fill` for a backdrop banner, fixed `width`/`height` for a logo-equivalent avatar, both with `sizes`
and `loading="eager"` since hero content is always above the fold, never `lazy`):
```tsx
<ResponsiveImage src={backgroundImageURL} alt="" fill sizes="(max-width: 760px) calc(100vw - 24px), (max-width: 1099px) calc(100vw - 48px), 1360px" loading="eager" fetchPriority="high" />
...
<ResponsiveImage src={avatarURL} alt={`${avatarLabel} Avatar`} width={140} height={140} sizes="(max-width: 760px) 100px, (max-width: 1099px) 120px, 140px" loading="eager" />
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verify Hero image URL shape and swap to optimized ResponsiveImage rendering</name>
  <files>frontend/src/components/fansubs/FansubHeroSection.tsx</files>
  <read_first>
    frontend/src/components/fansubs/FansubHeroSection.tsx (full file, 180 lines — the banner block at
    lines 101-113 and logo block at lines 118-131, both currently plain `next/image` with `unoptimized`)
    frontend/next.config.mjs (targeted, lines 7-19, 40-44 — `configuredApiMediaPatterns()` builds a
    `remotePatterns` entry from `NEXT_PUBLIC_API_URL` with `pathname: '/api/v1/media/**'`;
    `dangerouslyAllowLocalIP` is `true` whenever `NODE_ENV !== 'production'`, which the dev-mode
    frontend container satisfies)
    frontend/src/lib/api.ts (targeted, lines 365-385 — `resolveApiUrl`: any value starting with `/api/`
    is routed through `resolvePublicApiUrl`, which prefixes `NEXT_PUBLIC_API_URL`, producing an
    absolute URL matching the remotePattern above)
  </read_first>
  <action>
    Confirm the live URL shape by running
    `docker compose exec -T team4sv30-db psql -U team4s -d team4s_v2 -c "SELECT slug, logo_url, banner_url FROM fansub_groups;"`
    — already confirmed during planning for the `new-subs` group:
    `logo_url = /api/v1/media/files/logo_1787928990830_d3b5184c5dc28cca.png`,
    `banner_url = /api/v1/media/files/banner_1787929528893_61f798bd03c48a67.png` — both start with
    `/api/v1/media/`, matching `configuredApiMediaPatterns()`'s `pathname: '/api/v1/media/**'`
    remotePattern with zero `next.config.mjs` changes required. Re-run the query yourself to confirm
    this still holds at execution time (schema/data drift check).
    If confirmed (expected outcome): replace both `<Image src={bannerURL} ... unoptimized priority />`
    (lines 103-111) and `<Image src={logoURL} ... unoptimized />` (lines 120-127) with
    `<ResponsiveImage>` (import from `@/components/ui/ResponsiveImage`, mirroring
    `MemberProfileHero.tsx`'s import), keeping the exact same `width`/`height`/`className`/`alt` values
    already present, adding a `sizes` prop appropriate to each element's actual rendered box (banner:
    `1200px` fixed per its `styles.heroBannerImg` container; logo: `132px` fixed) and `loading="eager"`
    (hero content is always above the fold — do not use `lazy`). Keep `priority` on the banner
    `<Image>` only if it was already `priority` (it is, line 110) — `ResponsiveImage` forwards all
    `ImageProps` except `src`/`unoptimized`, so `priority` passes through unchanged.
    If NOT confirmed (URL shape differs from the remotePattern at execution time, e.g. an absolute
    external URL or a path outside `/api/v1/media/**`): make NO code change, and document in the plan's
    SUMMARY exactly what URL shape was found and why the swap was not safe (D10 forbids forcing
    optimization when the backend/media origin technically disagrees).
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit"</automated>
  </verify>
  <done>Either: (a) FansubHeroSection.tsx's logo/banner render via ResponsiveImage with no unoptimized prop, tsc is clean, and a live curl against the group's actual resolved logo/banner URL through /_next/image returns 200; or (b) the SUMMARY documents the confirmed URL shape and the reasoned decision not to change it.</done>
</task>

<task type="auto">
  <name>Task 2: Document the buildInitials/getMemberInitials divergence (C3)</name>
  <files>frontend/src/components/fansubs/FansubHeroSection.tsx</files>
  <read_first>
    frontend/src/components/fansubs/FansubHeroSection.tsx (already read in Task 1 — `buildInitials` at
    lines 28-36)
    frontend/src/components/fansubs/fansubTeamInitials.ts (full file — `getMemberInitials`'s
    single-word `.slice(0, 2)` branch vs. `buildInitials`'s uniform one-char-per-word `.map(part =>
    part[0])` behavior, per RESEARCH.md Pitfall 5)
  </read_first>
  <action>
    Add a code comment directly above `buildInitials` (line 28) in
    `frontend/src/components/fansubs/FansubHeroSection.tsx` stating, in German (per CLAUDE.md's
    Sprachqualität rule, with correct umlauts), that this function is deliberately NOT the same as
    `fansubTeamInitials.getMemberInitials`: `buildInitials` takes one character from each of the first
    two words of a GROUP name (e.g. a single-word group name like "Coalguys" yields one letter, "C"),
    while `getMemberInitials` takes two characters for a single-word PERSON name and first+last word
    for 3+-word names. State that consolidating onto `getMemberInitials`'s behavior is a visible
    fallback-avatar rendering change requiring explicit product sign-off, not a default cleanup, per
    `152-USER-REQUEST.md` C3. Do not change `buildInitials`'s logic or its call site.
  </action>
  <verify>
    <automated>grep -q "getMemberInitials" frontend/src/components/fansubs/FansubHeroSection.tsx</automated>
  </verify>
  <done>A German-language comment directly above buildInitials explains the deliberate divergence from getMemberInitials with concrete before/after examples; buildInitials's behavior and call site are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Analyze the project-banner image path (E3, analysis-only)</name>
  <files>N/A — analysis only, no source files modified unless a genuine issue is found</files>
  <read_first>
    frontend/src/components/fansubs/FansubProjectBannerCard.tsx (full file — already confirmed during
    planning to render via `next/image` with `sizes={BANNER_IMAGE_SIZES}` and `loading="lazy"`, i.e.
    already matching E3's "already lazy + correct sizes" bar; it currently also sets `unoptimized`)
  </read_first>
  <action>
    Per USER-REQUEST E3 ("Nur analysieren. Wenn vorhandene Lösung bereits lazy + korrektes sizes nutzt
    und Image-Optimierung schwierig wäre, belassen"), confirm `FansubProjectBannerCard.tsx`'s existing
    `sizes`/`loading="lazy"` usage is correct for its rendered geometry, and confirm whether its
    `resolvedImageUrl` source values also match an existing `next.config.mjs` `remotePatterns`/
    `localPatterns` entry (same check as Task 1, reusing whatever URL-shape finding that task already
    produced if the project-banner source is the same `/api/v1/media/**` shape). Do NOT change this
    file's code in this task — E3 is analysis-only per the user's explicit instruction. Record the
    finding (already-adequate lazy+sizes usage; optimizer-eligibility conclusion either way) in this
    plan's SUMMARY as the closing note for Workstream E.
  </action>
  <verify>
    <automated>grep -q "loading=\"lazy\"" frontend/src/components/fansubs/FansubProjectBannerCard.tsx</automated>
  </verify>
  <done>The SUMMARY records the E3 analysis conclusion (lazy+sizes already correct; optimizer-eligibility finding) with no unintended code change to FansubProjectBannerCard.tsx.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Hero logo/banner image URLs | Server-provided (`group.logo_url`/`banner_url`, not user input) resolved through `resolveApiUrl` into a URL matched against `next.config.mjs`'s existing `remotePatterns` before the Next.js optimizer fetches it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-152-05-01 | Information Disclosure (SSRF-adjacent: local-path/local-IP image optimizer abuse) | `next.config.mjs` `remotePatterns` + `dangerouslyAllowLocalIP` | accept | This plan adds zero new `next.config.mjs` entries — it only removes the `unoptimized` prop on two `<Image>` elements whose `src` already resolves against an existing, narrowly-scoped `remotePatterns` entry (`configuredApiMediaPatterns()`, itself derived from the operator-controlled `NEXT_PUBLIC_API_URL` env var, not user input) |
</threat_model>

<verification>
`tsc --noEmit` clean. If Task 1's swap is applied: a live curl against the resolved
`/_next/image?url=...&w=...` for the actual `logo_url`/`banner_url` values returns 200. Task 2's
grep-based verify confirms the comment references the sibling function by name.
</verification>

<success_criteria>
buildInitials's divergence is documented, not silently merged; Hero image optimization is either
applied (with live 200 proof) or explicitly declined with a documented reason — never silently
skipped.
</success_criteria>

<output>
Create `.planning/phases/152-public-fansub-gruppenseite-konsolidierung-und-modernisierung/152-05-SUMMARY.md` when done
</output>
