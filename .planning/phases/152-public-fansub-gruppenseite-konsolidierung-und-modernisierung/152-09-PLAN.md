---
phase: 152-public-fansub-gruppenseite-konsolidierung-und-modernisierung
plan: 09
type: execute
wave: 3
depends_on: ["152-01", "152-02", "152-03", "152-04", "152-05", "152-06", "152-07", "152-08"]
files_modified: []
autonomous: true
requirements: [P152-06]

must_haves:
  truths:
    - "The full frontend suite (test + typecheck + lint) and full backend suite (go build/vet/test) are green with zero new failures against the pre-152 baseline."
    - "History image delivery before/after (raw PNG vs. optimized WebP) is measured and documented with an exact byte-size and percentage-reduction figure."
    - "Hero image delivery before/after is measured and documented if Plan 152-05 changed it; if not, the documented no-change decision is restated."
    - "A temporary multi-entry History fixture (including a releases_10000-tagged entry) exists in the live database, ready for Plan 152-10's visual QA pass, and is NOT yet removed by this plan."
  artifacts:
    - path: ".planning/phases/152-public-fansub-gruppenseite-konsolidierung-und-modernisierung/152-09-SUMMARY.md"
      provides: "Full regression-gate results + before/after image performance evidence table"
  key_links:
    - from: "152-09-SUMMARY.md"
      to: "live curl measurements against :3000/_next/image"
      via: "documented byte sizes for a History badge PNG before (raw) and after (optimized WebP) the phase's changes"
      pattern: "history-event-badges-transparent"
---

<objective>
Run the complete automated regression gate across every plan landed in Waves 1-2, measure and document
History/Hero image-delivery performance before/after (P152-06), and seed the temporary multi-entry
History fixture (including one `releases_10000`-tagged entry) that Plan 152-10's Visual QA pass needs.
This is a closing block: it depends on all 8 prior plans and runs only after every functional wave is
done (critical_sequencing_constraint #6). It is deliberately autonomous/automation-only — the
human-facing Visual QA checkpoint is Plan 152-10, kept separate per the "checkpoint + implementation
never share a plan" rule.

Purpose: Every prior plan's own task-level verification only proved its own slice; this plan proves the
WHOLE phase compiles/tests/lints/builds together, with no cross-plan regression, and produces the
concrete before/after performance evidence the phase's Definition of Done requires.
Output: A full green regression-gate result; a documented image-performance evidence table; a live,
temporary History fixture ready for the next plan's screenshot pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-public-fansub-gruppenseite-konsolidierung-und-modernisierung/152-RESEARCH.md
@.planning/phases/152-public-fansub-gruppenseite-konsolidierung-und-modernisierung/152-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated regression gate</name>
  <files>N/A — verification/measurement/DB-seed commands only, no source files modified</files>
  <read_first>
    .planning/phases/152-public-fansub-gruppenseite-konsolidierung-und-modernisierung/152-VALIDATION.md
    (the "Full suite command" rows and "Sampling Rate" section this task follows exactly)
  </read_first>
  <action>
    Rebuild and restart both containers to pick up every landed change:
    `docker compose up -d --build team4sv30-backend` and
    `docker compose build team4sv30-frontend && docker restart team4sv30-frontend`. Run the full
    backend suite: `go build ./...` and `go test ./...` inside the `golang:1.25-alpine` container
    (network `team4s_default`, repo mounted at `/src`, working dir `/src/backend`) — compare failure
    count against the documented pre-152 baseline (`151-VERIFICATION.md`/`STATE.md`'s noted
    pre-existing DSN-conditioned failures) and confirm zero NEW failures. Run the full frontend suite
    inside `team4sv30-frontend`: `npm run test`, `npm run typecheck`, `npm run lint` — confirm zero new
    ESLint errors/warnings beyond the pre-existing documented count (`STATE.md` notes 13 pre-existing
    ESLint errors / 332 warnings as of Phase 151) and zero new test failures. Run `git diff --check` at
    the repo root to catch whitespace/conflict-marker issues across every file this phase touched.
  </action>
  <verify>
    <automated>docker run --rm -v /home/d1sk/team4s:/src -w /src/backend --network team4s_default golang:1.25-alpine go build ./...</automated>
  </verify>
  <done>Backend go build/test and frontend test/typecheck/lint all report zero NEW failures relative to the documented pre-152 baseline; git diff --check is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Document History and Hero image-delivery performance before/after (P152-06)</name>
  <files>N/A — verification/measurement/DB-seed commands only, no source files modified</files>
  <read_first>
    .planning/phases/152-public-fansub-gruppenseite-konsolidierung-und-modernisierung/152-CONTEXT.md
    (the "Bildauslieferung (Ist-Zustand, live gemessen)" table — the exact BEFORE figures this task's
    AFTER measurement is compared against: `founding.png` raw = 840090 bytes; initial visible-6-entries
    eager load ≈ 4.8 MB; reference optimized comparison from `member-achievement-badges` = 22496 bytes
    WebP @ w=256, a −99.3% reduction)
  </read_first>
  <action>
    Measure the AFTER state for a History badge: `curl -s -o /dev/null -w '%{size_download}\n'
    'http://192.168.235.196:3000/_next/image?url=%2Fhistory-event-badges-transparent%2Ffounding.png&w=256&q=75'`
    and compare against the BEFORE raw-PNG byte size (840090 bytes, from CONTEXT.md's audit) — compute
    and record the exact percentage reduction. Load the live `/fansubs/new-subs` page (or whichever
    slug the current DB has) with browser devtools network throttling disabled and record the total
    image payload for the initially-visible (first 6) History entries now that they render through
    `AchievementArtwork` with `loading="lazy"` — compare against the documented ~4.8 MB eager-load
    baseline. If Plan 152-05 (Task 1) swapped the Hero logo/banner to `ResponsiveImage`: measure their
    optimized byte sizes the same way and compare against the documented raw baselines (logo 350460
    bytes, banner 499682 bytes); if Plan 152-05 declined the swap, restate its documented no-change
    reason here instead of re-measuring. Record all figures (before, after, percentage reduction) in
    this plan's SUMMARY as the P152-06 evidence table.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" 'http://192.168.235.196:3000/_next/image?url=%2Fhistory-event-badges-transparent%2Ffounding.png&w=256&q=75' | grep -q '^200$'</automated>
  </verify>
  <done>The SUMMARY contains an exact before/after byte-size table for History (and Hero, if changed) images with computed percentage reductions, methodology matching the CONTEXT.md audit's own reference measurement.</done>
</task>

<task type="auto">
  <name>Task 3: Seed a temporary multi-entry History fixture for Visual QA (D10-compliant, not new fixture infrastructure)</name>
  <files>N/A — verification/measurement/DB-seed commands only, no source files modified</files>
  <read_first>
    .planning/phases/152-public-fansub-gruppenseite-konsolidierung-und-modernisierung/152-CONTEXT.md
    (the "Datenlage" section — live DB currently has exactly ONE History entry for `new-subs`
    (`founding`, 2012); D10 explicitly excludes building new large fixture infrastructure, so this task
    is a temporary, manually-scripted set of INSERT statements, removed by Plan 152-10 after the visual
    QA pass, not a durable seed/fixture file)
  </read_first>
  <action>
    Insert 6-8 additional `fansub_group_history` rows for the `new-subs` group (or whichever group the
    live DB currently has) via
    `docker compose exec -T team4sv30-db psql -U team4s -d team4s_v2`, covering: at least one entry per
    category (`history`, `project_count`, `release_count`), one plain-tone entry (e.g. `milestone`),
    the two `emphasis: 'legendary'` entries (`projects_500` and `releases_10000` — the latter is the
    explicitly-required `releases_10000`-tagged entry per the sequencing constraint), and at least one
    entry with a custom admin-authored title (distinct from its registry label, e.g. title
    `"Projektor gekauft"` on a `milestone`-type row) to visually confirm A5's freetext-preservation
    behavior. Use `status='confirmed'` so `listPublicFansubHistory`'s existing WHERE clause includes
    them. Record the exact INSERT statements (with their generated IDs) in this plan's SUMMARY so Plan
    152-10 can reference and later DELETE precisely these rows, and no others, once its screenshot pass
    is complete.
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-db psql -U team4s -d team4s_v2 -tAc "SELECT COUNT(*) FROM fansub_group_history WHERE status='confirmed'" | grep -qE '^[7-9]$|^[1-9][0-9]+$'</automated>
  </verify>
  <done>At least 7 confirmed fansub_group_history rows exist for visual QA, covering all three categories, both legendary-emphasis events, and one custom-titled entry; the exact seeded row IDs are recorded in the SUMMARY for later cleanup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Direct DB write via `docker compose exec ... psql` | Operator-controlled, not user-facing; temporary rows are explicitly tracked for removal in the next plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-152-09-01 | Tampering (leaving test data in the live database) | Temporary `fansub_group_history` seed rows | mitigate | Row IDs are explicitly recorded in this plan's SUMMARY specifically so Plan 152-10 can delete exactly those rows and no others; D10 forbids this becoming durable fixture infrastructure |
</threat_model>

<verification>
Full backend/frontend regression suites green with zero new failures. Live curl-measured image
performance evidence recorded. At least 7 confirmed History rows exist for the next plan's Visual QA.
</verification>

<success_criteria>
The complete phase compiles, tests, lints, and builds together with no cross-plan regression; History
(and Hero, if applicable) image-delivery performance is documented with exact before/after figures; a
temporary, precisely-tracked History fixture is ready for Plan 152-10.
</success_criteria>

<output>
Create `.planning/phases/152-public-fansub-gruppenseite-konsolidierung-und-modernisierung/152-09-SUMMARY.md` when done
</output>
