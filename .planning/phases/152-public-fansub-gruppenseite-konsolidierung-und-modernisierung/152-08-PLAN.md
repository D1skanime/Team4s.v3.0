---
phase: 152-public-fansub-gruppenseite-konsolidierung-und-modernisierung
plan: 08
type: execute
wave: 2
depends_on: ["152-03"]
files_modified:
  - backend/internal/repository/fansub_public_profile_query_budget_test.go
autonomous: true
requirements: [P152-09]

must_haves:
  truths:
    - "A single GetPublicProfileBySlug call issues the same fixed number of SQL queries regardless of how many projects/members/history/media rows the group has (no N+1, constant budget)."
    - "The new constant is documented and lower than the pre-152 baseline of 12, reflecting Plan 152-03's B1/B2 reduction."
    - "GetFansubGroupDomainProjection's query count no longer includes the contributors query, and its JSON response's contributors array stays empty even for a group with real contributor rows in the database."
  artifacts:
    - path: "backend/internal/repository/fansub_public_profile_query_budget_test.go"
      provides: "Constant-query-budget gate for both public-profile and domain-projection load paths"
      exports: ["TestFansubPublicProfileQueryBudgetIsConstant", "TestDomainProjectionQueryBudgetExcludesContributors"]
  key_links:
    - from: "backend/internal/repository/fansub_public_profile_query_budget_test.go"
      to: "backend/internal/repository/query_counter.go"
      via: "pgx.QueryTracer attached via pgxpool.Config.ConnConfig.Tracer, following the Phase-131 member_profile_query_budget_test.go template"
      pattern: "queryCounter"
---

<objective>
Add the query-budget regression gate (B4) proving Plan 152-03's public group load-path reduction (12 ->
7 static queries) and domain-projection contributors removal (3 -> 2) are both real and constant
(no N+1 growth with project/member/history/media/contributor volume). Depends on Plan 152-03
(`fansub_repository.go` + `domain_projection_repository.go` changes must already be in place) — this is
why it is a separate, later-wave plan rather than bundled into 152-03 (keeps that plan's context budget
manageable and avoids a DB-creation race with any parallel Wave-1 plan).

Purpose: `152-CONTEXT.md`'s audit found "kein Query-Budget-Test für die Fansub-Public-Seite, obwohl
Harness existiert" — the `pgx.QueryTracer`-based `query_counter.go` infra and its two existing templates
(`member_profile_query_budget_test.go`, `admin_users_query_budget_test.go`) are purpose-built for
exactly this and already proven in production use; this plan reuses that harness verbatim rather than
inventing new instrumentation (D04).
Output: A new guarded-Postgres test file pinning the constant query-budget for both the public-profile
and domain-projection endpoints, reusing the `team4s_phase152_test` database Plan 152-03 already
created, and closing the loop on B3's real behavioral proof (empty contributors despite seeded
contributor rows).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-public-fansub-gruppenseite-konsolidierung-und-modernisierung/152-RESEARCH.md
@.planning/phases/152-public-fansub-gruppenseite-konsolidierung-und-modernisierung/152-PATTERNS.md
</context>

<interfaces>
<!-- The exact template this file's scaffold copies (already fully read during planning). -->

From backend/internal/repository/member_profile_query_budget_test.go (full file, 214 lines — the
`openPhase131Postgres`/`resetPhase131Fixtures`/`mustExecPhase131`/`phase131ConstantQueryBudget`/
`TestPhase131PublicProfileQueryBudgetIsConstant` scaffold, `131` -> `152` substitution target):
```go
const phase131DSNEnv = "TEAM4S_PHASE131_TEST_DSN"
var phase131DatabasePattern = regexp.MustCompile(`^team4s_phase131_test(?:_[a-z0-9]+)?$`)
func openPhase131Postgres(t *testing.T) (*pgxpool.Pool, *queryCounter) { /* DSN parse, DB-name guard,
  tracer attach, runtime-DB self-check via SELECT current_database() */ }
const phase131ConstantQueryBudget = 20
func TestPhase131PublicProfileQueryBudgetIsConstant(t *testing.T) {
  // seeds few/many, resets counter, calls the repository method twice, asserts equal counts AND
  // equal to the pinned constant
}
```

The DSN for the throwaway Phase-152 test database (created in Plan 152-03, same credentials as the
backend container's `DATABASE_URL`, only the database name differs):
`postgres://team4s:team4s_dev_password@team4sv30-db:5432/team4s_phase152_test?sslmode=disable`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Constant-budget gate for the public group profile load path</name>
  <files>backend/internal/repository/fansub_public_profile_query_budget_test.go</files>
  <read_first>
    backend/internal/repository/member_profile_query_budget_test.go (full file, already read during
    planning — the exact scaffold this task copies)
    backend/internal/repository/fansub_public_profile_load_path_test.go (Plan 152-03's output — check
    whether `phase152DSNEnv`/`phase152DatabasePattern`/`openPhase152Postgres` are already declared
    there; if so, reuse those exact symbols from this same `repository` package instead of
    redeclaring them, since Go forbids duplicate top-level declarations in one package)
    backend/internal/repository/fansub_repository.go (post-152-03 state — `GetPublicProfileBySlug`,
    `getPublicGroupBase`, `attachPublicReleaseVersionsCount`)
  </read_first>
  <action>
    Create `fansub_public_profile_query_budget_test.go` in package `repository`, reusing the
    `team4s_phase152_test` database Plan 152-03 already created and schema-loaded (re-run the same
    idempotent check-then-create commands as a defensive fallback in case this runs against a fresh
    environment: `docker compose exec -T team4sv30-db psql -U team4s -d team4s_v2 -tAc "SELECT 1 FROM
    pg_database WHERE datname='team4s_phase152_test'"`, creating and schema-loading it only if absent).
    If `openPhase152Postgres` and its DSN-env/DB-name-pattern already exist in
    `fansub_public_profile_load_path_test.go` (Plan 152-03's file, same package), import and reuse them
    directly rather than redeclaring. Seed two groups: one with a SMALL row count (1 project, 1 history
    entry, 1 media item, 2 active members) and one with a LARGE row count (6 projects, 6 history
    entries, 6 media items, 6 active members) — mirroring `seedPhase131MemberWithCurrentProjects`'s
    few/many pattern. Reset the counter, call `repo.GetPublicProfileBySlug(ctx, smallGroupSlug)`,
    record the count; reset again, call for the large group, record the count. Assert both counts are
    EQUAL (`require.Equal`) via `TestFansubPublicProfileQueryBudgetIsConstant`, and assert the equal
    value matches a new named constant `const phase152PublicProfileConstantQueryBudget = 7`
    (documenting in a comment, mirroring `phase131ConstantQueryBudget`'s own comment style, that this
    reflects Plan 152-03's B1/B2 reduction from the pre-152 baseline of 12: `getPublicGroupBase` 1 +
    `attachPublicReleaseVersionsCount` 1 + stories/projects/history/media listers 4 + `ListGroupLinks`
    1 = 7). If the measured constant differs from 7, use the ACTUALLY MEASURED value as the pinned
    constant instead and document why in the comment — do not force a wrong number to pass.
  </action>
  <verify>
    <automated>docker run --rm -v /home/d1sk/team4s:/src -w /src/backend --network team4s_default -e TEAM4S_PHASE152_TEST_DSN='postgres://team4s:team4s_dev_password@team4sv30-db:5432/team4s_phase152_test?sslmode=disable' golang:1.25-alpine go test ./internal/repository/... -run 'FansubPublicProfileQueryBudgetIsConstant' -v</automated>
  </verify>
  <done>TestFansubPublicProfileQueryBudgetIsConstant passes, proving the public load path issues an identical, pinned query count regardless of project/history/media/member volume, and that count is documented as Plan 152-03's post-reduction constant.</done>
</task>

<task type="auto">
  <name>Task 2: Constant-budget + behavioral proof for the domain-projection contributors removal</name>
  <files>backend/internal/repository/fansub_public_profile_query_budget_test.go</files>
  <read_first>
    backend/internal/repository/domain_projection_repository.go (post-152-03 state — confirm
    `GetFansubGroupDomainProjection` no longer calls `listProjectionContributors`)
  </read_first>
  <action>
    In the same file, add `TestDomainProjectionQueryBudgetExcludesContributors`: seed a group that has
    at least one REAL row satisfying `listProjectionContributors`'s WHERE clause (a `hist_fansub_group_members`
    row with `visibility='public'`, a `members` row with `profile_visibility='public'`, an
    `anime_contributions` row with `is_public_on_anime_page=true` and a matching approved
    `review_statuses`/`visibilities` row, per the query read during Plan 152-03's grounding) so that,
    were the OLD code path still active, `Contributors` would be non-empty. Reset the counter, call
    `domainRepo.GetFansubGroupDomainProjection(ctx, groupID)`, assert: (1) the returned
    `response.Contributors` is an empty (non-nil, length-0) slice DESPITE the seeded contributor row —
    the real behavioral proof that B3's removal actually works, not just that the call is textually
    absent; (2) the measured query count equals a new named constant
    `const phase152DomainProjectionConstantQueryBudget = 2` (documenting the pre-152 baseline of 3,
    contributors query removed). If the measured count differs from 2, use the measured value and
    document why.
  </action>
  <verify>
    <automated>docker run --rm -v /home/d1sk/team4s:/src -w /src/backend --network team4s_default -e TEAM4S_PHASE152_TEST_DSN='postgres://team4s:team4s_dev_password@team4sv30-db:5432/team4s_phase152_test?sslmode=disable' golang:1.25-alpine go test ./internal/repository/... -run 'DomainProjectionQueryBudgetExcludesContributors' -v</automated>
  </verify>
  <done>TestDomainProjectionQueryBudgetExcludesContributors passes: a group with a real seeded contributor row still returns an empty Contributors slice from GetFansubGroupDomainProjection, and the query count equals the pinned, documented post-152 constant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | Test-only instrumentation (`pgx.QueryTracer`) against a dedicated throwaway database; never wired into any production request path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-152-08-01 | Tampering (accidental run against the live dev database) | `TEAM4S_PHASE152_TEST_DSN` handling | mitigate | Fail-closed DB-name-guard regex (`^team4s_phase152_test(?:_[a-z0-9]+)?$`) plus a runtime `SELECT current_database()` self-check, identical to the Phase-131 precedent — the test refuses to run against any database not matching this exact pattern, so it can never touch `team4s_v2` |
</threat_model>

<verification>
Both new tests green with `TEAM4S_PHASE152_TEST_DSN` set; running `go test ./internal/repository/...`
without the DSN set skips (not fails) these tests, matching the Phase-131 skip-if-unset convention.
</verification>

<success_criteria>
The public-profile and domain-projection endpoints both have a pinned, documented, empirically-measured
constant query budget; the domain-projection's contributors removal is proven with a real seeded row,
not a source-text absence check alone.
</success_criteria>

<output>
Create `.planning/phases/152-public-fansub-gruppenseite-konsolidierung-und-modernisierung/152-08-SUMMARY.md` when done
</output>
