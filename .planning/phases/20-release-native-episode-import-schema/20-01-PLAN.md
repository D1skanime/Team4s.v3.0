---
phase: 20-release-native-episode-import-schema
plan: 01
type: schema
wave: 1
depends_on: []
files_modified:
  - docs/architecture/db-schema-v2.md
  - database/migrations
  - backend/internal/repository
  - backend/internal/models
autonomous: true
requirements:
  - P20-SC1
  - P20-SC2
  - P20-SC3
must_haves:
  truths:
    - "A controlled local dev reset removes old anime/episode/import data before Naruto verification starts."
    - "The database has every missing table, column, constraint, and index required for release-native imports."
    - "The schema can represent one real media file covering multiple canonical episodes."
---

<objective>
Prepare the database for release-native episode imports and make the reset path reproducible.

Purpose: Phase 20 must not build more frontend behavior on the legacy `episode_versions` shape.
Output: Additive migrations, schema tests or migration checks, and a controlled local reset command/script for dev verification.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-release-native-episode-import-schema/20-CONTEXT.md
@DECISIONS.md
@docs/architecture/db-schema-v2.md

Read before editing:
@database/migrations
@backend/internal/repository/episode_import_repository.go
@backend/internal/models
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a controlled local import reset</name>
  <files>scripts or backend/dev tooling, documentation in STATUS.md if command changes</files>
  <action>
    Create or document a safe local-only reset that clears anime, episode, release, stream, and generated media import state so Naruto verification starts clean. The reset must avoid production ambiguity and be executable from the repo with Docker/dev DB settings.
  </action>
  <verify>
    <automated>Run the reset against the local Docker database, then query counts for anime, episodes, episode_titles, fansub_releases, release_versions, release_variants, release_streams, release_variant_episodes, and legacy episode_versions.</automated>
  </verify>
  <done>Local dev data can be cleared reproducibly before Phase 20 tests without manual clicking through the UI.</done>
</task>

<task type="auto">
  <name>Task 2: Align the architecture schema and add missing normalized schema pieces</name>
  <files>docs/architecture/db-schema-v2.md, database/migrations, backend/internal/repository schema tests if present</files>
  <action>
    Use `docs/architecture/db-schema-v2.md` as the canonical source for the Phase 20 additions: filler metadata on `Episode`, `EpisodeFillerType`, and normalized release coverage for one file covering multiple canonical episodes. Then add migrations for missing fields and constraints:
    - episode filler fields: `filler_type_id`, `filler_source`, `filler_note`
    - `EpisodeFillerType` lookup values: `unknown`, `canon`, `filler`, `mixed`, `recap`
    - episode number text/display fields if absent: `number_text`, and confirm `number_decimal`
    - `release_variants` unique `(release_version_id, filename)`
    - `release_variant_episodes` coverage join with indexes by variant and episode
    - any missing modified/modified_by fields only where existing conventions require them
    Keep `release_streams` as the canonical stream table because it already references `stream_sources`.
  </action>
  <verify>
    <automated>Run migrations from a clean local DB and assert all tables/constraints exist. Run backend repository tests that depend on the new schema.</automated>
  </verify>
  <done>The architecture document and executable schema match the target model plus the required multi-episode coverage join.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the schema contract in tests or audit output</name>
  <files>backend/internal/repository tests or database schema audit tooling</files>
  <action>
    Add a focused automated check that fails if the release-native tables/columns/indexes required by Phase 20 disappear or are renamed accidentally.
  </action>
  <verify>
    <automated>Run the new schema contract check plus the existing backend test package that owns migrations/repositories.</automated>
  </verify>
  <done>Future agents cannot silently fall back to the old episode_versions-only model.</done>
</task>

</tasks>

<success_criteria>
- A clean local DB can be prepared without UI hand cleanup.
- `docs/architecture/db-schema-v2.md` is updated before migrations are written.
- Missing schema pieces for filler, titles, releases, streams, and multi-episode coverage are present.
- The chosen canonical stream table is documented and tested.
</success_criteria>

<output>
Create `20-01-SUMMARY.md` with reset command, migration names, and schema verification output.
</output>
