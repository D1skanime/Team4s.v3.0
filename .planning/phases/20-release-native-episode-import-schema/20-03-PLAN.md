---
phase: 20-release-native-episode-import-schema
plan: 03
type: frontend
wave: 3
depends_on:
  - 20-02-PLAN.md
files_modified:
  - frontend/src/app/admin/anime/[id]/episodes/import/page.tsx
  - frontend/src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.ts
  - frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.ts
  - frontend/src/types/episodeImport.ts
autonomous: true
requirements:
  - P20-SC4
  - P20-SC5
must_haves:
  truths:
    - "The import UI exposes release source, group, version, variant, and filler context before apply."
    - "Operators can correct season-split Jellyfin files into canonical AniSearch episode numbers."
    - "A single file can be intentionally mapped to multiple canonical episodes from the UI."
---

<objective>
Update the operator workbench so the new schema is visible and controllable before apply.

Purpose: The backend cannot infer every real-library release detail correctly; the UI must make correction practical.
Output: Import UI and DTO updates for release-native mapping, filler display, source/group/version edits, and multi-target confirmation.
</objective>

<context>
@.planning/phases/20-release-native-episode-import-schema/20-CONTEXT.md
@.planning/phases/19-episode-import-operator-workbench/19-UAT.md
@frontend/src/app/admin/anime/[id]/episodes/import/page.tsx
@frontend/src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.ts
@frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.ts
@frontend/src/types/episodeImport.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Show canonical episode metadata and filler status</name>
  <files>frontend/src/app/admin/anime/[id]/episodes/import/page.tsx, frontend/src/types/episodeImport.ts</files>
  <action>
    Render canonical episode number, preferred title, available title languages if compact enough, and filler badge/status in the preview workbench.
  </action>
  <verify>
    <automated>Frontend component/helper tests cover filler and title fallback display. Typecheck passes.</automated>
  </verify>
  <done>Operators can see whether a target episode is filler and which title came from AniSearch.</done>
</task>

<task type="auto">
  <name>Task 2: Add release fields to mapping rows</name>
  <files>frontend/src/app/admin/anime/[id]/episodes/import/page.tsx, frontend/src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.ts, frontend/src/types/episodeImport.ts</files>
  <action>
    Add compact controls or editable fields for source/group/version/variant metadata where backend detection is uncertain. Keep good auto-detected values visible, but let the operator override before apply.
  </action>
  <verify>
    <automated>Frontend tests prove edited release metadata is included in the apply payload.</automated>
  </verify>
  <done>Real release identity can be corrected without database hand edits.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve practical bulk mapping for Naruto-style season splits</name>
  <files>frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.ts, frontend/src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.ts</files>
  <action>
    Keep Phase 19 bulk confirmation controls while supporting comma-list multi-target mappings such as `9,10`. Ensure bulk actions do not erase intentional multi-target coverage.
  </action>
  <verify>
    <automated>Mapping helper tests cover season-offset suggestions, multi-target rows, bulk confirm, and apply-button enablement.</automated>
  </verify>
  <done>The UI remains fast enough for hundreds of Naruto rows and still supports special combined episodes.</done>
</task>

</tasks>

<success_criteria>
- The workbench exposes the data that will be written to the release graph.
- Filler status is visible before apply.
- Multi-target and bulk mapping remain practical at Naruto scale.
</success_criteria>

<output>
Create `20-03-SUMMARY.md` with screenshots or notes for the Naruto-scale workbench state.
</output>

