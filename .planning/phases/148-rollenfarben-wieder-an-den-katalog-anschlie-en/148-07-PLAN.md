---
phase: 148-rollenfarben-wieder-an-den-katalog-anschlie-en
plan: 07
type: execute
wave: 3
depends_on: ["148-01", "148-02", "148-03", "148-04", "148-05", "148-06"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Backend build/vet/test, frontend typecheck/lint/test/build, and the OpenAPI contract all run green with zero new failures versus the pre-phase baseline"
    - "A live UAT on :3000 (via the 127.0.0.1:3300 tunnel) shows an actually rendered, role-dependent color on the release-detail public note card and on the project-member page, measured via getComputedStyle, not by eye"
  artifacts: []
  key_links: []
---

<objective>
Close Phase 148 with the same full regression sweep + live UAT gate pattern established by Phase
147 Plan 06 (`147-06-SUMMARY.md`): run backend/frontend/contract automation across all six prior
plans' combined changes, confirm zero new failures versus the pre-phase baseline, then verify live
on `:3000` that the previously-dead role color now actually renders — measured via
`getComputedStyle`, satisfying ROADMAP Phase 148 Success Criterion 8 exactly as written.

Purpose: each of Plans 148-01 through 148-06 was verified in isolation; this plan is the
integration/verification gate proving they compose correctly on `main` and that the phase's
observable goal (rollenfarben are visible again, app-wide, from exactly one source) is true in a
running instance, not just in unit tests.

Output: a documented regression sweep result table (mirroring `147-06-SUMMARY.md`'s format) and a
live UAT sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-rollenfarben-wieder-an-den-katalog-anschlie-en/148-UI-SPEC.md
@.planning/phases/147-rollen-registry-letzte-parallelkataloge-aufl-sen/147-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated regression sweep</name>
  <files></files>
  <read_first>
    .planning/phases/147-rollen-registry-letzte-parallelkataloge-aufl-sen/147-06-SUMMARY.md (the exact command set and baseline-comparison method to replicate: go build/vet/test, targeted repository/permissions/models/handlers packages, real-Postgres integration test, npx tsc --noEmit, npx vitest run, npx eslint src, docker compose build)
  </read_first>
  <action>
    On `team4s-linux`, with the current tip of `main` (all six prior plans landed): run `go build
    ./...`, `go vet ./...`, and the full `go test ./...` — compare the failing-test set against the
    pre-Phase-148 baseline (a `git archive` checkout of the commit immediately preceding Plan
    148-01's first commit) exactly as Phase 147's Plan 06 did, confirming the failing set is
    identical (i.e. zero new backend failures introduced by this phase). Run
    `go test ./internal/repository/... -run TestPublicNoteRoleCode` with
    `TEAM4S_PHASE117_TEST_DSN` set, confirming the new role_color_key sub-case passes. Run
    `npx tsc --noEmit`, `npx vitest run` (all frontend tests), and `npx eslint src` in the frontend
    container/workspace, confirming zero new failures/errors versus the same pre-phase baseline
    comparison method. Run `docker compose build team4sv30-frontend` (production build parity, per
    Phase 147's precedent) to confirm the production build succeeds. Re-confirm via grep, repo-wide,
    that zero remaining references exist to: `categoryForRole`, `getRoleClassName` inside
    `FansubAppMembersOverview.tsx` specifically (the other two files' same-named functions are
    intentionally untouched, see Plan 148-04), and any `--role-accent-<code>`/`-default`/`-base`
    token outside `LayeredBadgeArtwork.module.css`.
  </action>
  <acceptance_criteria>
    <automated>go build ./... and go vet ./... both exit 0</automated>
    <automated>go test ./... failing-test set is identical to the documented pre-Phase-148 baseline (zero new failures)</automated>
    <automated>npx tsc --noEmit exits 0; npx vitest run reports zero new failures versus baseline</automated>
    <automated>grep -rn "categoryForRole" frontend/src returns empty</automated>
    <automated>grep -rln -- "--role-accent-" frontend/src returns only frontend/src/components/profile/LayeredBadgeArtwork.module.css (if it appears there at all) or nothing</automated>
  </acceptance_criteria>
  <done>Backend build/vet/test, frontend typecheck/lint/test/build, and the two grep gates above all confirm zero new failures and zero remaining dead-token/dead-function references outside the explicitly-excluded files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live UAT sign-off on :3000</name>
  <files></files>
  <action>Present the automated regression sweep results from Task 1, then walk through the five live-UAT checks below via the 127.0.0.1:3300 tunnel and wait for explicit human sign-off before the phase is considered complete.</action>
  <what-built>
    All ten previously-dead CSS-module role-accent chains are reconnected to
    `role_definitions.color_key` via the `data-color-key`/`--role-chip-accent`/`--role-accent`
    seam; `categoryForRole()`, `getRoleClassName()`'s broken colorClassMap, and every dead
    `--role-accent-<code>` token reference are removed; the public note card now carries
    `role_color_key` end-to-end with no extra client-side fetch.
  </what-built>
  <how-to-verify>
    Via the `127.0.0.1:3300` SSH tunnel to `team4s-linux` (`http://192.168.235.196:3000` is the
    Linux frontend itself, per `CLAUDE.md`'s canonical dev environment):
    1. Open a release-detail page with public notes (e.g.
       `/anime/1/group/1/releases/46`, matching Phase 147's UAT fixture) and confirm the note
       card header band, role text, avatar, and bottom stripe now show a distinct, non-gray color
       per role — not the previous transparent/gray-fallback appearance. Open DevTools, select the
       note card's `<article>`, and confirm `getComputedStyle(el).getPropertyValue('--role-accent')`
       returns a real hex value (not empty string) and `el.dataset.colorKey` matches
       `role_definitions.color_key` for that note's role.
    2. Open a project-member page (e.g.
       `/fansubs/new-subs/fansubprojekt/buddy-complex/mitwirkende/type`, same fixture as Phase 147)
       and confirm the role chips render distinct per-role colors, including at least one
       leadership role (`fansub_lead`/`founder`/`co_leader`/`techadmin`/`gfxler`) showing its own
       hex rather than neutral gray (proves the presentationForRole icon/color decoupling from
       Plan 148-01 actually took effect visually).
    3. Open the member profile badge carousel for a member with an earned role badge and confirm
       the role-progress card (RoleBadgeCard) also shows a distinct per-role color, not gray.
    4. Open `/admin/fansubs/{id}/edit`, the members tab, and confirm role badges there (previously
       always falling back to `fansubEditRoleDefault`) now show distinct per-role colors via
       DevTools `getComputedStyle` on a badge element.
    5. Confirm `LayeredBadgeArtwork`-driven badge artwork (the member profile's earned-badge
       hero art) is visually unchanged (still its own hardcoded teal `#17a7a5`), proving this
       phase did not touch that file.
  </how-to-verify>
  <resume-signal>Type "approved" once all five checks render/behave as described, or describe which check failed and its observed vs. expected color/attribute.</resume-signal>
  <done>Human verifier has typed "approved" for all five live checks; ROADMAP Phase 148 Success Criterion 8 is confirmed true against the running system.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Verification-only plan; no code changes, no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-12 | none applicable | regression/UAT gate | accept | This plan performs no code changes; it only runs existing automation and a human visual/DOM check against already-reviewed changes from Plans 148-01 through 148-06. |
</threat_model>

<verification>
- Full backend/frontend/contract regression sweep green with zero new failures versus the documented pre-phase baseline.
- Live UAT sign-off across all five checks above, approved by the human verifier.
</verification>

<success_criteria>
- ROADMAP Phase 148 Success Criteria 1-9 are all independently confirmed true against the live running system (not just unit tests).
- Zero new backend/frontend/contract test failures versus the pre-Phase-148 baseline.
</success_criteria>

<output>
Create `.planning/phases/148-rollenfarben-wieder-an-den-katalog-anschlie-en/148-07-SUMMARY.md` when done
</output>
