---
phase: 148-rollenfarben-wieder-an-den-katalog-anschlie-en
plan: 08
type: execute
gap_closure: true
wave: 4
depends_on: ["148-04", "148-07"]
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/GroupMembersHistTable.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/GroupMembersHistTable.test.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/FansubAppMemberAddModal.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/FansubAppMemberAddModal.test.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css
autonomous: true
requirements: []
must_haves:
  truths:
    - "grep -rln -- '--role-accent-' frontend/src returns only frontend/src/components/profile/MemberCurrentProjectsSection.test.tsx (a benign false positive inside a negative-assertion regex literal) — zero live CSS references remain outside LayeredBadgeArtwork.module.css's own intentional value"
    - "GroupMembersHistTable.tsx and FansubAppMemberAddModal.tsx no longer define or call a local getRoleClassName()/roleClassMap; their role badges/buttons carry data-color-key from presentationForRole(roles, code).colorKey, matching the seam already used by FansubAppMembersOverview.tsx"
    - "FansubEdit.module.css's .fansubEditRoleBadge and .fansubEditRoleOption base rules keep their exact pre-existing background/border/color-mix formulas, with only the dead --role-accent: var(--role-accent-base) self-assignment removed; the nine role-specific class blocks that fed the dead --role-accent-<code> tokens are deleted entirely"
  artifacts:
    - path: "frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css"
      provides: "Base role-badge/role-option formulas reading --role-accent from the [data-color-key] seam instead of a dead token; zero role-specific dead-token class blocks"
  key_links:
    - from: "frontend/src/app/admin/fansubs/[id]/edit/GroupMembersHistTable.tsx"
      to: "frontend/src/styles/globals.css"
      via: "data-color-key attribute on the role Badge, consumed by the [data-color-key] -> --role-accent derivation"
      pattern: "data-color-key={presentationForRole(roles, role.role_code).colorKey}"
    - from: "frontend/src/app/admin/fansubs/[id]/edit/FansubAppMemberAddModal.tsx"
      to: "frontend/src/styles/globals.css"
      via: "data-color-key attribute on the role-option Button, consumed by the [data-color-key] -> --role-accent derivation"
      pattern: "data-color-key={presentationForRole(roles, option.code).colorKey}"
---

<objective>
Close the gap `148-VERIFICATION.md` found against ROADMAP Phase 148 Success Criteria 1 and 2: two
sibling admin components on the fansub-edit page — `GroupMembersHistTable.tsx` (historical member
role badges) and `FansubAppMemberAddModal.tsx` (role-selection buttons in the add-member/invite
modal) — each maintained their own local, role-code-keyed `getRoleClassName()`/`roleClassMap`,
identical in shape to the one Plan 148-04 already removed from `FansubAppMembersOverview.tsx`, just
uncaught because they live in different files. Both selected classes defined in
`FansubEdit.module.css` that self-assigned `--role-accent` from `--role-accent-<code>` tokens
undefined anywhere in the repo — the exact dead-token pattern this phase exists to fix, still live
on two rendered admin surfaces.

Purpose: make ROADMAP Phase 148 Success Criteria 1 ("kein CSS-Modul weist sich mehr eine Farbe aus
einem `--role-accent-<code>`-Token zu, und kein Selektor wählt eine Farbe anhand eines
Rollen-Codes") and 2 ("keine Referenz auf die nicht existierenden Tokens ... bleibt im Repo übrig")
literally true, not just true for the one file 148-04 happened to touch.

Output: both components migrated onto the same `presentationForRole()`/`data-color-key` seam
148-04 already established as the pattern; the now-fully-unused role-specific CSS class blocks
removed from `FansubEdit.module.css`; a dedicated data-color-key-seam test added to each
component's existing test file, mirroring the SC7 test 148-04 added for
`FansubAppMembersSection.test.tsx`.
</objective>

<context>
@.planning/phases/148-rollenfarben-wieder-an-den-katalog-anschlie-en/148-UI-SPEC.md
@.planning/phases/148-rollenfarben-wieder-an-den-katalog-anschlie-en/148-VERIFICATION.md
@.planning/phases/148-rollenfarben-wieder-an-den-katalog-anschlie-en/148-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate GroupMembersHistTable.tsx and FansubAppMemberAddModal.tsx onto the data-color-key seam; remove the dead CSS class blocks</name>
  <files>
    frontend/src/app/admin/fansubs/[id]/edit/GroupMembersHistTable.tsx
    frontend/src/app/admin/fansubs/[id]/edit/FansubAppMemberAddModal.tsx
    frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css
  </files>
  <action>
    In both TSX files: remove the local `getRoleClassName()` function; add
    `useRoleCatalog('fansub_group')` (already globally available via the root-layout
    `RoleCatalogProvider`, same pattern as `FansubAppMembersOverview.tsx`) to get `roles`; replace
    each `getRoleClassName(code)` call site with `data-color-key={presentationForRole(roles,
    code).colorKey}` on the same element, keeping the existing base class
    (`fansubEditRoleBadge`/`fansubEditRoleOption`) and dropping only the dead role-specific class
    from the className list.

    In `FansubEdit.module.css`: remove the `--role-accent: var(--role-accent-base);` line from
    `.fansubEditRoleBadge` and `.fansubEditRoleOption` (every other declaration in both rules stays
    byte-for-byte unchanged — Restoration Rule). Delete the nine now-fully-unused role-specific
    class blocks (`.fansubEditRoleLead`, `.fansubEditRoleProjectLead`, `.fansubEditRoleEditor`,
    `.fansubEditRoleTranslator`, `.fansubEditRoleTimer`, `.fansubEditRoleTypesetter`,
    `.fansubEditRoleQuality`, `.fansubEditRoleEncoder`, `.fansubEditRoleDefault`) entirely.
  </action>
  <acceptance_criteria>
    <automated>grep -rn "getRoleClassName" frontend/src returns empty</automated>
    <automated>grep -rln -- "--role-accent-" frontend/src returns only frontend/src/components/profile/MemberCurrentProjectsSection.test.tsx</automated>
    <automated>npx tsc --noEmit reports zero new errors (the one pre-existing generated-route-type error is unaffected)</automated>
  </acceptance_criteria>
  <done>Both components render role color via data-color-key/presentationForRole; the dead CSS class blocks are gone; the two grep gates pass exactly as specified.</done>
</task>

<task type="auto">
  <name>Task 2: Add data-color-key-seam tests to both components' existing test files</name>
  <files>
    frontend/src/app/admin/fansubs/[id]/edit/GroupMembersHistTable.test.tsx
    frontend/src/app/admin/fansubs/[id]/edit/FansubAppMemberAddModal.test.tsx
  </files>
  <action>
    Add a `vi.mock('@/providers/RoleCatalogProvider', ...)` with a hoisted mock catalog to each test
    file (neither used the hook before this plan). For each component, add tests proving via real
    render + assertion (not source substring search): (1) the role badge/button carries
    `data-color-key` matching the catalog's `color_key` for that role code, and no
    role-code-specific dead class remains in its className; (2) changing the catalog's `color_key`
    changes the rendered `data-color-key`, while changing only `label_de` does not — the same
    seam-independence property 148-04 proved for `FansubAppMembersSection.test.tsx`.
  </action>
  <acceptance_criteria>
    <automated>npx vitest run on both test files passes with the new tests included</automated>
  </acceptance_criteria>
  <done>Both test files carry a real-render proof of the data-color-key seam, including the color_key-changes/label_de-does-not-change property.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Same trust boundary as 148-04: `data-color-key` only ever carries a value already present in the already-fetched, already-authorized role catalog. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-148-13 | Information Disclosure | data-color-key attribute | accept | Same disposition as T-148-01 (148-01-PLAN.md): value is a bounded hex or 'neutral', already present in the authorized catalog payload — no new field, no new trust boundary. |
</threat_model>

<verification>
- `grep -rln -- "--role-accent-" frontend/src` returns only the one benign test-file false positive.
- `grep -rn "getRoleClassName" frontend/src` returns empty.
- Full frontend test suite green; `tsc --noEmit`/`eslint` clean on touched files (pre-existing route-type error unaffected).
</verification>

<success_criteria>
- ROADMAP Phase 148 Success Criteria 1 and 2 are true repo-wide, not just for the one file 148-04 touched.
- `148-VERIFICATION.md`'s SC1/SC2 findings are fully addressed, ready for re-verification.
</success_criteria>

<output>
Create `.planning/phases/148-rollenfarben-wieder-an-den-katalog-anschlie-en/148-08-SUMMARY.md` when done
</output>
