---
phase: 148-rollenfarben-wieder-an-den-katalog-anschlie-en
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/audits/2026-09-05-hardcoding-drift-audit.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "HC-09's audit table and prose no longer claim 'null Referenzen im gesamten Repo' (zero references repo-wide) for RoleTranslator/RoleTypesetter/RoleTechadmin/RoleGfxler"
    - "The correction explains the counting method that produced the original 'Test 0' figure (only qualified permissions.X references were counted, missing unqualified in-package test fixture usages) and records that Phase 147 already removed the four constants and converted those fixtures to literals"
  artifacts:
    - path: ".planning/audits/2026-09-05-hardcoding-drift-audit.md"
      provides: "Corrected HC-09 section"
  key_links: []
---

<objective>
Correct a single factual error in HC-09 of `.planning/audits/2026-09-05-hardcoding-drift-audit.md`
(CONTEXT decision #7, deliberately its own separate, small plan — not folded into any frontend or
backend restoration plan). The audit's HC-09 table currently shows "Prod 0 / Test 0" for
`RoleTranslator`, `RoleTypesetter`, `RoleTechadmin`, `RoleGfxler` and states "Vier Konstanten haben
null Referenzen im gesamten Repo" ("four constants have zero references in the entire repo"). That
sentence is factually wrong: those four constants were used, unqualified (package-internal, so
never matching a qualified `permissions.X` grep), in test fixtures across
`effective_rights_test.go`, `effective_rights_capability_impact_preview_test.go`,
`capability_registry_test.go`, and `permissions_test.go` — confirmed via `git show 79bbdff9`
(commit `test(147-05): convert four package-internal test fixtures to string literals`), which
converted exactly those four files' fixtures from the bare constant names to string literals as
part of Phase 147's HC-09 remediation.

Purpose: the correct claim is "no production reference" (which was and remains true), not "zero
references repo-wide" (which was never true) — the "Test 0" column in the original table undercounted
because it only matched qualified `permissions.X` call syntax, missing the unqualified in-package
usages that exist precisely because these test files live inside the `permissions` package itself.

Output: HC-09's table/prose corrected in place; a short addendum explaining the counting-method gap
and noting Phase 147 already resolved the underlying issue (constants removed, fixtures now use
string literals).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct HC-09's false "zero references repo-wide" claim</name>
  <files>.planning/audits/2026-09-05-hardcoding-drift-audit.md</files>
  <read_first>
    .planning/audits/2026-09-05-hardcoding-drift-audit.md, HC-09 section (currently lines 358-387: table header, findings table row, the "Produktionsnutzungen" sub-table, and the "Vier Konstanten haben null Referenzen im gesamten Repo" sentence plus its surrounding paragraph and recommendation)
    .planning/phases/147-rollen-registry-letzte-parallelkataloge-aufl-sen/147-06-SUMMARY.md (confirms Phase 147 already removed the four constants and converted the fixtures to literals, requirements-completed: [HC-01, HC-02, HC-03, HC-09])
  </read_first>
  <action>
    In the HC-09 section, correct the sentence "Vier Konstanten haben null Referenzen im gesamten
    Repo." to state the accurate claim: the four constants had zero *production* references, but
    they did have package-internal test-fixture references in
    `effective_rights_test.go`, `effective_rights_capability_impact_preview_test.go`,
    `capability_registry_test.go`, and `permissions_test.go` — used unqualified (bare
    `RoleTranslator` etc., not `permissions.RoleTranslator`) because those test files live inside the
    `permissions` package itself, which is why a grep for qualified `permissions.X` usage (the
    counting method implied by the table's "Test" column) returned 0 for these four specifically,
    while genuinely counting non-zero for constants like `RoleFansubLead` that are also referenced
    from *other* packages using the qualified form. Add a short note stating Phase 147 (see
    `147-06-SUMMARY.md`, `requirements-completed: [..., HC-09]`) already removed the four constants
    from `permissions.go` and converted all four test files' fixtures to string literals (commit
    `79bbdff9`), so the underlying finding is resolved — this edit corrects the historical record of
    *why* the original "Test 0" figure was measured, not a call to re-open remediation. Keep the
    rest of HC-09 (Priorität P3, Kategorie E, Fundstelle, Empfehlung, the "Produktionsnutzungen"
    table's other rows) unchanged — only the false zero-references-repo-wide claim and its
    immediate justification are corrected.
  </action>
  <acceptance_criteria>
    <automated>grep -n "null Referenzen im gesamten Repo" .planning/audits/2026-09-05-hardcoding-drift-audit.md returns empty</automated>
    <automated>grep -n "keine Produktionsreferenz" .planning/audits/2026-09-05-hardcoding-drift-audit.md returns at least 1 match within the HC-09 section</automated>
    <automated>grep -n "147-06-SUMMARY\|Phase 147" .planning/audits/2026-09-05-hardcoding-drift-audit.md returns at least 1 match near the HC-09 section</automated>
  </acceptance_criteria>
  <done>HC-09's false "zero references repo-wide" claim is corrected to "no production reference, but package-internal test-fixture references existed"; the counting-method gap is explained; Phase 147's already-completed remediation is noted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Internal documentation edit only, no code/runtime surface touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-11 | none applicable | audit markdown file | accept | Pure documentation correction; no code, no data, no trust boundary involved. |
</threat_model>

<verification>
- `grep -n "null Referenzen im gesamten Repo"` in the audit file returns empty.
- Manual read confirms HC-09's other fields (Priorität, Kategorie, Fundstelle, Empfehlung, the rest of the Produktionsnutzungen table) are unchanged.
</verification>

<success_criteria>
- HC-09 states "keine Produktionsreferenz" (not "zero references repo-wide") and explains the counting-method gap that produced the original "Test 0" figure.
- The correction notes Phase 147 already removed the four constants and converted the four test files' fixtures to string literals.
</success_criteria>

<output>
Create `.planning/phases/148-rollenfarben-wieder-an-den-katalog-anschlie-en/148-06-SUMMARY.md` when done
</output>
