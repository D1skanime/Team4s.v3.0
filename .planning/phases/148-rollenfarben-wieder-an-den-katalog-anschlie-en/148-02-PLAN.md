---
phase: 148-rollenfarben-wieder-an-den-katalog-anschlie-en
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/styles/globals.css
  - frontend/src/components/public/PublicNoteCard.module.css
  - frontend/src/components/fansubs/projectMember/ProjectMemberPage.module.css
  - frontend/src/components/fansubs/projectMember/ProjectMemberReleasesSection.module.css
  - frontend/src/lib/roleCatalog.accessibility.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "There is exactly one place where a catalog role color arrives in CSS: globals.css derives --role-accent from the existing --role-chip-accent/data-color-key seam and defines a neutral default"
    - "No CSS module self-assigns --role-accent from a --role-accent-<code>/-default/-base token anymore in the three simple-fallback modules"
    - "Every restored color-mix formula introduced across this phase is proven, per catalog hex, to meet its WCAG threshold (4.5:1 text / 3:1 non-text) by reading the actual CSS text, not by hand-copied percentages"
  artifacts:
    - path: "frontend/src/styles/globals.css"
      provides: "[data-color-key] { --role-accent: var(--role-chip-accent, #596176); } and :root { --role-accent: #596176; }"
    - path: "frontend/src/lib/roleCatalog.accessibility.test.ts"
      provides: "Contrast proofs for every restored formula across the ten dead surfaces (except the already-proven .role-catalog-chip formula)"
  key_links:
    - from: "frontend/src/components/public/PublicNoteCard.module.css"
      to: "frontend/src/styles/globals.css"
      via: "var(--role-accent) with no dead fallback"
      pattern: "var\\(--role-accent\\)(?!, var\\(--role-accent-default\\))"
---

<objective>
Add the single derivation point that connects the dead `--role-accent` variable to the already
working, catalog-driven `--role-chip-accent`/`data-color-key` seam in `globals.css` (CONTEXT
decision #1), then remove the dead `, var(--role-accent-default)` fallback from the three CSS
modules whose only problem is that dead fallback (PublicNoteCard, ProjectMemberPage,
ProjectMemberReleasesSection - none of these three has a `[data-role-code]` color selector to
remove). Finally, extend `roleCatalog.accessibility.test.ts` (the existing, precedent-setting
contrast test) to prove WCAG contrast for every restored formula introduced across this whole
phase, per the UI-SPEC's locked Contrast Requirements table - reading each CSS module's actual
color-mix expression, never hand-copying percentages.

Purpose: per ROADMAP's Ausgangsbefund, `--role-accent` and `--role-accent-default` both resolve to
the empty string today (measured live via getComputedStyle); the header band, text, avatar and
border stripe on these three surfaces are consequently invisible/invalid-at-computed-value-time.

Output: globals.css with the two-line derivation; three CSS modules with their dead fallback
removed (formulas otherwise byte-for-byte unchanged, per the UI-SPEC Restoration Rule); an extended
roleCatalog.accessibility.test.ts covering PublicNoteCard, the three role-chip surfaces,
RoleBadgeCard.stages/MemberBadgeChain, the FansubEdit role-toggle (both states) and historical-role
label, and the /me/projects role-row border - all fifteen catalog hex values each.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-rollenfarben-wieder-an-den-katalog-anschlie-en/148-UI-SPEC.md
</context>

<interfaces>
Current frontend/src/styles/globals.css (verified live), the working seam block at lines 270-297:

/* One bounded catalog color_key-to-token seam; role codes never select color. */
[data-color-key='#183b7c'] { --role-chip-accent: #183b7c; }
(14 more per-hex lines follow the same pattern)
[data-color-key='neutral'] { --role-chip-accent: #596176; }

.role-catalog-chip {
  --role-chip-accent: #596176;
  --role-chip-text: #111827;
  --role-chip-border: #475569;
  --role-chip-focus: #111827;
  border-color: var(--role-chip-border);
  background: color-mix(in srgb, var(--role-chip-accent) 14%, var(--surface-card));
  color: var(--role-chip-text);
  box-shadow: inset 0 3px 0 var(--role-chip-accent);
}

Relevant root tokens (verified live) needed by the new contrast-test task:
--color-text-primary: #1c1c1e (--text-primary aliases this via var(), not a direct hex itself)
--color-text-secondary: #6b6b70 (--text-secondary aliases this via var(), not a direct hex itself)
--surface-card: #ffffff
--color-border: #e1e1e6
--border-subtle: rgba(45, 41, 38, 0.12) (not a plain hex - parse/flatten accordingly)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: globals.css derivation plus fallback removal in the three simple modules</name>
  <files>
    frontend/src/styles/globals.css,
    frontend/src/components/public/PublicNoteCard.module.css,
    frontend/src/components/fansubs/projectMember/ProjectMemberPage.module.css,
    frontend/src/components/fansubs/projectMember/ProjectMemberReleasesSection.module.css
  </files>
  <read_first>
    frontend/src/styles/globals.css, lines 255-301 (the existing data-color-key seam and .role-catalog-chip block)
    frontend/src/components/public/PublicNoteCard.module.css, full file (138 lines, four var(--role-accent, var(--role-accent-default)) sites)
    frontend/src/components/fansubs/projectMember/ProjectMemberPage.module.css, lines 255-271 (.roleChip, three sites)
    frontend/src/components/fansubs/projectMember/ProjectMemberReleasesSection.module.css, lines 95-111 (.roleTag, three sites)
    frontend/src/lib/roleCatalog.accessibility.test.ts (existing test asserts globals.css never contains a data-role-code selector - do not violate this)
  </read_first>
  <action>
    In globals.css, immediately after the "[data-color-key='neutral'] { --role-chip-accent:
    #596176; }" line and before the ".role-catalog-chip {" rule, add exactly two new rules per
    CONTEXT decision #1: a ":root" rule setting "--role-accent: #596176;" and a "[data-color-key]"
    rule setting "--role-accent: var(--role-chip-accent, #596176);". The neutral hex (#596176) is
    the value already used by the seam's own neutral branch - do not choose a new value. Do not add
    any [data-role-code] selector anywhere in globals.css. Do not touch the per-hex
    [data-color-key='#...'] lines or .role-catalog-chip.
    In PublicNoteCard.module.css: at all four sites reading "var(--role-accent,
    var(--role-accent-default))" (current lines 13, 25, 33, 56 - the .card bottom border, .head
    background, .role text, .avatar background), remove the dead ", var(--role-accent-default)"
    fallback so each becomes a plain "var(--role-accent)" reference; the surrounding color-mix
    ratios (55%, 38%, 70%) and every other declaration stay byte-for-byte identical per the
    Restoration Rule - no new ratio, no new fallback value.
    In ProjectMemberPage.module.css: same fallback removal at the three .roleChip sites (current
    lines 267-269, border 34% / background 14% / text 82% color-mix ratios) - formulas unchanged,
    only the dead fallback removed. Also correct the stale comment at current line 261
    ("data-role-code -> --role-accent aus globals.css") to instead reference data-color-key as the
    actual color driver, since data-role-code only ever carries the semantic code after this
    phase's other plans land.
    In ProjectMemberReleasesSection.module.css: same fallback removal at the three .roleTag sites
    (current lines 108-110, border 32% / background 12% / text 78%).
  </action>
  <acceptance_criteria>
    <automated>grep -A1 "data-color-key\]" frontend/src/styles/globals.css | grep -c "role-chip-accent, #596176" returns 1</automated>
    <automated>grep -c -- ", var(--role-accent-default)" frontend/src/components/public/PublicNoteCard.module.css returns 0; same command on ProjectMemberPage.module.css and ProjectMemberReleasesSection.module.css also returns 0</automated>
    <automated>grep -c "role-accent" frontend/src/lib/roleCatalog.accessibility.test.ts's existing "not.toMatch(/\[data-role-code" assertion still passes: npx vitest run src/lib/roleCatalog.accessibility.test.ts exits 0</automated>
  </acceptance_criteria>
  <done>globals.css derives --role-accent from the catalog seam with a neutral default; the three modules read --role-accent with no dead fallback and unchanged color-mix ratios; the pre-existing accessibility test still passes untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend roleCatalog.accessibility.test.ts with contrast proofs for every restored formula</name>
  <files>frontend/src/lib/roleCatalog.accessibility.test.ts</files>
  <read_first>
    frontend/src/lib/roleCatalog.accessibility.test.ts, full file (59 lines - existing hexRgb/mix/luminance/contrast/rootHex helpers and the two existing `it()` blocks)
    .planning/phases/148-rollenfarben-wieder-an-den-katalog-anschlie-en/148-UI-SPEC.md, "Contrast Requirements" table (locked thresholds and formulas per surface) and "How this is checked" section (mandates extending this exact file, reading real CSS text)
    frontend/src/components/public/PublicNoteCard.module.css (post-Task-1 state: .head 55% bg mix, .role 38% text mix, .avatar 70% bg mix + white text, .card 8px solid border stripe)
    frontend/src/components/fansubs/projectMember/ProjectMemberPage.module.css, ProjectMemberReleasesSection.module.css, frontend/src/components/profile/MemberCurrentProjectsSection.module.css (post-Plan-01/02 state: three chip formulas, ratios differ slightly per file - 78-82% text / 12-14% bg / 32-34% border, read each file's own numbers, do not hand-copy one file's ratio onto another)
    frontend/src/components/profile/RoleBadgeCard.stages.module.css (lines 178, 197, 202, 262, 325 - 28-42% border/box-shadow mixes vs --border-subtle, unchanged by this phase)
    frontend/src/components/profile/MemberBadgeChain.module.css (line 405 - 32% mix border vs --border-subtle, unchanged by this phase)
    frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css (lines 3781-3814 - .fansubEditMemberRoleToggle raw 100%-strength border/text, .fansubEditMemberRoleToggleSelected solid fill + white #fff text, both formulas unchanged by this phase except one dead assignment line removed elsewhere in the file; lines 3629-3633 - .fansubEditHistoricalRoleBadge small, 74% text mix vs --text-secondary)
    frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.tsx (the 3px borderInlineStartColor: 'var(--role-accent)' role-row indicator, solid/no-mix, checked against the row's own background)
  </read_first>
  <behavior>
    For each of the fifteen ROLE_COLOR_KEYS hex values (reuse the existing hexRgb/mix/luminance/
    contrast helpers, extend rootHex or add a small helper to resolve --text-primary/--text-secondary
    via their underlying --color-text-primary/--color-text-secondary hex, and to resolve
    --border-subtle's rgba value flattened over --surface-card), prove:
    - PublicNoteCard .head background (color-mix role-accent 55% + --color-border) vs its
      .role text (color-mix role-accent 38% + --text-primary) meets 4.5:1.
    - PublicNoteCard .avatar background (color-mix role-accent 70% + #000) vs white #fff text
      meets 4.5:1.
    - PublicNoteCard .card bottom stripe (solid role-accent) vs the adjacent --surface-card/
      --color-border meets 3:1 (non-text).
    - Each of ProjectMemberPage/ProjectMemberReleasesSection/MemberCurrentProjectsSection chip
      formulas: text-mix vs bg-mix meets 4.5:1, border-mix vs bg-mix meets 3:1 - reading each
      file's own percentages independently (they are not identical across the three files).
    - RoleBadgeCard.stages.module.css and MemberBadgeChain.module.css border/box-shadow mixes vs
      --border-subtle meet 3:1 (non-text; the RoleBadgeCard.module.css radial-gradient/drop-shadow
      decorative uses are exempt per UI-SPEC and must NOT be asserted here).
    - FansubEdit .fansubEditMemberRoleToggle (unselected): raw role-accent as text AND border color
      meets 4.5:1 against the toggle's own background - flag explicitly if any of the 15 hex values
      fails (UI-SPEC calls this the highest-risk row; if a failure is found, do not alter the hex
      palette - report the failing hex(es) in the test/summary for a follow-up formula fix, per
      UI-SPEC's guidance, rather than silently forcing the assertion to pass).
    - FansubEdit .fansubEditMemberRoleToggleSelected: white #fff text on solid role-accent fill
      meets 4.5:1 - same failure-flagging rule as above.
    - FansubEdit .fansubEditHistoricalRoleBadge small (74% role-accent mix + --text-secondary) meets
      4.5:1.
    - The /me/projects role-row 3px solid border-inline-start (raw role-accent) vs the row's own
      background meets 3:1 (non-text).
  </behavior>
  <action>
    Add new it() blocks to roleCatalog.accessibility.test.ts (or a sibling *.accessibility.test.ts
    file colocated next to roleCatalog.ts if that keeps this file under a reasonable size), each
    reading the actual post-restoration CSS text via fs.readFileSync from the files listed in
    read_first and extracting the real color-mix percentages with a regex per selector (never
    hand-copying a percentage number into the test). Reuse the existing hexRgb/mix/luminance/
    contrast function signatures; add a small rgba-to-rgb flattening helper for --border-subtle
    (composite its alpha over the resolved --surface-card hex) since it is not a plain 6-digit hex
    like the other root tokens the existing rootHex() helper expects.
  </action>
  <acceptance_criteria>
    <automated>npx vitest run src/lib/roleCatalog.accessibility.test.ts exits 0</automated>
    <automated>Test file contains at least one assertion per surface listed in behavior above, each looping over ROLE_COLOR_KEYS (15 values), reading the formula text from the actual CSS file rather than a hardcoded percentage literal</automated>
  </acceptance_criteria>
  <done>roleCatalog.accessibility.test.ts (or its sibling) proves WCAG contrast for every restored formula in this phase across all 15 catalog hex values, reading real CSS text; any genuine formula-level contrast failure for the FansubEdit role-toggle rows is explicitly reported, not silently suppressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure CSS-variable derivation and a test file; no new input surface, no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-03 | Information Disclosure | globals.css seam | accept | The new [data-color-key]/:root rules only ever expose the same 15 already-public catalog hex values already rendered by the six working admin surfaces; no new data is surfaced. |
| T-148-04 | Tampering | none (no package installs) | accept | Pure CSS/test edits to existing first-party files. |
</threat_model>

<verification>
- `npx vitest run src/lib/roleCatalog.accessibility.test.ts` exits 0 with the extended coverage.
- `grep -c ", var(--role-accent-default)"` returns 0 across the three simple-fallback modules.
- `grep -n "data-role-code" frontend/src/styles/globals.css` returns empty (no color-by-code selector introduced).
</verification>

<success_criteria>
- globals.css derives --role-accent from the existing catalog seam via exactly the two locked rules, with no new hex chosen.
- PublicNoteCard/ProjectMemberPage/ProjectMemberReleasesSection modules read --role-accent with zero dead fallback references remaining, formulas byte-for-byte unchanged.
- Every restored color-mix/solid-color formula across the whole phase has an automated, real-CSS-text-driven WCAG contrast proof for all 15 catalog hex values.
</success_criteria>

<output>
Create `.planning/phases/148-rollenfarben-wieder-an-den-katalog-anschlie-en/148-02-SUMMARY.md` when done
</output>
