---
phase: 69-fansub-contributions-contract-und-permission-haertung
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/repository/hist_group_members_repository.go
  - backend/internal/repository/anime_contributions_repository.go
autonomous: true
requirements:
  - P69-SC2
  - P69-SC6
  - P69-SC7

must_haves:
  truths:
    - "hist_group_members_repository bietet CreateWithAutoMember: legt members-Zeile (nickname=display_name) an und dann hist_fansub_group_members in einer Transaktion"
    - "anime_contributions_repository.Create uebertraegt den status-Wert aus dem Input statt 'draft' hartzucodieren"
    - "anime_contributions_repository.CreateOrUpdate fuehrt bei UNIQUE-Konflikt ein UPDATE durch (Upsert-Semantik)"
  artifacts:
    - path: "backend/internal/repository/hist_group_members_repository.go"
      provides: "CreateWithAutoMember(ctx, input HistGroupMemberAutoCreateInput) (*HistGroupMemberDisplayRow, error)"
      exports: ["CreateWithAutoMember", "HistGroupMemberAutoCreateInput"]
    - path: "backend/internal/repository/anime_contributions_repository.go"
      provides: "Create mit status aus Input; neue Methode CreateOrUpdate mit ON CONFLICT DO UPDATE"
      exports: ["CreateOrUpdate"]
  key_links:
    - from: "HistGroupMembersRepository.CreateWithAutoMember"
      to: "members(nickname) + hist_fansub_group_members"
      via: "DB-Transaktion: INSERT INTO members RETURNING id, dann INSERT INTO hist_fansub_group_members"
    - from: "AnimeContributionsRepository.Create"
      to: "anime_contributions.status"
      via: "input.Status statt hartkodiertem 'draft'"
    - from: "AnimeContributionsRepository.CreateOrUpdate"
      to: "anime_contributions"
      via: "INSERT ... ON CONFLICT (fansub_group_id, anime_id, fansub_group_member_id) DO UPDATE SET ..."
---

<objective>
Erweitert zwei bestehende Repositories um die Faehigkeiten, die in Wave 2 benoetigt werden:

1. hist_group_members_repository: Neue Methode CreateWithAutoMember, die aus einem display_name
   automatisch eine members-Zeile anlegt und dann den hist_fansub_group_members-Eintrag erzeugt.
   user_id-Verknuepfung bewusst weggelassen (siehe Annahme A1 unten).

2. anime_contributions_repository: (a) status-Feld in Create aus Input statt hartkodiert 'draft';
   (b) neue Methode CreateOrUpdate, die bei Unique-Konflikt die bestehende Contribution aktualisiert
   statt einen Fehler zu werfen — loest F6 (Upsert-Semantik) auf Repo-Ebene.

Purpose: Handler in Wave 2 koennen diese Methoden aufrufen, ohne eigene Transaktionslogik zu schreiben.
Output: Erweiterungen in zwei bestehenden Go-Dateien.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/PROJECT.md
@C:/Users/admin/Documents/Team4s/.planning/ROADMAP.md
@C:/Users/admin/Documents/Team4s/.planning/phases/69-fansub-contributions-contract-und-permission-haertung/69-CONTEXT.md
</context>

<interfaces>
<!-- Bestehende Typen in hist_group_members_repository.go -->

type HistGroupMemberInput struct {
    FansubGroupID int64
    MemberID      int64   // wird in CreateWithAutoMember intern gesetzt
    JoinedYear    *int
    LeftYear      *int
    Status        string
    Visibility    string
    CreatedBy     *int64
}

type HistGroupMemberDisplayRow struct {
    ID            int64     `json:"id"`
    FansubGroupID int64     `json:"fansub_group_id"`
    MemberID      int64     `json:"member_id"`
    DisplayName   string    `json:"display_name"`
    AppUserID     *int64    `json:"app_user_id"`
    AppUsername   *string   `json:"app_username"`
    JoinedYear    *int      `json:"joined_year"`
    LeftYear      *int      `json:"left_year"`
    Status        string    `json:"status"`
    CreatedAt     time.Time `json:"created_at"`
}

func (r *HistGroupMembersRepository) ListByFansubGroupWithDisplay(ctx, fansubGroupID) ([]HistGroupMemberDisplayRow, error)
func (r *HistGroupMembersRepository) Create(ctx, input HistGroupMemberInput) (*HistGroupMemberRow, error)

<!-- Bestehende Typen in anime_contributions_repository.go -->

type AnimeContributionInput struct {
    FansubGroupMemberID     int64
    RoleCodes               []string
    StartedYear             *int
    EndedYear               *int
    Note                    *string
    IsPublicOnAnimePage     bool
    IsPublicOnMemberProfile bool
    CreatedBy               *int64
    // FEHLT: Status string (wird in diesem Plan ergaenzt)
}

func (r *AnimeContributionsRepository) Create(ctx, fansubGroupID, animeID int64, input AnimeContributionInput) (*AnimeContributionRow, error)
func (r *AnimeContributionsRepository) GetByIDWithDisplay(ctx, id int64) (*AnimeContributionDisplayRow, error)

<!-- DB-Schema members-Tabelle (0044) -->
-- members.user_id REFERENCES users(id) -- Legacy, KEIN app_users-Bezug
-- members.nickname VARCHAR(120) NOT NULL

<!-- Annahme A1: app_user_id-Feld -->
-- Das FE sendet app_user_id, aber members.user_id referenziert users(id) (Legacy-Tabelle).
-- Da kein Mapping app_users(id) -> users(id) ohne Join existiert und der FK-Typ nicht passt,
-- wird app_user_id beim Member-Auto-Create ignoriert.
-- Nur nickname (= display_name) wird gesetzt; user_id bleibt NULL.
-- Diese Entscheidung ist im Plan dokumentiert und kann spaeter in einer separaten Phase
-- erweitert werden, sobald ein stabiles Mapping vorliegt.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: HistGroupMembersRepository — CreateWithAutoMember</name>
  <files>backend/internal/repository/hist_group_members_repository.go</files>
  <action>
  Lies backend/internal/repository/hist_group_members_repository.go vollstaendig, bevor du etwas aenderst.
  Lies auch backend/internal/repository/fansub_group_app_members_repository.go fuer das Transaktions-Muster (dbPool.BeginTx, defer Rollback).

  Ergaenze am Ende der Datei:

  Neuen Eingabe-Typ hinzufuegen:

      type HistGroupMemberAutoCreateInput struct {
          FansubGroupID int64
          DisplayName   string  // wird als members.nickname gesetzt
          JoinedYear    *int
          LeftYear      *int
          Status        string
          Visibility    string
          CreatedBy     *int64
      }

  Neue Methode CreateWithAutoMember:

  func (r *HistGroupMembersRepository) CreateWithAutoMember(
      ctx context.Context,
      input HistGroupMemberAutoCreateInput,
  ) (*HistGroupMemberDisplayRow, error) {

  Logik:
  1. Transaktion beginnen (pgx.TxOptions{}).
  2. INSERT INTO members (nickname) VALUES ($display_name) RETURNING id.
     user_id bleibt NULL (per Annahme A1: members.user_id referenziert users(id) Legacy-Tabelle,
     kein app_user_id-Mapping vorhanden).
  3. INSERT INTO hist_fansub_group_members
         (fansub_group_id, member_id, joined_year, left_year, status, visibility, created_by)
         VALUES ($1, $memberID, $2, $3, $4, $5, $6)
         RETURNING id, fansub_group_id, member_id, joined_year, left_year, status, created_at.
  4. HistGroupMemberDisplayRow aufbauen: DisplayName = input.DisplayName, AppUserID = nil, AppUsername = nil.
  5. Transaktion committen.
  6. Fehlerbehandlung: isUniqueViolation => ErrConflict, isForeignKeyViolation => ErrNotFound.

  Rueckgabe: *HistGroupMemberDisplayRow (hat dieselbe JSON-Shape wie ListByFansubGroupWithDisplay).

  Datei muss nach Ergaenzung unter 450 Zeilen bleiben. Falls sie dadurch ueberschritten wird,
  nur CreateWithAutoMember und HistGroupMemberAutoCreateInput in eine neue Datei
  hist_group_members_autocreate_repository.go auslagern (selbes Package, kein neuer Struct).
  </action>
  <verify>
    <automated>go build ./backend/...</automated>
  </verify>
  <done>
  - HistGroupMemberAutoCreateInput exportiert
  - CreateWithAutoMember exportiert und gibt *HistGroupMemberDisplayRow zurueck
  - Transaktion umschliesst INSERT members + INSERT hist_fansub_group_members
  - user_id in members bleibt NULL (Annahme A1)
  - go build ./backend/... fehlerfrei
  - Keine Datei ueberschreitet 450 Zeilen
  </done>
</task>

<task type="auto">
  <name>Task 2: AnimeContributionsRepository — status im Create + CreateOrUpdate</name>
  <files>backend/internal/repository/anime_contributions_repository.go</files>
  <action>
  Lies backend/internal/repository/anime_contributions_repository.go vollstaendig, bevor du etwas aenderst.

  Aenderung 1 — Status-Feld im Input:
  AnimeContributionInput um Status-Feld ergaenzen:
      Status string  // "draft" | "proposed" | "confirmed" | "disputed" | "hidden"

  Im bestehenden Create-INSERT den harkodierten Wert 'draft' durch $input.Status ersetzen.
  Wenn input.Status leer ist, Default 'draft' verwenden:
      status = input.Status, falls leer dann "draft"
  (Implementierung: `if input.Status == "" { input.Status = "draft" }` vor dem INSERT)

  Aenderung 2 — Neue Methode CreateOrUpdate:
  func (r *AnimeContributionsRepository) CreateOrUpdate(
      ctx context.Context,
      fansubGroupID int64,
      animeID int64,
      input AnimeContributionInput,
  ) (*AnimeContributionDisplayRow, error)

  Logik — INSERT mit ON CONFLICT DO UPDATE in einer Transaktion:
  1. Transaktion beginnen.
  2. INSERT INTO anime_contributions (...alle Felder wie in Create...) VALUES (...)
     ON CONFLICT (fansub_group_id, anime_id, fansub_group_member_id)
     DO UPDATE SET
         status                  = EXCLUDED.status,
         note                    = EXCLUDED.note,
         started_year            = EXCLUDED.started_year,
         ended_year              = EXCLUDED.ended_year,
         is_public_on_anime_page = EXCLUDED.is_public_on_anime_page,
         is_public_on_member_profile = EXCLUDED.is_public_on_member_profile,
         updated_by              = EXCLUDED.updated_by,
         updated_at              = NOW()
     RETURNING id.
  3. Bestehende anime_contribution_roles per DELETE loeschen, neue einfuegen (analog zu Update).
  4. GetByIDWithDisplay(ctx, newID) aufrufen und zurueckgeben.
  5. Transaktion committen.

  Rueckgabe: *AnimeContributionDisplayRow (hat die Frontend-konforme JSON-Shape mit member_display_name).

  Fehlerbehandlung: isForeignKeyViolation => ErrNotFound (unbekannte role_codes oder fehlende Gruppe/Anime).

  Datei hat derzeit ~492 Zeilen (ueberschreitet 450). Da die Datei bereits zu gross ist,
  CreateOrUpdate in eine neue Datei anime_contributions_upsert_repository.go auslagern
  (selbes Package, kein neuer Struct — Extension-Methode auf AnimeContributionsRepository).
  Die Aenderung an AnimeContributionInput (Status-Feld) bleibt in der Originaldatei.
  </action>
  <verify>
    <automated>go build ./backend/...</automated>
  </verify>
  <done>
  - AnimeContributionInput hat Status-Feld
  - Create verwendet input.Status (mit "draft"-Fallback)
  - CreateOrUpdate existiert (in anime_contributions_upsert_repository.go) und gibt *AnimeContributionDisplayRow zurueck
  - ON CONFLICT (fansub_group_id, anime_id, fansub_group_member_id) in der Upsert-Query
  - go build ./backend/... fehlerfrei
  - Keine Datei ueberschreitet 450 Zeilen
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Handler → Repository | CreateWithAutoMember legt implizit einen members-Eintrag an; Handler muss display_name validiert haben |
| Repository → DB | Transaktion schutzt vor halbfertigen Zustaenden bei CreateWithAutoMember |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-69-03 | Tampering | CreateWithAutoMember — member_id Injektion | mitigate | member_id kommt nur aus dem frisch angelegten INSERT; nie aus dem Request |
| T-69-04 | Information Disclosure | app_user_id wird ignoriert | accept | user_id bleibt NULL per Annahme A1; keine falschen Verknuepfungen moeglich |
| T-69-05 | Tampering | CreateOrUpdate — status Manipulation | mitigate | Gueltiger status wird im Handler validiert (Wave 2); Repo akzeptiert was reinkommt |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | Keine Paketinstallation |
</threat_model>

<verification>
Nach Implementierung:
1. go build ./backend/... -- kein Fehler
2. grep "CreateWithAutoMember" backend/internal/repository/hist_group_members_repository.go -- Treffer
3. grep "Status" backend/internal/repository/anime_contributions_repository.go -- Treffer in AnimeContributionInput
4. grep "ON CONFLICT" backend/internal/repository/anime_contributions_upsert_repository.go -- Treffer
5. wc -l backend/internal/repository/anime_contributions_repository.go -- unter 450
6. wc -l backend/internal/repository/hist_group_members_repository.go -- unter 450
</verification>

<success_criteria>
- CreateWithAutoMember in hist_group_members_repository: members + hist_fansub_group_members in einer Transaktion
- AnimeContributionInput.Status vorhanden; Create nutzt es (Default "draft")
- CreateOrUpdate in anime_contributions_upsert_repository.go: ON CONFLICT DO UPDATE + Rollenzustand atomar
- Alle Dateien unter 450 Zeilen
- go build fehlerfrei
</success_criteria>

<output>
Erstelle .planning/phases/69-fansub-contributions-contract-und-permission-haertung/69-02-SUMMARY.md wenn fertig.
</output>
