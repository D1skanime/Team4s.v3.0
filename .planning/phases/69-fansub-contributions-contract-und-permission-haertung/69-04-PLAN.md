---
phase: 69-fansub-contributions-contract-und-permission-haertung
plan: 04
type: execute
wave: 3
depends_on:
  - 69-03
files_modified:
  - frontend/src/types/fansub.ts
  - frontend/src/lib/api.ts
  - frontend/src/app/admin/fansubs/[id]/edit/GroupMembersTab.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/MemberRolesTab.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionsTab.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionModal.tsx
autonomous: false
requirements:
  - P69-SC1
  - P69-SC3
  - P69-SC8

must_haves:
  truths:
    - "HistFansubGroupMemberListResponse hat { data: HistFansubGroupMember[] } statt { members: ... }"
    - "HistFansubGroupMemberResponse hat { data: HistFansubGroupMember } statt { member: ... }"
    - "HistGroupMemberRoleListResponse hat { data: HistGroupMemberRole[] } statt { roles: ... }"
    - "AnimeContributionListResponse hat { data: AnimeContribution[] } statt { contributions: ... }"
    - "listMemberRoles uebergibt memberId als ?member_id= Query-Parameter"
    - "GroupMembersTab, MemberRolesTab, AnimeContributionsTab lesen .data statt benannte Keys"
    - "AnimeContributionModal enthaelt ausschliesslich seed-konforme role_codes (quality_checker statt qc); kein Freitext-Input"
    - "MemberRolesTab nutzt Dropdown aus FANSUB_GROUP_ROLE_OPTIONS statt Freitext-Feld"
    - "Alle drei Tabs laden ohne Fehler in der Admin-UI"
  artifacts:
    - path: "frontend/src/types/fansub.ts"
      provides: "Envelope-Typen mit {data: ...}-Shape"
      contains: "data: HistFansubGroupMember[]"
    - path: "frontend/src/lib/api.ts"
      provides: "listMemberRoles mit memberId-Parameter; api.ts-Helfer konsumieren .data"
    - path: "frontend/src/app/admin/fansubs/[id]/edit/GroupMembersTab.tsx"
      provides: "Tab liest response.data statt response.members"
    - path: "frontend/src/app/admin/fansubs/[id]/edit/MemberRolesTab.tsx"
      provides: "Tab liest response.data; Rollenauswahl per Dropdown aus FANSUB_GROUP_ROLE_OPTIONS"
    - path: "frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionsTab.tsx"
      provides: "Tab liest response.data statt response.members/.contributions"
    - path: "frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionModal.tsx"
      provides: "Seed-konforme Rollenchips (quality_checker statt qc); kein Freitext-Input"
  key_links:
    - from: "listMemberRoles in api.ts"
      to: "GET /admin/fansubs/:id/member-roles?member_id=N"
      via: "memberId als Pflicht-Parameter; 400 wenn weggelassen"
    - from: "GroupMembersTab.tsx"
      to: "HistFansubGroupMemberListResponse"
      via: "response.data statt response.members"
    - from: "AnimeContributionModal.tsx — SUGGESTED_ROLES"
      to: "FANSUB_GROUP_ROLE_OPTIONS in fansub.ts"
      via: "Import und Filterung auf anime_contribution-relevante Codes"
---

<objective>
Behebt alle Frontend-seitigen Contract-Brueche (F1, F3, F8):
- Envelope-Typen in fansub.ts auf {data: ...} umstellen
- api.ts-Helfer konsumieren .data korrekt; listMemberRoles erhaelt memberId-Parameter
- Alle drei Tabs lesen .data statt benannte Keys
- AnimeContributionModal: SUGGESTED_ROLES durch seed-konforme Liste ersetzen; Freitext-Input entfernen
- MemberRolesTab: Rollencode-Freitext durch Dropdown aus FANSUB_GROUP_ROLE_OPTIONS ersetzen

Purpose: Tabs laden tatsaechlich Daten statt undefined-Fehler; keine ungueltige role_codes mehr moeglich.
Output: Angepasste TypeScript-Typen, api.ts-Helfer und drei Tab-Komponenten plus Modal.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/PROJECT.md
@C:/Users/admin/Documents/Team4s/.planning/ROADMAP.md
@C:/Users/admin/Documents/Team4s/.planning/phases/69-fansub-contributions-contract-und-permission-haertung/69-CONTEXT.md
</context>

<interfaces>
<!-- Aktuelle (falsche) Envelope-Typen in fansub.ts (Zeilen 502-600) -->
interface HistFansubGroupMemberListResponse { members: HistFansubGroupMember[] }
interface HistFansubGroupMemberResponse     { member: HistFansubGroupMember }
interface HistGroupMemberRoleListResponse   { roles: HistGroupMemberRole[] }
interface HistGroupMemberRoleResponse       { role: HistGroupMemberRole }
interface AnimeContributionListResponse     { contributions: AnimeContribution[] }
interface AnimeContributionResponse         { contribution: AnimeContribution }

<!-- Korrekte Envelope-Typen (nach D3: Backend liefert {data: ...}) -->
interface HistFansubGroupMemberListResponse { data: HistFansubGroupMember[] }
interface HistFansubGroupMemberResponse     { data: HistFansubGroupMember }
interface HistGroupMemberRoleListResponse   { data: HistGroupMemberRole[] }
interface HistGroupMemberRoleResponse       { data: HistGroupMemberRole }
interface AnimeContributionListResponse     { data: AnimeContribution[] }
interface AnimeContributionResponse         { data: AnimeContribution }

<!-- Vorhandene Konstante fuer seed-konforme Rollencodes (fansub.ts:344) -->
export const FANSUB_GROUP_ROLE_OPTIONS: FansubGroupRoleOption[] = [
  { code: 'fansub_lead', label: 'Fansub-Lead', description: '...' },
  { code: 'project_lead', label: 'Projektleitung', description: '...' },
  { code: 'translator', label: 'Übersetzung', description: '...' },
  { code: 'timer', label: 'Timing', description: '...' },
  { code: 'typesetter', label: 'Typesetting', description: '...' },
  { code: 'editor', label: 'Editing', description: '...' },
  { code: 'encoder', label: 'Encoding', description: '...' },
  { code: 'raw_provider', label: 'Raw-Quelle', description: '...' },
  { code: 'quality_checker', label: 'Qualitätscheck', description: '...' },
  { code: 'designer', label: 'Design', description: '...' },
]
// Fuer AnimeContributionModal: fansub_lead entfernen (kein anime_contribution-Rollencode);
// rest der Liste plus quality_checker ist vollstaendig seed-konform.
// Seed anime_contribution codes: translator, editor, timer, typesetter, encoder,
// raw_provider, quality_checker, project_lead, designer, admin, other, project_manager

<!-- listMemberRoles aktuell (api.ts:6637) — fehlt member_id -->
export async function listMemberRoles(fansubId: number, authToken?: string)
// NACH der Aenderung:
export async function listMemberRoles(fansubId: number, memberId: number, authToken?: string)
// URL: `${API_BASE_URL}/api/v1/admin/fansubs/${fansubId}/member-roles?member_id=${memberId}`

<!-- Wo MemberRolesTab listMemberRoles aufruft (MemberRolesTab.tsx:99-104) -->
const [rolesRes, membersRes] = await Promise.all([
  listMemberRoles(fansubId),   // FIX: muss memberId uebergeben (aber welche?)
  listGroupMembers(fansubId),
])
// Logik: Tab laedt alle Mitglieder zuerst, dann iteriert ueber alle und laedt deren Rollen,
// ODER: Tab laedt Rollen fuer ein ausgewaehltes Mitglied on-demand.
// Einfachste Loesung (ohne UX-Umbau): Alle Mitglieder laden, dann fuer jedes seine Rollen
// sequentiell laden und flach zusammenfuehren. Alternativer Ansatz: Dropdown "Mitglied waehlen"
// und listMemberRoles erst bei Auswahl aufrufen.
// ENTSCHEIDUNG IM PLAN: Lazy-Load pro Mitglied beim Tab-Oeffnen (Promise.all ueber alle Members).
// Das ist korrekt, skaliert auf MVP-Level und erfordert keinen UX-Umbau.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: fansub.ts — Envelope-Typen auf {data: ...} umstellen</name>
  <files>frontend/src/types/fansub.ts</files>
  <action>
  Lies frontend/src/types/fansub.ts Zeilen 488-601 vollstaendig, bevor du aenderst.

  Sechs Interface-Definitionen aendern:

  1. HistFansubGroupMemberListResponse: "members: HistFansubGroupMember[]" -> "data: HistFansubGroupMember[]"
  2. HistFansubGroupMemberResponse:     "member: HistFansubGroupMember"    -> "data: HistFansubGroupMember"
  3. HistGroupMemberRoleListResponse:   "roles: HistGroupMemberRole[]"     -> "data: HistGroupMemberRole[]"
  4. HistGroupMemberRoleResponse:       "role: HistGroupMemberRole"        -> "data: HistGroupMemberRole"
  5. AnimeContributionListResponse:     "contributions: AnimeContribution[]" -> "data: AnimeContribution[]"
  6. AnimeContributionResponse:         "contribution: AnimeContribution"   -> "data: AnimeContribution"

  Nichts anderes in der Datei aendern. Nur die sechs Feldnamen-Aenderungen.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
  - Alle sechs Response-Interfaces haben "data" statt der alten benannten Keys
  - TypeScript-Compiler meldet keine neuen Fehler durch diese Aenderung allein
    (Fehler entstehen erst bis alle Konsumenten auch aktualisiert sind)
  </done>
</task>

<task type="auto">
  <name>Task 2: api.ts — .data-Konsum + listMemberRoles-Signatur mit memberId</name>
  <files>frontend/src/lib/api.ts</files>
  <action>
  Lies frontend/src/lib/api.ts Zeilen 6510-6830 vollstaendig.

  Aenderung 1 — listGroupMembers (Zeile ~6536):
  return response.json() as Promise liefert jetzt HistFansubGroupMemberListResponse mit .data.
  Keine Aenderung an der Funktion selbst noetig — der Rueckgabetyp stimmt nach Task 1 bereits.

  Aenderung 2 — listMemberRoles-Signatur (Zeile ~6637):
  Parameter memberId: number als zweiten Parameter hinzufuegen (nach fansubId, vor authToken).
  URL-Pfad ergaenzen: `?member_id=${memberId}` an den Endpunkt anhaengen.
  authToken-Parameter bleibt dritter Parameter.

  Aenderung 3 — upsertAnimeContribution umbenennen:
  Den Helper in api.ts unveraendert lassen (er heisst upsert, macht POST — passt zum Backend-Verhalten,
  das jetzt ON CONFLICT DO UPDATE nutzt). Keine Namensaenderung noetig.

  Sonstige api.ts-Helfer:
  Die Funktionen geben die Response als Promise<XxxResponse> zurueck. Da die Response-Typen
  in Task 1 auf .data umgestellt wurden, sind keine weiteren api.ts-Aenderungen noetig —
  der Compiler zeigt Fehler in den Konsumenten (Tabs), die in Task 3 behoben werden.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | grep "listMemberRoles" | head -5</automated>
  </verify>
  <done>
  - listMemberRoles hat memberId als zweiten Pflicht-Parameter
  - URL enthaelt ?member_id=${memberId}
  - Keine anderen api.ts-Helfer veraendert
  </done>
</task>

<task type="auto">
  <name>Task 3: Tabs + Modal — .data-Konsum, seed-konforme Rollen, Dropdown statt Freitext</name>
  <files>
    frontend/src/app/admin/fansubs/[id]/edit/GroupMembersTab.tsx,
    frontend/src/app/admin/fansubs/[id]/edit/MemberRolesTab.tsx,
    frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionsTab.tsx,
    frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionModal.tsx
  </files>
  <action>
  Lies alle vier Dateien vollstaendig, bevor du aenderst.

  === GroupMembersTab.tsx ===
  Zeile ~91 (load-Funktion): response.members -> response.data
  Zeile ~109: membersRes.members -> membersRes.data (falls vorhanden)
  Alle weiteren Stellen in der Datei, die .members auf der Response referenzieren.

  === MemberRolesTab.tsx ===

  1. .data-Konsum (load-Funktion Zeile ~103):
     rolesRes.roles -> rolesRes.data
     membersRes.members -> membersRes.data

  2. listMemberRoles-Aufruf (Zeile ~100):
     Da listMemberRoles jetzt einen memberId-Parameter erwartet, muss der Tab fuer jedes
     geladene Mitglied dessen Rollen laden.

     Neue Ladelogik in load():
     a. Lade alle Mitglieder: const membersRes = await listGroupMembers(fansubId)
     b. Lade Rollen fuer jedes Mitglied parallel:
            const roleResponses = await Promise.all(
              (membersRes.data ?? []).map(m => listMemberRoles(fansubId, m.id))
            )
     c. Flatmap: const allRoles = roleResponses.flatMap(r => r.data ?? [])
     d. setRoles(sorted) und setMembers(membersRes.data ?? []) wie bisher

  3. Freitext-Rollenfeld durch Dropdown ersetzen:
     Im Modal (FormFields.roleCode, Zeile ~60):
     - Import: import { FANSUB_GROUP_ROLE_OPTIONS } from '@/types/fansub'
     - Statt <input type="text" ... > fuer roleCode ein <select> rendern:
           <select value={form.roleCode} onChange={(e) => setForm(f => ({...f, roleCode: e.target.value}))}>
             <option value="">Rolle auswählen…</option>
             {FANSUB_GROUP_ROLE_OPTIONS.map(opt => (
               <option key={opt.code} value={opt.code}>{opt.label}</option>
             ))}
           </select>
     - Label anpassen: "Rolle" (unveraendert, aber jetzt Dropdown)
     - Validierung in handleSave: !form.roleCode.trim() bleibt, passt zum leeren Option

  === AnimeContributionsTab.tsx ===
  Zeile ~41: membersResp.members -> membersResp.data
  Zeile ~50: resp.contributions -> resp.data
  Alle weiteren Stellen, die .members oder .contributions auf diesen Responses referenzieren.
  (resp.contributions ?? []).length -> (resp.data ?? []).length

  === AnimeContributionModal.tsx ===

  1. SUGGESTED_ROLES ersetzen:
     Den lokalen SUGGESTED_ROLES-Array (Zeile ~15-22) entfernen.
     Stattdessen FANSUB_GROUP_ROLE_OPTIONS aus @/types/fansub importieren und filtern:

         import { FANSUB_GROUP_ROLE_OPTIONS } from '@/types/fansub'

         // Anime-Contribution-konforme Rollen (fansub_lead ist kein anime_contribution-Code)
         const ANIME_CONTRIBUTION_ROLES = FANSUB_GROUP_ROLE_OPTIONS.filter(
           (r) => r.code !== 'fansub_lead'
         )

  2. Freitext-Input entfernen:
     Den kompletten Block "Freitext-Rolle hinzufuegen" (Zeile ~283-311) entfernen:
     - roleInputByMemberId State
     - setRoleInputByMemberId State-Setter
     - <input type="text" placeholder="Eigene Rolle eingeben…" ...>-Block
     - "Hinzufügen"-Button

  3. Chip-Rendering anpassen:
     SUGGESTED_ROLES.map -> ANIME_CONTRIBUTION_ROLES.map (restlicher Chip-Code unveraendert)

  4. "Benutzerdefinierte Rollen"-Block (Zeile ~254-281) entfernen:
     Da es keinen Freitext mehr gibt, koennen auch keine Rollen vorhanden sein, die nicht in
     ANIME_CONTRIBUTION_ROLES sind. Diesen Block entfernen.

  Alle user-facing Strings mit korrekten Umlauten.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>
  - GroupMembersTab liest response.data
  - MemberRolesTab liest response.data; laedt Rollen per member_id fuer alle Mitglieder; Rollenauswahl per Dropdown
  - AnimeContributionsTab liest response.data
  - AnimeContributionModal nutzt ANIME_CONTRIBUTION_ROLES (quality_checker statt qc); kein Freitext-Input
  - TypeScript compiliert fehlerfrei (npx tsc --noEmit)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Frontend-seitige Contract-Angleichung: Envelope-Typen auf {data: ...}, listMemberRoles mit member_id,
  alle drei Tabs aktualisiert, AnimeContributionModal mit seed-konformen Rollenchips.
  </what-built>
  <how-to-verify>
  1. Docker Compose starten (oder npm run dev in frontend/).
  2. Als Admin zu /admin/fansubs/[id]/edit navigieren (eine bestehende Gruppe).
  3. Tab "Mitglieder" oeffnen:
     - Tab laedt ohne Fehler (kein "undefined"-Fehler in der Konsole)
     - Mitglieder werden angezeigt (display_name sichtbar)
  4. Tab "Rollen" oeffnen:
     - Tab laedt ohne Fehler
     - Rollen-Modal oeffnen: Rollenfeld ist ein Dropdown, kein Freitext
     - Dropdown enthaelt z.B. "Übersetzung", "Timing", "Qualitätscheck" (kein "QC")
  5. Tab "Contributions" oeffnen:
     - Tab laedt ohne Fehler
     - Modal oeffnen: Rollenchips zeigen "Qualitätscheck" (quality_checker), kein "QC"-Chip
     - Kein Freitext-Eingabefeld fuer Rollen sichtbar
  6. Browser-Konsole: keine unhandled Promise-Fehler oder "Cannot read properties of undefined"
  </how-to-verify>
  <resume-signal>Tippe "freigegeben" oder beschreibe gefundene Probleme</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Browser → API | Frontend sendet nur seed-konforme role_codes; Backend validiert trotzdem gegen role_definitions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-69-10 | Tampering | Freitext-Rollen im Modal | mitigate | Freitext entfernt; Backend validiert role_codes gegen role_definitions (unabhaengige Schicht) |
| T-69-11 | Information Disclosure | Tabs bei Permission-Denial | mitigate | Handler gibt 403 zurueck; Tab zeigt Fehlermeldung statt leerer Liste (Fehlerbehandlung bereits vorhanden) |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Abhaengigkeiten installiert |
</threat_model>

<verification>
Nach Implementierung:
1. npx tsc --noEmit --project frontend/tsconfig.json -- kein Fehler
2. grep "response\.members\|response\.roles\|response\.contributions" frontend/src/app/admin/fansubs/\[id\]/edit/ -- keine Treffer (alle .data)
3. grep "quality_checker" frontend/src/app/admin/fansubs/\[id\]/edit/AnimeContributionModal.tsx -- implizit via FANSUB_GROUP_ROLE_OPTIONS (kein direktes "qc")
4. grep "\bqc\b" frontend/src/app/admin/fansubs/\[id\]/edit/AnimeContributionModal.tsx -- kein Treffer
5. grep "member_id=" frontend/src/lib/api.ts -- Treffer in listMemberRoles
6. Manuelle Tab-Pruefung (Checkpoint)
</verification>

<success_criteria>
- HistFansubGroupMemberListResponse.data, HistGroupMemberRoleListResponse.data, AnimeContributionListResponse.data
- listMemberRoles sendet ?member_id=N
- Alle drei Tabs lesen .data und laden fehlerfrei
- AnimeContributionModal: "qc"-Code weg, "quality_checker" via FANSUB_GROUP_ROLE_OPTIONS
- MemberRolesTab: Dropdown aus FANSUB_GROUP_ROLE_OPTIONS statt Freitext
- TypeScript fehlerfrei
- Checkpoint: Admin-User bestaetigt Tab-Funktionstuechtigkeit
</success_criteria>

<output>
Erstelle .planning/phases/69-fansub-contributions-contract-und-permission-haertung/69-04-SUMMARY.md wenn fertig.
</output>
