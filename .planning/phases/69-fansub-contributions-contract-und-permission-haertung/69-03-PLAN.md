---
phase: 69-fansub-contributions-contract-und-permission-haertung
plan: 03
type: execute
wave: 2
depends_on:
  - 69-01
  - 69-02
files_modified:
  - backend/internal/handlers/fansub_hist_group_members_handler.go
  - backend/internal/handlers/fansub_hist_group_member_roles_handler.go
  - backend/internal/handlers/fansub_anime_contributions_handler.go
  - backend/cmd/server/main.go
autonomous: true
requirements:
  - P69-SC1
  - P69-SC2
  - P69-SC3
  - P69-SC4
  - P69-SC5
  - P69-SC7

must_haves:
  truths:
    - "ListHistGroupMembers ruft ListByFansubGroupWithDisplay auf und gibt {data: [...]} zurueck"
    - "CreateHistGroupMember nimmt display_name entgegen, ruft CreateWithAutoMember auf, gibt {data: item} zurueck"
    - "Alle schreibenden Handler (Create, Update, Delete) pruefen CanForFansubGroup mit ActionFansubGroupMembersManage"
    - "Alle lesenden Handler pruefen CanForFansubGroup mit ActionFansubGroupMembersView"
    - "CreateHistGroupMemberRole prueft MemberBelongsToFansub vor dem Create"
    - "CreateAnimeContribution prueft MemberBelongsToFansub vor dem Create und gibt Status weiter"
    - "Alle 12 Handler-Methoden (4+4+4) auditieren Permission-Denials"
    - "main.go uebergibt permissionSvc und auditLogRepo an alle drei neuen Handler"
  artifacts:
    - path: "backend/internal/handlers/fansub_hist_group_members_handler.go"
      provides: "Member-Auto-Create-Flow + Permission-Pruefungen fuer alle 4 Endpunkte"
      exports: ["FansubHistGroupMembersHandler", "NewFansubHistGroupMembersHandler"]
    - path: "backend/internal/handlers/fansub_hist_group_member_roles_handler.go"
      provides: "Cross-Group-Guard + Permission-Pruefungen fuer alle 4 Endpunkte"
      exports: ["FansubHistGroupMemberRolesHandler", "NewFansubHistGroupMemberRolesHandler"]
    - path: "backend/internal/handlers/fansub_anime_contributions_handler.go"
      provides: "MemberBelongsToFansub-Guard + status-Durchreichung + Permission-Pruefungen + CreateOrUpdate"
      exports: ["FansubAnimeContributionsHandler", "NewFansubAnimeContributionsHandler"]
    - path: "backend/cmd/server/main.go"
      provides: "Handler-Konstruktoren erhalten permissionSvc und auditLogRepo"
  key_links:
    - from: "FansubHistGroupMembersHandler.CreateHistGroupMember"
      to: "HistGroupMembersRepository.CreateWithAutoMember"
      via: "req.DisplayName -> input.DisplayName"
    - from: "alle schreibenden Handler"
      to: "permissionSvc.CanForFansubGroup"
      via: "permissionActorFromContext + ActionFansubGroupMembersManage + auditPermissionDenied"
    - from: "FansubAnimeContributionsHandler.CreateAnimeContribution"
      to: "contributionsRepo.MemberBelongsToFansub"
      via: "Cross-Group-Check vor dem Repo-Create"
---

<objective>
Haertet die drei Handler aus Phasen 62/63 ab:
- Permission-Check (CanForFansubGroup) in allen 12 Methoden (Lesen: MembersView, Schreiben: MembersManage)
- Member-Auto-Create-Flow im GroupMembers-Handler (display_name → CreateWithAutoMember)
- Cross-Group-Guard in MemberRoles- und Contributions-Handler (MemberBelongsToFansub)
- Status-Feld beim Contributions-Create durchreichen
- ListHistGroupMembers auf WithDisplay umstellen
- Contributions: CreateOrUpdate statt reinem Create (F6-Fix)
- main.go: permissionSvc + auditLogRepo in alle drei Handler injizieren

Purpose: Sicherheitskritische Fehlstellen F2, F3, F4, F5, F7 beheben; alle Admin-Endpunkte
sind danach gruppenberechtigt und funktional korrekt.
Output: Drei ueberarbeitete Handler-Dateien + main.go-Anpassung.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/PROJECT.md
@C:/Users/admin/Documents/Team4s/.planning/ROADMAP.md
@C:/Users/admin/Documents/Team4s/.planning/phases/69-fansub-contributions-contract-und-permission-haertung/69-CONTEXT.md
</context>

<interfaces>
<!-- Permission-Muster aus fansub_group_links.go (1:1 replizieren) -->

// permissionActorFromContext gibt (identity, actor, ok) zurueck; schreibt bei Fehler direkt in c
identity, actor, ok := permissionActorFromContext(c)
if !ok { return }

// CanForFansubGroup-Pruefung
result, err := h.permissionSvc.CanForFansubGroup(c.Request.Context(), actor, permissions.ActionFansubGroupMembersManage, fansubID)
if err != nil {
    writePermissionInternalError(c, err, "Berechtigung konnte nicht geprüft werden.")
    return
}
if !result.Allowed {
    auditPermissionDenied(c, h.auditLogRepo, identity, "event.type.denied", &fansubID, "scope_type", &targetID, permissions.ActionFansubGroupMembersManage, result)
    writePermissionDenied(c, result)
    return
}

// Vorhandene Permission-Actions fuer diesen Scope
permissions.ActionFansubGroupMembersView   // fuer GET/List
permissions.ActionFansubGroupMembersManage // fuer POST/PATCH/DELETE

<!-- Neue Repository-Methoden aus Wave 1 (Plan 02) -->
func (r *HistGroupMembersRepository) CreateWithAutoMember(ctx, input HistGroupMemberAutoCreateInput) (*HistGroupMemberDisplayRow, error)
type HistGroupMemberAutoCreateInput struct {
    FansubGroupID int64
    DisplayName   string
    JoinedYear    *int
    LeftYear      *int
    Status        string
    Visibility    string
    CreatedBy     *int64
}

func (r *AnimeContributionsRepository) CreateOrUpdate(ctx, fansubGroupID, animeID int64, input AnimeContributionInput) (*AnimeContributionDisplayRow, error)
// AnimeContributionInput hat jetzt Status string

func (r *AnimeContributionsRepository) MemberBelongsToFansub(ctx, memberID, fansubGroupID int64) (bool, error)
func (r *HistGroupMembersRepository) ListByFansubGroupWithDisplay(ctx, fansubGroupID) ([]HistGroupMemberDisplayRow, error)

<!-- Repository fuer Cross-Group-Check bei MemberRoles -->
// HistGroupMemberRolesRepository hat keinen eigenen Group-Check,
// aber HistGroupMembersRepository hat GetByID (gibt HistGroupMemberRow mit FansubGroupID).
// Fuer den MemberRoles-Cross-Group-Check: GetByID auf histMembersRepo,
// dann row.FansubGroupID == fansubID pruefen.
// histMembersRepo muss daher im RolesHandler als neues Feld vorhanden sein.

<!-- main.go: aktuelle Handler-Konstruktoren (Zeilen 333-335) -->
histGroupMembersHandler := handlers.NewFansubHistGroupMembersHandler(histGroupMembersRepo, badgeService)
histGroupMemberRolesHandler := handlers.NewFansubHistGroupMemberRolesHandler(histGroupMemberRolesRepo, badgeService)
animeContributionsHandler := handlers.NewFansubAnimeContributionsHandler(animeContributionsRepo, histGroupMemberRolesRepo)
// permissionSvc und auditLogRepo existieren bereits in main.go (Zeilen 126-127)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: fansub_hist_group_members_handler — Permission + Auto-Create + WithDisplay</name>
  <files>backend/internal/handlers/fansub_hist_group_members_handler.go</files>
  <action>
  Lies backend/internal/handlers/fansub_hist_group_members_handler.go und backend/internal/handlers/fansub_group_links.go vollstaendig (Permission-Muster).

  Struct-Erweiterung:
  FansubHistGroupMembersHandler bekommt zwei neue Felder: permissionSvc und auditLogRepo.
  Importiere "team4s.v3/backend/internal/permissions" falls noch nicht vorhanden.

  NewFansubHistGroupMembersHandler-Signatur anpassen:
      func NewFansubHistGroupMembersHandler(
          repo *repository.HistGroupMembersRepository,
          badgeService *services.BadgeService,
          permissionSvc *permissions.Service,
          auditLogRepo *repository.AuditLogRepository,
      ) *FansubHistGroupMembersHandler

  Request-Struct anpassen:
  histGroupMemberCreateRequest um DisplayName string `json:"display_name"` ergaenzen.
  Feld MemberID int64 `json:"member_id"` beibehalten (fuer Abwaertskompatibilitaet, aber nicht mehr zwingend).

  ListHistGroupMembers:
  - permissionActorFromContext aufrufen
  - CanForFansubGroup mit ActionFansubGroupMembersView pruefen
  - auditPermissionDenied bei Ablehnung (Event: "hist_group_member.list.denied")
  - histMembersRepo.ListByFansubGroupWithDisplay statt ListByFansubGroup aufrufen
  - c.JSON(200, gin.H{"data": items})

  CreateHistGroupMember:
  - permissionActorFromContext aufrufen
  - CanForFansubGroup mit ActionFansubGroupMembersManage pruefen
  - auditPermissionDenied bei Ablehnung (Event: "hist_group_member.create.denied")
  - Validierung: req.DisplayName muss nicht-leer sein (badRequest falls leer)
  - histMembersRepo.CreateWithAutoMember aufrufen mit HistGroupMemberAutoCreateInput{
        FansubGroupID: fansubID,
        DisplayName:   strings.TrimSpace(req.DisplayName),
        JoinedYear:    req.JoinedYear,
        LeftYear:      req.LeftYear,
        Status:        req.Status,
        Visibility:    req.Visibility,
        CreatedBy:     &identity.AppUserID,
    }
  - ErrConflict => 409 "dieses mitglied ist bereits in der gruppe eingetragen"
  - ErrNotFound => 404 "fansubgruppe nicht gefunden"
  - Audit-Log bei Erfolg (Event: "hist_group_member.created", TargetType: "hist_fansub_group_member", TargetID: &item.ID)
  - c.JSON(201, gin.H{"data": item})

  UpdateHistGroupMember:
  - permissionActorFromContext + CanForFansubGroup ActionFansubGroupMembersManage
  - auditPermissionDenied bei Ablehnung (Event: "hist_group_member.update.denied")
  - Restliche Logik unveraendert
  - Audit-Log bei Erfolg (Event: "hist_group_member.updated")
  - c.JSON(200, gin.H{"data": item})

  DeleteHistGroupMember:
  - permissionActorFromContext + CanForFansubGroup ActionFansubGroupMembersManage
  - auditPermissionDenied bei Ablehnung (Event: "hist_group_member.delete.denied")
  - Audit-Log bei Erfolg (Event: "hist_group_member.deleted")
  - c.Status(204) unveraendert

  Zeilenlimit: Wenn die Datei durch die Aenderungen >= 450 Zeilen erreicht, die Validierungsfunktionen
  und den histGroupMemberCreateRequest in eine separate Datei
  fansub_hist_group_members_request.go auslagern.

  Alle user-facing Strings mit korrekten Umlauten (ä, ö, ü, ß).
  </action>
  <verify>
    <automated>go build ./backend/...</automated>
  </verify>
  <done>
  - NewFansubHistGroupMembersHandler nimmt permissionSvc + auditLogRepo entgegen
  - ListHistGroupMembers nutzt ListByFansubGroupWithDisplay
  - CreateHistGroupMember nutzt CreateWithAutoMember (display_name statt member_id)
  - Alle 4 Methoden pruefen CanForFansubGroup
  - Alle 4 Methoden auditieren Permission-Denials
  - go build fehlerfrei
  - Keine Datei ueberschreitet 450 Zeilen
  </done>
</task>

<task type="auto">
  <name>Task 2: fansub_hist_group_member_roles_handler — Permission + Cross-Group-Guard</name>
  <files>backend/internal/handlers/fansub_hist_group_member_roles_handler.go</files>
  <action>
  Lies backend/internal/handlers/fansub_hist_group_member_roles_handler.go und backend/internal/handlers/fansub_group_links.go vollstaendig.

  Struct-Erweiterung:
  FansubHistGroupMemberRolesHandler bekommt drei neue Felder:
  - permissionSvc *permissions.Service
  - auditLogRepo *repository.AuditLogRepository
  - histMembersRepo *repository.HistGroupMembersRepository  // fuer Cross-Group-Check

  NewFansubHistGroupMemberRolesHandler-Signatur anpassen:
      func NewFansubHistGroupMemberRolesHandler(
          repo *repository.HistGroupMemberRolesRepository,
          badgeService *services.BadgeService,
          permissionSvc *permissions.Service,
          auditLogRepo *repository.AuditLogRepository,
          histMembersRepo *repository.HistGroupMembersRepository,
      ) *FansubHistGroupMemberRolesHandler

  ListHistGroupMemberRoles:
  - permissionActorFromContext + CanForFansubGroup ActionFansubGroupMembersView
  - auditPermissionDenied bei Ablehnung (Event: "hist_group_member_role.list.denied")
  - Bestehende member_id-Query-Pflicht unveraendert (F3 ist FE-seitig, der Handler ist bereits korrekt)
  - c.JSON(200, gin.H{"data": items})

  CreateHistGroupMemberRole:
  - permissionActorFromContext + CanForFansubGroup ActionFansubGroupMembersManage
  - auditPermissionDenied bei Ablehnung (Event: "hist_group_member_role.create.denied")
  - Cross-Group-Guard: histMembersRepo.GetByID(ctx, req.HistFansubGroupMemberID) aufrufen.
    Wenn row.FansubGroupID != fansubID => 422 "mitglied gehoert nicht zu dieser fansubgruppe"
  - Restliche Logik unveraendert (role_code-Validierung bleibt)
  - Audit-Log bei Erfolg (Event: "hist_group_member_role.created", TargetID: &item.ID)
  - c.JSON(201, gin.H{"data": item})

  UpdateHistGroupMemberRole:
  - permissionActorFromContext + CanForFansubGroup ActionFansubGroupMembersManage
  - auditPermissionDenied bei Ablehnung (Event: "hist_group_member_role.update.denied")
  - Audit-Log bei Erfolg (Event: "hist_group_member_role.updated")
  - c.JSON(200, gin.H{"data": item})

  DeleteHistGroupMemberRole:
  - permissionActorFromContext + CanForFansubGroup ActionFansubGroupMembersManage
  - auditPermissionDenied bei Ablehnung (Event: "hist_group_member_role.delete.denied")
  - Audit-Log bei Erfolg (Event: "hist_group_member_role.deleted")
  - c.Status(204)

  Zeilenlimit: Wenn >= 450 Zeilen, Validierungsfunktionen in fansub_hist_group_member_roles_request.go auslagern.

  Alle user-facing Strings mit korrekten Umlauten.
  </action>
  <verify>
    <automated>go build ./backend/...</automated>
  </verify>
  <done>
  - NewFansubHistGroupMemberRolesHandler nimmt permissionSvc + auditLogRepo + histMembersRepo entgegen
  - CreateHistGroupMemberRole prueft Gruppen-Zugehoerigkeit (FansubGroupID-Vergleich)
  - Alle 4 Methoden pruefen CanForFansubGroup
  - Alle 4 Methoden auditieren Permission-Denials
  - go build fehlerfrei
  - Keine Datei ueberschreitet 450 Zeilen
  </done>
</task>

<task type="auto">
  <name>Task 3: fansub_anime_contributions_handler + main.go — Permission, MemberGuard, Status, Upsert-Semantik</name>
  <files>backend/internal/handlers/fansub_anime_contributions_handler.go, backend/cmd/server/main.go</files>
  <action>
  Lies backend/internal/handlers/fansub_anime_contributions_handler.go,
  backend/internal/handlers/fansub_group_links.go und backend/cmd/server/main.go vollstaendig.

  === fansub_anime_contributions_handler.go ===

  Struct-Erweiterung:
  FansubAnimeContributionsHandler bekommt zwei neue Felder:
  - permissionSvc *permissions.Service
  - auditLogRepo *repository.AuditLogRepository

  NewFansubAnimeContributionsHandler-Signatur anpassen:
      func NewFansubAnimeContributionsHandler(
          contributionsRepo *repository.AnimeContributionsRepository,
          rolesRepo *repository.HistGroupMemberRolesRepository,
          permissionSvc *permissions.Service,
          auditLogRepo *repository.AuditLogRepository,
      ) *FansubAnimeContributionsHandler

  animeContributionCreateRequest um Status-Feld ergaenzen:
      Status string `json:"status"`

  ListAnimeContributions:
  - permissionActorFromContext + CanForFansubGroup ActionFansubGroupMembersView
  - auditPermissionDenied bei Ablehnung (Event: "anime_contribution.list.denied")
  - h.contributionsRepo.ListByFansubAndAnimeWithDisplay statt ListByFansubAndAnime aufrufen
  - c.JSON(200, gin.H{"data": items})

  CreateAnimeContribution:
  - permissionActorFromContext + CanForFansubGroup ActionFansubGroupMembersManage
  - auditPermissionDenied bei Ablehnung (Event: "anime_contribution.create.denied")
  - Cross-Group-Guard: contributionsRepo.MemberBelongsToFansub aufrufen.
    Wenn false => 422 "mitglied gehoert nicht zu dieser fansubgruppe"
  - Status aus req.Status in das AnimeContributionInput.Status setzen ("draft" als Fallback wenn leer)
  - contributionsRepo.CreateOrUpdate statt contributionsRepo.Create aufrufen
    (per D3-Logik: Frontend nennt es "upsert", Backend implementiert ON CONFLICT DO UPDATE)
  - ErrConflict (unerwarteter Unique-Fehler ausserhalb ON CONFLICT) => 409
  - Audit-Log bei Erfolg (Event: "anime_contribution.created", TargetID: &item.ID)
  - c.JSON(201, gin.H{"data": item})

  UpdateAnimeContribution:
  - permissionActorFromContext + CanForFansubGroup ActionFansubGroupMembersManage
  - auditPermissionDenied bei Ablehnung (Event: "anime_contribution.update.denied")
  - Audit-Log bei Erfolg (Event: "anime_contribution.updated")
  - GetByIDWithDisplay statt GetByID fuer die Antwort (wenn Repo das nach Update nicht direkt liefert,
    nutze contributionsRepo.GetByIDWithDisplay(ctx, contributionID) am Ende)
  - c.JSON(200, gin.H{"data": item})

  DeleteAnimeContribution:
  - permissionActorFromContext + CanForFansubGroup ActionFansubGroupMembersManage
  - auditPermissionDenied bei Ablehnung (Event: "anime_contribution.delete.denied")
  - Audit-Log bei Erfolg (Event: "anime_contribution.deleted")
  - c.Status(204)

  Zeilenlimit: Datei ist derzeit ~268 Zeilen — wird durch die Aenderungen wahrscheinlich unter 450 bleiben.
  Falls doch >= 450, Permission-Hilfsmethoden in fansub_anime_contributions_permissions.go auslagern.

  Alle user-facing Strings mit korrekten Umlauten.

  === backend/cmd/server/main.go ===

  Zeilen 333-335 aktualisieren (Handler-Konstruktoren):

      histGroupMembersHandler := handlers.NewFansubHistGroupMembersHandler(
          histGroupMembersRepo, badgeService, permissionSvc, auditLogRepo,
      )
      histGroupMemberRolesHandler := handlers.NewFansubHistGroupMemberRolesHandler(
          histGroupMemberRolesRepo, badgeService, permissionSvc, auditLogRepo, histGroupMembersRepo,
      )
      animeContributionsHandler := handlers.NewFansubAnimeContributionsHandler(
          animeContributionsRepo, histGroupMemberRolesRepo, permissionSvc, auditLogRepo,
      )

  permissionSvc (Zeile 126) und auditLogRepo (Zeile 127) existieren bereits — kein neuer Repo noetig.
  </action>
  <verify>
    <automated>go build ./backend/...</automated>
  </verify>
  <done>
  - NewFansubAnimeContributionsHandler nimmt permissionSvc + auditLogRepo entgegen
  - ListAnimeContributions nutzt ListByFansubAndAnimeWithDisplay
  - CreateAnimeContribution: MemberBelongsToFansub-Guard + Status aus Request + CreateOrUpdate
  - UpdateAnimeContribution: Permission + auditiert
  - DeleteAnimeContribution: Permission + auditiert
  - main.go: alle drei Handler-Konstruktoren mit permissionSvc + auditLogRepo aktualisiert
  - go build ./backend/... fehlerfrei
  - Keine Handler-Datei ueberschreitet 450 Zeilen
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| HTTP-Request → Handler | fansubID aus URL-Param; actor aus Auth-Context; Cross-Group-Versuche werden im Handler abgewiesen |
| Handler → Repository | Nach Permission- und Guard-Check werden nur valide Werte weitergegeben |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-69-06 | Elevation of Privilege | Alle 12 Handler-Methoden ohne Permission-Check | mitigate | CanForFansubGroup in jeder Methode; auditPermissionDenied bei Ablehnung |
| T-69-07 | Tampering | Cross-Group-Member in MemberRoles-Create | mitigate | GetByID-Check: row.FansubGroupID == fansubID |
| T-69-08 | Tampering | Cross-Group-Member in Contributions-Create | mitigate | MemberBelongsToFansub + Composite-FK (0088) |
| T-69-09 | Tampering | status-Manipulation im Contribution-Create | mitigate | Handler validiert erlaubte Status-Werte (draft/proposed/confirmed/disputed/hidden) per Enum-Check |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | Keine Paketinstallation |
</threat_model>

<verification>
Nach Implementierung:
1. go build ./backend/... -- kein Fehler
2. grep "CanForFansubGroup" backend/internal/handlers/fansub_hist_group_members_handler.go -- mindestens 4 Treffer
3. grep "CanForFansubGroup" backend/internal/handlers/fansub_hist_group_member_roles_handler.go -- mindestens 4 Treffer
4. grep "CanForFansubGroup" backend/internal/handlers/fansub_anime_contributions_handler.go -- mindestens 4 Treffer
5. grep "CreateWithAutoMember" backend/internal/handlers/fansub_hist_group_members_handler.go -- Treffer
6. grep "MemberBelongsToFansub" backend/internal/handlers/fansub_anime_contributions_handler.go -- Treffer
7. grep "ListByFansubGroupWithDisplay" backend/internal/handlers/fansub_hist_group_members_handler.go -- Treffer
8. grep "ListByFansubAndAnimeWithDisplay" backend/internal/handlers/fansub_anime_contributions_handler.go -- Treffer
9. grep "permissionSvc" backend/cmd/server/main.go -- mehr Treffer als zuvor (Handler-Konstruktoren)
</verification>

<success_criteria>
- Alle 12 Handler-Methoden pruefen CanForFansubGroup und auditieren Denials
- Member-Create-Flow: display_name → CreateWithAutoMember (P69-SC2)
- Cross-Group-Guards in MemberRoles und Contributions aktiv (P69-SC5)
- Status-Feld im Contributions-Create durchgereicht (P69-SC7)
- ListHistGroupMembers und ListAnimeContributions nutzen WithDisplay-Repos (P69-SC1)
- CreateAnimeContribution nutzt CreateOrUpdate (P69-SC6)
- main.go korrekt verdrahtet, go build fehlerfrei
</success_criteria>

<output>
Erstelle .planning/phases/69-fansub-contributions-contract-und-permission-haertung/69-03-SUMMARY.md wenn fertig.
</output>
