---
phase: 06-provisioning-and-lifecycle-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/models/asset_lifecycle.go
  - backend/internal/repository/asset_lifecycle_subjects.go
  - backend/internal/repository/asset_lifecycle_audit.go
  - backend/internal/repository/asset_lifecycle_repository_test.go
  - backend/internal/services/asset_lifecycle_service.go
  - backend/internal/services/asset_lifecycle_errors.go
  - backend/internal/services/asset_lifecycle_service_test.go
  - backend/internal/handlers/media_upload_test.go
autonomous: true
requirements:
  - PROV-04
  - LIFE-03
  - LIFE-04
must_haves:
  truths:
    - "Asset lifecycle validation is driven by one reusable V2-first anime policy contract instead of handler-local anime-cover assumptions."
    - "Only the supported Phase 6 entity type `anime` is accepted, while the contract remains extensible for later entity types."
    - "Entity existence, path containment, and reserved-folder safety are validated before any filesystem mutation is attempted."
    - "Every provisioning or lifecycle action can carry the acting admin user ID into a durable audit shape that later upload and delete flows can reuse."
  artifacts:
    - path: "backend/internal/models/asset_lifecycle.go"
      provides: "Shared policy, validation, provisioning-result, and audit DTOs for the generic lifecycle contract"
    - path: "backend/internal/services/asset_lifecycle_service.go"
      provides: "Reusable policy-driven lifecycle service that resolves canonical roots, validates subjects, and plans idempotent provisioning"
    - path: "backend/internal/repository/asset_lifecycle_subjects.go"
      provides: "Repository helpers for validating anime existence and persisting lifecycle audit events"
  key_links:
    - from: "backend/internal/services/asset_lifecycle_service.go"
      to: "backend/internal/repository/asset_lifecycle_subjects.go"
      via: "subject validation and lifecycle audit persistence"
      pattern: "LookupAssetLifecycleSubject|RecordAssetLifecycleEvent"
    - from: "backend/internal/services/asset_lifecycle_service.go"
      to: "backend/internal/models/asset_lifecycle.go"
      via: "shared lifecycle policy and result types"
      pattern: "AssetLifecyclePolicy|ProvisioningResult|AssetLifecycleError"
    - from: "backend/internal/handlers/media_upload_test.go"
      to: "backend/internal/services/asset_lifecycle_service.go"
      via: "foundation-level tests pinning policy behavior before upload integration"
      pattern: "TestAssetLifecycle"
---

<objective>
Create the reusable Phase 6 anime lifecycle foundation before touching the upload handler.

Purpose: define one authoritative server-side contract for canonical anime roots, supported asset-slot combinations, subject validation, path safety, and actor-attributed audit events in the V2 direction.
Output: shared lifecycle models, a policy-driven service, repository seams for anime lookup plus audit persistence, and focused tests that pin safety behavior.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-provisioning-and-lifecycle-foundations/06-CONTEXT.md
@.planning/phases/06-provisioning-and-lifecycle-foundations/06-RESEARCH.md
@.planning/phases/06-provisioning-and-lifecycle-foundations/06-VALIDATION.md
@backend/internal/handlers/media_upload.go
@backend/internal/repository/media_upload.go
@backend/internal/repository/anime_assets.go
@backend/internal/handlers/fansub_media_upload.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing coverage for lifecycle policy validation, subject lookup, and actor-attributed audit behavior</name>
  <files>backend/internal/repository/asset_lifecycle_repository_test.go, backend/internal/services/asset_lifecycle_service_test.go, backend/internal/handlers/media_upload_test.go</files>
  <read_first>
    - backend/internal/handlers/media_upload.go
    - backend/internal/repository/media_upload.go
    - backend/internal/repository/anime_assets.go
    - backend/internal/repository/anime_assets.go
    - .planning/phases/06-provisioning-and-lifecycle-foundations/06-CONTEXT.md
    - .planning/phases/06-provisioning-and-lifecycle-foundations/06-RESEARCH.md
    - .planning/phases/06-provisioning-and-lifecycle-foundations/06-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `anime` is the only accepted Phase 6 entity type, with a V2-first allowlist for `cover`, `banner`, `logo`, `background`, and `background_video`.
    - Test 2: invalid entity IDs, unsupported asset types, and escaped storage paths fail before `os.MkdirAll` or any other filesystem mutation runs.
    - Test 3: existing valid canonical folders produce `already_exists` statuses, while missing required folders produce `created` statuses in a deterministic result payload.
    - Test 4: actor attribution is preserved in the service result or audit input so later upload integration can write durable lifecycle events with the authenticated admin user ID.
    - Test 5: inconsistent reserved layout, such as a file where a required folder should exist, returns a typed operator-safe validation error that names the reserved folder.
  </behavior>
  <action>Create failing unit tests around the new lifecycle foundation before implementing it. Add repository tests for anime subject lookup and lifecycle audit persistence aligned with the anime V2 asset seam. Add service tests that pin the exact policy table for `anime`, reject any freeform path composition, reject unsupported entity types and asset types, report `created` versus `already_exists` folder statuses, and preserve the acting admin `user_id` in the audit input. Extend `backend/internal/handlers/media_upload_test.go` only with foundation-level assertions that the future upload integration cannot bypass the new validation service.</action>
  <acceptance_criteria>
    - Test files assert that only `anime` is an accepted Phase 6 contract entity type.
    - At least one test fails when a reserved folder path resolves outside the configured storage root.
    - At least one test fails when the service encounters a file where a required canonical folder should exist.
    - At least one test asserts that the lifecycle audit input contains the authenticated admin user ID.
    - The new tests fail until the lifecycle policy, validation, and audit seams are implemented.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/services ./internal/repository -run "Test.*AssetLifecycle.*" -count=1</automated>
    <automated>cd backend && go test ./internal/handlers -run "Test.*MediaUpload.*" -count=1</automated>
  </verify>
  <done>A red-green safety net exists for the lifecycle policy contract, preflight validation, and actor attribution before upload integration starts.</done>
</task>

<task type="auto">
  <name>Task 2: Implement shared lifecycle models, repository seams, and the policy-driven validation service</name>
  <files>backend/internal/models/asset_lifecycle.go, backend/internal/repository/asset_lifecycle_subjects.go, backend/internal/repository/asset_lifecycle_audit.go, backend/internal/repository/asset_lifecycle_repository_test.go, backend/internal/services/asset_lifecycle_service.go, backend/internal/services/asset_lifecycle_errors.go, backend/internal/services/asset_lifecycle_service_test.go, backend/internal/handlers/media_upload_test.go</files>
  <read_first>
    - backend/internal/models/media_upload.go
    - backend/internal/repository/media_upload.go
    - backend/internal/repository/anime_assets.go
    - backend/internal/handlers/media_upload.go
    - backend/internal/handlers/fansub_media_upload.go
    - .planning/phases/06-provisioning-and-lifecycle-foundations/06-CONTEXT.md
    - .planning/phases/06-provisioning-and-lifecycle-foundations/06-RESEARCH.md
  </read_first>
  <action>Introduce `backend/internal/models/asset_lifecycle.go` with explicit policy and result types for anime V2 lifecycle work, including `AssetLifecyclePolicy`, `ProvisioningFolderStatus`, `ProvisioningResult`, `AssetLifecycleSubject`, and typed operator-safe errors. Implement repository helpers in `backend/internal/repository/asset_lifecycle_subjects.go` that validate whether an `anime` ID exists in the anime tables and align subject metadata with the current anime V2 asset seam, plus `backend/internal/repository/asset_lifecycle_audit.go` methods that persist lifecycle audit events keyed by actor `user_id`, entity type, entity ID, asset type, action, and result details. Implement `backend/internal/services/asset_lifecycle_service.go` so it resolves canonical absolute roots from server-owned anime policy, uses `filepath.Rel`-style containment checks instead of string-prefix guards, validates anime existence plus per-slot asset-type allowlists, detects reserved file-vs-directory collisions, and returns deterministic `created` or `already_exists` statuses for each required folder without mutating unrelated paths.</action>
  <acceptance_criteria>
    - `backend/internal/models/asset_lifecycle.go` defines shared lifecycle policy, subject, provisioning-result, and typed error structs used outside a single handler.
    - `backend/internal/services/asset_lifecycle_service.go` contains one Phase 6 policy table that names only `anime` and its allowed asset types.
    - Path containment checks in the new lifecycle service do not use `strings.HasPrefix`.
    - Repository seams expose subject validation and lifecycle audit persistence methods that accept actor user ID and entity identifiers.
    - The automated tests in this plan pass.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/services ./internal/repository -run "Test.*AssetLifecycle.*" -count=1</automated>
    <automated>cd backend && go test ./internal/handlers -run "Test.*MediaUpload.*" -count=1</automated>
  </verify>
  <done>The repo has one reusable lifecycle foundation for policy, validation, path safety, and actor-attributed audit support.</done>
</task>

</tasks>

<verification>
Run the targeted service/repository tests, then inspect the lifecycle service code to confirm that canonical path resolution and slot allowlists now live outside the upload handler and that the implementation targets the anime V2 seam instead of extending legacy upload metadata.
</verification>

<success_criteria>
Phase 6 has a reusable backend foundation: one V2-first anime lifecycle policy contract validates anime subjects, blocks unsafe paths before mutation, and carries admin attribution into durable audit-ready seams.
</success_criteria>

<output>
After completion, create `.planning/phases/06-provisioning-and-lifecycle-foundations/06-01-SUMMARY.md`
</output>
