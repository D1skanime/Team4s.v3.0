---
phase: 06-provisioning-and-lifecycle-foundations
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - backend/internal/handlers/media_upload.go
  - backend/internal/handlers/media_upload_image.go
  - backend/internal/handlers/media_upload_test.go
  - backend/internal/models/media_upload.go
  - backend/internal/repository/media_upload.go
  - backend/cmd/server/admin_routes.go
autonomous: true
requirements:
  - PROV-01
  - PROV-02
  - PROV-03
  - LIFE-02
must_haves:
  truths:
    - "The generic admin upload flow automatically provisions missing canonical anime folders on first upload without trusting client-provided paths."
    - "Re-running upload against an already-valid canonical structure is idempotent and reports which folders were created versus already present."
    - "Provisioning and upload failures return operator-usable validation or storage details instead of one generic processing failure."
    - "The existing `/admin/upload` seam becomes the first consumer of the Phase 6 lifecycle service rather than re-implementing folder logic inline."
  artifacts:
    - path: "backend/internal/handlers/media_upload.go"
      provides: "Thin upload handler that delegates provisioning validation and error mapping to the lifecycle service"
    - path: "backend/internal/models/media_upload.go"
      provides: "Upload response contract extended with provisioning status detail for the current upload request"
    - path: "backend/internal/handlers/media_upload_test.go"
      provides: "Behavioral tests for first-upload provisioning, idempotent reuse, and operator-safe failure messages"
  key_links:
    - from: "backend/internal/handlers/media_upload.go"
      to: "backend/internal/services/asset_lifecycle_service.go"
      via: "preflight provisioning before file processing"
      pattern: "EnsureCanonicalLayout|ProvisionForUpload"
    - from: "backend/internal/handlers/media_upload.go"
      to: "backend/internal/models/media_upload.go"
      via: "upload response now includes provisioning statuses and operator-safe error details"
      pattern: "UploadResponse|ProvisioningResult"
    - from: "backend/internal/handlers/media_upload.go"
      to: "backend/internal/repository/media_upload.go"
      via: "existing media asset/file persistence remains the write path after lifecycle preflight succeeds"
      pattern: "CreateMediaAsset|CreateMediaFile|CreateAnimeMedia"
---

<objective>
Integrate the new lifecycle foundation into the existing generic upload seam so Phase 6 delivers real anime provisioning behavior and operator-safe failures.

Purpose: make `/admin/upload` the first validated consumer of canonical anime provisioning, idempotent folder reuse, and detailed lifecycle errors without yet building the full Phase 7 upload UX.
Output: upload handler integration, response/error-shape hardening, and tests covering first-upload provisioning plus failure behavior.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-provisioning-and-lifecycle-foundations/06-CONTEXT.md
@.planning/phases/06-provisioning-and-lifecycle-foundations/06-RESEARCH.md
@.planning/phases/06-provisioning-and-lifecycle-foundations/06-VALIDATION.md
@.planning/phases/06-provisioning-and-lifecycle-foundations/06-01-PLAN.md
@backend/internal/handlers/media_upload.go
@backend/internal/handlers/media_upload_image.go
@backend/internal/models/media_upload.go
@backend/internal/repository/media_upload.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing upload-path coverage for auto-provisioning, idempotent reuse, and detailed operator failures</name>
  <files>backend/internal/handlers/media_upload_test.go</files>
  <read_first>
    - backend/internal/handlers/media_upload.go
    - backend/internal/models/media_upload.go
    - backend/internal/repository/media_upload.go
    - .planning/phases/06-provisioning-and-lifecycle-foundations/06-01-PLAN.md
    - .planning/phases/06-provisioning-and-lifecycle-foundations/06-CONTEXT.md
    - .planning/phases/06-provisioning-and-lifecycle-foundations/06-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: the first successful upload for an `anime` request provisions the canonical required folders and exposes per-folder `created` or `already_exists` statuses in the response.
    - Test 2: repeating the same upload for a valid existing structure does not recreate folders and reports idempotent reuse.
    - Test 3: the manual anime create/edit upload path works without Jellyfin-derived metadata and still uses the same lifecycle service path.
    - Test 4: invalid entity type, invalid entity ID, unsupported asset type, or invalid reserved folder layout produce operator-usable error messages that name the failing field or folder.
    - Test 5: low-level storage failures no longer collapse into only `verarbeitung fehlgeschlagen` when the lifecycle service already knows the exact provisioning problem.
  </behavior>
  <action>Extend `backend/internal/handlers/media_upload_test.go` with failing handler-level tests that exercise the new lifecycle service integration through `/admin/upload`. Cover first-upload canonical provisioning for `anime`, idempotent reuse on a second upload, the manual anime create/edit path without Jellyfin-derived metadata, and explicit German error messages for invalid entity type, invalid entity ID, unsupported asset type, and reserved-folder collisions. Assert that the upload response carries provisioning-status detail and that known validation failures are surfaced as operator-usable messages instead of a single generic processing error.</action>
  <acceptance_criteria>
    - Handler tests assert the response includes provisioning statuses for at least one successful upload.
    - Handler tests assert repeated upload preserves idempotent `already_exists` signaling.
    - Handler tests include at least one upload request that proves the manual anime path works without any Jellyfin payload or lookup.
    - Handler tests fail if validation and provisioning failures are still returned only as `verarbeitung fehlgeschlagen`.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/handlers -run "TestMediaUploadHandler_.*" -count=1</automated>
  </verify>
  <done>There is a red-green safety net for lifecycle-aware upload behavior and operator-safe error responses.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate lifecycle preflight into the generic upload handler and expose provisioning status details</name>
  <files>backend/internal/handlers/media_upload.go, backend/internal/handlers/media_upload_image.go, backend/internal/handlers/media_upload_test.go, backend/internal/models/media_upload.go, backend/internal/repository/media_upload.go, backend/cmd/server/admin_routes.go</files>
  <read_first>
    - backend/internal/handlers/media_upload.go
    - backend/internal/handlers/media_upload_image.go
    - backend/internal/repository/media_upload.go
    - backend/internal/services/asset_lifecycle_service.go
    - backend/internal/services/asset_lifecycle_errors.go
    - backend/internal/models/asset_lifecycle.go
    - .planning/phases/06-provisioning-and-lifecycle-foundations/06-CONTEXT.md
    - .planning/phases/06-provisioning-and-lifecycle-foundations/06-RESEARCH.md
  </read_first>
  <action>Refactor `backend/internal/handlers/media_upload.go` so `/admin/upload` validates and binds the restrictive request contract, resolves the authenticated admin actor, and calls the new lifecycle service before any file processing or `os.MkdirAll` work happens. Replace the current global `allowedEntityTypes` and `allowedAssetTypes` usage with lifecycle-policy validation that exposes only the canonical Phase 6 contract type `anime`, while steering successful persistence toward the anime V2 seam instead of deepening legacy upload metadata. Extend `backend/internal/models/media_upload.go` so the upload response can include provisioning-status detail, and update the handler error mapping to return field- or folder-specific validation messages for subject lookup, unsupported asset type, invalid reserved structure, and storage failures. Keep the existing media asset/media file persistence path in `backend/internal/repository/media_upload.go` only as an integration seam where necessary, but make successful uploads reuse the lifecycle service result rather than duplicating path derivation logic inline.</action>
  <acceptance_criteria>
    - `backend/internal/handlers/media_upload.go` delegates canonical provisioning and validation to the lifecycle service before file writes.
    - The public upload contract does not require any freeform path input and no longer exposes a broad global entity-type map.
    - Successful upload responses include provisioning status detail or an equivalent nested lifecycle result.
    - Validation failures for invalid entity type, invalid entity ID, unsupported asset type, or reserved-structure collisions return specific operator-safe messages.
    - `cd backend && go test ./internal/handlers ./internal/repository -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/handlers -run "TestMediaUploadHandler_.*" -count=1</automated>
    <automated>cd backend && go test ./internal/handlers ./internal/repository -count=1</automated>
  </verify>
  <done>The generic upload seam now uses canonical provisioning and surfaces idempotent, operator-safe lifecycle feedback without reopening Phase 7 scope.</done>
</task>

</tasks>

<verification>
Run the full backend handler and repository suite, then manually exercise a first upload and repeat upload for one anime in the non-Jellyfin create/edit path to confirm the response reports provisioning status and the filesystem is only created or reused inside the canonical anime root.
</verification>

<success_criteria>
Phase 6 now delivers real behavior: first upload provisions canonical folders safely, repeated upload is idempotent, and lifecycle failures tell operators exactly what went wrong.
</success_criteria>

<output>
After completion, create `.planning/phases/06-provisioning-and-lifecycle-foundations/06-02-SUMMARY.md`
</output>
