---
phase: 110-member-badges-ranglisten-ui-und-e2e-abnahme
plan: 03
type: execute
wave: 2
depends_on: ["110-02"]
files_modified:
  - frontend/src/types/profile.ts
  - frontend/src/components/profile/MemberProfileHero.tsx
  - frontend/src/components/profile/MemberProfileHero.test.tsx
  - frontend/src/components/profile/memberBadgeLabels.ts
  - frontend/src/components/profile/MemberBadgeChain.test.tsx
autonomous: true
requirements: [D-02, D-03]

must_haves:
  truths:
    - "D-02: The public member profile hero (isPublicView branch only) renders a HeroMetrics entry labeled 'Punkte' with the real profile.total_points value, including the honest value 0 (never hidden, never a placeholder) — never shown in the own-profile edit view"
    - "D-02: No ranking placement ('Platz N') or points breakdown is rendered anywhere in this plan's scope — explicitly deferred per D-02"
    - "D-03: memberBadgeLabels.ts's MEMBER_BADGE_PRESENTATIONS and PUBLIC_MEMBER_BADGE_CATALOG contain exactly the 8 locked role_entry_* entries (translator/timer/encoder/typesetter/quality_checker/project_lead/editor/raw_provider) with the exact German labels, lucide icons, and indigo/info styling from the UI-SPEC table"
    - "D-03: MemberBadgeChain (unmodified) renders each role-entry badge in the existing locked (Lock icon) state by default and in the earned state (real icon, indigo) the moment the badge_code is present in earnedBadges — proven by an automated test, not by inspection"
    - "SC-3: Badge display follows the existing Anime-/Fansub-Stil, UI-system, mobile, and accessibility rules by construction — zero new UI components are introduced, only catalog data and one HeroMetrics call"
  artifacts:
    - path: "frontend/src/types/profile.ts"
      provides: "total_points: number on PublicMemberProfileData, mirroring the Go/OpenAPI field added in Plan 110-02"
    - path: "frontend/src/components/profile/MemberProfileHero.tsx"
      provides: "HeroMetrics('Punkte', profile.total_points) rendered only in the isPublicView branch"
    - path: "frontend/src/components/profile/memberBadgeLabels.ts"
      provides: "8 new role_entry_* presentation + catalog entries, appended after the existing 9"
  key_links:
    - from: "frontend/src/components/profile/MemberProfileHero.tsx"
      to: "frontend/src/components/ui/HeroMetrics.tsx"
      via: "HeroMetrics items=[{label:'Punkte', value: totalPoints}]"
      pattern: "HeroMetrics"
    - from: "frontend/src/components/profile/memberBadgeLabels.ts"
      to: "frontend/src/components/profile/MemberBadgeChain.tsx"
      via: "PUBLIC_MEMBER_BADGE_CATALOG / getMemberBadgePresentation consumed unchanged"
      pattern: "role_entry_"
---

<objective>
Complete Phase 110's third and final CONTEXT.md-scoped slice: render the `total_points` field
(D-02) and the 8 role-entry badge catalog entries (D-03) that Plan 110-02 made available on the
backend. This plan is pure frontend composition — it adds zero new UI components, reusing
`HeroMetrics` and the existing `MemberBadgeChain`/`memberBadgeLabels.ts` catalog exactly as
`110-CONTEXT.md` and `110-UI-SPEC.md` require ("kein neues Badge-UI erfinden").

Purpose: make the backend work from Plan 110-02 visible to real users on the public member
profile, closing out the three-idea scope the user explicitly locked in ("klein bleiben, Idee
für Idee").
Output: public profile hero shows the point count; the "Auszeichnungen" (MemberBadgeChain)
section shows the 8 new role-entry badges in locked/earned state, all covered by RTL tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/110-member-badges-ranglisten-ui-und-e2e-abnahme/110-CONTEXT.md
@.planning/phases/110-member-badges-ranglisten-ui-und-e2e-abnahme/110-UI-SPEC.md
@.planning/phases/110-member-badges-ranglisten-ui-und-e2e-abnahme/110-RESEARCH.md
@.planning/phases/110-member-badges-ranglisten-ui-und-e2e-abnahme/110-PATTERNS.md

<!-- Plan 110-02 must have landed first: this plan reads profile.total_points, which only
     exists once the backend/OpenAPI/type changes from 110-02 are in place. -->
</context>

<interfaces>
From frontend/src/types/profile.ts (lines 144-148, 202-231, existing, verified — edit target):
```typescript
export interface PublicMemberBadge {
  id: number
  badge_code: string
  badge_category: string
}
export interface PublicMemberProfileData {
  member_id: number
  // ...
  public_badges: PublicMemberBadge[]
  recent_media: MemberProfileRecentMedia[]
  // ...
}
```

From frontend/src/components/ui/HeroMetrics.tsx (full file, existing, verified):
```tsx
export interface HeroMetricItem { label: string; value: ReactNode }
export interface HeroMetricsProps { items: HeroMetricItem[]; ariaLabel: string; className?: string }
export function HeroMetrics({ items, ariaLabel, className }: HeroMetricsProps)
```

From frontend/src/components/profile/MemberBadgeChain.tsx (full file, existing, verified —
DO NOT MODIFY, already handles earned/locked/progress/unknown-badge fallback generically):
```tsx
type MemberBadgeChainProps = { earnedBadges: PublicMemberBadge[]; catalog?: PublicMemberBadgeCatalogItem[] }
export function MemberBadgeChain({ earnedBadges, catalog = PUBLIC_MEMBER_BADGE_CATALOG }: MemberBadgeChainProps)
```

Role code -> badge_code -> label -> icon -> palette table (locked, from 110-UI-SPEC.md section 3):

| Role code | badge_code | Label | Icon | variant | palette | badge_category |
|---|---|---|---|---|---|---|
| translator | role_entry_translator | Erste Übersetzung | Languages | info | indigo | role_entry |
| timer | role_entry_timer | Erstes Timing | Clock3 | info | indigo | role_entry |
| encoder | role_entry_encoder | Erster Encode | Cpu | info | indigo | role_entry |
| typesetter | role_entry_typesetter | Erstes Typesetting | Type | info | indigo | role_entry |
| quality_checker | role_entry_quality_checker | Erste Qualitätsprüfung | ShieldCheck | info | indigo | role_entry |
| project_lead | role_entry_project_lead | Erste Dokumentation als Projektleitung | ClipboardList | info | indigo | role_entry |
| editor | role_entry_editor | Erstes Editing | Scissors | info | indigo | role_entry |
| raw_provider | role_entry_raw_provider | Erste Raw-Bereitstellung | HardDrive | info | indigo | role_entry |
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hero-Punktzahl (D-02) — Typ, MemberProfileHero, Tests</name>
  <files>frontend/src/types/profile.ts, frontend/src/components/profile/MemberProfileHero.tsx, frontend/src/components/profile/MemberProfileHero.test.tsx</files>
  <read_first>
    frontend/src/types/profile.ts (lines 144-148, 202-231 — `PublicMemberBadge` sibling-field
    style, `PublicMemberProfileData` edit target; `public_badges`/`recent_media` are required,
    non-optional fields — mirror that for `total_points`, not `?`)
    frontend/src/components/profile/MemberProfileHero.tsx (full file, 202 lines — existing
    helper-function pattern: `getAccountDisplayName`, `getProfileStatus` etc. each narrow the
    `MemberProfileData | PublicMemberProfileData` union via `'field' in profile`; imports line
    4 `import { Button, PageHeader } from '@/components/ui'`; insertion point: inside
    `styles.heroCopy`, directly after the `</h2>` closing `heroTitleRow` block (line ~170) and
    before the `{profile.bio ? (` block (line ~171), gated on `isPublicView`)
    frontend/src/components/profile/MemberProfileHero.test.tsx (lines 1-38 — `makePublicProfile`
    builder helper that already sets every currently-required `PublicMemberProfileData` field;
    this helper must gain a `total_points` default once the type requires it, or every existing
    test in this file will fail to typecheck)
    frontend/src/components/fansubs/FansubHeroSection.tsx (line 4 import, line 142 usage —
    real `HeroMetrics` consumer pattern: `<HeroMetrics items={heroStats} ariaLabel="..." />`)
    frontend/src/components/ui/HeroMetrics.tsx (full file — confirms `items=[]` renders
    nothing, so an explicit single-item array is required, not a conditional based on value)
  </read_first>
  <behavior>
    - `isPublicView={true}` with `profile.total_points = 220` renders a `<dl>` (via
      `HeroMetrics`) containing label "Punkte" and value "220"
    - `isPublicView={true}` with `profile.total_points = 0` still renders the "Punkte" metric
      with value "0" — the metric is never hidden or suppressed for a legitimately zero score
    - `isPublicView={false}` (own-profile edit view, `MemberProfileData` — no `total_points`
      field) never attempts to render the metric and never throws
    - No "Platz N" / ranking-position text is rendered anywhere (D-02 defers this)
  </behavior>
  <action>
    In `frontend/src/types/profile.ts`, add `total_points: number` to
    `PublicMemberProfileData`, placed directly after `public_badges` (non-optional, matching
    the required-in-OpenAPI contract from Plan 110-02). In
    `frontend/src/components/profile/MemberProfileHero.tsx`, add `HeroMetrics` to the existing
    `import { Button, PageHeader } from '@/components/ui'` line. Add a small helper function
    following the file's existing narrowing-helper style (alongside `getAccountDisplayName`/
    `getProfileStatus`): `function getTotalPoints(profile: MemberProfileData |
    PublicMemberProfileData): number | null { return 'total_points' in profile ?
    profile.total_points : null }`. At the insertion point described in `read_first`, render
    `{isPublicView && totalPoints !== null ? (<HeroMetrics items={[{ label: 'Punkte', value:
    totalPoints }]} ariaLabel="Mitglied-Punktzahl" />) : null}` (compute `const totalPoints =
    getTotalPoints(profile)` near the other derived-value `const`s at the top of the
    component). Update `MemberProfileHero.test.tsx`'s `makePublicProfile` builder to include a
    `total_points: 0` default in its returned object (so the type change does not break every
    existing test in the file), then add the three new test cases from the behavior block
    (write them FIRST as RED against the not-yet-updated component, confirm GREEN after the
    component change).
  </action>
  <acceptance_criteria>
    - Source: `frontend/src/types/profile.ts`'s `PublicMemberProfileData` interface contains
      `total_points: number` (non-optional)
    - Source: `MemberProfileHero.tsx` imports `HeroMetrics` from `@/components/ui` and contains
      `ariaLabel="Mitglied-Punktzahl"`
    - Source: no "Platz" / ranking-position string exists in the diff (grep confirms absence)
    - Behavior/test: `npm run test -- MemberProfileHero` (from `frontend/`) passes, including
      the `total_points=220`, `total_points=0`, and own-profile-view-no-crash cases
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- MemberProfileHero</automated>
  </verify>
  <done>The public member profile hero shows an honest "Punkte" metric (including 0) sourced
  from `profile.total_points`, never rendered in the own-profile edit view, verified by
  automated tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rollen-Einstiegs-Badge-Katalog (D-03) — memberBadgeLabels.ts, MemberBadgeChain-Tests</name>
  <files>frontend/src/components/profile/memberBadgeLabels.ts, frontend/src/components/profile/MemberBadgeChain.test.tsx</files>
  <read_first>
    frontend/src/components/profile/memberBadgeLabels.ts (full file, 67 lines — existing
    `MEMBER_BADGE_PRESENTATIONS` record and `PUBLIC_MEMBER_BADGE_CATALOG` array shapes to
    extend verbatim; existing `lucide-react` import list: `BadgeCheck, CalendarClock, Crown,
    Hexagon, Layers, Shield, Sparkles, Star`; `getMemberBadgePresentation`'s generic fallback
    at the bottom already covers any `badge_code` not in the map — do not duplicate that logic)
    frontend/src/components/profile/MemberBadgeChain.tsx (full file, 89 lines — confirms
    `catalogWithEarnedBadges` appends any `earnedBadges` entry not already in `catalog` in
    array order, so no manual reordering is needed for the 8 new entries; this file is NOT
    modified by this task)
    frontend/src/components/profile/MemberBadgeChain.test.tsx (full file, 70 lines — existing
    dynamic-import test pattern, local `catalog` array + `earnedBadges` prop + RTL
    `screen`/`within` assertions to extend with role-entry cases)
  </read_first>
  <behavior>
    - `MEMBER_BADGE_PRESENTATIONS` and `PUBLIC_MEMBER_BADGE_CATALOG` contain all 8 new
      `role_entry_*` keys with the exact labels/icons/palette from the interfaces table above
    - `MemberBadgeChain` rendered with a catalog containing `role_entry_translator` and
      `earnedBadges=[]` shows it in locked state (`aria-label="Erste Übersetzung gesperrt"`,
      `Lock` icon)
    - `MemberBadgeChain` rendered with the same catalog and
      `earnedBadges=[{ id: 0, badge_code: 'role_entry_translator', badge_category:
      'role_entry' }]` shows it in earned state (real `Languages` icon, no "gesperrt" label)
    - Existing catalog entries (`founding_member`, etc.) and existing test cases are unaffected
  </behavior>
  <action>
    In `frontend/src/components/profile/memberBadgeLabels.ts`, add `Languages, Clock3, Cpu,
    Type, ShieldCheck, ClipboardList, Scissors, HardDrive` to the existing `lucide-react`
    import. Append the 8 entries from the interfaces table above to
    `MEMBER_BADGE_PRESENTATIONS` (each `{ label, variant: 'info', Icon, palette: 'indigo' }`)
    and to `PUBLIC_MEMBER_BADGE_CATALOG` (each `{ badge_code, label:
    MEMBER_BADGE_PRESENTATIONS.role_entry_X.label, badge_category: 'role_entry' }`), after the
    existing 9 entries in both structures — do not reorder or remove any existing entry. In
    `MemberBadgeChain.test.tsx`, add the two new test cases from the behavior block, reusing
    the file's existing dynamic-import + local `catalog` array pattern with a `role_entry_
    translator` entry added to that local catalog for the test (do not depend on importing the
    full production `PUBLIC_MEMBER_BADGE_CATALOG` unless the test explicitly wants to assert
    against the real catalog's contents — if so, import it directly and assert
    `role_entry_translator`/its label are present in the array, matching the file's existing
    assertion style).
  </action>
  <acceptance_criteria>
    - Source: `memberBadgeLabels.ts`'s `MEMBER_BADGE_PRESENTATIONS` contains all 8 keys
      `role_entry_translator`, `role_entry_timer`, `role_entry_encoder`,
      `role_entry_typesetter`, `role_entry_quality_checker`, `role_entry_project_lead`,
      `role_entry_editor`, `role_entry_raw_provider`, each with `palette: 'indigo'` and
      `variant: 'info'`
    - Source: `PUBLIC_MEMBER_BADGE_CATALOG` contains the same 8 `badge_code` values with
      `badge_category: 'role_entry'`
    - Source: all German labels use correct umlauts (`Übersetzung`, `Qualitätsprüfung`) — no
      ASCII substitutes
    - Behavior/test: `npm run test -- MemberBadgeChain` (from `frontend/`) passes, including the
      new locked/earned role-entry cases, with zero regressions to the existing 2 test cases
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- MemberBadgeChain</automated>
  </verify>
  <done>The 8 role-entry badges are part of the existing badge catalog with correct German
  copy, icons, and indigo styling, rendering correctly in locked/earned state through the
  unmodified `MemberBadgeChain` component, verified by automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public profile page (SSR) -> rendered HTML | No new trust boundary; consumes the already-authorized `GetPublicMemberProfile` response from Plan 110-02 as-is |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-07 | Tampering (state integrity) | Badge earned/locked rendering | mitigate | `earnedBadges` is passed fresh from the SSR-fetched profile response on every page load, per the existing `MemberBadgeChain` contract; this plan introduces no client-side cache/memoization of badge membership (Pitfall 5) — a reversed badge from Plan 110-02 is invisible on the very next render |
| T-110-08 | Tampering | `total_points` display | accept | The frontend renders `profile.total_points` as received; it never computes, adjusts, or trusts a client-supplied point value — all point logic lives server-side (Plan 110-02) |
| T-110-SC | Tampering | npm/pip/cargo installs | accept (not applicable) | Zero new package installs in this plan |

Note: this plan is pure read-side rendering of already-authorized, already-scoped backend data.
No new abuse surface is introduced; ROADMAP SC6 remains out of scope per `110-CONTEXT.md`.
</threat_model>

<verification>
- `cd frontend && npm run test -- MemberProfileHero` passes
- `cd frontend && npm run test -- MemberBadgeChain` passes
- `cd frontend && npm run typecheck` (or equivalent) is clean
- Live UAT (combined with Plan 110-01/110-02, per 110-VALIDATION.md Manual-Only
  Verifications): `docker restart team4sv30-frontend` + hard refresh, visit a public member
  profile with `total_points > 0` and one with `total_points = 0`; confirm the "Punkte" metric
  and the role-entry badge locked/earned states render correctly on mobile width and with
  correct umlauts
</verification>

<success_criteria>
The public member profile hero displays an honest, always-visible point count, and the
existing "Auszeichnungen" (`MemberBadgeChain`) section displays the 8 new role-entry badges in
correct locked/earned state with correct German copy — using zero new UI components, verified
by automated tests.
</success_criteria>

<output>
Create `.planning/phases/110-member-badges-ranglisten-ui-und-e2e-abnahme/110-03-SUMMARY.md` when done
</output>
