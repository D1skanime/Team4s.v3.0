---
phase: 14-create-provider-search-separation-and-result-selection
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - frontend/src/types/admin.ts
  - frontend/src/lib/api/admin-anime-intake.ts
  - frontend/src/lib/api.admin-anime.test.ts
  - frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts
  - frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts
  - frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx
  - frontend/src/app/admin/anime/create/page.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "AniSearch title search can return multiple candidates without loading draft detail immediately."
    - "The existing exact-ID AniSearch path still works unchanged as a first-class create action."
    - "AniSearch title search fetches a candidate list first and does not fan out into per-result detail crawling."
  artifacts:
    - path: "frontend/src/lib/api/admin-anime-intake.ts"
      provides: "AniSearch title-search candidate helper separate from the exact-ID detail load helper"
      contains: "AniSearch"
    - path: "frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts"
      provides: "Controller state for AniSearch title query and candidate results"
      contains: "anisearch"
    - path: "frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx"
      provides: "AniSearch card UI with both exact-ID and title-search entry points"
      contains: "AniSearch"
  key_links:
    - from: "frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx"
      to: "frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts"
      via: "title-search input and search action props"
      pattern: "onSubmit"
    - from: "frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts"
      to: "frontend/src/lib/api/admin-anime-intake.ts"
      via: "candidate-list search before detail load"
      pattern: "AniSearch"
---

<objective>
Add AniSearch title-search candidate retrieval in the create flow while preserving the existing exact-ID AniSearch load path unchanged.

Purpose: After Jellyfin search is separated cleanly, Phase 14 can add the new AniSearch capability without mixing it into the final detail-crawl step too early.
Output: AniSearch title-search query state, candidate-list API/helper seam, and card-level UI that can show the operator multiple candidate results before any detail load begins.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/phases/14-create-provider-search-separation-and-result-selection/14-CONTEXT.md
@.planning/phases/14-create-provider-search-separation-and-result-selection/14-UI-SPEC.md
@.planning/phases/12-create-anisearch-intake-reintroduction-and-draft-merge-control/12-CONTEXT.md
@frontend/src/types/admin.ts
@frontend/src/lib/api/admin-anime-intake.ts
@frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts
@frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx
@frontend/src/app/admin/anime/create/page.tsx

<interfaces>
From `frontend/src/lib/api/admin-anime-intake.ts`:
```ts
export async function loadAdminAnimeCreateAniSearchDraft(
  payload: AdminAnimeAniSearchCreateRequest,
  authToken?: string,
): Promise<AdminAnimeAniSearchCreateResponse>
```

From `frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx`:
```tsx
interface CreateAniSearchIntakeCardProps {
  anisearchID: string
  onAniSearchIDChange: (value: string) => void
  onSubmit: () => void
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression coverage for candidate-first AniSearch title search</name>
  <files>frontend/src/lib/api.admin-anime.test.ts, frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts, frontend/src/app/admin/anime/create/page.test.tsx</files>
  <read_first>@frontend/src/lib/api.admin-anime.test.ts @frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts @frontend/src/app/admin/anime/create/page.test.tsx @.planning/phases/14-create-provider-search-separation-and-result-selection/14-CONTEXT.md</read_first>
  <acceptance_criteria>`frontend/src/lib/api.admin-anime.test.ts` contains an AniSearch title-search case that returns candidate rows without draft detail; `useAdminAnimeCreateController.test.ts` contains a case proving AniSearch title search stores candidate results without calling the existing exact-ID draft load path; `page.test.tsx` asserts the AniSearch card exposes both exact-ID entry and title-search entry in the same card.</acceptance_criteria>
  <action>Add tests that pin the intended split between AniSearch candidate search and exact-ID detail load. Cover three conditions explicitly: exact-ID load still works as before, title search yields multiple candidates without hydrating the draft, and candidate metadata includes title, type, year, and ID for popup presentation. Keep these regressions narrow and avoid implementing candidate selection in this plan.</action>
  <verify>
    <automated>cd frontend && npm test -- src/lib/api.admin-anime.test.ts src/app/admin/anime/create/useAdminAnimeCreateController.test.ts src/app/admin/anime/create/page.test.tsx</automated>
  </verify>
  <done>The regression suite proves AniSearch title search is candidate-first, preserves the exact-ID path, and stops short of triggering detail crawl before selection.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AniSearch title-search helper, controller state, and card entry UI</name>
  <files>frontend/src/types/admin.ts, frontend/src/lib/api/admin-anime-intake.ts, frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts, frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx, frontend/src/app/admin/anime/create/page.tsx</files>
  <read_first>@frontend/src/types/admin.ts @frontend/src/lib/api/admin-anime-intake.ts @frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts @frontend/src/app/admin/anime/create/CreateAniSearchIntakeCard.tsx @.planning/phases/14-create-provider-search-separation-and-result-selection/14-UI-SPEC.md</read_first>
  <acceptance_criteria>`frontend/src/types/admin.ts` contains candidate-list DTOs that include title, type, year, and AniSearch ID; `frontend/src/lib/api/admin-anime-intake.ts` exports an AniSearch title-search helper distinct from `loadAdminAnimeCreateAniSearchDraft`; `CreateAniSearchIntakeCard.tsx` exposes both exact-ID and title-search inputs in the same AniSearch card; `useAdminAnimeCreateController.ts` stores candidate results without applying them to the draft yet.</acceptance_criteria>
  <action>Add typed AniSearch candidate-list interfaces to `frontend/src/types/admin.ts`, then implement a candidate-search helper in `frontend/src/lib/api/admin-anime-intake.ts` that queries AniSearch by title and returns only candidate metadata rows. Extend `useAdminAnimeCreateController.ts` with AniSearch title-query state, loading state, and candidate-result state, but do not let candidate search call `loadAdminAnimeCreateAniSearchDraft(...)` yet. Update `CreateAniSearchIntakeCard.tsx` so the AniSearch card contains both the preserved exact-ID path and a separate title-search input/action in the same card, with source-local helper text and unsaved-state messaging still intact. Do not build the popup chooser or selected-candidate detail load yet; this plan ends at candidate retrieval and presentation readiness.</action>
  <verify>
    <automated>cd frontend && npm test -- src/lib/api.admin-anime.test.ts src/app/admin/anime/create/useAdminAnimeCreateController.test.ts src/app/admin/anime/create/page.test.tsx && npm run build</automated>
  </verify>
  <done>The create route can search AniSearch by title and hold candidate results safely without disturbing the preserved exact-ID load path or prematurely hydrating the draft.</done>
</task>

</tasks>

<verification>
Plan 14-02 is complete when AniSearch title search returns only candidate metadata, the exact-ID path still behaves as before, and the create route holds candidate results without launching the detail crawler.
</verification>

<success_criteria>
- AniSearch title search exists as a separate entry path in the AniSearch card.
- Candidate search returns metadata rows only.
- No draft hydration or detail crawl occurs until the operator chooses a candidate.
</success_criteria>

<output>
After completion, create `.planning/phases/14-create-provider-search-separation-and-result-selection/14-02-SUMMARY.md`
</output>
