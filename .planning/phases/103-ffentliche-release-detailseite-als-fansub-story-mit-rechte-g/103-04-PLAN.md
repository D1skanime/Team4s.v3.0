---
phase: 103
plan: "04"
type: execute
wave: 2
depends_on: ["103-01"]
files_modified:
  - shared/contracts/episode-versions.yaml
  - shared/contracts/openapi.yaml
  - backend/internal/handlers/episode_playback_grant.go
  - backend/internal/handlers/episode_playback_access.go
  - backend/internal/handlers/segment_stream.go
  - backend/internal/handlers/segment_stream_test.go
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.test.tsx
  - frontend/src/app/api/segments/[id]/stream/route.ts
  - frontend/src/app/api/segments/[id]/stream/route.test.ts
requirements_addressed: [P103-SC3]
must_haves:
  truths:
    - "Guests see Kara information but no playback action; any authenticated session can play a public ready release-bound segment."
    - "Playback remains bounded by Phase-98 persisted segment limits and short-lived segment/cache-bound grants."
---

<objective>
Expand the release-bound Kara timeline and separate ordinary authenticated public playback from segment management rights.
</objective>

<read_first>
- `.planning/phases/98-segmentstream-serverseitig-begrenzter-hls-jellyfin-playback-/98-CONTEXT.md`
- `.planning/phases/103-ffentliche-release-detailseite-als-fansub-story-mit-rechte-g/103-CONTEXT.md`
- `docs/frontend/auth-api-client.md`
- `docs/frontend/streaming-auth-handoff.md`
- `frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.tsx`
- `frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx`
- `backend/internal/handlers/episode_playback_grant.go`
- `backend/internal/handlers/episode_playback_access.go`
- `backend/internal/handlers/segment_stream.go`
- `frontend/src/app/api/segments/[id]/stream/route.ts`
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Split public segment-play permission from management</name>
  <files>shared/contracts/episode-versions.yaml, shared/contracts/openapi.yaml, backend/internal/handlers/episode_playback_grant.go, backend/internal/handlers/episode_playback_access.go, backend/internal/handlers/segment_stream.go, backend/internal/handlers/segment_stream_test.go</files>
  <action>Keep render/source mutation under `release_version.segments.manage`. Change public grant issuance to require an active authenticated app session plus a segment that is public, ready and bound to the requested release version. Continue issuing the Phase-98 short TTL grant bound to `theme_segment_id`, cache/source and persisted start/end; reject caller-supplied widened bounds and cross-release ids. Keep technical failures private. Update contracts and security tests for guest denial, authenticated allow, unavailable segment, tampering, expiry and cross-release access.</action>
  <verify>`go test ./internal/handlers ./internal/auth` from backend; contract validation.</verify>
  <acceptance>Ordinary authenticated users can play only eligible Kara, without gaining manage/render rights.</acceptance>
</task>

<task type="auto">
  <name>Task 2: Implement the expanded horizontal Kara timeline</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.tsx, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.test.tsx, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.module.css, frontend/src/app/api/segments/[id]/stream/route.ts, frontend/src/app/api/segments/[id]/stream/route.test.ts</files>
  <action>Extend the existing timeline rather than create a parallel component. Plot segments against full duration and show name/type/start/end/duration/participants/preview. Guests get no play control/login prompt. For an authenticated active session (`hasAccessToken || hasRefreshToken`), clicking a ready mark opens the detail area and autoplays through the existing relay; switching selection stops/unloads the prior player. Show `Noch nicht abspielbar` without diagnostics for unavailable segments. Do not read auth cookies or construct bearer headers in UI.</action>
  <verify>`ThemeTimeline.test.tsx` covers guest UI, access-token/refresh-only sessions, autoplay, switch-stop and unavailable status. `frontend/src/app/api/segments/[id]/stream/route.test.ts` covers guest denial, refresh-only restoration through central handoff, bounded grant/stream forwarding, absence/rejection of caller-controlled start/end bounds, range forwarding and contained relay errors.</verify>
  <acceptance>The timeline is informative for everyone and playable only through the bounded central seam for active sessions.</acceptance>
</task>

</tasks>

<verification>
- focused Go security tests
- focused `ThemeTimeline.test.tsx` and `frontend/src/app/api/segments/[id]/stream/route.test.ts`
- `npm --prefix frontend run typecheck`
- `npm --prefix frontend run lint`
- `git diff --check`
</verification>

<success_criteria>
- Public Kara playback and administrative segment management are demonstrably separate.
- Missing/expired access token with valid refresh token succeeds through the central refresh/relay boundary.
</success_criteria>
