---
phase: 103
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/contracts/openapi.yaml
  - shared/contracts/episode-versions.yaml
  - backend/internal/repository/release_detail_public_repository.go
  - backend/internal/repository/release_detail_public_repository_helpers.go
  - backend/internal/repository/release_detail_public_repository_test.go
  - backend/internal/handlers/group_contributors_handler.go
  - backend/internal/handlers/group_contributors_handler_test.go
  - frontend/src/types/releaseDetail.ts
requirements_addressed: [P103-SC1, P103-SC2, P103-SC5]
must_haves:
  truths:
    - "One public aggregate for the concrete release_version_id provides complete release identity, technical tracks, selected preview, exact contributors, release-bound Kara segments, and same-group navigation."
    - "Public visibility gates and release-version ownership remain server enforced; episode remains neutral."
  artifacts:
    - path: shared/contracts/openapi.yaml
      provides: Canonical public release-detail response contract
    - path: backend/internal/repository/release_detail_public_repository.go
      provides: Release-version aggregate and same-group navigation
    - path: frontend/src/types/releaseDetail.ts
      provides: Frontend DTO matching the contract
---

<objective>
Extend the existing public release-detail aggregate and contract so later UI/playback plans consume one documented, release-version-owned source of truth.
</objective>

<read_first>
- `AGENTS.md`
- `docs/engineering/implementation-contract.md`
- `docs/architecture/db-schema-fansub-domain.md`
- `docs/api/api-contracts.md`
- `.planning/phases/103-ffentliche-release-detailseite-als-fansub-story-mit-rechte-g/103-CONTEXT.md`
- `.planning/phases/103-ffentliche-release-detailseite-als-fansub-story-mit-rechte-g/103-RESEARCH.md`
- `shared/contracts/openapi.yaml`
- `shared/contracts/episode-versions.yaml`
- `backend/internal/repository/release_detail_public_repository.go`
- `backend/internal/repository/release_detail_public_repository_helpers.go`
- `frontend/src/types/releaseDetail.ts`
- `.planning/phases/102-fansubprojekte-ui-schrittweise-verbessern/102-CONTEXT.md`
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Specify the complete public release-version projection</name>
  <files>shared/contracts/openapi.yaml, shared/contracts/episode-versions.yaml, frontend/src/types/releaseDetail.ts</files>
  <action>Extend the existing GET `/anime/{id}/group/{groupId}/releases/{releaseVersionId}` schema, not a parallel detail endpoint. Add curated release name/fallback identity, version, all participating groups, date, duration, resolution, video/audio codec and audio language, individually named subtitle tracks, `is_preview_candidate`, member avatar/fallback fields, note author/date/role, release-bound segment id/name/type/start/end/duration/readiness/participants/preview, and previous/next targets. Contract the existing images cursor with a required/validated four-value `category` filter; return the category total, returned count, next cursor and `has_more` so each chapter expands independently. The initial aggregate exposes totals per category, not only one global count. Navigation targets must carry the current group context, prefer the same version number, then public standard version. Do not expose file/Jellyfin/import names, internal render diagnostics, or episode-owned media.</action>
  <verify>Contract YAML parses; frontend DTO names/nullability exactly match documented JSON.</verify>
  <acceptance>The canonical contract can render every D-02–D-17 and D-33–D-36 field without UI inference.</acceptance>
</task>

<task type="auto">
  <name>Task 2: Implement aggregate reads and focused contract tests</name>
  <files>backend/internal/repository/release_detail_public_repository.go, backend/internal/repository/release_detail_public_repository_helpers.go, backend/internal/repository/release_detail_public_repository_test.go, backend/internal/handlers/group_contributors_handler.go, backend/internal/handlers/group_contributors_handler_test.go</files>
  <action>Extend `GetPublicReleaseDetail` and focused helpers. Select only approved/public/ready `release_version_media`; project the singleton selected preview deterministically and a safe null fallback. Load technical tracks from existing release-version/source seams, exact release-version contributions, published release-version notes, and only segments bound to this release version. Resolve group-cooperation labels without changing ownership. Implement previous/next by anime episode ordering plus current `fansub_group_id`, same mapper/version first and documented public fallback second. Change `ListReleaseVersionImagesCursor` and its existing handler to validate and query one category, use `(sort_order,id)` within that category, and calculate that category's total/remaining state; do not preserve a global cursor as the UI basis. Add tests for empty and highly uneven categories, independent cursors, category-total accuracy, invalid category, no image, selected preview not first, multiple subtitle tracks, no data leak, exact contributor scope, cooperation context, same-version navigation and fallback.</action>
  <verify>`go test ./internal/repository ./internal/handlers` from `backend`; contract-focused tests pass.</verify>
  <acceptance>The runtime payload matches OpenAPI and never attaches release media to or authorizes by a neutral episode.</acceptance>
</task>

</tasks>

<verification>
- `go test ./internal/repository ./internal/handlers` (backend)
- Parse/validate changed OpenAPI files with the repository's existing contract command
- `npm --prefix frontend run typecheck`
- `git diff --check`
</verification>

<success_criteria>
- Public aggregate and DTO contain all display data with explicit nullability.
- Preview selection, public visibility, authorship and group-faithful navigation have focused regression tests.
- No new public detail, upload, auth or media ownership seam exists.
</success_criteria>
