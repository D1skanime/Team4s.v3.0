---
phase: 103
plan: "08"
type: execute
gap_closure: true
wave: 2
depends_on: ["103-06"]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/lib/api.auth-refresh.test.ts
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/releaseDetailPageData.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.test.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseEpisodePlayer.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseEpisodePlayer.test.tsx
  - frontend/src/app/api/releases/[id]/playback-access/route.ts
  - frontend/src/app/api/releases/[id]/playback-access/route.test.ts
requirements_addressed: [P103-UAT4, P103-UAT5]
must_haves:
  truths:
    - "Public Karaoke data is session-neutral and never disappears after login."
    - "A refresh-only entitled Platform Admin reaches the backend access resolver and sees the ready full-episode action."
---

<objective>Separate immutable public release data from authenticated playback capability and repair the single authoritative refresh handoff for segment/full-release playback.</objective>

<read_first>
- `AGENTS.md`
- `.planning/debug/103-karaoke-auth-visibility.md`
- `.planning/debug/103-full-episode-admin-action.md`
- `docs/frontend/auth-api-client.md`
- `docs/frontend/streaming-auth-handoff.md`
- `frontend/src/lib/api.ts`
- `frontend/src/lib/useAuthSession.ts`
- `frontend/src/lib/api.auth-refresh.test.ts`
- `frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.tsx`
- `frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseEpisodePlayer.tsx`
- `frontend/src/app/api/releases/[id]/playback-access/route.ts`
- `frontend/src/app/api/releases/[id]/stream/route.ts`
- `frontend/src/lib/server/streamRelayAuth.ts`
- `frontend/src/app/api/segments/[id]/stream/route.ts`
- `backend/internal/handlers/release_playback_access.go`
- `backend/internal/handlers/segment_stream.go`
</read_first>

<tasks>
<task type="auto">
  <name>Task 1: Make public release/segment projection session-neutral</name>
  <action>Route the public aggregate through a public no-auth server read rather than auth preflight. Keep the Karaoke section derived only from public `segments`; auth initialization may add controls but never hide segment titles. Do not change release-version segment ownership or expose diagnostics. Add integrated guest→access-token→refresh-only rerender tests.</action>
  <verify>The same segment titles remain across all session states; guests have no play affordance/login prompt; refresh-only users get play actions and old video stops on switch.</verify>
</task>
<task type="auto">
  <name>Task 2: Unify protected playback-access authentication</name>
  <action>Use the existing central browser `apiClientFetch` seam as the only authority for the protected JSON availability read. Add a typed `getReleasePlaybackAccess(releaseVersionID)` helper in `frontend/src/lib/api.ts` that calls the private backend `/api/v1/release-versions/{id}/playback-access` with `cache:'no-store'`; update `ReleaseEpisodePlayer` to call that helper after `isClientInitialized && (hasAccessToken || hasRefreshToken)`. Remove the component's plain-fetch dependency on `/api/releases/{id}/playback-access` and delete the now-redundant JSON Next relay plus its source-text-only test after `rg` confirms no callers. Do not alter the existing `/api/releases/{id}/stream` and `/api/segments/{id}/stream` server relays: they remain the grant/byte-stream cookie handoff. Preserve backend private no-store behavior, central access-token refresh and persisted refresh-token rotation, entitlement resolver/readiness response, release grant issuance, and final stream rechecks. Never construct bearer headers or role-check in the component. Keep auth failures local and test-observable without exposing entitlement details in public UI.</action>
  <verify>`api.auth-refresh.test.ts` executes access-token success and absent/expired access plus valid refresh through `apiClientFetch`, asserts the rotated session is persisted, exactly one retry reaches the private backend, and denial remains denial. `ReleaseEpisodePlayer.test.tsx` asserts the typed helper is not called before initialization/without session, is called for refresh-only, renders only allow+ready, and scopes auth errors locally. `rg` proves no remaining `/api/releases/{id}/playback-access` caller/route while stream relays remain.</verify>
</task>
</tasks>

<verification>
- commit each task atomically after its focused checks pass
- focused central auth-refresh/API-helper/component tests
- focused backend playback/segment permission tests if contracts touched
- `npm --prefix frontend run typecheck`
- `npm --prefix frontend run build`
- `git diff --check`
- live browser guest then Sheppert refresh-only then Platform Admin; inspect network/backend request statuses and ensure Karaoke persists
</verification>

<success_criteria>Public data never depends on auth state; protected JSON playback access has exactly one browser authority (`apiClientFetch`), refresh-only rotation works, and grant/stream relays plus backend authorization remain server-authoritative.</success_criteria>
