---
phase: 103
plan: "03"
type: execute
wave: 2
depends_on: ["103-01"]
files_modified:
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.module.css
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseDetailHero.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseGallery.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseNotesList.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ContributorsRow.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseNavigation.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseDetailHero.test.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseGallery.test.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseNotesList.test.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ContributorsRow.test.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseNavigation.test.tsx
requirements_addressed: [P103-SC1, P103-SC2, P103-SC5]
must_haves:
  truths:
    - "The page reads as a Fansub release story and remains complete when images or other sections are absent."
    - "Large image/text sets remain on-page, grouped by the four categories and contributor roles."
  artifacts:
    - path: frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.tsx
      provides: Release-story composition
---

<objective>
Recompose the existing public release route in the public Fansub visual language, emphasizing technical identity, exact people, categorized images and role-grouped writing.
</objective>

<read_first>
- `AGENTS.md`
- `docs/frontend/ui-system.md`
- `docs/agent-guidelines-ui.md`
- `.planning/phases/103-ffentliche-release-detailseite-als-fansub-story-mit-rechte-g/103-CONTEXT.md`
- `frontend/src/app/fansubs/[slug]/page.tsx`
- `frontend/src/components/fansubs/FansubTeamSection.tsx`
- all existing files in `frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/`
- `frontend/src/types/releaseVersionMedia.ts`
- `frontend/src/types/releaseDetail.ts`
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Build the release identity hero and exact contributor summary</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.tsx, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseDetailHero.tsx, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ContributorsRow.tsx, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseDetailHero.test.tsx, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ContributorsRow.test.tsx, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.module.css</files>
  <action>Extend the existing components with global UI primitives and public Fansub spacing/tokens. Render chosen preview or a fully usable text hero. Show episode/title, curated release label/version, all cooperation groups, date, duration, resolution, codecs, audio language and each subtitle track without accordion, plus image/text/Fansubber counts. Show only release-version contributors with roles and avatar/fallback. Omit empty local sections. Use correct German umlauts and avoid raw file/import names.</action>
  <verify>Focused component tests cover preview/no-image, multiple tracks/cooperation, exact contributors and empty sections; typecheck.</verify>
  <acceptance>The header is informative but the episode player is not visually central; no placeholder UI represents persisted fields.</acceptance>
</task>

<task type="auto">
  <name>Task 2: Convert media and notes into scalable in-page chapters</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseGallery.tsx, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseNotesList.tsx, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseGallery.test.tsx, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseNotesList.test.tsx, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.module.css</files>
  <action>Reuse the now category-aware cursor seam. Group images into the four existing category labels; show uploader, description and category total. Initially expose 6 desktop/4 tablet/2 mobile and expand `Weitere X Bilder anzeigen` in-place using that category's independent cursor/remaining total. Never fill a sparse category by consuming another category's page and never share cursors between chapters. Group notes by role and show author, avatar/fallback and date; keep pagination in-page and preserve groups. Avoid a mixed global feed and avoid new upload/media logic.</action>
  <verify>`ReleaseGallery.test.tsx` covers uneven category distributions, independent cursor expansion/counts, 6/4/2 responsive limits and duplicate-free merge; `ReleaseNotesList.test.tsx` covers author metadata, role grouping and text-only/image-only releases.</verify>
  <acceptance>Many images and texts remain navigable on the same release page, and empty chapters do not render.</acceptance>
</task>

<task type="auto">
  <name>Task 3: Add group-faithful release navigation</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseNavigation.tsx, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseNavigation.test.tsx, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.tsx, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/page.module.css</files>
  <action>Render previous/next only when aggregate targets exist. Preserve current groupId in URLs, label episode/version clearly, and never infer another group's target client-side. Keep navigation visually secondary at the end of the story.</action>
  <verify>Component tests cover same version, fallback version, cooperation group context and absent edge.</verify>
  <acceptance>`Nächster Release` never silently changes Fansub group.</acceptance>
</task>

</tasks>

<verification>
- focused frontend tests for the release-detail route/components
- `npm --prefix frontend run typecheck`
- `npm --prefix frontend run lint`
- `npm --prefix frontend run build` if feasible
- `git diff --check`
</verification>

<success_criteria>
- Story hierarchy matches the locked decisions and public Fansub UI.
- Desktop/tablet/mobile density is usable with large datasets and with text-only data.
</success_criteria>
