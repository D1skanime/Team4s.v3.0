---
phase: 103
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/0129_release_playback_entitlements.up.sql
  - database/migrations/0129_release_playback_entitlements.down.sql
  - backend/internal/migrations/phase103_release_playback_entitlements_test.go
  - backend/internal/repository/release_playback_entitlement_repository.go
  - backend/internal/repository/release_playback_entitlement_repository_test.go
  - backend/internal/permissions/permissions.go
requirements_addressed: [P103-SC4]
must_haves:
  truths:
    - "Full-release entitlement resolves global, group, project and release rules with most-specific-wins for role and direct-user subjects."
    - "No entitlement is scoped to a neutral episode and management UI remains deferred."
  artifacts:
    - path: backend/internal/repository/release_playback_entitlement_repository.go
      provides: Single effective entitlement resolver
---

<objective>
Create the minimal reversible persistence and central resolver required to secure rare full-release playback, without building the deferred rights-management UI.
</objective>

<read_first>
- `AGENTS.md` (especially migration stop conditions)
- `docs/architecture/db-schema-fansub-domain.md`
- `docs/engineering/implementation-contract.md`
- `.planning/phases/103-ffentliche-release-detailseite-als-fansub-story-mit-rechte-g/103-CONTEXT.md`
- `.planning/phases/103-ffentliche-release-detailseite-als-fansub-story-mit-rechte-g/103-RESEARCH.md`
- `backend/internal/permissions/permissions.go`
- `backend/internal/permissions/capability_registry.go`
- `backend/internal/repository/authz_permissions.go` — existing `AuthzRepository.ResolveReleaseVersion` group-context resolver; extend/compose it instead of duplicating release-version joins.
- `backend/internal/repository/fansub_group_app_members_repository.go` — canonical application-group membership repository backed by `fansub_group_members` and `fansub_group_member_roles`.
- `backend/internal/permissions/permissions.go` — exact `permissions.Resolver` interface and `permissions.Service` capability evaluation seam.
- `backend/internal/migrations/runner.go` — canonical runtime migration discovery/runner for root `database/migrations`.
- `backend/internal/migrations/runner_test.go` — migration-chain/up/down runner analog.
- `database/migrations/0128_group_history_single_use_events.up.sql` and `database/migrations/0128_group_history_single_use_events.down.sql` — confirmed current chain tip and reversible SQL naming analog.
- `backend/internal/handlers/episode_playback_access.go`
- `backend/internal/repository/fansub_group_member_media_permissions.go`
- current files in the canonical migration directory
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Add the reversible entitlement rule model</name>
  <files>database/migrations/0129_release_playback_entitlements.up.sql, database/migrations/0129_release_playback_entitlements.down.sql, backend/internal/migrations/phase103_release_playback_entitlements_test.go</files>
  <action>The planning inventory confirmed root `database/migrations` ends at tracked 0128 and `git status` contains no competing/untracked migration. At execution start re-check both facts before creating 0129; if any competing migration has appeared, stop and report rather than renumber/guess. Add one normalized rule table for subject type `app_user` or canonical application role code (the same role identity used by the data-driven capability registry, not a free-form capability string), effect allow/deny, and exactly one scope level: global, fansub group, project (`anime_id` + `fansub_group_id`), or concrete `release_version_id`. Role membership is resolved by composing `FansubGroupAppMemberRepository` with `AuthzRepository.ResolveReleaseVersion` and the existing `permissions.Resolver`/`permissions.Service`; do not duplicate release-version/group joins. Capability eligibility remains separate from content-scope rules. Add FKs/checks/uniqueness/audit columns and indexes needed for lookup. Do not reference `episode_id`; do not reuse group-media administration flags. Make down reversible and test up/down plus invalid mixed scopes. Do not seed broad production access.</action>
  <verify>Run the repository migration up/down test path and the Phase-103 migration structural test.</verify>
  <acceptance>The schema can express discussed examples and cannot express ambiguous/mixed/episode scope.</acceptance>
</task>

<task type="auto">
  <name>Task 2: Implement one most-specific-wins resolver</name>
  <files>backend/internal/repository/release_playback_entitlement_repository.go, backend/internal/repository/release_playback_entitlement_repository_test.go, backend/internal/permissions/permissions.go</files>
  <action>Add a repository/service method accepting authenticated app-user identity and concrete `release_version_id`; extend/compose `AuthzRepository.ResolveReleaseVersion`, `FansubGroupAppMemberRepository`, and the `permissions.Resolver`/`permissions.Service` interfaces to resolve anime/participating groups and canonical role codes once, then evaluate direct-user and role content-scope rules. Return a typed decision including winning scope for server diagnostics only. Specificity order is release > project > group > global. At equal scope, an explicit direct-user rule wins over any role rule; among conflicting role rules at the same scope, deny wins. Capability eligibility is checked before/alongside this resolver but is not persisted as a content subject. Preserve explicit platform-admin eligibility through the same resolver path rather than a handler bypass. Add table-driven tests for inheritance, direct user versus role conflicts, allow/deny role conflicts, cooperation group context, specific overrides, no rule, missing release and cross-group isolation.</action>
  <verify>`go test ./internal/repository ./internal/permissions ./internal/migrations` from `backend`.</verify>
  <acceptance>All callers can receive the same authoritative allow/deny result; no handler must recreate hierarchy SQL.</acceptance>
</task>

</tasks>

<verification>
- Migration 0129 up and down through `backend/internal/migrations/runner.go` on an isolated test database, including chain discovery from root `database/migrations`
- `go test ./internal/repository ./internal/permissions ./internal/migrations`
- `git diff --check`
</verification>

<success_criteria>
- The entitlement hierarchy is persisted, constrained, reversible and centrally resolved.
- Full rights-management UI, bulk assignment and inheritance visualization remain out of scope.
</success_criteria>
