---
phase: 103
plan: "05"
type: execute
wave: 3
depends_on: ["103-01", "103-02", "103-03", "103-04"]
files_modified:
  - shared/contracts/openapi.yaml
  - shared/contracts/episode-versions.yaml
  - shared/contracts/asset-stream.yaml
  - backend/internal/handlers/episode_version_stream.go
  - backend/internal/handlers/episode_version_grants.go
  - backend/internal/handlers/episode_playback_stream.go
  - backend/internal/handlers/episode_playback_stream_test.go
  - backend/internal/handlers/episode_playback_access.go
  - backend/internal/handlers/episode_playback_access_test.go
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseEpisodePlayer.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseDetailHero.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseEpisodePlayer.test.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseDetailHero.test.tsx
  - frontend/src/app/api/releases/[id]/stream/route.ts
  - frontend/src/app/api/releases/[id]/stream/route.test.ts
  - frontend/src/app/api/releases/[id]/playback-access/route.ts
  - frontend/src/app/api/releases/[id]/playback-access/route.test.ts
requirements_addressed: [P103-SC4, P103-SC6]
must_haves:
  truths:
    - "The full-episode button appears only when the central resolver allows playback and a stream source is ready."
    - "Grant issuance and the protected stream enforce the same entitlement; hiding UI is never the security boundary."
    - "The completed release story passes responsive live browser UAT from the real Fansub project route."
---

<objective>
Secure and integrate rare full-release playback as a secondary dialog action, then verify the complete Phase-103 experience and auth boundary.
</objective>

<read_first>
- `docs/frontend/auth-api-client.md`
- `docs/frontend/streaming-auth-handoff.md`
- `shared/contracts/asset-stream.yaml`
- `shared/contracts/episode-versions.yaml`
- `backend/internal/repository/release_playback_entitlement_repository.go`
- `backend/internal/handlers/episode_version_stream.go`
- `backend/internal/handlers/episode_version_grants.go`
- `backend/internal/handlers/episode_playback_stream.go`
- `frontend/src/app/api/releases/[id]/stream/route.ts`
- `frontend/src/components/ui/Dialog.tsx` (or the actual existing Dialog primitive found by search)
- the completed Phase-103 route/components
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Add protected no-store availability and enforce entitlement in grant and stream</name>
  <files>shared/contracts/openapi.yaml, shared/contracts/episode-versions.yaml, shared/contracts/asset-stream.yaml, backend/internal/handlers/episode_playback_access.go, backend/internal/handlers/episode_playback_access_test.go, backend/internal/handlers/episode_version_stream.go, backend/internal/handlers/episode_version_grants.go, backend/internal/handlers/episode_playback_stream.go, backend/internal/handlers/episode_playback_stream_test.go</files>
  <action>Do not personalize the cacheable public release aggregate. Extend the existing playback-access handler seam as authenticated `GET /release-versions/{releaseVersionId}/playback-access` returning only `{can_play, stream_ready}` (or the focused contract's equivalent), with anonymous 401/no availability and `Cache-Control: private, no-store`; it must resolve both normal access-token and refresh-restored sessions through central auth middleware. Update `openapi.yaml`, `episode-versions.yaml` and `asset-stream.yaml` for availability, grant, readiness, auth and error semantics. Inject/use the same resolver for availability, release grant issuance and final protected stream authorization. Remove any authenticated-user bypass. Require a release-version-bound short-lived grant and recheck entitlement on stream use. Tests must prove guest behavior, refresh-restored identity, two users with opposite decisions cannot share cached results, no rule, global/group/project/release rules, most-specific override, stale/tampered grant, revoked access, source unavailable and cooperation scope.</action>
  <verify>`go test ./internal/handlers ./internal/repository` from backend.</verify>
  <acceptance>Direct stream requests cannot bypass the same decision that controls the button.</acceptance>
</task>

<task type="auto">
  <name>Task 2: Add secondary dialog playback through the existing relay</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseEpisodePlayer.tsx, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseEpisodePlayer.test.tsx, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseDetailHero.tsx, frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseDetailHero.test.tsx, frontend/src/app/api/releases/[id]/playback-access/route.ts, frontend/src/app/api/releases/[id]/playback-access/route.test.ts, frontend/src/app/api/releases/[id]/stream/route.ts, frontend/src/app/api/releases/[id]/stream/route.test.ts</files>
  <action>Use the global Dialog primitive and existing `/api/releases/[id]/stream` relay. Add a same-origin server relay for the protected playback-access endpoint using `streamRelayAuth`/central refresh handoff and forwarding `private, no-store`; never cache availability in static/server public page output. Render a small `Episode abspielen` action beside technical data only after the protected availability read returns allowed+ready; otherwise render nothing. Preserve page scroll/state on close, stop media on close, and scope errors to the dialog. Gate active sessions with `hasAccessToken || hasRefreshToken`; central client/relay performs refresh. Do not add local cookie/bearer/Jellyfin logic.</action>
  <verify>`ReleaseEpisodePlayer.test.tsx` and `ReleaseDetailHero.test.tsx` cover hidden/allowed states, dialog cleanup and local errors; `playback-access/route.test.ts` covers guest, refresh-only and no-store/two-user isolation headers; `stream/route.test.ts` covers grant/relay errors and refresh handoff.</verify>
  <acceptance>The release story remains fully usable without full-episode playback.</acceptance>
</task>

<task type="auto">
  <name>Task 3: Run full verification and shared-flow live UAT</name>
  <files>no production files unless a discovered defect requires a scoped fix</files>
  <action>Run backend/frontend contract, security, typecheck, lint and build checks. In the Codex in-app browser start at the user-visible public Fansub page, follow the project entry into a concrete release, and verify desktop/tablet/mobile: text-only fallback; full technical tracks; exact members; 6/4/2 image chapters and expansion; role notes; timeline guest/auth behavior; segment autoplay/switch stop; hidden/allowed full-player states; same-group next release and cooperation context. Explicitly test missing/expired access token plus valid refresh token for both protected actions. Record unrelated existing failures separately and fix Phase-103 regressions.</action>
  <verify>`git diff --check`; live screenshots/notes identify exact routes and viewport sizes.</verify>
  <acceptance>All P103 criteria are evidence-backed in automated checks and the real public navigation flow.</acceptance>
</task>

</tasks>

<verification>
- backend focused and full feasible Go tests
- shared contract validation
- frontend focused tests, typecheck, lint and build
- live in-app browser UAT at desktop/tablet/mobile
- refresh-session regression for Kara and full release
- `git diff --check`
</verification>

<success_criteria>
- Full-release playback is rare, secondary, centrally authorized and stream-protected.
- All Phase-103 content/playback/navigation decisions work through the user-visible Fansub project flow.
- Remaining risk is only the explicitly deferred rights-management UI/tooling.
</success_criteria>
