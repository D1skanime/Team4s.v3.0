---
phase: 13-anisearch-relation-follow-through-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts
  - frontend/src/app/admin/anime/create/createPageHelpers.ts
  - frontend/src/types/admin.ts
  - backend/internal/handlers/admin_content_test.go
  - backend/internal/repository/anime_relations_admin_test.go
autonomous: true
requirements: [ENR-05, ENR-10]
must_haves:
  truths:
    - "AniSearch relations loaded on the create route are still present when the final create request is submitted."
    - "Create follow-through tests prove relation persistence can succeed without blocking anime creation."
    - "Operator-visible AniSearch relation counts describe actual attempted/applied/skipped results instead of stale draft assumptions."
  artifacts:
    - path: "frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts"
      provides: "Create-save seam regression for AniSearch-owned relations"
      contains: "relations"
    - path: "backend/internal/handlers/admin_content_test.go"
      provides: "Create handler follow-through regression coverage"
      contains: "AniSearch"
    - path: "backend/internal/repository/anime_relations_admin_test.go"
      provides: "Idempotent relation apply verification"
      contains: "ApplyAdminAnimeEnrichmentRelationsDetailed"
  key_links:
    - from: "frontend/src/app/admin/anime/create/createPageHelpers.ts"
      to: "frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts"
      via: "final create payload construction"
      pattern: "appendCreateSourceLinkageToPayload"
    - from: "backend/internal/handlers/admin_content_anime.go"
      to: "backend/internal/repository/anime_relations_admin.go"
      via: "best-effort create follow-through"
      pattern: "ApplyAdminAnimeEnrichmentRelationsDetailed"
---

<objective>
Lock the AniSearch relation follow-through seam with regressions before mutating production behavior.

Purpose: Phase 13 is a repair phase, so the first step is proving exactly where create-time AniSearch relations drop out and what the backend should report after save.
Output: Focused frontend and backend regressions for create payload relation carry-through, best-effort relation persistence, and truthful summary counts.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-anisearch-relation-follow-through-repair/13-RESEARCH.md
@.planning/phases/12-create-anisearch-intake-reintroduction-and-draft-merge-control/12-VERIFICATION.md
@backend/internal/handlers/admin_content_anime.go
@backend/internal/repository/anime_relations_admin.go
@frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts
@frontend/src/app/admin/anime/create/createPageHelpers.ts
@frontend/src/types/admin.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add create-save seam regressions for AniSearch relations</name>
  <files>frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts, frontend/src/app/admin/anime/create/createPageHelpers.ts, frontend/src/types/admin.ts</files>
  <read_first>@frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts @frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts @frontend/src/app/admin/anime/create/createPageHelpers.ts @frontend/src/types/admin.ts</read_first>
  <acceptance_criteria>`useAdminAnimeCreateController.test.ts` contains at least one assertion proving AniSearch-loaded `relations` are present in the final create payload; `createPageHelpers.ts` still has a single create-linkage helper seam; no new alternate create payload path is introduced.</acceptance_criteria>
  <action>Expand the create-controller regression suite so it captures the exact failure mode from Phase 13 research: after AniSearch draft load, the final create request must include both `source='anisearch:{id}'` and the AniSearch `relations` array. Keep the tests anchored to the existing helper seam in `appendCreateSourceLinkageToPayload(...)` rather than inventing a second payload builder. If type refinements are needed in `frontend/src/types/admin.ts`, make them explicit and limited to AniSearch create relation carry-through.</action>
  <verify>
    <automated>cd frontend && npm test -- src/app/admin/anime/create/useAdminAnimeCreateController.test.ts</automated>
  </verify>
  <done>The frontend regression suite fails unless AniSearch relations survive from loaded draft state into the final create submit payload.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add backend follow-through regressions for attempted/applied/skipped relation outcomes</name>
  <files>backend/internal/handlers/admin_content_test.go, backend/internal/repository/anime_relations_admin_test.go</files>
  <read_first>@backend/internal/handlers/admin_content_anime.go @backend/internal/handlers/admin_content_test.go @backend/internal/repository/anime_relations_admin.go @backend/internal/repository/anime_relations_admin_test.go</read_first>
  <acceptance_criteria>`admin_content_test.go` contains a create-route assertion for AniSearch relation follow-through when `relations` are present; `anime_relations_admin_test.go` contains at least one assertion for attempted/applied/skipped-existing counts from `ApplyAdminAnimeEnrichmentRelationsDetailed`.</acceptance_criteria>
  <action>Add or extend backend tests so they pin the intended Phase 13 contract: create remains `201 Created`, AniSearch follow-through is best-effort, resolvable approved relations are written through the normalized relation tables, and the response summary reports truthful `relations_attempted`, `relations_applied`, and `relations_skipped_existing` values. Reuse the existing repository helper rather than planning a replacement relation store or alternate apply path.</action>
  <verify>
    <automated>cd backend && go test ./internal/handlers ./internal/repository -run "Test.*AniSearch.*Relation|TestCreateAnime" -count=1</automated>
  </verify>
  <done>The backend regressions define exactly what successful create-time AniSearch relation persistence and summary reporting must look like before production code is touched.</done>
</task>

</tasks>

<verification>
Wave 1 is complete when the test suite proves AniSearch relations must survive into the final create request and the backend must report accurate follow-through outcomes without blocking create.
</verification>

<success_criteria>
- Frontend tests fail unless AniSearch relations are carried into the final create payload.
- Backend tests fail unless follow-through writes and counts are accurate.
- No alternate create or relation workflow is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/13-anisearch-relation-follow-through-repair/13-01-SUMMARY.md`
</output>
