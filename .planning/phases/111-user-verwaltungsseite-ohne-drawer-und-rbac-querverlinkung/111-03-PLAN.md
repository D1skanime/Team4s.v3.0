---
phase: 111-user-verwaltungsseite-ohne-drawer-und-rbac-querverlinkung
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/admin/users/AdminUsersClient.tsx
  - frontend/src/app/admin/users/useUserListFilters.ts
  - frontend/src/app/admin/users/AdminUsers.module.css
  - frontend/src/app/admin/users/page.test.tsx
  - frontend/src/app/admin/users/AdminUsersClient.test.tsx
autonomous: true
requirements: [D-01, D-06]

must_haves:
  truths:
    - "D-01 (Liste): Ein Klick auf eine Benutzerzeile navigiert zur Detailroute statt einen Drawer/Inline-Panel zu öffnen; der responsive Dual-Mode entfällt."
    - "D-06: Suche, Status und globale Rolle sind URL-Query-Parameter (q/status/role); Reload mit vorgegebener URL zeigt dieselben Filterwerte."
    - "Der Zurück-Link von /admin/users/[id] kann die exakte vorherige Liste wiederherstellen, weil die Liste ihren aktuellen Query-String beim Navigieren mitgibt (from=)."
  artifacts:
    - path: "frontend/src/app/admin/users/useUserListFilters.ts"
      provides: "URL-Query-Sync-Hook für q/status/role (Analogie useFansubEditMainTab.ts)"
    - path: "frontend/src/app/admin/users/AdminUsersClient.tsx"
      provides: "Liste ohne Drawer/Dual-Mode, Zeilen-Navigation mit from-Parameter"
      contains: "router.push"
  key_links:
    - from: "frontend/src/app/admin/users/AdminUsersClient.tsx"
      to: "frontend/src/app/admin/users/[id]/UserDetailPageClient.tsx (Plan 111-02)"
      via: "router.push(`/admin/users/${id}?from=...`)"
      pattern: "from=\\$\\{encodeURIComponent"
---

<objective>
`AdminUsersClient.tsx` verliert den Drawer/Dual-Mode-Detail-Pfad (D-01) und wird auf URL-Query-Parameter für Suche/Status/globale Rolle umgestellt (D-06), sodass ein Zeilenklick zur neuen `/admin/users/[id]`-Route navigiert (statt lokalen State zu setzen) und dabei den aktuellen Listen-Query-String als `from`-Parameter mitgibt — Grundlage für den exakten Zurück-Link aus Plan 111-02.

Purpose: Macht die Liste selbst deep-linkbar/teilbar und stellt sicher, dass der RBAC-Impact-Sprung (D-05, Plan 111-05) zuverlässig auf einer gefilterten Ansicht landet.
Output: `useUserListFilters.ts` (neuer Hook), umgebautes `AdminUsersClient.tsx`, bereinigtes CSS, überarbeitete/neue Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-user-verwaltungsseite-ohne-drawer-und-rbac-querverlinkung/111-CONTEXT.md
@.planning/phases/111-user-verwaltungsseite-ohne-drawer-und-rbac-querverlinkung/111-UI-SPEC.md
@.planning/phases/111-user-verwaltungsseite-ohne-drawer-und-rbac-querverlinkung/111-RESEARCH.md

<interfaces>
<!-- Kontrakt mit Plan 111-02 (paralleler Wave-1-Plan, unabhängige Dateien):
     `/admin/users/[id]/UserDetailPageClient.tsx` liest `useSearchParams().get('from')`
     und baut daraus `backHref = /admin/users?${decodeURIComponent(from)}`.
     Dieser Plan MUSS beim Zeilen-Klick exakt erzeugen:
     `/admin/users/${item.id}${currentQuery ? `?from=${encodeURIComponent(currentQuery)}` : ''}`
     wobei `currentQuery = searchParams.toString()` der AKTUELLE Listen-Query-String ist. -->

Aus `frontend/src/app/admin/users/AdminUsersClient.tsx` (aktueller Stand, vollständig gelesen — Zeilenangaben beziehen sich auf diese Version):
```typescript
function useDesktopUserDetails(): boolean { /* Zeilen 59-72, wird komplett entfernt */ }

export function AdminUsersClient() {
  const [selectedUserId, setSelectedUserId] = useState<number | null>(null)   // entfällt
  const [params, setParams] = useState<AdminUserListParams>({ sort: 'last_activity_desc', limit: 25, offset: 0 })
  const [searchValue, setSearchValue] = useState('')                          // bleibt als Debounce-Zwischenwert
  const [hasConflictsOnly, setHasConflictsOnly] = useState(false)             // bleibt lokal (nicht Teil von D-06)
  // Zeilen 108-122: handleSearchChange/handleStatusChange/handleRoleChange setzen lokal `params`
  // Zeilen 170-172: <label htmlFor="role-filter" className={styles.roleFilterLabel}>Globale Rolle</label>  -- Pitfall 5
  // Zeilen 196-213: Inline-Desktop-Detail-Panel-Block (styles.desktopDetailPanel) -- entfällt
  // Zeilen 245-252: <AdminUserTableRow onClick={() => setSelectedUserId(item.id)} .../>
  // Zeilen 266-271: <UserDetailDrawer userId={selectedUserId} onClose={...} /> -- entfällt (Datei von Plan 111-02 gelöscht)
}
```

Referenz-Hook `frontend/src/app/admin/fansubs/[id]/edit/hooks/useFansubEditMainTab.ts` (vollständig, Struktur-Vorlage für `useUserListFilters.ts`):
```typescript
"use client";
import { useCallback, useEffect, useMemo, useState } from "react";
import { usePathname, useRouter, useSearchParams } from "next/navigation";

export function useFansubEditMainTab({ isPlatformAdmin, capabilities }: Args) {
  const router = useRouter();
  const pathname = usePathname();
  const searchParams = useSearchParams();
  // ... liest searchParams.get(...), synct via useEffect ...
  const handleMainTabChange = useCallback((tab) => {
    const nextSearchParams = new URLSearchParams(searchParams.toString());
    if (tab === "basic") nextSearchParams.delete("tab");
    else nextSearchParams.set("tab", tab);
    const query = nextSearchParams.toString();
    router.replace(query ? `${pathname}?${query}` : pathname, { scroll: false });
  }, [capabilities, isPlatformAdmin, pathname, router, searchParams]);
  return { activeMainTab, availableMainTabs, handleMainTabChange };
}
```

Aus `frontend/src/components/ui/FormField.tsx` (vollständiger Vertrag, für Pitfall 5):
```typescript
export interface FormFieldProps {
  label?: string; htmlFor?: string; hint?: string; error?: string; required?: boolean; disabled?: boolean; children: ReactNode
}
```

Filter→URL-Contract (111-UI-SPEC.md, verbindlich):
| URL param | API param | Steuerelement |
|---|---|---|
| `q` | `q` | `Input type="search"`, 300ms debounced vor URL-Schreibung |
| `status` | `status` | `Select`, sofort geschrieben |
| `role` | `global_role` | `Select`, sofort geschrieben — Übersetzung `role`→`global_role` NUR an der `listAdminUsersPage(params)`-Aufrufstelle, NICHT in der URL |

`has_conflicts` bleibt bewusst lokaler State (nicht Teil von D-06, siehe UI-SPEC Filter-Contract).

`next/navigation`-Mock-Muster für Tests (aus `frontend/src/app/admin/fansubs/[id]/edit/page.test.tsx:22-26`):
```typescript
vi.mock('next/navigation', () => ({
  useParams: () => ({ id: '88' }),
  usePathname: () => '/admin/fansubs/88/edit',
  useRouter: () => ({ replace: vi.fn() }),
  useSearchParams: () => new URLSearchParams(),
}))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — page.test.tsx überarbeiten, AdminUsersClient.test.tsx neu für URL-Round-Trip</name>
  <files>frontend/src/app/admin/users/page.test.tsx, frontend/src/app/admin/users/AdminUsersClient.test.tsx</files>
  <read_first>
    frontend/src/app/admin/users/page.test.tsx (ganze Datei)
    frontend/src/app/admin/users/AdminUsersClient.tsx (ganze Datei)
    frontend/src/app/admin/fansubs/[id]/edit/page.test.tsx (next/navigation-Mock-Muster, Zeilen 22-26)
    111-VALIDATION.md §"Per-Task Verification Map" (Zeilen D-06 round-trip)
  </read_first>
  <behavior>
    - Test "clicking_row_navigates_to_detail_route" (page.test.tsx, ersetzt `clicking_row_opens_drawer`): Klick auf Zeile „Aki" → es existiert eine Navigation zu `/admin/users/1` im DOM (via gemocktem `next/link` oder erfasstem `router.push`-Aufruf), KEIN Drawer-/Tab-Text (`Übersicht`/`Globale Rollen`) erscheint zusätzlich zur Tabelle.
    - Test "url filter roundtrip — write" (AdminUsersClient.test.tsx): Status-`Select` auf `disabled` ändern → gemockter `router.replace` wird mit einem Pfad aufgerufen, der `status=disabled` enthält.
    - Test "url filter roundtrip — read" (AdminUsersClient.test.tsx): `useSearchParams` liefert vorab `role=content_admin&status=active` → Status-Select zeigt `active` ausgewählt, Rollen-Select zeigt `content_admin` ausgewählt.
  </behavior>
  <action>
    Entferne aus `page.test.tsx` die Tests `clicking_row_opens_drawer` und `clicking_row_keeps_table_visible_on_desktop` (testen das per D-01/D-02 entfernte Drawer-/Tab-Verhalten) und ersetze sie durch `clicking_row_navigates_to_detail_route` wie oben spezifiziert; behalte den bestehenden `next/link`-Mock (Zeilen 13-17) und `listAdminUsersPage`/`getCurrentUser`-Mocks bei. Erzeuge `AdminUsersClient.test.tsx` (NEU) mit `vi.mock('next/navigation', ...)` nach dem `fansubs/[id]/edit`-Muster, wobei `useRouter`/`useSearchParams` pro Testfall überschreibbar sind (`vi.fn()` mit `mockReturnValueOnce`), und den beiden oben spezifizierten Round-Trip-Tests. RED bestätigen: Importfehler auf `useUserListFilters` bzw. abweichendes Zeilen-Klick-Verhalten (Drawer öffnet noch).
  </action>
  <acceptance_criteria>
    <automated>cd frontend && npx vitest run src/app/admin/users/page.test.tsx src/app/admin/users/AdminUsersClient.test.tsx --reporter=dot</automated>
    Testlauf ist rot (neue/umgeschriebene Testfälle schlagen fehl, alte Drawer-Tests existieren nicht mehr).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/app/admin/users/page.test.tsx src/app/admin/users/AdminUsersClient.test.tsx --reporter=dot</automated>
  </verify>
  <done>page.test.tsx überarbeitet, AdminUsersClient.test.tsx neu erstellt, RED bestätigt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — useUserListFilters-Hook extrahieren, AdminUsersClient umbauen, CSS bereinigen</name>
  <files>frontend/src/app/admin/users/useUserListFilters.ts, frontend/src/app/admin/users/AdminUsersClient.tsx, frontend/src/app/admin/users/AdminUsers.module.css</files>
  <read_first>
    frontend/src/app/admin/fansubs/[id]/edit/hooks/useFansubEditMainTab.ts (vollständig, Struktur-Vorlage)
    frontend/src/app/admin/users/AdminUsersClient.tsx (ganze Datei — siehe Zeilenangaben im context-Block)
    frontend/src/app/admin/users/AdminUsers.module.css (ganze Datei)
    frontend/src/components/ui/FormField.tsx (Props-Vertrag)
  </read_first>
  <behavior>
    (siehe Task 1 — dieser Task macht die dort spezifizierten RED-Tests grün)
  </behavior>
  <action>
    Erstelle `useUserListFilters.ts`: kapselt `q` (lokaler Debounce-Zwischenwert 300ms wie heute, danach `router.replace`), `status`, `role` (URL-Key `role`, API-Param bleibt `global_role` — Übersetzung erfolgt an der Aufrufstelle von `listAdminUsersPage`, NICHT im Hook/URL) über `useSearchParams()`/`router.replace(path, { scroll: false })` (Muster wie `useFansubEditMainTab.ts`); Rückgabeform `{ params: AdminUserListParams, searchValue, handleSearchChange, handleStatusChange, handleRoleChange, handlePageChange }` (Namen an die bestehenden lokalen Handler in `AdminUsersClient.tsx` angelehnt, damit der Rest der Komponente strukturell unverändert bleibt). In `AdminUsersClient.tsx`: entferne `useDesktopUserDetails()` (Zeilen 59-72), `selectedUserId`-State, den Inline-Detail-Block (Zeilen 196-213, inkl. `styles.desktopDetailPanel`-Markup), die `UserDetailDrawer`-Einbindung (Zeilen 266-271) sowie die Imports von `UserDetailContent`/`UserDetailDrawer` (Zeilen 25-26, beide Dateien sind durch Plan 111-02 gelöscht); ersetze lokale `params`/`searchValue`-States durch `useUserListFilters()`. Zeilen-Klick (`AdminUserTableRow onClick`): `router.push(\`/admin/users/${item.id}${currentQuery ? \`?from=${encodeURIComponent(currentQuery)}\` : ''}\`)` mit `currentQuery = searchParams.toString()` (exakter Kontrakt siehe context-Block). Entferne `isSelected`-Prop/`aria-selected`/`.selectedRow`-Verzweigung aus `AdminUserTableRow` (kein Auswahlzustand mehr — nur Navigation). Ersetze das rohe `<label htmlFor="role-filter" className={styles.roleFilterLabel}>Globale Rolle</label>` durch `<FormField label="Globale Rolle" htmlFor="role-filter"><Select id="role-filter" .../></FormField>` (Pitfall 5, `FormField` aus `@/components/ui` importieren). Entferne aus `AdminUsers.module.css` die nicht mehr referenzierten Regeln `.desktopDetailPanel`, `.desktopDetailHeader`, `.desktopDetailTitle`, `.desktopDetailMeta`, `.roleFilterLabel` (durch `FormField` ersetzt) und `.selectedRow` (kein Auswahlzustand mehr). Bestätige GREEN und prüfe die Zeilenzahl.
  </action>
  <acceptance_criteria>
    <automated>cd frontend && npx vitest run src/app/admin/users --reporter=dot</automated>
    wc -l frontend/src/app/admin/users/AdminUsersClient.tsx liefert einen Wert kleiner als 450 (Pitfall 4).
    grep -n "useDesktopUserDetails\|UserDetailDrawer\|UserDetailContent\|selectedUserId" frontend/src/app/admin/users/AdminUsersClient.tsx liefert keine Treffer.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/app/admin/users --reporter=dot</automated>
  </verify>
  <done>useUserListFilters.ts implementiert, AdminUsersClient.tsx drawerfrei und &lt; 450 Zeilen, alle Task-1-Tests grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser URL → `AdminUsersClient` Filter-State | `q`/`status`/`role` werden aus der URL gelesen statt nur lokal gehalten |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-05 | Tampering | Manipulierter `?role=`-Query-Param mit unbekanntem Rollen-Code | accept | Bereits durch bestehende parametrisierte SQL ($1/$2/$3 in `admin_users_queries.go`) und leere-Ergebnis-Semantik (`EmptyState`) abgedeckt — ein unbekannter Code liefert schlicht 0 Treffer, kein Schaden (siehe 111-RESEARCH.md Security Domain). |
| T-111-06 | Elevation of Privilege | Zeilen-Navigation zu `/admin/users/[id]` | accept | Ziel-Route ist durch Plan 111-02s `PlatformAdminGate` abgesichert — diese Liste selbst bleibt unter dem bestehenden `/admin/users`-Gate unverändert. |

Keine neuen high-severity Threats — bestehende Gates greifen unverändert.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/app/admin/users --reporter=dot` grün.
- `cd frontend && npm test` (volle Suite) bleibt grün.
- Docker: `docker restart team4sv30-frontend` + Strg+F5. Liste filtern (Status+Rolle) → URL zeigt `?status=...&role=...`; Seiten-Reload mit dieser URL zeigt dieselben Filterwerte in den Selects.
</verification>

<success_criteria>
- `AdminUsersClient.tsx` enthält keinen Drawer-/Dual-Mode-Code mehr und ist < 450 Zeilen.
- `q`/`status`/`role` sind über die URL synchronisiert und überleben einen Reload.
- Zeilen-Klick navigiert zur Detailroute mit `from=`-Parameter, der den aktuellen Listen-Query-String trägt.
</success_criteria>

<output>
Create `.planning/phases/111-user-verwaltungsseite-ohne-drawer-und-rbac-querverlinkung/111-03-SUMMARY.md` when done
</output>
