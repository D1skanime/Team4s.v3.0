---
phase: 111-user-verwaltungsseite-ohne-drawer-und-rbac-querverlinkung
plan: 04
type: execute
wave: 2
depends_on: ["111-01"]
files_modified:
  - frontend/src/app/admin/users/resolveRoleLink.ts
  - frontend/src/app/admin/users/tabs/UserGlobalRolesTab.tsx
  - frontend/src/app/admin/users/tabs/UserGlobalRolesTab.test.tsx
  - frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx
  - frontend/src/app/admin/users/tabs/UserGroupRightsTab.test.tsx
autonomous: true
requirements: [D-04]

must_haves:
  truths:
    - "D-04: Jede auf UserGroupRightsTab angezeigte Rolle, die in listRoleCapabilities() auflösbar ist, führt zu einem Link 'Was darf diese Rolle?' zur Capability-Detailansicht."
    - "D-04 (Fallback): Rollen ohne auflösbaren Capability-Eintrag bleiben schlichter Text ohne Link — insbesondere die drei globalen App-Rollen auf UserGlobalRolesTab, die strukturell NIE auflösbar sind (Pitfall 1)."
  artifacts:
    - path: "frontend/src/app/admin/users/resolveRoleLink.ts"
      provides: "resolveRoleLink(roleCode, matrix) -> string | null Lookup-Utility"
  key_links:
    - from: "frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx"
      to: "/admin/role-capabilities?role={role_code}"
      via: "resolveRoleLink + Button href"
      pattern: "resolveRoleLink"
---

<objective>
User→Rolle-Querverlinkung ("Was darf diese Rolle?") auf `UserGroupRightsTab` (Gruppenrollen, echte `role_definitions`-Codes) implementieren. `UserGlobalRolesTab` (globale App-Rollen) bleibt bewusst UNVERÄNDERT — sie sind laut 111-RESEARCH.md Pitfall 1 strukturell nie in `listRoleCapabilities()` auflösbar, ein Regressionstest sichert das explizit ab, damit ein späteres UAT das nicht fälschlich als "Link fehlt" meldet.

Purpose: Schließt D-04 — die einzige der beiden RBAC-Querverlinkungen, die vollständig client-seitig lösbar ist (kein Backend-Enrichment nötig).
Output: `resolveRoleLink.ts`, erweitertes `UserGroupRightsTab.tsx`, zwei neue Testdateien.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-user-verwaltungsseite-ohne-drawer-und-rbac-querverlinkung/111-CONTEXT.md
@.planning/phases/111-user-verwaltungsseite-ohne-drawer-und-rbac-querverlinkung/111-RESEARCH.md
@.planning/phases/111-user-verwaltungsseite-ohne-drawer-und-rbac-querverlinkung/111-UI-SPEC.md

<interfaces>
<!-- Voraussetzung aus Plan 111-01 (Wave 1, bereits ausgeführt): -->
Aus `frontend/src/types/admin-capability.ts` (nach Plan 111-01 erweitert):
```typescript
export interface RoleEntry {
  role_code: string;
  label_de: string;
  actions: RoleActionState[];
  assignable?: boolean;
  capability_editable?: boolean;
  contexts?: string[];
  global_assignment_count?: number | null;  // NEU (Plan 111-01, D-05) — hier irrelevant
  role_kind?: 'global_app_role' | string;    // NEU (Plan 111-01, D-05) — hier irrelevant
}
export interface RoleCapabilityMatrix {
  roles: RoleEntry[];
  all_actions: ActionEntry[];
}
```
Für D-04 relevant ist NUR `role_code` — die drei synthetischen `global_app_role`-Zeilen aus Plan 111-01 tauchen zwar jetzt in `matrix.roles` auf, aber ihre `role_code`-Werte (`platform_admin`/`content_admin`/`user`) matchen ohnehin nie gegen `UserGlobalRolesTab`s Badges, weil dieser Tab (siehe Action unten) BEWUSST kein Matching implementiert.

Aus `frontend/src/lib/api.ts:9474` (unverändert nutzbar):
```typescript
export async function listRoleCapabilities(): Promise<RoleCapabilityMatrix>
```

Aus `frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx` (aktueller Stand, vollständig — Zeilenangaben beziehen sich auf diese Version):
```typescript
function RightsTable({ rights }: { rights: AdminGroupRightsSummary[] }) {
  // ... Zeilen 52-58 ...
  {r.granted_roles.length === 0 ? (
    <Badge variant="muted">–</Badge>
  ) : (
    r.granted_roles.map((role) => (
      <Badge key={role} variant="info">{role}</Badge>
    ))
  )}
  // ...
}
export function UserGroupRightsTab({ userId }: Props) {
  // eigener useCallback/useEffect-Fetch von getAdminUserGroupRights(userId)
}
```
`AdminGroupRightsSummary.granted_roles: string[]` (aus `frontend/src/types/admin-users.ts:152-158`).

Aus `frontend/src/app/admin/users/tabs/UserGlobalRolesTab.tsx` (aktueller Stand, vollständig — bleibt in diesem Plan UNVERÄNDERT):
```typescript
function roleLabel(role: string): string { /* ... */ }
// RolesTable (Zeilen 198-227):
<Badge variant="info">{roleLabel(role)}</Badge>
```

Analoges bestehendes Aktions-Link-Muster (`UserGroupRightsTab.tsx:76-84`, wird für D-04 als Vorbild für Platzierung genutzt, aber NICHT per `window.open`, sondern per `Button href`):
```typescript
<Button variant="ghost" size="sm" onClick={() => window.open(`/admin/fansubs/${r.fansub_group_id}/edit`, '_blank')}>
  Gruppe bearbeiten
</Button>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Tests für auflösbare/unauflösbare Rollen-Links + Regressionsschutz globale Rollen</name>
  <files>frontend/src/app/admin/users/tabs/UserGlobalRolesTab.test.tsx, frontend/src/app/admin/users/tabs/UserGroupRightsTab.test.tsx</files>
  <read_first>
    frontend/src/app/admin/users/tabs/UserGlobalRolesTab.tsx (ganze Datei)
    frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx (ganze Datei)
    frontend/src/app/admin/users/tabs/UserClaimsTab.test.tsx (Mock-Muster für `@/lib/api` in Tab-Tests, Zeilen 1-33)
    frontend/src/types/admin-users.ts (AdminGroupRightsSummary/AdminUserGroupRightsResponse, Zeilen 152-163)
    111-VALIDATION.md §"Per-Task Verification Map" (Zeilen D-04)
  </read_first>
  <behavior>
    - Test "global role never links" (UserGlobalRolesTab.test.tsx): `getAdminUserGlobalRoles` liefert `roles: ['platform_admin']`; nach Render ist `platform_admin`/`Plattform-Admin` als `Badge` sichtbar, aber es existiert KEIN `link`/`button` mit Text „Was darf diese Rolle?" im DOM (Regressionsschutz gegen eine künftige Fehlannahme, siehe Pitfall 1).
    - Test "resolvable role link" (UserGroupRightsTab.test.tsx): `getAdminUserGroupRights` liefert eine Gruppe mit `granted_roles: ['fansub_lead']`; `listRoleCapabilities` liefert eine Matrix mit `role_code: 'fansub_lead'`; nach Render existiert ein Link/Button mit `href="/admin/role-capabilities?role=fansub_lead"` und `aria-label` enthält „fansub_lead" oder das zugehörige Label.
    - Test "unresolvable role no link" (UserGroupRightsTab.test.tsx): `granted_roles: ['ghost_role']`; `listRoleCapabilities`-Matrix enthält `ghost_role` NICHT; nach Render ist `ghost_role` als `Badge` sichtbar, aber ohne begleitenden Link/Button.
  </behavior>
  <action>
    Erzeuge `UserGlobalRolesTab.test.tsx` (NEU) mit dem oben spezifizierten Regressionstest (mock `@/lib/api`: `getAdminUserGlobalRoles` liefert `{ roles: ['platform_admin'], assignable_roles: [] }`). Erzeuge `UserGroupRightsTab.test.tsx` (NEU) mit den zwei oben spezifizierten Tests (mock `@/lib/api`: `getAdminUserGroupRights` UND `listRoleCapabilities`, analog zum Mock-Muster aus `UserClaimsTab.test.tsx:14-25`). RED bestätigen: Importfehler auf `../resolveRoleLink`, sobald `UserGroupRightsTab.tsx` in Task 2 darauf verweist — vor Task 2 sind die "resolvable role link"/"unresolvable role no link"-Tests rot, weil noch kein Link im DOM erscheint (kein Importfehler, da `resolveRoleLink` in Task 1 noch nicht referenziert wird — RED zeigt sich als fehlgeschlagene Assertion, nicht als Compile-Fehler).
  </action>
  <acceptance_criteria>
    <automated>cd frontend && npx vitest run src/app/admin/users/tabs/UserGlobalRolesTab.test.tsx src/app/admin/users/tabs/UserGroupRightsTab.test.tsx --reporter=dot</automated>
    "resolvable role link" und "unresolvable role no link" schlagen fehl (kein Link im DOM vorhanden); "global role never links" ist bereits grün (bestätigt nur den Ist-Zustand, siehe Hinweis in Task 2).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/app/admin/users/tabs/UserGlobalRolesTab.test.tsx src/app/admin/users/tabs/UserGroupRightsTab.test.tsx --reporter=dot</automated>
  </verify>
  <done>Beide Testdateien existieren; "resolvable role link"/"unresolvable role no link" sind rot, "global role never links" ist bereits grün.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — resolveRoleLink.ts implementieren, in UserGroupRightsTab verdrahten</name>
  <files>frontend/src/app/admin/users/resolveRoleLink.ts, frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx</files>
  <read_first>
    (wie Task 1)
    frontend/src/components/ui/Button.tsx (href-Fähigkeit)
  </read_first>
  <action>
    Erstelle `frontend/src/app/admin/users/resolveRoleLink.ts`: `export function resolveRoleLink(roleCode: string, matrix: RoleCapabilityMatrix | null): string | null` — `const entry = matrix?.roles.find(r => r.role_code === roleCode); if (!entry) return null; return \`/admin/role-capabilities?role=${encodeURIComponent(roleCode)}\`` (reine Lookup-Funktion, kein Zusatz-Request). In `UserGroupRightsTab.tsx`: ergänze einen eigenen, unabhängigen `useEffect`/`useState`-Fetch von `listRoleCapabilities()` (paralleler Fetch neben `getAdminUserGroupRights`, kein Blocking — unkritisch laut RESEARCH.md, da diese Sektion laut D-03 ohnehin sofort geladen wird); importiere `resolveRoleLink` und `Button` aus `@/components/ui`; rendere in `RightsTable` neben jeder `granted_roles`-`Badge` (aktuell Zeilen 55-57) bedingt einen `Button variant="ghost" size="sm" href={link}` mit sichtbarem Text `Was darf diese Rolle?` und `aria-label={\`Rechte der Rolle ${role} ansehen\`}`, wenn `resolveRoleLink(role, matrix)` nicht `null` liefert; sonst bleibt nur die `Badge` (kein Platzhalter-Element). `UserGlobalRolesTab.tsx` bleibt UNVERÄNDERT — kein Matching-Code wird dort ergänzt (Pitfall 1: ein Match-Versuch gegen `platform_admin`/`content_admin`/`user` würde ohnehin nie zutreffen; der Task-1-Regressionstest beweist das ohne Produktionscode-Änderung). Bestätige GREEN für alle drei Tests aus Task 1.
  </action>
  <acceptance_criteria>
    <automated>cd frontend && npx vitest run src/app/admin/users/tabs/UserGlobalRolesTab.test.tsx src/app/admin/users/tabs/UserGroupRightsTab.test.tsx --reporter=dot</automated>
    grep -n "resolveRoleLink" frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx liefert einen Treffer.
    grep -n "resolveRoleLink\|listRoleCapabilities" frontend/src/app/admin/users/tabs/UserGlobalRolesTab.tsx liefert KEINEN Treffer (bestätigt bewusste Nicht-Änderung, Pitfall 1).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/app/admin/users/tabs --reporter=dot</automated>
  </verify>
  <done>resolveRoleLink.ts implementiert, UserGroupRightsTab zeigt den Link für auflösbare Rollen, UserGlobalRolesTab bleibt unverändert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `UserGroupRightsTab` → `/admin/role-capabilities` | Reine Client-Navigation zwischen zwei bereits Platform-Admin-only geschützten Admin-Flächen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-07 | Information Disclosure | Neuer Link legt offen, dass eine Rolle in der Capability-Matrix existiert | accept | Zielseite `/admin/role-capabilities` ist bereits Platform-Admin-only; kein neues Datenleck, der Link navigiert nur zwischen zwei Flächen, die derselbe Nutzer ohnehin schon sehen darf. |

Keine neuen high-severity Threats — bestehende `PlatformAdminGate`-Gates auf beiden verlinkten Flächen greifen unverändert.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/app/admin/users/tabs --reporter=dot` grün.
- `cd frontend && npm test` (volle Suite) bleibt grün.
- Docker: `docker restart team4sv30-frontend` + Strg+F5. Live-UAT mit echten `app_user_global_roles`/`fansub_group_members`-Daten: User mit Gruppenrolle öffnen → „Was darf diese Rolle?" klicken → landet auf `/admin/role-capabilities` mit der passenden Rolle vorausgewählt (sobald Plan 111-05 gelandet ist).
</verification>

<success_criteria>
- `UserGroupRightsTab` zeigt den User→Rolle-Link für jede in `listRoleCapabilities()` auflösbare Rolle.
- `UserGlobalRolesTab` bleibt unverändert und zeigt nachweislich keinen Link für globale App-Rollen.
</success_criteria>

<output>
Create `.planning/phases/111-user-verwaltungsseite-ohne-drawer-und-rbac-querverlinkung/111-04-SUMMARY.md` when done
</output>
