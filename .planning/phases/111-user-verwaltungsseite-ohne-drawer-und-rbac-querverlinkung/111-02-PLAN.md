---
phase: 111-user-verwaltungsseite-ohne-drawer-und-rbac-querverlinkung
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/admin/users/[id]/page.tsx
  - frontend/src/app/admin/users/[id]/page.test.tsx
  - frontend/src/app/admin/users/[id]/UserDetailPageClient.tsx
  - frontend/src/app/admin/users/[id]/UserDetailPageClient.test.tsx
  - frontend/src/app/admin/users/UserDetailContent.tsx
  - frontend/src/app/admin/users/UserDetailDrawer.tsx
  - frontend/src/app/admin/users/UserDetailDrawer.test.tsx
autonomous: true
requirements: [D-01, D-02, D-03, D-06]

must_haves:
  truths:
    - "D-01: /admin/users/[id] ist eine eigene, deep-linkbare Route unter PlatformAdminGate; der UserDetailDrawer entfällt ersatzlos."
    - "D-02: Die Detailbereiche sind gestapelte Accordion-Sektionen (@/components/ui Accordion), keine role=tablist/tab-Leiste mehr vorhanden."
    - "D-03: Übersicht/Globale Rollen/Gruppenmitgliedschaften/Gruppenrechte sind initial offen+geladen; die übrigen 5 Sektionen sind eingeklappt und laden lazy, ohne bei erneutem Öffnen erneut zu fetchen."
    - "D-06 (Teil): Der Zurück-Link der Detailseite liest den ?from=-Parameter und fällt ohne ihn auf /admin/users zurück."
  artifacts:
    - path: "frontend/src/app/admin/users/[id]/page.tsx"
      provides: "Server-Route unter PlatformAdminGate, dynamic='force-dynamic'"
    - path: "frontend/src/app/admin/users/[id]/UserDetailPageClient.tsx"
      provides: "Accordion-Host mit openIds/loadedIds-Lazy-Cache und Zurück-Link"
  key_links:
    - from: "frontend/src/app/admin/users/[id]/UserDetailPageClient.tsx"
      to: "frontend/src/app/admin/users/tabs/*.tsx"
      via: "Accordion items[].children bedingt auf loadedIds"
      pattern: "loadedIds.has\\("
    - from: "frontend/src/app/admin/users/[id]/UserDetailPageClient.tsx"
      to: "/admin/users"
      via: "backHref aus useSearchParams().get('from')"
      pattern: "backHref"
---

<objective>
Neue, eigene Detailroute `/admin/users/[id]` bauen, die den User-Detail-Drawer vollständig ersetzt (D-01) und die handgebaute `role="tablist"`-Leiste in `UserDetailContent.tsx` durch einen kontrollierten `Accordion` mit Lazy-Load ablöst (D-02/D-03). Löst damit die im UI-Design-Contract benannte CLAUDE.md-Verletzung (kein handgebautes Tab-Markup).

Purpose: Details werden progressiv auf einer eigenen, teilbaren Seite offengelegt statt in einem überladenen Drawer — Kernanliegen der Phase.
Output: `[id]/page.tsx`, `[id]/UserDetailPageClient.tsx`, Löschung von `UserDetailContent.tsx`/`UserDetailDrawer.tsx` (+Test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-user-verwaltungsseite-ohne-drawer-und-rbac-querverlinkung/111-CONTEXT.md
@.planning/phases/111-user-verwaltungsseite-ohne-drawer-und-rbac-querverlinkung/111-UI-SPEC.md
@.planning/phases/111-user-verwaltungsseite-ohne-drawer-und-rbac-querverlinkung/111-PATTERNS.md

<interfaces>
<!-- Kontrakt mit Plan 111-03 (paralleler Wave-1-Plan, unabhängige Dateien):
     Die Liste (AdminUsersClient.tsx, Plan 111-03) navigiert per
     `router.push(`/admin/users/${id}${currentQuery ? `?from=${encodeURIComponent(currentQuery)}` : ''}`)`.
     Dieser Plan MUSS `from` exakt so lesen: `useSearchParams().get('from')`, dann
     `decodeURIComponent(fromQuery)` als Query-String an `/admin/users?` anhängen.
     Ohne `from` → Fallback `/admin/users` (kein Query-String). -->

Aus `frontend/src/app/admin/users/UserDetailContent.tsx` (vollständig, wird durch diesen Plan abgelöst):
```typescript
const TABS: TabDef[] = [
  { id: 'overview', label: 'Übersicht' },
  { id: 'roles', label: 'Globale Rollen' },
  { id: 'claims', label: 'Member-Profil & Claims' },
  { id: 'memberships', label: 'Gruppenmitgliedschaften' },
  { id: 'group-rights', label: 'Gruppenrechte' },
  { id: 'contributions', label: 'Beiträge' },
  { id: 'media', label: 'Medien' },
  { id: 'audit', label: 'Audit' },
  { id: 'streaming', label: 'Streaming' },
]
const [activeTab, setActiveTab] = useState<TabId>('overview')
const [activatedTabs, setActivatedTabs] = useState<Set<TabId>>(new Set(['overview']))
function handleTabChange(tabId: TabId) {
  setActiveTab(tabId)
  setActivatedTabs((prev) => new Set([...prev, tabId]))
}
```
Jedes Tab-Panel rendert bedingt `activatedTabs.has(id) && <TabComponent userId={userId} />` — dieses Cache-Verhalten MUSS 1:1 als `loadedIds` in den neuen Accordion-Host übernommen werden (Pitfall 3), sonst Regression (Re-Fetch bei erneutem Öffnen).

Aus `frontend/src/components/ui/Accordion.tsx` (vollständiger Vertrag):
```typescript
export interface AccordionItemDef { id: string; title: ReactNode; children: ReactNode }
export interface AccordionProps {
  items: AccordionItemDef[]
  mode?: 'multi' | 'single'
  openIds?: Set<string>
  onOpenChange?: (next: Set<string>) => void
}
```
Kritisch (Accordion.tsx:85-94): geschlossene Panels werden komplett aus dem DOM entfernt (`{isOpen ? <div>{item.children}</div> : null}`) — beim Wiederöffnen wird `children` neu gemountet. Ohne zusätzliches `loadedIds`-Set (separat von `openIds`) würde jedes Wiederöffnen neu fetchen.

Referenzmuster kontrollierter Accordion (`frontend/src/app/admin/role-capabilities/RoleCapabilityClient.tsx:70-73` + `RoleCapabilityDetail.tsx:180-186`):
```typescript
const [openCategories, setOpenCategories] = useState<Set<string>>(new Set())
// ...
<Accordion items={accordionItems} mode="multi" openIds={openCategories} onOpenChange={onOpenCategoriesChange} />
```

Aus `frontend/src/components/ui/PageHeader.tsx` (vollständiger Vertrag):
```typescript
export interface PageHeaderProps {
  eyebrow?: string
  title: string
  description?: string
  actions?: ReactNode
  breadcrumbs?: ReactNode
}
```
`description` ist `string` (kein ReactNode) — der Status-`Badge` neben dem Titel (UI-SPEC Primary Visual Anchor) gehört daher in den `actions`-Slot (ReactNode), NICHT in `description`.

Aus `frontend/src/components/ui/Button.tsx` (href-fähig, Auszug):
```typescript
type LinkButtonProps = CommonButtonProps & AnchorHTMLAttributes<HTMLAnchorElement> & { href: string }
export type ButtonProps = NativeButtonProps | LinkButtonProps
```
`Button href="..."` rendert ein `<a>` mit Client-Navigation — kein `window.location`/`window.open` nötig.

Next.js-16-`[id]`-Routen-Konvention in diesem Repo (verifiziert an `frontend/src/app/admin/fansubs/[id]/edit/page.tsx`, vollständig):
```typescript
"use client";
import { useParams } from "next/navigation";
export default function AdminFansubEditPage() {
  const params = useParams<{ id: string }>();
  // ...
}
```
`page.tsx` bleibt dennoch Server Component + `PlatformAdminGate`-Wrapper (wie `frontend/src/app/admin/role-capabilities/page.tsx`, vollständig):
```typescript
import { PlatformAdminGate } from '@/components/auth/PlatformAdminGate'
export default function RoleCapabilitiesPage() {
  return (
    <PlatformAdminGate>
      <main><RoleCapabilityClient /></main>
    </PlatformAdminGate>
  )
}
```
→ Kombination für `[id]/page.tsx`: Server-Component-Wrapper wie oben, aber `UserDetailPageClient` (der `'use client'`-Teil) liest `useParams`/`useSearchParams` SELBST — `page.tsx` reicht keine Props durch.

`next/navigation`-Mock-Muster für Tests (verifiziert aus `frontend/src/app/admin/fansubs/[id]/edit/page.test.tsx:22-26`):
```typescript
vi.mock('next/navigation', () => ({
  useParams: () => ({ id: '88' }),
  usePathname: () => '/admin/fansubs/88/edit',
  useRouter: () => ({ replace: vi.fn() }),
  useSearchParams: () => new URLSearchParams(),
}))
```

`PlatformAdminGate`-Mock-Muster für Tests (verifiziert aus `frontend/src/app/admin/users/page.test.tsx:19-56`):
```typescript
vi.mock('@/lib/useAuthSession', () => ({
  useAuthSession: () => ({ hasAccessToken: true, hasRefreshToken: true, isClientInitialized: true }),
}))
vi.mock('@/lib/api', () => ({
  getCurrentUser: vi.fn().mockResolvedValue({ data: { id: 1, display_name: 'Admin', is_platform_admin: true } }),
  // ...
}))
```

`getAdminUserOverview`-Response enthält `display_name`/`status` (aus `frontend/src/app/admin/users/tabs/UserOverviewTab.tsx`, Zeilen 15/287-296) — `UserDetailPageClient` führt einen EIGENEN, unabhängigen Fetch dieser Funktion aus (nicht über `UserOverviewTab` durchgereicht), analog dazu, dass alle 9 Tab-Komponenten bereits unabhängig voneinander fetchen.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Tests für Route-Gate, Accordion-Struktur, Default-Open, Lazy-Cache, Zurück-Link</name>
  <files>frontend/src/app/admin/users/[id]/page.test.tsx, frontend/src/app/admin/users/[id]/UserDetailPageClient.test.tsx</files>
  <read_first>
    frontend/src/app/admin/users/UserDetailContent.tsx (ganze Datei — Referenz für die 9 Item-IDs/Labels)
    frontend/src/app/admin/users/page.test.tsx (ganze Datei — Mock-Muster für PlatformAdminGate/getCurrentUser)
    frontend/src/app/admin/fansubs/[id]/edit/page.test.tsx (next/navigation-Mock-Muster, Zeilen 22-26)
    frontend/src/components/ui/Accordion.tsx (aria-expanded/role="region"-Semantik, Zeilen 62-94)
    111-UI-SPEC.md §"Section / Accordion contract" (Tabelle Order/Item id/Label/Default state)
    111-VALIDATION.md §"Per-Task Verification Map" (Zeilen D-01/D-02/D-03, exakte Testnamen)
  </read_first>
  <behavior>
    - Test "gate blocks non admin" (page.test.tsx): `getCurrentUser` mock mit `is_platform_admin: false` → Seiteninhalt (Accordion/PageHeader) NICHT im DOM, stattdessen der PlatformAdminGate-Hinweistext.
    - Test "gate renders for admin" (page.test.tsx): `is_platform_admin: true` → `UserDetailPageClient` wird gerendert.
    - Test "back link" (UserDetailPageClient.test.tsx): `useSearchParams` liefert `from=q%3Dabc%26status%3Dactive` → Zurück-Link-`href` ist `/admin/users?q=abc&status=active`.
    - Test "no from param fallback" (UserDetailPageClient.test.tsx): `useSearchParams` liefert leeren String → Zurück-Link-`href` ist `/admin/users` (kein Query-String).
    - Test "no tablist" (UserDetailPageClient.test.tsx): nach Render liefert `screen.queryAllByRole('tab')` und `screen.queryAllByRole('tablist')` je ein leeres Array.
    - Test "default open sections" (UserDetailPageClient.test.tsx): Accordion-Header für `overview`/`roles`/`memberships`/`group-rights` haben `aria-expanded="true"` UND ihre (gemockten) Tab-Platzhalter sind im DOM sichtbar; Header für `claims`/`contributions`/`media`/`audit`/`streaming` haben `aria-expanded="false"` und ihre Platzhalter sind NICHT im DOM.
    - Test "no refetch on reopen" (UserDetailPageClient.test.tsx): Accordion-Header für `claims` anklicken (öffnet, Mock-Tab-Komponente mit `vi.fn()`-Ladefunktion wird 1× gemountet) → Header erneut anklicken (schließt) → Header ein drittes Mal anklicken (öffnet wieder) → Mock-Ladefunktion weiterhin nur 1× aufgerufen.
  </behavior>
  <action>
    Erzeuge `frontend/src/app/admin/users/[id]/page.test.tsx` und `frontend/src/app/admin/users/[id]/UserDetailPageClient.test.tsx` (beide NEU — Importfehler auf `./page`/`./UserDetailPageClient` sind das erwartete RED-Signal). Mocke in `UserDetailPageClient.test.tsx` alle 9 Tab-Komponenten via `vi.mock('../../tabs/UserXyzTab', () => ({ UserXyzTab: (props) => <div data-testid="tab-xyz">...</div> }))` mit je einer `vi.fn()`-gestützten simulierten Ladefunktion (z. B. über einen modulweiten Zähler), damit ausschließlich die Accordion-Host-Logik getestet wird, nicht die echten Tab-Implementierungen. Mocke `next/navigation` pro Testfall überschreibbar (kein statisches `vi.mock` mit fixem Rückgabewert, sondern `vi.fn()`, dessen Rückgabe pro `it(...)` via `mockReturnValueOnce`/`mockImplementation` gesetzt wird) nach dem Muster aus `fansubs/[id]/edit/page.test.tsx:22-26`. Mocke `@/lib/api` mit `getAdminUserOverview: vi.fn().mockResolvedValue({ id: 1, display_name: 'Aki', status: 'active', ... })` und `getCurrentUser`/`useAuthSession` analog zum bestehenden `page.test.tsx`. Lauf `npx vitest` und RED bestätigen (Importfehler auf die beiden fehlenden Module).
  </action>
  <acceptance_criteria>
    <automated>cd frontend && npx vitest run "src/app/admin/users/[id]" --reporter=dot</automated>
    Testlauf schlägt fehl mit Importfehlern auf `./page` bzw. `./UserDetailPageClient` (RED bestätigt, kein anderer Fehlertyp wie z. B. Syntaxfehler im Testcode selbst).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run "src/app/admin/users/[id]" --reporter=dot</automated>
  </verify>
  <done>Beide Testdateien existieren, RED bestätigt (Importfehler auf die noch fehlenden Module).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Route + Accordion-Host implementieren, Drawer/Tab-Leiste löschen</name>
  <files>frontend/src/app/admin/users/[id]/page.tsx, frontend/src/app/admin/users/[id]/UserDetailPageClient.tsx, frontend/src/app/admin/users/UserDetailContent.tsx, frontend/src/app/admin/users/UserDetailDrawer.tsx, frontend/src/app/admin/users/UserDetailDrawer.test.tsx</files>
  <read_first>
    frontend/src/app/admin/users/tabs/UserOverviewTab.tsx (Props-Signatur + getAdminUserOverview-Response-Form, Zeilen 15-21, 287-296)
    frontend/src/app/admin/role-capabilities/page.tsx (PlatformAdminGate-Wrapper, vollständig)
    frontend/src/app/admin/fansubs/[id]/edit/page.tsx (useParams-Konvention, vollständig)
    frontend/src/components/ui/PageHeader.tsx, Button.tsx, Accordion.tsx (Props-Verträge, siehe context-Block oben)
  </read_first>
  <behavior>
    (siehe Task 1 — dieser Task macht die dort spezifizierten RED-Tests grün)
  </behavior>
  <action>
    `frontend/src/app/admin/users/[id]/page.tsx`: Server Component, `export const dynamic = 'force-dynamic'`, `<PlatformAdminGate><main><UserDetailPageClient /></main></PlatformAdminGate>` — keine Props-Weitergabe. `frontend/src/app/admin/users/[id]/UserDetailPageClient.tsx`: `'use client'`; `userId = Number(useParams<{ id: string }>().id)`; `fromQuery = useSearchParams().get('from')`; `backHref = fromQuery ? \`/admin/users?${decodeURIComponent(fromQuery)}\` : '/admin/users'`. Eigener `useEffect`/`useState`-Fetch von `getAdminUserOverview(userId)` für `display_name`/`status` (unabhängig von `UserOverviewTab`s internem Fetch). Render `PageHeader` mit `eyebrow="Benutzerverwaltung"`, `title={displayName || \`Benutzer #${userId}\`}`, `breadcrumbs={<Button variant="ghost" size="sm" href={backHref} leftIcon={<ChevronLeft size={16} />}>Zurück zur Liste</Button>}` (Import `ChevronLeft` aus `lucide-react`), `actions={<Badge variant={statusVariant}>{statusLabel}</Badge>}` sobald die Übersichts-Daten geladen sind (vorher `PageHeader` ohne `actions`). Baue `openIds`/`loadedIds` exakt nach dem 111-PATTERNS.md-Migrationsmuster: beide initial `new Set(['overview','roles','memberships','group-rights'])`; `handleOpenChange(next) { setOpenIds(next); setLoadedIds(prev => new Set([...prev, ...next])) }`. Baue die 9 `AccordionItemDef`s in der UI-SPEC-Reihenfolge (`overview`→„Übersicht" via `UserOverviewTab`, `roles`→„Globale Rollen" via `UserGlobalRolesTab`, `memberships`→„Gruppenmitgliedschaften" via `UserGroupMembershipsTab`, `group-rights`→„Gruppenrechte" via `UserGroupRightsTab`, `claims`→„Member-Profil & Claims" via `UserClaimsTab`, `contributions`→„Beiträge" via `UserContributionsTab`, `media`→„Medien" via `UserMediaTab`, `audit`→„Audit" via `UserAuditTab`, `streaming`→„Streaming" via `UserStreamingGrantsTab`), jeweils `children: loadedIds.has(id) ? <TabComponent userId={userId} /> : null`. Render `<Accordion items={items} mode="multi" openIds={openIds} onOpenChange={handleOpenChange} />`. Lösche `frontend/src/app/admin/users/UserDetailContent.tsx`, `frontend/src/app/admin/users/UserDetailDrawer.tsx` und `frontend/src/app/admin/users/UserDetailDrawer.test.tsx` vollständig (D-01 — restlose Löschung, keine toten Imports). Bestätige, dass alle Tests aus Task 1 GREEN sind.
  </action>
  <acceptance_criteria>
    <automated>cd frontend && npx vitest run "src/app/admin/users/[id]" --reporter=dot</automated>
    test -f frontend/src/app/admin/users/UserDetailContent.tsx → Datei existiert NICHT mehr.
    test -f frontend/src/app/admin/users/UserDetailDrawer.tsx → Datei existiert NICHT mehr.
    grep -rln "UserDetailDrawer\|UserDetailContent" frontend/src/app/admin/users --include=*.tsx liefert außerhalb des `[id]`-Verzeichnisses keine Treffer.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run "src/app/admin/users/[id]" --reporter=dot && npx vitest run src/app/admin/users --reporter=dot</automated>
  </verify>
  <done>Route + Accordion-Host implementiert, alle Task-1-Tests grün, Drawer/UserDetailContent restlos gelöscht.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `/admin/users/[id]` | Neue dynamische Route, potenzieller Umgehungsversuch des Admin-Gates über direkten URL-Aufruf |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-03 | Elevation of Privilege | `/admin/users/[id]/page.tsx` | mitigate | `PlatformAdminGate` MUSS die Route serverseitig/vor-Render umschließen — 1:1 identisch zum bestehenden Muster auf `/admin/role-capabilities` und `/admin/users`. Task 1 erzwingt einen expliziten Gate-Test ("gate blocks non admin"). |
| T-111-04 | Information Disclosure (IDOR) | Beliebige `userId` im Pfad | accept | Bereits durch `PlatformAdminGate` abgedeckt — Admin sieht alle User by design, kein zusätzlicher Owner-Check nötig (identisch zur bestehenden Liste). |
</threat_model>

<verification>
- `cd frontend && npx vitest run "src/app/admin/users/[id]" --reporter=dot` grün.
- `cd frontend && npm test` (volle Suite) bleibt grün.
- Docker: `docker restart team4sv30-frontend` + Strg+F5, Direktaufruf `http://localhost:3000/admin/users/1` als Platform-Admin → Accordion mit 4 offenen Sektionen sichtbar, kein Tab-Markup.
</verification>

<success_criteria>
- `/admin/users/[id]` existiert als eigene Route unter `PlatformAdminGate`.
- Kein `role="tablist"`/`role="tab"` mehr im DOM dieser Seite.
- Items 1-4 offen+geladen, Items 5-9 lazy mit Re-Fetch-Schutz.
- `UserDetailDrawer.tsx`/`UserDetailContent.tsx` sind gelöscht, keine toten Referenzen.
</success_criteria>

<output>
Create `.planning/phases/111-user-verwaltungsseite-ohne-drawer-und-rbac-querverlinkung/111-02-SUMMARY.md` when done
</output>
