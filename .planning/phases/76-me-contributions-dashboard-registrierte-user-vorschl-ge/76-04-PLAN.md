---
phase: 76-me-contributions-dashboard-registrierte-user-vorschl-ge
plan: 04
type: execute
wave: 4
depends_on:
  - 76-02
  - 76-03
files_modified:
  - frontend/src/components/contributions/ReportModal.tsx
  - frontend/src/components/contributions/ReportFormFehler.tsx
  - frontend/src/components/contributions/ReportFormStory.tsx
  - frontend/src/components/contributions/ReportFormMedia.tsx
  - frontend/src/components/contributions/RejectReasonModal.tsx
  - frontend/src/components/contributions/ProposalForm.tsx
autonomous: true
requirements:
  - Runde 6
  - H
  - K

must_haves:
  truths:
    - "ReportModal öffnet als `Modal` aus `@/components/ui` (kein handgebautes div-Modal); Schritt-Navigation Typ → Ziel → Feld (D-05)"
    - "ReportFormFehler und ReportFormStory nutzen `submitSuggestion` aus api.ts und FormField+Textarea-Primitive (D-06)"
    - "ReportFormMedia nutzt `authorizedUploadXhr` und setzt owner_member_id; Backend antwortet mit review_status='in_review', visibility='internal' (Decision 8)"
    - "ReportModal integriert bestehenden ProposalForm für Contribution-Typ (D-06 NUR INTEGRIEREN)"
    - "Claim-Typ zeigt Hinweis-Text + Weiterleitungs-CTA — KEIN Inline-Formular (Lock H, C5)"
    - "ProposalForm.tsx: handgebautes div-Modal ersetzt durch `Modal`-Primitive; Scope- und Rollen-`<button>` durch `Button`-Primitive (C2)"
    - "RejectReasonModal: Pflicht-Begründung in `Textarea`, Senden erst aktiv wenn ≥5 Zeichen, nutzt `rejectAnimeContributionWithReason` aus api.ts (D-09)"
    - "Alle Umlaute korrekt (C3); keine Datei >450 Zeilen (C4)"
  artifacts:
    - path: "frontend/src/components/contributions/ReportModal.tsx"
      provides: "Unified Melde-Einstieg: Typ → Ziel → Formular (D-05)"
    - path: "frontend/src/components/contributions/RejectReasonModal.tsx"
      provides: "Pflicht-Begründungs-Modal für 'Das war ich nicht' (D-09)"
    - path: "frontend/src/components/contributions/ReportFormFehler.tsx"
      provides: "Fehler/Korrektur-Formular mit submitSuggestion"
    - path: "frontend/src/components/contributions/ReportFormStory.tsx"
      provides: "Story-Formular mit submitSuggestion"
    - path: "frontend/src/components/contributions/ReportFormMedia.tsx"
      provides: "Medien-Upload mit authorizedUploadXhr + Decision-8-Felder"
    - path: "frontend/src/components/contributions/ProposalForm.tsx"
      provides: "Migrierte Version: Modal-Primitive + Button-Primitive statt handgebautem div + native buttons"
  key_links:
    - from: "frontend/src/components/contributions/ReportModal.tsx"
      to: "frontend/src/components/contributions/ProposalForm.tsx"
      via: "type='contribution' rendert ProposalForm (D-06 NUR INTEGRIEREN)"
      pattern: "ProposalForm"
    - from: "frontend/src/components/contributions/ReportFormMedia.tsx"
      to: "frontend/src/lib/api.ts"
      via: "authorizedUploadXhr('/api/v1/me/suggestions/media', { file, fields: { target_type, target_id, category } })"
      pattern: "authorizedUploadXhr"
    - from: "frontend/src/components/contributions/RejectReasonModal.tsx"
      to: "frontend/src/lib/api.ts"
      via: "rejectAnimeContributionWithReason(contributionId, reason)"
      pattern: "rejectAnimeContributionWithReason"
---

<objective>
Unified Melde-Modal (D-05/D-06) und Reject-Pflicht-Begründungs-Modal (D-09): alle neuen Formulare und die Migration von `ProposalForm` auf `Modal`-Primitive (C2).

Purpose: Nach diesem Plan sind alle user-facing Interaktions-Flächen vollständig. Pläne 03 und 04 laufen parallel (Wave 4), da keine file_modified-Überschneidungen bestehen.

Output: 5 neue Komponenten + ProposalForm-Migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-CONTEXT.md
@.planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-UI-SPEC.md
@.planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-PATTERNS.md

<interfaces>
<!-- Aus frontend/src/components/ui/index.ts (Primitive-Inventar): -->
<!-- Modal({ open, onClose, title, footer, children }), Button({ variant, size, type, onClick, disabled }), -->
<!-- FormField({ label, htmlFor, required, hint }), Select, Textarea, Input -->

<!-- ReportModal-Props (PATTERNS.md): -->
```typescript
interface ReportModalProps {
  open: boolean
  onClose: () => void
  onSuccess: () => void
  prefillType?: 'fehler' | 'story' | 'medien' | 'contribution' | 'claim'
  prefillContributionId?: number  // für "Details korrigieren" D-10
}
type SuggestionType = 'fehler' | 'story' | 'medien' | 'contribution' | 'claim'
```

<!-- RejectReasonModal-Props (PATTERNS.md): -->
```typescript
interface RejectReasonModalProps {
  open: boolean
  contributionId: number | null
  onClose: () => void
  onConfirm: (contributionId: number, reason: string) => Promise<void>
}
```

<!-- ProposalForm.tsx: bestehende handgebaute Struktur (PATTERNS.md §ProposalForm): -->
<!-- - Position:fixed-div-Modal (Zeilen 134–138) → Modal-Primitive -->
<!-- - Scope-Karten native <button> (Zeilen 148–188) → Button-Primitive -->
<!-- - Rollen-Chip native <button> (Zeilen 239–242) → Button variant="subtle" -->
<!-- - Footer native <button> (Zeilen 284–289) → Button-Primitive -->

<!-- Claim-UI-SPEC (Lock H, C5): -->
<!-- Kein Inline-Formular; nur Hinweis + Link zu Claim-Flow -->
<!-- Copy: "Das Beanspruchen eines Profils läuft über einen eigenen, geschützten Ablauf. Du wirst dorthin weitergeleitet." -->
<!-- CTA: "Zum Beanspruchen-Ablauf" (Button oder Link) -->

<!-- authorizedUploadXhr (aus api.ts, Pflicht für Media-Upload per Lock K): -->
<!-- authorizedUploadXhr(url: string, { file: File, fields: Record<string, string>, onProgress? }) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RejectReasonModal.tsx + ProposalForm.tsx-Migration (D-09, C2)</name>
  <files>
    frontend/src/components/contributions/RejectReasonModal.tsx,
    frontend/src/components/contributions/ProposalForm.tsx
  </files>
  <read_first>
    - frontend/src/components/contributions/ProposalForm.tsx (vollständig lesen; 294 Zeilen; Zeilen 134–138 div-Modal, 148–188 Scope-Karten-buttons, 239–242 Rollen-Chip-buttons, 284–289 Footer-buttons; bestehende Props-Interface; import-Struktur)
    - frontend/src/components/ui/index.ts (Modal-API: open, onClose, title, footer Props; Button-API: variant, size, type, disabled, onClick)
    - .planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-PATTERNS.md §RejectReasonModal (vollständiger Implementierungs-Snippet inkl. isValid-Check)
    - .planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-PATTERNS.md §ProposalForm (vier Migrations-Punkte: div→Modal, Scope-buttons→Button, Rollen-buttons→Button, Footer-buttons→Button)
    - .planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-UI-SPEC.md §Reject-Pflicht-Begründungs-Modal (Copywriting-Contract)
  </read_first>
  <behavior>
    - RejectReasonModal.tsx: neue Datei; Props `open`, `contributionId: number | null`, `onClose`, `onConfirm: (id: number, reason: string) => Promise<void>`; State: `reason: string`, `isSubmitting: boolean`, `error: string|null`; `isValid = reason.trim().length >= 5`; Render: `Modal open={open} onClose={onClose} title="Zuordnung widersprechen"`; Body: `FormField label="Warum trifft diese Zuordnung nicht zu?" required hint="Die Begründung ist erforderlich und hilft dem Gruppen-Leader, den Widerspruch einzuordnen. Es wird nichts gelöscht."` + `Textarea rows={3} placeholder="Kurze Begründung…"`; Footer: `Button variant="secondary"` „Abbrechen" + `Button variant="danger" disabled={!isValid || isSubmitting}` „Widerspruch senden"; on-submit: `onConfirm(contributionId, reason)` → onClose bei Erfolg; `role="alert"` für Fehlertext
    - ProposalForm.tsx Migration (C2): (1) Zeilen 134–138 ersetzen — remove position:fixed-div, import Modal, wrap content in `<Modal open={true} onClose={onClose} title="Mitwirkung vorschlagen" footer={...}>`; (2) Scope-Karten-`<button>` ersetzen durch `<Button variant={scope === val ? 'primary' : 'secondary'} type="button" aria-pressed={scope === val} onClick={...}>`; (3) Rollen-Chip-`<button>` ersetzen durch `<Button variant="subtle" size="sm" type="button" aria-pressed={sel} onClick={...}>`; (4) Footer-`<button>` ersetzen durch `<Button variant="secondary">Abbrechen</Button>` und `<Button type="submit" variant="primary" disabled={...}>Zur Bestätigung senden</Button>`; (5) Import-Block: import Modal, Button aus `@/components/ui`; (6) remove S-Objekt (Inline-Style-Helfer) wenn nicht anderweitig genutzt; CLAUDE.md 450-Zeilen-Limit: ProposalForm.tsx hat 294 Zeilen; Migration tauscht Code 1:1 aus, kein Overhead zu erwarten; falls >450 → ProposalFormScopeSelector.tsx auslagern
  </behavior>
  <action>Erstelle RejectReasonModal.tsx per PATTERNS.md-Snippet. Migriere ProposalForm.tsx: ersetze die vier nativen Konstrukte durch Primitives (Modal, Button); entferne inline-style-Objekt S falls komplett ersetzt. Datei-Check: ProposalForm ≤ 450 Zeilen nach Migration.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript-Kompilierung fehlerfrei; ProposalForm.tsx nutzt kein handgebautes position:fixed-div und keine nativen `<button>` mehr; RejectReasonModal rendert Modal mit Pflicht-Textarea und disabled Senden-Button wenn Eingabe leer; beide Dateien ≤450 Zeilen</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReportModal.tsx + ReportFormFehler/Story/Media.tsx (D-05/D-06)</name>
  <files>
    frontend/src/components/contributions/ReportModal.tsx,
    frontend/src/components/contributions/ReportFormFehler.tsx,
    frontend/src/components/contributions/ReportFormStory.tsx,
    frontend/src/components/contributions/ReportFormMedia.tsx
  </files>
  <read_first>
    - frontend/src/components/contributions/ProposalForm.tsx (nach Migration aus Task 1; als Referenz für Formular-Submit-Pattern und api.ts-Nutzung)
    - frontend/src/lib/api.ts (authorizedUploadXhr-Signatur; submitSuggestion aus Plan 02; ApiError-Klasse; getApiBaseUrl)
    - frontend/src/components/ui/index.ts (Select, FormField, Textarea, Button, Modal)
    - .planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-PATTERNS.md §ReportModal (Schritt-Navigation, SUGGESTION_TYPES, Props)
    - .planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-PATTERNS.md §ReportFormFehler (handleSubmit-Pattern, Formularfelder)
    - .planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-PATTERNS.md §ReportFormMedia (Upload-Seam-Import, Upload-Flow)
    - .planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-UI-SPEC.md §Unified Melde-Modal (Copywriting-Contract für alle Typ-Labels, Hinweis-Texte, Melde Abbrechen/Senden)
  </read_first>
  <behavior>
    - ReportModal.tsx: Props `open`, `onClose`, `onSuccess`, `prefillType?: SuggestionType`, `prefillContributionId?: number`; State: `type: SuggestionType | null = prefillType ?? null`; Render: `Modal open={open} onClose={onClose} title="Vorschlagen oder melden"`; Schritt 1 (wenn type==null): Typ-Auswahl als 5 `Button`-Karten (`fehler`, `story`, `medien`, `contribution`, `claim`) mit Labels aus UI-SPEC §Copywriting (`variant={selected ? 'primary' : 'secondary'}`, `aria-pressed`); `FormField label="Was möchtest du melden oder vorschlagen?" required`; Schritt 2+3 (wenn type gesetzt): je Typ das Sub-Formular einbetten oder Claim-Hinweis zeigen; Zurück-Button im Header oder Footer; Review-Hinweis-Text unter Sub-Formular (UI-SPEC §Melde Hinweis); Footer: Abbrechen + Senden (disabled bis Sub-Formular valide); Claim-Typ: nur Hinweis-Text + `Button` „Zum Beanspruchen-Ablauf" als `href`-Link (Lock H: KEIN Schreib-Endpoint)
    - ReportFormFehler.tsx: Props `onSuccess`, `prefillContributionId?: number`; State für `targetType`, `targetId`, `contentText`, `isSubmitting`, `error`; Felder: `Select` für Ziel-Typ (Anime/Contribution), `Select` für target_id (wenn contribution → zeigt contribution-ID aus context, vorbefüllt falls prefillContributionId), `Textarea` für Freitext (required, min-5); Submit: `submitSuggestion({ suggestion_type: 'error_report', target_type, target_id, content_text })`; Hinweis: „Dein Vorschlag wird geprüft…" (UI-SPEC)
    - ReportFormStory.tsx: identische Struktur wie ReportFormFehler, aber `suggestion_type: 'story'`; Textarea-Label: „Story vorschlagen" / „Was ist deine Story?"; `Textarea` ist RichText-fähig aber plain text reicht für v1 (D-07 review-gebunden)
    - ReportFormMedia.tsx: Props `onSuccess`; State `targetType`, `targetId`, `category`, `file: File | null`, `isUploading`, `error`; Felder: `Select` Ziel-Typ, `Select` Kategorie (Pflicht), Datei-Upload via `input type="file"` — AUSNAHME: file-input ist kein ersetzbarer Primitiv-Typ (`@/components/ui` hat kein FileInput); nach Datei-Auswahl Upload via `authorizedUploadXhr('/api/v1/me/suggestions/media', { file, fields: { target_type, target_id, category } })`; Backend setzt `review_status='in_review'`, `visibility='internal'` automatisch (Decision 8); Hinweis „Hochgeladene Medien sind zunächst nur intern sichtbar und werden vor der Veröffentlichung geprüft." (UI-SPEC §Medien-Sichtbarkeit-Hinweis); Fortschritts-/Fehlerzustand sichtbar; Datei ≤450 Zeilen
    - Alle Strings korrekte Umlaute (C3); alle Modals/Selects/Buttons aus `@/components/ui` (C1)
  </behavior>
  <action>Erstelle ReportModal.tsx als Schritt-Navigator (Typ → Sub-Formular). Erstelle ReportFormFehler.tsx, ReportFormStory.tsx (analog), ReportFormMedia.tsx (Upload via authorizedUploadXhr). Claim-Typ im Modal ist nur ein Hinweis+Link (Lock H). ProposalForm-Integration für contribution-Typ bleibt.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript fehlerfrei; ReportModal rendert ohne Fehler; Claim-Typ hat keinen Schreib-Endpoint; ReportFormMedia nutzt authorizedUploadXhr; alle Dateien ≤450 Zeilen; kein natives `<select>`/`<button>`/`<textarea>` außer FileInput in ReportFormMedia</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ReportFormMedia → POST /me/suggestions/media | Datei-Upload mit Owner-Kontext; Backend setzt review_status + visibility |
| RejectReasonModal → POST /me/anime-contributions/:id/reject | contributionId + reason; Backend prüft Ownership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-04-01 | Elevation of Privilege | ReportFormMedia Upload | mitigate | Decision-8-Matrix: Backend setzt `owner_member_id`, `review_status='in_review'`, `visibility='internal'`; Frontend kann diese Felder nicht überschreiben |
| T-76-04-02 | Tampering | Claim-Pfad im Modal | mitigate | Claim-Typ in ReportModal hat KEINEN POST-Call; nur Navigation-Link (Lock H); kein Schreibpfad in Claims-Domäne |
| T-76-04-03 | Denial of Service | Media-Upload Größe | mitigate | Backend-Handler (Plan 02/Media-Service) setzt Upload-Limit; kein zusätzlicher clientseitiger Größen-Check nötig aber empfohlen (vor Upload prüfen) |
| T-76-04-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Pakete |
</threat_model>

<verification>
Nach Plan 04:
- `cd frontend && npx tsc --noEmit` — fehlerfrei
- `cd frontend && npm test -- ProposalForm 2>&1` — kein Regressionstest-Fehler (falls bestehende Tests vorhanden)
- `cd frontend && npx eslint src/components/contributions/ProposalForm.tsx` — kein no-restricted-syntax auf buttons außerhalb ui/
- Alle 5 neuen Dateien ≤450 Zeilen
- ReportModal Claim-Typ: kein API-Call, nur href-Navigation
</verification>

<success_criteria>
- Unified Melde-Modal öffnet und navigiert Typ → Ziel → Formular
- ProposalForm nutzt keine handgebauten Modals/Buttons mehr
- RejectReasonModal enforces ≥5-Zeichen-Bedingung client-seitig (Senden disabled)
- Media-Upload ist review-gebunden (Backend entscheidet visibility)
- Lock H respektiert: Claim ist reiner Redirect
</success_criteria>

<output>
Erstelle `.planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-04-SUMMARY.md` wenn fertig
</output>
