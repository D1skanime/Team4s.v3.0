---
phase: 76-me-contributions-dashboard-registrierte-user-vorschl-ge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/0097_member_suggestions.up.sql
  - database/migrations/0097_member_suggestions.down.sql
  - shared/contracts/openapi.yaml
  - frontend/src/types/contributions.ts
  - frontend/src/components/contributions/ContributionInbox.test.tsx
  - frontend/src/components/contributions/ContributionSummary.test.tsx
  - backend/internal/handlers/contributions_me_handler_test.go
  - backend/internal/handlers/suggestions_me_handler_test.go
autonomous: true
requirements:
  - E
  - Runde 6
  - H
  - K

must_haves:
  truths:
    - "Migration 0097 legt `member_suggestions` mit CHECK-Constraints für `suggestion_type`, `target_type`, `status` an (Lock H: kein FK auf anime_contributions oder member_claims)"
    - "Migration 0097 ergänzt `anime_contributions.member_reason TEXT NULL` für D-09"
    - "OpenAPI-Contract beschreibt `POST /me/suggestions`, `GET /me/suggestions` und den erweiterten `POST /me/anime-contributions/{id}/reject` mit `member_reason`-Body"
    - "Frontend-Typ `MeAnimeContribution` trägt `fansub_group_name`, `is_own_proposal`, `member_reason` (K)"
    - "Neue Frontend-Typen `MeSuggestion` und `MeSuggestionsResponse` sind in `types/contributions.ts` deklariert"
    - "Wave-0-Tests sind rote Testgerüste (failing): ContributionInbox.test.tsx, ContributionSummary.test.tsx, contributions_me_handler_test.go (D-09 reject-reason), suggestions_me_handler_test.go (D-06/D-07)"
  artifacts:
    - path: "database/migrations/0097_member_suggestions.up.sql"
      provides: "member_suggestions-Tabelle + member_reason-Spalte auf anime_contributions"
    - path: "shared/contracts/openapi.yaml"
      provides: "OpenAPI-Einträge für neue Suggestion-Endpoints + erweiterten reject-Endpoint"
    - path: "frontend/src/types/contributions.ts"
      provides: "Erweiterter MeAnimeContribution-Typ + neue MeSuggestion-Typen"
    - path: "frontend/src/components/contributions/ContributionInbox.test.tsx"
      provides: "Rotes Test-Gerüst für Inbox-Filter-Logik (D-03)"
    - path: "frontend/src/components/contributions/ContributionSummary.test.tsx"
      provides: "Rotes Test-Gerüst für useMemo-Aggregat + Chip-Toggle (D-11/D-12)"
    - path: "backend/internal/handlers/contributions_me_handler_test.go"
      provides: "Roter Test: Reject ohne member_reason → 422 (D-09)"
    - path: "backend/internal/handlers/suggestions_me_handler_test.go"
      provides: "Rote Tests: Suggestion-Submit + audit_logs-Eintrag (D-06/D-07)"
  key_links:
    - from: "database/migrations/0097_member_suggestions.up.sql"
      to: "app_users.id"
      via: "FK submitter_app_user_id REFERENCES app_users(id) ON DELETE RESTRICT"
      pattern: "REFERENCES app_users"
    - from: "shared/contracts/openapi.yaml"
      to: "frontend/src/types/contributions.ts"
      via: "MeSuggestion-DTO-Felder stimmen mit OpenAPI-Schema überein"
      pattern: "MeSuggestion|member_suggestion"
---

<objective>
Schema-/Contract-/Typ-Fundament für Phase 76: neue `member_suggestions`-Tabelle (Lock H), additive `member_reason`-Spalte auf `anime_contributions` (D-09), OpenAPI-Einträge für alle neuen Endpoints, Frontend-Typ-Erweiterungen und rote Wave-0-Testgerüste.

Purpose: Lock K (Contract-zuerst) verlangt, dass DB-Schema + OpenAPI + DTO + Typen vor jeder Backend-Implementierung festgelegt sind. Wave-0-Tests fixieren das erwartete Verhalten bevor Code geschrieben wird.

Output: Migration 0097, erweiterte `openapi.yaml`, erweitertes `types/contributions.ts`, 4 rote Test-Dateien.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-CONTEXT.md
@.planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-RESEARCH.md
@.planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-PATTERNS.md

<interfaces>
<!-- Bestehende Migrations-Struktur: analog 0086_anime_contributions.up.sql -->
<!-- Bestehende OpenAPI-Einträge: GET/POST /me/anime-contributions/* -->
<!-- Bestehende Typen in frontend/src/types/contributions.ts Zeilen 75–92 -->

Aus database/migrations/0086_anime_contributions.up.sql (Referenz-Pattern):
```sql
CREATE TABLE anime_contributions (
    id BIGSERIAL PRIMARY KEY,
    ...
    status VARCHAR(20) NOT NULL DEFAULT 'proposed',
    CONSTRAINT chk_anime_contributions_status CHECK (
        status IN ('proposed', 'confirmed', 'disputed', 'hidden', 'draft')
    )
);
```

Aus database/migrations/0075_audit_logs.up.sql (Audit-Pattern):
```sql
CREATE TABLE audit_logs (
    id BIGSERIAL PRIMARY KEY,
    actor_app_user_id BIGINT NULL REFERENCES app_users(id) ON DELETE SET NULL,
    event_type VARCHAR(100) NOT NULL,
    target_type VARCHAR(60) NULL,
    target_id BIGINT NULL,
    action_name VARCHAR(60) NULL,
    outcome VARCHAR(20) NULL DEFAULT 'allowed',
    payload JSONB NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

Aus frontend/src/types/contributions.ts (zu erweitern, Zeilen 75–92):
```typescript
export interface MeAnimeContribution {
  id: number
  anime_id: number
  anime_title: string
  fansub_group_id: number
  status: 'confirmed' | 'proposed' | 'draft' | 'disputed' | 'hidden'
  role_codes: string[]
  is_public_on_member_profile: boolean
  review_note?: string | null
  can_self_publish: boolean
  created_at: string
  // fansub_group_name, is_own_proposal, member_reason FEHLEN → müssen ergänzt werden
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0097 — member_suggestions-Tabelle + member_reason-Spalte</name>
  <files>database/migrations/0097_member_suggestions.up.sql, database/migrations/0097_member_suggestions.down.sql</files>
  <read_first>
    - database/migrations/0096_hist_group_members_confirmation_audit.up.sql (höchste bestehende Nummer verifizieren)
    - database/migrations/0086_anime_contributions.up.sql (Referenz-Pattern für CHECK-Constraints und FK-Stil)
    - database/migrations/0075_audit_logs.up.sql (app_users FK-Pattern)
    - .planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-RESEARCH.md §Pattern 5 (generische Tabellen-Struktur)
  </read_first>
  <behavior>
    - Migration 0097.up legt `member_suggestions` an: Spalten `id BIGSERIAL PK`, `submitter_app_user_id BIGINT NOT NULL REFERENCES app_users(id) ON DELETE RESTRICT`, `suggestion_type VARCHAR(40) NOT NULL`, `target_type VARCHAR(40) NOT NULL`, `target_id BIGINT NOT NULL`, `content_text TEXT NULL`, `media_asset_id BIGINT NULL REFERENCES media_assets(id) ON DELETE SET NULL`, `status VARCHAR(20) NOT NULL DEFAULT 'pending'`, `review_note TEXT NULL`, `reviewer_app_user_id BIGINT NULL REFERENCES app_users(id) ON DELETE SET NULL`, `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`, `updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`
    - CHECK-Constraints: `chk_member_suggestions_type` = `suggestion_type IN ('error_report', 'story', 'media')`, `chk_member_suggestions_target_type` = `target_type IN ('anime', 'contribution', 'fansub_group', 'member')`, `chk_member_suggestions_status` = `status IN ('pending', 'in_review', 'approved', 'rejected')`
    - Indexe: `idx_member_suggestions_submitter ON member_suggestions (submitter_app_user_id)`, `idx_member_suggestions_status ON member_suggestions (status)`
    - KEIN FK auf `anime_contributions` oder `member_claims` — Lock H strikt einhalten
    - Zweiter DDL-Block in derselben .up.sql-Datei: `ALTER TABLE anime_contributions ADD COLUMN IF NOT EXISTS member_reason TEXT NULL;` mit `COMMENT ON COLUMN anime_contributions.member_reason IS 'Pflicht-Begründung des Members bei Widerspruch (Das war ich nicht, Phase 76 D-09)';`
    - 0097.down.sql: `ALTER TABLE anime_contributions DROP COLUMN IF EXISTS member_reason;` dann `DROP TABLE IF EXISTS member_suggestions;` (Reihenfolge umgekehrt zur up-Datei)
  </behavior>
  <action>Erstelle `database/migrations/0097_member_suggestions.up.sql` mit zwei DDL-Blöcken: (1) CREATE TABLE member_suggestions mit allen Spalten, CHECK-Constraints und Indexen per RESEARCH Pattern 5; (2) ALTER TABLE anime_contributions ADD COLUMN IF NOT EXISTS member_reason TEXT NULL mit COMMENT. Erstelle `0097_member_suggestions.down.sql` mit DROP COLUMN dann DROP TABLE.</action>
  <verify>
    <automated>cd backend && go run ./cmd/migrate/main.go up 2>&1 | tail -5</automated>
  </verify>
  <done>Migration 0097 läuft ohne Fehler durch; `member_suggestions` existiert in der DB mit korrekten CHECK-Constraints; `anime_contributions.member_reason` existiert als TEXT NULL; down-Migration kehrt alles sauber um</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OpenAPI-Contract + Frontend-Typen (Lock K)</name>
  <files>shared/contracts/openapi.yaml, frontend/src/types/contributions.ts</files>
  <read_first>
    - shared/contracts/openapi.yaml (bestehende Me-Contributions-Einträge suchen via grep "me/anime-contributions"; Envelope-Format verstehen)
    - frontend/src/types/contributions.ts (vollständig lesen; bestehende MeAnimeContribution-Felder Zeilen 75–92)
    - .planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-PATTERNS.md §frontend/src/types/contributions.ts (MeAnimeContribution-Erweiterung + neue MeSuggestion-Typen)
    - .planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-RESEARCH.md §D-09 Reject-Endpoint-Erweiterung
  </read_first>
  <behavior>
    - openapi.yaml: Erweitere bestehenden `POST /me/anime-contributions/{contributionId}/reject` um optionalen Request-Body `requestBody` mit `application/json`-Schema `{ member_reason: { type: string, minLength: 5 } }` (required: [member_reason])
    - openapi.yaml: Füge `POST /api/v1/me/suggestions` hinzu mit Request-Body-Schema `{ suggestion_type: enum [error_report, story, media], target_type: enum [anime, contribution, fansub_group, member], target_id: integer, content_text: string (nullable) }`, Response 201 mit `{ data: MemberSuggestionResponse }`
    - openapi.yaml: Füge `GET /api/v1/me/suggestions` hinzu, Response 200 `{ data: MemberSuggestionResponse[] }`
    - openapi.yaml: Definiere Schema `MemberSuggestionResponse` mit Feldern: id (integer), suggestion_type (string), target_type (string), target_id (integer), content_text (string nullable), status (enum pending/in_review/approved/rejected), review_note (string nullable), created_at (string)
    - openapi.yaml: Erweiterung Schema `MeAnimeContribution` (falls inline definiert) um Felder: `fansub_group_name` (string), `is_own_proposal` (boolean), `member_reason` (string nullable)
    - frontend/src/types/contributions.ts: Ergänze `MeAnimeContribution` um `fansub_group_name?: string`, `is_own_proposal: boolean`, `member_reason?: string | null` — analog zu bestehenden Feldern mit JSDoc-Kommentar „Phase 76"
    - frontend/src/types/contributions.ts: Füge neue Interfaces `MeSuggestion` und `MeSuggestionsResponse` aus PATTERNS.md §frontend/src/types/contributions.ts hinzu
  </behavior>
  <action>In `shared/contracts/openapi.yaml`: (a) Erweitere den bestehenden reject-Endpoint um required `member_reason`-Body; (b) Füge zwei neue Suggestion-Endpoints hinzu; (c) Definiere Schema `MemberSuggestionResponse`. In `frontend/src/types/contributions.ts`: (a) Ergänze drei neue Felder auf `MeAnimeContribution` per D-09/D-12; (b) Füge `MeSuggestion` und `MeSuggestionsResponse` als neue Exports hinzu.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript-Kompilierung fehlerfrei; `MeAnimeContribution` hat `fansub_group_name`, `is_own_proposal`, `member_reason`; `MeSuggestion` und `MeSuggestionsResponse` sind exportiert; OpenAPI-Einträge für POST/GET /me/suggestions und erweiterter reject vorhanden</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave-0-Testgerüste (rote Tests für D-03, D-09, D-06/D-07)</name>
  <files>
    frontend/src/components/contributions/ContributionInbox.test.tsx,
    frontend/src/components/contributions/ContributionSummary.test.tsx,
    backend/internal/handlers/contributions_me_handler_test.go,
    backend/internal/handlers/suggestions_me_handler_test.go
  </files>
  <read_first>
    - frontend/src/components/contributions/MyContributionsSection.tsx (Test-Referenz-Pattern für Vitest-Component-Tests)
    - backend/internal/handlers/contribution_proposals_me_handler.go (bestehende Handler-Teststruktur und Auth-Mocking-Pattern)
    - .planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-VALIDATION.md (Per-Task Verification Map — exakte Test-Namen und Commands)
    - .planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-PATTERNS.md §ContributionInbox-Inbox-Filter-Pattern (D-03a-Logik)
  </read_first>
  <behavior>
    - ContributionInbox.test.tsx: Importiere Typen aus `@/types/contributions`; beschreibe `describe('ContributionInbox', () => { ... })`; schreibe 3 failing `it`-Tests: (1) `'filtert zugeordnet-aber-unbestätigt (is_own_proposal=false, status=proposed)'` — erwartet, dass das Inbox-Filterresultat 1 pending-Item enthält; (2) `'schließt eigene Vorschläge aus der pending-Liste aus (is_own_proposal=true, status=proposed)'`; (3) `'zeigt bestrittene Items (status=disputed)'` — Tests sollen FAIL, weil ContributionInbox noch nicht existiert (importiere es als `import { ContributionInbox } from './ContributionInbox'` — Datei existiert noch nicht)
    - ContributionSummary.test.tsx: `describe('ContributionSummary', () => { ... })`; 3 failing Tests: (1) `'aggregiert Zähler nach Status korrekt'` mit Beispieldaten; (2) `'Chip-Klick setzt activeFilters.status'`; (3) `'Toggle-Klick auf aktiven Chip setzt Filter zurück auf null'` — Tests sollen FAIL, weil ContributionSummary noch nicht existiert
    - contributions_me_handler_test.go: Go-Testdatei `package handlers_test`; Import `testing`, `net/http/httptest`, `github.com/stretchr/testify/assert`; Funktion `func TestRejectContributionRequiresReason(t *testing.T)` — sendet POST ohne Body an den Handler, erwartet 422/400-Response; soll compilieren aber FAIL (Handler prüft noch keinen Body)
    - suggestions_me_handler_test.go: Go-Testdatei `package handlers_test`; Funktion `func TestSuggestionAudit(t *testing.T)` — sendet POST mit validem Body, erwartet 201 + audit_logs-Eintrag; soll compilieren aber FAIL (Handler existiert noch nicht)
    - Kein Watch-Flag, kein `--watch`, keine interaktiven Tests
  </behavior>
  <action>Erstelle 4 Testdateien als rote Gerüste nach VALIDATION.md-Spec. Frontend-Tests nutzen `vi.mock`/`@testing-library/react` analog zu bestehenden Contribution-Tests. Go-Tests verwenden `httptest.NewRecorder()` und `httptest.NewRequest()`. Alle Tests müssen compilieren; alle sollen fehlschlagen, weil die implementierten Komponenten/Handler in Plan 02 und 03 erstellt werden.</action>
  <verify>
    <automated>cd frontend && npm test -- ContributionInbox ContributionSummary 2>&1 | tail -20</automated>
  </verify>
  <done>Alle 4 Test-Dateien existieren und compilieren; `npm test -- ContributionInbox` und `npm test -- ContributionSummary` zeigen FAIL (Komponenten noch nicht implementiert); Go-Tests compilieren (`cd backend && go build ./internal/handlers/...` läuft durch)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → Backend | Alle neuen Suggestion-/Reject-Endpoints sind Me-Endpoints, nur für eingeloggten User |
| DB-Constraint-Grenze | CHECK-Constraints auf `member_suggestions` verhindern ungültige suggestion_type/target_type/status-Werte |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01-01 | Tampering | member_suggestions FK | mitigate | Kein FK auf anime_contributions/member_claims in 0097 — Lock H strukturell durchgesetzt |
| T-76-01-02 | Tampering | openapi.yaml reject-body | mitigate | member_reason: minLength: 5 in OpenAPI-Schema; Backend binding:"required,min=5" folgt in Plan 02 |
| T-76-01-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Pakete in Phase 76 (RESEARCH: "Keine neuen externen Pakete") |
</threat_model>

<verification>
Nach Plan 01:
- `cd backend && go run ./cmd/migrate/main.go up` zeigt Migration 0097 ohne Fehler
- `cd frontend && npx tsc --noEmit` zeigt keine Fehler
- `cd frontend && npm test -- ContributionInbox ContributionSummary` zeigt rote Tests
- `cd backend && go build ./internal/handlers/...` kompiliert ohne Fehler
- `member_suggestions` hat KEINEN FK auf `anime_contributions` oder `member_claims`
</verification>

<success_criteria>
- Migration 0097 läuft durch und legt `member_suggestions` + `anime_contributions.member_reason` an
- OpenAPI hat POST/GET /me/suggestions und erweiterten reject-Endpoint mit member_reason
- Frontend-Typen kompilieren ohne Fehler
- 4 rote Testgerüste existieren und compilieren
</success_criteria>

<output>
Erstelle `.planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-01-SUMMARY.md` wenn fertig
</output>
