---
phase: 76-me-contributions-dashboard-registrierte-user-vorschl-ge
plan: 02
type: execute
wave: 2
depends_on:
  - 76-01
files_modified:
  - backend/internal/repository/anime_contributions_proposal_repository.go
  - backend/internal/repository/member_suggestions_repository.go
  - backend/internal/handlers/contributions_me_handler.go
  - backend/internal/handlers/suggestions_me_handler.go
  - backend/cmd/server/main.go
  - frontend/src/lib/api.ts
autonomous: true
requirements:
  - E
  - Runde 6
  - H
  - K

must_haves:
  truths:
    - "`ListByMemberIDWithProposalFields` liefert `fansub_group_name` und `is_own_proposal` im Response (D-12, D-03a)"
    - "Reject-Endpoint gibt 422/400 zurück wenn `member_reason` fehlt oder kürzer als 5 Zeichen ist (D-09 per K)"
    - "Reject-Endpoint schreibt `member_reason` in `anime_contributions.member_reason` bei Status-Update auf `disputed` (D-09)"
    - "`POST /me/suggestions` speichert neuen Vorschlag in `member_suggestions` mit korrektem Submitter, Typ, Ziel, Status='pending' (D-06, Lock H)"
    - "`POST /me/suggestions` schreibt einen `audit_logs`-Eintrag mit `event_type='member_suggestion.submitted'` (D-07)"
    - "`GET /me/suggestions` liefert eigene Vorschläge geordnet nach `created_at DESC` (Runde 6)"
    - "Alle neuen Backend-Handler nutzen `requireMeIdentity` + `resolveVerifiedMemberID` (V2/V4 Auth)"
    - "Frontend `rejectAnimeContributionWithReason` sendet `member_reason`-Body; `submitSuggestion` und `getMySuggestions` sind in `api.ts` vorhanden (Lock K)"
  artifacts:
    - path: "backend/internal/repository/member_suggestions_repository.go"
      provides: "MemberSuggestionsRepository mit Create + ListBySubmitter"
    - path: "backend/internal/handlers/suggestions_me_handler.go"
      provides: "SuggestionsMeHandler mit CreateSuggestion + ListSuggestions"
    - path: "backend/internal/handlers/contributions_me_handler.go"
      provides: "Erweiterter RejectMyAnimeContributionWithReason + erweiterte ListByMemberID-Query"
    - path: "frontend/src/lib/api.ts"
      provides: "rejectAnimeContributionWithReason, submitSuggestion, getMySuggestions"
  key_links:
    - from: "backend/internal/handlers/suggestions_me_handler.go"
      to: "backend/internal/repository/member_suggestions_repository.go"
      via: "suggestionsRepo.Create(ctx, SuggestionInput{...})"
      pattern: "suggestionsRepo\\.Create"
    - from: "backend/internal/handlers/contributions_me_handler.go"
      to: "anime_contributions.member_reason"
      via: "updateMyAnimeContributionStatusWithReason"
      pattern: "member_reason"
    - from: "frontend/src/lib/api.ts"
      to: "POST /api/v1/me/suggestions"
      via: "submitSuggestion(body: SubmitSuggestionBody)"
      pattern: "submitSuggestion"
---

<objective>
Backend-Implementierung: Repository + Handler für `member_suggestions` (neue Vorschlagstypen), Erweiterung des Reject-Endpoints um Pflicht-Begründung (D-09), Erweiterung der `ListByMemberID`-Query um `fansub_group_name` und `is_own_proposal`, Frontend-API-Helfer in `api.ts`.

Purpose: Die Wave-0-Tests aus Plan 01 sollen grün werden. Alle neuen Endpunkte sind eigenständig testbar und Lock-H-konform.

Output: Neues Repository und Handler für Suggestions, erweiterter Contributions-Handler, neue api.ts-Helfer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-CONTEXT.md
@.planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-PATTERNS.md
@.planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-RESEARCH.md

<interfaces>
<!-- Aus backend/internal/handlers/contributions_me_handler.go (349 Zeilen, bestehend) -->
<!-- Schlüssel-Pattern für requireMeIdentity + resolveVerifiedMemberID: -->
```go
// Zeilen 67–74 (auth):
identity, ok := requireMeIdentity(c)
if !ok { return }

// Zeilen 82–90 (member-ID):
memberID, err := h.resolveVerifiedMemberID(c.Request.Context(), identity.AppUserID)
if errors.Is(err, repository.ErrNotFound) {
    c.JSON(http.StatusNotFound, gin.H{"error": gin.H{"message": "kein verifizierter Member-Account verknüpft"}})
    return
}
if err != nil { internalError(c, "interner Serverfehler"); return }
```

<!-- Aus backend/internal/repository/anime_contributions_proposal_repository.go -->
<!-- ListByMemberIDWithProposalFields-Query (Zeilen 243–299) — zu erweitern um JOIN fansub_groups: -->
<!-- Aktuelle SELECT-Cols: animeContributionSelectCols + anime_title + can_self_publish + review_note -->
<!-- NEU hinzufügen: fg.name AS fansub_group_name, COALESCE(ac.created_by = $2, false) AS is_own_proposal -->
<!-- Analog zur Empfehlung in RESEARCH.md §Code Examples -->

<!-- Aus backend/internal/handlers/contribution_proposals_me_handler.go (Analog für Suggestions-Handler): -->
```go
// Konstruktor-Pattern (Zeilen 81–101):
type SuggestionsMeHandler struct {
    suggestionsRepo *repository.MemberSuggestionsRepository
    auditLogRepo    *repository.AuditLogRepository
}
func NewSuggestionsMeHandler(...) *SuggestionsMeHandler { ... }

// Audit-Pattern (Zeilen 351–358):
_ = h.auditLogRepo.Write(c.Request.Context(), repository.AuditLogEntry{
    ActorAppUserID: &identity.AppUserID,
    EventType:      "member_suggestion.submitted",
    TargetType:     req.TargetType,
    TargetID:       &req.TargetID,
    Action:         "submit",
    Outcome:        "allowed",
})
```

<!-- Aus frontend/src/lib/api.ts (Zeilen 7473–7497, bestehende rejectAnimeContribution): -->
```typescript
// Bestehend (kein Body):
export async function rejectAnimeContribution(contributionId: number): Promise<void> { ... }
// NEU (mit Body, D-09):
export async function rejectAnimeContributionWithReason(
  contributionId: number, memberReason: string
): Promise<void> { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend — member_suggestions Repository + Contributions-Query-Erweiterung</name>
  <files>
    backend/internal/repository/member_suggestions_repository.go,
    backend/internal/repository/anime_contributions_proposal_repository.go
  </files>
  <read_first>
    - backend/internal/repository/anime_contributions_proposal_repository.go (vollständig lesen; insb. ListByMemberIDWithProposalFields Zeilen 233–299, animeContributionSelectCols, JOIN-Struktur)
    - backend/internal/repository/anime_contributions_repository.go (AnimeContributionRow-Struct + animeContributionSelectCols-Const)
    - backend/internal/repository/anime_contributions_inputs.go (AnimeContributionRow-Struct mit json-Tags)
    - .planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-PATTERNS.md §backend/internal/repository/member_suggestions_repository.go (Repository-Struktur + Create + ListBySubmitter)
    - .planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-PATTERNS.md §anime_contributions_proposal_repository-Erweiterung (Reject mit member_reason)
  </read_first>
  <behavior>
    - Neue Datei `member_suggestions_repository.go`: Package `repository`; Typen `SuggestionInput`, `MemberSuggestionRow` (alle Felder mit json-Tags); Struct `MemberSuggestionsRepository` mit `db *pgxpool.Pool`; Konstruktor `NewMemberSuggestionsRepository`; Methode `Create(ctx, SuggestionInput) (*MemberSuggestionRow, error)` mit INSERT RETURNING; Methode `ListBySubmitter(ctx, appUserID int64) ([]MemberSuggestionRow, error)` mit SELECT WHERE submitter_app_user_id = $1 ORDER BY created_at DESC LIMIT 100; FK-Verletzung → `ErrNotFound` wrappen (analog isForeignKeyViolation-Muster aus bestehendem Code)
    - `anime_contributions_proposal_repository.go`: Erweiterung `ListByMemberIDWithProposalFields`: (1) Füge `fg.name AS fansub_group_name` und `COALESCE(ac.created_by = $2, false) AS is_own_proposal` zum SELECT hinzu; (2) Füge `JOIN fansub_groups fg ON fg.id = ac.fansub_group_id` hinzu; (3) Ergänze `$2 = appUserID` als zweiten Query-Parameter; (4) GROUP BY um `fg.name` erweitern; (5) `MemberContributionWithProposalRow`-Struct um `FansubGroupName string`, `IsOwnProposal bool` ergänzen (json-Tags: `fansub_group_name`, `is_own_proposal`); (6) Scan-Call um die neuen Felder erweitern
    - Methode `RejectWithMemberReason(ctx, contributionID, actorAppUserID int64, reviewNote, memberReason *string) error` in `anime_contributions_proposal_repository.go` oder `anime_contributions_repository.go` hinzufügen — UPDATE SET status='disputed', review_note=$2, member_reason=$3, updated_at=NOW() WHERE id=$1 AND status IN ('proposed', 'confirmed')
    - CLAUDE.md 450-Zeilen-Limit: Falls `anime_contributions_proposal_repository.go` nach Erweiterung ≥450 Zeilen → neue Methoden in separate Datei `anime_contributions_reject_repository.go` auslagern
  </behavior>
  <action>Erstelle `member_suggestions_repository.go` mit Create + ListBySubmitter. Erweitere `anime_contributions_proposal_repository.go`: (a) neue JOIN fansub_groups + is_own_proposal-Projektion in ListByMemberIDWithProposalFields; (b) füge RejectWithMemberReason-Methode hinzu (ggf. in neue Datei). Datei-Limit: bei >440 Zeilen splitten.</action>
  <verify>
    <automated>cd backend && go build ./internal/repository/... 2>&1</automated>
  </verify>
  <done>Go-Build für repository-Package fehlerfrei; `MemberSuggestionsRepository` mit `Create` und `ListBySubmitter`; `MemberContributionWithProposalRow` hat `FansubGroupName` und `IsOwnProposal`; `RejectWithMemberReason` vorhanden; kein 450-Zeilen-Überschuss</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backend — Handler für Suggestions + erweiterter Reject-Handler + main.go-Verdrahtung</name>
  <files>
    backend/internal/handlers/suggestions_me_handler.go,
    backend/internal/handlers/contributions_me_handler.go,
    backend/cmd/server/main.go
  </files>
  <read_first>
    - backend/internal/handlers/contributions_me_handler.go (vollständig lesen; RejectMyAnimeContribution Zeilen 155–230, updateMyAnimeContributionStatus, requireMeIdentity, resolveVerifiedMemberID, authorizeAnimeContributionOwner)
    - backend/internal/handlers/contribution_proposals_me_handler.go (vollständig lesen als Analogie für SuggestionsMeHandler-Konstruktor + AuditLog-Pattern)
    - backend/cmd/server/main.go (Routing-Abschnitt suchen; bestehende Handler-Instanziierung; Me-Contributions-Routen)
    - .planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-PATTERNS.md §suggestions_me_handler.go (Konstruktor, Request-DTO, Auth-Chain, Typ-Validierung, Audit-Write)
  </read_first>
  <behavior>
    - Neue Datei `suggestions_me_handler.go`: Struct `SuggestionsMeHandler` mit `suggestionsRepo *repository.MemberSuggestionsRepository` und `auditLogRepo *repository.AuditLogRepository`; Konstruktor `NewSuggestionsMeHandler`; Request-DTO `createSuggestionRequest` mit `SuggestionType string "required"`, `TargetType string "required"`, `TargetID int64 "required,min=1"`, `ContentText *string`; Methode `CreateSuggestion(c *gin.Context)`: requireMeIdentity → ShouldBindJSON → Typ-Validierung (map[string]bool für error_report/story/media) → suggestionsRepo.Create → auditLogRepo.Write(event_type="member_suggestion.submitted") → c.JSON(201, gin.H{"data": row}); Methode `ListSuggestions(c *gin.Context)`: requireMeIdentity → suggestionsRepo.ListBySubmitter → c.JSON(200, gin.H{"data": rows})
    - `contributions_me_handler.go`: Ersetze `RejectMyAnimeContribution` (oder füge Variante hinzu, falls alter Code noch gebraucht): neuer Handler `RejectMyAnimeContributionWithReason` liest Body `meContributionRejectRequest { MemberReason string "json:\"member_reason\" binding:\"required,min=5\"" }` → bei Fehler badRequest(c, "Begründung ist erforderlich.") → authorizeAnimeContributionOwner-Check → `h.repo.RejectWithMemberReason(ctx, contributionID, identity.AppUserID, nil, &req.MemberReason)` → 200; Route in main.go auf die neue Methode zeigen; CLAUDE.md: Falls nach Erweiterung ≥450 Zeilen → Request-Structs in `contributions_me_request_types.go` auslagern (analog Phase-67-02-Muster aus STATE.md)
    - `main.go`: Instanziiere `memberSuggestionsRepo := repository.NewMemberSuggestionsRepository(dbPool)`; `suggestionsMeHandler := handlers.NewSuggestionsMeHandler(memberSuggestionsRepo, auditLogRepo)`; Registriere Routen `meGroup.POST("/suggestions", suggestionsMeHandler.CreateSuggestion)`, `meGroup.GET("/suggestions", suggestionsMeHandler.ListSuggestions)`; Ändere bestehende reject-Route von alter Methode auf `RejectMyAnimeContributionWithReason`
  </behavior>
  <action>Erstelle `suggestions_me_handler.go` mit CreateSuggestion + ListSuggestions. Erweitere `contributions_me_handler.go` um RejectMyAnimeContributionWithReason mit Pflicht-member_reason (ggf. Request-Structs auslagern bei Limit). Verdrahte beide in `main.go` mit Routen.</action>
  <verify>
    <automated>cd backend && go build ./... 2>&1 | head -20</automated>
  </verify>
  <done>Backend-Build vollständig grün; Routen GET/POST /me/suggestions registriert; reject-Route zeigt auf RejectMyAnimeContributionWithReason; contributions_me_handler.go und suggestions_me_handler.go beide ≤450 Zeilen</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Frontend api.ts — neue Me-Helfer (Lock K)</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts (Zeilen 7394–7600 lesen: bestehende getMyAnimeContributions, confirmAnimeContribution, rejectAnimeContribution, patchAnimeContributionVisibility, selfPublishContribution — als Referenz für authorizedFetch-Pattern, getApiBaseUrl, parseApiErrorPayload, ApiError-Throwing)
    - frontend/src/types/contributions.ts (aktuelle MeSuggestion, SubmitSuggestionBody — aus Plan 01)
    - .planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-PATTERNS.md §api.ts (rejectAnimeContributionWithReason, submitSuggestion, getMySuggestions — Implementierungs-Snippets)
  </read_first>
  <behavior>
    - Füge `rejectAnimeContributionWithReason(contributionId: number, memberReason: string): Promise<void>` hinzu — analog zu `rejectAnimeContribution` (POST mit JSON-Body `{ member_reason: memberReason }`, kein RETURNING-Body erwartet)
    - Füge `SubmitSuggestionBody`-Interface hinzu: `{ suggestion_type: 'error_report' | 'story' | 'media', target_type: 'anime' | 'contribution' | 'fansub_group' | 'member', target_id: number, content_text?: string | null }`
    - Füge `submitSuggestion(body: SubmitSuggestionBody): Promise<void>` hinzu — POST /api/v1/me/suggestions mit JSON-Body
    - Füge `getMySuggestions(): Promise<MeSuggestionsResponse>` hinzu — GET /api/v1/me/suggestions, analog zu getMyAnimeContributions (no-store cache)
    - Import `MeSuggestionsResponse` aus `@/types/contributions` falls nicht bereits importiert
    - NICHT die bestehende `rejectAnimeContribution`-Funktion löschen oder verändern (könnte noch anderswo genutzt werden)
    - Alle neuen Funktionen nach bestehenden Me-Contributions-Helfern einordnen (Abschnitt-Kommentar "// === Phase 76: neue Me-Suggestion-Helfer ===" hinzufügen)
  </behavior>
  <action>Erweitere `frontend/src/lib/api.ts` nach dem letzten bestehenden Me-Contributions-Helfer um 3 neue Funktionen: rejectAnimeContributionWithReason (mit Body), submitSuggestion, getMySuggestions. Füge SubmitSuggestionBody-Interface hinzu.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript-Kompilierung fehlerfrei; `rejectAnimeContributionWithReason`, `submitSuggestion`, `getMySuggestions` sind exportiert; alte `rejectAnimeContribution` unverändert vorhanden</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → POST /me/suggestions | Authentifizierter User-Kontext; requireMeIdentity prüft Token |
| Handler → DB | suggestion_type/target_type durch Backend-Validierung + DB-CHECK |
| Reject-Handler → DB | Ownership-Check via authorizeAnimeContributionOwner vor member_reason-Write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-02-01 | Tampering | RejectWithMemberReason | mitigate | `authorizeAnimeContributionOwner` muss vor jedem Reject-Call bleiben; Eigentümerprüfung auf DB-Ebene via WHERE-Klausel |
| T-76-02-02 | Tampering | CreateSuggestion target_type | mitigate | Backend-Validierung `validTypes := map[string]bool{...}` verhindert Injection in falsche Domäne; DB-CHECK als zweite Schranke |
| T-76-02-03 | Information Disclosure | member_reason Feld | mitigate | `member_reason` erscheint nur in `getMyAnimeContributions`-Response (eigener User); niemals in public API; Phase-78-Leader-Review sieht es über Admin-Seam |
| T-76-02-04 | Elevation of Privilege | media_asset_id in member_suggestions | mitigate | In Plan 02 ist `media_asset_id` NULL (kein Upload-Endpoint noch); Phase 76 Plan 04 setzt Owner-Kontext per Decision-8-Matrix |
| T-76-02-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Pakete |
</threat_model>

<verification>
Nach Plan 02:
- `cd backend && go build ./... 2>&1` — fehlerfrei
- `cd backend && go test ./internal/handlers/... -run TestRejectContributionRequiresReason` — grün (Wave-0-Test aus Plan 01 dreht von rot auf grün)
- `cd backend && go test ./internal/handlers/... -run TestSuggestionAudit` — grün oder annähernd (ggf. Audit-Repo-Stub nötig)
- `cd frontend && npx tsc --noEmit` — fehlerfrei
- contributions_me_handler.go ≤ 450 Zeilen
</verification>

<success_criteria>
- Alle neuen Backend-Handler nutzen requireMeIdentity + ownership-Check
- Reject-Endpoint mit leerem Body gibt 422/400 zurück
- POST /me/suggestions speichert in member_suggestions (Lock H: kein Write in anime_contributions/member_claims)
- Audit-Log-Eintrag für jede Suggestion-Submission
- Frontend-Helfer kompilieren typsicher
</success_criteria>

<output>
Erstelle `.planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-02-SUMMARY.md` wenn fertig
</output>
