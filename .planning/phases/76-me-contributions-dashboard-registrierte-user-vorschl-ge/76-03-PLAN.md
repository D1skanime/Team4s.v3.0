---
phase: 76-me-contributions-dashboard-registrierte-user-vorschl-ge
plan: 03
type: execute
wave: 3
depends_on:
  - 76-02
files_modified:
  - frontend/src/components/contributions/ContributionInbox.tsx
  - frontend/src/components/contributions/ContributionSummary.tsx
  - frontend/src/components/contributions/ContributionFilters.tsx
  - frontend/src/components/contributions/VisibilityDropdown.tsx
  - frontend/src/components/contributions/contributions.module.css
autonomous: true
requirements:
  - E
  - H
  - K

must_haves:
  truths:
    - "`ContributionInbox` filtert client-seitig nach D-03: zugeordnet-aber-unbestätigt (is_own_proposal=false + status=proposed), disputed (!is_own_proposal), eigene abgelehnte (is_own_proposal + status=disputed), Sichtbarkeit offen (confirmed + !is_public)"
    - "`ContributionSummary` produziert per useMemo korrekte Zähler pro Status/Gruppe/Anime/Rolle und rendert Stat-Chips als `Button variant='subtle'` mit `aria-pressed` (D-12)"
    - "Chip-Klick setzt activeFilters; erneuter Klick auf aktiven Chip löscht Filter (Toggle, D-12)"
    - "`VisibilityDropdown` nutzt `Select` aus `@/components/ui` statt nativem `<select>` (C2)"
    - "Wave-0-Tests ContributionInbox.test.tsx und ContributionSummary.test.tsx sind grün"
    - "Alle Umlaute korrekt: Ö, ü, ä etc. in allen Strings (C3)"
    - "Keine Datei überschreitet 450 Zeilen (C4)"
  artifacts:
    - path: "frontend/src/components/contributions/ContributionInbox.tsx"
      provides: "Inbox-Sektion mit useMemo-Filter für D-03a/b/c/d"
    - path: "frontend/src/components/contributions/ContributionSummary.tsx"
      provides: "Summary-Aggregat + klickbare Stat-Chips (D-12)"
    - path: "frontend/src/components/contributions/ContributionFilters.tsx"
      provides: "FilterState-Typ + useMemo-Filterlogik (D-11)"
    - path: "frontend/src/components/contributions/VisibilityDropdown.tsx"
      provides: "Migrierte Version mit Select-Primitive (C2)"
  key_links:
    - from: "frontend/src/components/contributions/ContributionInbox.tsx"
      to: "frontend/src/types/contributions.ts"
      via: "MeAnimeContribution.is_own_proposal Feld"
      pattern: "is_own_proposal"
    - from: "frontend/src/components/contributions/ContributionSummary.tsx"
      to: "frontend/src/components/contributions/ContributionFilters.tsx"
      via: "FilterState-Typ + onFilterChange-Callback"
      pattern: "FilterState|onFilterChange"
---

<objective>
Frontend-Kernkomponenten für das Dashboard: `ContributionInbox` (D-02/D-03), `ContributionSummary` mit Stat-Chips (D-12), `ContributionFilters` (D-11), Migration von `VisibilityDropdown` auf `Select`-Primitive (C2).

Purpose: Die Dashboard-Sektionen entstehen als eigenständige, testbare Komponenten, bevor sie in Plan 05 in page.tsx verdrahtet werden. Wave-0-Tests drehen grün.

Output: 4 neue/migrierte Komponenten + CSS-Erweiterungen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-CONTEXT.md
@.planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-UI-SPEC.md
@.planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-PATTERNS.md

<interfaces>
<!-- Aus frontend/src/components/contributions/MyContributionsSection.tsx (54 Zeilen — Referenz-Pattern) -->
```typescript
'use client'
import { Card, EmptyState, SectionHeader } from '@/components/ui'
import type { MeAnimeContribution } from '@/types/contributions'
import { ContributionCard } from './ContributionCard'
import styles from './contributions.module.css'

interface MyContributionsSectionProps {
  contributions: MeAnimeContribution[]
  onVisibilityChange?: (id: number, isPublic: boolean) => void
}
```

<!-- Aus frontend/src/components/ui/index.ts (Primitive-Inventar, Pflicht): -->
<!-- Button, Badge, Card, Modal, Select, FormField, Textarea, SectionHeader, EmptyState, ErrorState, LoadingState -->

<!-- FilterState-Typ (neu, aus PATTERNS.md ContributionFilters): -->
```typescript
interface ContributionFilterState {
  status: string | null
  group: string | null
  anime: number | null
  role: string | null
  year: number | null
}
```

<!-- Aus UI-SPEC.md Copywriting Contract (verbindliche Strings): -->
<!-- Inbox-Titel: "Offene Aktionen" -->
<!-- Inbox-Beschreibung: "Diese Punkte brauchen deine Aufmerksamkeit – bestätige Zuordnungen, kläre Widersprüche oder entscheide über die Sichtbarkeit." -->
<!-- Summary-Titel: "Überblick & Filter" -->
<!-- Summary-Beschreibung: "Tippe auf einen Wert, um die Listen darunter zu filtern. Erneutes Tippen hebt den Filter auf." -->
<!-- Filter-Achsen-Labels: "Anime", "Gruppe", "Rolle", "Zeitraum", "Status" -->
<!-- "Filter zurücksetzen" -->
<!-- Inbox leer: "Keine offenen Aktionen" + "Es gibt gerade nichts zu klären. Neue Zuordnungen oder Rückmeldungen erscheinen hier." -->
<!-- "Das war ich" / "Das war ich nicht" / "Details korrigieren" -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ContributionInbox.tsx — Inbox-Sektion (D-02/D-03)</name>
  <files>frontend/src/components/contributions/ContributionInbox.tsx</files>
  <read_first>
    - frontend/src/components/contributions/MyContributionsSection.tsx (vollständig; Referenz-Pattern für Card+SectionHeader+ContributionCard-Komposition)
    - frontend/src/components/contributions/ContributionCard.tsx (vollständig; bestehende Props interface; mode="pending"-Buttons; aktuelle onConfirm/onReject-Props)
    - frontend/src/components/ui/index.ts (verfügbare Primitives checken; Card, SectionHeader, EmptyState, Badge, Button)
    - frontend/src/types/contributions.ts (MeAnimeContribution-Felder nach Plan 01; is_own_proposal, status-Enum)
    - .planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-PATTERNS.md §ContributionInbox (Inbox-Filter-Pattern, Sections-Render-Pattern)
    - frontend/src/components/contributions/ContributionInbox.test.tsx (Wave-0-Test aus Plan 01 — welche Interfaces werden erwartet)
  </read_first>
  <behavior>
    - Props: `contributions: MeAnimeContribution[]`, `onConfirm: (id: number) => void`, `onRejectWithReason: (id: number) => void`, `onCorrect: (id: number) => void` (für D-10 „Details korrigieren"), `onVisibilityChange: (id: number, isPublic: boolean) => void`
    - useMemo-Inbox-Filter (D-03): (a) `pending` = `status === 'proposed' && !is_own_proposal` (zugeordnet-aber-unbestätigt); (b) `disputed` = `status === 'disputed' && !is_own_proposal` (bestrittene); (c) `rejectedOwn` = `status === 'disputed' && is_own_proposal` (eigene abgelehnte); (d) `visibilityPending` = `status === 'confirmed' && !is_public_on_member_profile`
    - Render: `Card variant="section"` → `SectionHeader title="Offene Aktionen"` mit Beschreibung aus UI-SPEC; wenn alle vier Gruppen leer → `EmptyState title="Keine offenen Aktionen"`; sonst je Gruppe rendern
    - pending-Items: `ContributionCard mode="pending"` mit `onConfirm={onConfirm}` und `onRejectWithReason={onRejectWithReason}` und `onCorrect={onCorrect}`
    - disputed-Items (D-03b): Card + `Badge variant="danger"` + Hinweistext + `Button size="sm" variant="secondary"` „Details korrigieren" → `onCorrect(id)`
    - rejectedOwn-Items (D-03c): Card + `Badge variant="danger"` + Ablehngrund (`review_note`, Präfix: „Ablehngrund: ") + `Button size="sm" variant="secondary"` „Details korrigieren"
    - visibilityPending-Items (D-03d): Card + `Badge variant="warning"` + Prompt-Text „Soll diese bestätigte Mitwirkung öffentlich im Profil erscheinen?" + `VisibilityDropdown`
    - Alle Strings per UI-SPEC §Copywriting (korrekte Umlaute, keine ASCII-Ersetzungen)
    - Datei ≤450 Zeilen; falls Inboxgruppen zu groß werden: Hilfs-Subkomponenten in dieselbe Datei (kein extra Export nötig)
  </behavior>
  <action>Erstelle `ContributionInbox.tsx` mit useMemo-Filter für D-03a/b/c/d. Nutze Card, SectionHeader, EmptyState, Badge, Button aus `@/components/ui`. Kein handgebautes native Element. Strings aus UI-SPEC §Copywriting mit korrekten Umlauten.</action>
  <verify>
    <automated>cd frontend && npm test -- ContributionInbox 2>&1 | tail -20</automated>
  </verify>
  <done>ContributionInbox.test.tsx-Tests grün; Inbox filtert korrekt nach is_own_proposal und status; EmptyState wenn keine Items; Datei ≤450 Zeilen; TypeScript-kompilierbar</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ContributionSummary.tsx + ContributionFilters.tsx + VisibilityDropdown-Migration (D-11/D-12/C2)</name>
  <files>
    frontend/src/components/contributions/ContributionSummary.tsx,
    frontend/src/components/contributions/ContributionFilters.tsx,
    frontend/src/components/contributions/VisibilityDropdown.tsx,
    frontend/src/components/contributions/contributions.module.css
  </files>
  <read_first>
    - frontend/src/components/contributions/VisibilityDropdown.tsx (vollständig; aktuell 47 Zeilen mit nativem &lt;select&gt;; zu migrierende Logik: value, onChange, disabled, options)
    - frontend/src/components/contributions/contributions.module.css (vollständig; bestehende Klassen; neue Klassen hinzufügen ohne Hardcode-Pixel)
    - frontend/src/components/ui/index.ts (Select-Primitive: Props, Import-Pfad)
    - .planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-PATTERNS.md §VisibilityDropdown (Migrations-Pattern mit Select-Primitive)
    - .planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-PATTERNS.md §ContributionSummary (useMemo-Aggregat + Stat-Chip-Pattern)
    - .planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-PATTERNS.md §ContributionFilters (Filter-State-Pattern)
    - frontend/src/components/contributions/ContributionSummary.test.tsx (Wave-0-Test — welche Props werden erwartet)
    - .planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-UI-SPEC.md §Spacing Scale + §Color (Token-Pflicht: --space-*, --accent-primary; aktiver Chip Accent-Optik)
  </read_first>
  <behavior>
    - ContributionSummary.tsx: Props `contributions: MeAnimeContribution[]`, `activeFilters: ContributionFilterState`, `onFilterChange: (f: ContributionFilterState) => void`; useMemo für `{ byStatus: Map<string,number>, byGroup: Map<string,number>, byRole: Map<string,number> }`; Render: `Card variant="section"` → `SectionHeader title="Überblick & Filter"` + Beschreibung aus UI-SPEC; Achsen-Gruppen als Flex-Chip-Zeilen; pro Achse `Button variant="subtle" size="sm" aria-pressed={...}` mit Zähler; Klick: `onFilterChange({ ...activeFilters, [achse]: toggle })`; Achse-Label aus UI-SPEC: „Anime", „Gruppe", „Rolle", „Status"; „Filter zurücksetzen"-Button erscheint wenn min. ein Filter aktiv ist; aktiver Chip: CSS-Klasse `.chipActive` mit `border-color: var(--accent-primary)` + `color: var(--accent-primary)` (kein inline-style); Inaktiver Chip: neutrale Optik via `.chipInactive`
    - ContributionFilters.tsx: Exportiert `ContributionFilterState`-Interface und `applyFilters(filters: ContributionFilterState)`-Util-Funktion (Pure Function: (c: MeAnimeContribution) => boolean); Komponente `ContributionFilters` ist optional (Chips sind in Summary integriert); Datei dient als shared-Types-Datei für Filter-State
    - VisibilityDropdown.tsx: Ersetze native `<select>` (Zeilen 33–43) durch `Select` aus `@/components/ui`; Props und Logik identisch — `value`, `onChange`, `disabled`, `aria-label`; Options: „Öffentlich im Member-Profil" / „Nur intern sichtbar" (Strings per UI-SPEC); entferne `style={...}`-Inline-Objekte auf dem Select-Element
    - contributions.module.css: Füge neue Klassen `.inboxContainer`, `.summaryChips`, `.chipActive`, `.chipInactive` hinzu — ausschließlich mit `--space-*`-Tokens und CSS-Custom-Properties (`--accent-primary`); keine Hardcode-Pixel; bestehende Pixel-Klassen bei Berührung auf Tokens umstellen
  </behavior>
  <action>Erstelle ContributionSummary.tsx mit useMemo + Button-Chips + Toggle-Logik. Erstelle ContributionFilters.tsx als Filter-State-Modul. Migriere VisibilityDropdown.tsx: native &lt;select&gt; → Select-Primitive. Erweitere contributions.module.css um neue Token-basierte Klassen.</action>
  <verify>
    <automated>cd frontend && npm test -- ContributionSummary 2>&1 | tail -20</automated>
  </verify>
  <done>ContributionSummary.test.tsx-Tests grün; Chip-Toggle funktioniert; VisibilityDropdown nutzt kein natives &lt;select&gt; mehr; `npx eslint src/components/contributions/VisibilityDropdown.tsx` zeigt keine no-restricted-syntax-Fehler; alle CSS-Klassen nutzen --space-*-Tokens</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-State → useMemo-Filter | Filterlogik bleibt rein client-seitig (D-11); kein neuer API-Call für Filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-03-01 | Information Disclosure | ContributionInbox member_reason | mitigate | `member_reason` nur im eigenen Dashboard sichtbar; Inbox rendert es nur für is_own_proposal=true Items |
| T-76-03-02 | Spoofing | is_own_proposal client-seitig | accept | is_own_proposal wird server-seitig berechnet (Plan 02 COALESCE-Logik); kein clientseitiger Vergleich mit unkontrollierter app_user_id |
| T-76-03-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Pakete |
</threat_model>

<verification>
Nach Plan 03:
- `cd frontend && npm test -- ContributionInbox ContributionSummary` — beide Testsuiten grün
- `cd frontend && npx eslint src/components/contributions/VisibilityDropdown.tsx` — keine no-restricted-syntax-Fehler
- `cd frontend && npx tsc --noEmit` — fehlerfrei
- Alle neuen Dateien ≤450 Zeilen
</verification>

<success_criteria>
- ContributionInbox.test.tsx und ContributionSummary.test.tsx beide grün
- VisibilityDropdown nutzt Select-Primitive
- Filter-State-Typ (ContributionFilterState) ist exportiert und nutzbar
- Kein natives `<select>`/`<button>`/`<input>` in neuen Komponenten
</success_criteria>

<output>
Erstelle `.planning/phases/76-me-contributions-dashboard-registrierte-user-vorschl-ge/76-03-SUMMARY.md` wenn fertig
</output>
