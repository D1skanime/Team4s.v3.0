# 131-05 — Documented, enforced page bounds; hardcoded LIMIT 3 -> bounded page (Wave 2)

**Requirement:** PMPF-03, PMCT-06, D-04/D-05 (SC3). **Depends on:** 131-04 (stable ordering the bounds page over).
**Goal:** Every public list has a documented, enforced initial size and max page size; the hardcoded `LIMIT 3` latest-contributions slice and the currently-UNBOUNDED previous-contributions load become bounded, separately-loadable pages carrying only what their card renders (no UI-unused child collections).

**Read first:** `131-CONTEXT.md` D-04 (starting contract: current_projects 6/max 24; latest_contributions 3/max 20; previous_contributions 6/max 24; contributions endpoint 20/max 50) + D-05, `backend/internal/handlers/app_public_profile.go` (`parseBoundedProjectPageValue` default 6 / max 24 / offset 0..10000 — the reusable clamp seam), `backend/internal/repository/member_profile_contributions_repository.go` (`loadLatestContributions` hardcoded `LIMIT 3` L97; `loadPreviousContributions` no LIMIT L144-169), and the member contributions continuation endpoint (identify: `/api/v1/members/{slug}/contributions` route + its handler/loader; confirm exact handler before editing).

**Files**
- MODIFY `backend/internal/repository/member_profile_contributions_repository.go` (parameterize limit/offset for latest + previous; enforce max).
- MODIFY `backend/internal/repository/member_profile_public_repository.go` (pass the documented INITIAL sizes: latest 3, previous 6).
- MODIFY `backend/internal/handlers/app_public_profile.go` and/or the contributions handler (reuse/extend `parseBoundedProjectPageValue` for each list's documented initial+max).

**Steps**
1. Parameterize `loadLatestContributions(ctx, memberID, limit, offset)` (replace hardcoded `LIMIT 3` with `$limit OFFSET $offset`) and `loadPreviousContributions(ctx, memberID, limit, offset)` (add `LIMIT/OFFSET` — today it is unbounded). The initial profile load passes the documented initials (latest 3, previous 6).
2. Enforce max page sizes via the existing clamp seam: extend/parameterize `parseBoundedProjectPageValue` (or a shared helper) so each continuation endpoint clamps to its documented max (projects 24, latest 20, previous 24, contributions 50). Initials are FIXED; max numbers may be tuned after 131-08 baseline (keep them as named constants).
3. D-05: verify each list page carries only the fields its card renders (no unused child collections); drop any over-fetched columns.
4. Add a member contributions continuation loader/endpoint page if the previous/latest lists need their own "Mehr anzeigen" beyond the embedded initial page (reuse the existing projects continuation shape; do not invent a parallel projection).

**Verification**
- `go build ./...`; `go test ./internal/repository/ ./internal/handlers/ -run "Bound|Page|Contributions|Projects"` GREEN (`TEAM4S_PHASE131_TEST_DSN` / DB-free handler bound tests).
- Tests prove: over-max requests clamp to the documented max; initial payload uses documented initials; previous_contributions is now bounded (a member with many previous rows returns only the initial page).
- No list returns UI-unused child collections.

**Commit:** `feat(131-05): documented enforced page bounds for public profile lists`

**Notes/deviations to surface:** Initials are the CONTRACT (fixed); max sizes are tunable post-baseline (131-08) — keep them as constants, not literals. The OpenAPI documentation of these bounds + honest continuation semantics lands in 131-06. `loadPreviousContributions` being unbounded today is a real gap this plan closes.
