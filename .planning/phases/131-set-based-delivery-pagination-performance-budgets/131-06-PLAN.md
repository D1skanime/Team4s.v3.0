# 131-06 — OpenAPI pagination contract + TS/api.ts parity (Wave 3)

**Requirement:** PMCT-06, D-03/D-04. **Depends on:** 131-04 (honest total/ordering), 131-05 (bounds).
**Goal:** The offset/limit/total continuation semantics and the documented per-list page bounds are the OpenAPI contract, and the frontend TypeScript types + `api.ts` helpers parse them in full parity — including any new bounded continuation loader introduced in 131-05.

**Read first:** `131-05-PLAN.md` (final bound numbers + any new continuation endpoint), `docs/api/api-contracts.md` (OpenAPI as source), `shared/contracts/openapi.yaml` (`PublicMemberProjectsPage` {items,total,limit,offset} + `PublicMemberProjectsEnvelope`; contribution schemas; the member contributions response schema), `frontend/src/types/profile.ts` (`PublicMemberProjectsPage`, contributions types), `frontend/src/lib/api.ts` (`getMemberProfile` + projects/contributions continuation helpers).

**Files**
- MODIFY `shared/contracts/openapi.yaml` (document limit/offset/total honest semantics + max page sizes per list; parameter schemas for each continuation endpoint).
- MODIFY `frontend/src/types/profile.ts` (mirror any page/parameter shape changes).
- MODIFY `frontend/src/lib/api.ts` (continuation helpers pass documented bounds; parse the honest total/continuation).

**Steps**
1. In OpenAPI, for each paginated operation document: `limit` (default = documented initial, maximum = documented max), `offset` (min 0), and that `total` reflects the full visible set (honest continuation). State the stable-ordering guarantee (D-02) in the operation description. Keep the envelope shapes minimal (D-05 — no unused child collections).
2. Mirror to TS: `PublicMemberProjectsPage` and any contributions page type stay `{ items, total, limit, offset }`; add types for any new continuation loader from 131-05.
3. `api.ts`: continuation helpers request the documented page sizes and consume `total`/`offset` for "Mehr anzeigen"; no client-side rebuild of the full profile (SC2).
4. Contract-first discipline: OpenAPI is the source; Go request bounds (131-05) and TS must agree with it.

**Verification**
- YAML parses; `npm run -w frontend typecheck` passes (only the pre-existing unrelated dirty-file errors remain — verify no NEW errors in touched files).
- The documented `maximum` per list matches the Go clamp constants (131-05) and the TS helper bounds — a cross-layer parity check (extend the 130-07-style parity approach if useful).
- `previous_contributions` / `latest_contributions` continuation (if added) is documented and typed.

**Commit:** `feat(131-06): document offset pagination contract and align TS/api helpers`

**Notes/deviations to surface:** Do NOT change initials (contract); only document them + the maxes. Respect the frozen pre-existing dirty frontend files — touch only what this plan needs and keep NEW typecheck errors at zero. If the member contributions endpoint already fully documents pagination, this is partly verify-only — say so.
