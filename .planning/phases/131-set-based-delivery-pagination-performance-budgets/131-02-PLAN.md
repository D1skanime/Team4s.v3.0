# 131-02 — Extend the evidence harness; baseline both seed profiles (Wave 1)

**Requirement:** PMPF-07, D-06. **Depends on:** nothing (runs against the current build; parallel to 131-01).
**Goal:** Extend `frontend/scripts/collect-member-profile-evidence.mjs` to capture the full metric set — payload size, request/latency, image waterfall, and Web Vitals — for BOTH seed profiles (`sheppert`, `csubs-leader`), and produce a reproducible, committed baseline artifact under the phase dir. Budgets are locked later (131-08) as baseline + ~20% plus the absolute ceilings.

**Read first:** `131-CONTEXT.md` (D-06/D-08; the two seed profiles are the fixtures, reused not recreated), `frontend/scripts/collect-member-profile-evidence.mjs` (current harness), `scripts/seed-member-profile-fixtures.mjs` + `scripts/README-seed.md` (129-01 seed; how to get both profiles + tokens), `frontend/src/lib/api.ts` (endpoints/ports the harness hits). Confirm live ports via `docker ps` (memory: KC/Backend host ports differ from `.env`).

**Files**
- MODIFY `frontend/scripts/collect-member-profile-evidence.mjs` (add the missing metrics + both-profile iteration; keep idempotent/reproducible).
- NEW `.planning/phases/131-set-based-delivery-pagination-performance-budgets/evidence/baseline-<profile>.json` for each profile (committed baseline artifacts).
- NEW `.planning/phases/131-.../evidence/README-baseline.md` (how to reproduce: env, ports, seed, run command).

**Steps**
1. Ensure the harness seeds/uses both reference profiles (reuse the 129 fixture; do NOT re-create data). Parameterize base URL/ports from env (never `.env` literals).
2. Capture per profile: initial-profile payload bytes, project/contribution continuation payload bytes, request count + latency, image waterfall (count/sizes/timing), and Web Vitals (LCP, CLS, INP) — the D-06 metric set.
3. Emit a deterministic JSON artifact per profile under the phase `evidence/` dir; re-running produces stable structure (values may vary run-to-run — record method + environment).
4. Do NOT lock budgets here — this is the baseline capture. Note absolute ceilings for later: LCP<=2.5s, CLS<=0.1, INP<=200ms, constant query count.

**Verification**
- The harness runs against the live dev stack for BOTH profiles and writes both baseline artifacts; a second run reproduces them (structure-stable).
- `README-baseline.md` lets a fresh operator reproduce the numbers.
- Web-Vitals + payload + waterfall fields are present and non-empty for both profiles.

**Commit:** `feat(131-02): extend evidence harness and capture both-profile performance baseline`

**Notes/deviations to surface:** Authoritative live sign-off remains the bundled Phase-134 UAT (D-08). This plan needs the live dev stack + 129 seed runnable; if the stack/seed is unavailable, capture is blocked (flag it, do not fabricate numbers). Image-variant/compression work is Phase 133 (PMPF-06/08), not here — only MEASURE the waterfall.
