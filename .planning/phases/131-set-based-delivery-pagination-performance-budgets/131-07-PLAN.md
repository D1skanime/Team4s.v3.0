# 131-07 — Cache-class separation lock (no shared cache in 131) (Wave 3)

**Requirement:** PMPF-04, D-09. **Depends on:** 131-04/131-05 (final response shapes). Parallel to 131-06.
**Goal:** Public (anonymous) and viewer-specific (owner / private-preview) responses stay in SEPARATE cache classes so an owner preview can never leak to an anonymous cache; no shared/public cache is introduced in 131. Lock the existing behaviour with a test and document the decision.

**Read first:** `131-CONTEXT.md` D-09, `backend/internal/handlers/app_public_profile.go` + the cache-header seam (`setPublicMemberResponseCache` / `private, no-store` + `Vary: Authorization` from Phase 128), the Phase-128 owner-preview cache decisions in `128-CONTEXT.md`, and the existing cache-header test (`TestPublicMemberCacheHeaders` in `app_public_profile_test.go`).

**Files**
- MODIFY (only if a gap is found) the cache-header seam in `backend/internal/handlers/app_public_profile.go`.
- MODIFY `backend/internal/handlers/app_public_profile_test.go` (extend the cache-class lock test).
- NEW/MODIFY a short decision note in the phase dir documenting the D-09 cache-class policy.

**Steps**
1. Verify: viewer-dependent responses (is_owner / is_private_preview true) emit `Cache-Control: private, no-store` and `Vary: Authorization`; anonymous public responses use the same private, no-store class (no shared public cache exists). Confirm no code path caches an owner-preview body where an anonymous request could read it.
2. Lock test: assert that an owner/private-preview response and an anonymous response carry the separating cache headers, and that NO shared/public cache-control (e.g. `public, max-age`) is emitted anywhere on these routes.
3. Document the D-09 policy (no shared cache in 131; separate viewer classes; a future shared cache requires measured bottleneck + complete invalidation) as a short note under the phase dir.

**Verification**
- `go test ./internal/handlers/ -run "Cache|Viewer"` GREEN (DB-free).
- Test proves the separating headers on both response classes and the ABSENCE of any `public`/shared cache-control on the public member routes.

**Commit:** `test(131-07): lock viewer/anonymous cache-class separation; no shared cache`

**Notes/deviations to surface:** This is expected to be largely VERIFY-ONLY — Phase 128 already set `private, no-store` + `Vary: Authorization`; if so, the deliverable is the lock test + the documented policy, and no production header change. State explicitly if nothing needed changing. Introducing any shared cache is OUT of scope (D-09).
