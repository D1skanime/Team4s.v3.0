# 131-08 — Re-measure, lock budgets, evidence-backed indexes only (Wave 4)

**Requirement:** PMPF-05, PMPF-07, D-06/D-08/D-10 (SC4/SC5). **Depends on:** 131-02 (baseline), 131-03/04/05/06 (structural changes).
**Goal:** Re-run the evidence harness on BOTH seed profiles after the structural work; lock payload/latency budgets at baseline + ~20% plus the absolute ceilings; commit the reproducible evidence; and add an index ONLY where `EXPLAIN (ANALYZE, BUFFERS)` on both profiles proves it helps (else explicitly none).

**Read first:** `131-02-PLAN.md` (baseline artifacts + harness), `131-CONTEXT.md` D-06/D-07/D-08/D-10 (budgets = baseline + ~20%; absolute ceilings LCP<=2.5s, CLS<=0.1, INP<=200ms, constant query count; no speculative indexes), the current index set on the touched tables, and both fixture query plans.

**Files**
- MODIFY `frontend/scripts/collect-member-profile-evidence.mjs` (assert against locked budgets; reuse 131-02 harness).
- NEW `.planning/phases/131-.../evidence/post-change-<profile>.json` + a `BUDGETS.md` recording the locked thresholds and their basis.
- NEW `database/migrations/NNNN_*.sql` (ONLY if an index is justified) + its `EXPLAIN (ANALYZE, BUFFERS)` evidence artifact for BOTH profiles.

**Steps**
1. Re-run the harness on both profiles post-change; record post-change artifacts. Confirm the constant query budget (131-03 test) and the payload reductions from bounds (131-05).
2. Lock budgets in `BUDGETS.md`: payload + latency = post-change baseline + ~20%; plus the absolute Web-Vitals ceilings and the constant-query-count ceiling. Wire the harness to FAIL if a profile exceeds a locked budget.
3. Indexes (D-10): for each candidate, capture `EXPLAIN (ANALYZE, BUFFERS)` on BOTH seed profiles BEFORE and AFTER; add the index (reversible migration) ONLY if evidence shows a real improvement. If none is justified, record "no index added" WITH the plans that prove existing indexes suffice — do not add speculative indexes.
4. If a migration lands, reset/reseed disposable data per the milestone test-data rule.

**Verification**
- Harness runs GREEN against both profiles within the locked budgets; a deliberately shrunk budget makes it FAIL (prove the gate bites, then restore).
- Any added index has committed `EXPLAIN (ANALYZE, BUFFERS)` evidence for both profiles; migration is reversible; `go build`/tests green.
- `BUDGETS.md` states each threshold and its basis (baseline+margin vs absolute ceiling).

**Commit:** `test(131-08): lock performance budgets and evidence-backed indexes on both profiles`

**Notes/deviations to surface:** Authoritative live sign-off is the bundled Phase-134 UAT (D-08) — this plan produces the reproducible, committed evidence + locked budgets, not the final milestone sign-off. Needs the live dev stack + 129 seed; if unavailable, budget-lock is blocked (flag, do not fabricate). Expected likely outcome on indexes: none justified for the modest per-member lists — record the proof either way (no silent "covered").
