# 131-03 — Constant query budget: batch the per-card N+1 (Wave 2)

**Requirement:** PMPF-01, D-07 (SC1). **Depends on:** 131-01 (query tracer + characterization).
**Goal:** A public profile load issues a query count that is CONSTANT regardless of the member's project count — no per-project/per-card round-trips. Close the confirmed per-card N+1 where `loadCurrentProjects` issues one `loadCurrentProjectReleaseVersions` query per project, and add the enforcing constant-budget test (GREEN after this fix).

**Read first:** `131-01-PLAN.md` (tracer + the documented per-card seam), `backend/internal/repository/member_profile_projects_repository.go` (`loadCurrentProjects` L11, per-project call at L109, `loadCurrentProjectReleaseVersions` L180 with `ORDER BY ... rv.id`), `backend/internal/repository/member_profile_public_repository.go` (the ~10 sequential loaders; each already one query), `docs/engineering/implementation-contract.md` (reuse-first; no parallel projections).

**Files**
- MODIFY `backend/internal/repository/member_profile_projects_repository.go` — collapse the per-project release-version loads into ONE set-based query for the whole page (e.g. a single query filtered by the page's `(anime_id, fansub_group_id)` pairs, or JSON aggregation folded into `loadCurrentProjects`), then attach release versions to their project in Go.
- MODIFY `backend/internal/repository/member_profile_query_budget_test.go` — add the enforcing assertion.

**Steps**
1. Replace the in-loop `loadCurrentProjectReleaseVersions(ctx, memberID, item.AnimeID, item.FansubGroupID)` (L109) with: load the projects page (1 query), then load ALL release versions for that page's projects in ONE query (`WHERE (anime_id, fansub_group_id) IN (...)` or a lateral/JSON-agg variant), and group them in Go by `(anime_id, fansub_group_id)`. Preserve the existing per-project release-version ORDER BY (`... rv.id`) inside the batched result.
2. Verify `GetPublicMemberProjectsByID` (the projects continuation loader) uses the same non-N+1 path (it shares `loadCurrentProjects`).
3. Enforcing test: seed member A (few projects) and member B (many projects); assert `queryCount(A) == queryCount(B)` AND equals the expected constant (document the number, ~11). This flips the 131-01 characterization into a hard ceiling (D-07).
4. Behaviour-preserving: identical JSON output for the reference profiles before/after (roles, cover art, release versions, ordering unchanged).

**Verification**
- `go build ./...`; `go test ./internal/repository/ -run QueryBudget` GREEN with `TEAM4S_PHASE131_TEST_DSN`: query count identical across project counts and constant.
- Existing public-profile/projects PostgreSQL tests + `*SourceInvariants` stay green (behaviour preserved).
- Any touched production `.go` file stays <=450 lines (`wc -l`); split only if a touched file exceeds it (no monolith exists — see note).

**Commit:** `perf(131-03): batch current-project release versions into a constant query budget`

**Notes/deviations to surface:** No 1810-line monolith to split — `member_profile_repository.go` is already 188 lines (129-S-02 done); the CONTEXT's split task is obsolete. The per-card N+1 is bounded by page size today (6), so SC1-by-total-count was already technically met; this plan closes the stricter D-07 "no per-card reads" and makes the constant a proven ceiling. Do not change page sizes here (that is 131-05).
