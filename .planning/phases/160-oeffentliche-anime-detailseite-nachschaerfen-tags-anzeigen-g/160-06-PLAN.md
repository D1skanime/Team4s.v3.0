---
phase: 160-oeffentliche-anime-detailseite-nachschaerfen-tags-anzeigen-g
plan: 06
type: execute
wave: 4
depends_on: ["160-01", "160-05"]
files_modified:
  - frontend/src/app/anime/[id]/page.tsx
  - frontend/src/app/anime/[id]/page.module.css
  - frontend/src/app/anime/[id]/page.test.tsx
  - frontend/src/app/anime/[id]/page.performance.test.ts
  - frontend/src/app/anime/[id]/animeDetailData.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-06: Die Tag-Chips zeigen den vom Backend bereits aufgelösten deutschen Namen (oder Grundname als Fallback) ohne eigene Sprachlogik im Frontend."
    - "D-09: Jeder Tag-/Genre-Chip verlinkt korrekt URL-kodiert auf /suche?type=anime&tag=<Name> bzw. /suche?type=anime&genre=<Name>, gebaut über URLSearchParams (keine neue Encoding-Konvention)."
    - "D-12: Reihenfolge in der Infokarte: Titel → Status/Typ/Jahr → Beschreibung → Tags → Linie → Jellyfin-Banner → Verwandte Anime."
    - "D-13: Die Linie bleibt an den Banner gebunden; ohne Banner keine Linie nach den Tags, nur Abstand. Ohne Tags: keine Überschrift, kein Container, kein zusätzlicher Abstand."
    - "D-14: Tags erscheinen ausschließlich im neuen Tags-Block — nicht im Genre-Bereich links, nicht in der Statuszeile, nicht unter Verwandte Anime, nicht in der Episoden-Sektion."
    - "D-15: Die Überschrift über den Tag-Chips lautet exakt 'Tags'."
    - "D-16: Tag-Chips sind echte Links (Next Link), per Tastatur erreichbar mit sichtbarem Fokus, stehen in einer über die Überschrift 'Tags' benannten Liste, sichtbarer/zugänglicher Linktext ist nur der Name."
    - "D-17: Tag-Chips sind optisch Inhaltsnavigation (normale Schreibweise, kleine/mittlere abgerundete Chips, dezenter Hintergrund, Hover-/Fokus-Rückmeldung, Pointer, ausreichender Kontrast, kein CTA-Stil), über globale Design-Tokens, nicht wie die Metadaten-Badges."
    - "D-18: Tags sind alphabetisch nach angezeigtem Namen sortiert."
    - "D-19: Alle Tags werden angezeigt, Chips brechen um (Desktop/Tablet/Mobile), keine horizontale Scrollleiste, kein Einklappen."
    - "D-20: Genre-Chips links am Poster werden Links auf /suche?type=anime&genre=<Name> im heutigen Chip-Stil plus Hover/Pointer/Fokus; der Platzhalter-Chip 'Anime' bei fehlenden Genres bleibt unverlinkt."
  artifacts:
    - path: "frontend/src/app/anime/[id]/page.tsx"
      provides: "Tags-Block zwischen Beschreibung und Emby-Link/AnimeInfoBanner; Genre-Chips als Link"
    - path: "frontend/src/app/anime/[id]/page.module.css"
      provides: ".tagsSection/.tagsList/.tagChip (Inhaltsnavigation-Stil, globale Tokens) und Hover/Fokus-Zustände für .genreChip als Link"
  key_links:
    - from: "frontend/src/app/anime/[id]/page.tsx"
      to: "/suche"
      via: "new URLSearchParams({ type: 'anime', tag: name }).toString()"
      pattern: "URLSearchParams\\(\\{ type: 'anime'"
    - from: "frontend/src/app/anime/[id]/page.tsx"
      to: "anime.tags"
      via: "conditional render only when anime.tags && anime.tags.length > 0"
      pattern: "anime\\.tags"
---

<objective>
Render the Tags block on the public anime detail page and convert genre chips into real links, both
pointing at the now-working q-less `/suche` filter (plan 160-05) and displaying the already-German-
resolved names (plan 160-01).

Purpose: This is Layer 3 (Öffentliche Anzeige, D-06/D-09/D-12–D-20), the final and most heavily
decision-constrained layer of the mandatory build order — it depends on Layer 1 shipping the resolved
display names and Layer 2 accepting the link targets it generates.
Output: A new Tags block in the info card (own divider, own empty-state suppression), genre chips
converted from `<span>` to `<Link>` (except the "Anime" placeholder), all styled as content navigation
per D-17, all covered by extended component/performance/data tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/160-oeffentliche-anime-detailseite-nachschaerfen-tags-anzeigen-g/160-CONTEXT.md
@.planning/phases/160-oeffentliche-anime-detailseite-nachschaerfen-tags-anzeigen-g/160-RESEARCH.md
@.planning/phases/160-oeffentliche-anime-detailseite-nachschaerfen-tags-anzeigen-g/160-PATTERNS.md

<interfaces>
`AnimeDetail` (`frontend/src/types/anime.ts:62-63`): `genres?: string[]; tags?: string[]` — UNCHANGED
shape, already present, already German-resolved server-side by plan 160-01 (frontend does zero
language logic).

`page.tsx` (existing, full file 301 lines) exact insertion point: between the `<p
className={styles.description}>` block (lines 188-190) and the `{embySeriesUrl && (...)}` block (line
192) — per CONTEXT.md's discretion note, the Emby link (when present) stays AFTER the Tags block.
`AnimeInfoBanner` renders at line 207 and owns its OWN `<hr>` only when a banner URL exists
(`frontend/src/components/anime/AnimeMediaProvider.tsx:162-179` — `if (!bannerURL) return null`, else
`<><hr .../><Image .../></>`). The Tags block's divider is INDEPENDENT of this — do not modify
`AnimeMediaProvider.tsx`.

Current genre chip block to convert (`page.tsx:154-167`):
```
<div className={styles.genresSection}>
  <span className={styles.genresLabel}>Genres</span>
  <div className={styles.genres}>
    {anime.genres && anime.genres.length > 0 ? (
      anime.genres.map((genre) => (
        <span key={genre} className={styles.genreChip}>{genre}</span>
      ))
    ) : (
      <span className={styles.genreChip}>Anime</span>
    )}
  </div>
</div>
```
Only the `.map()` branch becomes `<Link>`; the `else` placeholder branch STAYS a plain `<span>` (Pitfall
4 — do not link the "Anime" fallback).

Existing real-Link-chip precedent already in this file (`page.tsx:232-243`, fansub chips): `<Link
key={...} href={...} prefetch={false} className={styles.fansubChip}>{name}</Link>` — `Link` is already
imported (`import Link from 'next/link'`, line 3), no new import needed.

CSS analogs in `page.module.css` (existing): `.genreChip` (lines 301-316, subtle rounded chip, no
uppercase — closest to D-17's target look already), `.divider` (lines 458-463, `height: 1px; background:
rgba(255,255,255,0.25); margin: 20px 0;`), `.badge` (lines 373-382, uppercase/CTA look — explicitly
what D-17 says NOT to copy for tags). `frontend/src/styles/globals.css:216-219` provides a global
`:focus-visible { outline: 2px solid var(--focus-outline); outline-offset: 2px; }` baseline (applies
automatically to any new `<Link>`); `frontend/src/app/suche/SearchResults.module.css:29-38`
(`.resultCard:focus-visible { box-shadow: var(--focus-ring); }`) is the richer per-component
focus-treatment precedent to mirror using the same `var(--focus-ring)` design token, per D-17's "globale
Design-Tokens" requirement (page.module.css otherwise uses hardcoded rgba values throughout — legacy,
not to be extended for the NEW rules).

Existing test file conventions (`page.test.tsx`, full file, uses `Children`/`isValidElement` tree
walking over the directly-invoked async Server Component — NOT `@testing-library/react` DOM rendering;
read the full file to reuse its existing element/text-extraction helper style before adding new
assertions).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tags block + genre chips as links</name>
  <files>frontend/src/app/anime/[id]/page.tsx, frontend/src/app/anime/[id]/page.module.css, frontend/src/app/anime/[id]/page.test.tsx</files>
  <read_first>
    frontend/src/app/anime/[id]/page.tsx (full file), frontend/src/app/anime/[id]/page.module.css (full
    file, focus on lines 154-462 covering genresSection/genres/genreChip/badge/description/infoBanner/divider),
    frontend/src/components/anime/AnimeMediaProvider.tsx (AnimeInfoBanner, lines 162-179 — confirms the
    divider-binding behavior, do not modify this file), frontend/src/app/anime/[id]/page.test.tsx (full
    file — existing tree-walking test helper style to extend), frontend/src/app/suche/useDebouncedSearch.ts
    (buildStateQuery lines 119-134 — confirms URLSearchParams is the established encoding mechanism to
    replicate manually here since page.tsx is a Server Component and cannot import this client hook)
  </read_first>
  <behavior>
    - `anime.tags` has multiple entries → a "Tags" heading renders, followed by one `<Link>` chip per
      tag, sorted alphabetically as delivered by the backend (no client-side re-sort needed — verify
      the backend order is trusted, not re-sorted, to avoid double-sorting divergence).
    - `anime.tags` has exactly one entry → the same block renders with one chip (not a degenerate
      "container of zero" state).
    - `anime.tags` is empty or undefined → NO "Tags" heading, NO container element, NO extra spacing
      (D-13) — the DOM has zero trace of the tags feature.
    - A tag named `"Real Robot"` links to `/suche?type=anime&tag=Real+Robot` or
      `/suche?type=anime&tag=Real%20Robot` (URLSearchParams's own encoding — space becomes `+`, which is
      the STANDARD `URLSearchParams.toString()` behavior and must be accepted as correct, since D-09
      mandates reusing the established mechanism, not a specific encoding character).
    - A tag named `"PSI-Kräfte"` links with the umlaut correctly percent-encoded (verify via decoding
      the generated href, not via string equality on the raw encoded form).
    - Each tag chip is a real `<a>`/`Link` element with the tag name as its only accessible text content
      (no extra decoration text), reachable via keyboard (native anchor semantics — no `tabIndex={-1}`
      or click-handler-only implementation).
    - `anime.genres` entries render as `<Link>` elements pointing to `/suche?type=anime&genre=<name>`;
      the "Anime" fallback (when `anime.genres` is empty/undefined) renders as a plain, non-link `<span>`
      exactly as today.
    - The Tags block sits AFTER the description paragraph and BEFORE the `embySeriesUrl` conditional
      block and `AnimeInfoBanner` in DOM order (verify via element sequence in the returned tree, not
      just presence).
    - When `AnimeInfoBanner` has no banner (no bannerURL), there is still visible spacing after the Tags
      block but no duplicate divider line (the Tags block's own `<hr>` renders only when tags exist,
      independent of the banner's own conditional `<hr>`).
  </behavior>
  <action>
    In `page.tsx`, add a helper function (module-level, above the component, or inline) that builds a
    search href from a filter kind (`'tag'` or `'genre'`) and a name via `new URLSearchParams({ type:
    'anime', [kind]: name }).toString()` prefixed with `/suche?`. Insert a new block immediately after
    the `<p className={styles.description}>...</p>` element (currently ending at line 190) and before
    the `{embySeriesUrl && (...)}` block (currently starting at line 192): conditionally render (only
    when `anime.tags && anime.tags.length > 0`) an `<hr className={styles.divider} />` (D-13's own,
    independent divider) followed by a `<div className={styles.tagsSection}>` containing an `<h2
    className={styles.tagsLabel}>Tags</h2>` (exact heading text per D-15) and a `<ul
    className={styles.tagsList}>` where each tag renders as `<li key={tag}><Link
    className={styles.tagChip} href={buildFilterHref('tag', tag)}>{tag}</Link></li>` — trust the
    backend's already-alphabetical order (D-18 is satisfied server-side per plan 160-01, do not
    re-`.sort()` client-side). Use a semantic `<ul>`/`<li>` list (D-16: "Chips stehen in einer Liste,
    die über die Überschrift benannt ist" — associate via `aria-labelledby` linking the `<ul>` to the
    heading's `id`, or wrap both in a `<section aria-label="Tags">`, whichever reads cleaner — Claude's
    discretion on the exact ARIA wiring as long as the list-heading relationship is programmatically
    determinable). Convert the existing `anime.genres.map((genre) => <span ...>)` branch (lines
    157-162) to `anime.genres.map((genre) => <Link key={genre} className={styles.genreChip}
    href={buildFilterHref('genre', genre)}>{genre}</Link>)`; leave the `else` fallback branch (`<span
    className={styles.genreChip}>Anime</span>`) completely untouched (Pitfall 4 — do not link the
    placeholder).

    In `page.module.css`, add `.tagsSection` (block-level container, reasonable top/bottom margin
    matching the surrounding rhythm — inspect `.genresSection`'s spacing for a matching value),
    `.tagsLabel` (small uppercase-or-not label matching `.genresLabel`'s existing treatment — reuse that
    exact look, do not invent a new label style), `.tagsList` (`list-style: none`, `display: flex`,
    `flex-wrap: wrap`, `gap` matching `.genres`'s existing gap value, `padding: 0`, `margin: 0` — D-19's
    "brechen um, keine horizontale Scrollleiste" requirement), and `.tagChip` (base it on `.genreChip`'s
    existing padding/border-radius/background/font-size/transition values but explicitly ensure
    `text-transform` is NOT set to uppercase — mirror the "no CTA style" requirement by keeping the same
    subtle/dezent visual weight as `.genreChip`, add `cursor: pointer` explicitly, and add a
    `.tagChip:focus-visible { outline: none; box-shadow: var(--focus-ring); }` rule using the design
    token from `globals.css`/`SearchResults.module.css`'s precedent, per D-17's "globale Design-Tokens"
    instruction). Add the equivalent `.genreChip:focus-visible { outline: none; box-shadow:
    var(--focus-ring); }` rule so the now-link genre chips get the same richer focus treatment (D-20
    "sichtbaren Fokus").
  </action>
  <verify>
    <automated>cd frontend && npx vitest run "src/app/anime/[id]/page.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run "src/app/anime/[id]/page.test.tsx"` passes, including all pre-existing cases
      unchanged (no regression to description/banner/relations/metadata-badge rendering).
    - New test cases pass for: multiple tags, exactly one tag, zero/undefined tags (no heading, no
      container element present at all), special-character tag names ("Real Robot", "PSI-Kräfte") with
      hrefs that decode back to the exact original name via `decodeURIComponent`/`URLSearchParams`
      parsing (not raw string comparison against a hand-written encoded literal), DOM order (Tags block
      before Emby link / AnimeInfoBanner, after description), genre chips are `<a>`/`Link` elements with
      `href` containing `type=anime&genre=`, and the "Anime" placeholder chip has NO `href` attribute.
    - `grep -n "Tags</h2>\|>Tags<" "frontend/src/app/anime/[id]/page.tsx"` matches the literal heading
      text "Tags".
    - `grep -n "URLSearchParams" "frontend/src/app/anime/[id]/page.tsx"` matches (confirms D-09's
      established-mechanism reuse, not hand-rolled encoding).
    - `grep -n "<input\b\|<select\b\|<textarea\b" "frontend/src/app/anime/[id]/page.tsx"` returns no
      matches introduced by this change (no native form elements — not directly relevant here but
      confirms no accidental scope creep).
  </acceptance_criteria>
  <done>The Tags block renders per D-12–D-19 with correctly encoded links, and genre chips are real links per D-20, all proven by an extended, executing component test.</done>
</task>

<task type="auto">
  <name>Task 2: Regression guards — performance budget and data-shape tests</name>
  <files>frontend/src/app/anime/[id]/page.performance.test.ts, frontend/src/app/anime/[id]/animeDetailData.test.ts</files>
  <read_first>
    frontend/src/app/anime/[id]/page.performance.test.ts (full file — existing fetch/query-count
    assertions), frontend/src/app/anime/[id]/animeDetailData.test.ts (full file — existing
    loadAnimeDetail shape assertions), frontend/src/app/anime/[id]/page.tsx (as modified by Task 1 —
    confirm the Tags block and genre-link conversion read exclusively from the already-fetched `anime`
    prop, introducing zero new fetch/network calls)
  </read_first>
  <behavior>
    - The existing SSR fetch/request-count budget in `page.performance.test.ts` is unchanged by the Tags
      block and genre-link conversion (both read from data already present on the `anime` object; no
      new `fetch`/`getX` calls were introduced by Task 1).
    - `animeDetailData.test.ts`'s assertions about the shape/passthrough of `loadAnimeDetail`'s result
      (including `tags`/`genres`) still pass unchanged, confirming no accidental type or fetch-path
      regression from plan 160-01's backend changes leaking into this data-loading layer.
  </behavior>
  <action>
    Run both existing test files as-is first to confirm they still pass after Task 1's `page.tsx`
    changes (they should, since Task 1 introduces no new data fetching). If either file's existing
    assertions enumerate the exact set of rendered sections/elements in a way that would need updating
    to account for the new Tags block or the genre-chip-to-Link conversion, update those specific
    assertions minimally to match the new structure — do not weaken or remove any existing budget/shape
    assertion in the process. Add one explicit regression assertion to `page.performance.test.ts`
    confirming the total counted fetch/query calls for a detail page WITH tags present is identical to
    the count for a detail page WITHOUT tags (proves the Tags block adds zero network cost, consistent
    with CONTEXT.md's "ohne zusätzliche Anfrage" discretion note carried through from the backend).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run "src/app/anime/[id]/page.performance.test.ts" "src/app/anime/[id]/animeDetailData.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - Both test files pass.
    - The new fetch-count-parity assertion (with tags vs. without tags) passes, proving zero added
      network cost from the Tags block.
    - No existing assertion in either file was deleted or weakened to make the suite pass (verify via
      `git diff` showing only additive or minimally-adjusted-for-structure changes, not deletions of
      coverage).
  </acceptance_criteria>
  <done>Performance budget and data-shape regression coverage confirms the Tags block and genre-link conversion introduce zero new network cost and no data-shape drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anime detail SSR → rendered tag/genre chip text and hrefs | Server-resolved German/base names rendered as link text and query-string values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-11 | Tampering / Information Disclosure | Tag/genre name rendered as link text and query value | accept | React auto-escapes all text children; no `dangerouslySetInnerHTML` anywhere in this file; `URLSearchParams` handles encoding, no hand-rolled string concatenation into the href |
</threat_model>

<verification>
`npx vitest run "src/app/anime/[id]/page.test.tsx" "src/app/anime/[id]/page.performance.test.ts" "src/app/anime/[id]/animeDetailData.test.ts"` all pass.
</verification>

<success_criteria>
The public anime detail page shows a correctly positioned, correctly labeled, correctly styled,
correctly linked Tags block (D-12–D-19), and genre chips are real links (D-20), with zero added network
cost and no regression to existing description/banner/relations/badge rendering.
</success_criteria>

<output>
Create `.planning/phases/160-oeffentliche-anime-detailseite-nachschaerfen-tags-anzeigen-g/160-06-SUMMARY.md` when done
</output>
