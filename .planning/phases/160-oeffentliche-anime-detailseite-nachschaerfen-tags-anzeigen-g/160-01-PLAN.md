---
phase: 160-oeffentliche-anime-detailseite-nachschaerfen-tags-anzeigen-g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/0168_tag_genre_language_names.up.sql
  - database/migrations/0168_tag_genre_language_names.down.sql
  - backend/internal/repository/anime_metadata.go
  - backend/internal/repository/anime_public_read_integration_test.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-01: Mehrsprachigkeit (Tag-/Genre-Sprachnamen) ist als eigener, vorgezogener Datenmodell-Schritt umgesetzt, bevor die öffentliche Anzeige darauf aufbaut."
    - "D-02: Sprachnamen für Tags und Genres werden nach dem anime_titles-Muster gespeichert — je eine Tabelle 'Tag + Sprache + Name' (tag_names) und 'Genre + Sprache + Name' (genre_names) über die bestehende languages-Tabelle, keine festen Spalten je Sprache."
    - "D-03: tags.name und genres.name bleiben unverändert der eindeutige Grundname; bestehende Tag-/Genre-Zeilen werden von der Migration nicht verändert oder automatisch einer Sprache zugeordnet."
    - "D-06: Die öffentliche Detail-Leseabfrage (loadNormalizedAnimeMetadata) liefert den deutschen Namen, wenn einer existiert, sonst den Grundnamen — ohne Sprachumschalter, ohne dass Tags/Genres ohne deutschen Namen verschwinden."
    - "D-07: Genres erhalten exakt dasselbe Mehrsprachen-Datenmodell (genre_names) wie Tags (tag_names)."
  artifacts:
    - path: "database/migrations/0168_tag_genre_language_names.up.sql"
      provides: "tag_names und genre_names Tabellen (id, tag_id/genre_id FK, language_id FK, name, created_at, updated_at, UNIQUE(tag_id/genre_id, language_id))"
    - path: "database/migrations/0168_tag_genre_language_names.down.sql"
      provides: "Rollback: DROP TABLE IF EXISTS tag_names, genre_names"
    - path: "backend/internal/repository/anime_metadata.go"
      provides: "loadNormalizedAnimeMetadata liest COALESCE(deutscher Name, Grundname) für Genres und Tags über LEFT JOIN, ohne die Statement-Anzahl zu erhöhen"
  key_links:
    - from: "backend/internal/repository/anime_metadata.go"
      to: "genre_names / tag_names (language_id = languages.code='de')"
      via: "LEFT JOIN ... AND language_id = (SELECT id FROM languages WHERE code = 'de')"
      pattern: "LEFT JOIN (genre_names|tag_names)"
    - from: "backend/internal/repository/anime_public_read_integration_test.go"
      to: "backend/internal/repository/anime_metadata.go"
      via: "TestAnimePublicReadDetailStoredSlugAndSQLBudget asserts len(queries) == 7 and resolved German names"
      pattern: "len\\(queries\\) != 7"
---

<objective>
Lay the foundational data model for multilingual (German-first) tag and genre display names, and wire
the public anime-detail read path to resolve them — WITHOUT changing the SQL statement budget of the
existing public detail read.

Purpose: This is Layer 1 (Mehrsprachigkeit) of the mandatory three-layer build order for the "Tags und
Genres" sub-step of Phase 160 (see 160-CONTEXT.md D-01–D-07). Layer 2 (search, plan 160-04) and Layer 3
(public display, plan 160-06) both depend on this plan's tables and resolved-name query existing first.
Output: Migration 0168 (tag_names/genre_names tables) + `loadNormalizedAnimeMetadata` extended to
COALESCE the German name over the base name, proven by an extended
`TestAnimePublicReadDetailStoredSlugAndSQLBudget` that still asserts exactly 7 SQL statements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/160-oeffentliche-anime-detailseite-nachschaerfen-tags-anzeigen-g/160-CONTEXT.md
@.planning/phases/160-oeffentliche-anime-detailseite-nachschaerfen-tags-anzeigen-g/160-RESEARCH.md
@.planning/phases/160-oeffentliche-anime-detailseite-nachschaerfen-tags-anzeigen-g/160-PATTERNS.md

<interfaces>
Existing minimal fixture schema in `anime_public_read_integration_test.go` (`openAnimePublicReadFixture`,
lines 1-100) already declares `CREATE TABLE genres (id BIGINT PRIMARY KEY, name TEXT);`,
`CREATE TABLE tags (id BIGINT PRIMARY KEY, name TEXT);`, `CREATE TABLE anime_genres (anime_id BIGINT,
genre_id BIGINT);`, `CREATE TABLE anime_tags (anime_id BIGINT, tag_id BIGINT);`, and
`CREATE TABLE languages (id BIGINT PRIMARY KEY, code TEXT);` with only `INSERT INTO languages VALUES
(1, 'ja');` seeded. This fixture is a standalone inline minimal schema (NOT the real migrations) — it
must be extended in place, not replaced.

`loadNormalizedAnimeMetadata` (backend/internal/repository/anime_metadata.go:38-135) issues exactly
THREE `db.Query` calls (titles, genres, tags) inside `AnimeRepository.GetByID`, which together with
schema/episode/relation reads make up the 7-statement budget asserted at
`anime_public_read_integration_test.go:255` (`len(queries) != 7`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 0168 — tag_names and genre_names tables</name>
  <files>database/migrations/0168_tag_genre_language_names.up.sql, database/migrations/0168_tag_genre_language_names.down.sql</files>
  <read_first>
    database/migrations/0021_add_normalized_metadata_tables.up.sql (anime_titles pattern — the D-02 template, same shape minus title_type_id),
    database/migrations/0021_add_normalized_metadata_tables.down.sql (down-file convention),
    database/migrations/0167_episode_type_source.up.sql and .down.sql (most recent migration — numbering, header-comment, German-comment convention),
    database/migrations/0020_add_metadata_reference_tables.up.sql (confirms `languages` already seeds `('de', 'German')` — no languages seed migration needed)
  </read_first>
  <action>
    Create `database/migrations/0168_tag_genre_language_names.up.sql` with a header comment in the
    established German style (mirror 0167's "-- Migration NNNN: Beschreibung." convention) explaining
    this adds per-language display names for tags and genres (D-02), mirroring `anime_titles` but
    without a `title_type_id` equivalent since each tag/genre has exactly one name per language, per
    D-03. Create two tables: `tag_names` (columns: `id BIGSERIAL PRIMARY KEY`, `tag_id BIGINT NOT NULL
    REFERENCES tags(id) ON DELETE CASCADE`, `language_id BIGINT NOT NULL REFERENCES languages(id)`,
    `name TEXT NOT NULL`, `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`, `updated_at TIMESTAMPTZ NOT
    NULL DEFAULT NOW()`, constraint `uq_tag_name_language UNIQUE (tag_id, language_id)`) and
    `genre_names` (identical shape with `genre_id BIGINT NOT NULL REFERENCES genres(id) ON DELETE
    CASCADE` and constraint `uq_genre_name_language UNIQUE (genre_id, language_id)`). Add
    `CREATE INDEX IF NOT EXISTS idx_tag_names_tag ON tag_names(tag_id)` and
    `CREATE INDEX IF NOT EXISTS idx_genre_names_genre ON genre_names(genre_id)`. Use
    `CREATE TABLE IF NOT EXISTS` for both tables, consistent with every existing migration in this
    repo. Do NOT insert any rows (D-03: no automatic language assignment of existing tag/genre data).
    Create `database/migrations/0168_tag_genre_language_names.down.sql` with
    `DROP TABLE IF EXISTS tag_names;` and `DROP TABLE IF EXISTS genre_names;` (indexes drop
    automatically with their tables).
  </action>
  <verify>
    <automated>ls database/migrations/0168_tag_genre_language_names.up.sql database/migrations/0168_tag_genre_language_names.down.sql</automated>
  </verify>
  <acceptance_criteria>
    - `database/migrations/0168_tag_genre_language_names.up.sql` exists and contains
      `CREATE TABLE IF NOT EXISTS tag_names` and `CREATE TABLE IF NOT EXISTS genre_names`.
    - The up file contains exactly two `UNIQUE` constraints named `uq_tag_name_language` and
      `uq_genre_name_language`.
    - `database/migrations/0168_tag_genre_language_names.down.sql` contains
      `DROP TABLE IF EXISTS tag_names` and `DROP TABLE IF EXISTS genre_names`.
    - Running the project's migration tool (`backend/cmd/migrate`, e.g. via
      `docker compose exec backend go run ./cmd/migrate up` per CLAUDE.md's Docker-only workflow)
      against the dev database applies migration 0168 without error, and
      `docker compose exec backend go run ./cmd/migrate down` followed by `up` round-trips cleanly.
    - `psql`-level inspection (`\d tag_names`, `\d genre_names`) shows both foreign keys and both
      unique constraints present.
  </acceptance_criteria>
  <done>Migration 0168 exists, applies cleanly forward and backward, and matches the anime_titles-style shape required by D-02/D-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve German tag/genre names in the public detail read without adding a query</name>
  <files>backend/internal/repository/anime_metadata.go, backend/internal/repository/anime_public_read_integration_test.go</files>
  <read_first>
    backend/internal/repository/anime_metadata.go (current genre SELECT lines 67-77, tag SELECT lines
    98-108, and the surrounding loadNormalizedAnimeMetadata/mergeNormalizedAnimeMetadata/uniqueSortedGenres functions — read the WHOLE file to understand that dedup/alphabetical sort
    already happens in Go on whatever string values are scanned from SQL),
    backend/internal/repository/anime_public_read_integration_test.go (full file — the
    openAnimePublicReadFixture inline schema lines 1-100, and TestAnimePublicReadDetailStoredSlugAndSQLBudget lines 225-281 which is the SQL-budget assertion to extend, not break),
    database/migrations/0168_tag_genre_language_names.up.sql (just created in Task 1 — use its exact
    table/column names: tag_names(tag_id, language_id, name), genre_names(genre_id, language_id, name))
  </read_first>
  <behavior>
    - Given a genre linked to an anime that HAS a genre_names row for language code 'de', loadNormalizedAnimeMetadata returns that German name in place of genres.name.
    - Given a genre linked to an anime that has NO genre_names row for 'de', loadNormalizedAnimeMetadata falls back to the base genres.name (D-06: "Tags/Genres ohne deutschen Namen bleiben sichtbar").
    - Identical behavior for tags via tag_names.
    - AnimeRepository.GetByID's total traced SQL statement count for the detail read stays exactly 7 (no new db.Query call added — Pitfall 2).
  </behavior>
  <action>
    In `anime_metadata.go`, change the genre SELECT (currently `SELECT g.name FROM anime_genres ag JOIN
    genres g ON g.id = ag.genre_id WHERE ag.anime_id = $1 ORDER BY g.name ASC`) to select
    `COALESCE(gn.name, g.name)` instead of `g.name`, adding `LEFT JOIN genre_names gn ON gn.genre_id =
    g.id AND gn.language_id = (SELECT id FROM languages WHERE code = 'de')` before the `WHERE` clause.
    Keep `ORDER BY g.name ASC` unchanged (it only affects tie-breaking; final alphabetical order by
    displayed name is already produced by `uniqueSortedGenres`'s `sort.Strings` in Go — D-18 is
    satisfied automatically once the COALESCEd name is what gets scanned). Apply the identical
    transformation to the tag SELECT using `tag_names tn`/`tn.tag_id = t.id`. Do not touch
    `mergeNormalizedAnimeMetadata`, `uniqueSortedGenres`, `pickNormalizedTitle`, or the
    `normalizedAnimeMetadata` struct — genres/tags remain `[]string` of already-resolved display names.
    Do not add a fourth `db.Query` call.

    In `anime_public_read_integration_test.go`'s `openAnimePublicReadFixture`, extend the inline schema
    SQL string: add `CREATE TABLE tag_names (id BIGINT PRIMARY KEY, tag_id BIGINT, language_id BIGINT,
    name TEXT);` and `CREATE TABLE genre_names (id BIGINT PRIMARY KEY, genre_id BIGINT, language_id
    BIGINT, name TEXT);`. Add `INSERT INTO languages VALUES (2, 'de');` (the fixture currently only
    seeds id=1 code='ja'). Add genre/tag fixture rows for anime id=1: two genres (one WITH a German
    name row, one WITHOUT) and two tags (one WITH, one WITHOUT), e.g. `INSERT INTO genres VALUES (1,
    'Action'), (2, 'Comedy'); INSERT INTO anime_genres VALUES (1, 1), (1, 2); INSERT INTO genre_names
    VALUES (1, 1, 2, 'Aktion');` (only genre 1 gets a German translation; genre 2 stays untranslated to
    prove the COALESCE fallback branch) and the mirrored pattern for `tags`/`anime_tags`/`tag_names`.
    Extend `TestAnimePublicReadDetailStoredSlugAndSQLBudget` (or add a focused subtest inside the same
    function) to assert `detail.Genres` contains `"Aktion"` (not `"Action"`) and `"Comedy"` (untranslated
    genre keeps its base name), with the equivalent assertion for `detail.Tags`, while leaving the
    `len(queries) != 7` assertion completely unchanged.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/repository/... -run TestAnimePublicReadDetailStoredSlugAndSQLBudget -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/repository/... -run TestAnimePublicReadDetailStoredSlugAndSQLBudget -v` passes.
    - Test log output shows `detail SQL statements = 7` (unchanged budget).
    - The new German-name assertions fail if the LEFT JOIN/COALESCE change is reverted (proves the test
      is not a tautology — verify by temporarily checking the assertion targets the resolved value, not
      the base value).
    - `grep -c "LEFT JOIN genre_names" backend/internal/repository/anime_metadata.go` equals 1.
    - `grep -c "LEFT JOIN tag_names" backend/internal/repository/anime_metadata.go` equals 1.
    - `grep -c "db.Query(" backend/internal/repository/anime_metadata.go` stays at 3 (titles, genres,
      tags — no fourth query introduced).
  </acceptance_criteria>
  <done>loadNormalizedAnimeMetadata resolves German tag/genre names via COALESCE+LEFT JOIN with zero new queries, proven by the extended SQL-budget integration test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public HTTP → AnimeRepository.GetByID | Unauthenticated read of anime detail including resolved tag/genre names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-01 | Tampering | anime_metadata.go LEFT JOIN | accept | No user-controlled input enters this query path (language_id resolved via a fixed subquery on a static code literal 'de'); values already parameterized upstream via animeID bind |
| T-160-02 | Information Disclosure | tag_names/genre_names schema | accept | Purely additive, no PII; German names are curated admin content, not user-submitted |
</threat_model>

<verification>
`go test ./internal/repository/... -run TestAnimePublicReadDetailStoredSlugAndSQLBudget -v` passes with
`detail SQL statements = 7`. Migration 0168 applies and rolls back cleanly against the dev database.
</verification>

<success_criteria>
tag_names/genre_names tables exist per D-02/D-03. The public anime detail read resolves German names
via COALESCE with zero added SQL statements, falling back to the base name when no German translation
exists (D-06), for both tags and genres (D-07).
</success_criteria>

<output>
Create `.planning/phases/160-oeffentliche-anime-detailseite-nachschaerfen-tags-anzeigen-g/160-01-SUMMARY.md` when done
</output>
