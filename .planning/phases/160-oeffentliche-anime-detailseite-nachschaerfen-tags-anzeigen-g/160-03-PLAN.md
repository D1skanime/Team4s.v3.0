---
phase: 160-oeffentliche-anime-detailseite-nachschaerfen-tags-anzeigen-g
plan: 03
type: execute
wave: 3
depends_on: ["160-02"]
files_modified:
  - frontend/src/types/admin.ts
  - frontend/src/lib/api.ts
  - frontend/src/app/admin/tags-genres/page.tsx
  - frontend/src/app/admin/tags-genres/TagsGenresAdminClient.tsx
  - frontend/src/app/admin/tags-genres/TagsGenresAdminClient.test.tsx
  - frontend/src/app/admin/page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-04: Admins pflegen die deutschen Tag-/Genre-Namen auf einer eigenen Admin-Seite über eine Liste mit Grundname, Nutzungsanzahl und einem Namensfeld je Sprache (jetzt Deutsch); die Übersetzung gilt global für alle Anime."
    - "D-07: Genres erhalten dieselbe Pflege-Oberfläche wie Tags (dieselbe Seite, gleiches Interaktionsmuster)."
  artifacts:
    - path: "frontend/src/app/admin/tags-genres/page.tsx"
      provides: "PlatformAdminGate-geschützte Route /admin/tags-genres"
    - path: "frontend/src/app/admin/tags-genres/TagsGenresAdminClient.tsx"
      provides: "Tabellen-Client mit @/components/ui Table/FormField/Input/Button, pro Zeile editierbares deutsches Namensfeld für Tags und Genres"
  key_links:
    - from: "frontend/src/app/admin/tags-genres/TagsGenresAdminClient.tsx"
      to: "frontend/src/lib/api.ts"
      via: "getAdminTagNames / updateAdminTagName / getAdminGenreNames / updateAdminGenreName"
      pattern: "getAdminTagNames|updateAdminTagName|getAdminGenreNames|updateAdminGenreName"
    - from: "frontend/src/app/admin/page.tsx"
      to: "frontend/src/app/admin/tags-genres/page.tsx"
      via: "Link href=\"/admin/tags-genres\""
      pattern: "/admin/tags-genres"
---

<objective>
Build the new admin page for maintaining German tag and genre display names, consuming the endpoints
from plan 160-02, using exclusively `@/components/ui` primitives per CLAUDE.md.

Purpose: Completes Layer 1 (Mehrsprachigkeit) with the D-04/D-07 maintenance surface. This is the last
plan in Layer 1 before Layer 2 (search) and Layer 3 (public display) can be considered fully shippable
end-to-end (though Layer 3's read path already works via plan 160-01's COALESCE fallback regardless of
whether any translations exist yet).
Output: `/admin/tags-genres` route with a list+inline-edit table for both tags and genres, reachable
from the admin overview page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/160-oeffentliche-anime-detailseite-nachschaerfen-tags-anzeigen-g/160-CONTEXT.md
@.planning/phases/160-oeffentliche-anime-detailseite-nachschaerfen-tags-anzeigen-g/160-RESEARCH.md
@.planning/phases/160-oeffentliche-anime-detailseite-nachschaerfen-tags-anzeigen-g/160-PATTERNS.md

<interfaces>
Backend contract from plan 160-02 (already implemented, do not re-derive):
`GET /api/v1/admin/tags/names` → `{"data": [{"id": number, "name": string, "count": number, "name_de":
string|null}]}`. `PATCH /api/v1/admin/tags/:id/names/de` body `{"name": string}` → `{"data": {"id":
number, "name_de": string}}` (empty string clears). Identical shape for
`/api/v1/admin/genres/names` and `/api/v1/admin/genres/:id/names/de`.

From `frontend/src/types/admin.ts` (existing, lines 385-403): `GenreToken`/`TagToken`/
`AdminGenreTokensResponse`/`AdminTagTokensResponse` — existing sibling types to place the new ones near
(do not reuse/mutate these — they lack `id`/`name_de` and are consumed elsewhere for other purposes).

From `frontend/src/lib/api.ts` (existing, lines 589-613, 6440-6512): `getApiBaseUrl()`,
`authorizedFetch(url, {authToken, cache})`, `ApiError`, `parseApiError(response, fallbackMessage)` —
the exact helper set already used by `getAdminGenreTokens`/`getAdminTagTokens` (lines ~6455-6512),
which is the closest existing analog for the two new GET functions and the pattern to copy for the two
new PATCH functions (PATCH needs `method: "PATCH"`, `headers: {"Content-Type": "application/json"}`,
`body: JSON.stringify({name})`).

From `frontend/src/app/admin/groups/page.tsx` + `AdminGroupsClient.tsx` (existing): the
`page.tsx` → `<PlatformAdminGate><main><XClient /></main></PlatformAdminGate>` wrapper pattern, and the
`'use client'` + `@/components/ui` (`Table`, `TableBody`, `TableCell`, `TableHead`, `TableHeaderCell`,
`TableRow`, `Button`, `Input`, `EmptyState`, `ErrorState`, `LoadingState`) import set for the client
component shell.

From `frontend/src/app/admin/fansubs/[id]/edit/FansubCommunityLinksList.tsx` (existing, lines 79-101):
the per-row `FormField`+`Input` state-update-by-key mechanics — bind an `Input`'s `value`/`onChange` to
a local array keyed by row id via `setRows((current) => current.map((item) => item.id === row.id ? {
...item, nameDE: event.target.value } : item))` — adapt this to persist via an individual PATCH call
(e.g. on blur, or an explicit small `Button` per row) rather than one bulk parent-form submit, since
each row is an independent global resource (NOT a sub-resource of one parent form like the fansub links
are).

Explicit anti-pattern (do NOT copy): `frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditGenreSection.tsx`
uses a raw native `<input>` (predates the `no-restricted-syntax` ESLint rule) — legacy debt, not a
pattern to extend, per CLAUDE.md's `@/components/ui` mandate overriding "closest analog" local
consistency.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frontend contracts — types and API helper functions</name>
  <files>frontend/src/types/admin.ts, frontend/src/lib/api.ts</files>
  <read_first>
    frontend/src/types/admin.ts lines 375-404 (GenreToken/TagToken/AdminGenreTokensResponse/AdminTagTokensResponse — sibling shapes and placement),
    frontend/src/lib/api.ts lines 6440-6512 (getAdminGenreTokens/getAdminTagTokens — exact fetch/auth/error-handling pattern to mirror)
  </read_first>
  <action>
    In `frontend/src/types/admin.ts`, add near the existing `GenreToken`/`TagToken` block: `interface
    AdminTagNameRow { id: number; name: string; count: number; name_de: string | null }`, `interface
    AdminTagNamesResponse { data: AdminTagNameRow[] }`, `interface AdminGenreNameRow { id: number; name:
    string; count: number; name_de: string | null }`, `interface AdminGenreNamesResponse { data:
    AdminGenreNameRow[] }`, and `interface AdminUpsertNameResponse { data: { id: number; name_de:
    string } }` (shared response shape for both PATCH endpoints). In `frontend/src/lib/api.ts`, add
    four new exported async functions near `getAdminTagTokens` (~line 6487): `getAdminTagNames(authToken?:
    string): Promise<AdminTagNamesResponse>` (GET `${API_BASE_URL}/api/v1/admin/tags/names` via
    `authorizedFetch`, same error-throwing shape as `getAdminTagTokens`), `updateAdminTagName(tagId:
    number, name: string, authToken?: string): Promise<AdminUpsertNameResponse>` (PATCH
    `${API_BASE_URL}/api/v1/admin/tags/${tagId}/names/de` via `authorizedFetch` with
    `{method: "PATCH", headers: {"Content-Type": "application/json"}, body: JSON.stringify({ name })}`),
    and the genre mirrors `getAdminGenreNames`/`updateAdminGenreName` against
    `/api/v1/admin/genres/names` and `/api/v1/admin/genres/${genreId}/names/de`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "admin.ts\|lib/api.ts" || echo "no type errors in touched files"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "interface AdminTagNameRow" frontend/src/types/admin.ts` matches.
    - `grep -n "interface AdminGenreNameRow" frontend/src/types/admin.ts` matches.
    - `grep -n "export async function getAdminTagNames" frontend/src/lib/api.ts` matches.
    - `grep -n "export async function updateAdminTagName" frontend/src/lib/api.ts` matches.
    - `grep -n "export async function getAdminGenreNames" frontend/src/lib/api.ts` matches.
    - `grep -n "export async function updateAdminGenreName" frontend/src/lib/api.ts` matches.
    - `cd frontend && npx tsc --noEmit` reports no new errors attributable to these two files.
  </acceptance_criteria>
  <done>Types and API helper functions exist as the contract the client component (Task 2) will consume.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TagsGenresAdminClient — list + inline German name editing</name>
  <files>frontend/src/app/admin/tags-genres/TagsGenresAdminClient.tsx, frontend/src/app/admin/tags-genres/TagsGenresAdminClient.test.tsx</files>
  <read_first>
    frontend/src/app/admin/groups/AdminGroupsClient.tsx (Table shell + @/components/ui import set to
    mirror), frontend/src/app/admin/fansubs/[id]/edit/FansubCommunityLinksList.tsx (per-row
    FormField/Input state-update-by-key mechanics, lines 79-101), frontend/src/types/admin.ts (the new
    types from Task 1), frontend/src/lib/api.ts (the new helper functions from Task 1)
  </read_first>
  <behavior>
    - On mount, fetches both tag names and genre names and renders two sections/tabs (or two stacked
      tables), each row showing base name, usage count, and an editable German-name `Input`.
    - Editing a row's Input and confirming (blur or an explicit small `Button`) calls
      `updateAdminTagName`/`updateAdminGenreName` for that row's id only — other rows are untouched.
    - Clearing a row's Input to empty and confirming calls the update function with an empty string
      (clears the translation) and the row then displays no German name.
    - A failed list fetch renders `ErrorState` (from `@/components/ui`) with a retry action, not a raw
      thrown error.
    - An empty tag or genre list renders `EmptyState` (from `@/components/ui`), not an empty native
      table body.
    - No native `<input>`, `<select>`, `<textarea>`, or `<button>` elements are used anywhere in this
      component — exclusively `@/components/ui` primitives (`Table`, `TableBody`, `TableCell`,
      `TableHead`, `TableHeaderCell`, `TableRow`, `FormField`, `Input`, `Button`, `EmptyState`,
      `ErrorState`, `LoadingState`).
  </behavior>
  <action>
    Create `'use client'` component `TagsGenresAdminClient` in
    `frontend/src/app/admin/tags-genres/TagsGenresAdminClient.tsx`. On mount (`useEffect`), call
    `getAdminTagNames()` and `getAdminGenreNames()`, track loading/error/data state per list
    independently (or combined — Claude's discretion per RESEARCH.md, keep it simple). Render a `Table`
    per entity type (tags, genres) with columns Grundname / Nutzungsanzahl / Deutscher Name, one
    `TableRow` per item, the German-name cell containing a `FormField`+`Input` bound to local editable
    state keyed by row id (mirror the `setLinks((current) => current.map(...))` pattern from
    `FansubCommunityLinksList.tsx`), persisting via `updateAdminTagName`/`updateAdminGenreName` on
    `onBlur` (simplest reliable per-row-save trigger — do not require a full-page bulk submit). On a
    successful PATCH, update local state with the server's returned `name_de` value (source of truth
    after write). Use `ErrorState`/`EmptyState`/`LoadingState` from `@/components/ui` for the
    loading/error/empty branches, consistent with `SearchResults.tsx`'s usage of the same primitives.
    Do not introduce any native form elements.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/admin/tags-genres/TagsGenresAdminClient.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/app/admin/tags-genres/TagsGenresAdminClient.test.tsx` passes.
    - Test asserts: rows render both the tag list and genre list from mocked
      `getAdminTagNames`/`getAdminGenreNames`; editing one row's German-name input and blurring calls
      `updateAdminTagName` with that row's id and the new value, and does NOT call it for any other row;
      clearing a row's input and blurring calls the update function with an empty string; a rejected
      list fetch renders the `ErrorState` retry affordance; an empty list renders `EmptyState`.
    - `grep -n "<input\b\|<select\b\|<textarea\b\|<button\b" frontend/src/app/admin/tags-genres/TagsGenresAdminClient.tsx` returns no matches (native elements forbidden).
  </acceptance_criteria>
  <done>TagsGenresAdminClient lists and persists German tag/genre names per row using only @/components/ui primitives, proven by an executing Vitest suite.</done>
</task>

<task type="auto">
  <name>Task 3: Route wrapper and admin overview navigation entry</name>
  <files>frontend/src/app/admin/tags-genres/page.tsx, frontend/src/app/admin/page.tsx</files>
  <read_first>
    frontend/src/app/admin/groups/page.tsx (exact PlatformAdminGate wrapper pattern to copy),
    frontend/src/app/admin/page.tsx (existing card-menu structure — Bereiche section, lines 32-55)
  </read_first>
  <action>
    Create `frontend/src/app/admin/tags-genres/page.tsx` mirroring `admin/groups/page.tsx` exactly:
    `export const dynamic = 'force-dynamic'`, default export wraps
    `PlatformAdminGate` around a `main` containing `TagsGenresAdminClient`. In
    `frontend/src/app/admin/page.tsx`, add one new secondary-style `Link` to `/admin/tags-genres` with
    the visible label "Tags und Genres" inside the existing actions block (alongside the existing
    `/admin/fansubs` and `/admin/episodes` secondary links), satisfying CONTEXT.md's
    "Navigation/Platzierung ... Claude's Discretion" note with the lowest-risk, already-established
    placement.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/admin/tags-genres/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PlatformAdminGate" frontend/src/app/admin/tags-genres/page.tsx` matches.
    - `grep -n "TagsGenresAdminClient" frontend/src/app/admin/tags-genres/page.tsx` matches.
    - `grep -n "/admin/tags-genres" frontend/src/app/admin/page.tsx` matches.
    - `grep -n "Tags und Genres" frontend/src/app/admin/page.tsx` matches (real German umlaut-safe
      label per CLAUDE.md Sprachqualität — no umlauts needed here but verify no ASCII substitution
      elsewhere in the added text).
    - `cd frontend && npx vitest run src/app/admin/tags-genres/` passes (no regression in Task 2's suite).
  </acceptance_criteria>
  <done>/admin/tags-genres is a reachable, gated route with a discoverable entry point from the admin overview page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → new admin page | Same authenticated admin boundary as the rest of `/admin/*`, no new boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-06 | Elevation of Privilege | /admin/tags-genres route | mitigate | Wrapped in `PlatformAdminGate`, identical to every other `/admin/*` route in this codebase |
| T-160-07 | Tampering | German-name input values | accept | React auto-escapes all rendered text; input values flow to the backend as `$n` bind parameters (mitigated server-side in plan 160-02) |
</threat_model>

<verification>
`npx vitest run src/app/admin/tags-genres/` passes. The route is reachable via a link from
`/admin/page.tsx` and gated by `PlatformAdminGate`.
</verification>

<success_criteria>
Admins can view and edit German tag and genre names globally from a single, discoverable,
`@/components/ui`-only admin page (D-04, D-07).
</success_criteria>

<output>
Create `.planning/phases/160-oeffentliche-anime-detailseite-nachschaerfen-tags-anzeigen-g/160-03-SUMMARY.md` when done
</output>
