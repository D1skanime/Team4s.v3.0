---
phase: 160-oeffentliche-anime-detailseite-nachschaerfen-tags-anzeigen-g
plan: 05
type: execute
wave: 3
depends_on: ["160-04"]
files_modified:
  - frontend/src/app/suche/useDebouncedSearch.ts
  - frontend/src/app/suche/useDebouncedSearch.test.tsx
  - frontend/src/app/suche/SearchResults.tsx
  - frontend/src/app/suche/SearchResults.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-08: Die Suchergebnisfläche feuert und rendert Ergebnisse, wenn tag oder genre in der URL gesetzt ist, auch ohne q — das UX-seitige Gegenstück zur Backend-Ausnahme aus Plan 160-04. Für alle anderen Filter (ohne tag/genre) bleibt der bestehende q>=2-Leerzustand unverändert."
    - "D-11: Der aktive Filter bleibt ausschließlich über das bestehende, vorbelegte Filterfeld sichtbar; es wird keine neue Hinweis-UI für den Nur-Filter-Modus eingeführt."
  artifacts:
    - path: "frontend/src/app/suche/useDebouncedSearch.ts"
      provides: "MIN_QUERY_LENGTH-Gate mit additivem tag/genre-Bypass NUR für die Ergebnissuche, Vorschläge bleiben unverändert an MIN_QUERY_LENGTH gebunden"
    - path: "frontend/src/app/suche/SearchResults.tsx"
      provides: "Leerzustand-Gate spiegelt denselben Bypass — kein 'Wonach suchst du?' mehr, wenn tag/genre gesetzt ist"
  key_links:
    - from: "frontend/src/app/suche/SearchResults.tsx"
      to: "frontend/src/app/suche/useDebouncedSearch.ts"
      via: "const { filters } = useDebouncedSearch({ role: 'results' })"
      pattern: "filters\\.tag|filters\\.genre"
---

<objective>
Mirror the backend's D-08 q-required bypass on the frontend: the search-results hook must still fire a
request, and the results panel must still render (instead of showing the "Wonach suchst du?" empty
state), when `tag` or `genre` is set in the URL even though `q` is empty.

Purpose: Completes Layer 2 (Nur-Filter-Suche). This is a UX nicety / defense-in-depth mirror of plan
160-04's authoritative backend gate (per RESEARCH.md's Architectural Responsibility Map) — without it,
the tag/genre chip links that plan 160-06 will generate would land on a dead "type something" screen
even though the backend would happily answer.
Output: `useDebouncedSearch.ts`'s length guard and `SearchResults.tsx`'s empty-state guard both extended
with an additive `!filters.tag && !filters.genre` condition, scoped ONLY to the results-fetch path (not
autocomplete suggestions, which are unaffected by D-08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/160-oeffentliche-anime-detailseite-nachschaerfen-tags-anzeigen-g/160-CONTEXT.md
@.planning/phases/160-oeffentliche-anime-detailseite-nachschaerfen-tags-anzeigen-g/160-RESEARCH.md
@.planning/phases/160-oeffentliche-anime-detailseite-nachschaerfen-tags-anzeigen-g/160-PATTERNS.md

<interfaces>
`useDebouncedSearch.ts` (existing, full file 285 lines): `MIN_QUERY_LENGTH = 2` (line 18),
`SearchFilters` interface (lines 26-34) already has `genre?: string; tag?: string`, `readSearchState`
(lines 85-100) already reconstructs `filters` from the URL (`genre`/`tag` query params) as the URL
source of truth — no new URL-parsing logic needed, the values already flow into `state.filters`. The
debounced effect (lines 200-262) has a SINGLE early-return guard at lines 206-217
(`if (trimmed.length < MIN_QUERY_LENGTH) { ...abort everything...; return }`) that currently gates BOTH
the results fetch (`fetchResults` block, lines 220-238) AND the suggestions fetch (`fetchSuggestions`
block, lines 241-258) — these must be decoupled: the results-fetch bypass must NOT also bypass the
suggestions minimum length, since D-08 only applies to `/api/v1/search`, not
`/api/v1/search/suggestions` (that endpoint's own 400 gate is untouched by plan 160-04).

`SearchResults.tsx` (existing, full file 252 lines): `const { q, type, page, results, meta, isLoading,
error, setType, setPage } = useDebouncedSearch({ role: 'results' })` (line 110) does NOT currently
destructure `filters` — it is already returned by the hook via `...state` in the hook's return object
(line 273), just not consumed here yet. The empty-state gate is `if (trimmedQuery.length <
MIN_QUERY_LENGTH) { return <EmptyState title="Wonach suchst du?" .../> }` (lines 119-128). Since this
component always uses `role: 'results'`, `fetchSuggestions` is always `false` for this hook instance —
no suggestions-coupling risk to worry about here specifically, but the hook-level fix in Task 1 must
still be correct in isolation since `useDebouncedSearch` is also used with `role: 'input'`/`'full'`
elsewhere.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additive tag/genre bypass in useDebouncedSearch, scoped to results only</name>
  <files>frontend/src/app/suche/useDebouncedSearch.ts, frontend/src/app/suche/useDebouncedSearch.test.tsx</files>
  <read_first>
    frontend/src/app/suche/useDebouncedSearch.ts (full file — the debounced effect at lines 200-262 in
    particular), frontend/src/app/suche/useDebouncedSearch.test.tsx (existing test structure/mocking
    conventions to extend, not replace)
  </read_first>
  <behavior>
    - `role: 'results'`, `state.q === ''`, `state.filters.tag === 'Amnesia'` → the results fetch
      (`getSearch`) IS called (not aborted/skipped).
    - `role: 'results'`, `state.q === ''`, `state.filters.genre === 'Action'` → the results fetch IS
      called.
    - `role: 'results'`, `state.q === ''`, no tag/genre filter set → the results fetch is still
      SKIPPED, `results`/`meta` cleared, exactly as today (no regression).
    - `role: 'full'`, `state.q === ''`, `state.filters.tag === 'Amnesia'` → the results fetch IS called,
      but the SUGGESTIONS fetch (`getSearchSuggestions`) is NOT called with an empty/too-short query —
      suggestions remain gated at `MIN_QUERY_LENGTH` regardless of tag/genre (D-08 does not extend to
      `/search/suggestions`).
    - `role: 'input'`, `state.q === ''` → unaffected either way (no results fetch in this role at all).
  </behavior>
  <action>
    In the debounced effect, change the single early-return guard (`if (trimmed.length <
    MIN_QUERY_LENGTH) { ...; return }`) so it no longer unconditionally aborts everything. Compute
    `const bypassMinLength = trimmed.length === 0 && Boolean(state.filters.tag || state.filters.genre)`
    (mirror the backend's narrow "absent, not merely short" reading from plan 160-04 — only an EMPTY
    trimmed q bypasses, a present-but-1-character q does not). Restructure so: when `trimmed.length <
    MIN_QUERY_LENGTH && !bypassMinLength`, keep the exact current behavior (abort both controllers,
    clear results/suggestions/meta/error, `setIsLoading(false)`, `return`) — this is the "still blocked"
    path, unchanged for the no-tag/genre case. When the guard does NOT trigger (either because
    `trimmed.length >= MIN_QUERY_LENGTH` OR `bypassMinLength` is true), proceed into the existing
    `if (fetchResults) { ... }` block unchanged. Inside the existing `if (fetchSuggestions) { ... }`
    block, add an explicit `trimmed.length >= MIN_QUERY_LENGTH` condition wrapping the
    `getSearchSuggestions` call (so suggestions never fire for an empty/too-short query even when the
    outer guard was bypassed for tag/genre) — if `fetchSuggestions` is true but the length check fails,
    do nothing (leave suggestions state as-is, do not clear it destructively since that was already
    handled by the abort path when applicable).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/suche/useDebouncedSearch.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/app/suche/useDebouncedSearch.test.tsx` passes.
    - New test cases (per the behavior spec) all pass: results-role with tag-only fetches; results-role
      with genre-only fetches; results-role with neither still skips; full-role with tag-only fetches
      results but does NOT call `getSearchSuggestions`.
    - `grep -n "filters.tag || state.filters.genre" frontend/src/app/suche/useDebouncedSearch.ts`
      matches (or an equivalent destructured form — verify the bypass condition references both).
    - Existing pre-change tests in this file (non-tag/genre length-gate cases) still pass unchanged.
  </acceptance_criteria>
  <done>useDebouncedSearch fires the results request for a tag/genre-only, q-less state while leaving autocomplete suggestions correctly gated at MIN_QUERY_LENGTH.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SearchResults empty-state gate mirrors the same bypass</name>
  <files>frontend/src/app/suche/SearchResults.tsx, frontend/src/app/suche/SearchResults.test.tsx</files>
  <read_first>
    frontend/src/app/suche/SearchResults.tsx (full file — the empty-state gate at lines 109-128),
    frontend/src/app/suche/SearchResults.test.tsx (existing test structure to extend)
  </read_first>
  <behavior>
    - URL has `tag=Amnesia`, no `q` → `SearchResults` does NOT render the "Wonach suchst du?" initial
      empty state; it renders the normal loading/results/tabs flow driven by the hook's `results`/
      `isLoading` state.
    - URL has `genre=Action`, no `q` → same as above.
    - URL has neither `tag` nor `genre` and no `q` → the "Wonach suchst du?" empty state still renders,
      unchanged from today.
    - URL has `tag=Amnesia` AND `q` present but only 1 character → the "Wonach suchst du?" gate is
      irrelevant here since Task 1 ensures the hook still returns `results: null`/`isLoading: false` in
      that case (present-but-short still blocks) — verify `SearchResults` does not crash and falls
      through to its normal `!results` branches gracefully (no explicit new UI needed for this edge
      case beyond what already exists).
  </behavior>
  <action>
    Destructure `filters` from the `useDebouncedSearch({ role: 'results' })` call (alongside the
    existing `q, type, page, results, meta, isLoading, error, setType, setPage`). Change the empty-state
    gate from `if (trimmedQuery.length < MIN_QUERY_LENGTH) { return <EmptyState .../> }` to `if
    (trimmedQuery.length < MIN_QUERY_LENGTH && !filters.tag && !filters.genre) { return <EmptyState
    .../> }` — identical bypass condition shape to Task 1's hook-level change, kept in sync
    intentionally (both files independently gate the same UX moment).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/suche/SearchResults.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/app/suche/SearchResults.test.tsx` passes.
    - New test cases: tag-only URL state does not render the "Wonach suchst du?" `EmptyState`; genre-only
      URL state does not render it; neither-set URL state still renders it (regression guard).
    - `grep -n "!filters.tag && !filters.genre" frontend/src/app/suche/SearchResults.tsx` matches (or
      equivalent).
  </acceptance_criteria>
  <done>The results panel renders for tag/genre-only searches instead of showing the generic "type something" empty state, matching the backend's D-08 bypass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/v1/search | Frontend gate is UX-only defense-in-depth; the authoritative gate is the backend (plan 160-04) per RESEARCH.md's Architectural Responsibility Map |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-10 | Denial of Service | Additional q-less fetch triggers | accept | Debounce (250ms) and AbortController cancellation are unchanged; the backend (plan 160-04) is the authoritative rate/length gate, this is purely a UX mirror |
</threat_model>

<verification>
`npx vitest run src/app/suche/useDebouncedSearch.test.tsx` and `npx vitest run src/app/suche/SearchResults.test.tsx` both pass.
</verification>

<success_criteria>
A tag/genre chip link (to be generated in plan 160-06) lands on a working results page, not a "type
something" empty state, while the generic empty-state behavior for a truly filter-less, query-less
visit is unchanged.
</success_criteria>

<output>
Create `.planning/phases/160-oeffentliche-anime-detailseite-nachschaerfen-tags-anzeigen-g/160-05-SUMMARY.md` when done
</output>
