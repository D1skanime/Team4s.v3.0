---
phase: 109-ranglisten-und-punkteprojektionen
plan: 03
type: execute
wave: 3
depends_on: ["109-02"]
files_modified:
  - backend/internal/handlers/member_point_totals_handler.go
  - backend/cmd/server/main.go
  - shared/contracts/openapi.yaml
  - frontend/src/lib/api.ts
autonomous: true
requirements: [GAM-01, GAM-04, GAM-05]

must_haves:
  truths:
    - "Ein neuer, unauthentifizierter GET-Endpunkt liefert Member → gespeicherte Netto-Gesamtpunkte, absteigend sortiert, seitenweise — als reine Lese-Projektion aus der in Plan 109-02 persistierten Tabelle (D-05: keine Laufzeit-Aggregation im Handler)."
    - "Der neue Endpunkt ist KEIN Unterpfad von /members/:slug und kollidiert nicht mit der bestehenden Route v1.GET(\"/members/:slug\", ...) in backend/cmd/server/main.go:352."
    - "Backend-Response, OpenAPI-Contract und Frontend-Typ/API-Helper sind synchron (gleiche Feldnamen, gleiche Paginierungs-Semantik)."
    - "Es entsteht keine UI, kein Einstiegspunkt und keine Badge-Darstellung (per CONTEXT.md deferred nach Phase 110) — nur Backend-Endpunkt, Contract und typisierter Frontend-API-Helper ohne UI-Konsumenten."
  artifacts:
    - path: "backend/internal/handlers/member_point_totals_handler.go"
      provides: "Dünner GET-Handler GetMemberPointRanking, der ausschließlich MemberPointTotalsRepository.ListRanking aufruft"
      exports: ["MemberPointRankingHandler", "NewMemberPointRankingHandler"]
    - path: "shared/contracts/openapi.yaml"
      provides: "Pfad /api/v1/member-point-ranking (GET) mit page-Query-Parameter und MemberPointRankingResponse-Schema"
      contains: "/api/v1/member-point-ranking"
    - path: "frontend/src/lib/api.ts"
      provides: "getMemberPointRanking(page?) + MemberPointRankingRow/MemberPointRankingResponse-Typen"
      contains: "getMemberPointRanking"
  key_links:
    - from: "backend/cmd/server/main.go"
      to: "backend/internal/handlers/member_point_totals_handler.go"
      via: "v1.GET(\"/member-point-ranking\", memberPointRankingHandler.GetMemberPointRanking)"
      pattern: "member-point-ranking"
    - from: "frontend/src/lib/api.ts"
      to: "/api/v1/member-point-ranking"
      via: "getMemberPointRanking fetch"
      pattern: "member-point-ranking"
---

<objective>
Schließe Phase 109 mit dem dünnen Lese-Handler, der Routenregistrierung, dem OpenAPI-Contract und
den synchronisierten Frontend-Typen/API-Helfern ab, sodass „Member → gespeicherte Netto-Gesamtpunkte,
absteigend sortiert, paginiert" über einen eigenen Top-Level-Endpunkt konsumierbar ist — ohne
UI, ohne Badges, ohne Gruppen-/Kategorie-/Zeitraum-Filter (D-03/D-04 bleiben Phase 110).

Purpose: Die in 109-02 gebaute Persistenzschicht für spätere Konsumenten (Phase 110 Ranglisten-UI/
Badges) erreichbar machen, exakt nach dem bestehenden Muster öffentlicher, unauthentifizierter
Read-Endpunkte in diesem Projekt.
Output: Funktionierender GET /api/v1/member-point-ranking, dokumentiert in openapi.yaml, mit
typisiertem Frontend-Helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-ranglisten-und-punkteprojektionen/109-CONTEXT.md
@.planning/phases/109-ranglisten-und-punkteprojektionen/109-RESEARCH.md
@.planning/phases/109-ranglisten-und-punkteprojektionen/109-PATTERNS.md
@backend/internal/handlers/contributions_public_handler.go
@backend/internal/handlers/member_archive_handler.go
@backend/internal/repository/member_point_totals_repository.go
@frontend/src/lib/api.ts
</context>

<interfaces>
<!-- Aus Plan 109-02, bereits vorhanden — direkt konsumieren, nicht neu ableiten. -->

Aus `backend/internal/repository/member_point_totals_repository.go`:
```go
type MemberPointRankingRow struct {
    MemberID    int64
    DisplayName string
    Slug        *string
    TotalPoints int64
}
func NewMemberPointTotalsRepository(db *pgxpool.Pool) *MemberPointTotalsRepository
func (r *MemberPointTotalsRepository) ListRanking(ctx context.Context, page int) ([]MemberPointRankingRow, int, error)
```

Bounds-/Fehler-Handler-Konvention aus `backend/internal/handlers/member_archive_handler.go` (Zeilen
27–41, wörtlich übernehmen):
```go
pageStr := c.DefaultQuery("page", "1")
page, err := strconv.Atoi(pageStr)
if err != nil || page < 1 {
    page = 1
}
if page > 1000 {
    page = 1000
}
```

Routenregistrierungsstelle in `backend/cmd/server/main.go` (Zeilen 524–536, Konstruktions- und
Registrierungs-Block direkt vor/nach den anderen Public-Read-Routen):
```go
archiveRepo := repository.NewMemberArchiveRepository(dbPool)
archiveHandler := handlers.NewMemberArchiveHandler(archiveRepo)
...
contributionsPublicHandler := handlers.NewContributionsPublicHandler(animeContributionsRepo)
...
v1.GET("/archiv", archiveHandler.SearchArchive)
...
v1.GET("/members/:slug/contributions", contributionsPublicHandler.GetMemberContributions)
```
Kollisionsregel: `v1.GET("/members/:slug", authOptionalMiddleware, publicProfileHandler.GetPublicMemberProfile)`
steht bereits in Zeile 352 — der neue Endpunkt MUSS ein eigener Top-Level-Pfad sein
(`/member-point-ranking`), kein Unterpfad von `/members/:slug`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Handler + Routenregistrierung</name>
  <files>backend/internal/handlers/member_point_totals_handler.go, backend/cmd/server/main.go</files>
  <behavior>
    - GET /api/v1/member-point-ranking ohne Auth-Header liefert 200 mit {data: [...], total, page}.
    - Ungültiger/fehlender page-Parameter fällt auf Seite 1 zurück; page>1000 wird auf 1000 geklemmt (kein 400).
    - Repository-Fehler liefert 500 mit deutscher Fehlermeldung "interner serverfehler" (korrekte Umlaute), kein Stacktrace/Leak.
    - Response-Feldnamen sind snake_case (member_id, display_name, slug, total_points) analog contributions.ts-Konvention.
  </behavior>
  <read_first>
    backend/internal/handlers/contributions_public_handler.go (Package-/Handler-Struktur-/Konstruktor-Konvention, badRequest/internalError-Helper);
    backend/internal/handlers/member_archive_handler.go (page-Parsing-Bounds-Konvention, Response-Envelope {data, total, page});
    backend/cmd/server/main.go Zeilen 340-370 und 505-549 (Kollisionsregel /members/:slug in Zeile 352; Konstruktions-/Registrierungsblock für Public-Read-Handler in Zeilen 524-543).
  </read_first>
  <action>
    Lege `backend/internal/handlers/member_point_totals_handler.go` in Package `handlers` an.
    Definiere `MemberPointRankingHandler struct { repo *repository.MemberPointTotalsRepository }` mit
    Konstruktor `NewMemberPointRankingHandler(repo *repository.MemberPointTotalsRepository)
    *MemberPointRankingHandler`. Implementiere `func (h *MemberPointRankingHandler)
    GetMemberPointRanking(c *gin.Context)`: parse `page` exakt nach dem Muster aus
    `member_archive_handler.go` (`c.DefaultQuery("page", "1")`, `strconv.Atoi`, page<1→1,
    page>1000→1000 — kein 400 bei ungültigem Wert, konsistent mit Repository-Klemmung); rufe
    `h.repo.ListRanking(c.Request.Context(), page)` auf; bei Fehler `internalError(c, "interner
    serverfehler")` und return; bei Erfolg `c.JSON(http.StatusOK, gin.H{"data": rows, "total": total,
    "page": page})` (Envelope-Form identisch zu `SearchArchive`, NICHT die abweichende
    `{"members": ...}`-Form aus dem Research-Entwurf — Konsistenz mit dem einzigen bereits
    existierenden paginierten Public-Endpunkt hat Vorrang). In `backend/cmd/server/main.go`:
    registriere im bestehenden Konstruktionsblock um Zeile 524-534 `memberPointTotalsRepo :=
    repository.NewMemberPointTotalsRepository(dbPool)` und `memberPointRankingHandler :=
    handlers.NewMemberPointRankingHandler(memberPointTotalsRepo)`; registriere die Route direkt neben
    den anderen unauthentifizierten Public-Read-Routen (z. B. neben Zeile 536/543) als `v1.GET(
    "/member-point-ranking", memberPointRankingHandler.GetMemberPointRanking)` — explizit KEIN
    Unterpfad von `/members/:slug` (Kollisionsregel, Pitfall 3), explizit ohne `authMiddleware`
    (analog `/archiv`, `/fansubs/:id/contributions`, `/members/:slug/contributions` — alles
    unauthentifizierte Public-Reads in diesem Projekt).
  </action>
  <acceptance_criteria>
    go build ./backend/... kompiliert fehlerfrei. Server startet ohne Gin-Routenkonflikt-Panic
    (Pitfall 3 nicht eingetreten). curl -s http://localhost:PORT/api/v1/member-point-ranking liefert
    200 mit JSON-Envelope {data, total, page} gegen eine laufende Docker-Backend-Instanz (falls
    verfügbar) oder zumindest ein erfolgreicher go vet/go build als Minimalnachweis, falls kein
    laufender Server erreichbar ist.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./...</automated>
  </verify>
  <done>Handler kompiliert, Route ist registriert, keine Kollision mit /members/:slug, Response-Envelope entspricht {data, total, page}.</done>
</task>

<task type="auto">
  <name>Task 2: OpenAPI-Contract + Frontend-Typen/API-Helper</name>
  <files>shared/contracts/openapi.yaml, frontend/src/lib/api.ts</files>
  <read_first>
    shared/contracts/openapi.yaml Abschnitt /api/v1/members/{slug} (Zeilen 516-563, Response-/Error-Schema-Stil) und /api/v1/anime (Zeilen 564-620, page-Query-Parameter-Stil);
    frontend/src/lib/api.ts Funktion getMemberContributions (Zeilen 9101-9126, Public-GET-ohne-Auth-Muster) und searchArchive/ArchiveMemberRow/ArchiveSearchResponse (Zeilen 9318-9364, paginierte Query-Params + co-lokalisierte Typen).
  </read_first>
  <action>
    Ergänze in `shared/contracts/openapi.yaml` unter den bestehenden Pfaden einen neuen Eintrag
    `/api/v1/member-point-ranking` mit `get:` Operation, `tags: [Ranking]`, `operationId:
    getMemberPointRanking`, Query-Parameter `page` (`type: integer`, `minimum: 1`, `default: 1`, Stil
    aus `/api/v1/anime`), Response `200` mit Schema `MemberPointRankingResponse` (Felder `data:
    array` von `MemberPointRankingRow`, `total: integer`, `page: integer`) und `500` mit
    `ErrorResponse` (Stil aus `/api/v1/members/{slug}`). Definiere im `components.schemas`-Abschnitt
    `MemberPointRankingRow` mit `member_id` (`type: integer`, `format: int64`), `display_name`
    (`type: string`), `slug` (`type: string`, `nullable: true`), `total_points` (`type: integer`,
    `format: int64`) sowie `MemberPointRankingResponse` mit `data` (Array von
    `MemberPointRankingRow`), `total` (`integer`), `page` (`integer`). In `frontend/src/lib/api.ts`:
    füge co-lokalisiert (Stil `ArchiveMemberRow`/`ArchiveSearchResponse`, Zeilen 9318-9333) die
    Interfaces `MemberPointRankingRow` (`member_id: number`, `display_name: string`, `slug: string |
    null`, `total_points: number`) und `MemberPointRankingResponse` (`data:
    MemberPointRankingRow[]`, `total: number`, `page: number`) ein. Implementiere `export async
    function getMemberPointRanking(page?: number): Promise<MemberPointRankingResponse>` nach dem
    exakten Muster von `searchArchive` (Zeilen 9341-9364): `URLSearchParams` nur mit `page` befüllen
    wenn `page && page > 1`, Fetch gegen
    `${API_BASE_URL}/api/v1/member-point-ranking${query.toString() ? '?' + query.toString() : ''}`
    ohne Auth-Header, `parseApiErrorPayload`/`ApiError` bei `!response.ok` exakt wie in
    `getMemberContributions`. Keine UI-Komponente, keinen Aufrufer dieser Funktion und keinen
    Navigations-Einstiegspunkt in diesem Task erstellen (D-03/D-04/Deferred — UI ist Phase 110).
  </action>
  <acceptance_criteria>
    OpenAPI-Datei bleibt gültiges YAML (parsebar). frontend: tsc --noEmit kompiliert ohne neue
    Fehler. getMemberPointRanking ist exportiert und in keiner UI-Komponente referenziert (grep
    bestätigt genau einen Definitionsort, keine Import-Verwendung außerhalb von Tests).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && (npx --no-install @redocly/cli lint ../shared/contracts/openapi.yaml 2>/dev/null || node -e "const y=require('js-yaml');const fs=require('fs');y.load(fs.readFileSync('../shared/contracts/openapi.yaml','utf8'));console.log('yaml ok')") && npm run typecheck</automated>
  </verify>
  <done>OpenAPI-Contract und Frontend-Typen/API-Helper sind synchron mit dem Backend-Endpunkt; kein UI-Konsument existiert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Öffentlicher HTTP-Client → GET /api/v1/member-point-ranking | Unauthentifizierter Request, `page`-Query-Parameter kreuzt die Grenze |
| Handler → MemberPointTotalsRepository | Interner Aufruf, keine zusätzliche Vertrauensgrenze (Repository validiert bereits selbst) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-01 | Tampering | `page`-Query-Parameter am Handler | mitigate | `strconv.Atoi` + Bounds-Klemmung exakt wie `member_archive_handler.go`; ungültige Werte fallen auf Seite 1 zurück statt Fehler/Injection-Oberfläche |
| T-109-02 | Information Disclosure | JSON-Response des neuen Endpunkts | mitigate | Response enthält ausschließlich `member_id`, `display_name`, `slug`, `total_points` (aus Plan 109-02 bereits gefiltert auf `profile_visibility='public'`); keine `actor_app_user_id`, keine Ledger-Interna, kein Stacktrace bei 500 |
| T-109-03 | Denial of Service | Wiederholte Anfragen an den Endpunkt | accept (strukturell durch D-05 gelöst) | Handler ruft nur `ListRanking` gegen die materialisierte Tabelle auf, keine Laufzeit-Aggregation, kein Fan-out |
| T-109-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Paketabhängigkeiten in diesem Plan (nur bestehende gin/pgx/Frontend-Fetch-Muster; `@redocly/cli` wird nur optional per `npx` zur Contract-Validierung genutzt, nicht als Projektabhängigkeit installiert) |
</threat_model>

<verification>
`cd backend && go build ./... && go vet ./...` muss fehlerfrei sein. `cd frontend && npm run
typecheck` muss fehlerfrei sein. OpenAPI-Datei muss gültiges YAML bleiben (Redocly-Lint oder
js-yaml-Fallback). Vor Phase-Abschluss: `cd backend && go test ./... -v` vollständig grün
(inklusive aller Wave-0/Wave-2-Tests aus 109-01/109-02 und der bestehenden Phase-106/107-
Boundary-Tests).
</verification>

<success_criteria>
GET /api/v1/member-point-ranking ist erreichbar, unauthentifiziert, korrekt paginiert und kollidiert
nicht mit /members/:slug. OpenAPI-Contract und Frontend-Typ/API-Helper sind synchron. Keine UI wurde
gebaut (Phase 110 bleibt zuständig). Alle Phase-109-Tests (Wave 1-3) sind grün.
</success_criteria>

<output>
Create `.planning/phases/109-ranglisten-und-punkteprojektionen/109-03-SUMMARY.md` when done
</output>
