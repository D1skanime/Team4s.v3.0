---
phase: 109-ranglisten-und-punkteprojektionen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/migrations/phase109_member_point_totals_test.go
  - backend/internal/repository/member_point_totals_repository_test.go
autonomous: true
requirements: [GAM-01, GAM-02, GAM-03, GAM-04, GAM-05]

must_haves:
  truths:
    - "Ein automatisierter Migrations-Contract-Test für die neue Migration 0139 existiert und schlägt vor der Implementierung erwartungsgemäß fehl (RED), weil die Migrationsdateien noch nicht existieren."
    - "Ein automatisierter Repository-Test für Concurrency, Reversal und Ranking-Sortierung existiert und kompiliert vor der Implementierung erwartungsgemäß nicht (RED), weil MemberPointTotalsRepository noch nicht existiert."
    - "Die Tests kodieren bereits D-01 (Netto-Punktsumme absteigend sortiert), D-05 (persistierte Summe) und D-06 (transaktionale, retry-sichere Fortschreibung inkl. Reversal-Senkung) als spätere Abnahmekriterien für Plan 109-02."
    - "Keine der bestehenden Boundary-geschützten Dateien (point_service.go, point_ledger_repository.go, point_rules_repository.go, review_service.go, Review-Repositories, 0131-Migration) wird von diesem Plan verändert."
  artifacts:
    - path: "backend/internal/migrations/phase109_member_point_totals_test.go"
      provides: "Migrations-Contract-Test für Tabelle+Trigger+Guard-Trigger der Migration 0139 (Up/Down/Live-Up-Down-Up)"
    - path: "backend/internal/repository/member_point_totals_repository_test.go"
      provides: "Concurrency-, Idempotenz-, Reversal-, Ranking-Sortierungs- und JSON-Feldnamen-Tests für MemberPointTotalsRepository"
  key_links:
    - from: "backend/internal/migrations/phase109_member_point_totals_test.go"
      to: "database/migrations/0139_member_point_totals.up.sql"
      via: "readPhase106Migration(t, phase109UpFile) / phase106MigrationPath(t, phase109UpFile)"
      pattern: "phase109UpFile|phase109MigrationName"
    - from: "backend/internal/repository/member_point_totals_repository_test.go"
      to: "backend/internal/repository/point_ledger_repository.go"
      via: "openPointLedgerPostgres(t) + NewPointLedgerRepository(pool).InsertAward/InsertReversal (bestehende, unveränderte Datei)"
      pattern: "openPointLedgerPostgres|NewPointLedgerRepository"
---

<objective>
Wave 0 (RED): Lege die automatisierten Test-Scaffolds an, die die spätere Migration 0139
(`member_point_totals`-Tabelle + `AFTER INSERT`-Trigger + Guard-Trigger) und das spätere
`MemberPointTotalsRepository` gegen D-01 (absteigend sortierte Netto-Summe), D-05 (persistierte
Summe) und D-06 (transaktionale, retry-sichere, reversal-fähige Fortschreibung) verifizieren.
Beide Testdateien referenzieren noch nicht existierenden Code/Migrationsdateien und MÜSSEN an
dieser Stelle fehlschlagen (Migrationstest: Datei nicht gefunden; Repository-Test: Kompilierfehler
wegen undefinierter Symbole). Das ist der beabsichtigte RED-Zustand vor Plan 109-02. Der
Repository-Test kodiert zusätzlich einen JSON-Serialisierungs-Contract (snake_case Response-Keys),
den Plan 109-02 über explizite Struct-Tags erfüllen muss.

Purpose: Nyquist-konforme Testabdeckung VOR der Implementierung sicherstellen, exakt nach dem in
109-VALIDATION.md vorgegebenen Muster, und die bestehenden Phase-106/107-Boundary-Tests durch reine
Testdatei-Ergänzung (keine Produktivcode-Änderung) unangetastet lassen.
Output: Zwei neue, kompilierbare (Migrationstest) bzw. bewusst nicht kompilierende
(Repository-Test) Testdateien, die die Abnahmekriterien für Plan 109-02 exakt festlegen —
inklusive der snake_case-JSON-Feldnamen-Assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-ranglisten-und-punkteprojektionen/109-CONTEXT.md
@.planning/phases/109-ranglisten-und-punkteprojektionen/109-RESEARCH.md
@.planning/phases/109-ranglisten-und-punkteprojektionen/109-PATTERNS.md
@.planning/phases/109-ranglisten-und-punkteprojektionen/109-VALIDATION.md
@backend/internal/migrations/phase106_member_points_test.go
@backend/internal/repository/point_ledger_repository_test.go
@backend/internal/repository/point_ledger_repository.go
@backend/internal/repository/member_archive_repository.go
@backend/internal/repository/anime_contributions_public_repository.go
@backend/internal/testsupport/phase106_postgres.go
@database/migrations/0131_member_point_foundation.up.sql
@database/migrations/0131_member_point_foundation.down.sql
</context>

<interfaces>
<!-- Bereits vorhandene, in derselben Package wiederverwendbare Test-Helper. Keine Kopie anlegen. -->

Aus `backend/internal/migrations/phase106_member_points_test.go` (package `migrations`, generisch,
NICHT Phase-106-spezifisch trotz Namenspräfix — direkt mit neuen Dateinamen aufrufbar):
```
func readPhase106Migration(t testing.TB, name string) string
func phase106MigrationPath(t testing.TB, name string) string
func phase106RepoRoot(t testing.TB) string
func normalizePhase106SQL(sql string) string
func requireSQLContains(t testing.TB, sql string, needles ...string)
func requireOrder(t testing.TB, sql, first, second string)
func assertPhase106TableExists(t testing.TB, pool *pgxpool.Pool, table string)
```

Aus `backend/internal/repository/point_ledger_repository_test.go` (package `repository`, direkt
wiederverwendbar):
```
func openPointLedgerPostgres(t *testing.T) *pgxpool.Pool // wendet NUR 0131 an + seedet members(1)/app_users(10)/fansub_groups(20)/release_versions(30)/point_rules(101)
func postgresAwardInput(key string) PointAwardInput
func pointInt64Ptr(v int64) *int64
```

Aus `backend/internal/testsupport/phase106_postgres.go`:
```
func OpenPhase106Postgres(t *testing.T) *pgxpool.Pool // isolierte Schema-Fixture; members(id BIGINT PRIMARY KEY) — KEINE nickname/display_name/profile_visibility-Spalten
func ApplySQLFile(t testing.TB, pool *pgxpool.Pool, path string)
```

Aus `backend/internal/repository/anime_contributions_public_repository.go` (bereits importierbar,
gleiche Package `repository`):
```
const memberSlugExpr = `NULLIF(LOWER(TRIM(BOTH '-' FROM REGEXP_REPLACE(TRIM(%s), '[^a-z0-9]+', '-', 'gi'))), '')`
const memberDisplayExpr = `COALESCE(NULLIF(TRIM(%s.display_name), ''), %s.nickname)`
```

Erwartete JSON-Struct-Tags von `MemberPointRankingRow` (Plan 109-02 MUSS diese setzen, analog
`ArchiveMemberRow`/`anime_contributions_public_repository.go`-Struct-Tags): `member_id`,
`display_name`, `slug`, `total_points` — der in diesem Plan geschriebene
`TestMemberPointRankingRowJSONFieldNames`-Test erzwingt das über eine `json.Marshal`-Assertion.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrations-Contract-Test für 0139 (RED)</name>
  <files>backend/internal/migrations/phase109_member_point_totals_test.go</files>
  <read_first>
    backend/internal/migrations/phase106_member_points_test.go (Analog + wiederverwendbare Helper: readPhase106Migration, phase106MigrationPath, requireSQLContains, requireOrder, assertPhase106TableExists, phase106RepoRoot);
    database/migrations/0131_member_point_foundation.up.sql und .down.sql (Trigger-/Funktions-/Migrationsstil, den 0139 übernehmen wird);
    backend/internal/testsupport/phase106_postgres.go (OpenPhase106Postgres/ApplySQLFile-Signaturen für den Live-Up-Down-Up-Test);
    .planning/phases/109-ranglisten-und-punkteprojektionen/109-RESEARCH.md Abschnitt "Pattern 1"/"Pattern 2" (vollständiger SQL-Wortlaut, den 0139 in Plan 109-02 exakt umsetzen wird — die hier geschriebenen Contract-Fragmente müssen zu diesem Wortlaut passen).
  </read_first>
  <action>
    Lege in Package `migrations` die Datei `phase109_member_point_totals_test.go` an. Prüfe
    unmittelbar vorher per Verzeichnislisting von `database/migrations`, ob `0139` noch die höchste
    freie Nummer ist (Stand dieser Planung: höchste vorhandene ist 0138); falls ein paralleler
    GSD-Lauf 0139 bereits belegt hat, verschiebe Name/Konstanten in dieser Datei und in Plan 109-02
    konsistent auf die nächste freie Nummer (Pitfall 4) — Inhalt/Form bleiben unverändert. Definiere
    Konstanten `phase109MigrationName = "0139_member_point_totals"`, `phase109UpFile =
    phase109MigrationName + ".up.sql"`, `phase109DownFile = phase109MigrationName + ".down.sql"`.
    Implementiere `TestPhase109MigrationUpContract`: lade den Up-Inhalt über die bestehende,
    generische Funktion `readPhase106Migration(t, phase109UpFile)` (direkte Wiederverwendung, keine
    Kopie); prüfe per `requireOrder` die Reihenfolge `create table member_point_totals` vor
    `create function apply_point_ledger_entry_to_member_total` vor `create trigger
    point_ledger_apply_member_total`; prüfe per `requireSQLContains`, dass folgende Fragmente
    enthalten sind: `member_id bigint primary key references members(id)`, `total_points bigint not
    null default 0`, `after insert on point_ledger_entries`, `for each row execute function
    apply_point_ledger_entry_to_member_total`, `on conflict (member_id) do update`, `total_points =
    member_point_totals.total_points + new.point_value`, `before insert or update or delete on
    member_point_totals`, `pg_trigger_depth() = 0`, `raise exception`. Prüfe zusätzlich per
    `require.NotContains`, dass der Up-Inhalt weder `insert into point_ledger_entries` (der Trigger
    darf niemals selbst in den fremden Ledger schreiben) noch `on delete cascade` enthält (FK auf
    `members` bleibt ohne Cascade, analog 0131). Implementiere `TestPhase109MigrationDownContract`:
    lade den Down-Inhalt über `readPhase106Migration(t, phase109DownFile)`; prüfe per
    `requireSQLContains`, dass `drop trigger`, `drop function` und `drop table if exists
    member_point_totals` enthalten sind; prüfe per `requireOrder`, dass `drop trigger` vor `drop
    table if exists member_point_totals` und `drop function` vor `drop table if exists
    member_point_totals` steht; prüfe per `require.NotContains`, dass der Down-Inhalt NICHT `drop
    table if exists point_ledger_entries` enthält (0139 darf nur seinen eigenen Trigger auf der
    fremden Tabelle entfernen, niemals die Tabelle selbst — die gehört Phase 106). Implementiere
    `TestPhase109MigrationLiveUpDownUp`: öffne `pool := testsupport.OpenPhase106Postgres(t)`; wende
    zuerst `phase106MigrationPath(t, "0131_member_point_foundation.up.sql")` an (Abhängigkeit: der
    0139-Trigger hängt am Ziel `point_ledger_entries` aus 0131), danach `phase109UpFile`, dann
    `phase109DownFile`, dann erneut `phase109UpFile`; prüfe abschließend per
    `assertPhase106TableExists(t, pool, "member_point_totals")`. Diese Datei referenziert
    ausschließlich noch nicht existierende Migrationsdateien — das Kompilieren gelingt, die
    Testausführung schlägt erwartungsgemäß fehl (Datei nicht gefunden). Keine Produktivcode-Datei in
    diesem Task anfassen.
  </action>
  <acceptance_criteria>
    Datei backend/internal/migrations/phase109_member_point_totals_test.go existiert und kompiliert
    (go vet meldet keinen Syntax-/Typfehler). go test ./backend/internal/migrations/... -run
    TestPhase109Migration -v schlägt aktuell erwartungsgemäß fehl, weil
    database/migrations/0139_member_point_totals.up.sql (oder die tatsächlich gewählte freie Nummer)
    noch nicht existiert — dieser RED-Zustand ist das beabsichtigte Ergebnis dieses Tasks und wird
    erst durch Plan 109-02 grün.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go vet ./internal/migrations/... 2>&1 | tee /tmp/phase109-vet.log; ! grep -q "^#" /tmp/phase109-vet.log; go test ./internal/migrations/... -run TestPhase109Migration -v 2>&1 | grep -qi "no such file or directory\|FAIL"</automated>
  </verify>
  <done>Testdatei kompiliert; TestPhase109Migration* schlägt aktuell dokumentiert (RED) fehl, weil die Migrationsdateien fehlen.</done>
</task>

<task type="auto">
  <name>Task 2: Repository-Test für Concurrency/Reversal/Ranking/JSON-Contract (RED)</name>
  <files>backend/internal/repository/member_point_totals_repository_test.go</files>
  <read_first>
    backend/internal/repository/point_ledger_repository_test.go (Analog: openPointLedgerPostgres, postgresAwardInput, TestPointLedgerPostgresConcurrentAward-Concurrency-Muster mit WaitGroup+Channel);
    backend/internal/repository/member_archive_repository.go (Bounds-Check-Pattern page<1→1, page>1000→1000; Verwendung von memberDisplayExpr/memberSlugExpr im Query-Builder; JSON-Struct-Tag-Stil auf ArchiveMemberRow als Namensvorbild für member_id/display_name/slug/total_points);
    backend/internal/repository/anime_contributions_public_repository.go (memberDisplayExpr/memberSlugExpr-Konstanten, bereits im Package `repository` vorhanden — nicht neu definieren);
    backend/internal/testsupport/phase106_postgres.go (createPhase106Prerequisites: members-Tabelle im Test-Fixture hat NUR die Spalte id — nickname/display_name/profile_visibility fehlen und müssen testlokal per ALTER TABLE ergänzt werden, OHNE diese Datei zu verändern);
    .planning/phases/109-ranglisten-und-punkteprojektionen/109-RESEARCH.md Abschnitt "Pitfall 2" und "Pitfall 5" (Concurrency- bzw. Tie-Break-Anforderungen, die dieser Test exakt einfordert).
  </read_first>
  <action>
    Lege in Package `repository` die Datei `member_point_totals_repository_test.go` an. Definiere
    einen lokalen Helper `openMemberPointTotalsPostgres(t *testing.T) *pgxpool.Pool`, der
    `openPointLedgerPostgres(t)` wiederverwendet (wendet 0131 an und seedet members(1)/app_users(10)/
    fansub_groups(20)/release_versions(30)/point_rules(101)), danach per
    `testsupport.ApplySQLFile` die neue Migration `0139_member_point_totals.up.sql` anwendet (relativer
    Pfad exakt wie in `openPointLedgerPostgres` über `runtime.Caller(0)` + `filepath.Join(...,
    "database", "migrations", ...)` aufgebaut), und danach testlokal per `pool.Exec` ein `ALTER TABLE
    members ADD COLUMN nickname TEXT, ADD COLUMN display_name TEXT, ADD COLUMN profile_visibility
    TEXT NOT NULL DEFAULT 'public'` ausführt (rein testlokale Schema-Erweiterung der isolierten
    Testschema-Instanz — `testsupport/phase106_postgres.go` bleibt unverändert). Implementiere
    `TestMemberPointTotalsPostgresConcurrentAwardsSumCorrectly`: zwei parallele `InsertAward`-Aufrufe
    über `NewPointLedgerRepository(pool)` für `MemberID: 1` mit unterschiedlichen
    `SourceKey`/`IdempotencyKey` (Basis `postgresAwardInput("award:mpt-concurrent-a")` und eine
    zweite Variante mit `SourceKey`/`IdempotencyKey` `"mpt-concurrent-b"`), gestartet über exakt das
    WaitGroup+Channel-Muster aus `TestPointLedgerPostgresConcurrentAward`; danach `SELECT
    total_points FROM member_point_totals WHERE member_id = 1` und Assertion, dass die Summe genau
    der Summe beider `RulePointValue` (2×10=20) entspricht. Implementiere
    `TestMemberPointTotalsPostgresRetrySameAwardDoesNotDoubleCount`: derselbe `PointAwardInput`
    zweimal sequenziell an `InsertAward` übergeben (identischer `IdempotencyKey` — Retry-Fall); nach
    beiden Aufrufen muss `total_points` für den Member genau einmal um den Regelwert (10) gestiegen
    sein, nicht doppelt (beweist Pitfall 2: `ON CONFLICT DO NOTHING` erzeugt kein zweites
    Insert-Event, der Trigger feuert nicht erneut). Implementiere
    `TestMemberPointTotalsPostgresReversalLowersTotal`: `InsertAward` gefolgt von `InsertReversal`
    (`OriginalEntryID` des Awards, `ActorAppUserID: 10`, plausibler `IdempotencyKey`, `Reason:
    "Testkorrektur"`); Assertion, dass `total_points` nach Award+Reversal wieder auf 0 zurückfällt
    (Netto-Semantik aus D-01/D-06). Implementiere
    `TestMemberPointTotalsRankingOrderAndTieBreak`: seede zusätzlich zwei weitere Members (2, 3) mit
    `nickname`/`display_name`/`profile_visibility='public'` direkt per SQL; erzeuge über
    `InsertAward`-Aufrufe eine Punktverteilung mit bewusstem Gleichstand (z. B. Member 1 und Member 2
    beide auf 20 Punkte über zwei Awards à 10 mit unterschiedlichen `SourceKey`/`IdempotencyKey`,
    Member 3 auf 5 Punkte); rufe die (in Plan 109-02 zu implementierende) Methode
    `NewMemberPointTotalsRepository(pool).ListRanking(ctx, 1)` auf und assere: Rückgabe enthält genau
    3 Zeilen in der Reihenfolge total_points DESC mit `member_id ASC` als Tie-Break bei
    Punktgleichstand (Member 1 vor Member 2, da gleiche Punktzahl, kleinere ID zuerst; Member 3
    zuletzt), und dass `DisplayName`/`Slug` aus `members.display_name`/`nickname` über die
    wiederverwendeten Konstanten `memberDisplayExpr`/`memberSlugExpr` korrekt abgeleitet sind.
    Implementiere `TestMemberPointTotalsRankingPageBounds`: rufe `ListRanking(ctx, 0)` (muss intern
    wie Seite 1 behandelt werden) und `ListRanking(ctx, 5000)` (muss auf die interne Obergrenze
    geklemmt werden, analog `member_archive_repository.go`: page<1→1, page>1000→1000) auf und
    assere, dass beide Aufrufe ohne Fehler zurückkehren (kein Panic, kein negativer OFFSET).
    Implementiere zusätzlich `TestMemberPointRankingRowJSONFieldNames` (schließt die
    Verifikationslücke aus dem Checker-Review — eine reine Go-Feldnamen-Prüfung reicht nicht, weil
    `encoding/json` ohne Struct-Tags PascalCase erzeugen würde): baue einen Beispielwert
    `MemberPointRankingRow{MemberID: 1, DisplayName: "Test", Slug: pointStringPtr("test"),
    TotalPoints: 20}`, serialisiere ihn per `json.Marshal`, und assere per `require.Contains` auf den
    resultierenden JSON-String, dass er die snake_case-Schlüssel `"member_id"`, `"display_name"`,
    `"slug"`, `"total_points"` enthält; assere zusätzlich per `require.NotContains`, dass er NICHT die
    PascalCase-Schlüssel `"MemberID"`, `"DisplayName"`, `"TotalPoints"` enthält. Diese
    Datei referenziert `MemberPointTotalsRepository`, `NewMemberPointTotalsRepository`,
    `MemberPointRankingRow` und `ListRanking`, die erst in Plan 109-02 entstehen — das Kompilieren
    schlägt an dieser Stelle bewusst fehl (RED). Keine Produktivcode-Datei in diesem Task anfassen.
  </action>
  <acceptance_criteria>
    Datei backend/internal/repository/member_point_totals_repository_test.go existiert und enthält
    u. a. `TestMemberPointRankingRowJSONFieldNames`. go build ./backend/internal/repository/...
    schlägt aktuell erwartungsgemäß mit "undefined: NewMemberPointTotalsRepository" (oder einem der
    anderen neuen Symbole) fehl — dieser RED-Zustand ist das beabsichtigte Ergebnis dieses Tasks und
    wird erst durch Plan 109-02 grün, sobald `MemberPointRankingRow` mit den korrekten snake_case
    JSON-Struct-Tags existiert. Bestehende Tests in derselben Package (z. B.
    TestPointLedgerPostgresConcurrentAward) dürfen durch das Hinzufügen dieser Datei nicht verändert
    oder gebrochen werden.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./internal/repository/... 2>&1 | grep -q "undefined: NewMemberPointTotalsRepository\|undefined: MemberPointTotalsRepository"</automated>
  </verify>
  <done>Testdatei existiert (inklusive JSON-Feldnamen-Assertion); Build schlägt dokumentiert (RED) mit undefined-Symbol-Fehler fehl, bis Plan 109-02 das Repository mit korrekten Struct-Tags liefert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-Prozess → isolierte Postgres-Schema-Fixture | Nur in CI/lokalem Testlauf relevant, kein Produktions-Trust-Boundary; per `OpenPhase106Postgres` schema-isoliert |
| (Produktions-Boundaries folgen in Plan 109-02/109-03, sobald Migration/Repository/Handler existieren) | — |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-01 | Tampering | zukünftiger `page`-Query-Parameter | mitigate (encoded by test, implemented in 109-02/109-03) | `TestMemberPointTotalsRankingPageBounds` in diesem Plan legt bereits die Bounds-Erwartung (page<1→1, page>1000→1000) fest, die Plan 109-02 erfüllen muss |
| T-109-02 | Information Disclosure | zukünftige Ranking-Query gegen `members` | mitigate (encoded by test, implemented in 109-02) | `TestMemberPointTotalsRankingOrderAndTieBreak` seedet nur `profile_visibility='public'`-Members; Plan 109-02 muss diesen Filter in der Query erzwingen |
| T-109-03 | Denial of Service | zukünftige Ranking-Query | accept (strukturell durch D-05 gelöst) | Kein Runtime-`SUM` — Persistierung wird in Plan 109-02 umgesetzt, dieser Plan fügt nur Tests hinzu |
| T-109-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Paketabhängigkeiten in diesem Plan (nur Go-Standardbibliothek + bestehende `pgx`/`testify`) |
</threat_model>

<verification>
`cd backend && go vet ./internal/migrations/... ./internal/repository/...` muss kompilieren (der
Migrationstest kompiliert und läuft, scheitert aber am Runtime-Dateizugriff; der Repository-Test
scheitert bereits beim `go build` wegen undefinierter Symbole — beides ist der erwartete RED-Zustand
dieses Plans). `go test ./backend/internal/services/... -run
'TestPointServicePhase106Boundary|TestPhase107ReviewServiceBoundary' -v` muss weiterhin grün bleiben
(unverändert, Regressionsschutz).
</verification>

<success_criteria>
Beide Testdateien existieren, sind syntaktisch korrekt und beschreiben exakt die Abnahmekriterien
(D-01, D-05, D-06, snake_case-JSON-Contract) für Plan 109-02. Kein Produktivcode wurde verändert.
Die bestehenden Boundary-Tests aus Phase 106/107 sind weiterhin grün.
</success_criteria>

<output>
Create `.planning/phases/109-ranglisten-und-punkteprojektionen/109-01-SUMMARY.md` when done
</output>
</output>
