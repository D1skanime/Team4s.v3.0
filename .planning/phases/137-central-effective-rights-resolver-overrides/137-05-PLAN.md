---
phase: 137-central-effective-rights-resolver-overrides
plan: 05
type: execute
wave: 4
depends_on: [137-04]
files_modified:
  - backend/internal/permissions/permissions.go
  - backend/internal/permissions/effective_rights_integration_test.go
autonomous: true
requirements: [CAP-03, QUAL-03]
must_haves:
  truths:
    - "All existing group-scoped Can* entry points derive their group decision from ResolveGroupRights."
    - "CanReviewForFansubGroup is no longer an override-blind parallel decision engine."
    - "CanForRelease, CanForReleaseVersion and CanForReleaseVersionMedia become override-aware through the shared group-context path."
    - "Existing external Result and ReviewAuthorizationResult shapes/reason behavior remain compatible for callers."
  artifacts:
    - path: "backend/internal/permissions/permissions.go"
      provides: "Minimal delegation edits from legacy entry points to the central resolver"
    - path: "backend/internal/permissions/effective_rights_integration_test.go"
      provides: "Regression coverage across existing authorization entry points"
  key_links:
    - from: "CanForFansubGroup / canForContext / CanReviewForFansubGroup"
      to: "ResolveGroupRights"
      via: "single decision engine"
      pattern: "ResolveGroupRights"
---

<objective>
Route every existing group-scoped enforcement path through the new resolver without breaking callers.

Purpose: CAP-03 is only true if actual runtime enforcement and inspection use the same precedence.
Output: Minimal legacy-entry-point changes plus regression tests.
</objective>

<context>
@AI-HANDOFF.md
@.planning/phases/137-central-effective-rights-resolver-overrides/137-CONTEXT.md
@.planning/phases/137-central-effective-rights-resolver-overrides/137-RESEARCH.md
@backend/internal/permissions/permissions.go
@backend/internal/permissions/effective_rights.go
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Prove all existing group-scoped entry points observe overrides</name>
  <read_first>permissions.go CanForFansubGroup, CanForRelease, CanForReleaseVersion, CanForReleaseVersionMedia, CanReviewForFansubGroup; 137-RESEARCH.md Pitfall 2/Open Question 4</read_first>
  <files>backend/internal/permissions/effective_rights_integration_test.go</files>
  <behavior>
    - A user-deny changes the result returned through CanForFansubGroup.
    - The same precedence applies to release/release-version/media entry points that resolve to that group.
    - A review.*.decide user-deny changes CanReviewForFansubGroup even when review delegation grants it.
    - Platform-admin behavior stays unchanged.
    - Existing reason/result projection remains compatible.
  </behavior>
  <action>Add regression tests first around existing public permission methods, not merely the internal evaluator.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/permissions -run 'EffectiveRights.*Integration|CanFor.*Override|Review.*Override' -count=1</automated></verify>
  <done>Tests expose any remaining parallel/override-blind enforcement path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delegate existing enforcement to ResolveGroupRights</name>
  <read_first>effective_rights_integration_test.go; permissions.go; effective_rights.go</read_first>
  <files>backend/internal/permissions/permissions.go</files>
  <action>Make the smallest possible edits in the already-large permissions.go: replace the shared per-group role loop with a ResolveGroupRights call + action lookup, and make CanReviewForFansubGroup project its ReviewAuthorizationResult from the same group resolution. Preserve public signatures and existing Result/Reason contracts. Do not duplicate resolver logic in permissions.go.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/permissions -count=1 &amp;&amp; git diff --check</automated></verify>
  <done>All group-scoped runtime authorization entry points share the central resolver and existing permission tests stay green.</done>
</task>
</tasks>

<verification>Full permissions package tests plus `git diff --check`.</verification>
<success_criteria>No production group-scoped permission path can bypass user overrides or specialized-grant precedence.</success_criteria>
<output>After completion, create `.planning/phases/137-central-effective-rights-resolver-overrides/137-05-SUMMARY.md`.</output>
