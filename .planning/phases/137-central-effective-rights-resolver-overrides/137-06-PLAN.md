---
phase: 137-central-effective-rights-resolver-overrides
plan: 06
type: execute
wave: 4
depends_on: [137-01, 137-03, 137-04]
files_modified:
  - backend/internal/services/effective_rights_service.go
  - backend/internal/services/effective_rights_service_test.go
  - backend/internal/services/effective_rights_concurrency_test.go
autonomous: true
requirements: [CAP-05, CAP-06, CAP-07, QUAL-03]
must_haves:
  truths:
    - "Override mutation supports exactly SET ALLOW, SET DENY and REMOVE; no toggle semantics."
    - "A real change requires an active target membership, an overridable action, actor authorization for the same target group, and a reason."
    - "Exact repeats are true no-ops: changed=false, no new domain-history row, no artificial state change."
    - "Mutation and history append are one transaction; history failure rolls back state."
    - "Concurrent conflicting mutations serialize to a consistent committed state/history."
    - "A successful real mutation and a successful no-op report activation_status=active in Phase 137."
  artifacts:
    - path: "backend/internal/services/effective_rights_service.go"
      provides: "Transactional override orchestration"
    - path: "backend/internal/services/effective_rights_service_test.go"
      provides: "Authorization, validation, idempotency, rollback and dormant-membership tests"
    - path: "backend/internal/services/effective_rights_concurrency_test.go"
      provides: "Deterministic conflicting-mutation race proof"
  key_links:
    - from: "EffectiveRightsService"
      to: "permissions.ResolveGroupRights"
      via: "management-capability authorization against target group"
      pattern: "override.manage|ResolveGroupRights"
    - from: "override mutation"
      to: "override history"
      via: "same pgx transaction"
      pattern: "Commit|Rollback"
---

<objective>
Implement secure, idempotent and fully audited per-user override mutation.

Purpose: D06-D08 require state and history to move atomically under the same group-scoped authorization decision.
Output: Transactional service plus deterministic integration/concurrency tests.
</objective>

<context>
@AI-HANDOFF.md
@.planning/phases/137-central-effective-rights-resolver-overrides/137-CONTEXT.md
@.planning/phases/137-central-effective-rights-resolver-overrides/137-RESEARCH.md
@backend/internal/services/review_service.go
@backend/internal/repository/review_delegation_repository.go
@backend/internal/repository/authz_user_overrides.go
@backend/internal/permissions/effective_rights.go
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify mutation, no-op, rollback and dormant-membership behavior</name>
  <read_first>137-CONTEXT.md D02/D06/D07/D08; review_service.go; repository/authz_user_overrides.go; existing concurrency test pattern</read_first>
  <files>backend/internal/services/effective_rights_service_test.go, backend/internal/services/effective_rights_concurrency_test.go</files>
  <behavior>
    - none->allow, none->deny, allow->deny, deny->allow, allow->none, deny->none create exactly one history transition.
    - allow->allow, deny->deny, none->none are no-op with no history append.
    - Missing/blank required reason on real mutation is rejected according to the Phase-136 contract; no-op handling must not fabricate an audit event.
    - New mutation against inactive/non-member target is rejected.
    - Stored override can remain dormant when membership later becomes inactive; service does not auto-delete it.
    - Actor must hold the dedicated management capability in the target group; self-mutation is allowed only under that same rule.
    - Non-overridable/unknown actions are rejected before persistence.
    - Forced history insert failure rolls back override state.
    - Two conflicting concurrent mutations produce a valid serial order and matching immutable history.
  </behavior>
  <action>Build integration/service tests first. Reuse the release-review barrier-channel pattern for the race and the Phase-137 Postgres harness for actual transactional behavior.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/services -run 'EffectiveRights|Override.*Concurrency' -count=1</automated></verify>
  <done>The complete D06 mutation state machine and failure modes are executable tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement transactional EffectiveRightsService mutation</name>
  <read_first>effective_rights_service_test.go; effective_rights_concurrency_test.go; review_service.go; repository/authz_user_overrides.go; permissions/effective_rights.go</read_first>
  <files>backend/internal/services/effective_rights_service.go</files>
  <action>Mirror the established Begin -> defer Rollback -> transaction repositories -> authorize -> validate -> lock -> compare -> mutate -> append history -> Commit pattern. Authorize using the new dedicated management capability against the target group, with platform-admin flowing through the existing resolver bypass. Validate active target membership and user_overridable=true server-side; rely on the DB constraints as fail-closed backstop. Exact before==after returns no_op without history. Successful Phase-137 results use activation_status=active. Do not create a cache or publish pending states.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/services -run 'EffectiveRights|Override.*Concurrency' -count=1 &amp;&amp; git diff --check</automated></verify>
  <done>All mutation, rollback, idempotency and concurrency tests pass with one atomic transaction per real change.</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Privilege escalation/self-grant | Dedicated target-group management capability checked before mutation. |
| Audit gap | State + history in one transaction; failure rolls back. |
| Race / lost update | Target/current state locking and deterministic concurrency tests. |
| Membership bypass | Active target membership required for new mutation. |
</threat_model>

<verification>Focused service + concurrency tests and `git diff --check`.</verification>
<success_criteria>Every real override transition is authorized, valid, atomic and auditable; exact retries are harmless no-ops.</success_criteria>
<output>After completion, create `.planning/phases/137-central-effective-rights-resolver-overrides/137-06-SUMMARY.md`.</output>
