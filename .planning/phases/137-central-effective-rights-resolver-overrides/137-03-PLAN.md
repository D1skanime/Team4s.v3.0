---
phase: 137-central-effective-rights-resolver-overrides
plan: 03
type: execute
wave: 2
depends_on: [137-01]
files_modified:
  - backend/internal/repository/authz_user_overrides.go
  - backend/internal/repository/authz_user_overrides_test.go
  - backend/internal/testsupport/phase137_postgres.go
autonomous: true
requirements: [CAP-01, CAP-02, CAP-05, CAP-06, CAP-07, QUAL-03]
must_haves:
  truths:
    - "One repository surface can batch-load current overrides for one user+fansub-group without querying per capability."
    - "Mutation support can lock the active target membership and current override state safely inside a caller-owned transaction."
    - "History is inserted append-only and listed only within an explicit group scope."
    - "Repository methods use repository.DBTX so the same code works on pool and transaction."
  artifacts:
    - path: "backend/internal/repository/authz_user_overrides.go"
      provides: "Batched load, lock/upsert/delete and history persistence for user overrides"
    - path: "backend/internal/testsupport/phase137_postgres.go"
      provides: "Real-Postgres safety harness for Phase 137"
  key_links:
    - from: "authz_user_overrides.go"
      to: "user_group_capability_overrides"
      via: "group-scoped batched current-state query"
      pattern: "fansub_group_id"
    - from: "mutation repository"
      to: "fansub_group_members"
      via: "active target membership row lock"
      pattern: "FOR UPDATE"
---

<objective>
Provide transaction-safe, batch-oriented persistence primitives for the resolver and mutation service.

Purpose: Keep SQL and row-locking details out of the decision engine while preventing N+1 and cross-group access patterns.
Output: New repository module, real-Postgres tests and Phase-137 test harness.
</objective>

<context>
@AI-HANDOFF.md
@.planning/phases/137-central-effective-rights-resolver-overrides/137-CONTEXT.md
@.planning/phases/137-central-effective-rights-resolver-overrides/137-RESEARCH.md
@backend/internal/repository/authz_permissions.go
@backend/internal/repository/review_delegation_repository.go
@database/migrations/0146_capability_policy_catalog.up.sql
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Establish the Phase-137 real-Postgres harness and repository behavior tests</name>
  <read_first>backend/internal/testsupport/phase117_postgres.go; backend/internal/testsupport/phase107_postgres.go; repository test conventions; migration 0146; 137-RESEARCH.md Pattern 3/4</read_first>
  <files>backend/internal/testsupport/phase137_postgres.go, backend/internal/repository/authz_user_overrides_test.go</files>
  <behavior>
    - Batched load returns all current overrides for exactly one app_user_id + fansub_group_id.
    - Foreign group rows never leak into the result.
    - Active target membership can be resolved and locked by user+group.
    - Inactive/non-member target is distinguishable to the service without exposing unrelated group data.
    - Upsert/delete operations expose before/after state to the caller.
    - History listing is always constrained by fansub_group_id and subject.
  </behavior>
  <action>Create the safe test harness and failing repository integration tests first, following the existing phase107/117 DSN guards and real-Postgres conventions.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/repository -run Phase137 -count=1</automated></verify>
  <done>Tests define the batch/load/lock/history contract and fail before the new repository exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement batch and transactional override repository primitives</name>
  <read_first>backend/internal/repository/authz_user_overrides_test.go; backend/internal/repository/review_delegation_repository.go; backend/internal/repository/authz_permissions.go; database/migrations/0146_capability_policy_catalog.up.sql</read_first>
  <files>backend/internal/repository/authz_user_overrides.go</files>
  <action>Implement a new repository file using the existing DBTX abstraction. Provide only the primitives needed by the resolver/service: batch current overrides for user+group; resolve/lock target membership; read/lock current override state; upsert allow/deny; delete; append history; group-scoped history listing; capability-catalog validation data as appropriate. Prefer the minimum number of grouped queries and never issue SQL per action code. Do not add resolver business precedence here.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/repository -run Phase137 -count=1 &amp;&amp; git diff --check</automated></verify>
  <done>Repository tests pass with group-scoped, transaction-compatible, batch-oriented persistence.</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| BOLA/IDOR through user/group IDs | Every query includes explicit group scope; target membership is server-resolved. |
| Audit/state race | Lock-capable primitives support one caller-owned transaction. |
| N+1 | Current overrides are loaded as one set, never per action. |
</threat_model>

<verification>Real-Postgres repository tests and `git diff --check`.</verification>
<success_criteria>The resolver and mutation service can consume secure, grouped facts without embedding SQL.</success_criteria>
<output>After completion, create `.planning/phases/137-central-effective-rights-resolver-overrides/137-03-SUMMARY.md`.</output>
