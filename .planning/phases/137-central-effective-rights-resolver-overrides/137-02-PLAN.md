---
phase: 137-central-effective-rights-resolver-overrides
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/contracts/admin-capabilities.yaml
  - shared/contracts/openapi.yaml
  - frontend/src/types/admin-capability.ts
  - backend/internal/handlers/admin_capability_contract_test.go
autonomous: true
requirements: [CAP-01, CAP-02, CAP-05, CAP-07]
must_haves:
  truths:
    - "EffectiveRightState is extended additively rather than replaced by a competing richer inspector DTO."
    - "The contract can represent all granting roles, direct allow/deny state, specialized grants, decisive source, non-deniable state and a machine-readable reason code."
    - "Existing Phase-136 fields and enum values remain compatible."
    - "Successful Phase-137 override mutations report activation_status=active; pending/failed remain schema-compatible but are not emitted by Phase 137."
  artifacts:
    - path: "shared/contracts/admin-capabilities.yaml"
      provides: "Canonical additive effective-rights and override API schemas"
    - path: "shared/contracts/openapi.yaml"
      provides: "Root OpenAPI mirror"
    - path: "frontend/src/types/admin-capability.ts"
      provides: "TypeScript mirror for Phase 138+ consumers"
  key_links:
    - from: "EffectiveRightState"
      to: "runtime GroupRightsResolution projection"
      via: "one provenance-capable response contract"
      pattern: "granting_roles|specialized_grants|decisive_source|reason_code"
---

<objective>
Close the Phase-136 DTO provenance gap before backend routes consume the contract.

Purpose: D04 requires all sources plus the decisive source. The existing five-field DTO cannot express that truth.
Output: Additively extended OpenAPI/TS contract with no parallel decision DTO.
</objective>

<context>
@AI-HANDOFF.md
@.planning/phases/137-central-effective-rights-resolver-overrides/137-CONTEXT.md
@.planning/phases/137-central-effective-rights-resolver-overrides/137-RESEARCH.md
@shared/contracts/admin-capabilities.yaml
@shared/contracts/openapi.yaml
@frontend/src/types/admin-capability.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend EffectiveRightState and provenance vocabulary additively</name>
  <read_first>137-CONTEXT.md; 137-RESEARCH.md; shared/contracts/admin-capabilities.yaml; shared/contracts/openapi.yaml; frontend/src/types/admin-capability.ts; existing contract mirror tests</read_first>
  <files>shared/contracts/admin-capabilities.yaml, shared/contracts/openapi.yaml, frontend/src/types/admin-capability.ts, backend/internal/handlers/admin_capability_contract_test.go</files>
  <behavior>
    - Preserve action_code, allowed, provenance, decisive and non_deniable.
    - Add granting_roles:string[], user_allow:boolean, user_deny:boolean, specialized_grants:string[], decisive_source and reason_code.
    - Extend provenance/decisive vocabulary so platform_admin, specialized_grant and no_grant/default_deny can be represented without removing previous values.
    - Preserve existing CapabilityOverrideState/Mutation/Audit schemas unless an additive field is necessary.
    - Keep activation_status enum compatibility but document/test that Phase 137 emits active for a successful mutation.
  </behavior>
  <action>Update the canonical contract and both mirrors together. Add focused contract assertions that prevent the YAML/OpenAPI/TS shapes from drifting. Do not introduce a second richer inspector state type.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/handlers -run 'Capability.*Contract|EffectiveRight' -count=1 &amp;&amp; git diff --check</automated></verify>
  <done>The contract expresses D04's full provenance model additively and mirrors remain aligned.</done>
</task>
</tasks>

<verification>Focused contract tests and `git diff --check`.</verification>
<success_criteria>Phase 137 has one compatible effective-right representation capable of serving both mutation results and inspection.</success_criteria>
<output>After completion, create `.planning/phases/137-central-effective-rights-resolver-overrides/137-02-SUMMARY.md`.</output>
