---
phase: 137-central-effective-rights-resolver-overrides
plan: 07
type: execute
wave: 5
depends_on: [137-02, 137-05, 137-06]
files_modified:
  - backend/internal/handlers/admin_effective_rights_handler.go
  - backend/internal/handlers/admin_effective_rights_handler_test.go
  - backend/internal/router/router.go
  - shared/contracts/admin-capabilities.yaml
  - shared/contracts/openapi.yaml
autonomous: true
requirements: [CAP-01, CAP-02, CAP-05, CAP-06, CAP-07, QUAL-03]
must_haves:
  truths:
    - "An authorized group administrator can inspect the complete effective-rights list for one active member of exactly that group."
    - "Inspection is a projection of ResolveGroupRights, not a second computation."
    - "Mutation supports allow/deny/remove through the locked contract and delegates to EffectiveRightsService."
    - "Override history is readable only within the caller's authorized group scope."
    - "Foreign group/user/membership/history identifiers do not disclose cross-group existence and are rejected neutrally using existing API conventions."
    - "No Phase-138 administration UI is implemented."
  artifacts:
    - path: "backend/internal/handlers/admin_effective_rights_handler.go"
      provides: "Inspection, mutation and history HTTP endpoints"
    - path: "backend/internal/handlers/admin_effective_rights_handler_test.go"
      provides: "Contract, authorization and BOLA/IDOR tests"
    - path: "shared/contracts/admin-capabilities.yaml"
      provides: "Path operations for Phase-137 backend API"
  key_links:
    - from: "inspection endpoint"
      to: "ResolveGroupRights"
      via: "single group resolution"
      pattern: "ResolveGroupRights"
    - from: "mutation endpoint"
      to: "EffectiveRightsService"
      via: "validated target-group command"
      pattern: "MutateOverride"
---

<objective>
Expose the Phase-137 backend/API boundary for inspection, mutation and immutable history.

Purpose: Make the resolver and override service consumable by Phase 138 without building Phase-138 UX.
Output: Group-scoped handlers/routes/contracts with negative BOLA/IDOR coverage.
</objective>

<context>
@AI-HANDOFF.md
@.planning/phases/137-central-effective-rights-resolver-overrides/137-CONTEXT.md
@.planning/phases/137-central-effective-rights-resolver-overrides/137-RESEARCH.md
@backend/internal/handlers/fansub_hist_group_member_roles_handler.go
@backend/internal/handlers/admin_capability_handler.go
@backend/internal/handlers/permission_authz.go
@backend/internal/services/effective_rights_service.go
@backend/internal/permissions/effective_rights.go
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Lock the group-scoped HTTP contract and BOLA/IDOR behavior in handler tests</name>
  <read_first>137-CONTEXT.md D08/D10; shared contracts; current admin handler routing/error conventions; fansub_hist_group_member_roles_handler.go</read_first>
  <files>backend/internal/handlers/admin_effective_rights_handler_test.go, shared/contracts/admin-capabilities.yaml, shared/contracts/openapi.yaml</files>
  <behavior>
    - Inspection returns the complete relevant capability list for target user+group in one response.
    - Response includes full additive provenance fields.
    - Mutation accepts allow, deny and remove according to the existing Phase-136 request model.
    - History is group-scoped.
    - A caller authorized only for group A cannot inspect/mutate/read history for group B.
    - Manipulated group_id/target_user_id/body-vs-path mismatch is rejected before domain mutation.
    - Unknown/non-overridable action errors are neutral/consistent with existing 4xx conventions.
    - Inactive target membership cannot receive a new override.
  </behavior>
  <action>Define route operations following existing Team4s admin/fansub route conventions; prefer a group ID in the path as the authorization-bearing scope and cross-check any legacy body group_id instead of trusting it. Extend the already-canonical contract rather than inventing a separate endpoint DTO family.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/handlers -run 'EffectiveRights|Override|BOLA|IDOR' -count=1</automated></verify>
  <done>Tests define complete inspection/mutation/history behavior and cross-group rejection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement inspection, mutation and history handlers/routes</name>
  <read_first>admin_effective_rights_handler_test.go; effective_rights_service.go; permissions/effective_rights.go; existing handler helpers/router conventions</read_first>
  <files>backend/internal/handlers/admin_effective_rights_handler.go, backend/internal/router/router.go</files>
  <action>Implement thin handlers. Resolve actor through existing auth middleware, authorize/resolve against the target group, call ResolveGroupRights exactly once for inspection, delegate mutations to EffectiveRightsService, and list history only with explicit group scope. Map foreign-scoped resources to the project's neutral not-found/422 behavior without revealing foreign existence. Do not add any frontend/UI work.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/handlers -run 'EffectiveRights|Override|BOLA|IDOR' -count=1 &amp;&amp; git diff --check</automated></verify>
  <done>Backend routes satisfy CAP-01/02/05/06/07 and negative group-scope tests pass.</done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Cross-group inspection | Path-scoped authorization + server-side membership resolution. |
| Cross-group mutation | Target user/group cross-check before service mutation. |
| Enumeration | Reuse neutral existing not-found/422 conventions; no foreign-membership detail. |
</threat_model>

<verification>Focused handler/security tests plus `git diff --check`.</verification>
<success_criteria>Phase 138 can consume secure effective-rights/mutation/history APIs without any duplicated permission logic.</success_criteria>
<output>After completion, create `.planning/phases/137-central-effective-rights-resolver-overrides/137-07-SUMMARY.md`.</output>
