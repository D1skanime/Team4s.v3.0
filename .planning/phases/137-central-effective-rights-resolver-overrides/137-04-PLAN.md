---
phase: 137-central-effective-rights-resolver-overrides
plan: 04
type: execute
wave: 3
depends_on: [137-02, 137-03]
files_modified:
  - backend/internal/permissions/effective_rights.go
  - backend/internal/permissions/effective_rights_test.go
  - backend/internal/permissions/review_grant_provider.go
autonomous: true
requirements: [CAP-01, CAP-02, CAP-03, QUAL-03]
must_haves:
  truths:
    - "ResolveGroupRights is the single provenance-capable group-wide decision primitive for one actor+fansub-group."
    - "The resolver evaluates platform-admin > disabled > no-active-membership > user-deny > user-allow > role-grant > specialized-grant > default-deny."
    - "Dormant overrides remain visible as stored data but have no effective impact without active membership."
    - "All granting roles and specialized grants are preserved in provenance even when a higher-precedence deny wins."
    - "Review delegation is the first SpecializedGrantProvider and does not become a generic override row."
    - "No SQL is executed per capability."
  artifacts:
    - path: "backend/internal/permissions/effective_rights.go"
      provides: "GroupRightsResolution, per-action result state, precedence evaluator and resolver"
    - path: "backend/internal/permissions/review_grant_provider.go"
      provides: "SpecializedGrantProvider adapter over existing ReviewGrantContext resolution"
    - path: "backend/internal/permissions/effective_rights_test.go"
      provides: "Pure-Go precedence/provenance matrix"
  key_links:
    - from: "ResolveGroupRights"
      to: "role grants + user overrides + specialized providers"
      via: "batched source acquisition then in-memory evaluation"
      pattern: "ResolveGroupRights"
---

<objective>
Build the central Effective-Rights Resolver and make the approved precedence executable in one place.

Purpose: Enforcement and inspection must consume the same decision truth, including full provenance.
Output: New permission files with group-wide resolution, specialized-provider seam and exhaustive pure-Go tests.
</objective>

<context>
@AI-HANDOFF.md
@.planning/phases/137-central-effective-rights-resolver-overrides/137-CONTEXT.md
@.planning/phases/137-central-effective-rights-resolver-overrides/137-RESEARCH.md
@backend/internal/permissions/permissions.go
@backend/internal/repository/authz_permissions.go
@backend/internal/repository/authz_user_overrides.go
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Encode D01-D05 precedence and provenance as table-driven unit tests</name>
  <read_first>137-CONTEXT.md D01-D05; 137-RESEARCH.md Pattern 1/2 and Pitfall 2/4; backend/internal/permissions/permissions.go</read_first>
  <files>backend/internal/permissions/effective_rights_test.go</files>
  <behavior>
    - Platform admin always allows and is non-deniable.
    - Disabled actor denies.
    - No active membership denies even if dormant allow exists.
    - User deny beats user allow, role grant and specialized review grant.
    - User allow grants without a role but only with active membership.
    - Multiple role grants are OR-combined and all granting roles remain visible.
    - Specialized review grant allows only when no higher-precedence source denies.
    - Default/no-grant denies with deterministic reason/decisive source.
    - Provenance preserves losing lower-precedence sources.
  </behavior>
  <action>Write pure-Go tests around in-memory source fixtures before implementing the resolver. Include the complete negative matrix from 137-CONTEXT.md that does not require HTTP/DB.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/permissions -run 'EffectiveRights|ResolveGroupRights' -count=1</automated></verify>
  <done>The tests uniquely define the approved decision semantics and initially fail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GroupRightsResolution and centralized precedence</name>
  <read_first>backend/internal/permissions/effective_rights_test.go; permissions.go; repository/authz_permissions.go; repository/authz_user_overrides.go</read_first>
  <files>backend/internal/permissions/effective_rights.go</files>
  <action>Create the new resolver file rather than growing permissions.go beyond its existing debt. Define GroupRightsResolution and per-action state capable of projecting the additive EffectiveRightState contract. Batch-load membership/roles/role-capabilities/overrides/specialized grants at group granularity and evaluate all relevant canonical actions in memory. Do not require exactly one round trip per category: optimize for no duplicate loads, no per-capability SQL and no N+1. Preserve existing platform-admin and disabled semantics.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/permissions -run 'EffectiveRights|ResolveGroupRights' -count=1</automated></verify>
  <done>The central resolver passes the full pure-Go precedence/provenance matrix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wrap existing review delegation as SpecializedGrantProvider</name>
  <read_first>137-CONTEXT.md D05; repository/authz_permissions.go ResolveActorReviewGrantContext; permissions.go CanReviewForFansubGroup</read_first>
  <files>backend/internal/permissions/review_grant_provider.go, backend/internal/permissions/effective_rights_test.go</files>
  <action>Introduce the smallest internal SpecializedGrantProvider seam and adapt the existing ResolveActorReviewGrantContext result as the first provider. Do not duplicate the review query and do not migrate review grants into user_group_capability_overrides. Add a test proving Review Delegation + User Deny resolves DENY while provenance still contains review_delegation.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/permissions -run 'EffectiveRights|Review.*Grant|ResolveGroupRights' -count=1 &amp;&amp; git diff --check</automated></verify>
  <done>Review delegation participates as a specialized grant source and obeys D01 precedence.</done>
</task>
</tasks>

<verification>Pure-Go resolver suite plus `git diff --check`.</verification>
<success_criteria>One resolver can explain and decide every relevant group-scoped capability from all approved source categories.</success_criteria>
<output>After completion, create `.planning/phases/137-central-effective-rights-resolver-overrides/137-04-SUMMARY.md`.</output>
