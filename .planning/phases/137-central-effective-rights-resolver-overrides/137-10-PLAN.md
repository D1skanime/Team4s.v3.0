---
phase: 137-central-effective-rights-resolver-overrides
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/contracts/admin-capabilities.yaml
  - shared/contracts/openapi.yaml
autonomous: true
requirements: [CAP-01, CAP-02]
gap_closure: true

must_haves:
  truths:
    - "GET /api/v1/admin/fansubs/{id}/app-members/{appUserId}/effective-rights documents a 400 response in both shared/contracts/admin-capabilities.yaml and shared/contracts/openapi.yaml, matching the handler's actual badRequest behavior for a malformed id or appUserId path parameter."
    - "GET /api/v1/admin/fansubs/{id}/app-members/{appUserId}/capability-overrides/history documents a 400 response in both contract files, matching the same handler behavior."
    - "No other endpoint, response code, or schema in either contract file changes."
  artifacts:
    - path: shared/contracts/admin-capabilities.yaml
      provides: "400 response blocks added to both GET effective-rights endpoints"
      contains: "Ungültige Fansubgruppen-ID"
    - path: shared/contracts/openapi.yaml
      provides: "400 response blocks added to both GET effective-rights endpoints"
      contains: "Invalid path parameter"
  key_links:
    - from: "admin-capabilities.yaml GET .../effective-rights responses"
      to: "openapi.yaml GET .../effective-rights responses"
      via: "phase136_contract_parity_test.go cross-file comparison"
      pattern: "\"400\""
    - from: "admin-capabilities.yaml GET .../capability-overrides/history responses"
      to: "openapi.yaml GET .../capability-overrides/history responses"
      via: "phase136_contract_parity_test.go cross-file comparison"
      pattern: "\"400\""
---

<objective>
Close GAP-03 from 137-UAT.md: document the already-reachable `400 Bad Request` response for
`AdminEffectiveRightsHandler.GetEffectiveRights` and `AdminEffectiveRightsHandler.ListOverrideHistory`
in both `shared/contracts/admin-capabilities.yaml` and `shared/contracts/openapi.yaml`, which
currently only document `401/403/404/500` (effective-rights) and `401/403/500` (history) — omitting
`400` even though `parseGroupAndTarget` calls `badRequest` (HTTP 400) for both endpoints whenever
`:id` or `:appUserId` fails to parse as a positive integer.

Purpose: 137-REVIEW.md (WR-03) confirms this is a real contract/behavior mismatch — the handler's
actual status-code surface and the OpenAPI contract disagree, and neither `phase136_contract_parity_test.go`
nor `admin_capability_contract_test.go` catches it since those tests compare the two YAML files
against each other and against Go/TS DTOs, not against actual handler status-code behavior.

Output: both contract files gain a `400` response block on both GET endpoints, keeping the two
files consistent with each other and matching real handler behavior; no other endpoint or schema
changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-central-effective-rights-resolver-overrides/137-UAT.md
@.planning/phases/137-central-effective-rights-resolver-overrides/137-PATTERNS.md
@.planning/phases/137-central-effective-rights-resolver-overrides/137-REVIEW.md
</context>

<interfaces>
<!-- Exact current handler behavior being documented; no handler code is touched by this plan. -->

backend/internal/handlers/admin_effective_rights_handler.go's parseGroupAndTarget (lines 96-108,
unchanged by this plan) calls parseFansubID(c.Param("id")) and returns badRequest(c, "ungültige
fansub id") on failure, or strconv.ParseInt(c.Param("appUserId"), ...) with badRequest(c, "ungültige
app_user_id") on failure/non-positive value. This is called by both GetEffectiveRights and
ListOverrideHistory (and by MutateOverride, which already documents 400 correctly in both contract
files — use that existing block as the description-wording analog, adapted from "request body" to
"path parameter").

Existing exact-shape 400 block already used for the identical "malformed path ID" case in
shared/contracts/admin-capabilities.yaml at lines 39-43 (GET /api/v1/admin/fansubs/{id}/capabilities):
a "400" key with description "Ungültige Fansubgruppen-ID" and content.application/json.schema $ref
"#/components/schemas/ErrorResponse" — copy this shape verbatim into both GET endpoints this plan
targets, ordering it immediately after "200" and before "401" to match this file's existing status
ordering convention (200, 400, 401, 403, ...).

shared/contracts/openapi.yaml uses the same $ref schema and its own English-description convention
already visible on neighboring blocks for these same two endpoints (e.g. "401": description:
"Authentication required") — mirror that phrasing style for 400, e.g. "Invalid path parameter (id
or appUserId)".
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add missing 400 response blocks to both GET effective-rights endpoints in both contract files</name>
  <files>shared/contracts/admin-capabilities.yaml, shared/contracts/openapi.yaml</files>
  <read_first>
    shared/contracts/admin-capabilities.yaml lines 1-50 (the 400 analog block at lines 39-43) and
    lines 255-436 (both target GET path items and their current responses blocks: effective-rights
    at ~259-313, history at ~381-436, plus the sibling PUT .../capability-overrides 400 block at
    ~352-357 for wording-analog only);
    shared/contracts/openapi.yaml lines 4155-4345 (same two GET path items in English: effective-rights
    responses at ~4189-4218, history responses at ~4310-4340, plus the sibling PUT endpoint's
    existing 400 block for wording-analog only);
    backend/internal/handlers/admin_effective_rights_handler.go lines 94-108 (parseGroupAndTarget,
    the exact behavior being documented — read-only, not modified).
  </read_first>
  <action>
    In shared/contracts/admin-capabilities.yaml, insert one new "400" response entry into the
    responses block of GET /api/v1/admin/fansubs/{id}/app-members/{appUserId}/effective-rights,
    placed immediately after the existing "200" entry and before the existing "401" entry. Use the
    exact block shape already present at lines 39-43 (description: "Ungültige Fansubgruppen-ID oder
    ungültige app_user_id", content.application/json.schema $ref "#/components/schemas/ErrorResponse")
    — adjust the description text to mention both id and appUserId since parseGroupAndTarget rejects
    either. Insert an identical-shaped "400" entry (same description text) into the responses block
    of GET /api/v1/admin/fansubs/{id}/app-members/{appUserId}/capability-overrides/history,
    immediately after "200" and before "401".

    In shared/contracts/openapi.yaml, insert the equivalent "400" entries at the same two GET path
    items' responses blocks, same insertion position (after "200", before "401"), using English
    phrasing consistent with this file's own convention on neighboring blocks — description "Invalid
    path parameter (id or appUserId)" — and the same $ref: "#/components/schemas/ErrorResponse"
    schema reference the sibling PUT endpoint in this same file already uses.

    Do not modify any other response code, path item, schema, or endpoint in either file. Do not
    reorder or reformat any content outside the exact two responses blocks touched.
  </action>
  <verify>
    <automated>grep -A5 '"400"' shared/contracts/admin-capabilities.yaml | grep -c "ErrorResponse"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '"400"' on the responses block for each of the 4 touched path-item/file combinations
      shows the new entry present (2 files x 2 endpoints = 4 new "400" keys total).
    - Both YAML files remain valid YAML (no parse errors) — confirmed by the automated command below.
    - No diff exists outside the two GET path items' responses blocks in either file (git diff
      --stat shows only these two files changed; git diff shows only 400-block additions, no other
      line changes).
  </acceptance_criteria>
  <done>Both shared/contracts/admin-capabilities.yaml and shared/contracts/openapi.yaml document 400 for both GET effective-rights/history endpoints, matching real handler behavior, with no unrelated changes.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run contract parity and TypeScript checks</name>
  <files>shared/contracts/admin-capabilities.yaml, shared/contracts/openapi.yaml</files>
  <read_first>
    backend/internal/handlers/phase136_contract_parity_test.go (TestPhase136ContractParity — cross-file
    YAML comparison this task must keep green);
    backend/internal/handlers/admin_capability_contract_test.go (TestEffectiveRightStateProvenanceContract,
    TestCapabilityOverrideSchemasUnchangedContract — DTO/schema shape parity tests unaffected by a
    response-code-only change, run as a regression check).
  </read_first>
  <action>
    Run the backend contract parity test suite and the frontend TypeScript compiler to confirm
    Task 1's edits did not break YAML structure or cross-file/DTO parity, and that no unrelated drift
    was introduced. This is a verification-only task — no further file edits are expected unless a
    parsing or parity failure surfaces, in which case fix only the malformed block from Task 1 (do
    not touch unrelated content).
  </action>
  <verify>
    <automated>docker compose exec team4sv30-backend go test ./internal/handlers -run 'TestPhase136ContractParity' -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - docker compose exec team4sv30-backend go test ./internal/handlers -run 'TestPhase136ContractParity|Phase136|EffectiveRightState|CapabilityOverrideSchemasUnchanged' -v -count=1
      passes with zero failures.
    - docker compose exec team4sv30-frontend npx tsc --noEmit reports zero new errors versus the
      pre-existing baseline (137-VERIFICATION.md recorded zero admin-capability/EffectiveRight
      errors before this plan; confirm the same after).
    - git diff --check shared/contracts/admin-capabilities.yaml shared/contracts/openapi.yaml reports
      no whitespace errors.
  </acceptance_criteria>
  <done>Contract parity tests and tsc --noEmit both pass after the 400-block additions, confirming no structural or cross-file drift was introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Contract consumers (frontend TS client, future SDK generators) -> OpenAPI contract files | Documentation-only boundary; this plan changes only documented response metadata for an already-existing, already-enforced handler behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-10-01 | Tampering (contract/code drift) | admin-capabilities.yaml / openapi.yaml GET endpoint response docs | mitigate | Closes a real, previously-undocumented reachable status code (WR-03) so client-generated tooling and future contract-parity tests correctly model the handler's actual 400 path, reducing the risk of a client mishandling an undocumented status as an unexpected error. |
| T-137-10-02 | Information Disclosure | New 400 response body ($ref ErrorResponse) | accept | Reuses the existing generic ErrorResponse schema already used by every other 400/401/403/404/500 block in both files; discloses nothing beyond what the schema already exposes elsewhere. |
| T-137-10-SC | Tampering | npm/pip/cargo installs | accept | No package-manager dependency change in this plan. |
</threat_model>

<verification>
Run the contract parity test suite (TestPhase136ContractParity and admin_capability_contract_test.go's
tests) and `npx tsc --noEmit` in the frontend container; confirm both green and confirm `git diff`
touches only the two intended 400 blocks per file.
</verification>

<success_criteria>
Both GET effective-rights and GET override-history endpoints document 400 in both contract files,
matching actual handler behavior; contract parity and TypeScript checks remain green; no unrelated
API surface changed.
</success_criteria>

<output>
Create `.planning/phases/137-central-effective-rights-resolver-overrides/137-10-SUMMARY.md` when done
</output>
