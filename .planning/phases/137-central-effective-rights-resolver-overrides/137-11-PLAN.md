---
phase: 137-central-effective-rights-resolver-overrides
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/permissions/effective_rights.go
  - backend/internal/services/effective_rights_service.go
autonomous: true
requirements: [QUAL-03]
gap_closure: true

must_haves:
  truths:
    - "effective_rights.go's file-level doc comment no longer claims that production per-user override wiring is unavailable or that ResolveGroupRights degrades to a role-only shape in production; it instead states the gap was closed in Plan 137-05."
    - "effective_rights_service.go's error-sentinel var block is gofmt-clean (no diff under gofmt -l)."
    - "No runtime/behavioral change is introduced by either edit — both files' actual logic is byte-identical before and after except for the comment text and whitespace-only formatting."
  artifacts:
    - path: backend/internal/permissions/effective_rights.go
      provides: "Corrected file-level doc comment (GAP-04 / WR-01)"
    - path: backend/internal/services/effective_rights_service.go
      provides: "gofmt-clean error-sentinel var block (GAP-05 / WR-06)"
  key_links:
    - from: "effective_rights.go doc comment"
      to: "backend/internal/repository/authz_permissions.go ResolveActorGroupMembership/ResolveActorUserOverrides"
      via: "corrected prose reference, no code link"
      pattern: "137-05"
---

<objective>
Close GAP-04 and GAP-05 from 137-UAT.md: correct the stale file-level doc comment in
`backend/internal/permissions/effective_rights.go` that still claims per-user override
production-wiring is unavailable (WR-01 in 137-REVIEW.md, already contradicted by
`authz_permissions.go`'s implementation and by 137-VERIFICATION.md's independent confirmation), and
fix the `gofmt`-misaligned error-sentinel `var (...)` block in
`backend/internal/services/effective_rights_service.go` (WR-06).

Purpose: A future maintainer reading only `effective_rights.go`'s header would wrongly conclude that
per-user overrides are not enforced in production and could reintroduce a "known gap" workaround or
distrust a working security control. The `gofmt` misalignment is a mechanical formatting drift that
would surface as a diff under any `gofmt -l`/CI formatting check.

Output: both files with corrected comment text / formatting only — zero behavioral change, per
137-UAT.md's explicit "GAP-04 explicitly forbids any runtime change — comment-only" and "no
large-scale cosmetic changes in unrelated areas" constraints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-central-effective-rights-resolver-overrides/137-UAT.md
@.planning/phases/137-central-effective-rights-resolver-overrides/137-PATTERNS.md
@.planning/phases/137-central-effective-rights-resolver-overrides/137-REVIEW.md
</context>

<interfaces>
<!-- Ground truth that makes the current comment stale; no code here is modified by this plan. -->

backend/internal/repository/authz_permissions.go lines 386-391 carry two compile-time interface
assertions (`var _ permissions.GroupRightsMembershipResolver = (*AuthzRepository)(nil)`, `var _
permissions.GroupRightsOverridesResolver = (*AuthzRepository)(nil)`) and the two concrete methods
ResolveActorGroupMembership (line 325) and ResolveActorUserOverrides (line 361), proven against real
Postgres by TestPhase137AuthzRepositoryImplementsGroupRightsOptionalInterfaces. main.go wires this
same concrete AuthzRepository into permissions.NewService in production. This closes the exact gap
137-04-SUMMARY.md flagged and 137-05-SUMMARY.md/137-VERIFICATION.md confirm closed.

frontend/src/types/admin-capability.ts and both shared/contracts/*.yaml files already carry
EffectiveRightState's extended provenance fields (granting_roles, user_allow, user_deny,
specialized_grants, decisive_source, reason_code) per 137-VERIFICATION.md — the second stale
paragraph's claim that the Go DTO/HTTP projection gap is still open is also false; it closed in
Plan 137-02/137-07.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: GAP-04 — correct the stale production-wiring doc comment</name>
  <files>backend/internal/permissions/effective_rights.go</files>
  <read_first>
    backend/internal/permissions/effective_rights.go lines 1-45 (full current file-level doc comment,
    reproduced exactly in this plan's context above);
    .planning/phases/137-central-effective-rights-resolver-overrides/137-REVIEW.md WR-01 section
    (exact staleness finding and required fix shape);
    .planning/phases/137-central-effective-rights-resolver-overrides/137-PATTERNS.md GAP-04 section
    (ground truth: authz_permissions.go:325,361,386-391).
  </read_first>
  <action>
    Replace the two stale paragraphs in effective_rights.go's file-level doc comment — the paragraph
    beginning "Known production-wiring gap (documented rather than silently worked around): the
    concrete resolver backing production Service instances..." (lines 23-33) and the paragraph
    beginning "The Go DTO consumed by the admin HTTP layer..." (lines 35-38) — with a short,
    accurate replacement stating that both gaps closed: production wiring closed in Plan 137-05
    (backend/internal/repository/authz_permissions.go implements both GroupRightsMembershipResolver
    and GroupRightsOverridesResolver via ResolveActorGroupMembership/ResolveActorUserOverrides,
    proven by TestPhase137AuthzRepositoryImplementsGroupRightsOptionalInterfaces against real
    Postgres, and wired into production via main.go), and the Go DTO/HTTP projection extension
    closed in Plan 137-02/137-07 (EffectiveRightState in
    backend/internal/handlers/capability_policy_contract.go and both shared/contracts/*.yaml files
    already carry the full D04 provenance shape). Keep the surrounding architectural description
    (GroupRightsResolution/ResolveGroupRights summary, precedence order, dormant-overrides
    description at lines 1-21) completely unchanged — only the two stale paragraphs are replaced.
    Do not change any code below the comment block (import block, type definitions, function
    bodies) — this is a documentation-only edit.
  </action>
  <verify>
    <automated>docker compose exec team4sv30-backend go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - git diff backend/internal/permissions/effective_rights.go shows changes confined to the
      comment block (lines ~22-38); zero diff outside comment lines.
    - The replaced comment text no longer contains the words "does not yet implement", "Until a
      later plan wires them", "not yet live end-to-end", or "has not been extended to this additive
      shape yet".
    - docker compose exec team4sv30-backend go build ./... succeeds (comment-only change cannot
      break compilation, but confirms no accidental code touch).
    - docker compose exec team4sv30-backend go test ./internal/permissions/... -count=1 passes with
      identical results to the pre-change baseline (same pass count, zero new failures) — proves no
      behavioral change.
  </acceptance_criteria>
  <done>effective_rights.go's file-level doc comment accurately states both the production-wiring gap and the DTO extension gap are closed (Plan 137-05 / 137-02+137-07 respectively), with zero code/behavior change.</done>
</task>

<task type="auto">
  <name>Task 2: GAP-05 — gofmt the error-sentinel var block</name>
  <files>backend/internal/services/effective_rights_service.go</files>
  <read_first>
    backend/internal/services/effective_rights_service.go lines 75-90 (current misaligned var block,
    reproduced exactly in this plan's context above);
    .planning/phases/137-central-effective-rights-resolver-overrides/137-REVIEW.md WR-06 section
    (exact misalignment: ErrEffectiveRightsTargetNotActiveMember's = breaks the column alignment
    gofmt would otherwise produce for the whole block).
  </read_first>
  <action>
    Run gofmt -w on backend/internal/services/effective_rights_service.go only. This mechanically
    re-aligns the var (...) block declaring ErrEffectiveRightsMutationInvalid /
    ErrEffectiveRightsCapabilityDenied / ErrEffectiveRightsActionUnknown /
    ErrEffectiveRightsActionNotOverridable / ErrEffectiveRightsTargetNotActiveMember /
    ErrEffectiveRightsReasonRequired so all six "=" signs line up at the column dictated by the
    longest identifier (ErrEffectiveRightsTargetNotActiveMember), matching gofmt's own output. Do
    not run gofmt on any other file in this task — 137-UAT.md explicitly forbids large-scale
    cosmetic changes in unrelated areas/packages. If gofmt -w produces any diff beyond the var block
    (it should not, since the rest of the file is already gofmt-clean per the review), inspect it
    before committing and revert any unrelated hunk.
  </action>
  <verify>
    <automated>docker compose exec team4sv30-backend gofmt -l backend/internal/services/effective_rights_service.go</automated>
  </verify>
  <acceptance_criteria>
    - docker compose exec team4sv30-backend gofmt -l backend/internal/services/effective_rights_service.go
      (or the equivalent path relative to the container's /app root) prints nothing (clean).
    - git diff backend/internal/services/effective_rights_service.go shows changes confined to the
      var (...) block's whitespace alignment (lines ~80-87); zero non-whitespace diff.
    - docker compose exec team4sv30-backend go build ./... succeeds.
    - docker compose exec team4sv30-backend go test ./internal/services/... -count=1 passes with
      identical results to the pre-change baseline (whitespace-only change cannot affect test
      outcomes; confirms no accidental logic touch).
  </acceptance_criteria>
  <done>effective_rights_service.go's error-sentinel var block is gofmt-clean with zero non-whitespace changes; gofmt -l reports no remaining violations for this file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Future maintainers reading source comments | Documentation accuracy is itself a control surface — a stale comment claiming a security-relevant gap is still open can cause a future engineer to reintroduce an unnecessary workaround or distrust a correctly functioning enforcement path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-11-01 | Repudiation (documentation-as-control) | effective_rights.go file-level doc comment | mitigate | Corrects a comment falsely claiming per-user override enforcement is not yet live in production (WR-01); leaving it stale risks a future engineer reintroducing a "known gap" workaround or distrusting a working, already-proven security control. |
| T-137-11-02 | n/a (cosmetic) | effective_rights_service.go var block formatting | accept | Whitespace-only change; zero behavioral or security surface. |
| T-137-11-SC | Tampering | npm/pip/cargo installs | accept | No package-manager dependency change in this plan. |
</threat_model>

<verification>
Confirm both diffs are scoped exactly as described (comment-only for Task 1, whitespace-only for
Task 2) via `git diff`, and that `go build ./...` plus the `internal/permissions`/`internal/services`
test suites produce identical pass/fail results to the pre-plan baseline.
</verification>

<success_criteria>
effective_rights.go's doc comment accurately reflects the shipped state (production wiring closed in
137-05, DTO extension closed in 137-02/137-07); effective_rights_service.go is gofmt-clean; zero
behavioral change in either file; both files' test suites pass identically to before.
</success_criteria>

<output>
Create `.planning/phases/137-central-effective-rights-resolver-overrides/137-11-SUMMARY.md` when done
</output>
