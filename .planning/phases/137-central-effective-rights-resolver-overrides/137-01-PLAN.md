---
phase: 137-central-effective-rights-resolver-overrides
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/0150_effective_rights_overrides.up.sql
  - database/migrations/0150_effective_rights_overrides.down.sql
  - backend/internal/migrations/phase137_effective_rights_overrides_test.go
autonomous: true
requirements: [CAP-05, CAP-06, CAP-07, QUAL-03]
must_haves:
  truths:
    - "The catalog has a dedicated non-overridable group-scoped capability for managing per-user capability overrides."
    - "Only deliberately selected capabilities become user_overridable=true; all others remain fail-closed false by default."
    - "The seven Phase-136 group media/page/link actions and all three review.*.decide actions are valid per-user override targets."
    - "The override-management capability is seeded only to the existing role that already owns broad group member administration: fansub_lead; founder/co_leader are not promoted merely by role name."
    - "Migration 0146 is not edited; Phase 137 uses the next additive migration and remains reversible."
  artifacts:
    - path: "database/migrations/0150_effective_rights_overrides.up.sql"
      provides: "Management capability, explicit user_overridable pilot set, and role-capability seed"
    - path: "database/migrations/0150_effective_rights_overrides.down.sql"
      provides: "Dependency-safe reversal of Phase-137 catalog data"
    - path: "backend/internal/migrations/phase137_effective_rights_overrides_test.go"
      provides: "Fresh up/down/up proof for the Phase-137 catalog delta"
  key_links:
    - from: "action_definitions.user_overridable"
      to: "user_group_capability_overrides"
      via: "existing composite FK from migration 0146"
      pattern: "user_overridable"
    - from: "user_group_capability_override.manage"
      to: "role_capabilities"
      via: "fansub_lead seed only"
      pattern: "fansub_lead"
---

<objective>
Make the Phase-136 override schema actually usable while keeping the capability catalog fail-closed.

Purpose: Phase 136 intentionally shipped zero overridable actions. Phase 137 needs an additive catalog migration before any real override can be stored or tested.
Output: Migration 0150 plus fresh-database migration contract tests.
</objective>

<context>
@AGENTS.md
@CLAUDE.md
@AI-HANDOFF.md
@.planning/phases/137-central-effective-rights-resolver-overrides/137-CONTEXT.md
@.planning/phases/137-central-effective-rights-resolver-overrides/137-RESEARCH.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-CONTEXT.md
@database/migrations/0146_capability_policy_catalog.up.sql
@database/migrations/0146_capability_policy_catalog.down.sql
@database/migrations/0108_capability_registry.up.sql
@database/migrations/0134_review_foundation.up.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify the Phase-137 catalog delta in migration tests</name>
  <read_first>CLAUDE.md; AI-HANDOFF.md; 137-CONTEXT.md; 137-RESEARCH.md; database/migrations/0146_capability_policy_catalog.up.sql; database/migrations/0108_capability_registry.up.sql; database/migrations/0134_review_foundation.up.sql; existing backend/internal/migrations/phase136* tests</read_first>
  <files>backend/internal/migrations/phase137_effective_rights_overrides_test.go</files>
  <behavior>
    - Assert that a dedicated group-scoped override-management action exists and remains user_overridable=false.
    - Assert user_overridable=true exactly for: fansub_group_media.upload, fansub_group_media.update, fansub_group_media.reorder, fansub_group_page.general_edit, fansub_group_page.technical_links_edit, fansub_group_page.founding_history_edit, fansub_group_links.update, review.text.decide, review.image.decide, review.contribution.decide.
    - Assert representative protected/platform/security/delegation/audit/admin capabilities remain false.
    - Assert fansub_lead receives the override-management capability.
    - Assert founder and co_leader do not receive it solely because of their names; current role authority remains capability-based.
    - Assert fresh up/down/up reversibility without editing 0146.
  </behavior>
  <action>Create the migration contract test first and make it fail against the current schema. Reuse the existing disposable-Postgres migration-test conventions and explicit catalog assertions from Phase 136.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/migrations -run Phase137 -count=1</automated></verify>
  <done>The focused migration test precisely describes the intended Phase-137 catalog delta and initially fails before migration 0150 exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement reversible migration 0150</name>
  <read_first>backend/internal/migrations/phase137_effective_rights_overrides_test.go; database/migrations/0146_capability_policy_catalog.up.sql; database/migrations/0146_capability_policy_catalog.down.sql; database/migrations/0108_capability_registry.up.sql; database/migrations/0134_review_foundation.up.sql</read_first>
  <files>database/migrations/0150_effective_rights_overrides.up.sql, database/migrations/0150_effective_rights_overrides.down.sql</files>
  <action>Add the next paired migration only. Add the dedicated management capability using the canonical action naming convention; prefer `user_group_capability_override.manage` unless the existing catalog naming rules require a mechanically equivalent canonical spelling. Keep it non-overridable. Flip exactly the ten approved group/review actions to user_overridable=true. Seed management only to fansub_lead because current code/migrations show fansub_lead owns broad `fansub_group.members.manage` authority while founder/co_leader do not. Down migration must remove the new role mapping/action and restore the ten flags to false without touching migration 0146.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/migrations -run Phase137 -count=1 &amp;&amp; git diff --check</automated></verify>
  <done>Fresh up/down/up passes, the approved ten actions are overridable, the management capability is protected and seeded only to fansub_lead, and historical migrations remain unchanged.</done>
</task>

</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Privilege escalation by making management itself overridable | Management action is explicitly user_overridable=false and protected by existing schema checks. |
| Accidental broad opt-in | Migration names the exact ten approved action codes; default remains false. |
| Role-name-based privilege promotion | Seed follows current capability authority, not names such as founder/co_leader. |
</threat_model>

<verification>Focused migration test plus `git diff --check`.</verification>
<success_criteria>The existing Phase-136 schema has a usable, fail-closed Phase-137 catalog configuration without changing historical migrations.</success_criteria>
<output>After completion, create `.planning/phases/137-central-effective-rights-resolver-overrides/137-01-SUMMARY.md`.</output>
