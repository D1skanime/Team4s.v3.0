---
phase: 04.1-anime-v2-cutover-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/repository/admin_content_anime_create_v2.go
  - backend/internal/repository/admin_content_anime_metadata.go
  - backend/internal/repository/admin_content_anime_update_v2.go
  - backend/internal/repository/admin_content_sync.go
  - backend/internal/repository/anime_schema.go
  - backend/internal/repository/anime_v2.go
  - backend/internal/repository/admin_content_anime_create_v2_test.go
  - backend/internal/repository/admin_content_anime_update_v2_test.go
  - backend/internal/repository/admin_content_test.go
autonomous: true
requirements:
  - INTK-06
  - ASST-04
must_haves:
  truths:
    - "Admins can save an existing anime through the shared edit route while the live runtime stays on the v2 schema."
    - "A cover uploaded or replaced from the edit route persists in the active anime record and appears again on the next detail or edit read."
    - "A Jellyfin-linked anime keeps usable linkage for sync-preview and media lookup after create and after later manual edits, even on mixed local schemas."
  artifacts:
    - path: "backend/internal/repository/admin_content_anime_create_v2.go"
      provides: "Schema-aware v2 create persistence that tolerates slug-first local schemas with some runtime columns still absent"
    - path: "backend/internal/repository/admin_content_anime_update_v2.go"
      provides: "Schema-aware v2 edit persistence plus cover-link handling that preserves readback and Jellyfin linkage"
    - path: "backend/internal/repository/admin_content_sync.go"
      provides: "Jellyfin sync-source reads that continue resolving source linkage after manual edits"
    - path: "backend/internal/repository/anime_v2.go"
      provides: "v2 detail and media-lookup reads that return persisted cover media instead of legacy flat-column cover state"
  key_links:
    - from: "backend/internal/repository/admin_content_anime_create_v2.go"
      to: "backend/internal/repository/anime_schema.go"
      via: "schema-aware insert column selection"
      pattern: "loadAnimeV2SchemaInfo|HasContentType|HasStatus|HasMaxEpisodes|HasSource"
    - from: "backend/internal/repository/admin_content_anime_update_v2.go"
      to: "backend/internal/repository/anime_v2.go"
      via: "poster media write then detail readback"
      pattern: "applyAnimeCoverPatchV2|poster.file_path"
    - from: "backend/internal/repository/admin_content_anime_update_v2.go"
      to: "backend/internal/repository/admin_content_sync.go"
      via: "source persistence and fallback linkage"
      pattern: "HasSource|sourceSelect|media_external"
---

<objective>
Stabilize the live backend v2 create/edit/read stack so admin persistence works against the active schema, cover uploads survive refresh, and Jellyfin linkage remains usable across manual edits.

Purpose: Finish the cutover-critical backend seam before provenance work resumes, without reverting to legacy flat-column writes.
Output: Schema-aware v2 create/update/read repository behavior plus focused regression coverage for mixed local schemas, cover readback, and Jellyfin linkage persistence.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04.1-anime-v2-cutover-stabilization/04.1-CONTEXT.md
@STATUS.md
@TOMORROW.md
@backend/internal/repository/admin_content_anime_metadata.go
@backend/internal/repository/admin_content_anime_create_v2.go
@backend/internal/repository/admin_content_anime_update_v2.go
@backend/internal/repository/admin_content_sync.go
@backend/internal/repository/anime_schema.go
@backend/internal/repository/anime_v2.go
@backend/internal/repository/admin_content_anime_create_v2_test.go
@backend/internal/repository/admin_content_test.go
@.planning/phases/03-jellyfin-assisted-intake/03-04-SUMMARY.md

<interfaces>
Current v2 schema probe:
```go
type animeV2SchemaInfo struct {
	HasSlug        bool
	HasContentType bool
	HasStatus      bool
	HasMaxEpisodes bool
	HasSource      bool
}
```

Current v2 update entry:
```go
func (r *AdminContentRepository) updateAnimeV2(
	ctx context.Context,
	tx pgx.Tx,
	id int64,
	input models.AdminAnimePatchInput,
	actorUserID int64,
	schema animeV2SchemaInfo,
) (*models.AdminAnimeItem, error)
```

Current sync-source read seam:
```go
func (r *AdminContentRepository) GetAnimeSyncSource(
	ctx context.Context,
	animeID int64,
) (*models.AdminAnimeSyncSource, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing repository coverage for mixed-schema v2 create, edit, and read persistence</name>
  <files>backend/internal/repository/admin_content_anime_create_v2_test.go, backend/internal/repository/admin_content_anime_update_v2_test.go, backend/internal/repository/admin_content_test.go</files>
  <read_first>
    - backend/internal/repository/admin_content_anime_create_v2.go
    - backend/internal/repository/admin_content_anime_update_v2.go
    - backend/internal/repository/admin_content_sync.go
    - backend/internal/repository/anime_v2.go
    - backend/internal/repository/anime_schema.go
    - .planning/phases/04.1-anime-v2-cutover-stabilization/04.1-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: A v2 schema with `slug` but without one or more runtime columns (`content_type`, `status`, `max_episodes`, `source`) still builds a valid create path instead of writing missing columns.
    - Test 2: An existing anime on the v2 edit path keeps its persisted cover on the next `loadAdminAnimeItemV2` and `getByIDV2` read after a cover patch.
    - Test 3: When `source` is absent, the fallback linkage used by `GetAnimeSyncSource` and media lookup still survives a later manual cover replacement by preserving the older Jellyfin poster linkage.
    - Test 4: When `source` exists, create and update continue to use the native `anime.source` column and do not regress to legacy flat-column writes.
  </behavior>
  <action>Create failing repository-focused regression tests in `backend/internal/repository/admin_content_anime_create_v2_test.go`, a new `backend/internal/repository/admin_content_anime_update_v2_test.go`, and `backend/internal/repository/admin_content_test.go`. The tests must pin the exact mixed-schema phase risk described in `04.1-CONTEXT.md`: `slug` can exist while `source`, `status`, or `max_episodes` may still be absent. Cover both query-shaping or helper behavior and the persistence/readback semantics for cover and Jellyfin linkage so the implementation can be changed safely in Task 2.</action>
  <acceptance_criteria>
    - Tests fail against the current slug-only create detection and any linkage path that depends on removed flat cover columns.
    - Tests explicitly name the mixed-schema cases rather than using only a fully-populated v2 schema.
    - Tests cover both source-column-present and source-column-absent behavior.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/repository -count=1</automated>
  </verify>
  <done>There is a red-green safety net for schema-aware v2 create/edit/read behavior, cover readback, and Jellyfin linkage preservation.</done>
</task>

<task type="auto">
  <name>Task 2: Make the v2 repository create, edit, and read seams schema-aware and cover-persistent</name>
  <files>backend/internal/repository/admin_content_anime_create_v2.go, backend/internal/repository/admin_content_anime_metadata.go, backend/internal/repository/admin_content_anime_update_v2.go, backend/internal/repository/admin_content_sync.go, backend/internal/repository/anime_schema.go, backend/internal/repository/anime_v2.go, backend/internal/repository/admin_content_anime_create_v2_test.go, backend/internal/repository/admin_content_anime_update_v2_test.go, backend/internal/repository/admin_content_test.go</files>
  <read_first>
    - backend/internal/repository/admin_content_anime_create_v2.go
    - backend/internal/repository/admin_content_anime_metadata.go
    - backend/internal/repository/admin_content_anime_update_v2.go
    - backend/internal/repository/admin_content_sync.go
    - backend/internal/repository/anime_v2.go
    - backend/internal/repository/anime_schema.go
    - STATUS.md
    - TOMORROW.md
  </read_first>
  <action>Replace the current slug-only v2 create gate with full `loadAnimeV2SchemaInfo(...)` usage so `createAnimeV2(...)` only inserts columns that actually exist in the active local schema. Keep `slug`, `anime_type_id`, `description`, `year`, `folder_name`, and `modified_by` on the v2 path, but treat `content_type`, `status`, `max_episodes`, and `source` as conditional fields that are added only when the schema reports them. Keep the Phase 3 source seam from `03-04-SUMMARY.md`: when `anime.source` exists, preserve Jellyfin linkage there across create and edit; when it does not exist, do not regress linkage by deleting the older Jellyfin poster link during a manual cover replacement. Ensure `applyAnimeCoverPatchV2(...)`, `loadAdminAnimeItemV2(...)`, `getByIDV2(...)`, `getMediaLookupByIDV2(...)`, and `getAnimeSyncSourceV2(...)` all read cover data from `poster.file_path` and keep the source lookup usable for preview, metadata sync, backdrop fetch, and episode sync without reviving legacy `anime.cover_image` assumptions.</action>
  <acceptance_criteria>
    - `CreateAnime(...)` no longer fails solely because a local v2 schema has `slug` but not one of the later runtime columns.
    - `UpdateAnime(...)` keeps using the shared Phase 1 editor path while writing through the active v2 repository seam rather than the legacy flat-column branch when `slug` exists.
    - A manual cover upload or replacement persists in `anime_media` or `media_assets` such that the next detail or edit read returns the same cover again.
    - Jellyfin-linked anime keep a usable source lookup after later manual edits in both `HasSource=true` and `HasSource=false` local schemas.
    - No new create or update code path reintroduces reliance on legacy `anime.cover_image` for the live v2 read stack.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/repository -count=1</automated>
  </verify>
  <done>The live backend v2 create/edit/read stack is stable against mixed local schemas, persisted covers read back correctly, and Jellyfin linkage survives later manual edits.</done>
</task>

</tasks>

<verification>
Run the repository test suite, then perform a focused local runtime smoke check from `STATUS.md`: save an existing anime on `/admin/anime/{id}/edit`, upload or replace its cover, refresh the detail/edit route, and confirm that a Jellyfin-linked record still exposes source context through the sync-preview path.
</verification>

<success_criteria>
The v2 backend no longer depends on removed legacy cover columns or on a fully-complete local schema to keep admin create, edit, read, cover persistence, and Jellyfin linkage working.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-anime-v2-cutover-stabilization/04.1-01-SUMMARY.md`
</output>
