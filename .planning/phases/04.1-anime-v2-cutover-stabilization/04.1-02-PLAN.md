---
phase: 04.1-anime-v2-cutover-stabilization
plan: 02
type: execute
wave: 2
depends_on:
  - "04.1-01"
files_modified:
  - backend/internal/handlers/admin_content_anime.go
  - backend/internal/handlers/error_responses.go
  - backend/internal/handlers/admin_content_test.go
  - frontend/src/lib/api.ts
  - frontend/src/lib/api/admin-anime-intake.ts
  - frontend/src/app/admin/anime/create/page.tsx
  - frontend/src/app/admin/anime/create/page.test.tsx
  - frontend/src/app/admin/anime/[id]/edit/page.tsx
  - frontend/src/app/admin/anime/[id]/edit/page.test.tsx
  - frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts
  - frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts
autonomous: true
requirements:
  - RLY-01
must_haves:
  truths:
    - "When admin anime create, save, or load fails during the v2 cutover, the operator sees actionable context instead of only a generic internal-server-error message."
    - "The shared create route keeps surfacing backend validation or schema-mismatch details through the active create submit seam."
    - "The shared edit route keeps surfacing backend validation or schema-mismatch details through the existing mutation error path."
    - "The operator-visible error path stays aligned with the live backend response contract instead of inventing a second ad-hoc frontend parser."
  artifacts:
    - path: "backend/internal/handlers/error_responses.go"
      provides: "Structured internal error payloads with stable code and operator-readable details"
    - path: "frontend/src/lib/api.ts"
      provides: "Admin anime update and load clients that preserve error code and details in ApiError"
    - path: "frontend/src/lib/api/admin-anime-intake.ts"
      provides: "Create-side admin anime client that preserves error code and details in ApiError"
    - path: "frontend/src/app/admin/anime/create/page.tsx"
      provides: "Create-route error rendering that shows actionable backend detail during save failures"
    - path: "frontend/src/app/admin/anime/[id]/edit/page.tsx"
      provides: "Edit-route load failure rendering that shows actionable backend detail when getAnimeByID(...) fails before the form loads"
    - path: "frontend/src/app/admin/anime/[id]/edit/page.test.tsx"
      provides: "Explicit regression coverage for edit-route load failure detail rendering"
    - path: "frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts"
      provides: "Edit-route mutation messaging that surfaces backend details for save and cover-patch failures"
  key_links:
    - from: "backend/internal/handlers/admin_content_anime.go"
      to: "backend/internal/handlers/error_responses.go"
      via: "writeInternalErrorResponse"
      pattern: "writeInternalErrorResponse"
    - from: "frontend/src/lib/api/admin-anime-intake.ts"
      to: "frontend/src/app/admin/anime/create/page.tsx"
      via: "createAdminAnime submit failure propagation"
      pattern: "createAdminAnime|ApiError|details"
    - from: "frontend/src/lib/api.ts"
      to: "frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts"
      via: "ApiError details propagation"
      pattern: "parseApiErrorPayload|new ApiError"
    - from: "frontend/src/lib/api.ts"
      to: "frontend/src/app/admin/anime/[id]/edit/page.tsx"
      via: "edit-route anime load failure propagation"
      pattern: "getAnimeByID|ApiError"
    - from: "frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts"
      to: "backend/internal/handlers/admin_content_anime.go"
      via: "updateAdminAnime patch failure reporting"
      pattern: "updateAdminAnime|onError"
---

<objective>
Expose operator-usable error context across the admin anime create, edit, and load flows so the v2 cutover fails visibly and debuggably instead of collapsing into a generic message.

Purpose: Close the reliability gap blocking safe local cutover verification before broader provenance work resumes.
Output: Structured backend error responses, API client propagation of code and details, create/edit/load UI messaging, and regression tests that show actionable operator context.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04.1-anime-v2-cutover-stabilization/04.1-CONTEXT.md
@backend/internal/handlers/admin_content_anime.go
@backend/internal/handlers/error_responses.go
@backend/internal/handlers/admin_content_test.go
@frontend/src/lib/api.ts
@frontend/src/lib/api/admin-anime-intake.ts
@frontend/src/types/admin.ts
@frontend/src/app/admin/anime/create/page.tsx
@frontend/src/app/admin/anime/create/page.test.tsx
@frontend/src/app/admin/anime/[id]/edit/page.tsx
@frontend/src/app/admin/anime/[id]/edit/page.test.tsx
@frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts
@frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts
@frontend/src/app/admin/anime/components/AnimePatchForm/AnimeCoverField.tsx
@.planning/phases/02-manual-intake-baseline/02-03-SUMMARY.md

<interfaces>
Current backend error helper:
```go
func writeInternalErrorResponse(c *gin.Context, message string, err error, operationDetails string)
```

Current frontend error shape:
```ts
export class ApiError extends Error {
  status: number
  retryAfterSeconds: number | null
  code: string | null
  details: string | null
}
```

Current admin patch client:
```ts
export async function updateAdminAnime(
  animeID: number,
  payload: AdminAnimePatchRequest,
  authToken?: string,
): Promise<AdminAnimeUpsertResponse>
```

Current create client seam:
```ts
createAdminAnime(payload: AdminAnimeCreateRequest, authToken?: string): Promise<AdminAnimeUpsertResponse>
```

Current load seam:
```ts
getAnimeByID(id: number, options?: { include_disabled?: boolean }): Promise<{ data: AnimeDetail }>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing handler and frontend coverage for operator-visible anime create, edit, and load errors</name>
  <files>backend/internal/handlers/admin_content_test.go, frontend/src/app/admin/anime/create/page.test.tsx, frontend/src/app/admin/anime/[id]/edit/page.tsx, frontend/src/app/admin/anime/[id]/edit/page.test.tsx, frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts</files>
  <read_first>
    - backend/internal/handlers/admin_content_anime.go
    - backend/internal/handlers/error_responses.go
    - backend/internal/handlers/admin_content_test.go
    - frontend/src/lib/api.ts
    - frontend/src/lib/api/admin-anime-intake.ts
    - frontend/src/app/admin/anime/create/page.tsx
    - frontend/src/app/admin/anime/create/page.test.tsx
    - frontend/src/app/admin/anime/[id]/edit/page.tsx
    - frontend/src/app/admin/anime/[id]/edit/page.test.tsx
    - frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts
    - frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts
  </read_first>
  <behavior>
    - Test 1: A handler-side schema mismatch returns `error.code` plus a detailed operator message naming the missing column or table.
    - Test 2: `createAdminAnime(...)` preserves backend `code` and `details` in the thrown `ApiError` instead of discarding them.
    - Test 3: `updateAdminAnime(...)` preserves backend `code` and `details` in the thrown `ApiError` instead of discarding them.
    - Test 4: The create page surfaces backend `details` for create failures so the shared create surface no longer shows only the generic internal error text.
    - Test 5: `useAnimePatchMutations` surfaces backend `details` for save or cover-patch failures so the shared edit surface no longer shows only the generic internal error text.
    - Test 6: The active edit-route load consumer in `frontend/src/app/admin/anime/[id]/edit/page.tsx` surfaces backend `details` when `getAnimeByID(...)` fails before the form loads.
  </behavior>
  <action>Add failing regression coverage in `backend/internal/handlers/admin_content_test.go`, `frontend/src/app/admin/anime/create/page.test.tsx`, `frontend/src/app/admin/anime/[id]/edit/page.test.tsx`, and `frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts` for the phase-specific reliability target from `04.1-CONTEXT.md`. The tests must prove that a backend internal error can carry operator-usable detail text through the handler response contract, through both create and update API helpers, and into the active create page, the active edit-route mutation callback, and the active edit-route load screen in `frontend/src/app/admin/anime/[id]/edit/page.tsx` that consumes `getAnimeByID(...)`.</action>
  <acceptance_criteria>
    - Tests fail if `createAdminAnime(...)` or `updateAdminAnime(...)` continue to throw only a plain string message.
    - Tests fail if handler internal errors drop the missing-column or missing-table context.
    - Tests assert the create page and edit mutation error callback both receive actionable text, not just `interner serverfehler`.
    - Tests name `frontend/src/app/admin/anime/[id]/edit/page.tsx` plus `frontend/src/app/admin/anime/[id]/edit/page.test.tsx` as the concrete load seam for `getAnimeByID(...)` and assert that backend details are surfaced there.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/handlers -count=1 && cd ../frontend && npm test -- src/app/admin/anime/create/page.test.tsx src/app/admin/anime/[id]/edit/page.test.tsx src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts</automated>
  </verify>
  <done>There is end-to-end test coverage proving that backend internal error detail can reach the admin create workflow, the admin edit workflow, and the named load seam.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve backend error details through the admin anime create, edit, and load clients plus their UI seams</name>
  <files>backend/internal/handlers/admin_content_anime.go, backend/internal/handlers/error_responses.go, backend/internal/handlers/admin_content_test.go, frontend/src/lib/api.ts, frontend/src/lib/api/admin-anime-intake.ts, frontend/src/app/admin/anime/create/page.tsx, frontend/src/app/admin/anime/create/page.test.tsx, frontend/src/app/admin/anime/[id]/edit/page.tsx, frontend/src/app/admin/anime/[id]/edit/page.test.tsx, frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts, frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts</files>
  <read_first>
    - backend/internal/handlers/admin_content_anime.go
    - backend/internal/handlers/error_responses.go
    - frontend/src/lib/api.ts
    - frontend/src/lib/api/admin-anime-intake.ts
    - frontend/src/app/admin/anime/create/page.tsx
    - frontend/src/app/admin/anime/[id]/edit/page.tsx
    - frontend/src/app/admin/anime/[id]/edit/page.test.tsx
    - frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts
    - frontend/src/app/admin/anime/components/AnimePatchForm/AnimeCoverField.tsx
    - .planning/phases/04.1-anime-v2-cutover-stabilization/04.1-CONTEXT.md
  </read_first>
  <action>Keep the existing structured internal error contract in `backend/internal/handlers/error_responses.go`, but strengthen the admin anime create and save wording in `backend/internal/handlers/admin_content_anime.go` so failures mention the v2 cutover concerns explicitly: schema mismatch, save persistence, cover persistence, source-link persistence, or readback context. In `frontend/src/lib/api/admin-anime-intake.ts`, update `createAdminAnime(...)` to use the same `parseApiErrorPayload(...)` plus `ApiError` code/details behavior already used by richer helpers in `frontend/src/lib/api.ts`; if the create helper currently lives elsewhere, keep that seam but make the create path explicit in this plan. In `frontend/src/lib/api.ts`, keep `getAnimeByID(...)` as the named load seam and change `updateAdminAnime(...)` to use `parseApiErrorPayload(...)` and throw `ApiError` with `code` and `details` populated, matching load behavior. In `frontend/src/app/admin/anime/create/page.tsx`, when create submission fails, prefer a message that includes `ApiError.details` so the create route shows actionable operator context. In `frontend/src/app/admin/anime/[id]/edit/page.tsx`, when the initial `getAnimeByID(...)` load fails, render backend `details` in the operator-visible failure state instead of collapsing the error to a generic message. In `frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts`, when the caught error is an `ApiError`, prefer a message that includes `details` so the existing error callback receives operator-usable context during both general save and cover-upload-followed-by-patch failures. Do not build separate ad-hoc parsers in the page or hook; the API layer remains the single error-decoding seam.</action>
  <acceptance_criteria>
    - Backend handler failures return `error.code` and `error.details` for schema mismatch and phase-specific save failures.
    - `createAdminAnime(...)`, `updateAdminAnime(...)`, and the named load seam `getAnimeByID(...)` all preserve `ApiError.code` and `ApiError.details`.
    - The create page forwards actionable detail text to its error UI for create failures.
    - The edit page forwards actionable detail text to its load-failure UI when `getAnimeByID(...)` fails before the editor is usable.
    - `useAnimePatchMutations` forwards actionable detail text to `onError` for patch and cover-update failures.
    - The implementation reuses `parseApiErrorPayload(...)` instead of inventing another response parser.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/handlers -count=1 && cd ../frontend && npm test -- src/app/admin/anime/create/page.test.tsx src/app/admin/anime/[id]/edit/page.test.tsx src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts && npm run build</automated>
  </verify>
  <done>Operators using the shared admin create route, shared admin edit route, and named load seam get actionable cutover error context instead of only a generic internal error banner.</done>
</task>

</tasks>

<verification>
Run the handler test slice, the create-page and patch-mutation tests, and the frontend production build. Then force representative admin create, edit-load, and save failures locally and verify that the create route, edit-route load state, and edit-route save path each surface the operator detail string from the backend response.
</verification>

<success_criteria>
The admin anime create route, edit-route load state, and edit-route save path preserve backend validation and internal-failure context end-to-end, making v2 cutover failures understandable enough to debug without direct server log access.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-anime-v2-cutover-stabilization/04.1-02-SUMMARY.md`
</output>
