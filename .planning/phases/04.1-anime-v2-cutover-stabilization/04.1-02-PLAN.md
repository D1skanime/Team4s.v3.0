---
phase: 04.1-anime-v2-cutover-stabilization
plan: 02
type: execute
wave: 2
depends_on:
  - "04.1-01"
files_modified:
  - backend/internal/handlers/admin_content_anime.go
  - backend/internal/handlers/error_responses.go
  - backend/internal/handlers/admin_content_test.go
  - frontend/src/lib/api.ts
  - frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts
  - frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts
autonomous: true
requirements:
  - RLY-01
must_haves:
  truths:
    - "When admin anime save or load fails during the v2 cutover, the operator sees actionable context instead of only a generic internal-server-error message."
    - "The shared edit route keeps surfacing backend validation or schema-mismatch details through the existing mutation error path."
    - "The operator-visible error path stays aligned with the live backend response contract instead of inventing a second ad-hoc frontend parser."
  artifacts:
    - path: "backend/internal/handlers/error_responses.go"
      provides: "Structured internal error payloads with stable code and operator-readable details"
    - path: "frontend/src/lib/api.ts"
      provides: "Admin anime update client that preserves error code and details in ApiError"
    - path: "frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts"
      provides: "Edit-route mutation messaging that surfaces backend details for save and cover-patch failures"
  key_links:
    - from: "backend/internal/handlers/admin_content_anime.go"
      to: "backend/internal/handlers/error_responses.go"
      via: "writeInternalErrorResponse"
      pattern: "writeInternalErrorResponse"
    - from: "frontend/src/lib/api.ts"
      to: "frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts"
      via: "ApiError details propagation"
      pattern: "parseApiErrorPayload|new ApiError"
    - from: "frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts"
      to: "backend/internal/handlers/admin_content_anime.go"
      via: "updateAdminAnime patch failure reporting"
      pattern: "updateAdminAnime|onError"
---

<objective>
Expose operator-usable error context across the admin anime save and cover-patch flow so the v2 cutover fails visibly and debuggably instead of collapsing into a generic message.

Purpose: Close the reliability gap blocking safe local cutover verification before broader provenance work resumes.
Output: Structured backend error responses, API client propagation of code and details, and edit-route mutation messaging/tests that show actionable save context.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04.1-anime-v2-cutover-stabilization/04.1-CONTEXT.md
@backend/internal/handlers/admin_content_anime.go
@backend/internal/handlers/error_responses.go
@backend/internal/handlers/admin_content_test.go
@frontend/src/lib/api.ts
@frontend/src/types/admin.ts
@frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts
@frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts
@frontend/src/app/admin/anime/components/AnimePatchForm/AnimeCoverField.tsx
@.planning/phases/02-manual-intake-baseline/02-03-SUMMARY.md

<interfaces>
Current backend error helper:
```go
func writeInternalErrorResponse(c *gin.Context, message string, err error, operationDetails string)
```

Current frontend error shape:
```ts
export class ApiError extends Error {
  status: number
  retryAfterSeconds: number | null
  code: string | null
  details: string | null
}
```

Current admin patch client:
```ts
export async function updateAdminAnime(
  animeID: number,
  payload: AdminAnimePatchRequest,
  authToken?: string,
): Promise<AdminAnimeUpsertResponse>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing handler and frontend coverage for operator-visible anime save errors</name>
  <files>backend/internal/handlers/admin_content_test.go, frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts</files>
  <read_first>
    - backend/internal/handlers/admin_content_anime.go
    - backend/internal/handlers/error_responses.go
    - backend/internal/handlers/admin_content_test.go
    - frontend/src/lib/api.ts
    - frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts
    - frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts
  </read_first>
  <behavior>
    - Test 1: A handler-side schema mismatch returns `error.code` plus a detailed operator message naming the missing column or table.
    - Test 2: `updateAdminAnime(...)` preserves backend `code` and `details` in the thrown `ApiError` instead of discarding them.
    - Test 3: `useAnimePatchMutations` surfaces backend `details` for save or cover-patch failures so the shared edit surface no longer shows only the generic internal error text.
  </behavior>
  <action>Add failing regression coverage in `backend/internal/handlers/admin_content_test.go` and `frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts` for the phase-specific reliability target from `04.1-CONTEXT.md`. The tests must prove that a backend internal error can carry operator-usable detail text through the handler response contract, through `frontend/src/lib/api.ts`, and finally into the edit-route mutation error callback used by the current shared editor surface.</action>
  <acceptance_criteria>
    - Tests fail if `updateAdminAnime(...)` continues to throw only a plain string message.
    - Tests fail if handler internal errors drop the missing-column or missing-table context.
    - Tests assert the edit mutation error callback receives actionable text, not just `interner serverfehler`.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/handlers -count=1 && cd ../frontend && npm test -- src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts</automated>
  </verify>
  <done>There is end-to-end test coverage proving that backend internal error detail can reach the admin edit workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve backend error details through the admin anime patch client and hook</name>
  <files>backend/internal/handlers/admin_content_anime.go, backend/internal/handlers/error_responses.go, backend/internal/handlers/admin_content_test.go, frontend/src/lib/api.ts, frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts, frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts</files>
  <read_first>
    - backend/internal/handlers/admin_content_anime.go
    - backend/internal/handlers/error_responses.go
    - frontend/src/lib/api.ts
    - frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts
    - frontend/src/app/admin/anime/components/AnimePatchForm/AnimeCoverField.tsx
    - .planning/phases/04.1-anime-v2-cutover-stabilization/04.1-CONTEXT.md
  </read_first>
  <action>Keep the existing structured internal error contract in `backend/internal/handlers/error_responses.go`, but strengthen the admin anime save wording in `backend/internal/handlers/admin_content_anime.go` so failures mention the v2 cutover concerns explicitly: schema mismatch, save persistence, cover persistence, or linkage sync context. In `frontend/src/lib/api.ts`, change `updateAdminAnime(...)` to use `parseApiErrorPayload(...)` and throw `ApiError` with `code` and `details` populated, matching the richer behavior already used by `getAnimeByID(...)` and Jellyfin admin helpers. In `frontend/src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.ts`, when the caught error is an `ApiError`, prefer a message that includes `details` so the existing error callback receives operator-usable context during both general save and cover-upload-followed-by-patch failures. Do not build a second bespoke parser in the hook; the API layer remains the single error-decoding seam.</action>
  <acceptance_criteria>
    - Backend handler failures return `error.code` and `error.details` for schema mismatch and phase-specific save failures.
    - `updateAdminAnime(...)` throws `ApiError` carrying backend `code` and `details`.
    - `useAnimePatchMutations` forwards actionable detail text to `onError` for patch and cover-update failures.
    - The implementation reuses `parseApiErrorPayload(...)` instead of inventing another response parser.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/handlers -count=1 && cd ../frontend && npm test -- src/app/admin/anime/hooks/internal/anime-patch/useAnimePatchMutations.test.ts && npm run build</automated>
  </verify>
  <done>Operators using the shared admin edit route get actionable save and cover-patch error context during the v2 cutover instead of only a generic internal error banner.</done>
</task>

</tasks>

<verification>
Run the handler test slice, the patch-mutation test, and the frontend production build. Then force a representative admin save failure locally and verify that the edit UI surfaces the operator detail string from the backend response.
</verification>

<success_criteria>
The admin anime edit flow preserves backend validation and internal-failure context end-to-end, making v2 cutover failures understandable enough to debug without direct server log access.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-anime-v2-cutover-stabilization/04.1-02-SUMMARY.md`
</output>
