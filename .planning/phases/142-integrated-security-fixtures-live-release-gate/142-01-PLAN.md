# Phase 142 Plan 01: Final Release Gate

## Objective

Close the v1.4 milestone evidence gaps without creating parallel auth, fixture, ownership, or review systems.

## Read First

- `docs/api/api-contracts.md`
- `docs/frontend/auth-api-client.md`
- `.planning/v1.4-MILESTONE-AUDIT.md`
- `.planning/notes/uat-fixture-plan-v14-close.md`
- `.planning/phases/140-review-delegation-management/140-VERIFICATION.md`
- `.planning/phases/141-actor-decidable-review-queue/141-VERIFICATION.md`

## Tasks

1. Synchronize the canonical OpenAPI review-queue declaration with the existing backend DTO and frontend type: `view=own` and `allowed_types`.
2. Add a focused backend contract test that parses OpenAPI and checks the corresponding frontend type.
3. Reuse `api.auth-refresh.test.ts`, `seed-member-profile-fixtures.mjs`, and `seed-phase139-contribution-fixtures.mjs` as the canonical auth/fixture seams; document the exact commands and what each can prove.
4. Record the outstanding live UAT steps at the required responsive, keyboard, and 400-percent-zoom boundaries. Do not claim these checks without recorded human evidence.
5. Reconcile Phase 140's verified RDEL requirements in roadmap and requirements tracking.

## Acceptance Criteria

- Shared OpenAPI, backend DTO, and frontend type agree on the release-review queue shape.
- The contract test fails when either `own` or `allowed_types` is removed.
- The phase verification distinguishes automated evidence from outstanding human UAT.
- No new auth client, fixture transport, media ownership path, or review service is introduced.
