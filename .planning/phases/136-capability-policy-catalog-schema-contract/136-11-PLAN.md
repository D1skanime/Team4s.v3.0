---
phase: 136-capability-policy-catalog-schema-contract
plan: 11
type: execute
wave: 5
depends_on: [136-03]
files_modified:
  - frontend/src/providers/RoleCatalogProvider.tsx
  - frontend/src/providers/RoleCatalogProvider.test.tsx
  - frontend/src/app/layout.tsx
  - frontend/src/app/layout.test.tsx
autonomous: true
requirements: [CAP-11, CAP-13, QUAL-01]
must_haves:
  truths:
    - "D-20–D-23: the app root loads all three public contexts once and mounts one provider backed by the Plan-03 adapter."
    - "Neutral load failure remains renderable and leaf role consumers issue no duplicate catalog requests."
    - "The unused legacy route /anime/[id]/group/[groupId]/releases is not a Phase-136 consumer."
  artifacts:
    - path: "frontend/src/app/layout.test.tsx"
      provides: "Owning root-layout loading and provider-mount integration proof"
  key_links:
    - from: "frontend/src/app/layout.tsx"
      to: "frontend/src/providers/RoleCatalogProvider.tsx"
      via: "three listRoleDefinitions calls normalized by existing roleCatalog adapter"
      pattern: "RoleCatalogProvider"
---
<objective>Mount the already-implemented catalog adapter once at the application root and prove real root wiring.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/136-capability-policy-catalog-schema-contract/136-CONTEXT.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-03-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify provider behavior and root-layout integration</name>
  <read_first>frontend/src/lib/api.ts; frontend/src/lib/roleCatalog.ts; frontend/src/lib/roleCatalog.test.ts; frontend/src/app/layout.tsx; existing provider and root-layout test patterns</read_first>
  <files>frontend/src/providers/RoleCatalogProvider.test.tsx, frontend/src/app/layout.test.tsx</files>
  <behavior>
    - Root layout requests fansub_group, anime_contribution and group_history exactly once each and mounts RoleCatalogProvider around children.
    - Provider selectors return normalized, ordered context rows including injected karaoke_fx and an invented future role.
    - One failed context yields neutral empty/error state without crashing the root or installing a static fallback.
    - A representative leaf beneath the layout reads provider data and performs no listRoleDefinitions request itself.
  </behavior>
  <action>Write an owning provider unit test and root-layout integration test before wiring. Mock the central `listRoleDefinitions` boundary, render the actual root layout/provider seam, prove all three loads, provider mounting, neutral partial/total failure, and absence of duplicate leaf-level requests. Do not include or modernize the legacy `/anime/[id]/group/[groupId]/releases` route.</action>
  <verify><automated>docker compose exec -T team4sv30-frontend npm test -- --run 'RoleCatalogProvider|app/layout' </automated></verify>
  <acceptance_criteria>Tests fail before provider/root wiring and directly prove the checker-requested root behavior rather than only provider internals.</acceptance_criteria>
  <done>The task acceptance criteria are satisfied and its automated verification passes.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Wire the catalog provider at the app root</name>
  <read_first>frontend/src/lib/roleCatalog.ts; frontend/src/providers/RoleCatalogProvider.test.tsx; frontend/src/app/layout.test.tsx; frontend/src/app/layout.tsx; frontend/src/lib/api.ts</read_first>
  <files>frontend/src/providers/RoleCatalogProvider.tsx, frontend/src/app/layout.tsx</files>
  <action>Create typed context selectors/hooks using the existing Plan-03 adapter, then load the three contexts through `listRoleDefinitions` in the root layout and mount one provider. Deduplicate by code while retaining contexts and server ordering/presentation. Expose scoped neutral error state; never add bearer/session logic, leaf fetching or static role fallback.</action>
  <verify><automated>docker compose exec -T team4sv30-frontend npm test -- --run 'RoleCatalogProvider|app/layout' &amp;&amp; docker compose exec -T team4sv30-frontend npm run typecheck &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>Adapter exists before import; root integration proves three loads and one provider; leaves can consume without duplicate requests.</acceptance_criteria>
  <done>The task acceptance criteria are satisfied and its automated verification passes.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| public API JSON→root provider | Untrusted labels/semantic keys become shared presentation state. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-136-26 | Spoofing/XSS | root provider | mitigate | Adapter text rendering and allowlisted semantic keys only. |
| T-136-27 | Denial of Service | root catalog load | mitigate | Neutral bounded failure state and exactly three deduplicated requests. |
</threat_model>
<verification>Owning provider/root integration tests, typecheck and diff check pass.</verification>
<success_criteria>The active application tree receives one catalog source; no leaf-level duplicate or static truth exists.</success_criteria>
<output>After completion, create `.planning/phases/136-capability-policy-catalog-schema-contract/136-11-SUMMARY.md`.</output>
