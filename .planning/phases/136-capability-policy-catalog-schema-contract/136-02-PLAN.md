---
phase: 136-capability-policy-catalog-schema-contract
plan: 02
type: execute
wave: 3
depends_on: [136-09]
files_modified:
  - backend/internal/permissions/permissions.go
  - backend/internal/permissions/capability_registry_test.go
  - backend/internal/repository/authz_permissions.go
  - backend/internal/repository/authz_capability_mutations.go
  - backend/internal/handlers/admin_capability_handler.go
  - backend/internal/handlers/admin_capability_handler_test.go
  - backend/internal/repository/hist_group_member_roles_repository.go
  - backend/internal/repository/hist_group_member_roles_fansub_group_test.go
  - backend/internal/handlers/fansub_hist_group_member_roles_handler.go
  - backend/internal/handlers/fansub_hist_group_member_roles_handler_test.go
autonomous: true
requirements: [CAP-04, CAP-11, CAP-12, CAP-13, CAP-14, QUAL-01]
must_haves:
  truths:
    - "D-02/D-03/D-05: backend catalog projections expose override policy directly from action_definitions and fail closed for missing metadata."
    - "D-06: platform-admin provenance is separate, IdP-owned and non-deniable; no group-role row impersonates it."
    - "D-12/D-13: assignable zero-right roles remain visible with a derived operative-capability count for later contextual UI."
    - "D-20–D-23: karaoke_fx appears from database rows without additions to a Go role matrix, whitelist or label map."
  artifacts:
    - path: "backend/internal/repository/authz_capability_mutations.go"
      provides: "Canonical catalog projection DTO"
    - path: "backend/internal/permissions/permissions.go"
      provides: "Fail-closed DB-loaded policy/cache seam"
  key_links:
    - from: "AdminCapabilityHandler.ListCapabilityMatrix"
      to: "AuthzRepository.ListCapabilityMatrix"
      via: "typed catalog projection after platform-admin guard"
      pattern: "ListCapabilityMatrix"
---

<objective>Project the canonical database catalog through the existing backend repository, permission cache and platform-admin handler without a second runtime role truth.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/136-capability-policy-catalog-schema-contract/136-CONTEXT.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-RESEARCH.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-01-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend the canonical repository projection and matrix DTO</name>
  <read_first>AGENTS.md; docs/engineering/implementation-contract.md; docs/api/api-contracts.md; database/migrations/0146_capability_policy_catalog.up.sql; backend/internal/repository/authz_permissions.go; backend/internal/repository/authz_capability_mutations.go; backend/internal/handlers/admin_capability_handler.go; backend/internal/handlers/admin_capability_handler_test.go</read_first>
  <files>backend/internal/repository/authz_permissions.go, backend/internal/repository/authz_capability_mutations.go, backend/internal/handlers/admin_capability_handler.go, backend/internal/handlers/admin_capability_handler_test.go, backend/internal/repository/hist_group_member_roles_repository.go, backend/internal/repository/hist_group_member_roles_fansub_group_test.go, backend/internal/handlers/fansub_hist_group_member_roles_handler.go, backend/internal/handlers/fansub_hist_group_member_roles_handler_test.go</files>
  <behavior>
    - Matrix actions return code, label_de, category, sort order, help/description and user_overridable.
    - Matrix roles return code, label_de, contexts, sort order, assignable, semantic presentation keys and derived operative capability count including zero.
    - Every review.* action is present; rows are ordered by catalog sort order/code.
    - Handler authenticates platform admin before querying and returns the project error envelope on repository failure.
  </behavior>
  <action>Extend the existing CapabilityMatrixActionState/RoleEntry/ActionEntry family and ListCapabilityMatrix query; do not create parallel DTOs. Also make `HistGroupMemberRolesRepository.ListFansubGroupRoleDefinitions` the exact owner of the member-scoped projection: return code, label_de, contexts, sort_order, assignable, semantic color/icon keys, and derived operative_capability_count, and have `FansubHistGroupMemberRolesHandler.ListGroupHistoryRoleDefinitions` serialize that DTO unchanged for `context=fansub_group`. Remove handler-side static global labels and derivations where catalog projection owns them. Preserve synthetically projected IdP global roles only as explicitly read-only `global_app_role` provenance, never as role_definitions rows or group override targets per D-04/D-06. Use parameterized queries, rows.Err and wrapped errors. Add exact JSON handler/repository tests covering karaoke_fx, a zero-right role, review capability metadata, protected `user_overridable=false`, member-scoped metadata, and guard-first failure.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/repository ./internal/handlers -run 'Capability|Catalog' -count=1</automated></verify>
  <acceptance_criteria>No handler map supplies runtime role labels; one repository projection returns assignability and operative count directly/derivatively from catalog tables; exact JSON tests pass.</acceptance_criteria>
  <done>The task acceptance criteria are satisfied and its automated verification passes.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Make the permission catalog fail closed and database-owned</name>
  <read_first>backend/internal/permissions/permissions.go; backend/internal/permissions/capability_registry_test.go; backend/internal/repository/authz_permissions.go; database/migrations/0108_capability_registry.up.sql; database/migrations/0146_capability_policy_catalog.up.sql</read_first>
  <files>backend/internal/permissions/permissions.go, backend/internal/permissions/capability_registry_test.go</files>
  <behavior>
    - Startup loads every catalog role/action and metadata atomically; missing/incomplete load cannot fall back to stale role grants.
    - Action constants may remain compile-time enforcement identifiers, but roleMatrix/static role lists are not runtime authority.
    - karaoke_fx is learned from the loader and receives no action unless the DB maps one.
    - platform_admin bypass remains separate and cannot be denied by group catalog data.
  </behavior>
  <action>Extend CatalogLoader and LoadFansubGroupCatalog against the repository projection, replace the stale AllowedActionsForRole roleMatrix fallback with a fail-closed result after guaranteeing startup load/error propagation, and retain compile-time Action constants only for call sites. Add completeness/atomic-swap tests including unknown catalog role, karaoke_fx zero-right, every review action and simulated loader failure. Do not implement per-user override evaluation or mutation in this phase.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/permissions ./internal/repository -count=1 &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>Search of non-test Go runtime code finds no role grant matrix or karaoke-specific branch; failed startup load is surfaced/fails closed; tests prove DB-loaded behavior.</acceptance_criteria>
  <done>The task acceptance criteria are satisfied and its automated verification passes.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| PostgreSQL→permission cache | Catalog rows become authorization inputs. |
| browser→platform catalog handler | Untrusted caller requests privileged metadata. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-136-04 | Elevation | permission fallback | mitigate | Remove stale static grants and fail closed on incomplete catalog load. |
| T-136-05 | Information Disclosure | matrix handler | mitigate | Platform-admin guard runs before repository access. |
| T-136-06 | Spoofing | global-role projection | mitigate | Mark IdP roles read-only and exclude them from group role/override tables. |
</threat_model>
<verification>Focused backend packages pass; inspect for static runtime role authority; `git diff --check` passes.</verification>
<success_criteria>Backend consumers receive complete DB-owned catalog metadata and cannot silently use stale hard-coded grants.</success_criteria>
<output>After completion, create `.planning/phases/136-capability-policy-catalog-schema-contract/136-02-SUMMARY.md`.</output>
