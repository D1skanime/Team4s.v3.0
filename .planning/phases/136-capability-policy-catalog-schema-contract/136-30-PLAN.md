---
phase: 136-capability-policy-catalog-schema-contract
plan: 30
type: execute
wave: 17
depends_on: [136-29]
files_modified: [frontend/src/lib/roleCatalog.ts, frontend/src/lib/roleCatalog.test.ts, frontend/src/lib/roleCatalog.accessibility.test.ts, frontend/src/styles/globals.css, frontend/src/app/admin/fansubs/[id]/edit/FansubAppMemberEditorPanel.tsx, frontend/src/app/admin/fansubs/[id]/edit/FansubAppMemberEditorPanel.test.tsx, frontend/src/components/contributions/AnimeGroupCard.tsx, frontend/src/components/contributions/AnimeGroupCard.test.tsx, frontend/src/app/admin/fansubs/[id]/edit/DefaultCrewManager.tsx, frontend/src/app/admin/fansubs/[id]/edit/DefaultCrewManager.test.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionNotesTab.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionNotesTab.test.tsx]
autonomous: true
gap_closure: true
requirements: [CAP-11, CAP-12, CAP-13, QUAL-01]
must_haves:
  truths:
    - "Member picker, Meine Projekte, default crew and ReleaseVersionNotes show Typesetting and the exact catalog-backed role colors."
    - "Every active Phase-136 consumer receives its bounded semantic data-color-key from the catalog; no CSS selector or TypeScript map keys colors by role code."
    - "Automated token tests resolve all 15 chip treatments and prove WCAG contrast for text/background, border/background, and focus indicator/background."
    - "Labels remain visible; Karaoke-FX remains distinct; Administration, Sonstiges and platform roles remain excluded where already excluded."
  artifacts:
    - {path: frontend/src/app/admin/fansubs/[id]/edit/FansubAppMemberEditorPanel.tsx, provides: "catalog-backed member picker presentation"}
    - {path: frontend/src/components/contributions/AnimeGroupCard.tsx, provides: "catalog-backed Meine Projekte chips"}
    - {path: frontend/src/app/admin/fansubs/[id]/edit/DefaultCrewManager.tsx, provides: "catalog-backed default crew selector/chips"}
    - {path: frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionNotesTab.tsx, provides: "catalog-backed notes role badges"}
  key_links:
    - {from: frontend/src/lib/roleCatalog.ts, to: frontend/src/styles/globals.css, via: "bounded catalog color_key emitted as data-color-key and resolved by the sole semantic key-to-token seam", pattern: "data-color-key|colorKey"}
---
<objective>
Wire the corrected central role semantics into only the active surfaces named by the failed live checkpoint.
Purpose: Close the visible residual without parallel hardcodes, legacy-route work, or broad badge redesign.
Output: Four active surfaces and focused regression tests.
</objective>
<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@docs/engineering/implementation-contract.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-UAT.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-22-SUMMARY.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-23-SUMMARY.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-29-SUMMARY.md
@frontend/src/lib/roleCatalog.ts
@frontend/src/providers/RoleCatalogProvider.tsx
@frontend/src/app/admin/fansubs/[id]/edit/FansubAppMemberEditorPanel.tsx
@frontend/src/components/contributions/AnimeGroupCard.tsx
@frontend/src/app/admin/fansubs/[id]/edit/DefaultCrewManager.tsx
@frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionNotesTab.tsx
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create one bounded semantic color-key seam with exhaustive contrast proof</name>
  <files>frontend/src/lib/roleCatalog.ts, frontend/src/lib/roleCatalog.test.ts, frontend/src/lib/roleCatalog.accessibility.test.ts, frontend/src/styles/globals.css</files>
  <behavior>
    - The adapter accepts only the 15 migrated semantic color_key values plus neutral fallback and returns no presentation keyed from role code.
    - Consumers emit data-color-key (or the adapter's resolved CSS custom property); globals.css contains one semantic color_key-to-token selector family and no role-code-to-color selector/map.
    - Changing a fixture row's color_key changes the rendered consumer treatment without editing a role-specific selector or mapping.
    - All 15 treatments resolve concrete text, background, border and focus colors; computed contrast meets 4.5:1 for normal text/background and 3:1 for border/background and focus-indicator/adjacent-background.
  </behavior>
  <action>Extend roleCatalog.ts and the existing global tokens; do not add a second palette helper. Bound the exact 15 semantic keys from migration 0149, preserve a readable neutral fallback, and expose only data-color-key or resolved CSS variables to components. Define each hex token once and one reusable chip treatment; semantic color_key selectors may resolve tokens, but role codes must never appear in color CSS selectors or a TypeScript role-to-color map. Add a deterministic jsdom/token test that parses/resolves the actual globals.css custom properties for all 15 keys, composites alpha backgrounds, computes relative luminance/contrast, and asserts text at least 4.5:1, border/non-text boundary at least 3:1, and focus indicator against its adjacent background at least 3:1. Add a mutation-style consumer test: change only a catalog fixture color_key and assert the emitted data-color-key/computed token changes without per-role edits.</action>
  <verify><automated>docker compose exec -T team4sv30-frontend npm test -- --run src/lib/roleCatalog.test.ts src/lib/roleCatalog.accessibility.test.ts &amp;&amp; docker compose exec -T team4sv30-frontend npx eslint src/lib/roleCatalog.ts src/lib/roleCatalog.test.ts src/lib/roleCatalog.accessibility.test.ts &amp;&amp; ! grep -nE "data-role-code=.*(typesetter|karaoke_fx|translator|editor|timer|encoder|designer)" frontend/src/styles/globals.css &amp;&amp; git diff --check</automated></verify>
  <done>One semantic color_key-to-token seam owns all 15 accessible chip treatments, with automated contrast and catalog-mutation proof.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Apply the shared semantic-key seam to member picker and Meine Projekte</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/FansubAppMemberEditorPanel.tsx, frontend/src/app/admin/fansubs/[id]/edit/FansubAppMemberEditorPanel.test.tsx, frontend/src/components/contributions/AnimeGroupCard.tsx, frontend/src/components/contributions/AnimeGroupCard.test.tsx</files>
  <behavior>
    - The assignable fansub-group picker orders/labels roles from the catalog, shows Typesetting and Karaoke-FX with their exact accents, and excludes admin/other/platform roles.
    - Meine Projekte renders each role through the shared seam; Typesetting is wine, Karaoke-FX mustard, and labels remain visible.
    - Unknown roles stay readable/neutral without inventing catalog rows.
  </behavior>
  <action>Consume the existing RoleCatalogProvider and the Task-1 roleCatalog/shared chip seam. Do not create local label/color maps. Preserve the member picker's established fansub_group + assignable filter (D-12/D-22) and current Administration/Sonstiges/platform exclusions. In AnimeGroupCard preserve contribution grouping, visibility actions and catalog ordering; change only semantic presentation. Components must emit the bounded catalog data-color-key, never role code for CSS coloring. Add assertions for catalog label, data-color-key, exclusions, unknown fallback, and a fixture whose color_key changes while its role code stays constant.</action>
  <verify><automated>docker compose exec -T team4sv30-frontend npm test -- --run 'src/app/admin/fansubs/[id]/edit/FansubAppMemberEditorPanel.test.tsx' src/components/contributions/AnimeGroupCard.test.tsx</automated></verify>
  <done>Both surfaces render the locked label/palette from the catalog with exclusions unchanged.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Apply the same semantic-key seam to default crew and ReleaseVersionNotes</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/DefaultCrewManager.tsx, frontend/src/app/admin/fansubs/[id]/edit/DefaultCrewManager.test.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionNotesTab.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionNotesTab.test.tsx</files>
  <behavior>
    - Default crew roles come from the contribution catalog in catalog order and display exact labels/colors without changing persistence calls.
    - ReleaseVersionNotes uses the same label/presentation seam for Typesetting and Karaoke-FX and never exposes Typesetting / FX or raw role codes.
    - Existing note editing, crew assignment, capability gating and refresh-session behavior remain intact.
  </behavior>
  <action>Replace only residual local/neutral presentation with the Task-1 shared seam. DefaultCrewManager must derive selectable/displayed roles from the existing anime_contribution catalog rather than a new constant and retain its current API helpers. ReleaseVersionNotes must keep catalog ordering and role-agnostic help copy while emitting bounded catalog data-color-key values. Prove that changing only color_key for typesetter changes both surfaces without role-specific edits. Retain hasAccessToken || hasRefreshToken session gating and the central refresh-capable API client. Do not touch API contracts, Phase-137 override behavior, the legacy releases route, or RoleBadgeCard/Finding #34.</action>
  <verify><automated>docker compose exec -T team4sv30-frontend npm test -- --run 'src/app/admin/fansubs/[id]/edit/DefaultCrewManager.test.tsx' 'src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionNotesTab.test.tsx' &amp;&amp; docker compose exec -T team4sv30-frontend npx eslint 'src/app/admin/fansubs/[id]/edit/FansubAppMemberEditorPanel.tsx' src/components/contributions/AnimeGroupCard.tsx 'src/app/admin/fansubs/[id]/edit/DefaultCrewManager.tsx' 'src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionNotesTab.tsx' &amp;&amp; git diff --check</automated></verify>
  <done>All four failed active surfaces share exact catalog semantics and retain their existing functional/auth boundaries.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| protected browser→central API client | Member, crew and notes data require refresh-capable authenticated requests. |
| catalog projection→interactive selectors | Only context-valid assignable roles may become group assignments. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-136-G37 | Elevation of privilege | member/default-crew selectors | mitigate | Preserve context+assignable filtering and admin/other/platform exclusions; server authorization unchanged. |
| T-136-G38 | Spoofing | protected active surfaces | mitigate | Preserve active-session gating and central refresh seam; never construct bearer headers locally. |
| T-136-G39 | Tampering | role presentation | mitigate | Bound catalog color_key before data-color-key/CSS use; one semantic key-to-token seam and neutral fallback, never role-code CSS. |
</threat_model>
<verification>Run exhaustive token/contrast tests plus all four focused suites, owned-file ESLint, role-code CSS absence gate, and git diff check; scan touched sources to ensure no Typesetting / FX or local palette map remains.</verification>
<success_criteria>The four named active surfaces and existing adapter consumers show exact catalog labels/colors consistently without changing authorization, routes, or badge architecture.</success_criteria>
<output>After completion, create .planning/phases/136-capability-policy-catalog-schema-contract/136-30-SUMMARY.md</output>
