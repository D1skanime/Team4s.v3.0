---
phase: 136-capability-policy-catalog-schema-contract
plan: 03
type: execute
wave: 3
depends_on: [136-02]
files_modified:
  - shared/contracts/admin-capabilities.yaml
  - shared/contracts/openapi.yaml
  - frontend/src/types/admin-capability.ts
  - frontend/src/types/fansub.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/roleCatalog.ts
  - frontend/src/lib/roleCatalog.test.ts
autonomous: true
requirements: [CAP-03, CAP-04, CAP-11, CAP-12, CAP-13, QUAL-01]
must_haves:
  truths:
    - "D-01–D-11: contracts publish scoped precedence, provenance, reason/audit and mutation shapes while runtime mutation/evaluation remains Phase 137."
    - "D-12/D-13: DTOs represent assignable zero-right roles without expanding compact selector UI in this phase."
    - "D-20–D-23: role codes are open strings and one catalog-backed adapter renders karaoke_fx or any future catalog row with a neutral fallback."
  artifacts:
    - path: "frontend/src/lib/roleCatalog.ts"
      provides: "Catalog-backed role lookup/order/presentation adapter; not a role catalog"
    - path: "shared/contracts/admin-capabilities.yaml"
      provides: "Focused canonical API shapes"
  key_links:
    - from: "frontend/src/lib/roleCatalog.ts"
      to: "RoleDefinitionOption"
      via: "injected server catalog rows"
      pattern: "RoleDefinitionOption"
    - from: "frontend/src/lib/api.ts"
      to: "/api/v1/admin/fansubs/{id}/role-definitions"
      via: "central refresh-capable apiClientFetch/authorizedFetch helper"
      pattern: "listFansubGroupRoleDefinitions"
---
<objective>Synchronize API contract layers and introduce the shared catalog-backed frontend role presentation adapter.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/136-capability-policy-catalog-schema-contract/136-CONTEXT.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-RESEARCH.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-02-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Synchronize policy, catalog and future resolver contract shapes</name>
  <read_first>AGENTS.md; docs/api/api-contracts.md; docs/frontend/auth-api-client.md; shared/contracts/admin-capabilities.yaml; shared/contracts/openapi.yaml; backend/internal/repository/authz_capability_mutations.go; backend/internal/handlers/admin_capability_handler.go; frontend/src/types/admin-capability.ts; frontend/src/types/fansub.ts; frontend/src/lib/api.ts</read_first>
  <files>shared/contracts/admin-capabilities.yaml, shared/contracts/openapi.yaml, frontend/src/types/admin-capability.ts, frontend/src/types/fansub.ts, frontend/src/lib/api.ts</files>
  <behavior>
    - Focused YAML, root OpenAPI, Go JSON and TS DTOs share catalog fields exactly.
    - Effective-right provenance distinguishes idp_global_role, group_role, user_allow and user_deny, with decisive/non-deniable state.
    - Override request/result, impact preview, activation status and audit item encode D-01–D-11 without promising endpoints not implemented until 137/138.
    - Group surfaces use the member-scoped role-definition route; platform-only matrix routes stay platform-only.
  </behavior>
  <action>Extend existing schemas/types rather than adding a second family. Define open string role/action codes, catalog label/category/order/help/user_overridable, role contexts/order/assignability/semantic presentation/operative count, precedence/provenance, allow|deny, structured reason fields, before/after audit fields and `persisted|active|pending|failed` status. Mark platform bypass IdP-owned/read-only/non-deniable and audit visibility semantics. Update listRoleCapabilities/listFansubGroupRoleDefinitions through the central API client only; no component fetch, token parameter, bearer construction or Keycloak helper. Remove the closed FansubGroupRoleCode union and FANSUB_GROUP_ROLE_OPTIONS runtime authority from fansub.ts, retaining request compatibility as open strings.</action>
  <verify><automated>docker compose exec -T team4sv30-frontend npm run typecheck &amp;&amp; docker compose exec -T team4sv30-backend go test ./internal/handlers -run Capability -count=1</automated></verify>
  <acceptance_criteria>Contract field names/types match across both YAML files, Go JSON and TS; protected helpers use the central refresh seam; no exported static role options remain in fansub.ts.</acceptance_criteria>
  <done>The task acceptance criteria are satisfied and its automated verification passes.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Build the catalog adapter with semantic presentation and neutral fallback</name>
  <read_first>docs/frontend/ui-system.md; docs/agent-guidelines-ui.md; frontend/src/lib/profileLabels.ts; frontend/src/lib/roleColors.ts; frontend/src/components/profile/MemberBadgeChain.tsx; frontend/src/types/admin-capability.ts; frontend/src/lib/roleCatalog.ts (create); frontend/src/lib/roleCatalog.test.ts (create)</read_first>
  <files>frontend/src/lib/roleCatalog.ts, frontend/src/lib/roleCatalog.test.ts</files>
  <behavior>
    - getRole/labelForRole/orderForContext/presentationForRole accept injected catalog rows and arbitrary string codes.
    - Known roles use server label/order/context and allowlisted semantic presentation keys.
    - Unknown roles render a readable label and neutral `other` presentation without crashing or gaining assignability.
    - Adding injected karaoke_fx requires no adapter source edit or per-consumer role branch.
  </behavior>
  <action>Create a pure adapter/library that receives RoleDefinitionOption[] and exposes stable lookup/filter/order/presentation helpers. It may map finite semantic `color_key`/`icon_key` values to frontend tokens/icons, but must contain no list of valid role codes, labels, grants, badge thresholds or points. Preserve readableCodeLabel fallback and unknown-code retention. Tests must inject karaoke_fx and an invented future_role and prove both flow without modifying the adapter. Do not introduce provider/UI redesign here.</action>
  <verify><automated>docker compose exec -T team4sv30-frontend npm test -- --run frontend/src/lib/roleCatalog.test.ts &amp;&amp; docker compose exec -T team4sv30-frontend npm run typecheck &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>Adapter tests pass; grep of roleCatalog.ts contains no `gfxler|techadmin|founder|co_leader|karaoke_fx|typer` role-code branch or static valid-role array; unknown roles use neutral fallback.</acceptance_criteria>
  <done>The task acceptance criteria are satisfied and its automated verification passes.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| API JSON→React | Catalog labels and semantic keys are untrusted presentation data. |
| browser session→protected catalog API | Refresh-valid sessions must remain active. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-136-07 | Spoofing/XSS | role adapter | mitigate | Render labels as text; allowlist semantic presentation keys and neutralize unknowns. |
| T-136-08 | Elevation | frontend role types | mitigate | Open role codes are display/transport only; backend remains authorization owner. |
| T-136-09 | Session regression | API helper | mitigate | Central apiClientFetch/authorizedFetch only; regression test with absent access token and valid refresh token. |
</threat_model>
<verification>Focused Vitest, frontend typecheck, handler contract tests, no-token boundary test and diff check pass.</verification>
<success_criteria>All contract layers agree and frontend consumers have one data-driven presentation adapter without a second role catalog.</success_criteria>
<output>After completion, create `.planning/phases/136-capability-policy-catalog-schema-contract/136-03-SUMMARY.md`.</output>
