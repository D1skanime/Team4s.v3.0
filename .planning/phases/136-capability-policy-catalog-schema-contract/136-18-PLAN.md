---
phase: 136-capability-policy-catalog-schema-contract
plan: 18
type: execute
wave: 11
depends_on: [136-17]
files_modified:
  - backend/internal/handlers/capability_policy_contract.go
  - backend/internal/handlers/phase136_contract_parity_test.go
autonomous: true
gap_closure: true
requirements: [CAP-04, QUAL-01]
must_haves:
  truths:
    - "Go JSON DTOs mirror every policy schema with the same enum vocabulary, requiredness and nullable platform-admin reason."
    - "A semantic parity test fails on missing schema families, enum/field/optionality drift, or loss of changed/no-op and non-deniable states."
  artifacts:
    - path: backend/internal/handlers/capability_policy_contract.go
      provides: backend-owned DTO contract for future handlers
    - path: backend/internal/handlers/phase136_contract_parity_test.go
      provides: focused/root OpenAPI, Go and TS semantic parity gate
  key_links:
    - from: backend/internal/handlers/capability_policy_contract.go
      to: shared/contracts/admin-capabilities.yaml
      via: JSON tags and enum constants checked semantically
---
<objective>Add backend DTOs and a real cross-layer parity gate for the policy family.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/136-capability-policy-catalog-schema-contract/136-CONTEXT.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-VERIFICATION.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-17-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Mirror policy DTOs in Go and enforce semantic parity</name>
  <read_first>AGENTS.md; docs/api/api-contracts.md; backend/internal/handlers/admin_capability_handler.go; backend/internal/handlers/phase136_contract_parity_test.go; shared/contracts/admin-capabilities.yaml; shared/contracts/openapi.yaml; frontend/src/types/admin-capability.ts</read_first>
  <files>backend/internal/handlers/capability_policy_contract.go, backend/internal/handlers/phase136_contract_parity_test.go</files>
  <behavior>
    - Go types and JSON tags cover the entire Plan-17 schema family and marshal platform-admin reason omission distinctly from a supplied categorized reason.
    - Tests parse/inspect both YAML contracts and Go/TS owners; substring-only field-name checks are insufficient.
    - Removing a schema, required field, enum member, non_deniable, changed/no-op, before/after, or reason optionality makes the test fail.
  </behavior>
  <action>Create backend contract DTOs adjacent to the existing admin-capability handler vocabulary, with typed string constants for shared enums and pointers only where OpenAPI/TS permit absence/null. Replace the shallow Phase136ContractParity assertions with semantic table-driven checks over both YAML documents, reflected Go JSON tags/types and the TypeScript declarations. Keep DTOs transport-only; do not implement resolver, mutation repository, endpoints, or duplicate domain models.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/handlers -run Phase136ContractParity -count=1 &amp;&amp; docker compose exec -T team4sv30-frontend npm run typecheck &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>The named parity suite proves full OpenAPI/Go/TypeScript agreement and catches optionality/enumeration drift, not merely substrings.</acceptance_criteria>
  <done>The task acceptance criteria are satisfied and its automated verification passes.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| JSON transport→Go DTO | Missing/pointer fields can silently weaken policy semantics. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-136-G10 | Tampering | policy DTO parity | mitigate | Semantic reflected/parsing tests across all four owners. |
</threat_model>
<verification>Named Go parity suite, frontend typecheck and `git diff --check` pass.</verification>
<success_criteria>QUAL-01 has an executable cross-layer contract gate.</success_criteria>
<output>After completion, create `.planning/phases/136-capability-policy-catalog-schema-contract/136-18-SUMMARY.md`.</output>
