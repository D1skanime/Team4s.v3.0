---
phase: 136-capability-policy-catalog-schema-contract
plan: 17
type: execute
wave: 10
depends_on: [136-16]
files_modified:
  - shared/contracts/admin-capabilities.yaml
  - shared/contracts/openapi.yaml
  - frontend/src/types/admin-capability.ts
  - backend/internal/handlers/phase136_policy_yaml_ts_contract_test.go
autonomous: true
gap_closure: true
requirements: [CAP-04, QUAL-01]
must_haves:
  truths:
    - "Focused/root OpenAPI and TypeScript publish one complete effective-rights, override, provenance, impact, audit, activation and mutation-result vocabulary."
    - "D-01–D-11 are representable: group scope, allow/deny, non-deniable IdP provenance, optional platform-admin reason, other-text rule, and idempotent/no-op result."
  artifacts:
    - path: shared/contracts/admin-capabilities.yaml
      provides: focused canonical policy schema family
    - path: shared/contracts/openapi.yaml
      provides: root cross-surface mirror
    - path: frontend/src/types/admin-capability.ts
      provides: exact TypeScript mirror
    - path: backend/internal/handlers/phase136_policy_yaml_ts_contract_test.go
      provides: same-plan semantic YAML-to-TypeScript policy-family parity gate
  key_links:
    - from: shared/contracts/admin-capabilities.yaml
      to: frontend/src/types/admin-capability.ts
      via: identical names, requiredness, enums and nullable reason fields
---
<objective>Define the complete synchronized policy contract family before Phase 137 implements the resolver.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/136-capability-policy-catalog-schema-contract/136-CONTEXT.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-VERIFICATION.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-03-SUMMARY.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-16-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify the synchronized policy schema family</name>
  <read_first>AGENTS.md; docs/api/api-contracts.md; docs/frontend/auth-api-client.md; shared/contracts/admin-capabilities.yaml; shared/contracts/openapi.yaml; frontend/src/types/admin-capability.ts; backend/internal/handlers/phase136_contract_parity_test.go; database/migrations/0146_capability_policy_catalog.up.sql; .planning/phases/136-capability-policy-catalog-schema-contract/136-CONTEXT.md</read_first>
  <files>shared/contracts/admin-capabilities.yaml, shared/contracts/openapi.yaml, frontend/src/types/admin-capability.ts, backend/internal/handlers/phase136_policy_yaml_ts_contract_test.go</files>
  <behavior>
    - Matching schemas/types cover EffectiveRightState, provenance, scoped override state/effect/reason, impact preview, immutable audit item, activation status and mutation result.
    - The reason category enum is exactly task_delegation, security_measure, role_gap, other; reason can be absent only for platform-admin mutation, while other text is contractually required/validated.
    - Mutation result distinguishes changed from exact idempotent no-op and carries before/after/effective state without claiming runtime resolver behavior.
    - The same-plan semantic test parses both YAML documents and compares the complete TypeScript policy family; deleting or drifting a schema, required field, enum, nullable reason, before/after, impact/audit/activation field, or changed/no-op state fails Plan 17 itself.
  </behavior>
  <action>Write the full schema family in the focused contract, mirror it in root OpenAPI, and make TypeScript requiredness/enums identical. Model group scope (`group_id`), target user, action code, allow/deny, provenance including `idp_global_role` and `non_deniable`, impact counts/items, immutable audit actor/timestamp/before/after/reason, activation state, and changed/exact-no-op mutation status. The reason-category vocabulary is exactly `task_delegation`, `security_measure`, `role_gap`, `other`; reason is nullable/omittable only for a platform-admin mutation, and `other` requires reason text. Create `phase136_policy_yaml_ts_contract_test.go` in this plan using `gopkg.in/yaml.v3`: parse both YAML files into schema models and semantically compare the complete TypeScript policy-schema family, including schema membership, property types, requiredness, enums, nullable reason semantics, group scope, allow/deny, non-deniable provenance, impact/audit/activation, before/after, changed/no-op, and platform-admin optional reason. Do not use substring-only presence as proof; table-drive exact expected schemas/properties/enums and parse TypeScript declarations/unions sufficiently to fail on optionality or vocabulary drift. Reuse existing naming where sound; remove the partial drift instead of creating a second family. Do not add endpoints, resolver logic, or UI. Plan 18 extends this already-green YAML/TS proof to Go DTO reflection; it is not the first YAML parity gate.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/handlers -run '^TestPhase136PolicyYAMLTypeScriptContract$' -count=1 &amp;&amp; docker compose exec -T team4sv30-frontend npm run typecheck &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>Both OpenAPI files and TypeScript express the same complete policy vocabulary and every D-01–D-11 state needed by later phases.</acceptance_criteria>
  <done>The task acceptance criteria are satisfied and its automated verification passes.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| contract→future mutation implementation | Ambiguous optionality can bypass reason, scope or non-deniable controls. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-136-G08 | Elevation | override schema | mitigate | Explicit group scope, protected provenance and allow/deny enums. |
| T-136-G09 | Repudiation | reason/audit schemas | mitigate | Exact reason and before/after actor/timestamp contract. |
</threat_model>
<verification>The named semantic YAML/TypeScript contract test, TypeScript typecheck and `git diff --check` pass; Plan 18 extends the proven family to Go.</verification>
<success_criteria>The cross-surface policy contract is complete without implementing Phase-137 runtime behavior.</success_criteria>
<output>After completion, create `.planning/phases/136-capability-policy-catalog-schema-contract/136-17-SUMMARY.md`.</output>
