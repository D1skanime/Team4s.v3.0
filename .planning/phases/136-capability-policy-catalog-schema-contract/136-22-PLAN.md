---
phase: 136-capability-policy-catalog-schema-contract
plan: 22
type: execute
wave: 12
depends_on: [136-20]
files_modified:
  - backend/internal/repository/anime_contributions_member_project_repository.go
  - backend/internal/repository/anime_contributions_member_project_repository_test.go
  - frontend/src/components/contributions/AnimeGroupCard.tsx
  - frontend/src/components/contributions/AnimeGroupCard.test.tsx
  - frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.tsx
  - frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.test.tsx
autonomous: true
gap_closure: true
requirements: [CAP-11, CAP-12, CAP-13, QUAL-01]
must_haves:
  truths:
    - "My Projects and project detail use canonical labels, catalog order and semantic colors."
    - "Typesetting and Karaoke-FX remain separate roles on both active surfaces."
  artifacts:
    - path: backend/internal/repository/anime_contributions_member_project_repository.go
      provides: code/label arrays ordered as one catalog-derived pair
    - path: frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.tsx
      provides: catalog-backed role detail presentation
  key_links:
    - from: frontend/src/components/contributions/AnimeGroupCard.tsx
      to: frontend/src/lib/roleCatalog.ts
      via: labelForRole, orderForContext and presentationForRole
---
<objective>Remove the catalog presentation drift on the two active project surfaces.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/136-capability-policy-catalog-schema-contract/136-UAT.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-03-SUMMARY.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-07-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Keep project role codes and labels paired in catalog order</name>
  <read_first>AGENTS.md; docs/engineering/implementation-contract.md; backend/internal/repository/anime_contributions_member_project_repository.go; backend/internal/repository/anime_contributions_member_project_repository_test.go; database/migrations/0146_capability_policy_catalog.up.sql</read_first>
  <files>backend/internal/repository/anime_contributions_member_project_repository.go, backend/internal/repository/anime_contributions_member_project_repository_test.go</files>
  <behavior>
    - role_codes and role_labels have matching indexes.
    - Typesetting, Karaoke-FX and Encoding follow role_definitions.sort_order rather than independent alphabetic sorts.
  </behavior>
  <action>Change the existing lateral aggregation so code and label arrays use the same role row ordering (`sort_order`, then stable code). Do not create a new endpoint or client regrouping seam. Add focused SQL/repository coverage for paired arrays and catalog order.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/repository -run 'MemberProject.*Role|ProjectDetail.*Role' -count=1</automated></verify>
  <done>The project DTO cannot mismatch a code with another role's label and is catalog ordered.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Reuse semantic role presentation on project cards and detail</name>
  <read_first>AGENTS.md; docs/frontend/ui-system.md; docs/agent-guidelines-ui.md; frontend/src/lib/roleCatalog.ts; frontend/src/providers/RoleCatalogProvider.tsx; frontend/src/components/contributions/AnimeGroupCard.tsx; frontend/src/components/contributions/AnimeGroupCard.test.tsx; frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.tsx; frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.test.tsx</read_first>
  <files>frontend/src/components/contributions/AnimeGroupCard.tsx, frontend/src/components/contributions/AnimeGroupCard.test.tsx, frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.tsx, frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.test.tsx</files>
  <behavior>
    - Role chips/rows carry distinct allowlisted semantic color keys from the catalog.
    - Canonical labels and order render Typesetting, Karaoke-FX, Encoding; no Typesetting / FX text appears.
    - Unknown roles remain visible with neutral presentation.
  </behavior>
  <action>Use the existing root `useRoleCatalog('anime_contribution')` and `roleCatalog` adapter in both leaves. Replace the fixed info badge and raw API-label rendering with the established semantic `data-role-code`/CSS token pattern already used by catalog-driven profile surfaces. Do not add leaf catalog requests, local maps, new badge system work (Finding #34), or touch the legacy `/anime/[id]/group/[groupId]/releases` route.</action>
  <verify><automated>docker compose exec -T team4sv30-frontend npm test -- --run src/components/contributions/AnimeGroupCard.test.tsx 'src/app/me/projects/[animeId]/group/[fansubGroupId]/page.test.tsx' &amp;&amp; docker compose exec -T team4sv30-frontend npm run typecheck &amp;&amp; git diff --check</automated></verify>
  <done>Both active surfaces show catalog labels, order and semantic colors with no local role authority.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| DB catalog→project DTO→React | Dynamic catalog metadata crosses into UI presentation. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-136-G17 | Tampering | semantic color metadata | mitigate | Existing allowlisted adapter maps unknown values to neutral. |
| T-136-G18 | Information integrity | paired role arrays | mitigate | One ordering expression plus regression test. |
</threat_model>
<verification>Focused backend/frontend tests, typecheck and diff check pass.</verification>
<success_criteria>UAT screenshots no longer show merged labels, wrong order or identical semantic colors on active project surfaces.</success_criteria>
<output>After completion, create `.planning/phases/136-capability-policy-catalog-schema-contract/136-22-SUMMARY.md`.</output>
