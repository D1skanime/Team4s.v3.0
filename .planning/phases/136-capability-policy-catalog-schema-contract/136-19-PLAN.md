---
phase: 136-capability-policy-catalog-schema-contract
plan: 19
type: execute
wave: 9
depends_on: [136-08]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/lib/api.role-catalog.test.ts
  - frontend/src/providers/RoleCatalogProvider.tsx
  - frontend/src/providers/RoleCatalogProvider.test.tsx
  - frontend/src/app/layout.test.tsx
autonomous: true
gap_closure: true
requirements: [CAP-11, QUAL-01]
must_haves:
  truths:
    - "A public catalog 200 body is accepted only when the top level is an array and every row has all required fields with valid types/contexts."
    - "Malformed objects or incomplete rows throw a visible catalog contract error; the provider exposes a compact context-scoped error instead of empty success."
  artifacts:
    - path: frontend/src/lib/api.role-catalog.test.ts
      provides: top-level and per-row runtime contract negatives
    - path: frontend/src/providers/RoleCatalogProvider.test.tsx
      provides: compact error-state propagation proof
  key_links:
    - from: frontend/src/lib/api.ts
      to: frontend/src/providers/RoleCatalogProvider.tsx
      via: thrown ApiError captured by root load and injected as context error
---
<objective>Fail visibly and safely when the public role catalog violates its runtime contract.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/136-capability-policy-catalog-schema-contract/136-CONTEXT.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-VERIFICATION.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-11-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Runtime-validate every public catalog response row</name>
  <read_first>AGENTS.md; docs/api/api-contracts.md; docs/frontend/auth-api-client.md; frontend/src/lib/api.ts; frontend/src/lib/api.test.ts; frontend/src/types/admin-capability.ts; shared/contracts/openapi.yaml; backend/internal/handlers/role_catalog_handler.go</read_first>
  <files>frontend/src/lib/api.ts, frontend/src/lib/api.role-catalog.test.ts</files>
  <behavior>
    - Non-array 200 JSON throws an ApiError/contract error rather than returning [].
    - Missing/wrong-type code, label_de, contexts, sort_order, assignable, color_key, icon_key, operative_capability_count or has_operative_capabilities throws with a compact German message.
    - Valid empty array remains a valid empty catalog and valid complete rows are returned unchanged.
  </behavior>
  <action>Add focused failing helper tests, then add one reusable private validator at the central listRoleDefinitions helper boundary. Validate the documented top-level array and every required public row field/context value; do not cast unknown JSON. Preserve apiClientFetch and central refresh behavior, add no bearer/token handling, and use the project ApiError shape with correct German umlauts.</action>
  <verify><automated>docker compose exec -T team4sv30-frontend npm test -- --run src/lib/api.role-catalog.test.ts src/lib/api.no-token-boundary.test.ts</automated></verify>
  <acceptance_criteria>Malformed 200 payloads cannot become valid empty/partial catalogs and normal auth-refresh ownership remains central.</acceptance_criteria>
  <done>The task acceptance criteria are satisfied and its automated verification passes.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Surface compact provider errors through the root load</name>
  <read_first>frontend/src/providers/RoleCatalogProvider.tsx; frontend/src/providers/RoleCatalogProvider.test.tsx; frontend/src/app/layout.tsx; frontend/src/app/layout.test.tsx; frontend/src/lib/api.role-catalog.test.ts</read_first>
  <files>frontend/src/providers/RoleCatalogProvider.tsx, frontend/src/providers/RoleCatalogProvider.test.tsx, frontend/src/app/layout.test.tsx</files>
  <behavior>
    - A contract failure in one context produces an error for that context, not an empty-success state, while valid contexts remain available.
    - Total failure exposes one compact German catalog-unavailable state and never restores static role authority.
  </behavior>
  <action>Extend the provider/root tests so thrown helper contract errors are captured by the existing root loading seam and exposed through useRoleCatalog(context). Keep the UI compact and context-scoped; use exactly `Rollenkatalog konnte nicht geladen werden.` as the user-facing fallback when no safe detail exists. Do not add leaf fetches, local role lists, or redesign selectors.</action>
  <verify><automated>docker compose exec -T team4sv30-frontend npm test -- --run src/providers/RoleCatalogProvider.test.tsx src/app/layout.test.tsx &amp;&amp; docker compose exec -T team4sv30-frontend npm run typecheck &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>Consumers can distinguish valid empty data from a compact visible contract failure, per context.</acceptance_criteria>
  <done>The task acceptance criteria are satisfied and its automated verification passes.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| public JSON→trusted catalog | An incomplete response must not become role authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-136-G11 | Tampering | listRoleDefinitions parser | mitigate | Validate every required field and context before publication. |
| T-136-G12 | Denial of Service | provider partial failure | mitigate | Context-scoped compact errors preserve valid contexts without static fallback. |
</threat_model>
<verification>Focused Vitest, auth-boundary test, typecheck and `git diff --check` pass.</verification>
<success_criteria>Malformed catalog data fails visibly and never silently disables role consumers.</success_criteria>
<output>After completion, create `.planning/phases/136-capability-policy-catalog-schema-contract/136-19-SUMMARY.md`.</output>
