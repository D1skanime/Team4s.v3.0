---
phase: 136-capability-policy-catalog-schema-contract
plan: 16
type: execute
wave: 9
depends_on: [136-08]
files_modified:
  - database/migrations/0146_capability_policy_catalog.up.sql
  - backend/internal/migrations/phase136_capability_policy_catalog_test.go
  - frontend/src/lib/roleCatalog.ts
  - frontend/src/lib/roleCatalog.test.ts
autonomous: true
gap_closure: true
requirements: [CAP-12, CAP-13, CAP-14, QUAL-04]
must_haves:
  truths:
    - "Every existing capability, explicitly every review.* action, has canonical category, order, German label and help metadata."
    - "The exact karaoke_fx migration row uses semantic color/icon keys accepted by the shared adapter and remains distinct from Typesetting."
    - "Migration 0146 passes a fresh disposable PostgreSQL Up/Down/Up cycle with constraints, seeds and reverse indexes verified."
  artifacts:
    - path: database/migrations/0146_capability_policy_catalog.up.sql
      provides: complete catalog metadata, one reason vocabulary and supported karaoke presentation keys
    - path: backend/internal/migrations/phase136_capability_policy_catalog_test.go
      provides: exact-row, completeness, index and live reversibility proof
  key_links:
    - from: database/migrations/0146_capability_policy_catalog.up.sql
      to: frontend/src/lib/roleCatalog.ts
      via: exact shared color_key/icon_key vocabulary
---
<objective>Repair catalog completeness and semantic-key drift, then prove the reversible schema on fresh disposable PostgreSQL.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/136-capability-policy-catalog-schema-contract/136-CONTEXT.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-VERIFICATION.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-01-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Complete capability metadata and align exact Karaoke-FX semantics</name>
  <read_first>AGENTS.md; database/migrations/0108_capability_registry.up.sql; database/migrations/0146_capability_policy_catalog.up.sql; database/migrations/0146_capability_policy_catalog.down.sql; backend/internal/migrations/phase136_capability_policy_catalog_test.go; frontend/src/lib/roleCatalog.ts; frontend/src/lib/roleCatalog.test.ts; frontend/src/types/admin-capability.ts</read_first>
  <files>database/migrations/0146_capability_policy_catalog.up.sql, backend/internal/migrations/phase136_capability_policy_catalog_test.go, frontend/src/lib/roleCatalog.ts, frontend/src/lib/roleCatalog.test.ts</files>
  <behavior>
    - No action_definitions row has empty category, non-deterministic order, empty German label, or empty help text after 0146; every review.* row is asserted individually.
    - The exact karaoke_fx row's color_key/icon_key are accepted by presentationForRole and are non-neutral and distinct from typer/typesetter.
    - SQL uses the agreed English identifiers task_delegation, security_measure, role_gap, other for override reasons; other requires text and platform-admin reason optionality remains representable.
  </behavior>
  <action>Add failing migration source/live assertions for the complete existing action inventory, with an explicit review.* enumeration rather than checking only the seven new actions. Populate canonical German metadata for every current capability in 0146 without editing historical migrations. Align karaoke_fx to existing supported semantic keys or extend the bounded semantic-key adapter registry once; do not add a role-code allowlist. Normalize the migration reason CHECK vocabulary to the locked cross-layer identifiers and preserve D-07–D-11 constraints. No backfill/compatibility code is needed because test data is disposable.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/migrations -run Phase136 -count=1 &amp;&amp; docker compose exec -T team4sv30-frontend npm test -- --run src/lib/roleCatalog.test.ts</automated></verify>
  <acceptance_criteria>All actions including review.* have complete canonical metadata, and the exact seeded karaoke_fx row renders with supported non-neutral semantic keys.</acceptance_criteria>
  <done>The task acceptance criteria are satisfied and its automated verification passes.</done>
</task>
<task type="auto">
  <name>Task 2: Run the fresh PostgreSQL Up/Down/Up proof</name>
  <read_first>AGENTS.md; docker-compose.yml; backend/internal/migrations/phase136_capability_policy_catalog_test.go; database/migrations/0146_capability_policy_catalog.up.sql; database/migrations/0146_capability_policy_catalog.down.sql</read_first>
  <files>backend/internal/migrations/phase136_capability_policy_catalog_test.go</files>
  <action>Use the existing Compose database and the test's isolated disposable schema/database mechanism to run fresh Up, verify schema/metadata/seeds/checks/reverse indexes and representative EXPLAIN, Down, verify removal, then Up and repeat assertions. Do not reset live reference profiles, volumes, media, or .env; do not edit an older migration. Record the exact Docker Compose command/result in the summary.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/migrations -run 'Phase136.*Live|Phase136.*Fresh|Phase136' -count=1 &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>A fresh disposable PostgreSQL Up/Down/Up run executes rather than skips and verifies the complete 0146 contract twice.</acceptance_criteria>
  <done>The task acceptance criteria are satisfied and its automated verification passes.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| migration rows→runtime catalogs | Stored metadata controls permission explanation and role presentation. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-136-G05 | Tampering | action metadata | mitigate | Completeness assertions cover every row and every review.* capability. |
| T-136-G06 | Spoofing | karaoke semantic keys | mitigate | Exact migration-row adapter test; bounded semantic keys only. |
| T-136-G07 | Denial of Service | reverse lookups | mitigate | Live index and representative EXPLAIN assertions after both Up runs. |
</threat_model>
<verification>Migration, adapter, fresh Up/Down/Up and `git diff --check` gates pass.</verification>
<success_criteria>CAP-12, CAP-13 and QUAL-04 are evidenced by complete metadata and a real reversible fresh-database run.</success_criteria>
<output>After completion, create `.planning/phases/136-capability-policy-catalog-schema-contract/136-16-SUMMARY.md`.</output>
