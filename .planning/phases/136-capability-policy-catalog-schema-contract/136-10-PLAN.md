---
phase: 136-capability-policy-catalog-schema-contract
plan: 10
type: execute
wave: 2
depends_on: [136-01]
files_modified:
  - backend/internal/repository/role_catalog_repository.go
  - backend/internal/repository/role_catalog_repository_test.go
  - backend/internal/handlers/role_catalog_handler.go
  - backend/internal/handlers/role_catalog_handler_test.go
  - backend/internal/handlers/role_catalog_router_integration_test.go
  - backend/cmd/server/main.go
autonomous: true
requirements: [CAP-11, CAP-13, QUAL-01]
must_haves:
  truths:
    - "D-20–D-23: one unauthenticated presentation-only endpoint exposes catalog roles for exactly fansub_group, anime_contribution and group_history."
    - "The public payload is ordered and never leaks grants, actions, overrides, audit data or IdP global roles."
    - "The unused legacy route /anime/[id]/group/[groupId]/releases is neither changed, catalog-wired, tested nor deleted in Phase 136."
  artifacts:
    - path: "backend/internal/repository/role_catalog_repository.go"
      provides: "Public presentation-only role catalog projection"
    - path: "backend/internal/handlers/role_catalog_router_integration_test.go"
      provides: "Exact router registration, audience and payload proof"
  key_links:
    - from: "GET /api/v1/role-definitions?context={context}"
      to: "RoleCatalogRepository.ListPublicRoleDefinitions"
      via: "RoleCatalogHandler registered directly on v1 without auth middleware"
      pattern: "role-definitions"
---

<objective>Expose the canonical role catalog through one bounded, unauthenticated, presentation-only backend route.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/136-capability-policy-catalog-schema-contract/136-CONTEXT.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-RESEARCH.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-01-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify public projection and exact router audience</name>
  <read_first>AGENTS.md; docs/api/api-contracts.md; database/migrations/0146_capability_policy_catalog.up.sql; backend/cmd/server/main.go; existing router integration tests; backend/internal/repository/role_catalog_repository.go (create); backend/internal/handlers/role_catalog_handler.go (create)</read_first>
  <files>backend/internal/repository/role_catalog_repository_test.go, backend/internal/handlers/role_catalog_handler_test.go, backend/internal/handlers/role_catalog_router_integration_test.go</files>
  <behavior>
    - Unauthenticated GET /api/v1/role-definitions succeeds for exactly fansub_group, anime_contribution and group_history.
    - Missing/invalid context returns the project 400 envelope; no auth challenge occurs.
    - Rows are ordered by sort_order then code and include only code, label_de, contexts, sort_order, assignable, semantic presentation metadata, operative_capability_count and derived zero-right state.
    - JSON never contains action/grant/override/audit/global-role fields; karaoke_fx is a zero-right fixture.
  </behavior>
  <action>Write repository, handler and router-level integration tests first. The router test must construct the real v1 route registration from main/server routing seams, issue unauthenticated requests for all three allowed contexts, assert invalid context 400, stable ordering, exact allowed JSON keys and forbidden sensitive fields. Assert the legacy `/anime/[id]/group/[groupId]/releases` page is outside this plan; do not add route coverage or frontend wiring for it.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/repository ./internal/handlers -run 'RoleCatalog|RoleDefinitionsRouter' -count=1</automated></verify>
  <acceptance_criteria>Tests fail before implementation and explicitly prove exact public registration, unauthenticated audience, three contexts, invalid-context 400, ordering, zero-right karaoke_fx and forbidden payload fields.</acceptance_criteria>
  <done>The task acceptance criteria are satisfied and its automated verification passes.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement the sole public presentation catalog route</name>
  <read_first>backend/internal/repository/role_catalog_repository_test.go; backend/internal/handlers/role_catalog_handler_test.go; backend/internal/handlers/role_catalog_router_integration_test.go; backend/cmd/server/main.go; backend/internal/repository/authz_capability_mutations.go</read_first>
  <files>backend/internal/repository/role_catalog_repository.go, backend/internal/handlers/role_catalog_handler.go, backend/cmd/server/main.go</files>
  <action>Implement `RoleCatalogRepository.ListPublicRoleDefinitions(ctx, contextName)` and `RoleCatalogHandler.ListPublicRoleDefinitions`; allowlist exactly the three contexts and select only presentation metadata plus derived operative count/zero-right state. Register `v1.GET("/role-definitions", roleCatalogHandler.ListPublicRoleDefinitions)` outside auth middleware. Reuse catalog tables and project error envelopes; do not introduce a static Go role list, page-specific endpoint or second catalog. Do not modify or delete `/anime/[id]/group/[groupId]/releases`.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/repository ./internal/handlers -run 'RoleCatalog|RoleDefinitionsRouter' -count=1 &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>The exact router-level contract passes unauthenticated and the public response cannot expose authorization data.</acceptance_criteria>
  <done>The task acceptance criteria are satisfied and its automated verification passes.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| public caller→catalog handler | Unauthenticated input selects a bounded presentation context. |
| database→public JSON | Internal authorization metadata must not cross the public projection. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-136-24 | Information Disclosure | public catalog DTO | mitigate | Explicit projection and exact-key/forbidden-field router assertions. |
| T-136-25 | Denial of Service | context selector | mitigate | Fixed three-value allowlist and parameterized bounded query. |
</threat_model>
<verification>Focused repository/handler/router integration tests and diff check pass.</verification>
<success_criteria>Clients have one safe public role catalog endpoint with exact audience and payload guarantees.</success_criteria>
<output>After completion, create `.planning/phases/136-capability-policy-catalog-schema-contract/136-10-SUMMARY.md`.</output>
