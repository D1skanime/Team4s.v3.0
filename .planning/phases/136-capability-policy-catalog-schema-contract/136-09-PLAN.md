---
phase: 136-capability-policy-catalog-schema-contract
plan: 09
type: execute
wave: 2
depends_on: [136-01]
files_modified:
  - backend/internal/permissions/permissions.go
  - backend/internal/handlers/fansub_media_upload.go
  - backend/internal/handlers/fansub_media_review_handler.go
  - backend/internal/handlers/fansub_groups.go
  - backend/internal/handlers/fansub_group_links.go
  - backend/internal/handlers/fansub_group_history_handler.go
  - backend/internal/handlers/phase136_narrow_role_defaults_enforcement_test.go
  - database/migrations/0146_capability_policy_catalog.up.sql
  - backend/internal/migrations/phase136_capability_policy_catalog_test.go
autonomous: true
requirements: [CAP-13]
must_haves:
  truths:
    - "D-15, D-16, D-17, D-18, D-19: every seeded operative role default is checked by an exact server mutation action; no seed points at an unenforced capability."
    - "Media deletion, member administration, role/capability administration and broad fansub_group.edit are not granted by the confirmed defaults."
    - "Phase 137 remains the sole owner of per-user Allow/Deny precedence and resolver behavior."
  artifacts:
    - path: "backend/internal/handlers/phase136_narrow_role_defaults_enforcement_test.go"
      provides: "Executable seed-to-handler enforcement map for every confirmed default"
  key_links:
    - from: "database/migrations/0146_capability_policy_catalog.up.sql"
      to: "fansub media/page/link/history mutation handlers"
      via: "exact action codes asserted by the enforcement contract test"
      pattern: "fansub_group_(media|page|links)"
---
<objective>Wire every narrow capability seeded for gfxler, techadmin, founder and co_leader to its exact server-side mutation handler before the role defaults are accepted.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/136-capability-policy-catalog-schema-contract/136-CONTEXT.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-RESEARCH.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-01-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify the seed-to-handler authorization contract</name>
  <read_first>AGENTS.md; database/migrations/0146_capability_policy_catalog.up.sql; backend/internal/permissions/permissions.go; backend/internal/handlers/fansub_media_upload.go; backend/internal/handlers/fansub_media_review_handler.go; backend/internal/handlers/fansub_groups.go; backend/internal/handlers/fansub_group_links.go; backend/internal/handlers/fansub_group_history_handler.go; existing permission-deny handler tests</read_first>
  <files>backend/internal/handlers/phase136_narrow_role_defaults_enforcement_test.go</files>
  <behavior>
    - Every D-15–D-18 seed action is referenced by one named mutation handler before repository mutation.
    - gfxler media upload/update/reorder is allowed by its narrow actions, while media delete remains denied.
    - techadmin technical-link edits, founder founding/history edits and co_leader general-page/link edits use separate narrow actions.
    - Requests mixing fields from multiple permission classes require every applicable action and cannot smuggle a broad field through a narrow grant.
  </behavior>
  <action>Create a table-driven enforcement contract test that parses the 0146 role_capabilities seeds and maps each seeded action to the exact handler and denial test. Exercise 403/no-repository-mutation behavior for missing actions, mixed-field PATCH requests, foreign-group scope, deletion, member admin and role admin. The test must fail while any seed has no handler enforcement. Do not test or implement per-user override precedence.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/handlers -run Phase136NarrowRoleDefaults -count=1</automated></verify>
  <acceptance_criteria>The failing test enumerates every exact seeded action and proves broad fansub_group.edit is neither required nor granted for the narrow operations.</acceptance_criteria>
  <done>The task acceptance criteria are satisfied and its automated verification passes.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Enforce narrow media, page-field, link and history actions</name>
  <read_first>backend/internal/handlers/phase136_narrow_role_defaults_enforcement_test.go; backend/internal/permissions/permissions.go; backend/internal/handlers/fansub_media_upload.go; backend/internal/handlers/fansub_media_review_handler.go; backend/internal/handlers/fansub_groups.go; backend/internal/handlers/fansub_group_links.go; backend/internal/handlers/fansub_group_history_handler.go; backend/internal/models/fansub.go</read_first>
  <files>backend/internal/permissions/permissions.go, backend/internal/handlers/fansub_media_upload.go, backend/internal/handlers/fansub_media_review_handler.go, backend/internal/handlers/fansub_groups.go, backend/internal/handlers/fansub_group_links.go, backend/internal/handlers/fansub_group_history_handler.go, database/migrations/0146_capability_policy_catalog.up.sql, backend/internal/migrations/phase136_capability_policy_catalog_test.go</files>
  <action>Add compile-time constants matching the 0146 narrow catalog codes. Keep image/logo/banner upload on media.upload, metadata changes on media.update, and ordering on media.reorder; never reuse media.delete. Authorize UpdateFansub only after binding/validating the patch and derive required actions from the fields actually set: general content, technical-link fields, and founding/history fields are independent checks, and mixed patches require all applicable checks. UpdateFansubLink uses links.update without authorizing delete. UpdateGroupHistory uses page.founding_history_edit rather than members.manage. Preserve group-scope/BOLA checks, audit the exact denied/allowed action, and keep platform-admin behavior through the existing permission service. Only after those enforcement tests pass, add the D-15–D-18 role_capabilities mappings to migration 0146 and extend its fresh migration assertions; D-19 exclusions must be asserted explicitly. Do not add per-user resolver logic, broad grants, UI, or new endpoints.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/handlers -run 'Phase136NarrowRoleDefaults|FansubMedia|FansubGroupHistory|FansubLink|UpdateFansub' -count=1 &amp;&amp; docker compose exec -T team4sv30-backend go test ./internal/migrations -run Phase136 -count=1 &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>Every confirmed default reaches a real guarded operation; missing or mixed-field permissions fail before mutation; delete/member/role/capability administration remain excluded.</acceptance_criteria>
  <done>The task acceptance criteria are satisfied and its automated verification passes.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| request fields→mutation action | Untrusted partial patches determine which narrow permission is required. |
| seeded role grant→runtime handler | A catalog grant is dangerous if no exact server handler enforces it. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-136-21 | Elevation | mixed fansub patch | mitigate | Bind first, derive all required actions, require every action before mutation. |
| T-136-22 | Tampering | media/link/history handlers | mitigate | Exact action per operation, group-scoped permission check and no-mutation denial tests. |
| T-136-23 | Elevation | broad/deletion capabilities | mitigate | Seed-to-handler test rejects broad or destructive mappings for D-15–D-19 roles. |
</threat_model>
<verification>Focused seed-to-handler, denial/no-mutation, mixed-field and group-scope tests pass; `git diff --check` passes.</verification>
<success_criteria>Confirmed role defaults are usable only through exact enforced operations, with no broad or destructive privilege leakage.</success_criteria>
<output>After completion, create `.planning/phases/136-capability-policy-catalog-schema-contract/136-09-SUMMARY.md`.</output>
