---
phase: 136-capability-policy-catalog-schema-contract
plan: 24
type: execute
wave: 12
depends_on: [136-20]
files_modified:
  - frontend/src/app/me/releases/[versionId]/workspace/page.tsx
  - frontend/src/app/me/releases/[versionId]/workspace/page.test.tsx
  - frontend/src/app/me/releases/[versionId]/workspace/workspace.module.css
autonomous: true
gap_closure: true
requirements: [CAP-13, QUAL-01]
must_haves:
  truths:
    - "A contributor with can_manage_segments sees the existing segment editor in the release workspace."
    - "Previous/next navigation stays within the user's current anime/group project and follows the existing stable project release order."
  artifacts:
    - path: frontend/src/app/me/releases/[versionId]/workspace/page.tsx
      provides: segment tab and project-scoped adjacent release navigation
  key_links:
    - from: frontend/src/app/me/releases/[versionId]/workspace/page.tsx
      to: frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx
      via: existing release-version context props guarded by can_manage_segments
    - from: frontend/src/app/me/releases/[versionId]/workspace/page.tsx
      to: frontend/src/lib/api.ts#getMyProjectDetail
      via: central authorized helper and selected group/anime IDs
---
<objective>Wire the already-built segment and adjacent navigation seams into the active contributor workspace.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/136-capability-policy-catalog-schema-contract/136-UAT.md
@docs/frontend/auth-api-client.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add capability-gated segments to the contributor workspace</name>
  <read_first>AGENTS.md; docs/engineering/implementation-contract.md; docs/frontend/auth-api-client.md; frontend/src/app/me/releases/[versionId]/workspace/page.tsx; frontend/src/app/me/releases/[versionId]/workspace/page.test.tsx; frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx; frontend/src/types/episodeVersion.ts; frontend/src/types/releaseVersionMedia.ts</read_first>
  <files>frontend/src/app/me/releases/[versionId]/workspace/page.tsx, frontend/src/app/me/releases/[versionId]/workspace/page.test.tsx</files>
  <behavior>
    - can_manage_segments=true adds a Segmente tab using the existing SegmenteTab.
    - can_manage_segments=false never mounts segment controls.
    - Segment-only access counts as valid workspace access.
  </behavior>
  <action>Import and render the existing `SegmenteTab` with the canonical IDs already present in `EpisodeVersionEditorContext`; pass the real release version ID and existing anime/group/episode values. Extend `hasAnyWorkspaceAccess` and default-tab selection. Do not clone segment logic, add endpoints, or attach segment/media data to episodes.</action>
  <verify><automated>docker compose exec -T team4sv30-frontend npm test -- --run 'src/app/me/releases/[versionId]/workspace/page.test.tsx'</automated></verify>
  <done>The existing segment editor is reachable exactly when the server capability permits it.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add project-scoped previous and next release navigation</name>
  <read_first>AGENTS.md; docs/frontend/ui-system.md; frontend/src/app/me/releases/[versionId]/workspace/page.tsx; frontend/src/app/me/releases/[versionId]/workspace/page.test.tsx; frontend/src/components/ui/AdjacentNavigation.tsx; frontend/src/lib/api.ts; frontend/src/types/contributions.ts</read_first>
  <files>frontend/src/app/me/releases/[versionId]/workspace/page.tsx, frontend/src/app/me/releases/[versionId]/workspace/page.test.tsx, frontend/src/app/me/releases/[versionId]/workspace/workspace.module.css</files>
  <behavior>
    - Middle releases show previous and next; endpoints show only the available neighbor.
    - Hrefs target `/me/releases/{id}/workspace` and preserve the validated project `return_to`.
    - A release absent from the user's project list yields no cross-project navigation.
  </behavior>
  <action>After resolving the selected group and anime, call existing `getMyProjectDetail` through the central refresh-aware client, locate the current release in its stable `releases` order, and render existing `AdjacentNavigation`. Keep loading/error state local to navigation so media/notes/segments remain usable. Do not use or modernize the legacy anime/group releases route.</action>
  <verify><automated>docker compose exec -T team4sv30-frontend npm test -- --run 'src/app/me/releases/[versionId]/workspace/page.test.tsx' &amp;&amp; docker compose exec -T team4sv30-frontend npm run typecheck &amp;&amp; git diff --check</automated></verify>
  <done>Contributor navigation is bounded to the authenticated user's current project release list.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| protected project DTO→navigation | User-specific release membership controls visible neighbors. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-136-G20 | Information disclosure | adjacent release navigation | mitigate | Use only getMyProjectDetail results for selected group/anime; no global release list. |
| T-136-G21 | Elevation of privilege | segment tab | mitigate | Server capability gate plus existing server-side segment guards; UI is not authority. |
| T-136-G22 | Session regression | protected helper | mitigate | Reuse authorizedFetch helper and test refresh-token-valid path. |
</threat_model>
<verification>Workspace tests cover capability, neighbors, bounds and refresh-session behavior; typecheck passes.</verification>
<success_criteria>The active contributor route exposes the existing segments and navigation without parallel APIs or legacy-route work.</success_criteria>
<output>After completion, create `.planning/phases/136-capability-policy-catalog-schema-contract/136-24-SUMMARY.md`.</output>
