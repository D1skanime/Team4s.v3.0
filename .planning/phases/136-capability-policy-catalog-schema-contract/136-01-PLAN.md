---
phase: 136-capability-policy-catalog-schema-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/0146_capability_policy_catalog.up.sql
  - database/migrations/0146_capability_policy_catalog.down.sql
  - backend/internal/migrations/phase136_capability_policy_catalog_test.go
autonomous: true
requirements: [CAP-03, CAP-04, CAP-12, CAP-13, CAP-14, QUAL-04]
must_haves:
  truths:
    - "D-01–D-06: The schema and catalog describe group-scoped allow/deny overrides, deny precedence, explicit opt-in, protected capabilities, and a non-deniable IdP platform bypass without implementing the Phase-137 resolver."
    - "D-07–D-11: Current override state and immutable history can record required reason categories, platform-admin reason exemption, full provenance, scoped visibility inputs, and real transitions without encoding no-op rows."
    - "D-12 and D-14–D-21: assignability remains independent of operative rights; confirmed narrow defaults and distinct zero-right karaoke_fx are seeded centrally."
    - "A fresh disposable database applies and reverses migration 0146 with reverse-lookup indexes and no compatibility/backfill path."
  artifacts:
    - path: "database/migrations/0146_capability_policy_catalog.up.sql"
      provides: "Canonical catalog metadata, narrow role defaults, override state/history schema, constraints and indexes"
    - path: "database/migrations/0146_capability_policy_catalog.down.sql"
      provides: "Dependency-ordered reversal"
    - path: "backend/internal/migrations/phase136_capability_policy_catalog_test.go"
      provides: "Fresh up/down and seed/index contract proof"
  key_links:
    - from: "user_group_capability_overrides.action_code"
      to: "action_definitions.code"
      via: "foreign key plus user_overridable policy metadata"
      pattern: "REFERENCES action_definitions"
    - from: "role_capabilities"
      to: "role_definitions/action_definitions"
      via: "confirmed narrow defaults and reverse index"
      pattern: "role_capabilities_action_role_idx"
---

<objective>
Establish the reversible database authority for capability policy, role presentation, confirmed defaults, scoped override state and immutable audit history.

Purpose: Make later resolver and UI work consume one enforceable catalog/schema instead of inventing policy in code.
Output: Migration 0146 plus fresh-database contract tests.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-capability-policy-catalog-schema-contract/136-CONTEXT.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-RESEARCH.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-PATTERNS.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify the fresh schema and catalog contract in migration tests</name>
  <read_first>AGENTS.md; docs/engineering/implementation-contract.md; database/migrations/0108_capability_registry.up.sql; database/migrations/0112_role_model_cleanup.up.sql; database/migrations/0121_neutral_role_labels.up.sql; database/migrations/0145_member_public_identity_visibility.up.sql; database/migrations/0145_member_public_identity_visibility.down.sql; backend/internal/migrations/phase136_capability_policy_catalog_test.go (create); existing tests under backend/internal/migrations</read_first>
  <files>backend/internal/migrations/phase136_capability_policy_catalog_test.go</files>
  <behavior>
    - Fresh up exposes description/help, sort/category and user_overridable=false-by-default on every action, including review actions, per D-02/D-03/D-05 and CAP-12.
    - karaoke_fx is assignable in fansub_group and anime_contribution contexts, distinct from typer/typesetting, and has zero operative capabilities per D-20/D-21.
    - gfxler, techadmin, founder and co_leader receive only the confirmed narrow defaults from D-15–D-19.
    - Current override rows are unique per user/group/action with allow|deny effects; history can represent before/after plus actor/reason while exact no-op suppression remains Phase 137 service behavior per D-01/D-04/D-07–D-11.
    - Reverse role/action and override subject/action indexes exist; down then up is repeatable.
  </behavior>
  <action>Create table-driven PostgreSQL migration assertions before implementation. Assert exact columns, defaults, FKs, CHECK/UNIQUE constraints, reason-category shape, indexes and seed rows. Assert protected/global/admin actions remain `user_overridable=false`, platform admin is not stored as a group override, and no migration edits/backfill compatibility are used. Include a catalog query proving roles with zero mappings remain visible.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/migrations -run Phase136 -count=1</automated></verify>
  <acceptance_criteria>The focused test initially fails because 0146 does not exist, then names every required object/seed/index explicitly; grep finds `karaoke_fx`, `user_overridable`, `allow`, `deny`, and `role_capabilities_action_role_idx` in the test.</acceptance_criteria>
  <done>The task acceptance criteria are satisfied and its automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement reversible catalog, defaults, override and audit schema</name>
  <read_first>backend/internal/migrations/phase136_capability_policy_catalog_test.go; database/migrations/0108_capability_registry.up.sql; database/migrations/0112_role_model_cleanup.up.sql; database/migrations/0121_neutral_role_labels.up.sql; database/migrations/0145_member_public_identity_visibility.up.sql; database/migrations/0145_member_public_identity_visibility.down.sql; docs/architecture/db-schema-fansub-domain.md</read_first>
  <files>database/migrations/0146_capability_policy_catalog.up.sql, database/migrations/0146_capability_policy_catalog.down.sql</files>
  <action>Add the next paired migration only. Extend action_definitions with German help/description and `user_overridable boolean NOT NULL DEFAULT false`; extend role_definitions only with stable semantic presentation metadata needed by clients, never CSS classes/component names. Seed exact field/media capability keys after reconciling current handlers: upload/update/reorder group images/logo/banner, technical links, founding/history data, and general page content/links. Map them to gfxler/techadmin/founder/co_leader exactly per D-15–D-19 and exclude deletion/member/role/capability administration. Seed `karaoke_fx` in both contexts, assignable and zero-right. Create current override and append-only history tables with app-user/group/action/actor FKs, constrained effect and reason categories, timestamps, before/after effect support, uniqueness and reverse indexes. Encode the documented precedence/provenance vocabulary in schema comments/contract metadata, but do not implement runtime evaluation, UI, or audit mutation logic assigned to Phases 137–138. Down must remove new objects in dependency order. Do not preserve/backfill disposable rows and never edit historical migrations.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/migrations -run Phase136 -count=1 &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>Fresh up/down/up passes; all new capabilities default non-overridable unless explicitly opted in; protected actions cannot be seeded overridable; confirmed roles have only requested grants; karaoke_fx is catalog-visible with zero mappings; reverse lookup EXPLAIN/assertions use the new indexes.</acceptance_criteria>
  <done>The task acceptance criteria are satisfied and its automated verification passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| writer→PostgreSQL | Any future writer must be unable to persist malformed scope/effect/provenance rows. |
| group policy→IdP authority | Group data must not represent or deny platform-admin authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-136-01 | Tampering/Elevation | override schema | mitigate | Composite uniqueness, FKs, constrained effects, fail-closed catalog flag and protected seed assertions. |
| T-136-02 | Repudiation | history schema | mitigate | Append-only event shape with actor, target, group, action, before/after, reason and timestamp; transactional writes deferred explicitly to 137. |
| T-136-03 | Denial of Service | reverse lookups | mitigate | Action-first and subject/group indexes verified on representative fixtures. |
</threat_model>

<verification>Run the focused migration test, inspect the 0146-only diff, and run `git diff --check`.</verification>
<success_criteria>The database is the sole enforceable catalog/schema authority and a clean disposable database proves reversible installation.</success_criteria>
<output>After completion, create `.planning/phases/136-capability-policy-catalog-schema-contract/136-01-SUMMARY.md`.</output>
