---
phase: 136-capability-policy-catalog-schema-contract
plan: 15
type: execute
wave: 9
depends_on: [136-08]
files_modified:
  - backend/internal/handlers/fansub_admin.go
  - backend/internal/handlers/fansub_group_links.go
  - backend/internal/handlers/fansub_group_links_test.go
  - backend/internal/repository/fansub_repository.go
  - backend/internal/repository/fansub_repository_group_links_test.go
autonomous: true
gap_closure: true
requirements: [CAP-13, QUAL-01]
must_haves:
  truths:
    - "D-11: an allowed link-update audit record exists only after validation and successful scoped persistence."
    - "Invalid, conflicting, missing, failed, and exact no-op updates never create an allowed domain-audit event."
  artifacts:
    - path: backend/internal/handlers/fansub_group_links_test.go
      provides: mutation/audit ordering and no-op regression tests
    - path: backend/internal/repository/fansub_repository_group_links_test.go
      provides: transactional changed/no-op result and rollback regression tests
  key_links:
    - from: backend/internal/handlers/fansub_group_links.go
      to: AuditLogRepository.Write
      via: successful UpdateGroupLink result only
---
<objective>Make allowed link-update audit entries truthful and ordered after the successful scoped state transition.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/136-capability-policy-catalog-schema-contract/136-CONTEXT.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-VERIFICATION.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-09-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Move allowed audit behind the real link transition</name>
  <read_first>AGENTS.md; docs/engineering/implementation-contract.md; backend/internal/handlers/fansub_admin.go; backend/internal/handlers/fansub_group_links.go; backend/internal/handlers/fansub_group_links_test.go; backend/internal/repository/audit_logs.go; backend/internal/repository/fansub_repository.go; backend/internal/repository/fansub_repository_test.go</read_first>
  <files>backend/internal/handlers/fansub_admin.go, backend/internal/handlers/fansub_group_links.go, backend/internal/handlers/fansub_group_links_test.go, backend/internal/repository/fansub_repository.go, backend/internal/repository/fansub_repository_group_links_test.go</files>
  <behavior>
    - `TestPhase136GroupLinkAuditValidationFailure`, `TestPhase136GroupLinkAuditPermissionDenied`, `TestPhase136GroupLinkAuditRepositoryNotFound`, `TestPhase136GroupLinkAuditRepositoryConflict`, `TestPhase136GroupLinkAuditRepositoryFailure`, and `TestPhase136GroupLinkAuditExactNoOp` assert mutation-call count, allowed-audit count, and zero false allowed audit.
    - `TestPhase136GroupLinkAuditSuccess` asserts exactly one repository mutation occurs before exactly one allowed `fansub_group_link.updated` audit, including scoped target/action payload.
    - Permission denial retains the existing denied security/audit behavior, records no allowed event, and never calls mutation.
  </behavior>
  <action>Write the named `TestPhase136GroupLinkAudit...` spy suite first. In `fansub_admin.go`, add narrow handler-owned function seams initialized by `NewFansubHandler`: an update function matching `func(context.Context, int64, int64, models.FansubGroupLinkPatchInput) (*models.FansubGroupLink, bool, error)` and an audit-write function matching `func(context.Context, repository.AuditLogEntry) error`; production delegates to the existing repositories, while tests inject ordered spies without replacing the whole FansubRepository. Change `FansubRepository.UpdateGroupLink` in `fansub_repository.go` to return `(*models.FansubGroupLink, bool, error)`. Its concrete transaction must begin, select the scoped row `FOR UPDATE`, return `ErrNotFound` for a missing `(group_id,id)`, normalize and compare every supplied field, and return the current row with `changed=false` without UPDATE or legacy sync on an exact no-op. For a changed patch, update the row and synchronize `fansub_groups.website_url/discord_url/irc_url` through transaction-bound query helpers before commit; any update, uniqueness, legacy-sync, or commit failure rolls back and returns no successful changed result. Add focused repository tests in `fansub_repository_group_links_test.go` for scoped not-found, exact no-op/no writes, changed commit, conflict/error rollback, and legacy-sync rollback. Then make `UpdateFansubLink` validate first, call the injected update seam once, emit no allowed audit when `changed=false`, and only after a committed `changed=true` result call the injected audit seam exactly once. Preserve the group-scoped permission/BOLA check, existing denied audit behavior, response status semantics, and German error payloads. Do not introduce Phase-137 override audit logic.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/handlers -run '^TestPhase136GroupLinkAudit' -count=1 &amp;&amp; docker compose exec -T team4sv30-backend go test ./internal/repository -run '^TestPhase136UpdateGroupLink' -count=1 &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>Only a real successful changed link mutation emits one allowed domain-audit event; every failed/no-op path emits none.</acceptance_criteria>
  <done>The task acceptance criteria are satisfied and its automated verification passes.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| mutation result→audit log | Audit evidence must describe persisted reality, not attempted input. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-136-G04 | Repudiation | link-update allowed audit | mitigate | Persist first and test all failure/no-op branches for zero allowed events. |
</threat_model>
<verification>Focused handler tests and `git diff --check` pass.</verification>
<success_criteria>Link audit history contains only real successful transitions.</success_criteria>
<output>After completion, create `.planning/phases/136-capability-policy-catalog-schema-contract/136-15-SUMMARY.md`.</output>
