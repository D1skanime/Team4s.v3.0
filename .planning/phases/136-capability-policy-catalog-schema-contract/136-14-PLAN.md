---
phase: 136-capability-policy-catalog-schema-contract
plan: 14
type: execute
wave: 9
depends_on: [136-08]
files_modified:
  - backend/internal/handlers/fansub_group_history_handler.go
  - backend/internal/handlers/fansub_groups.go
  - backend/internal/handlers/phase136_narrow_role_defaults_enforcement_test.go
autonomous: true
gap_closure: true
requirements: [CAP-13]
must_haves:
  truths:
    - "D-17: founding_history_edit can create or modify only founding events; it cannot mutate unrelated stored or requested history types."
    - "D-18/D-19: general_edit cannot change status or group_type, and mixed patches require every narrow permission before mutation."
  artifacts:
    - path: backend/internal/handlers/phase136_narrow_role_defaults_enforcement_test.go
      provides: event-type, lifecycle-field, mixed-patch and BOLA regression coverage
  key_links:
    - from: backend/internal/handlers/fansub_group_history_handler.go
      to: repository history lookup/mutation
      via: authorize requested and stored event types before create/PATCH mutation
    - from: backend/internal/handlers/fansub_groups.go
      to: requiredFansubGroupPatchActions
      via: each lifecycle field maps to a stronger existing action
---
<objective>Close the founder and co-leader privilege-escalation gaps without broadening role defaults or implementing Phase-137 overrides.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/136-capability-policy-catalog-schema-contract/136-CONTEXT.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-VERIFICATION.md
@.planning/phases/136-capability-policy-catalog-schema-contract/136-09-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify event-aware and lifecycle-field authorization failures</name>
  <read_first>AGENTS.md; docs/engineering/implementation-contract.md; backend/internal/handlers/fansub_group_history_handler.go; backend/internal/handlers/fansub_groups.go; backend/internal/handlers/phase136_narrow_role_defaults_enforcement_test.go; backend/internal/repository/fansub_group_history_repository.go; backend/internal/models/fansub.go</read_first>
  <files>backend/internal/handlers/phase136_narrow_role_defaults_enforcement_test.go</files>
  <behavior>
    - A founder-only actor may create a founding event but receives 403 for every unrelated event type.
    - PATCH checks both the stored event type and requested event_type; changing founding to unrelated, unrelated to founding, or editing an unrelated stored event is denied before mutation.
    - A co-leader-only actor may edit approved general content but cannot change status or group_type.
    - Mixed patches require all mapped actions; a missing action yields 403 with zero repository mutation, including a foreign-group/BOLA negative.
  </behavior>
  <action>Extend the existing handler contract tests with executable request-level cases and mutation spies. Do not settle for source substring assertions. Keep platform-admin behavior through the existing permission service and use the existing German error envelope.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/handlers -run 'Phase136NarrowRoleDefaults|GroupHistory|UpdateFansub' -count=1</automated></verify>
  <acceptance_criteria>Tests fail on the current over-broad guards and cover create, stored/requested PATCH types, lifecycle fields, mixed patches, and foreign-group scope.</acceptance_criteria>
  <done>The task acceptance criteria are satisfied and its automated verification passes after Task 2.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Enforce narrow event and lifecycle permissions before mutation</name>
  <read_first>backend/internal/handlers/phase136_narrow_role_defaults_enforcement_test.go; backend/internal/handlers/fansub_group_history_handler.go; backend/internal/handlers/fansub_groups.go; backend/internal/permissions/permissions.go; backend/internal/repository/fansub_group_history_repository.go</read_first>
  <files>backend/internal/handlers/fansub_group_history_handler.go, backend/internal/handlers/fansub_groups.go, backend/internal/handlers/phase136_narrow_role_defaults_enforcement_test.go</files>
  <action>Per D-17, authorize founding_history_edit only when both the persisted and requested effective event type are founding; load the existing row before PATCH authorization and reject cross-type smuggling before validation/mutation. Route non-founding history through the already-established stronger history-management authority, without granting it to founder. Per D-18/D-19, remove Status and GroupType from general_edit and map them to the established lifecycle/broad administration action; require every deduplicated action for mixed patches before UpdateGroup runs. Preserve group scope, platform-admin bypass, exact denial audit action, and existing request validation. Do not add override resolution.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/handlers -run 'Phase136NarrowRoleDefaults|GroupHistory|UpdateFansub' -count=1 &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>Founder cannot mutate unrelated history, co-leader cannot mutate lifecycle fields, and all mixed/foreign-scope denials happen before repository mutation.</acceptance_criteria>
  <done>The task acceptance criteria are satisfied and its automated verification passes.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| request payload→permission action | Untrusted event and patch fields select required authority. |
| route group→stored row | History rows must belong to the requested group before authorization/mutation. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-136-G01 | Elevation | history create/PATCH | mitigate | Check effective stored and requested event type before mutation. |
| T-136-G02 | Elevation | status/group_type patch | mitigate | Map lifecycle fields to stronger authority and require all mixed-patch actions. |
| T-136-G03 | Tampering | cross-group history ID | mitigate | Scoped lookup plus BOLA no-mutation tests. |
</threat_model>
<verification>Focused Go handler tests and `git diff --check` pass.</verification>
<success_criteria>D-17–D-19 defaults grant only the approved narrow operations.</success_criteria>
<output>After completion, create `.planning/phases/136-capability-policy-catalog-schema-contract/136-14-SUMMARY.md`.</output>
