---
phase: 128-canonical-public-identity-visibility-foundation
plan: 10
type: execute
wave: 3
depends_on: [128-06, 128-07, 128-08, 128-09]
files_modified:
  - backend/internal/repository/anime_contributions_public_repository.go
  - backend/internal/repository/anime_contributions_public_member_test.go
  - backend/internal/repository/project_member_public_repository.go
  - backend/internal/repository/project_member_public_repository_test.go
autonomous: true
requirements: [PMID-03, PMPR-02, PMPR-03]
must_haves:
  truths:
    - "Contribution and project-member repositories accept resolved member IDs and never resolve slugs independently (D-08, D-10)."
    - "Numeric/nickname-derived inbound identity and empty-200 missing contributions are removed (D-05, D-20)."
  artifacts:
    - path: backend/internal/repository/anime_contributions_public_repository.go
      provides: ID-based member contribution loader
    - path: backend/internal/repository/project_member_public_repository.go
      provides: ID-based project relation/detail seam
  key_links:
    - from: backend/internal/repository/project_member_public_repository.go
      to: ResolvePublicMemberAccess
      via: handler-provided memberID, never a local slug lookup
---
<objective>
Remove independent nickname/numeric resolution from contribution and project-member repositories.

Purpose: Make the shared access decision the only inbound member identity seam.
Output: ID-based loaders and deletion of the obsolete shared slug expression.
</objective>
<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-06-SUMMARY.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-07-SUMMARY.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-08-SUMMARY.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-09-SUMMARY.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Convert member contributions to resolved-ID loading</name>
  <read_first>
    backend/internal/repository/anime_contributions_public_repository.go
    backend/internal/repository/anime_contributions_public_member_test.go
    backend/internal/repository/member_public_access_repository.go
    .planning/phases/128-canonical-public-identity-visibility-foundation/128-PATTERNS.md
  </read_first>
  <files>backend/internal/repository/anime_contributions_public_repository.go, backend/internal/repository/anime_contributions_public_member_test.go</files>
  <action>Change GetPublicMemberContributions to GetPublicMemberContributionsByID(ctx,memberID) and retain only the timeline query. Delete its nickname REGEXP_REPLACE, id::text fallback, and missing-as-empty behavior. Preserve existing public contribution predicates and canonical group/release ownership. After Plans 06-08 and this task remove all consumers, delete memberSlugExpr entirely and add a package source invariant rejecting it, deriveMemberSlug, and normalizeMemberProfileSlug.</action>
  <verify>
    <automated>docker compose exec -T team4sv30-backend go test ./internal/repository -run 'PublicMemberContributions|MemberSlugInvariant' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Method accepts int64 memberID only.
    - No nickname/numeric member resolver or empty missing response remains.
    - Repository package contains no memberSlugExpr or old normalizer definitions/calls.
  </acceptance_criteria>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Convert project-member relation gate to resolved ID</name>
  <read_first>
    backend/internal/repository/project_member_public_repository.go
    backend/internal/repository/project_member_public_repository_test.go
    backend/internal/repository/member_public_access_repository.go
    docs/architecture/db-schema-fansub-domain.md
  </read_first>
  <files>backend/internal/repository/project_member_public_repository.go, backend/internal/repository/project_member_public_repository_test.go</files>
  <action>Replace ResolveMemberRelation(animeID,groupID,memberSlug) with an ID-based relation check such as HasMemberRelation(ctx,animeID,groupID,memberID). Remove its slug lookup. In GetSummary select the already resolved member's stored public_slug, preserving relation/content ownership and existing note/media/release queries. Update source/behavior tests to reject local member resolution and require the handler-provided ID.</action>
  <verify>
    <automated>docker compose exec -T team4sv30-backend go test ./internal/repository -run 'ProjectMember|MemberSlugInvariant' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Project relation method accepts memberID and performs only anime/group relation checking.
    - Summary uses stored slug.
    - No project-member nickname/numeric resolution remains.
  </acceptance_criteria>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Asset | Control |
|---|---|---|
| Handler access result -> loaders | Member detail | Resolved numeric DB ID only |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation and verification |
|---|---|---|---|---|
| T-128-BOLA | Elevation | Contribution/project loaders | mitigate | No independent resolver |
| T-128-POSTLOAD | Information disclosure | Timeline/relation queries | mitigate | Handler must supply allowed ID |
| T-128-ENUMERATION | Information disclosure | Missing contributions | mitigate | Empty-200 behavior deleted |
| T-128-IDENTITY-DRIFT | Spoofing | Summary link | mitigate | Stored slug selection |
</threat_model>
<verification>Run both focused repository suites, grep for old slug helpers, and git diff --check.</verification>
<success_criteria>Every retained subresource repository consumes the one access result rather than resolving identity itself.</success_criteria>
<output>After completion, create .planning/phases/128-canonical-public-identity-visibility-foundation/128-10-SUMMARY.md</output>
