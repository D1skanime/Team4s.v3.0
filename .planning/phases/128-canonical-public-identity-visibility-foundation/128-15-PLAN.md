---
phase: 128-canonical-public-identity-visibility-foundation
plan: 15
type: execute
wave: 7
depends_on: [128-03, 128-13, 128-14]
files_modified:
  - frontend/src/app/members/[slug]/MemberProfileContent.tsx
  - frontend/src/app/members/[slug]/page.tsx
  - frontend/src/app/members/[slug]/page.test.tsx
  - frontend/src/app/members/[slug]/page.module.css
autonomous: true
requirements: [PMID-03, PMPR-01, PMPR-04, PMPR-05]
must_haves:
  truths:
    - "Public and owner rendering reuse the existing MemberProfileHero and section composition, not a reduced second page (D-13, D-17)."
    - "Missing/private-denied/numeric/invalid server results invoke Next notFound() before protected content, producing the route-level HTTP 404 document (D-09)."
    - "The server page performs anonymous no-store fetch only; refresh-only ownership recovery stays inside the 404 document's existing central client seam (D-15)."
  artifacts:
    - path: frontend/src/app/members/[slug]/MemberProfileContent.tsx
      provides: Route-local authoritative existing composition
    - path: frontend/src/app/members/[slug]/page.tsx
      provides: Anonymous SSR, route-level notFound authority, and privacy-neutral metadata
  key_links:
    - from: frontend/src/app/members/[slug]/page.tsx
      to: frontend/src/lib/api.ts
      via: token-free no-store getMemberProfile
---
<objective>
Preserve the real public profile composition while making SSR states privacy-neutral and client-refresh compatible.

Purpose: Remove server cookie/token authority without building a new SSR refresh system.
Output: Route-local reusable composition, neutral states, metadata, and scoped styles.
</objective>
<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-03-SUMMARY.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-13-SUMMARY.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-14-SUMMARY.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-UI-SPEC.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extract only the route-local authoritative profile composition</name>
  <read_first>
    frontend/src/app/members/[slug]/page.tsx
    frontend/src/app/members/[slug]/page.test.tsx
    frontend/src/components/profile/MemberProfileHero.tsx
    frontend/src/components/ui/LoadingState.tsx
    frontend/src/components/ui/ErrorState.tsx
    .planning/phases/128-canonical-public-identity-visibility-foundation/128-UI-SPEC.md
  </read_first>
  <files>frontend/src/app/members/[slug]/MemberProfileContent.tsx, frontend/src/app/members/[slug]/page.test.tsx</files>
  <action>Move the existing toolbar, MemberProfileHero, membership, projects, badge, and contribution composition intact into a route-local MemberProfileContent accepting authoritative PublicMemberProfileData, stored slug, and viewer access props. This small reuse boundary is required so owner preview cannot duplicate/reduce the page; it is not the broader Phase-132 SSR architecture. Preserve section order, badge derivation, and dirty imported profile/carousel components without modifying/reformatting them. Make toolbar slots owner-aware so later client authority can show edit or correction, never both.</action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend npm test -- --run 'src/app/members/[slug]/page.test.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - One route-local composition renders all existing sections and MemberProfileHero.
    - No duplicate/reduced profile grid is introduced.
    - Dirty MemberBadgeChain/MemberStory/memberBadgeLabels/FocalCarousel files are unchanged.
  </acceptance_criteria>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Make page and metadata privacy-neutral without server token reads</name>
  <read_first>
    frontend/src/app/members/[slug]/page.tsx
    frontend/src/app/members/[slug]/page.test.tsx
    frontend/src/app/members/[slug]/page.module.css
    frontend/src/app/members/[slug]/MemberProfileContent.tsx
    docs/frontend/auth-api-client.md
  </read_first>
  <files>frontend/src/app/members/[slug]/page.tsx, frontend/src/app/members/[slug]/page.test.tsx, frontend/src/app/members/[slug]/page.module.css</files>
  <action>Remove cookies/AUTH_TOKEN_COOKIE_NAME/readViewerToken/token-parameter logic. Keep anonymous public SSR through token-free getMemberProfile with no-store. For API 404 plus invalid/numeric syntax, call Next notFound() before MemberProfileContent or any protected detail is emitted; do not render OwnHiddenProfilePreview directly from page.tsx. Keep denial metadata privacy-neutral with title Profil nicht verfügbar | Team4s and noindex. Non-404 failures use ErrorState exact copy. Preserve route-owned notice/state styles and 36rem container rules for the client gate added in Plan 16, remove legacy profileGrid styles, and preserve existing page geometry. Do not add a server refresh/BFF seam or accept an HTTP 200 denied document. Update page tests to mock next/navigation notFound and prove it is invoked for missing, private-denied, numeric, and invalid results (D-09/D-15).</action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend npm test -- --run 'src/app/members/[slug]/page.test.tsx' && docker compose exec -T team4sv30-frontend npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Page imports no next/headers auth cookie constant and passes no token.
    - Missing/private/numeric/invalid page execution reaches Next notFound(), never a rendered 200 client gate; metadata is indistinguishable and noindex.
    - CSS contains container-type, 36rem query, min-width:0, no overflow-wrap:anywhere in new notice/state rules.
  </acceptance_criteria>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Asset | Control |
|---|---|---|
| SSR/API result -> HTML | Private details | Anonymous fetch and neutral client gate |
| Cached render -> viewer | Owner DTO | no-store; no server token cache key |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation and verification |
|---|---|---|---|---|
| T-128-ENUMERATION | Information disclosure | Page/metadata | mitigate | Identical neutral output/noindex |
| T-128-CACHE | Information disclosure | SSR | mitigate | Token-free no-store; no shared owner data |
| T-128-AUTH-BOUNDARY | Elevation | Server page | mitigate | Remove cookie/token reads |
| T-128-POSTLOAD | Information disclosure | Composition | mitigate | Render only allowed authoritative DTO |
</threat_model>
<verification>Run page tests, typecheck, confirm dirty files unchanged, and git diff --check.</verification>
<success_criteria>The route preserves its real composition while denial and refresh uncertainty remain privacy-neutral.</success_criteria>
<output>After completion, create .planning/phases/128-canonical-public-identity-visibility-foundation/128-15-SUMMARY.md</output>
