---
phase: 128-canonical-public-identity-visibility-foundation
plan: 14
type: execute
wave: 1
depends_on: [128-03]
files_modified:
  - frontend/proxy.ts
  - frontend/proxy.test.ts
autonomous: true
requirements: [PMID-03, PMPR-01]
must_haves:
  truths:
    - "Technically equivalent member paths redirect 308 to one syntax-canonical URL without existence lookup (D-18)."
    - "Numeric, guessed nickname, ambiguous encoding, and unusable paths never resolve or redirect to a member (D-19, D-20)."
  artifacts:
    - path: frontend/proxy.ts
      provides: Syntax-only member path canonicalization
  key_links:
    - from: frontend/proxy.ts
      to: /members/{slug}
      via: one-segment decode/normalize/re-encode preserving query
---
<objective>
Add the narrow Next.js proxy that canonicalizes member-path syntax without consulting identity or visibility.

Purpose: Produce one URL spelling without creating a privacy oracle.
Output: frontend/proxy.ts and green redirect matrix.
</objective>
<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-03-SUMMARY.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-RESEARCH.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement syntax-only canonical member redirects</name>
  <read_first>
    frontend/proxy.test.ts
    frontend/package.json
    frontend/src/app/members/[slug]/page.tsx
    .planning/phases/128-canonical-public-identity-visibility-foundation/128-RESEARCH.md
    .planning/phases/128-canonical-public-identity-visibility-foundation/128-UI-SPEC.md
  </read_first>
  <files>frontend/proxy.ts, frontend/proxy.test.ts</files>
  <action>Use the pinned Next 16 Proxy API from Research Pattern 6. Match only /members/{one-segment} and descendants that carry a member slug. Decode exactly once, reject encoded/decoded slash, backslash, control, empty, unusable, and numeric segments; trim edge whitespace, lowercase the canonical ASCII stored-slug syntax, re-encode, preserve path suffix/query, and return permanent 308 only when the syntax differs. Never query API/database, regenerate from nickname, or redirect numeric/guessed slugs. Ensure missing/private inputs receive identical proxy behavior so redirects reveal no existence.</action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend npm test -- --run proxy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Case/space/equivalent encoding redirect to one URL with query preserved.
    - Numeric, encoded separator/control, and unusable inputs do not redirect.
    - Proxy imports no API helper, auth helper, database, or nickname normalizer.
  </acceptance_criteria>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Asset | Control |
|---|---|---|
| Raw encoded URL -> Next routing | Canonical identity/privacy | Decode once; syntax only |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation and verification |
|---|---|---|---|---|
| T-128-ENCODED-PATH | Tampering | proxy | mitigate | Separator/control/encoding tests |
| T-128-ENUMERATION | Information disclosure | redirect | mitigate | No existence lookup |
| T-128-IDENTITY-DRIFT | Spoofing | route | mitigate | Never derive nickname |
| T-128-BOLA | Elevation | detail access | accept | Proxy grants no access; backend resolver gates |
</threat_model>
<verification>Run proxy tests and git diff --check.</verification>
<success_criteria>Canonical-equivalent URLs redirect safely and every noncanonical identity guess remains unresolved.</success_criteria>
<output>After completion, create .planning/phases/128-canonical-public-identity-visibility-foundation/128-14-SUMMARY.md</output>
