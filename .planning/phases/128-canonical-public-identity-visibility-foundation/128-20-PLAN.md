---
phase: 128-canonical-public-identity-visibility-foundation
plan: 20
type: execute
wave: 8
depends_on: [128-05, 128-06, 128-07, 128-08, 128-09, 128-10, 128-11, 128-12, 128-13, 128-14, 128-16, 128-18, 128-19]
files_modified: []
autonomous: false
requirements: [PMID-01, PMID-02]
must_haves:
  truths:
    - "No live member row is reset until the user sees the exact database identity, cascade scope, SQL, and explicitly approves (D-21)."
    - "Approval is scoped to team4s_v2 on the canonical Linux Compose PostgreSQL service only."
  artifacts:
    - path: database/migrations/0145_member_public_identity_visibility.up.sql
      provides: Migration that requires the approved empty-members state
  key_links:
    - from: user approval
      to: 128-21
      via: exact phrase APPROVE PHASE 128 MEMBER RESET
---
<objective>
Stop at the destructive live-member reset approval boundary with exact target evidence and procedure.

Purpose: Satisfy the database stop condition without weakening the non-empty migration precondition.
Output: Explicit approval or rejection recorded in the plan summary; no mutation in this plan.
</objective>
<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-04-SUMMARY.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-05-SUMMARY.md
@AGENTS.md
</context>
<tasks>
<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Approve or reject the exact Phase-128 live reset</name>
  <read_first>
    AGENTS.md
    database/migrations/0145_member_public_identity_visibility.up.sql
    docker-compose.yml
    README.md
    .planning/phases/128-canonical-public-identity-visibility-foundation/128-CONTEXT.md
  </read_first>
  <files>none ? approval gate only; do not modify product or database state</files>
  <action>Run only the read-only checks below on team4s-linux. Present their unredacted database name/current user/server address/port, current member count, exact one-row sheppert/csubs-leader app-user mapping, recursively discovered FK tables that TRUNCATE would cascade into, migration status, and current git migration status. Then present this literal Plan-21 procedure. First psql transaction: BEGIN; DO $$ BEGIN IF current_database() &lt;&gt; 'team4s_v2' THEN RAISE EXCEPTION 'wrong database: %', current_database(); END IF; IF (SELECT count(*) FROM app_users WHERE preferred_username IN ('csubs-leader','sheppert')) &lt;&gt; 2 OR EXISTS (SELECT preferred_username FROM app_users WHERE preferred_username IN ('csubs-leader','sheppert') GROUP BY preferred_username HAVING count(*) &lt;&gt; 1) THEN RAISE EXCEPTION 'Phase 128 app-user fixtures missing or ambiguous'; END IF; END $$; TRUNCATE TABLE members RESTART IDENTITY CASCADE; COMMIT;. Then run docker compose exec -T team4sv30-backend /app/migrate up. Then second psql transaction: BEGIN; INSERT INTO members (nickname, display_name, public_slug, profile_visibility, noindex) VALUES ('CSubs Leader','CSubs Leader','csubs-leader','private',false), ('Sheppert','Sheppert','sheppert','public',false); INSERT INTO member_claims (member_id,app_user_id,claim_status,verification_method,verified_at) SELECT m.id,u.id,'verified','phase128_fixture',now() FROM members m JOIN app_users u ON u.preferred_username=m.public_slug WHERE m.public_slug IN ('csubs-leader','sheppert'); DO $$ BEGIN IF (SELECT count(*) FROM members WHERE public_slug IN ('csubs-leader','sheppert')) &lt;&gt; 2 OR (SELECT count(*) FROM member_claims WHERE claim_status='verified' AND verification_method='phase128_fixture') &lt;&gt; 2 THEN RAISE EXCEPTION 'Phase 128 reseed invariant failed'; END IF; END $$; COMMIT;. State that TRUNCATE deletes all recursively listed member-dependent disposable rows, is not recoverable from the repository, and does not use docker compose down -v. Wait for the exact approval phrase; do not infer approval.</action>
  <verify>
    <automated>docker compose exec -T team4sv30-db psql -U team4s -d team4s_v2 -X -v ON_ERROR_STOP=1 -P pager=off -c "SELECT current_database(), current_user, inet_server_addr(), inet_server_port(); SELECT count(*) AS member_count FROM members; SELECT preferred_username,id,status FROM app_users WHERE preferred_username IN ('csubs-leader','sheppert') ORDER BY preferred_username; WITH RECURSIVE deps(parent_table,child_table) AS (SELECT confrelid,conrelid FROM pg_constraint WHERE contype='f' AND confrelid='members'::regclass UNION SELECT c.confrelid,c.conrelid FROM pg_constraint c JOIN deps d ON c.confrelid=d.child_table WHERE c.contype='f') SELECT DISTINCT child_table::regclass AS cascading_table FROM deps ORDER BY 1;" &amp;&amp; docker compose exec -T team4sv30-backend /app/migrate status &amp;&amp; git status --short -- database/migrations backend/database/migrations</automated>
  </verify>
  <acceptance_criteria>
    - Read-only output proves exact target and cascade scope.
    - The user receives the exact truncate/migrate/two-fixture reseed procedure and recovery warning.
    - No database mutation occurs before exact approval.
  </acceptance_criteria>
  <decision>Authorize the destructive Phase-128 reset and clean two-member reseed?</decision>
  <options>
    <option id="approve">
      <name>Approve reset</name>
      <pros>Allows 0145 to apply from an empty disposable member state and creates canonical UAT identities.</pros>
      <cons>Deletes all current member-dependent disposable rows listed by PostgreSQL.</cons>
    </option>
    <option id="reject">
      <name>Reject reset</name>
      <pros>Preserves current local runtime rows.</pros>
      <cons>0145 remains unapplied to team4s_v2 because it must fail closed on non-empty members.</cons>
    </option>
  </options>
  <resume-signal>Type exactly APPROVE PHASE 128 MEMBER RESET to continue, or REJECT PHASE 128 MEMBER RESET to stop.</resume-signal>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Asset | Control |
|---|---|---|
| Executor -> live PostgreSQL | Disposable but material local data | Identity/cascade evidence plus explicit approval |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation and verification |
|---|---|---|---|---|
| T-128-DB-TARGET | Tampering | Live reset | mitigate | Exact DB identity and user approval |
| T-128-IDENTITY-DRIFT | Spoofing | Reseed | mitigate | Exact stored slugs after migration |
| T-128-BOLA | Elevation | Fixture ownership | mitigate | Claims bind by exact preferred_username |
| T-128-DOS | Denial of service | Cascade | mitigate | Enumerate cascade scope before approval |
</threat_model>
<verification>No write verification exists in this plan; the automated command is read-only and the decision is recorded.</verification>
<success_criteria>The destructive boundary is explicit, fully scoped, and cannot proceed on implicit approval.</success_criteria>
<output>After decision, create .planning/phases/128-canonical-public-identity-visibility-foundation/128-20-SUMMARY.md with target evidence and approval phrase.</output>
