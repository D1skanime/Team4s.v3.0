---
phase: 128-canonical-public-identity-visibility-foundation
plan: 13
type: execute
wave: 6
depends_on: [128-03, 128-12]
files_modified:
  - shared/contracts/openapi.yaml
  - frontend/src/types/profile.ts
  - scripts/validate-phase128-member-openapi.py
  - frontend/src/types/__tests__/phase128-member-contract.test.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/api.auth-refresh.test.ts
autonomous: true
requirements: [PMID-03, PMPR-01, PMPR-03, PMPR-04, PMPR-05]
must_haves:
  truths:
    - "OpenAPI, runtime envelopes, TypeScript DTOs, and central helpers describe one stored-slug access contract."
    - "Visible profile responses carry authoritative viewer.is_owner/is_private_preview facts; denial is neutral 404 (D-09, D-13, D-17)."
    - "All protected member helpers use central refresh and no-store without token parameters or bare fetch (D-12, D-15)."
  artifacts:
    - path: shared/contracts/openapi.yaml
      provides: Seven documented optional-auth member operations
    - path: frontend/src/types/profile.ts
      provides: Required slug, public/private, viewer access types
    - path: frontend/src/lib/api.ts
      provides: Central no-store member helpers
  key_links:
    - from: frontend/src/lib/api.ts
      to: shared/contracts/openapi.yaml
      via: matching status/envelope fields and routes
---
<objective>
Synchronize the public member contract across OpenAPI, TypeScript, and the central refresh-capable API client.

Purpose: Remove hidden response unions and protected bare-fetch/token drift.
Output: One documented optional-auth contract and matching frontend helpers.
</objective>
<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-03-SUMMARY.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-12-SUMMARY.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-PATTERNS.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Update the canonical OpenAPI and TypeScript DTOs</name>
  <read_first>
    shared/contracts/openapi.yaml
    backend/internal/models/member_profile.go
    backend/internal/handlers/app_public_profile.go
    backend/internal/handlers/contributions_public_handler.go
    backend/internal/repository/anime_contributions_public_repository.go
    backend/internal/repository/project_member_public_repository.go
    frontend/src/types/profile.ts
    frontend/src/types/contributions.ts
    frontend/src/types/projectMember.ts
    frontend/package.json
    docs/api/api-contracts.md
  </read_first>
  <files>shared/contracts/openapi.yaml, frontend/src/types/profile.ts, scripts/validate-phase128-member-openapi.py, frontend/src/types/__tests__/phase128-member-contract.test.ts</files>
  <action>Treat the live OpenAPI inventory as six existing GET operations plus one absent operation. Update these six existing path keys in place without changing or duplicating their operationIds: `/api/v1/members/{slug}` (`getMemberProfile`), `/api/v1/members/{slug}/projects` (`getMemberProjects`), and `/api/v1/anime/{animeId}/group/{groupId}/members/{memberSlug}` plus its `/notes`, `/media`, and `/releases` children (`getProjectMemberSummary`, `getProjectMemberNotes`, `getProjectMemberMedia`, `getProjectMemberReleases`). Add only the absent `/api/v1/members/{slug}/contributions` path with operationId `getMemberContributions`. All seven GET operations declare optional bearer access as `security: [{bearerAuth: []}, {}]`, document identical missing/private-non-owner/numeric `404` behavior using `ErrorResponse` and the neutral message, and describe viewer-dependent cache behavior with response headers `Cache-Control: private, no-store` and `Vary: Authorization` wherever the contract represents those responses. Preserve the existing success envelopes/operation IDs: `PublicMemberProfileEnvelope`, `PublicMemberProjectsEnvelope`, `ProjectMemberSummary`, `ProjectMemberNotesCursorResponse`, `ProjectMemberMediaCursorResponse`, and `ProjectMemberReleasesCursorResponse`; add the contributions schema from the existing Go `PublicMemberContributionsResponse` and matching frontend DTO rather than inventing another envelope. Profile `200` becomes only `PublicMemberProfileEnvelope` with required `viewer.is_owner` and `viewer.is_private_preview`; remove `MemberProfileHidden` and every `visible:false` union, change `ProfileVisibility` to `public|private`, and require `slug` in `PublicMemberProfileData`. The repository has no dedicated OpenAPI validator and the backend/frontend containers cannot read the repo-level shared contract, so add `scripts/validate-phase128-member-openapi.py` for the confirmed canonical-host PyYAML 5.4.1 runtime. Its custom SafeLoader must recursively reject duplicate mapping keys before construction, reject duplicate operationIds, and assert the exact seven path/operation/security/200-schema/404/ErrorResponse/cache-description/header contracts without mutating files. Add a separate frontend DTO parity test that imports the profile, contributions, and project-member types, proves the required stored slug/viewer/visibility fields, and source-rejects `MemberProfileHidden`, `members_only`, and visible-false unions. Keep Phase-129 projection fields and Phase-130 minimization out of scope.</action>
  <verify>
    <automated>python3 scripts/validate-phase128-member-openapi.py &amp;&amp; docker compose exec -T team4sv30-frontend npm test -- --run src/types/__tests__/phase128-member-contract.test.ts &amp;&amp; git diff --check -- shared/contracts/openapi.yaml frontend/src/types/profile.ts scripts/validate-phase128-member-openapi.py frontend/src/types/__tests__/phase128-member-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Six pre-existing OpenAPI operations are updated in place and only `/api/v1/members/{slug}/contributions` is added; all seven member surfaces parse and validate with their original or prescribed unique operationIds and no duplicate YAML path keys.
    - Every operation has optional bearer security, the neutral 404/ErrorResponse contract, explicit viewer-dependent cache semantics/headers, and its correct runtime success schema.
    - OpenAPI has no MemberProfileHidden schema or visible-false response union; TypeScript has public|private, required public slug, exact viewer fields, and DTOs matching the Go/OpenAPI response names.
  </acceptance_criteria>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Route every member helper through central refresh/no-store</name>
  <read_first>
    frontend/src/lib/api.ts
    frontend/src/lib/api.auth-refresh.test.ts
    docs/frontend/auth-api-client.md
    frontend/src/types/profile.ts
    shared/contracts/openapi.yaml
  </read_first>
  <files>frontend/src/lib/api.ts, frontend/src/lib/api.auth-refresh.test.ts</files>
  <action>Make getMemberProfile token-free and call apiClientFetch with cache:no-store. Keep getMemberProjects on the same seam. Convert getMemberContributions and getProjectMemberSummary/Notes/Media/Releases from bare fetch to apiClientFetch, preserving AbortSignal, cursor, and error parsing. Parse only documented 200/404 branches. Extend refresh tests so an expired/missing access token plus valid refresh retries profile and a project-member helper; source assertions reject bare fetch in these helper bodies. Do not create a new SSR refresh seam, read cookies in UI, or expose bearer construction.</action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend npm test -- --run src/lib/api.auth-refresh.test.ts src/lib/api.no-token-boundary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All seven member helper paths use apiClientFetch/authorizedFetch and cache:no-store.
    - getMemberProfile has no authToken parameter.
    - Refresh-only and no-token-boundary tests pass.
  </acceptance_criteria>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Asset | Control |
|---|---|---|
| API runtime -> browser DTO | Private access state | Allow-listed documented envelope |
| Browser session -> request | Tokens | Central API client only |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation and verification |
|---|---|---|---|---|
| T-128-AUTH-BOUNDARY | Elevation | API helpers | mitigate | Token-free helpers/no-token test |
| T-128-CACHE | Information disclosure | Fetch cache | mitigate | cache:no-store on all helpers |
| T-128-ENUMERATION | Information disclosure | Error union | mitigate | One documented 404 shape |
| T-128-BOLA | Elevation | Viewer flags | mitigate | Server-computed fields only |
</threat_model>
<verification>Run focused refresh/no-token tests, inspect OpenAPI/type/runtime field parity, and git diff --check.</verification>
<success_criteria>All public-member API surfaces share one documented, refresh-capable, no-store contract.</success_criteria>
<output>After completion, create .planning/phases/128-canonical-public-identity-visibility-foundation/128-13-SUMMARY.md</output>
