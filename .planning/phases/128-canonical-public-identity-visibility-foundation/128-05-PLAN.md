---
phase: 128-canonical-public-identity-visibility-foundation
plan: 05
type: execute
wave: 3
depends_on: [128-01, 128-04]
files_modified:
  - backend/internal/repository/member_public_slug.go
  - backend/internal/repository/member_public_slug_test.go
  - backend/internal/repository/member_requests_repository.go
  - backend/internal/repository/fansub_group_app_members_repository.go
  - backend/internal/repository/hist_group_members_repository.go
autonomous: true
requirements: [PMID-01, PMID-02]
must_haves:
  truths:
    - "All three production creation transactions use one allocator (D-01, D-05)."
    - "Concurrent collisions allocate base/base-2/base-3 safely, including literal suffix races (D-02)."
    - "Normalization is deterministic and invalid/reserved results fail without fallback (D-03, D-04)."
  artifacts:
    - path: backend/internal/repository/member_public_slug.go
      provides: One normalizer and transaction-scoped allocator
  key_links:
    - from: backend/internal/repository/member_requests_repository.go
      to: backend/internal/repository/member_public_slug.go
      via: allocation inside existing transaction
    - from: backend/internal/repository/fansub_group_app_members_repository.go
      to: backend/internal/repository/member_public_slug.go
      via: allocation on supplied pgx.Tx
    - from: backend/internal/repository/hist_group_members_repository.go
      to: backend/internal/repository/member_public_slug.go
      via: allocation before INSERT
---

<objective>
Implement one transaction-safe canonical slug allocator and use it in every production member creation seam.

Purpose: Allocate identity exactly once without repository-specific drift.
Output: Shared allocator and three wired creation repositories.
</objective>
<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-01-SUMMARY.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-04-SUMMARY.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-PATTERNS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement the shared normalizer and allocator</name>
  <read_first>
    backend/internal/repository/member_profile_repository.go
    backend/internal/repository/member_public_slug_test.go
    backend/internal/repository/member_requests_repository.go
    database/migrations/0145_member_public_identity_visibility.up.sql
    .planning/phases/128-canonical-public-identity-visibility-foundation/128-RESEARCH.md
  </read_first>
  <files>backend/internal/repository/member_public_slug.go, backend/internal/repository/member_public_slug_test.go</files>
  <action>Create normalizePublicMemberSlug(nickname) (string,error) and allocatePublicMemberSlugTx(ctx,tx,nickname) (string,error). Apply explicit ä/ö/ü/ß -> ae/oe/ue/ss and & -> und before NFD accent removal and separator collapse, matching migration length/reserved rules. Acquire one transaction advisory lock for the entire members.public_slug namespace before selecting the smallest available readable suffix; a per-base lock is forbidden because name and literal name-2 can race. Rely on the unique constraint as final invariant. Return validation/database errors; never random, UUID, numeric, or ID fallback.</action>
  <verify>
    <automated>docker compose exec -T -e TEAM4S_PHASE128_TEST_DSN='postgres://team4s:team4s_dev_password@team4sv30-db:5432/team4s_phase128_test?sslmode=disable' team4sv30-backend go test -race ./internal/repository -run 'MemberSlug|PublicIdentity' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - One normalizer implementation exists and uses existing x/text.
    - Concurrency tests pass on guarded DSN; unit cases pass without it.
    - Allocator accepts pgx.Tx and starts no nested transaction.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire all production member INSERT paths</name>
  <read_first>
    backend/internal/repository/member_requests_repository.go
    backend/internal/repository/fansub_group_app_members_repository.go
    backend/internal/repository/hist_group_members_repository.go
    backend/internal/repository/member_public_slug.go
    docs/engineering/implementation-contract.md
  </read_first>
  <files>backend/internal/repository/member_requests_repository.go, backend/internal/repository/fansub_group_app_members_repository.go, backend/internal/repository/hist_group_members_repository.go</files>
  <action>Extend exactly the three production INSERT INTO members statements. ApproveRequest allocates after FOR UPDATE; ensureAppUserMemberAnchorTx resolves the final nickname then allocates on its supplied transaction; CreateWithAutoMember allocates before member insert in its existing transaction. Insert public_slug explicitly, preserve dependent claim/membership writes and public visibility, and wrap errors. Do not add a fourth creation path, nested transaction, SQL slug derivation, or fallback.</action>
  <verify>
    <automated>docker compose exec -T -e TEAM4S_PHASE128_TEST_DSN='postgres://team4s:team4s_dev_password@team4sv30-db:5432/team4s_phase128_test?sslmode=disable' team4sv30-backend go test -race ./internal/repository -run 'MemberSlug|ApproveRequest|AutoMember|MemberAnchor' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Exactly three production member INSERT files call allocatePublicMemberSlugTx.
    - Allocation and insert share the existing transaction in all paths.
    - Repository tests and git diff --check pass.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Asset | Control |
|---|---|---|
| Nickname -> identity | Canonical slug | Shared validated normalizer |
| Concurrent transactions -> namespace | Unique stable URL | Global tx lock plus unique constraint |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation and verification |
|---|---|---|---|---|
| T-128-SLUG-RACE | Spoofing | Allocator | mitigate | Namespace lock and race tests |
| T-128-IDENTITY-DRIFT | Spoofing | Creation | mitigate | Creation-only write plus DB trigger |
| T-128-ENCODED-PATH | Tampering | Normalizer | mitigate | Reject controls/separators/numeric/reserved |
| T-128-DB-TARGET | Tampering | Integration tests | mitigate | Dedicated DSN only |
</threat_model>

<verification>Run repository tests with -race, inspect all member INSERTs, and run git diff --check.</verification>
<success_criteria>Every legitimate creation path persists one immutable slug through the same allocator.</success_criteria>
<output>After completion, create .planning/phases/128-canonical-public-identity-visibility-foundation/128-05-SUMMARY.md</output>
