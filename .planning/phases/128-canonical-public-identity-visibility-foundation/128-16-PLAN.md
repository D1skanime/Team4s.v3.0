---
phase: 128-canonical-public-identity-visibility-foundation
plan: 16
type: execute
wave: 7
depends_on: [128-03, 128-13, 128-15]
files_modified:
  - frontend/src/app/members/[slug]/OwnHiddenProfilePreview.tsx
  - frontend/src/app/members/[slug]/OwnHiddenProfilePreview.test.tsx
  - frontend/src/app/members/[slug]/OwnProfileEditLink.tsx
  - frontend/src/app/members/[slug]/OwnProfileEditLink.test.tsx
  - frontend/src/app/members/[slug]/MemberProfileContent.tsx
autonomous: true
requirements: [PMPR-01, PMPR-04, PMPR-05]
must_haves:
  truths:
    - "Refresh-only owner loads getMemberProfile through central refresh, then sees the full composition and notice (D-13, D-14, D-15)."
    - "Owner toolbar is read-only/edit-only with no self-correction and no duplicate own-profile lookup (D-16, D-17)."
    - "Anonymous/non-owner/missing all end in the same neutral unavailable output and no stale detail flash (D-09, D-12)."
  artifacts:
    - path: frontend/src/app/members/[slug]/OwnHiddenProfilePreview.tsx
      provides: Client refresh/access gate
    - path: frontend/src/app/members/[slug]/OwnProfileEditLink.tsx
      provides: Authoritative viewer toolbar action resolver
  key_links:
    - from: frontend/src/app/members/[slug]/OwnHiddenProfilePreview.tsx
      to: frontend/src/lib/api.ts
      via: token-free getMemberProfile through central refresh
---
<objective>
Replace the reduced hidden preview and duplicate owner lookup with the authoritative public endpoint/access result.

Purpose: Deliver refresh-only no-flash owner preview at the canonical route.
Output: Central-client preview gate and authoritative viewer toolbar actions.
</objective>
<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-03-SUMMARY.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-13-SUMMARY.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-15-SUMMARY.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-UI-SPEC.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Rewrite the hidden-profile client access gate</name>
  <read_first>
    frontend/src/app/members/[slug]/OwnHiddenProfilePreview.tsx
    frontend/src/app/members/[slug]/OwnHiddenProfilePreview.test.tsx
    frontend/src/app/members/[slug]/MemberProfileContent.tsx
    frontend/src/lib/useAuthSession.ts
    frontend/src/lib/api.ts
    docs/frontend/auth-api-client.md
  </read_first>
  <files>frontend/src/app/members/[slug]/OwnHiddenProfilePreview.tsx, frontend/src/app/members/[slug]/OwnHiddenProfilePreview.test.tsx</files>
  <action>Use useAuthSession with hasAccessToken || hasRefreshToken and isClientInitialized. While unresolved/refreshing render LoadingState with exact UI-SPEC text/aria live only. With active session call token-free getMemberProfile(slug), guard unmount/slug races, and on data.viewer.is_owner render the exact aside notice, two Button links, and MemberProfileContent with owner props. On no session or ApiError 404 render the shared neutral unavailable presentation; on other error render ErrorState. Delete getOwnProfile, slugifyMemberName, numeric matching, toPublicProfile, reduced grid/media/contribution reconstruction, and any direct token/refresh logic.</action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend npm test -- --run 'src/app/members/[slug]/OwnHiddenProfilePreview.test.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - Refresh-only test renders neutral loading then exact notice plus full content.
    - No logged-out, unavailable, correction, or stale content flashes for owner.
    - Source contains no getOwnProfile, slugifier, DTO conversion, bearer, cookie, or direct refresh.
  </acceptance_criteria>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Make viewer toolbar actions use authoritative access</name>
  <read_first>
    frontend/src/app/members/[slug]/OwnProfileEditLink.tsx
    frontend/src/app/members/[slug]/OwnProfileEditLink.test.tsx
    frontend/src/app/members/[slug]/MemberProfileContent.tsx
    frontend/src/components/profile/CorrectionReportModal.tsx
    frontend/src/lib/api.ts
  </read_first>
  <files>frontend/src/app/members/[slug]/OwnProfileEditLink.tsx, frontend/src/app/members/[slug]/OwnProfileEditLink.test.tsx, frontend/src/app/members/[slug]/MemberProfileContent.tsx</files>
  <action>Refactor the existing toolbar client boundary to resolve viewer authority with getMemberProfile(storedSlug), not getOwnProfile/member-ID comparison. During auth initialization show neither owner nor correction action. Verified owner renders edit action(s) and no CorrectionReportModal; authenticated/anonymous non-owner public viewer renders correction only after neutral resolution; unavailable renders neither. Reuse the server-provided viewer flags and central refresh seam; avoid duplicate concurrent requests where the preview already passes viewer facts into MemberProfileContent.</action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend npm test -- --run 'src/app/members/[slug]/OwnProfileEditLink.test.tsx' 'src/app/members/[slug]/OwnHiddenProfilePreview.test.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - Owner and correction are mutually exclusive.
    - Refresh-only owner does not call getOwnProfile or compare numeric member IDs.
    - Viewer-specific request remains no-store through getMemberProfile.
  </acceptance_criteria>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Asset | Control |
|---|---|---|
| Browser auth state -> preview | Refresh credentials | useAuthSession plus central client |
| Access response -> toolbar/content | Private identity/detail | Server viewer flags |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation and verification |
|---|---|---|---|---|
| T-128-AUTH-BOUNDARY | Elevation | Preview/actions | mitigate | No token/direct refresh code |
| T-128-BOLA | Elevation | Owner UI | mitigate | Server-computed viewer ownership |
| T-128-ENUMERATION | Information disclosure | Loading/404 | mitigate | Neutral no-flash state |
| T-128-CACHE | Information disclosure | Owner fetch | mitigate | Central no-store helper |
</threat_model>
<verification>Run preview/action tests, typecheck, no-token boundary, and git diff --check.</verification>
<success_criteria>A refresh-only verified owner sees the complete canonical profile without duplicate authority, flash, or cache leakage.</success_criteria>
<output>After completion, create .planning/phases/128-canonical-public-identity-visibility-foundation/128-16-SUMMARY.md</output>
