---
phase: 128-canonical-public-identity-visibility-foundation
plan: 21
type: execute
wave: 10
depends_on: [128-20]
files_modified: []
autonomous: true
requirements: [PMID-01, PMID-02, PMID-03, PMPR-01, PMPR-02, PMPR-03, PMPR-04, PMPR-05]
must_haves:
  truths:
    - "After explicit approval, live disposable members are reset, 0145 applies, and clean sheppert/csubs-leader canonical identities are reseeded (D-21)."
    - "All automated identity/access/cache/refresh/route invariants pass on the rebuilt Compose runtime."
  artifacts:
    - path: database/migrations/0145_member_public_identity_visibility.up.sql
      provides: Applied live canonical identity schema
    - path: shared/contracts/openapi.yaml
      provides: Final documented access contract
  key_links:
    - from: team4s_v2 members.public_slug
      to: /members/{slug}
      via: seeded sheppert and csubs-leader rows plus verified claims
---
<objective>
Execute the approved reset/migration/reseed procedure, rebuild the runtime, and run the complete automated phase gate.

Purpose: Prove the planned schema and code operate together on the canonical Compose environment.
Output: Applied 0145, deterministic local UAT identities, green focused/full suites, and source invariants.
</objective>
<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-20-SUMMARY.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-VALIDATION.md
@AGENTS.md
</context>
<tasks>
<task type="auto">
  <name>Task 1: Execute the approved guarded reset, migration, and clean reseed</name>
  <read_first>
    .planning/phases/128-canonical-public-identity-visibility-foundation/128-20-SUMMARY.md
    database/migrations/0145_member_public_identity_visibility.up.sql
    docker-compose.yml
    README.md
    AGENTS.md
  </read_first>
  <files>none — approved runtime database operation only</files>
  <action>Abort unless the dependency summary contains the exact approval phrase. Re-run the Plan-20 RUNTIME_PROFILE=local, database identity, recursive cascade table-and-exact-row-count, app-user uniqueness, migration-chain, and git migration checks; abort on any drift, missing/ambiguous csubs-leader or sheppert app user, untracked migration, non-local runtime profile, or database other than team4s_v2. Execute exactly the two SQL transactions and intervening migrate command printed and approved in Plan 20: guarded BEGIN/DO/TRUNCATE members RESTART IDENTITY CASCADE/COMMIT; migrate up; then guarded INSERT of CSubs Leader/csubs-leader/private and Sheppert/sheppert/public, verified member_claims joined only through app_users.preferred_username with verification_method phase128_fixture, two-row/two-claim assertion, and COMMIT. Use psql -X -v ON_ERROR_STOP=1 for both transactions so an assertion or statement failure cannot continue. Do not preserve/import old member rows, use aliases/backfill, touch media files, or use docker compose down -v.</action>
  <verify>
    <automated>docker compose exec -T team4sv30-backend /app/migrate status && docker compose exec -T team4sv30-db psql -U team4s -d team4s_v2 -X -v ON_ERROR_STOP=1 -P pager=off -c "SELECT nickname, public_slug, profile_visibility FROM members ORDER BY public_slug; SELECT count(*) FROM member_claims WHERE claim_status='verified' AND verification_method='phase128_fixture';"</automated>
  </verify>
  <acceptance_criteria>
    - Migration status shows 0145 applied.
    - Exactly csubs-leader/private and sheppert/public member slugs exist with two verified fixture claims.
    - No old-row compatibility/alias/history data is created and tracked badge assets/media are untouched.
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Rebuild Compose and run the complete automated phase gate</name>
  <read_first>
    .planning/phases/128-canonical-public-identity-visibility-foundation/128-VALIDATION.md
    backend/internal/handlers/public_member_access_matrix_test.go
    backend/internal/repository/member_public_slug_test.go
    frontend/src/lib/api.no-token-boundary.test.ts
    frontend/src/app/members/[slug]/page.test.tsx
    AGENTS.md
  </read_first>
  <files>none — verification only</files>
  <action>Rebuild/restart only backend and frontend Compose services, confirm all services healthy, and run focused repository/handler/frontend tests first. Pass TEAM4S_PHASE128_TEST_DSN explicitly to every backend go test command; its absence or a skipped Phase-128 PostgreSQL test fails this gate. Then run backend full tests, frontend full Vitest, typecheck, lint, production build, and git diff --check. Run source-invariant grep proving: all three production INSERTs call the allocator; no memberSlugExpr/deriveMemberSlug/normalizeMemberProfileSlug/slugifyMemberName/slug-or-member-id fallback remains; no protected member helper uses bare fetch; all seven routes use optional auth/shared resolver; OpenAPI/types/runtime viewer and slug fields match. After the rebuilt Next runtime is ready, curl the HTML routes (not API endpoints) for /members/phase128-missing and anonymous /members/csubs-leader on Linux port 3000 and require literal status 404 for both. If either document returns 200 because of streaming or any framework behavior, stop Phase 128 at this failed acceptance criterion; do not waive or defer D-09. Treat pre-existing unrelated lint warnings separately but do not waive Phase-128 failures.</action>
  <verify>
    <automated>docker compose up -d --build team4sv30-backend team4sv30-frontend &amp;&amp; docker compose ps &amp;&amp; docker compose exec -T -e TEAM4S_PHASE128_TEST_DSN='postgres://team4s:team4s_dev_password@team4sv30-db:5432/team4s_phase128_test?sslmode=disable' team4sv30-backend go test ./internal/repository ./internal/handlers ./internal/migrations -run 'PublicMember|MemberSlug|MemberAccess|ProjectMember|Phase128|SlugImmutable' -count=1 &amp;&amp; docker compose exec -T team4sv30-frontend npm test -- --run src/lib/api.auth-refresh.test.ts 'src/app/members/[slug]/page.test.tsx' 'src/app/members/[slug]/OwnHiddenProfilePreview.test.tsx' &amp;&amp; test "$(curl -sS -o /tmp/phase128-missing.html -w '%{http_code}' http://127.0.0.1:3000/members/phase128-missing)" = 404 &amp;&amp; test "$(curl -sS -o /tmp/phase128-private.html -w '%{http_code}' http://127.0.0.1:3000/members/csubs-leader)" = 404 &amp;&amp; docker compose exec -T -e TEAM4S_PHASE128_TEST_DSN='postgres://team4s:team4s_dev_password@team4sv30-db:5432/team4s_phase128_test?sslmode=disable' team4sv30-backend go test ./... -count=1 &amp;&amp; docker compose exec -T team4sv30-frontend npm test -- --run &amp;&amp; docker compose exec -T team4sv30-frontend npm run typecheck &amp;&amp; docker compose exec -T team4sv30-frontend npm run lint &amp;&amp; docker compose exec -T team4sv30-frontend npm run build &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Focused and full backend/frontend suites pass.
    - Missing and private-anonymous HTML documents both return literal HTTP 404; any 200 is a blocking Phase-128 failure.
    - Typecheck, lint, build, and git diff --check pass or unrelated pre-existing lint debt is separately evidenced.
    - Source invariants report zero prohibited identity/auth patterns and exact seven-route coverage.
  </acceptance_criteria>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Asset | Control |
|---|---|---|
| Approved executor -> live DB | Runtime data/schema | Revalidated identity and transactional exact SQL |
| Built code -> public network | Private profile data | Full access/cache/auth test gate |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation and verification |
|---|---|---|---|---|
| T-128-DB-TARGET | Tampering | Reset/migrate | mitigate | Approval plus repeated DB guard |
| T-128-SLUG-RACE | Spoofing | Creation | mitigate | Guarded concurrency suite |
| T-128-BOLA | Elevation | Routes | mitigate | Full access matrix |
| T-128-ENUMERATION | Information disclosure | 404 | mitigate | Handler/page equality tests |
| T-128-POSTLOAD | Information disclosure | Loaders | mitigate | Zero-call spies |
| T-128-CACHE | Information disclosure | Owner result | mitigate | Header/client tests |
| T-128-AUTH-BOUNDARY | Elevation | Browser | mitigate | no-token/refresh tests |
| T-128-ENCODED-PATH | Tampering | Proxy | mitigate | Canonicalization suite |
</threat_model>
<verification>Both task commands must pass; record all outputs and unrelated pre-existing issues in the summary.</verification>
<success_criteria>The canonical runtime runs 0145 with clean fixtures and every automated Phase-128 security/contract gate is green.</success_criteria>
<output>After completion, create .planning/phases/128-canonical-public-identity-visibility-foundation/128-21-SUMMARY.md</output>
