---
phase: 128-canonical-public-identity-visibility-foundation
plan: 04
type: execute
wave: 1
depends_on: [128-01, 128-02]
files_modified:
  - database/migrations/0145_member_public_identity_visibility.up.sql
  - database/migrations/0145_member_public_identity_visibility.down.sql
  - backend/internal/migrations/phase128_public_identity_test.go
  - backend/internal/models/member_profile.go
  - backend/internal/handlers/app_public_profile.go
  - backend/internal/handlers/app_auth_test.go
autonomous: true
requirements: [PMID-01, PMID-02, PMPR-01]
must_haves:
  truths:
    - "PostgreSQL requires one unique canonical public_slug and rejects changes after insert (D-01, D-02)."
    - "Schema and Go allow only public/private visibility (D-06)."
    - "0145 refuses non-empty members and contains no backfill, alias, or compatibility behavior (D-21)."
  artifacts:
    - path: database/migrations/0145_member_public_identity_visibility.up.sql
      provides: Stored slug, private visibility, constraints, and trigger
    - path: database/migrations/0145_member_public_identity_visibility.down.sql
      provides: Reversible dependency-order rollback
  key_links:
    - from: backend/internal/models/member_profile.go
      to: database/migrations/0145_member_public_identity_visibility.up.sql
      via: matching visibility and stored-slug contract
---

<objective>
Add the reversible canonical member identity/visibility schema and matching Go vocabulary.

Purpose: Establish database invariants before repositories consume them.
Output: Migration 0145, guarded migration proof, required DTO slug, and private visibility constant.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-01-SUMMARY.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-CONTEXT.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-PATTERNS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement migration 0145 with fail-closed preconditions</name>
  <read_first>
    database/migrations/0044_add_db_schema_v2_target_tables.up.sql
    database/migrations/0077_member_profiles_mvp.up.sql
    database/migrations/0126_member_profile_public_defaults.up.sql
    database/migrations/0144_drop_theme_segment_playback_sources_legacy_unique.up.sql
    backend/internal/migrations/phase128_public_identity_test.go
    AGENTS.md
  </read_first>
  <files>database/migrations/0145_member_public_identity_visibility.up.sql, database/migrations/0145_member_public_identity_visibility.down.sql, backend/internal/migrations/phase128_public_identity_test.go</files>
  <action>First run git status --short for both migration directories and list the head; stop if any untracked migration exists or 0145 is not next. Add new 0145 files only. Up must raise actionable text before ALTER when members is non-empty; replace the old visibility constraint with public/private and public default; add varchar(120) public_slug NOT NULL, named unique/canonical ASCII-hyphen/non-numeric/reserved checks (including ranking), and a BEFORE UPDATE immutable trigger/function. Down drops trigger/function/slug constraints/column in reverse order and restores the exact 0126-era public default plus public|members_only constraint. No UPDATE/INSERT/backfill is allowed per D-21.</action>
  <verify>
    <automated>docker compose exec -T team4sv30-backend go test ./internal/migrations -run 'Phase128|PublicIdentity|SlugImmutable' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - 0145 is the only new migration number.
    - Up SQL fails before ALTER on non-empty members and contains no row mutation.
    - Guarded up/down/up and immutability tests pass or safely skip without the dedicated DSN.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace runtime visibility and public identity fields</name>
  <read_first>
    backend/internal/handlers/app_public_profile.go
    backend/internal/models/member_profile.go
    backend/internal/repository/member_profile_repository.go
    backend/internal/handlers/app_auth_test.go
    shared/contracts/openapi.yaml
    .planning/phases/128-canonical-public-identity-visibility-foundation/128-CONTEXT.md
  </read_first>
  <files>backend/internal/models/member_profile.go, backend/internal/handlers/app_auth_test.go, backend/internal/handlers/app_public_profile.go</files>
  <action>Replace ProfileVisibilityMembersOnly with ProfileVisibilityPrivate and update existing own-profile test fixtures/validation to accept private and reject members_only. Add required Slug string json:slug to PublicMemberProfile. Replace the old AppUserID-as-authorization idea with internal server-computed owner/access fields suitable for the shared resolver; serialize no app-user ID. Update the existing interim app_public_profile.go comparisons and hidden reason literal to private so the package compiles against the new closed vocabulary; do not add a deprecated alias or retain members_only compatibility. Plan 11 subsequently replaces that interim 200 hidden branch with the already-specified shared resolver/neutral-404 behavior.</action>
  <verify>
    <automated>docker compose exec -T team4sv30-backend go test ./internal/handlers -run 'OwnProfile|ProfileVisibility' -count=1</automated>
  </verify>
    - Existing public handler comparisons compile with ProfileVisibilityPrivate and contain no members_only response literal.
  <acceptance_criteria>
    - Production Go contains no ProfileVisibilityMembersOnly constant.
    - Tests reject members_only and accept private.
    - Public DTO requires slug and exposes no app-user ownership identifier.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Asset | Control |
|---|---|---|
| Repository writes -> members | Immutable identity | DB checks, uniqueness, trigger |
| Migration runner -> DB | Schema/disposable rows | Non-empty precondition; no live apply |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation and verification |
|---|---|---|---|---|
| T-128-SLUG-RACE | Spoofing | Unique slug | mitigate | Unique constraint; allocator follows |
| T-128-IDENTITY-DRIFT | Spoofing | Slug update | mitigate | Immutable trigger/live test |
| T-128-DB-TARGET | Tampering | Migration | mitigate | Non-empty fail-close and guarded DSN |
| T-128-BOLA | Elevation | DTO | mitigate | No public AppUserID authorization |
</threat_model>

<verification>Run migration/handler tests, inspect only scoped diffs, and run git diff --check.</verification>
<success_criteria>Schema and Go agree on immutable slugs and public/private visibility without touching live rows.</success_criteria>
<output>After completion, create .planning/phases/128-canonical-public-identity-visibility-foundation/128-04-SUMMARY.md</output>
