---
phase: 128-canonical-public-identity-visibility-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/internal/testsupport/phase128_postgres.go
  - backend/internal/testsupport/phase128_postgres_test.go
  - backend/internal/migrations/phase128_public_identity_test.go
  - backend/internal/repository/member_public_slug_test.go
autonomous: true
requirements: [PMID-01, PMID-02]
must_haves:
  truths:
    - "The PostgreSQL test seam refuses team4s_v2 before opening a connection (D-21)."
    - "Wave 0 provisions exactly team4s_phase128_test and every PostgreSQL-backed gate receives its explicit DSN instead of skipping."
    - "Tests require a stored, unique, immutable slug and transaction-safe suffix allocation (D-01, D-02)."
    - "Tests cover Müller & Söhne -> mueller-und-soehne and invalid/reserved results (D-03, D-04)."
  artifacts:
    - path: backend/internal/testsupport/phase128_postgres.go
      provides: Required, fail-on-missing TEAM4S_PHASE128_TEST_DSN fixture for team4s_phase128_test
    - path: backend/internal/migrations/phase128_public_identity_test.go
      provides: Reversible 0145 migration proof
    - path: backend/internal/repository/member_public_slug_test.go
      provides: Normalizer, allocator, concurrency, and creation-path tests
  key_links:
    - from: backend/internal/migrations/phase128_public_identity_test.go
      to: database/migrations/0145_member_public_identity_visibility.up.sql
      via: guarded up/down/up execution
---

<objective>
Create the fail-closed PostgreSQL fixture and executable identity tests that gate Phase 128.

Purpose: Prevent migration or concurrency proof against the live database and establish RED tests for PMID-01/02.
Output: Dedicated test support plus migration and slug lifecycle tests.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-CONTEXT.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-RESEARCH.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-PATTERNS.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-VALIDATION.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add the dedicated Phase-128 PostgreSQL safety fixture</name>
  <read_first>
    backend/internal/testsupport/phase117_postgres.go
    backend/internal/testsupport/phase117_postgres_test.go
    backend/internal/testsupport/phase106_postgres.go
    AGENTS.md
  </read_first>
  <files>backend/internal/testsupport/phase128_postgres.go, backend/internal/testsupport/phase128_postgres_test.go</files>
  <action>First provision the dedicated database named exactly team4s_phase128_test through the running team4sv30-db service. Use an idempotent shell guard that queries pg_database as team4s against postgres and runs createdb -U team4s -O team4s team4s_phase128_test only when the exact name is absent; never drop, recreate, or reset an existing database. Then copy the Phase-117 schema-isolation boundary but deliberately do not inherit its missing-DSN t.Skip behavior: OpenPhase128Postgres must t.Fatalf when TEAM4S_PHASE128_TEST_DSN is empty. Accept only parsed database names matching ^team4s_phase128_test(?:_[a-z0-9]+)?$, verify SELECT current_database() equals the parsed name, require schemas matching ^phase128_[a-z0-9_]+$, create only members/member_claims prerequisites, and guard cleanup from public. Explicitly reject team4s_v2, postgres, templates, malformed suffixes, uppercase, and hyphens. Ignore DATABASE_URL and never use docker compose down -v per D-21.</action>
  <verify>
    <automated>docker compose exec -T team4sv30-db sh -eu -c 'exists="$(psql -U team4s -d postgres -X -tAc "SELECT 1 FROM pg_database WHERE datname = '\''team4s_phase128_test'\''")"; if [ "$exists" != "1" ]; then createdb -U team4s -O team4s team4s_phase128_test; fi; test "$(psql -U team4s -d postgres -X -tAc "SELECT count(*) FROM pg_database WHERE datname = '\''team4s_phase128_test'\''")" = "1"' &amp;&amp; docker compose exec -T -e TEAM4S_PHASE128_TEST_DSN='postgres://team4s:team4s_dev_password@team4sv30-db:5432/team4s_phase128_test?sslmode=disable' team4sv30-backend go test ./internal/testsupport -run 'Phase128' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The new fixture contains TEAM4S_PHASE128_TEST_DSN and no DATABASE_URL lookup.
    - Safety tests reject team4s_v2 and public.
    - OpenPhase128Postgres fails, rather than skips, when the dedicated DSN is absent.
    - The idempotent provision check leaves one database named exactly team4s_phase128_test and never targets team4s_v2.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define reversible migration and immutability tests</name>
  <read_first>
    backend/internal/migrations/phase106_member_points_test.go
    backend/internal/testsupport/phase128_postgres.go
    database/migrations/0044_add_db_schema_v2_target_tables.up.sql
    database/migrations/0077_member_profiles_mvp.up.sql
    database/migrations/0126_member_profile_public_defaults.up.sql
    database/migrations/0144_drop_theme_segment_playback_sources_legacy_unique.up.sql
    AGENTS.md
  </read_first>
  <files>backend/internal/migrations/phase128_public_identity_test.go</files>
  <action>Create compiling source-contract and guarded live tests modeled on Phase 106. Require members.public_slug to be NOT NULL, unique, canonical-format constrained, non-numeric, reserved-word protected, and immutable; require visibility exactly public/private per D-06. Prove non-empty members makes 0145 fail before mutation, then issue ROLLBACK and assert schema/data unchanged. Prove empty up -> down -> up, nickname UPDATE preserves slug, and direct slug UPDATE fails. Run only through OpenPhase128Postgres. Before production files exist, keep the named RED assertion TestPhase128MigrationRequiresStoredIdentity compilable by inspecting the absent/old migration contract; do not reference undefined production symbols. Plan 04 replaces its expected assertion failure with the guarded behavioral green result.</action>
  <verify>
    <automated>docker compose exec -T -e TEAM4S_PHASE128_TEST_DSN='postgres://team4s:team4s_dev_password@team4sv30-db:5432/team4s_phase128_test?sslmode=disable' team4sv30-backend sh -lc 'out="$(mktemp)"; if go test -json ./internal/migrations -run "Phase128|PublicIdentity|SlugImmutable" -count=1 &gt;"$out" 2&gt;&amp;1; then cat "$out"; exit 1; fi; cat "$out"; grep -Eq '"'"'"Action"'"'":"'"'"fail"'"',"'"'"Test"'"'":"'"'"TestPhase128MigrationRequiresStoredIdentity"'"' "$out"; ! grep '"'"'"Action"'"'":"'"'"fail"'"',"'"'"Test"'"':' "$out" | grep -v '"'"'"Test"'"'":"'"'"TestPhase128MigrationRequiresStoredIdentity"'"'; ! grep -Eqi "build failed|undefined:|syntax error|connection refused|unsafe TEAM4S_PHASE128_TEST_DSN|panic:" "$out"'</automated>
  </verify>
  <acceptance_criteria>
    - Tests name both 0145 files and perform up/down/up.
    - Non-empty precondition rollback leaves schema/data unchanged.
    - RED verification accepts only TestPhase128MigrationRequiresStoredIdentity and rejects compile, syntax, DSN-safety, or connection failures.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Define normalizer, allocation, and creation-seam tests</name>
  <read_first>
    backend/internal/repository/member_profile_repository.go
    backend/internal/repository/member_requests_repository.go
    backend/internal/repository/fansub_group_app_members_repository.go
    backend/internal/repository/hist_group_members_repository.go
    backend/internal/testsupport/phase128_postgres.go
    .planning/phases/128-canonical-public-identity-visibility-foundation/128-PATTERNS.md
  </read_first>
  <files>backend/internal/repository/member_public_slug_test.go</files>
  <action>Add table-driven cases for explicit German transliteration, ampersand-to-und, decomposable accents, punctuation collapse, whitespace, length, empty, numeric, reserved, control, and path-separator input. Add guarded concurrent scenarios proving name/name-2/name-3 and a literal name-2 base cannot collide. Add source invariants requiring all three production INSERT INTO members paths to call one shared allocator and rejecting random/member-ID fallback generation (D-02 through D-05). Keep the Wave-0 RED contract compilable without undefined production symbols by making TestPhase128MemberSlugContract inspect the expected source/creation seams; Plan 05 converts the same cases to direct normalizer/allocator behavior once the contract exists.</action>
  <verify>
    <automated>docker compose exec -T -e TEAM4S_PHASE128_TEST_DSN='postgres://team4s:team4s_dev_password@team4sv30-db:5432/team4s_phase128_test?sslmode=disable' team4sv30-backend sh -lc 'out="$(mktemp)"; if go test -json -race ./internal/repository -run "MemberSlug|PublicIdentity" -count=1 &gt;"$out" 2&gt;&amp;1; then cat "$out"; exit 1; fi; cat "$out"; grep -Eq '"'"'"Action"'"'":"'"'"fail"'"',"'"'"Test"'"'":"'"'"TestPhase128MemberSlugContract"'"' "$out"; ! grep '"'"'"Action"'"'":"'"'"fail"'"',"'"'"Test"'"':' "$out" | grep -v '"'"'"Test"'"'":"'"'"TestPhase128MemberSlugContract"'"'; ! grep -Eqi "build failed|undefined:|syntax error|connection refused|unsafe TEAM4S_PHASE128_TEST_DSN|panic:" "$out"'</automated>
  </verify>
  <acceptance_criteria>
    - Müller & Söhne and collision sequences are asserted literally.
    - Exactly three production member INSERT files are inventoried.
    - No accepted case uses numeric, random, UUID, or member-ID fallback.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Asset | Control |
|---|---|---|
| Test runner -> PostgreSQL DSN | Live disposable database | Dedicated env/name/current_database guards |
| Concurrent creation -> slug namespace | Stable public identity | Serialization plus unique constraint tests |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation and verification |
|---|---|---|---|---|
| T-128-TEST-DSN | Tampering / Denial of Service | `OpenPhase128Postgres` and Compose test commands | mitigate | Parse `TEAM4S_PHASE128_TEST_DSN` before connection, accept only `^team4s_phase128_test(?:_[a-z0-9]+)?$`, verify `current_database()`, reject `team4s_v2`, and run `go test ./internal/testsupport -run Phase128` with the explicit guarded DSN. |
| T-128-DESTRUCTIVE-RESET | Tampering / Denial of Service | Wave-0 database provisioning | mitigate | Provision only an absent exact `team4s_phase128_test`; never drop, recreate, truncate, reset, consult `DATABASE_URL`, or run `docker compose down -v`. The idempotent SQL assertion proves exactly one guarded database exists and Plan 01 never mutates live `team4s_v2` (D-21). |
| T-128-IDENTITY-DRIFT | Spoofing | `members.public_slug` lifecycle | mitigate | Migration/repository tests prove nickname updates preserve the stored slug and direct slug updates fail (D-01, D-05); the guarded migration test regex includes `SlugImmutable|Nickname`. |
| T-128-SLUG-RACE | Spoofing | Concurrent slug allocation | mitigate | Race-enabled guarded PostgreSQL tests cover `name`, `name-2`, `name-3`, and a literal `name-2` base under the unique constraint and one shared transaction allocator (D-02). |
| T-128-MIGRATION-PRECONDITION | Tampering | Migration 0145 against non-empty `members` | mitigate | The migration test seeds a non-empty table, requires 0145 to fail before mutation, rolls back, and proves schema/data remain unchanged before exercising empty up/down/up (D-21). |
</threat_model>

<verification>
1. Idempotently provision and verify the exact guarded database without dropping or resetting anything:
   `docker compose exec -T team4sv30-db sh -eu -c 'exists="$(psql -U team4s -d postgres -X -tAc "SELECT 1 FROM pg_database WHERE datname = '\''team4s_phase128_test'\''")"; if [ "$exists" != "1" ]; then createdb -U team4s -O team4s team4s_phase128_test; fi; test "$(psql -U team4s -d postgres -X -tAc "SELECT count(*) FROM pg_database WHERE datname = '\''team4s_phase128_test'\''")" = "1"'`
2. Run the safety fixture with the DSN passed explicitly:
   `docker compose exec -T -e TEAM4S_PHASE128_TEST_DSN='postgres://team4s:team4s_dev_password@team4sv30-db:5432/team4s_phase128_test?sslmode=disable' team4sv30-backend go test ./internal/testsupport -run 'Phase128' -count=1`
3. Run both intentional RED gates through the guarded DSN and accept only their named contract assertion failures, never a compile, syntax, connection, panic, or unsafe-DSN failure:
   `docker compose exec -T -e TEAM4S_PHASE128_TEST_DSN='postgres://team4s:team4s_dev_password@team4sv30-db:5432/team4s_phase128_test?sslmode=disable' team4sv30-backend sh -lc 'out="$(mktemp)"; if go test -json ./internal/migrations -run "Phase128|PublicIdentity|SlugImmutable" -count=1 >"$out" 2>&1; then cat "$out"; exit 1; fi; grep -Eq '"'"'"Action"'"'":"'"'"fail"'"',"'"'"Test"'"'":"'"'"TestPhase128MigrationRequiresStoredIdentity"'"' "$out"; ! grep '"'"'"Action"'"'":"'"'"fail"'"',"'"'"Test"'"':' "$out" | grep -v '"'"'"Test"'"'":"'"'"TestPhase128MigrationRequiresStoredIdentity"'"'; ! grep -Eqi "build failed|undefined:|syntax error|connection refused|unsafe TEAM4S_PHASE128_TEST_DSN|panic:" "$out"'`
   `docker compose exec -T -e TEAM4S_PHASE128_TEST_DSN='postgres://team4s:team4s_dev_password@team4sv30-db:5432/team4s_phase128_test?sslmode=disable' team4sv30-backend sh -lc 'out="$(mktemp)"; if go test -json -race ./internal/repository -run "MemberSlug|PublicIdentity" -count=1 >"$out" 2>&1; then cat "$out"; exit 1; fi; grep -Eq '"'"'"Action"'"'":"'"'"fail"'"',"'"'"Test"'"'":"'"'"TestPhase128MemberSlugContract"'"' "$out"; ! grep '"'"'"Action"'"'":"'"'"fail"'"',"'"'"Test"'"':' "$out" | grep -v '"'"'"Test"'"'":"'"'"TestPhase128MemberSlugContract"'"'; ! grep -Eqi "build failed|undefined:|syntax error|connection refused|unsafe TEAM4S_PHASE128_TEST_DSN|panic:" "$out"'`
4. Validate this plan's frontmatter/structure and balanced structural tags:
   `./scripts/gsd-linux.sh frontmatter validate .planning/phases/128-canonical-public-identity-visibility-foundation/128-01-PLAN.md --schema plan`
   `./scripts/gsd-linux.sh verify plan-structure .planning/phases/128-canonical-public-identity-visibility-foundation/128-01-PLAN.md`
   `python3 -c "from pathlib import Path; import re; p=Path('.planning/phases/128-canonical-public-identity-visibility-foundation/128-01-PLAN.md'); s=p.read_text(); tags=('objective','execution_context','context','tasks','task','read_first','files','action','verify','automated','acceptance_criteria','threat_model','verification','success_criteria','output'); bad=[t for t in tags if len(re.findall(r'<'+t+r'(?:\s[^>]*)?>',s)) != s.count('</'+t+'>')]; assert not bad, bad; assert s.rstrip().endswith('<'+'/output>')"`
5. Run `git diff --check -- .planning/phases/128-canonical-public-identity-visibility-foundation/128-01-PLAN.md backend/internal/testsupport/phase128_postgres.go backend/internal/testsupport/phase128_postgres_test.go backend/internal/migrations/phase128_public_identity_test.go backend/internal/repository/member_public_slug_test.go`.
</verification>

<success_criteria>
- `OpenPhase128Postgres` fails closed without its explicit DSN, rejects `team4s_v2`, and verifies the connected database and isolated schema before any test mutation.
- Exactly one non-destructively provisioned database named `team4s_phase128_test` backs every PostgreSQL test in this plan.
- The migration RED gate compiles and fails only `TestPhase128MigrationRequiresStoredIdentity`; the repository RED gate compiles under `-race` and fails only `TestPhase128MemberSlugContract`.
- Tests cover immutable stored identity, transaction-safe readable suffix allocation, German transliteration, reserved/unusable rejection, and a non-empty migration precondition with rollback proof.
- Frontmatter/structure validation, structural tag balance, and `git diff --check` pass.
</success_criteria>

<output>After completion, create `.planning/phases/128-canonical-public-identity-visibility-foundation/128-01-SUMMARY.md`.</output>
