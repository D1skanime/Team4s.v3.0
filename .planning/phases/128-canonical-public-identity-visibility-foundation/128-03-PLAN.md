---
phase: 128-canonical-public-identity-visibility-foundation
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/proxy.test.ts
  - frontend/src/lib/api.auth-refresh.test.ts
  - frontend/src/app/members/[slug]/page.test.tsx
  - frontend/src/app/members/[slug]/OwnHiddenProfilePreview.test.tsx
  - frontend/src/app/me/profile/page.test.tsx
autonomous: true
requirements: [PMID-03, PMPR-01, PMPR-04, PMPR-05]
must_haves:
  truths:
    - "Canonical-equivalent paths redirect permanently; numeric/guessed slugs remain unavailable (D-18, D-19, D-20)."
    - "A refresh-only owner sees neutral loading then the complete authoritative profile without 404/login flash (D-13, D-15, D-17)."
    - "Owner presentation is read-only, private, carries exact notice/actions, and suppresses correction (D-12, D-14, D-16)."
  artifacts:
    - path: frontend/proxy.test.ts
      provides: Canonical URL syntax/encoding matrix
    - path: frontend/src/lib/api.auth-refresh.test.ts
      provides: Central refresh tests for member helpers
    - path: frontend/src/app/members/[slug]/page.test.tsx
      provides: Neutral metadata and composition tests
  key_links:
    - from: frontend/src/app/members/[slug]/OwnHiddenProfilePreview.test.tsx
      to: frontend/src/lib/api.ts
      via: token-free getMemberProfile
---

<objective>
Create the frontend RED contract for canonical navigation, neutral states, central refresh, and owner preview.

Purpose: Fail direct-token, numeric-fallback, cache, and no-flash regressions before UI changes.
Output: Focused Vitest contracts for proxy, API client, route, preview, and links.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-CONTEXT.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-UI-SPEC.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-VALIDATION.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define canonical URL and neutral route tests</name>
  <read_first>
    frontend/src/app/members/[slug]/page.tsx
    frontend/src/app/members/[slug]/page.test.tsx
    frontend/src/app/members/[slug]/page.module.css
    .planning/phases/128-canonical-public-identity-visibility-foundation/128-RESEARCH.md
    .planning/phases/128-canonical-public-identity-visibility-foundation/128-UI-SPEC.md
  </read_first>
  <files>frontend/proxy.test.ts, frontend/src/app/members/[slug]/page.test.tsx</files>
  <action>Create the no-local-analog proxy matrix from Research Pattern 6: case, edge plain/encoded whitespace, equivalent one-segment encoding, query preservation, encoded slash, control, unusable, and numeric input. Extend page tests so missing/private non-owner share title Profil nicht verf?gbar | Team4s, noindex, exact UI-SPEC copy/action, heading structure, and absence of profile/owner/correction data. Record the Phase-128 boundary: backend/API 404 equality is the automated status gate; no new server refresh seam is created.</action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend npm test -- --run proxy.test.ts 'src/app/members/[slug]/page.test.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - Redirect tests prove syntax-only behavior and 308 query preservation.
    - Neutral UI contains no requested slug, nickname, login/private reason, edit, or correction action.
    - Dirty profile/carousel source-read files remain unmodified.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define refresh-only and authoritative preview tests</name>
  <read_first>
    frontend/src/lib/api.auth-refresh.test.ts
    frontend/src/lib/api.ts
    frontend/src/app/members/[slug]/OwnHiddenProfilePreview.tsx
    frontend/src/app/members/[slug]/OwnHiddenProfilePreview.test.tsx
    frontend/src/app/me/profile/page.test.tsx
    docs/frontend/auth-api-client.md
    .planning/phases/128-canonical-public-identity-visibility-foundation/128-UI-SPEC.md
  </read_first>
  <files>frontend/src/lib/api.auth-refresh.test.ts, frontend/src/app/members/[slug]/OwnHiddenProfilePreview.test.tsx, frontend/src/app/me/profile/page.test.tsx</files>
  <action>Extend existing refresh mocks for getMemberProfile, getMemberContributions, and one project-member helper: no/expired access plus valid refresh must refresh centrally, retry with the new bearer internally, and retain no-store. Rewrite preview expectations to getMemberProfile(slug), authoritative public DTO/access result, neutral loading, exact owner notice/actions, full composition, and no correction. Add source assertions rejecting getOwnProfile conversion, token/cookie reads, direct refresh/bearer, protected bare fetch, nickname slugification, and slug || member_id per D-05/D-15/D-17.</action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend npm test -- --run src/lib/api.auth-refresh.test.ts 'src/app/members/[slug]/OwnHiddenProfilePreview.test.tsx' src/app/me/profile/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Refresh-only starts with no access token and a valid refresh token.
    - Preview asserts loading -> full owner content with no unavailable/correction flash.
    - No protected UI token parameter or bare fetch is accepted.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Asset | Control |
|---|---|---|
| Encoded path -> Next route | Canonical identity | Decode once and reject ambiguity |
| Browser session -> API | Refresh credentials | Central client only |
| Viewer result -> cache/UI | Private profile | no-store and neutral gate |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation and verification |
|---|---|---|---|---|
| T-128-ENCODED-PATH | Tampering | proxy | mitigate | Encoding/case/numeric matrix |
| T-128-AUTH-BOUNDARY | Elevation | preview/API | mitigate | Refresh and no-token tests |
| T-128-ENUMERATION | Information disclosure | metadata/state | mitigate | Identical neutral output |
| T-128-CACHE | Information disclosure | owner preview | mitigate | no-store assertions |
</threat_model>

<verification>Run focused Vitest commands and git diff --check; RED failures must map to Phase-128 behavior.</verification>
<success_criteria>Frontend route, refresh, cache, and identity invariants are specified before production edits.</success_criteria>
<output>After completion, create .planning/phases/128-canonical-public-identity-visibility-foundation/128-03-SUMMARY.md</output>
