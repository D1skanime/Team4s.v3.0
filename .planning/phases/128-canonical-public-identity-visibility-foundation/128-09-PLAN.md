---
phase: 128-canonical-public-identity-visibility-foundation
plan: 09
type: execute
wave: 2
depends_on: [128-02, 128-04]
files_modified:
  - backend/internal/repository/member_public_access_repository.go
  - backend/internal/repository/member_public_access_repository_test.go
  - backend/internal/repository/member_profile_repository.go
  - backend/internal/repository/member_profile_repository_test.go
autonomous: true
requirements: [PMID-02, PMID-03, PMPR-01, PMPR-02]
must_haves:
  truths:
    - "One minimal stored-slug/verified-claim decision runs before profile/project detail (D-07, D-08)."
    - "Missing/private non-owner/numeric/guessed slug all become ErrNotFound (D-09, D-19, D-20)."
    - "Own and public DTOs map stored slug and nickname updates never rewrite it (D-01, D-05)."
  artifacts:
    - path: backend/internal/repository/member_public_access_repository.go
      provides: ResolvePublicMemberAccess on existing MemberProfileRepository
    - path: backend/internal/repository/member_profile_repository.go
      provides: ID-based profile/projects loaders and stored slug mapping
  key_links:
    - from: backend/internal/repository/member_public_access_repository.go
      to: member_claims
      via: verified exact owner predicate
    - from: backend/internal/repository/member_profile_repository.go
      to: members.public_slug
      via: ID-based detail projection
---
<objective>
Add the shared deny-first resolver to MemberProfileRepository and split profile/project detail loading behind resolved member IDs.

Purpose: Make access precede all expensive/private profile fan-out.
Output: Minimal resolver plus ID-based profile/project loaders.
</objective>
<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-02-SUMMARY.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-04-SUMMARY.md
@.planning/phases/128-canonical-public-identity-visibility-foundation/128-PATTERNS.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement ResolvePublicMemberAccess on the existing repository</name>
  <read_first>
    backend/internal/repository/member_profile_repository.go
    backend/internal/repository/member_public_access_repository_test.go
    backend/internal/models/member_profile.go
    backend/internal/middleware/comment_auth.go
    docs/architecture/db-schema-fansub-domain.md
  </read_first>
  <files>backend/internal/repository/member_public_access_repository.go, backend/internal/repository/member_public_access_repository_test.go</files>
  <action>Add ResolvePublicMemberAccess(ctx, requestedSlug, viewerAppUserID) to MemberProfileRepository, not a parallel store. Reject empty, numeric, non-canonical/unsafe input before query. Query exact members.public_slug and only member ID, stored slug, visibility, plus EXISTS verified member_claim for the supplied viewer. Public allows all; private allows only exact verified owner; missing/private denial returns the same ErrNotFound. Do not consult members.user_id, legacy linkage, login presence, role/admin flags, or detail tables. Return server-computed IsOwner and IsPrivatePreview facts for handlers per D-07/D-11/D-17.</action>
  <verify>
    <automated>docker compose exec -T team4sv30-backend go test ./internal/repository -run 'PublicMemberAccess|VisibilityFirst' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Resolver SQL selects no badges/memberships/projects/contributions/media/story/points.
    - All denial cases return ErrNotFound.
    - Verified claim equality is the only private grant.
  </acceptance_criteria>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Move profile and projects to ID-based loaders</name>
  <read_first>
    backend/internal/repository/member_profile_repository.go
    backend/internal/repository/member_profile_repository_test.go
    backend/internal/repository/member_public_access_repository.go
    backend/internal/models/member_profile.go
    .planning/phases/128-canonical-public-identity-visibility-foundation/128-PATTERNS.md
  </read_first>
  <files>backend/internal/repository/member_profile_repository.go, backend/internal/repository/member_profile_repository_test.go</files>
  <action>Replace GetPublicMemberProfile(slug) lookup/fallback with GetPublicMemberProfileByID(ctx,memberID), retaining existing detail fan-out only after an allowed resolver result. Map m.public_slug to required PublicMemberProfile.Slug; remove AppUserID authorization mapping, numeric fallback, O(n) normalized scan, and own-profile deriveMemberSlug mapping. Add GetPublicMemberProjectsByID that calls loadCurrentProjects/count directly and never calls the full profile loader. Update UpdateOwnProfile validation to public/private; omit public_slug from every update SQL. Keep temporary obsolete normalizer definitions only until Plan 10 removes their last package consumers, with no call sites remaining in this file.</action>
  <verify>
    <automated>docker compose exec -T team4sv30-backend go test ./internal/repository -run 'MemberProfile|PublicMemberProjects|SlugImmutable|VisibilityFirst' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Profile/projects detail methods accept memberID, not slug.
    - No public profile detail query resolves nickname or numeric ID.
    - Own/public DTO slug reads m.public_slug and update SQL never writes it.
  </acceptance_criteria>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Asset | Control |
|---|---|---|
| Slug/viewer -> repository | Private existence | Minimal exact lookup and verified claim |
| Resolver -> detail loaders | Profile detail | ID passed only after allow |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation and verification |
|---|---|---|---|---|
| T-128-BOLA | Elevation | Resolver | mitigate | Claim matrix; admin ignored |
| T-128-ENUMERATION | Information disclosure | Denial | mitigate | One ErrNotFound |
| T-128-POSTLOAD | Information disclosure | Profile fan-out | mitigate | ID loader invoked only after resolver |
| T-128-IDENTITY-DRIFT | Spoofing | DTO/update | mitigate | Stored mapping and immutable DB |
</threat_model>
<verification>Run focused repository tests and git diff --check.</verification>
<success_criteria>Member identity/access is minimal and authoritative before profile/project detail loading.</success_criteria>
<output>After completion, create .planning/phases/128-canonical-public-identity-visibility-foundation/128-09-SUMMARY.md</output>
