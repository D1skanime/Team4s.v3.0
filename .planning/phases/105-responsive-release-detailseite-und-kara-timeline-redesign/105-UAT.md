---
phase: 105-responsive-release-detailseite-und-kara-timeline-redesign
plan: "05"
created: 2026-07-19
updated: 2026-07-19
status: blocked-automated-gate
route: http://127.0.0.1:3000/fansubs/c-subs/fansubprojekt/vipers-creed/releases/1
---

# Phase 105: Automatisierte Gates und Live-UAT

## Aktueller Status

Die Live-UAT wurde nicht begonnen. Die zuvor blockierende Auth-Boundary-Suite ist nach dem minimalen Rule-3-Fix vollständig grün. Das nächste verpflichtende Fail-fast-Gate, der projektweite Lint, stoppt jedoch an einem bereits in Plan 105-02 dokumentierten Fehler außerhalb der Phase-105-Dateien. Der Plan darf weder zu Build/Abschlussscans noch zur sichtbaren Browserprüfung fortschreiten, bevor `npm run lint` grün ist.

Pretty Route für die spätere gemeinsame Prüfung: [Viper's Creed Release 1](http://127.0.0.1:3000/fansubs/c-subs/fansubprojekt/vipers-creed/releases/1)

## Automatisierte Gates

Ausführungskontext: `C:\Users\admin\Documents\Team4s\frontend`, PowerShell 5.1. Vollständige Wiederholung nach Rule-3-Fix gestartet `2026-07-19T13:54:15Z`.

| Gate | Befehl | Exitcode | Ergebnis | Evidenz |
| --- | --- | ---: | --- | --- |
| Vollständige Phase-/Pretty-Route-/PublicReleaseBlock-/Auth-Suite | `npm test -- --run "src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]" "src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/releases/[releaseVersionId]/page.test.tsx" "src/components/fansubs/__tests__/PublicReleaseBlock.test.tsx" src/lib/api.auth-refresh.test.ts src/lib/api.no-token-boundary.test.ts` | 0 | PASS | 14/14 Testdateien und 90/90 Tests PASS. |
| Explizite Refresh-Regression | Bestandteil desselben Befehls: `src/lib/api.auth-refresh.test.ts` | 0 (Testdatei) | PASS | 19/19 Tests PASS; fehlender/abgelaufener Access-Token mit gültigem Refresh-Token bleibt aktive Session und wird zentral erneuert. |
| Explizite No-Token-Boundary | Bestandteil desselben Befehls: `src/lib/api.no-token-boundary.test.ts` | 0 (Testdatei) | PASS | 9/9 Tests PASS nach zentraler Auth-Seam-Korrektur. |
| Typecheck | `npm run typecheck` | 0 | PASS | `tsc --noEmit` ohne Fehler. |
| Projektweiter Lint | `npm run lint` | 1 | **BLOCKED** | 323 Meldungen: 1 Fehler, 322 Warnungen. Blockierender Fehler: `frontend/src/components/fansubs/FansubStorySection.tsx:49`, `react-hooks/set-state-in-effect`. |
| Produktions-Build | `npm run build` | — | Nicht ausgeführt | Fail-fast nach dem fehlgeschlagenen Lintgate. |
| Arbeitsbaumprüfung | `git diff --check` | — | Nicht ausgeführt | Fail-fast nach dem fehlgeschlagenen Lintgate. |

### Behobene Rule-3-Abweichung

| Testvertrag | Datei | Korrektur | Commit |
| --- | --- | --- | --- |
| Browser-Komponenten führen keine Token-Props/-Locals | `frontend/src/components/groups/GroupHistorySection.tsx` | `authToken`-Prop und Weitergabe entfernt; list/create/update/delete werden tokenlos über die bestehenden zentralen Seams aufgerufen. | `a6ecc310` |
| Direkter Browser-`fetch` bleibt in erlaubten zentralen/Public-Seams | `frontend/src/app/me/profile/components/ProfileBackgroundCard.tsx` | Source-Blob wird mit `apiClientFetch(sourceBackgroundURL)` geladen; Fehler- und Blob-Verhalten bleibt erhalten. | `a6ecc310` |

Fokussierte Evidenz: `api.no-token-boundary` 9/9, `api.auth-refresh` 19/19, `GroupHistorySection` 46/46 und `ProfileBackgroundCard` 2/2 PASS; Typecheck und gezielter ESLint ohne Fehler.

### Aktueller blockierender Befund

`frontend/src/components/fansubs/FansubStorySection.tsx:49` ruft `setIsMobileStoryOpen(false)` synchron innerhalb eines `useEffect` auf und verletzt `react-hooks/set-state-in-effect`. `git blame` führt die Zeile auf Commit `05a41a06b` vom 2026-07-13 zurück. Der identische Altbefund ist bereits in `105-02-SUMMARY.md` dokumentiert und liegt außerhalb des ausdrücklich freigegebenen Zwei-Komponenten-Fix-Scope.

### Quellscans

Die planmäßigen Scans auf neue `document.cookie`-, `Authorization: Bearer`-, `getRuntimeAuthToken`-, Keycloak-Refresh-, freie Segment-Bounds-, API-/Contract-/DB-/Media-Ownership- und sichtbare ASCII-Umlaut-Seams wurden wegen des vorgeschalteten fail-fast Lintgate nicht als Abschlussgate ausgeführt. Die dedizierte Auth-Boundary-Suite ist inzwischen vollständig grün.

## Live-UAT-Voraussetzung

Vor der Browserprüfung müssen der vollständige Testbefehl, Typecheck, Lint, Build, `git diff --check` und alle Quellscans ausnahmslos PASS sein. Erst dann wird der laufende Frontend-Stand über den dokumentierten Projektweg aktualisiert und die Pretty Route über den sichtbaren Pfad `Fansubseite → Fansub-Projekt → Release ansehen` geöffnet.

## Viewport-Matrix

| Viewport | Erwartung | Messung / Beobachtung | Screenshot | Ergebnis |
| ---: | --- | --- | --- | --- |
| 390 px | Keine horizontale Timeline; Kara-Karten vertikal; Gallery 2 Spalten; Texte/Beteiligte 1 Spalte; Navigation gestapelt und mindestens 48 px hoch; `scrollWidth <= clientWidth`; Fokus nicht abgeschnitten. | Noch nicht ausgeführt: Automatikgate blockiert. | — | Nicht geprüft |
| 768 px | Timeline mit 3 Zeitmarken; Segmentkarten 1 Spalte; Gallery 2; Texte 1; Beteiligte 2; kein Überlauf oder Fokus-Clipping. | Noch nicht ausgeführt: Automatikgate blockiert. | — | Nicht geprüft |
| 1024 px | Timeline mit 3 Zeitmarken; Segmentkarten 2 Spalten; Gallery 3; Texte 2, sofern je mindestens 320 px; Beteiligte mindestens 2. | Noch nicht ausgeführt: Automatikgate blockiert. | — | Nicht geprüft |
| 1440 px | Timeline mit 5 Zeitmarken; Segmentkarten 2 Spalten; Gallery 4; Texte 2; Beteiligte `auto-fit` ab 220 px; volle Public-Breite. | Noch nicht ausgeführt: Automatikgate blockiert. | — | Nicht geprüft |
| Resize 1024 → 390 → 1440 px | Auswahl, Playerzustand, DOM-Reihenfolge und normaler Seitenfluss bleiben stabil; kein horizontaler Überlauf. | Noch nicht ausgeführt: Automatikgate blockiert. | — | Nicht geprüft |

## Session- und Rechte-Matrix

| Zustand | Erwartung | Live-Beobachtung | Ergebnis |
| --- | --- | --- | --- |
| Gast | Segmentinformationen sichtbar; keine Kara-CTA, kein Login-Hinweis, keine unavailable-Diagnose, kein Autoplay; keine Vollfolge. | Noch nicht ausgeführt: Automatikgate blockiert. | Nicht geprüft |
| Eingeloggter Fansubber | Ready-Segmente bieten `Kara abspielen`; unavailable zeigt nur `Noch nicht abspielbar`; Player erscheint groß unter der Auswahl. | Noch nicht ausgeführt: Automatikgate blockiert. | Nicht geprüft |
| Refresh-only ohne Access-Token | Bestehende deterministische Browser-Fixture oder natürlicher lokaler Tokenablauf; aktive Session über `hasAccessToken || hasRefreshToken`; zentraler Client erneuert vor geschütztem Zugriff. Keine manuelle Cookie-/Token-Manipulation. | Noch nicht ausgeführt: Automatikgate blockiert. | Nicht geprüft |
| Vollfolgenberechtigter Nutzer | `Vollständige Episode` ausschließlich bei zentralem `can_play && stream_ready`; Vollfolge bleibt gegenüber der Release-Dokumentation sekundär. | Noch nicht ausgeführt: Automatikgate blockiert. | Nicht geprüft |

## Kara- und Player-Matrix

| Fall | Erwartung | Live-Beobachtung | Ergebnis |
| --- | --- | --- | --- |
| Typen OP / ED / IN / Middle / unbekannt | Jeder Typ besitzt Textlabel und eigene ruhige Medienfarbe; Statusrot wird nicht als Typfarbe verwendet. | Noch nicht ausgeführt: Automatikgate blockiert. | Nicht geprüft |
| Kurze und kollidierende Segmente | Fachproportion bleibt exakt; Hit-Ziel mindestens 44×44 px; Außenlabels bleiben lesbar, nutzen zwei Lanes und verbreitern die Seite nicht. | Noch nicht ausgeführt: Automatikgate blockiert. | Nicht geprüft |
| Deep-Link Gast / Session | Gast erhält Hervorhebung ohne Streamversuch; aktive Session darf nur bei bereitem Segment automatisch starten. | Noch nicht ausgeführt: Automatikgate blockiert. | Nicht geprüft |
| Schneller Segmentwechsel | Vorheriger Stream wird pausiert, `src` entfernt und Mediaelement neu geladen, bevor der nächste Stream startet. | Noch nicht ausgeführt: Automatikgate blockiert. | Nicht geprüft |
| Playerfehler / Retry | Lokale öffentliche Fehlermeldung ohne Diagnose; erneute Aktion startet denselben servergebundenen Segmentpfad. | Noch nicht ausgeführt: Automatikgate blockiert. | Nicht geprüft |
| Tastatur / Auswahl | Enter und Leertaste funktionieren; genau ein `aria-pressed=true`; sichtbarer Fokus; `aria-live=polite`; keine bedeutungslosen Gast-/unavailable-Tabstopps. | Noch nicht ausgeführt: Automatikgate blockiert. | Nicht geprüft |
| Reduced Motion | Auswahl ohne Layoutsprung/Zoom; Übergänge und Smooth-Scroll entfallen. | Noch nicht ausgeführt: Automatikgate blockiert. | Nicht geprüft |

## Content-, Overlay- und Fehler-Matrix

| Fall | Erwartung | Live-Beobachtung | Ergebnis |
| --- | --- | --- | --- |
| Hero Preview → Logo → text-only | Release-Preview bevorzugt, dann Anime-Logo nur als Darstellung, andernfalls bewusst komponierter Text-Hero ohne leere Medienbox. | Noch nicht ausgeführt: Automatikgate blockiert. | Nicht geprüft |
| Keine Karas / Bilder / Texte / Beteiligte | Leere Abschnitte werden vollständig ausgelassen und hinterlassen keine Platzhalter oder Trennlücken. | Noch nicht ausgeführt: Automatikgate blockiert. | Nicht geprüft |
| Viele Bilder | Gemeinsames responsives Raster; lokales Nachladen; Dedupe; bereits sichtbare Bilder bleiben bei Nachladefehler erhalten. | Noch nicht ausgeführt: Automatikgate blockiert. | Nicht geprüft |
| Lightbox | Ganze Bildfläche öffnet Original; Escape schließt; Fokus kehrt zum Auslöser zurück; Caption erscheint vollständig und nicht doppelt. | Noch nicht ausgeführt: Automatikgate blockiert. | Nicht geprüft |
| Lange Teamtexte | Sechs Zeilen initial; `Weiterlesen`/`Weniger anzeigen` wirkt kartenspezifisch und stabil über Cursor-Merge. | Noch nicht ausgeführt: Automatikgate blockiert. | Nicht geprüft |
| Mehrere Gruppen / doppelte Rollen | Gruppenherkunft bleibt sichtbar; Contributor-Person-/Rollen-Paare werden nicht dupliziert; nur aktuelle Release-Version. | Noch nicht ausgeführt: Automatikgate blockiert. | Nicht geprüft |
| Lokale Nachladefehler | Bilder-/Textfehler bleiben auf die betroffene Sektion begrenzt; geladene Inhalte bleiben sichtbar. | Noch nicht ausgeführt: Automatikgate blockiert. | Nicht geprüft |

## Navigation und Zielbild

| Fall | Erwartung | Live-Beobachtung | Ergebnis |
| --- | --- | --- | --- |
| Beide / eine / keine Kante | Fehlende Richtung wird ausgelassen; vorhandene Kanten bleiben gruppentreu und bevorzugen dieselbe Version, sonst Public Default. | Noch nicht ausgeführt: Automatikgate blockiert. | Nicht geprüft |
| Coop-Kontext | Öffnende Fansubgruppe bleibt Route-/Navigationskontext; Kooperationsgruppen stehen im Hero; keine gruppenfremde Ersatzkante. | Noch nicht ausgeführt: Automatikgate blockiert. | Nicht geprüft |
| Produktfluss | Pretty Route wird über sichtbare Fansub-/Projekt-Navigation erreicht, nicht nur als technische Direkt-URL. | Noch nicht ausgeführt: Automatikgate blockiert. | Nicht geprüft |
| Zielbild | Karas direkt nach Hero; Desktop volle Breite; Mobile vertikale Karas ohne Spur; ruhige Karten/Player; gemeinsames Bilderraster; lesbare Teamtexte; release-spezifische Beteiligte; Vollfolge sekundär; Navigation am Seitenende. | Noch nicht ausgeführt: Automatikgate blockiert. | Nicht geprüft |

## Menschliche Freigabe

Es liegt keine Freigabe vor. Automatisierte Screenshots oder Agentenurteil gelten nicht als Freigabesignal. Nach vollständig grünen Automatikgates muss der Nutzer mindestens Desktop und Mobile im gemeinsam sichtbaren Browser prüfen und ausdrücklich `approved` beziehungsweise `freigegeben` antworten oder konkrete Gaps mit Viewport, Zustand, Erwartung und Ist nennen.
