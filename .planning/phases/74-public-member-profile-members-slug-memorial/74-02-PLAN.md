---
phase: 74-public-member-profile-members-slug-memorial
plan: 02
type: execute
wave: 3
depends_on: ["74-00", "74-01"]
files_modified:
  - shared/contracts/admin-content.yaml
  - backend/internal/handlers/member_memorial_handler.go
  - backend/internal/repository/member_claims_repository.go
  - backend/internal/repository/member_claim_invitations_repository.go
  - backend/cmd/server/main.go
  - frontend/src/lib/api.ts
autonomous: true
requirements: [J, K, D-12, D-13, D-14, D-15, D-16, D-17]
must_haves:
  truths:
    - "Nur Global Admin kann memorial setzen; Nicht-Admin → 403 (J/D-14)"
    - "Memorial ist ein Status auf members.profile_status; app_user-Account bleibt unberührt (D-13)"
    - "Claim-Flows gegen ein memorial-Profil werden server-seitig abgelehnt (409) in BEIDEN Pfaden SubmitClaim + AcceptInvitation (J/D-17, Fallstrick 3)"
    - "Memorial-Set UND Claim-Block schreiben einen audit_logs-Eintrag mit Actor/Target/Outcome — nicht öffentlich (D-15)"
    - "Der Setter-Endpoint ist Global-Admin-gegated, NICHT gruppen-capability-gegated (D-16-Caveat, Fallstrick 4)"
  artifacts:
    - path: "backend/internal/handlers/member_memorial_handler.go"
      provides: "POST /api/v1/admin/members/:id/memorial — requirePlatformAdminIdentity + UPDATE + audit"
      exports: ["NewMemberMemorialHandler"]
      contains: "requirePlatformAdminIdentity"
    - path: "backend/internal/repository/member_claims_repository.go"
      provides: "Memorial-Guard in SubmitClaim (409 memorial_not_claimable)"
      contains: "memorial_not_claimable"
    - path: "backend/internal/repository/member_claim_invitations_repository.go"
      provides: "Memorial-Guard in AcceptInvitation (409 memorial_not_claimable)"
      contains: "memorial_not_claimable"
    - path: "backend/cmd/server/main.go"
      provides: "Route-Registration POST /api/v1/admin/members/:id/memorial"
      contains: "memorial"
  key_links:
    - from: "member_memorial_handler.go"
      to: "repository.AuditLogRepository.Write"
      via: "EventType member_profile.memorial_set, Outcome allowed"
      pattern: "member_profile.memorial_set"
    - from: "SubmitClaim + AcceptInvitation"
      to: "members.profile_status"
      via: "SELECT profile_status WHERE id guard vor INSERT"
      pattern: "profile_status"
---

<objective>
Implementiere die zwei gekoppelten Memorial-Schreibaktionen aus D-06 (Phase 72): den Global-Admin-only Memorial-Setter und die server-seitige Claim-Sperre gegen Memorial-Profile in BEIDEN Claim-Pfaden — beides mit Audit (D-15). Das Setzen verändert nur `members.profile_status`, niemals den `app_user`-Account (D-13).

Purpose: Erfüllt Erfolgskriterien 3 & 4 (Schutz + Setzbarkeit gemeinsam). Alle Seams existieren (requirePlatformAdminIdentity, AuditLogRepository.Write, SubmitClaim, AcceptInvitation) — Phase 74 verdrahtet sie. Die Frontend-Setter-Action-UI kommt in Plan 06; hier entsteht der Endpoint + api.ts-Funktion.
Output: 1 neuer Handler, 2 Repo-Guards, 1 Route, 1 admin-content-Contract-Eintrag, 1 api.ts-Funktion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-RESEARCH.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-PATTERNS.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-VALIDATION.md

<interfaces>
<!-- Verifiziert aus Quellcode -->

backend/internal/repository/audit_logs.go — AuditLogEntry{ ActorAppUserID *int64; EventType string; ScopeType string; ScopeID *int64; TargetType string; TargetID *int64; Action string; Outcome string; ReasonCode *string; Payload map[string]any }; func (r *AuditLogRepository) Write(ctx, entry) error (fehlertolerant via `_ =`).

backend/internal/handlers/platform_admin_authz.go — requirePlatformAdminIdentity(c, roleChecker, "admin") (Z. 15-73) → (identity, ok); bei !ok hat der Handler 401/403/500 bereits geschrieben; identity.AppUserID garantiert Global Admin; prüft auch Disabled/Pending.

backend/internal/repository/member_claims_repository.go — SubmitClaim (Z. 107-134); ClaimMutationError{ Code string; Message string; HTTPStatus int } (Z. 90).
backend/internal/repository/member_claim_invitations_repository.go — AcceptInvitation (Z. 104) — zweiter Claim-Pfad, erzeugt verified claim.

backend/internal/handlers/contribution_proposals_me_handler.go — Write-Ketten-Muster (Z. 229-361) + Interface-Stub-Muster (Z. 25-53) für testbare Handler-Konstruktion.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Memorial-Setter-Handler + Route + admin-content-Contract + api.ts</name>
  <behavior>
    - Nicht-Global-Admin → HTTP 403 (Plan-00 TestMemorialSetterRejectsNonPlatformAdmin grün).
    - Global Admin → members.profile_status='memorial' gesetzt + audit_logs-Eintrag (Plan-00 TestMemorialSetterWritesAuditLog grün).
    - app_user-Account bleibt unberührt (kein Account-UPDATE).
  </behavior>
  <read_first>
    - backend/internal/handlers/platform_admin_authz.go (requirePlatformAdminIdentity Z. 15-73)
    - backend/internal/handlers/contribution_proposals_me_handler.go (Konstruktor-/Interface-Stub-Muster Z. 25-53; Write-Kette + Audit Z. 352-358)
    - backend/internal/repository/audit_logs.go (AuditLogEntry-Felder)
    - backend/internal/handlers/member_memorial_handler_test.go (Plan-00-Stub — erwartete Konstruktor-Signatur NewMemberMemorialHandler + 403/Audit-Erwartung)
    - backend/cmd/server/main.go (Handler-Konstruktion + Route-Registration-Stil; admin-Routen unter /api/v1/admin)
    - shared/contracts/admin-content.yaml (bestehende admin-content-Pfad-Struktur)
    - frontend/src/lib/api.ts (submitMemberClaim Z. 2759-2765 — authorizedFetch-Muster für die neue setMemberMemorial-Funktion)
    - 74-RESEARCH.md §Muster 5 (Memorial-Setter) + Fallstrick 4 (nicht an Gruppen-Capability hängen)
  </read_first>
  <action>
    1. `member_memorial_handler.go` (NEU): `NewMemberMemorialHandler(memberRepo, auditLogRepo, roleChecker)` mit interface-getriebenen Abhängigkeiten (testbar, Muster aus contribution_proposals_me_handler.go). Handler `SetMemorial`: `requirePlatformAdminIdentity(c, h.roleChecker, "admin")` (NICHT CanForFansubGroup — D-16-Caveat/Fallstrick 4) → memberID aus Param parsen → vorigen Status lesen → `UPDATE members SET profile_status='memorial', updated_at=NOW() WHERE id=$1` (parametrisiert, pgx $N) → `auditLogRepo.Write` mit EventType "member_profile.memorial_set", TargetType "member", TargetID &memberID, Action "set_memorial", Outcome "allowed", Payload {"previous_status": prevStatus} → `c.JSON(200, gin.H{"data": gin.H{"member_id": memberID, "profile_status": "memorial"}})`. KEIN app_user-Account-UPDATE (D-13). Deutsche, kleingeschriebene Fehler-Strings im Stil des Authz-Analogs.
    2. `main.go`: Handler konstruieren + Route `POST /api/v1/admin/members/:id/memorial` unter authMiddleware registrieren.
    3. `admin-content.yaml`: Pfad `/api/v1/admin/members/{id}/memorial` (POST) dokumentieren (Lock K) — Response-Schema { data: { member_id, profile_status } }, 403 für Nicht-Admin, mit korrekten Umlauten in Beschreibungen.
    4. `api.ts`: `setMemberMemorial(memberId, token?)` nach submitMemberClaim-Muster (authorizedFetch, kein ad-hoc-Fetch).
  </action>
  <verify>
    <automated>cd backend && go test ./internal/handlers/... -run Memorial && go build ./... && cd ../frontend && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Plan-00 Memorial-Handler-Tests (403 + Audit) GREEN.
    - Route POST /api/v1/admin/members/:id/memorial in main.go registriert.
    - admin-content.yaml dokumentiert den Endpoint (Lock K).
    - Handler nutzt requirePlatformAdminIdentity, NICHT CanForFansubGroup.
    - Kein app_user-Account-UPDATE im Handler/Repo.
  </acceptance_criteria>
  <done>Memorial-Setter ist Global-Admin-gegated, auditiert, contract-dokumentiert; api.ts-Funktion vorhanden; Plan-00-Tests grün.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Claim-Sperre gegen Memorial in beiden Claim-Pfaden + Audit</name>
  <behavior>
    - SubmitClaim gegen memorial-Profil → ClaimMutationError Code "memorial_not_claimable", HTTPStatus 409.
    - AcceptInvitation gegen memorial-Profil → gleicher 409-Fehler.
    - Beide Ablehn-Ereignisse schreiben audit_logs (Outcome "denied").
    - Plan-00 TestClaimSubmit/AcceptInvitationBlockedForMemorialProfile grün.
  </behavior>
  <read_first>
    - backend/internal/repository/member_claims_repository.go (SubmitClaim Z. 107-134; ClaimMutationError Z. 90)
    - backend/internal/repository/member_claim_invitations_repository.go (AcceptInvitation Z. 104)
    - backend/internal/repository/audit_logs.go (AuditLogEntry für denied-Ereignis)
    - backend/internal/repository/member_claims_memorial_guard_test.go (Plan-00-Stub — Ziel "memorial_not_claimable" in BEIDEN Dateien)
    - 74-RESEARCH.md §Muster 6 (Claim-Sperre) + Fallstrick 3 (beide Pfade) + §Code-Beispiele Audit
  </read_first>
  <action>
    1. Gemeinsamer Helper `assertMemberClaimable(ctx, db, memberID)` (in member_claims_repository.go oder einer geteilten Repo-Datei): `SELECT profile_status FROM members WHERE id=$1` (parametrisiert); falls 'memorial' → return `&ClaimMutationError{Code:"memorial_not_claimable", Message:"Dieses Profil wird als Gedenkprofil geführt und kann nicht beansprucht werden.", HTTPStatus:409}` (deutscher String mit ü/ß).
    2. In `SubmitClaim` VOR dem INSERT (Z. ~112) den Helper aufrufen; bei Block-Fehler vor return ein audit_logs-denied-Ereignis schreiben (EventType "member_claim.memorial_blocked", TargetType "member", TargetID &memberID, Outcome "denied", Action "submit_claim"). Audit-Schreibung fehlertolerant (`_ =`), blockiert den Fehler-Return nicht.
    3. In `AcceptInvitation` denselben Helper VOR der Claim-Erzeugung aufrufen (zweiter Pfad — Fallstrick 3) + analoges denied-Audit (Action "accept_invitation").
    Falls die Audit-Schreibung in den Repos eine AuditLogRepository-Abhängigkeit erfordert, die dort noch nicht injiziert ist, injiziere sie über das bestehende Konstruktor-/With…Deps-Muster ODER reiche das Ablehn-Ereignis an die aufrufende Handler-Schicht zur Audit-Schreibung durch — wähle den Weg, der das 450-Zeilen-Limit und die bestehende DI-Konvention (main.go, explizite Konstruktoren) respektiert; dokumentiere die Wahl im SUMMARY.
    Beachte: Memorial-Block darf KEINE Daten löschen; nur ablehnen.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/repository/... -run Claim && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - Plan-00 Claim-Guard-Tests GREEN für BEIDE Pfade.
    - "memorial_not_claimable" + HTTPStatus 409 in member_claims_repository.go UND member_claim_invitations_repository.go nachweisbar.
    - Block-Ereignisse erzeugen ein audit_logs-Ereignis mit Outcome "denied".
    - Fehlermeldung trägt korrekte Umlaute ("geführt").
  </acceptance_criteria>
  <done>Claim-Sperre greift server-seitig in beiden Pfaden mit 409 + denied-Audit; Tests grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/Admin-UI → POST /admin/members/:id/memorial | Privilegierte Schreibaktion; nur Global Admin |
| Browser → Claim-Flows (SubmitClaim/AcceptInvitation) | Registrierter User; darf memorial nicht claimen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-02-EOP | Elevation of Privilege | Memorial-Setter | mitigate | requirePlatformAdminIdentity (Global-Admin-only, D-14); NICHT Gruppen-Capability (Fallstrick 4); Nicht-Admin → 403 |
| T-74-02-BYP | Tampering | Claim-Sperre Umgehung über zweiten Pfad | mitigate | Guard in BEIDEN Pfaden SubmitClaim + AcceptInvitation (Fallstrick 3); server-seitig, nicht nur UI |
| T-74-02-REP | Repudiation | Memorial-Set / Claim-Block ohne Nachweis | mitigate | audit_logs-Eintrag (Actor/Target/Outcome) für allowed + denied (D-15), nicht öffentlich |
| T-74-02-TAMP | Tampering | Account-Status mit-verändert | mitigate | Nur members.profile_status-UPDATE; kein app_user-Account-Write (D-13) |
| T-74-02-SC | Tampering | go installs | accept | Keine neuen Pakete (RESEARCH §Package Legitimacy Audit n/a) |
</threat_model>

<verification>
- `cd backend && go test ./internal/handlers/... -run Memorial && go test ./internal/repository/... -run Claim` grün.
- `cd backend && go build ./...` grün.
- `cd frontend && npm run typecheck` grün (api.ts-Funktion).
- admin-content.yaml Review: Memorial-Endpoint dokumentiert.
</verification>

<success_criteria>
Memorial-Setter Global-Admin-only + auditiert; Claim-Sperre in beiden Pfaden 409 + denied-Audit; app_user-Account unberührt; Contract dokumentiert.
</success_criteria>

<output>
Create `.planning/phases/74-public-member-profile-members-slug-memorial/74-02-SUMMARY.md` when done
</output>
