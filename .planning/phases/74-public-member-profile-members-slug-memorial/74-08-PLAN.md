---
phase: 74-public-member-profile-members-slug-memorial
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/models/member_profile.go
  - backend/internal/repository/member_profile_repository.go
  - shared/contracts/openapi.yaml
  - frontend/src/types/profile.ts
  - frontend/src/app/me/profile/components/MemberProfileHero.tsx
autonomous: true
gap_closure: true
requirements: [C, K, D-01]
must_haves:
  truths:
    - "MemberProfile (/me-DTO) trägt einen aus dem Nickname abgeleiteten slug, konsistent zum public memberSlugExpr (GAP-9)"
    - "Der '/me/profile' Button 'Öffentliches Profil ansehen' verlinkt auf /members/{slug} (nickname-abgeleitet), nicht /members/{member_id} (GAP-9)"
    - "Der Contract (MemberProfile schema + frontend type) ist Lock-K-konsistent um slug erweitert"
  artifacts:
    - path: "backend/internal/models/member_profile.go"
      provides: "MemberProfile.Slug (json:\"slug\")"
      contains: "Slug"
    - path: "frontend/src/app/me/profile/components/MemberProfileHero.tsx"
      provides: "publicProfileHref nutzt profile.slug"
      contains: "profile.slug"
  key_links:
    - from: "MemberProfileHero.tsx (/me)"
      to: "/members/{slug}"
      via: "publicProfileHref = /members/${profile.slug}"
      pattern: "members/\\$\\{profile.slug"
---

<objective>
GAP-9: Der Button „Öffentliches Profil ansehen" auf `/me/profile` verlinkt auf `/members/${profile.member_id}` und erzeugt damit `/members/3` statt der kanonischen Slug-URL `/members/ballelboy`. Die Public-Member-Seite leitet zwar `id::text`-Slugs intern auf, aber die kanonische, nickname-abgeleitete Slug-URL (konsistent mit dem `memberSlugExpr` aus `GetPublicMemberProfile`) ist gefordert.

Fix end-to-end (Lock K): `MemberProfile` (`/me`-DTO) bekommt ein `slug`-Feld, aus dem Nickname abgeleitet mit derselben Slug-Regel wie das Public-Profil; der Frontend-Link nutzt es.

Purpose: Konsistente, lesbare Public-URLs aus dem eigenen Bereich. Self-contained full-stack (Backend + Contract + Type + der eine Consumer-Link) — keine Abhängigkeit zu anderen Gap-Plänen.
Output: erweitertes Modell + Repo-Projektion + OpenAPI-Schema + frontend type + Link-Fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-CONTEXT.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-VERIFICATION.md

<interfaces>
<!-- Aus Codebase extrahiert. -->

Slug-Regel (memberSlugExpr, member_profile_repository.go:382 + anime_contributions_public_repository.go:296):
  LOWER(TRIM(BOTH '-' FROM REGEXP_REPLACE(TRIM(nickname), '[^a-z0-9]+', '-', 'gi')))

MemberProfile struct (models/member_profile.go:62-96): MemberID, AppUserID, DisplayName, FansubName, ... (NEU: Slug string `json:"slug"`)

/me-Profil-Query (member_profile_repository.go:661-754): selektiert bereits m.nickname in baseRow; Slug daraus ableitbar (im SQL via memberSlugExpr ODER in Go aus nickname).

MemberProfileData (frontend/src/types/profile.ts:53-104): member_id, fansub_name, ... (NEU: slug: string)

MemberProfileHero (/me) (frontend/src/app/me/profile/components/MemberProfileHero.tsx:19):
  const publicProfileHref = `/members/${profile.member_id}`  // -> `/members/${profile.slug}`

OpenAPI MemberProfile schema: shared/contracts/openapi.yaml:8521 (required-Liste Z. 8523-8539; properties ab 8540)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — MemberProfile.Slug ableiten + projizieren</name>
  <files>backend/internal/models/member_profile.go, backend/internal/repository/member_profile_repository.go</files>
  <read_first>
    - backend/internal/models/member_profile.go (Z. 62-96 MemberProfile struct)
    - backend/internal/repository/member_profile_repository.go (Z. 661-754 /me-Profil-Query: m.nickname wird bereits selektiert; baseRow-Struct + Scan; Z. 382 memberSlugExpr als Referenz)
  </read_first>
  <action>
    1. `MemberProfile` struct um `Slug string \`json:"slug"\`` erweitern (nach FansubName platzieren, z. B. neben den Identitätsfeldern).
    2. In der /me-Profil-Query (Z. 661-754): den Slug aus dem Nickname ableiten. Bevorzugt in Go (DRY, kein doppeltes SQL): der Scan liest bereits `m.nickname`; eine kleine Helper-Funktion `deriveMemberSlug(nickname string) string` (im repository-Paket) spiegelt memberSlugExpr: lowercase, alle Nicht-`[a-z0-9]`-Sequenzen zu `-`, führende/abschließende `-` trimmen. Slug nach dem Scan in `profile.Slug` setzen. Alternativ als zusätzliche SELECT-Spalte via memberSlugExpr auf `m.nickname` — dann konsistent zum Public-Profil und kein Drift-Risiko. Wähle die SQL-Variante, wenn der Scan ohnehin schon angefasst werden muss; sonst die Go-Helper-Variante. WICHTIG: identisches Ergebnis zum memberSlugExpr (Regex `[^a-z0-9]+` case-insensitive, daher zuerst lowercasen).
    3. Edge-Case: leerer/whitespace-Nickname -> Slug leer; dann Fallback nicht hier erzwingen (Frontend entscheidet, siehe Task 3).
    Keine Migration. Datei <= 450 Zeilen beachten.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go vet ./internal/...</automated>
  </verify>
  <acceptance_criteria>
    - MemberProfile struct hat Slug string mit json:"slug".
    - /me-Profil-Repo setzt Slug aus dem Nickname mit derselben Regel wie memberSlugExpr (lowercase, [^a-z0-9]+ -> '-', getrimmt).
    - `go build ./...` grün.
  </acceptance_criteria>
  <done>Backend liefert profile.slug für /me; konsistent mit Public-Slug.</done>
</task>

<task type="auto">
  <name>Task 2: Contract + frontend type (Lock K) — slug</name>
  <files>shared/contracts/openapi.yaml, frontend/src/types/profile.ts</files>
  <read_first>
    - shared/contracts/openapi.yaml (Z. 8521-8540 MemberProfile schema: required-Liste + properties)
    - frontend/src/types/profile.ts (Z. 53-104 MemberProfileData)
    - backend/internal/models/member_profile.go (autoritativer json-Tag nach Task 1)
  </read_first>
  <action>
    1. `openapi.yaml` MemberProfile-Schema: `slug: { type: string }` zu properties hinzufügen und zur `required`-Liste ergänzen (Backend liefert es immer, ggf. leer-String). Kurze description: "Nickname-derived public profile slug (mirror of the /members/:slug route key)."
    2. `frontend/src/types/profile.ts`: `MemberProfileData` um `slug: string` erweitern (nach fansub_name). Bestehende Felder unverändert. `PublicMemberProfileData` NICHT verändern (Public-Slug kommt dort über die Route, nicht das DTO).
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - openapi.yaml MemberProfile hat slug (property + required).
    - MemberProfileData hat slug: string.
    - `npm run typecheck` grün.
  </acceptance_criteria>
  <done>Contract + frontend type tragen slug konsistent zum Backend.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend — /me/profile Link auf Slug umstellen (GAP-9)</name>
  <files>frontend/src/app/me/profile/components/MemberProfileHero.tsx</files>
  <read_first>
    - frontend/src/app/me/profile/components/MemberProfileHero.tsx (Z. 19 publicProfileHref)
    - frontend/src/types/profile.ts (MemberProfileData.slug nach Task 2)
  </read_first>
  <action>
    1. Z. 19: `const publicProfileHref = \`/members/${profile.slug || profile.member_id}\``. Fallback auf member_id, falls slug leer ist (leerer Nickname) — die Public-Seite löst `id::text` weiterhin auf, also bleibt der Link robust. Button-Text/Umlaute unverändert („Öffentliches Profil ansehen").
    2. Keine weiteren Änderungen; `@/components/ui` Button bleibt.
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - publicProfileHref nutzt profile.slug (mit member_id-Fallback).
    - `npm run typecheck` + `npm run lint` grün.
  </acceptance_criteria>
  <done>„Öffentliches Profil ansehen" verlinkt auf die nickname-abgeleitete Slug-URL (GAP-9 geschlossen).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /me/profile-Link | Nur Navigation; kein Schreibpfad. Slug ist öffentlich abgeleitet, kein Geheimnis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-08-INFO | Information Disclosure | slug im /me-DTO | accept | Slug ist aus dem öffentlichen Nickname abgeleitet und entspricht der bereits öffentlichen /members/:slug-Route; kein zusätzlicher Scope |
| T-74-08-DRIFT | Tampering | Slug-Ableitung weicht vom Public-memberSlugExpr ab → toter Link | mitigate | Dieselbe Regel (lowercase, [^a-z0-9]+ -> '-', getrimmt) wie memberSlugExpr; member_id-Fallback im Frontend hält den Link robust |
| T-74-08-SC | Tampering | npm/go installs | accept | Keine neuen Pakete |
</threat_model>

<verification>
- `cd backend && go build ./...` grün.
- `cd frontend && npm run typecheck && npm run lint` grün.
- Live (Plan 74-11): /me/profile → „Öffentliches Profil ansehen" landet auf /members/ballelboy (nicht /members/3).
</verification>

<success_criteria>
GAP-9 geschlossen: /me/profile verlinkt auf die kanonische, nickname-abgeleitete Slug-URL; slug ist Lock-K-konsistent (Backend + Contract + Type) modelliert.
</success_criteria>

<output>
Create `.planning/phases/74-public-member-profile-members-slug-memorial/74-08-SUMMARY.md` when done
</output>
</content>
