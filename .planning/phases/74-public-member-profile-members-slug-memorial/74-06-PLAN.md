---
phase: 74-public-member-profile-members-slug-memorial
plan: 06
type: execute
wave: 5
depends_on: ["74-02", "74-03", "74-04", "74-05"]
files_modified:
  - frontend/src/components/profile/CorrectionReportModal.tsx
  - frontend/src/components/profile/MemorialSetterAction.tsx
  - frontend/src/app/members/[slug]/page.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.tsx
autonomous: false
requirements: [C, H, J, K, D-12, D-16, D-17, D-18]
must_haves:
  truths:
    - "Registrierte User können vom Public-Profil aus einen Korrektur-Vorschlag einreichen (Modal → POST /me/members/:id/correction) (D-18)"
    - "Der Korrektur-Flow erzeugt nur einen review-gebundenen Vorschlag, keine direkte öffentliche Änderung (D-18)"
    - "Global Admin kann memorial vom Leader-Workspace-Einstieg aus setzen (POST /admin/members/:id/memorial); Aktion nur sichtbar für Global Admin (D-16)"
    - "Beim Versuch, ein memorial-Profil zu claimen, zeigt die UI den verständlichen server-seitigen 409-Hinweis (D-17)"
    - "Alle drei Schreibaktionen sind shippbar im Profil/Workspace (D-12)"
  artifacts:
    - path: "frontend/src/components/profile/CorrectionReportModal.tsx"
      provides: "Korrektur-Modal (Modal/FormField/Button) → submitMemberCorrection; nur registrierte User"
      contains: "submitMemberCorrection"
    - path: "frontend/src/components/profile/MemorialSetterAction.tsx"
      provides: "Global-Admin-only Memorial-Setter-Trigger → setMemberMemorial"
      contains: "setMemberMemorial"
  key_links:
    - from: "CorrectionReportModal.tsx"
      to: "api.ts submitMemberCorrection"
      via: "POST /api/v1/me/members/:id/correction"
      pattern: "submitMemberCorrection"
    - from: "MemorialSetterAction.tsx"
      to: "api.ts setMemberMemorial"
      via: "POST /api/v1/admin/members/:id/memorial"
      pattern: "setMemberMemorial"
---

<objective>
Verdrahte die Frontend-UI der drei Schreibaktionen (D-12): das Korrektur-melden-Modal auf dem Public-Profil (D-18), die Global-Admin-only Memorial-Setter-Aktion am Leader-Workspace-Einstieg (D-16) und den verständlichen UI-Hinweis bei server-seitig abgelehntem Claim gegen ein Memorial-Profil (D-17).

Purpose: Schließt Phase 74 ab — Backend-Endpoints (Plan 02/03) und Layout (Plan 04/05) sind vorhanden; hier entsteht die Bedien-Oberfläche. Enthält einen blocking Human-Verify-Checkpoint, weil die würdevolle Memorial-Gesamtdarstellung und die Write-Flows live geprüft werden müssen (74-VALIDATION §Manual-Only).
Output: 2 neue Komponenten + Einhängung in Public-Profil + Leader-Workspace + 1 Verify-Checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-RESEARCH.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-PATTERNS.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-VALIDATION.md

<interfaces>
<!-- Aus Plan 02/03 (api.ts) + 74-PATTERNS.md -->

api.ts (nach Plan 02/03):
  setMemberMemorial(memberId, token?) → POST /api/v1/admin/members/:id/memorial (Global-Admin)
  submitMemberCorrection(memberId, { targetType, targetId?, reasonText }, token?) → POST /api/v1/me/members/:id/correction (registriert)
  submitMemberClaim (bestehend) → 409 memorial_not_claimable bei Memorial (Plan 02 Guard)

Auth-Gating-Konvention (Lock K): geschützte UI auf hasAccessToken||hasRefreshToken gaten; Global-Admin-Sichtbarkeit über bestehende Capability-/Session-Seam (KEINE Token-/Cookie-Direktzugriffe).

ProposalForm.tsx (Form-Aufbau-Analog) + @/components/ui Modal/FormField/Button/Select.
ClaimManagementPanel.tsx (Leader-Workspace-Einhängepunkt für Memorial-Setter, D-16).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CorrectionReportModal (Public-Profil) + Memorial-Claim-409-Hinweis</name>
  <files>frontend/src/components/profile/CorrectionReportModal.tsx, frontend/src/app/members/[slug]/page.tsx</files>
  <read_first>
    - frontend/src/components/contributions/ProposalForm.tsx (Form-Aufbau-Analog)
    - frontend/src/components/profile/MemberClaimSection.tsx bzw. der bestehende Claim-Einstieg auf dem Public-Profil (für 409-Hinweis-Einhängung, D-17)
    - frontend/src/lib/api.ts (submitMemberCorrection aus Plan 03; submitMemberClaim-Fehlerbehandlung)
    - frontend/src/components/ui/ (Modal, FormField, Select, Button, Textarea — Primitives-Pflicht)
    - frontend/src/app/members/[slug]/page.tsx (Einhängepunkt CorrectionReportModal-Trigger)
    - 74-RESEARCH.md §CorrectionReportModal + §Muster 6 (Claim-Sperre UI-Hinweis)
    - 74-PATTERNS.md §CorrectionReportModal
  </read_first>
  <action>
    1. `CorrectionReportModal.tsx` (NEU, 'use client'): Modal mit `@/components/ui` Modal/FormField/Select/Textarea/Button. Felder: Zielkontext (Select profile/contribution/rolle → target_type, optional target_id aus dem Kontext), Freitext-Begründung (Textarea → reason_text, required). Submit → `submitMemberCorrection` (Plan 03). Nur registrierten Usern anzeigen (auf hasAccessToken||hasRefreshToken gaten — D-18/Decision 6); anonyme Besucher sehen den Trigger nicht. Erfolgs-/Fehler-Feedback; klare Sprache „Dein Vorschlag wird geprüft." (review-gebunden, keine direkte Änderung). Korrekte Umlaute.
    2. Public-Profil-Trigger: dezenten „Korrektur melden"-Button in der passenden Sektion (z. B. neben Contributions/Hero) einbinden, der das Modal öffnet.
    3. Memorial-Claim-409-Hinweis (D-17): Im bestehenden Claim-Einstieg den server-seitigen 409 `memorial_not_claimable` abfangen und den verständlichen deutschen Hinweis anzeigen („Dieses Profil wird als Gedenkprofil geführt und kann nicht beansprucht werden."). UI-Gate ersetzt NICHT die Server-Sperre (die bleibt autoritativ, Plan 02).
    Nur `@/components/ui`; kein ad-hoc-Fetch (Lock K).
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - CorrectionReportModal ruft submitMemberCorrection; nur für eingeloggte User sichtbar.
    - Felder: target_type (Select), reason_text (Textarea, required) — über @/components/ui.
    - Claim-Einstieg zeigt bei 409 memorial_not_claimable den deutschen Gedenk-Hinweis.
    - `npm run typecheck` + `npm run lint` grün (keine nativen Form-Elemente).
  </acceptance_criteria>
  <done>Korrektur-Modal verdrahtet (review-gebunden, nur registriert); Memorial-Claim-409-Hinweis sichtbar.</done>
</task>

<task type="auto">
  <name>Task 2: MemorialSetterAction im Leader Workspace (Global-Admin-only)</name>
  <files>frontend/src/components/profile/MemorialSetterAction.tsx, frontend/src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.tsx</files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.tsx (Einhängepunkt D-16; bestehende Member-/Claim-Panel-Struktur)
    - frontend/src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.module.css
    - frontend/src/lib/api.ts (setMemberMemorial aus Plan 02)
    - frontend/src/components/ui/ (Button, Modal — Confirm-Dialog Primitives)
    - 74-RESEARCH.md §Muster 5 + §Fallstrick 4 (nur Global Admin, nicht Gruppen-Capability) + D-16-Caveat
    - 74-PATTERNS.md §MemorialSetterAction
  </read_first>
  <action>
    1. `MemorialSetterAction.tsx` (NEU, 'use client'): Trigger + Bestätigungs-Modal (`@/components/ui` Modal/Button) → `setMemberMemorial(memberId)` (Plan 02). NUR für Global Admin sichtbar — Sichtbarkeit über die bestehende Session-/Capability-Seam (Global-Admin-Flag), NICHT über Gruppen-Capability (D-16-Caveat/Fallstrick 4). Deutliche Sprache, dass Memorial global wirkt und nicht den Account deaktiviert (D-13). Erfolgs-/Fehler-Feedback (403 falls Backend ablehnt). Korrekte Umlaute.
    2. Einhängung in `ClaimManagementPanel.tsx` (Leader-Workspace-Einstieg D-16) — als zusätzliche Aktion pro Member-Profil. KEIN Vorbau von `/admin/users` (Phase 80, deferred).
    Nur `@/components/ui`; kein ad-hoc-Fetch.
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - MemorialSetterAction ruft setMemberMemorial; nur für Global Admin sichtbar (nicht Gruppen-Capability).
    - Einhängung in ClaimManagementPanel; kein /admin/users-Vorbau.
    - Bestätigungs-Modal + verständliche Sprache (global, kein Account-Eingriff).
    - `npm run typecheck` + `npm run lint` grün.
  </acceptance_criteria>
  <done>Memorial-Setter-Aktion im Leader-Workspace, Global-Admin-only, mit Bestätigung; alle drei Write-Flows shippen (D-12).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-Verify — Memorial-Würdigkeit + alle drei Write-Flows (live :3000)</name>
  <action>Blocking Human-Verify-Checkpoint nach Abschluss von Task 1 und Task 2. Keine Code-Änderung — der Executor pausiert und lässt den Nutzer live verifizieren. Details siehe what-built / how-to-verify.</action>
  <what-built>
    Vollständiges dreistufiges Public-Member-Profil mit Status-Pill, würdevoller Memorial-Hero, Top-N-Badges, clientseitiger Contribution-Filterung, Korrektur-melden-Modal und Global-Admin Memorial-Setter im Leader-Workspace; server-seitige Claim-Sperre gegen Memorial.
  </what-built>
  <how-to-verify>
    Dev-Server :3000 (Hot-Reload, Keycloak redirect_uri nur für 3000 — NICHT Docker :3002):
    1. `/members/[slug]` eines normalen Profils öffnen: Sektions-Reihenfolge Hero → Badges → Geschichte → Beiträge; Sticky-Anker-Nav scrollt/hebt aktive Sektion hervor (Desktop) bzw. Chip-Leiste (Mobil-Viewport). Status-Pill neben Nickname mit Tooltip.
    2. Contributions: Filter (Anime/Gruppe/Rolle/Zeitraum/Status) reduzieren clientseitig; unbestätigte gedämpft + Badge „unbestätigt"; Inline-Expand zeigt Subtypes.
    3. Badges: Top-N prominent + „alle anzeigen".
    4. Als Global Admin im Leader-Workspace `/admin/fansubs/[id]/edit` ein Member-Profil auf memorial setzen → Profil neu laden: würdevolle Memorial-Hero („Dieses Profil wird als historisches Gedenkprofil geführt."), KEINE Aktivitätsmetrik/Mengen-Badges, Contributions bleiben sichtbar.
    5. Als normaler User versuchen, das memorial-Profil zu claimen → verständlicher 409-Hinweis; Claim wird abgelehnt.
    6. Als registrierter User „Korrektur melden" → Vorschlag absenden → Bestätigung „wird geprüft" (keine sofortige öffentliche Änderung).
    Prüfe: keine ASCII-Umlaut-Ersetzungen in user-facing Strings.
  </how-to-verify>
  <verify>
    <human-check>Live-Verifikation auf Dev-Server :3000 gemäß how-to-verify durchgeführt.</human-check>
  </verify>
  <resume-signal>Tippe "approved" oder beschreibe Abweichungen.</resume-signal>
  <done>Nutzer bestätigt würdevolle Memorial-Darstellung, alle drei Write-Flows und korrekte Umlaute live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Korrektur-/Memorial-/Claim-UI | UI-Trigger; Autorität liegt server-seitig (Plan 02/03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-06-EOP | Elevation of Privilege | Memorial-Setter-UI sichtbar für Nicht-Admin | mitigate | Sichtbarkeit nur für Global Admin; Server-Gate (Plan 02) bleibt autoritativ (403) |
| T-74-06-BYP | Tampering | Claim-UI umgeht Memorial-Sperre | mitigate | UI-Hinweis ergänzt nur; Server-409 (Plan 02) ist die echte Sperre (D-17) |
| T-74-06-EOP2 | Elevation of Privilege | Korrektur-Modal als direkte Änderung | mitigate | Ruft nur submitMemberCorrection (review-gebunden, Plan 03); keine öffentliche Mutation |
| T-74-06-SC | Tampering | npm installs | accept | Keine neuen Pakete (RESEARCH §Package Legitimacy Audit n/a) |
</threat_model>

<verification>
- `cd frontend && npm run typecheck && npm run lint` grün.
- Blocking Human-Verify-Checkpoint live auf :3000 (Memorial-Würdigkeit, alle drei Write-Flows, Umlaute).
</verification>

<success_criteria>
Alle drei Schreibaktionen sind über die UI bedienbar (D-12); Memorial-Setter Global-Admin-only (D-16); Korrektur review-gebunden (D-18); Claim-Sperre-Hinweis (D-17); live human-verifiziert.
</success_criteria>

<output>
Create `.planning/phases/74-public-member-profile-members-slug-memorial/74-06-SUMMARY.md` when done
</output>
