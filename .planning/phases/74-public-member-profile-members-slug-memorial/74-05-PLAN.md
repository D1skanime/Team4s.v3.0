---
phase: 74-public-member-profile-members-slug-memorial
plan: 05
type: execute
wave: 4
depends_on: ["74-00", "74-04"]
files_modified:
  - frontend/src/components/profile/MemberBadgeHighlights.tsx
  - frontend/src/components/profile/MemberBadgeChips.tsx
  - frontend/src/components/profile/MemberContributionFilters.tsx
  - frontend/src/components/profile/MemberRoleTimeline.tsx
  - frontend/src/app/members/[slug]/page.tsx
autonomous: true
requirements: [C, "Badges-13", D-06, D-07, D-08, D-10, D-11]
must_haves:
  truths:
    - "Top-N prominente Badges im Hero-Bereich + 'alle anzeigen' für den Rest; Quelle = DTO public_badges, kein UI-Recompute (D-11/Badges-13)"
    - "Bei memorial werden Mengen-/Gamification-Badges unterdrückt (D-10)"
    - "Contributions sind clientseitig filterbar (Anime/Gruppe/Rolle/Zeitraum/Status) via useMemo auf der geladenen role_timeline — kein API-Call (D-06)"
    - "Detail-Subtypes/Notes über Inline-Expand pro Contribution, NICHT als neue Hauptrollen (D-07)"
    - "Unbestätigte/historische Contributions sichtbar aber gedämpft mit Badge 'unbestätigt'; Status-Filter blendet ein/aus (D-08)"
    - "Native <button> in MemberBadgeChips.tsx (Z. 64) auf @/components/ui Button migriert (CLAUDE.md UI-Pflicht)"
  artifacts:
    - path: "frontend/src/components/profile/MemberBadgeHighlights.tsx"
      provides: "Top-N + 'alle anzeigen' Slicing auf public_badges; Memorial-Unterdrückung"
      contains: "public"
    - path: "frontend/src/components/profile/MemberContributionFilters.tsx"
      provides: "Client useMemo-Filter (Anime/Gruppe/Rolle/Zeitraum/Status), @/components/ui Select"
      contains: "useMemo"
    - path: "frontend/src/components/profile/MemberRoleTimeline.tsx"
      provides: "Inline-Expand + gedämpfte unbestätigte Einträge"
      contains: "unbestätigt"
  key_links:
    - from: "MemberContributionFilters.tsx"
      to: "role_timeline (geladen)"
      via: "useMemo-Filterung ohne fetch"
      pattern: "useMemo"
    - from: "MemberBadgeHighlights.tsx"
      to: "public_badges (DTO)"
      via: "Top-N-Slicing, kein State-Recompute"
      pattern: "slice"
---

<objective>
Liefere die kuratierte Badge-Anzeige (Top-N + „alle anzeigen", D-11) und die clientseitige Contribution-Filterung (D-06) mit Inline-Expand (D-07) und gedämpften unbestätigten Einträgen (D-08). Migriere die Altlast — natives `<button>` in `MemberBadgeChips.tsx` — auf `@/components/ui` Button.

Purpose: Vervollständigt Ebene 1 (Badges) und Ebene 3 (filterbare Contributions) des dreistufigen Profils. Hängt sich in die in Plan 04 erzeugten #badges- und #beitraege-Sektions-Platzhalter ein. Quelle bleibt das DTO/Badge-Service (kein UI-Recompute, Lock 13); Filterung bleibt clientseitig (kein Contract-Change, D-06).
Output: 2 neue + 2 erweiterte Komponenten + Verdrahtung in page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-RESEARCH.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-PATTERNS.md
@.planning/phases/74-public-member-profile-members-slug-memorial/74-VALIDATION.md

<interfaces>
<!-- Aus 74-PATTERNS.md (verifiziert) + Plan-01-DTO -->

public_badges: { id: number; badge_code: string; badge_category: string }[] (aus PublicMemberProfileData, Plan 01).
role_timeline: aus getMemberContributions(slug) — trägt has_unverified-Flag (D-08). Einträge mit anime_id/Gruppe/Rolle/Zeit-Feldern für Filterachsen (D-06).

MemberBadgeChips.tsx (heute, Z. 1-83): filtert bereits b.visibility==='public' (Z. 28-31); natives <button> (Z. 64) → MIGRIEREN.
MemberRoleTimeline.tsx (heute): nutzt role_timeline + has_unverified.

Client-Filter-Muster (74-RESEARCH §Muster 4):
  'use client'; const filtered = useMemo(() => roleTimeline.filter(e => (animeFilter==='all'||e.anime_id===animeFilter) && (statusFilter==='all'||matchesStatus(e,statusFilter))), [roleTimeline, animeFilter, statusFilter, ...])
  KEIN fetch; Filter-UI via @/components/ui Select/Button.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contribution-Filter + Timeline-Inline-Expand + gedämpfte unbestätigte</name>
  <behavior>
    - MemberContributionFilters reduziert die role_timeline clientseitig ohne fetch (Plan-00 MemberContributionFilters.test.tsx grün; fetch NICHT aufgerufen).
    - Statusfilter blendet unbestätigte Einträge ein/aus (D-08).
    - Inline-Expand zeigt Detail-Subtypes/Notes pro Contribution (D-07), keine neuen Hauptrollen.
  </behavior>
  <read_first>
    - frontend/src/components/profile/MemberRoleTimeline.tsx (heutiges role_timeline + has_unverified Handling)
    - frontend/src/components/profile/MemberContributionFilters.test.tsx (Plan-00-Stub — Ziel: kein fetch, clientseitige Reduktion)
    - frontend/src/components/ui/ (Select, Button — Filter-UI Primitives-Pflicht)
    - frontend/src/components/contributions/VerifiedBadge.tsx (Badge-„unbestätigt"-Anzeige-Analog)
    - 74-RESEARCH.md §Muster 4 (Client-Filter) + §Anti-Patterns (keine Server-Filterung; kein natives select)
    - 74-PATTERNS.md §MemberRoleTimeline / MemberContributionFilters
  </read_first>
  <action>
    1. `MemberContributionFilters.tsx` (NEU, 'use client'): Filter-UI mit `@/components/ui` Select für Anime/Gruppe/Rolle/Zeitraum/Status; `useMemo` reduziert die per Prop übergebene `role_timeline` rein clientseitig (D-06 — KEIN fetch/API-Call, kein Contract-Change). Status-Achse nutzt `status` + `has_unverified` (D-08). Übergibt die gefilterte Liste an die Timeline.
    2. `MemberRoleTimeline.tsx` (ERWEITERN): Inline-Expand pro Contribution-Zeile für Detail-Subtypes/Notes (Dialog-/Karaoke-Timing, Song-/Dialogübersetzung, Translation Check, Segmentbezüge) — als ausklappbares Detail, NICHT als neue Hauptrolle (D-07). Unbestätigte/historische Einträge optisch gedämpft + Badge „unbestätigt" (D-08). Hauptrollen-Labels bleiben stabil (z. B. „Übersetzung", „Timing").
    Nur `@/components/ui`-Primitives (kein natives `<select>/<button>`); korrekte Umlaute.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run MemberContributionFilters MemberRoleTimeline && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Plan-00 MemberContributionFilters.test.tsx GREEN; fetch nachweislich NICHT aufgerufen.
    - Statusfilter blendet unbestätigte ein/aus; gedämpfte Darstellung + Badge „unbestätigt".
    - Inline-Expand zeigt Subtypes ohne neue Hauptrollen.
    - Filter-UI nutzt @/components/ui Select; keine nativen Form-Elemente.
  </acceptance_criteria>
  <done>Clientseitige Filterung + Inline-Expand + gedämpfte unbestätigte Einträge funktionieren; Plan-00-Filtertest grün.</done>
</task>

<task type="auto">
  <name>Task 2: Badge-Highlights (Top-N + alle anzeigen) + Chips-Altlast-Migration + page-Verdrahtung</name>
  <read_first>
    - frontend/src/components/profile/MemberBadgeChips.tsx (heute Z. 1-83 — visibility-Filter Z. 28-31; natives <button> Z. 64 MIGRIEREN)
    - frontend/src/components/profile/memberBadgeLabels.ts (Kategorie-/Label-Mapping)
    - frontend/src/app/members/[slug]/page.tsx (nach Plan 04 — #badges + #beitraege Sektions-Platzhalter)
    - frontend/src/components/ui/ (Button — Pflicht-Migration des nativen <button>)
    - 74-RESEARCH.md §Public-Badge-Quelle + §Anti-Patterns (kein State-Recompute) + Fallstrick 5
    - 74-PATTERNS.md §MemberBadgeHighlights / MemberBadgeChips
  </read_first>
  <action>
    1. `MemberBadgeHighlights.tsx` (NEU/aus Chips abgeleitet, 'use client'): nimmt `public_badges` (aus DTO, Plan 01); zeigt Top-N (z. B. 3-5, nach Kategorie/Wichtigkeit sortiert) prominent + „alle anzeigen"-Toggle für den Rest (D-11). Reines Slicing auf der bereits server-gefilterten Liste — KEIN State-Recompute (Lock 13). Bei `profile_status==='memorial'` Mengen-/Gamification-Badges unterdrücken (D-10). „alle anzeigen"-Toggle via `@/components/ui` Button.
    2. `MemberBadgeChips.tsx` (ERWEITERN/Altlast): natives `<button>` (Z. 64) → `@/components/ui` Button migrieren (CLAUDE.md UI-Pflicht; ESLint no-restricted-syntax). visibility==='public'-Filter beibehalten.
    3. `page.tsx` (Verdrahtung): #badges-Sektion mit `MemberBadgeHighlights` (public_badges), #beitraege-Sektion mit `MemberContributionFilters` + `MemberRoleTimeline` füllen (Platzhalter aus Plan 04 ersetzen). Empty-States bleiben Platzhalter (D-05).
    Nur `@/components/ui`-Primitives; korrekte Umlaute; ≤450 Zeilen pro Datei; page.tsx bleibt ≤150 Z. Server Component.
  </action>
  <verify>
    <automated>cd frontend && npm run lint && npm run typecheck && npx vitest run MemberProfileHero</automated>
  </verify>
  <acceptance_criteria>
    - MemberBadgeHighlights sliced public_badges (Top-N + alle anzeigen); kein State-Recompute.
    - Bei memorial sind Mengen-/Gamification-Badges unterdrückt.
    - MemberBadgeChips.tsx hat KEIN natives `<button>` mehr (npm run lint ohne no-restricted-syntax-Verstoß für diese Datei).
    - page.tsx #badges + #beitraege gefüllt; bleibt Server Component ≤150 Z.
    - `npm run typecheck` grün.
  </acceptance_criteria>
  <done>Badge-Highlights kuratiert (Top-N/Memorial-Unterdrückung), native-button-Altlast migriert, Sektionen verdrahtet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (Client-Komponenten) → bereits geladene Daten | Keine neuen Server-Calls; reine Client-Transformation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-05-TAMP | Tampering | Badge-State im UI | mitigate | Top-N nur Slicing auf DTO-Liste; kein Recompute (Lock 13); Quelle = Badge-Service via DTO |
| T-74-05-INFO | Information Disclosure | unbestätigte Contributions | accept | Sichtbar aber gedämpft (D-08) — bewusste Produktentscheidung; bereits public über getMemberContributions |
| T-74-05-SC | Tampering | npm installs | accept | Keine neuen Pakete (RESEARCH §Package Legitimacy Audit n/a) |
</threat_model>

<verification>
- `cd frontend && npx vitest run MemberContributionFilters MemberRoleTimeline MemberProfileHero` grün.
- `cd frontend && npm run lint` ohne no-restricted-syntax-Verstoß in MemberBadgeChips.tsx.
- `cd frontend && npm run typecheck` grün.
</verification>

<success_criteria>
Top-N-Badge-Kuration (Memorial-Unterdrückung), clientseitige Contribution-Filterung + Inline-Expand + gedämpfte unbestätigte, native-button-Altlast migriert.
</success_criteria>

<output>
Create `.planning/phases/74-public-member-profile-members-slug-memorial/74-05-SUMMARY.md` when done
</output>
