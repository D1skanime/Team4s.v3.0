---
phase: 149-tote-css-tokens-sanieren-und-den-notiz-kontrast-schlie-en
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/episodes/EpisodesOverview/VersionRow.module.css
  - "frontend/src/app/fansubs/[slug]/page.module.css"
  - frontend/src/components/fansubs/FansubPublicSections.module.css
  - "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.module.css"
  - frontend/src/components/ui/ui.module.css
  - frontend/src/app/admin/roles/RoleCapabilityDetail.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "ROADMAP Phase 149 Success Criterion 1 (partial): none of the 8 fallback-free occurrences of --color-info, --accent-primary-strong, --border-default, --border-soft, --radius reference an undefined token anymore"
    - "ROADMAP Phase 149 Success Criterion 2 (partial): every replacement resolves to the exact same computed value as before, including the --radius -> --radius-sm resolution locked by 149-UI-SPEC.md"
  artifacts:
    - path: "frontend/src/components/episodes/EpisodesOverview/VersionRow.module.css"
      provides: "var(--accent-primary) at the 3 verified fallback-free lines (99,100,101) replacing var(--color-info)"
    - path: "frontend/src/app/admin/roles/RoleCapabilityDetail.tsx"
      provides: "borderRadius: 'var(--radius-sm)' at line 224 replacing the undefined 'var(--radius)'"
  key_links:
    - from: "frontend/src/app/admin/roles/RoleCapabilityDetail.tsx"
      to: "frontend/src/styles/globals.css"
      via: "var(--radius-sm) resolving to 6px"
      pattern: "var\\(--radius-sm\\)"
---

<objective>
Fix 8 fallback-free dead-token references (`--color-info` in `VersionRow.module.css`,
`--accent-primary-strong` in 2 public fansub-page files, `--border-default` in
`ThemeTimeline.module.css`, `--border-soft` in `ui.module.css`, and `--radius` in
`RoleCapabilityDetail.tsx`) by substituting each dead token for its locked replacement per
`149-UI-SPEC.md`. This is Block A, file group 3 of 3 (parallel with Plans 149-01 and 149-02, zero
file overlap) and completes Block A's full 15-file, 39-occurrence scope across the three plans.

Purpose: same defect class as Plans 149-01/02 — undefined custom properties referenced without a
fallback render the browser's inherited/initial value instead of the intended one. The `--radius`
case additionally resolves an ambiguity the ROADMAP left open ("`--radius-md` ist der Kandidat");
`149-UI-SPEC.md` resolved it by comparing the surrounding `role="alert"` box against two
structurally identical boxes elsewhere in the codebase (`MemorialSetterAction.tsx`,
`CorrectionReportModal.tsx`, both hardcoding `borderRadius: 6`) and locked `--radius-sm` (6px) as
the correct replacement, overriding the ROADMAP's tentative guess.

Output: 6 files with dead token references replaced by their locked-mapping replacement token.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-tote-css-tokens-sanieren-und-den-notiz-kontrast-schlie-en/149-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix --color-info in VersionRow.module.css and --accent-primary-strong in the two public fansub page files</name>
  <files>
    frontend/src/components/episodes/EpisodesOverview/VersionRow.module.css
    "frontend/src/app/fansubs/[slug]/page.module.css"
    frontend/src/components/fansubs/FansubPublicSections.module.css
  </files>
  <read_first>
    149-UI-SPEC.md table rows "`--color-info` | `--accent-primary`" and
    "`--accent-primary-strong` | `--accent-deep`" (Color — Part A section) — confirms
    `globals.css:29` defines `--accent-primary: var(--color-primary)` and `globals.css:32` defines
    `--accent-deep: #185fa5`, and lists these exact 5 occurrences (3 + 1 + 1) as the only
    fallback-free ones for these two tokens.
    frontend/src/styles/globals.css:29,32 (replacement tokens' own definitions — do not change).
  </read_first>
  <action>
    In `VersionRow.module.css`, replace `var(--color-info)` → `var(--accent-primary)` at lines 99,
    100, 101 (the `.subtitleSoftsub` info-badge rule: border, background, and color declarations).
    In `app/fansubs/[slug]/page.module.css:259`, replace `var(--accent-primary-strong)` →
    `var(--accent-deep)` (the `.heroLink:hover` rule). In
    `components/fansubs/FansubPublicSections.module.css:860`, replace
    `var(--accent-primary-strong)` → `var(--accent-deep)` (the `.inlineLink:hover` rule).
  </action>
  <acceptance_criteria>
    <automated>grep -n "var(--color-info)" frontend/src/components/episodes/EpisodesOverview/VersionRow.module.css returns empty; grep -c "var(--accent-primary)" same file equals 3</automated>
    <automated>grep -n "var(--accent-primary-strong)" "frontend/src/app/fansubs/[slug]/page.module.css" frontend/src/components/fansubs/FansubPublicSections.module.css returns empty</automated>
    <automated>grep -n "var(--accent-deep)" "frontend/src/app/fansubs/[slug]/page.module.css" | grep -q ":259:"; grep -n "var(--accent-deep)" frontend/src/components/fansubs/FansubPublicSections.module.css | grep -q ":860:"</automated>
  </acceptance_criteria>
  <done>All 3 VersionRow.module.css occurrences reference var(--accent-primary); both accent-primary-strong occurrences reference var(--accent-deep) at their original line numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Fix --border-default, --border-soft, --radius in ThemeTimeline.module.css, ui.module.css, RoleCapabilityDetail.tsx</name>
  <files>
    "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.module.css"
    frontend/src/components/ui/ui.module.css
    frontend/src/app/admin/roles/RoleCapabilityDetail.tsx
  </files>
  <read_first>
    149-UI-SPEC.md table rows "`--border-default` | `--color-border`", "`--border-soft` |
    `--border-subtle`", and the "`--radius` resolution" subsection — confirms `globals.css:12`
    defines `--color-border: #e1e1e6`, `globals.css:138` defines
    `--border-subtle: rgba(45,41,38,0.12)`, `globals.css:113` defines `--radius-sm: 6px`, and the
    reasoning for locking `--radius-sm` (not `--radius-md`) by matching sibling `role="alert"` boxes
    in `MemorialSetterAction.tsx`/`CorrectionReportModal.tsx`.
    frontend/src/styles/globals.css:12,113,138 (replacement tokens' own definitions — do not change).
  </read_first>
  <action>
    In `ThemeTimeline.module.css:58`, replace `var(--border-default)` → `var(--color-border)`. In
    `ui.module.css:643`, replace `var(--border-soft)` → `var(--border-subtle)`. In
    `RoleCapabilityDetail.tsx:224`, replace the inline-style value `'var(--radius)'` →
    `'var(--radius-sm)'` (the `role="alert"` error box's `borderRadius` property) — do not change
    any other property in that style object (padding, border, fontSize).
  </action>
  <acceptance_criteria>
    <automated>grep -n "var(--border-default)" "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ThemeTimeline.module.css" returns empty; grep -n "var(--color-border)" same file shows a match at line 58</automated>
    <automated>grep -n "var(--border-soft)" frontend/src/components/ui/ui.module.css returns empty; grep -n "var(--border-subtle)" same file shows a match at line 643</automated>
    <automated>grep -n "var(--radius)" frontend/src/app/admin/roles/RoleCapabilityDetail.tsx returns empty; grep -n "borderRadius: 'var(--radius-sm)'" same file shows a match at line 224</automated>
  </acceptance_criteria>
  <done>All 3 occurrences reference their locked replacement token at their original line numbers; RoleCapabilityDetail.tsx's alert box renders with the same 6px radius as before via an explicit, defined token.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure CSS/inline-style custom-property name substitution in episode-version, public fansub-page, release-timeline, shared UI, and admin-role presentational code; no input handling, no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-03 | none applicable | VersionRow.module.css / fansubs/[slug]/page.module.css / FansubPublicSections.module.css / ThemeTimeline.module.css / ui.module.css / RoleCapabilityDetail.tsx | accept | Token-name-only substitution with identical resolved value; no behavior, data, or auth logic touched. |
</threat_model>

<verification>
- grep gates above show zero remaining fallback-free references to --color-info, --accent-primary-strong, --border-default, --border-soft, --radius in these 6 files.
- npx tsc --noEmit stays clean for RoleCapabilityDetail.tsx.
</verification>

<success_criteria>
- The 8 fallback-free occurrences covered by this plan's file group are fixed.
- Combined with Plans 149-01 and 149-02, all 39 fallback-free occurrences across all 15 files (per 149-UI-SPEC.md's corrected count) are fixed — completing ROADMAP Success Criterion 1 for Block A.
- No new token introduced; no existing token's stored value changed.
</success_criteria>

<output>
Create `.planning/phases/149-tote-css-tokens-sanieren-und-den-notiz-kontrast-schlie-en/149-03-SUMMARY.md` when done
</output>
