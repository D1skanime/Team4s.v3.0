---
phase: 149-tote-css-tokens-sanieren-und-den-notiz-kontrast-schlie-en
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/admin/users/tabs/UserOverviewTab.tsx
  - frontend/src/app/admin/users/tabs/UserAuditTab.tsx
  - frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx
  - "frontend/src/app/anime/[id]/group/[groupId]/releases/page.module.css"
  - frontend/src/components/groups/GroupEdgeNavigation.module.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "ROADMAP Phase 149 Success Criterion 1 (partial): none of the 16 fallback-free occurrences of --color-text-muted, --color-text, --color-surface, --color-text-tertiary in these 5 files reference an undefined token anymore"
    - "ROADMAP Phase 149 Success Criterion 2 (partial): every replacement in these files resolves to the exact same computed value as before (pure token-name substitution, no value change)"
  artifacts:
    - path: "frontend/src/app/admin/users/tabs/UserOverviewTab.tsx"
      provides: "var(--text-muted) at the two spans that previously read var(--color-text-muted)"
    - path: "frontend/src/app/anime/[id]/group/[groupId]/releases/page.module.css"
      provides: "var(--text-primary)/var(--surface-card)/var(--text-faint) at the 10 verified fallback-free lines"
  key_links:
    - from: "frontend/src/app/admin/users/tabs/UserOverviewTab.tsx"
      to: "frontend/src/styles/globals.css"
      via: "var(--text-muted) resolving to --color-text-secondary"
      pattern: "var\\(--text-muted\\)"
---

<objective>
Fix 16 fallback-free dead-token references (`--color-text-muted`, `--color-text`, `--color-surface`,
`--color-text-tertiary`) across 5 files by substituting each dead token name for its
already-defined replacement, per the locked mapping in `149-UI-SPEC.md`. This is Block A, file
group 1 of 3 (the other two groups are Plans 149-02 and 149-03, running in parallel — zero file
overlap between the three plans).

Purpose: these declarations are currently invalid-at-computed-value-time (the referenced custom
property does not exist anywhere and there is no fallback), so the browser renders the inherited/
initial color instead of the intended one. Renaming to the existing equivalent token makes the
declaration valid again with the exact same intended color.

Output: 5 files with dead token references replaced by their locked-mapping replacement token. No
new token introduced, no existing token's stored value changed, no fallback-protected line touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-tote-css-tokens-sanieren-und-den-notiz-kontrast-schlie-en/149-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix --color-text-muted in the three admin user tabs</name>
  <files>
    frontend/src/app/admin/users/tabs/UserOverviewTab.tsx
    frontend/src/app/admin/users/tabs/UserAuditTab.tsx
    frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx
  </files>
  <read_first>
    149-UI-SPEC.md table row "`--color-text-muted` | `--text-muted`" (Color — Part A section) —
    confirms `globals.css:24` already defines `--text-muted: var(--color-text-secondary)` and lists
    these exact 4 occurrences as the only fallback-free ones for this token.
    frontend/src/styles/globals.css:24 (the replacement token's own definition — do not change it).
  </read_first>
  <action>
    In each file, replace the inline-style string `'var(--color-text-muted)'` with
    `'var(--text-muted)'` — nothing else in the surrounding style object changes:
    - `UserOverviewTab.tsx:105` — `<span style={{ color: 'var(--color-text-muted)', fontSize: '0.85rem' }}>` → `color: 'var(--text-muted)'`
    - `UserOverviewTab.tsx:118` — `<p style={{ margin: 0, fontSize: '0.8rem', color: 'var(--color-text-muted)' }}>` → `color: 'var(--text-muted)'`
    - `UserAuditTab.tsx:96` — `<TableCell style={{ fontSize: '0.85rem', color: 'var(--color-text-muted)' }}>` → `color: 'var(--text-muted)'`
    - `UserGroupRightsTab.tsx:289` — `<span style={{ color: 'var(--color-text-muted)', marginLeft: 'var(--space-2)' }}>` → `color: 'var(--text-muted)'`
    Do not touch any other inline style, prop, or JSX in these files.
  </action>
  <acceptance_criteria>
    <automated>grep -rn "color-text-muted" frontend/src/app/admin/users/tabs/UserOverviewTab.tsx frontend/src/app/admin/users/tabs/UserAuditTab.tsx frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx returns empty (zero matches)</automated>
    <automated>grep -c "var(--text-muted)" frontend/src/app/admin/users/tabs/UserOverviewTab.tsx equals 2; same grep on UserAuditTab.tsx equals 1; on UserGroupRightsTab.tsx equals 1</automated>
  </acceptance_criteria>
  <done>All 4 occurrences across the 3 files reference var(--text-muted) instead of the undefined var(--color-text-muted); no other line in any of the 3 files changed.</done>
</task>

<task type="auto">
  <name>Task 2: Fix --color-text, --color-surface, --color-text-tertiary in releases/page.module.css and GroupEdgeNavigation.module.css</name>
  <files>
    "frontend/src/app/anime/[id]/group/[groupId]/releases/page.module.css"
    frontend/src/components/groups/GroupEdgeNavigation.module.css
  </files>
  <read_first>
    149-UI-SPEC.md table rows for `--color-text`→`--text-primary`, `--color-surface`→`--surface-card`,
    `--color-text-tertiary`→`--text-faint` (Color — Part A section) — confirms exact line numbers
    and explicitly warns that `page.module.css:37` and `:93` (`var(--color-surface, #f9f9f9)`) are
    fallback-protected and MUST NOT be touched.
    frontend/src/styles/globals.css:22,129,136 (replacement tokens' own definitions — do not change).
  </read_first>
  <action>
    In `releases/page.module.css`, replace only the following fallback-free occurrences (leave every
    other line, including the fallback-protected `var(--color-surface, #f9f9f9)` at lines 37 and 93
    and every `--color-text-secondary`/`--color-text-primary` occurrence, untouched):
    - Lines 154, 182, 286: `var(--color-text)` → `var(--text-primary)`
    - Lines 243, 322, 353, 381: `var(--color-surface)` → `var(--surface-card)`
    - Lines 271, 354, 400: `var(--color-text-tertiary)` → `var(--text-faint)`
    In `GroupEdgeNavigation.module.css`, replace:
    - Lines 80, 87: `var(--color-surface)` → `var(--surface-card)`
  </action>
  <acceptance_criteria>
    <automated>grep -n "var(--color-text)\|var(--color-surface)\b\|var(--color-text-tertiary)" "frontend/src/app/anime/[id]/group/[groupId]/releases/page.module.css" | grep -v ", #f9f9f9)" returns empty</automated>
    <automated>grep -c "var(--color-surface, #f9f9f9)" "frontend/src/app/anime/[id]/group/[groupId]/releases/page.module.css" still equals 2 (fallback-protected lines 37/93 unchanged)</automated>
    <automated>grep -n "var(--color-surface)" frontend/src/components/groups/GroupEdgeNavigation.module.css returns empty; grep -c "var(--surface-card)" frontend/src/components/groups/GroupEdgeNavigation.module.css equals 2</automated>
  </acceptance_criteria>
  <done>All 10 fallback-free occurrences in releases/page.module.css and both occurrences in GroupEdgeNavigation.module.css now reference their locked replacement token; the two fallback-protected --color-surface lines in releases/page.module.css are byte-identical to before.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure CSS/inline-style custom-property name substitution in admin-only and public releases-page presentational code; no input handling, no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-01 | none applicable | UserOverviewTab.tsx / UserAuditTab.tsx / UserGroupRightsTab.tsx / releases/page.module.css / GroupEdgeNavigation.module.css | accept | Token-name-only substitution with identical resolved value; no behavior, data, or auth logic touched. |
</threat_model>

<verification>
- grep gates above show zero remaining fallback-free references to the 3 dead tokens in these 5 files.
- npx tsc --noEmit stays clean for the 3 touched .tsx files (no type errors introduced by the string literal change).
</verification>

<success_criteria>
- The 16 fallback-free occurrences of `--color-text-muted`, `--color-text`, `--color-surface`, `--color-text-tertiary` covered by this plan's file group are fixed.
- No fallback-protected occurrence in these 5 files was touched.
- No new token introduced; no existing token's stored value changed.
</success_criteria>

<output>
Create `.planning/phases/149-tote-css-tokens-sanieren-und-den-notiz-kontrast-schlie-en/149-01-SUMMARY.md` when done
</output>
