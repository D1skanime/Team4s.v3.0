---
phase: 149-tote-css-tokens-sanieren-und-den-notiz-kontrast-schlie-en
plan: 06
type: execute
wave: 3
depends_on: ["149-01", "149-02", "149-03", "149-04", "149-05"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "ROADMAP Phase 149 Success Criterion 7 (part 1): frontend typecheck/lint/test all run green with zero new failures versus the pre-phase baseline"
    - "ROADMAP Phase 149 Success Criterion 7 (part 2): a live UAT on :3000 (via the 127.0.0.1:3300 tunnel) shows, via getComputedStyle, an actually resolved (non-empty) color for --color-text-muted, --color-text, and --surface-muted at real DOM nodes, plus a visibly lighter PublicNoteCard header band"
  artifacts: []
  key_links: []
---

<objective>
Close Phase 149 with the same full regression sweep + live UAT gate pattern established by Phase
148 Plan 07 (`148-07-PLAN.md`): run the frontend automation suite across all five prior plans'
combined changes, confirm zero new failures versus the pre-phase baseline, then verify live on
`:3000` that the dead-token renames now actually resolve to real colors and the PublicNoteCard
band is visibly lighter — measured via `getComputedStyle`, satisfying ROADMAP Phase 149 Success
Criterion 7 exactly as written.

Purpose: each of Plans 149-01 through 149-05 was verified in isolation (grep-level and unit-test
proofs); this plan is the integration/verification gate proving they compose correctly on `main`
and that the phase's observable goal (dead tokens render real colors again, a guard prevents
recurrence, the note card clears contrast) is true in a running instance, not just in unit tests.

Output: a documented regression sweep result table (mirroring `148-07-SUMMARY.md`'s format) and a
live UAT sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-tote-css-tokens-sanieren-und-den-notiz-kontrast-schlie-en/149-UI-SPEC.md
@.planning/phases/148-rollenfarben-wieder-an-den-katalog-anschlie-en/148-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated regression sweep</name>
  <files></files>
  <read_first>
    .planning/phases/148-rollenfarben-wieder-an-den-katalog-anschlie-en/148-07-SUMMARY.md (the exact
    command set and baseline-comparison method to replicate for the frontend side: npx tsc
    --noEmit, npx vitest run, npx eslint src, docker compose build for production-build parity).
    .planning/phases/149-tote-css-tokens-sanieren-und-den-notiz-kontrast-schlie-en/149-01-SUMMARY.md,
    149-02-SUMMARY.md, 149-03-SUMMARY.md, 149-04-SUMMARY.md, 149-05-SUMMARY.md (confirms all five
    prior plans landed and what each changed).
  </read_first>
  <action>
    On `team4s-linux`, with the current tip of `main` (all five prior plans landed): this phase is
    frontend-only (no backend Go file was touched by any of Plans 149-01 through 149-05), so the
    backend regression sweep is not required — confirm this by running `git diff --stat
    &lt;pre-phase-commit&gt;..HEAD -- backend/` and verifying it is empty. Run `npx tsc --noEmit`,
    `npx vitest run` (the full frontend suite, including the new
    `cssCustomProperties.guard.test.ts` and the updated `roleCatalog.accessibility.test.ts`), and
    `npx eslint src` in the frontend container/workspace, comparing the failing/error set against
    the pre-Phase-149 baseline (a `git archive` checkout of the commit immediately preceding Plan
    149-01's first commit, exactly as Phase 148's Plan 07 did) — confirm the failing set is
    identical (zero new failures introduced by this phase). Run `docker compose build
    team4sv30-frontend` to confirm the production build succeeds. Re-confirm via grep, repo-wide,
    that zero remaining fallback-free references exist to any of the 12 fixed dead tokens
    (`--color-text-muted`, `--color-text`, `--color-surface`, `--surface-muted`, `--accent`,
    `--color-text-tertiary`, `--color-info`, `--success`, `--accent-primary-strong`,
    `--border-default`, `--border-soft`, `--radius`) outside of a `var(--token, fallback)`
    construction, and that `PublicNoteCard.module.css:25` reads exactly `45%`.
  </action>
  <acceptance_criteria>
    <automated>git diff --stat &lt;pre-phase-commit&gt;..HEAD -- backend/ is empty (frontend-only phase confirmed)</automated>
    <automated>npx tsc --noEmit exits 0</automated>
    <automated>npx vitest run reports zero new failures versus the documented pre-Phase-149 baseline, and the new cssCustomProperties.guard.test.ts plus the updated roleCatalog.accessibility.test.ts both report fully green</automated>
    <automated>npx eslint src reports zero new errors/warnings versus baseline</automated>
    <automated>grep -n "color-mix(in srgb, var(--role-accent) 45%, var(--color-border))" frontend/src/components/public/PublicNoteCard.module.css shows exactly one match</automated>
  </acceptance_criteria>
  <done>Frontend typecheck/test/lint/build all confirm zero new failures versus the pre-phase baseline, the backend is confirmed untouched, and the grep gate confirms the PublicNoteCard band and all 12 dead-token fixes are in place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live UAT sign-off on :3000</name>
  <files></files>
  <action>Present the automated regression sweep results from Task 1, then walk through the four live-UAT checks below via the 127.0.0.1:3300 tunnel and wait for explicit human sign-off before the phase is considered complete.</action>
  <what-built>
    12 previously-dead CSS custom-property references across 15 files now resolve to their
    already-defined replacement token with an identical intended value; a new automated guard test
    (`cssCustomProperties.guard.test.ts`) fails on any future reintroduction of this defect class;
    the public note card's role-tinted header band mix is lowered from 55% to 45%, clearing WCAG AA
    4.5:1 text contrast for all 15 catalog colors, and the corresponding test now enforces this as
    a real threshold instead of a known-gap snapshot.
  </what-built>
  <how-to-verify>
    Via the `127.0.0.1:3300` SSH tunnel to `team4s-linux` (`http://192.168.235.196:3000` is the
    Linux frontend itself, per `CLAUDE.md`'s canonical dev environment):
    1. Open `/admin/users/&lt;any-user-id&gt;` and switch to the Overview tab. Open DevTools, select
       the small muted-text element rendered by `UserOverviewTab.tsx` (the "zuletzt aktiv"-style
       secondary text), and run
       `getComputedStyle(el).getPropertyValue('color')` in the console — confirm it returns a real,
       non-empty RGB color (previously this rendered inherited/black-ish text with the dead
       property doing nothing).
    2. Open an anime release page under
       `/anime/&lt;id&gt;/group/&lt;groupId&gt;/releases` and select any element that previously read
       `var(--color-text)` (e.g. a release-row label). Confirm
       `getComputedStyle(el).getPropertyValue('color')` returns the same dark text color used
       elsewhere on the page (matching `--text-primary`/`#1c1c1e`), not an empty string.
    3. Open `/me/projects/&lt;animeId&gt;/group/&lt;fansubGroupId&gt;` (or
       `/admin/fansubs/&lt;id&gt;/edit`'s media-review drawer) and select a card/row element that uses
       `var(--surface-muted)`. Confirm `getComputedStyle(el).getPropertyValue('background-color')`
       resolves to the off-white `--surface-card-muted` value (`#fbfaf8`), not an empty string or
       the page's base background.
    4. Open a release-detail page with public notes (e.g. the same fixture used in Phase 148's
       UAT) and find a note whose role color is on the darker end of the catalog (e.g. `#183b7c`
       or `#7b3c4e`). Confirm the header band is visibly lighter than before Phase 149 (45% vs the
       old 55% band mix) while the role-name text inside it remains clearly legible.
  </how-to-verify>
  <resume-signal>Type "approved" once all four checks render/behave as described, or describe which check failed and its observed vs. expected value.</resume-signal>
  <done>Human verifier has typed "approved" for all four live checks; ROADMAP Phase 149 Success Criterion 7 is confirmed true against the running system.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Verification-only plan; no code changes, no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-06 | none applicable | regression/UAT gate | accept | This plan performs no code changes; it only runs existing automation and a human visual/DOM check against already-reviewed changes from Plans 149-01 through 149-05. |
</threat_model>

<verification>
- Full frontend regression sweep (typecheck/test/lint/build) green with zero new failures versus the documented pre-phase baseline.
- Confirmed the backend was untouched by this phase.
- Live UAT sign-off across all four checks above, approved by the human verifier.
</verification>

<success_criteria>
- ROADMAP Phase 149 Success Criteria 1-7 are all independently confirmed true against the live running system (not just unit tests).
- Zero new frontend test/lint/build failures versus the pre-Phase-149 baseline.
</success_criteria>

<output>
Create `.planning/phases/149-tote-css-tokens-sanieren-und-den-notiz-kontrast-schlie-en/149-06-SUMMARY.md` when done
</output>
