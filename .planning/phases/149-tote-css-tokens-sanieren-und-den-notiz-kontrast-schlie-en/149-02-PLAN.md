---
phase: 149-tote-css-tokens-sanieren-und-den-notiz-kontrast-schlie-en
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/project.module.css"
  - "frontend/src/app/admin/fansubs/[id]/edit/GroupMediaReviewSection.module.css"
  - "frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css"
  - frontend/src/components/navigation/Breadcrumbs.module.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "ROADMAP Phase 149 Success Criterion 1 (partial): none of the 15 fallback-free occurrences of --surface-muted, --accent, --success, and the Breadcrumbs two-level dead reference reference an undefined token anymore"
    - "ROADMAP Phase 149 Success Criterion 2 (partial): every replacement resolves to the exact same computed value as before; the Breadcrumbs fix keeps --breadcrumb-separator-color as the outer customization seam and only repairs the inner reference"
  artifacts:
    - path: "frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css"
      provides: "var(--accent-primary) and var(--color-success) at the 7 verified fallback-free lines (501,502,523,524,525,531,533)"
    - path: "frontend/src/components/navigation/Breadcrumbs.module.css"
      provides: "color: var(--breadcrumb-separator-color, var(--text-faint)); at line 41"
  key_links:
    - from: "frontend/src/app/admin/fansubs/[id]/edit/GroupMediaReviewSection.module.css"
      to: "frontend/src/styles/globals.css"
      via: "var(--surface-card-muted) resolving to #fbfaf8"
      pattern: "var\\(--surface-card-muted\\)"
---

<objective>
Fix 15 fallback-free dead-token references (`--surface-muted` across 2 files, `--accent` and
`--success` in `FansubEdit.module.css`, and the two-level dead reference in
`Breadcrumbs.module.css`) by substituting each dead token for its locked replacement per
`149-UI-SPEC.md`. This is Block A, file group 2 of 3 (parallel with Plans 149-01 and 149-03, zero
file overlap).

Purpose: same defect class as Plan 149-01 — undefined custom properties referenced without a
fallback render the browser's inherited/initial color instead of the intended one. The
`Breadcrumbs.module.css:41` case is the special two-level variant: its fallback is itself a `var()`
pointing at another undefined token, which per CSS `var()` substitution rules does not rescue the
declaration — it stays invalid despite superficially "having a fallback."

Output: 4 files with dead token references replaced by their locked-mapping replacement token,
including the Breadcrumbs inner-reference fix that preserves the outer customization seam.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-tote-css-tokens-sanieren-und-den-notiz-kontrast-schlie-en/149-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix --surface-muted in project.module.css and GroupMediaReviewSection.module.css</name>
  <files>
    "frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/project.module.css"
    "frontend/src/app/admin/fansubs/[id]/edit/GroupMediaReviewSection.module.css"
  </files>
  <read_first>
    149-UI-SPEC.md table row "`--surface-muted` | `--surface-card-muted`" (Color — Part A section) —
    confirms `globals.css:130` already defines `--surface-card-muted: #fbfaf8` and lists these exact
    7 occurrences (2 + 5) as the only fallback-free ones for this token.
    frontend/src/styles/globals.css:130 (replacement token's own definition — do not change it).
  </read_first>
  <action>
    In `project.module.css`, replace `var(--surface-muted)` → `var(--surface-card-muted)` at lines
    58 and 91. In `GroupMediaReviewSection.module.css`, replace `var(--surface-muted)` →
    `var(--surface-card-muted)` at lines 39, 187, 280, 379, 423. Line 39 is inside
    `background: color-mix(in srgb, var(--surface-muted) 70%, transparent);` — rename only the inner
    `var()` token name; the `color-mix()` wrapper and the 70% percentage stay unchanged.
  </action>
  <acceptance_criteria>
    <automated>grep -n "var(--surface-muted)" "frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/project.module.css" returns empty; grep -c "var(--surface-card-muted)" same file equals 2</automated>
    <automated>grep -n "var(--surface-muted)" "frontend/src/app/admin/fansubs/[id]/edit/GroupMediaReviewSection.module.css" returns empty; grep -c "var(--surface-card-muted)" same file equals 5</automated>
    <automated>grep -n "color-mix(in srgb, var(--surface-card-muted) 70%, transparent)" "frontend/src/app/admin/fansubs/[id]/edit/GroupMediaReviewSection.module.css" returns exactly one match at line 39 (percentage/wrapper unchanged)</automated>
  </acceptance_criteria>
  <done>All 7 occurrences across both files reference var(--surface-card-muted) instead of the undefined var(--surface-muted); the color-mix() percentage at line 39 of GroupMediaReviewSection.module.css is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Fix --accent, --success in FansubEdit.module.css and the Breadcrumbs two-level dead reference</name>
  <files>
    "frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css"
    frontend/src/components/navigation/Breadcrumbs.module.css
  </files>
  <read_first>
    149-UI-SPEC.md table rows "`--accent` | `--accent-primary`" and "`--success` | `--color-success`"
    (Color — Part A section) — confirms `globals.css:29` defines `--accent-primary: var(--color-primary)`
    and `globals.css:13` defines `--color-success: #28a745`, and explicitly notes these 7 FansubEdit
    lines are distinct from the `.fansubEditMemberRoleToggle*` rules Phase 148 owns (no overlap).
    149-UI-SPEC.md section "`--color-text-tertiary` special case: Breadcrumbs.module.css:41" — the
    exact before/after for the two-level dead reference and why only the inner var() is replaced.
  </read_first>
  <action>
    In `FansubEdit.module.css`, replace `var(--accent)` → `var(--accent-primary)` at lines 501, 502,
    523, 524, 525 (the `.fansubEditCandidateResult:hover`/`:focus-visible` and
    `.fansubEditCandidateResultSelected` rules), and replace `var(--success)` →
    `var(--color-success)` at lines 531, 533 (the `.fansubEditCandidateSelection` rule). Do not
    touch any `.fansubEditMemberRoleToggle*` rule in this file — those belong to Phase 148 and are
    out of scope.
    In `Breadcrumbs.module.css:41`, change exactly:
    `color: var(--breadcrumb-separator-color, var(--color-text-tertiary));`
    to:
    `color: var(--breadcrumb-separator-color, var(--text-faint));`
    Keep `--breadcrumb-separator-color` as the outer token unchanged — only the inner fallback
    target is replaced.
  </action>
  <acceptance_criteria>
    <automated>grep -n "var(--accent)\|var(--success)" "frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css" returns empty</automated>
    <automated>grep -c "var(--accent-primary)" "frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css" is greater than or equal to 5 (the 5 renamed occurrences); grep -c "var(--color-success)" same file equals 2</automated>
    <automated>grep -n "fansubEditMemberRoleToggle" "frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css" — output byte-identical to pre-change (untouched)</automated>
    <automated>grep -n "color: var(--breadcrumb-separator-color, var(--text-faint));" frontend/src/components/navigation/Breadcrumbs.module.css returns exactly one match at line 41</automated>
    <automated>grep -c "breadcrumb-separator-color" frontend/src/components/navigation/Breadcrumbs.module.css equals 1 (outer token preserved, not removed)</automated>
  </acceptance_criteria>
  <done>FansubEdit.module.css's 7 dead references (5 --accent, 2 --success) now use the replacement tokens with the .fansubEditMemberRoleToggle* rules untouched; Breadcrumbs.module.css:41's inner dead reference now points at --text-faint while --breadcrumb-separator-color remains the outer customization seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure CSS custom-property name substitution in admin-only fansub-edit and shared navigation presentational code; no input handling, no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-02 | none applicable | project.module.css / GroupMediaReviewSection.module.css / FansubEdit.module.css / Breadcrumbs.module.css | accept | Token-name-only substitution with identical resolved value; no behavior, data, or auth logic touched. |
</threat_model>

<verification>
- grep gates above show zero remaining fallback-free references to --surface-muted, --accent, --success in these 4 files.
- Breadcrumbs.module.css's outer --breadcrumb-separator-color seam is preserved (still present, still the first fallback argument).
</verification>

<success_criteria>
- The 15 fallback-free occurrences covered by this plan's file group are fixed.
- The Breadcrumbs.module.css two-level dead reference is closed while preserving the outer customization seam.
- No new token introduced; no existing token's stored value changed.
</success_criteria>

<output>
Create `.planning/phases/149-tote-css-tokens-sanieren-und-den-notiz-kontrast-schlie-en/149-02-SUMMARY.md` when done
</output>
