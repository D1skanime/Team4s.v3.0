---
phase: 117-kara-segment-zeit-override-anzeige
plan: 02
type: execute
wave: 2
depends_on: ["117-01"]
files_modified:
  - backend/internal/testsupport/phase117_postgres.go
  - backend/internal/testsupport/phase117_postgres_test.go
  - backend/internal/models/admin_anime_themes.go
  - backend/internal/repository/theme_segment_assignments.go
  - backend/internal/repository/theme_segment_overrides.go
  - backend/internal/repository/theme_segment_assignments_integration_test.go
autonomous: true
requirements: [D-01, D-03]

must_haves:
  truths:
    - "Ein Kara-Segment kann per Repository-CRUD mehreren release_versions zugewiesen werden (D-03)"
    - "Ein Override fuer eine nicht zugewiesene Release-Version wird abgelehnt, nicht still erzeugt (D-01, D-03 Datenintegritaet)"
    - "Assignment- und Override-CRUD sind gegen eine echte isolierte PostgreSQL-Instanz integrationsgetestet, nicht nur String-Pattern-geprueft (VALIDATION.md Wave 0)"
  artifacts:
    - path: "backend/internal/testsupport/phase117_postgres.go"
      provides: "OpenPhase117Postgres(t) -- isolierte Schema-Fixture mit theme_segment_assignments/theme_segment_episode_overrides/theme_segment_render_cache angewendet"
    - path: "backend/internal/repository/theme_segment_assignments.go"
      provides: "AssignThemeSegmentToReleaseVersion / UnassignThemeSegmentFromReleaseVersion / ListThemeSegmentAssignments"
    - path: "backend/internal/repository/theme_segment_overrides.go"
      provides: "UpsertThemeSegmentEpisodeOverride / DeleteThemeSegmentEpisodeOverride / GetThemeSegmentEpisodeOverride / ListThemeSegmentEpisodeOverrides"
  key_links:
    - from: "theme_segment_overrides.go UpsertThemeSegmentEpisodeOverride"
      to: "theme_segment_episode_overrides.fk_theme_segment_episode_override_assignment"
      via: "Postgres FK-Fehlercode 23503 -> repository.ErrConflict"
      pattern: "pgErr.Code == \"23503\""
---

<objective>
Baut die Wave-0-DB-Testinfrastruktur fuer Phase 117 (analog `testsupport.OpenPhase107Postgres`) und
die additive Repository-Schicht fuer geteilte Kara-Zuweisung + Per-Version-Zeit-Override auf dem in
Plan 117-01 geschaffenen Schema. Beruehrt NICHT die bestehende Resolution-Logik
(`resolved_variant`/`ListAnimeSegments`) -- das ist Plan 117-03.

Purpose: VALIDATION.md fordert explizit echte DB-Integrationstests statt der heutigen
String-Pattern-Tests (`segment_playback_resolution_test.go`). Diese Wave-0-Infrastruktur ist
Voraussetzung fuer alle folgenden Backend-Wellen (117-03, 117-04, 117-05).
Output: `OpenPhase117Postgres(t)`-Testfixture, zwei neue fokussierte Repository-Dateien, erweiterte
Go-Modelle, ein gruener Integrationstest-Satz.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-kara-segment-zeit-override-anzeige/117-RESEARCH.md
@.planning/phases/117-kara-segment-zeit-override-anzeige/117-PATTERNS.md
@backend/internal/testsupport/phase107_postgres.go
@backend/internal/repository/theme_segment_render_cache.go
@backend/internal/models/admin_anime_themes.go
</context>

<interfaces>
Aus `backend/internal/testsupport/phase106_postgres.go` (paketweit wiederverwendbar, KEINE
Kopie noetig):
`func openPhasePostgres(t *testing.T, dsnEnv string, databasePattern *regexp.Regexp, schemaPrefix
string, schemaPattern *regexp.Regexp, createPrerequisites func(testing.TB, *pgxpool.Pool))
*pgxpool.Pool` -- generischer Fixture-Aufbau (Skip wenn DSN-Env fehlt, isoliertes Schema,
`search_path`-Guard, `t.Cleanup`).
`func ApplySQLFile(t testing.TB, pool *pgxpool.Pool, path string)` -- wendet eine echte
`.up.sql`-Migrationsdatei nach `public`-Ziel-Guard an.

Aus `backend/internal/repository/theme_segment_render_cache.go` (Analog fuer neue Repository-Dateien):
Spalten-Konstante + `scan*(row pgx.Row)`-Funktion + `validate*Input(input) error` +
`INSERT ... ON CONFLICT ... RETURNING`-Muster (siehe `UpsertThemeSegmentRenderCacheQueued`,
Zeilen 41-95) + `Exec`+`RowsAffected()`-Delete-Muster (Zeilen 181-194).

Aus `backend/internal/models/admin_anime_themes.go`: `AdminThemeSegment`-Struct (Zeilen 34-77) --
wird um Zuweisungs-/Override-Uebersichtsfelder erweitert, keine bestehenden Felder werden entfernt.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0-Testfixture OpenPhase117Postgres</name>
  <files>backend/internal/testsupport/phase117_postgres.go, backend/internal/testsupport/phase117_postgres_test.go</files>
  <read_first>
    backend/internal/testsupport/phase107_postgres.go (vollstaendiges Analog: DSN-Env-Name-Muster
    `TEAM4S_PHASE<N>_TEST_DSN`, Datenbank-/Schema-Regex `^team4s_phase<N>_test_[a-z0-9]+$` /
    `^phase<N>_[a-z0-9_]+$`, `createPhase107Prerequisites` als Vorbild fuer minimale
    Stub-FK-Eltern-Tabellen + `ApplySQLFile`-Aufrufe fuer echte Migrationsdateien);
    backend/internal/testsupport/phase107_postgres_test.go (vollstaendiges Analog fuer die
    Guard-Tests: DSN-Isolation, Datenbank-/Schema-Namens-Validierung);
    database/migrations/0049_extend_theme_segments.up.sql (Zeilen 5-17, fuer die
    theme_segments-Basisspalten VOR 0049: `id`, `theme_id`, `start_episode_id`, `end_episode_id`,
    `created_at` -- das ist der minimale Stub, den 0049 danach per ALTER umbaut);
    diesen Plan 117-01 (Migrationsdateien 0141/0142/0143 -- werden hier per `ApplySQLFile`
    angewendet, nicht neu geschrieben).
  </read_first>
  <behavior>
    - `OpenPhase117Postgres(t)` mit `TEAM4S_PHASE117_TEST_DSN` unset -> `t.Skip` (kein Fehlschlag),
      analog `TestPhase107DSNSelectionIgnoresDatabaseURL`.
    - `validatePhase117DatabaseName("team4s_phase117_test_a")` -> nil; `validatePhase117DatabaseName("team4s_v2")` -> Fehler.
    - `validatePhase117SchemaName("phase117_abc123")` -> nil; `validatePhase117SchemaName("public")` -> Fehler.
    - Isolation: ein mit `TEAM4S_PHASE106_TEST_DSN` gesetztes DSN darf `OpenPhase117Postgres`
      NICHT oeffnen (eigener Env-Name, analog `TestPhase107DSNIsolationBetweenPhases`).
  </behavior>
  <action>
    Erzeuge `backend/internal/testsupport/phase117_postgres.go` im Paket `testsupport`. Definiere
    `const phase117DSNEnv = "TEAM4S_PHASE117_TEST_DSN"`,
    `phase117DatabasePattern = regexp.MustCompile("^team4s_phase117_test_[a-z0-9]+$")`,
    `phase117SchemaPattern = regexp.MustCompile("^phase117_[a-z0-9_]+$")`. Implementiere
    `func OpenPhase117Postgres(t *testing.T) *pgxpool.Pool` als duennen Wrapper um das bereits im
    Paket vorhandene `openPhasePostgres(...)` (KEIN Duplikat der Isolationslogik), mit
    `createPhase117Prerequisites` als `createPrerequisites`-Callback. Implementiere
    `createPhase117Prerequisites(t testing.TB, pool *pgxpool.Pool)`: lege per rohem `pool.Exec` die
    minimalen Stub-Eltern-Tabellen an, die von den unter Test stehenden Queries referenziert werden
    -- `anime(id BIGINT PRIMARY KEY)`, `episodes(id BIGINT PRIMARY KEY, anime_id BIGINT NOT NULL
    REFERENCES anime(id), sort_index INT, episode_number TEXT)`, `fansub_groups(id BIGINT PRIMARY
    KEY)`, `fansub_releases(id BIGINT PRIMARY KEY, episode_id BIGINT NOT NULL REFERENCES
    episodes(id))`, `release_versions(id BIGINT PRIMARY KEY, release_id BIGINT NOT NULL REFERENCES
    fansub_releases(id), version VARCHAR(20) NOT NULL DEFAULT 'v1')`,
    `release_version_groups(release_version_id BIGINT NOT NULL REFERENCES release_versions(id),
    fansub_group_id BIGINT NOT NULL REFERENCES fansub_groups(id), PRIMARY KEY
    (release_version_id, fansub_group_id))`, `release_variants(id BIGINT PRIMARY KEY,
    release_version_id BIGINT NOT NULL REFERENCES release_versions(id), duration_seconds INT)`,
    `stream_sources(id BIGINT PRIMARY KEY, provider_type TEXT, external_id TEXT, url TEXT)`,
    `release_streams(id BIGINT PRIMARY KEY, variant_id BIGINT REFERENCES release_variants(id),
    stream_source_id BIGINT REFERENCES stream_sources(id))`, `theme_types(id BIGINT PRIMARY KEY,
    name TEXT NOT NULL)`, `themes(id BIGINT PRIMARY KEY, anime_id BIGINT NOT NULL REFERENCES
    anime(id), theme_type_id BIGINT NOT NULL REFERENCES theme_types(id), title TEXT, created_at
    TIMESTAMPTZ NOT NULL DEFAULT NOW())`, `theme_segments(id BIGSERIAL PRIMARY KEY, theme_id BIGINT
    NOT NULL REFERENCES themes(id), start_episode_id BIGINT, end_episode_id BIGINT, created_at
    TIMESTAMPTZ NOT NULL DEFAULT NOW())` (exakt die Vor-0049-Basisform). Validiere das Stub-SQL vor
    Ausfuehrung wie im Analog mit `validatePhase106SQL` (paketweit bereits vorhanden, kein
    Duplikat). Wende danach in dieser Reihenfolge echte Migrationsdateien per `ApplySQLFile` an:
    `0049_extend_theme_segments.up.sql`, `0051_extend_theme_segments_source.up.sql`,
    `0054_theme_segment_playback_sources.up.sql`, `0122_theme_segment_render_cache.up.sql`,
    `0141_theme_segment_assignments.up.sql`, `0142_theme_segment_episode_overrides.up.sql`,
    `0143_theme_segment_render_cache_release_version.up.sql` -- Pfadaufloesung analog
    `phase107MigrationPath` (dieselbe `runtime.Caller(0)`-basierte relative Pfadfunktion, als
    `phase117MigrationPath` dupliziert, da unterschiedliches Paket-Caller-Verzeichnis nicht sicher
    geteilt werden kann). Implementiere `validatePhase117DatabaseName`/`validatePhase117SchemaName`
    analog den Phase-107-Pendants. Erzeuge `phase117_postgres_test.go` mit denselben vier
    Guard-Test-Funktionen wie `phase107_postgres_test.go` (DSN-Skip, Isolation, Datenbank-Guard,
    Schema-Validation), umbenannt auf `Phase117`.
  </action>
  <acceptance_criteria>
    - `go build ./...` (in `backend/`) kompiliert fehlerfrei.
    - `go test ./internal/testsupport/... -run Phase117` laeuft gruen (Skip-Fall ohne gesetztes
      `TEAM4S_PHASE117_TEST_DSN` zaehlt als bestanden, kein Fehlschlag).
    - Mit gesetztem `TEAM4S_PHASE117_TEST_DSN` (lokale isolierte Test-DB `team4s_phase117_test_a`)
      wendet `OpenPhase117Postgres(t)` alle sieben Migrationsdateien fehlerfrei an und liefert einen
      nutzbaren `*pgxpool.Pool` mit `search_path` auf das isolierte Schema gesetzt.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/testsupport/... -run Phase117 -v</automated>
  </verify>
  <done>OpenPhase117Postgres liefert eine funktionsfaehige, isolierte Fixture mit allen sieben Migrationen angewendet; Guard-Tests gruen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modelle + Repository-CRUD fuer Zuweisung und Zeit-Override</name>
  <files>backend/internal/models/admin_anime_themes.go, backend/internal/repository/theme_segment_assignments.go, backend/internal/repository/theme_segment_overrides.go</files>
  <read_first>
    backend/internal/repository/theme_segment_render_cache.go (vollstaendiges Analog: Spalten-Konstante,
    `scan*`-Funktion, `INSERT ... ON CONFLICT ... DO UPDATE ... RETURNING`, `validate*Input`,
    Delete+RowsAffected, `trimStringPtr`-Wiederverwendung -- Paket-weit, kein Duplikat);
    backend/internal/models/admin_anime_themes.go Zeilen 34-77 (AdminThemeSegment -- wird erweitert,
    nicht ersetzt) und Zeilen 149-193 (SegmentLibrary*-Structs als Namens-/Kommentar-Stilvorbild).
  </read_first>
  <behavior>
    - `AssignThemeSegmentToReleaseVersion(ctx, segmentID, releaseVersionID)` legt bei Erstaufruf eine
      Zeile an und ist beim zweiten Aufruf mit denselben Argumenten idempotent (kein Duplikat, kein
      Fehler) -- `ON CONFLICT (theme_segment_id, release_version_id) DO NOTHING`.
    - `UnassignThemeSegmentFromReleaseVersion(ctx, segmentID, releaseVersionID)` entfernt die
      Zuweisung UND kaskadiert (per DB-FK `ON DELETE CASCADE` aus Plan 117-01) einen eventuell
      vorhandenen Override fuer dasselbe Paar.
    - `ListThemeSegmentAssignments(ctx, segmentID)` liefert alle `release_version_id`-Werte fuer ein
      Segment, sortiert aufsteigend.
    - `UpsertThemeSegmentEpisodeOverride(ctx, input)` fuer ein `(segmentID, releaseVersionID)`-Paar,
      das NICHT in `theme_segment_assignments` existiert, liefert `repository.ErrConflict` (aus
      Postgres-FK-Fehlercode 23503 uebersetzt) -- kein stiller Erfolg, kein generischer 500er.
    - `GetThemeSegmentEpisodeOverride(ctx, segmentID, releaseVersionID)` liefert
      `repository.ErrNotFound`, wenn kein Override existiert.
  </behavior>
  <action>
    Erweitere `AdminThemeSegment` in `backend/internal/models/admin_anime_themes.go` um
    `AssignedReleaseVersionIDs []int64 \`json:"assigned_release_version_ids,omitempty"\``, `IsShared
    bool \`json:"is_shared"\`` (true wenn `len(AssignedReleaseVersionIDs) > 1`), `HasEpisodeOverride
    bool \`json:"has_episode_override"\`` (true wenn mindestens eine zugewiesene Release-Version
    einen Override hat). Fuege neue Structs `AdminThemeSegmentAssignment{ID int64, ThemeSegmentID
    int64, ReleaseVersionID int64, CreatedAt time.Time}` und `AdminThemeSegmentEpisodeOverride{ID
    int64, ThemeSegmentID int64, ReleaseVersionID int64, StartTime string, EndTime string, CreatedAt
    time.Time, UpdatedAt time.Time}` (StartTime/EndTime als HH:MM:SS-String, exakt wie
    `AdminThemeSegment.StartTime`) sowie Input-DTOs
    `AdminThemeSegmentEpisodeOverrideUpsertInput{ThemeSegmentID int64, ReleaseVersionID int64,
    StartTime string, EndTime string}` hinzu. Erzeuge `backend/internal/repository/theme_segment_assignments.go`
    mit `AssignThemeSegmentToReleaseVersion`, `UnassignThemeSegmentFromReleaseVersion`,
    `ListThemeSegmentAssignments(ctx, segmentID) ([]int64, error)` als Methoden auf
    `*AdminContentRepository` (gleicher Empfaenger-Typ wie `theme_segment_render_cache.go`), nach dem
    Spalten-Konstante+Scan-Funktions-Muster des Analogs. Erzeuge
    `backend/internal/repository/theme_segment_overrides.go` mit `UpsertThemeSegmentEpisodeOverride`,
    `DeleteThemeSegmentEpisodeOverride`, `GetThemeSegmentEpisodeOverride`,
    `ListThemeSegmentEpisodeOverrides(ctx, segmentID) ([]models.AdminThemeSegmentEpisodeOverride,
    error)`. In `UpsertThemeSegmentEpisodeOverride` fange `pgconn.PgError` mit `Code == "23503"` ab
    (FK-Verletzung gegen `fk_theme_segment_episode_override_assignment`) und mappe auf
    `repository.ErrConflict` (gleiches Muster wie `CreateAnimeSegment` in
    `admin_content_anime_themes.go:504-511`). Validiere `StartTime`/`EndTime` NICHT hier auf
    Business-Regeln (max. 4 Minuten etc.) -- das bleibt Handler-Verantwortung ueber die bestehende
    `validateSegmentTimes` (Wiederverwendung folgt in Plan 117-05); dieses Repository validiert nur
    Nicht-Leerheit/Format-Minimalanforderungen analog `validateThemeSegmentRenderCacheUpsertInput`.
  </action>
  <acceptance_criteria>
    - `go build ./...` kompiliert fehlerfrei.
    - `go vet ./...` liefert keine neuen Warnungen fuer die drei geaenderten/neuen Dateien.
    - `theme_segment_assignments.go` und `theme_segment_overrides.go` bleiben jeweils unter 300
      Zeilen (CLAUDE.md 450-Zeilen-Limit mit deutlichem Puffer, da beide Dateien schmal und
      fokussiert sind).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./internal/models/... ./internal/repository/...</automated>
  </verify>
  <done>Modelle und beide neuen Repository-Dateien kompilieren; CRUD-Methoden folgen dem theme_segment_render_cache.go-Muster.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DB-Integrationstest fuer Zuweisung + Override-Datenintegritaet</name>
  <files>backend/internal/repository/theme_segment_assignments_integration_test.go</files>
  <read_first>
    backend/internal/repository/segment_playback_resolution_test.go (bestehendes, ungenuegendes
    String-Pattern-Testmuster -- NICHT wiederholen, dieser Test muss gegen eine echte DB laufen);
    backend/internal/testsupport/phase107_postgres_test.go (Aufrufmuster fuer
    `OpenPhase<N>Postgres(t)` in einem echten Repository-Integrationstest -- falls kein
    Repository-Test dieses Muster bereits nutzt, orientiere dich an
    `backend/internal/testsupport/phase107_postgres.go` direkt: `pool :=
    testsupport.OpenPhase117Postgres(t)`, danach `repository.NewAdminContentRepository(pool)` oder
    aequivalenten Konstruktor -- exakten Konstruktornamen per Grep auf `func NewAdminContentRepository`
    verifizieren, bevor der Test geschrieben wird).
  </read_first>
  <behavior>
    - Zwei `INSERT`-Zeilen als Testdaten (anime, episodes, fansub_groups, fansub_releases,
      release_versions x2, release_version_groups, theme_types, themes, theme_segments x1) --
      ein geteiltes Segment, zwei Release-Versionen.
    - `AssignThemeSegmentToReleaseVersion(segment, releaseVersionA)` +
      `AssignThemeSegmentToReleaseVersion(segment, releaseVersionB)` -> `ListThemeSegmentAssignments`
      liefert beide IDs.
    - `UpsertThemeSegmentEpisodeOverride` fuer `releaseVersionA` -> `GetThemeSegmentEpisodeOverride`
      liefert die gespeicherte Zeit; `releaseVersionB` bleibt ohne Override (kein Cross-Talk).
    - `UpsertThemeSegmentEpisodeOverride` fuer eine DRITTE, nicht zugewiesene `release_version_id`
      -> Fehler ist `errors.Is(err, repository.ErrConflict)`.
    - `UnassignThemeSegmentFromReleaseVersion(segment, releaseVersionA)` -> nachfolgendes
      `GetThemeSegmentEpisodeOverride(segment, releaseVersionA)` liefert `repository.ErrNotFound`
      (Cascade-Beweis).
  </behavior>
  <action>
    Erzeuge `backend/internal/repository/theme_segment_assignments_integration_test.go` mit Build-Tag
    oder Laufzeit-Skip identisch zum `testsupport`-Muster (der Test wird automatisch uebersprungen,
    wenn `TEAM4S_PHASE117_TEST_DSN` fehlt -- das ist bereits in `OpenPhase117Postgres` eingebaut,
    kein zusaetzlicher Skip noetig). Baue die Testdaten wie im `<behavior>`-Block beschrieben per
    direkten `pool.Exec`-INSERTs aus dem Test heraus (nicht ueber Produktions-Handler). Implementiere
    die vier Behavior-Faelle als eigene `t.Run`-Subtests mit `testify/require`. Nutze
    `errors.Is(err, repository.ErrConflict)` und `errors.Is(err, repository.ErrNotFound)` fuer die
    Fehlerpruefungen (nicht String-Vergleich der Fehlermeldung).
  </action>
  <acceptance_criteria>
    - `go test ./internal/repository/... -run TestThemeSegmentAssignments -v` ist gruen, wenn
      `TEAM4S_PHASE117_TEST_DSN` gesetzt ist, und wird sauber uebersprungen (kein Fail), wenn nicht.
    - Der Konflikt-Testfall (Override ohne Zuweisung) UND der Cascade-Testfall (Unassign loescht
      Override) sind beide als eigene Assertions vorhanden -- nicht nur der Happy Path.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/repository/... -run TestThemeSegmentAssignments -v</automated>
  </verify>
  <done>Vier Behavior-Faelle gruen (bei gesetzter Test-DSN) bzw. sauber uebersprungen (ohne DSN); Konflikt- und Cascade-Pfad sind explizit getestet, nicht nur der Happy Path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test-Fixture -> isolierte Test-DB | Nur ueber explizit gesetzte `TEAM4S_PHASE117_TEST_DSN`, niemals `DATABASE_URL` (Analog Phase 106/107 -- verhindert versehentliches Schreiben in Produktions-/Dev-DB) |
| Repository-Schicht -> Postgres | Interne Server-Schicht, kein direkter Nutzereingabepfad in diesem Plan (Handler-Validierung folgt in Plan 117-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-117-02-01 | Tampering | `OpenPhase117Postgres` DSN-Handling | mitigate | Wiederverwendung des bestehenden `openPhasePostgres`-Guards (Datenbank-/Schema-Namens-Regex, `public`-Schema-Verbot) -- kein neuer, ungeprüfter Codepfad |
| T-117-02-02 | Elevation of Privilege | Override-Upsert ohne Zuweisungspruefung | mitigate | DB-seitige composite FK (`fk_theme_segment_episode_override_assignment`, Plan 117-01) erzwingt Zuweisung vor Override; Repository uebersetzt 23503 in `ErrConflict` statt stillem Erfolg |
| T-117-02-SC | Tampering | keine neuen Paket-Installationen | accept | Nur Go-Standardbibliothek + bereits vorhandene `pgx`/`testify`-Abhaengigkeiten |
</threat_model>

<verification>
`go build ./...` und `go vet ./...` fehlerfrei; `go test ./internal/testsupport/... -run Phase117`
und `go test ./internal/repository/... -run TestThemeSegmentAssignments` gruen (mit oder ohne
gesetzte Test-DSN).
</verification>

<success_criteria>
Wave-0-DB-Testinfrastruktur existiert und ist wiederverwendbar fuer Plan 117-03. Assignment- und
Override-Repository-CRUD ist additiv, beruehrt keine bestehende Resolution-Logik, und ist gegen eine
echte Postgres-Instanz integrationsgetestet (nicht nur String-Pattern-geprueft).
</success_criteria>

<output>
Create `.planning/phases/117-kara-segment-zeit-override-anzeige/117-02-SUMMARY.md` when done
</output>
