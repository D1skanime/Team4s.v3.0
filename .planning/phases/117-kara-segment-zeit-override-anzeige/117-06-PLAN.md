---
phase: 117-kara-segment-zeit-override-anzeige
plan: 06
type: execute
wave: 6
depends_on: ["117-05", "117-07"]
files_modified:
  - frontend/src/types/admin.ts
  - frontend/src/lib/api.ts
  - frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseSegments.ts
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.test.tsx
autonomous: true
requirements: [D-01, D-03]

must_haves:
  truths:
    - "Admin kann fuer die aktuell geoeffnete Folge eines geteilten Kara eine abweichende Zeit setzen und speichern, ohne ein neues Segment anzulegen (D-01, UI-SPEC Surface 1)"
    - "Admin kann einen bestehenden Override entfernen, wonach die Folge wieder die Basis-Zeit verwendet (D-01)"
    - "Der Override-Block nutzt ausschliesslich @/components/ui Primitives (Switch, FormField, Input, Button) -- kein neues natives <input type=checkbox> (CLAUDE.md UI-System-Pflicht)"
  artifacts:
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx"
      provides: "Override-Block gemaess UI-SPEC Surface 1 (Switch + zwei FormField/Input + Button 'Override entfernen')"
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseSegments.ts"
      provides: "setSegmentOverride / removeSegmentOverride Hook-Methoden"
  key_links:
    - from: "SegmentEditPanel.tsx Override-Switch"
      to: "useReleaseSegments.ts setSegmentOverride"
      via: "onSave-Callback ruft PUT .../assignments/:releaseVersionId/override ueber api.ts"
      pattern: "setSegmentOverride"
---

<objective>
Baut UI-SPEC Surface 1 (Admin: Per-Folge Zeit-Override-Eingabe) auf den in Plan 117-05 geschaffenen
Endpunkten. Der Override-Block erscheint nur bei geteilten Segmenten (`is_shared`) und respektiert
die Data-Ownership-Regel aus dem UI-SPEC: kein automatisches Speichern, nur über den bestehenden
"Speichern"-Button.

Purpose: Macht D-01 (Per-Folge-Zeit-Override ohne Re-Encode, kein neues Segment) für Admins
tatsächlich bedienbar, exakt nach dem in 117-UI-SPEC.md Surface 1 verbindlich festgelegten
Verhalten/Copy.
Output: Funktionsfähiger Override-Eingabe-Block, neue Hook-Methoden, neue typisierte API-Client-Funktionen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-kara-segment-zeit-override-anzeige/117-UI-SPEC.md
@frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx
@frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx
@frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.test.tsx
@frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseSegments.ts
</context>

<interfaces>
Aus `frontend/src/components/ui` (Pflicht-Primitives, siehe UI-SPEC Zeilen 33-43):
`Switch` (`frontend/src/components/ui/Switch.tsx:8-13`): `{ checked: boolean; onCheckedChange:
(next: boolean) => void; disabled?: boolean; label?: string }` — Referenzverwendung
`frontend/src/app/admin/role-capabilities/RoleCapabilityDetail.tsx:5,102-108`.
`FormField` (`frontend/src/components/ui/FormField.tsx:5-13`): `{ label?, htmlFor?, hint?, error?,
required?, disabled?, children }` — Referenzverwendung
`frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaDetailPanel.tsx`.

Aus `frontend/src/types/admin.ts` (Zeilen 834-869, wird erweitert): `AdminThemeSegment` bekommt neue
Felder `assigned_release_version_ids?: number[]`, `is_shared?: boolean`, `has_episode_override?:
boolean`, `assigned_episodes?: { release_version_id: number; episode_number: string }[]`
(Backend-Spiegel aus Plan 117-03 Task 2 — `assigned_episodes` liefert die ECHTE Episodennummer je
Zuweisung, Grundlage für die UI-SPEC-konformen "Folge {N}"-Chips in Plan 117-07).

Bestehende API-Client-Signaturkonvention (`frontend/src/lib/api.ts:6537-6710`, `getAnimeSegments`/
`createAnimeSegment`/`updateAnimeSegment`): `(..., authToken?: string, releaseVariantId?: number |
null) => Promise<...>`, Query-Param `release_variant_id` (semantisch `release_versions.id`, siehe
Plan 117-05 `<interfaces>`).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Typen + API-Client-Funktionen + Hook-Methoden</name>
  <files>frontend/src/types/admin.ts, frontend/src/lib/api.ts, frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseSegments.ts</files>
  <read_first>
    frontend/src/types/admin.ts Zeilen 834-869 (AdminThemeSegment) und Zeilen 924-951
    (AdminThemeSegmentCreateRequest/PatchRequest als Formatvorbild für neue Request-Typen);
    frontend/src/lib/api.ts Zeilen 6616-6656 (updateAnimeSegment — exaktes Muster für
    URLSearchParams-Aufbau mit `release_variant_id`, `authorizedFetch`, Fehler-Envelope-Parsing per
    `parseApiErrorPayload`);
    frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseSegments.ts Zeilen 164-201
    (`create`/`update` — Guard-Clause -> try/await -> reload/poll -> catch-setErrorMessage-Muster,
    hier für die neuen Methoden zu übernehmen).
  </read_first>
  <behavior>
    - `setSegmentOverride(segmentId, releaseVersionId, {start_time, end_time})` ruft PUT
      `.../segments/:segmentId/assignments/:releaseVersionId/override` und lädt danach die
      Segmentliste neu (`load()`), damit `has_episode_override` aktualisiert wird.
    - `removeSegmentOverride(segmentId, releaseVersionId)` ruft DELETE auf denselben Pfad + `/override`
      und lädt danach neu.
    - Beide Methoden folgen dem bestehenden `try { await api(...) } catch (error) {
      setErrorMessage(...); return null/false }`-Muster — kein neues Fehlerbehandlungsmuster.
  </behavior>
  <action>
    Erweitere `AdminThemeSegment` in `frontend/src/types/admin.ts` um
    `assigned_release_version_ids?: number[]`, `is_shared?: boolean`, `has_episode_override?:
    boolean`, `assigned_episodes?: { release_version_id: number; episode_number: string }[]`
    (Backend-Spiegel, snake_case, exakt wie die übrigen Felder dieses Interface — `assigned_episodes`
    spiegelt `AdminThemeSegmentAssignmentEpisode` aus Plan 117-03 Task 2). Füge neue Typen
    `AdminThemeSegmentOverrideRequest { start_time: string; end_time: string }` hinzu (nach dem
    Muster von `AdminThemeSegmentPatchRequest`, Zeilen 939-951). Füge in `frontend/src/lib/api.ts`
    nach `deleteAnimeSegment` (Zeile ~6690) vier neue exportierte Funktionen hinzu:
    `assignAnimeSegment(animeId, segmentId, releaseVersionId, authToken?)`,
    `unassignAnimeSegment(animeId, segmentId, releaseVersionId, authToken?)`,
    `upsertAnimeSegmentEpisodeOverride(animeId, segmentId, releaseVersionId, input:
    AdminThemeSegmentOverrideRequest, authToken?)`,
    `deleteAnimeSegmentEpisodeOverride(animeId, segmentId, releaseVersionId, authToken?)` — jede folgt
    exakt dem `updateAnimeSegment`-Muster (`authorizedFetch`, `withAuthHeader`,
    `parseApiErrorPayload` bei `!response.ok`, Rückgabe `{ data: AdminThemeSegment }`), mit den
    Pfaden `POST/DELETE /api/v1/admin/anime/${animeId}/segments/${segmentId}/assignments`
    (Unassign mit `/${releaseVersionId}` Suffix, Assign mit Body `{release_version_id:
    releaseVersionId}`) bzw. `PUT/DELETE
    .../segments/${segmentId}/assignments/${releaseVersionId}/override`. Erweitere
    `useReleaseSegments.ts` um `setSegmentOverride`/`removeSegmentOverride`, die diese neuen
    API-Funktionen mit dem bestehenden `animeId`/`hasAuthSession`-Guard aufrufen und danach `await
    load()` ausführen (analog `update`, Zeilen 176-189); gib beide Funktionen im Return-Objekt des
    Hooks zurück (nach `render`).
  </action>
  <acceptance_criteria>
    - `cd frontend && npm run typecheck` (oder `tsc --noEmit`, projektüblicher Befehl per
      `package.json` verifizieren) läuft fehlerfrei.
    - `assignAnimeSegment`/`unassignAnimeSegment`/`upsertAnimeSegmentEpisodeOverride`/
      `deleteAnimeSegmentEpisodeOverride` sind aus `@/lib/api` exportiert und werden von
      `useReleaseSegments.ts` importiert (kein direkter `fetch` in der Hook-Datei).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run typecheck</automated>
  </verify>
  <done>Typen, API-Client-Funktionen und Hook-Methoden für Zuweisung/Override sind vorhanden und typsicher.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Override-Eingabe-Block in SegmentEditPanel (UI-SPEC Surface 1)</name>
  <files>frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx</files>
  <read_first>
    .planning/phases/117-kara-segment-zeit-override-anzeige/117-UI-SPEC.md Abschnitt "Surface 1"
    (Zeilen 142-187 — verbindliches Layout/Copy/Primitive-Pflicht);
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx (vollständig,
    bereits gelesen — Einfügepunkt: NACH dem bestehenden "Zeitbereich im Video"-Block, Zeilen
    221-295, VOR dem "Aktive Playback-Quelle"-Block, Zeile 297);
    frontend/src/components/ui/Switch.tsx (vollständig) und
    frontend/src/app/admin/role-capabilities/RoleCapabilityDetail.tsx Zeilen 100-110
    (Referenzverwendung von `Switch`);
    frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaDetailPanel.tsx
    (Referenzverwendung von `FormField` + `Input`/`Textarea`).
  </read_first>
  <behavior>
    - `editingSegment?.is_shared !== true` -> Override-Block wird NICHT gerendert (UI-SPEC
      Sichtbarkeitsbedingung).
    - `editingSegment?.is_shared === true` -> `Switch` sichtbar, Label "Zeit nur für diese Folge
      abweichend setzen"; `checked` initial = vorhandener Override für die aktuelle Release-Version.
    - Switch aktiv -> zwei `FormField`+`Input`-Paare "Start (Folge {N})"/"Ende (Folge {N})", Hinweistext
      exakt aus UI-SPEC Copywriting Contract; bei bereits vorhandenem Override zusätzlich
      `Button variant="secondary"` "Override entfernen" mit `window.confirm`-Bestätigung aus UI-SPEC.
    - Speichern des Override läuft NUR über den bestehenden zentralen "Speichern"-Button (keinen
      zweiten Save-Button einführen); Validierungsfehler erscheinen über `FormField`-`error`-Prop mit
      denselben deutschen Strings wie beim Basis-Zeitbereich.
  </behavior>
  <action>
    Ergänze `SegmentEditPanelProps` um `currentReleaseVersionId: number | null`,
    `onSaveOverride: (input: { startTime: string; endTime: string }) => void`,
    `onRemoveOverride: () => void`, `isSavingOverride: boolean`, `overrideError: string | null` (der
    Aufrufer `SegmenteTab.tsx`/`useEpisodeVersionEditor.ts` verdrahtet diese in Task 3 dieses Plans;
    hier zunächst nur die Prop-Schnittstelle + Rendering). Füge direkt nach dem bestehenden
    Zeitbereich-Block (nach Zeile 295, vor Zeile 297) einen neuen bedingten Block ein, der NUR
    rendert, wenn `editingSegment?.is_shared === true`. Importiere `Switch` und `FormField` aus
    `@/components/ui` (zusätzlich zu den bereits importierten lucide-Icons — bestehende native
    `<input>`/`<select>` in DIESER Datei bleiben für alle bereits vorhandenen Felder unverändert
    (vorbestehende Abweichung, laut UI-SPEC/CLAUDE.md nicht Teil dieser Phase); NUR der neue
    Override-Block nutzt ausschließlich Primitives. Rendere `Switch` mit
    `label="Zeit nur für diese Folge abweichend setzen"`, `checked={overrideEnabled}`,
    `onCheckedChange={(next) => { if (!next) { onRemoveOverride() } setOverrideEnabled(next) }}`.
    Bei `overrideEnabled`: `<h4>Zeit-Override für Folge {currentEpisodeNumber}</h4>` (Heading laut
    UI-SPEC Typography), zwei `FormField`-Blöcke mit `label="Start (Folge {N})"`/`"Ende (Folge {N})"`,
    `hint="Diese Zeit gilt nur für Folge {N}. Das Kara selbst bleibt unverändert und wird nicht neu
    kodiert — nur die Vorschau wird neu vorbereitet."`, je ein `Input` (aus `@/components/ui`) mit
    demselben `parseFlexibleTimeInput`/`formatTimeInput`-`onBlur`-Verhalten wie die bestehenden
    Zeitfelder (Wiederverwendung der bereits importierten Helper aus `SegmenteTab.helpers`).
    Validierungsfehler aus `overrideError` erscheinen über die `error`-Prop von `FormField`. Wenn
    bereits ein Override existiert (`editingSegment?.has_episode_override` UND aktuelle Folge
    betroffen — exaktes Feld dafür kommt aus `assigned_release_version_ids`/Override-Detail, das
    Task 3 in `SegmenteTab.tsx` bereits pro Segment lädt; falls hier noch keine granulare
    Pro-Folge-Info vorliegt, zeige den "Override entfernen"-Button, sobald `overrideEnabled` beim
    Öffnen bereits `true` war), rendere darunter `Button variant="secondary"` mit Label "Override
    entfernen", `onClick` löst `window.confirm("Override entfernen? Folge {N} verwendet danach wieder
    die Basis-Zeit des geteilten Segments.")` aus und ruft bei Bestätigung `onRemoveOverride()` auf.
  </action>
  <acceptance_criteria>
    - Grep in `SegmentEditPanel.tsx` zeigt `Switch`, `FormField` als Importe aus `@/components/ui`.
    - Der neue Override-Block enthält KEIN natives `<input type="checkbox">` und keinen
      handgebauten Toggle-Button (Grep negativ).
    - Bei `editingSegment` mit `is_shared: false` rendert die Komponente (laut Testfall in Task 3)
      keinen Override-Block.
    - Alle vier Copy-Strings aus UI-SPEC Copywriting Contract (Toggle-Label, Sektionstitel,
      Hinweistext, Feld-Labels) sind wortgleich im Code vorhanden (deutsche Umlaute korrekt: ä ö ü
      ä ü — insbesondere "für", "überschrieben", "gültig").
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run typecheck</automated>
  </verify>
  <done>Override-Block ist gemäß UI-SPEC Surface 1 implementiert, primitive-konform, erscheint nur bei geteilten Segmenten.</done>
</task>

<task type="auto">
  <name>Task 3: Verdrahtung SegmenteTab -> SegmentEditPanel + Wave-0-Frontend-Test</name>
  <files>frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.test.tsx</files>
  <read_first>
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx (Aufrufstelle von
    `<SegmentEditPanel ... />` — per Grep auf `<SegmentEditPanel` lokalisieren, da nicht in den
    bisher gelesenen Zeilen 1-230 enthalten);
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.test.tsx (bestehendes
    Testmuster für diese Komponente, als Vorbild für den neuen Testfall).
  </read_first>
  <action>
    Ergänze die `<SegmentEditPanel>`-Aufrufstelle in `SegmenteTab.tsx` um die in Task 2 definierten
    neuen Props: `currentReleaseVersionId={releaseVariantId ?? null}` (bereits vorhandene Prop/Variable
    dieser Datei, semantisch `release_versions.id`), `onSaveOverride={(input) => { if
    (!editingSegment || currentReleaseVersionId == null) return;
    void setSegmentOverride(editingSegment.id, currentReleaseVersionId, { start_time:
    input.startTime, end_time: input.endTime }) }}`, `onRemoveOverride={() => { if (!editingSegment
    || currentReleaseVersionId == null) return; void removeSegmentOverride(editingSegment.id,
    currentReleaseVersionId) }}`, `isSavingOverride`/`overrideError` aus lokalem State (analog dem
    bestehenden `isSaving`/`formError`-State-Paar dieser Datei). `setSegmentOverride`/
    `removeSegmentOverride` kommen aus dem bereits vorhandenen `useReleaseSegments(...)`-Hook-Aufruf
    dieser Datei (aus Task 1 erweitert). Füge in `SegmenteTab.test.tsx` einen neuen Testfall hinzu,
    der beweist: ein Segment mit `is_shared: true` im Mock-State rendert den Override-Switch (per
    `screen.getByRole` o.ä., konsistent mit bestehenden Testmustern in dieser Datei); ein Segment mit
    `is_shared: false` (oder Feld fehlt) rendert ihn nicht.
  </action>
  <acceptance_criteria>
    - `npm test -- SegmenteTab` (in `frontend/`) ist grün, inklusive des neuen Testfalls.
    - `SegmenteTab.tsx` bleibt unter 800 Zeilen (aktuell 743 — geringfügige Prop-Verdrahtung darf die
      450-Zeilen-Regel für NEUE Dateien nicht auf diese bestehende, bereits über dem Limit liegende
      Datei anwenden; falls die Änderung die Datei spürbar wachsen lässt, verschiebe die neuen
      Callback-Definitionen nach `SegmenteTab.helpers.tsx`).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- SegmenteTab</automated>
  </verify>
  <done>SegmentEditPanel ist mit den neuen Override-Callbacks verdrahtet; ein automatisierter Test beweist die bedingte Sichtbarkeit des Override-Blocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> Admin-API | Bestehender authentifizierter Admin-Client (`authorizedFetch`), kein neuer Boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-117-06-01 | Tampering | Client-seitige Zeitvalidierung umgangen | accept | Server-seitige `validateSegmentTimes` (Plan 117-05) bleibt die durchsetzende Instanz; Client-Validierung ist nur UX-Komfort |
| T-117-06-SC | Tampering | keine neuen Paket-Installationen | accept | Nur bereits vorhandene `@/components/ui`-Primitives, keine neue npm-Abhängigkeit |
</threat_model>

<verification>
`npm run typecheck` und `npm test -- SegmenteTab` (in `frontend/`) grün.
</verification>

<success_criteria>
Admins können über die bestehende Segment-Editor-Seitenleiste einen Per-Folge-Zeit-Override setzen
und entfernen, ausschließlich über `@/components/ui`-Primitives, ohne neues Segment und ohne
automatisches Speichern.
</success_criteria>

<output>
Create `.planning/phases/117-kara-segment-zeit-override-anzeige/117-06-SUMMARY.md` when done
</output>
