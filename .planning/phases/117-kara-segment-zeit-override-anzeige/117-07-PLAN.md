---
phase: 117-kara-segment-zeit-override-anzeige
plan: 07
type: execute
wave: 4
depends_on: ["117-03"]
files_modified:
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.helpers.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.test.tsx
autonomous: true
requirements: [D-02, D-03]

must_haves:
  truths:
    - "Ein geteiltes Kara erscheint in der Admin-Tabelle mit einem 'Geteiltes Segment'-Badge statt optisch identisch zu einem Einzel-Segment (D-03 Sichtbarkeit)"
    - "Ein Kara mit einem Zeit-Override auf mindestens einer zugewiesenen Folge zeigt zusaetzlich ein 'Zeit hier ueberschrieben'-Badge (D-01/D-02 Transparenz)"
    - "Die Zuweisungsliste (welche Folgen) ist ueber einen DisclosureIndicator einsehbar, ohne die Tabelle standardmaessig zu ueberladen (D-02 keine visuelle Verdopplung)"
  artifacts:
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx"
      provides: "Badge('Geteiltes Segment')/Badge('Zeit hier ueberschrieben') + Zuweisungsliste-Disclosure gemaess UI-SPEC Surface 2"
  key_links:
    - from: "SegmenteTab.tsx Tabellenzeile"
      to: "AdminThemeSegment.is_shared / has_episode_override"
      via: "aus ListAnimeSegments-Hydration (Plan 117-03 Task 2) bereits im geladenen segments-State vorhanden"
      pattern: "is_shared"
---

<objective>
Baut UI-SPEC Surface 2 (Admin: Entdopplungs-/"geteiltes Segment"-Indikator) auf den bereits über
`ListAnimeSegments` gelieferten Feldern `is_shared`/`assigned_release_version_ids`/
`has_episode_override` (Plan 117-03 Task 2). Reine Lese-/Darstellungs-Erweiterung der bestehenden
Segment-Tabelle — kein neuer Schreibpfad, keine Abhängigkeit von den Assignment/Override-Endpunkten
aus Plan 117-05.

Purpose: Macht D-03 (geteiltes Kara) und die "Zeit hier überschrieben"-Transparenz aus D-01/D-02 im
Admin-Editor sichtbar, exakt nach dem in 117-UI-SPEC.md Surface 2 verbindlich festgelegten
Verhalten/Copy. Läuft parallel zu Plan 117-04 (reine Backend-Welle), da beide unabhängig auf Plan
117-03 aufbauen und keine Dateien teilen.
Output: Badge-Anzeige + Zuweisungslisten-Disclosure in der bestehenden Segment-Tabelle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-kara-segment-zeit-override-anzeige/117-UI-SPEC.md
@frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.helpers.tsx
</context>

<interfaces>
Aus Plan 117-03 Task 2 (bereits im `segments`-State von `useReleaseSegments` vorhanden, kein neuer
API-Aufruf nötig): `AdminThemeSegment.is_shared: boolean`,
`AdminThemeSegment.assigned_release_version_ids: number[]`,
`AdminThemeSegment.has_episode_override: boolean`.

Aus `@/components/ui` (Pflicht-Primitives): `Badge` (bereits importiert in `SegmenteTab.tsx:30-37`
über den `Table*`-Barrel-Import — Import-Statement um `Badge`, `DisclosureIndicator` ergänzen),
`DisclosureIndicator` (`frontend/src/components/ui/DisclosureIndicator.tsx:6-11`):
`{ open?: boolean; size?: 'sm'|'md'|'lg'; variant?: 'plain'|'button'; className?: string }` — kein
eingebauter Click-Handler, in einen `<button>` wrappen.

Bestehendes Range-Highlight-Muster (`SegmenteTab.helpers.tsx:174-181`, `isSegmentActiveForEpisode`)
als Stilvorbild für eine neue, analoge Helper-Funktion.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Badge-Helper + Zuweisungslisten-Helper</name>
  <files>frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.helpers.tsx</files>
  <read_first>
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.helpers.tsx (vollständig,
    bereits gelesen — `isSegmentActiveForEpisode` Zeilen 174-181 als direktes Stilvorbild).
  </read_first>
  <behavior>
    - `isCurrentEpisodeAssigned(segment, currentReleaseVersionId)`: `true`, wenn
      `currentReleaseVersionId` in `segment.assigned_release_version_ids` enthalten ist (Ersatz-/
      Ergänzungslogik zu `isSegmentActiveForEpisode` für den neuen, zuweisungsbasierten Fall).
    - `formatAssignmentChipLabel(releaseVersionId, hasOverride)`: liefert `"Folge {releaseVersionId}"`
      ohne Override bzw. `"Folge {releaseVersionId} · Zeit angepasst"` mit Override — HINWEIS: die
      Anzeige soll laut UI-SPEC die Episodennummer zeigen, nicht die interne `release_version_id`;
      da `AdminThemeSegment` aktuell keine Episodennummer-pro-Zuweisung liefert, nutze
      `releaseVersionId` als Platzhalter-Label UND dokumentiere im Executor-Summary explizit, dass
      eine künftige Erweiterung (z. B. `assigned_episode_numbers` statt `assigned_release_version_ids`)
      die reale Episodennummer liefern müsste — kein Blocker für diesen Plan, da UI-SPEC das konkrete
      Feld ausdrücklich der Planungsphase überlässt (UI-SPEC Zeile 149-150).
  </behavior>
  <action>
    Ergänze in `SegmenteTab.helpers.tsx` zwei neue exportierte Funktionen:
    `isCurrentEpisodeAssigned(segment: AdminThemeSegment, currentReleaseVersionId: number | null):
    boolean` (gibt `false` zurück, wenn `currentReleaseVersionId == null` oder
    `segment.assigned_release_version_ids` fehlt) und
    `formatAssignmentChipLabel(releaseVersionId: number, hasOverride: boolean): string` exakt nach
    dem `<behavior>`-Block. Platziere beide direkt unterhalb von `isSegmentActiveForEpisode`, gleicher
    Kommentarstil (kurzer Zweck-Kommentar auf Deutsch).
  </action>
  <acceptance_criteria>
    - `cd frontend && npm run typecheck` fehlerfrei.
    - Beide Funktionen sind aus `SegmenteTab.helpers.tsx` exportiert und ohne Seiteneffekte (reine
      Funktionen).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run typecheck</automated>
  </verify>
  <done>Zwei neue reine Helper-Funktionen verfügbar für Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Badges + Zuweisungslisten-Disclosure in der Segment-Tabelle</name>
  <files>frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.test.tsx</files>
  <read_first>
    .planning/phases/117-kara-segment-zeit-override-anzeige/117-UI-SPEC.md Abschnitt "Surface 2"
    (Zeilen 190-227 — verbindliches Layout/Copy/Primitive-Pflicht);
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx Zeilen 30-37 (Table-
    Primitive-Imports) und Zeilen 569-578 (bestehende Tabellenzeilen-Struktur, `styles.tableRowActive`
    -- exakte Zeilen der Episoden-/Zeitbereich-Spalte per Grep auf `TableCell` innerhalb der
    Segment-Zeilen-Map-Funktion lokalisieren, da nicht in den bisher gelesenen Zeilen 1-230 enthalten).
  </read_first>
  <behavior>
    - Segment mit `is_shared: false` (oder Feld fehlt): Tabellenzeile identisch zum heutigen Rendering,
      keine Badges, kein DisclosureIndicator (kein visuelles Rauschen im Standardfall).
    - Segment mit `is_shared: true`, `has_episode_override: false`: `Badge variant="info"`
      "Geteiltes Segment" direkt vor dem bestehenden Bereichstext.
    - Segment mit `is_shared: true`, `has_episode_override: true`: zusätzlich `Badge
      variant="warning"` "Zeit hier überschrieben" direkt neben dem "Geteiltes Segment"-Badge.
    - Klick auf `DisclosureIndicator` bei einem geteilten Segment öffnet eine Chip-Liste mit einem
      `Badge`-Chip pro `assigned_release_version_ids`-Eintrag, aktuell geöffnete Folge optisch
      hervorgehoben (`variant="info"` statt `variant="neutral"`/`"warning"`).
  </behavior>
  <action>
    Importiere `Badge` und `DisclosureIndicator` zusätzlich zum bestehenden `@/components/ui`-Barrel-
    Import in `SegmenteTab.tsx` (Zeile 30-37 erweitern, ein Import-Statement, kein Duplikat). Füge in
    der Tabellenzeilen-Render-Funktion (per Grep auf die Segment-Map-Stelle lokalisieren) VOR dem
    bestehenden Bereichstext (`formatEpisodeRange`-Aufruf) einen bedingten Badge-Block ein: wenn
    `segment.is_shared`, rendere `<Badge variant="info">Geteiltes Segment</Badge>`; wenn zusätzlich
    `segment.has_episode_override`, rendere direkt danach `<Badge variant="warning">Zeit hier
    überschrieben</Badge>`. Füge am Zeilenende (oder in derselben Zelle) einen lokalen
    `openAssignmentsFor: number | null`-State (per Segment-ID) und einen
    `<button type="button" onClick={() => setOpenAssignmentsFor(open === segment.id ? null :
    segment.id)}><DisclosureIndicator open={openAssignmentsFor === segment.id} variant="button"
    size="sm" /></button>` hinzu, NUR wenn `segment.is_shared`. Rendere bei geöffnetem Zustand eine
    Unterzeile (zusätzliche `TableRow` mit `colSpan` über die bestehende Spaltenanzahl, konsistent
    mit dem bestehenden Table-Primitive-Muster) mit einem `Badge`-Chip je
    `segment.assigned_release_version_ids`-Eintrag, Text über `formatAssignmentChipLabel` (aus Task 1),
    `variant="info"` für den Eintrag, der `currentReleaseVersionId` entspricht (per
    `isCurrentEpisodeAssigned`-äquivalentem Vergleich), sonst `variant={hasOverride ? 'warning' :
    'neutral'}` (Override-Zustand pro Zuweisung ist mit den heutigen `AdminThemeSegment`-Feldern nur
    aggregiert über `has_episode_override` bekannt, nicht pro Zuweisung — verwende deshalb
    `has_episode_override` segmentweit als Näherung für JEDEN Chip außer dem aktuell geöffneten, und
    dokumentiere diese Näherung im Executor-Summary als bekannte Grenze bis ein
    Pro-Zuweisung-Override-Feld existiert). Ergänze in `SegmenteTab.test.tsx` zwei Testfälle: (1) ein
    Segment mit `is_shared: true, has_episode_override: true` im Mock-State rendert beide Badges; (2)
    ein Segment ohne `is_shared` rendert keinen Badge und keinen DisclosureIndicator.
  </action>
  <acceptance_criteria>
    - `npm test -- SegmenteTab` (in `frontend/`) ist grün, inklusive der zwei neuen Testfälle.
    - Grep bestätigt `Badge`/`DisclosureIndicator` als Importe aus `@/components/ui` in
      `SegmenteTab.tsx` (kein natives `<span class="badge">`-Ersatzmarkup).
    - Der Standardfall (kein geteiltes Segment) erzeugt laut Testfall (2) keine zusätzlichen
      DOM-Knoten gegenüber dem bisherigen Rendering.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- SegmenteTab</automated>
  </verify>
  <done>Badges und Zuweisungslisten-Disclosure sind implementiert, primitive-konform, und mit zwei automatisierten Testfällen abgesichert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser (Admin) -> bereits geladener Segment-State | Reine Darstellungs-Erweiterung, kein neuer Netzwerk-Aufruf, kein neuer Trust Boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-117-07-01 | Information Disclosure | Zuweisungsliste zeigt release_version_id-Werte statt Episodennummern | accept | Bekannte, im Task 1 dokumentierte Näherung (kein sensibler Wert, nur ein interner Fremdschlüssel als Label); keine Sicherheitsauswirkung, nur UX-Politur für eine Folgephase |
| T-117-07-SC | Tampering | keine neuen Paket-Installationen | accept | Nur bereits vorhandene `@/components/ui`-Primitives |
</threat_model>

<verification>
`npm run typecheck` und `npm test -- SegmenteTab` (in `frontend/`) grün.
</verification>

<success_criteria>
Geteilte Kara-Segmente sind in der Admin-Tabelle klar als solche erkennbar (Badge), Zeit-Overrides
sind transparent markiert, und die Zuweisungsliste ist über eine Disclosure einsehbar, ohne den
Standardfall (nicht geteiltes Segment) optisch zu verändern.
</success_criteria>

<output>
Create `.planning/phases/117-kara-segment-zeit-override-anzeige/117-07-SUMMARY.md` when done
</output>
