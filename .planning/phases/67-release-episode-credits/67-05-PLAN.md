---
phase: 67-release-episode-credits
plan: 05
type: execute
wave: 3
depends_on: ["67-02"]
files_modified:
  - backend/internal/handlers/contribution_proposals_me_handler.go
  - backend/internal/handlers/contribution_proposals_me_test.go
autonomous: true
requirements: [P67-SC1]

must_haves:
  truths:
    - "Der Member-Vorschlag (POST /me/contribution-proposals) akzeptiert ein optionales release_version_id."
    - "Ist release_version_id gesetzt und die eigene Gruppe war an dieser Version nicht beteiligt, antwortet der Endpunkt mit 422 (D-03 auch im Member-Pfad, Pitfall 5)."
    - "Ohne release_version_id bleibt das Vorschlagsverhalten unveraendert (additiv, NULL-default)."
  artifacts:
    - path: "backend/internal/handlers/contribution_proposals_me_handler.go"
      provides: "release_version_id im Request + D-03-Validierung + Durchreichen an ProposalInput"
      contains: "GroupParticipatesInReleaseVersion"
  key_links:
    - from: "CreateProposal (Member-Handler)"
      to: "GroupParticipatesInReleaseVersion"
      via: "Beteiligungspruefung bei gesetztem release_version_id, vor CreateProposal"
      pattern: "GroupParticipatesInReleaseVersion"
---

<objective>
Der Member-Vorschlagspfad bekommt dieselbe Versions-Dimension wie das Leader-CRUD: createProposalRequest traegt ein optionales release_version_id, der Handler validiert bei gesetztem Wert die Gruppenbeteiligung (D-03, GroupParticipatesInReleaseVersion) und reicht release_version_id an repository.ProposalInput durch (Feld bereits in Plan 02 ergaenzt).

Purpose: D-08 (beide Eingabeflaechen) + Pitfall 5 (D-03 darf im Member-Pfad nicht uebergangen werden). Verhindert, dass ein Member eine inkonsistente Version vorschlaegt, die das Leader-Formular ablehnen wuerde.
Output: erweitertes createProposalRequest, neue Checker-Abhaengigkeit (DB-Impl im selben File, kein main.go-Eingriff), D-03-Block im CreateProposal-Handler, Stub-Test fuer 422 bei gruppen-fremder Version.

Hinweis zur Eingabe-UI: Eine Frontend-Vorschlags-Form existiert derzeit NICHT (frontend/src/app/me/contributions/page.tsx listet nur Beitraege; kein Submit-Formular und kein api.ts-Aufrufer von /me/contribution-proposals). Das gruppen-gefilterte Dropdown fuer den Member-Vorschlag (D-08/D-09 UI-Anteil) kann erst gebaut werden, wenn diese Form existiert (Phase-65-Restscope). Dieser Plan haertet die Backend-Annahme des Feldes ab, sodass die spaetere Member-Form das Feld nur noch senden muss.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/67-release-episode-credits/67-CONTEXT.md
@.planning/phases/67-release-episode-credits/67-RESEARCH.md
@.planning/phases/67-release-episode-credits/67-PATTERNS.md

<interfaces>
contribution_proposals_me_handler.go:
  createProposalRequest (Z. 179-187): FansubGroupID, AnimeID, FansubGroupMemberID, RoleCodes, Note, StartedYear, EndedYear.
  CreateProposal (Z. 199+): Sicherheitskette Auth -> Pflichtfelder -> Member-Identitaet -> Ownership -> Rollenvalidierung -> DB -> Audit. req.FansubGroupID ist die Gruppe; ProposalInput-Aufbau Z. 260-267; CreateProposal-Aufruf Z. 269.
  Interface-DI-Muster: ProposalRepository, RolesRepository, MemberResolver, OwnershipChecker, MembershipsLister; DB-Impls als private structs mit *pgxpool.Pool (z. B. dbOwnershipChecker Z. 112). Konstruktor NewContributionProposalsMeHandler(proposalRepo, rolesRepo, db, auditLogRepo) baut die DB-Impls aus db (Z. 71-86).
  Stub-Test-Muster: contribution_proposals_me_test.go (proposalRepoStub etc.).

repository.ProposalInput hat in Plan 02 das Feld ReleaseVersionID *int64 erhalten.
repository.AnimeContributionsRepository.GroupParticipatesInReleaseVersion(ctx, fansubGroupID, releaseVersionID int64) (bool, error) existiert (Plan 02).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: release_version_id + D-03-Check im Member-Proposal-Handler</name>
  <files>backend/internal/handlers/contribution_proposals_me_handler.go, backend/internal/handlers/contribution_proposals_me_test.go</files>
  <read_first>
    - backend/internal/handlers/contribution_proposals_me_handler.go (komplett: Interfaces Z. 22-58, Konstruktor Z. 70-86, DB-Impls Z. 88-175, createProposalRequest Z. 179, CreateProposal Z. 197-290)
    - backend/internal/handlers/contribution_proposals_me_test.go (Stub-Muster, TestCreateProposal_* )
    - backend/internal/repository/anime_contributions_release_lookup_repository.go (GroupParticipatesInReleaseVersion-Signatur, Plan 02)
    - .planning/phases/67-release-episode-credits/67-PATTERNS.md (Pitfall 5, Member-Pfad)
    - .planning/phases/67-release-episode-credits/67-UI-SPEC.md (D-03-Fehlertext)
  </read_first>
  <behavior>
    - createProposalRequest mit release_version_id=null verhaelt sich wie bisher.
    - release_version_id gesetzt + Gruppe beteiligt -> Vorschlag wird angelegt (201/200 wie bisher).
    - release_version_id gesetzt + Gruppe NICHT beteiligt -> 422 mit deutscher Meldung.
  </behavior>
  <action>
    1. createProposalRequest um ReleaseVersionID *int64 json:"release_version_id" erweitern.
    2. Neues Interface ReleaseVersionParticipationChecker mit GroupParticipatesInReleaseVersion(ctx, fansubGroupID, releaseVersionID int64) (bool, error). Als Handler-Feld releaseVersionChecker aufnehmen. DB-Impl: entweder das bestehende *repository.AnimeContributionsRepository nutzen (falls bereits als Abhaengigkeit verfuegbar) ODER eine kleine private struct dbReleaseVersionParticipationChecker{db *pgxpool.Pool} im selben File, die release_version_groups via EXISTS abfragt — analog zu GroupParticipatesInReleaseVersion. Im Konstruktor aus dem vorhandenen db-Pool bauen, KEINE Signaturaenderung des Konstruktors und KEIN main.go-Eingriff.
    3. Im CreateProposal-Handler, NACH dem Ownership-Check und VOR dem ProposalInput-Aufbau: wenn req.ReleaseVersionID != nil, h.releaseVersionChecker.GroupParticipatesInReleaseVersion(ctx, req.FansubGroupID, *req.ReleaseVersionID) aufrufen; Repo-Fehler -> internalError; !participates -> HTTP 422 mit gin.H message "Diese Gruppe war an der gewaehlten Release-Version nicht beteiligt." (korrekte Umlaute).
    4. ReleaseVersionID in den ProposalInput-Aufbau (Z. 260-267) als ReleaseVersionID: req.ReleaseVersionID aufnehmen.
    5. Stub-Test in contribution_proposals_me_test.go: einen releaseVersionCheckerStub ergaenzen (oder bestehende Stub-Struktur erweitern), Test TestCreateProposal_ForeignReleaseVersionRejected -> erwartet 422; bestehende Tests gruen halten (Checker liefert bei nil-release_version_id keinen Aufruf).
    Datei <= 450 Zeilen halten.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/handlers/... -run "CreateProposal|ReleaseVersion" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... erfolgreich; Konstruktor-Signatur unveraendert (kein main.go-Eingriff).
    - createProposalRequest.ReleaseVersionID (*int64) existiert; ProposalInput erhaelt release_version_id.
    - GroupParticipatesInReleaseVersion wird nur bei gesetztem release_version_id aufgerufen; 422 bei gruppen-fremder Version mit korrekten Umlauten.
    - go test ./internal/handlers/... -run "CreateProposal|ReleaseVersion" gruen, inklusive neuem Foreign-Version-Test.
    - contribution_proposals_me_handler.go <= 450 Zeilen.
  </acceptance_criteria>
  <done>Member-Vorschlag nimmt release_version_id an und erzwingt D-03 serverseitig; bestehende Proposal-Tests bleiben gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated Member -> Proposal-API | Member sendet release_version_id im Vorschlag (untrusted) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-05-CG | Tampering / Elevation | Member-Vorschlag mit fremder release_version_id | mitigate | D-03 GroupParticipatesInReleaseVersion-Check im Member-Pfad (Pitfall 5) -> 422. |
| T-67-05-OWN | Elevation | fansub_group_member_id-Spoofing | mitigate | Bestehender Ownership-Check (MemberIDForFansubGroupMember == verifiedMemberID) bleibt vorgelagert. |
| T-67-05-SQLI | Tampering | release_version_id in Query | mitigate | Parametrisierte EXISTS-Query ($1/$2). |
</threat_model>

<verification>
- `cd backend && go build ./...` gruen.
- `go test ./internal/handlers/... -run "CreateProposal|ReleaseVersion" -count=1` gruen.
</verification>

<success_criteria>
- D-08 Backend-Anteil: Member-Vorschlagspfad nimmt das optionale release_version_id an und validiert es (D-03, Pitfall 5).
- Eingabe-UI fuer den Member-Vorschlag wird nachgezogen, sobald die Vorschlags-Form existiert (Phase-65-Restscope) — als Folgearbeit notiert.
</success_criteria>

<output>
Create `.planning/phases/67-release-episode-credits/67-05-SUMMARY.md` when done
</output>
