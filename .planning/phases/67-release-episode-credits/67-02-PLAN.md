---
phase: 67-release-episode-credits
plan: 02
type: execute
wave: 2
depends_on: ["67-01"]
files_modified:
  - backend/internal/repository/anime_contributions_release_lookup_repository.go
  - backend/internal/repository/anime_contributions_release_lookup_repository_test.go
  - backend/internal/repository/anime_contributions_repository.go
  - backend/internal/repository/anime_contributions_upsert_repository.go
  - backend/internal/repository/anime_contributions_proposal_repository.go
  - backend/internal/handlers/fansub_anime_contributions_handler.go
autonomous: true
requirements: [P67-SC1]

must_haves:
  truths:
    - "Ein Leader kann eine Contribution mit gesetztem release_version_id speichern, sofern die Gruppe an dieser Version beteiligt ist; andernfalls antwortet das Backend mit 422."
    - "Der Upsert nutzt das vierspaltige ON CONFLICT-Target, sodass ein versions-spezifischer Eintrag NICHT den anime-weiten Eintrag desselben Members ueberschreibt."
    - "release_version_id ist beim Bearbeiten setzbar, aenderbar und entfernbar (Patch)."
    - "Der Member-Vorschlagspfad (ProposalInput/CreateProposal) traegt release_version_id mit und durchlaeuft dieselbe Beteiligungspruefung."
  artifacts:
    - path: "backend/internal/repository/anime_contributions_release_lookup_repository.go"
      provides: "GroupParticipatesInReleaseVersion + gruppen-gefiltertes Dropdown-Query"
      contains: "GroupParticipatesInReleaseVersion"
    - path: "backend/internal/repository/anime_contributions_upsert_repository.go"
      provides: "Upsert mit release_version_id im vierspaltigen ON CONFLICT-Target"
      contains: "release_version_id"
  key_links:
    - from: "fansub_anime_contributions_handler.go (Create/Update)"
      to: "GroupParticipatesInReleaseVersion"
      via: "Handler-Validierung bei gesetztem release_version_id (D-03)"
      pattern: "GroupParticipatesInReleaseVersion"
    - from: "anime_contributions_upsert_repository.go"
      to: "anime_contributions(release_version_id)"
      via: "ON CONFLICT vierspaltig"
      pattern: "on conflict \\(fansub_group_id, anime_id, fansub_group_member_id, release_version_id\\)"
---

<objective>
Backend-Schreibpfad fuer das optionale release_version_id: neue Repo-Methode GroupParticipatesInReleaseVersion (D-03) und das gruppen-gefilterte Dropdown-Query in einer neuen Datei, Erweiterung von AnimeContributionInput + Upsert (vierspaltiges ON CONFLICT-Target, Pitfall 1), Patch-Pfad (D-10), Leader-Handler-Validierung (422 bei gruppen-fremder Version) und additive Erweiterung des Member-Proposal-Pfades.

Purpose: P67-SC1 Schreib- und Konsistenz-Anteil. Verhindert Cross-Group-Daten-Tampering (T-67-02-CG) und das faelschliche Upserten versions-spezifischer Eintraege auf die anime-weite Zeile.
Output: GroupParticipatesInReleaseVersion + Dropdown-Lookup, erweiterter Upsert/Input/Patch, validierender Leader-Handler, erweiterter Proposal-Pfad, Repo-Test fuer das Lookup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/67-release-episode-credits/67-CONTEXT.md
@.planning/phases/67-release-episode-credits/67-RESEARCH.md
@.planning/phases/67-release-episode-credits/67-PATTERNS.md

<interfaces>
EXISTS-Guard-Muster (anime_contributions_repository.go:90-102):
  func (r *AnimeContributionsRepository) MemberBelongsToFansub(ctx, memberID, fansubGroupID int64) (bool, error)
  -> Query: SELECT EXISTS(SELECT 1 FROM hist_fansub_group_members WHERE id=$1 AND fansub_group_id=$2)

AnimeContributionInput (anime_contributions_repository.go:36-46): Felder FansubGroupMemberID, RoleCodes, Status, StartedYear, EndedYear, Note, IsPublicOnAnimePage, IsPublicOnMemberProfile, CreatedBy.
AnimeContributionPatchInput (anime_contributions_repository.go:50-59): Pointer-/Doppelpointer-Felder; nil = nicht aendern.

Upsert (anime_contributions_upsert_repository.go:32-70): INSERT ... ON CONFLICT (fansub_group_id, anime_id, fansub_group_member_id) DO UPDATE SET ...; isForeignKeyViolation -> ErrNotFound.

Leader-Handler (fansub_anime_contributions_handler.go):
  animeContributionCreateRequest (Z. 48-57), animeContributionPatchRequest (Z. 59-67, Doppelpointer fuer nullable).
  Cross-Group-Guard via MemberBelongsToFansub mit 422 (Z. 162-176).
  Audit-Payload map[string]any (Z. 244-254, 356-366).
  CreateOrUpdate-Aufruf Z. 221; Update-Aufruf Z. 341.

Proposal (anime_contributions_proposal_repository.go): type ProposalInput (Z. 15) mit FansubGroupMemberID/RoleCodes; func (r *AnimeContributionsRepository) CreateProposal(ctx, fansubGroupID, animeID int64, input ProposalInput) (Z. 41) INSERT INTO anime_contributions (fansub_group_id, anime_id, fansub_group_member_id, status, ...).

Dropdown-Query-Muster (RESEARCH Pattern 3, abgeleitet aus ListFansubAnimeReleases in admin_content_fansub_releases.go):
  SELECT rv.id, ep.episode_number, ep.sort_index, rv.version
  FROM release_versions rv JOIN release_version_groups rvg ON rvg.release_version_id=rv.id
  JOIN fansub_releases fr ON fr.id=rv.release_id JOIN episodes ep ON ep.id=fr.episode_id
  WHERE rvg.fansub_group_id=$1 AND ep.anime_id=$2
  ORDER BY COALESCE(ep.sort_index, 2147483647), ep.id, rv.version

450-Zeilen-Limit: anime_contributions_repository.go=447, fansub_anime_contributions_handler.go=434. NEUE Methoden in die NEUE lookup-Datei; Handler kompakt halten.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lookup-Repo (GroupParticipatesInReleaseVersion + Dropdown-Query) + Test</name>
  <files>backend/internal/repository/anime_contributions_release_lookup_repository.go, backend/internal/repository/anime_contributions_release_lookup_repository_test.go</files>
  <read_first>
    - backend/internal/repository/anime_contributions_repository.go (MemberBelongsToFansub-Muster Z. 88-102, Receiver-Typ, Imports)
    - backend/internal/repository/admin_content_fansub_releases.go (ListFansubAnimeReleases: JOIN-Kette + NULL-safe Sortierung; Header-Konvention Z. 1-22)
    - backend/internal/repository/anime_contributions_public_repository.go (DTO-/JSON-Tag-Konventionen)
    - database/migrations/0035_add_release_tables.up.sql (release_versions/fansub_releases/release_version_groups/episodes-Spalten; episode_number ist VARCHAR)
    - .planning/phases/67-release-episode-credits/67-PATTERNS.md (Abschnitt zur lookup-Datei)
  </read_first>
  <behavior>
    - GroupParticipatesInReleaseVersion liefert true, wenn eine Zeile in release_version_groups mit (release_version_id, fansub_group_id) existiert; sonst false.
    - Das Dropdown-Lookup liefert pro Release-Version der Gruppe eine Zeile (release_version_id, episode_number, version), sortiert nach Episode (sort_index NULL-safe) dann version.
    - Falls die Gruppe an keiner Version des Anime beteiligt ist, liefert das Lookup eine leere Liste (kein Fehler).
  </behavior>
  <action>
    Lege anime_contributions_release_lookup_repository.go an (package repository, Receiver *AnimeContributionsRepository, derselbe Pool). Methoden:
    1. GroupParticipatesInReleaseVersion(ctx, fansubGroupID, releaseVersionID int64) (bool, error) im exakten EXISTS-Muster von MemberBelongsToFansub; Query gegen release_version_groups WHERE release_version_id=$1 AND fansub_group_id=$2; parametrisiert ($1/$2), nie String-Konkatenation (SQL-Injection-Mitigation).
    2. ListGroupReleaseVersionsForAnime(ctx, fansubGroupID, animeID int64) ([]GroupReleaseVersionOption, error) mit dem Dropdown-Query aus dem interfaces-Block; neuer Struct GroupReleaseVersionOption{ ReleaseVersionID int64 json:"release_version_id"; EpisodeNumber string json:"episode_number"; Version string json:"version" } (snake_case JSON, Spiegel zu frontend types). episode_number als string scannen (VARCHAR).
    Deutscher Doc-Comment-Header wie admin_content_fansub_releases.go. Datei deutlich unter 450 Zeilen halten.
    Schreibe anime_contributions_release_lookup_repository_test.go mit DB-gestuetzten Tests (sofern Testinfrastruktur vorhanden ist wie in den repository-Tests); andernfalls minimaler Aufbau, der Query-Form und Methodensignatur sichert. Test-Run-Name muss "ReleaseVersion" enthalten, damit -run ReleaseVersion greift.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/repository/... -run ReleaseVersion -count=1</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... erfolgreich.
    - GroupParticipatesInReleaseVersion und ListGroupReleaseVersionsForAnime existieren mit den genannten Signaturen am Receiver *AnimeContributionsRepository.
    - Queries sind vollstaendig parametrisiert ($1/$2).
    - Datei < 450 Zeilen.
  </acceptance_criteria>
  <done>Neue Lookup-Datei kompiliert, Test mit "ReleaseVersion"-Namen laeuft (gruen oder skip ohne DB).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Input + Upsert + Patch + Proposal um release_version_id erweitern</name>
  <files>backend/internal/repository/anime_contributions_repository.go, backend/internal/repository/anime_contributions_upsert_repository.go, backend/internal/repository/anime_contributions_proposal_repository.go</files>
  <read_first>
    - backend/internal/repository/anime_contributions_upsert_repository.go (CreateOrUpdate vollstaendig, ON CONFLICT-Stelle Z. 48)
    - backend/internal/repository/anime_contributions_repository.go (AnimeContributionInput Z. 36-46, AnimeContributionPatchInput Z. 50-59, Update-Methode)
    - backend/internal/repository/anime_contributions_proposal_repository.go (ProposalInput Z. 15, CreateProposal INSERT Z. 41+)
    - .planning/phases/67-release-episode-credits/67-PATTERNS.md (Upsert-MOD-Abschnitt, Pitfall 1)
  </read_first>
  <behavior>
    - CreateOrUpdate mit release_version_id=NULL upsertet auf den anime-weiten Eintrag; mit gesetztem release_version_id entsteht/aktualisiert ein separater versions-spezifischer Eintrag (kein Overwrite des anime-weiten).
    - Patch mit ReleaseVersionID-Doppelpointer: nil = nicht aendern, *nil = auf NULL setzen, *wert = setzen.
    - CreateProposal traegt release_version_id in den INSERT.
  </behavior>
  <action>
    1. AnimeContributionInput um ReleaseVersionID *int64 erweitern. AnimeContributionPatchInput um ReleaseVersionID **int64 erweitern (Doppelpointer-Muster wie StartedYear, D-10).
    2. In CreateOrUpdate: release_version_id in INSERT-Spaltenliste + VALUES als neuen Parameter aufnehmen; ON CONFLICT-Target auf (fansub_group_id, anime_id, fansub_group_member_id, release_version_id) erweitern; release_version_id NICHT ins DO UPDATE SET (Teil des Conflict-Keys). isForeignKeyViolation/ErrNotFound-Mapping unveraendert lassen (faengt ungueltige release_version_id mit ab).
    3. In der Update-Methode (anime_contributions_repository.go): release_version_id in den bestehenden dynamischen/COALESCE-Patch-Stil aufnehmen, sodass ReleaseVersionID-Doppelpointer korrekt setzen/auf-NULL/unveraendert abbildet. Dem vorhandenen Patch-Mechanismus der Datei folgen.
    4. ProposalInput um ReleaseVersionID *int64 erweitern; CreateProposal-INSERT um release_version_id-Spalte + Parameter ergaenzen (additiv, keine Verhaltensaenderung bei NULL).
    Keine user-facing Strings hier; Repo-Schicht. 450-Zeilen-Limit von anime_contributions_repository.go (447) beachten: falls die Update-Erweiterung das Limit reisst, betroffene Methode minimal halten oder kommentarsparsam ergaenzen; keine neue Datei noetig wenn die Aenderung nur wenige Zeilen ist.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/repository/... -run "ReleaseVersion|Contribution" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... erfolgreich.
    - ON CONFLICT-Target in CreateOrUpdate ist vierspaltig inkl. release_version_id.
    - AnimeContributionInput.ReleaseVersionID (*int64), AnimeContributionPatchInput.ReleaseVersionID (**int64), ProposalInput.ReleaseVersionID (*int64) existieren.
    - CreateProposal-INSERT enthaelt release_version_id.
    - anime_contributions_repository.go bleibt <= 450 Zeilen (CLAUDE.md).
  </acceptance_criteria>
  <done>Upsert, Patch-Input, Proposal tragen release_version_id; vierspaltiges ON CONFLICT aktiv; Build gruen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Leader-Handler D-03-Validierung + Request-Structs + Audit</name>
  <files>backend/internal/handlers/fansub_anime_contributions_handler.go</files>
  <read_first>
    - backend/internal/handlers/fansub_anime_contributions_handler.go (Create Z. 122-257, Update Z. 261-369, Request-Structs Z. 48-67, Audit Z. 244-254)
    - backend/internal/repository/anime_contributions_release_lookup_repository.go (GroupParticipatesInReleaseVersion-Signatur aus Task 1)
    - .planning/phases/67-release-episode-credits/67-PATTERNS.md (Handler-MOD-Abschnitt, Pitfall 5)
    - .planning/phases/67-release-episode-credits/67-UI-SPEC.md (Fehlertext D-03)
  </read_first>
  <behavior>
    - Create/Update mit release_version_id, an der die Gruppe beteiligt ist -> 201/200.
    - Create/Update mit gruppen-fremder release_version_id -> 422 mit deutscher Meldung.
    - Create/Update ohne release_version_id (NULL) -> unveraendertes Verhalten (kein Beteiligungs-Check).
  </behavior>
  <action>
    1. animeContributionCreateRequest um ReleaseVersionID *int64 json:"release_version_id" erweitern; animeContributionPatchRequest um ReleaseVersionID **int64 json:"release_version_id" (Doppelpointer wie StartedYear, D-10).
    2. In CreateAnimeContribution direkt nach dem MemberBelongsToFansub-Block: wenn req.ReleaseVersionID != nil, h.contributionsRepo.GroupParticipatesInReleaseVersion(ctx, fansubID, *req.ReleaseVersionID) aufrufen; bei Repo-Fehler internalError; bei !participates HTTP 422 mit gin.H message "Diese Gruppe war an der gewaehlten Release-Version nicht beteiligt." (korrekte Umlaute, an UI-SPEC angelehnt). ReleaseVersionID in das repository.AnimeContributionInput durchreichen.
    3. In UpdateAnimeContribution analog: wenn req.ReleaseVersionID != nil && *req.ReleaseVersionID != nil, denselben Beteiligungs-Check mit dem Zielwert fahren; ReleaseVersionID in AnimeContributionPatchInput durchreichen.
    4. Audit-Payload in Create und Update um "release_version_id" ergaenzen (Wert oder nil).
    Wegen 434 Zeilen: Validierungslogik kompakt; falls das Limit reisst, eine kleine Hilfsfunktion validateReleaseVersionParticipation in eine neue Datei backend/internal/handlers/fansub_contributions_validation.go auslagern (package handlers) und aus beiden Handlern aufrufen. Datei darf 450 Zeilen nicht ueberschreiten.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go vet ./internal/handlers/... && go test ./internal/handlers/... -run ReleaseVersion -count=1</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... und go vet erfolgreich.
    - GroupParticipatesInReleaseVersion wird in BEIDEN Schreibpfaden (Create + Update) nur bei gesetztem release_version_id aufgerufen.
    - 422-Antwort nutzt korrekte deutsche Umlaute (kein ae/oe/ue/ss-Ersatz).
    - Audit-Payload enthaelt release_version_id.
    - fansub_anime_contributions_handler.go (und ggf. neue Validierungsdatei) <= 450 Zeilen.
  </acceptance_criteria>
  <done>Leader-Create/Update validiert die Versionsbeteiligung serverseitig und gibt 422 bei gruppen-fremder Version; Build/vet gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated Leader -> Admin-API | Leader sendet release_version_id im Create/Update-Body |
| Authenticated Member -> Proposal-API | Member sendet release_version_id im Vorschlag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-02-CG | Tampering / Elevation | Create/Update mit fremder release_version_id | mitigate | D-03 Handler-Validierung GroupParticipatesInReleaseVersion -> 422; in beiden Schreibpfaden. |
| T-67-02-SQLI | Tampering | release_version_id in Queries | mitigate | Parametrisierte pgx-Queries ($1/$2), nie String-Konkatenation. |
| T-67-02-IDOR | Elevation | Update bestehender Contribution | mitigate | Bestehender CanForFansubGroup(MembersManage)-Check bleibt vorgelagert; Versions-Check additiv. |
| T-67-02-DUP | Tampering | Upsert ON CONFLICT-Target | mitigate | Vierspaltiges Target verhindert Overwrite des anime-weiten Eintrags durch versions-spezifischen (Pitfall 1). |
| T-67-02-SC | Tampering | go/pgx Module installs | accept | Keine neuen Pakete (RESEARCH: Package Legitimacy Audit nicht zutreffend). |
</threat_model>

<verification>
- `cd backend && go build ./...` und `go vet ./internal/handlers/...` gruen.
- `go test ./internal/repository/... -run "ReleaseVersion|Contribution" -count=1` gruen.
- `go test ./internal/handlers/... -run ReleaseVersion -count=1` gruen.
</verification>

<success_criteria>
- P67-SC1 Schreib-/Konsistenz-Anteil erfuellt: optionales release_version_id wird angenommen, validiert (D-03) und ohne Overwrite gespeichert (vierspaltiges ON CONFLICT).
- Member-Proposal-Pfad traegt release_version_id mit (D-08, Pitfall 5).
</success_criteria>

<output>
Create `.planning/phases/67-release-episode-credits/67-02-SUMMARY.md` when done
</output>
