---
phase: 164-oeffentliche-anime-seite-episode-release-ui-read-model-infinite-scroll
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/fansubs/FansubGroupPicker.tsx
  - frontend/src/components/fansubs/FansubGroupPicker.module.css
  - frontend/src/components/fansubs/FansubGroupContext.tsx
  - frontend/src/components/fansubs/FansubGroupContext.module.css
  - frontend/src/app/anime/[id]/page.module.css
  - frontend/src/components/fansubs/FansubGroupPicker.test.tsx
  - frontend/src/components/fansubs/FansubGroupContext.test.tsx
autonomous: true
gap_closure: true
requirements: [REQ-164-05, REQ-164-09]

must_haves:
  truths:
    - "GAP-04: The 'Fansub-Gruppe' eyebrow label above the chips is clearly legible against the actual dark background it renders on."
    - "GAP-05: Group chips have a visibly smaller corner radius than a full pill."
    - "GAP-06: There is visible spacing between individual chips and between the chip row and the FansubGroupContext card below it."
    - "GAP-07: The selected group chip has a filled accent background (not just an outline), passing WCAG AA text contrast."
    - "GAP-08: The FansubGroupContext card uses the same neutral glass style as the episode/release cards, not a dominant white card."
    - "GAP-09: 'Mehr lesen ->' uses a design-system primitive/token-based style instead of the browser-default blue link; 'Zur Fansub-Gruppe' and 'Zum Projekt' are right-aligned."
    - "GAP-10: The tag chips below the anime description are clearly legible (WCAG AA) against their actual rendered background, independent of the info-card's background gradient opacity at that scroll position."
  artifacts:
    - path: "frontend/src/components/fansubs/FansubGroupContext.module.css"
      provides: "Neutral glass card style (--glass-surface/--glass-border/--glass-text tokens) matching EpisodeGlassCard.module.css's .card, replacing the opaque white card"
  key_links:
    - from: "frontend/src/components/fansubs/FansubGroupContext.tsx"
      to: "frontend/src/components/ui/Button.tsx"
      via: "variant=\"text\" Button used for the \"Mehr lesen ->\" link instead of a raw next/link <Link>"
      pattern: "variant=\"text\""
---

<objective>
Close the seven pure-frontend visual/contrast gaps from the 2026-09-18 live UAT (`164-UAT.md`
GAP-04 through GAP-10, excluding GAP-03 which lives in `episodePreviewFormat.ts` and is handled by
164-12) in the fansub group picker, the fansub group context card, and the anime detail page's tag
chips.

Purpose: The Auftraggeber's live UAT on Naruto/11eyes found the "Fansub-Gruppe" label, the group
chips, the FansubGroupContext card, and the tag chips all suffer from insufficient contrast or a
stylistic mismatch against the new glass-styled episode/release cards this phase introduced. None of
these fixes require backend changes or new data -- they are token/style corrections within already
shipped Phase 162/160/164 components.

Output: `FansubGroupPicker`/`FansubGroupContext` render with corrected spacing, radius, active-state
contrast, and a neutral glass card matching `EpisodeGlassCard`'s established style; the anime page's
tag chips use a self-contained, contrast-safe dark chip style independent of the info-card's
gradient opacity at their scroll position.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164-oeffentliche-anime-seite-episode-release-ui-read-model-infinite-scroll/164-UAT.md
@.planning/phases/164-oeffentliche-anime-seite-episode-release-ui-read-model-infinite-scroll/164-UI-SPEC.md

<interfaces>
<!-- Established local "glass" token block (neutral, no classification tint) -- GAP-08's target style,
copy the exact values, do not invent new ones (frontend/src/components/fansubs/EpisodeGlassCard.module.css:5-30):
.card {
  --glass-surface: rgba(255, 255, 255, 0.06);
  --glass-surface-strong: rgba(255, 255, 255, 0.10);
  --glass-border: rgba(255, 255, 255, 0.14);
  --glass-border-strong: rgba(255, 255, 255, 0.22);
  --glass-text: rgba(255, 255, 255, 0.92);
  --glass-text-muted: rgba(255, 255, 255, 0.60);
  background: var(--glass-surface);
  border: 1px solid var(--glass-border);
  border-radius: var(--radius-md);
  backdrop-filter: blur(14px) saturate(1.15);
  -webkit-backdrop-filter: blur(14px) saturate(1.15);
}
@supports (backdrop-filter: blur(20px)) { .card { background: var(--glass-surface); backdrop-filter: blur(14px) saturate(1.15); } }

FansubGroupPicker/FansubGroupContext render inside FansubVersionBrowser.tsx's dark
`.episodesSection` (164-UI-SPEC.md Color table, "Dominant (60%)" row: `rgba(255,255,255,0.04)` on a
dark page background) -- this is the "real background" GAP-04/07/08 must contrast against, not a
light card background.

Existing "filled selected chip, computed-AA-contrast" precedent (frontend/src/components/anime/LetterFilter.module.css:27-31):
.active { color: white; background: var(--accent-primary); } -- do NOT copy this literally, its
contrast is ~3.6:1 against white text (fails 4.5:1 AA for 14px text). Use `var(--accent-dark)`
(= `var(--color-primary-active)` = #3f60ad) instead, which computes to ~6:1 against white -- passes AA.
Both tokens already exist in frontend/src/styles/globals.css (lines 6, 31).

Radius tokens (frontend/src/styles/globals.css:113-115): --radius-sm: 6px; --radius-md: 12px;
--radius-lg: 12px. Chips currently use `border-radius: 999px` (full pill, GAP-05's complaint).

Button primitive's "styled as a link" variant (frontend/src/components/ui/Button.tsx, `variant="text"`;
frontend/src/components/ui/ui.module.css:126-145 `.buttonText` -- underlined, `color: inherit` by
default, supports `href` for real anchor rendering exactly like the existing
`<Button href={...} variant="secondary">` calls already used in this same file for "Zur Fansub-Gruppe").

Tag chips (GAP-10) live in the anime detail page's `.infoCard`, whose background is a top-to-bottom
opacity gradient (frontend/src/app/anime/[id]/page.module.css:334-341, from
rgba(255,255,255,0.95) at the top fading to rgba(255,255,255,0.08) at the bottom) -- the description
and tags render low enough in that gradient that the card is nearly transparent there, revealing a
dark cover/banner behind it. The existing `--surface-sunken`/`--text-primary`/`--text-muted` tokens
used today (page.module.css:407-448) are tuned for an opaque LIGHT card and lose contrast in that
near-transparent zone. Fix per the Auftraggeber's own suggested remedy ("entweder weiß oder viel
dunkler"): make the tag chip self-contained (its own dark, sufficiently opaque background + light
text), independent of whatever is behind the gradient at that scroll position -- do not attempt to
compute contrast against the gradient itself (not a fixed value).
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix FansubGroupPicker contrast, radius, spacing, and selected-chip state (GAP-04, 05, 06, 07)</name>
  <files>frontend/src/components/fansubs/FansubGroupPicker.tsx, frontend/src/components/fansubs/FansubGroupPicker.module.css, frontend/src/components/fansubs/FansubGroupPicker.test.tsx</files>
  <read_first>
    frontend/src/components/fansubs/FansubGroupPicker.tsx (full 73-line file)
    frontend/src/components/fansubs/FansubGroupPicker.module.css (full 45-line file)
    frontend/src/components/fansubs/FansubGroupPicker.test.tsx (existing test coverage, to extend without breaking)
  </read_first>
  <action>
    In `FansubGroupPicker.module.css`: (GAP-04) change `.eyebrowLabel`'s `color: var(--text-muted)` to a
    light-on-dark tone appropriate for the real dark `.episodesSection` background -- declare a local
    `--glass-text-muted: rgba(255, 255, 255, 0.60)` custom property on the component's root selector (this
    file currently declares no glass tokens at all) and use it, matching the exact value already
    established in `EpisodeGlassCard.module.css`/`ReleasePreviewRow.module.css` for the identical parent
    background. (GAP-05) change `.chip`'s `border-radius: 999px` to `var(--radius-md)`. (GAP-06) increase
    `.chipRow`'s `gap` from `var(--space-2)` (8px) to `var(--space-3)` (12px); add a `margin-bottom:
    var(--space-5)` (24px) to the component's outer wrapping element (currently a bare `<div>` in
    `FansubGroupPicker.tsx` with no class -- give it a new `.wrapper` class in the CSS module and apply
    it in the TSX) so there is visible space before the `FansubGroupContext` card that follows it in
    `FansubVersionBrowser.tsx`. (GAP-07) change `.chip[aria-pressed='true']` from
    `background: var(--surface-sunken); border: 2px solid var(--accent-primary);` to
    `background: var(--accent-dark); border: 2px solid var(--accent-dark); color: #ffffff;` -- also add
    the same local `--glass-text` custom property block used elsewhere in this phase if a light default
    chip text color needs declaring for the unselected state's readability against the same dark
    background (verify the existing `.chip` text color `var(--text-primary)` -- a dark-on-light color --
    also needs the same light-on-dark correction as `.eyebrowLabel`; if so, fix it identically).
    Update `FansubGroupPicker.test.tsx` only if an existing snapshot/DOM assertion on class names or the
    wrapper's markup structure breaks from the new `.wrapper` class -- do not add new tests purely for
    CSS values (no automated contrast-ratio test tooling in this repo; contrast is verified by the
    documented arithmetic in this plan's acceptance criteria and the phase's eventual re-UAT).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/fansubs/FansubGroupPicker.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "border-radius: var(--radius-md)" frontend/src/components/fansubs/FansubGroupPicker.module.css` returns 1 for `.chip`.
    - `grep -c "999px" frontend/src/components/fansubs/FansubGroupPicker.module.css` returns 0.
    - `grep -c "var(--accent-dark)" frontend/src/components/fansubs/FansubGroupPicker.module.css` returns at least 2 (background + border on the selected state).
    - `grep -c "margin-bottom: var(--space-5)" frontend/src/components/fansubs/FansubGroupPicker.module.css` returns 1.
    - `grep -c "var(--space-3)" frontend/src/components/fansubs/FansubGroupPicker.module.css` returns at least 1 for `.chipRow`'s gap.
    - Contrast arithmetic documented in the task commit/summary: `--accent-dark` (#3f60ad) against white text computes to >=4.5:1 (relative luminance method, matches this plan's Interfaces section derivation of ~6:1).
    - `npx vitest run src/components/fansubs/FansubGroupPicker.test.tsx` passes with 0 failures.
  </acceptance_criteria>
  <done>The eyebrow label and unselected chip text are legible against the dark section background, chips have a moderate (not full-pill) radius, visible spacing separates chips from each other and from the card below, and the selected chip has a filled, AA-contrast-passing accent background.</done>
</task>

<task type="auto">
  <name>Task 2: Convert FansubGroupContext to the neutral glass card style and fix its link/button styling (GAP-08, GAP-09)</name>
  <files>frontend/src/components/fansubs/FansubGroupContext.tsx, frontend/src/components/fansubs/FansubGroupContext.module.css, frontend/src/components/fansubs/FansubGroupContext.test.tsx</files>
  <read_first>
    frontend/src/components/fansubs/FansubGroupContext.tsx (full 51-line file)
    frontend/src/components/fansubs/FansubGroupContext.module.css (full 38-line file)
    frontend/src/components/fansubs/EpisodeGlassCard.module.css (`.card`, lines 5-37 -- the exact glass style to reuse, see Interfaces)
    frontend/src/components/ui/Button.tsx (the `variant="text"` + `href` support, lines 1-40)
    frontend/src/components/fansubs/FansubGroupContext.test.tsx (existing test coverage, to extend without breaking)
  </read_first>
  <action>
    In `FansubGroupContext.module.css`: (GAP-08) replace `.card`'s `border: 1px solid var(--border-subtle);
    background: var(--surface-card);` (and `.name`/`.storyPreview`'s `color: var(--text-primary)`) with
    the exact neutral glass block from `EpisodeGlassCard.module.css`'s `.card` (see Interfaces --
    `--glass-surface`, `--glass-border`, `--glass-text`, `backdrop-filter: blur(14px) saturate(1.15)`,
    including the `@supports` fallback block) -- no classification tint (this card is neutral, matching
    GAP-08's explicit "keine Klassifikations-Tönung"). Keep `border-radius: var(--radius-md)` (already
    correct). (GAP-09 link) change `.moreLink`'s styling away from `color: var(--accent-primary);
    text-decoration: none;` -- since the TSX change below switches this element from a raw `<Link>` to a
    `Button variant="text"` (which already supplies its own underline/hover treatment via
    `.buttonText`/`.buttonText:hover` in `ui.module.css`), replace `.moreLink`'s CSS with just a color
    override (e.g. `color: var(--glass-text);` or a slightly accented light tone) plus `font-weight: 600`
    to make it "interessanter" than the default link color while staying token-based -- do not reintroduce
    a hardcoded blue. (GAP-09 alignment) change `.navRow` to `justify-content: flex-end; flex-wrap: wrap;`
    (right-aligns "Zur Fansub-Gruppe"/"Zum Projekt", allows wrapping on mobile per the UAT's explicit
    "mobil darf umbrechen").
    In `FansubGroupContext.tsx`: replace the raw `<Link href={...} className={styles.moreLink}>Mehr
    lesen →</Link>` with `<Button href={...} variant="text" className={styles.moreLink}>Mehr lesen
    →</Button>` (the `Link` import from `next/link` becomes unused for this element -- remove it if
    nothing else in the file still uses `next/link`; `Button` is already imported in this file).
    Update `FansubGroupContext.test.tsx` for the changed DOM shape (an `<a>` rendered by `Button`
    `href` mode has the same tag/role as the previous `next/link` `<Link>`, but verify any test querying
    by `styles.moreLink`'s exact previous class combination or by `Link`-specific test IDs still resolves
    correctly; update assertions that specifically expected a `next/link`-rendered anchor if any exist).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/fansubs/FansubGroupContext.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "surface-card\|border-subtle" frontend/src/components/fansubs/FansubGroupContext.module.css` returns 0 (fully converted to glass tokens).
    - `grep -c "glass-surface\|glass-text\|glass-border" frontend/src/components/fansubs/FansubGroupContext.module.css` returns at least 3.
    - `grep -c "backdrop-filter: blur(14px) saturate(1.15)" frontend/src/components/fansubs/FansubGroupContext.module.css` returns at least 1.
    - `grep -c 'variant="text"' frontend/src/components/fansubs/FansubGroupContext.tsx` returns 1.
    - `grep -c "justify-content: flex-end" frontend/src/components/fansubs/FansubGroupContext.module.css` returns 1.
    - `npx vitest run src/components/fansubs/FansubGroupContext.test.tsx` passes with 0 failures.
  </acceptance_criteria>
  <done>FansubGroupContext renders as a neutral glass card matching the episode/release cards, "Mehr lesen →" uses the Button text-variant primitive instead of a raw blue link, and the two navigation buttons are right-aligned with mobile wrap allowed.</done>
</task>

<task type="auto">
  <name>Task 3: Fix tag chip contrast on the anime detail page (GAP-10)</name>
  <files>frontend/src/app/anime/[id]/page.module.css</files>
  <read_first>
    frontend/src/app/anime/[id]/page.module.css (`.tagsLabel`/`.tagsList`/`.tagChip`/`.tagChip:hover`, lines 400-448, and `.infoCard`'s gradient background, lines 326-341 -- read both to understand why the previous 160-phase contrast fix (commit 9233adf2, dark text on `--surface-sunken`) does not hold at this scroll position)
  </read_first>
  <action>
    Make `.tagChip` self-contained and contrast-safe regardless of what is visible behind the
    `.infoCard`'s near-transparent lower gradient zone: change `background: var(--surface-sunken);
    color: var(--text-primary); border: 1px solid var(--border-subtle);` to a sufficiently opaque dark
    background with light text -- e.g. `background: rgba(20, 18, 16, 0.62); color: rgba(255, 255, 255,
    0.92); border: 1px solid rgba(255, 255, 255, 0.18);` (reuse the same rgba-literal-token convention
    already established for this phase's other glass surfaces rather than inventing a new named custom
    property, per the "feature-local tokens when global light-card tokens don't fit" precedent in
    164-UI-SPEC.md's own design-decision 1/2). Update `.tagChip:hover`'s
    `background: color-mix(in srgb, var(--surface-sunken) 70%, black 8%); border-color:
    var(--border-strong);` to a hover state consistent with the new dark chip (e.g. a lighter-alpha
    version of the same dark background). Update `.tagsLabel`'s `color: var(--text-muted)` to a light
    tone (e.g. `rgba(255, 255, 255, 0.75)`) for the same reason. Do not change `.tagsSection`/`.tagsList`
    layout properties (gap/flex-wrap/margin) -- only the color/background of the label and chips.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p .</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var(--surface-sunken)\|var(--text-primary)\|var(--text-muted)" frontend/src/app/anime/\[id\]/page.module.css` inside the `.tagChip`/`.tagsLabel`/`.tagChip:hover` rule bodies specifically returns 0 (manually confirm by reading the diff, since the grep also matches unrelated rules elsewhere in this large file).
    - `.tagChip`'s new background/text combination computed contrast ratio (relative luminance method) is documented in the task commit/summary as >=4.5:1, following the same "compute and document" convention as commit 9233adf2 (referenced in `.planning/STATE.md`'s Phase 160 entry).
    - `npx tsc --noEmit -p .` passes (CSS module changes don't break type imports).
  </acceptance_criteria>
  <done>Tag chips and their heading are legible with a self-contained dark background and light text, independent of the info-card's gradient opacity at that scroll position, matching the Auftraggeber's own suggested fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure CSS/styling and a link->Button primitive swap; no new data flow, no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-20 | Spoofing | `Button variant="text"` replacing `next/link` `<Link>` for "Mehr lesen →" | accept | `href` is still the same already-validated internal path (`/fansubs/${activeGroup.slug}#geschichte`) built from server-provided data, unchanged by this task; no new client-controlled URL construction. |
</threat_model>

<verification>
`cd frontend && npx vitest run src/components/fansubs/FansubGroupPicker.test.tsx src/components/fansubs/FansubGroupContext.test.tsx && npx tsc --noEmit -p . && npx eslint src/components/fansubs/FansubGroupPicker.tsx src/components/fansubs/FansubGroupContext.tsx src/app/anime/\[id\]/page.module.css` -- 0 new failures.
</verification>

<success_criteria>
All seven visual/contrast gaps (GAP-04 through GAP-10) are fixed using only existing global or
already-established feature-local tokens; no native `<select>/<input>/<textarea>/<button>` introduced;
no invented CSS custom properties beyond the rgba-literal convention already established elsewhere in
this same phase.
</success_criteria>

<output>
Create `.planning/phases/164-oeffentliche-anime-seite-episode-release-ui-read-model-infinite-scroll/164-11-SUMMARY.md` when done
</output>
