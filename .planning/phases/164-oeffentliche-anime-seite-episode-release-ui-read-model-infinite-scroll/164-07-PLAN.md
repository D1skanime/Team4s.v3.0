---
phase: 164-oeffentliche-anime-seite-episode-release-ui-read-model-infinite-scroll
plan: 07
type: execute
wave: 5
depends_on: ["164-01", "164-02", "164-03", "164-04", "164-05", "164-06"]
files_modified:
  - docs/audits/164-performance-gates.md
  - frontend/src/app/dev/episode-windowing-preview/page.tsx
autonomous: false
requirements: [REQ-164-45, REQ-164-46, REQ-164-47, REQ-164-48]

must_haves:
  truths:
    - "Every one of the 12 performance gates in 164-USER-REQUEST.md §47 has a named, executed verification (automated test reference or live measurement), recorded in one audit document, not asserted in prose without evidence."
    - "A live browser session against real Naruto (anime_id=4) proves functional/visual correctness (D-01..D-20 rendering, group filter, Coop, error/loading states) while the isolated fixture-DB tests from plans 164-03/164-05/164-06 are cited as the evidence for scale-dependent gates real Naruto cannot exercise (documented explicitly, not silently skipped)."
    - "Mobile GPU/blur cost is measured via a real Chrome DevTools Performance/Layers profile against a throwaway, dev-only large-dataset rendering harness, not estimated and not sourced from a team4s_v2 data change."
    - "No task in this plan modifies team4s_v2 data; all live steps against the real database are read-only, matching the pattern already established in Phase 163's closing plan (163-05)."
  artifacts:
    - path: "docs/audits/164-performance-gates.md"
      provides: "The one-document gate-by-gate evidence record (12 gates, each with method + result + pass/fail) required before this phase can be considered execution-ready, plus the §53 planning closing-report content"
      contains: "Gate 12"
---

<objective>
Close the phase with a full automated gate, a live-browser Naruto UAT (functional/visual correctness on
real data), a real Chrome DevTools mobile-performance measurement against a throwaway large-dataset
harness (no team4s_v2 write), and one consolidated audit document mapping every one of §47's 12 performance
gates to its actual evidence (D-46/47/48, REQ-164-45/46/47/48).

Purpose: 164-USER-REQUEST.md §52 requires the checker to reject vague treatment of any of the 12 gates; this
plan is where every gate gets a named, checkable answer in one place, and where the phase's own §53-mandated
closing report content gets assembled from real evidence rather than restated intentions.

Output: `docs/audits/164-performance-gates.md` (12-gate evidence table + Naruto UAT notes + mobile-profile
summary + §53 report content), plus the full green automated suite run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164-oeffentliche-anime-seite-episode-release-ui-read-model-infinite-scroll/164-CONTEXT.md
@.planning/phases/164-oeffentliche-anime-seite-episode-release-ui-read-model-infinite-scroll/164-USER-REQUEST.md
@.planning/phases/164-oeffentliche-anime-seite-episode-release-ui-read-model-infinite-scroll/164-VALIDATION.md
@.planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-05-PLAN.md

<interfaces>
The 12 gates (164-USER-REQUEST.md §47 / 164-CONTEXT.md D-46), each must be mapped to one of the following
evidence sources produced by this phase's prior plans, not re-invented here:
  1. Initial Load lädt nur erste Episode-Page -> live curl/Network-tab check against real Naruto
     (:3300/:3000) + plan 164-05's useWindowedEpisodePages.test.ts single-page-no-eviction case.
  2. Keine weiteren Episode-Pages werden vorab geladen -> same live Network-tab check (zero requests beyond
     the SSR page-1 fetch until scroll).
  3. Nächste Page erst bei Scroll-Bedarf -> plan 164-05's bottom-sentinel behavior tests + live scroll
     observation (functional confirmation only, real Naruto has just 1 page so this gate's mechanics are
     proven by the fixture-DB test, not by real-data scroll).
  4. Query-Anzahl wächst nicht linear -> plan 164-01's exact-count budget assertions (3/4 statements) +
     plan 164-03's 52-episode scale fixture proving the same exact count holds at scale.
  5. Episode-Aufklappen erzeugt keinen Request-Wasserfall -> plan 164-06 Task 1's zero-request-on-expand
     call-count assertion.
  6. DOM wächst nicht unbegrenzt -> plan 164-05's DOM_WINDOW_SIZE=3 bound + this plan's live Chrome DevTools
     DOM-node-count measurement against the dev-only large-dataset harness.
  7. Frühere Pages können beim Zurückscrollen wieder erscheinen -> plan 164-05's cache-hit/cache-miss
     restoration tests.
  8. Scrollposition bleibt stabil -> plan 164-05's spacer-height/scrollTop-delta tests + this plan's live
     visual confirmation against the dev-only harness (scroll behavior needs a real browser, not jsdom).
  9. Gruppenfilter + Cursor bleiben konsistent -> Phase 163's existing cursor-scope tests (unchanged) + plan
     164-06 Task 1's filter-switch race-safety tests.
  10. Response enthält keine schweren Release-Detaildaten -> plan 164-01's DTO field list (scalar/boolean
      only) + this plan's live curl response-body inspection against real Naruto confirming no note bodies/
      image URLs/segment data appear.
  11. Mobile bleibt performant -> this plan's live Chrome DevTools mobile-viewport Performance/Layers
      profile against the dev-only harness.
  12. Keine Race Conditions bei schnellem Scrollen/Filterwechsel -> plan 164-06 Task 1's rapid A/B/C
      filter-switch test + plan 164-05's forward/backward-in-flight-then-switch discard tests.

Existing closing-plan precedent to mirror in structure (163-05-PLAN.md, already read during this phase's
planning): container-rebuild, live-browser-UAT-checkpoint against real Naruto, closing report — this plan
follows the same shape, extended with the dev-only mobile-profiling step this phase additionally requires.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full automated gate and assemble the 8 fully-automatable performance-gate evidence rows</name>
  <files>docs/audits/164-performance-gates.md</files>
  <read_first>
    All prior plans' SUMMARY.md files in this phase directory (164-01 through 164-06) — this task consumes
    their actual test-file names/results, it does not re-derive them;
    164-USER-REQUEST.md §47 (the exact 12 gate descriptions to quote verbatim as row headers);
    docs/audits/ existing files (if any) for this repo's established audit-document format/tone to match.
  </read_first>
  <action>
    Run, in order, and capture pass/fail plus key output lines: `cd backend && go build ./... &amp;&amp; go vet
    ./... &amp;&amp; go test ./...`; `cd frontend && npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test &amp;&amp; npm run build`;
    `git diff --check`. Create `docs/audits/164-performance-gates.md` with a table of all 12 gates (quoting
    each gate's German text from §47 verbatim as the row label) and, for gates 1-5, 7, 9, 10, 12 (the ones
    with a purely automated evidence source per the interfaces section), fill in: the exact test file +
    test name that proves it, the command used to run it, and its pass/fail result from this task's run.
    Leave gates 6, 8, 11 (the ones requiring a live browser measurement) marked "pending Task 2/3 of this
    plan" for now. Include a short section quoting plan 164-01's and 164-03's exact measured query-count
    figures (3 unfiltered / 4 filtered, confirmed at both small and 52-episode scale) as the concrete Gate-4
    evidence, not a restated intention.
  </action>
  <acceptance_criteria>
    - `cd backend && go build ./... && go vet ./... && go test ./...` exits 0 (or the audit document
      explicitly records which specific test was skipped and why, e.g. missing TEAM4S_PHASE117_TEST_DSN in
      this environment, without treating a skip as a silent pass).
    - `cd frontend && npm run typecheck && npm run lint && npm run test && npm run build` exits 0.
    - `git diff --check` exits 0 (no whitespace errors).
    - docs/audits/164-performance-gates.md contains a row for all 12 gates, with gates 1-5/7/9/10/12 fully
      filled in (test file + command + result), not left as "TBD".
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go build ./... && go vet ./... && go test ./... && cd ../frontend && npm run typecheck && npm run lint && npm run test && npm run build</automated>
  </verify>
  <done>The full automated suite is green (or explicitly documented exceptions are recorded), and 9 of the
  12 performance gates have their automated evidence recorded in docs/audits/164-performance-gates.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live Naruto browser UAT — functional/visual correctness, Coop link, filter switching, response shape</name>
  <files>none (manual verification only, no file changes)</files>
  <action>
    Present the live-browser verification steps below to the user against the deployed containers (rebuild
    first if this phase's changes are not yet running: `docker compose build team4sv30-backend
    team4sv30-frontend && docker compose up -d team4sv30-backend team4sv30-frontend`), and wait for explicit
    "approved" or a description of which step failed before proceeding to Task 3.
  </action>
  <what-built>
    The complete Episode-/Release-UI rework (glass cards, tinted classification, group-first release
    preview, dezenter Fließtext, Infinite Scroll with bidirectional bounded windowing, filter-consistent
    cursor behavior) is implemented and automated-tested per plans 164-01 through 164-06. This checkpoint
    verifies functional and visual correctness against the real, live Naruto anime, and captures gates 8/9
    (partial, real-data scope) plus D-48's real-data-reachable visual subset.
  </what-built>
  <how-to-verify>
    1. Connect via the SSH tunnel and open `http://127.0.0.1:3300/anime/4` (Naruto) in a real browser.
    2. Confirm the Network tab shows exactly one episode-list request on initial load (Gate 1/2) and the
       episode list renders with the new glass card style (subtle tint per episode's filler/canon
       classification, dezent classification+type label, plain-text version count + chevron, no colored
       pill badge).
    3. Expand each of Naruto's 5 episodes; confirm zero additional network requests fire per expansion
       (Gate 5) and each expanded release shows group-first identity, a `·`-joined dezent tech line (never
       chip/badge), a conditional extras line, a conditional date line, and a `Zum Release →` button (not a
       Play button, not a clickable whole-card).
    4. Click `Zum Release →` on episode 5's Coop release; confirm it lands on the existing, already-live
       release-detail page for one of the two participating groups; use the browser Back button; confirm
       the anime page reloads with the same active fansub filter still selected from the URL and
       (approximately) the same scroll position (Gate 8/partial, D-44).
    5. Switch between `AnimeOwnage`, `Project Messiah`, and `Alle` filters several times, including rapidly;
       confirm the episode list only ever shows the currently selected filter's data, with the dimmed/aria-
       busy transition from Phase 163 still intact and no stale/mixed episodes ever visible (Gate 9/12,
       real-data scope).
    6. Confirm the response payload (via Network tab, inspect the JSON) for this endpoint contains only the
       documented scalar/boolean fields — no note bodies, image URLs, or segment data (Gate 10).
  </how-to-verify>
  <verify>
    <human-check>User confirms all six checks pass on the live Naruto page.</human-check>
  </verify>
  <done>User has typed "approved" for all six checks, or listed exactly which step failed and what was
  observed (routed back through /gsd:plan-phase 164 --gaps).</done>
  <resume-signal>Type "approved" once all six checks pass, or describe the specific step and observed
  deviation if any check fails.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Dev-only large-dataset harness — live DOM-node count and mobile GPU/blur profiling</name>
  <files>frontend/src/app/dev/episode-windowing-preview/page.tsx</files>
  <action>
    Build a throwaway, dev-only Next.js route at frontend/src/app/dev/episode-windowing-preview/page.tsx
    (matching the existing /dev/ui-system precedent's conventions for a non-production debug route),
    generating a client-side 50+ episode mock PublicGroupedEpisode[] array in the same shape as plan
    164-03's fixture (mixed filler_type/episode_type values, Coop, images/notes/karaoke, with/without logos
    and dates — reuse the fixture's data description, do not call any backend endpoint), rendering it
    through FansubVersionBrowser exactly as the real anime page does. Add an explicit code comment marking
    the route as a temporary, non-production debug aid. Then present the live-measurement verification
    steps below to the user and wait for explicit "approved" (with measured numbers) before considering
    this plan, and the phase, closed.
  </action>
  <what-built>
    A throwaway, dev-only large-dataset rendering harness (not connected to team4s_v2, not a production
    route) rendering FansubVersionBrowser with a 50+ episode synthetic dataset matching plan 164-03's fixture
    variety, for the two gates that genuinely require a real browser + real GPU/paint pipeline to measure:
    DOM-node growth bound (Gate 6) and mobile performance under multiple simultaneously-expanded glass cards
    while scrolling (Gate 11). This is the only place in this phase's plans where "scale" is visually
    observed live, and it deliberately never touches the real database.
  </what-built>
  <how-to-verify>
    1. Confirm the harness route (e.g. under `/dev/...`, matching the existing `/dev/ui-system` precedent)
       renders FansubVersionBrowser with a client-side-generated 50+ episode mock dataset (same shape as
       plan 164-03's fixture: mixed classifications/types, Coop, images/notes/karaoke, with/without logos
       and dates) — confirm via reading the harness route's source that it performs zero requests to any
       backend and contains an explicit comment marking it as a temporary, non-production debug aid.
    2. Scroll through several page-loads worth of the mock dataset; open Chrome DevTools > Elements, and
       spot-check the DOM node count for the episode list container stays within the low-hundreds order of
       magnitude estimated in 164-RESEARCH.md's Plan-Checker Inputs, even after loading 4+ pages (Gate 6) —
       record the actual measured count in the audit document, replacing the prior estimate.
    3. Switch Chrome DevTools to a mobile viewport emulation (e.g. a common mid-range Android profile),
       expand 5+ episodes simultaneously, and record a Performance/Layers profile while scrolling; confirm
       no sustained dropped-frame/paint-storm pattern and record the actual frame-rate/paint-cost numbers in
       the audit document (Gate 11) — replacing 164-RESEARCH.md's estimate with a measured result.
    4. Decide and record in the audit document whether the throwaway harness route should be deleted after
       this checkpoint or retained as a permanent dev-only debug aid (developer's call, either is acceptable
       per this being a non-production route).
  </how-to-verify>
  <verify>
    <human-check>User confirms the measured DOM-node count stays bounded and no sustained dropped-frame/
    paint-storm pattern occurs on the mobile-viewport profile.</human-check>
  </verify>
  <done>User has typed "approved" with the measured DOM-node count and frame-rate/paint-cost summary
  recorded in docs/audits/164-performance-gates.md, and has recorded the keep-or-delete decision for the
  harness route.</done>
  <resume-signal>Type "approved" with the measured DOM-node count and frame-rate/paint-cost summary, or
  describe the specific deviation observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Dev-only large-dataset harness | Client-only, mock data, no backend/database call — must never be reachable from the public/production route tree without an explicit dev-mode gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-164-12 | Information Disclosure | Dev-only large-dataset harness route | mitigate | Route renders only client-generated mock data, performs zero backend requests, and is explicitly reviewed at checkpoint task 3 for either deletion or an explicit dev-only gate before being left in the tree — never wired into the public anime page's route tree. |
| T-164-13 | Tampering | git diff --check / full suite gate | accept | Standard closing-gate hygiene already used by every prior phase in this repo (163-05 precedent); no new risk introduced. |
</threat_model>

<verification>
- Full backend (`go build`, `go vet`, `go test ./...`) and frontend (`typecheck`, `lint`, `test`, `build`)
  suites green.
- `git diff --check` clean.
- `docs/audits/164-performance-gates.md` contains all 12 gates with concrete evidence (test reference or
  measured live result), none left as an unverified restatement of intent.
- Both human-verify checkpoints explicitly approved.
</verification>

<success_criteria>
- Every one of §47's 12 performance gates is closed with named, checkable evidence.
- Real-Naruto live UAT confirms functional/visual correctness on real data without requiring any team4s_v2
  write.
- Mobile/GPU cost and DOM-node growth are measured (not estimated) against a throwaway, non-production
  harness.
- The plan's own SUMMARY.md quotes 164-USER-REQUEST.md §53's exact required closing-report fields (plan
  count, wave count, chosen public-read architecture, query budget, API request structure, pagination/
  cursor strategy, infinite-scroll trigger, windowing strategy, cache strategy, old-page handling, backward
  loading, scroll stability, episode-expand request behavior, mobile-first plan, performance gates, checker
  finding, commits) so the orchestrator can relay it verbatim per §53's "nur berichten" instruction.
</success_criteria>

<output>
Create .planning/phases/164-oeffentliche-anime-seite-episode-release-ui-read-model-infinite-scroll/164-07-SUMMARY.md when done
</output>
