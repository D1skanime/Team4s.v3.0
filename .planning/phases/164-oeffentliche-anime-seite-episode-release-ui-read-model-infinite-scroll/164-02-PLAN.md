---
phase: 164-oeffentliche-anime-seite-episode-release-ui-read-model-infinite-scroll
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/contracts/openapi.yaml
  - frontend/src/types/episodeVersion.ts
autonomous: true
requirements: [REQ-164-21, REQ-164-26]

must_haves:
  truths:
    - "OpenAPI's PublicGroupedEpisode/PublicEpisodeVersion schemas describe exactly the same additive fields the backend plan (164-01) adds, so contract drift cannot occur between the two parallel Wave-1 plans."
    - "The TypeScript PublicGroupedEpisode/PublicEpisodeVersion types describe the same additive fields, keeping the existing Pick<EpisodeVersion, ...> composition style rather than duplicating fields already declared on EpisodeVersion."
    - "No heavy/detail field (note bodies, image URLs, segment data) is added to either contract, only the scalar/boolean fields named in 164-CONTEXT.md D-21."
  artifacts:
    - path: "shared/contracts/openapi.yaml"
      provides: "Additive PublicEpisodeVersion/PublicGroupedEpisode schema fields: container, video_codec, has_images, has_notes, has_karaoke, filler_type, episode_type"
      contains: "has_karaoke"
    - path: "frontend/src/types/episodeVersion.ts"
      provides: "Additive PublicEpisodeVersion/PublicGroupedEpisode TS fields matching the OpenAPI/Go DTO shape"
      contains: "has_karaoke"
  key_links:
    - from: "shared/contracts/openapi.yaml (PublicEpisodeVersion schema)"
      to: "backend/internal/models/episode_version.go (PublicEpisodeVersion struct, plan 164-01)"
      via: "identical field names/types, additive only, both plans reviewed against 164-PATTERNS.md exact field list"
      pattern: "has_karaoke"
---

<objective>
Extend the public episode-list OpenAPI schema and the corresponding frontend TypeScript types with the
same additive fields plan 164-01 adds to the Go DTOs (D-21, REQ-164-21/26), so every consumer of this
contract (OpenAPI docs, frontend/src/lib/api.ts callers, later frontend plans in this phase) sees one
agreed shape before any frontend rendering work begins.

Purpose: Interface-first ordering. This plan has no runtime dependency on 164-01's Go implementation (the
field names/types are already fixed by 164-CONTEXT.md D-21 and 164-PATTERNS.md's exact field list), so it
runs in the same wave, in parallel, touching entirely different files.

Output: Updated shared/contracts/openapi.yaml PublicEpisodeVersion/PublicGroupedEpisode schemas; updated
frontend/src/types/episodeVersion.ts PublicEpisodeVersion/PublicGroupedEpisode types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/164-oeffentliche-anime-seite-episode-release-ui-read-model-infinite-scroll/164-CONTEXT.md
@.planning/phases/164-oeffentliche-anime-seite-episode-release-ui-read-model-infinite-scroll/164-PATTERNS.md

<interfaces>
Current OpenAPI schemas to extend additively (shared/contracts/openapi.yaml lines 15390-15499).

PublicEpisodeVersion (line 15390): object, additionalProperties false, required
[id, variant_id, release_version_id, anime_id, episode_number, fansub_groups]. Current properties: id,
variant_id, release_version_id, anime_id, episode_number, title (nullable string), release_version
(nullable string), fansub_groups (array of FansubGroupSummary), video_quality (nullable string),
subtitle_type (nullable enum hardsub/softsub), release_date (nullable date-time).

PublicGroupedEpisode (line 15434): object, required [episode_id, episode_number, version_count, versions].
Current properties: episode_id, episode_number, episode_title (nullable), default_version_id, version_count,
versions (array of PublicEpisodeVersion).

Current TS types to extend additively (frontend/src/types/episodeVersion.ts lines 153-168):

export type PublicEpisodeVersion = Pick of EpisodeVersion with keys
  id, variant_id, release_version_id, anime_id, episode_number,
  title, release_version, video_quality, subtitle_type, release_date
  intersected with { fansub_groups: FansubGroupSummary[] }

export interface PublicGroupedEpisode has episode_id, episode_number, episode_title, default_version_id,
version_count, versions: PublicEpisodeVersion[].

Note: EpisodeVersion (the base interface the Pick draws from) does not currently declare container/
video_codec/has_images/has_notes/has_karaoke; these are new-to-the-public-contract fields with no existing
private/admin equivalent on EpisodeVersion, so they must be added directly to the PublicEpisodeVersion
intersection type (the trailing object-literal part of the type), not via Pick.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend OpenAPI PublicEpisodeVersion/PublicGroupedEpisode schemas additively</name>
  <files>shared/contracts/openapi.yaml</files>
  <read_first>
    shared/contracts/openapi.yaml lines 15380-15499 (PublicEpisodeVersion, PublicGroupedEpisode,
    PublicGroupedEpisodesPagination, PublicGroupedEpisodesResponse schemas in full);
    shared/contracts/openapi.yaml FansubGroupSummary schema (around line 14137) to confirm logo_url is
    already a nullable string property there (no schema change needed for groups themselves, only the SQL
    source of that value changes in plan 164-01, not the shape);
    164-CONTEXT.md decision D-21 (exact field list) and 164-PATTERNS.md's model-extension section (exact
    Go field names/JSON tags to mirror one to one).
  </read_first>
  <action>
    In the PublicEpisodeVersion schema, add four properties: container (type string, nullable true),
    video_codec (type string, nullable true), has_images (type boolean), has_notes (type boolean),
    has_karaoke (type boolean). Add has_images, has_notes, has_karaoke to the schema's required array
    (always present, non-nullable booleans, matching the Go DTO's non-pointer-with-no-omitempty
    convention); do not add container/video_codec to required (nullable, omitempty on the Go side).
    In the PublicGroupedEpisode schema, add two properties: filler_type (type string, enum
    canon/filler/mixed/recap/unknown) and episode_type (type string, enum
    episode/special/ova/ona/movie/recap/preview/prologue/epilogue/bonus), both non-nullable
    (COALESCE-guaranteed at the SQL level per plan 164-01), both added to the schema's required array
    alongside the existing episode_id/episode_number/version_count/versions.
  </action>
  <acceptance_criteria>
    - grep for has_karaoke in shared/contracts/openapi.yaml matches inside the PublicEpisodeVersion schema
      block (between its "type: object" line near 15390 and the next top-level schema key).
    - grep for filler_type in shared/contracts/openapi.yaml matches inside the PublicGroupedEpisode schema
      block.
    - PublicEpisodeVersion still declares additionalProperties false after the edit (new fields added to
      properties, not bypassing the allowlist).
    - The YAML file still parses as valid YAML after the edit.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/d1sk/team4s && python3 -c "import yaml; yaml.safe_load(open('shared/contracts/openapi.yaml'))"</automated>
  </verify>
  <done>PublicEpisodeVersion and PublicGroupedEpisode OpenAPI schemas carry the seven new fields with
  correct nullability/required-array placement, and the file still parses as valid YAML.</done>
</task>

<task type="auto">
  <name>Task 2: Extend frontend PublicEpisodeVersion/PublicGroupedEpisode TypeScript types additively</name>
  <files>frontend/src/types/episodeVersion.ts</files>
  <read_first>
    frontend/src/types/episodeVersion.ts (full file, 187 lines, in particular lines 1-32 for the base
    EpisodeVersion interface and lines 153-168 for the current PublicEpisodeVersion/PublicGroupedEpisode
    definitions and the existing Pick-and-intersect composition style);
    frontend/src/types/fansub.ts (FansubGroupSummary interface, confirm logo_url shape, no change needed
    here);
    164-CONTEXT.md decision D-21 and 164-PATTERNS.md's model-extension section (exact field names to mirror
    from the OpenAPI/Go changes in Task 1 of this plan and in plan 164-01).
  </read_first>
  <action>
    Extend the PublicEpisodeVersion type's trailing intersection object literal (currently only
    { fansub_groups: FansubGroupSummary[] }) to also declare: container?: string | null,
    video_codec?: string | null, has_images: boolean, has_notes: boolean, has_karaoke: boolean (booleans
    non-optional, matching the OpenAPI required-array placement from Task 1; container/video_codec optional
    and nullable, matching omitempty). Extend the PublicGroupedEpisode interface with two new non-optional
    fields: filler_type: 'canon' | 'filler' | 'mixed' | 'recap' | 'unknown' and episode_type: 'episode' |
    'special' | 'ova' | 'ona' | 'movie' | 'recap' | 'preview' | 'prologue' | 'epilogue' | 'bonus' (both
    always-present union string types, not optional, matching the COALESCE guarantee from plan 164-01). Do
    not modify the base EpisodeVersion interface; these fields exist only on the public projection types.
  </action>
  <acceptance_criteria>
    - grep for has_karaoke in frontend/src/types/episodeVersion.ts matches within the PublicEpisodeVersion
      type definition.
    - grep for filler_type in frontend/src/types/episodeVersion.ts matches within the PublicGroupedEpisode
      interface.
    - cd frontend && npx tsc --noEmit exits 0 (no type errors introduced by the additive change; existing
      consumers of PublicEpisodeVersion/PublicGroupedEpisode in FansubVersionBrowser.tsx and page.tsx remain
      structurally compatible since all new fields are additive).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/d1sk/team4s/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>PublicEpisodeVersion and PublicGroupedEpisode TypeScript types carry the seven new fields with
  correct optionality, and the frontend still typechecks cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Contract definition (this plan) versus runtime implementation (plan 164-01) | No live request crosses this boundary; the risk is purely documentation/type drift between two parallel plans, not a runtime attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-164-04 | Information Disclosure | OpenAPI/TS schema additions | accept | Both files add only scalar/boolean fields already scoped by 164-CONTEXT.md D-21/D-26 as safe for public exposure; no note bodies, media URLs, or internal IDs are introduced by this plan. Cross-checked against plan 164-01's actual Go DTO fields in the shared must_haves key_link. |
</threat_model>

<verification>
- python3 -c "import yaml; yaml.safe_load(open('shared/contracts/openapi.yaml'))" exits 0.
- cd frontend && npx tsc --noEmit exits 0.
- grep confirms has_karaoke and filler_type appear in both the OpenAPI schema blocks and the TS type
  definitions.
</verification>

<success_criteria>
- OpenAPI and TypeScript contracts for the public episode-list response carry the same seven additive
  fields plan 164-01 implements in Go, with matching nullability.
- No existing field, schema, or route is removed or renamed.
- Frontend typecheck remains green.
</success_criteria>

<output>
Create .planning/phases/164-oeffentliche-anime-seite-episode-release-ui-read-model-infinite-scroll/164-02-SUMMARY.md when done
</output>
