---
phase: 164-oeffentliche-anime-seite-episode-release-ui-read-model-infinite-scroll
plan: 03
type: execute
wave: 2
depends_on: ["164-01"]
files_modified:
  - backend/internal/repository/episode_version_public_scale_fixture_test.go
autonomous: true
requirements: [REQ-164-22, REQ-164-23, REQ-164-46, REQ-164-47]

must_haves:
  truths:
    - "An isolated-schema Postgres fixture proves the extended public episode-list endpoint's query count stays exactly 3 (unfiltered) / 4 (group-filtered) per HTTP request even when an anime has 50+ episodes-with-releases spanning multiple pages, mixed filler_type/episode_type values, logos, dates, images, notes, karaoke, and a Coop release."
    - "Cursor pagination across this larger fixture proves atomic, non-overlapping, filter-scoped pages exactly as the existing 125-variant/30-episode fixtures already prove at smaller scale, closing the D-47 gap that no anime in team4s_v2 reaches this scale today."
    - "No task in this plan writes to team4s_v2; the fixture lives entirely inside testsupport.OpenPhase117Postgres's isolated schema."
  artifacts:
    - path: "backend/internal/repository/episode_version_public_scale_fixture_test.go"
      provides: "A 50+ episode-with-releases fixture (mixed classifications/types/logos/dates/images/notes/karaoke/coop) proving constant query budget and correct pagination at scale"
      contains: "generate_series"
  key_links:
    - from: "backend/internal/repository/episode_version_public_scale_fixture_test.go"
      to: "backend/internal/repository/episode_version_public_query.go (ListPublicGroupedByAnimeID, plan 164-01)"
      via: "same episodePublicRequest/episodePublicTracer harness as the existing integration tests"
      pattern: "assertPublicBudget"
---

<objective>
Build a dedicated, isolated-schema Postgres fixture reaching the scale that no anime in team4s_v2 reaches
today (max 13 episodes-with-releases; Naruto has 5) so this phase's D-46 gates 3/4 and D-47/D-48's scale-
and-variety requirements can be exercised automatically instead of only against the real, small Naruto
dataset (164-RESEARCH.md Open Question 1, Pitfall 1).

Purpose: Closes the Wave 0 gap identified in 164-VALIDATION.md ("no such fixture exists today") before any
frontend windowing work (164-05) needs realistic multi-page data to test against, and gives the final gate
plan (164-07) a reusable, reproducible large dataset for its automated performance assertions.

Output: A new backend integration test file with a 50+ episode-with-releases fixture covering every value
in D-48's visual test matrix (all 5 filler_type values, at least episode/special/ova/movie episode_type
values, releases with/without logos, with/without dates, with/without images/notes/karaoke, one Coop
release), proving the exact query-budget assertions from plan 164-01 hold at this scale across multiple
cursor pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-oeffentliche-anime-seite-episode-release-ui-read-model-infinite-scroll/164-CONTEXT.md
@.planning/phases/164-oeffentliche-anime-seite-episode-release-ui-read-model-infinite-scroll/164-RESEARCH.md
@.planning/phases/164-oeffentliche-anime-seite-episode-release-ui-read-model-infinite-scroll/164-VALIDATION.md
@.planning/phases/164-oeffentliche-anime-seite-episode-release-ui-read-model-infinite-scroll/164-01-PLAN.md

<interfaces>
Reusable fixture/harness pieces already defined in episode_version_public_integration_test.go (same
package repository_test, this new file joins that package): episodePublicTracer (pgx QueryTracer counting
statements/rows), episodePublicRequest(t, pool, path, status) (runs the real Gin handler against a real
pgxpool.Pool), assertPublicBudget(t, tr, limit) (now asserting 3 statements per plan 164-01), and
publicEpisodeEnvelope (JSON response struct). This new file must call these existing exported-within-package
helpers directly, not redefine them.

Reusable minimal media/notes visibility-gate fixture pattern (project_member_public_repository_episodes_integration_test.go
lines 41-88, package repository not repository_test, but the same table/column shapes apply):
  CREATE TABLE IF NOT EXISTS visibilities (id BIGSERIAL PRIMARY KEY, name VARCHAR(40) NOT NULL UNIQUE);
  INSERT INTO visibilities (name) VALUES ('public') ON CONFLICT (name) DO NOTHING;
  CREATE TABLE review_statuses (id BIGSERIAL PRIMARY KEY, code VARCHAR(40) NOT NULL UNIQUE);
  INSERT INTO review_statuses (code) VALUES ('approved');
  ALTER TABLE media_assets ADD COLUMN status ..., ADD COLUMN visibility_id ..., ADD COLUMN review_status_id ...;
  CREATE TABLE release_version_media (id, release_version_id, media_asset_id, uploaded_by_user_id,
    fansub_group_id, deleted_at, category VARCHAR(30));
Minimal working values proven elsewhere in this codebase (member_profile_contribution_badges_repository_test.go
lines 117-119): visibilities id=1 name='public'; review_statuses id=1 code='approved';
media_assets(id, status='ready', visibility_id=1, review_status_id=1).

Existing large-scale precedent to mirror in shape, not literally reuse (episode_version_public_group_filter_test.go
lines 59-70, anime_id=8's 29-episode generate_series padding fixture) — this new fixture is a superset:
it needs release_version_media/release_version_notes rows too, which the group-filter fixture does not have.

Base schema already provided by testsupport.OpenPhase117Postgres (read the function itself, then confirm
via a local trial run, before ALTER-ing tables that may already have the needed columns) — do not
redeclare a table/column that already exists in the base schema; only ALTER/CREATE what is actually
missing, following openEpisodeVersionPublicFixture's and openEpisodeVersionPublicGroupFilterFixture's own
precedent of doing exactly this via targeted ALTER TABLE/CREATE TABLE statements.

episode_filler_types/episode_types lookup values already live in the base migrations (0044, 0031):
filler types unknown/canon/filler/mixed/recap; episode types episode/special/ova/ona/movie/recap/preview/
prologue/epilogue/bonus. Verify whether testsupport.OpenPhase117Postgres already seeds these lookup tables
before writing INSERT statements for them (avoid duplicate-key fixture failures).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the 50+ episode scale/variety fixture and prove constant query budget across multiple cursor pages</name>
  <files>backend/internal/repository/episode_version_public_scale_fixture_test.go</files>
  <read_first>
    backend/internal/repository/episode_version_public_integration_test.go (full file, especially
    openEpisodeVersionPublicFixture's ALTER/CREATE pattern and assertPublicBudget after plan 164-01's edit
    to require.Len(t, tr.queries, 3, ...));
    backend/internal/repository/episode_version_public_group_filter_test.go (full file, especially the
    anime_id=8 29-episode generate_series padding pattern and assertPublicBudgetWithGroupFilter after plan
    164-01's edit);
    backend/internal/repository/project_member_public_repository_episodes_integration_test.go lines 1-160
    (exact minimal visibilities/review_statuses/media_assets/release_version_media/release_version_notes
    fixture columns and INSERT shapes);
    backend/internal/testsupport/phase117_postgres.go (confirm exactly what base schema/seed rows
    testsupport.OpenPhase117Postgres already provides, so this fixture does not redeclare an existing table
    or lookup row);
    164-UI-SPEC.md's "Visueller Testfall-Katalog" table (the 25-row list this fixture's data must be able to
    exercise once frontend plans consume it).
  </read_first>
  <behavior>
    - Given a fixture anime with 52 episodes each carrying exactly one public release (chosen to comfortably
      exceed both a 24-item page size and this phase's likely bounded-window size), requesting page 1
      (limit=24) returns 24 episodes, has_more=true, and exactly 3 SQL statements (assertPublicBudget).
    - Given the same anime, walking the full cursor chain to the last page (mirroring
      TestEpisodeVersionPublicAtomicPages's existing loop pattern) visits every episode exactly once, with
      no episode ID ever repeated across pages (same seen-map assertion technique already used in that
      test).
    - Given the fixture includes at least one episode per filler_type value (canon/filler/mixed/recap/
      unknown) and at least 4 distinct episode_type values (episode/special/ova/movie), a request for that
      anime's full episode list contains at least one row with each value, provable via a simple grouped
      count assertion.
    - Given one release_version in the fixture has an approved public screenshot image, one has an approved
      public typesetting_karaoke media row, one has a published public note, and one has a Coop
      (2-group) release_version_groups assignment, the corresponding response rows show has_images=true,
      has_karaoke=true, has_notes=true, and fansub_groups length 2, respectively — and all other rows in the
      fixture show the correct false/1-group defaults (proving the batched flags query and group
      aggregation are correct at scale, not just in the small existing fixtures).
    - Given an active group filter on this larger fixture, the query budget is exactly 4 statements
      (assertPublicBudgetWithGroupFilter with groupFilterPresent=true), and pagination still returns only
      that group's matching episodes across all pages (same non-overlap proof as the unfiltered case).
  </behavior>
  <action>
    Create backend/internal/repository/episode_version_public_scale_fixture_test.go, package
    repository_test (same package as episode_version_public_integration_test.go, so it can call the
    existing unexported episodePublicRequest/episodePublicTracer/assertPublicBudget/
    assertPublicBudgetWithGroupFilter/episodeIDs helpers directly without duplication). Define
    openEpisodeVersionPublicScaleFixture(t *testing.T) (*pgxpool.Pool, *episodePublicTracer) following the
    exact same construction shape as openEpisodeVersionPublicGroupFilterFixture: call
    testsupport.OpenPhase117Postgres(t), then one pool.Exec with: (a) the same anime/fansub_groups/status
    column ALTERs already proven necessary in the sibling fixtures; (b) a dedicated fixture anime_id (pick
    an unused id, e.g. 9, distinct from 1-8 already used by sibling fixtures in this package) with 52
    episodes generated via generate_series, each with one fansub_release/release_version/release_variant
    row; (c) explicit filler_type_id/episode_type_id assignments on a representative subset of episodes
    covering all 5 filler values and at least 4 episode-type values (using the actual lookup IDs from
    episode_filler_types/episode_types — query or hardcode the known seed IDs, verified via read_first); (d)
    two fansub_groups (reuse or extend the existing stored-one/stored-two ids from the sibling fixture, or
    add new ones scoped to this fixture's anime) with one Coop release_version_groups row; (e) the
    visibilities/review_statuses/media_assets/release_version_media/release_version_notes minimal schema
    additions and rows from the read_first analog, attached to 3 distinct release_version_ids (one
    screenshot, one typesetting_karaoke, one published note); (f) at least one release_version with a
    populated release_date and one without, and one release_variant with container/video_codec populated
    and one without (exercising the omitempty/Unbekannt-fallback boundary for later frontend work). Write
    TestEpisodeVersionPublicScaleBudgetAndPagination that: walks the full unfiltered cursor chain at
    limit=24 asserting assertPublicBudget(t, tr, 24) on every page and zero duplicate episode IDs across
    pages (mirroring TestEpisodeVersionPublicAtomicPages's seen-map technique); asserts the classification/
    episode-type/flags/Coop correctness behaviors above via targeted field checks on the known fixture rows;
    repeats the same walk with an active fansub filter, asserting assertPublicBudgetWithGroupFilter(t, tr,
    24, true) and that only that group's episodes appear across all pages.
  </action>
  <acceptance_criteria>
    - grep for generate_series in the new file returns at least 1 (proves scale, not hand-enumerated rows).
    - grep for assertPublicBudget and assertPublicBudgetWithGroupFilter in the new file both return at least
      1 (reuses the shared harness, does not redefine a parallel budget assertion).
    - The new file's episode count for the fixture anime is 52 or greater (grep for the literal episode
      count used in generate_series, or assert via require.GreaterOrEqual(t, totalEpisodesSeen, 52) in the
      test body).
    - cd backend && go test ./internal/repository/... -run TestEpisodeVersionPublicScale -v -count=1 passes
      (or cleanly skips if TEAM4S_PHASE117_TEST_DSN is unset in this environment; document which occurred).
    - No SQL statement in this file targets team4s_v2 or reads DATABASE_URL; the only pool used is the one
      returned by testsupport.OpenPhase117Postgres.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/repository/... -run TestEpisodeVersionPublicScale -v -count=1</automated>
  </verify>
  <done>A 50+ episode isolated-schema fixture exists covering every D-48 classification/type/extras/Coop
  variant, and an automated test proves the exact 3/4-statement query budget from plan 164-01 holds across
  multiple cursor pages at this scale, closing the D-46 gate 3/4 and D-47 scale-verification gap that real
  team4s_v2 data cannot exercise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test fixture -> isolated Postgres schema | No production data crosses this boundary; testsupport.OpenPhase117Postgres provisions a throwaway, uniquely-scoped schema per test run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-164-05 | Tampering | New fixture SQL | accept | Fixture SQL runs exclusively against the isolated testsupport.OpenPhase117Postgres schema, never against team4s_v2 or DATABASE_URL; enforced by the acceptance criterion checking no team4s_v2/DATABASE_URL reference exists in the new file. |
</threat_model>

<verification>
- cd backend && go test ./internal/repository/... -run TestEpisodeVersionPublicScale -v -count=1 passes (or
  cleanly skips per the existing DSN-gated convention).
- grep confirms generate_series, assertPublicBudget, and assertPublicBudgetWithGroupFilter all appear in
  the new file.
- No reference to team4s_v2 or os.Getenv("DATABASE_URL") appears anywhere in the new file.
</verification>

<success_criteria>
- The scale/variety gap identified in 164-RESEARCH.md (no anime in team4s_v2 reaches 24+ episodes-with-
  releases) is closed by an automated, reproducible fixture, not by a live data change.
- Query budget (3/4 statements) is proven constant across multiple pages at 52-episode scale, directly
  supporting D-46 gate 4 ("Query-Anzahl wächst nicht linear mit Episodenanzahl").
- The fixture's data variety (5 filler types, 4+ episode types, logo/no-logo, date/no-date, images/notes/
  karaoke/none, Coop) is available for later frontend plans and the final UAT plan to reference.
</success_criteria>

<output>
Create .planning/phases/164-oeffentliche-anime-seite-episode-release-ui-read-model-infinite-scroll/164-03-SUMMARY.md when done
</output>
