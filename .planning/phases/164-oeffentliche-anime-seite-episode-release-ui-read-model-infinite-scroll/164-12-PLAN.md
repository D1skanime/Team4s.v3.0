---
phase: 164-oeffentliche-anime-seite-episode-release-ui-read-model-infinite-scroll
plan: 12
type: execute
wave: 3
depends_on: ["164-08", "164-10"]
files_modified:
  - frontend/src/components/fansubs/episodePreviewFormat.ts
  - frontend/src/components/fansubs/ReleasePreviewRow.tsx
  - frontend/src/components/fansubs/EpisodeGlassCard.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/episodeVersionEditorUtils.ts
  - frontend/src/components/fansubs/episodePreviewFormat.test.ts
  - frontend/src/app/admin/episode-versions/[versionId]/edit/episodeVersionEditorUtils.test.ts
autonomous: true
gap_closure: true
requirements: [REQ-164-02, REQ-164-03, REQ-164-04, REQ-164-08, REQ-164-11, REQ-164-12, REQ-164-14]

must_haves:
  truths:
    - "GAP-02: The public release preview always displays the backend-computed release_name field, never a raw title/filename, and the admin edit placeholder mirrors the identical format, coop-capable."
    - "GAP-03: The tech line (resolution/container/codec/subtitle) omits any value that is missing instead of showing 'Unbekannt'; if no value is present at all, the whole line is omitted."
    - "GAP-11 (public half): The classification/type line ('Filler · Episode') uses the DB-sourced filler_type_label/episode_type_label from the API response, not a hardcoded frontend map; the unknown-classification-hides-its-label rule (UI-SPEC decision 4) still applies, keyed on the filler_type code."
  artifacts:
    - path: "frontend/src/components/fansubs/episodePreviewFormat.ts"
      provides: "resolveReleaseName reads version.release_name; classificationAndTypeLine reads episode.filler_type_label/episode_type_label; formatTechValue/formatSubtitleType omit missing values instead of falling back to 'Unbekannt'"
  key_links:
    - from: "frontend/src/components/fansubs/ReleasePreviewRow.tsx"
      to: "frontend/src/components/fansubs/episodePreviewFormat.ts"
      via: "resolveReleaseName(version) returning version.release_name"
      pattern: "release_name"
---

<objective>
Close the frontend-consumption half of GAP-02 and GAP-11, plus all of GAP-03, from `164-UAT.md`.
This plan makes `episodePreviewFormat.ts` (public release preview text) and
`episodeVersionEditorUtils.ts` (admin editor placeholder) consume the new backend fields shipped by
164-08 (`release_name`) and 164-10 (`filler_type_label`/`episode_type_label`), and removes the
hardcoded label maps and the "Unbekannt" tech-line fallback.

Purpose: 164-08 and 164-10 built the backend read-model fields; nothing on the frontend uses them
yet. Until this plan runs, the public page still shows the old raw title/hardcoded classification
labels and the "Unbekannt" tech-line fallback the Auftraggeber explicitly rejected.

Output: episodePreviewFormat.ts's resolveReleaseName, classificationLabel, episodeTypeLabel,
formatTechValue, formatSubtitleType are rewritten to consume the new API fields and the corrected
omit-don't-fallback tech-line rule; episodeVersionEditorUtils.ts's defaultReleaseTitle is rewritten
to the identical "&lt;Episodentitel&gt; · (&lt;Gruppe(n)&gt;) · &lt;Version&gt;" format, made coop-capable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164-oeffentliche-anime-seite-episode-release-ui-read-model-infinite-scroll/164-UAT.md
@.planning/phases/164-oeffentliche-anime-seite-episode-release-ui-read-model-infinite-scroll/164-08-SUMMARY.md
@.planning/phases/164-oeffentliche-anime-seite-episode-release-ui-read-model-infinite-scroll/164-10-SUMMARY.md

<interfaces>
Current frontend/src/components/fansubs/episodePreviewFormat.ts (full file, 111 lines) -- functions
this task rewrites:

- classificationLabel(fillerType): currently a hardcoded CLASSIFICATION_LABELS Record; must instead
  read the episode's filler_type_label from the API response (still returns null for the
  unknown-code case, per UI-SPEC decision 4 -- that suppression stays code-driven, not label-driven,
  since the DB now has a non-null label even for "unknown").
- episodeTypeLabel(episodeType): currently a hardcoded EPISODE_TYPE_LABELS Record; must instead read
  the episode's episode_type_label from the API response.
- classificationAndTypeLine(fillerType, episodeType): signature must change to accept the
  already-resolved label strings (or the whole PublicGroupedEpisode) instead of just the two codes,
  since the labels no longer come from a local map.
- formatTechValue(value) / formatSubtitleType(value): currently fall back to
  TECH_VALUE_FALLBACK = 'Unbekannt'; must instead signal "omit this value" (return null) so
  ReleasePreviewRow.tsx's techLine construction can filter missing entries out before joining, and
  omit the whole techLine paragraph if nothing remains.
- resolveReleaseName(version): currently (version.title||'').trim() || 'Release #'+id; must instead
  return version.release_name (164-08's new, always-populated field) directly.

Caller: frontend/src/components/fansubs/ReleasePreviewRow.tsx imports all of the above; read it to
confirm exact call sites (techLine array construction, resolveReleaseName call) before changing
signatures.

Caller: frontend/src/components/fansubs/EpisodeGlassCard.tsx calls classificationAndTypeLine with
the PublicGroupedEpisode's filler_type/episode_type codes today -- read it to confirm the exact call
site and update it to also pass the new filler_type_label/episode_type_label fields per the new
signature.

Backend fields this plan consumes (already shipped by 164-08/164-10, do not re-derive them
client-side):
- PublicEpisodeVersion.release_name: string (164-08 Task 2) -- frontend/src/types/episodeVersion.ts
- PublicGroupedEpisode.filler_type_label: string, episode_type_label: string (164-10 Task 2) --
  frontend/src/types/episodeVersion.ts

Current frontend/src/app/admin/episode-versions/[versionId]/edit/episodeVersionEditorUtils.ts
defaultReleaseTitle (lines 149-153):
  const groupName = context.selected_groups[0]?.name || 'Fansub'
  const version = context.version.release_version || 'v1'
  return 'Episode ' + padEpisodeNumber(context.version.episode_number) + ' · ' + groupName + ' · ' + version

Note: EpisodeVersionEditorContext (frontend/src/types/episodeVersion.ts:66-81) has NO episode-title
field at all (only version.episode_number) -- there is no backend round trip planned for this
placeholder; it must stay a zero-latency, purely local computation reacting to in-progress group
selection in the form. Discretion: keep the existing "Episode NNN" (padded number) fallback wording
as the "&lt;Episodentitel&gt;" placeholder position -- this satisfies "same rule/format" (structure:
"&lt;Episodentitel&gt; · (&lt;Gruppe(n)&gt;) · &lt;Version&gt;", middle-dot separators, coop-capable) without
requiring a new backend field just for an admin-only placeholder; only this file (GAP-02's explicitly
named artifact) needs the format/coop fix, not a new context field.

Existing coop-sort convention to replicate exactly (D-11,
frontend/src/components/fansubs/episodePreviewFormat.ts:107-110 resolveCoopLinkGroupId):
[...groups].sort((a, b) => a.name.localeCompare(b.name) || a.id - b.id)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Consume backend-computed release_name and remove the tech-line "Unbekannt" fallback</name>
  <files>frontend/src/components/fansubs/episodePreviewFormat.ts, frontend/src/components/fansubs/ReleasePreviewRow.tsx, frontend/src/components/fansubs/episodePreviewFormat.test.ts</files>
  <read_first>
    frontend/src/components/fansubs/episodePreviewFormat.ts (full 111-line file)
    frontend/src/components/fansubs/ReleasePreviewRow.tsx (full 95-line file, especially the techLine
    construction around lines 27-32 and the resolveReleaseName(version) call around line 73)
    frontend/src/types/episodeVersion.ts (PublicEpisodeVersion.release_name, added by 164-08)
  </read_first>
  <behavior>
    - Test 1: resolveReleaseName({ release_name: "Episode 1 · (Strawhat Subs) · v1", ... }) returns
      that string verbatim (no more title/"Release #id" logic).
    - Test 2: formatTechValue(undefined), formatTechValue(null), formatTechValue('') all return null
      (not "Unbekannt").
    - Test 3: formatTechValue('1080p') returns '1080p' unchanged.
    - Test 4: formatSubtitleType(null) returns null; formatSubtitleType('softsub') returns 'Softsub'
      unchanged.
    - Test 5 (ReleasePreviewRow-level): a version with all four tech values missing renders no
      techLine element at all; a version with only video_quality set renders a tech line containing
      only that one value, no stray "·" separators.
  </behavior>
  <action>
    Remove TECH_VALUE_FALLBACK and change formatTechValue/formatSubtitleType to return
    string | null (trimmed value, or null when missing/blank) instead of falling back to
    'Unbekannt'. Change resolveReleaseName(version: PublicEpisodeVersion): string to simply
    return version.release_name (delete the old title/"Release #" fallback logic entirely -- the
    backend now guarantees a non-empty value per 164-08).
    In ReleasePreviewRow.tsx, change the techLine construction from
    [formatTechValue(...), formatTechValue(...), formatTechValue(...), formatSubtitleType(...)].join(' · ')
    to filter out null entries before joining (e.g.
    [...].filter((value): value is string => value !== null).join(' · ')), and conditionally render
    the techLine paragraph only when the filtered array is non-empty (mirror the existing conditional
    pattern already used for extras.length > 0 / dateLine a few lines below in the same file).
    Before adding new tests, rewrite the stale pre-existing assertions in
    episodePreviewFormat.test.ts that this change invalidates: the 'formatTechValue' describe block
    (current lines 63-70, asserting formatTechValue(null)/(undefined)/('')->'Unbekannt') and the
    'formatSubtitleType' describe block (current lines 72-79, asserting formatSubtitleType(null)/
    (undefined)->'Unbekannt') must be rewritten to assert the new null-return-on-missing behavior
    (Test 2 and Test 4 above) instead of the old 'Unbekannt' fallback -- do not leave the old
    'Unbekannt' assertions alongside the new ones. Also rewrite the 'resolveReleaseName' describe
    block (current lines 129-137, asserting resolveReleaseName({title:'Mein Release',
    release_version_id:5})->'Mein Release' and resolveReleaseName({title:null,
    release_version_id:5})->'Release #5') to assert Test 1's new release_name-passthrough behavior
    instead -- delete the old title/"Release #" fallback assertions entirely, since resolveReleaseName
    no longer reads version.title or falls back to 'Release #ID'.
    Add/extend episodePreviewFormat.test.ts with Test 1-4 above.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/fansubs/episodePreviewFormat.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "TECH_VALUE_FALLBACK" frontend/src/components/fansubs/episodePreviewFormat.ts returns 0.
    - grep -c "release_name" frontend/src/components/fansubs/episodePreviewFormat.ts returns at least 1.
    - grep -c "'Unbekannt'" frontend/src/components/fansubs/episodePreviewFormat.ts returns 0.
    - New tests in episodePreviewFormat.test.ts cover Test 1-4 and pass.
    - npx vitest run src/components/fansubs/episodePreviewFormat.test.ts passes with 0 failures.
  </acceptance_criteria>
  <done>resolveReleaseName always returns the backend-computed name; the tech line omits missing values entirely instead of showing "Unbekannt", and disappears completely when no tech values are present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consume DB-sourced classification/episode-type labels and mirror the format in the admin placeholder</name>
  <files>frontend/src/components/fansubs/episodePreviewFormat.ts, frontend/src/components/fansubs/EpisodeGlassCard.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/episodeVersionEditorUtils.ts, frontend/src/components/fansubs/episodePreviewFormat.test.ts, frontend/src/app/admin/episode-versions/[versionId]/edit/episodeVersionEditorUtils.test.ts</files>
  <read_first>
    frontend/src/components/fansubs/episodePreviewFormat.ts (CLASSIFICATION_LABELS/EPISODE_TYPE_LABELS
    and classificationLabel/episodeTypeLabel/classificationAndTypeLine, lines 4-45 -- Task 1 above
    already ran in this same plan; build on its output, don't redo it)
    frontend/src/components/fansubs/EpisodeGlassCard.tsx (find the classificationAndTypeLine call site)
    frontend/src/app/admin/episode-versions/[versionId]/edit/episodeVersionEditorUtils.ts
    (defaultReleaseTitle, lines 149-153, and padEpisodeNumber, lines 145-147)
    frontend/src/types/episodeVersion.ts (PublicGroupedEpisode.filler_type_label/episode_type_label,
    added by 164-10; FansubGroupSummary for the coop sort in defaultReleaseTitle)
  </read_first>
  <behavior>
    - Test 1: classificationAndTypeLine for filler_type='filler' with filler_type_label='Zusatzfolge'
      and episode_type='episode' with episode_type_label='Episode' returns "Zusatzfolge · Episode"
      (DB label text, not the old hardcoded "Filler").
    - Test 2: classificationAndTypeLine for filler_type='unknown' (any label value) returns just the
      episode_type_label alone, no classification segment -- the UI-SPEC decision 4 suppression stays
      keyed on the code, not the label.
    - Test 3 (episodeVersionEditorUtils): defaultReleaseTitle with two selected_groups returns
      "Episode 001 · (GroupA × GroupB) · v1" (coop-capable, groups sorted by name then id, matching
      resolveCoopLinkGroupId's exact sort).
    - Test 4: defaultReleaseTitle with one selected group returns "Episode 001 · (GroupName) · v1"
      (single-group case, parens still present per the exact format).
  </behavior>
  <action>
    Delete CLASSIFICATION_LABELS and EPISODE_TYPE_LABELS from episodePreviewFormat.ts entirely.
    Change classificationLabel/episodeTypeLabel to plain pass-through helpers reading the label
    directly off the episode object (or delete them and inline the read at the one call site in
    classificationAndTypeLine, whichever keeps the diff smallest) -- classificationAndTypeLine's new
    signature takes the PublicGroupedEpisode (or its filler_type/filler_type_label/episode_type/
    episode_type_label fields explicitly) and returns
    `filler_type === 'unknown' ? episode_type_label : \`${filler_type_label} · ${episode_type_label}\``.
    Update EpisodeGlassCard.tsx's call site to pass the new fields through.
    In episodeVersionEditorUtils.ts, rewrite defaultReleaseTitle to build the group-name segment from
    ALL of context.selected_groups (not just [0]), sorted with the exact same
    `.sort((a, b) => a.name.localeCompare(b.name) || a.id - b.id)` convention as
    resolveCoopLinkGroupId, joined with ' × ', wrapped in parens: `(${groupNames})`. Keep the existing
    'Episode ' + padEpisodeNumber(...) episode-title-position wording (per the Interfaces section's
    documented discretion) and the existing version fallback (`context.version.release_version ||
    'v1'`). Final format: `Episode ${padded} · (${groupNames}) · ${version}` -- exact middle-dot
    separators matching the backend format, single-group case still wraps the one name in parens (no
    special-case for count===1).
    Before adding new tests, rewrite the stale pre-existing blocks in episodePreviewFormat.test.ts
    that reference the deleted hardcoded maps: the 'classificationLabel' describe block (current
    lines 17-25, asserting classificationLabel('canon')->'Haupthandlung' etc. against the deleted
    CLASSIFICATION_LABELS map) and the 'episodeTypeLabel' describe block (current lines 27-44,
    asserting episodeTypeLabel('episode')->'Episode' etc. against the deleted EPISODE_TYPE_LABELS
    map, plus its "uses a distinct label from classificationLabel for the shared recap string" check)
    must be deleted if you inlined the reads, or rewritten to exercise the new pass-through behavior
    if you kept classificationLabel/episodeTypeLabel as thin wrappers -- match whichever path you
    took above. The 'classificationAndTypeLine' describe block (current lines 46-54, currently
    calling classificationAndTypeLine('unknown','episode') and classificationAndTypeLine('filler',
    'ova') with two bare code-string args) must be rewritten for the new signature per Test 1-2
    above (resolved label strings or the PublicGroupedEpisode object, not two bare codes). Finally,
    prune the test file's top-of-file import statement (currently importing classificationAndTypeLine,
    classificationLabel, episodeTypeLabel, and others from './episodePreviewFormat') to only import
    functions that still exist after this task's rewrite -- if classificationLabel/episodeTypeLabel
    were deleted rather than kept as pass-throughs, remove them from the import list, otherwise the
    whole test file fails to compile.
    Extend episodePreviewFormat.test.ts with Test 1-2, and episodeVersionEditorUtils.test.ts (create it
    if it does not exist) with Test 3-4.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/fansubs/episodePreviewFormat.test.ts frontend/src/app/admin/episode-versions/[versionId]/edit/episodeVersionEditorUtils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "CLASSIFICATION_LABELS\|EPISODE_TYPE_LABELS" frontend/src/components/fansubs/episodePreviewFormat.ts returns 0.
    - grep -c "filler_type_label\|episode_type_label" frontend/src/components/fansubs/episodePreviewFormat.ts returns at least 2.
    - grep -c "selected_groups\[0\]" frontend/src/app/admin/episode-versions/[versionId]/edit/episodeVersionEditorUtils.ts returns 0 (no longer single-group-only).
    - grep -c "localeCompare" frontend/src/app/admin/episode-versions/[versionId]/edit/episodeVersionEditorUtils.ts returns at least 1 (coop sort applied).
    - New tests cover Test 1-4 above and pass.
    - npx tsc --noEmit -p frontend passes (no type errors from the signature changes).
  </acceptance_criteria>
  <done>Public classification/type labels come from the DB via the API response, never a hardcoded frontend map; the admin placeholder mirrors the same "&lt;title&gt; · (&lt;groups&gt;) · &lt;version&gt;" format and is coop-capable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public API response (release_name, filler_type_label, episode_type_label) -> rendered text | Server-controlled data already computed/validated by 164-08/164-10; this plan only changes which field the frontend reads, not how it's rendered (still plain text, no dangerouslySetInnerHTML) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-21 | Tampering | classificationAndTypeLine's new label-consuming signature | accept | Labels are rendered as plain React text children (JSX auto-escapes), identical to how the old hardcoded label strings were rendered -- no new rendering pathway introduced. |
</threat_model>

<verification>
`cd frontend && npx vitest run src/components/fansubs/episodePreviewFormat.test.ts "src/app/admin/episode-versions/[versionId]/edit/episodeVersionEditorUtils.test.ts" && npx tsc --noEmit -p .` -- 0 new failures.
</verification>

<success_criteria>
The public release preview and the admin editor placeholder both use the single backend-computed
default-name rule (164-08); the public classification/type line and the admin's own labels are
sourced from the same DB lookup tables (164-10), never a hardcoded frontend map; the tech line never
shows "Unbekannt".
</success_criteria>

<output>
Create `.planning/phases/164-oeffentliche-anime-seite-episode-release-ui-read-model-infinite-scroll/164-12-SUMMARY.md` when done
</output>
</output>
