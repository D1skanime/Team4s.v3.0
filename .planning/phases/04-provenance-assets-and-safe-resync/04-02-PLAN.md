---
phase: 04-provenance-assets-and-safe-resync
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/repository/anime_assets.go
  - backend/internal/repository/anime_assets_test.go
  - backend/internal/handlers/admin_content_anime_assets.go
  - backend/internal/handlers/jellyfin_metadata_resync.go
  - backend/internal/handlers/jellyfin_metadata_resync_test.go
  - backend/internal/handlers/anime_backdrops_handler.go
  - backend/internal/handlers/anime_backdrops_test.go
  - frontend/src/types/admin.ts
  - frontend/src/lib/api.ts
autonomous: true
requirements:
  - ASST-01
  - ASST-02
  - ASST-03
  - ASST-05
  - OWNR-02
  - RLY-02
must_haves:
  truths:
    - "Admins can inspect persisted `cover`, `banner`, and `backgrounds` on an existing anime and see whether each active slot is manual or provider-owned."
    - "Admins can replace or remove a persisted slot explicitly, and later Jellyfin refresh keeps manual replacements protected instead of silently overwriting them."
    - "Public runtime reads prefer persisted anime assets before Jellyfin fallback, and removing a persisted slot re-enables Jellyfin fallback only for that slot."
    - "Provider-owned banner/background slots can refresh on explicit resync, while manual banner/background slots retain order and identity."
  artifacts:
    - path: "backend/internal/repository/anime_assets.go"
      provides: "Ownership-aware cover/banner/background persistence and provider-refresh rules"
    - path: "backend/internal/handlers/admin_content_anime_assets.go"
      provides: "Admin HTTP endpoints for explicit slot-level replace/remove actions"
    - path: "backend/internal/handlers/anime_backdrops_handler.go"
      provides: "Public runtime precedence of persisted banner/background assets over Jellyfin fallback"
  key_links:
    - from: "backend/internal/handlers/admin_content_anime_assets.go"
      to: "backend/internal/repository/anime_assets.go"
      via: "slot-level assign, add, and delete endpoints"
      pattern: "AssignManualBanner|AddManualBackground|ClearBanner|RemoveBackground|AssignManualCover|ClearCover"
    - from: "backend/internal/handlers/jellyfin_metadata_resync.go"
      to: "backend/internal/repository/anime_assets.go"
      via: "provider-owned asset apply for cover/banner/backgrounds"
      pattern: "ApplyProviderCover|ApplyProviderBanner|ApplyProviderBackgrounds"
    - from: "backend/internal/handlers/anime_backdrops_handler.go"
      to: "backend/internal/repository/anime_assets.go"
      via: "resolved persisted assets before Jellyfin fallback"
      pattern: "GetResolvedAssets"
---

<objective>
Finish the slot-level anime asset persistence and runtime precedence rules for `cover`, `banner`, and `backgrounds` so manual overrides stay protected, provider refresh is explicit, and public/runtime reads are deterministic.

Purpose: close the persisted asset semantics behind the Phase 04 provenance UI before the edit route relies on them as trustworthy operator controls.
Output: repository + handler + runtime behavior for ownership-aware slot actions, protected provider refresh, and persisted-over-fallback reads.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-provenance-assets-and-safe-resync/04-CONTEXT.md
@.planning/phases/04-provenance-assets-and-safe-resync/04-RESEARCH.md
@.planning/phases/04-provenance-assets-and-safe-resync/04-VALIDATION.md
@.planning/phases/04-provenance-assets-and-safe-resync/04-UI-SPEC.md
@backend/internal/repository/anime_assets.go
@backend/internal/repository/anime_assets_test.go
@backend/internal/handlers/admin_content_anime_assets.go
@backend/internal/handlers/jellyfin_metadata_resync.go
@backend/internal/handlers/anime_backdrops_handler.go
@backend/internal/handlers/anime_backdrops_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing coverage for protected slot actions, provider refresh rules, and persisted-over-fallback runtime reads</name>
  <files>backend/internal/repository/anime_assets_test.go, backend/internal/handlers/jellyfin_metadata_resync_test.go, backend/internal/handlers/anime_backdrops_test.go</files>
  <read_first>
    - backend/internal/repository/anime_assets.go
    - backend/internal/handlers/jellyfin_metadata_resync.go
    - backend/internal/handlers/anime_backdrops_handler.go
    - .planning/phases/04-provenance-assets-and-safe-resync/04-CONTEXT.md
    - .planning/phases/04-provenance-assets-and-safe-resync/04-VALIDATION.md
    - .planning/phases/04-provenance-assets-and-safe-resync/04-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: manual `banner` and manual `backgrounds` remain active after explicit Jellyfin metadata apply, while provider-owned slots refresh.
    - Test 2: new provider backgrounds append without deleting or reordering existing manual backgrounds.
    - Test 3: removing a persisted manual banner/background re-enables Jellyfin fallback for that slot on the public runtime read.
    - Test 4: slot-level assign/remove endpoints return predictable not-found or failure responses instead of ambiguous silent success.
    - Test 5: resolved asset reads expose ownership labels that distinguish manual from provider-backed slots.
  </behavior>
  <action>Expand `backend/internal/repository/anime_assets_test.go`, `backend/internal/handlers/jellyfin_metadata_resync_test.go`, and `backend/internal/handlers/anime_backdrops_test.go` to pin the Phase 04 slot contract. Add tests that cover protected manual banner/background persistence, provider refresh for provider-owned slots only, append-only provider background growth, and public runtime precedence switching back to Jellyfin fallback after explicit removal. The handler-side tests must also pin operator-safe error behavior for slot action failures.</action>
  <acceptance_criteria>
    - `backend/internal/repository/anime_assets_test.go` includes tests for manual protection, provider updates, and background append/order behavior.
    - `backend/internal/handlers/anime_backdrops_test.go` includes a case where persisted banner/backgrounds beat Jellyfin fallback and a case where fallback resumes after removal.
    - The new tests fail against any implementation that silently overwrites manual assets or drops persisted runtime precedence.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/repository -run "Test.*Anime.*Asset.*|TestReconcileAnimeProviderBackgrounds.*" -count=1</automated>
    <automated>cd backend && go test ./internal/handlers -run "Test.*JellyfinMetadata.*|Test.*AnimeBackdrops.*" -count=1</automated>
  </verify>
  <done>There is a regression net for slot ownership, protected refresh behavior, and persisted-over-fallback runtime reads.</done>
</task>

<task type="auto">
  <name>Task 2: Finish ownership-aware slot persistence, runtime precedence, and operator-safe slot action handlers</name>
  <files>backend/internal/repository/anime_assets.go, backend/internal/repository/anime_assets_test.go, backend/internal/handlers/admin_content_anime_assets.go, backend/internal/handlers/jellyfin_metadata_resync.go, backend/internal/handlers/jellyfin_metadata_resync_test.go, backend/internal/handlers/anime_backdrops_handler.go, backend/internal/handlers/anime_backdrops_test.go, frontend/src/types/admin.ts, frontend/src/lib/api.ts</files>
  <read_first>
    - backend/internal/repository/anime_assets.go
    - backend/internal/handlers/admin_content_anime_assets.go
    - backend/internal/handlers/jellyfin_metadata_resync.go
    - backend/internal/handlers/anime_backdrops_handler.go
    - frontend/src/types/admin.ts
    - frontend/src/lib/api.ts
    - .planning/phases/04-provenance-assets-and-safe-resync/04-CONTEXT.md
    - .planning/phases/04-provenance-assets-and-safe-resync/04-RESEARCH.md
    - .planning/phases/04-provenance-assets-and-safe-resync/04-UI-SPEC.md
  </read_first>
  <action>Complete the persisted anime asset lifecycle in `backend/internal/repository/anime_assets.go` so `AssignManualCover`, `AssignManualBanner`, `AddManualBackground`, `ClearCover`, `ClearBanner`, `RemoveBackground`, `ApplyProviderCover`, `ApplyProviderBanner`, and `ApplyProviderBackgrounds` obey the locked Phase 04 rules: `manual` slots never auto-overwrite, `provider` slots refresh only on explicit metadata apply, missing slots may fill, and provider background updates append or update by provider key without deleting manual entries or reordering them. In `backend/internal/handlers/admin_content_anime_assets.go`, keep slot actions explicit and operator-safe for assign/add/delete routes. In `backend/internal/handlers/anime_backdrops_handler.go`, ensure persisted banner/background reads win before Jellyfin fallback and that fallback resumes only after the persisted slot is absent. Keep `frontend/src/types/admin.ts` and `frontend/src/lib/api.ts` aligned with these slot action endpoints so the edit route can call them without inventing new shapes.</action>
  <acceptance_criteria>
    - `backend/internal/repository/anime_assets.go` contains separate logic for manual slot assignment, provider slot apply, and provider background reconciliation by `provider_key`.
    - `backend/internal/handlers/admin_content_anime_assets.go` exposes explicit assign/add/delete handlers for cover, banner, and backgrounds with operator-safe error responses.
    - `backend/internal/handlers/anime_backdrops_handler.go` checks persisted assets before probing Jellyfin fallback for banner/background output.
    - `frontend/src/lib/api.ts` exports slot action helpers for cover, banner, and backgrounds that preserve API error details.
    - All automated tests in this plan pass.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/repository -run "Test.*Anime.*Asset.*|TestReconcileAnimeProviderBackgrounds.*" -count=1</automated>
    <automated>cd backend && go test ./internal/handlers -run "Test.*JellyfinMetadata.*|Test.*AnimeBackdrops.*" -count=1</automated>
  </verify>
  <done>The persisted asset layer is trustworthy: slot ownership is explicit, manual overrides survive later resync, and runtime precedence matches the operator mental model.</done>
</task>

</tasks>

<verification>
Run the targeted repository and handler suites, then smoke one linked anime on the public anime route to confirm a persisted banner/background hides Jellyfin fallback until explicitly removed.
</verification>

<success_criteria>
Phase 04 asset semantics are enforced in code: slot actions are explicit, manual replacements stay protected, provider assets refresh only when allowed, and public runtime precedence is deterministic.
</success_criteria>

<output>
After completion, create `.planning/phases/04-provenance-assets-and-safe-resync/04-02-SUMMARY.md`
</output>
