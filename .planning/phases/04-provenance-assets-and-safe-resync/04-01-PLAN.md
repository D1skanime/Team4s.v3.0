---
phase: 04-provenance-assets-and-safe-resync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/models/admin_content.go
  - backend/internal/repository/admin_content_sync.go
  - backend/internal/handlers/jellyfin_metadata_resync.go
  - backend/internal/handlers/jellyfin_metadata_resync_test.go
  - frontend/src/types/admin.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/api.admin-anime.test.ts
autonomous: true
requirements:
  - JFIN-03
  - OWNR-02
  - OWNR-03
  - OWNR-04
  - OWNR-05
  - RLY-02
must_haves:
  truths:
    - "Admins can open an existing linked anime and load a persisted Jellyfin provenance summary that exposes source identity, folder/path context, and current persisted asset ownership without creating or mutating the anime."
    - "Admins can request a metadata preview for an existing anime and see which Jellyfin-backed fields would fill, stay protected, or remain unchanged before any apply action runs."
    - "Admins can apply metadata refresh through the persisted anime route with fill-only semantics: empty fields fill, non-empty manual values stay protected, and intentionally cleared values can refill on a later explicit resync."
    - "Unlinked anime and Jellyfin upstream failures return operator-usable admin error responses instead of ambiguous generic failures."
  artifacts:
    - path: "backend/internal/handlers/jellyfin_metadata_resync.go"
      provides: "Admin provenance, metadata preview, and metadata apply HTTP contract for persisted anime"
    - path: "backend/internal/repository/admin_content_sync.go"
      provides: "Repository seam for persisted sync-source lookup and fill-only metadata apply behavior"
    - path: "frontend/src/lib/api.ts"
      provides: "Typed admin API client methods for Jellyfin provenance, preview, and apply flows with structured error parsing"
  key_links:
    - from: "backend/internal/handlers/jellyfin_metadata_resync.go"
      to: "backend/internal/repository/admin_content_sync.go"
      via: "GetAnimeSyncSource and ApplyJellyfinSyncMetadata"
      pattern: "GetAnimeSyncSource|ApplyJellyfinSyncMetadata"
    - from: "backend/internal/handlers/jellyfin_metadata_resync.go"
      to: "backend/internal/models/admin_content.go"
      via: "serialized provenance, diff, and persisted-asset response types"
      pattern: "AdminAnimeJellyfinProvenanceContext|AdminAnimeJellyfinMetadataPreviewResult|AdminAnimeJellyfinMetadataApplyResult"
    - from: "frontend/src/lib/api.ts"
      to: "backend/internal/handlers/jellyfin_metadata_resync.go"
      via: "GET/POST admin anime jellyfin context/metadata endpoints"
      pattern: "getAdminAnimeJellyfinContext|previewAdminAnimeMetadataFromJellyfin|applyAdminAnimeMetadataFromJellyfin"
---

<objective>
Stabilize the persisted Jellyfin provenance and metadata resync contract so the edit route has a trustworthy backend source-of-truth for linked identity, fill-only diffs, explicit apply, and operator-usable failure modes.

Purpose: close the core post-create metadata maintenance seam before the UI and asset flows build on it.
Output: repository + handler + typed client behavior for persisted provenance, metadata preview/apply, and structured admin errors.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-provenance-assets-and-safe-resync/04-CONTEXT.md
@.planning/phases/04-provenance-assets-and-safe-resync/04-RESEARCH.md
@.planning/phases/04-provenance-assets-and-safe-resync/04-VALIDATION.md
@.planning/phases/04-provenance-assets-and-safe-resync/04-UI-SPEC.md
@backend/internal/models/admin_content.go
@backend/internal/repository/admin_content_sync.go
@backend/internal/handlers/jellyfin_metadata_resync.go
@backend/internal/handlers/jellyfin_metadata_resync_test.go
@frontend/src/types/admin.ts
@frontend/src/lib/api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing coverage for persisted provenance, fill-only metadata preview/apply, and operator-safe failure paths</name>
  <files>backend/internal/handlers/jellyfin_metadata_resync_test.go, frontend/src/lib/api.admin-anime.test.ts</files>
  <read_first>
    - backend/internal/handlers/jellyfin_metadata_resync.go
    - backend/internal/repository/admin_content_sync.go
    - backend/internal/models/admin_content.go
    - frontend/src/lib/api.ts
    - frontend/src/types/admin.ts
    - .planning/phases/04-provenance-assets-and-safe-resync/04-CONTEXT.md
    - .planning/phases/04-provenance-assets-and-safe-resync/04-VALIDATION.md
    - .planning/phases/04-provenance-assets-and-safe-resync/04-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: `GET /api/v1/admin/anime/:id/jellyfin/context` returns persisted linkage state, source kind, folder/path identity, persisted asset ownership, and a non-mutating manual state for unlinked anime.
    - Test 2: metadata preview returns diff rows where empty fields are marked `fill`, existing non-empty values are marked `protect`, and identical values are marked `keep`.
    - Test 3: metadata apply preserves non-empty manual values, allows explicitly empty values to refill, and never creates a new anime record.
    - Test 4: missing linkage and Jellyfin upstream failures preserve explicit admin error payloads that the client can surface with `message`, `code`, and `details`.
    - Test 5: frontend API helpers throw `ApiError` with parsed `code` and `details` for context, preview, and apply endpoints.
  </behavior>
  <action>Create or expand focused regression tests in `backend/internal/handlers/jellyfin_metadata_resync_test.go` and `frontend/src/lib/api.admin-anime.test.ts`. The backend tests must pin the persisted Phase 04 contract: context is read-only, preview is fill-only and explicit, apply reuses the existing anime record, and error payloads stay operator-usable. The frontend tests must pin `getAdminAnimeJellyfinContext(...)`, `previewAdminAnimeMetadataFromJellyfin(...)`, and `applyAdminAnimeMetadataFromJellyfin(...)` so they preserve structured `ApiError` details instead of collapsing failures to a generic string.</action>
  <acceptance_criteria>
    - `backend/internal/handlers/jellyfin_metadata_resync_test.go` contains test cases for context, preview, apply, and error classification rather than only helper mapping assertions.
    - `frontend/src/lib/api.admin-anime.test.ts` contains one test each for context, preview, and apply error parsing with asserted `ApiError.code` or `ApiError.details`.
    - The new tests fail against any implementation that auto-applies metadata, overwrites protected values, or drops structured admin error payloads.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/handlers -run "Test.*JellyfinMetadata.*|Test.*JellyfinContext.*" -count=1</automated>
    <automated>cd frontend && npm test -- src/lib/api.admin-anime.test.ts</automated>
  </verify>
  <done>There is a red-green safety net for persisted provenance, explicit fill-only preview/apply behavior, and operator-safe error transport.</done>
</task>

<task type="auto">
  <name>Task 2: Finish the persisted provenance and metadata preview/apply contract across models, repository, handlers, and typed client seams</name>
  <files>backend/internal/models/admin_content.go, backend/internal/repository/admin_content_sync.go, backend/internal/handlers/jellyfin_metadata_resync.go, backend/internal/handlers/jellyfin_metadata_resync_test.go, frontend/src/types/admin.ts, frontend/src/lib/api.ts, frontend/src/lib/api.admin-anime.test.ts</files>
  <read_first>
    - backend/internal/models/admin_content.go
    - backend/internal/repository/admin_content_sync.go
    - backend/internal/handlers/jellyfin_metadata_resync.go
    - backend/internal/handlers/error_responses.go
    - frontend/src/types/admin.ts
    - frontend/src/lib/api.ts
    - .planning/phases/04-provenance-assets-and-safe-resync/04-CONTEXT.md
    - .planning/phases/04-provenance-assets-and-safe-resync/04-RESEARCH.md
    - .planning/phases/04-provenance-assets-and-safe-resync/04-UI-SPEC.md
  </read_first>
  <action>Complete the persisted anime Jellyfin metadata contract without widening scope beyond edit-route maintenance. In `backend/internal/repository/admin_content_sync.go`, keep `GetAnimeSyncSource(...)` and `ApplyJellyfinSyncMetadata(...)` as the repository source-of-truth for fill-only metadata apply: `source` and `folder_name` may update when empty or when an explicit Jellyfin series ID was provided, `year`, `description`, and `max_episodes` only fill empty values, and no path creates a new anime row. In `backend/internal/handlers/jellyfin_metadata_resync.go`, ensure `GetAnimeJellyfinContext(...)`, `PreviewAnimeMetadataFromJellyfin(...)`, and `ApplyAnimeMetadataFromJellyfin(...)` return stable payloads built from `AdminAnimeJellyfinProvenanceContext`, `AdminAnimeJellyfinMetadataPreviewResult`, and `AdminAnimeJellyfinMetadataApplyResult`, with `diff` actions limited to `fill`, `keep`, or `protect`. Preserve operator-usable error transport by routing upstream Jellyfin failures through structured admin error responses with `message`, `code`, and `details`. In `frontend/src/types/admin.ts` and `frontend/src/lib/api.ts`, keep the typed contract aligned with the backend payloads and expose `getAdminAnimeJellyfinContext(...)`, `previewAdminAnimeMetadataFromJellyfin(...)`, and `applyAdminAnimeMetadataFromJellyfin(...)` as detail-preserving `ApiError` clients.</action>
  <acceptance_criteria>
    - `backend/internal/models/admin_content.go` exports persisted provenance and metadata-preview/apply structs that include persisted asset ownership and diff rows used by the edit route.
    - `backend/internal/handlers/jellyfin_metadata_resync.go` contains all three persisted anime endpoints and no endpoint auto-applies on load.
    - `backend/internal/repository/admin_content_sync.go` contains fill-only update logic for persisted anime metadata and does not overwrite non-empty manual values.
    - `frontend/src/lib/api.ts` exports three Jellyfin metadata client helpers that throw `ApiError` with parsed `code` and `details`.
    - Automated tests in this plan pass after the contract is implemented.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/handlers -run "Test.*JellyfinMetadata.*|Test.*JellyfinContext.*" -count=1</automated>
    <automated>cd frontend && npm test -- src/lib/api.admin-anime.test.ts</automated>
  </verify>
  <done>The edit route has a dependable persisted provenance and metadata resync contract that later UI and asset work can compose without hidden apply behavior or weak error feedback.</done>
</task>

</tasks>

<verification>
Run the targeted backend handler suite and frontend API tests, then manually confirm with a linked anime that loading `/admin/anime/{id}/edit` can fetch a Jellyfin context and metadata preview without mutating the anime before apply.
</verification>

<success_criteria>
The Phase 04 metadata maintenance seam is contractually stable: persisted linked identity is inspectable, fill-only preview/apply behavior is explicit, and admin failures return actionable error context.
</success_criteria>

<output>
After completion, create `.planning/phases/04-provenance-assets-and-safe-resync/04-01-SUMMARY.md`
</output>
