---
phase: 162
plan: 03
type: execute
wave: 3
depends_on: ["162-02"]
files_modified:
  - frontend/src/components/fansubs/FansubVersionBrowser.tsx
  - frontend/src/components/fansubs/FansubVersionBrowser.module.css
  - frontend/src/components/fansubs/FansubVersionBrowser.test.tsx
autonomous: true
requirements: [REQ-162-01, REQ-162-02, REQ-162-03, REQ-162-08, REQ-162-09, REQ-162-13, REQ-162-14, REQ-162-15, REQ-162-18, REQ-162-19, REQ-162-20]

must_haves:
  truths:
    - "Ein Anime ohne Fansub-Gruppe zeigt weder Chip-Zeile noch gruppenspezifischen Bereich noch einen leeren Platzhalter"
    - "Bei genau einer Gruppe ist diese automatisch aktiv, ohne 'Alle'-Chip und ohne Klick"
    - "Bei 2+ Gruppen ist 'Alle' ohne URL-Parameter die Standardauswahl; gruppenspezifische Elemente sind dann vollständig ausgeblendet (nicht disabled)"
    - "Ein Chip-Wechsel erzeugt keinen neuen fachlichen Datenrequest (kein zusätzlicher getGroupedEpisodes-Aufruf)"
    - "Browser Zurück/Vor stellt die jeweils vorherige Gruppenauswahl korrekt wieder her"
    - "Ein ungültiger oder nicht mehr vorhandener fansub-Parameter fällt sauber auf 'Alle' zurück, ohne Fehler"
    - "Eine Coop-Version (mehrere fansub_groups an derselben Version) erzeugt keinen künstlichen dritten Chip — jede beteiligte Gruppe bleibt einzeln auswählbar"
    - "Die bestehende Folgen-/Versionsfilterung (getSummaryVersion, groupMatchedVersions) ist byte-identisch unverändert"
    - "D-01: Die URL ist die einzige Quelle des Gruppenkontexts; die localStorage-Persistenz (anime:<id>:fansub-filter) und der storage-Event-Multitab-Sync aus Phase 159 D-02 werden entfernt, nicht parallel weitergeführt"
    - "D-02: URL-Parameter fansub=<slug> wird nur bei 2+ Gruppen geschrieben; ungültiger/entfernter Slug fällt sauber auf 'Alle' zurück, ohne Umleitung"
    - "D-03: Chip-Klick erzeugt einen neuen Historieneintrag (push, scroll:false) ohne Server-Refetch der Seite"
    - "D-09: Die Gruppenwahl filtert weiterhin die Folgen-Versionen wie heute; 'Alle' bedeutet kein Gruppenfilter"
    - "D-13: Umschalt-Buttons der alten Lösung in FansubVersionBrowser entfallen ersatzlos, keine alte und neue Lösung parallel"
    - "D-14: Eine Coop-Version erzeugt keinen künstlichen dritten Chip, keine neue Coop-Entität — bestehende anime_fansub_groups/release_version_groups-Zuordnung wird verwendet"
  artifacts:
    - path: "frontend/src/components/fansubs/FansubVersionBrowser.tsx"
      provides: "URL-basierter Gruppenzustand (window.history.pushState/popstate), Wiring von FansubGroupPicker + FansubGroupContext"
      contains: "window.history.pushState"
    - path: "frontend/src/components/fansubs/FansubVersionBrowser.test.tsx"
      provides: "Automatisierte Abdeckung der Testfälle A-I aus 162-USER-REQUEST.md §16"
  key_links:
    - from: "frontend/src/components/fansubs/FansubVersionBrowser.tsx"
      to: "window.history / popstate"
      via: "pushState ohne next/navigation router.push"
      pattern: "addEventListener\\('popstate'"
    - from: "frontend/src/components/fansubs/FansubVersionBrowser.tsx"
      to: "frontend/src/components/fansubs/FansubGroupPicker.tsx + FansubGroupContext.tsx"
      via: "Props-Wiring (options/activeGroupId/onSelect, activeGroup/animeSlug)"
      pattern: "FansubGroupPicker|FansubGroupContext"
---

<objective>
`FansubVersionBrowser.tsx` von localStorage-Persistenz auf URL-basierten Gruppenzustand umstellen
(D-01–D-04) und die in Plan 162-02 gebauten `FansubGroupPicker`/`FansubGroupContext`-Komponenten
wiring, inklusive vollständiger 0/1/2+-Gruppen-Logik (§1) und Entfernung der alten
Inline-Chip-/CTA-Markup. Die bestehende Episoden-/Versionsfilterung (D-09) bleibt unangetastet.

Purpose: Dies ist der Kern der Phase — löst die eigentliche Nutzerverwirrung (§ Ausgangslage) auf,
indem genau eine eindeutige Auswahl über die URL geteilt/zurücknavigiert werden kann, ohne
Server-Refetch bei jedem Klick (D-03).
Output: Umgebautes `FansubVersionBrowser.tsx`, bereinigtes CSS-Modul, vollständig neu geschriebene
Testsuite mit Abdeckung der Testfälle A–I.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-oeffentliche-anime-seite-fansub-gruppenauswahl-kurzgeschichte-navigation/162-CONTEXT.md
@.planning/phases/162-oeffentliche-anime-seite-fansub-gruppenauswahl-kurzgeschichte-navigation/162-RESEARCH.md
@.planning/phases/162-oeffentliche-anime-seite-fansub-gruppenauswahl-kurzgeschichte-navigation/162-02-SUMMARY.md

<interfaces>
<!-- Verträge aus Plan 162-02, hier zu konsumieren (siehe 162-02-SUMMARY.md für den tatsächlichen
     Endstand, falls Details während der Ausführung angepasst wurden): -->
```ts
export interface FansubGroupPickerOption { id: number; slug: string; name: string; logo_url?: string | null }
export interface FansubGroupPickerProps {
  options: FansubGroupPickerOption[]
  activeGroupId: number | null
  showAllChip: boolean
  onSelect: (groupId: number | null) => void
}
export interface FansubGroupContextActiveGroup { id: number; slug: string; name: string; story_preview?: string | null }
export interface FansubGroupContextProps { activeGroup: FansubGroupContextActiveGroup | null; animeSlug?: string }
```

<!-- Offizielles Next.js-16-Muster für URL-Updates ohne Server-Refetch (RESEARCH.md "Pattern 1",
     https://nextjs.org/docs/app/getting-started/linking-and-navigating, Abschnitt "Native History API"):
     `window.history.pushState`/`replaceState` synchronisieren mit `usePathname`/`useSearchParams`,
     OHNE einen Server-Component-Refetch auszulösen — anders als `router.push`/`router.replace`, die
     auf dieser bereits dynamischen Route (liest schon `searchParams.grid_query`/`from`) bei jeder
     Navigation einen frischen RSC-Fetch erzwingen würden. -->

<!-- Unverändert zu lassende Filterlogik (FansubVersionBrowser.tsx, aktuelle Zeilen 99-114, 280-284) —
     NICHT anfassen, D-09: -->
```tsx
function getSummaryVersion(episode: PublicGroupedEpisode, activeFansubGroupID: number | null): PublicEpisodeVersion | null {
  if (episode.versions.length === 0) return null
  if (activeFansubGroupID === null) {
    if (episode.default_version_id) {
      const defaultVersion = episode.versions.find((item) => item.variant_id === episode.default_version_id)
      if (defaultVersion) return defaultVersion
    }
    return episode.versions[0]
  }
  const preferred = episode.versions.find((item) => item.fansub_groups?.some((g) => g.id === activeFansubGroupID))
  return preferred || episode.versions[0]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FansubVersionBrowser.tsx — URL-Zustand statt localStorage, Wiring der neuen Komponenten</name>
  <files>frontend/src/components/fansubs/FansubVersionBrowser.tsx, frontend/src/components/fansubs/FansubVersionBrowser.module.css</files>
  <read_first>
    frontend/src/components/fansubs/FansubVersionBrowser.tsx (vollständig — aktueller Stand vor dem Umbau, insbesondere Zeilen 1-53 Imports/Typen/localStorage-Helfer, 127-184 Komponentenkörper mit Mount-Effekt und selectFansubGroup, 239-273 JSX-Render-Block mit altem filterRow/groupCtaRow)
    frontend/src/components/fansubs/FansubGroupPicker.tsx (Endstand aus Plan 162-02)
    frontend/src/components/fansubs/FansubGroupContext.tsx (Endstand aus Plan 162-02)
    frontend/src/components/fansubs/FansubVersionBrowser.module.css (vollständig, aktueller Stand)
  </read_first>
  <action>
    Entfernen: `PersistedFilterState`-Interface, `getStorageKey`, `parseStoredSelection`, den kompletten
    Mount-`useEffect` mit `window.localStorage.getItem`/`storage`-Event-Listener (aktuelle Zeilen
    146-172), den `window.localStorage.setItem`-Aufruf in `selectFansubGroup` sowie die Prop
    `onActiveFansubChange` (tote Prop, keine Aufrufstelle im Repo) aus `FansubVersionBrowserProps` und
    der Destrukturierung. Entfernen: den `import { ActiveFansubStory } from './ActiveFansubStory'`,
    das gesamte `<div className={styles.filterRow}>`-Block (alte native `<button>`-Chips) und den
    `<div className={styles.groupCtaRow}>`-CTA-Block („Gruppenbereich“, D-13).

    Neue Prop `initialActiveSlug?: string | null` zu `FansubVersionBrowserProps` hinzufügen (SSR-Wert
    aus `searchParams.fansub`, D-04 — wird ab Plan 162-04 von `page.tsx` befüllt, in diesem Plan bereits
    im Typ und in der Initiallogik berücksichtigen).

    Zustandsmodell: `fansubOptions`/`validIDs` bleiben wie heute (`collectFansubOptions`). Neu:
    `const showAllChip = fansubOptions.length >= 2`. Initialer State:
    `const [selectedSlug, setSelectedSlug] = useState<string | null>(fansubOptions.length === 1 ? null : (initialActiveSlug ?? null))`
    (bei genau einer Gruppe wird der Parameter laut D-02 ignoriert). Ableitung
    `activeFansubGroupID`: `fansubOptions.length === 0` → `null`; `fansubOptions.length === 1` →
    die ID der einzigen Gruppe (immer aktiv, kein Chip nötig); sonst → die `id` der
    `fansubOptions`-Relation, deren `fansub_group.slug === selectedSlug`, oder `null` („Alle“), wenn kein
    Treffer.

    Neue Funktion `updateFansubSelection(groupID: number | null)`: nur wirksam, wenn
    `fansubOptions.length >= 2` (bei 1 Gruppe gibt es keinen Klickpfad, da kein Chip gerendert wird);
    `slug = groupID === null ? null : fansubOptions.find((r) => r.fansub_group?.id === groupID)?.fansub_group?.slug ?? null`;
    `const params = new URLSearchParams(window.location.search)`; `if (slug) params.set('fansub', slug); else params.delete('fansub')`;
    `const query = params.toString()`; `window.history.pushState(null, '', \`${window.location.pathname}${query ? \`?${query}\` : ''}\`)`
    (NICHT `router.push`/`router.replace` aus `next/navigation` — löst auf dieser bereits dynamischen
    Route einen RSC-Refetch aus und verletzt D-03, siehe RESEARCH.md Pitfall 1); danach
    `setSelectedSlug(slug)`.

    Neuer `useEffect` (Dependency-Array `[]`) registriert einen `popstate`-Listener:
    `function handlePopState() { const params = new URLSearchParams(window.location.search);
    setSelectedSlug(params.get('fansub')) }`; `window.addEventListener('popstate', handlePopState)`;
    Cleanup via `removeEventListener`.

    Render-Reihenfolge (D-10, unter der Section-Öffnung, vor der Episodenliste): wenn
    `fansubOptions.length > 0`, `<FansubGroupPicker options={fansubOptions.map((r) => r.fansub_group!)}
    activeGroupId={activeFansubGroupID} showAllChip={showAllChip} onSelect={updateFansubSelection} />`
    gefolgt von `<FansubGroupContext activeGroup={activeFansubGroupID !== null ?
    (fansubOptions.find((r) => r.fansub_group?.id === activeFansubGroupID)?.fansub_group ?? null) : null}
    animeSlug={animeSlug} />`; wenn `fansubOptions.length === 0`, BEIDE Komponenten NICHT rendern
    (REQ-162-01, §1 „keine Gruppe“) — direkt mit der bestehenden Episodenliste fortfahren.

    NICHT anfassen: `getSummaryVersion`, `groupMatchedVersions`, `mergeEpisodes`, `loadMore`,
    `expandedEpisodes`-State, Pagination-Logik, die komplette Episoden-`<ul>`-JSX (D-09) — nur die jetzt
    tote lokale Variable `groupProjectHref` entfernen (ihre Funktion übernimmt `FansubGroupContext`
    intern).

    CSS-Bereinigung in FansubVersionBrowser.module.css: `.filterRow` (inkl. der drei
    `::-webkit-scrollbar*`-Regeln), `.groupCtaRow`, `.groupButton` (inkl. `:hover`), `.filterChip`,
    `.filterChipActive`, `.logo` vollständig entfernen (Verantwortung liegt jetzt bei
    `FansubGroupPicker.module.css`/`FansubGroupContext.module.css`). `.section`, `.emptyBox`,
    `.episodeList` und alles darunter unverändert lassen.
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "localStorage" frontend/src/components/fansubs/FansubVersionBrowser.tsx` == 0
    - `grep -c "onActiveFansubChange" frontend/src/components/fansubs/FansubVersionBrowser.tsx` == 0
    - `grep -c "window.history.pushState" frontend/src/components/fansubs/FansubVersionBrowser.tsx` >= 1
    - `grep -c "addEventListener('popstate'" frontend/src/components/fansubs/FansubVersionBrowser.tsx` >= 1
    - `grep -c "router.push\|router.replace" frontend/src/components/fansubs/FansubVersionBrowser.tsx` == 0
    - `grep -c "function getSummaryVersion" frontend/src/components/fansubs/FansubVersionBrowser.tsx` == 1 (Funktion weiterhin vorhanden, unverändert dem Namen nach)
    - `npm run typecheck` liefert 0 Fehler
  </acceptance_criteria>
  <done>FansubVersionBrowser.tsx nutzt ausschließlich URL-Zustand (kein localStorage), wiret FansubGroupPicker/FansubGroupContext korrekt, Episoden-/Versionsfilterung unverändert, Typecheck grün.</done>
</task>

<task type="auto">
  <name>Task 2: FansubVersionBrowser.test.tsx — vollständiger Rewrite (Testfälle A-I + D-03-Beweis)</name>
  <files>frontend/src/components/fansubs/FansubVersionBrowser.test.tsx</files>
  <read_first>
    frontend/src/components/fansubs/FansubVersionBrowser.test.tsx (vollständig, aktueller Stand vor dem Rewrite)
    frontend/src/components/fansubs/FansubVersionBrowser.tsx (Endstand aus Task 1 dieses Plans)
    162-VALIDATION.md (Abschnitt "Wave-0-Lücken" — Postgres-Test bereits in 162-01 erledigt; die beiden verbleibenden Punkte: pushState/popstate-Component-Tests und der vollständige Testdatei-Rewrite gehören zu dieser Task)
    162-RESEARCH.md (Abschnitt "Assumptions Log", Zeile A1 — MEDIUM-Confidence-Risiko beim popstate-Pfad, das hier per echtem Component-Test verifiziert werden muss)
  </read_first>
  <action>
    Entfernen: die `storage()`-Helper-Funktion, das komplette `describe('one deterministic group
    owner', ...)`-Block mit allen `StorageEvent`/`Storage.prototype.setItem`-Spies und
    localStorage-Fixtures. Die `fansubs`-Fixture am Dateianfang bleibt bei 2 Gruppen (deckt „Alle“
    default), ergänzt um eine eigene 1-Gruppen-Fixture (`const singleFansub = [fansubs[0]]`) und eine
    0-Gruppen-Fixture (`const noFansubs: AnimeFansubRelation[] = []`) für die neuen Fälle.

    Neue `it`-Blöcke ergänzen (jeweils mit Testfall-Kennung aus §16 im Testnamen):
    - Testfall A: `render(<FansubVersionBrowser animeID={22} fansubs={noFansubs} episodes={[]} />)` →
      `screen.queryByRole('group', { name: 'Fansub-Gruppe' })` ist `null`, `screen.queryByText('Alle')`
      ist `null`.
    - Testfall B/C: `render(<FansubVersionBrowser animeID={22} fansubs={singleFansub}
      storyGroups={buildFansubStoryGroups(singleFansub)} episodes={stateEpisodes} />)` → kein Element mit
      Text „Alle“; der einzige Chip hat `aria-pressed="true"` OHNE vorherigen Klick; Gruppenname als
      Überschrift sichtbar.
    - Testfall D: `render(stateBrowser())` (2 Gruppen, kein `initialActiveSlug`) → „Alle“-Chip
      `aria-pressed="true"`; `screen.queryByRole('article')` (Gruppenbereich) ist `null`.
    - Testfall E/F: Klick auf einen Gruppen-Chip → Gruppenbereich zeigt Name/Story dieser Gruppe; Klick
      auf die andere Gruppe → Inhalt wechselt vollständig (alter Gruppenname verschwindet aus dem
      Gruppenbereich).
    - Testfall G: `render(<FansubVersionBrowser ... initialActiveSlug="saved-secondary" ... />)` (Slug
      einer existierenden Gruppe) → deren Chip ist beim ersten Render bereits `aria-pressed="true"`, ohne
      Klick.
    - Testfall H: `initialActiveSlug="does-not-exist"` → „Alle“-Chip `aria-pressed="true"`, kein Absturz,
      kein Chip fälschlich aktiv.
    - D-03-Beweis (kein Refetch): `groupedMock.mockClear()` vor dem Klick, Klick auf einen Chip, danach
      `expect(getGroupedEpisodes).not.toHaveBeenCalled()`.
    - Zurück/Vor-Beweis (Assumptions Log A1): Klick auf Chip A (State ändert sich, `pushState` wurde
      aufgerufen), dann im Test selbst `window.history.pushState(null, '', <vorherige-URL-ohne-fansub>)`
      gefolgt von `act(() => window.dispatchEvent(new PopStateEvent('popstate')))` — assert, dass die
      Auswahl auf „Alle“/die vorherige Gruppe zurückspringt, OHNE dass `getGroupedEpisodes` erneut
      aufgerufen wurde.
    - Coop-Fixture (Testfall I): eine Version mit `fansub_groups: [fansubs[0].fansub_group!,
      fansubs[1].fansub_group!]` — beide Gruppen bleiben einzeln als Chips wählbar (kein dritter
      „Coop“-Chip, `screen.queryByText(/coop/i)` ist `null`), Auswahl jeder einzelnen Gruppe zeigt die
      geteilte Version.
    - a11y: `screen.getByRole('group', { name: 'Fansub-Gruppe' })` vorhanden, sobald `fansubOptions.length
      >= 2`.

    Anpassen (nicht löschen): das bestehende `describe('authoritative anime project navigation', ...)` —
    alle Vorkommen von `screen.getByRole('link', { name: 'Zum Gruppenbereich' })` durch die zwei
    getrennten Assertions `screen.getByRole('link', { name: 'Zur Fansub-Gruppe' })` (href
    `/fansubs/<slug>`) und `screen.getByRole('link', { name: 'Zum Projekt' })` (href
    `/fansubs/<slug>/fansubprojekt/<animeSlug>`) ersetzen; der Test „retains numeric compatibility …“
    (Fallback auf `/anime/22/group/7`) entfällt ersatzlos (D-12 verbietet diesen Fallback für „Zum
    Projekt“ jetzt) — stattdessen durch einen Test ersetzen, der beweist, dass „Zum Projekt“ bei
    fehlendem `animeSlug` gar nicht gerendert wird.

    UNVERÄNDERT lassen: das komplette `describe('bounded public inventory continuation', ...)` sowie den
    finalen `it('deduplicates variants only inside the same canonical episode', ...)`-Test — diese
    prüfen Pagination/Merge-Logik, die laut D-09 nicht angefasst wird.
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend npx vitest run src/components/fansubs/FansubVersionBrowser.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "StorageEvent" frontend/src/components/fansubs/FansubVersionBrowser.test.tsx` == 0
    - `grep -c "Zum Gruppenbereich" frontend/src/components/fansubs/FansubVersionBrowser.test.tsx` == 0
    - `grep -c "pushState" frontend/src/components/fansubs/FansubVersionBrowser.test.tsx` >= 1
    - `grep -c "popstate" frontend/src/components/fansubs/FansubVersionBrowser.test.tsx` >= 1
    - `grep -c "not.toHaveBeenCalled" frontend/src/components/fansubs/FansubVersionBrowser.test.tsx` >= 1 (D-03-Beweis vorhanden)
    - vitest run: 0 Fehler, keine `skip`/`todo`-Tests im Diff
  </acceptance_criteria>
  <done>FansubVersionBrowser.test.tsx deckt Testfälle A-I, den D-03-No-Refetch-Beweis und das popstate-Assumption-Log-Risiko automatisiert ab; komplett grün, keine localStorage-Reste.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser-URL (`?fansub=`) → Client-Zustand | Nutzergesteuerter Query-Parameter fließt in die Chip-Auswahl |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-05 | Tampering / Information Disclosure | `?fansub=<beliebiger-string>` in `FansubVersionBrowser.tsx` | mitigate | Der Parameterwert wird ausschließlich zum Vergleich gegen die bereits geladene, serverseitig autoritative `fansubOptions`-Liste verwendet (`fansubOptions.find(...)`) — niemals in HTML interpoliert oder als Redirect-Ziel genutzt; kein Treffer → sauberer Fallback auf „Alle“ (D-02), React escaped Text-Rendering ohnehin automatisch |
</threat_model>

<verification>
- `docker compose exec -T team4sv30-frontend npx vitest run src/components/fansubs` — vollständige Fansub-Komponentensuite grün
- `docker compose exec -T team4sv30-frontend npm run typecheck` — keine Typfehler
- `docker compose exec -T team4sv30-frontend npx vitest run` — volle Frontend-Suite grün (Regressionscheck gegen page.test.tsx vor Plan 162-04)
</verification>

<success_criteria>
- Kein localStorage-/StorageEvent-Code mehr in FansubVersionBrowser.tsx
- 0/1/2+-Gruppen-Logik inkl. „Alle“-Default vollständig automatisiert getestet (Testfälle A-I)
- Chip-Wechsel erzeugt nachweislich keinen zusätzlichen Datenrequest
- Bestehende Episoden-/Versionsfilterung byte-identisch unverändert
</success_criteria>

<output>
Create `.planning/phases/162-oeffentliche-anime-seite-fansub-gruppenauswahl-kurzgeschichte-navigation/162-03-SUMMARY.md` when done
</output>
