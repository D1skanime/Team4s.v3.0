---
phase: 162
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/models/fansub.go
  - backend/internal/repository/fansub_repository.go
  - backend/internal/repository/fansub_repository_test.go
  - backend/internal/repository/fansub_story_preview_postgres_test.go
  - shared/contracts/openapi.yaml
  - frontend/src/types/fansub.ts
autonomous: true
requirements: [REQ-162-10, REQ-162-16, REQ-162-17]

must_haves:
  truths:
    - "GET /api/v1/anime/{id}/fansubs liefert für jede Gruppe mit einer veröffentlichten öffentlichen Geschichte einen nicht-leeren story_preview-Wert, ohne einen zusätzlichen Request pro Gruppe"
    - "Gruppen ohne veröffentlichte öffentliche Geschichte (fehlende Zeile oder nicht-public/nicht-published/gelöscht) liefern story_preview als null/fehlend"
    - "Lange Geschichtstexte werden serverseitig rune-sicher auf maximal 500 Zeichen gekürzt, ohne Umlaute mitten im Byte zu zerschneiden"
    - "Die bestehende Gruppenreihenfolge (is_primary DESC, name ASC) bleibt durch die Erweiterung unverändert"
    - "D-05, D-06: story_preview liefert ausschließlich body_text der ersten öffentlichen, veröffentlichten fansub_group_notes-Geschichte (Filter/Sortierung wie listPublicFansubStories), serverseitig rune-sicher auf 500 Zeichen gekürzt"
  artifacts:
    - path: "backend/internal/models/fansub.go"
      provides: "FansubGroupSummary.StoryPreview additives Feld"
      contains: "StoryPreview"
    - path: "backend/internal/repository/fansub_repository.go"
      provides: "LATERAL-Join Story-Vorschau in ListAnimeFansubs, rune-sichere Kürzung"
      contains: "LEFT JOIN LATERAL"
    - path: "shared/contracts/openapi.yaml"
      provides: "story_preview additive Property am FansubGroupSummary-Schema"
      contains: "story_preview"
    - path: "frontend/src/types/fansub.ts"
      provides: "story_preview TS-Feld an FansubGroupSummary"
      contains: "story_preview"
    - path: "backend/internal/repository/fansub_story_preview_postgres_test.go"
      provides: "echter Postgres-Integrationstest für die LATERAL-Join-Erweiterung"
  key_links:
    - from: "backend/internal/repository/fansub_repository.go (ListAnimeFansubs)"
      to: "fansub_group_notes"
      via: "LEFT JOIN LATERAL ... ON true"
      pattern: "LEFT JOIN LATERAL"
    - from: "frontend/src/types/fansub.ts (FansubGroupSummary)"
      to: "shared/contracts/openapi.yaml (FansubGroupSummary)"
      via: "additive story_preview Property, synchron in beiden Schichten"
      pattern: "story_preview"
---

<objective>
Additive Backend-/Contract-Erweiterung: `ListAnimeFansubs` liefert pro Fansub-Gruppe eine
rune-sicher gekürzte Vorschau ihrer ersten veröffentlichten öffentlichen Geschichte
(`story_preview`), gebündelt in der bestehenden Query (kein N+1). Go-Model, OpenAPI-Contract und
TS-Typ werden synchron additiv erweitert (D-05/D-06/D-13-Vorbereitung, REQ-162-10/16/17).

Purpose: Alle nachfolgenden Frontend-Pläne (162-02 ff.) brauchen dieses Feld, um die
Kurzgeschichte im gruppenspezifischen Bereich zu rendern, ohne einen zusätzlichen Request pro
Gruppe oder das volle Fansub-Profil zu laden.
Output: Erweiterte `ListAnimeFansubs`-Query samt Go-Model, OpenAPI-Schema, TS-Typ und
Postgres-Integrationstest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-oeffentliche-anime-seite-fansub-gruppenauswahl-kurzgeschichte-navigation/162-CONTEXT.md
@.planning/phases/162-oeffentliche-anime-seite-fansub-gruppenauswahl-kurzgeschichte-navigation/162-RESEARCH.md

<interfaces>
<!-- Aktuelle ListAnimeFansubs-Query (backend/internal/repository/fansub_repository.go:1286-1294),
     unverändert außer der additiven LATERAL-Erweiterung: -->
```go
rows, err := r.db.Query(ctx, `
    SELECT
        afg.anime_id, afg.fansub_group_id, afg.is_primary, afg.notes, afg.created_at,
        fg.id, fg.slug, fg.name, fg.logo_url, fg.founded_year, fg.dissolved_year, fg.country, fg.status
    FROM anime_fansub_groups afg
    JOIN fansub_groups fg ON fg.id = afg.fansub_group_id
    WHERE afg.anime_id = $1
    ORDER BY afg.is_primary DESC, fg.name ASC
`, animeID)
```

<!-- Story-Filter-Semantik, die die neue LATERAL-Subquery 1:1 übernehmen muss
     (backend/internal/repository/fansub_repository.go:397-406, listPublicFansubStories): -->
```go
SELECT id, title, body_html, body_text, COALESCE(updated_at, created_at)::text
FROM fansub_group_notes
WHERE fansub_group_id = $1
  AND visibility = 'public'
  AND status = 'published'
  AND deleted_at IS NULL
ORDER BY sort_order ASC, id ASC
```

<!-- Aktuelles Go-Model (backend/internal/models/fansub.go:120-130): -->
```go
type FansubGroupSummary struct {
	ID            int64   `json:"id"`
	Slug          string  `json:"slug"`
	Name          string  `json:"name"`
	LogoURL       *string `json:"logo_url,omitempty"`
	FoundedYear   *int32  `json:"founded_year,omitempty"`
	DissolvedYear *int32  `json:"dissolved_year,omitempty"`
	Country       *string `json:"country,omitempty"`
	Status        string  `json:"status,omitempty"`
}
```

<!-- Aktueller TS-Typ (frontend/src/types/fansub.ts:49-58): -->
```ts
export interface FansubGroupSummary {
  id: number;
  slug: string;
  name: string;
  logo_url?: string | null;
  founded_year?: number | null;
  dissolved_year?: number | null;
  country?: string | null;
  status?: FansubStatus;
}
```

<!-- Etabliertes Postgres-Integrationstest-Muster (backend/internal/repository/anime_relations_admin_postgres_test.go,
     vollständig): env-DSN-gated (`os.Getenv`, `t.Skipf` wenn leer), Datenbanknamens-Regex-Guard
     gegen versehentliches Laufen gegen team4s_v2, `pgxpool.NewWithConfig`, Fixture-Seed/Cleanup
     via `t.Cleanup`, hohe ID-Range zur Kollisionsvermeidung (926_160_00x-Muster). Für diese Phase:
     eigener DSN-Env-Name `TEAM4S_FANSUB_TEST_DSN`, eigener Datenbanknamens-Regex
     `^team4s_fansub_test(?:_[a-z0-9]+)?$`, eigene ID-Range (z. B. 926_162_0xx). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rune-sichere Story-Vorschau — Go-Model, SQL-Erweiterung, Scan</name>
  <files>backend/internal/models/fansub.go, backend/internal/repository/fansub_repository.go, backend/internal/repository/fansub_repository_test.go</files>
  <read_first>
    backend/internal/repository/fansub_repository.go (Zeilen 1274-1329 ListAnimeFansubs, Zeilen 397-430 listPublicFansubStories als Filter-Vorlage, Import-Block Zeilen 1-14 — `strings` ist bereits importiert)
    backend/internal/models/fansub.go (Zeilen 120-130 FansubGroupSummary)
    backend/internal/repository/fansub_repository_test.go (Zeilen 220-247, der bestehende `TestListAnimeFansubs_SummaryIncludesFansubStoryFacts`-Test, der NICHT als Vorlage für neue Tests dienen darf, aber dessen gepinntes SQL-Fragment `fg.founded_year, fg.dissolved_year, fg.country, fg.status` nach der Erweiterung weiterhin als zusammenhängender Substring existieren muss)
  </read_first>
  <behavior>
    - `truncateStoryPreviewRunes(nil, 500)` -> `nil`
    - `truncateStoryPreviewRunes(ptr(""), 500)` -> `nil`
    - `truncateStoryPreviewRunes(ptr("   "), 500)` -> `nil` (nur Whitespace zählt als leer)
    - `truncateStoryPreviewRunes(ptr("kurzer Text"), 500)` -> unverändert zurückgegeben (kein Padding, kein Abschneiden)
    - `truncateStoryPreviewRunes(ptr(strings.Repeat("ä", 600)), 500)` -> Ergebnis hat exakt 500 Runes (`len([]rune(*result)) == 500`), `utf8.ValidString(*result)` ist `true` (kein zerschnittenes Mehrbyte-Zeichen)
  </behavior>
  <action>
    In backend/internal/models/fansub.go an `FansubGroupSummary` (nach dem `Status`-Feld) additiv anhängen:
    `StoryPreview *string \`json:"story_preview,omitempty"\``.

    In backend/internal/repository/fansub_repository.go eine neue Konstante `const fansubStoryPreviewRuneLimit = 500`
    und eine neue Funktion `truncateStoryPreviewRunes(raw *string, limit int) *string` ergänzen: `nil` bei
    `raw == nil`; `strings.TrimSpace` anwenden, bei leerem Ergebnis `nil` zurückgeben; via `[]rune(trimmed)`
    kürzen (niemals Byte-Slicing, siehe RESEARCH.md Pitfall 2); Ergebnis als `*string` zurückgeben.

    `ListAnimeFansubs` (Zeilen 1286-1294) um einen `LEFT JOIN LATERAL`-Zweig erweitern, der exakt die
    Filter-/Sortiersemantik von `listPublicFansubStories` übernimmt (`visibility = 'public'`,
    `status = 'published'`, `deleted_at IS NULL`, `ORDER BY sort_order ASC, id ASC`, `LIMIT 1`), zusätzlich
    `story.body_text` als letzte SELECT-Spalte, `story ON true`. Die bestehende Spaltenreihenfolge/das
    bestehende `WHERE`/`ORDER BY` NICHT verändern — `fg.founded_year, fg.dissolved_year, fg.country, fg.status`
    muss als zusammenhängender Substring erhalten bleiben (pinning-Test in Task-Lesebasis). Im Scan-Block
    (Zeilen 1304-1318) eine zusätzliche `var storyBodyText *string`-Scan-Zielvariable als letztes Scan-Argument
    ergänzen; danach `group.StoryPreview = truncateStoryPreviewRunes(storyBodyText, fansubStoryPreviewRuneLimit)`
    setzen, bevor `item.FansubGroup = &group` zugewiesen wird.

    In backend/internal/repository/fansub_repository_test.go einen neuen Table-Test
    `TestTruncateStoryPreviewRunes` ergänzen, der exakt die fünf Fälle aus `<behavior>` als
    Verhaltens-Assertions gegen die echte Funktion prüft (kein String-Matching des Quelltexts,
    kein `os.ReadFile`-Muster — CLAUDE.md-Teststil-Pflicht).
  </action>
  <verify>
    <automated>docker exec -w /app team4sv30-backend go test ./internal/repository -run 'TestTruncateStoryPreviewRunes|TestListAnimeFansubs_SummaryIncludesFansubStoryFacts' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LEFT JOIN LATERAL" backend/internal/repository/fansub_repository.go` == 1
    - `grep -c "story.body_text" backend/internal/repository/fansub_repository.go` >= 1
    - `grep -c "StoryPreview" backend/internal/models/fansub.go` >= 1
    - `grep -c "fg.founded_year, fg.dissolved_year, fg.country, fg.status" backend/internal/repository/fansub_repository.go` >= 1 (bestehender Pin-Test bleibt grün, unverändert)
    - `go test` (Verify-Kommando) zeigt PASS für `TestTruncateStoryPreviewRunes` (alle 5 Unterfälle) UND für den unveränderten `TestListAnimeFansubs_SummaryIncludesFansubStoryFacts`
  </acceptance_criteria>
  <done>ListAnimeFansubs scannt und liefert StoryPreview über einen gebündelten LATERAL-Join; die Kürzung ist rune-sicher und per echtem Unit-Test bewiesen; der bestehende Pin-Test bleibt unangetastet grün.</done>
</task>

<task type="auto">
  <name>Task 2: OpenAPI- und TS-Contract-Sync für story_preview</name>
  <files>shared/contracts/openapi.yaml, frontend/src/types/fansub.ts</files>
  <read_first>
    shared/contracts/openapi.yaml (Zeilen 14132-14149, FansubGroupSummary-Schema)
    frontend/src/types/fansub.ts (Zeilen 49-58, FansubGroupSummary-Interface)
  </read_first>
  <action>
    In shared/contracts/openapi.yaml am `FansubGroupSummary`-Schema (nach der `logo_url`-Property) additiv
    ergänzen: `story_preview:` mit `type: string` und `nullable: true` — NICHT zu `required` hinzufügen.
    Die bestehende, unabhängig von dieser Phase existierende Schema-Drift (fehlende
    `founded_year`/`dissolved_year`/`country`/`status`-Properties in diesem Schema, obwohl Go-Model und
    TS-Typ sie bereits haben) NICHT beheben — außerhalb des Auftragsscopes (§ Arbeitsweise Punkt 9,
    "keine unrelated Refactorings").

    In frontend/src/types/fansub.ts am `FansubGroupSummary`-Interface (nach `status?: FansubStatus;`)
    additiv ergänzen: `story_preview?: string | null;`. Dieses Interface wird auch von
    `PublicEpisodeVersion.fansub_groups[]` und `FansubProjectResolution` konsumiert — dort bleibt das Feld
    schlicht ungesetzt, es ist nur an `GET /api/v1/anime/{id}/fansubs` befüllt (kein Fallback-Wert setzen).
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "story_preview" shared/contracts/openapi.yaml` >= 1
    - `grep -c "story_preview" frontend/src/types/fansub.ts` == 1
    - `story_preview` erscheint in beiden Dateien NICHT in einer `required:`-Liste
    - `npm run typecheck` liefert 0 Fehler
  </acceptance_criteria>
  <done>OpenAPI-Schema und TS-Typ tragen synchron das additive, optionale story_preview-Feld; Typecheck grün.</done>
</task>

<task type="auto">
  <name>Task 3: Postgres-Integrationstest für die LATERAL-Join-Erweiterung</name>
  <files>backend/internal/repository/fansub_story_preview_postgres_test.go</files>
  <read_first>
    backend/internal/repository/anime_relations_admin_postgres_test.go (vollständig, Struktur-Vorlage: DSN-Env-Guard, Datenbanknamens-Regex, Fixture-Seed/Cleanup, echter Repository-Aufruf, `require.Equal`-Assertions)
    backend/internal/repository/fansub_repository.go (ListAnimeFansubs nach Task-1-Änderung, listPublicFansubStories als Filtersemantik-Referenz)
    backend/internal/models/fansub.go (FansubGroupSummary.StoryPreview nach Task 1)
  </read_first>
  <action>
    Neue Datei backend/internal/repository/fansub_story_preview_postgres_test.go im Package `repository`
    anlegen, exakt nach dem Muster von `anime_relations_admin_postgres_test.go`: DSN-Env-Konstante
    `const fansubStoryPreviewDSNEnv = "TEAM4S_FANSUB_TEST_DSN"`; Datenbanknamens-Guard-Regex
    `^team4s_fansub_test(?:_[a-z0-9]+)?$`; `t.Skipf`, wenn die Env-Variable leer ist; ID-Range
    `926_162_0xx` für Anime/Gruppen, um Kollisionen mit anderen Fixtures auszuschließen.

    Fixture: 1 Anime, 3 `fansub_groups` (Gruppe A `is_primary=true`, Gruppe B `is_primary=false` mit Name
    alphabetisch vor/nach Gruppe C zur Sortierprüfung, Gruppe C ohne gültige Story), `anime_fansub_groups`-
    Zeilen für alle drei. `fansub_group_notes`: für Gruppe A zwei Zeilen — eine mit früherem `sort_order`
    und einem >500 Runes langen Text voller Umlaute (`ä`/`ö`/`ü`/`ß`, `visibility='public'`,
    `status='published'`, `deleted_at IS NULL`), eine zweite mit späterem `sort_order` (darf NICHT
    ausgewählt werden — beweist `ORDER BY sort_order ASC, id ASC LIMIT 1`); für Gruppe B eine Zeile mit
    kurzem Text (< 500 Runes, unverändert erwartet); für Gruppe C ausschließlich eine Zeile mit
    `status='draft'` ODER `visibility != 'public'` (muss ignoriert werden, Ergebnis `nil`).

    Test `TestListAnimeFansubs_StoryPreview` ruft `NewFansubRepository(pool).ListAnimeFansubs(ctx, animeID)`
    (oder den tatsächlichen Konstruktornamen aus fansub_repository.go — im read_first verifizieren) auf und
    prüft: (1) Rückgabereihenfolge weiterhin `is_primary DESC, name ASC` unverändert; (2) Gruppe A hat
    `StoryPreview` mit genau `fansubStoryPreviewRuneLimit`-Runes-Länge und `utf8.ValidString`; (3) Gruppe A
    Text stammt aus der Zeile mit dem früheren `sort_order`, nicht der späteren; (4) Gruppe B `StoryPreview`
    entspricht exakt dem gespeicherten kurzen Text; (5) Gruppe C `StoryPreview` ist `nil`.
  </action>
  <verify>
    <automated>docker exec -w /app team4sv30-backend go vet ./internal/repository && docker exec -w /app team4sv30-backend go test ./internal/repository -run TestListAnimeFansubs_StoryPreview -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./internal/repository` liefert keine Fehler (Datei kompiliert)
    - `go test -run TestListAnimeFansubs_StoryPreview` liefert entweder PASS (wenn `TEAM4S_FANSUB_TEST_DSN` gesetzt ist) oder eine explizite `--- SKIP` mit Verweis auf `TEAM4S_FANSUB_TEST_DSN` (beides gültig, ein stiller Erfolg ohne Ausführung ist nicht akzeptabel)
    - Die Datei verwendet KEIN `os.ReadFile("fansub_repository.go")` + `strings.Contains`-Muster (verbotenes Altmuster laut CLAUDE.md)
  </acceptance_criteria>
  <done>Neuer, real ausführender Postgres-Integrationstest beweist die LATERAL-Join-Erweiterung end-to-end (Kürzung, Rune-Sicherheit, Filterung, Sortierung), ohne das verbotene String-Matching-Testmuster fortzuführen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| API → Client | `GET /api/v1/anime/{id}/fansubs` bleibt öffentlich, unauthentifiziert lesbar; neues Feld `story_preview` ist reiner Text, kein HTML |
| Repository → Postgres | `animeID` bleibt einziger externer Parameter in der erweiterten Query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-01 | Tampering | `ListAnimeFansubs` LATERAL-Join-SQL | mitigate | Bestehendes `pgx`-Parameter-Binding (`$1` für `animeID`) unverändert; die neue LATERAL-Subquery referenziert ausschließlich Spalten (`fg.id`), keine neuen Nutzereingaben fließen in die SQL-Struktur ein |
| T-162-02 | Information Disclosure | `story_preview`-Feld an fremden `FansubGroupSummary`-Konsumenten (`PublicEpisodeVersion.fansub_groups[]`, `FansubProjectResolution`) | accept | Feld ist `omitempty`/optional; dort einfach ungesetzt, kein Datenleck, da nur `ListAnimeFansubs` es befüllt |
</threat_model>

<verification>
- `docker exec -w /app team4sv30-backend go test ./internal/repository -run 'Fansub' -count=1` — gesamte Fansub-Repository-Testsuite grün (bestehende + neue Tests)
- `docker compose exec -T team4sv30-frontend npm run typecheck` — TS-Typ kompiliert fehlerfrei mit dem neuen Feld
</verification>

<success_criteria>
- `ListAnimeFansubs` liefert `story_preview` gebündelt in einer Query, kein zusätzlicher Request pro Gruppe
- Rune-sichere Kürzung bewiesen durch echten Unit-Test (Task 1) und Postgres-Integrationstest (Task 3)
- OpenAPI/Go/TS synchron additiv erweitert, keine bestehende Property als `required` markiert
- Bestehender Pin-Test `TestListAnimeFansubs_SummaryIncludesFansubStoryFacts` bleibt unverändert grün
</success_criteria>

<output>
Create `.planning/phases/162-oeffentliche-anime-seite-fansub-gruppenauswahl-kurzgeschichte-navigation/162-01-SUMMARY.md` when done
</output>
