---
phase: 162
plan: 05
type: execute
wave: 5
depends_on: ["162-04"]
files_modified: []
autonomous: false
requirements: [REQ-162-21]

must_haves:
  truths:
    - "Alle in §17 geforderten Konstellationen wurden gegen den aktuellen Live-Datenbestand desktop und mobil geprüft, mit konkreten Anime-/Gruppen-IDs"
    - "Kein Agenten-Schreibzugriff auf die Datenbank fand während der Verifikation statt (ausschließlich SELECT)"
    - "Jede nicht per Live-Daten abdeckbare Konstellation ist explizit als solche dokumentiert (kein stillschweigendes Auslassen)"
  artifacts: []
  key_links: []
---

<objective>
Vor der Browser-Verifikation den aktuellen, tatsächlichen Datenbestand read-only inventarisieren
(§17-Matrix auf reale Anime-/Gruppen-/Coop-IDs mappen, da der Auftraggeber die Testdaten selbst
pflegt und sich der Stand seit der Recherche geändert haben kann), danach Desktop- und
Mobile-Verifikation aller in §16/§17 geforderten Konstellationen durchführen.

Purpose: Schließt die Phase mit dem einzigen in dieser Umgebung nicht automatisierbaren Prüfschritt
ab (kein Browser-Automatisierungs-Tool verfügbar, siehe STATE.md Phase-160-Präzedenzfall) — ohne
diesen Schritt bleibt unklar, ob die implementierten Regeln auch visuell/interaktiv korrekt wirken.
Output: Aktualisierte Datenbestands-Zuordnungstabelle + zwei abgeschlossene Human-Verify-Checkpoints
(Desktop, Mobile).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-oeffentliche-anime-seite-fansub-gruppenauswahl-kurzgeschichte-navigation/162-USER-REQUEST.md
@.planning/phases/162-oeffentliche-anime-seite-fansub-gruppenauswahl-kurzgeschichte-navigation/162-CONTEXT.md
@.planning/phases/162-oeffentliche-anime-seite-fansub-gruppenauswahl-kurzgeschichte-navigation/162-RESEARCH.md

<interfaces>
<!-- Baseline-Queries aus 162-RESEARCH.md "Read-only Datenbestands-Inventar" (Stand 2026-09-17,
     ausschließlich SELECT), hier read-only erneut auszuführen, da sich der Bestand seither geändert
     haben kann: -->
```sql
-- Query 1: Gruppen pro Anime, mit Logo-Status
SELECT a.id AS anime_id, a.slug, a.title,
       fg.id AS group_id, fg.slug AS group_slug, fg.name AS group_name,
       fg.logo_url, afg.is_primary
FROM anime_fansub_groups afg
JOIN anime a ON a.id = afg.anime_id
JOIN fansub_groups fg ON fg.id = afg.fansub_group_id
ORDER BY a.id, afg.is_primary DESC, fg.name;

-- Query 2: Coop-Release-Versionen (2+ Gruppen an derselben Version)
SELECT rvg.release_version_id, COUNT(*) AS group_count, array_agg(fg.name ORDER BY fg.name) AS groups
FROM release_version_groups rvg
JOIN fansub_groups fg ON fg.id = rvg.fansub_group_id
GROUP BY rvg.release_version_id HAVING COUNT(*) > 1;

-- Query 3: Öffentliche Fansub-Geschichten
SELECT fansub_group_id, id, title, visibility, status, deleted_at IS NOT NULL AS deleted,
       length(body_text) AS body_len, sort_order
FROM fansub_group_notes ORDER BY fansub_group_id, sort_order, id;

-- Query 4 (neu, für Testfall A): Anime ohne jede Fansub-Gruppen-Zuordnung
SELECT a.id, a.slug, a.title
FROM anime a
LEFT JOIN anime_fansub_groups afg ON afg.anime_id = a.id
WHERE afg.anime_id IS NULL
LIMIT 5;
```

<!-- Letzter bekannter Stand aus RESEARCH.md (2026-09-17, KANN veraltet sein — Query 1-4 oben liefern
     den aktuellen Stand): Anime 1 (buddy-complex) = 1 Gruppe mit Logo, keine Story; Anime 2 (11eyes) =
     3 Gruppen, alle mit Logo+Story; Anime 3 (11eyes-pink-phantasmagoria) = 1 Gruppe mit Logo+Story;
     Anime 4 (naruto) = 2 Gruppen, echte Coop-Version release_version_id=60. Keine Gruppe ohne Logo zum
     Recherchezeitpunkt bekannt — Testfall K ggf. nur per Vitest-Fixture abgedeckt (162-02), nicht live. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read-only Datenbestands-Inventar aktualisieren</name>
  <files><!-- bewusst leer: read-only DB-Inventar / Human-Verify-Checkpoint, keine Datei wird verändert --></files>
  <read_first>
    162-RESEARCH.md (Abschnitt "Read-only Datenbestands-Inventar", als Baseline/Vorlage für die vier Queries)
  </read_first>
  <action>
    Ausschließlich `SELECT`-Statements gegen die laufende Live-Datenbank ausführen, KEINE
    INSERT/UPDATE/DELETE-Anweisung unter irgendeinem Vorwand (verbindlicher Rahmen des
    Auftraggebers). Alle vier Queries aus `<interfaces>` über
    `docker compose exec -T team4sv30-db psql -U team4s -d team4s_v2 -c "<query>"` ausführen. Aus den
    Ergebnissen eine aktuelle Zuordnungstabelle für die §17-Matrix erstellen: mindestens ein
    anime_id/slug pro Konstellation „1 Gruppe“, „2+ Gruppen“, „Gruppe mit Logo“, „Gruppe mit
    Geschichte“, „Gruppe ohne Geschichte“, „Coop“ sowie das Ergebnis von Query 4 für „0 Gruppen“ (falls
    vorhanden, sonst explizit als „nicht live vorhanden, nur automatisiert über Vitest-Fixture
    abgedeckt“ vermerken). Prüfen, ob inzwischen eine Gruppe OHNE Logo existiert (Testfall K) — falls
    ja, deren anime_id/group_slug notieren; falls nein, ebenfalls explizit als offenen Punkt vermerken
    (kein Blocker, siehe RESEARCH.md).
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-db psql -U team4s -d team4s_v2 -c "SELECT 1;"</automated>
  </verify>
  <acceptance_criteria>
    - Alle vier ausgeführten Statements beginnen mit `SELECT` (kein `INSERT`/`UPDATE`/`DELETE`/`TRUNCATE`)
    - Die resultierende Zuordnungstabelle enthält mindestens eine konkrete anime_id für jede der Konstellationen „1 Gruppe“, „2+ Gruppen“, „Logo vorhanden“, „Geschichte vorhanden“, „Geschichte fehlt“
    - Für „0 Gruppen“ und „Gruppe ohne Logo“ liegt entweder eine konkrete ID oder ein expliziter „nicht live vorhanden“-Vermerk vor
  </acceptance_criteria>
  <done>Aktuelle, read-only ermittelte Zuordnungstabelle liegt vor und wird an Task 2/3 übergeben; nachweislich keine Schreiboperation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Desktop-Verifikation §16/§17</name>
  <files><!-- bewusst leer: read-only DB-Inventar / Human-Verify-Checkpoint, keine Datei wird verändert --></files>
  <read_first>
    Task 1 dieses Plans (aktualisierte Datenbestands-Zuordnungstabelle mit konkreten anime_id-Werten)
    162-USER-REQUEST.md (§16 Testfälle A-L, §17 Browser-Matrix)
  </read_first>
  <action>Task 1's aktuelle Zuordnungstabelle heranziehen, dann jeden Schritt unten über den SSH-Tunnel gegen die dort ermittelten konkreten Anime-IDs durchgehen und auf explizites Sign-off warten, bevor die Phase als abgeschlossen gilt.</action>
  <what-built>
    Filter-Chip-Zeile („Fansub-Gruppe“-Label + Chips, role=group) + gruppenspezifischer Bereich (Name,
    3-Zeilen-Story, „Mehr lesen →“, „Zur Fansub-Gruppe“, „Zum Projekt“) auf `/anime/[id]`, URL-Zustand
    über `?fansub=<slug>`, Browser Zurück/Vor, vollständige Entfernung der alten Gruppen-Chips/CTA.
  </what-built>
  <how-to-verify>
    Über den SSH-Tunnel `http://127.0.0.1:3300/anime/<id>` mit den in Task 1 ermittelten konkreten IDs
    aufrufen (Desktop-Breite, kein Mobile-Emulator):
    1. Anime mit 0 Gruppen (falls in Task 1 gefunden, sonst überspringen und vermerken): kein
       Gruppenbereich, kein Platzhalter sichtbar (Testfall A).
    2. Anime mit genau 1 Gruppe: kein „Alle“-Chip, Gruppe automatisch aktiv, Story-Vorschau (falls
       vorhanden) mit „Mehr lesen →“, beide Navigationsziele funktionieren; falls die Gruppe keine
       Story hat, kompakter Block ohne Platzhaltertext (Testfall B/C).
    3. Anime mit 2+ Gruppen: „Alle“ standardmäßig aktiv, kein gruppenspezifischer Bereich sichtbar
       (Testfall D); Klick auf eine Gruppe zeigt deren Bereich, Wechsel zur nächsten Gruppe aktualisiert
       Name/Story/Links korrekt (Testfall E/F); URL zeigt `?fansub=<slug>`, Browser-Zurück stellt „Alle“
       (oder die vorherige Gruppe) wieder her, Browser-Vor stellt die Auswahl wieder her (Testfall G);
       manuelles Ändern der URL auf einen ungültigen Slug fällt sauber auf „Alle“ zurück (Testfall H).
    4. Coop-Anime (falls in Task 1/RESEARCH.md verfügbar, z. B. Naruto/release_version_id=60): beide
       beteiligten Gruppen einzeln auswählbar, kein künstlicher dritter „Coop“-Chip (Testfall I).
    5. Gruppe mit Logo: 20px-Logo sichtbar im Chip (Testfall J); Gruppe ohne Logo, falls in Task 1
       gefunden: sauberer Text-Chip ohne Dummy-Icon (Testfall K, sonst als „nur automatisiert geprüft“
       vermerken).
    6. Tastaturnavigation: Tab durch Chips → aktiver Chip optisch klar erkennbar (Hintergrund + Rahmen +
       Fettung, nicht nur Farbe) → sichtbarer Fokusring bei jedem Tab-Stopp → Tab-Reihenfolge im
       Gruppenbereich: „Mehr lesen →“ (falls vorhanden) → „Zur Fansub-Gruppe“ → „Zum Projekt“.
  </how-to-verify>
  <resume-signal>Antworte mit "approved" oder beschreibe konkrete Abweichungen je Testfall-Buchstabe.</resume-signal>
  <acceptance_criteria>
    - Jeder der 6 Schritte wurde gegen mindestens eine konkrete anime_id aus Task 1 durchgeführt oder explizit als „live nicht verfügbar“ vermerkt
    - Antwort enthält entweder "approved" oder eine nach Testfall-Buchstabe (A-L) sortierte Abweichungsliste
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Mobile-Verifikation §16/§17 (Testfall L)</name>
  <files><!-- bewusst leer: read-only DB-Inventar / Human-Verify-Checkpoint, keine Datei wird verändert --></files>
  <read_first>
    Task 1 dieses Plans (aktualisierte Datenbestands-Zuordnungstabelle)
    Task 2 dieses Plans (Desktop-Ergebnis als Vergleichsbasis)
    162-UI-SPEC.md (Abschnitt "Zusätzliche Interaktions-/A11y-Vorgaben", Wrap-/Touch-Ziel-Vorgaben)
  </read_first>
  <action>Dieselben Anime-IDs aus Task 1/2 in der DevTools-Gerätemulation durchgehen und auf explizites Sign-off warten, bevor die Phase als abgeschlossen gilt.</action>
  <what-built>
    Dieselbe Struktur wie Task 2, responsive Wrap-Verhalten der Chip-Zeile und einspaltiger
    gruppenspezifischer Bereich.
  </what-built>
  <how-to-verify>
    Über Chrome DevTools-Gerätemulation (Breiten 320px, 375px, 414px) gegen
    `http://127.0.0.1:3300/anime/<id>` mit denselben IDs aus Task 1/2:
    1. Anime mit 2+ Gruppen und mehreren/langen Gruppennamen: Chips brechen sauber um (`flex-wrap`),
       KEINE horizontale Scrollleiste erscheint, kein Chip sprengt die Bildschirmbreite (Testfall L).
    2. Gruppenspezifischer Bereich bleibt einspaltig, volle Breite, kein internes horizontales
       Scrollen, kein Layout-Sprung der Seite beim Gruppenwechsel.
    3. Logo-Chip und Text-only-Chip (falls verfügbar) bleiben bei 320px lesbar, kein Überlappen von
       Logo und Text.
    4. Alle Navigationsziele weiterhin per Touch erreichbar und mit ausreichendem Abstand (mind.
       `--control-height-sm` = 36px Zielgröße).
  </how-to-verify>
  <resume-signal>Antworte mit "approved" oder beschreibe konkrete Abweichungen je Breite/Testfall.</resume-signal>
  <acceptance_criteria>
    - Alle 4 Schritte wurden bei mindestens den Breiten 320px und 375px durchgeführt
    - Antwort enthält entweder "approved" oder eine nach Breite/Testfall sortierte Abweichungsliste
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Agent → Live-Datenbank | Ausschließlich Lesezugriff für die Vorplanung der Verifikationsmatrix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-07 | Tampering | Agenten-Zugriff auf `team4sv30-db` in Task 1 | mitigate | Ausschließlich `SELECT`-Statements, kein Schreibzugriff unter irgendeinem Vorwand — verbindlicher Rahmen des Auftraggebers; keine neuen/geänderten Zeilen als Ergebnis dieser Phase |
</threat_model>

<verification>
- Task 1: alle vier Queries laufen erfolgreich und liefern eine dokumentierte Zuordnungstabelle
- Task 2/3: Auftraggeber bestätigt "approved" oder liefert konkrete, nach Testfall-Buchstabe (A-L) sortierte Befunde
</verification>

<success_criteria>
- Jede in §16 (Testfälle A-L) und §17 (Browser-Matrix) geforderte Konstellation ist entweder live
  desktop+mobil verifiziert oder explizit als „nur automatisiert abgedeckt, keine Live-Entsprechung“
  dokumentiert — kein stillschweigendes Auslassen
- Kein Schreibzugriff auf die Datenbank während der gesamten Verifikation
</success_criteria>

<output>
Create `.planning/phases/162-oeffentliche-anime-seite-fansub-gruppenauswahl-kurzgeschichte-navigation/162-05-SUMMARY.md` when done
</output>
