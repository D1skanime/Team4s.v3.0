---
phase: 162
plan: 04
type: execute
wave: 4
depends_on: ["162-03"]
files_modified:
  - frontend/src/app/anime/[id]/page.tsx
  - frontend/src/app/anime/[id]/page.module.css
  - frontend/src/app/anime/[id]/page.test.tsx
autonomous: true
requirements: [REQ-162-13, REQ-162-15, REQ-162-19]

must_haves:
  truths:
    - "Der graue Chip-Link-Block (fansubRow) existiert nicht mehr auf der Anime-Seite"
    - "Die Initialauswahl der Fansub-Gruppe ist beim ersten Server-Render bereits korrekt (searchParams.fansub wird gelesen und weitergereicht, kein Hydration-Flackern)"
    - "Andere Query-Parameter (from, grid_query) bleiben von der fansub-Erweiterung unberührt"
  artifacts:
    - path: "frontend/src/app/anime/[id]/page.tsx"
      provides: "SSR-Lesen von searchParams.fansub, Weitergabe als initialActiveSlug-Prop, fansubRow entfernt"
      contains: "initialActiveSlug"
    - path: "frontend/src/app/anime/[id]/page.module.css"
      provides: "bereinigtes CSS ohne fansubRow/fansubChip"
  key_links:
    - from: "frontend/src/app/anime/[id]/page.tsx"
      to: "frontend/src/components/fansubs/FansubVersionBrowser.tsx"
      via: "initialActiveSlug-Prop"
      pattern: "initialActiveSlug="
---

<objective>
`page.tsx` an den neuen URL-Mechanismus anschließen (D-04): `searchParams.fansub` serverseitig lesen
und als `initialActiveSlug` an `FansubVersionBrowser` durchreichen, sowie den letzten redundanten
Alt-UI-Rest (`fansubRow`-Chip-Block, D-13 Punkt 1) entfernen.

Purpose: Schließt die SSR-Determinismus-Lücke (D-04) — ohne diesen Plan bliebe die Erstauswahl beim
ersten Render immer „Alle“, unabhängig vom URL-Parameter, was zu sichtbarem Hydration-Flackern
führen würde.
Output: Bereinigtes `page.tsx`/`page.module.css`, erweiterte `page.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-oeffentliche-anime-seite-fansub-gruppenauswahl-kurzgeschichte-navigation/162-CONTEXT.md
@.planning/phases/162-oeffentliche-anime-seite-fansub-gruppenauswahl-kurzgeschichte-navigation/162-03-SUMMARY.md

<interfaces>
<!-- Aktuelles searchParams-Lesemuster (frontend/src/app/anime/[id]/page.tsx:40-43, 72-83),
     als Vorbild für die additive fansub-Property: -->
```tsx
interface AnimeDetailPageProps {
  params: Promise<{ id: string }>
  searchParams?: Promise<{ from?: string | string[]; grid_query?: string | string[] }>
}
...
const resolvedSearchParams = ((await searchParams) ?? {}) as {
  from?: string | string[]
  grid_query?: string | string[]
}
const rawGridQuery = typeof resolvedSearchParams.grid_query === 'string' ? resolvedSearchParams.grid_query : ''
const gridQuery = normalizeGridQuery(rawGridQuery)
```

<!-- Zu entfernender Block (D-13 Punkt 1), frontend/src/app/anime/[id]/page.tsx:253-268: -->
```tsx
{animeFansubsResponse && animeFansubsResponse.data.length > 0 && (
  <div className={styles.fansubRow}>
    {animeFansubsResponse.data.map((relation) =>
      relation.fansub_group ? (
        <Link key={relation.fansub_group.id} href={`/fansubs/${relation.fansub_group.slug}`}
              prefetch={false} className={styles.fansubChip}>
          {relation.fansub_group.name}
        </Link>
      ) : null,
    )}
  </div>
)}
```

<!-- Bestehende Props-Übergabe an FansubVersionBrowser (page.tsx:269-278), um initialActiveSlug erweitert: -->
```tsx
<FansubVersionBrowser
  key={anime.id}
  animeID={anime.id}
  animeSlug={anime.slug}
  fansubs={animeFansubsResponse?.data ?? []}
  storyGroups={fansubStoryGroups}
  episodes={groupedEpisodesResponse.data.episodes}
  pagination={groupedEpisodesResponse.data.pagination}
/>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: page.tsx — searchParams.fansub lesen, fansubRow entfernen</name>
  <files>frontend/src/app/anime/[id]/page.tsx</files>
  <read_first>
    frontend/src/app/anime/[id]/page.tsx (vollständig, aktueller Stand)
    frontend/src/components/fansubs/FansubVersionBrowser.tsx (Endstand aus Plan 162-03, insbesondere `initialActiveSlug?: string | null`-Prop)
  </read_first>
  <action>
    `AnimeDetailPageProps['searchParams']` um `fansub?: string | string[]` erweitern (gleiche Stelle wie
    `from`/`grid_query`). In `AnimeDetailContent` den `resolvedSearchParams`-Cast-Typ ebenfalls um
    `fansub?: string | string[]` erweitern. Neue Konstante `const rawFansubParam = typeof
    resolvedSearchParams.fansub === 'string' ? resolvedSearchParams.fansub : undefined` nach dem
    bestehenden `rawGridQuery`-Muster (Zeilen 81-82) ergänzen — KEINE serverseitige Validierung gegen
    `animeFansubsResponse` hier durchführen (das übernimmt `FansubVersionBrowser` bereits client-seitig
    laut D-02/162-03).

    Den kompletten `{animeFansubsResponse && animeFansubsResponse.data.length > 0 && (<div
    className={styles.fansubRow}>...)}`-Block (aktuelle Zeilen 253-268) vollständig entfernen.

    An der bestehenden `<FansubVersionBrowser .../>`-Stelle die neue Prop `initialActiveSlug={rawFansubParam}`
    zusätzlich zu den bestehenden Props (`animeID`, `animeSlug`, `fansubs`, `storyGroups`, `episodes`,
    `pagination`) ergänzen.
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fansubRow" frontend/src/app/anime/\[id\]/page.tsx` == 0
    - `grep -c "initialActiveSlug" frontend/src/app/anime/\[id\]/page.tsx` >= 1
    - `grep -c "rawFansubParam" frontend/src/app/anime/\[id\]/page.tsx` >= 1
    - `npm run typecheck` liefert 0 Fehler
  </acceptance_criteria>
  <done>page.tsx liest searchParams.fansub serverseitig, reicht ihn als initialActiveSlug durch und rendert den alten grauen Chip-Block nicht mehr.</done>
</task>

<task type="auto">
  <name>Task 2: page.module.css — fansubRow/fansubChip entfernen</name>
  <files>frontend/src/app/anime/[id]/page.module.css</files>
  <read_first>
    frontend/src/app/anime/[id]/page.module.css (Zeilen rund um `.fansubRow`/`.fansubChip`/`.fansubChip:hover`, unmittelbar vor `.emptyEpisodes`)
  </read_first>
  <action>
    Die drei Regelblöcke `.fansubRow`, `.fansubChip`, `.fansubChip:hover` vollständig entfernen.
    `.episodesSection h2` (davor) und `.emptyEpisodes` (danach) unverändert lassen.
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend npx vitest run src/app/anime</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fansubRow\|fansubChip" frontend/src/app/anime/\[id\]/page.module.css` == 0
  </acceptance_criteria>
  <done>Totes CSS für den entfernten Chip-Block ist beseitigt.</done>
</task>

<task type="auto">
  <name>Task 3: page.test.tsx — initialActiveSlug-Weitergabe und CSS-Bereinigung beweisen</name>
  <files>frontend/src/app/anime/[id]/page.test.tsx</files>
  <read_first>
    frontend/src/app/anime/[id]/page.test.tsx (vollständig, aktueller Stand — insbesondere Zeilen 129-135, das bestehende Muster zur Prüfung durchgereichter Props auf dem `FansubVersionBrowser`-Element, und Zeilen 163-168, das etablierte Muster für CSS-Content-Assertionen per `readFileSync` in genau dieser Datei)
  </read_first>
  <action>
    In der bestehenden `describe('anime detail integration without invented data', ...)` zwei neue `it`-
    Blöcke ergänzen: (1) `AnimeDetailPage({ ...paramsFor('22'), searchParams: Promise.resolve({ fansub:
    'bloody-shadow' }) })` laden (analog zum bestehenden `generateMetadata`-Aufrufmuster mit
    `searchParams` in Zeile 46-48) und assertieren, dass das gefundene `FansubVersionBrowser`-Element
    `props.initialActiveSlug === 'bloody-shadow'` hat; (2) ohne `fansub`-Parameter laden und assertieren,
    dass `props.initialActiveSlug === undefined` ist. Einen dritten `it`-Block nach dem bestehenden
    `heroBanner`/`heroContainer`-CSS-Test (Zeilen 163-168 als Vorlage) ergänzen, der per `readFileSync`
    auf `page.module.css` beweist, dass weder `.fansubRow` noch `.fansubChip` als Selektor vorkommen
    (`expect(css).not.toMatch(/\.fansubRow|\.fansubChip/)`).
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend npx vitest run src/app/anime/\[id\]/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - vitest run: alle bestehenden UND die 3 neuen Tests grün, 0 Fehler
    - neuer Test beweist `initialActiveSlug` wird 1:1 aus `searchParams.fansub` übernommen (kein Trimmen/Transformieren nötig laut D-02, Validierung passiert client-seitig)
  </acceptance_criteria>
  <done>page.test.tsx beweist die D-04-SSR-Prop-Weitergabe und die vollständige CSS-Bereinigung.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| URL-Query (`searchParams.fansub`) → Server Component Props | Rohwert wird serverseitig gelesen, aber unvalidiert nur als Hinweis-Prop weitergereicht |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-06 | Tampering | `page.tsx` `rawFansubParam` | accept | Der Wert wird serverseitig NICHT in eine Query, einen Redirect oder HTML-Attribut-Kontext eingebettet — er ist ausschließlich eine an `FansubVersionBrowser` weitergereichte Initial-Hinweis-Prop, die dort gegen die geladene `fansubOptions`-Allowlist geprüft wird (162-03, T-162-05); serverseitige Doppelprüfung wäre redundant, kein zusätzliches Risiko durch den Verzicht darauf |
</threat_model>

<verification>
- `docker compose exec -T team4sv30-frontend npx vitest run src/app/anime` — vollständige Anime-Seiten-Suite grün
- `docker compose exec -T team4sv30-frontend npm run typecheck` — keine Typfehler
- `docker compose exec -T team4sv30-frontend npx vitest run` — volle Frontend-Suite grün vor der Browser-Verifikation
</verification>

<success_criteria>
- Kein `fansubRow`/`fansubChip`-Rest in Code oder CSS
- `initialActiveSlug` wird nachweislich aus `searchParams.fansub` an `FansubVersionBrowser` durchgereicht
- Alle bestehenden page.test.tsx-Fälle (Tags/Genres aus Phase 160, Emby, Pagination) bleiben unverändert grün
</success_criteria>

<output>
Create `.planning/phases/162-oeffentliche-anime-seite-fansub-gruppenauswahl-kurzgeschichte-navigation/162-04-SUMMARY.md` when done
</output>
