---
phase: 162
plan: 02
type: execute
wave: 2
depends_on: ["162-01"]
files_modified:
  - frontend/src/components/fansubs/FansubGroupPicker.tsx
  - frontend/src/components/fansubs/FansubGroupPicker.module.css
  - frontend/src/components/fansubs/FansubGroupPicker.test.tsx
  - frontend/src/components/fansubs/FansubGroupContext.tsx
  - frontend/src/components/fansubs/FansubGroupContext.module.css
  - frontend/src/components/fansubs/FansubGroupContext.test.tsx
  - frontend/src/components/fansubs/ActiveFansubStory.tsx
  - frontend/src/components/fansubs/ActiveFansubStory.module.css
  - frontend/src/components/fansubs/__tests__/ActiveFansubStory.test.tsx
autonomous: true
requirements: [REQ-162-04, REQ-162-05, REQ-162-06, REQ-162-07, REQ-162-09, REQ-162-10, REQ-162-11, REQ-162-12, REQ-162-13, REQ-162-18, REQ-162-19]

must_haves:
  truths:
    - "Eine Gruppe mit Logo zeigt ein 20px-Logo im Chip; eine Gruppe ohne Logo zeigt nur den Gruppennamen ohne Dummy-Icon"
    - "Der aktive Chip unterscheidet sich vom inaktiven durch mindestens drei gleichzeitige, nicht-farbliche Signale (Hintergrund, Rahmenbreite, Textgewicht) zusätzlich zur Akzentfarbe"
    - "Ein Chip navigiert niemals (kein href), er wählt nur den Kontext (onSelect-Callback)"
    - "Eine Gruppe ohne Geschichte zeigt weder Platzhaltertext noch „Mehr lesen“ — nur Name + zwei Navigationsziele"
    - "„Zum Projekt“ wird nicht gerendert, wenn kein kanonischer Pretty-Pfad ermittelbar ist (fehlender Anime- oder Gruppen-Slug) — kein Fallback auf die technische Route"
    - "Der Gruppenname im gruppenspezifischen Bereich ist eine Überschrift ohne Link"
  artifacts:
    - path: "frontend/src/components/fansubs/FansubGroupPicker.tsx"
      provides: "Filter-Chip-Zeile (role=group, Button-Primitive, aria-pressed)"
      min_lines: 30
    - path: "frontend/src/components/fansubs/FansubGroupContext.tsx"
      provides: "Gruppenspezifischer Bereich (Name, Story-Vorschau, zwei Navigationsziele), ersetzt ActiveFansubStory.tsx"
      min_lines: 25
  key_links:
    - from: "frontend/src/components/fansubs/FansubGroupContext.tsx"
      to: "frontend/src/lib/fansubProjectRoutes.ts (buildPublicFansubProjectPath)"
      via: "Button href="
      pattern: "buildPublicFansubProjectPath"
    - from: "frontend/src/components/fansubs/FansubGroupPicker.tsx"
      to: "@/components/ui (Button)"
      via: "aria-pressed Toggle"
      pattern: "aria-pressed"
---

<objective>
Zwei neue, eigenständig testbare Präsentationskomponenten bauen (Interface-First für Plan 162-03,
das sie wiring wird): `FansubGroupPicker.tsx` (Filter-Chip-Zeile) und `FansubGroupContext.tsx`
(gruppenspezifischer Bereich, ersetzt `ActiveFansubStory.tsx`). Beide sind reine Props-gesteuerte
Client-Komponenten ohne eigene URL-/Storage-Logik — der Zustand kommt vom Elternteil (Plan 162-03).

Purpose: Modularität (≤450 Zeilen je Datei), Interface-First-Ordering — Plan 162-03 kann gegen
diese fertigen Verträge wiren, statt Chip- und Bereichslogik inline in `FansubVersionBrowser.tsx`
zu verschachteln.
Output: `FansubGroupPicker.tsx`/`.module.css`/`.test.tsx`, `FansubGroupContext.tsx`/`.module.css`/
`.test.tsx`; `ActiveFansubStory.tsx`/`.module.css`/Test entfernt (D-13, keine alte und neue Lösung
parallel).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-oeffentliche-anime-seite-fansub-gruppenauswahl-kurzgeschichte-navigation/162-CONTEXT.md
@.planning/phases/162-oeffentliche-anime-seite-fansub-gruppenauswahl-kurzgeschichte-navigation/162-UI-SPEC.md

<interfaces>
<!-- Kontrakt für FansubGroupPicker.tsx (neu, von Plan 162-03 konsumiert): -->
```ts
export interface FansubGroupPickerOption {
  id: number
  slug: string
  name: string
  logo_url?: string | null
}
export interface FansubGroupPickerProps {
  options: FansubGroupPickerOption[]
  activeGroupId: number | null   // null = "Alle" ausgewählt
  showAllChip: boolean            // nur true, wenn options.length >= 2
  onSelect: (groupId: number | null) => void
}
```

<!-- Kontrakt für FansubGroupContext.tsx (neu, ersetzt ActiveFansubStory.tsx): -->
```ts
export interface FansubGroupContextActiveGroup {
  id: number
  slug: string
  name: string
  story_preview?: string | null
}
export interface FansubGroupContextProps {
  activeGroup: FansubGroupContextActiveGroup | null
  animeSlug?: string
}
```

<!-- Closest Analog für den Chip-Toggle (frontend/src/app/admin/fansubs/[id]/edit/AnimeReleasesFilterBar.tsx, vollständig): -->
```tsx
'use client'
import { Button } from '@/components/ui'
import styles from './FansubEdit.module.css'
export type CockpitFilter = 'all' | 'no-contributions' | 'no-note'
export function AnimeReleasesFilterBar({ activeFilter, onFilterChange }: { activeFilter: CockpitFilter; onFilterChange: (f: CockpitFilter) => void }) {
  return (
    <div className={styles.chipRow}>
      <Button variant="ghost" size="sm" aria-pressed={activeFilter === 'all'} onClick={() => onFilterChange('all')}
        className={activeFilter === 'all' ? styles.filterChipActive : undefined}>
        Alle
      </Button>
      {/* weitere Chips analog */}
    </div>
  )
}
```

<!-- Bestehende Komponente, die ersetzt wird (frontend/src/components/fansubs/ActiveFansubStory.tsx, vollständig): -->
```tsx
'use client'
import Link from 'next/link'
import { FansubGroupSummary } from '@/types/fansub'
import { buildFansubStoryPreview } from '@/lib/fansub-summary'
import styles from './ActiveFansubStory.module.css'
export function ActiveFansubStory({ activeFansubGroupID, groups }: { activeFansubGroupID: number | null; groups: FansubGroupSummary[] }) {
  const activeGroup = groups.find((group) => group.id === activeFansubGroupID)
  if (!activeGroup) return null
  const preview = buildFansubStoryPreview(activeGroup)
  return (
    <article className={styles.card}>
      <h3 className={styles.title}><Link href={`/fansubs/${activeGroup.slug}`} prefetch={false}>{activeGroup.name}</Link></h3>
      <p className={styles.text}>{preview}</p>
    </article>
  )
}
```

<!-- fansubProjectRoutes.ts (frontend/src/lib/fansubProjectRoutes.ts:1-5), unverändert zu konsumieren: -->
```ts
export function buildPublicFansubProjectPath(fansubSlug: string, animeSlug: string): string {
  return `/fansubs/${encodeURIComponent(fansubSlug.trim())}/fansubprojekt/${encodeURIComponent(animeSlug.trim())}`
}
```

<!-- Button-Primitive-Kontrakt (frontend/src/components/ui/Button.tsx:8-34): rendert bei gesetztem
     `href` ein echtes `<a>`, sonst ein `<button type="button">`. Varianten: primary/secondary/ghost/
     subtle/danger/success/text; Größen: sm/md/lg. -->

<!-- Design-Tokens (frontend/src/styles/globals.css): --surface-card: #ffffff; --surface-sunken: #f0ece5;
     --border-subtle: rgba(45,41,38,.12); --text-primary/--text-muted; --accent-primary; --focus-ring;
     --space-1:4px .. --space-5:24px; --control-height-sm: 36px. Bestehendes line-clamp:3-Muster
     (z. B. frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.module.css:259-264):
     `display:-webkit-box; -webkit-box-orient:vertical; -webkit-line-clamp:3; overflow:hidden;` -->

<!-- Anker-Ziel für "Mehr lesen →" (frontend/src/components/fansubs/FansubStorySection.tsx:100), unverändert: -->
```tsx
<section id="geschichte">
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FansubGroupPicker.tsx — Filter-Chip-Zeile</name>
  <files>frontend/src/components/fansubs/FansubGroupPicker.tsx, frontend/src/components/fansubs/FansubGroupPicker.module.css, frontend/src/components/fansubs/FansubGroupPicker.test.tsx</files>
  <read_first>
    frontend/src/components/fansubs/FansubVersionBrowser.tsx (Zeilen 55-61 `resolveLogoUrl`, Zeilen 243-261 alte `filterRow`-Chip-Markup, die hier als Muster für Logo/Name-Rendering dient, aber NICHT hier gelöscht wird — das ist Plan 162-03)
    frontend/src/app/admin/fansubs/[id]/edit/AnimeReleasesFilterBar.tsx (vollständig, closest analog laut UI-SPEC Punkt 1)
    frontend/src/components/ui/Button.tsx (ButtonProps-Kontrakt, Zeilen 8-34)
    162-UI-SPEC.md (Abschnitte "Eigenständig getroffene Design-Entscheidungen" Punkte 1-10, "Spacing Scale", "Typography", "Color")
  </read_first>
  <behavior>
    - `options=[]` → Komponente rendert `null` (kein `role="group"`-Element im DOM)
    - `options` mit 1 Eintrag, `showAllChip=false` → kein Chip mit dem Text „Alle“; genau ein Chip mit `aria-pressed="true"`, wenn `activeGroupId` dessen `id` ist
    - `options` mit 2 Einträgen, `showAllChip=true` → „Alle“-Chip erscheint als erstes Element vor den Gruppen-Chips; bei `activeGroupId=null` trägt der „Alle“-Chip `aria-pressed="true"`
    - Option mit gesetztem `logo_url` → ein `role="img"`-Element mit `alt=""` und 20×20px wird vor dem Namen gerendert
    - Option ohne `logo_url` → kein Bild-Element für diesen Chip, nur der Name
    - Klick auf „Alle“ ruft `onSelect(null)` auf; Klick auf einen Gruppen-Chip ruft `onSelect(option.id)` auf
    - Kein natives `<button>`/`<select>`/`<input>` im gerenderten Output — ausschließlich das `Button`-Primitive
  </behavior>
  <action>
    Neue Datei FansubGroupPicker.tsx (`'use client'`) mit den in `<interfaces>` definierten Typen
    `FansubGroupPickerOption`/`FansubGroupPickerProps`. Bei `options.length === 0` `return null`. Sonst:
    ein `<span className={styles.eyebrowLabel}>Fansub-Gruppe</span>` (12px/600, `var(--text-muted)`)
    gefolgt von `<div role="group" aria-label="Fansub-Gruppe" className={styles.chipRow}>`. Innerhalb:
    wenn `showAllChip`, zuerst `<Button variant="ghost" size="sm" aria-pressed={activeGroupId === null}
    onClick={() => onSelect(null)} className={classNames(styles.chip, activeGroupId === null &&
    styles.chipActive)}>Alle</Button>` (D-11: „Alle“ steht vorn); danach pro `options`-Eintrag ein Chip in
    der gegebenen Reihenfolge (keine Neusortierung, D-11) mit demselben `Button`-Muster, `aria-pressed=
    {activeGroupId === option.id}`. Logo: lokale `resolveLogoUrl`-Funktion 1:1 aus
    `FansubVersionBrowser.tsx` Zeilen 55-61 übernehmen (Präfix `/covers/` für relative Werte,
    `http(s)://`/`/`-Werte unverändert); wenn URL vorhanden `<Image src={logoURL} alt="" width={20}
    height={20} unoptimized className={styles.logo} />` vor dem Namen rendern, sonst kein Bild-Element.
    CSS (FansubGroupPicker.module.css): `.chipRow{display:flex;flex-wrap:wrap;gap:var(--space-2);
    align-items:center}`; `.eyebrowLabel{font-size:12px;font-weight:600;line-height:1.2;
    color:var(--text-muted)}`; `.chip{border-radius:999px;min-height:var(--control-height-sm);
    background:var(--surface-card);border:1px solid var(--border-subtle);color:var(--text-primary);
    font-size:14px;font-weight:400;max-width:min(100%,260px);overflow:hidden;text-overflow:ellipsis;
    white-space:nowrap;display:inline-flex;align-items:center;gap:var(--space-1)}`;
    `.chip[aria-pressed="true"]{background:var(--surface-sunken);border:2px solid var(--accent-primary);
    font-weight:600}`; `.logo{width:20px;height:20px;border-radius:50%;object-fit:cover}`. KEIN
    `variant="primary"` (Auftrag §2: kein übertriebener Primary-CTA-Stil), NUR `variant="ghost"`.

    FansubGroupPicker.test.tsx (RTL/Vitest, `@vitest-environment jsdom`) mit den sechs Fällen aus
    `<behavior>` als eigenständige `it`-Blöcke.
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend npx vitest run src/components/fansubs/FansubGroupPicker.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<button" frontend/src/components/fansubs/FansubGroupPicker.tsx` == 0 (nur `Button`-Primitive)
    - `grep -c 'role="group"' frontend/src/components/fansubs/FansubGroupPicker.tsx` >= 1
    - `grep -c "aria-label=\"Fansub-Gruppe\"" frontend/src/components/fansubs/FansubGroupPicker.tsx` >= 1
    - vitest run: alle 6 Fälle aus `<behavior>` grün, 0 Fehler
  </acceptance_criteria>
  <done>FansubGroupPicker.tsx existiert, deckt 0/1/2+-Gruppen-Logik, Logo/Text-Chips und aria-pressed-Toggling ab; Test grün.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FansubGroupContext.tsx — gruppenspezifischer Bereich (ersetzt ActiveFansubStory.tsx)</name>
  <files>frontend/src/components/fansubs/FansubGroupContext.tsx, frontend/src/components/fansubs/FansubGroupContext.module.css, frontend/src/components/fansubs/FansubGroupContext.test.tsx, frontend/src/components/fansubs/ActiveFansubStory.tsx, frontend/src/components/fansubs/ActiveFansubStory.module.css, frontend/src/components/fansubs/__tests__/ActiveFansubStory.test.tsx</files>
  <read_first>
    frontend/src/components/fansubs/ActiveFansubStory.tsx (vollständig, wird ersetzt)
    frontend/src/components/fansubs/ActiveFansubStory.module.css (vollständig, wird ersetzt)
    frontend/src/components/fansubs/__tests__/ActiveFansubStory.test.tsx (vollständig, wird ersetzt)
    frontend/src/lib/fansubProjectRoutes.ts (Zeilen 1-5, `buildPublicFansubProjectPath`, kein eigener Leerstring-Fallback — Aufrufer prüft vorher)
    frontend/src/types/fansub.ts (FansubGroupSummary nach 162-01, inkl. `story_preview?: string | null`)
    frontend/src/components/fansubs/FansubStorySection.tsx (Zeile 100, `id="geschichte"`-Anker, Zielort für „Mehr lesen →“)
  </read_first>
  <behavior>
    - `activeGroup=null` → Komponente rendert `null` (kein DOM-Output)
    - `activeGroup` mit nicht-leerem `story_preview` → Überschrift zeigt den Gruppennamen als reinen Text, NICHT innerhalb eines `<a>`/Link-Elements; Story-Absatz mit dem `story_preview`-Text; ein „Mehr lesen →“-Link mit `href="/fansubs/<slug>#geschichte"`
    - `activeGroup` mit `story_preview` `undefined`, `null` oder leer/nur-Whitespace → kein Story-Absatz, kein „Mehr lesen“-Link — nur Überschrift + Navigationsblock (Testfall C, §7)
    - „Zur Fansub-Gruppe“-Link ist immer vorhanden mit `href="/fansubs/<slug>"`
    - „Zum Projekt“-Link fehlt vollständig (kein Element mit diesem Namen im DOM), wenn `animeSlug` fehlt/leer ist — kein Fallback auf `/anime/<id>/group/<id>` (D-12)
    - „Zum Projekt“-Link ist vorhanden mit `href=buildPublicFansubProjectPath(activeGroup.slug, animeSlug)`, wenn beide Slugs gesetzt sind
  </behavior>
  <action>
    Neue Datei FansubGroupContext.tsx (`'use client'`) mit den in `<interfaces>` definierten Typen
    `FansubGroupContextActiveGroup`/`FansubGroupContextProps`. `if (!activeGroup) return null`. Rendern:
    `<article className={styles.card}><h3 className={styles.name}>{activeGroup.name}</h3>` OHNE `<Link>`
    (D-13 Punkt 4 — Überschrift ohne Link). Story-Block: `const trimmedPreview =
    activeGroup.story_preview?.trim()`; wenn `trimmedPreview` truthy, `<p
    className={styles.storyPreview}>{trimmedPreview}</p>` gefolgt von `<Link
    href={\`/fansubs/${activeGroup.slug}#geschichte\`} prefetch={false} className={styles.moreLink}>Mehr
    lesen →</Link>`; sonst beide Elemente komplett weglassen (kein Platzhaltertext, §7). Navigationsblock
    immer: `<div className={styles.navRow}><Button href={\`/fansubs/${activeGroup.slug}\`}
    variant="secondary" size="sm">Zur Fansub-Gruppe</Button>` gefolgt von, NUR wenn
    `animeSlug?.trim() && activeGroup.slug?.trim()`, `<Button
    href={buildPublicFansubProjectPath(activeGroup.slug, animeSlug)} variant="secondary" size="sm">Zum
    Projekt</Button></div>` — sonst endet der `navRow`-Block nach dem ersten Button, kein
    Fallback-`href` auf `/anime/<id>/group/<id>` (D-12 verbietet diesen Fallback für „Zum Projekt“
    explizit).

    CSS (FansubGroupContext.module.css, Startpunkt aus ActiveFansubStory.module.css, aber auf die in
    UI-SPEC festgelegten Tokens umgestellt): `.card{border:1px solid var(--border-subtle);
    border-radius:var(--radius-md);background:var(--surface-card);padding:var(--space-5);
    margin-bottom:var(--space-4)}`; `.name{margin:0 0 var(--space-2);font-size:18px;font-weight:600;
    color:var(--text-primary)}`; `.storyPreview{margin:0 0 var(--space-2);color:var(--text-primary);
    font-size:14px;line-height:1.5;display:-webkit-box;-webkit-box-orient:vertical;
    -webkit-line-clamp:3;overflow:hidden}`; `.moreLink{font-size:14px;font-weight:600;
    color:var(--accent-primary);text-decoration:none}`; `.navRow{display:flex;gap:var(--space-2);
    margin-top:var(--space-2)}`.

    Alte Dateien LÖSCHEN: ActiveFansubStory.tsx, ActiveFansubStory.module.css,
    __tests__/ActiveFansubStory.test.tsx (leeres __tests__-Verzeichnis darf zurückbleiben oder ebenfalls
    entfernt werden). `FansubGroupContext.test.tsx` neu anlegen (RTL/Vitest) mit den sechs Fällen aus
    `<behavior>` als eigenständige `it`-Blöcke, inklusive Prüfung, dass die Überschrift KEIN `<a>`-Vorfahre
    hat (`within(...).queryByRole('link', { name: activeGroup.name })` ist `null`).
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend npx vitest run src/components/fansubs/FansubGroupContext.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f frontend/src/components/fansubs/ActiveFansubStory.tsx` schlägt fehl (Datei existiert nicht mehr)
    - `grep -c "Keine Historie hinterlegt" frontend/src/components/fansubs/FansubGroupContext.tsx` == 0 (kein Platzhaltertext übernommen)
    - `grep -c "buildFansubStoryPreview\|buildFansubFactSummary" frontend/src/components/fansubs/FansubGroupContext.tsx` == 0 (Faktenzeile entfällt laut D-07, story_preview kommt direkt aus dem Backend-Feld)
    - vitest run: alle 6 Fälle aus `<behavior>` grün, 0 Fehler
  </acceptance_criteria>
  <done>FansubGroupContext.tsx ersetzt ActiveFansubStory.tsx vollständig (D-05/D-07/D-08/D-12/D-13-konform); alte Datei/CSS/Test entfernt; neuer Test grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Server-geladene Slugs → Link-`href` | `activeGroup.slug`/`animeSlug` stammen aus der bereits serverseitig geladenen `fansubOptions`-Liste, nicht aus rohen URL-Query-Werten |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-03 | Tampering (Open Redirect) | `FansubGroupContext.tsx` Navigationsziele | mitigate | Beide `href`-Werte werden ausschließlich aus serverseitig geladenen, bereits vertrauenswürdigen Slugs über `buildPublicFansubProjectPath`/Template-Literal gebaut — niemals aus einem rohen Query-Parameter direkt als Redirect-Ziel übernommen |
| T-162-04 | Information Disclosure (reflektierter XSS) | `FansubGroupContext.tsx` Story-Text-Rendering | mitigate | Nur `{trimmedPreview}` als React-Text-Kind gerendert (automatisches Escaping), kein `dangerouslySetInnerHTML`, kein `RichTextRenderer` (D-05 verbietet HTML-Rendering explizit) |
</threat_model>

<verification>
- `docker compose exec -T team4sv30-frontend npx vitest run src/components/fansubs` — beide neuen Testdateien grün, keine verwaiste Referenz auf `ActiveFansubStory`
- `docker compose exec -T team4sv30-frontend npm run typecheck` — keine Typfehler durch die neuen Interfaces
</verification>

<success_criteria>
- FansubGroupPicker.tsx und FansubGroupContext.tsx existieren, sind eigenständig testbar und folgen exakt den UI-SPEC-Tokens (Farbe/Typografie/Spacing)
- ActiveFansubStory.tsx/.module.css/Test vollständig entfernt, keine parallele Alt-Lösung
- Beide Komponenten nutzen ausschließlich `@/components/ui`-Primitives, kein natives `<button>`
</success_criteria>

<output>
Create `.planning/phases/162-oeffentliche-anime-seite-fansub-gruppenauswahl-kurzgeschichte-navigation/162-02-SUMMARY.md` when done
</output>
