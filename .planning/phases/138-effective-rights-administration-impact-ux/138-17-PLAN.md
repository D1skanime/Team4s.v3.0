---
phase: 138-effective-rights-administration-impact-ux
plan: 17
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/admin/users/AdminUsersClient.tsx
  - frontend/src/app/admin/users/AdminUsersClient.test.tsx
  - frontend/src/app/admin/users/tabs/GuidedRevokeFlow.test.tsx
autonomous: true
gap_closure: true
requirements: [CAP-08]
must_haves:
  truths:
    - "Benutzerliste zeigt für einen last_activity_at-Zeitstempel gleich jetzt oder in der Zukunft 'Heute', niemals 'vor -N Tagen' in Tagen-, Monats- oder Jahres-Zweig."
    - "Ein automatisierter Regressionstest belegt, dass die Kombination non_deniable=true UND user_deny=true in GuidedRevokeFlow den Bestätigungsschritt erreicht statt in der 'nicht persönlich entzogen werden'-Erklärung sackzugassen."
    - "Der bereits vorhandene Gegentest (non_deniable=true OHNE user_deny) zeigt weiterhin nur die Erklärung und bietet keinen Entzugs-Button."
  artifacts:
    - path: frontend/src/app/admin/users/AdminUsersClient.tsx
      provides: "exportierte, gegen negative Werte geclampte formatRelativeDate"
    - path: frontend/src/app/admin/users/AdminUsersClient.test.tsx
      provides: "Unit-Tests für Zeitstempel jetzt/in der Zukunft"
    - path: frontend/src/app/admin/users/tabs/GuidedRevokeFlow.test.tsx
      provides: "WR-01 non_deniable+user_deny Regressionstest (7. Test in der Datei)"
  key_links:
    - from: frontend/src/app/admin/users/AdminUsersClient.test.tsx
      to: frontend/src/app/admin/users/AdminUsersClient.tsx
      via: "direkter Import von formatRelativeDate"
      pattern: "import \\{[^}]*formatRelativeDate"
    - from: frontend/src/app/admin/users/tabs/GuidedRevokeFlow.test.tsx
      to: frontend/src/app/admin/users/tabs/GuidedRevokeFlow.tsx
      via: "state={non_deniable:true, user_deny:true} rendert GuidedRevokeFlow"
      pattern: "non_deniable: true,\\s*user_deny: true"
---

<objective>
Close GAP-01 (negative relative-time display in the admin user list) and GAP-03 (untested
WR-01 non-deniable + dormant personal deny branch order in `GuidedRevokeFlow`), both narrow,
already-root-caused defects from `138-HUMAN-UAT.md`.

Purpose: GAP-01 fixes a live-visible cosmetic bug ("vor -1 Tagen") admins see on the primary user
worklist. GAP-03 closes the exact automated-test gap that keeps `138-VERIFICATION.md` on
`status: human_needed` — the WR-01 branch-order fix already exists in code
(`isNonDeniable && !isRemoveMode` in `GuidedRevokeFlow.tsx`) but has no regression test pinning it.

Requirements note: `requirements: [CAP-08]` in this plan's frontmatter is satisfied entirely by
Task 2 (GAP-03), which regression-tests `GuidedRevokeFlow.tsx` — the CAP-08 guided-revoke-flow
requirement. Task 1 (GAP-01) is a D-04 user-worklist display fix with no roadmap requirement ID of
its own; it is bundled into this plan only because it is small and file-disjoint from GAP-03, not
because it relates to CAP-08.

Output: A clamp fix + exported, directly-unit-testable `formatRelativeDate` in
`AdminUsersClient.tsx`; a new WR-01 regression test (plus confirmation the existing counter-test
still passes) in `GuidedRevokeFlow.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-HUMAN-UAT.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-VERIFICATION.md

Both gaps are independent (disjoint files, disjoint domains — user list vs. guided revoke flow).
No new interfaces are created; both tasks are localized fixes/tests against existing, shipped
components from Plans 138-01 and 138-08.
</context>

<interfaces>
<!-- Existing formatRelativeDate signature (private, becomes exported — no signature change) -->
From frontend/src/app/admin/users/AdminUsersClient.tsx (current, lines 29-37):
```typescript
function formatRelativeDate(isoDate: string | null): string {
  if (!isoDate) return '—'
  const diff = Date.now() - new Date(isoDate).getTime()
  const days = Math.floor(diff / (1000 * 60 * 60 * 24))
  if (days === 0) return 'Heute'
  if (days === 1) return 'Gestern'
  if (days < 30) return `vor ${days} Tagen`
  if (days < 365) return `vor ${Math.floor(days / 30)} Monat(en)`
  return `vor ${Math.floor(days / 365)} Jahr(en)`
}
```

<!-- GuidedRevokeFlow branch under test (unchanged unless a new test proves it wrong) -->
From frontend/src/app/admin/users/tabs/GuidedRevokeFlow.tsx (lines 85-88, 157-169):
```typescript
const isRemoveMode = state.user_deny === true
const isNonDeniable = state.non_deniable === true
// ...
if (isNonDeniable && !isRemoveMode) {
  return (<Modal ...><p>Dieses Recht kann für {appUserDisplayName} nicht persönlich entzogen werden. ...</p></Modal>)
}
```

<!-- Existing test fixtures to reuse verbatim (GuidedRevokeFlow.test.tsx) -->
```typescript
function makeState(overrides: Partial<EffectiveRightState> = {}): EffectiveRightState { /* ... */ }
function makeMutationResult(overrides: Partial<CapabilityOverrideMutationResult> = {}): CapabilityOverrideMutationResult { /* ... */ }
const defaultProps = { open: true, onClose: vi.fn(), fansubGroupId: 1, fansubGroupName: 'Sakura-Fansub', appUserId: 42, appUserDisplayName: 'Maxine Muster', actionCode: 'fansub_group.members.manage', actionLabel: 'Mitglieder verwalten', onMutated: vi.fn(), matrix: null }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix GAP-01 — clamp negative relative-time output in AdminUsersClient</name>
  <files>frontend/src/app/admin/users/AdminUsersClient.tsx, frontend/src/app/admin/users/AdminUsersClient.test.tsx</files>
  <read_first>
    - frontend/src/app/admin/users/AdminUsersClient.tsx (lines 1-40: imports + formatRelativeDate)
    - frontend/src/app/admin/users/AdminUsersClient.test.tsx (existing mocks/describe blocks — new tests must not break the D-06 URL-filter round-trip tests already there)
    - frontend/src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.tsx (only line ~354 references "Tagen"; confirm during execution this remains a static "Der Link läuft in 7 Tagen ab." string, not a duplicated relative-date function — if a `formatRelativeDate`-style implementation is found there, apply the identical clamp fix and record it in the SUMMARY)
  </read_first>
  <behavior>
    - Test: `formatRelativeDate(new Date(Date.now() + 60_000).toISOString())` returns exactly `'Heute'` (future timestamp, clock-skew case from GAP-01).
    - Test: `formatRelativeDate(new Date(Date.now()).toISOString())` returns exactly `'Heute'` (zero-diff edge case).
    - Test: neither result matches `/vor -\d/` (no negative-day output in any branch).
  </behavior>
  <action>
    In `formatRelativeDate` (currently lines 29-37 of `AdminUsersClient.tsx`), clamp the millisecond
    diff to a non-negative floor before computing `days`, so any timestamp at or after `Date.now()`
    (including future timestamps caused by client/server clock skew) always falls into the
    `days === 0` → `'Heute'` branch. Do not change the `days === 1` ("Gestern"), `days < 30`,
    `days < 365`, or year branches beyond this clamp — only the input to `Math.floor` changes.
    Add the `export` keyword to `formatRelativeDate` so the new tests can import it directly instead
    of asserting through a full component render. Then add a new `describe` block to
    `AdminUsersClient.test.tsx` that imports `formatRelativeDate` from `./AdminUsersClient` and
    asserts the three behaviors above. Per GAP-01's scope note, confirm (via the `ClaimManagementPanel.tsx`
    read above) there is no duplicated relative-date formatter to fix in parallel — this file's static
    "7 Tagen ab" text is unrelated link-expiry copy, not a second implementation.
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/app/admin/users/AdminUsersClient.test.tsx --reporter=basic"</automated>
  </verify>
  <acceptance_criteria>
    - `formatRelativeDate` is exported from `AdminUsersClient.tsx` (grep: `export function formatRelativeDate`)
    - `formatRelativeDate(<ISO string ≥ Date.now()>)` returns `'Heute'` for both a +60s future timestamp and an exact-now timestamp
    - No test assertion or manual trace of the function permits a `/vor -\d+ Tagen/`, `/vor -\d+ Monat/`, or `/vor -\d+ Jahr/` output
    - Pre-existing D-06 URL-filter round-trip tests in `AdminUsersClient.test.tsx` still pass unmodified
    - `docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/app/admin/users/AdminUsersClient.test.tsx --reporter=basic"` exits 0
  </acceptance_criteria>
  <done>formatRelativeDate is exported and clamped; new unit tests prove no negative output for now/future timestamps; existing filter tests still green; ClaimManagementPanel.tsx duplicate-check documented in SUMMARY.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close GAP-03 — WR-01 regression test for non_deniable + dormant personal deny</name>
  <files>frontend/src/app/admin/users/tabs/GuidedRevokeFlow.test.tsx</files>
  <read_first>
    - frontend/src/app/admin/users/tabs/GuidedRevokeFlow.tsx (full file — especially lines 85-88 `isRemoveMode`/`isNonDeniable` derivation and lines 157-169 the `isNonDeniable && !isRemoveMode` guard)
    - frontend/src/app/admin/users/tabs/GuidedRevokeFlow.test.tsx (full file — reuse `makeState`, `makeMutationResult`, `defaultProps` verbatim; note the existing counter-test at the top of the `describe('GuidedRevokeFlow', ...)` block covering non_deniable=true WITHOUT user_deny, and the existing `user_deny`-only test near the bottom covering the "Abweichung entfernen" happy path)
    - .planning/phases/138-effective-rights-administration-impact-ux/138-VERIFICATION.md (lines documenting the WR-01 human-verification gap and exact wording of the expected behavior)
  </read_first>
  <behavior>
    - Test (new, positive case): render `GuidedRevokeFlow` with `state = makeState({ allowed: true, non_deniable: true, user_deny: true, decisive_source: 'platform_admin', granting_roles: [] })`. Assert `screen.queryByText(/nicht persönlich entzogen werden/)` is `null` (no dead-end explanation). Assert `screen.getByRole('button', { name: 'Abweichung entfernen' })` exists and is enabled. Click it; assert `mockMutateCapabilityOverride` was called with `{ group_id: 1, target_user_id: 42, action_code: 'fansub_group.members.manage', effect: null, reason: { category: 'task_delegation', text: null } }` and that `screen.findByText('Gespeichert und sofort aktiv.')` resolves.
    - Test (existing, counter-case — verify unmodified, do not duplicate): `non_deniable: true` without `user_deny` still shows only the explanation and no "Recht entziehen"/"Abweichung entfernen" button.
  </behavior>
  <action>
    Add one new test to the existing `describe('GuidedRevokeFlow', ...)` block in
    `GuidedRevokeFlow.test.tsx`, named to reference WR-01 explicitly (e.g. "WR-01: non_deniable=true
    UND user_deny=true erreicht den Bestätigungsschritt statt der Sackgasse-Erklärung"). Mock
    `mockMutateCapabilityOverride.mockResolvedValueOnce(makeMutationResult({ activation_status: 'active' }))`
    before rendering. Assert the three behaviors listed above. Do not modify
    `GuidedRevokeFlow.tsx`'s `isNonDeniable && !isRemoveMode` condition or any other branch logic —
    per GAP-03's scope, this is a tests-only task. If (and only if) the new assertions fail against
    the current code, stop, report the exact defect (which condition/line produces the wrong
    result), and apply the minimal corrective change to the branch condition — never invert or
    weaken the new test to match broken behavior. Verify the existing non_deniable-without-user_deny
    test (already present, first `it(...)` in the file) still passes unchanged — this is the required
    GAP-03 counter-test and needs no new code.
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/app/admin/users/tabs/GuidedRevokeFlow.test.tsx --reporter=basic"</automated>
  </verify>
  <acceptance_criteria>
    - `GuidedRevokeFlow.test.tsx` contains a new test combining `non_deniable: true` and `user_deny: true` that asserts the confirm/status path is reachable (no dead-end text, "Abweichung entfernen" button present and functional)
    - `mutateCapabilityOverride` is asserted called with `effect: null` (not `'deny'`) for that scenario, matching the "removal", not "new deny", semantics
    - The pre-existing counter-test (`non_deniable: true` without `user_deny`) still asserts `screen.queryByRole('button', { name: 'Recht entziehen' })` is `null` and passes unmodified
    - `GuidedRevokeFlow.tsx` is unchanged unless the new test proved the branch logic wrong (in which case the SUMMARY documents the exact defect and minimal fix)
    - `docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/app/admin/users/tabs/GuidedRevokeFlow.test.tsx --reporter=basic"` reports 7/7 tests passing
  </acceptance_criteria>
  <done>WR-01's non_deniable+user_deny combination is pinned by an automated regression test that reaches the confirm step and does not dead-end; the existing counter-test still proves the opposite (non_deniable without user_deny) is still blocked; 138-VERIFICATION.md's one open human_needed item now has automated coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin browser → AdminUsersClient (existing) | Renders already-fetched, already-authorized `last_activity_at` values; this plan touches only client-side date formatting, no new input or endpoint. |
| Admin browser → GuidedRevokeFlow (existing) | This plan adds test coverage only; no production code path changes; the mutation call (`mutateCapabilityOverride`) and its backend authorization are unchanged and out of scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|------------------|
| T-138-17-01 | Information Disclosure | `formatRelativeDate` clamp | accept | Clamping only changes a display string derived from data the admin is already authorized to see (own worklist row); no new data exposed. |
| T-138-17-02 | Tampering | GuidedRevokeFlow WR-01 test | accept | Test-only change; production authorization for `mutateCapabilityOverride` (backend-enforced, verified in Plan 138-08) is untouched by this plan. |
| T-138-17-SC | Tampering | npm installs | accept | No new packages introduced by this plan. |
</threat_model>

<verification>
1. `docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/app/admin --reporter=basic"` — confirm both touched test files pass and no other `src/app/admin` test regresses (the five pre-existing unrelated failures listed in the phase's common constraints — `FansubAppMembersSection.test.tsx`, `fansubs/[id]/edit/page.test.tsx`, `useGroupMembersTab.test.ts`, `UserContributionsTab.test.tsx`, `ResponsiveImage.config.test.ts` — are expected to remain red and are not this plan's responsibility).
2. `grep -n "export function formatRelativeDate" frontend/src/app/admin/users/AdminUsersClient.tsx` — confirms the export.
3. `grep -c "isNonDeniable && !isRemoveMode" frontend/src/app/admin/users/tabs/GuidedRevokeFlow.tsx` — confirms the WR-01 condition is unchanged (still exactly 1) unless a documented defect fix was required.
</verification>

<success_criteria>
- Admin user list never renders "vor -N Tagen" for any timestamp at or after the current time.
- `138-VERIFICATION.md`'s sole `human_needed` gap (WR-01 non_deniable+user_deny) now has an automated regression test pinning the expected non-dead-end behavior.
- No file outside `files_modified` is touched; no existing 138-01..138-16 PLAN/SUMMARY file is modified.
</success_criteria>

<output>
Create `.planning/phases/138-effective-rights-administration-impact-ux/138-17-SUMMARY.md` when done
</output>
