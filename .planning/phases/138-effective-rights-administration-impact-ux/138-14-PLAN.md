---
phase: 138-effective-rights-administration-impact-ux
plan: 14
type: execute
wave: 3
depends_on: ["138-04", "138-10"]
files_modified:
  - backend/internal/permissions/effective_rights_claim_activation_preview.go
  - backend/internal/permissions/effective_rights_claim_activation_preview_test.go
  - backend/internal/repository/member_claims_activate_repository.go
  - backend/internal/handlers/admin_claim_activation_impact_handler.go
  - backend/internal/handlers/capability_policy_contract.go
  - backend/cmd/server/admin_routes.go
  - backend/cmd/server/main.go
  - shared/contracts/admin-capabilities.yaml
  - frontend/src/types/admin-capability.ts
  - frontend/src/lib/api.ts
  - frontend/src/app/admin/claims/ClaimDecisionImpactPanel.tsx
  - frontend/src/app/admin/claims/ClaimDecisionImpactPanel.test.tsx
  - frontend/src/app/admin/claims/ClaimsClient.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Before approving a claim in a way that actually changes rights, the admin sees a real diff of which effective rights would be gained (D-24)."
    - "The impact preview is attached to the RIGHT action — ActivateClaimedMember, which really does create membership+roles, not VerifyClaim, which changes only claim_status (138-RESEARCH.md Pitfall 4/Open Question 1, resolved this session by direct repository read)."
    - "Deciding VerifyClaim/RejectClaim never fabricates a rights diff — it honestly states no effective-rights change occurs for that action (D-24, binding honesty rule)."
  artifacts:
    - path: "backend/internal/permissions/effective_rights_claim_activation_preview.go"
      provides: "Preview of the real fansub_group_members+roles ActivateClaimedMember would create"
      contains: "func (s *Service) PreviewClaimActivationImpact"
    - path: "frontend/src/app/admin/claims/ClaimDecisionImpactPanel.tsx"
      provides: "Per-decision impact panel distinguishing the two claim decision actions honestly"
      contains: "activateClaimedMember"
  key_links:
    - from: "frontend/src/app/admin/claims/ClaimDecisionImpactPanel.tsx"
      to: "frontend/src/lib/api.ts"
      via: "getClaimActivationImpactPreview then activateClaimedMember"
      pattern: "getClaimActivationImpactPreview"
---

<objective>
Close D-24 correctly, using this session's direct-read finding that resolves 138-RESEARCH.md's Open
Question 1: `VerifyClaim` (`member_claims.claim_status: pending -> verified`) creates NO membership/role
row at all; `ActivateClaimedMember` (a separate, already-live action,
`member_claims_activate_repository.go`, confirmed by direct read) is the one that actually creates a real
`fansub_group_members` row plus every non-governance role from the member's activatable historical roles
(via the already-existing `EnsureInvitationAcceptance` reuse). This plan builds a real, resolver-backed
preview for `ActivateClaimedMember` specifically, and wires the Claims workspace's decision actions (built
by Plan 138-10) so `VerifyClaim`/`RejectClaim` show the honest "no rights change" copy while
`ActivateClaimedMember` shows a real before/after diff before confirming.

Purpose: satisfies D-24's binding honesty rule — "Keine Rechtewirkung verstecken" — without ever
fabricating a diff for an action that provably has none.
Output: `Service.PreviewClaimActivationImpact` (new permissions-package method reusing the same
`loadGroupRightsSources`/`evaluateGroupRights` primitives Plan 138-04 already extracted) + a new
platform-admin-... actually group-scoped handler + `ClaimDecisionImpactPanel` wired into the Claims page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-CONTEXT.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-RESEARCH.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-UI-SPEC.md

<interfaces>
From backend/internal/repository/member_claims_activate_repository.go (`ActivateClaimedMember`, read
directly this session, full 90-line file) — the EXACT real side effects to preview, without performing
them: step 1 resolves `appUserID` via `SELECT mc.app_user_id FROM member_claims mc JOIN
hist_fansub_group_members hgm ON hgm.member_id = mc.member_id WHERE mc.member_id = $1 AND
hgm.fansub_group_id = $2 AND mc.claim_status = 'verified' AND mc.app_user_id IS NOT NULL LIMIT 1`; step 2
(`collectActivatableHistoricalRoles`, private method) resolves the non-governance role codes via `SELECT
DISTINCT r.role_code FROM hist_group_member_roles r JOIN hist_fansub_group_members hgm ON hgm.id =
r.hist_fansub_group_member_id WHERE hgm.member_id = $1 AND hgm.fansub_group_id = $2 AND r.role_code NOT IN
('fansub_lead', 'founder') ORDER BY r.role_code`; step 3 (NOT previewed — this plan never calls it) creates
the real membership row. Add a new, read-only, non-mutating method to
`backend/internal/repository/member_claims_activate_repository.go`: `func (r
*MemberClaimsRepository) PreviewActivatableRoles(ctx context.Context, memberID int64, fansubGroupID int64)
(appUserID int64, roleCodes []string, err error)` running EXACTLY the same two SELECT queries as steps 1-2
above (extract `collectActivatableHistoricalRoles`'s existing body into this new method's second half, or
call it directly since it is already a method on the same receiver) — zero writes, zero call into
`EnsureInvitationAcceptance`.

New permissions-package method (new file, reusing Plan 138-04's already-extracted
`loadGroupRightsSources`): `func (s *Service) PreviewClaimActivationImpact(ctx context.Context, appUserID
int64, fansubGroupID int64, roleCodes []string) (before *GroupRightsResolution, after
*GroupRightsResolution, err error)`. Implementation: `before, err := s.ResolveGroupRights(ctx,
Actor{AppUserID: appUserID}, fansubGroupID)` — call the REAL, unmodified `ResolveGroupRights` for the
"before" side (the target is NOT yet an active member, so this naturally and correctly resolves to the
`no_active_membership` deny-all state via the resolver's own existing logic — no special-casing needed).
For "after": call `s.loadGroupRightsSources(ctx, Actor{AppUserID: appUserID}, fansubGroupID)` (same helper,
a second real read of THEIR CURRENT sources — cheap, one extra round trip, needed since they may already
hold specialized grants that should still count), then override `sources.ActiveMembership = true` and
`sources.Roles = roleCodes` (the activatable set, replacing whatever `sources.Roles` currently loaded, since
by construction a non-active-member has no group roles yet today — document this precondition in a code
comment rather than silently assuming it), evaluate via `evaluateGroupRights(actor, fansubGroupID, sources,
allKnownActions)`.

Handler: group-scoped auth (Source A, `CanForFansubGroup(ActionFansubGroupHistoricalMembersLink,
fansubGroupID)` — the SAME action `requireFansubPermission` already uses for `VerifyClaim`/
`ActivateClaimedMember` themselves, per `member_claims_handler.go`'s `requireFansubPermission` helper read
directly this session; reuse it exactly, do not invent a different gate for the preview than for the real
action). Route: `GET /admin/fansubs/:id/historical-members/:memberId/claim-activation-impact` (mirrors
`ActivateClaimedMember`'s own path shape exactly, GET instead of POST, no body).

DTO: reuse `RoleAssignmentImpactPreview`-shaped output is NOT quite right (that DTO's `role_code`/`change`
fields don't fit "multiple roles at once, membership itself is the change") — add a new
`ClaimActivationImpactPreview struct { TargetAppUserID int64 \`json:"target_app_user_id"\`; RoleCodes
[]string \`json:"role_codes"\`; Before []EffectiveRightState \`json:"before"\`; After
[]EffectiveRightState \`json:"after"\` }` to `capability_policy_contract.go`, projected via the same
existing `effectiveRightStatesFromResolution` helper (D-20 reuse, zero new projection logic).

Frontend: `getClaimActivationImpactPreview(fansubGroupId: number, memberId: number):
Promise<ClaimActivationImpactPreview>` in api.ts (`apiClientFetch`, GET, `cache: "no-store"`).
`ClaimDecisionImpactPanel` (inside a `Modal`, per UI-SPEC Section G): for a claim row's "Verifizieren"
action (status=pending), render the UI-SPEC-locked honest copy directly ("Diese Entscheidung ändert keine
effektiven Rechte.") with a simple confirm calling the EXISTING `verifyMemberClaim(fansubId, claimId)` —
no preview call at all, since none is needed (D-24: never fabricate a diff for an action with none). For a
claim row's "Als aktives Mitglied übernehmen" action (status=verified, `activateClaimedMember` — already
exists in api.ts, confirmed this session), fetch `getClaimActivationImpactPreview`, render the same
gained/lost/unchanged summary + changed-only table pattern established by Plan 138-09's
`RoleAssignmentImpactModal` (reuse that exact rendering approach, do not invent a third table-rendering
convention for the third impact-preview surface this phase builds), confirm calls
`activateClaimedMember(fansubId, memberId)`. "Ablehnen" (`rejectMemberClaim`, already exists) needs no
preview either — the locked D-24 reject copy: "Ablehnen: Es entsteht keine neue Zuordnung. {Benutzer}
bleibt im aktuellen Zustand."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — non-mutating role preview + resolver-backed impact + handler/route</name>
  <files>backend/internal/repository/member_claims_activate_repository.go, backend/internal/permissions/effective_rights_claim_activation_preview.go, backend/internal/permissions/effective_rights_claim_activation_preview_test.go, backend/internal/handlers/admin_claim_activation_impact_handler.go, backend/internal/handlers/capability_policy_contract.go, backend/cmd/server/admin_routes.go, backend/cmd/server/main.go, backend/internal/handlers/admin_claim_activation_impact_handler_test.go</files>
  <read_first>backend/internal/repository/member_claims_activate_repository.go, backend/internal/handlers/member_claims_handler.go, backend/internal/permissions/effective_rights_role_assignment_preview.go</read_first>
  <action>
Add `PreviewActivatableRoles` to member_claims_activate_repository.go exactly as specified (zero writes,
reuses the existing two SELECT queries verbatim, calling the already-private `collectActivatableHistoricalRoles` directly since it lives on the same receiver).

Create effective_rights_claim_activation_preview.go implementing `PreviewClaimActivationImpact` exactly
per the interfaces block (real `ResolveGroupRights` for "before", `loadGroupRightsSources` +
`ActiveMembership=true` + `Roles=roleCodes` override + `evaluateGroupRights` for "after"). Write
effective_rights_claim_activation_preview_test.go (pure Go, fixture-driven) proving: a target with zero
current sources goes from "before" = mostly `no_grant`/`no_active_membership` reasons to "after" = allowed
for every action any of the `roleCodes` grants; a target who ALREADY holds a specialized grant (e.g. review
delegation) in that group keeps it reflected in "after" too (the override does not erase pre-existing
specialized-grant sources); an empty `roleCodes` slice still correctly flips `ActiveMembership` to true in
"after" (membership alone can matter for actions gated only on `!ActiveMembership`, if any exist).

Add `ClaimActivationImpactPreview` DTO to capability_policy_contract.go. Create
admin_claim_activation_impact_handler.go: group-scoped auth via `requireFansubPermission`-equivalent
(`CanForFansubGroup(ActionFansubGroupHistoricalMembersLink, fansubGroupID)`, mirroring
`member_claims_handler.go`'s existing helper exactly), parse `:id`/`:memberId`, call
`PreviewActivatableRoles` then `PreviewClaimActivationImpact`, project via
`effectiveRightStatesFromResolution`, respond `gin.H{"data": preview}`. Register `GET
/admin/fansubs/:id/historical-members/:memberId/claim-activation-impact` directly after the existing
`POST .../historical-members/:memberId/activate` route; wire construction in main.go reusing the existing
`memberClaimsRepo`/`permissionSvc`.

Write admin_claim_activation_impact_handler_test.go proving: 200 with a real before/after diff for a valid
verified claim with activatable roles; 422 (or an appropriate honest error) when
`PreviewActivatableRoles` finds zero activatable roles (mirrors `ActivateClaimedMember`'s own
`no_activatable_roles` error code — reuse the SAME `ClaimMutationError` shape for consistency); 403 for a
caller lacking `ActionFansubGroupHistoricalMembersLink`.
  </action>
  <acceptance_criteria>
    <criterion>grep -c "func (r \*MemberClaimsRepository) PreviewActivatableRoles" backend/internal/repository/member_claims_activate_repository.go equals 1</criterion>
    <criterion>grep -c "func (s \*Service) PreviewClaimActivationImpact" backend/internal/permissions/effective_rights_claim_activation_preview.go equals 1</criterion>
    <criterion>grep -c "claim-activation-impact" backend/cmd/server/admin_routes.go equals 1</criterion>
    <criterion>docker compose exec team4sv30-backend go build ./... exits 0</criterion>
    <criterion>docker compose exec team4sv30-backend go test ./internal/permissions/... ./internal/handlers/... ./internal/repository/... -run 'ClaimActivation' -count=1 -v exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-backend go test ./internal/permissions/... ./internal/handlers/... ./internal/repository/... -run 'ClaimActivation' -count=1 -v exits 0</automated>
  </verify>
  <done>A real, non-mutating, resolver-backed preview exists for the ONE claim decision action that actually changes rights, correctly excluding VerifyClaim/RejectClaim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ClaimDecisionImpactPanel + contract chain + wiring into ClaimsClient</name>
  <files>shared/contracts/admin-capabilities.yaml, frontend/src/types/admin-capability.ts, frontend/src/lib/api.ts, frontend/src/app/admin/claims/ClaimDecisionImpactPanel.tsx, frontend/src/app/admin/claims/ClaimDecisionImpactPanel.test.tsx, frontend/src/app/admin/claims/ClaimsClient.tsx</files>
  <read_first>frontend/src/app/admin/claims/ClaimsClient.tsx (Plan 138-10 output, same session), frontend/src/app/admin/users/tabs/RoleAssignmentImpactModal.tsx (Plan 138-09 output, same session — reuse its table-rendering approach), frontend/src/lib/api.ts (activateClaimedMember, verifyMemberClaim, rejectMemberClaim)</read_first>
  <behavior>
    - For a pending claim's Verifizieren action, the panel renders the exact locked 'Diese Entscheidung ändert keine effektiven Rechte.' copy with zero preview fetch, confirm calls verifyMemberClaim.
    - For a verified claim's Als-aktives-Mitglied-übernehmen action, the panel fetches getClaimActivationImpactPreview and renders a real gained-rights table before confirm is enabled.
    - A claim-activation preview compute failure blocks confirm with the same locked error copy pattern used elsewhere in this phase.
    - The Ablehnen action renders the locked 'Ablehnen: Es entsteht keine neue Zuordnung...' copy with zero preview fetch, confirm calls rejectMemberClaim.
  </behavior>
  <action>
Add the `ClaimActivationImpactPreview` schema + `GET .../claim-activation-impact` path to
shared/contracts/admin-capabilities.yaml. Add the matching TS interface to
frontend/src/types/admin-capability.ts and `getClaimActivationImpactPreview` to frontend/src/lib/api.ts
per the interfaces block.

Build `ClaimDecisionImpactPanel` as a `Modal` accepting `{open, onClose, fansubGroupId, memberId,
claimId, appUserDisplayName, decision: 'verify' | 'activate' | 'reject', onDecided: () => void}`,
branching per `decision` exactly as specified in the `<behavior>` block, reusing
`RoleAssignmentImpactModal.tsx`'s changed-actions-table rendering approach for the `'activate'` branch
(import/extract a small shared table-rendering helper if that keeps both files under the 450-line cap,
otherwise duplicate the minimal rendering JSX — prefer extraction if it does not create an awkward cross-
directory import; document the choice made).

Wire `ClaimDecisionImpactPanel` into `ClaimsClient.tsx`'s Aktion column (which Plan 138-10 intentionally
left empty) — render the correct available action(s) per row based on `claim_status`
(`pending` -> Verifizieren + Ablehnen; `verified` -> Als-aktives-Mitglied-übernehmen), opening the panel in
the matching `decision` mode, refreshing the list via the existing `loadClaims`-equivalent callback on
`onDecided`.
  </action>
  <acceptance_criteria>
    <criterion>grep -c "Diese Entscheidung ändert keine effektiven Rechte" frontend/src/app/admin/claims/ClaimDecisionImpactPanel.tsx equals 1</criterion>
    <criterion>grep -c "getClaimActivationImpactPreview" frontend/src/app/admin/claims/ClaimDecisionImpactPanel.tsx equals 1</criterion>
    <criterion>grep -c "Ablehnen: Es entsteht keine neue Zuordnung" frontend/src/app/admin/claims/ClaimDecisionImpactPanel.tsx equals 1</criterion>
    <criterion>grep -c "ClaimDecisionImpactPanel" frontend/src/app/admin/claims/ClaimsClient.tsx is greater than or equal to 1</criterion>
    <criterion>docker compose exec team4sv30-frontend npm test -- --run "ClaimDecisionImpactPanel" exits 0 with all 4 behavior cases passing</criterion>
    <criterion>docker compose exec team4sv30-frontend npx tsc --noEmit exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npm test -- --run "ClaimDecisionImpactPanel" exits 0</automated>
  </verify>
  <done>D-24 is fully and honestly satisfied: the Claims workspace previews a real rights diff only for the one action that actually creates one, and never fabricates a diff for Verifizieren/Ablehnen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin browser -> new GET .../claim-activation-impact | Group-scoped, delegable (fansub_lead-eligible) authenticated request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-138-25 | Elevation of Privilege | admin_claim_activation_impact_handler.go | mitigate | Reuses the EXACT authorization action (`ActionFansubGroupHistoricalMembersLink`) the real `ActivateClaimedMember`/`VerifyClaim` actions already require — no privilege gap between preview and the real decision |
| T-138-26 | Information Disclosure | PreviewActivatableRoles | accept | Returns only role codes/app_user_id already reachable by an authorized caller via the existing claim/historical-member surfaces; no new data class is exposed |
</threat_model>

<verification>
- `docker compose exec team4sv30-backend go build ./...` exits 0
- `docker compose exec team4sv30-backend go test ./internal/permissions/... ./internal/handlers/... ./internal/repository/... -count=1` exits 0
- `docker compose exec team4sv30-frontend npm test -- --run "ClaimDecisionImpactPanel"` exits 0
- `docker compose exec team4sv30-frontend npx tsc --noEmit` exits 0
</verification>

<success_criteria>
D-24 is satisfied honestly: only ActivateClaimedMember (the action that genuinely creates rights-relevant
state) gets a real resolver-backed preview; VerifyClaim/RejectClaim show the honest "no change" copy.
</success_criteria>

<output>
Create `.planning/phases/138-effective-rights-administration-impact-ux/138-14-SUMMARY.md` when done
</output>
