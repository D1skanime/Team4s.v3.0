---
phase: 138-effective-rights-administration-impact-ux
plan: 15
type: execute
wave: 4
depends_on: ["138-03", "138-06", "138-08", "138-09", "138-10", "138-11", "138-12"]
files_modified:
  - frontend/src/components/admin/AdminMainNav.tsx
  - frontend/src/components/admin/AdminMainNav.test.tsx
  - frontend/src/app/admin/page.tsx
  - frontend/src/app/admin/layout.tsx
  - frontend/src/app/admin/users/[id]/UserDetailPageClient.tsx
  - frontend/src/app/admin/users/[id]/UserDetailPageClient.test.tsx
  - frontend/src/app/admin/users/AdminUsersClient.tsx
  - frontend/src/app/admin/users/AdminUsersClient.test.tsx
  - frontend/src/app/admin/users/tabs/UserOverviewTab.tsx
autonomous: true
requirements: [UADM-01]

must_haves:
  truths:
    - "A persistent main navigation (Benutzer | Gruppen | Rollen | Capabilities | Claims | Änderungen) exists across the admin area, and every listed area actually resolves to a real route built by an earlier plan in this phase (D-01, D-02)."
    - "The user-detail page is a real Tabs navigation (Übersicht | Rollen & Rechte | Beiträge | Claims | Streaming | Änderungen), replacing the long Accordion page — D-03, binding."
    - "The user list shows exactly D-04's locked column set, never raw Beiträge/Release-Arbeitsflächen/Medienuploads counts as headline columns (D-04)."
    - "The user overview shows compact, per-group summaries, never large bare statistic tiles (D-05)."
  artifacts:
    - path: "frontend/src/components/admin/AdminMainNav.tsx"
      provides: "The one shared, persistent D-01 top-level navigation component"
      contains: "Änderungen"
    - path: "frontend/src/app/admin/users/[id]/UserDetailPageClient.tsx"
      provides: "Tabs-based, not Accordion-based, D-03 user-detail navigation"
      contains: "Rollen & Rechte"
  key_links:
    - from: "frontend/src/components/admin/AdminMainNav.tsx"
      to: "frontend/src/app/admin/roles/page.tsx"
      via: "nav link to /admin/roles"
      pattern: "/admin/roles"
    - from: "frontend/src/components/admin/AdminMainNav.tsx"
      to: "frontend/src/app/admin/claims/page.tsx"
      via: "nav link to /admin/claims"
      pattern: "/admin/claims"
    - from: "frontend/src/components/admin/AdminMainNav.tsx"
      to: "frontend/src/app/admin/changes/page.tsx"
      via: "nav link to /admin/changes"
      pattern: "/admin/changes"
---

<objective>
This is the final IA "glue" plan: build the D-01 persistent main navigation now that every area it links to
is a real, built route (Plan 138-01/05/10/11/12's new `/admin/roles`, `/admin/claims`, `/admin/changes`
pages, plus the existing `/admin/users`, `/admin/fansubs`, `/admin/role-capabilities`), and replace the
long Accordion-based `UserDetailPageClient.tsx` with D-03's real Tabs navigation. Also closes D-04/D-05:
the current `AdminUsersClient.tsx` table (confirmed by direct read this session) genuinely violates D-04
today, rendering Beiträge/Release-Arbeitsflächen/Medienuploads as headline columns — this plan corrects it
to the locked column set.

Purpose: this is where the whole phase's individually-built pieces become one coherent, navigable admin
module, satisfying D-01/D-02/D-03/D-04/D-05 together — no earlier plan in this phase could build this
safely since it depends on every route/tab existing first.
Output: a shared `AdminMainNav`, a corrected `AdminUsersClient` table, a corrected `UserOverviewTab`, and a
Tabs-based `UserDetailPageClient`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-CONTEXT.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-RESEARCH.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-UI-SPEC.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-PATTERNS.md

<interfaces>
D-01 (locked, exact wording): `Benutzer | Gruppen | Rollen | Capabilities | Claims | Änderungen`. Real
route targets, confirmed this session: Benutzer -> `/admin/users` (existing), Gruppen -> `/admin/fansubs`
(existing — "Gruppen" is this phase's chosen label for the existing fansub-group list, no new route),
Rollen -> `/admin/roles` (Plan 138-12), Capabilities -> `/admin/role-capabilities` (existing), Claims ->
`/admin/claims` (Plan 138-10), Änderungen -> `/admin/changes` (Plan 138-11).

Build `AdminMainNav` using `@/components/ui`'s `Tabs` primitive (or a simple `Button variant="ghost"` row
with an active-state indicator using `--accent-primary` per UI-SPEC's Color contract — planner/executor
discretion on exact primitive, `Tabs` is the closer semantic match for a persistent top nav, confirm at
execution time whether `Tabs` supports rendering as `Link`-based navigation rather than only in-page
panel-switching; if it does not, use a styled `Button variant="ghost"`/active-indicator row instead — do
not build a native `<nav>`/`<ul>` from scratch when a primitive covers most of the need). Mount it in
`frontend/src/app/admin/layout.tsx` (create this file if it does not already exist — check first) so it
renders above every `/admin/*` route consistently, OR mount it inside `admin/page.tsx` plus each new
top-level page built this phase if a shared layout file does not already exist and creating one risks
touching unrelated existing admin routes' rendering — verify the current `frontend/src/app/admin/`
directory structure at execution time and choose the approach that adds the nav without regressing any
existing admin page's current layout (grep for an existing `admin/layout.tsx` first; only create one if
none exists, and if none exists, evaluate whether adding one is safe for ALL existing admin routes before
doing so — if risky, mount `AdminMainNav` individually into `admin/page.tsx` plus every route built or
touched by this phase, which is the lower-risk fallback).

D-03 (locked, exact wording): `Übersicht | Rollen & Rechte | Beiträge | Claims | Streaming | Änderungen`.
Current `UserDetailPageClient.tsx` (read directly this session, full 178-line file) has 9 Accordion items:
`overview, roles (Globale Rollen), memberships (Gruppenmitgliedschaften), group-rights (Gruppenrechte),
claims (Member-Profil & Claims), contributions (Beiträge), media (Medien), audit (Audit), streaming
(Streaming)`. Map onto D-03's 6 tabs: `Übersicht` -> `UserOverviewTab` (unchanged component, extended per
D-05 below); `Rollen & Rechte` -> ONE tab hosting `UserGlobalRolesTab` + `UserGroupMembershipsTab`'s role
summary + the now-canonical `UserGroupRightsTab` (Plans 138-06/08/09) stacked as sub-sections (global
roles first, then per-group Rollen+Rechte — `UserGroupRightsTab` already IS per-group-structured per
D-11, so it can absorb `UserGroupMembershipsTab`'s content directly; keep `UserGlobalRolesTab` as a small
section ABOVE it, do not delete that component, just compose it); `Beiträge` -> `UserContributionsTab`
(Plan 138-03's fix) PLUS `UserMediaTab` nested as a secondary section within the same tab (media uploads
are contribution-adjacent content; this is a documented judgment call, not a silent drop of the existing
Medien surface — record this choice explicitly in the plan SUMMARY); `Claims` -> `UserClaimsTab`
(unchanged); `Streaming` -> `UserStreamingGrantsTab` (unchanged, D-30 binding: must not be functionally
damaged — verify with its existing test suite after the refactor, zero behavior change expected since this
tab is only being re-parented into a Tabs panel instead of an Accordion item); `Änderungen` ->
`UserAuditTab` (unchanged component, just renamed nav label per D-25's "Änderungen not Audit" terminology
rule — the component's OWN internal copy may still need small D-25 wording alignment, but a full rewrite of
`UserAuditTab.tsx` itself is out of THIS plan's scope, only the nav label and tab wiring change here).

Replace the `Accordion`/`openIds`/`loadedIds` state machine with `Tabs` (`@/components/ui`) — check
`Tabs`'s existing API (controlled `activeId`/`onActiveIdChange` or similar) and preserve the existing
lazy-load-on-first-open behavior (`loadedIds` set) since several of these tabs perform their own data
fetch — do not regress to "fetch everything eagerly on mount" (a real perf regression for 6 tabs' worth of
distinct API calls). Sync the active tab with a URL query param (`?tab=`) mirroring the existing `?tab=`
convention already used elsewhere in this codebase (grep the fansub group edit page's tab-URL-sync pattern
per the earlier `260820-600` quick-task entry in STATE.md and reuse the SAME convention, not a new one).

D-04 (locked column list, exact): `Name, E-Mail, Status (aktiv/deaktiviert), globale Rolle, Member-Profil
(aktiv/nicht aktiv), Anzahl Gruppenmitgliedschaften, offene Claims, letzte Aktivität, Aktionen`. Current
`AdminUsersClient.tsx` (read directly this session) renders 12 columns including the 3 explicitly forbidden
ones (`Beiträge`, `Release-Arbeitsflächen`, `Medienuploads`) plus `Leitungskontext`/`Konflikte`, which are
not in the locked list either. Reduce the `TableHead`/`AdminUserTableRow` render to EXACTLY the 9 locked
columns (add an explicit "Aktionen" column if none renders today — reuse the existing row-click navigation
as the action, or add a small `Button variant="ghost"` "Details" per row if row-click alone does not read
as an obvious "Aktionen" column). Preserve the existing "Nur mit Konflikten" filter TOGGLE (still useful
functionality, D-04 does not forbid the FILTER, only forbids a dedicated headline COLUMN) but remove the
`Konflikte` table column itself.

D-05 (locked): `UserOverviewTab.tsx` must show compact per-group summaries like the locked example
("New-Subs — Rolle: Co-Leitung / ✓ Gruppe bearbeiten ✓ Mitglieder verwalten ✕ Review freigeben / Keine
persönlichen Rechteabweichungen · Keine offenen Claims"), never large bare stat tiles ("18 effektive
Rechte", "13 Beiträge"). Read the current `UserOverviewTab.tsx` at execution time to determine whether it
already violates this (not directly confirmed this session) and correct only what actually violates it —
do not rewrite a compliant section.

D-31 (locked boundary, out of scope by design — not a gap): "Benutzer löschen" stays deaktivieren/reaktivieren only; Phase 138 builds NO new deletion architecture. This plan does not add a delete action anywhere in the user list/detail surfaces it touches — only confirm the existing deactivate/reactivate actions (if present in `AdminUsersClient.tsx`/`UserOverviewTab.tsx` today) remain functionally unchanged by this plan's column/tab refactors.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AdminMainNav (D-01/D-02) + AdminUsersClient column fix (D-04)</name>
  <files>frontend/src/components/admin/AdminMainNav.tsx, frontend/src/components/admin/AdminMainNav.test.tsx, frontend/src/app/admin/page.tsx, frontend/src/app/admin/layout.tsx, frontend/src/app/admin/users/AdminUsersClient.tsx, frontend/src/app/admin/users/AdminUsersClient.test.tsx</files>
  <read_first>frontend/src/app/admin/page.tsx, frontend/src/app/admin/users/AdminUsersClient.tsx, frontend/src/components/ui/Tabs.tsx</read_first>
  <action>
Build `AdminMainNav` per the interfaces block (6 fixed links, active-state indicator using
`--accent-primary`, D-35-compliant primitives only). Mount it per the safest available option determined
at execution time (shared layout if safe, else per-page). Update `admin/page.tsx`'s existing card links to
include the 3 newly-built areas (Rollen, Claims, Änderungen) alongside the existing ones, consistent with
`AdminMainNav`'s link set.

Reduce `AdminUsersClient.tsx`'s `TableHead`/`AdminUserTableRow` to exactly D-04's 9 locked columns, removing
Beiträge/Release-Arbeitsflächen/Medienuploads/Leitungskontext/Konflikte cells while preserving the "Nur mit
Konflikten" filter toggle unchanged. Verify `AdminUserListItem`'s existing fields cover every remaining
column (they do, per the type already read this session) — no backend change needed for this task.
  </action>
  <acceptance_criteria>
    <criterion>grep -c "Änderungen" frontend/src/components/admin/AdminMainNav.tsx is greater than or equal to 1</criterion>
    <criterion>grep -c "/admin/roles\|/admin/claims\|/admin/changes" frontend/src/components/admin/AdminMainNav.tsx equals 3</criterion>
    <criterion>grep -c "Release-Arbeitsflächen\|Medienuploads" frontend/src/app/admin/users/AdminUsersClient.tsx equals 0</criterion>
    <criterion>grep -c "Beiträge&lt;/TableHeaderCell&gt;" frontend/src/app/admin/users/AdminUsersClient.tsx equals 0</criterion>
    <criterion>docker compose exec team4sv30-frontend npm test -- --run "AdminMainNav|AdminUsersClient" exits 0</criterion>
    <criterion>docker compose exec team4sv30-frontend npx tsc --noEmit exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npm test -- --run "AdminMainNav|AdminUsersClient" exits 0</automated>
  </verify>
  <done>A real, persistent D-01 navigation exists linking to every area this phase built, and the user list shows exactly D-04's locked column set.</done>
</task>

<task type="auto">
  <name>Task 2: UserOverviewTab compactness check (D-05) + UserDetailPageClient Tabs rewrite (D-03)</name>
  <files>frontend/src/app/admin/users/tabs/UserOverviewTab.tsx, frontend/src/app/admin/users/[id]/UserDetailPageClient.tsx, frontend/src/app/admin/users/[id]/UserDetailPageClient.test.tsx</files>
  <read_first>frontend/src/app/admin/users/tabs/UserOverviewTab.tsx, frontend/src/app/admin/users/[id]/UserDetailPageClient.tsx, frontend/src/app/admin/users/tabs/UserGlobalRolesTab.tsx, frontend/src/app/admin/users/tabs/UserGroupMembershipsTab.tsx, frontend/src/app/admin/users/tabs/UserMediaTab.tsx, frontend/src/app/admin/users/tabs/UserStreamingGrantsTab.tsx, frontend/src/components/ui/Tabs.tsx</read_first>
  <action>
Read `UserOverviewTab.tsx` and correct any bare large statistic tile it renders (if none exists, verify and
leave unchanged — do not manufacture a change for its own sake). Rewrite `UserDetailPageClient.tsx`
replacing `Accordion`/`openIds`/`loadedIds` with `Tabs` per the interfaces block's exact 6-tab mapping:
Übersicht (`UserOverviewTab`), Rollen & Rechte (`UserGlobalRolesTab` stacked above the now-canonical
`UserGroupRightsTab`, with `UserGroupMembershipsTab`'s content absorbed into `UserGroupRightsTab`'s
per-group sections — remove the standalone `UserGroupMembershipsTab` mount from this page, but do not
delete the component file itself unless it has zero remaining consumers anywhere in the codebase, verified
by grep before deletion), Beiträge (`UserContributionsTab` + `UserMediaTab` nested as a secondary section),
Claims (`UserClaimsTab`), Streaming (`UserStreamingGrantsTab`, unchanged behavior), Änderungen
(`UserAuditTab`, unchanged component, renamed tab label only). Preserve the existing lazy-load-on-first-open
behavior and the `?tab=` URL sync convention (grep the codebase's existing tab-URL-sync pattern from the
fansub edit page and match it, per the interfaces block).
  </action>
  <acceptance_criteria>
    <criterion>grep -c "Rollen &amp; Rechte\|Rollen & Rechte" frontend/src/app/admin/users/[id]/UserDetailPageClient.tsx equals 1</criterion>
    <criterion>grep -c "&lt;Accordion" frontend/src/app/admin/users/[id]/UserDetailPageClient.tsx equals 0</criterion>
    <criterion>grep -c "UserStreamingGrantsTab" frontend/src/app/admin/users/[id]/UserDetailPageClient.tsx equals 1</criterion>
    <criterion>docker compose exec team4sv30-frontend npm test -- --run "UserDetailPageClient|UserStreamingGrantsTab" exits 0 with zero regression in the Streaming tab's own test suite (D-30 binding)</criterion>
    <criterion>docker compose exec team4sv30-frontend npx tsc --noEmit exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npm test -- --run "UserDetailPageClient|UserStreamingGrantsTab" exits 0</automated>
  </verify>
  <done>The user-detail page uses D-03's real 6-tab navigation instead of a 9-item Accordion, with zero functional regression to the Streaming placeholder (D-30) or any absorbed tab's data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A (pure frontend navigation/composition refactor) | No new server-side surface; every consumed endpoint is already authorized by an earlier plan in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-138-27 | Information Disclosure (regression risk) | UserDetailPageClient.tsx Tabs rewrite | mitigate | Every tab's own data-loading and permission-gating logic is UNCHANGED by this refactor (only the container/navigation shell changes) — covered by re-running each absorbed tab's own existing test suite, not just the new Tabs shell's tests |
</threat_model>

<verification>
- `docker compose exec team4sv30-frontend npm test -- --run "AdminMainNav|AdminUsersClient|UserDetailPageClient|UserStreamingGrantsTab|UserOverviewTab"` exits 0
- `docker compose exec team4sv30-frontend npx tsc --noEmit` exits 0
</verification>

<success_criteria>
D-01/D-02's persistent main navigation links to every real route this phase built; the user list matches
D-04's locked columns exactly; the user overview stays compact per D-05; the user-detail page uses D-03's
real 6-tab navigation with zero regression to any absorbed tab, including the D-30-protected Streaming
placeholder.
</success_criteria>

<output>
Create `.planning/phases/138-effective-rights-administration-impact-ux/138-15-SUMMARY.md` when done
</output>
