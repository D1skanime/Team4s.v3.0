---
phase: 138-effective-rights-administration-impact-ux
plan: 12
type: execute
wave: 2
depends_on: ["138-01"]
files_modified:
  - frontend/src/app/admin/roles/page.tsx
  - frontend/src/app/admin/roles/RolesClient.tsx
  - frontend/src/app/admin/roles/RoleHoldersTable.tsx
  - frontend/src/app/admin/roles/RoleHoldersTable.test.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "The 'Rollen' top-level nav view answers 'who holds this role' FIRST — the role's default capability set is reachable but not the first information shown (D-07)."
    - "Benutzer and Gruppe are directly clickable in the role-holders table; the 'Benutzer + Gruppe' entry leads to the canonical Benutzer-in-Gruppe-Editor (D-02, D-09)."
  artifacts:
    - path: "frontend/src/app/admin/roles/page.tsx"
      provides: "New /admin/roles top-level route"
    - path: "frontend/src/app/admin/roles/RoleHoldersTable.tsx"
      provides: "Benutzer | Gruppe | Status | Rechte-Abweichungen | letzte Aktivität table"
      contains: "listRoleHolders"
  key_links:
    - from: "frontend/src/app/admin/roles/RolesClient.tsx"
      to: "frontend/src/lib/api.ts"
      via: "listRoleHolders(roleCode)"
      pattern: "listRoleHolders"
    - from: "frontend/src/app/admin/roles/RoleHoldersTable.tsx"
      to: "frontend/src/app/admin/users/[id]"
      via: "clickable navigation to the user's canonical editor"
      pattern: "/admin/users/"
---

<objective>
Build the D-07 "Rollen" top-level nav view: a role picker (fansub-group-context roles only — global app
roles keep using the existing `/admin/users?role=` filter per Plan 138-01's scope decision) whose default
answer is "wer besitzt diese Rolle?" (Benutzer | Gruppe | Status | Rechte-Abweichungen | letzte Aktivität),
consuming Plan 138-01's new role-holder endpoint. The role's default capability set (today's
`/admin/role-capabilities` split-view) remains reachable but is explicitly NOT the first thing this new
view shows, per D-07's binding ordering.

Purpose: closes D-07 and gives D-02's "Rolle → Benutzer → Gruppe" bidirectional navigation path a real
target that did not exist before this phase.
Output: `/admin/roles` route with a role picker + `RoleHoldersTable`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-CONTEXT.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-RESEARCH.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-UI-SPEC.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-PATTERNS.md

<interfaces>
From Plan 138-01 (already merged by this wave): `listRoleHolders(roleCode: string): Promise<RoleHolderEntry[]>` already exists in `frontend/src/lib/api.ts`; `RoleHolderEntry` type
(`app_user_id, display_name, email, fansub_group_id, fansub_group_name, membership_status,
has_overrides`) already exists in `frontend/src/types/admin-capability.ts`.

Role picker data source: reuse the ALREADY-FETCHED `listRoleCapabilities()` matrix (same function
`RoleCapabilityClient.tsx` already calls), filtering to `role.role_kind !== 'global_app_role' &&
role.contexts?.includes('fansub_group')` — this is the exact "real fansub-group-context roles" set Plan
138-01's endpoint accepts; do not build a second, separately-fetched role list.

`RoleHoldersTable` analog (`frontend/src/app/admin/users/tabs/UserAuditTab.tsx`'s `AuditTable` function,
read directly this session, lines 70-110) — the exact pure `Table`/`TableHead`/`TableBody`/`TableRow`
shape with an `EmptyState` early-return to mirror. Columns: Benutzer (`Button variant="ghost"` navigating
to `/admin/users/${app_user_id}`), Gruppe (`Button variant="ghost"` navigating to
`/admin/fansubs/${fansub_group_id}/edit`), Status (`Badge`, `membership_status` mapped via the SAME
`statusVariant`/`statusLabel` helpers already used elsewhere in this codebase — grep
`UserDetailPageClient.tsx`'s `statusVariant`/`statusLabel` and reuse the exact mapping, do not invent a
second one), Rechte-Abweichungen (`Badge variant={has_overrides ? 'info' : 'muted'}`, text "Ja"/"Nein" —
D-07 explicitly forbids a raw count here, a simple presence indicator is correct), letzte Aktivität
(`RoleHolderEntry` does not carry this field today — 138-RESEARCH.md's D-07 column list includes it but
Plan 138-01's endpoint does not return it; render "–" for this column in this plan and note in the
SUMMARY that "letzte Aktivität" is not currently backed by data — do not silently invent a fake
timestamp, and do not block this plan on adding it since it is not one of the four hard-gate roadmap
criteria; a follow-up can extend Plan 138-01's query later if this gap needs closing).

"Der Eintrag `Benutzer + Gruppe` führt in den kanonischen Benutzer-in-Gruppe-Editor" (D-07/D-09): the
Benutzer cell's link target is `/admin/users/${app_user_id}` (the canonical editor lives in the user-detail
"Rollen & Rechte" area built by Plans 138-06/08/09 — there is no separate per-group deep-link parameter
needed for THIS plan since the user-detail tab already lists ALL of that user's groups; clicking through is
sufficient to satisfy D-07/D-09's "same canonical editor" requirement without inventing a second, parallel
one-group-only view).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /admin/roles route + role picker</name>
  <files>frontend/src/app/admin/roles/page.tsx, frontend/src/app/admin/roles/RolesClient.tsx</files>
  <read_first>frontend/src/app/admin/role-capabilities/RoleCapabilityClient.tsx, frontend/src/app/admin/role-capabilities/RoleMasterList.tsx</read_first>
  <action>
Create `page.tsx` (PlatformAdminGate wrapper, mirrors the existing `admin/role-capabilities/page.tsx`
pattern). Build `RolesClient.tsx`: fetch `listRoleCapabilities()` once, filter to fansub-group-context
roles per the interfaces block, render a compact role picker (reuse `RoleMasterList.tsx`'s Card-row shape
if it fits, or a simpler `Select`/list — planner's discretion per CONTEXT.md §6's open visual-density
question, keep it simple: a clickable list of role Cards, selecting one loads `RoleHoldersTable` below via
`listRoleHolders(roleCode)`). Include a `Button variant="ghost"` link per selected role to
`/admin/role-capabilities?role=${roleCode}` labeled "Standard-Capabilities dieser Rolle ansehen" — this is
the D-07-mandated "reachable but not first" link to the existing Capabilities split-view (which already
supports the `?role=` preselection per 138-PATTERNS.md).
  </action>
  <acceptance_criteria>
    <criterion>grep -c "PlatformAdminGate" frontend/src/app/admin/roles/page.tsx equals 1</criterion>
    <criterion>grep -c "Standard-Capabilities dieser Rolle ansehen" frontend/src/app/admin/roles/RolesClient.tsx equals 1</criterion>
    <criterion>grep -c "role_kind !== 'global_app_role'" frontend/src/app/admin/roles/RolesClient.tsx is greater than or equal to 1</criterion>
    <criterion>docker compose exec team4sv30-frontend npx tsc --noEmit exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npx tsc --noEmit exits 0</automated>
  </verify>
  <done>A role picker exists for fansub-group-context roles, with a clearly secondary (not first) link to the existing Capabilities split-view.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RoleHoldersTable</name>
  <files>frontend/src/app/admin/roles/RoleHoldersTable.tsx, frontend/src/app/admin/roles/RoleHoldersTable.test.tsx, frontend/src/app/admin/roles/RolesClient.tsx</files>
  <read_first>frontend/src/app/admin/users/tabs/UserAuditTab.tsx, frontend/src/app/admin/users/[id]/UserDetailPageClient.tsx (statusVariant/statusLabel)</read_first>
  <behavior>
    - Given zero holders for a selected role, renders an EmptyState, not a broken empty table.
    - Given holders exist, every Benutzer cell is a real clickable Button link to /admin/users/:id.
    - Given holders exist, every Gruppe cell is a real clickable Button link to /admin/fansubs/:id/edit.
    - A holder with has_overrides=true renders the 'Ja' Rechte-Abweichungen badge; false renders 'Nein'.
  </behavior>
  <action>
Build `RoleHoldersTable` exactly per the interfaces block (columns, badge mapping, navigation targets,
EmptyState fallback). Wire it into `RolesClient.tsx` below the role picker, loading on role selection via
`listRoleHolders(roleCode)` with its own `LoadingState`/`ErrorState` triad independent of the picker's own
loading state. D-32 (responsive, binding): below 760px, `RoleHoldersTable` collapses each holder into a compact `Card`-based row instead of a raw wide `Table` (reuse the same existing narrow-viewport card-row pattern identified for Plan 138-10, do not invent a second one).
  </action>
  <acceptance_criteria>
    <criterion>grep -c "listRoleHolders" frontend/src/app/admin/roles/RolesClient.tsx equals 1</criterion>
    <criterion>docker compose exec team4sv30-frontend npm test -- --run "RoleHoldersTable" exits 0 with all 4 behavior cases passing</criterion>
    <criterion>grep -c "759px\|760px" frontend/src/app/admin/roles/RoleHoldersTable.tsx is greater than or equal to 1 (D-32: narrow-viewport card-row collapse)</criterion>
    <criterion>docker compose exec team4sv30-frontend npx tsc --noEmit exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npm test -- --run "RoleHoldersTable" exits 0</automated>
  </verify>
  <done>D-07's "who holds this role" table is live and fully bidirectionally navigable, wired into the new /admin/roles route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A (frontend-only) | Consumes an already-authorized (platform-admin-gated) backend endpoint from Plan 138-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-138-22 | Information Disclosure | RolesClient.tsx | accept | Page is wrapped in `PlatformAdminGate`; the underlying endpoint is independently platform-admin-gated server-side |
</threat_model>

<verification>
- `docker compose exec team4sv30-frontend npm test -- --run "RoleHoldersTable"` exits 0
- `docker compose exec team4sv30-frontend npx tsc --noEmit` exits 0
</verification>

<success_criteria>
/admin/roles answers "who holds this role" first, with real bidirectional navigation to the canonical
user-in-group editor and a clearly secondary link to the existing Capabilities split-view.
</success_criteria>

<output>
Create `.planning/phases/138-effective-rights-administration-impact-ux/138-12-SUMMARY.md` when done
</output>
