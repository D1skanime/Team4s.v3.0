---
phase: 138-effective-rights-administration-impact-ux
plan: 16
type: execute
wave: 4
depends_on: ["138-05", "138-10", "138-11", "138-12"]
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/fansubEditTypes.ts
  - frontend/src/app/admin/fansubs/[id]/edit/mainTabRouting.ts
  - frontend/src/app/admin/fansubs/[id]/edit/fansubEditAccess.ts
  - frontend/src/app/admin/fansubs/[id]/edit/GroupRolesTab.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/GroupRolesTab.test.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/GroupChangesTab.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/GroupChangesTab.test.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/GroupMembersTable.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "The group view has real Rollen and Änderungen perspectives sourced from this phase's real endpoints, scoped to this one group (D-06)."
    - "The existing member list gains visible role-abweichung status and direct navigation into the canonical user-in-group editor, satisfying D-06's per-member column requirements without creating a second, competing editor (D-06, D-09, D-34)."
    - "The existing invitation-management 'claims' tab (Phase 135, HistoricalMemberCard claim invites) is not conflated with D-06's member-claims decision perspective — the group view links OUT to the existing central /admin/claims workspace, filtered to this group, rather than rebuilding claim-decision UI a third time (D-09/D-34)."
  artifacts:
    - path: "frontend/src/app/admin/fansubs/[id]/edit/GroupRolesTab.tsx"
      provides: "This group's role-holder perspective, reusing Plan 138-01's role-holder query"
      contains: "listRoleHolders"
    - path: "frontend/src/app/admin/fansubs/[id]/edit/GroupChangesTab.tsx"
      provides: "This group's Änderungen history, reusing Plan 138-05's filtered audit endpoint"
      contains: "listChanges"
  key_links:
    - from: "frontend/src/app/admin/fansubs/[id]/edit/GroupChangesTab.tsx"
      to: "frontend/src/lib/api.ts"
      via: "listChanges({fansub_group_id})"
      pattern: "listChanges"
    - from: "frontend/src/app/admin/fansubs/[id]/edit/GroupRolesTab.tsx"
      to: "frontend/src/app/admin/users/[id]"
      via: "clickable navigation to the canonical user-in-group editor"
      pattern: "/admin/users/"
---

<objective>
Close D-06 (Gruppenansicht) using the already-built group-scoped tools from this phase: a new "Rollen" tab
(reusing Plan 138-01's role-holder query, scoped to roles this group actually has holders for) and a new
"Änderungen" tab (reusing Plan 138-05's filtered `listChanges`, scoped via `fansub_group_id`). The existing
member list (`collaboration` tab) is extended with a Rechteabweichungen indicator and direct navigation to
the canonical user-in-group editor rather than being rebuilt, satisfying D-06's "Benutzer" column
requirements. The existing `claims` tab (Phase 135 claim-INVITATION management — a different concept from
member-claims decisions, confirmed by direct read of `fansubEditAccess.ts` this session, which gates it on
`can_view_invitations`/`can_create_invitation`/`can_cancel_invitation`) is left untouched; a link to the
central `/admin/claims?fansub_group_id={id}` workspace (Plan 138-10, already supports this filter) is added
instead of building a second, conflicting claims surface inside this page (D-09/D-34, binding).

Purpose: closes D-06 without inventing a competing editor and without conflating two genuinely different
"claims" concepts this codebase already has.
Output: two new tabs (`GroupRolesTab`, `GroupChangesTab`) added to `FansubEditClient`'s existing tab
system, plus a small extension to the existing member table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-CONTEXT.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-RESEARCH.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-UI-SPEC.md

<interfaces>
From `frontend/src/app/admin/fansubs/[id]/edit/fansubEditAccess.ts` (read directly this session, full
file) — the existing `MAIN_TABS`/`MainTab`/`canUseMainTab`/`visibleMainTabs` tab-registration system to
extend, NOT replace: add two new `MainTab` values (`"roles"` and `"changes"`, exact string keys — verify
against `fansubEditTypes.ts`'s real `MainTab` union at execution time and match its existing naming
convention, e.g. lowercase-no-separator like the existing `vorschlaege`/`pruefungen` keys) to
`mainTabRouting.ts`'s `MAIN_TABS` array, and add corresponding `canUseMainTab` cases: `"roles"` visible
whenever `"collaboration"` is (same gate: `can_view_members || can_manage_members || ...`, since role
visibility is a subset of member visibility), `"changes"` visible under the same gate as `"basic"`
(`can_edit_group || ...`, an editing-adjacent capability, matching this codebase's existing pattern of
gating "who can see history/audit" behind edit-adjacent capabilities — verify against how `notes`/
`readiness` are gated for the closest existing analog to a history-like tab).

`GroupRolesTab` (new): reuses `listRoleHolders(roleCode)` from Plan 138-01 per-role, but this is a
GROUP-scoped view (not a global role picker like Plan 138-12's `/admin/roles`) — the simplest, honest
approach without a new backend endpoint: fetch this group's own member-role list via the ALREADY-EXISTING
`listFansubAppMembers(fansubId)` (grep `frontend/src/lib/api.ts` for its real name/shape, used elsewhere in
this same `FansubEditClient` tree already) which already returns each member's `roles: string[]`, then
group client-side by role code into a "who holds this role IN THIS GROUP" table — this avoids calling
Plan 138-01's cross-group endpoint N times (once per role) for a view that only ever needs ONE group's
data, which the existing app-members endpoint already has in one call. Do not add a new backend endpoint
for this tab; it is a pure client-side regrouping of already-fetched, already-authorized group-member data.
Columns: Rolle | Benutzer (clickable) | Status | Rechte-Abweichungen (reuse whatever override-presence
signal is already available on the app-member row; if none exists, render "–" rather than fabricating a
value — do not add a new backend join just for this secondary indicator in this group-scoped view, since
Plan 138-01's cross-group `/admin/roles` view already provides the authoritative override-aware version).

`GroupChangesTab` (new): calls `listChanges({fansub_group_id: fansubId, limit: 25, offset: 0})` (Plan
138-05's function, already supports this filter) and renders using the SAME `translateChangeEntry`
function Plan 138-11 already built (import it, do not duplicate the translation logic — D-26/central
consistency) inside a compact `Table`/`Card` list, with a "Alle Änderungen ansehen" link to
`/admin/changes?gruppe={fansubId}` for the full central view (D-27: context-scoped history stays additional
to, never a replacement for, the central Änderungen area).

Existing member table extension (`GroupMembersTable.tsx` or whatever the real component name is inside the
`collaboration` tab — verify the exact file at execution time, the interfaces block's filename above is a
best-effort guess from this session's directory listing and MUST be confirmed, not assumed, before editing):
add a "Rechteabweichungen" column/badge per member row (reuse the same override-presence signal decision as
`GroupRolesTab` above) and make the member's name a clickable `Button variant="ghost"` link to
`/admin/users/${app_user_id}` if it is not already (grep first — this table may already be clickable for
another purpose; do not double-wrap an existing link).

Link-out for Claims (not a new tab): add a `Button variant="secondary"` "Claims dieser Gruppe ansehen" (or
similar, styled as a clear cross-navigation per UI-SPEC's accent-link convention) near the existing `claims`
(invitations) tab's content, navigating to `/admin/claims?fansub_group_id={fansubId}` — this satisfies
D-06's Claims-tab requirement via the central workspace built in Plan 138-10, per D-09/D-34's "no redundant
editor" rule, while leaving the existing invitation-management tab's own content and label completely
unchanged (it serves a genuinely different, still-valid purpose).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GroupRolesTab + GroupChangesTab (new tabs)</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/fansubEditTypes.ts, frontend/src/app/admin/fansubs/[id]/edit/mainTabRouting.ts, frontend/src/app/admin/fansubs/[id]/edit/fansubEditAccess.ts, frontend/src/app/admin/fansubs/[id]/edit/GroupRolesTab.tsx, frontend/src/app/admin/fansubs/[id]/edit/GroupRolesTab.test.tsx, frontend/src/app/admin/fansubs/[id]/edit/GroupChangesTab.tsx, frontend/src/app/admin/fansubs/[id]/edit/GroupChangesTab.test.tsx</files>
  <read_first>frontend/src/app/admin/fansubs/[id]/edit/fansubEditAccess.ts, frontend/src/app/admin/fansubs/[id]/edit/fansubEditTypes.ts, frontend/src/app/admin/fansubs/[id]/edit/mainTabRouting.ts, frontend/src/app/admin/fansubs/[id]/edit/FansubEditClient.tsx, frontend/src/app/admin/changes/ChangeEntryTranslator.ts (Plan 138-11 output, same session)</read_first>
  <behavior>
    - GroupRolesTab groups the group's own already-fetched member-role data by role code and renders each role's holders with clickable user navigation.
    - GroupRolesTab with zero members renders an EmptyState, not a broken grouped table.
    - GroupChangesTab renders translateChangeEntry's sentence per entry, scoped only to this group's changes (asserted against a fixture list.listChanges mock called with fansub_group_id matching this page's id).
    - GroupChangesTab renders a working link to /admin/changes?gruppe={fansubId} for the full central view.
  </behavior>
  <action>
Add `"roles"` and `"changes"` to `MainTab`/`MAIN_TABS`/`canUseMainTab`/`visibleMainTabs` exactly per the
interfaces block (verify exact existing key-naming convention first, match it). Build `GroupRolesTab`
consuming the group's already-available app-member list (reuse whichever existing hook/fetch
`FansubEditClient`'s `collaboration` tab already uses for this data — do not add a second fetch of the
same data), grouping by role client-side, columns Rolle | Benutzer (clickable) | Status |
Rechte-Abweichungen (or "–" if no signal is available, per the interfaces block's honest-fallback
decision). Build `GroupChangesTab` consuming `listChanges({fansub_group_id: fansubId, ...})` and
`translateChangeEntry` (imported from Plan 138-11's file, not duplicated), plus the "Alle Änderungen
ansehen" link.
  </action>
  <acceptance_criteria>
    <criterion>grep -c "\"roles\"\|'roles'" frontend/src/app/admin/fansubs/[id]/edit/mainTabRouting.ts is greater than or equal to 1</criterion>
    <criterion>grep -c "listChanges" frontend/src/app/admin/fansubs/[id]/edit/GroupChangesTab.tsx equals 1</criterion>
    <criterion>grep -c "translateChangeEntry" frontend/src/app/admin/fansubs/[id]/edit/GroupChangesTab.tsx equals 1</criterion>
    <criterion>docker compose exec team4sv30-frontend npm test -- --run "GroupRolesTab|GroupChangesTab" exits 0 with all 4 behavior cases passing</criterion>
    <criterion>docker compose exec team4sv30-frontend npx tsc --noEmit exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npm test -- --run "GroupRolesTab|GroupChangesTab" exits 0</automated>
  </verify>
  <done>The group view has real Rollen and Änderungen tabs, both scoped correctly to this one group and reusing this phase's already-built endpoints/translation logic with zero duplication.</done>
</task>

<task type="auto">
  <name>Task 2: Member table Rechteabweichungen + navigation, Claims link-out</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/GroupMembersTable.tsx, frontend/src/app/admin/fansubs/[id]/edit/FansubEditClient.tsx</files>
  <read_first>frontend/src/app/admin/fansubs/[id]/edit/FansubEditClient.tsx</read_first>
  <action>
First, verify the EXACT existing component name for the `collaboration` tab's member list and the existing
`claims` (invitations) tab's content location (grep `FansubEditClient.tsx`'s imports — the file path in
`files_modified` above is a best-effort guess and must be confirmed or corrected before editing). Add a
"Rechteabweichungen" column/badge to that member table per the interfaces block's honest-fallback decision,
and ensure the member name/identity cell is a real clickable navigation link to
`/admin/users/${app_user_id}` (skip if already clickable for the same purpose — verify, do not double-wrap).
Add the "Claims dieser Gruppe ansehen" link-out button near the existing invitations tab's content,
navigating to `/admin/claims?fansub_group_id={fansubId}`, without altering that tab's existing label,
content, or behavior otherwise.
  </action>
  <acceptance_criteria>
    <criterion>grep -c "Rechteabweichungen" frontend/src/app/admin/fansubs/[id]/edit/GroupMembersTable.tsx is greater than or equal to 1</criterion>
    <criterion>grep -c "/admin/claims?fansub_group_id=" frontend/src/app/admin/fansubs/[id]/edit/FansubEditClient.tsx is greater than or equal to 1</criterion>
    <criterion>docker compose exec team4sv30-frontend npm test -- --run "FansubEditClient" exits 0 (no regression to the existing invitations/collaboration tabs)</criterion>
    <criterion>docker compose exec team4sv30-frontend npx tsc --noEmit exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npm test -- --run "FansubEditClient" exits 0</automated>
  </verify>
  <done>D-06 is fully satisfied: Benutzer (extended existing member list), Rollen (new tab), Claims (link-out to the central workspace, no duplicated editor), Änderungen (new tab) are all real and correctly scoped to this one group.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A (frontend-only, reuses already-authorized data/endpoints) | GroupRolesTab reuses already-fetched, already-authorized group-member data; GroupChangesTab calls the already platform-admin-gated Plan 138-05 endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-138-28 | Information Disclosure | GroupChangesTab.tsx | accept | `listChanges` is platform-admin-gated server-side (Plan 138-05); this tab is only reachable from within the already platform-admin-gated `FansubEditClient` page, no privilege gap introduced |
</threat_model>

<verification>
- `docker compose exec team4sv30-frontend npm test -- --run "GroupRolesTab|GroupChangesTab|FansubEditClient"` exits 0
- `docker compose exec team4sv30-frontend npx tsc --noEmit` exits 0
</verification>

<success_criteria>
D-06's Gruppenansicht (Benutzer | Rollen | Claims | Änderungen) is fully real: two new correctly-scoped
tabs, an extended member list with role-deviation visibility and navigation, and a link-out to the central
Claims workspace instead of a duplicated editor.
</success_criteria>

<output>
Create `.planning/phases/138-effective-rights-administration-impact-ux/138-16-SUMMARY.md` when done
</output>
