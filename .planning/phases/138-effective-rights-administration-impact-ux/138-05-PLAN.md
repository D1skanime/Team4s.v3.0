---
phase: 138-effective-rights-administration-impact-ux
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/repository/member_claims_list_repository.go
  - backend/internal/handlers/admin_claims_list_handler.go
  - backend/internal/repository/audit_logs_query.go
  - backend/internal/handlers/admin_changes_handler.go
  - backend/internal/handlers/capability_policy_contract.go
  - backend/cmd/server/admin_routes.go
  - backend/cmd/server/main.go
  - shared/contracts/admin-capabilities.yaml
  - frontend/src/types/admin-users.ts
  - frontend/src/lib/api.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "A cross-group, filterable Claims list exists as a real backend query (D-23) — today only a per-group pending-only query exists."
    - "A cross-group, filterable 'Änderungen' (audit) list exists as a real backend query (D-25/D-28) — today only a per-user embedded audit query exists."
    - "Both new endpoints use real, existing status/field vocabulary from the code — no invented statuses or categories (D-23 binding: 'Reale Status aus dem Code verwenden')."
  artifacts:
    - path: "backend/internal/repository/member_claims_list_repository.go"
      provides: "ListClaims filtered/paginated cross-group query"
      contains: "func (r *MemberClaimsRepository) ListClaims"
    - path: "backend/internal/repository/audit_logs_query.go"
      provides: "ListChanges filtered/paginated audit_logs query"
      contains: "func (r *AuditLogRepository) ListChanges"
    - path: "backend/internal/handlers/admin_claims_list_handler.go"
      provides: "GET /admin/claims, platform-admin-gated"
      contains: "func (h *AdminClaimsListHandler) ListClaims"
    - path: "backend/internal/handlers/admin_changes_handler.go"
      provides: "GET /admin/changes, platform-admin-gated"
      contains: "func (h *AdminChangesHandler) ListChanges"
  key_links:
    - from: "backend/cmd/server/admin_routes.go"
      to: "backend/internal/handlers/admin_claims_list_handler.go"
      via: "v1.GET(\"/admin/claims\", ...)"
      pattern: "admin/claims"
    - from: "backend/cmd/server/admin_routes.go"
      to: "backend/internal/handlers/admin_changes_handler.go"
      via: "v1.GET(\"/admin/changes\", ...)"
      pattern: "admin/changes"
---

<objective>
Build the two backend list endpoints D-23 (central Claims workspace) and D-25/D-28 (central Änderungen
workspace) require and that do not exist today: `member_claims_repository.go` only has a per-group,
pending-only `ListPendingClaimsForGroup`, and `audit_logs.go` is write-only with no filtered list query
anywhere in the codebase (only a per-user embedded query in `admin_users_tab_repository.go`). Both new
endpoints are cross-group, platform-admin-gated, filtered, paginated read projections over real,
already-existing status/field vocabulary — no new tables, no invented statuses.

Purpose: unblocks the D-23 (Claims) and D-25 (Änderungen) top-level nav pages built in later plans.
Output: `MemberClaimsRepository.ListClaims` + `AuditLogRepository.ListChanges`, two new platform-admin-gated
handlers/routes, and the full contract chain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-CONTEXT.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-RESEARCH.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-PATTERNS.md

<interfaces>
Real, confirmed vocabulary (do not invent alternatives):
- `member_claims.claim_status` has exactly 3 real values: `pending`, `verified`, `rejected` (confirmed by
  grep across `member_claims_repository.go`). `claim_type` has exactly ONE real value today, the literal
  string `'claim'` (hardcoded in `admin_users_tab_repository.go`'s existing query — there is no
  `claim_type` column on the table). The new `ListClaims` filter should still accept an optional
  `claim_type` query param for forward-compatibility (matching D-23's filter list literally) but only
  ever needs to match the single literal `'claim'` value today — do not invent additional claim types.
- `audit_logs` columns (confirmed by direct read of `audit_logs.go`'s `Write()` INSERT and
  `admin_users_tab_repository.go`'s `GetUserAudit` SELECT): `id, actor_app_user_id, actor_legacy_user_id,
  event_type, scope_type, scope_id, target_type, target_id, action_name, outcome, reason_code, payload,
  created_at`. There is no separate "gruppe"/"rolle"/"capability"/"claim" column — D-25/D-28's filter
  dimensions map onto this schema as: `benutzer` -> `actor_app_user_id` OR `(target_type='app_user' AND
  target_id=...)` (mirror `GetUserAudit`'s existing OR-clause exactly), `gruppe` -> `scope_type='group' AND
  scope_id=...`, `zeitraum` -> `created_at BETWEEN`, `akteur` -> `actor_app_user_id` (same as benutzer's
  actor half). `rolle`/`capability`/`claim` filters do not have dedicated columns — filter them via
  `target_type` (e.g. `'role_capability'`, `'user_group_capability_override'`, `'member_claim'`) combined
  with a `payload @> '{"role_code": "..."}'::jsonb` / `payload @> '{"action_code": "..."}'::jsonb` JSONB
  containment check against the existing `payload` column (already used by every audit write call site
  seen this session, e.g. `Payload: map[string]any{"role_code": roleCode, "action_code": actionCode}`).

Pagination/clamping convention to reuse verbatim (grep `admin_users_repository.go` `ListAdminUsersPage`
for the exact clamp function before writing a new one — do not duplicate the clamping logic, extract a
shared helper if one does not already exist as an importable function).

`MemberClaimRow` (existing struct, `member_claims_repository.go`, read directly): `{ID int64,
AppUserID int64, MemberID int64, MemberNickname string, ClaimStatus string, Note string, CreatedAt
time.Time}` — the new `ListClaims` should return a richer row including fansub_group_id/name (join
`hist_fansub_group_members` for the claim's group context, mirroring `ListPendingClaimsForGroup`'s existing
join) and app_user email/display_name (join `app_users`) for clickable navigation — define a new
`ClaimListRow` type in the new file rather than overloading `MemberClaimRow` (different shape, different
call sites).

Platform-admin-only pattern (Source B): both new endpoints are cross-group admin workspaces (D-23
"Zentral: Arbeitsqueue", D-25 central "Änderungen"), so both use `requirePlatformAdminIdentity` as the
first handler action — mirrors `AdminCapabilityHandler`/this phase's Plan 01 role-holders endpoint. Do not
invent a third authorization pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Claims — cross-group filtered list (D-23)</name>
  <files>backend/internal/repository/member_claims_list_repository.go, backend/internal/handlers/admin_claims_list_handler.go, backend/internal/handlers/capability_policy_contract.go, backend/cmd/server/admin_routes.go, backend/cmd/server/main.go, backend/internal/repository/member_claims_list_repository_test.go</files>
  <read_first>backend/internal/repository/member_claims_repository.go, backend/internal/handlers/member_claims_handler.go, backend/internal/repository/admin_users_repository.go (pagination clamp convention), backend/internal/handlers/platform_admin_authz.go</read_first>
  <action>
Add `type ClaimListRow struct { ClaimID int64; AppUserID int64; AppUserEmail string; AppUserDisplayName
string; MemberID int64; MemberNickname string; ClaimStatus string; ClaimType string; FansubGroupID int64;
FansubGroupName string; Note string; CreatedAt time.Time; VerifiedAt *time.Time }` to
member_claims_list_repository.go (new file, mirrors `ListPendingClaimsForGroup`'s join shape but adds
`app_users` + drops the group scope, adding it as an optional filter instead).

Implement `func (r *MemberClaimsRepository) ListClaims(ctx context.Context, filter ClaimListFilter) ([]ClaimListRow, int, error)` where `ClaimListFilter` has optional `Status, Type *string`, `FansubGroupID,
AppUserID *int64`, `From, To *time.Time`, `Limit, Offset int`. Query: `SELECT mc.id, COALESCE(mc.app_user_id,0), COALESCE(au.email,''), COALESCE(NULLIF(au.display_name,''), NULLIF(au.preferred_username,''), au.email, ''), mc.member_id, COALESCE(NULLIF(m.nickname,''),'Mitglied'), mc.claim_status, 'claim', hgm.fansub_group_id, fg.name, COALESCE(mc.note,''), mc.created_at, mc.verified_at FROM member_claims mc JOIN members m ON m.id = mc.member_id JOIN hist_fansub_group_members hgm ON hgm.member_id = mc.member_id JOIN fansub_groups fg ON fg.id = hgm.fansub_group_id LEFT JOIN app_users au ON au.id = mc.app_user_id` with dynamic `WHERE` fragments built the same way `admin_users_repository.go`'s existing dynamic-filter query already does (grep it for the exact `args := []any{}` + incrementing `$N` placeholder pattern and copy it verbatim, do not invent a new dynamic-SQL-building convention), a `COUNT(*) OVER()` window column (or a parallel COUNT query, matching whichever convention `ListAdminUsersPage` already uses) for the total count, `ORDER BY mc.created_at DESC`, clamped `LIMIT`/`OFFSET` (default 25, max 100, same clamp helper convention as `admin_users_repository.go`).

Create admin_claims_list_handler.go: `AdminClaimsListHandler` wrapping `capabilityAuthzRepo` (platform-admin check) + a narrow `claimsListRepo` interface exposing `ListClaims`. `ListClaims(c *gin.Context)`: `requirePlatformAdminIdentity` first; parse query params `status`, `claim_type`, `fansub_group_id`, `app_user_id`, `from`, `to`, `limit`, `offset` (all optional); build the filter struct; call the repo; respond `gin.H{"data": rows, "meta": gin.H{"total": total, "limit": limit, "offset": offset}}` mirroring `AdminUserListResponse`'s existing envelope shape.

Register `v1.GET("/admin/claims", auth, deps.adminClaimsListHandler.ListClaims)` in admin_routes.go; add the field to `adminRouteHandlers`; wire construction in main.go reusing the existing `memberClaimsRepo`/`authzRepo` (grep main.go for the existing `memberClaimsRepo` variable name and construct `adminClaimsListHandler := handlers.NewAdminClaimsListHandler(authzRepo, memberClaimsRepo)` next to `memberClaimsHandler`'s construction).

Write member_claims_list_repository_test.go proving: status filter returns only matching rows; fansub_group_id filter scopes correctly; an empty result set returns an empty (not nil) slice with total=0; pagination limit/offset works; the clamp rejects/clamps an out-of-range limit (e.g. limit=99999 clamps to the max, not passed through raw).
  </action>
  <acceptance_criteria>
    <criterion>grep -c "func (r \*MemberClaimsRepository) ListClaims" backend/internal/repository/member_claims_list_repository.go equals 1</criterion>
    <criterion>grep -c "requirePlatformAdminIdentity" backend/internal/handlers/admin_claims_list_handler.go equals 1</criterion>
    <criterion>grep -c "admin/claims" backend/cmd/server/admin_routes.go equals 1</criterion>
    <criterion>docker compose exec team4sv30-backend go build ./... exits 0</criterion>
    <criterion>docker compose exec -e TEAM4S_PHASE137_TEST_DSN=$TEAM4S_PHASE137_TEST_DSN team4sv30-backend go test ./internal/repository/... -run TestListClaims -count=1 -v exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -e TEAM4S_PHASE137_TEST_DSN=$TEAM4S_PHASE137_TEST_DSN team4sv30-backend go test ./internal/repository/... -run TestListClaims -count=1 -v exits 0</automated>
  </verify>
  <done>A real, tested, platform-admin-gated cross-group Claims list endpoint exists with status/type/group/user/date filtering and clamped pagination.</done>
</task>

<task type="auto">
  <name>Task 2: Änderungen — cross-group filtered audit list (D-25/D-28)</name>
  <files>backend/internal/repository/audit_logs_query.go, backend/internal/handlers/admin_changes_handler.go, backend/internal/handlers/capability_policy_contract.go, backend/cmd/server/admin_routes.go, backend/cmd/server/main.go, shared/contracts/admin-capabilities.yaml, frontend/src/types/admin-users.ts, frontend/src/lib/api.ts, backend/internal/repository/audit_logs_query_test.go</files>
  <read_first>backend/internal/repository/audit_logs.go, backend/internal/repository/admin_users_tab_repository.go (GetUserAudit), backend/internal/repository/admin_users_repository.go (pagination clamp convention)</read_first>
  <action>
Create audit_logs_query.go (kept separate from the write-only audit_logs.go per the 450-line discipline already flagged in 138-RESEARCH.md for other handlers) implementing `func (r *AuditLogRepository) ListChanges(ctx context.Context, filter ChangeListFilter) ([]ChangeListRow, int, error)` where `ChangeListFilter` has optional `AppUserID, ScopeGroupID *int64`, `TargetType *string` (role_capability | user_group_capability_override | member_claim | app_user | ...), `From, To *time.Time`, `Limit, Offset int`. `ChangeListRow` mirrors the existing `AdminAuditEntry` field set (`event_id, event_type, target_type, target_id, action, outcome, occurred_at`) plus `actor_app_user_id`, `scope_type`, `scope_id`, and `payload` (raw JSONB, returned as `json.RawMessage` — the frontend translator (a later plan) reads specific known keys out of it per event_type, this endpoint does not interpret payload contents). Query selects from `audit_logs` with the `benutzer`/`gruppe`/`zeitraum`/`akteur` WHERE fragments specified in the interfaces block above (reuse `GetUserAudit`'s existing benutzer OR-clause shape: `actor_app_user_id = $N OR (target_type = 'app_user' AND target_id = $N)`), `ORDER BY created_at DESC, id DESC`, same clamped LIMIT/OFFSET convention as Task 1.

Create admin_changes_handler.go: `AdminChangesHandler` wrapping `capabilityAuthzRepo` + a narrow `changesRepo` interface exposing `ListChanges`. `ListChanges(c *gin.Context)`: `requirePlatformAdminIdentity` first; parse query params (`benutzer`, `gruppe`, `target_type`, `from`, `to`, `limit`, `offset`); respond with the same `data`/`meta` envelope shape as Task 1.

Register `v1.GET("/admin/changes", auth, deps.adminChangesHandler.ListChanges)` in admin_routes.go; add the field to `adminRouteHandlers`; wire construction in main.go reusing the existing `auditLogRepo` variable (construct `adminChangesHandler := handlers.NewAdminChangesHandler(authzRepo, auditLogRepo)`).

Add `ChangeListRow`/`ChangeListResponse` schemas and the `GET /admin/changes` path to shared/contracts/admin-capabilities.yaml. Add the matching `AdminChangeEntry`/`AdminChangesListResponse` TS interfaces to frontend/src/types/admin-users.ts and `export async function listChanges(params: {...}): Promise<AdminChangesListResponse>` to frontend/src/lib/api.ts, matching `listAdminUsersPage`'s existing query-string-building convention (grep it and reuse the same URLSearchParams pattern, do not invent a second one).

Write audit_logs_query_test.go proving: benutzer filter matches both actor AND target rows (mirroring GetUserAudit's OR semantics); gruppe filter scopes to scope_type='group' rows only; date range filtering works at the boundary (inclusive `from`, exclusive or inclusive `to` — document the exact choice in a code comment); pagination clamps correctly.
  </action>
  <acceptance_criteria>
    <criterion>grep -c "func (r \*AuditLogRepository) ListChanges" backend/internal/repository/audit_logs_query.go equals 1</criterion>
    <criterion>grep -c "requirePlatformAdminIdentity" backend/internal/handlers/admin_changes_handler.go equals 1</criterion>
    <criterion>grep -c "admin/changes" backend/cmd/server/admin_routes.go equals 1</criterion>
    <criterion>grep -c "export async function listChanges" frontend/src/lib/api.ts equals 1</criterion>
    <criterion>docker compose exec team4sv30-backend go build ./... exits 0</criterion>
    <criterion>docker compose exec -e TEAM4S_PHASE137_TEST_DSN=$TEAM4S_PHASE137_TEST_DSN team4sv30-backend go test ./internal/repository/... -run TestListChanges -count=1 -v exits 0</criterion>
    <criterion>docker compose exec team4sv30-frontend npx tsc --noEmit exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -e TEAM4S_PHASE137_TEST_DSN=$TEAM4S_PHASE137_TEST_DSN team4sv30-backend go test ./internal/repository/... -run TestListChanges -count=1 -v exits 0</automated>
  </verify>
  <done>A real, tested, platform-admin-gated cross-group Änderungen (audit) list endpoint exists with benutzer/gruppe/target_type/zeitraum filtering, clamped pagination, and the full D-35 contract chain, ready for a later frontend translation layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin browser -> new GET /admin/claims, GET /admin/changes | Platform-admin-only, cross-group read surfaces; all filter params are attacker-controlled query-string input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-138-10 | Information Disclosure | admin_claims_list_handler.go / admin_changes_handler.go | mitigate | Both are platform-admin-gated as the first handler action (Source B) — this is intentionally MORE restrictive than the existing per-group `ListPendingClaimsForGroup` (which is group-delegable), since these are cross-group aggregation views that would otherwise let a group-scoped delegate enumerate claims/audit entries outside their own group |
| T-138-11 | Tampering (SQL injection) | dynamic WHERE-fragment building | mitigate | Every filter value is bound via a numbered placeholder ($N), reusing the exact `admin_users_repository.go` dynamic-filter convention already audited in prior phases — never string-concatenated into the query text |
| T-138-12 | Denial of Service | unclamped limit/offset | mitigate | Both new list methods reuse the existing clamp convention (default 25, hard max) rather than trusting a raw client-supplied limit |
</threat_model>

<verification>
- `docker compose exec team4sv30-backend go build ./...` exits 0
- `docker compose exec -e TEAM4S_PHASE137_TEST_DSN=$TEAM4S_PHASE137_TEST_DSN team4sv30-backend go test ./internal/repository/... ./internal/handlers/... -run 'ListClaims|ListChanges' -count=1` exits 0
- `docker compose exec team4sv30-frontend npx tsc --noEmit` exits 0
</verification>

<success_criteria>
Two new platform-admin-gated, filtered, paginated, tested cross-group list endpoints exist (Claims,
Änderungen) using only real status/field vocabulary, ready for later frontend workspace pages.
</success_criteria>

<output>
Create `.planning/phases/138-effective-rights-administration-impact-ux/138-05-SUMMARY.md` when done
</output>
