---
phase: 138-effective-rights-administration-impact-ux
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/repository/admin_users_tab_repository.go
  - backend/internal/models/admin_users.go
  - frontend/src/types/admin-users.ts
  - frontend/src/app/admin/users/tabs/UserContributionsTab.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "The Beiträge tab never shows an internal release_versions.id as if it were a business version number (D-29)."
    - "The fix is narrowly scoped to the display bug only — no grouping, range-collapse, or pagination change (D-29 explicitly reserves that work for Phase 139)."
  artifacts:
    - path: "backend/internal/repository/admin_users_tab_repository.go"
      provides: "ListUserContributions additionally selects the real version label and episode number"
      contains: "release_versions.version"
    - path: "frontend/src/app/admin/users/tabs/UserContributionsTab.tsx"
      provides: "Renders '{Anime-Titel} · Episode {episode_number} · Version {version}' instead of the raw release_version_id"
  key_links:
    - from: "backend/internal/repository/admin_users_tab_repository.go"
      to: "frontend/src/app/admin/users/tabs/UserContributionsTab.tsx"
      via: "AdminContributionItem.version / .episode_number fields"
      pattern: "episode_number"
---

<objective>
Fix the one narrowly-scoped D-29 display bug: `UserContributionsTab.tsx` renders
`Version {item.release_version_id}` (an internal `release_versions.id` primary key) as if it were the
real business version label. Extend `ListUserContributions`'s existing query with the real
`release_versions.version` label and the episode number, and render them instead — additive, no schema
migration, no grouping/range-collapse work (that belongs to Phase 139 per D-29/138-CONTEXT.md, explicitly
out of scope here).

Purpose: closes the one Beiträge-surface defect Phase 138 is authorized to touch without pulling Phase
139's full redesign forward.
Output: corrected contribution rows showing "{Anime-Titel} · Episode {N} · Version {V}".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-CONTEXT.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-RESEARCH.md

<interfaces>
From backend/internal/repository/admin_users_tab_repository.go, `ListUserContributions` (read directly
this session, lines 149-233) — the exact query to extend:
```go
rows, err := r.db.Query(ctx, `
    SELECT
        ac.id, ac.fansub_group_id, fg.name AS fansub_group_name,
        ac.anime_id, a.title AS anime_title,
        ac.release_version_id,
        CASE WHEN ac.release_version_id IS NULL THEN 'project_default' ELSE 'release_override' END,
        COALESCE(ac.dispute_state, ''),
        COALESCE(ARRAY_AGG(acr.role_code ORDER BY acr.role_code) FILTER (WHERE acr.role_code IS NOT NULL), ARRAY[]::text[]),
        (ac.member_id IS NULL) AS is_legacy_historical
    FROM anime_contributions ac
    JOIN fansub_groups fg ON fg.id = ac.fansub_group_id
    JOIN anime a ON a.id = ac.anime_id
    LEFT JOIN hist_fansub_group_members hfgm ON hfgm.id = ac.fansub_group_member_id
    LEFT JOIN anime_contribution_roles acr ON acr.anime_contribution_id = ac.id
    WHERE COALESCE(ac.member_id, hfgm.member_id) = $1
    GROUP BY ac.id, ac.member_id, fg.name, a.title
    ORDER BY a.title, ac.release_version_id NULLS FIRST, ac.id
`, memberID)
```
Add `LEFT JOIN release_versions rv ON rv.id = ac.release_version_id`,
`LEFT JOIN fansub_releases fr ON fr.id = rv.release_id`,
`LEFT JOIN episodes ep ON ep.id = fr.episode_id`, select `rv.version` and `ep.episode_number`
additionally (both nullable — `ac.release_version_id` is nullable for project-default contributions,
per the existing `CASE WHEN ac.release_version_id IS NULL` branch), and add both new columns to the
`GROUP BY` clause (`rv.version, ep.episode_number`) since the query is aggregate (`ARRAY_AGG`).

From backend/internal/models/admin_users.go, `AdminContributionItem` (read directly, lines 156-166) —
add two nullable fields: `ReleaseVersionLabel *string \`json:"release_version_label"\`` and
`EpisodeNumber *int \`json:"episode_number"\``. Keep the existing `ReleaseVersionID *int64` field
unchanged (still needed internally, just not rendered raw in the UI anymore).

From frontend/src/types/admin-users.ts, `AdminContributionItem` interface (lines 170-180) — mirror the
two new nullable fields (`release_version_label: string | null`, `episode_number: number | null`).

From frontend/src/app/admin/users/tabs/UserContributionsTab.tsx line 80 (read directly, the exact bug
site):
```tsx
{item.release_version_id != null ? (
  <Badge variant="info">Version {item.release_version_id}</Badge>
) : (
  <Badge variant="muted">–</Badge>
)}
```
Replace with a render using `item.anime_title`, `item.episode_number`, `item.release_version_label` —
when `release_version_id` is null (project-default contribution, no specific release version), keep the
existing `<Badge variant="muted">–</Badge>` fallback unchanged; when `release_version_id` is set but
`episode_number`/`release_version_label` are unexpectedly null (a data-integrity edge case the join
cannot resolve), fall back to the existing raw-ID badge rather than rendering a broken/empty string —
never silently render "Episode null · Version null".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — extend ListUserContributions with the real version label + episode number</name>
  <files>backend/internal/repository/admin_users_tab_repository.go, backend/internal/models/admin_users.go, backend/internal/repository/admin_users_tab_repository_test.go</files>
  <read_first>backend/internal/repository/admin_users_tab_repository.go, backend/internal/models/admin_users.go</read_first>
  <action>
Extend the SQL query and Go struct exactly as specified in the interfaces block: add the two LEFT JOINs
(release_versions, fansub_releases, episodes), select `rv.version` and `ep.episode_number` as two
additional nullable scan targets, add them to GROUP BY, and scan them into the two new
`AdminContributionItem` fields (`&item.ReleaseVersionLabel`, `&item.EpisodeNumber` — use `sql.NullString`/
`sql.NullInt32` or pgx-compatible pointer scanning consistent with how the rest of this file already
handles nullable columns; grep this same file for its existing nullable-scan convention and match it
exactly rather than introducing a new one).

Extend or add a test in admin_users_tab_repository_test.go (reuse whatever Postgres test harness this
file's existing tests already use — grep the file for its current harness before adding a new one)
proving: a contribution with a real `release_version_id` returns the correct `release_versions.version`
string and `episodes.episode_number` int; a project-default contribution (`release_version_id IS NULL`)
returns both new fields as nil/null without breaking the query's GROUP BY aggregation.
  </action>
  <acceptance_criteria>
    <criterion>grep -c "release_versions.version\|rv.version" backend/internal/repository/admin_users_tab_repository.go is greater than or equal to 1</criterion>
    <criterion>grep -c "ReleaseVersionLabel\|EpisodeNumber" backend/internal/models/admin_users.go equals 2</criterion>
    <criterion>docker compose exec team4sv30-backend go build ./... exits 0</criterion>
    <criterion>docker compose exec team4sv30-backend go test ./internal/repository/... -run TestListUserContributions -count=1 -v exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-backend go test ./internal/repository/... -run TestListUserContributions -count=1 -v exits 0</automated>
  </verify>
  <done>ListUserContributions returns the real business version label and episode number alongside the existing internal ID, additive and non-breaking for project-default (null) rows.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — render the real label, fix the display bug</name>
  <files>frontend/src/types/admin-users.ts, frontend/src/app/admin/users/tabs/UserContributionsTab.tsx, frontend/src/app/admin/users/tabs/UserContributionsTab.test.tsx</files>
  <read_first>frontend/src/types/admin-users.ts, frontend/src/app/admin/users/tabs/UserContributionsTab.tsx</read_first>
  <action>
Add `release_version_label: string | null` and `episode_number: number | null` to the
`AdminContributionItem` TypeScript interface in frontend/src/types/admin-users.ts.

In UserContributionsTab.tsx, replace the `Version {item.release_version_id}` render (line ~80) with:
when `item.release_version_id != null` AND both `item.episode_number != null` AND
`item.release_version_label != null`, render `<Badge variant="info">{item.anime_title} · Episode {item.episode_number} · Version {item.release_version_label}</Badge>`; when `item.release_version_id != null` but either new field is unexpectedly null, fall back to the prior raw-ID badge (`Version {item.release_version_id}`) rather than a broken render; when `item.release_version_id == null`, keep the existing `<Badge variant="muted">–</Badge>` unchanged. Use the correct German middle-dot character `·` (U+00B7), not a hyphen or ASCII substitute, and do not touch any other part of the tab's layout, grouping, or pagination.
  </action>
  <acceptance_criteria>
    <criterion>grep -c "release_version_label" frontend/src/types/admin-users.ts equals 1</criterion>
    <criterion>grep -c "Version {item.release_version_id}" frontend/src/app/admin/users/tabs/UserContributionsTab.tsx equals 1 (only the fallback path retains the raw-ID render, not the primary path)</criterion>
    <criterion>grep -c "episode_number" frontend/src/app/admin/users/tabs/UserContributionsTab.tsx is greater than or equal to 1</criterion>
    <criterion>docker compose exec team4sv30-frontend npm test -- --run "UserContributionsTab" exits 0</criterion>
    <criterion>docker compose exec team4sv30-frontend npx tsc --noEmit exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npm test -- --run "UserContributionsTab" exits 0</automated>
  </verify>
  <done>The Beiträge tab renders the real anime title, episode number, and business version label instead of the internal release_version_id, with a safe fallback for incomplete data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | Pure read-projection extension of an already-authorized, already-scoped (member-anchored) query; no new trust boundary is crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-138-06 | Information Disclosure | ListUserContributions | accept | The two new joined columns (release version label, episode number) are already-public-within-admin business data already reachable via other admin surfaces; no new sensitive field is exposed |
</threat_model>

<verification>
- `docker compose exec team4sv30-backend go build ./...` exits 0
- `docker compose exec team4sv30-backend go test ./internal/repository/... -run TestListUserContributions -count=1` exits 0
- `docker compose exec team4sv30-frontend npm test -- --run "UserContributionsTab"` exits 0
- `docker compose exec team4sv30-frontend npx tsc --noEmit` exits 0
</verification>

<success_criteria>
UserContributionsTab.tsx never renders a raw release_versions.id as a fake version number; the real
{Anime-Titel} · Episode {N} · Version {V} label renders wherever the data is complete, D-29's narrow scope
is respected (no grouping/range-collapse/pagination change).
</success_criteria>

<output>
Create `.planning/phases/138-effective-rights-administration-impact-ux/138-03-SUMMARY.md` when done
</output>
