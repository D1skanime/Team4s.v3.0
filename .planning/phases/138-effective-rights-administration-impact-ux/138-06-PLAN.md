---
phase: 138-effective-rights-administration-impact-ux
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx
  - frontend/src/app/admin/users/tabs/UserGroupRightsTab.test.tsx
autonomous: true
requirements: [UADM-01]

must_haves:
  truths:
    - "The existing UserGroupRightsTab shows the COMPLETE effective capability set and its provenance per group, sourced from the real Phase-137 resolver — not the old two-boolean heuristic (D-09, D-11, Roadmap criterion 1)."
    - "Capabilities are grouped by group first, then by real registry category, with a compact default view and progressive detail on open (D-11, D-12, D-13) — never a flat cross-group list."
    - "The frontend introduces zero new precedence logic — it only renders the resolver's own EffectiveRightState fields (D-14, binding)."
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "getEffectiveRights, mutateCapabilityOverride, listOverrideHistory — zero of these exist today"
      contains: "export async function getEffectiveRights"
    - path: "frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx"
      provides: "Canonical, multi-group, category-grouped effective-rights inspection surface"
      contains: "getEffectiveRights"
  key_links:
    - from: "frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx"
      to: "frontend/src/lib/api.ts"
      via: "getEffectiveRights(fansubGroupId, appUserId) per membership"
      pattern: "getEffectiveRights"
---

<objective>
This is the foundational, contract-first half of UADM-01: wire the three already-shipped Phase-137
effective-rights endpoints into `frontend/src/lib/api.ts` (zero of them are consumed anywhere in the
frontend today, per 138-RESEARCH.md R-09 — confirmed by a full-file grep this session), and re-point
`UserGroupRightsTab.tsx` at the real resolver instead of the old `GetUserGroupRights` two-boolean
heuristic endpoint. This plan delivers the INSPECTION half of UADM-01 completely (D-11/D-12/D-13's full
per-group, categorized, provenance-capable catalog); the EDITING half (CAP-08's guided revoke/grant flows,
D-16/D-17) is Plan 138-08, which depends on this plan and extends the same file.

Purpose: today's tab (confirmed read directly this session) is read-only and backed by a heuristic
`AdminGroupRightsSummary{can_edit_content, can_view_members}` shape that cannot represent D-12's five
states (erlaubt/nicht erlaubt/persönlich entzogen/persönlich zusätzlich erlaubt/non-deniable) at all.
Output: a corrected, multi-group, categorized effective-rights inspection UI plus the three new api.ts
functions later plans (guided flows, impact previews) depend on.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-CONTEXT.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-RESEARCH.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-UI-SPEC.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-PATTERNS.md

<interfaces>
Already-shipped backend endpoints this plan wires up (no backend change needed, all three exist and are
tested per Phase 137):
- `GET /api/v1/admin/fansubs/:id/app-members/:appUserId/effective-rights` -> `{data: EffectiveRightState[]}`
- `PUT /api/v1/admin/fansubs/:id/app-members/:appUserId/capability-overrides` -> `{data:
  CapabilityOverrideMutationResult}` (body: `CapabilityOverrideMutationRequest`)
- `GET /api/v1/admin/fansubs/:id/app-members/:appUserId/capability-overrides/history?limit=&offset=` ->
  `{data: CapabilityOverrideAuditItem[]}`
All three TS types (`EffectiveRightState`, `CapabilityOverrideMutationResult`,
`CapabilityOverrideMutationRequest`, `CapabilityOverrideAuditItem`) already exist in
`frontend/src/types/admin-capability.ts` (confirmed read directly this session) — do not redefine them.

`getAdminUserGroupRights` (frontend/src/lib/api.ts, lines 3738-3753, read directly) — exact
`apiClientFetch`/`parseApiErrorPayload`/`ApiError` shape to reuse verbatim for `getEffectiveRights` and
`listOverrideHistory` (simple GETs, `cache: "no-store"`).

`grantRoleCapability` (frontend/src/lib/api.ts, ~line 9808, read directly) — exact `authorizedFetch`
shape to reuse for `mutateCapabilityOverride` (this is a PUT with a JSON body and must use
`authorizedFetch` + `getApiBaseUrl()`, matching the mutation-function convention, NOT `apiClientFetch`
which the read-only functions in this same block use — 138-PATTERNS.md flags these as two genuinely
different, non-interchangeable conventions already present in this file; match the correct one per
function, do not mix them).

Current `UserGroupRightsTab.tsx` (read directly this session, full 187-line file) — the exact structure
to replace: single `getAdminUserGroupRights(userId)` call returning `AdminUserGroupRightsResponse{
group_rights: AdminGroupRightsSummary[] }` where each row only carries `can_edit_content`/
`can_view_members` booleans. Replace with: first call `getAdminUserGroupMemberships(userId)` (existing,
already-shipped endpoint returning `AdminGroupMembershipSummary[]` with `fansub_group_id`,
`fansub_group_name`, `roles: string[]`, `joined_at` per `frontend/src/types/admin-users.ts`) to discover
which groups this user belongs to, THEN call `getEffectiveRights(membership.fansub_group_id, userId)` ONCE
PER membership (D-11: rights are structured by group first — this is intentionally N calls for N
memberships, not one flattened call; N is bounded by how many groups one admin user realistically belongs
to, no pagination needed here). Keep the existing `listRoleCapabilities()` fetch (already present, used
for `resolveRoleLink` and now ALSO reused to build a `action_code -> {category, label_de}` lookup map
from `matrix.all_actions: ActionEntry[]` for D-12's category grouping — do not fetch action metadata a
second way).

UI-SPEC.md D-12/D-13 contract: per group, group capabilities by `all_actions[code].category` into
`Accordion` sections (7 real categories per 138-RESEARCH.md R-02: `gruppe`, `gruppenmedien`,
`gruppenseite`, `projekt`, `rechteverwaltung`, `release`, `review` — reuse/extend
`frontend/src/app/admin/role-capabilities/capabilityCategories.ts`'s `categoryDisplayLabel` function
rather than hardcoding a second label map). Default compact row per capability: `Capability | Effektiv |
Quelle` (Table columns), where "Effektiv" is `state.allowed` rendered as a `Badge` and "Quelle" is a
human string derived from `state.decisive_source` + `state.granting_roles.join(' + ')` (the exact D-13
example: "Erlaubt / Co-Leitung + Encoder"). Clicking a row expands (progressive detail, D-13) to show the
full provenance: `granting_roles`, `user_allow`, `user_deny`, `specialized_grants`, `decisive_source`,
`non_deniable`, `reason_code` — this plan renders these fields read-only; Plan 138-08 adds the
history panel and action buttons in the same expanded area.

D-15/D-33 (binding, applies even to this read-only plan): never render a bare `Switch` for a capability's
allow/deny state — this plan is inspection-only and renders zero mutation controls, so this constraint is
automatically satisfied here; it becomes load-bearing once Plan 138-08 adds the action buttons.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api.ts — wire the three shipped Phase-137 effective-rights endpoints</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>frontend/src/lib/api.ts (getAdminUserGroupRights ~3738, grantRoleCapability ~9808), frontend/src/types/admin-capability.ts</read_first>
  <action>
Add three functions near the existing role-capabilities block (~line 9778 onward), each using the
correct fetch convention per the interfaces block above:

`export async function getEffectiveRights(fansubGroupId: number, appUserId: number): Promise<EffectiveRightState[]>` — GET `/api/v1/admin/fansubs/${fansubGroupId}/app-members/${appUserId}/effective-rights`, `apiClientFetch`, `cache: "no-store"`, unwrap `{data: ...}`.

`export async function mutateCapabilityOverride(fansubGroupId: number, appUserId: number, body: CapabilityOverrideMutationRequest): Promise<CapabilityOverrideMutationResult>` — PUT `/api/v1/admin/fansubs/${fansubGroupId}/app-members/${appUserId}/capability-overrides`, `authorizedFetch` + `getApiBaseUrl()`, JSON body, unwrap `{data: ...}`.

`export async function listOverrideHistory(fansubGroupId: number, appUserId: number, limit = 25, offset = 0): Promise<CapabilityOverrideAuditItem[]>` — GET `/api/v1/admin/fansubs/${fansubGroupId}/app-members/${appUserId}/capability-overrides/history?limit=${limit}&offset=${offset}`, `apiClientFetch`, `cache: "no-store"`, unwrap `{data: ...}`.

All three throw `ApiError(response.status, parsed.message, null, parsed.code, parsed.details)` on a
non-ok response via `parseApiErrorPayload`, matching every existing function in this file exactly.
  </action>
  <acceptance_criteria>
    <criterion>grep -c "export async function getEffectiveRights" frontend/src/lib/api.ts equals 1</criterion>
    <criterion>grep -c "export async function mutateCapabilityOverride" frontend/src/lib/api.ts equals 1</criterion>
    <criterion>grep -c "export async function listOverrideHistory" frontend/src/lib/api.ts equals 1</criterion>
    <criterion>docker compose exec team4sv30-frontend npx tsc --noEmit exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npx tsc --noEmit exits 0</automated>
  </verify>
  <done>All three Phase-137 endpoints are consumable from the frontend with correct types and the established fetch conventions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UserGroupRightsTab becomes the canonical, multi-group, categorized inspection surface</name>
  <files>frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx, frontend/src/app/admin/users/tabs/UserGroupRightsTab.test.tsx</files>
  <read_first>frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx, frontend/src/types/admin-users.ts, frontend/src/types/admin-capability.ts, frontend/src/app/admin/role-capabilities/capabilityCategories.ts, frontend/src/lib/api.ts (getAdminUserGroupMemberships)</read_first>
  <behavior>
    - Given a user with zero group memberships, the tab renders the locked D-05-adjacent empty state ("Keine Rechte in dieser Gruppe." is per-group; the zero-membership case renders a top-level EmptyState instead, e.g. "Keine Gruppenmitgliedschaften.").
    - Given a user with one group membership, the tab renders exactly one group section showing every action from the resolved EffectiveRightState[] catalog, grouped into Accordion sections by the real category (gruppe/gruppenmedien/gruppenseite/projekt/rechteverwaltung/release/review), each row showing Capability/Effektiv/Quelle.
    - A capability whose decisive_source is 'user_deny' renders "Quelle" as a human 'persönliche Abweichung' label, not the raw string 'user_deny'.
    - A capability with multiple granting_roles renders them joined as "RoleA + RoleB", matching the locked D-13 example format.
    - Expanding a row reveals non_deniable, reason_code, and the full granting_roles/specialized_grants lists without throwing when any of those arrays is empty.
    - Given two group memberships, the tab renders two independent, non-flattened group sections (D-11) — capabilities from one group never bleed into the other's section.
  </behavior>
  <action>
Rewrite `loadData` to: call `getAdminUserGroupMemberships(userId)`; for each returned membership, call
`getEffectiveRights(membership.fansub_group_id, userId)`; store results as
`Map<fansubGroupId, EffectiveRightState[]>` alongside the membership list in component state (a single
`useState` holding `{memberships: AdminGroupMembershipSummary[], rightsByGroup: Record<number,
EffectiveRightState[]>}` is sufficient — do not introduce a second loading state machine per group,
resolve all group calls via `Promise.all` and set state once). Keep the existing `listRoleCapabilities()`
effect for the action-metadata lookup map, reused for BOTH the existing `resolveRoleLink` cross-navigation
AND the new category grouping (build `const actionMeta = new Map(matrix.all_actions.map(a => [a.code,
a]))` once).

Replace the old `RightsTable` component with a new per-group renderer: `SectionHeader` per group (group
name + link to `/admin/fansubs/${id}/edit`, reusing the existing `Button` navigation-link pattern from the
old `RightsTable`'s "Gruppe bearbeiten" cell), followed by an `Accordion` (mode="multi") with one item per
real category present in that group's `EffectiveRightState[]` (skip categories with zero actions for this
catalog — do not render an empty accordion section), each item containing a `Table` with columns
Capability (from `actionMeta.get(state.action_code)?.label_de ?? state.action_code`), Effektiv (`Badge
variant={state.allowed ? 'success' : 'muted'}`, text "Erlaubt"/"Nicht erlaubt"), Quelle (a small pure
helper function `decisiveSourceLabel(state: EffectiveRightState): string` mapping `decisive_source` ->
German label: `platform_admin` -> "Plattform-Admin", `group_role` -> `state.granting_roles.join(' + ')`,
`user_allow` -> "persönliche Abweichung (zusätzlich erlaubt)", `user_deny` -> "persönliche Abweichung
(entzogen)", `specialized_grant` -> `state.specialized_grants.join(' + ')`, `no_grant` -> "–"). Each row is
expandable (reuse `Table`'s existing row-click/expand pattern if one exists in this codebase, else a
simple controlled `Set<string>` of expanded action_codes per group, mirroring the existing
`openCategories` controlled-set pattern already used in `RoleCapabilityDetail.tsx`) revealing
`non_deniable`, `reason_code`, full `granting_roles`/`specialized_grants` arrays, `user_allow`/`user_deny`
booleans — all read-only text/Badge rendering, zero action buttons in this plan (Plan 138-08 adds them in
the same expanded area).

Keep `LoadingState`/`ErrorState`/`EmptyState` from `@/components/ui` for the three non-happy-path renders,
matching every other tab in this codebase (138-PATTERNS.md Shared Patterns). Delete the now-dead
`getAdminUserGroupRights` import and the old `AdminGroupRightsSummary` type usage from this file (the
heuristic endpoint itself stays live in the backend for any other consumer — do not delete the backend
endpoint, only stop using it here, per 138-RESEARCH.md's "verify at plan time whether anything else
depends on it" caveat — grep the frontend for other `getAdminUserGroupRights` call sites before removing
the import; if none remain outside this file, that is expected and fine).

Write the 6 `<behavior>` cases above as Vitest + Testing Library tests in
UserGroupRightsTab.test.tsx, mocking `getAdminUserGroupMemberships`/`getEffectiveRights`/
`listRoleCapabilities` at the `@/lib/api` module boundary (grep an existing tab test file, e.g.
`UserAuditTab.test.tsx` if present, for this codebase's established API-mocking convention and match it).
  </action>
  <acceptance_criteria>
    <criterion>grep -c "getAdminUserGroupRights" frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx equals 0 (fully re-pointed, old heuristic call removed)</criterion>
    <criterion>grep -c "getEffectiveRights" frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx is greater than or equal to 1</criterion>
    <criterion>grep -c "getAdminUserGroupMemberships" frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx is greater than or equal to 1</criterion>
    <criterion>grep -c "&lt;Switch" frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx equals 0 (D-15: no bare switch in this surface)</criterion>
    <criterion>docker compose exec team4sv30-frontend npm test -- --run "UserGroupRightsTab" exits 0 with all 6 behavior cases passing</criterion>
    <criterion>docker compose exec team4sv30-frontend npx tsc --noEmit exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npm test -- --run "UserGroupRightsTab" exits 0</automated>
  </verify>
  <done>UserGroupRightsTab.tsx is the correct, multi-group, categorized, fully-provenance-capable read surface sourced from the real Phase-137 resolver, ready for Plan 138-08 to add editing actions on top.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A (frontend-only, read-only) | This plan consumes already-authorized, already-tested backend endpoints; it introduces no new server-side trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-138-13 | Tampering (client-side precedence drift) | UserGroupRightsTab.tsx | mitigate | The component only ever renders fields already present on `EffectiveRightState` — no client-side recomputation of `allowed`/`decisive_source`/precedence is introduced (D-14, binding); covered by the "zero new precedence logic" must-have and reviewable via source inspection at execution time |
</threat_model>

<verification>
- `docker compose exec team4sv30-frontend npm test -- --run "UserGroupRightsTab"` exits 0
- `docker compose exec team4sv30-frontend npx tsc --noEmit` exits 0
</verification>

<success_criteria>
UserGroupRightsTab.tsx is re-pointed at the real Phase-137 resolver, shows the complete per-group,
categorized, provenance-capable effective-rights catalog (D-11/D-12/D-13), and introduces zero client-side
precedence logic (D-14) — the inspection half of UADM-01 / Roadmap criterion 1 is fully satisfied.
</success_criteria>

<output>
Create `.planning/phases/138-effective-rights-administration-impact-ux/138-06-SUMMARY.md` when done
</output>
