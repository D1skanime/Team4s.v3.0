---
phase: 138-effective-rights-administration-impact-ux
plan: 10
type: execute
wave: 2
depends_on: ["138-05"]
files_modified:
  - frontend/src/app/admin/claims/page.tsx
  - frontend/src/app/admin/claims/ClaimsClient.tsx
  - frontend/src/app/admin/claims/ClaimsClient.test.tsx
  - frontend/src/app/admin/claims/useClaimsListFilters.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "A central Claims workspace exists as a real top-level admin route, listing claims across ALL groups with status/type/group/user/date filters sourced from real status vocabulary (D-23)."
    - "Every user/group cell in the list is a real clickable navigation target, never inert text (D-02 bidirectional navigation)."
  artifacts:
    - path: "frontend/src/app/admin/claims/page.tsx"
      provides: "New /admin/claims top-level route"
    - path: "frontend/src/app/admin/claims/ClaimsClient.tsx"
      provides: "Filtered, paginated Claims list client"
      contains: "listClaims"
  key_links:
    - from: "frontend/src/app/admin/claims/ClaimsClient.tsx"
      to: "frontend/src/lib/api.ts"
      via: "listClaims(params)"
      pattern: "listClaims"
---

<objective>
Build the D-23 central Claims workspace as a new top-level admin route, consuming Plan 138-05's new
cross-group filtered `GET /admin/claims` endpoint. This plan is the list/filter/navigation shell only;
D-24's decision-impact preview (verify/activate actions) is Plan 138-14, which wires its confirm actions
into this same page once this plan's list surface exists.

Purpose: closes D-23's "Zentral (`Claims`): Arbeitsqueue mit Status, Benutzer, Gruppe, Claim-Typ,
beantragter Rolle bzw. Zielobjekt, Datum, direkter Aktion, Navigation zu Benutzer und zu Gruppe."
Output: `/admin/claims` route with filter bar, paginated table, and working bidirectional navigation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-CONTEXT.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-RESEARCH.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-UI-SPEC.md
@.planning/phases/138-effective-rights-administration-impact-ux/138-PATTERNS.md

<interfaces>
Analog to copy structurally (frontend/src/app/admin/users/page.tsx + AdminUsersClient.tsx +
useUserListFilters.ts, read directly this session) — the established list+filter+pagination shape:
`useUserListFilters.ts`'s exact URL-synced, debounced-search, stable-`useMemo`'d-params pattern (the
file's own comment explains WHY the memo is required — an unstable params object causes an infinite
`useEffect -> load -> render -> new object -> useEffect` loop). Build `useClaimsListFilters.ts` on this
EXACT shape with claim-relevant params: `status` (offen=pending/genehmigt=verified/abgelehnt=rejected —
map the German filter labels to the real `pending`/`verified`/`rejected` values, never invent a 4th
status), `claim_type` (today only ever `'claim'`, keep the filter present but do not over-build UI for a
single-value dimension — a simple pre-selected/disabled indicator is enough, not a full Select of fake
options), `fansub_group_id`, `app_user_id`, `from`/`to` date range.

From Plan 138-05 (already merged by this wave): `listChanges`... no — `listClaims(params: {status?, claim_type?, fansub_group_id?, app_user_id?, from?, to?, limit?, offset?}): Promise<{data: ClaimListRow[], meta: {total, limit, offset}}>` already exists in `frontend/src/lib/api.ts`; `ClaimListRow` type already exists in `frontend/src/types/admin-users.ts` with fields `claim_id, app_user_id, app_user_email, app_user_display_name, member_id, member_nickname, claim_status, claim_type, fansub_group_id, fansub_group_name, note, created_at, verified_at`.

UI-SPEC.md empty-state copy (locked, reuse verbatim): "Keine offenen Claims." / "Für diesen Filter liegen
aktuell keine zu entscheidenden Claims vor." — use this specifically when the `status=pending` filter is
active and returns zero rows; a generic `EmptyState` for other filter combinations returning zero rows.

D-02 bidirectional navigation (binding): every `app_user_display_name`/`member_nickname` cell and every
`fansub_group_name` cell must be a `Button variant="ghost"` (or an equivalently styled inline link using
`--accent-primary`) navigating to `/admin/users/${app_user_id}` and `/admin/fansubs/${fansub_group_id}/edit`
respectively — never inert text, per UI-SPEC Section A ("never a raw `<a>` or unstyled clickable `<div>`").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useClaimsListFilters hook</name>
  <files>frontend/src/app/admin/claims/useClaimsListFilters.ts</files>
  <read_first>frontend/src/app/admin/users/useUserListFilters.ts</read_first>
  <action>
Build `useClaimsListFilters.ts` mirroring `useUserListFilters.ts`'s exact structure: `useRouter`/
`usePathname`/`useSearchParams`, URL-synced `status`/`claim_type`/`fansub_group_id`/`app_user_id`/`from`/
`to` params (read from `searchParams`, written via `router.replace(..., {scroll: false})`, never
`router.push`), a stable `useMemo`'d `params` object returned alongside setter callbacks
(`handleStatusChange`, `handleGroupChange`, `handleDateRangeChange`, `handlePageChange`) and a default
`limit` (25, matching the backend's default clamp from Plan 138-05).
  </action>
  <acceptance_criteria>
    <criterion>grep -c "useMemo" frontend/src/app/admin/claims/useClaimsListFilters.ts is greater than or equal to 1</criterion>
    <criterion>grep -c "router.replace" frontend/src/app/admin/claims/useClaimsListFilters.ts is greater than or equal to 1</criterion>
    <criterion>docker compose exec team4sv30-frontend npx tsc --noEmit exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npx tsc --noEmit exits 0</automated>
  </verify>
  <done>A URL-synced, render-loop-safe filter hook exists for the Claims workspace.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /admin/claims route + ClaimsClient</name>
  <files>frontend/src/app/admin/claims/page.tsx, frontend/src/app/admin/claims/ClaimsClient.tsx, frontend/src/app/admin/claims/ClaimsClient.test.tsx</files>
  <read_first>frontend/src/app/admin/users/page.tsx, frontend/src/app/admin/users/AdminUsersClient.tsx</read_first>
  <behavior>
    - Given zero claims match status=pending, the empty state renders the exact locked "Keine offenen Claims." copy.
    - Given claims exist, each row renders clickable Button-based navigation to the user and the group, never plain text.
    - Changing the status filter updates the URL query string and re-fetches via listClaims with the new status param.
    - Pagination controls call listClaims with the correct offset for page 2.
  </behavior>
  <action>
Create `page.tsx` as a thin server component wrapping `ClaimsClient` in `PlatformAdminGate` (mirror
`frontend/src/app/admin/users/page.tsx`'s exact wrapper shape). Build `ClaimsClient.tsx` mirroring
`AdminUsersClient.tsx`'s state triad (`items`/`isLoading`/`error`/`total`) + `useCallback` load function +
`Pagination`+`Table` render, using `useClaimsListFilters` (Task 1) and `listClaims`. Table columns: Status
(`Badge`, variant mapped pending=warning/verified=success/rejected=danger), Benutzer (clickable), Gruppe
(clickable), Mitglied (`member_nickname`), Erstellt (`created_at`, formatted), Aktion (a placeholder
`Button variant="ghost"` "Details" navigating to nothing yet in THIS plan — Plan 138-14 replaces it with
the real verify/activate action buttons; do not render a dead/no-op button here, instead render nothing in
the Aktion column for now and add a code comment noting Plan 138-14 completes it, OR simply omit the
Aktion column entirely in this plan and let Plan 138-14 add it — prefer omitting the column entirely over
shipping a dead button, matching this codebase's "no placeholder UI" discipline).

D-32 (responsive, binding): below 760px, do not render the raw wide `Table` unmodified — collapse each row into a compact `Card`-based row (reuse whichever existing narrow-viewport card-row pattern already exists elsewhere in the admin area, e.g. the fansub group edit page's member/claim card rows; confirm the exact existing component at execution time per UI-SPEC Section D rather than inventing a new one). Never a horizontally-squeezed desktop matrix on a small screen.
  </action>
  <acceptance_criteria>
    <criterion>grep -c "Keine offenen Claims" frontend/src/app/admin/claims/ClaimsClient.tsx equals 1</criterion>
    <criterion>grep -c "PlatformAdminGate" frontend/src/app/admin/claims/page.tsx equals 1</criterion>
    <criterion>grep -c "variant=\"ghost\"" frontend/src/app/admin/claims/ClaimsClient.tsx is greater than or equal to 2 (user + group navigation cells)</criterion>
    <criterion>docker compose exec team4sv30-frontend npm test -- --run "ClaimsClient" exits 0 with all 4 behavior cases passing</criterion>
    <criterion>grep -c "759px\|760px" frontend/src/app/admin/claims/ClaimsClient.tsx is greater than or equal to 1 (D-32: narrow-viewport card-row collapse, not a squeezed table)</criterion>
    <criterion>docker compose exec team4sv30-frontend npx tsc --noEmit exits 0</criterion>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npm test -- --run "ClaimsClient" exits 0</automated>
  </verify>
  <done>/admin/claims is a real, filterable, paginated, bidirectionally-navigable Claims workspace consuming Plan 138-05's endpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A (frontend-only) | Consumes an already-authorized (platform-admin-gated) backend endpoint; this plan adds no new server surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-138-20 | Information Disclosure | ClaimsClient.tsx | accept | Page is wrapped in the existing `PlatformAdminGate` client-side gate AND the underlying endpoint is server-side platform-admin-gated (defense in depth already established elsewhere in this codebase) |
</threat_model>

<verification>
- `docker compose exec team4sv30-frontend npm test -- --run "ClaimsClient"` exits 0
- `docker compose exec team4sv30-frontend npx tsc --noEmit` exits 0
</verification>

<success_criteria>
/admin/claims lists claims across all groups with real status/type/group/user/date filters, clamped
pagination, and full bidirectional navigation to users and groups.
</success_criteria>

<output>
Create `.planning/phases/138-effective-rights-administration-impact-ux/138-10-SUMMARY.md` when done
</output>
