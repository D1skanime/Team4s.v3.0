---
phase: 120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo
plan: "02"
type: execute
wave: 2
depends_on: [120-01]
files_modified:
  - frontend/src/lib/api.test.ts
  - frontend/src/components/contributions/ReportModal.test.tsx
  - frontend/src/app/me/profile/page.test.tsx
  - frontend/src/app/fansubs/__tests__/publicPageWidthContract.test.ts
  - frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts
  - backend/internal/config/config_test.go
  - backend/internal/migrations/phase106_member_points_test.go
  - .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-02-SUMMARY.md
autonomous: true
requirements: [D-14]
must_haves:
  truths:
    - "The nine currently reproducible inherited frontend failures are corrected without changing application semantics."
    - "Backend full-suite tests are isolated from Compose SMTP defaults and resolve Phase-106 artifacts in both host and /app layouts."
    - "Clean isolated Compose typecheck and production build are green before any Phase-120 feature test or implementation begins."
  artifacts:
    - {path: frontend/src/components/contributions/ReportModal.test.tsx, provides: "client-portal-aware SSR test harness"}
    - {path: backend/internal/config/config_test.go, provides: "canonical-versus-legacy SMTP test isolation"}
    - {path: .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-02-SUMMARY.md, provides: "reproduction and complete baseline gate evidence"}
  key_links:
    - {from: 119-05-SUMMARY.md, to: 120-02-SUMMARY.md, via: "each recorded failure is reproduced and fixed or proven stale by a named green command"}
---
<objective>Stabilize the inherited project-wide test/typecheck/build baseline before Phase-120 feature work, correcting only exact 119-05 failures that still reproduce.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha/119-05-SUMMARY.md
@.planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-01-SUMMARY.md
@docker-compose.yml
@docker-compose.override.yml
@frontend/Dockerfile
@backend/Dockerfile.dev
@AGENTS.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Correct the nine reproducible inherited frontend tests</name>
  <files>frontend/src/lib/api.test.ts, frontend/src/components/contributions/ReportModal.test.tsx, frontend/src/app/me/profile/page.test.tsx, frontend/src/app/fansubs/__tests__/publicPageWidthContract.test.ts, frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts</files>
  <read_first>
    .planning/phases/119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha/119-05-SUMMARY.md
    frontend/src/lib/api.ts
    frontend/src/lib/api.test.ts
    frontend/src/components/contributions/ReportModal.tsx
    frontend/src/components/contributions/ReportModal.test.tsx
    frontend/src/app/me/profile/page.tsx
    frontend/src/app/me/profile/page.test.tsx
    frontend/src/app/fansubs/__tests__/publicPageWidthContract.test.ts
    frontend/src/app/admin/anime/create/useAdminAnimeCreateController.test.ts
  </read_first>
  <behavior>Runtime-host assertions derive from configured NEXT_PUBLIC_API_URL; five server-render portal cases use a deterministic test-only portal seam; retained background fetch asserts the resolved display URL; width contract matches the existing public shell; Jellyfin cover expectation uses configured runtime origin.</behavior>
  <action>First rerun the five-file command and record the exact current 9-failure matrix (1 resolveApiUrl, 5 ReportModal portal/server-render, 1 retained background fetch, 1 public width contract, 1 Jellyfin runtime URL). If any recorded failure is already green, record it as stale and do not change that case. For remaining failures, change only these test files: derive host-sensitive expected URLs from the same env/config fixture as runtime rather than hardcoded localhost; mock/normalize createPortal only in ReportModal's server-render tests while keeping the existing client submit test real; make the crop-fetch assertion use the resolved retained display URL; align the static width assertion with the already-shipped `width:min(100%, var(--public-page-max-width))` public shell instead of requiring a removed 769px literal. Do not change `api.ts`, ReportModal production behavior, profile crop behavior, public CSS, or anime controller semantics.</action>
  <verify><automated>docker compose exec -T team4sv30-frontend npm test -- --run src/lib/api.test.ts src/components/contributions/ReportModal.test.tsx src/app/me/profile/page.test.tsx src/app/fansubs/__tests__/publicPageWidthContract.test.ts src/app/admin/anime/create/useAdminAnimeCreateController.test.ts &amp;&amp; git diff --check</automated></verify>
  <done>All 64 selected tests are green with test/environment corrections only; stale cases have explicit green evidence and no invented edit.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Isolate the two backend full-suite failures</name>
  <files>backend/internal/config/config_test.go, backend/internal/migrations/phase106_member_points_test.go</files>
  <read_first>
    backend/internal/config/config.go
    backend/internal/config/config_test.go
    backend/internal/migrations/phase106_member_points_test.go
    backend/internal/testsupport/phase106_postgres.go
    docker-compose.yml
    docker-compose.override.yml
  </read_first>
  <behavior>Legacy SMTP fallback test clears canonical Compose vars before Load; migration-boundary test locates backend test-support files from runtime.Caller while migrations still resolve from repository/mounted root.</behavior>
  <action>Reproduce both named failures first. In `TestLoad_FallsBackToLegacySMTPNames`, explicitly set canonical `SMTP_FROM` and `SMTP_USER` empty before setting legacy vars so Compose defaults cannot invalidate the unit premise; preserve canonical precedence tests. In the migration test, separate backend-source-root resolution (`runtime.Caller` file, up from `internal/migrations` to the backend root) from repository/migration-root resolution, then use the backend root only for `internal/testsupport/phase106_postgres*.go`; preserve all Phase-106 boundary tokens and SQL paths. If either failure is stale, record green evidence and do not edit its file.</action>
  <verify><automated>docker compose exec -T team4sv30-backend go test ./internal/config ./internal/migrations -count=1 &amp;&amp; docker compose exec -T team4sv30-backend go test ./... -count=1 &amp;&amp; git diff --check</automated></verify>
  <done>Both inherited backend failures and the full backend suite are green in the current Compose layout.</done>
</task>
<task type="auto">
  <name>Task 3: Prove a clean isolated frontend baseline</name>
  <files>.planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-02-SUMMARY.md</files>
  <read_first>
    frontend/package.json
    frontend/tsconfig.json
    frontend/Dockerfile
    docker-compose.yml
    docker-compose.override.yml
    .planning/phases/119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha/119-05-SUMMARY.md
  </read_first>
  <action>Do not mutate host dependencies or delete the shared `frontend_next` volume. Reproduce ordinary running-container typecheck only to identify `.next/dev/types` drift, then run typecheck and build in fresh one-shot Compose containers with an anonymous `/app/.next`, `NODE_ENV=production`, no dependencies, and the current bind-mounted source/node_modules. The exact isolated build is the accepted deterministic correction for the recorded `/_not-found`/`/_global-error` failure; it must finish all 23 static pages. Run the full frontend suite after Task 1. Record every old 119-05 failure as `corrected` or `stale-green`, exact commands/statuses, and warnings separately. Do not modify package scripts, Next config, Dockerfile, application source or generated `.next` artifacts in this task.</action>
  <verify><automated>gate=0; docker compose exec -T team4sv30-frontend npm test -- --run || gate=1; docker compose run --rm --no-deps -T -e NODE_ENV=production -v /app/.next team4sv30-frontend npm run typecheck || gate=1; docker compose run --rm --no-deps -T -e NODE_ENV=production -v /app/.next team4sv30-frontend npm run build || gate=1; docker compose exec -T team4sv30-frontend npm run lint || gate=1; docker compose exec -T team4sv30-backend go test ./... -count=1 || gate=1; git diff --check || gate=1; test "$gate" -eq 0</automated></verify>
  <done>Full frontend/backend tests, isolated typecheck, isolated production build, lint and diff check are green before Plan 03.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Disposition | Mitigation Plan |
|---|---|---|---|
| T-120-01 | Tampering | mitigate | Plan 01 authorization remains prerequisite; this plan does not touch its overlap set. |
| T-120-02 | Information disclosure | accept | Test-only corrections; no visibility/runtime change. |
| T-120-03 | SSRF | accept | URL expectations only; no allowlist/runtime change. |
| T-120-04 | Elevation | accept | No auth/session behavior changes. |
| T-120-05 | Upload renderer | accept | No media pipeline changes. |
| T-120-06 | Availability | mitigate | Full suites and clean isolated production build block all feature work. |
</threat_model>
<verification>Every 119-05 failure is explicitly corrected or proven stale; all project-wide gates are green.</verification>
<success_criteria>Phase 120 starts feature work from a deterministic green baseline rather than conflicting with known failures.</success_criteria>
<source_coverage>GOAL stable prerequisite covered; REQ none; RESEARCH inherited baseline gate covered; CONTEXT D-14 covered; deferred none.</source_coverage>
<output>After completion create 120-02-SUMMARY.md with the exact failure disposition matrix.</output>
