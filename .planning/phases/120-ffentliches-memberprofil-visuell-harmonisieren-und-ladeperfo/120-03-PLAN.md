---
phase: 120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo
plan: "03"
type: execute
wave: 3
depends_on: [120-02, 120-01]
files_modified: ["frontend/src/app/members/[slug]/page.test.tsx", frontend/src/components/profile/MemberProfileHero.test.tsx, frontend/src/components/ui/ResponsiveImage.test.tsx]
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-15, D-17, D-18, D-19, D-20, D-21, D-22]
must_haves:
  truths:
    - "D-15/D-18: metadata and page SSR share one auth-keyed read while public, hidden and owner results remain isolated."
    - "D-01–D-05: Hero B preserves the display crop, full copy, local scrim and stable 100/120/140px avatar geometry."
    - "D-19–D-22: tests require prioritized hero/avatar, responsive sizes, cached WebP and one-shot display-original fallback without source_original_url."
  artifacts:
    - {path: "frontend/src/app/members/[slug]/page.test.tsx", provides: "dedupe/visibility/SSR contracts"}
    - {path: frontend/src/components/profile/MemberProfileHero.test.tsx, provides: "Hero B contracts"}
    - {path: frontend/src/components/ui/ResponsiveImage.test.tsx, provides: "optimizer fallback contract"}
  key_links:
    - {from: "frontend/src/app/members/[slug]/page.tsx", to: frontend/src/lib/api.ts#getMemberProfile, via: "slug+token React cache key"}
---
<objective>Create Wave-0 tests for auth-correct SSR, Hero B and responsive WebP delivery on the owner-authorized Phase-119 snapshot. Purpose: make leakage, duplicate reads, crop regressions and unsafe fallback fail first. Output: expected-red tests only.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-CONTEXT.md
@.planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-RESEARCH.md
@.planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-UI-SPEC.md
@.planning/sketches/004-memberprofil-hero/README.md
@.planning/phases/119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha/119-05-SUMMARY.md
@AGENTS.md
@docs/engineering/implementation-contract.md
@docs/frontend/auth-api-client.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Lock request dedupe, visibility and SSR semantics</name>
  <precondition><automated>docker compose run --rm --no-deps -T -v &quot;$PWD:/workspace&quot; -w /workspace team4sv30-frontend node .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/verify-overlap-chain.mjs check --authorization .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-01-PHASE119-AUTHORIZATION.json</automated>Run before any read/edit. Missing or mismatched authorization aborts; never refresh or auto-accept.</precondition>
  <files>frontend/src/app/members/[slug]/page.test.tsx</files>
  <read_first>
    .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-CONTEXT.md
    frontend/src/app/members/[slug]/page.tsx
    frontend/src/app/members/[slug]/page.test.tsx
    frontend/src/lib/api.ts
    backend/internal/handlers/app_public_profile.go
    docs/frontend/auth-api-client.md
  </read_first>
  <behavior>Same slug+token metadata/page calls dedupe; anonymous versus owner token does not; hidden/404/error stay exact; badge/profile copy exists in SSR.</behavior>
  <action>Before editing, run the existing route suite green and record it in the summary. Extend existing hoisted mocks and cookie fixture with exactly named tests `Phase 120 RED: deduplicates metadata and page reads for the same slug and viewer token` (`getMemberProfile` expected once, current implementation receives twice) and `Phase 120 RED: isolates anonymous and owner-preview cache keys` (distinct tokens must yield distinct calls/results). Assert both entry points normalize the same cookie. Hidden responses render only the existing preview and leak no name, badges, image URL or source_original_url. Preserve Phase-119 fixtures and the Plan-01 snapshot; no broad snapshots.</action>
  <verify><automated>docker compose exec -T team4sv30-frontend npm test -- --run 'src/app/members/[slug]/page.test.tsx' -t '^(?!Phase 120 RED:)' || exit 1; set +e; docker compose exec -T team4sv30-frontend npm test -- --run 'src/app/members/[slug]/page.test.tsx' -t 'Phase 120 RED: (deduplicates metadata and page reads for the same slug and viewer token|isolates anonymous and owner-preview cache keys)' &gt;/tmp/phase120-route-red.log 2&gt;&amp;1; red_status=$?; set -e; test "$red_status" -ne 0 &amp;&amp; grep -F 'Phase 120 RED: deduplicates metadata and page reads for the same slug and viewer token' /tmp/phase120-route-red.log &amp;&amp; grep -F 'Phase 120 RED: isolates anonymous and owner-preview cache keys' /tmp/phase120-route-red.log &amp;&amp; grep -E 'expected.*1.*received.*2|AssertionError' /tmp/phase120-route-red.log &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>The pre-existing route suite is green first; expected-red output names both new tests and the one-call-versus-two assertion rather than accepting an arbitrary non-zero exit.</acceptance_criteria>
  <done>Loader and visibility gaps are deterministic red tests.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Lock Hero B and responsive fallback</name>
  <precondition><automated>docker compose run --rm --no-deps -T -v &quot;$PWD:/workspace&quot; -w /workspace team4sv30-frontend node .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/verify-overlap-chain.mjs check --authorization .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-01-PHASE119-AUTHORIZATION.json</automated>Run before any read/edit. Missing or mismatched authorization aborts; never refresh or auto-accept.</precondition>
  <files>frontend/src/components/profile/MemberProfileHero.test.tsx, frontend/src/components/ui/ResponsiveImage.test.tsx</files>
  <read_first>
    .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-RESEARCH.md
    .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-PATTERNS.md
    .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-UI-SPEC.md
    .planning/phases/119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha/119-05-SUMMARY.md
    .planning/sketches/004-memberprofil-hero/index.html
    frontend/src/components/profile/MemberProfileHero.tsx
    frontend/src/components/profile/profile.module.css
    frontend/src/components/profile/MemberProfileHero.test.tsx
    frontend/src/app/anime/[id]/group/[groupId]/sections/LatestReleaseSection.tsx
    frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseDetailHero.tsx
    frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseGallery.tsx
    frontend/src/lib/api.ts
  </read_first>
  <behavior>Hero uses public_url, full bio/meta, local scrim and prioritized fixed geometry; responsive image retries the identical display original once without resizing.</behavior>
  <action>Before editing, run the existing Hero suite green. Add exactly named tests `Phase 120 RED: prioritizes both hero background and avatar with differentiated discovery` and `Phase 120 RED: falls back exactly once to display original without geometry change`. The first requires background eager plus `fetchPriority="high"` (or the single preload) and avatar eager/not-lazy with fixed 100/120/140 geometry—D-19 prioritizes both and permits differentiated preload/fetchpriority semantics. The second covers optimized first render, truthful sizes/dimensions, one error switch to the identical display URL unoptimized, no second loop and no source_original_url input. Preserve memorial/edit consumers and correct German umlauts.</action>
  <verify><automated>docker compose exec -T team4sv30-frontend npm test -- --run src/components/profile/MemberProfileHero.test.tsx -t '^(?!Phase 120 RED:)' || exit 1; set +e; docker compose exec -T team4sv30-frontend npm test -- --run src/components/profile/MemberProfileHero.test.tsx src/components/ui/ResponsiveImage.test.tsx -t 'Phase 120 RED: (prioritizes both hero background and avatar with differentiated discovery|falls back exactly once to display original without geometry change)' &gt;/tmp/phase120-image-red.log 2&gt;&amp;1; red_status=$?; set -e; test "$red_status" -ne 0 &amp;&amp; grep -F 'Phase 120 RED: prioritizes both hero background and avatar with differentiated discovery' /tmp/phase120-image-red.log &amp;&amp; grep -F 'Phase 120 RED: falls back exactly once to display original without geometry change' /tmp/phase120-image-red.log &amp;&amp; grep -E 'fetchpriority|loading|unoptimized|AssertionError' /tmp/phase120-image-red.log &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>The pre-existing Hero suite is green first; expected-red output names both new tests and a priority/loading/fallback assertion, rejecting arbitrary test crashes.</acceptance_criteria>
  <done>D-01–D-05 and D-19–D-22 image behavior is expected-red.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Disposition | Mitigation Plan |
|---|---|---|---|
| T-120-01 | Information Disclosure: hidden profile/cache | mitigate | Cache tests key slug+token and assert backend-owned hidden/owner separation. |
| T-120-03 | Information Disclosure: source original | mitigate | Negative DOM/network assertions prohibit source_original_url. |
| T-120-04 | SSRF: image optimizer | mitigate | Tests require narrow Team4s origins/paths, never wildcard proxy. |
| T-120-05 | Elevation: refresh-only actions | mitigate | Regress central hasAccessToken || hasRefreshToken action tests. |
| T-120-06 | Upload renderer | accept | No renderer selected; failed Next proof must stop/replan, not add encoder. |
| T-120-07 | Accessibility/DoS: deferred UI | mitigate | SSR content and fixed geometry asserted. |
</threat_model>
<verification>Expected-red suites fail only on Phase-120 behavior; current Phase-119 focused tests stay green.</verification>
<success_criteria>Wave 0 specifies D-01–D-05 and D-15/D-17–D-22 without production edits.</success_criteria>
<source_coverage>GOAL covered; REQ none mapped; RESEARCH cache/optimizer/fallback covered; CONTEXT D-01–D-05,D-15,D-17–D-22 covered; no deferred items.</source_coverage>
<output>After completion create 120-03-SUMMARY.md.</output>
