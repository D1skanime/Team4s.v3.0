---
phase: 120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo
plan: "05"
type: execute
wave: 4
depends_on: [120-02, 120-03]
files_modified: ["frontend/src/app/members/[slug]/page.tsx", "frontend/src/app/members/[slug]/page.test.tsx", frontend/Dockerfile, frontend/next.config.mjs, frontend/src/components/ui/ResponsiveImage.tsx, frontend/src/components/ui/ResponsiveImage.test.tsx, frontend/scripts/run-profile-image-probe.mjs, frontend/scripts/verify-profile-image-delivery.mjs, frontend/public/__phase120-image-probe/alpha-badge.png, frontend/test/fixtures/phase120-image-probe/avatar.png, frontend/test/fixtures/phase120-image-probe/hero.png, frontend/test/fixtures/phase120-image-probe/project-cover.png, frontend/test/fixtures/phase120-image-probe/group-logo.png]
autonomous: true
requirements: [D-15, D-18, D-19, D-20, D-21, D-22]
must_haves:
  truths:
    - "D-15/D-18: metadata and page share one request-scoped slug+token read without persistent caching."
    - "D-20–D-22: a blocking, repeatable probe proves actual RIFF/WEBP bytes, MIME, requested dimensions, alpha, cache reuse and stable one-shot original fallback across every required URL class."
    - "No image consumer migration or backend renderer/API work occurs unless the suitability probe passes completely."
  artifacts:
    - {path: frontend/src/components/ui/ResponsiveImage.tsx, provides: "optimized image with one-shot fallback"}
    - {path: frontend/next.config.mjs, provides: "narrow WebP widths/origin/path allowlist"}
    - {path: frontend/scripts/run-profile-image-probe.mjs, provides: "isolated Next and fixture-origin lifecycle on ports 3100/3101"}
    - {path: frontend/scripts/verify-profile-image-delivery.mjs, provides: "five-class image-byte, dimension, alpha, cache and fallback verifier"}
  key_links:
    - {from: generateMetadata and MemberProfilePage, to: getMemberProfile, via: "same cached loader and cookie normalization"}
---
<objective>Implement auth-keyed request dedupe and prove the frontend-only image architecture. Purpose: remove duplicate reads and deliver cached WebP without parallel media logic. Output: loader, narrow config and fallback primitive.</objective>
<scope_note>Plan 05 intentionally keeps the eleven-file image-probe slice atomic: fixtures, isolated lifecycle runner, byte-level verifier, narrow Next configuration, runner-image script copy and the one-shot fallback component form one fail-closed suitability gate. Splitting them would allow image consumers to depend on a partially runnable proof. Request deduplication remains a separate task and file pair inside the plan.</scope_note>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-03-SUMMARY.md
@.planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-PATTERNS.md
@.planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-RESEARCH.md
@.planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-UI-SPEC.md
@.planning/phases/119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha/119-CONTEXT.md
@.planning/phases/119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha/119-05-SUMMARY.md
@docs/api/api-contracts.md
@docs/frontend/auth-api-client.md
@frontend/src/app/media/[...path]/route.ts
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement request-scoped auth-keyed dedupe</name>
  <precondition><automated>docker compose run --rm --no-deps -T -v &quot;$PWD:/workspace&quot; -w /workspace team4sv30-frontend node .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/verify-overlap-chain.mjs check --authorization .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-01-PHASE119-AUTHORIZATION.json</automated>Run before any read/edit. Missing or mismatched authorization aborts; never refresh or auto-accept.</precondition>
  <files>frontend/src/app/members/[slug]/page.tsx, frontend/src/app/members/[slug]/page.test.tsx</files>
  <read_first>
    frontend/src/app/members/[slug]/page.tsx
    frontend/src/app/members/[slug]/page.test.tsx
    frontend/src/lib/api.ts
    frontend/src/types/profile.ts
    backend/internal/handlers/app_public_profile.go
  </read_first>
  <action>Add one module-level React cache wrapper over existing getMemberProfile with primitive slug and normalized token. Factor cookie reading so metadata/page call it identically. Keep cache:'no-store' and backend visibility authority; no unstable_cache, Map or tokenless key. Keep complete SSR content.</action>
  <verify><automated>docker compose exec -T team4sv30-frontend npm test -- --run 'src/app/members/[slug]/page.test.tsx' &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>One call for identical key, distinct calls for anonymous/owner; grep finds no unstable_cache or new Map.</acceptance_criteria>
  <done>Duplicate reads are removed without cross-viewer reuse.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Pass the blocking Next WebP suitability gate</name>
  <precondition><automated>docker compose run --rm --no-deps -T -v &quot;$PWD:/workspace&quot; -w /workspace team4sv30-frontend node .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/verify-overlap-chain.mjs check --authorization .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-01-PHASE119-AUTHORIZATION.json</automated>Run before any read/edit. Missing or mismatched authorization aborts; never refresh or auto-accept.</precondition>
  <files>frontend/Dockerfile, frontend/next.config.mjs, frontend/src/components/ui/ResponsiveImage.tsx, frontend/src/components/ui/ResponsiveImage.test.tsx, frontend/scripts/run-profile-image-probe.mjs, frontend/scripts/verify-profile-image-delivery.mjs, frontend/public/__phase120-image-probe/alpha-badge.png, frontend/test/fixtures/phase120-image-probe/avatar.png, frontend/test/fixtures/phase120-image-probe/hero.png, frontend/test/fixtures/phase120-image-probe/project-cover.png, frontend/test/fixtures/phase120-image-probe/group-logo.png</files>
  <read_first>
    .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-RESEARCH.md
    .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-PATTERNS.md
    .planning/phases/120-ffentliches-memberprofil-visuell-harmonisieren-und-ladeperfo/120-UI-SPEC.md
    .planning/phases/119-sammlungskarten-f-r-fortschritt-punkte-beitr-ge-mitgliedscha/119-05-SUMMARY.md
    frontend/next.config.mjs
    frontend/src/app/media/[...path]/route.ts
    frontend/src/app/anime/[id]/group/[groupId]/sections/LatestReleaseSection.tsx
    frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseDetailHero.tsx
    frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseGallery.tsx
    frontend/src/lib/api.ts
    backend/internal/services/media_service.go
    backend/internal/handlers/admin_content_release_version_media.go
    backend/internal/handlers/fansub_media_upload.go
    backend/internal/handlers/app_profile.go
    backend/internal/repository/member_profile_repository.go
    shared/contracts/openapi.yaml
  </read_first>
  <action>Create five deterministic repo-owned PNG fixtures at the declared paths: transparent alpha badge, profile avatar, wide profile hero, 2:3 project cover and square group logo. The alpha fixture is stored directly at the built public path `/__phase120-image-probe/alpha-badge.png`. Add Debian's `webp` package to the frontend runner image so `webpinfo`/`dwebp` are reproducible image-level verification tools before Playwright is introduced. In the Dockerfile runner stage add `COPY --from=builder /app/scripts ./scripts` after the existing config copies so both checked-in probe scripts exist in the built runtime image; do not rely on a host bind mount for scripts. `run-profile-image-probe.mjs` refuses unless `PHASE120_IMAGE_PROBE=1`, starts an in-process fixture origin on `127.0.0.1:3101`, and maps exactly `/api/v1/media/phase120-project-cover.png` and `/api/v1/media/phase120-group-logo.png`. It spawns the built Next server on `127.0.0.1:3100` with `API_INTERNAL_URL=http://127.0.0.1:3101`, waits for bounded health, invokes the verifier, then terminates both servers on success, failure or signal. The isolated Compose command mounts avatar/hero read-only at `/media/profile/phase120`, yielding exact source mappings `/media/profile/phase120/avatar.png` and `/media/profile/phase120/hero.png`; no live DB, backend or host media is read. The verifier requests `/_next/image` for all five exact source URLs and proves `image/webp`, RIFF at bytes 0–3, WEBP at 8–11, no HTML, parsed dimensions, exact 128/160/512/640 widths plus 256 avatar, 96/192 project, 64/128 group-logo and 640/1080/1480/1920 hero candidates. Run `webpinfo` for the alpha flag/chunk and `dwebp` to a temporary PAM, then assert transparent fixture pixels and edge samples remain transparent/clean; clean only verifier temp files. Repeat each URL and require byte-identical SHA-256 plus a Next cache-hit/age transition. Configure only exact origin/path allowlisting. Implement ResponsiveImage/tests so one forced optimizer error falls back exactly once to the identical display original, never `source_original_url`, without box/dimension changes and with stable second-error behavior. Any missing class, MIME, signature, dimension, alpha, cache, fallback or cleanup assertion aborts and replans; no backend encoder.</action>
  <verify><automated>docker compose exec -T team4sv30-frontend npm test -- --run src/components/ui/ResponsiveImage.test.tsx &amp;&amp; docker compose -f docker-compose.yml build team4sv30-frontend &amp;&amp; docker compose -f docker-compose.yml run --rm --no-deps -T team4sv30-frontend sh -lc 'test -f scripts/run-profile-image-probe.mjs &amp;&amp; test -f scripts/verify-profile-image-delivery.mjs' &amp;&amp; docker compose -f docker-compose.yml run --rm --no-deps -T -e NODE_ENV=production -e PHASE120_IMAGE_PROBE=1 -e API_INTERNAL_URL=http://127.0.0.1:3101 -v "$PWD/frontend/test/fixtures/phase120-image-probe/avatar.png:/media/profile/phase120/avatar.png:ro" -v "$PWD/frontend/test/fixtures/phase120-image-probe/hero.png:/media/profile/phase120/hero.png:ro" team4sv30-frontend node scripts/run-profile-image-probe.mjs --next-port 3100 --fixture-port 3101 --verifier scripts/verify-profile-image-delivery.mjs --require-url-classes static-badge,profile-avatar,profile-hero,api-project,api-group --require-widths 128,160,512,640 --require-alpha --require-cache-hit --require-original-fallback &amp;&amp; docker compose run --rm --no-deps -T -e NODE_ENV=production -v /app/.next team4sv30-frontend npm run typecheck &amp;&amp; ! grep -E 'hostname: *["'"']\*|pathname: *["'"']/\*\*' frontend/next.config.mjs &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>The built runner contains `scripts/run-profile-image-probe.mjs` and `scripts/verify-profile-image-delivery.mjs`; the command exits zero only with RIFF/WEBP signatures, image/webp MIME, parsed 128/160/512/640 dimensions, alpha preservation, all five URL classes, repeated-request cache evidence and exactly-one geometry-stable display-original fallback; no backend/OpenAPI/DTO/upload diff exists.</acceptance_criteria>
  <done>The complete Next suitability gate is green and downstream image migrations may begin; otherwise execution is stopped for replan.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Disposition | Mitigation Plan |
|---|---|---|---|
| T-120-01 | cache disclosure | mitigate | Primitive slug+token key and public/hidden/owner tests. |
| T-120-03 | source original disclosure | mitigate | Only display public_url reaches ResponsiveImage. |
| T-120-04 | optimizer SSRF | mitigate | Exact Team4s origin and path allowlist; no wildcard. |
| T-120-05 | refresh-only actions | mitigate | No browser auth change; final central-seam regression. |
| T-120-06 | upload renderer/decompression/cleanup | accept | Next-only read delivery; failed proof blocks backend work. |
| T-120-07 | image DoS/CLS | mitigate | Fixed geometry, bounded widths and cached optimizer. |
</threat_model>
<verification>Route/image suites, deterministic five-class byte-level optimizer probe, alpha/dimension/cache/fallback assertions, typecheck and diff check pass.</verification>
<success_criteria>D-15,D-18–D-22 foundation is green with no backend/API/media ownership change.</success_criteria>
<source_coverage>GOAL covered; REQ none; RESEARCH primary recommendation/proof gate covered; CONTEXT D-15,D-18–D-22 covered.</source_coverage>
<output>After completion create 120-05-SUMMARY.md.</output>
