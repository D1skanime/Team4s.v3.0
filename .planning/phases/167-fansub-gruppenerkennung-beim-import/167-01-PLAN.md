---
phase: 167-fansub-gruppenerkennung-beim-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/importutil/fansub_group.go
  - backend/internal/importutil/fansub_group_test.go
  - backend/internal/importutil/fansub_release_version.go
  - backend/internal/importutil/fansub_release_version_test.go
autonomous: true
requirements: [REQ-167-01, REQ-167-02, REQ-167-03, REQ-167-04, REQ-167-05, REQ-167-06, REQ-167-18]

must_haves:
  truths:
    - "For all 13 real filenames in 167-USER-REQUEST.md's measurement table, DeriveFansubGroupName returns exactly the value the Auftraggeber expects, including both previously-wrong cases (dmpd-mashle... -> \"dmpd\", Serie_01_[AEC71BC3].mkv -> \"\"); the new scene-schema pattern recognizes gruppe-titel.sXXeYY without regressing the four already-working schemas (D-07), and this is proven by real table-driven tests, not fakes (D-10)"
    - "A technical bracket (CRC checksum, resolution, codec/container, pure digits) is never returned as a group name, regardless of its position among multiple brackets in the same filename (D-06)"
    - "The parser evaluates only the filename; the full path is never concatenated into the search evidence unless the filename itself is empty (D-05)"
    - "A release filename ending in v2/v3/v4 (with or without a preceding checksum/bracket) is detected as a release version distinct from the default v1"
  artifacts:
    - path: "backend/internal/importutil/fansub_group.go"
      provides: "Hardened DeriveFansubGroupName with uniform technical-token denylist and new scene-schema prefix pattern"
    - path: "backend/internal/importutil/fansub_group_test.go"
      provides: "Table-driven test covering all 13 real filenames plus denylist/scene-schema edge cases"
      contains: "TestDeriveFansubGroupName"
    - path: "backend/internal/importutil/fansub_release_version.go"
      provides: "DeriveReleaseVersion(fileName string) (string, bool) for D-04 version detection"
    - path: "backend/internal/importutil/fansub_release_version_test.go"
      provides: "Table-driven test for version detection including the glued-to-bracket v4 case"
      contains: "TestDeriveReleaseVersion"
  key_links:
    - from: "backend/internal/handlers/admin_episode_import.go"
      to: "backend/internal/importutil/fansub_group.go"
      via: "existing call importutil.DeriveFansubGroupName(media.FileName, media.Path) at buildEpisodeImportPreview"
      pattern: "importutil\\.DeriveFansubGroupName"
---

<objective>
Harden the fansub-group-from-filename parser (`DeriveFansubGroupName`) so it evaluates the filename only
(D-05), never returns a technical token as a group name regardless of where it appears (D-06), recognizes the
scene-release `gruppe-titel.sXXeYY…` schema without regressing the four already-working schemas (D-07), and add
a sibling release-version detector for `v2`/`v3`/`v4` suffixes (D-04). Both functions get real table-driven
tests against the 13 real filenames from `167-USER-REQUEST.md` — this package currently has zero tests (D-10).

Purpose: this is the foundational correctness fix every downstream matching/UI plan in this phase depends on —
if the parser is wrong, batch matching (Plan 02) and the preview UI (Plan 05/07) inherit the wrong candidate
string.
Output: `backend/internal/importutil/fansub_group.go` rewritten in place (same public signature,
`DeriveFansubGroupName(fileName, fullPath string) string`, so both existing call sites keep compiling
unchanged), a new `DeriveReleaseVersion` function, and two new table-driven test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-fansub-gruppenerkennung-beim-import/167-USER-REQUEST.md
@.planning/phases/167-fansub-gruppenerkennung-beim-import/167-RESEARCH.md
</context>

<interfaces>
Current full file (verbatim, no tests exist today) — the baseline to harden in place, not replace wholesale:

```go
// Source: backend/internal/importutil/fansub_group.go (full file, current state)
var (
	bracketedFansubGroupPattern = regexp.MustCompile(`\[(?P<group>[^\[\]]+)\]`)
	suffixedFansubGroupPattern  = regexp.MustCompile(`(?i)(?:^|[._\-\s])s\d{1,2}e\d{1,4}(?:-\d{1,4})?[-._\s]+(?P<group>[^.]+)$`)
)

func DeriveFansubGroupName(fileName string, fullPath string) string {
	evidence := strings.TrimSpace(fileName + " " + fullPath)
	if match := bracketedFansubGroupPattern.FindStringSubmatch(evidence); len(match) >= 2 {
		return strings.TrimSpace(match[1])
	}
	baseName := strings.TrimSpace(fileName)
	if baseName == "" {
		normalizedPath := strings.ReplaceAll(strings.TrimSpace(fullPath), "\\", "/")
		if normalizedPath != "" { baseName = path.Base(normalizedPath) }
	}
	if baseName == "" { return "" }
	baseName = strings.TrimSpace(strings.TrimSuffix(baseName, filepath.Ext(baseName)))
	if baseName == "" { return "" }
	if match := suffixedFansubGroupPattern.FindStringSubmatch(baseName); len(match) >= 2 {
		return strings.TrimSpace(strings.Trim(match[1], "-._ "))
	}
	return ""
}
```

Both call sites (do not change these — they already treat the return value as a plain fallback string):
`backend/internal/handlers/admin_episode_import.go:438` and
`backend/internal/repository/episode_import_repository_release_helpers.go:427` (`deriveFansubGroupName` wrapper).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden DeriveFansubGroupName — filename-only, uniform denylist, new scene-schema pattern</name>
  <files>backend/internal/importutil/fansub_group.go, backend/internal/importutil/fansub_group_test.go</files>
  <read_first>
    backend/internal/importutil/fansub_group.go (current implementation, exact code above)
    .planning/phases/167-fansub-gruppenerkennung-beim-import/167-RESEARCH.md Section 1.1 and Section 2 (hand-traced regex behavior for all 13 filenames, both failure-case root causes, Pitfall 4 bracket-ordering risk)
    .planning/phases/167-fansub-gruppenerkennung-beim-import/167-USER-REQUEST.md (measurement table, denylist token list in §A)
    backend/internal/repository/anime_test.go lines 67-99 (`TestBuildAnimeListWhere_FansubFilter` — table-driven idiom to copy: `tests := []struct{ name string; ... }{...}` + `t.Run`)
  </read_first>
  <behavior>
    - Table test `TestDeriveFansubGroupName` with one `t.Run` sub-test per row, using the exact 13 filenames from
      `167-USER-REQUEST.md`'s measurement table as `fileName` input (fullPath left empty in every case, proving
      D-05's filename-only rule) and the expected group name as `want`:
      1. `[SHFS]07-Ghost_01_[H.264][1280x720][3390FBD1].mkv` -> `SHFS`
      2. `[GFE]Hand_Maid_May_01_[97D9369A].ogm` -> `GFE`
      3. `[FH-Subs]Needless EP01 720p (ger.sub).mp4` -> `FH-Subs`
      4. `[BDnP]NIGHT.HEAD.2041.S01E01[Web.1080p.AAC].mkv` -> `BDnP`
      5. `[BnP]NIGHT.HEAD.2041.S01E09[Web.1080p.AAC].mkv` -> `BnP`
      6. `[GK]No Game, No Life - 01(720p 10bit)[C281B950]v4.mkv` -> `GK`
      7. `[Pure-Ani-me] Macross Delta 01 Ger Sub.mkv` -> `Pure-Ani-me`
      8. `[L-S] Natsume Yuujinchou S1 - 02.x264 (1280x720 h264 AAC)[C4B217BC].1080P.mkv` -> `L-S`
      9. `Naruto Ger Sub 012.avi` -> `` (empty)
      10. `Naruto_026-027_ger_Sub_Uncut(1920).mkv` -> `` (empty, double-episode, no group)
      11. `Naruto.S01E01-AnimeOwnage.avi` -> `AnimeOwnage`
      12. `dmpd-mashle.magic.and.muscles.s01e17.german.dl.anime.1080p.web.h264.mkv` -> `dmpd` (was `""`, must now be correct)
      13. `Serie_01_[AEC71BC3].mkv` -> `` (empty; was `AEC71BC3`, must no longer be returned)
    - Additional edge-case sub-tests (not in the client's table, covering Pitfall 4 and D-05 explicitly):
      - Reordered checksum-before-group brackets, e.g. `[3390FBD1]Title[SHFS].mkv` -> `SHFS` (first NON-technical
        bracket wins, not literally the first bracket).
      - A filename with an empty `fileName` but a non-empty `fullPath` containing a real bracketed group in the
        last path segment -> the group is still found (path-as-fallback still works when filename is empty).
      - A filename with a real bracketed group AND a `fullPath` that also contains an unrelated bracket in a
        parent directory name -> the filename's own group wins and the path's bracket is never considered
        (proves D-05's "path only when filename empty" rule, not "path in addition to filename").
    - Run `cd backend && go test ./internal/importutil/... -run TestDeriveFansubGroupName -v` before editing
      `fansub_group.go` to confirm the new test fails against the current unhardened implementation (RED),
      then implement until it passes (GREEN).
  </behavior>
  <action>
    Rewrite `backend/internal/importutil/fansub_group.go` keeping the exported signature
    `DeriveFansubGroupName(fileName string, fullPath string) string` unchanged (both call sites must keep
    compiling without edits). Replace the internal logic with:
    1. Evidence selection (D-05 fix): if `strings.TrimSpace(fileName)` is non-empty, evidence is the filename
       alone — never concatenate `fullPath` into it. Only when the trimmed filename is empty, fall back to
       `path.Base` of the path-separator-normalized `fullPath` (keep the existing backslash-to-slash
       normalization and empty-string guards from the current implementation).
    2. Extension stripping: `strings.TrimSuffix(baseName, filepath.Ext(baseName))`, trimmed — unchanged from
       today.
    3. Bracket branch (uniform denylist, Pitfall 4 fix): use `bracketedFansubGroupPattern.FindAllStringSubmatch(baseName, -1)`
       (note: search the extension-stripped filename-only `baseName`, not the raw pre-extension-strip string,
       so a bracket that happens to include the extension can never match) and iterate the matches in order;
       return the trimmed content of the first match for which `isTechnicalGroupToken` (new helper, see below)
       returns `false`. If every bracket is technical or there are no brackets, fall through.
    4. New scene-schema branch (D-07): add `scenePrefixGroupPattern = regexp.MustCompile(`^(?P<group>[A-Za-z0-9]+)-`)`
       and `sceneEpisodeMarkerPattern = regexp.MustCompile(`(?i)s\d{1,2}e\d{1,4}`)`. Only attempt this branch when
       `sceneEpisodeMarkerPattern.MatchString(baseName)` is true (guards against unrelated hyphenated filenames
       like `Naruto_026-027...`, which has no scene marker and must stay empty). If it matches and
       `scenePrefixGroupPattern.FindStringSubmatch(baseName)` captures a group, return it after
       `isTechnicalGroupToken` clears it (fresh candidate, not reusing the bracket check's cached result).
    5. Existing suffix branch (`suffixedFansubGroupPattern`, unchanged regex) runs next, its captured group also
       gated through `isTechnicalGroupToken` before being returned.
    6. If nothing survives, return `""` (D-06's "guess nothing" rule).
    Add a new unexported helper `isTechnicalGroupToken(candidate string) bool` implementing the uniform denylist
    (applies identically to every bracket candidate, the scene-prefix candidate, and the suffix candidate):
    - Exact 8-character hex CRC: `^[0-9a-fA-F]{8}$`.
    - Pure digits (covers resolutions given as bare numbers and any year): `^\d+$`.
    - Resolution-with-p: case-insensitive `^\d{3,4}p$` (matches `1080p`, `720p`).
    - Resolution WxH: `^\d{3,4}x\d{3,4}$` (matches `1280x720`).
    - Compound codec/container match: lower-case the candidate with `.`, `_`, ` ` removed and compare against
      the exact set `{"h264","x264","x265","hevc","10bit","aac","flac","bd","web","webdl","bdrip","dvdrip"}`
      (this is what makes `H.264` and `Web.1080p.AAC`-style dotted tokens resolve to `h264`/`web...` correctly).
    - Exact whole-word language/source tags (case-insensitive, compared as the whole candidate, not
      sub-tokenized, so real group names containing "Sub"/"Subs" like `FH-Subs`/`Dragon-Subs` are never
      affected): `{"ger","gersub","german","dl","eng","subbed","dub","uncut"}`.
    - Multi-token bracket fallback: if the compound-stripped whole-candidate check above did not already match,
      split the candidate on `[._\s-]+` and require it to have at least one part; if EVERY resulting part
      individually satisfies one of the checks above (hex8 / pure-digits / resolution-p / resolution-WxH /
      exact codec token / exact language token), the whole candidate is technical. This is what correctly
      rejects `Web.1080p.AAC` (all three sub-tokens technical) while correctly ACCEPTING `FH-Subs` (`FH` is not
      technical, so "all parts technical" is false) and `Pure-Ani-me`/`L-S` (same reasoning).
    Do not touch `admin_episode_import.go` or `episode_import_repository_release_helpers.go` in this task — both
    existing call sites keep working unmodified because the signature is unchanged.
  </action>
    <verify>
    <automated>cd backend && go test ./internal/importutil/... -run TestDeriveFansubGroupName -v</automated>
  </verify>
  <acceptance_criteria>
    `cd backend && go test ./internal/importutil/... -run TestDeriveFansubGroupName -v` exits 0 and its output
    lists all 13 named sub-tests (one per real filename) plus the 3 additional edge-case sub-tests as PASS.
    `go vet ./internal/importutil/...` is clean. `grep -c "isTechnicalGroupToken" backend/internal/importutil/fansub_group.go`
    is at least 2 (definition + at least one call site) confirming the denylist is actually wired in, not just
    declared. No `os.ReadFile`/`strings.Contains`-on-own-source pattern anywhere in the new test file (this is a
    pure-function table test, the Teststil concern from CLAUDE.md does not apply to non-handler code, but the
    test must call `DeriveFansubGroupName` directly and assert on its return value, not on source text).
  </acceptance_criteria>
  <done>All 13 real filenames plus the 3 new edge cases pass; the two previously-wrong cases (`dmpd-mashle...`
  and `Serie_01_[AEC71BC3].mkv`) now match the Auftraggeber's expected values; none of the previously-correct 11
  cases regressed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Release-version detection (v2/v3/v4)</name>
  <files>backend/internal/importutil/fansub_release_version.go, backend/internal/importutil/fansub_release_version_test.go</files>
  <read_first>
    backend/internal/importutil/fansub_group.go (as rewritten by Task 1 — reuse the same extension-stripping idiom, same package, same file-header/import style)
    .planning/phases/167-fansub-gruppenerkennung-beim-import/167-RESEARCH.md Open Question 5 (the exact `[GK]No Game, No Life - 01(720p 10bit)[C281B950]v4.mkv` glued-to-bracket edge case, and the recommended `(?i)v([2-9])$` design)
  </read_first>
  <behavior>
    Table test `TestDeriveReleaseVersion` covering:
    - `[GK]No Game, No Life - 01(720p 10bit)[C281B950]v4.mkv` -> `("v4", true)` (v4 glued directly to `]`, zero
      separator, immediately before the extension — the real fixture this regex must handle without a
      preceding separator assumption).
    - A filename ending in `.v3.mkv` (dot-separated) -> `("v3", true)`.
    - A filename ending in `-v2.mkv` (hyphen-separated) -> `("v2", true)`.
    - `[SHFS]07-Ghost_01_[H.264][1280x720][3390FBD1].mkv` (no version suffix at all) -> `("", false)`.
    - A filename ending in `v1.mkv` -> `("", false)` (v1 is the implicit default per D-04, never surfaced as a
      "detected" version).
    - A filename containing `v4` in the MIDDLE of the name but not at the end (e.g. `Groupv4Release_01.mkv`) ->
      `("", false)` (must anchor to the true end of the extension-stripped name, not match anywhere).
  </behavior>
  <action>
    New file, same package (`importutil`). Export
    `func DeriveReleaseVersion(fileName string) (version string, detected bool)`. Strip the extension the same
    way `DeriveFansubGroupName` does (`strings.TrimSuffix(baseName, filepath.Ext(baseName))`, trimmed). Apply
    `releaseVersionPattern = regexp.MustCompile(`(?i)v([2-9])$`)` to the extension-stripped name (anchored to
    true end, no preceding-separator requirement so it matches both `...]v4` glued and `...-v2`/`...·v3`
    separated forms uniformly). If it matches, return the matched substring lower-cased (e.g. `"v4"`) and
    `true`; otherwise return `"", false` (this covers both "no version at all" and "v1", since `v1` is excluded
    by the `[2-9]` character class per D-04's "v2/v3/v4" wording).
  </action>
    <verify>
    <automated>cd backend && go test ./internal/importutil/... -run TestDeriveReleaseVersion -v</automated>
  </verify>
  <acceptance_criteria>
    `cd backend && go test ./internal/importutil/... -run TestDeriveReleaseVersion -v` exits 0, all 6 named
    sub-tests PASS, including the glued-to-bracket `v4` case verified against the real filename verbatim.
  </acceptance_criteria>
  <done>DeriveReleaseVersion correctly detects v2/v3/v4 in both glued and separated positions, never flags v1 or
  false-positive mid-string occurrences, and is exported for Plan 05's preview-time wiring to call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jellyfin filename -> Go regex parser | Filenames originate from files placed in the Jellyfin media library by admins/scripts, not directly from an untrusted network client, but are still attacker-influenceable if a malicious file is ever dropped into a watched folder |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-167-01 | Denial of Service | `bracketedFansubGroupPattern`, `scenePrefixGroupPattern`, `sceneEpisodeMarkerPattern`, `suffixedFansubGroupPattern`, `releaseVersionPattern` | mitigate | All five regexes use simple bounded quantifiers (`+`, `{1,2}`, `{1,4}`, `{3,4}`) over non-overlapping character classes with no nested unbounded groups (no `(.*)+`/`(a+)+`-style constructs) — no catastrophic-backtracking shape is introduced. Filenames are also bounded by real filesystem path-length limits (effectively <260 chars), keeping worst-case regex evaluation trivially fast regardless. |
</threat_model>

<verification>
`cd backend && go test ./internal/importutil/... -v` (full package, both new test files) exits 0.
`cd backend && go build ./...` succeeds (both existing call sites in `admin_episode_import.go` and
`episode_import_repository_release_helpers.go` still compile against the unchanged `DeriveFansubGroupName` signature).
</verification>

<success_criteria>
- All 13 real filenames from `167-USER-REQUEST.md` produce the Auftraggeber-expected result, with both
  previously-wrong cases now correct and zero regressions on the 11 previously-correct cases.
- Version detection correctly handles the glued-to-bracket `v4` real-world case.
- `backend/internal/importutil` has real, executable, table-driven tests where none existed before (D-10).
</success_criteria>

<output>
Create `.planning/phases/167-fansub-gruppenerkennung-beim-import/167-01-SUMMARY.md` when done.
</output>
