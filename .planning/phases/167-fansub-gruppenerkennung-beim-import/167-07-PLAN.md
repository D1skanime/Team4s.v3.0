---
phase: 167-fansub-gruppenerkennung-beim-import
plan: 07
type: execute
wave: 2
depends_on: ["167-04"]
files_modified:
  - frontend/src/app/admin/anime/[id]/episodes/import/FansubGroupOriginHint.tsx
  - frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportMappingRow.tsx
  - frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.ts
  - frontend/src/app/admin/anime/[id]/episodes/import/FansubGroupOriginHint.test.tsx
autonomous: true
requirements: [REQ-167-08, REQ-167-09, REQ-167-11, REQ-167-12, REQ-167-15, REQ-167-18, REQ-167-22]

must_haves:
  truths:
    - "A row with an exact server-side match shows 'Erkannt aus Dateiname: X -> Y (Alias/Name/Slug)' without requiring an extra confirm click"
    - "A row with no exact match but fuzzy suggestions shows up to 3 'Meinten Sie: {Gruppenname}?' buttons that add the group as a chip on click, never auto-applied"
    - "Manually picking a different group than the one the row's own kürzel already resolves to shows a visible conflict warning with an explicit, confirmed 'Trotzdem umhängen' action, never a silent switch"
    - "A detected release version shows an 'Aus Dateiname übernommen' hint that permanently disappears once the admin edits the field"
    - "Every new interactive element uses @/components/ui primitives; EpisodeImportMappingRow.tsx and page.tsx do not grow"
  artifacts:
    - path: "frontend/src/app/admin/anime/[id]/episodes/import/FansubGroupOriginHint.tsx"
      provides: "Origin hint, suggestion chips, and conflict warning/reassign UI for one mapping row"
    - path: "frontend/src/app/admin/anime/[id]/episodes/import/FansubGroupOriginHint.test.tsx"
      provides: "Vitest coverage of all 4 states (A/B/C/D) from 167-UI-SPEC.md"
  key_links:
    - from: "frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportMappingRow.tsx"
      to: "frontend/src/app/admin/anime/[id]/episodes/import/FansubGroupOriginHint.tsx"
      via: "<FansubGroupOriginHint row={row} selectedFansubGroups={selectedFansubGroups} onAddSelectedFansubGroup={...} fansubID={...} /> rendered below styles.groupSelector"
      pattern: "FansubGroupOriginHint"
    - from: "frontend/src/app/admin/anime/[id]/episodes/import/FansubGroupOriginHint.tsx"
      to: "frontend/src/lib/api.ts"
      via: "reassignFansubAlias(originGroupId, aliasId, { target_fansub_group_id: currentGroupId })"
      pattern: "reassignFansubAlias"
---

<objective>
Build the visible half of D-08/D-09's promise in the import-mapping row: an origin hint for exact matches, up
to 3 non-auto-applied "meinten Sie...?" suggestions for unmatched kürzel, and a conflict warning with an
explicit confirmed "Trotzdem umhängen" action when the admin's current selection disagrees with what the
kürzel already resolves to server-side (D-02). Also surface the "Aus Dateiname übernommen" hint for a detected
release version. Every new element goes into a brand-new file so neither `EpisodeImportMappingRow.tsx` (326
lines) nor `page.tsx` (597 lines, already over budget) grows for this phase's UI work — per 167-UI-SPEC.md's
explicit Design Boundary.

Purpose: makes Plan 05's server-computed matching visible and actionable per 167-UI-SPEC.md's approved contract.
Output: one new component file, two small additive edits to existing files, one new Vitest test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-fansub-gruppenerkennung-beim-import/167-UI-SPEC.md
</context>

<interfaces>
`EpisodeImportMappingRowCard`'s existing group-chip block (verified,
`frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportMappingRow.tsx:174-281`) — the exact insertion
points:

```tsx
            </div>
            <div className={styles.releaseMetaActions}>
              {/* <-- insert <FansubGroupOriginHint .../> HERE, between the closing </div> of
                   styles.groupSelector (line 243) and this releaseMetaActions div (line 244) */}
              <button ...>Episode</button>
              ...
          </label>
          <label className={styles.releaseMeta}>
            <span className={styles.releaseMetaLabel}>Version</span>
            <input
              className={styles.releaseMetaInput}
              value={row.release_version ?? ''}
              ...
              onChange={(event) => onSetRelease(sourceKey, { releaseVersion: event.target.value })}
            />
            {/* <-- insert the version hint <span> HERE, after the <input>, before this </label> */}
          </label>
```

`row.fansub_group_match_origin`/`row.fansub_group_suggestions`/`row.release_version_source` (Plan 04's TS
contract, Plan 05's Go population) — `fansub_group_match_origin` now carries `{ raw, matched_via, group_id,
group_name, alias_id? }` (patched during planning to include `group_id`/`alias_id` so this plan's conflict
detection and reassign action have everything they need without a second network round trip).

`FansubHandler`'s `EpisodeImportMappingRowCard` component already computes `selectedFansubGroups`
(`EpisodeImportSelectedFansubGroup[]`, derived from `row.fansub_groups ?? EMPTY_SELECTED_FANSUB_GROUPS`) and
`handleSelectExistingGroup` (adds a group as a chip) — reuse these exact patterns, do not duplicate the
chip-adding logic; call the existing `onAddSelectedFansubGroup` prop for both the suggestion-chip click and the
post-reassign chip update.

`ClaimManagementPanel.tsx`'s `useConfirmDialog` pattern (verified, referenced in `167-PATTERNS.md`) — the exact
confirm-dialog shape to copy for "Trotzdem umhängen":
```tsx
import { useConfirmDialog } from '@/components/ui'
const { confirm, confirmDialog } = useConfirmDialog()
async function handleReassign() {
  if (!(await confirm({ title: 'Alias umhängen?', confirmLabel: 'Trotzdem umhängen', tone: 'danger' }))) return
  ...
}
```
Remember to render `{confirmDialog}` once near this component's root.

`reassignFansubAlias` (Plan 04, `frontend/src/lib/api.ts`) — signature
`(fansubID: number, aliasID: number, payload: { target_fansub_group_id: number }, authToken?: string) =>
Promise<FansubAliasResponse>`. Here, `fansubID` is the alias's CURRENT/source group
(`row.fansub_group_match_origin.group_id`), `aliasID` is `row.fansub_group_match_origin.alias_id`, and
`target_fansub_group_id` is whichever group the admin currently has selected in this row.

`setMappingReleaseMeta` (verified, `episodeImportMapping.ts:182-195`, full current body) — the exact function to
extend for `release_version_source`:
```ts
export function setMappingReleaseMeta(
  rows: EpisodeImportMappingRow[],
  sourceKey: string,
  meta: { fansubGroupName?: string; releaseVersion?: string },
): EpisodeImportMappingRow[] {
  return rows.map((row) =>
    jellyfinSourceKey(row) === sourceKey
      ? {
          ...row,
          fansub_group_name:
            meta.fansubGroupName !== undefined ? (meta.fansubGroupName || null) : row.fansub_group_name,
          release_version:
            meta.releaseVersion !== undefined ? (meta.releaseVersion || null) : row.release_version,
        }
      : row,
  )
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FansubGroupOriginHint component</name>
  <files>frontend/src/app/admin/anime/[id]/episodes/import/FansubGroupOriginHint.tsx, frontend/src/app/admin/anime/[id]/episodes/import/FansubGroupOriginHint.test.tsx</files>
  <read_first>
    frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportMappingRow.tsx (full file — props shape, existing selectedFansubGroups derivation, styles import)
    frontend/src/types/episodeImport.ts (as extended by Plan 04 — EpisodeImportMappingRow's new fields)
    frontend/src/lib/api.ts (reassignFansubAlias, as added by Plan 04)
    .planning/phases/167-fansub-gruppenerkennung-beim-import/167-UI-SPEC.md "Screen- und Zustandskatalog" section 1 and "Copywriting Contract" (exact German strings, exact component variants/sizes)
  </read_first>
  <behavior>
    Vitest table-driven tests for `FansubGroupOriginHint`, one `describe`/`it` per UI-SPEC state:
    - **Zustand A** (exact match, no conflict): `row.fansub_group_match_origin` set, `selectedFansubGroups`
      contains exactly one entry whose `id === origin.group_id` -> renders the text "Erkannt aus Dateiname:
      {raw} → {group_name} (Alias)" (or "(Name)"/"(Slug)" depending on `matched_via`), no button, no badge.
    - **Zustand B** (no match, suggestions present): `row.fansub_group_match_origin` is `null`,
      `row.fansub_group_suggestions` has 2 entries -> renders exactly 2 buttons labeled "Meinten Sie:
      {Gruppenname}?", each calling `onAddSelectedFansubGroup` with that suggestion's `{id, name, slug}` on
      click.
    - **Zustand C** (conflict): `row.fansub_group_match_origin` set with `group_id: 5`, `selectedFansubGroups`
      contains one entry with `id: 9` (a DIFFERENT group than the origin) -> renders a warning badge "Kürzel
      „{raw}" gehört bereits zu {origin.group_name}." and a danger button "Trotzdem zu {name of id 9} umhängen"
      that, on click + confirm, calls `reassignFansubAlias(origin.group_id, origin.alias_id, {
      target_fansub_group_id: 9 })` and then calls `onAddSelectedFansubGroup`/an update callback so the parent
      row's origin reflects the new state (mock `reassignFansubAlias` in this test).
    - **Zustand D** (nothing): `fansub_group_match_origin` null, `fansub_group_suggestions` empty/undefined ->
      renders nothing (component returns `null`).
    - Conflict button is ONLY rendered when `origin.matched_via === 'alias'` and `origin.alias_id` is present
      (reassigning only makes sense for an alias-tier match — a name/slug-tier match has no alias row to move);
      add a sub-test asserting a `matched_via: 'name'` origin with a differing selection shows the warning text
      but NOT the reassign button (nothing sensible to reassign).
  </behavior>
  <action>
    Create `frontend/src/app/admin/anime/[id]/episodes/import/FansubGroupOriginHint.tsx`, a client component
    (`'use client'`) importing `Badge, Button, useConfirmDialog` from `@/components/ui` and `Sparkles` from
    `lucide-react`. Props: `row: EpisodeImportMappingRow`, `selectedFansubGroups:
    EpisodeImportSelectedFansubGroup[]`, `onAddSelectedFansubGroup: (fansubGroup:
    EpisodeImportSelectedFansubGroup) => void`, `sourceKey: string`, `label: string` (for aria-labels).
    Implementation:
    1. If `!row.fansub_group_match_origin && !(row.fansub_group_suggestions?.length)`, return `null` (Zustand D).
    2. Compute `currentGroupID = selectedFansubGroups[0]?.id ?? null` (this row only ever has one primary group
       selection in the conflict-detection sense per UI-SPEC; multiple simultaneous groups are an existing,
       unrelated multi-group feature this plan does not touch).
    3. If `row.fansub_group_match_origin` exists AND (`currentGroupID === null` OR `currentGroupID ===
       origin.group_id`): render Zustand A — a non-interactive `<span>` with an `aria-hidden` `<Sparkles
       size={12}/>` icon and the exact German text `Erkannt aus Dateiname: {origin.raw} → {origin.group_name}
       ({matchedViaLabel})` where `matchedViaLabel` maps `'alias'→'Alias'`, `'name'→'Name'`, `'slug'→'Slug'`.
       Style with a small inline style or a new CSS module class at 14px/400/`var(--text-secondary)` (create
       `FansubGroupOriginHint.module.css` if this repo's convention for new components is a colocated CSS
       module — check `EpisodeImportMappingRow.tsx`'s own `import styles from './page.module.css'` precedent;
       either reuse `page.module.css` additively or add a small new colocated module, whichever keeps this
       component self-contained).
    4. Else if `row.fansub_group_match_origin` exists AND `currentGroupID !== origin.group_id` (Zustand C):
       render `<Badge variant="warning">Kürzel „{origin.raw}" gehört bereits zu {origin.group_name}.</Badge>`.
       If `origin.matched_via === 'alias' && origin.alias_id`, also render `<Button variant="danger" size="sm"
       onClick={handleReassign}>Trotzdem zu {currentGroupName} umhängen</Button>` where `currentGroupName` is
       resolved from `selectedFansubGroups[0].name` (fallback to `#${id}` if name is absent, matching the
       existing group-chip label convention in `EpisodeImportMappingRow.tsx`). `handleReassign` uses
       `useConfirmDialog` with `{ title: 'Alias umhängen?', description: '„{origin.raw}" gehört aktuell zu
       {origin.group_name}. Nach dem Umhängen ist der Alias nur noch bei {currentGroupName} hinterlegt. Bereits
       importierte Releases bleiben unverändert.', confirmLabel: 'Trotzdem umhängen', tone: 'danger' }`
       (exact copy from 167-UI-SPEC.md's Copywriting Contract) before calling `reassignFansubAlias(origin.group_id,
       origin.alias_id, { target_fansub_group_id: currentGroupID! })`; on success, the component has no
       further server-side truth to refresh from within this row alone (the origin was computed once at
       preview time) — surface a brief inline confirmation state (e.g. a local `useState` boolean flipping the
       badge to a neutral "Umgehängt." text) rather than attempting to re-fetch the whole preview. Render
       `{confirmDialog}` once at this component's root.
    5. Else if `row.fansub_group_suggestions?.length` (Zustand B, only reached when there is no origin at all):
       render up to 3 `<Button variant="subtle" size="sm">Meinten Sie: {suggestion.name}?</Button>` in a flex
       row with `gap: var(--space-2)`, each `onClick={() => onAddSelectedFansubGroup({ id: suggestion.id, name:
       suggestion.name, slug: suggestion.slug })}`.
    Every interactive element in this file is a `Button`/`Badge` from `@/components/ui` — no native
    `<button>`/`<input>` anywhere in this new file (D-11, mandatory, no exception for this new code even though
    its sibling file has legacy native controls).
  </action>
    <verify>
    <automated>cd frontend && npx vitest run src/app/admin/anime/[id]/episodes/import/FansubGroupOriginHint.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    `cd frontend && npx vitest run src/app/admin/anime/\[id\]/episodes/import/FansubGroupOriginHint.test.tsx -v`
    (adjust the glob-escaping for this shell's `[id]` path segment as the existing test scripts in this repo
    already do) exits 0 with all documented sub-tests (A, B, C with reassign, C without reassign for
    name/slug-tier, D) passing. `grep -c "<button\|<input\|<select" frontend/src/app/admin/anime/\[id\]/episodes/import/FansubGroupOriginHint.tsx`
    is 0 (no native interactive elements).
  </acceptance_criteria>
  <done>FansubGroupOriginHint correctly renders all 4 UI-SPEC states, gates the reassign action to alias-tier
  conflicts only, and uses exclusively `@/components/ui` primitives.</done>
</task>

<task type="auto">
  <name>Task 2: Wire into EpisodeImportMappingRow + release-version-manual tracking</name>
  <files>frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportMappingRow.tsx, frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.ts</files>
  <read_first>
    frontend/src/app/admin/anime/[id]/episodes/import/EpisodeImportMappingRow.tsx lines 166-281 (exact JSX to insert into, shown in the interfaces section)
    frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.ts lines 182-196 (setMappingReleaseMeta, full current body shown in interfaces section)
  </read_first>
  <action>
    In `EpisodeImportMappingRow.tsx`, import `FansubGroupOriginHint` and insert
    `<FansubGroupOriginHint row={row} selectedFansubGroups={selectedFansubGroups}
    onAddSelectedFansubGroup={(g) => onAddSelectedFansubGroup(sourceKey, g)} sourceKey={sourceKey}
    label={label} />` immediately after the `</div>` that closes `styles.groupSelector` (the line right before
    `<div className={styles.releaseMetaActions}>`). Insert a version-hint `<span>` immediately after the
    existing release-version `<input>` and before that `<label>`'s closing tag: `{row.release_version_source ===
    'detected' ? <span className={styles.releaseMetaHint}>Aus Dateiname übernommen</span> : null}` — add a
    `releaseMetaHint` class to the existing `page.module.css` if this component's styles live there (check the
    file's `import styles from './page.module.css'` line), sized 12px/400/`var(--text-muted)` per the UI-SPEC.
    Do not touch any other line in this file.
    In `episodeImportMapping.ts`'s `setMappingReleaseMeta`, when `meta.releaseVersion !== undefined` (the admin
    edited the field — this is the ONLY call site for a manual edit, since `onSetRelease` is wired directly to
    the version `<input>`'s `onChange`), also set `release_version_source: 'manual'` on the returned row object
    (spread `...row` first, then override both `release_version` and `release_version_source` together) so the
    "Aus Dateiname übernommen" hint permanently disappears once the admin touches the field, per UI-SPEC's
    explicit "kein erneutes Erscheinen" rule.
  </action>
    <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    `cd frontend && npx tsc --noEmit` succeeds. `wc -l frontend/src/app/admin/anime/\[id\]/episodes/import/EpisodeImportMappingRow.tsx`
    reports at most 2 more lines than its pre-plan baseline of 326 (one import line + one JSX insertion line,
    plus the version-hint span — verify against the actual captured baseline in this plan's SUMMARY). `wc -l
    frontend/src/app/admin/anime/\[id\]/episodes/import/page.tsx` is unchanged from its pre-plan baseline of
    597 (this task does not touch `page.tsx` at all). `cd frontend && npx vitest run
    src/app/admin/anime/\[id\]/episodes/import/episodeImportMapping.test.ts` (if it exists) still passes with no
    regressions, plus a new/extended case asserting `setMappingReleaseMeta` sets `release_version_source:
    'manual'` when `releaseVersion` is provided.
  </acceptance_criteria>
  <done>FansubGroupOriginHint renders in the actual import mapping row; manually editing the version field
  permanently clears the "detected" hint for that row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser -> reassignFansubAlias | Frontend-initiated PATCH; the actual authorization boundary is enforced server-side by Plan 03's dual permission check, this component only surfaces the confirmation UX |

## STRIDE Threat Register

No new server-reachable threats — this plan is presentation-layer only, consuming already-validated
server-computed data (`fansub_group_match_origin`/`fansub_group_suggestions`) and delegating the one mutating
action (reassign) to Plan 03's already-threat-modeled endpoint. `T-167-IDOR`'s actual mitigation lives in Plan
03's handler; this component cannot bypass it (a rejected reassign surfaces the server's error, nothing more).
</threat_model>

<verification>
`cd frontend && npx tsc --noEmit` succeeds. `cd frontend && npx vitest run src/app/admin/anime/\[id\]/episodes/import/` exits 0.
</verification>

<success_criteria>
- All 4 UI-SPEC states render correctly and use only `@/components/ui` primitives.
- Reassign requires an explicit confirmed action, never a silent switch.
- Neither EpisodeImportMappingRow.tsx nor page.tsx grows beyond a couple of glue lines.
</success_criteria>

<output>
Create `.planning/phases/167-fansub-gruppenerkennung-beim-import/167-07-SUMMARY.md` when done.
</output>
