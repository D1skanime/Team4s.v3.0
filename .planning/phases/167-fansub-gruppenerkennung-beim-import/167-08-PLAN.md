---
phase: 167-fansub-gruppenerkennung-beim-import
plan: 08
type: execute
wave: 2
depends_on: ["167-04"]
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/FansubAliasSection.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/FansubDetailsTab.tsx
  - frontend/src/app/admin/changes/ChangeEntryTranslator.ts
  - frontend/src/app/admin/fansubs/[id]/edit/FansubAliasSection.test.tsx
  - frontend/src/app/admin/changes/ChangeEntryTranslator.test.ts
autonomous: true
requirements: [REQ-167-16, REQ-167-17, REQ-167-22]

must_haves:
  truths:
    - "An admin can view, create, reassign, and delete a fansub group's aliases from the group edit page without leaving it"
    - "Every alias mutation (create/delete/reassign/learned) is readable in German in the group's existing audit history tab, not just written to the raw audit log"
    - "Deleting or reassigning an alias requires an explicit confirmed action"
    - "The alias management section uses only @/components/ui primitives with correct German umlauts throughout, staying within the 450-line production-file budget (D-11)"
  artifacts:
    - path: "frontend/src/app/admin/fansubs/[id]/edit/FansubAliasSection.tsx"
      provides: "Alias list table + add-form + per-row reassign/delete, D-09's group-management UI"
    - path: "frontend/src/app/admin/changes/ChangeEntryTranslator.ts"
      provides: "German sentences for fansub_group_alias.created/.deleted/.reassigned/.learned"
  key_links:
    - from: "frontend/src/app/admin/fansubs/[id]/edit/FansubDetailsTab.tsx"
      to: "frontend/src/app/admin/fansubs/[id]/edit/FansubAliasSection.tsx"
      via: "<FansubAliasSection fansubID={fansubID} .../> rendered as a sibling of <FansubBasicInfoTab .../> inside the activeMainTab === 'basic' branch"
      pattern: "FansubAliasSection"
    - from: "frontend/src/app/admin/fansubs/[id]/edit/FansubAliasSection.tsx"
      to: "frontend/src/lib/api.ts"
      via: "getFansubAliases/createFansubAlias/deleteFansubAlias/reassignFansubAlias, getFansubList for the reassign target dropdown"
      pattern: "reassignFansubAlias"
---

<objective>
Give admins a real UI for D-09's alias lifecycle (create, reassign, delete), which today exists only as
unreachable/partially-reachable backend handlers. Render it as a new section inside the existing "Grunddaten"
tab of the fansub group edit page — a sibling of `FansubBasicInfoTab`, not a new top-level tab (per
167-UI-SPEC.md's Design-Entscheidung 8, since that file is already at 440/450 lines with no headroom). Also
close the loop on D-09's audit-visibility requirement: extend the existing generic `GroupChangesTab` translator
with real German sentences for the four alias event types, since otherwise they would render as an untranslated
raw fallback despite being correctly written to the audit log.

Purpose: this is the operator-facing management surface every other plan in this phase (auto-learn, reassign
endpoint, conflict UI) assumes exists.
Output: one new component file, one minimal sibling-render edit, four new translator cases, two new/extended
Vitest test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-fansub-gruppenerkennung-beim-import/167-UI-SPEC.md
</context>

<interfaces>
`FansubDetailsTab.tsx`'s exact sibling-render insertion point (verified, lines 94-109):

```tsx
{tabUsesLeftWorkspace ? (
  <div className={styles.fansubEditLeftColumn}>
    {activeMainTab === "basic" ? (
      <FansubBasicInfoTab
        styles={styles}
        details={details}
        fansubID={fansubID}
        group={group}
        capabilities={capabilities}
        isPlatformAdmin={isPlatformAdmin}
        hasAuthSession={hasAuthSession}
        onToast={onToast}
        communityLinksList={communityLinksList}
      />
    ) : null}
  </div>
) : null}
```
Insert `<FansubAliasSection fansubID={fansubID} isPlatformAdmin={isPlatformAdmin} hasAuthSession={hasAuthSession}
onToast={onToast} />` immediately after `<FansubBasicInfoTab .../>`, still inside the same
`activeMainTab === "basic"` conditional. Do not add it inside `FansubBasicInfoTab.tsx` (440 lines, ~10 lines of
headroom).

`GroupRolesTab.tsx`'s loading/error/empty/table shell (verified, lines 122-178) — the exact structural pattern
to copy, with one deviation UI-SPEC calls out explicitly: the "Neuer Alias" form must render even when the
table is empty (only the table portion branches on `EmptyState`, the form below it always renders once loading/
error are past).

`ClaimManagementPanel.tsx`'s `useConfirmDialog` usage (already quoted in Plan 07's interfaces section) — reuse
identically for both "Alias löschen?" and "Alias umhängen?" (`tone: 'danger'` for both, per UI-SPEC
Design-Entscheidung 11).

`FansubCommunityLinksList.tsx`'s add-row form shape (verified, referenced in `167-PATTERNS.md`):
```tsx
import { Button, FormField, Input, Select } from "@/components/ui";
<FormField label="Typ" htmlFor={id}>
  <Select id={id} value={...} onChange={...}>{options.map(...)}</Select>
</FormField>
<FormField label="Name" htmlFor={id}>
  <Input id={id} value={...} onChange={...} placeholder="..." />
</FormField>
```

`frontend/src/lib/api.ts` functions to use as-is (no changes needed here): `getFansubAliases(fansubID)`,
`createFansubAlias(fansubID, payload, authToken?)`, `deleteFansubAlias(fansubID, aliasID, authToken?)` (all
pre-existing), plus `reassignFansubAlias(fansubID, aliasID, payload, authToken?)` (Plan 04, new). Also
`getFansubList({ per_page: 100 })` (pre-existing, used elsewhere in this directory) for the reassign target
dropdown's group list — per UI-SPEC Design-Entscheidung 10, a plain `Select` loaded once, no async search.

`ChangeEntryTranslator.ts`'s exact switch-case shape and `payloadString` helper (verified, full relevant excerpt
already read):
```ts
function payloadString(payload: Record<string, unknown> | null | undefined, key: string): string | undefined {
  const value = payload?.[key]
  return typeof value === 'string' ? value : undefined
}
...
switch (entry.event_type) {
  case 'role_capability.granted':
  ...
  case 'member_claim.verified':
    return { sentence: `Admin hat einen Mitgliedschaftsanspruch bestätigt (Claim #${entry.target_id ?? '?'}).` }
  default: { ... }
}
```
The real audit payloads this plan's new cases must translate (verified/designed across Plans 03/06):
`fansub_group_alias.created` -> `Payload{"alias": string}`; `.deleted` -> `Payload{}` (no alias text available,
only `entry.target_id`); `.reassigned` -> `Payload{"alias": string, "from_group_id": number, "to_group_id":
number}`; `.learned` -> `Payload{"alias": string, "source": "episode_import"}`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FansubAliasSection component</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/FansubAliasSection.tsx, frontend/src/app/admin/fansubs/[id]/edit/FansubAliasSection.test.tsx</files>
  <read_first>
    frontend/src/app/admin/fansubs/[id]/edit/GroupRolesTab.tsx (full loading/error/empty/table shell to copy)
    frontend/src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.tsx (useConfirmDialog usage)
    frontend/src/app/admin/fansubs/[id]/edit/FansubCommunityLinksList.tsx (add-row FormField+Input+Select shape)
    frontend/src/lib/api.ts (getFansubAliases/createFansubAlias/deleteFansubAlias/reassignFansubAlias/getFansubList signatures)
    frontend/src/types/fansub.ts (FansubAlias, FansubAliasCreateRequest, FansubAliasReassignRequest, FansubGroup)
    .planning/phases/167-fansub-gruppenerkennung-beim-import/167-UI-SPEC.md "Screen- und Zustandskatalog" section 2 and "Copywriting Contract" (exact German strings)
  </read_first>
  <behavior>
    Vitest tests for `FansubAliasSection`, mocking the `@/lib/api` module:
    - Initial load shows `LoadingState`, then renders the alias table once `getFansubAliases` resolves.
    - Empty alias list renders `EmptyState` for the table portion while the "Neuer Alias" `FormField`/`Input`/
      `Button` still render below it (per UI-SPEC's explicit deviation from `GroupRolesTab`'s full-replace).
    - `getFansubAliases` rejecting renders `ErrorState` with the exact copy "Aliase konnten nicht geladen
      werden".
    - Submitting the add-form calls `createFansubAlias` with the typed alias text; on success the list reloads
      (assert `getFansubAliases` called a second time); on a conflict error the `FormField` shows the inline
      message "Dieses Kürzel gehört bereits zu einer anderen Gruppe." (no toast, no dialog).
    - Clicking "Löschen" for a row opens the confirm dialog with title "Alias löschen?"; confirming calls
      `deleteFansubAlias` and reloads the list; cancelling calls neither.
    - Selecting a different target group and clicking "Umhängen" opens the confirm dialog with title "Alias
      umhängen?"; confirming calls `reassignFansubAlias` with the selected `target_fansub_group_id` and reloads
      the list; the "Umhängen" button is `disabled` while the selected target equals the alias's current group.
  </behavior>
  <action>
    Create `frontend/src/app/admin/fansubs/[id]/edit/FansubAliasSection.tsx`, a client component importing
    `Badge, Button, Card, EmptyState, ErrorState, FormField, Input, LoadingState, Select, Table, TableBody,
    TableCell, TableHead, TableHeaderCell, TableRow, useConfirmDialog` from `@/components/ui`, `Plus, Trash2`
    from `lucide-react`. Props: `fansubID: number`, `isPlatformAdmin: boolean`, `hasAuthSession: boolean`,
    `onToast: (message: string) => void`. State: `aliases: FansubAlias[] | null`, `groups: FansubGroup[]`
    (loaded once via `getFansubList({ per_page: 100 })` for the reassign dropdown options, excluding the
    current `fansubID` from the option list per-row), `loading`, `loadError`, `newAliasText`, `newAliasError`,
    per-row `reassignTargetByAliasID: Record<number, number>`. On mount, load both aliases and the group list.
    Render, in order: `Card variant="section"` containing Heading "Aliase" (18/700); then `LoadingState`
    (title "Aliase werden geladen", description "Team4s lädt die hinterlegten Kürzel dieser Fansubgruppe.") while
    `loading`; else `ErrorState` (title "Aliase konnten nicht geladen werden", description the server message or
    "Bitte später erneut versuchen.") if `loadError`; else the `Table` (headers "Alias" | "Erstellt am" |
    "Aktionen", or `EmptyState` title "Noch keine Aliase hinterlegt" / description "Aliase werden automatisch
    ergänzt, sobald beim Import ein unbekanntes Kürzel dieser Gruppe zugeordnet wird. Ein Alias kann hier auch
    manuell angelegt werden." when `aliases.length === 0`) — the table/empty-state branch is the ONLY thing that
    varies on empty; below it, unconditionally (once loading/error are past), render the "Neuer Alias"
    `FormField` (label "Neuer Alias", `Input` placeholder "z. B. BDnP" maxLength 120, `Button variant="primary"
    size="sm" leftIcon={<Plus size={14}/>}` "Alias hinzufügen"). Each table row: alias text, formatted
    `created_at`, then a `Select` (`aria-label="Neue Gruppe für Alias {alias}"`, options = `groups` filtered to
    exclude `fansub_group_id === fansubID`) + `Button variant="secondary" size="sm" disabled={selectedTarget ===
    row.fansub_group_id || !selectedTarget}` "Umhängen" + `Button variant="danger" size="sm" iconOnly
    leftIcon={<Trash2 size={14}/>} aria-label="Alias {alias} löschen"`. Wire delete/reassign through
    `useConfirmDialog` exactly per the Copywriting Contract quoted in this plan's interfaces section (title,
    description with the real alias/group names substituted, confirmLabel, `tone: 'danger'`); render
    `{confirmDialog}` once at the component root. After any successful create/delete/reassign, re-call
    `getFansubAliases` (no optimistic update, per UI-SPEC). All disabled states respect `!hasAuthSession`
    (mirror the disabled-when-no-session pattern already used elsewhere in this directory, e.g.
    `FansubCommunityLinksList.tsx`).
  </action>
    <verify>
    <automated>cd frontend && npx vitest run src/app/admin/fansubs/[id]/edit/FansubAliasSection.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    `cd frontend && npx vitest run "src/app/admin/fansubs/\[id\]/edit/FansubAliasSection.test.tsx" -v` exits 0
    with all 6+ documented cases passing. `grep -c "<button\|<input\|<select"
    "frontend/src/app/admin/fansubs/[id]/edit/FansubAliasSection.tsx"` is 0 (primitives only).
  </acceptance_criteria>
  <done>FansubAliasSection provides full create/reassign/delete alias CRUD with confirmed destructive actions
  and an empty-state that still allows manual creation.</done>
</task>

<task type="auto">
  <name>Task 2: Wire into FansubDetailsTab + audit sentence translation</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/FansubDetailsTab.tsx, frontend/src/app/admin/changes/ChangeEntryTranslator.ts, frontend/src/app/admin/changes/ChangeEntryTranslator.test.ts</files>
  <read_first>
    frontend/src/app/admin/fansubs/[id]/edit/FansubDetailsTab.tsx lines 82-109 (exact insertion point, shown in interfaces)
    frontend/src/app/admin/changes/ChangeEntryTranslator.ts (full file — switch structure, payloadString helper, default fallback)
    frontend/src/app/admin/changes/ChangeEntryTranslator.test.ts (existing test file style, to extend consistently)
  </read_first>
  <action>
    In `FansubDetailsTab.tsx`, add `import { FansubAliasSection } from "./FansubAliasSection"` and insert
    `<FansubAliasSection fansubID={fansubID} isPlatformAdmin={isPlatformAdmin} hasAuthSession={hasAuthSession}
    onToast={onToast} />` immediately after `<FansubBasicInfoTab .../>`, inside the same `activeMainTab ===
    "basic"` block. Do not modify anything else in this file.
    In `ChangeEntryTranslator.ts`, add a small `payloadNumber(payload, key)` helper mirroring `payloadString`'s
    shape (returns `number | undefined`, checking `typeof value === 'number'`), then add four new `case` blocks
    immediately after the existing `member_claim.activated` case, before `default`:
    - `case 'fansub_group_alias.created':` -> `{ sentence: \`Admin hat den Alias "${payloadString(entry.payload,
      'alias') ?? 'unbekannt'}" angelegt.\` }`.
    - `case 'fansub_group_alias.deleted':` -> `{ sentence: \`Admin hat den Alias #${entry.target_id ?? '?'}
      entfernt.\` }` (no alias text is available in this event's payload — stay honest, use the id, per this
      file's own "niemals raten" discipline documented at the top of the file).
    - `case 'fansub_group_alias.reassigned':` -> `{ sentence: \`Admin hat den Alias "${payloadString(entry.payload,
      'alias') ?? 'unbekannt'}" von Gruppe #${payloadNumber(entry.payload, 'from_group_id') ?? '?'} zu Gruppe
      #${payloadNumber(entry.payload, 'to_group_id') ?? '?'} umgehängt.\` }`.
    - `case 'fansub_group_alias.learned':` -> `{ sentence: \`Beim Import wurde der Alias "${payloadString(entry.payload,
      'alias') ?? 'unbekannt'}" automatisch als neuer Alias gelernt.\` }` (phrased as a system action, not
      "Admin hat...", since this event fires from the import-apply code path, not a direct admin alias-edit
      action — keeps the sentence honest about who/what actually triggered it).
  </action>
    <verify>
    <automated>cd frontend && npx vitest run src/app/admin/changes/ChangeEntryTranslator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `cd frontend && npx tsc --noEmit` succeeds. `cd frontend && npx vitest run
    src/app/admin/changes/ChangeEntryTranslator.test.ts -v` exits 0, including new cases asserting each of the
    four event types produces the exact sentence text specified above for a representative payload.
    `wc -l "frontend/src/app/admin/fansubs/[id]/edit/FansubBasicInfoTab.tsx"` is unchanged from its pre-plan
    baseline of 440 (this task never touches that file).
  </acceptance_criteria>
  <done>All four fansub_group_alias.* event types render real German sentences in GroupChangesTab instead of
  falling through to the generic fallback; FansubBasicInfoTab.tsx is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser -> FansubAliasSection mutations | All three mutating actions (create/delete/reassign) are frontend-initiated calls to already-permission-checked backend endpoints (existing `CreateFansubAlias`/`DeleteFansubAlias`, Plan 03's `ReassignFansubAlias`) — this component enforces no authorization itself, it only reflects `hasAuthSession`/`isPlatformAdmin` for UX disabling, matching the existing sibling components' convention in this directory |

## STRIDE Threat Register

No new server-reachable threats — presentation layer only, delegating every mutation to already-threat-modeled
backend endpoints (Plan 03's dual-permission reassign, the existing audited create/delete handlers).
</threat_model>

<verification>
`cd frontend && npx tsc --noEmit` succeeds. `cd frontend && npx vitest run "src/app/admin/fansubs/\[id\]/edit/" src/app/admin/changes/ChangeEntryTranslator.test.ts` exits 0.
</verification>

<success_criteria>
- Admins can fully manage a group's aliases from the existing group edit page.
- All four alias event types are legible in German in the existing audit history UI.
</success_criteria>

<output>
Create `.planning/phases/167-fansub-gruppenerkennung-beim-import/167-08-SUMMARY.md` when done.
</output>
