---
phase: 167-fansub-gruppenerkennung-beim-import
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/models/fansub_group_match.go
  - backend/internal/repository/fansub_group_match.go
  - backend/internal/repository/episode_import_repository.go
  - backend/internal/testsupport/phase167_postgres.go
  - backend/internal/repository/phase167_fansub_match_test.go
autonomous: true
requirements: [REQ-167-07, REQ-167-08, REQ-167-11, REQ-167-19, REQ-167-20]

must_haves:
  truths:
    - "A batch of N filename-derived candidate strings is resolved against fansub_groups/fansub_group_aliases in exactly one SQL query regardless of N (D-08)"
    - "An exact alias/name/slug match is distinguished from a fuzzy 'did you mean' suggestion; only exact matches are eligible for auto-selection, fuzzy ones are never auto-applied (D-03)"
    - "The exact-match query byte-matches the production functional-index expression regexp_replace(lower(f_unaccent(col)), '[^a-z0-9]+', '', 'g') so it stays index-friendly (Pitfall 3)"
    - "Alias/name/slug matching is case-insensitive and separator-insensitive (space/hyphen/underscore/dot variants of the same group all resolve to the same group)"
    - "The alias write-path and its uniqueness rule are proven against a real, isolated Postgres schema (phase167_postgres.go), not a fake-only substitute (D-10)"
  artifacts:
    - path: "backend/internal/models/fansub_group_match.go"
      provides: "FansubGroupMatch, FansubGroupSuggestion result types shared by repository and handler layers"
      contains: "type FansubGroupMatch struct"
    - path: "backend/internal/repository/fansub_group_match.go"
      provides: "Batch exact-match query (= ANY($1::text[]) + unnest) and trigram suggestion query, adapted from buildSearchFansubQuery"
      contains: "= ANY($1::text[])"
    - path: "backend/internal/testsupport/phase167_postgres.go"
      provides: "OpenPhase167Postgres fixture with minimal fansub_groups/fansub_group_aliases tables and a schema-local f_unaccent wrapper"
      exports: ["OpenPhase167Postgres"]
    - path: "backend/internal/repository/phase167_fansub_match_test.go"
      provides: "Unit SQL-string-assertion tests plus real-DB integration tests (exact-match tiers, query-budget)"
  key_links:
    - from: "backend/internal/repository/episode_import_repository.go"
      to: "backend/internal/repository/fansub_group_match.go"
      via: "EpisodeImportRepository.ResolveFansubGroupMatches method delegating to resolveFansubGroupMatches(ctx, r.db, candidates)"
      pattern: "func \\(r \\*EpisodeImportRepository\\) ResolveFansubGroupMatches"
---

<objective>
Build the single-query batched matcher that resolves N filename-derived candidate group tokens against
`fansub_groups.name`/`.slug`/`fansub_group_aliases.normalized_alias` in one round trip (D-08), plus a separate,
smaller trigram "did you mean" suggestion query (D-03) — both adapted from the existing single-term
`buildSearchFansubQuery` in `search_fansub.go` rather than invented from scratch. Also stand up the isolated
`phase167` Postgres test fixture (D-10's real-database requirement) that Plan 06's alias write-path tests will
reuse.

Purpose: this is the data-layer half of the phase's core value ("Kürzel wird automatisch der Gruppe zugeordnet")
— Plan 05 wires its output into the preview response; Plan 06 wires the "no auto-create" gating around it.
Output: two new repository-layer files, one new model file, one new testsupport fixture, one new test file with
both unit (SQL-string) and real-Postgres integration coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-fansub-gruppenerkennung-beim-import/167-RESEARCH.md
</context>

<interfaces>
Closest analog, adapt (not reinvent) the WHERE-expression — `backend/internal/repository/search_fansub.go`
(full file already read in RESEARCH.md; the exact expression to mirror byte-for-byte per column):

```
regexp_replace(lower(f_unaccent(<col>)), '[^a-z0-9]+', '', 'g')
```

`buildSearchFansubOrder`'s rank tiers (0=alias/name-normalized exact, 1=exact name, 2=exact slug, 3=alias
trigram, 4=name trigram, 5=rest) — ranks 0-2 map to `FansubGroupMatch` (exact, auto-selectable per D-08); ranks
3-4 map to `FansubGroupSuggestion` (fuzzy, suggestion-only per D-03, never auto-applied).

`backend/internal/repository/query_counter.go` (full file, already read) — reuse `queryCounter` directly for the
D-08/D-20 constant-query-budget proof; do not write a new query logger.

`backend/internal/testsupport/phase106_postgres.go` — exposes the shared `openPhasePostgres(t, dsnEnv,
databasePattern, schemaPrefix, schemaPattern, createPrerequisites)` helper that every phaseNNN fixture (135,
150, 165, ...) already calls. **Critical, phase-167-specific gotcha discovered during planning:** this shared
helper sets `search_path` to the new isolated schema ONLY and hard-fails
(`t.Fatalf("unsafe search_path schemas: %v", schemas)`) if `public` is present in it. Production's
`f_unaccent`/`unaccent`/`pg_trgm` objects live in the `public` schema (migrations `0140_search_foundation.up.sql`,
`0152_f_unaccent_search_path_fix.up.sql`), so a bare `f_unaccent(...)` call issued through the isolated Phase-167
pool will fail to resolve — `public` is never in that pool's search_path by design. `createPhase167Prerequisites`
must therefore also create a **schema-local** `f_unaccent` wrapper function (see Task 1) so the isolated fixture
can exercise the exact-match query without needing `public` in its search_path; production code keeps calling
bare `f_unaccent(...)` unqualified (production's default search_path already includes `public`, and the
functional index expression must stay byte-identical to `0140_search_foundation.up.sql`'s).

`backend/internal/repository/fansub_repository.go:1727-1735`, `normalizeAliasKey` (existing, same package,
reuse for the Go-side literal used only when generating the `$2`-style normalized bind parameter for
`fansub_group_aliases.normalized_alias` equality — this column is genuinely ASCII-stripped-only, unlike
`fansub_groups.name`/`.slug` which must go through SQL-side `f_unaccent`, per RESEARCH.md Pitfall 5/Open
Question 2 — do not write a third normalization function).

Live DB inventory (read-only, verified): `fansub_groups` has 10 rows, `fansub_group_aliases` has 1 row
(`alias='NS'` -> New-Subs). Test fixtures should not assume any particular row count exists in production;
integration tests seed their own rows into the isolated schema.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Phase-167 isolated Postgres fixture</name>
  <files>backend/internal/testsupport/phase167_postgres.go</files>
  <read_first>
    backend/internal/testsupport/phase165_postgres.go (structural template to mirror, full file)
    backend/internal/testsupport/phase106_postgres.go (shared openPhasePostgres helper + the public-schema search_path guard described above)
    database/migrations/0009_fansub_groups.up.sql (real fansub_groups columns/constraints to mirror)
    database/migrations/0014_fansub_group_aliases.up.sql (real fansub_group_aliases columns/constraints to mirror)
    database/migrations/0152_f_unaccent_search_path_fix.up.sql (the real f_unaccent wrapper body to mirror locally)
  </read_first>
  <action>
    Create `backend/internal/testsupport/phase167_postgres.go`, package `testsupport`, mirroring
    `phase165_postgres.go`'s structure exactly with every `165` renamed to `167`: DSN env constant
    `phase167DSNEnv = "TEAM4S_PHASE167_TEST_DSN"`, `phase167DatabasePattern =
    regexp.MustCompile(`^team4s_phase167_test_[a-z0-9]+$`)`, `phase167SchemaPattern =
    regexp.MustCompile(`^phase167_[a-z0-9_]+$`)`, exported `OpenPhase167Postgres(t *testing.T) *pgxpool.Pool`
    calling `openPhasePostgres(t, phase167DSNEnv, phase167DatabasePattern, "phase167_", phase167SchemaPattern,
    createPhase167Prerequisites)`. `createPhase167Prerequisites(t testing.TB, pool *pgxpool.Pool)` executes one
    SQL block that creates, inside the already-active isolated schema (no schema prefix needed in the DDL itself
    — the pool's search_path already points at it):
    1. `fansub_groups` mirroring `0009_fansub_groups.up.sql`'s real columns exactly: `id BIGSERIAL PRIMARY KEY,
       slug VARCHAR(120) NOT NULL, name VARCHAR(120) NOT NULL, status VARCHAR(20) NOT NULL DEFAULT 'active'`, plus
       `CREATE UNIQUE INDEX ... ON fansub_groups (slug)` and `... (name)` matching production's uniqueness
       guarantees.
    2. `fansub_group_aliases` mirroring `0014_fansub_group_aliases.up.sql` exactly: `id BIGSERIAL PRIMARY KEY,
       fansub_group_id BIGINT NOT NULL REFERENCES fansub_groups(id) ON DELETE CASCADE, alias VARCHAR(120) NOT
       NULL, normalized_alias VARCHAR(120) NOT NULL, created_at/updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`,
       `CONSTRAINT uq_fansub_group_aliases_normalized_alias UNIQUE (normalized_alias)`, `CONSTRAINT
       uq_fansub_group_aliases_group_normalized UNIQUE (fansub_group_id, normalized_alias)`.
    3. A schema-local `f_unaccent(text) RETURNS text LANGUAGE sql IMMUTABLE AS 'SELECT
       public.unaccent(''public.unaccent''::regdictionary, $1)'` function — this is the exact production wrapper
       body from `0152_f_unaccent_search_path_fix.up.sql`, just re-created inside the isolated schema so it
       resolves without `public` in the pool's search_path (the reference to `public.unaccent` inside the
       function body is itself schema-qualified, so it works regardless of the CALLER's search_path). The
       `unaccent` extension itself is created once per-database already (prior migrations); do not re-run
       `CREATE EXTENSION` here.
    Validate the assembled SQL string with the same `validatePhase106SQL`-style guard the sibling fixtures use
    (or the phase167-local equivalent) before executing, matching the existing defensive pattern.
  </action>
    <verify>
    <automated>cd backend && go build ./internal/testsupport/...</automated>
  </verify>
  <acceptance_criteria>
    `cd backend && TEAM4S_PHASE167_TEST_DSN=<local-dsn> go test ./internal/testsupport/... -run TestPhase167 -v`
    (or the closest available smoke path — if no dedicated smoke test exists yet, this is proven implicitly by
    Task 3's integration tests) succeeds in creating the schema, tables, and local `f_unaccent` without error.
    Without the env var set, `go test ./internal/testsupport/...` still compiles and any test calling
    `OpenPhase167Postgres` skips cleanly (`t.Skipf`), never fails.
    `grep -c "team4s_phase167_test_\[a-z0-9\]" backend/internal/testsupport/phase167_postgres.go` >= 1 and
    `grep -c "phase167_\[a-z0-9_\]" backend/internal/testsupport/phase167_postgres.go` >= 1 (fail-closed guards
    present, per the repo's non-negotiable "never run against team4s_v2" convention).
  </acceptance_criteria>
  <done>OpenPhase167Postgres exists, is gated by TEAM4S_PHASE167_TEST_DSN, validates database/schema name
  patterns before touching anything, and creates a working local f_unaccent alongside minimal fansub_groups/
  fansub_group_aliases tables.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Batch exact-match + trigram-suggestion queries</name>
  <files>backend/internal/models/fansub_group_match.go, backend/internal/repository/fansub_group_match.go, backend/internal/repository/episode_import_repository.go</files>
  <read_first>
    backend/internal/repository/search_fansub.go (full file — WHERE-expression and rank-order to adapt)
    backend/internal/repository/episode_import_repository.go (current 193-line file — EpisodeImportRepository struct, existing method style, error-wrapping idiom)
    backend/internal/repository/fansub_repository.go lines 1727-1735 (normalizeAliasKey, reuse do-not-reinvent)
  </read_first>
  <behavior>
    - `TestBuildFansubGroupBatchMatchQuery` (unit, no DB): asserts the generated SQL string contains
      `= ANY($1::text[])` (or `unnest($1::text[])`), contains the byte-exact expression
      `regexp_replace(lower(f_unaccent(fansub_groups.name)), '[^a-z0-9]+', '', 'g')` and the `.slug` sibling, and
      does NOT contain any string-concatenation of the input candidates into the SQL text itself (proves the
      SQL-injection mitigation statically, mirroring `TestBuildAnimeListWhere_FansubFilter`'s string-assertion
      idiom).
    - `TestBuildFansubGroupSuggestionQuery` (unit, no DB): asserts the generated SQL string contains the `%`
      trigram similarity operator against `f_unaccent(...)` and an `ORDER BY similarity(...) DESC LIMIT` clause,
      and is a textually SEPARATE query from the exact-match one (not folded together, per RESEARCH.md Section
      4.1's explicit guidance to keep the index-friendly equality path clean).
  </behavior>
  <action>
    Create `backend/internal/models/fansub_group_match.go` with three new exported types: `FansubGroupMatch
    struct { RowOrdinal int; RawCandidate string; GroupID int64; GroupName string; GroupSlug string; MatchedVia
    string; MatchedAlias *string; MatchedAliasID *int64 }` (`MatchedVia` is one of `"alias"`, `"name"`, `"slug"`; `MatchedAliasID` is populated with `fga.id` only when `MatchedVia == "alias"`, needed downstream by Plan 07/08 to call `reassignFansubAlias`), `FansubGroupSuggestion
    struct { RowOrdinal int; RawCandidate string; GroupID int64; GroupName string; GroupSlug string }`, and
    `LearnedFansubAlias struct { FansubGroupID int64; Alias string }` (this third type is populated by Plan 06,
    declared here so both plans share one type-ownership file rather than duplicating it).
    Create `backend/internal/repository/fansub_group_match.go`, package `repository`, with two package-level
    functions:
    1. `resolveFansubGroupMatches(ctx context.Context, q fansubGroupMatchQuerier, candidates []string)
       ([]models.FansubGroupMatch, error)` — builds ONE query using
       `WITH candidates AS (SELECT * FROM unnest($1::text[]) WITH ORDINALITY AS c(raw_group, row_ord))` joined
       against `fansub_group_aliases`/`fansub_groups` using the byte-exact `regexp_replace(lower(f_unaccent(...)),
       '[^a-z0-9]+', '', 'g')` expression for `.name`/`.slug` equality and `fga.normalized_alias =
       regexp_replace(lower(f_unaccent(c.raw_group)), '[^a-z0-9]+', '', 'g')` for the alias tier (mirrors
       `search_fansub.go`'s rank-0/1/2 tiers exactly, just against an array instead of one scalar). Bind the
       candidates slice as a single `$1::text[]` parameter — never string-concatenate any candidate into the SQL
       text. Skip candidates that are empty strings before binding. Populate `MatchedVia` based on which tier
       produced the row (alias > name > slug precedence, matching `buildSearchFansubOrder`'s tier priority).
       When the alias tier produced the match, also select `fga.id` and populate `MatchedAliasID` with it
       (leave `MatchedAliasID` nil for name/slug-tier matches, since there is no alias row involved) — Plan
       07/08 need this numeric alias id to call `reassignFansubAlias` when the admin later triggers "Trotzdem
       umhängen" on a conflicting kürzel. Return one `FansubGroupMatch` row per candidate that resolved to
       exactly one group; a candidate with zero or more-than-one exact-tier match is simply absent from the
       result slice (ambiguous or no-match rows are Plan 05's problem to classify, not this function's).
    2. `suggestSimilarFansubGroups(ctx context.Context, q fansubGroupMatchQuerier, candidate string, limit int)
       ([]models.FansubGroupSuggestion, error)` — one query per unresolved candidate (bounded, called only for
       rows with no exact match, per RESEARCH.md's explicit "keep this separate from the exact-match path"
       guidance), using `f_unaccent(fansub_groups.name) % f_unaccent($1)` ordered by
       `similarity(f_unaccent(fansub_groups.name), f_unaccent($1)) DESC LIMIT $2`. Cap `limit` to 3 server-side
       (UI-SPEC's "max 3 chips") regardless of the caller-supplied value.
    Define `fansubGroupMatchQuerier` as a narrow interface (`Query`/`QueryRow` methods, same shape as
    `search_fansub.go`'s `searchQuerier`) so both functions work against `*pgxpool.Pool` directly or a `pgx.Tx`.
    Wrap every SQL error with `fmt.Errorf("<verb> <noun>: %w", err)` per the package's existing convention.
    In `backend/internal/repository/episode_import_repository.go`, add two thin methods on
    `*EpisodeImportRepository` delegating to the two functions above: `ResolveFansubGroupMatches(ctx,
    candidates []string) ([]models.FansubGroupMatch, error)` calling `resolveFansubGroupMatches(ctx, r.db,
    candidates)`, and `SuggestSimilarFansubGroups(ctx, candidate string) ([]models.FansubGroupSuggestion, error)`
    calling `suggestSimilarFansubGroups(ctx, r.db, candidate, 3)`. This file is only 193 lines today; this
    addition stays well under the 450-line budget.
  </behavior>
    <verify>
    <automated>cd backend && go test ./internal/repository/... -run TestBuildFansubGroupBatchMatchQuery -v</automated>
  </verify>
  <acceptance_criteria>
    `cd backend && go test ./internal/repository/... -run TestBuildFansubGroupBatchMatchQuery -v` and
    `-run TestBuildFansubGroupSuggestionQuery -v` both exit 0. `grep -c "regexp_replace(lower(f_unaccent"
    backend/internal/repository/fansub_group_match.go` >= 2 (name + slug tiers present, byte-identical to the
    migration's indexed expression — no re-derivation). `grep -c "+ candidate\|+ raw_group\|fmt.Sprintf.*%s.*raw"
    backend/internal/repository/fansub_group_match.go` is 0 (no string-concatenation of candidate values into
    SQL text — parameterized only). `go build ./...` succeeds.
  </acceptance_criteria>
  <done>resolveFansubGroupMatches issues exactly one SQL statement per call regardless of candidate-slice
  length; suggestSimilarFansubGroups is a clearly separate, smaller query; both are wired onto
  EpisodeImportRepository.</done>
</task>

<task type="auto">
  <name>Task 3: Real-Postgres integration coverage — exact-match tiers and constant query budget</name>
  <files>backend/internal/repository/phase167_fansub_match_test.go</files>
  <read_first>
    backend/internal/repository/segment_origin_query_budget_test.go (query_counter wiring pattern to copy)
    backend/internal/repository/fansub_story_preview_postgres_test.go lines 34-69 (skip-if-unset + fail-closed database-name guard + seedFixture/cleanup pairing, structural template)
    backend/internal/testsupport/phase167_postgres.go (as created in Task 1 — OpenPhase167Postgres signature)
  </read_first>
  <action>
    Add real-Postgres integration tests in the same file as Task 2's unit tests (or a sibling file with the same
    package/build — either is acceptable, keep it one file for this plan's scope), gated by
    `testsupport.OpenPhase167Postgres(t)` (auto-skips if `TEAM4S_PHASE167_TEST_DSN` unset):
    1. `TestResolveFansubGroupMatches_ExactTiers`: seed 2-3 groups plus at least one alias into the isolated
       schema, then call `resolveFansubGroupMatches` with a candidate slice covering: an alias-tier hit, a
       name-tier hit, a slug-tier hit, a candidate matching nothing, and case/separator variants of an existing
       name (e.g. `"team-4s"`/`"Team 4S"` both resolving to a group named `"Team4s"`) — assert the returned
       `MatchedVia` values and that the no-match candidate produces no row.
    2. `TestResolveFansubGroupMatches_ConstantQueryBudget`: wire a `queryCounter` exactly per
       `query_counter.go`'s documented usage (second `pgxpool.NewWithConfig` on the same DSN with
       `config.ConnConfig.Tracer = counter`), call `resolveFansubGroupMatches` once with a 1-element candidate
       slice and once with a 50-element candidate slice (synthetic strings, do not need to resolve to anything),
       `counter.reset()` immediately before each call, and assert `counter.count()` is identical both times
       (proving D-08/D-20's "one query regardless of file count").
    3. `TestResolveFansubGroupMatches_UniquenessRespected` (D-10's alias write-path proof, using the schema's own
       `UNIQUE(normalized_alias)` constraint rather than Go-level validation): attempt to insert two alias rows
       with the same `normalized_alias` for different groups directly against the fixture pool and assert the
       second insert fails with a real Postgres unique-violation error (`isUniqueViolation`-detectable), proving
       the constraint this phase's alias-learning (Plan 06) and reassign (Plan 03) logic both depend on is
       actually enforced by the schema, not just assumed.
  </action>
    <verify>
    <automated>TEAM4S_PHASE167_TEST_DSN=<local-dsn> go test ./internal/repository/... -run TestResolveFansubGroupMatches -v</automated>
  </verify>
  <acceptance_criteria>
    `TEAM4S_PHASE167_TEST_DSN=<local-dsn> go test ./internal/repository/... -run Phase167 -v` (matches all three
    new test names via substring `Phase167`... if naming above doesn't share that substring, use
    `-run TestResolveFansubGroupMatches` instead) exits 0 with all three tests PASS. Without the DSN set, the
    same command reports `SKIP`, never `FAIL`. `cd backend && go test ./internal/repository/... -short` (unit
    tier only) still passes with the integration tests skipped.
  </acceptance_criteria>
  <done>Real Postgres proves exact-match resolution across all three tiers, a constant query count independent
  of candidate-slice size, and that fansub_group_aliases' global uniqueness constraint is live and enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filename-derived candidate strings -> batch-match SQL | Candidate strings originate from `DeriveFansubGroupName` output (Plan 01), themselves derived from filenames on disk — treated as untrusted input at the SQL boundary regardless of upstream validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-167-SQLi | Tampering | `resolveFansubGroupMatches`, `suggestSimilarFansubGroups` | mitigate | Every candidate value is bound via `= ANY($1::text[])`/`$1` parameterized placeholders exclusively — never string-concatenated into the SQL text. Task 2's acceptance criteria include a static grep proving no `fmt.Sprintf`-into-SQL of a candidate value exists. |
| T-167-DoS-batch | Denial of Service | `resolveFansubGroupMatches` candidate slice | accept (deferred to Plan 05) | This plan's function itself does not cap array length — the caller (Plan 05's preview-time wiring, which receives the bounded set of files in one Jellyfin folder) is the natural place to enforce a sanity cap, since this repository function has no request-boundary context of its own. Documented here so Plan 05 does not drop this requirement. |
</threat_model>

<verification>
`cd backend && go build ./...` succeeds. `cd backend && go test ./internal/repository/... ./internal/testsupport/... -v` (unit tier) exits 0. With `TEAM4S_PHASE167_TEST_DSN` exported against a local isolated Postgres, the full integration suite in Task 3 also passes.
</verification>

<success_criteria>
- One parameterized SQL query resolves any number of filename candidates against `fansub_groups`/
  `fansub_group_aliases`, proven by a real query-count assertion, not just code inspection.
- The exact-match query's normalization expression byte-matches the production functional index.
- A real Postgres fixture exists for this phase's alias/group tables, reusable by Plan 06.
</success_criteria>

<output>
Create `.planning/phases/167-fansub-gruppenerkennung-beim-import/167-02-SUMMARY.md` when done.
</output>
