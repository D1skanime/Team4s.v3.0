---
phase: 108-bestehende-beitragsquellen-anbinden
plan: "02"
type: tdd
wave: 2
depends_on: ["108-01"]
files_modified:
  - backend/internal/repository/release_crew_snapshot_repository.go
  - backend/internal/repository/release_crew_snapshot_repository_test.go
  - backend/internal/repository/admin_content_fansub_releases_contributions_repository.go
  - backend/internal/repository/admin_content_fansub_releases_contributions_repository_test.go
  - backend/internal/handlers/admin_content_fansub_releases_contributions_handlers.go
  - backend/internal/handlers/admin_content_fansub_releases_contributions_handlers_test.go
autonomous: true
requirements: [GAM-01, GAM-02, GAM-05]
must_haves:
  truths:
    - "D-12: D-13: Project-team changes synchronize all and only inherited snapshots; individually edited snapshots remain independent."
    - "D-10: D-11: D-14: Readers return stored complete snapshots and never substitute project defaults based on partial release rows; each release owns its complete crew and unrelated roles remain intact."
  artifacts:
    - path: "backend/internal/repository/release_crew_snapshot_repository.go"
      provides: "Locked complete-set storage and inherited synchronization"
    - path: "backend/internal/repository/admin_content_fansub_releases_contributions_repository.go"
      provides: "Stored-snapshot-only effective contribution read projection"
  key_links:
    - from: "GetEffectiveContributionsForVersion"
      to: "ReleaseCrewSnapshotRepository.LoadComplete"
      via: "stored rows plus snapshot_mode; no anime_default fallback"
    - from: "project crew mutation"
      to: "inherited release snapshots"
      via: "shared synchronizer filtered by snapshot_mode"
---
<objective>Replace read-time fallback with complete stored release snapshots and make the existing GET path consume that stored truth. Purpose: fulfill D-10–D-14 before points orchestration and preserve empty independent snapshots. Output: DBTX repository plus repository/handler read-path tests.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md @docs/architecture/db-schema-fansub-domain.md @.planning/phases/108-bestehende-beitragsquellen-anbinden/108-CONTEXT.md @.planning/phases/108-bestehende-beitragsquellen-anbinden/108-PATTERNS.md @.planning/phases/108-bestehende-beitragsquellen-anbinden/108-01-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement locked complete-snapshot repository contracts</name>
  <files>backend/internal/repository/release_crew_snapshot_repository.go, backend/internal/repository/release_crew_snapshot_repository_test.go</files>
  <read_first>
backend/internal/repository/release_crew_snapshot_repository.go
backend/internal/repository/admin_content_fansub_releases_contributions_repository.go
backend/internal/repository/anime_contributions_upsert_repository.go
backend/internal/repository/anime_contributions_release_lookup_repository.go
backend/internal/repository/anime_contributions_release_lookup_repository_test.go
backend/internal/repository/anime_contributions_member_project_repository.go
database/migrations/0137_phase108_contribution_sources.up.sql
docs/architecture/db-schema-fansub-domain.md
.planning/phases/108-bestehende-beitragsquellen-anbinden/108-VALIDATION.md
  </read_first>
  <behavior>
    - `LoadComplete` returns only stored release rows plus `inherited|independent`, including an empty independent set (D-10).
    - `ReplaceInTx` locks `(release_version_id,fansub_group_id)`, normalizes unique member/role units, validates canonical context, and replaces the complete set without committing.
    - `SyncInheritedInTx` changes only inherited snapshots and leaves independent snapshots byte-for-byte unchanged (D-12, D-13).
    - Gon/Übersetzung, Mia/QC, Anton/Edit remain separate units and changing one role never deletes an unrelated role (D-03, D-14).
  </behavior>
  <action>Create `ReleaseCrewSnapshotRepository` with DBTX methods named `LoadComplete`, `ReplaceInTx`, `SeedInheritedInTx`, and `SyncInheritedForProjectInTx` (or equally explicit names frozen by tests). Reuse the advisory transaction-lock pattern from `anime_contributions_upsert_repository.go`, but lock the full release/group context. Store crew through existing `anime_contributions` and `anime_contribution_roles`; do not use fallback reads, self-owned nested transactions, a universal contribution model, or legacy `fansubgroup_id`. The repository returns before/after normalized unit sets for the service and never writes ledger rows.</action>
  <verify><automated>cd backend &amp;&amp; go test ./internal/repository -run 'ReleaseCrewSnapshot|EveryReleaseReadsCompleteStoredSnapshot|ProjectCrewChangeSyncsInherited' -count=1</automated></verify>
  <acceptance_criteria>
- Tests prove complete persisted reads for inherited and empty independent states.
- Tests prove sync touches only inherited rows and does not partially merge independent rows.
- `rg -n "anime_default|fallback|fansubgroup_id" backend/internal/repository/release_crew_snapshot_repository.go` returns no executable fallback or legacy-column use.
- Focused repository tests pass.
  </acceptance_criteria>
  <done>D-10, D-12, and the storage half of D-13/D-14 are executable without points or UI assumptions.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Route effective-contributions GET through stored complete snapshots</name>
  <files>backend/internal/repository/admin_content_fansub_releases_contributions_repository.go, backend/internal/repository/admin_content_fansub_releases_contributions_repository_test.go, backend/internal/handlers/admin_content_fansub_releases_contributions_handlers.go, backend/internal/handlers/admin_content_fansub_releases_contributions_handlers_test.go</files>
  <read_first>
backend/internal/repository/release_crew_snapshot_repository.go
backend/internal/repository/admin_content_fansub_releases_contributions_repository.go
backend/internal/handlers/admin_content_fansub_releases_contributions_handlers.go
backend/internal/handlers/admin_content_fansub_releases_contributions_handlers_test.go
docs/architecture/db-schema-fansub-domain.md
.planning/phases/108-bestehende-beitragsquellen-anbinden/108-CONTEXT.md
  </read_first>
  <behavior>
    - GET returns persisted complete rows plus `snapshot_mode: inherited|independent`; it never queries project defaults (D-10, D-11).
    - An empty independent snapshot returns `data: []` and `snapshot_mode: independent`, never `anime_default`.
    - An inherited snapshot returns its stored rows even when the current project roster later differs.
    - Permission and release/group context validation still occur before snapshot data is returned.
  </behavior>
  <action>Replace `FansubReleasesContributionsRepository.ListEffectiveContributionsForVersion` all-or-nothing fallback implementation with a projection over `ReleaseCrewSnapshotRepository.LoadComplete` (or share its DBTX query without duplicating fallback logic). Update `EffectiveContributionsResult` and `GetEffectiveContributionsForVersion` to return stored rows and `snapshot_mode`; remove `IsOverride`, `source=anime_default`, and the project-contribution fallback query. Preserve the existing permission-first handler and row display projection. Add repository and handler tests for populated inherited, populated independent, empty independent, and missing/invalid context reads.</action>
  <verify><automated>cd backend &amp;&amp; go test ./internal/repository ./internal/handlers -run 'EffectiveContributions.*(Inherited|Independent|Empty)|EveryReleaseReadsCompleteStoredSnapshot' -count=1</automated></verify>
  <acceptance_criteria>
- No executable `anime_default` or second-step fallback query remains in `admin_content_fansub_releases_contributions_repository.go`.
- Empty independent handler response is exactly an empty data array with independent snapshot mode.
- Inherited and independent tests prove GET uses persisted rows, not the current project roster.
- Permission denial and release/group mismatch occur before returning stored rows.
  </acceptance_criteria>
  <done>The existing drawer GET path reads complete stored truth and can distinguish empty independent snapshots without fallback.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| project crew→release snapshot | Project defaults cross into release-owned stored truth |
| release creation→group context | Snapshot may be written only after canonical group ownership exists |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-108-04 | T | complete-set replacement | mitigate | context advisory lock, normalized Set, one caller-owned transaction |
| T-108-05 | E | release/group scoping | mitigate | server joins real release version and canonical fansub group before writes |
| T-108-06 | D | concurrent sync/edit | mitigate | same context lock and inherited predicate serialize mutation |
</threat_model>
<verification>`cd backend && go test ./internal/repository ./internal/handlers -run 'ReleaseCrewSnapshot|EffectiveContributions' -count=1` then `git diff --check`.</verification>
<success_criteria>Stored snapshots replace all-or-nothing fallback, including an unambiguous empty independent read, and only inherited snapshots are eligible for synchronization.</success_criteria>
<output>After completion, create `.planning/phases/108-bestehende-beitragsquellen-anbinden/108-02-SUMMARY.md`.</output>
