---
phase: 108-bestehende-beitragsquellen-anbinden
plan: "07"
type: tdd
wave: 4
depends_on: ["108-03"]
files_modified:
  - backend/internal/repository/episode_import_repository_release_helpers.go
  - backend/internal/repository/episode_import_repository_release_helpers_test.go
  - backend/internal/repository/episode_version_repository.go
  - backend/internal/repository/episode_version_repository_test.go
autonomous: true
requirements: [GAM-01, GAM-02, GAM-03, GAM-04, GAM-05]
must_haves:
  truths:
    - "D-11: Both canonical release-creation boundaries seed the latest confirmed project roster and award every Member x Release x Role atomically after group ownership exists."
    - "Empty rosters persist explicit inherited snapshots; accountless historical members remain eligible beneficiaries."
    - "Creation retry, reprocessing, and injected failures preserve exact-once atomic behavior without reseeding an existing release."
  artifacts:
    - path: "backend/internal/repository/episode_import_repository_release_helpers.go"
      provides: "Import release-creation transaction hook after upsertReleaseVersionGroup"
    - path: "backend/internal/repository/episode_version_repository.go"
      provides: "Manual/admin release-creation transaction hook after syncEpisodeVersionSelectedGroups"
  key_links:
    - from: "upsertImportReleaseGraph and EpisodeVersionRepository.Create"
      to: "ReleaseCrewService.SeedCreatedReleaseInTx contract"
      via: "narrow injected transaction hook after release_version_groups ownership creation"
---
<objective>Wire the reusable release-crew transaction contract into both canonical release-creation boundaries. Purpose: make D-02, D-11, and D-15 executable at creation time after canonical group ownership exists. Output: two repository-side creation hooks and focused atomicity/idempotency tests.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md @docs/engineering/implementation-contract.md @docs/architecture/db-schema-fansub-domain.md @.planning/phases/108-bestehende-beitragsquellen-anbinden/108-CONTEXT.md @.planning/phases/108-bestehende-beitragsquellen-anbinden/108-RESEARCH.md @.planning/phases/108-bestehende-beitragsquellen-anbinden/108-03-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Seed and award crews at both canonical release creation boundaries</name>
  <files>backend/internal/repository/episode_import_repository_release_helpers.go, backend/internal/repository/episode_import_repository_release_helpers_test.go, backend/internal/repository/episode_version_repository.go, backend/internal/repository/episode_version_repository_test.go</files>
  <read_first>
backend/internal/services/release_crew_service.go
backend/internal/repository/episode_import_repository_release_helpers.go
backend/internal/repository/episode_import_repository_release_helpers_test.go
backend/internal/repository/episode_version_repository.go
backend/internal/repository/episode_version_repository_test.go
backend/internal/repository/episode_version_repository_write_helpers.go
backend/internal/repository/anime_contributions_member_project_repository.go
docs/architecture/db-schema-fansub-domain.md
  </read_first>
  <behavior>
    - `upsertImportReleaseGraph` invokes crew seeding only for a newly created release graph, after `upsertReleaseVersionGroup`, inside the import transaction.
    - `EpisodeVersionRepository.Create` invokes crew seeding after `syncEpisodeVersionSelectedGroups`, inside its existing transaction.
    - Each boundary reads only the latest confirmed project roster and atomically persists an inherited snapshot plus one award per normalized Member x Release Version x Role (D-02, D-11, D-15).
    - Empty roster persists explicit empty inherited state; accountless members receive points; injected failure rolls back release graph, snapshot, and ledger; retry does not duplicate awards.
  </behavior>
  <action>Define a narrow repository-side transaction hook interface to avoid a repository-to-services import cycle, add constructor/dependency injection seams on both repositories, and invoke the hook at `upsertImportReleaseGraph` and `EpisodeVersionRepository.Create`. Plan 108-08 binds these seams to the shared `ReleaseCrewService.SeedCreatedReleaseInTx` instance in production `main.go`; this plan proves the repository contracts independently with test hooks. Call the hook for every real `(release_version_id,fansub_group_id)` only after the respective group-link function completes. Do not call `SeedInheritedInTx` directly because creation must include PointService awards in the same caller-owned transaction. Preserve import reprocessing behavior: existing releases are not reseeded as new. Add populated, empty, latest-confirmed-roster, accountless-member, rollback-at-award, and retry tests for both callers.</action>
  <verify><automated>cd backend &amp;&amp; go test ./internal/repository -run 'EpisodeImport.*Crew|EpisodeVersionCreate.*Crew|ReleaseCreation.*(Award|Rollback|Retry|Latest|Accountless)' -count=1</automated></verify>
  <acceptance_criteria>
- Import and manual creation tests prove seeding occurs only after canonical `release_version_groups(release_version_id, fansub_group_id)` ownership exists in the same transaction.
- Both callers copy the latest confirmed project roster, persist an explicit empty inherited snapshot, and credit an accountless member normally.
- Injected snapshot/award failure restores release graph/group link, snapshot, and ledger to their pre-transaction states for both callers.
- Lost-response/repeated invocation leaves one inherited snapshot and one effective award per normalized Member x Release Version x Role.
- Proposed/draft/disputed/hidden rows never seed the new release or produce points.
  </acceptance_criteria>
  <done>Every current release creation surface establishes canonical ownership first, then atomically seeds and awards the complete latest confirmed project crew.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| release creation→crew hook | Canonical group ownership must exist before snapshot/award creation |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-108-20 | T | creation ordering | mitigate | tests require release_version_groups before the injected seed hook |
| T-108-21 | D | creation retry | mitigate | caller-owned transaction, stable source identities, rollback and retry tests at both creators |
</threat_model>
<verification>Run focused creation integration tests with retry sampling, then `git diff --check`.</verification>
<success_criteria>Both canonical release creators expose and exercise transaction hooks after group ownership, with exact complete-snapshot awards and rollback/idempotency proof.</success_criteria>
<output>After completion, create `.planning/phases/108-bestehende-beitragsquellen-anbinden/108-07-SUMMARY.md`.</output>
