---
phase: 108-bestehende-beitragsquellen-anbinden
plan: "07"
type: tdd
wave: 4
depends_on: ["108-03"]
files_modified:
  - backend/internal/repository/episode_import_repository_release_helpers.go
  - backend/internal/repository/episode_import_repository_release_helpers_test.go
  - backend/internal/repository/episode_version_repository.go
  - backend/internal/repository/episode_version_repository_test.go
  - backend/cmd/server/main.go
  - backend/internal/handlers/fansub_anime_contributions_handler.go
  - backend/internal/handlers/fansub_anime_contributions_delete_handler.go
  - backend/internal/handlers/fansub_anime_contributions_handler_test.go
  - backend/internal/repository/anime_contributions_upsert_repository.go
  - backend/internal/repository/anime_contributions_member_repository.go
autonomous: true
requirements: [GAM-01, GAM-02, GAM-03, GAM-04, GAM-05]
must_haves:
  truths:
    - "D-11: Both canonical release-creation boundaries seed the latest project roster and award every Member x Release x Role atomically after group ownership exists."
    - "D-12: Project-roster add, role change, and remove synchronize and point-diff inherited snapshots in the same transaction."
    - "Independent snapshots remain byte-for-byte and ledger-for-ledger unchanged by project-roster mutations."
    - "Retries, failures, and accountless historical members preserve exact-once atomic behavior."
  artifacts:
    - path: "backend/internal/repository/episode_import_repository_release_helpers.go"
      provides: "Import release-creation integration after upsertReleaseVersionGroup"
    - path: "backend/internal/repository/episode_version_repository.go"
      provides: "Manual/admin release-creation integration after syncEpisodeVersionSelectedGroups"
    - path: "backend/internal/handlers/fansub_anime_contributions_handler.go"
      provides: "Canonical project-roster create/update service routing"
    - path: "backend/internal/handlers/fansub_anime_contributions_delete_handler.go"
      provides: "Canonical project-roster delete service routing"
  key_links:
    - from: "upsertImportReleaseGraph and EpisodeVersionRepository.Create"
      to: "ReleaseCrewService.SeedCreatedReleaseInTx"
      via: "existing pgx.Tx after release_version_groups ownership creation"
    - from: "CreateAnimeContribution, UpdateAnimeContribution, DeleteAnimeContribution"
      to: "ReleaseCrewService.ApplyProjectRosterMutation"
      via: "one transaction owning project-row mutation, inherited snapshot sync, and PointService diffs"
---
<objective>Wire the reusable release-crew service into every live creation and project-roster mutation boundary. Purpose: make D-02, D-11, D-12, and D-15 executable rather than repository-only capabilities. Output: two creation integrations and transactional project add/change/remove integrations with focused tests.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md @docs/engineering/implementation-contract.md @docs/architecture/db-schema-fansub-domain.md @.planning/phases/108-bestehende-beitragsquellen-anbinden/108-CONTEXT.md @.planning/phases/108-bestehende-beitragsquellen-anbinden/108-RESEARCH.md @.planning/phases/108-bestehende-beitragsquellen-anbinden/108-03-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Seed and award crews at both canonical release creation boundaries</name>
  <files>backend/internal/repository/episode_import_repository_release_helpers.go, backend/internal/repository/episode_import_repository_release_helpers_test.go, backend/internal/repository/episode_version_repository.go, backend/internal/repository/episode_version_repository_test.go, backend/cmd/server/main.go</files>
  <read_first>
backend/internal/services/release_crew_service.go
backend/internal/repository/episode_import_repository_release_helpers.go
backend/internal/repository/episode_import_repository_release_helpers_test.go
backend/internal/repository/episode_version_repository.go
backend/internal/repository/episode_version_repository_test.go
backend/internal/repository/episode_version_repository_write_helpers.go
backend/internal/repository/anime_contributions_member_project_repository.go
backend/cmd/server/main.go
docs/architecture/db-schema-fansub-domain.md
  </read_first>
  <behavior>
    - `upsertImportReleaseGraph` invokes crew seeding only for a newly created release graph, after `upsertReleaseVersionGroup`, inside the import transaction.
    - `EpisodeVersionRepository.Create` invokes crew seeding after `syncEpisodeVersionSelectedGroups`, inside its existing transaction.
    - Each boundary reads the latest project roster and atomically persists an inherited snapshot plus one award per normalized Member x Release Version x Role (D-02, D-11, D-15).
    - Empty roster persists explicit empty inherited state; accountless members receive points; injected failure rolls back release graph, snapshot, and ledger; retry does not duplicate awards.
  </behavior>
  <action>Define a narrow repository-side transaction hook interface (to avoid a repository→services import cycle), implement it with `ReleaseCrewService.SeedCreatedReleaseInTx`, and inject it into both repositories from `backend/cmd/server/main.go`. Invoke that hook at the two live creation callers proven by research: `upsertImportReleaseGraph` and `EpisodeVersionRepository.Create`. Call it for every real `(release_version_id,fansub_group_id)` only after the respective group-link function has completed. Do not call `SeedInheritedInTx` directly because creation must include PointService awards in the same caller-owned transaction. Preserve import reprocessing behavior: existing releases are not reseeded as new. Add populated, empty, latest-roster, accountless-member, rollback-at-award, and retry tests for both callers.</action>
  <acceptance_criteria>
    - `TestEpisodeImportReleaseCreationSeedsCrewAfterGroupLink` proves `upsertImportReleaseGraph` calls `SeedCreatedReleaseInTx` only for a newly created graph and only after the canonical `release_version_groups(release_version_id, fansub_group_id)` row exists in the same transaction.
    - `TestEpisodeVersionCreateSeedsCrewAfterSelectedGroups` proves `EpisodeVersionRepository.Create` calls `SeedCreatedReleaseInTx` for every selected canonical group only after `syncEpisodeVersionSelectedGroups` has created its `release_version_groups` link.
    - `TestReleaseCreationUsesLatestProjectRoster`, `TestReleaseCreationPersistsEmptyInheritedRoster`, and `TestReleaseCreationCreditsAccountlessMember` pass for both creation callers and assert respectively that the newest project roster is copied, an explicit empty inherited snapshot is stored, and a member without `app_user` receives the normal Member x Release Version x Role point.
    - `TestReleaseCreationCrewAwardFailureRollsBackAtomically` passes for both callers and asserts the release graph/group link, inherited snapshot, and all point-ledger rows equal their pre-transaction state after an injected snapshot or award failure.
    - `TestReleaseCreationCrewRetryIsIdempotent` passes for both callers and asserts a lost-response/repeated invocation leaves one inherited snapshot and exactly one effective award per normalized Member x Release Version x Role.
    - `cd backend &amp;&amp; go test ./internal/repository -run 'EpisodeImport.*Crew|EpisodeVersionCreate.*Crew|ReleaseCreation.*(Award|Rollback|Retry|Latest|Accountless)' -count=1` exits 0.
  </acceptance_criteria>
  <verify><automated>cd backend &amp;&amp; go test ./internal/repository -run 'EpisodeImport.*Crew|EpisodeVersionCreate.*Crew|ReleaseCreation.*(Award|Rollback|Retry|Latest|Accountless)' -count=1</automated></verify>
  <done>Every current release creation surface establishes canonical ownership first, then atomically seeds and awards the complete latest project crew.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Route project-roster add, change, and remove through inherited synchronization</name>
  <files>backend/internal/handlers/fansub_anime_contributions_handler.go, backend/internal/handlers/fansub_anime_contributions_delete_handler.go, backend/internal/handlers/fansub_anime_contributions_handler_test.go, backend/internal/repository/anime_contributions_upsert_repository.go, backend/internal/repository/anime_contributions_member_repository.go</files>
  <read_first>
backend/internal/services/release_crew_service.go
backend/internal/handlers/fansub_anime_contributions_handler.go
backend/internal/handlers/fansub_anime_contributions_delete_handler.go
backend/internal/repository/anime_contributions_upsert_repository.go
backend/internal/repository/anime_contributions_member_repository.go
backend/internal/repository/anime_contributions_inputs.go
frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionModal.tsx
.planning/phases/108-bestehende-beitragsquellen-anbinden/108-PATTERNS.md
  </read_first>
  <behavior>
    - Successful anime-level create adds units to every inherited release snapshot and awards their release-role diffs atomically.
    - Anime-level role/member update reverses removed units, credits added units, and preserves unchanged units for inherited snapshots.
    - Anime-level delete removes and reverses units from inherited snapshots.
    - Release-version-specific contribution mutations do not trigger project-roster sync; independent snapshots and ledger state remain byte-for-byte unchanged.
    - Failure at project mutation, any snapshot, or any ledger operation rolls back the entire action; repeated/concurrent requests remain exact-once.
  </behavior>
  <action>Extract DBTX variants for the existing `AnimeContributionsRepository.CreateOrUpdate`, `Update`, and `Delete` logic without duplicating parsing or ownership rules. Route `FansubAnimeContributionsHandler.CreateAnimeContribution`, `.UpdateAnimeContribution`, and `DeleteAnimeContribution` through one `ReleaseCrewService.ApplyProjectRosterMutation` command when the resulting contribution is project-level (`release_version_id IS NULL`). The service must own one transaction containing the canonical project-row mutation, `SyncInheritedForProjectInTx`, and all PointService credit/reversal/restoration diffs. Preserve existing permission-first validation, response, audit, and post-commit badge behavior. Add tests for add/change/remove across multiple inherited releases, mixed inherited/independent releases, byte-for-byte independent preservation, latest roster used by later release creation, rollback at each fault point, retry/concurrency, and release-specific no-sync.</action>
  <acceptance_criteria>
    - `TestCreateAnimeContributionSyncsInheritedProjectRoster`, `TestUpdateAnimeContributionDiffsInheritedProjectRoster`, and `TestDeleteAnimeContributionSyncsInheritedProjectRoster` prove the three canonical handler methods `CreateAnimeContribution`, `UpdateAnimeContribution`, and `DeleteAnimeContribution` delegate project-level mutations to `ApplyProjectRosterMutation`.
    - The create/update/delete tests assert that only snapshots with `snapshot_mode='inherited'` change, and that point-ledger writes equal the normalized set diff: added Member x Release Version x Role units are credited, removed units are reversed, restored units are restored once, and unchanged units produce no ledger write.
    - `TestProjectRosterMutationPreservesIndependentSnapshotBytes` compares serialized contribution rows, role rows, snapshot metadata, and ledger rows before and after each project-roster mutation and proves every independent snapshot is byte-for-byte unchanged.
    - `TestReleaseSpecificAnimeContributionDoesNotSyncProjectRoster` exercises release-specific create, update, and delete mutations and proves they do not invoke project-default synchronization or alter any other release snapshot.
    - `TestProjectRosterMutationRollsBackDomainSnapshotsAndPoints` injects failure at the project mutation, inherited snapshot fan-out, and ledger stages and proves project rows, every affected snapshot, and all ledger rows roll back to their exact pre-request state.
    - `TestProjectRosterMutationRetryIsIdempotent` and `TestProjectRosterMutationConcurrentRequestsAreExactOnce` prove repeated and concurrent equivalent requests converge on one project roster, one inherited snapshot result per release, and one effective point transition per semantic unit without duplicate awards, reversals, or restorations.
    - `cd backend &amp;&amp; go test ./internal/handlers ./internal/repository ./internal/services -run 'ProjectRoster|AnimeContribution.*Inherited|IndependentSnapshotUnchanged' -count=1` exits 0.
  </acceptance_criteria>
  <verify><automated>cd backend &amp;&amp; go test ./internal/handlers ./internal/repository ./internal/services -run 'ProjectRoster|AnimeContribution.*Inherited|IndependentSnapshotUnchanged' -count=1</automated></verify>
  <done>All canonical project-team mutations transactionally synchronize and point-diff inherited releases only, while independent release truth never changes.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| release creation→crew service | Group ownership must exist before snapshot/award creation |
| project roster→inherited releases | One project mutation fans out to multiple release-owned snapshots and ledger entries |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-108-20 | T | creation ordering | mitigate | tests require release_version_groups before SeedCreatedReleaseInTx |
| T-108-21 | R | project sync awards | mitigate | stable per-member/release/role sources and append-only reversals/restorations |
| T-108-22 | D | fan-out rollback/retry | mitigate | one pgx.Tx, context locks, fault injection, concurrent retry tests |
| T-108-23 | E | independent snapshot | mitigate | snapshot_mode predicate plus byte-for-byte before/after assertions |
</threat_model>
<verification>Run focused creation and project-roster integration tests with retry sampling, then `git diff --check`.</verification>
<success_criteria>Both live release creators and all three project-roster mutation handlers invoke ReleaseCrewService transactionally, with exact awards and no independent-snapshot mutation.</success_criteria>
<output>After completion, create `.planning/phases/108-bestehende-beitragsquellen-anbinden/108-07-SUMMARY.md`.</output>
