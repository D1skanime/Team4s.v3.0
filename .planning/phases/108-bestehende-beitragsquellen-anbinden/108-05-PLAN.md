---
phase: 108-bestehende-beitragsquellen-anbinden
plan: "05"
type: execute
wave: 4
depends_on: ["108-03", "108-04"]
files_modified:
  - shared/contracts/openapi.yaml
  - shared/contracts/admin-content.yaml
  - frontend/src/types/fansub.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/api.phase108.test.ts
  - frontend/src/lib/api.auth-refresh.test.ts
  - frontend/src/app/admin/fansubs/[id]/edit/ReleaseContributionDrawer.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/ReleaseContributionDrawer.test.tsx
autonomous: true
requirements: [GAM-01, GAM-02, GAM-04, GAM-05]
must_haves:
  truths:
    - "The drawer loads and saves a complete stored crew through one typed replace request."
    - "Inherited/independent status is visible; empty independent state never falls back and no reset-to-project action exists."
    - "Protected crew and project-text saves work with absent/expired access token plus valid refresh session through the central client."
    - "Backend, both OpenAPI contracts, frontend DTOs, and helper agree without exposing ledger internals."
  artifacts:
    - path: "shared/contracts/openapi.yaml"
      provides: "Canonical complete-set replace contract"
    - path: "frontend/src/lib/api.ts"
      provides: "Central-auth `replaceReleaseCrew` helper"
    - path: "frontend/src/app/admin/fansubs/[id]/edit/ReleaseContributionDrawer.tsx"
      provides: "Single-request complete-set editor"
  key_links:
    - from: "ReleaseContributionDrawer.tsx"
      to: "frontend/src/lib/api.ts"
      via: "one replaceReleaseCrew call with normalized staged rows"
    - from: "frontend/src/lib/api.ts"
      to: "replace endpoint"
      via: "apiClientFetch central auth-refresh seam"
---
<objective>Publish the complete-set contract and switch the existing drawer from row orchestration to one protected typed replacement. Purpose: make the atomic backend behavior reachable without auth or contract drift. Output: aligned YAML/DTO/helper/UI and focused tests.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md @docs/api/api-contracts.md @docs/frontend/auth-api-client.md @docs/frontend/ui-system.md @docs/agent-guidelines-ui.md @.planning/phases/108-bestehende-beitragsquellen-anbinden/108-CONTEXT.md @.planning/phases/108-bestehende-beitragsquellen-anbinden/108-PATTERNS.md @.planning/phases/108-bestehende-beitragsquellen-anbinden/108-03-SUMMARY.md @.planning/phases/108-bestehende-beitragsquellen-anbinden/108-04-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Freeze and implement the typed complete-set client contract</name>
  <files>shared/contracts/openapi.yaml, shared/contracts/admin-content.yaml, frontend/src/types/fansub.ts, frontend/src/lib/api.ts, frontend/src/lib/api.phase108.test.ts, frontend/src/lib/api.auth-refresh.test.ts</files>
  <read_first>
shared/contracts/openapi.yaml
shared/contracts/admin-content.yaml
docs/api/api-contracts.md
docs/frontend/auth-api-client.md
frontend/src/types/fansub.ts
frontend/src/lib/api.ts
frontend/src/lib/api.auth-refresh.test.ts
backend/internal/handlers/admin_content_fansub_releases_contributions_handlers.go
backend/internal/handlers/admin_content_anime_project_notes.go
.planning/phases/108-bestehende-beitragsquellen-anbinden/108-VALIDATION.md
  </read_first>
  <behavior>
    - `ReplaceReleaseCrewRequest` contains only rows of member_id and role_codes; response contains complete rows and `snapshot_mode: inherited|independent`.
    - Both YAML contracts, backend response, TypeScript DTO, and helper method/path/status/error behavior match.
    - `replaceReleaseCrew` uses `apiClientFetch`; no token parameter, bearer header, direct cookie/storage read, or Keycloak refresh call is added.
    - Absent/expired access token with valid refresh token completes crew replace and existing project-note mutation through central refresh.
  </behavior>
  <action>Add the replace operation and schemas to both canonical contracts, extend `EffectiveContributionsResponse` with `snapshot_mode`, add explicit request/response types in `fansub.ts`, and add exactly one `replaceReleaseCrew` helper in `api.ts` using existing `apiClientFetch`, `parseApiErrorPayload`, and `ApiError`. Do not expose beneficiary override, actor, reviewer, point value, rule version, idempotency key, award status, effective time, or generation. Add `api.phase108.test.ts` and extend the established refresh-only tests for both protected mutations.</action>
  <verify><automated>cd frontend &amp;&amp; npm test -- api.phase108 api.auth-refresh &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>
- `rg -n "ReplaceReleaseCrewRequest|snapshot_mode|replaceReleaseCrew" shared/contracts/openapi.yaml shared/contracts/admin-content.yaml frontend/src/types/fansub.ts frontend/src/lib/api.ts` finds aligned declarations.
- `rg -n "Authorization: Bearer|getRuntimeAuthToken|getRuntimeRefreshToken|document.cookie" frontend/src/lib/api.phase108.test.ts frontend/src/app/admin/fansubs/[id]/edit/ReleaseContributionDrawer.tsx` returns no production boundary violation.
- Helper test asserts one request, exact path/method/body, response parsing, and non-2xx error.
- Refresh-only session tests pass for replace and note save.
  </acceptance_criteria>
  <done>The typed contract is consistent across all surfaces and uses the central auth-refresh client exclusively.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Replace drawer row fan-out with one complete-set save</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/ReleaseContributionDrawer.tsx, frontend/src/app/admin/fansubs/[id]/edit/ReleaseContributionDrawer.test.tsx</files>
  <read_first>
frontend/src/app/admin/fansubs/[id]/edit/ReleaseContributionDrawer.tsx
frontend/src/app/admin/fansubs/[id]/edit/ReleaseContributionDrawer.test.tsx
frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionModal.tsx
frontend/src/types/fansub.ts
frontend/src/lib/api.ts
frontend/src/components/ui
docs/frontend/ui-system.md
docs/agent-guidelines-ui.md
docs/frontend/auth-api-client.md
  </read_first>
  <behavior>
    - Save sends one normalized full staged row set and closes only after success; no `Promise.all` row delete/upsert orchestration remains.
    - Drawer displays inherited versus independent status and accepts/reloads an empty independent set without project fallback.
    - Exact Gon/Mia/Anton edit preserves Anton while removing Mia/QC and adding Gon/QC.
    - No “Projektbesetzung neu übernehmen” action is rendered; scoped loading/error and cancellation behavior remain.
  </behavior>
  <action>Retain the existing drawer, staged editor, member picker, role controls, request-cancellation guard, and global UI primitives. Replace only its multi-request save with one `replaceReleaseCrew` call using the full normalized staged rows. Render the response `snapshot_mode` with correct German umlauts; remove fallback-specific restrictions/messages and any row-level delete/upsert imports. Do not add a universal contribution editor, global state, new route, or reset-to-project action.</action>
  <verify><automated>cd frontend &amp;&amp; npm test -- ReleaseContributionDrawer &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated></verify>
  <acceptance_criteria>
- Component test asserts one replace call and zero row-level delete/upsert calls.
- Tests cover inherited load, manual independence, empty independent reload, request failure staying open, and Gon/Mia/Anton preservation.
- `rg -n "Promise\\.all|deleteAnimeContribution|upsertAnimeContribution|Projektbesetzung neu übernehmen" 'frontend/src/app/admin/fansubs/[id]/edit/ReleaseContributionDrawer.tsx'` returns no match.
- Changed German user-facing copy uses ä/ö/ü/Ä/Ö/Ü/ß, never ASCII substitutions.
  </acceptance_criteria>
  <done>The existing user-visible route performs one atomic complete-set save through the typed central client and clearly reflects permanent independence.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser UI→protected API | Staged rows and refresh-only session cross the central client |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-108-14 | S | browser auth | mitigate | `apiClientFetch` owns refresh and bearer; UI gates on access-or-refresh session |
| T-108-15 | T | request DTO | mitigate | typed allowlisted rows; backend remains authoritative |
| T-108-16 | D | row fan-out | mitigate | one atomic replace request and scoped loading/error state |
</threat_model>
<verification>Run focused Vitest, `npm run typecheck`, `npm run lint`, contract diff check, and the no-token-boundary regression.</verification>
<success_criteria>The existing drawer and project-note flow are contract-aligned, refresh-safe, single-request, and free of fallback/reset or ledger-internal fields.</success_criteria>
<output>After completion, create `.planning/phases/108-bestehende-beitragsquellen-anbinden/108-05-SUMMARY.md`.</output>
