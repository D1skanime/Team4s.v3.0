---
phase: 108-bestehende-beitragsquellen-anbinden
plan: "08"
type: tdd
wave: 5
depends_on: ["108-04", "108-07"]
files_modified:
  - backend/internal/handlers/fansub_anime_contributions_handler.go
  - backend/internal/handlers/fansub_anime_contributions_delete_handler.go
  - backend/internal/handlers/fansub_anime_contributions_handler_test.go
  - backend/internal/repository/anime_contributions_upsert_repository.go
  - backend/internal/repository/anime_contributions_member_repository.go
  - backend/cmd/server/main.go
  - backend/cmd/server/admin_routes.go
  - backend/cmd/server/phase108_runtime_wiring_test.go
  - backend/internal/handlers/admin_content_handler.go
  - backend/internal/handlers/contribution_review_handler.go
  - backend/internal/handlers/contributions_me_handler.go
autonomous: true
requirements: [GAM-01, GAM-02, GAM-03, GAM-04, GAM-05]
must_haves:
  truths:
    - "D-12: Project-roster add, role change, and remove synchronize and point-diff all and only inherited snapshots in the same transaction."
    - "D-13: Independent snapshots remain byte-for-byte and ledger-for-ledger unchanged by project-roster mutations."
    - "Generic POST/PATCH/DELETE remain project-row-only; every release-specific row mutation and release_version_id ownership move is rejected."
    - "Only confirmed project truth fans out; proposed, draft, disputed, and hidden rows never change effective snapshots or points."
    - "One shared service graph is injected into generic mutations, Replace, notes, leader/member confirmation, and both release-creation hooks."
  artifacts:
    - path: "backend/internal/handlers/fansub_anime_contributions_handler.go"
      provides: "Project-roster create/update routing and release-specific mutation rejection"
    - path: "backend/internal/handlers/fansub_anime_contributions_delete_handler.go"
      provides: "Project-roster delete routing and release-specific target rejection"
    - path: "backend/cmd/server/phase108_runtime_wiring_test.go"
      provides: "Complete production constructor and route reachability proof"
  key_links:
    - from: "CreateAnimeContribution, UpdateAnimeContribution, DeleteAnimeContribution"
      to: "ReleaseCrewService.ApplyProjectRosterMutation"
      via: "one transaction owning project-row mutation, inherited snapshot sync, and PointService diffs"
    - from: "backend/cmd/server/main.go"
      to: "both release repository transaction hooks"
      via: "the same ReleaseCrewService.SeedCreatedReleaseInTx adapter proven in 108-07"
---
<objective>Harden generic contribution mutations and complete the Phase-108 production service graph. Purpose: make D-12/D-13 and the corrected review/runtime boundaries executable without permitting release-specific generic writes. Output: transactional inherited synchronization, project-only restrictions, and complete constructor/route wiring.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md @docs/engineering/implementation-contract.md @docs/architecture/db-schema-fansub-domain.md @.planning/phases/108-bestehende-beitragsquellen-anbinden/108-CONTEXT.md @.planning/phases/108-bestehende-beitragsquellen-anbinden/108-RESEARCH.md @.planning/phases/108-bestehende-beitragsquellen-anbinden/108-03-SUMMARY.md @.planning/phases/108-bestehende-beitragsquellen-anbinden/108-04-SUMMARY.md @.planning/phases/108-bestehende-beitragsquellen-anbinden/108-07-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Restrict generic mutations and synchronize confirmed project truth</name>
  <files>backend/internal/handlers/fansub_anime_contributions_handler.go, backend/internal/handlers/fansub_anime_contributions_delete_handler.go, backend/internal/handlers/fansub_anime_contributions_handler_test.go, backend/internal/repository/anime_contributions_upsert_repository.go, backend/internal/repository/anime_contributions_member_repository.go</files>
  <read_first>
backend/internal/services/release_crew_service.go
backend/internal/handlers/fansub_anime_contributions_handler.go
backend/internal/handlers/fansub_anime_contributions_delete_handler.go
backend/internal/repository/anime_contributions_upsert_repository.go
backend/internal/repository/anime_contributions_member_repository.go
backend/internal/repository/anime_contributions_inputs.go
frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionModal.tsx
.planning/phases/108-bestehende-beitragsquellen-anbinden/108-PATTERNS.md
  </read_first>
  <behavior>
    - Anime-level confirmed create/update/delete point-diffs every inherited release snapshot atomically while independent snapshots never change (D-12, D-13).
    - POST rejects non-NULL release_version_id; PATCH rejects every payload carrying release_version_id and every stored release-specific target; DELETE rejects every stored release-specific target.
    - Generic PATCH cannot promote review status; only confirmed project rows affect inherited snapshots, while proposed/draft/disputed/hidden CRUD leaves snapshots and points unchanged.
    - Failure at project mutation, snapshot fan-out, or ledger operation rolls back the entire action; repeated/concurrent requests remain exact-once (D-15–D-18, D-19a).
  </behavior>
  <action>Keep generic POST/PATCH/DELETE solely for project rows. POST rejects any non-NULL `release_version_id`. PATCH rejects every request containing that field, including null/same/different release, then loads the target in route group/anime scope and rejects it if stored `release_version_id` is non-NULL even when omitted. DELETE performs the same scoped load and rejection. Generic PATCH also rejects review-status transitions; dedicated leader/member commands own them. Route allowed project mutations through one service transaction: confirmed project rows synchronize and point-diff inherited snapshots, while proposed/draft/disputed/hidden CRUD leaves effective snapshots and ledger byte-for-byte unchanged. Release crews can be created, changed, emptied, or removed only through Replace. Preserve permission-first validation, response, audit, and post-commit badge behavior.</action>
  <verify><automated>cd backend &amp;&amp; go test ./internal/handlers ./internal/repository ./internal/services -run 'ProjectRoster|AnimeContribution.*Inherited|IndependentSnapshotUnchanged|ProjectOnly|ReleaseSpecificRejected' -count=1</automated></verify>
  <acceptance_criteria>
- Create/update/delete tests prove canonical handlers delegate confirmed project mutations to `ApplyProjectRosterMutation`.
- Only inherited snapshots change; added units credit, removed units reverse, restored units restore once, and unchanged units create no ledger write.
- Independent contribution rows, role rows, snapshot metadata, and ledger rows remain byte-for-byte unchanged.
- POST/PATCH/DELETE rejection covers all release_version_id moves and stored release-specific targets with zero writes.
- Fault injection and sequential/concurrent tests prove one semantic result across project row, inherited snapshots, and ledger.
  </acceptance_criteria>
  <done>Canonical generic mutations are project-only and confirmed-only, and their inherited fan-out is atomic, exact-once, and unable to alter independent releases.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Wire the shared services through every production constructor and route</name>
  <files>backend/cmd/server/main.go, backend/cmd/server/admin_routes.go, backend/cmd/server/phase108_runtime_wiring_test.go, backend/internal/handlers/admin_content_handler.go, backend/internal/handlers/contribution_review_handler.go, backend/internal/handlers/contributions_me_handler.go</files>
  <read_first>
backend/cmd/server/main.go
backend/cmd/server/admin_routes.go
backend/internal/handlers/admin_content_handler.go
backend/internal/handlers/admin_content_fansub_releases_contributions_handlers.go
backend/internal/handlers/admin_content_anime_project_notes.go
backend/internal/handlers/fansub_anime_contributions_handler.go
backend/internal/handlers/contribution_review_handler.go
backend/internal/handlers/contributions_me_handler.go
backend/internal/repository/episode_import_repository.go
backend/internal/repository/episode_import_repository_release_helpers.go
backend/internal/repository/episode_version_repository.go
  </read_first>
  <behavior>
    - `main.go` constructs one PointService-backed ReleaseCrewService and ProjectNoteCreditService graph after their repositories exist.
    - AdminContentHandler, generic contribution handler, leader review handler, member `/me` handler, and both 108-07 release-creation hooks receive the same service instances.
    - `admin_routes.go` registers complete-set Replace; existing proposal and `/me` routes remain registered in `main.go`.
    - Production-graph tests reach generic project mutation, Replace, leader confirm, member confirm, project note, import creation, and manual release creation.
  </behavior>
  <action>Construct the Phase-108 repositories/services once in `main.go` after PointService and inject them into `AdminContentHandler`, `FansubAnimeContributionsHandler`, `ContributionReviewHandler`, `ContributionsMeHandler`, and both release-creation repository hook seams created by 108-07. Extend the exact constructor/fluent dependency seams in `admin_content_handler.go`; do not create handler-local services. Register the complete-set method/path in `admin_routes.go`, retaining proposal and `/me` route ownership in `main.go`. Add compile-time production-graph and routed HTTP tests that fail if any generic project, Replace, review, member, note, import-creation, or manual-creation path bypasses the shared services.</action>
  <verify><automated>cd backend &amp;&amp; go test ./cmd/server ./internal/handlers ./internal/repository -run 'Phase108RuntimeWiring|ReleaseCrewRoute|ConfirmProposal|ConfirmMyAnimeContribution|ProjectNote|ReleaseCreation|ProjectOnly|ReleaseSpecificRejected' -count=1</automated></verify>
  <acceptance_criteria>
- Constructor assertions name every dependency and prove there is no handler-local service construction.
- Router tests reach every generic, Replace, review, member, and note mutation through production registration.
- Import and manual release creators use the same service-backed seeding hook after `release_version_groups` ownership exists.
- Generic route tests prove release-specific mutations stay rejected in the fully wired production graph.
  </acceptance_criteria>
  <done>All production constructors, dependencies, creation hooks, and routes execute the intended Phase-108 transaction boundaries with no live bypass.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| project roster→inherited releases | One project mutation fans out to multiple release-owned snapshots and ledger entries |
| production constructors→domain services | Every live route and both release creators must share the intended transaction services |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-108-22 | D | fan-out rollback/retry | mitigate | one pgx.Tx, context locks, fault injection, concurrent retry tests |
| T-108-23 | E | independent snapshot | mitigate | snapshot_mode predicate plus byte-for-byte before/after assertions |
| T-108-24 | E | generic release mutation | mitigate | payload and stored-target rejection before mutation |
| T-108-25 | T | runtime service bypass | mitigate | production-graph constructor and routed HTTP tests cover every mutation owner |
</threat_model>
<verification>Run focused project-roster, route-boundary, creation-hook wiring, and production-graph tests, then `git diff --check`.</verification>
<success_criteria>Generic mutations are project-only and confirmed-only; inherited synchronization is atomic; all runtime owners and both release creators share the intended Phase-108 services.</success_criteria>
<output>After completion, create `.planning/phases/108-bestehende-beitragsquellen-anbinden/108-08-SUMMARY.md`.</output>
