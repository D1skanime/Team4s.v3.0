---
phase: 108-bestehende-beitragsquellen-anbinden
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - database/migrations/0137_phase108_contribution_sources.up.sql
  - database/migrations/0137_phase108_contribution_sources.down.sql
  - backend/internal/migrations/phase108_contribution_sources_test.go
autonomous: true
requirements: [GAM-01, GAM-02, GAM-03, GAM-04, GAM-05]
must_haves:
  truths:
    - "Every release/group context can persist an inherited or permanently independent complete crew snapshot, including an empty independent snapshot."
    - "Release-role work is worth exactly 1 point and project-text authorship exactly 5 points through immutable server-owned rules."
    - "A reversed identical release-role unit can be restored once through a new append-only generation."
    - "No existing row is copied, preserved, reconciled, backfilled, or awarded by the migration."
  artifacts:
    - path: "database/migrations/0137_phase108_contribution_sources.up.sql"
      provides: "Snapshot state, source lifecycle state, constraints, and immutable point rules"
    - path: "backend/internal/migrations/phase108_contribution_sources_test.go"
      provides: "Up/down, constraint, rule, and no-backfill proof"
  key_links:
    - from: "release crew source lifecycle"
      to: "point_ledger_entries"
      via: "stored award/reversal identity and generation consumed by PointService"
    - from: "snapshot status"
      to: "release_version_groups"
      via: "real release_version_id plus canonical fansub_group_id"
---
<objective>Create and prove the canonical Phase-108 persistence contract before service implementation. Purpose: distinguish complete inherited snapshots, permanent independent snapshots, and append-only credit lifecycles without migrating disposable rows. Output: reversible migration and failing-then-passing migration tests.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md @C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md @docs/engineering/implementation-contract.md @docs/architecture/db-schema-fansub-domain.md @.planning/phases/108-bestehende-beitragsquellen-anbinden/108-CONTEXT.md @.planning/phases/108-bestehende-beitragsquellen-anbinden/108-RESEARCH.md @.planning/phases/108-bestehende-beitragsquellen-anbinden/108-VALIDATION.md @.planning/phases/108-bestehende-beitragsquellen-anbinden/108-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Freeze the Phase-108 schema and no-backfill contract</name>
  <files>backend/internal/migrations/phase108_contribution_sources_test.go</files>
  <read_first>
AGENTS.md
docs/engineering/implementation-contract.md
docs/architecture/db-schema-fansub-domain.md
.planning/phases/108-bestehende-beitragsquellen-anbinden/108-CONTEXT.md
.planning/phases/108-bestehende-beitragsquellen-anbinden/108-VALIDATION.md
backend/internal/migrations/release_review_lifecycle_test.go
backend/internal/migrations/release_review_contribution_rule_test.go
database/migrations/0131_member_point_foundation.up.sql
database/migrations/0131_member_point_foundation.down.sql
database/migrations/0136_release_review_contribution_rule.up.sql
  </read_first>
  <behavior>
    - Clean-schema up/down proves snapshot status accepts only inherited/independent and an empty independent snapshot remains representable (D-10, D-13, D-22).
    - Fixed immutable rules are 1 point for Member x real release version x role and 5 points for the first non-empty project-text lifecycle (D-02, D-05).
    - Source lifecycle state supports one reversal and a later generation/restoration while ledger history remains append-only (D-17, D-19, D-19a).
    - SQL contains no INSERT-SELECT data copy, existing-row UPDATE, backfill, reconciliation, compatibility view, or historical award scan (D-20, D-21).
  </behavior>
  <action>Create `TestPhase108MigrationUpDown`, `TestPhase108PointRules`, `TestPhase108SnapshotAndLifecycleConstraints`, and `TestPhase108MigrationContainsNoBackfillOrCompatibilityCopy`. Inspect `git status --short` and the last tracked migration immediately before choosing 0137; stop if another untracked migration exists. Tests must read the exact migration files, exercise PostgreSQL up/down using existing migration test support, and use comment-filtered/static assertions that cannot pass from header prose alone.</action>
  <verify><automated>cd backend &amp;&amp; go test ./internal/migrations -run Phase108 -count=1</automated></verify>
  <acceptance_criteria>
- `rg -n "TestPhase108(MigrationUpDown|PointRules|SnapshotAndLifecycleConstraints|MigrationContainsNoBackfillOrCompatibilityCopy)" backend/internal/migrations/phase108_contribution_sources_test.go` finds all four tests.
- The test rejects data-copy/backfill/reconciliation/compatibility SQL after stripping SQL comments.
- The focused migration test initially fails because migration 0137 does not exist.
  </acceptance_criteria>
  <done>The executable test contract covers D-02, D-05, D-10, D-13, D-17, D-19, D-19a, and D-20–D-22 without assuming preservation of test rows.</done>
</task>
<task type="auto">
  <name>Task 2: Add reversible snapshot and credit lifecycle schema</name>
  <files>database/migrations/0137_phase108_contribution_sources.up.sql, database/migrations/0137_phase108_contribution_sources.down.sql</files>
  <read_first>
backend/internal/migrations/phase108_contribution_sources_test.go
database/migrations/0131_member_point_foundation.up.sql
database/migrations/0131_member_point_foundation.down.sql
database/migrations/0135_release_review_lifecycle.up.sql
database/migrations/0135_release_review_lifecycle.down.sql
database/migrations/0136_release_review_contribution_rule.up.sql
database/migrations/0136_release_review_contribution_rule.down.sql
database/migrations/0086_anime_contributions.up.sql
database/migrations/0091_anime_contributions_release_version.up.sql
database/migrations/0105_anime_contributions_member_id.up.sql
  </read_first>
  <action>Add canonical additive structures keyed by real `release_version_id` and `fansub_group_id` for complete crew snapshot mode and source-generation state, plus a project-note credit lifecycle keyed by `anime_id` and `fansub_group_id`. Use `release_version_groups.fansub_group_id`, existing `members`, `anime_contributions`, `anime_contribution_roles`, and Phase-106 ledger/rule references; do not add another contribution table, identity, ledger, text store, media relation, fallback flag, reset action, or old-row default/backfill. Insert immutable v1 rule rows with categories `fansub_work` (1) and `platform_contribution` (5); exact codes may follow research discretion but must be frozen in tests. Down must remove only Phase-108 objects/rules and remain reversible on a clean disposable database.</action>
  <verify><automated>cd backend &amp;&amp; go test ./internal/migrations -run Phase108 -count=1</automated></verify>
  <acceptance_criteria>
- `Get-ChildItem database/migrations -Filter '0137_phase108_contribution_sources.*.sql'` returns exactly the up and down migration.
- `rg -n "fansubgroup_id|release_media|release_version_media|media_assets|media_files" database/migrations/0137_phase108_contribution_sources.*.sql` returns no matches.
- `rg -n "INSERT\\s+.*SELECT|backfill|reconcil|compatib" database/migrations/0137_phase108_contribution_sources.*.sql` has no executable SQL match after comments are ignored.
- `cd backend && go test ./internal/migrations -run Phase108 -count=1` passes.
  </acceptance_criteria>
  <done>Fresh databases have constrained snapshot/lifecycle state and the two fixed rules; down reverses it; no existing-row preservation or historical award is present.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| migration→canonical domain | New state must reference real release/group/member/note owners |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-108-01 | T | snapshot/source schema | mitigate | foreign keys, unique constraints, status checks, real release_version_id and canonical fansub_group_id |
| T-108-02 | R | credit generation | mitigate | persisted generation and ledger references preserve append-only provenance |
| T-108-03 | I | disposable rows | mitigate | static test forbids copying, reconciling, or awarding existing rows |
</threat_model>
<verification>`cd backend && go test ./internal/migrations -run Phase108 -count=1` and `git diff --check -- database/migrations/0137_phase108_contribution_sources.up.sql database/migrations/0137_phase108_contribution_sources.down.sql backend/internal/migrations/phase108_contribution_sources_test.go`.</verification>
<success_criteria>Phase-108 persistence is canonical, reversible, source-keyed, and free of backfill or parallel domain structures.</success_criteria>
<output>After completion, create `.planning/phases/108-bestehende-beitragsquellen-anbinden/108-01-SUMMARY.md`.</output>
