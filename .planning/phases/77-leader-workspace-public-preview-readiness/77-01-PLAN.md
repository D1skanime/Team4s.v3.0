---
phase: 77-leader-workspace-public-preview-readiness
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/ReadinessTab.test.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/page.test.tsx
autonomous: true
requirements: [F, I, K]

must_haves:
  truths:
    - "ReadinessTab.test.tsx existiert und läuft rot (RED-Phase) bevor Produktionscode geschrieben wird"
    - "Req F: Tab sichtbar bei can_edit_group=true, unsichtbar bei reiner Mitgliedschaft (can_view_members only)"
    - "Req I: PublicPreviewPanel rendert ohne Schreib-Buttons — kein onSave/onChange — read-only-Vertrag per D-01"
    - "Lock K: Readiness-Tab ruft ausschließlich listGroupMembers, listPendingMemberClaims, getAdminFansubAnime auf"
    - "D-04: Klick auf Sprungmarke löst router.replace mit korrektem ?tab= aus"
    - "D-06: Claims-Zähler trägt Badge variant=info, kein warning/fehlt-Urteil"
  artifacts:
    - path: "frontend/src/app/admin/fansubs/[id]/edit/ReadinessTab.test.tsx"
      provides: "Vollständiges Testgerüst für Phase 77 — alle sechs Req→Test-Cases"
      min_lines: 80
  key_links:
    - from: "ReadinessTab.test.tsx"
      to: "page.test.tsx"
      via: "Gemeinsame Mock-Konventionen (vi.hoisted, vi.mock @/lib/api, next/navigation)"
      pattern: "vi\\.hoisted.*apiMocks"
---

<objective>
Wave-0-Testgerüst für Phase 77: `ReadinessTab.test.tsx` anlegen (roter Zustand) und bestehende `page.test.tsx` um Capability-Gating-Cases für den neuen „readiness"-Tab erweitern.

Zweck: Nyquist-Regel erfüllen — alle sechs Req→Test-Zeilen aus VALIDATION.md müssen als rote Tests vorliegen, bevor Plan 02 die Komponenten implementiert. Tests decken Req F, I, K, D-04, D-06 ab.

Ausgabe: Zwei Testdateien — eine neue (`ReadinessTab.test.tsx`) und eine erweiterte (`page.test.tsx`). Beide laufen nach diesem Plan ROT (Import-Fehler für `ReadinessTab`), was der TDD-RED-Phase entspricht.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/77-leader-workspace-public-preview-readiness/77-CONTEXT.md
@.planning/phases/77-leader-workspace-public-preview-readiness/77-VALIDATION.md
@.planning/phases/77-leader-workspace-public-preview-readiness/77-PATTERNS.md
</context>

<interfaces>
<!-- Mock-Pattern aus page.test.tsx (verifiziert, Zeilen 1–56) -->
<!-- vi.hoisted für API-Mocks; vi.mock next/navigation mit replace: vi.fn() -->

Aus page.test.tsx Zeilen 34–56:
```typescript
const apiMocks = vi.hoisted(() => ({
  createFansubAlias: vi.fn(),
  getAdminFansubAnime: vi.fn(),
  getFansubByID: vi.fn(),
  getFansubGroupCapabilities: vi.fn(),
  getCurrentUser: vi.fn(),
  // … weitere bestehende Mocks
}))
vi.mock('@/lib/api', () => ({
  ApiError: class ApiError extends Error {
    status: number
    constructor(status: number, message: string) { super(message); this.status = status }
  },
  ...apiMocks,
}))
```

Aus page.test.tsx Zeilen 22–27:
```typescript
vi.mock('next/navigation', () => ({
  useParams: () => ({ id: '88' }),
  usePathname: () => '/admin/fansubs/88/edit',
  useRouter: () => ({ replace: vi.fn() }),
  useSearchParams: () => new URLSearchParams(),
}))
```

Capability-Default aus page.test.tsx Zeilen 155–170:
```typescript
apiMocks.getFansubGroupCapabilities.mockResolvedValue({
  data: {
    can_edit_group: true,
    can_manage_links: true,
    can_view_members: true,
    can_manage_members: true,
    can_edit_notes: true,
    can_view_invitations: true,
    can_create_invitation: true,
    can_cancel_invitation: true,
    can_view_releases: true,
    can_view_release_media: true,
    can_upload_release_media: true,
    can_edit_release_notes: true,
  },
})
```

FansubGroup-Felder (types/fansub.ts): logo_url, banner_url, founded_year, country, status, name, slug, group_type (kein has_notes-Feld)
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: ReadinessTab.test.tsx anlegen (Wave-0, RED-Phase)</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/ReadinessTab.test.tsx</files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/page.test.tsx (vollständig — Mock-Konventionen, vi.hoisted-Muster, beforeEach-Setup, Zeilen 1–205)
    - .planning/phases/77-leader-workspace-public-preview-readiness/77-VALIDATION.md (Req→Test-Map, alle sechs Rows)
    - .planning/phases/77-leader-workspace-public-preview-readiness/77-PATTERNS.md (ReadinessTab.test.tsx-Sektion, Zeilen 324–398)
    - .planning/phases/77-leader-workspace-public-preview-readiness/77-UI-SPEC.md (Copywriting Contract — exakte deutsche Strings für Assertions)
  </read_first>
  <behavior>
    - Test 1 (Req F): render(ReadinessTab, { fansubId: 88, group: {..., can_edit_group: true} }) → screen enthält Readiness-Tab-Inhalte (SectionHeader-Titel „Veröffentlichung & Pflegezustand" sichtbar)
    - Test 2 (Req F — Gating-Gegenbeweis): ReadinessTab mit capabilities { can_view_members: true, can_edit_group: false, can_edit_notes: false } → tab-Inhalt ist NICHT sichtbar oder Komponente rendert keinen verwertbaren Zustand
    - Test 3 (Req I — read-only): render(ReadinessTab) → kein Button mit label „Speichern" / kein input type=submit / kein onSave-Prop vorhanden in gerendertem Baum
    - Test 4 (Lock K — keine neuen Endpunkte): Nach render + await, apiMocks überprüfen → nur listGroupMembers, listPendingMemberClaims, getAdminFansubAnime wurden aufgerufen; kein unbekannter fetch-Aufruf
    - Test 5 (D-04 Sprungmarke): Klick auf einen Sprungmarken-Button (z. B. mit text-match „Im Medien-Tab ergänzen") → router.replace wurde mit String der form „?tab=media" aufgerufen
    - Test 6 (D-06 informative Zähler): apiMocks.listPendingMemberClaims resolved mit Array length 3 → gerenderter Text enthält „Offene Claims: 3" und kein Element hat aria-label „Status: fehlt" für diesen Eintrag
  </behavior>
  <action>
    Datei neu anlegen. Kopf exakt analog zu page.test.tsx:
    - `// @vitest-environment jsdom`
    - Imports: `createElement, type ReactNode` aus react; `afterEach, beforeEach, describe, expect, it, vi` aus vitest; `cleanup, render, screen, waitFor, fireEvent` aus @testing-library/react
    - `vi.mock('next/link', ...)` — identisch zu page.test.tsx
    - `vi.mock('next/navigation', ...)` — usePathname → '/admin/fansubs/88/edit', useRouter → { replace: vi.fn() }, useSearchParams → new URLSearchParams()
    - `const apiMocks = vi.hoisted(() => ({ listGroupMembers: vi.fn(), listPendingMemberClaims: vi.fn(), getAdminFansubAnime: vi.fn() }))`
    - `vi.mock('@/lib/api', ...)` — ApiError-Klasse + ...apiMocks (per D-01 nur die drei genutzten Seams)
    - Mocks für `./PublicPreviewPanel` und `./FansubProfileTabs` (falls importiert): stub als `<div data-testid="public-preview-panel" />`
    - `afterEach(() => { cleanup(); vi.clearAllMocks() })`
    - `beforeEach()`: listGroupMembers resolved mit `{ data: [] }`, listPendingMemberClaims resolved mit `[]`, getAdminFansubAnime resolved mit `{ data: [] }`
    - Hilfsfunktion `makeGroup(overrides)` liefert minimales FansubGroup-Objekt: id:88, slug:'subgroup', name:'SubGroup', status:'active', group_type:'group', logo_url:null, banner_url:null, anime_relations_count:0, release_versions_count:0, members_count:0, aliases_count:0, created_at:'2026-01-01T00:00:00Z', updated_at:'2026-01-01T00:00:00Z'
    - Sechs describe/it-Blöcke gemäß Req→Test-Map aus VALIDATION.md (Req F×2, I, K, D-04, D-06)
    - Import: `import { ReadinessTab } from './ReadinessTab'` — dieser Import schlägt fehl (Datei existiert noch nicht) → roter Zustand ist korrekt und erwartet in Wave 0

    Hinweis: Kein fenced code block im action. Alle Stringliterale für Assertions aus UI-SPEC Copywriting Contract entnehmen: SectionHeader-Titel = „Veröffentlichung & Pflegezustand", Informativ-Zähler = „Offene Claims: {n}", Sprungmarken-Buttons enden mit →.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/admin/fansubs/\\[id\\]/edit/ReadinessTab.test.tsx 2>&1 | head -30</automated>
  </verify>
  <done>ReadinessTab.test.tsx existiert; `npx vitest run ReadinessTab.test.tsx` schlägt fehl mit „Cannot find module './ReadinessTab'"-Fehler (erwartet — RED-Phase). Alle sechs describe-Blöcke sind in der Datei vorhanden.</done>
</task>

<task type="tdd">
  <name>Task 2: page.test.tsx — Capability-Gating-Cases für „readiness"-Tab ergänzen</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/page.test.tsx</files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/page.test.tsx (vollständig lesen — bestehende describe-Struktur, um neue Cases korrekt einzuhängen ohne Dopplung)
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx (Zeilen 199–268 — MAIN_TABS, canUseMainTab, resolveMainTabForAccess; SectionKey-Typ Zeilen 121–133)
    - .planning/phases/77-leader-workspace-public-preview-readiness/77-RESEARCH.md (Req F Tab-Gating-Logik, Zeilen 207–226)
  </read_first>
  <behavior>
    - Neuer describe-Block „canUseMainTab — readiness-Tab (Req F)":
      - „gibt true zurück wenn can_edit_group=true (isPlatformAdmin=false)": canUseMainTab('readiness', false, { ...allesFalse, can_edit_group: true }) === true
      - „gibt true zurück wenn can_edit_notes=true (isPlatformAdmin=false)": canUseMainTab('readiness', false, { ...allesFalse, can_edit_notes: true }) === true
      - „gibt false zurück bei reiner Mitgliedschaft (can_view_members=true, can_edit_group=false, can_edit_notes=false)": canUseMainTab('readiness', false, { ...allesFalse, can_view_members: true }) === false
      - „gibt false zurück wenn capabilities=null": canUseMainTab('readiness', false, null) === false
      - „gibt true zurück für isPlatformAdmin=true unabhängig von capabilities": canUseMainTab('readiness', true, null) === true
    - Diese Tests testen `canUseMainTab` direkt als Unit-Funktion (Import aus page.tsx oder inline-Reimplementierung in Tests — bevorzuge direkten Import wenn möglich)
  </behavior>
  <action>
    Bestehende Datei erweitern. Neuen describe-Block am Ende der Datei anfügen (vor dem letzten `}` der Datei, falls nötig, oder als eigenständigen Block).

    Da `canUseMainTab` nicht direkt exportiert ist (es ist ein Modulscoped-Function in page.tsx), müssen die Tests über das Render-Verhalten des Tab-Menüs gehen: Tab „Veröffentlichung" ist sichtbar/nicht sichtbar basierend auf capabilities in getFansubGroupCapabilities-Mock. Alternativ: Falls canUseMainTab exportierbar gemacht wird (als separater Export in Plan 03), direkte Unit-Tests.

    Vorerst über Render-Verhalten: Mock getFansubGroupCapabilities mit restriktiven Capabilities, render AdminFansubEditPage, assert screen.queryByRole('button', { name: 'Veröffentlichung' }) ist null (Tab nicht sichtbar bei nur can_view_members). Diese Tests laufen ROT solange MAIN_TABS „readiness" noch nicht enthält (das kommt in Plan 03).

    Konkrete Ergänzung: Einen describe-Block „Tab ‚Veröffentlichung' — Capability-Gating (Req F)" mit drei it-Cases hinzufügen. Jeder Case mockt getFansubGroupCapabilities individuell (mockResolvedValueOnce) und prüft das Tab-Menü nach await waitFor.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/admin/fansubs/\\[id\\]/edit/page.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>page.test.tsx enthält neuen describe-Block für Req F; bestehende Tests laufen weiterhin durch; neue Req-F-Cases laufen ROT solange page.tsx noch kein „readiness" in MAIN_TABS/canUseMainTab enthält.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Grenze | Beschreibung |
|--------|--------------|
| Browser → Testumgebung | jsdom-Tests erhalten keine echten API-Aufrufe — vi.mock isoliert alle Seams |

## STRIDE Threat Register

| Threat ID | Kategorie | Komponente | Disposition | Mitigationsplan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01 | Tampering | Test-Mock-Vollständigkeit | accept | Wave-0-Tests sind rote Tests — unvollständige Mocks führen zu Compile-Fehler, nicht zu falschem Grün; wird in Plan 02 behoben |
| T-77-SC | Tampering | npm/pip install | accept | Phase 77 installiert keine neuen Pakete (RESEARCH bestätigt); kein slopcheck nötig |
</threat_model>

<verification>
cd frontend && npx vitest run src/app/admin/fansubs/\\[id\\]/edit/ReadinessTab.test.tsx 2>&1 | head -10
Erwartetes Ergebnis: Fehler „Cannot find module './ReadinessTab'" — entspricht erwartetem RED-Zustand.

cd frontend && npx vitest run src/app/admin/fansubs/\\[id\\]/edit/page.test.tsx 2>&1 | tail -10
Erwartetes Ergebnis: Bestehende Tests grün; neue Req-F-Cases rot.
</verification>

<success_criteria>
- ReadinessTab.test.tsx existiert mit sechs describe/it-Blöcken (Req F×2, I, K, D-04, D-06)
- page.test.tsx enthält neuen describe-Block mit mindestens drei Capability-Gating-Cases für den „readiness"-Tab
- Beide Dateien kompilieren syntaktisch korrekt (kein TypeScript-Fehler außer fehlendem ReadinessTab-Modul)
- Alle neuen Testfälle laufen ROT (RED-Phase erfüllt — Produktionscode fehlt noch)
- Alle bisher grünen Tests in page.test.tsx bleiben grün
</success_criteria>

<output>
Erstelle `.planning/phases/77-leader-workspace-public-preview-readiness/77-01-SUMMARY.md` wenn fertig
</output>
