---
phase: 77-leader-workspace-public-preview-readiness
plan: 03
type: execute
wave: 2
depends_on: [77-02]
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
autonomous: true
requirements: [F, I, K]

must_haves:
  truths:
    - "SectionKey und MainTab in page.tsx enthalten 'readiness' als gültigen Wert (per D-03)"
    - "MAIN_TABS enthält Eintrag { key: 'readiness', label: 'Veröffentlichung' } (per D-03)"
    - "canUseMainTab('readiness') gibt true zurück bei can_edit_group=true oder can_edit_notes=true (per D-08)"
    - "canUseMainTab('readiness') gibt false zurück bei nur can_view_members=true (Success Criterion 4)"
    - "Tab-Render-Zweig delegiert activeMainTab === 'readiness' an ReadinessTab-Komponente (per D-03)"
    - "page.tsx erhält KEINEN zusätzlichen Logik-Code — Readiness-Logik liegt vollständig in ReadinessTab.tsx"
    - "Formular-Ausschluss: readiness-Tab liegt außerhalb des Save-Formulars (analog zu mitglieder/rollen/vorschlaege)"
  artifacts:
    - path: "frontend/src/app/admin/fansubs/[id]/edit/page.tsx"
      provides: "Readiness-Tab verdrahtet (SectionKey, MAIN_TABS, canUseMainTab-Case, Render-Zweig)"
      contains: "readiness"
  key_links:
    - from: "page.tsx:canUseMainTab"
      to: "FansubGroupCapabilities.can_edit_group / can_edit_notes"
      via: "case 'readiness': return capabilities.can_edit_group || capabilities.can_edit_notes"
      pattern: "case.*readiness"
    - from: "page.tsx:MAIN_TABS"
      to: "ReadinessTab"
      via: "activeMainTab === 'readiness' → <ReadinessTab>"
      pattern: "activeMainTab.*readiness"
---

<objective>
`page.tsx` chirurgisch erweitern: SectionKey-Typ, MAIN_TABS, canUseMainTab-Switch-Case, Formular-Ausschluss und Tab-Render-Zweig für den neuen „readiness"-Tab einhängen. Alle Readiness-Logik bleibt in ReadinessTab.tsx — page.tsx erhält nur vier minimale Eingriffe.

Zweck: Phase-77-Workspace-Integration vervollständigen. Nach diesem Plan ist der Tab „Veröffentlichung" in der Workspace-Tab-Leiste sichtbar (capability-gated) und rendert `<ReadinessTab>`.

Ausgabe: Geänderte page.tsx (nur minimale Ergänzungen, kein Umbau).
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/77-leader-workspace-public-preview-readiness/77-CONTEXT.md
@.planning/phases/77-leader-workspace-public-preview-readiness/77-RESEARCH.md
@.planning/phases/77-leader-workspace-public-preview-readiness/77-PATTERNS.md
@.planning/phases/77-leader-workspace-public-preview-readiness/77-02-SUMMARY.md
</context>

<interfaces>
<!-- Bestehende page.tsx-Andockpunkte (verifiziert, Zeilen angegeben) -->

SectionKey-Typ (page.tsx Zeilen 121–132):
```
type SectionKey =
  | "basic" | "media" | "links" | "collaboration" | "releases"
  | "anime-projekte" | "notes" | "mitglieder" | "rollen"
  | "claims" | "vorschlaege";
type MainTab = SectionKey;
```

MAIN_TABS (page.tsx Zeilen 199–210):
```
const MAIN_TABS: Array<{ key: MainTab; label: string }> = [
  { key: "basic", label: "Grunddaten" },
  { key: "notes", label: "Gruppengeschichte" },
  ...
  { key: "anime-projekte", label: "Anime-Einblicke" },
];
```

canUseMainTab (page.tsx Zeilen 216–250):
```
function canUseMainTab(tab: MainTab, isPlatformAdmin: boolean, capabilities: FansubGroupCapabilities | null): boolean {
  // switch(tab) ... default: return false;
}
```

Formular-Ausschluss (page.tsx Zeilen 2396–2401):
```
{activeMainTab !== "releases" &&
 activeMainTab !== "anime-projekte" &&
 activeMainTab !== "notes" &&
 activeMainTab !== "mitglieder" &&
 activeMainTab !== "rollen" &&
 activeMainTab !== "vorschlaege" ? (
   <form ...>...</form>
) : null}
```

Tab-Render-Zweig (page.tsx Zeilen 3489–3492):
```
{activeMainTab === "mitglieder" ? <GroupMembersTab fansubId={fansubID} /> : null}
{activeMainTab === "rollen" ? <MemberRolesTab fansubId={fansubID} /> : null}
{activeMainTab === "claims" ? <ClaimManagementPanel groupId={fansubID} /> : null}
{activeMainTab === "vorschlaege" ? <ReviewQueue fansubId={fansubID} /> : null}
```

ReadinessTab Props: { fansubId: number; group: FansubGroup } — group ist im page-State als group (FansubGroup) verfügbar
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: page.tsx — vier chirurgische Eingriffe</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/page.tsx</files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx (Zeilen 121–133: SectionKey; Zeilen 199–210: MAIN_TABS; Zeilen 216–250: canUseMainTab; Zeilen 2394–2410: Formular-Ausschluss; Zeilen 3480–3495: Tab-Render-Zweige; Zeilen 90–110: bestehende Imports)
    - frontend/src/app/admin/fansubs/[id]/edit/ReadinessTab.tsx (existiert nach Plan 02 — Props-Signatur prüfen)
    - .planning/phases/77-leader-workspace-public-preview-readiness/77-PATTERNS.md (page.tsx-Sektion Zeilen 241–320)
  </read_first>
  <behavior>
    - Nach Änderung: MAIN_TABS.some(t => t.key === 'readiness') === true
    - Nach Änderung: canUseMainTab('readiness', false, { ...allesFalse, can_edit_group: true }) === true
    - Nach Änderung: canUseMainTab('readiness', false, { ...allesFalse, can_edit_notes: true }) === true
    - Nach Änderung: canUseMainTab('readiness', false, { ...allesFalse, can_view_members: true }) === false
    - Nach Änderung: render AdminFansubEditPage mit ?tab=readiness → screen enthält Inhalte des ReadinessTab
    - Nach Änderung: readiness-Tab liegt NICHT im Save-Formular (activeMainTab === 'readiness' ist im Formular-Ausschluss)
  </behavior>
  <action>
    HINWEIS: page.tsx hat ~3.800 Zeilen — alle Edits auf präzise Zeilenbereiche beschränken.

    Eingriff 1 — SectionKey (Zeilen 121–132):
    Union-Typ um `| "readiness"` erweitern. Den Typ-Block als Ganzes ersetzen; nur `| "readiness"` als neues Element an letzter Position vor dem Semikolon einfügen. Exakt: nach `| "vorschlaege"` die Zeile `| "readiness"` anfügen.

    Eingriff 2 — MAIN_TABS (Zeilen 199–210):
    Neuen Eintrag als letzte Zeile des Arrays einfügen, vor der abschließenden `];`. Exakt: `{ key: "readiness", label: "Veröffentlichung" },` nach dem „anime-projekte"-Eintrag. Tab-Label: „Veröffentlichung" (korrekter Umlaut, per UI-SPEC).

    Eingriff 3 — canUseMainTab (Zeilen 216–250):
    Vor dem `default:`-Zweig einen neuen Case einfügen:
    `case "readiness": return capabilities.can_edit_group || capabilities.can_edit_notes;`
    Begründung per D-08: Composite aus edit_group (Grunddaten/Medien-Zugang) und edit_notes (Story-/Notizen-Zugang). Reine Mitgliedschaft allein (can_view_members) genügt NICHT.

    Eingriff 4a — Formular-Ausschluss (Zeilen 2396–2401):
    `readiness`-Tab dem bestehenden Ausschluss-Block hinzufügen:
    `activeMainTab !== "readiness" &&` — analog zu den bestehenden Zeilen.
    Ohne diesen Eingriff würde der readiness-Tab fälschlicherweise im Save-Formular gerendert.

    Eingriff 4b — Tab-Render-Zweig (nach Zeile 3492):
    Nach dem `vorschlaege`-Zweig einfügen:
    `{activeMainTab === "readiness" ? (<ReadinessTab fansubId={fansubID} group={group} />) : null}`
    `group`-Variable ist im page-State als `group` (FansubGroup | null) vorhanden — falls null, ReadinessTab nicht rendern: Bedingung `activeMainTab === "readiness" && group ? (<ReadinessTab fansubId={fansubID} group={group} />) : null`.

    Eingriff 4c — Import-Ergänzung (Zeilen 90–110, import-Abschnitt):
    `import { ReadinessTab } from "./ReadinessTab";` nach den bestehenden lokalen Importen hinzufügen.

    Keine weiteren Änderungen an page.tsx. Keine neue Logik, kein neuer State, keine neuen Fetch-Aufrufe direkt in page.tsx (Lock K).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/admin/fansubs/\\[id\\]/edit/ 2>&1 | tail -20</automated>
  </verify>
  <done>
    - page.tsx enthält `| "readiness"` in SectionKey-Typ
    - page.tsx enthält `{ key: "readiness", label: "Veröffentlichung" }` in MAIN_TABS
    - page.tsx enthält `case "readiness": return capabilities.can_edit_group || capabilities.can_edit_notes;` in canUseMainTab
    - page.tsx enthält `activeMainTab !== "readiness"` im Formular-Ausschluss-Block
    - page.tsx enthält `activeMainTab === "readiness" && group ? (<ReadinessTab fansubId={fansubID} group={group} />) : null`
    - page.tsx enthält `import { ReadinessTab } from "./ReadinessTab"`
    - Alle Tests in page.test.tsx grün (inkl. neue Req-F-Capability-Gating-Cases aus Plan 01)
    - Alle Tests im gesamten frontend/src/app/admin/fansubs/-Verzeichnis grün
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Vollständiger readiness-Tab im Workspace `/admin/fansubs/[id]/edit`:
    - Tab „Veröffentlichung" in der Tab-Leiste (nur sichtbar bei can_edit_group oder can_edit_notes)
    - Readiness-Checkliste mit 8 bewertbaren Kriterien (success/warning-Badges)
    - Informative Zähler für offene Claims/Vorschläge (info-Badge, keine warning-Farbe)
    - Alle Sprungmarken navigieren auf den zuständigen Tab
    - PublicPreviewPanel mit Fallback-Hinweis-Badge und FansubProfileTabs/GroupLeaderTimeline
  </what-built>
  <how-to-verify>
    1. Dev-Server unter :3000 starten (Hot-Reload)
    2. Als Admin oder fansub_lead einloggen
    3. `/admin/fansubs/[id]/edit` einer Gruppe aufrufen die `can_edit_group` oder `can_edit_notes` hat
    4. Tab „Veröffentlichung" in der Tab-Leiste sichtbar prüfen
    5. Tab anklicken → Readiness-Checkliste erscheint mit korrekten deutschen Labels und korrekten Umlauten
    6. Gruppe ohne logo_url → „Logo vorhanden" zeigt Badge „fehlt" (warning, kein danger/rot)
    7. Sprungmarke „Im Medien-Tab ergänzen →" anklicken → Workspace wechselt zu Tab „Medien" ohne Seitenneuladen
    8. Offene Claims-Zähler zeigt Badge variant=info (blaue Tönung, KEIN warning-Bernstein)
    9. Preview-Bereich zeigt FansubProfileTabs-Inhalt mit Fallback-Badge „Vorschau im Übergangsmodus"
    10. Als Nutzer ohne can_edit_group und ohne can_edit_notes → Tab „Veröffentlichung" NICHT in Tab-Leiste sichtbar
    11. Direkter URL-Aufruf `?tab=readiness` ohne Berechtigung → Workspace lädt anderen Tab (resolveMainTabForAccess-Fallback)
  </how-to-verify>
  <resume-signal>„approved" eingeben oder gefundene Probleme beschreiben</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Grenze | Beschreibung |
|--------|--------------|
| Browser → Workspace-Tab-Navigation | ?tab=readiness als URL-Parameter kommt vom Browser; resolveMainTabForAccess filtert unberechtigte Zugriffe |
| Capability-Check | canUseMainTab wertet FansubGroupCapabilities aus — Daten kommen vom Backend-Capability-Endpoint |

## STRIDE Threat Register

| Threat ID | Kategorie | Komponente | Disposition | Mitigationsplan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-05-EoP | Elevation of Privilege | ?tab=readiness-URL-Manipulation ohne Capability | mitigate | resolveMainTabForAccess (page.tsx Zeilen 261–268) fällt auf ersten erlaubten Tab zurück; neuer canUseMainTab-Case für "readiness" gibt false zurück bei fehlenden Capabilities [VERIFIED Andockpunkt] |
| T-77-06-ID | Information Disclosure | ReadinessTab aggregiert Member/Claims-Zähler | accept | Alle drei genutzten Seams (listGroupMembers, listPendingMemberClaims, getAdminFansubAnime) sind bereits durch bestehende Admin-Auth (FansubEditAccessGate) geschützt; keine neuen Datenquellen |
| T-77-07-EoP | Elevation of Privilege | Sprungmarke auf claims/mitglieder-Tab ohne Capability | accept | Sprungmarken ändern nur die URL; resolveMainTabForAccess und canUseMainTab prüfen die Capability für den Ziel-Tab — kein Capability-Bypass durch Sprungmarken-Klick möglich [VERIFIED: page.tsx resolveMainTabForAccess] |
| T-77-SC | Tampering | npm install | accept | Keine neuen Pakete (RESEARCH.md bestätigt); kein slopcheck nötig |
</threat_model>

<verification>
cd frontend && npx vitest run src/app/admin/fansubs/\\[id\\]/edit/ 2>&1 | tail -10
Erwartetes Ergebnis: alle Tests grün (0 failed).

cd frontend && npx vitest run 2>&1 | tail -10
Erwartetes Ergebnis: gesamte Suite grün.

powershell -Command "Select-String -Path 'frontend/src/app/admin/fansubs/[id]/edit/page.tsx' -Pattern 'readiness' | Select-Object -First 10"
Erwartetes Ergebnis: Mindestens 4 Treffer (SectionKey, MAIN_TABS, canUseMainTab, Render-Zweig).
</verification>

<success_criteria>
- page.tsx enthält alle fünf Eingriffe: SectionKey, MAIN_TABS, canUseMainTab-Case, Formular-Ausschluss, Render-Zweig, Import
- Tab „Veröffentlichung" in Workspace sichtbar für can_edit_group/can_edit_notes, unsichtbar für reine Mitgliedschaft
- Readiness-Checkliste zeigt 8 bewertbare + 2 informative Kriterien
- Alle Sprungmarken navigieren auf korrekten Ziel-Tab
- Alle unit-Tests grün (page.test.tsx inkl. Req-F-Cases, ReadinessTab.test.tsx inkl. alle sechs Cases)
- Gesamte Vitest-Suite grün vor /gsd:verify-work
- Checkpoint:human-verify bestätigt korrektes Tab-Gating und visuelle Konformität
</success_criteria>

<output>
Erstelle `.planning/phases/77-leader-workspace-public-preview-readiness/77-03-SUMMARY.md` wenn fertig
</output>
