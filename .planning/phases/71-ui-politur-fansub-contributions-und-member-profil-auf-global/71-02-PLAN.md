---
phase: 71
plan: 02
type: execute
wave: 2
depends_on: [71-01]
files_modified:
  - frontend/src/components/profile/memberBadgeLabels.ts
  - frontend/src/components/profile/MemberBadgeChips.tsx
  - frontend/src/components/profile/MemberBadgeHighlights.tsx
  - frontend/src/app/me/profile/components/AchievementBadgesCard.tsx
  - frontend/src/app/me/profile/components/AchievementBadgesCard.test.tsx
  - .planning/phases/71-ui-politur-fansub-contributions-und-member-profil-auf-global/71-02-SUMMARY.md
autonomous: true
requirements:
  - P71-SC3
  - P71-SC4
  - D-10
  - D-12
  - D-13

must_haves:
  truths:
    - "D-10: Public/display badge chips do not render owner mutation controls such as Ausblenden"
    - "D-10: Badge visibility remains editable in /me/profile through AchievementBadgesCard"
    - "D-12: Fixed badge_code values render with lucide-react icons plus global Badge variants"
    - "D-13: This plan only touches badge surfaces in Phase 71 scope, not all public/profile UI"
    - "P71-SC3: Badge-Chip management is edit-context only, not public/display inline mutation"
    - "P71-SC4: Display surfaces show; edit surfaces edit"
    - "Touched user-facing German strings use correct umlauts"
  artifacts:
    - path: "frontend/src/components/profile/memberBadgeLabels.ts"
      provides: "Shared badge label/presentation metadata without text glyph prefixes"
    - path: "frontend/src/components/profile/MemberBadgeChips.tsx"
      provides: "Read-only display badge chips"
    - path: "frontend/src/components/profile/MemberBadgeHighlights.tsx"
      provides: "Public badge highlights using global Badge presentation"
    - path: "frontend/src/app/me/profile/components/AchievementBadgesCard.tsx"
      provides: "Edit-context badge visibility management with shared presentation"
---

<objective>
Move badge mutation fully into the owner edit context and upgrade derived badges to shared icon/color presentation.

Public/display badge components must show badges only. `/me/profile` remains the place where badge visibility can be changed.
</objective>

<context>
@.planning/phases/71-ui-politur-fansub-contributions-und-member-profil-auf-global/71-CONTEXT.md
@.planning/phases/71-ui-politur-fansub-contributions-und-member-profil-auf-global/71-UI-SPEC.md
@.planning/phases/71-ui-politur-fansub-contributions-und-member-profil-auf-global/71-RESEARCH.md
@frontend/src/components/ui/index.ts
@frontend/src/app/me/profile/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce shared badge presentation metadata</name>
  <files>frontend/src/components/profile/memberBadgeLabels.ts</files>
  <read_first>
    - frontend/src/components/profile/memberBadgeLabels.ts
    - frontend/src/components/ui/Badge.tsx
    - frontend/src/types/contributions.ts
    - frontend/src/types/profile.ts
  </read_first>
  <action>
Refactor `memberBadgeLabels.ts` so label text no longer contains glyph prefixes. Keep `formatMemberBadgeLabel(code)` returning plain German label text.

Add an exported helper such as `getMemberBadgePresentation(code)` or exported metadata map that provides:

- `label`
- `variant` compatible with the global `Badge` primitive
- `Icon` from `lucide-react`

Use the Phase 71 UI-SPEC mapping:

- `founding_member` -> warning + `Crown`
- `historical_leader` -> info + `Shield`
- `long_term_member` -> success + `CalendarClock`
- `first_contribution` -> neutral + `Sparkles`
- `verified` -> success + `BadgeCheck`
- productive badges -> muted/neutral/warning + `Layers`
- `all_rounder` -> info + `Hexagon`

Do not add backend fields or asset uploads.
  </action>
  <verify>
    <automated>rg -n "â˜…|â™¦|â—†|âœ¦|âœ“|★|♦|◆|✦|✓" frontend/src/components/profile/memberBadgeLabels.ts</automated>
  </verify>
  <done>Badge labels are plain text; presentation metadata carries icon/variant.</done>
</task>

<task type="auto">
  <name>Task 2: Make MemberBadgeChips display-only</name>
  <files>frontend/src/components/profile/MemberBadgeChips.tsx</files>
  <read_first>
    - frontend/src/components/profile/MemberBadgeChips.tsx
    - frontend/src/app/members/[slug]/page.tsx
    - frontend/src/app/me/profile/page.tsx
  </read_first>
  <action>
Remove mutation behavior from `MemberBadgeChips`:

- remove `useState` if no longer needed;
- remove `Button` import;
- remove `patchMyBadgeVisibility` import;
- remove `token` and `onVisibilityChanged` props if no current caller still needs them;
- remove the `Ausblenden` button and error state;
- render global `Badge` instances with icon + label from the shared presentation metadata.

Keep visibility filtering:

- own profile display can see non-hidden badges if this component is still used there;
- public display sees only `visibility === 'public'`.
  </action>
  <verify>
    <automated>rg -n "patchMyBadgeVisibility|Ausblenden|badgeHideBtn|badgeError|onVisibilityChanged|token\\?" frontend/src/components/profile/MemberBadgeChips.tsx</automated>
  </verify>
  <done>`MemberBadgeChips` is display-only and has no direct badge visibility mutation path.</done>
</task>

<task type="auto">
  <name>Task 3: Apply badge presentation to public highlights and edit manager</name>
  <files>
    frontend/src/components/profile/MemberBadgeHighlights.tsx
    frontend/src/app/me/profile/components/AchievementBadgesCard.tsx
    frontend/src/app/me/profile/components/AchievementBadgesCard.test.tsx
  </files>
  <read_first>
    - frontend/src/components/profile/MemberBadgeHighlights.tsx
    - frontend/src/app/me/profile/components/AchievementBadgesCard.tsx
    - frontend/src/app/me/profile/components/AchievementBadgesCard.test.tsx
    - frontend/src/components/ui/Badge.tsx
  </read_first>
  <action>
Use the shared presentation metadata in both public highlights and the edit manager.

For `MemberBadgeHighlights`:

- render each badge with global `Badge` where feasible;
- show icon + label;
- keep existing memorial gamification filtering and show-more behavior.

For `AchievementBadgesCard`:

- keep visibility `Select` inside `FormField`;
- show icon + label for the badge identity;
- keep the visibility status `Badge`;
- update tests to expect plain label text and edit-context visibility controls.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- src/app/me/profile/components/AchievementBadgesCard.test.tsx</automated>
  </verify>
  <done>Badge presentation is shared and edit controls remain only in `/me/profile`.</done>
</task>

<task type="auto">
  <name>Task 4: Write Plan 02 summary</name>
  <files>.planning/phases/71-ui-politur-fansub-contributions-und-member-profil-auf-global/71-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/71-ui-politur-fansub-contributions-und-member-profil-auf-global/71-02-PLAN.md
  </read_first>
  <action>
Write `71-02-SUMMARY.md` with changed files, behavior, checks, and any remaining badge presentation risks.
  </action>
  <verify>
    <automated>Test-Path .planning/phases/71-ui-politur-fansub-contributions-und-member-profil-auf-global/71-02-SUMMARY.md</automated>
  </verify>
  <done>Summary exists.</done>
</task>

</tasks>

<verification>
- `npm --prefix frontend run test -- src/app/me/profile/components/AchievementBadgesCard.test.tsx`
- `npm --prefix frontend run typecheck`
- `npm --prefix frontend run lint`
- `rg -n "patchMyBadgeVisibility|Ausblenden" frontend/src/components/profile/MemberBadgeChips.tsx`
- `git diff --check`
</verification>

<success_criteria>
Public/display badge chips and highlights are read-only, `/me/profile` remains the badge visibility editor, and fixed badge codes have icon + color presentation without new backend or upload work.
</success_criteria>

<output>
Create `.planning/phases/71-ui-politur-fansub-contributions-und-member-profil-auf-global/71-02-SUMMARY.md`.
</output>
