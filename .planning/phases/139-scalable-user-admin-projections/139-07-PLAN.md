---
phase: 139-scalable-user-admin-projections
plan: 07
type: execute
wave: 5
depends_on: ["139-02", "139-05"]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx
  - frontend/src/app/admin/users/tabs/UserGroupRightsTab.test.tsx
  - frontend/src/app/admin/users/tabs/UserOverviewTab.tsx
  - frontend/src/app/admin/users/tabs/UserOverviewTab.test.tsx
  - frontend/src/app/admin/users/[id]/UserDetailPageClient.tsx
autonomous: true
requirements: [UADM-06, UADM-07]

must_haves:
  truths:
    - "D22/F-01: UserGroupRightsTab.tsx no longer eagerly Promise.all-fetches getEffectiveRights for every membership on mount — a group's rights are fetched only after that ONE group is selected/expanded"
    - "F-01: UserOverviewTab.tsx's GroupRightsSummarySection no longer performs the byte-identical eager fan-out (membershipsResp.memberships.map(getEffectiveRights)) — it calls the new batched getAdminUserRightsSummary endpoint (139-05) exactly once"
    - "Phase-138 D-05 parity: GroupSummaryCard's rendered content (role label, first-3 headline states, hasDeviation flag, openClaimsCount) stays visually and textually unchanged — only its data source changes"
    - "Deep-link preservation: ?tab=roles-rights&group={fansubGroupId} still pre-selects and eagerly fetches exactly that ONE group's rights on mount, never a fan-out over all memberships"
    - "UADM-07: UserGroupRightsTab.tsx's SectionHeader gains the locked 'Aktionsfähig — hier werden persönliche Rechteabweichungen für eine ausgewählte Gruppe geprüft und geändert.' description, since it currently has no purpose banner at all"
    - "UserGroupRightsTab.test.tsx no longer contains an assertion that mockGetEffectiveRights is called with two different group IDs simultaneously on initial render (the old multi-group eager-fetch behavior D22 removes) — its rewritten assertions match lazy single-group selection instead"
    - "UserOverviewTab.test.tsx's vi.mock('@/lib/api', ...) exports getAdminUserRightsSummary, and at least one test asserts it is called exactly once while getEffectiveRights is never called from this tab (closing the Wave-0 gap for F-01 Overview-tab test coverage)"
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "getAdminUserContributions/getAdminUserMedia paginated signatures, new getAdminUserRightsSummary, getAdminUserGroupMemberships(limit,offset)"
  key_links:
    - from: "frontend/src/app/admin/users/tabs/UserOverviewTab.tsx"
      to: "frontend/src/lib/api.ts"
      via: "getAdminUserRightsSummary(userId)"
      pattern: "getAdminUserRightsSummary"
    - from: "frontend/src/app/admin/users/[id]/UserDetailPageClient.tsx"
      to: "frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx"
      via: "initialGroupId prop threaded from searchParams.get('group')"
      pattern: "initialGroupId"
    - from: "frontend/src/app/admin/users/tabs/UserOverviewTab.test.tsx"
      to: "frontend/src/app/admin/users/tabs/UserOverviewTab.tsx"
      via: "mockGetAdminUserRightsSummary assertion proving exactly-once batched fetch, zero getEffectiveRights calls"
      pattern: "mockGetAdminUserRightsSummary"
---

<objective>
Close F-01's two known frontend fan-out locations for real: `UserGroupRightsTab.tsx`'s eager
`Promise.all(getEffectiveRights per group)` becomes lazy-on-select (D22), and
`UserOverviewTab.tsx`'s byte-identical fan-out is replaced with one call to 139-05's new batched
`GET /admin/users/:userId/rights-summary` endpoint. Also wires `frontend/src/lib/api.ts` to the new
paginated contributions/media/rights-summary/group-memberships backend shapes 139-03/139-04/139-05
built, so 139-08/139-09's tab rewrites have real functions to call. Both tabs' EXISTING test files
(Phase 138) currently assert the eager fan-out behavior this plan removes — this plan rewrites
those assertions in the same tasks that change the behavior, not as a follow-up, so the test suite
never asserts a contradiction with the shipped code.

Purpose: UADM-06 is only honestly satisfied once BOTH fan-out call sites are gone — this is the
plan that closes the SECOND one (Overview), the exact gap the external review package's plan set
missed entirely (only covered UserGroupRightsTab.tsx). This plan is also where 139-VALIDATION.md's
Wave-0 gap ("Frontend: new test coverage for the F-01 Overview-tab batched-summary fetch replacing
the fan-out") gets closed, by rewriting UserOverviewTab.test.tsx's assertions to pin the new
batched call.

Output: `api.ts` gains/changes four functions. `UserGroupRightsTab.tsx` gains a bounded group
selector and lazy per-group fetch, and its test file's assertions are rewritten to match.
`UserOverviewTab.tsx`'s `GroupRightsSummarySection` swaps its data source, and its test file's mock
and assertions are rewritten to match. `UserDetailPageClient.tsx` threads the `?group=` deep-link
param through.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-scalable-user-admin-projections/139-CONTEXT.md
@.planning/phases/139-scalable-user-admin-projections/139-DISCUSS.md
@.planning/phases/139-scalable-user-admin-projections/139-RESEARCH.md
@.planning/phases/139-scalable-user-admin-projections/139-UI-SPEC.md
@.planning/phases/139-scalable-user-admin-projections/139-PATTERNS.md
</context>

<interfaces>
<!-- New backend endpoints/shapes this plan's api.ts functions call, from 139-01/139-03/139-04/139-05 -->
GET /api/v1/admin/users/:userId/contributions?anime_id=&fansub_group_id=&role_code=&only_deviations=&from=&to=&limit=&offset=
  -> AdminUserContributionsPage { data, meta, filter_options }
GET /api/v1/admin/users/:userId/media?anime_id=&fansub_group_id=&release_version_id=&media_type=&from=&to=&limit=&offset=
  -> AdminUserMediaPage { data, meta, filter_options }
GET /api/v1/admin/users/:userId/rights-summary?limit=&offset=
  -> AdminUserRightsSummaryPage { data: AdminUserGroupRightsSummaryItem[], meta }
GET /api/v1/admin/users/:userId/group-memberships?limit=&offset= (139-05 added pagination)
  -> existing shape + additive meta field

<!-- Existing single-group endpoint, UNCHANGED, both fixes below just change WHEN this is called -->
export async function getEffectiveRights(fansubGroupId: number, appUserId: number): Promise<EffectiveRightState[]>

<!-- listClaims query-building + envelope-typed fetch pattern to copy (api.ts:3868-3892) -->
const query = new URLSearchParams(); if (params.x != null) query.set('x', String(params.x)); ...
const response = await apiClientFetch(`/api/v1/...?${query.toString()}`, { cache: 'no-store' })
if (!response.ok) { const parsed = await parseApiErrorPayload(...); throw new ApiError(...) }
return response.json()

<!-- UserGroupRightsTab.tsx's current eager fan-out (lines 71-124) this plan replaces -->
loadData(): Promise.all([getAdminUserGroupMemberships(userId), listRoleCapabilities()]) then
  Promise.all(memberships.map(m => getEffectiveRights(m.fansub_group_id, userId)))  // <- D22 violation

<!-- UserOverviewTab.tsx's GroupRightsSummarySection current byte-identical fan-out (lines 132-153) -->
loadSummary(): same shape, membershipsResp.memberships.map(m => getEffectiveRights(...))  // <- F-01

<!-- GroupSummaryCard's LOCKED render shape (Phase 138 D-05), do not change (lines 90-125) -->
GroupSummaryCardProps { membership, states: EffectiveRightState[], actionLabels, matrix, openClaimsCount }
headlineStates = states.slice(0, HEADLINE_CAPABILITY_LIMIT); hasDeviation = states.some(s => s.user_allow || s.user_deny)

<!-- Existing test file assertions this plan MUST rewrite, not merely leave stale
     (UserGroupRightsTab.test.tsx:237-269, "rendert zwei unabhängige Gruppensektionen...") -->
it('rendert zwei unabhängige Gruppensektionen ohne Vermischung der Capabilities (D-11)', async () => {
  // seeds 2 memberships, asserts mockGetEffectiveRights is called with BOTH group IDs
  // (toHaveBeenCalledWith(1,1) AND toHaveBeenCalledWith(2,1)) and BOTH GroupSections render
  // simultaneously -- this directly contradicts D22's lazy-single-selection rewrite and MUST be
  // rewritten to assert the NEW behavior (only the selected group's rights are fetched/rendered).
})

<!-- Existing test file mock this plan MUST extend, not merely leave incomplete
     (UserOverviewTab.test.tsx:20-31, vi.mock('@/lib/api', ...)) -->
vi.mock('@/lib/api', () => ({
  getAdminUserOverview: ..., getAdminUserGroupMemberships: ..., getEffectiveRights: ...,
  listRoleCapabilities: ..., updateAdminUserStatus: ...,
  // getAdminUserRightsSummary is NOT exported here today -- after Task 3 replaces loadSummary's
  // body with a call to it, every test in this file hits a runtime error from the unmocked
  // function unless this mock block is extended.
}))
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: api.ts — paginated contributions/media, new rights-summary, paginated group-memberships</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    frontend/src/lib/api.ts (locate via grep: getAdminUserContributions, getAdminUserMedia,
    getAdminUserGroupMemberships, getEffectiveRights, listClaims — read each function's exact
    current body; listClaims's query-building convention at ~lines 3868-3892 is the pattern every
    new/changed function here must follow)
    frontend/src/types/admin-users.ts (139-02's new AdminUserContributionsPage/
    AdminUserContributionsParams/AdminUserMediaPage/AdminUserMediaParams/
    AdminUserRightsSummaryPage types this task imports)
  </read_first>
  <action>
    Change `getAdminUserContributions(userId: number)` to `getAdminUserContributions(userId:
    number, params: AdminUserContributionsParams = {}): Promise<AdminUserContributionsPage>` —
    build a `URLSearchParams` from every field on `params` (only set when not
    `undefined`/`null`/empty string, matching `listClaims`'s exact null-check convention), fetch
    `/api/v1/admin/users/${userId}/contributions?${query.toString()}`, same error-handling
    (`parseApiErrorPayload`/`ApiError`) as every other function in this file, return
    `response.json() as Promise<AdminUserContributionsPage>`.

    Change `getAdminUserMedia(userId: number)` to `getAdminUserMedia(userId: number, params:
    AdminUserMediaParams = {}): Promise<AdminUserMediaPage>`, identical shape.

    Add `export async function getAdminUserRightsSummary(userId: number, limit?: number, offset?:
    number): Promise<AdminUserRightsSummaryPage>` — same fetch/error-handling shape, query params
    `limit`/`offset` only when provided.

    Change `getAdminUserGroupMemberships(userId: number)` to accept optional `limit?: number,
    offset?: number` parameters, appended as query params when provided (backward compatible —
    calling with no params still works, matching 139-05's additive backend change).

    Do NOT modify `getEffectiveRights` — it stays exactly as-is, only its call sites' TIMING
    changes in Tasks 2/3.
  </action>
  <acceptance_criteria>
    <source_assertion>api.ts's getAdminUserContributions signature accepts a second params
    argument typed AdminUserContributionsParams with a default value of {}</source_assertion>
    <source_assertion>api.ts exports a new function getAdminUserRightsSummary</source_assertion>
    <test_command>docker exec team4sv30-frontend sh -c "cd /app && npx tsc --noEmit"</test_command>
  </acceptance_criteria>
  <verify>
    <automated>docker exec team4sv30-frontend sh -c "cd /app && npx tsc --noEmit"</automated>
  </verify>
  <done>All four api.ts functions compile against the new backend shapes; tsc clean project-wide
  (any pre-existing unrelated tsc errors are out of scope, per CONTEXT.md's baseline-debt note —
  confirm via `git stash` + re-run tsc that any remaining errors predate this task before treating
  them as this task's responsibility).</done>
</task>

<task type="auto">
  <name>Task 2: UserGroupRightsTab.tsx — lazy per-group fetch (D22) + deep-link + UADM-07 banner + test rewrite</name>
  <files>
    frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx,
    frontend/src/app/admin/users/tabs/UserGroupRightsTab.test.tsx,
    frontend/src/app/admin/users/[id]/UserDetailPageClient.tsx
  </files>
  <read_first>
    frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx (full file, 269 lines — loadData at
    lines 71-124 is the function this task restructures; the render block at lines 197-220 maps
    over ALL memberships rendering a full GroupSection per membership today — this task changes it
    to render a selector + only the SELECTED group's GroupSection)
    frontend/src/app/admin/users/tabs/UserGroupRightsTab.test.tsx (full file — the test at lines
    237-269, "rendert zwei unabhängige Gruppensektionen ohne Vermischung der Capabilities (D-11)",
    seeds 2 memberships and asserts `mockGetEffectiveRights` is called with BOTH group IDs
    (`toHaveBeenCalledWith(1, 1)` AND `toHaveBeenCalledWith(2, 1)`) with BOTH `GroupSection`s
    rendering simultaneously — this pins the OLD eager fan-out and directly contradicts this
    task's D22 rewrite; it MUST be rewritten, not left in place, or it fails against the new code.
    The other tests in this file that seed exactly ONE membership (lines 115-235) are compatible
    with lazy-select as-is IF that single membership auto-selects on mount — decide during
    implementation whether a single-membership response auto-selects (recommended, since D-11's
    multi-group isolation intent is preserved and the existing single-membership tests keep
    passing unchanged) and document that choice in the SUMMARY)
    frontend/src/app/admin/users/[id]/UserDetailPageClient.tsx (lines 80-165 or wherever
    searchParams/tab logic lives — confirm there is currently NO `group` search param read
    anywhere in this file; `<UserGroupRightsTab userId={userId} />` at ~line 163 is the call site
    this task adds a new prop to)
    frontend/src/components/ui (Button — for the collapsed membership-row selector, per UI-SPEC:
    "Button variant='ghost', full-width, one per membership")
  </read_first>
  <action>
    In `UserDetailPageClient.tsx`: read `const initialGroupId = searchParams.get('group')` (parse
    to `number | undefined`), pass it as a new prop `<UserGroupRightsTab userId={userId}
    initialGroupId={initialGroupId} />`.

    In `UserGroupRightsTab.tsx`: accept `initialGroupId?: number` in `Props`. Add
    `selectedGroupId: number | null` state (initialized `null`, set from `initialGroupId` once
    memberships load and that ID is confirmed present among them; if `initialGroupId` is absent AND
    exactly one membership is returned, auto-select that one membership so the existing
    single-membership test coverage keeps passing unchanged). Restructure `loadData` to fetch ONLY
    `getAdminUserGroupMemberships(userId)` + `listRoleCapabilities()` on mount (remove the
    `Promise.all(memberships.map(getEffectiveRights))` fan-out entirely — this is the D22 fix).
    Keep `rightsByGroup: Record<number, EffectiveRightState[]>` as local cache state, but populate
    it lazily: add a new `loadRightsForGroup(groupId: number)` callback that calls
    `getEffectiveRights(groupId, userId)` for exactly that one group and merges the result into
    `rightsByGroup` — call this (a) once, automatically, when `selectedGroupId` resolves from
    `initialGroupId` (or single-membership auto-select) on mount, and (b) whenever the admin
    selects a different group AND that group's rights are not already cached in `rightsByGroup`.
    Do not re-fetch an already-cached group's rights on re-selection.

    Replace the current "map over ALL memberships, render a GroupSection per membership" render
    block with: a collapsed selector (one `Button variant="ghost"` full-width row per membership,
    per UI-SPEC — showing group name + role label, `onClick` sets `selectedGroupId` and triggers
    `loadRightsForGroup` if not cached) followed by, ONLY when `selectedGroupId !== null`, the
    existing `GroupSection` component rendered for JUST that one membership (same props it
    receives today, just for one membership instead of mapped over all). If the paginated
    `getAdminUserGroupMemberships` response's `meta.total` exceeds the returned page size, render
    the existing `Pagination` primitive beneath the selector list (per UI-SPEC's "Membership list
    pagination (if the membership count itself is unbounded)").

    Add `description="Aktionsfähig — hier werden persönliche Rechteabweichungen für eine
    ausgewählte Gruppe geprüft und geändert."` to the existing `<SectionHeader title="Effektive
    Rechte nach Gruppe" />` call (currently has no `description` prop at all — this closes UADM-07
    for this tab).

    `handleMutated` (the existing post-mutation reload callback) must re-fetch ONLY the currently
    selected group's rights (not re-trigger the old eager fan-out) — change its body from `void
    loadData()` to re-invoke `loadRightsForGroup(selectedGroupId)` when a group is selected, plus
    a lightweight membership-list refresh if the mutation could have changed role/status (matches
    the existing "immediately show new state" comment's intent without reverting to eager
    fan-out).

    In `UserGroupRightsTab.test.tsx`: rewrite the "rendert zwei unabhängige Gruppensektionen ohne
    Vermischung der Capabilities (D-11)" test (lines 237-269) so it matches the NEW lazy-select
    behavior instead of the old eager fan-out: seed 2 memberships, assert that on initial render
    `mockGetEffectiveRights` is called ZERO times (no group auto-selected when there is more than
    one membership and no `initialGroupId`), assert both membership rows/selector buttons are
    visible (group names "Sakura-Fansub" and "New-Subs" both rendered in the selector), then
    simulate selecting the first group and assert `mockGetEffectiveRights` is called with
    `(1, 1)` exactly once and that group's `GroupSection` content appears; then simulate selecting
    the second group and assert `mockGetEffectiveRights` is called with `(2, 1)` and the FIRST
    group's content is no longer the one attributed to it being fetched a second time (cache reuse
    — selecting back to group 1 afterward must NOT call `mockGetEffectiveRights` with `(1, 1)`
    again). Keep the D-11 isolation intent (capabilities from one group never bleed into another's
    rendered section) as the core assertion, just re-scoped to lazy single-selection instead of
    simultaneous multi-section rendering. Leave every other existing test in this file (single
    membership, decisive_source formatting, granting_roles join, provenance disclosure, role
    assignment) unchanged unless the auto-select decision above requires a minor adjustment to
    make the single-membership tests' existing `mockGetEffectiveRights` assertions still pass.
  </action>
  <acceptance_criteria>
    <source_assertion>UserGroupRightsTab.tsx's loadData function no longer contains
    "memberships.map" chained with "getEffectiveRights" inside a Promise.all</source_assertion>
    <source_assertion>UserGroupRightsTab.tsx declares a selectedGroupId state variable and a
    loadRightsForGroup function that calls getEffectiveRights for exactly one group</source_assertion>
    <source_assertion>UserGroupRightsTab.tsx's SectionHeader call includes a description prop
    containing the string "Aktionsfähig"</source_assertion>
    <source_assertion>UserDetailPageClient.tsx passes an initialGroupId prop to
    UserGroupRightsTab</source_assertion>
    <source_assertion>UserGroupRightsTab.test.tsx's rewritten D-11 test no longer asserts
    mockGetEffectiveRights was called with both group 1 and group 2 as part of the SAME initial
    render/waitFor block with no intervening selection action</source_assertion>
    <test_command>docker exec team4sv30-frontend sh -c "cd /app && npx vitest run src/app/admin/users/tabs/UserGroupRightsTab 2>&1 | tail -60"</test_command>
  </acceptance_criteria>
  <verify>
    <automated>docker exec team4sv30-frontend sh -c "cd /app && npx tsc --noEmit && npx vitest run src/app/admin/users"</automated>
  </verify>
  <done>Rights tab no longer fetches any group's rights until that group is selected; deep-link
  pre-selection still works with exactly one fetch; UserGroupRightsTab.test.tsx's assertions match
  the new lazy-select behavior (no test asserts the old simultaneous multi-group fan-out); Guided
  Revoke/Grant/RoleAssignmentImpactModal wiring (D21, out of scope) remains completely
  untouched.</done>
</task>

<task type="auto">
  <name>Task 3: UserOverviewTab.tsx — replace the fan-out with the batched rights-summary call + test rewrite</name>
  <files>
    frontend/src/app/admin/users/tabs/UserOverviewTab.tsx,
    frontend/src/app/admin/users/tabs/UserOverviewTab.test.tsx
  </files>
  <read_first>
    frontend/src/app/admin/users/tabs/UserOverviewTab.tsx (full file, 440 lines —
    GroupRightsSummarySection's loadSummary at lines 139-165ish is the ENTIRE function this task
    replaces; GroupSummaryCard at lines 90-125 stays byte-identical — read it to confirm exactly
    which props it needs so the replacement loadSummary supplies the same shape)
    frontend/src/app/admin/users/tabs/UserOverviewTab.test.tsx (full file — the `vi.mock('@/lib/api',
    ...)` block at lines 20-31 does NOT export `getAdminUserRightsSummary`; after this task's
    `loadSummary` rewrite calls it, every test in this file hits a runtime "not a function" error
    from the unmocked import unless this mock block is extended with a new
    `mockGetAdminUserRightsSummary` entry. This file's existing tests also configure
    `mockGetAdminUserGroupMemberships`/`mockGetEffectiveRights` return values feeding the OLD
    fan-out — those setup calls and any assertions on `mockGetEffectiveRights` being called per
    group must be rewritten to instead configure/assert `mockGetAdminUserRightsSummary`)
    frontend/src/types/admin-users.ts (139-02's AdminUserGroupRightsSummaryItem/
    AdminHeadlineCapabilityState types — the new response shape this task consumes)
  </read_first>
  <action>
    Replace `GroupRightsSummarySection`'s `loadSummary` function body: instead of
    `getAdminUserGroupMemberships` + `Promise.all(getEffectiveRights per group)`, call the new
    `getAdminUserRightsSummary(userId)` (Task 1) exactly once. Since `AdminUserGroupRightsSummaryItem`
    already carries `HeadlineStates` (pre-sliced to 3) and `HasDeviation` (pre-computed over the
    full evaluated action set, per 139-05's backend), `GroupSummaryCard`'s rendering no longer
    needs the raw `EffectiveRightState[]`/`HEADLINE_CAPABILITY_LIMIT`/`.slice(0, 3)`/`.some(user_allow
    || user_deny)` client-side logic — adapt `GroupSummaryCard`'s props to accept the new
    pre-computed shape directly (`headlineStates: AdminHeadlineCapabilityState[]`, `hasDeviation:
    boolean` as direct props instead of deriving them from a raw `states` array), while keeping
    every RENDERED string/element (role label join, `✓/✕ {label}` lines, "Persönliche
    Rechteabweichungen vorhanden"/"Keine ..." text, open-claims line) byte-for-byte identical to
    today's output — this is a data-source swap, not a visual change (Phase 138 D-05 locked).
    `matrix`/`listRoleCapabilities()` may still be fetched alongside if `roleLabelFor` still needs
    it for the role label join (verify whether 139-05's backend already resolves role labels
    server-side per its own plan's documented choice — if so, drop the separate
    `listRoleCapabilities()` call entirely here too; if the backend intentionally left role codes
    unresolved, keep fetching the matrix once, unchanged).

    If `AdminUserGroupRightsSummaryItem`'s membership count is paginated (139-05), thread that
    pagination through the same way Task 2 does for the Rights tab (a `Pagination` primitive below
    the summary cards when `meta.total` exceeds the page).

    In `UserOverviewTab.test.tsx`: extend the `vi.mock('@/lib/api', ...)` block with a new
    `getAdminUserRightsSummary: (...args: unknown[]) => mockGetAdminUserRightsSummary(...args)`
    entry (declaring `const mockGetAdminUserRightsSummary = vi.fn()` alongside the file's other
    mock function declarations). Rewrite every existing test in this file that currently configures
    `mockGetAdminUserGroupMemberships`/`mockGetEffectiveRights` to drive
    `GroupRightsSummarySection`'s rendering: replace those setups with
    `mockGetAdminUserRightsSummary.mockResolvedValueOnce(...)` returning an
    `AdminUserRightsSummaryPage`-shaped payload whose `HeadlineStates`/`HasDeviation`/`RoleLabel`/
    `OpenClaimsCount` fields produce the SAME rendered text these tests already assert on (role
    label, ✓/✕ lines, deviation/no-deviation text, open-claims line) — this proves Phase 138 D-05
    parity holds through the data-source swap. Add at least one NEW test (or extend an existing
    one) that explicitly asserts `mockGetAdminUserRightsSummary` is called exactly once and
    `mockGetEffectiveRights` is never called anywhere in this tab's render/mount cycle — this is
    the F-01 Overview-tab regression guard 139-VALIDATION.md's Wave-0 gap requires.
  </action>
  <acceptance_criteria>
    <source_assertion>UserOverviewTab.tsx's GroupRightsSummarySection no longer contains
    "getEffectiveRights" anywhere in its loadSummary function</source_assertion>
    <source_assertion>UserOverviewTab.tsx calls getAdminUserRightsSummary exactly once inside
    loadSummary</source_assertion>
    <source_assertion>GroupSummaryCard's rendered JSX still contains the literal strings
    "Persönliche Rechteabweichungen vorhanden" and "Keine persönlichen Rechteabweichungen"
    unchanged</source_assertion>
    <source_assertion>UserOverviewTab.test.tsx's vi.mock('@/lib/api', ...) block exports
    getAdminUserRightsSummary</source_assertion>
    <source_assertion>UserOverviewTab.test.tsx contains an assertion that
    mockGetAdminUserRightsSummary was called exactly once (toHaveBeenCalledTimes(1)) and a
    separate assertion that mockGetEffectiveRights was never called
    (not.toHaveBeenCalled())</source_assertion>
    <test_command>docker exec team4sv30-frontend sh -c "cd /app && npx vitest run src/app/admin/users/tabs/UserOverviewTab 2>&1 | tail -60"</test_command>
  </acceptance_criteria>
  <verify>
    <automated>docker exec team4sv30-frontend sh -c "cd /app && npx tsc --noEmit && npx vitest run src/app/admin/users"</automated>
  </verify>
  <done>Overview tab (the default tab every admin sees) issues exactly one rights-related network
  call on mount instead of 1+N; GroupSummaryCard's visible content is provably unchanged
  (Phase 138 D-05 parity, no silent semantic change); UserOverviewTab.test.tsx's mock and
  assertions match the new batched call, including an explicit exactly-once/never-called guard
  pair (closes 139-VALIDATION.md's Wave-0 frontend test-coverage gap for F-01's Overview-tab
  fix).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser -> new/changed api.ts functions | No new trust boundary — every function still routes through apiClientFetch's existing central refresh-capable session/request path (CLAUDE.md's "no component reads tokens or constructs bearer headers" constraint, unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-14 | Information Disclosure | initialGroupId deep-link param | accept | the group ID from the URL only selects WHICH already-authorized group's data to fetch; the backend's existing requirePlatformAdminIdentity gate and per-group authorization are unchanged and still enforced server-side — the client cannot use this param to access anything the backend would not already permit |
</threat_model>

<verification>
`docker exec team4sv30-frontend sh -c "cd /app && npx tsc --noEmit && npx vitest run src/app/admin/users"` green (accounting for the documented pre-existing baseline debt in CONTEXT.md — no
NEW failure in any file this plan touches).
</verification>

<success_criteria>
Both F-01 fan-out locations (Rights tab, Overview tab) are closed. Overview tab's default-view
network cost is O(1) rights-related requests, not O(N) groups. Deep-link and Phase-138 D-05 visual
parity both hold. Both tabs' existing test files (Phase 138) are rewritten in the same tasks that
change behavior, so no test in the suite asserts the old eager-fan-out shape after this plan lands.
</success_criteria>

<output>
Create `.planning/phases/139-scalable-user-admin-projections/139-07-SUMMARY.md` when done
</output>
</output>
