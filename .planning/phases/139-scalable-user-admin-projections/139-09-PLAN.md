---
phase: 139-scalable-user-admin-projections
plan: 09
type: execute
wave: 6
depends_on: ["139-02", "139-07"]
files_modified:
  - frontend/src/app/admin/users/tabs/UserMediaTab.tsx
  - frontend/src/app/admin/users/tabs/mediaTab.module.css
  - frontend/src/app/admin/users/tabs/UserMediaTab.test.tsx
autonomous: true
requirements: [UADM-05, UADM-06, UADM-07, UADM-08]

must_haves:
  truths:
    - "D11/D12: every rendered item is grouped under a Release/Episode block (Anime -> Projekt/Fansubgruppe -> Release/Episode -> Medien), never a flat list"
    - "D13: the result-count Badge and Pagination both reflect Meta.Total counting release/episode blocks, not raw media items"
    - "D15/D16: exactly one canonical action per block, Button variant=primary 'Release-Medien öffnen' linking to /me/releases/{releaseVersionId}/workspace — no inline edit/upload/delete/reorder control exists anywhere in this tab"
    - "D17: thumbnails use ResponsiveImage with loading=lazy and reserved aspect-ratio geometry, never the original full-size asset for list display"
    - "D18: no physical file path, storage id, derivative-file inventory, image-size/format analysis, or missing-file health indicator is ever rendered anywhere in this tab — that diagnostic depth is explicitly reserved for a later Metabase/reporting idea (D27), not built here even in a small form"
    - "D19: the hasScopePermission()-derived 'Berechtigung aktiv/fehlt' badge and the groupByReleaseVersion() client-side grouping function are both deleted outright, with no replacement — and no raw release_version:<id> string is ever rendered"
    - "D14/D23: every filter (Anime, Projekt/Gruppe, Release/Episode, Medientyp, Zeitraum) writes to the URL via useUserMediaFilters and triggers a server refetch"
    - "UADM-08/D26: the same container-query mechanism as the Contributions tab (760px/600px breakpoints), with thumbnails shrinking 96x96 -> 64x64 at <=600px"
  artifacts:
    - path: "frontend/src/app/admin/users/tabs/UserMediaTab.tsx"
      provides: "Full rewrite: grouped release/episode-block projection replacing groupByReleaseVersion()"
    - path: "frontend/src/app/admin/users/tabs/mediaTab.module.css"
      provides: "Container-query responsive rules including the 96px/64px thumbnail breakpoint"
  key_links:
    - from: "frontend/src/app/admin/users/tabs/UserMediaTab.tsx"
      to: "frontend/src/app/admin/users/useUserMediaFilters.ts"
      via: "useUserMediaFilters() hook"
      pattern: "useUserMediaFilters"
    - from: "frontend/src/app/admin/users/tabs/UserMediaTab.tsx"
      to: "/me/releases/{releaseVersionId}/workspace"
      via: "Button variant=primary href"
      pattern: "workspace"
---

<objective>
Fully rewrite `UserMediaTab.tsx` from its current `groupByReleaseVersion()`-based client-side
grouping (the exact anti-pattern D23 forbids once the backend groups server-side) into the
UI-SPEC-locked release/episode-block projection consuming 139-04's grouped backend endpoint and
139-02's media filter hook — deleting the fake `hasScopePermission()` permission badge entirely
(D19) and fixing the currently-broken empty `PublicURL` rendering.

Purpose: closes UADM-05 on the visible surface; removes a real, currently-shipped fake-permission
signal that misleads admins today.

Output: `UserMediaTab.tsx` rewritten; a new `mediaTab.module.css`; `UserMediaTab.test.tsx` created
or rewritten (RESEARCH.md notes no test file was found for this component today — confirm which
applies before writing).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-scalable-user-admin-projections/139-CONTEXT.md
@.planning/phases/139-scalable-user-admin-projections/139-DISCUSS.md
@.planning/phases/139-scalable-user-admin-projections/139-UI-SPEC.md
@.planning/phases/139-scalable-user-admin-projections/139-PATTERNS.md
</context>

<interfaces>
<!-- UI-SPEC's locked Component Inventory — Media Tab (verbatim structural mapping) -->
Tab purpose banner + result count: SectionHeader(title="Medien", description=<informational copy
  below>, actions=Badge variant="neutral" showing Meta.Total)
Filter bar: Toolbar, same construction as Contributions
Anime/Projekt-Gruppe/Release-Episode/Medientyp filters: FormField+Select each, options from
  data.filter_options
Zeitraum: two FormField+DatePicker ("Von"/"Bis")
Reset: Button variant="ghost" "Filter zurücksetzen"
One release/episode block: Card variant="nestedFlat"
Block header: "Anime · Projekt/Gruppe" (Heading 16/700) + sub-line "Episode 5 · Version 1"
  (Label 12/700, var(--text-soft)) — NEVER the raw release_version:<id> string
Thumbnails: ResponsiveImage, bounded box 96x96 desktop / 64x64 at <=600px container, loading="lazy",
  reserved aspect-ratio geometry — never the original full-size asset
Meta line per item: "Hochgeladen: {date}" (formatDate, existing de-DE convention) + media-type
  Badge variant="neutral"
Removed entirely, no replacement: the hasScopePermission()-derived "Berechtigung aktiv/fehlt" Badge
One canonical action per block: Button variant="primary" "Release-Medien öffnen" ->
  /me/releases/{releaseVersionId}/workspace
Pagination: Pagination primitive, bottom of tab
Loading/Error/Empty: same pattern as Contributions

<!-- Explicit non-goals this tab must never render (D18/D27, "no even a small version") -->
No physical file path, no storage id, no derivative-file inventory, no image-size/format analysis,
no missing-file health indicator — anywhere in this component, ever.

<!-- Copywriting Contract (exact locked strings) -->
Purpose banner: "Informativ — zeigt Medien, die diesem Benutzer im jeweiligen Anime-, Projekt- und
  Release-Kontext zugeordnet sind. Änderungen erfolgen in der Release-Medien-Arbeitsfläche."
Primary CTA: "Release-Medien öffnen" (this REPLACES the currently-shipped "Arbeitsfläche öffnen"
  string — a locked correction, not an addition)
Empty (no data): "Keine Medien vorhanden."
Empty (filtered to zero): "Keine Medien für diese Filter." + "Filter anpassen oder zurücksetzen,
  um weitere Einträge zu sehen." + Button variant="ghost" "Filter zurücksetzen"
Error: same pattern as Contributions

<!-- Existing code this plan DELETES outright, no replacement (UserMediaTab.tsx current file) -->
function hasScopePermission(ownerContext: string): boolean {
  return ownerContext.startsWith('release_version:') && ownerContext.trim().length > 'release_version:'.length
}
function groupByReleaseVersion(items: AdminMediaItemSummary[]): Map<string, AdminMediaItemSummary[]> {
  // client-side Map grouping over the FULL unbounded response — the exact D23 anti-pattern
}

<!-- Container-query responsive rules (D26, locked breakpoints, same mechanism as Contributions) -->
Root container: container-type: inline-size; container-name: admin-user-projection;
@container admin-user-projection (max-width: 760px) { toolbar fields stack }
@container admin-user-projection (max-width: 600px) { block header wraps; thumbnails 96px -> 64px }
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite UserMediaTab.tsx as the grouped release/episode-block projection</name>
  <files>frontend/src/app/admin/users/tabs/UserMediaTab.tsx</files>
  <behavior>
    - Test: given a mocked getAdminUserMedia response with one AdminMediaReleaseBlock containing 2
      items, the component renders exactly one block with "Episode 5 · Version 1"-style sub-line
      text (or the anime/group equivalent) and both items' thumbnails/meta lines, and never
      renders the literal substring "release_version:" anywhere in the DOM.
    - Test: the block's primary action button reads exactly "Release-Medien öffnen" (not
      "Arbeitsfläche öffnen") and links to `/me/releases/{releaseVersionId}/workspace`.
    - Test: no element in the rendered output contains the text "Berechtigung aktiv" or
      "Berechtigung fehlt" (proves hasScopePermission's badge is gone, not just relabeled).
    - Test: no element in the rendered output contains a physical file path, a storage id label,
      a derivative-file count, or an image-size/format-analysis string (D18 — the rendered DTO
      fields themselves make this structurally true, this test guards against a future regression
      that re-adds such a field to the render).
    - Test: given meta.total=17 and a page of 5 blocks, the SectionHeader count Badge and
      Pagination both reflect 17.
    - Test: changing the Medientyp Select calls getAdminUserMedia again with media_type encoded in
      the request (assert on the mocked fetch call args).
    - Test: thumbnails render via a component whose props include loading="lazy" (assert the
      rendered <img>'s loading attribute, or the ResponsiveImage mock's received prop).
  </behavior>
  <read_first>
    frontend/src/app/admin/users/tabs/UserMediaTab.tsx (full file, 189 lines — MediaCard at lines
    66-105 is the closest existing shape to keep/extend; hasScopePermission and
    groupByReleaseVersion are deleted outright per the interfaces block above; the
    loadData/isLoading/error skeleton is the one part to keep the shape of)
    frontend/src/app/admin/users/useUserMediaFilters.ts (139-02)
    frontend/src/lib/api.ts (139-07 — paginated getAdminUserMedia(userId, params))
    frontend/src/components/ui/ResponsiveImage.tsx (existing primitive — exact prop names for
    bounded box size, loading, aspect-ratio reservation; reuse, do not hand-roll an <img>)
    frontend/src/lib/dateFormat or wherever formatDate's existing de-DE convention lives (grep the
    current UserMediaTab.tsx's own import for this — reuse exactly, do not reimplement)
  </read_first>
  <action>
    Delete `hasScopePermission` and `groupByReleaseVersion` entirely — no replacement, no
    commented-out remnant. Import and call `useUserMediaFilters()` for all filter state/setters.
    Build the component tree per the `<interfaces>` Component Inventory: `SectionHeader` with the
    locked Media purpose-banner copy and result-count `Badge`; `Toolbar` with `FormField`+`Select`
    for Anime/Projekt-Gruppe/Release-Episode/Medientyp (options from
    `data.filter_options.animes`/`.groups`/`.releases_or_episodes`/`.media_types`), two
    `FormField`+`DatePicker` for Von/Bis, trailing `Button variant="ghost"` "Filter zurücksetzen".

    For each `AdminMediaReleaseBlock` in `data.data`: render a `Card variant="nestedFlat"` with a
    header showing `"${anime_title} · ${fansub_group_name}"` (Heading 16/700) and a sub-line
    showing `episode_number` and/or `release_version_label` (e.g. "Episode 5 · Version 1", falling
    back gracefully when `episode_number` is null — never render the raw `release_version_id`
    integer or the string "release_version:" anywhere). Render each `AdminMediaItem` in
    `items` via a `ResponsiveImage` thumbnail (96×96 desktop / 64×64 at ≤600px container per the
    CSS module in Task 2, `loading="lazy"`) + a meta line "Hochgeladen: {formatDate(uploaded_at)}"
    + a `Badge variant="neutral"` showing `media_type`. Render exactly ONE `Button
    variant="primary"` per BLOCK (not per item) reading "Release-Medien öffnen", `href`
    `/me/releases/${release_version_id}/workspace` (matches D16's one-action-per-block rule, not
    one per media item). Do not render, fetch, or derive any physical storage path, storage id,
    derivative-file inventory, or image-size/format-analysis value anywhere in this component
    (D18) — the `AdminMediaItem` DTO (139-01) structurally has no such field, keep it that way.

    Render `Pagination` bound to `data.meta`, `LoadingState`/`ErrorState`(+retry action)/
    `EmptyState` (two variants) per the Copywriting Contract's exact strings. `loadData` now calls
    `getAdminUserMedia(userId, params)` where `params` comes from `useUserMediaFilters()`, inside a
    `useCallback` depending on `[userId, params]`.
  </action>
  <acceptance_criteria>
    <source_assertion>UserMediaTab.tsx does not declare any function named hasScopePermission or
    groupByReleaseVersion</source_assertion>
    <source_assertion>UserMediaTab.tsx contains the literal string "Release-Medien öffnen" and does
    NOT contain the literal string "Arbeitsfläche öffnen"</source_assertion>
    <source_assertion>UserMediaTab.tsx renders at most one primary action Button per
    AdminMediaReleaseBlock, not per AdminMediaItem</source_assertion>
    <source_assertion>UserMediaTab.tsx imports ResponsiveImage from @/components/ui and passes a
    loading="lazy" prop to it</source_assertion>
    <test_command>docker exec team4sv30-frontend sh -c "cd /app && npx vitest run src/app/admin/users/tabs/UserMediaTab 2>&1 | tail -80"</test_command>
  </acceptance_criteria>
  <verify>
    <automated>docker exec team4sv30-frontend sh -c "cd /app && npx tsc --noEmit"</automated>
  </verify>
  <done>Component fully replaces client-side grouping with server-grouped release/episode blocks;
  the fake permission badge and raw release_version:<id> string are both gone; exactly one
  canonical action per block; zero storage-diagnostic content anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Container-query CSS module (96px/64px thumbnail breakpoint) + test file</name>
  <files>
    frontend/src/app/admin/users/tabs/mediaTab.module.css,
    frontend/src/app/admin/users/tabs/UserMediaTab.test.tsx
  </files>
  <read_first>
    frontend/src/components/profile/RoleBadgeCard.module.css (@container syntax precedent)
    frontend/src/app/admin/users/tabs/contributionsTab.module.css (139-08 — same
    container-name/breakpoint convention this file must match exactly for consistency across both
    new tabs)
  </read_first>
  <action>
    Create `mediaTab.module.css` with the identical `.root { container-type: inline-size;
    container-name: admin-user-projection; }` declaration and the same 760px filter-stack
    breakpoint as `contributionsTab.module.css` (139-08), plus the media-specific 600px rule:
    `@container admin-user-projection (max-width: 600px) { .blockHeader { flex-wrap: wrap; }
    .thumbnail { width: 64px; height: 64px; } }` (desktop default 96×96 declared on `.thumbnail`
    outside the container query). Apply `flex-wrap: wrap` + `min-width: 0` on every header/meta row
    combining text + badges, same defensive rule as 139-08.

    Confirm via `find frontend/src/app/admin/users/tabs -iname "UserMediaTab.test*"` whether a test
    file already exists (RESEARCH.md's Wave-0-gaps table flagged this as unconfirmed). If it does
    not exist, create `UserMediaTab.test.tsx` from scratch containing the RED tests from Task 1's
    `<behavior>` block. If it does exist, rewrite it fully against the new component (delete any
    assertion referencing `hasScopePermission`/`groupByReleaseVersion`/the old "Arbeitsfläche
    öffnen" copy). Run the full file and confirm zero failures.
  </action>
  <acceptance_criteria>
    <source_assertion>mediaTab.module.css declares .thumbnail with a 64px width/height rule inside
    an @container admin-user-projection (max-width: 600px) block</source_assertion>
    <test_command>docker exec team4sv30-frontend sh -c "cd /app && npx vitest run src/app/admin/users/tabs/UserMediaTab -v 2>&1 | tail -100"</test_command>
  </acceptance_criteria>
  <verify>
    <automated>docker exec team4sv30-frontend sh -c "cd /app && npx vitest run src/app/admin/users/tabs/UserMediaTab"</automated>
  </verify>
  <done>CSS module matches the Contributions tab's container-query convention exactly, with the
  additional 96px->64px thumbnail rule. Test file is green and contains zero reference to deleted
  functions/copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser -> rendered media thumbnails | ResponsiveImage renders PublicURL values the backend already validated/derived (139-04); no user-supplied path reaches the DOM unvalidated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-16 | Information Disclosure | removed hasScopePermission badge | mitigate | deleting this fake signal outright (D19) prevents admins from being misled into believing a real authorization check occurred where none did — this IS the mitigation, not a residual risk |
| T-139-17b | Information Disclosure | storage/technical diagnostics | mitigate | this component structurally never fetches or renders physical path/storage id/derivative inventory/format-analysis data (D18) — there is no such field in the DTO it consumes, so there is nothing to leak by omission-by-design |
</threat_model>

<verification>
`docker exec team4sv30-frontend sh -c "cd /app && npx tsc --noEmit && npx vitest run src/app/admin/users/tabs/UserMediaTab"` fully green.
</verification>

<success_criteria>
UADM-05 is visible and provable in the UI. The fake permission badge and client-side grouping are
both gone with zero trace. Exactly one canonical, correctly-labeled action per block. No storage
diagnostic content anywhere.
</success_criteria>

<output>
Create `.planning/phases/139-scalable-user-admin-projections/139-09-SUMMARY.md` when done
</output>
