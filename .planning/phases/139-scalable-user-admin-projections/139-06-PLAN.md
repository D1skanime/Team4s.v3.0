---
phase: 139-scalable-user-admin-projections
plan: 06
type: execute
wave: 5
depends_on: ["139-03", "139-04", "139-05"]
files_modified:
  - backend/internal/repository/admin_users_query_budget_test.go
  - scripts/seed-phase139-contribution-fixtures.mjs
  - scripts/README-seed.md
autonomous: true
requirements: [QUAL-06]

must_haves:
  truths:
    - "D25/QUAL-06: query count for ListUserContributions, GetUserMedia, and GetUserRightsSummary is constant regardless of data volume (few-vs-many fixture proof, mirroring member_profile_query_budget_test.go's Phase-131 precedent)"
    - "D24: a pagination-drift test proves filters, counts, and items all derive from the exact same server-side filtered/grouped dataset across multiple pages — no duplicate or missing blocks when walking a large result set"
    - "F-03: a deliberate, executed decision closes the 'no live independent+different data' gap — a reproducible seed-script extension creates real independent-identical and independent-different release_crew_snapshots rows via the real PUT /admin/release-versions/:versionId/contributions/effective endpoint, enabling live UAT of the 'Nur Abweichungen' filter, not left as an open question"
  artifacts:
    - path: "backend/internal/repository/admin_users_query_budget_test.go"
      provides: "Constant-query-budget gate for all three new/changed endpoints + pagination-drift proof"
    - path: "scripts/seed-phase139-contribution-fixtures.mjs"
      provides: "Idempotent, API-driven seed script producing a real independent-and-different + independent-but-identical release_crew_snapshots pair for live UAT"
  key_links:
    - from: "scripts/seed-phase139-contribution-fixtures.mjs"
      to: "backend/internal/handlers/admin_content_fansub_releases_contributions_handlers.go"
      via: "PUT /api/v1/admin/release-versions/:versionId/contributions/effective"
      pattern: "contributions/effective"
---

<objective>
Close QUAL-06's mandatory query-count and pagination-drift gates for the three endpoints Phase 139
built (contributions, media, rights-summary), and execute F-03's required decision: extend the
existing `seed-member-profile-fixtures.mjs` convention with a new sibling script that creates real
`independent`-and-different plus `independent`-but-identical `release_crew_snapshots` rows against
the live database, since RESEARCH.md confirmed `team4s_v2` currently has 13 rows, all `inherited`,
zero `independent` — meaning the override-detection UI has nothing real to show today without this.

Purpose: QUAL-06 is a phase-wide gate, not a per-endpoint afterthought — this plan proves the
constant-query-budget property holds across all three endpoints together (the property most likely
to silently regress if any endpoint's query is later "simplified" back into a per-row loop) and
gives the human live-UAT step (139-10) real data to click through instead of an empty
"Nur Abweichungen" filter.

Output: one new backend query-budget/pagination-drift test file, one new idempotent Node seed
script (mirrors `seed-member-profile-fixtures.mjs`'s exact conventions), and a README addition
documenting how to run it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-scalable-user-admin-projections/139-CONTEXT.md
@.planning/phases/139-scalable-user-admin-projections/139-RESEARCH.md
@.planning/phases/139-scalable-user-admin-projections/139-VALIDATION.md
</context>

<interfaces>
<!-- Query-budget gate pattern to copy exactly (member_profile_query_budget_test.go:176-213, Phase 131) -->
counter.reset(); result, err := repo.SomeMethod(ctx, fewArgs); fewCount := counter.count()
counter.reset(); result, err := repo.SomeMethod(ctx, manyArgs); manyCount := counter.count()
require.Equal(t, fewCount, manyCount, "constant query budget violated")
require.Equal(t, phase139ConstantQueryBudgetXxx, manyCount, "...update only with an intentional, documented loader change")

<!-- Existing seed script conventions to mirror exactly (scripts/seed-member-profile-fixtures.mjs) -->
#!/usr/bin/env node — Node 18+ global fetch, zero npm dependencies, idempotent (check-existence-
then-create, 409/duplicate treated as success)
const API = (process.env.SEED_API_BASE || 'http://192.168.235.196:18092').replace(/\/+$/, '')
const KC = (process.env.SEED_KC_BASE || 'http://192.168.235.196:18081').replace(/\/+$/, '')
async function kcToken(username, password) { /* POST .../protocol/openid-connect/token, grant_type=password */ }
Run via: docker exec team4sv30-frontend node /tmp/seed.mjs (per scripts/README-seed.md:34-41)

<!-- Target write endpoint for the F-03 demo data (RESEARCH.md-confirmed exact call) -->
PUT /api/v1/admin/release-versions/:versionId/contributions/effective
(admin_content_fansub_releases_contributions_handlers.go:66, ReplaceEffectiveContributionsForVersion)
-- a crew payload SET-EQUAL to the anime+group's project-standard rows produces independent+identical;
   a crew payload that genuinely DIFFERS produces independent+different
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Query-count-is-constant gate for contributions/media/rights-summary</name>
  <files>backend/internal/repository/admin_users_query_budget_test.go</files>
  <read_first>
    backend/internal/repository/member_profile_query_budget_test.go (full file — the exact
    seed-few/seed-many + counter.reset()/counter.count() + require.Equal structure to copy three
    times, once per endpoint)
    backend/internal/repository/query_counter.go (package-private queryCounter, reused as-is —
    zero changes)
    backend/internal/testsupport/phase139_postgres.go (139-01 harness)
  </read_first>
  <action>
    Write `openPhase139PostgresWithCounter(t) (*pgxpool.Pool, *queryCounter)` in this test file
    (package `repository`, mirrors `openPhase131Postgres`'s exact shape — parse
    `TEAM4S_PHASE139_TEST_DSN`, guard the database-name regex, wire `config.ConnConfig.Tracer =
    counter`, open the pool, `t.Cleanup(pool.Close)` — note this duplicates
    `testsupport.OpenPhase139Postgres`'s connection logic locally in package `repository` because
    the counter must be wired onto the pool's `pgxpool.Config` before `pgxpool.NewWithConfig`,
    which `testsupport.OpenPhase139Postgres` does not expose a seam for — exactly the same
    duplication `openPhase131Postgres` already accepts for the identical reason, not a new
    pattern).

    `TestPhase139ContributionsQueryBudgetIsConstant`: seed a user with 2 anime+project blocks (few)
    and a second user with 20 anime+project blocks, each with several range entries and at least
    one real deviation (F-03 pattern, seeded directly via SQL not the live endpoint, since this is
    an isolated-schema integration test). `counter.reset()`; call `ListUserContributions` for the
    few-user; `fewCount := counter.count()`. `counter.reset()`; call for the many-user;
    `manyCount := counter.count()`. `require.Equal(t, fewCount, manyCount)`. Pin the exact observed
    count as `phase139ContributionsQueryBudget` (document in a comment that this constant may only
    change alongside an intentional, documented loader change, matching Phase 131's own doc
    comment convention).

    `TestPhase139MediaQueryBudgetIsConstant`: identical shape for `GetUserMedia` (2 vs 20
    release/episode blocks).

    `TestPhase139RightsSummaryQueryBudgetIsConstant`: identical shape for `GetUserRightsSummary`
    (2 vs 20 group memberships, at least one with a real override and one where the actor is a
    normal role holder) — this is the test that would catch a regression back to
    `ResolveGroupRights`-per-group-in-a-loop, since that anti-pattern's query count would scale
    linearly with group count while this test's fixed few/many comparison would fail.
  </action>
  <acceptance_criteria>
    <test_command>docker exec team4sv30-backend sh -c "cd /app && TEAM4S_PHASE139_TEST_DSN=\"$TEAM4S_PHASE139_TEST_DSN\" go test ./internal/repository/... -run TestPhase139.*QueryBudget -v"</test_command>
    <source_assertion>each of the three tests calls counter.reset() exactly twice and asserts
    require.Equal on the two resulting counts before asserting a specific pinned budget
    constant</source_assertion>
  </acceptance_criteria>
  <verify>
    <automated>docker exec team4sv30-backend sh -c "cd /app && go build ./... && TEAM4S_PHASE139_TEST_DSN=\"$TEAM4S_PHASE139_TEST_DSN\" go test ./internal/repository/... -run TestPhase139.*QueryBudget -v"</automated>
  </verify>
  <done>All three query-budget tests green (or SKIP cleanly with no DSN), each proving a
  volume-independent, constant SQL round-trip count.</done>
</task>

<task type="auto">
  <name>Task 2: Pagination-drift test (D24 coherence at scale)</name>
  <files>backend/internal/repository/admin_users_query_budget_test.go</files>
  <read_first>
    backend/internal/repository/admin_users_contributions_query_test.go (139-03's
    TestListUserContributionsPaginationNeverSplitsAProjectBlock — this task's drift test extends
    that same seeded-fixture idea to a larger volume and multi-page walk, do not duplicate the
    seed helpers, reuse them from that file if in the same package)
  </read_first>
  <action>
    `TestPhase139ContributionsPaginationDriftAtScale`: seed one user with 30 distinct anime+project
    blocks (mix of standard-only and deviation-containing blocks, some matching an
    `only_deviations` filter and some not). Walk pages with `limit=5` from `offset=0` through
    `offset=25` (6 pages); collect every returned `(AnimeID, FansubGroupID)` pair across all pages;
    assert the union is exactly the 30 seeded pairs with zero duplicates and zero gaps, and that
    every page's `Meta.Total` equals 30 (or the filtered count when `OnlyDeviations` is applied,
    matching the SAME filtered dataset the items came from — D24). Repeat the same walk with
    `only_deviations=true` and assert `Meta.Total` equals only the count of blocks that actually
    contain at least one real deviation, and that the returned items are exactly that subset (not
    merely filtered client-side after the fact — this is a server-side integration test, so proving
    the exact right subset returned IS proving D24/D23 together).

    `TestPhase139MediaPaginationDriftAtScale`: identical shape for `GetUserMedia` (30
    release/episode blocks, walk with limit=5, assert union/count coherence with and without an
    active `media_type` filter).
  </action>
  <acceptance_criteria>
    <test_command>docker exec team4sv30-backend sh -c "cd /app && TEAM4S_PHASE139_TEST_DSN=\"$TEAM4S_PHASE139_TEST_DSN\" go test ./internal/repository/... -run TestPhase139.*PaginationDrift -v"</test_command>
    <source_assertion>both drift tests walk at least 5 distinct offset values and assert the
    unioned result set has zero duplicate grouping keys</source_assertion>
  </acceptance_criteria>
  <verify>
    <automated>docker exec team4sv30-backend sh -c "cd /app && TEAM4S_PHASE139_TEST_DSN=\"$TEAM4S_PHASE139_TEST_DSN\" go test ./internal/repository/... -run TestPhase139.*PaginationDrift -v"</automated>
  </verify>
  <done>Both drift tests green (or SKIP cleanly with no DSN), proving D24 coherence across a
  realistic multi-page walk with and without an active filter.</done>
</task>

<task type="auto">
  <name>Task 3: F-03 seed script — real independent-identical + independent-different demo data</name>
  <files>scripts/seed-phase139-contribution-fixtures.mjs, scripts/README-seed.md</files>
  <read_first>
    scripts/seed-member-profile-fixtures.mjs (full file — the exact conventions this new sibling
    script must follow: shebang, Node 18+ zero-dependency, env-var-configurable API/KC base URLs,
    kcToken helper, idempotent check-existence-then-create pattern)
    scripts/README-seed.md (existing doc structure — how to run a seed script via `docker exec
    team4sv30-frontend node /tmp/seed.mjs`, this task adds a new section for this script)
    backend/internal/handlers/admin_content_fansub_releases_contributions_handlers.go (line ~66,
    ReplaceEffectiveContributionsForVersion — the exact request body shape the PUT endpoint
    expects, needed to construct both payloads)
  </read_first>
  <action>
    Decision executed here (F-03, resolves RESEARCH.md's explicit "planner must decide and name"
    instruction — Option (a), a reproducible seed-script extension, not "automated-only"): create
    `scripts/seed-phase139-contribution-fixtures.mjs` following `seed-member-profile-fixtures.mjs`'s
    exact conventions (shebang, zero deps, `kcToken` auth helper, idempotent). The script:

    1. Authenticates as an existing admin account (reuse the same `SEED_ADMIN_USER`/`SEED_ADMIN_PW`
       env-var convention already established).
    2. Finds a real anime+fansub-group pair with at least one existing project-standard
       `anime_contributions` row (`release_version_id IS NULL`) and at least one existing release
       version under that same anime+group — via the existing admin contributions/release-version
       list endpoints (GET, read-only discovery, no new endpoint needed), preferring the
       sheppert/csubs-leader fixture data `seed-member-profile-fixtures.mjs` already establishes if
       a suitable pair exists there, otherwise the first suitable real pair found.
    3. Reads that pair's current project-standard crew (member_id + role_codes per role) via the
       existing read endpoint.
    4. Calls `PUT /api/v1/admin/release-versions/:versionId/contributions/effective` on ONE release
       version under that anime+group with a crew payload that is SET-EQUAL to the project standard
       (same member_id/role_codes set, no actual change) — this produces a real
       `independent`-but-identical row (F-03 case 1).
    5. Calls the same PUT endpoint on a DIFFERENT release version under the same anime+group with a
       crew payload that genuinely differs (e.g. an added role or a substituted member) — this
       produces a real `independent`-and-different row (F-03 case 2), the one that must appear
       under a live "Nur Abweichungen" filter.
    6. Prints a clear `RESULT: PASS` / `RESULT: FAIL` summary matching
       `seed-member-profile-fixtures.mjs`'s existing pass/fail reporting convention, plus the exact
       anime/group/release-version identifiers it touched so a human running live UAT (139-10)
       knows exactly which user/anime/project to open.
    7. Is idempotent: re-running the script must detect the already-seeded state (e.g. by
       re-reading the crew before writing) and treat "already matches the intended target state" as
       success, not a duplicate-error failure.

    Add a new section to `scripts/README-seed.md` documenting: purpose (F-03 live-UAT demo data
    for Phase 139's override-detection filter), exact run command (`docker exec
    team4sv30-frontend node /tmp/seed-phase139-contribution-fixtures.mjs` after copying the script
    into the container, mirroring the existing script's documented invocation), and which user/
    anime/project the script prints for the live-UAT step to use.
  </action>
  <acceptance_criteria>
    <source_assertion>seed-phase139-contribution-fixtures.mjs contains no import statement for any
    package outside Node's built-in globals (fetch, FormData, Blob) — zero npm dependencies,
    matching the existing script's constraint</source_assertion>
    <source_assertion>the script calls PUT against a path containing "/contributions/effective" at
    least twice with two different release-version IDs</source_assertion>
    <source_assertion>README-seed.md contains a new section referencing
    seed-phase139-contribution-fixtures.mjs</source_assertion>
    <human_check>Run the script once against the live stack (docker exec team4sv30-frontend node
    /tmp/seed-phase139-contribution-fixtures.mjs after copying it in) and confirm it prints
    RESULT: PASS along with a concrete anime/project/user identifier; re-run it a second time and
    confirm it still reports success (idempotent) rather than a duplicate-conflict error.</human_check>
  </acceptance_criteria>
  <verify>
    <automated>docker exec team4sv30-backend sh -c "cd /app && node --check /dev/stdin < /tmp/noop 2>/dev/null; echo n/a-script-is-frontend-side"</automated>
  </verify>
  <done>Script exists, follows the established zero-dependency/idempotent convention, and (per the
  human_check) has been run at least once against the live stack producing a real
  independent-and-different release_crew_snapshots row team4s_v2 did not have before this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed script -> live admin write endpoint | seed-phase139-contribution-fixtures.mjs authenticates as a real admin and writes real crew data via an existing, already-authorized endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-13 | Tampering | seed-phase139-contribution-fixtures.mjs | accept | the script only ever calls the existing, already-authorized, already-audited PUT contributions/effective endpoint with real admin credentials supplied via env vars — it introduces no new write path or bypass, mirroring seed-member-profile-fixtures.mjs's existing accepted risk posture |
| T-139-SC | Tampering | package installs | mitigate | this plan adds zero new npm/pip/cargo packages (Package Legitimacy Audit N/A, per RESEARCH.md) |
</threat_model>

<verification>
`docker exec team4sv30-backend sh -c "cd /app && TEAM4S_PHASE139_TEST_DSN=... go test ./internal/repository/... -run TestPhase139 -v"` green for both query-budget and pagination-drift tests;
seed script run once confirmed to produce real independent-and-different live data.
</verification>

<success_criteria>
QUAL-06's constant-query-budget and pagination-drift properties are proven for all three Phase-139
endpoints together. F-03's "no live data" gap is closed with a real, reproducible, idempotent seed
path — not left as an open question for 139-10's live UAT to discover unprepared.
</success_criteria>

<output>
Create `.planning/phases/139-scalable-user-admin-projections/139-06-SUMMARY.md` when done
</output>
