---
phase: 139-scalable-user-admin-projections
plan: 05
type: execute
wave: 4
depends_on: ["139-01", "139-04"]
files_modified:
  - backend/internal/permissions/effective_rights_batch_summary.go
  - backend/internal/repository/authz_user_overrides.go
  - backend/internal/repository/authz_permissions.go
  - backend/internal/repository/admin_users_tab_repository.go
  - backend/internal/handlers/admin_users_handler.go
  - backend/internal/handlers/admin_users_handler_test.go
  - backend/cmd/server/admin_routes.go
  - backend/cmd/server/main.go
  - shared/contracts/admin-capabilities.yaml
  - backend/internal/handlers/admin_capability_contract_test.go
  - backend/internal/permissions/effective_rights_batch_summary_test.go
autonomous: true
requirements: [UADM-06]

must_haves:
  truths:
    - "F-01/UADM-06: a genuinely new batched endpoint answers every group membership's compact rights summary in O(1) SQL round trips regardless of how many groups the user belongs to — never one ResolveGroupRights call (with its own internal per-group SQL) looped per group, which RESEARCH.md explicitly names as the anti-pattern that moves the N+1 from HTTP to Postgres without fixing it"
    - "D21: the batched summary's precedence logic is the same, unmodified pure evaluateGroupRights function Phase 137 already ships (via a new thin exported façade, EvaluateGroupRightsFromSources) — this plan adds zero new precedence rules and changes zero existing resolver semantics"
    - "Phase-138 D-05 content parity: GroupSummaryCard's rendered content (role label, first-3 headline states, hasDeviation flag, openClaimsCount) stays computable from this endpoint's response without silently changing what D-05 locked, and the plan explicitly documents (not silently ships) the one disclosed scope note about specialized-grant (review-delegation) coverage in the batched headline path versus the full single-group Rights tab"
    - "F-01 finding #1: GetUserGroupMemberships gains real server-side LIMIT/OFFSET pagination (previously fully unbounded)"
    - "adminUsersRepoStub in admin_users_handler_test.go implements the AdminUsersRepository interface's new/changed GetUserGroupMemberships (limit/offset) and GetUserRightsSummary signatures, and buildAdminUsersHandler's call to NewAdminUsersHandler is updated for its new constructor parameter, so backend/internal/handlers/... continues to compile after this plan lands"
  artifacts:
    - path: "backend/internal/permissions/effective_rights_batch_summary.go"
      provides: "GroupRightsSourcesInput + Service.EvaluateGroupRightsFromSources (thin façade over the existing unexported evaluateGroupRights) + Service.ResolveGroupRightsBatch (batched multi-group orchestration)"
    - path: "backend/internal/repository/authz_user_overrides.go"
      provides: "LoadCurrentOverridesForGroups — one query across all group IDs via fansub_group_id = ANY($2)"
  key_links:
    - from: "backend/cmd/server/admin_routes.go"
      to: "backend/internal/handlers/admin_users_handler.go"
      via: "GET /admin/users/:userId/rights-summary -> AdminUsersHandler.GetUserRightsSummary"
      pattern: "rights-summary"
    - from: "backend/internal/handlers/admin_users_handler.go"
      to: "backend/internal/permissions/effective_rights_batch_summary.go"
      via: "ResolveGroupRightsBatch"
      pattern: "ResolveGroupRightsBatch"
    - from: "backend/internal/handlers/admin_users_handler_test.go"
      to: "backend/internal/handlers/admin_users_handler.go"
      via: "adminUsersRepoStub.GetUserGroupMemberships/GetUserRightsSummary must satisfy the updated AdminUsersRepository interface, and buildAdminUsersHandler must call NewAdminUsersHandler with its new parameter count"
      pattern: "func buildAdminUsersHandler"
---

<objective>
Close F-01's more consequential half: `UserOverviewTab.tsx` (the tab every admin sees by default,
`DEFAULT_DETAIL_TAB = 'overview'`) has the identical eager `Promise.all(getEffectiveRights per
group)` fan-out as the Rights tab, but — unlike the Rights tab, which can reasonably defer to
per-group on-select — the Overview tab must render every group's compact summary simultaneously on
mount (Phase 138 D-05). This requires a genuinely new, batched backend endpoint, not a frontend-only
fix. This plan builds that endpoint with real O(1)-SQL-round-trip batching, reusing Phase 137's
existing pure precedence evaluator with zero semantic change (D21).

Purpose: UADM-06 is not honestly satisfied until BOTH fan-out locations are closed. The external
review package covered only the Rights tab; RESEARCH.md's F-01 correction and the mandatory findings
in the planning context both require this endpoint.

Output: a new `GET /admin/users/:userId/rights-summary` endpoint backed by genuine batch-loading
(existing `GetUserGroupMemberships` now paginated for roles+status, a new
`LoadCurrentOverridesForGroups` query for overrides, a new batched review-grant query for
specialized grants) feeding the existing unexported `evaluateGroupRights` precedence engine via a
new small exported façade — zero new precedence logic, full D21 compliance. `adminUsersRepoStub` in
`admin_users_handler_test.go` (the third implementer of `AdminUsersRepository`, alongside the real
repository and this plan's own new code) is updated in lockstep for every interface change this
plan makes, and `buildAdminUsersHandler`'s call to `NewAdminUsersHandler` (the SECOND call site of
that constructor, alongside `main.go`) is updated for the new dependency parameter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-scalable-user-admin-projections/139-CONTEXT.md
@.planning/phases/139-scalable-user-admin-projections/139-DISCUSS.md
@.planning/phases/139-scalable-user-admin-projections/139-RESEARCH.md
@.planning/phases/139-scalable-user-admin-projections/139-PATTERNS.md
</context>

<interfaces>
<!-- Existing PURE precedence engine this plan must reuse unmodified (permissions/effective_rights.go) -->
func evaluateGroupRights(actor Actor, fansubGroupID int64, sources groupRightsSources, actions []Action) *GroupRightsResolution
// unexported; zero I/O; this plan adds an EXPORTED thin wrapper, never duplicates this logic

type groupRightsSources struct { Roles []Action; ActiveMembership bool; Overrides []UserCapabilityOverride; SpecializedGrants []SpecializedGrant } // unexported

<!-- Existing single-group batch-load this plan's new batch variant must mirror the shape of -->
func (s *Service) loadGroupRightsSources(ctx, actor Actor, fansubGroupID int64) (groupRightsSources, bool, error)
// issues up to 4 per-group queries (roles, membership, overrides, specialized grants) -- calling
// this once per group in a loop is the EXACT anti-pattern this plan must NOT reproduce

<!-- Existing single-group override load this plan adds a batch sibling for (authz_user_overrides.go:93-107) -->
func (r *AuthzUserOverridesRepository) LoadCurrentOverrides(ctx, appUserID, fansubGroupID int64) ([]UserGroupCapabilityOverride, error)
// SQL: WHERE app_user_id = $1 AND fansub_group_id = $2 -- batch sibling changes fansub_group_id = $2
// to fansub_group_id = ANY($2::bigint[]) and groups results into a map[int64][]UserGroupCapabilityOverride

<!-- Existing single-group review-grant load this plan adds a batch sibling for (authz_permissions.go:197+) -->
func (r *AuthzRepository) ResolveActorReviewGrantContext(ctx, appUserID, fansubGroupID int64) (*permissions.ReviewGrantContext, error)
// underlying SQL joins fansub_group_members/app_users/member_claims/fansub_group_member_review_capabilities
// WHERE fgm.app_user_id = $1 AND fgm.fansub_group_id = $2 -- batch sibling: fansub_group_id = ANY($2::bigint[]),
// drop "FOR SHARE OF fgm" (not needed for a read-only summary), group by fansub_group_id

<!-- Existing batch-load-then-evaluate-in-memory precedent to model this plan's shape after (Phase 138-07) -->
func (s *Service) PreviewGroupRightsCapabilityChange(...) // effective_rights_capability_impact_preview.go:63
// loads sources for ALL role holders in one/few batched queries, then evaluates in-memory per holder

<!-- Existing route registration convention (admin_routes.go:186-188, 298) -->
v1.GET("/admin/users/:userId/media", auth, deps.adminUsersHandler.GetUserMedia)
v1.GET("/admin/fansubs/:id/app-members/:appUserId/effective-rights", auth, deps.adminEffectiveRightsHandler.GetEffectiveRights)

<!-- Existing handler construction (main.go) -->
permissionSvc := permissions.NewService(authzRepo)   // line 136, already available before line 505
adminUsersHandler := handlers.NewAdminUsersHandler(adminUsersRepo, authzRepo, auditLogRepo)  // line 505

<!-- Existing SECOND call site of the constructor this plan must also update
     (admin_users_handler_test.go:158-164) -->
func buildAdminUsersHandler(repo *adminUsersRepoStub, authz *adminAuthzRepoStub, audit *adminAuditStub) *AdminUsersHandler {
    return NewAdminUsersHandler(repo, authz, audit)
}
// NewAdminUsersHandler currently takes exactly 3 arguments. When Task 3 adds a 4th
// (adminUsersRightsResolver) parameter, THIS call site breaks too, not just main.go's — pass a
// minimal fake/nil-safe implementation of the new narrow interface here (its methods are never
// exercised by this file's existing tests, which do not target GetUserRightsSummary).

<!-- Existing test-only stub this plan MUST update in lockstep (admin_users_handler_test.go:27-91) -->
type adminUsersRepoStub struct { membershipsResult *models.AdminUserGroupMembershipsResult; membershipsErr error; ... }
func (s *adminUsersRepoStub) GetUserGroupMemberships(ctx context.Context, appUserID int64) (*models.AdminUserGroupMembershipsResult, error) {
    return s.membershipsResult, s.membershipsErr
}
// GetUserGroupMemberships gains limit/offset params in this plan (F-01 finding #1); the stub's
// method signature must change to match, and a NEW GetUserRightsSummary stub method (with new
// backing fields, e.g. rightsSummaryResult/rightsSummaryErr) must be added since this plan adds
// that method to the AdminUsersRepository interface for the first time.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: permissions package — exported batch-evaluation façade (zero precedence-logic change)</name>
  <files>
    backend/internal/permissions/effective_rights_batch_summary.go,
    backend/internal/permissions/effective_rights_batch_summary_test.go
  </files>
  <behavior>
    - TestEvaluateGroupRightsFromSourcesMatchesResolveGroupRights: given the SAME roles/
      activeMembership/overrides/specializedGrants a fake single-group Resolver would supply,
      assert `Service.EvaluateGroupRightsFromSources(actor, groupID, sources)` returns a
      byte-identical `*GroupRightsResolution` (same Can()/provenance/reason-code values for every
      action) to what `Service.ResolveGroupRights(ctx, actor, groupID)` returns when wired against
      an equivalent fake single-group resolver returning the same facts — proving this is a pure
      re-projection, not a second decision engine (D21).
    - TestResolveGroupRightsBatchMatchesPerGroupResolveGroupRights: given a fake batch resolver
      supplying facts for 3 groups (including one where the actor is a platform admin/disabled —
      exercising the fast path — and one with a real user_deny override), assert
      `Service.ResolveGroupRightsBatch(ctx, actor, []int64{g1,g2,g3})` returns a
      `map[int64]*GroupRightsResolution` where EVERY entry is byte-identical to calling
      `Service.ResolveGroupRights(ctx, actor, gN)` individually for that same gN against an
      equivalent single-group fake resolver.
    - TestResolveGroupRightsBatchIssuesConstantFakeCallCount: assert the fake batch resolver's
      batch methods (ResolveActorGroupMembershipsForGroups/ResolveActorUserOverridesForGroups/
      review-grant batch equivalent) are each called EXACTLY ONCE regardless of whether 3 or 30
      group IDs are passed — proving genuine batching, not a loop that happens to share one Go
      function.
  </behavior>
  <read_first>
    backend/internal/permissions/effective_rights.go (full file — evaluateGroupRights,
    groupRightsSources, loadGroupRightsSources, GroupRightsMembershipResolver/
    GroupRightsOverridesResolver optional-interface pattern; this plan's new batch interfaces
    mirror this exact optional-capability-via-type-assertion convention)
    backend/internal/permissions/effective_rights_capability_impact_preview.go (full file —
    PreviewGroupRightsCapabilityChange, the existing batch-load-then-evaluate-in-memory precedent
    this plan's ResolveGroupRightsBatch should structurally resemble)
    backend/internal/permissions/effective_rights_test.go (existing fake-resolver test pattern to
    reuse for this plan's new tests — do not invent a second fake-resolver convention)
  </read_first>
  <action>
    Create `effective_rights_batch_summary.go` in package `permissions`. Define:

    `type GroupRightsSourcesInput struct { Roles []Action; ActiveMembership bool; Overrides
    []UserCapabilityOverride; SpecializedGrants []SpecializedGrant }` — an EXPORTED mirror of the
    unexported `groupRightsSources`, so callers outside this package (the new `admin_users`
    repository/handler) can supply pre-loaded facts without needing package-internal access.

    `func (s *Service) EvaluateGroupRightsFromSources(actor Actor, fansubGroupID int64, sources
    GroupRightsSourcesInput) *GroupRightsResolution` — converts `GroupRightsSourcesInput` to the
    unexported `groupRightsSources{...}` and calls the existing unexported `evaluateGroupRights`
    directly. Zero new precedence logic — this function's entire body is the type conversion plus
    one delegating call.

    `type GroupRightsMembershipBatchResolver interface { ResolveActorGroupMembershipsForGroups(ctx
    context.Context, appUserID int64, fansubGroupIDs []int64) (map[int64]*GroupMembershipState,
    error) }`, `type GroupRightsOverridesBatchResolver interface {
    ResolveActorUserOverridesForGroups(ctx context.Context, appUserID int64, fansubGroupIDs
    []int64) (map[int64][]UserCapabilityOverride, error) }`, `type SpecializedGrantBatchProvider
    interface { ResolveGroupGrantsForGroups(ctx context.Context, actorAppUserID int64,
    fansubGroupIDs []int64) (map[int64][]SpecializedGrant, error) }` — new optional batch
    interfaces mirroring the existing single-group `GroupRightsMembershipResolver`/
    `GroupRightsOverridesResolver`/`SpecializedGrantProvider` pattern exactly (type-assertion
    discovery, graceful degradation to empty when unsupported — same convention as
    `specializedGrantProviders()`).

    `func (s *Service) ResolveGroupRightsBatch(ctx context.Context, actor Actor, fansubGroupIDs
    []int64, rolesByGroup map[int64][]Action) (map[int64]*GroupRightsResolution, error)` — the
    orchestrator. `rolesByGroup` is passed IN by the caller (Task 2's repository layer already has
    this from `GetUserGroupMemberships`'s existing single query — this function does not re-fetch
    roles itself, avoiding a redundant round trip). For the platform-admin/disabled fast path
    (mirrors `loadGroupRightsSources`'s existing fast-path check), every group in the result map
    gets `evaluateGroupRights(actor, groupID, groupRightsSources{}, allKnownActions)` with zero
    further I/O. Otherwise: if `s.resolver` implements `GroupRightsMembershipBatchResolver`, call
    it ONCE with the full `fansubGroupIDs` slice to get active-membership facts for every group; if
    it implements `GroupRightsOverridesBatchResolver`, call it ONCE for overrides; discover every
    `SpecializedGrantBatchProvider` the resolver's specialized-grant providers support and call
    each ONCE. Assemble one `GroupRightsSourcesInput` per group from `rolesByGroup[groupID]` plus
    the three batch-loaded maps (falling back to `len(rolesByGroup[groupID]) > 0` for
    ActiveMembership when no `GroupRightsMembershipBatchResolver` is available, mirroring
    `loadGroupRightsSources`'s existing single-group fallback), then call
    `EvaluateGroupRightsFromSources` per group — this per-group step is a PURE in-memory call, zero
    I/O, so total SQL round trips stay constant (2-3) regardless of how many groups are in
    `fansubGroupIDs`.

    Document explicitly in a comment above `ResolveGroupRightsBatch` (do not leave this silent, per
    the F-01 finding's explicit instruction): "If the underlying resolver does not implement
    SpecializedGrantBatchProvider for a given specialized-grant source (e.g. review delegation has
    no batch variant wired yet), that source contributes zero grants to every group's batched
    result — a group where the ONLY reason a capability would show allowed=true is a specialized
    grant (not a role or user_allow) may show allowed=false in this batched summary until a batch
    variant is wired. This is a disclosed, bounded scope note for Phase 139's Overview-tab compact
    headline only; the full single-group Rights tab (UserGroupRightsTab.tsx, calling
    ResolveGroupRights unchanged) always includes specialized grants with full fidelity."
  </action>
  <acceptance_criteria>
    <test_command>docker exec team4sv30-backend sh -c "cd /app && go test ./internal/permissions/... -run TestEvaluateGroupRightsFromSources -run TestResolveGroupRightsBatch -v"</test_command>
    <test_command>docker exec team4sv30-backend sh -c "cd /app && go build ./... && go vet ./internal/permissions/..."</test_command>
    <source_assertion>effective_rights_batch_summary.go declares EvaluateGroupRightsFromSources
    with a body of at most a few lines that calls the existing unexported evaluateGroupRights, and
    contains no independent if/switch precedence logic of its own</source_assertion>
  </acceptance_criteria>
  <verify>
    <automated>docker exec team4sv30-backend sh -c "cd /app && go build ./... && go test ./internal/permissions/... -v"</automated>
  </verify>
  <done>All 3 new tests green. Full internal/permissions package suite stays green (zero
  regression in Phase 137/138's existing resolver tests).</done>
</task>

<task type="auto">
  <name>Task 2: Batched repository primitives + GetUserGroupMemberships pagination + GetUserRightsSummary</name>
  <files>
    backend/internal/repository/authz_user_overrides.go,
    backend/internal/repository/authz_permissions.go,
    backend/internal/repository/admin_users_tab_repository.go
  </files>
  <read_first>
    backend/internal/repository/authz_user_overrides.go (full file — LoadCurrentOverrides at
    lines 93-133, the exact single-group query this task adds a batch sibling for)
    backend/internal/repository/authz_permissions.go (lines 190-260 or wherever
    ResolveActorReviewGrantContext's full SQL body ends — the exact single-group query this task
    adds a batch sibling for)
    backend/internal/repository/admin_users_tab_repository.go (lines 64-101, GetUserGroupMemberships
    — the existing single query already returning roles+status per group in ONE round trip; this
    task adds LIMIT/OFFSET to it and adds a new GetUserRightsSummary method alongside it)
    backend/internal/models/admin_users.go (139-01's new AdminUserRightsSummaryPage/
    AdminUserGroupRightsSummaryItem/AdminHeadlineCapabilityState DTOs this method populates)
  </read_first>
  <action>
    In `authz_user_overrides.go`: add `func (r *AuthzUserOverridesRepository)
    LoadCurrentOverridesForGroups(ctx context.Context, appUserID int64, fansubGroupIDs []int64)
    (map[int64][]UserGroupCapabilityOverride, error)` — one query: `SELECT fansub_group_id,
    action_code, effect FROM user_group_capability_overrides WHERE app_user_id = $1 AND
    fansub_group_id = ANY($2::bigint[]) ORDER BY fansub_group_id, action_code`, grouping scanned
    rows into the returned map by `fansub_group_id`. Implement
    `GroupRightsOverridesBatchResolver`'s `ResolveActorUserOverridesForGroups` on
    `AuthzRepository` (or wherever `AuthzUserOverridesRepository` is embedded/reachable from) by
    delegating to this new method and converting `UserGroupCapabilityOverride` to
    `permissions.UserCapabilityOverride` per group.

    In `authz_permissions.go`: add a batch sibling to `ResolveActorReviewGrantContext`, e.g.
    `ResolveActorReviewGrantContextsForGroups(ctx, appUserID int64, fansubGroupIDs []int64)
    (map[int64][]permissions.Action, error)` — same JOIN shape as the existing single-group query
    but `WHERE fgm.app_user_id = $1 AND fgm.fansub_group_id = ANY($2::bigint[])`, `GROUP BY
    fgm.fansub_group_id`, dropping `FOR SHARE OF fgm` (read-only summary, no mutation follows).
    Implement `SpecializedGrantBatchProvider`'s `ResolveGroupGrantsForGroups` by delegating to this
    new method and wrapping each action as `permissions.SpecializedGrant{Action: a, Source:
    "review_delegation"}` (matching `review_grant_provider.go`'s existing source constant).

    Also implement `GroupRightsMembershipBatchResolver`'s `ResolveActorGroupMembershipsForGroups`
    similarly — either via a new one-query batch (`fansub_group_members.status = ANY(...)`-style)
    or, if simpler and equally correct, by deriving `ActiveMembership` per group directly from the
    same `GetUserGroupMemberships` result the caller already has (roles + status) rather than a
    fourth new query — prefer reusing existing data over adding a query, since `GetUserGroupMemberships`
    already returns `MemberStatus` per membership.

    In `admin_users_tab_repository.go`: add `limit int, offset int` parameters to
    `GetUserGroupMemberships`, wrap its existing query with `COUNT(*) OVER()` +
    `ClampAdminListPage(limit, offset)` + `LIMIT/OFFSET` (F-01 finding #1 — the query was
    previously fully unbounded), and add `Meta AdminListMeta` to
    `models.AdminUserGroupMembershipsResult` additively (existing `Memberships` field name/shape
    stays, this is a backward-compatible addition — do not rename `Memberships`). This changes
    `GetUserGroupMemberships`'s public signature — Task 3 updates the `AdminUsersRepository`
    interface and `admin_users_handler_test.go`'s `adminUsersRepoStub` to match (do not update the
    stub here; Task 3 owns every interface-facing consumer in one place).

    Add `func (r *AdminUsersRepository) GetUserRightsSummary(ctx context.Context, appUserID int64,
    limit, offset int, permissionSvc *permissions.Service, matrix ...) (*models.AdminUserRightsSummaryPage,
    error)` (exact dependency-injection shape left to the executor — either thread `permissionSvc`
    in via the repository's constructor, matching how `AdminUsersHandler` already receives
    `authzRepo`, or accept it as a call parameter; pick whichever keeps `AdminUsersRepository`'s
    existing constructor signature stable for its OTHER already-wired callers, documenting the
    choice in the SUMMARY). Steps: (1) call the now-paginated `GetUserGroupMemberships` to get the
    page of memberships with roles+status; (2) build `fansubGroupIDs` from that page; (3) call
    `permissionSvc.ResolveGroupRightsBatch(ctx, actor, fansubGroupIDs, rolesByGroup)` (Task 1) to
    get every group's full `*GroupRightsResolution`; (4) for each group, slice the first 3
    `Can()`-derived states into `HeadlineStates` (mirroring the frontend's existing
    `HEADLINE_CAPABILITY_LIMIT = 3` convention — resolve action labels via the existing role/action
    catalog the same way `UserOverviewTab.tsx`'s `actionLabels` map already does, or return raw
    `action_code` and let the frontend continue resolving labels via its already-loaded
    `listRoleCapabilities()` matrix, whichever avoids duplicating label-resolution logic — prefer
    NOT resolving labels server-side since the frontend's `roleLabelFor`/`actionLabels` pattern
    already exists and this endpoint's response should stay label-agnostic like the raw
    `EffectiveRightState` shape does today), compute `HasDeviation` as `states.some(user_allow ||
    user_deny)` over the FULL evaluated action set (not just the headline slice — matches the
    existing frontend logic byte-for-byte, D-05 parity), and read `RoleLabel`/`OpenClaimsCount`
    from the membership row already fetched in step 1 (OpenClaimsCount: reuse whatever existing
    per-user open-claims count source `AdminUserOverview.OpenClaimsCount` already reads — do not
    recompute it a second way). This is a NEW method on `AdminUsersRepository` that Task 3 also
    adds to the interface, handler, route, and `adminUsersRepoStub` — Task 2 only creates the
    concrete implementation.
  </action>
  <acceptance_criteria>
    <test_command>docker exec team4sv30-backend sh -c "cd /app && go build ./..."</test_command>
    <source_assertion>authz_user_overrides.go declares LoadCurrentOverridesForGroups accepting a
    []int64 and returning a map[int64][]UserGroupCapabilityOverride</source_assertion>
    <source_assertion>admin_users_tab_repository.go's GetUserGroupMemberships accepts limit/offset
    parameters and its SQL contains COUNT(*) OVER()</source_assertion>
    <test_command>docker exec team4sv30-backend sh -c "cd /app && TEAM4S_PHASE139_TEST_DSN=\"$TEAM4S_PHASE139_TEST_DSN\" go test ./internal/repository/... -run TestGetUserRightsSummary -v"</test_command>
  </acceptance_criteria>
  <verify>
    <automated>docker exec team4sv30-backend sh -c "cd /app && go build ./... && go test ./internal/repository/... ./internal/permissions/..."</automated>
  </verify>
  <done>New batch repository methods compile and are wired as the optional interfaces
  ResolveGroupRightsBatch discovers; GetUserGroupMemberships is paginated; GetUserRightsSummary
  assembles the full response using zero per-group SQL beyond the fixed 2-3 batch queries. (Note:
  the module as a whole will not compile clean via `go build ./...` until Task 3 also updates the
  AdminUsersRepository interface to match — this task's own `go build ./...` check may need
  Task 3's changes landed first if executed strictly in isolation; if so, verify this task's new
  repository code by scoping the build to `./internal/repository/...` and `./internal/permissions/...`
  instead, and defer the whole-module build check to Task 3.)</done>
</task>

<task type="auto">
  <name>Task 3: Handler + route + admin-capabilities.yaml contract (F-02 Option B) + interface/stub lockstep update</name>
  <files>
    backend/internal/handlers/admin_users_handler.go,
    backend/internal/handlers/admin_users_handler_test.go,
    backend/cmd/server/admin_routes.go,
    backend/cmd/server/main.go,
    shared/contracts/admin-capabilities.yaml,
    backend/internal/handlers/admin_capability_contract_test.go
  </files>
  <read_first>
    backend/internal/handlers/admin_users_handler.go (the AdminUsersHandler struct/constructor and
    the AdminUsersRepository interface at lines 16-28 — this task adds a permissions-service
    dependency to the struct/constructor AND adds/changes two AdminUsersRepository interface
    methods: GetUserGroupMemberships gains limit/offset per Task 2's repository change, and a new
    GetUserRightsSummary method is added)
    backend/internal/handlers/admin_users_handler_test.go (full file — `buildAdminUsersHandler` at
    lines 158-164 is the SECOND call site of `NewAdminUsersHandler`, alongside `main.go`'s; it
    currently calls `NewAdminUsersHandler(repo, authz, audit)` with exactly 3 arguments. When this
    task adds a 4th constructor parameter, this call site breaks too. `adminUsersRepoStub` at
    lines 27-91 is the third implementer of `AdminUsersRepository` — its `GetUserGroupMemberships`
    method (lines 69-71) must gain limit/offset parameters and a new `GetUserRightsSummary` stub
    method (with new backing fields, e.g. `rightsSummaryResult *models.AdminUserRightsSummaryPage`,
    `rightsSummaryErr error`) must be added, matching whatever exact interface shape this task
    lands on)
    backend/cmd/server/main.go (line ~136 permissionSvc construction, line ~505
    NewAdminUsersHandler call site — permissionSvc is already in scope at that point, no reordering
    needed; this is the FIRST of the two call sites this task updates)
    backend/internal/handlers/admin_effective_rights_handler.go (lines 40-70 — the narrow
    `effectiveRightsPermissionService` interface pattern already used to avoid importing the full
    concrete *permissions.Service type into the handlers package's public surface; mirror this
    narrow-interface convention rather than depending on the concrete type directly)
    shared/contracts/admin-capabilities.yaml (top ~40 lines — the openapi: 3.0.3 structure, the
    existing /api/v1/admin/fansubs/{id}/capabilities path shape to copy for the new path's
    structure)
    backend/internal/handlers/admin_capability_contract_test.go (existing contract-test structure
    — TestEffectiveRightStateProvenanceContract / TestCapabilityOverrideSchemasUnchangedContract —
    this task extends this file with a new test for the rights-summary schema)
  </read_first>
  <action>
    First, update `AdminUsersRepository`'s interface declaration in `admin_users_handler.go`: (a)
    change `GetUserGroupMemberships`'s signature to accept `limit, offset int` (matching Task 2's
    concrete repository method exactly); (b) add a new `GetUserRightsSummary(ctx context.Context,
    appUserID int64, limit, offset int) (*models.AdminUserRightsSummaryPage, error)` method
    (adjust the exact parameter list to match whatever shape Task 2's concrete method landed on).

    Extend `NewAdminUsersHandler`'s constructor with a new narrow interface parameter (e.g.
    `adminUsersRightsResolver` exposing only `ResolveGroupRightsBatch`, mirroring
    `effectiveRightsPermissionService`'s narrow-interface convention rather than depending on the
    concrete `*permissions.Service`), and update `AdminUsersHandler`'s struct + BOTH existing call
    sites to pass it: (1) `main.go`'s `adminUsersHandler := handlers.NewAdminUsersHandler(adminUsersRepo,
    authzRepo, auditLogRepo, permissionSvc)` (`permissionSvc` already in scope), and (2)
    `admin_users_handler_test.go`'s `buildAdminUsersHandler` helper, which calls
    `NewAdminUsersHandler(repo, authz, audit)` today — add a 4th argument there too (a minimal
    fake/nil-safe implementation of the new narrow interface is sufficient; this file's existing
    tests do not exercise `GetUserRightsSummary`). Also update the `GetUserGroupMemberships`
    handler to parse `limit`/`offset` (same pattern as `ListUsers`).

    Add `func (h *AdminUsersHandler) GetUserRightsSummary(c *gin.Context)` mirroring every other
    handler entry point's exact shape (`requirePlatformAdminIdentity` first, `parseUserID`, parse
    `limit`/`offset`, call `h.repo.GetUserRightsSummary(...)`, `internalError` on failure with the
    German message "Rechteübersicht konnte nicht geladen werden.", `c.JSON(http.StatusOK,
    result)`).

    Register the route in `admin_routes.go` immediately after the existing
    `/admin/users/:userId/group-rights` line (matching this file's existing grouping-by-domain
    ordering): `v1.GET("/admin/users/:userId/rights-summary", auth,
    deps.adminUsersHandler.GetUserRightsSummary)`.

    F-02 decision executed here (Option B, per RESEARCH.md's hybrid recommendation — this endpoint
    lives in the SAME contract family as the existing effective-rights endpoints it is adjacent
    to, unlike contributions/media which stayed in admin-content.yaml per 139-03/139-04): add a
    new `paths./api/v1/admin/users/{userId}/rights-summary.get` entry to
    `shared/contracts/admin-capabilities.yaml`, real OpenAPI 3.0.3 shape (copy the structure of
    the existing `/api/v1/admin/fansubs/{id}/capabilities` path), with a `schemas.
    AdminUserRightsSummaryPage`/`AdminUserGroupRightsSummaryItem` schema block mirroring the new Go
    DTOs field-for-field. Extend `admin_capability_contract_test.go` with a new test (e.g.
    `TestAdminUserRightsSummarySchemaContract`) proving the Go DTO's JSON field set matches the new
    OpenAPI schema's declared properties, following this file's existing contract-test pattern
    exactly (it already proves two other schemas this way).

    Finally, update `admin_users_handler_test.go`'s `adminUsersRepoStub` IN LOCKSTEP with the
    interface changes made above: (a) change its `GetUserGroupMemberships` method to accept
    `limit, offset int` (ignoring them in the stub body, same behavior as today — no test in this
    file currently asserts on pagination for this method); (b) add a new
    `GetUserRightsSummary` stub method backed by new `rightsSummaryResult`/`rightsSummaryErr`
    fields on the struct, returning them directly. Without both of these changes,
    `go test ./internal/handlers/...` fails to compile.
  </action>
  <acceptance_criteria>
    <source_assertion>admin_routes.go registers GET /admin/users/:userId/rights-summary routed to
    deps.adminUsersHandler.GetUserRightsSummary</source_assertion>
    <source_assertion>admin-capabilities.yaml declares a paths entry for
    /api/v1/admin/users/{userId}/rights-summary with a get operation</source_assertion>
    <source_assertion>admin_users_handler_test.go's buildAdminUsersHandler calls
    NewAdminUsersHandler with the new (4th) constructor argument, and adminUsersRepoStub declares a
    GetUserRightsSummary method plus a GetUserGroupMemberships method accepting limit/offset</source_assertion>
    <test_command>docker exec team4sv30-backend sh -c "cd /app && go build ./... && go test ./internal/handlers/... -run AdminCapability"</test_command>
    <test_command>docker exec team4sv30-backend sh -c "cd /app && go test ./internal/handlers/... -run AdminUsers"</test_command>
  </acceptance_criteria>
  <verify>
    <automated>docker exec team4sv30-backend sh -c "cd /app && go build ./... && go test ./internal/handlers/... -v"</automated>
  </verify>
  <done>New route reachable, authorized by the unchanged platform-admin gate, contract-tested
  against admin-capabilities.yaml's new schema entry. adminUsersRepoStub and buildAdminUsersHandler
  both compile against the updated AdminUsersRepository interface and NewAdminUsersHandler
  constructor — `go build ./...` and `go test ./internal/handlers/...` are both clean across the
  whole module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser -> GET /admin/users/:userId/rights-summary | New endpoint surfacing every group's capability summary for one user in one response |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-10 | Elevation of Privilege | GetUserRightsSummary handler | accept | unchanged requirePlatformAdminIdentity gate; the endpoint reveals no more than the existing per-group effective-rights endpoint already reveals when called once per group — this is a projection of the same data, not a new privilege boundary |
| T-139-11 | Tampering | LoadCurrentOverridesForGroups / batch review-grant query | mitigate | fansub_group_id = ANY($2::bigint[]) with the group ID list always sourced server-side from the user's OWN paginated GetUserGroupMemberships result, never taken directly as unvalidated client input |
| T-139-12 | Information Disclosure | disclosed specialized-grant scope note | accept | explicitly documented in code comments and this plan (not a silent semantic change); the full-fidelity single-group Rights tab remains the authoritative surface for any specialized-grant-sourced capability |
</threat_model>

<verification>
`docker exec team4sv30-backend sh -c "cd /app && go build ./... && go test ./internal/permissions/... ./internal/repository/... ./internal/handlers/..."` green; new route reachable and
contract-tested.
</verification>

<success_criteria>
A single new endpoint answers every group's compact rights summary for a user in O(1) SQL round
trips (not O(N) groups). Precedence logic is provably identical to the existing single-group
resolver (D21). GetUserGroupMemberships is no longer unbounded. `adminUsersRepoStub` and
`buildAdminUsersHandler` in `admin_users_handler_test.go` stay in lockstep with
`AdminUsersRepository`/`NewAdminUsersHandler`, so the handlers package's existing test suite keeps
compiling.
</success_criteria>

<output>
Create `.planning/phases/139-scalable-user-admin-projections/139-05-SUMMARY.md` when done
</output>
</output>
