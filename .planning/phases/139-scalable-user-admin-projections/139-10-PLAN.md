---
phase: 139-scalable-user-admin-projections
plan: 10
type: execute
wave: 7
depends_on: ["139-06", "139-08", "139-09"]
files_modified: []
autonomous: false
requirements: [UADM-02, UADM-03, UADM-04, UADM-05, UADM-06, UADM-07, UADM-08, QUAL-06]

must_haves:
  truths:
    - "D01: nothing across 139-01 through 139-09 expanded into Claims, Änderungen/Audit redesign, role/capability redesign, review delegation, streaming, new media editing, or new contribution editing — confirmed by a diff review of files_modified across all nine prior plans against 139-CONTEXT.md's explicit non-goals list"
    - "D27: no Metabase-style deep technical media/storage dashboard was built anywhere in this phase, even in a small form — confirmed alongside D18's per-plan checks (139-04/139-09)"
    - "D25/QUAL-06: full backend + frontend suites are green, with every new-vs-pre-existing failure explicitly triaged against the documented baseline (139-RESEARCH.md's confirmed-current baseline, not the stale CONTEXT.md numbers)"
    - "D26/UADM-08: no page-level horizontal overflow at 1440x900 (desktop-first back-office target) and graceful degradation confirmed live at a narrow container width, keyboard operability confirmed for filters/pagination on both new tabs"
    - "F-03: the live 'Nur Abweichungen' filter is confirmed, by a human, to show the real independent-and-different row 139-06's seed script produced — not an empty result set"
    - "UADM-07: both Contributions and Media tabs are confirmed, by a human, to state their informational posture and the Rights tab states its actionable posture, exactly once each, via SectionHeader"
    - "Every requirement UADM-02 through UADM-08 and QUAL-06 has at least one automated test proving it, per 139-VALIDATION.md's Phase Requirements -> Test Map, now filled in with real plan/task IDs instead of TBD"
  artifacts: []
  key_links: []
---

<objective>
Close Phase 139 with the mandatory full-suite regression gate, the QUAL-06/UADM-08 live
verification steps VALIDATION.md flags as manual-only (container-query overflow, F-03 live data),
a scope-boundary confirmation against D01/D27's explicit non-goals, and a baseline-vs-regression
triage that distinguishes Phase-139-caused failures from the documented pre-existing debt
(CONTEXT.md's baseline note, RESEARCH.md's corrected, larger current baseline count).

Purpose: no phase-level artifact change happens here — this plan's only job is to PROVE the nine
prior plans (139-01 through 139-09) collectively satisfy every UADM-02..08/QUAL-06 requirement
without scope creep into any of CONTEXT.md's named exclusions, catch any cross-plan integration gap
the individual plans' scoped tests could not see (e.g. a filter param name mismatch between
139-02's TS types and 139-03's Go query params), and get explicit human sign-off on the two things
automation genuinely cannot verify (real CSS container-query overflow behavior, and that the F-03
seed data is visible through the live UI).

Output: a full green (or explicitly-triaged) test suite, a filled-in 139-VALIDATION.md Phase
Requirements -> Test Map, and human confirmation of the two manual-only verifications.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-scalable-user-admin-projections/139-CONTEXT.md
@.planning/phases/139-scalable-user-admin-projections/139-RESEARCH.md
@.planning/phases/139-scalable-user-admin-projections/139-VALIDATION.md
@.planning/phases/139-scalable-user-admin-projections/139-UI-SPEC.md
</context>

<interfaces>
<!-- Confirmed CURRENT baseline this plan must triage against (139-RESEARCH.md, run 2026-08-24) -->
Frontend full suite (pre-Phase-139): 17 failed test files, 46 failed tests (of 2072 total) — the 5
  files CONTEXT.md named PLUS 12 additional files RESEARCH.md discovered are ALL pre-existing,
  none touch any Phase-139 file.
Backend full suite (pre-Phase-139): 65 FAIL lines across internal/handlers (24, nil
  permissions.Service.LoadCache), internal/migrations (5, TEAM4S_PHASE128_TEST_DSN unset),
  internal/repository (36, TEAM4S_PHASE128_TEST_DSN unset + one unrelated live-UAT-server
  unreachability + one real pre-existing assertion failure) — zero touch any admin_users-prefixed
  file. internal/permissions and internal/services are fully green pre-Phase-139.

<!-- Explicit non-goals to confirm untouched (139-CONTEXT.md, D01/D27) -->
Do NOT expand into: Claims, Änderungen/Audit redesign, role/capability redesign, review delegation,
Streaming, new media editing, new contribution editing, Metabase integration, technical
storage-health dashboards.

<!-- Full suite commands (139-VALIDATION.md) -->
Backend full: docker exec team4sv30-backend sh -c "cd /app && go test ./..."
Backend real-Postgres tier: docker exec team4sv30-backend sh -c "cd /app && TEAM4S_PHASE139_TEST_DSN=<dsn> go test ./internal/repository/... ./internal/testsupport/... ./internal/permissions/..."
Frontend full: docker exec team4sv30-frontend sh -c "cd /app && npx vitest run"
Frontend typecheck: docker exec team4sv30-frontend sh -c "cd /app && npx tsc --noEmit"

<!-- Live UAT access path (CLAUDE.md) -->
Browser login/UAT via SSH tunnel: http://127.0.0.1:3300 (frontend also directly reachable at
http://192.168.235.196:3000)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Full regression suite + baseline-vs-regression triage + D01/D27 scope-boundary check</name>
  <files>.planning/phases/139-scalable-user-admin-projections/139-VALIDATION.md</files>
  <read_first>
    .planning/phases/139-scalable-user-admin-projections/139-RESEARCH.md (the confirmed baseline
    table quoted in the interfaces block above — the exact file/failure-count list every new
    failure after this plan's 9 prior plans must be diffed against)
    .planning/phases/139-scalable-user-admin-projections/139-VALIDATION.md (Wave 0 Requirements
    checklist and the Phase Requirements -> Test Map table this task fills in)
    .planning/phases/139-scalable-user-admin-projections/139-CONTEXT.md (the explicit non-goals
    list this task's D01/D27 scope check runs against)
  </read_first>
  <action>
    Run `git diff --stat main...HEAD -- backend/ frontend/ shared/ scripts/` (or the equivalent
    committed-history diff across 139-01 through 139-09's commits) and confirm every touched file
    is one this phase's own `files_modified` frontmatter lists across the nine prior plans, and
    that none of them are Claims/Änderungen/role-capability-matrix/review-delegation/streaming/a
    second media-editing or contribution-editing surface, or any Metabase-style storage-health
    dashboard file (D01/D27) — if any out-of-scope file appears, flag it explicitly rather than
    silently accepting it.

    Run the full backend suite (`go test ./...`), the real-Postgres tier with a disposable
    `TEAM4S_PHASE139_TEST_DSN` (create one per the documented Phase-137/138/139 manual convention
    if none exists this session, drop it after), and the full frontend suite (`npx vitest run`)
    plus `npx tsc --noEmit`. For every failing test/file, cross-reference against the confirmed
    baseline quoted in `<interfaces>` above: if the failing file/test is in that baseline list AND
    is not one of the files this phase's plans (139-01 through 139-09) modified, mark it
    pre-existing and out of scope (per CONTEXT.md's explicit instruction: do not attribute
    unchanged pre-existing failures to Phase 139). If a failing test IS in a file this phase
    touched, or is a genuinely NEW failure not in the baseline list, it is a Phase-139 regression —
    fix it before proceeding to Task 2. Update `139-VALIDATION.md`'s "Per-Task Verification Map"
    table, replacing every "TBD" Plan/Wave/Task-ID cell with the real plan numbers (139-01 through
    139-09) and the real automated command each requirement's test now runs under, and set
    `nyquist_compliant: true` in its frontmatter once every row has a real, passing command.
  </action>
  <acceptance_criteria>
    <test_command>docker exec team4sv30-backend sh -c "cd /app && go build ./... && go test ./..."</test_command>
    <test_command>docker exec team4sv30-frontend sh -c "cd /app && npx tsc --noEmit && npx vitest run"</test_command>
    <source_assertion>139-VALIDATION.md's frontmatter reads nyquist_compliant: true and no row in
    its Per-Task Verification Map table still reads "TBD"</source_assertion>
  </acceptance_criteria>
  <verify>
    <automated>docker exec team4sv30-backend sh -c "cd /app && go test ./... 2>&1 | tail -30"</automated>
    <automated>docker exec team4sv30-frontend sh -c "cd /app && npx vitest run 2>&1 | tail -30"</automated>
  </verify>
  <done>Every failing test is either explicitly triaged as documented pre-existing debt (with the
  exact file/reason cited) or fixed as a genuine Phase-139 regression. No out-of-scope file/feature
  found in the D01/D27 scope check. 139-VALIDATION.md's Verification Map is fully filled in,
  nyquist_compliant: true.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live UAT — grouped projections, batched rights summary, container-query overflow, F-03 live data</name>
  <read_first>
    .planning/phases/139-scalable-user-admin-projections/139-UI-SPEC.md
    .planning/phases/139-scalable-user-admin-projections/139-06-SUMMARY.md
    frontend/src/app/admin/users/tabs/UserContributionsTab.tsx
    frontend/src/app/admin/users/tabs/UserMediaTab.tsx
    frontend/src/app/admin/users/tabs/UserOverviewTab.tsx
    frontend/src/app/admin/users/tabs/UserGroupRightsTab.tsx
    CLAUDE.md
  </read_first>
  <files>none — shared in-app browser verification only</files>
  <action>
    Before opening the browser, confirm 139-06's seed script ran successfully this session (check
    its printed anime/project/user identifiers in `139-06-SUMMARY.md`) so the "Nur Abweichungen"
    check below has real data to find. Then, via the canonical SSH-tunnel path
    (`http://127.0.0.1:3300`, per CLAUDE.md), log in as a platform admin and walk through the six
    checks listed in `<how-to-verify>` below in order, on the exact user/anime/project 139-06's
    seed script reported. Capture a screenshot or explicit written observation for each of the six
    checks before reporting the resume-signal.
  </action>
  <acceptance_criteria>
    - Overview tab (default tab) loads every group's summary with no more than one rights-related
      network request, and GroupSummaryCard's rendered text is unchanged from Phase 138's shipped
      D-05 example shape.
    - Beiträge tab shows the project standard always visible, a real "Abweichung vom
      Projektstandard" badge with inline (not click-gated) delta text on the 139-06-seeded project,
      and "Nur Abweichungen" narrows correctly rather than returning empty.
    - Medien tab groups by Release/Episode, shows exactly one "Release-Medien öffnen" button per
      block, and contains zero instances of "Berechtigung aktiv/fehlt", "release_version:", or any
      storage-path/format-analysis text.
    - Rechte-Tab fetches no group's rights until a group is explicitly selected.
    - No page-level horizontal scrollbar at any tested narrow width on either new tab.
    - Every filter/pagination control on both new tabs is keyboard-reachable and operable with a
      visible focus outline.
  </acceptance_criteria>
  <verify>
    <automated>echo "This task is human-verification-only; the six checks above have no automated equivalent (real CSS container-query overflow and live seeded-data visibility, per 139-VALIDATION.md's Manual-Only Verifications table)."</automated>
  </verify>
  <what-built>
    Server-side grouped Contributions and Media projections (139-03/139-04/139-08/139-09), a
    batched Overview rights summary and a lazy-loading Rights tab (139-05/139-07), container-query
    responsive layouts for both new tabs (139-08/139-09), and a real independent-and-different
    override row seeded live (139-06) for the "Nur Abweichungen" filter to actually show something.
  </what-built>
  <how-to-verify>
    1. Via the SSH-tunnel path (`http://127.0.0.1:3300`, per CLAUDE.md), log in as a platform
       admin and open any user's detail page. Confirm the Overview tab (default tab) loads without
       a visible delay caused by per-group requests, and shows each group's role + up to 3
       capabilities + deviation line exactly as before (Phase 138 D-05 shape unchanged).
    2. Open the Beiträge tab. Confirm anime/project blocks render with the project standard always
       visible, and confirm at least one project shows an "Abweichung vom Projektstandard" badge
       WITH inline delta text visible without clicking — this should be the real row 139-06's seed
       script produced. Toggle "Nur Abweichungen" and confirm the list narrows to only blocks
       containing a real deviation (not an empty result).
    3. Open the Medien tab. Confirm items are grouped under Release/Episode blocks, thumbnails
       load lazily, and each block has exactly one "Release-Medien öffnen" button that opens the
       correct `/me/releases/{id}/workspace` route. Confirm no "Berechtigung aktiv/fehlt" badge
       appears anywhere, no raw `release_version:<id>` text appears anywhere, and no physical
       path/storage id/derivative-inventory/format-analysis text appears anywhere (D18).
    4. Open the Rechte-Tab (Rights). Confirm no group's rights are visible until you select a
       group from the list, and confirm selecting a group fetches only that group (watch the
       Network tab: no burst of parallel effective-rights requests on tab load).
    5. Resize the browser window down to a narrow width on the Beiträge and Medien tabs (or use
       DevTools device toolbar). Confirm the filter toolbar stacks to one column, card headers
       wrap instead of overflowing, and there is no page-level horizontal scrollbar at any width
       you test.
    6. Tab through the filter controls and Pagination buttons on both tabs using only the
       keyboard; confirm every control receives a visible focus outline and is operable (Enter/
       Space activates buttons, arrow/Enter works for Select).
  </how-to-verify>
  <resume-signal>Type "approved" once all six checks pass, or describe exactly which check failed
  and what you observed instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Full-phase surface | This plan verifies, it does not introduce new code — no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-18 | (verification-only) | n/a | accept | this plan's tasks are read-only verification and documentation updates; no production code changes originate here |
</threat_model>

<verification>
Full backend + frontend suites green (accounting for the documented, re-confirmed baseline).
Human checkpoint approved for the six live-UAT items above. D01/D27 scope-boundary check clean.
</verification>

<success_criteria>
Every UADM-02 through UADM-08 and QUAL-06 requirement has a real, passing automated test AND, where
automation cannot reach (container-query overflow, live F-03 data visibility), explicit human
confirmation. Phase 139 is provably complete against its own ROADMAP.md success criteria and stayed
inside CONTEXT.md's declared boundaries — not just "plans executed."
</success_criteria>

<output>
Create `.planning/phases/139-scalable-user-admin-projections/139-10-SUMMARY.md` when done
</output>
