---
phase: 139-scalable-user-admin-projections
plan: 08
type: execute
wave: 6
depends_on: ["139-02", "139-07"]
files_modified:
  - frontend/src/app/admin/users/tabs/UserContributionsTab.tsx
  - frontend/src/app/admin/users/tabs/contributionsTab.module.css
  - frontend/src/app/admin/users/tabs/UserContributionsTab.test.tsx
autonomous: true
requirements: [UADM-02, UADM-03, UADM-04, UADM-06, UADM-07, UADM-08]

must_haves:
  truths:
    - "D02/D03: every rendered project block shows the anime title, project/group badge, and an always-visible project-standard row — never behind an accordion"
    - "D04/D05/D06: standard-equivalent ranges render with Badge variant=muted 'Entspricht Projektstandard'; real deviations render with Badge variant=warning 'Abweichung vom Projektstandard' plus an always-visible inline delta line, never behind a click"
    - "D08/D09: Pagination paginates whole project blocks; the result-count Badge in SectionHeader's actions reflects the same Meta.Total the Pagination primitive uses"
    - "D10/D23: every filter (Anime, Projekt/Gruppe, Beitragsrolle, Nur Abweichungen, Zeitraum) writes to the URL via useUserContributionsFilters and triggers a server refetch — zero client-side grouping/filtering/range-collapsing exists in this component"
    - "UADM-07: SectionHeader's description states the informational posture and points to the real edit surfaces (D20)"
    - "UADM-08/D26: the tab's root list container declares container-type:inline-size + a named container, with @container rules at 760px (filter stack) and 600px (card header wrap, chip wrap) — no page-level horizontal overflow at any width"
    - "CLAUDE.md: every filter control is a @/components/ui primitive (Select/FormField/Switch/DatePicker/Button) — zero hand-built native <select>/<input>/<button>"
  artifacts:
    - path: "frontend/src/app/admin/users/tabs/UserContributionsTab.tsx"
      provides: "Full rewrite: grouped-card projection replacing the flat Table"
    - path: "frontend/src/app/admin/users/tabs/contributionsTab.module.css"
      provides: "Container-query responsive rules for the new grouped-card layout"
  key_links:
    - from: "frontend/src/app/admin/users/tabs/UserContributionsTab.tsx"
      to: "frontend/src/app/admin/users/useUserContributionsFilters.ts"
      via: "useUserContributionsFilters() hook"
      pattern: "useUserContributionsFilters"
    - from: "frontend/src/app/admin/users/tabs/UserContributionsTab.tsx"
      to: "frontend/src/lib/api.ts"
      via: "getAdminUserContributions(userId, params)"
      pattern: "getAdminUserContributions"
---

<objective>
Fully rewrite `UserContributionsTab.tsx` from its current flat `Table`-based rendering (242 lines,
zero grouping, zero filters, zero pagination) into the grouped-card projection UI-SPEC locks:
Anime+Project blocks with an always-visible project standard, collapsed standard-equivalent ranges,
and always-visible real deviations — consuming 139-03's grouped backend endpoint and 139-02's
filter hook, entirely through `@/components/ui` primitives.

Purpose: closes UADM-02/UADM-03/UADM-04 on the visible surface (the backend alone does not satisfy
the roadmap goal — an admin must be able to SEE the grouped, deviation-highlighted projection).

Output: `UserContributionsTab.tsx` rewritten; a new `contributionsTab.module.css` for the
container-query responsive layer (this component currently styles entirely via inline `style={{}}`
objects, which cannot express container queries, per RESEARCH.md Pitfall 5); `
UserContributionsTab.test.tsx` updated/rewritten to assert the new grouped-card behavior (the file
is already red on unrelated Phase-136 grounds — this plan must cleanly separate that pre-existing
failure from any new assertions, per CONTEXT.md's baseline-debt instruction).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-scalable-user-admin-projections/139-CONTEXT.md
@.planning/phases/139-scalable-user-admin-projections/139-DISCUSS.md
@.planning/phases/139-scalable-user-admin-projections/139-UI-SPEC.md
@.planning/phases/139-scalable-user-admin-projections/139-PATTERNS.md
</context>

<interfaces>
<!-- UI-SPEC's locked Component Inventory — Contributions Tab (verbatim structural mapping) -->
Tab purpose banner + result count: SectionHeader(title="Beiträge", description=<informational
  copy below>, actions=Badge variant="neutral" showing Meta.Total)
Filter bar: Toolbar(leading=filter cluster, trailing=reset Button + count Badge)
Anime filter: FormField(label="Anime") > Select, options from filter_options.animes
Projekt/Gruppe filter: FormField(label="Projekt/Gruppe") > Select, options from filter_options.groups
Beitragsrolle filter: FormField(label="Beitragsrolle") > Select, options from useRoleCatalog('anime_contribution')
Nur-Abweichungen: Switch label="Nur Abweichungen"
Zeitraum: two FormField("Von"/"Bis") > DatePicker
Reset: Button variant="ghost" "Filter zurücksetzen"
One project block: Card variant="nestedFlat"
Block header: Anime title (Heading 16/700) + Badge variant="neutral" project/group name
Project standard row (D03, always visible): plain text row (Label style) + role chips
  (Badge variant="neutral" className=ROLE_CATALOG_CHIP_CLASS)
Standard-equivalent range row: range label (Body) + Badge variant="muted" "Entspricht Projektstandard"
Real-deviation row: range label (Body) + Badge variant="warning" "Abweichung vom Projektstandard"
  + inline delta line directly beneath, ALWAYS visible, never behind a click
Pagination: Pagination primitive, bottom of tab
Loading/Error/Empty: LoadingState / ErrorState(+retry action) / EmptyState (two variants)

<!-- Copywriting Contract (exact locked strings) -->
Purpose banner: "Informativ — zeigt die fachlichen Beiträge dieses Benutzers nach Anime und Projekt.
  Änderungen erfolgen in den bestehenden Projekt- und Release-Arbeitsflächen."
Secondary CTA (single-project blocks only): "Projekt öffnen" (Button variant="secondary",
  href=`/me/projects/${animeId}/group/${fansubGroupId}`)
Nur-Abweichungen toggle label: "Nur Abweichungen"
Standard-range badge text: "Entspricht Projektstandard" (Badge variant="muted")
Deviation badge text: "Abweichung vom Projektstandard" (Badge variant="warning")
Reset button: "Filter zurücksetzen" (Button variant="ghost")
Empty (no data): "Keine Beiträge vorhanden."
Empty (filtered to zero): "Keine Beiträge für diese Filter." + body "Filter anpassen oder
  zurücksetzen, um weitere Einträge zu sehen." + Button variant="ghost" "Filter zurücksetzen" action
Error: title "Fehler beim Laden", body "Daten konnten nicht geladen werden. Erneut versuchen." +
  Button variant="secondary" "Erneut versuchen" wired to loadData()

<!-- Container-query responsive rules (D26, locked breakpoints) -->
Root container: container-type: inline-size; container-name: admin-user-projection;
@container admin-user-projection (max-width: 760px) { toolbar filter fields stack to one column;
  reset Button + count Badge move below the filter stack }
@container admin-user-projection (max-width: 600px) { grouped Card block header row wraps;
  role-chip rows flex-wrap: wrap }
Reference syntax already shipped in this codebase (copy the @container mechanism, not the content):
  frontend/src/components/profile/RoleBadgeCard.module.css:241 (@container member-badge-carousel)
  frontend/src/app/members/[slug]/page.module.css:90-97 (container-type/container-name declaration)

<!-- Chip pattern to reuse exactly (AnimeGroupCard.tsx:180-222, 191) -->
import { ROLE_CATALOG_CHIP_CLASS, labelForRole, presentationForRole } from '@/lib/roleCatalog'
<Badge variant="neutral" className={ROLE_CATALOG_CHIP_CLASS} data-color-key={presentationForRole(...).colorKey}>{label}</Badge>
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite UserContributionsTab.tsx as the grouped-card projection</name>
  <files>frontend/src/app/admin/users/tabs/UserContributionsTab.tsx</files>
  <behavior>
    - Test: given a mocked getAdminUserContributions response with one AdminContributionProjectBlock
      containing a standard-equivalent range and a deviation range, the component renders the
      anime title, the project-standard role chips, a "Entspricht Projektstandard" badge for the
      first range, and a "Abweichung vom Projektstandard" badge PLUS the deviation's
      DeviationDetail text for the second range, both range rows visible without any additional
      user interaction (no click needed to reveal the deviation detail).
    - Test: given a mocked response with meta.total=42 and a page of 25 items, the result-count
      Badge in SectionHeader's actions and the Pagination primitive both reflect 42, not the
      length of the current page's Data array.
    - Test: changing the "Nur Abweichungen" Switch calls getAdminUserContributions again with
      only_deviations=true encoded in the request (assert on the mocked fetch call args, not on
      any client-side filtering of the already-rendered list).
    - Test: the empty-filtered-to-zero state renders "Keine Beiträge für diese Filter." with a
      "Filter zurücksetzen" action, distinct from the true-empty "Keine Beiträge vorhanden." state.
    - Test: the error state's "Erneut versuchen" button re-invokes the load function (assert the
      mocked fetch is called an additional time after clicking it).
  </behavior>
  <read_first>
    frontend/src/app/admin/users/tabs/UserContributionsTab.tsx (full file, 242 lines — the
    ContributionSection/Table/TableRow structure at lines 42-149 is deleted entirely; the
    loadData/isLoading/error state-management skeleton at lines 151-176 is the one part to KEEP
    the shape of, adapted to call the new paginated getAdminUserContributions)
    frontend/src/app/admin/users/useUserContributionsFilters.ts (139-02 — the hook this component
    now imports for every filter's state/setters)
    frontend/src/lib/api.ts (139-07 — the paginated getAdminUserContributions(userId, params)
    function this component calls)
    frontend/src/components/contributions/AnimeGroupCard.tsx (lines 180-222 — the Card
    variant="nestedFlat" + role-chip + single-project-action-button shape to reuse structurally;
    this file is NOT modified, only its patterns are copied)
    frontend/src/components/ui (index — confirm exact prop names for SectionHeader, Toolbar,
    FormField, Select, Switch, DatePicker, Badge, Card, Pagination, LoadingState, ErrorState,
    EmptyState before writing JSX against them)
    frontend/src/lib/roleCatalog.ts (ROLE_CATALOG_CHIP_CLASS/labelForRole/presentationForRole
    exports, and frontend/src/providers/RoleCatalogProvider.tsx's useRoleCatalog hook — the
    existing role-catalog source for the Beitragsrolle filter's Select options)
  </read_first>
  <action>
    Delete the entire `ContributionSection`/`Table`/`TableRow` rendering block (lines ~42-149) and
    the flat `getAdminUserContributions(userId)` single-arg call. Import and call
    `useUserContributionsFilters()` for all filter state/setters. Build the new component tree
    exactly per the `<interfaces>` Component Inventory above: `SectionHeader` with the locked
    purpose-banner copy and a `Badge variant="neutral"` actions slot showing `data?.meta.total`;
    a `Toolbar` with `FormField`+`Select` for Anime/Projekt-Gruppe (options from
    `data.filter_options.animes`/`.groups`, re-populated on every successful load, not
    hardcoded), `FormField`+`Select` for Beitragsrolle (options from
    `useRoleCatalog('anime_contribution')`, unchanged existing hook), a `Switch` for "Nur
    Abweichungen", two `FormField`+`DatePicker` for Von/Bis, and a trailing `Button
    variant="ghost"` "Filter zurücksetzen" that calls each filter setter with an empty value (or a
    dedicated `resetAll` helper added to the filter hook if that is cleaner — keep the hook's
    existing per-field setter API from 139-02 as the primary contract, add a thin local reset
    wrapper here if needed).

    For each `AdminContributionProjectBlock` in `data.data`: render a `Card variant="nestedFlat"`
    with a header row (anime title as an `h3`/`strong` at Heading 16/700 per Typography, plus
    `Badge variant="neutral"` showing `fansub_group_name`), the always-visible project-standard row
    (role chips via `ROLE_CATALOG_CHIP_CLASS`/`presentationForRole`, reusing the exact
    `AnimeGroupCard.tsx` chip pattern), and one row per `AdminContributionRangeEntry`: if
    `!is_deviation`, render `{from_label}–{to_label}` (or just `{from_label}` when they are equal)
    plus `Badge variant="muted"` "Entspricht Projektstandard"; if `is_deviation`, render the same
    range label plus `Badge variant="warning"` "Abweichung vom Projektstandard" AND, directly
    beneath it in the same row group, the `deviation_detail` text at Body 14px — never behind a
    disclosure/accordion (UI-SPEC Divergence #2, locked). If the block's Anime maps to exactly one
    project (i.e., this admin surface's per-user data naturally has one group per anime+project
    block already, per the grouping key itself), render `Button variant="secondary"` "Projekt
    öffnen" linking to `/me/projects/${anime_id}/group/${fansub_group_id}` in the block header's
    trailing area.

    Render `Pagination` bound to `data.meta` beneath the list, driving `handlePageChange` from the
    filter hook. Render `LoadingState`/`ErrorState` (with `action` wired to re-invoke `loadData`)/
    `EmptyState` (two variants: true-empty vs filtered-to-zero, distinguished by whether any filter
    is currently active) per the Copywriting Contract's exact locked strings.

    Delete the old single-arg `getAdminUserContributions(userId)` call and its response typing;
    `loadData` now calls `getAdminUserContributions(userId, params)` where `params` comes from
    `useUserContributionsFilters()`, inside a `useCallback` depending on `[userId, params]`
    (matches `useClaimsListFilters.ts`'s own documented rationale for why `params` must be a
    stable `useMemo`'d reference).
  </action>
  <acceptance_criteria>
    <source_assertion>UserContributionsTab.tsx contains no import of Table or TableRow from
    @/components/ui</source_assertion>
    <source_assertion>UserContributionsTab.tsx imports and calls
    useUserContributionsFilters</source_assertion>
    <source_assertion>UserContributionsTab.tsx contains the literal strings "Entspricht
    Projektstandard" and "Abweichung vom Projektstandard"</source_assertion>
    <source_assertion>UserContributionsTab.tsx renders deviation_detail text without any onClick/
    expanded-state gate around it (no useState toggle controls its visibility)</source_assertion>
    <test_command>docker exec team4sv30-frontend sh -c "cd /app && npx vitest run src/app/admin/users/tabs/UserContributionsTab 2>&1 | tail -80"</test_command>
  </acceptance_criteria>
  <verify>
    <automated>docker exec team4sv30-frontend sh -c "cd /app && npx tsc --noEmit"</automated>
  </verify>
  <done>Component fully replaces the flat table with the grouped-card projection; every filter is
  a real @/components/ui primitive; deviations render inline, never gated.</done>
</task>

<task type="auto">
  <name>Task 2: Container-query CSS module + rewrite UserContributionsTab.test.tsx (baseline-clean)</name>
  <files>
    frontend/src/app/admin/users/tabs/contributionsTab.module.css,
    frontend/src/app/admin/users/tabs/UserContributionsTab.test.tsx
  </files>
  <read_first>
    frontend/src/components/profile/RoleBadgeCard.module.css (lines 235-250 — the exact
    @container syntax already shipped in this codebase, proving it works without a new
    dependency)
    frontend/src/app/members/[slug]/page.module.css (lines 85-100 — container-type/container-name
    declaration convention)
    frontend/src/app/admin/users/tabs/UserContributionsTab.test.tsx (current file — read in full;
    RESEARCH.md confirms 2 of its existing failures are pre-existing Phase-136 hex-color-
    normalization-vs-stale-fixture debt, unrelated to this plan; every OTHER assertion in this file
    tests the OLD flat-table rendering this plan just deleted and must be replaced, not left
    stale)
  </read_first>
  <action>
    Create `contributionsTab.module.css`: declare `.root { container-type: inline-size;
    container-name: admin-user-projection; }` on the tab's outermost wrapper class, imported and
    applied in `UserContributionsTab.tsx` (Task 1) as the root list container's className. Add
    `@container admin-user-projection (max-width: 760px) { .toolbarFields { flex-direction:
    column; } .toolbarTrailing { margin-top: var(--space-2); } }` and `@container
    admin-user-projection (max-width: 600px) { .blockHeader { flex-wrap: wrap; } .roleChips {
    flex-wrap: wrap; } }`. Every header/meta row combining text + badges uses `flex-wrap: wrap`
    and `min-width: 0` on its flex children (RESEARCH.md's cited most-common overflow-cause
    pattern in this codebase's own CSS history) — apply this defensively even outside the two
    named breakpoints, on the block header row itself.

    Rewrite `UserContributionsTab.test.tsx` from scratch against the new grouped-card component:
    port over the RED tests written in Task 1's `<behavior>` block (they should already exist from
    Task 1's TDD cycle — this task's job is to ensure the FULL test file is internally consistent,
    removing every assertion that referenced the deleted Table/TableRow/ContributionSection
    structure). Explicitly comment at the top of the file: "The 2 pre-existing Phase-136
    hex-color-normalization failures documented in 139-RESEARCH.md/139-CONTEXT.md applied to the
    OLD flat-table fixture data and no longer apply to this file's rewritten grouped-card
    fixtures — if a NEW failure appears here after this rewrite, it is a Phase-139 regression, not
    inherited debt." Run the full file and confirm zero failures (this file's own pre-existing
    debt was tied to fixture color-key values in the old row-rendering path, which no longer
    exists post-rewrite — confirm this is genuinely true by running the suite, not by assumption).
  </action>
  <acceptance_criteria>
    <source_assertion>contributionsTab.module.css declares container-type: inline-size and at
    least two @container admin-user-projection blocks</source_assertion>
    <test_command>docker exec team4sv30-frontend sh -c "cd /app && npx vitest run src/app/admin/users/tabs/UserContributionsTab -v 2>&1 | tail -100"</test_command>
  </acceptance_criteria>
  <verify>
    <automated>docker exec team4sv30-frontend sh -c "cd /app && npx vitest run src/app/admin/users/tabs/UserContributionsTab"</automated>
  </verify>
  <done>CSS module exists with the locked 760px/600px container-query breakpoints. Test file is
  fully green against the new component (the old Phase-136 debt either no longer applies or is
  explicitly re-triaged and documented, not silently ignored).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser -> rendered contribution data | All rendered fields (anime title, role labels, deviation detail text) originate from the platform-admin-only backend response; no user-supplied HTML is ever dangerouslySetInnerHTML'd here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-15 | Tampering | filter query params in the URL | accept | client-supplied filter values are advisory only; the server (139-03) independently validates/parameterizes every value, so a manipulated URL can at worst request a different (still authorized) filtered view of the same platform-admin-only data |
</threat_model>

<verification>
`docker exec team4sv30-frontend sh -c "cd /app && npx tsc --noEmit && npx vitest run src/app/admin/users/tabs/UserContributionsTab"` fully green.
</verification>

<success_criteria>
UADM-02/UADM-03/UADM-04 are visible and provable in the UI, not just in the backend response
shape. Every UI-SPEC-locked copy string, badge variant, and container-query breakpoint is present.
Zero client-side grouping/filtering exists.
</success_criteria>

<output>
Create `.planning/phases/139-scalable-user-admin-projections/139-08-SUMMARY.md` when done
</output>
