# 129-01 — Reusable API-driven seed fixture (Wave 1)

**Requirement:** enables live UAT for 129-134; V-03/V-04. **Depends on:** nothing.
**Goal:** an idempotent script that populates `sheppert`(member 2)/`csubs-leader`(member 1) + supporting groups/anime/releases with the full scenario matrix via the real API, reusable as the Phase-134 fixture.

**Read first:** `129-RESEARCH.md §3` (verified endpoints, ports, auth, ordering), `frontend/scripts/collect-member-profile-evidence.mjs` (harness analog).

**Files**
- NEW `scripts/seed-member-profile-fixtures.mjs` (idempotent; node, no extra deps).
- NEW `scripts/README-seed.md` (how to run, env vars, token accounts).

**Steps**
1. Token helper: direct-grant against `http://192.168.235.196:18081/realms/team4s/.../token`, client `team4s-frontend`, `grant_type=password`. Accounts from env: `SEED_ADMIN_USER/PW` (csubs-leader/123 = Token A), `SEED_SHEPPERT_USER/PW` (Token B = sheppert/123, confirmed). Fail loudly if Token B auth fails (do NOT silently skip member-2 `/me/profile`).
2. Implement steps 1-10 of RESEARCH §3 in dependency order, each idempotent (check-then-create; rely on documented unique constraints; treat 409/duplicate as success).
3. Scenario coverage asserted in-script after seeding (GET the two public profiles, assert: 2 memberships one current/one historical, a multi-role membership, ≥10 confirmed distinct-anime projects, a same-anime-two-versions case, memorial on one member, a year-only active period, >1 project page).
4. Base URL + ports from env (`SEED_API_BASE` default `http://192.168.235.196:18092`), NEVER from `.env` literals.

**Verification**
- `node scripts/seed-member-profile-fixtures.mjs` runs green twice in a row (idempotency).
- Both reference profiles render every scenario fact (manual GET assertions in-script pass).

**Commit:** `feat(129-01): reusable API seed fixture for member-profile scenarios`

**Notes/deviations to surface:** Token B = sheppert/123 (confirmed). The degenerate year-only-null-date row (RESEARCH §3 SQL) is OUT of scope here — PMDA-01 fix (129-09) makes the year observable from the API-set period.

