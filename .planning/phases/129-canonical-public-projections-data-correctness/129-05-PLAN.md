# 129-05 — Public progress uses only publicly-permissible facts (Wave 3) — PMDA-06

**Depends on:** 129-03. **Goal:** badge progress/archivist counts derive only from confirmed+public facts.

**Files:** `member_profile_progress_repository.go`, `member_profile_contribution_badges_repository.go`.

**Steps**
1. Add `AND is_public_on_member_profile = true` to the "progress" family project count (`:36-38`).
2. Add public-visibility + approved-review filters to the archivist media count (analog: `loadLatestContributions` media joins).
3. Leave `total_points` untouched (already authoritative).

**Verification:** 129-02 progress/archivist tests GREEN (private/unapproved facts no longer inflate). Full repo suite green.

**Commit:** `fix(129-05): compute public badge progress from public facts only`

