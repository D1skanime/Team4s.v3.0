# 129-10 — Membership projection: dedupe, is_current, hist roles, all roles (Wave 4) — PMDA-02, PMDA-05, PMDA-10

**Depends on:** 129-03, 129-08 (role-pair shape). **Goal:** memberships are one card per group, mark current vs historical, and list ALL approved public roles from `hist_group_member_roles`; no internal codes.

**Files:** `member_profile_memberships_repository.go`, `backend/internal/models/member_profile.go`, `shared/contracts/openapi.yaml`, `frontend/src/types/profile.ts`, `frontend/src/components/profile/MembershipsSection.tsx` (+ test).

**Steps**
1. Backend: dedupe to one row per `fg.id` (`DISTINCT ON (fg.id)` deterministic order, or aggregate join/left dates); add `is_current` (`left_date IS NULL`); join `hist_group_member_roles` gated `visibility='public'` and aggregate ALL role pairs (CONTEXT D-04: all canonical hist role_codes, membership-visibility-gated, no new registry; exclude internal permission codes if any surface).
2. Contract+types: add `is_current` + role pairs to the membership shape; drop reliance on empty `app_member_roles` for the public path.
3. Frontend: render all roles (not `[0]`), show current/historical distinctly.

**Verification:** 129-02 membership tests GREEN (dedupe, is_current, all-roles, no-internal); `MembershipsSection` test updated + green.

**Commit:** `fix(129-10): project canonical memberships with all public roles`

