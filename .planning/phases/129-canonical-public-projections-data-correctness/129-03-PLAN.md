# 129-03 — Split member_profile_repository.go (Wave 2, behavior-preserving)

**Requirement:** S-02 (450-line limit). **Depends on:** 129-02 (tests exist as regression net).
**Goal:** split the 1810-line file into the RESEARCH §2 layout, ≤450 lines each, NO behavior change.

**Files:** create the 7 sibling files in RESEARCH §2; move functions verbatim (same package, same receiver). No signature/logic changes.

**Steps**
1. Move function clusters file-by-file; compile after each move (`go build ./...`).
2. Keep shared helpers in the base file; do not duplicate.
3. `member_profile_recent_repository.go`: keep loaders for now (own-profile consumer); their public removal happens in 129-06.

**Verification:** `go build ./...` green; `go test ./backend/internal/repository/...` outcome identical to pre-split (RED tests still RED, others green); `wc -l` on each new file ≤450.

**Commit:** `refactor(129-03): split member profile repository into domain files`

