# 129-11 — Full automated gate + seed-backed check (Wave 5)

**Depends on:** 129-04..129-10. **Goal:** the whole phase is green and the seeded reference profiles render correctly.

**Steps**
1. Backend: `go build ./...`; `go test ./...`; with `TEAM4S_PHASE129_TEST_DSN` set, ALL 129-02 contracts now GREEN.
2. Frontend: `npm run build` (authoritative via `docker compose build` per project rule); `npm test`.
3. File-size gate: every touched production file ≤450 lines.
4. Run `scripts/seed-member-profile-fixtures.mjs` against a fresh DB; GET both public profiles and confirm each PMDA fact renders (year-only period, current+historical memberships with all roles, correct project count with terminating load-more, structured roles, public-only badge progress, no recent_* leak, memorial state). Capture output as evidence under the phase dir.
5. NOTE: authoritative live sign-off is the bundled Phase-134 UAT (DECISIONS 2026-08-14) — this step is the phase-local correctness check, not the milestone UAT.

**Verification:** all suites green; evidence captured. **Commit:** `test(129-11): full Phase 129 correctness gate with seeded profiles`
