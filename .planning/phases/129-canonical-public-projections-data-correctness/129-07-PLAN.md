# 129-07 — Remove dead /members/:slug/contributions surface (Wave 3) — PMDA-03, PMDA-09, PMDA-10, PMPR-06

**Depends on:** 129-03. **Goal:** remove the frontend-dead, leaky contributions endpoint + components (CONTEXT D-11).

**Files:** `backend/cmd/server/main.go` (route `:557`), `anime_contributions_public_repository.go` (`GetPublicMemberContributionsByID` + branch-3 `fgmr.role`), handler `contributions_public_handler.go`/`project_member_public_handler`, `frontend/src/lib/api.ts` (`getMemberContributions`), `MemberRoleTimeline.tsx`, `MemberContributionFilters.tsx`, related tests.

**Steps**
1. Confirm at execute-time there is NO non-test consumer (grep imports). If a live consumer exists → FALL BACK: keep the endpoint but add `status='confirmed'` and restrict roles to approved public roles from `hist_group_member_roles`/`role_definitions` (no raw `fgmr.role`); coordinate any contract change with Phase 130.
2. Otherwise remove route, handler, repo method, api helper, components, and their tests.
3. Update OpenAPI to drop the removed path.

**Verification:** `go build`/`go test` green; `npm test` green; `page.test.tsx` still asserts no call. 129-02 assertion for this surface GREEN.

**Commit:** `chore(129-07): remove dead member-contributions endpoint and components`

