# 129-04 — Current-projects count/rows parity (Wave 3) — PMDA-08, PMDA-11

**Depends on:** 129-03. **Goal:** `countCurrentProjects` uses the exact predicate set of `loadCurrentProjects` so count == listed rows and load-more terminates.

**Files:** `member_profile_projects_repository.go`.

**Steps**
1. Reconcile the `anime_contribution_roles acr` divergence: either add a matching `EXISTS(SELECT 1 FROM anime_contribution_roles …)` to the count, or drop the INNER JOIN requirement from the list query — pick whichever matches the intended visible-set semantics (a confirmed+public project with no role rows: decide include/exclude and apply to BOTH). Document the choice in the commit.
2. Ensure `GetPublicMemberProjectsByID` paging uses the same pair.

**Verification:** 129-02 count-parity test GREEN; load-more returns empty only when truly exhausted. `go test ./backend/internal/repository/...`.

**Commit:** `fix(129-04): align current-projects count with visible rows`

