# 129-08 — Structured role code+label pairs (Wave 4) — PMDA-04

**Depends on:** 129-03, 129-04. **Goal:** profile projections deliver `{code,label_de}` role pairs from `role_definitions`; frontend renders label + styles by code (no label→code reverse map).

**Files:** `member_profile_projects_repository.go`, `member_profile_contributions_repository.go`, `backend/internal/models/member_profile.go`, `shared/contracts/openapi.yaml`, `frontend/src/types/profile.ts`, `frontend/src/components/profile/MemberCurrentProjectsSection.tsx` (+ its test).

**Steps**
1. Backend: replace label-only `ARRAY_AGG` with paired output (analog: `anime_contributions_public_repository.go:102-103`). Change `Roles []string` → `[]PublicMemberRole{Code,LabelDe}` on `PublicMemberCurrentProject`, `PublicMemberProjectReleaseVersion`, `PublicMemberPreviousContribution`.
2. Contract+types: add the role-pair shape.
3. Frontend: consume `code` for styling (`roleColorCode(code)`), `label_de` for text; DELETE `ROLE_CODE_BY_LABEL` and the reverse map.

**Verification:** 129-02 role-pair test GREEN; `MemberCurrentProjectsSection.test.tsx` updated + green; no remaining label→code derivation (grep).

**Commit:** `fix(129-08): deliver role code and label as structured pairs`

