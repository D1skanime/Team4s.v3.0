# 129-09 — Year-only active-period precision end-to-end (Wave 4) — PMDA-01

**Depends on:** 129-03. **Goal:** public profile preserves year-only precision distinctly from full dates.

**Files:** `member_profile_public_repository.go`, `backend/internal/models/member_profile.go`, `shared/contracts/openapi.yaml`, `frontend/src/types/profile.ts`, profile hero/membership render components (+ tests).

**Steps**
1. Backend: SELECT `active_from_year`/`active_until_year` in the public query; stop passing hard-nil to `profileActivityDateOrYear`. Emit either explicit `active_from_year`/`active_until_year` fields OR a precision flag alongside the date so year-only ≠ Jan-1 full date.
2. Contract+types: add the year/precision fields to `PublicMemberProfileData`.
3. Frontend: render "seit 2019"/"2015–2019" for year precision vs full date. Visual polish stays minimal (deep styling → Phase 133).

**Verification:** 129-02 year-only test GREEN; component test covers year vs full-date render; `go test`+`npm test` green.

**Commit:** `fix(129-09): preserve year-only active period precision`

