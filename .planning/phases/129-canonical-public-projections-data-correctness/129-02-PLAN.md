# 129-02 — RED PostgreSQL projection contracts (Wave 1)

**Requirements:** PMDA-01..11, PMPR-06 (failing assertions of correct projection). **Depends on:** nothing (self-seeding tests).
**Goal:** table-driven PostgreSQL tests that self-seed fixtures and assert the CORRECT projection for each defect in RESEARCH §1. They FAIL against current code (RED) and become the green gate for waves 3-4.

**Read first:** `129-RESEARCH.md §1`, `member_profile_repository_postgres_test.go` (DSN + fixture pattern).

**Files**
- EDIT/NEW `backend/internal/repository/member_profile_public_repository_postgres_test.go` (new DSN env `TEAM4S_PHASE129_TEST_DSN`; skip-if-unset like Phase 128).
- NEW `backend/internal/repository/member_profile_memberships_repository_test.go`.
- EDIT `member_profile_progress_repository` test, `member_profile_contribution_badges_repository_test.go`.

**RED assertions (must fail now)**
1. Year-only member → public DTO exposes the year period distinctly (PMDA-01).
2. Two hist memberships same group → ONE card; current (null left_date) and historical are distinct facts; public roles from `hist_group_member_roles` (PMDA-02/05); all approved roles present, no internal codes (PMDA-10/PMPR-06).
3. Confirmed+public project with zero `anime_contribution_roles` → count == listed rows (PMDA-08).
4. Roles delivered as `{code,label}` pairs, not label-only (PMDA-04).
5. Confirmed-but-private contribution does NOT inflate badge progress; unapproved media does NOT inflate archivist count (PMDA-06).
6. `recent_media`/`recent_contributions` absent from public DTO (PMDA-09) and no unfiltered media leak (PMDA-07/PMPR-06).
7. `GetPublicMemberContributionsByID` (if retained) requires `status='confirmed'` and emits no raw `fgmr.role` (PMDA-03/10) — OR assert the route is removed (see 129-07).

**Verification:** with `TEAM4S_PHASE129_TEST_DSN` set, `go test ./backend/internal/repository/ -run Phase129` shows the new tests RED; the rest of the suite stays green.

**Commit:** `test(129-02): RED PostgreSQL contracts for public projection correctness`

