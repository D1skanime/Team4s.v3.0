# 129-06 — Remove recent_* from public DTO; secure own-profile media (Wave 3) — PMDA-07, PMDA-09, PMPR-06

**Depends on:** 129-03. **Goal:** the public DTO no longer carries/computes `recent_media`/`recent_contributions`; any retained media loader is visibility/review-gated.

**Files:** `member_profile_public_repository.go`, `member_profile_recent_repository.go`, `backend/internal/models/member_profile.go`, `shared/contracts/openapi.yaml`, `frontend/src/types/profile.ts`.

**Steps**
1. Stop calling `loadRecentMedia`/`loadRecentContributions` in `GetPublicMemberProfileByID`; remove `RecentMedia`/`RecentContributions` from `PublicMemberProfile` (keep on own-profile DTO where `GetOwnProfile` still uses them).
2. If any own-profile media loader remains public-adjacent, add `visibilities`(public)+`review_statuses`(approved) joins.
3. Update OpenAPI + TS types to drop the public recent_* fields.

**Verification:** 129-02 "recent_* absent + no media leak" test GREEN; own-profile page still renders its recent data; `go test` + `npm test` green.

**Commit:** `fix(129-06): drop dead recent projections from public profile DTO`

