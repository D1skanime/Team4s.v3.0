---
phase: 49-zentraler-auth-api-client-und-token-lifecycle-h-rtung
plan: 02
type: execute
wave: 2
depends_on:
  - 49-01
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/lib/useAuthSession.ts
  - frontend/src/components/auth/AuthSessionSwitchGuard.tsx
  - frontend/src/lib/api.auth-refresh.test.ts
  - frontend/src/lib/api.session-switch.test.ts
  - frontend/src/components/auth/AuthSessionSwitchGuard.test.tsx
  - frontend/src/app/auth/page.test.tsx
autonomous: true
requirements:
  - AUTH-API-CLIENT-01
must_haves:
  truths:
    - "Protected normal API requests use the central client for proactive refresh, bearer attachment, and exactly one auth-related 401 retry."
    - "Concurrent protected requests and upload preflights share one runtime refresh operation."
    - "Pages/components consume token-free session booleans; raw token and expiry metadata stay private inside api.ts."
    - "Upload retry behavior is eligible only when auth failure is proven before persistence or the endpoint is proven idempotent."
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "Central auth/API lifecycle, private expiry metadata, authorized fetch, and central upload/XHR wrapper"
    - path: "frontend/src/lib/useAuthSession.ts"
      provides: "Token-free session snapshot hook for UI callers"
    - path: "frontend/src/lib/api.auth-refresh.test.ts"
      provides: "Proactive refresh, concurrency, 401 retry, and upload-wrapper lifecycle tests"
  key_links:
    - from: "frontend/src/lib/api.ts"
      to: "frontend/src/lib/keycloakAuth.ts"
      via: "refreshRuntimeSession and central lifecycle helper calls"
      pattern: "runtimeSessionRefreshPromise"
    - from: "frontend/src/lib/useAuthSession.ts"
      to: "frontend/src/lib/api.ts"
      via: "getAuthSessionSnapshot booleans only"
      pattern: "hasAccessToken"
    - from: "frontend/src/lib/api.ts"
      to: "49-02-SUMMARY.md or 49-VERIFICATION.md"
      via: "upload retry eligibility evidence"
      pattern: "uploadFansubMedia|uploadAdminAnimeMedia|uploadReleaseVersionMedia"
---

<objective>
Harden the central frontend auth/API client so normal protected requests, auth-state resync, and upload/XHR preflight behavior are owned by `frontend/src/lib/api.ts`.

Purpose: Plan 49-03 can only remove token ownership from pages/components safely after the central client proves proactive refresh, shared refresh coordination, exactly-one 401 retry, token-free snapshots, and upload retry eligibility rules.
Output: hardened central client, token-free session hook, focused auth lifecycle tests, and `49-02-SUMMARY.md`.
</objective>

<context>
@.planning/phases/49-zentraler-auth-api-client-und-token-lifecycle-h-rtung/49-auth-api-client-inventory.md
@.planning/phases/49-zentraler-auth-api-client-und-token-lifecycle-h-rtung/49-auth-api-client-boundaries.md
@.planning/phases/49-zentraler-auth-api-client-und-token-lifecycle-h-rtung/49-RESEARCH.md
@.planning/phases/49-zentraler-auth-api-client-und-token-lifecycle-h-rtung/49-PATTERNS.md
@frontend/src/lib/api.ts
@frontend/src/lib/useAuthSession.ts
@frontend/src/lib/keycloakAuth.ts
@frontend/src/components/auth/AuthSessionSwitchGuard.tsx
@frontend/src/lib/api.auth-refresh.test.ts
@frontend/src/lib/api.session-switch.test.ts
@frontend/src/components/auth/AuthSessionSwitchGuard.test.tsx
@frontend/src/app/auth/page.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture lifecycle characterization and upload-retry eligibility evidence</name>
  <files>frontend/src/lib/api.auth-refresh.test.ts, frontend/src/lib/api.session-switch.test.ts, frontend/src/components/auth/AuthSessionSwitchGuard.test.tsx, frontend/src/app/auth/page.test.tsx</files>
  <read_first>
    - frontend/src/lib/api.auth-refresh.test.ts
    - frontend/src/lib/api.session-switch.test.ts
    - frontend/src/components/auth/AuthSessionSwitchGuard.test.tsx
    - frontend/src/app/auth/page.test.tsx
    - backend upload handlers/repository paths identified by 49-auth-api-client-inventory.md
  </read_first>
  <action>
    Add or update only characterization tests that pass against the current implementation, and document the missing lifecycle expectations that Tasks 2 and 3 must implement and make green. Do not add intentionally failing assertions in this auto task. Inspect the backend persistence order for `uploadFansubMedia`, `uploadAdminAnimeMedia`, `uploadAdminReleaseThemeAsset`, `uploadAdminReleaseThemeAssetForRelease`, `uploadReleaseVersionMedia`, and fetch/FormData upload paths such as `uploadSegmentAsset` and `uploadOwnProfileAvatar`. Record retry eligibility evidence in `49-02-SUMMARY.md`: wrapper retry is allowed only when 401 is proven to occur before persistence or the endpoint is proven idempotent; otherwise later implementation and tests must require no automatic retry and a re-auth error.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run test -- api.auth-refresh.test.ts api.session-switch.test.ts components/auth/AuthSessionSwitchGuard.test.tsx app/auth/page.test.tsx</automated>
  </verify>
  <done>
    Existing lifecycle behavior is characterized by passing tests, missing lifecycle expectations are listed for Tasks 2 and 3, and the summary records endpoint-specific retry safety evidence before production code changes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement private expiry metadata and central request gating</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts
    - frontend/src/lib/keycloakAuth.ts
    - .planning/phases/49-zentraler-auth-api-client-und-token-lifecycle-h-rtung/49-auth-api-client-boundaries.md
  </read_first>
  <action>
    Add private expiry metadata handling inside `frontend/src/lib/api.ts`; do not expose raw token, parsed token claims, or expiry timestamps through `AuthSessionSnapshot`. Add and enable the proactive-refresh and central-request tests described by Task 1 while implementing them: proactive refresh near expiry, no refresh when token is comfortably valid, concurrent protected requests sharing one refresh, auth-related 401 refreshing and retrying exactly once, second 401 not looping, and refresh failure clearing runtime auth state. Implement an internal `ensureFreshRuntimeSession({ reason })` path used before protected normal requests and upload preflight. Reuse the existing `runtimeSessionRefreshPromise` for proactive refresh and 401 recovery. Preserve the central request shape: initial send, optional shared refresh, exactly one retry for auth-related 401, then return or surface the non-recoverable auth failure without another loop. Keep Keycloak as identity/session lifecycle only and leave backend permission ownership untouched.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run test -- api.auth-refresh.test.ts</automated>
  </verify>
  <done>
    `api.ts` owns proactive refresh decisions privately, normal protected requests pass through the central gatekeeper, concurrent refreshes share one promise, and no UI-facing snapshot exposes token or expiry data beyond booleans.
  </done>
</task>

<task type="auto">
  <name>Task 3: Make UI session snapshots token-free and add central upload wrapper</name>
  <files>frontend/src/lib/api.ts, frontend/src/lib/useAuthSession.ts, frontend/src/components/auth/AuthSessionSwitchGuard.tsx, frontend/src/lib/api.session-switch.test.ts, frontend/src/components/auth/AuthSessionSwitchGuard.test.tsx, frontend/src/app/auth/page.test.tsx</files>
  <read_first>
    - frontend/src/lib/useAuthSession.ts
    - frontend/src/components/auth/AuthSessionSwitchGuard.tsx
    - frontend/src/app/auth/page.tsx
    - frontend/src/lib/api.ts upload helper sections listed in 49-PATTERNS.md
  </read_first>
  <action>
    Update `useAuthSession` so pages/components receive token-free state such as `hasAccessToken`, `hasRefreshToken` if needed by UI, `displayName` if already available, and `isClientInitialized`. Keep expiry metadata private in `api.ts` per the resolved research decision. Add and enable session-switch resync and upload-wrapper tests described by Task 1 while implementing them. Add a central upload/XHR helper in `api.ts` that performs preflight refresh, attaches `Authorization` only inside the wrapper, preserves progress callbacks, coordinates refresh through `runtimeSessionRefreshPromise`, and applies the upload retry eligibility rule from Task 1. Existing upload helper signatures may continue compiling for Plan 49-03, but no new page/component token ownership may be introduced.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run test -- api.auth-refresh.test.ts api.session-switch.test.ts components/auth/AuthSessionSwitchGuard.test.tsx app/auth/page.test.tsx</automated>
  </verify>
  <done>
    UI session consumption is token-free, auth-state resync still works, and central upload/XHR auth lifecycle exists with retry disabled for endpoints that are not proven safe.
  </done>
</task>

</tasks>

<verification>
Automated:
1. `cd C:/Users/admin/Documents/Team4s/frontend && npm run test -- api.auth-refresh.test.ts api.session-switch.test.ts components/auth/AuthSessionSwitchGuard.test.tsx app/auth/page.test.tsx`
2. `cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck`
3. `cd C:/Users/admin/Documents/Team4s/frontend && npm run lint` if not blocked by unrelated existing lint failures
4. `cd C:/Users/admin/Documents/Team4s && git diff --check -- frontend/src/lib/api.ts frontend/src/lib/useAuthSession.ts frontend/src/components/auth/AuthSessionSwitchGuard.tsx frontend/src/lib/api.auth-refresh.test.ts frontend/src/lib/api.session-switch.test.ts frontend/src/components/auth/AuthSessionSwitchGuard.test.tsx frontend/src/app/auth/page.test.tsx`
</verification>

<success_criteria>
- Protected normal API requests refresh proactively when needed and do not refresh unnecessarily.
- Parallel protected requests and upload preflights share exactly one refresh operation.
- Auth-related 401 responses cause at most one refresh plus one retry.
- Refresh failure clears/broadcasts auth state consistently.
- `AuthSessionSnapshot` and `useAuthSession` remain token-free and do not expose expiry metadata.
- Upload retry happens only for endpoints proven auth-before-persistence or idempotent; unsafe endpoints surface a re-auth error without automatic retry.
- Streaming routes remain untouched.
</success_criteria>
