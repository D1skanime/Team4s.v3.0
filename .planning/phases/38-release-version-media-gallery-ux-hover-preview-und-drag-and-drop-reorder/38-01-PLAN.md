---
phase: 38-release-version-media-gallery-ux-hover-preview-und-drag-and-drop-reorder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseVersionMedia.ts
  - frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaGallery.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaDetailPanel.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.test.tsx
  - frontend/src/lib/api.ts
  - frontend/src/types/releaseVersionMedia.ts
autonomous: true
requirements:
  - RVM-FRONTEND-01
must_haves:
  truths:
    - "Phase 38 ersetzt das sichtbare sort_order-Zahlenfeld durch category-scoped Reorder-UX und fuehrt keine zweite konkurrierende Sortier-Seam ein."
    - "Persistiertes Reorder muss den bestehenden Backend-Contract POST /media/reorder mit items[{id, sort_order}] treffen; der stale ordered_ids-Frontend-Typ darf nicht still weiterleben."
    - "Drag-and-Drop darf keine Medien zwischen Kategorien verschieben; Reihenfolge aendert sich nur innerhalb der bereits gesetzten category."
---

<objective>
Implement the reorder foundation for release-version media: remove the legacy numeric sort field from the detail panel, fix live in-memory resort behavior, and add category-scoped drag-and-drop that persists through the existing reorder backend seam.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-release-version-media-gallery-ux-hover-preview-und-drag-and-drop-reorder/38-CONTEXT.md
@.planning/phases/38-release-version-media-gallery-ux-hover-preview-und-drag-and-drop-reorder/38-RESEARCH.md
@frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseVersionMedia.ts
@frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaGallery.tsx
@frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaDetailPanel.tsx
@frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.tsx
@frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.test.tsx
@frontend/src/lib/api.ts
@frontend/src/types/releaseVersionMedia.ts
@backend/internal/handlers/admin_content_release_version_media.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Correct the reorder contract and make sort-affecting state update live immediately</name>
  <read_first>
    - frontend/src/types/releaseVersionMedia.ts
    - frontend/src/lib/api.ts
    - frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseVersionMedia.ts
    - backend/internal/handlers/admin_content_release_version_media.go
  </read_first>
  <action>
Repair the stale frontend reorder seam before any drag UI is added. The backend reorder endpoint already exists and expects `items[]` with `{id, sort_order}` pairs, while the frontend type still advertises `ordered_ids`. Bring the type and API helper back in line with the real backend contract, then extend the release-version media hook with the smallest project-owned reorder helper needed for Phase 38.

At the same time, close the live re-sort bug in local state: whenever a patch changes `sort_order`, or when a persisted reorder succeeds, the in-memory `items` array must immediately reflect the new category-local order without forcing a full reload.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npx vitest run "src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.test.tsx" && npx tsc --noEmit</automated>
  </verify>
  <done>
    - `ReleaseVersionMediaReorderRequest` no longer uses the stale `ordered_ids` shape.
    - `frontend/src/lib/api.ts` posts the backend-compatible reorder payload shape.
    - `useReleaseVersionMedia.ts` exposes a reorder-capable seam and keeps `items` visibly sorted after sort-affecting updates.
    - `npx vitest run ...ReleaseVersionMediaSection.test.tsx` and `npx tsc --noEmit` exit 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the legacy sort field with category-scoped drag-and-drop reorder</name>
  <read_first>
    - frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaGallery.tsx
    - frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaDetailPanel.tsx
    - frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.tsx
    - frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.test.tsx
    - .planning/phases/38-release-version-media-gallery-ux-hover-preview-und-drag-and-drop-reorder/38-CONTEXT.md
  </read_first>
  <action>
Replace the visible numeric sort-order editing flow with a direct gallery interaction. Remove the sort-order input and save button from the detail panel, then implement category-scoped drag-and-drop in the gallery grid so operators can reorder media cards directly where they see them.

Keep the scope intentionally narrow:
- prefer a project-owned seam first (native drag/drop plus explicit reorder persistence) instead of adding a new dependency unless there is a concrete blocker
- only reorder within the current category section
- keep existing card click-to-edit behavior intact
- provide enough drag feedback that the operator can tell which card is being moved and where it will land
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npx vitest run "src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.test.tsx" && npx tsc --noEmit</automated>
  </verify>
  <done>
    - `ReleaseVersionMediaDetailPanel.tsx` no longer exposes the legacy sort-order form controls.
    - Gallery cards can be reordered within a category and the new order persists through the backend seam.
    - Cross-category drag is blocked in the UI rather than silently attempting a category move.
    - Regression tests cover at least one local live-reorder case and one persisted reorder payload case.
    - `npx vitest run ...ReleaseVersionMediaSection.test.tsx` and `npx tsc --noEmit` exit 0.
  </done>
</task>

</tasks>

<verification>
Automated:
1. `cd C:/Users/admin/Documents/Team4s/frontend && npx vitest run "src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.test.tsx"`
2. `cd C:/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit`

Live:
1. Open `/admin/episode-versions/41/edit?tab=media`
2. Confirm the detail panel no longer shows the old sort-order number field
3. Drag one screenshot card within its category and drop it to a new position
4. Confirm the gallery reorders immediately without manual reload
5. Reload the page and confirm the new order persisted
6. Confirm dragging into another category is blocked or ignored rather than changing category
</verification>

<success_criteria>
- The numeric sort-order editor is gone from the visible UX.
- Operators can reorder media directly in the gallery within a category.
- Local and persisted order stay aligned immediately after the change.
</success_criteria>

<output>
Create `.planning/phases/38-release-version-media-gallery-ux-hover-preview-und-drag-and-drop-reorder/38-01-SUMMARY.md`.
</output>
