---
phase: 24-release-segmente-op-ed-timing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/0049_extend_theme_segments.up.sql
  - database/migrations/0049_extend_theme_segments.down.sql
  - backend/internal/models/admin_anime_themes.go
  - backend/internal/repository/admin_content_anime_themes.go
  - backend/internal/handlers/admin_content_anime_theme_segments.go
  - backend/cmd/server/admin_routes.go
autonomous: true
requirements:
  - P24-SC2
  - P24-SC4

must_haves:
  truths:
    - "Migration laeuft sauber auf einem frischen Schema durch (up und down)"
    - "GET /api/v1/admin/anime/:animeId/segments?group_id=X&version=v1 gibt 200 mit Array zurueck"
    - "POST /api/v1/admin/anime/:animeId/segments legt ein Segment an und gibt 201 zurueck"
    - "PATCH /api/v1/admin/anime/:animeId/segments/:segmentId aktualisiert ein Segment"
    - "DELETE /api/v1/admin/anime/:animeId/segments/:segmentId gibt 204 zurueck"
    - "Playback-Query WHERE fansub_group_id=X AND version=Y AND episode BETWEEN start AND end liefert korrekte Segmente"
  artifacts:
    - path: "database/migrations/0049_extend_theme_segments.up.sql"
      provides: "schema migration fuer theme_segments mit neuen Spalten und Constraints"
    - path: "database/migrations/0049_extend_theme_segments.down.sql"
      provides: "reversibler Rollback der Migration"
    - path: "backend/internal/models/admin_anime_themes.go"
      provides: "AdminThemeSegment neues Model, AdminThemeSegmentCreateInput, AdminThemeSegmentPatchInput"
    - path: "backend/internal/repository/admin_content_anime_themes.go"
      provides: "ListAnimeSegments, CreateAnimeSegment, UpdateAnimeSegment, DeleteAnimeSegment"
    - path: "backend/internal/handlers/admin_content_anime_theme_segments.go"
      provides: "ListAnimeSegments, CreateAnimeSegment, UpdateAnimeSegment, DeleteAnimeSegment handler"
    - path: "backend/cmd/server/admin_routes.go"
      provides: "neue Segment-Routen registriert, alte Segment-Routen entfernt"
  key_links:
    - from: "backend/internal/handlers/admin_content_anime_theme_segments.go"
      to: "backend/internal/repository/admin_content_anime_themes.go"
      via: "h.themeRepo.ListAnimeSegments / CreateAnimeSegment / UpdateAnimeSegment / DeleteAnimeSegment"
      pattern: "themeRepo\\.(List|Create|Update|Delete)AnimeSegment"
    - from: "backend/cmd/server/admin_routes.go"
      to: "backend/internal/handlers/admin_content_anime_theme_segments.go"
      via: "v1.GET/POST/PATCH/DELETE /admin/anime/:animeId/segments"
      pattern: "adminContentHandler\\.(List|Create|Update|Delete)AnimeSegment"
---

<objective>
Migration 0049 erweitert die theme_segments-Tabelle um Release-Kontext (fansub_group_id, version, plain-integer Episodenbereich, Zeitbereich, Jellyfin-Quelle) und entfernt die alten FK-Spalten. Das Backend erhaelt vier neue CRUD-Endpunkte auf der anime-level URL, die alten FK-basierten Segment-Handler werden vollstaendig ersetzt.

Purpose: Legt das Datenfundament fuer die Segmente-Tab UI und den Playback-Query-Seam.
Output: Lauffaehige Migration + 4 Backend-Endpunkte + neues Go-Model.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-release-segmente-op-ed-timing/24-CONTEXT.md

@database/migrations/0048_seed_theme_types.up.sql
@backend/internal/models/admin_anime_themes.go
@backend/internal/repository/admin_content_anime_themes.go
@backend/internal/handlers/admin_content_anime_theme_segments.go
@backend/cmd/server/admin_routes.go

<interfaces>
<!-- Bestehendes Model -- wird in diesem Plan ERSETZT -->
<!-- Aus backend/internal/models/admin_anime_themes.go: -->
```go
// AdminAnimeThemeSegment -- ALTES MODEL (hat start_episode_id, end_episode_id FKs)
// Wird durch neues AdminThemeSegment ersetzt.

type AdminThemeType struct {
  ID   int64  `json:"id"`
  Name string `json:"name"`
}

type AdminAnimeTheme struct {
  ID            int64     `json:"id"`
  AnimeID       int64     `json:"anime_id"`
  ThemeTypeID   int64     `json:"theme_type_id"`
  ThemeTypeName string    `json:"theme_type_name"`
  Title         *string   `json:"title"`
  CreatedAt     time.Time `json:"created_at"`
}
```

<!-- Bestehende Repo-Methoden die BEHALTEN werden (Themes, nicht Segments): -->
<!-- ListThemeTypes, ListAdminAnimeThemes, CreateAdminAnimeTheme, UpdateAdminAnimeTheme, DeleteAdminAnimeTheme -->
<!-- Die alten Methoden ListAdminAnimeThemeSegments, CreateAdminAnimeThemeSegment, DeleteAdminAnimeThemeSegment werden ERSETZT -->

<!-- Bestehende Routen die ENTFERNT werden: -->
<!-- GET  /admin/anime/:id/themes/:themeId/segments -->
<!-- POST /admin/anime/:id/themes/:themeId/segments -->
<!-- DELETE /admin/anime/:id/themes/:themeId/segments/:segmentId -->

<!-- Neue Routen die HINZUKOMMEN: -->
<!-- GET    /admin/anime/:animeId/segments?group_id=X&version=v1 -->
<!-- POST   /admin/anime/:animeId/segments -->
<!-- PATCH  /admin/anime/:animeId/segments/:segmentId -->
<!-- DELETE /admin/anime/:animeId/segments/:segmentId -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0049 — theme_segments erweitern</name>
  <files>
    database/migrations/0049_extend_theme_segments.up.sql
    database/migrations/0049_extend_theme_segments.down.sql
  </files>
  <action>
Erstelle `database/migrations/0049_extend_theme_segments.up.sql` mit folgendem Inhalt (exakt so, keine Abweichungen):

```sql
-- Phase 24: theme_segments um Release-Kontext erweitern.
-- start_episode_id / end_episode_id (FK auf episodes) werden durch plain integers ersetzt.
-- Neue Spalten: fansub_group_id, version, start_episode, end_episode, start_time, end_time, source_jellyfin_item_id.

ALTER TABLE theme_segments
  ADD COLUMN fansub_group_id bigint REFERENCES fansub_groups(id) ON DELETE CASCADE,
  ADD COLUMN version varchar(20) NOT NULL DEFAULT 'v1',
  ADD COLUMN start_episode integer,
  ADD COLUMN end_episode integer,
  ADD COLUMN start_time interval,
  ADD COLUMN end_time interval,
  ADD COLUMN source_jellyfin_item_id text;

-- Alte FK-Spalten entfernen (waren nie produktiv befullt).
ALTER TABLE theme_segments
  DROP COLUMN IF EXISTS start_episode_id,
  DROP COLUMN IF EXISTS end_episode_id;

-- Constraints fuer Bereichsgueltigkeit.
ALTER TABLE theme_segments
  ADD CONSTRAINT chk_episode_range CHECK (end_episode IS NULL OR start_episode IS NULL OR end_episode >= start_episode),
  ADD CONSTRAINT chk_time_range CHECK (
    end_time IS NULL OR start_time IS NULL OR end_time > start_time
  );

-- Indizes fuer Playback-Query (anime_id, fansub_group_id, version, episode range).
CREATE INDEX idx_theme_segment_group ON theme_segments(fansub_group_id);
CREATE INDEX idx_theme_segment_ep_range ON theme_segments(fansub_group_id, version, start_episode, end_episode);
```

Erstelle `database/migrations/0049_extend_theme_segments.down.sql`:

```sql
DROP INDEX IF EXISTS idx_theme_segment_ep_range;
DROP INDEX IF EXISTS idx_theme_segment_group;

ALTER TABLE theme_segments
  DROP CONSTRAINT IF EXISTS chk_time_range,
  DROP CONSTRAINT IF EXISTS chk_episode_range;

ALTER TABLE theme_segments
  ADD COLUMN IF NOT EXISTS start_episode_id bigint REFERENCES episodes(id) ON DELETE SET NULL,
  ADD COLUMN IF NOT EXISTS end_episode_id bigint REFERENCES episodes(id) ON DELETE SET NULL;

ALTER TABLE theme_segments
  DROP COLUMN IF EXISTS source_jellyfin_item_id,
  DROP COLUMN IF EXISTS end_time,
  DROP COLUMN IF EXISTS start_time,
  DROP COLUMN IF EXISTS end_episode,
  DROP COLUMN IF EXISTS start_episode,
  DROP COLUMN IF EXISTS version,
  DROP COLUMN IF EXISTS fansub_group_id;
```
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s && ls database/migrations/0049_extend_theme_segments.up.sql database/migrations/0049_extend_theme_segments.down.sql</automated>
  </verify>
  <done>Beide Migrationsdateien existieren. up.sql enthaelt ADD COLUMN fansub_group_id, version, start_episode, end_episode, start_time, end_time, source_jellyfin_item_id sowie DROP COLUMN start_episode_id / end_episode_id und beide Constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Go-Model und Repository — neue Segment-CRUD-Methoden</name>
  <files>
    backend/internal/models/admin_anime_themes.go
    backend/internal/repository/admin_content_anime_themes.go
  </files>
  <action>
**In `backend/internal/models/admin_anime_themes.go`:**

Ersetze die alten Segment-Typen (`AdminAnimeThemeSegment`, `AdminAnimeThemeSegmentCreateInput`) durch neue Typen. Die Theme-Typen (`AdminThemeType`, `AdminAnimeTheme`, `AdminAnimeThemeCreateInput`, `AdminAnimeThemePatchInput`) bleiben unveraendert.

Neue Typen hinzufuegen:

```go
// AdminThemeSegment repraesentiert ein Release-Segment (OP/ED-Timing) fuer eine Fansub-Gruppe und Version.
type AdminThemeSegment struct {
  ID                   int64     `json:"id"`
  ThemeID              int64     `json:"theme_id"`
  AnimeID              int64     `json:"anime_id"`
  ThemeTitle           *string   `json:"theme_title"`
  ThemeTypeName        string    `json:"theme_type_name"`
  FansubGroupID        *int64    `json:"fansub_group_id"`
  Version              string    `json:"version"`
  StartEpisode         *int      `json:"start_episode"`
  EndEpisode           *int      `json:"end_episode"`
  StartTime            *string   `json:"start_time"`   // Interval als HH:MM:SS-String
  EndTime              *string   `json:"end_time"`     // Interval als HH:MM:SS-String
  SourceJellyfinItemID *string   `json:"source_jellyfin_item_id"`
  CreatedAt            time.Time `json:"created_at"`
}

// AdminThemeSegmentCreateInput enthaelt die Felder zum Anlegen eines neuen Segments.
type AdminThemeSegmentCreateInput struct {
  ThemeID              int64   `json:"theme_id"`
  FansubGroupID        *int64  `json:"fansub_group_id"`
  Version              string  `json:"version"`
  StartEpisode         *int    `json:"start_episode"`
  EndEpisode           *int    `json:"end_episode"`
  StartTime            *string `json:"start_time"`
  EndTime              *string `json:"end_time"`
  SourceJellyfinItemID *string `json:"source_jellyfin_item_id"`
}

// AdminThemeSegmentPatchInput enthaelt die optionalen Felder fuer ein Segment-Update.
type AdminThemeSegmentPatchInput struct {
  ThemeID              *int64  `json:"theme_id"`
  FansubGroupID        *int64  `json:"fansub_group_id"`
  Version              *string `json:"version"`
  StartEpisode         *int    `json:"start_episode"`
  EndEpisode           *int    `json:"end_episode"`
  StartTime            *string `json:"start_time"`
  EndTime              *string `json:"end_time"`
  SourceJellyfinItemID *string `json:"source_jellyfin_item_id"`
}
```

**In `backend/internal/repository/admin_content_anime_themes.go`:**

Ersetze die drei alten Methoden `ListAdminAnimeThemeSegments`, `CreateAdminAnimeThemeSegment`, `DeleteAdminAnimeThemeSegment` durch vier neue Methoden:

```go
// ListAnimeSegments gibt alle Segmente eines Anime gefiltert nach group_id und version zurueck.
// groupID=0 und version="" bedeutet: kein Filter (alle Segmente des Anime).
func (r *AdminContentRepository) ListAnimeSegments(ctx context.Context, animeID int64, groupID int64, version string) ([]models.AdminThemeSegment, error) {
  // Pruefe ob Anime existiert
  // JOIN: theme_segments ts JOIN themes t ON t.id = ts.theme_id JOIN theme_types tt ON tt.id = t.theme_type_id
  // WHERE t.anime_id = $animeID
  // Optionaler Filter: AND ts.fansub_group_id = $groupID (wenn groupID > 0)
  // Optionaler Filter: AND ts.version = $version (wenn version != "")
  // ORDER BY ts.id
  // Scan: id, theme_id, t.anime_id, t.title (theme_title), tt.name (theme_type_name),
  //       fansub_group_id, version, start_episode, end_episode,
  //       start_time::text, end_time::text, source_jellyfin_item_id, ts.created_at
  // Intervall-Spalten mit ::text casten fuer HH:MM:SS-Darstellung
}

// CreateAnimeSegment legt ein neues Segment an.
// Prueft ob das Theme zum animeID gehoert.
func (r *AdminContentRepository) CreateAnimeSegment(ctx context.Context, animeID int64, input models.AdminThemeSegmentCreateInput) (*models.AdminThemeSegment, error) {
  // Pruefe ob Theme existiert und zu animeID gehoert: SELECT anime_id FROM themes WHERE id = $themeID
  // INSERT INTO theme_segments (theme_id, fansub_group_id, version, start_episode, end_episode, start_time, end_time, source_jellyfin_item_id)
  // VALUES ($1, $2, $3, $4, $5, $6::interval, $7::interval, $8)
  // RETURNING id, created_at
  // Dann ListAnimeSegments aufrufen oder direkten SELECT um vollstaendigen Datensatz zurueckzugeben
  // Bei pgErr.Code == "23503" -> ErrConflict (FK-Verletzung fansub_groups)
  // Bei pgErr.Code == "23514" -> ErrConflict (CHECK-Constraint verletzt: episode_range oder time_range)
}

// UpdateAnimeSegment aktualisiert ein Segment (partieller Patch).
func (r *AdminContentRepository) UpdateAnimeSegment(ctx context.Context, segmentID int64, input models.AdminThemeSegmentPatchInput) error {
  // Dynamischer SET-Builder wie in UpdateAdminAnimeTheme
  // Unterstuetzte Felder: theme_id, fansub_group_id, version, start_episode, end_episode,
  //                       start_time (als ::interval cast), end_time (als ::interval cast),
  //                       source_jellyfin_item_id
  // WHERE id = $N
  // RowsAffected() == 0 -> ErrNotFound
  // pgErr.Code == "23514" -> ErrConflict (Constraint verletzt)
}

// DeleteAnimeSegment loescht ein Segment anhand seiner ID.
func (r *AdminContentRepository) DeleteAnimeSegment(ctx context.Context, segmentID int64) error {
  // DELETE FROM theme_segments WHERE id = $1
  // RowsAffected() == 0 -> ErrNotFound
}
```

Wichtig: Die Interval-Spalten (start_time, end_time) als `start_time::text` und `end_time::text` im SELECT scannen, damit Postgres das Interval als "HH:MM:SS"-String liefert statt als Go-Duration.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go build ./internal/models/... && go build ./internal/repository/...</automated>
  </verify>
  <done>go build erfolgreich fuer models und repository. Neue Typen AdminThemeSegment, AdminThemeSegmentCreateInput, AdminThemeSegmentPatchInput existieren. Vier neue Repo-Methoden: ListAnimeSegments, CreateAnimeSegment, UpdateAnimeSegment, DeleteAnimeSegment.</done>
</task>

<task type="auto">
  <name>Task 3: Handler ersetzen und Routen neu registrieren</name>
  <files>
    backend/internal/handlers/admin_content_anime_theme_segments.go
    backend/cmd/server/admin_routes.go
  </files>
  <action>
**`backend/internal/handlers/admin_content_anime_theme_segments.go` komplett ersetzen:**

Die Datei implementiert vier neue Handler. Alle alten FK-basierten Handler (ListAnimeThemeSegments, CreateAnimeThemeSegment, DeleteAnimeThemeSegment) werden entfernt.

```go
package handlers

// ListAnimeSegments verarbeitet GET /api/v1/admin/anime/:animeId/segments
// Query-Parameter: group_id (optional, int64), version (optional, string, default "")
// Gibt alle theme_segments des Anime gefiltert nach group_id+version zurueck.
func (h *AdminContentHandler) ListAnimeSegments(c *gin.Context) { ... }

// CreateAnimeSegment verarbeitet POST /api/v1/admin/anime/:animeId/segments
// Body: { theme_id, fansub_group_id?, version, start_episode?, end_episode?,
//         start_time?, end_time?, source_jellyfin_item_id? }
func (h *AdminContentHandler) CreateAnimeSegment(c *gin.Context) { ... }

// UpdateAnimeSegment verarbeitet PATCH /api/v1/admin/anime/:animeId/segments/:segmentId
// Body: alle Felder optional (partieller Patch)
func (h *AdminContentHandler) UpdateAnimeSegment(c *gin.Context) { ... }

// DeleteAnimeSegment verarbeitet DELETE /api/v1/admin/anime/:animeId/segments/:segmentId
func (h *AdminContentHandler) DeleteAnimeSegment(c *gin.Context) { ... }
```

Request-Structs definieren:
- `adminAnimeSegmentCreateRequest` mit allen Feldern als JSON-Tags
- `adminAnimeSegmentPatchRequest` mit allen Feldern als Pointer + JSON-Tags

Fehlerbehandlung:
- `repository.ErrNotFound` → 404
- `repository.ErrConflict` → 409 mit `code: "invalid_theme_or_group"` oder `code: "constraint_violation"`
- Sonstige Fehler → `writeInternalErrorResponse`

**In `backend/cmd/server/admin_routes.go`:**

Alte Zeilen entfernen:
```
v1.GET("/admin/anime/:id/themes/:themeId/segments", ...)
v1.POST("/admin/anime/:id/themes/:themeId/segments", ...)
v1.DELETE("/admin/anime/:id/themes/:themeId/segments/:segmentId", ...)
```

Neue Zeilen am Ende des Theme-Blocks hinzufuegen (nach den bestehenden Theme-Routen):
```go
v1.GET("/admin/anime/:id/segments", auth, deps.adminContentHandler.ListAnimeSegments)
v1.POST("/admin/anime/:id/segments", auth, deps.adminContentHandler.CreateAnimeSegment)
v1.PATCH("/admin/anime/:id/segments/:segmentId", auth, deps.adminContentHandler.UpdateAnimeSegment)
v1.DELETE("/admin/anime/:id/segments/:segmentId", auth, deps.adminContentHandler.DeleteAnimeSegment)
```

Hinweis: Die URL nutzt `:id` (nicht `:animeId`) weil admin_routes.go dieses Pattern durchgehend fuer Anime-Routen verwendet.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go build ./...</automated>
  </verify>
  <done>go build ./... erfolgreich. Vier neue Handler registriert. Alte themeId/segments-Routen aus admin_routes.go entfernt. Neue /admin/anime/:id/segments Routen eingetragen.</done>
</task>

</tasks>

<verification>
Nach Abschluss aller Tasks:

1. Migration syntaktisch korrekt:
   `grep -c "ADD COLUMN" database/migrations/0049_extend_theme_segments.up.sql` → mindestens 7
   `grep -c "DROP COLUMN IF EXISTS" database/migrations/0049_extend_theme_segments.down.sql` → mindestens 7

2. Build sauber:
   `cd backend && go build ./...` → kein Fehler

3. Neue Routen registriert:
   `grep "segments" backend/cmd/server/admin_routes.go` → zeigt 4 neue Zeilen, keine alten themeId-Routen mehr

4. Alte Methoden nicht mehr referenziert:
   `grep -r "ListAdminAnimeThemeSegments\|CreateAdminAnimeThemeSegment\|DeleteAdminAnimeThemeSegment" backend/` → keine Treffer
</verification>

<success_criteria>
- Migration 0049 up/down existieren und sind syntaktisch korrekt
- `go build ./...` laeuft ohne Fehler durch
- Vier neue Handler: ListAnimeSegments, CreateAnimeSegment, UpdateAnimeSegment, DeleteAnimeSegment
- Vier neue Routen: GET/POST/PATCH/DELETE /api/v1/admin/anime/:id/segments[/:segmentId]
- Alte FK-basierte Segment-Methoden und -Routen vollstaendig entfernt
- Neues AdminThemeSegment-Model mit fansub_group_id, version, plain-integer episode range, interval times
</success_criteria>

<output>
Nach Abschluss `.planning/phases/24-release-segmente-op-ed-timing/24-01-SUMMARY.md` erstellen.
</output>
