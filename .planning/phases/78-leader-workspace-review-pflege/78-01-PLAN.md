---
phase: 78-leader-workspace-review-pflege
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/ContributionsReviewSection.test.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/GroupMediaReviewSection.test.tsx
  - backend/internal/handlers/fansub_media_review_handler_test.go
autonomous: true
requirements: [F, H, I, G, K]
must_haves:
  truths:
    - "D-01: Tests prüfen getrennte Domänen-Flächen (Claims-Tab vs. Contribution-Review-Seam), kein gebündelter Review-Tab."
    - "D-07: Test deckt den „Nur offene anzeigen\"-Filter-Toggle-Default (offen zuerst) in ContributionsReviewSection ab."
    - "D-08: Test verifiziert Capability-Gating (ContributionsReviewSection rendert null ohne can_manage_members; GroupMediaReviewSection ohne can_edit_group)."
    - "D-09: Backend-Test verifiziert Audit-Eintrag (auditLogRepo.Write) bei erfolgreicher Medien-Review-Mutation und Deny-Audit bei fehlender Berechtigung."
    - "Lock H: ContributionsReviewSection-Test zeigt ausschließlich Contribution-Proposals, niemals Claims."
  artifacts:
    - path: "frontend/src/app/admin/fansubs/[id]/edit/ContributionsReviewSection.test.tsx"
      provides: "RED-Tests SC1/SC4 für Contribution-Review (Gating, offen-Filter, Trennung von Claims)"
      min_lines: 40
    - path: "frontend/src/app/admin/fansubs/[id]/edit/GroupMediaReviewSection.test.tsx"
      provides: "RED-Tests SC3 für Gruppenmedien-Review (Gating, Sichtbarkeit/Status-Selektoren)"
      min_lines: 30
    - path: "backend/internal/handlers/fansub_media_review_handler_test.go"
      provides: "RED-Tests SC3/D-09 für Medien-Review-Handler (Permission-Deny, Audit-Pflicht, Enum-Validierung)"
      min_lines: 50
  key_links:
    - from: "ContributionsReviewSection.test.tsx"
      to: "@/lib/api (listGroupProposals, confirmProposal, rejectProposal)"
      via: "vi.mock('@/lib/api')"
      pattern: "vi\\.mock\\(.@/lib/api"
    - from: "fansub_media_review_handler_test.go"
      to: "auditLogWriter Stub"
      via: "Audit-Assertion nach Mutation"
      pattern: "auditLog|Write"
---

<objective>
Wave-0-Testgerüst (RED) für Phase 78: alle in 78-VALIDATION.md als MISSING markierten automatisierten Tests anlegen, damit die Implementierungspläne (78-02/03/04) gegen vorgegebene Verhaltensverträge laufen (TDD-RED).

Purpose: Nyquist-Compliance herstellen — jede SC mit fehlender Testdatei (SC1/SC3/SC4, D-09) erhält ein scheiterndes Testgerüst, bevor Produktionscode entsteht. Das verhindert „grün ohne Beweis".
Output: Drei neue Testdateien, die zunächst rot sind (Komponenten/Handler existieren noch nicht).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-leader-workspace-review-pflege/78-CONTEXT.md
@.planning/phases/78-leader-workspace-review-pflege/78-VALIDATION.md
@.planning/phases/78-leader-workspace-review-pflege/78-UI-SPEC.md
@.planning/phases/78-leader-workspace-review-pflege/78-PATTERNS.md

<interfaces>
<!-- Aus dem Codebase extrahiert — Executor nutzt diese direkt, keine erneute Exploration. -->

Frontend api.ts-Helfer (lib/api.ts), die ContributionsReviewSection konsumieren wird:
- listGroupProposals(fansubId: number, authToken?): Promise<GroupProposalsResponse>  // GET /api/v1/admin/fansubs/{id}/contribution-proposals
- confirmProposal(fansubId: number, proposalId: number, authToken?): Promise<...>
- rejectProposal(fansubId: number, proposalId: number, reviewNote?: string, authToken?): Promise<...>

Typ GroupProposalRow (@/types/contributions): { id, member_display_name, anime_title, role_codes: string[], note?, created_at }

FansubGroupCapabilities (@/types/fansub.ts:163) hat u.a.: can_manage_members, can_edit_group, can_create_invitation.

Backend-Handler-Analog (contribution_review_handler.go):
- Interface ReviewRepository, reviewPermissionChecker (CanForFansubGroup), auditLogWriter
- NewContributionReviewHandler(reviewRepo, permissionSvc, auditLogRepo)
- Permission-Muster: permissionActorFromContext(c) → parseFansubID → CanForFansubGroup → bei Deny auditPermissionDenied + writePermissionDenied
- Audit-Muster: h.auditLogRepo.Write(ctx, repository.AuditLogEntry{ActorAppUserID, EventType, ScopeType, ScopeID, TargetType, TargetID, Action, Outcome})

Test-Analoge:
- Frontend: frontend/src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.test.tsx
- Backend: backend/internal/handlers/contribution_review_handler_test.go
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frontend Wave-0-RED-Tests für ContributionsReviewSection und GroupMediaReviewSection</name>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.test.tsx (Test-Setup-Analog: `// @vitest-environment jsdom`, vi.mock('@/lib/api'), @testing-library/react render/screen/fireEvent/waitFor, afterEach cleanup+clearAllMocks)
    - frontend/src/components/contributions/ReviewQueue.tsx (Datenpipeline + GroupProposalRow-Felder, die der Test stubben muss)
    - frontend/src/types/fansub.ts (FansubGroupCapabilities-Felder can_manage_members, can_edit_group)
    - frontend/src/types/contributions.ts (GroupProposalRow-Shape)
    - .planning/phases/78-leader-workspace-review-pflege/78-UI-SPEC.md (Copywriting-Contract: exakte Strings „Offene Vorschläge ({n})", „Nur offene anzeigen", „In Prüfung", „Medien prüfen", „Sichtbarkeit", „Prüfstatus")
  </read_first>
  <action>
    Erstelle ContributionsReviewSection.test.tsx mit `// @vitest-environment jsdom`, vi.mock('@/lib/api', () => ({ listGroupProposals, confirmProposal, rejectProposal als vi.fn() })). Importiere die noch nicht existierende `ContributionsReviewSection` aus './ContributionsReviewSection' (Import-Fehler = erwartetes RED). Tests: (1) Gating — bei capabilities ohne can_manage_members rendert die Komponente nichts (container leer) [D-08]; (2) zeigt Sektions-Titel „Offene Vorschläge" und gemockte Proposal-Zeilen mit member_display_name + anime_title [SC1]; (3) „Nur offene anzeigen"-Toggle ist standardmäßig aktiv und filtert auf offene Proposals [D-07]; (4) confirmProposal wird mit fansubId aufgerufen, niemals eine Claim-API [Lock H / SC1]; (5) nur Proposals der übergebenen fansubId werden geladen (listGroupProposals mit fansubId aufgerufen) [SC4 / D-04].
    Erstelle GroupMediaReviewSection.test.tsx analog: importiere die noch nicht existierende `GroupMediaReviewSection` aus './GroupMediaReviewSection'. Tests: (1) Gating ohne can_edit_group → null-Render [D-08]; (2) bei vorhandenen Medien rendert Sichtbarkeit-Select (Label „Sichtbarkeit") und Prüfstatus-Select (Label „Prüfstatus") [D-05]; (3) Owner-Korrektheit wird nur als Badge „Owner-Zuordnung prüfen" angezeigt, kein Owner-Edit-Feld [D-05]. Mocke den künftigen api.ts-Review-Helfer als vi.fn().
    Verwende exakte deutsche Strings mit korrekten Umlauten aus dem Copywriting-Contract der UI-SPEC. Keine Produktionskomponenten erstellen — nur Tests.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/admin/fansubs/[id]/edit/ContributionsReviewSection.test.tsx src/app/admin/fansubs/[id]/edit/GroupMediaReviewSection.test.tsx 2>&1 | grep -Eiv '^#' | grep -Ei "fail|cannot find module|error" </automated>
  </verify>
  <acceptance_criteria>
    - Beide Testdateien existieren und schlagen fehl mit Modul-/Import-Fehler oder fehlgeschlagenen Assertions (RED-Beweis), weil ContributionsReviewSection.tsx und GroupMediaReviewSection.tsx noch nicht existieren.
    - ContributionsReviewSection.test.tsx enthält mindestens je einen Test für Gating (D-08), Trennung-von-Claims (SC1/Lock H), offen-Filter-Default (D-07) und fansubId-Scope (SC4).
    - Alle user-facing Test-Assertion-Strings nutzen korrekte Umlaute (keine ae/oe/ue/ss).
  </acceptance_criteria>
  <done>Beide Frontend-RED-Tests vorhanden und rot; Assertions referenzieren die exakten UI-SPEC-Strings.</done>
</task>

<task type="auto">
  <name>Task 2: Backend Wave-0-RED-Test für fansub_media_review_handler (SC3, D-09 Audit)</name>
  <read_first>
    - backend/internal/handlers/contribution_review_handler_test.go (Stub-Repo + Stub-Permission-Checker über Interfaces, auditLogWriter-Stub, Audit-Assertion bei erfolgreicher Mutation)
    - backend/internal/handlers/contribution_review_handler.go:18-49 (Interface-Muster ReviewRepository/reviewPermissionChecker/auditLogWriter + Konstruktor)
    - backend/internal/handlers/fansub_media_upload.go:17-37 (Permission-Muster CanForFansubGroup + auditPermissionDenied bei Deny)
    - backend/internal/handlers/admin_content_release_version_media.go:595-606 (rawBody-Parse + category-Ablehnung als Enum-Validierungs-Analog)
  </read_first>
  <action>
    Erstelle fansub_media_review_handler_test.go im package handlers. Definiere Stub-Implementierungen für ein künftiges Handler-Interface (Repo mit einer UpdateFansubMediaReview-Methode, reviewPermissionChecker, auditLogWriter) analog contribution_review_handler_test.go. Schreibe Tests gegen die noch nicht existierende `NewFansubMediaReviewHandler`/`PatchFansubMediaReview` (Compile-Fehler = erwartetes RED): (1) bei Deny-Result (CanForFansubGroup → Allowed=false) wird HTTP 403 zurückgegeben UND ein Deny-Audit-Eintrag geschrieben (EventType endend auf „.denied") [D-09]; (2) bei erfolgreicher Mutation wird ein Audit-Eintrag mit EventType „fansub_group_media.visibility_updated", ActorAppUserID gesetzt, ScopeType group, TargetType „fansub_group_media" geschrieben [D-09]; (3) ungültiger Sichtbarkeits-/Reviewstatus-Enum-Wert im Body → HTTP 400 ohne Mutation [V5 Input Validation / SC3]; (4) Owner-Mismatch (Medium gehört fremder Gruppe) → kein Update [Tampering-Mitigation / SC3].
    Verwende permissions.ActionFansubGroupEdit als erwartete Permission-Action. Keine Produktions-Handler-Datei erstellen.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/handlers/... -run TestFansubMediaReview 2>&1 | grep -Eiv '^#' | grep -Ei "fail|undefined|cannot|error"</automated>
  </verify>
  <acceptance_criteria>
    - fansub_media_review_handler_test.go existiert und schlägt mit Compile-Fehler („undefined: NewFansubMediaReviewHandler" o.ä.) oder fehlgeschlagenen Assertions fehl (RED-Beweis).
    - Test enthält explizite Audit-Assertion: bei Erfolg EventType „fansub_group_media.visibility_updated"; bei Deny ein „.denied"-EventType.
    - Test enthält Enum-Validierungs-Fall (ungültiger Status → 400, keine Mutation).
    - Go-Response-Strings im Test mit korrekten Umlauten (sofern user-facing).
  </acceptance_criteria>
  <done>Backend-RED-Test vorhanden, kompiliert nicht / schlägt fehl; Audit- und Enum-Validierungs-Verhalten sind als Assertions kodiert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Admin-API | Untrusted Leader-Eingaben (Proposal-Confirm/Reject, Medien-Status) queren hier; Capability-Gating im Frontend ist nicht vertrauenswürdig. |
| Testgerüst → Produktionsvertrag | Tests definieren den erwarteten Sicherheits-Vertrag (Deny→403+Audit), den 78-02/03/04 erfüllen müssen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-01 | Elevation of Privilege | Capability-Gating der Review-Komponenten | mitigate | Frontend-Test verifiziert null-Render ohne Capability; serverseitige Durchsetzung wird in 78-03 getestet (dieser Plan kodiert den Deny→403-Vertrag). |
| T-78-02 | Repudiation | Medien-Review-Mutation | mitigate | Backend-Test erzwingt Audit-Assertion (auditLogRepo.Write) bei jeder Mutation und bei Deny — verhindert späteren Audit-Bypass (D-09). |
| T-78-03 | Tampering | Owner-Mismatch beim Medien-Patch | mitigate | Test deckt Cross-Group-Medium-Fall ab (kein Update); zwingt 78-03 zur fansubID-Scope-Prüfung. |
| T-78-SC | Tampering | npm/go installs | accept | Keine neuen Paketinstalls in diesem Plan (nur Testdateien gegen vorhandene Frameworks Vitest 3 / go test). |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/app/admin/fansubs/[id]/edit/ContributionsReviewSection.test.tsx src/app/admin/fansubs/[id]/edit/GroupMediaReviewSection.test.tsx` → rot (erwartet, RED).
- `cd backend && go test ./internal/handlers/... -run TestFansubMediaReview` → rot/Compile-Fehler (erwartet, RED).
- Bestehende Suites bleiben unberührt: `cd frontend && npx vitest run` zeigt keine neuen Regressions außerhalb der neuen RED-Dateien.
</verification>

<success_criteria>
- Drei neue Testdateien existieren und sind rot (TDD-RED), weil die Produktionskomponenten/-Handler noch fehlen.
- Tests kodieren D-07 (offen-Filter-Default), D-08 (Gating), D-09 (Audit), SC1/SC3/SC4 und Lock H (Claim/Contribution-Trennung) als überprüfbare Assertions.
- Alle Test-Strings mit korrekten deutschen Umlauten.
</success_criteria>

<output>
Create `.planning/phases/78-leader-workspace-review-pflege/78-01-SUMMARY.md` when done
</output>
