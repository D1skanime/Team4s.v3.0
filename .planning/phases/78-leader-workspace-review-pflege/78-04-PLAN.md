---
phase: 78-leader-workspace-review-pflege
plan: 04
type: execute
wave: 3
depends_on: [78-02, 78-03]
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/GroupMediaReviewSection.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/GroupMediaReviewSection.module.css
  - frontend/src/app/admin/fansubs/[id]/edit/UserSuggestionsInbox.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
autonomous: false
requirements: [F, G, H, I, K]
must_haves:
  truths:
    - "D-01: Neue Flächen docken an bestehende Domänen-Tabs an (media/vorschlaege/notes); kein neuer Review-Tab, MAIN_TABS unverändert in Struktur."
    - "D-03: Phase-76-Vorschlags-Stubs werden typgerecht im zuständigen Domänen-Tab platziert (Medien→media, Story→notes, Contribution→vorschlaege), kein generischer Sammel-Eingang."
    - "D-04: Phase-76-Stub ist strikt auf die geöffnete Gruppe gescoped und capability-gated; keine erfundene Phase-76-API."
    - "D-05: GroupMediaReviewSection setzt nur Sichtbarkeit/Reviewstatus über patchFansubMediaReview; Owner-Korrektheit wird nur als Badge geflaggt, nicht umgehängt."
    - "D-06: Gruppenmedien-Prüfung lebt an der Owner-Fläche (media-Tab), nicht in einem zentralen Medienprüf-Tab."
    - "D-08: Alle neuen Flächen erhalten capabilities und rendern null ohne Berechtigung (canUseMainTab-Stil)."
    - "Lock F: Verdrahtung nur in /admin/fansubs/[id]/edit; keine Duplizierung in /admin/my-groups/[id]."
  artifacts:
    - path: "frontend/src/app/admin/fansubs/[id]/edit/GroupMediaReviewSection.tsx"
      provides: "Sichtbarkeit/Reviewstatus-Selektoren für fansub_group_media + Owner-Flag (D-05/D-06)"
      max_lines: 450
    - path: "frontend/src/app/admin/fansubs/[id]/edit/UserSuggestionsInbox.tsx"
      provides: "Capability-gegateter, gruppen-gescopeter Phase-76-Vorschlags-EmptyState-Stub (D-03/D-04)"
      max_lines: 200
    - path: "frontend/src/app/admin/fansubs/[id]/edit/page.tsx"
      provides: "Import + bedingtes Render der neuen Flächen in media/vorschlaege/notes-Tabs (keine neue Logik)"
  key_links:
    - from: "GroupMediaReviewSection.tsx"
      to: "@/lib/api (patchFansubMediaReview)"
      via: "Reuse 78-03-Helfer"
      pattern: "patchFansubMediaReview"
    - from: "page.tsx"
      to: "ContributionsReviewSection / GroupMediaReviewSection / UserSuggestionsInbox"
      via: "bedingtes Tab-Render mit capabilities-Prop"
      pattern: "ContributionsReviewSection|GroupMediaReviewSection|UserSuggestionsInbox"
---

<objective>
Die Frontend-Verdrahtung der Review-/Pflege-Flächen im kanonischen Leader-Workspace abschließen: Gruppenmedien-Prüfung (D-05/D-06) als eigene Komponente im `media`-Tab, capability-gegatete Phase-76-Vorschlags-Stubs (D-03/D-04) in den zuständigen Domänen-Tabs, und page.tsx chirurgisch an die neuen Komponenten anschließen (nur Import + bedingtes Render).

Purpose: Schließt SC3 (Medienprüfung an Owner-Fläche), SC4 (Phase-76-Vorschläge im Gruppenkontext) und integriert die in 78-02 gebaute ContributionsReviewSection in den vorschlaege-Tab. Hängt von api.ts-Helfern (78-03) und den Review-Komponenten/page.tsx-Render-Stellen (78-02) ab.
Output: GroupMediaReviewSection (+CSS), UserSuggestionsInbox-Stub, page.tsx-Verdrahtung; Human-Verify-Checkpoint inkl. SC5-Negativnachweis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-leader-workspace-review-pflege/78-CONTEXT.md
@.planning/phases/78-leader-workspace-review-pflege/78-UI-SPEC.md
@.planning/phases/78-leader-workspace-review-pflege/78-PATTERNS.md
@.planning/phases/78-leader-workspace-review-pflege/78-02-SUMMARY.md
@.planning/phases/78-leader-workspace-review-pflege/78-03-SUMMARY.md

<interfaces>
<!-- Aus Codebase extrahiert. -->
page.tsx-Render-Stellen (bedingtes Tab-Render):
  - Z.3491: {activeMainTab === "claims" ? <ClaimManagementPanel groupId={fansubID} /> : null}
  - Z.3492: {activeMainTab === "vorschlaege" ? <ReviewQueue fansubId={fansubID} /> : null}  // hier ContributionsReviewSection einsetzen
  - Z.3483-3488: notes-Tab rendert NotesTab + GroupHistorySection
  - Z.2772: media-Tab rendert MediaUpload-Grid (rechte Spalte)
page.tsx-Gating: canUseMainTab (Z.216-250); capabilities: FansubGroupCapabilities | null im Scope der Seite.
  - media-Gate: can_edit_group; vorschlaege-Gate: can_manage_members; notes-Gate: can_edit_notes.

api.ts (aus 78-03):
  - patchFansubMediaReview(fansubId, mediaId, { visibility?, review_status? }, authToken?)

@/components/ui: Select, FormField, Button, Card, Badge, Table, EmptyState, SectionHeader, Toolbar, LoadingState, ErrorState.
GroupMediaReviewSection.test.tsx (aus 78-01) definiert den Verhaltensvertrag.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GroupMediaReviewSection.tsx (+CSS) — Sichtbarkeit/Reviewstatus + Owner-Flag</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/GroupMediaReviewSection.tsx, frontend/src/app/admin/fansubs/[id]/edit/GroupMediaReviewSection.module.css</files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/GroupMediaReviewSection.test.tsx (RED-Vertrag aus 78-01 — muss grün werden)
    - frontend/src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.tsx (Struktur/Gating/Primitivnutzung)
    - frontend/src/app/admin/fansubs/[id]/edit/AnimeContributionModal.tsx (Select/FormField-Nutzungsmuster)
    - frontend/src/lib/api.ts (patchFansubMediaReview-Signatur aus 78-03; ggf. List-Helfer für Gruppenmedien)
    - .planning/phases/78-leader-workspace-review-pflege/78-UI-SPEC.md (Pattern 3, Copywriting Medienprüfung — exakte Strings „Medien prüfen", „Sichtbarkeit", „Prüfstatus", „Owner-Zuordnung prüfen", „Änderungen speichern")
  </read_first>
  <behavior>
    - Test: ohne capabilities.can_edit_group → null-Render (D-08).
    - Test: rendert pro Medium eine Karte mit Select „Sichtbarkeit" (Intern/Öffentlich) und Select „Prüfstatus" (In Prüfung/Freigegeben/Abgelehnt/Archiviert/Entfernt) [D-05].
    - Test: „Änderungen speichern" ruft patchFansubMediaReview(fansubId, mediaId, { visibility, review_status }).
    - Test: erkannte Owner-Inkonsistenz zeigt Badge „Owner-Zuordnung prüfen" — KEIN Owner-Edit-Feld (D-05).
  </behavior>
  <action>
    Erstelle GroupMediaReviewSection.tsx mit Props { fansubId: number; capabilities: FansubGroupCapabilities }. Gate `if (!capabilities.can_edit_group) return null` (D-08). Lade die Gruppenmedien (vorhandener/zu nutzender List-Helfer; falls keiner existiert, nutze den aus 78-03 etablierten Endpoint/Helfer). Pro Medium: Card variant="section" mit FormField „Sichtbarkeit" → Select (Optionen Intern/Öffentlich) und FormField „Prüfstatus" → Select (Optionen In Prüfung/Freigegeben/Abgelehnt/Archiviert/Entfernt) — deutsche Labels fix, value-Attribute an reale DB-Enum-Werte (78-03-Befund) gebunden. Bei erkannter Owner-Inkonsistenz Badge variant="warning" „Owner-Zuordnung prüfen" anzeigen (nur Hinweis, kein Umhängen — D-05). Speichern-Button (variant="primary", Label „Änderungen speichern") ruft patchFansubMediaReview; bei Erfolg Toast „Prüfstatus aktualisiert."; bei Fehler ErrorState/Inline-Fehler „Änderungen konnten nicht gespeichert werden.". LoadingState/EmptyState mit exakten Copywriting-Strings. Nur @/components/ui-Primitives (kein natives select/input/button). Colocated GroupMediaReviewSection.module.css mit mediaReviewGrid (auto-fill minmax(280px,1fr)) gemäß UI-SPEC Layout-Regeln. Datei ≤450 Zeilen; korrekte Umlaute.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/admin/fansubs/[id]/edit/GroupMediaReviewSection.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - GroupMediaReviewSection.test.tsx (aus 78-01) ist grün.
    - `grep -v '^#' GroupMediaReviewSection.tsx` enthält keine nativen `<select`/`<input`/`<button`/`<textarea`-Tags; Import aus `@/components/ui`.
    - `grep "patchFansubMediaReview" GroupMediaReviewSection.tsx` matcht; kein nacktes `fetch(`.
    - Kein Owner-Edit-Feld (Source: nur Badge „Owner-Zuordnung prüfen", kein owner_type/owner_id-Setter).
    - Datei ≤450 Zeilen; Strings mit Umlauten.
  </acceptance_criteria>
  <done>GroupMediaReviewSection grün getestet, gegated, GDS-konform, schreibt nur Sichtbarkeit/Reviewstatus.</done>
</task>

<task type="auto">
  <name>Task 2: UserSuggestionsInbox.tsx — capability-gegateter, gruppen-gescopeter Phase-76-Stub</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/UserSuggestionsInbox.tsx</files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.tsx (EmptyState-Nutzung als Analog)
    - .planning/phases/78-leader-workspace-review-pflege/78-UI-SPEC.md (Pattern 4 + Copywriting Phase-76-Stub: Titel „Noch keine Nutzer-Vorschläge", Beschreibung „Nutzer-Vorschläge erscheinen hier, sobald Phase 76 implementiert ist.")
    - .planning/phases/78-leader-workspace-review-pflege/78-RESEARCH.md (Befund 5 — Phase 76 NICHT implementiert; kein erfundener Endpoint)
  </read_first>
  <action>
    Erstelle UserSuggestionsInbox.tsx mit Props { fansubId: number; domain: 'media' | 'notes' | 'contribution'; capabilities: FansubGroupCapabilities }. Capability-Gate je Domäne (media→can_edit_group, notes→can_edit_notes, contribution→can_manage_members) — null wenn fehlend (D-08). Da Phase 76 nicht implementiert ist: rendere ausschließlich EmptyState variant="compact" mit Titel „Noch keine Nutzer-Vorschläge" und Beschreibung „Nutzer-Vorschläge erscheinen hier, sobald Phase 76 implementiert ist." (D-03 typgerechtes Routing per domain-Prop; D-04 fansubId-Scope als Prop durchgereicht). KEIN Fetch, KEIN erfundener Phase-76-Endpoint. Setze einen TODO-Kommentar mit Verweis: „// Phase 76 Follow-Up: echte Vorschlags-Liste pro domain+fansubId, gescoped (D-03/D-04)". Nur @/components/ui-Primitives; ≤200 Zeilen; korrekte Umlaute.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -Eiv '^#' | grep -Ei "UserSuggestionsInbox|error" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - UserSuggestionsInbox.tsx existiert, exportiert eine Komponente mit domain-Prop ('media'|'notes'|'contribution') und capabilities-Gate.
    - `grep -E "fetch\\(|/api/v1.*suggestion|phase76|phase-76" UserSuggestionsInbox.tsx` liefert nichts (kein erfundener Endpoint).
    - EmptyState-Strings exakt „Noch keine Nutzer-Vorschläge" mit Umlauten.
    - `cd frontend && npx tsc --noEmit` ohne neue Fehler.
  </acceptance_criteria>
  <done>Phase-76-Stub vorhanden: gegated, gruppen-gescoped, typgerecht per domain — ohne erfundene API.</done>
</task>

<task type="auto">
  <name>Task 3: page.tsx chirurgisch verdrahten — neue Flächen in media/vorschlaege/notes</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/page.tsx</files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx:95-110 (Import-Block), :2770-2820 (media-Tab-Render rechte Spalte), :3483-3493 (notes/claims/vorschlaege-Render)
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx:216-268 (canUseMainTab/visibleMainTabs/resolveMainTabForAccess — MAIN_TABS NICHT erweitern)
    - .planning/phases/78-leader-workspace-review-pflege/78-CONTEXT.md (D-01: Tabs erweitern, kein neuer Tab; Claude's Discretion Readiness-Sprungmarken)
  </read_first>
  <action>
    Reine Verdrahtung — KEINE neue Logik in page.tsx, KEINE MAIN_TABS-Erweiterung (D-01). Ergänze Imports für ContributionsReviewSection, GroupMediaReviewSection, UserSuggestionsInbox.
    (a) vorschlaege-Tab (Z.3492): ersetze `<ReviewQueue fansubId={fansubID} />` durch `<ContributionsReviewSection fansubId={fansubID} capabilities={capabilities} />` und ergänze darunter `<UserSuggestionsInbox fansubId={fansubID} domain="contribution" capabilities={capabilities} />`.
    (b) media-Tab: ergänze in der media-Render-Region `<GroupMediaReviewSection fansubId={fansubID} capabilities={capabilities} />` unterhalb des MediaUpload-Grids und darunter `<UserSuggestionsInbox fansubId={fansubID} domain="media" capabilities={capabilities} />`.
    (c) notes-Tab (Z.3483-3488): ergänze `<UserSuggestionsInbox fansubId={fansubID} domain="notes" capabilities={capabilities} />` am Ende der notes-Sektion.
    Die Komponenten gaten sich selbst über capabilities (null-Render), sodass page.tsx keine zusätzlichen Bedingungen braucht. Übergib das im Seiten-Scope vorhandene `capabilities`-Objekt. Falls Phase-77-Readiness-Sprungmarken-Deep-Links gewünscht sind: NUR das bestehende `?tab=`-Muster nutzen, nicht duplizieren (Claude's Discretion — optional, kein Pflichtteil). page.tsx wächst um < ~10 Zeilen reine Render-Verdrahtung.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx vitest run 2>&1 | grep -Eiv '^#' | grep -Ei "fail|error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "ContributionsReviewSection|GroupMediaReviewSection|UserSuggestionsInbox" page.tsx` matcht alle drei.
    - `<ReviewQueue` wird im vorschlaege-Tab nicht mehr gerendert (ersetzt).
    - MAIN_TABS-Array unverändert (kein neuer Tab-Key) — `grep -c "key:" page.tsx` im MAIN_TABS-Block unverändert gegenüber Ausgangsstand.
    - `cd frontend && npx tsc --noEmit` ohne Fehler; volle Vitest-Suite grün.
  </acceptance_criteria>
  <done>page.tsx rendert die drei neuen Flächen in den korrekten Domänen-Tabs; keine neue Logik, kein neuer Tab.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-Verify — Review-/Pflege-Flächen live + SC5-Negativnachweis</name>
  <action>
    Verifiziere die Phase-78-Review-/Pflege-Flaechen live im Browser (Dev-Server :3000) gemaess der Pruefschritte unten; bestaetige Gating, offen-Filter, Medien-Sichtbarkeit/Reviewstatus-Speichern, Owner-Flag-Anzeige, Phase-76-Stubs und den SC5-Negativnachweis (keine Duplizierung in /admin/my-groups). Pausiere fuer die Entwickler-Freigabe.
  </action>
  <what-built>
    Im Leader-Workspace /admin/fansubs/[id]/edit sind jetzt aktionierbar: Contribution-Review im vorschlaege-Tab (offen-Filter), Claim-Filter im claims-Tab, Gruppenmedien-Prüfung (Sichtbarkeit/Reviewstatus) im media-Tab, und Phase-76-Vorschlags-Stubs (EmptyState) in media/notes/vorschlaege. Alle gegated, alle auf bestehenden Seams, ohne neuen Tab.
  </what-built>
  <how-to-verify>
    1. Dev-Server :3000 (NICHT Docker :3002 — Memory testing_live_dev_server). Mit Leader-Account /admin/fansubs/<gruppe>/edit öffnen.
    2. vorschlaege-Tab: ContributionsReviewSection sichtbar, „Nur offene anzeigen"-Toggle aktiv (offen zuerst); Bestätigen/Ablehnen funktioniert; keine Claims sichtbar (Lock H). Darunter Phase-76-Stub „Noch keine Nutzer-Vorschläge".
    3. claims-Tab: „Nur offene anzeigen"-Toggle vorhanden und wirksam.
    4. media-Tab: GroupMediaReviewSection mit Sichtbarkeit-/Prüfstatus-Selektoren; „Änderungen speichern" → Toast „Prüfstatus aktualisiert."; ggf. „Owner-Zuordnung prüfen"-Badge (kein Owner-Edit-Feld). Darunter Phase-76-Stub.
    5. notes-Tab: Phase-76-Stub am Ende sichtbar.
    6. SC5-Negativnachweis (Lock F): /admin/my-groups/<gruppe> öffnen → es erscheinen KEINE Claim-/Contribution-/Medien-Review-Aktionen dort.
    7. SC1/D-08 serverseitig (optional, falls Nicht-Leader-Account verfügbar): Review-/Media-PATCH-Endpoint mit Nicht-Leader → 403 erwartet.
    8. Umlaute in allen sichtbaren Strings korrekt.
  </how-to-verify>
  <resume-signal>Tippe „approved" oder beschreibe Abweichungen (Gating, Toggle, Toast, Owner-Flag, my-groups-Duplikat).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Admin-API | Leader-Medien-/Review-Aktionen queren hier; Frontend-Gating ist UX, serverseitige Durchsetzung (78-03) ist maßgeblich. |
| Kanonischer Workspace ↔ /admin/my-groups | Review-/Pflege-Logik darf nur im kanonischen Workspace existieren (Lock F/SC5). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-12 | Elevation of Privilege | Neue Flächen ohne Gating | mitigate | Jede Komponente gated `if (!capabilities.<feld>) return null`; serverseitige Durchsetzung in 78-03; Human-Verify prüft Gating live (D-08/SC5). |
| T-78-13 | Information Disclosure | Phase-76-Stub könnte fremde Gruppen-Vorschläge zeigen | mitigate | Stub rendert nur EmptyState, kein Fetch; fansubId+domain als Scope-Props vorbereitet (D-04); echte Daten erst nach Phase 76. |
| T-78-14 | Tampering | Owner-Umhängen über Media-UI | mitigate | UI bietet nur Sichtbarkeit/Reviewstatus-Selektoren + Owner-Flag-Badge; kein Owner-Edit-Feld (D-05); Backend erzwingt es zusätzlich. |
| T-78-15 | Elevation of Privilege | Duplizierte Review-Logik in /admin/my-groups (Lock F) | mitigate | Verdrahtung ausschließlich in /admin/fansubs/[id]/edit; SC5-Negativnachweis im Human-Verify. |
| T-78-SC | Tampering | npm installs | accept | Keine neuen Pakete; nur vorhandene @/components/ui + lucide-react. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/app/admin/fansubs/[id]/edit/GroupMediaReviewSection.test.tsx` → grün.
- `cd frontend && npx tsc --noEmit && npx vitest run` → keine Regressions.
- `cd frontend && npx eslint src/app/admin/fansubs/[id]/edit/GroupMediaReviewSection.tsx src/app/admin/fansubs/[id]/edit/UserSuggestionsInbox.tsx` → keine native-Element-Warnungen.
- Human-Verify (Task 4) bestanden inkl. SC5-Negativnachweis.
</verification>

<success_criteria>
- GroupMediaReviewSection und UserSuggestionsInbox existieren, sind gegated (D-08), GDS-konform und an den korrekten Owner-/Domänen-Flächen verdrahtet (D-01/D-03/D-06).
- page.tsx rendert die drei Flächen ohne neue Logik und ohne neuen Tab; ContributionsReviewSection ersetzt ReviewQueue im vorschlaege-Tab.
- Gruppenmedien-Prüfung schreibt nur Sichtbarkeit/Reviewstatus (D-05); Phase-76-Stub ohne erfundene API.
- Human-Verify bestätigt SC5 (keine Duplizierung in /admin/my-groups).
</success_criteria>

<output>
Create `.planning/phases/78-leader-workspace-review-pflege/78-04-SUMMARY.md` when done
</output>
