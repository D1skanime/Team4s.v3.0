---
phase: 78-leader-workspace-review-pflege
plan: 03
type: execute
wave: 2
depends_on: [78-01]
files_modified:
  - shared/contracts/admin-content.yaml
  - backend/internal/handlers/fansub_media_review_handler.go
  - backend/internal/handlers/admin_content_release_version_media.go
  - backend/internal/repository/media_repository.go
  - backend/cmd/server/main.go
  - backend/internal/routes/admin_routes.go
  - frontend/src/lib/api.ts
autonomous: false
requirements: [G, I, K, F]
must_haves:
  truths:
    - "D-05: Aktionsumfang = Sichtbarkeit + Reviewstatus setzen; Owner-Korrektheit wird nur geflaggt zurückgegeben, NIE umgehängt (kein Owner-Typ-Wechsel)."
    - "D-06: Gruppenmedien schreiben in fansub_group_media, Release-Version-Medien in release_version_media — jede Mutation in die korrekte Owner-Tabelle (Lock G)."
    - "D-08: Jeder Endpoint prüft serverseitig CanForFansubGroup/CanForReleaseVersionMedia; Gruppenmitgliedschaft allein genügt nicht."
    - "D-09: Jede erfolgreiche Mutation schreibt einen Audit-Eintrag mit Akteur-Attribution; Deny erzeugt .denied-Audit (Lock I/K)."
    - "Lock K: Neue/erweiterte Endpoints folgen der Contract-Sequenz admin-content.yaml → DTO → Repo → api.ts; kein ad-hoc-Fetch."
  artifacts:
    - path: "backend/internal/handlers/fansub_media_review_handler.go"
      provides: "PATCH-Handler für fansub_group_media Sichtbarkeit/Reviewstatus mit Permission + Audit + Enum-Validierung"
      max_lines: 450
    - path: "frontend/src/lib/api.ts"
      provides: "patchFansubMediaReview + erweiterter patchReleaseVersionMediaItem (visibility/review_status)"
    - path: "shared/contracts/admin-content.yaml"
      provides: "Contract-Einträge für die neuen/erweiterten Media-Review-Felder"
  key_links:
    - from: "fansub_media_review_handler.go"
      to: "repository.AuditLogRepository.Write"
      via: "Audit nach Mutation"
      pattern: "auditLogRepo\\.Write|EventType:\\s*.fansub_group_media"
    - from: "fansub_media_review_handler.go"
      to: "permissionSvc.CanForFansubGroup(ActionFansubGroupEdit)"
      via: "serverseitige Autorisierung"
      pattern: "CanForFansubGroup"
    - from: "frontend/src/lib/api.ts"
      to: "PATCH /api/v1/admin/fansubs/:id/media/:mediaId"
      via: "authorizedFetch"
      pattern: "patchFansubMediaReview"
---

<objective>
Die Backend-/Contract-/API-Schicht für die Medienprüfung (D-05/D-06): einen neuen PATCH-Endpoint für Sichtbarkeit/Reviewstatus auf `fansub_group_media` schaffen und `PatchReleaseVersionMedia` additiv um visibility/review_status erweitern — vertragskonform (Lock K), in die korrekten Owner-Tabellen (Lock G), serverseitig autorisiert und vollständig auditiert (D-09/Lock I).

Purpose: SC3 (Medienprüfung schreibt korrekte Owner-Tabelle), liefert die api.ts-Helfer, die 78-04 (UI) konsumiert. Berührt NICHT page.tsx oder Review-Komponenten → konfliktfrei parallel zu 78-02.
Output: Neuer Handler + Repo-Methode + Route-Registrierung + Contract-Einträge + zwei api.ts-Helfer.

WICHTIG — Schema-Abhängigkeit (Phase 72): Die genauen Spalten für Sichtbarkeit/Reviewstatus (`media_assets.status`, `visibilities`-Lookup, ggf. Felder auf `fansub_group_media`/`release_version_media`) sind eine [ASSUMED]-Annahme (RESEARCH A1), bis Phase 72 ausgeführt ist. Task 0 verifiziert das Live-Schema VOR jeder irreversiblen Implementierung (blocking checkpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-leader-workspace-review-pflege/78-CONTEXT.md
@.planning/phases/78-leader-workspace-review-pflege/78-RESEARCH.md
@.planning/phases/78-leader-workspace-review-pflege/78-PATTERNS.md
@.planning/milestones/v1.2-DISCUSSION.md

<interfaces>
<!-- Aus Codebase extrahiert. -->
Permission-Muster (fansub_media_upload.go:17-37):
  identity, actor, ok := permissionActorFromContext(c)
  fansubID, err := parseFansubID(c.Param("id"))
  result, err := h.permissionSvc.CanForFansubGroup(ctx, actor, permissions.ActionFansubGroupEdit, fansubID)
  if !result.Allowed { auditPermissionDenied(...); writePermissionDenied(c, result); return }

Audit-Muster (contribution_review_handler.go:142-151):
  h.auditLogRepo.Write(ctx, repository.AuditLogEntry{ActorAppUserID, EventType, ScopeType: permissions.ScopeTypeGroup, ScopeID:&fansubID, TargetType, TargetID, Action, Outcome:"allowed"})

Bestehender PatchReleaseVersionMedia (admin_content_release_version_media.go:549-700):
  - rawBody map[string]interface{} (Z.595); category-Änderung wird abgelehnt (Z.600-606)
  - parseOptionalCaptionField + is_preview_candidate-Parse (Z.608-618)
  - repository.ReleaseVersionMediaPatchInput (Z.630); EventType "release_version_media.updated" (Z.694)
  - Permission CanForReleaseVersionMedia(..., ActionReleaseVersionMediaUpdate, relationID) (Z.570)

api.ts-Analoge:
  - patchReleaseVersionMediaItem(versionId, mediaId, patch: ReleaseVersionMediaPatchRequest, authToken?) → PATCH /api/v1/admin/release-versions/{versionId}/media/{mediaId}
  - uploadFansubMedia/deleteFansubMedia → Basis /api/v1/admin/fansubs/{id}/media
  - authorizedFetch + getApiBaseUrl + ApiError

Sichtbarkeits-/Status-Werte (v1.2-DISCUSSION Entscheidung 8): intern / in Prüfung / öffentlich / abgelehnt / archiviert / entfernt.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Phase-72-Schema-Gate — Sichtbarkeit/Reviewstatus-Spalten live verifizieren</name>
  <action>
    Inspiziere das Live-Dev-Schema (Postgres 16) der medien-relevanten Tabellen, bevor irgendein Endpoint/Repo/Migration-Code entsteht: \d media_assets, \d fansub_group_media, \d release_version_media, \d visibilities bzw. aequivalente information_schema-Queries. Protokolliere die realen Spalten und erlaubten Enum-/Lookup-Werte fuer Sichtbarkeit (Achse 1) und Reviewstatus (Achse 2) und ob diese direkt auf fansub_group_media/release_version_media oder ueber media_assets.status / FK auf visibilities aufgeloest werden. Pausiere danach fuer die Entwickler-Entscheidung (schema-ok vs. phase72-fehlt); schreibe noch keinen Produktionscode.
  </action>
  <what-built>
    Automatisierte Schema-Inspektion der medien-relevanten Tabellen, bevor irgendein Endpoint/Repo-Code geschrieben wird. Der Executor führt gegen die lokale Dev-DB aus:
    - `\d media_assets`, `\d fansub_group_media`, `\d release_version_media`, `\d visibilities` (oder äquivalente information_schema-Queries),
    und protokolliert die tatsächlichen Spalten/Enum-Werte für Sichtbarkeit (Achse 1) und Reviewstatus (Achse 2).
  </what-built>
  <how-to-verify>
    1. Dev-Stack läuft (Docker Compose Postgres 16).
    2. Bestätige, ob `fansub_group_media` und `release_version_media` direkte visibility-/review_status-Spalten haben ODER ob diese über `media_assets` (Spalte `status` / FK auf `visibilities`) aufgelöst werden.
    3. Bestätige die exakten erlaubten Enum-/Lookup-Werte für beide Achsen.
    4. Entscheide:
       - Wenn die Phase-72-Spalten vorhanden sind → Implementierung mit den realen Spaltennamen fortsetzen (Tasks 1-3).
       - Wenn die Spalten FEHLEN (Phase 72 noch nicht ausgeführt) → STOPP und an den Entwickler eskalieren: Phase 72 muss zuerst ausgeführt werden ODER der Plan-Scope für Medienprüfung wird verschoben (78-04 fällt dann auf reine Phase-76-Stubs + Claim/Contribution-Review zurück).
  </how-to-verify>
  <resume-signal>Tippe „schema-ok: <reale Spaltennamen>" um mit Tasks 1-3 fortzufahren, oder „phase72-fehlt" um den Medien-Scope zu eskalieren.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Contract + Repo + Handler — PATCH fansub_group_media (Sichtbarkeit/Reviewstatus)</name>
  <files>shared/contracts/admin-content.yaml, backend/internal/repository/media_repository.go, backend/internal/handlers/fansub_media_review_handler.go, backend/cmd/server/main.go, backend/internal/routes/admin_routes.go</files>
  <read_first>
    - backend/internal/handlers/fansub_media_review_handler_test.go (RED-Vertrag aus 78-01 — muss grün werden)
    - backend/internal/handlers/contribution_review_handler.go:18-49,61-151 (Interface-getriebenes Handler-Muster + Audit)
    - backend/internal/handlers/fansub_media_upload.go:17-37 (Permission + Deny-Audit-Muster, ActionFansubGroupEdit)
    - backend/internal/repository/media_repository.go (vorhandene media_assets-Zugriffe; Owner-/Status-Spalten aus Task-0-Befund)
    - backend/internal/repository/audit_logs.go (AuditLogEntry-Felder)
    - backend/internal/routes/admin_routes.go (Route-Registrierungs-Muster mit authMiddleware)
    - shared/contracts/admin-content.yaml (Contract-Struktur für /admin/fansubs/:id/media)
  </read_first>
  <action>
    Contract zuerst (Lock K): ergänze in admin-content.yaml den Pfad `PATCH /api/v1/admin/fansubs/{id}/media/{mediaId}` mit Request-Body-Schema { visibility, review_status } (Enum-Werte aus Task-0-Befund) und Response-Schema des aktualisierten Medien-Items.
    Repository: füge in media_repository.go eine Methode `UpdateFansubGroupMediaReview(ctx, fansubGroupID, mediaID int64, visibility, reviewStatus string) error` hinzu, die NUR Sichtbarkeit/Reviewstatus auf der korrekten Owner-Tabelle (fansub_group_media bzw. media_assets gemäß Task-0-Befund) setzt und per WHERE-Klausel die fansubID-Zugehörigkeit erzwingt (kein Update bei fremder Gruppe → Tampering-Mitigation). NIEMALS owner_type/owner_id ändern (D-05).
    Handler: erstelle fansub_media_review_handler.go im package handlers, analog contribution_review_handler.go interface-getrieben (Repo-Interface mit UpdateFansubGroupMediaReview, reviewPermissionChecker, auditLogWriter) plus Konstruktor NewFansubMediaReviewHandler. Methode PatchFansubMediaReview: permissionActorFromContext → parseFansubID → parse mediaID aus Param → CanForFansubGroup(ActionFansubGroupEdit) → bei Deny auditPermissionDenied("fansub_group_media.review.denied") + writePermissionDenied. Body parsen (rawBody map), visibility/review_status gegen erlaubte Enum-Werte validieren (ungültig → 400, keine Mutation, V5). Bei Erfolg Repo-Update + Audit Write EventType "fansub_group_media.visibility_updated" (ActorAppUserID, ScopeTypeGroup, ScopeID=&fansubID, TargetType "fansub_group_media", TargetID=&mediaID, Action ActionFansubGroupEdit, Outcome "allowed"). Datei ≤450 Zeilen.
    Verdrahtung: Handler in main.go konstruieren (mediaRepo/permissionSvc/auditLogRepo injizieren) und Route in admin_routes.go mit authMiddleware registrieren.
    Go-Response-Strings mit korrekten Umlauten.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/handlers/... -run TestFansubMediaReview 2>&1 | grep -Eiv '^#' | grep -Ei "ok|pass|fail"</automated>
  </verify>
  <acceptance_criteria>
    - TestFansubMediaReview (aus 78-01) ist grün: Deny→403+Deny-Audit, Erfolg→Audit "fansub_group_media.visibility_updated", ungültiger Enum→400 ohne Mutation, Owner-Mismatch→kein Update.
    - `grep "fansub_group_media.visibility_updated" backend/internal/handlers/fansub_media_review_handler.go` matcht.
    - Repo-Methode ändert kein owner_type/owner_id (Source-Inspektion: kein `owner_type =`/`owner_id =` im UPDATE).
    - admin-content.yaml enthält den neuen PATCH-Pfad; `cd backend && go build ./...` ohne Fehler.
    - fansub_media_review_handler.go ≤450 Zeilen.
  </acceptance_criteria>
  <done>Neuer Gruppenmedien-Review-Endpoint vertragskonform, gegated, auditiert; Backend-RED-Test grün.</done>
</task>

<task type="auto">
  <name>Task 2: PatchReleaseVersionMedia additiv um visibility/review_status erweitern</name>
  <files>backend/internal/handlers/admin_content_release_version_media.go, shared/contracts/admin-content.yaml</files>
  <read_first>
    - backend/internal/handlers/admin_content_release_version_media.go:549-700 (rawBody-Parse, ReleaseVersionMediaPatchInput, EventType "release_version_media.updated", CanForReleaseVersionMedia)
    - backend/internal/repository (ReleaseVersionMediaPatchInput-Definition)
    - shared/contracts/admin-content.yaml (release-versions media PATCH-Schema)
  </read_first>
  <action>
    Erweitere den bestehenden PATCH-Handler additiv: parse `visibility` und `review_status` aus rawBody analog zum is_preview_candidate-Parse (optional, nur setzen wenn Key vorhanden), validiere gegen die erlaubten Enum-Werte (Task-0-Befund; ungültig → 400). Ergänze die Felder in ReleaseVersionMediaPatchInput und im Repo-Update-Pfad (korrekte Owner-Tabelle release_version_media bzw. media_assets — Lock G). Owner-Typ NICHT änderbar (D-05). Bei erfolgreicher Mutation mit gesetzten Review-Feldern Audit EventType "release_version_media.visibility_updated" schreiben (zusätzlich/statt "release_version_media.updated" je nach geänderten Feldern); bestehender Deny-Audit "release_version_media.update.denied" und Permission CanForReleaseVersionMedia bleiben. Ergänze die neuen optionalen Felder im admin-content.yaml-Schema des release-versions-media-PATCH. category-Ablehnung unverändert lassen.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/handlers/... -run TestPatchReleaseVersionMedia 2>&1 | grep -Eiv '^#' | grep -Ei "ok|pass|fail|undefined"</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && go build ./...` ohne Fehler; bestehende release-version-media-Tests bleiben grün.
    - `grep "release_version_media.visibility_updated" backend/internal/handlers/admin_content_release_version_media.go` matcht.
    - visibility/review_status werden nur additiv geparst (kein Bruch bestehender caption/is_preview_candidate-Pfade); ungültige Enum-Werte → 400.
    - admin-content.yaml release-versions-media-PATCH-Schema enthält die neuen optionalen Felder.
  </acceptance_criteria>
  <done>Release-Version-Media-PATCH unterstützt Sichtbarkeit/Reviewstatus additiv mit Audit; korrekte Owner-Tabelle.</done>
</task>

<task type="auto">
  <name>Task 3: api.ts-Helfer (Lock K) — patchFansubMediaReview + Erweiterung patchReleaseVersionMediaItem</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts:2245-2280 (uploadFansubMedia/deleteFansubMedia — Basis-Pfad /admin/fansubs/{id}/media, authorizedFetch-Muster)
    - frontend/src/lib/api.ts:6050-6075 (patchReleaseVersionMediaItem + ReleaseVersionMediaPatchRequest-Typ)
    - frontend/src/lib/api.ts (ApiError, getApiBaseUrl, authorizedFetch)
  </read_first>
  <action>
    Ergänze `patchFansubMediaReview(fansubId: number, mediaId: number, patch: { visibility?: string; review_status?: string }, authToken?): Promise<FansubGroupMediaItem>` als typisierten Helfer (authorizedFetch, PATCH /api/v1/admin/fansubs/{fansubId}/media/{mediaId}, ApiError-Handling) — kein ad-hoc-Fetch. Definiere den zugehörigen Response-Typ passend zum Contract aus Task 1.
    Erweitere den Typ `ReleaseVersionMediaPatchRequest` um optionale Felder `visibility?` und `review_status?`, sodass patchReleaseVersionMediaItem diese durchreicht (Backend aus Task 2). Keine bestehende Signatur brechen.
    Stelle sicher, dass die Helfer die im Contract dokumentierten Enum-Werte als Typunion erlauben (oder string mit Kommentar auf die Quelle).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -Eiv '^#' | grep -Ei "error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` ohne neue Typfehler.
    - `grep "patchFansubMediaReview" frontend/src/lib/api.ts` matcht; Helfer nutzt authorizedFetch (kein nacktes `fetch(`).
    - ReleaseVersionMediaPatchRequest enthält optionale visibility/review_status; bestehende Aufrufer brechen nicht.
  </acceptance_criteria>
  <done>Beide api.ts-Helfer vorhanden/erweitert, typgeprüft, contract-aligned (Lock K); bereit für 78-04-UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Admin-Media-API | Untrusted Sichtbarkeits-/Status-Werte und mediaId queren hier; serverseitige Validierung + Scope-Prüfung Pflicht. |
| Handler → Owner-Tabelle | Mutation darf nur die kanonische Owner-Tabelle der Fläche treffen (Lock G); keine Owner-Umhängung. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-07 | Elevation of Privilege | Capability-Bypass auf Media-PATCH (Direktaufruf) | mitigate | Jeder Endpoint ruft CanForFansubGroup(ActionFansubGroupEdit) bzw. CanForReleaseVersionMedia(ActionReleaseVersionMediaUpdate); Frontend-Gating ist nicht maßgeblich (SC5/D-08). |
| T-78-08 | Tampering | IDOR — Leader von Gruppe A patcht Medium von Gruppe B (D-04) | mitigate | Repo-UPDATE erzwingt fansubID-Zugehörigkeit per WHERE; Owner-Mismatch-Test (78-01) deckt es ab; Permission-Scope auf fansubID. |
| T-78-09 | Repudiation | Mutation ohne Audit | mitigate | Pflicht-auditLogRepo.Write nach jeder Mutation (visibility_updated) + Deny-Audit; durch Backend-Test erzwungen (D-09). |
| T-78-10 | Tampering | Verbotenes Owner-Umhängen über Review-Patch | mitigate | Repo-Methode setzt ausschließlich visibility/review_status; Source-Inspektion bestätigt kein owner_type/owner_id-UPDATE (D-05); Owner-Typ-Wechsel ist Phase 79. |
| T-78-11 | Tampering (Input) | Ungültige Sichtbarkeits-/Status-Enums | mitigate | Enum-Validierung serverseitig (V5); ungültig → 400 ohne Mutation. |
| T-78-SC | Tampering | go/npm installs | accept | Keine neuen Pakete; nur vorhandene Frameworks/Treiber (pgx, gin). |
</threat_model>

<verification>
- `cd backend && go build ./... && go test ./internal/handlers/... -run "TestFansubMediaReview|TestPatchReleaseVersionMedia"` → grün.
- `cd backend && go test ./...` → keine Regressions.
- `cd frontend && npx tsc --noEmit` → ohne neue Fehler.
- Source-Gate: kein `owner_type`/`owner_id`-UPDATE in den Review-Repo-Methoden (`grep -v '^//' | grep -Ei "owner_(type|id)\\s*="` liefert nichts im UPDATE).
</verification>

<success_criteria>
- Neuer fansub_group_media-Review-PATCH + erweiterter release_version_media-PATCH existieren, sind vertragskonform (Lock K), gegated (D-08) und auditiert (D-09).
- Mutationen treffen ausschließlich die kanonische Owner-Tabelle (Lock G), ohne Owner-Umhängen (D-05).
- IDOR/Cross-Group ist durch fansubID-Scope verhindert (D-04).
- api.ts-Helfer vorhanden und typgeprüft.
</success_criteria>

<output>
Create `.planning/phases/78-leader-workspace-review-pflege/78-03-SUMMARY.md` when done
</output>
