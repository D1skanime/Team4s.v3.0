---
phase: 78-leader-workspace-review-pflege
plan: 03
type: execute
wave: 2
depends_on: [78-01]
files_modified:
  - shared/contracts/admin-content.yaml
  - backend/internal/handlers/fansub_media_review_handler.go
  - backend/internal/repository/media_repository.go
  - backend/cmd/server/main.go
  - backend/internal/routes/admin_routes.go
  - frontend/src/lib/api.ts
autonomous: false
requirements: [G, I, K, F]
must_haves:
  truths:
    - "D-05: Aktionsumfang = Sichtbarkeit + Reviewstatus setzen; Owner-Korrektheit wird nur geflaggt zurückgegeben, NIE umgehängt (kein Owner-Typ-Wechsel)."
    - "D-06: Gruppenmedien schreiben in fansub_group_media — jede Mutation in die korrekte Owner-Tabelle (Lock G); Release-Version-Medien sind Scope von 78-05."
    - "D-08: Jeder Endpoint (GET-Liste UND PATCH) prüft serverseitig CanForFansubGroup(ActionFansubGroupEdit); Gruppenmitgliedschaft allein genügt nicht."
    - "D-09: Jede erfolgreiche Mutation schreibt einen Audit-Eintrag mit Akteur-Attribution; Deny erzeugt .denied-Audit (Lock I/K)."
    - "Lock K: Neue Endpoints (GET-Liste + PATCH) folgen der Contract-Sequenz admin-content.yaml → DTO → Repo → Route → api.ts (Typ + Helfer); kein ad-hoc-Fetch."
  artifacts:
    - path: "backend/internal/handlers/fansub_media_review_handler.go"
      provides: "GET-Liste + PATCH-Handler für fansub_group_media Sichtbarkeit/Reviewstatus mit Permission + Audit + Enum-Validierung"
      max_lines: 450
    - path: "frontend/src/lib/api.ts"
      provides: "listFansubGroupMedia (Liste, Lese-Quelle für 78-04) + patchFansubMediaReview (Mutation)"
    - path: "shared/contracts/admin-content.yaml"
      provides: "Contract-Einträge GET-Liste + PATCH der Gruppenmedien-Review-Felder + Response-Schema FansubGroupMediaItem"
  key_links:
    - from: "fansub_media_review_handler.go"
      to: "repository.AuditLogRepository.Write"
      via: "Audit nach Mutation"
      pattern: "auditLogRepo\\.Write|EventType:\\s*.fansub_group_media"
    - from: "fansub_media_review_handler.go"
      to: "permissionSvc.CanForFansubGroup(ActionFansubGroupEdit)"
      via: "serverseitige Autorisierung (GET + PATCH)"
      pattern: "CanForFansubGroup"
    - from: "frontend/src/lib/api.ts"
      to: "GET /api/v1/admin/fansubs/:id/media"
      via: "authorizedFetch (Lese-Quelle für GroupMediaReviewSection)"
      pattern: "listFansubGroupMedia"
    - from: "frontend/src/lib/api.ts"
      to: "PATCH /api/v1/admin/fansubs/:id/media/:mediaId"
      via: "authorizedFetch"
      pattern: "patchFansubMediaReview"
---

<objective>
Die Backend-/Contract-/API-Schicht für die Gruppenmedien-Prüfung (D-05/D-06): einen GET-Listen-Endpoint (Lese-Quelle für die UI) UND einen PATCH-Endpoint für Sichtbarkeit/Reviewstatus auf `fansub_group_media` schaffen — vertragskonform (Lock K), in die korrekte Owner-Tabelle (Lock G), serverseitig autorisiert und vollständig auditiert (D-09/Lock I).

Purpose: SC3 (Gruppenmedien-Prüfung schreibt korrekte Owner-Tabelle) und liefert BEIDE api.ts-Helfer, die 78-04 (UI) konsumiert — `listFansubGroupMedia` als Lese-Quelle (rendert die Karten) und `patchFansubMediaReview` als Mutation. Berührt NICHT page.tsx oder Review-Komponenten → konfliktfrei parallel zu 78-02. Die Release-Version-Media-Prüfung (PATCH-Extension + Release-Drawer-Wiring) ist in **Plan 78-05** ausgelagert, damit 78-03 fokussiert bleibt (Scope-Sanity) und der release_version_media-PATCH einen echten Frontend-Caller erhält.
Output: GET-Liste + PATCH-Handler + Repo-Methoden + Route-Registrierung + Contract-Einträge + zwei api.ts-Helfer (List + Patch) + Response-Typ.

WICHTIG — Schema-Abhängigkeit (Phase 72): Die genauen Spalten für Sichtbarkeit/Reviewstatus (`media_assets.status`, `visibilities`-Lookup, ggf. Felder auf `fansub_group_media`) sind eine [ASSUMED]-Annahme (RESEARCH A1, jetzt RESOLVED-zur-Ausführungszeit), bis Phase 72 ausgeführt ist. Task 0 verifiziert das Live-Schema VOR jeder irreversiblen Implementierung (blocking checkpoint). Der `phase72-fehlt`-Zweig ist ein dokumentierter HARTER STOPP, der den Medien-Scope re-skopiert (siehe Task 0).

KANONISCHER ENUM-SATZ (identisch in 78-01-RED-Test, hier und UI-SPEC):
  visibility: intern | oeffentlich
  review_status: in_pruefung | freigegeben | abgelehnt | archiviert | entfernt
(Quelle: 78-RESEARCH.md „Offene Fragen (RESOLVED)". Physische Spalten/FK-Auflösung kommt aus Task 0; die value-Strings bleiben stabil.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-leader-workspace-review-pflege/78-CONTEXT.md
@.planning/phases/78-leader-workspace-review-pflege/78-RESEARCH.md
@.planning/phases/78-leader-workspace-review-pflege/78-PATTERNS.md
@.planning/milestones/v1.2-DISCUSSION.md

<interfaces>
<!-- Aus Codebase extrahiert. -->
Permission-Muster (fansub_media_upload.go:17-37):
  identity, actor, ok := permissionActorFromContext(c)
  fansubID, err := parseFansubID(c.Param("id"))
  result, err := h.permissionSvc.CanForFansubGroup(ctx, actor, permissions.ActionFansubGroupEdit, fansubID)
  if !result.Allowed { auditPermissionDenied(...); writePermissionDenied(c, result); return }

Audit-Muster (contribution_review_handler.go:142-151):
  h.auditLogRepo.Write(ctx, repository.AuditLogEntry{ActorAppUserID, EventType, ScopeType: permissions.ScopeTypeGroup, ScopeID:&fansubID, TargetType, TargetID, Action, Outcome:"allowed"})

Bestehende Fansub-Media-Basis (fansub_media_upload.go):
  - Basis-Route /api/v1/admin/fansubs/:id/media (uploadFansubMedia/deleteFansubMedia)
  - media_repository.go hält die fansub_group_media-/media_assets-Zugriffe

api.ts-Analoge:
  - uploadFansubMedia/deleteFansubMedia → Basis /api/v1/admin/fansubs/{id}/media
  - authorizedFetch + getApiBaseUrl + ApiError

Sichtbarkeits-/Status-value-Strings (kanonisch, s. Objective): intern/oeffentlich · in_pruefung/freigegeben/abgelehnt/archiviert/entfernt.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Phase-72-Schema-Gate — Sichtbarkeit/Reviewstatus-Spalten live verifizieren</name>
  <action>
    Inspiziere das Live-Dev-Schema (Postgres 16) der medien-relevanten Tabellen, bevor irgendein Endpoint/Repo/Migration-Code entsteht: \d media_assets, \d fansub_group_media, \d visibilities bzw. aequivalente information_schema-Queries. Protokolliere die realen Spalten und erlaubten Enum-/Lookup-Werte fuer Sichtbarkeit (Achse 1) und Reviewstatus (Achse 2) und ob diese direkt auf fansub_group_media oder ueber media_assets.status / FK auf visibilities aufgeloest werden. Pausiere danach fuer die Entwickler-Entscheidung (schema-ok vs. phase72-fehlt); schreibe noch keinen Produktionscode.
  </action>
  <what-built>
    Automatisierte Schema-Inspektion der medien-relevanten Tabellen, bevor irgendein Endpoint/Repo-Code geschrieben wird. Der Executor führt gegen die lokale Dev-DB aus:
    - `\d media_assets`, `\d fansub_group_media`, `\d visibilities` (oder äquivalente information_schema-Queries),
    und protokolliert die tatsächlichen Spalten/Enum-Werte für Sichtbarkeit (Achse 1) und Reviewstatus (Achse 2).
  </what-built>
  <how-to-verify>
    1. Dev-Stack läuft (Docker Compose Postgres 16).
    2. Bestätige, ob `fansub_group_media` direkte visibility-/review_status-Spalten hat ODER ob diese über `media_assets` (Spalte `status` / FK auf `visibilities`) aufgelöst werden.
    3. Bestätige die exakten erlaubten Enum-/Lookup-Werte für beide Achsen und mappe sie auf den kanonischen value-Satz (intern/oeffentlich · in_pruefung/freigegeben/abgelehnt/archiviert/entfernt).
    4. Entscheide:
       - Wenn die Phase-72-Spalten vorhanden sind → Implementierung mit den realen Spaltennamen fortsetzen (Tasks 1-2).
       - Wenn die Spalten FEHLEN (Phase 72 noch nicht ausgeführt) → HARTER STOPP, an den Entwickler eskalieren: Phase 72 muss zuerst ausgeführt werden ODER der Plan-Scope für Medienprüfung wird verschoben. In diesem Fall RE-SKOPIERT der Stopp auch 78-04: GroupMediaReviewSection (78-04 Task 1) fällt auf einen reinen Phase-76-Stub + Claim/Contribution-Review zurück, und 78-05 (Release-Version-Media) wird ebenfalls verschoben. Dieser `phase72-fehlt`-Zweig ist der dokumentierte Re-Scope-Pfad.
  </how-to-verify>
  <resume-signal>Tippe „schema-ok: <reale Spaltennamen>" um mit Tasks 1-2 fortzufahren, oder „phase72-fehlt" um den Medien-Scope zu eskalieren (re-skopiert 78-04 + verschiebt 78-05).</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Contract + Repo + Handler — GET-Liste + PATCH fansub_group_media (Sichtbarkeit/Reviewstatus)</name>
  <files>shared/contracts/admin-content.yaml, backend/internal/repository/media_repository.go, backend/internal/handlers/fansub_media_review_handler.go, backend/cmd/server/main.go, backend/internal/routes/admin_routes.go</files>
  <read_first>
    - backend/internal/handlers/fansub_media_review_handler_test.go (RED-Vertrag aus 78-01 — muss grün werden)
    - backend/internal/handlers/contribution_review_handler.go:18-49,61-151 (Interface-getriebenes Handler-Muster + Audit)
    - backend/internal/handlers/fansub_media_upload.go:17-37 (Permission + Deny-Audit-Muster, ActionFansubGroupEdit; Basis-Route /admin/fansubs/:id/media)
    - backend/internal/repository/media_repository.go (vorhandene media_assets-/fansub_group_media-Zugriffe; Owner-/Status-Spalten aus Task-0-Befund)
    - backend/internal/repository/audit_logs.go (AuditLogEntry-Felder)
    - backend/internal/routes/admin_routes.go (Route-Registrierungs-Muster mit authMiddleware)
    - shared/contracts/admin-content.yaml (Contract-Struktur für /admin/fansubs/:id/media)
  </read_first>
  <action>
    Contract zuerst (Lock K): ergänze in admin-content.yaml ZWEI Einträge:
    (1) `GET /api/v1/admin/fansubs/{id}/media` (Liste) mit Response-Schema `FansubGroupMediaListResponse` = { data: FansubGroupMediaItem[] }, wobei `FansubGroupMediaItem` mindestens { id, kind/category, url/preview_url, visibility, review_status, owner_type, owner_id, owner_consistent: bool } enthält (Felder gemäß Task-0-Befund; `owner_consistent` flaggt Owner-Inkonsistenz NUR lesend für das UI-Badge — kein Setter).
    (2) `PATCH /api/v1/admin/fansubs/{id}/media/{mediaId}` mit Request-Body { visibility, review_status } (kanonische Enum-value-Strings) und Response = das aktualisierte `FansubGroupMediaItem`.

    Repository (media_repository.go):
    - `ListFansubGroupMediaForReview(ctx, fansubGroupID int64) ([]FansubGroupMediaReviewRow, error)` — liest die Gruppenmedien der Gruppe (korrekte Owner-Tabelle fansub_group_media bzw. JOIN media_assets gemäß Task-0-Befund) inkl. visibility/review_status/owner_type/owner_id und eines abgeleiteten owner_consistent-Flags; per WHERE strikt auf fansubGroupID gescoped (IDOR-Schutz/D-04).
    - `UpdateFansubGroupMediaReview(ctx, fansubGroupID, mediaID int64, visibility, reviewStatus string) error` — setzt NUR Sichtbarkeit/Reviewstatus auf der korrekten Owner-Tabelle und erzwingt per WHERE-Klausel die fansubID-Zugehörigkeit (kein Update bei fremder Gruppe → Tampering-Mitigation). NIEMALS owner_type/owner_id ändern (D-05).

    Handler (fansub_media_review_handler.go im package handlers), interface-getrieben analog contribution_review_handler.go (Repo-Interface mit ListFansubGroupMediaForReview + UpdateFansubGroupMediaReview, reviewPermissionChecker, auditLogWriter) plus Konstruktor NewFansubMediaReviewHandler:
    - `ListFansubGroupMedia` (GET): permissionActorFromContext → parseFansubID → CanForFansubGroup(ActionFansubGroupEdit) → bei Deny auditPermissionDenied("fansub_group_media.list.denied") + writePermissionDenied; bei Erfolg Repo-Liste lesen und als FansubGroupMediaListResponse serialisieren. (Read-Pfad: kein Mutation-Audit nötig, aber Deny-Audit pflicht.)
    - `PatchFansubMediaReview` (PATCH): permissionActorFromContext → parseFansubID → parse mediaID aus Param → CanForFansubGroup(ActionFansubGroupEdit) → bei Deny auditPermissionDenied("fansub_group_media.review.denied") + writePermissionDenied. Body parsen (rawBody map), visibility/review_status gegen die kanonischen Enum-value-Strings validieren (ungültig → 400, keine Mutation, V5). Bei Erfolg Repo-Update + Audit Write EventType "fansub_group_media.visibility_updated" (ActorAppUserID, ScopeTypeGroup, ScopeID=&fansubID, TargetType "fansub_group_media", TargetID=&mediaID, Action ActionFansubGroupEdit, Outcome "allowed").
    Datei ≤450 Zeilen.

    Verdrahtung: Handler in main.go konstruieren (mediaRepo/permissionSvc/auditLogRepo injizieren) und BEIDE Routen (GET-Liste + PATCH) in admin_routes.go mit authMiddleware registrieren.
    Go-Response-Strings mit korrekten Umlauten.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/handlers/... -run TestFansubMediaReview && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - TestFansubMediaReview (aus 78-01) ist grün: Deny→403+Deny-Audit, Erfolg→Audit "fansub_group_media.visibility_updated", ungültiger Enum→400 ohne Mutation, Owner-Mismatch→kein Update.
    - `grep "fansub_group_media.visibility_updated" backend/internal/handlers/fansub_media_review_handler.go` matcht.
    - Beide Repo-Methoden vorhanden: `grep -E "ListFansubGroupMediaForReview|UpdateFansubGroupMediaReview" backend/internal/repository/media_repository.go` matcht beide.
    - Repo-Update ändert kein owner_type/owner_id (Source-Inspektion: kein `owner_type =`/`owner_id =` im UPDATE).
    - admin-content.yaml enthält den neuen GET-Listen-Pfad UND den PATCH-Pfad; `cd backend && go build ./...` ohne Fehler.
    - fansub_media_review_handler.go ≤450 Zeilen.
  </acceptance_criteria>
  <done>Gruppenmedien GET-Liste + Review-PATCH vertragskonform, gegated (GET+PATCH), auditiert; Backend-RED-Test grün; Lese-Quelle für 78-04 steht.</done>
</task>

<task type="auto">
  <name>Task 2: api.ts-Helfer (Lock K) — listFansubGroupMedia (Lese-Quelle) + patchFansubMediaReview (Mutation)</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts:2245-2280 (uploadFansubMedia/deleteFansubMedia — Basis-Pfad /admin/fansubs/{id}/media, authorizedFetch-Muster)
    - frontend/src/lib/api.ts (ApiError, getApiBaseUrl, authorizedFetch)
    - shared/contracts/admin-content.yaml (FansubGroupMediaItem / FansubGroupMediaListResponse aus Task 1)
  </read_first>
  <action>
    Definiere den Response-Typ `FansubGroupMediaItem` passend zum Contract aus Task 1 ({ id; kind?; visibility: string; review_status: string; owner_type: string; owner_id: number; owner_consistent: boolean; preview_url?; ... }) und ggf. `FansubGroupMediaListResponse = { data: FansubGroupMediaItem[] }`.
    Ergänze `listFansubGroupMedia(fansubId: number, authToken?): Promise<FansubGroupMediaItem[]>` als typisierten Helfer (authorizedFetch, GET /api/v1/admin/fansubs/{fansubId}/media, ApiError-Handling) — kein ad-hoc-Fetch. DIES ist die Lese-Quelle, die GroupMediaReviewSection (78-04 Task 1) konsumiert.
    Ergänze `patchFansubMediaReview(fansubId: number, mediaId: number, patch: { visibility?: string; review_status?: string }, authToken?): Promise<FansubGroupMediaItem>` (authorizedFetch, PATCH /api/v1/admin/fansubs/{fansubId}/media/{mediaId}, ApiError-Handling).
    Stelle sicher, dass die Helfer die kanonischen Enum-value-Strings als Typunion erlauben (oder string mit Kommentar auf 78-RESEARCH „Offene Fragen (RESOLVED)").
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` ohne neue Typfehler.
    - `grep -E "listFansubGroupMedia|patchFansubMediaReview" frontend/src/lib/api.ts` matcht beide; beide nutzen authorizedFetch (kein nacktes `fetch(`).
    - Response-Typ `FansubGroupMediaItem` exportiert und enthält visibility/review_status/owner_consistent.
  </acceptance_criteria>
  <done>Beide api.ts-Helfer (List-Lesequelle + Patch-Mutation) vorhanden, typgeprüft, contract-aligned (Lock K); bereit für 78-04-UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Admin-Media-API | Untrusted Sichtbarkeits-/Status-Werte, fansubId und mediaId queren hier (GET-Liste UND PATCH); serverseitige Validierung + Scope-Prüfung Pflicht. |
| Handler → Owner-Tabelle | Mutation darf nur die kanonische Owner-Tabelle der Fläche (fansub_group_media) treffen (Lock G); keine Owner-Umhängung. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-07 | Elevation of Privilege | Capability-Bypass auf Media-GET/PATCH (Direktaufruf) | mitigate | Beide Endpoints rufen CanForFansubGroup(ActionFansubGroupEdit); Frontend-Gating ist nicht maßgeblich (SC5/D-08). Die GET-Liste ist ebenfalls gegated (kein anonymes Lesen fremder Gruppenmedien). |
| T-78-08 | Tampering | IDOR — Leader von Gruppe A liest/patcht Medium von Gruppe B (D-04) | mitigate | Repo-LIST und -UPDATE erzwingen fansubID-Zugehörigkeit per WHERE; Owner-Mismatch-Test (78-01) deckt PATCH ab; Permission-Scope auf fansubID gilt für beide. |
| T-78-09 | Repudiation | Mutation ohne Audit | mitigate | Pflicht-auditLogRepo.Write nach jeder Mutation (visibility_updated) + Deny-Audit (auch auf GET-Deny: "fansub_group_media.list.denied"); durch Backend-Test erzwungen (D-09). |
| T-78-10 | Tampering | Verbotenes Owner-Umhängen über Review-Patch | mitigate | Repo-Methode setzt ausschließlich visibility/review_status; Source-Inspektion bestätigt kein owner_type/owner_id-UPDATE (D-05); owner_consistent ist NUR lesend; Owner-Typ-Wechsel ist Phase 79. |
| T-78-11 | Tampering (Input) | Ungültige Sichtbarkeits-/Status-Enums | mitigate | Enum-Validierung serverseitig gegen kanonischen value-Satz (V5); ungültig → 400 ohne Mutation. |
| T-78-SC | Tampering | go/npm installs | accept | Keine neuen Pakete; nur vorhandene Frameworks/Treiber (pgx, gin). |
</threat_model>

<verification>
- `cd backend && go build ./... && go test ./internal/handlers/... -run TestFansubMediaReview` → grün.
- `cd backend && go test ./...` → keine Regressions.
- `cd frontend && npx tsc --noEmit` → ohne neue Fehler.
- Source-Gate: kein `owner_type`/`owner_id`-UPDATE in den Review-Repo-Methoden (`grep -v '^//' backend/internal/repository/media_repository.go | grep -Ei "owner_(type|id)\\s*="` liefert nichts im UPDATE-Pfad).
</verification>

<success_criteria>
- Neuer fansub_group_media GET-Listen-Endpoint (Lese-Quelle) + Review-PATCH existieren, sind vertragskonform (Lock K), beide gegated (D-08) und auditiert/deny-auditiert (D-09).
- Mutationen treffen ausschließlich die kanonische Owner-Tabelle (Lock G), ohne Owner-Umhängen (D-05); owner_consistent wird nur geflaggt.
- IDOR/Cross-Group ist durch fansubID-Scope auf GET und PATCH verhindert (D-04).
- api.ts-Helfer `listFansubGroupMedia` (Lese-Quelle für 78-04) + `patchFansubMediaReview` vorhanden und typgeprüft.
</success_criteria>

<output>
Create `.planning/phases/78-leader-workspace-review-pflege/78-03-SUMMARY.md` when done
</output>
</content>
