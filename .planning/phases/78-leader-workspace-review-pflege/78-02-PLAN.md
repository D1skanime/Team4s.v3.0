---
phase: 78-leader-workspace-review-pflege
plan: 02
type: execute
wave: 2
depends_on: [78-01]
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/ContributionsReviewSection.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/ContributionsReviewSection.module.css
  - frontend/src/components/contributions/ReviewQueue.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.tsx
autonomous: true
requirements: [F, H, I, K]
must_haves:
  truths:
    - "D-01: Contribution-Review bleibt im vorschlaege-Tab-Seam, Claims im claims-Tab — getrennte Flächen, kein gebündelter Review-Tab."
    - "D-02: Externe Mitwirkende/Contributions werden nur über den Contribution-Seam reviewt, nicht über Member-Tabs."
    - "D-07: ClaimManagementPanel und ContributionsReviewSection haben einen „Nur offene anzeigen\"-Toggle (Standard aktiv = offen zuerst); erledigte bleiben einsehbar."
    - "D-08: ContributionsReviewSection rendert null ohne capabilities.can_manage_members (Capability-Gating ohne neues Contract-Feld)."
    - "D-09: Bestätigen/Ablehnen laufen über bestehende confirmProposal/rejectProposal-Helfer mit serverseitigem Audit; kein neuer Frontend-Mutationspfad."
    - "Lock H: ContributionsReviewSection zeigt ausschließlich Contribution-Proposals, niemals Claims/Member-Requests."
  artifacts:
    - path: "frontend/src/app/admin/fansubs/[id]/edit/ContributionsReviewSection.tsx"
      provides: "Contribution-Proposal-Review mit @/components/ui-Primitives, offen-Filter, Gating"
      max_lines: 450
    - path: "frontend/src/app/admin/fansubs/[id]/edit/ContributionsReviewSection.module.css"
      provides: "Colocated Styles (reviewSection, cardStack, rejectExpansion, cardFooterActions)"
    - path: "frontend/src/components/contributions/ReviewQueue.tsx"
      provides: "Auf @/components/ui-Primitives migriert (keine nativen button/textarea/Inline-Styles)"
      max_lines: 450
    - path: "frontend/src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.tsx"
      provides: "„Nur offene anzeigen\"-Toggle (D-07)"
      max_lines: 450
  key_links:
    - from: "ContributionsReviewSection.tsx"
      to: "@/lib/api (listGroupProposals/confirmProposal/rejectProposal)"
      via: "Reuse bestehender Helfer"
      pattern: "listGroupProposals|confirmProposal|rejectProposal"
    - from: "ContributionsReviewSection.tsx"
      to: "@/components/ui"
      via: "Primitives-Import"
      pattern: "from .@/components/ui."
---

<objective>
Den Contribution-Review-Seam im `vorschlaege`-Tab auf eine eigene, GDS-konforme Komponente heben und den Claims-Filter (D-07) ergänzen — auf bestehenden Seams, ohne Parallel-Queue, ohne Vermischung mit Claims (Lock H).

Purpose: SC1 (offene Claims/Contributions getrennt prüfbar), D-07 (offen zuerst), CLAUDE.md-UI-Primitives-Pflicht (ReviewQueue-Migration). Diese Aufgaben berühren NICHT page.tsx und NICHT api.ts → konfliktfrei parallel zu 78-03.
Output: Neue ContributionsReviewSection (+ CSS), migrierte ReviewQueue, ClaimManagementPanel mit Filter-Toggle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-leader-workspace-review-pflege/78-CONTEXT.md
@.planning/phases/78-leader-workspace-review-pflege/78-UI-SPEC.md
@.planning/phases/78-leader-workspace-review-pflege/78-PATTERNS.md

<interfaces>
<!-- Aus Codebase extrahiert. -->
api.ts-Helfer (Reuse, keine Änderung in diesem Plan):
- listGroupProposals(fansubId, authToken?) → { data: GroupProposalRow[] }
- confirmProposal(fansubId, proposalId, authToken?)
- rejectProposal(fansubId, proposalId, reviewNote?, authToken?)

GroupProposalRow (@/types/contributions): { id, member_display_name, anime_title, role_codes: string[], note?, created_at }
FansubGroupCapabilities (@/types/fansub.ts:163): can_manage_members, can_create_invitation, ...

@/components/ui exportiert: Badge, Button, Card, EmptyState, SectionHeader, Table, TableBody, TableCell, TableHead, TableHeaderCell, TableRow, Toolbar, Textarea, LoadingState, ErrorState.

Referenz-Stil (PFLICHT): ClaimManagementPanel.tsx + ClaimManagementPanel.module.css (Primitivnutzung, CSS-Klassen tableWrapHeaderLineWine/inlineError/rowActions).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ContributionsReviewSection.tsx (+ CSS) bauen — GDS-konform, gegated, offen-Filter</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/ContributionsReviewSection.tsx, frontend/src/app/admin/fansubs/[id]/edit/ContributionsReviewSection.module.css</files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/ContributionsReviewSection.test.tsx (RED-Vertrag aus 78-01 — diese Tests müssen grün werden)
    - frontend/src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.tsx (Struktur/Gating/Primitivnutzung — PRIMÄR-Analog für Markup-Stil)
    - frontend/src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.module.css (CSS-Klassen-Muster)
    - frontend/src/components/contributions/ReviewQueue.tsx (Datenpipeline: loadProposals/handleConfirm/handleReject übernehmen, Markup NICHT übernehmen)
    - .planning/phases/78-leader-workspace-review-pflege/78-UI-SPEC.md (Pattern 1/2, Copywriting-Contract — exakte Strings; Pattern 5 Gating)
  </read_first>
  <behavior>
    - Test: ohne capabilities.can_manage_members → Komponente rendert null (D-08).
    - Test: zeigt „Offene Vorschläge ({n})" + eine Card pro gemocktem GroupProposalRow (member_display_name, anime_title, role_codes als Badge).
    - Test: „Nur offene anzeigen"-Toggle standardmäßig aktiv; im aktiven Zustand werden erledigte (status != proposed) ausgeblendet, im inaktiven sichtbar mit Badge variant="muted".
    - Test: „Vorschlag bestätigen" ruft confirmProposal(fansubId, id) und entfernt die Card optimistisch; niemals wird eine Claim-API aufgerufen (Lock H).
    - Test: Ablehnen öffnet Inline-Expansion mit Textarea (Placeholder „Ablehngrund (optional)"), „Ablehnung bestätigen" ruft rejectProposal.
  </behavior>
  <action>
    Erstelle ContributionsReviewSection.tsx mit Props { fansubId: number; capabilities: FansubGroupCapabilities }. Gate: `if (!capabilities.can_manage_members) return null` (D-08). Daten/Aktionen via Reuse von listGroupProposals/confirmProposal/rejectProposal (loadProposals useCallback + try/finally + readErrorMessage-Muster aus ReviewQueue). State: proposals, isLoading, error, rejectingId, rejectNote, cardErrors, showOnlyOpen (useState true). Markup ausschließlich mit @/components/ui-Primitives im ClaimManagementPanel-Stil: SectionHeader (Titel „Offene Vorschläge ({n})", Beschreibung „Mitglieder-eingereichte Contributions prüfen und bestätigen oder ablehnen."), Toolbar mit Filter-Toggle (Button variant ghost/subtle, Label „Nur offene anzeigen"/„Alle anzeigen") im trailing-Slot (D-07), Card variant="nested" pro Vorschlag, Badge variant="warning" „In Prüfung", Badge variant="info" für role_codes, Inline-Reject-Expansion mit Textarea + Button variant="danger" „Ablehnung bestätigen" + Button variant="ghost" „Abbrechen", Footer Button variant="success" „Vorschlag bestätigen" + Button variant="danger" „Vorschlag ablehnen". LoadingState/ErrorState/EmptyState-Primitives für die jeweiligen Zustände (exakte Empty-Strings aus Copywriting-Contract). Erstelle ContributionsReviewSection.module.css mit reviewSection/filterRow/cardStack/cardFooterActions/rejectExpansion/rejectActions/inlineError gemäß UI-SPEC Layout-Regeln (Design-Token-Variablen, keine Hex-Inline-Styles). Datei ≤450 Zeilen. Alle Strings mit korrekten Umlauten. KEIN window.confirm für Bestätigen.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/admin/fansubs/[id]/edit/ContributionsReviewSection.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ContributionsReviewSection.test.tsx (aus 78-01) ist grün.
    - `grep -v '^#' ContributionsReviewSection.tsx` enthält keine nativen `<button`/`<textarea`/`<select`/`<input`-Tags; Import aus `@/components/ui` vorhanden.
    - Datei ≤450 Zeilen (`wc -l` bestätigt).
    - Sektions-Titel-String exakt „Offene Vorschläge" und Toggle-Strings „Nur offene anzeigen"/„Alle anzeigen" mit Umlauten.
  </acceptance_criteria>
  <done>ContributionsReviewSection grün getestet, GDS-konform, gegated, mit offen-Filter; CSS colocated.</done>
</task>

<task type="auto">
  <name>Task 2: ReviewQueue auf @/components/ui migrieren + ClaimManagementPanel offen-Filter (D-07)</name>
  <files>frontend/src/components/contributions/ReviewQueue.tsx, frontend/src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.tsx</files>
  <read_first>
    - frontend/src/components/contributions/ReviewQueue.tsx (zu migrieren: native button Z.255-340, native textarea Z.239, Inline-Styles durchgehend)
    - frontend/src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.tsx (self — 349 Zeilen, Spielraum bis 450; bestehende useState-Muster copyStates/approveNicknames, Toolbar bereits importiert)
    - frontend/src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.test.tsx (darf nicht brechen)
    - .planning/phases/78-leader-workspace-review-pflege/78-UI-SPEC.md (Migrations-Pflicht, Pattern 1/6; Copywriting Claim-Review-Strings)
  </read_first>
  <action>
    ReviewQueue.tsx: ersetze native `<textarea>` durch `Textarea`, native `<button>` durch `Button` (variant="success" Bestätigen, variant="danger" Ablehnen, variant="ghost" Abbrechen), Status-Chip durch `Badge variant="warning"`, Karten-Markup durch `Card variant="nested"`, Loading/Error/Empty durch `LoadingState`/`ErrorState`/`EmptyState`. Alle Inline-Style-Objekte entfernen; falls Stiltrennung nötig, colocated ReviewQueue.module.css anlegen mit Token-basierten Klassen. Datenlogik (loadProposals/handleConfirm/handleReject) unverändert lassen. Strings/Umlaute beibehalten.
    HINWEIS Discretion-Entscheidung: Falls die page.tsx-Verdrahtung in 78-04 ContributionsReviewSection als Ersatz im vorschlaege-Tab nutzt, bleibt ReviewQueue dennoch primitivkonform migriert (keine UI-Schuld zurücklassen) — Entfernung erst, wenn 78-04 die Render-Stelle umstellt; in diesem Plan NUR migrieren, nicht löschen.
    ClaimManagementPanel.tsx: ergänze einen „Nur offene anzeigen"-Toggle (useState<boolean> Standard true) im Toolbar-trailing-Slot der Members-Sektion (D-07): bei aktiv werden historische Mitglieder ohne offenen/pending Claim ausgeblendet bzw. nach unten gruppiert, erledigte mit Badge variant="muted"; bei inaktiv vollständige Liste. Toggle-Strings „Nur offene anzeigen"/„Alle anzeigen". Bestehende Mutations-Helfer (verifyMemberClaim/rejectMemberClaim) und window.confirm für Claim-Ablehnen unverändert. Datei ≤450 Zeilen halten.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.test.tsx 2>&1 | grep -Eiv '^#' | grep -Ei "pass|fail"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' frontend/src/components/contributions/ReviewQueue.tsx` enthält keine nativen `<button`/`<textarea`-Tags und keine `style={{`-Inline-Styles mehr; Import aus `@/components/ui` vorhanden.
    - ClaimManagementPanel.test.tsx bleibt grün.
    - ClaimManagementPanel enthält einen sichtbaren Toggle mit Default-State „nur offene" (showOnlyOpen useState true) und den Strings „Nur offene anzeigen"/„Alle anzeigen".
    - Beide Dateien ≤450 Zeilen.
  </acceptance_criteria>
  <done>ReviewQueue primitivkonform migriert; ClaimManagementPanel hat funktionsfähigen offen-Filter; bestehende Tests grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Admin-API | Leader-Confirm/Reject-Aktionen queren hier; Frontend-Gating ist UX, nicht Sicherheit. |
| Contribution-Domäne ↔ Claim-Domäne | Strikte Trennung (Lock H/Entscheidung 10) — kein gemeinsamer Render-/Daten-Pfad. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-04 | Elevation of Privilege | ContributionsReviewSection ohne Capability | mitigate | Komponenten-Gate `if (!capabilities.can_manage_members) return null`; serverseitige Durchsetzung liegt im bestehenden contribution_review_handler (ActionFansubGroupMembersManage). |
| T-78-05 | Information Disclosure | Cross-Domain-Leak (Contribution als Claim darstellen) | mitigate | Separate Komponente + separate Datenpipeline; ContributionsReviewSection ruft ausschließlich Contribution-Proposal-Helfer (Test-Assertion in 78-01). |
| T-78-06 | Repudiation | Confirm/Reject ohne Audit | accept | Reuse bestehender confirmProposal/rejectProposal → Backend-Audit (contribution_review_handler.go) bereits vorhanden; kein neuer Mutationspfad. |
| T-78-SC | Tampering | npm installs | accept | Keine neuen Pakete; nur vorhandene @/components/ui-Primitives. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/app/admin/fansubs/[id]/edit/ContributionsReviewSection.test.tsx` → grün.
- `cd frontend && npx vitest run src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.test.tsx` → grün.
- `cd frontend && npx vitest run` → keine neuen Regressions.
- ESLint no-restricted-syntax: `cd frontend && npx eslint src/components/contributions/ReviewQueue.tsx` ohne native-Element-Warnungen.
</verification>

<success_criteria>
- ContributionsReviewSection existiert, ist gegated (D-08), hat offen-Filter (D-07), nutzt nur GDS-Primitives und zeigt nie Claims (Lock H).
- ReviewQueue.tsx ist vollständig auf @/components/ui migriert (UI-Schuld behoben).
- ClaimManagementPanel hat einen funktionierenden „Nur offene anzeigen"-Toggle.
- Alle drei Dateien ≤450 Zeilen; alle Strings mit korrekten Umlauten.
</success_criteria>

<output>
Create `.planning/phases/78-leader-workspace-review-pflege/78-02-SUMMARY.md` when done
</output>
