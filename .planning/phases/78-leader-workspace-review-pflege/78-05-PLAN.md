---
phase: 78-leader-workspace-review-pflege
plan: 05
type: execute
wave: 3
depends_on: [78-03]
files_modified:
  - shared/contracts/admin-content.yaml
  - backend/internal/handlers/admin_content_release_version_media.go
  - frontend/src/lib/api.ts
  - frontend/src/app/admin/.../ReleaseVersionMediaReviewSection.tsx
  - frontend/src/app/admin/.../ReleaseVersionMediaReviewSection.module.css
autonomous: false
requirements: [G, I, K, F]
must_haves:
  truths:
    - "D-05: Release-Version-Media-Review setzt nur Sichtbarkeit/Reviewstatus; Owner-Korrektheit wird nur geflaggt, NIE umgehängt."
    - "D-06: Release-Version-Medien schreiben in release_version_media (Lock G), Prüfung lebt an der Owner-Fläche Release-Drawer — kein zentraler Medienprüf-Tab."
    - "D-08: Der erweiterte PATCH prüft serverseitig CanForReleaseVersionMedia(ActionReleaseVersionMediaUpdate); Frontend-Gating ist UX, nicht Sicherheit."
    - "D-09: Erfolgreiche Review-Mutation schreibt Audit 'release_version_media.visibility_updated'; bestehender Deny-Audit bleibt (Lock I/K)."
    - "Lock K: Additive Contract-Erweiterung admin-content.yaml → DTO → Handler → api.ts; ReleaseVersionMediaReviewSection konsumiert den erweiterten PATCH (kein toter Backend-Key-Link)."
  artifacts:
    - path: "backend/internal/handlers/admin_content_release_version_media.go"
      provides: "PatchReleaseVersionMedia additiv um visibility/review_status + Audit erweitert"
      max_lines: 450
    - path: "frontend/src/app/admin/.../ReleaseVersionMediaReviewSection.tsx"
      provides: "Owner-Fläche im Release-Drawer: Sichtbarkeit/Reviewstatus-Selektoren + Owner-Flag (D-05/D-06), konsumiert den erweiterten PATCH"
      max_lines: 450
    - path: "frontend/src/lib/api.ts"
      provides: "ReleaseVersionMediaPatchRequest um optionale visibility/review_status erweitert"
  key_links:
    - from: "ReleaseVersionMediaReviewSection.tsx"
      to: "@/lib/api (patchReleaseVersionMediaItem)"
      via: "Speichern-Aktion konsumiert erweiterten PATCH"
      pattern: "patchReleaseVersionMediaItem"
    - from: "admin_content_release_version_media.go"
      to: "repository.AuditLogRepository.Write"
      via: "Audit nach Review-Mutation"
      pattern: "release_version_media.visibility_updated"
    - from: "admin_content_release_version_media.go"
      to: "CanForReleaseVersionMedia(ActionReleaseVersionMediaUpdate)"
      via: "serverseitige Autorisierung (bestehend)"
      pattern: "CanForReleaseVersionMedia"
---

<objective>
Die Release-Version-Media-Hälfte der Medienprüfung (D-05/D-06): den bestehenden `PatchReleaseVersionMedia`-Handler additiv um Sichtbarkeit/Reviewstatus erweitern UND die konsumierende UI (`ReleaseVersionMediaReviewSection` im Release-Drawer) bauen — sodass der erweiterte PATCH einen echten Frontend-Caller erhält (kein toter Backend-Key-Link, WARNING 7 Option a).

Purpose: Schließt die zweite Owner-Fläche aus D-06 (Release-Drawer release-version media), aus 78-03 ausgelagert für Scope-Sanity. Vertragskonform (Lock K), in die korrekte Owner-Tabelle release_version_media (Lock G), serverseitig autorisiert (bestehende CanForReleaseVersionMedia) und auditiert (D-09). Teilt admin-content.yaml + api.ts mit 78-03 (impliziter Datei-Dependency) → Wave 3, sequenziell nach 78-03; disjunkt zu 78-04 (parallel in Wave 3 möglich).
Output: Erweiterter Release-Version-Media-PATCH + Audit + Contract-Erweiterung + api.ts-Typerweiterung + neue ReleaseVersionMediaReviewSection-Komponente (im Release-Drawer eingehängt).

ABHÄNGIGKEIT vom 78-03 Task-0-Schema-Gate: Die kanonischen value-Strings (s.u.) und die reale Spalten-/FK-Auflösung stammen aus dem 78-03 Task-0-Befund. Falls dort `phase72-fehlt` entschieden wird, wird dieser Plan ebenfalls verschoben (siehe 78-03 Task 0 Re-Scope-Pfad).

KANONISCHER ENUM-SATZ (identisch zu 78-03 / UI-SPEC):
  visibility: intern | oeffentlich
  review_status: in_pruefung | freigegeben | abgelehnt | archiviert | entfernt
(Quelle: 78-RESEARCH.md „Offene Fragen (RESOLVED)".)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-leader-workspace-review-pflege/78-CONTEXT.md
@.planning/phases/78-leader-workspace-review-pflege/78-RESEARCH.md
@.planning/phases/78-leader-workspace-review-pflege/78-UI-SPEC.md
@.planning/phases/78-leader-workspace-review-pflege/78-PATTERNS.md
@.planning/milestones/v1.2-DISCUSSION.md

<interfaces>
<!-- Aus Codebase extrahiert. -->
Bestehender PatchReleaseVersionMedia (admin_content_release_version_media.go:549-700):
  - rawBody map[string]interface{} (Z.595); category-Änderung wird abgelehnt (Z.600-606)
  - parseOptionalCaptionField + is_preview_candidate-Parse (Z.608-618)
  - repository.ReleaseVersionMediaPatchInput (Z.630); EventType "release_version_media.updated" (Z.694)
  - Permission CanForReleaseVersionMedia(..., ActionReleaseVersionMediaUpdate, relationID) (Z.570)

api.ts-Analog:
  - patchReleaseVersionMediaItem(versionId, mediaId, patch: ReleaseVersionMediaPatchRequest, authToken?) → PATCH /api/v1/admin/release-versions/{versionId}/media/{mediaId}
  - authorizedFetch + getApiBaseUrl + ApiError

Release-Drawer-Seam (UI-SPEC Z.160): ReleaseVersionMediaDrawerSummary.tsx bleibt read-only; neue ReleaseVersionMediaReviewSection als separater Slot im Drawer. Der konkrete Drawer-Host + die Mediendaten-Quelle (read) werden beim read_first ermittelt (vorhandener Drawer-Render + bestehende Summary-Datenpipeline).

@/components/ui: Select, FormField, Button, Card, Badge, EmptyState, SectionHeader, Toolbar, LoadingState, ErrorState.

Sichtbarkeits-/Status-value-Strings (kanonisch, s. Objective): intern/oeffentlich · in_pruefung/freigegeben/abgelehnt/archiviert/entfernt.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PatchReleaseVersionMedia additiv um visibility/review_status + Audit erweitern (Contract + Handler + api.ts-Typ)</name>
  <files>shared/contracts/admin-content.yaml, backend/internal/handlers/admin_content_release_version_media.go, frontend/src/lib/api.ts</files>
  <read_first>
    - backend/internal/handlers/admin_content_release_version_media.go:549-700 (rawBody-Parse, ReleaseVersionMediaPatchInput, EventType "release_version_media.updated", CanForReleaseVersionMedia)
    - backend/internal/repository (ReleaseVersionMediaPatchInput-Definition; Owner-/Status-Spalten aus 78-03-Task-0-Befund)
    - shared/contracts/admin-content.yaml (release-versions media PATCH-Schema)
    - frontend/src/lib/api.ts:6050-6075 (patchReleaseVersionMediaItem + ReleaseVersionMediaPatchRequest-Typ)
  </read_first>
  <action>
    Contract zuerst (Lock K): ergänze im admin-content.yaml-Schema des release-versions-media-PATCH die neuen OPTIONALEN Felder { visibility?, review_status? } (kanonische Enum-value-Strings) — additiv, bestehende Felder unverändert.
    Handler: erweitere den bestehenden PATCH-Handler additiv — parse `visibility` und `review_status` aus rawBody analog zum is_preview_candidate-Parse (optional, nur setzen wenn Key vorhanden), validiere gegen die kanonischen Enum-value-Strings (ungültig → 400, keine Mutation). Ergänze die Felder in ReleaseVersionMediaPatchInput und im Repo-Update-Pfad (korrekte Owner-Tabelle release_version_media bzw. media_assets — Lock G). Owner-Typ NICHT änderbar (D-05). Bei erfolgreicher Mutation mit gesetzten Review-Feldern Audit EventType "release_version_media.visibility_updated" schreiben (zusätzlich/statt "release_version_media.updated" je nach geänderten Feldern); bestehender Deny-Audit "release_version_media.update.denied" und Permission CanForReleaseVersionMedia bleiben. category-Ablehnung unverändert lassen.
    api.ts: erweitere den Typ `ReleaseVersionMediaPatchRequest` um optionale Felder `visibility?` und `review_status?`, sodass patchReleaseVersionMediaItem diese durchreicht. Keine bestehende Signatur brechen.
    Go-Response-Strings mit korrekten Umlauten.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/handlers/... -run TestPatchReleaseVersionMedia && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && go build ./...` ohne Fehler; bestehende release-version-media-Tests bleiben grün.
    - `grep "release_version_media.visibility_updated" backend/internal/handlers/admin_content_release_version_media.go` matcht.
    - visibility/review_status werden nur additiv geparst (kein Bruch bestehender caption/is_preview_candidate-Pfade); ungültige Enum-Werte → 400.
    - admin-content.yaml release-versions-media-PATCH-Schema enthält die neuen optionalen Felder; `ReleaseVersionMediaPatchRequest` (api.ts) enthält optionale visibility/review_status.
    - `cd frontend && npx tsc --noEmit` ohne neue Fehler.
  </acceptance_criteria>
  <done>Release-Version-Media-PATCH unterstützt Sichtbarkeit/Reviewstatus additiv mit Audit (korrekte Owner-Tabelle); api.ts-Typ erweitert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReleaseVersionMediaReviewSection.tsx (+CSS) — Caller des erweiterten PATCH im Release-Drawer</name>
  <files>frontend/src/app/admin/.../ReleaseVersionMediaReviewSection.tsx, frontend/src/app/admin/.../ReleaseVersionMediaReviewSection.module.css</files>
  <read_first>
    - frontend/src/app/admin/.../ReleaseVersionMediaDrawerSummary.tsx (read-only Summary-Datenpipeline + Drawer-Host-Stelle; NICHT umbauen, daneben neuen Slot einhängen)
    - frontend/src/app/admin/fansubs/[id]/edit/ClaimManagementPanel.tsx (Gating/Primitivnutzung-Analog)
    - frontend/src/lib/api.ts (patchReleaseVersionMediaItem mit erweitertem ReleaseVersionMediaPatchRequest aus Task 1)
    - .planning/phases/78-leader-workspace-review-pflege/78-UI-SPEC.md (Pattern 3 Medien-Sichtbarkeit/Reviewstatus-Selektor; Copywriting „Sichtbarkeit", „Prüfstatus", „Owner-Zuordnung prüfen", „Änderungen speichern", Toast „Prüfstatus aktualisiert.")
  </read_first>
  <behavior>
    - Test: ohne can_edit_group (bzw. die im Drawer geltende Release-Capability) → null-Render (D-08).
    - Test: rendert pro Release-Version-Medium Select „Sichtbarkeit" (Intern/Öffentlich) und Select „Prüfstatus" (In Prüfung/Freigegeben/Abgelehnt/Archiviert/Entfernt) mit kanonischen value-Strings [D-05].
    - Test: „Änderungen speichern" ruft patchReleaseVersionMediaItem(versionId, mediaId, { visibility, review_status }).
    - Test: erkannte Owner-Inkonsistenz zeigt Badge „Owner-Zuordnung prüfen" — KEIN Owner-Edit-Feld (D-05).
  </behavior>
  <action>
    Erstelle ReleaseVersionMediaReviewSection.tsx als separaten Slot im Release-Drawer (read-only Summary bleibt). Props { versionId: number; capabilities: <Release-Capability-Shape>; media: <aus bestehender Summary-Pipeline> }. Gate: null-Render ohne die geltende Capability (D-08). Pro Medium: Card mit FormField „Sichtbarkeit" → Select (Optionen Intern=intern/Öffentlich=oeffentlich) und FormField „Prüfstatus" → Select (In Prüfung=in_pruefung/Freigegeben=freigegeben/Abgelehnt=abgelehnt/Archiviert=archiviert/Entfernt=entfernt) — deutsche Labels fix, value-Attribute = kanonische Enum-value-Strings. Bei erkannter Owner-Inkonsistenz Badge variant="warning" „Owner-Zuordnung prüfen" (nur Hinweis, kein Umhängen — D-05). Speichern-Button (variant="primary", „Änderungen speichern") ruft patchReleaseVersionMediaItem; Erfolg → Toast „Prüfstatus aktualisiert."; Fehler → Inline/ErrorState „Änderungen konnten nicht gespeichert werden.". Nur @/components/ui-Primitives (kein natives select/input/button). Colocated CSS. Datei ≤450 Zeilen; korrekte Umlaute.
    Hänge die Komponente an der ermittelten Drawer-Render-Stelle ein (neuer Slot neben der read-only Summary) — minimal-invasiv, keine Summary-Logik ändern.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx vitest run -t ReleaseVersionMediaReviewSection && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "patchReleaseVersionMediaItem" ReleaseVersionMediaReviewSection.tsx` matcht; kein nacktes `fetch(` — der erweiterte PATCH hat damit einen echten Caller (kein toter Key-Link).
    - `grep -v '^#' ReleaseVersionMediaReviewSection.tsx` enthält keine nativen `<select`/`<input`/`<button`/`<textarea`-Tags; Import aus `@/components/ui`.
    - Kein Owner-Edit-Feld (Source: nur Badge „Owner-Zuordnung prüfen", kein owner_type/owner_id-Setter).
    - Select-value-Attribute nutzen die kanonischen Enum-value-Strings; Datei ≤450 Zeilen; Umlaute korrekt.
    - `cd frontend && npx tsc --noEmit` ohne neue Fehler.
  </acceptance_criteria>
  <done>ReleaseVersionMediaReviewSection im Release-Drawer konsumiert den erweiterten PATCH; D-06 zweite Owner-Fläche geliefert; kein toter Backend-Key-Link.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Release-Version-Media-API | Untrusted Sichtbarkeits-/Status-Werte + mediaId queren hier; serverseitige Validierung + Permission Pflicht. |
| Handler → Owner-Tabelle | Mutation darf nur release_version_media treffen (Lock G); keine Owner-Umhängung. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-16 | Elevation of Privilege | Capability-Bypass auf Release-Media-PATCH | mitigate | Bestehende CanForReleaseVersionMedia(ActionReleaseVersionMediaUpdate) bleibt maßgeblich; Frontend-Gating ist UX (D-08). |
| T-78-17 | Tampering | Owner-Umhängen über Release-Media-Review | mitigate | Handler setzt nur visibility/review_status; Owner-Typ NICHT änderbar (D-05); owner_consistent nur lesend; Owner-Wechsel ist Phase 79. |
| T-78-18 | Repudiation | Review-Mutation ohne Audit | mitigate | Audit "release_version_media.visibility_updated" bei Erfolg; bestehender Deny-Audit "release_version_media.update.denied" bleibt (D-09). |
| T-78-19 | Tampering (Input) | Ungültige Sichtbarkeits-/Status-Enums | mitigate | Enum-Validierung serverseitig gegen kanonischen value-Satz (V5); ungültig → 400 ohne Mutation. |
| T-78-SC | Tampering | go/npm installs | accept | Keine neuen Pakete; nur vorhandene Frameworks + @/components/ui. |
</threat_model>

<verification>
- `cd backend && go build ./... && go test ./internal/handlers/... -run TestPatchReleaseVersionMedia` → grün.
- `cd backend && go test ./...` → keine Regressions.
- `cd frontend && npx tsc --noEmit && npx vitest run -t ReleaseVersionMediaReviewSection` → grün.
- `cd frontend && npx eslint <ReleaseVersionMediaReviewSection-Pfad>` → keine native-Element-Warnungen.
- Source-Gate: kein owner_type/owner_id-UPDATE im Release-Media-Review-Pfad.
</verification>

<success_criteria>
- Release-Version-Media-PATCH unterstützt additiv Sichtbarkeit/Reviewstatus (Lock K), gegated (D-08), auditiert (D-09), korrekte Owner-Tabelle (Lock G), kein Owner-Umhängen (D-05).
- ReleaseVersionMediaReviewSection im Release-Drawer konsumiert den erweiterten PATCH → kein toter Backend-Key-Link (WARNING 7 a gelöst).
- D-06 zweite Owner-Fläche (Release-Drawer) geliefert; Theme-Assets bleiben explizit deferred (siehe 78-04 Scope-Note).
</success_criteria>

<output>
Create `.planning/phases/78-leader-workspace-review-pflege/78-05-SUMMARY.md` when done
</output>
</content>
