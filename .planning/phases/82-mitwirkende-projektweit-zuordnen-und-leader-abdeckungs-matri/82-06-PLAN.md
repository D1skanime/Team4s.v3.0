---
phase: 82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri
plan: 06
type: execute
wave: 4
depends_on:
  - 82-05
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/page.test.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/ProjectCockpitBadges.test.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/DefaultCrewManager.test.tsx
autonomous: false
requirements:
  - D-04
  - D-05
  - D-07
  - D-10
  - D-11
  - D-12
  - D-13

must_haves:
  truths:
    - "Vitest-Tests für parseMainTab('anime-projekte') === 'releases' sind grün"
    - "Vitest-Tests für MAIN_TABS ohne 'anime-projekte' sind grün"
    - "Bestehende Vitest-Tests unter src/app/admin/fansubs bleiben grün"
    - "npm run typecheck ist fehlerfrei"
    - "Vitest-Test: ProjectCockpitBadges rendert KEINEN 'N Folgen'-Badge — kein episodeCount-Prop (per D-12)"
    - "Vitest-Test: AnimeContributionModal role_codes[] kann >1 Eintrag enthalten (per D-05)"
    - "Human-UAT: Tab 'Anime-Einblicke' ist aus der Tab-Leiste verschwunden"
    - "Human-UAT: ?tab=anime-projekte landet im releases-Tab"
    - "Human-UAT: Status-Badges sichtbar in Projektkarte"
    - "Human-UAT: Einblick-Block erscheint beim Aufklappen"
    - "Human-UAT: 'Standard-Team übernehmen'-Button ist sichtbar und liefert Feedback nach Klick"
    - "Human-UAT: Abdeckungs-Matrix (CoverageMatrix) ist sichtbar mit katalog-getriebenen Spalten"
  artifacts:
    - path: "frontend/src/app/admin/fansubs/[id]/edit/page.test.tsx"
      provides: "parseMainTab + MAIN_TABS Tests"
    - path: "frontend/src/app/admin/fansubs/[id]/edit/ProjectCockpitBadges.test.tsx"
      provides: "Badge-Render-Tests (ohne episodeCount-Test)"
    - path: "frontend/src/app/admin/fansubs/[id]/edit/DefaultCrewManager.test.tsx"
      provides: "Standard-Team-Tests"
    - path: ".planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-06-SUMMARY.md"
      provides: "Phase-Abschluss-Dokumentation"
  key_links:
    - from: "Vitest parseMainTab-Test"
      to: "page.tsx:parseMainTab"
      via: "Unit-Test-Import"
      pattern: "parseMainTab.*anime-projekte.*releases"
---

<objective>
Automatisierte Tests für die kritischen Routing-, Badge- und Standard-Team-Logiken schreiben und die Phase mit einem Human-UAT auf dem Dev-Server abschließen.

Purpose: Phase 82 ist erst abgeschlossen wenn die Routing-Umstellung testbar, die Standard-Team-Funktion live verifiziert und die Abdeckungs-Matrix sichtbar ist. Regressionen an bestehenden Fansub-Edit-Tests werden hier aufgedeckt.
Output: Grüne Vitest-Tests; Human-Verify-Freigabe; Phase-Abschluss-Commit.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-CONTEXT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-RESEARCH.md
@C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vitest-Tests — parseMainTab, MAIN_TABS, ProjectCockpitBadges, DefaultCrewManager</name>
  <files>
    frontend/src/app/admin/fansubs/[id]/edit/page.test.tsx
    frontend/src/app/admin/fansubs/[id]/edit/ProjectCockpitBadges.test.tsx
    frontend/src/app/admin/fansubs/[id]/edit/DefaultCrewManager.test.tsx
  </files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx (parseMainTab-Funktion und MAIN_TABS-Export-Status; falls parseMainTab exportiert ist direkt importieren)
    - frontend/src/app/admin/fansubs/[id]/edit/ProjectCockpitBadges.tsx (Props-Interface, Badge-Render-Logik; bestätigen dass episodeCount-Prop fehlt)
    - frontend/src/app/admin/fansubs/[id]/edit/DefaultCrewManager.tsx (Props-Interface, applyDefaultCrew-Aufruf)
    - frontend/vitest.config.ts (Test-Konfiguration; jsdom-Environment-Direktive wenn nötig)
    - Vorhandene test-Dateien unter frontend/src/app/admin/fansubs/ als Stil-Vorlage
    - C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-RESEARCH.md (Phase-Requirements Test-Map Z. 522–530)
  </read_first>
  <action>
    page.test.tsx (neue oder erweiternde Datei):
    1. Falls parseMainTab exportiert ist: direkt importieren und testen. Falls nicht: Testfall über URL-Rendering via Router-Mock schreiben oder parseMainTab in einer Hilfsdatei extrahieren und exportieren.
    2. Test-Fälle (RESEARCH Test-Map):
       - parseMainTab("anime-projekte") === "releases" (D-13)
       - MAIN_TABS enthält kein Element mit key === "anime-projekte" (D-13)
       - parseMainTab("releases") === "releases"
       - parseMainTab("basic") === "basic"
       - parseMainTab("rollen") === "collaboration" (bestehende Regression)
    3. Jsdom-Direktive wenn nötig: // @vitest-environment jsdom

    ProjectCockpitBadges.test.tsx (neue Datei):
    1. // @vitest-environment jsdom
    2. Import: ProjectCockpitBadges aus ./ProjectCockpitBadges; render aus @testing-library/react
    3. Test-Fälle:
       - contributionCount=0 → Badge "Mitwirkende fehlen" sichtbar
       - contributionCount=3 → Badge "Mitwirkende (3)" sichtbar
       - note=null (geladen, fehlt) → Badge "Einblick fehlt" sichtbar
       - note={...AnimeFansubProjectNote} → Badge "Einblick vorhanden" sichtbar
       - note=undefined (noch nicht geladen) → kein Einblick-Badge
       - KEIN Test für "N Folgen"-Badge (Props hat kein episodeCount; D-12 prohibit fake-status; RESEARCH Q1 kein episode_count)

    DefaultCrewManager.test.tsx (neue Datei):
    1. // @vitest-environment jsdom
    2. Mock: vi.mock('@/lib/api', ...) → listDefaultCrew gibt [] zurück; applyDefaultCrew gibt { applied_count: 2 } zurück
    3. Test-Fälle:
       - Rendern mit leerem crew → EmptyState "Noch kein Standard-Team konfiguriert." sichtbar
       - "Standard-Team übernehmen"-Button ist sichtbar (kein toter Button — per D-04)
       - Nach Klick auf "Standard-Team übernehmen": applyDefaultCrew wurde aufgerufen; Ergebnis-Text "2 Contributions angelegt" sichtbar

    Bestehende Tests nicht löschen oder umbenennen. Falls bestehende Tests an "anime-projekte" fehlschlagen: gezielt reparieren (MAIN_TABS-Mock oder parseMainTab-Erwartungen aktualisieren).
  </action>
  <verify>
    <automated>cd "C:/Users/admin/Documents/Team4s/frontend" && npx vitest run src/app/admin/fansubs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run src/app/admin/fansubs läuft ohne FAIL
    - Test "parseMainTab('anime-projekte') === 'releases'" ist PASS
    - Test "MAIN_TABS enthält kein 'anime-projekte'" ist PASS
    - Test "Badge 'Mitwirkende fehlen' bei contributionCount=0" ist PASS
    - Test "Badge 'Einblick fehlt' bei note=null" ist PASS
    - KEIN Test für "N Folgen"-Badge existiert (D-12, kein episode_count)
    - Test "Standard-Team übernehmen Button ruft applyDefaultCrew auf" ist PASS
    - Test "EmptyState sichtbar bei leerer Crew" ist PASS
    - Alle vorher grünen Tests unter src/app/admin/fansubs bleiben grün
    - npm run typecheck fehlerfrei
  </acceptance_criteria>
  <done>Automatisierte Tests grün; Routing, Badge-Logik und Standard-Team-Button verifiziert</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Human-UAT — Projekt-Cockpit live auf Dev-Server :3000</name>
  <what-built>
    Das vollständige Projekt-Cockpit im Tab „Anime & Veröffentlichungen":
    - Tab „Anime-Einblicke" aus der Leiste entfernt
    - ?tab=anime-projekte → leitet auf releases um
    - Status-Badges (Mitwirkende, Einblick) in Projektkarten-Kopfzeile (KEIN "N Folgen"-Badge)
    - Filterchips [Alle] [Mitwirkende fehlen] [Einblick fehlt] oberhalb der Liste
    - Einblick-Block im aufgeklappten Projektbereich (Zustandsmaschine)
    - CoverageMatrix (Abdeckungs-Matrix) sichtbar mit katalog-getriebenen Rollen-Spalten
    - DefaultCrewManager mit "Standard-Team übernehmen"-Button (verdrahtet, kein toter Button)
    - AnimeContributionModal zeigt App- und historische Member; Mehrfachrollen über Checkboxen
    - Migrationen 0104–0107 sind appliziert
  </what-built>
  <how-to-verify>
    Voraussetzung: Dev-Server läuft auf http://localhost:3000 (npm run dev im frontend-Verzeichnis) UND Backend mit aktualisierten Migrationen.

    1. URL aufrufen: http://localhost:3000/admin/fansubs/[ID]/edit (beliebige Fansub-Gruppe)
    2. Tab-Leiste prüfen: Kein Tab „Anime-Einblicke" sichtbar. Tab „Anime & Veröffentlichungen" existiert.
    3. Tab wechseln zu „Anime & Veröffentlichungen" — Liste der Anime-Projekte erscheint.
    4. Filterchip-Reihe sichtbar: [Alle] [Mitwirkende fehlen] [Einblick fehlt] (kein [Offene Punkte]-Chip).
    5. In der Projektkarte eines Anime: Status-Badges sichtbar (Mitwirkende-Badge, Einblick-Badge falls geladen). KEIN "N Folgen"-Badge.
    6. Projektkarte aufklappen (Klapptrigger): Abschnitt „Projekt-Einblick" erscheint. Wenn kein Einblick: EmptyState mit „Einblick hinzufügen". Wenn Einblick vorhanden: Text + „Einblick bearbeiten".
    7. URL direkt aufrufen: http://localhost:3000/admin/fansubs/[ID]/edit?tab=anime-projekte → landet im Tab „Anime & Veröffentlichungen" (kein 404, kein separater Einblicke-Tab).
    8. „Mitwirkende"-Button aufrufen: Modal öffnet; zeigt sowohl App-Member als auch historische Member; Rollen sind als Checkboxen dargestellt (Mehrfachauswahl möglich).
    9. Filterchip „Mitwirkende fehlen" klicken: Liste filtert auf Projekte ohne Mitwirkende.
    10. Filterchip „Einblick fehlt" klicken: Liste filtert auf Projekte ohne Einblick.
    11. Abdeckungs-Matrix sichtbar: Tabelle mit Projekten als Zeilen und Rollen als Spalten (katalog-getrieben).
    12. DefaultCrewManager / Standard-Team-Bereich sichtbar: Button „Standard-Team übernehmen" vorhanden. Klick auf Button → Rückmeldung erscheint (z.B. "N Contributions angelegt" oder Fehlermeldung). Kein toter Button, kein Seiteneffekt-loser Klick.
  </how-to-verify>
  <resume-signal>Tippe "approved" wenn alle 12 Prüfpunkte bestanden sind, oder beschreibe was abweicht.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| UAT-Umgebung | Manuelle Überprüfung auf Dev-Server mit echten Daten |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-06-01 | Tampering | Test-Mocks überschreiben echte Logik | mitigate | Mocks nur in Test-Kontext (vitest.mock); Produktionscode bleibt unverändert |
| T-82-06-SC | Tampering | npm/pip/cargo installs | accept | keine Package-Installs in diesem Plan |
</threat_model>

<verification>
1. `npx vitest run src/app/admin/fansubs` → alle Tests PASS, 0 FAIL
2. `npm run typecheck` → 0 Fehler
3. Human-UAT: alle 12 Prüfpunkte bestätigt
</verification>

<success_criteria>
- Alle Vitest-Tests grün (parseMainTab-Redirect, MAIN_TABS-Bereinigung, Badge-Render ohne episodeCount, Standard-Team-Button-Verdrahtung)
- Bestehende Fansub-Vitest-Tests bleiben grün
- Human-UAT bestanden: Cockpit funktioniert live auf :3000
- Standard-Team übernehmen gibt echtes Feedback (kein toter Button)
- Phase 82 abschlussbereit
</success_criteria>

<output>
Create `.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-06-SUMMARY.md` when done
</output>
