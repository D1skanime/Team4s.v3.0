---
phase: 82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri
plan: 03
type: execute
wave: 2
depends_on:
  - 82-02
files_modified:
  - frontend/src/types/fansub.ts
  - frontend/src/lib/api.ts
autonomous: true
requirements:
  - D-01
  - D-02
  - D-04
  - D-14

must_haves:
  truths:
    - "AnimeContribution.member_id (number) existiert; fansub_group_member_id ist entfernt (per D-01)"
    - "UpsertAnimeContributionRequest.member_id (number) existiert; fansub_group_member_id ist entfernt (per D-01)"
    - "UnifiedGroupMember-Interface existiert in frontend/src/types/fansub.ts mit member_id, display_name, source, has_app_account, group_roles (per D-02)"
    - "listUnifiedGroupMembers(fansubId)-Helper existiert in api.ts und ruft /unified-members auf (per D-02, D-14)"
    - "DefaultCrewEntry-Interface existiert in frontend/src/types/fansub.ts (per D-04)"
    - "listDefaultCrew(fansubId)-Helper existiert in api.ts (GET /default-crew) (per D-04, D-14)"
    - "upsertDefaultCrewEntry(fansubId, memberID, roleCode)-Helper existiert in api.ts (PUT /default-crew) (per D-04, D-14)"
    - "deleteDefaultCrewEntry(fansubId, memberID, roleCode)-Helper existiert in api.ts (DELETE /default-crew/:memberId/:roleCode) (per D-04, D-14)"
    - "applyDefaultCrew(fansubId, animeIds?)-Helper existiert in api.ts (POST /default-crew/apply) (per D-04, D-14)"
    - "npm run typecheck läuft fehlerfrei nach den Änderungen"
  artifacts:
    - path: "frontend/src/types/fansub.ts"
      provides: "AnimeContribution.member_id, UpsertAnimeContributionRequest.member_id, UnifiedGroupMember, DefaultCrewEntry"
    - path: "frontend/src/lib/api.ts"
      provides: "listUnifiedGroupMembers, listDefaultCrew, upsertDefaultCrewEntry, deleteDefaultCrewEntry, applyDefaultCrew"
  key_links:
    - from: "frontend/src/types/fansub.ts:AnimeContribution"
      to: "backend AnimeContributionDisplayRow"
      via: "json tag member_id"
      pattern: "member_id: number"
    - from: "frontend/src/lib/api.ts:listUnifiedGroupMembers"
      to: "/api/v1/admin/fansubs/:id/unified-members"
      via: "authorizedFetch"
      pattern: "unified-members"
    - from: "frontend/src/lib/api.ts:applyDefaultCrew"
      to: "/api/v1/admin/fansubs/:id/default-crew/apply"
      via: "authorizedFetch POST"
      pattern: "default-crew/apply"
---

<objective>
Frontend-Typ-Deklarationen und API-Helper auf die neue member_id-Semantik umstellen sowie alle nötigen Typen und Helper für das Standard-Team-Backend anlegen. AnimeContribution und UpsertAnimeContributionRequest umbenennen, UnifiedGroupMember und DefaultCrewEntry hinzufügen, alle fünf default-crew-Helper in api.ts anlegen.

Purpose: Plan 04 und 05 können gegen typsichere Interfaces bauen. Der „Standard-Team übernehmen"-Button (Plan 05) hat fertige API-Helper ohne raw-fetch-Implementierung. TypeScript-Compiler prüft alle Verwendungsstellen.
Output: Typsichere TypeScript-Interfaces + neue API-Helper; `npm run typecheck` grün.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-CONTEXT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-RESEARCH.md
@C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-PATTERNS.md
@C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-02-SUMMARY.md

<interfaces>
<!-- Relevante Typen aus fansub.ts (RESEARCH-Befund Z. 385–403) -->

export interface AnimeContribution {
  id: number;
  fansub_group_member_id: number;  // → umbenennen auf member_id
  member_display_name: string;
  anime_id: number;
  fansub_group_id: number;
  status: string;
  role_codes?: string[];
  release_version_id?: number | null;
  created_at: string;
}

export interface UpsertAnimeContributionRequest {
  fansub_group_member_id: number;  // → umbenennen auf member_id
  status: string;
  role_codes: string[];
  release_version_id?: number | null;
}

<!-- API-Helper-Muster aus api.ts (RESEARCH Z. 334–338 + PATTERNS authorizedFetch-Pattern) -->
export async function listGroupMembers(fansubId: number, authToken?: string)
// Vorlage für neuen listUnifiedGroupMembers-Helper
// Endpoint: GET /api/v1/admin/fansubs/${fansubId}/unified-members

<!-- UnifiedGroupMember aus RESEARCH.md (Go-Struct-Entsprechung) -->
type UnifiedGroupMember = {
  member_id: number;
  display_name: string;
  source: 'hist' | 'app';
  has_app_account: boolean;
  group_roles: string[];
}

<!-- DefaultCrewEntry — Backend-Struct aus Plan 82-02 -->
type DefaultCrewEntry = {
  id: number;
  fansub_group_id: number;
  member_id: number;
  role_code: string;
  created_by: number | null;
  created_at: string;
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frontend-Typen migrieren — AnimeContribution.member_id, UnifiedGroupMember, DefaultCrewEntry</name>
  <files>
    frontend/src/types/fansub.ts
  </files>
  <read_first>
    - frontend/src/types/fansub.ts (vollständig; AnimeContribution Z. 618–640, UpsertAnimeContributionRequest Z. 641–654, HistFansubGroupMember-Definition für Abgrenzung)
    - C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-RESEARCH.md (Abschnitt 6 Frontend-Typen-Migration, Z. 385–403)
  </read_first>
  <action>
    1. In AnimeContribution: Feld fansub_group_member_id: number entfernen, member_id: number hinzufügen (gleiche Position). Alle weiteren Felder unverändert lassen.

    2. In UpsertAnimeContributionRequest: Feld fansub_group_member_id: number → member_id: number.

    3. Neues Interface UnifiedGroupMember nach den bestehenden Contribution-Interfaces einfügen:
       export interface UnifiedGroupMember {
         member_id: number;
         display_name: string;
         source: 'hist' | 'app';
         has_app_account: boolean;
         group_roles: string[];
       }

    4. Neues Interface DefaultCrewEntry direkt nach UnifiedGroupMember einfügen:
       export interface DefaultCrewEntry {
         id: number;
         fansub_group_id: number;
         member_id: number;
         role_code: string;
         created_by: number | null;
         created_at: string;
       }

    5. Prüfen ob HistFansubGroupMember an anderen Stellen in fansub.ts auf fansub_group_member_id verweist — NICHT ändern (andere Tabelle, andere Semantik).

    6. Datei-Zeilenlimit 450: fansub.ts ist eine große Typen-Datei; nur additive Änderungen, kein Splitting nötig, aber prüfen ob Zeilen nach Änderungen ≤ 450.
  </action>
  <verify>
    <automated>cd "C:/Users/admin/Documents/Team4s/frontend" && npm run typecheck 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - AnimeContribution hat Feld member_id: number (kein fansub_group_member_id)
    - UpsertAnimeContributionRequest hat Feld member_id: number (kein fansub_group_member_id)
    - UnifiedGroupMember ist als exportiertes Interface in fansub.ts definiert
    - DefaultCrewEntry ist als exportiertes Interface in fansub.ts definiert mit allen Feldern: id, fansub_group_id, member_id, role_code, created_by, created_at
    - npm run typecheck meldet keine neuen Fehler durch die Typen-Änderungen
    - grep -n "fansub_group_member_id" frontend/src/types/fansub.ts ergibt 0 Treffer in AnimeContribution + UpsertAnimeContributionRequest
  </acceptance_criteria>
  <done>AnimeContribution und UpsertAnimeContributionRequest nutzen member_id; UnifiedGroupMember und DefaultCrewEntry exportiert</done>
</task>

<task type="auto">
  <name>Task 2: API-Helper — listUnifiedGroupMembers + default-crew-Helper in api.ts anlegen</name>
  <files>
    frontend/src/lib/api.ts
  </files>
  <read_first>
    - frontend/src/lib/api.ts (Z. 7241–7266 für listGroupMembers-Vorlage; Z. 7518–7578 für listAnimeContributions-Vorlage; upsertAnimeContribution für Mutation-Vorlage; DELETE-Helper-Muster für deleteDefaultCrewEntry)
    - C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-PATTERNS.md (authorizedFetch-Pattern Z. 775–790)
  </read_first>
  <action>
    1. Neuen Helper listUnifiedGroupMembers nach dem bestehenden listGroupMembers-Helper einfügen:
       - Signatur: export async function listUnifiedGroupMembers(fansubId: number, authToken?: string): Promise<UnifiedGroupMember[]>
       - Endpoint: GET /api/v1/admin/fansubs/${fansubId}/unified-members
       - Pattern: authorizedFetch mit getApiBaseUrl(); response.ok prüfen; bei Fehler ApiError werfen; response.json() → .data array extrahieren

    2. Default-Crew-Helper als Block nach listUnifiedGroupMembers einfügen (D-04, D-14):
       a. listDefaultCrew(fansubId, authToken?): Promise<DefaultCrewEntry[]>
          GET /api/v1/admin/fansubs/${fansubId}/default-crew → .data[]
       b. upsertDefaultCrewEntry(fansubId, memberID, roleCode, authToken?): Promise<void>
          PUT /api/v1/admin/fansubs/${fansubId}/default-crew, Body: { member_id: memberID, role_code: roleCode }
       c. deleteDefaultCrewEntry(fansubId, memberID, roleCode, authToken?): Promise<void>
          DELETE /api/v1/admin/fansubs/${fansubId}/default-crew/${memberID}/${roleCode}
       d. applyDefaultCrew(fansubId, animeIds?: number[], authToken?): Promise<{ applied_count: number }>
          POST /api/v1/admin/fansubs/${fansubId}/default-crew/apply, Body: { anime_ids: animeIds ?? [] }
       Alle vier nutzen ausschließlich authorizedFetch (D-14); kein raw fetch.

    3. upsertAnimeContribution-Helper: Parameter fansub_group_member_id → member_id im Request-Body. Signatur: data: UpsertAnimeContributionRequest (Interface bereits aktualisiert in Task 1).

    4. Kein neuer manueller Auth/Bearer-Fetch — ausschließlich authorizedFetch nutzen (D-14).

    5. Import: UnifiedGroupMember + DefaultCrewEntry aus @/types/fansub (type-import). api.ts ist sehr groß — nur additive Änderungen + gezielte Inline-Umbenennung. Kein Restrukturieren.
  </action>
  <verify>
    <automated>cd "C:/Users/admin/Documents/Team4s/frontend" && npm run typecheck 2>&1 | grep -E "error TS" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - listUnifiedGroupMembers-Funktion existiert in api.ts (grep "export.*listUnifiedGroupMembers" → Treffer)
    - listDefaultCrew-Funktion existiert (grep "export.*listDefaultCrew" → Treffer)
    - upsertDefaultCrewEntry-Funktion existiert (grep "export.*upsertDefaultCrewEntry" → Treffer)
    - deleteDefaultCrewEntry-Funktion existiert (grep "export.*deleteDefaultCrewEntry" → Treffer)
    - applyDefaultCrew-Funktion existiert (grep "export.*applyDefaultCrew" → Treffer)
    - Alle fünf Funktionen rufen authorizedFetch auf (kein raw fetch)
    - upsertAnimeContribution sendet member_id im Request-Body statt fansub_group_member_id
    - npm run typecheck läuft fehlerfrei für die geänderten Helper
    - grep "fansub_group_member_id" frontend/src/lib/api.ts ergibt 0 Treffer in den Contribution-Helpers
  </acceptance_criteria>
  <done>Alle API-Helper typsicher und über authorizedFetch; default-crew-Block komplett (per D-04, D-14)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| TypeScript-Typen → API-Calls | Typen sind reine Compile-Zeit-Garantie; Runtime-Validierung liegt im Backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-03-01 | Tampering | member_id im UpsertAnimeContributionRequest | mitigate | Backend-seitiger Cross-Group-Guard (Plan 82-02) ist die maßgebliche Sicherung; TypeScript-Typ verhindert falsch-typisierte Aufrufe nur at compile time |
| T-82-03-02 | Information Disclosure | listUnifiedGroupMembers exposing App-Member-Daten | mitigate | authorizedFetch trägt Token; Backend-Permission-Guard (Plan 82-02) blockt Unberechtigte |
| T-82-03-03 | Tampering | applyDefaultCrew — Contributions für falsche Gruppe anlegen | mitigate | Backend-Permission-Guard MembersManage + fansubGroupID aus URL-Param (Plan 82-02); Frontend sendet korrekte fansubId aus State |
| T-82-03-SC | Tampering | npm/pip/cargo installs | accept | keine Package-Installs in diesem Plan |
</threat_model>

<verification>
Nach beiden Tasks:
1. `cd frontend && npm run typecheck` → keine neuen TypeScript-Fehler
2. `grep -n "member_id" frontend/src/types/fansub.ts | grep -E "AnimeContribution|UpsertAnime"` → zeigt member_id-Felder
3. `grep -n "fansub_group_member_id" frontend/src/types/fansub.ts` → kein Treffer in Contribution-Interfaces
4. `grep -n "export.*listUnifiedGroupMembers\|export.*listDefaultCrew\|export.*upsertDefaultCrewEntry\|export.*deleteDefaultCrewEntry\|export.*applyDefaultCrew" frontend/src/lib/api.ts` → 5 Treffer
</verification>

<success_criteria>
- AnimeContribution.member_id und UpsertAnimeContributionRequest.member_id typisiert
- UnifiedGroupMember + DefaultCrewEntry exportierte Interfaces
- listUnifiedGroupMembers + alle vier default-crew-API-Helper über authorizedFetch
- npm run typecheck ohne neue Fehler
</success_criteria>

<output>
Create `.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-03-SUMMARY.md` when done
</output>
