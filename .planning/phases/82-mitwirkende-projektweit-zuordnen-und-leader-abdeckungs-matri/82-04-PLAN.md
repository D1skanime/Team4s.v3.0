---
phase: 82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri
plan: 04
type: execute
wave: 3
depends_on:
  - 82-03
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/ProjectCockpitBadges.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNoteWorkspace.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNotesSection.tsx
autonomous: true
requirements:
  - D-10
  - D-11
  - D-12
  - D-17

must_haves:
  truths:
    - "ProjectCockpitBadges-Komponente rendert Badge variant=danger 'Mitwirkende fehlen' wenn contributionCount=0 (per D-11, D-12)"
    - "ProjectCockpitBadges-Komponente rendert Badge variant=neutral 'Mitwirkende ({N})' wenn contributionCount > 0 (per D-11)"
    - "ProjectCockpitBadges-Komponente rendert Badge variant=success 'Einblick vorhanden' / variant=warning 'Einblick fehlt' je nach note-Prop (per D-11)"
    - "AnimeProjectNoteWorkspace implementiert Zustandsmaschine NICHT_GELADEN→LADEN→VORHANDEN|FEHLT|FEHLER (per D-10, UI-SPEC)"
    - "AnimeProjectNoteWorkspace nutzt RichTextRenderer aus @/components/editor für Lesen (per D-17)"
    - "AnimeProjectNoteWorkspace nutzt RichTextEditor aus @/components/editor für Bearbeiten (per D-17)"
    - "AnimeProjectNoteWorkspace nutzt getAnimeFansubProjectNote + upsertAnimeFansubProjectNote aus api.ts (per D-14)"
    - "AnimeProjectNotesSection.tsx hat KEINE nativen <select>/<button> mehr (per CLAUDE.md, UI-SPEC)"
    - "Alle drei Dateien ≤ 450 Zeilen"
  artifacts:
    - path: "frontend/src/app/admin/fansubs/[id]/edit/ProjectCockpitBadges.tsx"
      provides: "Status-Badge-Reihe; < 450 Zeilen"
    - path: "frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNoteWorkspace.tsx"
      provides: "Inline-Einblick-Block mit Zustandsmaschine; < 450 Zeilen"
    - path: "frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNotesSection.tsx"
      provides: "Altfall migriert: native <select>/<button> ersetzt durch @/components/ui-Primitives"
  key_links:
    - from: "AnimeProjectNoteWorkspace"
      to: "api.ts:getAnimeFansubProjectNote"
      via: "useEffect onExpand lazy load"
      pattern: "getAnimeFansubProjectNote\\("
    - from: "ProjectCockpitBadges"
      to: "@/components/ui Badge"
      via: "variant prop"
      pattern: "<Badge variant="
---

<objective>
Drei Frontend-Komponenten erstellen bzw. migrieren: (1) neue ProjectCockpitBadges-Komponente für Status-Badges, (2) neue AnimeProjectNoteWorkspace-Komponente für den Inline-Einblick-Block, (3) Pflichtmigration von AnimeProjectNotesSection.tsx von nativen HTML-Elementen auf UI-Primitives.

Purpose: Diese Komponenten sind die Bausteine des Projekt-Cockpits. Plan 05 verdrahtet sie in page.tsx — ohne diese Bausteine kann die Verdrahtung nicht beginnen.
Output: Drei React-Komponenten, typsicher, unter 450 Zeilen, ausschließlich @/components/ui-Primitives.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-CONTEXT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-UI-SPEC.md
@C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-PATTERNS.md
@C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-03-SUMMARY.md

<interfaces>
<!-- Schlüssel-Imports aus Analog-Dateien -->

Aus @/components/ui (Pflicht-Imports, verifiziert aus AnimeContributionModal.tsx):
  import { Badge, Button, Card, EmptyState, ErrorState, FormField, SectionHeader, Select } from '@/components/ui'

Aus @/components/editor (RichText, verifiziert aus AnimeProjectNotesSection.tsx):
  import { RichTextEditor, RichTextRenderer } from '@/components/editor'

Aus @/lib/api (Einblick-Helper, verifiziert Z. 6899–7006):
  import { getAnimeFansubProjectNote, upsertAnimeFansubProjectNote, deleteAnimeFansubProjectNote } from '@/lib/api'
  import { ApiError } from '@/lib/api'

Aus @/types/fansubNotes (DTO-Interface, verifiziert):
  import type { AnimeFansubProjectNote } from '@/types/fansubNotes'

Aus FansubEdit.module.css (CSS-Klassen, verifiziert):
  .chipRow — flex-wrap für Badge-Reihe; gap: var(--space-2)
  .fansubEditAnimeReleaseHeaderRow — Kopfzeile

ProjectCockpitBadges Props (aus PATTERNS.md Z. 635–658):
  type Props = {
    contributionCount: number
    note: AnimeFansubProjectNote | null | undefined  // undefined = noch nicht geladen
    episodeCount?: number
  }

AnimeProjectNoteWorkspace Props:
  type Props = {
    fansubId: number
    animeId: number
    expanded: boolean  // lazy load trigger
  }

Zustandsmaschine (UI-SPEC Z. 234–251):
  type NoteLoadState = 'idle' | 'loading' | 'present' | 'missing' | 'editing' | 'error'
  // Übergänge: expanded=true → loading; note→present|missing; Bearbeiten→editing
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProjectCockpitBadges.tsx + AnimeProjectNotesSection.tsx migrieren</name>
  <files>
    frontend/src/app/admin/fansubs/[id]/edit/ProjectCockpitBadges.tsx
    frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNotesSection.tsx
  </files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNotesSection.tsx (vollständig; native <select> Z. 216–240, native <button> Z. 115/121, gesamte Struktur für Migrations-Überblick)
    - frontend/src/app/admin/fansubs/[id]/edit/FansubEdit.module.css (.chipRow, vorhandene Klassen)
    - frontend/src/components/ui/index.ts (oder äquivalente Exports-Datei; welche Primitives existieren)
    - C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-PATTERNS.md (ProjectCockpitBadges-Pattern Z. 609–658, AnimeProjectNotesSection-Pattern Z. 510–605)
    - C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-UI-SPEC.md (Badge-Farben, Copywriting Contract)
  </read_first>
  <action>
    ProjectCockpitBadges.tsx — neue Datei anlegen ('use client'):
    - Props: contributionCount: number, note: AnimeFansubProjectNote | null | undefined, episodeCount?: number
    - Import: Badge aus @/components/ui; AnimeFansubProjectNote aus @/types/fansubNotes; CSS-Modul von FansubEdit.module.css
    - Render: div mit className={styles.chipRow} und gap: var(--space-2) (NICHT 0.45rem Legacy-Wert):
      - episodeCount !== undefined → Badge variant="muted" Text "{episodeCount} Folgen" (UI-SPEC Copywriting)
      - contributionCount > 0 → Badge variant="neutral" Text "Mitwirkende ({contributionCount})"
      - contributionCount === 0 → Badge variant="danger" Text "Mitwirkende fehlen"
      - note !== undefined: note !== null → Badge variant="success" "Einblick vorhanden"; note === null → Badge variant="warning" "Einblick fehlt"
      - note === undefined (noch nicht geladen) → kein Einblick-Badge (lazy, D-12)
    - Datei-Limit: < 50 Zeilen, weit unter 450.

    AnimeProjectNotesSection.tsx — Migration:
    - native <button> an Z. 115 und Z. 121 → Button-Import aus @/components/ui; Z. 115: variant="ghost" size="sm"; Z. 121: variant="danger" size="sm" mit disabled={deleting}
    - native <select> für Sichtbarkeit (Z. ~218–224) → FormField + Select aus @/components/ui; label="Sichtbarkeit"; onChange-Handler beibehalten
    - native <select> für Status (Z. ~225–240) → FormField + Select aus @/components/ui; label="Status"; onChange-Handler beibehalten
    - RichTextEditor und RichTextRenderer bleiben unverändert (bereits korrekt)
    - Import-Block anpassen: Button, FormField, Select aus @/components/ui hinzufügen; native button/select nicht mehr direkt genutzt
    - Datei-Limit nach Änderungen prüfen: wenn > 450 Zeilen → AnimeProjectNoteForm in _Form.tsx auslagern
    - ESLint-Warnings für native Elemente dürfen nach Migration nicht mehr existieren
  </action>
  <verify>
    <automated>cd "C:/Users/admin/Documents/Team4s/frontend" && npm run typecheck 2>&1 | grep -E "AnimeProjectNotesSection|ProjectCockpitBadges" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - ProjectCockpitBadges.tsx existiert und exportiert eine default oder named Komponente
    - ProjectCockpitBadges verwendet ausschließlich Badge aus @/components/ui (grep "native" → kein Treffer)
    - AnimeProjectNotesSection.tsx hat keine nativen <select> mehr (grep "<select" → 0 Treffer)
    - AnimeProjectNotesSection.tsx hat keine nativen <button> mehr (grep "<button" → 0 Treffer)
    - Beide Dateien importieren aus @/components/ui (grep "from '@/components/ui'" → Treffer)
    - npm run typecheck ohne neue Fehler durch diese Dateien
    - Beide Dateien ≤ 450 Zeilen (wc -l oder Editor-Zeilenanzeige)
    - Badge-Texte stimmen mit UI-SPEC Copywriting Contract überein: "Mitwirkende fehlen", "Mitwirkende ({N})", "Einblick vorhanden", "Einblick fehlt", "{N} Folgen"
  </acceptance_criteria>
  <done>ProjectCockpitBadges bereit für Einbau in page.tsx; AnimeProjectNotesSection Altfall-Migration abgeschlossen</done>
</task>

<task type="auto">
  <name>Task 2: AnimeProjectNoteWorkspace.tsx — neue Einblick-Inline-Komponente</name>
  <files>
    frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNoteWorkspace.tsx
  </files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNotesSection.tsx (vollständig nach Task 1 Migration — Zustandsmaschine, API-Aufrufe, RichTextEditor-Integration als Vorlage)
    - frontend/src/types/fansubNotes.ts (AnimeFansubProjectNote-Interface, UpsertAnimeFansubProjectNoteRequest)
    - C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-PATTERNS.md (AnimeProjectNoteWorkspace-Pattern Z. 662–717)
    - C:/Users/admin/Documents/Team4s/.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-UI-SPEC.md (Zustandsmaschine Z. 234–251, Aufgeklappt-Abschnitt 2 Z. 161–169, Copywriting Contract)
  </read_first>
  <action>
    AnimeProjectNoteWorkspace.tsx — neue Datei ('use client'):
    Props: fansubId: number, animeId: number, expanded: boolean

    Zustandsmaschine: type NoteLoadState = 'idle' | 'loading' | 'present' | 'missing' | 'editing' | 'error'
    State: const [noteState, setNoteState] = useState<NoteLoadState>('idle') + const [note, setNote] = useState<AnimeFansubProjectNote | null>(null) + const [saving, setSaving] = useState(false) + const [saveError, setSaveError] = useState<string | null>(null)

    Lazy-Load-Effect (expanded als Trigger, D-12):
    useEffect(() => {
      if (!expanded || noteState !== 'idle') return
      let cancelled = false
      setNoteState('loading')
      getAnimeFansubProjectNote(fansubId, animeId)
        .then((n) => { if (!cancelled) { setNote(n); setNoteState(n ? 'present' : 'missing') } })
        .catch(() => { if (!cancelled) setNoteState('error') })
      return () => { cancelled = true }
    }, [expanded, fansubId, animeId, noteState])

    Render-Zweige:
    - idle/loading: SectionHeader "Projekt-Einblick" + Ladeindikator (einfacher Text "Wird geladen…" oder Spinner)
    - error: SectionHeader + ErrorState mit Button "Erneut versuchen" (setzt noteState → 'idle')
    - missing: SectionHeader + EmptyState title="Projekt-Einblick fehlt" description="Noch kein Einblick für dieses Projekt vorhanden." action=<Button variant="primary" size="sm" onClick={() => setNoteState('editing')}>Einblick hinzufügen</Button>
    - present (nicht editing): SectionHeader + RichTextRenderer bodyHtml={note.bodyHtml} + Button variant="ghost" size="sm" "Einblick bearbeiten" → setNoteState('editing')
    - editing: SectionHeader + RichTextEditor mit RichTextEditor value={ensureRichTextValue(form.bodyJson)} onChange + Button variant="primary" size="sm" "Einblick speichern" (onClick handleSave) + Button variant="ghost" size="sm" "Abbrechen" + saveError-Anzeige

    handleSave: setSaving(true) → upsertAnimeFansubProjectNote(fansubId, animeId, payload) → setNote(saved) + setNoteState('present') → catch setSaveError(...)

    Imports: alle aus @/components/ui (Button, EmptyState, ErrorState, SectionHeader); RichTextEditor + RichTextRenderer aus @/components/editor; getAnimeFansubProjectNote + upsertAnimeFansubProjectNote aus @/lib/api; AnimeFansubProjectNote aus @/types/fansubNotes; FansubEdit.module.css für CSS-Klassen.

    ensureRichTextValue: kleine Hilfsfunktion die null/undefined auf leeres TipTap-Dokument mapped (Vorlage aus AnimeProjectNotesSection.tsx übernehmen).

    Datei-Limit: Zielgröße ~150–200 Zeilen, weit unter 450. KEIN nativer <textarea> oder <button> — ausschließlich Primitives.
  </action>
  <verify>
    <automated>cd "C:/Users/admin/Documents/Team4s/frontend" && npm run typecheck 2>&1 | grep "AnimeProjectNoteWorkspace" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - AnimeProjectNoteWorkspace.tsx existiert und exportiert eine Komponente (export function oder export default)
    - Zustandsmaschine hat alle Zustände: idle, loading, present, missing, editing, error
    - Lazy-Load-Effect ist abhängig von expanded (useEffect(..., [expanded, ...]))
    - Keine nativen <textarea>, <button>, <select> in der Datei (grep → 0 Treffer)
    - RichTextRenderer aus @/components/editor wird für 'present'-Zustand genutzt
    - RichTextEditor aus @/components/editor wird für 'editing'-Zustand genutzt
    - EmptyState für 'missing'-Zustand mit korrekten Texten aus UI-SPEC Copywriting Contract
    - getAnimeFansubProjectNote und upsertAnimeFansubProjectNote werden aus @/lib/api importiert (kein manueller fetch)
    - npm run typecheck ohne neue Fehler durch diese Datei
    - Datei ≤ 450 Zeilen
  </acceptance_criteria>
  <done>AnimeProjectNoteWorkspace mit Zustandsmaschine; lazy; UI-Primitives; bereit für Einbau in page.tsx (Plan 05)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Browser → upsertAnimeFansubProjectNote | Rich-Text-Inhalt fließt vom Browser über api.ts zum Backend |
| AnimeProjectNoteWorkspace-State | Fehlerlose Zustandsmaschine verhindert Race Conditions beim lazy Load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-04-01 | Tampering | upsertAnimeFansubProjectNote — Rich-Text-Content | mitigate | Backend sanitiert TipTap-JSON via bluemonday-Policy (bereits implementiert, Phase 41); Frontend nutzt nur autorisierten api.ts-Seam |
| T-82-04-02 | Tampering | AnimeProjectNoteWorkspace — cancelled-Flag fehlt | mitigate | useEffect cleanup setzt cancelled=true → kein setState nach Unmount (Race Condition verhindert) |
| T-82-04-03 | Information Disclosure | getAnimeFansubProjectNote — unauthorisierter Aufruf | mitigate | authorizedFetch trägt Token; Backend-Permission-Guard ist maßgebliche Sicherung |
| T-82-04-SC | Tampering | npm/pip/cargo installs | accept | keine Package-Installs in diesem Plan |
</threat_model>

<verification>
Nach beiden Tasks:
1. `npm run typecheck` → keine neuen Fehler
2. `grep -n "<select\|<button\|<textarea" frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNotesSection.tsx` → 0 Treffer
3. `grep -n "<select\|<button\|<textarea" frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNoteWorkspace.tsx` → 0 Treffer
4. `grep -n "from '@/components/ui'" frontend/src/app/admin/fansubs/[id]/edit/ProjectCockpitBadges.tsx` → Treffer (Badge)
5. `wc -l frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNoteWorkspace.tsx` → ≤ 450
</verification>

<success_criteria>
- ProjectCockpitBadges rendert korrekte Badge-Varianten für alle Status
- AnimeProjectNoteWorkspace implementiert vollständige Zustandsmaschine mit lazy load
- AnimeProjectNotesSection.tsx hat keine nativen HTML-Elemente mehr wo Primitives existieren
- Alle drei Dateien ≤ 450 Zeilen, TypeScript-Fehlerfreiheit
</success_criteria>

<output>
Create `.planning/phases/82-mitwirkende-projektweit-zuordnen-und-leader-abdeckungs-matri/82-04-SUMMARY.md` when done
</output>
