---
phase: 10-create-tags-and-metadata-card-refactor
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - database/migrations/0019_add_reference_data_tables.up.sql
  - database/migrations/0019_add_reference_data_tables.down.sql
  - database/migrations/0022_add_junction_tables.up.sql
  - database/migrations/0022_add_junction_tables.down.sql
  - backend/internal/repository/admin_content.go
  - backend/internal/repository/admin_content_anime_metadata.go
  - backend/internal/repository/admin_content_anime_delete.go
  - backend/internal/handlers/admin_content_handler.go
  - backend/internal/handlers/admin_content_anime.go
  - backend/internal/models/admin_content.go
  - backend/cmd/server/admin_routes.go
  - backend/internal/repository/admin_content_test.go
  - backend/internal/handlers/admin_content_test.go
autonomous: true
requirements: [TAG-01, TAG-03]
must_haves:
  truths:
    - "The database persists tags through normalized `tags` and `anime_tags` tables instead of a comma-separated inline string."
    - "Create and patch writes replace tag links authoritatively with deduped, trimmed, sorted values."
    - "Tag links are removed during anime delete cleanup, and admins can fetch tag suggestions through a dedicated token endpoint."
  artifacts:
    - path: "database/migrations/0019_add_reference_data_tables.up.sql"
      provides: "Reference-table definition for `tags`"
      contains: "CREATE TABLE tags"
    - path: "database/migrations/0022_add_junction_tables.up.sql"
      provides: "Junction-table definition for `anime_tags`"
      contains: "CREATE TABLE anime_tags"
    - path: "backend/internal/repository/admin_content.go"
      provides: "Authoritative normalized tag write and token-query behavior"
      contains: "replaceAuthoritativeAnimeTags"
    - path: "backend/internal/repository/admin_content_anime_metadata.go"
      provides: "Create and patch normalization logic for tag lists"
      contains: "normalizeTagList"
    - path: "backend/internal/handlers/admin_content_anime.go"
      provides: "Admin route behavior for create and tag-token listing"
      contains: "ListTagTokens"
  key_links:
    - from: "backend/internal/handlers/admin_content_anime.go"
      to: "backend/internal/repository/admin_content.go"
      via: "tag token listing"
      pattern: "ListTagTokens"
    - from: "backend/internal/repository/admin_content_anime_metadata.go"
      to: "backend/internal/repository/admin_content.go"
      via: "authoritative tag replace path used by create and patch"
      pattern: "replaceAuthoritativeAnimeTags"
    - from: "backend/internal/repository/admin_content_anime_delete.go"
      to: "database/migrations/0022_add_junction_tables.up.sql"
      via: "delete cleanup for anime_tags"
      pattern: "anime_tags"
---

<objective>
Implement the backend schema, repository writes, delete cleanup, and admin token-listing path for normalized anime tags.

Purpose: The create-page tags UI is not real until tags can be stored, reloaded, queried for suggestions, and cleaned up exactly like genres at the persistence layer.
Output: Migration coverage, repository normalization helpers, create and patch persistence, delete cleanup, and backend tests for tags.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-create-tags-and-metadata-card-refactor/10-CONTEXT.md
@.planning/phases/10-create-tags-and-metadata-card-refactor/10-UI-SPEC.md
@backend/internal/repository/admin_content.go
@backend/internal/repository/admin_content_anime_metadata.go
@backend/internal/repository/admin_content_anime_delete.go
@backend/internal/handlers/admin_content_anime.go
@backend/internal/models/admin_content.go
@database/migrations/0019_add_reference_data_tables.up.sql
@database/migrations/0022_add_junction_tables.up.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing backend coverage for normalized tags and cleanup</name>
  <files>backend/internal/repository/admin_content_test.go, backend/internal/handlers/admin_content_test.go</files>
  <read_first>@backend/internal/repository/admin_content.go @backend/internal/repository/admin_content_anime_metadata.go @backend/internal/repository/admin_content_anime_delete.go @backend/internal/handlers/admin_content_anime.go @.planning/phases/10-create-tags-and-metadata-card-refactor/10-CONTEXT.md</read_first>
  <behavior>
    - Test 1: create normalization trims, dedupes, and sorts tag input before persistence.
    - Test 2: patch writes replace prior `anime_tags` links authoritatively.
    - Test 3: tag-token listing returns `{ name, count }` rows through a dedicated handler path.
    - Test 4: deleting an anime removes `anime_tags` links together with the anime lifecycle cleanup.
  </behavior>
  <acceptance_criteria>`go test ./internal/repository ./internal/handlers -run "Test.*Tag"` fails before implementation and includes assertions against normalized stored names, cleared `anime_tags` rows after patch and delete, and a successful `GET /admin/tags` handler response.</acceptance_criteria>
  <action>Add repository tests that create anime metadata with repeated tag values such as `[" Mecha ", "mecha", "Classic"]` and assert the stored tag set becomes exactly `["Classic", "Mecha"]` or the chosen canonical casing once. Add patch tests proving an updated tag set replaces old links instead of appending. Add delete tests proving `anime_tags` rows disappear when the owning anime is deleted. Add handler tests for `ListTagTokens` that assert the JSON payload shape `{ data: [{ name, count }] }` and that the route does not reuse the genre endpoint or response name.</action>
  <verify>
    <automated>cd backend && go test ./internal/repository ./internal/handlers -run "Test.*Tag" -count=1</automated>
  </verify>
  <done>Backend tests pin the normalized tag schema, authoritative persistence, token listing, and delete cleanup before implementation lands.</done>
</task>

<task type="auto">
  <name>Task 2: Implement migrations, repository helpers, and admin tag-token listing</name>
  <files>database/migrations/0019_add_reference_data_tables.up.sql, database/migrations/0019_add_reference_data_tables.down.sql, database/migrations/0022_add_junction_tables.up.sql, database/migrations/0022_add_junction_tables.down.sql, backend/internal/repository/admin_content.go, backend/internal/repository/admin_content_anime_metadata.go, backend/internal/repository/admin_content_anime_delete.go, backend/internal/handlers/admin_content_handler.go, backend/internal/handlers/admin_content_anime.go, backend/internal/models/admin_content.go, backend/cmd/server/admin_routes.go</files>
  <read_first>@database/migrations/0019_add_reference_data_tables.up.sql @database/migrations/0022_add_junction_tables.up.sql @backend/internal/repository/admin_content.go @backend/internal/repository/admin_content_anime_metadata.go @backend/internal/repository/admin_content_anime_delete.go @backend/internal/handlers/admin_content_anime.go</read_first>
  <acceptance_criteria>`database/migrations/0019_add_reference_data_tables.up.sql` defines `tags (id, name, created_at, updated_at)` or the repo's equivalent reference-table shape with a unique `name`; `database/migrations/0022_add_junction_tables.up.sql` defines `anime_tags (anime_id, tag_id)` with a uniqueness constraint; `backend/internal/repository/admin_content.go` contains `replaceAuthoritativeAnimeTags` and `ListTagTokens`; `backend/internal/repository/admin_content_anime_metadata.go` contains `normalizeTagList` and applies it during create and patch; `backend/cmd/server/admin_routes.go` registers `GET /admin/tags`; `backend/internal/repository/admin_content_anime_delete.go` clears `anime_tags` during anime delete.</acceptance_criteria>
  <action>Extend the reference-data and junction-table migrations so they create and drop normalized `tags` and `anime_tags` tables parallel to `genres` and `anime_genres`. In `backend/internal/repository/admin_content.go`, add `replaceAuthoritativeAnimeTags(ctx, tx, animeID, tags []string)` plus a `buildAuthoritativeTagTokensQuery` and `ListTagTokens(ctx, query, limit)` that mirror the genre token flow while reading from `anime_tags` and `tags`. In `backend/internal/repository/admin_content_anime_metadata.go`, add `TagsSet bool`, `Tags []string`, and a `normalizeTagList([]string)` helper that trims whitespace, collapses internal spacing, dedupes case-insensitively, and sorts the final slice; apply it in both create and patch write builders so tags are authoritative. In `backend/internal/repository/admin_content_anime_delete.go`, clear `anime_tags` links in both hybrid and V2 delete paths. In `backend/internal/handlers/admin_content_anime.go`, add a `ListTagTokens` handler mirroring `ListGenreTokens`, and register it as `GET /api/v1/admin/tags` in `backend/cmd/server/admin_routes.go`.</action>
  <verify>
    <automated>cd backend && go test ./internal/repository ./internal/handlers ./cmd/server -count=1</automated>
  </verify>
  <done>The backend can persist normalized tags, list tag suggestions, and clean up tag links safely across create, patch, and delete flows.</done>
</task>

</tasks>

<verification>
Tags are durable backend metadata instead of draft-only values. The schema, repository layer, and admin handler surface all treat tags analogously to genres.
</verification>

<success_criteria>
- `tags` and `anime_tags` exist as normalized tables in migrations.
- Create and patch write normalized tags authoritatively through repository helpers.
- Delete cleanup removes `anime_tags` rows for deleted anime.
- The backend exposes `GET /api/v1/admin/tags` with the expected token payload.
</success_criteria>

<output>
After completion, create `.planning/phases/10-create-tags-and-metadata-card-refactor/10-02-SUMMARY.md`
</output>
