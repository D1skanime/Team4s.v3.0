---
phase: 28-segment-playback-sources-from-jellyfin-runtime
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - frontend/src/types/admin.ts
  - frontend/src/lib/api.ts
  - frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditorPage.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseSegments.ts
autonomous: true
requirements:
  - P28-SC1
  - P28-SC2
  - P28-SC3
  - P28-SC4
must_haves:
  truths:
    - "The segment editor makes episode-version/Jellyfin playback the obvious default path and stops implying that upload is required before segment work can begin."
    - "Frontend sends one explicit release-variant playback context through the existing segment API seam instead of inventing a separate playback DTO."
    - "Runtime-aware UX helps early, but the UI still behaves honestly when duration_seconds is null."
  artifacts:
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx"
      provides: "Default-playback messaging, fallback-selection controls, and runtime-aware validation feedback"
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx"
      provides: "Table/panel playback summaries bound to hydrated playback metadata"
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseSegments.ts"
      provides: "Release-variant-aware segment requests for list/create/update/delete/upload flows"
---

<objective>
Bring the segment editor UI and typed API contract in line with the new backend playback rules.

Purpose: Phase 28 should feel like "segments on the current release file" with optional fallback, not "upload a file first, then maybe play something else."
Output: Release-variant-aware API helpers, default-playback UI copy, and runtime-aware editor behavior for known and unknown durations.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/28-phase-28-segment-playback-sources-from-jellyfin-runtime-segmente-nutzen-standardmaessig-episode-version-jellyfin-stream-als-playback-quelle-zeitgrenzen-kommen-aus-release-variants-duration-seconds-upload-bleibt-optionaler-fallback/28-CONTEXT.md
@.planning/phases/28-phase-28-segment-playback-sources-from-jellyfin-runtime-segmente-nutzen-standardmaessig-episode-version-jellyfin-stream-als-playback-quelle-zeitgrenzen-kommen-aus-release-variants-duration-seconds-upload-bleibt-optionaler-fallback/28-RESEARCH.md
@frontend/src/types/admin.ts
@frontend/src/lib/api.ts
@frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditorPage.tsx
@frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx
@frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx
@frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseSegments.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread release-variant playback context through the existing segment API contract</name>
  <files>frontend/src/types/admin.ts, frontend/src/lib/api.ts, frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditorPage.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseSegments.ts</files>
  <read_first>
    - frontend/src/types/admin.ts
    - frontend/src/lib/api.ts
    - frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditorPage.tsx
    - frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseSegments.ts
    - .planning/phases/28-phase-28-segment-playback-sources-from-jellyfin-runtime-segmente-nutzen-standardmaessig-episode-version-jellyfin-stream-als-playback-quelle-zeitgrenzen-kommen-aus-release-variants-duration-seconds-upload-bleibt-optionaler-fallback/28-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Segment list/create/update calls include the current release-variant anchor from the episode-version editor context.
    - Test 2: The typed response contract exposes hydrated `playback_source_kind`, `playback_release_variant_id`, `playback_jellyfin_item_id`, `playback_source_label`, and `playback_duration_seconds` without a second shadow interface.
    - Test 3: Upload/delete/reload keeps the same release-variant context so the refreshed segment row reflects the backend-resolved default playback immediately.
  </behavior>
  <action>Treat the episode-version editor's current `version.id` as the canonical release-variant anchor for this flow, thread it through `EpisodeVersionEditorPage` into `useReleaseSegments` as an explicitly named `releaseVariantId`, and include it on segment list/create/update/delete/upload requests through one consistent contract field or query parameter named `release_variant_id`. Update `AdminThemeSegment`, segment create/patch input types, and helper signatures to consume the hydrated `playback_*` metadata already coming from the backend rather than inventing frontend-only playback inference.</action>
  <acceptance_criteria>
    - `Select-String -Path frontend/src/lib/api.ts -Pattern "releaseVariant|playback|segments"` returns matches in segment list/create/update helpers.
    - `Select-String -Path frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseSegments.ts -Pattern "releaseVariant|versionId|reload"` returns matches.
    - `Select-String -Path frontend/src/types/admin.ts -Pattern "playback_source_kind|playback_release_variant_id|playback_duration_seconds"` returns matches in `AdminThemeSegment`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>The frontend uses one explicit release-variant playback context and one typed segment DTO.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reframe the editor around default episode-version playback and explicit uploaded fallback</name>
  <files>frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx</files>
  <read_first>
    - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx
    - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx
    - .planning/phases/28-phase-28-segment-playback-sources-from-jellyfin-runtime-segmente-nutzen-standardmaessig-episode-version-jellyfin-stream-als-playback-quelle-zeitgrenzen-kommen-aus-release-variants-duration-seconds-upload-bleibt-optionaler-fallback/28-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: A new segment can be created and saved with the default playback path visible even when no upload exists.
    - Test 2: The panel makes uploaded segment files an explicit fallback choice and does not imply upload automatically becomes the active playback source.
    - Test 3: The table and side panel show the resolved playback status from `playback_source_kind`/`playback_source_label`, separate from raw provenance fields.
  </behavior>
  <action>Replace the current source-choice copy and panel arrangement so the happy path reads `Episode-Version / Jellyfin-Stream (Standard)` first. Keep uploaded asset handling available, but phrase it as `hochgeladener Fallback` or equivalent explicit override, not as the primary required source. Render resolved playback metadata in both the table and the edit panel using the hydrated `playback_*` fields, and keep any legacy `jellyfin_theme` option visually secondary if it must remain for compatibility.</action>
  <acceptance_criteria>
    - `Select-String -Path frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx -Pattern "Standard|Fallback|playback|Episode-Version|Jellyfin"` returns matches.
    - `Select-String -Path frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx -Pattern "playback_source_kind|playback_source_label|uploaded|episode_version"` returns matches.
    - `cd frontend && npm.cmd run build` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm.cmd run build</automated>
  </verify>
  <done>The UI now describes and displays the real default playback behavior instead of the old upload-first mental model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Runtime-aware UX for known-duration clamps and null-duration fallback</name>
  <files>frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditorPage.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx</files>
  <read_first>
    - frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditorPage.tsx
    - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx
    - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx
    - .planning/phases/28-phase-28-segment-playback-sources-from-jellyfin-runtime-segmente-nutzen-standardmaessig-episode-version-jellyfin-stream-als-playback-quelle-zeitgrenzen-kommen-aus-release-variants-duration-seconds-upload-bleibt-optionaler-fallback/28-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: When `durationSeconds` or `segment.playback_duration_seconds` is known, the end-time input clamps early and shows the real runtime limit in UI copy.
    - Test 2: Backend validation errors for over-runtime values surface as actionable field or panel feedback instead of a generic failure toast.
    - Test 3: When duration is null, the timeline and editor still work with honest copy explaining that no real runtime bound is available yet.
  </behavior>
  <action>Use the resolved playback duration as the first runtime authority in the segment editor and fall back to page-level `version.duration_seconds` only when the segment playback DTO does not provide one. Keep the existing clamp-on-blur/save behavior, but add clear helper text for runtime-known and runtime-null cases and map backend validation errors for `start_time`/`end_time` into visible form feedback. Ensure the timeline component receives the best available real duration and still renders a nullable-safe layout when no duration exists.</action>
  <acceptance_criteria>
    - `Select-String -Path frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx -Pattern "durationSeconds|playback_duration_seconds|clamp|runtime"` returns matches.
    - `Select-String -Path frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx -Pattern "duration|runtime|Fehler|end_time|start_time"` returns matches.
    - `cd frontend && npx tsc --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>The editor helps with runtime limits when known and stays honest and usable when runtime is null.</done>
</task>

</tasks>

<verification>
Build the frontend and confirm the editor now shows default episode-version playback, explicit upload fallback, and runtime-aware feedback using the hydrated backend playback DTO.
</verification>

<success_criteria>
The frontend contract and UX match the backend playback model and let admins work on segments without upload-first friction.
</success_criteria>

<output>
After completion, create `.planning/phases/28-phase-28-segment-playback-sources-from-jellyfin-runtime-segmente-nutzen-standardmaessig-episode-version-jellyfin-stream-als-playback-quelle-zeitgrenzen-kommen-aus-release-variants-duration-seconds-upload-bleibt-optionaler-fallback/28-02-SUMMARY.md`
</output>
