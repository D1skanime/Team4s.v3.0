---
phase: 161-jellyfin-12-kompatibilitaet-und-mediasource-import
plan: "05"
type: execute
wave: 4
depends_on: ["161-04"]
files_modified: ["backend/internal/repository/jellyfin_source_repository.go","backend/internal/repository/episode_import_repository.go","backend/internal/repository/episode_import_repository_release_helpers.go","backend/internal/repository/episode_version_repository_write_helpers.go","backend/internal/repository/episode_import_source_integration_test.go","backend/internal/repository/jellyfin_source_fixture_test.go","backend/internal/handlers/admin_content_handler.go","backend/internal/handlers/admin_episode_import.go","backend/internal/handlers/admin_episode_import_validation.go","backend/internal/handlers/admin_episode_import_test.go"]
autonomous: true
requirements: ["P161-SOURCE","P161-METADATA","P161-ITEMS","P161-REGRESSION"]
must_haves:
  truths:
    - "Applying a reviewed import saves container, tracks and source identity atomically on the existing item-bound release graph."
    - "Repeating the same import creates no duplicate versions, variants, streams or crew credits."
    - "Tampered, missing or stale source selection produces no graph writes and cannot cross anime/fansub ownership."
  artifacts:
    - path: "backend/internal/repository/jellyfin_source_repository.go"
      provides: "Revalidate import selections and persist coherent source snapshots"
    - path: "backend/internal/repository/episode_import_repository.go"
      provides: "Revalidate import selections and persist coherent source snapshots"
  key_links:
    - from: "backend/internal/handlers/admin_episode_import.go"
      to: "backend/internal/repository/episode_import_repository.go"
      via: "server-revalidated candidate passed into existing transaction"
    - from: "backend/internal/repository/jellyfin_source_repository.go"
      to: "stream_sources.metadata"
      via: "jellyfin_source namespace linked to actual provider/item identity"
---

<objective>
Revalidate import selections and persist coherent source snapshots.

Purpose: Applying a reviewed import saves container, tracks and source identity atomically on the existing item-bound release graph.
Output: Implemented behavior, focused regression tests and 161-05-SUMMARY.md.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-CONTEXT.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-RESEARCH.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-MEDIASOURCE-RESEARCH.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-PATTERNS.md

<interfaces>
Plan 02 defines models.JellyfinSourceSnapshot; Plan 04 defines reviewed media_source_id. Add EpisodeImportRepository.GetJellyfinSourceBindings(ctx, itemIDs []string) (map[string]models.JellyfinSourceSnapshot, error) as one batch read and extend adminEpisodeImportRepository accordingly. Shared repository helper upserts provider/item/url plus only the metadata.jellyfin_source namespace using the existing transaction and preserves unrelated JSON keys. Existing UNIQUE(provider_type, external_id) is unchanged. Dedicated test database team4s_phase117_test_161 is available; reuse testsupport.OpenPhase117Postgres with an explicit TEAM4S_PHASE117_TEST_DSN and its unique-schema guard.
</interfaces>

Execution is on /home/d1sk/team4s via SSH, with tests/builds in Compose containers. No worktrees. Execute plans sequentially in the shared canonical checkout even when technical waves permit independent work. Check git status and Compose first; preserve unrelated edits. For every task read its edited files once, then the listed analogs. Do not reread the entire research corpus or invent a new architecture.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared snapshot persistence and guarded integration fixture</name>
  <files>backend/internal/repository/jellyfin_source_repository.go, backend/internal/repository/episode_import_repository.go, backend/internal/repository/episode_version_repository_write_helpers.go, backend/internal/repository/jellyfin_source_fixture_test.go</files>
  <read_first>
  @backend/internal/repository/jellyfin_source_repository.go
  @backend/internal/repository/episode_import_repository.go
  @backend/internal/repository/episode_version_repository_write_helpers.go
  @backend/internal/repository/jellyfin_source_fixture_test.go
  @backend/internal/repository/episode_version_public_integration_test.go
  @backend/internal/testsupport/phase117_postgres.go
  @backend/internal/testsupport/phase106_postgres.go
  @backend/internal/repository/release_stream_repository_helpers.go
  @database/migrations/0037_add_release_decomposition_tables.up.sql
  </read_first>
  <behavior>
    - One binding batch returns only requested actual item IDs and existing namespace snapshots.
    - Upsert preserves unrelated JSON keys and provider/item uniqueness; an incomplete refresh may retain complete tracks only for the same resolved binding (exact stored ID or permitted unique stored-path recovery). A different item/source with omitted MediaStreams fails before mutation.
    - The fixture rejects application DSNs, uses one unique schema and cleans only that schema.
  </behavior>
  <action>Per D-06/D-10/D-13, extend the existing stream-source upsert seam with typed namespace handling and a batched binding read. Replace the duplicate ensureStreamSourceID/upsert SQL mechanics with the shared helper, keeping each caller's ownership validation. A complete snapshot may be retained across an incomplete response only for the same resolved binding. If new item/source B omits MediaStreams, reject the apply with a clear source conflict before any graph mutation; never copy A's tracks to B. Test complete A to incomplete B with zero writes and rollback, plus incomplete same-A retaining A. Lock the source row when changing its snapshot and reject a concurrently changed binding inconsistent with the server-resolved selection. Do not encode source ID into external_id, add track rows to release_streams or create a migration. Build one reusable isolated fixture on the existing testsupport guard with the canonical Apply prerequisites, including metadata JSONB; never fall back to DATABASE_URL.</action>
  <verify><automated>docker exec -e TEAM4S_PHASE117_TEST_DSN -w /app team4sv30-backend go test ./internal/repository -run 'TestJellyfinSourceRepository' -count=1</automated></verify>
  <done>One repository seam owns snapshot read/write and its fixture proves JSON namespace isolation; skipped DB tests are reported as unexecuted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write source metadata atomically in create and repeat import</name>
  <files>backend/internal/repository/episode_import_repository.go, backend/internal/repository/episode_import_repository_release_helpers.go, backend/internal/repository/episode_import_source_integration_test.go</files>
  <read_first>
  @backend/internal/repository/episode_import_repository.go
  @backend/internal/repository/episode_import_repository_release_helpers.go
  @backend/internal/repository/episode_import_source_integration_test.go
  @backend/internal/repository/episode_import_repository_apply.go
  @backend/internal/repository/episode_import_repository_release_helpers_test.go
  @backend/internal/repository/episode_import_repository_autoassign_test.go
  </read_first>
  <behavior>
    - Source A persists its own filename/container/codecs/duration/audio/subtitles, even when top-level fields and source B conflict.
    - Repeat apply preserves graph counts, source binding, group links and crew seeding behavior; reordered A is the same source.
    - Wrong anime, duplicate candidate ID, conflicting source, or omitted confirmed candidate rolls back with no mutation.
  </behavior>
  <action>Per D-07/D-08/D-10/D-12, update both create and existing-variant branches to persist chosen-source container and technical fields together with the source snapshot and key-free URL inside the existing transaction. Do not infer the container from an operator title. Keep provider/item and canonical episode/version/group ownership checks, locking and normalized stream idempotency. Reject duplicate or absent confirmed candidates instead of map overwrite or synthesized item-only candidates. Reapply cannot silently change A to B; compare the current locked snapshot against the server-resolved binding and its allowed same-path recovery. Add executable DB assertions for metadata, rollback and all graph counts, including a no-snapshot existing item whose explicit apply establishes the initial resolvable binding without a background backfill.</action>
  <verify><automated>docker exec -e TEAM4S_PHASE117_TEST_DSN -w /app team4sv30-backend go test ./internal/repository -run 'Test.*(EpisodeImportSource|JellyfinSourceRepository)' -count=1</automated></verify>
  <done>Create/repeat/rollback integration tests prove source coherence and idempotency on the guarded test database; live rows are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rehydrate reviewed items before any import write</name>
  <files>backend/internal/handlers/admin_content_handler.go, backend/internal/handlers/admin_episode_import.go, backend/internal/handlers/admin_episode_import_validation.go, backend/internal/handlers/admin_episode_import_test.go</files>
  <read_first>
  @backend/internal/handlers/admin_content_handler.go
  @backend/internal/handlers/admin_episode_import.go
  @backend/internal/handlers/admin_episode_import_validation.go
  @backend/internal/handlers/admin_episode_import_test.go
  @backend/internal/handlers/jellyfin_source_batch.go
  @backend/internal/handlers/jellyfin_media_source.go
  @frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.ts
  </read_first>
  <behavior>
    - Preview produces one coherent candidate per actual item and preserves existing coverage filtering.
    - Apply batches only confirmed item IDs, revalidates selected source and anime folder/series ownership, and ignores client technical metadata/URLs as authority.
    - Missing, foreign, stale or tampered source produces 400/409/502 as documented and zero repository Apply calls; skipped rows trigger no source request.
  </behavior>
  <action>Per D-05/D-06/D-07/D-12/D-13, wire the shared projection into loadEpisodeImportMediaCandidates and mapping creation. Before Apply, load existing bindings once, hydrate confirmed actual item IDs through Plan 02's bounded batch, verify ownership using canonical anime series/folder context and exact returned IDs, resolve the source and compare it with the reviewed media_source_id. Replace posted technical candidate data with the server result. Preserve all existing canonical episode and fansub validation. Keep stale selection separate from unavailable Jellyfin; no writes on either error. Record preview's unchanged collection request count and apply's deliberate increase from zero to ceil(N/100) provider requests plus one binding batch; no per-item source/stream fan-out.</action>
  <verify><automated>docker exec -w /app team4sv30-backend go test ./internal/handlers -run 'Test.*(EpisodeImport|11eyes)' -count=1</automated></verify>
  <done>Import uses the shared resolver and server-owned metadata; request-count and zero-write error tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

Existing browser-to-Team4s authorization, Team4s-to-Jellyfin requests, repository data ownership and isolated test execution remain explicit boundaries. No new public endpoint, credentials or schema is required.

| Threat ID | STRIDE | Component | Disposition | Mitigation |
|---|---|---|---|---|
| T161-05-1 | T | Untrusted import payload | mitigate | Rehydrate confirmed IDs in batches; verify canonical anime/folder ownership and reviewed source before Apply. |
| T161-05-2 | T | Concurrent source changes | mitigate | Lock source/variant rows and compare expected binding before atomically writing snapshot and columns. |
| T161-05-3 | E | Database fixtures | mitigate | Dedicated database-name guard and unique schema; no app DATABASE_URL or live reset/backfill. |
</threat_model>

<verification>
Run every task's automated gate in the canonical Linux environment. Before DB tests, the executor/root reads the existing Compose PostgreSQL credentials through docker inspect into memory, constructs a URL-escaped DSN for team4s_phase117_test_161, and sets TEAM4S_PHASE117_TEST_DSN only in the host subprocess environment passed to docker exec -e TEAM4S_PHASE117_TEST_DSN. Use the existing Python subprocess/config-reading seam, not a new application helper. Never print or persist the DSN, password or key. Integration tests must actually execute in guarded unique schemas; a skipped required test is a failed verification gate, not persistence proof. Run scoped checks and git diff --check. Record known global failures separately against docs/audits/2026-09-15-jellyfin12/checks-baseline.json and extended-checks-baseline.json. Plan 09 runs integrated live and broad gates. Do not modify application rows or provider libraries to satisfy a test.
</verification>

<success_criteria>
- Applying a reviewed import saves container, tracks and source identity atomically on the existing item-bound release graph.
- Repeating the same import creates no duplicate versions, variants, streams or crew credits.
- Tampered, missing or stale source selection produces no graph writes and cannot cross anime/fansub ownership.
- Every task gate passes or a concrete blocking discrepancy is reported; no unsupported completion claim.
- Only this plan's intended files are committed; source ownership and other providers remain intact.
</success_criteria>

<output>
After completion, create .planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-05-SUMMARY.md with changes, tests, exact request/query counts where relevant, commit IDs and unresolved evidence limits.
</output>
