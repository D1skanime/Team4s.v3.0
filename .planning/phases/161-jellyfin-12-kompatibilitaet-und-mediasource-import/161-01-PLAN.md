---
phase: 161-jellyfin-12-kompatibilitaet-und-mediasource-import
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["backend/internal/jellyfin/request.go","backend/internal/jellyfin/request_test.go","backend/internal/handlers/jellyfin_client.go","backend/internal/handlers/anime_backdrops_client.go","backend/internal/handlers/group_assets_jellyfin.go","backend/internal/handlers/jellyfin12_metadata_test.go","backend/internal/handlers/jellyfin_client_series.go","backend/internal/handlers/group_assets_jellyfin_test.go"]
autonomous: true
requirements: ["P161-AUTH","P161-API","P161-ITEMS"]
must_haves:
  truths:
    - "Metadata, artwork and group-library reads authenticate under Jellyfin 12 without putting the server key in a URL."
    - "Group roots are direct children; recursive group media enumerates every page and exact item lookup cannot return another item."
    - "Fanart and Emby authentication behavior remains unchanged."
  artifacts:
    - path: "backend/internal/jellyfin/request.go"
      provides: "Authenticated Jellyfin requests and correct metadata queries"
    - path: "backend/internal/jellyfin/request_test.go"
      provides: "Authenticated Jellyfin requests and correct metadata queries"
  key_links:
    - from: "backend/internal/handlers/jellyfin_client.go"
      to: "backend/internal/jellyfin/request.go"
      via: "all configured Jellyfin JSON requests"
    - from: "backend/internal/handlers/group_assets_jellyfin.go"
      to: "/Items"
      via: "explicit recursion, exact IDs and complete paging"
---

<objective>
Authenticated Jellyfin requests and correct metadata queries.

Purpose: Metadata, artwork and group-library reads authenticate under Jellyfin 12 without putting the server key in a URL.
Output: Implemented behavior, focused regression tests and 161-01-SUMMARY.md.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-CONTEXT.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-RESEARCH.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-MEDIASOURCE-RESEARCH.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-PATTERNS.md

<interfaces>
Existing AdminContentHandler.fetchJellyfinJSON(ctx context.Context, apiPath string, query url.Values, target any) (int, error) owns status classification and decoding. GroupAssetsHandler.listPagedGroupItems(ctx, baseValues, pageSize) and cloneURLValues already implement page iteration. Introduce narrow internal/jellyfin helpers BuildURL(baseURL, apiPath string, query url.Values) (*url.URL, error), NewRequest(ctx context.Context, method, targetURL, baseURL, apiKey string) (*http.Request, error), and Do(client *http.Client, req *http.Request, baseURL string) (*http.Response, error). Preserve an existing client timeout/transport without mutating shared client fields. All subsequent plans use these helpers; do not create a second auth implementation.
</interfaces>

Execution is on /home/d1sk/team4s via SSH, with tests/builds in Compose containers. No worktrees. Execute plans sequentially in the shared canonical checkout even when technical waves permit independent work. Check git status and Compose first; preserve unrelated edits. For every task read its edited files once, then the listed analogs. Do not reread the entire research corpus or invent a new architecture.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and prove the shared request boundary</name>
  <files>backend/internal/jellyfin/request.go, backend/internal/jellyfin/request_test.go</files>
  <read_first>
  @backend/internal/jellyfin/request.go
  @backend/internal/jellyfin/request_test.go
  @backend/internal/handlers/jellyfin_client.go
  @backend/internal/handlers/anime_backdrops_client.go
  @backend/internal/handlers/group_assets_jellyfin.go
  @backend/internal/handlers/fansub_admin.go
  </read_first>
  <behavior>
    - A synthetic key appears only in Authorization: MediaBrowser Token="..."; URL, returned errors and captured diagnostics contain no key.
    - Configured-origin requests preserve base-path prefixes and query values. A different origin, HTTPS downgrade or cross-origin redirect never receives the key.
    - Concurrent calls do not mutate a shared http.Client; cancellation and caller timeouts still work.
  </behavior>
  <action>Per D-01/D-02/D-11, extract the repeated Jellyfin URL/request policy into the specified internal package, without moving provider ownership, decoders or application status mapping. Reject malformed credential values containing CR/LF, remove legacy Jellyfin auth query keys from configured-origin URLs, and restrict redirect forwarding to the same normalized scheme/host/effective port. A stored foreign fallback remains uncredentialed and cannot pass through the authenticated constructor. Sanitize transport errors, including synthetic key-bearing url.Error values, before any wrapper logs them; preserve errors.Is cancellation/timeout classification. Keep authorization-header formatting in this one helper. Do not edit Fanart, the Emby-only playback proxy, or legacy redaction patterns.</action>
  <verify><automated>docker exec -w /app team4sv30-backend go test ./internal/jellyfin -count=1</automated></verify>
  <done>The shared transport's origin, redirect, cancellation and secret tests pass using httptest; no provider key is carried by a URL.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire existing JSON and artwork HTTP owners</name>
  <files>backend/internal/handlers/jellyfin_client.go, backend/internal/handlers/anime_backdrops_client.go, backend/internal/handlers/group_assets_jellyfin.go, backend/internal/handlers/jellyfin12_metadata_test.go</files>
  <read_first>
  @backend/internal/handlers/jellyfin_client.go
  @backend/internal/handlers/anime_backdrops_client.go
  @backend/internal/handlers/group_assets_jellyfin.go
  @backend/internal/handlers/jellyfin12_metadata_test.go
  @backend/internal/handlers/jellyfin_client_test.go
  @backend/internal/handlers/admin_content_episode_version_editor_context_test.go
  </read_first>
  <behavior>
    - Admin, Anime JSON/status and GroupAssets requests capture the same modern auth header.
    - Existing non-UTF8 response normalization remains correct; 401, 5xx, malformed JSON and transport failures preserve their existing status/error behavior without leaking the key.
    - The editor still returns its documented degraded context on Jellyfin failure.
  </behavior>
  <action>Per D-02/D-11, replace URL-auth construction and direct execution in the four JSON/status call sites with Plan 01's helpers. Preserve each owner's decoder, response body closure, status handling, timeouts and cache boundaries. Leave the bodies' unrelated logic intact and keep quick260915-dws graceful editor enrichment failure behavior. Add real request-capture tests across the owners in one focused Jellyfin-12 test file rather than source-string assertions.</action>
  <verify><automated>docker exec -w /app team4sv30-backend go test ./internal/handlers -run 'Test.*(Jellyfin12Metadata|NormalizeJellyfin|EpisodeVersionEditorContext)' -count=1</automated></verify>
  <done>All four metadata/status HTTP execution sites use the shared request policy; editor outage tests remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Correct proven GetItems semantics and exact lookup</name>
  <files>backend/internal/handlers/group_assets_jellyfin.go, backend/internal/handlers/group_assets_jellyfin_test.go, backend/internal/handlers/jellyfin_client_series.go, backend/internal/handlers/jellyfin12_metadata_test.go</files>
  <read_first>
  @backend/internal/handlers/group_assets_jellyfin.go
  @backend/internal/handlers/group_assets_jellyfin_test.go
  @backend/internal/handlers/jellyfin_client_series.go
  @backend/internal/handlers/jellyfin12_metadata_test.go
  @.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-CALLERS.md
  @docs/audits/2026-09-15-jellyfin12/openapi-used-endpoints.json
  @backend/internal/handlers/jellyfin_search_test.go
  @backend/internal/handlers/jellyfin_intake_preview_test.go
  </read_first>
  <behavior>
    - Direct group root lookup sends Recursive=false and cannot select nested project roots; child enumeration remains Recursive=true.
    - A 201-plus-item fixture, partial final page, empty page and inconsistent repeated page terminate without lost or duplicated IDs.
    - Group detail and exact series-ID reads reject absent or wrong IDs; bounded title search limits retain their intended semantics.
  </behavior>
  <action>Per D-04/D-05/D-13, set explicit root and child recursion, route listSubgroupChildren through listPagedGroupItems, and retain its sorting. Make the existing paging seam reject a non-progressing/repeated page or inconsistent total instead of claiming completeness; use IDs to detect duplicates. Replace getGroupItemDetails's documented-but-failing key-only /Items/{id} read with /Items?Ids=&lt;id&gt; and require an exact result. Remove invalid ItemFields values identified by the live schema from touched series/group requests while preserving normally returned base fields. Remove exact-series lookup's first-item fallback. Preserve the deliberate bounded search limit. Do not describe /Items/{id} as removed, and do not invent a UserId.</action>
  <verify><automated>docker exec -w /app team4sv30-backend go test ./internal/handlers -run 'Test.*(GroupAssets|Jellyfin12Metadata|JellyfinSearch|JellyfinIntake)' -count=1</automated></verify>
  <done>Fixture IDs and totals prove complete group paging and exact lookups; all changed field names exist in the recorded Jellyfin-12 ItemFields enum.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

Existing browser-to-Team4s authorization, Team4s-to-Jellyfin requests, repository data ownership and isolated test execution remain explicit boundaries. No new public endpoint, credentials or schema is required.

| Threat ID | STRIDE | Component | Disposition | Mitigation |
|---|---|---|---|---|
| T161-01-1 | I | Jellyfin request and redirect boundary | mitigate | Attach credentials only at the configured origin; reject foreign/downgrade redirects; sanitize error paths. |
| T161-01-2 | D | GetItems pagination | mitigate | Detect non-progress, duplicate IDs and inconsistent totals; preserve request cancellation. |
| T161-01-3 | T | Exact item lookup | mitigate | Require requested IDs and never choose the first unrelated result. |
</threat_model>

<verification>
Run every task's automated gate in the canonical Linux environment. Integration tests require the explicit dedicated test DSN and must actually execute; skip is not persistence proof. Run scoped checks and git diff --check. Record known global failures separately against docs/audits/2026-09-15-jellyfin12/checks-baseline.json and extended-checks-baseline.json. Plan 09 runs integrated live and broad gates. Do not modify application rows or provider libraries to satisfy a test.
</verification>

<success_criteria>
- Metadata, artwork and group-library reads authenticate under Jellyfin 12 without putting the server key in a URL.
- Group roots are direct children; recursive group media enumerates every page and exact item lookup cannot return another item.
- Fanart and Emby authentication behavior remains unchanged.
- Every task gate passes or a concrete blocking discrepancy is reported; no unsupported completion claim.
- Only this plan's intended files are committed; source ownership and other providers remain intact.
</success_criteria>

<output>
After completion, create .planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-01-SUMMARY.md with changes, tests, exact request/query counts where relevant, commit IDs and unresolved evidence limits.
</output>
