---
phase: 161-jellyfin-12-kompatibilitaet-und-mediasource-import
plan: "08"
type: execute
wave: 7
depends_on: ["161-07"]
files_modified: ["backend/internal/repository/release_detail_public_repository_helpers.go", "backend/internal/repository/release_detail_public_source_integration_test.go", "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseDetailHero.test.tsx", "backend/internal/repository/theme_segment_playback_resolution_integration_test.go"]
autonomous: true
requirements: ["P161-SOURCE","P161-METADATA","P161-REGRESSION"]
must_haves:
  truths:
    - "Container, audio language and all subtitle tracks on a public release describe one selected variant/source."
    - "Unknown language remains unknown while a real subtitle track and its codec/flags remain visible."
    - "Existing no-snapshot releases remain readable without triggering Jellyfin requests."
    - "Guarded integration tests prove that release playback, theme playback and the public projection use the same selected source."
  artifacts:
    - path: "backend/internal/repository/release_detail_public_repository_helpers.go"
      provides: "Project technical release facts from the same selected source"
    - path: "backend/internal/repository/release_detail_public_source_integration_test.go"
      provides: "Project technical release facts from the same selected source"
  key_links:
    - from: "backend/internal/repository/release_detail_public_repository_helpers.go"
      to: "backend/internal/repository/release_variant_source_repository.go"
      via: "one selected variant/source for all technical fields"
    - from: "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseDetailHero.test.tsx"
      to: "frontend/src/types/releaseDetail.ts"
      via: "existing public contract display"
---

<objective>
Project technical release facts from the same selected source.

Purpose: Container, audio language and all subtitle tracks on a public release describe one selected variant/source.
Output: Implemented behavior, focused regression tests and 161-08-SUMMARY.md.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-CONTEXT.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-RESEARCH.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-MEDIASOURCE-RESEARCH.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-PATTERNS.md

<interfaces>
Keep loadReleaseTechnical(ctx, releaseVersionID) (publicReleaseTechnical, []PublicReleaseSubtitleTrack, error). PublicReleaseSubtitleTrack already has Language *string, Label string, Format *string, Forced bool, Default bool. Public and focused contracts were aligned in Plan 04. Reuse Plan 07's selected variant/source query and Plan 02's typed snapshot. Existing ReleaseDetailHero.formatSubtitleTracks already renders label/language/format and no layout changes are needed.
</interfaces>

Execution is on /home/d1sk/team4s via SSH, with tests/builds in Compose containers. No worktrees. Execute plans sequentially in the shared canonical checkout even when technical waves permit independent work. Check git status and Compose first; preserve unrelated edits. For every task read its edited files once, then the listed analogs. Do not reread the entire research corpus or invent a new architecture.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Read scalar and track facts from one selected variant/source</name>
  <files>backend/internal/repository/release_detail_public_repository_helpers.go, backend/internal/repository/release_detail_public_source_integration_test.go</files>
  <read_first>
  @backend/internal/repository/release_detail_public_repository_helpers.go
  @backend/internal/repository/release_detail_public_source_integration_test.go
  @backend/internal/repository/release_variant_source_repository.go
  @backend/internal/repository/release_detail_public_repository.go
  @backend/internal/repository/jellyfin_source_fixture_test.go
  @shared/contracts/episode-versions.yaml
  </read_first>
  <behavior>
    - A selected MKV/ja/de variant with a contrasting MP4/de/en sibling exposes only the selected variant's metadata.
    - The selected audio codec and language come from the same selected audio index; subtitles use their real codec/default/forced fields.
    - Unknown-language ASS remains one track with language null; no tracks is []; no-snapshot fallback is restricted to the same selected variant.
    - Public metadata makes zero provider requests and no SQL query per track.
  </behavior>
  <action>Per D-06/D-08/D-09/D-11, replace first-variant scalars plus all-variant subtitle aggregation with one selected variant/source record from the shared seam. Project container and existing technical columns with the typed source audio/subtitle snapshot. Subtitle Format is the codec, not the release's softsub/hardsub classification; prefer supplied display title, then explicit known language, then a neutral track label without guessing language. For preexisting absent snapshots retain existing scalar/language data only within that selected variant, with nullable values and no upstream enrichment. Add guarded DB tests with multiple variants/groups, complete-empty and missing snapshots, unknown language and real flags.</action>
  <verify><automated>docker exec -e TEAM4S_PHASE117_TEST_DSN -w /app team4sv30-backend go test ./internal/repository -run 'TestPublicReleaseTechnicalSource' -count=1</automated></verify>
  <done>The public API's existing technical fields are coherent, nullable and DB-only, proven by contrasting-variant integration fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify the unchanged public display with populated and missing facts</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseDetailHero.test.tsx</files>
  <read_first>
  @frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseDetailHero.test.tsx
  @frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseDetailHero.tsx
  @frontend/src/types/releaseDetail.ts
  @.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-UI-SPEC.md
  </read_first>
  <behavior>
    - Existing hero displays container, selected audio language and subtitle labels/codecs/default/forced without a new layout.
    - A known track with null language is visible; absent facts retain the existing empty state.
    - No private source path, upstream URL, item ID or key appears in rendered markup.
  </behavior>
  <action>Per D-09/D-11/D-12, extend the current component test with the source-coherent API fixtures from the repository test. Reuse the existing hero and technical information structure; implementation work here is test coverage unless an actual contract-display defect is proven. Do not add technical provenance controls or redesign the public release page. If a concrete formatting defect requires a source change, report it as a scoped plan deviation with the exact contract cause before editing the component.</action>
  <verify><automated>docker exec team4sv30-frontend npx vitest run 'src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseDetailHero.test.tsx'</automated></verify>
  <done>Existing display tests prove populated/unknown/empty source metadata and no private-data exposure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove final playback and selected-source database integration</name>
  <files>backend/internal/repository/theme_segment_playback_resolution_integration_test.go</files>
  <read_first>
  @backend/internal/repository/theme_segment_playback_resolution_integration_test.go
  @backend/internal/repository/jellyfin_source_fixture_test.go
  @backend/internal/repository/release_variant_source_repository.go
  @backend/internal/repository/theme_segment_playback_resolution.go
  @backend/internal/repository/theme_segment_render_cache.go
  @backend/internal/handlers/segment_stream.go
  @backend/internal/handlers/segment_render_refresh.go
  @backend/internal/handlers/segment_render_worker_test.go
  </read_first>
  <behavior>
    - A/B sources under one item yield distinct selected-source cache identities; reordered sources preserve the bound choice.
    - Release and theme playback resolve the same owned variant/source used by the public technical projection, including explicit variant ownership and no-snapshot fixtures.
    - Stored source fingerprint and execution identity cannot drift silently; a changed source cannot render B into the cached A entry.
    - Snapshot hydration has bounded database statements independent of track count, and missing/incomplete new sources leave persisted data unchanged.
  </behavior>
  <action>Per D-07/D-11/D-12/D-13, extend the existing guarded theme-playback integration tests against the completed Plan 07 seams. Use the shared unique-schema fixture and the existing source metadata namespace. Assert the selected item/source pair, default versus explicit variant ownership, source fingerprint/cache key separation, no-snapshot behavior and cross-variant isolation against real database results. Cross-check the selected variant against this plan's public technical fixture so the two consumers cannot silently use different defaults. Preserve the request-level worker drift tests from Plan 07; do not add a parallel playback implementation. Record actual executed integration cases and statement counters. This is a required phase acceptance task, not deferred or optional coverage.</action>
  <verify><automated>docker exec -e TEAM4S_PHASE117_TEST_DSN -w /app team4sv30-backend go test ./internal/repository -run 'Test.*(ThemeSegmentPlaybackResolution|PublicReleaseTechnicalSource|SourceIdentity)' -count=1</automated></verify>
  <done>Final guarded database integration proves selected-source playback/public coherence and cache isolation; required tests execute without skips and no application rows are changed.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

Existing browser-to-Team4s authorization, Team4s-to-Jellyfin requests, repository data ownership and isolated test execution remain explicit boundaries. No new public endpoint, credentials or schema is required.

| Threat ID | STRIDE | Component | Disposition | Mitigation |
|---|---|---|---|---|
| T161-08-1 | T | Cross-variant metadata mixing | mitigate | Use a single selected variant/source for scalar and track projection. |
| T161-08-2 | I | Public release information | mitigate | Expose only existing technical facts; never private source identity, path, URL or secrets. |
| T161-08-3 | D | Public provider dependency | mitigate | Keep projection DB-only with bounded statements and no per-track queries. |
</threat_model>

<verification>
Run every task's automated gate in the canonical Linux environment. Before DB tests, the executor/root reads the existing Compose PostgreSQL credentials through docker inspect into memory, constructs a URL-escaped DSN for team4s_phase117_test_161, and sets TEAM4S_PHASE117_TEST_DSN only in the host subprocess environment passed to docker exec -e TEAM4S_PHASE117_TEST_DSN. Use the existing Python subprocess/config-reading seam, not a new application helper. Never print or persist the DSN, password or key. Integration tests must actually execute in guarded unique schemas; a skipped required test is a failed verification gate, not persistence proof. Run scoped checks and git diff --check. Record known global failures separately against docs/audits/2026-09-15-jellyfin12/checks-baseline.json and extended-checks-baseline.json. Plan 09 runs integrated live and broad gates. Do not modify application rows or provider libraries to satisfy a test.
</verification>

<success_criteria>
- Container, audio language and all subtitle tracks on a public release describe one selected variant/source.
- Unknown language remains unknown while a real subtitle track and its codec/flags remain visible.
- Existing no-snapshot releases remain readable without triggering Jellyfin requests.
- Every task gate passes or a concrete blocking discrepancy is reported; no unsupported completion claim.
- Only this plan's intended files are committed; source ownership and other providers remain intact.
</success_criteria>

<output>
After completion, create .planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-08-SUMMARY.md with changes, tests, exact request/query counts where relevant, commit IDs and unresolved evidence limits.
</output>
