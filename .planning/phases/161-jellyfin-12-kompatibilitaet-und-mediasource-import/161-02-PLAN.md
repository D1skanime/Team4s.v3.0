---
phase: 161-jellyfin-12-kompatibilitaet-und-mediasource-import
plan: "02"
type: execute
wave: 2
depends_on: ["161-01"]
files_modified: ["backend/internal/models/jellyfin_source.go","backend/internal/models/jellyfin_source_test.go","backend/internal/handlers/jellyfin_media_source.go","backend/internal/handlers/jellyfin_media_source_test.go","backend/internal/handlers/jellyfin_client.go","backend/internal/handlers/jellyfin_client_series.go","backend/internal/handlers/jellyfin_source_batch.go","backend/internal/handlers/jellyfin_source_batch_test.go"]
autonomous: true
requirements: ["P161-SOURCE","P161-API","P161-ITEMS","P161-METADATA"]
must_haves:
  truths:
    - "A chosen media file keeps its own container, audio and subtitle metadata even when Jellyfin reorders alternative sources."
    - "Lost or ambiguous stored source bindings fail explicitly instead of switching to another file."
    - "11eyes Episode 1 yields three genuine item candidates with three own-path sources; nested alternatives are not duplicated."
  artifacts:
    - path: "backend/internal/models/jellyfin_source.go"
      provides: "Typed source binding and deterministic batch projection"
    - path: "backend/internal/models/jellyfin_source_test.go"
      provides: "Typed source binding and deterministic batch projection"
  key_links:
    - from: "backend/internal/handlers/jellyfin_client.go"
      to: "backend/internal/handlers/jellyfin_media_source.go"
      via: "nested source DTO feeds the shared resolver"
    - from: "backend/internal/handlers/jellyfin_media_source.go"
      to: "backend/internal/models/jellyfin_source.go"
      via: "one typed coherent snapshot"
---

<objective>
Typed source binding and deterministic batch projection.

Purpose: A chosen media file keeps its own container, audio and subtitle metadata even when Jellyfin reorders alternative sources.
Output: Implemented behavior, focused regression tests and 161-02-SUMMARY.md.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-CONTEXT.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-RESEARCH.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-MEDIASOURCE-RESEARCH.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-PATTERNS.md

<interfaces>
Define models.JellyfinSourceSnapshot with Version int, MediaSourceID string, SourcePath string, StreamsComplete bool, SelectedAudioIndex *int32, AudioTracks []JellyfinAudioTrack and SubtitleTracks []JellyfinSubtitleTrack. Audio track: Index, Codec, Language *string, IsDefault; subtitle: Index, Codec, Language *string, DisplayTitle, IsDefault, IsForced. JSON namespace is jellyfin_source and its schema version is 1. Container, filename, video/audio codecs and duration remain existing variant columns. A server-only resolved-source value combines this snapshot with those technical fields. The pure resolveJellyfinMediaSource(item jellyfinEpisode, stored *models.JellyfinSourceSnapshot) returns that value or a typed missing/ambiguous conflict. Keep upstream JellyfinItemID and selected MediaSourceID separate.
</interfaces>

Execution is on /home/d1sk/team4s via SSH, with tests/builds in Compose containers. No worktrees. Execute plans sequentially in the shared canonical checkout even when technical waves permit independent work. Check git status and Compose first; preserve unrelated edits. For every task read its edited files once, then the listed analogs. Do not reread the entire research corpus or invent a new architecture.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the typed snapshot and pure source resolver</name>
  <files>backend/internal/models/jellyfin_source.go, backend/internal/models/jellyfin_source_test.go, backend/internal/handlers/jellyfin_media_source.go, backend/internal/handlers/jellyfin_media_source_test.go, backend/internal/handlers/jellyfin_client.go</files>
  <read_first>
  @backend/internal/handlers/jellyfin_client.go
  @backend/internal/models/jellyfin_source.go
  @backend/internal/models/jellyfin_source_test.go
  @backend/internal/handlers/jellyfin_media_source.go
  @backend/internal/handlers/jellyfin_media_source_test.go
  @.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-MEDIASOURCE-RESEARCH.md
  @backend/internal/handlers/jellyfin_helpers.go
  @backend/internal/handlers/segment_render_subtitles.go
  @backend/go.mod
  @docs/audits/2026-09-15-jellyfin12/fixtures/11eyes-episode1.json
  @docs/audits/2026-09-15-jellyfin12/fixtures/11eyes-series.json
  </read_first>
  <behavior>
    - Stored exact source ID wins; unique stored full path recovers changed source ID; missing/ambiguous stored binding never falls through to item.Path or a sole remaining different source.
    - For a new binding choose a unique exact item.Path/source.Path match, otherwise the sole valid source, otherwise conflict. Missing source, duplicate source identity and wrong requested item fail.
    - A/B fixtures with poisoned top-level streams/container, reversed arrays, and the real three-item 11eyes fixture remain source coherent.
    - jpn/deu/ger/eng normalize explicitly; und, blank and malformed language remain null; an unknown-language subtitle track remains present.
    - Full sanitized 11eyes series fixture preserves 27 actual item candidates and 27 unique own-path selections while observing 38 nested sources and 11 non-item alternatives.
  </behavior>
  <action>Per D-06/D-07/D-09/D-10, first extend the existing Jellyfin DTO in jellyfin_client.go with source Path, Container, RunTimeTicks, DefaultAudioStreamIndex, presence-aware MediaStreams and justified stream fields. Define the exact shared snapshot once and implement a pure resolver using the research decision table. Compare full paths case-sensitively, allowing only trim/separator normalization; never match a basename or open a source path. A source with explicitly present empty MediaStreams is complete and empty; omitted MediaStreams is incomplete and must not erase prior complete tracks. Never borrow item-level streams. Select representative audio by valid source DefaultAudioStreamIndex, then deterministic default track/index, then lowest valid audio index; preserve codec and language from that same track. Use already installed x/text/language Parse and Tag.Raw without inferred locales or a manual ISO table. Per D-16, the approved Japanese default belongs only to the existing public audio-language display. Persist raw unknown audio language as null and do not invent an audio or subtitle track; subtitle language has no default. Persist only consumer-justified fields; audio channel/layout and unused external paths remain unpersisted. Include JSON serialization tests and all resolver edge cases before implementation.</action>
  <verify><automated>docker exec -w /app team4sv30-backend go test ./internal/models ./internal/handlers -run 'Test.*(JellyfinSource|ResolveJellyfinMediaSource|11eyes)' -count=1</automated></verify>
  <done>The typed snapshot is fixed for downstream plans and every selection/unknown-language fixture passes; no schema or migration is introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Decode nested sources and provide bounded exact-ID batch reads</name>
  <files>backend/internal/handlers/jellyfin_client.go, backend/internal/handlers/jellyfin_client_series.go, backend/internal/handlers/jellyfin_source_batch.go, backend/internal/handlers/jellyfin_source_batch_test.go, backend/internal/handlers/jellyfin_media_source_test.go</files>
  <read_first>
  @backend/internal/handlers/jellyfin_client.go
  @backend/internal/handlers/jellyfin_client_series.go
  @backend/internal/handlers/jellyfin_source_batch.go
  @backend/internal/handlers/jellyfin_source_batch_test.go
  @backend/internal/handlers/jellyfin_media_source_test.go
  @backend/internal/handlers/jellyfin_client.go
  @backend/internal/handlers/jellyfin_client_series.go
  @backend/internal/handlers/jellyfin_client_test.go
  @docs/audits/2026-09-15-jellyfin12/discovery-11eyes-coverage.json
  @docs/audits/2026-09-15-jellyfin12/discovery-11eyes-completeness.json
  </read_first>
  <behavior>
    - One episode collection response contains the nested source fields required for all items, with no per-source or per-stream fetch.
    - Exact-ID hydration chunks at 100 unique item IDs, verifies returned identity/completeness, and performs ceil(N/100) metadata calls; an empty selection performs none.
    - Duration enrichment uses the same chosen source, and a wrong/missing item never supplies another item's runtime.
  </behavior>
  <action>Per D-04/D-05/D-07/D-13, reuse the already compiling source/stream DTO extensions from Task 1 without another upstream JSON model. Request valid MediaSources,MediaStreams,Path fields through the existing listJellyfinEpisodes collection seam; retain TotalRecordCount for completeness checks and explicit paging only where the collection actually paginates. Add one bounded exact-item batch seam usable by both AdminContentHandler and FansubHandler through the shared request transport, not a second token/URL builder or a fake handler instance. Deduplicate requested IDs, require exact results and no extras, propagate missing/duplicate items as diagnostics. Adapt source-dependent duration lookup to the resolver and remove first-item fallback. Do not turn unresolved nested MediaSource IDs into Item IDs or issue further per-source discovery calls: live 11eyes has 27 actual items, 38 sources and 11 non-item source IDs.</action>
  <verify><automated>docker exec -w /app team4sv30-backend go test ./internal/handlers -run 'Test.*(JellyfinSourceBatch|JellyfinMediaSource|11eyes|JellyfinEpisodeDuration)' -count=1</automated></verify>
  <done>All source consumers can reuse one typed resolver and one bounded hydration seam; item/source identities remain distinct and request-count assertions pass.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

Existing browser-to-Team4s authorization, Team4s-to-Jellyfin requests, repository data ownership and isolated test execution remain explicit boundaries. No new public endpoint, credentials or schema is required.

| Threat ID | STRIDE | Component | Disposition | Mitigation |
|---|---|---|---|---|
| T161-02-1 | T | Provider item/source identity | mitigate | Reject wrong IDs, duplicate IDs and lost bindings; never substitute a different file. |
| T161-02-2 | I | Source paths and tracks | mitigate | Keep private paths in internal/admin flows only; no filesystem access or inferred languages. |
| T161-02-3 | D | Batch hydration | mitigate | Use bounded chunks, cancellation and explicit collection completeness checks instead of fan-out. |
</threat_model>

<verification>
Run every task's automated gate in the canonical Linux environment. Integration tests require the explicit dedicated test DSN and must actually execute; skip is not persistence proof. Run scoped checks and git diff --check. Record known global failures separately against docs/audits/2026-09-15-jellyfin12/checks-baseline.json and extended-checks-baseline.json. Plan 09 runs integrated live and broad gates. Do not modify application rows or provider libraries to satisfy a test.
</verification>

<success_criteria>
- A chosen media file keeps its own container, audio and subtitle metadata even when Jellyfin reorders alternative sources.
- Lost or ambiguous stored source bindings fail explicitly instead of switching to another file.
- 11eyes Episode 1 yields three genuine item candidates with three own-path sources; nested alternatives are not duplicated.
- Every task gate passes or a concrete blocking discrepancy is reported; no unsupported completion claim.
- Only this plan's intended files are committed; source ownership and other providers remain intact.
</success_criteria>

<output>
After completion, create .planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-02-SUMMARY.md with changes, tests, exact request/query counts where relevant, commit IDs and unresolved evidence limits.
</output>
