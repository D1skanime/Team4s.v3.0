---
phase: 161-jellyfin-12-kompatibilitaet-und-mediasource-import
plan: "07"
type: execute
wave: 6
depends_on: ["161-06"]
files_modified: ["backend/internal/models/theme_segment_render_cache.go","backend/internal/repository/release_variant_source_repository.go","backend/internal/repository/episode_version_repository.go","backend/internal/repository/theme_segment_playback_resolution.go","backend/internal/repository/theme_segment_render_cache.go","backend/internal/handlers/episode_version_stream.go","backend/internal/handlers/segment_render_subtitles.go","backend/internal/handlers/segment_render_worker.go","backend/internal/handlers/admin_content_episode_version_editor_helpers.go","backend/internal/handlers/segment_stream.go","backend/internal/handlers/segment_render_refresh.go","backend/internal/repository/episode_version_stream_identity_test.go","backend/internal/handlers/segment_render_subtitles_test.go","backend/internal/handlers/segment_render_worker_test.go"]
autonomous: true
requirements: ["P161-SOURCE","P161-METADATA","P161-REGRESSION"]
must_haves:
  truths:
    - "Video, subtitle download and segment rendering use the same item/source binding as imported technical metadata."
    - "Different sources under one item cannot share a derived segment cache entry."
    - "Missing or stale source identity fails visibly without selecting another file or changing live data."
  artifacts:
    - path: "backend/internal/models/theme_segment_render_cache.go"
      provides: "Carry selected-source identity through playback, subtitles and cache"
    - path: "backend/internal/repository/release_variant_source_repository.go"
      provides: "Carry selected-source identity through playback, subtitles and cache"
  key_links:
    - from: "backend/internal/repository/release_variant_source_repository.go"
      to: "backend/internal/handlers/episode_version_stream.go"
      via: "owned selected MediaSourceId"
    - from: "backend/internal/handlers/segment_render_subtitles.go"
      to: "backend/internal/handlers/segment_render_worker.go"
      via: "one source's tracks, video input and prepared identity"
    - from: "backend/internal/handlers/segment_stream.go"
      to: "backend/internal/services/segment_render_service.go"
      via: "item plus resolved source in stream cache key"
    - from: "backend/internal/handlers/segment_render_refresh.go"
      to: "backend/internal/services/segment_render_service.go"
      via: "item plus resolved source in refresh cache key"
---

<objective>
Carry selected-source identity through playback, subtitles and cache.

Purpose: Video, subtitle download and segment rendering use the same item/source binding as imported technical metadata.
Output: Implemented behavior, focused regression tests and 161-07-SUMMARY.md.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-CONTEXT.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-RESEARCH.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-MEDIASOURCE-RESEARCH.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-PATTERNS.md

<interfaces>
Plan 04 extends internal models.ReleaseStreamSource with selected identity/snapshot. Add corresponding server-only selected-source fields to ThemeSegmentRenderSource (json:"-" unless an existing authorized admin contract explicitly needs them). Introduce a focused shared selected-variant/source query seam in release_variant_source_repository.go by extracting GetReleaseStreamSource's existing variant membership/default selection; order default by variant ID then stream ID and require explicit variant ownership. Public Plan 08 consumes the same selected variant. Existing SegmentRenderWindow.SourceIdentity already participates in BuildSegmentRenderCacheKey; include the actual item ID and resolved MediaSourceID.
</interfaces>

Execution is on /home/d1sk/team4s via SSH, with tests/builds in Compose containers. No worktrees. Execute plans sequentially in the shared canonical checkout even when technical waves permit independent work. Check git status and Compose first; preserve unrelated edits. For every task read its edited files once, then the listed analogs. Do not reread the entire research corpus or invent a new architecture.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve one owned variant and expose its internal source binding</name>
  <files>backend/internal/models/theme_segment_render_cache.go, backend/internal/repository/release_variant_source_repository.go, backend/internal/repository/episode_version_repository.go, backend/internal/repository/theme_segment_playback_resolution.go, backend/internal/repository/theme_segment_render_cache.go</files>
  <read_first>
  @backend/internal/models/theme_segment_render_cache.go
  @backend/internal/repository/release_variant_source_repository.go
  @backend/internal/repository/episode_version_repository.go
  @backend/internal/repository/theme_segment_playback_resolution.go
  @backend/internal/repository/theme_segment_render_cache.go
  @backend/internal/models/episode_version.go
  @backend/internal/repository/jellyfin_source_repository.go
  @backend/internal/repository/episode_version_stream_identity_test.go
  @backend/internal/repository/release_detail_public_repository_helpers.go
  </read_first>
  <behavior>
    - Default release playback and later public technical projection choose the same variant/source; explicit variant IDs remain scoped to their release version.
    - Source snapshot fields are read in the existing selected-source query, not a query per track.
    - Missing snapshot remains a readable existing record and public reads need no upstream request.
  </behavior>
  <action>Per D-06/D-07/D-11/D-13, extract the existing variant/source selection into the specified reusable repository seam and use it for GetReleaseStreamSource and relevant theme source reads. Preserve source-kind precedence and explicit version/variant membership. Decode only the chosen source's namespace through the shared typed model. Return internal selected identity and optional binding evidence for handlers, without expanding public DTOs. Keep these reads DB-only and bound query counts; do not update snapshots or old render caches during a read.</action>
  <verify><automated>docker exec -e TEAM4S_PHASE117_TEST_DSN -w /app team4sv30-backend go test ./internal/repository -run 'Test.*(ReleaseStreamIdentity|EpisodeVersionStream|ThemeSegmentPlaybackResolution)' -count=1</automated></verify>
  <done>Playback repositories return one coherent owned variant/source binding and preserve no-snapshot records without opportunistic writes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Use that source in video/subtitle/render requests and cache identity</name>
  <files>backend/internal/handlers/episode_version_stream.go, backend/internal/handlers/segment_render_subtitles.go, backend/internal/handlers/segment_render_worker.go, backend/internal/handlers/admin_content_episode_version_editor_helpers.go, backend/internal/handlers/segment_render_subtitles_test.go</files>
  <read_first>
  @backend/internal/handlers/episode_version_stream.go
  @backend/internal/handlers/segment_render_subtitles.go
  @backend/internal/handlers/segment_render_worker.go
  @backend/internal/handlers/admin_content_episode_version_editor_helpers.go
  @backend/internal/handlers/segment_stream.go
  @backend/internal/handlers/segment_render_refresh.go
  @backend/internal/handlers/jellyfin_media_source.go
  @backend/internal/handlers/jellyfin_source_batch.go
  @backend/internal/services/segment_render_service.go
  @backend/internal/handlers/segment_render_refresh_test.go
  </read_first>
  <behavior>
    - Release video URL includes the selected MediaSourceId; subtitle URL uses the same source and its own track index.
    - Exact item lookup never returns the first unrelated item and no item-level stream or first-source fallback remains.
    - Sources A/B under the same item yield different cache keys before cache lookup; reordered sources do not change a bound key.
    - A no-snapshot existing binding resolves deterministically at the existing runtime boundary without persisting; ambiguity produces an explicit failure.
  </behavior>
  <action>Per D-07/D-09/D-11, wire the internal binding into the existing video URL builder and subtitle resolver. Use the shared resolver for the existing metadata read, choose subtitle tracks only from the selected source, and remove unsafe findJellyfinItem/resolveJellyfinMediaSourceID fallbacks. For bound rows use stored source identity; recover changed ID only through unique stored path when a metadata read occurs and do not persist on read. If source identity is not yet bound, resolve once before cache lookup/render preparation so the final key and eventual input use the same source; reuse that result rather than issuing another metadata call per consumer. Include item+resolved source in SourceIdentity before BuildSegmentRenderCacheKey and propagate it through the worker. Preserve explicit media-asset source behavior, grant ownership, FFmpeg header/redirect protections and existing render lifecycle. Do not reset or delete old caches.</action>
  <verify><automated>docker exec -w /app team4sv30-backend go test ./internal/handlers -run 'Test.*(SegmentSubtitle|SegmentSourceIdentity|SegmentPlayback|ReleaseStream)' -count=1</automated></verify>
  <done>The same selected source drives video, subtitle and cache identity with no silent fallback or extra per-track requests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire cache identity and prove request-level isolation</name>
  <files>backend/internal/handlers/segment_stream.go, backend/internal/handlers/segment_render_refresh.go, backend/internal/repository/episode_version_stream_identity_test.go, backend/internal/handlers/segment_render_worker_test.go</files>
  <read_first>
  @backend/internal/repository/episode_version_stream_identity_test.go
  @backend/internal/handlers/segment_render_subtitles_test.go
  @backend/internal/handlers/segment_render_worker_test.go
  @backend/internal/services/segment_render_service_test.go
  @backend/internal/repository/jellyfin_source_fixture_test.go
  </read_first>
  <behavior>
    - Contrasting A/B sources prove exact video item/source query, subtitle source/index and distinct cache identity.
    - ID reorder, unique path recovery, disappeared binding and wrong-item responses have deterministic outcomes.
    - Snapshot source read adds no SQL per subtitle/track; bound render retains one metadata read plus at most one subtitle download.
  </behavior>
  <action>Per D-12/D-13, wire item plus resolved source identity into both segment_stream.go and segment_render_refresh.go before each cache lookup, then extend existing request-capture, stream-identity and worker tests to exercise actual wiring, not only helper return values. Final guarded theme-playback integration coverage is owned by Plan 08 Task 3, after these production seams are complete. Include multi-variant ownership and no-snapshot fixtures, a cached A result followed by explicit B context, and a source change between request preparation and worker execution that cannot render B into A's cache key. Compare the prepared item/source identity with the persisted cache.SourceFingerprint before starting FFmpeg; fail the job as stale/conflict if preparation and execution identities differ. Use the existing source_fingerprint column and worker lifecycle, not a second preparation pipeline. Record DB/provider call counters and preserve current grant/cancellation/fan-out regressions.</action>
  <verify><automated>docker exec -e TEAM4S_PHASE117_TEST_DSN -w /app team4sv30-backend go test ./internal/repository ./internal/handlers ./internal/services -run 'Test.*(SourceIdentity|StreamIdentity|SegmentSubtitle|SegmentRenderWorker|ThemeSegmentPlaybackResolution)' -count=1</automated></verify>
  <done>Request-capture, stream-identity and worker tests prove selected-source requests and cache isolation; the final guarded theme-playback database acceptance follows in Plan 08 Task 3.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

Existing browser-to-Team4s authorization, Team4s-to-Jellyfin requests, repository data ownership and isolated test execution remain explicit boundaries. No new public endpoint, credentials or schema is required.

| Threat ID | STRIDE | Component | Disposition | Mitigation |
|---|---|---|---|---|
| T161-07-1 | T | Cached clip identity | mitigate | Resolve source before hashing; reject identity drift before worker execution. |
| T161-07-2 | E | Explicit variant selector | mitigate | Keep exact release-version membership at the shared repository read. |
| T161-07-3 | I | Internal source metadata | mitigate | Keep paths/IDs server-side and preserve header/redirect redaction from Plan 03. |
</threat_model>

<verification>
Run every task's automated gate in the canonical Linux environment. Before DB tests, the executor/root reads the existing Compose PostgreSQL credentials through docker inspect into memory, constructs a URL-escaped DSN for team4s_phase117_test_161, and sets TEAM4S_PHASE117_TEST_DSN only in the host subprocess environment passed to docker exec -e TEAM4S_PHASE117_TEST_DSN. Use the existing Python subprocess/config-reading seam, not a new application helper. Never print or persist the DSN, password or key. Integration tests must actually execute in guarded unique schemas; a skipped required test is a failed verification gate, not persistence proof. Run scoped checks and git diff --check. Record known global failures separately against docs/audits/2026-09-15-jellyfin12/checks-baseline.json and extended-checks-baseline.json. Plan 09 runs integrated live and broad gates. Do not modify application rows or provider libraries to satisfy a test.
</verification>

<success_criteria>
- Video, subtitle download and segment rendering use the same item/source binding as imported technical metadata.
- Different sources under one item cannot share a derived segment cache entry.
- Missing or stale source identity fails visibly without selecting another file or changing live data.
- Every task gate passes or a concrete blocking discrepancy is reported; no unsupported completion claim.
- Only this plan's intended files are committed; source ownership and other providers remain intact.
</success_criteria>

<output>
After completion, create .planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-07-SUMMARY.md with changes, tests, exact request/query counts where relevant, commit IDs and unresolved evidence limits.
</output>
