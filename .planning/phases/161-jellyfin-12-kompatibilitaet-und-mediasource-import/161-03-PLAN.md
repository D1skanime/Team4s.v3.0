---
phase: 161-jellyfin-12-kompatibilitaet-und-mediasource-import
plan: "03"
type: execute
wave: 2
depends_on: ["161-01"]
files_modified: ["backend/internal/handlers/fansub_admin.go","backend/internal/handlers/episode_version_media_image.go","backend/internal/handlers/episode_version_media_video.go","backend/internal/handlers/media_proxy_test.go","backend/internal/handlers/episode_version_stream.go","backend/internal/handlers/asset_stream_handler.go","backend/internal/handlers/episode_version_stream_identity_test.go","backend/internal/handlers/segment_render_subtitles.go","backend/internal/handlers/segment_render_worker.go","backend/internal/services/segment_render_service.go","backend/internal/services/segment_render_service_test.go","backend/internal/handlers/segment_render_subtitles_test.go"]
autonomous: true
requirements: ["P161-AUTH","P161-REGRESSION"]
must_haves:
  truths:
    - "Images, release video, protected assets and segment input use modern Jellyfin authentication while retaining existing access and Range behavior."
    - "FFmpeg neither leaks the provider key nor forwards it across a redirect."
    - "Emby and Fanart behavior is preserved."
  artifacts:
    - path: "backend/internal/handlers/fansub_admin.go"
      provides: "Authenticate binary proxies, subtitle downloads and FFmpeg"
    - path: "backend/internal/handlers/episode_version_media_image.go"
      provides: "Authenticate binary proxies, subtitle downloads and FFmpeg"
  key_links:
    - from: "backend/internal/handlers/fansub_admin.go"
      to: "backend/internal/jellyfin/request.go"
      via: "provider-specific URLs and request headers"
    - from: "backend/internal/handlers/segment_render_worker.go"
      to: "backend/internal/services/segment_render_service.go"
      via: "separate HTTPHeaders plus bounded authenticated FFmpeg input"
---

<objective>
Authenticate binary proxies, subtitle downloads and FFmpeg.

Purpose: Images, release video, protected assets and segment input use modern Jellyfin authentication while retaining existing access and Range behavior.
Output: Implemented behavior, focused regression tests and 161-03-SUMMARY.md.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-CONTEXT.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-RESEARCH.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-MEDIASOURCE-RESEARCH.md
@.planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-PATTERNS.md

<interfaces>
Plan 01 supplies backend/internal/jellyfin request helpers. Existing FansubHandler.buildProviderStreamURL(provider, itemID string, fallbackURL *string) and buildProviderImageURL branch by provider; keep their ownership. Existing services.SegmentRenderCommandInput carries StreamURL and BuildFFmpegSegmentArgs returns argv. Add HTTPHeaders http.Header as a server-only field; input HTTP options precede -i. The installed FFmpeg http protocol exposes -max_redirects (default 8), so set -max_redirects 0 for authenticated provider inputs. No browser DTO receives those headers.
</interfaces>

Execution is on /home/d1sk/team4s via SSH, with tests/builds in Compose containers. No worktrees. Execute plans sequentially in the shared canonical checkout even when technical waves permit independent work. Check git status and Compose first; preserve unrelated edits. For every task read its edited files once, then the listed analogs. Do not reread the entire research corpus or invent a new architecture.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate provider-aware image and video proxy requests</name>
  <files>backend/internal/handlers/fansub_admin.go, backend/internal/handlers/episode_version_media_image.go, backend/internal/handlers/episode_version_media_video.go, backend/internal/handlers/media_proxy_test.go</files>
  <read_first>
  @backend/internal/handlers/fansub_admin.go
  @backend/internal/handlers/episode_version_media_image.go
  @backend/internal/handlers/episode_version_media_video.go
  @backend/internal/handlers/media_proxy_test.go
  @backend/internal/handlers/episode_playback_proxy.go
  @backend/internal/services/anime_create_enrichment.go
  @backend/internal/jellyfin/request.go
  </read_first>
  <behavior>
    - Jellyfin image and media-video requests receive header auth and key-free URLs; captured Emby requests retain their existing query auth.
    - Range/User-Agent forwarding, response code/content headers and cancellation remain intact.
    - A foreign stored URL receives no Jellyfin credential, and a redirect cannot forward one.
  </behavior>
  <action>Per D-02/D-11, make only the Jellyfin branch of the existing provider URL builders key-free and wire the MediaImage/MediaVideo HTTP owners through Plan 01's authenticated transport when provider and configured origin match. Preserve Emby builders, the separate Emby-only playback handler and Fanart exactly. Keep existing image sizing/static-video parameters and proxy authorization. Retain legacy URL redaction support; do not blanket-remove api_key throughout the repository. Cover binary success, Range206, 401, 5xx and canceled requests with captured HTTP requests.</action>
  <verify><automated>docker exec -w /app team4sv30-backend go test ./internal/handlers -run 'Test.*(MediaProxy|MediaImage|MediaVideo)' -count=1</automated></verify>
  <done>Both general media proxies use provider-specific authentication and retain binary/Range semantics; explicit Emby regression assertions pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate release and protected asset streaming</name>
  <files>backend/internal/handlers/episode_version_stream.go, backend/internal/handlers/asset_stream_handler.go, backend/internal/handlers/episode_version_stream_identity_test.go, backend/internal/handlers/media_proxy_test.go</files>
  <read_first>
  @backend/internal/handlers/episode_version_stream.go
  @backend/internal/handlers/asset_stream_handler.go
  @backend/internal/handlers/episode_version_stream_identity_test.go
  @backend/internal/handlers/media_proxy_test.go
  @backend/internal/handlers/episode_version_grants.go
  @backend/internal/auth/release_grant.go
  @backend/internal/handlers/asset_stream_handler.go
  </read_first>
  <behavior>
    - Release and asset stream requests use the selected provider's authentication without relaxing grant/entitlement checks.
    - An explicit release variant still cannot cross release-version ownership; stored foreign fallbacks remain uncredentialed.
    - Upstream 401/5xx and Range206 responses are handled consistently without exposing URL secrets.
  </behavior>
  <action>Per D-02/D-11, route the remaining two binary HTTP execution sites through the same Jellyfin request policy. Normalize trusted legacy Jellyfin fallback URLs by removing only credential query keys before execution; never send the configured key to another origin. Preserve grants, explicit variant selectors, start ticks, Range and caller disconnect cancellation. Do not modify source selection in this task; Plan 07 wires the newly persisted source selector after repository changes.</action>
  <verify><automated>docker exec -w /app team4sv30-backend go test ./internal/handlers -run 'Test.*(ReleaseStream|StreamRelease|StreamAsset|MediaProxy)' -count=1</automated></verify>
  <done>Release and asset streaming are authenticated under Jellyfin 12; no new authorization bypass or foreign-origin credential forwarding is possible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate subtitle and FFmpeg HTTP authentication safely</name>
  <files>backend/internal/handlers/segment_render_subtitles.go, backend/internal/handlers/segment_render_worker.go, backend/internal/services/segment_render_service.go, backend/internal/services/segment_render_service_test.go, backend/internal/handlers/segment_render_subtitles_test.go</files>
  <read_first>
  @backend/internal/handlers/segment_render_subtitles.go
  @backend/internal/handlers/segment_render_worker.go
  @backend/internal/services/segment_render_service.go
  @backend/internal/services/segment_render_service_test.go
  @backend/internal/handlers/segment_render_subtitles_test.go
  @backend/internal/handlers/admin_content_episode_version_editor_helpers.go
  @docs/audits/2026-09-15-jellyfin12/ffmpeg-redirect-baseline.json
  @backend/internal/handlers/segment_render_worker_test.go
  @backend/internal/services/segment_render_service.go
  </read_first>
  <behavior>
    - Subtitle GETs have header auth and no api_key query parameter.
    - FFmpeg receives separate server-only HTTP headers and -max_redirects 0 before -i; its StreamURL remains key-free.
    - An executable two-origin HTTP redirect fixture proves the second origin receives neither a request nor credentials; stderr/error sanitization removes the synthetic key and modern header token.
  </behavior>
  <action>Per D-02/D-12, use the shared request constructor for downloadJellyfinSubtitle. Supply the renderer HTTPHeaders separately from the URL through SegmentRenderCommandInput, using the same auth-header builder as Go requests. Pass -max_redirects 0 before -i for authenticated Jellyfin input, supported by the installed FFmpeg; verify it by invoking the installed binary against controlled local test servers and a tiny in-memory or temporary fixture on the container filesystem. Add the actual Jellyfin key to existing worker diagnostic sanitization and recognize Authorization: MediaBrowser Token values, without logging argv or headers. Preserve existing render limits/output/cache lifecycle. The installed-binary two-origin proof in ffmpeg-redirect-baseline.json already demonstrates zero foreign requests at max_redirects=0, versus credential forwarding at the default 8; preserve that executable regression. No new proxy is needed.</action>
  <verify><automated>docker exec -w /app team4sv30-backend go test ./internal/handlers ./internal/services -run 'Test.*(SegmentSubtitle|FFmpeg|SanitizeSegmentRenderLog)' -count=1</automated></verify>
  <done>Subtitle auth and executable FFmpeg redirect/secret tests pass; all nine direct HTTP sites and the indirect renderer now have modern auth coverage.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

Existing browser-to-Team4s authorization, Team4s-to-Jellyfin requests, repository data ownership and isolated test execution remain explicit boundaries. No new public endpoint, credentials or schema is required.

| Threat ID | STRIDE | Component | Disposition | Mitigation |
|---|---|---|---|---|
| T161-03-1 | I | Mixed-provider proxies | mitigate | Only configured Jellyfin origin receives its key; retain Emby and Fanart isolation. |
| T161-03-2 | I | FFmpeg redirects and logs | mitigate | Disable input redirects, prove behavior with two servers, redact real/synthetic key and modern token formats. |
| T161-03-3 | E | Release/asset proxies | mitigate | Preserve existing grants, entitlements and explicit variant membership checks. |
</threat_model>

<verification>
Run every task's automated gate in the canonical Linux environment. Integration tests require the explicit dedicated test DSN and must actually execute; skip is not persistence proof. Run scoped checks and git diff --check. Record known global failures separately against docs/audits/2026-09-15-jellyfin12/checks-baseline.json and extended-checks-baseline.json. Plan 09 runs integrated live and broad gates. Do not modify application rows or provider libraries to satisfy a test.
</verification>

<success_criteria>
- Images, release video, protected assets and segment input use modern Jellyfin authentication while retaining existing access and Range behavior.
- FFmpeg neither leaks the provider key nor forwards it across a redirect.
- Emby and Fanart behavior is preserved.
- Every task gate passes or a concrete blocking discrepancy is reported; no unsupported completion claim.
- Only this plan's intended files are committed; source ownership and other providers remain intact.
</success_criteria>

<output>
After completion, create .planning/phases/161-jellyfin-12-kompatibilitaet-und-mediasource-import/161-03-SUMMARY.md with changes, tests, exact request/query counts where relevant, commit IDs and unresolved evidence limits.
</output>
