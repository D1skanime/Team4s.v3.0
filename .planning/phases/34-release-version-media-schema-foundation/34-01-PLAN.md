---
phase: 34-release-version-media-schema-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/0059_release_version_media_schema.up.sql
  - database/migrations/0059_release_version_media_schema.down.sql
autonomous: true
requirements:
  - RVM-SCHEMA-01

must_haves:
  truths:
    - "Tabelle release_version_media existiert mit allen Feldern, FKs, CHECK-Constraints und Indexen"
    - "media_assets hat Spalte status VARCHAR(20) NOT NULL DEFAULT 'ready' mit CHECK-Constraint"
    - "media_files hat Spalte status VARCHAR(20) NOT NULL DEFAULT 'ready' mit CHECK-Constraint"
    - "Down-Migration setzt alle Änderungen vollständig zurück (DROP TABLE + DROP COLUMN)"
    - "Alle bestehenden media_assets- und media_files-Zeilen erhalten automatisch status='ready' durch DEFAULT"
  artifacts:
    - path: "database/migrations/0059_release_version_media_schema.up.sql"
      provides: "Up-Migration: CREATE TABLE release_version_media + ALTER TABLE media_assets/media_files"
      min_lines: 50
    - path: "database/migrations/0059_release_version_media_schema.down.sql"
      provides: "Down-Migration: DROP TABLE + DROP COLUMN + DROP INDEX"
      min_lines: 10
  key_links:
    - from: "release_version_media.release_version_id"
      to: "release_versions.id"
      via: "REFERENCES release_versions(id) ON DELETE CASCADE"
      pattern: "REFERENCES release_versions\\(id\\)"
    - from: "release_version_media.media_asset_id"
      to: "media_assets.id"
      via: "REFERENCES media_assets(id) ON DELETE RESTRICT"
      pattern: "REFERENCES media_assets\\(id\\)"
    - from: "release_version_media.is_preview_candidate"
      to: "category check"
      via: "CHECK constraint limits TRUE to screenshot/typesetting_karaoke"
      pattern: "chk_rvm_preview_category"
---

<objective>
Datenbankgrundlage für das Release-Version-Media-Upload-System legen: neue Tabelle `release_version_media`, `status`-Spalten in `media_assets` und `media_files`, alle Constraints und Indexe.

Purpose: Phase 35 (Backend-Service) und Phase 36 (Frontend) können auf dieser Schemagrundlage aufbauen. Keine Backend- oder Frontend-Änderungen in dieser Phase.
Output: Migration 0059 up + down — zwei SQL-Dateien in `database/migrations/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-release-version-media-schema-foundation/34-CONTEXT.md

<interfaces>
<!-- Bestehende Tabellen, gegen die FKs erzeugt werden -->

Aus database/migrations/0035_add_release_tables.up.sql:
```sql
CREATE TABLE release_versions (
    id BIGSERIAL PRIMARY KEY,
    release_id BIGINT NOT NULL REFERENCES fansub_releases(id) ON DELETE CASCADE,
    version VARCHAR(20) NOT NULL DEFAULT 'v1',
    ...
);
```

Aus database/migrations/0024_recreate_media_assets.up.sql:
```sql
CREATE TABLE media_assets (
    id BIGSERIAL PRIMARY KEY,
    media_type_id BIGINT,
    file_path TEXT NOT NULL,
    caption TEXT,
    mime_type VARCHAR(100) NOT NULL,
    format VARCHAR(50),
    uploaded_by BIGINT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    modified_at TIMESTAMPTZ,
    modified_by BIGINT
);
```

Aus database/migrations/0026_add_media_tables.up.sql:
```sql
CREATE TABLE media_files (
    id BIGSERIAL PRIMARY KEY,
    media_id BIGINT NOT NULL REFERENCES media_assets(id) ON DELETE CASCADE,
    variant VARCHAR(50),
    storage_id VARCHAR(255),
    path TEXT NOT NULL,
    width INTEGER,
    height INTEGER,
    size BIGINT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

Migrationsstil (aus 0058_rename_theme_types_kara.up.sql):
```sql
-- Kommentar: kurze Erklärung was die Migration tut
UPDATE theme_types SET name = 'OP Kara' WHERE id = 1;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Up-Migration 0059 schreiben</name>
  <files>database/migrations/0059_release_version_media_schema.up.sql</files>
  <read_first>
    - database/migrations/0058_rename_theme_types_kara.up.sql (Migrationsstil und Kommentarkonvention)
    - database/migrations/0026_add_media_tables.up.sql (media_files und release_media Definitionen — zeigt FK-Stil und Index-Naming)
    - database/migrations/0035_add_release_tables.up.sql (release_versions Definition — Quelle des FK-Ziels)
    - database/migrations/0024_recreate_media_assets.up.sql (media_assets aktuelles Schema — zeigt welche Spalten bereits existieren)
    - .planning/phases/34-release-version-media-schema-foundation/34-CONTEXT.md (alle Constraint-Namen und Index-Namen aus den Decisions)
  </read_first>
  <action>
Erstelle `database/migrations/0059_release_version_media_schema.up.sql` mit exakt diesem Inhalt:

```sql
-- Migration 0059: Release-Version-Media schema foundation
-- Neue Tabelle release_version_media + status-Spalten in media_assets/media_files

-- 1. Neue Tabelle: release_version_media
CREATE TABLE release_version_media (
    id                   BIGSERIAL PRIMARY KEY,
    release_version_id   BIGINT NOT NULL REFERENCES release_versions(id) ON DELETE CASCADE,
    media_asset_id       BIGINT NOT NULL REFERENCES media_assets(id) ON DELETE RESTRICT,
    category             VARCHAR(30) NOT NULL,
    caption              TEXT NULL,
    sort_order           INT NOT NULL DEFAULT 0,
    is_preview_candidate BOOLEAN NOT NULL DEFAULT false,
    uploaded_by_user_id  BIGINT NULL REFERENCES users(id) ON DELETE SET NULL,
    created_at           TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at           TIMESTAMPTZ,
    deleted_at           TIMESTAMPTZ NULL,
    deleted_by_user_id   BIGINT NULL REFERENCES users(id) ON DELETE SET NULL,

    CONSTRAINT chk_rvm_category
        CHECK (category IN ('screenshot', 'typesetting_karaoke', 'fun_outtake', 'other')),
    CONSTRAINT chk_rvm_preview_category
        CHECK (is_preview_candidate = false OR category IN ('screenshot', 'typesetting_karaoke'))
);

-- Indexe fuer release_version_media
CREATE INDEX idx_rvm_release_version ON release_version_media(release_version_id);
CREATE INDEX idx_rvm_media_asset     ON release_version_media(media_asset_id);
CREATE INDEX idx_rvm_category        ON release_version_media(category);
CREATE INDEX idx_rvm_public          ON release_version_media(release_version_id, category)
    WHERE deleted_at IS NULL;

-- 2. status-Spalte in media_assets
ALTER TABLE media_assets
    ADD COLUMN status VARCHAR(20) NOT NULL DEFAULT 'ready';

ALTER TABLE media_assets
    ADD CONSTRAINT chk_media_assets_status
        CHECK (status IN ('processing', 'ready', 'failed', 'deleted'));

CREATE INDEX idx_media_assets_status ON media_assets(status);

-- 3. status-Spalte in media_files
ALTER TABLE media_files
    ADD COLUMN status VARCHAR(20) NOT NULL DEFAULT 'ready';

ALTER TABLE media_files
    ADD CONSTRAINT chk_media_files_status
        CHECK (status IN ('processing', 'ready', 'failed', 'missing', 'deleted'));

CREATE INDEX idx_media_files_status ON media_files(status);
```

Hinweise:
- `uploaded_by_user_id` ist INT NULL (kein BIGINT) — konsistent mit den Decisions in CONTEXT.md
- `deleted_by_user_id` ist INT NULL — analog uploaded_by_user_id
- ON DELETE RESTRICT für media_asset_id, damit kein media_asset gelöscht werden kann, das noch referenziert ist
- ON DELETE SET NULL für user-FKs, damit User-Deletes keine release_version_media-Zeilen kaskadierend löschen
- Alle bestehenden media_assets/media_files-Zeilen erhalten automatisch status='ready' durch den DEFAULT — kein expliziter UPDATE nötig (per D: "kein Backfill nötig")
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE release_version_media" /c/Users/admin/Documents/Team4s/database/migrations/0059_release_version_media_schema.up.sql && grep -c "chk_rvm_category" /c/Users/admin/Documents/Team4s/database/migrations/0059_release_version_media_schema.up.sql && grep -c "chk_rvm_preview_category" /c/Users/admin/Documents/Team4s/database/migrations/0059_release_version_media_schema.up.sql && grep -c "idx_rvm_public" /c/Users/admin/Documents/Team4s/database/migrations/0059_release_version_media_schema.up.sql && grep -c "chk_media_assets_status" /c/Users/admin/Documents/Team4s/database/migrations/0059_release_version_media_schema.up.sql && grep -c "chk_media_files_status" /c/Users/admin/Documents/Team4s/database/migrations/0059_release_version_media_schema.up.sql</automated>
  </verify>
  <acceptance_criteria>
    - Datei `database/migrations/0059_release_version_media_schema.up.sql` existiert
    - `grep "CREATE TABLE release_version_media"` findet genau 1 Treffer
    - `grep "chk_rvm_category"` findet genau 1 Treffer
    - `grep "chk_rvm_preview_category"` findet genau 1 Treffer
    - `grep "idx_rvm_public"` findet genau 1 Treffer mit WHERE-Klausel `WHERE deleted_at IS NULL`
    - `grep "chk_media_assets_status"` findet genau 1 Treffer
    - `grep "chk_media_files_status"` findet genau 1 Treffer
    - `grep "REFERENCES release_versions(id)"` findet einen Treffer
    - `grep "REFERENCES media_assets(id)"` findet einen Treffer
    - `grep "idx_media_assets_status"` findet genau 1 Treffer
    - `grep "idx_media_files_status"` findet genau 1 Treffer
  </acceptance_criteria>
  <done>Up-Migration enthält alle 3 Abschnitte (CREATE TABLE + 4 Indexe, ALTER media_assets + Index, ALTER media_files + Index) mit korrekten Constraint-Namen aus CONTEXT.md.</done>
</task>

<task type="auto">
  <name>Task 2: Down-Migration 0059 schreiben</name>
  <files>database/migrations/0059_release_version_media_schema.down.sql</files>
  <read_first>
    - database/migrations/0059_release_version_media_schema.up.sql (up-Migration, die diese down-Migration exakt umkehren muss)
    - database/migrations/0058_rename_theme_types_kara.down.sql (Stil der down-Migrationsdatei)
    - database/migrations/0026_add_media_tables.up.sql (zeigt welche Indexe auf media_files existieren — für DROP INDEX Reihenfolge)
  </read_first>
  <action>
Erstelle `database/migrations/0059_release_version_media_schema.down.sql` mit exakt diesem Inhalt:

```sql
-- Migration 0059 down: Revert release_version_media schema foundation

-- 3. Revert: status-Spalte aus media_files entfernen
DROP INDEX IF EXISTS idx_media_files_status;
ALTER TABLE media_files DROP CONSTRAINT IF EXISTS chk_media_files_status;
ALTER TABLE media_files DROP COLUMN IF EXISTS status;

-- 2. Revert: status-Spalte aus media_assets entfernen
DROP INDEX IF EXISTS idx_media_assets_status;
ALTER TABLE media_assets DROP CONSTRAINT IF EXISTS chk_media_assets_status;
ALTER TABLE media_assets DROP COLUMN IF EXISTS status;

-- 1. Revert: release_version_media-Tabelle droppen (Indexe werden kaskadierend gedroppt)
DROP TABLE IF EXISTS release_version_media;
```

Reihenfolge: umgekehrte Reihenfolge der Up-Migration (3 → 2 → 1).
Für `release_version_media` reicht ein `DROP TABLE IF EXISTS` — die Indexe werden durch PostgreSQL automatisch mit der Tabelle gelöscht.
Für ALTER TABLE media_assets/media_files: erst Index droppen, dann Constraint droppen, dann Spalte droppen.
  </action>
  <verify>
    <automated>grep -c "DROP TABLE IF EXISTS release_version_media" /c/Users/admin/Documents/Team4s/database/migrations/0059_release_version_media_schema.down.sql && grep -c "DROP COLUMN IF EXISTS status" /c/Users/admin/Documents/Team4s/database/migrations/0059_release_version_media_schema.down.sql</automated>
  </verify>
  <acceptance_criteria>
    - Datei `database/migrations/0059_release_version_media_schema.down.sql` existiert
    - `grep "DROP TABLE IF EXISTS release_version_media"` findet genau 1 Treffer
    - `grep "DROP COLUMN IF EXISTS status"` findet genau 2 Treffer (für media_files und media_assets)
    - `grep "DROP CONSTRAINT IF EXISTS chk_media_files_status"` findet genau 1 Treffer
    - `grep "DROP CONSTRAINT IF EXISTS chk_media_assets_status"` findet genau 1 Treffer
    - `grep "DROP INDEX IF EXISTS idx_media_files_status"` findet genau 1 Treffer
    - `grep "DROP INDEX IF EXISTS idx_media_assets_status"` findet genau 1 Treffer
    - Die Reihenfolge ist umgekehrt zur Up-Migration (media_files zuerst zurückgesetzt, dann media_assets, dann release_version_media gedroppt)
  </acceptance_criteria>
  <done>Down-Migration setzt exakt alle Änderungen der Up-Migration zurück: Tabelle wird gedroppt, beide status-Spalten mit Constraints und Indexen werden aus media_files und media_assets entfernt.</done>
</task>

</tasks>

<verification>
Nach Abschluss beider Tasks:

1. Beide Migrationsdateien existieren:
   - `database/migrations/0059_release_version_media_schema.up.sql`
   - `database/migrations/0059_release_version_media_schema.down.sql`

2. Strukturprüfung Up-Migration:
   ```bash
   grep -E "(CREATE TABLE|ALTER TABLE|ADD COLUMN|ADD CONSTRAINT|CREATE INDEX)" \
     database/migrations/0059_release_version_media_schema.up.sql
   ```
   Erwartet: 1x CREATE TABLE, 4x CREATE INDEX für rvm_*, 2x ALTER TABLE ADD COLUMN, 2x ALTER TABLE ADD CONSTRAINT, 2x CREATE INDEX für media_*

3. FK-Referenzen korrekt:
   ```bash
   grep "REFERENCES release_versions" database/migrations/0059_release_version_media_schema.up.sql
   grep "REFERENCES media_assets" database/migrations/0059_release_version_media_schema.up.sql
   ```

4. Constraint-Namen aus CONTEXT.md vorhanden:
   ```bash
   grep -E "(chk_rvm_category|chk_rvm_preview_category|chk_media_assets_status|chk_media_files_status)" \
     database/migrations/0059_release_version_media_schema.up.sql | wc -l
   ```
   Erwartet: 4

5. Preview-Rule-Constraint korrekt:
   ```bash
   grep "chk_rvm_preview_category" database/migrations/0059_release_version_media_schema.up.sql
   ```
   Erwartet: `CHECK (is_preview_candidate = false OR category IN ('screenshot', 'typesetting_karaoke'))`

6. Down-Migration vollständig:
   ```bash
   grep -c "IF EXISTS" database/migrations/0059_release_version_media_schema.down.sql
   ```
   Erwartet: mindestens 7 (1x DROP TABLE, 2x DROP INDEX, 2x DROP CONSTRAINT, 2x DROP COLUMN)
</verification>

<success_criteria>
- `database/migrations/0059_release_version_media_schema.up.sql` existiert mit CREATE TABLE release_version_media, allen 4 rvm-Indexen, beiden ALTER TABLE ADD COLUMN status und beiden CHECK-Constraints
- `database/migrations/0059_release_version_media_schema.down.sql` existiert und kehrt alle Up-Änderungen um
- Constraint-Namen exakt: `chk_rvm_category`, `chk_rvm_preview_category`, `chk_media_assets_status`, `chk_media_files_status`
- Index-Namen exakt: `idx_rvm_release_version`, `idx_rvm_media_asset`, `idx_rvm_category`, `idx_rvm_public`, `idx_media_assets_status`, `idx_media_files_status`
- FK auf release_versions(id) mit ON DELETE CASCADE
- FK auf media_assets(id) mit ON DELETE RESTRICT
- is_preview_candidate-Constraint deckt nur screenshot und typesetting_karaoke ab
- Kein Backend-Code, kein Frontend-Code berührt
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/34-release-version-media-schema-foundation/34-01-SUMMARY.md` mit:
- Was wurde implementiert (beide SQL-Dateien, alle Constraints/Indexe)
- Entscheidungen, die festgehalten wurden (ON DELETE RESTRICT vs CASCADE für media_asset_id)
- Phase-35-Handoff: welche Tabelle/Spalten jetzt verfügbar sind
</output>
