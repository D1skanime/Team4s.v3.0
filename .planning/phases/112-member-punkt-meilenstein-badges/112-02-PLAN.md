---
phase: 112-member-punkt-meilenstein-badges
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/profile/memberBadgeLabels.ts
  - frontend/src/components/profile/memberBadgeLabels.test.ts
autonomous: true
requirements: [GAM-04]
must_haves:
  truths:
    - "D-01: Die 6 Punktschwellen (1/50/200/500/1000/2500) sind selbst die Meilenstein-Stufen; 200 Punkte → eindeutig 'Erfahrener Mitwirkender', kein zusätzliches Bronze/Silber/Gold."
    - "D-03: deriveMilestoneBadge liefert NUR den höchsten erreichten Meilenstein (Einzahl) und null unter 1 Punkt — keine Kette."
    - "D-04: Der dynamische Resolver löst role_volume_<roleCode>_<tier>-Codes ohne hartcodierte Rollenliste auf (Rollenname via FANSUB_GROUP_ROLE_OPTIONS), roleCode ist der geparste Rollencode als Merge-Schlüssel."
    - "GAM-04: Beide Ableitungen sind reine Read-time-Projektionen ohne Persistenz/Punktvergabe."
  artifacts:
    - path: "frontend/src/components/profile/memberBadgeLabels.ts"
      provides: "6 statische point_milestone_* Presentations, MemberBadgePalette-Erweiterung, dynamischer role_volume_-Resolver, deriveMilestoneBadge"
      contains: "deriveMilestoneBadge"
    - path: "frontend/src/components/profile/memberBadgeLabels.test.ts"
      provides: "Unit-Tests für deriveMilestoneBadge-Grenzwerte + resolveRoleVolumePresentation-Parsing"
      contains: "deriveMilestoneBadge"
  key_links:
    - from: "getMemberBadgePresentation"
      to: "resolveRoleVolumePresentation"
      via: "Parsing-Branch vor dem statischen Map-Fallback"
      pattern: "role_volume_"
    - from: "resolveRoleVolumePresentation"
      to: "FANSUB_GROUP_ROLE_OPTIONS"
      via: "Rollencode → deutsches Rollenlabel"
      pattern: "FANSUB_GROUP_ROLE_OPTIONS"
---

<objective>
Erweitere `memberBadgeLabels.ts` um die reine Präsentations-/Ableitungslogik beider neuer Badge-Familien:
Typ 2 (6 statische Punkt-Meilenstein-Einträge + `deriveMilestoneBadge`) und die Typ-3-Auflösung
(`MemberBadgePalette`-Erweiterung + dynamischer `resolveRoleVolumePresentation`-Branch für
`role_volume_<roleCode>_<tier>`-Codes). Kein Rendering, keine SSR-Verdrahtung — das ist Plan 112-03.

Purpose: Die Katalog-/Resolver-Daten sind die gemeinsame Grundlage, die 112-03 (Rendering) importiert
und die Backend-Typ-3-Codes aus 112-01 clientseitig in deutsche Labels/Paletten/Icons übersetzt. Als
reine Utility-Datei ist sie unabhängig unit-testbar (kein jsdom/RTL nötig).
Output: erweiterte memberBadgeLabels.ts + neuer memberBadgeLabels.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/112-member-punkt-meilenstein-badges/112-CONTEXT.md
@.planning/phases/112-member-punkt-meilenstein-badges/112-UI-SPEC.md
@.planning/phases/112-member-punkt-meilenstein-badges/112-PATTERNS.md
@.planning/phases/112-member-punkt-meilenstein-badges/112-RESEARCH.md

<interfaces>
<!-- Verified current shapes (working tree, 2026-07-28, memberBadgeLabels.ts = 109 lines). -->

MemberBadgePalette (line 22, current):
  export type MemberBadgePalette = 'gold' | 'indigo' | 'orange' | 'mint' | 'red'
  // Phase 112 delta: add | 'bronze' | 'silver' | 'platinum'

MemberBadgePresentation shape (has: label, variant, Icon, palette, group, roleCode?)
  group is MemberBadgeGroup ('roles' | 'progress' | 'membership' | 'special')
  roleCode?: string — set only for roles-group; buildMemberBadgeGroups merges same-roleCode into one row.

Existing static entry shape (MEMBER_BADGE_PRESENTATIONS, e.g. lines 55/49):
  role_entry_translator: { label: 'Erste Übersetzung', variant: 'info', Icon: Languages, palette: 'indigo', group: 'roles', roleCode: 'translator' }
  first_contribution: { label: 'Erste Mitwirkung', variant: 'neutral', Icon: Sparkles, palette: 'mint', group: 'progress' }

Existing resolver fallback (lines ~99-108, current):
  export function getMemberBadgePresentation(badgeCode: string): MemberBadgePresentation {
    return (MEMBER_BADGE_PRESENTATIONS[badgeCode] ?? { label: badgeCode, variant: 'neutral', Icon: Sparkles, palette: 'mint', group: 'special' })
  }

Role labels (single source of truth — frontend/src/types/fansub.ts lines ~436-449, correct-umlaut German):
  FANSUB_GROUP_ROLE_OPTIONS: [{ code, label }] — e.g. translator→'Übersetzung', quality_checker→'Qualitätsprüfung', raw_provider→'Raw-Bereitstellung', project_lead→'Projektleitung', ... (12 codes)

PublicMemberBadge (frontend/src/types/profile.ts): { id, badge_code, badge_category }
Analog test file for a pure helper: frontend/src/components/profile/deriveKnownFor.test.ts
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Typ-2 statische Meilenstein-Einträge + deriveMilestoneBadge (Gruppe „Fortschritt")</name>
  <files>frontend/src/components/profile/memberBadgeLabels.ts, frontend/src/components/profile/memberBadgeLabels.test.ts</files>
  <read_first>
    - frontend/src/components/profile/memberBadgeLabels.ts (kompletter Bestand: Icon-Imports, MEMBER_BADGE_PRESENTATIONS, PUBLIC_MEMBER_BADGE_CATALOG, getMemberBadgePresentation)
    - frontend/src/components/profile/deriveKnownFor.test.ts (Pure-Function-Vitest-Analog, gleiches Verzeichnis)
    - frontend/src/types/profile.ts (PublicMemberBadge Typ, total_points)
    - .planning/phases/112-member-punkt-meilenstein-badges/112-UI-SPEC.md (Typ-2-Tabelle, „Kein Locked-Zustand für Typ 2")
  </read_first>
  <behavior>
    - deriveMilestoneBadge(0) === null
    - deriveMilestoneBadge(1).badge_code === 'point_milestone_first'
    - deriveMilestoneBadge(49).badge_code === 'point_milestone_first'
    - deriveMilestoneBadge(50).badge_code === 'point_milestone_active'
    - deriveMilestoneBadge(199).badge_code === 'point_milestone_active'
    - deriveMilestoneBadge(200).badge_code === 'point_milestone_experienced'
    - deriveMilestoneBadge(2500).badge_code === 'point_milestone_legend'
    - deriveMilestoneBadge(2501).badge_code === 'point_milestone_legend'
    - deriveMilestoneBadge(x) gibt { id: 0, badge_code, badge_category: 'progress' } zurück
  </behavior>
  <action>
    Ergänze die Icon-Import-Zeile um `Flag, Flame, Award, Medal, Trophy, Gem` (aus lucide-react; keine
    Kollision mit den 16 bestehenden Icons).

    Füge zu MEMBER_BADGE_PRESENTATIONS GENAU die 6 statischen Einträge aus der UI-SPEC-Typ-2-Tabelle
    hinzu, jeweils mit `group: 'progress'` und korrekten Umlauten:
    point_milestone_first (Label „Erster Beitrag", Icon Flag, variant muted, palette mint),
    point_milestone_active (Label „Aktiver Mitwirkender", Icon Flame, variant neutral, palette mint),
    point_milestone_experienced (Label „Erfahrener Mitwirkender", Icon Award, variant success, palette orange),
    point_milestone_engaged (Label „Engagierter Mitwirkender", Icon Medal, variant success, palette orange),
    point_milestone_veteran (Label „Veteran", Icon Trophy, variant warning, palette gold),
    point_milestone_legend (Label „Archiv-Legende", Icon Gem, variant warning, palette gold).
    NUR in die statische Map — füge sie NICHT zu PUBLIC_MEMBER_BADGE_CATALOG hinzu (D-03 „keine Kette":
    im Katalog würden bis zu 5 gesperrte Meilenstein-Chips gleichzeitig erscheinen; der eine erreichte
    fließt zur Laufzeit über den earned-but-not-in-catalog-Fallback ein).

    Definiere die absteigend sortierte Konstante POINT_MILESTONES als Array von { threshold, badge_code }
    mit den Paaren 2500→point_milestone_legend, 1000→point_milestone_veteran, 500→point_milestone_engaged,
    200→point_milestone_experienced, 50→point_milestone_active, 1→point_milestone_first.

    Exportiere die pure Funktion `deriveMilestoneBadge(totalPoints: number): PublicMemberBadge | null`:
    `const hit = POINT_MILESTONES.find((m) => totalPoints >= m.threshold)`, gib bei Treffer
    `{ id: 0, badge_code: hit.badge_code, badge_category: 'progress' }` zurück, sonst `null`. Gleiche
    „highest reached step"-Form wie der Go-`highestRoleVolumeTier` (RESEARCH Don't-Hand-Roll) — identischer
    Vergleichsstil, kein zweiter Ableitungsweg.

    Erstelle memberBadgeLabels.test.ts (Analog deriveKnownFor.test.ts) mit allen Grenzwert-Assertions aus
    <behavior> plus einer Assertion, dass badge_category 'progress' und id 0 ist.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- memberBadgeLabels</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "point_milestone_" memberBadgeLabels.ts` >= 6 (statische Einträge) und die 6 Codes tauchen NICHT in PUBLIC_MEMBER_BADGE_CATALOG auf.
    - Alle 6 Labels mit korrekten Umlauten (keine ae/oe/ue-Ersetzung).
    - deriveMilestoneBadge exportiert; alle <behavior>-Grenzwerte grün via `npm run test -- memberBadgeLabels`.
    - POINT_MILESTONES ist absteigend sortiert; deriveMilestoneBadge(0) === null.
    - Icons Flag/Flame/Award/Medal/Trophy/Gem importiert; `npm run typecheck` (nach Task 2) sauber.
  </acceptance_criteria>
  <done>6 Typ-2-Presentations + deriveMilestoneBadge existieren, Grenzwert-Tests grün, kein Katalog-Locked-Zustand.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MemberBadgePalette-Erweiterung + dynamischer role_volume_-Resolver (Typ 3)</name>
  <files>frontend/src/components/profile/memberBadgeLabels.ts, frontend/src/components/profile/memberBadgeLabels.test.ts</files>
  <read_first>
    - frontend/src/components/profile/memberBadgeLabels.ts (MemberBadgePalette Zeile 22, getMemberBadgePresentation Fallback ~99-108)
    - frontend/src/types/fansub.ts (FANSUB_GROUP_ROLE_OPTIONS, ~436-449)
    - .planning/phases/112-member-punkt-meilenstein-badges/112-UI-SPEC.md (Typ-3-Tier-Tabelle, Chip-Label-Formel)
    - .planning/phases/112-member-punkt-meilenstein-badges/112-PATTERNS.md (resolveRoleVolumePresentation-Delta, Multi-Underscore-Rollencodes)
  </read_first>
  <behavior>
    - getMemberBadgePresentation('role_volume_translator_gold') → { label: 'Gold · 320+', group: 'roles', roleCode: 'translator', palette: 'gold' }
    - getMemberBadgePresentation('role_volume_quality_checker_bronze') → roleCode === 'quality_checker' (Multi-Underscore korrekt), label 'Bronze · 12+'
    - getMemberBadgePresentation('role_volume_translator_platinum') → label 'Platin · 510+', palette 'platinum', Icon Gem
    - getMemberBadgePresentation('role_volume_translator_silver') → label 'Silber · 108+', palette 'silver'
    - getMemberBadgePresentation('role_volume_unknownrole_gold') → roleCode 'unknownrole', label 'Gold · 320+' (Fallback: roher role_code als Rollenname bleibt für Präfix nutzbar; Resolver wirft nicht)
    - getMemberBadgePresentation('founding_member') → unverändert aus statischer Map (kein Regress)
  </behavior>
  <action>
    Erweitere `MemberBadgePalette` (Zeile 22) um `| 'bronze' | 'silver' | 'platinum'` ('gold' bleibt
    für die Gold-Stufe wiederverwendet).

    Implementiere `resolveRoleVolumePresentation(badgeCode: string): MemberBadgePresentation`: entferne
    das führende `role_volume_`-Präfix, ermittle das Tier durch Prüfung, mit welchem der bekannten
    Tier-Suffixe (`_bronze` | `_silver` | `_gold` | `_platinum`) der Reststring endet, und schneide es
    ab, um den Rollencode zu erhalten. WICHTIG: Rollencodes enthalten selbst Unterstriche
    (quality_checker, raw_provider, project_lead) — ein naives split('_') ist verboten; splitte NUR das
    bekannte Tier-Suffix ab. Mappe den geparsten Rollencode via FANSUB_GROUP_ROLE_OPTIONS (import aus
    `@/types/fansub`) auf das deutsche Rollenlabel; Fallback: roher Rollencode-String, falls kein Treffer
    (defensiv, gleiches Muster wie der bestehende Fallback; RESEARCH Open Question 2). Verwende die Maps
    Tier→Label { bronze:'Bronze', silver:'Silber', gold:'Gold', platinum:'Platin' } und
    Tier→Schwelle { bronze:12, silver:108, gold:320, platinum:510 }. Rückgabe:
    { label: `${tierLabel} · ${threshold}+` (Trenner U+00B7 wie im bestehenden „Produktiv · 10+ Anime"),
    variant je Tier (bronze muted, silver neutral, gold warning, platinum info), Icon (bronze/silver/gold
    → Medal, platinum → Gem), palette (bronze/silver/gold/platinum), group: 'roles',
    roleCode: <geparster Rollencode> }. Der roleCode MUSS der geparste Rollencode sein (nicht der volle
    BadgeCode) — das ist der Merge-Schlüssel für buildMemberBadgeGroups in 112-03.

    Füge in getMemberBadgePresentation VOR dem statischen Map-Lookup einen Parsing-Branch ein:
    `if (badgeCode.startsWith('role_volume_')) return resolveRoleVolumePresentation(badgeCode)`. Der
    bestehende statische Fallback bleibt danach unverändert (kein Regress für founding_member etc.).

    Ergänze memberBadgeLabels.test.ts um die <behavior>-Fälle (Tier-Parse, Multi-Underscore-roleCode,
    Unknown-Role-Fallback, Nicht-Regress der statischen Map).
  </action>
  <verify>
    <automated>cd frontend && npm run test -- memberBadgeLabels && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - MemberBadgePalette enthält 'bronze', 'silver', 'platinum'.
    - resolveRoleVolumePresentation parst `role_volume_quality_checker_bronze` zu roleCode 'quality_checker' (kein naives split('_')).
    - Alle Tier-Labels korrekt deutsch: Bronze/Silber/Gold/Platin; Chip-Label-Formel `{Tier} · {Schwelle}+` mit U+00B7-Trenner.
    - getMemberBadgePresentation-Branch trifft `role_volume_`-Codes vor dem statischen Fallback; statische Codes (founding_member) unverändert.
    - `npm run test -- memberBadgeLabels` grün; `npm run typecheck` sauber.
    - `wc -l memberBadgeLabels.ts` bleibt < 450 (Line-Limit-Sanity-Check).
  </acceptance_criteria>
  <done>Palette erweitert, dynamischer role_volume_-Resolver liefert deutsche Tier-Labels + geparsten roleCode; Tests grün, typecheck sauber.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend (role_volume_-Codes) → Frontend-Resolver | Maschinenlesbare Codes werden clientseitig in deutsche Labels/Paletten übersetzt; keine Vertrauensentscheidung, reine Anzeige-Ableitung. |
| SSR-Render → öffentliche Profil-Anzeige | Reine Leseansicht, keine Eingabe, keine neuen Controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-05 | Tampering (Anzeige-Integrität) | deriveMilestoneBadge Pure-Function | mitigate | Reine Funktion ohne State/Cache; bei jedem Render neu ausgewertet, sodass eine gesunkene total_points-Summe den Meilenstein sofort senkt (D-02). Grenzwert-Unit-Tests fixieren die Schwellen-Eindeutigkeit (D-01). |
| T-112-06 | Denial of Service / Robustheit | resolveRoleVolumePresentation Parsing | mitigate | Unbekannte Rollencodes fallen defensiv auf den rohen String zurück (kein throw, kein Verstecken) — mirror des bestehenden getMemberBadgePresentation-Fallbacks; Test deckt Unknown-Role ab. |
| T-112-07 | Information Disclosure | Tier-Label statt Rohzahl | accept | Nur gerundete Schwelle („320+") wird angezeigt, nicht die exakte Credit-Zahl — konsistent mit Backend-Disposition T-112-03. |
| T-112-SC | Tampering (Supply Chain) | npm install / neue Pakete | accept | Keine neuen Pakete — nur zusätzliche Icon-Namen aus dem bereits importierten lucide-react (RESEARCH Package Legitimacy Audit: N/A). Kein slopcheck-Gate nötig. |
</threat_model>

<verification>
- `cd frontend && npm run test -- memberBadgeLabels` grün (deriveMilestoneBadge-Grenzwerte + Resolver-Parsing).
- `cd frontend && npm run typecheck` sauber.
- Line-Count-Sanity: memberBadgeLabels.ts < 450 Zeilen (PATTERNS/RESEARCH: bleibt komfortabel darunter, aber prüfen).
- Alle neuen deutschen Labels mit korrekten Umlauten (CLAUDE.md).
</verification>

<success_criteria>
- Typ 2: 6 statische point_milestone_*-Presentations (nur Map, nicht Katalog) + deriveMilestoneBadge liefert höchsten erreichten Meilenstein / null (D-01, D-03).
- Typ 3: MemberBadgePalette + dynamischer role_volume_-Resolver übersetzt Backend-Codes in deutsche Tier-Labels/Paletten/Icons mit geparstem roleCode als Merge-Schlüssel (D-04).
- Alle Ableitungen rein, unabhängig unit-getestet, keine Persistenz (GAM-04).
</success_criteria>

<output>
Create `.planning/phases/112-member-punkt-meilenstein-badges/112-02-SUMMARY.md` when done
</output>
