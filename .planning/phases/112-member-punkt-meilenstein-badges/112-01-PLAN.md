---
phase: 112-member-punkt-meilenstein-badges
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/repository/member_profile_role_volume_repository.go
  - backend/internal/repository/member_profile_repository.go
  - backend/internal/repository/member_profile_repository_postgres_test.go
autonomous: true
requirements: [GAM-04]
must_haves:
  truths:
    - "D-04: Pro punktfähiger Rolle wird die Netto-Anzahl awarded release_role_credit_lifecycles gezählt (keine hartcodierte Rollenliste) und nur die höchste erreichte Stufe (Bronze 12 / Silber 108 / Gold 320 / Platin 510) als abgeleitetes Badge emittiert."
    - "D-02: Storniert (lifecycle_status != 'awarded') zählt nicht; fällt die Netto-Zahl unter eine Schwelle, verschwindet/senkt sich das Badge beim nächsten Read (kein Cache, ID:0, kein Schreibpfad)."
    - "GAM-04: Typ-3-Badges sind read-time-computed und werden nie in member_badges persistiert — reine abgeleitete Projektion, kein neuer Buchungspfad."
  artifacts:
    - path: "backend/internal/repository/member_profile_role_volume_repository.go"
      provides: "loadRoleVolumeBadges COUNT/GROUP-BY-Projektion + highestRoleVolumeTier Schwellen-Helfer"
      contains: "func (r *MemberProfileRepository) loadRoleVolumeBadges"
    - path: "backend/internal/repository/member_profile_repository.go"
      provides: "Call-Site, das Typ-3-Badges an profile.PublicBadges anhängt"
      contains: "loadRoleVolumeBadges"
    - path: "backend/internal/repository/member_profile_repository_postgres_test.go"
      provides: "Tier-Grenz-Unit-Test + Postgres Live-Projektions-Integrationstest"
      contains: "TestHighestRoleVolumeTier"
  key_links:
    - from: "backend/internal/repository/member_profile_repository.go GetPublicMemberProfile"
      to: "loadRoleVolumeBadges"
      via: "append in profile.PublicBadges nach loadPublicBadges"
      pattern: "append\\(profile.PublicBadges"
    - from: "loadRoleVolumeBadges"
      to: "release_role_credit_lifecycles"
      via: "COUNT(*) ... WHERE lifecycle_status='awarded' GROUP BY role_code"
      pattern: "GROUP BY role_code"
---

<objective>
Füge die Backend-Datenschicht für Typ-3-Rollen-Volumen-Badges (D-04) hinzu: eine rollen-gefilterte
Netto-Zählung der `release_role_credit_lifecycles`-Buchungen, die pro Rolle nur die höchste erreichte
Volumenstufe (Bronze 12 / Silber 108 / Gold 320 / Platin 510) als synthetisches, nie persistiertes
`PublicMemberBadge` emittiert.

Purpose: Die rohe Pro-Rolle-Credit-Zahl existiert bisher nirgends (Typ 1 prüft nur EXISTENZ per
`DISTINCT role_code`). Diese Zählung ist die einzige neue Backend-Abhängigkeit der Phase; Typ 2
(Punkt-Meilensteine) bleibt reine Frontend-Ableitung (Plan 112-02/112-03).
Output: neues Repository-Split-File + 1-Zeilen-Call-Site + Go-Unit- und Postgres-Integrationstest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/112-member-punkt-meilenstein-badges/112-CONTEXT.md
@.planning/phases/112-member-punkt-meilenstein-badges/112-RESEARCH.md
@.planning/phases/112-member-punkt-meilenstein-badges/112-PATTERNS.md
@.planning/phases/112-member-punkt-meilenstein-badges/112-VALIDATION.md

<interfaces>
<!-- Verified current shapes (working tree, 2026-07-28). Executor uses these directly. -->

Model (backend/internal/models/member_profile.go, unchanged — Typ 3 reuses it):
  type PublicMemberBadge struct {
    ID            int64  `json:"id"`
    BadgeCode     string `json:"badge_code"`
    BadgeCategory string `json:"badge_category"`
  }
  // profile.PublicBadges []PublicMemberBadge `json:"public_badges"`

Analog to mirror — the live role-entry UNION inside loadPublicBadges
(backend/internal/repository/member_profile_repository.go lines ~597-616):
  SELECT DISTINCT role_code FROM release_role_credit_lifecycles
  WHERE member_id = $1 AND lifecycle_status = 'awarded' ORDER BY role_code
  -> items = append(items, models.PublicMemberBadge{ID: 0, BadgeCode: "role_entry_"+roleCode, BadgeCategory: "role_entry"})

Call-site analog (member_profile_repository.go lines ~531-538):
  profile.PublicBadges, loadErr = r.loadPublicBadges(ctx, row.memberID)
  if loadErr != nil { return nil, loadErr }
  profile.TotalPoints, loadErr = r.loadTotalPoints(ctx, row.memberID)
  if loadErr != nil { return nil, loadErr }

Split-file convention analog: backend/internal/repository/member_profile_story_image_repository.go
  (101 lines, package repository, extension method on *MemberProfileRepository, shares pool via r.db)

Schema (database/migrations/0137_phase108_contribution_sources.up.sql):
  release_role_credit_lifecycles(release_version_id, fansub_group_id, member_id, role_code,
    generation, lifecycle_status IN ('pending','awarded','reversed'), ...
    UNIQUE(release_version_id, fansub_group_id, member_id, role_code, generation))

Test helpers already present (member_profile_repository_postgres_test.go):
  openMemberProfileBadgeLifecyclePostgres(t) *pgxpool.Pool   // applies 0137 migration to disposable schema
  insertRoleEntryLifecycleRow(t, pool, memberID, roleCode, generation, lifecycleStatus, awardEntryID, reversalEntryID)  // seeds release_version_id=30, fansub_group_id=20
  containsPublicBadge(badges, badgeCode, badgeCategory) bool
  Naming convention: TestLoadPublicBadgesPostgres*
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Neues Split-File member_profile_role_volume_repository.go mit loadRoleVolumeBadges + highestRoleVolumeTier</name>
  <files>backend/internal/repository/member_profile_role_volume_repository.go, backend/internal/repository/member_profile_repository_postgres_test.go</files>
  <read_first>
    - backend/internal/repository/member_profile_repository.go (loadPublicBadges role-entry UNION, lines ~566-618 — die exakte Query-/Scan-/Emit-Form zum Spiegeln)
    - backend/internal/repository/member_profile_story_image_repository.go (Split-File-Konvention: package/imports/extension-method-Header)
    - backend/internal/models/member_profile.go (PublicMemberBadge struct, ~lines 187-191)
    - backend/internal/repository/member_profile_repository_postgres_test.go (Test-Namensmuster + Helfer)
    - .planning/phases/112-member-punkt-meilenstein-badges/112-PATTERNS.md (Abschnitt member_profile_role_volume_repository.go)
  </read_first>
  <behavior>
    - highestRoleVolumeTier(11) == "" ; highestRoleVolumeTier(12) == "bronze"
    - highestRoleVolumeTier(107) == "bronze" ; highestRoleVolumeTier(108) == "silver"
    - highestRoleVolumeTier(319) == "silver" ; highestRoleVolumeTier(320) == "gold"
    - highestRoleVolumeTier(509) == "gold" ; highestRoleVolumeTier(510) == "platinum"
    - highestRoleVolumeTier(0) == ""
  </behavior>
  <action>
    Erstelle die NEUE Datei backend/internal/repository/member_profile_role_volume_repository.go im
    `package repository`. Baue NICHT in member_profile_repository.go ein (Datei ist bereits 1875 Zeilen,
    4× über dem 450-Limit — RESEARCH Pitfall 3).

    Implementiere die pure Funktion `highestRoleVolumeTier(count int) string` per switch: count >= 510
    → "platinum", >= 320 → "gold", >= 108 → "silver", >= 12 → "bronze", sonst "". Tier-Tokens sind
    intern-englisch (bronze/silver/gold/platinum) — konsistent mit dem bestehenden
    `productive_bronze/silver/gold`-Code-Präzedenzfall; das deutsche Label `Platin` wird clientseitig
    aufgelöst (Plan 112-02), Backend emittiert nur maschinenlesbare Codes (keine deutschen Strings über
    die Go/Client-Grenze).

    Implementiere die Extension-Methode `func (r *MemberProfileRepository) loadRoleVolumeBadges(ctx
    context.Context, memberID int64) ([]models.PublicMemberBadge, error)`: `r.db.Query` mit
    `SELECT role_code, COUNT(*) AS credit_count FROM release_role_credit_lifecycles WHERE member_id = $1
    AND lifecycle_status = 'awarded' GROUP BY role_code ORDER BY role_code`. Scanne `(roleCode string,
    count int)`; für jede Zeile mit `tier := highestRoleVolumeTier(count); tier != ""` hänge
    `models.PublicMemberBadge{ID: 0, BadgeCode: "role_volume_" + roleCode + "_" + tier, BadgeCategory:
    "role_volume"}` an. Verwende das Error-Wrapping-Idiom `fmt.Errorf("<verb> role-volume badges for
    member %d: %w", memberID, err)` an Query/Scan/Err-Schritten, `defer rows.Close()`. Initialisiere den
    Rückgabe-Slice als `make([]models.PublicMemberBadge, 0)`. ID:0 und die nie-persistiert/frisch-je-Read-
    Semantik werden verbatim übernommen — das erfüllt GAM-04 + D-02 Live-Projektion.

    Füge in member_profile_repository_postgres_test.go den reinen Go-Unit-Test `TestHighestRoleVolumeTier`
    hinzu, der alle Grenzwerte aus dem <behavior>-Block abdeckt (11/12, 107/108, 319/320, 509/510, 0).
    Dieser Test ist von Postgres unabhängig und deckt die D-04-Schwellenlogik direkt ab (VALIDATION
    112-BE-01) — verlasse dich NICHT auf Seed-Daten, die Gold/Platin natürlich erreichen (Pitfall 2).
  </action>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/repository/... -run TestHighestRoleVolumeTier</automated>
  </verify>
  <acceptance_criteria>
    - Datei backend/internal/repository/member_profile_role_volume_repository.go existiert, `package repository`, keine Query in member_profile_repository.go verschoben.
    - `grep -c "func " member_profile_role_volume_repository.go` >= 2 (loadRoleVolumeBadges + highestRoleVolumeTier).
    - SQL enthält `lifecycle_status = 'awarded'` und `GROUP BY role_code`; emittierter BadgeCode-Präfix ist `role_volume_`, BadgeCategory ist `role_volume`, ID ist 0.
    - Tier-Tokens sind exakt `bronze`/`silver`/`gold`/`platinum` (nicht `platin`).
    - `go test ./internal/repository/... -run TestHighestRoleVolumeTier` grün an allen 9 Grenzwerten.
    - `go build ./...` sauber.
  </acceptance_criteria>
  <done>loadRoleVolumeBadges + highestRoleVolumeTier existieren in einem neuen Split-File; TestHighestRoleVolumeTier grün.</done>
</task>

<task type="auto">
  <name>Task 2: Call-Site verdrahten + Postgres Live-Projektions-Integrationstest</name>
  <files>backend/internal/repository/member_profile_repository.go, backend/internal/repository/member_profile_repository_postgres_test.go</files>
  <read_first>
    - backend/internal/repository/member_profile_repository.go (Call-Site-Block loadPublicBadges/loadTotalPoints, lines ~531-538)
    - backend/internal/repository/member_profile_repository_postgres_test.go (TestLoadPublicBadgesPostgresRoleEntryReversedHidden lines ~131-164, insertRoleEntryLifecycleRow ~68-88, containsPublicBadge ~90-97)
    - .planning/phases/112-member-punkt-meilenstein-badges/112-PATTERNS.md (Call-Site + Postgres-Test-Delta)
    - .planning/phases/112-member-punkt-meilenstein-badges/112-VALIDATION.md (112-BE-02, Manual-Only Gold/Platin-Hinweis)
  </read_first>
  <action>
    Füge in member_profile_repository.go GENAU EINEN Block direkt nach der bestehenden
    `profile.PublicBadges, loadErr = r.loadPublicBadges(...)`-Zuweisung ein: rufe
    `volumeBadges, loadErr := r.loadRoleVolumeBadges(ctx, row.memberID)` auf, behalte das exakte
    Idiom `if loadErr != nil { return nil, loadErr }`, dann `profile.PublicBadges =
    append(profile.PublicBadges, volumeBadges...)`. Dies ist die EINZIGE Änderung an der 1875-Zeilen-
    Datei — ein `git diff --stat` mit mehr als wenigen Zeilen hier ist das Pitfall-3-Warnsignal.

    Füge in member_profile_repository_postgres_test.go den Integrationstest
    `TestLoadPublicBadgesPostgresRoleVolume` hinzu (Namensmuster TestLoadPublicBadgesPostgres*). Nutze
    `openMemberProfileBadgeLifecyclePostgres(t)`. Seede für einen Member 12 awarded Credits EINER Rolle
    (z. B. `translator`) über `insertRoleEntryLifecycleRow` — variiere den `generation`-Parameter
    (oder release_version_id), um die UNIQUE(release_version_id, fansub_group_id, member_id, role_code,
    generation)-Constraint zu erfüllen; füge NICHT 12 identische Zeilen ein. Assert über
    `containsPublicBadge(badges, "role_volume_translator_bronze", "role_volume")` == true nach
    `repo.GetPublicMemberProfile`/`loadPublicBadges`-Read. Storniere danach eine Buchung (UPDATE
    lifecycle_status='reversed', reversal_entry_id — Muster aus TestLoadPublicBadgesPostgresRoleEntryReversedHidden),
    sodass die Netto-Zahl auf 11 fällt, und assert, dass `role_volume_translator_bronze` NICHT mehr
    enthalten ist (frisch je Read, kein Cache → D-02/GAM-04). Verlasse dich NICHT auf ambient Seed-Daten
    für Gold/Platin (Pitfall 2 / VALIDATION Manual-Only) — die hohen Stufen werden nur über
    TestHighestRoleVolumeTier bewiesen.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/repository/... -run 'TestLoadPublicBadgesPostgresRoleVolume|TestHighestRoleVolumeTier'</automated>
  </verify>
  <acceptance_criteria>
    - member_profile_repository.go enthält `append(profile.PublicBadges, volumeBadges...)` und einen loadRoleVolumeBadges-Aufruf mit dem `if loadErr != nil { return nil, loadErr }`-Idiom.
    - `git diff --stat backend/internal/repository/member_profile_repository.go` zeigt nur wenige (<10) hinzugefügte Zeilen (Pitfall-3-Gate).
    - TestLoadPublicBadgesPostgresRoleVolume: 12 Credits → `role_volume_translator_bronze` sichtbar; Storno auf 11 → nicht mehr sichtbar.
    - Test seedet distinkte generation/release_version-Werte (keine 12 identischen Zeilen), respektiert die UNIQUE-Constraint.
    - `go build ./...` sauber; beide Tests grün.
  </acceptance_criteria>
  <done>Call-Site verdrahtet, Typ-3-Badges fließen in public_badges; Postgres Live-Projektions-Test (award→reverse→hidden) grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → GET /members/[slug] (public, unauthenticated) | Öffentlicher Profil-Read; keine neue Auth-Grenze, unverändert aus Phase 110. |
| Repository → release_role_credit_lifecycles (Postgres) | Abgeleitete Zähl-Projektion; keine Schreib-/Buchungspfade. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Tampering (State-Integrität) | loadRoleVolumeBadges Netto-Zählung | mitigate | `WHERE lifecycle_status = 'awarded'` filtert stornierte/pending Buchungen; frisch je Read berechnet (kein Cache), sodass ein Storno beim nächsten Read sofort das Badge senkt/entfernt — Postgres-Test TestLoadPublicBadgesPostgresRoleVolume beweist award→reverse→hidden. |
| T-112-02 | Tampering (kein Fake-Punktpfad) | ID:0 / kein INSERT in member_badges | mitigate | Typ-3-Badges werden read-time synthetisiert (ID:0), nie persistiert; kein neuer Buchungs-/Schreibpfad (GAM-04). Kein `INSERT`/`UPDATE` auf member_badges/point_ledger in dieser Datei. |
| T-112-03 | Information Disclosure | Roh-Credit-Zahl pro Rolle | accept | Backend emittiert nur den gerundeten Tier-Token (z. B. `role_volume_translator_gold`), nicht die exakte Zahl; die Info ist aus bereits öffentlichen Daten (Typ-1 role_entry EXISTS) ohnehin näherungsweise ableitbar. |
| T-112-04 | Elevation / Access Control | Profil-Read-Autorisierung | accept | Keine neue Zugriffsgrenze — dieselbe bereits öffentliche GetPublicMemberProfile-Leseoperation, unverändert aus Phase 110. |
| T-112-SC | Tampering (Supply Chain) | go get / neue Pakete | accept | Keine neuen Third-Party-Pakete (RESEARCH Package Legitimacy Audit: N/A — pure Erweiterung bestehender pgx/v5-Nutzung). Keine Install-Tasks, kein slopcheck-Gate nötig. |
</threat_model>

<verification>
- `cd backend && go build ./...` sauber.
- `cd backend && go test ./internal/repository/...` grün (inkl. TestHighestRoleVolumeTier + TestLoadPublicBadgesPostgresRoleVolume).
- member_profile_repository.go-Diff bleibt minimal (Pitfall-3-Gate).
- Manueller UAT-Hinweis (VALIDATION Manual-Only): Gold/Platin sind in Wegwerf-Testdaten ohne gezieltes Seeding nicht natürlich sichtbar (Phase 108 D-20 kein Backfill) — bei Live-UAT genügend Credits reversibel seeden oder „nur Bronze live beobachtbar" ehrlich dokumentieren.
</verification>

<success_criteria>
- Neues Split-File member_profile_role_volume_repository.go trägt loadRoleVolumeBadges + highestRoleVolumeTier; member_profile_repository.go wächst nur um den Append-Call-Site.
- Typ-3-Badges (`role_volume_<roleCode>_<tier>`, category `role_volume`) fließen in profile.PublicBadges, netto gezählt, nie persistiert (GAM-04, D-02, D-04).
- Backend-Suite grün; Tier-Grenzen und Live-Projektion (Storno-Rückstufung) automatisiert bewiesen.
</success_criteria>

<output>
Create `.planning/phases/112-member-punkt-meilenstein-badges/112-01-SUMMARY.md` when done
</output>
