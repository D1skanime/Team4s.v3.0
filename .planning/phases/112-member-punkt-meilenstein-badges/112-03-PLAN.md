---
phase: 112-member-punkt-meilenstein-badges
plan: 03
type: execute
wave: 2
depends_on: ["112-02"]
files_modified:
  - frontend/src/components/profile/MemberBadgeChain.tsx
  - frontend/src/components/profile/MemberBadgeChain.module.css
  - frontend/src/components/profile/MemberBadgeChain.test.tsx
  - frontend/src/app/members/[slug]/page.tsx
autonomous: true
requirements: [GAM-04]
must_haves:
  truths:
    - "D-04: Jede Zeile der Rollen-Gruppe zeigt einen '{Rollenname}:'-Präfix; Typ-1-Einstieg und Typ-3-Volumenstufe derselben Rolle stehen in EINER Zeile (z. B. 'Übersetzung: [Erste Übersetzung] [Gold · 320+]')."
    - "D-03: Der höchste erreichte Punkt-Meilenstein erscheint als genau eine Zeile in der Gruppe 'Fortschritt'; bei 0 Punkten keine Meilenstein-Zeile."
    - "D-01/D-02: total_points fließt über deriveMilestoneBadge in earnedBadges; Live-Rückstufung bei gesunkener Summe ohne Persistenz (GAM-04)."
  artifacts:
    - path: "frontend/src/components/profile/MemberBadgeChain.tsx"
      provides: "roleLabel-Rollenname-Präfix als erstes Flex-Kind jeder roles-Gruppen-Zeile"
      contains: "roleLabel"
    - path: "frontend/src/components/profile/MemberBadgeChain.module.css"
      provides: "bronze/silver/platinum data-palette-Regeln + .roleLabel-Klasse"
      contains: "data-palette=\"bronze\""
    - path: "frontend/src/app/members/[slug]/page.tsx"
      provides: "Merge des Meilenstein-Badges in earnedBadges via deriveMilestoneBadge"
      contains: "deriveMilestoneBadge"
  key_links:
    - from: "frontend/src/app/members/[slug]/page.tsx"
      to: "deriveMilestoneBadge (memberBadgeLabels.ts)"
      via: "total_points → gemischtes earnedBadges-Array"
      pattern: "deriveMilestoneBadge\\(profile.total_points"
    - from: "MemberBadgeChain.tsx roles-Zeile"
      to: "FANSUB_GROUP_ROLE_OPTIONS"
      via: "roleCode → deutsches Rollenlabel-Präfix"
      pattern: "FANSUB_GROUP_ROLE_OPTIONS"
---

<objective>
Verdrahte die in 112-02 gebaute Ableitungs-/Resolver-Logik in die sichtbare „Auszeichnungen"-Sektion:
(1) render den Rollenname-Präfix (`{Rollenname}:`) als erstes Kind jeder Rollen-Gruppen-Zeile in
`MemberBadgeChain.tsx` + die drei Tier-Paletten-CSS-Regeln und die `.roleLabel`-Klasse, (2) mische den
per `deriveMilestoneBadge` abgeleiteten Meilenstein in `members/[slug]/page.tsx` in `earnedBadges`.

Purpose: Kein neues Badge-UI, kein Rebuild von buildMemberBadgeGroups (bereits generisch aus 110-04) —
nur der additive Zeilen-Präfix und die SSR-Verdrahtung lösen das CONTEXT-Beispiel
„Übersetzung: Erste Übersetzung · Gold · 320+" auf. Backend-Typ-3-Codes (112-01) und Meilenstein-
Ableitung (112-02) fließen über das bestehende earnedBadges-Array automatisch in die richtigen Zeilen.
Output: MemberBadgeChain.tsx + .module.css + Component-Test + page.tsx-Verdrahtung.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/112-member-punkt-meilenstein-badges/112-UI-SPEC.md
@.planning/phases/112-member-punkt-meilenstein-badges/112-PATTERNS.md
@.planning/phases/112-member-punkt-meilenstein-badges/112-02-SUMMARY.md

<interfaces>
<!-- From Plan 112-02 (Wave 1, must have landed): -->
memberBadgeLabels.ts exports:
  deriveMilestoneBadge(totalPoints: number): PublicMemberBadge | null   // { id:0, badge_code, badge_category:'progress' } or null (<1)
  getMemberBadgePresentation(badgeCode): MemberBadgePresentation        // now resolves role_volume_* dynamically (group:'roles', roleCode:<parsed>)
  MemberBadgePalette now includes 'bronze' | 'silver' | 'platinum'

<!-- Verified current shapes (working tree, 2026-07-28): -->
MemberBadgeChain.tsx (150 lines):
  buildMemberBadgeGroups (line 59) — roles-group merges by getPresentation(item.badge_code).roleCode ?? item.badge_code (line 70). NO structural change needed.
  roles-group row render uses <li key={row.key} className={styles.badgeRow}> (line 119); row.key is the roleCode for roles group.
  Imports { Card, SectionHeader } from '@/components/ui' (CLAUDE.md primitives mandate — keep, add no native controls).

MemberBadgeChain.module.css:
  .groupTitle (lines 60-68): font-size 12px, font-weight 700, line-height 1.2, color var(--text-secondary)
  .badgeRow (line 70): display inline-flex, gap 8px
  Existing palette rules: .badgeStep[data-palette="gold"],.badgeStepLocked[data-palette="gold"]{--badge-accent:var(--color-warning);} (lines 84-107 for gold/indigo/orange/mint/red)

members/[slug]/page.tsx:
  line 90:  const publicBadges = profile.public_badges ?? []
  line ~138: <MemberBadgeChain earnedBadges={publicBadges} ... />
  profile.total_points: number (already threaded, frontend/src/types/profile.ts line 226)

Role labels: FANSUB_GROUP_ROLE_OPTIONS (frontend/src/types/fansub.ts) — roleCode → German label
Component test analog: MemberBadgeChain.test.tsx (existing injectable-presentation harness, fakePresentation({ group, roleCode }))
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rollenname-Präfix in MemberBadgeChain.tsx + Tier-Paletten & .roleLabel in module.css</name>
  <files>frontend/src/components/profile/MemberBadgeChain.tsx, frontend/src/components/profile/MemberBadgeChain.module.css, frontend/src/components/profile/MemberBadgeChain.test.tsx</files>
  <read_first>
    - frontend/src/components/profile/MemberBadgeChain.tsx (buildMemberBadgeGroups ~59-85, roles-Zeilen-Render ~118-142)
    - frontend/src/components/profile/MemberBadgeChain.module.css (.groupTitle ~60-68, .badgeRow ~70, data-palette-Regeln ~84-107)
    - frontend/src/components/profile/MemberBadgeChain.test.tsx (bestehender injectable-presentation-Harness)
    - frontend/src/types/fansub.ts (FANSUB_GROUP_ROLE_OPTIONS)
    - .planning/phases/112-member-punkt-meilenstein-badges/112-UI-SPEC.md (Zeilen-Komposition-Abschnitt, Badge-Palette-Erweiterung)
  </read_first>
  <behavior>
    - buildMemberBadgeGroups mit synthetischem { role_entry_translator } + { role_volume_translator_gold } liefert EINE 'roles'-Gruppenzeile mit beiden Items (row.key === 'translator').
    - Die gerenderte roles-Zeile enthält ein .roleLabel-Element mit Text 'Übersetzung:' vor den Chips.
    - Nicht-roles-Gruppen (progress/membership/special) rendern ohne .roleLabel-Präfix.
    - Eine roles-Zeile mit nur { role_entry_translator } (unter 12 Credits) zeigt trotzdem den 'Übersetzung:'-Präfix und genau einen Chip.
  </behavior>
  <action>
    KEINE strukturelle Änderung an buildMemberBadgeGroups' Gruppierungs-/Merge-Logik — sie ist bereits
    generisch (110-04) und mergt role_volume_translator_gold automatisch mit role_entry_translator, weil
    resolveRoleVolumePresentation (112-02) den geparsten roleCode zurückgibt. Das ist der Grund, warum
    hier kein Rebuild nötig ist.

    Eine einzige additive Änderung im roles-Gruppen-Zeilen-Render (`<li className={styles.badgeRow}>`,
    ~Zeile 119): rendere als ERSTES Flex-Kind ein `<span className={styles.roleLabel}>{roleName}:</span>`
    vor `row.items.map(...)`. Löse roleName aus dem roleCode der Zeile (row.key für die roles-Gruppe) via
    FANSUB_GROUP_ROLE_OPTIONS (import aus `@/types/fansub`) auf; Fallback auf den rohen Code, falls kein
    Treffer. Gilt EINHEITLICH für jede roles-Zeile (auch wenn nur ein Typ-1-Chip vorhanden ist — kein
    Sonderfall „nur bei Merge"). Nicht-roles-Gruppen bleiben unverändert (kein Präfix). Bestehendes
    Chip-Markup (.badgeStep/.badgeStepLocked, data-palette, .badgeItem/.badgeIcon) verbatim erhalten.
    Reuse Card/SectionHeader aus @/components/ui unverändert — keine neuen nativen Controls (CLAUDE.md).

    In MemberBadgeChain.module.css: füge unter den bestehenden data-palette-Regeln drei neue Paar-Regeln
    nach exakt derselben Form hinzu (UI-SPEC Badge-Palette-Erweiterung, keine neuen Hex-Literale):
    bronze → `--badge-accent: color-mix(in srgb, var(--color-warning) 55%, var(--text-secondary) 45%)`;
    silver → `--badge-accent: var(--text-secondary)`;
    platinum → `--badge-accent: color-mix(in srgb, var(--color-primary) 45%, var(--text-secondary) 55%)`.
    (gold wird unverändert wiederverwendet.) Füge die Klasse `.roleLabel { font-size: 12px; font-weight:
    700; line-height: 1.2; color: var(--text-secondary); white-space: nowrap; }` hinzu (übernimmt die
    exakten .groupTitle-Werte; neue Klasse nur, weil sie inline in der Flex-.badgeRow sitzt).

    Ergänze MemberBadgeChain.test.tsx um die <behavior>-Fälle (Merge zu einer Zeile + .roleLabel-Präfix-
    Render + Nur-Typ-1-Zeile mit Präfix), nutze den bestehenden injectable-presentation-Harness.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- MemberBadgeChain && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "roleLabel" MemberBadgeChain.tsx` >= 1; Präfix-Text hat Form `{roleName}:` und wird für jede roles-Zeile gerendert.
    - buildMemberBadgeGroups bleibt strukturell unverändert (nur Render-Delta) — Merge-Logik nicht angefasst.
    - module.css enthält `[data-palette="bronze"]`, `[data-palette="silver"]`, `[data-palette="platinum"]`-Regeln (nur Tokens + color-mix, keine Hex) und `.roleLabel`.
    - Component-Test: synthetisches role_entry_translator + role_volume_translator_gold → EINE roles-Zeile mit beiden Chips + 'Übersetzung:'-Präfix.
    - Nicht-roles-Gruppen ohne Präfix; `npm run test -- MemberBadgeChain` grün; `npm run typecheck` sauber.
    - Rollenname mit korrekten Umlauten (Übersetzung, Qualitätsprüfung).
  </acceptance_criteria>
  <done>Rollen-Zeilen tragen den {Rollenname}:-Präfix, Tier-Paletten + .roleLabel im CSS, Merge- und Präfix-Tests grün.</done>
</task>

<task type="auto">
  <name>Task 2: SSR-Verdrahtung — Meilenstein-Badge in earnedBadges mischen (members/[slug]/page.tsx)</name>
  <files>frontend/src/app/members/[slug]/page.tsx</files>
  <read_first>
    - frontend/src/app/members/[slug]/page.tsx (publicBadges-Assembly Zeile 90, MemberBadgeChain-Aufruf ~138-139, total_points-Nutzung)
    - frontend/src/components/profile/memberBadgeLabels.ts (deriveMilestoneBadge-Signatur, aus 112-02)
    - .planning/phases/112-member-punkt-meilenstein-badges/112-PATTERNS.md (members/[slug]/page.tsx-Delta)
  </read_first>
  <action>
    Importiere `deriveMilestoneBadge` aus `@/components/profile/memberBadgeLabels`. Direkt nach der
    bestehenden `const publicBadges = profile.public_badges ?? []`-Zeile: leite den Meilenstein ab und
    mische ihn in das an MemberBadgeChain übergebene Array —
    `const milestoneBadge = deriveMilestoneBadge(profile.total_points ?? 0)` und
    `const earnedBadges = milestoneBadge ? [...publicBadges, milestoneBadge] : publicBadges`. Übergib
    `earnedBadges` (statt `publicBadges`) an `<MemberBadgeChain earnedBadges={...} />`. MemberBadgeChain
    bleibt domänen-agnostisch (erfährt nichts über total_points direkt — 110-03-Präzedenz); kein neuer
    Fetch, kein neuer Prop. Bei 0 Punkten liefert deriveMilestoneBadge null → keine Fortschritt-Zeile
    (D-03; leere Gruppen versteckt buildMemberBadgeGroups bereits). Backend-Typ-3-Badges sind bereits in
    profile.public_badges enthalten (112-01) und brauchen hier keine gesonderte Behandlung.
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck && npm run test -- MemberBadgeChain memberBadgeLabels</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx importiert und ruft deriveMilestoneBadge(profile.total_points ?? 0) auf.
    - `<MemberBadgeChain earnedBadges={earnedBadges} />` erhält das gemischte Array (Meilenstein additiv, nur wenn nicht null).
    - Kein neuer Prop an MemberBadgeChain, kein neuer Fetch; MemberBadgeChain bleibt ohne total_points-Kenntnis.
    - `npm run typecheck` sauber; Frontend-Suite grün.
  </acceptance_criteria>
  <done>total_points fließt über deriveMilestoneBadge in earnedBadges; höchster Meilenstein erscheint in der Fortschritt-Gruppe, 0 Punkte → keine Zeile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSR (page.tsx) → MemberBadgeChain render | Server-Component reicht abgeleitetes earnedBadges-Array durch; reine Anzeige, keine Eingabe. |
| profile.public_badges / total_points → Render | Bereits öffentliche, in Phase 110/112-01 abgeleitete Daten; keine neue Auth-Grenze. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-08 | Tampering (Anzeige-Integrität) | earnedBadges-Merge in page.tsx | mitigate | Meilenstein wird bei jedem SSR-Render frisch aus total_points abgeleitet (kein Cache/Memo eines „jemals erreicht"-Zustands); gesunkene Summe senkt/entfernt die Zeile beim nächsten Render — identisch zur etablierten Live-Projektion (T-110-09-Muster). |
| T-112-09 | Tampering (kein Fake-Punktpfad) | Rendering-Schicht | mitigate | Reine Leseansicht; kein Schreibpfad zu member_badges, keine Punktvergabe durch Anzeige (GAM-04). ID:0-Badges werden nie persistiert. |
| T-112-10 | Spoofing/UI-Konsistenz | Global-UI-Primitives-Mandat | mitigate | Keine handgebauten nativen Controls; Card/SectionHeader aus @/components/ui unverändert wiederverwendet, additive Anzeige innerhalb bestehender .badgeStep/.badgeRow-Markup (CLAUDE.md). |
| T-112-SC | Tampering (Supply Chain) | npm install / neue Pakete | accept | Keine neuen Pakete (RESEARCH Package Legitimacy Audit: N/A). Kein slopcheck-Gate nötig. |
</threat_model>

<verification>
- `cd frontend && npm run test -- MemberBadgeChain memberBadgeLabels` grün.
- `cd frontend && npm run typecheck` sauber; Full-Suite `npm run test` grün vor /gsd:verify-work.
- Manueller UAT (VALIDATION Manual-Only): Live-Sicht `/members/[slug]` — Fortschritt-Meilenstein-Zeile, Rollen-Zeile mit Präfix + Bronze-Chip; Gold/Platin nur mit gezieltem reversiblem Credit-Seeding sichtbar (Phase 108 kein Backfill), sonst „Bronze-only" ehrlich dokumentieren.
- Live-Test-Prozedur (MEMORY): nach Frontend-Edit `docker restart team4sv30-frontend` + Strg+F5 (HMR greift nicht auf :3000).
</verification>

<success_criteria>
- Jede Rollen-Gruppen-Zeile trägt den {Rollenname}:-Präfix; Typ-1 + Typ-3 derselben Rolle in EINER Zeile (D-04, CONTEXT-Beispiel aufgelöst).
- Höchster Punkt-Meilenstein erscheint als eine Zeile in der Fortschritt-Gruppe (D-01/D-03); 0 Punkte → keine Zeile.
- Live-Rückstufung ohne Persistenz (GAM-04/D-02); keine neuen Primitives/Controls, globales UI-System eingehalten.
</success_criteria>

<output>
Create `.planning/phases/112-member-punkt-meilenstein-badges/112-03-SUMMARY.md` when done
</output>
