---
phase: 41-globalen-tiptap-rich-text-editor-einfuehren
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/services/tiptap_service.go
  - backend/internal/services/tiptap_service_test.go
  - backend/go.mod
  - backend/go.sum
autonomous: true
requirements:
  - TIPTAP-EDITOR-01
must_haves:
  truths:
    - "TipTapService mit ValidateJSON, RenderHTML, ExtractText, IsEmpty existiert"
    - "Validator lehnt nicht erlaubte Node-Typen (codeBlock, image, script) ab"
    - "Validator lehnt freie Hex-Farben ab, erlaubt Token-Namen (red, blue etc.)"
    - "Renderer erzeugt sanitisiertes HTML aus TipTap JSON"
    - "IsEmpty erkennt leeres Dokument {type:doc, content:[{type:paragraph}]} korrekt als leer"
    - "bluemonday ist in go.mod als direct dependency"
    - "go test ./internal/services/... -run TestTipTap läuft grün"
  artifacts:
    - path: backend/internal/services/tiptap_service.go
      provides: "TipTapService mit allen vier Methoden"
      exports: ["TipTapService", "NewTipTapService", "ValidateJSON", "RenderHTML", "ExtractText", "IsEmpty"]
    - path: backend/internal/services/tiptap_service_test.go
      provides: "Unit-Tests für alle vier Methoden"
      contains: "TestTipTap"
  key_links:
    - from: backend/internal/services/tiptap_service.go
      to: github.com/microcosm-cc/bluemonday
      via: "newTipTapSanitizerPolicy()"
      pattern: "bluemonday.NewPolicy"
---

<objective>
Go TipTap Service implementieren: JSON-Validierung gegen Allowlist, HTML-Rendering mit Custom Walker, HTML-Sanitizing via bluemonday, Plaintext-Extraktion und IsEmpty-Prüfung.

Purpose: Das Backend muss TipTap JSON empfangen, validieren, in sanitisiertes HTML umwandeln und Plaintext extrahieren. Es gibt keine fertige Go-Bibliothek dafür — ein schlanker Recursive Walker ist die Lösung.

Output: backend/internal/services/tiptap_service.go + tiptap_service_test.go, bluemonday auf direct dependency hochgestuft.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-globalen-tiptap-rich-text-editor-einfuehren/41-CONTEXT.md
@.planning/phases/41-globalen-tiptap-rich-text-editor-einfuehren/41-RESEARCH.md

<interfaces>
<!-- Bestehender Service zum Orientieren am Muster: -->
<!-- backend/internal/services/markdown_service.go:
  type MarkdownService struct { md goldmark.Markdown; sanitizer *bluemonday.Policy }
  func NewMarkdownService() *MarkdownService
  func (s *MarkdownService) RenderMarkdown(md string) (string, error)
-->

<!-- go.mod enthält bereits: github.com/microcosm-cc/bluemonday v1.0.27 (indirect) -->
<!-- bluemonday muss nach `go get github.com/microcosm-cc/bluemonday@v1.0.27` direct werden -->

<!-- TipTap JSON Struktur (aus RESEARCH.md):
  { "type": "doc", "content": [...nodes] }
  Node: { "type": string, "attrs": map, "marks": []Mark, "content": []Node, "text": string }
  Mark: { "type": string, "attrs": map }
-->

<!-- Erlaubte Nodes: doc, paragraph, text, heading, bulletList, orderedList, listItem,
     blockquote, horizontalRule, table, tableRow, tableCell, tableHeader -->
<!-- Erlaubte Marks: bold, italic, textStyle (mit colorToken: default|gray|red|orange|yellow|green|blue|purple) -->
<!-- NICHT erlaubt: codeBlock, code, strike, hardBreak, image, link, iframe, html, script -->

<!-- HTML-Sanitizing Allowlist (bluemonday Custom Policy):
  Tags: p, h1, h2, h3, strong, em, ul, ol, li, blockquote, table, thead, tbody, tr, th, td, hr, span
  Attribute class auf span, td, th; colspan/rowspan auf td, th; data-color-token auf span
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TipTap Service Tests anlegen (Wave 0 — erst Tests, dann Implementierung)</name>
  <read_first>
    - backend/internal/services/markdown_service.go (Muster für Service-Struktur und bluemonday-Nutzung)
    - backend/go.mod (aktueller Stand der Abhängigkeiten)
  </read_first>
  <files>backend/internal/services/tiptap_service_test.go</files>
  <behavior>
    - TestTipTapValidateJSON_valid: {"type":"doc","content":[{"type":"paragraph","content":[{"type":"text","text":"Hallo"}]}]} → kein Fehler
    - TestTipTapValidateJSON_invalidNode: {"type":"doc","content":[{"type":"codeBlock","content":[]}]} → Fehler enthält "nicht erlaubter Node-Typ"
    - TestTipTapValidateJSON_invalidMark: text-Node mit mark type "strike" → Fehler enthält "nicht erlaubter Mark-Typ"
    - TestTipTapValidateJSON_invalidColorToken: textStyle mark mit colorToken "#ff0000" → Fehler enthält "nicht erlaubtes Farb-Token"
    - TestTipTapValidateJSON_validColorToken: textStyle mark mit colorToken "red" → kein Fehler
    - TestTipTapRenderHTML_heading: heading level 2 → Ergebnis enthält "&lt;h2&gt;" oder "<h2>"
    - TestTipTapRenderHTML_bold: text mit bold mark → Ergebnis enthält "<strong>"
    - TestTipTapRenderHTML_table: table-Node → Ergebnis enthält "<table>" und "<thead>"
    - TestTipTapRenderHTML_colorToken: textStyle colorToken "red" → Ergebnis enthält 'data-color-token="red"'
    - TestTipTapRenderHTML_noScript: doc mit script-Injection im text → Ergebnis enthält KEIN "<script"
    - TestTipTapExtractText: paragraph + heading + listItem mit Text → gibt kombinierten Plaintext zurück, kein HTML
    - TestTipTapIsEmpty_emptyDoc: {"type":"doc","content":[{"type":"paragraph"}]} → true
    - TestTipTapIsEmpty_emptyParagraph: {"type":"doc","content":[{"type":"paragraph","content":[{"type":"text","text":""}]}]} → true
    - TestTipTapIsEmpty_withContent: doc mit echtem Text → false
    - TestTipTapIsEmpty_whitespaceOnly: doc mit nur Leerzeichen → true
  </behavior>
  <action>
    Erstelle `backend/internal/services/tiptap_service_test.go` mit allen Tests.

    ```go
    package services_test

    import (
        "testing"
        "github.com/stretchr/testify/assert"
        "github.com/stretchr/testify/require"
        "team4s.v3/backend/internal/services"
    )

    func newTestTipTapService(t *testing.T) *services.TipTapService {
        t.Helper()
        return services.NewTipTapService()
    }
    ```

    Schreibe alle 14 Tests als separate Funktionen. Tests müssen zunächst ROTER
    kompilierungsfähig sein (TipTapService existiert noch nicht → Compile-Fehler erwartet).
    Schreibe die Tests vollständig aus, auch wenn sie nicht compilieren.

    Paketname: `services_test` (Black-Box-Test).
    Import: `team4s.v3/backend/internal/services`.
    Assertions: `require.NoError` für "muss funktionieren", `assert.Error` für "muss Fehler liefern",
    `assert.Contains` für String-Prüfungen.
  </action>
  <verify>
    <automated>MISSING — tiptap_service.go muss erst in Task 2 erstellt werden, damit Tests compilieren. Danach: cd backend && go test ./internal/services/... -run TestTipTap -v 2>&1 | head -60</automated>
  </verify>
  <done>
    tiptap_service_test.go existiert mit 14+ Test-Funktionen.
    Alle Tests decken ValidateJSON, RenderHTML, ExtractText, IsEmpty ab.
    Paket ist `services_test`.
  </done>
</task>

<task type="auto">
  <name>Task 2: TipTapService implementieren + bluemonday auf direct hochstufen</name>
  <read_first>
    - backend/internal/services/tiptap_service_test.go (aus Task 1 — Tests als Spec)
    - backend/internal/services/markdown_service.go (Muster: bluemonday-Policy, Service-Struct)
    - backend/go.mod (aktueller Stand)
    - .planning/phases/41-globalen-tiptap-rich-text-editor-einfuehren/41-RESEARCH.md (Pattern 3 + Pattern 4 — Go Walker + bluemonday Policy)
  </read_first>
  <files>backend/internal/services/tiptap_service.go, backend/go.mod, backend/go.sum</files>
  <action>
    **Schritt 1: bluemonday auf direct hochstufen**
    ```bash
    cd backend && go get github.com/microcosm-cc/bluemonday@v1.0.27
    ```
    Das verschiebt bluemonday in go.mod von `// indirect` zu direkt.

    **Schritt 2: tiptap_service.go erstellen**

    Die Datei darf 450 Zeilen nicht überschreiten. Bei Bedarf Hilfsfunktionen in eine tiptap_service_helpers.go auslagern.

    Struktur der Datei:
    ```go
    package services

    import (
        "encoding/json"
        "fmt"
        "html/template"
        "strings"
        "github.com/microcosm-cc/bluemonday"
    )

    // TipTapService validiert TipTap JSON, rendert HTML und extrahiert Text.
    type TipTapService struct {
        sanitizer *bluemonday.Policy
    }

    // NewTipTapService erstellt einen TipTapService mit enger bluemonday-Policy.
    func NewTipTapService() *TipTapService {
        return &TipTapService{sanitizer: newTipTapSanitizerPolicy()}
    }
    ```

    **Typen:**
    ```go
    type TipTapNode struct {
        Type    string            `json:"type"`
        Attrs   map[string]any    `json:"attrs,omitempty"`
        Marks   []TipTapMark      `json:"marks,omitempty"`
        Content []TipTapNode      `json:"content,omitempty"`
        Text    string            `json:"text,omitempty"`
    }

    type TipTapMark struct {
        Type  string         `json:"type"`
        Attrs map[string]any `json:"attrs,omitempty"`
    }
    ```

    **Allowlists:**
    ```go
    var allowedTipTapNodes = map[string]bool{
        "doc": true, "paragraph": true, "text": true,
        "heading": true, "bulletList": true, "orderedList": true,
        "listItem": true, "blockquote": true, "horizontalRule": true,
        "table": true, "tableRow": true, "tableCell": true, "tableHeader": true,
    }

    var allowedTipTapMarks = map[string]bool{
        "bold": true, "italic": true, "textStyle": true,
    }

    var allowedColorTokens = map[string]bool{
        "default": true, "gray": true, "red": true, "orange": true,
        "yellow": true, "green": true, "blue": true, "purple": true,
    }
    ```

    **ValidateJSON:** Unmarshal + validateNode (rekursiv). Prüft Node-Typ, Mark-Typen, colorToken-Wert.
    Fehlertext: "nicht erlaubter Node-Typ: %q", "nicht erlaubter Mark-Typ: %q", "nicht erlaubtes Farb-Token: %q".

    **RenderHTML:** Unmarshal + renderNode (rekursiv) → strings.Builder → sanitizer.SanitizeBytes.
    Node-Mapping:
    - doc → kein eigener Tag, nur Content rendern
    - paragraph → `<p>...</p>`
    - text → text mit Marks gewrappt; EscapeString aus html/template für den Text-Inhalt
    - heading level 1-3 → `<h1>`/`<h2>`/`<h3>` (level aus Attrs["level"].(float64), default 1, clamp 1-3)
    - bulletList → `<ul>...</ul>`
    - orderedList → `<ol>...</ol>`
    - listItem → `<li>...</li>`
    - blockquote → `<blockquote>...</blockquote>`
    - horizontalRule → `<hr>`
    - table → `<table>...</table>`
    - tableRow → `<tr>...</tr>`
    - tableHeader → `<th>...</th>` (mit colspan/rowspan aus Attrs wenn vorhanden)
    - tableCell → `<td>...</td>` (mit colspan/rowspan aus Attrs wenn vorhanden)

    Marks auf text-Nodes:
    - bold → `<strong>...</strong>`
    - italic → `<em>...</em>`
    - textStyle mit colorToken → `<span data-color-token="TOKEN" class="color-token-TOKEN">...</span>`
      Nur wenn colorToken vorhanden und nicht "default".
    Marks werden von innen nach außen gewrappt (innerster Wrapper zuerst).

    **ExtractText:** Rekursiv alle text-Node-Texte sammeln, durch Leerzeichen getrennt. strings.TrimSpace am Ende.

    **IsEmpty:** ExtractText aufrufen, strings.TrimSpace("") prüfen. Bei Fehler → true.

    **bluemonday Policy:**
    ```go
    func newTipTapSanitizerPolicy() *bluemonday.Policy {
        p := bluemonday.NewPolicy()
        p.AllowElements("p", "h1", "h2", "h3", "strong", "em",
            "ul", "ol", "li", "blockquote",
            "table", "thead", "tbody", "tr", "th", "td", "hr", "span")
        p.AllowAttrs("class").OnElements("span", "td", "th")
        p.AllowAttrs("colspan", "rowspan").OnElements("td", "th")
        p.AllowAttrs("data-color-token").OnElements("span")
        return p
    }
    ```

    WICHTIG: Datei unter 450 Zeilen halten. Falls nötig, renderNode-Hilfsfunktionen in eine
    separate `tiptap_service_helpers.go` auslagern.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/services/... -run TestTipTap -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    go test ./internal/services/... -run TestTipTap läuft grün (alle 14+ Tests PASS).
    go build ./... läuft ohne Fehler.
    tiptap_service.go ist unter 450 Zeilen.
    go.mod enthält github.com/microcosm-cc/bluemonday ohne "// indirect".
  </done>
</task>

</tasks>

<verification>
```bash
cd backend && go test ./internal/services/... -run TestTipTap -v
cd backend && go build ./...
grep "microcosm-cc/bluemonday" backend/go.mod | grep -v indirect
wc -l backend/internal/services/tiptap_service.go
```
</verification>

<success_criteria>
- go test ./internal/services/... -run TestTipTap: alle Tests PASS
- go build ./...: kein Fehler
- tiptap_service.go enthält TipTapService struct mit NewTipTapService, ValidateJSON, RenderHTML, ExtractText, IsEmpty
- go.mod enthält bluemonday ohne "indirect"
- tiptap_service.go ist unter 450 Zeilen
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/41-globalen-tiptap-rich-text-editor-einfuehren/41-02-SUMMARY.md` erstellen.
</output>
