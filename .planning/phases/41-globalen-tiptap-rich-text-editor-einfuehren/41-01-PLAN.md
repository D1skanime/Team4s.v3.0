---
phase: 41-globalen-tiptap-rich-text-editor-einfuehren
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/0067_fansub_group_notes_tiptap.up.sql
  - database/migrations/0067_fansub_group_notes_tiptap.down.sql
  - database/migrations/0068_member_group_stories_tiptap.up.sql
  - database/migrations/0068_member_group_stories_tiptap.down.sql
  - database/migrations/0069_anime_fansub_project_notes_tiptap.up.sql
  - database/migrations/0069_anime_fansub_project_notes_tiptap.down.sql
  - database/migrations/0070_release_version_notes_tiptap.up.sql
  - database/migrations/0070_release_version_notes_tiptap.down.sql
autonomous: true
requirements:
  - TIPTAP-EDITOR-01
must_haves:
  truths:
    - "Alle vier Texttabellen haben body_json JSONB NULL, body_text TEXT NOT NULL DEFAULT '', editor_type TEXT NOT NULL DEFAULT 'tiptap', content_schema_version INT NOT NULL DEFAULT 1"
    - "body_html und body_markdown bleiben unverändert erhalten (kein DROP)"
    - "Migrations sind idempotent (IF NOT EXISTS / IF EXISTS)"
    - "Down-Migrations entfernen exakt die vier neuen Spalten"
  artifacts:
    - path: database/migrations/0067_fansub_group_notes_tiptap.up.sql
      provides: "ALTER TABLE fansub_group_notes ADD COLUMN IF NOT EXISTS"
      contains: "body_json"
    - path: database/migrations/0068_member_group_stories_tiptap.up.sql
      provides: "ALTER TABLE member_group_stories ADD COLUMN IF NOT EXISTS"
      contains: "body_json"
    - path: database/migrations/0069_anime_fansub_project_notes_tiptap.up.sql
      provides: "ALTER TABLE anime_fansub_project_notes ADD COLUMN IF NOT EXISTS"
      contains: "body_json"
    - path: database/migrations/0070_release_version_notes_tiptap.up.sql
      provides: "ALTER TABLE release_version_notes ADD COLUMN IF NOT EXISTS"
      contains: "body_json"
  key_links:
    - from: "database/migrations/0067-0070 UP"
      to: "fansub_group_notes, member_group_stories, anime_fansub_project_notes, release_version_notes"
      via: "ALTER TABLE ADD COLUMN IF NOT EXISTS"
      pattern: "ADD COLUMN IF NOT EXISTS body_json"
---

<objective>
DB-Migrationen für TipTap-Felder anlegen: body_json JSONB, body_text TEXT, editor_type TEXT und content_schema_version INT für alle vier Texttabellen.

Purpose: Phase 41 braucht JSONB-Speicherung für TipTap-Dokumente in den vier vorhandenen Texttabellen. body_html existiert bereits aus Phase 40 (Migrations 0061-0064) — nur die neuen Felder werden ergänzt, nichts wird gelöscht.

Migrations-Nummerierung: 0066 ist bereits vergeben (0066_anime_fansub_project_notes_context_guard — noch ungetrackt aber im Working Tree). TipTap-Migrations beginnen daher bei 0067.

Output: 8 SQL-Dateien (je 4x .up.sql + .down.sql) in database/migrations/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/41-globalen-tiptap-rich-text-editor-einfuehren/41-CONTEXT.md
@.planning/phases/41-globalen-tiptap-rich-text-editor-einfuehren/41-RESEARCH.md

<interfaces>
<!-- Bestehende Tabellenstruktur aus Phase 40 (Migrations 0061-0064): -->
<!-- fansub_group_notes: id, fansub_group_id, title, body_markdown TEXT, body_html TEXT, visibility, status, sort_order, created_by_user_id, updated_by_user_id, created_at, updated_at, deleted_at, deleted_by_user_id -->
<!-- member_group_stories: id, fansub_group_id, member_id, role_id, title, body_markdown TEXT, body_html TEXT, visibility, status, sort_order, ... (timestamps + audit) -->
<!-- anime_fansub_project_notes: id, fansub_group_id, anime_id, title, body_markdown TEXT, body_html TEXT, visibility, status, sort_order, ... -->
<!-- release_version_notes: id, release_version_id, member_id, role_id, title, body_markdown TEXT, body_html TEXT, visibility, status, sort_order, ... -->
<!-- body_html existiert BEREITS — kein ADD COLUMN für body_html nötig -->
<!-- 0066 ist bereits vergeben (context_guard) — neue TipTap-Migrations: 0067-0070 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrations 0067 und 0068 anlegen (fansub_group_notes + member_group_stories)</name>
  <read_first>
    - database/migrations/0061_fansub_group_notes.up.sql (Tabellenstruktur, insb. vorhandene Spalten)
    - database/migrations/0062_member_group_stories.up.sql (Tabellenstruktur)
    - database/migrations/0066_anime_fansub_project_notes_context_guard.up.sql (letztes vorhandenes Migration-Pattern)
  </read_first>
  <files>
    database/migrations/0067_fansub_group_notes_tiptap.up.sql,
    database/migrations/0067_fansub_group_notes_tiptap.down.sql,
    database/migrations/0068_member_group_stories_tiptap.up.sql,
    database/migrations/0068_member_group_stories_tiptap.down.sql
  </files>
  <action>
    Erstelle vier SQL-Dateien für die ersten zwei Tabellen.

    WICHTIG: 0066 ist bereits durch `0066_anime_fansub_project_notes_context_guard` belegt.
    TipTap-Migrations beginnen bei 0067.

    **0067_fansub_group_notes_tiptap.up.sql:**
    ```sql
    ALTER TABLE fansub_group_notes
      ADD COLUMN IF NOT EXISTS body_json              JSONB  NULL,
      ADD COLUMN IF NOT EXISTS body_text              TEXT   NOT NULL DEFAULT '',
      ADD COLUMN IF NOT EXISTS editor_type            TEXT   NOT NULL DEFAULT 'tiptap',
      ADD COLUMN IF NOT EXISTS content_schema_version INT    NOT NULL DEFAULT 1;
    ```

    **0067_fansub_group_notes_tiptap.down.sql:**
    ```sql
    ALTER TABLE fansub_group_notes
      DROP COLUMN IF EXISTS body_json,
      DROP COLUMN IF EXISTS body_text,
      DROP COLUMN IF EXISTS editor_type,
      DROP COLUMN IF EXISTS content_schema_version;
    ```

    **0068_member_group_stories_tiptap.up.sql:**
    ```sql
    ALTER TABLE member_group_stories
      ADD COLUMN IF NOT EXISTS body_json              JSONB  NULL,
      ADD COLUMN IF NOT EXISTS body_text              TEXT   NOT NULL DEFAULT '',
      ADD COLUMN IF NOT EXISTS editor_type            TEXT   NOT NULL DEFAULT 'tiptap',
      ADD COLUMN IF NOT EXISTS content_schema_version INT    NOT NULL DEFAULT 1;
    ```

    **0068_member_group_stories_tiptap.down.sql:**
    ```sql
    ALTER TABLE member_group_stories
      DROP COLUMN IF EXISTS body_json,
      DROP COLUMN IF EXISTS body_text,
      DROP COLUMN IF EXISTS editor_type,
      DROP COLUMN IF EXISTS content_schema_version;
    ```

    WICHTIG: body_html existiert bereits in beiden Tabellen aus Migration 0061 / 0062 — KEIN ADD COLUMN body_html.
  </action>
  <verify>
    <automated>grep -l "body_json" database/migrations/0067_fansub_group_notes_tiptap.up.sql database/migrations/0068_member_group_stories_tiptap.up.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ADD COLUMN IF NOT EXISTS body_json" database/migrations/0067_fansub_group_notes_tiptap.up.sql` → findet Treffer
    - `grep "ADD COLUMN IF NOT EXISTS body_json" database/migrations/0068_member_group_stories_tiptap.up.sql` → findet Treffer
    - `grep "ADD COLUMN.*body_html" database/migrations/0067_fansub_group_notes_tiptap.up.sql` → kein Treffer
  </acceptance_criteria>
  <done>
    Beide .up.sql-Dateien enthalten ADD COLUMN IF NOT EXISTS für body_json, body_text, editor_type, content_schema_version.
    Beide .down.sql-Dateien enthalten DROP COLUMN IF EXISTS für dieselben vier Spalten.
    Kein ADD COLUMN body_html (würde scheitern, da bereits vorhanden).
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrations 0069 und 0070 anlegen (anime_fansub_project_notes + release_version_notes)</name>
  <read_first>
    - database/migrations/0063_anime_fansub_project_notes.up.sql (Tabellenstruktur)
    - database/migrations/0064_release_version_notes.up.sql (Tabellenstruktur)
    - database/migrations/0067_fansub_group_notes_tiptap.up.sql (Muster aus Task 1)
  </read_first>
  <files>
    database/migrations/0069_anime_fansub_project_notes_tiptap.up.sql,
    database/migrations/0069_anime_fansub_project_notes_tiptap.down.sql,
    database/migrations/0070_release_version_notes_tiptap.up.sql,
    database/migrations/0070_release_version_notes_tiptap.down.sql
  </files>
  <action>
    Erstelle vier SQL-Dateien analog zu 0067/0068 — identisches Muster, andere Tabellennamen.

    **0069_anime_fansub_project_notes_tiptap.up.sql:**
    ```sql
    ALTER TABLE anime_fansub_project_notes
      ADD COLUMN IF NOT EXISTS body_json              JSONB  NULL,
      ADD COLUMN IF NOT EXISTS body_text              TEXT   NOT NULL DEFAULT '',
      ADD COLUMN IF NOT EXISTS editor_type            TEXT   NOT NULL DEFAULT 'tiptap',
      ADD COLUMN IF NOT EXISTS content_schema_version INT    NOT NULL DEFAULT 1;
    ```

    **0069_anime_fansub_project_notes_tiptap.down.sql:**
    ```sql
    ALTER TABLE anime_fansub_project_notes
      DROP COLUMN IF EXISTS body_json,
      DROP COLUMN IF EXISTS body_text,
      DROP COLUMN IF EXISTS editor_type,
      DROP COLUMN IF EXISTS content_schema_version;
    ```

    **0070_release_version_notes_tiptap.up.sql:**
    ```sql
    ALTER TABLE release_version_notes
      ADD COLUMN IF NOT EXISTS body_json              JSONB  NULL,
      ADD COLUMN IF NOT EXISTS body_text              TEXT   NOT NULL DEFAULT '',
      ADD COLUMN IF NOT EXISTS editor_type            TEXT   NOT NULL DEFAULT 'tiptap',
      ADD COLUMN IF NOT EXISTS content_schema_version INT    NOT NULL DEFAULT 1;
    ```

    **0070_release_version_notes_tiptap.down.sql:**
    ```sql
    ALTER TABLE release_version_notes
      DROP COLUMN IF EXISTS body_json,
      DROP COLUMN IF EXISTS body_text,
      DROP COLUMN IF EXISTS editor_type,
      DROP COLUMN IF EXISTS content_schema_version;
    ```

    WICHTIG: release_version_notes hat ebenfalls bereits body_html aus Migration 0064 — kein ADD COLUMN body_html.
  </action>
  <verify>
    <automated>grep -l "body_json" database/migrations/0069_anime_fansub_project_notes_tiptap.up.sql database/migrations/0070_release_version_notes_tiptap.up.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ADD COLUMN IF NOT EXISTS body_json" database/migrations/0069_anime_fansub_project_notes_tiptap.up.sql` → findet Treffer
    - `grep "ADD COLUMN IF NOT EXISTS body_json" database/migrations/0070_release_version_notes_tiptap.up.sql` → findet Treffer
    - Alle 8 Dateien (0067-0070, je .up.sql + .down.sql) existieren
  </acceptance_criteria>
  <done>
    Alle 8 Migrationsdateien (0067-0070, je .up.sql + .down.sql) existieren.
    Jede .up.sql enthält: ADD COLUMN IF NOT EXISTS body_json JSONB NULL, body_text TEXT NOT NULL DEFAULT '', editor_type TEXT NOT NULL DEFAULT 'tiptap', content_schema_version INT NOT NULL DEFAULT 1.
    Kein .up.sql versucht body_html hinzuzufügen.
  </done>
</task>

</tasks>

<verification>
```bash
# Alle 8 Migrationsdateien vorhanden (0067-0070)
ls database/migrations/006[7-9]_*tiptap*.sql database/migrations/0070_*tiptap*.sql

# Kein ADD COLUMN body_html in den neuen Migrations
grep -r "ADD COLUMN.*body_html" database/migrations/006[7-9]_*tiptap*.sql database/migrations/0070_*tiptap*.sql && echo "FEHLER" || echo "OK"

# IF NOT EXISTS in allen UP-Migrations vorhanden
grep -c "IF NOT EXISTS" database/migrations/0067_fansub_group_notes_tiptap.up.sql
```
</verification>

<success_criteria>
- 8 SQL-Dateien existieren: 0067-0070, je .up.sql + .down.sql
- Jede .up.sql enthält genau 4 ADD COLUMN IF NOT EXISTS: body_json, body_text, editor_type, content_schema_version
- Keine .up.sql enthält ADD COLUMN body_html (bereits vorhanden)
- Jede .down.sql enthält DROP COLUMN IF EXISTS für dieselben 4 Spalten
- go build ./... läuft grün (Migrations werden erst beim Dockerstart angewendet)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/41-globalen-tiptap-rich-text-editor-einfuehren/41-01-SUMMARY.md` erstellen.
</output>
