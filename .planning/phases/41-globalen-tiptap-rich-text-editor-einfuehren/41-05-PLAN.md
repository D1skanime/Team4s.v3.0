---
phase: 41-globalen-tiptap-rich-text-editor-einfuehren
plan: "05"
type: execute
wave: 3
depends_on:
  - "41-03"
  - "41-04"
files_modified:
  - frontend/src/types/fansubNotes.ts
  - frontend/src/types/releaseVersionNotes.ts
  - frontend/src/lib/api.ts
  - frontend/src/app/admin/fansubs/[id]/edit/NotesTab.helpers.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/NotesTab.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNotesSection.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionNotesTab.tsx
  - backend/internal/repository/release_version_notes_repository.go
  - backend/internal/handlers/admin_content_handler.go
autonomous: true
requirements:
  - TIPTAP-EDITOR-01
must_haves:
  truths:
    - "GroupNoteDraft.bodyMarkdown ist durch bodyJson: unknown ersetzt"
    - "NotesTab.helpers.tsx verwendet RichTextEditor statt Markdown-Textarea"
    - "AnimeProjectNotesSection.tsx verwendet RichTextEditor"
    - "ReleaseVersionNotesTab.tsx verwendet RichTextEditor mit mode='shortnote'"
    - "API-Calls senden body_json als JSON-Objekt (nicht body_markdown)"
    - "BulkNoteInput hat bodyJson statt bodyMarkdown"
    - "Phase-40-Hilfetexte (ROLE_HELP_TEXTS, Schreibimpulse) bleiben erhalten"
    - "npm run typecheck + npm run lint laufen grün"
  artifacts:
    - path: frontend/src/types/fansubNotes.ts
      provides: "Typ-Definitionen mit bodyJson, bodyText, editorType, contentSchemaVersion"
      contains: "bodyJson"
    - path: frontend/src/app/admin/fansubs/[id]/edit/NotesTab.helpers.tsx
      provides: "GroupNoteDraft + StoryDraft mit bodyJson, RichTextEditor-Nutzung"
      contains: "RichTextEditor"
    - path: frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionNotesTab.tsx
      provides: "Release-Notizen mit RichTextEditor mode=shortnote"
      contains: "shortnote"
  key_links:
    - from: frontend/src/app/admin/fansubs/[id]/edit/NotesTab.helpers.tsx
      to: frontend/src/components/editor/index.ts
      via: "import { RichTextEditor } from '@/components/editor'"
      pattern: "RichTextEditor"
    - from: frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionNotesTab.tsx
      to: frontend/src/components/editor/index.ts
      via: "import { RichTextEditor } from '@/components/editor'"
      pattern: "mode.*shortnote"
---

<objective>
Admin-UI auf RichTextEditor umstellen: alle vier Textbereiche ersetzen Markdown-Textarea durch RichTextEditor, Typen und API-Calls werden auf body_json migriert, release_version_notes BulkUpsert bekommt body_json, Phase-40-Hilfetexte bleiben erhalten.

Purpose: Die globalen Komponenten aus Plan 41-04 und das Backend aus Plan 41-03 sind bereit — jetzt werden die vier Admin-UIs verdrahtet.

Output: Alle vier Textbereiche nutzen RichTextEditor; Typen und API-Layer sind aktualisiert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-globalen-tiptap-rich-text-editor-einfuehren/41-CONTEXT.md
@.planning/phases/41-globalen-tiptap-rich-text-editor-einfuehren/41-RESEARCH.md

<interfaces>
<!-- RichTextEditor Props (aus Plan 41-04):
  value: unknown | null
  onChange: (value: unknown) => void
  placeholder?: string
  helperText?: string
  mode?: 'longform' | 'shortnote'
  disabled?: boolean
  minHeight?: number
  Import: import { RichTextEditor } from '@/components/editor'
-->

<!-- Aktuelles FansubGroupNote-API-Response (nach Plan 41-03):
  id, fansubGroupId, title, bodyMarkdown (legacy), bodyHtml, bodyJson, bodyText,
  editorType, contentSchemaVersion, visibility, status, sortOrder, ...
  Admin-Create/Update Request: { title, body_json: object, visibility, status, sort_order }
-->

<!-- GroupNoteDraft (aktuell — muss geändert werden):
  bodyMarkdown: string → bodyJson: unknown | null
-->

<!-- StoryDraft (aktuell — muss geändert werden):
  bodyMarkdown: string → bodyJson: unknown | null
-->

<!-- NoteFormState in ReleaseVersionNotesTab (aktuell):
  bodyMarkdown: string → bodyJson: unknown | null
-->

<!-- BulkNoteInput in release_version_notes_repository.go (Go):
  BodyMarkdown string → BodyJSON []byte
  BodyHTML     string → bleibt (wird vom Service befüllt)
-->

<!-- release_version_notes Handler befindet sich in admin_content_fansub_notes.go
  (in Phase 40 noch in der alten Datei — prüfen in welcher Datei nach Plan 41-03-Split)
  BulkUpsertReleaseVersionNotes nimmt BulkNoteInput-Slice entgegen
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript-Typen und API-Layer migrieren + release_version_notes BulkUpsert auf body_json</name>
  <read_first>
    - frontend/src/types/fansubNotes.ts (aktuelle Typdefinitionen)
    - frontend/src/types/releaseVersionNotes.ts (aktuelle Typdefinitionen inkl. BulkNoteInput)
    - frontend/src/lib/api.ts (API-Funktionen für notes und bulkUpsertReleaseVersionNotes)
    - backend/internal/repository/release_version_notes_repository.go (BulkNoteInput Struct)
    - backend/internal/handlers/ (welche Datei hat den release_version_notes Handler nach Plan 41-03?)
  </read_first>
  <files>
    frontend/src/types/fansubNotes.ts,
    frontend/src/types/releaseVersionNotes.ts,
    frontend/src/lib/api.ts,
    backend/internal/repository/release_version_notes_repository.go,
    backend/internal/handlers/admin_content_handler.go
  </files>
  <action>
    **1. fansubNotes.ts aktualisieren**

    FansubGroupNote: `bodyMarkdown?: string` (legacy, optional) beibehalten;
    neu hinzufügen: `bodyJson?: unknown | null`, `bodyText?: string | null`,
    `editorType?: string | null`, `contentSchemaVersion?: number | null`.

    Analog für MemberGroupStory und AnimeFansubProjectNote.

    **2. releaseVersionNotes.ts aktualisieren**

    ReleaseVersionNote: analog zu oben um bodyJson etc. erweitern.

    BulkNoteInput: `bodyMarkdown: string` → `bodyJson: unknown | null`.
    (bodyMarkdown komplett entfernen oder als optional legacy behalten — bevorzugt entfernen
    da der Backend-Struct in Plan 41-03 umgestellt wird)

    **3. api.ts aktualisieren**

    createFansubGroupNote Request-Typ: `body_markdown` → `body_json: unknown`.
    updateFansubGroupNote Request-Typ analog.
    createMemberGroupStory, updateMemberGroupStory analog.
    upsertAnimeFansubProjectNote analog.
    bulkUpsertReleaseVersionNotes: BulkNoteInput.bodyMarkdown → bodyJson, Serialisierung prüfen.

    **4. Go: BulkNoteInput und release_version_notes Handler**

    In backend/internal/repository/release_version_notes_repository.go:
    - BulkNoteInput: `BodyMarkdown string` → `BodyJSON []byte` + `BodyText string` +
      `EditorType string` + `ContentSchemaVersion int`
    - BulkUpsertReleaseVersionNotes SQL: body_json, body_text, editor_type, content_schema_version
      zu den INSERT/UPDATE-Feldern hinzufügen.
    - ReleaseVersionNote Struct: BodyJSON []byte, BodyText string, EditorType string, ContentSchemaVersion int.

    In admin_content_handler.go (oder der Datei wo BulkUpsertReleaseVersionNotes Handler ist):
    - Handler: body_json aus Request parsen, tiptapSvc.ValidateJSON/RenderHTML/ExtractText aufrufen,
      in BulkNoteInput mit BodyJSON/BodyHTML/BodyText befüllen.
    - WithReleaseVersionNoteDeps() bereits vorhanden — tiptapSvc ist über WithTipTapDeps gesetzt.
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "bodyJson" frontend/src/types/fansubNotes.ts` → findet Treffer
    - `grep "bodyJson" frontend/src/types/releaseVersionNotes.ts` → findet Treffer
    - `grep "body_json" frontend/src/lib/api.ts` → findet Treffer
    - `grep "BodyJSON" backend/internal/repository/release_version_notes_repository.go` → findet Treffer
    - `cd frontend && npm run typecheck` → kein Fehler
    - `cd backend && go build ./...` → kein Fehler
  </acceptance_criteria>
  <done>
    Alle TypeScript-Typen haben bodyJson-Feld.
    API-Funktionen senden body_json statt body_markdown.
    Go BulkNoteInput hat BodyJSON statt BodyMarkdown.
    typecheck und go build laufen grün.
  </done>
</task>

<task type="auto">
  <name>Task 2: Vier Admin-UI-Bereiche auf RichTextEditor umstellen</name>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/NotesTab.helpers.tsx (GroupNoteDraft, StoryDraft, Editor-Komponenten)
    - frontend/src/app/admin/fansubs/[id]/edit/NotesTab.tsx (API-Calls, State-Management)
    - frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNotesSection.tsx (Formular-State)
    - frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionNotesTab.tsx (NoteFormState, ROLE_HELP_TEXTS)
    - frontend/src/components/editor/index.ts (Export-Surface aus Plan 41-04)
  </read_first>
  <files>
    frontend/src/app/admin/fansubs/[id]/edit/NotesTab.helpers.tsx,
    frontend/src/app/admin/fansubs/[id]/edit/NotesTab.tsx,
    frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNotesSection.tsx,
    frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionNotesTab.tsx
  </files>
  <action>
    **1. NotesTab.helpers.tsx**

    GroupNoteDraft: `bodyMarkdown: string` → `bodyJson: unknown | null`
    StoryDraft: `bodyMarkdown: string` → `bodyJson: unknown | null`

    emptyGroupNoteDraft(): `bodyJson: null` statt `bodyMarkdown: ''`
    emptyStoryDraft(): `bodyJson: null` statt `bodyMarkdown: ''`

    GroupNoteEditor-Komponente (oder equivalent): Ersetze die Markdown-Textarea
    durch `<RichTextEditor value={draft.bodyJson} onChange={(v) => onUpdate({ ...draft, bodyJson: v })} mode="longform" />`.

    Import hinzufügen: `import { RichTextEditor } from '@/components/editor'`

    MarkdownToolbarInline und MarkdownPreview entfernen (oder als Stubs belassen falls
    andere Stellen noch verwenden — prüfen mit grep).

    CLAUDE.md-Limit: NotesTab.helpers.tsx darf 450 Zeilen nicht überschreiten.
    Wenn nötig, split in helpers.tsx + editor.tsx oder ähnlich.

    **2. NotesTab.tsx**

    groupNoteFromApi: `bodyMarkdown: ...` → `bodyJson: note.bodyJson ?? null`
    storyFromApi: analog.

    Save-Payloads: `body_markdown: draft.bodyMarkdown` → `body_json: draft.bodyJson`.

    **3. AnimeProjectNotesSection.tsx**

    NoteFormState: `bodyMarkdown: string` → `bodyJson: unknown | null`

    Textarea-Rendering ersetzen durch:
    `<RichTextEditor value={formState.bodyJson} onChange={(v) => setFormState(s => ({ ...s, bodyJson: v }))} mode="longform" />`

    Phase-40-Schreibimpulse (helperText-Props) beibehalten und als helperText-Prop übergeben.

    Save-Payload: `body_json: formState.bodyJson` statt `body_markdown: formState.bodyMarkdown`.

    **4. ReleaseVersionNotesTab.tsx**

    NoteFormState: `bodyMarkdown: string` → `bodyJson: unknown | null`

    buildInitialState: `bodyMarkdown: existing?.bodyMarkdown ?? ''` → `bodyJson: existing?.bodyJson ?? null`

    RoleNoteField-Komponente (oder inline): Ersetze Textarea durch:
    `<RichTextEditor
       value={formState.bodyJson}
       onChange={(v) => setFormStates(s => ({ ...s, [key]: { ...s[key], bodyJson: v, isDirty: true } }))}
       mode="shortnote"
       placeholder={roleInfo.placeholder}
       helperText={roleInfo.helpText}
     />`

    ROLE_HELP_TEXTS bleibt unverändert erhalten.

    Save-Payload (BulkNoteInput): `bodyJson: formState.bodyJson` statt `bodyMarkdown: ...`.

    CHAR_WARN_LIMIT-Logik kann entfernt werden (CharacterCount-Extension übernimmt das optional).

    WICHTIG für alle vier Dateien:
    - Import `{ RichTextEditor } from '@/components/editor'` hinzufügen
    - `marked`-Import und `MarkdownPreview` entfernen (sofern nicht anderweitig genutzt)
    - 450-Zeilen-Limit einhalten
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "RichTextEditor" frontend/src/app/admin/fansubs/[id]/edit/NotesTab.helpers.tsx` → findet Treffer
    - `grep "RichTextEditor" frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNotesSection.tsx` → findet Treffer
    - `grep "shortnote" frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionNotesTab.tsx` → findet Treffer
    - `grep "ROLE_HELP_TEXTS" frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionNotesTab.tsx` → findet Treffer (Phase-40-Hilfetexte erhalten)
    - `grep "bodyMarkdown" frontend/src/app/admin/fansubs/[id]/edit/NotesTab.tsx` → kein Treffer ODER nur als Legacy-Feld beim API-Response-Lesen
    - `wc -l frontend/src/app/admin/fansubs/[id]/edit/NotesTab.helpers.tsx` → unter 450
    - `wc -l frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionNotesTab.tsx` → unter 450
    - `cd frontend && npm run typecheck` → kein Fehler
  </acceptance_criteria>
  <done>
    Alle vier UI-Bereiche verwenden RichTextEditor.
    Phase-40-Hilfetexte bleiben erhalten (ROLE_HELP_TEXTS, Schreibimpulse).
    mode="shortnote" wird für release_version_notes verwendet.
    npm run typecheck grün.
    Alle geänderten Dateien unter 450 Zeilen.
  </done>
</task>

</tasks>

<verification>
```bash
cd frontend && npm run typecheck
cd frontend && npm run lint
cd backend && go build ./...
grep -r "RichTextEditor" frontend/src/app/admin/fansubs/[id]/edit/
grep "shortnote" frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionNotesTab.tsx
grep "ROLE_HELP_TEXTS" frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionNotesTab.tsx
```
</verification>

<success_criteria>
- npm run typecheck: kein Fehler
- npm run lint: kein Fehler
- go build ./...: kein Fehler
- Alle vier Admin-UIs importieren und verwenden RichTextEditor
- ReleaseVersionNotesTab nutzt mode="shortnote"
- ROLE_HELP_TEXTS bleibt unverändert in ReleaseVersionNotesTab
- Alle geänderten Dateien unter 450 Zeilen
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/41-globalen-tiptap-rich-text-editor-einfuehren/41-05-SUMMARY.md` erstellen.
</output>
