---
phase: 65
slug: member-vorschlaege-review-queue
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-02
---

# Phase 65 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — custom CSS Modules (`ui.module.css`) + CSS custom properties (`globals.css`) |
| Preset | not applicable |
| Component library | Custom — `@/components/ui` (`Button`, `Badge`, `Card`, `Modal`, `Drawer`, `FormField`, `Input`, `Textarea`, `Select`, `EmptyState`, `ErrorState`, `LoadingState`, `SectionHeader`, `Toolbar`) |
| Icon library | lucide-react |
| Font | Inter, "Segoe UI", system-ui, sans-serif (`--font-sans` from globals.css) |

Source: `frontend/src/styles/globals.css`, `frontend/src/components/ui/ui.module.css`

---

## Spacing Scale

Declared values — all from `globals.css` `--space-*` tokens:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px (`--space-1`) | Icon gaps, role-chip gaps, badge gaps |
| sm | 8px (`--space-2`) | Compact element spacing, button cluster gaps |
| md | 16px (`--space-4`) | Default element spacing, form field gaps, card padding |
| lg | 24px (`--space-5`) | Section padding, modal body padding |
| xl | 32px (`--space-6`) | Grouped-section gaps (e.g. „In Prüfung" / „Bestätigt" / „Abgelehnt") |
| 2xl | 48px (`--space-7`) | Major section breaks between proposal form and list sections |
| 3xl | 64px (`--space-8`) | Page-level top spacing |

Exceptions:
- Touch targets for confirm/reject action buttons: minimum 44px height (`--control-height-md` = 44px)
- Icon-only close button in modal header: 40px × 40px (`.closeButton` in ui.module.css)

Source: `globals.css` lines 58–65; `ui.module.css` `.control-height-md` = 44px

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px | 400 | 1.5 |
| Label / field label | 13px | 700 | 1 |
| Section heading (`h2`) | ~17px (1.08rem via `.sectionTitle`) | 600 | 1.15 |
| Hint / meta text (year range, group ref, timestamp) | 12px | 400 | 1.45 |

Notes:
- Role chips / badges: 12px at weight 700 (`.badge` in ui.module.css)
- Button labels: 14px at weight 600 (`.button` in ui.module.css)
- Form section heading uses `.sectionTitle` class: 1.08rem / line-height 1.15 / letter-spacing -0.015em

Source: `globals.css` body `font-size: 16px; line-height: 1.5`; `ui.module.css` field/badge/button rules

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#f6f4ef` (`--surface-canvas`) | Page background, section backgrounds |
| Secondary (30%) | `#ffffff` + `#e6ebf3`→`#f4f7fb` gradient (`.card`) | Contribution cards, review queue cards, form container |
| Accent (10%) | `#5f84dd` (`--color-primary`) | „+ Beitrag vorschlagen"-Button (primary), active tab indicator, focus ring |
| Destructive | `#82122c` (`--button-danger-mid`) | „Ablehnen"-Button, field error text, Duplikat-Fehler-Banner |

Accent reserved for:
- The primary „+ Beitrag vorschlagen" CTA button (`.buttonPrimary`)
- The „Bestätigen" action button in the Review-Queue (`.buttonSuccess` green is used here — see note below)
- Active tab/filter badge highlight

Color clarification for review actions:
- „Bestätigen" uses `.buttonSuccess` (green gradient, `--button-success-*` tokens) — not the blue accent — because it is a positive confirmation, not primary navigation
- „Ablehnen" uses `.buttonDanger` (dark red gradient, `--button-danger-*` tokens)
- „Historisch öffentlich schalten" (90-Tage self-publish) uses `.buttonSecondary` (dark blue gradient) to visually distinguish it from Leader-confirmed green

Status badge color mapping (via `Badge` component variants):
- `proposed` / „In Prüfung": `variant="warning"` (amber, `#8a6420` text)
- `confirmed` / „Bestätigt": `variant="success"` (green, `#246b52` text)
- `disputed` / „Abgelehnt": `variant="danger"` (red-toned, `#7b2941` text)
- `(historisch)` unverified self-published: `variant="muted"` (grey, `#66758d` text)

Source: `globals.css` color tokens; `ui.module.css` badge/button variant rules

---

## Component Inventory

### New components to create

| Component | Path | Description |
|-----------|------|-------------|
| `ProposalForm` | `frontend/src/components/contributions/ProposalForm.tsx` | Modal form: Anime-select, group-select (own groups only), multi-select role chips (from `role_definitions`), Freitext-Notizfeld (prominent, optional), `started_year`/`ended_year` YearPicker (optional), 90-Tage-Hinweis (D-13) |
| `MyProposalsSection` | `frontend/src/components/contributions/MyProposalsSection.tsx` | Extends `MyContributionsSection` sektion 3 — renders status-grouped proposals: „In Prüfung", „Bestätigt", „Abgelehnt"; includes „+ Beitrag vorschlagen" button and self-publish trigger |
| `ReviewQueue` | `frontend/src/components/contributions/ReviewQueue.tsx` | Leader/Admin view per group — list of `proposed` entries as cards with Confirm/Reject actions and optional rejection-note textarea |

### Existing components to extend / consume

| Component | File | How Used |
|-----------|------|---------|
| `ContributionCard` | `contributions/ContributionCard.tsx` | Extend to support `mode="proposal"` for the member dashboard (showing status badge, optional `review_note` when disputed) |
| `Modal` | `ui/Modal.tsx` | Wraps `ProposalForm` — `width: min(560px, 100%)` per `.modalPanel` |
| `Card` | `ui/Card.tsx` | Review-Queue proposal cards, variant `cardCompact` |
| `Badge` | `ui/Badge.tsx` | Status chips and role chips throughout |
| `Button` | `ui/Button.tsx` | All action buttons (primary, success, danger, secondary, ghost) |
| `FormField` + `Input` + `Textarea` + `Select` | `ui/` | ProposalForm fields |
| `EmptyState` | `ui/EmptyState.tsx` | Empty queue and empty proposal sections |
| `ErrorState` | `ui/ErrorState.tsx` | API-Fehler in form submission and queue load |
| `SectionHeader` | `ui/SectionHeader.tsx` | Section headings with count badge and action button |

---

## Interaction Contracts

### ProposalForm (Modal)

- Trigger: Button „+ Beitrag vorschlagen" in `MyProposalsSection`
- Opens as `Modal` (centered, backdrop blur)
- Form fields (top to bottom):
  1. **Gruppe** — `Select` populated with user's own `hist_fansub_group_members` entries only. Required. Placeholder: „Gruppe auswählen"
  2. **Anime** — `Select` or search-enabled combobox. Required. Placeholder: „Anime suchen oder auswählen"
  3. **Rollen** — Multi-select chip list from `role_definitions` (`context='anime_contribution'`), using `label_de`. Minimum 1 required. Invalid state shown when submitting with 0 roles.
  4. **Notiz** — `Textarea` (prominent, 3 rows minimum). Placeholder: „Kurze Erläuterung (z. B. »war 2005 Co-Editor«)". Optional but visually prominent.
  5. **Von Jahr / Bis Jahr** — `YearPicker` pair side-by-side. Both optional.
  6. **Hinweis-Banner** (D-13): static info strip below the form fields, before submit button. Text: „Reagiert kein Gruppen-Leader binnen 90 Tagen, kannst du den Vorschlag selbst als unverifizierten historischen Eintrag öffentlich schalten."
- Footer: left-aligned cancel ghost button, right-aligned primary submit button „Beitrag einreichen"
- Submit loading: primary button shows spinner, disabled
- Duplicate error (409): inline `ErrorState` above the form fields, variant danger: „Für diese Kombination aus Gruppe, Anime und deiner Identität existiert bereits ein Beitrag."
- Validation error (0 roles): field-level error below role chips: „Bitte wähle mindestens eine Rolle aus."

### MyProposalsSection (Member Dashboard)

- Section heading: „Eigene Vorschläge" with count of all proposals in parentheses
- Button „+ Beitrag vorschlagen" in section header actions, `.buttonPrimary` size small
- Three sub-sections, each with count in heading, collapsed when empty:
  - „In Prüfung (N)" — proposed entries. Shows proposal card with status badge `warning`, role chips, anime name, group name, submitted date. No action buttons.
  - „Bestätigt (N)" — confirmed entries. Shows status badge `success`. No action buttons (read-only).
  - „Abgelehnt (N)" — disputed entries. Shows status badge `danger`. If `review_note` present: display below card as soft-label „Ablehngrund:" followed by the text.
- 90-Tage self-publish trigger: appears on a `proposed` card only when `can_self_publish === true`. Button variant `.buttonSecondary` size small, label „Historisch öffentlich schalten". On click: confirm via inline text prompt (no modal needed): static info text „Dieser Eintrag wird als unverifizierten historischen Beitrag öffentlich sichtbar" + confirm button „Jetzt öffentlich schalten" inline below the card.

### ReviewQueue (Leader/Admin, within `admin/my-groups/[id]`)

- Section heading: „Offene Vorschläge (N)" with `SectionHeader`
- Empty state: `EmptyState` with message: „Keine offenen Vorschläge für diese Gruppe."
- Each proposed entry rendered as `.cardCompact`:
  - Row 1: Member display name or nick + status badge `warning` „In Prüfung"
  - Row 2: Anime title + role chips (`Badge variant="info"`)
  - Row 3: Notiz text (if present, grey `--text-soft`, italic, max 3 lines with text-overflow)
  - Row 4: „Eingereicht am {date}" in `--text-faint` 12px
  - Footer (`.cardFooter`): left — ghost button with timestamp; right — button cluster: „Bestätigen" (`.buttonSuccess` small) + „Ablehnen" (`.buttonDanger` small)
- „Ablehnen" click: expands an inline rejection-note field below the card (not a modal). `Textarea` with placeholder „Ablehngrund (optional)" + confirm button „Ablehnung bestätigen" (`.buttonDanger` small) + cancel link. Submits with optional `review_note` body.
- Action loading states: button shows spinner, disabled. Other cards remain interactive.
- On confirm/reject success: card animates out (opacity 0 over 200ms) and is removed from list. Count badge updates.
- API error on action: inline `ErrorState` below the affected card only. Global queue remains usable.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | „+ Beitrag vorschlagen" |
| Review confirm action | „Bestätigen" |
| Review reject action | „Ablehnen" |
| Self-publish action | „Historisch öffentlich schalten" |
| Modal submit | „Beitrag einreichen" |
| Modal cancel | „Abbrechen" |
| Reject confirm | „Ablehnung bestätigen" |
| Empty state heading — proposals section | „Noch keine Vorschläge" |
| Empty state body — proposals section | „Schlage deinen ersten Beitrag für eine Gruppe vor, in der du Mitglied bist." |
| Empty state heading — review queue | „Keine offenen Vorschläge" |
| Empty state body — review queue | „Für diese Gruppe wurden noch keine Vorschläge eingereicht." |
| Error state — proposal form (duplicate) | „Für diese Kombination aus Gruppe, Anime und deiner Identität existiert bereits ein Beitrag." |
| Error state — proposal form (validation: 0 roles) | „Bitte wähle mindestens eine Rolle aus." |
| Error state — proposal form (generic) | „Der Vorschlag konnte nicht eingereicht werden. Bitte versuche es erneut." |
| Error state — review queue load | „Die Vorschläge konnten nicht geladen werden. Bitte Seite neu laden." |
| Error state — review action | „Aktion fehlgeschlagen. Bitte versuche es erneut." |
| 90-Tage hint (in form) | „Reagiert kein Gruppen-Leader binnen 90 Tagen, kannst du den Vorschlag selbst als unverifizierten historischen Eintrag öffentlich schalten." |
| Self-publish inline confirm text | „Dieser Eintrag wird als unverifizierten historischen Beitrag öffentlich sichtbar." |
| Self-publish confirm button | „Jetzt öffentlich schalten" |
| Self-publish cancel | „Abbrechen" |
| Review note label | „Ablehngrund:" |
| Review note placeholder | „Ablehngrund (optional)" |
| Status labels | „In Prüfung" (proposed), „Bestätigt" (confirmed), „Abgelehnt" (disputed), „(historisch)" (unverified self-published) |
| Proposal section headings | „In Prüfung (N)", „Bestätigt (N)", „Abgelehnt (N)" |
| Confirmed proposals read-only note | „Dieser Beitrag wurde durch einen Gruppen-Leader bestätigt." |

Destructive actions in this phase:
- „Ablehnen" in Review-Queue: status → `disputed`, no delete. No separate confirmation modal required — Leader explicitly clicks reject, sees expansion of rejection-note field, then clicks „Ablehnung bestätigen". Two-step is sufficient; no window.confirm or separate confirm modal.
- „Ablehnen" is NOT irreversible (internal audit record remains). Therefore no hard "Are you sure?" modal.
- „Historisch öffentlich schalten" (self-publish): one-time action after 90 days, effectively irreversible for the member. Inline two-step: info text then confirm button — no separate modal.

Source: CONTEXT.md D-19 (German, correct umlauts), D-16/D-17/D-13, design sketches in `<specifics>` block

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — project uses custom CSS Modules | not applicable |
| Third-party | none | not applicable |

No shadcn, no external registry. Project uses custom `@/components/ui` library with `ui.module.css`. Registry vetting gate is not triggered.

Source: absence of `frontend/components.json` confirmed; `frontend/package.json` does not list `@shadcn/ui`

---

## States and Accessibility

### Required visual states for all interactive elements
- Default, hover (`transform: translateY(-1px)` + shadow lift per `.button:hover`)
- Focus-visible: `outline: 2px solid var(--focus-outline)` + `outline-offset: 2px` (globals.css `:focus-visible`)
- Loading: spinner inside button, `aria-disabled="true"`, `cursor: not-allowed`
- Disabled: `opacity: 0.56`, `cursor: not-allowed`, `transform: none`
- Error (form field): `.controlInvalid` border-color `rgba(130,18,44,0.34)` + red focus ring + `.fieldError` text below

### Accessibility requirements
- `ProposalForm` modal: `role="dialog"`, `aria-modal="true"`, `aria-labelledby` pointing to dialog title
- Focus trap inside modal while open; focus returns to trigger button on close
- Role chip multi-select: `role="group"` with `aria-label="Rollen auswählen"`, each chip is a `<button>` or `<input type="checkbox">` with visible label from `label_de`
- Review-Queue card actions: each confirm/reject button has `aria-label` identifying the member: `aria-label="Vorschlag von {member_name} bestätigen"`
- Status badges: `aria-label` set to readable status: „Status: In Prüfung" / „Status: Bestätigt" / „Status: Abgelehnt"
- Error banners: `role="alert"` for live region announcement
- Empty states: no `role="alert"` — static, not live

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
