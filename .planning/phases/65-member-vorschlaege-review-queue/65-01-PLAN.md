---
phase: 65-member-vorschlaege-review-queue
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/0089_anime_contributions_review_note.up.sql
  - database/migrations/0089_anime_contributions_review_note.down.sql
  - backend/internal/repository/anime_contributions_proposal_repository.go
autonomous: true
requirements:
  - P65-SC1
  - P65-SC2
  - P65-SC3

must_haves:
  truths:
    - "Migration 0089 fügt die Spalte review_note TEXT NULL zu anime_contributions hinzu und ist up/down idempotent"
    - "ProposalRepository-Datei existiert mit CreateProposal, ListProposedByGroup, Confirm, Reject und SelfPublish-Methoden auf AnimeContributionsRepository"
    - "can_self_publish wird on-read berechnet (status=proposed AND created_at+90d < NOW()) und in der Proposal-Methode zurückgegeben"
  artifacts:
    - path: "database/migrations/0089_anime_contributions_review_note.up.sql"
      provides: "ALTER TABLE anime_contributions ADD COLUMN IF NOT EXISTS review_note TEXT NULL"
    - path: "database/migrations/0089_anime_contributions_review_note.down.sql"
      provides: "ALTER TABLE anime_contributions DROP COLUMN IF EXISTS review_note"
    - path: "backend/internal/repository/anime_contributions_proposal_repository.go"
      provides: "CreateProposal, ListProposedByGroup, Confirm, Reject, SelfPublish"
  key_links:
    - from: "backend/internal/repository/anime_contributions_proposal_repository.go"
      to: "backend/internal/repository/anime_contributions_repository.go"
      via: "Methoden auf AnimeContributionsRepository (gleiche Receiver-Typ-Erweiterung wie anime_contributions_member_repository.go)"
      pattern: "func \\(r \\*AnimeContributionsRepository\\)"
    - from: "anime_contributions_proposal_repository.go"
      to: "anime_contributions"
      via: "CreateProposal setzt status='proposed', created_by=appUserID, is_public_on_*=false"
      pattern: "status.*proposed"
---

<objective>
Migration 0089 ergänzt die für D-08 benötigte review_note-Spalte an anime_contributions. Die neue Repository-Datei anime_contributions_proposal_repository.go liefert alle Datenbankoperationen für Vorschläge und Review-Aktionen — nach dem etablierten Auslagerungs-Muster von anime_contributions_member_repository.go, da anime_contributions_repository.go bereits 447 Zeilen hat.

Purpose: Datenbankschicht und Repository-Interface als gemeinsame Grundlage für alle Proposal/Review-Handler der Wellen 2 und 3.
Output: Migration + neue Repository-Datei mit allen Proposal/Review-Methoden.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/65-member-vorschlaege-review-queue/65-CONTEXT.md
@.planning/phases/65-member-vorschlaege-review-queue/65-RESEARCH.md
@.planning/phases/65-member-vorschlaege-review-queue/65-PATTERNS.md
</context>

<interfaces>
<!-- Schlüssel-Typen und Contracts aus dem bestehenden Repository. -->
<!-- Executor liest diese Dateien einmal vollständig, keine Codebase-Exploration nötig. -->

Aus backend/internal/repository/anime_contributions_repository.go (Zeilen 1–55):
  type AnimeContributionsRepository struct { db *pgxpool.Pool }
  func NewAnimeContributionsRepository(db *pgxpool.Pool) *AnimeContributionsRepository
  type AnimeContributionRow struct { ID, FansubGroupID, AnimeID, FansubGroupMemberID int64; Status string; Note *string; StartedYear, EndedYear *int; IsPublicOnAnimePage, IsPublicOnMemberProfile bool; ConfirmedBy, CreatedBy, UpdatedBy *int64; ConfirmedAt *time.Time; CreatedAt, UpdatedAt time.Time; RoleCodes []string }
  type AnimeContributionInput struct { FansubGroupMemberID int64; RoleCodes []string; Status string; StartedYear, EndedYear *int; Note *string; IsPublicOnAnimePage, IsPublicOnMemberProfile bool; CreatedBy *int64 }
  func (r *AnimeContributionsRepository) Create(ctx, fansubGroupID, animeID int64, input AnimeContributionInput) (*AnimeContributionRow, error)
  func (r *AnimeContributionsRepository) GetByID(ctx, id int64) (*AnimeContributionRow, error)
  -- isUniqueViolation(err) → ErrConflict; isForeignKeyViolation(err) → ErrNotFound
  -- addArg()-Builder-Pattern für Update (Zeilen 359–415)
  -- animeContributionSelectCols (Zeilen 55–80) — wiederverwenden in ListProposedByGroup

Aus backend/internal/repository/anime_contributions_member_repository.go:
  -- Auslagerungs-Vorbild: gleicher package-Header, Methoden auf *AnimeContributionsRepository
  func (r *AnimeContributionsRepository) ListByMemberID(ctx, memberID int64) ([]AnimeContributionRow, error)
  -- rows-Close-Pattern, scan-Loop, rows.Err()-Check

Aus database/migrations/0088_anime_contributions_constraints.up.sql:
  -- ALTER TABLE anime_contributions ADD CONSTRAINT ... (Muster für up-Migration)
  -- Gegenstück 0088.down.sql: ALTER TABLE anime_contributions DROP CONSTRAINT IF EXISTS ...
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0089 — review_note-Spalte</name>
  <files>database/migrations/0089_anime_contributions_review_note.up.sql, database/migrations/0089_anime_contributions_review_note.down.sql</files>
  <read_first>
    - database/migrations/0088_anime_contributions_constraints.up.sql (Muster für up-Migration)
    - database/migrations/0088_anime_contributions_constraints.down.sql (Muster für down-Migration)
  </read_first>
  <action>
    Erstelle zwei neue Migrationsdateien (append-only, nächste freie Nummer nach 0088) unter database/migrations/.

    up-Migration (0089_anime_contributions_review_note.up.sql):
    - ALTER TABLE anime_contributions ADD COLUMN IF NOT EXISTS review_note TEXT NULL;
    - Keine anderen Änderungen. Keine NOT NULL-Constraints. Keine Defaults. Bestehende Zeilen erhalten NULL.

    down-Migration (0089_anime_contributions_review_note.down.sql):
    - ALTER TABLE anime_contributions DROP COLUMN IF EXISTS review_note;

    Wichtig: review_note ist das Ablehngrund-Feld (D-08) für Leader/Admins — getrennt vom bestehenden note-Feld (D-02, Member-Kontext). NICHT das note-Feld verwenden oder umbenennen.
  </action>
  <verify>
    <automated>grep -r "review_note" database/migrations/0089_anime_contributions_review_note.up.sql | grep "ADD COLUMN IF NOT EXISTS review_note TEXT NULL"</automated>
  </verify>
  <acceptance_criteria>
    - 0089_anime_contributions_review_note.up.sql enthält exakt "ADD COLUMN IF NOT EXISTS review_note TEXT NULL"
    - 0089_anime_contributions_review_note.down.sql enthält exakt "DROP COLUMN IF EXISTS review_note"
    - Keine anderen Tabellen oder Spalten werden geändert
    - Beide Dateien liegen unter database/migrations/ (nicht backend/database/migrations/)
  </acceptance_criteria>
  <done>Beide Migrationsdateien existieren; up fügt review_note NULL hinzu, down entfernt sie wieder.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Proposal-Repository — CreateProposal, ListProposedByGroup, Confirm, Reject, SelfPublish</name>
  <files>backend/internal/repository/anime_contributions_proposal_repository.go</files>
  <read_first>
    - backend/internal/repository/anime_contributions_repository.go (AnimeContributionRow, AnimeContributionInput, animeContributionSelectCols, Create-TX-Muster Zeilen 286–356, addArg-Builder Zeilen 359–415, isUniqueViolation, isForeignKeyViolation, ErrConflict, ErrNotFound)
    - backend/internal/repository/anime_contributions_member_repository.go (Auslagerungs-Muster: package-Header, Receiver-Typ, rows-Scan-Loop)
  </read_first>
  <behavior>
    - CreateProposal(ctx, fansubGroupID, animeID int64, memberID int64, input ProposalInput): status='proposed', is_public_on_anime_page=false, is_public_on_member_profile=false, created_by=appUserID; isUniqueViolation → ErrConflict; fk-Fehler → ErrNotFound; gibt *AnimeContributionRow zurück
    - ListProposedByGroup(ctx, fansubGroupID int64): SELECT wo status='proposed' AND fansub_group_id=$1; enthält member_display_name und anime_title per JOIN
    - Confirm(ctx, contributionID, actorAppUserID int64): status='confirmed', is_public_on_anime_page=true, is_public_on_member_profile=true, confirmed_by=actorAppUserID, confirmed_at=NOW(); RowsAffected=0 → ErrNotFound
    - Reject(ctx, contributionID, actorAppUserID int64, reviewNote *string): status='disputed', review_note=$reviewNote; RowsAffected=0 → ErrNotFound; kein Hard-Delete
    - SelfPublish(ctx, contributionID, appUserID int64): PRÜFT serverseitig (status='proposed' AND created_at+INTERVAL '90 days' < NOW()); status bleibt 'proposed' (NICHT 'confirmed'); is_public_on_anime_page=true, is_public_on_member_profile=true, confirmed_by=appUserID, confirmed_at=NOW(); 90-Tage-Bedingung nicht erfüllt → ErrConflict; RowsAffected=0 → ErrNotFound
    - ListByMemberIDWithProposalFields erweitert ListByMemberID um can_self_publish: (ac.status='proposed' AND ac.created_at + INTERVAL '90 days' < NOW()) AS can_self_publish, review_note
  </behavior>
  <action>
    Neue Datei backend/internal/repository/anime_contributions_proposal_repository.go erstellen.
    - package repository, gleicher Header wie anime_contributions_member_repository.go
    - Imports: context, fmt, time (kein pgx direkt — nutze r.db wie die anderen Auslagerungen)
    - Kommentar: "Ausgelagert aus anime_contributions_repository.go fuer das 450-Zeilen-Limit."
    - ProposalInput als neuer Input-Typ: FansubGroupMemberID int64, RoleCodes []string (min 1), Note *string, StartedYear *int, EndedYear *int, AppUserID int64

    CreateProposal: TX beginnen; INSERT anime_contributions mit status='proposed', is_public_on_*=false, created_by=input.AppUserID; dann INSERT anime_contribution_roles (eine Zeile pro code); isUniqueViolation → ErrConflict; fk-Verletzung → ErrNotFound; Commit; Rückgabe via GetByID.

    ListProposedByGroup: SELECT animeContributionSelectCols + hfgm.member_display_name + a.title_de AS anime_title WHERE ac.status='proposed' AND ac.fansub_group_id=$1 ORDER BY ac.created_at ASC. Gibt []GroupProposalRow zurück (neuer Typ mit ID, FansubGroupMemberID, MemberDisplayName, AnimeID, AnimeTitle, RoleCodes, Note, CreatedAt).

    Confirm: UPDATE SET status='confirmed', is_public_on_anime_page=true, is_public_on_member_profile=true, confirmed_by=$actorID, confirmed_at=NOW(), updated_at=NOW() WHERE id=$contributionID AND status='proposed'; RowsAffected=0 → ErrNotFound.

    Reject: UPDATE SET status='disputed', review_note=$reviewNote, updated_at=NOW() WHERE id=$contributionID AND status='proposed'; RowsAffected=0 → ErrNotFound; kein Hard-Delete.

    SelfPublish: Erst SELECT für 90-Tage-Check (status='proposed' AND created_at + INTERVAL '90 days' < NOW()); pgx.ErrNoRows → ErrConflict (noch nicht abgelaufen oder falsche Status); dann UPDATE SET is_public_on_anime_page=true, is_public_on_member_profile=true, confirmed_by=$appUserID, confirmed_at=NOW(), updated_at=NOW() WHERE id=$id AND status='proposed' (status BLEIBT 'proposed').

    AnimeContributionRow erweitern: CanSelfPublish bool und ReviewNote *string als zusätzliche Felder für die Member-Listen-Query. Diese Felder werden in animeContributionSelectCols NICHT hinzugefügt (nur in den Proposal-Queries explizit berechnet).

    KEINE Änderungen an anime_contributions_repository.go (450-Zeilen-Limit bereits fast erreicht).
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go build ./internal/repository/...</automated>
  </verify>
  <acceptance_criteria>
    - go build ./internal/repository/... läuft ohne Fehler durch
    - anime_contributions_proposal_repository.go enthält alle 5 Methoden: CreateProposal, ListProposedByGroup, Confirm, Reject, SelfPublish
    - SelfPublish setzt status NICHT auf 'confirmed' (grep -c "status.*confirmed" der SelfPublish-Methode ergibt 0)
    - anime_contributions_repository.go bleibt unter 450 Zeilen (wc -l < 450)
    - anime_contributions_proposal_repository.go selbst bleibt unter 450 Zeilen
    - ErrConflict wird bei isUniqueViolation in CreateProposal zurückgegeben
    - ErrConflict wird zurückgegeben wenn SelfPublish vor Ablauf der 90 Tage aufgerufen wird
  </acceptance_criteria>
  <done>Repository-Datei kompiliert; alle 5 Methoden vorhanden; SelfPublish lässt Status auf 'proposed'.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB-Schema → Repository | review_note-Spalte akzeptiert beliebigen TEXT — wird durch Application-Layer-Länge und React-Default-Escaping eingeschränkt, aber nicht auf DB-Ebene |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01-01 | Tampering | SelfPublish-Repository | mitigate | 90-Tage-Check läuft serverseitig in SelfPublish via SELECT-vor-UPDATE innerhalb derselben Methode — Frontend-Gate ist nicht vertrauenswürdig |
| T-65-01-02 | Tampering | SQL-Queries in allen Methoden | accept | pgx parametrisierte Queries ($1/$2…) — SQL-Injection nicht möglich; bestehendes projektweites Muster |
| T-65-01-03 | Repudiation | Confirm/Reject ohne Audit | mitigate | confirmed_by/created_by werden in der DB persistiert; Audit-Log-Schreiben erfolgt im Handler (Plan 65-03) |
| T-65-01-SC | Tampering | npm/pip/cargo installs | accept | Diese Phase installiert keine neuen Pakete — Slopcheck nicht zutreffend |
</threat_model>

<verification>
- go build ./internal/repository/... grün
- grep -c "status.*=.*'confirmed'" anime_contributions_proposal_repository.go | SelfPublish-Bereich = 0 (Status bleibt 'proposed' bei Selbstschaltung)
- wc -l backend/internal/repository/anime_contributions_repository.go zeigt < 450
- Beide Migrationsdateien unter database/migrations/ vorhanden und enthalten die erwarteten ALTER TABLE-Statements
</verification>

<success_criteria>
- Migration 0089 ist syntaktisch korrekt und add/drop review_note idempotent
- Alle 5 Repository-Methoden sind implementiert und kompilieren
- SelfPublish prüft 90-Tage serverseitig und setzt Status NICHT auf 'confirmed'
- 450-Zeilen-Limit in allen berührten Dateien eingehalten
- Keine externen Pakete hinzugefügt
</success_criteria>

<output>
Erstelle .planning/phases/65-member-vorschlaege-review-queue/65-01-SUMMARY.md wenn fertig.
</output>
