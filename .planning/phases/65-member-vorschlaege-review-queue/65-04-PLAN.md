---
phase: 65-member-vorschlaege-review-queue
plan: 04
type: execute
wave: 3
depends_on:
  - 65-02
  - 65-03
files_modified:
  - frontend/src/types/contributions.ts
  - frontend/src/lib/api.ts
  - frontend/src/components/contributions/ProposalForm.tsx
  - frontend/src/components/contributions/MyProposalsSection.tsx
  - frontend/src/components/contributions/ReviewQueue.tsx
  - frontend/src/components/contributions/ContributionCard.tsx
  - frontend/src/app/me/contributions/page.tsx
  - frontend/src/app/admin/my-groups/[id]/page.tsx
  - frontend/src/styles/ui.module.css
autonomous: true
requirements:
  - P65-SC1
  - P65-SC2
  - P65-SC3

must_haves:
  truths:
    - "Member sieht 'Eigene Vorschläge'-Sektion auf me/contributions mit '+ Beitrag vorschlagen'-Button und Status-Gruppierung (In Prüfung / Bestätigt / Abgelehnt)"
    - "ProposalForm (Modal) erzwingt min. 1 Rolle, zeigt 90-Tage-Hinweis, blockiert Duplikat mit deutschem Fehlertext"
    - "90-Tage-Selbstschaltung erscheint nur auf proposed-Karten mit can_self_publish=true; zwei-Schritt-Bestätigung inline (kein Modal)"
    - "Leader/Admin sieht Review-Queue auf admin/my-groups/[id] mit Bestätigen/Ablehnen-Aktionen pro Vorschlag"
    - "CSS-Korrekturen: .fieldLabel 14px, .sectionTitle 1rem, .button font-weight 700"
  artifacts:
    - path: "frontend/src/types/contributions.ts"
      provides: "ProposalFormData, GroupProposalRow, GroupProposalsResponse; MeAnimeContribution erweitert um review_note und can_self_publish"
    - path: "frontend/src/lib/api.ts"
      provides: "createContributionProposal, selfPublishContribution, listGroupProposals, confirmProposal, rejectProposal"
    - path: "frontend/src/components/contributions/ProposalForm.tsx"
      provides: "Modal-Formular (Gruppe, Anime, Rollen, Notiz, Jahre, Hinweis-Banner)"
    - path: "frontend/src/components/contributions/MyProposalsSection.tsx"
      provides: "Status-gruppierte Vorschläge + CTA + SelfPublish-Trigger"
    - path: "frontend/src/components/contributions/ReviewQueue.tsx"
      provides: "Leader/Admin-Queue mit Confirm/Reject pro Karte"
  key_links:
    - from: "frontend/src/app/me/contributions/page.tsx"
      to: "frontend/src/components/contributions/MyProposalsSection.tsx"
      via: "MyProposalsSection in 'Eigene Vorschläge'-Sektion eingebunden (ersetzt Platzhalter)"
      pattern: "MyProposalsSection"
    - from: "frontend/src/app/admin/my-groups/[id]/page.tsx"
      to: "frontend/src/components/contributions/ReviewQueue.tsx"
      via: "ReviewQueue als neue Card-Section unterhalb der historischen Credits (D-06)"
      pattern: "ReviewQueue"
---

<objective>
Frontend-Slice: Typen, API-Calls und alle drei neuen Komponenten (ProposalForm, MyProposalsSection, ReviewQueue) werden erstellt und in die bestehenden Seiten eingebunden. ContributionCard erhält mode="proposal". CSS-Typo-Fixes aus UI-SPEC werden in ui.module.css angewendet.

Purpose: Vollständige member-seitige und leader-seitige UX für Vorschläge und Review-Queue gemäß UI-SPEC.
Output: 5 neue/geänderte Frontend-Dateien + 2 Seiten-Erweiterungen + CSS-Fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-member-vorschlaege-review-queue/65-CONTEXT.md
@.planning/phases/65-member-vorschlaege-review-queue/65-RESEARCH.md
@.planning/phases/65-member-vorschlaege-review-queue/65-PATTERNS.md
@.planning/phases/65-member-vorschlaege-review-queue/65-UI-SPEC.md
@.planning/phases/65-member-vorschlaege-review-queue/65-02-SUMMARY.md
@.planning/phases/65-member-vorschlaege-review-queue/65-03-SUMMARY.md
</context>

<interfaces>
<!-- Schlüssel-Contracts aus existierenden Dateien. -->

Aus frontend/src/types/contributions.ts (bestehend, Zeilen 50–62):
  export interface MeAnimeContribution { id: number; anime_id: number; fansub_group_id: number; fansub_group_member_id: number; status: 'confirmed' | 'proposed' | 'draft' | 'disputed' | 'hidden'; role_codes: string[]; started_year: number | null; ended_year: number | null; is_public_on_anime_page: boolean; is_public_on_member_profile: boolean; note: string | null }

Aus frontend/src/lib/api.ts (bestehend, Zeilen 6910–6960):
  export async function confirmAnimeContribution(contributionId: number): Promise<void>
  export async function rejectAnimeContribution(contributionId: number): Promise<void>
  -- authorizedFetch + parseApiErrorPayload + ApiError Pattern
  -- getApiBaseUrl() für Server/Browser-URLs

Aus frontend/src/components/contributions/MyContributionsSection.tsx (bestehend):
  -- readErrorMessage(error: unknown, fallback: string): string
  -- Error-Banner mit role="alert"
  -- useAuthSession Hook für hasAccessToken, isClientInitialized

Aus frontend/src/app/admin/my-groups/[id]/page.tsx (bestehend, 421 Zeilen):
  -- useCallback + useEffect Daten-Lade-Pattern
  -- Card, SectionHeader, EmptyState, ErrorState, LoadingState
  -- groupId aus params?.id (parseInt)
  -- styles aus page.module.css

Aus frontend/src/styles/ui.module.css:
  -- .fieldLabel { font-size: 13px } → muss 14px sein (UI-SPEC)
  -- .sectionTitle (zweite Deklaration) { font-size: 1.08rem } → muss 1rem sein
  -- .button { font-weight: 600 } → muss 700 sein

Aus UI-SPEC (Copywriting-Contract):
  CTA: „+ Beitrag vorschlagen"
  Confirm-Action: „Vorschlag bestätigen" (.buttonSuccess)
  Reject-Action: „Vorschlag ablehnen" (.buttonDanger)
  Self-publish: „Historisch öffentlich schalten" (.buttonSecondary)
  Modal-Submit: „Beitrag einreichen"
  Modal-Cancel: „Abbrechen"
  Empty proposals: „Noch keine Vorschläge" / „Schlage deinen ersten Beitrag für eine Gruppe vor, in der du Mitglied bist."
  Empty queue: „Keine offenen Vorschläge" / „Für diese Gruppe wurden noch keine Vorschläge eingereicht."
  90-Tage-Hinweis: „Reagiert kein Gruppen-Leader binnen 90 Tagen, kannst du den Vorschlag selbst als unverifizierten historischen Eintrag öffentlich schalten."
  Ablehngrund: „Ablehngrund:" / Placeholder: „Ablehngrund (optional)"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Typen, API-Calls und CSS-Fixes</name>
  <files>frontend/src/types/contributions.ts, frontend/src/lib/api.ts, frontend/src/styles/ui.module.css</files>
  <read_first>
    - frontend/src/types/contributions.ts (bestehende MeAnimeContribution-Interface, Zeilen 50–80)
    - frontend/src/lib/api.ts (Zeilen 6760–6987: bestehende Contributions-Calls, authorizedFetch-Pattern)
    - frontend/src/styles/ui.module.css (Suche nach .fieldLabel, .sectionTitle, .button für font-size/font-weight)
  </read_first>
  <action>
    1. frontend/src/types/contributions.ts erweitern:
       - MeAnimeContribution um folgende optionale Felder ergänzen: review_note?: string | null; can_self_publish?: boolean
       - Neue Interface ProposalFormData hinzufügen: { fansub_group_id: number; anime_id: number; fansub_group_member_id: number; role_codes: string[]; note?: string | null; started_year?: number | null; ended_year?: number | null }
       - Neue Interface GroupProposalRow hinzufügen: { id: number; fansub_group_member_id: number; member_display_name: string; anime_id: number; anime_title: string; role_codes: string[]; note: string | null; created_at: string }
       - Neue Interface GroupProposalsResponse hinzufügen: { data: GroupProposalRow[] }

    2. frontend/src/lib/api.ts am Ende des Contributions-Abschnitts (nach rejectAnimeContribution) ergänzen:
       - createContributionProposal(body: ProposalFormData): Promise<void>
         POST /api/v1/me/contribution-proposals, JSON-Body, kein Rückgabewert
       - selfPublishContribution(contributionId: number): Promise<void>
         POST /api/v1/me/anime-contributions/${contributionId}/self-publish, kein Body
       - listGroupProposals(fansubId: number): Promise<GroupProposalsResponse>
         GET /api/v1/admin/fansubs/${fansubId}/contribution-proposals
       - confirmProposal(fansubId: number, cid: number): Promise<void>
         POST /api/v1/admin/fansubs/${fansubId}/contribution-proposals/${cid}/confirm, kein Body
       - rejectProposal(fansubId: number, cid: number, note?: string): Promise<void>
         POST /api/v1/admin/fansubs/${fansubId}/contribution-proposals/${cid}/reject, Body: { review_note: note ?? null }
       Jeder Call: authorizedFetch + parseApiErrorPayload + throw new ApiError bei !response.ok (exakt wie confirmAnimeContribution-Muster).

    3. frontend/src/styles/ui.module.css — genau drei Änderungen:
       - .fieldLabel: font-size 13px → 14px (eliminiert 5. Font-Größe per UI-SPEC)
       - .sectionTitle (zweite Deklaration ≈ Zeile 664): font-size 1.08rem → 1rem
       - .button: font-weight 600 → 700

    Alle String-Werte in api.ts und types/contributions.ts sind Code-Identifier (kein Umlaut-Constraint für Variablennamen).
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit läuft ohne Fehler (oder nur pre-existierende Fehler ohne Änderung durch diese Task)
    - grep "can_self_publish" frontend/src/types/contributions.ts ergibt ≥ 1 Treffer
    - grep "review_note" frontend/src/types/contributions.ts ergibt ≥ 1 Treffer
    - grep "createContributionProposal" frontend/src/lib/api.ts ergibt ≥ 1 Treffer (export function)
    - grep "listGroupProposals" frontend/src/lib/api.ts ergibt ≥ 1 Treffer
    - grep "selfPublishContribution" frontend/src/lib/api.ts ergibt ≥ 1 Treffer
    - grep "font-size: 14px" frontend/src/styles/ui.module.css | grep fieldLabel ergibt Treffer
    - grep "font-weight: 700" frontend/src/styles/ui.module.css | grep ".button " ergibt Treffer
    - grep "1.08rem" frontend/src/styles/ui.module.css ergibt 0 Treffer (vollständig ersetzt)
  </acceptance_criteria>
  <done>Typen erweitert, API-Calls ergänzt, CSS-Fixes angewendet; TypeScript kompiliert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ProposalForm, MyProposalsSection und ReviewQueue</name>
  <files>frontend/src/components/contributions/ProposalForm.tsx, frontend/src/components/contributions/MyProposalsSection.tsx, frontend/src/components/contributions/ReviewQueue.tsx, frontend/src/components/contributions/ContributionCard.tsx, frontend/src/app/me/contributions/page.tsx, frontend/src/app/admin/my-groups/[id]/page.tsx</files>
  <read_first>
    - frontend/src/components/contributions/MyContributionsSection.tsx (State-Management-Pattern, Error-Banner, readErrorMessage, useAuthSession-Nutzung)
    - frontend/src/components/contributions/ContributionCard.tsx (bestehende Props-Struktur, um mode="proposal" ergänzen)
    - frontend/src/app/admin/my-groups/[id]/page.tsx (421 Zeilen — vollständig lesen, Card-Pattern, groupId, useAuthSession, loadDetail-Pattern)
    - frontend/src/app/me/contributions/page.tsx (Platzhalter für "Eigene Vorschläge"-Sektion identifizieren)
    - frontend/src/components/ui/index.ts oder ui/ (welche UI-Komponenten exportiert werden)
  </read_first>
  <behavior>
    Frontend-Tests (ProposalForm.test.tsx, ReviewQueue.test.tsx falls Zeit erlaubt):
    - ProposalForm: Submit mit role_codes=[] → Fehlermeldung "Bitte wähle mindestens eine Rolle aus." erscheint
    - ProposalForm: 90-Tage-Hinweis-Text sichtbar ohne Interaktion
    - ReviewQueue: Render mit leerer Liste → EmptyState "Keine offenen Vorschläge"
    - ReviewQueue: Render mit Proposal → Bestätigen/Ablehnen-Button sichtbar
  </behavior>
  <action>
    Neue Datei ProposalForm.tsx (frontend/src/components/contributions/):
    - 'use client'; Modal-Wrapper (role="dialog", aria-modal="true", aria-labelledby="proposal-form-title")
    - Props: { onSuccess: () => void; onClose: () => void; ownGroups: { id: number; group_name: string; fansub_group_member_id: number }[] }
    - State: selectedGroupMemberId, selectedAnimeId, selectedRoleCodes (string[]), note, startedYear, endedYear, isSubmitting, error
    - Felder von oben nach unten:
      1. Gruppe-Select (Placeholder "Gruppe auswählen", Pflicht)
      2. Anime-Suche/Select (Placeholder "Anime suchen oder auswählen", Pflicht) — einfaches Select mit Anime-ID-Eingabe für V1 reicht (keine Live-Suche nötig, da Datenbankgröße überschaubar)
      3. Rollen-Multi-Select (Checkbox-Chips aus role_definitions, label_de als Label, min. 1 Pflicht)
         Bei Submit mit 0 Rollen: inline Fehler unter den Chips "Bitte wähle mindestens eine Rolle aus." (D-04)
         role="group" aria-label="Rollen auswählen"
      4. Notiz-Textarea (3 Zeilen, Placeholder "Kurze Erläuterung (z. B. »war 2005 Co-Editor«)", optional aber visuell prominent)
      5. Von Jahr / Bis Jahr — zwei Number-Inputs nebeneinander (optional)
      6. Hinweis-Banner (statisch, kein role="alert"): „Reagiert kein Gruppen-Leader binnen 90 Tagen, kannst du den Vorschlag selbst als unverifizierten historischen Eintrag öffentlich schalten." (D-13)
    - Footer: links „Abbrechen"-Ghost-Button (onClick=onClose), rechts „Beitrag einreichen"-Primary-Button
    - Duplikat-Fehler (API-Status 409): inline ErrorState über dem Formular: „Für diese Kombination aus Gruppe, Anime und deiner Identität existiert bereits ein Beitrag."
    - Allgemeiner Fehler: „Der Vorschlag konnte nicht eingereicht werden. Bitte versuche es erneut."
    - role_definitions aus /api/v1/... laden (oder als Prop übergeben) — Implementierungs-Entscheidung: als Prop aus MyProposalsSection übergeben, damit ProposalForm testbar bleibt
    - Dateigrößen-Limit: < 450 Zeilen

    Neue Datei MyProposalsSection.tsx (frontend/src/components/contributions/):
    - 'use client'; Props: { memberId?: number } (oder keine Props — lädt eigene Daten via /me/anime-contributions)
    - State: proposals (MeAnimeContribution[]), isLoading, error, showForm, selfPublishConfirming (number | null)
    - Daten-Laden: GET /api/v1/me/anime-contributions (bestehender Endpunkt), gefiltert auf status in ['proposed','confirmed','disputed']
    - Header: SectionHeader title="Eigene Vorschläge" + count + Button "+ Beitrag vorschlagen" (.buttonPrimary, onClick → showForm=true)
    - Bei showForm=true: ProposalForm als Modal öffnen
    - Drei Sub-Sektionen mit h2 und count (collapsed wenn leer):
      „In Prüfung (N)": proposals mit status='proposed', kein can_self_publish oder noch nicht abgelaufen
      „Bestätigt (N)": status='confirmed'
      „Abgelehnt (N)": status='disputed', zeigt review_note wenn vorhanden als „Ablehngrund: {text}"
    - 90-Tage-Selbstschaltung: Auf proposed-Karten mit can_self_publish===true:
      Button „Historisch öffentlich schalten" (.buttonSecondary, onClick → setSelfPublishConfirming(c.id))
      Inline-Bestätigung wenn selfPublishConfirming===c.id:
        Text „Dieser Eintrag wird als unverifizierten historischen Beitrag öffentlich sichtbar."
        Button „Jetzt öffentlich schalten" (.buttonSecondary, onClick → handleSelfPublish(c.id))
        Cancel-Link „Abbrechen" (onClick → setSelfPublishConfirming(null))
    - handleSelfPublish: selfPublishContribution(id) aufrufen; bei Erfolg Proposal aus Liste entfernen (optimistisch); bei Fehler: inline Error
    - Status-Badge-Mapping (Badge variant): proposed→"warning", confirmed→"success", disputed→"danger"
    - Dateigrößen-Limit: < 450 Zeilen

    Neue Datei ReviewQueue.tsx (frontend/src/components/contributions/):
    - 'use client'; Props: { fansubId: number }
    - State: proposals (GroupProposalRow[]), isLoading, error, rejectingId (number | null), rejectNote (''), actionError (string | null)
    - Daten-Laden: listGroupProposals(fansubId) auf Mount + useCallback+useEffect (analog my-groups/[id]/page.tsx)
    - SectionHeader title=`Offene Vorschläge (${proposals.length})`
    - Leere Liste: EmptyState title="Keine offenen Vorschläge" description="Für diese Gruppe wurden noch keine Vorschläge eingereicht."
    - Jeder Vorschlag als Card (variant="cardCompact" oder compact):
      - Row 1: member_display_name + Badge variant="warning" „In Prüfung" (aria-label="Status: In Prüfung")
      - Row 2: anime_title + Rollen-Chips (Badge variant="info" je role_code)
      - Row 3: note (grau, kursiv, max 3 Zeilen text-overflow) — nur wenn vorhanden
      - Row 4: „Eingereicht am {created_at formatted}" (--text-faint, 12px)
      - Footer: „Vorschlag bestätigen" (.buttonSuccess, aria-label=`Vorschlag von ${row.member_display_name} bestätigen`) + „Vorschlag ablehnen" (.buttonDanger, aria-label=`Vorschlag von ${row.member_display_name} ablehnen`)
    - Ablehnen-Expansion: bei rejectingId===row.id: Textarea (Placeholder "Ablehngrund (optional)") + „Ablehnung bestätigen"-Button (.buttonDanger) + Cancel-Link
    - handleConfirm: confirmProposal(fansubId, id); Erfolg → optimistisch aus Liste entfernen + CSS-Ausblenden (opacity 0, 200ms)
    - handleReject: rejectProposal(fansubId, id, note || undefined); Erfolg → optimistisch entfernen
    - Fehler auf einer Karte: inline unter der Karte; globale Queue bleibt benutzbar
    - Dateigrößen-Limit: < 450 Zeilen

    ContributionCard.tsx erweitern (mode="proposal"):
    - Neuen optionalen Prop mode?: 'default' | 'proposal' ergänzen
    - Bei mode="proposal": review_note (aus MeAnimeContribution) als „Ablehngrund: {text}" anzeigen wenn status='disputed' und review_note vorhanden
    - Bestehende Funktionalität nicht verändern (mode=undefined → Standardverhalten)

    me/contributions/page.tsx:
    - Platzhalter für "Eigene Vorschläge"-Sektion suchen und durch <MyProposalsSection /> ersetzen
    - Import ergänzen

    admin/my-groups/[id]/page.tsx (421 Zeilen — VORSICHT: 450-Zeilen-Limit):
    - <ReviewQueue fansubId={groupId} /> als neue Card-Section unterhalb der bestehenden "Historische Credits"-Card einfügen
    - Import ergänzen
    - Die Datei darf NICHT über 450 Zeilen wachsen — ReviewQueue kapselt eigenen State komplett (keine neuen State-Variablen in page.tsx nötig)

    Alle Deutsch-Strings mit korrekten Umlauten (ä, ö, ü, ß). Keine ASCII-Ersetzungen (D-19).
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - npm run build läuft ohne Fehler
    - grep "MyProposalsSection" frontend/src/app/me/contributions/page.tsx ergibt ≥ 1 Treffer
    - grep "ReviewQueue" frontend/src/app/admin/my-groups/[id]/page.tsx ergibt ≥ 1 Treffer
    - wc -l frontend/src/app/admin/my-groups/[id]/page.tsx < 450
    - grep "Beitrag vorschlagen" frontend/src/components/contributions/MyProposalsSection.tsx ergibt Treffer (kein ASCII-ae)
    - grep "Reagiert kein Gruppen-Leader" frontend/src/components/contributions/ProposalForm.tsx ergibt Treffer (D-13-Hinweis vorhanden)
    - grep "can_self_publish" frontend/src/components/contributions/MyProposalsSection.tsx ergibt ≥ 1 Treffer
    - grep "review_note" frontend/src/components/contributions/MyProposalsSection.tsx ergibt ≥ 1 Treffer (Ablehngrund-Anzeige)
    - grep "CanForFansubGroup\|Vorschlag bestätigen" frontend/src/components/contributions/ReviewQueue.tsx ergibt ≥ 1 Treffer
    - wc -l frontend/src/components/contributions/ProposalForm.tsx < 450
    - wc -l frontend/src/components/contributions/MyProposalsSection.tsx < 450
    - wc -l frontend/src/components/contributions/ReviewQueue.tsx < 450
  </acceptance_criteria>
  <done>npm run build grün; alle Komponenten erstellt und eingebunden; Umlaut-Regel eingehalten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Member-Browser → ProposalForm-Eingabe | Untrusted Freitext (note, review_note) — React escaped by default |
| Member-Browser → SelfPublish-Button | Frontend-Gate ist nicht vertrauenswürdig — Backend re-validiert (Plan 65-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-04-01 | Tampering | XSS über note / review_note in JSX | accept | React escaped Textnodes per Default; kein dangerouslySetInnerHTML für Freitext in ProposalForm, MyProposalsSection, ReviewQueue |
| T-65-04-02 | Tampering | SelfPublish-Button erscheint durch Frontend-Manipulation früher | accept | Frontend-Gate ist nur UX (can_self_publish als berechnetes Feld). Serverseitige 90-Tage-Validierung in Plan 65-02 verhindert vorzeitige Schaltung |
| T-65-04-03 | Information Disclosure | review_note (Ablehngrund) für nicht berechtigte Nutzer sichtbar | accept | review_note wird nur auf eigenen disputed-Contributions angezeigt (über /me/anime-contributions — Member sieht nur eigene); kein öffentlicher Endpunkt |
| T-65-04-04 | Elevation of Privilege | ReviewQueue ruft Leader-Endpunkte ohne Auth | mitigate | authorizedFetch in allen API-Calls sendet Access-Token; Backend prüft CanForFansubGroup; 401/403 werden als ApiError geworfen und in ErrorState angezeigt |
| T-65-04-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen npm-Pakete — Slopcheck nicht zutreffend |
</threat_model>

<verification>
- npm run build grün (Next.js-Kompilierung ohne Fehler)
- wc -l admin/my-groups/[id]/page.tsx < 450
- alle drei neuen Komponenten-Dateien < 450 Zeilen
- grep "Bitte wähle mindestens eine Rolle aus" ProposalForm.tsx → Treffer (D-04 Fehlermeldung)
- grep "90 Tage\|90-Tage\|90 tage" ProposalForm.tsx → Treffer (D-13-Hinweis)
- Manuelle Browser-Verifikation (Checkpoint):
  - me/contributions: "Eigene Vorschläge"-Sektion mit Button sichtbar
  - Vorschlag-Modal öffnet sich, 90-Tage-Hinweis sichtbar
  - admin/my-groups/[id]: Review-Queue-Sektion sichtbar
</verification>

<success_criteria>
- npm run build und TypeScript-Check grün
- ProposalForm: Gruppe, Anime, Rollen (min. 1), Notiz, Jahre, 90-Tage-Hinweis, Duplikat-Fehler (409)
- MyProposalsSection: Status-Gruppierung In Prüfung/Bestätigt/Abgelehnt; SelfPublish-Trigger bei can_self_publish=true
- ReviewQueue: Karten mit Confirm/Reject; Ablehnen-Expansion inline; optimistisches Entfernen
- ContributionCard: mode="proposal" zeigt review_note bei disputed
- me/contributions-Platzhalter durch MyProposalsSection ersetzt
- admin/my-groups/[id] enthält ReviewQueue; Datei bleibt unter 450 Zeilen
- CSS: fieldLabel 14px, sectionTitle 1rem, button font-weight 700
- Alle user-facing Strings korrekte deutsche Umlaute (D-19)
</success_criteria>

<output>
Erstelle .planning/phases/65-member-vorschlaege-review-queue/65-04-SUMMARY.md wenn fertig.
</output>
