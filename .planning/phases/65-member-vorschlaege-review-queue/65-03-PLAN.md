---
phase: 65-member-vorschlaege-review-queue
plan: 03
type: execute
wave: 2
depends_on:
  - 65-01
files_modified:
  - backend/internal/handlers/contribution_review_handler.go
  - backend/internal/handlers/contribution_review_handler_test.go
  - backend/cmd/server/main.go
autonomous: true
requirements:
  - P65-SC2

must_haves:
  truths:
    - "GET /api/v1/admin/fansubs/:id/contribution-proposals gibt proposed-Einträge der Gruppe zurück (nur Leader/Admins)"
    - "POST /api/v1/admin/fansubs/:id/contribution-proposals/:cid/confirm setzt status=confirmed + beide Sichtbarkeitsflags + confirmed_by/at (D-10)"
    - "POST /api/v1/admin/fansubs/:id/contribution-proposals/:cid/reject setzt status=disputed + optionalen review_note (D-07, D-08); kein Hard-Delete"
    - "Nicht-Leader/Nicht-Admin → 403 (D-09, CanForFansubGroup)"
    - "D-14: confirmed_by wird bei Confirm auf die App-User-ID des Leaders/Admins gesetzt; Audit-Log-Einträge 'anime_contribution.confirmed' und 'anime_contribution.rejected' nach jeder erfolgreichen Review-Aktion"
    - "D-19: Alle deutschen Response-Strings im Review-Handler nutzen korrekte Umlaute (ä, ö, ü, ß) — keine ASCII-Ersetzungen"
  artifacts:
    - path: "backend/internal/handlers/contribution_review_handler.go"
      provides: "ContributionReviewHandler mit ListProposals, ConfirmProposal, RejectProposal"
    - path: "backend/internal/handlers/contribution_review_handler_test.go"
      provides: "Unit-Tests für P65-SC2 (Confirm, Reject, Permissions)"
  key_links:
    - from: "backend/internal/handlers/contribution_review_handler.go"
      to: "backend/internal/permissions/permissions.go"
      via: "CanForFansubGroup mit ActionFansubGroupMembersManage"
      pattern: "CanForFansubGroup.*ActionFansubGroupMembersManage"
    - from: "backend/cmd/server/main.go"
      to: "contribution_review_handler.go"
      via: "registerAdminRoutes — neue Review-Routen unter admin/fansubs/:id"
      pattern: "admin/fansubs/:id/contribution-proposals"
---

<objective>
Neuer Handler contribution_review_handler.go liefert die drei Leader/Admin-Review-Endpunkte: GET (Queue), POST confirm, POST reject. Die Autorisierung erfolgt ausschließlich über CanForFansubGroup mit ActionFansubGroupMembersManage — deckt Fansub-Lead und Plattform-Admin in einem Aufruf ab (D-09).

Purpose: Leader-getriebene Review-Queue mit vollständiger Autorisierung und Audit-Attribution für alle Review-Aktionen.
Output: Handler-Datei + Test-Datei + Routen-Registrierung in registerAdminRoutes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-member-vorschlaege-review-queue/65-CONTEXT.md
@.planning/phases/65-member-vorschlaege-review-queue/65-RESEARCH.md
@.planning/phases/65-member-vorschlaege-review-queue/65-PATTERNS.md
@.planning/phases/65-member-vorschlaege-review-queue/65-01-SUMMARY.md
</context>

<interfaces>
<!-- Schlüssel-Contracts aus dem Codebase. -->

Aus backend/internal/handlers/fansub_anime_contributions_handler.go (Autorisierungs-Pattern):
  type FansubAnimeContributionsHandler struct { contributionsRepo *repository.AnimeContributionsRepository; rolesRepo *repository.HistGroupMemberRolesRepository; permissionSvc *permissions.Service; auditLogRepo *repository.AuditLogRepository }
  func (h *FansubAnimeContributionsHandler) CreateAnimeContribution(c *gin.Context)
  -- CanForFansubGroup(ctx, actor, permissions.ActionFansubGroupMembersManage, fansubID) → result.Allowed
  -- writePermissionInternalError, writePermissionDenied, auditPermissionDenied (package-Helpers)
  -- Audit-Log-Pattern: auditLogRepo.Write(AuditLogEntry{...})
  -- ErrConflict → 409; ErrNotFound → 404

Aus backend/internal/permissions/permissions.go:
  ActionFansubGroupMembersManage permissions.Action
  func (s *Service) CanForFansubGroup(ctx, actor, action, fansubID) (Result, error)
  -- Plattform-Admin wird kurzgeschlossen (ReasonPlatformAdmin) — kein separater Admin-Check nötig

Aus backend/internal/repository/anime_contributions_proposal_repository.go (aus Plan 65-01):
  type GroupProposalRow struct { ID, FansubGroupMemberID int64; MemberDisplayName, AnimeTitle string; AnimeID int64; RoleCodes []string; Note *string; CreatedAt time.Time }
  func (r *AnimeContributionsRepository) ListProposedByGroup(ctx, fansubGroupID int64) ([]GroupProposalRow, error)
  func (r *AnimeContributionsRepository) Confirm(ctx, contributionID, actorAppUserID int64) error
  func (r *AnimeContributionsRepository) Reject(ctx, contributionID, actorAppUserID int64, reviewNote *string) error
  -- ErrNotFound wenn Contribution nicht existiert oder nicht im Status 'proposed'

Aus backend/cmd/server/main.go:
  func registerAdminRoutes(v1 *gin.RouterGroup, authMiddleware gin.HandlerFunc, h adminRouteHandlers) { ... }
  -- animeContributionsHandler bereits als adminRouteHandlers.animeContributionsHandler bekannt
  -- Neue Review-Routen unter admin/fansubs/:id/... (analog /admin/fansubs/:id/anime/:animeId/contributions)

Aus backend/internal/handlers/admin_content_anime_relations_test.go:
  -- Test-Muster: gin.SetMode(gin.TestMode); Stub-Structs implementieren Interface; httptest
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ContributionReviewHandler + Test-Stubs</name>
  <files>backend/internal/handlers/contribution_review_handler.go, backend/internal/handlers/contribution_review_handler_test.go</files>
  <read_first>
    - backend/internal/handlers/fansub_anime_contributions_handler.go (Handler-Struct, CanForFansubGroup-Pattern, Audit-Log, ErrConflict/ErrNotFound-Mapping, Zeilen 1–424)
    - backend/internal/repository/anime_contributions_proposal_repository.go (GroupProposalRow, ListProposedByGroup, Confirm, Reject aus Plan 65-01)
    - backend/internal/handlers/admin_content_anime_relations_test.go (Test-Muster)
    - backend/internal/permissions/permissions.go (ActionFansubGroupMembersManage, Result-Typ)
  </read_first>
  <behavior>
    Tests (contribution_review_handler_test.go) müssen ROT sein bevor der Handler geschrieben wird:
    - TestListProposals_RequiresLeaderOrAdmin: nicht-Leader → CanForFansubGroup gibt Allowed=false → 403
    - TestListProposals_Success: Leader → 200 + JSON-Array mit proposed-Einträgen
    - TestConfirmProposal_RequiresLeaderOrAdmin: nicht-Leader → 403
    - TestConfirmProposal_Success: Leader → 200; Confirm-Stub aufgerufen mit fansubID und contributionID; EventType "anime_contribution.confirmed" im Audit
    - TestConfirmProposal_NotFound: Confirm-Stub gibt ErrNotFound → 404
    - TestRejectProposal_Success: Leader + optionaler Ablehngrund → 200; review_note korrekt weitergegeben
    - TestRejectProposal_WithoutNote: Leader + leerer Ablehngrund → 200; review_note=nil
    - TestRejectProposal_NotFound: Reject-Stub gibt ErrNotFound → 404
  </behavior>
  <action>
    Neue Datei contribution_review_handler.go erstellen.

    Handler-Struct ContributionReviewHandler mit:
    - reviewRepo ReviewRepository (Interface für Stub-Tests)
    - permissionSvc *permissions.Service
    - auditLogRepo *repository.AuditLogRepository

    Interface-Definition direkt im Handler-File:
      type ReviewRepository interface {
        ListProposedByGroup(ctx context.Context, fansubGroupID int64) ([]repository.GroupProposalRow, error)
        Confirm(ctx context.Context, contributionID, actorAppUserID int64) error
        Reject(ctx context.Context, contributionID, actorAppUserID int64, reviewNote *string) error
      }

    func NewContributionReviewHandler(reviewRepo ReviewRepository, permissionSvc *permissions.Service, auditLogRepo *repository.AuditLogRepository) *ContributionReviewHandler

    ListProposals (GET /api/v1/admin/fansubs/:id/contribution-proposals):
    1. Actor aus Middleware-Identity holen (analog fansub_anime_contributions_handler.go)
    2. fansubID aus :id-Param parsen → 400 bei ungültig
    3. permissionSvc.CanForFansubGroup(ctx, actor, ActionFansubGroupMembersManage, fansubID) → 403 wenn nicht erlaubt (mit auditPermissionDenied)
    4. reviewRepo.ListProposedByGroup(ctx, fansubID) → ErrNotFound → 404
    5. 200 + JSON: { "data": [...GroupProposalRow] }

    ConfirmProposal (POST /api/v1/admin/fansubs/:id/contribution-proposals/:cid/confirm):
    1. Actor aus Identity
    2. fansubID + contributionID aus Params → 400 bei ungültig
    3. CanForFansubGroup → 403 wenn nicht erlaubt
    4. reviewRepo.Confirm(ctx, contributionID, actorAppUserID) → ErrNotFound → 404
    5. auditLogRepo.Write mit EventType "anime_contribution.confirmed", ScopeType group, ScopeID &fansubID, TargetType "anime_contribution", TargetID &contributionID, Outcome "allowed"
    6. 200 + { "message": "Vorschlag wurde bestätigt." }

    RejectProposal (POST /api/v1/admin/fansubs/:id/contribution-proposals/:cid/reject):
    1–3. Analog ConfirmProposal
    4. ShouldBindJSON für RejectRequest{ ReviewNote *string `json:"review_note"` } (leer = nil, kein Fehler)
    5. reviewRepo.Reject(ctx, contributionID, actorAppUserID, req.ReviewNote) → ErrNotFound → 404
    6. auditLogRepo.Write mit EventType "anime_contribution.rejected", Payload map[string]any{"has_note": req.ReviewNote != nil}
    7. 200 + { "message": "Vorschlag wurde abgelehnt." }

    Alle deutschen Strings mit korrekten Umlauten (D-19).

    Test-Datei contribution_review_handler_test.go:
    - reviewRepoStub und permissionSvcStub als inline Stub-Structs
    - permissionSvcStub.Allowed bool-Flag für einfaches Umschalten
    - Alle 8 Test-Fälle aus den Behavior-Punkten

    Dateigrößen-Check: beide Dateien unter 450 Zeilen.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go test ./internal/handlers/ -run "TestListProposals|TestConfirmProposal|TestRejectProposal" -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - go test ./internal/handlers/ -run "TestListProposals|TestConfirmProposal|TestRejectProposal" → alle 8 Tests grün
    - Handler-Datei kompiliert ohne Fehler
    - CanForFansubGroup wird auf ALLEN drei Endpunkten aufgerufen (grep "CanForFansubGroup" contribution_review_handler.go ergibt ≥ 3 Treffer)
    - auditLogRepo.Write wird nach Confirm und Reject aufgerufen (grep "auditLogRepo.Write" ergibt ≥ 2 Treffer)
    - wc -l contribution_review_handler.go < 450
    - wc -l contribution_review_handler_test.go < 450
    - Keine ASCII-Umlaut-Ersetzungen (ae/oe/ue) in deutschen Strings
  </acceptance_criteria>
  <done>Handler und Tests grün; ListProposals, ConfirmProposal, RejectProposal mit Autorisierung und Audit.</done>
</task>

<task type="auto">
  <name>Task 2: registerAdminRoutes — Review-Routen eintragen</name>
  <files>backend/cmd/server/main.go</files>
  <read_first>
    - backend/cmd/server/main.go (Zeilen 357–386, bestehende adminRouteHandlers-Struct und registerAdminRoutes-Aufruf)
    - backend/internal/handlers/contribution_review_handler.go (NewContributionReviewHandler-Signatur aus Task 1)
  </read_first>
  <action>
    Zwei Änderungen in backend/cmd/server/main.go:

    1. ContributionReviewHandler instanziieren: nach der Zeile mit animeContributionsHandler (≈ Zeile 358):
       reviewHandler := handlers.NewContributionReviewHandler(animeContributionsRepo, permissionSvc, auditLogRepo)

    2. Routen in registerAdminRoutes eintragen (innerhalb der bestehenden Admin-Routen-Gruppe, analog bestehende fansub/:id-Routen):
       v1.GET("/admin/fansubs/:id/contribution-proposals", authMiddleware, reviewHandler.ListProposals)
       v1.POST("/admin/fansubs/:id/contribution-proposals/:cid/confirm", authMiddleware, reviewHandler.ConfirmProposal)
       v1.POST("/admin/fansubs/:id/contribution-proposals/:cid/reject", authMiddleware, reviewHandler.RejectProposal)

    Reihenfolge-Hinweis: /confirm und /reject müssen als Literal-Routen VOR einer möglichen /:cid-Catch-Route registriert werden (Gin-Regel). Da /confirm und /reject unterschiedliche Pfade sind, ist die Reihenfolge unkritisch — trotzdem in der richtigen Gruppe.

    KEINE anderen Zeilen in main.go ändern.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go build ./cmd/server/ 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - go build ./cmd/server/ erfolgreich
    - grep "contribution-proposals" backend/cmd/server/main.go ergibt mindestens 3 Treffer (GET + 2 POST)
    - Bestehende Routen-Registrierungen bleiben unverändert
    - adminRouteHandlers-Struct muss NICHT erweitert werden (reviewHandler kann direkt verdrahtet werden)
  </acceptance_criteria>
  <done>Backend kompiliert; alle drei Review-Routen registriert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Leader-Browser → POST /admin/fansubs/:id/contribution-proposals/:cid/confirm | Autorisierter Actor, aber fansubID und contributionID kommen aus URL — IDOR-Risiko |
| Leader-Browser → POST .../reject | Gleiches IDOR-Risiko + free-text review_note |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-03-01 | Elevation of Privilege | ListProposals / Confirm / Reject — kein Leader | mitigate | CanForFansubGroup auf ALLEN drei Endpunkten; Plattform-Admin ist Kurzschluss; auditPermissionDenied bei Ablehnung (D-09) |
| T-65-03-02 | Elevation of Privilege | IDOR — Leader einer anderen Gruppe bestätigt Vorschlag in fremder Gruppe | mitigate | CanForFansubGroup prüft Berechtigung FÜR die übergebene fansubID; nur berechtigte Leader sehen/bearbeiten Einträge ihrer Gruppe |
| T-65-03-03 | Tampering | review_note enthält XSS-Payload | accept | review_note ist reiner Freitext (TEXT NULL in DB); Frontend rendert ihn als React-Textknoten — kein dangerouslySetInnerHTML; XSS nicht möglich |
| T-65-03-04 | Repudiation | Confirm/Reject ohne Audit-Spur | mitigate | auditLogRepo.Write nach jeder erfolgreichen Confirm/Reject-Aktion mit ActorAppUserID, TargetID und Outcome |
| T-65-03-05 | Repudiation | Hard-Delete bei Ablehnen | mitigate | Reject setzt status='disputed' — kein DELETE; Observability-Constraint (D-07) |
| T-65-03-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Pakete — Slopcheck nicht zutreffend |
</threat_model>

<verification>
- go test ./internal/handlers/ -run "TestListProposals|TestConfirmProposal|TestRejectProposal" → alle grün
- go build ./cmd/server/ → erfolgreich
- grep "CanForFansubGroup" contribution_review_handler.go ≥ 3 Treffer
- grep "auditLogRepo.Write" contribution_review_handler.go ≥ 2 Treffer
- wc -l contribution_review_handler.go < 450
</verification>

<success_criteria>
- Alle 8 Review-Handler-Tests grün
- go build ./cmd/server/ grün
- ListProposals, ConfirmProposal, RejectProposal: Autorisierung via CanForFansubGroup auf allen drei
- Confirm: status=confirmed, beide Flags true, confirmed_by/at gesetzt; Audit-Log-Eintrag
- Reject: status=disputed, review_note optional (nil wenn leer), kein Delete; Audit-Log-Eintrag
- Deutsche Fehlermeldungen mit korrekten Umlauten
</success_criteria>

<output>
Erstelle .planning/phases/65-member-vorschlaege-review-queue/65-03-SUMMARY.md wenn fertig.
</output>
