---
phase: 134-fixture-backed-verification-rollout
plan: 04
type: execute
wave: 3
depends_on: ["134-02", "134-03"]
files_modified:
  - scripts/phase134-green-gate.sh
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [PMQA-07]

must_haves:
  truths:
    - "A single script run proves typecheck, lint, focused backend + frontend tests, build, and git diff --check are all green for the member-profile surface, per the user-confirmed scope decision (not the full unscoped suite)."
    - "The gate's output explicitly names the 9 known pre-existing, out-of-scope stale-assertion failures documented in 133-.../deferred-items.md as a flagged, non-blocking note -- never silently fixed, never silently ignored."
    - "ROADMAP.md's Progress table and STATE.md's completed_phases counter accurately reflect that Phases 129/130/131 are executed and gate-green, per RESEARCH.md's Ground Truth findings -- without rewriting STATE.md's Accumulated Context wholesale."
  artifacts:
    - path: "scripts/phase134-green-gate.sh"
      provides: "One CI-style orchestration script: backend build/vet/scoped-test, frontend typecheck/lint/scoped-test/build, git diff --check, with a KNOWN_DEFERRED allow-list that separates genuinely-new failures from the 9 already-documented pre-existing ones."
  key_links:
    - from: "scripts/phase134-green-gate.sh"
      to: ".planning/phases/133-responsive-accessible-efficient-visual-delivery/deferred-items.md"
      via: "hardcoded KNOWN_DEFERRED list of file+test-name pairs, cross-checked against actual go test / vitest output"
      pattern: "KNOWN_DEFERRED"
---

<objective>
Implement PMQA-07's automated closing gate (CONTEXT.md D-08): typecheck, lint, focused backend +
frontend tests, build, and `git diff --check`, scoped to the member-profile surface per the
user-confirmed scope decision recorded in this phase's planning context (not the full unscoped
suite — matching every prior Phase 128-133 plan's precedent). The gate must not silently swallow the
9 pre-existing, out-of-scope stale-assertion failures already fully triaged in
`.planning/phases/133-.../deferred-items.md`; it must flag them by name in its summary output
without treating them as blocking.

Bundled into this same plan (per the user-confirmed scope decision #2 recorded in
134-VALIDATION.md): a small, tightly-scoped documentation fix correcting `ROADMAP.md`'s stale
"Progress" table rows for Phases 129/130/131 and reconciling `STATE.md`'s `completed_phases`
counter, per RESEARCH.md's "Ground Truth: Phase 129/130/131 Execution Status" findings. This is
grouped with the gate plan because both are about closing the loop on accurate, trustworthy
rollout status — it is explicitly NOT part of the PMQA-01..07 automated verification surface and
must stay small.

Purpose: give the milestone one authoritative, rerunnable command proving the member-profile surface
is quality-gated before the shared-DB reset (Plan 134-05) and live UAT (Plan 134-06) proceed.
Output: `scripts/phase134-green-gate.sh`, its first green run's output captured as evidence, and
corrected `ROADMAP.md`/`STATE.md` tracking rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-fixture-backed-verification-rollout/134-CONTEXT.md
@.planning/phases/134-fixture-backed-verification-rollout/134-RESEARCH.md
@.planning/phases/134-fixture-backed-verification-rollout/134-VALIDATION.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/deferred-items.md
@frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the scoped green-gate orchestration script with a known-deferred allow-list</name>
  <files>scripts/phase134-green-gate.sh</files>
  <read_first>.planning/phases/133-responsive-accessible-efficient-visual-delivery/deferred-items.md, frontend/package.json (scripts block), scripts/README-seed.md (for the repo's existing bash-script conventions)</read_first>
  <action>
Write `scripts/phase134-green-gate.sh` (bash, `set -uo pipefail` — NOT `-e`, because the script must
continue past a failing sub-command to collect ALL results before reporting, not abort on the first
red step). Structure:

1. Backend section (run inside `team4sv30-backend` via `docker compose exec -T`): `go build ./...`,
   `go vet ./...`, then `go test ./internal/repository/... ./internal/handlers/... ./internal/migrations/... ./internal/testsupport/... -run 'Phase12|Phase13' -count=1 -timeout=300s` (this regex
   covers every `TestPhase128.../TestPhase129.../TestPhase131.../TestPhase132.../TestPhase134...`
   already confirmed present via `grep -rln "func TestPhase13"`/`"func TestPhase128"` across those
   four packages). Requires `TEAM4S_PHASE128_TEST_DSN`, `TEAM4S_PHASE129_TEST_DSN`,
   `TEAM4S_PHASE134_MIGRATION_DSN`, `TEAM4S_PHASE134_TEST_DSN` to be exported in the calling shell
   before this script runs (document this as a precondition comment at the top of the file — do not
   hardcode credentials into the script; read them from the environment and `docker compose exec -T
   -e VAR=...` pass-through per var).

2. Frontend section (run inside `team4sv30-frontend` via `docker exec`): `npm run typecheck`, `npm
   run lint`, then a SCOPED vitest run: `npx vitest run src/components/profile/ src/app/members/
   src/types/__tests__/v12-projection-contract.test.ts` (the member-profile-relevant surface per the
   user-confirmed scope decision), then `npm run build`.

3. Repo-wide: `git diff --check` (whitespace-error check, cheap, always full-repo scope — this is
   not a test-suite scope decision, it is a formatting gate).

4. Define a `KNOWN_DEFERRED` array (as a bash array of `"file::test-name-substring"` pairs) with
   exactly these 9 entries transcribed from `.planning/phases/133-.../deferred-items.md`'s "Still
   deferred" sections: (1)
   `MemberBadgeChain.test.tsx::renders the generated contribution artwork without a fallback icon`,
   (2) `MemberBadgeChain.test.tsx::renders independent family cards with authoritative progressbar values`,
   (3) `MemberBadgeChain.test.tsx::keeps category order, a non-founder founding stage locked`,
   (4) `MemberBadgeChain.test.tsx::Phase 127 RED chain suppresses legacy Special`,
   (5) `MembershipsSection.test.tsx::keeps membership cards bounded in a responsive overflow-safe grid`,
   (6) `ResponsiveImage.config.test.ts::allows public release-version contribution media without opening all media paths`,
   (7) `v12-projection-contract.test.ts::keeps PublicMemberBadge role-progress metadata aligned`,
   (8) `no-token-boundary.test.ts::keeps docs and tests out of production boundary scans`,
   (9) `ReleaseVersionNotesTab.test.tsx::öffnet gespeicherte eigene Notizen`. (Note: entries 8-9 fall
   outside this gate's SCOPED test invocation above and should never actually appear in its output —
   list them anyway so the allow-list is complete and self-documenting if the scope is ever widened.)

5. Parse the vitest output for FAIL lines; for each, check whether it matches a `KNOWN_DEFERRED`
   entry (substring match on file + test name). Partition into `NEW_FAILURES` (blocking) vs
   `DEFERRED_FAILURES` (non-blocking, printed but not gate-failing).

6. Final summary block: print pass/fail per section, print `DEFERRED_FAILURES` under a clearly
   labeled "KNOWN DEFERRED (not blocking, see 133-.../deferred-items.md)" heading, and exit 1 if
   ANY backend step failed, OR `git diff --check` failed, OR `NEW_FAILURES` is non-empty; exit 0
   otherwise (deferred-only frontend failures do not fail the gate).
  </action>
  <acceptance_criteria>
    - `scripts/phase134-green-gate.sh` is executable and contains all 9 `KNOWN_DEFERRED` entries verbatim (file + distinctive test-name substring) matching `.planning/phases/133-.../deferred-items.md`.
    - The script exits non-zero if `go build`, `go vet`, the scoped `go test`, `npm run typecheck`, `npm run lint`, `npm run build`, or `git diff --check` fails.
    - The script exits non-zero if any vitest failure is NOT found in `KNOWN_DEFERRED`.
    - The script exits 0 when every failure present is in `KNOWN_DEFERRED` and every other step passes.
  </acceptance_criteria>
  <verify>
    <automated>test -x scripts/phase134-green-gate.sh && bash -n scripts/phase134-green-gate.sh</automated>
  </verify>
  <done>Script exists, is syntactically valid bash, and implements the allow-list partitioning logic described above.</done>
</task>

<task type="auto">
  <name>Task 2: Run the gate, resolve any genuinely new regression, capture evidence</name>
  <files>scripts/phase134-green-gate.sh, .planning/phases/134-fixture-backed-verification-rollout/evidence/green-gate-output.txt</files>
  <read_first>scripts/phase134-green-gate.sh (as written in Task 1)</read_first>
  <action>
Run `scripts/phase134-green-gate.sh` with all four required DSN env vars exported
(`TEAM4S_PHASE128_TEST_DSN`, `TEAM4S_PHASE129_TEST_DSN`, `TEAM4S_PHASE134_MIGRATION_DSN`,
`TEAM4S_PHASE134_TEST_DSN` — reuse the exact DSN values already established in Plans 134-02/134-03's
verify commands). If it exits non-zero due to a genuinely NEW failure (not in `KNOWN_DEFERRED`),
fix it: if the failure is inside a file this phase's own plans (134-01/02/03) touched, fix it
directly; if it is a pre-existing failure this phase's plans did NOT touch and did not cause, do
NOT silently patch it — instead add it to `KNOWN_DEFERRED` in Task 1's script with a one-line
comment citing the evidence that it predates Phase 134 (mirror the exact `git log -1 -- <file>`
evidence style already used throughout `deferred-items.md`), and re-run.

Once the gate exits 0, save its full stdout/stderr to
`.planning/phases/134-fixture-backed-verification-rollout/evidence/green-gate-output.txt` (create
the `evidence/` directory if absent).
  </action>
  <acceptance_criteria>
    - `scripts/phase134-green-gate.sh` exits 0 on the final run.
    - `.planning/phases/134-fixture-backed-verification-rollout/evidence/green-gate-output.txt` exists and contains the "KNOWN DEFERRED" summary section.
    - No file outside this phase's own `files_modified` lists (across all six Phase 134 plans) was modified to "fix" a pre-existing failure — any newly-discovered pre-existing failure was added to `KNOWN_DEFERRED`, not silently patched.
  </acceptance_criteria>
  <verify>
    <automated>TEAM4S_PHASE128_TEST_DSN='postgres://team4s:team4s_dev_password@team4sv30-db:5432/team4s_phase128_test?sslmode=disable' TEAM4S_PHASE129_TEST_DSN='postgres://team4s:team4s_dev_password@team4sv30-db:5432/team4s_phase129_test?sslmode=disable' TEAM4S_PHASE134_MIGRATION_DSN='postgres://team4s:team4s_dev_password@team4sv30-db:5432/postgres?sslmode=disable' TEAM4S_PHASE134_TEST_DSN='postgres://team4s:team4s_dev_password@team4sv30-db:5432/team4s_phase134_test?sslmode=disable' bash scripts/phase134-green-gate.sh | tee .planning/phases/134-fixture-backed-verification-rollout/evidence/green-gate-output.txt</automated>
  </verify>
  <done>Gate is green end-to-end; evidence file captured; every known-failure is explicitly named and traceable, never silently handled either way.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile ROADMAP.md's Progress table and STATE.md's completed_phases counter</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/134-fixture-backed-verification-rollout/134-RESEARCH.md (the "Ground Truth: Phase 129/130/131 Execution Status" section)</read_first>
  <action>
In `.planning/ROADMAP.md`'s `## Progress` table, change the three stale rows:
`129. Canonical Public Projections & Data Correctness` from `0/TBD Not started` to `11/11 Complete`
(per that phase's own "**Plans**: 11 plans across 5 waves" line already in the same file's Phase
Details section); `130. Public DTO & Cross-Layer Contract Alignment` from `0/TBD Not started` to
`7/7 Complete` (per "**Plans**: 7 plans across 4 waves"); `131. Set-Based Delivery, Pagination &
Performance Budgets` from `0/TBD Not started` to `8/8 Complete` (per "**Plans**: 8 plans across 4
waves"). In the same file's `## Phases` checklist near the top, change the Phase 131 checkbox from
`- [ ]` to `- [x]` (128/129/130/132 are already correctly `[x]`; 131 was the one remaining
inconsistency; leave 133 as `- [ ]` since its Plan 12 manual UAT is genuinely, deliberately
deferred). Add one `Completed` date value to the Progress table's Phase 129/130/131 rows if a
same-format date is derivable from `git log -1 --format=%ad --date=short -- .planning/phases/129*
.planning/phases/130* .planning/phases/131*` — otherwise leave that column as `-` unchanged rather
than guessing.

In `.planning/STATE.md`'s frontmatter, change `completed_phases: 2` to `completed_phases: 5` (128,
129, 130, 131, 132 are all fully executed and gate-green per RESEARCH.md's Ground Truth findings;
133 stays excluded from this count since its Plan 12 is deliberately deferred, and 134 is
in-progress). Do NOT change `total_plans`, `completed_plans`, or `percent` in this task — those were
not identified as incorrect by RESEARCH.md and recomputing them accurately would require a fuller
plan-by-plan audit outside this task's small, cheap scope.

In `.planning/STATE.md`'s `## Accumulated Context` > `### Decisions` section, add exactly THREE new
bullet points (one per phase, not a full decision-by-decision reconstruction) summarizing Phases
129/130/131 at the same level of detail as this file's existing `[Phase 132]`/`[Phase 133]` bullets,
each citing its source: `[Phase 129]: <one-sentence summary of the canonical public-projection
correctness work>, per .planning/phases/129-.../129-VERIFICATION.md.` and equivalent one-line
entries for 130 (contract alignment, `130-*-SUMMARY.md` files, no `130-VERIFICATION.md` per
RESEARCH.md's noted doc-completeness gap — cite that gap explicitly in the bullet) and 131
(performance budgets, `.planning/phases/131-.../evidence/BUDGETS.md`). Keep each bullet to 1-2
sentences — this is explicitly NOT a full STATE.md rewrite per the user-confirmed scope decision.
  </action>
  <acceptance_criteria>
    - `.planning/ROADMAP.md`'s Progress table rows for Phases 129, 130, 131 no longer read `0/TBD` or `Not started`.
    - `.planning/ROADMAP.md`'s Phase 131 checkbox in the `## Phases` list is `- [x]`.
    - `.planning/STATE.md`'s frontmatter `completed_phases` value is `5`.
    - `.planning/STATE.md`'s Decisions section contains exactly one new bullet each for `[Phase 129]`, `[Phase 130]`, `[Phase 131]` (in addition to whatever `[Phase 129]`-prefixed bullets, if any, already existed before this task — grep for the count before and after to confirm exactly 3 net new top-level phase-summary bullets were added, not a larger rewrite).
    - No other STATE.md sections (Pending Todos, Blockers/Concerns, Performance Metrics, Session Continuity) were modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "0/TBD\|Not started" .planning/ROADMAP.md | grep -q '^1$' && grep -n "completed_phases: 5" .planning/STATE.md</automated>
  </verify>
  <done>ROADMAP.md and STATE.md accurately reflect Phases 129/130/131 as executed, with only Phase 133's Progress row remaining "Not started"-free-but-incomplete (11/12) and Phase 134 untouched by this task's edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Green-gate script output -> milestone-close decision | This gate is the evidence basis for proceeding to the shared-DB reset (134-05) and live UAT (134-06); a false-green result here cascades into a false-confidence milestone close. |
| Planning-doc edits (ROADMAP.md/STATE.md) -> future planning agents | Downstream `/gsd:plan-phase` and `/gsd:complete-milestone` invocations read these files as ground truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-134-11 | Repudiation (false-green evidence) | scripts/phase134-green-gate.sh allow-list partitioning | mitigate | `KNOWN_DEFERRED` is a fixed, explicit, reviewable array transcribed from an already-committed triage document — the gate cannot be made to pass by silently widening this list without a corresponding, visible source-controlled diff to the script. |
| T-134-12 | Tampering (scope creep) | Task 3's doc reconciliation | mitigate | Task 3's acceptance criteria explicitly bound the edit to 3 net-new Decision bullets + 2 known fields, with an automated grep-count check preventing a larger, unreviewed STATE.md rewrite from passing verification. |
| T-134-SC | Tampering | No new package-manager installs in this plan | accept | This plan adds zero new npm/pip/cargo dependencies — pure orchestration of already-installed tools. |
</threat_model>

<verification>
`scripts/phase134-green-gate.sh` exits 0 with the four DSNs set. `grep -c "0/TBD\|Not started"
.planning/ROADMAP.md` drops to exactly 1 (Phase 134 itself, still legitimately not started at plan
time). `.planning/STATE.md`'s `completed_phases: 5`.
</verification>

<success_criteria>
- The green gate is a single, rerunnable, scoped command proving PMQA-07.
- The 9 known pre-existing failures are named, not fixed, not ignored.
- ROADMAP.md/STATE.md tracking drift for Phases 129/130/131 is corrected without a wholesale rewrite.
</success_criteria>

<output>
Create `.planning/phases/134-fixture-backed-verification-rollout/134-04-SUMMARY.md` when done
</output>
