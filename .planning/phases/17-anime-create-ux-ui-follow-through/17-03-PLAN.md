---
wave: 2
depends_on: [17-01]
files_modified:
  - frontend/src/app/admin/anime/create/CreateJellyfinCard.tsx
  - frontend/src/app/admin/anime/create/page.tsx
  - frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts
  - frontend/src/app/admin/anime/create/page.module.css
autonomous: true
---

# Plan 17-03: Jellyfin Card — Kompakte Ordnersuche + Explizites Übernehmen-Gate

## Objective

Jellyfin bekommt eine eigenständige `CreateJellyfinCard`-Komponente. Die Ergebnisse zeigen zunächst nur Ordnername, Pfad und Poster — kein Asset-Dump. Jellyfin-Assets erscheinen im geteilten Asset-Bereich erst nach explizitem `Jellyfin übernehmen`. Die eingebettete Jellyfin-Suche aus dem `titleActions`-Block in `page.tsx` wird durch `CreateJellyfinCard` ersetzt. `JellyfinDraftAssets` wird aus Section 1 herausgelöst und nach Section 2 (Plan 17-04) verschoben.

---

## Tasks

### Task 1: CreateJellyfinCard.tsx anlegen

<read_first>
- frontend/src/app/admin/anime/create/CreateJellyfinResultsPanel.tsx (vollständig)
- frontend/src/app/admin/anime/components/JellyfinIntake/JellyfinCandidateReview.tsx
- frontend/src/app/admin/anime/create/page.module.css
- frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts (Zeilen 1–100, Interface-Typen)
</read_first>

<action>
Neue Datei `frontend/src/app/admin/anime/create/CreateJellyfinCard.tsx` anlegen:

```tsx
"use client";

import createStyles from "./page.module.css";
import styles from "../../admin.module.css";
import { JellyfinCandidateReview } from "../components/JellyfinIntake/JellyfinCandidateReview";
import type { AdminJellyfinIntakeSearchItem } from "@/types/admin";

interface CreateJellyfinCardProps {
  query: string;
  candidates: AdminJellyfinIntakeSearchItem[];
  selectedCandidateID: string | undefined;
  hasActivePreview: boolean;
  hasAdoptedAssets: boolean;
  isSearching: boolean;
  isLoadingPreview: boolean;
  canSearch: boolean;
  isSubmitting: boolean;
  showResults: boolean;
  onQueryChange: (value: string) => void;
  onSearch: () => void;
  onSelectCandidate: (id: string) => void;
  onLoadCandidatePreview: (id: string) => void;
  onAdopt: () => void;
  onDiscard: () => void;
}

export function CreateJellyfinCard({
  query,
  candidates,
  selectedCandidateID,
  hasActivePreview,
  hasAdoptedAssets,
  isSearching,
  isLoadingPreview,
  canSearch,
  isSubmitting,
  showResults,
  onQueryChange,
  onSearch,
  onSelectCandidate,
  onLoadCandidatePreview,
  onAdopt,
  onDiscard,
}: CreateJellyfinCardProps) {
  return (
    <section className={createStyles.providerCard}>
      <div className={createStyles.providerCardHeader}>
        <p className={createStyles.resultsEyebrow}>Jellyfin</p>
        <h2 className={createStyles.resultsTitle}>Jellyfin</h2>
        <p className={createStyles.resultsText}>
          Dateiordner und ursprüngliche Assets
        </p>
      </div>

      <div className={styles.inputRow}>
        <label className={styles.field}>
          <span>Suche</span>
          <input
            value={query}
            placeholder="z. B. Shingeki no Kyojin"
            onChange={(e) => onQueryChange(e.target.value)}
            onKeyDown={(e) => {
              if (e.key === "Enter" && canSearch && !isSearching) onSearch();
            }}
          />
        </label>
        <button
          className={createStyles.primaryAction}
          type="button"
          disabled={!canSearch || isSearching || isSubmitting}
          onClick={onSearch}
        >
          {isSearching ? "Sucht..." : "Scannen"}
        </button>
      </div>

      {showResults && candidates.length > 0 ? (
        <div className={createStyles.jellyfinResultsWrap}>
          <p className={createStyles.resultsEyebrow}>
            Gefundene Ordner &nbsp;
            <strong>{candidates.length} Treffer</strong>
          </p>
          <JellyfinCandidateReview
            query={query.trim()}
            candidates={candidates}
            selectedCandidateID={selectedCandidateID}
            isLoadingPreview={isLoadingPreview}
            onSelectCandidate={onSelectCandidate}
            onLoadCandidatePreview={onLoadCandidatePreview}
          />

          {hasActivePreview ? (
            <div className={createStyles.jellyfinAdoptBar}>
              <p className={createStyles.jellyfinAdoptNote}>
                Der ausgewählte Jellyfin-Ordner wird gespeichert und dient als Quelle für originalen Dateipfad, Episoden-Zuordnung und Standard-Assets (Cover, Banner, Logo, Hintergründe, Videos).
              </p>
              {!hasAdoptedAssets ? (
                <button
                  className={createStyles.primaryAction}
                  type="button"
                  onClick={onAdopt}
                >
                  Jellyfin übernehmen
                </button>
              ) : (
                <span className={createStyles.statusPill}>Ausgewählt</span>
              )}
              <button
                className={`${styles.buttonSecondary} ${styles.buttonDanger}`}
                type="button"
                onClick={onDiscard}
              >
                Auswahl verwerfen
              </button>
            </div>
          ) : null}
        </div>
      ) : null}

      {showResults && candidates.length === 0 && !isSearching ? (
        <p className={createStyles.resultsText}>
          Keine Ordner gefunden. Prüfe die Schreibweise.
        </p>
      ) : null}
    </section>
  );
}
```
</action>

<acceptance_criteria>
- `frontend/src/app/admin/anime/create/CreateJellyfinCard.tsx` existiert
- Datei enthält `export function CreateJellyfinCard`
- Datei enthält `Jellyfin übernehmen`
- Datei enthält `onAdopt`
- Datei enthält `Dateiordner und ursprüngliche Assets`
- Datei enthält `Auswahl verwerfen`
- Datei enthält keine direkte Logik zum Laden von Assets — nur Callback `onAdopt`
</acceptance_criteria>

---

### Task 2: CSS-Klassen für Jellyfin-Card und Provider-Grid hinzufügen

<read_first>
- frontend/src/app/admin/anime/create/page.module.css (Ende der Datei, nach den in 17-01 hinzugefügten Klassen)
</read_first>

<action>
Am Ende von `page.module.css` hinzufügen:

```css
/* ── Provider Grid (Section 1) ───────────────────────────────────────────── */

.providerGrid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 24px;
  align-items: start;
}

@media (max-width: 800px) {
  .providerGrid {
    grid-template-columns: 1fr;
  }
}

.providerCard {
  background: #fff;
  border: 1px solid #e2e6ea;
  border-radius: 12px;
  padding: 24px;
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.providerCardHeader {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

/* ── Jellyfin ────────────────────────────────────────────────────────────── */

.jellyfinResultsWrap {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.jellyfinAdoptBar {
  display: flex;
  flex-direction: column;
  gap: 12px;
  background: #f0fdf4;
  border: 1px solid #bbf7d0;
  border-radius: 8px;
  padding: 16px;
}

.jellyfinAdoptNote {
  font-size: 13px;
  color: #15803d;
  margin: 0;
  line-height: 1.5;
}
```
</action>

<acceptance_criteria>
- `page.module.css` enthält `.providerGrid {`
- `page.module.css` enthält `.providerCard {`
- `page.module.css` enthält `.jellyfinAdoptBar {`
- `page.module.css` enthält `grid-template-columns: 1fr 1fr`
</acceptance_criteria>

---

### Task 3: useAdminAnimeCreateController.ts — handleJellyfinAdopt ergänzen

<read_first>
- frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts (vollständig)
</read_first>

<action>
In `useAdminAnimeCreateController.ts` einen neuen Handler `handleJellyfinAdopt` einführen. Dieser kapselt den bestehenden Aufruf `hydrateManualDraftFromJellyfinPreview` und macht ihn zu einer expliziten Benutzeraktion:

1. Im `handlers`-Return-Objekt des Controllers `handleJellyfinAdopt` hinzufügen:
```ts
handleJellyfinAdopt: () => {
  if (!jellyfin.preview) return;
  hydrateManualDraftFromJellyfinPreview(jellyfin.preview);
  setSuccessMessage("Jellyfin-Assets wurden zur Prüfung übernommen.");
},
```

   (Die genaue Signatur von `hydrateManualDraftFromJellyfinPreview` aus dem bestehenden Code übernehmen — sie wird bereits aufgerufen, jetzt wird sie hinter einem expliziten Handler gekapselt.)

2. Die Zeile 861:
   ```ts
   setSuccessMessage("Jellyfin-Vorschau verworfen. Der Entwurf bleibt ungespeichert.");
   ```
   ersetzen durch:
   ```ts
   setSuccessMessage("Jellyfin-Auswahl verworfen. Der Anime wurde noch nicht erstellt.");
   ```

3. Zeile 1302:
   ```ts
   "Jellyfin-Suche wieder geoeffnet. Der aktuelle Entwurf bleibt bearbeitbar."
   ```
   ersetzen durch:
   ```ts
   "Jellyfin-Suche wieder geöffnet."
   ```

4. `handleJellyfinAdopt` im `handlers`-Objekt exportieren (neben `handleDiscardJellyfinPreview`, `restartJellyfinReview` usw.).
</action>

<acceptance_criteria>
- `useAdminAnimeCreateController.ts` enthält `handleJellyfinAdopt`
- `useAdminAnimeCreateController.ts` enthält nicht `Der Entwurf bleibt ungespeichert`
- `useAdminAnimeCreateController.ts` enthält `Der Anime wurde noch nicht erstellt`
- `grep -c "handleJellyfinAdopt" frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts` gibt mindestens `2` (Definition + Export)
</acceptance_criteria>

---

### Task 4: page.tsx — CreateJellyfinCard verdrahten, eingebettete Suche entfernen

<read_first>
- frontend/src/app/admin/anime/create/page.tsx (vollständig, Stand nach 17-01)
- frontend/src/app/admin/anime/create/CreateJellyfinCard.tsx (gerade erstellt)
</read_first>

<action>
In `page.tsx`:

1. `CreateJellyfinCard` importieren:
   ```tsx
   import { CreateJellyfinCard } from "./CreateJellyfinCard";
   ```

2. Den `<section className={createStyles.resultsPanel}>` Block (Jellyfin-Suche innerhalb `titleActions`, Zeilen 198–237) vollständig entfernen.

3. Den `titleActions`-Prop in `ManualCreateWorkspace` vereinfachen auf nur noch `CreateAniSearchIntakeCard`:
   ```tsx
   titleActions={
     <div className={createStyles.sourceActionStack}>
       <CreateAniSearchIntakeCard ... />
     </div>
   }
   ```

4. In Section 1 (`id="section-1"`) direkt nach `ManualCreateWorkspace` (oder statt des alten Inline-Blocks) das Provider-Grid mit beiden Cards einbauen:
   ```tsx
   <div className={createStyles.providerGrid}>
     <CreateAniSearchIntakeCard
       anisearchID={controller.anisearch.input}
       searchQuery={controller.anisearch.searchQuery}
       isLoading={controller.anisearch.isLoading}
       isSearchingCandidates={controller.anisearch.isSearchingCandidates}
       filteredExistingCount={controller.anisearch.filteredExistingCount}
       candidates={controller.anisearch.candidates}
       result={controller.anisearch.result}
       conflict={controller.anisearch.conflict}
       errorMessage={controller.anisearch.errorMessage}
       onAniSearchIDChange={handlers.setAniSearchID}
       onSearchQueryChange={handlers.setAniSearchSearchQuery}
       onSearchSubmit={() => { void handlers.handleAniSearchCandidateSearch(); }}
       onCandidateDismiss={handlers.clearAniSearchState}
       onCandidateSelect={(candidate) => { void handlers.handleAniSearchCandidateSelect(candidate); }}
       onSubmit={() => { void handlers.handleAniSearchDraftLoad(); }}
     />
     <CreateJellyfinCard
       query={jellyfin.intake.query}
       candidates={jellyfin.intake.candidates}
       selectedCandidateID={jellyfin.intake.reviewState.selectedCandidate?.jellyfin_series_id}
       hasActivePreview={jellyfin.hasSelectedPreview}
       hasAdoptedAssets={jellyfin.selectedDraftAssetCount > 0}
       isSearching={jellyfin.intake.isSearching}
       isLoadingPreview={jellyfin.intake.isLoadingPreview}
       canSearch={jellyfin.searchState.canSearch}
       isSubmitting={status.isSubmittingCreate}
       showResults={jellyfin.showResults}
       onQueryChange={handlers.setJellyfinQuery}
       onSearch={() => { void handlers.handleJellyfinSearch(); }}
       onSelectCandidate={handlers.handleJellyfinCandidateSelect}
       onLoadCandidatePreview={(id) => { void handlers.handleJellyfinCandidateReview(id); }}
       onAdopt={handlers.handleJellyfinAdopt}
       onDiscard={handlers.handleDiscardJellyfinPreview}
     />
   </div>
   ```

5. Den `ManualCreateWorkspace`-Aufruf auf reine Metadaten-Props reduzieren — `titleActions`, `titleHint`, `typeHint` und `draftAssets` Props entfernen oder auf `undefined` setzen (sie wandern in Plan 17-04 in die richtige Section).

   **Achtung:** `ManualCreateWorkspace` hat diese Props als optional deklariert (prüfen). Falls nicht optional, in diesem Plan die Props als leer übergeben:
   ```tsx
   titleActions={undefined}
   draftAssets={undefined}
   ```
</action>

<acceptance_criteria>
- `page.tsx` enthält `import { CreateJellyfinCard }`
- `page.tsx` enthält `<CreateJellyfinCard`
- `page.tsx` enthält `onAdopt={handlers.handleJellyfinAdopt}`
- `page.tsx` enthält `className={createStyles.providerGrid}`
- `page.tsx` enthält nicht `Jellyfin nutzt ein eigenes Suchfeld. Der finale Titel im`
- `grep -c "resultsPanel" frontend/src/app/admin/anime/create/page.tsx` gibt `0` (alter eingebetteter Block weg)
- `npx tsc --noEmit` läuft ohne neue TypeScript-Fehler im create-Verzeichnis
</acceptance_criteria>
