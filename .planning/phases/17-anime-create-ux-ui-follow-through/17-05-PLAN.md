---
wave: 4
depends_on: [17-04]
files_modified:
  - frontend/src/app/admin/anime/create/CreateReviewSection.tsx
  - frontend/src/app/admin/anime/create/page.tsx
  - frontend/src/app/admin/anime/create/page.module.css
  - frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts
autonomous: true
---

# Plan 17-05: Review Section + CTA + Abschluss-Copy-Sweep

## Objective

Section 4 (`Prüfen & Anlegen`) bekommt eine `CreateReviewSection`-Komponente mit Bereitschaftsübersicht und dem einzigen finalen CTA `Anime erstellen`. Der `Entwurf speichern`-Button (falls vorhanden) wird entfernt. Verbleibende „Entwurf"-Copy im Controller und Header wird bereinigt. Die bestehenden `auth`-Status-Pills und Auth-Logik können im Header verbleiben oder in die Review-Section integriert werden. Der TypeScript-Build und die Vitest-Suite müssen am Ende sauber durchlaufen.

---

## Tasks

### Task 1: CreateReviewSection.tsx anlegen

<read_first>
- frontend/src/app/admin/anime/create/page.tsx (Stand nach 17-04, um Prüf-Daten zu sehen)
- frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts (missingFields, status, auth)
- frontend/src/app/admin/anime/create/page.module.css
</read_first>

<action>
Neue Datei `frontend/src/app/admin/anime/create/CreateReviewSection.tsx` anlegen:

```tsx
"use client";

import createStyles from "./page.module.css";
import styles from "../../admin.module.css";

interface CreateReviewSectionProps {
  missingFields: string[];
  hasTitle: boolean;
  hasCover: boolean;
  hasAniSearch: boolean;
  hasJellyfin: boolean;
  assetCount: number;
  isSubmitting: boolean;
  successMessage?: string | null;
  errorMessage?: string | null;
  onSubmit: () => void;
}

export function CreateReviewSection({
  missingFields,
  hasTitle,
  hasCover,
  hasAniSearch,
  hasJellyfin,
  assetCount,
  isSubmitting,
  successMessage,
  errorMessage,
  onSubmit,
}: CreateReviewSectionProps) {
  const canCreate = missingFields.length === 0;

  return (
    <div className={createStyles.reviewCard}>
      <ul className={createStyles.reviewChecklist}>
        <li className={hasTitle ? createStyles.reviewCheckOk : createStyles.reviewCheckWarn}>
          {hasTitle ? "✓" : "○"} Titel vorhanden
        </li>
        <li className={hasCover ? createStyles.reviewCheckOk : createStyles.reviewCheckWarn}>
          {hasCover ? "✓" : "○"} Cover vorhanden{!hasCover ? " (Pflicht)" : ""}
        </li>
        <li className={hasAniSearch ? createStyles.reviewCheckOk : createStyles.reviewCheckMuted}>
          {hasAniSearch ? "✓" : "○"} AniSearch geladen{!hasAniSearch ? " (optional)" : ""}
        </li>
        <li className={hasJellyfin ? createStyles.reviewCheckOk : createStyles.reviewCheckMuted}>
          {hasJellyfin ? "✓" : "○"} Jellyfin übernommen{!hasJellyfin ? " (optional)" : ""}
        </li>
        {assetCount > 0 ? (
          <li className={createStyles.reviewCheckOk}>
            ✓ {assetCount} Asset{assetCount !== 1 ? "s" : ""} bereit
          </li>
        ) : (
          <li className={createStyles.reviewCheckMuted}>○ Noch keine Assets (optional)</li>
        )}
      </ul>

      {missingFields.length > 0 ? (
        <div className={createStyles.reviewMissing}>
          <strong>Fehlend:</strong> {missingFields.join(", ")}
        </div>
      ) : null}

      <p className={createStyles.reviewNote}>
        Noch nicht erstellt — der Anime wird erst nach dem Klick auf „Anime erstellen" angelegt.
      </p>

      {errorMessage ? (
        <div className={styles.errorBox}>{errorMessage}</div>
      ) : null}
      {successMessage ? (
        <div className={styles.successBox}>{successMessage}</div>
      ) : null}

      <div className={createStyles.reviewActions}>
        <button
          className={createStyles.createCTA}
          type="button"
          disabled={!canCreate || isSubmitting}
          onClick={onSubmit}
        >
          {isSubmitting ? "Wird erstellt…" : "Anime erstellen"}
        </button>
      </div>
    </div>
  );
}
```
</action>

<acceptance_criteria>
- `frontend/src/app/admin/anime/create/CreateReviewSection.tsx` existiert
- Datei enthält `export function CreateReviewSection`
- Datei enthält `Anime erstellen`
- Datei enthält `Noch nicht erstellt`
- Datei enthält `Wird beim Erstellen` — NEIN, hier steht `Noch nicht erstellt — der Anime wird erst nach dem Klick`
- Datei enthält nicht `Entwurf speichern`
- Datei enthält `missingFields`
</acceptance_criteria>

---

### Task 2: CSS für Review Section hinzufügen

<read_first>
- frontend/src/app/admin/anime/create/page.module.css (Ende, nach allen vorherigen Additions)
</read_first>

<action>
Am Ende von `page.module.css` hinzufügen:

```css
/* ── Review Section ──────────────────────────────────────────────────────── */

.reviewCard {
  background: #fff;
  border: 1px solid #e2e6ea;
  border-radius: 12px;
  padding: 24px;
  display: flex;
  flex-direction: column;
  gap: 16px;
  max-width: 560px;
}

.reviewChecklist {
  list-style: none;
  padding: 0;
  margin: 0;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.reviewChecklist li {
  font-size: 14px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.reviewCheckOk {
  color: #15803d;
}

.reviewCheckWarn {
  color: #b42318;
  font-weight: 600;
}

.reviewCheckMuted {
  color: #9ca3af;
}

.reviewMissing {
  font-size: 13px;
  color: #b42318;
  background: #fef2f2;
  border: 1px solid #fecaca;
  border-radius: 8px;
  padding: 10px 14px;
}

.reviewNote {
  font-size: 12px;
  color: #6b7280;
  margin: 0;
}

.reviewActions {
  display: flex;
  gap: 12px;
  align-items: center;
}

.createCTA {
  background: #6d4aff;
  color: #fff;
  border: none;
  border-radius: 8px;
  padding: 12px 28px;
  font-size: 15px;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.15s;
}

.createCTA:hover:not(:disabled) {
  background: #5b3de8;
}

.createCTA:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}
```
</action>

<acceptance_criteria>
- `page.module.css` enthält `.reviewCard {`
- `page.module.css` enthält `.createCTA {`
- `page.module.css` enthält `background: #6d4aff`
- `page.module.css` enthält `.reviewChecklist {`
</acceptance_criteria>

---

### Task 3: page.tsx — ReviewSection einbauen + finaler Copy-Sweep

<read_first>
- frontend/src/app/admin/anime/create/page.tsx (vollständig, Stand nach 17-04)
- frontend/src/app/admin/anime/create/CreateReviewSection.tsx (gerade erstellt)
</read_first>

<action>
In `page.tsx`:

1. `CreateReviewSection` importieren:
   ```tsx
   import { CreateReviewSection } from "./CreateReviewSection";
   ```

2. Den Platzhalter in Section 4 (`id="section-4"`) ersetzen:
   ```tsx
   <section id="section-4" className={createStyles.pageSection}>
     <div className={createStyles.sectionHeading}>
       <span className={createStyles.sectionNumber}>4</span>
       <div>
         <h2 className={createStyles.sectionTitle}>Prüfen & Anlegen</h2>
         <p className={createStyles.sectionSub}>Abschließende Kontrolle.</p>
       </div>
     </div>
     <CreateReviewSection
       missingFields={manualDraft.missingFields}
       hasTitle={manualDraft.values.title.trim().length > 0}
       hasCover={!!manualDraft.stagedCover || !!manualDraft.values.coverImage}
       hasAniSearch={!!controller.anisearch.result}
       hasJellyfin={jellyfin.hasSelectedPreview}
       assetCount={jellyfin.selectedDraftAssetCount + (manualDraft.stagedAssets.background?.length ?? 0)}
       isSubmitting={status.isSubmittingCreate}
       successMessage={status.successMessage}
       errorMessage={errorMessage}
       onSubmit={handlers.handleCreateSubmit}
     />
   </section>
   ```

3. Die globalen `errorMessage` und `status.successMessage`-Boxen (Zeilen 128–131) aus dem page-level entfernen, da sie jetzt in `CreateReviewSection` angezeigt werden.

4. Die Auth-Status-Variablen `authStatusReady`, `authStatusClassName`, `authStatusLabel` (Zeilen 71–81) entfernen, da der Auth-Status-Pill aus dem Header entfernt wurde.

5. Sicherstellen dass `page.tsx` keinen Button mit Text `Entwurf speichern` enthält.

6. Die `pageIntro` in der Header auf:
   ```tsx
   Schritt für Schritt zum perfekten Ergebnis.
   ```
   belassen (aus 17-01).
</action>

<acceptance_criteria>
- `page.tsx` enthält `import { CreateReviewSection }`
- `page.tsx` enthält `<CreateReviewSection`
- `page.tsx` enthält `onSubmit={handlers.handleCreateSubmit}`
- `grep -c "Entwurf speichern" frontend/src/app/admin/anime/create/page.tsx` gibt `0`
- `grep -c "authStatusReady" frontend/src/app/admin/anime/create/page.tsx` gibt `0`
- `page.tsx` enthält nicht `<div className={styles.errorBox}>` auf page-level (nur in ReviewSection)
</acceptance_criteria>

---

### Task 4: Finaler Copy- und TypeScript-Sweep

<read_first>
- frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts (alle verbleibenden "Entwurf"-Vorkommen)
- frontend/src/app/admin/anime/create/page.tsx (final)
</read_first>

<action>
1. In `useAdminAnimeCreateController.ts` alle verbleibenden sichtbaren Copy-Strings mit „Entwurf" prüfen:
   ```bash
   grep -n "Entwurf" frontend/src/app/admin/anime/create/useAdminAnimeCreateController.ts
   ```
   Jeden gefundenen User-facing String (kein Variablenname, kein Kommentar) durch die temporale Alternative ersetzen:
   - `"... Entwurf ..."` → `"... Anime noch nicht erstellt ..."`

2. TypeScript-Prüfung ausführen:
   ```bash
   npx tsc --noEmit 2>&1 | grep -i "error" | head -20
   ```
   Alle gemeldeten Fehler beheben.

3. Vitest-Suite ausführen:
   ```bash
   npx vitest run frontend/src/app/admin/anime/create/ 2>&1 | tail -20
   ```
   Alle fehlschlagenden Tests reparieren.
</action>

<acceptance_criteria>
- `grep -rn "Entwurf speichern" frontend/src/app/admin/anime/create/` gibt keine Treffer
- `npx tsc --noEmit` läuft ohne Fehler (oder nur bekannte Pre-existing-Fehler, die nicht in Phase 17 eingeführt wurden)
- `npx vitest run frontend/src/app/admin/anime/create/` läuft ohne Fehler
- `grep -c "Entwurf" frontend/src/app/admin/anime/create/page.tsx` gibt `0`
</acceptance_criteria>
