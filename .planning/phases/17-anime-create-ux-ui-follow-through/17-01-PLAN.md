---
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/admin/anime/create/page.tsx
  - frontend/src/app/admin/anime/create/page.module.css
  - frontend/src/app/admin/anime/create/CreatePageStepper.tsx
autonomous: true
---

# Plan 17-01: Stepper + Vier-Sektionen-Seitenstruktur

## Objective

Die `/admin/anime/create`-Seite bekommt einen sichtbaren Stepper oben (Anime finden / Assets / Details / Prüfen & Anlegen) und wird in vier benannte `<section>`-Blöcke gegliedert. Die aktuellen Status-Pills im Header (`statusBar`) werden entfernt — ihre Information wandert in Section 4 (Plan 17-05). Die Provider-Controls (AniSearch, Jellyfin) und Assets bleiben inhaltlich unverändert; nur Struktur und Navigation werden angepasst.

---

## Tasks

### Task 1: CreatePageStepper-Komponente anlegen

<read_first>
- frontend/src/app/admin/anime/create/page.module.css
- frontend/src/app/admin/anime/create/page.tsx (Zeilen 1-90, bestehende Imports und Styles)
</read_first>

<action>
Neue Datei `frontend/src/app/admin/anime/create/CreatePageStepper.tsx` anlegen:

```tsx
"use client";

import createStyles from "./page.module.css";

const STEPS = [
  { id: 1, label: "Anime finden", sub: "AniSearch & Jellyfin" },
  { id: 2, label: "Assets", sub: "Cover, Banner, Logo & Hintergründe" },
  { id: 3, label: "Details", sub: "Infos & Beschreibung" },
  { id: 4, label: "Prüfen & Anlegen", sub: "Abschließende Kontrolle" },
] as const;

interface CreatePageStepperProps {
  activeStep?: 1 | 2 | 3 | 4;
}

export function CreatePageStepper({ activeStep = 1 }: CreatePageStepperProps) {
  return (
    <nav className={createStyles.stepper} aria-label="Erstellungsschritte">
      {STEPS.map((step) => {
        const isActive = step.id === activeStep;
        const isDone = step.id < activeStep;
        return (
          <a
            key={step.id}
            href={`#section-${step.id}`}
            className={[
              createStyles.stepperItem,
              isActive ? createStyles.stepperItemActive : "",
              isDone ? createStyles.stepperItemDone : "",
            ]
              .filter(Boolean)
              .join(" ")}
            aria-current={isActive ? "step" : undefined}
          >
            <span className={createStyles.stepperNumber}>{step.id}</span>
            <span className={createStyles.stepperLabel}>
              <strong>{step.label}</strong>
              <small>{step.sub}</small>
            </span>
          </a>
        );
      })}
    </nav>
  );
}
```
</action>

<acceptance_criteria>
- `frontend/src/app/admin/anime/create/CreatePageStepper.tsx` existiert
- Datei enthält `export function CreatePageStepper`
- Datei enthält alle vier Labels: `Anime finden`, `Assets`, `Details`, `Prüfen & Anlegen`
- Datei enthält `href="#section-1"` bis `href="#section-4"`
- Datei enthält `aria-current={isActive ? "step" : undefined}`
</acceptance_criteria>

---

### Task 2: Stepper-CSS-Klassen in page.module.css hinzufügen

<read_first>
- frontend/src/app/admin/anime/create/page.module.css (vollständig lesen)
</read_first>

<action>
Am Ende von `page.module.css` folgende CSS-Klassen anhängen (nicht ersetzen):

```css
/* ── Stepper ─────────────────────────────────────────────────────────────── */

.stepper {
  display: flex;
  gap: 0;
  background: #fff;
  border: 1px solid #e2e6ea;
  border-radius: 12px;
  overflow: hidden;
  margin-bottom: 32px;
}

.stepperItem {
  display: flex;
  align-items: center;
  gap: 12px;
  flex: 1;
  padding: 16px 20px;
  text-decoration: none;
  color: #6b7280;
  border-right: 1px solid #e2e6ea;
  transition: background 0.15s;
}

.stepperItem:last-child {
  border-right: none;
}

.stepperItem:hover {
  background: #f4f6f8;
}

.stepperItemActive {
  color: #6d4aff;
}

.stepperItemActive .stepperNumber {
  background: #6d4aff;
  color: #fff;
}

.stepperItemDone .stepperNumber {
  background: #e9e4ff;
  color: #6d4aff;
}

.stepperNumber {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 28px;
  height: 28px;
  border-radius: 50%;
  background: #f4f6f8;
  font-size: 13px;
  font-weight: 700;
  flex-shrink: 0;
}

.stepperLabel {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.stepperLabel strong {
  font-size: 14px;
  font-weight: 600;
  line-height: 1.3;
}

.stepperLabel small {
  font-size: 11px;
  font-weight: 400;
  line-height: 1.3;
  color: #9ca3af;
}

/* ── Sektionsstruktur ────────────────────────────────────────────────────── */

.pageSection {
  margin-bottom: 40px;
  scroll-margin-top: 80px;
}

.sectionHeading {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 20px;
}

.sectionNumber {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 32px;
  height: 32px;
  border-radius: 50%;
  background: #6d4aff;
  color: #fff;
  font-size: 14px;
  font-weight: 700;
  flex-shrink: 0;
}

.sectionTitle {
  font-size: 18px;
  font-weight: 700;
  color: #111827;
  margin: 0;
}

.sectionSub {
  font-size: 13px;
  color: #6b7280;
  margin: 0;
}
```
</action>

<acceptance_criteria>
- `page.module.css` enthält `.stepper {`
- `page.module.css` enthält `.stepperItemActive {`
- `page.module.css` enthält `.stepperNumber {`
- `page.module.css` enthält `.pageSection {`
- `page.module.css` enthält `.sectionNumber {`
- `page.module.css` enthält `color: #6d4aff`
</acceptance_criteria>

---

### Task 3: page.tsx in vier Sektionen umstrukturieren

<read_first>
- frontend/src/app/admin/anime/create/page.tsx (vollständig)
- frontend/src/app/admin/anime/create/CreatePageStepper.tsx (gerade erstellt)
</read_first>

<action>
In `page.tsx`:

1. `CreatePageStepper` importieren:
   ```tsx
   import { CreatePageStepper } from "./CreatePageStepper";
   ```

2. Den `<header>`-Block mit `statusBar` (Zeilen 92–126) ersetzen durch:
   ```tsx
   <header className={createStyles.pageHeader}>
     <div className={createStyles.pageTitleBlock}>
       <h1 className={createStyles.pageTitle}>Anime erstellen</h1>
       <p className={createStyles.pageIntro}>
         Schritt für Schritt zum perfekten Ergebnis.
       </p>
     </div>
   </header>
   <CreatePageStepper activeStep={1} />
   ```

3. Den `<section className={createStyles.workspaceSection}>` (Zeile 133) ersetzen durch vier benannte Sektionen:

   ```tsx
   {/* ── Section 1: Anime finden ──────────────────────────────── */}
   <section id="section-1" className={createStyles.pageSection}>
     <div className={createStyles.sectionHeading}>
       <span className={createStyles.sectionNumber}>1</span>
       <div>
         <h2 className={createStyles.sectionTitle}>Anime finden</h2>
         <p className={createStyles.sectionSub}>
           Suche den Anime in AniSearch und wähle anschließend den passenden Ordner in Jellyfin aus.
         </p>
       </div>
     </div>
     {/* Provider-Cards kommen hier rein — Inhalt wird in 17-02 und 17-03 angepasst */}
     <ManualCreateWorkspace
       {/* alle bestehenden Props UNVERÄNDERT übernehmen */}
       ...
       titleActions={...} {/* bestehender titleActions-Block unverändert */}
       draftAssets={...}  {/* bestehender draftAssets-Block unverändert */}
       ...
     />
   </section>

   {/* ── Section 2: Assets ────────────────────────────────────── */}
   <section id="section-2" className={createStyles.pageSection}>
     <div className={createStyles.sectionHeading}>
       <span className={createStyles.sectionNumber}>2</span>
       <div>
         <h2 className={createStyles.sectionTitle}>Assets</h2>
         <p className={createStyles.sectionSub}>
           Prüfe und ergänze die Assets.
         </p>
       </div>
     </div>
     {/* Inhalt wird in Plan 17-04 eingesetzt */}
   </section>

   {/* ── Section 3: Details ───────────────────────────────────── */}
   <section id="section-3" className={createStyles.pageSection}>
     <div className={createStyles.sectionHeading}>
       <span className={createStyles.sectionNumber}>3</span>
       <div>
         <h2 className={createStyles.sectionTitle}>Details</h2>
         <p className={createStyles.sectionSub}>
           Ergänze die Metadaten und Beschreibung.
         </p>
       </div>
     </div>
     {/* Metadaten-Formular aus ManualCreateWorkspace wird hier isoliert — Plan 17-04 */}
   </section>

   {/* ── Section 4: Prüfen & Anlegen ──────────────────────────── */}
   <section id="section-4" className={createStyles.pageSection}>
     <div className={createStyles.sectionHeading}>
       <span className={createStyles.sectionNumber}>4</span>
       <div>
         <h2 className={createStyles.sectionTitle}>Prüfen & Anlegen</h2>
         <p className={createStyles.sectionSub}>
           Abschließende Kontrolle vor dem Erstellen.
         </p>
       </div>
     </div>
     {/* Inhalt wird in Plan 17-05 eingesetzt */}
   </section>
   ```

   **Wichtig:** In diesem Plan bleibt `ManualCreateWorkspace` mit allen Props erhalten und landet in Section 1. Die Aufsplittung in die richtigen Sektionen erfolgt in Plans 17-03 und 17-04. Section 2, 3 und 4 sind vorerst leere Platzhalter mit Heading.

4. Den `<CreateJellyfinResultsPanel ...>` Block (Zeilen 342–354) in `page.tsx` entfernen — wird in Plan 17-03 durch die neue Jellyfin-Card ersetzt.

5. Die Zeilen für `authStatusReady`, `authStatusClassName`, `authStatusLabel` (Zeilen 71–81) können vorerst stehen bleiben (werden in 17-05 bereinigt).
</action>

<acceptance_criteria>
- `page.tsx` enthält `import { CreatePageStepper } from "./CreatePageStepper"`
- `page.tsx` enthält `<CreatePageStepper`
- `page.tsx` enthält `id="section-1"` bis `id="section-4"`
- `page.tsx` enthält nicht mehr `<div className={createStyles.statusBar}>`
- `page.tsx` enthält `Schritt für Schritt zum perfekten Ergebnis.`
- `page.tsx` enthält `<h2 className={createStyles.sectionTitle}>Anime finden</h2>`
- `page.tsx` enthält `<h2 className={createStyles.sectionTitle}>Assets</h2>`
- `page.tsx` enthält `<h2 className={createStyles.sectionTitle}>Details</h2>`
- `page.tsx` enthält `<h2 className={createStyles.sectionTitle}>Prüfen & Anlegen</h2>`
- `grep -c "CreateJellyfinResultsPanel" frontend/src/app/admin/anime/create/page.tsx` gibt `0` aus
</acceptance_criteria>
