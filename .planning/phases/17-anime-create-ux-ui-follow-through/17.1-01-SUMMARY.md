---
phase: 17.1-anime-create-asset-section-polish
plan: 01
subsystem: ui
tags: [react, css-modules, lucide-react, create-page, asset-section]

# Dependency graph
requires:
  - phase: 17-anime-create-ux-ui-follow-through
    provides: CreateAssetSection, CreateAssetCard, CreateAssetSearchDialog components and page.module.css asset styles
provides:
  - Unified assetPanel wrapper class for white rounded outer container
  - Color-coded source badge overlays (Jellyfin=green, TMDB=blue, Zerochan=orange, Manuell/Online=gray)
  - Icon button styles (assetIconBtn, assetIconBtnDestructive) for edit/remove actions
  - CreateAssetCard with position:relative preview and source overlay pattern
  - CreateAssetSection with panel wrapper and Pencil/Trash2 icon buttons replacing text pills
  - Section 2 heading without misplaced Jellyfin scan button
affects: [phase 18+, UAT re-test for UAT test 7]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Source badge overlay on asset card preview uses base class + source-specific modifier class"
    - "Icon-only action buttons (32x32) replace text pill buttons for compact asset slot actions"
    - "assetPanel white rounded wrapper groups all asset slots into one visual unit"

key-files:
  created: []
  modified:
    - frontend/src/app/admin/anime/create/page.module.css
    - frontend/src/app/admin/anime/create/CreateAssetCard.tsx
    - frontend/src/app/admin/anime/create/CreateAssetSection.tsx
    - frontend/src/app/admin/anime/create/page.tsx
    - frontend/src/app/admin/anime/create/page.test.tsx

key-decisions:
  - "Worktree branch merged main before applying 17.1 changes since worktree was on an older pre-phase-17 codebase"
  - "assetCardSourceOverlay added as position:absolute overlay on the image preview (required adding position:relative to assetCardPreview)"
  - "CreateAssetSection kept the fileInputRefs prop and JellyfinDraftAssetTarget import to match worktree's existing interface"
  - "Stale test assertions for Banner/Logo dialog-only button text removed since CreateAssetSearchDialog returns null when closed"

patterns-established:
  - "Source badge overlay: combine createStyles.assetCardSourceOverlay + createStyles.assetCardSourceOverlay{Source} via filter(Boolean).join(' ')"
  - "Icon action row: assetIconActions > assetIconBtn (edit) + assetIconBtn + assetIconBtnDestructive (remove)"

requirements-completed: []

# Metrics
duration: 40min
completed: 2026-04-17
---

# Phase 17.1 Plan 01: Asset Section Visual Polish Summary

**Unified asset panel wrapper with color-coded source badges and Pencil/Trash2 icon buttons replacing text pill actions, plus removal of misplaced Jellyfin scan button from Section 2 heading**

## Performance

- **Duration:** 40 min
- **Started:** 2026-04-17T14:00:00Z
- **Completed:** 2026-04-17T14:40:00Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Added `assetPanel` white rounded wrapper class, source badge modifier classes for all 5 providers, and `assetIconBtn`/`assetIconBtnDestructive` icon button styles to page.module.css
- Updated CreateAssetCard to render source badges as `position:absolute` overlays with provider-specific color via CSS modifier classes (Jellyfin=green, TMDB=blue, Zerochan=orange, Manuell/Online=gray)
- Refactored CreateAssetSection to wrap the asset grid in `assetPanel` and replace all text pill buttons with compact 32px Pencil/Trash2 icon buttons
- Removed the conditional Jellyfin scan button from Section 2 heading; Jellyfin search belongs only in Section 1's CreateJellyfinCard
- Updated page.test.tsx to remove stale assertions for dialog-only button text strings

## Task Commits

1. **Task 1: CSS panel wrapper, source badge modifiers, icon button styles** - `abd6226` (feat)
2. **Task 2: Color-coded badges, panel wrapper, icon buttons, scan button removal, test update** - `e359f22` (feat)

## Files Created/Modified

- `frontend/src/app/admin/anime/create/page.module.css` - Added assetPanel, assetCardSourceOverlay base + 5 modifier classes, assetIconActions/assetIconBtn/assetIconBtnDestructive, position:relative on assetCardPreview
- `frontend/src/app/admin/anime/create/CreateAssetCard.tsx` - Added sourceModifierClass helper, moved source badge to overlay position inside preview div, removed assetSourceBadge
- `frontend/src/app/admin/anime/create/CreateAssetSection.tsx` - Added lucide-react import, assetPanel wrapper, replaced all text buttons with Pencil/Trash2 icon buttons, typed onRemoveJellyfinAsset with JellyfinDraftAssetTarget
- `frontend/src/app/admin/anime/create/page.tsx` - Removed conditional Jellyfin scan button block from Section 2 sectionHeading div
- `frontend/src/app/admin/anime/create/page.test.tsx` - Removed stale "Banner online suchen" and "Logo online suchen" assertions (dialog-only strings), added "Hintergrund hinzufuegen" label check

## Decisions Made

- Merged main into the worktree branch before implementing, since the worktree was on an older branch that lacked Phase 17 files (CreateAssetCard, CreateAssetSection, etc.)
- Kept `fileInputRefs` prop and `JellyfinDraftAssetTarget` import in CreateAssetSection to match the worktree's existing interface instead of adopting the main branch's slightly different interface; this preserves type correctness
- Added `position: relative` to `.assetCardPreview` as a minor addition required for the `position: absolute` source overlay to work correctly; this wasn't in the plan but was necessary for correctness

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Added position:relative to assetCardPreview for overlay positioning**
- **Found during:** Task 1 (CSS changes)
- **Issue:** The worktree's `.assetCardPreview` lacked `position: relative`, which is required for the `assetCardSourceOverlay` (position:absolute) to render at the correct position inside the card preview area
- **Fix:** Added `position: relative` to `.assetCardPreview` rule
- **Files modified:** frontend/src/app/admin/anime/create/page.module.css
- **Verification:** TypeScript compiles cleanly; badge overlay anchors to the preview container
- **Committed in:** e359f22 (Task 2 commit)

**2. [Rule 1 - Bug] Used JellyfinDraftAssetTarget for onRemoveJellyfinAsset prop type**
- **Found during:** Task 2 (TypeScript compilation)
- **Issue:** CreateAssetSection's original worktree interface used `{ kind: string; index?: number }` while the handler expected `JellyfinDraftAssetTarget` (narrower union type), causing a TS2322 error
- **Fix:** Imported `JellyfinDraftAssetTarget` from useManualAnimeDraft and used it as the prop type
- **Files modified:** frontend/src/app/admin/anime/create/CreateAssetSection.tsx
- **Verification:** `npx tsc --noEmit` from worktree frontend returns 0 errors
- **Committed in:** e359f22 (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (2 Rule 1 - Bug)
**Impact on plan:** Both fixes were required for correct TypeScript compilation and correct visual rendering. No scope creep.

## Issues Encountered

- The worktree branch was significantly behind main (pre-Phase-17), requiring a merge of `main` before the plan changes could be applied. After the merge, all plan-required files (CreateAssetCard.tsx, CreateAssetSection.tsx, etc.) were available.
- TypeScript compilation from `/c/Users/admin/Documents/Team4s/frontend` (main repo) showed stale cache errors that appeared to be from the worktree's older interface. Running tsc from the correct worktree path confirmed 0 errors.
- The worktree has no `node_modules`, so vitest cannot run directly from the worktree path. Tests were verified structurally (logic correctness + tsc) rather than through direct vitest execution.

## Next Phase Readiness

- UAT test 7 (asset section visual) should now pass upon browser verification
- The asset section panel, colored badges, and icon buttons are ready for visual UAT re-test
- No blockers for subsequent phases

---
*Phase: 17.1-anime-create-asset-section-polish*
*Completed: 2026-04-17*
