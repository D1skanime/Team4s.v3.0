---
phase: 150-badge-regeln-eine-autoritative-schwellenquelle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/badges/thresholds.go
  - backend/internal/badges/thresholds_test.go
  - .planning/phases/150-badge-regeln-eine-autoritative-schwellenquelle/evidence/before-member-type-profile.txt
  - .planning/phases/150-badge-regeln-eine-autoritative-schwellenquelle/evidence/before-dashboard.json
autonomous: true
requirements: [SC-1, SC-7, SC-9]

must_haves:
  truths:
    - "A single Go package holds every badge-family threshold number used anywhere in the backend"
    - "The registry's boundary math (current tier / next tier / next threshold / remaining) is proven correct at every family's edges before any production call site depends on it"
    - "A real 'before' snapshot of member `type`'s public profile and a real member's dashboard exists on disk, so the final Live-UAT can prove zero visible change"
  artifacts:
    - path: "backend/internal/badges/thresholds.go"
      provides: "The one authoritative threshold registry (RoleVolume, Points, Progress, ContributionProjects, ContributionChronicle, ContributionArchivist, Membership families + membership-years constants) with CurrentTier/NextTier/Remaining helpers"
      contains: "package badges"
    - path: "backend/internal/badges/thresholds_test.go"
      provides: "Table-driven boundary proofs for every family (no Postgres needed, pure functions)"
    - path: ".planning/phases/150-badge-regeln-eine-autoritative-schwellenquelle/evidence/before-member-type-profile.txt"
      provides: "Pre-change baseline text capture for the Live-UAT diff"
  key_links:
    - from: "backend/internal/badges/thresholds.go"
      to: "backend/internal/repository/*.go, backend/internal/services/badge_service.go (Plans 150-02/150-03)"
      via: "Go import team4s.v3/backend/internal/badges"
      pattern: "team4s\\.v3/backend/internal/badges"
---

<objective>
Create the ONE authoritative Go threshold registry for all badge families (D-01) and capture a
real "before" evidence snapshot of member `type`'s public profile plus one authenticated
dashboard, so the phase's final Live-UAT (150-06) has something honest to diff against.

Purpose: every later plan in this phase (150-02, 150-03) repoints existing hardcoded switches/maps
to this registry instead of inventing their own numbers. Capturing evidence now, before any
production code changes, is the only way the final parity check is real rather than asserted.

Output: `backend/internal/badges` package (registry + unit tests), two evidence files under the
phase's `evidence/` directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/150-badge-regeln-eine-autoritative-schwellenquelle/150-CONTEXT.md
@.planning/phases/150-badge-regeln-eine-autoritative-schwellenquelle/150-UI-SPEC.md

<interfaces>
<!-- Every one of these numbers must appear NOWHERE ELSE as a literal in backend/ after this
phase (SC-1). This plan only CREATES the registry; it changes no consumer yet. -->

Confirmed families and their EXACT current literal values (verified against team4s-linux,
commit 016da485, 2026-09-06):

- role_volume (backend/internal/repository/member_profile_role_volume_repository.go:15-28,
  71-121): bronze=12, silver=108, gold=320, platinum=510. Below 12 the existing code calls this
  tier "" in `highestRoleVolumeTier` but "entry" in `roleVolumeProgressBadge` — both behaviors
  must be preservable from the registry (registry itself only needs to return "" below the first
  tier; the "entry" relabeling stays local to role_volume_repository.go, D-03: no behavior change).
- points (backend/internal/repository/member_profile_progress_repository.go:70-73): tier CODES
  are themselves the full badge codes — point_milestone_first=1, point_milestone_active=50,
  point_milestone_experienced=200, point_milestone_engaged=500, point_milestone_veteran=1000,
  point_milestone_legend=2500.
- progress (member_profile_progress_repository.go:67-69): first_contribution=1,
  productive_bronze=10, productive_silver=25, productive_gold=50. The SAME thresholds are used a
  second time in services/badge_service.go's computeProductiveTiers with the SAME three codes.
- contribution_projects (member_profile_contribution_badges_repository.go:22-33 +
  member_profile_progress_repository.go:74 + member_profile_dashboard_repository.go:104): bronze=1,
  silver=5, gold=15.
- contribution_chronicle (contribution_badges_repository.go:40-51 + progress_repository.go:75 +
  dashboard_repository.go:105): bronze=10, silver=50, gold=150.
- contribution_archivist (contribution_badges_repository.go:59-70 + progress_repository.go:76 +
  dashboard_repository.go:106): bronze=10, silver=50, gold=150 (same numbers as chronicle, a
  SEPARATE family/count source — do not merge the two families into one).
- membership (progress_repository.go:77-79 + services/badge_service.go:35-36,144-160):
  long_term_member=5 years (services/badge_service.go:137-161, currently a literal
  `INTERVAL '5 years'` SQL fragment appearing twice in the same query), membership_7_years=7,
  membership_10_years=10 (services/badge_service.go:35-36, passed today as bare Go int arguments
  to computeMembershipMilestone).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture pre-change UAT evidence baseline</name>
  <files>.planning/phases/150-badge-regeln-eine-autoritative-schwellenquelle/evidence/before-member-type-profile.txt, .planning/phases/150-badge-regeln-eine-autoritative-schwellenquelle/evidence/before-dashboard.json</files>
  <action>
    Before touching any production code, capture the "before" state the final Live-UAT (150-06)
    will diff against, per 150-UI-SPEC.md's Parity Verification Method sections A and B.

    A. Public profile: run `frontend/scripts/collect-member-profile-evidence.mjs` (or, if its
    flags do not fit a single-member capture, a minimal Playwright script following the same
    page-object pattern) against `http://192.168.235.196:3000/members/type` (or the SSH-tunnel
    path `http://127.0.0.1:3300/members/type`). Save the captured `document.body.innerText` for
    the badge-chain section plus the specific named regression numbers from ROADMAP.md
    (`role_volume_typesetter_bronze`: current_count 13, current_tier bronze, next_threshold 108,
    remaining_count 95, next_tier silver) into
    `evidence/before-member-type-profile.txt`.

    B. Dashboard: curl the authenticated `GET /api/v1/me/dashboard` endpoint (or reuse the login
    pattern from `frontend/scripts/capture-phase134-uat-evidence.mjs` if it already authenticates)
    for a real member with non-trivial points/role-volume/contribution activity. Per
    150-UI-SPEC.md's Claude's-Discretion default, try `sheppert` or `csubs-leader` first (credentials
    documented in prior phase evidence, e.g. `134-UAT.md`); if neither has meaningful role-volume or
    points data at capture time, use any real member account with real activity and record which
    one in the plan's SUMMARY. Save the raw JSON response to `evidence/before-dashboard.json`.

    If the dev stack (`docker compose ps`) is not already running, start it first; do not skip this
    capture — Plan 150-06's Live-UAT depends on it existing.
  </action>
  <verify>
    <automated>test -s .planning/phases/150-badge-regeln-eine-autoritative-schwellenquelle/evidence/before-member-type-profile.txt && test -s .planning/phases/150-badge-regeln-eine-autoritative-schwellenquelle/evidence/before-dashboard.json</automated>
  </verify>
  <done>Both evidence files exist, are non-empty, and the profile capture contains the string "role_volume_typesetter_bronze" plus the named regression numbers (13 / bronze / 108 / 95 / silver).</done>
</task>

<task type="auto">
  <name>Task 2: Create the single Go threshold registry package</name>
  <files>backend/internal/badges/thresholds.go, backend/internal/badges/thresholds_test.go</files>
  <action>
    Create package `badges` at `backend/internal/badges/thresholds.go` (new package, sibling to
    `repository`/`services`/`permissions` — matches this codebase's small-package-explicit-struct
    convention). Define a `Tier` struct (`Code string`, `Threshold int64`) and a `Family` struct
    (`Tiers []Tier`, ascending order). Export exactly six family values matching the numbers listed
    in this plan's `<interfaces>` block verbatim: `RoleVolume`, `Points`, `Progress`,
    `ContributionProjects`, `ContributionChronicle`, `ContributionArchivist`, `Membership` — plus
    three bare int constants for services/badge_service.go's SQL-argument call sites:
    `MembershipLongTermYears = 5`, `Membership7Years = 7`, `Membership10Years = 10` (these three
    MUST read from the same source-of-truth values as the `Membership` family's tiers — define the
    family first and derive the constants as `int64` literals matching family entries exactly, or
    document in a comment why they are declared independently if Go's const-from-slice limitation
    forces that; either way a test must prove they match).

    Add three methods on `Family`: `CurrentTier(count int64) string` (the highest tier whose
    threshold is <= count, or "" if none), `NextTier(count int64) (tier Tier, ok bool)` (the first
    tier whose threshold is > count; ok=false once count already meets the highest tier),
    `Remaining(count int64) int64` (0 if at/above the highest tier, else next threshold minus
    count — never negative).

    Do not wire this package into any existing file yet — that is Plans 150-02 and 150-03. This
    plan's only job is that the registry exists, is correct, and is importable.
  </action>
  <verify>
    <automated>cd backend && go build ./internal/badges/... && go vet ./internal/badges/... && go test ./internal/badges/... -run . -v</automated>
  </verify>
  <done>`go build`/`go vet` clean; `go test ./internal/badges/...` passes with boundary cases for all seven exported families/constants (last-value-below-first-tier returns "", exact-threshold-count returns that tier, one-below-max returns max-1 remaining=1, at-max returns Remaining=0 and NextTier ok=false), matching every number in this plan's interfaces block exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This plan adds a pure, unexported-nothing Go package with no I/O, no DB access, no HTTP surface, and a local evidence-capture script run against an already-authorized dev endpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-01 | Tampering | evidence capture script (curl to authenticated /me/dashboard) | accept | Credentials are pre-existing dev fixtures already used in prior phases (134-UAT.md); no new secret is introduced, output is written to a git-tracked evidence file the developer reviews before commit. |
| T-150-SC | Supply chain | package installs | accept | This plan introduces no new npm/pip/cargo dependency; it only adds a Go file and runs an existing Playwright script. |
</threat_model>

<verification>
- `cd backend && go build ./... && go vet ./...` still clean for the whole module (no other package touched).
- `go test ./internal/badges/... -v` green.
- Evidence files present and non-empty under `.planning/phases/150-badge-regeln-eine-autoritative-schwellenquelle/evidence/`.
</verification>

<success_criteria>
- Exactly one new Go package exists holding every badge threshold number named in ROADMAP.md's Ausgangsbefund table.
- The registry's helper methods are proven correct at every family boundary via table-driven tests, with no Postgres dependency.
- A real, timestamped "before" snapshot of member `type`'s public profile and one member's dashboard exists on disk for the phase's final Live-UAT diff.
</success_criteria>

<output>
Create `.planning/phases/150-badge-regeln-eine-autoritative-schwellenquelle/150-01-SUMMARY.md` when done. Record which member account was used for the dashboard capture (Task 1) if not `sheppert`/`csubs-leader`.
</output>
