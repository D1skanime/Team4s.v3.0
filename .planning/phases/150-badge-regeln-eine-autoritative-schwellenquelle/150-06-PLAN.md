---
phase: 150-badge-regeln-eine-autoritative-schwellenquelle
plan: 06
type: execute
wave: 6
depends_on: ["150-01", "150-02", "150-03", "150-04", "150-05", "150-07"]
files_modified: []
autonomous: false
requirements: [SC-1, SC-2, SC-6, SC-7, SC-9]

must_haves:
  truths:
    - "go build/vet/test show no new failures versus the commit-016da485 baseline"
    - "Frontend vitest, tsc --noEmit, and the frontend Docker build all succeed"
    - "A grep-based search proves exactly one Go location holds each family's threshold numbers"
    - "Changing one registry constant changes the rendered UI with zero TypeScript edits, and this propagation is demonstrated on a rendered STAGE LADDER, not only the single dashboard/profile progress row (Context Revision 2026-09-06, D-28)"
    - "A real human, on :3000, confirms member `type`'s role-volume ladder and points progress render exactly as before"
  artifacts: []
  key_links:
    - from: "backend/internal/badges"
      to: "rendered UI at :3000"
      via: "one Go constant change propagating through repository -> API response -> frontend render with no TS edit, demonstrated on both a single progress row AND a multi-stage ladder"
      pattern: "SC-6 propagation demo"
---

<objective>
Run the phase's Regression-Gates verbatim (ROADMAP.md), close the three success criteria that can
only be proven at the end (SC-1 search proof, SC-6 propagation proof, SC-9 live UAT), and get an
external, human-run Live-UAT sign-off at member `type` before this phase is considered done. This
plan is intentionally NOT autonomous -- D-23 requires it to be run externally by the user.

**Context Revision 2026-09-06 (D-28) extends this plan's SC-6 propagation proof and Live-UAT
checklist.** Plans 150-03/150-05 (revised) moved each badge family's multi-stage "ladder" data
(the stage lists feeding the Anime-Projekte/Beiträge/Mitgliedschaft/Punkte collection cards and the
role badge card's medal ladder) from a frontend constant into `badge_progress[].stages`. SC-6's
propagation proof must therefore demonstrate that a registry change reaches a rendered STAGE
LADDER (e.g. the role badge card's medal row, or the points-milestone ladder), not only the single
progress-row numbers (current_count/current_tier/next_threshold/remaining_count) the pre-revision
plan checked. This is an extension of the existing demo, not a new task or a new evidence file.

Purpose: every prior plan in this phase proved its own slice in isolation (unit tests, per-file
Postgres tests). This plan proves the WHOLE system together, against the real dev stack, with the
real "before" evidence captured in Plan 150-01.

Output: a completion record (this plan's SUMMARY) with pass/fail for every regression gate, the
grep proof for SC-1, the propagation demo for SC-6 (now covering both the single progress row and a
rendered stage ladder), and the human's live sign-off for SC-9.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/150-badge-regeln-eine-autoritative-schwellenquelle/150-CONTEXT.md
@.planning/phases/150-badge-regeln-eine-autoritative-schwellenquelle/150-UI-SPEC.md
@.planning/phases/150-badge-regeln-eine-autoritative-schwellenquelle/evidence/before-member-type-profile.txt
@.planning/phases/150-badge-regeln-eine-autoritative-schwellenquelle/evidence/before-dashboard.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the automated Regression-Gates and the SC-1/SC-6 structural proofs</name>
  <files></files>
  <action>
    Run, in order, and capture full output for the SUMMARY:
    (1) `cd backend && go build ./... && go vet ./...` -- must be clean.
    (2) `go test ./...` -- compare failures against a baseline `git archive` checkout of commit
    016da485 (per ROADMAP.md: 51 pre-existing DSN-related failures are the known baseline; this run
    must show no NEW failures beyond that set, and ideally fewer, since Plan 150-02's Task 3 added
    a real Postgres fixture where a source-substring test used to be).
    (3) Real-Postgres badge repository tests specifically: confirm every `*_postgres_test.go` /
    `*Postgres*` test under `backend/internal/repository` and the new `services` package fixture
    (Plan 150-02 Task 3) ran against a real database, not skipped.
    (4) `cd frontend && npx vitest run` (full suite) -- no new failures versus pre-phase baseline
    (STATE.md notes 2 of 9 known-flaky non-reproducible failures from Phase 149 unrelated to this
    phase; treat those as pre-existing if they reappear, but any NEW failing test file is a
    blocker).
    (5) `npx tsc --noEmit` -- clean.
    (6) `docker compose build team4sv30-frontend` -- succeeds.

    SC-1 proof: run `grep -rn "12\|108\|320\|510" backend/internal/repository/member_profile_role_volume_repository.go backend/internal/repository/member_profile_progress_repository.go backend/internal/repository/member_profile_dashboard_repository.go backend/internal/services/badge_service.go` and confirm none of these numbers appear as a bare literal outside of the `badges.` package import/comment context (they will legitimately appear in doc comments describing what the registry does -- that is fine; the point is no executable literal remains). Also `grep -rn "badges\." backend/internal/repository backend/internal/services` to show every consuming call site.

    SC-6 propagation proof (extended per D-28 -- must cover BOTH a single progress row AND a
    rendered stage ladder, not only one or the other): temporarily change one registry constant in
    `backend/internal/badges/thresholds.go` (e.g. bump role_volume's bronze threshold from 12 to
    13), rebuild the backend, and:
    (a) Single progress row: hit the live public profile API for a member sitting at exactly 12
    (or seed one) and confirm the rendered role badge's `current_count`/`current_tier`/`rankLabel`
    changes accordingly (member drops from "bronze" back to "entry" at count 12, since bronze now
    requires 13) with ZERO TypeScript file touched.
    (b) Stage ladder (new per D-28): for the SAME member/role, fetch the rendered public-profile
    HTML (`curl http://192.168.235.196:3000/members/<slug>` or the SSH-tunnel path) before and after
    the bump, and grep the role badge card's medal-ladder markup (`data-role-stage-state`,
    `data-role-stage="bronze"`, the visible `12+`/`13+` threshold text inside
    `roleBadgeCardStagesStyles.roleStageThreshold`) to confirm: the "bronze" stage's rendered
    threshold text changes from `12+` to `13+`, AND the stage's `data-role-stage-state` flips from
    `current`/`reached` to `locked` for a member sitting at exactly 12 -- proving the CHANGE reaches
    the multi-stage ladder, not only the single progress-row summary. If member `type`'s own
    typesetter role does not sit near this exact boundary, either seed a throwaway test member at
    count 12 for this demo or pick the closest real member with an active role at a tier boundary
    and adjust the bumped constant/expected before/after values accordingly -- document whichever
    approach was used in the SUMMARY.
    Revert the constant change immediately afterward and rebuild again -- do not leave this
    temporary edit committed.
  </action>
  <verify>
    <automated>echo "Manual regression-gate run -- see task action for exact commands; record pass/fail per gate in the SUMMARY"</automated>
  </verify>
  <done>All six regression gates pass (or any pre-existing failure is explicitly matched against the documented baseline count); the SC-1 grep proof is captured verbatim; the SC-6 propagation demo is captured verbatim for BOTH the single progress row AND the rendered stage ladder (D-28), including the before/after ladder markup snippets.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live UAT sign-off at member `type` and the dashboard, on :3000</name>
  <files></files>
  <action>Present Task 1's automated regression-gate results, SC-1 grep proof, and SC-6 propagation demo (both the single progress row and the stage-ladder proof), then walk through the seven live checks below via the 127.0.0.1:3300 tunnel (or 192.168.235.196:3000 directly on team4s-linux) and wait for explicit human sign-off before this phase is considered complete.</action>
  <what-built>
    A complete badge-threshold single-source-of-truth refactor: one Go registry, six repointed
    backend call sites, four closed contract gaps plus a server-supplied per-family stage-list field
    (Context Revision D-24), one fixed duplicate-emission defect, and a frontend rollback of the
    four originally-named computation sites PLUS the badge-family/role-volume stage ladders (D-25)
    -- all automated tests green (Task 1).
  </what-built>
  <how-to-verify>
    1. Ensure the dev stack is running (`docker compose ps` on team4s-linux) and rebuilt with this
       phase's changes.
    2. Visit `http://127.0.0.1:3300/members/type` (SSH-tunnel path) or
       `http://192.168.235.196:3000/members/type` directly on the Linux host.
    3. Confirm the role badge for the typesetter role shows exactly: current_count 13, tier
       Bronze, "Noch 95 Mitwirkungen bis Silber" (or the equivalent rendered copy) -- matching
       Plan 150-01's captured `before-member-type-profile.txt` byte-for-byte in this section.
    4. Confirm the typesetter role's medal LADDER (the "Medaillen für Typesetting" stage strip)
       still shows all five stages (Einstieg/Bronze/Silber/Gold/Platin) with the SAME earned/
       current/locked states and the SAME `12+`/`108+`/`320+`/`510+` threshold labels as before this
       phase's changes -- this is the new D-28 check confirming the ladder itself (not just the
       single progress line) survived the data-source swap unchanged.
    5. Log in as the member used for the dashboard evidence capture (Plan 150-01's SUMMARY records
       which one) and visit `/me/dashboard`. Confirm the points row shows exactly: current_count
       39, next_threshold 50, remaining_count 11 (per ROADMAP.md's named regression gate and
       150-UI-SPEC.md's D-14 requirement) -- not just the role-volume row.
    6. If this member has any role still at entry tier (count 1-11), note its displayed count: per
       Plan 150-04's documented correction, this number may now differ from "1" if it was
       previously always hardcoded to 1 by the frontend's old fallback -- confirm this is the
       CORRECT real count, not a regression, by cross-checking against the database
       (`release_role_credit_lifecycles` for that member/role).
    7. Diff the freshly captured after-state against `evidence/before-member-type-profile.txt` and
       `evidence/before-dashboard.json` (reuse the same capture script/curl command from Plan
       150-01 Task 1) and confirm zero unexpected differences, including in the ladder text captured
       by `document.body.innerText` (stage labels and threshold numbers are visible text nodes, so a
       clean innerText diff already covers the ladder; check 4 above is the targeted, named
       confirmation of that same fact).
  </how-to-verify>
  <resume-signal>Type "approved" once all seven checks pass, or describe exactly which number/behavior differs from expectation.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | This plan runs verification only; it changes no production code except a temporary, reverted registry-constant edit used solely to demonstrate SC-6. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-07 | Tampering | temporary SC-6 propagation demo edit to thresholds.go | mitigate | The task explicitly requires reverting the edit and rebuilding before sign-off; the checkpoint's human reviewer confirms the final running system matches the committed source, not the temporary demo state. |
| T-150-SC | Supply chain | package installs | accept | No new dependency introduced. |
</threat_model>

<verification>
All six ROADMAP.md Regression-Gates pass; SC-1/SC-6 structural proofs captured (SC-6 covering both
the single progress row and a rendered stage ladder, per D-28); human sign-off recorded on the exact
named numbers for both role-volume/its ladder and points progress at member `type` / the dashboard
member.
</verification>

<success_criteria>
- Full automated regression-gate suite green with no new failures vs. the documented baseline.
- SC-1 (single registry, searchable) and SC-6 (threshold change propagates without a TS edit, to
  BOTH a single progress row and a multi-stage ladder) are demonstrated, not merely asserted.
- A human confirms live, real-data parity for both the public-profile role-volume number/ladder and
  the dashboard points-progress number named in ROADMAP.md and 150-UI-SPEC.md's D-14.
</success_criteria>

<output>
Create `.planning/phases/150-badge-regeln-eine-autoritative-schwellenquelle/150-06-SUMMARY.md` when done, and update `.planning/ROADMAP.md`'s Phase 150 progress row plus `.planning/STATE.md` to reflect phase completion once the human approves.
</output>
</output>
