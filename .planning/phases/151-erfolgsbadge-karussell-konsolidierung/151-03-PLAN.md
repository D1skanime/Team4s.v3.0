---
phase: 151-erfolgsbadge-karussell-konsolidierung
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/FocalCarouselInternals.tsx
  - frontend/src/components/ui/FocalCarousel.tsx
  - frontend/src/components/ui/FocalCarousel.module.css
  - frontend/src/components/ui/FocalCarousel.test.tsx
autonomous: true
requirements: [P151-05, P151-09]

must_haves:
  truths:
    - "Manual horizontal scrolling updates the clear active item from live geometry without waiting for a stale 120ms state"
    - "Pointer/wheel interruption and rapid navigation cancel or retarget an in-flight animation without snap competition or stale active state"
    - "Only direct carousel-owned items participate in geometry, inert, active, and navigation calculations; nested carousel items remain owned by their nested track"
    - "Adjacent motion remains 210ms, free-scroll settle remains 120ms, and non-adjacent/reduced-motion navigation settles immediately"
    - "All items stay mounted/inert as today, while a 100/200-item stress fixture avoids unchanged full-subtree rerenders and performs no requests"
  artifacts:
    - path: "frontend/src/components/ui/FocalCarousel.tsx"
      provides: "Measured minimal interaction fixes and memoized direct-item rendering"
      exports: ["FocalCarousel", "FocalCarouselItemState"]
    - path: "frontend/src/components/ui/FocalCarousel.module.css"
      provides: "Existing generic snap/animation/reduced-motion contract without profile overrides"
    - path: "frontend/src/components/ui/FocalCarousel.test.tsx"
      provides: "Regression matrix for manual/interrupted/rapid/nested/reduced-motion/stress behavior"
  key_links:
    - from: "frontend/src/components/ui/FocalCarousel.tsx"
      to: "trackRef.current.children"
      via: "direct-item-only geometry helper"
      pattern: "data-focal-carousel-item"
    - from: "frontend/src/components/ui/FocalCarousel.tsx"
      to: "frontend/src/components/ui/FocalCarousel.module.css"
      via: "isNavigating class disables snap only during the 210ms programmatic move"
      pattern: "isNavigating"
---

<objective>
Validate and narrowly harden the canonical generic carousel without replacing its proven engine.

Purpose: D05 requires a continuously clear active card under manual, interrupted, rapid, nested, keyboard, pointer, wheel, and reduced-motion use while preserving accessibility and full mount.
Output: focused regression tests plus only the minimal generic TSX/CSS fixes those tests demonstrate.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md

No executor may stage/commit. Do not edit any profile component/CSS or `badgeArtwork.ts`; Plans 01/02 own those same-wave files.
</execution_context>

<context>
<read_first>
- .planning/phases/151-erfolgsbadge-karussell-konsolidierung/151-CONTEXT.md
- .planning/phases/151-erfolgsbadge-karussell-konsolidierung/151-RESEARCH.md
- .planning/phases/151-erfolgsbadge-karussell-konsolidierung/151-PATTERNS.md
- .planning/phases/151-erfolgsbadge-karussell-konsolidierung/151-UI-SPEC.md
- .planning/phases/151-erfolgsbadge-karussell-konsolidierung/151-VALIDATION.md
- docs/engineering/implementation-contract.md
- docs/frontend/ui-system.md
- docs/agent-guidelines-ui.md
- frontend/src/components/ui/FocalCarousel.tsx
- frontend/src/components/ui/FocalCarousel.module.css
- frontend/src/components/ui/FocalCarousel.test.tsx
- frontend/src/hooks/useNearViewportActivation.ts
</read_first>

<interfaces>
Preserve the exported generic `FocalCarousel<T>` props and `FocalCarouselItemState` shape exactly.
Preserve `NAVIGATION_DURATION_MS = 210`; retain the current 120ms free-scroll settle duration.
Any direct-item helper stays private and generic. No badge/profile/domain prop or CSS selector may enter `components/ui`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 151-03-01: Lock concrete carousel defects with focused RED regressions</name>
  <files>frontend/src/components/ui/FocalCarousel.test.tsx</files>
  <behavior>
    - Dispatching manual scroll through multiple item centers changes aria/current active state during motion and settles to the nearest direct item after 120ms.
    - Pointer-down or horizontal wheel during a 210ms adjacent animation cancels stale animation state, preserves live scroll position, and settles once.
    - A rapid next/next/previous sequence retargets from current geometry and ends on the requested item without competing timers.
    - Nested `[data-focal-carousel-item]` descendants never affect parent nearest-item, active, inert, or count behavior.
    - Reduced motion and non-adjacent Home/End/direct navigation are immediate; vertical wheel and vertical pointer intent remain page-owned.
    - With 100 then 200 mounted items, navigation invokes renderItem only for state-changing items within a strict bounded count; no fetch/XMLHttpRequest occurs.
  </behavior>
  <action>
    Extend the existing geometry/matchMedia/fake-timer harness; do not replace it with a second test engine.
    First run the current suite and retain every green mounted/inert/axe/keyboard/pointer/wheel assertion.
    Add deterministic tests for the six behaviors above, including direct child plus nested carousel fixtures and
    render spies for 100/200 items. Record which new tests fail against current code in the SUMMARY so production
    edits are evidence-driven. Do not weaken existing assertions to make current behavior pass.
  </action>
  <verify><automated>docker compose exec -T team4sv30-frontend npx vitest run src/components/ui/FocalCarousel.test.tsx --reporter=dot</automated></verify>
  <done>New tests precisely reproduce any current defects and protect every already-proven interaction/a11y invariant; failures identify source behavior rather than CSS snapshots alone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 151-03-02: Apply minimal direct-item, interruption, and render-containment fixes</name>
  <files>frontend/src/components/ui/FocalCarousel.tsx, frontend/src/components/ui/FocalCarousel.module.css, frontend/src/components/ui/FocalCarousel.test.tsx</files>
  <behavior>
    - The RED matrix from Task 151-03-01 passes without changing public props, timings, mounted/inert behavior, or a11y.
    - Unchanged item subtrees do not rerender on active-index updates; previous/current items still receive exact state.
  </behavior>
  <action>
    Add one private direct-item enumerator based on `trackRef.current.children` (never descendant-wide querySelectorAll)
    and reuse it in nearest geometry, active/inert, animation, and settle paths. During user scroll, update the
    nearest active index on a single scheduled animation frame while retaining the 120ms final settle. Centralize
    cancellation of prior RAF/timer/navigation state and call it on pointer/wheel interruption and rapid retarget.
    Keep snap disabled only during the existing 210ms adjacent programmatic animation; non-adjacent and reduced
    motion remain immediate. Introduce a private memoized direct-item renderer with stable primitive props and a
    stable `showAll` callback so internal active changes rerender only changed items. Preserve full DOM mount,
    `inert`, expand/collapse focus restoration, counter labels, lazy interaction activation, and endpoint pass-through.
    Change CSS only if a failing test proves the existing generic snap/reduced-motion class needs a focused fix.
    No new engine, virtualization, request/cache logic, domain selectors, or broad rewrite is authorized.
  </action>
  <verify><automated>docker compose exec -T team4sv30-frontend npx vitest run src/components/ui/FocalCarousel.test.tsx --reporter=dot</automated></verify>
  <done>All old and new tests/axe checks pass; 210/120 timings remain; nested ownership and interruption are deterministic; 100/200-item render/request budgets pass.</done>
</task>

</tasks>

<threat_model>
| Threat | Disposition | Mitigation |
|---|---|---|
| Event-driven denial/jank | mitigate | One RAF for live scroll, centralized cancellation, bounded render-count stress at 100/200 items. |
| Focus/inert state confusion | mitigate | Direct-child ownership plus existing axe, keyboard, focus restoration, mounted/inert regressions. |
| Motion accessibility regression | mitigate | Reduced-motion immediate path remains tested and bypasses smooth animation. |
| Nested component state tampering | mitigate | Parent reads only direct owned children; descendants cannot affect parent indexing/count. |
</threat_model>

<verification>
Focused Vitest/axe suite passes repeatedly with fake timers drained; scoped ESLint on the three owned files and `git diff --check` pass; no profile CSS changes exist in this plan.
</verification>

<success_criteria>
The current engine is measurably hardened at its concrete failure seams, not rewritten; every input mode ends with one clear active card, stable neighbors, bounded rendering, full mount, inert, and preserved accessibility.
</success_criteria>

<output>
Create `151-03-SUMMARY.md` separating pre-existing green behavior, reproduced defects, minimal fixes, stress counts, and commands. Do not commit.
</output>
