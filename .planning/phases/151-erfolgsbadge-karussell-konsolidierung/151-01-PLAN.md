---
phase: 151-erfolgsbadge-karussell-konsolidierung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/frontend/member-achievement-artwork.md
  - DECISIONS.md
  - frontend/src/components/profile/badgeArtwork.ts
  - frontend/src/components/profile/badgeArtwork.test.ts
  - frontend/public/member-achievement-badges/role_entry_karaoke_fx.png
  - frontend/public/member-achievement-badges/role-karaoke_fx-motif.png
  - frontend/public/member-achievement-badges/rank-frame-karaoke_fx-bronze.png
  - frontend/public/member-achievement-badges/rank-frame-karaoke_fx-silver.png
  - frontend/public/member-achievement-badges/rank-frame-karaoke_fx-gold.png
  - frontend/public/member-achievement-badges/rank-frame-karaoke_fx-platinum.png
autonomous: true
requirements: [P151-02, P151-06, P151-07, P151-09]

must_haves:
  truths:
    - "Karaoke-FX resolves Entry plus Bronze/Silber/Gold/Platin through the ordinary role artwork path, with no icon-key or JSX special case"
    - "Every current anime_contribution catalog role has an explicit presentation strategy and complete shipped files; Timer is the sole direct-volume strategy"
    - "Coverage is measured against the actual canonical catalog boundary and actual Linux filesystem, not against the artwork manifest itself or a second handwritten role list"
    - "All 107 pre-existing PNGs remain byte-identical and the six new transparent PNGs match the existing role-art visual language"
  artifacts:
    - path: "frontend/src/components/profile/badgeArtwork.ts"
      provides: "Role-code presentation manifest, resolvers, and pure coverage validator"
      exports: ["resolveBadgeArtwork", "resolveLayeredRoleArtwork", "validateRoleArtworkCoverage", "roleArtworkStrategyFor"]
    - path: "frontend/src/components/profile/badgeArtwork.test.ts"
      provides: "Resolver, live-catalog, real-filesystem, and negative completeness proofs"
    - path: "frontend/public/member-achievement-badges/role_entry_karaoke_fx.png"
      provides: "Karaoke-FX Entry artwork"
    - path: "frontend/public/member-achievement-badges/role-karaoke_fx-motif.png"
      provides: "Reusable Karaoke-FX volume motif"
  key_links:
    - from: "frontend/src/components/profile/badgeArtwork.test.ts"
      to: "GET /api/v1/role-definitions?context=anime_contribution"
      via: "Compose-only live catalog assertion using API_INTERNAL_URL"
      pattern: "anime_contribution"
    - from: "frontend/src/components/profile/badgeArtwork.ts"
      to: "frontend/public/member-achievement-badges"
      via: "manifest-approved deterministic filenames after role-code membership"
      pattern: "roleArtworkStrategyFor"
---

<objective>
Complete the artwork/resolver contract and add Karaoke-FX art without introducing role or threshold business truth.

Purpose: D02/D06/D07 require a catalog-driven completeness gate and ordinary Karaoke rendering while preserving Phase-150 authority and every existing asset byte.
Output: one role-code presentation manifest, compatible public resolvers, exhaustive tests, and six new Karaoke PNGs.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md

No executor may commit or stage files. The coordinator owns sequential commits after Wave 1 merges.
</execution_context>

<context>
<read_first>
- .planning/phases/151-erfolgsbadge-karussell-konsolidierung/151-CONTEXT.md
- .planning/phases/151-erfolgsbadge-karussell-konsolidierung/151-RESEARCH.md
- .planning/phases/151-erfolgsbadge-karussell-konsolidierung/151-PATTERNS.md
- .planning/phases/151-erfolgsbadge-karussell-konsolidierung/151-UI-SPEC.md
- .planning/phases/151-erfolgsbadge-karussell-konsolidierung/151-SOURCE-ARTWORK-REVIEW.md
- docs/engineering/implementation-contract.md
- frontend/src/components/profile/badgeArtwork.ts
- frontend/src/components/profile/badgeArtwork.test.ts
- frontend/src/lib/roleCatalog.ts
- frontend/src/providers/RoleCatalogProvider.tsx
- frontend/src/components/ui/ResponsiveImage.tsx
</read_first>

<interfaces>
Keep these signatures callable so Plan 02 can consume the module concurrently:
`resolveBadgeArtwork(badgeCode: string, roleIconKey?: string): string | undefined`
`resolveLayeredRoleArtwork(badgeCode: string, roleIconKey?: string): LayeredRoleArtwork | undefined`
The optional second argument remains source-compatible but MUST NOT gate role artwork.

Add `roleArtworkStrategyFor(roleCode)` returning `layered | direct-volume | undefined`, and a pure
`validateRoleArtworkCoverage(catalogRows, availablePublicPaths)` result that names missing roles,
missing files, and declared exceptions. Exceptions are presentation-only and initially empty.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 151-01-01: Generate and validate the six Karaoke-FX transparent assets</name>
  <files>frontend/public/member-achievement-badges/role_entry_karaoke_fx.png, frontend/public/member-achievement-badges/role-karaoke_fx-motif.png, frontend/public/member-achievement-badges/rank-frame-karaoke_fx-bronze.png, frontend/public/member-achievement-badges/rank-frame-karaoke_fx-silver.png, frontend/public/member-achievement-badges/rank-frame-karaoke_fx-gold.png, frontend/public/member-achievement-badges/rank-frame-karaoke_fx-platinum.png</files>
  <action>
    Per D01/D03/D06, use only the existing Linux built-in `image_gen.imagegen` with
    `referenced_image_paths` pointing at representative shipped role Entry, motif, and four role-frame PNGs.
    Generate one transparent Entry, one transparent Karaoke motif, and Bronze/Silber/Gold/Platin transparent
    frames in the existing visual language and exact filenames above. Preserve alpha, square high-resolution
    source geometry, readable silhouette, and frame hierarchy. Do not install/configure a tool, use a CLI/API
    fallback, generate on Windows, resize/recompress, or edit any of the original 107 files. Inspect each result
    against its local references before accepting it; regenerate only the new file when a mismatch is found.
  </action>
  <verify>
    <automated>test "$(find frontend/public/member-achievement-badges -maxdepth 1 -type f -name '*.png' | wc -l)" -eq 113 &amp;&amp; file frontend/public/member-achievement-badges/{role_entry_karaoke_fx,role-karaoke_fx-motif,rank-frame-karaoke_fx-bronze,rank-frame-karaoke_fx-silver,rank-frame-karaoke_fx-gold,rank-frame-karaoke_fx-platinum}.png | grep -c 'RGBA' | grep -qx '6' &amp;&amp; python3 .planning/phases/151-erfolgsbadge-karussell-konsolidierung/checks/check-artwork-sources.py</automated>
  </verify>
  <done>Exactly six new RGBA PNGs exist at the deterministic paths, all original source hashes remain unchanged, and `git diff --name-only -- frontend/public/member-achievement-badges` lists no pre-existing file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 151-01-02: Replace icon-key gating with a catalog-checkable role artwork contract</name>
  <files>frontend/src/components/profile/badgeArtwork.ts, frontend/src/components/profile/badgeArtwork.test.ts</files>
  <behavior>
    - Karaoke-FX and the other catalog roles resolve Entry and four volume codes without consulting icon_key.
    - Timer explicitly resolves four complete direct-volume images; every other role volume uses motif plus rank frame.
    - A live catalog role absent from the presentation manifest is reported even if every manifest row is complete.
    - Removing one expected path from the real filesystem set reports that exact missing file; unknown/speculative codes remain unresolved.
  </behavior>
  <action>
    Per D02/D06/D07, replace `ROLE_ARTWORK_BY_ICON_KEY` with a presentation-only role-code manifest.
    Store strategy only—no labels, contexts, sort order, permissions, assignability, thresholds, or tier numbers.
    Derive the established Entry/motif/frame names only after manifest membership; declare Timer's
    `direct-volume` strategy directly. Preserve all approved non-role maps and both public resolver signatures.
    Add the pure coverage helper described above. In the test, build the positive available-path set with
    `node:fs` from the real Linux public directory and obtain the positive role boundary from the running public
    role-catalog endpoint (`API_INTERNAL_URL`, Compose required). Do not use a second 12-role fixture or infer
    required roles by enumerating the artwork manifest. Add explicit negative proofs by appending one synthetic
    live-catalog row and by removing one real expected file path from a copied path set.
  </action>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend npx vitest run src/components/profile/badgeArtwork.test.ts --reporter=dot</automated>
  </verify>
  <done>The resolver API stays source-compatible, icon_key no longer selects art, Timer is the only direct-volume exception, live catalog coverage is complete, and both missing-role and missing-file tests fail closed.</done>
</task>

</tasks>

<task_order_clarification>
Task01 creates and visually reviews six new assets, with a file/RGBA/original-hash-only gate.
Task02 then reproduces resolver/coverage failures against those real files and implements the manifest;
the final complete live catalog plus actual-filesystem suite must be GREEN. Never mock or skip missing
files. Record actual image-generation prompts, Linux reference/output paths and review decisions.
</task_order_clarification>

<threat_model>
| Threat | Disposition | Mitigation |
|---|---|---|
| Catalog spoofing/parallel registry | mitigate | Coverage input comes from the live bounded public catalog; the manifest stores presentation strategy only. |
| Path traversal/speculative probing | mitigate | Resolvers accept only manifest role codes and fixed filename grammar; no runtime file probing or user path joins. |
| Source-art tampering | mitigate | Only six explicit new paths may change; Plan 05 re-hashes all 107 originals. |
| Supply-chain/tool drift | mitigate | Existing built-in image generation only; no dependency, config, CLI, or runtime change. |
</threat_model>

<verification>
Focused resolver test passes inside Compose; six RGBA files exist; no original artwork path is modified; `git diff --check` is clean for owned files.
</verification>

<success_criteria>
All 12 current catalog roles resolve five stages, Karaoke uses the normal layered strategy, Timer's direct strategy is declared, negative completeness proofs work, and no frontend business/threshold registry is introduced.
</success_criteria>

<output>
Create `151-01-SUMMARY.md` with the manifest contract, generated filenames, commands/results, and any regenerated new asset. Do not commit.
</output>
