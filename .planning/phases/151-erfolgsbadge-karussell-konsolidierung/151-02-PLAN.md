---
phase: 151-erfolgsbadge-karussell-konsolidierung
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/profile/AchievementArtwork.tsx
  - frontend/src/components/profile/AchievementArtwork.module.css
  - frontend/src/components/profile/AchievementArtwork.test.tsx
  - frontend/src/components/profile/RoleAchievementCard.tsx
  - frontend/src/components/profile/AchievementStages.tsx
  - frontend/src/components/profile/AnimeProjectAchievementStage.tsx
  - frontend/src/components/profile/ContributionAchievementStage.tsx
  - frontend/src/components/profile/MembershipStage.tsx
  - frontend/src/components/profile/PointsAchievementStage.tsx
  - frontend/src/components/profile/achievementStageHelpers.tsx
  - frontend/src/components/profile/MemberBadgeChain.tsx
  - frontend/src/components/profile/MemberBadgeChain.test.tsx
  - frontend/src/components/profile/BadgeFamilyCard.module.css
  - frontend/src/components/profile/LayeredBadgeArtwork.module.css
autonomous: true
requirements: [P151-03, P151-04, P151-05, P151-09]

must_haves:
  truths:
    - "Every primary role/progress/points/membership/contribution artwork uses one centered contain slot: hero 192/216/240, marker 64/80, always 8px inset"
    - "The 562px and 658px container transitions include card borders, and portrait/intrinsic image dimensions cannot enlarge a minmax(0,1fr) grid track"
    - "Active, inactive, locked, and preview cards reserve equal artwork and text geometry; emphasis never scales/translates cards or hides copy in a height-changing way"
    - "The unreachable FamilyCollectionCard inner carousel is removed, while productive stage semantics, typography, full mount, and progress data remain unchanged"
    - "Plan 05 can render production artwork and role-card composition through stable exported component props"
  artifacts:
    - {path: "frontend/src/components/profile/AchievementArtwork.tsx", provides: "Shared direct/layered ResponsiveImage renderer with hero/stage contract", exports: ["AchievementArtwork", "AchievementArtworkDescriptor", "AchievementArtworkProps"]}
    - {path: "frontend/src/components/profile/AchievementArtwork.module.css", provides: "Sole hero/marker geometry and layer containment owner"}
    - {path: "frontend/src/components/profile/RoleAchievementCard.tsx", provides: "Memoizable productive role-card composition for chain/gallery", exports: ["RoleAchievementCard", "RoleAchievementCardProps"]}
    - {path: "frontend/src/components/profile/AchievementStages.tsx", provides: "Existing family stage renderers extracted without behavior changes", exports: ["AnimeProjectAchievementStage", "ContributionAchievementStage", "MembershipStage", "PointsAchievementStage"]}
    - {path: "frontend/src/components/profile/MemberBadgeChain.tsx", provides: "Catalog/family orchestration with indexed lookup and explicit preview reset"}
  key_links:
    - {from: "frontend/src/components/profile/MemberBadgeChain.tsx", to: "frontend/src/components/profile/RoleAchievementCard.tsx", via: "FocalCarousel.renderItem passes catalog/server presentation", pattern: "RoleAchievementCard"}
    - {from: "frontend/src/components/profile/RoleAchievementCard.tsx", to: "frontend/src/components/profile/AchievementArtwork.tsx", via: "normalized direct/layered descriptor", pattern: "AchievementArtwork"}
---

<objective>
Consolidate shared achievement rendering, card/stage composition, and responsive CSS without touching the generic carousel engine.

Purpose: D03/D04/D05 require one bounded container-owned slot and stable card geometry; removing dead nested scrolling and isolating the role card reduces duplicate rendering without redesigning the product.
Output: shared artwork component/tests, focused memoized role card, existing stage renderers in AchievementStages, and simplified chain. Plan04 subsequently removes the family CSS conflicts.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md

If the chain remains above the repository 450-line guide after removing dead code and extracting the role card, move only the existing family stage renderers to AchievementStages.tsx within this ownership. Do not invent a parallel component or change their behavior.

Plan04 exclusively owns the ten family-layout CSS files removed from this plan frontmatter. Keep existing class names stable and leave those CSS files to Plan04. AchievementArtwork CSS, obsolete LayeredBadgeArtwork CSS and dead BadgeFamilyCard CSS remain owned here.

No executor may stage/commit. Do not edit `badgeArtwork.ts` or any `FocalCarousel*` file; Plans 01 and 03 own them in the same wave.
</execution_context>

<context>
<read_first>
- .planning/phases/151-erfolgsbadge-karussell-konsolidierung/151-CONTEXT.md
- .planning/phases/151-erfolgsbadge-karussell-konsolidierung/151-RESEARCH.md
- .planning/phases/151-erfolgsbadge-karussell-konsolidierung/151-PATTERNS.md
- .planning/phases/151-erfolgsbadge-karussell-konsolidierung/151-UI-SPEC.md
- .planning/phases/151-erfolgsbadge-karussell-konsolidierung/151-PROTOTYPE.html
- docs/engineering/implementation-contract.md
- docs/frontend/ui-system.md
- docs/agent-guidelines-ui.md
- frontend/src/components/profile/MemberBadgeChain.tsx
- frontend/src/components/profile/MemberBadgeChain.test.tsx
- frontend/src/components/profile/memberBadgeFamilies.ts
- frontend/src/components/profile/memberBadgeLabels.ts
- frontend/src/components/profile/badgeArtwork.ts
- frontend/src/components/ui/ResponsiveImage.tsx
- frontend/src/components/ui/FocalCarousel.tsx
- all CSS modules listed in this plan's files_modified
</read_first>

<interfaces>
Consume the existing Plan-01-compatible resolver signatures; no Karaoke branch is allowed here.
Define `AchievementArtworkDescriptor` as `{kind:'direct';src:string}` or
`{kind:'layered';motifSrc:string;frameSrc:string}`. `AchievementArtworkProps` includes descriptor,
badgeCode, alt, `size:'hero'|'stage'`, decorative/priority flags, and an optional className only;
the consumer cannot set dimensions. `RoleAchievementCardProps` takes roleCode, roleLabel, colorKey,
count, catalog items, `RoleProgressPresentation`, and `FocalCarouselItemState`; it owns no catalog fetch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 151-02-01: Create the shared direct/layered artwork slot</name>
  <files>frontend/src/components/profile/AchievementArtwork.tsx, frontend/src/components/profile/AchievementArtwork.module.css, frontend/src/components/profile/AchievementArtwork.test.tsx, frontend/src/components/profile/LayeredBadgeArtwork.module.css</files>
  <behavior>
    - Direct and layered descriptors render through ResponsiveImage with identical outer/content boxes and useful/decorative alt behavior.
    - Hero sizes advertise 192/216/240px and marker sizes 64/80px; no consumer-supplied width can override them.
    - Explicit minmax tracks, zero intrinsic image minima, max-inline/block-size 100%, and object-fit contain bound square and 2:3 portrait sources inside the 8px inset.
  </behavior>
  <action>
    Extract the repeated ResponsiveImage and four-layer role/progress markup into the profile-domain
    `AchievementArtwork`. Reuse existing ResponsiveImage retry/delivery and layered mist/backdrop mechanics;
    move only the mechanics actually consumed, then delete `LayeredBadgeArtwork.module.css` if empty.
    The component owns `data-achievement-art`, dimensions, accurate `sizes`, alt/decorative behavior, and
    layer positioning. It introduces no labels, tier logic, state colors, fetches, or generic UI variant.
  </action>
  <verify><automated>docker compose exec -T team4sv30-frontend npx vitest run src/components/profile/AchievementArtwork.test.tsx src/components/ui/ResponsiveImage.test.tsx --reporter=dot</automated></verify>
  <done>One tested component owns all direct/layered slot geometry, and the corrected portrait containment contract is locked structurally.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 151-02-02: Migrate productive cards/stages and remove dead rendering work</name>
  <files>frontend/src/components/profile/AchievementStages.tsx, frontend/src/components/profile/RoleAchievementCard.tsx, frontend/src/components/profile/MemberBadgeChain.tsx, frontend/src/components/profile/MemberBadgeChain.test.tsx, frontend/src/components/profile/BadgeFamilyCard.module.css</files>
  <behavior>
    - MemberBadgeChain uses AchievementArtwork for all earned primary and stage art while preserving locked text/progress semantics and current German copy.
    - RoleAchievementCard is memoized; one-step active changes rerender only cards whose item state changed while every card stays mounted.
    - Family preview resets when server metric/current stage changes without count-derived remount keys.
    - Earned badge lookup is one pre-indexed Map; FamilyCollectionCard and its 140ms observer/wheel/settle engine are absent.
  </behavior>
  <action>
    Extract the existing AnimeProjectAchievementStage, ContributionAchievementStage, MembershipStage and
    PointsAchievementStage renderers into AchievementStages.tsx if required to leave MemberBadgeChain and
    the extracted production modules at or below450 lines. Preserve semantics and export the existing named
    renderers. Keep their behavioral coverage in MemberBadgeChain.test.tsx.

    Extract only the role card into the stable interface above and memoize it; keep family mapping in
    `memberBadgeFamilies.ts` and orchestration in MemberBadgeChain. Replace repeated `earnedBadges.find`
    with a per-render Map. Replace remount keys with focused effects that reset local preview state from
    `family.currentCount/currentStage`. Remove unreachable FamilyCollectionCard, its event engine, exclusive
    CSS module, and stale tests. Do not split every local function, virtualize cards, change copy/heading levels,
    hide inactive text, or modify the own-profile `AchievementBadgesCard.tsx` visibility manager.
  </action>
  <verify><automated>docker compose exec -T team4sv30-frontend npx vitest run src/components/profile/MemberBadgeChain.test.tsx src/components/profile/memberBadgeLabels.test.ts --reporter=dot</automated></verify>
  <done>Chain/stage/role production modules satisfy the450-line guide, with existing family behavior covered. Productive compositions use the shared slot, full mount/inert remains, dead nested scrolling is gone, preview reset is explicit, and render-count/height-preservation regressions pass.</done>
</task>


</tasks>

<threat_model>
| Threat | Disposition | Mitigation |
|---|---|---|
| Intrinsic-image layout denial/overflow | mitigate | minmax tracks, zero minima, bounded images, and Plan 05 image-inside-slot assertions. |
| Accessibility regression from hidden/inactive content | mitigate | Preserve full mount/inert and text space; retain focus, progress, locked text, and alt semantics. |
| Cross-plan CSS/interaction drift | mitigate | This plan never edits/selects FocalCarousel internals; Plan 03 exclusively owns the engine. |
| API/auth/domain drift | accept | No API, protected UI, auth, backend, schema, or persisted-data control changes are in scope. |
</threat_model>

<verification>
Focused component tests and scoped ESLint pass; shared artwork geometry is contained; family CSS cleanup is owned by Plan04; `git diff --check` is clean for owned files.
</verification>

<success_criteria>
The static 6/6 UI review and corrected 320/390/1440 plus 561/562/657/658 prototype behavior are encoded in production components without typography or carousel reinterpretation.
</success_criteria>

<output>
Create `151-02-SUMMARY.md` listing extracted/deleted modules, stable exports, focused results, and any intentional retained family CSS. Do not commit.
</output>
