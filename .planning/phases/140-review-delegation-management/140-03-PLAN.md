---
phase: 140-review-delegation-management
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/admin-review-delegation.ts
  - frontend/src/lib/api.ts
  - frontend/src/app/admin/users/tabs/ReviewDelegationSection.tsx
  - frontend/src/app/admin/users/tabs/ReviewDelegationSection.test.tsx
  - frontend/src/app/admin/users/tabs/GroupSection.tsx
  - frontend/src/app/admin/users/tabs/CapabilityDetailRow.tsx
  - frontend/src/app/admin/users/tabs/CapabilityDetailRow.test.tsx
  - frontend/src/app/admin/users/tabs/userGroupRightsHelpers.ts
autonomous: true
requirements: [RDEL-01, RDEL-02, RDEL-03]

must_haves:
  truths:
    - "RDEL-01: the new 'Prüf-/Freigabe-Rechte' section fetches and renders a target member's current media/image, note/text, and contribution review delegations, including eligibility-aware disabled state, without waiting for a 422 to explain ineligibility"
    - "RDEL-02: an admin can independently grant or revoke each of the three delegable review rights via a Switch toggle, optimistic-update on click, with inline error + revert on mutation failure"
    - "RDEL-03: the new section is visibly and technically distinct from the generic effective-rights Accordion -- its own SectionHeader, Switch-based controls, no shared component tree with GuidedGrantFlow/GuidedRevokeFlow -- and the grant affordance for the three review actions is removed from the generic override UI (CapabilityDetailRow), replaced by an explanatory note + jump-link, while the deny/revoke affordance for those same actions remains completely untouched"
  artifacts:
    - path: "frontend/src/types/admin-review-delegation.ts"
      provides: "ReviewDelegationRow / ReviewDelegationMutationRequest / ReviewDelegationMutationResult TS types"
    - path: "frontend/src/app/admin/users/tabs/ReviewDelegationSection.tsx"
      provides: "the RDEL-03 dedicated 'Prüf-/Freigabe-Rechte' section, id=review-delegation-section"
    - path: "frontend/src/app/admin/users/tabs/CapabilityDetailRow.tsx"
      provides: "grant-affordance removal for the 3 review actions (Option d asymmetric separation), deny/revoke unaffected"
  key_links:
    - from: "frontend/src/app/admin/users/tabs/ReviewDelegationSection.tsx"
      to: "frontend/src/lib/api.ts (getReviewDelegations/mutateReviewDelegation)"
      via: "fetch on mount + Switch onCheckedChange"
      pattern: "getReviewDelegations|mutateReviewDelegation"
    - from: "frontend/src/app/admin/users/tabs/CapabilityDetailRow.tsx"
      to: "frontend/src/app/admin/users/tabs/ReviewDelegationSection.tsx"
      via: "document.getElementById('review-delegation-section')?.scrollIntoView(...) jump-link"
      pattern: "review-delegation-section"
    - from: "frontend/src/app/admin/users/tabs/GroupSection.tsx"
      to: "frontend/src/app/admin/users/tabs/ReviewDelegationSection.tsx"
      via: "sibling mount directly after GroupRolesSection, before the generic Accordion"
      pattern: "ReviewDelegationSection"
---

<objective>
Build the frontend half of Review Delegation Management: a new, dedicated
"Prüf-/Freigabe-Rechte" section in the existing group-member editor (RDEL-03), backed by two
new API client functions and TS types matching the backend contract locked in Plan 140-02;
and the asymmetric grant-removal change to the generic effective-rights UI required by
140-CONTEXT.md's binding Option (d) decision (grant only through the new section; deny/revoke
stays exactly as-is everywhere).

Purpose: RDEL-02/RDEL-03 are UI-facing requirements -- a leader must be able to see and
toggle each delegable review right in a visibly distinct control, and must never be able to
grant the same three actions through the older, generic override mechanism (which would
silently defeat the "technically separate" requirement Phase 138 already shipped a
competing path for, per 140-RESEARCH.md's Contradiction Found).

Output: `ReviewDelegationSection.tsx` (new, mounted in `GroupSection.tsx`), two new
`frontend/src/lib/api.ts` functions, one new types file, and a modified
`CapabilityDetailRow.tsx`/`userGroupRightsHelpers.ts` pair implementing the locked
asymmetric separation. This plan does not depend on Plan 140-01/140-02's backend code --
the exact wire contract (routes, JSON field names) is already locked in this phase's
planning documents, so frontend work proceeds against mocked `@/lib/api` in tests, matching
this codebase's existing convention (no component test in this tree hits a real network
call).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/140-review-delegation-management/140-CONTEXT.md
@.planning/phases/140-review-delegation-management/140-UI-SPEC.md
@.planning/phases/140-review-delegation-management/140-PATTERNS.md

@frontend/src/app/admin/users/tabs/GroupRolesSection.tsx
@frontend/src/app/admin/users/tabs/GroupSection.tsx
@frontend/src/app/admin/users/tabs/CapabilityDetailRow.tsx
@frontend/src/app/admin/users/tabs/userGroupRightsHelpers.ts
@frontend/src/app/admin/users/tabs/GuidedGrantFlow.test.tsx
@frontend/src/components/ui/Switch.tsx
@frontend/src/components/ui/ErrorState.tsx
@frontend/src/types/admin-capability.ts
</context>

<interfaces>
<!-- Locked wire contract (matches Plan 140-01/140-02's backend DTOs exactly -- do not invent a different shape). -->

Backend response/request shapes this plan's TS types and fetch functions MUST match
byte-for-byte (field names, not just types):
```
GET /api/v1/admin/fansubs/:id/app-members/:appUserId/review-delegations
  -> { data: ReviewDelegationRow[] }
  ReviewDelegationRow = {
    action_code: string
    granted: boolean
    membership_active: boolean
    app_user_active: boolean
    has_verified_claim: boolean
    eligible_for_grant: boolean
  }

PUT /api/v1/admin/fansubs/:id/app-members/:appUserId/review-delegations
  body: { action_code: string, grant: boolean }
  -> { data: { action_code: string, granted: boolean } }
```
No `reason` field anywhere in the request (unlike `mutateCapabilityOverride`) -- the
underlying `ReviewDelegationCommand` has no reason field and the audit-event DB trigger
forbids one (140-RESEARCH.md Pitfall 4).

From frontend/src/lib/api.ts:10146-10199 (exact fetch/error-handling shape to mirror):
```ts
export async function getEffectiveRights(fansubGroupId: number, appUserId: number): Promise<EffectiveRightState[]> {
  const response = await apiClientFetch(
    `/api/v1/admin/fansubs/${fansubGroupId}/app-members/${appUserId}/effective-rights`,
    { cache: "no-store" },
  )
  if (!response.ok) {
    const parsed = await parseApiErrorPayload(response, `API request failed: ${response.status}`)
    throw new ApiError(response.status, parsed.message, null, parsed.code, parsed.details)
  }
  const resp = (await response.json()) as { data: EffectiveRightState[] }
  return resp.data
}

export async function mutateCapabilityOverride(
  fansubGroupId: number, appUserId: number, body: CapabilityOverrideMutationRequest,
): Promise<CapabilityOverrideMutationResult> {
  const API_BASE_URL = getApiBaseUrl()
  const response = await authorizedFetch(
    `${API_BASE_URL}/api/v1/admin/fansubs/${fansubGroupId}/app-members/${appUserId}/capability-overrides`,
    { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body) },
  )
  if (!response.ok) {
    const parsed = await parseApiErrorPayload(response, `API request failed: ${response.status}`)
    throw new ApiError(response.status, parsed.message, null, parsed.code, parsed.details)
  }
  const resp = (await response.json()) as { data: CapabilityOverrideMutationResult }
  return resp.data
}
```

Switch primitive contract (frontend/src/components/ui/Switch.tsx, full file, do not modify):
```tsx
export interface SwitchProps extends Omit<ButtonHTMLAttributes<HTMLButtonElement>, 'onChange' | 'onClick' | 'type'> {
  checked: boolean
  onCheckedChange: (next: boolean) => void
  disabled?: boolean
  label?: string
}
```

getErrorStateCopy (frontend/src/components/ui/ErrorState.tsx, do not modify):
```ts
export function getErrorStateCopy(error: unknown, options?: { defaultTitle?, defaultDescription?, permissionDescription? }): { title, description }
```

Current CapabilityDetailRow.tsx grant/revoke condition to change (lines 24-26):
```tsx
const showRevoke = state.allowed && !state.non_deniable
const showGrant = !state.allowed
const showRemoveOverride = state.user_allow || state.user_deny
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire types + API client functions (interface-first)</name>
  <files>frontend/src/types/admin-review-delegation.ts, frontend/src/lib/api.ts</files>
  <read_first>
    frontend/src/types/admin-capability.ts lines 140-260 (EffectiveRightState /
    CapabilityOverrideMutationRequest/Result -- DTO naming/doc-comment style to mirror, kept
    visually distinct per 140-RESEARCH.md), frontend/src/lib/api.ts lines 10146-10199 (exact
    functions to mirror, shown in interfaces block above)
  </read_first>
  <action>
    Create admin-review-delegation.ts exporting exactly: `ReviewDelegationRow { action_code:
    string; granted: boolean; membership_active: boolean; app_user_active: boolean;
    has_verified_claim: boolean; eligible_for_grant: boolean }`;
    `ReviewDelegationMutationRequest { action_code: string; grant: boolean }`;
    `ReviewDelegationMutationResult { action_code: string; granted: boolean }`. Add a doc
    comment noting these are intentionally separate from `EffectiveRightState`/
    `CapabilityOverride*` (a distinct domain, per 140-RESEARCH.md naming guidance) even though
    the wire shapes are simple.

    In api.ts, add (near the existing `getEffectiveRights`/`mutateCapabilityOverride`
    functions, importing the two new types at the top of the file alongside the existing
    `admin-capability` type imports): `getReviewDelegations(fansubGroupId: number, appUserId:
    number): Promise<ReviewDelegationRow[]>` hitting `GET
    /api/v1/admin/fansubs/${fansubGroupId}/app-members/${appUserId}/review-delegations` via
    `apiClientFetch(..., { cache: "no-store" })`, mirroring `getEffectiveRights`'s exact
    error-handling shape (throw `ApiError` via `parseApiErrorPayload` on non-ok, unwrap `{
    data }`); and `mutateReviewDelegation(fansubGroupId: number, appUserId: number, body:
    ReviewDelegationMutationRequest): Promise<ReviewDelegationMutationResult>` hitting `PUT`
    the same path via `authorizedFetch` with `Content-Type: application/json` and
    `JSON.stringify(body)`, mirroring `mutateCapabilityOverride`'s exact shape. Do not add a
    `reason` field anywhere in the request body.
  </action>
  <acceptance_criteria>
    - `grep -n "export interface ReviewDelegationRow\|export interface ReviewDelegationMutationRequest\|export interface ReviewDelegationMutationResult" frontend/src/types/admin-review-delegation.ts` shows all three
    - `grep -n "export async function getReviewDelegations\|export async function mutateReviewDelegation" frontend/src/lib/api.ts` shows both new functions
    - `grep -n "reason" frontend/src/types/admin-review-delegation.ts` returns nothing (no reason field, Pitfall 4)
    - project TypeScript compiles with no new type errors
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit"</automated>
  </verify>
  <done>Two new types-matching-the-locked-backend-contract exist; two new api.ts functions exist mirroring the exact existing fetch/error patterns; TypeScript compiles clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReviewDelegationSection + mount + tests</name>
  <files>frontend/src/app/admin/users/tabs/ReviewDelegationSection.tsx, frontend/src/app/admin/users/tabs/ReviewDelegationSection.test.tsx, frontend/src/app/admin/users/tabs/GroupSection.tsx</files>
  <read_first>
    140-UI-SPEC.md's "Component Contract" and "Copywriting Contract" sections in full (exact
    copy strings, row order, interaction model table, ineligible-note mapping),
    frontend/src/app/admin/users/tabs/GroupRolesSection.tsx (sibling-section structural
    pattern: own SectionHeader, own state, `marginBottom: 'var(--space-3)'` wrapper),
    frontend/src/app/admin/users/tabs/GroupSection.tsx (mount point: directly after
    `<GroupRolesSection .../>`, before the `accordionItems.length === 0 ? ... :
    <Accordion .../>` block), frontend/src/components/ui/Switch.tsx (props contract),
    frontend/src/components/ui/ErrorState.tsx (getErrorStateCopy),
    frontend/src/app/admin/users/tabs/GuidedGrantFlow.test.tsx (makeState-style fixture
    pattern to mirror for building ReviewDelegationRow test fixtures, and the
    `vi.mock('@/lib/api', ...)` pattern)
  </read_first>
  <behavior>
    - Renders exactly 3 rows always in this fixed order: review.image.decide ("Medien/Bilder
      prüfen"), review.text.decide ("Notizen/Texte prüfen"), review.contribution.decide
      ("Mitwirkungen prüfen") -- regardless of the order the mocked `getReviewDelegations`
      resolves them in.
    - A row whose `granted=true` renders its Switch checked/enabled, regardless of
      `eligible_for_grant`.
    - A row with `granted=false, eligible_for_grant=false` renders its Switch disabled and
      shows exactly one of the three ineligible-note strings based on which of
      `membership_active`/`app_user_active`/`has_verified_claim` is false: "Nicht aktiv in
      dieser Gruppe." / "Konto ist deaktiviert." / "Keine verifizierte historische
      Zuordnung.".
    - Clicking an enabled Switch optimistically flips its checked state immediately, then
      calls `mutateReviewDelegation(fansubGroupId, appUserId, { action_code, grant: <new
      state> })`.
    - On mutation success, the Switch stays in its new (optimistic) state.
    - On mutation failure (rejected promise), the Switch snaps back to its prior state and an
      inline error reading exactly "Änderung konnte nicht gespeichert werden. Bitte erneut
      versuchen." appears below that row; the Switch remains clickable for retry.
    - On section-level GET failure, the 3-row list is replaced by `ErrorState` using
      `getErrorStateCopy(err, { defaultDescription: 'Die Prüf-/Freigabe-Rechte konnten nicht
      geladen werden.' })`.
    - The section root element has `id="review-delegation-section"`.
  </behavior>
  <action>
    Create ReviewDelegationSection.tsx: `export function ReviewDelegationSection({
    fansubGroupId, appUserId }: { fansubGroupId: number; appUserId: number })`. On mount,
    call `getReviewDelegations(fansubGroupId, appUserId)`; while pending, render the 3 fixed
    rows with each Switch in a disabled placeholder state (no `EmptyState` while loading, per
    UI-SPEC). On resolve, store rows keyed by `action_code` in local state; on reject, store
    the `getErrorStateCopy(...)` result and render `ErrorState` in place of the row list. Root
    wrapper: `<div id="review-delegation-section" style={{ marginBottom: 'var(--space-3)' }}>`
    containing `<SectionHeader level={3} title="Prüf-/Freigabe-Rechte" description="Eigenständige
    Delegation für Medien-, Text- und Mitwirkungsprüfung — unabhängig von Rollen und
    allgemeinen Rechte-Abweichungen." />` then the fixed-order row list (`gap: var(--space-3)`
    between rows, `gap: var(--space-2)` within a row). Render order and labels: image row
    "Medien/Bilder prüfen", text row "Notizen/Texte prüfen", contribution row "Mitwirkungen
    prüfen" -- iterate over a HARDCODED local array of the three `{action_code, label}` pairs
    in this exact order, looking up each row's live state from the keyed map (per UI-SPEC:
    "NEVER derived from a sorted list"). Implement the optimistic-toggle handler per the
    <behavior> block above, catching the mutate promise's rejection to revert state and set a
    per-row inline error message (exact copy: "Änderung konnte nicht gespeichert werden. Bitte
    erneut versuchen."). If the component approaches 450 lines, extract a
    `useReviewDelegations` hook -- expect ~90-140 lines total per UI-SPEC's own estimate, so
    this should not be necessary.

    Edit GroupSection.tsx: import `ReviewDelegationSection` and mount
    `<ReviewDelegationSection fansubGroupId={membership.fansub_group_id}
    appUserId={appUserId} />` directly after the existing `<GroupRolesSection .../>` element
    and before the `accordionItems.length === 0 ? ... : <Accordion .../>` block (locked
    reading order: group name -> roles -> Prüf-/Freigabe-Rechte -> generic capability
    categories).

    Write ReviewDelegationSection.test.tsx mocking `@/lib/api`'s `getReviewDelegations`/
    `mutateReviewDelegation` (mirror GuidedGrantFlow.test.tsx's `vi.mock('@/lib/api', ...)`
    pattern including a minimal `ApiError` class), asserting every bullet in the <behavior>
    block above as a distinct test case.
  </action>
  <acceptance_criteria>
    - `grep -n "id=\"review-delegation-section\"" frontend/src/app/admin/users/tabs/ReviewDelegationSection.tsx` matches
    - `grep -n "ReviewDelegationSection" frontend/src/app/admin/users/tabs/GroupSection.tsx` shows both the import and the mount, positioned after `GroupRolesSection` and before the Accordion block (verify by reading the surrounding lines, not grep alone)
    - `wc -l frontend/src/app/admin/users/tabs/ReviewDelegationSection.tsx` stays under 450 (expect ~90-140 per UI-SPEC)
    - all new test cases pass; existing `GroupRolesSection.test.tsx`/`UserGroupRightsTab.test.tsx` remain green (regression, unmodified)
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/app/admin/users/tabs/ReviewDelegationSection.test.tsx src/app/admin/users/tabs/GroupRolesSection.test.tsx src/app/admin/users/tabs/UserGroupRightsTab.test.tsx"</automated>
  </verify>
  <done>ReviewDelegationSection renders the 3 fixed rows with eligibility-aware disabled state and the correct ineligible-note copy, supports optimistic grant/revoke toggle with inline-error revert on failure, is mounted as a sibling directly after GroupRolesSection and before the generic Accordion, and all behavior-block assertions plus regression tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Asymmetric grant-removal for the 3 review actions (Option d) + regression coverage</name>
  <files>frontend/src/app/admin/users/tabs/CapabilityDetailRow.tsx, frontend/src/app/admin/users/tabs/CapabilityDetailRow.test.tsx, frontend/src/app/admin/users/tabs/userGroupRightsHelpers.ts</files>
  <read_first>
    140-CONTEXT.md's "Binding decision: asymmetric separation (Option d)" section in full
    (exact rationale for why deny must survive unchanged), 140-UI-SPEC.md's "Provenance
    surfacing in the generic rights view" section (exact locked copy strings and mechanism),
    frontend/src/app/admin/users/tabs/CapabilityDetailRow.tsx (current showGrant/showRevoke/
    showRemoveOverride logic, full file -- only 98 lines), frontend/src/app/admin/users/tabs/
    userGroupRightsHelpers.ts (existing predicate style: roleLabelFor/actionLabelFor),
    frontend/src/app/admin/users/tabs/GuidedGrantFlow.test.tsx (makeState() fixture helper to
    reuse verbatim in the new test file)
  </read_first>
  <behavior>
    - For `action_code` in `{review.image.decide, review.text.decide,
      review.contribution.decide}` with `state.allowed === false`: the "Recht zusätzlich
      erlauben" button does NOT render; instead a static line reads exactly "Gewähren nur über
      „Prüf-/Freigabe-Rechte" oben." followed by a `Button variant="ghost" size="sm"` labeled
      exactly "Zu Prüf-/Freigabe-Rechte springen".
    - Clicking that ghost button calls `document.getElementById('review-delegation-section')`
      and, if found, its `scrollIntoView({ behavior: 'smooth' })`.
    - For any OTHER `action_code` (e.g. `fansub_group.members.manage`) with `state.allowed
      === false`: the "Recht zusätzlich erlauben" button STILL renders exactly as before
      (regression guard -- this change must be scoped to exactly the 3 review actions).
    - For a review action_code with `state.allowed === true, state.non_deniable === false`:
      the "Recht entziehen" button still renders (deny/revoke path unaffected, per the binding
      decision).
    - `showRemoveOverride`'s "Abweichung entfernen" button logic is untouched for all action
      codes.
  </behavior>
  <action>
    In userGroupRightsHelpers.ts add: `const REVIEW_DELEGATION_ACTION_CODES = new
    Set(['review.image.decide', 'review.text.decide', 'review.contribution.decide'])` and
    `export function isReviewDelegationAction(actionCode: string): boolean { return
    REVIEW_DELEGATION_ACTION_CODES.has(actionCode) }` -- a fixed, hardcoded triad per
    CONTEXT.md, NOT derived from the matrix/catalog (mirrors the backend's
    `isDelegableReviewAction`, duplicated client-side as a pure string check).

    In CapabilityDetailRow.tsx: import `isReviewDelegationAction` from
    `./userGroupRightsHelpers`. Change `const showGrant = !state.allowed` to `const showGrant =
    !state.allowed && !isReviewDelegationAction(state.action_code)`. Leave `showRevoke`/
    `showRemoveOverride` and their existing button blocks completely untouched. Where the
    grant button currently renders (`{showGrant && (<Button ...>Recht zusätzlich
    erlauben</Button>)}`), add an adjacent conditional block: `{!state.allowed &&
    isReviewDelegationAction(state.action_code) && (...)}` rendering a `<div>` with
    `fontSize: '0.8125rem', color: 'var(--color-text-secondary)'` containing the exact text
    `Gewähren nur über „Prüf-/Freigabe-Rechte" oben.` and a `<Button variant="ghost" size="sm"
    onClick={() => document.getElementById('review-delegation-section')?.scrollIntoView({
    behavior: 'smooth' })}>Zu Prüf-/Freigabe-Rechte springen</Button>`.

    Create CapabilityDetailRow.test.tsx (new file -- none exists today) reusing the
    `makeState()` fixture pattern from GuidedGrantFlow.test.tsx, asserting every bullet in the
    <behavior> block above, including a `document.getElementById` mock/spy to assert
    `scrollIntoView` is called on click of the jump-link button.
  </action>
  <acceptance_criteria>
    - `grep -n "isReviewDelegationAction" frontend/src/app/admin/users/tabs/userGroupRightsHelpers.ts frontend/src/app/admin/users/tabs/CapabilityDetailRow.tsx` shows the export and both use sites (showGrant condition + the new conditional block)
    - `grep -n "Gewähren nur über" frontend/src/app/admin/users/tabs/CapabilityDetailRow.tsx` matches the exact locked copy (correct umlaut, no ASCII substitute)
    - `wc -l frontend/src/app/admin/users/tabs/CapabilityDetailRow.tsx` stays under 450 (expect ~115-125, up from 98)
    - all new CapabilityDetailRow.test.tsx cases pass; existing `GuidedGrantFlow.test.tsx`/`GuidedRevokeFlow.test.tsx`/`UserGroupRightsTab.test.tsx` remain green, unmodified (regression -- confirms the generic override mechanism's deny/revoke path for these 3 actions is untouched)
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/app/admin/users/tabs/CapabilityDetailRow.test.tsx src/app/admin/users/tabs/GuidedGrantFlow.test.tsx src/app/admin/users/tabs/GuidedRevokeFlow.test.tsx src/app/admin/users/tabs/UserGroupRightsTab.test.tsx"</automated>
  </verify>
  <done>The grant affordance for the 3 review actions is removed from CapabilityDetailRow and replaced by the exact locked explanatory note + jump-link; every other action's grant affordance and the review actions' own deny/revoke affordance are provably unaffected by regression tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser UI -> `/lib/api` client -> backend | Frontend never independently decides authorization or eligibility; it only renders server-computed `eligible_for_grant`/`granted` flags and forwards toggle intents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-09 | Elevation of Privilege | ReviewDelegationSection's optimistic toggle | mitigate | The optimistic UI state is cosmetic only; the authoritative decision remains the backend's `services.ReviewService.changeDelegation` (Plan 140-01) -- on mutation failure the Switch reverts and no client-side flag is ever trusted as a substitute for a real server response |
| T-140-10 | Elevation of Privilege | Generic override UI's grant path for the 3 review actions | mitigate | `showGrant` in CapabilityDetailRow.tsx is now `false` for all 3 review actions unconditionally, regardless of any other row state -- verified by a dedicated regression test; `GuidedGrantFlow` is never invoked for these action codes since its only call site (`onOpenGrant`) is gated by this same condition (confirmed via repo-wide grep during research: no other call site exists) |
| T-140-11 | Repudiation / Tampering | Cross-domain confusion between delegation grant and generic-override deny for the same 3 actions | accept | This is the explicit, binding, human-approved resolution (140-CONTEXT.md Option d) -- deny/revoke intentionally remains reachable via the generic path (required so a leader's whole role need not be revoked to remove one review right), and the provenance label (`decisiveSourceLabel`'s existing `specialized_grant` branch, unchanged) already discloses which mechanism produced an effective allow |
</threat_model>

<verification>
Run both scoped vitest commands above; then run the full frontend suite once
(`docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run"`) and diff
failures against the Phase-139 baseline (`.planning/phases/139-scalable-user-admin-projections/139-BASELINE.md`
-- 43 pre-existing red tests across 15 files) to confirm zero new regressions introduced by
this plan's touched files.
</verification>

<success_criteria>
- `ReviewDelegationSection.tsx` exists, is mounted in the locked reading-order position, and
  passes all behavior-block test assertions.
- `CapabilityDetailRow.tsx`'s grant affordance is removed for exactly the 3 review actions;
  every other action and the deny/revoke side are provably unaffected by regression tests.
- No hand-built native `<select>/<input>/<textarea>/<button>` introduced (only `@/components/ui`
  primitives: `SectionHeader`, `Switch`, `Button`, `ErrorState`).
- All new German user-facing strings use correct umlauts (ä/ö/ü/ß), no ASCII substitutes.
</success_criteria>

<output>
Create `.planning/phases/140-review-delegation-management/140-03-SUMMARY.md` when done.
</output>
