---
phase: 165-library-discovery-assisted-anime-creation
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/handlers/jellyfin_discovery.go
  - backend/internal/handlers/jellyfin_discovery_test.go
  - backend/internal/handlers/jellyfin_discovery_cache.go
  - backend/internal/handlers/jellyfin_discovery_cache_test.go
  - backend/internal/handlers/admin_content_handler.go
  - backend/go.mod
  - backend/go.sum
autonomous: true
requirements: [REQ-165-06, REQ-165-07, REQ-165-13]
gap_closure: true

must_haves:
  truths:
    - "Searching the Discovery library matches the series/movie title and the folder's own name, never a shared parent-directory prefix like /media/Anime/... (GAP-14)."
    - "Two concurrent Discovery snapshot rebuilds (TTL expiry race or two 'Aktualisieren' clicks) result in exactly one real Jellyfin fetch, not two overlapping ones (GAP-15)."
    - "The existing filter-before-paginate fix (GAP-02/GAP-09 backend half, already shipped) remains intact and green after this plan's changes."
  artifacts:
    - path: "backend/internal/handlers/jellyfin_discovery.go"
      provides: "buildSortedJellyfinDiscoveryEntries matches title + folder-basename only, not the full path"
    - path: "backend/internal/handlers/jellyfin_discovery_cache.go"
      provides: "buildJellyfinDiscoverySnapshot single-flights concurrent rebuild calls via golang.org/x/sync/singleflight"
  key_links:
    - from: "buildJellyfinDiscoverySnapshot"
      to: "singleflight.Group.Do"
      via: "h.discoverySnapshotGroup.Do(discoverySnapshotCacheKey, ...)"
      pattern: "discoverySnapshotGroup"
---

<objective>
Close the two remaining backend Discovery-listing gaps from 165-UAT.md: GAP-14 (search matches the full
Jellyfin path, so a common prefix like `/media/Anime/` makes the search box effectively useless) and
GAP-15 (concurrent snapshot rebuilds are not de-duplicated, so a TTL-expiry race or two admins clicking
"Aktualisieren" at once can fire multiple overlapping full-library Jellyfin fetches). Also VERIFY (no code
change expected, only a green-suite confirmation) that GAP-09's backend half — filter-before-paginate — is
already fully fixed by the prior GAP-02 quick-task (commit bf6cc886): `buildJellyfinDiscoveryFilteredPage`
already resolves status for the ENTIRE remaining scan window before truncating to `limit`, and
`TestJellyfinDiscovery_HasMoreReflectsGenuineFilterMatches` /
`TestJellyfinDiscovery_FilteredPaginationAdvancesAcrossRawWindow` already cover exactly this. GAP-09's
remaining piece (the frontend empty-state text) is fixed separately in 165-16.

Purpose: search that only matches on a shared path prefix is not really search; unbounded concurrent
Jellyfin rebuilds risk hammering the upstream server and wasting the very TTL cache D-19 introduced.
Output: title/folder-name-scoped search; single-flighted snapshot rebuilds; a documented, test-backed
confirmation that GAP-09's backend fix already holds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-library-discovery-assisted-anime-creation/165-CONTEXT.md
@.planning/phases/165-library-discovery-assisted-anime-creation/165-UAT.md
@.planning/phases/165-library-discovery-assisted-anime-creation/165-01-SUMMARY.md
</context>

<interfaces>
From `backend/internal/handlers/jellyfin_discovery.go` (existing, full function — edit exactly the
free-text filter predicate, nothing else in this file):
```go
func buildSortedJellyfinDiscoveryEntries(snapshot []jellyfinSeriesItem, query string) []jellyfinDiscoverySnapshotEntry {
	lowerQuery := strings.ToLower(strings.TrimSpace(query))
	for _, item := range snapshot {
		if lowerQuery != "" &&
			!strings.Contains(strings.ToLower(item.Name), lowerQuery) &&
			!strings.Contains(strings.ToLower(item.Path), lowerQuery) {
			// BUG: item.Path is the FULL path (e.g. "/media/Anime/Serie/Anime.TV.Sub/Naruto"),
			// so any query containing "media" or "anime" matches almost everything.
			continue
		}
		// ...
	}
}
```

From `backend/internal/handlers/admin_episode_import.go` line 617 (existing convention to reuse for
extracting the folder's own basename — do not reinvent):
```go
return path.Base(strings.ReplaceAll(itemPath, "\\", "/"))
```

From `backend/internal/handlers/jellyfin_discovery_cache.go` (existing, full file — wrap the fetch call in
Task 2, no other logic changes):
```go
func (h *AdminContentHandler) buildJellyfinDiscoverySnapshot(ctx context.Context, bypassCache bool) ([]jellyfinSeriesItem, error) {
	if !bypassCache {
		if cached, ok := h.readDiscoverySnapshotCache(ctx); ok {
			return cached, nil
		}
	}
	items, err := h.fetchJellyfinDiscoverySnapshot(ctx) // <- concurrent callers each invoke this independently today
	if err != nil {
		return nil, err
	}
	h.writeDiscoverySnapshotCache(ctx, items)
	return items, nil
}
```

`golang.org/x/sync/singleflight` — already present in `backend/go.mod`/`go.sum` as an INDIRECT dependency
(transitively vetted, not a new package install — no package-legitimacy checkpoint needed). This plan
promotes it to a direct import; run `go mod tidy` after adding the import so `go.mod` reflects it as
direct.

`backend/internal/handlers/admin_content_handler.go` (existing struct, full file — add exactly one new
field near the existing `discoveryCache discoveryCacheStore` field, ~line 232):
```go
type AdminContentHandler struct {
	// ... existing fields ...
	discoveryCache discoveryCacheStore
	// new: discoverySnapshotGroup singleflight.Group — zero-value-ready, no constructor change needed
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GAP-14 — search matches title + folder basename, not the full path</name>
  <files>backend/internal/handlers/jellyfin_discovery.go, backend/internal/handlers/jellyfin_discovery_test.go</files>
  <read_first>
    backend/internal/handlers/jellyfin_discovery.go lines 122-149 (buildSortedJellyfinDiscoveryEntries, full function)
    backend/internal/handlers/admin_episode_import.go lines 610-635 (existing path.Base(strings.ReplaceAll(...)) convention to reuse verbatim)
    backend/internal/handlers/jellyfin_discovery_test.go lines 237-264 (TestJellyfinDiscovery_NoFuzzyMatchBetweenSimilarTitles — existing search-adjacent test fixture pattern to extend)
    165-UAT.md GAP-14 entry
  </read_first>
  <behavior>
    - Test 1 (new): two snapshot items sharing the path prefix `/media/Anime/Serie/Anime.TV.Sub/`, one
      named "Naruto" at `.../Naruto`, one named "Bleach" at `.../Bleach`. Searching `q=media` (the shared
      prefix segment) returns ZERO entries (today it would match both). Searching `q=naruto` returns
      exactly the Naruto entry (title match). Searching `q=Bleach` (folder basename equals title here, use
      a second fixture where the folder basename DIFFERS from the display title, e.g. folder
      `.../Bleach.Legacy.Sub` with `item.Name == "Bleach"`) also matches via the folder-basename check, not
      the full path.
    - Test 2 (regression): the existing `TestJellyfinDiscovery_NoFuzzyMatchBetweenSimilarTitles` and all
      other existing search-adjacent tests in this file continue to pass unchanged.
  </behavior>
  <action>
    In `buildSortedJellyfinDiscoveryEntries`, replace the `!strings.Contains(strings.ToLower(item.Path),
    lowerQuery)` clause with a check against ONLY the folder's own basename, derived via the exact same
    convention `admin_episode_import.go` already uses (`path.Base(strings.ReplaceAll(item.Path, "\\",
    "/"))`, lowercased) — never the full `item.Path`. Keep the `item.Name` (title) check unchanged. Add the
    `"path"` import if not already present in this file.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/handlers/... -run TestJellyfinDiscovery -v</automated>
  </verify>
  <acceptance_criteria>
    - New search-scope test passes: a query matching only the shared path prefix returns zero results.
    - Title and folder-basename matches both still work.
    - All pre-existing `TestJellyfinDiscovery_*` tests remain green.
  </acceptance_criteria>
  <done>Discovery search matches title or folder name only; a shared parent-directory segment no longer makes every entry match.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GAP-15 — single-flight concurrent snapshot rebuilds; confirm GAP-09 backend fix intact</name>
  <files>backend/internal/handlers/jellyfin_discovery_cache.go, backend/internal/handlers/jellyfin_discovery_cache_test.go, backend/internal/handlers/admin_content_handler.go, backend/go.mod, backend/go.sum</files>
  <read_first>
    backend/internal/handlers/jellyfin_discovery_cache.go lines 70-90 (buildJellyfinDiscoverySnapshot, full function)
    backend/internal/handlers/admin_content_handler.go lines 184-240 (AdminContentHandler struct — find the exact field-declaration style to match)
    backend/internal/handlers/jellyfin_discovery_cache_test.go lines 156-208 (TestJellyfinDiscoveryCache_PaginationSafetyNetAtScale — existing fake-Jellyfin-httptest.Server pattern with a request COUNTER, reuse this exact counting technique for the concurrency assertion)
    backend/internal/handlers/jellyfin_discovery_test.go lines 496-590 (TestJellyfinDiscovery_HasMoreReflectsGenuineFilterMatches, TestJellyfinDiscovery_FilteredPaginationAdvancesAcrossRawWindow — GAP-09/GAP-02's already-shipped fix, run as-is to confirm still green, no edits needed)
  </read_first>
  <behavior>
    - Test 1 (new, `TestJellyfinDiscoveryCache_ConcurrentRebuildsSingleFlight`): a fake Jellyfin HTTP server
      with a request counter and an artificial small delay on the first response (so concurrent callers
      overlap in time); fire N (e.g. 5) concurrent goroutines all calling `buildJellyfinDiscoverySnapshot`
      with an empty/expired cache. Assert the fake server received exactly 1 request for the snapshot fetch
      (not N), and all N goroutines received the same successful result.
    - Test 2 (regression, no new test needed — run existing suite): `go test ./internal/handlers/... -run
      TestJellyfinDiscoveryCache` and `-run TestJellyfinDiscovery$` both stay green, proving the
      single-flight wrapper does not change the existing cache-hit/cache-bypass/error-propagation behavior
      for the NON-concurrent case.
  </behavior>
  <action>
    Add `discoverySnapshotGroup singleflight.Group` as a new field on `AdminContentHandler`
    (`admin_content_handler.go`, next to the existing `discoveryCache discoveryCacheStore` field; zero-value
    ready, no constructor change required). Import `"golang.org/x/sync/singleflight"`. In
    `buildJellyfinDiscoverySnapshot` (`jellyfin_discovery_cache.go`), wrap the
    `h.fetchJellyfinDiscoverySnapshot(ctx)` call in `h.discoverySnapshotGroup.Do(discoverySnapshotCacheKey,
    func() (any, error) { return h.fetchJellyfinDiscoverySnapshot(context.Background()) })`, then type-assert
    the result back to `[]jellyfinSeriesItem`. Use `context.Background()` (not the caller's `ctx`)
    INSIDE the shared singleflight call specifically so that one caller's context cancellation cannot abort
    the fetch for the other concurrent waiters sharing the same in-flight call — document this choice with
    a one-line comment. The cache write (`h.writeDiscoverySnapshotCache`) stays outside the singleflight
    call, called once by the caller that received the (possibly shared) result, exactly as today. Run `cd
    backend && go mod tidy` after adding the import so `golang.org/x/sync` becomes a direct dependency in
    `go.mod`.

    Separately (no code change expected): run `go test ./internal/handlers/... -run TestJellyfinDiscovery
    -v` and confirm `TestJellyfinDiscovery_HasMoreReflectsGenuineFilterMatches` and
    `TestJellyfinDiscovery_FilteredPaginationAdvancesAcrossRawWindow` are green, evidencing GAP-09's backend
    half (filter resolved over the full remaining scan window BEFORE truncating to `limit`, shipped in
    commit bf6cc886) is intact. Record this confirmation in the plan's SUMMARY — no `<missing>` item to add
    for GAP-09 backend in this plan.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/handlers/... -run "TestJellyfinDiscoveryCache|TestJellyfinDiscovery" -v</automated>
  </verify>
  <acceptance_criteria>
    - New concurrency test proves exactly 1 upstream fetch for N concurrent rebuild calls.
    - All pre-existing `TestJellyfinDiscoveryCache_*` and `TestJellyfinDiscovery_*` tests remain green,
      including the two GAP-09/GAP-02 regression tests named above.
    - `go.mod` lists `golang.org/x/sync` as a direct (not indirect) dependency after `go mod tidy`.
    - `go vet ./...` clean.
  </acceptance_criteria>
  <done>Concurrent Discovery snapshot rebuilds collapse into one real Jellyfin fetch; GAP-09's backend filter-before-paginate fix is confirmed intact by the existing regression tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin browser → GET /admin/jellyfin/discovery?q=... | Free-text search param, already length-capped (120 runes) pre-existing; this plan only narrows WHAT is matched, not the input validation. |
| AdminContentHandler → Jellyfin `/Items` | Outbound only; single-flighting reduces load, does not change trust. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-22 | Denial of Service | Concurrent "Aktualisieren" clicks / TTL-expiry races hammering the Jellyfin upstream | mitigate | `singleflight.Group` collapses concurrent rebuild calls into exactly one upstream fetch (GAP-15). |
| T-165-23 | Information Disclosure | Search matching on the full filesystem path could reveal path segments unrelated to the searched title | accept | Already low-risk (admin-only surface); this plan strictly NARROWS what search matches (title + folder basename only), reducing rather than increasing exposure. |
</threat_model>

<verification>
- `cd backend && go build ./...` succeeds.
- `cd backend && go test ./internal/handlers/... -run "TestJellyfinDiscovery"` passes (includes both cache and listing suites).
- `cd backend && go vet ./...` clean.
</verification>

<success_criteria>
- Discovery search never matches on a shared path prefix, only on title or the folder's own name.
- Concurrent snapshot rebuilds never fire more than one real Jellyfin fetch.
- GAP-09's already-shipped backend fix is confirmed green, not silently regressed.
</success_criteria>

<output>
Create `.planning/phases/165-library-discovery-assisted-anime-creation/165-15-SUMMARY.md` when done
</output>
</content>
