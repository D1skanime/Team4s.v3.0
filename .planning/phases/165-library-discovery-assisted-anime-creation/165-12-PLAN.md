---
phase: 165-library-discovery-assisted-anime-creation
plan: 12
type: execute
wave: 2
depends_on: ["165-05"]
files_modified:
  - frontend/src/app/admin/anime/[id]/episodes/page.tsx
  - frontend/src/app/admin/anime/[id]/episodes/page.test.tsx
  - frontend/src/app/admin/anime/[id]/edit/page.tsx
  - frontend/src/app/admin/anime/[id]/edit/page.test.tsx
autonomous: true
requirements: [REQ-165-11]

must_haves:
  truths:
    - "The 'Zurück zur Bibliothek' link renders on the episodes overview page whenever a ?return= query param is present, and is absent otherwise, matching DiscoveryReturnLink's own null-render contract."
    - "The 'Zurück zur Bibliothek' link renders on the edit page whenever a ?return= query param is present, and is absent otherwise."
    - "Neither page's existing behavior (episode list rendering, edit form rendering) changes in any other way."
  artifacts:
    - path: "frontend/src/app/admin/anime/[id]/episodes/page.tsx"
      provides: "Renders DiscoveryReturnLink when ?return= is present (D-11's second target page)"
    - path: "frontend/src/app/admin/anime/[id]/edit/page.tsx"
      provides: "Renders DiscoveryReturnLink when ?return= is present (D-11's third target page)"
  key_links:
    - from: "frontend/src/app/admin/anime/[id]/episodes/page.tsx"
      to: "frontend/src/app/admin/anime/create/DiscoveryReturnLink.tsx"
      via: "useSearchParams().get(\"return\")"
      pattern: "DiscoveryReturnLink"
    - from: "frontend/src/app/admin/anime/[id]/edit/page.tsx"
      to: "frontend/src/app/admin/anime/create/DiscoveryReturnLink.tsx"
      via: "useSearchParams().get(\"return\")"
      pattern: "DiscoveryReturnLink"
---

<objective>
D-11 requires the "Zurück zur Bibliothek" return link on THREE pages after an assisted create: the Create
page itself (already wired by 165-08), the episodes overview page, and the edit page —
`buildAssistedCreateRedirectPath` (165-05) sends admins to exactly these last two URLs with `?return=...`
after an assisted create finishes. This plan closes the remaining two: wire `DiscoveryReturnLink` (165-05's
already-built, already-tested component) into `episodes/page.tsx` and `edit/page.tsx`.

Purpose: kept as its own small, dedicated plan (rather than folded into 165-08's already-substantial Create-
page work) to stay within the phase's 2-3-task-per-plan context budget, and because these two files have no
other Discovery-related changes anywhere else in the phase — no wave/file conflict, no shared state.
Output: both pages render `DiscoveryReturnLink` when `?return=` is present; no other behavior changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-library-discovery-assisted-anime-creation/165-CONTEXT.md
@.planning/phases/165-library-discovery-assisted-anime-creation/165-UI-SPEC.md
@.planning/phases/165-library-discovery-assisted-anime-creation/165-05-SUMMARY.md
</context>

<interfaces>
From 165-05 (this plan's only dependency, already implemented):
```tsx
export function DiscoveryReturnLink({ returnURL }: { returnURL?: string }): JSX.Element | null   // frontend/src/app/admin/anime/create/DiscoveryReturnLink.tsx
```
Renders `null` when `returnURL` is absent/empty; otherwise a `@/components/ui` `Button`-as-link with the
exact copy "Zurück zur Bibliothek". This plan performs no new validation of the value — same open-redirect
posture already accepted in 165-05's threat register (T-165-10): the value is only ever rendered as an
`href` on an explicit, human-clicked link.

Existing client-side `useSearchParams()` pattern to reuse (both target files are `"use client"` and already
use `useParams` for the `[id]` segment — NOT server components, so the async `searchParams`-Promise
pattern used elsewhere in this codebase does not apply here), `frontend/src/app/admin/users/AdminUsersClient.tsx:4,62`:
```tsx
import { useSearchParams } from 'next/navigation'
// ...
const searchParams = useSearchParams()
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire DiscoveryReturnLink onto the episodes overview and edit pages</name>
  <files>frontend/src/app/admin/anime/[id]/episodes/page.tsx, frontend/src/app/admin/anime/[id]/episodes/page.test.tsx, frontend/src/app/admin/anime/[id]/edit/page.tsx, frontend/src/app/admin/anime/[id]/edit/page.test.tsx</files>
  <read_first>
    frontend/src/app/admin/anime/[id]/episodes/page.tsx (full file, 452 lines — "use client", uses
    `useParams`; no existing `page.test.tsx` for this route today; header/`<main>` region where the link
    should render)
    frontend/src/app/admin/anime/[id]/edit/page.tsx (full file, 273 lines — same "use client"/`useParams`
    shape; `page.test.tsx` already exists for this route, 70 lines)
    frontend/src/app/admin/users/AdminUsersClient.tsx lines 4,62 (useSearchParams() pattern to reuse)
    frontend/src/app/admin/anime/create/DiscoveryReturnLink.tsx (the component being wired, already built and tested by 165-05 — do not modify it)
  </read_first>
  <behavior>
    - Test 1 (`episodes/page.test.tsx`, new file): rendering the page with `?return=%2Fadmin%2Fanime%2Fcreate%2Flibrary`
      in the mocked URL (mock `next/navigation`'s `useSearchParams`) renders `DiscoveryReturnLink` with
      `returnURL` equal to the decoded value, visible near the page header; rendering with no `return`
      param results in no such link rendered (relies on `DiscoveryReturnLink`'s own null-render contract —
      no new null-handling logic needed in this file).
    - Test 2 (`edit/page.test.tsx`, existing file, extended): identical two cases for the edit page; every
      pre-existing test case in this file continues to pass unmodified.
    - Test 3: on both pages, the value passed to `returnURL` is exactly `searchParams.get("return")` — no
      re-encoding, no trimming, no default substitution beyond `?? undefined`.
  </behavior>
  <action>
    In both `episodes/page.tsx` and `edit/page.tsx` (both already `"use client"` and already using
    `useParams` for the `[id]` segment), add `const searchParams = useSearchParams();` from
    `next/navigation` (mirroring `AdminUsersClient.tsx:4,62`) and render
    `<DiscoveryReturnLink returnURL={searchParams.get("return") ?? undefined} />` near each page's existing
    header row (immediately inside the top-level `<main>`, above the page's primary heading/content — pick
    the exact insertion point that reads naturally with each file's existing header markup), importing the
    component unchanged from 165-05's completed `frontend/src/app/admin/anime/create/DiscoveryReturnLink.tsx`.
    This is a purely additive, one-import + one-render-call change on each file — both already sit
    comfortably under CLAUDE.md's 450-line limit (452 and 273 lines respectively), so no split is needed
    here, unlike the oversized files elsewhere in this phase. No other markup on either page changes.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run 'src/app/admin/anime/[id]/episodes/page.test.tsx' 'src/app/admin/anime/[id]/edit/page.test.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - Both pages render the return link only when `?return=` is present; existing page behavior (episodes
      list, edit form) is byte-identical otherwise.
    - `episodes/page.test.tsx` is a new file (no test previously existed for this route) covering at least
      Test 1/3 plus a basic smoke render of the existing page content (proving the page still mounts and
      renders its core content, not just the new link).
    - All pre-existing cases in `edit/page.test.tsx` continue to pass unmodified — run the full pre-existing
      suite, not just the new cases.
    - `git diff episodes/page.tsx edit/page.tsx` shows only additive import + render lines, no deletions or
      restructuring of existing markup.
  </acceptance_criteria>
  <done>Both episodes/page.tsx and edit/page.tsx render a working "Zurück zur Bibliothek" link when ?return= is present, closing D-11's second and third target page (the first, the Create page, is closed by 165-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin browser (episodes/edit pages) → `?return=` query param | Client-side only, rendering-only consumption; no navigation happens without an explicit admin click on the rendered link. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-20 | Tampering | `?return=` query param on episodes/edit pages | accept | Same disposition as 165-05's T-165-10 (`DiscoveryReturnLink` itself performs no validation) — the value is only ever rendered as an `href` on an explicit, human-clicked link; no auto-navigation, no server-side trust placed on it, no new attack surface beyond what 165-05 already accepted for the Create page. |
</threat_model>

<verification>
- `cd frontend && npx vitest run 'src/app/admin/anime/[id]/episodes/page.test.tsx' 'src/app/admin/anime/[id]/edit/page.test.tsx'` passes.
- `cd frontend && npx tsc --noEmit` clean.
- `cd frontend && npm run lint` clean for touched files.
</verification>

<success_criteria>
- All three D-11 target pages (Create via 165-08, episodes overview, edit) render a working "Zurück zur Bibliothek" link when a return context is present.
- No regression to either page's pre-existing behavior.
</success_criteria>

<output>
Create `.planning/phases/165-library-discovery-assisted-anime-creation/165-12-SUMMARY.md` when done
</output>
