---
phase: 165-library-discovery-assisted-anime-creation
plan: 09
type: execute
wave: 3
depends_on: ["165-06", "165-05"]
files_modified:
  - frontend/src/app/admin/anime/create/library/page.tsx
  - frontend/src/app/admin/anime/create/library/DiscoveryLibraryPanel.tsx
  - frontend/src/app/admin/anime/create/library/DiscoveryLibraryPanel.test.tsx
  - frontend/src/app/admin/anime/create/library/DiscoveryLibraryCard.tsx
  - frontend/src/app/admin/anime/create/library/DiscoveryLibraryCard.test.tsx
  - frontend/src/app/admin/anime/create/library/useDiscoveryLibraryFilters.ts
  - frontend/src/app/admin/anime/create/library/useDiscoveryLibraryFilters.test.ts
  - frontend/src/app/admin/anime/create/library/discoveryPageHelpers.ts
  - frontend/src/app/admin/anime/create/library/discoveryPageHelpers.test.ts
  - frontend/src/types/admin.ts
  - frontend/src/lib/api.ts
autonomous: true
requirements: [REQ-165-03, REQ-165-04, REQ-165-06, REQ-165-07, REQ-165-11, REQ-165-13, REQ-165-16, REQ-165-18]

must_haves:
  truths:
    - "An admin can filter (Offen/Bereits vorhanden/Ignoriert/Alle, D-04) — Offen is default and stays 'zwei belastbare Zustände' (D-03) plus Ignoriert (D-17) — search, and page through the Jellyfin library without any native <select>/<input>/<button> in the new code."
    - "D-24: the Discovery list renders as a vertical DiscoveryLibraryCard stack (Poster / Titel / '{Jahr} | {Pfad}' / '{Typ} | {Bibliothek}' / Status-Badge (+ Statusdetail) / gestapelte Aktionen), field layout modeled on the existing frontend/src/app/admin/anime/components/JellyfinIntake/JellyfinCandidateCard.tsx — NOT its native <button>/<img>/CSS-module markup, only its field ordering — built exclusively with @/components/ui primitives (Card/Badge/Button). No Table/TableRow/TableHeaderCell remains on this page."
    - "D-25 (confirmed unchanged by the D-24 card-grid switch): pagination stays classic Zurück/Weiter with a client-side page counter and a client-held cursor-history stack — no infinite scroll, no numbered Pagination primitive; the card-grid change only affects what renders between the toolbar and the pager, not the pager logic itself."
    - "Filter, search, and cursor state survive as URL query params, so a 'Zurück zur Bibliothek' link restores exactly where the admin left off (D-11)."
    - "Picking 'Anime anlegen' navigates to /admin/anime/create with jellyfin_id/from=discovery/return params; zero Jellyfin detail requests happen before that click."
    - "Ignoring/unignoring a card updates its visibility in the current filter immediately, without a manual 'Bibliothek neu laden' click."
  artifacts:
    - path: "frontend/src/app/admin/anime/create/library/DiscoveryLibraryCard.tsx"
      provides: "Single Discovery card: Poster/Titel/Jahr|Pfad/Typ|Bibliothek/Status/Aktionen, @/components/ui only (D-24)"
    - path: "frontend/src/app/admin/anime/create/library/DiscoveryLibraryPanel.tsx"
      provides: "Toolbar + vertical DiscoveryLibraryCard stack + Pager Discovery list UI, D-13-compliant"
    - path: "frontend/src/app/admin/anime/create/library/useDiscoveryLibraryFilters.ts"
      provides: "URL-synced filter/search/cursor state, analog to useUserListFilters.ts"
  key_links:
    - from: "frontend/src/app/admin/anime/create/library/DiscoveryLibraryPanel.tsx"
      to: "backend/internal/handlers/jellyfin_discovery.go"
      via: "GET /admin/jellyfin/discovery via a new lib/api.ts function"
      pattern: "listAdminJellyfinDiscovery"
    - from: "frontend/src/app/admin/anime/create/library/DiscoveryLibraryPanel.tsx"
      to: "frontend/src/app/admin/anime/create/library/DiscoveryLibraryCard.tsx"
      via: "one DiscoveryLibraryCard per item, vertical stack gap: var(--space-3) (D-24, replaces the earlier Table/TableRow plan)"
      pattern: "DiscoveryLibraryCard"
    - from: "frontend/src/app/admin/anime/create/library/DiscoveryLibraryPanel.tsx"
      to: "frontend/src/app/admin/anime/create/DiscoveryReturnLink.tsx"
      via: "renders DiscoveryReturnLink when a return context is present"
      pattern: "DiscoveryReturnLink"
---

<objective>
Build the actual `/admin/anime/create/library` Discovery list page: toolbar (status filter, debounced
search, "Bibliothek neu laden"), a vertical `DiscoveryLibraryCard` stack (Poster/Titel/Jahr|Pfad/Typ|
Bibliothek/Status/Aktionen), cursor-based Zurück/Weiter pager, and row actions ("Anime anlegen" navigating
to the Create page with the Discovery hand-off query params, "Ignorieren"/"Nicht mehr ignorieren" for the
D-17 states already wired server-side by 165-06). Filter/search/cursor state lives in the URL (D-11) so the
"Zurück zur Bibliothek" link (165-05's `DiscoveryReturnLink`) restores it exactly.

**D-24 correction (live walkthrough 2026-09-21):** the Auftraggeber rejected the table layout this plan
originally specified in favor of the existing Jellyfin-direct-search card style. This plan now builds
`DiscoveryLibraryCard.tsx` — a NEW component whose field ordering (Poster, Titel, "{Jahr} | {Pfad}", "{Typ}
| {Bibliothek}", Status-Badge (+Statusdetail), Aktionen) is modeled on
`frontend/src/app/admin/anime/components/JellyfinIntake/JellyfinCandidateCard.tsx`, but whose markup is
`@/components/ui` primitives only (`Card`/`Badge`/`Button`) — `JellyfinCandidateCard.tsx` predates the D-13
primitives rule and uses native `<button>`/`<img>`/a CSS module; CLAUDE.md's "closest-analog" clause forbids
copying that markup pattern for new code even though the Auftraggeber wants the same visual field layout
(UI-SPEC Design-Entscheidung 1/22). **D-25 is unaffected** — the Zurück/Weiter pager and client-side cursor
history sit below the card list exactly as they sat below the table before this correction.

Purpose: this is the primary Discovery-list-consuming UI, following `AdminUsersClient.tsx`/
`useUserListFilters.ts` for the toolbar/filter/pager shell and `JellyfinCandidateCard.tsx` for the per-item
field layout only — explicitly NOT `JellyfinCandidateCard.tsx`'s markup (PATTERNS.md/UI-SPEC
Design-Entscheidung 1/22).
Output: `library/page.tsx`, `DiscoveryLibraryPanel.tsx`, `DiscoveryLibraryCard.tsx`,
`useDiscoveryLibraryFilters.ts`, `discoveryPageHelpers.ts`, plus the `lib/api.ts`/`types/admin.ts` contract
additions this page needs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-library-discovery-assisted-anime-creation/165-CONTEXT.md
@.planning/phases/165-library-discovery-assisted-anime-creation/165-RESEARCH.md
@.planning/phases/165-library-discovery-assisted-anime-creation/165-PATTERNS.md
@.planning/phases/165-library-discovery-assisted-anime-creation/165-UI-SPEC.md
@.planning/phases/165-library-discovery-assisted-anime-creation/165-06-SUMMARY.md
@.planning/phases/165-library-discovery-assisted-anime-creation/165-05-SUMMARY.md
</context>

<interfaces>
From 165-06 (this wave's dependency, already implemented) — the backend response shape this page consumes,
via a new `lib/api.ts` function to add in Task 1 (`listAdminJellyfinDiscovery(params)`), now including the
D-24 `library_context` field:
```
GET /admin/jellyfin/discovery?filter=offen|bereits_vorhanden|ignoriert|alle&q=...&cursor=...&refresh=true|false
→ { data: { items: [{ jellyfin_item_id, name, type_hint, production_year, path, library_context?, status, existing_anime_id?, poster_url?, ... }], has_more, next_cursor, total_snapshot_count } }
```

From 165-05 (this wave's dependency, already implemented):
```tsx
export function DiscoveryReturnLink({ returnURL }: { returnURL?: string }): JSX.Element | null
```

From `frontend/src/app/admin/users/AdminUsersClient.tsx` + `useUserListFilters.ts` (full files, the
D-13-compliant toolbar/filter/pager shell to reuse — NOT the per-row rendering, that is
`JellyfinCandidateCard.tsx`'s field layout, see below):
```tsx
'use client'
import { Badge, Button, EmptyState, ErrorState, FormField, Input, LoadingState, Select } from '@/components/ui'
export function useUserListFilters(limit = DEFAULT_LIMIT): UseUserListFiltersResult { /* URL-sync + 300ms debounce on search, useMemo-stabilized params object */ }
```
Note per UI-SPEC Design-Entscheidung 4: `Pagination` (numbered offset-style) is NOT reused — Discovery uses
two `Button`s ("Zurück"/"Weiter") + a client-side page counter + a client-held cursor-history stack, because
total page count is unknown ahead of time with seek-cursor pagination. D-25 (Update-Durchlauf 2) confirms
this pager logic is unaffected by the D-24 card-grid switch below.

**D-24 field-layout precedent (READ ONLY — copy the field ordering, never the markup)**,
`frontend/src/app/admin/anime/components/JellyfinIntake/JellyfinCandidateCard.tsx` (full file, ~138 lines):
```tsx
// Poster block -> Titel (h3) -> "{Jahr} | {Pfad}" line -> "{Typ} | {parent_context} | {library_context}" line
// -> conditional "Bereits importiert" box (existing_title + #existing_anime_id + Link) -> action row (2 buttons)
```
This card uses native `<article>`/`<button>`/`<img>` and its own CSS module (`JellyfinCandidateCard.module.css`)
— explicitly forbidden as a markup template (D-13/CLAUDE.md "closest-analog" clause; UI-SPEC
Design-Entscheidung 1). `DiscoveryLibraryCard.tsx` (Task 2) reuses only the field/line ordering shown above,
rebuilt with `Card`/`Badge`/`Button` from `@/components/ui`.

From `frontend/src/app/admin/anime/create/page.tsx` (165-08, sequenced before this in Wave 2) — the target
navigation for "Anime anlegen": `/admin/anime/create?jellyfin_id={id}&from=discovery&return={encodeURIComponent(currentDiscoveryURL)}`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contracts — API client function, types, and discoveryPageHelpers</name>
  <files>frontend/src/types/admin.ts, frontend/src/lib/api.ts, frontend/src/app/admin/anime/create/library/discoveryPageHelpers.ts, frontend/src/app/admin/anime/create/library/discoveryPageHelpers.test.ts, frontend/src/app/admin/anime/create/library/useDiscoveryLibraryFilters.ts, frontend/src/app/admin/anime/create/library/useDiscoveryLibraryFilters.test.ts</files>
  <read_first>
    frontend/src/app/admin/users/useUserListFilters.ts (full file — URL-sync + debounce pattern to adapt)
    frontend/src/lib/api.ts `getAdminAnimeJellyfinContext`/`applyAdminAnimeMetadataFromJellyfin` (existing authorizedFetch + ApiError pattern to replicate exactly for the new function)
    165-UI-SPEC.md Copywriting Contract (exact filter-option order: Offen [default] / Bereits vorhanden / Ignoriert / Alle) and Screen 2 (D-24 card meta-line format "{Typ} | {Bibliothek}", omitting the "| Bibliothek" suffix when library_context is null/empty)
  </read_first>
  <behavior>
    - Test 1 (`discoveryPageHelpers.test.ts`): `mapDiscoveryStatusToBadgeVariant("offen")` → `"muted"`,
      `"bereits_vorhanden"` → `"success"`, `"teilweise"` → `"warning"`, `"ignoriert"` → `"info"` (exact
      mapping from UI-SPEC Color section); `buildDiscoveryCreateURL({ jellyfinItemID, currentDiscoveryURL
      })` returns `/admin/anime/create?jellyfin_id=<id>&from=discovery&return=<encoded>`; `mapDiscoveryTypeHintToLabel("film")`
      → `"Film"`, unknown/missing → `"Unbekannt"` (UI-SPEC's exact type-label set: Serie/Film/OVA/Special/
      ONA/Unbekannt); `buildDiscoveryCardMetaLine(typeLabel, libraryContext)` returns `"{typeLabel} |
      {libraryContext}"` when `libraryContext` is a non-empty string, and returns `typeLabel` alone (no
      trailing separator) when `libraryContext` is `null`/`undefined`/empty (D-24).
    - Test 2 (`useDiscoveryLibraryFilters.test.ts`): default state (no URL params) resolves to
      `filter: "offen"`; changing filter/search writes `filter`/`q` URL params and resets `cursor`;
      `handleCursorChange` writes `cursor` WITHOUT resetting `filter`/`q`; changing filter/search DOES clear
      any existing `cursor` param (the "stiller Neustart" convention — filter/search change starts over at
      page 1).
  </behavior>
  <action>
    Add `listAdminJellyfinDiscovery(params: { filter?: string; q?: string; cursor?: string; refresh?:
    boolean }, authToken?: string): Promise<AdminJellyfinDiscoveryPageResponse>` to `lib/api.ts`, mirroring
    the exact `authorizedFetch`/`withAuthHeader`/`parseApiErrorPayload`/`ApiError` structure of
    `getAdminAnimeJellyfinContext` — GET `/api/v1/admin/jellyfin/discovery` with query params, `cache:
    "no-store"`. Add matching TS types to `types/admin.ts` mirroring 165-06's Go response DTOs field names,
    including `library_context?: string` on the item type (D-24). Create `discoveryPageHelpers.ts` with the
    pure mapping functions from the behavior spec plus `buildDiscoveryCreateURL` and the new
    `buildDiscoveryCardMetaLine` helper (D-24's "{Typ} | {Bibliothek}" formatting, consumed by
    `DiscoveryLibraryCard.tsx` in Task 2). Create `useDiscoveryLibraryFilters.ts` adapting
    `useUserListFilters.ts`'s shape to the `filter`/`q`/`cursor` URL-key set (per PATTERNS.md: same
    debounce/`useMemo`-stabilization mechanism, different keys) — expose a client-held cursor-history array
    (not just the current cursor) so "Zurück" can pop without a server-side backward cursor.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/admin/anime/create/library/discoveryPageHelpers.test.ts src/app/admin/anime/create/library/useDiscoveryLibraryFilters.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All mapping functions match UI-SPEC's exact status/badge/type-label tables.
    - `buildDiscoveryCardMetaLine`'s empty-`library_context` behavior is covered by an explicit test case
      (this is the D-24 field that has no analog in the pre-existing helpers, so it needs its own assertion).
    - `useDiscoveryLibraryFilters`'s `params` return value is `useMemo`-stabilized (a reference-equality
      test across two renders with unchanged URL params proves this, guarding against the documented
      infinite-loop risk in the analog).
    - `listAdminJellyfinDiscovery` throws `ApiError` with status/message/details on a non-2xx response,
      matching every other `lib/api.ts` function's error contract.
  </acceptance_criteria>
  <done>API client function, types (including library_context), pure helpers, and URL-state hook all exist and are independently tested before the card/panel components consume them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DiscoveryLibraryCard (D-24 — new card component, @/components/ui only)</name>
  <files>frontend/src/app/admin/anime/create/library/DiscoveryLibraryCard.tsx, frontend/src/app/admin/anime/create/library/DiscoveryLibraryCard.test.tsx</files>
  <read_first>
    frontend/src/app/admin/anime/components/JellyfinIntake/JellyfinCandidateCard.tsx (READ ONLY — field/line ordering to reuse, markup NOT to copy; see Interfaces)
    165-UI-SPEC.md Screen 2 (full card layout spec: 96px poster/aspect-ratio 2/3, Heading 16/600 title, "{Jahr} | {Pfad}" line, "{Typ} | {Bibliothek}" line via Task 1's buildDiscoveryCardMetaLine, Status-Badge + Statusdetail + Caption rules, stacked action buttons per status) and Design-Entscheidung 22 (exact deviations from the reference card: single-column vertical list not a grid, 2/3 poster aspect ratio not 16/10, no "Ansehen"/"Im Fokus" selection interaction)
    frontend/src/components/ui barrel exports for Card/Badge/Button (frontend/src/components/ui/index.ts) — prop shapes (CardProps/BadgeVariant/ButtonVariant/ButtonSize)
  </read_first>
  <behavior>
    - Test 1: renders the poster block (96px wide, `aspect-ratio: 2/3`, `<img loading="lazy" alt="Poster von
      {name}">` when `poster_url` is present; a fixed-size placeholder box with `alt=""` when absent — native
      `<img>` is the sole D-13-exempt element per UI-SPEC Design-Entscheidung 13), the title as a Heading
      (16/600), the "{Jahr} | {Pfad}" meta line, and the "{Typ} | {Bibliothek}" meta line built via Task 1's
      `buildDiscoveryCardMetaLine` (asserted to omit the "| Bibliothek" suffix when `library_context` is
      absent).
    - Test 2: status `"offen"`/`"teilweise"` renders a stacked pair of `Button`s — "Anime anlegen"
      (`variant="primary"`, `size="sm"`) above "Ignorieren" (`variant="ghost"`, `size="sm"`), `gap:
      var(--space-1)`; status `"bereits_vorhanden"` renders only `Button` "Anime öffnen"
      (`variant="secondary"`, `size="sm"`) and NO "Ignorieren" button (status-priority rule: ignoring an
      already-connected item is never offered); status `"ignoriert"` renders only `Button` "Nicht mehr
      ignorieren" (`variant="secondary"`, `size="sm"`).
    - Test 3: `Badge` variant matches UI-SPEC's Color section exactly via Task 1's
      `mapDiscoveryStatusToBadgeVariant` (`offen`→muted, `bereits_vorhanden`→success, `teilweise`→warning,
      `ignoriert`→info), and the badge text content is always the German status word itself (never
      color-only, WCAG 1.4.1); `"bereits_vorhanden"` additionally renders a statusdetail line
      `"{existingTitle} (#{existingAnimeID})"`.
    - Test 4: clicking "Anime anlegen"/"Anime öffnen" calls the card's `onCreate`/`onOpenExisting` prop with
      the item's `jellyfin_item_id`/`existing_anime_id`; clicking "Ignorieren"/"Nicht mehr ignorieren" calls
      `onIgnore`/`onUnignore` with the item's `jellyfin_item_id` — the card itself performs no API calls
      (pure prop-callback component, consistent with `JellyfinCandidateCard`'s callback-prop shape).
    - Test 5: zero native `<button>`/`<select>`/`<input>`/`<textarea>` elements render (the poster `<img>`
      is the sole exception) — `grep -rn "<button\|<select\|<input\|<textarea" DiscoveryLibraryCard.tsx`
      returns zero matches.
  </behavior>
  <action>
    Create `DiscoveryLibraryCard.tsx` exporting a component with props shaped after the response item from
    Task 1's types (`item: AdminJellyfinDiscoveryItem`) plus `onCreate`, `onOpenExisting`, `onIgnore`,
    `onUnignore` callbacks (each `(jellyfinItemID: string) => void`, `onOpenExisting` may instead take
    `existingAnimeID: number` — Claude's Discretion on exact callback signature, keep it a pure
    prop-callback component with zero internal API calls, mirroring `JellyfinCandidateCard`'s
    `onSelect`/`onAdopt` callback-prop shape). Build with `Card` (or a plain `<article>` if `Card`'s built-in
    header/footer slots don't fit this field layout — Claude's Discretion, the D-13 requirement is about
    interactive primitives, not literally wrapping in `<Card>`) containing: the poster block (96px, 2/3
    aspect ratio, `border-radius: var(--radius-md)`, `loading="lazy"` `<img>` or a `background:
    var(--surface-sunken)` placeholder box per UI-SPEC), a content block with the title (`Heading 16/600`),
    the "{Jahr} | {Pfad}" line (`Body 14/400`, `ellipsis`, `title=` attribute with the full path), the
    "{Typ} | {Bibliothek}" line via `buildDiscoveryCardMetaLine` (Task 1), the `Badge` status indicator (+
    statusdetail/caption per UI-SPEC's rules), and the stacked `Button` action pair per Test 2/3. Internal
    grid: `grid-template-columns: 96px minmax(0,1fr)`, `gap: var(--space-2)`, `padding: var(--space-4)` (per
    UI-SPEC Screen 2's exact card layout block). No click handler on the card body itself — only the two
    action buttons are focusable (UI-SPEC a11y section: at most two tab stops per card).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/admin/anime/create/library/DiscoveryLibraryCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 5 behaviors pass via real component rendering + `user-event` clicks (not shallow render).
    - `grep -rn "<button\|<select\|<input\|<textarea" DiscoveryLibraryCard.tsx` returns zero matches.
    - `npx eslint src/app/admin/anime/create/library/DiscoveryLibraryCard.tsx` reports zero
      `no-restricted-syntax` violations.
    - Copy strings (button labels, status words) match UI-SPEC's Copywriting Contract table verbatim,
      including umlauts.
  </acceptance_criteria>
  <done>DiscoveryLibraryCard exists as a pure, D-13-compliant, callback-driven component matching JellyfinCandidateCard's field layout but none of its markup, ready for DiscoveryLibraryPanel (Task 3) to render one per item.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DiscoveryLibraryPanel + library/page.tsx (vertical card list, D-24)</name>
  <files>frontend/src/app/admin/anime/create/library/page.tsx, frontend/src/app/admin/anime/create/library/DiscoveryLibraryPanel.tsx, frontend/src/app/admin/anime/create/library/DiscoveryLibraryPanel.test.tsx</files>
  <read_first>
    frontend/src/app/admin/users/AdminUsersClient.tsx (full file through line 260 — toolbar/empty/error/loading state composition pattern to reuse; its `Table` usage is NOT reused here, only its state/data-loading shape)
    165-UI-SPEC.md Screen 2 (full layout spec: toolbar fields, vertical card-list container, empty/error/loading states, pager, exact copy) and the a11y section ("aria-live=\"polite\" on the content area of the card list")
  </read_first>
  <behavior>
    - Test 1: initial render shows `LoadingState` (non-compact); after the fake `listAdminJellyfinDiscovery`
      resolves with items, renders a vertical stack (`gap: var(--space-3)`) with one `DiscoveryLibraryCard`
      (Task 2) per item — no `Table`/`TableRow`/`TableHeaderCell` anywhere in the rendered tree.
    - Test 2: passes the correct `item`/callback props to each `DiscoveryLibraryCard` (verified via the same
      per-status assertions Task 2 already covers internally — this test only proves wiring, not re-testing
      Task 2's own button logic: a `status: "offen"` item's card receives a non-undefined `onIgnore`, a
      `status: "bereits_vorhanden"` item's card does not trigger an ignore action when its only rendered
      button is clicked).
    - Test 3: clicking "Ignorieren" on a visible card (current filter = "Offen") calls the ignore API and,
      on success, removes that card from the currently rendered list without requiring a "Bibliothek neu
      laden" click (D-19 — reload only the status batch for the current page, not the whole snapshot).
    - Test 4: clicking "Weiter" with `has_more: true` fetches the next page using the returned cursor and
      increments the "Seite {n}" counter; clicking "Zurück" pops the client-held cursor history instead of
      issuing a server backward-cursor request (D-25, unaffected by the card-grid switch).
    - Test 5 (empty states): an empty result set under the default "Offen" filter renders `EmptyState`
      title "Keine offenen Einträge" with a "Alle Einträge anzeigen" action that switches `filter` to
      `"alle"`; an empty result set with a non-empty search query renders the dynamic "Keine Treffer für
      „{query}"" heading instead.
    - Test 6: `DiscoveryReturnLink` renders above the toolbar only when a return-context URL param exists in
      the page's own inbound `searchParams` (i.e., this page itself was reached via someone else's return
      link, not the common case).
  </behavior>
  <action>
    Create `library/page.tsx` as a Next.js server component reading `searchParams` (async Promise-unwrap
    convention per `admin/anime/page.tsx:16-32`) and rendering `PlatformAdminGate` + `PageHeader` (title
    "Bibliothek durchsuchen", description "Jellyfin-Einträge, die noch nicht oder bereits in Team4s
    vorhanden sind.") + `DiscoveryLibraryPanel`. Build `DiscoveryLibraryPanel.tsx` following
    `AdminUsersClient.tsx`'s data-loading/state-composition shape (`useState` for items/loading/error/
    snapshot-total, `useDiscoveryLibraryFilters` from Task 1 for filter/search/cursor, `useCallback`-wrapped
    load function, `useEffect` trigger) — Toolbar with `FormField label="Status"` `Select` (options in the
    UI-SPEC-mandated order: Offen/Bereits vorhanden/Ignoriert/Alle), `FormField label="Suche"` `Input`
    (debounced, placeholder "Titel oder Pfad durchsuchen"), trailing "{N} Einträge in der Bibliothek
    insgesamt" text + `Button variant="ghost" size="sm"` "Bibliothek neu laden" (`leftIcon={<RefreshCw
    size={16}/>}`, triggers `refresh: true` on the next fetch). Below the toolbar: a vertical container
    (`gap: var(--space-3)`, `aria-live="polite"`) rendering one `DiscoveryLibraryCard` (Task 2) per item,
    passing `onCreate`/`onOpenExisting` (navigate via `buildDiscoveryCreateURL`/`/admin/anime/{id}/edit`)
    and `onIgnore`/`onUnignore` (call the ignore/unignore API, then re-run only the current page's load —
    D-19 — not the whole snapshot). Pager row: two `Button`s ("Zurück"/"Weiter") + "Seite {n}" text, per
    UI-SPEC Design-Entscheidung 4/D-25 — do not use the `Pagination` primitive here; this logic is unchanged
    from what a table-based version would have needed, only the content above it changed.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/admin/anime/create/library/DiscoveryLibraryPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 6 behaviors pass via real component rendering + fake API responses (mock only `lib/api.ts`'s
      `listAdminJellyfinDiscovery`/ignore functions, not internal component logic).
    - `grep -rn "<Table\|<TableRow\|<TableHeaderCell\|<button\|<select\|<input\|<textarea" DiscoveryLibraryPanel.tsx`
      returns zero matches (proving the table layout is gone and no stray native elements were added; the
      card's own `<img>` lives in `DiscoveryLibraryCard.tsx`, not here).
    - `npx eslint src/app/admin/anime/create/library/` clean (no `no-restricted-syntax` violations).
    - Copy strings (button labels, headings, empty/error state text) match UI-SPEC's Copywriting Contract
      table verbatim, including umlauts.
  </acceptance_criteria>
  <done>The Discovery library page renders, filters, searches, pages, and actions correctly as a vertical DiscoveryLibraryCard stack using only @/components/ui primitives, matching UI-SPEC Screen 2 exactly (D-24), with the Zurück/Weiter pager unchanged (D-25).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin browser → GET /admin/jellyfin/discovery, POST/DELETE ignore | Existing admin-authenticated endpoints from 165-06; this plan only adds a browser-side consumer, no new backend surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-20 | Tampering | `filter`/`q`/`cursor` URL params | accept | These are display-filter/pagination inputs consumed server-side by 165-06's already-validated query-param parsing (badRequest on malformed input); no client-side trust decision is made from these values beyond what to render. |
| T-165-21 | Information Disclosure | Poster/path/status/library_context data in the card list | accept | Same Jellyfin-derived data already visible via the existing direct-search Jellyfin intake flow (including `library_context`, D-24) to the same admin-authenticated audience. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/app/admin/anime/create/library` passes.
- `cd frontend && npx tsc --noEmit` clean.
- `cd frontend && npm run lint` clean for touched files.
</verification>

<success_criteria>
- Discovery list page renders as a vertical DiscoveryLibraryCard stack (D-24), filters, searches, pages, and supports ignore/unignore with immediate visual consistency (D-19), all through `@/components/ui` primitives only.
- Filter/search/cursor state round-trips through the URL exactly (D-11); the Zurück/Weiter pager logic is unchanged from before the D-24 correction (D-25).
- Zero Jellyfin detail requests occur before an admin clicks a card's primary action (D-07).
</success_criteria>

<output>
Create `.planning/phases/165-library-discovery-assisted-anime-creation/165-09-SUMMARY.md` when done
</output>
