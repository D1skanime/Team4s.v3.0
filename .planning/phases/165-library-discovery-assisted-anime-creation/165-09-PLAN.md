---
phase: 165-library-discovery-assisted-anime-creation
plan: 09
type: execute
wave: 3
depends_on: ["165-06", "165-05"]
files_modified:
  - frontend/src/app/admin/anime/create/library/page.tsx
  - frontend/src/app/admin/anime/create/library/DiscoveryLibraryPanel.tsx
  - frontend/src/app/admin/anime/create/library/DiscoveryLibraryPanel.test.tsx
  - frontend/src/app/admin/anime/create/library/useDiscoveryLibraryFilters.ts
  - frontend/src/app/admin/anime/create/library/useDiscoveryLibraryFilters.test.ts
  - frontend/src/app/admin/anime/create/library/discoveryPageHelpers.ts
  - frontend/src/app/admin/anime/create/library/discoveryPageHelpers.test.ts
  - frontend/src/types/admin.ts
  - frontend/src/lib/api.ts
autonomous: true
requirements: [REQ-165-03, REQ-165-04, REQ-165-06, REQ-165-07, REQ-165-11, REQ-165-13, REQ-165-16, REQ-165-18]

must_haves:
  truths:
    - "An admin can filter (Offen/Bereits vorhanden/Ignoriert/Alle), search, and page through the Jellyfin library without any native <select>/<input>/<button> in the new code."
    - "Filter, search, and cursor state survive as URL query params, so a 'Zurück zur Bibliothek' link restores exactly where the admin left off (D-11)."
    - "Picking 'Anime anlegen' navigates to /admin/anime/create with jellyfin_id/from=discovery/return params; zero Jellyfin detail requests happen before that click."
    - "Ignoring/unignoring a row updates its visibility in the current filter immediately, without a manual 'Bibliothek neu laden' click."
  artifacts:
    - path: "frontend/src/app/admin/anime/create/library/DiscoveryLibraryPanel.tsx"
      provides: "Toolbar + Table + Pager Discovery list UI, D-13-compliant"
    - path: "frontend/src/app/admin/anime/create/library/useDiscoveryLibraryFilters.ts"
      provides: "URL-synced filter/search/cursor state, analog to useUserListFilters.ts"
  key_links:
    - from: "frontend/src/app/admin/anime/create/library/DiscoveryLibraryPanel.tsx"
      to: "backend/internal/handlers/jellyfin_discovery.go"
      via: "GET /admin/jellyfin/discovery via a new lib/api.ts function"
      pattern: "listAdminJellyfinDiscovery"
    - from: "frontend/src/app/admin/anime/create/library/DiscoveryLibraryPanel.tsx"
      to: "frontend/src/app/admin/anime/create/DiscoveryReturnLink.tsx"
      via: "renders DiscoveryReturnLink when a return context is present"
      pattern: "DiscoveryReturnLink"
---

<objective>
Build the actual `/admin/anime/create/library` Discovery list page: toolbar (status filter, debounced
search, "Bibliothek neu laden"), table (Poster/Titel/Typ/Jahr/Status/Aktion), cursor-based
Zurück/Weiter pager, and row actions ("Anime anlegen" navigating to the Create page with the Discovery
hand-off query params, "Ignorieren"/"Nicht mehr ignorieren" for the D-17 states already wired server-side
by 165-06). Filter/search/cursor state lives in the URL (D-11) so the "Zurück zur Bibliothek" link
(165-05's `DiscoveryReturnLink`) restores it exactly.

Purpose: this is the primary Discovery-list-consuming UI, following `AdminUsersClient.tsx`/
`useUserListFilters.ts` as the D-13-compliant analog — explicitly NOT `JellyfinCandidateCard.tsx`, which
predates the primitives rule (PATTERNS.md/UI-SPEC Design-Entscheidung 1).
Output: `library/page.tsx`, `DiscoveryLibraryPanel.tsx`, `useDiscoveryLibraryFilters.ts`,
`discoveryPageHelpers.ts`, plus the `lib/api.ts`/`types/admin.ts` contract additions this page needs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-library-discovery-assisted-anime-creation/165-CONTEXT.md
@.planning/phases/165-library-discovery-assisted-anime-creation/165-RESEARCH.md
@.planning/phases/165-library-discovery-assisted-anime-creation/165-PATTERNS.md
@.planning/phases/165-library-discovery-assisted-anime-creation/165-UI-SPEC.md
@.planning/phases/165-library-discovery-assisted-anime-creation/165-06-SUMMARY.md
@.planning/phases/165-library-discovery-assisted-anime-creation/165-05-SUMMARY.md
</context>

<interfaces>
From 165-06 (this wave's dependency, already implemented) — the backend response shape this page consumes,
via a new `lib/api.ts` function to add in Task 1 (`listAdminJellyfinDiscovery(params)`):
```
GET /admin/jellyfin/discovery?filter=offen|bereits_vorhanden|ignoriert|alle&q=...&cursor=...&refresh=true|false
→ { data: { items: [{ jellyfin_item_id, name, type_hint, production_year, path, status, existing_anime_id?, poster_url?, ... }], has_more, next_cursor, total_snapshot_count } }
```

From 165-05 (this wave's dependency, already implemented):
```tsx
export function DiscoveryReturnLink({ returnURL }: { returnURL?: string }): JSX.Element | null
```

From `frontend/src/app/admin/users/AdminUsersClient.tsx` + `useUserListFilters.ts` (full files, the
D-13-compliant list+filter analog — reuse the shape, adapt param names per UI-SPEC: URL keys become
`filter`/`q`/`cursor` instead of `status`/`q`/`offset`):
```tsx
'use client'
import { Badge, Button, EmptyState, ErrorState, FormField, Input, LoadingState, Select, Table, TableBody, TableCell, TableHead, TableHeaderCell, TableRow } from '@/components/ui'
export function useUserListFilters(limit = DEFAULT_LIMIT): UseUserListFiltersResult { /* URL-sync + 300ms debounce on search, useMemo-stabilized params object */ }
```
Note per UI-SPEC Design-Entscheidung 4: `Pagination` (numbered offset-style) is NOT reused — Discovery uses
two `Button`s ("Zurück"/"Weiter") + a client-side page counter + a client-held cursor-history stack, because
total page count is unknown ahead of time with seek-cursor pagination.

From `frontend/src/app/admin/anime/create/page.tsx` (165-08, sequenced before this in Wave 2) — the target
navigation for "Anime anlegen": `/admin/anime/create?jellyfin_id={id}&from=discovery&return={encodeURIComponent(currentDiscoveryURL)}`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contracts — API client function, types, and discoveryPageHelpers</name>
  <files>frontend/src/types/admin.ts, frontend/src/lib/api.ts, frontend/src/app/admin/anime/create/library/discoveryPageHelpers.ts, frontend/src/app/admin/anime/create/library/discoveryPageHelpers.test.ts, frontend/src/app/admin/anime/create/library/useDiscoveryLibraryFilters.ts, frontend/src/app/admin/anime/create/library/useDiscoveryLibraryFilters.test.ts</files>
  <read_first>
    frontend/src/app/admin/users/useUserListFilters.ts (full file — URL-sync + debounce pattern to adapt)
    frontend/src/lib/api.ts `getAdminAnimeJellyfinContext`/`applyAdminAnimeMetadataFromJellyfin` (existing authorizedFetch + ApiError pattern to replicate exactly for the new function)
    165-UI-SPEC.md Copywriting Contract (exact filter-option order: Offen [default] / Bereits vorhanden / Ignoriert / Alle)
  </read_first>
  <behavior>
    - Test 1 (`discoveryPageHelpers.test.ts`): `mapDiscoveryStatusToBadgeVariant("offen")` → `"muted"`,
      `"bereits_vorhanden"` → `"success"`, `"teilweise"` → `"warning"`, `"ignoriert"` → `"info"` (exact
      mapping from UI-SPEC Color section); `buildDiscoveryCreateURL({ jellyfinItemID, currentDiscoveryURL
      })` returns `/admin/anime/create?jellyfin_id=<id>&from=discovery&return=<encoded>`; `mapDiscoveryTypeHintToLabel("film")`
      → `"Film"`, unknown/missing → `"Unbekannt"` (UI-SPEC's exact type-label set: Serie/Film/OVA/Special/
      ONA/Unbekannt).
    - Test 2 (`useDiscoveryLibraryFilters.test.ts`): default state (no URL params) resolves to
      `filter: "offen"`; changing filter/search writes `filter`/`q` URL params and resets `cursor`;
      `handleCursorChange` writes `cursor` WITHOUT resetting `filter`/`q`; changing filter/search DOES clear
      any existing `cursor` param (the "stiller Neustart" convention — filter/search change starts over at
      page 1).
  </behavior>
  <action>
    Add `listAdminJellyfinDiscovery(params: { filter?: string; q?: string; cursor?: string; refresh?:
    boolean }, authToken?: string): Promise<AdminJellyfinDiscoveryPageResponse>` to `lib/api.ts`, mirroring
    the exact `authorizedFetch`/`withAuthHeader`/`parseApiErrorPayload`/`ApiError` structure of
    `getAdminAnimeJellyfinContext` — GET `/api/v1/admin/jellyfin/discovery` with query params, `cache:
    "no-store"`. Add matching TS types to `types/admin.ts` mirroring 165-06's Go response DTOs field names.
    Create `discoveryPageHelpers.ts` with the pure mapping functions from the behavior spec plus
    `buildDiscoveryCreateURL`. Create `useDiscoveryLibraryFilters.ts` adapting `useUserListFilters.ts`'s
    shape to the `filter`/`q`/`cursor` URL-key set (per PATTERNS.md: same debounce/`useMemo`-stabilization
    mechanism, different keys) — expose a client-held cursor-history array (not just the current cursor) so
    "Zurück" can pop without a server-side backward cursor.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/admin/anime/create/library/discoveryPageHelpers.test.ts src/app/admin/anime/create/library/useDiscoveryLibraryFilters.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All mapping functions match UI-SPEC's exact status/badge/type-label tables.
    - `useDiscoveryLibraryFilters`'s `params` return value is `useMemo`-stabilized (a reference-equality
      test across two renders with unchanged URL params proves this, guarding against the documented
      infinite-loop risk in the analog).
    - `listAdminJellyfinDiscovery` throws `ApiError` with status/message/details on a non-2xx response,
      matching every other `lib/api.ts` function's error contract.
  </acceptance_criteria>
  <done>API client function, types, pure helpers, and URL-state hook all exist and are independently tested before the panel component consumes them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DiscoveryLibraryPanel + library/page.tsx</name>
  <files>frontend/src/app/admin/anime/create/library/page.tsx, frontend/src/app/admin/anime/create/library/DiscoveryLibraryPanel.tsx, frontend/src/app/admin/anime/create/library/DiscoveryLibraryPanel.test.tsx</files>
  <read_first>
    frontend/src/app/admin/users/AdminUsersClient.tsx (full file through line 260 — table/toolbar/empty/error/loading state composition pattern, the D-13-compliant analog)
    frontend/src/app/admin/anime/components/JellyfinIntake/JellyfinCandidateCard.tsx (READ ONLY to understand what NOT to copy — native <button>/<img> markup, explicitly forbidden as a template per CLAUDE.md/UI-SPEC Design-Entscheidung 1)
    165-UI-SPEC.md Screen 2 (full layout spec: toolbar fields, table columns, action-column button stacking, empty/error/loading states, exact copy)
  </read_first>
  <behavior>
    - Test 1: initial render shows `LoadingState` (non-compact); after the fake `listAdminJellyfinDiscovery`
      resolves with items, renders a `Table` with columns Poster/Titel/Typ/Jahr/Status/Aktion and one
      `TableRow` per item.
    - Test 2: a row with `status: "offen"` renders a primary "Anime anlegen" button and a ghost
      "Ignorieren" button stacked vertically (`gap: var(--space-1)`); a row with `status: "bereits_vorhanden"`
      renders only a secondary "Anime öffnen" button and NO "Ignorieren" button (status-priority rule from
      UI-SPEC Color section — ignoring an already-connected item is never offered).
    - Test 3: clicking "Ignorieren" on a visible row (current filter = "Offen") calls the ignore API and,
      on success, removes that row from the currently rendered list without requiring a "Bibliothek neu
      laden" click (D-19 — reload only the status batch for the current page, not the whole snapshot).
    - Test 4: clicking "Weiter" with `has_more: true` fetches the next page using the returned cursor and
      increments the "Seite {n}" counter; clicking "Zurück" pops the client-held cursor history instead of
      issuing a server backward-cursor request.
    - Test 5 (empty states): an empty result set under the default "Offen" filter renders `EmptyState`
      title "Keine offenen Einträge" with a "Alle Einträge anzeigen" action that switches `filter` to
      `"alle"`; an empty result set with a non-empty search query renders the dynamic "Keine Treffer für
      „{query}"" heading instead.
    - Test 6: `DiscoveryReturnLink` renders above the toolbar only when a return-context URL param exists in
      the page's own inbound `searchParams` (i.e., this page itself was reached via someone else's return
      link, not the common case).
  </behavior>
  <action>
    Create `library/page.tsx` as a Next.js server component reading `searchParams` (async Promise-unwrap
    convention per `admin/anime/page.tsx:16-32`) and rendering `PlatformAdminGate` + `PageHeader` (title
    "Bibliothek durchsuchen", description "Jellyfin-Einträge, die noch nicht oder bereits in Team4s
    vorhanden sind.") + `DiscoveryLibraryPanel`. Build `DiscoveryLibraryPanel.tsx` following
    `AdminUsersClient.tsx`'s data-loading/state-composition shape (`useState` for items/loading/error/
    snapshot-total, `useDiscoveryLibraryFilters` from Task 1 for filter/search/cursor, `useCallback`-wrapped
    load function, `useEffect` trigger) — Toolbar with `FormField label="Status"` `Select` (options in the
    UI-SPEC-mandated order: Offen/Bereits vorhanden/Ignoriert/Alle), `FormField label="Suche"` `Input`
    (debounced, placeholder "Titel oder Pfad durchsuchen"), trailing "{N} Einträge in der Bibliothek
    insgesamt" text + `Button variant="ghost" size="sm"` "Bibliothek neu laden" (`leftIcon={<RefreshCw
    size={16}/>}`, triggers `refresh: true` on the next fetch). Table rows: `<img>` poster thumbnail (fixed
    40×56, `loading="lazy"`, per UI-SPEC Design-Entscheidung 13 — native `<img>` is exempt from the D-13
    ban list, only button/input/select/textarea are banned), title + path cell, type-hint text cell (via
    `mapDiscoveryTypeHintToLabel`), year-or-"—" cell, `Badge` status cell (via
    `mapDiscoveryStatusToBadgeVariant`) + caption per UI-SPEC's "Verknüpft über .../Mehrere Staffeln
    erkannt..." rules, action cell per Task's Test 2. Pager row: two `Button`s ("Zurück"/"Weiter") +
    "Seite {n}" text, per UI-SPEC Design-Entscheidung 4 — do not use the `Pagination` primitive here.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/admin/anime/create/library/DiscoveryLibraryPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 6 behaviors pass via real component rendering + fake API responses (mock only `lib/api.ts`'s
      `listAdminJellyfinDiscovery`/ignore functions, not internal component logic).
    - `grep -rn "<button\|<select\|<input\|<textarea" DiscoveryLibraryPanel.tsx` returns zero matches (the
      poster `<img>` is the sole native-element exception, explicitly allowed).
    - `npx eslint src/app/admin/anime/create/library/` clean (no `no-restricted-syntax` violations).
    - Copy strings (button labels, headings, empty/error state text) match UI-SPEC's Copywriting Contract
      table verbatim, including umlauts.
  </acceptance_criteria>
  <done>The Discovery library page renders, filters, searches, pages, and actions correctly using only @/components/ui primitives, matching UI-SPEC Screen 2 exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin browser → GET /admin/jellyfin/discovery, POST/DELETE ignore | Existing admin-authenticated endpoints from 165-06; this plan only adds a browser-side consumer, no new backend surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-20 | Tampering | `filter`/`q`/`cursor` URL params | accept | These are display-filter/pagination inputs consumed server-side by 165-06's already-validated query-param parsing (badRequest on malformed input); no client-side trust decision is made from these values beyond what to render. |
| T-165-21 | Information Disclosure | Poster/path/status data in the list | accept | Same Jellyfin-derived data already visible via the existing direct-search Jellyfin intake flow to the same admin-authenticated audience. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/app/admin/anime/create/library` passes.
- `cd frontend && npx tsc --noEmit` clean.
- `cd frontend && npm run lint` clean for touched files.
</verification>

<success_criteria>
- Discovery list page renders, filters, searches, pages, and supports ignore/unignore with immediate visual consistency (D-19), all through `@/components/ui` primitives only.
- Filter/search/cursor state round-trips through the URL exactly (D-11).
- Zero Jellyfin detail requests occur before an admin clicks a row's primary action (D-07).
</success_criteria>

<output>
Create `.planning/phases/165-library-discovery-assisted-anime-creation/165-09-SUMMARY.md` when done
</output>
