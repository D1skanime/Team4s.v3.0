---
phase: 165-library-discovery-assisted-anime-creation
plan: 10
type: execute
wave: 4
depends_on: ["165-07"]
files_modified:
  - frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinFolderList.tsx
  - frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinFolderList.test.tsx
  - frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinMetadataSection.tsx
  - frontend/src/types/admin.ts
  - frontend/src/lib/api.ts
autonomous: true
requirements: [REQ-165-17, REQ-165-13]

must_haves:
  truths:
    - "The anime edit page lists every connected Jellyfin folder, clearly distinguishing the main folder (no remove action) from additional folders (removable)."
    - "Removing an additional folder succeeds without a confirmation dialog (reversible action), shows a loading label during the mutation, and a single-line success message afterward."
    - "The removed folder's Discovery-list row becomes 'Offen' again on the next Discovery page load, with no additional UI action required here."
  artifacts:
    - path: "frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinFolderList.tsx"
      provides: "Folder list with main/extra badges and a remove action, @/components/ui only"
  key_links:
    - from: "frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinFolderList.tsx"
      to: "backend/internal/handlers/jellyfin_metadata_resync.go"
      via: "DELETE /admin/anime/:id/jellyfin/folders/:source via a new lib/api.ts function"
      pattern: "removeAdminAnimeJellyfinFolder"
---

<objective>
Render the "Verbundene Jellyfin-Ordner" section on the anime edit page: every connected folder from
165-07's extended context response, the main folder shown without a remove action, every additional folder
shown with an immediate (no-confirmation-dialog) "Ordner entfernen" action, reversibility communicated via
UI-SPEC's success copy rather than a `window.confirm()` gate.

Purpose: closes D-18's frontend half. Structurally modeled on `AnimeContextFansubManager.tsx`'s per-row
loading-state pattern, but deliberately NOT its `window.confirm()` step (UI-SPEC Design-Entscheidung 15 —
folder removal is reversible via the existing D-05 "Verbinden" path) and NOT its native `<button>` markup
(that file predates D-13).
Output: new `AnimeJellyfinFolderList.tsx`, wired into the existing `AnimeJellyfinMetadataSection.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-library-discovery-assisted-anime-creation/165-CONTEXT.md
@.planning/phases/165-library-discovery-assisted-anime-creation/165-RESEARCH.md
@.planning/phases/165-library-discovery-assisted-anime-creation/165-PATTERNS.md
@.planning/phases/165-library-discovery-assisted-anime-creation/165-UI-SPEC.md
@.planning/phases/165-library-discovery-assisted-anime-creation/165-07-SUMMARY.md
</context>

<interfaces>
From 165-07 (this wave's dependency, already implemented) — the extended context response this component
consumes:
```
GET /admin/anime/:id/jellyfin/context → { data: { ..., folders?: [{ jellyfin_item_id: string; is_main: boolean }] } }
DELETE /admin/anime/:id/jellyfin/folders/:source → 200 on success, 400 if :source is the main folder
```

From `frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinMetadataSection.tsx` (full file,
existing wiring point — `refreshContext`/`context` state to extend, render insertion point directly under
the existing single-context block):
```tsx
async function refreshContext(options?: { resetPreview?: boolean }) {
  const response = await getAdminAnimeJellyfinContext(animeID)
  setContext(response.data)
  onContextLoadedRef.current?.(response.data)
  // ...
  return response.data
}
```

From `frontend/src/app/admin/anime/components/AnimeContext/AnimeContextFansubManager.tsx` (structural
vorbild ONLY — per-row loading-state pattern to copy, `window.confirm()` and native `<button
className={styles.buttonSecondary}>` markup to NOT copy):
```tsx
const [isMutating, setIsMutating] = useState(false)
const [mutatingGroupID, setMutatingGroupID] = useState<number | null>(null)
const handleDetach = async (group) => {
  setIsMutating(true); setMutatingGroupID(group.id)
  try { await detachAnimeFansub(animeID, group.id); await onChanged(); onSuccess(`...`) }
  catch (error) { onError(formatError(error, '...')) }
  finally { setIsMutating(false); setMutatingGroupID(null) }
}
```

From `frontend/src/types/admin.ts` line 673-685 (existing type to extend):
```ts
export interface AdminAnimeJellyfinContext {
  anime_id: number; linked: boolean; source?: string; source_kind: "manual" | "jellyfin";
  jellyfin_series_id?: string; jellyfin_series_name?: string; jellyfin_series_path?: string;
  folder_name?: string; cover: AdminAnimeJellyfinCoverPreview; asset_slots?: AdminJellyfinIntakeAssetSlots;
  persisted_assets: AdminAnimePersistedAssets;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AnimeJellyfinFolderList component + API client function + type extension</name>
  <files>frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinFolderList.tsx, frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinFolderList.test.tsx, frontend/src/types/admin.ts, frontend/src/lib/api.ts</files>
  <read_first>
    frontend/src/app/admin/anime/components/AnimeContext/AnimeContextFansubManager.tsx (full file — structural vorbild only, see interfaces note on what NOT to copy)
    frontend/src/lib/api.ts `applyAdminAnimeMetadataFromJellyfin`/`getAdminAnimeJellyfinContext` (existing authorizedFetch pattern to replicate for the new DELETE function)
    165-UI-SPEC.md Screen 6 (exact layout, copy, and interaction spec for this section)
  </read_first>
  <behavior>
    - Test 1: given `folders: [{jellyfin_item_id: "abc", is_main: true}]` (today's single-folder regular
      case), renders one row with `Badge variant="muted"` "Haupt-Ordner" and no remove button anywhere in
      that row (not disabled — genuinely absent from the DOM, matching RESEARCH.md §12's anti-pattern
      warning).
    - Test 2: given a second folder `{jellyfin_item_id: "def", is_main: false}`, that row renders `Badge
      variant="muted"` "Zusatz-Ordner" plus a `Button variant="ghost" size="sm"` "Ordner entfernen" with no
      `window.confirm` call anywhere in the click handler.
    - Test 3: clicking "Ordner entfernen" immediately sets that row's button to a disabled, loading label
      "Wird entfernt…", calls the new `removeAdminAnimeJellyfinFolder(animeID, "def")` API function, and on
      success removes the row from the list and renders a `role="status"` success line "Ordner entfernt.
      Der Eintrag erscheint wieder als „offen" in der Bibliothek.".
    - Test 4: the component renders nothing (returns `null`) when `folders` is empty or undefined (an
      anime with no Jellyfin link at all — no empty-state noise on the edit page).
  </behavior>
  <action>
    Extend `AdminAnimeJellyfinContext` in `types/admin.ts` with `folders?: { jellyfin_item_id: string;
    is_main: boolean }[]`. Add `removeAdminAnimeJellyfinFolder(animeID: number, source: string, authToken?:
    string): Promise<void>` to `lib/api.ts`, mirroring the exact `authorizedFetch`/`withAuthHeader`/
    `parseApiErrorPayload`/`ApiError` shape of `applyAdminAnimeMetadataFromJellyfin`, calling `DELETE
    /api/v1/admin/anime/${animeID}/jellyfin/folders/${encodeURIComponent(source)}`. Create
    `AnimeJellyfinFolderList.tsx`: a `@/components/ui`-only component (`Button`/`Badge` exclusively — this
    is new code, the D-13 exception granted to `AnimeContextFansubManager.tsx` does not extend to this new
    file) taking `animeID`, `folders`, and an `onFolderRemoved` callback (to trigger the parent's
    `refreshContext`). Copy `AnimeContextFansubManager.tsx`'s per-row `isMutating`/`mutatingItemID`
    state-machine shape for the loading label, but skip its `window.confirm(...)` step entirely — call
    `removeAdminAnimeJellyfinFolder` immediately on click, per UI-SPEC Design-Entscheidung 15.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinFolderList.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 4 behaviors pass via real component rendering + user-event clicks.
    - `grep -c "window.confirm" AnimeJellyfinFolderList.tsx` returns 0.
    - `grep -rn "<button\|<select\|<input\|<textarea" AnimeJellyfinFolderList.tsx` returns 0 matches.
    - Copy strings match UI-SPEC Copywriting Contract exactly (real umlauts, exact quote characters).
  </acceptance_criteria>
  <done>Folder list component renders main/extra folders correctly, removes additional folders with no confirmation dialog, uses only @/components/ui primitives.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire into AnimeJellyfinMetadataSection</name>
  <files>frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinMetadataSection.tsx</files>
  <read_first>frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinMetadataSection.tsx (full file — refreshContext, context state, render tree insertion point)</read_first>
  <behavior>
    - Test 1: `AnimeJellyfinMetadataSection` renders `AnimeJellyfinFolderList` directly below the existing
      single-context block (AniSearch-ID / Jellyfin-Serie / Ordnerpfad / Quelle — unaffected, byte-identical
      rendering to before this task), passing `context?.folders` and an `onFolderRemoved` handler that calls
      the existing `refreshContext()` function (so the section reflects the removal immediately without a
      full page reload).
  </behavior>
  <action>
    Render `<AnimeJellyfinFolderList animeID={animeID} folders={context?.folders ?? []}
    onFolderRemoved={() => { void refreshContext(); }} />` immediately after the section's existing
    single-context block. Do not modify any other rendering in this file.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinMetadataSection.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Pre-existing `AnimeJellyfinMetadataSection.test.tsx` cases pass unmodified (full pre-existing suite,
      not just the new assertion).
    - `git diff AnimeJellyfinMetadataSection.tsx` shows a single additive render call, no restructuring of
      existing JSX.
  </acceptance_criteria>
  <done>Folder list section renders on the anime edit page, refreshes the surrounding context on removal, existing section behavior unregressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin browser → DELETE /admin/anime/:id/jellyfin/folders/:source | Existing endpoint from 165-07; this plan adds a browser-side consumer only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-22 | Tampering | Client-side omission of a "remove" button for the main folder | accept | Cosmetic-only guard; the authoritative rejection is 165-07's server-side equality check (T-165-17) — this UI omission is a UX affordance, not a security control. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/app/admin/anime/components/AnimeEditPage` passes.
- `cd frontend && npx tsc --noEmit` clean.
- `cd frontend && npm run lint` clean for touched files.
</verification>

<success_criteria>
- Every connected Jellyfin folder is visible on the anime edit page, main folder protected, additional folders removable without a confirmation dialog.
- Removal immediately refreshes the section via the existing context-reload mechanism.
</success_criteria>

<output>
Create `.planning/phases/165-library-discovery-assisted-anime-creation/165-10-SUMMARY.md` when done
</output>
