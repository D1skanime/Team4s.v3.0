---
phase: 101
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - "frontend/src/components/groups/GroupHistorySection.tsx"
  - "frontend/src/components/groups/GroupHistorySection.test.ts"
  - "frontend/src/components/groups/GroupHistoryForm.test.tsx"
autonomous: true
requirements_addressed:
  - "Phase 101 D-01"
  - "Phase 101 D-02"
  - "Phase 101 D-03"
  - "Phase 101 D-04"
  - "Phase 101 D-05"
  - "Phase 101 D-06"
must_haves:
  truths:
    - "No founded_year: only disabled `founding` is visible in the add selector."
    - "Founded_year present: only `founding` if unused plus `first_project` and `first_release` are visible before those two entries exist."
    - "Full catalog appears only after both `first_project` and `first_release` entries exist."
    - "Milestone year lower bound is founded_year when present."
    - "Milestone year upper bound is the current calendar year."
  artifacts:
    - path: "frontend/src/components/groups/GroupHistorySection.test.ts"
      provides: "Pure option gating coverage"
    - path: "frontend/src/components/groups/GroupHistoryForm.test.tsx"
      provides: "Form year-bound and suggested-year behavior coverage"
---

<objective>
Add regression tests and the frontend option/year rule foundation for Phase 101 before backend enforcement.
</objective>

<context>
@.planning/phases/101-meilensteine-zeitmanagement-und-anzeige-der-meilensteine-ver/101-CONTEXT.md
@.planning/phases/100-fansub-erfolge-freischaltlogik-und-meilenstein-katalog/100-CONTEXT.md
@AGENTS.md
@docs/engineering/implementation-contract.md
@docs/frontend/ui-system.md
@docs/agent-guidelines-ui.md
</context>

<read_first>
- `frontend/src/components/groups/GroupHistorySection.tsx` - existing `buildHistoryEventOptions`, used-entry handling, and save guards.
- `frontend/src/components/groups/GroupHistorySection.test.ts` - existing tests for founding, first_project, first_release, count achievements, and revival gating.
- `frontend/src/components/groups/GroupHistoryForm.tsx` - current `HISTORY_YEAR_MIN = 1990`, `HISTORY_YEAR_MAX = 2099`, and `YearPicker` wiring.
- `frontend/src/components/groups/GroupHistoryForm.test.tsx` - current form tests for disabled founding and suggested year.
- `frontend/src/components/ui/YearPicker.tsx` - existing constrained year control; reuse it instead of adding a second year picker.
- `frontend/src/lib/group-history-events.ts` - source catalog order and event labels.
</read_first>

<tasks>
<task id="T-101-01-01" type="tdd">
<title>Add RED tests for staged milestone visibility</title>
<files>
- `frontend/src/components/groups/GroupHistorySection.test.ts`
</files>
<action>
Add tests around `buildHistoryEventOptions` that assert:

1. `buildHistoryEventOptions([], null, null, false, false, false)` returns exactly one option with `value === 'founding'`, `disabled === true`, and `disabledReason === 'Gründungsjahr fehlt'`.
2. With `foundedYear = 2007`, no entries, and all unlock booleans false, the option values are exactly `['founding', 'first_project', 'first_release']`; `first_project` remains disabled with `Ausblick/Rollen fehlen`; `first_release` remains disabled with `Release/Kara fehlt`.
3. With `foundedYear = 2007`, entries containing `first_project` only, the full catalog is still not visible and `award`, `team_change`, `website_launch`, `projects_10`, and `releases_100` are absent.
4. With `foundedYear = 2007`, entries containing both `first_project` and `first_release`, later types such as `award`, `team_change`, and `website_launch` are visible subject to their existing rule states.
5. Editing an existing later entry such as `award` keeps `award` visible even if the add-flow full-catalog gate is not passed.
</action>
<acceptance_criteria>
- `frontend/src/components/groups/GroupHistorySection.test.ts` contains the string `returns only locked founding before a founding year exists`.
- `frontend/src/components/groups/GroupHistorySection.test.ts` contains the exact array assertion for `['founding', 'first_project', 'first_release']`.
- Running `npm --prefix frontend test -- GroupHistorySection.test.ts` fails before implementation and passes after implementation.
</acceptance_criteria>
</task>

<task id="T-101-01-02" type="execute">
<title>Implement staged option gating in buildHistoryEventOptions</title>
<files>
- `frontend/src/components/groups/GroupHistorySection.tsx`
</files>
<action>
Update `buildHistoryEventOptions` so the add-flow gates are applied before the existing per-achievement rules:

- If `foundedYear` is null/undefined/0 and `editTarget` is null, return only `founding` disabled with `disabledReason: 'Gründungsjahr fehlt'`.
- Define `hasFirstProjectEntry` from `entries.some((entry) => entry.event_type === 'first_project')`.
- Define `hasFirstReleaseEntry` from `entries.some((entry) => entry.event_type === 'first_release')`.
- Define `fullCatalogUnlocked = hasFirstProjectEntry && hasFirstReleaseEntry`.
- Before the full catalog is unlocked, allow only `founding`, `first_project`, and `first_release` in add mode.
- If `editTarget` is not null, always allow `editTarget.event_type` through the visibility gate so existing entries can be edited.
- Preserve all existing single-use hiding and disabled-reason behavior after the stage gate.
</action>
<acceptance_criteria>
- `frontend/src/components/groups/GroupHistorySection.tsx` contains `fullCatalogUnlocked`.
- `frontend/src/components/groups/GroupHistorySection.tsx` still contains the existing disabled reason strings `Ausblick/Rollen fehlen` and `Release/Kara fehlt`.
- `npm --prefix frontend test -- GroupHistorySection.test.ts` exits 0.
</acceptance_criteria>
</task>

<task id="T-101-01-03" type="tdd">
<title>Add RED tests for dynamic YearPicker bounds</title>
<files>
- `frontend/src/components/groups/GroupHistoryForm.test.tsx`
- `frontend/src/components/groups/GroupHistoryForm.tsx`
</files>
<action>
Add tests that render `GroupHistoryForm` with new year-bound props and assert:

1. With `minYear={2007}` and `maxYear={2026}`, opening the year picker does not show year `2006` and does show `2007`.
2. With `maxYear={2026}`, opening the year picker does not show year `2027`.
3. Selecting available `founding` with `suggestedYear: 2007` still pre-fills `year: '2007'`.
</action>
<acceptance_criteria>
- `frontend/src/components/groups/GroupHistoryForm.test.tsx` contains `does not offer years before the founding year`.
- `frontend/src/components/groups/GroupHistoryForm.test.tsx` contains `does not offer future years`.
- Running `npm --prefix frontend test -- GroupHistoryForm.test.tsx` fails before implementation and passes after implementation.
</acceptance_criteria>
</task>

<task id="T-101-01-04" type="execute">
<title>Thread year bounds from GroupHistorySection into GroupHistoryForm</title>
<files>
- `frontend/src/components/groups/GroupHistoryForm.tsx`
- `frontend/src/components/groups/GroupHistorySection.tsx`
</files>
<action>
Replace the hardcoded `HISTORY_YEAR_MAX = 2099` path with dynamic props:

- In `GroupHistoryForm.tsx`, add optional props `minYear?: number` and `maxYear?: number`.
- Keep a fallback constant `HISTORY_YEAR_MIN = 1990`.
- Replace `HISTORY_YEAR_MAX = 2099` with a current-year fallback computed as `new Date().getFullYear()`.
- Pass `minYear` and `maxYear` into the existing `YearPicker`.
- In `GroupHistorySection.tsx`, compute `historyMinYear = foundedYear ?? 1990`.
- In `GroupHistorySection.tsx`, compute `historyMaxYear = new Date().getFullYear()`.
- Pass `minYear={historyMinYear}` and `maxYear={historyMaxYear}` to `GroupHistoryForm`.
</action>
<acceptance_criteria>
- `frontend/src/components/groups/GroupHistoryForm.tsx` no longer contains `HISTORY_YEAR_MAX = 2099`.
- `frontend/src/components/groups/GroupHistorySection.tsx` contains `new Date().getFullYear()`.
- `npm --prefix frontend test -- GroupHistoryForm.test.tsx GroupHistorySection.test.ts` exits 0.
</acceptance_criteria>
</task>
</tasks>

<threat_model>
## Threat Model

- Severity: Medium. Frontend-only year bounds can be bypassed through direct API calls; Plan 101-02 must add backend validation.
- Severity: Low. Hiding options can accidentally hide the event currently being edited; D-06 and tests prevent edit dead ends.
- Severity: Low. Date precision is year-only by schema; this phase must not invent full-date persistence.
</threat_model>

<verification>
- `npm --prefix frontend test -- GroupHistorySection.test.ts GroupHistoryForm.test.tsx`
- `npm --prefix frontend run typecheck`
- `git diff --check`
</verification>

<success_criteria>
- The add selector is stage-gated exactly as D-03 through D-05 describe.
- The year picker offers no year before founded_year and no future year.
- Existing Phase 100 disabled/hidden behavior remains covered by tests.
</success_criteria>
