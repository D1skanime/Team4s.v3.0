---
phase: 83
plan: 02
type: execute
wave: 2
depends_on: [83-01]
files_modified:
  - backend/internal/permissions/permissions.go
  - backend/internal/repository/authz_permissions.go
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05]

must_haves:
  truths:
    - "per D-01/D-04: `CanForReleaseVersion` erlaubt nur Actors mit gültiger Contribution (Projekt-Default oder Release-Override)"
    - "per D-05: `fansub_lead` UND `project_lead` haben immer Zugriff — Leader-Check läuft VOR Contribution-Auflösung"
    - "per D-05 Datenquelle: `project_lead` kommt aus `fansub_group_member_roles.role` — GENAU der gleiche Pfad wie `fansub_lead` via `ListActorGroupRoles` (VERIFIED: authz_permissions.go Z. 161–168)"
    - "per D-02: `ListActorContributionRolesForVersion` gibt versions-spezifische Contribution-Rollen zurück; Fallback auf anime-weite Contributions wenn kein Override existiert"
    - "per D-03: Absenz im Override-Satz (Actor fehlt in der Liste für diese Release-Version) = kein Recht für dieses Release"
    - "Wave-0-Tests aus Plan 01 sind nach diesem Plan grün (inkl. `TestCanForReleaseVersionProjectLeadBypass`)"
    - "Resolver-Interface enthält neue Methode `ListActorContributionRolesForVersion(ctx, appUserID, releaseVersionID) ([]string, error)`"
  artifacts:
    - path: "backend/internal/permissions/permissions.go"
      provides: "Umgebautes CanForReleaseVersion mit Leader-Bypass + Contribution-Auflösung"
      contains: "ListActorContributionRolesForVersion"
    - path: "backend/internal/repository/authz_permissions.go"
      provides: "Neue Methode ListActorContributionRolesForVersion mit zweistufiger SQL-Auflösung"
  key_links:
    - from: "permissions.go CanForReleaseVersion"
      to: "authz_permissions.go ListActorContributionRolesForVersion"
      via: "s.resolver.ListActorContributionRolesForVersion(ctx, actor.AppUserID, releaseVersionID)"
      pattern: "ListActorContributionRolesForVersion"
    - from: "authz_permissions.go ListActorContributionRolesForVersion"
      to: "anime_contributions + anime_contribution_roles"
      via: "SQL JOIN release_version_groups WHERE release_version_id = $versionID"
      pattern: "anime_contributions.*release_version_id"
---

<objective>
Permission-Umbau: `CanForReleaseVersion` von gruppenrollen-basiert auf contribution-basierte Auflösung umstellen (D-01..D-05).

Purpose: Das ist die sicherheitskritischste Änderung in Phase 83. Nach diesem Plan steuern `anime_contributions` die Bearbeitungsrechte für Release-Inhalte — nicht mehr nur `fansub_group_member_roles`. Ein User ohne Contribution kann an einem Release nichts schreiben/hochladen (D-04), während Leaders immer handeln können (D-05).

Der `project_lead`-Bypass (D-05) ist strukturell identisch zum `fansub_lead`-Bypass: `ListActorGroupRoles` JOINt `fansub_group_member_roles` und gibt `role` zurück — sowohl `fansub_lead` als auch `project_lead` werden in dieser Tabelle gespeichert. Kein separater Abfragepfad nötig (VERIFIED: authz_permissions.go Z. 161–168).

Output: Erweitertes `Resolver`-Interface + neue Repository-Methode + umgebautes `CanForReleaseVersion`. Wave-0-Tests aus Plan 01 werden grün.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-CONTEXT.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-RESEARCH.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-PATTERNS.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-01-SUMMARY.md

<interfaces>
<!-- Aus backend/internal/permissions/permissions.go (345 Zeilen) -->
<!--
// Resolver-Interface (Z. 181–187) — NEU: ListActorContributionRolesForVersion hinzufügen:
type Resolver interface {
  ResolveFansubGroup(ctx, fansubGroupID int64) (*Context, error)
  ResolveRelease(ctx, releaseID int64) (*Context, error)
  ResolveReleaseVersion(ctx, releaseVersionID int64) (*Context, error)
  ResolveReleaseVersionMedia(ctx, relationID int64) (*Context, error)
  ListActorGroupRoles(ctx, appUserID int64, fansubGroupID int64) ([]string, error)
  // NEU:
  ListActorContributionRolesForVersion(ctx, appUserID int64, releaseVersionID int64) ([]string, error)
}

// Actor struct (relevant fields):
// IsPlatformAdmin bool
// AppUserID int64
// Status string

// ReasonCodes (aus permissions.go):
// ReasonUnauthorized, ReasonDisabledUser, ReasonNoMembership, ReasonInsufficientRole,
// ReasonResourceNotFound, ReasonAllowed, ReasonPlatformAdmin

// RoleFansubLead = "fansub_lead" (Z. 39)
// RoleProjectLead = "project_lead" (Z. 40)
// roleMatrix: map[string][]Action (Z. 24–75)
// Beide Leader-Rollen sind in roleMatrix mit privilegierten Actions eingetragen
-->

<!-- Aus backend/internal/repository/authz_permissions.go (188 Zeilen) -->
<!--
// AuthzRepository struct mit Feld db *pgxpool.Pool
// ListActorGroupRoles(ctx, appUserID int64, fansubGroupID int64) ([]string, error) — Vorlage für neue Methode
// ListActorGroupRoles Z. 161–168:
//   SELECT fgr.role
//   FROM fansub_group_members fgm
//   JOIN fansub_group_member_roles fgr ON fgr.fansub_group_member_id = fgm.id
//   WHERE fgm.app_user_id = $1
//     AND fgm.fansub_group_id = $2
//     AND fgm.status = 'active'
//
// SICHERHEITSHINWEIS: project_lead wird in fansub_group_member_roles.role gespeichert.
// ListActorGroupRoles gibt project_lead zurück — KEIN separater Abfragepfad nötig (D-05 ist vollständig abgedeckt).
-->

<!-- Aus database/migrations/0091_anime_contributions_release_version.up.sql -->
<!-- anime_contributions hat: id, fansub_group_id, anime_id, member_id, release_version_id (nullable) -->
<!-- UNIQUE (fansub_group_id, anime_id, member_id, release_version_id) NULLS NOT DISTINCT -->

<!-- anime_contribution_roles: id, anime_contribution_id, role_code -->
<!-- release_version_groups: release_version_id, fansub_group_id -->
<!-- members: id (member_id in anime_contributions) -->
<!-- fansub_group_members: id, member_id, app_user_id, fansub_group_id, status -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ListActorContributionRolesForVersion in authz_permissions.go</name>
  <files>backend/internal/repository/authz_permissions.go</files>
  <read_first>
    - backend/internal/repository/authz_permissions.go (vollständig: ListActorGroupRoles-Muster Z. 156–191, AuthzRepository-Struct)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-PATTERNS.md (Abschnitt authz_permissions.go — Neue Methode Z. 194–219)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-RESEARCH.md (Pattern 1 — zweistufige Query Z. 210–241)
    - database/migrations/0091_anime_contributions_release_version.up.sql (UNIQUE-Constraint, Spalten)
    - database/migrations/0085_anime_contribution_roles_catalog.up.sql (role_definitions, anime_contribution_roles)
  </read_first>
  <behavior>
    - Test: Für einen Actor mit versions-spezifischer Contribution (release_version_id = X) gibt die Methode dessen role_codes zurück
    - Test: Für einen Actor ohne versions-spezifische Contribution aber mit anime-weiter Contribution (release_version_id IS NULL) gibt die Methode dessen role_codes zurück (Fallback D-02)
    - Test: Für einen Actor ohne jede Contribution gibt die Methode eine leere Liste zurück (kein Panic, kein Fehler)
    - Test: appUserID=0 oder releaseVersionID=0 gibt nil, nil zurück (Defensiv-Check)
  </behavior>
  <action>
Neue Methode `ListActorContributionRolesForVersion` an `AuthzRepository` anhängen. Die Methode folgt dem Muster von `ListActorGroupRoles` (gleiche Error-Handling-Struktur, gleicher Rows-Loop).

Implementierung in zwei Schritten:

Schritt 1 — Versions-spezifische Query (release_version_id = $versionID):
JOIN fansub_group_members über member_id und fgsub_group_id, gefiltert auf app_user_id = $appUserID und fgm.status = 'active', JOIN anime_contribution_roles für role_code. WHERE ac.release_version_id = $versionID.

Schritt 2 — Nur ausführen wenn Schritt 1 keine Ergebnisse liefert (Fallback anime-weit):
JOIN release_versions + fansub_releases um anime_id der Version zu ermitteln. WHERE ac.release_version_id IS NULL AND ac.anime_id = fr.anime_id AND ac.fansub_group_id IN (SELECT fansub_group_id FROM release_version_groups WHERE release_version_id = $versionID) AND fgm.status = 'active'.

Defensiv-Check am Anfang: `appUserID <= 0 || releaseVersionID <= 0` → return nil, nil.

Error-Format: `fmt.Errorf("list actor contribution roles version=%d user=%d step=%d: %w", releaseVersionID, appUserID, step, err)`.

WICHTIG: `ListActorGroupRoles` wird NICHT entfernt — weiterhin für Leader-Check in permissions.go benötigt.

Der `fansub_group_id`-Scope im Join auf `fansub_group_members` begrenzt Zugriff auf korrekte Gruppe (Security: verhindert Cross-Gruppen-Leckage, T-83-01).
  </action>
  <verify>
    <automated>cd backend && go test ./internal/repository/... -run TestListActorContributionRolesForVersion -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    `ListActorContributionRolesForVersion` existiert in `authz_permissions.go`. `go build ./internal/repository/...` kompiliert ohne Fehler.
  </done>
  <acceptance_criteria>
    - `authz_permissions.go` enthält `func (r *AuthzRepository) ListActorContributionRolesForVersion(ctx context.Context, appUserID int64, releaseVersionID int64) ([]string, error)`
    - Methode implementiert zweistufige Auflösung: Schritt 1 versions-spezifisch, Schritt 2 nur wenn Schritt 1 leer (anime-weit, Fallback)
    - `fansub_group_members.app_user_id = $appUserID` und `fgm.status = 'active'` sind in beiden Queries vorhanden (Security-Scope T-83-01)
    - `go build ./internal/repository/...` exit 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolver-Interface erweitern + CanForReleaseVersion umbauen</name>
  <files>backend/internal/permissions/permissions.go</files>
  <read_first>
    - backend/internal/permissions/permissions.go (vollständig: roleMatrix Z. 24–75, RoleFansubLead Z. 39, RoleProjectLead Z. 40, Actor-Struct Z. 161–165, canForContext Z. 90–145, CanForReleaseVersion Z. 240–244, denied/roleAllows-Helpers)
    - backend/internal/repository/authz_permissions.go (ListActorGroupRoles Z. 156–168 — Query-Evidenz dass project_lead in fansub_group_member_roles.role gespeichert wird; KEIN separater Pfad für project_lead nötig)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-PATTERNS.md (Abschnitt permissions.go — Umbau-Muster Z. 60–135)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-RESEARCH.md (Pitfall 1 — Leader-Reihenfolge Z. 346–355, Pattern 1 Z. 185–210)
  </read_first>
  <behavior>
    - Test: Actor ohne Contribution (und ohne fansub_lead/project_lead-Rolle) → result.Allowed = false, ReasonCode = ReasonNoMembership
    - Test: Actor mit fansub_lead-Rolle (kein Contribution-Eintrag) → result.Allowed = true (Leader-Bypass, D-05)
    - Test: Actor mit project_lead-Rolle aus ListActorGroupRoles (kein Contribution-Eintrag) → result.Allowed = true (Leader-Bypass, D-05; project_lead wird via fansub_group_member_roles wie fansub_lead aufgelöst)
    - Test: Actor mit gültiger Contribution (role_code "translator") + Action, die translator erlaubt → result.Allowed = true
    - Test: Actor mit Contribution aber mit role_code, der die angeforderte Action nicht erlaubt → result.Allowed = false, ReasonCode = ReasonInsufficientRole
  </behavior>
  <action>
Zwei Änderungen in `permissions.go`:

**Änderung 1: Resolver-Interface erweitern**
Im `Resolver`-Interface die neue Methode `ListActorContributionRolesForVersion(ctx context.Context, appUserID int64, releaseVersionID int64) ([]string, error)` hinzufügen.

Alle bestehenden `Resolver`-Implementierungen müssen diese Methode implementieren. Die `AuthzRepository`-Implementierung kommt aus Task 1 dieses Plans.

**Änderung 2: CanForReleaseVersion umbauen**
Die aktuelle Implementierung (Delegation an `canForContext`) ersetzen durch eigenständige Methode:

Ablauf (strikt in dieser Reihenfolge — Pitfall 1 aus RESEARCH.md):
1. Basis-Checks: nil-Guard → deny(ReasonUnauthorized); `actor.AppUserID <= 0` → deny; `actor.Status == "disabled"` → deny(ReasonDisabledUser); `actor.IsPlatformAdmin` → allow(ReasonPlatformAdmin)
2. `ResolveReleaseVersion(ctx, releaseVersionID)` → resourceCtx; nil-Prüfung → deny(ReasonResourceNotFound)
3. **Leader-Check ZUERST (D-05)**: Für jede `fansubGroupID` in `resourceCtx.FansubGroupIDs`: `ListActorGroupRoles(ctx, actor.AppUserID, fansubGroupID)` → hat role `fansub_lead` ODER `project_lead` (beide in `fansub_group_member_roles.role` gespeichert — gleicher Pfad) UND `roleMatrix[role]` enthält action? → allow immediately
4. **Contribution-Check (D-01..D-04)**: `ListActorContributionRolesForVersion(ctx, actor.AppUserID, releaseVersionID)` → roleCodes; falls ein roleCode in `roleMatrix[code]` die action enthält → allow; falls roleCodes nicht leer aber keine action erlaubt → deny(ReasonInsufficientRole); falls roleCodes leer → deny(ReasonNoMembership)

`roleAllows`-Hilfsfunktion (bereits vorhanden) weiterverwenden. `denied`-Hilfsfunktion weiterverwenden. `canForContext` bleibt für andere Methoden erhalten — NICHT entfernen.

KRITISCH: Leader-Bypass (Schritt 3) MUSS vor Contribution-Check (Schritt 4) liegen. Ein `fansub_lead` oder `project_lead` hat oft selbst keine Contribution — würde sonst fälschlicherweise abgelehnt.

SICHERHEITSNACHWEIS project_lead: `ListActorGroupRoles` liest `fansub_group_member_roles.role` — diese Spalte speichert sowohl `fansub_lead` als auch `project_lead`. Kein separater Datenbankpfad erforderlich. Dies ist bereits im Test `TestCanForReleaseVersionProjectLeadBypass` aus Plan 01 abgedeckt.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/permissions/... -run "TestCanForReleaseVersion" -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    Wave-0-Tests aus Plan 01 (`TestCanForReleaseVersionContributionRequired`, `TestCanForReleaseVersionLeaderBypass`, `TestCanForReleaseVersionProjectLeadBypass`, `TestCanForReleaseVersionWithContribution`, `TestCanForReleaseVersionAbsenceInOverride`) sind grün. `go build ./...` kompiliert ohne Fehler.
  </done>
  <acceptance_criteria>
    - `Resolver`-Interface enthält `ListActorContributionRolesForVersion(ctx context.Context, appUserID int64, releaseVersionID int64) ([]string, error)`
    - `CanForReleaseVersion` ruft zuerst `ListActorGroupRoles` (Leader-Check für fansub_lead UND project_lead) auf, danach erst `ListActorContributionRolesForVersion`
    - `go test ./backend/internal/permissions/... -run TestCanForReleaseVersionLeaderBypass` → PASS
    - `go test ./backend/internal/permissions/... -run TestCanForReleaseVersionProjectLeadBypass` → PASS (project_lead via ListActorGroupRoles aufgelöst — kein separater Query-Pfad nötig, da fansub_group_member_roles.role beide Rollen speichert — EVIDENZ: authz_permissions.go Z. 161–168 gelesen in read_first)
    - `go test ./backend/internal/permissions/... -run TestCanForReleaseVersionContributionRequired` → PASS
    - `cd backend && go build ./...` exit 0 (alle Interface-Implementierungen kompilieren nach Interface-Erweiterung)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP-Request → CanForReleaseVersion | Nicht-autorisierte Requests dürfen nicht in Release-Inhalte schreiben |
| fansub_group_members.app_user_id | Actor-Identität ist an app_user_id gebunden, nicht frei wählbar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | Elevation of Privilege | `CanForReleaseVersion` — fehlende Contribution | mitigate | Actor ohne Contribution → `ReasonNoMembership` (deny). `ListActorContributionRolesForVersion` ist `fansub_group_id`-scoped via JOIN auf `release_version_groups`, verhindert Cross-Gruppen-Leckage |
| T-83-02 | Elevation of Privilege | Leader-Bypass-Reihenfolge (fansub_lead + project_lead) | mitigate | Leader-Check (Schritt 3) läuft VOR Contribution-Check (Schritt 4). Tests `TestCanForReleaseVersionLeaderBypass` und `TestCanForReleaseVersionProjectLeadBypass` verifikieren beide Leader-Rollen. Beide werden über denselben Pfad (`fansub_group_member_roles.role` via `ListActorGroupRoles`) aufgelöst |
| T-83-03 | Elevation of Privilege | Override-Absenz | mitigate | Absenz im Override-Satz (leere roleCodes für diese versionID) → `ReasonNoMembership`. Test `TestCanForReleaseVersionAbsenceInOverride` verifikiert das |
| T-83-CROSSGROUP | Tampering | `ListActorContributionRolesForVersion` Cross-Gruppen | mitigate | JOIN `fansub_group_members` auf `fansub_group_id = ac.fansub_group_id` begrenzt auf richtige Gruppe; zweite Query zusätzlich `IN (SELECT fansub_group_id FROM release_version_groups WHERE release_version_id = $versionID)` |
| T-83-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Pakete |
</threat_model>

<verification>
- `cd backend && go test ./internal/permissions/... -run "TestCanForReleaseVersion" -v` → alle fünf Tests PASS
- `cd backend && go test ./internal/repository/... -run TestListActorContributionRolesForVersion -v` → PASS oder SKIP (Wave-0-Stub)
- `cd backend && go build ./...` → exit 0 (alle Resolver-Implementierungen kompilieren nach Interface-Erweiterung)
</verification>

<success_criteria>
Permission-Umbau abgeschlossen: `CanForReleaseVersion` nutzt Contribution-basierte Auflösung mit Leader-Bypass (fansub_lead UND project_lead via `ListActorGroupRoles` — gleicher Datenpfad, keine Sonderlösung). Wave-0-Tests (D-01..D-05, inkl. project_lead) sind grün. Das Backend gibt jetzt nur noch Actors mit gültiger Contribution (oder Leader-Rolle) Schreibrechte auf Release-Inhalte.
</success_criteria>

<output>
Create `.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-02-SUMMARY.md` when done.
</output>
