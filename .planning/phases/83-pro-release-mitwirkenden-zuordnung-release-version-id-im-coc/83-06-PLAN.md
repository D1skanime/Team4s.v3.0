---
phase: 83
plan: 06
type: execute
wave: 4
depends_on: [83-04, 83-05]
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
autonomous: true
requirements: [D-06, D-08, D-11, D-12, D-15]

must_haves:
  truths:
    - "per D-06: Jede Release-Version-Zeile im Cockpit (tab=releases) hat einen 'Mitwirkende'-Button"
    - "per D-08: Cockpit zeigt pro Release-Zeile einen Status-Badge (Projektteam / Eigene Besetzung / Mitwirkende fehlen)"
    - "per D-11: Klick auf 'Mitwirkende' öffnet ReleaseContributionDrawer mit der richtigen releaseVersionId + releaseTitle"
    - "page.tsx wächst durch diesen Plan um maximal ~40 Zeilen (Einstiegspunkt, State, Badge — keine Drawer-Logik)"
  artifacts:
    - path: "frontend/src/app/admin/fansubs/[id]/edit/page.tsx"
      provides: "'Mitwirkende'-Button pro Release-Zeile + Drawer-Mount + Status-Badge"
      contains: "ReleaseContributionDrawer"
  key_links:
    - from: "page.tsx Release-Zeile"
      to: "ReleaseContributionDrawer.tsx"
      via: "contributionDrawerOpen && ReleaseContributionDrawer open={...}"
      pattern: "ReleaseContributionDrawer"
---

<objective>
Cockpit-Verdrahtung: `page.tsx` erhält den „Mitwirkende"-Button pro Release-Zeile, Status-Badge (Projektteam/Eigene Besetzung/Mitwirkende fehlen) und den `ReleaseContributionDrawer`-Mount.

Purpose: Der Drawer aus Plan 05 muss vom Cockpit aus erreichbar sein. Plan 06 fügt ausschließlich den Einstiegspunkt in `page.tsx` ein — keine Drawer-Logik. Das hält `page.tsx` (bereits >3200 Zeilen) so schlank wie möglich.

Output: `page.tsx` importiert und mountet `ReleaseContributionDrawer`. Release-Zeilen haben „Mitwirkende"-Button + Badge.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-CONTEXT.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-RESEARCH.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-PATTERNS.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-UI-SPEC.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-05-SUMMARY.md

<interfaces>
<!-- Aus 83-PATTERNS.md Abschnitt page.tsx (Z. 731–765) -->
<!--
// State-Pattern (analog bestehenden Drawer-States in page.tsx):
const [contributionDrawerOpen, setContributionDrawerOpen] = useState(false)
const [contributionDrawerVersionId, setContributionDrawerVersionId] = useState<number | null>(null)
const [contributionDrawerTitle, setContributionDrawerTitle] = useState<string>('')

// Open-Handler:
const openContributionDrawer = (versionId: number, title: string) => {
  setContributionDrawerVersionId(versionId)
  setContributionDrawerTitle(title)
  setContributionDrawerOpen(true)
}

// Drawer-Mount (außerhalb Tabellen-DOM-Struktur):
{contributionDrawerOpen && contributionDrawerVersionId ? (
  <ReleaseContributionDrawer
    open={contributionDrawerOpen}
    fansubId={fansubID}
    animeId={animeId}              // aus Release-Kontext
    releaseVersionId={contributionDrawerVersionId}
    releaseTitle={contributionDrawerTitle}
    onClose={() => setContributionDrawerOpen(false)}
    onSaved={() => { void refreshAnimeReleases(); }}
  />
) : null}
-->

<!-- Cockpit Release-Zeilen: Z. ~3080ff in page.tsx -->
<!-- Suchen nach: releaseVersions?.map, release-versions-Rendering, bestehende Release-Aktions-Buttons -->

<!-- Aus 83-UI-SPEC.md Copywriting + Color -->
<!--
// Badge Cockpit:
// "Projektteam" variant="muted" — wenn meta.is_override = false
// "Eigene Besetzung" variant="info" — wenn meta.is_override = true
// "Mitwirkende fehlen" variant="warning" — wenn keine Contributions vorhanden
// Aber: Badge braucht has_override-Info pro Release-Zeile (Pitfall 6 aus RESEARCH.md)
// Pragmatische Lösung: Badge ohne API-Call beim Listing (nur nach Drawer-Save aktualisieren)
// Oder: has_override aus listEffectiveContributionsForVersion.meta.is_override cachen
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: State + Handler + Drawer-Mount + Mitwirkende-Button in page.tsx</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/page.tsx</files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx (MUSS vollständig gelesen werden — Zeilenreferenzen für: bestehende Drawer-States ~Z. 1120–1131, openAnimeContributions-Handler ~Z. 1865–1886, Release-Versions-Rendering ~Z. 3080ff, vorhandene Drawer-Mounts am Ende der Seite, fansubID-Variable, refreshAnimeReleases-Funktion, anime.id-Zugriff)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-PATTERNS.md (Abschnitt page.tsx Z. 731–765)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-UI-SPEC.md (Primitive-Inventar, Copywriting: "Mitwirkende", aria-label "Mitwirkende für {Release-Titel} bearbeiten")
    - frontend/src/app/admin/fansubs/[id]/edit/ReleaseContributionDrawer.tsx (Props-Interface — aus Plan-05-Output)
  </read_first>
  <action>
In `page.tsx` chirurgisch drei Ergänzungen machen (minimale Änderungen, kein Umbau bestehender Logik):

**Ergänzung 1 — Imports (oben in der Datei):**
Import hinzufügen: `import { ReleaseContributionDrawer } from './ReleaseContributionDrawer'`

**Ergänzung 2 — State (nach bestehenden Drawer-States):**
Drei neue State-Variablen analog zu bestehenden Release-Drawer-States in `page.tsx`:
```typescript
const [contributionDrawerOpen, setContributionDrawerOpen] = useState(false)
const [contributionDrawerVersionId, setContributionDrawerVersionId] = useState<number | null>(null)
const [contributionDrawerTitle, setContributionDrawerTitle] = useState<string>('')
```

**Ergänzung 3 — Handler:**
```typescript
const openContributionDrawer = (versionId: number, title: string) => {
  setContributionDrawerVersionId(versionId)
  setContributionDrawerTitle(title)
  setContributionDrawerOpen(true)
}
```

**Ergänzung 4 — Button pro Release-Versions-Zeile:**
Im Release-Versions-Rendering-Block (~Z. 3080ff) in der Aktions-Reihe jeder Release-Version einen „Mitwirkende"-Button ergänzen:
```tsx
<Button
  variant="subtle"
  size="sm"
  aria-label={`Mitwirkende für ${version.label ?? version.id} bearbeiten`}
  onClick={() => openContributionDrawer(version.id, version.label ?? `Release-Version ${version.id}`)}
>
  Mitwirkende
</Button>
```
(WICHTIG: `version.label`, `version.id` — exakte Feldnamen aus der tatsächlichen Release-Versions-Datenstruktur in `page.tsx` verwenden; beim Lesen bestätigen. Falls kein `version.label` existiert → adäquates Feld nehmen.)

**Ergänzung 5 — Drawer-Mount (am Ende des JSX-Return, nach bestehenden Drawer-Mounts):**
```tsx
{contributionDrawerOpen && contributionDrawerVersionId !== null ? (
  <ReleaseContributionDrawer
    open={contributionDrawerOpen}
    fansubId={fansubID}
    animeId={/* anime.id aus Cockpit-State */}
    releaseVersionId={contributionDrawerVersionId}
    releaseTitle={contributionDrawerTitle}
    onClose={() => setContributionDrawerOpen(false)}
    onSaved={() => { void refreshAnimeReleases?.() }}
  />
) : null}
```
(fansubID, anime.id, refreshAnimeReleases — exakte Variablennamen aus `page.tsx` verwenden; beim Lesen bestätigen.)

KEINE Änderungen an bestehender Logik, bestehenden Drawer-Implementierungen oder bestehenden State-Variablen.
  </action>
  <verify>
    <automated>npm --prefix frontend run typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>
    `page.tsx` enthält Import von `ReleaseContributionDrawer`, neuen State + Handler, Button pro Release-Zeile, Drawer-Mount. TypeScript kompiliert ohne neue Fehler.
  </done>
  <acceptance_criteria>
    - `page.tsx` enthält `import { ReleaseContributionDrawer } from './ReleaseContributionDrawer'`
    - `page.tsx` enthält `contributionDrawerOpen` State-Variable
    - `page.tsx` enthält `openContributionDrawer`-Funktion
    - `page.tsx` enthält `ReleaseContributionDrawer` im JSX (Drawer-Mount)
    - Release-Zeilen-Rendering enthält Button mit Text "Mitwirkende" und `aria-label` mit korrektem Umlaut
    - `npm --prefix frontend run typecheck` exit 0 (keine neuen TypeScript-Fehler durch diesen Plan)
    - Wachstum von `page.tsx` ≤ 45 Zeilen gegenüber Vorher-Zustand
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page.tsx Button → ReleaseContributionDrawer | Props werden nur von Cockpit-State befüllt, nicht von Benutzereingabe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-CLIENT | Tampering | page.tsx openContributionDrawer | accept | `versionId` aus Release-Listen-Daten (API-geliefert), nicht aus DOM-Input. Backend validiert bei jedem Request |
| T-83-01 | Elevation of Privilege | Drawer-Öffnung | accept | Kein Recht-Check im Frontend nötig — Backend-Check in `GetEffectiveContributionsForVersion` (Plan 04) und `handleSave` in `ReleaseContributionDrawer` |
| T-83-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Pakete |
</threat_model>

<verification>
- `npm --prefix frontend run typecheck` → exit 0
- `npm --prefix frontend run lint` → keine neuen Fehler
- `page.tsx` enthält String "ReleaseContributionDrawer" (Import + Verwendung)
- `page.tsx` enthält String "Mitwirkende" als Button-Label
</verification>

<success_criteria>
Cockpit-Verdrahtung vollständig: Jede Release-Versions-Zeile hat einen „Mitwirkende"-Button. Klick öffnet `ReleaseContributionDrawer`. `page.tsx` wächst um maximal ~45 Zeilen. TypeScript kompiliert.
</success_criteria>

<output>
Create `.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-06-SUMMARY.md` when done.
</output>
