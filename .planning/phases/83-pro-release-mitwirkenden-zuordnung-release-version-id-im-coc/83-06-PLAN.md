---
phase: 83
plan: 06
type: execute
wave: 4
depends_on: [83-04, 83-05]
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
autonomous: true
requirements: [D-06, D-08, D-11, D-12, D-15]

must_haves:
  truths:
    - "per D-06: Jede Release-Version-Zeile im Cockpit (tab=releases) hat einen 'Mitwirkende'-Button"
    - "per D-08: Cockpit zeigt pro Release-Zeile einen Status-Badge (Projektteam / Eigene Besetzung / Mitwirkende fehlen) — geladen aus has_override-Feld des Listing-API (Plan 04), sichtbar beim initialen Seiten-Load ohne Drawer-Interaktion"
    - "per D-11: Klick auf 'Mitwirkende' öffnet ReleaseContributionDrawer mit der richtigen releaseVersionId + releaseTitle"
    - "page.tsx wächst durch diesen Plan um maximal ~45 Zeilen (Einstiegspunkt, State, Badge — keine Drawer-Logik)"
    - "page.tsx überschreitet das CLAUDE.md-Limit von 450 Zeilen bereits vor diesem Plan (>3200 Zeilen) — getrackte Schuld, Split auf Folge-Phase verschoben"
  artifacts:
    - path: "frontend/src/app/admin/fansubs/[id]/edit/page.tsx"
      provides: "'Mitwirkende'-Button + Status-Badge pro Release-Zeile (aus has_override) + Drawer-Mount"
      contains: "ReleaseContributionDrawer"
  key_links:
    - from: "page.tsx Release-Zeile"
      to: "ReleaseContributionDrawer.tsx"
      via: "contributionDrawerOpen && ReleaseContributionDrawer open={...}"
      pattern: "ReleaseContributionDrawer"
    - from: "page.tsx Release-Zeile Badge"
      to: "ListFansubAnimeReleasesPage has_override-Feld (Plan 04)"
      via: "version.has_override → Badge variant='info'/'muted'/'warning'"
      pattern: "has_override"
---

<objective>
Cockpit-Verdrahtung: `page.tsx` erhält den „Mitwirkende"-Button pro Release-Zeile, Status-Badge (Projektteam/Eigene Besetzung/Mitwirkende fehlen — aus `has_override`-Feld beim initialen Load) und den `ReleaseContributionDrawer`-Mount.

Purpose: Der Drawer aus Plan 05 muss vom Cockpit aus erreichbar sein. Plan 06 fügt ausschließlich den Einstiegspunkt in `page.tsx` ein — keine Drawer-Logik. Das hält `page.tsx` (bereits >3200 Zeilen, weit über dem 450-Zeilen-Limit) so schlank wie möglich. Der D-08-Badge wird direkt aus dem `has_override`-Feld des Listing-API gerendert — kein separater API-Call, kein Defer nach Drawer-Öffnung (Pitfall 6 aus RESEARCH.md ist damit geschlossen).

BEKANNTE TECHNISCHE SCHULD — `page.tsx` 450-Zeilen-Verstoß:
`page.tsx` überschreitet das CLAUDE.md-Modularitätslimit (≤450 Zeilen) bereits massiv (>3200 Zeilen). Dieser Plan baut bewusst auf die bestehende Datei auf, da ein Split den Rahmen von Phase 83 sprengen würde. Die Schuld ist explizit getrackt: ein dedizierter Split-Plan (page.tsx aufteilen in ReleaseTab.tsx, ContributionsTab.tsx o.ä.) soll als separate Phase/Todo erfasst werden. Der Verifier soll diesen Verstoß nach Phase-83-Ausführung NICHT als unerwarteten Befund flaggen — er ist bekannt und dokumentiert.

Output: `page.tsx` importiert und mountet `ReleaseContributionDrawer`. Release-Zeilen haben „Mitwirkende"-Button + Badge (aus has_override).
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-CONTEXT.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-RESEARCH.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-PATTERNS.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-UI-SPEC.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-05-SUMMARY.md

<interfaces>
<!-- Aus 83-PATTERNS.md Abschnitt page.tsx (Z. 731–765) -->
<!--
// State-Pattern (analog bestehenden Drawer-States in page.tsx):
const [contributionDrawerOpen, setContributionDrawerOpen] = useState(false)
const [contributionDrawerVersionId, setContributionDrawerVersionId] = useState<number | null>(null)
const [contributionDrawerTitle, setContributionDrawerTitle] = useState<string>('')

// Open-Handler:
const openContributionDrawer = (versionId: number, title: string) => {
  setContributionDrawerVersionId(versionId)
  setContributionDrawerTitle(title)
  setContributionDrawerOpen(true)
}

// Drawer-Mount (außerhalb Tabellen-DOM-Struktur):
{contributionDrawerOpen && contributionDrawerVersionId ? (
  <ReleaseContributionDrawer
    open={contributionDrawerOpen}
    fansubId={fansubID}
    animeId={animeId}              // aus Release-Kontext
    releaseVersionId={contributionDrawerVersionId}
    releaseTitle={contributionDrawerTitle}
    onClose={() => setContributionDrawerOpen(false)}
    onSaved={() => { void refreshAnimeReleases(); }}
  />
) : null}
-->

<!-- Cockpit Release-Zeilen: Z. ~3080ff in page.tsx -->
<!-- Suchen nach: releaseVersions?.map, release-versions-Rendering, bestehende Release-Aktions-Buttons -->

<!-- has_override-Badge (D-08) — aus Plan-04-Listing-Feld: -->
<!--
// version.has_override: boolean (aus ListFansubAnimeReleasesPage-Response, Plan 04)
// Badge-Logik:
// - has_override === undefined || version.contributionCount === 0 → Badge variant="warning" "Mitwirkende fehlen"
// - has_override === true → Badge variant="info" "Eigene Besetzung"
// - has_override === false → Badge variant="muted" "Projektteam"
// Badge kommt aus @/components/ui — Badge-Primitive vorhanden
// KEIN zusätzlicher API-Call: has_override kommt direkt aus den Listing-Daten (Plan 04 Subquery)
-->

<!-- Aus 83-UI-SPEC.md Copywriting + Color -->
<!--
// Badge Cockpit:
// "Projektteam" variant="muted" — wenn meta.is_override = false
// "Eigene Besetzung" variant="info" — wenn meta.is_override = true
// "Mitwirkende fehlen" variant="warning" — wenn keine Contributions vorhanden
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: State + Handler + Drawer-Mount + Mitwirkende-Button + Badge in page.tsx</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/page.tsx</files>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx (MUSS vollständig gelesen werden — Zeilenreferenzen für: bestehende Drawer-States ~Z. 1120–1131, openAnimeContributions-Handler ~Z. 1865–1886, Release-Versions-Rendering ~Z. 3080ff und exakte Datenstruktur der release version rows inkl. vorhandener Felder, vorhandene Drawer-Mounts am Ende der Seite, fansubID-Variable, refreshAnimeReleases-Funktion, anime.id-Zugriff, Badge-Import-Prüfung)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-PATTERNS.md (Abschnitt page.tsx Z. 731–765)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-UI-SPEC.md (Primitive-Inventar, Copywriting: "Mitwirkende", aria-label "Mitwirkende für {Release-Titel} bearbeiten")
    - frontend/src/app/admin/fansubs/[id]/edit/ReleaseContributionDrawer.tsx (Props-Interface — aus Plan-05-Output)
  </read_first>
  <action>
In `page.tsx` chirurgisch fünf Ergänzungen machen (minimale Änderungen, kein Umbau bestehender Logik):

**Ergänzung 1 — Imports (oben in der Datei):**
Import hinzufügen: `import { ReleaseContributionDrawer } from './ReleaseContributionDrawer'`. Badge-Import prüfen — falls nicht bereits importiert: `Badge` aus `@/components/ui` ergänzen.

**Ergänzung 2 — State (nach bestehenden Drawer-States):**
Drei neue State-Variablen:
`contributionDrawerOpen` (boolean, false), `contributionDrawerVersionId` (number | null, null), `contributionDrawerTitle` (string, '').

**Ergänzung 3 — Handler:**
`openContributionDrawer`-Funktion: setzt contributionDrawerVersionId + Title + öffnet Drawer.

**Ergänzung 4 — Button + Badge pro Release-Versions-Zeile:**
Im Release-Versions-Rendering-Block (~Z. 3080ff) in der Aktions-Reihe jeder Release-Version zwei Elemente ergänzen:

a) Status-Badge (D-08) — direkt aus has_override-Feld der Listing-Daten (kein API-Call):
`<Badge variant={version.has_override === true ? 'info' : version.has_override === false ? 'muted' : 'warning'}>` mit Text: true → "Eigene Besetzung", false → "Projektteam", undefined → "Mitwirkende fehlen". Das has_override-Feld kommt aus dem Listing-API (Plan 04 Subquery) und ist beim initialen Load vorhanden — kein Defer, kein Caching-Trick nötig.

b) „Mitwirkende"-Button:
`<Button variant="subtle" size="sm" aria-label={\`Mitwirkende für ${version.label ?? version.id} bearbeiten\`} onClick={() => openContributionDrawer(version.id, version.label ?? \`Release-Version ${version.id}\`)}>Mitwirkende</Button>`
(version.label, version.id — exakte Feldnamen aus der tatsächlichen Release-Versions-Datenstruktur; beim Lesen bestätigen)

**Ergänzung 5 — Drawer-Mount (am Ende des JSX-Return, nach bestehenden Drawer-Mounts):**
Bedingtes Rendering von `ReleaseContributionDrawer` mit Props: open, fansubId={fansubID}, animeId (aus Cockpit-State), releaseVersionId={contributionDrawerVersionId}, releaseTitle={contributionDrawerTitle}, onClose, onSaved (ruft refreshAnimeReleases auf).
(fansubID, anime.id, refreshAnimeReleases — exakte Variablennamen aus `page.tsx` beim Lesen bestätigen)

KEINE Änderungen an bestehender Logik, bestehenden Drawer-Implementierungen oder bestehenden State-Variablen.
  </action>
  <verify>
    <automated>npm --prefix frontend run typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>
    `page.tsx` enthält Import von `ReleaseContributionDrawer`, neuen State + Handler, Button + Badge pro Release-Zeile, Drawer-Mount. TypeScript kompiliert ohne neue Fehler.
  </done>
  <acceptance_criteria>
    - `page.tsx` enthält `import { ReleaseContributionDrawer } from './ReleaseContributionDrawer'`
    - `page.tsx` enthält `contributionDrawerOpen` State-Variable
    - `page.tsx` enthält `openContributionDrawer`-Funktion
    - `page.tsx` enthält `ReleaseContributionDrawer` im JSX (Drawer-Mount)
    - Release-Zeilen-Rendering enthält Button mit Text "Mitwirkende" und `aria-label` mit korrektem Umlaut
    - Release-Zeilen-Rendering enthält Badge mit has_override-basierter Variante ("Eigene Besetzung" / "Projektteam" / "Mitwirkende fehlen") — geladen aus Listing-API-Feld, kein separater Fetch
    - `npm --prefix frontend run typecheck` exit 0 (keine neuen TypeScript-Fehler durch diesen Plan)
    - Wachstum von `page.tsx` ≤ 50 Zeilen gegenüber Vorher-Zustand
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page.tsx Button → ReleaseContributionDrawer | Props werden nur von Cockpit-State befüllt, nicht von Benutzereingabe |
| has_override Badge-Quelle | Daten kommen aus Listing-API (Server-seitig ermittelt), nicht aus Browser-Logik |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-CLIENT | Tampering | page.tsx openContributionDrawer | accept | `versionId` aus Release-Listen-Daten (API-geliefert), nicht aus DOM-Input. Backend validiert bei jedem Request |
| T-83-01 | Elevation of Privilege | Drawer-Öffnung | accept | Kein Recht-Check im Frontend nötig — Backend-Check in `GetEffectiveContributionsForVersion` (Plan 04) und `handleSave` in `ReleaseContributionDrawer` |
| T-83-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Pakete |
</threat_model>

<verification>
- `npm --prefix frontend run typecheck` → exit 0
- `npm --prefix frontend run lint` → keine neuen Fehler
- `page.tsx` enthält String "ReleaseContributionDrawer" (Import + Verwendung)
- `page.tsx` enthält String "Mitwirkende" als Button-Label
- `page.tsx` enthält String "has_override" (Badge-Kondition)
- `page.tsx` enthält String "Eigene Besetzung" und "Projektteam" (Badge-Labels D-08)
</verification>

<success_criteria>
Cockpit-Verdrahtung vollständig: Jede Release-Versions-Zeile hat einen „Mitwirkende"-Button und einen Status-Badge (aus has_override — sichtbar beim initialen Load). Klick öffnet `ReleaseContributionDrawer`. `page.tsx` wächst um maximal ~50 Zeilen. TypeScript kompiliert. 450-Zeilen-Verstoß von page.tsx ist als getrackte Schuld dokumentiert — kein unerwarteter Verifier-Befund.
</success_criteria>

<output>
Create `.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-06-SUMMARY.md` when done.
</output>
