---
phase: 83
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/permissions/permissions_test.go
  - backend/internal/repository/authz_permissions_test.go
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05]

must_haves:
  truths:
    - "Wave-0-Testfälle für D-01/D-04: `TestCanForReleaseVersionContributionRequired` existiert und schlägt initial rot fehl"
    - "Wave-0-Testfall für D-05 fansub_lead: `TestCanForReleaseVersionLeaderBypass` existiert und schlägt initial rot fehl"
    - "Wave-0-Testfall für D-05 project_lead: `TestCanForReleaseVersionProjectLeadBypass` existiert und schlägt initial rot fehl"
    - "Wave-0-Testfall für D-02: `TestListActorContributionRolesForVersionFallback` existiert und schlägt initial rot fehl (Methode fehlt noch)"
    - "Wave-0-Testfall für D-03: `TestCanForReleaseVersionAbsenceInOverride` existiert und schlägt initial rot fehl"
    - "Alle Tests kompilieren (auch wenn sie scheitern) — d. h. kein Compiler-Fehler durch fehlende Symbole"
  artifacts:
    - path: "backend/internal/permissions/permissions_test.go"
      provides: "Wave-0-Testfälle für CanForReleaseVersion (contribution-basiert, fansub_lead-Bypass, project_lead-Bypass, Absenz)"
    - path: "backend/internal/repository/authz_permissions_test.go"
      provides: "Wave-0-Testfall für ListActorContributionRolesForVersion (Fallback-Logik)"
  key_links:
    - from: "permissions_test.go"
      to: "permissions.go CanForReleaseVersion"
      via: "s.CanForReleaseVersion(ctx, actor, action, versionID)"
      pattern: "CanForReleaseVersion"
    - from: "authz_permissions_test.go"
      to: "authz_permissions.go ListActorContributionRolesForVersion"
      via: "r.ListActorContributionRolesForVersion(ctx, appUserID, versionID)"
      pattern: "ListActorContributionRolesForVersion"
---

<objective>
Wave-0-Testgerüst für den sicherheitskritischen Permission-Umbau (D-01..D-05).

Ziel: Fünf rote Testfälle schreiben, die nach Plan 02 grün werden. Die Tests definieren das gewünschte Verhalten bevor Produktionscode geändert wird. Das stellt sicher, dass der Permission-Umbau korrekt implementiert ist und keine Regression einführt.

Purpose: Sicherheitskritischer Permission-Umbau braucht verifizierbares Vorher-/Nachher-Verhalten. Ohne Wave-0-Tests könnte Plan 02 den Umbau falsch implementieren, ohne dass es auffällt. Insbesondere der `project_lead`-Bypass (D-05) muss explizit getestet werden, da `project_lead` aus `fansub_group_member_roles` kommt — dieselbe Tabelle wie `fansub_lead`.

Output: Zwei Testdateien mit fünf neuen, rot-fehlschlagenden Testfällen.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-CONTEXT.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-RESEARCH.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-PATTERNS.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-VALIDATION.md

<interfaces>
<!-- Aus backend/internal/permissions/permissions.go (345 Zeilen) -->
<!-- Resolver-Interface (Z. 181–187): -->
<!--
type Resolver interface {
  ResolveFansubGroup(ctx context.Context, fansubGroupID int64) (*Context, error)
  ResolveRelease(ctx context.Context, releaseID int64) (*Context, error)
  ResolveReleaseVersion(ctx context.Context, releaseVersionID int64) (*Context, error)
  ResolveReleaseVersionMedia(ctx context.Context, relationID int64) (*Context, error)
  ListActorGroupRoles(ctx context.Context, appUserID int64, fansubGroupID int64) ([]string, error)
}
-->
<!-- Aktuelles CanForReleaseVersion (Z. 240–244): -->
<!--
func (s *Service) CanForReleaseVersion(ctx context.Context, actor Actor, action Action, releaseVersionID int64) (Result, error) {
  return s.canForContext(ctx, actor, []Action{action}, func(ctx context.Context) (*Context, error) {
    return s.resolver.ResolveReleaseVersion(ctx, releaseVersionID)
  })
}
-->
<!-- Actor struct: IsPlatformAdmin bool, AppUserID int64, Status string -->
<!-- Actions: ActionReleaseVersionNotesWrite, ActionReleaseVersionMediaUpload etc. -->
<!-- roleMatrix: map[string][]Action — z. B. "translator" → [ActionReleaseVersionNotesWrite] -->
<!-- RoleFansubLead = "fansub_lead" (Z. 39), RoleProjectLead = "project_lead" (Z. 40) -->
<!-- WICHTIG: ListActorGroupRoles JOINt fansub_group_members + fansub_group_member_roles -->
<!-- project_lead wird in fansub_group_member_roles.role gespeichert — GENAU wie fansub_lead -->
<!-- Beide Rollen kommen aus derselben Tabelle; kein separater Abfragepfad nötig (D-05) -->

<!-- Aus backend/internal/repository/authz_permissions.go (188 Zeilen) -->
<!-- AuthzRepository hat ListActorGroupRoles(ctx, appUserID, fansubGroupID) ([]string, error) -->
<!-- ListActorGroupRoles: SELECT fgr.role FROM fansub_group_members fgm JOIN fansub_group_member_roles fgr ON ... -->
<!-- Methode ListActorContributionRolesForVersion existiert noch NICHT — wird in Plan 02 ergänzt -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0-Tests für CanForReleaseVersion (D-01/D-03/D-04/D-05)</name>
  <files>backend/internal/permissions/permissions_test.go</files>
  <read_first>
    - backend/internal/permissions/permissions.go (vollständig lesen: Resolver-Interface Z. 181–191, canForContext Z. 90–145, CanForReleaseVersion Z. 240–244, Actor-Struct, ReasonCodes, roleMatrix Z. 24–75, RoleFansubLead Z. 39, RoleProjectLead Z. 40)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-PATTERNS.md (Abschnitt permissions.go — Umbau-Muster Z. 60–135)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-RESEARCH.md (Pitfall 1 — Leader-Reihenfolge Z. 346–355)
  </read_first>
  <action>
In `backend/internal/permissions/permissions_test.go` FÜNF neue Testfunktionen hinzufügen. Alle fünf Testfunktionen sollen nach Plan 02 grün werden. Jetzt schlagen sie rot fehl (erwartetes Verhalten fehlt noch im Produktionscode).

Für die Tests eine `mockResolver`-Hilfsstruct anlegen (implements `Resolver`), die folgende Methoden hat:
- `ResolveReleaseVersion` gibt einen `*Context` zurück mit `FansubGroupIDs: []int64{1}` für versionID 42
- `ListActorGroupRoles` gibt je nach `appUserID`-Wert vordefinierte Rollen zurück:
  - userID 99: `[]string{"fansub_lead"}` (fansub_lead-Bypass-Test)
  - userID 98: `[]string{"project_lead"}` (project_lead-Bypass-Test)
  - userID 1: `[]string{"member"}` (keine Leader-Rolle)
  - alle anderen: leere Liste
- `ListActorContributionRolesForVersion` gibt `[]string{"translator"}` für userID 2 + versionID 42 zurück; leere Liste für alle anderen (DIESE METHODE FEHLT noch im Interface → Tests kompilieren nicht bis Plan 02 das Interface erweitert)

Damit die Tests kompilieren, obwohl `ListActorContributionRolesForVersion` noch nicht im Interface existiert: Die Mock-Methode als Stub-Methode im Test-Mock deklarieren (z. B. Rückgabe leerer Liste oder panic-Stub). Alternativ: ein lokales Interface definieren, das die neue Methode enthält.

Fünf Testfälle:

1. `TestCanForReleaseVersionContributionRequired` (D-01/D-04):
   - Actor: `appUserID=3` (kein fansub_lead, kein project_lead, keine Contribution laut Mock)
   - Action: `ActionReleaseVersionNotesWrite`
   - Assert: `result.Allowed == false`
   - Assert: `result.ReasonCode` ist `ReasonNoMembership` oder `ReasonInsufficientRole`

2. `TestCanForReleaseVersionLeaderBypass` (D-05, fansub_lead):
   - Actor: `appUserID=99` (mock gibt `[]string{"fansub_lead"}` zurück aus `ListActorGroupRoles`)
   - `ListActorContributionRolesForVersion` für userID 99: leere Liste (Leader hat keine Contribution)
   - Action: `ActionReleaseVersionMediaUpload`
   - Assert: `result.Allowed == true` — fansub_lead darf trotz fehlender Contribution

3. `TestCanForReleaseVersionProjectLeadBypass` (D-05, project_lead):
   - Actor: `appUserID=98` (mock gibt `[]string{"project_lead"}` zurück aus `ListActorGroupRoles`)
   - `ListActorContributionRolesForVersion` für userID 98: leere Liste (project_lead hat keine Contribution)
   - Action: `ActionReleaseVersionMediaUpload`
   - Assert: `result.Allowed == true` — project_lead darf trotz fehlender Contribution
   - Begründung: project_lead wird in `fansub_group_member_roles.role` gespeichert — `ListActorGroupRoles` liefert ihn genauso wie fansub_lead (VERIFIED: authz_permissions.go Z. 161–168)

4. `TestCanForReleaseVersionWithContribution` (D-01/D-02):
   - Actor: `appUserID=2` (mock gibt `[]string{"translator"}` aus `ListActorContributionRolesForVersion` zurück)
   - Action: eine Action, die `translator` in `roleMatrix` hat
   - Assert: `result.Allowed == true`

5. `TestCanForReleaseVersionAbsenceInOverride` (D-03):
   - Actor: `appUserID=3` (kein fansub_lead, kein project_lead, laut Mock keine Contribution für versionID 42)
   - Action: `ActionReleaseVersionNotesWrite`
   - Dieses Szenario modelliert: für Release-Version 42 existiert ein Override-Satz, aber Actor 3 ist nicht darin enthalten
   - Assert: `result.Allowed == false`

Testdatei-Header: `//go:build integration` NICHT setzen — Unit-Tests ohne Build-Tag. Package `permissions_test`.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/permissions/... -run "TestCanForReleaseVersion" -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    Fünf neue Testfunktionen in `permissions_test.go` existieren. `go test ./internal/permissions/... -run TestCanForReleaseVersion -v` läuft durch (FAIL wegen fehlendem Produktionscode ist erwartet — ROT-Zustand). Tests kompilieren. Kein `t.Skip()` ohne Assertion.
  </done>
  <acceptance_criteria>
    - `permissions_test.go` enthält Funktionen `TestCanForReleaseVersionContributionRequired`, `TestCanForReleaseVersionLeaderBypass`, `TestCanForReleaseVersionProjectLeadBypass`, `TestCanForReleaseVersionWithContribution`, `TestCanForReleaseVersionAbsenceInOverride`
    - `TestCanForReleaseVersionProjectLeadBypass` prüft userID 98 mit `project_lead`-Rolle aus `ListActorGroupRoles` — kein separater Datenpfad, da `fansub_group_member_roles.role` beide Leader-Rollen speichert
    - Jeder Testfall hat mindestens eine `assert.Equal(t, ...)` oder `assert.True/False(t, ...)` Assertion auf `result.Allowed`
    - Kein Test verwendet `t.Skip()` ohne nachfolgende Assertions
    - Package-Deklaration `package permissions_test` (konsistent mit bestehenden Tests in der Datei)
    - `go test ./internal/permissions/... -run TestCanForReleaseVersion -v` bricht nicht mit Compiler-Fehler ab (nur Assertion-FAILs sind akzeptabel)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wave-0-Test für ListActorContributionRolesForVersion (D-02)</name>
  <files>backend/internal/repository/authz_permissions_test.go</files>
  <read_first>
    - backend/internal/repository/authz_permissions.go (vollständig: ListActorGroupRoles Z. 156–191 — Query-Muster, fansub_group_member_roles-Join, AuthzRepository-Struct)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-PATTERNS.md (Abschnitt authz_permissions.go — Neue Methode Z. 194–217)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-RESEARCH.md (Pattern 1 — Query-Struktur Z. 185–241)
  </read_first>
  <action>
In `backend/internal/repository/authz_permissions_test.go` eine neue Testfunktion `TestListActorContributionRolesForVersion` ergänzen.

Da `ListActorContributionRolesForVersion` noch nicht existiert, wird der Test zunächst nicht kompilieren — das ist das erwartete Wave-0-Verhalten (Rot). Die Teststruktur beschreibt das gewünschte Verhalten für Plan 02:

Funktion `TestListActorContributionRolesForVersion` mit zwei Sub-Tests (als Kommentar-Roadmap, noch ohne Assertions falls keine DB-Mock-Infrastruktur vorhanden):
- `t.Run("versions-spezifisch", ...)` — erwartet role_codes aus anime_contributions mit release_version_id=X wenn solche Einträge existieren
- `t.Run("fallback-anime-weit", ...)` — erwartet role_codes aus anime_contributions mit release_version_id IS NULL wenn kein versions-spezifischer Satz existiert

Falls bestehende `authz_permissions_test.go` bereits DB-Test-Infrastruktur (pgxpool/testcontainer) hat: echte Sub-Tests implementieren. Falls keine DB-Infrastruktur vorhanden: Testfunktion als Stub mit `t.Skip("Wave-0-Stub: ListActorContributionRolesForVersion noch nicht implementiert (Plan 02)")` markieren, aber Kommentare für beide Sub-Tests enthalten.

Prüfen ob `authz_permissions_test.go` bereits existiert. Falls die Datei neu angelegt werden muss: Standard-Package-Header `package repository_test` und Import von `testing` plus Testify.

Falls bestehende Datei existiert: Neue Funktion ans Ende anfügen.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/repository/... -run TestListActorContributionRolesForVersion -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    `authz_permissions_test.go` existiert und enthält `TestListActorContributionRolesForVersion`. Der Test läuft durch (SKIP oder FAIL ist akzeptabel — kein Compiler-Fehler durch fehlende Symbole in dieser Datei).
  </done>
  <acceptance_criteria>
    - Datei `backend/internal/repository/authz_permissions_test.go` existiert
    - Funktion `TestListActorContributionRolesForVersion` ist vorhanden
    - Kommentar erklärt: Schritt 1 (versions-spezifisch) und Schritt 2 (Fallback anime-weit) werden getestet
    - `go test ./internal/repository/... -run TestListActorContributionRolesForVersion -v` bricht nicht mit unerwarteten Compiler-Fehlern ab (nur Fehler wegen fehlender `ListActorContributionRolesForVersion`-Methode sind akzeptabel als Wave-0-Zustand)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Produktionscode | Tests definieren Sicherheitsanforderungen vor Implementierung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | Elevation of Privilege | `CanForReleaseVersion` — fehlende Contribution | mitigate | Wave-0-Test `TestCanForReleaseVersionContributionRequired` verifikiert: Kein Recht ohne Contribution (D-04) |
| T-83-02 | Elevation of Privilege | Leader-Bypass fansub_lead | mitigate | Wave-0-Test `TestCanForReleaseVersionLeaderBypass` verifikiert: fansub_lead hat immer Zugriff (D-05) — Reihenfolge Leader-Check VOR Contribution-Check ist sicherheitskritisch |
| T-83-02B | Elevation of Privilege | Leader-Bypass project_lead | mitigate | Wave-0-Test `TestCanForReleaseVersionProjectLeadBypass` verifikiert: project_lead hat Zugriff über `ListActorGroupRoles` (gleicher Pfad wie fansub_lead via fansub_group_member_roles.role) |
| T-83-03 | Elevation of Privilege | Override-Absenz | mitigate | Wave-0-Test `TestCanForReleaseVersionAbsenceInOverride` verifikiert: Absenz im Override = kein Recht für dieses Release (D-03) |
| T-83-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Pakete in diesem Plan |
</threat_model>

<verification>
- `go test ./backend/internal/permissions/... -run TestCanForReleaseVersion -v` läuft durch (FAILs sind erwartet — ROT)
- `go test ./backend/internal/repository/... -run TestListActorContributionRolesForVersion -v` läuft durch (SKIP oder FAIL ist erwartet)
- Fünf Testfunktionen in `permissions_test.go` vorhanden (inkl. `TestCanForReleaseVersionProjectLeadBypass`)
- `TestListActorContributionRolesForVersion` in `authz_permissions_test.go` vorhanden
</verification>

<success_criteria>
Wave-0-Testgerüst vollständig: Fünf permission-Tests (D-01..D-05, inkl. project_lead-Bypass) und ein Repository-Test (D-02) definieren das gewünschte Verhalten. Die Tests laufen durch den Test-Runner und scheitern erwartungsgemäß (ROT) bis Plan 02 die Produktionsimplementierung liefert.
</success_criteria>

<output>
Create `.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-01-SUMMARY.md` when done.
</output>
