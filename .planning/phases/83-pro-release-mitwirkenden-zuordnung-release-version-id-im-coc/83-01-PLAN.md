---
phase: 83
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/permissions/permissions_test.go
  - backend/internal/repository/authz_permissions_test.go
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05]

must_haves:
  truths:
    - "Wave-0-Testfälle für D-01/D-04: `TestCanForReleaseVersionContributionRequired` existiert und schlägt initial rot fehl"
    - "Wave-0-Testfall für D-05: `TestCanForReleaseVersionLeaderBypass` existiert und schlägt initial rot fehl"
    - "Wave-0-Testfall für D-02: `TestListActorContributionRolesForVersionFallback` existiert und schlägt initial rot fehl (Methode fehlt noch)"
    - "Wave-0-Testfall für D-03: `TestCanForReleaseVersionAbsenceInOverride` existiert und schlägt initial rot fehl"
    - "Alle vier Tests kompilieren (auch wenn sie scheitern) — d. h. kein Compiler-Fehler durch fehlende Symbole"
  artifacts:
    - path: "backend/internal/permissions/permissions_test.go"
      provides: "Wave-0-Testfälle für CanForReleaseVersion (contribution-basiert, Leader-Bypass, Absenz)"
    - path: "backend/internal/repository/authz_permissions_test.go"
      provides: "Wave-0-Testfall für ListActorContributionRolesForVersion (Fallback-Logik)"
  key_links:
    - from: "permissions_test.go"
      to: "permissions.go CanForReleaseVersion"
      via: "s.CanForReleaseVersion(ctx, actor, action, versionID)"
      pattern: "CanForReleaseVersion"
    - from: "authz_permissions_test.go"
      to: "authz_permissions.go ListActorContributionRolesForVersion"
      via: "r.ListActorContributionRolesForVersion(ctx, appUserID, versionID)"
      pattern: "ListActorContributionRolesForVersion"
---

<objective>
Wave-0-Testgerüst für den sicherheitskritischen Permission-Umbau (D-01..D-05).

Ziel: Vier rote Testfälle schreiben, die nach Plan 02 grün werden. Die Tests definieren das gewünschte Verhalten bevor Produktionscode geändert wird. Das stellt sicher, dass der Permission-Umbau korrekt implementiert ist und keine Regression einführt.

Purpose: Sicherheitskritischer Permission-Umbau braucht verifizierbares Vorher-/Nachher-Verhalten. Ohne Wave-0-Tests könnte Plan 02 den Umbau falsch implementieren, ohne dass es auffällt.

Output: Zwei Testdateien mit vier neuen, rot-fehlschlagenden Testfällen.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-CONTEXT.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-RESEARCH.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-PATTERNS.md
@.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-VALIDATION.md

<interfaces>
<!-- Aus backend/internal/permissions/permissions.go (345 Zeilen) -->
<!-- Resolver-Interface (Z. 181–187): -->
<!--
type Resolver interface {
  ResolveFansubGroup(ctx context.Context, fansubGroupID int64) (*Context, error)
  ResolveRelease(ctx context.Context, releaseID int64) (*Context, error)
  ResolveReleaseVersion(ctx context.Context, releaseVersionID int64) (*Context, error)
  ResolveReleaseVersionMedia(ctx context.Context, relationID int64) (*Context, error)
  ListActorGroupRoles(ctx context.Context, appUserID int64, fansubGroupID int64) ([]string, error)
}
-->
<!-- Aktuelles CanForReleaseVersion (Z. 240–244): -->
<!--
func (s *Service) CanForReleaseVersion(ctx context.Context, actor Actor, action Action, releaseVersionID int64) (Result, error) {
  return s.canForContext(ctx, actor, []Action{action}, func(ctx context.Context) (*Context, error) {
    return s.resolver.ResolveReleaseVersion(ctx, releaseVersionID)
  })
}
-->
<!-- Actor struct: IsPlatformAdmin bool, AppUserID int64, Status string -->
<!-- Actions: ActionReleaseVersionNotesWrite, ActionReleaseVersionMediaUpload etc. -->
<!-- roleMatrix: map[string][]Action — z. B. "translator" → [ActionReleaseVersionNotesWrite] -->

<!-- Aus backend/internal/repository/authz_permissions.go (188 Zeilen) -->
<!-- AuthzRepository hat ListActorGroupRoles(ctx, appUserID, fansubGroupID) ([]string, error) -->
<!-- Methode ListActorContributionRolesForVersion existiert noch NICHT — wird in Plan 02 ergänzt -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0-Tests für CanForReleaseVersion (D-01/D-03/D-04/D-05)</name>
  <files>backend/internal/permissions/permissions_test.go</files>
  <read_first>
    - backend/internal/permissions/permissions.go (vollständig lesen: Resolver-Interface Z. 181–191, canForContext Z. 90–145, CanForReleaseVersion Z. 240–244, Actor-Struct, ReasonCodes, roleMatrix Z. 24–75)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-PATTERNS.md (Abschnitt permissions.go — Umbau-Muster Z. 60–135)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-RESEARCH.md (Pitfall 1 — Leader-Reihenfolge Z. 346–355)
  </read_first>
  <action>
In `backend/internal/permissions/permissions_test.go` vier neue Testfunktionen hinzufügen. Alle vier Testfunktionen sollen nach Plan 02 grün werden. Jetzt schlagen sie rot fehl (erwartetes Verhalten fehlt noch im Produktionscode).

Für die Tests eine `mockResolver`-Hilfsstruct anlegen (implements `Resolver`), die folgende Methoden hat:
- `ResolveReleaseVersion` gibt einen `*Context` zurück mit `FansubGroupIDs: []int64{1}` für versionID 42
- `ListActorGroupRoles` gibt je nach `appUserID`-Wert vordefinierte Rollen zurück (z. B. `[]string{"fansub_lead"}` für userID 99, `[]string{"member"}` für userID 1)
- `ListActorContributionRolesForVersion` gibt `[]string{"translator"}` für userID 2 + versionID 42 zurück; leere Liste für userID 3 + versionID 42 (DIESE METHODE FEHLT noch im Interface → Tests kompilieren nicht bis Plan 02 das Interface erweitert)

Damit die Tests kompilieren, obwohl `ListActorContributionRolesForVersion` noch nicht im Interface existiert: Die Mock-Methode als Stub-Methode im Test-Mock deklarieren mit `panic("not yet in interface — add in plan 02")`. Alternativ: ein lokales Interface definieren, das die neue Methode enthält, damit die Tests kompilieren ohne das Produktions-Interface zu ändern.

Vier Testfälle:

1. `TestCanForReleaseVersionContributionRequired` (D-01/D-04):
   - Actor: `appUserID=3` (kein fansub_lead, keine Contribution laut Mock)
   - Action: `ActionReleaseVersionNotesWrite` (oder eine konkrete schreibende Action aus roleMatrix)
   - Assert: `result.Allowed == false`
   - Assert: `result.ReasonCode` ist `ReasonNoMembership` oder `ReasonInsufficientRole` (einer der beiden)

2. `TestCanForReleaseVersionLeaderBypass` (D-05):
   - Actor: `appUserID=99` (mock gibt `[]string{"fansub_lead"}` zurück)
   - `ListActorContributionRolesForVersion` für userID 99: leere Liste (Leader hat keine Contribution)
   - Action: `ActionReleaseVersionMediaUpload` (oder eine konkrete schreibende Action)
   - Assert: `result.Allowed == true` — Leader darf trotz fehlender Contribution

3. `TestCanForReleaseVersionWithContribution` (D-01/D-02):
   - Actor: `appUserID=2` (mock gibt `[]string{"translator"}` aus Contribution zurück)
   - Action: eine Action, die `translator` in `roleMatrix` hat
   - Assert: `result.Allowed == true`

4. `TestCanForReleaseVersionAbsenceInOverride` (D-03):
   - Actor: `appUserID=3` (kein fansub_lead, laut Mock keine Contribution für versionID 42)
   - Action: `ActionReleaseVersionNotesWrite`
   - Dieses Szenario modelliert: für Release-Version 42 existiert ein Override-Satz, aber Actor 3 ist nicht darin enthalten
   - Assert: `result.Allowed == false`
   - Hinweis: Der Mock für versionID 42 gibt für userID 3 eine leere Liste zurück — das deckt Absenz korrekt ab

Testdatei-Header: `//go:build integration` NICHT setzen — Unit-Tests ohne Build-Tag. Package `permissions_test`.
  </action>
  <verify>
    <automated>cd backend && go build ./internal/permissions/... 2>&1 | head -20</automated>
  </verify>
  <done>
    Vier neue Testfunktionen in `permissions_test.go` existieren. `go build ./internal/permissions/...` kompiliert erfolgreich (oder mit erwarteten Fehlern nur wegen fehlender Interface-Methode). Die Tests beschreiben das gewünschte Verhalten nach Plan 02 präzise.
  </done>
  <acceptance_criteria>
    - `permissions_test.go` enthält Funktionen `TestCanForReleaseVersionContributionRequired`, `TestCanForReleaseVersionLeaderBypass`, `TestCanForReleaseVersionWithContribution`, `TestCanForReleaseVersionAbsenceInOverride`
    - Jeder Testfall hat mindestens eine `assert.Equal(t, ...)` oder `assert.True/False(t, ...)` Assertion auf `result.Allowed`
    - Kein Test verwendet `t.Skip()` oder `t.Log()` ohne Assertion
    - Package-Deklaration `package permissions_test` (kein Underscore-Test-Package, um White-Box-Zugriff zu nutzen falls nötig, oder `package permissions` — konsistent mit bestehenden Tests in der Datei)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wave-0-Test für ListActorContributionRolesForVersion (D-02)</name>
  <files>backend/internal/repository/authz_permissions_test.go</files>
  <read_first>
    - backend/internal/repository/authz_permissions.go (vollständig: ListActorGroupRoles Z. 156–191, AuthzRepository-Struct)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-PATTERNS.md (Abschnitt authz_permissions.go — Neue Methode Z. 194–217)
    - .planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-RESEARCH.md (Pattern 1 — Query-Struktur Z. 185–241)
  </read_first>
  <action>
In `backend/internal/repository/authz_permissions_test.go` eine neue Testfunktion `TestListActorContributionRolesForVersion` ergänzen.

Da `ListActorContributionRolesForVersion` noch nicht existiert, wird der Test zunächst nicht kompilieren — das ist das erwartete Wave-0-Verhalten (Rot). Struktur:

```
func TestListActorContributionRolesForVersion(t *testing.T) {
  // Dieser Test verifiziert die Auflösungslogik (D-02):
  // 1. versions-spezifische Contributions werden bevorzugt
  // 2. Fallback auf anime-weite Contributions wenn kein versions-spezifischer Satz existiert
  //
  // Benötigt: Datenbankverbindung (oder Mock) — für Wave 0 als Stub markieren
  t.Skip("Wave-0-Stub: ListActorContributionRolesForVersion noch nicht implementiert (Plan 02)")
  // Nach Plan 02: t.Skip entfernen und echte Assertions einfügen
}
```

Alternativ (falls bestehende Test-Infrastruktur in `authz_permissions_test.go` bereits DB-Mocks hat): zwei Sub-Tests schreiben:
- `t.Run("versions-spezifisch", ...)` — erwartet role_codes aus anime_contributions mit release_version_id=X
- `t.Run("fallback-anime-weit", ...)` — erwartet role_codes aus anime_contributions mit release_version_id IS NULL

Prüfen ob `authz_permissions_test.go` bereits existiert. Falls die Datei neu angelegt werden muss: Standard-Package-Header `package repository_test` und Import von `testing` plus Testify.

Falls bestehende Datei existiert: Neue Funktion ans Ende anfügen.
  </action>
  <verify>
    <automated>cd backend && go build ./internal/repository/... 2>&1 | head -20</automated>
  </verify>
  <done>
    `authz_permissions_test.go` existiert und enthält `TestListActorContributionRolesForVersion`. Die Datei kompiliert (mit t.Skip als Stub, oder kompiliert noch nicht wegen fehlender Methode — beides akzeptabel als Wave-0-Zustand).
  </done>
  <acceptance_criteria>
    - Datei `backend/internal/repository/authz_permissions_test.go` existiert
    - Funktion `TestListActorContributionRolesForVersion` ist vorhanden
    - Kommentar erklärt: Schritt 1 (versions-spezifisch) und Schritt 2 (Fallback anime-weit) werden getestet
    - `go build ./internal/repository/...` schlägt nicht mit unerwarteten Fehlern fehl (nur fehlende `ListActorContributionRolesForVersion`-Methode ist akzeptabel)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Produktionscode | Tests definieren Sicherheitsanforderungen vor Implementierung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | Elevation of Privilege | `CanForReleaseVersion` | mitigate | Wave-0-Test `TestCanForReleaseVersionContributionRequired` verifikiert: Kein Recht ohne Contribution (D-04) |
| T-83-02 | Elevation of Privilege | Leader-Bypass | mitigate | Wave-0-Test `TestCanForReleaseVersionLeaderBypass` verifikiert: Leader hat immer Zugriff (D-05) — Reihenfolge Leader-Check VOR Contribution-Check ist sicherheitskritisch |
| T-83-03 | Elevation of Privilege | Override-Absenz | mitigate | Wave-0-Test `TestCanForReleaseVersionAbsenceInOverride` verifikiert: Absenz im Override = kein Recht für dieses Release (D-03) |
| T-83-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Pakete in diesem Plan |
</threat_model>

<verification>
- `go build ./backend/internal/permissions/...` läuft ohne Fehler
- `go build ./backend/internal/repository/...` läuft ohne Fehler
- Vier Testfunktionen in `permissions_test.go` vorhanden
- `TestListActorContributionRolesForVersion` in `authz_permissions_test.go` vorhanden
</verification>

<success_criteria>
Wave-0-Testgerüst vollständig: Vier permission-Tests (D-01..D-05) und ein Repository-Test (D-02) definieren das gewünschte Verhalten. Die Tests kompilieren und scheitern erwartungsgemäß (ROT) bis Plan 02 die Produktionsimplementierung liefert.
</success_criteria>

<output>
Create `.planning/phases/83-pro-release-mitwirkenden-zuordnung-release-version-id-im-coc/83-01-SUMMARY.md` when done.
</output>
