---
phase: 163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern
plan: 05
type: execute
wave: 5
depends_on: ["163-04"]
files_modified: []
autonomous: false
requirements: [REQ-163-16, REQ-163-23]

must_haves:
  truths:
    - "The live backend and frontend containers are running the phase's committed code (rebuilt/restarted, not stale)."
    - "A human has confirmed, live over the SSH tunnel (:3300) or the Linux frontend (:3000), that selecting AnimeOwnage hides Project-Messiah-only episodes, selecting Project Messiah hides AnimeOwnage-only episodes, 'Alle' shows both, and the one coop episode (Folge 5) is visible and shows its coop version under both single-group filters."
    - "The phase's Abschlussbericht (163-USER-REQUEST.md closing section) exists in this plan's SUMMARY.md and covers every required item: root cause, prior filter logic, new episode-visibility rule, in-episode version filtering, coop handling, pagination/cursor changes, public-visibility handling, query/performance impact, Naruto-episode before/after, tests, build/typecheck/lint, browser verification, open findings."
  artifacts:
    - path: ".planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-05-SUMMARY.md"
      provides: "The phase's Abschlussbericht, satisfying 163-USER-REQUEST.md's closing-section contract"
  key_links:
    - from: "163-05-SUMMARY.md"
      to: "163-01-SUMMARY.md, 163-02-SUMMARY.md, 163-03-SUMMARY.md, 163-04-SUMMARY.md"
      via: "Abschlussbericht synthesizes evidence already captured in each prior plan's SUMMARY, not re-derived"
      pattern: "Abschlussbericht"
---

<objective>
Rebuild the live containers with the phase's finished code, obtain the human live-browser sign-off required by 163-USER-REQUEST.md §16 (Naruto AnimeOwnage/Project Messiah/Alle/coop), and assemble the phase's mandatory closing report.

Purpose: Close out 163-USER-REQUEST.md's `## Verifikation mit realen Daten` and `## Abschlussbericht` requirements, which are explicitly deferred to a live, rebuilt environment and cannot be satisfied by unit/integration tests alone.
Output: Rebuilt containers, a human-approved live-browser UAT record, and a complete Abschlussbericht in this plan's SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-CONTEXT.md
@.planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-USER-REQUEST.md
@.planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-01-SUMMARY.md
@.planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-02-SUMMARY.md
@.planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-03-SUMMARY.md
@.planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild containers, live read-only API verification for Naruto</name>
  <files>none (deployment + read-only verification only)</files>
  <read_first>
    .planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-02-SUMMARY.md (documents that the live container was deliberately NOT rebuilt at that point — this task performs the deferred rebuild)
  </read_first>
  <action>
    On `team4s-linux`, in `/home/d1sk/team4s`: confirm `git status --short` is clean or contains only this phase's committed changes, then run `docker compose up -d --build team4sv30-backend` and `docker restart team4sv30-frontend` (matching the exact rebuild convention already used in prior phases per STATE.md, e.g. Phase 162/160/161). Confirm `docker compose ps` shows both containers healthy/running afterward.
    Then, read-only, run `curl http://127.0.0.1:18092/api/v1/anime/4/episodes?projection=public&limit=24` (unfiltered "Alle") and confirm: no episode has `version_count: 0`; the response includes a top-level `episode_count` field; the value matches the number of distinct episodes with >=1 public release for Naruto (per 163-CONTEXT.md D-18's documented real-data shape: episodes 1-5, i.e. `episode_count: 5`, unless the live dataset has changed since 2026-09-17 — if so, record the actual current value and note the drift, do not assume the old number).
    Resolve the live AnimeOwnage and Project Messiah slugs via `curl http://127.0.0.1:18092/api/v1/anime/4/fansubs` (already confirmed live in 163-RESEARCH.md as `animeownage`/`project-messiah`), then run `curl "http://127.0.0.1:18092/api/v1/anime/4/episodes?projection=public&limit=24&fansub=animeownage"` and `...&fansub=project-messiah`, confirming: the AnimeOwnage-filtered response excludes the Project-Messiah-only episodes and vice versa, both include the one coop episode with its version's `fansub_groups` containing both groups, and both requests return exactly the same HTTP query cost pattern already asserted by the backend test suite (no need to re-derive query counts here, just confirm 200 status and the expected episode ID sets).
  </action>
  <verify>
    <automated>curl -sf "http://127.0.0.1:18092/api/v1/anime/4/episodes?projection=public&limit=24" | grep -c '"version_count":0'</automated>
  </verify>
  <acceptance_criteria>
    `docker compose ps` shows both containers healthy after the rebuild/restart. The three `curl` responses above are recorded verbatim (or as a faithful excerpt) in SUMMARY.md, with an explicit statement of which episode IDs appeared under "Alle", AnimeOwnage, and Project Messiah, matching the D-18-documented shape (or the actual current live shape if it has drifted, with the drift noted).
  </acceptance_criteria>
  <done>Both containers are running the phase's finished code; live read-only curl evidence confirms the fix against the real Naruto dataset in team4s_v2, with no writes issued.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live browser UAT — Naruto AnimeOwnage/Project Messiah/Alle/coop (§16, D-19)</name>
  <files>none (manual verification only, no file changes)</files>
  <action>
    Present the live-browser verification steps below to the user against the containers rebuilt in Task 1, and wait for explicit "approved" or a description of which step failed before proceeding to Task 3.
  </action>
  <what-built>
    A server-side fansub group filter on the public anime episode list (`/anime/[id]`): selecting a concrete group now hides every episode that group has no matching or coop release for; switching groups shows a brief dimmed state (no skeleton) then swaps in the new group's episodes; the "Episoden (N)" heading reflects the current filter's hit count; a failed reload shows a compact error with a working retry button; an empty group shows a compact "keine Releases" message instead of empty cards.
  </what-built>
  <how-to-verify>
    Via the SSH tunnel `http://127.0.0.1:3300` (or directly `http://192.168.235.196:3000`), open the Naruto anime detail page and:
    1. With no group selected ("Alle" active), confirm the episode list shows only episodes with a documented release (per Task 1's recorded `episode_count`/episode-ID evidence) — not all 220 Naruto episodes.
    2. Click "AnimeOwnage". Confirm the episode list visibly dims briefly (no skeleton/spinner replacing it, no layout jump), then updates to show only AnimeOwnage's episodes; confirm any Project-Messiah-only episode from step 1 is now gone (not an empty card, not a "keine Version" text — fully absent from the list); confirm the "Episoden (N)" heading number changed to match.
    3. Click "Project Messiah". Confirm the reverse: AnimeOwnage-only episodes disappear, Project-Messiah-only episodes appear.
    4. Locate the one coop episode (Folge 5, or whichever episode Task 1's evidence identified as coop) and confirm it is visible under BOTH single-group filters and under "Alle", and that its version row shows both groups.
    5. Click "Alle" again. Confirm both groups' episodes reappear together.
    6. Use the browser's back button after step 5 (back to whatever the previous URL state was). Confirm the episode list reloads (briefly dims, then updates) rather than showing a stale cached view.
    7. (Optional, if time permits) Throttle/disconnect network briefly during a group switch to observe the error+"Erneut versuchen" state, then restore network and click retry to confirm recovery.
  </how-to-verify>
  <verify>
    <human-check>User confirms all 6 mandatory checks pass on the live Naruto page.</human-check>
  </verify>
  <done>User has typed "approved" for all 6 mandatory checks, or listed exactly which step failed and what was observed (routed back through /gsd:plan-phase 163 --gaps).</done>
  <resume-signal>Type "approved" if all 6 mandatory checks above pass, or describe exactly which step failed and what was observed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Assemble the Abschlussbericht (163-USER-REQUEST.md closing section)</name>
  <files>none (this plan's SUMMARY.md is the deliverable)</files>
  <read_first>
    .planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-USER-REQUEST.md (the literal `## Abschlussbericht` section — the required item list)
    .planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-01-SUMMARY.md through 163-04-SUMMARY.md (source evidence — synthesize, do not re-derive)
  </read_first>
  <action>
    Write this plan's SUMMARY.md to include a dedicated "Abschlussbericht" section covering, in order, every item 163-USER-REQUEST.md's closing section requires: (1) Ursache des bisherigen Fehlverhaltens (the `LEFT JOIN LATERAL` neutral-row bug plus the absence of any server-side group filter); (2) welche Filterlogik vorher vorhanden war (client-side-only, over already-loaded pages, per 163-RESEARCH.md's Summary); (3) wie die Episodensichtbarkeit jetzt bestimmt wird (the `INNER JOIN LATERAL` + `EXISTS` predicate, D-01/D-02); (4) wie Release-Versionen innerhalb einer Episode gefiltert werden (D-03, same predicate scopes `version_count`/`default_version_id`); (5) wie Coop behandelt wird (D-05, `EXISTS` is group-count-agnostic); (6) wie Pagination/Cursor angepasst wurde (cursor v2 + `GroupID` scope, D-06); (7) wie Public Visibility berücksichtigt wird (the same `EXISTS` doubles as the D-02 fail-closed gate, `ExistsVisible` unchanged); (8) Query-/Performance-Auswirkungen (Plan 163-02's after-fix EXPLAIN numbers vs. Plan 163-01's before-baseline, plus the constant 2-or-3-statement query budget); (9) Naruto Folge 23 vorher/nachher — cite Task 1's live curl evidence and 163-CONTEXT.md D-18's real-data mapping (episode 23 itself has no release at all; the equivalent real regression is episodes 3/4 hidden under AnimeOwnage and 1/2 hidden under Project Messiah); (10) Tests (cite the full Pflichtfälle A-J → test-function mapping across Plans 163-01/163-02/163-03/163-04); (11) Build/Typecheck/Lint (cite Plan 163-04 Task 3's results); (12) Browser-Verifikation (cite this plan's Task 2 human sign-off outcome, verbatim); (13) offene Befunde (list anything explicitly deferred: e.g. the native `<button>` in the episode header per CONTEXT.md's Deferred Ideas, the Coop-Kennzeichnung UI, any drift noted in Task 1 between the 2026-09-17 research baseline and the live dataset at execution time).
  </action>
  <verify>
    <automated>grep -Ec "Ursache|Filterlogik|Pagination|Query-|Naruto|Tests|Build|Browser-Verifikation|offene Befunde" .planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-05-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    SUMMARY.md contains a section literally covering all 13 items listed in `<action>` above, each with a concrete, specific answer (not "see above" placeholders) and citing the specific prior-plan SUMMARY.md or Task where the evidence was captured.
  </acceptance_criteria>
  <done>The Abschlussbericht is complete, specific, and traceable to the evidence captured across Plans 163-01 through 163-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live containers ↔ this plan | Rebuild/restart only; Task 1's `curl` calls are read-only GETs against the public endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-10 | Tampering | Container rebuild step | accept | Standard, already-established project rebuild convention (`docker compose up -d --build`); no schema/data change; low risk, already used in every prior phase per STATE.md |
| T-163-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages |
</threat_model>

<verification>
1. `docker compose ps` — both containers healthy after rebuild/restart.
2. Live read-only `curl` evidence recorded in SUMMARY.md, confirming the fix against real Naruto data.
3. Human checkpoint resume-signal recorded as "approved" (or documented gaps if not).
4. SUMMARY.md contains the complete 13-item Abschlussbericht.
</verification>

<success_criteria>
Live containers run the finished phase code; a human has confirmed the group filter works correctly on real Naruto data via the browser; the phase's mandatory Abschlussbericht is complete and traceable.
</success_criteria>

<output>
Create `.planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-05-SUMMARY.md` when done
</output>
