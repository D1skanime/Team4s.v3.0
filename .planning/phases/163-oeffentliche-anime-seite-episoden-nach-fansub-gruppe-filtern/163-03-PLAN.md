---
phase: 163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern
plan: 03
type: execute
wave: 3
depends_on: ["163-02"]
files_modified:
  - frontend/src/types/episodeVersion.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/fansub-summary.ts
  - frontend/src/lib/fansub-summary.test.ts
  - frontend/src/lib/api.episode-versions.test.ts
  - frontend/src/app/anime/[id]/page.test.tsx
  - frontend/src/components/fansubs/FansubVersionBrowser.test.tsx
  - frontend/src/components/fansubs/FansubVersionBrowser.groupSwitch.test.tsx
autonomous: true
requirements: [REQ-163-07, REQ-163-13, REQ-163-15]

must_haves:
  truths:
    - "A failing unit test exists proving getGroupedEpisodes forwards a fansub query parameter when present (api.ts does not do this yet)."
    - "A failing unit test exists proving a shared SSR-side helper resolves the effective fansub slug (undefined for 0/1 groups or an unmatched slug, the slug itself when it matches a real >=2-group option) — the helper does not exist yet."
    - "A failing test exists proving page.tsx forwards the resolved fansub slug into its SSR getGroupedEpisodes call (D-11), and that an unresolvable/single-group slug is never forwarded."
    - "A failing test exists proving page.tsx no longer renders the anime.episodes fallback list when the public fetch fails (D-16) and instead renders a neutral error state."
    - "A failing test exists proving a fansub chip click and a browser back/forward (popstate) each trigger exactly one getGroupedEpisodes refetch that discards (does not merge into) the previously loaded page (D-07/D-09)."
    - "A failing test exists proving the old episode list stays visible and is marked aria-busy while a group switch is in flight (D-08), and that a failed switch shows a retry control without leaking the old group's data (D-10)."
    - "The now-contradictory Phase-162 test asserting a chip click does NOT call getGroupedEpisodes, and the assertion for the removed 'Keine Version dieser Gruppe verfügbar.' text, are both corrected to match Phase 163's new contract."
  artifacts:
    - path: "frontend/src/lib/fansub-summary.ts"
      provides: "resolveActiveFansubSlug export (declared, not yet implemented — RED)"
    - path: "frontend/src/components/fansubs/FansubVersionBrowser.groupSwitch.test.tsx"
      provides: "New sibling test file for D-07..D-10/G group-switch-refetch behavior"
  key_links:
    - from: "frontend/src/app/anime/[id]/page.test.tsx"
      to: "frontend/src/lib/fansub-summary.ts#resolveActiveFansubSlug"
      via: "SSR fansub-forwarding decision"
      pattern: "resolveActiveFansubSlug"
---

<objective>
Establish the frontend failing-test baseline for Phase 163 BEFORE touching `page.tsx` or `FansubVersionBrowser.tsx` production code, per the Auftraggeber's mandatory test-first ordering. This plan adds/corrects test files and the pure TypeScript type contracts they need to reference — it does NOT implement any new rendering/fetch behavior in the two production components.

Purpose: Lock the exact contract (types, shared SSR-resolution helper signature, corrected/new test expectations) that Plan 163-04 must implement, so Plan 163-04 has an objective, automatable finish line and cannot silently under-deliver D-07..D-16.
Output: Extended type contracts (`fansub`/`episode_count` fields), a new (as-yet-unimplemented) `resolveActiveFansubSlug` export, corrected existing tests, and a new sibling test file for the group-switch-refetch behavior — all failing today for documented reasons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-CONTEXT.md
@.planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-RESEARCH.md
@.planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-PATTERNS.md
@.planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-02-SUMMARY.md

<interfaces>
<!-- Exact current source these tests exercise (read in full during planning, 2026-09-17). -->

Current `frontend/src/types/episodeVersion.ts` (lines 159-183, unchanged until this task edits it):
```ts
export interface PublicGroupedEpisode {
  episode_id: number
  episode_number: number
  episode_title?: string | null
  default_version_id?: number | null
  version_count: number
  versions: PublicEpisodeVersion[]
}
export interface PublicGroupedEpisodesResponse {
  data: {
    anime_id: number
    episodes: PublicGroupedEpisode[]
    pagination: { has_more: boolean; next_cursor: string | null; row_limit: number }
  }
}
export interface PublicGroupedEpisodesOptions {
  projection: 'public'
  limit?: number
  cursor?: string
  signal?: AbortSignal
}
```

Current `frontend/src/lib/api.ts` `getGroupedEpisodes` (lines 2151-2185):
```ts
export async function getGroupedEpisodes(animeID: number, options?: PublicGroupedEpisodesOptions) {
  const query = new URLSearchParams()
  if (options) {
    query.set("projection", options.projection)
    if (options.limit !== undefined) query.set("limit", String(options.limit))
    if (options.cursor !== undefined) query.set("cursor", options.cursor)
  }
  // ... authorizedFetch(`${API_BASE_URL}/api/v1/anime/${animeID}/episodes${suffix}`, { cache: "no-store", ...(options?.signal ? { signal: options.signal } : {}) })
}
```

Current `frontend/src/lib/fansub-summary.ts` (full file, 66 lines) exports `buildFansubFactSummary`, `buildFansubStoryPreview`, `buildFansubStoryGroups` — no slug-resolution helper exists yet.

Current `frontend/src/app/anime/[id]/page.tsx` (relevant excerpt, lines 82-107, 254-269, 269-298):
```tsx
const rawFansubParam = typeof resolvedSearchParams.fansub === 'string' ? resolvedSearchParams.fansub : undefined
const [animeFansubsResult, groupedEpisodesResult, commentsResult, relationsResult] = await Promise.allSettled([
  getAnimeFansubs(anime.id),
  getGroupedEpisodes(anime.id, { projection: 'public', limit: 24 }),  // <- does not forward rawFansubParam yet
  getAnimeComments(animeID, { page: 1, per_page: 10 }),
  getAnimeRelations(anime.id),
])
const episodeCount = anime.episodes.length  // <- D-12: must become the group-filtered hit count, sourced from the fetch response
// ...
<h2>Episoden ({episodeCount})</h2>
{groupedEpisodesResponse ? (
  <FansubVersionBrowser ... episodes={...} pagination={...} initialActiveSlug={rawFansubParam} />
) : anime.episodes.length === 0 ? (
  <div className={styles.emptyEpisodes}>Noch keine Episoden vorhanden.</div>
) : (
  <ul className={styles.episodeList}>{anime.episodes.map((episode) => ( /* releaseless episodes render here -- D-16 bug */ ))}</ul>
)}
```

Current `frontend/src/components/fansubs/FansubVersionBrowser.tsx` (full file, 323 lines — already read in full during planning; the exact `updateFansubSelection`, popstate handler, `loadMore`, `groupMatchedVersions`/`hasNoMatchingVersion`/`noVersionHint` block, and `<Button variant="secondary" loading={isLoading}>` retry-analog are all documented in 163-PATTERNS.md and must NOT be edited by this plan — Plan 163-04 owns that file's production changes; this plan only edits its `.test.tsx` sibling(s)).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contracts — types, api.ts forwarding, shared SSR slug-resolution helper (RED where testable)</name>
  <files>frontend/src/types/episodeVersion.ts, frontend/src/lib/api.ts, frontend/src/lib/fansub-summary.ts, frontend/src/lib/fansub-summary.test.ts, frontend/src/lib/api.episode-versions.test.ts</files>
  <read_first>
    frontend/src/types/episodeVersion.ts (full file, see interfaces block above)
    frontend/src/lib/api.ts lines 2151-2185 (see interfaces block above)
    frontend/src/lib/fansub-summary.ts (full file — existing `buildFansubStoryGroups` dedupe pattern is the closest analog for the new helper's group-collection logic)
    frontend/src/lib/fansub-summary.test.ts (full file — `makeRelation` helper, existing `describe` block conventions)
    frontend/src/lib/api.episode-versions.test.ts (full file — real-fetch-mock pattern at lines 23-33 is the exact analog to extend)
    .planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-PATTERNS.md (frontend `getGroupedEpisodes`/types sections)
  </read_first>
  <behavior>
    - `PublicGroupedEpisodesOptions` gains `fansub?: string`.
    - `PublicGroupedEpisodesResponse['data']` gains `episode_count: number` (required, matching the Go DTO's non-`omitempty` field from Plan 163-02).
    - `getGroupedEpisodes` forwards `fansub`: `if (options.fansub !== undefined) query.set("fansub", options.fansub)`, symmetric with the existing `limit`/`cursor` lines — implement this NOW (it is a one-line, low-risk, already-analog-matched change; write the test first per the RED step below, then this one line makes it pass within the same task, since it is glue code, not a design decision).
    - New export `resolveActiveFansubSlug(relations: AnimeFansubRelation[], rawSlug: string | undefined): string | undefined` in `fansub-summary.ts` — DECLARE the export with a `throw new Error('not implemented — see Plan 163-04')` body (or leave entirely unimplemented/commented) so the RED tests below fail for the RIGHT reason (assertion failure / thrown error), not a module-resolution crash that would also break unrelated tests importing this file. Do NOT implement the real resolution logic in this plan — that is Plan 163-04's job. The intended contract (for Plan 163-04 to implement against): with 0 or 1 distinct `fansub_group.id` values across `relations` (same dedupe rule as `buildFansubStoryGroups`), OR when `rawSlug` is falsy, OR when `rawSlug` does not match any option's `fansub_group.slug`, return `undefined`; otherwise return `rawSlug` unchanged.
  </behavior>
  <action>
    Add the two TypeScript interface fields exactly as specified in `<behavior>` (pure type additions, no test possible). Implement the `getGroupedEpisodes` one-line `fansub` forwarding AFTER first adding a new test case in `api.episode-versions.test.ts` (sibling to the existing "sends the public projection, escaped cursor, limit and abort signal once" test at lines 23-33): assert that calling `getGroupedEpisodes(1, { projection: 'public', fansub: 'animeownage' })` results in a fetch URL whose query string includes `fansub=animeownage`; confirm this fails against the CURRENT `api.ts` (RED — the current code never sets `fansub`), then apply the one-line fix and confirm GREEN. Since this specific sub-step is genuinely test-first with an immediate fix, it is exempt from the plan's overall RED/GREEN split (it is glue code low-risk enough to close within the same task per the task-level TDD exception for glue code — but the test itself must exist and be shown red before the fix, recorded in SUMMARY.md).
    Add the `resolveActiveFansubSlug` export as an unimplemented stub (per `<behavior>`) to `fansub-summary.ts`. Add a new `describe('resolveActiveFansubSlug', ...)` block to `fansub-summary.test.ts` using the existing `makeRelation` helper, covering: (a) zero relations → `undefined`; (b) exactly one relation with a `fansub_group` → `undefined` regardless of `rawSlug`; (c) two distinct groups, `rawSlug` matching the second group's slug → returns that slug unchanged; (d) two distinct groups, `rawSlug` matching neither → `undefined`; (e) two distinct groups, `rawSlug` is `undefined` → `undefined`; (f) two relations referencing the SAME `fansub_group.id` (duplicate) plus a third distinct group (mirrors `buildFansubStoryGroups`'s own dedupe test) with a matching `rawSlug` for the third group → returns that slug. Run these and confirm every case fails (RED) against the stub.
  </action>
  <verify>
    <automated>npx vitest run fansub-summary.test.ts api.episode-versions.test.ts</automated>
  </verify>
  <done>Type contracts exist; `getGroupedEpisodes` forwards `fansub` (GREEN, test-first); `resolveActiveFansubSlug` is exported but unimplemented and its new test cases fail (RED) for the documented reason (thrown "not implemented" error, not a crash elsewhere).</done>
</task>

<task type="auto">
  <name>Task 2: page.tsx RED — SSR fansub forwarding (D-11), fallback removal (D-16), episodeCount-as-prop (D-12)</name>
  <files>frontend/src/app/anime/[id]/page.test.tsx</files>
  <read_first>
    frontend/src/app/anime/[id]/page.test.tsx (full file, 298 lines — see interfaces block above for the exact current fallback/heading code these tests exercise)
    frontend/src/app/anime/[id]/page.tsx (full file, see interfaces block above)
    .planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-PATTERNS.md (`page.tsx` section: heading move, fallback removal)
  </read_first>
  <action>
    This test file uses a shallow JSX-tree walker (`elements`/`textContent` helpers at lines 103-113, `loadContent` at 114-117) that inspects the props PASSED TO `FansubVersionBrowser` as an element, without invoking the component itself — preserve this exact pattern (do not switch to `@testing-library/react` rendering here; that would invoke `FansubVersionBrowser`'s hooks, which is out of scope for a page.tsx-focused suite and inconsistent with every existing test in this file).
    1. Rewrite the "preserves the real episode inventory and stored fallback episode counters" test (lines 151-161): keep `groupedMock.mockRejectedValue(new ApiError(500, 'Versionen nicht verfügbar'))` and the `12 Episodes` poster-stat assertion (`anime.max_episodes`, unrelated to this bug) UNCHANGED. REMOVE the three assertions that depend on the old `anime.episodes` fallback list (`toContain('Episoden (1)')`, `toContain('Neutrale Folge')`, `toContain('Views: 3 | Downloads: 8')`) — these encode the exact D-16 bug being removed. REPLACE with: `expect(elements(content).some((item) => item.type === FansubVersionBrowser)).toBe(false)` (the browser must not render at all when the fetch failed) and `expect(text).toContain('Episoden konnten nicht geladen werden.')` (the new neutral error-state title Plan 163-04 will render — this exact German string, with a correct umlaut, must be the one used in the implementation).
    2. Rewrite the "requests one 24-row public page..." test (lines 191-203): change the mocked `groupedMock.mockResolvedValue(...)` payload to include `episode_count: 0` (matching `episodes: []`). REMOVE `expect(textContent(content)).toContain('Episoden (30)')` (this text derived from `anime.episodes.length`, which is exactly the wrong source per D-12). REPLACE with `expect(browser?.props.episodeCount).toBe(0)` — proving the heading's data now comes from the fetch response's `episode_count`, passed as a NEW `episodeCount` prop to `FansubVersionBrowser`, not from `anime.episodes.length`. This assertion is expected to fail (RED) until Plan 163-04 both adds the prop AND wires it — `browser?.props.episodeCount` will be `undefined` today, and `undefined !== 0` fails as intended.
    3. Add a NEW test, "D-11: reicht den ueber die Fansub-Relationen aufgeloesten Slug in die SSR-Episodenabfrage durch": mock `getAnimeFansubs` to resolve two distinct groups (e.g. slugs `alpha-subs`/`beta-subs`), set `searchParams: Promise.resolve({ fansub: 'beta-subs' })`, call `loadContent()`, and assert `expect(getGroupedEpisodes).toHaveBeenCalledExactlyOnceWith(22, { projection: 'public', limit: 24, fansub: 'beta-subs' })`. This fails today (RED) because `page.tsx` never forwards `fansub` to `getGroupedEpisodes` at all.
    4. Add a NEW test, "D-05/D-11: eine fremde/unbekannte Fansub-Slug wird NICHT an die SSR-Episodenabfrage weitergereicht": same two-group mock, `searchParams: Promise.resolve({ fansub: 'does-not-exist' })`, assert `expect(getGroupedEpisodes).toHaveBeenCalledExactlyOnceWith(22, { projection: 'public', limit: 24 })` (no `fansub` key at all — proving the SSR-side `resolveActiveFansubSlug` fallback, not merely "pass the raw value and let the backend 400"). Fails today for the SAME underlying reason (no forwarding logic exists yet, so this call currently happens to already match the CURRENT no-fansub behavior by accident — make sure the assertion is written so it will meaningfully flip to red-then-green across Plan 163-04, e.g. by first confirming in this same test, via a THIRD case below, that the single-group scenario proves the mechanism is real, not accidental).
    5. Add a NEW test, "162 D-02/163 D-11: bei genau einer Fansub-Gruppe wird der Parameter niemals weitergereicht": mock `getAnimeFansubs` to resolve exactly ONE relation whose slug IS `searchParams.fansub`'s value (i.e., a value that WOULD match if forwarding were naive/unconditional), assert `getGroupedEpisodes` is still called WITHOUT `fansub`. This is the test that actually proves cases 3/4 aren't accidental — it only passes once real resolution logic (matching `resolveActiveFansubSlug`'s contract) exists.
    Run the file and confirm the new/modified assertions fail (RED) for the documented reasons; the untouched tests in this file (Tags/Genre block, watchlist, Emby link, CSS clip checks, D-04 `initialActiveSlug` passthrough) must remain unaffected and GREEN.
  </action>
  <verify>
    <automated>npx vitest run page.test.tsx</automated>
  </verify>
  <done>page.test.tsx contains 5 corrected/new assertions (fallback removal, episodeCount-as-prop, D-11 forwarding x2, single-group non-forwarding) that fail today against the current page.tsx; all pre-existing unrelated assertions in the file remain green.</done>
</task>

<task type="auto">
  <name>Task 3: FansubVersionBrowser RED — invert Phase-162 no-refetch tests, remove D-15 dead-code assertion, new sibling file for D-07..D-10/G</name>
  <files>frontend/src/components/fansubs/FansubVersionBrowser.test.tsx, frontend/src/components/fansubs/FansubVersionBrowser.groupSwitch.test.tsx</files>
  <read_first>
    frontend/src/components/fansubs/FansubVersionBrowser.test.tsx (full file, 330 lines — already read in full during planning; every line reference below matches this exact file as of 2026-09-17)
    .planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-PATTERNS.md (FansubVersionBrowser.tsx section: `switchTo`/dimming/dead-code-removal design, `requestRef`/AbortController pattern)
    .planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-CONTEXT.md (D-07..D-10, D-15)
  </read_first>
  <action>
    In the EXISTING file (`FansubVersionBrowser.test.tsx`), apply exactly these corrections — do not add net-new describe blocks here beyond what is listed (net-new group-switch coverage belongs in the sibling file, to respect the 450-line cap: this file is already at 330 lines):
    1. Line 82 (`expect(screen.getByText('Keine Version dieser Gruppe verfügbar.')).toBeTruthy()`, inside "behaelt echte Episodentitel und Ausklapp-Verhalten"): replace with `expect(screen.queryByText('Keine Version dieser Gruppe verfügbar.')).toBeNull()` — per D-15 this text must never render again, even for an episode whose `versions` prop is empty.
    2. Lines 166-171 ("D-03: ein Chip-Wechsel loest keinen zusaetzlichen getGroupedEpisodes-Aufruf aus"): rename to "D-07: ein Chip-Wechsel loest einen Refetch mit dem Ziel-Slug aus" and invert the body — add `groupedMock.mockResolvedValueOnce({ data: { anime_id: 22, episodes: [], pagination: { has_more: false, next_cursor: null, row_limit: 24 } } })` before the click, then `fireEvent.click(...)`, then `await waitFor(() => expect(getGroupedEpisodes).toHaveBeenCalledExactlyOnceWith(22, expect.objectContaining({ projection: 'public', limit: 24, fansub: 'saved-primary', signal: expect.any(AbortSignal) })))`.
    3. Lines 182-195 ("Browser Zurueck/Vor stellt ueber popstate die vorherige Gruppenauswahl wieder her, ohne Refetch"): rename to "...MIT Refetch (D-09)" and invert: mock a resolved response before the `pushState`+`popstate` simulation, then assert `getGroupedEpisodes` WAS called (not `not.toHaveBeenCalled()`) after the popstate event, with no `fansub` key (since the simulated URL change removes the parameter, matching "Alle").
    4. "Testfall E/F" (lines 144-153, chip-clicking through Anderer Gruppenname → Zweite Gruppe → Alle using the static `stateEpisodes` fixture): this test currently relies on CLIENT-SIDE filtering of a single static prop and never mocks `getGroupedEpisodes`. Rewrite it to mock one resolved response PER click — before each `fireEvent.click`, call `groupedMock.mockResolvedValueOnce(...)` returning a payload whose `episodes[0].versions` contains ONLY the variant(s) belonging to the just-selected group (reuse the existing `variant(id, group)` helper and `publicPage`/`ended` helpers already defined later in the file — hoist/reuse them, do not redefine), then `await waitFor(...)` before asserting `assertGroup(...)`/`assertAllSelected()`. Confirm this still exercises `assertGroup(7)`/`assertGroup(9)`/`assertAllSelected()` exactly as before, just via the network path instead of client-side filtering.
    5. "Testfall I" (lines 197-210, coop version, no third chip): same treatment — mock BOTH group-switch fetches to resolve the identical `coopEpisodes` payload (a coop version legitimately appears under both group filters by definition), confirming `Variante 7` remains visible after switching to either group.
    6. The "bounded public inventory continuation" describe block's first test (lines 230-263, 125-variant pagination with 2 interleaved chip clicks): update its `groupedMock` stub sequence and its final call-count/argument assertions (`toHaveBeenCalledTimes(5)`, `toHaveBeenNthCalledWith(1, ...)`) to account for the chip clicks ALSO now issuing their own fetch each (previously free). Add explicit stubs for the "Zweite Gruppe" and "Anderer Gruppenname" clicks (each resolving a group-scoped page-1 payload) BEFORE the existing 5 "Weitere laden" clicks, and update the total expected call count and the `toHaveBeenNthCalledWith` index accordingly. Keep the substantive assertions (final version count, play-link count) unchanged in intent — only the call-accounting needs updating.
    Run `npx vitest run FansubVersionBrowser.test.tsx` and confirm items 1-3 above fail (RED) against the CURRENT component (which never fetches on chip-click/popstate and still renders the noVersionHint text), and that items 4-6 either fail outright (network mock now required but production code doesn't consume it yet, since no fetch is ever ISSUED by the current code on a chip click — these will actually hang/timeout or fail their `waitFor` — acceptable, document the exact failure mode in SUMMARY.md) or are adjusted to fail cleanly rather than hang: prefer explicit `await waitFor(..., { timeout: 500 })` wrapped assertions so a RED run fails fast with a clear message instead of the default multi-second timeout, keeping the overall suite's feedback latency low.

    Create the NEW sibling file `FansubVersionBrowser.groupSwitch.test.tsx` (same imports/mock setup as the existing file: `vi.mock('@/lib/api', ...)`, the same `fansubs`/`variant`/`publicPage`/`ended`/`continued` fixtures — duplicate the minimal subset needed, or factor shared fixtures into a small local module if duplication would exceed reasonable size; keep this new file itself ≤450 lines). Add these NEW test cases (all RED today, since none of this behavior exists in the current component):
    - "D-08: waehrend eines Gruppenwechsels bleibt die alte Liste sichtbar und wird als aria-busy markiert": render with an initial episode already visible; mock the switch fetch with a `deferred<PublicGroupedEpisodesResponse>()` (reuse the existing `deferred` helper pattern from the sibling file, redefined locally); click a group chip; assert the OLD episode's text is STILL in the DOM while the promise is pending, AND that the episode list container has `aria-busy="true"`; resolve the promise; assert `aria-busy` is removed/`false` and the NEW episode data is now shown.
    - "D-09: ein Gruppenwechsel bricht eine laufende Weitere-laden-Anfrage ab": start a `loadMore` fetch (pending, via a deferred promise), then click a DIFFERENT group chip mid-flight; assert the `loadMore` request's `AbortSignal.aborted === true`; assert only the group-switch's own (separate, later) fetch is allowed to set state once it resolves.
    - "D-10: ein fehlgeschlagener Gruppenwechsel zeigt einen Fehlerhinweis mit Erneut-versuchen-Button, ohne die alte Gruppe zu vermischen": mock the switch fetch to reject; click a chip; assert an error message ("Episoden konnten nicht geladen werden." or the D-20/component-local equivalent decided in Plan 163-04 — assert on a `role="alert"` element's text content, not a hardcoded implementation string, to stay resilient to minor copy changes) and a "Erneut versuchen" button both appear; assert the OLD group's episode data is still the only data shown underneath (not merged with a partial/empty new response); click retry; mock a success; assert the error clears and the new data replaces the old.
    - "Pflichtfall G: Filterwechsel Alle→AnimeOwnage→ProjectMessiah→Alle zeigt bei jedem Schritt ausschliesslich die zuletzt geladene Antwort, keine Vermischung": four sequential chip clicks (or three, since 2-group fixtures only support Alle/A/B — extend `fansubs` locally to 3 groups for this test if needed to faithfully cover a 3-way Alle→AO→PM→Alle cycle), each with a DISTINCT mocked payload; after each click, assert the rendered version set matches ONLY that step's mocked payload — no version from a previous step's payload is present.
    Confirm every new test in this sibling file fails today (RED) — the current component never calls `getGroupedEpisodes` on a chip click or popstate at all, so any `waitFor(() => expect(getGroupedEpisodes).toHaveBeenCalled())`-style assertion will time out/fail cleanly.
  </action>
  <verify>
    <automated>npx vitest run FansubVersionBrowser.test.tsx FansubVersionBrowser.groupSwitch.test.tsx</automated>
  </verify>
  <done>Both files exist; the 3 inverted/corrected assertions and the 3 rewritten mock-dependent tests in the existing file fail today for the documented reason; all 4 new test cases in the sibling file fail today for the documented reason; `wc -l` on both files is ≤450.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-only changes | This plan touches only `.test.ts(x)` files plus pure type/glue additions (`fansub`/`episode_count` fields, one-line query forwarding, an unimplemented stub) — no new runtime attack surface is introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-07 | Information Disclosure | `resolveActiveFansubSlug` stub (unimplemented) | accept | Stub throws/is inert; no behavior ships from this plan; real fail-closed logic lands in Plan 163-04 and is itself covered by the RED tests this plan authors |
| T-163-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages; Package Legitimacy Gate not applicable |

**Cross-reference:** D-01/D-02 (fail-closed visibility), D-05 (anime-scoped slug IDOR), and D-06 (cursor scope-check) are implemented and owned by Plan 163-02 (T-163-03..T-163-06); this plan's RED tests (Task 2/3) prove the frontend-side behavior those mitigations must satisfy once Plan 163-04 turns them GREEN.
</threat_model>

<verification>
1. `npx vitest run fansub-summary.test.ts api.episode-versions.test.ts page.test.tsx FansubVersionBrowser.test.tsx FansubVersionBrowser.groupSwitch.test.tsx` — every listed RED assertion fails for the documented reason; the `getGroupedEpisodes`-forwards-`fansub` sub-step and its test are GREEN (glue-code exception).
2. `wc -l frontend/src/components/fansubs/FansubVersionBrowser.groupSwitch.test.tsx frontend/src/components/fansubs/FansubVersionBrowser.test.tsx` — both ≤450.
3. `npm run typecheck` — EXPECTED to show new errors referencing the not-yet-existing `episodeCount` prop on `FansubVersionBrowserProps` (referenced by page.test.tsx's shallow-tree assertions and/or the new sibling test file); document this as an accepted, intentional RED state resolved by Plan 163-04, not a mistake in this plan.
</verification>

<success_criteria>
Every RED test described above exists, compiles (except the documented, intentional `episodeCount`-prop typecheck gap), and fails today for a clearly documented reason. No production behavior in `page.tsx` or `FansubVersionBrowser.tsx` was changed.
</success_criteria>

<output>
Create `.planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-03-SUMMARY.md` when done
</output>
