---
phase: 163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/repository/episode_version_public_integration_test.go
  - backend/internal/repository/episode_version_public_group_filter_test.go
autonomous: true
requirements: [REQ-163-15, REQ-163-17, REQ-163-24]

must_haves:
  truths:
    - "A failing test exists that proves the current publicEpisodeQuery returns a neutral row for episodes without any release (bug reproduction, corrected TestEpisodeVersionPublicMixedAndIdentity/TestEpisodeVersionPublicAtomicPages/TestEpisodeVersionPublicEmptyAndVisibility expectations)."
    - "A failing test exists that reproduces the exact Naruto Folge-23-style regression from D-18 (group-exclusive episodes disappear under the wrong group filter) using an isolated Postgres fixture, never team4s_v2."
    - "A failing test exists proving the group filter is applied before the pagination LIMIT (Pflichtfall F: matches beyond the naive first page are still found)."
    - "A failing test exists proving a release_versions row with variants but zero release_version_groups rows never makes its episode publicly visible (Pflichtfall I)."
    - "A documented pre-fix baseline (full go test ./... pass/fail counts, and a live read-only EXPLAIN (ANALYZE) for Naruto anime_id=4 in team4s_v2) exists in this plan's own execution trail (SUMMARY.md), not only in 163-RESEARCH.md."
  artifacts:
    - path: "backend/internal/repository/episode_version_public_integration_test.go"
      provides: "Corrected (RED) baseline assertions for the neutral-row bug, unchanged line count budget"
    - path: "backend/internal/repository/episode_version_public_group_filter_test.go"
      provides: "New sibling test file (356-line cap on the existing file respected) covering Pflichtfälle B, C, D, E, F, I and the updated J query-budget assertion"
  key_links:
    - from: "backend/internal/repository/episode_version_public_group_filter_test.go"
      to: "backend/internal/testsupport.OpenPhase117Postgres"
      via: "isolated schema/search_path fixture, never DATABASE_URL/team4s_v2"
      pattern: "testsupport\\.OpenPhase117Postgres"
---

<objective>
Establish the failing-test baseline for Phase 163 BEFORE any production code changes, per the Auftraggeber's mandatory test-first ordering (163-USER-REQUEST.md `## Arbeitsweise` steps 1-7) and per 163-CONTEXT.md D-19. This plan touches ONLY test files. No implementation code (`episode_version_public_query.go`, handler, model, OpenAPI) changes here — that is Plan 163-02.

Purpose: Prove the bug (neutral rows, no server-side group filter) with executable, isolated-fixture tests that fail for the documented reason, so Plan 163-02's implementation has an objective, automatable finish line. Also capture the "before" performance/regression baseline that the phase's Abschlussbericht (163-USER-REQUEST.md closing section) must cite from THIS execution, not only from research.
Output: Two test files (one corrected in place, one new sibling file) that fail today; a documented pre-fix `go test ./...` baseline; a documented live read-only `EXPLAIN (ANALYZE)` baseline for Naruto (anime_id=4) in `team4s_v2`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-CONTEXT.md
@.planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-RESEARCH.md
@.planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-PATTERNS.md
@.planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-USER-REQUEST.md

<interfaces>
<!-- Current (pre-fix) shape the tests must exercise. Do not re-derive: this is the actual file content as of 2026-09-17. -->

From backend/internal/repository/episode_version_public_query.go (unchanged in this plan, changes are Plan 163-02):
```go
type PublicEpisodeOptions struct {
	Limit  int
	Cursor string
}
type publicEpisodeCursor struct {
	Version       int   `json:"v"`
	AnimeID       int64 `json:"a"`
	EpisodeNumber int32 `json:"n"`
	EpisodeID     int64 `json:"e"`
	VariantID     int64 `json:"i"`
}
```
The public handler's strict allowlist (`backend/internal/handlers/episode_version_reads.go` line 31) is currently `"projection", "limit", "cursor", "includeVersions", "includeFansubs"` — it does **not** accept `fansub` yet. Any request in this plan's new tests that includes `?fansub=...` will currently be rejected with **400** ("ungültige Episodenoptionen") by `parseStrictNamedQuery`'s unknown-key rejection. That 400 IS the expected RED signal for those specific assertions — do not treat it as a test-authoring mistake; it is the documented pre-fix behavior this plan must capture, and Plan 163-02 is what turns it into a real 200/400-per-fail-closed-rule response.

From backend/internal/repository/episode_version_public_integration_test.go (full file already read; fixture/tracer/request helpers at lines 1-165 are reused verbatim — do not duplicate them into the new sibling file except where noted below):
```go
func openEpisodeVersionPublicFixture(t *testing.T) (*pgxpool.Pool, *episodePublicTracer)
func episodePublicRequest(t *testing.T, pool *pgxpool.Pool, path string, status int) ([]byte, publicEpisodeEnvelope)
func assertPublicBudget(t *testing.T, tr *episodePublicTracer, limit int)
type publicEpisodeFixture struct { EpisodeID int64; EpisodeNumber int32; EpisodeTitle string; VersionCount int; DefaultVersionID *int64; Versions []map[string]any }
type publicEpisodeEnvelope struct { Data struct { AnimeID int64; Episodes []publicEpisodeFixture; Pagination struct{ HasMore bool; NextCursor *string; RowLimit int } } }
```
Fixture data already inserted by `openEpisodeVersionPublicFixture` (anime IDs 1-6): anime 1 has episodes 11 (2 real variants, 2 groups each), 12 ("Neutral mixed", 0 releases), 13 ("Same number, another neutral episode", a release+version but 0 variants), 14 (1 real variant); anime 2 has zero episodes; anime 3 is `disabled` (404 expected, unrelated to this phase); anime 4 has episode 41 (125 real variants, version_id=40 belongs to groups 1 AND 2 — i.e. a coop-shaped fixture already) and episode 42 ("Neutral after many", 0 releases); anime 5 has episode 51 ("Only neutral", 0 releases); anime 6 is a "Foreign" anime used only for cross-anime cursor rejection tests (unrelated to group filtering, do not touch).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the pre-fix baseline (full test suite + live read-only EXPLAIN for Naruto)</name>
  <files>none (read-only capture; results recorded in this plan's SUMMARY.md only)</files>
  <read_first>
    backend/internal/repository/episode_version_public_query.go (current SQL, unchanged)
    .planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-RESEARCH.md (Environment Availability + Sources sections — this plan reproduces, not re-derives, that baseline)
  </read_first>
  <action>
    Run `cd /home/d1sk/team4s && go test ./... 2>&1 | tee /tmp/163-pre-fix-baseline.txt` from the backend module root (or `cd backend && go test ./...`, matching the project's existing Go module layout) and record the exact pass/fail summary. Diff it against the known pre-existing failure documented in STATE.md (`TestFansubRepository_PublicProfileSourceInvariants`, already-disabled/known-failing, unrelated to this phase) — confirm no OTHER unrelated failures exist beyond that one, and record the full list of failing test names verbatim in the SUMMARY.
    Then, read-only against the live dev stack (per CLAUDE.md's canonical environment: `team4s-linux`, containers already running per `docker compose ps`), run: `docker exec team4sv30-db psql -U <project's configured PG user> -d team4s_v2 -c "EXPLAIN (ANALYZE, BUFFERS) <the exact current publicEpisodeQuery text, with $1=4, $2=0, $3=0, $4=0, $5=25 substituted literally>"` — reproduce 163-RESEARCH.md's documented "before" numbers (unfiltered Naruto, anime_id=4, 220-episode inventory scan, ~1.2ms) as a FRESH read against the CURRENT live data (do not assume the numbers are unchanged; if the live data has drifted since 2026-09-17's research session — e.g. new releases imported — record the actual current numbers instead and note the drift). This is READ-ONLY: no `INSERT`/`UPDATE`/`DELETE`/`ALTER`, per the mandatory "no data changes to team4s_v2" constraint. Use the exact DB credentials/connection convention already established for this project's `docker exec` psql access (check `.env`/`docker-compose.yml` for the Postgres user/db name if unsure — do not guess a wrong username that would fail to connect).
    Record both results (Go suite baseline; live EXPLAIN before-numbers) verbatim in this plan's SUMMARY.md so the phase's Abschlussbericht (163-USER-REQUEST.md closing section) can cite a before/after pair captured during EXECUTION, not only during research.
  </action>
  <verify>
    <automated>test -s /tmp/163-pre-fix-baseline.txt</automated>
  </verify>
  <acceptance_criteria>
    SUMMARY.md contains: (1) the exact `go test ./...` pass/fail line counts and the full list of failing test names, with an explicit note distinguishing the one known pre-existing failure from any others; (2) the literal `EXPLAIN (ANALYZE, BUFFERS)` output text for the current (pre-fix) `publicEpisodeQuery` against `team4s_v2` anime_id=4, unfiltered; (3) an explicit statement "no writes were issued against team4s_v2 in this task."
  </acceptance_criteria>
  <done>Pre-fix baseline (test suite + live EXPLAIN) is documented in SUMMARY.md with literal numbers, not references to 163-RESEARCH.md.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the three existing assertions that currently encode the neutral-row bug (RED)</name>
  <files>backend/internal/repository/episode_version_public_integration_test.go</files>
  <read_first>
    backend/internal/repository/episode_version_public_integration_test.go (full file, already 355 lines — do not add net new lines beyond the corrected assertions themselves; this task only EDITS existing lines, it does not grow the file)
    .planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-PATTERNS.md (Pitfall 2 block, "must-fix baseline assertions in the existing file")
  </read_first>
  <action>
    Apply exactly these three corrections (per 163-PATTERNS.md's Pitfall 2, cross-checked against the fixture data read above) to `episode_version_public_integration_test.go`, and no other lines:
    1. In `TestEpisodeVersionPublicMixedAndIdentity`: change `require.Len(t, page.Data.Episodes, 4)` to `require.Len(t, page.Data.Episodes, 2)` (episode 12 "Neutral mixed" and episode 13 "another neutral episode" must disappear entirely once the fix lands; only episodes 11 and 14 remain). Update the immediately following `require.Equal(t, []int64{11, 13, 12, 14}, ...)` ordering assertion to `require.Equal(t, []int64{11, 14}, []int64{page.Data.Episodes[0].EpisodeID, page.Data.Episodes[1].EpisodeID})` and delete the now out-of-bounds `page.Data.Episodes[1]`/`page.Data.Episodes[2]` neutral-episode assertions (the `require.Empty(t, page.Data.Episodes[1].Versions)` / `require.Empty(t, page.Data.Episodes[2].Versions)` / `require.Nil(t, page.Data.Episodes[2].DefaultVersionID)` lines), keeping only the assertions for episode index 0 (episode 11) and the renumbered final "equal IDs" episode (now index 1, was index 3, episode 14).
    2. In `TestEpisodeVersionPublicAtomicPages`: change `require.Len(t, seen, 126)` to `require.Len(t, seen, 125)`, and replace `require.True(t, seen["42:0"])` with `require.False(t, seen["42:0"], "episode 42 has zero releases and must not appear at all after the fix")`. Also remove the `if len(episode.Versions) == 0 { key := fmt.Sprintf("%d:0", episode.EpisodeID); ...; rows++ }` neutral-row-counting branch inside the per-page loop, since after the fix no episode with zero versions can ever be returned by this endpoint — replace it with an explicit `require.NotEmpty(t, episode.Versions, "public projection must never return a neutral (versionless) episode row")` so a regression here fails loudly instead of silently recounting a row that should not exist.
    3. In `TestEpisodeVersionPublicEmptyAndVisibility`: change `require.Len(t, neutral.Data.Episodes, 1)` / `require.EqualValues(t, 51, neutral.Data.Episodes[0].EpisodeID)` / `require.NotNil(t, neutral.Data.Episodes[0].Versions)` / `require.Empty(t, neutral.Data.Episodes[0].Versions)` (four lines) to a single `require.Empty(t, neutral.Data.Episodes, "anime 5's only episode has zero releases and must not appear after the fix")`.
    Run `go test ./internal/repository/... -run TestEpisodeVersionPublic -v` and confirm all three corrected tests now FAIL against the current (unfixed) code, with failure messages matching the documented neutral-row counts (4 vs 2, 126 vs 125, seen["42:0"]==true vs expected false, 1 vs 0 episodes) — this is the expected, intentional RED state. Do not proceed to fix the production code in this plan.
  </action>
  <verify>
    <automated>go test ./internal/repository/... -run TestEpisodeVersionPublicMixedAndIdentity -run TestEpisodeVersionPublicAtomicPages -run TestEpisodeVersionPublicEmptyAndVisibility -v</automated>
  </verify>
  <acceptance_criteria>
    `go test ./internal/repository/... -run TestEpisodeVersionPublicMixedAndIdentity -v` fails with an actual-vs-expected mismatch on episode count (currently 4, expected 2). `go test ./internal/repository/... -run TestEpisodeVersionPublicAtomicPages -v` fails on `seen` length (currently 126, expected 125) and/or the `seen["42:0"]` assertion. `go test ./internal/repository/... -run TestEpisodeVersionPublicEmptyAndVisibility -v` fails on `neutral.Data.Episodes` not being empty. `wc -l backend/internal/repository/episode_version_public_integration_test.go` reports a line count within a few lines of the original 355 (net edits only, no new helper functions added).
  </acceptance_criteria>
  <done>All three corrected assertions fail today for the documented neutral-row reason; file line count is materially unchanged (no net-new test infrastructure added here).</done>
</task>

<task type="auto">
  <name>Task 3: New sibling test file covering Pflichtfälle B, C, D, E, F, I and the updated J query-budget assertion (RED)</name>
  <files>backend/internal/repository/episode_version_public_group_filter_test.go</files>
  <read_first>
    backend/internal/repository/episode_version_public_integration_test.go (copy the fixture/tracer/request-helper machinery pattern from lines 1-165 verbatim in spirit; package `repository_test`, same imports)
    .planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-PATTERNS.md (fixture-construction pattern, request/response helper pattern, foreign-cursor-rejects-before-SQL pattern sections)
    .planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-RESEARCH.md ("Phase requirements (§15 Pflichtfälle A–J) → test map" table)
  </read_first>
  <action>
    Create `backend/internal/repository/episode_version_public_group_filter_test.go`, package `repository_test`, importing the same packages as the existing integration test file. Because Go test helpers are package-scoped, this new file can call `openEpisodeVersionPublicFixture`, `episodePublicRequest`, and `episodePublicTracer` directly WITHOUT redefining them (same package, same test binary) — do NOT duplicate those functions; only add a second, purpose-built fixture-opener for this file's extra schema needs if the base fixture from Task 2's file is insufficient (see below), following the exact same `testsupport.OpenPhase117Postgres(t)` + `fixture.Exec(...)` + `ApplySQLFile(...)` + `pgxpool.NewWithConfig` + tracer-wiring pattern as `openEpisodeVersionPublicFixture` (never `DATABASE_URL`, never `team4s_v2`).

    Add a new fixture-opener, e.g. `openEpisodeVersionPublicGroupFilterFixture(t *testing.T) (*pgxpool.Pool, *episodePublicTracer)`, that runs the SAME schema-shimming SQL as `openEpisodeVersionPublicFixture` (copy verbatim) plus additive `INSERT` statements for:
    - **Pflichtfall E (Naruto-shaped regression, D-18):** a new anime (e.g. id=7) with episodes 1-5, where episode 1 has a release+version+variant in group "animeownage-like" (reuse existing fixture group ids 1/2, or insert two new named groups e.g. id=3 'animeownage-fixture', id=4 'project-messiah-fixture' with slugs matching those names) ONLY; episode 2 in that same group only; episode 3 and 4 in the OTHER group only; episode 5 in BOTH groups via two `release_version_groups` rows on the same `release_version_id` (coop, Pflichtfall D). This directly mirrors the real Naruto shape documented in 163-CONTEXT.md D-18 (AO on 1/2, PM on 3/4, coop on 5) without touching `team4s_v2`.
    - **Pflichtfall B/C:** covered by the same anime-7 fixture — requesting `fansub=<group-3-slug>` must return only episodes 1, 2, 5; `fansub=<group-4-slug>` must return only episodes 3, 4, 5; no `fansub` param ("Alle") must return all of 1-5.
    - **Pflichtfall F (pagination-scope leak):** extend anime 7 with 24 additional padding episodes (numbers 6-29) that ALL have a release+version+variant in group 3 only, then one more episode (number 30) that has a release+version+variant in group 4 ONLY. Request `fansub=<group-4-slug>&limit=1` and assert episode 30 is the ONLY and FIRST result (proving the filter runs inside the SQL before `LIMIT`, not after — a client-side/post-LIMIT filter would have returned zero rows on this first page).
    - **Pflichtfall I (non-public version):** insert one more `release_versions` row (e.g. id in the 20000+ auto-increment range already established by the existing fixture's `ALTER TABLE ... GENERATED BY DEFAULT AS IDENTITY (START WITH 20000)`) tied to a NEW episode (e.g. episode 31 on anime 7) with a `release_variants` row but ZERO `release_version_groups` rows. Assert this episode never appears, in "Alle" or any group filter — this is D-02's fail-closed rule ("Version ohne Gruppe macht keine Episode sichtbar").
    Write these as literal SQL `INSERT` statements inside the new fixture-opener's `fixture.Exec(...)` call, following the exact ID-numbering and column conventions already used in the base fixture (see `read_first`).

    Add these test functions (still against the CURRENT, unfixed handler/repository — they are expected to fail today, either with a 400 due to the `fansub` param being outside the current strict allowlist, or with a wrong episode set due to the missing filter — both are valid RED signatures, and the assertion text should make clear which failure mode is expected):
    - `TestEpisodeVersionPublicGroupFilterBasics` — Pflichtfälle B, C, D: for each of "Alle" / group-3 / group-4, assert the exact expected episode ID set and, for episode 5 (coop), assert its single version's `fansub_groups` array contains BOTH groups regardless of which single-group filter is active.
    - `TestEpisodeVersionPublicGroupFilterNarutoRegression` — Pflichtfall E: explicit assertion that "group-3 active" excludes episodes 3 and 4 while "group-4 active" excludes episodes 1 and 2 — name the test/assertions after the real Naruto shape (comment referencing D-18) even though the fixture uses synthetic anime id 7, not the live anime_id=4.
    - `TestEpisodeVersionPublicGroupFilterPaginationScope` — Pflichtfall F, as described above.
    - `TestEpisodeVersionPublicGroupFilterNonPublicVersion` — Pflichtfall I, as described above.
    - `TestEpisodeVersionPublicGroupFilterCursorScope` — Pflichtfall G (backend half) / D-06: fetch a `next_cursor` under `fansub=<group-3-slug>&limit=1`, then reissue a request for anime 7 with that SAME cursor but `fansub=<group-4-slug>` (or no `fansub` at all), asserting **400** and, following the exact `require.Empty(t, tr.queries, "foreign anime cursor must fail before SQL")` pattern already used for cross-anime cursors (163-PATTERNS.md), that zero SQL statements ran for the rejected request.
    - `TestEpisodeVersionPublicGroupFilterUnknownSlug` — Pflichtfall H (backend defense-in-depth half) / D-05: request anime 7 with `fansub=does-not-exist-for-this-anime`, asserting **400** (the chosen fail-closed behavior — never unfiltered data, per D-05's explicit "niemals ungefilterte Daten" rule) and that the response body's error message is a generic, non-disclosing string (do not distinguish "typo" from "belongs to another anime" in the message).
    - Extend/replace the query-budget assertion (Pflichtfall J): write a NEW local helper (do not mutate the shared `assertPublicBudget` in the other file, since its hardcoded `require.Len(t, tr.queries, 2, ...)` is correct for requests WITHOUT `fansub` and must stay that way) — e.g. `assertPublicBudgetWithGroupFilter(t *testing.T, tr *episodePublicTracer, limit int, groupFilterPresent bool)` that asserts `tr.queries` has length 2 when `groupFilterPresent` is false and length 3 when true (existence check + slug resolution + main statement), and asserts no query's SQL contains a per-episode loop marker (reuse the existing `require.NotContains(t, ..., "theme_segment")`/`"release_streams"` style checks). Use this helper in `TestEpisodeVersionPublicGroupFilterBasics` for both the `fansub`-set and "Alle" cases, to lock down Pflichtfall J for the new parameter.

    Run `go test ./internal/repository/... -run TestEpisodeVersionPublicGroupFilter -v` and confirm every new test FAILS today (400s for `fansub`-bearing requests due to the strict allowlist rejection, or wrong-episode-set failures for whichever path doesn't hit the allowlist). Do not implement any production code fix in this task.
  </action>
  <verify>
    <automated>go test ./internal/repository/... -run TestEpisodeVersionPublicGroupFilter -v</automated>
  </verify>
  <acceptance_criteria>
    `go test ./internal/repository/... -run TestEpisodeVersionPublicGroupFilter -v` exits non-zero (RED) with every new test function failing for a documented reason (either "expected 200 got 400" from the strict-allowlist rejection of `fansub`, or a wrong-episode-ID-set assertion). `wc -l backend/internal/repository/episode_version_public_group_filter_test.go` reports ≤450 lines (CLAUDE.md cap). `go vet ./...` (or `go build ./...`) succeeds — the new file must compile cleanly even though its assertions fail.
  </acceptance_criteria>
  <done>New sibling test file exists, compiles, and every new test function fails today against the unfixed code for the documented reason; Pflichtfälle B, C, D, E, F, G (backend half), H (backend half), I, J are each covered by at least one failing assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture DB → test binary | Isolated schema/search_path via `testsupport.OpenPhase117Postgres`; never `DATABASE_URL`/`team4s_v2` |
| Live `team4s_v2` (Task 1 only) → this plan | Read-only `psql`/`EXPLAIN` session; no write statements permitted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-01 | Tampering | Task 1's live `team4s_v2` session | mitigate | Session is `EXPLAIN (ANALYZE, BUFFERS)` only, no DML/DDL; acceptance criteria requires an explicit "no writes issued" statement in SUMMARY.md |
| T-163-02 | Information Disclosure | New fixture data (Pflichtfall I: version without a group) | accept | Fixture-only, isolated schema; no production data exposure risk — this IS the regression case being proven, not a live disclosure |
| T-163-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages (test-file-only Go changes); Package Legitimacy Gate not applicable per 163-RESEARCH.md |
</threat_model>

<verification>
1. `go test ./internal/repository/... -run TestEpisodeVersionPublic -v` — the 3 corrected assertions in the existing file fail with documented row-count mismatches.
2. `go test ./internal/repository/... -run TestEpisodeVersionPublicGroupFilter -v` — every new test in the sibling file fails with a documented reason (400-from-strict-allowlist or wrong-episode-set).
3. `go build ./... && go vet ./...` — both test files compile cleanly.
4. SUMMARY.md contains the literal pre-fix `go test ./...` baseline and the literal live `EXPLAIN (ANALYZE, BUFFERS)` before-numbers for Naruto anime_id=4 in `team4s_v2`, plus an explicit "no writes" confirmation.
</verification>

<success_criteria>
Both test files exist, compile, and fail today for documented reasons. No production code (repository/handler/model/OpenAPI) was touched. The pre-fix baseline (test suite + live EXPLAIN) is captured in this plan's own SUMMARY.md.
</success_criteria>

<output>
Create `.planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-01-SUMMARY.md` when done
</output>
