---
phase: 163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern
plan: 04
type: execute
wave: 4
depends_on: ["163-03"]
files_modified:
  - frontend/src/lib/fansub-summary.ts
  - frontend/src/app/anime/[id]/page.tsx
  - frontend/src/components/fansubs/FansubVersionBrowser.tsx
  - frontend/src/components/fansubs/FansubVersionBrowser.module.css
autonomous: true
requirements: [REQ-163-07, REQ-163-09, REQ-163-13, REQ-163-14, REQ-163-18, REQ-163-19, REQ-163-20, REQ-163-21, REQ-163-22, REQ-163-23]

must_haves:
  truths:
    - "Selecting a concrete fansub group hides every episode that group has no matching (or coop) release for, without ever showing an empty card or the removed 'Keine Version dieser Gruppe verfügbar.' text (D-01, D-03, D-15)."
    - "A chip click and a browser back/forward both trigger exactly one fresh, non-merged fetch for the target group, aborting any in-flight request (D-07, D-09)."
    - "During a group switch the previously shown episode list stays visible, dimmed and non-interactive (aria-busy) with no skeleton and no layout jump; the newly selected chip is immediately marked active (D-08)."
    - "A failed group switch shows a compact error with a working 'Erneut versuchen' retry button and shows no stale data from the previous group underneath it; the URL/chip stay on the new (failed) group (D-10)."
    - "The 'Episoden (N)' heading reflects the current filter's hit count from the server response and updates on every group switch, sourced from the client, not anime.episodes.length (D-12)."
    - "An SSR fetch failure never renders anime.episodes (releaseless episodes) any more; it renders a neutral error state instead (D-16)."
    - "A group with zero public releases shows the specific compact empty message, not a generic or missing state (D-14)."
    - "The '+N Versionen' badge already reflects only the active filter's version count with no additional code change needed, since episode.version_count arrives pre-scoped from Plan 163-02's fixed backend query (D-13)."
    - "Every loading, error, empty, and retry element introduced or touched in this plan uses @/components/ui primitives (EmptyState, ErrorState, Button) and global design tokens, with German umlaut-correct text (D-17)."
  artifacts:
    - path: "frontend/src/lib/fansub-summary.ts"
      provides: "Real resolveActiveFansubSlug implementation (Plan 163-03's stub replaced)"
    - path: "frontend/src/app/anime/[id]/page.tsx"
      provides: "SSR fansub forwarding, episodeCount-as-prop, ErrorState fallback"
    - path: "frontend/src/components/fansubs/FansubVersionBrowser.tsx"
      provides: "Group-switch refetch (switchTo), dimming, heading, empty/error states, dead-code removal"
  key_links:
    - from: "frontend/src/app/anime/[id]/page.tsx"
      to: "frontend/src/lib/fansub-summary.ts#resolveActiveFansubSlug"
      via: "SSR fansub-forwarding decision, mirrors client-side single-group/unmatched-slug fallback"
      pattern: "resolveActiveFansubSlug"
    - from: "frontend/src/components/fansubs/FansubVersionBrowser.tsx"
      to: "frontend/src/lib/api.ts#getGroupedEpisodes"
      via: "switchTo/loadMore, shared requestRef abort coordination"
      pattern: "requestRef\\.current\\?\\.abort"
---

<objective>
Turn every RED test from Plan 163-03 GREEN by implementing the group-switch refetch, SSR forwarding, and dead-code removal exactly as specified by the tests and by 163-CONTEXT.md D-07..D-16. No new test scope is introduced here.

Purpose: Deliver the client-visible half of the phase — filtering that actually removes episodes from the DOM per group, a calm (dimmed, non-skeleton) loading experience, honest error/retry, and a hit-count heading, per 163-USER-REQUEST.md §3-§9, §13-§14, §18.
Output: `resolveActiveFansubSlug` implemented; `page.tsx` forwards the resolved filter and no longer renders releaseless episodes on fetch failure; `FansubVersionBrowser.tsx` fetches a fresh page per group switch (never merges across filters), dims the old list while loading, shows a retry control on failure, and renders the hit-count heading itself.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-CONTEXT.md
@.planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-PATTERNS.md
@.planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-03-SUMMARY.md

<interfaces>
<!-- @/components/ui primitives this plan must use (CLAUDE.md UI-primitives rule) — read once, use directly. -->

```ts
// frontend/src/components/ui/EmptyState.tsx
export interface EmptyStateProps {
  title: string; description?: string; action?: ReactNode
  variant?: 'default' | 'withAction' | 'compact' | 'inline'; icon?: ReactNode; className?: string
}
// frontend/src/components/ui/ErrorState.tsx
export interface ErrorStateProps { title: string; description: string; action?: ReactNode }
// frontend/src/components/ui/Button.tsx
export function Button(props: ButtonProps): JSX.Element  // variant, size, loading, onClick, children — already imported in FansubVersionBrowser.tsx
// frontend/src/components/ui/classNames.ts (already used by FansubGroupPicker.tsx)
export function classNames(...args: Array<string | false | null | undefined>): string
```
Both `EmptyState`/`ErrorState` are exported from the `@/components/ui` barrel (`index.ts`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement resolveActiveFansubSlug + page.tsx SSR forwarding, episodeCount, and D-16 fallback removal</name>
  <files>frontend/src/lib/fansub-summary.ts, frontend/src/app/anime/[id]/page.tsx</files>
  <read_first>
    frontend/src/lib/fansub-summary.ts (Plan 163-03's unimplemented `resolveActiveFansubSlug` stub + `buildFansubStoryGroups`'s dedupe pattern to mirror)
    frontend/src/lib/fansub-summary.test.ts (Plan 163-03's RED test cases — this task must make them pass, unmodified)
    frontend/src/app/anime/[id]/page.tsx (full current file)
    frontend/src/app/anime/[id]/page.test.tsx (Plan 163-03's RED/modified assertions — this task must make them pass, unmodified)
  </read_first>
  <behavior>
    - `resolveActiveFansubSlug(relations, rawSlug)`: collect distinct `fansub_group.id` values across `relations` (same guard as `buildFansubStoryGroups`: skip null/missing `fansub_group`); if fewer than 2 distinct groups, or `rawSlug` is falsy, or no option's `fansub_group.slug === rawSlug`, return `undefined`; otherwise return `rawSlug` unchanged.
    - `page.tsx`: split the current 4-way `Promise.allSettled` into (a) a 1-element `Promise.allSettled([getAnimeFansubs(anime.id)])` awaited first, then (b) `resolveActiveFansubSlug(animeFansubsResponse?.data ?? [], rawFansubParam)` computed, then (c) a 3-element `Promise.allSettled([getGroupedEpisodes(anime.id, { projection: 'public', limit: 24, ...(resolvedSlug ? { fansub: resolvedSlug } : {}) }), getAnimeComments(...), getAnimeRelations(...)])` — preserve the exact existing `.status === 'fulfilled' ? value : null` unwrap idiom for every result, do not introduce try/catch where the existing allSettled idiom already covers resilience.
    - `episodeCount` is renamed in intent (not necessarily in variable name) to read `groupedEpisodesResponse?.data.episode_count ?? 0`, passed to `<FansubVersionBrowser episodeCount={episodeCount} .../>` as a NEW prop. The static `<h2>Episoden ({episodeCount})</h2>` line is DELETED from `page.tsx` (it moves into `FansubVersionBrowser`, Task 2).
    - The `groupedEpisodesResponse ? <FansubVersionBrowser .../> : anime.episodes.length === 0 ? <div className={styles.emptyEpisodes}>...</div> : <ul>...anime.episodes fallback...</ul>` three-way branch collapses to a two-way branch: `groupedEpisodesResponse ? <FansubVersionBrowser .../> : <ErrorState title="Episoden konnten nicht geladen werden." description="Die Episodenliste ist derzeit nicht verfügbar. Bitte laden Sie die Seite neu." />`. Import `ErrorState` from `@/components/ui`. The `anime.episodes`-based `<ul>` fallback branch and its `anime.episodes.length === 0` guard are DELETED — this is the exact D-16 fix (a public-endpoint fetch failure must never fall back to rendering releaseless episodes).
  </behavior>
  <action>
    Implement exactly the changes in `<behavior>`. Run `npx vitest run fansub-summary.test.ts page.test.tsx` and iterate until every assertion from Plan 163-03 Task 1/Task 2 is GREEN. Do not modify the test files themselves (they were authored/locked in Plan 163-03) — if a test appears to require a different implementation shape than described here, treat that as a signal to re-read Plan 163-03's test file rather than editing the test.
  </action>
  <verify>
    <automated>npx vitest run fansub-summary.test.ts page.test.tsx</automated>
  </verify>
  <done>resolveActiveFansubSlug is implemented and its tests pass; page.tsx forwards the resolved slug, passes episodeCount as a prop, and no longer renders anime.episodes on fetch failure; all of Plan 163-03's page-level RED tests are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement FansubVersionBrowser group-switch refetch, dimming, heading, empty/error states, dead-code removal</name>
  <files>frontend/src/components/fansubs/FansubVersionBrowser.tsx, frontend/src/components/fansubs/FansubVersionBrowser.module.css</files>
  <read_first>
    frontend/src/components/fansubs/FansubVersionBrowser.tsx (full current file, 323 lines)
    frontend/src/components/fansubs/FansubVersionBrowser.test.tsx (Plan 163-03's corrected assertions)
    frontend/src/components/fansubs/FansubVersionBrowser.groupSwitch.test.tsx (Plan 163-03's new D-07..D-10/G assertions — this task must make BOTH files' relevant tests pass)
    frontend/src/components/fansubs/FansubVersionBrowser.module.css (full current file, 190 lines)
  </read_first>
  <behavior>
    Replace the current `inventory`/`loadState` pair with three separate pieces of state, all keyed off the same shared `requestRef`:
    - `dataState: { slug: string | null; episodes: PublicGroupedEpisode[]; pagination: PublicGroupedEpisodesResponse['data']['pagination'] | undefined; episodeCount: number }` — the last SUCCESSFULLY committed page for the current filter. Initialize from props: `{ slug: activeGroup?.slug ?? null, episodes, pagination, episodeCount: props.episodeCount }` (reuse the existing `activeGroup` derivation already in the component — it already resolves the SAME single-group/unmatched-slug fallback rule as `resolveActiveFansubSlug`, so the initial `dataState.slug` always agrees with what `page.tsx` actually fetched).
    - `switchState: { loading: boolean; error: string | null }` — dedicated to a GROUP SWITCH in flight (D-08 dimming, D-10 error/retry). Never used for `loadMore`.
    - `loadMoreState: { loading: boolean; error: string | null }` — renamed continuation of the EXISTING `loadState` concept, dedicated to the "Weitere laden" action only. Behavior unchanged from today except it reads/writes `dataState` instead of `inventory`.
    - `requestRef: useRef<AbortController | null>(null)` — shared. `switchTo(targetSlug)` UNCONDITIONALLY calls `requestRef.current?.abort()` first (aborting either a stale switch OR an in-flight `loadMore` — this is the D-09 "Wechsel bricht Weitere-laden ab" requirement), then issues its own request. `loadMore()` keeps its existing `if (requestRef.current) return` guard (dedup/no-preempt for repeated loadMore clicks) — it must NOT preempt an in-flight switch.
    - `switchTo(targetSlug: string | null)`: abort+replace `requestRef`; `setLoadMoreState({ loading: false, error: null })` (clear any stale loadMore error, since we're leaving that page entirely); `setSwitchState({ loading: true, error: null })`; call `getGroupedEpisodes(animeID, { projection: 'public', limit: 24, ...(targetSlug ? { fansub: targetSlug } : {}), signal: controller.signal })`; on success (and `!aborted && requestRef.current === controller`), `setDataState({ slug: targetSlug, episodes: response.data.episodes, pagination: response.data.pagination, episodeCount: response.data.episode_count })` and `setSwitchState({ loading: false, error: null })`, then `requestRef.current = null` — do NOT call `mergeEpisodes` (D-07: old list and cursor are discarded, never merged); on failure (same guard), `setSwitchState({ loading: false, error: 'Episoden konnten nicht geladen werden.' })` and `requestRef.current = null`.
    - `resolveGroupSlugForFetch(rawSlug: string | null): string | null` — local helper: returns `null` if `fansubOptions.length < 2` or `!rawSlug` or no option's `fansub_group?.slug === rawSlug`; else returns `rawSlug`. `updateFansubSelection`'s existing slug computation is ALREADY guaranteed valid (derived directly from a real `fansubOptions` entry) so it can call `switchTo(slug)` directly without this helper; the `popstate` handler MUST use `switchTo(resolveGroupSlugForFetch(params.get('fansub')))` since it reads an arbitrary URL value.
    - `updateFansubSelection`: after the existing `pushState`/`setSelectedSlug(slug)` lines, add `switchTo(slug)`.
    - `popstate` handler: after `setSelectedSlug(params.get('fansub'))`, add `switchTo(resolveGroupSlugForFetch(params.get('fansub')))`.
    - `loadMore()`: change its guard to `if (!dataState.pagination?.has_more || !dataState.pagination.next_cursor || requestRef.current) return`; fetch with `{ projection: 'public', limit: 24, ...(dataState.slug ? { fansub: dataState.slug } : {}), cursor: dataState.pagination.next_cursor, signal: controller.signal }` (loadMore stays WITHIN the current filter — always pass `dataState.slug`, never the possibly-stale `selectedSlug`); on success, `setDataState((current) => ({ ...current, episodes: mergeEpisodes(current.episodes, response.data.episodes), pagination: response.data.pagination, episodeCount: response.data.episode_count }))` (mergeEpisodes stays correct here — this is a same-filter continuation, not a switch); on failure, `setLoadMoreState({ loading: false, error: 'Weitere Episoden konnten nicht geladen werden.' })`; the existing `finally` block's reset logic is unchanged in spirit, just targeting `loadMoreState`.
    - Heading (D-12): render `<h2>Episoden ({dataState.episodeCount})</h2>` as the FIRST child inside the component's returned `<section className={styles.section}>`, above the `fansubOptions.length > 0 ? <FansubGroupPicker .../> : null` block — this replaces the heading `page.tsx` used to render (Task 1 deletes it there).
    - Rendering order below the heading/picker: `switchState.error ? <ErrorState title="Episoden konnten nicht geladen werden." description={switchState.error} action={<Button variant="secondary" onClick={() => switchTo(resolveGroupSlugForFetch(selectedSlug))}>Erneut versuchen</Button>} /> : dataState.episodes.length === 0 ? (activeFansubGroupID !== null ? <EmptyState variant="compact" title="Keine Releases für diese Fansub-Gruppe" description="Für diese Fansub-Gruppe sind derzeit keine öffentlichen Releases hinterlegt." /> : <EmptyState variant="compact" title="Keine Episoden-Versionen vorhanden" />) : <ul aria-busy={switchState.loading} className={classNames(styles.episodeList, switchState.loading && styles.episodeListDimmed)}>...map dataState.episodes exactly as today...</ul>`. This is the D-10 "no stale data underneath the error" rule (error replaces the list entirely, it does not sit above a dimmed stale list) combined with D-08 (dimming only applies while loading, not on terminal error) and D-14 (group-specific vs. generic empty copy).
    - Inside the episode `<li>` map, DELETE the `groupMatchedVersions`/`hasNoMatchingVersion` local computation and the `hasNoMatchingVersion ? <div className={styles.noVersionHint}>...</div> : groupMatchedVersions.map(...)` conditional (D-15) — replace with a direct `episode.versions.map((version) => ( ...unchanged version row JSX... ))`, since `episode.versions` arriving from the server is now already scoped to the active filter (Pitfall 3). Do NOT change `getSummaryVersion` — it still operates correctly over the already-filtered `episode.versions` array (its `activeFansubGroupID`-based preference search becomes a no-op refinement, not incorrect; leaving it unchanged avoids unrelated scope creep).
    - Delete the now-dead `.noVersionHint`/`.noVersionText`/`.noVersionAction` CSS rules from `FansubVersionBrowser.module.css` (their only consumer is removed above) and add `.episodeListDimmed { opacity: 0.5; pointer-events: none; }`.
    - D-13 ("+N Versionen" badge reflects the active filter) requires NO code change — `episode.version_count` already arrives pre-scoped from the fixed backend query (Plan 163-02); `formatVersionCount(episode.version_count)` is already correct as-is.
  </behavior>
  <action>
    Implement exactly the state model, functions, and JSX changes described in `<behavior>`. Add the `episodeCount: number` field to `FansubVersionBrowserProps`. Run `npx vitest run FansubVersionBrowser.test.tsx FansubVersionBrowser.groupSwitch.test.tsx` and iterate until every test from Plan 163-03 is GREEN. Do not modify either test file (they were locked in Plan 163-03).
  </action>
  <verify>
    <automated>npx vitest run FansubVersionBrowser.test.tsx FansubVersionBrowser.groupSwitch.test.tsx</automated>
  </verify>
  <done>Both test files are fully GREEN; group switches fetch-and-replace (never merge); the old list dims via aria-busy while loading; a failed switch shows ErrorState+retry with no stale data beneath it; the heading and empty states are correct per D-12/D-14.</done>
</task>

<task type="auto">
  <name>Task 3: Full frontend regression (typecheck, lint, test, build)</name>
  <files>none (verification only)</files>
  <read_first>
    .planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-03-SUMMARY.md (documents the pre-existing 2 CSS-guard failures this task must diff against)
  </read_first>
  <action>
    Run, in order: `npm run typecheck` (must now be CLEAN — Plan 163-03's documented `episodeCount`-prop RED gap is resolved by Task 1/2 above; any OTHER new type error is a real regression to fix), `npm run lint` (0 new findings versus STATE.md's documented baseline — 13 errors/331 warnings globally as of the last recorded phase; this phase's changed files must show 0 findings), `npm run test` (full Vitest suite; diff against the 2 pre-existing, documented `cssCustomProperties.guard.test.ts` failures — 0 other new failures tolerated), and a scoped production build check for the touched routes (`npm run build` or the project's existing selective-build convention if a global build is known to have a pre-existing unrelated blocker per STATE.md — confirm which applies before running, and record which you used).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run test</automated>
  </verify>
  <acceptance_criteria>
    `npm run typecheck` exits 0. `npm run lint` shows 0 new findings in the 4 files this plan touched. `npm run test` shows 0 new failures beyond the 2 documented pre-existing CSS-guard failures. The build check either succeeds cleanly or reproduces only the already-documented pre-existing blocker (never a NEW one).
  </acceptance_criteria>
  <done>Full frontend regression is clean against the documented pre-existing baseline; SUMMARY.md records the exact command outputs and any pre-existing-vs-new failure diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser URL (chip click, manual edit, back/forward) → `getGroupedEpisodes` fansub param | Untrusted client-controlled filter value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-08 (D-05) | Tampering | Manually edited `?fansub=` URL value reaching a popstate-triggered refetch | mitigate | `resolveGroupSlugForFetch` validates the raw URL value against the actually-fetched `fansubOptions` before ever sending it as a filter (162's fallback rule, D-05); an unmatched value is treated as "Alle" client-side, and the backend (Plan 163-02) independently fails closed (400) as defense-in-depth if a request bypasses this client check entirely |
| T-163-09 | Denial of Service (self-inflicted, UX) | Rapid repeated chip clicks | mitigate | Each `switchTo` call aborts the previous in-flight request via the shared `requestRef`; only the last selection can ever set state (D-09) |
| T-163-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages; Package Legitimacy Gate not applicable |

**Cross-reference:** D-01/D-02 (fail-closed visibility) and D-06 (cursor scope-check) are enforced server-side by Plan 163-02 (T-163-03/T-163-05/T-163-04); this plan's client-side D-05 handling (T-163-08) is defense-in-depth on top of that server-side gate, not a replacement for it.
</threat_model>

<verification>
1. `npx vitest run fansub-summary.test.ts page.test.tsx FansubVersionBrowser.test.tsx FansubVersionBrowser.groupSwitch.test.tsx` — all GREEN.
2. `npm run typecheck` — 0 errors.
3. `npm run lint` — 0 new findings.
4. `npm run test` (full suite) — 0 new failures vs. documented baseline.
5. `git diff --stat` shows only the 4 files in `files_modified` above touched.
</verification>

<success_criteria>
Every RED test authored in Plan 163-03 is GREEN. The client-visible filtering, dimming, error/retry, and hit-count heading behaviors match 163-CONTEXT.md D-07 through D-16 exactly, with no unrelated files touched.
</success_criteria>

<output>
Create `.planning/phases/163-oeffentliche-anime-seite-episoden-nach-fansub-gruppe-filtern/163-04-SUMMARY.md` when done
</output>
