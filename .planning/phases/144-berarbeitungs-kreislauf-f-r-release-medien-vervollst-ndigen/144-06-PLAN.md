---
phase: 144
plan: 06
type: execute
wave: 5
depends_on: ["144-05"]
files_modified:
  - frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseVersionMedia.ts
  - frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.helpers.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.test.tsx
autonomous: true
requirements:
  - "Zielbild 1 (144-CONTEXT.md): file-replace control for a rejected item, submitter side"
  - "Zielbild 2 (144-CONTEXT.md): category field in the same form"
  - "UI-SPEC Interaction Contract — File Replace (submitter side)"
must_haves:
  truths:
    - "Opening the edit drawer on a rejected, editable item shows a category Select and a file-replace drop-zone, in addition to the existing caption field"
    - "Opening the edit drawer on a non-rejected item, or a rejected item the actor cannot edit, shows neither the category Select nor the file-replace control"
    - "The primary action button reads 'Erneut einreichen' and is disabled while nothing is staged, and 'Überarbeitung einreichen' once a file, category, or caption change is staged, for a rejected item"
    - "Submitting with a staged file calls the new replace action; submitting with only category/caption changes (no staged file) calls the existing patch action"
  artifacts:
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseVersionMedia.ts"
      provides: "replaceItem hook action"
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.tsx"
      provides: "category Select + file-replace drop-zone in the edit drawer, three-state primary button label"
  key_links:
    - from: "ReleaseVersionMediaSection.tsx (handleSaveSelectedItem)"
      to: "useReleaseVersionMedia.ts (replaceItem)"
      via: "await media.replaceItem(selectedItem.id, { file: stagedReplaceFile, category, caption, isPreviewCandidate })"
      pattern: "media\\.replaceItem\\("
    - from: "ReleaseVersionMediaSection.tsx"
      to: "frontend/src/app/admin/fansubs/releaseReviewPresentation.ts"
      via: "import { RELEASE_REVIEW_REJECTION_CATEGORY_LABELS } from '../../../fansubs/releaseReviewPresentation'"
      pattern: "RELEASE_REVIEW_REJECTION_CATEGORY_LABELS"
---

<objective>
Build the submitter-facing half of Zielbild 1 and 2: inside the existing "Medium bearbeiten" drawer,
a rejected item's submitter can now change the category, stage a replacement file, and submit both
(plus the existing caption field) in one action — closing CONTEXT.md's "Der Kreislauf fordert nach
einer Ablehnung eine Überarbeitung und verweigert dann genau die Änderung, die den Ablehnungsgrund
beheben würde" problem statement end to end on the submitter side.

Purpose: this is the UI consumer of 144-04's new PUT endpoint and 144-05's `replaceItem`
API-client wrapper; per 144-UI-SPEC.md's Interaction Contract, every new element extends the
already-shipped drawer rather than introducing a second visual language.
Output: a working `replaceItem` hook action; a category `Select` + file-replace drop-zone rendered
only for rejected, editable items; a three-state primary button label; tests proving the gating and
the wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/144-berarbeitungs-kreislauf-f-r-release-medien-vervollst-ndigen/144-CONTEXT.md
@.planning/phases/144-berarbeitungs-kreislauf-f-r-release-medien-vervollst-ndigen/144-UI-SPEC.md
@.planning/phases/144-berarbeitungs-kreislauf-f-r-release-medien-vervollst-ndigen/144-PATTERNS.md

<interfaces>
From frontend/src/lib/api.ts (landed in 144-05): `replaceReleaseVersionMediaFile(options: ReplaceReleaseVersionMediaFileOptions): Promise<ReleaseVersionMediaItem>` where `ReplaceReleaseVersionMediaFileOptions = { versionId, relationId, file, category?, caption?, isPreviewCandidate?, sourceRevision?, onProgress? }`.

From frontend/src/app/admin/fansubs/releaseReviewPresentation.ts (landed in 144-05): `RELEASE_REVIEW_REJECTION_CATEGORY_LABELS: Record<ReleaseReviewRejectionCategory, string>` — relative import path from this plan's component file is `../../../fansubs/releaseReviewPresentation`.

From frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseVersionMedia.ts (the whole 444-line file, read in full):
  `export interface UseReleaseVersionMediaResult { items; isLoading; error; reload; uploadItems; startUpload; retryUpload; clearUploadQueue; patchItem: (mediaId: number, patch: ReleaseVersionMediaPatchRequest) => Promise<void>; deleteItem; reorderItems; patchError; deleteError; reorderError; capabilities?; capabilitiesError? }` (lines 35-57) — add `replaceItem` and `replaceError` here.
  `patchItem` (lines 311-344) — the EXACT shape to mirror for `replaceItem`: `setPatchError(null)` -> try -> resolve `itemsRef.current.find(i => i.id === mediaId)` for the revision-bound guard -> call the API function -> `setItems(current => current.map(item => item.id === mediaId ? updated : item))` (reuse the same is_preview_candidate-exclusivity side effect at lines 326-328) -> catch -> `readUploadError` -> `setPatchError`/`setReplaceError` -> re-throw.
  `readUploadError(error, fallback)` (lines 77-85) — reuse directly, do not reimplement.
  `itemsRef` (line 99, kept in sync via a `useEffect` at lines 101-103) — the existing pattern for reading the LATEST item's `source_revision` without a stale closure.

From frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.tsx (the whole 671-line file, already read in full):
  `REJECTION_CATEGORY_LABELS` local const (lines 72-78) — DELETE this, replace all its usages with the imported `RELEASE_REVIEW_REJECTION_CATEGORY_LABELS`.
  `handleSaveSelectedItem` (lines 282-300) — the function this plan's staged-file branching logic extends.
  The "Medium bearbeiten" `Drawer` block (lines 600-668) — exact insertion points: category `FormField`+`Select` goes between the existing rejection-context `.statusHint` block (lines 638-645) and the `FormField label="Beschreibung"` block (line 646); the file-replace `FormField`+drop-zone goes immediately after the category field, before the caption `FormField`.
  Footer primary button (lines 617-624) — the exact ternary to extend into the three UI-SPEC states.
  Existing drop-zone markup (lines 507-550, the UPLOAD sheet's drop-zone) — reuse `styles.dropZone`/`styles.dropZoneActive`/`styles.dropZoneDisabled` class names verbatim for the NEW replace drop-zone; do not invent new CSS classes.
  `buildLocalPreviewURL` (imported from `./ReleaseVersionMediaSection.helpers`, already used at line 116) — call this for the staged replacement file so `.editPreview` (lines 630-636) swaps to the local object-URL when a file is staged.
  `canEditSelectedItem` (line 183), `selectedItem` (line 149) — the existing gating booleans this plan's new controls key off.

From frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.helpers.tsx (the whole 63-line file, already read in full):
  `CATEGORY_OPTIONS` (lines 11-16) — reuse for the new category `Select`, same options already used for the top-level segmented control.
  This is where to add a new pure helper `resolveEditDrawerPrimaryLabel(item: ReleaseVersionMediaItem, hasStagedChanges: boolean): string` per the "reine Konstanten und Utilities... kein State, keine Hooks, kein JSX" file convention already documented at the top of this file.

Global UI primitives (mandatory, CLAUDE.md): `Select`, `FormField` from `@/components/ui` — this file already imports `FormField`/`Textarea`/`Button`/`Badge`/`Drawer`/`EmptyState` from `@/components/ui`; add `Select` to that same import line. No native `<select>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: replaceItem hook action</name>
  <read_first>
    frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseVersionMedia.ts
  </read_first>
  <behavior>
    - `replaceItem(mediaId, { file, category, caption, isPreviewCandidate })` calls `replaceReleaseVersionMediaFile` with `versionId`, `relationId: mediaId`, the staged fields, and `sourceRevision` resolved from `itemsRef.current.find(i => i.id === mediaId)?.source_revision` (mirrors patchItem's revision-binding).
    - On success, replaces the matching item in `items` state with the API response and clears `replaceError`.
    - On failure, sets `replaceError` to a German fallback message via `readUploadError` and re-throws (caller decides UI feedback), leaving `items` state untouched.
  </behavior>
  <action>
    In useReleaseVersionMedia.ts: add `const [replaceError, setReplaceError] = useState<string | null>(null)`. Add
    `interface ReplaceItemOptions { file: File; category?: ReleaseVersionMediaCategory; caption?: string | null; isPreviewCandidate?: boolean }`
    and a `replaceItem = useCallback(async (mediaId: number, options: ReplaceItemOptions) => { if (versionId === null) return; setReplaceError(null); try { const currentItem = itemsRef.current.find(item => item.id === mediaId); const updated = await replaceReleaseVersionMediaFile({ versionId, relationId: mediaId, file: options.file, category: options.category, caption: options.caption, isPreviewCandidate: options.isPreviewCandidate, sourceRevision: currentItem?.source_revision ?? undefined }); setItems(current => current.map(item => { if (item.id === mediaId) return updated; if (options.isPreviewCandidate === true) return { ...item, is_preview_candidate: false }; return item })) } catch (replaceItemError) { const message = readUploadError(replaceItemError, 'Datei konnte nicht ersetzt werden. Versuch es erneut oder wähle eine andere Datei.'); setReplaceError(message); throw replaceItemError } }, [versionId])`.
    Import `replaceReleaseVersionMediaFile` from `@/lib/api` in the existing import block. Add `replaceItem` and `replaceError` to `UseReleaseVersionMediaResult` (interface + the hook's final return object).
  </action>
  <acceptance_criteria>
    <criterion type="source">UseReleaseVersionMediaResult declares `replaceItem: (mediaId: number, options: { file: File; category?: ReleaseVersionMediaCategory; caption?: string | null; isPreviewCandidate?: boolean }) => Promise<void>` and `replaceError: string | null`</criterion>
    <criterion type="cli">docker compose exec team4sv30-frontend npx tsc --noEmit reports no new errors in useReleaseVersionMedia.ts</criterion>
    <criterion type="test-command">docker compose exec team4sv30-frontend npx vitest run src/app/admin/episode-versions/\\[versionId\\]/edit/ReleaseVersionMediaSection.test.tsx passes (existing makeMediaState mock must be extended with replaceItem/replaceError in the SAME task or Task 3 — do not leave the mock stale)</criterion>
  </acceptance_criteria>
  <done>replaceItem exists, uses the same revision-binding/error-handling shape as patchItem, and the hook's public interface documents it.</done>
</task>

<task type="auto">
  <name>Task 2: Category field, file-replace control, and three-state submit button in the edit drawer</name>
  <read_first>
    frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.tsx
    frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.helpers.tsx
    frontend/src/app/admin/fansubs/releaseReviewPresentation.ts
  </read_first>
  <action>
    ReleaseVersionMediaSection.helpers.tsx: add `export function resolveEditDrawerPrimaryLabel(item: { review_state?: string | null } | null, hasStagedChanges: boolean): string { if (item?.review_state === 'rejected') return hasStagedChanges ? 'Überarbeitung einreichen' : 'Erneut einreichen'; return 'Speichern' }` per the UI-SPEC Copywriting Contract table's three CTA states.

    ReleaseVersionMediaSection.tsx:
    1. Import `Select` from `@/components/ui` (extend the existing import line). Import `RELEASE_REVIEW_REJECTION_CATEGORY_LABELS` from `../../../fansubs/releaseReviewPresentation`. Import `resolveEditDrawerPrimaryLabel` from `./ReleaseVersionMediaSection.helpers` (extend the existing named-import list).
    2. Delete the local `REJECTION_CATEGORY_LABELS` const (lines 72-78); replace its one usage (line 641) with `RELEASE_REVIEW_REJECTION_CATEGORY_LABELS`.
    3. Add component state: `const [editCategory, setEditCategory] = useState<ReleaseVersionMediaCategory>('screenshot')` and `const [stagedReplaceFile, setStagedReplaceFile] = useState<File | null>(null)`. In `openEditSheet(item)` (lines 193-198), add `setEditCategory(item.category); setStagedReplaceFile(null)`.
    4. Compute `const isRejectedEditable = Boolean(selectedItem?.review_state === 'rejected' && canEditSelectedItem)` and `const hasStagedChanges = Boolean(stagedReplaceFile) || (selectedItem != null && editCategory !== selectedItem.category) || (selectedItem != null && (editCaption.trim() || null) !== (selectedItem.caption ?? null))`.
    5. Inside the "Medium bearbeiten" Drawer body, between the existing rejection-context `.statusHint` block and the `Beschreibung` `FormField` (per this plan's interfaces block insertion point), add — gated on `isRejectedEditable`: (a) `FormField label="Kategorie" hint="Falsche Kategorie war der Ablehnungsgrund? Hier direkt korrigieren."` wrapping a `Select` bound to `editCategory`/`setEditCategory`, `options` built from `CATEGORY_OPTIONS`; (b) `FormField label="Datei ersetzen" hint="Ersetzt die aktuell abgelehnte Datei durch eine neue Fassung. Beschreibung und Kategorie kannst du im selben Formular anpassen."` wrapping a single-file drop-zone reusing `styles.dropZone`/`styles.dropZoneActive`/`styles.dropZoneDisabled` classes (a local `isReplaceDragActive` state + its own `<input type="file" accept="image/*">`, NO `multiple` attribute, `onChange`/`onDrop` setting `stagedReplaceFile` to the single selected/dropped file) with prompt text "Neue Datei hier hineinziehen oder antippen." per the UI-SPEC.
    6. Update `.editPreview` (lines 630-636): when `stagedReplaceFile` is set, render `<img src={buildLocalPreviewURL(stagedReplaceFile) ?? ''} alt="" />` instead of the persisted `selectedItem.original_url`/`thumbnail_url`, so the submitter sees exactly what they are about to submit.
    7. Update the footer primary button (lines 617-624): label becomes `resolveEditDrawerPrimaryLabel(selectedItem, hasStagedChanges)`; `disabled` becomes `!canEditSelectedItem || (selectedItem?.review_state === 'rejected' && !hasStagedChanges)` (prevents a no-op resubmit of an unchanged rejected item, per UI-SPEC).
    8. Rewrite `handleSaveSelectedItem` (lines 282-300): if `stagedReplaceFile` is set, call `await media.replaceItem(selectedItem.id, { file: stagedReplaceFile, category: editCategory !== selectedItem.category ? editCategory : undefined, caption: editCaption.trim() === '' ? null : editCaption.trim(), isPreviewCandidate: canEditPreviewCandidate ? editPreviewCandidate : undefined })` and on success show toast `'Überarbeitung eingereicht.'` (per UI-SPEC); else keep the existing `patchItem` call but extend its `patch` object with `category: editCategory !== selectedItem.category ? editCategory : undefined` (only for the rejected/editable case — non-rejected items never render the category field so `editCategory` stays equal to `selectedItem.category` for them and the field is naturally omitted). Reset `stagedReplaceFile` to `null` and `setSelectedItemId(null)` on success, matching the existing pattern.
  </action>
  <acceptance_criteria>
    <criterion type="source">ReleaseVersionMediaSection.tsx does not contain a local `REJECTION_CATEGORY_LABELS` declaration (only the imported `RELEASE_REVIEW_REJECTION_CATEGORY_LABELS`)</criterion>
    <criterion type="source">the file imports `Select` from `@/components/ui` and does not contain a native `<select` element</criterion>
    <criterion type="source">the footer primary button's label expression calls `resolveEditDrawerPrimaryLabel`</criterion>
    <criterion type="cli">docker compose exec team4sv30-frontend npx tsc --noEmit reports no new errors in ReleaseVersionMediaSection.tsx</criterion>
    <criterion type="cli">docker compose exec team4sv30-frontend npx eslint frontend/src/app/admin/episode-versions/\[versionId\]/edit/ReleaseVersionMediaSection.tsx reports zero no-restricted-syntax findings</criterion>
  </acceptance_criteria>
  <done>The edit drawer shows the category field and file-replace control only for rejected, editable items, the primary button reflects the three documented states, and submit routes to replaceItem or patchItem correctly depending on whether a file is staged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Frontend tests for the gating, button states, and submit routing</name>
  <read_first>
    frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMediaSection.test.tsx
  </read_first>
  <behavior>
    - Test: opening the edit drawer for a rejected, editable item (`can_update: true`) renders a "Kategorie"-labeled Select and a "Datei ersetzen"-labeled control; the same drawer for a `review_state: 'confirmed'` item renders neither.
    - Test: for a rejected editable item with no staged changes, the primary button reads "Erneut einreichen" and is disabled; after selecting a file via the replace input, it reads "Überarbeitung einreichen" and is enabled.
    - Test: clicking the (now-enabled) primary button with a staged file calls `media.replaceItem` with the staged file; clicking it after only changing the category Select (no staged file) calls `media.patchItem` with a `category` field, not `replaceItem`.
  </behavior>
  <action>
    Extend `makeMediaState` in ReleaseVersionMediaSection.test.tsx (lines 63-95) with `replaceItem: vi.fn().mockResolvedValue(undefined), replaceError: null`. Add a new `describe('ReleaseVersionMediaSection Phase 144 replace-file drawer', ...)` block (following the file's existing `describe`-per-feature convention) implementing the three behaviors above, using `makeItem({ review_state: 'rejected', can_update: true, rejection_category: 'quality.insufficient', rejection_reason: 'Testgrund' })` as the rejected fixture and `fireEvent.change`/`fireEvent.click` against the existing `openUploadSheet`-style helper pattern already in this file (add an analogous `openEditSheetFor(item)` helper if none exists for opening the edit drawer for a specific item).
  </action>
  <acceptance_criteria>
    <criterion type="test-command">docker compose exec team4sv30-frontend npx vitest run src/app/admin/episode-versions/\\[versionId\\]/edit/ReleaseVersionMediaSection.test.tsx passes, including the 3 new tests</criterion>
    <criterion type="cli">docker compose exec team4sv30-frontend npx vitest run reports zero unexpected regressions in files outside this plan's scope</criterion>
  </acceptance_criteria>
  <done>All three new behaviors are proven by automated tests, and the full existing test file still passes with the extended mock shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Submitter browser -> new drawer controls -> 144-04's PUT endpoint | Client-side gating only (`isRejectedEditable`) — the actual authorization decision is server-side in 144-04's handler; this UI must never be treated as an enforcement layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-06-01 | Spoofing | client-side visibility gate | accept | `isRejectedEditable`/`canEditSelectedItem` only controls what renders; even a manually-crafted request to the PUT endpoint is still gated by 144-04's server-side `canMutateReleaseVersionMediaRelation` check, which this plan does not and cannot weaken |
| T-144-06-02 | Tampering | staged file object-URL preview | accept | `buildLocalPreviewURL` only creates a browser-local `blob:` URL for the CURRENT user's own file selection, never transmitted or trusted server-side; the actual file bytes are validated server-side by 144-04's reused multipart guards |
</threat_model>

<verification>
- `docker compose exec team4sv30-frontend npx tsc --noEmit` clean for all touched files.
- `docker compose exec team4sv30-frontend npx vitest run src/app/admin/episode-versions/\[versionId\]/edit/ReleaseVersionMediaSection.test.tsx` passes.
- `docker compose exec team4sv30-frontend npx eslint .` reports zero `no-restricted-syntax` findings in the two touched files.
</verification>

<success_criteria>
A rejected item's submitter can change category and/or stage a replacement file in the same drawer
used for caption edits, sees a button label that honestly reflects what will happen, and submitting
routes correctly to the new replace action or the existing patch action.
</success_criteria>

<output>
Create `.planning/phases/144-berarbeitungs-kreislauf-f-r-release-medien-vervollst-ndigen/144-06-SUMMARY.md` when done
</output>
