---
phase: 75-anime-gruppen-deep-dive-anime-id-group-groupid
plan: 02
type: execute
wave: 2
depends_on:
  - 75-01
files_modified:
  - frontend/src/app/anime/[id]/group/[groupId]/page.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/page.module.css
  - frontend/src/app/anime/[id]/group/[groupId]/GroupSectionsNav.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/sections/HeroSection.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/sections/StorySection.tsx
autonomous: true
requirements:
  - D
  - A
  - K

must_haves:
  truths:
    - "page.tsx is an async Server Component ≤150 lines that orchestrates all fetches and renders section components as props receivers — narrative, einspaltige Scroll-Seite, bestehende Hero-/Showcase-Logik in Abschnitte verschoben, keine neue Route (D-01)"
    - "HeroSection exists as a separate file containing the existing hero/backdrop/poster/stats/breadcrumb/edge-nav/GroupAssetShowcase logic extracted from old page.tsx"
    - "StorySection renders anime_fansub_project_notes.body_html (preferred) with fallback to group.story; shows EmptyState when both null"
    - "GroupSectionsNav renders five anchor chips (Geschichte, Beteiligte, Releases, OP/ED/Middle, Medien) with IntersectionObserver active-state; uses Button variant ghost/subtle from @/components/ui"
    - "Section anchor IDs present in page layout: #story, #team, #releases, #themes, #medien"
    - "No native <button>/<select>/<input> outside @/components/ui (CLAUDE.md)"
    - "All user-facing strings use correct German umlauts (per CLAUDE.md)"
    - "No file exceeds 450 lines (CLAUDE.md modularity)"
  artifacts:
    - path: "frontend/src/app/anime/[id]/group/[groupId]/sections/HeroSection.tsx"
      provides: "Hero section component — extracted from old page.tsx hero block"
    - path: "frontend/src/app/anime/[id]/group/[groupId]/sections/StorySection.tsx"
      provides: "Story section with CollapsibleStory + EmptyState (D-05)"
    - path: "frontend/src/app/anime/[id]/group/[groupId]/GroupSectionsNav.tsx"
      provides: "Sticky navigation Client Component with IntersectionObserver (D-04)"
    - path: "frontend/src/app/anime/[id]/group/[groupId]/page.tsx"
      provides: "Refactored page orchestrator — sections layout shell with graceful fetch degradation"
  key_links:
    - from: "frontend/src/app/anime/[id]/group/[groupId]/page.tsx"
      to: "GroupSectionsNav, HeroSection, StorySection"
      via: "JSX import + props handoff"
      pattern: "import.*HeroSection|StorySection|GroupSectionsNav"
    - from: "frontend/src/app/anime/[id]/group/[groupId]/sections/StorySection.tsx"
      to: "CollapsibleStory"
      via: "import from @/components/groups/CollapsibleStory"
      pattern: "CollapsibleStory"
---

<objective>
Refactor the existing page.tsx into a lean orchestrator shell (≤150 lines) and extract HeroSection and StorySection as separate components. Add GroupSectionsNav as the single Client Component for sticky scroll-tracking navigation. This plan ships the structural page skeleton — all sections present with their anchor IDs, the section order locked per D-02, and existing Hero+Story logic faithfully moved without behavior change. The three new data sections (Team, Themes, Media) receive empty/stub props in this plan; they are wired with real data in Plan 03.

Purpose: Splitting page.tsx is required by the 450-line CLAUDE.md modularity constraint. Doing this before Plan 03 adds more sections ensures the orchestrator is clean before new content is added.

Output: Refactored page.tsx (≤150 lines), new sections/ directory, HeroSection.tsx, StorySection.tsx, GroupSectionsNav.tsx, extended page.module.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s-phase75/.planning/ROADMAP.md
@C:/Users/admin/Documents/Team4s-phase75/.planning/phases/75-anime-gruppen-deep-dive-anime-id-group-groupid/75-CONTEXT.md
@C:/Users/admin/Documents/Team4s-phase75/.planning/phases/75-anime-gruppen-deep-dive-anime-id-group-groupid/75-RESEARCH.md
@C:/Users/admin/Documents/Team4s-phase75/.planning/phases/75-anime-gruppen-deep-dive-anime-id-group-groupid/75-PATTERNS.md
@C:/Users/admin/Documents/Team4s-phase75/.planning/phases/75-anime-gruppen-deep-dive-anime-id-group-groupid/75-UI-SPEC.md
@C:/Users/admin/Documents/Team4s-phase75/.planning/phases/75-anime-gruppen-deep-dive-anime-id-group-groupid/75-01-SUMMARY.md

<interfaces>
<!-- Key patterns the executor MUST read before implementing. -->

Existing page.tsx analog (read first — preserve ALL existing logic, only move it):
  frontend/src/app/anime/[id]/group/[groupId]/page.tsx (currently ~290 lines)

CollapsibleStory interface (read before implementing StorySection):
  frontend/src/components/groups/CollapsibleStory.tsx
  Props: { content: string } — accepts plain text or HTML; collapse threshold 400 chars

Button primitive variants available (for GroupSectionsNav chips):
  variant="ghost" → transparent, muted text (inactive chip)
  variant="subtle" → 10% accent background (active chip)
  size="sm"

UI primitives available from @/components/ui:
  Button, Card, Badge, SectionHeader, EmptyState

Section anchor IDs (verbindlich per RESEARCH Befund 6 / D-04):
  #story, #team, #releases, #themes, #medien

Sticky-Nav labels (per UI-SPEC Copywriting Contract):
  Geschichte, Beteiligte, Releases, OP/ED/Middle, Medien

Section headings (per UI-SPEC):
  Projektgeschichte, Beteiligte am Projekt, Releases & Versionen, OP / ED / Middle, Release-Einblicke
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract HeroSection + StorySection from existing page.tsx (per D-01, D-02, A)</name>
  <files>
    frontend/src/app/anime/[id]/group/[groupId]/sections/HeroSection.tsx
    frontend/src/app/anime/[id]/group/[groupId]/sections/StorySection.tsx
  </files>
  <read_first>
    frontend/src/app/anime/[id]/group/[groupId]/page.tsx
    frontend/src/components/groups/CollapsibleStory.tsx
    frontend/src/types/group.ts
    frontend/src/components/groups/GroupEdgeNavigation.tsx
  </read_first>
  <action>
    Create sections/ directory under frontend/src/app/anime/[id]/group/[groupId]/.

    Create HeroSection.tsx: Move the entire hero block from existing page.tsx into this component. The hero block includes: backdrop image element, poster image element, info panel div with group logo, anime/group title, stats (member_count, episode_count), fansub link, GroupAssetShowcase component, GroupEdgeNavigation, and Breadcrumbs. Props interface HeroSectionProps must accept all data the hero currently consumes: group: GroupDetail, anime: AnimeDetail (read the existing type from types/group.ts or types/anime.ts), groupID: number, animeID: number, and the computed style objects and URLs already calculated in page.tsx (heroBackdropUrl, infoPanelBackgroundUrl, posterImage, heroStyle, infoPanelStyle, pageStyle, breadcrumbItems, navigationGroups, groupAssetsResponse, releaseEpisodes). Keep the exact JSX from the existing hero block — do not rewrite styling, class names, or element structure. Import Image from next/image, Link from next/link, Breadcrumbs, GroupEdgeNavigation, GroupAssetShowcase, styles from ../page.module.css. Component is a plain (non-client) function component receiving props.

    Create StorySection.tsx: Props interface StorySectionProps { story: string | null | undefined; projectNotesHtml: string | null | undefined }. Logic: const displayContent = projectNotesHtml || story || null. If displayContent: render SectionHeader title="Projektgeschichte" and CollapsibleStory content={displayContent}. If null: render SectionHeader and EmptyState variant="compact" title="Noch keine Projektgeschichte" description="Für dieses Projekt wurde bisher keine Geschichte hinterlegt." (correct umlauts). Import CollapsibleStory from @/components/groups/CollapsibleStory, SectionHeader and EmptyState from @/components/ui. Wrap in div with id="story" className={styles.storySection}. Keep ≤60 lines.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s-phase75/frontend && npm run typecheck 2>&amp;1 | grep -E "error|Error" | head -20</automated>
  </verify>
  <done>
    HeroSection.tsx and StorySection.tsx exist in sections/; npm run typecheck shows no errors for new files; HeroSection receives all existing hero props; StorySection prefers projectNotesHtml over story with EmptyState fallback; no file exceeds 150 lines
  </done>
</task>

<task type="auto">
  <name>Task 2: GroupSectionsNav + page.tsx refactor into orchestrator shell (per D-01, D-02, D-04, A)</name>
  <files>
    frontend/src/app/anime/[id]/group/[groupId]/GroupSectionsNav.tsx
    frontend/src/app/anime/[id]/group/[groupId]/page.tsx
    frontend/src/app/anime/[id]/group/[groupId]/page.module.css
  </files>
  <read_first>
    frontend/src/app/anime/[id]/group/[groupId]/page.tsx
    frontend/src/app/anime/[id]/group/[groupId]/page.module.css
    frontend/src/components/ui/Button.tsx
  </read_first>
  <action>
    Create GroupSectionsNav.tsx: 'use client' at top. Import useState, useEffect from react. Import Button from @/components/ui. Import styles from ./GroupSectionsNav.module.css (create this CSS module alongside). Const NAV_SECTIONS = [{id:'story', label:'Geschichte'}, {id:'team', label:'Beteiligte'}, {id:'releases', label:'Releases'}, {id:'themes', label:'OP/ED/Middle'}, {id:'medien', label:'Medien'}] as const. useState activeSection='story'. useEffect: IntersectionObserver with rootMargin '-40% 0px -55% 0px'; observe each section by id; on entry.isIntersecting set activeSection; disconnect on cleanup. Render: nav element with aria-label="Abschnitte", className styles.nav; map NAV_SECTIONS to Button with href={`#${id}`}, variant={activeSection===id?'subtle':'ghost'}, size="sm", aria-current={activeSection===id?'true':undefined}. Create GroupSectionsNav.module.css: .nav { display: flex; gap: 8px; position: sticky; top: var(--header-height, 64px); z-index: 10; background: var(--color-white, #fff); padding: 8px 0; overflow-x: auto; white-space: nowrap; } Keep file ≤80 lines total across .tsx and .module.css.

    Refactor page.tsx: Read existing page.tsx fully. Keep params resolution (animeID/groupID parseInt + notFound), keep all existing fetch calls (getGroupDetail, getAnimeByID, getGroupAssets, getGroupReleases, getAnimeFansubs) with their try/catch graceful degradation pattern. Add stub variables: let contributorsData: GroupContributorsResponse = {team_members:[], external_contributors:[]}; let themesData: GroupThemesResponse = {themes:[]}; let releaseMediaData: GroupReleaseMediaResponse = {items:[]}; (these are wired in Plan 03 — stub here to type-check). Import GroupContributorsResponse, GroupThemesResponse, GroupReleaseMediaResponse from '@/types/groupContributors'. Keep all existing computed values (heroBackdropUrl, infoPanelBackgroundUrl, posterImage, heroStyle, infoPanelStyle, pageStyle, breadcrumbItems, navigationGroups). Remove the inline hero JSX and story JSX blocks; replace with HeroSection and StorySection components passing computed props. Layout: main element wrapping GroupSectionsNav, then section id="story" with StorySection, then placeholder divs for id="team", id="releases", id="themes", id="medien" (these receive real section components in Plan 03). Keep HeroSection above the nav as before. Import all from Plan 02 new files. Ensure page.tsx ≤150 lines total. Ensure group.period is guarded: only pass to HeroSection if group.period != null.

    Extend page.module.css: Add CSS rules for .storySection (padding: 24px 0), .sectionShell (padding: 32px 0; border-top: 1px solid var(--border-subtle)), responsive grid variables. Do not remove existing styles.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s-phase75/frontend && npm run typecheck 2>&amp;1 | tail -20 &amp;&amp; npm run build 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    npm run typecheck exits 0; npm run build exits 0; page.tsx ≤150 lines; GroupSectionsNav.tsx uses Button primitive only (no native button); five anchor IDs present in page layout (#story, #team, #releases, #themes, #medien); correct German umlauts in all labels; HeroSection and StorySection rendered from page.tsx
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Component → Client Component | GroupSectionsNav receives no props from server (pure client scroll logic) |
| projectNotesHtml | Comes from backend body_html; if rendered via CollapsibleStory, must not use dangerouslySetInnerHTML unsanitized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-02-01 | Information Disclosure | StorySection — projectNotesHtml rendering | mitigate | Pass to CollapsibleStory which renders as text content; if backend returns HTML and CollapsibleStory accepts only plain text, pass as plain text only — do NOT use dangerouslySetInnerHTML without sanitization confirmation from CollapsibleStory internals |
| T-75-02-02 | Information Disclosure | page.tsx — group.period nil guard | mitigate | Guard `group.period != null` before passing to HeroSection (RESEARCH Pitfall 5) |
| T-75-02-SC | Tampering | npm installs | accept | No new external packages — no install tasks |
</threat_model>

<verification>
1. npm run typecheck exits 0 — no TypeScript errors in any new file
2. npm run build exits 0 — no build errors
3. page.tsx ≤150 lines
4. GroupSectionsNav uses Button from @/components/ui — no native button elements
5. Five section anchor IDs (#story, #team, #releases, #themes, #medien) in page layout
6. All user-facing strings use correct umlauts
7. No file exceeds 450 lines
</verification>

<success_criteria>
- Frontend builds and typechecks without errors
- Existing hero and story logic preserved — no functional regression
- Section scaffold with correct anchor IDs present
- GroupSectionsNav sticky nav works with IntersectionObserver + Button primitive chips
- 450-line file limit respected across all new files
</success_criteria>

<output>
Create `.planning/phases/75-anime-gruppen-deep-dive-anime-id-group-groupid/75-02-SUMMARY.md` when done
</output>
