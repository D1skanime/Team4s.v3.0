---
phase: 133
plan: 08
type: execute
wave: 3
depends_on: ["133-07"]
files_modified:
  - frontend/src/components/profile/MemberBadgeChain.module.css
  - frontend/src/components/profile/MemberBadgeChain.tsx
  - frontend/src/components/profile/MemberBadgeChain.test.tsx
  - frontend/src/components/profile/BadgeFamilyCard.module.css
  - frontend/src/components/profile/BadgeChip.module.css
autonomous: true
requirements: [PMUI-03, PMUI-04, PMUI-05, PMUI-06, PMUI-07]
user_setup: []

must_haves:
  truths:
    - "FamilyCollectionCard's CSS lives in BadgeFamilyCard.module.css, reuses the shared member-badge-carousel container (it renders as a FocalCarousel item), and its 820px CSS breakpoint stays numerically identical to the 820px value the component's own window.matchMedia calls use."
    - "The 820px magic number used twice in FamilyCollectionCard's window.matchMedia calls is a single named constant, not two inline string literals."
    - "The generic badge-row/badge-step rendering (non-role, non-family carousel items) and ContributionProgress both live in BadgeChip.module.css."
  artifacts:
    - path: "frontend/src/components/profile/BadgeFamilyCard.module.css"
      provides: "styles for FamilyCollectionCard(), reuses member-badge-carousel container"
      contains: "@container member-badge-carousel"
    - path: "frontend/src/components/profile/BadgeChip.module.css"
      provides: "styles for the generic badge-row rendering and ContributionProgress()"
      contains: ".badgeRow"
  key_links:
    - from: "frontend/src/components/profile/MemberBadgeChain.tsx"
      to: "FAMILY_CARD_COMPACT_QUERY constant"
      via: "a single module-level string constant referenced by both window.matchMedia call sites inside FamilyCollectionCard"
      pattern: "FAMILY_CARD_COMPACT_QUERY"
---

<objective>
Extract the two largest remaining chunks of `MemberBadgeChain.module.css`: `BadgeFamilyCard.module.css`
(owns `FamilyCollectionCard()`'s `.familyCard/.familyHero*/.familyStages/.familyStage*/
.specialAwardCard*`, ~260 lines) and `BadgeChip.module.css` (owns the generic non-role badge-row
rendering inline in `MemberBadgeChain()` plus `ContributionProgress()`'s
`.badgeRow*/.badgeStep*/.badgeItem*/.badgeIcon/.badgeArtwork/.contributionProgress*`, ~370 lines).
`FamilyCollectionCard` renders as a `FocalCarousel` item (verified: `renderItem` at
`MemberBadgeChain.tsx` line ~919), so its layout `@media` rules reuse the existing
`member-badge-carousel` container name, matching Plan 133-07's `ContributionAchievementStage`
pattern. The generic badge-row content also renders inside `FocalCarousel` (`renderItem`'s
non-roles branch), so `BadgeChip.module.css` reuses the same container name.

This plan also closes the RESEARCH.md/PATTERNS.md-documented JS anti-pattern in
`FamilyCollectionCard`: two separate `window.matchMedia('(max-width: 820px)')` calls hardcode the
same magic number inline, unrelated to any of this file's CSS breakpoint set
(480/520/900/1099/1440/1600/2100). This drives real scroll-centering BEHAVIOR (which stage
auto-centers on mobile), not just styling, so it cannot become pure CSS — but it must be extracted
to a single named constant (PMUI-07's "unnötige Resize-Listener"/magic-number cleanup intent) and
must match the CSS `@media (max-width: 820px) { .familyStages {...} }` rule this plan converts, so
both the JS behavior threshold and the CSS layout threshold stay reconciled at exactly 820px.

Purpose: PMUI-04 (bounded ownership) for the two largest remaining chunks; PMUI-07 (magic-number
extraction); PMUI-03/05/06 continuing the container-query/dedup discipline.
Output: `BadgeFamilyCard.module.css`, `BadgeChip.module.css`, `FAMILY_CARD_COMPACT_QUERY` constant,
updated imports/tests.

Scope note: `MemberBadgeChain.tsx` itself (928 lines, over CLAUDE.md's 450-line cap) is
accepted pre-existing debt deferred outside this phase's scope (see STATE.md's `[Phase 133]`
decision, formally resolving RESEARCH.md's Open Question 1). This plan only extracts CSS and
rewires the multi-alias imports established by Plan 133-04 — it does not split the `.tsx`
file's function bodies into separate files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-CONTEXT.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-RESEARCH.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-PATTERNS.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-UI-SPEC.md
</context>

<interfaces>
Current 820px magic-number sites (`MemberBadgeChain.tsx`, `FamilyCollectionCard()`, verified by
full-file read): one inside the scroll-centering `useEffect` (`window.matchMedia === 'function' ?
window.matchMedia('(max-width: 820px)') : null`) and one inside the scroll-settle `useEffect`
(`window.matchMedia('(max-width: 820px)')`). Both currently duplicate the literal string.
Existing named-constant precedent to mirror (`MemberBadgeChain.tsx`, module scope, already
present): `const COMPACT_BADGE_SIZES = '(max-width: 520px) 72px, 96px'` and
`const ACTIVE_BADGE_SIZES = '...'`. Add `const FAMILY_CARD_COMPACT_QUERY = '(max-width: 820px)'`
in the same module-scope constant block, and reference
`window.matchMedia(FAMILY_CARD_COMPACT_QUERY)` at both call sites instead of the inline literal.

Existing CSS rule this constant must stay numerically aligned with (verified current):
`@media (max-width: 820px) { .familyStages { display: flex; justify-content: flex-start; ... } }`
— converts to `@container member-badge-carousel (max-width: 820px) { .familyStages {...} }` in
`BadgeFamilyCard.module.css`.

`FamilyCollectionCard()` and the inline generic-badge-row render block both reference
`LockedStageArtwork` (from Plan 133-04's `lockedStageArtworkStyles`) and the shared layered-artwork
classes (from Plan 133-04's `layeredBadgeArtworkStyles`) — these two imports/usages are ALREADY
correct after Plan 133-04 and Plan 133-07; this plan does not touch them, only the
`BadgeFamilyCard`/`BadgeChip`-owned selectors.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract BadgeFamilyCard.module.css and the FAMILY_CARD_COMPACT_QUERY constant</name>
  <files>frontend/src/components/profile/MemberBadgeChain.module.css, frontend/src/components/profile/MemberBadgeChain.tsx, frontend/src/components/profile/BadgeFamilyCard.module.css</files>
  <read_first>
    frontend/src/components/profile/MemberBadgeChain.module.css
    frontend/src/components/profile/MemberBadgeChain.tsx
  </read_first>
  <action>
    Grep for every `.familyCard`/`.familyHero*`/`.familyEyebrow`/`.familyStages`/`.familyStage*`/
    `.specialAwardCard*`/`.specialAwardArtwork` selector in `MemberBadgeChain.module.css` and cut
    into `frontend/src/components/profile/BadgeFamilyCard.module.css` (~260 lines). Convert its
    device-width `@media` blocks (520px, 1099px, 820px — see `<interfaces>`) to
    `@container member-badge-carousel (...)` at the same pixel values, reusing the existing
    container name (do NOT declare a new container on `.familyCard`, since it already sits inside
    `.carouselShell`'s named container as a carousel item). Keep any `prefers-reduced-motion`
    block(s) for this selector group as `@media`.
    In `MemberBadgeChain.tsx`: add `import badgeFamilyCardStyles from './BadgeFamilyCard.module.css'`
    and update every `chainStyles.family*`/`chainStyles.specialAward*` reference inside
    `FamilyCollectionCard()` to `badgeFamilyCardStyles.*`.
    Add `const FAMILY_CARD_COMPACT_QUERY = '(max-width: 820px)'` to the module-scope constants
    block near `COMPACT_BADGE_SIZES`/`ACTIVE_BADGE_SIZES`. Replace both inline
    `'(max-width: 820px)'` literals inside `FamilyCollectionCard()`'s two `window.matchMedia(...)`
    calls with `window.matchMedia(FAMILY_CARD_COMPACT_QUERY)`.
    Delete the moved blocks from `MemberBadgeChain.module.css`.
  </action>
  <acceptance_criteria>
    - `BadgeFamilyCard.module.css` exists, is under 450 lines, contains
      `@container member-badge-carousel (max-width: 820px)` (matching the JS constant's value),
      and no unnamed/newly-named `container` declaration.
    - `MemberBadgeChain.tsx` contains exactly one occurrence of the string literal
      `'(max-width: 820px)'` (the constant declaration itself); both `window.matchMedia(...)` call
      sites reference `FAMILY_CARD_COMPACT_QUERY`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -c "'(max-width: 820px)'" src/components/profile/MemberBadgeChain.tsx</automated>
  </verify>
  <done>`BadgeFamilyCard.module.css` is extracted and container-query-driven; the 820px value exists
  exactly once as a named JS constant, matching the CSS breakpoint it drives alongside.</done>
</task>

<task type="auto">
  <name>Task 2: Extract BadgeChip.module.css and update MemberBadgeChain.test.tsx</name>
  <files>frontend/src/components/profile/BadgeChip.module.css, frontend/src/components/profile/MemberBadgeChain.module.css, frontend/src/components/profile/MemberBadgeChain.tsx, frontend/src/components/profile/MemberBadgeChain.test.tsx</files>
  <read_first>
    frontend/src/components/profile/MemberBadgeChain.module.css
    frontend/src/components/profile/MemberBadgeChain.tsx
    frontend/src/components/profile/MemberBadgeChain.test.tsx
  </read_first>
  <action>
    Grep for every `.badgeRow*`/`.badgeStep*`/`.badgeItem*`/`.badgeIcon`/`.badgeArtwork`/
    `.badgeText`/`.badgeDetail`/`.contributionProgress*` selector and cut into
    `frontend/src/components/profile/BadgeChip.module.css` (~370 lines) — this includes the
    `@media (max-width: 520px) { .group[data-badge-group="contributions"] .badgeArtwork {...} }`
    rule (verify via grep, exact selector text may include the `[data-badge-group]` qualifier;
    convert to `@container member-badge-carousel (max-width: 520px)`, reusing the existing name,
    since this content also renders inside the carousel). Keep any reduced-motion block as `@media`.
    In `MemberBadgeChain.tsx`: add `import badgeChipStyles from './BadgeChip.module.css'` and
    update every `chainStyles.badge*`/`chainStyles.contributionProgress*` reference inside
    `ContributionProgress()` and the inline generic badge-row render block (non-roles branch of the
    main `MemberBadgeChain()` function) to `badgeChipStyles.*`.
    Delete the moved blocks from `MemberBadgeChain.module.css`.
    In `MemberBadgeChain.test.tsx`: add `badgeFamilyCardCss` and `badgeChipCss` `readFileSync`
    constants, and update every existing assertion referencing selectors now owned by
    `BadgeFamilyCard.module.css`/`BadgeChip.module.css` (including the `@media`-to-`@container`
    conversions from Task 1 and this task) to read from the correct new constant with matching
    `@container member-badge-carousel` expectations instead of `@media`.
  </action>
  <acceptance_criteria>
    - `BadgeChip.module.css` exists and is under 450 lines.
    - `cd frontend && npx vitest run src/components/profile/MemberBadgeChain.test.tsx` passes with
      zero failures.
    - `MemberBadgeChain.module.css` no longer contains `.badgeRow`, `.familyCard`, or any selector
      moved by this plan or Plan 133-07.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/profile/MemberBadgeChain.test.tsx</automated>
  </verify>
  <done>`BadgeChip.module.css` is extracted; the full badge-chain test suite passes against the
  post-split module layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure CSS-module extraction, container-query conversion, and a JS magic-number-to-constant refactor; no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-03 | (none applicable) | `MemberBadgeChain.module.css` / `.tsx` split | accept | Presentation-only CSS/import refactor with no new trust boundary; out of scope for STRIDE. |
</threat_model>

<verification>
`cd frontend && npx vitest run src/components/profile/MemberBadgeChain.test.tsx` green; `wc -l` on
both new modules confirms <=450 lines; the 820px value appears exactly once as a JS literal;
`npm run typecheck` and `npm run lint` clean.
</verification>

<success_criteria>
- `BadgeFamilyCard.module.css` and `BadgeChip.module.css` exist, each <=450 lines, container-query-driven.
- The 820px magic number is a single named constant reconciled with its matching CSS breakpoint.
- Full existing badge-chain test coverage still passes.
</success_criteria>

<output>
Create `.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-08-SUMMARY.md` when done
</output>
