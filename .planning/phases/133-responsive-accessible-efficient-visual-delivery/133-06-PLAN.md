---
phase: 133
plan: 06
type: execute
wave: 2
depends_on: ["133-01", "133-03"]
files_modified:
  - frontend/src/components/profile/MemberProfileMemorialHero.tsx
  - frontend/src/components/profile/MemberProfileHero.test.tsx
autonomous: true
requirements: [PMA11Y-01, PMA11Y-04]
user_setup: []

must_haves:
  truths:
    - "A memorial profile page has exactly one <h1> containing the member's display name (currently it renders the name twice: once as <h1> via PageHeader, once as <h2>)."
    - "The memorial hero's rendered heading text, style, and adjacent MemberStatusPill are visually identical to before, just consolidated into a single semantic heading."
    - "The memorial hero renders zero axe-core violations."
  artifacts:
    - path: "frontend/src/components/profile/MemberProfileMemorialHero.tsx"
      provides: "single <h1 className={styles.heroTitle}> heading for the member name, no PageHeader title usage"
      contains: "h1 className={styles.heroTitle}"
  key_links:
    - from: "frontend/src/components/profile/MemberProfileMemorialHero.tsx"
      to: "frontend/src/components/profile/profile.module.css"
      via: ".heroTitle / .heroEyebrow / .heroTitleRow classes already defined for the non-memorial hero"
      pattern: "styles\\.heroTitle"
---

<objective>
Fix the duplicate-heading bug in `MemberProfileMemorialHero.tsx` (RESEARCH.md Pitfall 6, UI-SPEC.md's
locked "Heading hierarchy decision", confirmed by direct read of the current 100-line file): it
renders the member's display name TWICE — once as `<h1>` via `<PageHeader eyebrow="Fansub-Member"
title={displayName} />` (PageHeader renders `title` as `<h1 className={styles.pageTitle}>`
internally) and again a few lines later as `<h2 className={styles.heroTitleRow}>{displayName}...
</h2>`. The non-memorial hero (`MemberProfileHero.tsx`, public branch) already renders the correct
single-heading pattern. This plan makes the memorial hero match it exactly, per UI-SPEC.md's locked
decision: drop `PageHeader`'s `title` usage entirely, render the styled heading as the real
`<h1 className={styles.heroTitle}>` inside `.heroCopy`, and move "Fansub-Member" to a plain
`<p className={styles.heroEyebrow}>` (both classes already exist in `profile.module.css` for the
non-memorial branch — no CSS change needed, this is a pure JSX-structure fix).

Purpose: PMA11Y-01 (single, non-duplicated heading hierarchy across all three profile render
states — public, owner, memorial).
Output: single-`<h1>` memorial hero, extended `MemberProfileHero.test.tsx` coverage (heading count +
axe), matching the existing memorial `describe` block's conventions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-CONTEXT.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-RESEARCH.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-UI-SPEC.md
</context>

<interfaces>
Current buggy structure (`MemberProfileMemorialHero.tsx`, verified current full-file read):
```typescript
import { PageHeader } from '@/components/ui'
...
<div className={styles.hero}>
  <PageHeader eyebrow="Fansub-Member" title={displayName} />
  <div className={styles.heroPanel} ...>
    <div className={styles.heroAvatar}>...</div>
    <div className={styles.heroCopy}>
      <h2 className={styles.heroTitleRow}>
        <span>{displayName}</span>
        <MemberStatusPill status="memorial" />
      </h2>
      <p className={styles.memorialNotice}>Dieses Profil wird als historisches Gedenkprofil geführt.</p>
      {profile.bio ? <p className={styles.memorialBio}>{profile.bio}</p> : null}
      {knownFor... block ...}
    </div>
  </div>
</div>
```
Target structure — mirror `MemberProfileHero.tsx`'s public-view branch exactly (verified current):
```typescript
<div className={styles.heroCopy}>
  <p className={styles.heroEyebrow}>Fansub-Member</p>
  <div className={styles.heroTitleRow}>
    <h1 className={styles.heroTitle}>{displayName}</h1>
    <MemberStatusPill status="memorial" />
  </div>
  ...
</div>
```
Note `profile.module.css` needs NO change: `.heroTitle`, `.heroEyebrow`, `.heroTitleRow` are
already generic (verified — `.heroTitleRow` is a flex-row layout wrapper, `.heroTitle` styles
whatever heading tag it's applied to; there is no h2-only rule to remove). Do not modify
`profile.module.css` in this plan.
`PageHeader` import (`import { PageHeader } from '@/components/ui'`) becomes unused after this
change — remove the import along with the `<PageHeader .../>` JSX.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render the memorial hero's member name as a single &lt;h1&gt;</name>
  <files>frontend/src/components/profile/MemberProfileMemorialHero.tsx, frontend/src/components/profile/MemberProfileHero.test.tsx</files>
  <behavior>
    - Test: rendering `MemberProfileMemorialHero` (or `MemberProfileHero` with
      `profile_status: 'memorial'`, isPublicView) produces exactly one element matching
      `screen.getAllByRole('heading', { level: 1 })` with length 1.
    - Test: that single `<h1>` contains the display name text.
    - Test: `MemberStatusPill` with `status="memorial"` still renders adjacent to the heading.
    - Test (regression): the existing memorial-specific assertions in
      `describe('MemberProfileHero — Memorial-Variante (Wave-0 RED, D-10)', ...)` (the
      "Gedenk-Sprache" text and "no activity-metric" checks) still pass unmodified.
  </behavior>
  <read_first>
    frontend/src/components/profile/MemberProfileMemorialHero.tsx
    frontend/src/components/profile/MemberProfileHero.tsx
  </read_first>
  <action>
    In `MemberProfileMemorialHero.tsx`: remove the `import { PageHeader } from '@/components/ui'`
    import and the `<PageHeader eyebrow="Fansub-Member" title={displayName} />` JSX line.
    Inside `.heroCopy`, add `<p className={styles.heroEyebrow}>Fansub-Member</p>` as the first
    child, then replace `<h2 className={styles.heroTitleRow}><span>{displayName}</span>
    <MemberStatusPill status="memorial" /></h2>` with
    `<div className={styles.heroTitleRow}><h1 className={styles.heroTitle}>{displayName}</h1>
    <MemberStatusPill status="memorial" /></div>` — `styles.heroTitleRow` moves from the heading
    tag to a wrapping `<div>` (mirroring `MemberProfileHero.tsx`'s public-view branch exactly),
    and `styles.heroTitle` applies to the new `<h1>` (dropping the bare `<span>` wrapper, since the
    heading itself now carries the styling class directly, matching the non-memorial pattern).
    Leave every other line (avatar, memorial notice string, bio, known-for block) untouched — this
    is a heading-structure fix only, not a content or layout change.
  </action>
  <acceptance_criteria>
    - `MemberProfileMemorialHero.tsx` contains no `PageHeader` import or usage.
    - Exactly one `<h1 className={styles.heroTitle}>{displayName}</h1>` exists in the rendered
      output for a memorial profile.
    - `screen.getAllByRole('heading', { level: 1 })` returns exactly 1 element when rendering a
      memorial profile.
    - The existing "Gedenk-Sprache" and "no activity metric" tests in
      `describe('MemberProfileHero — Memorial-Variante (Wave-0 RED, D-10)', ...)` still pass.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/profile/MemberProfileHero.test.tsx</automated>
  </verify>
  <done>Every memorial-profile render has exactly one `<h1>` containing the member's display name;
  no duplicate accessible name announcement remains.</done>
</task>

<task type="auto">
  <name>Task 2: Add axe coverage for the memorial hero</name>
  <files>frontend/src/components/profile/MemberProfileHero.test.tsx</files>
  <read_first>
    frontend/src/components/profile/MemberProfileHero.test.tsx
    frontend/src/test/axeSetup.ts
  </read_first>
  <action>
    Import `axe` from `jest-axe` in `MemberProfileHero.test.tsx` (the global matcher is already
    registered via Plan 133-01's `setupFiles` — do not re-register). Inside
    `describe('MemberProfileHero — Memorial-Variante (Wave-0 RED, D-10)', ...)`, add one new test
    rendering the memorial variant and asserting
    `expect(await axe(container)).toHaveNoViolations()`. Also add one equivalent axe assertion to
    the main `describe('MemberProfileHero', ...)` block for the standard public-view render (reuse
    the `makePublicProfile()` fixture already defined at the top of this file) — this gives axe
    coverage for BOTH hero variants in one plan, since they share the same component contract.
  </action>
  <acceptance_criteria>
    - Two new axe assertions exist (memorial variant, public variant) and both pass with zero
      violations.
    - No pre-existing test in the file was weakened to make axe pass.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/profile/MemberProfileHero.test.tsx</automated>
  </verify>
  <done>Both the memorial and public hero variants have zero axe-core violations, proven by an
  automated test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Heading-structure fix on an already-shipped, server-rendered component; no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-03 | (none applicable) | `MemberProfileMemorialHero.tsx` | accept | Presentation/semantics-only change; out of scope for STRIDE. |
</threat_model>

<verification>
`cd frontend && npx vitest run src/components/profile/MemberProfileHero.test.tsx` green including
the new heading-count and axe assertions; `npm run typecheck` and `npm run lint` clean.
</verification>

<success_criteria>
- Memorial profiles render exactly one `<h1>` with the member's display name.
- Zero axe-core violations on both hero variants.
</success_criteria>

<output>
Create `.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-06-SUMMARY.md` when done
</output>
