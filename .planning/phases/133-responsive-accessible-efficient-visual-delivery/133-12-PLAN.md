---
phase: 133
plan: 12
type: execute
wave: 7
depends_on: ["133-11"]
files_modified: []
autonomous: false
requirements: [PMUI-01, PMUI-02, PMUI-06, PMA11Y-01, PMA11Y-02, PMA11Y-03, PMA11Y-04]
user_setup: []

must_haves:
  truths:
    - "A human has visually confirmed, at narrow/intermediate/wide/400%-zoom, that no profile section overflows or clips content (D-06's regression guard, which this phase adds ON TOP OF the automated tests, ahead of the authoritative bundled Phase-134 UAT)."
    - "A human has confirmed, keyboard-only, that carousel/paging/preview/disclosure controls are fully operable with visible focus and no stray tab stops (D-12)."
  artifacts: []
  key_links: []
---

<objective>
Perform the two manual evidence passes CONTEXT.md's D-06 and D-12 require IN ADDITION to the
automated tests already landed in Plans 133-01 through 133-11: (1) per-section visual spot-checks
at narrow/intermediate/wide/400%-zoom across the hero, badge-chain groups, and membership cards
(D-06 — "CSS regressions are hard to catch automatically"), and (2) a keyboard-only + 400%-zoom
manual pass confirming the carousel/paging/preview/disclosure controls behave as the automated
tests describe under real interaction (D-12). Both checkpoints are explicitly scoped as ADDITIONAL
evidence for this phase, distinct from and ahead of the AUTHORITATIVE bundled live UAT that
Phase 134 (PMQA-05) performs — do not treat this plan's sign-off as replacing Phase 134's gate.

Purpose: D-06/D-12's explicit deviation from the bundled-UAT default, closing the phase with
recorded human evidence for the categories automated tests cannot fully cover (visual regression,
full assistive-technology interaction).
Output: recorded pass/fail evidence for both manual checks; any issues found routed back through
`/gsd:plan-phase 133 --gaps` before the phase is considered verifiable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-CONTEXT.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: D-06 per-section visual spot-check (narrow / intermediate / wide / 400% zoom)</name>
  <files>none (manual verification only, no file changes)</files>
  <action>
    Present the four-viewport-state visual spot-check steps below to the user across both seed
    profiles and wait for explicit approval or a list of regressions before proceeding to Task 2.
  </action>
  <what-built>Plans 133-03 through 133-09 converted the hero and every badge-chain component
  (locked-stage artwork, layered artwork, role cards, badge chips, family cards, anime/points/
  contribution/membership stages) from device-width `@media` to container-query-driven layout, and
  de-duplicated four previously-conflicting role-card selectors. This checkpoint verifies no visual
  regression was introduced by that conversion.</what-built>
  <how-to-verify>
    Using the Windows SSH tunnel per CLAUDE.md (`http://127.0.0.1:3300`), open both seed profiles:
    `http://127.0.0.1:3300/members/sheppert` and `http://127.0.0.1:3300/members/csubs-leader`.
    For EACH of the following four viewport states, check the hero panel, EVERY badge-chain group
    (roles, progress/anime-projects, points, contributions, membership, and any "special" awards
    group present), and the membership-history section:
    1. Narrow (~360px wide, e.g. browser DevTools device toolbar at "iPhone SE" or similar).
    2. Intermediate (~900px wide).
    3. Wide (~1600px+ wide).
    4. 400% browser zoom at a normal desktop width (~1280px before zoom).
    At each state, confirm: no horizontal scrollbar on the page/body, no clipped or cut-off text or
    artwork, no overlapping elements, German text (including umlauts) wraps naturally instead of
    being truncated, and the role-card/badge-chip/family-card content that was de-duplicated in
    Plan 133-09 (`.roleLabel`, `.roleBadgeRow`, `.roleHeroArtwork`) renders sensibly (12px
    non-uppercase role label, flexible-height card, aspect-ratio-preserving hero artwork — not a
    squished or oversized image).
  </how-to-verify>
  <verify>
    <human-check>User confirms all four viewport states are clean on both seed profiles.</human-check>
  </verify>
  <done>User has typed "approved" for all four viewport states on both profiles, or listed a
  specific regression to route back through /gsd:plan-phase 133 --gaps.</done>
  <resume-signal>Type "approved" if all four viewport states are clean on both profiles, or list
  the specific section/viewport/profile combination that shows a regression.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-12 keyboard-only, 400% zoom, and screen-reader spot-check</name>
  <files>none (manual verification only, no file changes)</files>
  <action>
    Present the keyboard-only/zoom/screen-reader spot-check steps below to the user across both
    seed profiles and wait for explicit approval or a description of a failing control.
  </action>
  <what-built>Plan 133-05 added `inert` on inactive carousel slides and focus-on-expand handling to
  the shared `FocalCarousel` primitive; Plan 133-06 fixed the memorial-hero duplicate heading; every
  interactive control inherits the project's existing `:focus-visible` treatment
  (`--focus-outline`/`--focus-ring`, orange/coral) unchanged.</what-built>
  <how-to-verify>
    On both seed profiles (`http://127.0.0.1:3300/members/sheppert`,
    `http://127.0.0.1:3300/members/csubs-leader`), using keyboard only (Tab/Shift+Tab/Enter/Space/
    Arrow keys, no mouse):
    1. Tab through every badge-chain carousel (roles, progress, points, contributions, membership).
       Confirm: only the ACTIVE slide's content is reachable via Tab (no stray tab stops on
       off-screen slides), Arrow Left/Right/Home/End move the active item, visible focus outline
       (orange/coral, not blue) appears on every focused control.
    2. Activate "Alle Auszeichnungen anzeigen" (expand to grid) on at least one group via keyboard.
       Confirm focus lands on/near the "Weniger anzeigen" button after the view swaps (not lost to
       `<body>`), and activating "Weniger anzeigen" returns focus to the original toggle.
    3. Tab through the profile's project/contribution pagination controls ("Mehr laden" or
       equivalent) and confirm a status announcement occurs after loading more (aria-live).
    4. Confirm a logical, top-to-bottom heading order with `Ctrl+F6` / a screen-reader landmark
       list if available, or by visually scanning rendered heading levels — exactly one `<h1>` per
       page, including on a memorial-status profile if one is available to test.
    5. Zoom the browser to 400% and repeat a quick keyboard pass over the same controls, confirming
       nothing becomes unreachable or visually broken.
    If a screen reader (NVDA/VoiceOver/Orca) is available, do a quick spot-check that carousel
    controls announce their role/state ("Karussell", position counter, expanded/collapsed) — this
    is optional/best-effort, not a hard blocker if no screen reader is available in this
    environment.
  </how-to-verify>
  <verify>
    <human-check>User confirms keyboard operability, focus management, and heading order all check out.</human-check>
  </verify>
  <done>User has typed "approved", or described a specific failing control routed back through
  /gsd:plan-phase 133 --gaps.</done>
  <resume-signal>Type "approved" if keyboard operability, focus management, and heading order all
  check out, or describe the specific control/profile that failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Manual verification only; no code changes in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| n/a | n/a | n/a | accept | This plan performs no code change and introduces no new component or trust boundary. |
</threat_model>

<verification>
Both checkpoints approved by the user; any issues found are routed to
`/gsd:plan-phase 133 --gaps` to produce a follow-up plan rather than silently accepted.
</verification>

<success_criteria>
- D-06 visual spot-check approved across all four viewport states on both seed profiles.
- D-12 keyboard/zoom/screen-reader spot-check approved.
</success_criteria>

<output>
Create `.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-12-SUMMARY.md` when done
</output>
