---
phase: 133
plan: 11
type: execute
wave: 6
depends_on: ["133-02", "133-03", "133-05", "133-06", "133-09", "133-10"]
files_modified:
  - frontend/scripts/collect-member-profile-evidence.mjs
autonomous: true
requirements: [PMUI-01, PMUI-06, PMPF-06, PMPF-08, PMA11Y-04]
user_setup: []

must_haves:
  truths:
    - "collect-member-profile-evidence.mjs's budget-check mode now HARD-FAILS if either seed profile has document-level horizontal overflow, not just records it."
    - "The full frontend automated gate (typecheck, lint, unit tests, image/overflow budget-check) is green after every plan in this phase has landed."
    - "Every requirement this phase touches (PMUI-01..07, PMA11Y-01..04, PMPF-06, PMPF-08) has at least one automated check that currently passes."
  artifacts:
    - path: "frontend/scripts/collect-member-profile-evidence.mjs"
      provides: "hard pageOverflow/bodyOverflow assertion inside evaluateBudget(), turning an already-captured-but-unasserted field into a gate"
      contains: "pageOverflow"
  key_links:
    - from: "frontend/scripts/collect-member-profile-evidence.mjs"
      to: "capturePageMetrics()'s existing overflow snapshot"
      via: "evaluateBudget() asserting the captured overflow booleans are both false"
      pattern: "pageOverflow|bodyOverflow"
---

<objective>
Close out the phase's automated gate: turn the ALREADY-CAPTURED (per RESEARCH.md's Nyquist map)
`pageOverflow`/`bodyOverflow` DOM snapshot in `collect-member-profile-evidence.mjs` into a HARD
assertion inside `evaluateBudget()`/`runBudgetCheck` (PMUI-01/06 — "no document-level horizontal
overflow"), then run the complete cross-plan regression gate (`npm run typecheck`, `npm run lint`,
`npm test`, `node scripts/collect-member-profile-evidence.mjs --mode budget-check`) now that every
CSS-split, a11y, image-config, and image-budget plan in this phase (133-02 through 133-10) has
landed. Fix any drift the full-suite run surfaces — this plan is the phase's automated integration
checkpoint before the manual evidence pass (Plan 133-12).

Purpose: makes PMUI-01/06's "no horizontal overflow" a real automated gate instead of a
manually-inspected artifact, and proves the whole phase's changes compose cleanly together.
Output: hard overflow assertion, green full-suite run, any drift fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-CONTEXT.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-RESEARCH.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-VALIDATION.md
</context>

<interfaces>
`capturePageMetrics()` already returns a DOM snapshot including `pageOverflow`/`bodyOverflow`
booleans (RESEARCH.md's Phase Requirements -> Test Map row for PMUI-01/06: "existing `pageOverflow`/
`bodyOverflow` capture in `collect-member-profile-evidence.mjs`'s `snapshotDOM()`... turning it
into a hard assertion (vs. just recorded) is a Wave 0 addition if desired" — this plan is where
that addition happens, at the end of the phase once the final DOM/CSS shape is settled). Grep this
file for `pageOverflow`/`bodyOverflow`/`snapshotDOM` to find the exact current field names and
where they land in the returned metrics object before wiring the new assertion.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Turn the existing overflow snapshot into a hard budget-check assertion</name>
  <files>frontend/scripts/collect-member-profile-evidence.mjs</files>
  <read_first>
    frontend/scripts/collect-member-profile-evidence.mjs
  </read_first>
  <action>
    Locate the existing DOM overflow snapshot (grep `pageOverflow`/`bodyOverflow`/`snapshotDOM`)
    and confirm it is already returned from `capturePageMetrics()`'s `page` object per profile.
    Inside `evaluateBudget(slug, api, page)`, add a check asserting both overflow booleans are
    `false` for a rendered (`page.rendered === true`) page, following the exact same
    `pageCheck.xOk = ...; if (!pageCheck.xOk) breaches.push(...)` idiom already used for the
    Web-Vitals checks in this function. Attach the result as `pageCheck.overflowOk` (or an
    equivalently-named field consistent with the existing `pageCheck` object's other keys).
  </action>
  <acceptance_criteria>
    - `evaluateBudget()` includes an overflow check that pushes a breach string if either
      `pageOverflow` or `bodyOverflow` is `true` on a rendered page.
    - Running `node scripts/collect-member-profile-evidence.mjs --mode budget-check --output-dir /tmp/phase133-final-gate`
      against the current (post-phase) frontend state exits 0 for both seed profiles (i.e. neither
      profile currently overflows — if one does, this task must ALSO fix the overflow, not just add
      the assertion, since the plan's must_haves require the gate to pass, not just exist).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && node scripts/collect-member-profile-evidence.mjs --mode budget-check --output-dir /tmp/phase133-final-gate</automated>
  </verify>
  <done>The overflow snapshot is a hard gate, and both seed profiles pass it cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Run and green the full cross-plan regression gate</name>
  <files>any file surfaced by a full-suite regression (already owned by an earlier plan in this phase)</files>
  <read_first>
    frontend/vitest.config.ts
  </read_first>
  <action>
    Run, in order: `cd frontend && npm run typecheck`; `npm run lint`; `npm test` (full Vitest
    suite — this exercises every test file touched by Plans 133-01 through 133-10 in one pass,
    including the axe assertions, the CSS-module split assertions, and the config regression
    tests); `node scripts/collect-member-profile-evidence.mjs --mode budget-check --output-dir /tmp/phase133-final-gate`
    (re-run after Task 1, now asserting overflow + the image budget from Plan 133-10 together).
    If any command fails, fix the failure in the file it belongs to (one of the files already
    touched by an earlier plan in this phase — this task's job is to close any gap left by
    cross-plan integration, such as a CSS selector rename one plan's test didn't anticipate
    interacting with another plan's change) and re-run the full sequence until all four commands
    are green.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` exits 0.
    - `npm run lint` exits 0.
    - `npm test` exits 0 (every test file across the whole frontend suite, not just profile files).
    - `node scripts/collect-member-profile-evidence.mjs --mode budget-check` exits 0 for both
      profiles.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run typecheck && npm run lint && npm test</automated>
  </verify>
  <done>The entire frontend automated gate — typecheck, lint, full test suite, image/overflow
  budget-check — is green with every Phase 133 plan's changes composed together.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev-stack -> Playwright measurement script | Same as Plan 133-10: loopback-only, local dev stack, no external exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-04 | (none applicable) | `collect-member-profile-evidence.mjs` overflow gate | accept | Build/tooling script run against a local dev stack; no new trust boundary. |
</threat_model>

<verification>
`cd frontend && npm run typecheck && npm run lint && npm test` all green; `node
scripts/collect-member-profile-evidence.mjs --mode budget-check` exits 0 for both seed profiles.
</verification>

<success_criteria>
- Document-level horizontal overflow is a hard automated gate, not just a recorded artifact.
- The entire Phase 133 change set passes the full frontend regression gate in one run.
</success_criteria>

<output>
Create `.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-11-SUMMARY.md` when done
</output>
