---
phase: 133
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/profile/MemberBadgeChain.module.css
  - frontend/src/components/profile/MemberBadgeChain.tsx
  - frontend/src/components/profile/MemberBadgeChain.test.tsx
  - frontend/src/components/profile/LockedStageArtwork.module.css
  - frontend/src/components/profile/LayeredBadgeArtwork.module.css
autonomous: true
requirements: [PMUI-04, PMUI-05]
user_setup: []

must_haves:
  truths:
    - "MemberBadgeChain.module.css shrinks (this plan removes the LockedStageArtwork + LayeredBadgeArtwork selector blocks it currently owns, ~180 of its 2282 lines)."
    - "LockedStageArtwork.module.css and LayeredBadgeArtwork.module.css each exist, are each <=450 lines, and are each imported only by the functions/JSX that use their classes."
    - "The badge chain renders pixel-identical output before and after the split (same className strings resolve to the same underlying CSS declarations, just from a different module file)."
  artifacts:
    - path: "frontend/src/components/profile/LockedStageArtwork.module.css"
      provides: "styles for the LockedStageArtwork() component (locked-badge placeholder art)"
      contains: ".lockedStageArtwork"
    - path: "frontend/src/components/profile/LayeredBadgeArtwork.module.css"
      provides: "shared layered-artwork rules (mist/backdrop/motif/frame) reused across role, family, anime-project, and badge-chip rendering"
      contains: ".roleArtworkMotif"
  key_links:
    - from: "frontend/src/components/profile/MemberBadgeChain.tsx"
      to: "frontend/src/components/profile/LockedStageArtwork.module.css"
      via: "aliased CSS module import used inside the LockedStageArtwork() function"
      pattern: "import\\s+\\w+\\s+from\\s+'./LockedStageArtwork.module.css'"
    - from: "frontend/src/components/profile/MemberBadgeChain.tsx"
      to: "frontend/src/components/profile/LayeredBadgeArtwork.module.css"
      via: "aliased CSS module import used across FamilyCollectionCard, AnimeProjectAchievementStage, and the inline roles/badge-chip render blocks"
      pattern: "import\\s+\\w+\\s+from\\s+'./LayeredBadgeArtwork.module.css'"
---

<objective>
Begin splitting `MemberBadgeChain.module.css` (2282 lines, CLAUDE.md's 450-line limit violated by
5x) along the component-function boundaries already present in `MemberBadgeChain.tsx` (928 lines,
928 lines total, verified by direct read). This first split plan extracts the two
smallest, purely-static (no `@media`/`@container` present, verified by grep) chunks: the
locked-badge placeholder artwork (`LockedStageArtwork()` function, lines 61-70) and the shared
layered-artwork rendering (`.roleArtworkMist/.roleArtworkBackdrop/.roleArtworkMotif/.roleArtworkFrame/
.roleHeroArtworkLayered/.badgeArtworkLayered`, used across five separate render sites: the inline
roles-card render, the inline badge-chip render, `FamilyCollectionCard`, and
`AnimeProjectAchievementStage`). Splitting these two first (no responsive-conversion complexity)
establishes the multi-CSS-module import pattern the remaining split plans (133-07/08/09) reuse.

Purpose: PMUI-04 (bounded, component-owned CSS modules) and the first slice of PMUI-05 (this pair
carries no known duplicate-selector conflicts, unlike the role-card selectors handled in 133-09).
Output: `LockedStageArtwork.module.css`, `LayeredBadgeArtwork.module.css`, shrunk
`MemberBadgeChain.module.css`, updated `MemberBadgeChain.tsx` imports, updated
`MemberBadgeChain.test.tsx` CSS-source assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-CONTEXT.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-RESEARCH.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-PATTERNS.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-UI-SPEC.md
</context>

<interfaces>
`MemberBadgeChain.tsx` (current, verified by full read) declares a single
`import styles from './MemberBadgeChain.module.css'` (line 25) and uses `styles.xxx` throughout.
This plan changes that to a MULTI-import pattern that every later split plan (133-07/08/09) must
follow identically:
```typescript
import chainStyles from './MemberBadgeChain.module.css'
import lockedStageArtworkStyles from './LockedStageArtwork.module.css'
import layeredBadgeArtworkStyles from './LayeredBadgeArtwork.module.css'
```
Rename the existing bare `import styles from ...` to `import chainStyles from ...` and update
EVERY existing `styles.xxx` reference in the file to `chainStyles.xxx` EXCEPT the ones this plan
moves to the two new aliases below — later split plans (133-07/08/09) will introduce their own
aliases and touch their own subset of the (by-then-already-renamed) `chainStyles.xxx` references.

Selectors owned by `LockedStageArtwork.module.css` (move from `chainStyles.` to
`lockedStageArtworkStyles.` wherever referenced): `.lockedStageArtwork`,
`.lockedStageArtworkHero`, `.lockedStageHeroMedal`, `.lockedStageHeroQuestion`,
`.lockedStageHeroLock`, `.lockedStageHeroCopy`. Used only inside the `LockedStageArtwork()`
function (lines 61-70 of the current `MemberBadgeChain.tsx`).

Selectors owned by `LayeredBadgeArtwork.module.css` (move from `chainStyles.` to
`layeredBadgeArtworkStyles.` wherever referenced): `.roleArtworkMist`, `.roleArtworkBackdrop`,
`.roleArtworkMotif`, `.roleArtworkFrame`, `.roleHeroArtworkLayered`, `.badgeArtworkLayered`. Used
at FIVE separate JSX sites in the current file — grep `roleArtworkMist\|roleArtworkBackdrop\|
roleArtworkMotif\|roleArtworkFrame\|roleHeroArtworkLayered\|badgeArtworkLayered` in
`MemberBadgeChain.tsx` before editing to find all of them (as of this plan's writing: inside
`FamilyCollectionCard`'s family-hero and per-stage rendering, inside
`AnimeProjectAchievementStage`'s hero artwork, inside the inline roles-card render's hero-artwork
span, and inside the inline badge-chip render's earned-badge artwork span). Update all of them.

Existing test pattern to preserve (`MemberBadgeChain.test.tsx` line 10):
`const memberBadgeChainCss = readFileSync('src/components/profile/MemberBadgeChain.module.css', 'utf8')`
— this single variable is referenced by every CSS-source assertion in the 2063-line test file. Any
assertion that greps `memberBadgeChainCss` for a selector this plan moves will silently stop
matching once the selector is gone from that file.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract LockedStageArtwork.module.css and LayeredBadgeArtwork.module.css</name>
  <files>frontend/src/components/profile/MemberBadgeChain.module.css, frontend/src/components/profile/MemberBadgeChain.tsx, frontend/src/components/profile/LockedStageArtwork.module.css, frontend/src/components/profile/LayeredBadgeArtwork.module.css</files>
  <read_first>
    frontend/src/components/profile/MemberBadgeChain.module.css
    frontend/src/components/profile/MemberBadgeChain.tsx
  </read_first>
  <action>
    Grep `MemberBadgeChain.module.css` for each selector listed in this plan's `<interfaces>`
    section (both groups). Cut every matching rule block (there is no `@media`/`@container`
    wrapping either group, verified — this is a pure block move, no at-rule conversion needed) into
    two new files: `frontend/src/components/profile/LockedStageArtwork.module.css` (~85 lines) and
    `frontend/src/components/profile/LayeredBadgeArtwork.module.css` (~95 lines). Delete the moved
    blocks from `MemberBadgeChain.module.css`. Preserve declaration order and exact values —
    this is extraction, not rewriting.
    In `MemberBadgeChain.tsx`: rename the existing `import styles from './MemberBadgeChain.module.css'`
    to `import chainStyles from './MemberBadgeChain.module.css'` and add the two new imports shown
    in `<interfaces>`. Update every `styles.xxx` reference in the file: the ~12 selectors owned by
    the two new modules become `lockedStageArtworkStyles.xxx`/`layeredBadgeArtworkStyles.xxx`; every
    other `styles.xxx` reference (everything not in this plan's owned-selector list) becomes
    `chainStyles.xxx`. Do this for the ENTIRE file in this task (not just the two owned groups) so
    later split plans (133-07/08/09) start from a file with no remaining bare `styles.` references.
  </action>
  <acceptance_criteria>
    - `frontend/src/components/profile/MemberBadgeChain.tsx` contains zero occurrences of the bare
      identifier `styles.` (all replaced with `chainStyles.`/`lockedStageArtworkStyles.`/
      `layeredBadgeArtworkStyles.`).
    - `wc -l frontend/src/components/profile/LockedStageArtwork.module.css` and
      `LayeredBadgeArtwork.module.css` are each under 450.
    - `grep -c '\.lockedStageArtwork\|\.roleArtworkMist' frontend/src/components/profile/MemberBadgeChain.module.css`
      returns 0 (fully moved, no leftover duplicate).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -c '\bstyles\.' src/components/profile/MemberBadgeChain.tsx || true</automated>
  </verify>
  <done>Both new CSS modules exist under 450 lines; `MemberBadgeChain.tsx` imports and references
  them via the new aliases with zero remaining bare `styles.` references.</done>
</task>

<task type="auto">
  <name>Task 2: Update MemberBadgeChain.test.tsx CSS-source assertions for the moved selectors</name>
  <files>frontend/src/components/profile/MemberBadgeChain.test.tsx</files>
  <read_first>
    frontend/src/components/profile/MemberBadgeChain.test.tsx
  </read_first>
  <action>
    In `MemberBadgeChain.test.tsx`, add two new `readFileSync` constants near the existing
    `memberBadgeChainCss` declaration (line 10):
    `const lockedStageArtworkCss = readFileSync('src/components/profile/LockedStageArtwork.module.css', 'utf8')`
    and
    `const layeredBadgeArtworkCss = readFileSync('src/components/profile/LayeredBadgeArtwork.module.css', 'utf8')`.
    Grep the test file for every assertion that references a selector now owned by these two new
    modules (`.lockedStageArtwork`, `.roleArtworkMist`, `.roleArtworkMotif`, `.roleArtworkFrame`,
    `.roleHeroArtworkLayered`, `.badgeArtworkLayered`, etc.) and change the assertion's source
    variable from `memberBadgeChainCss` to the correct new constant, without changing the asserted
    regex/value content.
    Do not modify any assertion whose selector still lives in `MemberBadgeChain.module.css`.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/profile/MemberBadgeChain.test.tsx` passes with
      zero failures.
    - No assertion was deleted or weakened — every pre-existing CSS-content check still runs,
      pointed at the correct file.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/profile/MemberBadgeChain.test.tsx</automated>
  </verify>
  <done>The full `MemberBadgeChain.test.tsx` suite passes against the post-split module layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure CSS-module extraction + import wiring; no new trust boundary, no input handling change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-03 | (none applicable) | `MemberBadgeChain.module.css` / `.tsx` split | accept | Presentation-only CSS/import refactor with no new trust boundary; out of scope for STRIDE. |
</threat_model>

<verification>
`cd frontend && npx vitest run src/components/profile/MemberBadgeChain.test.tsx` green;
`wc -l` on both new modules confirms <=450 lines; `npm run typecheck` and `npm run lint` clean.
</verification>

<success_criteria>
- Two new component-owned CSS modules exist, each <=450 lines.
- `MemberBadgeChain.tsx` uses the multi-import alias pattern with zero bare `styles.` references left.
- Full existing test coverage for the badge chain still passes.
</success_criteria>

<output>
Create `.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-04-SUMMARY.md` when done
</output>
