---
phase: 133
plan: 10
type: execute
wave: 5
depends_on: ["133-02", "133-03", "133-09"]
files_modified:
  - frontend/scripts/collect-member-profile-evidence.mjs
autonomous: true
requirements: [PMPF-06, PMPF-08]
user_setup: []

must_haves:
  truths:
    - "Both seed profiles (sheppert, csubs-leader) have a measured, locked per-profile image-byte budget (total + per-image ceiling) captured against the FINAL post-split, post-quality-gate frontend state."
    - "Running --mode budget-check against the current stack (no further code changes) passes cleanly against the newly locked image budget."
    - "The image budget follows the same baseline+margin+absolute-ceiling method Phase 131 already locked for API payload/latency, in the same LOCKED_BUDGETS/evaluateBudget() structure."
  artifacts:
    - path: "frontend/scripts/collect-member-profile-evidence.mjs"
      provides: "imageWaterfall budget block per profile + the corresponding evaluateBudget() check"
      contains: "imageWaterfall"
  key_links:
    - from: "frontend/scripts/collect-member-profile-evidence.mjs"
      to: "LOCKED_BUDGETS.profiles[slug].imageWaterfall"
      via: "evaluateBudget() reading capturePageMetrics()'s existing page.imageWaterfall field"
      pattern: "imageWaterfall"
---

<objective>
Extend `collect-member-profile-evidence.mjs`'s existing `--mode budget-check` machinery with a
measured, locked image-byte budget (D-08/PMPF-08), reusing its ALREADY-implemented
`capturePageMetrics()` -> `imageWaterfall` capture (per-image `url/status/bytes/timing`, verified
current) and the `LOCKED_BUDGETS`/`evaluateBudget()` baseline+margin+absolute-ceiling pattern
Phase 131 already locked for API payload/latency. Do NOT extend `verify-profile-image-delivery.mjs`
for this — RESEARCH.md's Pitfall 1 confirms that script is a format/cache-correctness probe over
five fixed, unrelated `phase120` fixtures with no notion of the two real seed profiles or a
byte-size budget at all.

This plan runs LAST among the config/CSS-touching plans (depends on 133-02's quality gate, 133-03's
hero container conversion, and 133-09's completed badge-chain CSS split) so the measured baseline
reflects the FINAL image-request shape this phase produces — not an intermediate state that later
plans would invalidate.

Purpose: PMPF-06 (variant/sizes/quality correctness proven by measurement) and PMPF-08
(compressed sources + fixed asset/transfer budgets, measurement-anchored per D-08).
Output: locked `imageWaterfall` budget entries for `sheppert`/`csubs-leader`, extended
`evaluateBudget()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-CONTEXT.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-RESEARCH.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-UI-SPEC.md
</context>

<interfaces>
Already-captured field this plan reads, not builds (`collect-member-profile-evidence.mjs`,
`capturePageMetrics()`, verified current):
```javascript
imageWaterfall: {
  count: images.length,
  totalBytes: imageBytes,
  images: images.map((request) => ({
    url: request.url, status: request.status, bytes: request.responseBodyBytes,
    startTimeMs: request.timing.startTimeMs, responseEndMs: request.timing.responseEndMs,
  })),
},
```
Existing budget-lock shape to extend (`LOCKED_BUDGETS`/`evaluateBudget()`, verified current — the
`profiles.sheppert.initialProfile`/`projectsPage1`/etc. entries already lock `maxBytes`/`maxMedianMs`
per API endpoint; this plan adds an analogous `imageWaterfall` entry per profile, checked in a new
code block inside `evaluateBudget()` mirroring the existing per-endpoint `check.xOk = measured <=
limit; if (!check.xOk) breaches.push(...)` idiom):
```javascript
const LOCKED_BUDGETS = {
  absoluteCeilings: { lcpMs: 2500, cls: 0.1, inpMs: 200 },
  queryCountCeiling: 19,
  profiles: {
    sheppert: {
      initialProfile: { expectStatus: 200, maxBytes: 1952, maxMedianMs: 25 },
      // ADD: imageWaterfall: { maxTotalBytes: <measured*1.2>, maxSingleImageBytes: <measured-max*1.2, floored at a sane absolute ceiling> },
      ...
    },
    'csubs-leader': { ... },
  },
}
```
Live stack for measurement (already running on this host, verified via `docker compose ps`):
frontend on `http://127.0.0.1:3000`, backend on `http://127.0.0.1:18092` — these match the script's
own defaults (`PERF_BASE_URL`/`PERF_API_BASE`), so no explicit `--base-url`/`--api-base` flags are
required when running from `team4s-linux`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Measure a fresh image-byte baseline on both seed profiles</name>
  <files>frontend/scripts/collect-member-profile-evidence.mjs (read-only measurement in this task)</files>
  <read_first>
    frontend/scripts/collect-member-profile-evidence.mjs
  </read_first>
  <action>
    Confirm the live stack is running (`docker compose ps` should show `team4sv30-frontend` and
    `team4sv30-backend` up). Run
    `cd frontend && node scripts/collect-member-profile-evidence.mjs --mode budget-check --output-dir /tmp/phase133-image-baseline --assert false`
    (the `--assert false` flag captures without failing on the NOT-yet-locked image budget, per
    this script's existing `runBudgetCheck` argument shape). Inspect the written
    `post-change-sheppert.json`/`post-change-csubs-leader.json` artifacts' `imageWaterfall.totalBytes`
    and per-image `bytes` values for both profiles.
    Record the measured `totalBytes` and the single largest per-image `bytes` value for each
    profile — these are the baseline this task's numbers feed into Task 2's locked budget (baseline
    * 1.2 margin for the total, plus an absolute per-image KB ceiling set from the measured max
    single-image size with headroom, mirroring the existing `maxBytes`/`maxMedianMs` margin method
    already documented in this file's header comment for API payload/latency).
  </action>
  <acceptance_criteria>
    - `/tmp/phase133-image-baseline/post-change-sheppert.json` and
      `post-change-csubs-leader.json` exist and each contain a non-null
      `page.imageWaterfall.totalBytes` and non-empty `page.imageWaterfall.images` array.
    - The measured numbers are recorded (in the task's own working notes / commit message) for
      Task 2 to consume — do not discard them.
  </acceptance_criteria>
  <verify>
    <automated>test -f /tmp/phase133-image-baseline/post-change-sheppert.json && test -f /tmp/phase133-image-baseline/post-change-csubs-leader.json</automated>
  </verify>
  <done>A fresh image-byte measurement exists for both seed profiles against the final post-split,
  post-quality-gate frontend state.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the imageWaterfall budget in LOCKED_BUDGETS and evaluateBudget()</name>
  <files>frontend/scripts/collect-member-profile-evidence.mjs</files>
  <read_first>
    frontend/scripts/collect-member-profile-evidence.mjs
  </read_first>
  <action>
    Add an `imageWaterfall: { maxTotalBytes, maxSingleImageBytes }` entry to
    `LOCKED_BUDGETS.profiles.sheppert` and `LOCKED_BUDGETS.profiles['csubs-leader']`, using
    Task 1's measured baseline values with the same ~20% margin already documented in this file's
    header comment for the other budget categories (`maxTotalBytes` = measured totalBytes * 1.2,
    rounded to a clean number; `maxSingleImageBytes` = measured largest single-image bytes * 1.2,
    rounded to a clean number).
    Inside `evaluateBudget(slug, api, page)`, add a new check block after the existing Web-Vitals
    `pageCheck` logic, following the exact same idiom: read `page.imageWaterfall`, compare
    `totalBytes` against `maxTotalBytes` and the max of `images[].bytes` against
    `maxSingleImageBytes`, push to the same `breaches` array on overrun, and attach the result to
    the returned object as `pageCheck.imageWaterfall` (or a new top-level `imageWaterfallCheck` key
    — match whichever nesting the existing `pageCheck` object already uses for Web-Vitals so this
    reads as one consistent addition, not a bolt-on).
    Update this file's header comment (the block above `LOCKED_BUDGETS`, verified current, already
    documents "Budgets are LOCKED here...") to mention the new image-budget category and its
    measurement basis/date, mirroring how the existing API budgets document theirs.
  </action>
  <acceptance_criteria>
    - `LOCKED_BUDGETS.profiles.sheppert.imageWaterfall` and
      `LOCKED_BUDGETS.profiles['csubs-leader'].imageWaterfall` both exist with numeric
      `maxTotalBytes`/`maxSingleImageBytes`.
    - `evaluateBudget()` checks the new fields and pushes a breach string on overrun, following the
      existing idiom exactly.
    - Re-running `node scripts/collect-member-profile-evidence.mjs --mode budget-check --output-dir /tmp/phase133-image-lock-verify`
      (default `--assert true`) exits 0 for both profiles — the newly locked budget passes against
      the current (unchanged) frontend state.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && node scripts/collect-member-profile-evidence.mjs --mode budget-check --output-dir /tmp/phase133-image-lock-verify</automated>
  </verify>
  <done>Both seed profiles have a locked, measurement-anchored image-byte budget that the current
  frontend state passes cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev-stack -> Playwright measurement script | The script drives a headless browser against the locally-running dev stack over loopback; no external network exposure, no production credential use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-04 | (none applicable) | `collect-member-profile-evidence.mjs` budget extension | accept | Build/tooling script run against a local dev stack; no new trust boundary, no production data path. |
</threat_model>

<verification>
`cd frontend && node scripts/collect-member-profile-evidence.mjs --mode budget-check --output-dir /tmp/phase133-image-lock-verify`
exits 0 for both profiles with the newly locked image budget in effect.
</verification>

<success_criteria>
- Both seed profiles have a locked, measurement-anchored image-byte budget.
- The current frontend state passes the newly locked budget with no code changes required.
</success_criteria>

<output>
Create `.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-10-SUMMARY.md` when done
</output>
