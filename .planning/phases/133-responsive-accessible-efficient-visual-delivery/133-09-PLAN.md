---
phase: 133
plan: 09
type: execute
wave: 4
depends_on: ["133-08"]
files_modified:
  - frontend/src/components/profile/MemberBadgeChain.module.css
  - frontend/src/components/profile/MemberBadgeChain.tsx
  - frontend/src/components/profile/MemberBadgeChain.test.tsx
  - frontend/src/components/profile/RoleBadgeCard.module.css
autonomous: true
requirements: [PMUI-01, PMUI-02, PMUI-03, PMUI-04, PMUI-05, PMUI-06, PMPF-06]
user_setup: []

must_haves:
  truths:
    - "Every selector that was previously defined 2-3 times with conflicting declarations (.roleLabel, .roleBadgeRow, .roleHeroArtwork, .roleProgressTrack) now has exactly one canonical rule, resolved per UI-SPEC.md's locked table."
    - "MemberBadgeChain.module.css (the shrunk shell) is <=450 lines and contains only the shell selectors: .section/.chainCard/.groupList/.group/.carouselShell/.carouselSkeleton/.chain/.badgeWindow/.badgeGrid/.visuallyHidden."
    - "All 9 new CSS modules created across Plans 133-04/07/08/09 are each <=450 lines."
    - "Zero !important declarations remain in the badge-chain CSS modules unless each kept exception carries an explicit code comment justifying it."
  artifacts:
    - path: "frontend/src/components/profile/RoleBadgeCard.module.css"
      provides: "de-duplicated role-progress-card styles, reuses member-badge-carousel container"
      contains: ".roleBadgeRow"
    - path: "frontend/src/components/profile/MemberBadgeChain.module.css"
      provides: "shrunk shell CSS, <=450 lines"
      min_lines: 100
  key_links:
    - from: "frontend/src/components/profile/MemberBadgeChain.tsx"
      to: "frontend/src/components/profile/RoleBadgeCard.module.css"
      via: "roleBadgeCardStyles import used inside the inline roles-card render block"
      pattern: "roleBadgeCardStyles"
---

<objective>
Complete the `MemberBadgeChain.module.css` split by extracting the final and largest remaining
chunk — `RoleBadgeCard.module.css` (`.roleBadgeRow/.roleLabel/.roleHeroArtwork/.roleStatus/
.roleCount/.roleProgress*/.roleProgression/.roleNextCopy/.roleStage*/.currentChip`) — while
resolving the duplicate/conflicting selector definitions RESEARCH.md's Pattern 1 and UI-SPEC.md's
"Duplicate-selector resolution" table both document as the single highest-value PMUI-05 cleanup in
this phase. Four selectors are currently defined 2-3 times at widely separated line numbers with
genuinely different declarations, and the later block wins purely by source order — this plan
picks exactly ONE canonical rule per selector per UI-SPEC.md's locked table (not a new third
variant): `.roleLabel` keeps the 12px/non-uppercase/wrapping variant (rejects the
`0.82rem`/uppercase/`nowrap` variant, which risks 400%-zoom overflow); `.roleBadgeRow` keeps the
`min-height: 0; padding: 32px 24px` flexible-height variant (rejects the fixed `min-height: 420px`
variant); `.roleHeroArtwork` keeps `aspect-ratio: 1; height: auto` (rejects the fixed `320px`
width/height variant — this is also the canonical CLS-safe geometry pattern PMPF-06 wants copied
elsewhere); `.roleProgressTrack`'s near-identical duplicate is simply removed (no value conflict).

The role-card renders as a `FocalCarousel` item (the roles branch of `renderItem`, inline inside
`MemberBadgeChain()`), so it reuses the existing `member-badge-carousel` container name for its
`@container` conversions, matching every carousel-item module from Plans 133-07/08. This plan also
removes the file's 4 `!important` declarations (all on `.badgeWindow`'s `flex-basis`/`overflow` —
already moved to the shell in earlier plans, verify they still live in the shell after Plans
133-04/07/08 and resolve them here via normal cascade ordering) and performs a final PMPF-06
truthful-`sizes` review across the badge/role/hero `ResponsiveImage` call sites now that every
container breakpoint has settled.

Purpose: PMUI-05 (the phase's single highest-value duplicate-selector cleanup), PMUI-04 (final
shell size), PMPF-06 (sizes truthfulness after all container breakpoints are final).
Output: `RoleBadgeCard.module.css` (de-duplicated), shrunk `MemberBadgeChain.module.css` shell,
zero unjustified `!important`, reviewed `sizes` strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-CONTEXT.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-RESEARCH.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-PATTERNS.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-UI-SPEC.md
</context>

<interfaces>
UI-SPEC.md's locked canonical resolution table (authoritative — do not invent a third variant):

| Selector | Reject this variant | Keep this variant |
|---|---|---|
| `.roleLabel` | `font-size: 0.82rem; font-weight: 800; text-transform: uppercase; letter-spacing: 0.06em; white-space: nowrap;` | `justify-self: start; font-size: 12px; font-weight: 700; line-height: 1.2; letter-spacing: 0.04em;` |
| `.roleBadgeRow` | `min-height: 420px; padding: 24px 8px;` (plus its shared declarations with `.roleBadgeRowCompact`) | `width: 100%; min-width: 0; box-sizing: border-box; min-height: 0; display: grid; grid-template-columns: minmax(0, 1fr); place-items: center; gap: 16px; padding: 32px 24px; overflow: hidden; background: <the radial-gradient + var(--surface-card) value>;` |
| `.roleHeroArtwork` | `width: 320px; height: 320px;` (both duplicate declarations of this) | `aspect-ratio: 1; height: auto;` (drop the `.group[data-badge-group="roles"]` descendant-selector scoping once this lives alone in its own module — this rule becomes the plain `.roleHeroArtwork` in the new file) |
| `.roleProgressTrack` | the second, later near-identical block | the first (content is equivalent; keep whichever appears first after extraction, delete the redundant copy) |

Everything else under `.roleBadgeRow*`/`.roleLabel`/`.roleHeroArtwork*`/`.roleStatus`/`.roleCount`/
`.roleProgress*`/`.roleProgression`/`.roleNextCopy`/`.roleStage*`/`.currentChip` that has NO
conflicting duplicate (the `[data-role-code="..."]` per-role accent variants, the
`:is(...)` inactive/expanded state rules, etc.) moves as-is — only the four selectors in the table
above need a decision, not a rewrite of the whole selector group.

4 `!important` sites (verified current, both on `.badgeWindow`'s `flex-basis`/`overflow`, one pair
inside a plain rule and one pair inside a media/container-scoped rule) — grep
`!important` in `MemberBadgeChain.module.css` after Plans 133-04/07/08 land (they should still be
in the shell, since `.badgeWindow` is a shell-owned selector) and resolve by reordering/raising
specificity of the base `.badgeWindow` rule instead of adding `!important` to the later, more
specific rule that currently needs it to win.

Existing `sizes` constants to review (`MemberBadgeChain.tsx` module scope, unchanged names):
`COMPACT_BADGE_SIZES = '(max-width: 520px) 72px, 96px'`,
`ACTIVE_BADGE_SIZES = '(max-width: 520px) 248px, (max-width: 1099px) 280px, 320px'`. These are
viewport-width-based `sizes` strings for images rendered inside container-query-driven cards after
this phase's split — verify each still approximates the true rendered width at the NEW container
breakpoints (480/520/900/1099px etc. as extracted across Plans 133-04/07/08/09); update only if a
container breakpoint changed a rendered image's actual pixel width materially (do not invent new
breakpoints not already present in the CSS).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract RoleBadgeCard.module.css with de-duplicated canonical selectors</name>
  <files>frontend/src/components/profile/MemberBadgeChain.module.css, frontend/src/components/profile/MemberBadgeChain.tsx, frontend/src/components/profile/RoleBadgeCard.module.css</files>
  <read_first>
    frontend/src/components/profile/MemberBadgeChain.module.css
    frontend/src/components/profile/MemberBadgeChain.tsx
  </read_first>
  <action>
    Grep for every remaining `.roleBadgeRow*`/`.roleLabel`/`.roleHeroArtwork*`/`.roleStatus`/
    `.roleCount`/`.roleProgress*`/`.roleProgression`/`.roleNextCopy`/`.roleStage*`/`.currentChip`
    selector in `MemberBadgeChain.module.css` (this is now the largest remaining block after Plans
    133-04/07/08). Cut all of it into `frontend/src/components/profile/RoleBadgeCard.module.css`.
    Apply the `<interfaces>` duplicate-resolution table: keep exactly ONE declaration per
    `.roleLabel`/`.roleBadgeRow`/`.roleHeroArtwork`/`.roleProgressTrack`, matching the "Keep this
    variant" column verbatim; delete every rejected duplicate. Add the single-selector "no card
    surface" copy for `.roleBadgeRow` moved from the shell (Plan 133-07 left it there
    deliberately). Convert every remaining device-width `@media` for this selector group (verify
    via grep — includes blocks at multiple breakpoints such as 480/520/900/1099/1440/1600/2100px
    per RESEARCH.md's line inventory) to `@container member-badge-carousel (...)` at the SAME
    pixel values, reusing the existing container name (this component is a `FocalCarousel` item).
    While converting, if two or more `@media`/`@container` blocks at DIFFERENT breakpoints set
    near-identical properties on the same selector with only minor value differences (RESEARCH.md's
    Open Question 2 flags three separate ~1440px-adjacent blocks touching `.roleStatus`/
    `.roleCount`), consolidate them into one container-query block per logical breakpoint rather
    than preserving redundant near-duplicate blocks — this is expected to help bring the file under
    450 lines. If, after de-duplication and breakpoint consolidation, the file is still over 450
    lines, split the desktop-scale breakpoints (1440/1600/2100px equivalents) into a companion
    `RoleBadgeCard.wide.module.css` imported alongside the base file, both still owned exclusively
    by the same role-card render block.
    In `MemberBadgeChain.tsx`, add `import roleBadgeCardStyles from './RoleBadgeCard.module.css'`
    (and `roleBadgeCardWideStyles` if a companion file was needed) and update every
    `chainStyles.role*`/`chainStyles.currentChip` reference inside the inline roles-card render
    block to the new alias(es).
    Delete the moved blocks from `MemberBadgeChain.module.css`.
  </action>
  <acceptance_criteria>
    - `RoleBadgeCard.module.css` (plus an optional `.wide` companion) exists; each file is
      <=450 lines (`wc -l`).
    - `grep -c '\.roleLabel' RoleBadgeCard.module.css` (or its companion) is exactly 1, and its
      declaration contains `font-size: 12px` and does NOT contain `white-space: nowrap` or
      `text-transform: uppercase`.
    - `grep -A5 '^\.roleBadgeRow {' RoleBadgeCard.module.css` shows `min-height: 0` (not `420px`).
    - `grep -A3 '^\.roleHeroArtwork {' RoleBadgeCard.module.css` shows `aspect-ratio: 1` (not a
      fixed `320px` width/height).
    - `grep -c '\.roleProgressTrack {' RoleBadgeCard.module.css` (summed across base + companion
      file if split) is exactly 1.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -c '\.roleLabel {' src/components/profile/RoleBadgeCard.module.css</automated>
  </verify>
  <done>Every previously-duplicated role-card selector has exactly one canonical declaration
  matching UI-SPEC.md's locked table; the new module(s) are each <=450 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Shrink the shell to &lt;=450 lines, remove !important, review sizes truthfulness, finalize tests</name>
  <files>frontend/src/components/profile/MemberBadgeChain.module.css, frontend/src/components/profile/MemberBadgeChain.tsx, frontend/src/components/profile/MemberBadgeChain.test.tsx</files>
  <read_first>
    frontend/src/components/profile/MemberBadgeChain.module.css
    frontend/src/components/profile/MemberBadgeChain.tsx
    frontend/src/components/profile/MemberBadgeChain.test.tsx
  </read_first>
  <action>
    Confirm `MemberBadgeChain.module.css` now contains only shell selectors:
    `.section/.chainCard/.groupList/.group/.carouselShell/.carouselSkeleton/.chain/.skeletonControl/
    .skeletonCard/.badgeWindow/.badgeWindowActive/.badgeGrid/.visuallyHidden/.progressMeta` (the
    `container: member-badge-carousel / inline-size;` declaration on `.carouselShell` MUST remain
    untouched — every other module extracted across Plans 133-04/07/08/09 depends on it).
    Grep for `!important` in this file (should be exactly the 4 known sites on `.badgeWindow`'s
    `flex-basis`/`overflow`, both in a base rule and a container-scoped rule). Resolve by raising
    the specificity or reordering the base `.badgeWindow` rule so the later, more specific rule
    naturally wins without `!important`. If, after attempting normal cascade resolution, any single
    site genuinely cannot be resolved without `!important` (e.g. it overrides an inline style or a
    third-party component's own specificity), keep exactly that one and add a one-line code comment
    directly above it explaining why (per D-05, "nur als begründete Ausnahme").
    Review the `COMPACT_BADGE_SIZES`/`ACTIVE_BADGE_SIZES` `sizes` strings against the final
    container breakpoints now in `RoleBadgeCard.module.css`/`BadgeChip.module.css`/
    `BadgeFamilyCard.module.css` (per `<interfaces>`) — update only if a rendered image's true
    pixel width at a given breakpoint materially diverges from what the current `sizes` string
    claims; otherwise leave unchanged and note in the plan summary that they were reviewed and
    found accurate.
    In `MemberBadgeChain.test.tsx`, add the final `roleBadgeCardCss` (and `.wide` companion if
    created) `readFileSync` constant(s) and update every remaining assertion still pointed at
    `memberBadgeChainCss` for a now-moved role-card selector. Run the full file and fix any
    remaining failures.
  </action>
  <acceptance_criteria>
    - `wc -l frontend/src/components/profile/MemberBadgeChain.module.css` reports <=450.
    - `grep -c '!important' frontend/src/components/profile/MemberBadgeChain.module.css` (and every
      other split module from this phase) is 0, OR each remaining occurrence has a code comment
      directly above it justifying the exception.
    - `wc -l` on all 9-10 CSS modules created across Plans 133-04/07/08/09 confirms every file
      <=450 lines.
    - `cd frontend && npx vitest run src/components/profile/MemberBadgeChain.test.tsx` passes with
      zero failures.
    - `cd frontend && npm run lint && npm run typecheck` both pass.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && wc -l src/components/profile/MemberBadgeChain.module.css src/components/profile/RoleBadgeCard.module.css src/components/profile/BadgeChip.module.css src/components/profile/BadgeFamilyCard.module.css src/components/profile/LayeredBadgeArtwork.module.css src/components/profile/LockedStageArtwork.module.css src/components/profile/AnimeProjectStage.module.css src/components/profile/PointsAchievementStage.module.css src/components/profile/ContributionAchievementStage.module.css src/components/profile/MembershipStage.module.css && npx vitest run src/components/profile/MemberBadgeChain.test.tsx</automated>
  </verify>
  <done>The badge-chain CSS split is complete: every module (shell + 9-10 extracted files) is
  <=450 lines, zero unjustified `!important` remains, `sizes` strings were reviewed, and the full
  test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure CSS de-duplication, container-query conversion, and `sizes`-attribute review; no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-03 | (none applicable) | `MemberBadgeChain.module.css` / `.tsx` split completion | accept | Presentation-only CSS/import refactor with no new trust boundary; out of scope for STRIDE. |
</threat_model>

<verification>
`cd frontend && npx vitest run src/components/profile/MemberBadgeChain.test.tsx` green; `wc -l`
confirms every split CSS module (shell + all extracted files) is <=450 lines; `grep -c '!important'`
across all badge-chain CSS files is 0 or each occurrence is justified with a comment; `npm run lint`
and `npm run typecheck` clean.
</verification>

<success_criteria>
- The entire `MemberBadgeChain.module.css` split (Plans 133-04/07/08/09) is complete: shell +
  9-10 component-owned modules, all <=450 lines.
- All four previously-duplicated selectors have exactly one canonical declaration.
- Zero unjustified `!important`.
- `sizes` strings reviewed for truthfulness against final container breakpoints.
</success_criteria>

<output>
Create `.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-09-SUMMARY.md` when done
</output>
