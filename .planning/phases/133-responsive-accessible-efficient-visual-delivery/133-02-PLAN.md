---
phase: 133
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/next.config.mjs
  - frontend/src/components/ui/ResponsiveImage.config.test.ts
autonomous: true
requirements: [PMPF-06, PMPF-08]
user_setup: []

must_haves:
  truths:
    - "In a production build (NODE_ENV=production), the Next.js image optimizer refuses to optimize local-IP-hosted image sources."
    - "In development/test (NODE_ENV!=production), local-IP image optimization keeps working exactly as it does today (no dev-workflow regression)."
    - "next/image quality is bounded by an explicit next.config.mjs allow-list rather than an unbounded per-call-site prop."
    - "/media/**, /member-achievement-badges/**, and /covers/** remain in localPatterns after the edit (no E426 regression)."
  artifacts:
    - path: "frontend/next.config.mjs"
      provides: "env-gated dangerouslyAllowLocalIP + explicit images.qualities allow-list"
      contains: "dangerouslyAllowLocalIP: process.env.NODE_ENV"
    - path: "frontend/src/components/ui/ResponsiveImage.config.test.ts"
      provides: "regression coverage for the env gate and the retained localPatterns allow-list"
      contains: "dangerouslyAllowLocalIP"
  key_links:
    - from: "frontend/next.config.mjs"
      to: "process.env.NODE_ENV"
      via: "images.dangerouslyAllowLocalIP conditional expression"
      pattern: "dangerouslyAllowLocalIP:\\s*process\\.env\\.NODE_ENV"
---

<objective>
Close the two concrete image-delivery config gaps RESEARCH.md verified by direct read: (1)
`next.config.mjs`'s `images.dangerouslyAllowLocalIP` is currently unconditionally `true` with no
environment gate (Pitfall 2 — Next.js's own docs call this "not recommended for most users" outside
a controlled environment; D-09/PMPF-08 requires it restricted to development/test), and (2) no
`images.qualities` allow-list exists yet, so `next/image`'s quality bound is only implicit
(D-07/PMPF-06). Add a regression test guarding both, and a diff-check that the existing
`localPatterns` allow-list (`/media/**`, `/member-achievement-badges/**`, `/covers/**`) survives
the edit verbatim (Pitfall 3 — a documented E426-crash precedent in this repo if `/media/**` is
ever dropped).

Purpose: satisfy PMPF-08's "lokale-IP-Bildoptimierung bleibt auf Test und Entwicklung begrenzt" and
PMPF-06's "begrenzte Qualität" with a config-level guarantee, not a convention.
Output: gated `next.config.mjs`, extended `ResponsiveImage.config.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-CONTEXT.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-RESEARCH.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-UI-SPEC.md
</context>

<interfaces>
From frontend/next.config.mjs (current state, verbatim — do not restructure the surrounding object):
```
images: {
  formats: ['image/webp'],
  deviceSizes: [640, 1080, 1480, 1920],
  imageSizes: [64, 96, 128, 160, 192, 256, 512],
  localPatterns: [
    { pathname: '/__phase120-image-probe/alpha-badge.png', search: '' },
    { pathname: '/member-achievement-badges/**', search: '' },
    { pathname: '/covers/**', search: '' },
    { pathname: '/media/**', search: '' },
  ],
  remotePatterns: [ ...two loopback URL()s..., ...configuredApiMediaPatterns() ],
  dangerouslyAllowLocalIP: true,  // <- CHANGE TARGET
}
```
`configuredApiMediaPatterns()` (lines 7-19) is the existing env-branching helper pattern to mirror
for the new guard's shape (read, do not copy verbatim — the new guard is a single conditional
expression, not a function).

From frontend/src/components/ui/ResponsiveImage.config.test.ts (current state):
```
import nextConfig from '../../../next.config.mjs'
const localPatterns = nextConfig.images?.localPatterns
const remotePatterns = nextConfig.images?.remotePatterns ?? []
```
Existing assertions use `hasLocalMatch(localPatterns, path)` / `hasRemoteMatch([], remotePatterns, url)`
imported from `next/dist/shared/lib/match-local-pattern` / `match-remote-pattern`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Gate dangerouslyAllowLocalIP and add an explicit images.qualities allow-list</name>
  <files>frontend/next.config.mjs</files>
  <read_first>
    frontend/next.config.mjs
    frontend/src/components/ui/ResponsiveImage.tsx
  </read_first>
  <action>
    In `frontend/next.config.mjs`'s `images` object, replace the line
    `dangerouslyAllowLocalIP: true,` with
    `dangerouslyAllowLocalIP: process.env.NODE_ENV !== 'production',` — keep the existing comment
    above it about the deterministic probe origin being loopback-only, and add one line explaining
    the env gate (per Next.js 16's own guidance that this flag is "not recommended for most users"
    in production).
    Add `qualities: [75],` to the same `images` object (Next.js 16's own default quality when
    unset — making it explicit rather than implicit satisfies D-07/PMPF-06's "begrenzte Qualität"
    without changing any currently-rendered byte size, since no call site in
    `frontend/src/components/profile` or `frontend/src/components/ui/ResponsiveImage.tsx` currently
    passes an explicit `quality` prop).
    Do NOT modify `localPatterns`, `remotePatterns`, `deviceSizes`, `imageSizes`, or `formats` —
    diff the file after editing and confirm `/media/**`, `/member-achievement-badges/**`, and
    `/covers/**` are byte-for-byte unchanged in `localPatterns`.
  </action>
  <acceptance_criteria>
    - `frontend/next.config.mjs` contains the exact string
      `dangerouslyAllowLocalIP: process.env.NODE_ENV !== 'production'` (or an equivalent boolean
      expression that evaluates `false` when `NODE_ENV==='production'` and `true` otherwise).
    - `frontend/next.config.mjs` contains `qualities: [75]` inside the `images` object.
    - `localPatterns` array still contains all four original entries verbatim (`__phase120-image-probe`,
      `member-achievement-badges/**`, `covers/**`, `media/**`).
    - `cd frontend && npm run typecheck` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && node -e "process.env.NODE_ENV='production'; import('./next.config.mjs').then(m => { if (m.default.images.dangerouslyAllowLocalIP !== false) throw new Error('still allowed in production'); console.log('ok'); })"</automated>
  </verify>
  <done>`dangerouslyAllowLocalIP` resolves to `false` under `NODE_ENV=production` and `true`
  otherwise; `images.qualities` is explicit; `localPatterns` is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression coverage for the env gate and the retained localPatterns allow-list</name>
  <files>frontend/src/components/ui/ResponsiveImage.config.test.ts</files>
  <behavior>
    - Test: importing `nextConfig` with `NODE_ENV` unset/`'test'`/`'development'` yields
      `images.dangerouslyAllowLocalIP === true`.
    - Test: `images.qualities` contains `75` and has no value outside a sane 1-100 range.
    - Test (regression lock, Pitfall 3): `hasLocalMatch(localPatterns, '/media/profile/1/avatar/x/original.png')`
      is still `true` after this plan's edit (reuses the existing `it.each` pattern already in this
      file rather than duplicating a new assertion block).
  </behavior>
  <read_first>
    frontend/src/components/ui/ResponsiveImage.config.test.ts
    frontend/next.config.mjs
  </read_first>
  <action>
    Add a new `it(...)` (or `describe` block) to `ResponsiveImage.config.test.ts` asserting
    `nextConfig.images?.qualities` contains `75`, following the existing "import config, assert a
    derived property" shape already used in this file (no new test utility, no snapshot).
    Add a second `it(...)` that re-imports the config module under a mocked `NODE_ENV` (use
    `vi.stubEnv('NODE_ENV', 'production')` then dynamic `import('../../../next.config.mjs')` with
    a cache-busting query string, or read+eval the module fresh per Vitest's ESM re-import
    semantics — whichever approach this codebase's other env-conditional tests already use; check
    `next.config.mjs`'s existing `configuredApiMediaPatterns()` for a precedent test if one exists
    elsewhere in the suite) asserting `images.dangerouslyAllowLocalIP === false` under production
    and `=== true` under a non-production value. Run `vi.unstubAllEnvs()` in an `afterEach` if
    `vi.stubEnv` is used.
    Confirm the existing three `it.each`/`it(...)` blocks in this file still pass unmodified — this
    task adds coverage, it does not rewrite existing assertions.
  </action>
  <acceptance_criteria>
    - New test(s) exist asserting `images.qualities` contains `75`.
    - New test(s) exist asserting `dangerouslyAllowLocalIP` is `false` under `NODE_ENV=production`
      and `true` otherwise.
    - All pre-existing assertions in this file (allow-listed `/media/**`, badge, covers, remote
      Team4s API media paths) still pass unmodified.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/ui/ResponsiveImage.config.test.ts</automated>
  </verify>
  <done>`ResponsiveImage.config.test.ts` locks the env-gated `dangerouslyAllowLocalIP` behavior and
  the explicit `qualities` allow-list, alongside all pre-existing localPatterns/remotePatterns
  coverage, all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Next.js image optimizer (`/_next/image`) -> arbitrary requested URL | The optimizer fetches whatever URL matches `localPatterns`/`remotePatterns`; `dangerouslyAllowLocalIP` additionally permits optimizing sources that resolve to a local/loopback IP, which is the classic SSRF-adjacent surface for image optimizers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-01 | Tampering / Information Disclosure | `next.config.mjs` `images.dangerouslyAllowLocalIP` | mitigate | Gate to `process.env.NODE_ENV !== 'production'` (Task 1) so the local-IP optimization path is unreachable in any production deployment; loopback probe fixtures remain usable in dev/test only. |
| T-133-02 | Denial of Service (self-inflicted) | `next.config.mjs` `images.localPatterns` | mitigate | Task 2's regression test asserts `/media/**` (and the badge/covers paths) still `hasLocalMatch` after this edit, preventing the documented E426-crash precedent from recurring silently during this or a future config refactor. |
</threat_model>

<verification>
`cd frontend && npx vitest run src/components/ui/ResponsiveImage.config.test.ts` green; manual
`NODE_ENV=production node -e "..."` probe from Task 1 confirms the gate; `npm run typecheck` clean.
</verification>

<success_criteria>
- `dangerouslyAllowLocalIP` is environment-gated, not unconditional.
- `images.qualities` is explicit.
- `localPatterns` is unchanged and regression-tested.
</success_criteria>

<output>
Create `.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-02-SUMMARY.md` when done
</output>
