---
phase: 133
plan: 07
type: execute
wave: 2
depends_on: ["133-04"]
files_modified:
  - frontend/src/components/profile/MemberBadgeChain.module.css
  - frontend/src/components/profile/MemberBadgeChain.tsx
  - frontend/src/components/profile/MemberBadgeChain.test.tsx
  - frontend/src/components/profile/AnimeProjectStage.module.css
  - frontend/src/components/profile/PointsAchievementStage.module.css
  - frontend/src/components/profile/ContributionAchievementStage.module.css
  - frontend/src/components/profile/MembershipStage.module.css
autonomous: true
requirements: [PMUI-01, PMUI-02, PMUI-03, PMUI-04, PMUI-05, PMUI-06, PMUI-07]
user_setup: []

must_haves:
  truths:
    - "AnimeProjectAchievementStage, PointsAchievementStage, MembershipStage each respond to their OWN container width (they render standalone, not inside FocalCarousel) instead of viewport width."
    - "ContributionAchievementStage (which DOES render inside FocalCarousel) continues to use the existing shared member-badge-carousel container name, now fully migrated off @media."
    - "The cross-cutting 'no card surface' rule that currently spans five selectors in one shared block is split into one single-selector copy per new module, removing a broad multi-component selector (PMUI-07)."
  artifacts:
    - path: "frontend/src/components/profile/AnimeProjectStage.module.css"
      provides: "styles for AnimeProjectAchievementStage(), own local unnamed container"
      contains: "container-type: inline-size"
    - path: "frontend/src/components/profile/PointsAchievementStage.module.css"
      provides: "styles for PointsAchievementStage(), own local unnamed container"
      contains: "container-type: inline-size"
    - path: "frontend/src/components/profile/ContributionAchievementStage.module.css"
      provides: "styles for ContributionAchievementStage(), reuses the shared member-badge-carousel container"
      contains: "@container member-badge-carousel"
    - path: "frontend/src/components/profile/MembershipStage.module.css"
      provides: "styles for MembershipStage(), own local unnamed container"
      contains: "container-type: inline-size"
  key_links:
    - from: "frontend/src/components/profile/MemberBadgeChain.tsx"
      to: "the four new stage CSS modules"
      via: "aliased CSS module imports used inside their respective owning functions"
      pattern: "import styles from .Stage.module.css"
---

<objective>
Continue the `MemberBadgeChain.module.css` split (Plan 133-04 established the multi-import
pattern) by extracting the four achievement-stage modules whose CSS is currently interleaved with
each other and the rest of the 2282-line file: `.animeProject*` (owns `AnimeProjectAchievementStage`,
CSS at current `@media (max-width: 700px)` block plus base rules), `.points*` (owns
`PointsAchievementStage`, `@media (max-width: 900px)`/`(max-width: 520px)`), `.contributionAchievementStage/
.contributionStage*/.contributionTier*` (owns `ContributionAchievementStage`, a MIX of plain
`@media` and already-existing `@container member-badge-carousel` — verified by direct grep of the
current file), and `.membershipStage*/.membershipDurationTrack/.foundingMember*` (owns
`MembershipStage`, `@media (max-width: 900px)`/`(max-width: 520px)`).

Three of these four components (`AnimeProjectAchievementStage`, `PointsAchievementStage`,
`MembershipStage`) render STANDALONE — directly in `MemberBadgeChain`'s collectionGroups branch,
NOT as `FocalCarousel` items (verified by direct read of `MemberBadgeChain.tsx` lines 888-893) — so
each needs its OWN local `container-type: inline-size` declaration on its own Card root class and
uses UNNAMED `@container (...)` queries. `ContributionAchievementStage` DOES render as a
`FocalCarousel` item (line 918) and therefore sits inside the existing `member-badge-carousel`
named container already declared on `.carouselShell` in the (still-shell) `MemberBadgeChain.module.css`
— its remaining plain `@media` rules convert to `@container member-badge-carousel (...)`, reusing
that name, NOT a new one.

This plan also performs the PMUI-07 cross-cutting-selector cleanup: `MemberBadgeChain.module.css`
currently contains one shared rule block covering five selectors
(`.roleBadgeRow, .animeProjectStage, .pointsAchievementStage, .contributionAchievementStage,
.membershipStage { border: 0; border-radius: 0; background: transparent; box-shadow: none; }`)
plus its `.group[data-badge-group] :is(...)` scoped twin. Split the FOUR selectors owned by this
plan's modules out of that shared block into one single-selector copy per destination module
(identical declarations); leave `.roleBadgeRow`'s copy in the shrinking shell file for now
(Plan 133-09 moves it when RoleBadgeCard extracts).

Purpose: PMUI-03/04/05/06/07 for four components in one coherent pass; PMUI-01/02 preserved via the
same min-width:0/flexible-height patterns already dominant in this file.
Output: four new component-owned CSS modules, updated `MemberBadgeChain.tsx` imports, updated
`MemberBadgeChain.test.tsx` assertions.

Scope note: `MemberBadgeChain.tsx` itself (928 lines, over CLAUDE.md's 450-line cap) is
accepted pre-existing debt deferred outside this phase's scope (see STATE.md's `[Phase 133]`
decision, formally resolving RESEARCH.md's Open Question 1). This plan only extracts CSS and
rewires the multi-alias imports established by Plan 133-04 — it does not split the `.tsx`
file's function bodies into separate files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-CONTEXT.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-RESEARCH.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-PATTERNS.md
@.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-UI-SPEC.md
</context>

<interfaces>
`MemberBadgeChain.tsx` after Plan 133-04 already has `chainStyles` as the primary alias for
everything not yet split out. This plan adds four more imports, one per new module, named
`animeProjectStageStyles`, `pointsAchievementStageStyles`, `contributionAchievementStageStyles`,
and `membershipStageStyles`.

Update every `chainStyles.animeProject*` reference inside `AnimeProjectAchievementStage()` to
`animeProjectStageStyles.*`; every `chainStyles.points*` reference inside `PointsAchievementStage()`
to `pointsAchievementStageStyles.*`; every `chainStyles.contributionAchievementStage`,
`chainStyles.contributionStage*`, `chainStyles.contributionTier*` reference inside
`ContributionAchievementStage()` to `contributionAchievementStageStyles.*`; every
`chainStyles.membershipStage*`, `chainStyles.membershipDurationTrack`, `chainStyles.foundingMember*`
reference inside `MembershipStage()` to `membershipStageStyles.*`.

Existing shared "no card surface" rule to split (verified current, near the end of
`MemberBadgeChain.module.css`, one rule with five comma-separated selectors plus a
`.group[data-badge-group] :is(...)`-scoped twin with the same five selectors and declarations).
Replace both rules in the shell file with a version listing only `.roleBadgeRow` (kept there for
Plan 133-09), and add the equivalent single-selector declaration for each of the four selectors
this plan owns (base rule plus its `.group[data-badge-group] .<selector> { ... }` scoped twin) into
the corresponding new file.

Existing `@media`/`@container` blocks per component (verified via grep of the current file — exact
line numbers will have shifted after Plan 133-04's edits, re-grep before extracting):
- `.animeProjectStage` and its descendants: one `@media (max-width: 700px) { ... }` layout block,
  plus `@media (prefers-reduced-motion: reduce) { .animeProjectProgressTrack > span { transition: none; } }`
  (KEEP as `@media`, not layout).
- `.pointsAchievementStage` and its descendants: `@media (max-width: 900px) { .pointsStageHero {...} }`,
  `@media (max-width: 520px) { .pointsAchievementStage {...} }`,
  `@media (prefers-reduced-motion: reduce) { ... }` (KEEP).
- `.contributionAchievementStage` and its descendants: `@media (max-width: 900px) { .contributionStageHero {...} }`,
  `@container member-badge-carousel (max-width: 1100px) { ... }` (already correct, move as-is,
  no conversion needed), `@container member-badge-carousel (min-width: 640px) and (max-width: 1100px) { ... }`
  (already correct, move as-is), `@media (max-width: 520px) { .contributionAchievementStage {...} }`,
  `@media (prefers-reduced-motion: reduce) { ... }` (KEEP).
- `.membershipStage` and its descendants: `@media (max-width: 900px) { .membershipStageHero {...} }`,
  `@media (max-width: 520px) { .membershipStage {...} }`,
  `@media (prefers-reduced-motion: reduce) { ... }` (KEEP).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract AnimeProjectStage.module.css and PointsAchievementStage.module.css with local containers</name>
  <files>frontend/src/components/profile/MemberBadgeChain.module.css, frontend/src/components/profile/MemberBadgeChain.tsx, frontend/src/components/profile/AnimeProjectStage.module.css, frontend/src/components/profile/PointsAchievementStage.module.css</files>
  <read_first>
    frontend/src/components/profile/MemberBadgeChain.module.css
    frontend/src/components/profile/MemberBadgeChain.tsx
  </read_first>
  <action>
    Grep the current `MemberBadgeChain.module.css` for every `.animeProject*` and `.points*`
    selector (base rules plus their `@media` blocks). Cut them into
    `frontend/src/components/profile/AnimeProjectStage.module.css` (~90 lines) and
    `frontend/src/components/profile/PointsAchievementStage.module.css` (~85 lines) respectively.
    On EACH file's root Card selector (`.animeProjectStage` in one file, `.pointsAchievementStage`
    in the other) add `container-type: inline-size;` (unnamed — no `container-name`, since neither
    component nests inside another named container).
    Convert `@media (max-width: 700px) { ... }` (anime) and `@media (max-width: 900px) { ... }`
    plus `@media (max-width: 520px) { ... }` (points) to unnamed `@container (max-width: 700px)`,
    `@container (max-width: 900px)`, `@container (max-width: 520px)` respectively, preserving every
    declared value exactly.
    KEEP the `@media (prefers-reduced-motion: reduce) { ... }` blocks for both AS `@media` — do not
    convert reduced-motion (a device/OS-level signal, not layout).
    Add the single-selector "no card surface" copy for `.animeProjectStage` and
    `.pointsAchievementStage` (see `<interfaces>`) into each respective new file.
    In `MemberBadgeChain.tsx`, add the two new imports and update every reference inside
    `AnimeProjectAchievementStage()`/`PointsAchievementStage()` from `chainStyles.` to the new
    aliases.
    In `MemberBadgeChain.module.css`, delete the moved blocks and narrow the shared "no card
    surface" rule to remove `.animeProjectStage`/`.pointsAchievementStage` from its selector list.
  </action>
  <acceptance_criteria>
    - `AnimeProjectStage.module.css` and `PointsAchievementStage.module.css` each exist, are each
      under 450 lines, and each declare `container-type: inline-size` on their own root selector.
    - Neither new file contains a device-width `@media` rule for layout purposes (only the
      preserved `prefers-reduced-motion` block remains as `@media`).
    - `MemberBadgeChain.module.css`'s shared "no card surface" rule no longer lists
      `.animeProjectStage`/`.pointsAchievementStage`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -c 'max-width: 700px\|max-width: 900px\|max-width: 520px' src/components/profile/AnimeProjectStage.module.css src/components/profile/PointsAchievementStage.module.css</automated>
  </verify>
  <done>Both standalone stage modules exist under 450 lines with their own local containers, and
  the shared cross-cutting rule no longer references them.</done>
</task>

<task type="auto">
  <name>Task 2: Extract MembershipStage.module.css and ContributionAchievementStage.module.css; update tests</name>
  <files>frontend/src/components/profile/MembershipStage.module.css, frontend/src/components/profile/ContributionAchievementStage.module.css, frontend/src/components/profile/MemberBadgeChain.module.css, frontend/src/components/profile/MemberBadgeChain.tsx, frontend/src/components/profile/MemberBadgeChain.test.tsx</files>
  <read_first>
    frontend/src/components/profile/MemberBadgeChain.module.css
    frontend/src/components/profile/MemberBadgeChain.tsx
    frontend/src/components/profile/MemberBadgeChain.test.tsx
  </read_first>
  <action>
    Grep for every `.membershipStage*`/`.membershipDurationTrack`/`.foundingMember*` selector and
    cut into `frontend/src/components/profile/MembershipStage.module.css` (~105 lines): add
    `container-type: inline-size;` (unnamed) to `.membershipStage`, convert its two device-width
    `@media` blocks (900px, 520px) to unnamed `@container`, keep `prefers-reduced-motion` as
    `@media`, add its "no card surface" single-selector copy.
    Grep for every `.contributionAchievementStage`/`.contributionStage*`/`.contributionTier*`
    selector and cut into `frontend/src/components/profile/ContributionAchievementStage.module.css`
    (~110 lines): since this component IS a `FocalCarousel` item, do NOT add a local container —
    convert its remaining `@media (max-width: 900px)` and `@media (max-width: 520px)` blocks to
    `@container member-badge-carousel (max-width: 900px)` / `(max-width: 520px)` (reusing the
    EXISTING container name declared on `.carouselShell` in the shell file — do not redeclare it
    here), and move its two ALREADY-correct `@container member-badge-carousel (...)` blocks
    verbatim (no conversion needed, they are already container queries). Keep
    `prefers-reduced-motion` as `@media`. Add its "no card surface" single-selector copy.
    In `MemberBadgeChain.tsx`, add both new imports and update every reference inside
    `MembershipStage()`/`ContributionAchievementStage()` from `chainStyles.` to the new aliases.
    In `MemberBadgeChain.module.css`, delete the moved blocks and finish narrowing the shared "no
    card surface" rule so it lists only `.roleBadgeRow` (the sole selector not yet extracted).
    In `MemberBadgeChain.test.tsx`, add `membershipStageCss`/`contributionAchievementStageCss`
    (and, if Task 1 has not already, `animeProjectStageCss`/`pointsAchievementStageCss`)
    `readFileSync` constants, and update every existing assertion referencing selectors now owned
    by these four new files to read from the correct constant — including the `@media`-locking
    assertions that must now expect `@container` (member-badge-carousel for contribution, unnamed
    for the other three) instead of `@media`, at the SAME breakpoint values as before.
  </action>
  <acceptance_criteria>
    - `MembershipStage.module.css` and `ContributionAchievementStage.module.css` each exist and are
      each under 450 lines.
    - `ContributionAchievementStage.module.css` contains `@container member-badge-carousel` and
      does NOT declare its own `container-type`/`container-name`.
    - `MembershipStage.module.css` declares its own `container-type: inline-size`.
    - `MemberBadgeChain.module.css`'s shared "no card surface" rule now lists only `.roleBadgeRow`.
    - `cd frontend && npx vitest run src/components/profile/MemberBadgeChain.test.tsx` passes with
      zero failures.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/profile/MemberBadgeChain.test.tsx</automated>
  </verify>
  <done>All four achievement-stage modules are fully extracted, container-query-driven per their
  correct container strategy, and the full badge-chain test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure CSS-module extraction and container-query conversion; no new trust boundary, no input handling change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-03 | (none applicable) | `MemberBadgeChain.module.css` / `.tsx` split | accept | Presentation-only CSS/import refactor with no new trust boundary; out of scope for STRIDE. |
</threat_model>

<verification>
`cd frontend && npx vitest run src/components/profile/MemberBadgeChain.test.tsx` green; `wc -l` on
all four new modules confirms <=450 lines; `npm run typecheck` and `npm run lint` clean.
</verification>

<success_criteria>
- Four new component-owned CSS modules exist, each <=450 lines, each using the correct
  standalone-vs-carousel-item container strategy.
- The cross-cutting "no card surface" rule no longer spans more than one component's selector.
- Full existing badge-chain test coverage still passes.
</success_criteria>

<output>
Create `.planning/phases/133-responsive-accessible-efficient-visual-delivery/133-07-SUMMARY.md` when done
</output>
