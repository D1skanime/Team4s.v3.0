---
phase: 27-segment-library-identity-and-reuse
plan: 03
type: execute
wave: 3
depends_on:
  - 27-01
  - 27-02
files_modified:
  - frontend/src/app/admin/episode-versions/[id]/edit/components/SegmenteTab.tsx
  - frontend/src/app/admin/episode-versions/[id]/edit/components/SegmenteTab.helpers.tsx
  - frontend/src/app/admin/episode-versions/[id]/edit/components/SegmentEditPanel.tsx
  - frontend/src/lib/api.ts
  - STATUS.md
  - TOMORROW.md
autonomous: true
requirements:
  - P27-SC2
  - P27-SC3
  - P27-SC5
must_haves:
  truths:
    - "The segment editor clearly separates new upload from reuse of an existing library asset."
    - "Operators can judge provenance and reuse status directly in the UI without reopening database internals."
    - "Live verification proves that delete/recreate with the same AniSearch identity no longer destroys usable OP/ED work."
  artifacts:
    - path: "frontend/src/app/admin/episode-versions/[id]/edit/components/SegmentEditPanel.tsx"
      provides: "Upload-vs-reuse control surface with provenance display"
    - path: "frontend/src/app/admin/episode-versions/[id]/edit/components/SegmenteTab.tsx"
      provides: "Release-context integration and refreshed status display"
    - path: "STATUS.md"
      provides: "Current verified commands and live verification note for Phase 27"
  key_links:
    - from: "frontend/src/app/admin/episode-versions/[id]/edit/components/SegmentEditPanel.tsx"
      to: "frontend/src/lib/api.ts"
      via: "explicit reuse candidate lookup and attach actions"
      pattern: "reuse|attach|upload"
---

<objective>
Surface reusable segment-library behavior honestly in the editor and close Phase 27 with a live reimport/delete verification loop.

Purpose: Even with backend reuse support, Phase 27 is incomplete if operators cannot tell whether they are uploading a new OP/ED file or reusing an existing library asset.
Output: Segment editor UX for upload-vs-reuse plus updated handoff docs and live verification evidence.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-segment-library-identity-and-reuse/27-CONTEXT.md
@.planning/phases/27-segment-library-identity-and-reuse/27-RESEARCH.md
@.planning/phases/27-segment-library-identity-and-reuse/27-VALIDATION.md
@frontend/src/app/admin/episode-versions/[id]/edit/components/SegmenteTab.tsx
@frontend/src/app/admin/episode-versions/[id]/edit/components/SegmenteTab.helpers.tsx
@frontend/src/app/admin/episode-versions/[id]/edit/components/SegmentEditPanel.tsx
@frontend/src/lib/api.ts
@STATUS.md
@TOMORROW.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Segment editor UX for library reuse and provenance</name>
  <files>frontend/src/app/admin/episode-versions/[id]/edit/components/SegmenteTab.tsx, frontend/src/app/admin/episode-versions/[id]/edit/components/SegmenteTab.helpers.tsx, frontend/src/app/admin/episode-versions/[id]/edit/components/SegmentEditPanel.tsx, frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/app/admin/episode-versions/[id]/edit/components/SegmenteTab.tsx
    - frontend/src/app/admin/episode-versions/[id]/edit/components/SegmentEditPanel.tsx
    - frontend/src/lib/api.ts
    - .planning/phases/27-segment-library-identity-and-reuse/27-CONTEXT.md
    - .planning/phases/27-segment-library-identity-and-reuse/27-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: The operator can choose between uploading a new asset and reusing an existing library candidate.
    - Test 2: The UI visibly distinguishes reused library assets from freshly uploaded ones.
    - Test 3: Provenance details surface stable AniSearch/group context clearly enough for later fansub self-service and troubleshooting.
  </behavior>
  <action>Extend the segment editor UI so `SegmentEditPanel` offers an explicit upload-vs-reuse decision instead of a hidden auto-choice. Show library candidates returned by the new API seam with stable AniSearch identity, fansub group context, segment kind/name, and current asset label. Render provenance/status text in the table and panel so reused assets are not visually indistinguishable from fresh uploads. Keep the established Phase-25/26 layout and do not reintroduce a separate anime-themes management page.</action>
  <acceptance_criteria>
    - `Select-String -Path frontend/src/app/admin/episode-versions/[id]/edit/components/SegmentEditPanel.tsx -Pattern "reuse|library|provenance|upload"` returns matches.
    - `Select-String -Path frontend/src/app/admin/episode-versions/[id]/edit/components/SegmenteTab.tsx -Pattern "reused|library|source"` returns matches.
    - `cd frontend && npm.cmd run build` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm.cmd run build</automated>
  </verify>
  <done>The editor makes upload-vs-reuse explicit and shows trustworthy provenance for segment assets.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Live delete/reimport verification and handoff refresh</name>
  <files>STATUS.md, TOMORROW.md</files>
  <read_first>
    - STATUS.md
    - TOMORROW.md
    - .planning/phases/27-segment-library-identity-and-reuse/27-VALIDATION.md
    - .planning/phases/27-segment-library-identity-and-reuse/27-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: Live verification documents delete-detach and same-AniSearch rebind behavior honestly.
    - Test 2: Status docs keep valid commands and name the exact next human follow-through if anything remains open.
    - Test 3: The first task in `TOMORROW.md` stays concrete and <=15 minutes.
  </behavior>
  <action>Run the planned live Phase-27 scenario on the Docker environment: attach or upload a reusable segment asset, delete the local anime, recreate or reimport the same AniSearch anime, reopen the segment editor, and confirm the reusable asset is rediscoverable or already rebound. Record the result in `.planning/phases/27-segment-library-identity-and-reuse/27-VERIFICATION.md`, then update `STATUS.md` and `TOMORROW.md` with the exact verification outcome and next narrow follow-up. If a gap remains, describe the failed boundary precisely instead of declaring the phase complete.</action>
  <acceptance_criteria>
    - `STATUS.md` contains `Phase 27` and mentions reusable segment identity or reimport verification.
    - `TOMORROW.md` first task is a concrete <=15-minute follow-up tied to the Phase-27 result.
    - `.planning/phases/27-segment-library-identity-and-reuse/27-VERIFICATION.md` records whether delete/reimport reuse passed or what exact gap remains.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./... && cd ..\frontend && npm.cmd run build</automated>
  </verify>
  <done>Phase 27 ends with honest live evidence and current handoff docs.</done>
</task>

</tasks>

<verification>
Build the frontend, then complete the live delete/reimport scenario and capture the result in the phase verification plus handoff docs.
</verification>

<success_criteria>
Operators can reuse prior segment assets visibly and the project has live evidence that delete/reimport no longer destroys reusable OP/ED work.
</success_criteria>

<output>
After completion, create `.planning/phases/27-segment-library-identity-and-reuse/27-03-SUMMARY.md`
</output>
