---
phase: 27-segment-library-identity-and-reuse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/db-schema-v2.md
  - database/migrations/0052_segment_library_identity.up.sql
  - database/migrations/0052_segment_library_identity.down.sql
  - backend/internal/models/admin_anime_themes.go
  - backend/internal/repository/admin_content_anime_themes.go
  - backend/internal/repository/admin_content_anime_delete.go
  - backend/internal/repository/media_repository.go
autonomous: true
requirements:
  - P27-SC1
  - P27-SC4
must_haves:
  truths:
    - "Reusable OP/ED segment identity is anchored on stable AniSearch plus fansub-group context instead of the transient local anime.id alone."
    - "Anime delete detaches reusable segment-library data without blindly destroying still-reusable OP/ED definitions or media assets."
    - "Schema and repository changes stay additive-first so the verified Phase-24/25/26 operator surface can migrate safely."
  artifacts:
    - path: "database/migrations/0052_segment_library_identity.up.sql"
      provides: "Additive schema for reusable segment identity and local-vs-library ownership boundaries"
    - path: "backend/internal/repository/admin_content_anime_delete.go"
      provides: "Delete boundary that detaches reusable segment library data instead of hard-deleting it"
    - path: "docs/architecture/db-schema-v2.md"
      provides: "Documented source of truth for the new segment-library identity model"
  key_links:
    - from: "backend/internal/repository/admin_content_anime_themes.go"
      to: "backend/internal/repository/admin_content_anime_delete.go"
      via: "shared ownership rules for segment definitions and assets"
      pattern: "anisearch|segment_library|detach"
---

<objective>
Define the durable data model and delete boundary for reusable segment library identity before new reuse endpoints or UI are added.

Purpose: Prevent Phase 27 from layering reuse UX on top of a schema that still treats OP/ED assets as disposable anime-local byproducts.
Output: An additive schema/migration, documented identity rules, and repository delete semantics that distinguish reusable library data from local-only remnants.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-segment-library-identity-and-reuse/27-CONTEXT.md
@.planning/phases/27-segment-library-identity-and-reuse/27-RESEARCH.md
@.planning/phases/27-segment-library-identity-and-reuse/27-VALIDATION.md
@docs/architecture/db-schema-v2.md
@backend/internal/repository/admin_content_anime_themes.go
@backend/internal/repository/admin_content_anime_delete.go
@backend/internal/repository/media_repository.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additive schema for stable segment-library identity</name>
  <files>docs/architecture/db-schema-v2.md, database/migrations/0052_segment_library_identity.up.sql, database/migrations/0052_segment_library_identity.down.sql, backend/internal/models/admin_anime_themes.go</files>
  <read_first>
    - docs/architecture/db-schema-v2.md
    - .planning/phases/27-segment-library-identity-and-reuse/27-CONTEXT.md
    - .planning/phases/27-segment-library-identity-and-reuse/27-RESEARCH.md
    - backend/internal/models/admin_anime_themes.go
    - database/migrations/0051_extend_theme_segments_source.up.sql
  </read_first>
  <behavior>
    - Test 1: The schema can represent a reusable segment definition independently of one local `anime.id`.
    - Test 2: The schema can distinguish stable anime source identity (`provider`, `external_id`) from display-only title/name data.
    - Test 3: The schema can represent whether a segment definition/asset is reusable library data or local-only fallback data awaiting cleanup.
  </behavior>
  <action>Update `docs/architecture/db-schema-v2.md` and create migration `0052_segment_library_identity` with one additive reusable-definition layer. Introduce explicit stable anime identity fields based on AniSearch provenance (`anime_source_provider`, `anime_source_external_id` or a normalized reference table), keep `fansub_group_id`, keep normalized segment kind plus optional segment name, and add a local-vs-library ownership marker such as `identity_status` or `ownership_scope`. Do not make anime titles or file names authoritative. Document one concrete uniqueness rule covering `{provider, external_id, fansub_group_id, segment kind, normalized segment name}` and one concrete fallback state for legacy rows that cannot yet prove stable identity.</action>
  <acceptance_criteria>
    - `Select-String -Path docs/architecture/db-schema-v2.md -Pattern "segment library|anime_source|external_id|ownership"` returns matches.
    - `Select-String -Path database/migrations/0052_segment_library_identity.up.sql -Pattern "CREATE TABLE|ALTER TABLE|anisearch|fansub_group_id|segment"` returns matches.
    - `Select-String -Path database/migrations/0052_segment_library_identity.up.sql -Pattern "UNIQUE|unique"` returns at least one match for the stable identity rule.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/repository -count=1</automated>
  </verify>
  <done>The DB contract can express reusable segment identity independently from transient local anime rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Narrow anime delete from destroy-everything to detach-when-reusable</name>
  <files>backend/internal/repository/admin_content_anime_delete.go, backend/internal/repository/admin_content_anime_themes.go, backend/internal/repository/media_repository.go</files>
  <read_first>
    - backend/internal/repository/admin_content_anime_delete.go
    - backend/internal/repository/admin_content_anime_themes.go
    - backend/internal/repository/media_repository.go
    - .planning/phases/27-segment-library-identity-and-reuse/27-CONTEXT.md
    - .planning/phases/27-segment-library-identity-and-reuse/27-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Anime delete removes local anime associations but preserves reusable segment definitions/assets with stable source identity.
    - Test 2: Anime delete still removes truly orphaned local-only media leftovers.
    - Test 3: Repository behavior is explicit enough that later handler/UI work can rely on detach-vs-destroy semantics without re-deriving them ad hoc.
  </behavior>
  <action>Refactor the anime delete repository path so reusable segment-library records are detached from the local anime deletion path instead of being treated like generic `/media/anime/{id}` cleanup targets. Add one explicit repository rule for library-preserving delete, one explicit rule for local-only orphan cleanup, and tests or helper seams that make the boundary auditable. Keep the current hard cleanup for anime-owned cover/banner/background assets that do not participate in the segment library contract.</action>
  <acceptance_criteria>
    - `Select-String -Path backend/internal/repository/admin_content_anime_delete.go -Pattern "detach|segment|library|ownership|orphan"` returns matches.
    - `Select-String -Path backend/internal/repository/admin_content_anime_delete.go -Pattern "media/anime/"` still returns matches for anime-owned media cleanup.
    - `cd backend && go test ./internal/repository -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/repository -count=1</automated>
  </verify>
  <done>Anime delete now distinguishes reusable segment-library data from disposable local anime media.</done>
</task>

</tasks>

<verification>
Confirm the schema doc and migration define stable segment identity, then prove repository delete behavior distinguishes detach vs destroy without breaking existing anime-owned cleanup.
</verification>

<success_criteria>
Phase 27 has a durable reusable segment identity contract and a safe delete boundary before reuse endpoints or UI are added.
</success_criteria>

<output>
After completion, create `.planning/phases/27-segment-library-identity-and-reuse/27-01-SUMMARY.md`
</output>
