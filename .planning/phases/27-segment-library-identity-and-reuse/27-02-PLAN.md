---
phase: 27-segment-library-identity-and-reuse
plan: 02
type: execute
wave: 2
depends_on:
  - 27-01
files_modified:
  - backend/internal/models/admin_anime_themes.go
  - backend/internal/repository/admin_content_anime_themes.go
  - backend/internal/repository/episode_version_repository.go
  - backend/internal/handlers/admin_content_anime_theme_segments.go
  - backend/cmd/server/admin_routes.go
  - frontend/src/types/admin.ts
  - frontend/src/lib/api.ts
autonomous: true
requirements:
  - P27-SC1
  - P27-SC2
  - P27-SC3
must_haves:
  truths:
    - "The backend can look up reusable segment definitions through stable AniSearch/group identity and return them to the current release-context editor."
    - "Operators can attach an existing segment-library asset instead of always uploading a new duplicate file."
    - "Recreated or reimported anime with the same AniSearch identity can rediscover prior segment definitions and assets."
  artifacts:
    - path: "backend/internal/repository/admin_content_anime_themes.go"
      provides: "Authoritative reusable segment-definition lookup and attach/detach persistence seam"
    - path: "backend/internal/handlers/admin_content_anime_theme_segments.go"
      provides: "HTTP/API surface for reuse lookup and attach flows"
    - path: "frontend/src/lib/api.ts"
      provides: "Typed client seam for finding and reusing existing library segment assets"
  key_links:
    - from: "backend/internal/repository/episode_version_repository.go"
      to: "backend/internal/repository/admin_content_anime_themes.go"
      via: "release-context editor resolving reusable segment-library candidates"
      pattern: "anisearch|segment|reuse"
---

<objective>
Build the backend lookup and attachment seams that let the existing segment editor reuse prior OP/ED assets through stable AniSearch identity.

Purpose: Without this wave, Phase 27 would document good ownership rules but still force operators to re-upload duplicate files after reimport or anime recreation.
Output: Repository, handler, route, and typed client support for library lookup, attach, detach, and rebind behavior.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-segment-library-identity-and-reuse/27-CONTEXT.md
@.planning/phases/27-segment-library-identity-and-reuse/27-RESEARCH.md
@.planning/phases/27-segment-library-identity-and-reuse/27-VALIDATION.md
@backend/internal/models/admin_anime_themes.go
@backend/internal/repository/admin_content_anime_themes.go
@backend/internal/repository/episode_version_repository.go
@backend/internal/handlers/admin_content_anime_theme_segments.go
@backend/cmd/server/admin_routes.go
@frontend/src/types/admin.ts
@frontend/src/lib/api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repository seam for reusable segment lookup and attachment</name>
  <files>backend/internal/models/admin_anime_themes.go, backend/internal/repository/admin_content_anime_themes.go, backend/internal/repository/episode_version_repository.go</files>
  <read_first>
    - backend/internal/models/admin_anime_themes.go
    - backend/internal/repository/admin_content_anime_themes.go
    - backend/internal/repository/episode_version_repository.go
    - .planning/phases/27-segment-library-identity-and-reuse/27-CONTEXT.md
    - .planning/phases/27-segment-library-identity-and-reuse/27-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Given a release-context editor with an AniSearch-backed anime, the repository can list reusable segment definitions/assets for the same AniSearch identity and fansub group.
    - Test 2: The repository can attach an existing reusable segment asset to the current local usage without creating a duplicate media asset row.
    - Test 3: The repository returns enough provenance metadata to distinguish fresh upload, reused asset, and local-only fallback cases.
  </behavior>
  <action>Add one authoritative repository seam for reusable segment lookup keyed by AniSearch provider/external ID plus `fansub_group_id`, segment kind, and optional segment name. Add one attachment seam that binds the chosen reusable definition/asset into the current release-context segment record without duplicating `media_assets`. Extend DTOs/models so API responses carry provenance metadata such as stable anime identity, ownership scope, and attach source. Where the episode-version editor currently derives grouped segment state, thread the reusable lookup data through the existing release-context resolver instead of inventing a disconnected admin page.</action>
  <acceptance_criteria>
    - `Select-String -Path backend/internal/repository/admin_content_anime_themes.go -Pattern "Find.*Segment|Reuse|Attach|AniSearch|external_id"` returns matches.
    - `Select-String -Path backend/internal/models/admin_anime_themes.go -Pattern "provenance|ownership|external_id|reuse"` returns matches.
    - `cd backend && go test ./internal/repository -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/repository -count=1</automated>
  </verify>
  <done>The backend can find and attach reusable segment assets through stable AniSearch/group identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Handler and API routes for reuse discovery and rebind</name>
  <files>backend/internal/handlers/admin_content_anime_theme_segments.go, backend/cmd/server/admin_routes.go, frontend/src/types/admin.ts, frontend/src/lib/api.ts</files>
  <read_first>
    - backend/internal/handlers/admin_content_anime_theme_segments.go
    - backend/cmd/server/admin_routes.go
    - frontend/src/types/admin.ts
    - frontend/src/lib/api.ts
    - .planning/phases/27-segment-library-identity-and-reuse/27-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: The client can request reusable segment candidates for the current editor context.
    - Test 2: The client can attach a chosen reusable segment asset without going through file upload.
    - Test 3: The API response is explicit enough that the frontend can render provenance and avoid confusing reuse with a new upload.
  </behavior>
  <action>Implement concrete admin routes for library reuse discovery and attach/rebind, for example one GET/POST pair under the existing segment editor route family. Return a typed payload that includes `source_type`, `source_ref`, `source_label`, stable AniSearch identity, ownership scope, and whether the asset was reused vs uploaded. Extend `frontend/src/lib/api.ts` and `frontend/src/types/admin.ts` with typed helpers for candidate lookup and attach actions; do not overload the existing upload helper with hidden magic parameters.</action>
  <acceptance_criteria>
    - `Select-String -Path backend/cmd/server/admin_routes.go -Pattern "segment.*reuse|segments.*library|segments.*attach"` returns matches.
    - `Select-String -Path frontend/src/lib/api.ts -Pattern "reuse|attach.*segment"` returns matches.
    - `cd backend && go test ./internal/handlers -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && go test ./internal/handlers -count=1</automated>
  </verify>
  <done>The API exposes explicit reusable-segment discovery and attach flows for the current editor context.</done>
</task>

</tasks>

<verification>
Prove repository and handler layers support discovery and attach for reusable segments, then ensure the typed frontend seam can consume the new contract.
</verification>

<success_criteria>
Phase 27 can rediscover and reattach reusable segment assets through the current release-context editor instead of forcing duplicate uploads.
</success_criteria>

<output>
After completion, create `.planning/phases/27-segment-library-identity-and-reuse/27-02-SUMMARY.md`
</output>
