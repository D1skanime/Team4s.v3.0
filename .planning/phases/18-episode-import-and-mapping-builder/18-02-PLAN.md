---
phase: 18-episode-import-and-mapping-builder
plan: 02
type: execute
wave: 1
depends_on:
  - 18-01
files_modified:
  - database/migrations/0043_add_episode_version_episodes.up.sql
  - database/migrations/0043_add_episode_version_episodes.down.sql
  - backend/internal/models/episode_import.go
  - backend/internal/models/episode_version.go
  - backend/internal/repository/episode_import_repository.go
  - backend/internal/repository/episode_version_repository.go
  - backend/internal/repository/admin_content_episode.go
  - backend/internal/repository/episode_import_repository_test.go
autonomous: true
requirements:
  - P18-SC4
  - P18-SC5
must_haves:
  truths:
    - "One Jellyfin media/version can authoritatively cover more than one canonical Team4s episode."
    - "`episode_versions.episode_number` remains populated as the compatibility/primary display episode."
    - "Existing manual episode titles/statuses are not overwritten by import apply."
    - "Grouped admin reads show versions under every covered canonical episode using `episode_version_episodes` first and legacy `episode_versions.episode_number` as fallback."
  artifacts:
    - path: "database/migrations/0043_add_episode_version_episodes.up.sql"
      provides: "Join table and indexes for authoritative episode coverage"
    - path: "backend/internal/repository/episode_import_repository.go"
      provides: "Transactional apply for canonical episodes, Jellyfin versions, and coverage rows"
    - path: "backend/internal/repository/episode_version_repository.go"
      provides: "Grouped read compatibility with authoritative coverage table"
  key_links:
    - from: "backend/internal/repository/episode_import_repository.go"
      to: "database/migrations/0043_add_episode_version_episodes.up.sql"
      via: "writes episode_version_episodes after upserting version"
      pattern: "episode_version_episodes"
---

<objective>
Implement the database and repository foundation for authoritative multi-episode coverage.

Purpose: Make the Naruto 9+10 class of mapping representable without creating ambiguous duplicate Jellyfin version rows.
Output: Migration 0043, transactional apply repository, and grouped read compatibility through `episode_version_episodes`.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-episode-import-and-mapping-builder/18-CONTEXT.md
@.planning/phases/18-episode-import-and-mapping-builder/18-RESEARCH.md
@.planning/phases/18-episode-import-and-mapping-builder/18-01-SUMMARY.md

Read before editing:
@database/migrations/0012_episode_versions.up.sql
@database/migrations/0033_extend_episodes_table.up.sql
@database/migrations/0042_add_tag_tables_forward_fix.up.sql
@backend/internal/models/episode_import.go
@backend/internal/models/episode_version.go
@backend/internal/repository/admin_content_episode.go
@backend/internal/repository/episode_version_repository.go
@backend/internal/repository/episode_import_repository_test.go

<interfaces>
Compatibility rule:
```text
episode_version_episodes = authoritative canonical coverage.
episode_versions.episode_number = required compatibility/display primary episode, set to the lowest confirmed covered canonical episode.
```

Repository API to create:
```go
type EpisodeImportRepository struct { db *pgxpool.Pool }
func NewEpisodeImportRepository(db *pgxpool.Pool) *EpisodeImportRepository
func (r *EpisodeImportRepository) Apply(ctx context.Context, input models.EpisodeImportApplyInput) (*models.EpisodeImportApplyResult, error)
func (r *EpisodeImportRepository) PreviewExistingCoverage(ctx context.Context, animeID int64) (models.EpisodeImportExistingCoverage, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add episode_version_episodes migration and model coverage fields</name>
  <files>database/migrations/0043_add_episode_version_episodes.up.sql, database/migrations/0043_add_episode_version_episodes.down.sql, backend/internal/models/episode_version.go, backend/internal/models/episode_import.go</files>
  <behavior>
    - `episode_version_episodes` has `episode_version_id`, `episode_id`, `coverage_order`, and `created_at`.
    - Join rows cascade when an episode version or episode is deleted.
    - Primary key is `(episode_version_id, episode_id)`.
    - Index supports lookup by `(episode_id, coverage_order, episode_version_id)`.
    - `EpisodeVersion` can expose `covered_episode_numbers` while keeping existing `episode_number`.
  </behavior>
  <action>
    Create migration `0043_add_episode_version_episodes.*.sql`. Do not make `episode_versions.episode_number` nullable. Add optional coverage fields to `models.EpisodeVersion` if needed, but keep existing JSON fields intact for compatibility. Extend `episode_import.go` with apply input/result types including counts for `episodes_created`, `episodes_existing`, `versions_created`, `versions_updated`, `mappings_applied`, `skipped`, and `conflicts`.
  </action>
  <verify>
    <automated>cd backend; go test ./internal/migrations ./internal/models</automated>
  </verify>
  <done>Migration files exist, down migration removes the table/indexes, and Go model packages compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement transactional episode import apply repository</name>
  <files>backend/internal/repository/episode_import_repository.go, backend/internal/repository/episode_import_repository_test.go</files>
  <behavior>
    - Confirmed mappings create missing `episodes` using AniSearch canonical number/title and default status `disabled`.
    - Existing episodes keep manual title/status; title is fill-only when existing title is null/empty.
    - One media mapping with targets `[9,10]` creates or updates one Jellyfin `episode_versions` row with `episode_number=9`.
    - Coverage rows for the version point to both episode IDs with deterministic `coverage_order`.
    - Skipped rows do not create versions.
    - Conflicts are rejected before any mutation commits.
  </behavior>
  <action>
    Implement `EpisodeImportRepository.Apply` as one transaction. Upsert episodes by `(anime_id, episode_number)` using fill-only title semantics from `AdminContentRepository.UpsertEpisodeByAnimeAndNumber`, then upsert one Jellyfin `episode_versions` row by `(anime_id, media_provider='jellyfin', media_item_id)`. Replace that version's coverage rows in `episode_version_episodes` with the confirmed targets. Do not duplicate one media file into multiple version rows. Return count fields in `EpisodeImportApplyResult`.
  </action>
  <verify>
    <automated>cd backend; go test ./internal/repository -run EpisodeImportApply</automated>
  </verify>
  <done>Wave 0 repository red tests pass, including multi-episode coverage and manual title preservation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update grouped version reads to prefer coverage join rows</name>
  <files>backend/internal/models/episode_version.go, backend/internal/repository/episode_version_repository.go, backend/internal/repository/admin_content_episode.go</files>
  <behavior>
    - `ListGroupedByAnimeID(... includeVersions=true ...)` shows a covered version under every canonical episode in `episode_version_episodes`.
    - Legacy rows without coverage join rows still appear under `episode_versions.episode_number`.
    - Default-version ordering remains stable by episode number, release date desc nulls last, id asc.
    - Orphan counting/deletion treats join coverage as authoritative when present and keeps the legacy numeric fallback for older rows.
  </behavior>
  <action>
    Update grouped read SQL to left join `episode_version_episodes` and `episodes`, derive `group_episode_number` from coverage when present, and fall back to `ev.episode_number` when no coverage exists. Keep each `EpisodeVersion.EpisodeNumber` as the compatibility primary/display episode and populate coverage numbers if the model field was added. Adjust orphan helpers in `admin_content_episode.go` only as needed so imported coverage rows do not look orphaned.
  </action>
  <verify>
    <automated>cd backend; go test ./internal/repository -run "EpisodeImportApply|ListGroupedByAnimeID|OrphanedEpisodes"</automated>
  </verify>
  <done>Grouped reads and orphan helpers use authoritative coverage without breaking legacy episode version rows.</done>
</task>

</tasks>

<verification>
Run:
```powershell
cd backend; go test ./internal/repository ./internal/models
```
</verification>

<success_criteria>
- `episode_version_episodes` is the authoritative coverage table.
- `episode_versions.episode_number` remains required and stores the lowest covered canonical episode as compatibility/primary display.
- The Wave 0 repository tests are green.
- No public playback route behavior is redesigned; read compatibility stays limited to grouped/admin reads and orphan helpers needed for correctness.
</success_criteria>

<output>
After completion, create `.planning/phases/18-episode-import-and-mapping-builder/18-02-SUMMARY.md`.
</output>
