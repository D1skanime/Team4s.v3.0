---
phase: 18-episode-import-and-mapping-builder
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - backend/internal/models/episode_import.go
  - backend/internal/services/anisearch_episode_import_test.go
  - backend/internal/repository/episode_import_repository_test.go
  - backend/internal/handlers/admin_episode_import_test.go
  - frontend/src/types/episodeImport.ts
  - frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.ts
  - frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.test.ts
autonomous: true
requirements:
  - P18-SC1
  - P18-SC2
  - P18-SC3
  - P18-SC4
  - P18-SC5
must_haves:
  truths:
    - "AniSearch canonical episode rows have a typed backend and frontend contract before implementation."
    - "Jellyfin file candidates are represented separately from canonical episode rows."
    - "Mapping rows can target multiple canonical episode numbers for one media item."
    - "Preview/apply behavior is covered by red tests before repository, handler, and UI code is implemented."
  artifacts:
    - path: "backend/internal/models/episode_import.go"
      provides: "Backend DTOs for canonical episodes, media candidates, mapping rows, preview results, and apply results"
    - path: "frontend/src/types/episodeImport.ts"
      provides: "Frontend DTOs matching backend JSON"
  key_links:
    - from: "backend/internal/models/episode_import.go"
      to: "frontend/src/types/episodeImport.ts"
      via: "matching snake_case JSON fields"
      pattern: "target_episode_numbers"
---

<objective>
Create the Wave 0 test and contract slice for Phase 18.

Purpose: Lock the product decisions into executable tests so later waves cannot collapse AniSearch canonical episodes and Jellyfin file candidates into the same identity.
Output: Backend/frontend DTOs plus red tests for AniSearch episode import, Jellyfin media candidates, manual preview/apply, and multi-episode file mappings.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-episode-import-and-mapping-builder/18-CONTEXT.md
@.planning/phases/18-episode-import-and-mapping-builder/18-RESEARCH.md

Read these implementation references before editing:
@backend/internal/models/episode.go
@backend/internal/models/episode_version.go
@backend/internal/models/admin_content.go
@backend/internal/services/anisearch_client.go
@backend/internal/repository/admin_content_episode.go
@backend/internal/repository/episode_version_repository.go
@backend/internal/handlers/jellyfin_episode_sync.go
@backend/internal/handlers/admin_content_episode_version_editor_scan.go
@frontend/src/types/anime.ts
@frontend/src/types/episodeVersion.ts

<interfaces>
Create `backend/internal/models/episode_import.go` with DTOs for:
- `EpisodeImportCanonicalEpisode`: `episode_number`, `title`, optional `anisearch_episode_id`, `existing_episode_id`, `existing_title`.
- `EpisodeImportMediaCandidate`: `media_item_id`, `file_name`, `path`, optional `jellyfin_season_number`, `jellyfin_episode_number`, `stream_url`, `video_quality`.
- `EpisodeImportMappingRow`: `media_item_id`, `target_episode_numbers`, `suggested_episode_numbers`, and `status`.
- `EpisodeImportPreviewResult`, `EpisodeImportApplyInput`, and `EpisodeImportApplyResult`.

Status values are exactly `suggested`, `confirmed`, `conflict`, and `skipped`. Jellyfin fields are evidence only and must not redefine canonical episode numbers.
</interfaces>
</context>

<feature>
  <name>Episode import contract and red tests</name>
  <files>backend/internal/models/episode_import.go, backend/internal/services/anisearch_episode_import_test.go, backend/internal/repository/episode_import_repository_test.go, backend/internal/handlers/admin_episode_import_test.go, frontend/src/types/episodeImport.ts, frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.ts, frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.test.ts</files>
  <behavior>
    - AniSearch parser fixture returns canonical episode numbers and titles independent of Jellyfin season/index fields.
    - Repository apply test expects one `episode_versions` row with primary/display `episode_number=9` and two `episode_version_episodes` rows for canonical episodes 9 and 10.
    - Repository apply test expects existing manual episode titles to survive import when AniSearch provides a title for the same number.
    - Handler preview test expects media candidates with Jellyfin season/episode/path/media fields and canonical rows in separate arrays.
    - Frontend helper test expects a row edited from `[9]` to `[9,10]` to become `confirmed` without duplicating the media candidate.
    - Frontend helper test expects conflicts when two non-skipped rows claim the same canonical episode number from different media items.
  </behavior>
  <implementation>Create only contract skeletons, pure helper skeletons, fixtures, and tests in this wave. The backend repository, handler, and service tests may be intentionally red against missing methods; use explicit test names that later plans can target. Do not add production persistence or route behavior here.</implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: Add backend import DTOs and red service/repository/handler tests</name>
  <files>backend/internal/models/episode_import.go, backend/internal/services/anisearch_episode_import_test.go, backend/internal/repository/episode_import_repository_test.go, backend/internal/handlers/admin_episode_import_test.go</files>
  <action>
    Add the backend DTO skeleton from the interfaces block. Then add red tests named:
    - `TestParseAniSearchEpisodeListHTML_ReturnsCanonicalEpisodes`
    - `TestEpisodeImportApply_CreatesCoverageJoinRowsForMultiEpisodeMedia`
    - `TestEpisodeImportApply_PreservesExistingManualEpisodeTitle`
    - `TestPreviewEpisodeImport_SeparatesCanonicalEpisodesAndMediaCandidates`
    - `TestApplyEpisodeImport_RejectsUnconfirmedConflicts`

    The tests must encode locked decisions from 18-CONTEXT: AniSearch canonical episodes, Jellyfin media candidates only, manual preview/apply, and one media file can map to multiple canonical episodes. Use small inline fixtures/stubs; do not hit live AniSearch, Jellyfin, or Postgres in this wave.
  </action>
  <verify>
    <automated>cd backend; go test ./internal/services ./internal/repository ./internal/handlers -run "^$"; if ($LASTEXITCODE -ne 0) { throw "backend packages must compile before expected-red contract tests are accepted" }; $out = go test ./internal/services ./internal/repository ./internal/handlers -run "TestParseAniSearchEpisodeListHTML_ReturnsCanonicalEpisodes|TestEpisodeImportApply_CreatesCoverageJoinRowsForMultiEpisodeMedia|TestEpisodeImportApply_PreservesExistingManualEpisodeTitle|TestPreviewEpisodeImport_SeparatesCanonicalEpisodesAndMediaCandidates|TestApplyEpisodeImport_RejectsUnconfirmedConflicts" -v 2>&1; $code = $LASTEXITCODE; $text = $out -join "`n"; if ($code -eq 0) { throw "expected Wave 0 backend contract tests to be red before implementation" }; if ($text -match "build failed|undefined:|cannot find package|setup failed") { throw "expected-red backend tests must fail by assertion/not-implemented, not compile/setup errors" }; if ($text -notmatch "TestParseAniSearchEpisodeListHTML_ReturnsCanonicalEpisodes") { throw "expected backend contract test names were not executed" }; exit 0</automated>
  </verify>
  <done>Backend packages compile first, then the named red tests fail only by assertion or explicit not-implemented behavior because Phase 18 parser/repository/handler implementation is not present yet.</done>
</task>

<task type="auto">
  <name>Task 2: Add frontend import DTOs, mapping helper seam, and red helper tests</name>
  <files>frontend/src/types/episodeImport.ts, frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.ts, frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.test.ts</files>
  <action>
    Add TypeScript interfaces matching the backend DTO JSON exactly. Add a pure helper module for mapping operations that later UI code will call: `setMappingTargets`, `markMappingSkipped`, `detectMappingConflicts`, and `summarizeImportPreview`. Add red Vitest tests proving multi-target mapping, skipped rows, and conflict detection. Do not build UI in this wave.
  </action>
  <verify>
    <automated>cd frontend; npx tsc --noEmit --pretty false; if ($LASTEXITCODE -ne 0) { throw "frontend TypeScript must compile before expected-red mapping tests are accepted" }; $out = npm test -- episodeImportMapping --run --reporter verbose 2>&1; $code = $LASTEXITCODE; $text = $out -join "`n"; if ($code -eq 0) { throw "expected Wave 0 frontend mapping tests to be red before implementation" }; if ($text -match "Transform failed|Cannot find module|TS[0-9]{4}|SyntaxError") { throw "expected-red frontend tests must fail by assertion/not-implemented, not compile/import errors" }; if ($text -notmatch "episodeImportMapping") { throw "expected frontend mapping test file was not executed" }; exit 0</automated>
  </verify>
  <done>Frontend TypeScript compiles first, then the mapping helper red tests describe the mapping-builder behavior without depending on a route component.</done>
</task>

</tasks>

<verification>
Wave 0 is complete when both automated commands return success by confirming expected red tests. Later waves are responsible for turning these tests green.
</verification>

<success_criteria>
- Phase 18 contracts contain separate canonical episode, media candidate, mapping row, preview, and apply result DTOs.
- Red tests cover all five roadmap success criteria and all locked decisions from 18-CONTEXT.
- No public playback redesign, Anime Edit redesign, non-anime support, or automatic season-offset learning is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/18-episode-import-and-mapping-builder/18-01-SUMMARY.md`.
</output>
