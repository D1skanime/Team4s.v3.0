---
phase: 25-segmente-ui-mockup-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/repository/admin_content_anime_themes.go
  - backend/internal/handlers/admin_content_segments.go
  - backend/cmd/server/admin_routes.go
  - frontend/src/types/admin.ts
  - frontend/src/lib/api.ts
autonomous: true
requirements: [P25-SC3, P25-SC5]

must_haves:
  truths:
    - "GET /api/v1/admin/anime/:animeId/segments/suggestions liefert Segmente aus anderen Releases desselben Anime fuer Episode N"
    - "GET /api/v1/admin/jellyfin/items gibt Video/Audio-Items vom Jellyfin-Server zurueck, gefiltert nach Suchbegriff"
    - "Beide Endpunkte erfordern Auth und sind in admin_routes.go registriert"
    - "Frontend hat API-Helfer und Typen fuer beide neuen Endpunkte"
  artifacts:
    - path: "backend/internal/handlers/admin_content_segments.go"
      provides: "GetAnimeSegmentSuggestions + SearchJellyfinItems Handler"
      exports: ["GetAnimeSegmentSuggestions", "SearchJellyfinItems"]
    - path: "backend/internal/repository/admin_content_anime_themes.go"
      provides: "ListAnimeSegmentSuggestions SQL-Methode"
      contains: "ListAnimeSegmentSuggestions"
    - path: "frontend/src/types/admin.ts"
      provides: "JellyfinItemResult, AdminSegmentSuggestionsResponse"
    - path: "frontend/src/lib/api.ts"
      provides: "getAnimeSegmentSuggestions, searchJellyfinItems"
  key_links:
    - from: "backend/cmd/server/admin_routes.go"
      to: "handlers.AdminContentHandler"
      via: "GET /admin/anime/:id/segments/suggestions + GET /admin/jellyfin/items"
      pattern: "segments/suggestions|jellyfin/items"
    - from: "frontend/src/lib/api.ts"
      to: "/api/v1/admin/anime/:animeId/segments/suggestions"
      via: "fetch in getAnimeSegmentSuggestions"
      pattern: "segments/suggestions"
---

<objective>
Zwei neue Backend-Endpunkte und ihre Frontend-Gegenstuecke fuer das Vorschläge-System und die Jellyfin-Item-Suche.

Purpose: Phase 25 Frontend-Tasks (Plan 02) benoetigen diese Endpunkte — ohne sie koennen Vorschlaege-Leiste und JellyfinItemPicker nicht funktionieren.
Output: Zwei neue GET-Routen, eine neue Repository-Methode, neue TS-Typen und API-Helfer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-segmente-ui-mockup-alignment/25-CONTEXT.md

@backend/internal/repository/admin_content_anime_themes.go
@backend/internal/handlers/admin_content_handler.go
@backend/internal/handlers/jellyfin_client.go
@backend/cmd/server/admin_routes.go
@frontend/src/types/admin.ts
@frontend/src/lib/api.ts

<interfaces>
<!-- Bestehende Repository-Methode fuer Segmente — Suggestions-Methode wird nach demselben Muster angelegt -->
From backend/internal/repository/admin_content_anime_themes.go:
```go
func (r *AdminContentRepository) ListAnimeSegments(
    ctx context.Context,
    animeID int64,
    groupID int64,   // 0 = kein Filter
    version string,  // "" = kein Filter
) ([]models.AdminThemeSegment, error)
```

<!-- Bestehende Jellyfin-Infrastruktur — SearchJellyfinItems nutzt fetchJellyfinJSON direkt -->
From backend/internal/handlers/jellyfin_client.go:
```go
func (h *AdminContentHandler) fetchJellyfinJSON(
    ctx context.Context,
    apiPath string,
    query url.Values,
    target any,
) (int, error)

type jellyfinSeriesListResponse struct {
    Items []jellyfinSeriesItem `json:"Items"`
}
```

<!-- Handler-Struct-Felder (jellyfinBaseURL, jellyfinAPIKey bereits vorhanden) -->
From backend/internal/handlers/admin_content_handler.go:
```go
type AdminContentHandler struct {
    // jellyfinBaseURL, jellyfinAPIKey, httpClient, jellyfinAllowedLibraryIDs
    // themeRepo adminThemeRepository
    // ...
}
```

<!-- Bestehende Segment-Typen im Frontend -->
From frontend/src/types/admin.ts:
```typescript
export interface AdminThemeSegment {
  id: number
  theme_id: number
  anime_id: number
  theme_title: string | null
  theme_type_name: string
  fansub_group_id: number | null
  version: string
  start_episode: number | null
  end_episode: number | null
  start_time: string | null
  end_time: string | null
  source_jellyfin_item_id: string | null
  created_at: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repository-Methode ListAnimeSegmentSuggestions</name>
  <files>backend/internal/repository/admin_content_anime_themes.go</files>
  <behavior>
    - ListAnimeSegmentSuggestions(ctx, animeID, episodeNumber, excludeGroupID, excludeVersion) gibt Segmente zurueck deren Episodenbereich episodeNumber enthaelt, exklusive der aktuellen (excludeGroupID, excludeVersion)-Kombination
    - groupID=0 und version="" in excludeGroupID/excludeVersion bedeutet: nichts ausschliessen
    - Bei animeID<=0 wird ErrNotFound zurueckgegeben
    - Ergebnisse sind nach ts.id geordnet
  </behavior>
  <action>
Fuege am Ende von `backend/internal/repository/admin_content_anime_themes.go` eine neue Methode hinzu:

```go
// ListAnimeSegmentSuggestions gibt Segmente desselben Anime aus ANDEREN Releases zurueck,
// die episodeNumber abdecken (start_episode <= episodeNumber <= end_episode).
// excludeGroupID und excludeVersion definieren die aktuelle Release — diese wird ausgeschlossen.
func (r *AdminContentRepository) ListAnimeSegmentSuggestions(
    ctx context.Context,
    animeID int64,
    episodeNumber int,
    excludeGroupID int64,
    excludeVersion string,
) ([]models.AdminThemeSegment, error) {
```

SQL: Wie ListAnimeSegments, aber mit zusaetzlicher WHERE-Bedingung:
- `AND ts.start_episode <= $episodeNumber AND ts.end_episode >= $episodeNumber`
- Wenn excludeGroupID > 0: `AND NOT (ts.fansub_group_id = $excludeGroupID AND ts.version = $excludeVersion)`

Datei bleibt unter 450 Zeilen — pruefe die Zeilenanzahl nach dem Einfuegen.

Fuege ausserdem die Methode zur `adminThemeRepository`-Interface-Definition in `admin_content_handler.go` hinzu:
```go
ListAnimeSegmentSuggestions(ctx context.Context, animeID int64, episodeNumber int, excludeGroupID int64, excludeVersion string) ([]models.AdminThemeSegment, error)
```
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go build ./...</automated>
  </verify>
  <done>go build laeuft fehlerfrei; ListAnimeSegmentSuggestions ist im Interface und in der Implementierung vorhanden</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Handler GetAnimeSegmentSuggestions + SearchJellyfinItems</name>
  <files>backend/internal/handlers/admin_content_segments.go, backend/cmd/server/admin_routes.go</files>
  <behavior>
    - GetAnimeSegmentSuggestions: GET-Query-Params episode (int, Pflicht), exclude_group_id (int, optional), exclude_version (string, optional). Gibt JSON {data: AdminThemeSegment[]} zurueck. Bei fehlendem episode-Param: 400.
    - SearchJellyfinItems: GET-Query-Param q (string, Pflicht, min 1 Zeichen). Nutzt fetchJellyfinJSON mit IncludeItemTypes=Video,Audio, Recursive=true, SearchTerm=q, Limit=20. Gibt JSON {data: [{id, name, type, run_time_ticks}]} zurueck. Bei fehlendem q: 400. Bei nicht konfiguriertem Jellyfin: 503.
    - Beide Endpunkte benoetigen Auth (in admin_routes.go registriert)
  </behavior>
  <action>
Erstelle neue Datei `backend/internal/handlers/admin_content_segments.go` mit zwei Handler-Methoden:

**GetAnimeSegmentSuggestions** (Methode von *AdminContentHandler):
- Parse `:id` (animeID) aus URL-Param
- Parse `episode` aus Query (Pflicht, positiver Integer)
- Parse `exclude_group_id` und `exclude_version` aus Query (optional, defaults 0/"")
- Rufe `h.themeRepo.ListAnimeSegmentSuggestions(ctx, animeID, episodeNumber, excludeGroupID, excludeVersion)` auf
- Gib `gin.H{"data": segments}` mit 200 zurueck

**SearchJellyfinItems** (Methode von *AdminContentHandler):
- Parse `q` aus Query (Pflicht, min 1 Zeichen)
- Definiere lokale Response-Struct:
  ```go
  type jellyfinItemSearchResponse struct {
      Items []jellyfinItemResult `json:"Items"`
  }
  type jellyfinItemResult struct {
      ID           string `json:"Id"`
      Name         string `json:"Name"`
      Type         string `json:"Type"`
      RunTimeTicks *int64 `json:"RunTimeTicks"`
  }
  ```
- Rufe `h.fetchJellyfinJSON` mit `/Items`, `IncludeItemTypes=Video,Audio`, `Recursive=true`, `SearchTerm=q`, `Limit=20` auf
- Mappe Ergebnisse auf `[]{id, name, type, run_time_ticks}` und gib `gin.H{"data": items}` zurueck

In `backend/cmd/server/admin_routes.go` zwei Routen hinzufuegen:
```go
v1.GET("/admin/anime/:id/segments/suggestions", auth, deps.adminContentHandler.GetAnimeSegmentSuggestions)
v1.GET("/admin/jellyfin/items", auth, deps.adminContentHandler.SearchJellyfinItems)
```

Pruefe dass `admin_content_segments.go` unter 150 Zeilen bleibt.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go build ./...</automated>
  </verify>
  <done>go build fehlerfrei; beide Endpunkte sind im Router registriert; `curl` gegen /admin/anime/1/segments/suggestions?episode=3 gibt 200 oder 401 zurueck (kein 404)</done>
</task>

<task type="auto">
  <name>Task 3: Frontend-Typen und API-Helfer</name>
  <files>frontend/src/types/admin.ts, frontend/src/lib/api.ts</files>
  <action>
In `frontend/src/types/admin.ts` am Ende der Datei hinzufuegen:

```typescript
// --- Phase 25: Jellyfin Item Search ---

/** Ein Jellyfin-Item-Suchergebnis (Video oder Audio). */
export interface JellyfinItemResult {
  id: string
  name: string
  type: string            // "Video" | "Audio" | ...
  run_time_ticks: number | null  // Jellyfin RunTimeTicks (10.000 Ticks = 1ms)
}

/** API-Response fuer Jellyfin-Item-Suche. */
export interface AdminJellyfinItemsResponse {
  data: JellyfinItemResult[]
}

/** API-Response fuer Segment-Vorschlaege. */
export interface AdminSegmentSuggestionsResponse {
  data: AdminThemeSegment[]
}
```

In `frontend/src/lib/api.ts` nach dem Segment-Block (nach `deleteAnimeSegment`) hinzufuegen:

```typescript
// --- Phase 25: Segment-Vorschlaege + Jellyfin-Item-Suche ---

export async function getAnimeSegmentSuggestions(
  animeId: number,
  episode: number,
  excludeGroupId?: number | null,
  excludeVersion?: string | null,
  authToken?: string,
): Promise<AdminSegmentSuggestionsResponse> {
  const API_BASE_URL = getApiBaseUrl()
  const params = new URLSearchParams()
  params.set('episode', String(episode))
  if (excludeGroupId) params.set('exclude_group_id', String(excludeGroupId))
  if (excludeVersion) params.set('exclude_version', excludeVersion)
  const response = await fetch(
    `${API_BASE_URL}/api/v1/admin/anime/${animeId}/segments/suggestions?${params.toString()}`,
    { headers: withAuthHeader({}, authToken), cache: 'no-store' }
  )
  if (!response.ok) {
    const parsed = await parseApiErrorPayload(response, `API request failed: ${response.status}`)
    throw new ApiError(response.status, parsed.message, null, parsed.code, parsed.details)
  }
  return response.json() as Promise<AdminSegmentSuggestionsResponse>
}

export async function searchJellyfinItems(
  q: string,
  authToken?: string,
): Promise<AdminJellyfinItemsResponse> {
  const API_BASE_URL = getApiBaseUrl()
  const params = new URLSearchParams({ q })
  const response = await fetch(
    `${API_BASE_URL}/api/v1/admin/jellyfin/items?${params.toString()}`,
    { headers: withAuthHeader({}, authToken), cache: 'no-store' }
  )
  if (!response.ok) {
    const parsed = await parseApiErrorPayload(response, `API request failed: ${response.status}`)
    throw new ApiError(response.status, parsed.message, null, parsed.code, parsed.details)
  }
  return response.json() as Promise<AdminJellyfinItemsResponse>
}
```

Importiere `AdminSegmentSuggestionsResponse` und `AdminJellyfinItemsResponse` im Import-Block von `api.ts` (bei den `@/types/admin`-Importen).
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript-Pruefung ohne Fehler; beide Helfer sind exportiert und importierbar</done>
</task>

</tasks>

<verification>
- `cd backend && go build ./...` laeuft ohne Fehler
- `cd frontend && npx tsc --noEmit` laeuft ohne Fehler
- `grep -n "segments/suggestions\|jellyfin/items" backend/cmd/server/admin_routes.go` zeigt beide Routen
- `grep -n "getAnimeSegmentSuggestions\|searchJellyfinItems" frontend/src/lib/api.ts` zeigt beide Helfer
</verification>

<success_criteria>
- Zwei neue Backend-Routen registriert und erreichbar
- Repository-Methode korrekt implementiert (schliesst aktuelle Release aus)
- Frontend-Typen und API-Helfer sind typsicher und exportiert
- Kein Build-Fehler in Backend oder Frontend
</success_criteria>

<output>
Nach Abschluss `.planning/phases/25-segmente-ui-mockup-alignment/25-01-SUMMARY.md` erstellen.
</output>
