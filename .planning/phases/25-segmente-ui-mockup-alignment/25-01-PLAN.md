---
phase: 25-segmente-ui-mockup-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/repository/admin_content_anime_themes.go
  - backend/internal/handlers/admin_content_segments.go
  - backend/cmd/server/admin_routes.go
  - frontend/src/types/admin.ts
  - frontend/src/lib/api.ts
autonomous: true
requirements: [P25-SC3, P25-SC5]

must_haves:
  truths:
    - "GET /api/v1/admin/anime/:animeId/segments/suggestions liefert Segmente aus anderen Releases desselben Anime fuer Episode N"
    - "Segment-API und Frontend-Typen koennen explizite source_type/source_ref/source_label Werte tragen"
    - "Die aktuelle Release-Kombination bleibt klar ueber group_id + version filterbar"
    - "Alle geaenderten Endpunkte und Typen bleiben rueckwaertskompatibel genug fuer den bestehenden Segmente-Flow"
  artifacts:
    - path: "backend/internal/handlers/admin_content_segments.go"
      provides: "GetAnimeSegmentSuggestions Handler"
      exports: ["GetAnimeSegmentSuggestions"]
    - path: "backend/internal/repository/admin_content_anime_themes.go"
      provides: "ListAnimeSegmentSuggestions SQL-Methode"
      contains: "ListAnimeSegmentSuggestions"
    - path: "frontend/src/types/admin.ts"
      provides: "AdminSegmentSuggestionsResponse plus Segment-Source-Typen"
    - path: "frontend/src/lib/api.ts"
      provides: "getAnimeSegmentSuggestions plus Segment-Source-Contract"
  key_links:
    - from: "backend/cmd/server/admin_routes.go"
      to: "handlers.AdminContentHandler"
      via: "GET /admin/anime/:id/segments/suggestions"
      pattern: "segments/suggestions"
    - from: "frontend/src/lib/api.ts"
      to: "/api/v1/admin/anime/:animeId/segments/suggestions"
      via: "fetch in getAnimeSegmentSuggestions"
      pattern: "segments/suggestions"
---

<objective>
Backend- und Typengrundlage fuer den korrigierten Segmente-Slice: Vorschlaege-System und explizites Quellenmodell ohne vorgezogenen Jellyfin-Picker.

Purpose: Phase 25 Frontend-Tasks brauchen Suggestions und einen sauberen Segment-Source-Contract, aber keinen freien Jellyfin-Search-Endpunkt als Pflicht.
Output: Suggestions-Route, Repository-Methode, erweiterte Segment-Typen und API-Helfer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-segmente-ui-mockup-alignment/25-CONTEXT.md

@backend/internal/repository/admin_content_anime_themes.go
@backend/internal/handlers/admin_content_handler.go
@backend/cmd/server/admin_routes.go
@frontend/src/types/admin.ts
@frontend/src/lib/api.ts

<interfaces>
From backend/internal/repository/admin_content_anime_themes.go:
```go
func (r *AdminContentRepository) ListAnimeSegments(
    ctx context.Context,
    animeID int64,
    groupID int64,
    version string,
) ([]models.AdminThemeSegment, error)
```

From backend/internal/handlers/admin_content_handler.go:
```go
type AdminContentHandler struct {
    // themeRepo adminThemeRepository
    // ...
}
```

From frontend/src/types/admin.ts:
```typescript
export interface AdminThemeSegment {
  id: number
  theme_id: number
  anime_id: number
  theme_title: string | null
  theme_type_name: string
  fansub_group_id: number | null
  version: string
  start_episode: number | null
  end_episode: number | null
  start_time: string | null
  end_time: string | null
  source_type?: 'none' | 'jellyfin_theme' | 'release_asset' | null
  source_ref?: string | null
  source_label?: string | null
  source_jellyfin_item_id: string | null
  created_at: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repository-Methode ListAnimeSegmentSuggestions</name>
  <files>backend/internal/repository/admin_content_anime_themes.go</files>
  <behavior>
    - ListAnimeSegmentSuggestions(ctx, animeID, episodeNumber, excludeGroupID, excludeVersion) gibt Segmente zurueck deren Episodenbereich episodeNumber enthaelt, exklusive der aktuellen (excludeGroupID, excludeVersion)-Kombination
    - groupID=0 und version="" in excludeGroupID/excludeVersion bedeutet: nichts ausschliessen
    - Bei animeID<=0 wird ErrNotFound zurueckgegeben
    - Ergebnisse sind nach ts.id geordnet
  </behavior>
  <action>
Fuege am Ende von `backend/internal/repository/admin_content_anime_themes.go` eine neue Methode hinzu:

```go
func (r *AdminContentRepository) ListAnimeSegmentSuggestions(
    ctx context.Context,
    animeID int64,
    episodeNumber int,
    excludeGroupID int64,
    excludeVersion string,
) ([]models.AdminThemeSegment, error)
```

SQL:
- wie `ListAnimeSegments`
- zusaetzlich `start_episode <= episodeNumber <= end_episode`
- aktuelle Kombination `(fansub_group_id, version)` wird ausgeschlossen

Fuege ausserdem die Methode zur `adminThemeRepository`-Interface-Definition in `admin_content_handler.go` hinzu.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go build ./...</automated>
  </verify>
  <done>go build laeuft fehlerfrei; ListAnimeSegmentSuggestions ist im Interface und in der Implementierung vorhanden</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Handler GetAnimeSegmentSuggestions</name>
  <files>backend/internal/handlers/admin_content_segments.go, backend/cmd/server/admin_routes.go</files>
  <behavior>
    - GetAnimeSegmentSuggestions: GET-Query-Params episode (int, Pflicht), exclude_group_id (int, optional), exclude_version (string, optional)
    - Gibt JSON {data: AdminThemeSegment[]} zurueck
    - Bei fehlendem episode-Param: 400
    - Route benoetigt Auth und ist in admin_routes.go registriert
  </behavior>
  <action>
Erstelle oder passe `backend/internal/handlers/admin_content_segments.go` so an, dass `GetAnimeSegmentSuggestions` vorhanden ist:

- Parse `:id` als animeID
- Parse `episode` als Pflichtparameter
- Parse `exclude_group_id` und `exclude_version` optional
- Rufe `h.themeRepo.ListAnimeSegmentSuggestions(...)` auf
- Gib `gin.H{"data": segments}` zurueck

In `backend/cmd/server/admin_routes.go` die Route registrieren:

```go
v1.GET("/admin/anime/:id/segments/suggestions", auth, deps.adminContentHandler.GetAnimeSegmentSuggestions)
```
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go build ./...</automated>
  </verify>
  <done>go build fehlerfrei; Suggestions-Route ist im Router registriert</done>
</task>

<task type="auto">
  <name>Task 3: Frontend-Typen und API-Helfer fuer Suggestions und Source-Contract</name>
  <files>frontend/src/types/admin.ts, frontend/src/lib/api.ts</files>
  <action>
In `frontend/src/types/admin.ts` erweitern:

```typescript
export type AdminSegmentSourceType = 'none' | 'jellyfin_theme' | 'release_asset'
```

Segment-Typen so vorbereiten, dass `source_type`, `source_ref` und `source_label` sauber getragen werden koennen.

In `frontend/src/lib/api.ts` nach dem Segment-Block hinzufuegen:

```typescript
export async function getAnimeSegmentSuggestions(
  animeId: number,
  episode: number,
  excludeGroupId?: number | null,
  excludeVersion?: string | null,
  authToken?: string,
): Promise<AdminSegmentSuggestionsResponse>
```

Wichtig:
- kein freier `searchJellyfinItems`-Helfer als Pflicht fuer Phase 25
- Segment-Requests und Responses duerfen fuer Rueckwaertskompatibilitaet `source_jellyfin_item_id` temporaer weitertragen, aber das neue Frontend soll mit `source_type/source_ref/source_label` arbeiten koennen
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript-Pruefung ohne Fehler; Suggestions-Helfer und Segment-Source-Typen sind exportiert und importierbar</done>
</task>

</tasks>

<verification>
- `cd backend && go build ./...` laeuft ohne Fehler
- `cd frontend && npx tsc --noEmit` laeuft ohne Fehler
- `grep -n "segments/suggestions" backend/cmd/server/admin_routes.go` zeigt die Route
- `grep -n "getAnimeSegmentSuggestions" frontend/src/lib/api.ts` zeigt den Helfer
</verification>

<success_criteria>
- Suggestions-Route registriert und erreichbar
- Repository-Methode korrekt implementiert
- Frontend-Typen und API-Helfer sind typsicher und exportiert
- Segment-Source-Contract ist fuer `none|jellyfin_theme|release_asset` vorbereitet
- Kein Build-Fehler in Backend oder Frontend
</success_criteria>

<output>
Nach Abschluss `.planning/phases/25-segmente-ui-mockup-alignment/25-01-SUMMARY.md` erstellen.
</output>
