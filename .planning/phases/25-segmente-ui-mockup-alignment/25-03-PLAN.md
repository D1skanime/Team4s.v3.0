---
phase: 25-segmente-ui-mockup-alignment
plan: 03
type: execute
wave: 3
depends_on: [25-01, 25-02]
files_modified:
  - frontend/src/app/admin/episode-versions/[versionId]/edit/JellyfinItemPicker.test.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.test.tsx
autonomous: false
requirements: [P25-SC1, P25-SC2, P25-SC3, P25-SC4, P25-SC5]

must_haves:
  truths:
    - "Unit-Tests fuer JellyfinItemPicker und SegmenteTab (Hilfsfunktionen) laufen durch"
    - "Admin kann die Segmente-Seite im Browser oeffnen und alle 5 Tabs wechseln"
    - "Breadcrumb zeigt korrekten Pfad"
    - "Dreipunkt-Menue zeigt 'Loeschen' Option"
    - "Vorschlaege-Leiste erscheint wenn andere Releases Segmente haben"
    - "JellyfinItemPicker oeffnet sich und erlaubt Suche"
  artifacts:
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/JellyfinItemPicker.test.tsx"
      provides: "Tests fuer formatRunTimeTicks und Render-Verhalten"
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.test.tsx"
      provides: "Tests fuer calcDuration, Episoden-Format, Badge-Logik"
---

<objective>
Tests fuer Phase-25-Hilfsfunktionen und menschliche UAT-Verifikation der Mockup-Alignment-Aenderungen.

Purpose: Sicherstellt dass Hilfsfunktionen korrekt sind und der Admin die neue UI im Browser bestaetigt.
Output: Zwei Test-Dateien; Human-Freigabe der Seite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-segmente-ui-mockup-alignment/25-CONTEXT.md
@.planning/phases/25-segmente-ui-mockup-alignment/25-02-SUMMARY.md

@frontend/src/app/admin/episode-versions/[versionId]/edit/JellyfinItemPicker.tsx
@frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit-Tests fuer Hilfsfunktionen</name>
  <files>
    frontend/src/app/admin/episode-versions/[versionId]/edit/JellyfinItemPicker.test.tsx,
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.test.tsx
  </files>
  <behavior>
    JellyfinItemPicker formatRunTimeTicks:
    - formatRunTimeTicks(null) === "—"
    - formatRunTimeTicks(0) === "00:00"
    - formatRunTimeTicks(600_000_000) === "01:00" (60 Sekunden)
    - formatRunTimeTicks(36_000_000_000) === "1:00:00" (3600 Sekunden)
    - formatRunTimeTicks(90_000_000) === "01:30" (90 Sekunden)

    SegmenteTab calcDuration / Episoden-Anzeige:
    - Einzelepisode: start_episode === end_episode → nur "3" anzeigen, nicht "3 – 3"
    - calcDuration("00:00:30", "00:01:45") === "(01:15)"
    - calcDuration("00:00:00", "00:00:00") === "(00:00)"
    - Badge-Label: getTypeBadgeLabel("OP1") === "OP", getTypeBadgeLabel("ED2") === "ED", getTypeBadgeLabel("Insert") === "IN"
  </behavior>
  <action>
Da die Hilfsfunktionen in den Komponenten-Dateien definiert sind, exportiere sie temporaer fuer Tests ODER kopiere die Logik in die Test-Dateien als lokale Hilfsfunktionen und teste sie direkt.

Einfachster Ansatz: Definiere die zu testenden Funktionen als lokal in den Test-Dateien nach demselben Muster wie in den Produktionsdateien, und pruefe die Ein-/Ausgabe-Paare.

Alternativ: Wenn die Hilfsfunktionen bereits aus den Komponenten exportiert sind, importiere sie direkt.

**JellyfinItemPicker.test.tsx:**
```typescript
import { describe, it, expect } from 'vitest'
// formatRunTimeTicks aus JellyfinItemPicker importieren oder lokal replizieren
```

**SegmenteTab.test.tsx:**
```typescript
import { describe, it, expect } from 'vitest'
// calcDuration, getTypeBadgeLabel aus SegmenteTab importieren oder lokal replizieren
```

Nutze `vitest` (bereits im Projekt: `frontend/package.json` hat vitest 3).
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx vitest run src/app/admin/episode-versions 2>&1 | tail -20</automated>
  </verify>
  <done>Alle Tests gehen durch (PASS); keine Fehler in der Test-Ausgabe</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 25 UI-Aenderungen vollstaendig implementiert:
    - Breadcrumb-Navigation (Anime › Episode › Gruppe v1)
    - 5-Tab-Layout (Uebersicht stub, Dateien stub, Informationen, Segmente, Changelog stub)
    - Segmente-Tabelle: Typ-Badge + Name, Dauer in Klammern, Einzelepisode ohne Duplikat, Jellyfin-Quelle, Dreipunkt-Menue
    - Vorschlaege-Leiste (erscheint wenn andere Releases Segmente haben)
    - Timeline mit Hauptinhalt-Label und schwebendem Insert-Track
    - JellyfinItemPicker-Modal mit Suche
    - Video-Vorschau-Block im Formular
  </what-built>
  <how-to-verify>
    1. Navigiere zu einer Episode-Version-Edit-Seite: z.B. http://localhost:3002/admin/episode-versions/[ID]/edit
    2. Pruefe Breadcrumb oben: "Anime › [Anime-Name] › Episode [N] › [Gruppe] v1"
    3. Pruefe 5 Tabs: Uebersicht | Dateien | Informationen | Segmente | Changelog — alle klickbar
    4. Wechsle zu Tab "Informationen" — bisheriges Formular muss unveraendert erscheinen
    5. Wechsle zu Tab "Segmente":
       a. Tabelle: Typ-Badge mit Kurzcode (OP/ED/IN/PV) + Name daneben
       b. Zeitbereich-Spalte: Format "00:00:30 – 00:01:45 (01:15)"
       c. Quelle-Spalte: "Jellyfin Item #ID" oder "—"
       d. Aktionen: Bleistift-Button + Dreipunkt-Button → Klick auf Dreipunkt zeigt "Loeschen"
    6. Klicke "+ Segment hinzufuegen":
       a. Quelle-Feld zeigt "Jellyfin Item auswaehlen >" statt Text-Input
       b. Klick darauf oeffnet JellyfinItemPicker-Modal mit Suchfeld
       c. Sucheingabe loest Anfrage aus (auch wenn kein Jellyfin konfiguriert: Fehlermeldung ist OK)
    7. Tabs Uebersicht / Dateien / Changelog zeigen "demnachst verfuegbar"-Platzhalter
  </how-to-verify>
  <resume-signal>Tippe "approved" wenn alles wie beschrieben funktioniert, oder beschreibe gefundene Probleme</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run src/app/admin/episode-versions` — alle Tests PASS
- Browser-Verifikation durch Admin abgeschlossen
</verification>

<success_criteria>
- Unit-Tests laufen fehlerfrei durch
- Admin bestaetigt: Breadcrumb korrekt, 5 Tabs funktionieren, Segmente-Tabelle poliert, JellyfinItemPicker oeffnet sich
- Kein TypeScript-Fehler, kein Build-Fehler
</success_criteria>

<output>
Nach Abschluss `.planning/phases/25-segmente-ui-mockup-alignment/25-03-SUMMARY.md` erstellen.
</output>
