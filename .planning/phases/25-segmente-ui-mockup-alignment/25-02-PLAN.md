---
phase: 25-segmente-ui-mockup-alignment
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditorPage.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditor.module.css
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.module.css
  - frontend/src/app/admin/episode-versions/[versionId]/edit/JellyfinItemPicker.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/JellyfinItemPicker.module.css
autonomous: true
requirements: [P25-SC1, P25-SC2, P25-SC3, P25-SC4, P25-SC5]

must_haves:
  truths:
    - "Seite zeigt Breadcrumb: Anime › [Titel] › Episode [N] › [Gruppe] v[X]"
    - "Seite hat 5 Tabs: Uebersicht, Dateien, Informationen, Segmente, Changelog"
    - "Segmente-Tab Tabelle zeigt Typ als 'Opening (OP)' Badge-Format, Dauer in Klammern, Einzelepisode als '3' nicht '3 – 3'"
    - "Quelle-Spalte zeigt Jellyfin-Icon + 'Jellyfin Item #XXXXX'"
    - "Aktionen-Spalte hat Bearbeiten-Button + Dreipunkt-Menue mit 'Loeschen'"
    - "Vorschlaege-Leiste erscheint wenn Suggestions-Endpunkt Segmente zurueckgibt"
    - "Timeline zeigt 'Hauptinhalt'-Label zwischen OP/ED; IN/PV-Segmente schweben oberhalb"
    - "Formular: Quelle-Feld ist klickbarer JellyfinItemPicker-Row statt Text-Input"
    - "Formular: Video-Vorschau-Player erscheint wenn Jellyfin-Item ausgewaehlt"
  artifacts:
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditorPage.tsx"
      provides: "Breadcrumb + 5-Tab-Layout"
      contains: "uebersicht.*dateien.*informationen.*segmente.*changelog"
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx"
      provides: "Vollstaendig ueberarbeiteter Segmente-Tab"
    - path: "frontend/src/app/admin/episode-versions/[versionId]/edit/JellyfinItemPicker.tsx"
      provides: "Modal fuer Jellyfin-Item-Suche"
      exports: ["JellyfinItemPicker"]
  key_links:
    - from: "EpisodeVersionEditorPage.tsx"
      to: "SegmenteTab.tsx"
      via: "activeTab === 'segmente' render + episodeNumber prop"
      pattern: "episodeNumber"
    - from: "SegmenteTab.tsx"
      to: "getAnimeSegmentSuggestions (api.ts)"
      via: "useEffect auf episodeNumber"
      pattern: "getAnimeSegmentSuggestions"
    - from: "SegmenteTab.tsx (Formular)"
      to: "JellyfinItemPicker.tsx"
      via: "pickerOpen state + onSelect callback"
      pattern: "JellyfinItemPicker"
---

<objective>
Vollstaendiges Frontend-Mockup-Alignment: Breadcrumb, 5-Tab-Layout, SegmenteTab-Verbesserungen und neuer JellyfinItemPicker.

Purpose: Der Admin sieht die Segmente-Verwaltungsseite exakt wie im Mockup — mit Kontext-Navigation, polierter Tabelle, Vorschlaegen und Jellyfin-Item-Suche.
Output: Ueberarbeitete EpisodeVersionEditorPage, verbesserter SegmenteTab, neuer JellyfinItemPicker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-segmente-ui-mockup-alignment/25-CONTEXT.md
@.planning/phases/25-segmente-ui-mockup-alignment/25-01-SUMMARY.md

@frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditorPage.tsx
@frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditor.module.css
@frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx
@frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.module.css
@frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseSegments.ts
@frontend/src/types/episodeVersion.ts
@frontend/src/lib/api.ts

<interfaces>
<!-- contextData-Shape — Breadcrumb-Daten entnehmen -->
From frontend/src/types/episodeVersion.ts:
```typescript
export interface EpisodeVersionEditorContext {
  version: EpisodeVersion   // version.anime_id, version.episode_number
  anime_title: string
  anime_folder_path?: string | null
  collaboration_group_id?: number | null
  selected_groups: FansubGroupSummary[]  // [0].name = Gruppen-Name
}

export interface EpisodeVersion {
  id: number
  anime_id: number
  episode_number: number
  fansub_group?: FansubGroupSummary | null
  // ...
}
```

<!-- Neue API-Helfer aus Plan 01 -->
From frontend/src/lib/api.ts (nach Plan 01):
```typescript
export async function getAnimeSegmentSuggestions(
  animeId: number,
  episode: number,
  excludeGroupId?: number | null,
  excludeVersion?: string | null,
  authToken?: string,
): Promise<AdminSegmentSuggestionsResponse>

export async function searchJellyfinItems(
  q: string,
  authToken?: string,
): Promise<AdminJellyfinItemsResponse>
```

<!-- Neue Typen aus Plan 01 -->
From frontend/src/types/admin.ts (nach Plan 01):
```typescript
export interface JellyfinItemResult {
  id: string
  name: string
  type: string
  run_time_ticks: number | null
}
```

<!-- Bestehende SegmenteTab-Props -->
```typescript
interface SegmenteTabProps {
  animeId: number | null
  groupId: number | null
  version: string | null
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: JellyfinItemPicker-Komponente</name>
  <files>
    frontend/src/app/admin/episode-versions/[versionId]/edit/JellyfinItemPicker.tsx,
    frontend/src/app/admin/episode-versions/[versionId]/edit/JellyfinItemPicker.module.css
  </files>
  <action>
Erstelle `JellyfinItemPicker.tsx` als eigenstaendige Modal-Komponente:

**Props:**
```typescript
interface JellyfinItemPickerProps {
  isOpen: boolean
  onClose: () => void
  onSelect: (item: JellyfinItemResult) => void
  authToken?: string
}
```

**Verhalten:**
- Modal-Overlay (fixed, z-index 200) mit weissem Panel (400px breit, 500px max-height, scrollbar)
- Header: "Jellyfin Item auswaehlen" + X-Schliessen-Button (lucide-react `X`)
- Sucheingabe mit `Search`-Icon (lucide-react); onChange debounced 300ms ruft `searchJellyfinItems(q, authToken)` auf
- Ladezustand: "Suche laeuft..."
- Ergebnisliste: jedes Item zeigt Name, Type-Badge (Video=blau, Audio=lila), Laufzeit aus run_time_ticks (10_000_000 Ticks = 1s, Format MM:SS oder HH:MM:SS)
- Klick auf Item: `onSelect(item)` + `onClose()`
- Leerzustand bei query >= 2 und Keine Ergebnisse: "Keine Items gefunden."
- Leerzustand bei query < 2: "Suchbegriff eingeben..."
- Fehlerfall: Fehlermeldung anzeigen

Hilfsfunktion `formatRunTimeTicks(ticks: number | null): string` — gibt "—" bei null zurueck, sonst MM:SS (oder H:MM:SS wenn >= 3600s)

**CSS** in `JellyfinItemPicker.module.css`:
- `.overlay`: fixed inset-0, bg rgba(0,0,0,0.5), z-index 200, display flex, align/justify center
- `.modal`: bg white, border-radius 12px, width 400px, max-height 500px, display flex, flex-direction column, box-shadow
- `.header`: padding 16px, display flex, justify-content space-between, align-items center, border-bottom
- `.searchRow`: padding 12px 16px, display flex, align-items center, gap 8px, border-bottom
- `.searchInput`: flex 1, border none, outline none, font-size 14px
- `.results`: overflow-y auto, flex 1
- `.resultItem`: padding 10px 16px, cursor pointer, display flex, justify-content space-between, align-items center, border-bottom 1px solid #f0f0f3
- `.resultItem:hover`: background #fafafa
- `.resultName`: font-size 14px, color #1c1c1e
- `.resultMeta`: display flex, gap 8px, align-items center, font-size 12px, color #6b6b70
- `.typeBadge`: padding 2px 6px, border-radius 4px, font-size 11px, font-weight 600, color white
- `.emptyState`: padding 2rem, text-align center, color #6b6b70, font-size 14px

Komponente muss unter 150 Zeilen bleiben.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>JellyfinItemPicker.tsx und JellyfinItemPicker.module.css existieren; TypeScript-Pruefung ohne Fehler</done>
</task>

<task type="auto">
  <name>Task 2: SegmenteTab verbessern (Tabelle, Vorschlaege, Timeline, Formular)</name>
  <files>
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx,
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.module.css
  </files>
  <action>
Passe `SegmenteTab.tsx` an. Neue Props:

```typescript
interface SegmenteTabProps {
  animeId: number | null
  groupId: number | null
  version: string | null
  episodeNumber?: number | null  // NEU: fuer Suggestions-Filter
}
```

**1. Tabellen-Spalten-Aenderungen:**

Spalten: Typ | Name | Episoden | Zeitbereich | Quelle | Aktionen

- **Typ-Spalte**: Zeige `<span className={badgeClass}>OP</span>` PLUS daneben den Langnamen in grau — ODER ersetze die separate Name-Spalte: Typ-Zelle zeigt "Opening **(OP)**"-Format. Konkret: `{typeLongName} ({shortCode})` als Text in der Badge — Beispiel: Badge-Inhalt ist "Opening" und daneben kleiner grauer Text "(OP)". Einfachste Loesung: zwei spans nebeneinander: `<span class="badge">OP</span><span class="typeLabel">Opening</span>`

- **Episoden-Spalte**: Wenn start_episode === end_episode, nur "3" anzeigen, nicht "3 – 3". Guard: `if (seg.start_episode === seg.end_episode) return String(seg.start_episode)`

- **Zeitbereich-Spalte**: Format "00:00:30 – 00:01:45 **(01:15)**". Hilfsfunktion `calcDuration(start, end)` berechnet Differenz in Sekunden und gibt "(MM:SS)" zurueck.

- **Quelle-Spalte**: Zeige Jellyfin-Icon (SVG-Inline 14x14 oder `Play`-Icon als Platzhalter von lucide-react) + "Jellyfin Item #XXXXX" (Item-ID als Text). Bei null: "—".

- **Aktionen-Spalte**: Ersetze `Trash2`-Button durch `MoreVertical`-Icon-Button. State `openMenuId: number | null` pro Zeile. Klick auf MoreVertical toggelt ein kleines Dropdown-Menu mit einer Option: "Loeschen". Dropdown ist absolut positioniert, z-index 50. Klick ausserhalb schliesst Menu (onClick auf Overlay oder document-listener — einfachste Loesung: kleines absolutes div mit onBlur oder einem separaten click-handler auf dem Wrapper). Bearbeiten-Button (`Pencil`) bleibt erhalten.

**2. Vorschlaege-Leiste:**

State: `suggestions: AdminThemeSegment[]`, `suggestionsLoading: boolean`

useEffect auf `[animeId, groupId, version, episodeNumber]`:
```typescript
if (!animeId || !episodeNumber || !authToken) return
void getAnimeSegmentSuggestions(animeId, episodeNumber, groupId, version, authToken)
  .then(res => setSuggestions(res.data))
  .catch(() => {}) // Stille Fehler, Leiste bleibt verborgen
```

Render unter der Tabelle, nur wenn `suggestions.length > 0`:
```
[Zap-Icon] Vorschlaege fuer diese Episode
           Basierend auf anderen Releases dieses Animes
[Opening 1 (Ep. 1–12) uebernehmen] [Ending 1 (Ep. 1–12) uebernehmen]
```
"Uebernehmen"-Button ruft `create({ ...suggestion, fansub_group_id: groupId, version })` auf und entfernt das Segment aus der Vorschlaege-Liste.

**3. Timeline-Verbesserung:**

Zwei Spuren statt einer:
- **Obere Spur** (hoehe 16px): nur INSERT/IN und PV-Segmente (schwebend)
- **Untere Spur** (hoehe 32px): OP, ED und andere. PLUS "Hauptinhalt"-Label als Luekenbereich zwischen OP-Ende und ED-Start.

Hauptinhalt-Logik: Finde OP-Segment (erstes OP-Segment) und ED-Segment (erstes ED-Segment). Wenn beide vorhanden: berechne Gap zwischen OP-Ende und ED-Start. Rendere grauen Block fuer den Gap mit Text "Hauptinhalt".

**4. Formular-Quelle-Feld:**

Ersetze Text-Input fuer `sourceJellyfinItemId` durch klickbare Row:

```typescript
const [pickerOpen, setPickerOpen] = useState(false)
const [selectedJellyfinItem, setSelectedJellyfinItem] = useState<JellyfinItemResult | null>(null)
```

Wenn item ausgewaehlt: zeige Name + ID. Wenn nicht: zeige "Jellyfin Item auswaehlen >".
Klick oeffnet `<JellyfinItemPicker isOpen={pickerOpen} onClose={() => setPickerOpen(false)} onSelect={(item) => { setSelectedJellyfinItem(item); setFormState(s => ({...s, sourceJellyfinItemId: item.id})); setPickerOpen(false) }} authToken={authToken} />`

**5. Video-Vorschau-Player:**

Wenn `selectedJellyfinItem` gesetzt und `formState.startTime`/`endTime` vorhanden:

```typescript
// Jellyfin-Direct-URL via bestehenden /media-Endpunkt oder direkte Jellyfin-URL
// Format: {jellyfinBaseUrl}/Videos/{itemId}/stream?api_key={key}
// Da wir die Basis-URL nicht im Frontend kennen, nutzen wir den bestehenden
// /api/episodes/:id/play-Proxy NICHT — stattdessen zeigen wir nur die Zeitanzeige
// und einen eingebetteten <video>-Tag wenn die stream_url verfuegbar ist.
```

Einfachste V1-Implementierung: Zeige einen Info-Block "Video-Vorschau: [Item-Name]" mit Start/End-Zeitanzeige. Kein eingebetteter Player notwendig wenn keine direkte URL verfuegbar — der Admin sieht zumindest das ausgewaehlte Item und die Zeiten. FALLS die Items-Antwort eine stream-faehige URL enthaelt (sie tut es nicht in V1), kann der Player spaeter hinzugefuegt werden. Zeige stattdessen: Item-Name, Typ, Laufzeit, und die konfigurierten Start/End-Zeiten als "Vorschau-Zeitbereich".

Datei-Groessenpruefung: SegmenteTab.tsx darf 450 Zeilen NICHT ueberschreiten. Wenn noetig, lagere die Timeline-Logik in `SegmenteTabTimeline.tsx` aus.

**CSS-Ergaenzungen in SegmenteTab.module.css:**

```css
/* Vorschlaege-Leiste */
.suggestionsBar { ... }
.suggestionsTitle { ... }
.suggestionsSubtitle { ... }
.suggestionsButtons { ... }
.adoptButton { ... }

/* Dreipunkt-Menue */
.menuWrapper { position: relative; display: inline-block; }
.dropdownMenu { position: absolute; right: 0; top: 100%; background: white; border: 1px solid #e1e1e6; border-radius: 8px; z-index: 50; min-width: 120px; }
.dropdownItem { ... }

/* Timeline zweispurig */
.timelineFloatTrack { position: relative; height: 16px; background: transparent; margin-bottom: 4px; }
.timelineFloatBlock { position: absolute; height: 100%; border-radius: 3px; ... }

/* Quelle-Row (klickbar) */
.jellyfinPickerRow { display: flex; align-items: center; justify-content: space-between; min-height: 40px; padding: 8px 12px; border: 1px solid #e1e1e6; border-radius: 8px; cursor: pointer; }
.jellyfinPickerRow:hover { border-color: #ff6a3d; }

/* Video-Vorschau-Block */
.videoPreviewBlock { margin-top: 0.75rem; padding: 12px; background: #f9f9f9; border-radius: 8px; border: 1px solid #e1e1e6; font-size: 13px; }
```
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript fehlerfrei; SegmenteTab.tsx existiert und importiert JellyfinItemPicker; alle neuen State-Variablen typisiert</done>
</task>

<task type="auto">
  <name>Task 3: EpisodeVersionEditorPage — Breadcrumb und 5 Tabs</name>
  <files>
    frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditorPage.tsx,
    frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditor.module.css
  </files>
  <action>
Refaktoriere `EpisodeVersionEditorPage.tsx`:

**1. Tab-Typ und State:**
```typescript
type ActiveTab = 'uebersicht' | 'dateien' | 'informationen' | 'segmente' | 'changelog'
const [activeTab, setActiveTab] = useState<ActiveTab>('informationen')
```
Standard-Tab ist 'informationen' (ersetzt 'allgemein').

**2. Breadcrumb-Navigation:**
Aus `contextData` ableiten:
- `animeId` = `contextData.version.anime_id`
- `animeTitle` = `contextData.anime_title`
- `episodeNumber` = `contextData.version.episode_number`
- `groupName` = `contextData.selected_groups[0]?.name ?? 'Unbekannte Gruppe'`
- `versionLabel` = `segmentVersion` (aktuell 'v1')

Render oberhalb des Headers:
```tsx
<nav className={styles.breadcrumb}>
  <Link href="/admin/anime">Anime</Link>
  <ChevronRight size={14} />
  <Link href={`/admin/anime/${animeId}`}>{animeTitle}</Link>
  <ChevronRight size={14} />
  <Link href={`/admin/anime/${animeId}/episodes`}>Episode {episodeNumber}</Link>
  <ChevronRight size={14} />
  <span>{groupName} {versionLabel}</span>
</nav>
```
Icons aus lucide-react: `ChevronRight`.

**3. Seiten-Header anpassen:**
Ersetze bisherigen Header durch:
```tsx
<header className={styles.header}>
  <div className={styles.headerContent}>
    {/* Anime-Cover wenn vorhanden — contextData hat kein cover_image, daher weglassen oder Platzhalter */}
    <div>
      <h1 className={styles.title}>{editor.contextData?.anime_title ?? 'Anime'}</h1>
      <div className={styles.headerMeta}>
        <span className={styles.metaBadge}>Episode {episodeNumber}</span>
        <span className={styles.metaSeparator}>·</span>
        <span className={styles.metaBadge}>Release: {groupName} {versionLabel}</span>
        {editor.hasUnsavedChanges ? <span className={styles.unsavedBadge}>Ungespeicherte Aenderungen</span> : null}
      </div>
    </div>
  </div>
</header>
```

**4. 5-Tab-Navigation:**
```tsx
<div className={styles.tabNav}>
  {(['uebersicht', 'dateien', 'informationen', 'segmente', 'changelog'] as ActiveTab[]).map(tab => (
    <button
      key={tab}
      type="button"
      className={activeTab === tab ? styles.tabActive : styles.tab}
      onClick={() => setActiveTab(tab)}
    >
      {TAB_LABELS[tab]}
    </button>
  ))}
</div>
```

TAB_LABELS-Mapping:
```typescript
const TAB_LABELS: Record<ActiveTab, string> = {
  uebersicht: 'Uebersicht',
  dateien: 'Dateien',
  informationen: 'Informationen',
  segmente: 'Segmente',
  changelog: 'Changelog',
}
```

**5. Tab-Inhalte:**
- `uebersicht`: `<section className={styles.card}><p className={styles.helperText}>Uebersicht — demnachst verfuegbar.</p></section>`
- `dateien`: `<section className={styles.card}><p className={styles.helperText}>Dateien — demnachst verfuegbar.</p></section>`
- `informationen`: bisheriger `allgemein`-Inhalt (unveraendert, nur Tab-Name geaendert)
- `segmente`: `<SegmenteTab animeId={...} groupId={...} version={...} episodeNumber={episodeNumber} />`
- `changelog`: `<section className={styles.card}><p className={styles.helperText}>Changelog — demnachst verfuegbar.</p></section>`

**6. Aktions-Leiste:**
Verschiebe den "Speichern"-Button so dass er nur bei aktivem 'informationen'-Tab sichtbar ist (da Segmente ihren eigenen Speicher-Flow haben). Die `<section className={styles.actionBar}>` nur rendern wenn `activeTab === 'informationen'`.

**7. SegmenteTab-Props aktualisieren:**
```tsx
<SegmenteTab
  animeId={segmentAnimeId}
  groupId={segmentGroupId}
  version={segmentVersion}
  episodeNumber={editor.contextData?.version.episode_number ?? null}
/>
```

**CSS-Ergaenzungen in EpisodeVersionEditor.module.css:**
```css
.breadcrumb {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 13px;
  color: #6b6b70;
  margin-bottom: 16px;
  flex-wrap: wrap;
}
.breadcrumb a { color: #ff6a3d; text-decoration: none; }
.breadcrumb a:hover { text-decoration: underline; }

.headerContent { display: flex; align-items: flex-start; gap: 16px; }
.headerMeta { display: flex; align-items: center; gap: 8px; margin-top: 6px; flex-wrap: wrap; }
.metaBadge { font-size: 13px; color: #6b6b70; background: #f0f0f3; padding: 2px 8px; border-radius: 4px; }
.metaSeparator { color: #c0c0c5; }
```

Datei-Groessenpruefung: EpisodeVersionEditorPage.tsx darf 350 Zeilen nicht ueberschreiten — bisherige Datei hat 293 Zeilen, Refaktorierung sollte im Rahmen bleiben.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript fehlerfrei; EpisodeVersionEditorPage.tsx hat 5-Tab-State; Breadcrumb rendert Anime/Episode/Gruppe; SegmenteTab erhaelt episodeNumber-Prop</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` ohne Fehler
- `cd frontend && npm run build` ohne Fehler (oder Lint-Only wenn Build zu langsam)
- EpisodeVersionEditorPage.tsx: grep auf 'uebersicht.*dateien.*informationen.*segmente.*changelog' oder TAB_LABELS zeigt alle 5 Tabs
- SegmenteTab.tsx: grep auf 'episodeNumber' zeigt neue Prop und Suggestions-useEffect
- JellyfinItemPicker.tsx: existiert und exportiert JellyfinItemPicker
- Zeilenanzahl-Check: SegmenteTab.tsx unter 450 Zeilen
</verification>

<success_criteria>
- Breadcrumb zeigt korrekten Pfad aus contextData
- 5-Tab-Navigation funktioniert; Informationen-Tab zeigt unveraendertes Allgemein-Formular
- Segmente-Tabelle: Typ-Badge + Name nebeneinander, Dauer in Klammern, Einzelepisode ohne Duplikat
- Vorschlaege-Leiste erscheint bei Suggestions-Daten
- Timeline hat zwei Spuren und Hauptinhalt-Label
- JellyfinItemPicker oeffnet sich beim Klick auf Quelle-Feld
- Video-Vorschau-Block zeigt Item-Info wenn Jellyfin-Item ausgewaehlt
</success_criteria>

<output>
Nach Abschluss `.planning/phases/25-segmente-ui-mockup-alignment/25-02-SUMMARY.md` erstellen.
</output>
