---
phase: 21-fansub-group-chip-mapping-and-collaboration-wiring
plan: 02
type: frontend
wave: 2
depends_on:
  - 21-01-PLAN.md
files_modified:
  - frontend/src/app/admin/anime/[id]/episodes/import/page.tsx
  - frontend/src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.ts
  - frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.ts
  - frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.test.ts
  - frontend/src/types/episodeImport.ts
  - frontend/src/lib/api.ts
autonomous: true
requirements:
  - P21-SC1
  - P21-SC2
  - P21-SC4
must_haves:
  truths:
    - "Import mapping rows reuse existing fansub groups through chip selection instead of only a flat text field."
    - "Operators can still type a new group name and carry it as a chip before apply."
    - "Episode and Ab-hier patch actions copy the whole selected-chip set, not only one string value."
---

<objective>
Turn the import workbench's release-group input into a reusable chip-based selector with free-text fallback and set-aware patch helpers.

Purpose: operators need typo-safe reuse of existing groups and practical multi-row patching without exposing collaboration internals directly.
Output: chip/search UI in mapping rows, state helpers for selected groups, and apply payloads aligned with the backend contract.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/21-fansub-group-chip-mapping-and-collaboration-wiring/21-CONTEXT.md

Read these implementation references before editing:
@frontend/src/app/admin/anime/[id]/episodes/import/page.tsx
@frontend/src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.ts
@frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.ts
@frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.test.ts
@frontend/src/types/episodeImport.ts
@frontend/src/lib/api.ts
@frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditGenreSection.tsx
@frontend/src/app/admin/anime/components/AnimeContext/AnimeContextFansubManager.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the import-row group text field with chip selection plus search</name>
  <read_first>
    - frontend/src/app/admin/anime/[id]/episodes/import/page.tsx
    - frontend/src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.ts
    - frontend/src/types/episodeImport.ts
    - frontend/src/app/admin/anime/components/AnimeEditPage/AnimeEditGenreSection.tsx
    - frontend/src/app/admin/anime/components/AnimeContext/AnimeContextFansubManager.tsx
  </read_first>
  <action>
    Replace the single `Gruppe` text input in each mapping row with a chip-style control that can show selected groups, remove them individually, search `getFansubList` results, and add a free-text chip when no existing group matches. Keep the interaction compact enough for import-row density; do not expose collaboration as a manually chosen chip.
  </action>
  <acceptance_criteria>
    - frontend/src/app/admin/anime/[id]/episodes/import/page.tsx no longer renders only one plain group text input as the authoritative row control
    - frontend/src/app/admin/anime/[id]/episodes/import/page.tsx renders removable selected-group chips in each mapping row
    - frontend/src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.ts contains state and callbacks for adding and removing selected groups per mapping row
    - frontend/src/lib/api.ts is used to search existing fansub groups for the import-row selector
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Make patch helpers propagate full selected-group sets</name>
  <read_first>
    - frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.ts
    - frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.test.ts
    - frontend/src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.ts
  </read_first>
  <action>
    Change the existing `Episode` and `Ab hier` group patch helpers so they copy the full selected-group set from the source row to the target rows. Preserve deduplication and chip order stability. Do not collapse the selected set back into one display string during patching.
  </action>
  <acceptance_criteria>
    - frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.ts contains helpers that patch selected-group arrays rather than only one group-name string
    - frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.test.ts contains at least one test proving `Episode` copies multiple selected groups together
    - frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.test.ts contains at least one test proving `Ab hier` copies multiple selected groups together to later rows
    - frontend/src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.ts wires the new set-aware patch helpers into the page actions
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Send the selected groups in the apply payload without losing free-text entries</name>
  <read_first>
    - frontend/src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.ts
    - frontend/src/types/episodeImport.ts
    - frontend/src/lib/api.ts
  </read_first>
  <action>
    Update the preview/apply mapping state so each row carries the selected-group chip list and the final apply payload submits that list exactly as defined in Plan 21-01. Preserve newly typed groups in the payload even before the backend has assigned them ids.
  </action>
  <acceptance_criteria>
    - frontend/src/types/episodeImport.ts contains the selected-group list shape used in import mappings
    - frontend/src/app/admin/anime/[id]/episodes/import/useEpisodeImportBuilder.ts includes the selected-group list when calling applyEpisodeImport
    - frontend/src/app/admin/anime/[id]/episodes/import/episodeImportMapping.test.ts or a nearby import workbench test proves newly typed groups survive row edits and still appear in the apply payload shape
  </acceptance_criteria>
</task>

</tasks>

<verification>
Wave 2 is complete when import rows support searchable/removable chips, patch helpers copy full group sets, and apply sends those sets without collapsing them to one string.
</verification>

<success_criteria>
- Existing groups are reusable directly in the import UI.
- New names can be entered as chips without leaving the row.
- Patch actions operate on full group selections, not a lossy text surrogate.
</success_criteria>

<output>
After completion, create `.planning/phases/21-fansub-group-chip-mapping-and-collaboration-wiring/21-02-SUMMARY.md`.
</output>
