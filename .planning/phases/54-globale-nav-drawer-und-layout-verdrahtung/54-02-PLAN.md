---
phase: 54-globale-nav-drawer-und-layout-verdrahtung
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/AppShellClientWrapper.tsx
autonomous: true
requirements:
  - D-13
  - D-16
  - D-17
  - D-15

must_haves:
  truths:
    - "AppShellClientWrapper existiert als neue Datei mit 'use client'-Direktive"
    - "useAuthSession liefert mode (anonymous vs authenticated) über isClientInitialized-Guard"
    - "getOwnProfile wird nur aufgerufen wenn isClientInitialized === true UND hasAuthSession"
    - "Avatar-URL wird über resolveApiUrl(d.avatar?.public_url || '') aufgelöst — nicht d.avatar_url"
    - "canAccessAdmin wird aus account_global_roles berechnet (enthält 'platform_admin' oder 'admin')"
    - "usePathname() liefert currentPath für aktiven Nav-Link"
    - "Kein authToken als Prop an AppShell weitergeleitet"
    - "Bei Logout (hasAuthSession=false) wird setProfile(null) aufgerufen"
  artifacts:
    - path: "frontend/src/components/layout/AppShellClientWrapper.tsx"
      provides: "Client-Wrapper der Server/Client-Component-Grenze überbrückt"
      contains: "use client"
    - path: "frontend/src/components/layout/AppShellClientWrapper.tsx"
      provides: "isClientInitialized-Guard vor getOwnProfile"
      contains: "isClientInitialized"
  key_links:
    - from: "useAuthSession()"
      to: "AppShell mode-Prop"
      via: "hasAuthSession ? 'authenticated' : 'anonymous'"
      pattern: "hasAuthSession"
    - from: "getOwnProfile().data.avatar?.public_url"
      to: "AppShell user.avatarUrl"
      via: "resolveApiUrl(d.avatar?.public_url || '')"
      pattern: "avatar\\.public_url"
    - from: "getOwnProfile().data.account_global_roles"
      to: "AppShell canAccessAdmin-Prop"
      via: "includes('platform_admin') || includes('admin')"
      pattern: "account_global_roles"
---

<objective>
Neuen AppShellClientWrapper anlegen — der dedizierte 'use client'-Wrapper der die Server/Client-Component-Grenze zwischen Root-Layout (Server Component) und AppShell (Client Component) überbrückt.

Zweck: Root-Layout bleibt Server Component. Der Wrapper übernimmt Auth-State-Lesung, Profil-API-Call und Avatar-URL-Auflösung. Plan 03 integriert diesen Wrapper ins Root-Layout.

Ausgabe: AppShellClientWrapper.tsx mit korrektem isClientInitialized-Guard, resolveApiUrl-Muster, usePathname für aktiven Nav-Link, und tokenfreier Prop-Übergabe an AppShell.
</objective>

<execution_context>
@C:\Users\admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/54-globale-nav-drawer-und-layout-verdrahtung/54-CONTEXT.md
@.planning/phases/54-globale-nav-drawer-und-layout-verdrahtung/54-RESEARCH.md
@.planning/phases/54-globale-nav-drawer-und-layout-verdrahtung/54-PATTERNS.md

<interfaces>
Aus useAuthSession.ts (Zeilen 30–51) — immer isClientInitialized-Guard:
  const { hasAccessToken, hasRefreshToken, isClientInitialized } = useAuthSession()
  const hasAuthSession = hasAccessToken || hasRefreshToken
  Guard: if (!isClientInitialized || !hasAuthSession) { setProfile(null); return }

Aus api.ts — getOwnProfile und resolveApiUrl:
  getOwnProfile(): Promise<{ data: MemberProfileData }>
  resolveApiUrl(path: string): string

Aus profile.ts — MemberProfileData (kritisch):
  avatar?: { public_url: string; [weitere Felder] } | null
  account_display_name?: string | null
  fansub_name?: string | null
  email?: string | null
  account_global_roles: string[]
  ACHTUNG: KEIN direktes Feld avatar_url — immer avatar?.public_url verwenden

Aus next/navigation:
  usePathname(): string | null — liefert aktuellen URL-Pfad

Aus AuthSessionSwitchGuard.tsx (Analog für 'use client' + useEffect Pattern):
  'use client'
  import { useEffect } from 'react'
  export function AuthSessionSwitchGuard() {
    useEffect(() => { ... ; return () => { /* Cleanup */ } }, [])
    return null
  }

AppShell Props (nach Plan 01):
  mode: 'authenticated' | 'anonymous'
  currentPath?: string
  user?: { displayName?: string | null; email?: string | null; avatarUrl?: string | null } | null
  canAccessAdmin?: boolean
  children: ReactNode
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AppShellClientWrapper.tsx anlegen</name>
  <files>frontend/src/components/layout/AppShellClientWrapper.tsx</files>
  <read_first>
    frontend/src/lib/useAuthSession.ts
    frontend/src/lib/api.ts
    frontend/src/types/profile.ts
    frontend/src/components/auth/AuthSessionSwitchGuard.tsx
    .planning/phases/54-globale-nav-drawer-und-layout-verdrahtung/54-PATTERNS.md
  </read_first>
  <behavior>
    - Datei beginnt mit 'use client' als erste Zeile
    - useAuthSession() wird korrekt konsumiert: hasAccessToken, hasRefreshToken, isClientInitialized
    - hasAuthSession = hasAccessToken || hasRefreshToken
    - useEffect läuft auf [isClientInitialized, hasAuthSession]
    - Guard am Anfang des useEffect: if (!isClientInitialized || !hasAuthSession) { setProfile(null); return }
    - getOwnProfile wird nach Guard aufgerufen
    - Avatar-URL: resolveApiUrl(d.avatar?.public_url || '') — NICHT d.avatar_url
    - displayName: d.account_display_name || d.fansub_name || undefined
    - canAdmin: d.account_global_roles.includes('platform_admin') || d.account_global_roles.includes('admin')
    - Bei catch: setProfile(null) (stilles Fallback)
    - usePathname() für currentPath
    - AppShell bekommt: mode, currentPath, user (mit avatarUrl), canAccessAdmin
    - KEIN authToken als Prop
  </behavior>
  <action>
    Lies useAuthSession.ts, api.ts, profile.ts und AuthSessionSwitchGuard.tsx vollständig — extrahiere alle relevanten Exports und Typen in einem Lesevorgang.

    Erstelle frontend/src/components/layout/AppShellClientWrapper.tsx als neue Datei.

    Erste Zeile: 'use client'

    Imports (in Reihenfolge): React-Imports (useEffect, useState aus 'react'), type ReactNode aus 'react', usePathname aus 'next/navigation', getOwnProfile und resolveApiUrl aus '@/lib/api', useAuthSession aus '@/lib/useAuthSession', AppShell aus './AppShell'.

    Interface WrapperProfile: { displayName?: string; email?: string; avatarUrl?: string; canAdmin?: boolean }

    Funktion AppShellClientWrapper({ children }: { children: ReactNode }):
    1. useAuthSession() destructurieren: hasAccessToken, hasRefreshToken, isClientInitialized
    2. hasAuthSession = hasAccessToken || hasRefreshToken
    3. currentPath = usePathname()
    4. profile State: useState<WrapperProfile | null>(null)
    5. useEffect mit [isClientInitialized, hasAuthSession]:
       - if (!isClientInitialized || !hasAuthSession) { setProfile(null); return }
       - getOwnProfile().then((res) => { const d = res.data; setProfile({ displayName: d.account_display_name || d.fansub_name || undefined, email: d.email || undefined, avatarUrl: resolveApiUrl(d.avatar?.public_url || '') || undefined, canAdmin: d.account_global_roles.includes('platform_admin') || d.account_global_roles.includes('admin') }) }).catch(() => setProfile(null))
    6. Return: AppShell-Komponente mit Props: mode={hasAuthSession ? 'authenticated' : 'anonymous'}, currentPath={currentPath ?? undefined}, user={profile ? { displayName: profile.displayName, email: profile.email, avatarUrl: profile.avatarUrl } : null}, canAccessAdmin={profile?.canAdmin ?? false}. {children} als Kinder.

    Export: export function AppShellClientWrapper.

    KRITISCH: Das Feld heißt d.avatar?.public_url — kein d.avatar_url. Der isClientInitialized-Guard muss VOR dem hasAuthSession-Check stehen. Kein authToken in den Props.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    frontend/src/components/layout/AppShellClientWrapper.tsx existiert.
    Erste Zeile ist 'use client'.
    Datei enthält: isClientInitialized, avatar?.public_url, account_global_roles, usePathname.
    TypeScript-Compile ohne Fehler.
    Kein authToken in der Datei.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Server Component → Client Component | Root-Layout (Server) nutzt Wrapper als Grenze; kein Client-State im Server |
| API Response → AppShell Props | Avatar-URL ist öffentliche Media-URL, kein Token; canAccessAdmin aus Capabilities |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Elevation of Privilege | canAccessAdmin Berechnung | mitigate | Berechnung aus account_global_roles (Backend-Capabilities), nicht aus Keycloak-Claims; AppShell zeigt nur Nav-Link, Route bleibt server-seitig geschützt |
| T-54-02 | Information Disclosure | AppShellClientWrapper Props | mitigate | Kein authToken als Prop an AppShell; avatarUrl ist öffentliche URL; Token-free UI Boundary per auth-api-client.md |
| T-54-SC | Tampering | npm installs | accept | Keine neuen Pakete — nur bestehende Projekt-Utilities |
</threat_model>

<verification>
Nach Task 1:

1. `cd frontend && npx tsc --noEmit 2>&1 | head -20` — keine TypeScript-Fehler
2. `grep -n "avatar?.public_url" frontend/src/components/layout/AppShellClientWrapper.tsx` — Treffer vorhanden
3. `grep -n "isClientInitialized" frontend/src/components/layout/AppShellClientWrapper.tsx` — Treffer vorhanden
4. `grep -n "authToken" frontend/src/components/layout/AppShellClientWrapper.tsx` — KEIN Treffer (grep gibt leer aus)
5. `head -1 frontend/src/components/layout/AppShellClientWrapper.tsx` — gibt "'use client'" aus
</verification>

<success_criteria>
1. AppShellClientWrapper.tsx existiert, beginnt mit 'use client'
2. isClientInitialized-Guard ist vor getOwnProfile-Aufruf
3. Avatar-URL nutzt d.avatar?.public_url (nicht d.avatar_url) + resolveApiUrl
4. canAccessAdmin aus account_global_roles.includes('platform_admin' | 'admin')
5. currentPath via usePathname()
6. TypeScript kompiliert ohne Fehler
7. Kein authToken in den Props
</success_criteria>

<output>
Erstelle `.planning/phases/54-globale-nav-drawer-und-layout-verdrahtung/54-02-SUMMARY.md` wenn fertig
</output>
