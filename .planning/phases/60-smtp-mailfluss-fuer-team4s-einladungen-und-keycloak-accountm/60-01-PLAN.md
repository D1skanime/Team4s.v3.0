---
phase: 60-smtp-mailfluss-fuer-team4s-einladungen-und-keycloak-accountm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - infra/keycloak/realm-team4s.json
  - docs/operations/keycloak-auth-foundation-phase43.md
autonomous: true
requirements:
  - P60-SC1
  - P60-SC2
  - D-03
  - D-05
  - D-06
  - D-07
must_haves:
  truths:
    - "D-01: Keycloak besitzt Account- und Identity-Mails"
    - "D-03: Keycloak und Team4s Backend zeigen lokal auf denselben Mailpit-SMTP-Server"
    - "D-05: Mailpit laeuft lokal als eigener Compose-Service mit SMTP 1025 und UI 8025"
    - "D-06: Lokale Backend-Defaults nutzen Mailpit nur im lokalen Profil; Produktion wird explizit konfiguriert"
    - "D-07: Keycloak kann lokale Passwort-Reset-Mails an Mailpit senden"
    - "Keine produktiven SMTP-Secrets werden committed"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Mailpit service + Backend/Keycloak SMTP env wiring"
    - path: ".env.example"
      provides: "Dokumentierte lokale SMTP-Defaults und Mailjet-Platzhalter"
    - path: "infra/keycloak/realm-team4s.json"
      provides: "Lokale SMTP-Server-Konfiguration fuer Mailpit"
---

<objective>
Lokale SMTP-Infrastruktur herstellen: Mailpit in Docker Compose, Keycloak-Mailversand nach Mailpit und dokumentierte Env-Werte fuer Backend/Produktion.
</objective>

<context>
@C:/Users/admin/Documents/Team4s/.planning/phases/60-smtp-mailfluss-fuer-team4s-einladungen-und-keycloak-accountm/60-CONTEXT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/60-smtp-mailfluss-fuer-team4s-einladungen-und-keycloak-accountm/60-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Mailpit-Service in Docker Compose ergaenzen</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml - bestehende Service-Namen, Netz, Ports und Depends-On-Muster.
  </read_first>
  <action>
    Fuege `team4sv30-mailpit` mit Image `axllent/mailpit:latest` hinzu. Exponiere SMTP `1025:1025` und Web-UI `8025:8025`. Backend und Keycloak bekommen SMTP-Env, die innerhalb Compose auf `team4sv30-mailpit:1025` zeigt. Dies deckt D-03, D-05 und D-06 ab.
  </action>
  <verify>
    <automated>docker compose config</automated>
  </verify>
  <done>Compose validiert und Mailpit ist unter http://127.0.0.1:8025 erreichbar, sobald der Service gestartet ist.</done>
</task>

<task type="auto">
  <name>Task 2: Keycloak lokale SMTP-Konfiguration</name>
  <files>infra/keycloak/realm-team4s.json</files>
  <read_first>
    - infra/keycloak/realm-team4s.json - Realm-Import und resetPasswordAllowed.
    - docs/operations/keycloak-auth-foundation-phase43.md - Keycloak bleibt Account-Mail-Besitzer.
  </read_first>
  <action>
    Ergaenze eine lokale `smtpServer`-Konfiguration fuer Mailpit. Host muss im Container-Netz erreichbar sein. From-Adresse lokal, z.B. `account@team4s.local`. Keine echten Credentials. Dies deckt D-01 und D-07 ab.
  </action>
  <verify>
    <manual>Frischer lokaler Realm oder live aktualisierte SMTP-Einstellung: Keycloak Admin Console -> Realm team4s -> Email -> Test connection landet in Mailpit.</manual>
  </verify>
  <done>Keycloak Test-Mail erscheint in Mailpit.</done>
</task>

<task type="auto">
  <name>Task 3: Env-Beispiele und lokale Betriebsdoku aktualisieren</name>
  <files>.env.example, docs/operations/keycloak-auth-foundation-phase43.md</files>
  <read_first>
    - .env.example - bestehende lokale Env-Dokumentation.
    - docs/operations/keycloak-auth-foundation-phase43.md - lokaler Auth-Bootstrap.
  </read_first>
  <action>
    Dokumentiere SMTP-Variablen fuer lokalen Backend-Mailversand und einen separaten Abschnitt fuer spaetere Mailjet-Produktion. Keine echten Mailjet-Secrets. Beschreibe Mailpit-UI `http://127.0.0.1:8025`.
  </action>
  <verify>
    <automated>git diff --check -- docker-compose.yml .env.example infra/keycloak/realm-team4s.json docs/operations/keycloak-auth-foundation-phase43.md</automated>
  </verify>
  <done>Lokaler Mailpit-Flow und spaeterer Mailjet-SMTP-Wechsel sind reproduzierbar dokumentiert.</done>
</task>
</tasks>

<verification>
1. `docker compose config`
2. Mailpit-Service startet lokal.
3. Keycloak Test-Mail oder Passwort-Reset-Mail erscheint in Mailpit.
4. Keine echten SMTP-Secrets im Diff.
</verification>
