---
phase: 42-tiptap-collaboration-mvp-fuer-fansub-group-notes
plan: "01"
type: execute
wave: 1
depends_on:
  - "41"
files_modified:
  - frontend/src/components/editor/RichTextEditor.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/NotesTab.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/NotesTab.helpers.tsx
  - frontend/src/lib/api.ts
  - frontend/src/types/fansubNotes.ts
  - backend/internal/handlers/admin_content_fansub_group_notes.go
  - backend/internal/repository/fansub_group_notes_repository.go
  - backend/internal/handlers
  - docs/architecture
autonomous: true
requirements:
  - TIPTAP-COLLAB-01
must_haves:
  truths:
    - "Collaboration wird in Phase 42 nur fuer fansub_group_notes aktiviert."
    - "Phase 42 plant ausschliesslich einen self-hosted Collaboration-Stack."
    - "Der vorhandene Redis-Stack ist Teil der geplanten Collaboration-Topologie."
    - "Jedes Collaboration-Dokument ist 1:1 an fansub_group_notes.id gebunden."
    - "Persistente Fachquelle bleibt fansub_group_notes.body_json; Collaboration fuehrt keinen zweiten konkurrierenden Notizspeicher ein."
    - "Initialinhalt wird pro Dokument genau einmal gesetzt und nicht bei jedem Mount erneut injiziert."
    - "Undo/Redo im Collaboration-Modus verwendet keinen lokalen Verlauf, der fremde Aenderungen zurueckrollen kann."
    - "Auth fuer Collaboration ist explizit an bestehende Team4s-Berechtigung gekoppelt."
---

<objective>
Define and implement the self-hosted collaboration architecture seam for `fansub_group_notes` only: deterministic document identity, authorization gate, safe initial-content seeding, and durable persistence back into the existing note record.

Purpose: Before any rich presence polish or broad rollout, Team4s needs a domain-safe seam that proves collaborative editing can exist without drifting onto the wrong entity or creating a parallel source of truth.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-tiptap-collaboration-mvp-fuer-fansub-group-notes/42-CONTEXT.md
@.planning/phases/42-tiptap-collaboration-mvp-fuer-fansub-group-notes/42-RESEARCH.md
@.planning/phases/41-globalen-tiptap-rich-text-editor-einfuehren/41-CONTEXT.md
@.planning/phases/41-globalen-tiptap-rich-text-editor-einfuehren/41-UAT.md
@frontend/src/components/editor/RichTextEditor.tsx
@frontend/src/app/admin/fansubs/[id]/edit/NotesTab.tsx
@frontend/src/app/admin/fansubs/[id]/edit/NotesTab.helpers.tsx
@frontend/src/lib/api.ts
@frontend/src/types/fansubNotes.ts
@backend/internal/handlers/admin_content_fansub_group_notes.go
@backend/internal/repository/fansub_group_notes_repository.go

<interfaces>
<!-- Stable business object: fansub_group_notes.id -->
<!-- Canonical persisted fields: body_json, body_html, body_text -->
<!-- Collaboration doc name pattern for MVP: fansub-group-note:{noteId} -->
<!-- Self-hosted backend path only; no Tiptap Cloud/Platform branch in this phase -->
<!-- No collaboration for member_group_stories, anime_fansub_project_notes, release_version_notes in this phase -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the collaboration contract before UI wiring</name>
  <read_first>
    - .planning/phases/42-tiptap-collaboration-mvp-fuer-fansub-group-notes/42-CONTEXT.md
    - .planning/phases/42-tiptap-collaboration-mvp-fuer-fansub-group-notes/42-RESEARCH.md
    - frontend/src/types/fansubNotes.ts
    - frontend/src/lib/api.ts
    - backend/internal/handlers/admin_content_fansub_group_notes.go
    - backend/internal/repository/fansub_group_notes_repository.go
  </read_first>
  <action>
    Introduce the smallest self-hosted project-owned collaboration contract for existing group notes only.

    The implementation seam must define:
    - deterministic document identity based on `fansub_group_notes.id`
    - explicit self-hosted backend topology for the MVP (client, websocket backend, vorhandener Redis-Stack, auth seam, durable save seam)
    - an explicit backend route or token-issuance seam that authorizes collaboration access for the current note
    - the persisted save path back into the existing note row
    - an explicit "existing note required" rule so that collaboration starts only after a note record already exists

    Do not broaden this into a generic cross-note collaboration framework yet. The only business object in scope is `fansub_group_notes`.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/handlers ./internal/repository -count=1</automated>
  </verify>
  <done>
    - There is an explicit self-hosted collaboration document naming contract for group notes.
    - The architecture uses the existing Redis seam rather than treating Redis as a future optional add-on.
    - Collaboration authorization is note-scoped rather than group-wide or anime-wide.
    - Existing save/update handlers still own durable `body_json` persistence.
    - Tests or source invariants cover the note-scoped seam.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire RichTextEditor into collaboration mode for existing group notes</name>
  <read_first>
    - frontend/src/components/editor/RichTextEditor.tsx
    - frontend/src/app/admin/fansubs/[id]/edit/NotesTab.tsx
    - frontend/src/app/admin/fansubs/[id]/edit/NotesTab.helpers.tsx
    - .planning/phases/42-tiptap-collaboration-mvp-fuer-fansub-group-notes/42-RESEARCH.md
  </read_first>
  <action>
    Extend the existing group-note editor flow so an already-created note can open in a self-hosted collaboration-backed TipTap session.

    MVP behavior:
    - collaboration mode is entered only for an existing note with a note ID
    - initial content is seeded from persisted `body_json` exactly once when the collaboration document is empty
    - the editor does not blindly reapply content after every mount, refetch, or reconnect
    - collaboration mode disables incompatible local undo/redo history

    Keep the normal non-collaborative create flow intact for brand-new notes until the note exists.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck && npx vitest run "src/app/admin/fansubs/[id]/edit/NotesTab.test.tsx" "src/components/editor/RichTextEditor.test.tsx"</automated>
  </verify>
  <done>
    - Existing group notes can mount the editor in collaboration mode.
    - New note drafts do not require pre-save collaboration support.
    - Initial content is not duplicated.
    - Collaboration mode is isolated to `fansub_group_notes`.
  </done>
</task>

</tasks>

<verification>
Automated:
1. `cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/handlers ./internal/repository -count=1`
2. `cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck`
3. `cd C:/Users/admin/Documents/Team4s/frontend && npx vitest run "src/app/admin/fansubs/[id]/edit/NotesTab.test.tsx" "src/components/editor/RichTextEditor.test.tsx"`

Live:
1. Open the same existing group note in two browser sessions.
2. Confirm both sessions join the same note-scoped collaboration document.
3. Enter text in session A and confirm session B receives the shared change.
4. Reload one session and confirm content is not duplicated.
5. Save and confirm the durable note still persists through `body_json` -> `body_html` / `body_text`.
6. Confirm the flow does not depend on a managed Tiptap document cloud.
</verification>

<success_criteria>
- Collaboration is note-scoped, not broad group-scoped.
- The planned MVP path is self-hosted only.
- The planned MVP path reuses the existing Redis infrastructure.
- Existing group notes can be edited collaboratively without duplicating initial content.
- Durable persistence still flows through the existing note record.
- Phase 42 introduces no wrong-entity storage seam.
</success_criteria>

<output>
Create `.planning/phases/42-tiptap-collaboration-mvp-fuer-fansub-group-notes/42-01-SUMMARY.md`.
</output>
