---
phase: 72-dom-nen-projektionen-status-fundament
plan: 03
type: execute
wave: 2
depends_on: ["72-01"]
files_modified:
  - backend/internal/repository/media_ownership_projection_repository.go
  - backend/internal/handlers/media_ownership_projection_handler.go
  - backend/internal/repository/media_ownership_projection_repository_test.go
  - backend/cmd/server/main.go
autonomous: true
requirements: [G, I]
must_haves:
  truths:
    - "Die Medien-Projektion liefert pro Asset Owner-Typ, Owner-ID, Kategorie, Sichtbarkeit und Review-Status"
    - "Member-Medien-Owner kommt aus media_assets.owner_member_id, nicht aus einem neuen Owner-Feld"
    - "Sichtbarkeit kommt aus visibilities-Lookup, Review-Status aus review_statuses-Lookup"
    - "Die Projektion respektiert den owner_member_id-Scope (keine IDOR-Exposition fremder Member-Medien)"
    - "D-03: die Medien-Projektion liest Sichtbarkeit (visibilities) und Review-Status (review_statuses) als zwei getrennte Achsen, getrennt vom technischen media_assets.status"
    - "D-05: ausschließlich GET-Read-Projektion, keine Schreib-Endpunkte"
  artifacts:
    - path: "backend/internal/repository/media_ownership_projection_repository.go"
      provides: "Read-Projektion für Medien-Owner/Kategorie/Sichtbarkeit/Review pro Junction-Kontext"
      min_lines: 60
    - path: "backend/internal/handlers/media_ownership_projection_handler.go"
      provides: "Dünner GET-Handler für die Medien-Projektion (kein Envelope)"
      min_lines: 20
    - path: "backend/internal/repository/media_ownership_projection_repository_test.go"
      provides: "Source-Fragment-Test (no-DB): Owner/Visibility/Review-Achsen + owner-scope WHERE (IDOR)"
      min_lines: 40
  key_links:
    - from: "media_ownership_projection_repository.go"
      to: "visibilities / review_statuses"
      via: "LEFT JOIN auf media_assets.visibility_id / review_status_id"
      pattern: "LEFT JOIN (visibilities|review_statuses)"
    - from: "media_ownership_projection_repository.go"
      to: "media_assets.owner_member_id"
      via: "Owner-Scope WHERE (IDOR-Schutz)"
      pattern: "owner_member_id = \\$"
    - from: "main.go"
      to: "EXAKTE GET-Route: GET /api/v1/media-ownership/:ownerType/:ownerId"
      via: "v1.GET registration (GET-only, kein POST/PATCH/DELETE)"
      pattern: "\\.GET\\(\"/media-ownership/:ownerType/:ownerId\""
    - from: "media_ownership_projection_handler.go"
      to: "Response-Envelope-Form: DIREKTES DTO, KEIN {\"data\":...}-Wrapper"
      via: "c.JSON(http.StatusOK, response)"
      pattern: "c\\.JSON\\(http\\.StatusOK, response\\)"
---

<objective>
Baue die Medien-Ownership-Lese-Projektion, die pro Asset Owner-Typ, Owner-ID, Medienkategorie, Sichtbarkeit (`visibilities`) und Review-Status (`review_statuses`) als konsumierbare DTO liefert — komponiert pro Junction-Kontext, ohne ein zentrales Owner-Typ-Feld zu erzwingen (Lock A/G).

Purpose: Erfüllt Entscheidung G (Ownership-Matrix) und liefert das Medien-Lese-Aggregat, das die UI-Surfaces 73/74/75/79/80 zur sichtbaren Owner-/Kategorie-/Sichtbarkeits-/Review-Pflicht (Entscheidung 8) konsumieren.
Output: Neues Medien-Projektions-Repo + dünner GET-Handler + Wave-0-Test + GET-only Wiring.

GEPINNTER CONTRACT (verbindlich, Plan 04 spiegelt exakt dies — kein Beispiel):
- GET-Route: `GET /api/v1/media-ownership/:ownerType/:ownerId` (GET-only, D-05).
- Envelope: DIREKTES DTO (Liste der Rows), KEIN `{"data":...}`-Wrapper (folgt dem benachbarten contributions_public_handler-Analog, RESEARCH Q3 → no-envelope).
- Sichtbarkeit/Review ausschließlich per FK-Lookup-Join (media_assets.visibility_id → visibilities.name, media_assets.review_status_id → review_statuses.code).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-dom-nen-projektionen-status-fundament/72-RESEARCH.md
@.planning/phases/72-dom-nen-projektionen-status-fundament/72-PATTERNS.md

<interfaces>
<!-- Projektions-Muster + Medien-Wahrheit — Executor nutzt direkt -->
From backend/internal/repository/anime_contributions_public_repository.go (DTO-Shape + Error-Wrapping):
- snake_case json-Tags Pflicht; fmt.Errorf("context: %w", err) auf Query/Scan/rows.Err()
From backend/internal/repository/member_archive_repository.go:
- eigene Struct + Konstruktor-Muster für NEUES Repo
From backend/internal/handlers/contributions_public_handler.go:
- dünner Handler, parse*ID/badRequest/internalError-Helper, c.JSON ohne Envelope (GEPINNT)

<!-- no-DB Test-Pattern (load-bearing, kein t.Skip) -->
From backend/internal/repository/runtime_authority_test.go (CANONICAL no-DB Pattern):
- readRepositorySource(t, "media_ownership_projection_repository.go") liefert den Quelltext; strings.ToLower + strings.Contains je erwartetem Fragment, sonst t.Fatalf. Kein DB-Pool, kein Skip.

Medien-Owner-Matrix (v1.2-DISCUSSION Entscheidung 8 / RESEARCH §Don't Hand-Roll, Open Question Q4):
- Member-Medien: media_assets.owner_member_id (0090, bereits vorhanden) — NICHT neu anlegen
- Gruppen-Medien: fansub_group_media Junction
- Release-Version-Medien: release_version_media Junction
- Release-Theme-Assets: release_theme_assets Junction
- Owner-TYP ist HEUTE NICHT als Spalte in media_assets modelliert → pro Junction-Kontext komponieren, KEIN neues zentrales Owner-Typ-Feld erzwingen (Lock A/G)
- Sichtbarkeit aus 0096: media_assets.visibility_id -> visibilities.name
- Review aus 0096: media_assets.review_status_id -> review_statuses.code/label_de
- media_assets.status (processing/ready/failed/deleted) ist TECHNISCH, NICHT Review — getrennt halten (RESEARCH Pitfall 2)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 Source-Fragment-Test für Owner/Visibility/Review-Achsen + Owner-Scope (no-DB)</name>
  <files>backend/internal/repository/media_ownership_projection_repository_test.go</files>
  <read_first>
    - backend/internal/repository/runtime_authority_test.go (VERBINDLICHES no-DB Muster: readRepositorySource + strings.Contains; KEIN DB-Pool, KEIN t.Skip)
    - .planning/phases/72-dom-nen-projektionen-status-fundament/72-RESEARCH.md (§Validation Architecture: D-03 VisibilityReview; §Security Domain IDOR)
  </read_first>
  <behavior>
    - Die Repo-Datei MUSS owner_type/owner_id/category/visibility/review_status als getrennte Felder projizieren.
    - Member-Owner-ID stammt aus media_assets.owner_member_id.
    - Owner-Scope ist im WHERE durchgesetzt (media_assets.owner_member_id = $N) → IDOR-Schutz.
  </behavior>
  <action>
    Schreibe den Test VOR der Implementierung (RED — readRepositorySource schlägt fehl, weil die Datei fehlt). KEIN DB-Pool, KEIN t.Skip — load-bearing Source-Fragment-Test nach dem Muster `runtime_authority_test.go`.

    `TestMediaProjectionExposesAllOwnershipAxes` — `content := readRepositorySource(t, "media_ownership_projection_repository.go")`; `normalized := strings.ToLower(content)`; assertiere per strings.Contains (sonst t.Fatalf):
      - json-Tags / Selektion für owner_type, owner_id, media_category, visibility, review_status.
      - "left join visibilities" und "left join review_statuses".
      - Negativ: "media_assets.status" wird NICHT als Review-Quelle selektiert (kein Umdeuten technischen Lifecycles).

    `TestMediaProjectionMemberOwnerFromOwnerMemberId` — assertiere "owner_member_id" als Quelle der Member-Owner-ID.

    `TestMediaProjectionRespectsOwnerScope` (IDOR, load-bearing) — assertiere, dass der Quelltext eine parametrisierte Owner-Scope-WHERE-Klausel enthält: "owner_member_id = $" (pgx-Parameter, kein String-Konkat). Negativ: keine Owner-Scope-Bildung via Format-/Sprintf-Konkatenation von User-Input (assertiere, dass "fmt.sprintf" NICHT zum Bau der WHERE-owner-Klausel genutzt wird — prüfe Abwesenheit von "owner_member_id = " ohne "$").

    Optional `TestMediaProjectionHandlerHasNoEnvelope` — readBackendSource handler-Datei; "c.json(http.statusok, response)" vorhanden, kein "\"data\"" Wrapper.

    Alle Fragmente sind kanonisch; Task 2 baut exakt diese Form.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/repository/... -run TestMediaProjection</automated>
  </verify>
  <acceptance_criteria>
    - Quell-Assertion: Datei deklariert TestMediaProjectionExposesAllOwnershipAxes, TestMediaProjectionMemberOwnerFromOwnerMemberId, TestMediaProjectionRespectsOwnerScope.
    - Behavior-Assertion: VOR Task 2 hart RED (readRepositorySource → t.Fatalf), NACH Task 2 grün. KEIN t.Skip im Test.
    - Quell-Assertion: Tests prüfen fünf Ownership-Achsen, FK-Lookup-Joins, owner_member_id-Quelle und die parametrisierte owner-scope-WHERE (IDOR).
  </acceptance_criteria>
  <done>Source-Fragment-Tests kompilieren, laufen ohne DB, sind load-bearing und sperren die fünf Ownership-Achsen, die Member-Owner-Quelle und den parametrisierten Owner-Scope (IDOR).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Medien-Ownership-Projektions-Repo + GET-Handler + Wiring</name>
  <files>backend/internal/repository/media_ownership_projection_repository.go, backend/internal/handlers/media_ownership_projection_handler.go, backend/cmd/server/main.go</files>
  <read_first>
    - backend/internal/repository/anime_contributions_public_repository.go (DTO-Shape, Error-Wrapping)
    - backend/internal/repository/member_archive_repository.go (eigene Struct+Konstruktor)
    - backend/internal/handlers/contributions_public_handler.go (dünner Handler, c.JSON ohne Envelope)
    - backend/cmd/server/main.go (Wiring-Muster ~364/397/403-405)
    - database/migrations/0096_v12_status_foundation.up.sql (media_assets.visibility_id/review_status_id aus Plan 01)
  </read_first>
  <behavior>
    - GetMediaOwnershipProjection(ctx, ownerType, ownerID) liefert []MediaOwnershipRow mit owner_type, owner_id, category, visibility, review_status pro Asset.
    - Member-Owner: owner_id == media_assets.owner_member_id; Owner-Scope wird in WHERE (owner_member_id = $N) durchgesetzt.
    - visibility aus LEFT JOIN visibilities, review_status aus LEFT JOIN review_statuses; media_assets.status (technisch) bleibt außen vor.
  </behavior>
  <action>
    Erstelle ein NEUES fokussiertes Repo (< 450 Zeilen, eigene Struct `MediaOwnershipProjectionRepository` + Konstruktor) nach dem `member_archive_repository.go`-Muster. DTO `MediaOwnershipRow` mit snake_case json-Tags: `owner_type`, `owner_id`, `media_category`, `visibility`, `review_status`, plus Asset-Basisfelder (id, url/path-Lesefeld). Die Projektion komponiert pro Junction-Kontext (RESEARCH Q4): owner_type/owner_id leiten sich aus der gewählten Junction (fansub_group_media / release_version_media / release_theme_assets) bzw. media_assets.owner_member_id ab — KEIN neues zentrales Owner-Typ-Feld in media_assets schreiben (Lock A/G). visibility via `LEFT JOIN visibilities v ON v.id = media_assets.visibility_id`; review_status via `LEFT JOIN review_statuses rs ON rs.id = media_assets.review_status_id`. Alle Parameter als pgx-`$N`; Owner-Scope strikt parametrisiert in WHERE (`media_assets.owner_member_id = $N` für member-scope) zur IDOR-Vermeidung — KEINE Sprintf-/String-Konkatenation von User-Input in der WHERE-Klausel.

    Dünner GET-Handler (`MediaOwnershipProjectionHandler`), parse-Helper für ownerType/ownerId, `c.JSON(http.StatusOK, response)` DIREKT (kein Envelope, GEPINNT). main.go: Repo → Handler → GET-only Route mit EXAKTEM Pfad `v1.GET("/media-ownership/:ownerType/:ownerId", handler.GetMediaOwnershipProjection)`. KEINE Schreib-Routen (D-05). Datei < 450 Zeilen.

    Der Source-Fragment-Test aus Task 1 prüft Achsen, FK-Lookup-Joins, owner_member_id-Quelle, parametrisierte owner-scope-WHERE und (optional) no-envelope — schreibe Repo/Handler so, dass alle Fragmente exakt vorkommen.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/repository/... -run TestMediaProjection</automated>
  </verify>
  <acceptance_criteria>
    - Quell-Assertion: media_ownership_projection_repository.go deklariert MediaOwnershipProjectionRepository + Konstruktor und DTO mit json-Tags owner_type/owner_id/media_category/visibility/review_status.
    - Quell-Assertion: Repo enthält LEFT JOIN visibilities und LEFT JOIN review_statuses; Member-Scope-WHERE referenziert owner_member_id = $N (parametrisiert).
    - Quell-Assertion: main.go registriert exakt `v1.GET("/media-ownership/:ownerType/:ownerId", ...)`; Handler antwortet mit direktem DTO (kein {"data"}).
    - Behavior-Assertion: `go build ./...` erfolgreich; TestMediaProjection grün.
    - Negativ-Assertion: keine neue Owner-Typ-Spalte in media_assets; keine .POST/.PATCH/.DELETE-Route; Datei < 450 Zeilen; media_assets.status NICHT als Review umgedeutet; keine String-Konkatenation in WHERE.
  </acceptance_criteria>
  <done>Medien-Projektion liefert fünf Ownership-Achsen pro Junction-Kontext, respektiert parametrisierten owner_member_id-Scope (IDOR); GET-only unter exaktem Pfad verdrahtet; no-envelope; Build + Source-Fragment-Tests grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Public/scoped GET-Read | ownerType/ownerID-Param betritt die Medien-Query |
| Projektion → Owner-Scope | fremde Member-Medien dürfen nicht exponiert werden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-03-01 | Information Disclosure / Elevation | IDOR auf Member-Medien | mitigate | Owner-Scope strikt parametrisiert in WHERE über owner_member_id = $N (etabliert seit 0090); Source-Fragment-Test TestMediaProjectionRespectsOwnerScope sperrt die WHERE-Klausel + verbietet String-Konkatenation |
| T-72-03-02 | Spoofing | Owner-Typ-Konfusion (Owner-Spoofing downstream) | mitigate | Owner-Typ/-ID pro Junction-Kontext eindeutig komponiert; kein freies zentrales Owner-Feld |
| T-72-03-03 | Tampering | ownerType/ownerID in SQL | mitigate | Ausschließlich pgx-`$N`-Parameter; Param-Parsing/Validierung im Handler |
| T-72-03-04 | Information Disclosure | technischer media-status als Review fehlinterpretiert | mitigate | Review nur aus review_statuses-FK; media_assets.status bleibt getrennt (Source-Fragment-Negativ-Assertion) (RESEARCH Pitfall 2) |
| T-72-03-SC | Tampering | npm/pip/cargo installs | accept | Keine Paket-Installs in dieser Phase |
</threat_model>

<verification>
- `cd backend && go build ./...` erfolgreich.
- `cd backend && go test ./internal/repository/... -run TestMediaProjection` grün (no-DB Source-Fragment-Test, load-bearing).
- main.go registriert nur GET `/media-ownership/:ownerType/:ownerId` für die Medien-Projektion.
</verification>

<success_criteria>
- Medien-Projektion liefert owner_type/owner_id/category/visibility/review_status pro Asset.
- Member-Owner aus owner_member_id; Owner-Scope parametrisiert durchgesetzt (IDOR); kein neues Owner-Typ-Feld.
- Keine Schreib-Endpunkte; Datei < 450 Zeilen; gepinnte Route + no-envelope.
</success_criteria>

<output>
Create `.planning/phases/72-dom-nen-projektionen-status-fundament/72-03-SUMMARY.md` when done
</output>
