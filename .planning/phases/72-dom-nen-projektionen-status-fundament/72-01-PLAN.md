---
phase: 72-dom-nen-projektionen-status-fundament
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/0096_v12_status_foundation.up.sql
  - database/migrations/0096_v12_status_foundation.down.sql
  - backend/internal/repository/v12_status_foundation_migration_test.go
autonomous: true
requirements: [J, G]
must_haves:
  truths:
    - "Migration 0096 enthält additive ADD COLUMN / CHECK / FK / CREATE TABLE Statements ohne UNIQUE-Kollision"
    - "members trägt einen lesbaren memorial-fähigen Profilstatus (Wert existiert, kein Setter)"
    - "anime_contributions trägt eine vom Content-status getrennte dispute_state-Dimension"
    - "Medien-/Contribution-Träger tragen visibility_id (FK -> visibilities) und review_status_id (FK -> review_statuses)"
    - "down.sql spiegelt up.sql exakt (jede Spalte/Tabelle/Constraint/Index gedroppt)"
    - "D-01: dispute_state ist eine separate Konflikt-Dimension neben dem inhaltlichen Content-status (kein Reuse von status='disputed')"
    - "D-03: zwei orthogonale Achsen als Schema — visibility_id (FK visibilities) und review_status_id (FK review_statuses), getrennt vom technischen media_assets.status"
    - "D-04: der review_statuses-Lookup stellt die Werte (in_review/approved/...) bereit, auf denen die spätere Auto-Freigabe-Politik (Phase 76/78) aufsetzt — keine Write-Politik in Phase 72"
    - "D-05: Phase 72 liefert ausschließlich Schema/Migrationen, keine Schreib-Endpunkte (memorial/dispute/review/visibility-Writes erst in den nutzenden Phasen)"
    - "D-06: nur der memorial-Statuswert existiert (lesbar, kein Setter, keine Claim-Sperre — diese kommen gemeinsam in Phase 74)"
  artifacts:
    - path: "database/migrations/0096_v12_status_foundation.up.sql"
      provides: "Additive Statusfelder (members.profile_status, anime_contributions.dispute_state, visibility_id + review_status_id auf Trägern, review_statuses Lookup)"
      contains: "ADD COLUMN IF NOT EXISTS"
    - path: "database/migrations/0096_v12_status_foundation.down.sql"
      provides: "Exakter Rollback aller 0096-Schemaänderungen"
      contains: "DROP COLUMN IF EXISTS"
    - path: "backend/internal/repository/v12_status_foundation_migration_test.go"
      provides: "Wave-0 Source-Fragment-Test (no-DB) gegen 0096 up/down SQL-Text"
      min_lines: 40
  key_links:
    - from: "anime_contributions.dispute_state"
      to: "CHECK constraint chk_anime_contributions_dispute_state"
      via: "ALTER TABLE ADD CONSTRAINT"
      pattern: "dispute_state IN \\('none','open','resolved'\\)"
    - from: "media_assets.visibility_id"
      to: "visibilities(id)"
      via: "FK REFERENCES"
      pattern: "REFERENCES visibilities\\(id\\)"
    - from: "media_assets.review_status_id / anime_contributions.review_status_id"
      to: "review_statuses(id)"
      via: "FK REFERENCES"
      pattern: "REFERENCES review_statuses\\(id\\)"
---

<objective>
Lege das append-only Schema-Fundament für Meilenstein v1.2 in einer einzigen Migration `0096` an: alle additiven Statusfelder, die die nutzenden Phasen 73–80 brauchen, plus einen Wave-0-Test, der den SQL-Text der Migration (up + down) per Source-Fragment-Pattern ohne Test-DB absichert.

Purpose: Ohne diese Felder kann keine Lese-Projektion (Plan 02/03) die getrennten Achsen Sichtbarkeit/Review/Konflikt/memorial liefern. Schema ist die Wurzel der Abhängigkeit.
Output: Migrationspaar `0096_v12_status_foundation.{up,down}.sql` + ein no-DB Source-Fragment-Test.

KANONISCHE TRÄGERFORM (verbindlich, nicht nachträglich änderbar): Sichtbarkeit und Review werden AUSSCHLIESSLICH als FK-Lookup geführt:
`visibility_id BIGINT REFERENCES visibilities(id)` und `review_status_id BIGINT REFERENCES review_statuses(id)`.
Es gibt KEINE denormalisierte `review_status VARCHAR`-Spalte. Die Projektionen (Plan 02/03) joinen den Lookup und projizieren den String. Task 1 und Task 2 assertieren beide exakt diese FK-Spaltennamen — keine Form-Entscheidung mehr in Task 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-dom-nen-projektionen-status-fundament/72-CONTEXT.md
@.planning/phases/72-dom-nen-projektionen-status-fundament/72-RESEARCH.md
@.planning/phases/72-dom-nen-projektionen-status-fundament/72-PATTERNS.md

<interfaces>
<!-- Bestehende Status-/CHECK-Wahrheit (Researcher-verifiziert) — Executor nutzt diese Werte direkt -->
- anime_contributions.status CHECK (0086): draft, proposed, confirmed, disputed, hidden  (CONTENT-Achse, UNVERÄNDERT lassen)
- media_assets.status CHECK (0059): processing, ready, failed, deleted  (TECHNISCH, UNVERÄNDERT lassen)
- visibilities Lookup (0037), Spalte name, Werte: public, registered, fansubber, staff, private
- media_assets.owner_member_id (0090): BIGINT FK -> members(id) ON DELETE SET NULL  (bereits vorhanden, NICHT neu anlegen)
- members heute: KEINE status-Spalte; vorhanden is_currently_active, profile_visibility, active_from_date, active_until_date
- 4-Spalten-UNIQUE auf anime_contributions mit NULLS NOT DISTINCT (0091): dispute_state darf NICHT Teil davon werden
- Nächste freie Migrationsnummer: 0096 (höchste vorhandene: 0095_archive_search_indexes)

<!-- KANONISCHE neue Spaltennamen (von Plan 02/03 als gegeben vorausgesetzt) -->
- members.profile_status VARCHAR(20) NOT NULL DEFAULT 'active'  CHECK IN (active, historical, memorial)
- anime_contributions.dispute_state VARCHAR(20) NOT NULL DEFAULT 'none'  CHECK IN (none, open, resolved)
- review_statuses(id BIGSERIAL PK, code VARCHAR UNIQUE NOT NULL, label_de VARCHAR NOT NULL, sort_order INT)  Seed: in_review/approved/rejected/archived/removed
- anime_contributions.visibility_id BIGINT REFERENCES visibilities(id) ON DELETE SET NULL
- anime_contributions.review_status_id BIGINT REFERENCES review_statuses(id) ON DELETE SET NULL
- media_assets.visibility_id BIGINT REFERENCES visibilities(id) ON DELETE SET NULL
- media_assets.review_status_id BIGINT REFERENCES review_statuses(id) ON DELETE SET NULL

<!-- Test-Pattern (no-DB Source-Fragment) — Vorbild verbindlich -->
From backend/internal/repository/runtime_authority_test.go (CANONICAL no-DB Pattern):
- Helper readBackendSource(t, relativePath) liest eine Datei relativ zum backend-Root via runtime.Caller(0); kein DB-Pool nötig.
- Pattern: content := readBackendSource(t, filepath.Join(...)); normalized := strings.ToLower(content); für jedes erwartete SQL-Fragment strings.Contains(normalized, fragment), sonst t.Fatalf.
- Migrations liegen unter database/migrations/ — relativ zum backend-Root: filepath.Join("..", "database", "migrations", "0096_v12_status_foundation.up.sql").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 Source-Fragment-Test gegen 0096 up/down SQL (no-DB)</name>
  <files>backend/internal/repository/v12_status_foundation_migration_test.go</files>
  <read_first>
    - backend/internal/repository/runtime_authority_test.go (VERBINDLICHES Muster: readBackendSource + strings.Contains-Fragmentprüfung, kein DB-Pool)
    - .planning/phases/72-dom-nen-projektionen-status-fundament/72-RESEARCH.md (§Validation Architecture — Wave 0 Gaps)
  </read_first>
  <action>
    Erstelle den Wave-0-Test VOR der Migration (RED, schlägt fehl weil die SQL-Dateien noch nicht existieren). KEIN Test-DB-Pool, KEIN t.Skip — der Test läuft immer und ist load-bearing. Verwende das Source-Fragment-Muster aus `runtime_authority_test.go`: lies die SQL-Dateien als Text und assertiere auf erwartete Fragmente.

    Da die Migrationen unter `database/migrations/` (außerhalb von backend/) liegen, definiere im Test einen kleinen Helper, der den Migrationsordner relativ zur Testdatei über `runtime.Caller(0)` auflöst (analog `readBackendSource`, aber Pfad `filepath.Join(backendRoot, "..", "database", "migrations", name)`). `os.ReadFile`, bei Fehler `t.Fatalf`.

    `TestV12StatusFoundationUpStatements` — liest `0096_v12_status_foundation.up.sql`, normalisiert auf lower-case und prüft per `strings.Contains` ALLE folgenden Fragmente (sonst `t.Fatalf` mit klarer Meldung):
      - "add column if not exists profile_status"
      - "members" und "check" und "'active'" und "'historical'" und "'memorial'"
      - "add column if not exists dispute_state"
      - "dispute_state in ('none','open','resolved')"
      - "create table" und "review_statuses"
      - alle fünf Seed-Codes: "'in_review'", "'approved'", "'rejected'", "'archived'", "'removed'"
      - "add column if not exists visibility_id" und "references visibilities(id)"
      - "add column if not exists review_status_id" und "references review_statuses(id)"

    `TestV12StatusFoundationDownMirrors` — liest `0096_v12_status_foundation.down.sql`, normalisiert, prüft `strings.Contains`:
      - "drop column if exists profile_status"
      - "drop column if exists dispute_state"
      - "drop column if exists visibility_id"
      - "drop column if exists review_status_id"
      - "drop table" und "review_statuses"

    `TestV12StatusFoundationLeavesContentStatusUntouched` — liest beide Dateien, NEGATIV-Assertion: die Fragmente "anime_contributions.status" und "media_assets.status" als ALTER-Ziel dürfen NICHT vorkommen (kein Umdeuten technischer/Content-Status); `dispute_state` darf in KEINEM "unique"-Fragment auftauchen.

    Spaltennamen sind hier KANONISCH FIXIERT (FK-Form): profile_status, dispute_state, visibility_id, review_status_id. Task 2 baut exakt diese — es gibt keine spätere Anpassung dieses Tests.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/repository/... -run TestV12StatusFoundation</automated>
  </verify>
  <acceptance_criteria>
    - Quell-Assertion: Test-Datei deklariert TestV12StatusFoundationUpStatements, TestV12StatusFoundationDownMirrors, TestV12StatusFoundationLeavesContentStatusUntouched.
    - Behavior-Assertion: VOR Task 2 schlägt der Test hart fehl (SQL-Dateien fehlen → t.Fatalf beim Lesen); NACH Task 2 grün. KEIN t.Skip-Pfad existiert im Test.
    - Quell-Assertion: geprüfte Spaltennamen sind exakt members.profile_status, anime_contributions.dispute_state, anime_contributions.visibility_id/review_status_id, media_assets.visibility_id/review_status_id; review/visibility ausschließlich in FK-Form (kein review_status VARCHAR).
  </acceptance_criteria>
  <done>Source-Fragment-Test kompiliert, läuft ohne DB, ist load-bearing (kein Skip), deckt up-Statements, down-Mirror und die Negativ-Invarianten ab und dient als ehrliches Wave-0-RED-Gate.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0096 — additive Statusfelder up/down</name>
  <files>database/migrations/0096_v12_status_foundation.up.sql, database/migrations/0096_v12_status_foundation.down.sql</files>
  <read_first>
    - database/migrations/0090_member_story_images.up.sql (additive Spalte + partial Index)
    - database/migrations/0090_member_story_images.down.sql (exakter Mirror)
    - database/migrations/0086_anime_contributions.up.sql (CHECK-Enum-Muster, Zeile chk_anime_contributions_status)
    - database/migrations/0037_add_release_decomposition_tables.up.sql (visibilities Lookup + visibility_id FK-Muster)
    - database/migrations/0091_anime_contributions_release_version.up.sql (4-Spalten-UNIQUE NULLS NOT DISTINCT — dispute_state NICHT hineinnehmen)
  </read_first>
  <action>
    Erstelle das append-only Migrationspaar `0096`. UP-Datei, alle Spalten `ADD COLUMN IF NOT EXISTS` mit `NOT NULL DEFAULT` wo sinnvoll (keine Backfill-Logik nötig). Die Trägerform ist FIXIERT (FK-Lookup) — KEINE Form-Wahl mehr:
    1. memorial-fähiger Member-Status (J/D-06): `members.profile_status VARCHAR(20) NOT NULL DEFAULT 'active'` + CHECK `chk_members_profile_status` IN ('active','historical','memorial'). claimed/unclaimed bleiben derived (NICHT in profile_status).
    2. Konflikt-Dimension (D-01, getrennt vom Content-status): `anime_contributions.dispute_state VARCHAR(20) NOT NULL DEFAULT 'none'` + CHECK `chk_anime_contributions_dispute_state` IN ('none','open','resolved'). NICHT in den 4-Spalten-UNIQUE aus 0091 aufnehmen.
    3. Review-Status-Lookup (D-03 Achse 2, RESEARCH Q1 → Lookup): neue Tabelle `review_statuses(id BIGSERIAL PRIMARY KEY, code VARCHAR UNIQUE NOT NULL, label_de VARCHAR NOT NULL, sort_order INT)` analog `visibilities`; seede die fünf Werte `in_review, approved, rejected, archived, removed` mit deutschen Labels (in Prüfung / freigegeben / abgelehnt / archiviert / entfernt).
    4. Achse-1-Sichtbarkeit + Achse-2-Review auf Contribution-Träger (D-03/G): `anime_contributions.visibility_id BIGINT REFERENCES visibilities(id) ON DELETE SET NULL` UND `anime_contributions.review_status_id BIGINT REFERENCES review_statuses(id) ON DELETE SET NULL`. KEINE denormalisierte VARCHAR-Lesespalte — die Projektion joint den Lookup.
    5. Achse-1/Achse-2 auf Medien-Träger (G/D-03): `media_assets.visibility_id BIGINT REFERENCES visibilities(id) ON DELETE SET NULL` UND `media_assets.review_status_id BIGINT REFERENCES review_statuses(id) ON DELETE SET NULL`. `media_assets.status` (technisch) und `media_assets.owner_member_id` (0090) UNVERÄNDERT lassen.
    6. Partielle Indizes analog 0090 auf die neuen FK-Spalten WHERE NOT NULL.
    DOWN-Datei spiegelt exakt: DROP INDEX, DROP CONSTRAINT, DROP COLUMN (profile_status, dispute_state, visibility_id, review_status_id auf beiden Trägern), DROP TABLE review_statuses — in umgekehrter Reihenfolge. Content-Status (anime_contributions.status) und media_assets.status NICHT anfassen (D-05/Scope, RESEARCH Pitfall 1+2+5).
    Der Source-Fragment-Test aus Task 1 prüft genau diesen Wortlaut (FK-Spaltennamen, CHECK-Enum-Werte, Seed-Codes, DROP-Mirror) — schreibe die SQL so, dass alle Fragmente exakt vorkommen.
  </action>
  <verify>
    <automated>cd backend && go run ./cmd/migrate up && go run ./cmd/migrate down 1 && go run ./cmd/migrate up && go test ./internal/repository/... -run TestV12StatusFoundation</automated>
  </verify>
  <acceptance_criteria>
    - Quell-Assertion: up.sql enthält `ADD COLUMN IF NOT EXISTS` für members.profile_status, anime_contributions.dispute_state, anime_contributions.visibility_id, anime_contributions.review_status_id, media_assets.visibility_id, media_assets.review_status_id; CREATE TABLE review_statuses mit 5 Seed-Werten.
    - Quell-Assertion: down.sql droppt jede in up.sql angelegte Spalte/Constraint/Tabelle/Index (exakter Mirror); review/visibility ausschließlich in FK-Form (kein review_status VARCHAR irgendwo).
    - Behavior-Assertion: up→down 1→up läuft fehlerfrei; danach TestV12StatusFoundation grün (Source-Fragment-Test, no-DB).
    - Negativ-Assertion: weder up.sql noch down.sql ändern anime_contributions.status oder media_assets.status; dispute_state taucht in KEINEM UNIQUE-Constraint auf.
  </acceptance_criteria>
  <done>Migration 0096 ist additiv, idempotent, exakt reversibel; alle neuen Statusachsen sind in FK-Lookup-Form als Schema vorhanden; Task-1-Source-Fragment-Test grün; Migrate-Roundtrip sauber.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → DB-Schema | Schemaänderung muss bestehende Reads/Constraints nicht brechen (append-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01-01 | Tampering | bestehender Content-status/technischer media-status | mitigate | Nur additive Spalten; CHECK auf neue Felder; Content-/Technik-Status unangetastet (Source-Fragment-Negativ-Assertion in Task 1) (RESEARCH Pitfall 1+2) |
| T-72-01-02 | Denial of Service | UNIQUE-Kollision (0091 NULLS NOT DISTINCT) | mitigate | dispute_state ausdrücklich NICHT in UNIQUE; Migrate-Roundtrip (up→down→up) im verify deckt es ab |
| T-72-01-03 | Information Disclosure | memorial-Status setzbar ohne Schutz | accept | Nur Statuswert, kein Setter/Endpoint in 72 (D-06); Schutz+Setter gemeinsam in Phase 74 |
| T-72-01-SC | Tampering | npm/pip/cargo installs | accept | Keine Paket-Installs in dieser Phase (RESEARCH: keine neuen Pakete) |
</threat_model>

<verification>
- `cd backend && go test ./internal/repository/... -run TestV12StatusFoundation` grün (no-DB Source-Fragment-Test, läuft immer).
- `cd backend && go run ./cmd/migrate up && go run ./cmd/migrate down 1 && go run ./cmd/migrate up` läuft sauber (Roundtrip gegen lokale docker-compose PostgreSQL 16).
- Manuelle Diff-Prüfung: down.sql spiegelt up.sql exakt.
</verification>

<success_criteria>
- Migration 0096 vorhanden, additiv, reversibel, ohne UNIQUE-Kollision.
- members.profile_status (memorial-fähig), anime_contributions.dispute_state, visibility_id + review_status_id (FK-Lookup) auf Contribution- und Medien-Trägern existieren.
- Content-status und technischer media-status unverändert.
- Wave-0-Test ist load-bearing (kein Skip) und liefert echtes RED→GREEN-Signal.
</success_criteria>

<output>
Create `.planning/phases/72-dom-nen-projektionen-status-fundament/72-01-SUMMARY.md` when done
</output>
