---
phase: 72-dom-nen-projektionen-status-fundament
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/0096_v12_status_foundation.up.sql
  - database/migrations/0096_v12_status_foundation.down.sql
  - backend/internal/repository/v12_status_foundation_migration_test.go
autonomous: true
requirements: [J, G]
must_haves:
  truths:
    - "Migration 0096 läuft up→down→up sauber durch ohne UNIQUE-Kollision"
    - "members trägt einen lesbaren memorial-fähigen Profilstatus (Wert existiert, kein Setter)"
    - "anime_contributions trägt eine vom Content-status getrennte dispute_state-Dimension"
    - "Medien-/Contribution-Träger tragen visibility_id (FK -> visibilities) und einen separaten review_status"
  artifacts:
    - path: "database/migrations/0096_v12_status_foundation.up.sql"
      provides: "Additive Statusfelder (members.profile_status, anime_contributions.dispute_state, visibility_id + review_status auf Trägern, review_statuses Lookup)"
      contains: "ADD COLUMN IF NOT EXISTS"
    - path: "database/migrations/0096_v12_status_foundation.down.sql"
      provides: "Exakter Rollback aller 0096-Schemaänderungen"
      contains: "DROP COLUMN IF EXISTS"
    - path: "backend/internal/repository/v12_status_foundation_migration_test.go"
      provides: "Wave-0-Roundtrip- und Spalten-Existenz-Test gegen Test-DB"
      min_lines: 30
  key_links:
    - from: "anime_contributions.dispute_state"
      to: "CHECK constraint chk_anime_contributions_dispute_state"
      via: "ALTER TABLE ADD CONSTRAINT"
      pattern: "dispute_state IN \\('none','open','resolved'\\)"
    - from: "media_assets.visibility_id"
      to: "visibilities(id)"
      via: "FK REFERENCES"
      pattern: "REFERENCES visibilities\\(id\\)"
---

<objective>
Lege das append-only Schema-Fundament für Meilenstein v1.2 in einer einzigen Migration `0096` an: alle additiven Statusfelder, die die nutzenden Phasen 73–80 brauchen, plus Wave-0-Tests, die das Roundtrip-Verhalten und die Spalten-Existenz absichern.

Purpose: Ohne diese Felder kann keine Lese-Projektion (Plan 02/03) die getrennten Achsen Sichtbarkeit/Review/Konflikt/memorial liefern. Schema ist die Wurzel der Abhängigkeit.
Output: Migrationspaar `0096_v12_status_foundation.{up,down}.sql` + ein Go-Roundtrip-Test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-dom-nen-projektionen-status-fundament/72-CONTEXT.md
@.planning/phases/72-dom-nen-projektionen-status-fundament/72-RESEARCH.md
@.planning/phases/72-dom-nen-projektionen-status-fundament/72-PATTERNS.md

<interfaces>
<!-- Bestehende Status-/CHECK-Wahrheit (Researcher-verifiziert) — Executor nutzt diese Werte direkt -->
- anime_contributions.status CHECK (0086): draft, proposed, confirmed, disputed, hidden  (CONTENT-Achse, UNVERÄNDERT lassen)
- media_assets.status CHECK (0059): processing, ready, failed, deleted  (TECHNISCH, UNVERÄNDERT lassen)
- visibilities Lookup (0037), Spalte name, Werte: public, registered, fansubber, staff, private
- media_assets.owner_member_id (0090): BIGINT FK -> members(id) ON DELETE SET NULL  (bereits vorhanden, NICHT neu anlegen)
- members heute: KEINE status-Spalte; vorhanden is_currently_active, profile_visibility, active_from_date, active_until_date
- 4-Spalten-UNIQUE auf anime_contributions mit NULLS NOT DISTINCT (0091): dispute_state darf NICHT Teil davon werden
- Nächste freie Migrationsnummer: 0096 (höchste vorhandene: 0095_archive_search_indexes)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0-Roundtrip- und Spalten-Existenz-Test</name>
  <files>backend/internal/repository/v12_status_foundation_migration_test.go</files>
  <read_first>
    - backend/internal/repository/test_helpers.go (Test-DB-Setup, falls vorhanden — Muster für Pool-Zugang)
    - backend/internal/repository/runtime_authority_test.go (Muster für DB-gestützten Repo-Test)
    - .planning/phases/72-dom-nen-projektionen-status-fundament/72-RESEARCH.md (§Validation Architecture — Wave 0 Gaps)
  </read_first>
  <action>
    Erstelle den Wave-0-Test VOR der Migration (RED). Test-Funktion `TestV12StatusFoundationColumns` prüft per `information_schema.columns`-Query gegen die Test-DB, dass nach Anwendung der Migrationen folgende Spalten existieren: `members.profile_status`, `anime_contributions.dispute_state`, und die je-Träger review/visibility-Spalten (`media_assets.review_status`, `media_assets.visibility_id`, `anime_contributions.review_status`, `anime_contributions.visibility_id`). Zusätzlich `TestV12StatusFoundationDefaults`: bestehende Zeilen erhalten gültige Default-Werte (`members.profile_status='active'`, `anime_contributions.dispute_state='none'`). Nutze das bestehende Test-DB-Setup-Muster (kein neues Framework). Falls kein Test-DB-Helper existiert, markiere `<automated>MISSING — Wave 0 muss test_helpers.go Pool-Setup bereitstellen</automated>` und lege einen `t.Skip`-fähigen Gerüsttest an, der bei verfügbarem Pool greift. Test referenziert die Spaltennamen, die Task 2 anlegt — Namen sind hier kanonisch festgelegt.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/repository/... -run TestV12StatusFoundation</automated>
  </verify>
  <acceptance_criteria>
    - Quell-Assertion: Test-Datei deklariert `TestV12StatusFoundationColumns` und `TestV12StatusFoundationDefaults`.
    - Behavior-Assertion: Vor Task 2 schlägt der Test fehl (Spalten fehlen) oder skippt sauber bei fehlendem Pool; nach Task 2 grün.
    - Die im Test geprüften Spaltennamen sind exakt: members.profile_status, anime_contributions.dispute_state, anime_contributions.review_status, anime_contributions.visibility_id, media_assets.review_status, media_assets.visibility_id.
  </acceptance_criteria>
  <done>Test kompiliert, läuft, deckt Spalten-Existenz + Defaults ab und dient als Wave-0-Gate.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0096 — additive Statusfelder up/down</name>
  <files>database/migrations/0096_v12_status_foundation.up.sql, database/migrations/0096_v12_status_foundation.down.sql</files>
  <read_first>
    - database/migrations/0090_member_story_images.up.sql (additive Spalte + partial Index)
    - database/migrations/0090_member_story_images.down.sql (exakter Mirror)
    - database/migrations/0086_anime_contributions.up.sql (CHECK-Enum-Muster, Zeile chk_anime_contributions_status)
    - database/migrations/0037_add_release_decomposition_tables.up.sql (visibilities Lookup + visibility_id FK-Muster)
    - database/migrations/0091_anime_contributions_release_version.up.sql (4-Spalten-UNIQUE NULLS NOT DISTINCT — dispute_state NICHT hineinnehmen)
  </read_first>
  <action>
    Erstelle das append-only Migrationspaar `0096`. UP-Datei, alle Spalten `ADD COLUMN IF NOT EXISTS` mit `NOT NULL DEFAULT` (keine Backfill-Logik nötig):
    1. memorial-fähiger Member-Status (J/D-06): `members.profile_status VARCHAR(20) NOT NULL DEFAULT 'active'` + CHECK `chk_members_profile_status` IN ('active','historical','memorial'). claimed/unclaimed bleiben derived (NICHT in profile_status).
    2. Konflikt-Dimension (D-01, getrennt vom Content-status): `anime_contributions.dispute_state VARCHAR(20) NOT NULL DEFAULT 'none'` + CHECK `chk_anime_contributions_dispute_state` IN ('none','open','resolved'). NICHT in den 4-Spalten-UNIQUE aus 0091 aufnehmen.
    3. Review-Status-Lookup (D-03 Achse 2, RESEARCH Q1 Empfehlung Lookup): neue Tabelle `review_statuses(id BIGSERIAL PK, code VARCHAR UNIQUE NOT NULL, label_de VARCHAR NOT NULL, sort_order INT)` analog `visibilities`; seede die fünf Werte `in_review, approved, rejected, archived, removed` mit deutschen Labels (in Prüfung / freigegeben / abgelehnt / archiviert / entfernt).
    4. Achse-1-Sichtbarkeit + Achse-2-Review auf Contribution-Träger (D-03/G): `anime_contributions.visibility_id BIGINT REFERENCES visibilities(id) ON DELETE SET NULL` und `anime_contributions.review_status_id BIGINT REFERENCES review_statuses(id) ON DELETE SET NULL` (zusätzlich eine denormalisierte `review_status VARCHAR(20)`-Lesespalte ist NICHT nötig — Projektion joint Lookup; aber Task-1-Test erwartet eine abfragbare review_status-Repräsentation: liefere die Spalte `review_status_id` und passe den Test-Spaltennamen in Task 1 auf `review_status_id`/`visibility_id` an, falls du die reine-FK-Form wählst).
    5. Achse-1/Achse-2 auf Medien-Träger (G/D-03): `media_assets.visibility_id BIGINT REFERENCES visibilities(id) ON DELETE SET NULL` und `media_assets.review_status_id BIGINT REFERENCES review_statuses(id) ON DELETE SET NULL`. `media_assets.status` (technisch) und `media_assets.owner_member_id` (0090) UNVERÄNDERT lassen.
    6. Partielle Indizes analog 0090 auf die neuen FK-Spalten WHERE NOT NULL.
    DOWN-Datei spiegelt exakt: DROP INDEX, DROP CONSTRAINT, DROP COLUMN, DROP TABLE review_statuses — in umgekehrter Reihenfolge. Content-Status (anime_contributions.status) und media_assets.status NICHT anfassen (D-05/Scope, RESEARCH Pitfall 1+2+5). Wähle EINE konsistente Trägerform für review/visibility (FK-Lookup empfohlen) und halte Task-1-Testspalten damit synchron.
  </action>
  <verify>
    <automated>cd backend && go run ./cmd/migrate up && go run ./cmd/migrate down 1 && go run ./cmd/migrate up && go test ./internal/repository/... -run TestV12StatusFoundation</automated>
  </verify>
  <acceptance_criteria>
    - Quell-Assertion: up.sql enthält `ADD COLUMN IF NOT EXISTS` für members.profile_status, anime_contributions.dispute_state, anime_contributions.visibility_id, anime_contributions.review_status_id, media_assets.visibility_id, media_assets.review_status_id; CREATE TABLE review_statuses mit 5 Seed-Werten.
    - Quell-Assertion: down.sql droppt jede in up.sql angelegte Spalte/Constraint/Tabelle/Index (exakter Mirror).
    - Behavior-Assertion: up→down 1→up läuft fehlerfrei; danach TestV12StatusFoundation grün.
    - Negativ-Assertion: weder up.sql noch down.sql ändern anime_contributions.status oder media_assets.status; dispute_state taucht in KEINEM UNIQUE-Constraint auf.
  </acceptance_criteria>
  <done>Migration 0096 ist additiv, idempotent, exakt reversibel; alle neuen Statusachsen sind als Schema vorhanden; Task-1-Test grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → DB-Schema | Schemaänderung muss bestehende Reads/Constraints nicht brechen (append-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01-01 | Tampering | bestehender Content-status/technischer media-status | mitigate | Nur additive Spalten; CHECK auf neue Felder; Content-/Technik-Status unangetastet (RESEARCH Pitfall 1+2) |
| T-72-01-02 | Denial of Service | UNIQUE-Kollision (0091 NULLS NOT DISTINCT) | mitigate | dispute_state ausdrücklich NICHT in UNIQUE; Roundtrip-Test deckt Migration ab |
| T-72-01-03 | Information Disclosure | memorial-Status setzbar ohne Schutz | accept | Nur Statuswert, kein Setter/Endpoint in 72 (D-06); Schutz+Setter gemeinsam in Phase 74 |
| T-72-01-SC | Tampering | npm/pip/cargo installs | accept | Keine Paket-Installs in dieser Phase (RESEARCH: keine neuen Pakete) |
</threat_model>

<verification>
- `cd backend && go run ./cmd/migrate up && go run ./cmd/migrate down 1 && go run ./cmd/migrate up` läuft sauber.
- `cd backend && go test ./internal/repository/... -run TestV12StatusFoundation` grün.
- Manuelle Diff-Prüfung: down.sql spiegelt up.sql exakt.
</verification>

<success_criteria>
- Migration 0096 vorhanden, additiv, reversibel, ohne UNIQUE-Kollision.
- members.profile_status (memorial-fähig), anime_contributions.dispute_state, visibility_id + review-Lookup auf Contribution- und Medien-Trägern existieren und sind lesbar.
- Content-status und technischer media-status unverändert.
</success_criteria>

<output>
Create `.planning/phases/72-dom-nen-projektionen-status-fundament/72-01-SUMMARY.md` when done
</output>
