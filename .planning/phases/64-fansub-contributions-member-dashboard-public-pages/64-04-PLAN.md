---
phase: 64-fansub-contributions-member-dashboard-public-pages
plan: 04
type: execute
wave: 4
depends_on:
  - 64-01
  - 64-03
files_modified:
  - frontend/src/app/members/[slug]/page.tsx
  - frontend/src/components/profile/MemberRoleTimeline.tsx
  - frontend/src/components/profile/MemberBadgeChips.tsx
  - frontend/src/lib/api.ts
  - frontend/src/types/contributions.ts
autonomous: true
requirements:
  - P64-SC3
  - P64-SC5
  - P64-SC6

must_haves:
  truths:
    - "Öffentliches Member-Profil /members/[slug] zeigt Rollen-Timeline unterhalb des bestehenden Hero-Bereichs"
    - "Unverifizierte Einträge in der Rollen-Timeline tragen (historisch)-Label"
    - "Badge-Chips (founding_member, historical_leader, long_term_member) erscheinen im Member-Profil"
    - "Bestehende Phase-59-Komponenten (MemberProfileHero, MembershipsSection, etc.) bleiben unverändert"
    - "Leerer Zustand zeigt deutschsprachigen Hinweistext"
  artifacts:
    - path: "frontend/src/components/profile/MemberRoleTimeline.tsx"
      provides: "Rollen-Timeline-Komponente für öffentliches Member-Profil"
    - path: "frontend/src/components/profile/MemberBadgeChips.tsx"
      provides: "Badge-Chip-Anzeige mit drei Badge-Codes und Sichtbarkeits-Steuerung"
  key_links:
    - from: "frontend/src/app/members/[slug]/page.tsx"
      to: "/api/v1/members/:slug/contributions"
      via: "getMemberContributions() in api.ts"
    - from: "MemberBadgeChips.tsx"
      to: "/api/v1/me/badges/:badgeId/visibility"
      via: "PATCH für Badge-Ausblenden (nur wenn eigenes Profil)"
---

<objective>
Public Member-Profil Rollen-Timeline und Badge-Anzeige.

Purpose: Bestehende /members/[slug]-Seite um Rollen-Timeline und Badge-Chips erweitern ohne bestehende Phase-59-Komponenten zu ersetzen. Entspricht D-05 (Public Member-Profil LOCKED) aus 64-CONTEXT.md.
Output: MemberRoleTimeline, MemberBadgeChips, getMemberContributions-Helper, Integration in members/[slug]/page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/64-fansub-contributions-member-dashboard-public-pages/64-CONTEXT.md
@.planning/phases/62-fansub-contributions-admin-api/62-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Member-Contributions-Typen, API-Helper und Badge-Typen</name>
  <files>
    frontend/src/types/contributions.ts
    frontend/src/lib/api.ts
  </files>
  <read_first>
    - frontend/src/types/contributions.ts — bereits erstellte Typen aus Plan 02/03
    - frontend/src/lib/api.ts — getMemberProfile (Zeile 2633+) als Stil-Referenz für Member-Routen
    - .planning/phases/62-fansub-contributions-admin-api/62-CONTEXT.md — /members/:slug/contributions Response-Struktur
    - .planning/phases/61-fansub-contributions-datenmodell/61-CONTEXT.md — member_badges Felder
  </read_first>
  <action>
    Typen in frontend/src/types/contributions.ts ergänzen:

    ```
    PublicMemberRoleEntry {
      fansub_group_name: string
      fansub_group_slug: string
      role_code: string
      role_label: string
      context: 'group_history' | 'anime_contribution'
      anime_title: string | null        // nur bei anime_contribution
      anime_id: number | null
      started_year: number | null
      ended_year: number | null
      status: string                    // 'historical' | 'confirmed' | etc.
    }

    PublicMemberContributionsResponse {
      role_timeline: PublicMemberRoleEntry[]
      has_unverified: boolean           // true wenn mindestens ein status='historical'
    }

    MemberBadge {
      id: number
      badge_code: 'founding_member' | 'historical_leader' | 'long_term_member'
      badge_category: string
      visibility: 'public' | 'internal' | 'hidden'
      awarded_at: string
    }

    MemberBadgesResponse {
      badges: MemberBadge[]
    }
    ```

    In frontend/src/lib/api.ts ergänzen:

    - getMemberContributions(slug: string): Promise<PublicMemberContributionsResponse> — GET /api/v1/members/:slug/contributions ohne Auth (Public-Route)
    - getMyBadges(token: string): Promise<MemberBadgesResponse> — GET /api/v1/me/badges (Route prüfen ob aus Phase 62 vorhanden — falls nicht: Stub der leere Liste zurückgibt)
    - patchMyBadgeVisibility(token: string, badgeId: number, visibility: string): Promise<void> — PATCH /api/v1/me/badges/:badgeId/visibility (aus Plan 01 gebaut)
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | grep -v "^$" | head -20</automated>
  </verify>
  <done>
    PublicMemberRoleEntry, PublicMemberContributionsResponse, MemberBadge exportiert. getMemberContributions und patchMyBadgeVisibility in api.ts vorhanden. TypeScript kompiliert.
  </done>
</task>

<task type="auto">
  <name>Task 2: MemberRoleTimeline, MemberBadgeChips und Seitenintegration</name>
  <files>
    frontend/src/components/profile/MemberRoleTimeline.tsx
    frontend/src/components/profile/MemberBadgeChips.tsx
    frontend/src/app/members/[slug]/page.tsx
  </files>
  <read_first>
    - frontend/src/app/members/[slug]/page.tsx — vollständige bestehende Seite (lesen vor jeder Änderung)
    - frontend/src/components/profile/MemberProfileHero.tsx — bestehender Komponenten-Stil
    - frontend/src/components/profile/MembershipsSection.tsx — Card-Komponenten-Muster
    - frontend/src/components/profile/profile.module.css — bestehende CSS-Klassen
    - .planning/phases/64-fansub-contributions-member-dashboard-public-pages/64-CONTEXT.md — D-05 Layout, Badge-Chip-Design (★ ♦ ◆ Icons)
  </read_first>
  <action>
    Komponente frontend/src/components/profile/MemberRoleTimeline.tsx:
    - Props: entries: PublicMemberRoleEntry[], hasUnverified: boolean
    - Wenn entries leer: Hinweistext
      "Noch keine Contributions eingetragen. Du warst früher in einer Fansub-Gruppe aktiv? Bitte deinen ehemaligen Leader, dich einzutragen."
    - Für jeden Eintrag sortiert nach started_year (älteste zuerst, NULL zuletzt):
      Zeile: Jahr + Gruppenname + Rollen-Label
      Bei anime_contribution: "+ Anime: {anime_title}"
      Unverified: status === 'historical' → kleiner grauer Text "(historisch)" hinter dem Namen
    - Wenn hasUnverified: Disclaimer-Zeile am Ende "⚠ Einige Angaben historisch ungeprüft" (kleiner Text, kein roter Kasten — D-08 LOCKED)

    Komponente frontend/src/components/profile/MemberBadgeChips.tsx:
    - Props: badges: MemberBadge[], isOwnProfile: boolean, token?: string, onVisibilityChanged?: (badgeId: number, visibility: string) => void
    - Wenn badges leer: null rendern (kein leerer Container)
    - Pro Badge ein Chip mit Icon und Label:
      - founding_member: "★ Gründungsmitglied"
      - historical_leader: "♦ Historischer Leader"
      - long_term_member: "◆ 5+ Jahre Mitglied"
    - Wenn isOwnProfile=true: kleiner "Ausblenden"-Link neben jedem Badge
      - Klick → patchMyBadgeVisibility(token, badge.id, 'hidden') → onVisibilityChanged-Callback
    - Chips mit visibility='hidden' werden nicht gerendert (Backend filtert bereits, aber sicherheitshalber auch im Frontend)

    Integration in frontend/src/app/members/[slug]/page.tsx:
    - WICHTIG: Keine bestehenden Komponenten (MemberProfileHero, MembershipsSection, RecentContributionsSection, RecentMediaSection) ändern oder entfernen
    - getMemberContributions(slug) aufrufen (try/catch — bei Fehler null)
    - Wenn eigenes Profil (OwnProfileEditLink-Logik): getMyBadges(token) aufrufen
    - MemberBadgeChips direkt nach MemberProfileHero einfügen (vor profileGrid)
    - MemberRoleTimeline in profileGrid als neue section nach fullWidthSection einfügen
    - Kein neues Layout-Element das bestehende Sections verschiebt — nur append

    Alle deutschen Strings korrekt. Max 450 Zeilen in jeder Datei.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | grep -v "^$" | head -20</automated>
  </verify>
  <done>
    TypeScript kompiliert. MemberRoleTimeline ist in /members/[slug] unterhalb MemberProfileHero sichtbar. MemberBadgeChips erscheint nach Hero. Bestehende Phase-59-Komponenten sind unverändert. (historisch)-Label erscheint bei status='historical'. Badge-Ausblenden-Funktion ist nur bei isOwnProfile=true sichtbar.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Browser → /api/v1/members/:slug/contributions | Öffentliche Route |
| Member-Browser → PATCH /me/badges/:id/visibility | Eingeloggter Member |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-64-04-01 | Info Disclosure | MemberRoleTimeline | mitigate | Nur Daten rendern die vom Public-Endpoint zurückgegeben werden — kein clientseitiges Filtern nötig |
| T-64-04-02 | Elevation | Badge ausblenden | mitigate | isOwnProfile-Flag prüfen vor Rendern des Ausblenden-Links; patchMyBadgeVisibility braucht gültigen Token |
| T-64-04-03 | Tampering | MemberBadgeChips | mitigate | Badge-Ownership-Prüfung erfolgt im Backend (WHERE member_id=$2 in SQL aus Plan 01) |
| T-64-04-SC | Tampering | npm deps | accept | Keine neuen Packages |
</threat_model>

<verification>
npx tsc --noEmit muss ohne Fehler durchlaufen.
/members/[slug] zeigt MemberRoleTimeline nach dem Hero-Bereich.
Bestehende Phase-59-Komponenten (MemberProfileHero etc.) werden nicht verändert.
</verification>

<success_criteria>
- /members/[slug] zeigt Rollen-Timeline unterhalb Hero (D-05 per CONTEXT.md)
- Unverifizierte Einträge haben (historisch)-Label ohne roten Kasten
- Badge-Chips erscheinen mit ★ ♦ ◆ Icons und deutschen Labels
- Eigenes Profil zeigt "Ausblenden"-Aktion pro Badge
- Alle deutschen Strings korrekt (Umlaute: ä ö ü Ä Ö Ü ß)
- phase-59-Komponenten unverändert (kein Diff in MemberProfileHero.tsx, MembershipsSection.tsx, etc.)
</success_criteria>

<output>
Create `.planning/phases/64-fansub-contributions-member-dashboard-public-pages/64-04-SUMMARY.md` when done
</output>
