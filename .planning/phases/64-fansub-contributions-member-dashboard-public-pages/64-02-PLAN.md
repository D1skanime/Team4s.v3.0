---
phase: 64-fansub-contributions-member-dashboard-public-pages
plan: 02
type: execute
wave: 2
depends_on:
  - 64-01
files_modified:
  - frontend/src/app/me/contributions/page.tsx
  - frontend/src/components/contributions/MyContributionsSection.tsx
  - frontend/src/components/contributions/ContributionCard.tsx
  - frontend/src/components/contributions/VisibilityDropdown.tsx
  - frontend/src/lib/api.ts
  - frontend/src/types/contributions.ts
autonomous: true
requirements:
  - P64-SC1

must_haves:
  truths:
    - "Eingeloggter Member sieht /me/contributions mit drei Sektionen: Bestätigt, Ausstehend, Eigene Vorschläge (leer)"
    - "Ausstehende Contributions zeigen [Bestätigen]- und [Ablehnen]-Buttons"
    - "Bestätigte Contributions haben Sichtbarkeits-Dropdown: Öffentlich / Nur intern"
    - "Ablehnen setzt is_public_on_member_profile=false ohne Löschung"
  artifacts:
    - path: "frontend/src/app/me/contributions/page.tsx"
      provides: "Route /me/contributions, Server-Component, ruft GET /api/v1/me/anime-contributions auf"
    - path: "frontend/src/components/contributions/MyContributionsSection.tsx"
      provides: "Client-Component mit Bestätigen/Ablehnen/Sichtbarkeit-Interaktionen"
    - path: "frontend/src/types/contributions.ts"
      provides: "MeAnimeContribution, MeGroupContribution Typen"
  key_links:
    - from: "frontend/src/app/me/contributions/page.tsx"
      to: "/api/v1/me/anime-contributions"
      via: "getMyAnimeContributions() in api.ts"
    - from: "MyContributionsSection.tsx"
      to: "/api/v1/me/anime-contributions/:id/visibility"
      via: "patchContributionVisibility() in api.ts"
---

<objective>
Member-Dashboard /me/contributions für eigene Contributions.

Purpose: Member sieht eigene Contributions (bestätigt, ausstehend), kann bestätigen/ablehnen und Sichtbarkeit steuern. Entspricht D-01 (Member-Dashboard LOCKED) aus 64-CONTEXT.md.
Output: Route /me/contributions, Client-Komponenten für Interaktion, Typen, API-Helpers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/64-fansub-contributions-member-dashboard-public-pages/64-CONTEXT.md
@.planning/phases/62-fansub-contributions-admin-api/62-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contributions-Typen und API-Helpers</name>
  <files>
    frontend/src/types/contributions.ts
    frontend/src/lib/api.ts
  </files>
  <read_first>
    - frontend/src/types/profile.ts — bestehende Typen-Struktur
    - frontend/src/lib/api.ts — authorizedFetch-Muster, ApiError-Konstruktion (Zeilen 1471–1530 für Stil-Referenz)
    - .planning/phases/62-fansub-contributions-admin-api/62-CONTEXT.md — Response-Struktur für /me/anime-contributions
  </read_first>
  <action>
    Typen in frontend/src/types/contributions.ts erstellen:

    ```
    MeAnimeContribution {
      id: number
      anime_title: string
      anime_id: number
      fansub_group_name: string
      fansub_group_id: number
      roles: string[]          // role_codes
      role_labels: string[]    // label_de Werte
      started_year: number | null
      ended_year: number | null
      status: 'confirmed' | 'proposed' | 'draft' | 'disputed' | 'hidden'
      is_public_on_member_profile: boolean
      entered_by_display_name: string | null   // für ausstehende Einträge
    }

    MeGroupContribution {
      id: number
      fansub_group_name: string
      fansub_group_id: number
      role_code: string
      role_label: string
      started_year: number | null
      ended_year: number | null
      status: string
      visibility: string
    }

    MeAnimeContributionsResponse { contributions: MeAnimeContribution[] }
    MeGroupContributionsResponse { contributions: MeGroupContribution[] }
    ```

    In frontend/src/lib/api.ts vier neue Funktionen anhängen (bestehenden authorizedFetch-Stil verwenden):

    - getMyAnimeContributions(token: string): Promise<MeAnimeContributionsResponse> — GET /api/v1/me/anime-contributions mit Bearer-Token
    - patchAnimeContributionVisibility(token: string, contributionId: number, isPublic: boolean): Promise<void> — PATCH /api/v1/me/anime-contributions/:id/visibility mit Body {is_public_on_member_profile: isPublic}
    - confirmAnimeContribution(token: string, contributionId: number): Promise<void> — PATCH /api/v1/me/anime-contributions/:id/visibility mit Body {confirmed: true} — Backend setzt is_public_on_member_profile nicht, nur Status-Feld — falls Backend-Route nicht existiert: 404 still catch
    - rejectAnimeContribution(token: string, contributionId: number): Promise<void> — PATCH /api/v1/me/anime-contributions/:id/visibility mit Body {is_public_on_member_profile: false}

    Hinweis: Die exakten PATCH-Body-Felder richten sich nach dem in Phase 62 gebauten Endpoint PATCH /api/v1/me/anime-contributions/:contributionId/visibility — vor dem Implementieren den tatsächlichen Handler prüfen falls vorhanden.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | grep -v "^$" | head -20</automated>
  </verify>
  <done>
    TypeScript kompiliert ohne Fehler. contributions.ts exportiert MeAnimeContribution, MeGroupContribution. api.ts hat vier neue Funktionen.
  </done>
</task>

<task type="auto">
  <name>Task 2: /me/contributions Seite und Komponenten</name>
  <files>
    frontend/src/app/me/contributions/page.tsx
    frontend/src/components/contributions/MyContributionsSection.tsx
    frontend/src/components/contributions/ContributionCard.tsx
    frontend/src/components/contributions/VisibilityDropdown.tsx
  </files>
  <read_first>
    - frontend/src/app/me/profile/page.tsx — Server-Component-Muster mit useAuthSession/cookies, bestehende Layout-Struktur
    - frontend/src/app/members/[slug]/page.tsx — Seitenaufbau-Stil
    - frontend/src/components/profile/MembershipsSection.tsx — Card-Komponenten-Stil
    - frontend/src/components/ui/index.ts oder ui-Verzeichnis — verfügbare UI-Primitives (Card, Button, etc.)
    - .planning/phases/64-fansub-contributions-member-dashboard-public-pages/64-CONTEXT.md — D-01 Layout-Spezifikation
  </read_first>
  <action>
    Server-Component frontend/src/app/me/contributions/page.tsx:
    - Cookie-basierter Token-Abruf (identisch mit /me/profile/page.tsx-Muster)
    - Aufruf: getMyAnimeContributions(token)
    - Bei nicht eingeloggt (kein Token oder 401): Redirect auf /login oder Hinweistext "Bitte einloggen."
    - Props an MyContributionsSection weitergeben

    Client-Component frontend/src/components/contributions/MyContributionsSection.tsx ('use client'):
    - Props: initialContributions: MeAnimeContribution[], token: string
    - Lokaler State: useState mit Contributions-Liste
    - Drei Abschnitte entsprechend D-01:
      1. "Bestätigte Beiträge (N)" — status === 'confirmed', zeigt ContributionCard mit VisibilityDropdown
      2. "Ausstehend — noch nicht bestätigt (N)" — status === 'proposed' oder 'draft', zeigt ContributionCard mit [Bestätigen] [Ablehnen] Buttons
      3. "Eigene Vorschläge (N)" — leer im MVP, zeigt Hinweistext "Beitrag vorschlagen folgt in Phase 65."
    - Bestätigen-Handler: confirmAnimeContribution → optimistischer State-Update
    - Ablehnen-Handler: rejectAnimeContribution → Beitrag aus "Ausstehend" entfernen (bleibt intern erhalten)
    - Fehlerbehandlung: Toast oder inline Fehlermeldung auf Deutsch

    Client-Component frontend/src/components/contributions/ContributionCard.tsx:
    - Props: contribution: MeAnimeContribution, mode: 'confirmed' | 'pending', onConfirm?, onReject?, onVisibilityChange?
    - Zeigt: Anime-Titel (fett), Gruppe, Rollen als kleine Chips (role_labels)
    - Bei mode='pending': kleiner Text "eingetragen von: {entered_by_display_name}" falls vorhanden
    - Kein Link zur Anime-Detailseite im MVP (nur Text)

    Client-Component frontend/src/components/contributions/VisibilityDropdown.tsx:
    - Props: contributionId: number, isPublic: boolean, token: string, onChanged: (isPublic: boolean) => void
    - HTML <select> mit zwei Optionen:
      - value="public": "Öffentlich im Member-Profil"
      - value="internal": "Nur intern sichtbar"
    - onChange: patchAnimeContributionVisibility → onChanged-Callback
    - Ladeindikator während API-Call (disabled während Loading)

    Max 450 Zeilen pro Datei — ContributionCard und VisibilityDropdown sind eigenständige Dateien. Alle deutschen Strings mit korrekten Umlauten.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | grep -v "^$" | head -20</automated>
  </verify>
  <done>
    TypeScript kompiliert. /me/contributions-Seite existiert. MyContributionsSection zeigt drei Sektionen. VisibilityDropdown ruft PATCH-Endpoint auf. Alle deutschen Texte korrekt.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Member-Browser → /api/v1/me/... | Eingeloggter Member mit Bearer-Token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-64-02-01 | Elevation | /me/contributions page | mitigate | Token-Prüfung in Server-Component, kein Rendering ohne Auth |
| T-64-02-02 | Tampering | patchAnimeContributionVisibility | mitigate | Token aus Cookie, keine clientseitige Token-Speicherung |
| T-64-02-SC | Tampering | npm deps | accept | Keine neuen Packages — nur bestehende React/Next.js-Primitives |
</threat_model>

<verification>
npx tsc --noEmit muss ohne Fehler durchlaufen.
Route /me/contributions ist erreichbar.
</verification>

<success_criteria>
- /me/contributions zeigt bestätigte und ausstehende Contributions
- [Bestätigen] und [Ablehnen] lösen korrekte API-Calls aus
- VisibilityDropdown aktualisiert is_public_on_member_profile
- Alle deutschen UI-Strings korrekt (keine ae/oe/ue-Ersetzungen)
</success_criteria>

<output>
Create `.planning/phases/64-fansub-contributions-member-dashboard-public-pages/64-02-SUMMARY.md` when done
</output>
