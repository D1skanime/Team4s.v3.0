---
phase: 64-fansub-contributions-member-dashboard-public-pages
plan: 03
type: execute
wave: 3
depends_on:
  - 64-01
files_modified:
  - frontend/src/app/fansubs/[slug]/page.tsx
  - frontend/src/components/fansubs/GroupLeaderTimeline.tsx
  - frontend/src/app/anime/[id]/page.tsx
  - frontend/src/components/anime/AnimeContributionsSection.tsx
  - frontend/src/components/anime/GroupContributionBlock.tsx
  - frontend/src/lib/api.ts
  - frontend/src/types/contributions.ts
autonomous: true
requirements:
  - P64-SC2
  - P64-SC4

must_haves:
  truths:
    - "Öffentliches Gruppenprofil /fansubs/[slug] zeigt Leader-Timeline ganz oben"
    - "Unverifizierte Einträge in der Leader-Timeline tragen (historisch)-Label"
    - "Anime-Detailseite zeigt Contributions-Bereich mit Mitwirkenden und Rollen-Chips pro Fansub-Gruppe"
    - "Nicht öffentliche Members werden aggregiert als 'N weitere nicht öffentlich' angezeigt — keine Namen"
    - "Progressive Disclosure: Kompaktansicht zuerst, 'Alle anzeigen' für Vollansicht"
  artifacts:
    - path: "frontend/src/components/fansubs/GroupLeaderTimeline.tsx"
      provides: "Leader-Timeline-Komponente mit (historisch)-Label für unverified entries"
    - path: "frontend/src/components/anime/AnimeContributionsSection.tsx"
      provides: "Contributions-Bereich auf Anime-Seite mit Progressive Disclosure"
    - path: "frontend/src/components/anime/GroupContributionBlock.tsx"
      provides: "Pro-Gruppe-Block mit Mitwirkenden-Liste und Aggregations-Zeile"
  key_links:
    - from: "frontend/src/app/fansubs/[slug]/page.tsx"
      to: "/api/v1/fansubs/:id/contributions"
      via: "getFansubContributions() in api.ts"
    - from: "frontend/src/app/anime/[id]/page.tsx"
      to: "/api/v1/anime/:id/contributions"
      via: "getAnimeContributions() in api.ts"
---

<objective>
Public Gruppenprofil Leader-Timeline und Anime-Seite Contributions-Bereich.

Purpose: Öffentliche Timelines für Gruppenprofil (Leader-Timeline) und Anime-Seite (Mitwirkende mit Rollen-Chips pro Gruppe). Entspricht D-04 (Public Gruppenprofil LOCKED) und D-06 (Public Anime-Seite LOCKED) aus 64-CONTEXT.md.
Output: GroupLeaderTimeline-Komponente, AnimeContributionsSection, GroupContributionBlock, API-Helpers, Integration in bestehende Seiten.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/64-fansub-contributions-member-dashboard-public-pages/64-CONTEXT.md
@.planning/phases/62-fansub-contributions-admin-api/62-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: API-Helpers für Public Contributions und Typen-Erweiterung</name>
  <files>
    frontend/src/types/contributions.ts
    frontend/src/lib/api.ts
  </files>
  <read_first>
    - frontend/src/types/contributions.ts — bereits erstellte Typen aus Plan 02 (oder erstellen falls Plan 02 noch nicht ausgeführt)
    - frontend/src/lib/api.ts — authorizedFetch-Muster, bestehende getFansubBySlug/getFansubMembers (Zeilen 1471–1530)
    - .planning/phases/62-fansub-contributions-admin-api/62-CONTEXT.md — Response-Struktur für Public-Routen: /fansubs/:id/contributions, /anime/:id/contributions
  </read_first>
  <action>
    Typen in frontend/src/types/contributions.ts ergänzen (oder erstellen falls nicht vorhanden):

    ```
    PublicAnimeContribution {
      member_display_name: string
      member_slug: string | null       // null wenn nicht öffentlich
      roles: string[]                  // role_codes
      role_labels: string[]
      started_year: number | null
      ended_year: number | null
      is_verified: boolean             // false = historisch ungeprüft
    }

    PublicFansubContributionsResponse {
      contributions: PublicAnimeContribution[]
      total_hidden_count: number       // Anzahl nicht öffentlicher Members
    }

    PublicFansubLeaderEntry {
      member_display_name: string
      member_slug: string | null
      role_code: string               // 'leader' | 'founder'
      role_label: string
      started_year: number | null
      ended_year: number | null
      status: string                  // 'historical' | 'confirmed' | etc.
    }

    PublicGroupContributionsResponse {
      leader_timeline: PublicFansubLeaderEntry[]
      anime_count: number
      member_count: number
    }

    AnimeContributionGroup {
      fansub_group_id: number
      fansub_group_name: string
      fansub_group_slug: string
      active_from_year: number | null
      active_until_year: number | null
      contributors: PublicAnimeContribution[]
      hidden_contributor_count: number
    }

    PublicAnimeContributionsResponse {
      groups: AnimeContributionGroup[]
    }
    ```

    In frontend/src/lib/api.ts neue Funktionen ergänzen (kein Bearer-Token, Public-Routen):

    - getFansubContributions(fansubID: number): Promise<PublicGroupContributionsResponse> — GET /api/v1/fansubs/:id/contributions ohne Auth
    - getAnimeContributions(animeID: number): Promise<PublicAnimeContributionsResponse> — GET /api/v1/anime/:id/contributions ohne Auth

    Beide Funktionen verwenden fetch ohne authorizedFetch (Public-Routen brauchen kein Token). Cache: 'no-store' oder revalidate: 60 nach Team4s-Stil.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | grep -v "^$" | head -20</automated>
  </verify>
  <done>
    Neue Typen in contributions.ts exportiert. getFansubContributions und getAnimeContributions in api.ts vorhanden. TypeScript kompiliert.
  </done>
</task>

<task type="auto">
  <name>Task 2: GroupLeaderTimeline, Anime-Contributions-Komponenten und Seitenintegration</name>
  <files>
    frontend/src/components/fansubs/GroupLeaderTimeline.tsx
    frontend/src/components/anime/AnimeContributionsSection.tsx
    frontend/src/components/anime/GroupContributionBlock.tsx
    frontend/src/app/fansubs/[slug]/page.tsx
    frontend/src/app/anime/[id]/page.tsx
  </files>
  <read_first>
    - frontend/src/app/fansubs/[slug]/page.tsx — bestehende Seitenstruktur (vollständig lesen)
    - frontend/src/app/anime/[id]/page.tsx — bestehende Anime-Detailseite (vollständig lesen)
    - frontend/src/components/fansubs/FansubProfileTabs.tsx — bestehende Tab-Struktur, um Integration zu verstehen
    - frontend/src/components/profile/MembershipsSection.tsx — Karten-/Chip-Stil als Referenz
    - .planning/phases/64-fansub-contributions-member-dashboard-public-pages/64-CONTEXT.md — D-04 und D-06 Layout-Spezifikation
  </read_first>
  <action>
    Komponente frontend/src/components/fansubs/GroupLeaderTimeline.tsx:
    - Props: entries: PublicFansubLeaderEntry[]
    - Wenn entries leer: "Noch keine Gruppenhistorie eingetragen. Als Leader kannst du die Historie unter [Gruppen-Admin] pflegen."
    - Für jeden Eintrag: Jahr (started_year oder "?") + Trennstrich + member_display_name + Rollen-Label
    - Unverified-Marker: wenn entry.status === 'historical', kleiner grauer Zusatz "(historisch)" direkt hinter dem Namen
    - Keine Progressive Disclosure für Leader-Timeline (bleibt immer vollständig sichtbar, D-04: "prominent ganz oben")

    Komponente frontend/src/components/anime/GroupContributionBlock.tsx:
    - Props: group: AnimeContributionGroup, expanded: boolean, onToggle: () => void
    - Header: Gruppenname (fett) + "Aktiv: {from}–{until}" rechts ausgerichtet
    - Mitwirkende: maximal 3 in Kompaktansicht (expanded=false)
    - Pro Mitwirkendem: member_display_name + Rollen als kleine Chips (role_labels)
    - Unverified: is_verified === false → kleines "(historisch)" hinter dem Namen
    - Aggregations-Zeile wenn hidden_contributor_count > 0: "(N weitere nicht öffentlich)" — kein Name, kein Link
    - "Alle anzeigen" Link/Button wenn contributors.length > 3 und !expanded
    - expanded=true: alle contributors anzeigen

    Komponente frontend/src/components/anime/AnimeContributionsSection.tsx:
    - Props: animeID: number — ruft getAnimeContributions(animeID) direkt auf (Server-Component oder Client-Component mit useEffect)
    - Bei leerer groups-Liste: "Noch keine Mitwirkenden eingetragen."
    - Für jede Gruppe: ein GroupContributionBlock
    - expandedGroupId State mit useState (Client-Component, 'use client')
    - Max 450 Zeilen

    Integration in frontend/src/app/fansubs/[slug]/page.tsx:
    - getFansubContributions(group.id) aufrufen
    - GroupLeaderTimeline als ersten sichtbaren Bereich nach dem Gruppen-Header einfügen (vor Tabs oder als erstes Tab-Inhalt — bestehende Tab-Struktur prüfen und passend integrieren ohne FansubProfileTabs zu ersetzen)
    - Bei Fehler in getFansubContributions: Timeline-Sektion überspringen, keine Fehlerseite

    Integration in frontend/src/app/anime/[id]/page.tsx:
    - AnimeContributionsSection am Ende der Seite ergänzen (nach bestehenden Sektionen)
    - Wird nur gerendert, kein Fehler wenn API nicht antwortet (try/catch, bei Fehler null)

    Alle deutschen Strings korrekt. Max 450 Zeilen pro Datei.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | grep -v "^$" | head -20</automated>
  </verify>
  <done>
    TypeScript kompiliert. GroupLeaderTimeline zeigt (historisch)-Label für status='historical'. GroupContributionBlock aggregiert hidden members korrekt. AnimeContributionsSection ist in Anime-Seite integriert. GroupLeaderTimeline ist in Fansub-Profil-Seite ganz oben.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Browser → /api/v1/fansubs/:id/contributions | Öffentliche Route, kein Auth |
| Browser → /api/v1/anime/:id/contributions | Öffentliche Route, kein Auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-64-03-01 | Info Disclosure | hidden_contributor_count | mitigate | Backend gibt nur Zahl, nie Namen nicht-öffentlicher Members zurück — Frontend prüft: kein member_slug rendern wenn null |
| T-64-03-02 | Info Disclosure | GroupContributionBlock | mitigate | member_display_name nur rendern wenn member_slug vorhanden — anonymous contributors bekommen keine persönlichen Angaben |
| T-64-03-SC | Tampering | npm deps | accept | Keine neuen Packages |
</threat_model>

<verification>
npx tsc --noEmit muss ohne Fehler durchlaufen.
GroupLeaderTimeline erscheint auf /fansubs/[slug].
AnimeContributionsSection erscheint auf /anime/[id].
</verification>

<success_criteria>
- /fansubs/[slug] zeigt Leader-Timeline ganz oben (D-04 per CONTEXT.md)
- Unverifizierte Einträge haben (historisch)-Label in kleiner Schrift
- /anime/[id] zeigt Contributions-Bereich mit Rollen-Chips
- Nicht öffentliche Members werden nur als "N weitere nicht öffentlich" angezeigt — nie namentlich
- Progressive Disclosure: maximal 3 Mitwirkende in Kompaktansicht
</success_criteria>

<output>
Create `.planning/phases/64-fansub-contributions-member-dashboard-public-pages/64-03-SUMMARY.md` when done
</output>
