---
phase: 26-segment-source-asset-upload-and-persistence
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx
  - frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.module.css
  - frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseSegments.ts
autonomous: true
requirements: [P26-SC1, P26-SC3, P26-SC4]
---

<objective>
Den Segment-Editor so erweitern, dass `release_asset` wirklich benutzbar wird: Datei auswaehlen, hochladen, sehen und entfernen.

Purpose: Admin kann Segment-Dateien direkt am Segment pflegen, ohne den alten Theme-Asset-Nebenpfad zu brauchen.
Output: Upload-UI, Asset-Anzeige und Delete-Aktion im Segment-Panel.
</objective>

<context>
@.planning/phases/26-segment-source-asset-upload-and-persistence/26-CONTEXT.md
@.planning/phases/26-segment-source-asset-upload-and-persistence/26-01-SUMMARY.md
@frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx
@frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.module.css
@frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseSegments.ts
@frontend/src/lib/api.ts
@frontend/src/types/admin.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upload-UI nur fuer gespeicherte release_asset-Segmente</name>
  <files>
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx,
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.module.css
  </files>
  <action>
Im Seitenpanel:
- `source_type === 'release_asset'`
- und nur im Edit-/persistierten Zustand (`editingSegment != null`)

dann zeige eine Segment-Datei-Sektion:
- erlaubte Formate nennen: MP4, WebM, MKV (kein Audio)
- max 150 MB nennen
- Datei auswaehlen
- Upload starten
- Upload-Fortschritt/Busy-State zeigen
- API-Fehler direkt anzeigen

Wichtig:
- fuer **neue** noch nicht gespeicherte Segmente nur Hilfetext:
  - erst Segment speichern
  - danach Datei hochladen

Es gibt bewusst noch kein Playback.
  </action>
  <done>Der Upload-Flow ist im Segment-Panel klar und benutzbar</done>
</task>

<task type="auto">
  <name>Task 2: Hinterlegte Datei anzeigen und entfernen</name>
  <files>
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.tsx,
    frontend/src/app/admin/episode-versions/[versionId]/edit/SegmenteTab.module.css
  </files>
  <action>
Wenn ein Segment bereits:
- `source_type = 'release_asset'`
- `source_ref != null`

dann:
- Dateiname/Label anzeigen
- optional den Storage-Pfad sekundär anzeigen
- Button `Datei entfernen`

Delete:
1. confirm
2. `deleteSegmentAsset(...)`
3. reload
4. Panelzustand aktualisieren

Quelle-Spalte in der Tabelle soll dann das `source_label` bzw. den Dateinamen lesbar zeigen, nicht nur `Release-Asset`.
  </action>
  <done>Bestehende Segment-Datei ist sichtbar und wieder entfernbar</done>
</task>

<task type="auto">
  <name>Task 3: Hook-State nach Asset-Aktionen sauber reloaden</name>
  <files>frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseSegments.ts</files>
  <action>
Stelle sicher:
- `reload()` ist verlässlich vorhanden
- Upload und Delete triggern Re-Fetch
- Panel bekommt danach die frischen Segmentdaten
- kein veralteter lokaler Source-Zustand bleibt stehen
  </action>
  <done>Segment-Asset-Zustand ist nach Upload/Delete konsistent sichtbar</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit`
- Upload-Sektion erscheint nur bei `release_asset`
- neue Segmente verlangen erst Speichern, dann Upload
- nach Upload ist Dateiname sichtbar
- nach Delete ist die Segment-Datei verschwunden
- API-Fehler werden im Panel sichtbar
</verification>

<success_criteria>
- Upload-UI ist klar und nur im richtigen Zustand sichtbar
- Admin kann Datei hochladen und direkt am Segment sehen
- Admin kann Datei wieder entfernen
- Tabelle zeigt sinnvolle Quellenanzeige fuer hinterlegte Dateien
</success_criteria>
