---
phase: 61-fansub-contributions-datenmodell
plan: 02
type: execute
wave: 2
depends_on:
  - 61-01
files_modified:
  - database/migrations/0083_hist_group_member_roles.up.sql
  - database/migrations/0083_hist_group_member_roles.down.sql
  - database/migrations/0084_fansub_group_history.up.sql
  - database/migrations/0084_fansub_group_history.down.sql
  - database/migrations/0085_role_definitions_seed.up.sql
  - database/migrations/0085_role_definitions_seed.down.sql
autonomous: true
requirements:
  - P61-SC1
  - P61-SC2
  - P61-SC3
  - P61-SC4

must_haves:
  truths:
    - "hist_group_member_roles verknuepft historische Zeitraeume mit Rollencodes aus role_definitions"
    - "fansub_group_history speichert Gruppen-Meilensteine mit year (INT) und event_type"
    - "role_definitions enthaelt alle operativen Rollen und neuen Gruppenrollen mit contexts TEXT[]"
    - "leader, co_leader, founder haben contexts = ARRAY['group_history']"
  artifacts:
    - path: "database/migrations/0083_hist_group_member_roles.up.sql"
      provides: "hist_group_member_roles-Tabelle mit FK auf hist_fansub_group_members"
    - path: "database/migrations/0084_fansub_group_history.up.sql"
      provides: "fansub_group_history-Tabelle fuer Gruppen-Meilensteine"
    - path: "database/migrations/0085_role_definitions_seed.up.sql"
      provides: "role_definitions-Lookup-Tabelle mit 15 Seed-Zeilen"
  key_links:
    - from: "hist_group_member_roles.hist_fansub_group_member_id"
      to: "hist_fansub_group_members.id"
      via: "REFERENCES hist_fansub_group_members(id) ON DELETE CASCADE"
    - from: "hist_group_member_roles.role_code"
      to: "role_definitions.code"
      via: "REFERENCES role_definitions(code) ON DELETE RESTRICT"
    - from: "fansub_group_history.fansub_group_id"
      to: "fansub_groups.id"
      via: "REFERENCES fansub_groups(id) ON DELETE CASCADE"
---

<objective>
Baut Schicht 2 des Datenbankfundaments: historische Rollenzettraeume pro Mitglied, Gruppen-Meilensteinhistorie und die zentrale role_definitions-Lookup-Tabelle mit vollstaendigen Seed-Daten.

Purpose: role_definitions ist die einzige Rollenwelt — keine Enums, kein separates contributor_roles-Duplikat. hist_group_member_roles und fansub_group_history ermoeglichen historische Praesenz von Gruppenleitern und Gruppen-Eckdaten.
Output: Drei reversible Migrationen (0083, 0084, 0085).
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/PROJECT.md
@C:/Users/admin/Documents/Team4s/.planning/ROADMAP.md
@C:/Users/admin/Documents/Team4s/.planning/phases/61-fansub-contributions-datenmodell/61-CONTEXT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/61-fansub-contributions-datenmodell/61-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0083 — hist_group_member_roles</name>

  <read_first>
    - database/migrations/0082_historical_fansub_group_members.up.sql — hist_fansub_group_members Struktur (Ziel-FK)
    - database/migrations/0073_fansub_group_app_memberships.up.sql — fansub_group_member_roles Muster (nur Referenz fuer Stil, nicht Vorlage zum Kopieren)
    - .planning/phases/61-fansub-contributions-datenmodell/61-CONTEXT.md — Abschnitt "Historische Gruppenrollen inkl. Leader-Zeiträume" Feldliste
  </read_first>

  <action>
  Erstelle database/migrations/0083_hist_group_member_roles.up.sql:

  Tabelle hist_group_member_roles:
  - id BIGSERIAL PRIMARY KEY
  - hist_fansub_group_member_id BIGINT NOT NULL REFERENCES hist_fansub_group_members(id) ON DELETE CASCADE
  - role_code TEXT NOT NULL REFERENCES role_definitions(code) ON DELETE RESTRICT
  - started_year INT NULL
  - ended_year INT NULL
  - status VARCHAR(20) NOT NULL DEFAULT 'historical', CHECK IN ('draft', 'historical', 'confirmed', 'disputed')
  - visibility VARCHAR(20) NOT NULL DEFAULT 'internal', CHECK IN ('internal', 'public')
  - confirmed_by BIGINT NULL REFERENCES app_users(id) ON DELETE SET NULL
  - confirmed_at TIMESTAMPTZ NULL
  - source_note TEXT NULL
  - created_by BIGINT NULL REFERENCES app_users(id) ON DELETE SET NULL
  - created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
  - updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
  - CONSTRAINT chk_hist_group_member_roles_years CHECK (ended_year IS NULL OR started_year IS NULL OR ended_year >= started_year)

  Indizes:
  - idx_hist_group_member_roles_member ON hist_group_member_roles(hist_fansub_group_member_id)
  - idx_hist_group_member_roles_code ON hist_group_member_roles(role_code)
  - idx_hist_group_member_roles_status ON hist_group_member_roles(status)

  HINWEIS: role_definitions (FK-Ziel) wird in Migration 0085 angelegt. Da SQL-Migrationen in Reihenfolge laufen und 0085 nach 0083 kommt, muss der FK auf role_definitions(code) in 0083 entweder:
  a) als DEFERRABLE INITIALLY DEFERRED definiert werden, ODER
  b) in einer separaten ALTER TABLE in 0085 nachgetragen werden.
  Bevorzuge Variante b: Lege hist_group_member_roles ohne den role_code-FK an (role_code TEXT NOT NULL, kein REFERENCES), und fuge den FK via ALTER TABLE in 0085 nach dem CREATE TABLE role_definitions hinzu.

  Erstelle database/migrations/0083_hist_group_member_roles.down.sql:
  - DROP TABLE IF EXISTS hist_group_member_roles
  </action>

  <verify>
    <automated>
      grep -c "hist_fansub_group_member_id" database/migrations/0083_hist_group_member_roles.up.sql
      grep -c "BIGSERIAL" database/migrations/0083_hist_group_member_roles.up.sql
      grep "ON DELETE CASCADE" database/migrations/0083_hist_group_member_roles.up.sql
    </automated>
  </verify>

  <acceptance_criteria>
    - 0083_hist_group_member_roles.up.sql und .down.sql existieren
    - id ist BIGSERIAL PRIMARY KEY
    - hist_fansub_group_member_id referenziert hist_fansub_group_members(id) ON DELETE CASCADE
    - role_code ist TEXT NOT NULL (FK wird in 0085 nachgetragen — kein FK-Fehler beim Anlegen der Tabelle)
    - status CHECK IN ('draft', 'historical', 'confirmed', 'disputed') vorhanden
    - Jahren-Check (ended_year >= started_year wenn beide NOT NULL) vorhanden
    - Drei Indizes (member, code, status) vorhanden
    - down.sql entfernt nur hist_group_member_roles (kein Eingriff in andere Tabellen)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrationen 0084 (fansub_group_history) und 0085 (role_definitions + Seeds + FK-Nachtraege)</name>

  <read_first>
    - database/migrations/0065_seed_contributor_roles_kernrollen.up.sql — bestehende operative Rollen (10 Codes + 'other'); diese muessen in role_definitions uebernommen werden
    - database/migrations/0073_fansub_group_app_memberships.up.sql — Stil fuer fansub_group_id-Referenzen
    - .planning/phases/61-fansub-contributions-datenmodell/61-CONTEXT.md — Abschnitt "Gruppenhistorie / Meilensteine" und "Seed-Daten fuer role_definitions"
  </read_first>

  <action>
  DATEI 1: database/migrations/0084_fansub_group_history.up.sql

  Tabelle fansub_group_history:
  - id BIGSERIAL PRIMARY KEY
  - fansub_group_id BIGINT NOT NULL REFERENCES fansub_groups(id) ON DELETE CASCADE
  - year INT NULL
  - event_type TEXT NOT NULL, CHECK IN ('founding', 'disbanding', 'hiatus', 'rebranding', 'milestone', 'other')
  - title TEXT NULL
  - note TEXT NULL
  - status VARCHAR(20) NOT NULL DEFAULT 'historical', CHECK IN ('draft', 'historical', 'confirmed', 'disputed')
  - created_by BIGINT NULL REFERENCES app_users(id) ON DELETE SET NULL
  - created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()

  Indizes:
  - idx_fansub_group_history_group ON fansub_group_history(fansub_group_id)
  - idx_fansub_group_history_year ON fansub_group_history(year)
  - idx_fansub_group_history_event_type ON fansub_group_history(event_type)

  Erstelle database/migrations/0084_fansub_group_history.down.sql:
  - DROP TABLE IF EXISTS fansub_group_history

  ---

  DATEI 2: database/migrations/0085_role_definitions_seed.up.sql

  Schritt 1 — role_definitions anlegen:
  Tabelle role_definitions:
  - code TEXT PRIMARY KEY
  - label_de TEXT NOT NULL
  - contexts TEXT[] NOT NULL DEFAULT '{}'
  - sort_order INT NOT NULL DEFAULT 0

  Schritt 2 — Operative Rollen seedden (contexts = ARRAY['anime_contribution']):
  INSERT INTO role_definitions (code, label_de, contexts, sort_order) VALUES
    ('translator',      'Übersetzung',        ARRAY['anime_contribution'], 10),
    ('editor',          'Editing',            ARRAY['anime_contribution'], 20),
    ('timer',           'Timing',             ARRAY['anime_contribution'], 30),
    ('typesetter',      'Typesetting / FX',   ARRAY['anime_contribution'], 40),
    ('encoder',         'Encoding',           ARRAY['anime_contribution'], 50),
    ('raw_provider',    'Raw-Bereitstellung', ARRAY['anime_contribution'], 60),
    ('quality_checker', 'Qualitätsprüfung',   ARRAY['anime_contribution'], 70),
    ('project_lead',    'Projektleitung',     ARRAY['anime_contribution', 'group_history'], 80),
    ('designer',        'Design',             ARRAY['anime_contribution'], 90),
    ('admin',           'Administration',     ARRAY['anime_contribution'], 100),
    ('other',           'Sonstiges',          ARRAY['anime_contribution'], 110)
  ON CONFLICT (code) DO UPDATE SET
    label_de = EXCLUDED.label_de,
    contexts = EXCLUDED.contexts,
    sort_order = EXCLUDED.sort_order;

  Schritt 3 — Neue historische Gruppenrollen seedden (contexts = ARRAY['group_history']):
  INSERT INTO role_definitions (code, label_de, contexts, sort_order) VALUES
    ('founder',          'Gründer/in',         ARRAY['group_history'], 1),
    ('leader',           'Gruppenleitung',      ARRAY['group_history'], 2),
    ('co_leader',        'Co-Leitung',          ARRAY['group_history'], 3),
    ('project_manager',  'Projektmanagement',   ARRAY['group_history', 'anime_contribution'], 75)
  ON CONFLICT (code) DO UPDATE SET
    label_de = EXCLUDED.label_de,
    contexts = EXCLUDED.contexts,
    sort_order = EXCLUDED.sort_order;

  Schritt 4 — FK auf role_definitions in hist_group_member_roles nachrüsten:
  ALTER TABLE hist_group_member_roles
    ADD CONSTRAINT fk_hist_group_member_roles_role_code
    FOREIGN KEY (role_code) REFERENCES role_definitions(code) ON DELETE RESTRICT;

  Erstelle database/migrations/0085_role_definitions_seed.down.sql:
  - ALTER TABLE hist_group_member_roles DROP CONSTRAINT IF EXISTS fk_hist_group_member_roles_role_code
  - DROP TABLE IF EXISTS role_definitions
  HINWEIS: Seed-Daten werden durch DROP TABLE mitentfernt.
  </action>

  <verify>
    <automated>
      grep -c "role_definitions" database/migrations/0085_role_definitions_seed.up.sql
      grep -c "founder" database/migrations/0085_role_definitions_seed.up.sql
      grep -c "group_history" database/migrations/0085_role_definitions_seed.up.sql
      grep -c "anime_contribution" database/migrations/0085_role_definitions_seed.up.sql
      grep -c "fk_hist_group_member_roles_role_code" database/migrations/0085_role_definitions_seed.up.sql
    </automated>
  </verify>

  <acceptance_criteria>
    - 0084_fansub_group_history.up.sql und .down.sql existieren
    - fansub_group_history.event_type hat CHECK auf 6 Werte inkl. 'founding', 'disbanding', 'milestone'
    - 0085_role_definitions_seed.up.sql und .down.sql existieren
    - role_definitions.code ist TEXT PRIMARY KEY (kein SERIAL, kein UUID)
    - role_definitions.contexts ist TEXT[] (kein DB-Enum)
    - Alle 11 operativen Rollen (translator, editor, timer, typesetter, encoder, raw_provider, quality_checker, project_lead, designer, admin, other) sind in 0085 vorhanden
    - Alle 4 Gruppenrollen (founder, leader, co_leader, project_manager) sind vorhanden
    - founder, leader, co_leader haben ausschliesslich 'group_history' im contexts-Array
    - project_lead hat ARRAY['anime_contribution', 'group_history']
    - ON CONFLICT DO UPDATE verhindert Fehler bei wiederholtem Ausfuehren
    - FK fk_hist_group_member_roles_role_code wird in 0085 als ALTER TABLE nachgetragen
    - down.sql entfernt erst den FK-Constraint, dann die role_definitions-Tabelle
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Migration runner → Datenbank | Nur Migration-Skripte duerfen Schema-Aenderungen vornehmen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-61-03 | Tampering | role_definitions.code (TEXT PK) | mitigate | TEXT PK verhindert ungueltigen Enum-Wechsel; Rollencodes sind stabil und werden nicht enum-migriert |
| T-61-04 | Denial of Service | fansub_group_history ohne year-Pflicht | accept | year nullable ist Designentscheidung (manche Ereignisse ohne bekanntes Jahr); kein Sicherheitsrisiko |
| T-61-SC | Tampering | npm/pip/cargo installs | accept | Keine Paketinstallation in dieser Phase (nur SQL-Migrationen) |
</threat_model>

<verification>
Nach Ausfuehrung der Migrationen 0083, 0084, 0085:

1. SELECT code, label_de, contexts FROM role_definitions ORDER BY sort_order; -- muss 15 Zeilen zeigen
2. SELECT code FROM role_definitions WHERE 'group_history' = ANY(contexts); -- muss founder, leader, co_leader, project_lead, project_manager zeigen
3. \d hist_group_member_roles -- muss FK fk_hist_group_member_roles_role_code auf role_definitions zeigen
4. \d fansub_group_history -- event_type-Check und fansub_group_id-FK sichtbar
5. Down-Test: 0085.down, 0084.down, 0083.down in dieser Reihenfolge — keine Fehler
</verification>

<success_criteria>
- Migrationen 0083, 0084, 0085 laufen fehlerfrei up und down
- role_definitions hat 15 Seed-Zeilen (11 operative + 4 historische Gruppenrollen)
- FK hist_group_member_roles.role_code → role_definitions.code ist aktiv
- fansub_group_history existiert mit event_type-Check und CASCADE auf fansub_groups
- Alle Umlaute in label_de korrekt (Ü, ö, ä, ß) — keine ASCII-Ersetzungen
</success_criteria>

<output>
Erstelle .planning/phases/61-fansub-contributions-datenmodell/61-02-SUMMARY.md wenn fertig.
</output>
