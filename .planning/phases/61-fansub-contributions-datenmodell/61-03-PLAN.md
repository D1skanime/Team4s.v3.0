---
phase: 61-fansub-contributions-datenmodell
plan: 03
type: execute
wave: 3
depends_on:
  - 61-01
  - 61-02
files_modified:
  - database/migrations/0086_anime_contributions.up.sql
  - database/migrations/0086_anime_contributions.down.sql
  - database/migrations/0087_anime_contribution_roles_and_badges.up.sql
  - database/migrations/0087_anime_contribution_roles_and_badges.down.sql
autonomous: true
requirements:
  - P61-SC1
  - P61-SC3
  - P61-SC4
  - P61-SC5

must_haves:
  truths:
    - "anime_contributions.fansub_group_member_id ist NOT NULL und referenziert hist_fansub_group_members(id)"
    - "anime_contribution_roles loescht sich automatisch wenn die uebergeordnete Contribution geloescht wird"
    - "member_badges hat UNIQUE(member_id, badge_code) und badge_category-Check"
    - "Alle IDs in allen neuen Tabellen sind BIGSERIAL"
  artifacts:
    - path: "database/migrations/0086_anime_contributions.up.sql"
      provides: "anime_contributions-Tabelle mit NOT NULL fansub_group_member_id"
    - path: "database/migrations/0087_anime_contribution_roles_and_badges.up.sql"
      provides: "anime_contribution_roles (CASCADE) und member_badges"
  key_links:
    - from: "anime_contributions.fansub_group_member_id"
      to: "hist_fansub_group_members.id"
      via: "REFERENCES hist_fansub_group_members(id) ON DELETE RESTRICT NOT NULL"
    - from: "anime_contributions.fansub_group_id"
      to: "fansub_groups.id"
      via: "REFERENCES fansub_groups(id) ON DELETE RESTRICT"
    - from: "anime_contributions.anime_id"
      to: "anime.id"
      via: "REFERENCES anime(id) ON DELETE RESTRICT"
    - from: "anime_contribution_roles.anime_contribution_id"
      to: "anime_contributions.id"
      via: "REFERENCES anime_contributions(id) ON DELETE CASCADE"
    - from: "member_badges.member_id"
      to: "members.id"
      via: "REFERENCES members(id) ON DELETE CASCADE"
---

<objective>
Schliesst das Datenbankfundament ab: anime_contributions als historisches Faktenregister fuer Anime-Beteiligung, anime_contribution_roles als Rollenzuordnung pro Contribution (mit CASCADE), und member_badges als gespeicherte Badge-Ausgabe mit derived_from-Feldern.

Purpose: Diese drei Tabellen sind der Kern des Phase-61-Ziels — ohne sie ist das Contributions-Modell unvollstaendig. Phase 62 (API) baut direkt auf diesen Tabellen auf.
Output: Zwei reversible Migrationen (0086, 0087) mit Tabellen, Constraints, Indizes.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/PROJECT.md
@C:/Users/admin/Documents/Team4s/.planning/ROADMAP.md
@C:/Users/admin/Documents/Team4s/.planning/phases/61-fansub-contributions-datenmodell/61-CONTEXT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/61-fansub-contributions-datenmodell/61-01-SUMMARY.md
@C:/Users/admin/Documents/Team4s/.planning/phases/61-fansub-contributions-datenmodell/61-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0086 — anime_contributions</name>

  <read_first>
    - database/migrations/0082_historical_fansub_group_members.up.sql — hist_fansub_group_members Struktur (Ziel fuer fansub_group_member_id FK)
    - .planning/phases/61-fansub-contributions-datenmodell/61-CONTEXT.md — Abschnitt "Anime-Contributions" Feldliste und Status-Modell
    - database/migrations/0044_add_db_schema_v2_target_tables.up.sql — pruefe ob anime-Tabellenname 'anime' oder 'animes' (nur erste 10 Zeilen noetig)
  </read_first>

  <action>
  Erstelle database/migrations/0086_anime_contributions.up.sql:

  Tabelle anime_contributions:
  - id BIGSERIAL PRIMARY KEY
  - fansub_group_id BIGINT NOT NULL REFERENCES fansub_groups(id) ON DELETE RESTRICT
  - anime_id BIGINT NOT NULL REFERENCES anime(id) ON DELETE RESTRICT
  - fansub_group_member_id BIGINT NOT NULL REFERENCES hist_fansub_group_members(id) ON DELETE RESTRICT
    -- NOT NULL ist PFLICHT (D-Locked: kein Gastmitwirkender in Phase 61)
  - status VARCHAR(20) NOT NULL DEFAULT 'draft',
    CHECK IN ('draft', 'proposed', 'confirmed', 'disputed', 'hidden')
  - note TEXT NULL
  - started_year INT NULL
  - ended_year INT NULL
  - is_public_on_anime_page BOOLEAN NOT NULL DEFAULT false
  - is_public_on_member_profile BOOLEAN NOT NULL DEFAULT false
  - confirmed_by BIGINT NULL REFERENCES app_users(id) ON DELETE SET NULL
  - confirmed_at TIMESTAMPTZ NULL
  - created_by BIGINT NULL REFERENCES app_users(id) ON DELETE SET NULL
  - created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
  - updated_by BIGINT NULL REFERENCES app_users(id) ON DELETE SET NULL
  - updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
  - CONSTRAINT chk_anime_contributions_years CHECK (ended_year IS NULL OR started_year IS NULL OR ended_year >= started_year)

  Indizes:
  - idx_anime_contributions_group ON anime_contributions(fansub_group_id)
  - idx_anime_contributions_anime ON anime_contributions(anime_id)
  - idx_anime_contributions_member ON anime_contributions(fansub_group_member_id)
  - idx_anime_contributions_status ON anime_contributions(status)
  - idx_anime_contributions_public_anime ON anime_contributions(is_public_on_anime_page) WHERE is_public_on_anime_page = true
  - idx_anime_contributions_public_profile ON anime_contributions(is_public_on_member_profile) WHERE is_public_on_member_profile = true

  Erstelle database/migrations/0086_anime_contributions.down.sql:
  - DROP TABLE IF EXISTS anime_contributions
  </action>

  <verify>
    <automated>
      grep "fansub_group_member_id BIGINT NOT NULL" database/migrations/0086_anime_contributions.up.sql
      grep -c "BIGSERIAL" database/migrations/0086_anime_contributions.up.sql
      grep -c "ON DELETE RESTRICT" database/migrations/0086_anime_contributions.up.sql
      grep "is_public_on_anime_page" database/migrations/0086_anime_contributions.up.sql
    </automated>
  </verify>

  <acceptance_criteria>
    - 0086_anime_contributions.up.sql und .down.sql existieren
    - fansub_group_member_id ist BIGINT NOT NULL (KEIN NULL, kein DEFAULT NULL)
    - fansub_group_member_id referenziert hist_fansub_group_members(id) ON DELETE RESTRICT
    - anime_id referenziert anime(id) ON DELETE RESTRICT (nicht animes — Tabellenname vor Schreiben pruefen)
    - status CHECK IN ('draft', 'proposed', 'confirmed', 'disputed', 'hidden') vorhanden
    - is_public_on_anime_page BOOLEAN NOT NULL DEFAULT false vorhanden
    - is_public_on_member_profile BOOLEAN NOT NULL DEFAULT false vorhanden
    - Sechs Indizes vorhanden (zwei davon als Partial Index WHERE = true)
    - Jahres-Check (ended_year >= started_year wenn beide NOT NULL) vorhanden
    - down.sql entfernt nur anime_contributions
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migration 0087 — anime_contribution_roles und member_badges</name>

  <read_first>
    - database/migrations/0086_anime_contributions.up.sql — anime_contributions Struktur (Ziel fuer anime_contribution_id FK)
    - database/migrations/0082_historical_fansub_group_members.up.sql — hist_fansub_group_members (Vorlage fuer visibility-Pattern)
    - .planning/phases/61-fansub-contributions-datenmodell/61-CONTEXT.md — Abschnitt "Badges: gespeichert mit derived_from" und "Kaskaden"
  </read_first>

  <action>
  Erstelle database/migrations/0087_anime_contribution_roles_and_badges.up.sql:

  TEIL 1 — anime_contribution_roles:
  - id BIGSERIAL PRIMARY KEY
  - anime_contribution_id BIGINT NOT NULL REFERENCES anime_contributions(id) ON DELETE CASCADE
    -- CASCADE ist PFLICHT laut D-Locked: Contribution loeschen entfernt automatisch ihre Rollenzuordnungen
  - role_code TEXT NOT NULL REFERENCES role_definitions(code) ON DELETE RESTRICT
  - CONSTRAINT uq_anime_contribution_roles_contrib_role UNIQUE (anime_contribution_id, role_code)

  Indizes:
  - idx_anime_contribution_roles_contrib ON anime_contribution_roles(anime_contribution_id)
  - idx_anime_contribution_roles_code ON anime_contribution_roles(role_code)

  TEIL 2 — member_badges:
  - id BIGSERIAL PRIMARY KEY
  - member_id BIGINT NOT NULL REFERENCES members(id) ON DELETE CASCADE
  - badge_code TEXT NOT NULL
  - badge_category VARCHAR(30) NOT NULL, CHECK IN ('historical_achievement', 'supporter', 'platform')
  - derived_from_type TEXT NULL   -- z.B. 'anime_contribution', 'fansub_group_history'
  - derived_from_id BIGINT NULL   -- ID des Quelldatensatzes
  - status VARCHAR(20) NOT NULL DEFAULT 'active', CHECK IN ('active', 'revoked', 'pending')
  - visibility VARCHAR(20) NOT NULL DEFAULT 'public', CHECK IN ('public', 'internal', 'hidden')
  - awarded_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
  - CONSTRAINT uq_member_badges_member_code UNIQUE (member_id, badge_code)

  Indizes:
  - idx_member_badges_member ON member_badges(member_id)
  - idx_member_badges_category ON member_badges(badge_category)
  - idx_member_badges_visibility ON member_badges(visibility)

  Erstelle database/migrations/0087_anime_contribution_roles_and_badges.down.sql:
  - DROP TABLE IF EXISTS member_badges
  - DROP TABLE IF EXISTS anime_contribution_roles
  HINWEIS: Reihenfolge beim DROP beachten — anime_contribution_roles hat keinen FK auf member_badges, Reihenfolge ist egal. Beide in einem down.sql.
  </action>

  <verify>
    <automated>
      grep "ON DELETE CASCADE" database/migrations/0087_anime_contribution_roles_and_badges.up.sql
      grep "uq_anime_contribution_roles_contrib_role" database/migrations/0087_anime_contribution_roles_and_badges.up.sql
      grep "uq_member_badges_member_code" database/migrations/0087_anime_contribution_roles_and_badges.up.sql
      grep -c "BIGSERIAL" database/migrations/0087_anime_contribution_roles_and_badges.up.sql
      grep "derived_from_type" database/migrations/0087_anime_contribution_roles_and_badges.up.sql
    </automated>
  </verify>

  <acceptance_criteria>
    - 0087_anime_contribution_roles_and_badges.up.sql und .down.sql existieren
    - anime_contribution_roles.anime_contribution_id hat ON DELETE CASCADE (PFLICHT laut D-Locked)
    - UNIQUE(anime_contribution_id, role_code) als uq_anime_contribution_roles_contrib_role vorhanden
    - anime_contribution_roles.role_code referenziert role_definitions(code) ON DELETE RESTRICT
    - member_badges.id ist BIGSERIAL PRIMARY KEY
    - member_badges.badge_category CHECK IN ('historical_achievement', 'supporter', 'platform') vorhanden
    - member_badges.derived_from_type TEXT NULL und derived_from_id BIGINT NULL vorhanden (kein FK — polymorphe Referenz)
    - UNIQUE(member_id, badge_code) als uq_member_badges_member_code vorhanden
    - member_badges.visibility CHECK IN ('public', 'internal', 'hidden') vorhanden
    - down.sql entfernt beide Tabellen (member_badges und anime_contribution_roles)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Migration runner → Datenbank | Nur Migration-Skripte duerfen Schema-Aenderungen vornehmen |
| anime_contributions → hist_fansub_group_members | NOT NULL FK erzwingt gueltige Mitgliedschaft vor jeder Contribution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-61-05 | Tampering | anime_contributions.fansub_group_member_id | mitigate | NOT NULL + RESTRICT verhindert Contributions ohne gueltige Mitgliedschaft; kein anonymer Eintrag moeglich |
| T-61-06 | Information Disclosure | anime_contributions.is_public_on_anime_page DEFAULT false | accept | Schutzstandard by Default; Public-Schaltung erfordert explizite Admin-Aktion in Phase 62 |
| T-61-07 | Tampering | member_badges.derived_from_id (polymorphe Referenz ohne FK) | accept | Kein FK absichtlich (polymorphe Referenz); Integritaet wird durch Badge-Engine in Phase 68 sichergestellt |
| T-61-SC | Tampering | npm/pip/cargo installs | accept | Keine Paketinstallation in dieser Phase (nur SQL-Migrationen) |
</threat_model>

<verification>
Nach Ausfuehrung der Migrationen 0086 und 0087:

1. \d anime_contributions -- fansub_group_member_id ist NOT NULL und hat RESTRICT-FK
2. \d anime_contribution_roles -- anime_contribution_id hat ON DELETE CASCADE; UNIQUE sichtbar
3. \d member_badges -- UNIQUE(member_id, badge_code); badge_category-Check; derived_from_type NULL
4. Integritaetstest: INSERT INTO anime_contributions (..., fansub_group_member_id = NULL) muss mit NOT NULL-Fehler abgelehnt werden
5. Kaskaden-Test: DELETE FROM anime_contributions WHERE id = X loescht automatisch zugehoerige anime_contribution_roles-Zeilen
6. Down-Test: 0087.down, 0086.down — keine Fehler, Tabellen verschwinden
</verification>

<success_criteria>
- Migrationen 0086 und 0087 laufen fehlerfrei up und down
- anime_contributions.fansub_group_member_id ist NOT NULL (P61-SC5 erfuellt)
- anime_contribution_roles hat ON DELETE CASCADE auf anime_contribution_id (P61-SC3 erfuellt)
- member_badges hat UNIQUE(member_id, badge_code) und badge_category-Check
- Alle neuen Tabellen haben BIGSERIAL-IDs (P61-SC4 erfuellt)
- Gesamtmigrationskette 0081–0087 laeuft ohne Fehler durch (P61-SC1 erfuellt)
</success_criteria>

<output>
Erstelle .planning/phases/61-fansub-contributions-datenmodell/61-03-SUMMARY.md wenn fertig.
</output>
