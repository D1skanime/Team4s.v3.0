---
phase: 61-fansub-contributions-datenmodell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/0081_historical_members_identity.up.sql
  - database/migrations/0081_historical_members_identity.down.sql
  - database/migrations/0082_historical_fansub_group_members.up.sql
  - database/migrations/0082_historical_fansub_group_members.down.sql
autonomous: true
requirements:
  - P61-SC1
  - P61-SC3
  - P61-SC4
  - P61-SC5

must_haves:
  truths:
    - "Migration 0081 legt member_claims und ergaenzt members.noindex ohne Fehler durch (up und down)"
    - "Migration 0082 legt hist_fansub_group_members mit UNIQUE(fansub_group_id, member_id) an"
    - "Alle IDs sind BIGSERIAL, alle FKs referenzieren korrekte Tabellen"
  artifacts:
    - path: "database/migrations/0081_historical_members_identity.up.sql"
      provides: "member_claims-Tabelle und members.noindex-Spalte"
    - path: "database/migrations/0082_historical_fansub_group_members.up.sql"
      provides: "hist_fansub_group_members-Tabelle mit historischen Mitgliedschaftsfeldern"
  key_links:
    - from: "member_claims.member_id"
      to: "members.id"
      via: "REFERENCES members(id) ON DELETE CASCADE"
    - from: "member_claims.app_user_id"
      to: "app_users.id"
      via: "REFERENCES app_users(id) ON DELETE SET NULL"
    - from: "hist_fansub_group_members.fansub_group_id"
      to: "fansub_groups.id"
      via: "REFERENCES fansub_groups(id) ON DELETE RESTRICT"
    - from: "hist_fansub_group_members.member_id"
      to: "members.id"
      via: "REFERENCES members(id) ON DELETE RESTRICT"
---

<objective>
Legt das historische Identitaetsfundament: member_claims-Tabelle (App-User-zu-Member-Verbindung) und hist_fansub_group_members (historische Gruppenmitgliedschaft unabhaengig von App-Users).

Purpose: Alle nachfolgenden Phases 62-68 benoetigen diese Grundtabellen als FK-Anker.
Output: Zwei reversible Migrationen (0081, 0082) mit Tabellen, Constraints und Indizes.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/PROJECT.md
@C:/Users/admin/Documents/Team4s/.planning/ROADMAP.md
@C:/Users/admin/Documents/Team4s/.planning/phases/61-fansub-contributions-datenmodell/61-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0081 — member_claims und members.noindex</name>

  <read_first>
    - database/migrations/0077_member_profiles_mvp.up.sql — Stil ALTER TABLE members, Constraint-Benennung
    - database/migrations/0073_fansub_group_app_memberships.up.sql — Stil CREATE TABLE, FK-Definitionen, Index-Benennung
    - database/migrations/0080_member_profile_background.up.sql — kuerzeste Migrationen als Stil-Anker
    - Pruefe ob members.noindex bereits existiert: grep -r "noindex" database/migrations/
  </read_first>

  <action>
  Erstelle database/migrations/0081_historical_members_identity.up.sql:

  Schritt 1 — members.noindex ergaenzen (ALTER TABLE, nicht neues CREATE):
  - ALTER TABLE members ADD COLUMN IF NOT EXISTS noindex BOOLEAN NOT NULL DEFAULT true
  - Kommentar ueber die Spalte: noindex steuert Suchmaschinenindexierung des Member-Profils

  Schritt 2 — member_claims anlegen:
  - Tabellenname: member_claims
  - id BIGSERIAL PRIMARY KEY
  - member_id BIGINT NOT NULL REFERENCES members(id) ON DELETE CASCADE
  - app_user_id BIGINT NULL REFERENCES app_users(id) ON DELETE SET NULL
  - claim_status VARCHAR(20) NOT NULL DEFAULT 'pending', CHECK IN ('pending', 'verified', 'rejected')
  - verification_method TEXT NULL
  - verified_by BIGINT NULL REFERENCES app_users(id) ON DELETE SET NULL
  - verified_at TIMESTAMPTZ NULL
  - created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
  - UNIQUE(member_id, app_user_id)
  - Constraint-Name: uq_member_claims_member_user
  - Index idx_member_claims_member_id ON member_claims(member_id)
  - Index idx_member_claims_app_user_id ON member_claims(app_user_id)
  - Index idx_member_claims_status ON member_claims(claim_status)

  Erstelle database/migrations/0081_historical_members_identity.down.sql:
  - DROP TABLE IF EXISTS member_claims
  - ALTER TABLE members DROP COLUMN IF EXISTS noindex
  </action>

  <verify>
    <automated>
      grep -c "member_claims" database/migrations/0081_historical_members_identity.up.sql
      grep -c "noindex" database/migrations/0081_historical_members_identity.up.sql
      grep -c "BIGSERIAL" database/migrations/0081_historical_members_identity.up.sql
    </automated>
  </verify>

  <acceptance_criteria>
    - 0081_historical_members_identity.up.sql und .down.sql existieren
    - member_claims hat id BIGSERIAL PRIMARY KEY
    - member_claims.claim_status hat CHECK IN ('pending', 'verified', 'rejected')
    - UNIQUE(member_id, app_user_id) als uq_member_claims_member_user vorhanden
    - members.noindex BOOLEAN NOT NULL DEFAULT true ist in up.sql enthalten
    - down.sql entfernt member_claims und die noindex-Spalte vollstaendig
    - Alle drei Indizes (member_id, app_user_id, claim_status) sind vorhanden
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migration 0082 — hist_fansub_group_members</name>

  <read_first>
    - database/migrations/0073_fansub_group_app_memberships.up.sql — bestehende fansub_group_members (app-user-basiert); neues Modell ist historisch (member_id-basiert) und heisst ANDERS
    - database/migrations/0010_fansub_members.up.sql — altes fansub_members-Muster (joined/left als since_year/until_year)
    - .planning/phases/61-fansub-contributions-datenmodell/61-CONTEXT.md — Abschnitt "Gruppenmitgliedschaft" fuer Feldliste
  </read_first>

  <action>
  Erstelle database/migrations/0082_historical_fansub_group_members.up.sql:

  Tabelle hist_fansub_group_members (KEIN Ueberschreiben der bestehenden fansub_group_members aus 0073):
  - id BIGSERIAL PRIMARY KEY
  - fansub_group_id BIGINT NOT NULL REFERENCES fansub_groups(id) ON DELETE RESTRICT
  - member_id BIGINT NOT NULL REFERENCES members(id) ON DELETE RESTRICT
  - joined_year INT NULL
  - left_year INT NULL
  - status VARCHAR(20) NOT NULL DEFAULT 'historical', CHECK IN ('draft', 'historical', 'confirmed', 'disputed')
  - visibility VARCHAR(20) NOT NULL DEFAULT 'internal', CHECK IN ('internal', 'public')
  - created_by BIGINT NULL REFERENCES app_users(id) ON DELETE SET NULL
  - created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
  - updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
  - CONSTRAINT uq_hist_fansub_group_members_group_member UNIQUE (fansub_group_id, member_id)
  - CONSTRAINT chk_hist_fansub_group_members_years CHECK (left_year IS NULL OR joined_year IS NULL OR left_year >= joined_year)

  Indizes:
  - idx_hist_fansub_group_members_group ON hist_fansub_group_members(fansub_group_id)
  - idx_hist_fansub_group_members_member ON hist_fansub_group_members(member_id)
  - idx_hist_fansub_group_members_status ON hist_fansub_group_members(status)
  - idx_hist_fansub_group_members_visibility ON hist_fansub_group_members(visibility)

  Erstelle database/migrations/0082_historical_fansub_group_members.down.sql:
  - DROP TABLE IF EXISTS hist_fansub_group_members
  </action>

  <verify>
    <automated>
      grep -c "BIGSERIAL" database/migrations/0082_historical_fansub_group_members.up.sql
      grep -c "uq_hist_fansub_group_members_group_member" database/migrations/0082_historical_fansub_group_members.up.sql
      grep -c "ON DELETE RESTRICT" database/migrations/0082_historical_fansub_group_members.up.sql
    </automated>
  </verify>

  <acceptance_criteria>
    - 0082_historical_fansub_group_members.up.sql und .down.sql existieren
    - Tabellenname ist hist_fansub_group_members (nicht fansub_group_members — Konfliktvermeidung mit 0073)
    - UNIQUE(fansub_group_id, member_id) als uq_hist_fansub_group_members_group_member vorhanden
    - fansub_group_id und member_id referenzieren mit ON DELETE RESTRICT (kein CASCADE nach oben)
    - status CHECK IN ('draft', 'historical', 'confirmed', 'disputed') vorhanden
    - visibility CHECK IN ('internal', 'public') vorhanden
    - joined_year/left_year Jahresvergleich-CHECK vorhanden
    - Vier Indizes (group, member, status, visibility) vorhanden
    - down.sql entfernt die Tabelle vollstaendig
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Migration runner → Datenbank | Nur Migration-Skripte duerfen Schema-Aenderungen vornehmen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-61-01 | Tampering | member_claims.verified_by | mitigate | verified_by als FK auf app_users; NULL solange unbestätigt |
| T-61-02 | Information Disclosure | members.noindex DEFAULT true | accept | Schutzkonfiguration by Default; kein Datenverlust-Risiko |
| T-61-SC | Tampering | npm/pip/cargo installs | accept | Keine Paketinstallation in dieser Phase (nur SQL-Migrationen) |
</threat_model>

<verification>
Nach Ausfuehrung beider Migrationen (docker exec oder psql direkt):

1. \d member_claims — zeigt alle Spalten inkl. claim_status-Check und UNIQUE-Constraint
2. \d hist_fansub_group_members — zeigt UNIQUE, beide ON DELETE RESTRICT FKs, Jahr-Check
3. \d members — zeigt noindex BOOLEAN NOT NULL DEFAULT true
4. SELECT COUNT(*) FROM member_claims; -- muss 0 ergeben (leere Tabelle)
5. SELECT COUNT(*) FROM hist_fansub_group_members; -- muss 0 ergeben
6. Down-Migration ausfuehren und pruefen: member_claims und noindex-Spalte verschwinden, hist_fansub_group_members verschwinden
</verification>

<success_criteria>
- Migration 0081 und 0082 laufen fehlerfrei up und down
- member_claims existiert mit BIGSERIAL id, UNIQUE(member_id, app_user_id), claim_status-Check
- hist_fansub_group_members existiert mit UNIQUE(fansub_group_id, member_id), ON DELETE RESTRICT auf beiden FKs
- members.noindex BOOLEAN NOT NULL DEFAULT true ist gesetzt
- Bestehende fansub_group_members (0073, app-user-basiert) bleibt unveraendert
</success_criteria>

<output>
Erstelle .planning/phases/61-fansub-contributions-datenmodell/61-01-SUMMARY.md wenn fertig.
</output>
