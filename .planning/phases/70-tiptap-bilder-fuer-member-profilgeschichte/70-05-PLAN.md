---
phase: 70-tiptap-bilder-fuer-member-profilgeschichte
plan: 05
type: execute
wave: 3
depends_on: [70-03]
files_modified:
  - frontend/src/components/editor/StoryImageExtension.ts
  - frontend/src/components/editor/StoryImageNodeView.tsx
  - frontend/src/components/editor/StoryImageNodeView.module.css
  - frontend/src/components/editor/RichTextEditor.tsx
  - frontend/src/components/editor/StoryImageToolbarButton.tsx
  - frontend/src/components/editor/RichTextRenderer.module.css
  - frontend/src/lib/storyImageUpload.ts
  - frontend/src/lib/api.ts
autonomous: true
requirements: [D-01, D-02, D-06, D-07, D-09, D-10, D-11, D-12]

must_haves:
  truths:
    - "StoryImageExtension.ts als TipTap-Block-Node mit name='image', group='block', atom=true, draggable=true"
    - "Attribute: media_asset_id, pending_key, preview_url, width_percent (default 60), alignment (default 'center') — kein Alt-Text, keine Caption (D-02)"
    - "StoryImageNodeView zeigt preview_url (Object-URL) als Vorschau, Resize-Ziehgriff (unten rechts, 12px), L/M/R Ausrichtungs-Toolbar"
    - "RichTextEditor empfaengt enableImages Prop: nur bei true wird StoryImageExtension in Extensions-Array + Toolbar-Bild-Icon gerendert"
    - "onPendingImageAdded Callback wird aufgerufen wenn Nutzer eine Datei auswaehlt"
    - "uploadPendingStoryImages in storyImageUpload.ts traversiert TipTap-JSON, tauscht pending_key→media_asset_id, bereinigt preview_url"
    - "uploadOwnProfileStoryImage in api.ts via authorizedUploadXhr, Endpoint /api/v1/me/profile/story-images"
    - "StoryImageToolbarButton.tsx als ausgelagerter Toolbar-Button (praeventiever Split — RichTextEditor.tsx hat 408 Zeilen vor diesem Plan, ueberschreitet 450 nach Erweiterungen)"
    - "npm run typecheck und npm test erfolgreich"
    - "Wave-0-Frontend-Tests (StoryImageExtension.test.ts, storyImageUpload.test.ts) gruen"
  artifacts:
    - path: "frontend/src/components/editor/StoryImageExtension.ts"
      provides: "TipTap Node Extension mit Attributen und ReactNodeViewRenderer"
      contains: "name: 'image'"
    - path: "frontend/src/components/editor/StoryImageNodeView.tsx"
      provides: "React NodeView: Resize-Handle + Ausrichtungs-Toolbar + Object-URL-Vorschau"
      contains: "NodeViewWrapper"
    - path: "frontend/src/components/editor/StoryImageNodeView.module.css"
      provides: "CSS-Module fuer Wrapper, Toolbar, Resize-Handle, Ausrichtungs-Klassen"
      contains: "story-img-align"
    - path: "frontend/src/components/editor/StoryImageToolbarButton.tsx"
      provides: "Ausgelagerter Bild-einfuegen-Toolbar-Button inkl. hidden file-input"
      contains: "StoryImageToolbarButton"
    - path: "frontend/src/lib/storyImageUpload.ts"
      provides: "uploadPendingStoryImages: traversiert JSON, laedt hoch, tauscht Marker"
      contains: "uploadPendingStoryImages"
    - path: "frontend/src/lib/api.ts"
      provides: "uploadOwnProfileStoryImage Funktion"
      contains: "story-images"
  key_links:
    - from: "frontend/src/components/editor/RichTextEditor.tsx:enableImages prop"
      to: "frontend/src/app/me/profile/components/ProfileStoryCard.tsx"
      via: "ProfileStoryCard uebergibt enableImages={true} an RichTextEditor"
      pattern: "enableImages"
    - from: "frontend/src/lib/storyImageUpload.ts:uploadPendingStoryImages"
      to: "frontend/src/app/me/profile/page.tsx:handleSubmit"
      via: "handleSubmit ruft uploadPendingStoryImages vor updateOwnProfile auf"
      pattern: "uploadPendingStoryImages"
---

<objective>
Frontend-Seite der Image-Extension: TipTap-Extension, React-NodeView, CSS, opt-in RichTextEditor-Verdrahtung und Upload-Utility. Diese Schicht implementiert die Editor-Interaktion (Dateiauswahl, lokale Vorschau, Resize, Ausrichtung) und die deferred-Batch-Upload-Logik (pending_key → media_asset_id vor dem Save).

Purpose: Ohne diese Schicht kann kein Bild in den Editor eingefuegt, als Vorschau angezeigt oder beim Save aufgeloest werden.
Output: StoryImageExtension.ts, StoryImageNodeView.tsx + CSS, StoryImageToolbarButton.tsx (praeventiever Split), erweiterte RichTextEditor.tsx, storyImageUpload.ts, api.ts-Ergaenzung.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/ROADMAP.md
@C:/Users/admin/Documents/Team4s/.planning/phases/70-tiptap-bilder-fuer-member-profilgeschichte/70-CONTEXT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/70-tiptap-bilder-fuer-member-profilgeschichte/70-PATTERNS.md
@C:/Users/admin/Documents/Team4s/.planning/phases/70-tiptap-bilder-fuer-member-profilgeschichte/70-UI-SPEC.md

<interfaces>
<!-- ColorTokenExtension.ts als exaktes Vorbild fuer StoryImageExtension -->
<!-- Aus frontend/src/components/editor/ColorTokenExtension.ts (RESEARCH-verifiziert) -->

ColorTokenExtension-Pattern (lines 1-31):
```typescript
import { TextStyle } from '@tiptap/extension-text-style'
export const ColorTokenExtension = TextStyle.extend({
  addAttributes() {
    return {
      ...this.parent?.(),
      colorToken: {
        default: null,
        parseHTML: (element) => element.getAttribute('data-color-token') ?? null,
        renderHTML: (attributes) => { ... }
      },
    }
  },
})
```

StoryImageExtension nutzt Node.create (nicht TextStyle.extend), aber gleiches Attribut-Pattern.

<!-- RichTextEditor.tsx Props und Extensions (RESEARCH-verifiziert) -->

WICHTIG: RichTextEditor.tsx hat aktuell 408 Zeilen. Die Erweiterungen in diesem Plan
(enableImages-Prop, StoryImageExtension-Conditional, Toolbar-Integration) fuegen
schaetzungsweise 25-35 Zeilen hinzu → voraussichtlich 433-443 Zeilen nach Edit,
noch unter der 450-Zeilen-Grenze (CLAUDE.md). Dennoch wird der Toolbar-Button
PRAEVENTIV in StoryImageToolbarButton.tsx ausgelagert, um Headroom zu behalten
und spaeteren Erweiterungen Platz zu lassen.

Bestehende Props (lines 17-25):
```typescript
type RichTextEditorProps = {
  value: unknown | null
  onChange: (value: unknown) => void
  placeholder?: string
  helperText?: string
  mode?: 'longform' | 'shortnote'
  disabled?: boolean
  minHeight?: number
}
```

Extensions (lines 358-372):
```typescript
extensions: [
  StarterKit.configure({ codeBlock: false, code: false, strike: false, hardBreak: false }),
  Table.configure({ resizable: false }),
  TableRow, TableCell, TableHeader,
  ColorTokenExtension,
  Placeholder.configure({ placeholder: placeholder ?? 'Hier schreiben…' }),
  CharacterCount,
]
```

Toolbar-Button-Pattern (lines 251-258):
```typescript
<button type="button" className={styles.toolbarBtn} onClick={...} aria-label="..." title="...">
  {/* Unicode-Glyph oder Icon */}
</button>
```

Toolbar-Separator: <span className={styles.toolbarSep} />

<!-- api.ts authorizedUploadXhr Pattern (RESEARCH-verifiziert) -->

authorizedUploadXhr Interface (lines 2104-2110):
```typescript
interface AuthorizedUploadXhrOptions<T> {
  endpoint: string;
  buildBody: () => FormData;
  onProgress?: (percent: number) => void;
  retryEligibility: 'never' | 'auth-before-persistence' | 'idempotent';
  parsePayload?: (payload: unknown) => T;
}
```

<!-- RichTextEditor.module.css Tokens (UI-SPEC) -->
Zu verwendende Tokens (NICHT neu definieren):
- --accent-primary, --bg-card, --bg-secondary, --bg-primary
- --text-primary, --text-secondary, --text-muted
- --radius-sm, --radius-md
- .toolbarBtn: height 28px, min-width 28px, padding 0 5px
- .toolbarBtnActive: color-mix accent 18% bg, accent 44% border
- .toolbarSep: 1px breit, 18px hoch, margin 0 4px
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StoryImageExtension + storyImageUpload.ts + api.ts</name>
  <files>frontend/src/components/editor/StoryImageExtension.ts, frontend/src/lib/storyImageUpload.ts, frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/components/editor/ColorTokenExtension.ts — vollstaendig lesen (exaktes Muster fuer Extension-Struktur)
    - frontend/src/lib/api.ts — authorizedUploadXhr (lines 2104-2233), uploadFansubMedia (lines 2235-2255) als Muster fuer neue uploadOwnProfileStoryImage-Funktion; getApiBaseUrl und ApiError-Pattern
    - frontend/src/components/editor/StoryImageExtension.test.ts — Wave-0-Tests als Ziel dieser Task
    - frontend/src/lib/storyImageUpload.test.ts — Wave-0-Tests als Ziel dieser Task
  </read_first>
  <behavior>
    StoryImageExtension.ts:
    - Node.create({ name: 'image', group: 'block', atom: true, draggable: true })
    - addAttributes: media_asset_id (default null), pending_key (default null), preview_url (default null), width_percent (default 60), alignment (default 'center') — kein alt_text, keine caption (D-02)
    - parseHTML: [{ tag: 'img[data-story-image]' }]
    - renderHTML: ['img', mergeAttributes(attrs, { 'data-story-image': '' })]
    - addNodeView: ReactNodeViewRenderer(StoryImageNodeView)

    storyImageUpload.ts:
    - uploadPendingStoryImages(doc, pendingFiles, uploadFn, onProgress?): traversiert TipTap-JSON-Baum; fuer jeden image-Node mit pending_key != null: file aus pendingFiles.get(pending_key), ruft uploadFn auf, ersetzt media_asset_id, setzt pending_key=null, preview_url=null (Pitfall 2 aus RESEARCH); kein pending_key → nur preview_url=null setzen; Fehler bei einem Upload → wirft Exception (kein Partial-Save, Pitfall 7); D-14: pending_key-Eintraege in pendingFiles, die nicht im TipTap-JSON-Dokument vorkommen (Nutzer hat Bild entfernt), loesen keinen uploadFn-Call aus — nur Nodes im Dokument werden verarbeitet
    - Keine Parallele Uploads (sequentiell per await) um Fehlerzustand eindeutig zu halten

    api.ts (Ergaenzung):
    - StoryImageUploadResponse Interface: { media_asset_id: number; public_url: string }
    - uploadOwnProfileStoryImage(file: File, onProgress?: (pct: number) => void): Endpoint ${API_BASE_URL}/api/v1/me/profile/story-images; retryEligibility: 'never'; FormData field "image"; parsePayload: (payload.data as StoryImageUploadResponse)
    - typeof window === 'undefined' Guard (Server-Side-Guard, analog bestehende Upload-Funktion)
  </behavior>
  <action>
    StoryImageExtension.ts anlegen: Imports from '@tiptap/core' (Node, mergeAttributes) und '@tiptap/react' (ReactNodeViewRenderer) + './StoryImageNodeView' (Import existiert noch nicht → TypeScript-Fehler wird in Task 2 behoben; voruebergehend mit // @ts-ignore oder Stub-Import). Fuenf Attribute: media_asset_id, pending_key, preview_url, width_percent, alignment — kein alt_text, keine caption (D-02). Vollstaendige Extension-Implementierung analog PATTERNS.md-Code-Pattern fuer StoryImageExtension.

    storyImageUpload.ts anlegen: TypeScript-Utility, keine React-Imports. Typen: TipTapImageNode (lokaler Typ), exportierte Funktion uploadPendingStoryImages mit generic unknown Input/Output. traverseAndResolve rekursiv ueber Array.isArray(n.content) fuer Kinder-Traversal. Wichtig: async/await sequential statt Promise.all (single-failure-stop). D-14: Iteration laeuft ueber Nodes im Dokument, nicht ueber pendingFiles.keys() — damit werden automatisch nur Bilder hochgeladen, die noch im JSON vorhanden sind. Import: StoryImageUploadResponse aus './api' (wird nachtraeglich durch api.ts-Ergaenzung aufgeloest).

    api.ts: Am Ende der Datei (nach letzter Export-Funktion) StoryImageUploadResponse Interface und uploadOwnProfileStoryImage-Funktion ergaenzen. getApiBaseUrl und authorizedUploadXhr sind bereits in der Datei — importieren/wiederverwenden (kein neuer Export noetig fuer interne Helfer).
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck 2>&1 | grep -v "StoryImageNodeView" | head -20 && npm test -- --run storyImage 2>&1 | tail -10</automated>
  </verify>
  <done>npm run typecheck ohne Fehler ausser ggf. dem StoryImageNodeView-Import (wird in Task 2 behoben). uploadPendingStoryImages-Tests und StoryImageExtension-Attribut-Tests gruen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StoryImageNodeView + CSS + StoryImageToolbarButton + RichTextEditor-opt-in + RichTextRenderer-CSS</name>
  <files>frontend/src/components/editor/StoryImageNodeView.tsx, frontend/src/components/editor/StoryImageNodeView.module.css, frontend/src/components/editor/StoryImageToolbarButton.tsx, frontend/src/components/editor/RichTextEditor.tsx, frontend/src/components/editor/RichTextRenderer.module.css, frontend/src/app/me/profile/components/ProfileStoryCard.tsx, frontend/src/app/me/profile/components/profileFormTypes.ts</files>
  <read_first>
    - frontend/src/components/editor/RichTextEditor.tsx — vollstaendig lesen: aktuelle Zeilenzahl (408 Zeilen vor diesem Plan), Props (lines 17-25), extensions-Array (lines 358-372), EditorToolbar (alle Toolbar-Buttons, lines 106-341), styles-Referenzen aus RichTextEditor.module.css
    - frontend/src/components/editor/RichTextEditor.module.css — toolbarBtn, toolbarBtnActive, toolbarSep, editorShell:focus-within CSS-Werte (exakt uebernehmen)
    - frontend/src/app/me/profile/components/ProfileStoryCard.tsx — vollstaendig lesen: bestehende Props, RichTextEditor-Aufruf
    - 70-UI-SPEC.md — Component & Interaction Inventory (Sections 1-6) fuer exakte visuelle Werte
    - 70-PATTERNS.md — StoryImageNodeView.tsx Sektion fuer vollstaendiges Code-Pattern
  </read_first>
  <behavior>
    StoryImageToolbarButton.tsx (neue Datei — praeventiever Split):
    - Props: onFileSelected(file: File): void; disabled?: boolean; className?: string; btnClassName?: string
    - Rendert einen einzelnen Toolbar-Button + hidden file-input (accept="image/jpeg,image/png,image/webp")
    - useRef fuer fileInputRef (HTMLInputElement)
    - handleImageFileSelected: erstellt Object-URL, generiert pending_key (crypto.randomUUID()), ruft onFileSelected(file) auf; preview_url-Erzeugung und pending_key-Generierung koennen alternativ im Aufrufer (RichTextEditor) bleiben falls die Props so gestaltet werden; wichtig ist nur, dass die Datei-Input-Logik aus RichTextEditor.tsx herausgehalten wird
    - Bild-Icon: lucide Image (size=14, strokeWidth=1.75), aria-label="Bild einfügen", title="Bild einfügen"

    StoryImageNodeView.tsx:
    - NodeViewWrapper als Wrapper; selected-State (TipTap-Prop) steuert Sichtbarkeit von Toolbar + Resize-Handle
    - Zeigt preview_url (Object-URL) ODER public_url (nach Save laden — node.attrs.preview_url ?? null); falls beides null und media_asset_id null → return null (D-04)
    - Kein alt-Attribut auf dem img-Tag (D-02)
    - Schwebende Node-Toolbar (nur bei selected): drei Segment-Buttons L/M/R mit lucide AlignLeft/AlignCenter/AlignRight (size=14), onClick: updateAttributes({ alignment: 'left'|'center'|'right' }); aktiver Button = toolbarBtnActive-Klasse
    - Resize-Handle: onMouseDown → onMouseMove → delta berechnen → updateAttributes({ width_percent: Math.min(100, Math.max(10, newPercent)) }); onMouseUp → cleanup; Klick auf Handle: keine Alignment-Aenderung
    - Container-Breite: style={{ width: `${width_percent}%` }}
    - Ausrichtungs-Wrapper-Klassen: story-img-align-left/center/right (fuer Block-Ausrichtung; D-10)
    - data-drag-handle auf NodeViewWrapper fuer TipTap-native Drag-to-Reorder
    - aria-labels: "Links ausrichten", "Zentriert ausrichten", "Rechts ausrichten", "Bildbreite anpassen"

    StoryImageNodeView.module.css:
    - .storyImageWrapper: display block, position relative, margin 16px 0 (lg spacing)
    - .align-left: margin-right auto
    - .align-center: margin-left auto; margin-right auto
    - .align-right: margin-left auto
    - .selected (outline-ring): outline 2px solid color-mix(in srgb, var(--accent-primary) 42%, var(--text-secondary)); box-shadow 0 0 0 3px color-mix(in srgb, var(--accent-primary) 12%, transparent) — identisch zu editorShell:focus-within
    - .nodeToolbar: position absolute top 0 left 50% transform translateX(-50%); display flex; gap 4px (xs); background var(--bg-secondary) etc.; z-index 10
    - .toolbarBtn: height 28px; min-width 28px; padding 0 5px — exakt wie bestehende .toolbarBtn
    - .toolbarBtnActive: color-mix accent 18% bg, accent 44% border — wie bestehende .toolbarBtnActive
    - .imageContainer: position relative; display inline-block
    - .storyImage: display block; max-width 100%; height auto
    - .resizeHandle: position absolute; bottom 0; right 0; width 12px; height 12px; background var(--accent-primary); border-radius var(--radius-sm); cursor se-resize

    RichTextEditor.tsx-Erweiterungen:
    - Props: enableImages?: boolean; onPendingImageAdded?: (pendingKey: string, file: File) => void hinzufuegen
    - Extensions-Array: ...(enableImages ? [StoryImageExtension] : []) ergaenzen
    - Toolbar-Erweiterung: {enableImages && (<><span className={styles.toolbarSep} /><StoryImageToolbarButton onFileSelected={(file) => { const pendingKey = crypto.randomUUID(); editor.chain().focus().insertContent({ type: 'image', attrs: { pending_key: pendingKey, preview_url: URL.createObjectURL(file), width_percent: 60, alignment: 'center' } }).run(); onPendingImageAdded?.(pendingKey, file); }} /></>)} — nach Tabellen-Gruppe
    - Import StoryImageToolbarButton aus './StoryImageToolbarButton' ergaenzen
    - Import Image aus 'lucide-react' ist in StoryImageToolbarButton (nicht in RichTextEditor.tsx noetig)
    - Zeilenzahl nach Edit pruefen: 408 + geschaetzte 20-30 Zeilen Erweiterung = ~428-438 Zeilen → unter 450, aber durch StoryImageToolbarButton-Auslagerung hat RichTextEditor.tsx genuegend Headroom fuer kuenftige Erweiterungen

    RichTextRenderer.module.css:
    - Klassen fuer die vom Backend erzeugten img-Tags: .richTextOutput img.story-img-align-left, .story-img-align-center, .story-img-align-right (Block-Ausrichtung via class aus Server-HTML) — kein alt-bezogenes Styling noetig (D-02)

    ProfileStoryCard.tsx:
    - Props um onPendingImageAdded?(pendingKey: string, file: File) => void und uploadProgress?: Map<string,number> erweitern
    - RichTextEditor-Aufruf: enableImages={true}, onPendingImageAdded={onPendingImageAdded} ergaenzen
    - Upload-Fortschritt pro Bild anzeigen wenn uploadProgress.size > 0: fuer jeden Eintrag in uploadProgress "Bild wird hochgeladen … {n}%" + Progress-Balken (Accent-Fuellung)

    profileFormTypes.ts: KEINE Aenderung noetig (pendingImages Map lebt in page.tsx-State, nicht in MemberProfileFormState).
  </behavior>
  <action>
    StoryImageToolbarButton.tsx als neue Datei anlegen (praeventiever Split laut CLAUDE.md 450-Zeilen-Grenze). StoryImageNodeView.tsx anlegen. CSS-Module anlegen. RichTextEditor.tsx erweitern (nur Prop-Ergaenzung + Extension-Conditional + Toolbar-StoryImageToolbarButton-Einbindung — keine strukturelle Umformung). RichTextRenderer.module.css um drei img-Ausrichtungs-Klassen ergaenzen. ProfileStoryCard.tsx Prop-Ergaenzung + enableImages-Pass-through. profileFormTypes.ts prueft ob keine Aenderung noetig (falls doch → minimal ergaenzen). Zeilen von RichTextEditor.tsx vor (408) und nach Edit pruefen — falls der Wert nach dem Edit doch >450 ergibt: weitere Auslagerung vornehmen und in SUMMARY dokumentieren. Alle deutschen UI-Strings mit korrekten Umlauten (UI-SPEC Copywriting Contract). Kein alt-Attribut auf img-Tags in NodeView (D-02).
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck 2>&1 | tail -5 && npm test -- --run storyImage 2>&1 | tail -10</automated>
  </verify>
  <done>npm run typecheck ohne Fehler. npm test: alle StoryImageExtension- und storyImageUpload-Tests gruen. StoryImageNodeView.tsx, StoryImageNodeView.module.css, StoryImageToolbarButton.tsx existieren. RichTextEditor.tsx enthaelt enableImages-Prop und StoryImageExtension-Conditional. RichTextEditor.tsx bleibt unter 450 Zeilen (verifiziert mit wc -l).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Datei-Input → Object-URL | Lokale Browser-Datei wird nie direkt an Server gesendet bis Save; Object-URL ist sitzungsgebunden |
| pending_key → uploadPendingStoryImages | Mapping pending_key→File existiert nur im Browser-Speicher; serverseitig wird nur die hochgeladene media_asset_id gespeichert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-05-01 | Tampering | Object-URL in persistiertem body_json | mitigate | uploadPendingStoryImages setzt preview_url=null vor Save; TestStoryImageUpload_NoObjectUrlInJSON verifiziert (Pitfall 2) |
| T-70-05-02 | Tampering | pending_key in persistiertem body_json | mitigate | uploadPendingStoryImages setzt pending_key=null; Validator im Backend (ValidateJSON) kann image-nodes ohne media_asset_id ablehnen |
| T-70-05-03 | Elevation of Privilege | enableImages=true in falscher RichTextEditor-Instanz | mitigate | Prop-Guard: enableImages ist opt-in per Prop, Default undefined/false; nur ProfileStoryCard setzt enableImages={true} |
| T-70-05-04 | Tampering | GIF-Datei via file-input auswaehlen | mitigate | accept="image/jpeg,image/png,image/webp" auf input-Element begrenzt MIME; zusaetzlich Backend-MIME-Validierung in Plan 70-04 |
| T-70-05-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen npm-Packages; lucide-react und @tiptap/* bereits installiert |
</threat_model>

<verification>
npm typecheck ohne Fehler:
`cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck`

Frontend-Tests gruen:
`cd C:/Users/admin/Documents/Team4s/frontend && npm test -- --run storyImage`

RichTextEditor.tsx unter 450 Zeilen:
`wc -l C:/Users/admin/Documents/Team4s/frontend/src/components/editor/RichTextEditor.tsx`

Volle Test-Suite weiterhin gruen:
`cd C:/Users/admin/Documents/Team4s/frontend && npm test 2>&1 | tail -10`
</verification>

<success_criteria>
- StoryImageExtension.ts: name='image', atom=true, draggable=true, fuenf Attribute (media_asset_id, pending_key, preview_url, width_percent, alignment) — kein alt_text, keine caption (D-02)
- StoryImageNodeView.tsx: NodeViewWrapper, Resize-Handle, L/M/R Toolbar, Object-URL-Vorschau, D-04-konformer silent-skip, kein alt-Attribut auf img (D-02)
- StoryImageNodeView.module.css: CSS-Tokens aus bestehenden globalen Variablen, Resize-Handle 12px accent, Selektions-Ring wie editorShell:focus-within
- StoryImageToolbarButton.tsx: Ausgelagerter Toolbar-Button mit file-input (accept="image/jpeg,image/png,image/webp"), aria-label="Bild einfügen"; praeventiever Split damit RichTextEditor.tsx unter 450 Zeilen bleibt
- RichTextEditor.tsx: enableImages-Prop, StoryImageExtension-Conditional, StoryImageToolbarButton-Einbindung, unter 450 Zeilen (war 408, bleibt darunter)
- RichTextRenderer.module.css: drei story-img-align-*-Klassen fuer Block-Ausrichtung
- storyImageUpload.ts: uploadPendingStoryImages traversiert JSON, tauscht Marker, bereinigt preview_url, wirft bei Fehler; laedt nur Nodes hoch die noch im Dokument vorhanden sind (D-14)
- api.ts: uploadOwnProfileStoryImage via authorizedUploadXhr, Endpoint /api/v1/me/profile/story-images
- ProfileStoryCard.tsx: enableImages={true} an RichTextEditor, Upload-Fortschritts-Anzeige
- npm run typecheck gruen
- Alle Wave-0-Frontend-Tests gruen
</success_criteria>

<output>
Create `.planning/phases/70-tiptap-bilder-fuer-member-profilgeschichte/70-05-SUMMARY.md` when done
</output>
