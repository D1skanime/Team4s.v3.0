---
phase: 70-tiptap-bilder-fuer-member-profilgeschichte
plan: 06
type: execute
wave: 4
depends_on: [70-04, 70-05]
files_modified:
  - backend/internal/handlers/app_profile_story_image.go
  - backend/internal/handlers/app_profile.go
  - frontend/src/app/me/profile/page.tsx
autonomous: true
requirements: [D-01, D-03, D-06, D-07, D-13, D-21, D-22, D-23]

must_haves:
  truths:
    - "UpdateOwnProfile prueft IDOR: jede media_asset_id im neuen body_json muss owner_member_id == savender Member haben"
    - "UpdateOwnProfile fuehrt Cleanup-on-Save durch: alte minus neue image-IDs → Datei + DB-Zeile loeschen (nur own assets)"
    - "UpdateOwnProfile ruft RenderHTMLWithResolver mit DB-Resolver-Closure auf (nicht RenderHTML ohne Resolver)"
    - "Migration 0089 wird in dieser oder einer vorherigen Wave auf lokaler DB angewendet"
    - "page.tsx handleSubmit: vor updateOwnProfile werden alle pending images hochgeladen (uploadPendingStoryImages)"
    - "Bei Upload-Fehler: bestehende Bilder dieses Save-Versuchs werden zurueckgerollt; Fehlermeldung erscheint"
    - "pendingImages Map wird nach erfolgreichem Save geleert"
    - "go test ./internal/handlers/... -run TestStoryImage gruene Tests (IDOR, Cleanup, RoundTrip)"
  artifacts:
    - path: "backend/internal/handlers/app_profile_story_image.go"
      provides: "applyStoryImageLifecycle: IDOR-Check + Cleanup-on-Save + ErrIDORViolation Sentinel-Error"
      contains: "extractStoryImageIDsFromJSON"
    - path: "backend/internal/handlers/app_profile.go"
      provides: "UpdateOwnProfile: applyStoryImageLifecycle-Aufruf + RenderHTMLWithResolver statt RenderHTML"
      contains: "applyStoryImageLifecycle"
    - path: "frontend/src/app/me/profile/page.tsx"
      provides: "handleSubmit mit deferred-Batch-Upload + Rollback-Logik + pendingImages-State"
      contains: "uploadPendingStoryImages"
  key_links:
    - from: "backend/internal/handlers/app_profile_story_image.go:applyStoryImageLifecycle"
      to: "backend/internal/repository/member_profile_repository.go:GetStoryImageAssetsByMember"
      via: "IDOR-Batch-Check + Referenz-Diff fuer Cleanup"
      pattern: "GetStoryImageAssetsByMember"
    - from: "backend/internal/handlers/app_profile.go:UpdateOwnProfile"
      to: "backend/internal/handlers/app_profile_story_image.go:applyStoryImageLifecycle"
      via: "Einziger Einschub-Aufruf nach ValidateJSON-Check (Zeile ~160 in app_profile.go)"
      pattern: "applyStoryImageLifecycle"
    - from: "backend/internal/handlers/app_profile.go:UpdateOwnProfile"
      to: "backend/internal/services/tiptap_service.go:RenderHTMLWithResolver"
      via: "Ersetzt den bestehenden RenderHTML-Aufruf (Zeile ~162 in app_profile.go)"
      pattern: "RenderHTMLWithResolver"
    - from: "frontend/src/app/me/profile/page.tsx:handleSubmit"
      to: "frontend/src/lib/storyImageUpload.ts:uploadPendingStoryImages"
      via: "Vor updateOwnProfile: await uploadPendingStoryImages(form.memberStory, pendingImages, uploadOwnProfileStoryImage)"
      pattern: "uploadPendingStoryImages"
---

<objective>
Save-Flow-Verdrahtung: Backend UpdateOwnProfile erhaelt IDOR-Check + Cleanup-on-Save + RenderHTMLWithResolver; Frontend handleSubmit erhaelt deferred-Batch-Upload + Rollback. Nach diesem Plan ist der vollstaendige Round-Trip (Einfuegen, Save, Reload, Cleanup) implementiert und testbar.

Purpose: Ohne diese Verdrahtung sind Upload-Handler und Extension isoliert — kein Ende-zu-Ende-Flow funktioniert.
Output: Erweitertes app_profile_story_image.go (neue applyStoryImageLifecycle-Methode), minimal angepasstes app_profile.go (zwei chirurgische Einbaustellen in UpdateOwnProfile), erweitertes page.tsx (pendingImages-State + uploadPendingStoryImages vor Save).
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/ROADMAP.md
@C:/Users/admin/Documents/Team4s/.planning/phases/70-tiptap-bilder-fuer-member-profilgeschichte/70-CONTEXT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/70-tiptap-bilder-fuer-member-profilgeschichte/70-PATTERNS.md

<interfaces>
<!-- app_profile.go UpdateOwnProfile (Seam fuer Erweiterung) -->
<!-- Aus backend/internal/handlers/app_profile.go lines 94-161 (RESEARCH-verifiziert) -->

UpdateOwnProfile hat diese Ablauf-Struktur:
1. Auth-Guard (middleware.CommentAuthIdentityFromContext)
2. JSON-Binding des Request-Body
3. Profil laden (h.profileRepo.GetOwnProfile)
4. ValidateJSON fuer body_json (h.tiptapSvc.ValidateJSON)
5. RenderHTML → body_html erzeugen [WIRD GEAENDERT: RenderHTMLWithResolver]
6. Profil-Update via h.profileRepo.UpdateOwnProfile
7. Audit-Log
8. Response

Einbaustelle 1 (nach Schritt 4, ValidateJSON-Call):
  Aufruf: if err := h.applyStoryImageLifecycle(c.Request.Context(), identity, existingProfile, newBodyJSON); err != nil { ...422... }
Einbaustelle 2 (Schritt 5, RenderHTML-Zeile ersetzen):
  Alt:  bodyHTML, err := h.tiptapSvc.RenderHTML(newBodyJSON)
  Neu:  bodyHTML, err := h.tiptapSvc.RenderHTMLWithResolver(newBodyJSON, func(id int64) (string, bool) { /* Resolver aus assets-Map */ })

Diese zwei Stellen sind die EINZIGEN Aenderungen an app_profile.go (905 Zeilen).
Keine strukturellen Umformungen, keine neuen Imports ausser dem Resolver-Callback.

<!-- app_profile_story_image.go — neue applyStoryImageLifecycle-Methode -->

applyStoryImageLifecycle(ctx context.Context, identity middleware.AuthIdentity, profile models.MemberProfile, newBodyJSON []byte) error:
- Schritt 1 IDOR-Check: extractStoryImageIDsFromJSON(newBodyJSON) → newIDs. GetStoryImageAssetsByMember(ctx, profile.MemberID) → assets. Pruefe: alle newIDs muessen in assetOwnerMap[id] == true sein. Nicht-eigene ID → return ErrIDORViolation
- Schritt 2 Cleanup-on-Save: extractStoryImageIDsFromJSON(existingProfile.MemberStoryJSON) → oldIDs. toDelete = setDiff(oldIDs, newIDs). Fuer jede ID in toDelete: pruefe owner_member_id == profile.MemberID (aus assetOwnerMap), dann cleanupStoryImageAsset + h.profileRepo.DeleteStoryImageAsset. Audit-Log.
- assets-Map kann an den Resolver-Callback in app_profile.go zurueckgegeben werden (als zweiter Rueckgabewert) → Wiederverwendung im Resolver ohne zweiten DB-Call.

ErrIDORViolation als Sentinel-Error in app_profile_story_image.go definieren:
  var ErrIDORViolation = errors.New("story-bild gehört nicht diesem profil")

<!-- page.tsx handleSubmit (RESEARCH Pattern 5, Zeilen 249-280) -->

Aktuelle handleSubmit-Struktur:
1. event.preventDefault()
2. Validierungs-Guards (hasAuthSession, hasYearErrors)
3. setIsSaving(true), clearErrors
4. updateOwnProfile(payload) → response
5. applyProfile, setSuccess

Neuer Einschub zwischen 3 und 4:
3a. if (pendingImages.size > 0): resolvedStory = await uploadPendingStoryImages(...)
3b. Bei Fehler → Fehlermeldung laut UI-SPEC Copywriting Contract

<!-- Resolver-Callback fuer RenderHTMLWithResolver -->
Der Resolver fragt media_assets nach public_url WHERE id = $1 AND owner_member_id = $2.
Oder vereinfacht: WHERE id IN ($1,$2,...) — Batch-Query aus dem IDOR-Check wiederverwenden.
Resolver: func(id int64)(string, bool) — gibt (public_url, true) oder ("", false) zurueck.

<!-- page.tsx State (RESEARCH Pattern 5, Zeilen 129ff.) -->
Neuer State in page.tsx:
```typescript
const [pendingImages] = useState(() => new Map<string, File>())
const [uploadProgress, setUploadProgress] = useState<Map<string, number>>(new Map())
```
onPendingImageAdded Callback (an ProfileStoryCard weitergereicht):
```typescript
function handlePendingImageAdded(pendingKey: string, file: File) {
  pendingImages.set(pendingKey, file)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend applyStoryImageLifecycle + RenderHTMLWithResolver in UpdateOwnProfile</name>
  <files>backend/internal/handlers/app_profile_story_image.go, backend/internal/handlers/app_profile.go</files>
  <read_first>
    - backend/internal/handlers/app_profile.go — vollstaendig lesen (905 Zeilen): UpdateOwnProfile (lines 94-161) um die zwei exakten Einbaustellen zu lokalisieren: (1) den ValidateJSON-Aufruf (Zeile ~156) nach dem die applyStoryImageLifecycle-Zeile eingefuegt wird, (2) den RenderHTML-Aufruf (Zeile ~162) der durch RenderHTMLWithResolver ersetzt wird. Zeilenanzahl nach Edit beobachten (darf nur minimal wachsen).
    - backend/internal/handlers/app_profile_story_image.go — vollstaendig lesen (existiert nach Plan 70-04): extractStoryImageIDsFromJSON, cleanupStoryImageAsset, UploadOwnProfileStoryImage — um zu verstehen welche Helfer schon vorhanden sind und wo applyStoryImageLifecycle angehaengt wird
    - backend/internal/services/tiptap_service.go — RenderHTMLWithResolver-Signatur (aus Plan 70-03): func (s *TipTapService) RenderHTMLWithResolver(input string, resolver func(mediaAssetID int64)(string, bool)) (string, error)
    - backend/internal/repository/member_profile_repository.go — GetStoryImageAssetsByMember, DeleteStoryImageAsset (aus Plan 70-04)
    - backend/internal/handlers/app_profile_story_image_test.go — TestUpdateOwnProfileIDOR, TestStoryImageRoundTrip, TestStoryImageCleanup als Ziel dieser Task
  </read_first>
  <behavior>
    In app_profile_story_image.go neue Methode applyStoryImageLifecycle:
    - Signatur: func (h *AppAuthHandler) applyStoryImageLifecycle(ctx context.Context, identity middleware.AuthIdentity, profile models.MemberProfile, newBodyJSON []byte) (map[int64]string, error) — zweiter Rueckgabewert ist assetsURLMap (id→public_url) fuer Resolver-Wiederverwendung ohne zweiten DB-Call
    - IDOR-Check: extractStoryImageIDsFromJSON(newBodyJSON) → newIDs; GetStoryImageAssetsByMember(ctx, profile.MemberID) → assets; assets in Map[id]→public_url aufbauen; fuer jede ID in newIDs die nicht in der Map ist → return nil, ErrIDORViolation
    - Cleanup-on-Save: extractStoryImageIDsFromJSON(profile.MemberStoryJSON) → oldIDs; setDiff fuer IDs die in oldIDs aber nicht in newIDs sind; jede toDelete-ID loeschen (cleanupStoryImageAsset + DeleteStoryImageAsset + Audit-Log EventType "member_profile.story_image.cleaned_up")
    - ErrIDORViolation Sentinel-Error: var ErrIDORViolation = errors.New("story-bild gehört nicht diesem profil")

    In app_profile.go UpdateOwnProfile — EXAKT ZWEI chirurgische Einbaustellen:
    1. Nach dem ValidateJSON-Aufruf (bestehende Zeile ~156): eine neue Zeile einfuegen:
       assetsMap, err := h.applyStoryImageLifecycle(ctx, identity, existingProfile, newBodyJSON)
       if err != nil { if errors.Is(err, ErrIDORViolation) { writeValidationErrorResponse(c, err.Error()); return }; writeInternalErrorResponse(c, err); return }
    2. Den bestehenden RenderHTML-Aufruf (Zeile ~162) ersetzen durch:
       bodyHTML, err := h.tiptapSvc.RenderHTMLWithResolver(string(newBodyJSON), func(id int64) (string, bool) { url, ok := assetsMap[id]; return url, ok })
    Keine weiteren Aenderungen an app_profile.go.

    Imports in app_profile.go: "errors" hinzufuegen falls noch nicht vorhanden.
  </behavior>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go build ./... && go test ./internal/handlers/... -run "TestUpdateOwnProfile.*IDOR|TestStoryImage" -count=1</automated>
  </verify>
  <done>go build ./... erfolgreich. TestUpdateOwnProfileIDOR gruen (fremde ID → 422). TestStoryImageCleanup gruen (altes Bild nach Save geloescht). TestStoryImageRoundTrip gruen (body_json mit media_asset_id persistiert, reload erhaelt Attribute). Alle bestehenden Profile-Handler-Tests weiterhin gruen.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend page.tsx pendingImages-State + handleSubmit-Erweiterung + Migration anwenden</name>
  <files>frontend/src/app/me/profile/page.tsx</files>
  <read_first>
    - frontend/src/app/me/profile/page.tsx — vollstaendig lesen: handleSubmit (lines 249-280), State-Deklarationen (lines 100-140), ProfileStoryCard-Aufruf, bestehende Error/Success-Handling-Pattern
    - frontend/src/lib/storyImageUpload.ts — uploadPendingStoryImages-Signatur (aus Plan 70-05)
    - frontend/src/lib/api.ts — uploadOwnProfileStoryImage-Signatur (aus Plan 70-05)
    - 70-PATTERNS.md — page.tsx handleSubmit-Erweiterung + State-Design
    - 70-UI-SPEC.md — Copywriting Contract fuer Fehler-/Fortschritts-Texte
  </read_first>
  <action>
    page.tsx minimal erweitern:

    1. State ergaenzen (nach bestehenden isUploadingAvatar-o.ae. States):
       const [pendingImages] = useState(() => new Map<string, File>())
       const [uploadProgress, setUploadProgress] = useState<Map<string, number>>(new Map())

    2. Callback-Funktion ergaenzen:
       function handlePendingImageAdded(pendingKey: string, file: File) { pendingImages.set(pendingKey, file) }

    3. ProfileStoryCard-Aufruf erweitern: onPendingImageAdded={handlePendingImageAdded}, uploadProgress={uploadProgress} ergaenzen

    4. handleSubmit erweitern: Vor dem updateOwnProfile-Call pending images hochladen:
       let resolvedStory = form.memberStory
       if (pendingImages.size > 0) {
         resolvedStory = await uploadPendingStoryImages(
           form.memberStory, pendingImages, uploadOwnProfileStoryImage,
           (key, pct) => setUploadProgress(prev => new Map(prev).set(key, pct))
         ) as typeof form.memberStory
       }
       Dann resolvedStory statt form.memberStory im updateOwnProfile-Payload.
       Fehler-Catch: Wenn uploadPendingStoryImages wirft → Fehlermeldung aus UI-SPEC: "Mindestens ein Bild konnte nicht hochgeladen werden. Die Geschichte wurde nicht gespeichert. Bitte erneut versuchen."
       Nach erfolgreichem Save: pendingImages.clear(), setUploadProgress(new Map())
       finally: setUploadProgress(new Map()) (immer bereinigen)

    5. Imports ergaenzen: uploadPendingStoryImages aus '@/lib/storyImageUpload', uploadOwnProfileStoryImage aus '@/lib/api'

    Zeilen in page.tsx zaehlen vor und nach dem Edit — falls >450 Zeilen: Erweiterungen in eine Helfer-Datei auslagern (consistent mit CLAUDE.md).

    Begleitend: Migration 0089 auf lokaler Docker-DB anwenden (falls noch nicht durch Plan 70-02 Checkpoint geschehen):
    Dokumentiere in SUMMARY ob Migration angewendet wurde.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck 2>&1 | tail -5 && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>npm run typecheck ohne Fehler. npm test ohne neue Fehler. page.tsx enthaelt pendingImages-State, handlePendingImageAdded-Callback, uploadPendingStoryImages-Aufruf in handleSubmit, resolvedStory-Ersatz im updateOwnProfile-Payload, und korrekte Fehlermeldung.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UpdateOwnProfile body_json → applyStoryImageLifecycle | Unvertrauenswuerdiger body_json mit potenziell fremden media_asset_ids |
| cleanupStoryImageAsset → Dateisystem | Physisches Delete; nur nach owner_member_id-Verifizierung und isUploadPathWithinBase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-06-01 | Elevation of Privilege (IDOR) | body_json mit fremder media_asset_id eingebettet | mitigate | applyStoryImageLifecycle: Batch-IDOR-Check vor RenderHTMLWithResolver; 422 bei Fremd-ID; TestUpdateOwnProfileIDOR verifiziert (D-03/D-23) |
| T-70-06-02 | Tampering | Cleanup loescht falsche Assets | mitigate | DeleteStoryImageAsset WHERE id = $1 AND owner_member_id = $2 (DB-Level-Guard); cleanupStoryImageAsset prueft isUploadPathWithinBase vor os.RemoveAll |
| T-70-06-03 | Denial of Service | Sehr grosses body_json mit vielen image-Nodes → viele DB-Queries | mitigate | GetStoryImageAssetsByMember laedt alle Member-Assets in einer Query; IDOR-Check ist O(n) Lookup in Map; keine N+1 Abfragen |
| T-70-06-04 | Integrity | Orphane Assets nach Frontend-Upload-Fehler | accept | Einfachste Variante: Orphans werden beim naechsten Save via Cleanup-on-Save entfernt (da nicht mehr in body_json referenziert). Kein komplexer Rollback-API noetig. Risiko: temporaere Storage-Nutzung (max 10MB pro Bild). UAT-Nachweis in Plan 70-07 Szenario 6 ergaenzt. |
| T-70-06-05 | Tampering | Stored XSS via manipuliertem body_html (RenderHTMLWithResolver + bluemonday) | mitigate | RenderHTMLWithResolver erzeugt nur kontrollierte img-Tags; bluemonday-Policy (Plan 70-03) sanitisiert danach. Doppel-Defense. |
| T-70-06-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Packages |
</threat_model>

<verification>
Backend-Tests gruen (IDOR, Cleanup, RoundTrip):
`cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/handlers/... -run "TestStoryImage|TestUpdateOwnProfile.*IDOR" -count=1`

Gesamte Backend-Test-Suite:
`cd C:/Users/admin/Documents/Team4s/backend && go test ./... -count=1 2>&1 | tail -5`

Frontend typecheck:
`cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck`

Frontend Tests:
`cd C:/Users/admin/Documents/Team4s/frontend && npm test 2>&1 | tail -10`
</verification>

<success_criteria>
- app_profile_story_image.go: applyStoryImageLifecycle mit IDOR-Check und Cleanup-on-Save; gibt assetsURLMap zurueck fuer Resolver-Wiederverwendung
- app_profile.go: EXAKT zwei chirurgische Aenderungen: (1) applyStoryImageLifecycle-Aufruf nach ValidateJSON, (2) RenderHTMLWithResolver statt RenderHTML; keine weiteren Aenderungen
- ErrIDORViolation Sentinel-Error definiert ("story-bild gehört nicht diesem profil")
- page.tsx: pendingImages Map-State, handlePendingImageAdded Callback, uploadPendingStoryImages in handleSubmit, korrekte Fehlermeldung laut UI-SPEC
- go build ./... und npm run typecheck gruen
- TestUpdateOwnProfileIDOR, TestStoryImageCleanup, TestStoryImageRoundTrip gruen
- Bestehende Profile-Tests weiterhin gruen
</success_criteria>

<output>
Create `.planning/phases/70-tiptap-bilder-fuer-member-profilgeschichte/70-06-SUMMARY.md` when done
</output>
