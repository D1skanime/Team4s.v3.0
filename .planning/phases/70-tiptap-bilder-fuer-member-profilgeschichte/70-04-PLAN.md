---
phase: 70-tiptap-bilder-fuer-member-profilgeschichte
plan: 04
type: execute
wave: 2
depends_on: [70-02]
files_modified:
  - backend/internal/handlers/app_profile_story_image.go
  - backend/internal/models/member_profile.go
  - backend/internal/repository/member_profile_repository.go
  - backend/cmd/server/main.go
autonomous: true
requirements: [D-03, D-06, D-07, D-08, D-13, D-16, D-17, D-18, D-19, D-22]

must_haves:
  truths:
    - "POST /api/v1/me/profile/story-images nimmt multipart 'image' (jpg/png/webp, max 10MB) an"
    - "MIME-Validierung lehnt GIF ab (D-16)"
    - "Dateigroesse > 10MB wird abgelehnt (D-17)"
    - "Pixel-Bomb-Guard: W×H > 40_000_000 wird abgelehnt (D-19)"
    - "EXIF-Strip: imaging.Save re-enkodiert ohne EXIF-Metadaten (D-19)"
    - "Resize auf max 1600px Breite via imaging.Resize + imaging.Lanczos (D-19)"
    - "Pfad: /media/profile/{memberID}/story/{uuid}/original.{ext} (D-08)"
    - "media_assets INSERT mit owner_member_id = profile.MemberID (D-08/D-03)"
    - "Path-Traversal-Guard via isUploadPathWithinBase vor os.RemoveAll"
    - "Audit-Log EventType 'member_profile.story_image.uploaded'"
    - "StoryImageAssetRef, StoryImageUploadInput DTOs in models/member_profile.go"
    - "InsertStoryImageAsset, GetStoryImageAssetsByMember, DeleteStoryImageAsset in Repository"
    - "app_profile.go wird NICHT modifiziert (Split-Pflicht, bestehend 906 Zeilen)"
    - "go test ./internal/handlers/... -run TestStoryImageUpload gruen"
  artifacts:
    - path: "backend/internal/handlers/app_profile_story_image.go"
      provides: "UploadOwnProfileStoryImage handler + extractStoryImageIDsFromJSON + cleanupStoryImageAsset"
      contains: "UploadOwnProfileStoryImage"
    - path: "backend/internal/models/member_profile.go"
      provides: "StoryImageUploadInput + StoryImageAssetRef DTOs"
      contains: "StoryImageUploadInput"
    - path: "backend/internal/repository/member_profile_repository.go"
      provides: "InsertStoryImageAsset + GetStoryImageAssetsByMember + DeleteStoryImageAsset"
      contains: "InsertStoryImageAsset"
    - path: "backend/cmd/server/main.go"
      provides: "Route POST /api/v1/me/profile/story-images registriert"
      contains: "story-images"
  key_links:
    - from: "app_profile_story_image.go:UploadOwnProfileStoryImage"
      to: "member_profile_repository.go:InsertStoryImageAsset"
      via: "h.profileRepo.InsertStoryImageAsset(ctx, input)"
      pattern: "InsertStoryImageAsset"
    - from: "app_profile_story_image.go:extractStoryImageIDsFromJSON"
      to: "app_profile.go:UpdateOwnProfile"
      via: "Cleanup-Logik wird in Plan 70-06 in UpdateOwnProfile verdrahtet"
      pattern: "extractStoryImageIDsFromJSON"
---

<objective>
Backend Upload-Handler, DTOs und Repository-Methoden fuer Story-Bilder implementieren. Dies ist die server-side Implementierung fuer POST /api/v1/me/profile/story-images: MIME-Validierung, Pixel-Bomb-Guard, EXIF-Strip, Resize, member-eigener Pfad, media_assets-INSERT mit owner_member_id, Audit-Log. Ausserdem die Cleanup-Helpers, die in Plan 70-06 in UpdateOwnProfile verdrahtet werden.

Purpose: Member-seitige Upload-Seam; ohne diesen Handler koennen Profilbilder nicht hochgeladen werden.
Output: Neues app_profile_story_image.go (Handler + Helpers), erweiterte DTOs, drei neue Repository-Methoden, Route-Registrierung.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/ROADMAP.md
@C:/Users/admin/Documents/Team4s/.planning/phases/70-tiptap-bilder-fuer-member-profilgeschichte/70-CONTEXT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/70-tiptap-bilder-fuer-member-profilgeschichte/70-PATTERNS.md

<interfaces>
<!-- Aus app_profile.go (RESEARCH-verifiziert, exaktes Vorbild) -->

Auth-Guard Pattern (app_profile.go lines 339-351):
```go
func (h *AppAuthHandler) UploadOwnProfileStoryImage(c *gin.Context) {
    identity, ok := middleware.CommentAuthIdentityFromContext(c)
    if !ok {
        c.JSON(http.StatusUnauthorized, gin.H{"error": gin.H{"message": "Anmeldung erforderlich."}})
        return
    }
    if strings.TrimSpace(identity.AppUserStatus) == models.AppUserStatusDisabled {
        c.JSON(http.StatusForbidden, gin.H{"error": gin.H{"message": "Deaktivierte Benutzer dürfen keine Story-Bilder hochladen."}})
        return
    }
}
```

Pfad-Konstruktion (app_profile.go lines 401-408 fuer Avatar, angepasst fuer Story):
```go
mediaID := uuid.New().String()
relativeDir := fmt.Sprintf("/media/profile/%d/story/%s", profile.MemberID, mediaID)
filename := "original." + ext
relativePath := relativeDir + "/" + filename
absoluteDir := filepath.Join(h.mediaStorageDir, "profile", fmt.Sprintf("%d", profile.MemberID), "story", mediaID)
absolutePath := filepath.Join(absoluteDir, filename)
```

Path-Traversal-Guard (aus app_profile.go lines 752, 764 + media_upload.go):
```go
if ok, err := isUploadPathWithinBase(h.mediaStorageDir, targetDir); err == nil && ok {
    _ = os.RemoveAll(targetDir)
}
// isUploadPathWithinBase ist package-interne Funktion in handlers-Package
```

Pixel-Bomb-Guard (aus admin_content_release_version_media.go line 317):
```go
if cfg.Width*cfg.Height > 40_000_000 {
    c.JSON(http.StatusBadRequest, gin.H{"error": gin.H{"message": "Das Bild enthält zu viele Pixel (max. 40 MP)."}})
    return
}
```

EXIF-Strip + Resize (aus app_profile.go Pattern, imaging-basiert):
```go
if cfg.Width > 1600 {
    img = imaging.Resize(img, 1600, 0, imaging.Lanczos)
}
if err := imaging.Save(img, absolutePath); err != nil { // EXIF-Strip automatisch durch Re-Enkodierung
    _ = os.RemoveAll(absoluteDir)
    writeInternalErrorResponse(c, "Interner Serverfehler.", err, "Story-Bild konnte nicht gespeichert werden.")
    return
}
```

Audit-Log (app_profile.go lines 476-494):
```go
if h.auditLogRepo != nil {
    _ = h.auditLogRepo.Write(ctx, repository.AuditLogEntry{
        ActorAppUserID: &actorAppUserID,
        EventType: "member_profile.story_image.uploaded",
        ScopeType: "member_profile",
        ScopeID: &memberID,
        Action: "member_profile.story_image.upload",
        Outcome: "success",
        Payload: map[string]any{"mime_type": mimeType, "size_bytes": sizeBytes, "media_asset_id": newAssetID},
    })
}
```

Response (app_profile.go lines 203-206):
```go
c.JSON(http.StatusCreated, gin.H{"data": gin.H{
    "media_asset_id": newAssetID,
    "public_url": strings.TrimRight(h.mediaBaseURL, "/") + relativePath,
}})
```

<!-- Repository-Pattern (member_profile_repository.go, pgxpool.Pool) -->
Aus member_profile_repository.go lines 52-79:
```go
func (r *MemberProfileRepository) InsertStoryImageAsset(ctx context.Context, input models.StoryImageUploadInput) (int64, error) {
    var id int64
    err := r.db.QueryRow(ctx,
        `INSERT INTO media_assets (file_path, public_url, storage_path, mime_type, size_bytes, width, height, owner_member_id)
         VALUES ($1, $2, $3, $4, $5, $6, $7, $8) RETURNING id`,
        input.FilePath, input.PublicURL, input.StoragePath, input.MimeType,
        input.SizeBytes, input.Width, input.Height, input.OwnerMemberID,
    ).Scan(&id)
    return id, err
}
```

<!-- main.go Route-Pattern (aus bestehenden App-Auth-Routen) -->
Bestehendes Muster fuer /api/v1/me/* Routen:
meRoutes.POST("/profile/story-images", appAuthHandler.UploadOwnProfileStoryImage)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DTOs in member_profile.go und Repository-Methoden</name>
  <files>backend/internal/models/member_profile.go, backend/internal/repository/member_profile_repository.go</files>
  <read_first>
    - backend/internal/models/member_profile.go — vollstaendig lesen: MemberProfileAvatarUploadInput (lines 113-123) als exaktes Vorbild; bestehende DTOs nicht brechen
    - backend/internal/repository/member_profile_repository.go — vollstaendig lesen: Pool-Zugriff, pgxpool.Pool-Typ, bestehende Method-Signaturen; MemberProfileRepository-Struct
  </read_first>
  <action>
    In member_profile.go nach dem letzten bestehenden Upload-Input-Struct (MemberProfileBackgroundUploadInput oder letztem DTO) zwei neue Structs ergaenzen:
    - StoryImageUploadInput: FilePath, PublicURL, StoragePath, MimeType string; SizeBytes int64; Width, Height int; OwnerMemberID int64
    - StoryImageAssetRef: ID int64; StoragePath string; OwnerMemberID int64

    In member_profile_repository.go drei neue Methoden auf *MemberProfileRepository ergaenzen:
    - InsertStoryImageAsset(ctx, input models.StoryImageUploadInput)(int64, error): INSERT INTO media_assets (..., owner_member_id) RETURNING id. Spalten pruefen — falls media_assets andere Pflicht-Spalten hat (z.B. status), auf NULL-faehig oder Default-Wert setzen.
    - GetStoryImageAssetsByMember(ctx, memberID int64)([]models.StoryImageAssetRef, error): SELECT id, storage_path, owner_member_id FROM media_assets WHERE owner_member_id = $1
    - DeleteStoryImageAsset(ctx, assetID, ownerMemberID int64) error: DELETE FROM media_assets WHERE id = $1 AND owner_member_id = $2 (Sicherheits-Guard im Query)

    Zeilen in member_profile_repository.go nach Edit zaehlen — falls >450 Zeilen: neue Methoden in eine Datei member_profile_story_image_repository.go auslagern (analoges Split-Pattern wie Phase-69-02 Entscheidung).
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go build ./internal/models/... && go build ./internal/repository/...</automated>
  </verify>
  <done>go build fuer models und repository erfolgreich. StoryImageUploadInput und StoryImageAssetRef in member_profile.go. Drei neue Repository-Methoden kompilieren ohne Fehler.</done>
</task>

<task type="auto">
  <name>Task 2: Upload-Handler app_profile_story_image.go + Route-Registrierung</name>
  <files>backend/internal/handlers/app_profile_story_image.go, backend/cmd/server/main.go</files>
  <read_first>
    - backend/internal/handlers/app_profile.go — UploadOwnProfileAvatar (lines 338-497) als EXAKTES Vorbild: Auth-Guard, MIME-Detect, Size-Check, Profil-Laden, Pfad-Konstruktion, os.MkdirAll, image.Decode, imaging.Resize/Save, isUploadPathWithinBase, media_assets-INSERT, Audit-Log, Response. cleanupPreviousAvatarFiles (lines 745-755) als Vorbild fuer cleanupStoryImageAsset.
    - backend/cmd/server/main.go — Bereich der /api/v1/me/* Routen-Registrierung fuer AppAuthHandler
    - 70-PATTERNS.md — vollstaendige app_profile_story_image.go Patterns (detectStoryImage, UploadOwnProfileStoryImage, cleanupStoryImageAsset, extractStoryImageIDsFromJSON)
  </read_first>
  <action>
    Neue Datei backend/internal/handlers/app_profile_story_image.go anlegen. Package: handlers. Imports wie in PATTERNS.md Sektion 1. Methoden auf *AppAuthHandler:

    1. UploadOwnProfileStoryImage: Vollstaendige Implementierung analog UploadOwnProfileAvatar mit diesen Unterschieden:
       - Formfield: "image" (nicht "source_file")
       - MIME-Allowlist: nur image/jpeg, image/png, image/webp (kein GIF — D-16)
       - Max-Size: 10MB (D-17)
       - Pixel-Bomb-Guard nach image.DecodeConfig: W*H > 40_000_000 → 400 (D-19)
       - Resize: if cfg.Width > 1600 { img = imaging.Resize(img, 1600, 0, imaging.Lanczos) } (D-19)
       - imaging.Save (EXIF-Strip, D-19)
       - Pfad: /media/profile/{memberID}/story/{uuid}/original.{ext} (D-08)
       - media_assets INSERT via h.profileRepo.InsertStoryImageAsset (setzt owner_member_id, D-08)
       - Response: 201 Created mit {data: {media_asset_id, public_url}}
       - Audit-Log EventType "member_profile.story_image.uploaded" (D-08, Observability)
       - isUploadPathWithinBase vor os.RemoveAll fuer Cleanup bei Fehler (Path-Traversal-Guard)

    2. extractStoryImageIDsFromJSON(bodyJSON []byte) []int64: Parst TipTap-JSON-String, traversiert alle Nodes, sammelt alle media_asset_id-Werte aus Nodes mit type="image". Gibt leeres Slice zurueck wenn kein Image-Node vorhanden.

    3. cleanupStoryImageAsset(mediaStorageDir, storagePath string): Analog cleanupPreviousAvatarFiles — trimmt Prefix "/media/", baut absoluten Pfad, isUploadPathWithinBase-Check, os.RemoveAll des Verzeichnisses (nicht nur der Datei — uuid-Dir).

    In main.go: Route POST /api/v1/me/profile/story-images → appAuthHandler.UploadOwnProfileStoryImage ergaenzen, analog zu bestehenden /me/profile-Routen. Kein Edge-Fall: Route-Prefix und Auth-Middleware sind bereits fuer /api/v1/me/* eingerichtet.

    WICHTIG: app_profile.go wird NICHT veraendert (906 Zeilen, Split-Pflicht aus CLAUDE.md). Cleanup-on-Save-Verdrahtung (IDOR-Check + Referenz-Diff + Delete) kommt in Plan 70-06.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go build ./... && go test ./internal/handlers/... -run "TestStoryImageUpload.*Validation" -count=1</automated>
  </verify>
  <done>go build ./... erfolgreich. TestStoryImageUploadValidation_GIFRejected, _TooLarge, _PixelBomb gruen. Route POST /api/v1/me/profile/story-images in main.go registriert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /me/profile/story-images | Unvertrauenswuerdige Datei; MIME, Size, Pixel-Bomb werden vor Verarbeitung geprueft |
| imaging.Decode → Dateisystem | Bildverarbeitung mit Fremddaten; Pixel-Bomb-Guard und Resize verhindern Ressourcen-Erschoepfung |
| absoluteDir → isUploadPathWithinBase | Pfad-Traversal-Schutz vor os.MkdirAll und os.RemoveAll |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-04-01 | Tampering | Unrestricted File Upload: GIF/SVG/HTML als Bild getarnt | mitigate | mimetype.DetectReader (Magic-Byte-Check, nicht nur Extension) + Allowlist image/jpeg,image/png,image/webp; TestStoryImageUploadValidation_GIFRejected verifiziert |
| T-70-04-02 | Denial of Service | Decompression Bomb (riesige PNG/JPEG) | mitigate | image.DecodeConfig + W×H > 40_000_000-Check VOR vollstaendigem Decode; TestStoryImageUploadValidation_PixelBomb verifiziert |
| T-70-04-03 | Information Disclosure | EXIF-GPS-Koordinaten im hochgeladenen Bild | mitigate | imaging.Save re-enkodiert ohne EXIF; TestStoryImageUploadExifStrip verifiziert |
| T-70-04-04 | Tampering | Pfad-Traversal via manipulierten memberID/uuid | mitigate | isUploadPathWithinBase vor os.MkdirAll/os.RemoveAll; memberID kommt aus verifizierter Identity, uuid aus uuid.New() |
| T-70-04-05 | Elevation of Privilege | IDOR: Member B laedt Bild unter Member-A-Pfad hoch | mitigate | Pfad basiert auf identity.MemberID aus Auth-Middleware (nicht aus Request-Body); owner_member_id = identity.MemberID gesetzt |
| T-70-04-06 | Tampering | Dateisystem: orphane Assets bei Upload-Fehler | mitigate | os.RemoveAll(absoluteDir) bei jedem Fehler-Return nach MkdirAll; isUploadPathWithinBase-Check |
| T-70-04-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Packages — disintegration/imaging, mimetype, uuid sind bestehende go.mod-Eintraege |
</threat_model>

<verification>
Handler-Upload-Tests gruen:
`cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/handlers/... -run "TestStoryImageUpload" -count=1`

Kein Compile-Fehler:
`cd C:/Users/admin/Documents/Team4s/backend && go build ./...`

app_profile.go unveraendert (Zeilenanzahl identisch):
`wc -l backend/internal/handlers/app_profile.go`

Route registriert:
`grep "story-images" backend/cmd/server/main.go`
</verification>

<success_criteria>
- app_profile_story_image.go: UploadOwnProfileStoryImage mit MIME/Size/Pixel-Bomb/EXIF/Resize/Pfad/DB/Audit/Response
- app_profile_story_image.go: extractStoryImageIDsFromJSON + cleanupStoryImageAsset Helpers
- member_profile.go: StoryImageUploadInput + StoryImageAssetRef DTOs
- member_profile_repository.go: InsertStoryImageAsset, GetStoryImageAssetsByMember, DeleteStoryImageAsset
- main.go: Route POST /api/v1/me/profile/story-images registriert
- app_profile.go NICHT veraendert (906 Zeilen bleiben)
- go build ./... erfolgreich
- TestStoryImageUploadValidation-Tests gruen
</success_criteria>

<output>
Create `.planning/phases/70-tiptap-bilder-fuer-member-profilgeschichte/70-04-SUMMARY.md` when done
</output>
