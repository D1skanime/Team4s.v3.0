---
phase: 70-tiptap-bilder-fuer-member-profilgeschichte
plan: 03
type: execute
wave: 2
depends_on: [70-02]
files_modified:
  - backend/internal/services/tiptap_service.go
autonomous: true
requirements: [D-01, D-04, D-20, D-23]

must_haves:
  truths:
    - "allowedTipTapNodes enthaelt 'image': true"
    - "validateNode hat case 'image': prueft media_asset_id (positiv), alignment (left/center/right), width_percent (1-100)"
    - "RenderHTMLWithResolver(input, func(int64)(string,bool))(string,error) existiert — rueckwaertskompatibel"
    - "renderNodeWithResolver hat case 'image': loest media_asset_id via Resolver auf, generiert <img src=... style=width:N% class=story-img-align-X>"
    - "Fehlendes Asset (Resolver ok=false) → Node still ueberspringen (D-04)"
    - "bluemonday-Policy erlaubt img mit src-Regex ^/media/profile/\\d+/story/[a-z0-9-]+/original\\.(jpg|jpeg|png|webp)$, style^width:\\d{1,3}%$, class^story-img-align-(left|center|right)$"
    - "Bestehende RenderHTML-Signatur unveraendert (kein Breaking Change)"
    - "go test ./internal/services/... -run TestTipTap.*Image gruene Tests"
  artifacts:
    - path: "backend/internal/services/tiptap_service.go"
      provides: "Image-Node-Unterstuetzung: Allowlist, Validator, Renderer, bluemonday-Policy"
      contains: "allowedTipTapNodes.*image"
  key_links:
    - from: "backend/internal/services/tiptap_service.go:RenderHTMLWithResolver"
      to: "backend/internal/handlers/app_profile_story_image.go:UpdateOwnProfile"
      via: "Handler ruft RenderHTMLWithResolver mit DB-Resolver-Closure auf"
      pattern: "RenderHTMLWithResolver"
    - from: "tiptap_service.go:newTipTapSanitizerPolicy"
      to: "img-src-Regex"
      via: "bluemonday AllowAttrs src Matching Regexp"
      pattern: "story-img-align"
---

<objective>
TipTap-Service um Image-Node erweitern: Allowlist, Validator, Renderer und bluemonday-Policy. Dies ist die zentrale Backend-Schicht fuer D-01 (nur media_asset_id im body_json), D-04 (silent skip fehlender Assets), D-20 (strenge img-Sanitizing) und D-23 (XSS-Schutz via Policy).

Purpose: Ohne diese Schicht kann kein Image-Node im TipTap-JSON validiert, gerendert oder sanitisiert werden.
Output: Erweitertes tiptap_service.go mit Image-Unterstuetzung. Alle Wave-0-Tests fuer den Service werden gruen.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/ROADMAP.md
@C:/Users/admin/Documents/Team4s/.planning/phases/70-tiptap-bilder-fuer-member-profilgeschichte/70-CONTEXT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/70-tiptap-bilder-fuer-member-profilgeschichte/70-PATTERNS.md

<interfaces>
<!-- Bestehende tiptap_service.go-Schnittstellen (RESEARCH-verifiziert) -->

allowedTipTapNodes (Zeilen 37-42):
```go
var allowedTipTapNodes = map[string]bool{
    "doc": true, "paragraph": true, "text": true,
    "heading": true, "bulletList": true, "orderedList": true,
    "listItem": true, "blockquote": true, "horizontalRule": true,
    "table": true, "tableRow": true, "tableCell": true, "tableHeader": true,
    // NEU: "image": true,
}
```

validateNode switch (ab Zeile 70) — bestehende Case-Struktur (tableRow, etc.) als Muster.

renderNode switch (Zeilen 139-224) — bestehender Case "paragraph", "horizontalRule" etc. als Muster.
Funktion-Signatur: func renderNode(node TipTapNode, sb *strings.Builder) → NEU: func renderNodeWithResolver(node TipTapNode, sb *strings.Builder, resolver func(int64)(string,bool))

Bestehende RenderHTML-Signatur (bleibt erhalten):
func (s *TipTapService) RenderHTML(input string) (string, error)

Neue Methode (zusaetzlich):
func (s *TipTapService) RenderHTMLWithResolver(input string, resolver func(mediaAssetID int64)(string, bool)) (string, error)

newTipTapSanitizerPolicy (Zeilen 361-370):
```go
func newTipTapSanitizerPolicy() *bluemonday.Policy {
    p := bluemonday.NewPolicy()
    p.AllowElements("p", "h1", "h2", "h3", "strong", "em",
        "ul", "ol", "li", "blockquote",
        "table", "thead", "tbody", "tr", "th", "td", "hr", "span")
    p.AllowAttrs("class").OnElements("span", "td", "th")
    p.AllowAttrs("colspan", "rowspan").OnElements("td", "th")
    p.AllowAttrs("data-color-token").OnElements("span")
    return p
}
```
NEU: "img" + drei AllowAttrs-Calls.
Import "regexp" hinzufuegen.

TipTapNode-Struct (bestehend):
```go
type TipTapNode struct {
    Type    string                 `json:"type"`
    Attrs   map[string]interface{} `json:"attrs,omitempty"`
    Content []TipTapNode           `json:"content,omitempty"`
    Text    string                 `json:"text,omitempty"`
    Marks   []TipTapMark           `json:"marks,omitempty"`
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Allowlist + validateNode-Erweiterung</name>
  <files>backend/internal/services/tiptap_service.go</files>
  <read_first>
    - backend/internal/services/tiptap_service.go — vollstaendig lesen (906 Zeilen laut RESEARCH): allowedTipTapNodes, validateNode-Switch, TipTapNode-Struct, vorhandene Imports
    - backend/internal/services/tiptap_service_test.go — Wave-0-Tests fuer TestTipTapValidateImageNode_* als Ziel dieser Task
  </read_first>
  <behavior>
    - allowedTipTapNodes["image"] = true → ValidateJSON akzeptiert image-Nodes
    - case "image" in validateNode: rawID aus node.Attrs["media_asset_id"] muss float64 und > 0 sein → sonst error
    - alignment wenn gesetzt: nur "left", "center", "right" → sonst error
    - width_percent wenn gesetzt: float64, 1-100 → sonst error
    - Kein Alt-Text, keine Caption (D-05) — werden nicht validiert/benoetigt
  </behavior>
  <action>
    tiptap_service.go lesen. In allowedTipTapNodes die Zeile "image": true ergaenzen (nach "horizontalRule"). In der validateNode-Funktion einen neuen case "image" ergaenzen — analog zu case "table", "tableRow" etc. (direkt nach dem letzten Table-Case). Die Attribut-Pruefung extrahiert media_asset_id via type-assertion zu float64 (JSON-Zahlen sind float64 in Go interface{}), prueft > 0. Alignment-Check: string-Assertion + switch mit erlaubten Werten. width_percent: float64-Assertion, 1 <= wp <= 100. Kein return-Fehler wenn Attribut fehlt (alignment/width_percent sind optional, Defaults kommen aus dem Frontend).

    Sicherstellen: Zeilen in tiptap_service.go zaehlen nach Edit — die Datei darf durch diese reine Ergaenzung nicht uebers 450-Zeilen-Limit wachsen (Zeile 906 ist bereits ueberschritten, aber das ist bestehender Zustand; neuer Code hier ist minimal: ~25 Zeilen).
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go build ./internal/services/... && go test ./internal/services/... -run "TestTipTapValidate.*Image" -count=1</automated>
  </verify>
  <done>go build erfolgreich. TestTipTapValidateImageNode_Valid gruен. TestTipTapValidateImageNode_MissingID, _InvalidAlignment, _InvalidWidthPercent schlagen mit Fehler fehl (wie erwartet = PASS fuer Negative-Tests).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RenderHTMLWithResolver + bluemonday-img-Policy</name>
  <files>backend/internal/services/tiptap_service.go</files>
  <read_first>
    - backend/internal/services/tiptap_service.go — RenderHTML, renderNode (Zeilen 139-224), newTipTapSanitizerPolicy (Zeilen 361-370)
    - 70-PATTERNS.md — Sektion "tiptap_service.go MODIFY" fuer exakten Code-Pattern des renderNode image-case und der bluemonday-Policy-Erweiterung
    - backend/internal/services/tiptap_service_test.go — TestTipTapRenderHTMLImageNode_* und TestTipTapSanitizeImage_* als Ziel dieser Task
  </read_first>
  <behavior>
    - RenderHTML(input string)(string,error) bleibt unveraendert (kein Breaking Change fuer bestehende Aufrufer)
    - RenderHTMLWithResolver(input, func(int64)(string,bool))(string,error) als neue Methode
    - Intern: renderNodeWithResolver(node, sb, resolver) statt renderNode(node, sb) — beide Varianten koennen koexistieren (renderNode als Wrapper mit nil-Resolver oder Umbenennung mit Fallback)
    - case "image" in renderNodeWithResolver: resolver nil → return (D-04). rawID = node.Attrs["media_asset_id"].(float64); mediaAssetID = int64(rawID); resolver(mediaAssetID) → (url, ok); ok=false → return. width=60.0 default, alignment="center" default. Ausgabe: <img src="...htmlescaped..." style="width:N%" class="story-img-align-X">
    - bluemonday-Policy: img hinzufuegen zu AllowElements-Liste; AllowAttrs("src").Matching(regexp.MustCompile(...)).OnElements("img"); AllowAttrs("style").Matching(...).OnElements("img"); AllowAttrs("class").Matching(...).OnElements("img")
    - src-Regex: ^/media/profile/\d+/story/[a-z0-9-]+/original\.(jpg|jpeg|png|webp)$
    - style-Regex: ^width:\s*\d{1,3}%$
    - class-Regex: ^story-img-align-(left|center|right)$
    - Import "regexp" und "html/template" hinzufuegen (template.HTMLEscapeString fuer src)
  </behavior>
  <action>
    In tiptap_service.go eine neue Methode RenderHTMLWithResolver implementieren. Die bestehende RenderHTML-Methode ruft intern RenderHTMLWithResolver(input, nil) auf. renderNode wird zu renderNodeWithResolver mit zusaetzlichem resolver-Parameter umbenannt — bestehende Aufrufe von renderNode (intern) werden angepasst, um nil als Resolver zu uebergeben. KEINE aenderung an externen Aufrufen. Der case "image" wird in den renderNodeWithResolver-Switch ergaenzt (nach "horizontalRule" oder am Ende der Switch-Faelle). In newTipTapSanitizerPolicy wird "img" zu den AllowElements hinzugefuegt und drei AllowAttrs-Calls ergaenzt. Import "regexp" + pruefen ob "html/template" bereits importiert ist (falls nicht: ergaenzen). Alle Felder aus dem PATTERNS.md-Code-Pattern exakt uebernehmen (insbesondere den src-Regex und die fmt.Sprintf-Vorlage fuer das img-Tag). Zeilenanzahl nach Edit pruefen — tiptap_service.go ist bereits >450 Zeilen (bestehender Zustand); die Erweiterungen hier sind ~50 Zeilen und unvermeidlich. Falls die Datei nach Edit >600 Zeilen hat → Hinweis in SUMMARY notieren (kein Split moeglich ohne Breaking Change an internen Funktionen).
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go build ./internal/services/... && go test ./internal/services/... -run "TestTipTap.*Image" -count=1</automated>
  </verify>
  <done>go build erfolgreich (kein Compile-Fehler). Alle TestTipTapRenderHTMLImageNode_* und TestTipTapSanitizeImage_* Tests gruen. Bestehende TipTap-Tests (non-Image) weiterhin gruen: go test ./internal/services/... -count=1 alle PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TipTap body_json → renderNodeWithResolver | Unvertrauenswuerdiger body_json von Client; Resolver-Callback ist serverseitig kontrolliert |
| renderNodeWithResolver-Output → bluemonday | Renderer-Output wird durch Policy-Filter geleitet; Policy ist letzte Verteidigungslinie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-03-01 | Tampering (XSS) | bluemonday src-Regex zu permissiv | mitigate | Regex ^/media/profile/\d+/story/[a-z0-9-]+/original\.(jpg|jpeg|png|webp)$ in Test TestTipTapSanitizeImage_BlocksExternalSrc verifiziert |
| T-70-03-02 | Tampering (XSS) | style-Bypass via CSS-Injection | mitigate | style-Regex ^width:\s*\d{1,3}%$ laesst nur Width-Deklarationen durch; TestTipTapSanitizeImage_BlocksStyleBeyondWidth verifiziert |
| T-70-03-03 | Information Disclosure | Renderer loest media_asset_id via Resolver auf — URL koennte intern/sensitiv sein | accept | /media-URLs sind offentliche Asset-URLs; kein privates Datenleck; Pfad-Format ist deterministisch |
| T-70-03-04 | Denial of Service | Viele image-Nodes in body_json → viele Resolver-Aufrufe | mitigate | D-18: keine harte Bildanzahl-Grenze, aber body_json-Groessen-Validierung durch ValidateJSON schutzt vor beliebig grossen Dokumenten; Resolver-Calls sind DB-Batch-Query in Handler |
| T-70-03-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Packages — bluemonday und regexp sind bestehende Imports |
</threat_model>

<verification>
Alle Image-Service-Tests gruen:
`cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/services/... -run "TestTipTap.*Image" -count=1`

Bestehende Tests unveraendert gruен:
`cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/services/... -count=1 2>&1 | tail -5`

Kein Compile-Fehler:
`cd C:/Users/admin/Documents/Team4s/backend && go build ./...`
</verification>

<success_criteria>
- tiptap_service.go: allowedTipTapNodes enthaelt "image": true
- tiptap_service.go: case "image" in validateNode mit media_asset_id/alignment/width_percent-Pruefung
- tiptap_service.go: RenderHTMLWithResolver-Methode existiert, RenderHTML delegiert an sie mit nil-Resolver
- tiptap_service.go: case "image" in renderNodeWithResolver mit D-04-konformem silent-skip
- tiptap_service.go: newTipTapSanitizerPolicy enthaelt img + drei AllowAttrs-Calls mit Regex-Matching
- Alle Wave-0-TipTap-Service-Tests gruen (mindestens 10 Tests)
- Bestehende nicht-Image TipTap-Tests weiterhin gruen
</success_criteria>

<output>
Create `.planning/phases/70-tiptap-bilder-fuer-member-profilgeschichte/70-03-SUMMARY.md` when done
</output>
