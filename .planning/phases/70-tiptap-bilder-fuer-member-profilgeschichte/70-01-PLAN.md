---
phase: 70-tiptap-bilder-fuer-member-profilgeschichte
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/services/tiptap_service_test.go
  - backend/internal/handlers/app_profile_story_image_test.go
  - frontend/src/components/editor/StoryImageExtension.test.ts
  - frontend/src/lib/storyImageUpload.test.ts
autonomous: true
requirements: [D-01, D-03, D-16, D-17, D-19, D-20, D-21, D-22, D-23]

must_haves:
  truths:
    - "Backend-Tests fuer TipTap-Image-Node existieren und sind rot (ValidateJSON, RenderHTML, Sanitizing)"
    - "Backend-Tests fuer Story-Upload existieren und sind rot (MIME/Size/EXIF, IDOR, Cleanup)"
    - "Frontend-Tests fuer StoryImageExtension und uploadPendingStoryImages existieren und sind rot"
    - "Alle Test-Dateien kompilieren (go build ./..., npm run typecheck)"
  artifacts:
    - path: "backend/internal/services/tiptap_service_test.go"
      provides: "Red tests fuer Image-Node (TestTipTapValidateImageNode, TestTipTapRenderHTMLImageNode, TestTipTapSanitizeImage)"
    - path: "backend/internal/handlers/app_profile_story_image_test.go"
      provides: "Red tests fuer Upload-Handler und Cleanup (TestStoryImageUploadValidation, TestStoryImageUploadExifStrip, TestUpdateOwnProfileIDOR, TestStoryImageRoundTrip, TestStoryImageCleanup)"
    - path: "frontend/src/components/editor/StoryImageExtension.test.ts"
      provides: "Red tests fuer Attribut-Serialisierung und pending_key-Bereinigung"
    - path: "frontend/src/lib/storyImageUpload.test.ts"
      provides: "Red tests fuer uploadPendingStoryImages (Marker-Swap, Fehler-Atomizitaet)"
  key_links:
    - from: "backend/internal/services/tiptap_service_test.go"
      to: "backend/internal/services/tiptap_service.go"
      via: "TestTipTapValidateImageNode ruft ValidateJSON auf"
      pattern: "TestTipTapValidate.*Image"
    - from: "backend/internal/handlers/app_profile_story_image_test.go"
      to: "backend/internal/handlers/app_profile_story_image.go"
      via: "TestStoryImageUpload.*Validation prueoft MIME/Size-Rejektion"
      pattern: "TestStoryImage.*"
---

<objective>
Wave-0-Teststubs fuer alle Phase-70-Anforderungen anlegen. Tests beschreiben das erwartete Verhalten (rot), bevor die Implementierung existiert. Kein Produktionscode wird in diesem Plan veraendert.

Purpose: Nyquist-Compliance — jede Anforderung (D-01, D-03, D-16, D-17, D-19, D-20, D-21, D-22, D-23) hat vor der Implementierung eine prüfbare Testbeschreibung.
Output: Vier Test-Dateien mit roten Tests, die nach der Implementierung grün werden sollen.
</objective>

<execution_context>
@C:/Users/admin/Documents/Team4s/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/Documents/Team4s/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/ROADMAP.md
@C:/Users/admin/Documents/Team4s/.planning/phases/70-tiptap-bilder-fuer-member-profilgeschichte/70-CONTEXT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/70-tiptap-bilder-fuer-member-profilgeschichte/70-VALIDATION.md

<interfaces>
<!-- Bestehende TipTap-Service-Schnittstelle (Erwartung fuer Tests) -->
<!-- Aus backend/internal/services/tiptap_service.go (gelesen in RESEARCH) -->

Aus tiptap_service.go:
- func (s *TipTapService) ValidateJSON(input string) error
- func (s *TipTapService) RenderHTML(input string) (string, error)
  → Wird zu: RenderHTMLWithResolver(input string, resolver func(int64)(string,bool)) (string, error)
- var allowedTipTapNodes = map[string]bool{ "doc", "paragraph", "text", "heading", "bulletList", "orderedList", "listItem", "blockquote", "horizontalRule", "table", "tableRow", "tableCell", "tableHeader" }
- func newTipTapSanitizerPolicy() *bluemonday.Policy

<!-- Handler-Schnittstelle (Erwartung fuer Tests) -->
Handler-Methode auf *AppAuthHandler:
- func (h *AppAuthHandler) UploadOwnProfileStoryImage(c *gin.Context)
  POST multipart/form-data, field "image", Response: {"data": {"media_asset_id": N, "public_url": "..."}}

<!-- Repository-Schnittstelle (Erwartung fuer Tests) -->
- func (r *MemberProfileRepository) InsertStoryImageAsset(ctx, models.StoryImageUploadInput) (int64, error)
- func (r *MemberProfileRepository) GetStoryImageAssetsByMember(ctx, memberID int64) ([]models.StoryImageAssetRef, error)
- func (r *MemberProfileRepository) DeleteStoryImageAsset(ctx, assetID, ownerMemberID int64) error

<!-- Frontend-Schnittstelle (Erwartung fuer Tests) -->
Aus storyImageUpload.ts (neu):
- uploadPendingStoryImages(doc, pendingFiles, uploadFn, onProgress?) → Promise<unknown>
- Traversiert TipTap-JSON, ersetzt pending_key-Nodes durch media_asset_id
- Bei Fehler → wirft Exception (atomarer Abbruch)

Aus StoryImageExtension.ts (neu):
- StoryImageExtension = Node.create({ name: 'image', group: 'block', atom: true, draggable: true })
- Attribute: media_asset_id, pending_key, preview_url, width_percent (default 60), alignment (default 'center')
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend TipTap-Service-Tests (rot)</name>
  <files>backend/internal/services/tiptap_service_test.go</files>
  <read_first>
    - backend/internal/services/tiptap_service.go — vollstaendige Allowlist (Zeilen 37-42), validateNode-Switch, renderNode-Switch, newTipTapSanitizerPolicy (Zeilen 361-370)
    - backend/internal/services/tiptap_service_test.go — bestehende Test-Struktur und Import-Pattern
  </read_first>
  <behavior>
    - TestTipTapValidateImageNode_Valid: ValidateJSON mit image-node ({type:"image", attrs:{media_asset_id:42, width_percent:60, alignment:"center"}}) → kein Fehler (ERWARTET: schlaegt fehl bis "image" in allowedTipTapNodes)
    - TestTipTapValidateImageNode_MissingID: ValidateJSON ohne media_asset_id → Fehler
    - TestTipTapValidateImageNode_InvalidAlignment: alignment="diagonal" → Fehler
    - TestTipTapValidateImageNode_InvalidWidthPercent: width_percent=150 → Fehler
    - TestTipTapRenderHTMLImageNode_WithResolver: RenderHTMLWithResolver mit Resolver der ID 42 → "/media/profile/1/story/abc/original.jpg" mappt → HTML enthaelt &lt;img src="/media/..." style="width:60%" class="story-img-align-center"&gt;
    - TestTipTapRenderHTMLImageNode_MissingAsset: Resolver gibt (_, false) → Node still ueberspringen (D-04), kein &lt;img&gt; im Output
    - TestTipTapRenderHTMLImageNode_NilResolver: kein Resolver → Node still ueberspringen
    - TestTipTapSanitizeImage_AllowsValidImg: bluemonday-Policy laesst src="/media/profile/1/story/abc/original.jpg" + style="width:60%" + class="story-img-align-center" durch
    - TestTipTapSanitizeImage_BlocksExternalSrc: src="https://evil.com/x.jpg" → src wird geleert/entfernt
    - TestTipTapSanitizeImage_BlocksScript: &lt;script&gt; in HTML → entfernt
    - TestTipTapSanitizeImage_BlocksStyleBeyondWidth: style="color:red;width:60%" → nur width:60% bleibt oder beides entfernt
  </behavior>
  <action>
    Bestehende tiptap_service_test.go lesen. Neue Testfunktionen an das bestehende Package-/Import-Muster anhaengen. Alle Image-bezogenen Tests werden als separate TestXxx-Funktionen geschrieben. Jeder Test baut ein minimales TipTap-JSON-Dokument als Eingabe-String und prueft den Output von ValidateJSON / RenderHTMLWithResolver (neue Signatur, die in Plan 70-03 implementiert wird) / Sanitizer-Policy. Tests duerfen compile-Fehler haben weil RenderHTMLWithResolver noch nicht existiert — das ist erwartet (Wave-0-Stub). Falls die Funktion noch nicht existiert, Test gegen die bestehende RenderHTML-Signatur schreiben und TODO-Kommentar setzen; der Test wird in Plan 70-03 angepasst. Wichtig: kein GIF erlaubt in Policy (kein image/gif in mime-Allowlist); src-Regex muss exakt ^/media/profile/\d+/story/[a-z0-9-]+/original\.(jpg|jpeg|png|webp)$ sein (analog PATTERNS.md tiptap_service.go-Pattern).
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/services/... -run "TestTipTap.*Image" -count=1 2>&1 | head -30</automated>
  </verify>
  <done>Tests kompilieren und scheitern mit FAIL (nicht mit compile-Fehler). Mindestens 10 Image-Test-Funktionen in tiptap_service_test.go vorhanden.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backend Handler-Tests und Frontend-Tests (rot)</name>
  <files>backend/internal/handlers/app_profile_story_image_test.go, frontend/src/components/editor/StoryImageExtension.test.ts, frontend/src/lib/storyImageUpload.test.ts</files>
  <read_first>
    - backend/internal/handlers/app_profile.go — bestehende Test-Struktur (nach Analogon suchen, z.B. UploadOwnProfileAvatar-Tests falls vorhanden), writeInternalErrorResponse, AppAuthHandler-Struktur
    - frontend/src/components/editor/ColorTokenExtension.ts — Attribut-Pattern (Vorlage fuer StoryImageExtension-Attribut-Tests)
    - frontend/src/lib/api.ts — authorizedUploadXhr, uploadOwnProfileAvatar als Analogon-Pattern
    - 70-VALIDATION.md — Per-Task-Test-Map fuer Testadressen
  </read_first>
  <behavior>
    Backend Handler-Tests (app_profile_story_image_test.go):
    - TestStoryImageUploadValidation_GIFRejected: multipart-Request mit image/gif MIME → 400 mit Fehlermeldung
    - TestStoryImageUploadValidation_TooLarge: Datei >10MB → 400
    - TestStoryImageUploadValidation_PixelBomb: W×H > 40_000_000 → 400
    - TestStoryImageUploadExifStrip: JPEG mit EXIF-GPS-Metadaten → nach Upload kein GPS im gespeicherten Bild (imaging.Save stripped EXIF)
    - TestUpdateOwnProfileIDOR: body_json mit media_asset_id die einem anderen Member gehoert → 422
    - TestStoryImageRoundTrip: (Integrations-Stub) body_json mit image-node → persistiert, reload erhaelt media_asset_id, width_percent, alignment
    - TestStoryImageCleanup: body_json ohne zuvor persistierte image-nodes → Datei + DB-Zeile der alten Assets werden geloescht

    Frontend-Tests (StoryImageExtension.test.ts):
    - StoryImageExtension hat name='image', group='block', atom=true
    - Default-Attribute: media_asset_id=null, pending_key=null, preview_url=null, width_percent=60, alignment='center'
    - alignment akzeptiert nur 'left'|'center'|'right'

    Frontend-Tests (storyImageUpload.test.ts):
    - uploadPendingStoryImages mit pending_key-Node → ruft uploadFn auf, ersetzt durch media_asset_id, setzt pending_key=null, preview_url=null
    - Fehler beim Upload eines Bildes → gesamte Funktion wirft (kein Partial-Success, D-06/D-07)
    - Kein pending_key → Node unveraendert, aber preview_url=null gesetzt (Pitfall 2)
  </behavior>
  <action>
    Drei neue Test-Dateien anlegen. Backend: app_profile_story_image_test.go im Package handlers. Test-Stubs nutzen httptest.NewRecorder + gin.New() analog bestehender Handler-Tests. Fuer EXIF-Strip-Test: Minimal-JPEG mit EXIF-Metadaten in-memory synthetisieren (bytes.Buffer + JPEG-Header), nach Handler-Call das Ergebnis mit exif-Library (falls vorhanden) oder Byte-Pattern pruefen; alternativ: imaging.Save-Aufruf via Reflection nachweisen (source-inspection wie Phase 37). Fuer IDOR-Test: Mock-repository das asset.OwnerMemberID != savender Member zurueckgibt. Fuer Integrations-Stubs (RoundTrip, Cleanup): Test-Skelett mit TODO-Kommentar, darf zunachst panic("implement") beinhalten — wird in Plan 70-06 ausgefuellt. Frontend: Vitest-Tests analog bestehenden Editor-Tests. uploadPendingStoryImages-Tests nutzen Mock-uploadFn-Promise.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/handlers/... -run "TestStoryImage" -count=1 2>&1 | head -20 && cd C:/Users/admin/Documents/Team4s/frontend && npm test -- --run storyImage 2>&1 | head -20</automated>
  </verify>
  <done>Alle vier Test-Dateien existieren. Backend-Tests kompilieren (ggf. mit TODO-Stubs). Frontend-Tests laufen durch Vitest. Keiner der neuen Tests ist gruen (alle FAIL oder SKIP bis Implementierung).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-Code → Produktionscode | Tests beschreiben erwartetes Verhalten; keine Produktionskonfiguration wird veraendert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01-SC | Tampering | Test-Dateien koennen falsch positive Tests enthalten | mitigate | Tests muessen FAIL (nicht PASS) nach Wave 0 — Reviewer prüft Status |
| T-70-01-02 | Information Disclosure | Test-Dateien mit echten Media-Pfaden/IDs | accept | Tests nutzen Dummy-Werte (0, "abc", "/media/profile/1/story/abc/original.jpg") — keine echten Daten |
</threat_model>

<verification>
Backend-Tests laufen und schlagen fehl:
`cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/services/... ./internal/handlers/... -run "StoryImage|TipTap.*Image" -count=1`

Frontend-Tests laufen:
`cd C:/Users/admin/Documents/Team4s/frontend && npm test -- --run storyImage 2>&1 | tail -5`

Kein Produktionscode veraendert:
`git diff --name-only | grep -v _test.go`
</verification>

<success_criteria>
- Vier Test-Dateien angelegt (tiptap_service_test.go ergaenzt, drei neue Dateien)
- Mindestens 18 neue Testfunktionen gesamt (10 Backend-Service, 7 Backend-Handler, 5 Frontend)
- Alle Tests kompilieren ohne Import-Fehler
- Keine Tests sind gruen (alle FAIL auf fehlende Implementierung)
- go build ./... und npm run typecheck schlagen nicht wegen der Test-Dateien fehl
</success_criteria>

<output>
Create `.planning/phases/70-tiptap-bilder-fuer-member-profilgeschichte/70-01-SUMMARY.md` when done
</output>
