---
phase: 59-ffentliches-fansub-member-profil
plan: 03
type: execute
wave: 2
depends_on:
  - 59-01
files_modified:
  - frontend/src/components/profile/MemberProfileHero.tsx
  - frontend/src/components/profile/RecentMediaSection.tsx
  - frontend/src/components/profile/RecentContributionsSection.tsx
  - frontend/src/components/profile/MembershipsSection.tsx
  - frontend/src/components/profile/profile.module.css
  - frontend/src/app/me/profile/page.tsx
autonomous: true
requirements:
  - D-12
  - D-13
  - D-14
  - D-15
  - D-16
must_haves:
  truths:
    - "MemberProfileHero, RecentMediaSection, RecentContributionsSection liegen in frontend/src/components/profile/"
    - "/me/profile/page.tsx importiert alle drei Komponenten aus @/components/profile/"
    - "MembershipsSection ist neu in frontend/src/components/profile/ vorhanden"
    - "Kein Build-Fehler durch defekte CSS-Modul-Imports nach der Verschiebung"
  artifacts:
    - path: "frontend/src/components/profile/MemberProfileHero.tsx"
      provides: "Globalisierter Hero mit isPublicView-Prop"
    - path: "frontend/src/components/profile/MembershipsSection.tsx"
      provides: "Neue Fansub-Gruppen-Section mit Gruppenlogo, -name und Rollen-Badges"
    - path: "frontend/src/components/profile/profile.module.css"
      provides: "CSS-Modul für alle profile/-Komponenten"
  key_links:
    - from: "frontend/src/app/me/profile/page.tsx"
      to: "frontend/src/components/profile/"
      via: "import-Pfade"
      pattern: "@/components/profile/MemberProfileHero"
---

<objective>
Drei bestehende Profil-Komponenten nach frontend/src/components/profile/ verschieben, CSS-Modul-Imports reparieren, neue MembershipsSection anlegen, Import-Pfade in /me/profile/page.tsx aktualisieren.

Purpose: Entkoppelt die wiederverwendbaren Profil-Komponenten von der /me/profile-Route so dass /members/[slug] (Plan 04) sie ohne Pfad-Hacks einbinden kann.
Output: Vier Komponenten + CSS-Modul in frontend/src/components/profile/, aktualisierte Imports in /me/profile/page.tsx.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/PROJECT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/59-ffentliches-fansub-member-profil/59-CONTEXT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/59-ffentliches-fansub-member-profil/59-PATTERNS.md
@C:/Users/admin/Documents/Team4s/.planning/phases/59-ffentliches-fansub-member-profil/59-01-SUMMARY.md

<interfaces>
Aus frontend/src/app/me/profile/components/MemberProfileHero.tsx (Quelldatei):
  - Import: Image from 'next/image', Eye + Save from 'lucide-react'
  - Import: Button, PageHeader from '@/components/ui'
  - Import: type MemberProfileData from '@/types/profile'
  - Import: styles from '../page.module.css'  ← DIESER Import muss repariert werden
  - Props-Typ enthält isSaving, canSave, onSave — im Public-Modus nicht gerendert

Aus frontend/src/app/me/profile/components/RecentMediaSection.tsx (Quelldatei):
  - Import: styles from '../page.module.css'  ← DIESER Import muss repariert werden
  - isPublicView-Prop bereits vorhanden

Aus frontend/src/app/me/profile/components/RecentContributionsSection.tsx (Quelldatei):
  - Import: styles from '../page.module.css'  ← DIESER Import muss repariert werden
  - isPublicView-Prop bereits vorhanden

Aus frontend/src/app/me/profile/page.tsx (Import-Zeilen werden aktualisiert):
  - import { MemberProfileHero } from './components/MemberProfileHero'
  - import { RecentContributionsSection } from './components/RecentContributionsSection'
  - import { RecentMediaSection } from './components/RecentMediaSection'

Aus frontend/src/types/profile.ts (Plan 01 Ergebnis):
  export interface PublicMemberProfileData { ... memberships: MemberProfileMembership[] ... }
  export interface MemberProfileMembership { fansub_group_id: string; fansub_group_name: string; fansub_group_slug: string; app_member_roles: string[] }

UI-Komponenten vorhanden:
  import { Badge, Card, SectionHeader } from '@/components/ui' — bereits im Projekt
  import { Users } from 'lucide-react' — Phase-58-Muster für Gruppen-Fallback-Icon
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Komponenten verschieben + CSS-Modul anlegen</name>
  <files>frontend/src/components/profile/MemberProfileHero.tsx, frontend/src/components/profile/RecentMediaSection.tsx, frontend/src/components/profile/RecentContributionsSection.tsx, frontend/src/components/profile/profile.module.css, frontend/src/app/me/profile/page.tsx</files>
  <read_first>
    - frontend/src/app/me/profile/components/MemberProfileHero.tsx — vollständige Datei; enthält alle Props, Styles-Import und Render-Logik
    - frontend/src/app/me/profile/components/RecentMediaSection.tsx — vollständige Datei
    - frontend/src/app/me/profile/components/RecentContributionsSection.tsx — vollständige Datei
    - frontend/src/app/me/profile/page.module.css — relevante CSS-Klassen heraussuchen, die von MemberProfileHero/RecentMedia/RecentContributions genutzt werden
    - frontend/src/app/me/profile/page.tsx — aktuelle Import-Zeilen für die drei Komponenten
  </read_first>
  <action>
    Schritt 1 — Verzeichnis sicherstellen: frontend/src/components/profile/ anlegen falls nicht vorhanden.

    Schritt 2 — profile.module.css anlegen: Neue CSS-Datei frontend/src/components/profile/profile.module.css mit den CSS-Klassen, die MemberProfileHero, RecentMediaSection und RecentContributionsSection aus page.module.css benötigen. Nur die tatsächlich referenzierten Klassen übertragen — kein blindes Copy-Paste der gesamten page.module.css.

    Schritt 3 — Drei Komponenten kopieren: Dateien nach frontend/src/components/profile/ kopieren (Write-Tool). In jeder Datei einzige Änderung: CSS-Import von '../page.module.css' auf './profile.module.css' ändern. Alle anderen Imports, Props und Render-Logik bleiben identisch.

    Schritt 3a — MemberProfileHero Prop-Typ auf Union erweitern: In der neu kopierten Datei frontend/src/components/profile/MemberProfileHero.tsx den Import ergänzen: import type { MemberProfileData, PublicMemberProfileData } from '@/types/profile'. Den Props-Typ der Komponente auf profile: MemberProfileData | PublicMemberProfileData ändern. Alle anderen Props (isPublicView, isSaving, canSave, onSave) bleiben erhalten; isSaving/canSave/onSave als optional markieren (? anhängen), damit die Komponente mit beiden Datentypen ohne TypeScript-Fehler nutzbar ist. Plan 04 setzt diese Union-Typisierung voraus — ohne sie schlägt der Build in /members/[slug] fehl.

    Schritt 4 — MemberProfileHero anpassen: Falls der Hero-Komponente aktuell profile_visibility='public' zur öffentlichen Route fehlt: isPublicView-Prop bereits eingebaut oder hinzufügen falls nicht vorhanden. Im Public-Modus: kein Save-Button, kein "Öffentliches Profil ansehen"-Button. Im eigenen Profil-Modus: Verhalten unverändert.

    Schritt 5 — /me/profile/page.tsx Import-Pfade aktualisieren: Die drei Zeilen von './components/MemberProfileHero', './components/RecentMediaSection', './components/RecentContributionsSection' auf '@/components/profile/MemberProfileHero', '@/components/profile/RecentMediaSection', '@/components/profile/RecentContributionsSection' ändern. Keine weiteren Änderungen.

    Die Originaldateien in frontend/src/app/me/profile/components/ bleiben bestehen (werden in Plan 04 für die Hintergrundbild-Card genutzt). Die drei verschobenen Dateien werden dort aber nicht mehr benötigt — sie können gelöscht werden wenn der Build sauber ist.

    Sprachqualität: Alle deutschen user-facing Strings in den Komponenten mit korrekten Umlauten.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>npm run build ohne Fehler. Die drei Dateien existieren in frontend/src/components/profile/. frontend/src/app/me/profile/page.tsx enthält "@/components/profile/" in den drei Import-Zeilen. Kein "../page.module.css"-Import in den verschobenen Dateien.</done>
</task>

<task type="auto">
  <name>Task 2: MembershipsSection neue Komponente</name>
  <files>frontend/src/components/profile/MembershipsSection.tsx</files>
  <read_first>
    - frontend/src/app/fansubs/[slug]/page.tsx — Gruppen-Darstellungs-Muster (Gruppenlogo, Badge-Liste)
    - frontend/src/components/ui/index.ts oder ähnliches — verfügbare UI-Komponenten (Badge, Card, SectionHeader) prüfen
    - frontend/src/types/profile.ts — MemberProfileMembership-Interface (fansub_group_id, fansub_group_name, fansub_group_slug, app_member_roles)
    - frontend/src/components/profile/profile.module.css — welche Klassen bereits vorhanden, ggf. neue hinzufügen
  </read_first>
  <action>
    Neue Datei frontend/src/components/profile/MembershipsSection.tsx.

    Imports: Link from 'next/link', Users from 'lucide-react', Badge + Card + SectionHeader aus '@/components/ui' (oder verfügbare Varianten), MemberProfileMembership from '@/types/profile', styles from './profile.module.css'.

    Props: memberships: MemberProfileMembership[]. Keine weiteren Props nötig.

    Render-Logik:
    - Bei leerem Array: Section mit SectionHeader "Fansub-Gruppen" und Text "Keine Gruppen eingetragen." (kein EmptyState aus einer nicht-existenten Komponente — Text in einem p-Tag reicht)
    - Für jede Membership: Card-Block mit Link zu /fansubs/[fansub_group_slug], darin:
      - Gruppenlogo-Placeholder: Image wenn gruppenspezifisches Logo vorhanden (Feld prüfen ob vorhanden in MemberProfileMembership), sonst Users-Icon aus lucide-react (size={32}, aria-hidden="true")
      - Gruppenname als Link-Text
      - Rollen-Badges: app_member_roles.map(role => Badge mit role als Text)
    - Wenn app_member_roles leer: kein Badge-Container rendern

    Kein "use client" — diese Komponente ist eine reine Render-Komponente, wird in einer Server Component eingebunden.

    Section-Header-Text: "Fansub-Gruppen" — korrekter Umlaut (kein ASCII-Ersatz).

    Notwendige CSS-Klassen in profile.module.css ergänzen falls nötig (z.B. membershipsList, membershipCard).
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>npm run build ohne Fehler. MembershipsSection.tsx existiert in frontend/src/components/profile/. grep "fansubs/" in der Datei zeigt den Link-Pfad. grep "Users" zeigt den Fallback-Icon-Import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Props → Render | Daten kommen aus Server-Component; keine Benutzer-Eingabe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-06 | Information Disclosure | MembershipsSection | accept | Zeigt nur fansub_group_name und app_member_roles — keine sensiblen Daten |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | Kein neues npm-Paket |
</threat_model>

<verification>
1. npm run build ohne Fehler
2. /me/profile/page.tsx importiert aus @/components/profile/ (nicht ./components/)
3. Keine defekten CSS-Modul-Imports in den verschobenen Dateien
4. MembershipsSection rendert Gruppenlogo (Fallback Users-Icon), Namen als Link, Rollen als Badges
</verification>

<success_criteria>
- npm run build ohne Fehler
- Drei verschobene Komponenten in frontend/src/components/profile/
- /me/profile/page.tsx nutzt neue Import-Pfade
- MembershipsSection.tsx neu vorhanden mit korrekten Umlauten
</success_criteria>

<output>
Create `.planning/phases/59-ffentliches-fansub-member-profil/59-03-SUMMARY.md` when done
</output>
