---
phase: 59-ffentliches-fansub-member-profil
plan: 04
type: execute
wave: 3
depends_on:
  - 59-02
  - 59-03
files_modified:
  - frontend/src/app/members/[slug]/page.tsx
  - frontend/src/app/members/[slug]/page.module.css
  - frontend/src/lib/api.ts
autonomous: true
requirements:
  - D-01
  - D-02
  - D-05
  - D-06
  - D-07
  - D-09
  - D-10
  - D-11
  - D-12
  - D-15
  - D-17
must_haves:
  truths:
    - "Route /members/[slug] existiert als Next.js Server Component"
    - "Bei public-Profil werden MemberProfileHero, MembershipsSection, RecentMediaSection, RecentContributionsSection gerendert"
    - "Bei members_only + anonym: Hinweis Dieses Profil ist nicht öffentlich zugänglich."
    - "Bei unbekanntem Slug: Hinweis Mitglied nicht gefunden."
    - "Token für eingeloggte User wird via cookies() aus next/headers an Backend weitergeleitet"
  artifacts:
    - path: "frontend/src/app/members/[slug]/page.tsx"
      provides: "Öffentliche Member-Profilseite als Server Component"
    - path: "frontend/src/lib/api.ts"
      provides: "getMemberProfile(slug, authToken?) Helper"
  key_links:
    - from: "frontend/src/app/members/[slug]/page.tsx"
      to: "frontend/src/lib/api.ts"
      via: "getMemberProfile(slug, token)"
      pattern: "getMemberProfile"
    - from: "frontend/src/lib/api.ts"
      to: "/api/v1/members/:slug"
      via: "authorizedFetch"
      pattern: "/api/v1/members/"
---

<objective>
Öffentliche Route /members/[slug] als Next.js Server Component und den getMemberProfile-API-Helper implementieren.

Purpose: Aktiviert die in Phase 58 vorbereiteten isPublicView-Komponenten auf einer neuen öffentlich zugänglichen Route, die von überall verlinkbar ist.
Output: frontend/src/app/members/[slug]/page.tsx, page.module.css, getMemberProfile-Helper in api.ts.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/PROJECT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/59-ffentliches-fansub-member-profil/59-CONTEXT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/59-ffentliches-fansub-member-profil/59-RESEARCH.md
@C:/Users/admin/Documents/Team4s/.planning/phases/59-ffentliches-fansub-member-profil/59-PATTERNS.md
@C:/Users/admin/Documents/Team4s/.planning/phases/59-ffentliches-fansub-member-profil/59-02-SUMMARY.md
@C:/Users/admin/Documents/Team4s/.planning/phases/59-ffentliches-fansub-member-profil/59-03-SUMMARY.md

<interfaces>
Aus frontend/src/app/fansubs/[slug]/page.tsx (exaktes Muster für Server Component):
  export default async function FansubProfilePage({ params }: FansubProfilePageProps)
  const resolvedParams = await params
  try { ... } catch (error) { instanceof ApiError + error.status === 404 }

Aus frontend/src/lib/api.ts (Muster für getOwnProfile + getFansubBySlug):
  getApiBaseUrl() — wechselt zwischen intern/extern je nach SSR/Browser
  authorizedFetch(url, { cache: "no-store", authToken }) — authToken optional
  parseApiErrorPayload(response, fallbackMsg) → { message, code, details }
  class ApiError { constructor(status, message, ...) }
  Typ-Imports am Anfang der Datei: aus @/types/profile

Aus frontend/src/types/profile.ts (Plan 01 Ergebnis):
  export interface PublicMemberProfileData { member_id, fansub_name, bio, member_story_html, active_from_date, active_until_date, is_currently_active, profile_visibility, avatar, background_image, memberships, recent_media, recent_contributions }
  export type PublicMemberProfileResponse = { data: PublicMemberProfileData } | { visible: false; reason: string }

Aus frontend/src/components/profile/ (Plan 03 Ergebnis):
  export { MemberProfileHero } — Props: profile: MemberProfileData | PublicMemberProfileData, isPublicView?: boolean
  export { MembershipsSection } — Props: memberships: MemberProfileMembership[]
  export { RecentMediaSection } — Props: items, canView, isPublicView
  export { RecentContributionsSection } — Props: items, canView, isPublicView

Aus docs/frontend/auth-api-client.md:
  Server Components lesen Token via cookies() aus next/headers und geben ihn als authToken weiter
  authorizedFetch akzeptiert authToken?: string — kein Cookie-Forwarding nötig wenn kein Token
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: getMemberProfile-Helper in api.ts</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts — Zeilen um getOwnProfile (ca. Zeilen 2611–2635) und getFansubBySlug (ca. Zeilen 1475–1501) als Muster; Import-Block am Anfang für @/types/profile
    - frontend/src/types/profile.ts — PublicMemberProfileResponse-Typ (Plan 01 Ergebnis)
  </read_first>
  <action>
    Neuen Helper getMemberProfile am Ende der Datei (oder neben getOwnProfile) einfügen:

    Signatur: export async function getMemberProfile(slug: string, authToken?: string): Promise<PublicMemberProfileResponse>

    Implementierung:
    1. const API_BASE_URL = getApiBaseUrl()
    2. const encodedSlug = encodeURIComponent(slug)
    3. authorizedFetch(`${API_BASE_URL}/api/v1/members/${encodedSlug}`, { cache: "no-store", authToken })
    4. if (!response.ok): parseApiErrorPayload + throw new ApiError(response.status, ...)
    5. return response.json() as Promise<PublicMemberProfileResponse>

    Import-Block: PublicMemberProfileResponse zu den bestehenden profile-Typ-Importen hinzufügen.

    Kein Cache — Profile-Daten sollen nicht gestaltet gecacht werden (cache: "no-store").
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>tsc --noEmit ohne Fehler. grep "getMemberProfile" in api.ts zeigt die neue Funktion mit korrekter Signatur.</done>
</task>

<task type="auto">
  <name>Task 2: /members/[slug]/page.tsx Server Component</name>
  <files>frontend/src/app/members/[slug]/page.tsx, frontend/src/app/members/[slug]/page.module.css</files>
  <read_first>
    - frontend/src/app/fansubs/[slug]/page.tsx — vollständige Datei als exaktes Muster (Props-Interface, resolvedParams, try/catch, ApiError-Check, 404-Render-Zweig)
    - frontend/src/app/me/profile/page.tsx — Layout-Referenz (wie wird Hero + Sections angeordnet)
  </read_first>
  <action>
    Verzeichnis frontend/src/app/members/[slug]/ anlegen falls nicht vorhanden.

    page.tsx:
    - Keine 'use client'-Direktive — Server Component
    - Imports: Link from 'next/link', cookies from 'next/headers', ApiError + getMemberProfile from '@/lib/api', PublicMemberProfileData from '@/types/profile', MemberProfileHero from '@/components/profile/MemberProfileHero', MembershipsSection from '@/components/profile/MembershipsSection', RecentMediaSection from '@/components/profile/RecentMediaSection', RecentContributionsSection from '@/components/profile/RecentContributionsSection', styles from './page.module.css'
    - Props: { params: { slug: string } | Promise<{ slug: string }> }
    - Logik exakt nach Pattern aus PATTERNS.md (59-PATTERNS.md Server Component Pattern):
      1. const resolvedParams = await params
      2. const slug = (resolvedParams.slug || '').trim() — leer → Fehlermeldung-Seite
      3. const cookieStore = await cookies(); const token = cookieStore.get('access_token')?.value
      4. getMemberProfile(slug, token) in try/catch
      5. 'visible' in response && !response.visible → isHidden = true
      6. 'data' in response → profile = response.data
      7. catch ApiError 404 → "Mitglied nicht gefunden.", sonst "Profil konnte nicht geladen werden."
    - Render-Zweige:
      - isHidden: main > div "Dieses Profil ist nicht öffentlich zugänglich." (D-06, D-17)
      - !profile: main > div mit message
      - Profil vorhanden: main > MemberProfileHero(profile, isPublicView=true) + MembershipsSection(memberships) + RecentMediaSection(recent_media, canView=true, isPublicView=true) + RecentContributionsSection(recent_contributions, canView=true, isPublicView=true)
    - Alle deutschen Texte mit korrekten Umlauten: "Dieses Profil ist nicht öffentlich zugänglich." — kein ae/oe/ue

    page.module.css: Minimal-CSS — .page (max-width, padding), .errorBox (Hinweis-Container). Analog zu fansubs/[slug]/page.module.css falls vorhanden.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>npm run build ohne Fehler. frontend/src/app/members/[slug]/page.tsx existiert ohne "use client". grep "cookies" zeigt den next/headers-Import. grep "Dieses Profil" zeigt den deutschen Hinweis-Text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Component | URL-Parameter slug; nicht vertrauenswürdig; wird encodeURIComponent'd |
| Server Component → Backend | Token aus Cookie; nur weitergeleitet, nicht geprüft — Backend entscheidet über Auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-07 | Elevation of Privilege | Token-Forwarding in Server Component | mitigate | cookies() liest nur 'access_token' aus; kein Token = anonymer Request; Backend entscheidet (D-04) |
| T-59-08 | Information Disclosure | members_only-Zweig | mitigate | Frontend rendert nur was Backend zurückgibt; kein Frontend-Gate nötig (D-06) |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | Kein neues npm-Paket |
</threat_model>

<verification>
1. npm run build ohne Fehler
2. /members/[slug]/page.tsx ohne 'use client'
3. Token-Forwarding via cookies() vorhanden
4. Drei Render-Zweige: isHidden, kein Profil, Profil vorhanden
5. Alle deutschen Texte mit korrekten Umlauten
</verification>

<success_criteria>
- npm run build ohne Fehler
- Route /members/[slug] als Server Component vorhanden
- getMemberProfile in api.ts exportiert mit optionalem authToken
- Deutsche Hinweis-Texte mit korrekten Umlauten
- Alle vier Profil-Sections eingebunden mit isPublicView=true
</success_criteria>

<output>
Create `.planning/phases/59-ffentliches-fansub-member-profil/59-04-SUMMARY.md` when done
</output>
