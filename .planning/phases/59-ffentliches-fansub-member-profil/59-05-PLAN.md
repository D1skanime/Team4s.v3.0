---
phase: 59-ffentliches-fansub-member-profil
plan: 05
type: execute
wave: 3
depends_on:
  - 59-02
files_modified:
  - backend/internal/handlers/app_profile.go
  - backend/internal/repository/member_profile_repository.go
  - backend/cmd/server/main.go
  - frontend/src/app/me/profile/components/ProfileBackgroundCard.tsx
  - frontend/src/app/me/profile/page.tsx
  - frontend/src/lib/api.ts
autonomous: true
requirements:
  - D-09
must_haves:
  truths:
    - "POST /api/v1/me/profile/background nimmt eine Bilddatei entgegen, speichert sie und gibt die public_url zurück"
    - "Die members-Tabelle verknüpft das Hintergrundbild via background_media_id"
    - "Auf /me/profile ist eine ProfileBackgroundCard sichtbar mit Upload-Funktion (16:9-Cropper ohne Rundungen)"
    - "Das gespeicherte Hintergrundbild erscheint in der GET /api/v1/me/profile Response als background_image"
  artifacts:
    - path: "frontend/src/app/me/profile/components/ProfileBackgroundCard.tsx"
      provides: "Upload-Card für Hintergrundbild mit Phase-56-Cropper (16:9, keine Rundungen)"
  key_links:
    - from: "frontend/src/app/me/profile/components/ProfileBackgroundCard.tsx"
      to: "/api/v1/me/profile/background"
      via: "uploadOwnProfileBackground in api.ts"
      pattern: "uploadOwnProfileBackground"
    - from: "backend/internal/handlers/app_profile.go"
      to: "member_profile_repository.go"
      via: "AttachUploadedBackground"
      pattern: "AttachUploadedBackground"
---

<objective>
Hintergrundbild-Upload für das eigene Profil implementieren: Backend-Handler + Repository-Methode + Frontend-Card auf /me/profile.

Purpose: Ermöglicht Membern ihr Hintergrundbild hochzuladen, das dann auf der öffentlichen Profilseite /members/[slug] als Hero-Banner erscheint.
Output: POST /api/v1/me/profile/background, ProfileBackgroundCard-Komponente, background_image im Profil-Response.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/PROJECT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/59-ffentliches-fansub-member-profil/59-CONTEXT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/59-ffentliches-fansub-member-profil/59-RESEARCH.md
@C:/Users/admin/Documents/Team4s/.planning/phases/59-ffentliches-fansub-member-profil/59-PATTERNS.md
@C:/Users/admin/Documents/Team4s/.planning/phases/59-ffentliches-fansub-member-profil/59-02-SUMMARY.md

<interfaces>
Aus backend/internal/handlers/app_profile.go (Avatar-Upload als Referenz):
  func (h *AppAuthHandler) UploadOwnProfileAvatar(c *gin.Context) -- Pattern: MIME-Check, imaging.Resize, UUID-Dateiname, DB-Transaktion
  avatarAllowedImageMimeTypes map[string]bool — gleiche MIME-Liste für Hintergrundbild verwenden
  type memberProfileStore interface { AttachUploadedAvatar } — um AttachUploadedBackground erweitern

Aus backend/internal/repository/member_profile_repository.go:
  type MemberProfileAvatarUploadInput struct { MediaID uuid.UUID; StoragePath string; PublicURL string }
  func (r *MemberProfileRepository) AttachUploadedAvatar(ctx, appUserID int64, input MemberProfileAvatarUploadInput) -- Pattern für AttachUploadedBackground

Aus backend/internal/models/member_profile.go:
  MemberProfile struct -- enthält Avatar-Feld; muss BackgroundImage *MemberProfileBgImage ergänzt bekommen
  MemberProfileBgImage struct { PublicURL string } — aus Plan 01

Aus frontend/src/app/me/profile/components/MemberAvatarCard.tsx (Referenz für Frontend-Card):
  Upload-State (isUploading), Cropper-Modal, onFileSelected-Handler, api.uploadOwnProfileAvatar-Aufruf
  Exakt dasselbe Pattern für Hintergrundbild; andere Cropper-Props

Phase-56-Cropper-Komponente:
  frontend/src/components/CropperModal.tsx (oder ähnlicher Pfad) — Props prüfen (aspect, circularCrop, borderRadius)
  Für 16:9 ohne Rundungen: aspect={16/9}, circularCrop={false}

Aus frontend/src/lib/api.ts:
  uploadOwnProfileAvatar(formData) → async function analog verwenden für uploadOwnProfileBackground
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — Upload-Handler + Repository für Hintergrundbild</name>
  <files>backend/internal/handlers/app_profile.go, backend/internal/repository/member_profile_repository.go, backend/internal/models/member_profile.go, backend/cmd/server/main.go</files>
  <read_first>
    - backend/internal/handlers/app_profile.go — UploadOwnProfileAvatar vollständig lesen; MIME-Check, imaging.Resize-Aufruf, UUID-Dateiname, DB-Transaktion-Pattern, memberProfileStore interface
    - backend/internal/repository/member_profile_repository.go — AttachUploadedAvatar-Methode + MemberProfileAvatarUploadInput-Struct
    - backend/internal/models/member_profile.go — MemberProfile-Struct (enthält noch kein BackgroundImage-Feld), MemberProfileBgImage aus Plan 01
    - backend/cmd/server/main.go — POST /me/profile/avatar-Route als Referenz für neue Route-Registrierung
  </read_first>
  <action>
    Schritt 1 — models/member_profile.go: MemberProfile-Struct um Feld BackgroundImage *MemberProfileBgImage json:"background_image,omitempty" erweitern. Kein anderes Feld ändern.

    Schritt 2 — repository/member_profile_repository.go:
    - Neues Input-Struct MemberProfileBackgroundUploadInput (analog MemberProfileAvatarUploadInput): MediaID uuid.UUID, StoragePath string, PublicURL string.
    - Interface-Methode in memberProfileStore interface hinzufügen: AttachUploadedBackground(ctx context.Context, appUserID int64, input MemberProfileBackgroundUploadInput) (*models.MemberProfile, error).
    - AttachUploadedBackground implementieren analog zu AttachUploadedAvatar: INSERT INTO media(...), UPDATE members SET background_media_id = $mediaID WHERE app_user_id = $appUserID, dann GetOwnProfile zurückgeben.

    Schritt 3 — handlers/app_profile.go:
    - Neue Methode UploadOwnProfileBackground(c *gin.Context) analog zu UploadOwnProfileAvatar.
    - Gleiche MIME-Prüfung (avatarAllowedImageMimeTypes).
    - imaging.Resize auf 1920×1080 (16:9) mit imaging.Lanczos — kein Cropping, nur Resize mit Fill: imaging.Fill(img, 1920, 1080, imaging.Center, imaging.Lanczos). Alternativ imaging.Fit für proportionales Resize wenn Fill nicht verfügbar — Präferenz: imaging.Fill.
    - UUID-Dateiname mit Unterordner "backgrounds/" analog "avatars/".
    - DB-Transaktion analog Avatar.
    - Rückgabe: 200 + {"data": updatedProfile}.

    Schritt 4 — main.go: POST /me/profile/background-Route registrieren analog zur Avatar-Route, mit authMiddleware (kein authOptionalMiddleware — Upload erfordert Authentifizierung).
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go build ./...</automated>
  </verify>
  <done>go build ./... ohne Fehler. grep "UploadOwnProfileBackground" in app_profile.go. grep "me/profile/background" in main.go zeigt die Route. grep "BackgroundImage" in models/member_profile.go zeigt das neue Feld.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — ProfileBackgroundCard + api.ts-Helper + Einbindung</name>
  <files>frontend/src/app/me/profile/components/ProfileBackgroundCard.tsx, frontend/src/lib/api.ts, frontend/src/app/me/profile/page.tsx</files>
  <read_first>
    - frontend/src/app/me/profile/components/MemberAvatarCard.tsx — vollständige Datei; Upload-State, CropperModal-Einbindung, onFileSelected-Logik, API-Aufruf-Pattern
    - frontend/src/components/ — nach CropperModal (oder ähnlichem Namen) suchen; Props prüfen (aspect, circularCrop)
    - frontend/src/lib/api.ts — uploadOwnProfileAvatar-Funktion als Referenz für uploadOwnProfileBackground
    - frontend/src/app/me/profile/page.tsx — aktuelle Kartenstruktur; Position für neue Card (nach Avatar-Card oder am Ende)
  </read_first>
  <action>
    Schritt 1 — api.ts: Neue Funktion uploadOwnProfileBackground(formData: FormData): Promise<{ data: MemberProfileData }> analog zu uploadOwnProfileAvatar. POST /api/v1/me/profile/background. Kein authToken-Parameter nötig — authorizedFetch holt Token intern.

    Schritt 2 — ProfileBackgroundCard.tsx:
    - 'use client' Direktive (Upload-Interaktion ist Client-seitig)
    - Analog zu MemberAvatarCard: useState für isUploading, cropperOpen, selectedFile
    - Datei-Input (input type="file", accept="image/jpeg,image/png,image/webp"), bei Auswahl → Cropper öffnen
    - CropperModal mit aspect={16/9} und circularCrop={false} (kein borderRadius nötig)
    - Nach Crop: FormData bauen, uploadOwnProfileBackground aufrufen
    - Bei Erfolg: onSuccess-Callback aufrufen (gibt aktualisiertes Profil zurück) oder Seite neu laden
    - Bei Fehler: sichtbare Fehlermeldung (Observability — kein stilles Scheitern)
    - Card-Titel: "Hintergrundbild" — korrekter Umlaut
    - Wenn background_image vorhanden: Vorschau des aktuellen Hintergrundbilds (img-Tag mit public_url)
    - Props: profile: MemberProfileData (für aktuelles background_image), onSuccess?: () => void

    Schritt 3 — /me/profile/page.tsx: ProfileBackgroundCard nach dem Avatar-Card einbinden. Import hinzufügen: import { ProfileBackgroundCard } from './components/ProfileBackgroundCard'.

    Sprachqualität: Alle deutschen Strings mit korrekten Umlauten ("Hintergrundbild", "Hintergrundbild hochladen", Fehlermeldungen).
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>npm run build ohne Fehler. ProfileBackgroundCard.tsx existiert mit 'use client'. grep "Hintergrundbild" zeigt den korrekten Umlaut. grep "uploadOwnProfileBackground" in api.ts zeigt die neue Funktion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /me/profile/background | Datei-Upload; MIME-Typ nicht vertrauenswürdig |
| Handler → Dateisystem | UUID-Dateiname verhindert Path Traversal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-09 | Tampering | Hintergrundbild-Upload | mitigate | MIME-Check via mimetype-Bibliothek (gleiche wie Avatar-Upload); UUID-Dateiname |
| T-59-10 | Elevation of Privilege | POST /me/profile/background | mitigate | authMiddleware erforderlich — kein anonymer Upload möglich |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | Kein neues npm-Paket |
</threat_model>

<verification>
1. go build ./... ohne Fehler
2. npm run build ohne Fehler
3. POST /api/v1/me/profile/background in main.go mit authMiddleware
4. ProfileBackgroundCard auf /me/profile vorhanden
5. Deutsche Strings mit korrekten Umlauten
</verification>

<success_criteria>
- go build ./... ohne Fehler
- npm run build ohne Fehler
- Hintergrundbild-Upload-Endpoint registriert mit authMiddleware
- ProfileBackgroundCard mit 16:9-Cropper (circularCrop=false) auf /me/profile
- background_image im MemberProfile-Struct vorhanden
</success_criteria>

<output>
Create `.planning/phases/59-ffentliches-fansub-member-profil/59-05-SUMMARY.md` when done
</output>
