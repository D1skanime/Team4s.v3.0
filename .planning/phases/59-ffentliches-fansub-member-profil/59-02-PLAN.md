---
phase: 59-ffentliches-fansub-member-profil
plan: 02
type: execute
wave: 2
depends_on:
  - 59-01
files_modified:
  - backend/internal/repository/member_profile_repository.go
  - backend/internal/handlers/app_public_profile.go
  - backend/cmd/server/main.go
autonomous: true
requirements:
  - D-01
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
must_haves:
  truths:
    - "GET /api/v1/members/:slug gibt 200 + Profildaten bei public-Profil zurück"
    - "GET /api/v1/members/:slug gibt 200 + {visible:false, reason:members_only} bei anonymem Zugriff auf members_only-Profil zurück"
    - "GET /api/v1/members/:slug gibt 200 + Profildaten bei eingeloggtem Zugriff auf members_only-Profil zurück"
    - "Keine Keycloak-Felder (display_name, email, keycloak_subject) in der Response"
    - "Slug-Auflösung: zuerst normalisierter fansub_name, dann numerischer ID-Fallback"
  artifacts:
    - path: "backend/internal/handlers/app_public_profile.go"
      provides: "AppPublicProfileHandler + GetPublicMemberProfile Handler"
    - path: "backend/internal/repository/member_profile_repository.go"
      provides: "GetPublicMemberProfile Methode"
  key_links:
    - from: "backend/cmd/server/main.go"
      to: "app_public_profile.go"
      via: "NewAppPublicProfileHandler + v1.GET(/members/:slug)"
      pattern: "members/:slug"
    - from: "app_public_profile.go"
      to: "member_profile_repository.go"
      via: "publicMemberProfileStore interface"
      pattern: "GetPublicMemberProfile"
---

<objective>
Backend-Repository-Methode und Handler für den öffentlichen Member-Profil-Endpoint implementieren und in main.go registrieren.

Purpose: Liefert die serverseitige Logik für Slug-Auflösung, Sichtbarkeitsprüfung und sichere Daten-Serialisierung ohne Keycloak-Felder.
Output: app_public_profile.go, erweiterte member_profile_repository.go, Route in main.go.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/PROJECT.md
@C:/Users/admin/Documents/Team4s/.planning/ROADMAP.md
@C:/Users/admin/Documents/Team4s/.planning/phases/59-ffentliches-fansub-member-profil/59-CONTEXT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/59-ffentliches-fansub-member-profil/59-RESEARCH.md
@C:/Users/admin/Documents/Team4s/.planning/phases/59-ffentliches-fansub-member-profil/59-PATTERNS.md
@C:/Users/admin/Documents/Team4s/.planning/phases/59-ffentliches-fansub-member-profil/59-01-SUMMARY.md

<interfaces>
Aus backend/internal/handlers/app_profile.go (Referenz-Handler-Muster):
  type memberProfileStore interface { GetOwnProfile, UpdateOwnProfile, AttachUploadedAvatar }
  func (h *AppAuthHandler) GetOwnProfile(c *gin.Context) -- identity aus middleware, ErrNotFound → 404
  writeInternalErrorResponse(c, msg, err, detail) -- vorhanden im handlers-Package
  middleware.CommentAuthIdentityFromContext(c) → (identity, ok bool)

Aus backend/internal/models/member_profile.go (Phase 59 Plan 01):
  type PublicMemberProfile struct { MemberID, FansubName, Bio, MemberStoryHTML, ActiveFromDate, ActiveUntilDate, IsCurrentlyActive, ProfileVisibility, Avatar, BackgroundImage, Memberships, RecentMedia, RecentContributions }
  const ProfileVisibilityMembersOnly = "members_only"
  type MemberProfileMembership struct { FansubGroupID, FansubGroupName, FansubGroupSlug string; AppMemberRoles []string }

Aus backend/internal/repository/member_profile_repository.go (zu erweitern):
  type MemberProfileRepository struct { db *pgxpool.Pool; publicBaseURL string }
  func (r *MemberProfileRepository) GetOwnProfile(ctx, appUserID) (*models.MemberProfile, error) -- Query-Muster
  repository.ErrNotFound -- Sentinel error

Aus backend/cmd/server/main.go (Route-Registrierung):
  v1 := router.Group("/api/v1")
  authOptionalMiddleware -- bereits vorhanden für public Endpoints mit optionalem Auth
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repository-Methode GetPublicMemberProfile mit Slug-Auflösung</name>
  <files>backend/internal/repository/member_profile_repository.go</files>
  <read_first>
    - backend/internal/repository/member_profile_repository.go — vollständige Datei; GetOwnProfile-Methode als Muster für die SQL-Query-Struktur (Memberships, RecentMedia, RecentContributions laden)
    - backend/internal/models/member_profile.go — PublicMemberProfile-Struct aus Plan 01 (alle Felder + BackgroundImage)
    - backend/go.mod — golang.org/x/text ist vorhanden (v0.35.0); Import: golang.org/x/text/unicode/norm
  </read_first>
  <action>
    Neue Methode GetPublicMemberProfile(ctx context.Context, slug string) (*models.PublicMemberProfile, error) am Ende der Datei.

    Slug-Normalisierung: golang.org/x/text/unicode/norm für NFD-Normalisierung, dann strings.ToLower, dann regexp.MustCompile("[^a-z0-9]+").ReplaceAllString(..., "-") und Trim. Paket-Import hinzufügen.

    SQL-Query-Logik: Haupt-SELECT auf members WHERE LOWER(REGEXP_REPLACE(nickname, '[^a-z0-9]+', '-', 'gi')) = $1. Falls der Slug vollständig numerisch ist (regexp prüfen), zusätzlich OR id = $1::int als zweiten Lookup-Zweig. Bei mehreren Treffern (Slug-Kollision) ersten Treffer nehmen (ORDER BY id ASC LIMIT 1).

    Geladene Felder aus der DB: id, nickname (→ FansubName), bio, member_story_html, active_from_date::text, active_until_date::text, is_currently_active, profile_visibility. Kein display_name, kein email, kein keycloak_subject — per D-08.

    BackgroundImage: JOIN auf media WHERE media.id = members.background_media_id (LEFT JOIN), public_url bauen wie bei Avatar (r.publicBaseURL + "/media/" + media.storage_path).

    Avatar analog zu GetOwnProfile laden (LEFT JOIN auf media für Avatar).

    Memberships, RecentMedia, RecentContributions analog zu GetOwnProfile laden — dieselben Sub-Queries.

    Bei keinem Treffer: return nil, repository.ErrNotFound.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go build ./internal/repository/...</automated>
  </verify>
  <done>go build ./internal/repository/... ohne Fehler. grep "GetPublicMemberProfile" zeigt die neue Methode.</done>
</task>

<task type="auto">
  <name>Task 2: Handler app_public_profile.go + Route-Registrierung in main.go</name>
  <files>backend/internal/handlers/app_public_profile.go, backend/cmd/server/main.go</files>
  <read_first>
    - backend/internal/handlers/app_profile.go — Handler-Struct-Pattern, writeInternalErrorResponse-Signatur, Import-Liste
    - backend/cmd/server/main.go — authOptionalMiddleware-Definition (Zeile suchen mit grep), bestehende Handler-Registrierung (v1.GET-Aufrufe), memberProfileRepo-Variable (bereits instanziiert für /me/profile)
  </read_first>
  <action>
    Neue Datei app_public_profile.go im handlers-Package:

    Interface publicMemberProfileStore: Methode GetPublicMemberProfile(ctx context.Context, slug string) (*models.PublicMemberProfile, error).

    Struct AppPublicProfileHandler mit Feld profileRepo publicMemberProfileStore.

    Konstruktor NewAppPublicProfileHandler(profileRepo publicMemberProfileStore) *AppPublicProfileHandler.

    Handler GetPublicMemberProfile(c *gin.Context):
    1. slug aus c.Param("slug") + strings.TrimSpace; leer → 400
    2. _, isAuthenticated := middleware.CommentAuthIdentityFromContext(c) — optional, kein Fehler wenn absent
    3. h.profileRepo.GetPublicMemberProfile(ctx, slug) — bei ErrNotFound → 404 mit {"error":{"message":"mitglied nicht gefunden"}}; bei anderem Fehler → writeInternalErrorResponse
    4. Sichtbarkeitsprüfung: if profile.ProfileVisibility == models.ProfileVisibilityMembersOnly && !isAuthenticated → c.JSON(200, gin.H{"visible":false,"reason":"members_only"})
    5. Sonst: c.JSON(200, gin.H{"data": profile}) — kein separates toPublicResponse-Mapping nötig, da PublicMemberProfile-Struct bereits keine Keycloak-Felder enthält

    Imports: errors, net/http, strings, context, team4s.v3/backend/internal/middleware, team4s.v3/backend/internal/models, team4s.v3/backend/internal/repository, github.com/gin-gonic/gin.

    In main.go: Bestehende memberProfileRepo-Instanz wiederverwenden (nicht neu erstellen). Nach den /me/profile-Routen einfügen:
      publicProfileHandler := handlers.NewAppPublicProfileHandler(memberProfileRepo)
      v1.GET("/members/:slug", authOptionalMiddleware, publicProfileHandler.GetPublicMemberProfile)

    Die neue Route gehört in die öffentliche Route-Gruppe (kein authMiddleware, nur authOptionalMiddleware).
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go build ./...</automated>
  </verify>
  <done>go build ./... ohne Fehler. grep "members/:slug" in main.go zeigt die Route. grep "AppPublicProfileHandler" in app_public_profile.go zeigt Struct und Konstruktor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP → Handler | Slug-Parameter aus URL; nicht vertrauenswürdig |
| Handler → Repository | Slug wird normalisiert und parametrisiert an DB übergeben |
| Auth-Header → Sichtbarkeit | Bearer-Token optional; fehlender Token = anonym |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-02 | Information Disclosure | GetPublicMemberProfile Handler | mitigate | PublicMemberProfile-Struct enthält strukturell keine Keycloak-Felder (D-08); keine separate Mapping-Funktion nötig |
| T-59-03 | Elevation of Privilege | Sichtbarkeitsprüfung | mitigate | Backend prüft Auth-Status vor Daten-Rückgabe; Frontend bekommt nur {"visible":false} bei members_only+anonym (D-06) |
| T-59-04 | Information Disclosure | Slug-Enumeration | mitigate | HTTP 404 bei unbekanntem Slug — kein Unterschied zwischen "nicht vorhanden" und "gesperrt" in der 404-Antwort |
| T-59-05 | Tampering | SQL Slug-Parameter | mitigate | Parametrisierte Query ($1) — kein String-Concatenation |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | Kein neues Paket außer golang.org/x/text (bereits im go.mod v0.35.0 vorhanden) |
</threat_model>

<verification>
1. go build ./... ohne Fehler
2. GET /api/v1/members/:slug Route in main.go mit authOptionalMiddleware
3. Handler gibt bei members_only+anonym {"visible":false,"reason":"members_only"} zurück
4. Handler gibt bei public-Profil {"data":{...}} ohne Keycloak-Felder zurück
</verification>

<success_criteria>
- go build ./... ohne Fehler
- Route GET /api/v1/members/:slug registriert mit authOptionalMiddleware
- Handler korrekt: 404 bei unbekannt, {"visible":false} bei members_only+anonym, {"data":...} bei public
- Kein display_name/email/keycloak_subject in der JSON-Response
</success_criteria>

<output>
Create `.planning/phases/59-ffentliches-fansub-member-profil/59-02-SUMMARY.md` when done
</output>
