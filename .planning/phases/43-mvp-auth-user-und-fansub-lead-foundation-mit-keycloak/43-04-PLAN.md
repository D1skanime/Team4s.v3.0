---
phase: 43-mvp-auth-user-und-fansub-lead-foundation-mit-keycloak
plan: "04"
type: execute
wave: 4
depends_on:
  - 43-03-PLAN.md
files_modified:
  - docs
  - infra/keycloak
  - scripts/keycloak
  - backend/internal
  - frontend/src
  - .planning/phases/43-mvp-auth-user-und-fansub-lead-foundation-mit-keycloak
autonomous: true
requirements:
  - AUTH-FOUNDATION-01
must_haves:
  truths:
    - "Developer documentation must explain the Keycloak-versus-Team4s authority split concretely."
    - "Local bootstrap steps must be repeatable and must call out when Keycloak state must be reset intentionally."
    - "Phase 44 must inherit stable seams, not rediscover auth boundaries during permission-engine work."
    - "Auth and membership changes are not complete until browser/live verification proves them end to end."
    - "Phase 43 must leave behind a low-friction local dev/test story so later page rebuilds are not blocked by auth friction."
---

<objective>
Close Phase 43 with durable documentation, repeatable local bootstrap guidance, browser/live verification, and an explicit handoff to the later permission-engine phase.

Purpose: Security and identity work is only trustworthy when future developers can reproduce the setup, understand the business-boundary decisions, and verify the same flows locally without guessing which roles live in Keycloak versus Team4s.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/43-mvp-auth-user-und-fansub-lead-foundation-mit-keycloak/43-CONTEXT.md
@.planning/phases/43-mvp-auth-user-und-fansub-lead-foundation-mit-keycloak/43-RESEARCH.md
@.planning/phases/43-mvp-auth-user-und-fansub-lead-foundation-mit-keycloak/43-01-PLAN.md
@.planning/phases/43-mvp-auth-user-und-fansub-lead-foundation-mit-keycloak/43-02-PLAN.md
@.planning/phases/43-mvp-auth-user-und-fansub-lead-foundation-mit-keycloak/43-03-PLAN.md
@AGENTS.md
@docs/architecture/db-schema-fansub-domain.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write developer docs for setup, bootstrap, and authority boundaries</name>
  <read_first>
    - .planning/phases/43-mvp-auth-user-und-fansub-lead-foundation-mit-keycloak/43-CONTEXT.md
    - .planning/phases/43-mvp-auth-user-und-fansub-lead-foundation-mit-keycloak/43-RESEARCH.md
    - AGENTS.md
  </read_first>
  <action>
    Produce concise but concrete developer/operator documentation for the completed Phase-43 auth foundation.

    The docs must explain:
    - required `.env` values
    - local Compose/Keycloak startup flow
    - realm/client bootstrap path and which artifact is authoritative on clean vs already-initialized environments
    - how backchannel logout is configured between Keycloak and Team4s
    - when an existing Keycloak DB/volume prevents a fresh realm import
    - the safe local reset path for intentionally reapplying a fresh import
    - the SQL bootstrap path for the first Team4s `platform_admin`
    - the difference between Keycloak identity/global realm setup and Team4s app roles/group roles
    - the explicit boundary to Phase 44 so future work does not smuggle permission-engine scope back into Phase 43
    - the recommended local dev/test flow for later page work: how to sign in quickly, which local test user to use, how to re-create `platform_admin`, and how to reset back to a clean unauthenticated state

    This documentation should leave behind five concrete dev/test comfort outcomes:
    - one documented local test user for auth/page testing
    - one short bootstrap flow for Keycloak + realm/client + local admin readiness
    - one stable `/api/me`-based session seam for frontend page checks
    - one reproducible logout/session-reset flow for switching between auth states
    - no requirement that later page testing depends on a finalized drawer UX
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s && git diff --check</automated>
  </verify>
  <done>
    - New contributors can bootstrap local auth without guessing.
    - The first-admin path is documented exactly.
    - The Keycloak-vs-Team4s authority split is unambiguous in docs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the required regression checks and browser/live UAT checklist</name>
  <read_first>
    - backend/internal
    - frontend/src
    - .planning/phases/43-mvp-auth-user-und-fansub-lead-foundation-mit-keycloak/43-01-PLAN.md
    - .planning/phases/43-mvp-auth-user-und-fansub-lead-foundation-mit-keycloak/43-03-PLAN.md
  </read_first>
  <action>
    Close the phase with the smallest useful automated and manual verification bundle.

    Minimum verification scope:
    - backend auth/current-user tests for valid token, invalid token, pending user, and disabled user behavior
    - backend logout/session-invalidierungs tests for Keycloak backchannel logout behavior
    - backend membership/admin-route tests for platform-admin vs non-admin access
    - thin frontend checks for session-aware auth state and membership-management surface integrity where practical
    - a browser/live checklist that walks through login, `/api/me`, first-admin bootstrap, user assignment, and `fansub_lead` visibility
    - proof that a developer can move quickly between anonymous, authenticated, and admin states without ad-hoc manual repair steps

    Do not mark the phase complete without an end-to-end browser path that proves:
    - login works
    - bearer token reaches the backend
    - Team4s resolves the user correctly
    - platform-admin can manage users/groups
    - later page testing can rely on the documented auth/session setup instead of a hidden fixed-admin shortcut
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./... && go build ./...</automated>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck && npm run lint</automated>
  </verify>
  <done>
    - Phase 43 has automated regression coverage on the critical auth/admin seams.
    - There is a live verification checklist for the full local flow.
    - The team can re-run verification before building Phase 44 on top.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prepare the explicit Phase-44 handoff from auth foundation to permission engine</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/phases/44-app-permission-engine-fuer-fansub-release-und-media-kontexte/44-01-PLAN.md
    - .planning/phases/43-mvp-auth-user-und-fansub-lead-foundation-mit-keycloak/43-03-PLAN.md
  </read_first>
  <action>
    Leave behind a crisp handoff note for the next phase so the permission-engine work starts from proven seams instead of re-arguing auth foundations.

    The handoff should name:
    - the canonical authenticated principal shape
    - the canonical global-role seam
    - the canonical fansub-membership and `fansub_lead` tables
    - known deferred items that still belong to Phase 44 or later
    - any residual auth risks or operational caveats discovered during live verification

    Explicitly call out that Phase 44 must consume the Phase-43 seams rather than falling back to legacy `users`/`user_roles` or Keycloak role claims for business authorization.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s && git diff --check</automated>
  </verify>
  <done>
    - The next phase has an explicit seam-level handoff.
    - Residual risks are documented instead of rediscovered mid-implementation.
    - The auth foundation closes with a stable transition into permission-engine work.
  </done>
</task>

</tasks>

<verification>
Automated:
1. `cd C:/Users/admin/Documents/Team4s/backend && go test ./...`
2. `cd C:/Users/admin/Documents/Team4s/backend && go build ./...`
3. `cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck`
4. `cd C:/Users/admin/Documents/Team4s/frontend && npm run lint`
5. `cd C:/Users/admin/Documents/Team4s && git diff --check`

Live:
1. Start the local stack and verify `keycloak` plus `keycloak-db` are healthy.
2. Reach the Keycloak Admin Console and confirm the `team4s` realm and `team4s-frontend` client exist after bootstrap.
3. Log in through the frontend and confirm `/api/me` resolves the Team4s app user.
4. Apply the documented SQL bootstrap for the first `platform_admin` and verify admin-user access.
5. Assign a user to a fansub group, grant `fansub_lead`, and verify the detail UI reflects the change.
6. Verify a non-admin or disabled user cannot perform the protected admin actions.
7. Verify a Keycloak-triggered backchannel logout invalidates the local Team4s auth/session state and forces the user back into an unauthenticated state.
8. Verify the documented local test flow is actually fast enough for page work: sign in as the documented test user, switch to admin-ready state, sign out, and return to anonymous state without ad-hoc repo changes.
</verification>

<success_criteria>
- Developer docs explain setup, reset, bootstrap, and authority boundaries clearly.
- Phase 43 has explicit automated and live verification for the auth and membership foundation.
- The first-platform-admin bootstrap path is durable and repeatable.
- Phase 44 receives a clear seam-level handoff instead of rediscovering auth decisions.
- Later page testing and UI rebuild work has a documented low-friction local auth workflow instead of depending on hidden bypass knowledge.
</success_criteria>
