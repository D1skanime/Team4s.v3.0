---
phase: 86-daten-getriebene-capability-registry
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - database/migrations/0108_capability_registry.up.sql
  - database/migrations/0108_capability_registry.down.sql
  - backend/internal/permissions/capability_registry_test.go
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-10
must_haves:
  truths:
    - "D-01: Tabelle action_definitions(code PK, label_de, category, sort_order) existiert nach Migration 0108."
    - "D-02: Tabelle role_capabilities(role_code FK role_definitions, action_code FK action_definitions, PK) existiert."
    - "D-03: Seed in 0108.up.sql enthält exakt 18 action_definitions-Einträge und alle roleMatrix-Paare behavior-preserving (kein Verhaltenswechsel)."
    - "D-10: Wave-0-Test TestRoleMatrixSeedParity schlägt fehl (RED), solange LoadCache noch nicht auf role_capabilities zugreift."
    - "D-10: Wave-0-Test TestStartupConsistencyCheck schlägt fehl (RED), solange die Konsistenz-Prüfung in LoadCache nicht implementiert ist."
    - "Migrations-Smoke: 0108.up.sql lässt sich in go test via SQL-Fixture validieren (action_definitions hat >= 18 Zeilen, role_capabilities hat >= 10 Rollen × korrekte Action-Zahl)."
  artifacts:
    - path: "database/migrations/0108_capability_registry.up.sql"
      provides: "Tabellen-Schema + Seed aus roleMatrix"
      contains: "CREATE TABLE action_definitions"
    - path: "database/migrations/0108_capability_registry.down.sql"
      provides: "Rollback-Migration"
      contains: "DROP TABLE IF EXISTS role_capabilities"
    - path: "backend/internal/permissions/capability_registry_test.go"
      provides: "Wave-0-Tests RED"
      exports:
        - "TestRoleMatrixSeedParity"
        - "TestStartupConsistencyCheck"
        - "TestCacheLoadAndLookup"
  key_links:
    - from: "role_capabilities.role_code"
      to: "role_definitions(code)"
      via: "FK ON DELETE CASCADE"
      pattern: "REFERENCES role_definitions\\(code\\)"
    - from: "role_capabilities.action_code"
      to: "action_definitions(code)"
      via: "FK ON DELETE CASCADE"
      pattern: "REFERENCES action_definitions\\(code\\)"
---

<objective>
Migration 0108 legt action_definitions + role_capabilities an und seedet die vollständige roleMatrix (10 Rollen, 18 Action-Codes) behavior-preserving. Parallel entstehen Wave-0-Tests in capability_registry_test.go, die im RED-Zustand festhalten: Seed-Parity (alle Action-Konstanten im Seed vorhanden), Konsistenz-Check (alle Konstanten im Cache gefunden), Cache-Lookup (RoleAllowsAction liefert erwartete Werte). Tests bleiben RED, bis Plan 02 den Cache-Umbau liefert.

Purpose: Stabile Datenbasis + vordefinierter Nyquist-Testanker — Plan 02 und 03 implementieren dagegen.
Output: Migrations-Paar 0108 up/down + capability_registry_test.go mit 3 RED-Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/86-daten-getriebene-capability-registry/86-RESEARCH.md
@.planning/phases/86-daten-getriebene-capability-registry/86-CONTEXT.md
@backend/internal/permissions/permissions.go
@backend/internal/permissions/permissions_test.go
@database/migrations/0085_role_definitions_seed.up.sql
@database/migrations/0107_fansub_group_default_crew.up.sql

<interfaces>
<!-- Aus backend/internal/permissions/permissions.go — alle Action-Konstanten die in den Seed müssen -->
package permissions

type Action string

const (
    ActionFansubGroupEdit              Action = "fansub_group.edit"
    ActionFansubGroupLinksManage       Action = "fansub_group.links.manage"
    ActionFansubGroupMembersView       Action = "fansub_group.members.view"
    ActionFansubGroupMembersManage     Action = "fansub_group.members.manage"
    ActionFansubGroupInvitationsView   Action = "fansub_group.invitations.view"
    ActionFansubGroupInvitationsCreate Action = "fansub_group.invitations.create"
    ActionFansubGroupInvitationsCancel Action = "fansub_group.invitations.cancel"
    ActionFansubGroupInvitationsAccept Action = "fansub_group.invitations.accept"  // in action_definitions, KEIN role_capabilities-Eintrag
    ActionFansubGroupNotesWrite        Action = "fansub_group.notes.write"
    ActionAnimeFansubProjectNotesWrite Action = "anime_fansub_project.notes.write"
    ActionReleaseView                  Action = "release.view"
    ActionReleaseVersionView           Action = "release_version.view"
    ActionReleaseVersionMediaView      Action = "release_version_media.view"
    ActionReleaseVersionMediaUpload    Action = "release_version_media.upload"
    ActionReleaseVersionMediaUpdate    Action = "release_version_media.update"
    ActionReleaseVersionMediaDelete    Action = "release_version_media.delete"
    ActionReleaseVersionMediaDeleteOwn Action = "release_version_media.delete_own"
    ActionReleaseVersionNotesWrite     Action = "release_version.notes.write"
)

// CacheLoader-Interface (wird in Plan 02 in permissions.go definiert, hier als Referenz für Wave-0-Test)
type CacheLoader interface {
    LoadRoleCapabilities(ctx context.Context) (map[string][]Action, error)
}
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Migration 0108 — Schema + behavior-preserving Seed</name>
  <files>
    database/migrations/0108_capability_registry.up.sql
    database/migrations/0108_capability_registry.down.sql
  </files>
  <read_first>
    - database/migrations/0085_role_definitions_seed.up.sql — INSERT ON CONFLICT DO UPDATE-Syntax + BEGIN/COMMIT-Struktur
    - database/migrations/0107_fansub_group_default_crew.up.sql — FK REFERENCES role_definitions(code)-Syntax
    - backend/internal/permissions/permissions.go Zeilen 1–35 — alle 18 Action-Konstanten als Seed-Quelle
  </read_first>
  <behavior>
    - Nach Ausführen von 0108.up.sql: SELECT COUNT(*) FROM action_definitions = 18
    - Nach Ausführen von 0108.up.sql: SELECT COUNT(DISTINCT role_code) FROM role_capabilities = 10
    - Nach Ausführen von 0108.up.sql: SELECT COUNT(*) FROM role_capabilities >= 83 (Summe der roleMatrix-Einträge)
    - Nach Ausführen von 0108.down.sql: action_definitions und role_capabilities existieren nicht mehr
    - action_definitions enthält fansub_group.invitations.accept (kein role_capabilities-Eintrag dafür)
    - Idempotent: 0108.up.sql zweimal ausführen → kein Fehler (ON CONFLICT DO UPDATE / DO NOTHING)
  </behavior>
  <action>
    Erstelle database/migrations/0108_capability_registry.up.sql mit BEGIN/COMMIT-Wrapper (Konvention aus 0085/0107).

    Schritt 1 — CREATE TABLE action_definitions per D-01:
    Spalten code TEXT PRIMARY KEY, label_de TEXT NOT NULL, category TEXT, sort_order INT NOT NULL DEFAULT 0.
    IF NOT EXISTS verwenden (idempotent).

    Schritt 2 — CREATE TABLE role_capabilities per D-02:
    Spalten role_code TEXT NOT NULL REFERENCES role_definitions(code) ON DELETE CASCADE,
    action_code TEXT NOT NULL REFERENCES action_definitions(code) ON DELETE CASCADE,
    PRIMARY KEY(role_code, action_code). IF NOT EXISTS verwenden.

    Schritt 3 — INSERT action_definitions alle 18 Codes (auf Basis der permissions.go-Konstanten):
    Verwende ON CONFLICT (code) DO UPDATE SET label_de=EXCLUDED.label_de, category=EXCLUDED.category, sort_order=EXCLUDED.sort_order.
    Labels auf Deutsch (korrekte Umlaute per CLAUDE.md). Kategorien: 'gruppe', 'projekt', 'release'.
    ActionFansubGroupInvitationsAccept ('fansub_group.invitations.accept', 'Einladung annehmen', 'gruppe', 80) MUSS enthalten sein — wird von CanAcceptInvitation genutzt, steht aber NICHT in roleMatrix (Pitfall 2 aus Research).

    Schritt 4 — INSERT role_capabilities exakt aus roleMatrix (behavior-preserving, D-03):
    Alle 10 Rollen mit ihren Actions aus der Research-Tabelle (R-02). Verwende ON CONFLICT DO NOTHING.
    platform_admin bekommt KEINEN Eintrag — er ist globaler Bypass, nicht in roleMatrix.

    Erstelle database/migrations/0108_capability_registry.down.sql:
    DROP TABLE IF EXISTS role_capabilities; (zuerst — FK)
    DROP TABLE IF EXISTS action_definitions; (danach)
    Kein BEGIN/COMMIT nötig (DROP ist auto-commit in Postgres), aber der Konvention wegen einfügen.

    Vollständige roleMatrix für den Seed (aus Research R-02):
    - fansub_lead: fansub_group.edit, fansub_group.links.manage, fansub_group.members.view, fansub_group.members.manage, fansub_group.invitations.view, fansub_group.invitations.create, fansub_group.invitations.cancel, fansub_group.notes.write, anime_fansub_project.notes.write, release.view, release_version.view, release_version_media.view, release_version_media.upload, release_version_media.update, release_version_media.delete, release_version.notes.write
    - project_lead: fansub_group.edit, fansub_group.links.manage, fansub_group.members.view, fansub_group.invitations.view, fansub_group.notes.write, anime_fansub_project.notes.write, release.view, release_version.view, release_version_media.view, release_version_media.upload, release_version_media.update, release_version_media.delete, release_version.notes.write
    - designer: release.view, release_version.view, release_version_media.view, release_version_media.upload, release_version_media.update, release_version_media.delete_own
    - editor: release.view, release_version.view, fansub_group.notes.write, anime_fansub_project.notes.write, release_version.notes.write
    - translator: release.view, release_version.view, release_version.notes.write
    - timer: release.view, release_version.view, release_version.notes.write
    - typesetter: release.view, release_version.view, release_version.notes.write
    - encoder: release.view, release_version.view, release_version_media.view, release_version_media.upload, release_version_media.update, release_version_media.delete_own, release_version.notes.write
    - raw_provider: release.view, release_version.view
    - quality_checker: release.view, release_version.view, release_version_media.view, release_version.notes.write
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go build ./... 2>&amp;1 | head -5</automated>
  </verify>
  <done>
    0108_capability_registry.up.sql und .down.sql existieren in database/migrations/.
    up.sql enthält CREATE TABLE action_definitions, CREATE TABLE role_capabilities, INSERT für 18 action_definitions, INSERT für alle roleMatrix-Paare (ohne platform_admin).
    down.sql droppt beide Tabellen in korrekter Reihenfolge.
    go build ./... kompiliert ohne Fehler.
  </done>
</task>

<task type="tdd">
  <name>Task 2: Wave-0-Tests (RED) — Seed-Parity, Cache-Lookup, Konsistenz-Check</name>
  <files>
    backend/internal/permissions/capability_registry_test.go
  </files>
  <read_first>
    - backend/internal/permissions/permissions_test.go — Test-Patterns (package permissions, testify, resolverStub, mockResolverV83)
    - backend/internal/permissions/permissions.go Zeilen 1–60 — alle Action-Konstanten und Rollen-Konstanten
  </read_first>
  <behavior>
    - TestRoleMatrixSeedParity: Definiert eine lokale Variable mit allen 18 Action-Codes als Strings. Prüft per Stub-CacheLoader, ob alle im zurückgegebenen Cache vertreten sind. MUSS RED sein, solange CacheLoader-Interface und LoadCache() noch nicht existieren.
    - TestCacheLoadAndLookup: Erstellt stub-CacheLoader der roleMatrix zurückgibt. Ruft Service.LoadCache() auf. Prüft: fansub_lead darf fansub_group.edit, editor DARF NICHT fansub_group.members.manage. MUSS RED sein solange LoadCache() nicht existiert.
    - TestStartupConsistencyCheck: Erstellt stub-CacheLoader der unvollständigen Cache (fehlt eine Action) zurückgibt. Erwartet, dass LoadCache() einen Fehler zurückgibt. MUSS RED sein.
    - Bestehende Tests in permissions_test.go dürfen NICHT brechen (roleAllows fällt auf roleMatrix-Fallback zurück wenn LoadCache nicht aufgerufen wurde — diese Tests rufen LoadCache nicht auf).
  </behavior>
  <action>
    Erstelle backend/internal/permissions/capability_registry_test.go im package permissions (White-Box).

    Imports: testing, context, slices, github.com/stretchr/testify/assert, github.com/stretchr/testify/require.

    Definiere einen lokalen Stub-Typ stubCacheLoader, der CacheLoader implementiert (sobald das Interface in permissions.go existiert — für jetzt als vorausgreifende Typdefinition). Das Interface wird in Plan 02 zu permissions.go hinzugefügt; hier im Test definieren wir es als lokales Interface-Alias (damit der Test zuerst kompiliert und RED ist):
    Alternativ: Definiere das CacheLoader-Interface direkt hier als lokales Interface — in Plan 02 wird es in permissions.go verschoben und der Test importiert es dann.

    TestRoleMatrixSeedParity:
    - Definiert allActionCodes []string mit allen 18 Action-Code-Strings (nicht die Konstanten — Strings, damit der Test unabhängig von der Konstanten-Liste ist und den Seed separat validiert).
    - Erstellt stubCacheLoader der roleMatrix-equivalent zurückgibt.
    - Ruft svc.LoadCache(ctx, stub) auf.
    - Prüft via assert.NoError.
    - Für jeden Action-Code: prüfe, dass mindestens eine Rolle in ActionCode → role_capabilities vorhanden ist (außer fansub_group.invitations.accept — der darf ohne role_capabilities existieren).
    Test bleibt RED bis Plan 02 LoadCache implementiert.

    TestCacheLoadAndLookup:
    - stubCacheLoader gibt vollständige roleMatrix zurück.
    - svc.LoadCache(ctx, stub).
    - Erwartete Wahrheiten aus R-02: RoleAllowsAction(RoleFansubLead, ActionFansubGroupEdit) == true, RoleAllowsAction(RoleEditor, ActionFansubGroupMembersManage) == false, RoleAllowsAction(RoleRawProvider, ActionReleaseView) == true.

    TestStartupConsistencyCheck:
    - stubCacheLoader gibt unvollständigen Cache zurück (fehlt ActionReleaseVersionNotesWrite in allen Rollen).
    - svc.LoadCache(ctx, stub) muss error != nil zurückgeben (D-10: Startup-Assertion).
    Test bleibt RED bis Plan 02 die Konsistenz-Prüfung in LoadCache implementiert.

    Der Test muss im aktuellen Zustand kompilieren (daher go build ./... als Verify). Er wird FAIL-en beim go test, was das RED-Kriterium erfüllt.
    Wenn CacheLoader noch nicht in permissions.go definiert ist, definiere das Interface lokal im Test:
    type CacheLoader interface { LoadRoleCapabilities(ctx context.Context) (map[string][]Action, error) }
    und nutze assert.Panics oder compile-error als RED-Indikator für die noch nicht existente LoadCache-Methode.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go build ./internal/permissions/... 2>&amp;1 | head -20</automated>
  </verify>
  <done>
    capability_registry_test.go existiert in backend/internal/permissions/.
    Datei enthält TestRoleMatrixSeedParity, TestCacheLoadAndLookup, TestStartupConsistencyCheck.
    go build ./internal/permissions/... kompiliert (Test-Datei syntaktisch korrekt).
    go test ./internal/permissions/... -run "Seed|Cache|Consistency" zeigt FAIL (RED) für die drei neuen Tests, bestehende Tests bleiben grün.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → DB | SQL-Seed schreibt Berechtigungs-Matrix; falsche Einträge = Privilege Escalation |
| Test-Stub → permissions.Service | Stub-Daten simulieren DB-Inhalt; Stub muss roleMatrix 1:1 wiedergeben |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01 | Tampering | 0108.up.sql Seed | mitigate | Seed exakt aus Research R-02 roleMatrix übernehmen; TestRoleMatrixSeedParity validiert 1:1-Parität |
| T-86-02 | Elevation of Privilege | role_capabilities (zu viele Einträge) | mitigate | Nur roleMatrix-Paare seeden; platform_admin explizit ausschließen; kein role_capabilities-Eintrag für platform_admin |
| T-86-03 | Tampering | FK role_definitions(code) | mitigate | ON DELETE CASCADE per D-02; Down-Migration entfernt role_capabilities zuerst |
| T-86-04 | Information Disclosure | contributor-Bug in can_edit_content | accept | contributor ist kein gültiger Code; kein Seed-Eintrag dafür; wird in Plan 03 SQL-Join bereinigt |
| T-86-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Packages in dieser Phase |
</threat_model>

<verification>
Nach Task 1:
- database/migrations/0108_capability_registry.up.sql enthält CREATE TABLE action_definitions, CREATE TABLE role_capabilities, alle 18 action_definitions-Einträge, alle roleMatrix-Paare.
- 0108_capability_registry.down.sql droppt role_capabilities dann action_definitions.
- go build ./... kompiliert.

Nach Task 2:
- backend/internal/permissions/capability_registry_test.go enthält 3 Test-Funktionen.
- go test ./internal/permissions/... -run "Seed|Cache|Consistency" → FAIL (RED, erwartet).
- go test ./internal/permissions/... -run "TestRoleAllows|TestService|TestCan" → PASS (bestehende Tests unverändert grün).
</verification>

<success_criteria>
- 0108.up.sql: CREATE TABLE action_definitions + role_capabilities + 18-Zeilen-Seed + roleMatrix-Seed (10 Rollen), alles in BEGIN/COMMIT
- 0108.down.sql: DROP TABLE role_capabilities; DROP TABLE action_definitions;
- capability_registry_test.go: TestRoleMatrixSeedParity, TestCacheLoadAndLookup, TestStartupConsistencyCheck — compilieren, sind RED
- Bestehende permissions_test.go-Tests bleiben grün
- go build ./... kompiliert ohne Fehler
</success_criteria>

<output>
Erstelle .planning/phases/86-daten-getriebene-capability-registry/86-01-SUMMARY.md wenn fertig.
</output>
