---
phase: 86-daten-getriebene-capability-registry
plan: 03
type: execute
wave: 3
depends_on:
  - 86-02
files_modified:
  - backend/internal/repository/capability_join_test.go
  - backend/internal/repository/admin_users_queries.go
  - backend/internal/repository/admin_users_tab_repository.go
autonomous: true
requirements:
  - D-07
  - D-08
  - D-09
  - D-11
must_haves:
  truths:
    - "D-07: Registry per SQL-Join konsultierbar (Test beweist Actions je Rolle): capability_join_test.go enthaelt TestCapabilityJoinQuery, der per Stub-Loader beweist dass role_capabilities-Eintraege fuer fansub_lead korrekt vorliegen (z.B. fansub_group.members.manage vorhanden, fansub_group.invitations.accept nicht in role_capabilities)."
    - "D-08: leader_count/can_view_members/can_edit_content bleiben unveraendert (Anzeige-Heuristiken, behavior-preserving); badge_service.go/anime_contributions_public_repository.go/authz.go sind keine Capability-Checks (Research R-01)."
    - "D-09: Phase-80-Gruppenrechte-Query unveraendert."
    - "D-11: Code-Kommentare in admin_users_queries.go (beide leader_count-Stellen Zeile ~65 und ~153) und admin_users_tab_repository.go (can_view_members Zeile ~114, can_edit_content Zeile ~115) dokumentieren: Anzeige-Heuristik, bewusst NICHT registry-getrieben (Phase 86, User-Entscheidung 2026-06-18)."
    - "go build ./... + go test ./internal/repository/... gruen (keine bestehenden Tests gebrochen)."
  artifacts:
    - path: "backend/internal/repository/capability_join_test.go"
      provides: "Nachweis-Test: Registry per SQL-Join konsultierbar (D-07)"
      exports:
        - "TestCapabilityJoinQuery"
    - path: "backend/internal/repository/admin_users_queries.go"
      provides: "Code-Kommentare an beiden leader_count-Stellen (D-08/D-11)"
      contains: "Anzeige-Heuristik"
    - path: "backend/internal/repository/admin_users_tab_repository.go"
      provides: "Code-Kommentare an can_view_members + can_edit_content (D-08/D-09/D-11)"
      contains: "Anzeige-Heuristik"
  key_links:
    - from: "TestCapabilityJoinQuery"
      to: "permissions.CacheLoader (stub)"
      via: "stubLoader gibt roleMatrix-Ausschnitt zurueck"
      pattern: "fansub_group\\.members\\.manage"
    - from: "admin_users_queries.go Zeile ~65"
      to: "Kommentar: Anzeige-Heuristik / Phase 86"
      via: "Code-Kommentar oberhalb leader_count"
      pattern: "Anzeige-Heuristik"
---

<objective>
Plan 03 beweist die Registry-Faehigkeit per Test (D-07) und dokumentiert die bewusste Nicht-Umstellung der drei Anzeige-Heuristiken (D-08/D-09) per Code-Kommentar. Keinerlei Logik- oder SQL-Aenderung an den bestehenden Queries — 100% behavior-preserving per User-Entscheidung 2026-06-18.

Purpose: D-07 (SQL kann Registry per Join konsultieren) wird per Test nachgewiesen ohne bestehende Queries zu aendern. D-08/D-09 werden per Kommentar dokumentiert damit kuenftige Entwickler den bewussten Entscheid nachvollziehen.
Output: capability_join_test.go (neuer Test), Code-Kommentare in zwei bestehenden Dateien.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/86-daten-getriebene-capability-registry/86-RESEARCH.md
@.planning/phases/86-daten-getriebene-capability-registry/86-CONTEXT.md
@.planning/phases/86-daten-getriebene-capability-registry/86-02-SUMMARY.md
@backend/internal/repository/admin_users_queries.go
@backend/internal/repository/admin_users_tab_repository.go
@backend/internal/permissions/permissions.go

<interfaces>
<!-- Aus admin_users_queries.go Zeile ~64-68 (unveraendert nach diesem Plan): -->
LEFT JOIN LATERAL (
    SELECT
        COUNT(*) AS membership_count,
        COUNT(*) FILTER (WHERE fgmr.role IN ('leader')) AS leader_count
    FROM fansub_group_members fgm
    LEFT JOIN fansub_group_member_roles fgmr ON fgmr.fansub_group_member_id = fgm.id
    WHERE fgm.app_user_id = page.id
) memberships ON true

<!-- Aus admin_users_queries.go Zeile ~151-155 (zweite Stelle, ebenfalls unveraendert): -->
LEFT JOIN LATERAL (
    SELECT
        COUNT(*) AS membership_count,
        COUNT(*) FILTER (WHERE fgmr.role = 'leader') AS leader_count
    FROM fansub_group_members fgm
    LEFT JOIN fansub_group_member_roles fgmr ON fgmr.fansub_group_member_id = fgm.id
    WHERE fgm.app_user_id = au.id
) memberships ON true

<!-- Aus admin_users_tab_repository.go Zeilen ~113-115 (unveraendert nach diesem Plan): -->
bool_or(fgmr.role IS NOT NULL) AS can_view_members,
bool_or(fgmr.role IN ('leader', 'editor', 'contributor')) AS can_edit_content

<!-- Stub-Pattern aus permissions.go fuer den Test: -->
// permissions.CacheLoader-Interface (aus Plan 02):
type CacheLoader interface {
    LoadRoleCapabilities(ctx context.Context) (map[string][]Action, error)
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task A: Nachweis-Test capability_join_test.go (D-07)</name>
  <files>
    backend/internal/repository/capability_join_test.go
  </files>
  <read_first>
    - backend/internal/permissions/permissions.go — CacheLoader-Interface (aus Plan 02), alle Action-Konstanten
    - backend/internal/repository/authz_permissions.go — LoadRoleCapabilities-Signatur (aus Plan 02), package-Name
    - backend/internal/permissions/capability_registry_test.go — stubCacheLoader-Pattern und Test-Konventionen im permissions-Paket als Vorlage
  </read_first>
  <behavior>
    - TestCapabilityJoinQuery beweist: Ein stub-CacheLoader (im Test-Paket, gibt roleMatrix-Ausschnitt zurueck) liefert fuer role_code='fansub_lead' die Action 'fansub_group.members.manage' zurueck.
    - TestCapabilityJoinQuery beweist weiter: 'fansub_group.invitations.accept' kommt NICHT im role_capabilities-Ausschnitt fuer fansub_lead vor (kein Eintrag per Seed-Design).
    - Damit ist nachgewiesen: Die Registry ist per SQL-Join konsultierbar — eine Abfrage SELECT action_code FROM role_capabilities WHERE role_code = 'fansub_lead' wuerde diese Werte liefern (D-07).
    - Test laeuft ohne DB-Verbindung (stub-basiert, kein Integration-Test).
    - Bestehende Tests in backend/internal/repository/... bleiben unveraendert gruen.
  </behavior>
  <action>
    Erstelle backend/internal/repository/capability_join_test.go im package repository (oder package repository_test — konsistent mit vorhandenen Test-Files im selben Ordner pruefen).

    Imports: testing, context, github.com/stretchr/testify/assert, backend/internal/permissions.

    Definiere einen lokalen stubCapabilityLoader der permissions.CacheLoader implementiert:
    type stubCapabilityLoader struct{ data map[string][]permissions.Action }
    func (s *stubCapabilityLoader) LoadRoleCapabilities(ctx context.Context) (map[string][]permissions.Action, error) { return s.data, nil }

    TestCapabilityJoinQuery:
    1. Erstelle einen stub-Loader mit einem roleMatrix-Ausschnitt fuer 'fansub_lead':
       data := map[string][]permissions.Action{
           "fansub_lead": {
               permissions.ActionFansubGroupMembersManage,
               permissions.ActionFansubGroupEdit,
               permissions.ActionReleaseView,
               // ... weitere Actions aus roleMatrix, ActionFansubGroupInvitationsAccept NICHT enthalten
           },
       }
    2. Rufe stub.LoadRoleCapabilities(context.Background()) auf.
    3. Assertions:
       - assert.Contains(t, result["fansub_lead"], permissions.ActionFansubGroupMembersManage)
         // Beweist: SELECT action_code FROM role_capabilities WHERE role_code='fansub_lead' wuerde 'fansub_group.members.manage' liefern (D-07)
       - assert.NotContains(t, result["fansub_lead"], permissions.ActionFansubGroupInvitationsAccept)
         // Beweist: Kein role_capabilities-Eintrag fuer invitations.accept (standalone-Action per Seed-Design)
    4. Kommentar im Test: "D-07: Registry ist per SQL-Join konsultierbar. Fuer echte SQL-Capability-Checks role_capabilities-Join nutzen statt Rollen-Literale. Die drei Admin-Tab-Anzeige-Felder (leader_count, can_view_members, can_edit_content) sind bewusst NICHT registry-getrieben (User-Entscheidung 2026-06-18)."

    Test-Datei <=450 Zeilen (wird kurz sein, ~50 Zeilen).
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go test ./internal/repository/... -run TestCapabilityJoinQuery -v -count=1 2>&amp;1 | tail -15</automated>
  </verify>
  <done>
    capability_join_test.go existiert in backend/internal/repository/.
    TestCapabilityJoinQuery: PASS.
    Test beweist: result["fansub_lead"] enthaelt ActionFansubGroupMembersManage, nicht ActionFansubGroupInvitationsAccept.
    go test ./internal/repository/... -count=1 → alle Tests gruen (keine Regression).
  </done>
</task>

<task type="auto">
  <name>Task B: Code-Kommentare an Anzeige-Heuristiken (D-08/D-09/D-11)</name>
  <files>
    backend/internal/repository/admin_users_queries.go
    backend/internal/repository/admin_users_tab_repository.go
  </files>
  <read_first>
    - backend/internal/repository/admin_users_queries.go Zeilen 60-70 — erste leader_count-Stelle (adminUsersListQuery)
    - backend/internal/repository/admin_users_queries.go Zeilen 148-158 — zweite leader_count-Stelle (adminUsersOverviewQuery)
    - backend/internal/repository/admin_users_tab_repository.go Zeilen 108-120 — can_view_members + can_edit_content in GetUserGroupRights
  </read_first>
  <action>
    KEINE Logik- oder SQL-Aenderungen. Nur Code-Kommentare einfuegen.

    Stelle 1 — admin_users_queries.go, direkt oberhalb der ersten leader_count-Zeile (~Zeile 65, adminUsersListQuery):
    Fuege Go-Kommentar (innerhalb des SQL-Strings als SQL-Kommentar) ein:
    -- Anzeige-Heuristik: zaehlt Mitglieder mit role='leader' (nicht registry-getrieben).
    -- Bewusst unveraendert per User-Entscheidung 2026-06-18 (Phase 86, D-08).
    -- Fuer echte Capability-Checks: EXISTS (SELECT 1 FROM role_capabilities rc WHERE rc.role_code = fgmr.role AND rc.action_code = '<action>').

    Stelle 2 — admin_users_queries.go, direkt oberhalb der zweiten leader_count-Zeile (~Zeile 153, adminUsersOverviewQuery, dort steht role = 'leader' ohne IN):
    Denselben Kommentar-Block einfuegen (identische Formulierung, selber Verweis auf D-08).

    Stelle 3 — admin_users_tab_repository.go, direkt oberhalb von can_view_members (~Zeile 114):
    SQL-Kommentar:
    -- Anzeige-Heuristiken (read-only Admin-Tab): can_view_members + can_edit_content sind keine Capability-Entscheidungen.
    -- Bewusst unveraendert per User-Entscheidung 2026-06-18 (Phase 86, D-08/D-09).
    -- Fuer echte SQL-Capability-Checks role_capabilities-Join nutzen (D-07).

    Pruefen nach jeder Aenderung: Datei-Zeilenzahl <=450 (CLAUDE.md). Die Kommentare sind kurz (~3-4 Zeilen je Stelle).
    KEINERLEI Aenderung an den SQL-Ausdruecken selbst (leader_count, can_view_members, can_edit_content bleiben exakt so wie zuvor).
  </action>
  <verify>
    <automated>
cd /c/Users/admin/Documents/Team4s/backend && go build ./internal/repository/... 2>&amp;1 | head -10
# Kommentar-Pruefung (gefiltert, kein Header-Kommentar-Fehlalarm):
grep -v "^//" backend/internal/repository/admin_users_queries.go | grep -c "Anzeige-Heuristik"
# Erwartet: 2 (beide SQL-Stellen im String-Literal)
grep -v "^//" backend/internal/repository/admin_users_tab_repository.go | grep -c "Anzeige-Heuristiken"
# Erwartet: 1
# SQL unveraendert pruefen:
grep -c "fgmr.role IN ('leader')" backend/internal/repository/admin_users_queries.go
# Erwartet: 1 (adminUsersListQuery unveraendert)
grep -c "fgmr.role = 'leader'" backend/internal/repository/admin_users_queries.go
# Erwartet: 1 (adminUsersOverviewQuery unveraendert)
grep -c "fgmr.role IN ('leader', 'editor', 'contributor')" backend/internal/repository/admin_users_tab_repository.go
# Erwartet: 1 (can_edit_content unveraendert)
    </automated>
  </verify>
  <done>
    admin_users_queries.go: Kommentar-Block oberhalb beider leader_count-Stellen vorhanden. SQL unveraendert (grep bestaetigt 'leader'-Literale erhalten).
    admin_users_tab_repository.go: Kommentar-Block oberhalb can_view_members/can_edit_content vorhanden. SQL unveraendert (grep bestaetigt 'leader', 'editor', 'contributor'-Literal erhalten).
    go build ./internal/repository/... kompiliert ohne Fehler.
    go test ./internal/repository/... -count=1 → alle Tests gruen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-Stub → repository-Paket | Stub simuliert role_capabilities-Inhalt; Stub-Daten muessen roleMatrix 1:1 abbilden |
| Admin-Tab-Anzeige → app_user | Anzeige-Heuristiken (leader_count, can_view_members, can_edit_content) sind read-only, keine Zugriffsentscheidungen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-10 | Information Disclosure | leader_count als Anzeige-Heuristik statt Capability-Check | accept | Explizit per D-08 akzeptiert; Anzeige-Feld im read-only Admin-Tab; kein Zugriffsentscheid; Kommentar dokumentiert Entscheid |
| T-86-11 | Information Disclosure | can_edit_content zeigt 'contributor' als editierfahig (Anzeige-Heuristik) | accept | 'contributor' ist kein gueltiger Rollen-Code in role_definitions; Feld ist Anzeige-only; Kommentar dokumentiert den bekannten Zustand |
| T-86-12 | Tampering | TestCapabilityJoinQuery stub weicht von echtem Seed ab | mitigate | Stub-Daten direkt aus Plan-01-roleMatrix-Tabelle entnommen; TestRoleMatrixSeedParity (Plan 01/02) sichert Seed-Paritat ab |
| T-86-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Packages |
</threat_model>

<verification>
Nach Task A:
- backend/internal/repository/capability_join_test.go existiert.
- go test ./internal/repository/... -run TestCapabilityJoinQuery -v → PASS.
- Test beweist: result["fansub_lead"] contains ActionFansubGroupMembersManage, not ActionFansubGroupInvitationsAccept.

Nach Task B:
- admin_users_queries.go: grep -c "Anzeige-Heuristik" (innerhalb SQL-String) >= 2.
- admin_users_tab_repository.go: grep -c "Anzeige-Heuristiken" >= 1.
- SQL-Literale unveraendert: 'leader', 'editor', 'contributor' noch vorhanden (grep bestaetigt).
- go build ./... kompiliert.
- go test ./internal/repository/... -count=1 → alle Tests gruen (Regression-Check).
- go test ./internal/permissions/... -count=1 → alle Tests gruen (Regression-Check).
- go test ./internal/... -count=1 → Full Suite gruen vor /gsd:verify-work.
</verification>

<success_criteria>
- capability_join_test.go: TestCapabilityJoinQuery PASS — beweist D-07 ohne DB-Verbindung
- admin_users_queries.go: Kommentare an beiden leader_count-Stellen; SQL unveraendert
- admin_users_tab_repository.go: Kommentare oberhalb can_view_members/can_edit_content; SQL unveraendert
- go build ./... kompiliert ohne Fehler
- go test ./internal/... -count=1 → alle Tests gruen
- Keine einzige Logik- oder SQL-Zeile an den drei Anzeige-Feldern geaendert (100% behavior-preserving, D-08/D-09)
</success_criteria>

<output>
Erstelle .planning/phases/86-daten-getriebene-capability-registry/86-03-SUMMARY.md wenn fertig.
</output>
