---
phase: 86-daten-getriebene-capability-registry
plan: 03
type: execute
wave: 3
depends_on:
  - 86-02
files_modified:
  - backend/internal/repository/admin_users_queries.go
  - backend/internal/repository/admin_users_tab_repository.go
autonomous: true
requirements:
  - D-07
  - D-08
  - D-09
  - D-11
must_haves:
  truths:
    - "D-07: admin_users_queries.go Zeile 65 nutzt EXISTS (SELECT 1 FROM role_capabilities rc WHERE rc.role_code = fgmr.role AND rc.action_code = 'fansub_group.members.manage') statt fgmr.role IN ('leader')."
    - "D-07: admin_users_queries.go Zeile 153 (zweite leader_count-Stelle in adminUsersOverviewQuery) nutzt denselben role_capabilities-Join — BEIDE Stellen gleichzeitig umgestellt (Pitfall 6 aus Research)."
    - "D-08/D-09: admin_users_tab_repository.go Zeile 115 nutzt EXISTS (SELECT 1 FROM role_capabilities rc WHERE rc.role_code = fgmr.role AND rc.action_code = 'release_version.notes.write') statt fgmr.role IN ('leader', 'editor', 'contributor')."
    - "D-09: can_view_members bleibt bool_or(fgmr.role IS NOT NULL) — KEINE Änderung; kein Literal-Check dort."
    - "Verhalten unverändert: leader_count = Anzahl Mitglieder mit fansub_group.members.manage-Berechtigung (nur fansub_lead hat das); can_edit_content = Mitglieder mit release_version.notes.write (leader + editor — wie vorher, contributor-Bug bereinigt)."
    - "D-11: Kommentar in admin_users_queries.go und admin_users_tab_repository.go dokumentiert: 'Neues Recht = INSERT in action_definitions + role_capabilities, kein SQL-Edit hier nötig'."
    - "go build ./... + go test ./internal/repository/... grün."
  artifacts:
    - path: "backend/internal/repository/admin_users_queries.go"
      provides: "leader_count via role_capabilities-Join (beide Stellen)"
      contains: "role_capabilities rc"
    - path: "backend/internal/repository/admin_users_tab_repository.go"
      provides: "can_edit_content via role_capabilities-Join"
      contains: "role_capabilities rc"
  key_links:
    - from: "adminUsersListQuery leader_count"
      to: "role_capabilities.action_code='fansub_group.members.manage'"
      via: "EXISTS Subquery"
      pattern: "fansub_group\\.members\\.manage"
    - from: "GetUserGroupRights can_edit_content"
      to: "role_capabilities.action_code='release_version.notes.write'"
      via: "EXISTS Subquery"
      pattern: "release_version\\.notes\\.write"
---

<objective>
Die drei verifizierten SQL-Capability-Stellen werden auf role_capabilities-JOIN umgestellt: leader_count in adminUsersListQuery (Zeile 65) und adminUsersOverviewQuery (Zeile 153) in admin_users_queries.go, sowie can_edit_content in GetUserGroupRights (Zeile 115) in admin_users_tab_repository.go. Verhalten unverändert — nur 'contributor'-Bug in can_edit_content wird implizit bereinigt.

Purpose: Schließt D-07, D-08, D-09; SQL liest jetzt dieselbe Quelle wie Go (role_capabilities); keine Rollen-Literale mehr in Capability-Entscheidungen.
Output: admin_users_queries.go (2 SQL-Stellen geändert), admin_users_tab_repository.go (1 SQL-Stelle geändert).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/86-daten-getriebene-capability-registry/86-RESEARCH.md
@.planning/phases/86-daten-getriebene-capability-registry/86-02-SUMMARY.md
@backend/internal/repository/admin_users_queries.go
@backend/internal/repository/admin_users_tab_repository.go

<interfaces>
<!-- Aus admin_users_queries.go — Zeile 65 (aus Read-Lektüre oben) -->
-- VORHER:
COUNT(*) FILTER (WHERE fgmr.role IN ('leader')) AS leader_count
-- NACHHER (per D-07, fansub_group.members.manage — nur fansub_lead hat das):
COUNT(*) FILTER (WHERE EXISTS (
    SELECT 1 FROM role_capabilities rc
    WHERE rc.role_code = fgmr.role
      AND rc.action_code = 'fansub_group.members.manage'
)) AS leader_count

<!-- Aus admin_users_tab_repository.go — Zeile 115 (aus Read-Lektüre oben) -->
-- VORHER:
bool_or(fgmr.role IN ('leader', 'editor', 'contributor')) AS can_edit_content
-- NACHHER (per D-07/D-09, release_version.notes.write — leader + editor besitzen das; contributor-Bug bereinigt):
bool_or(EXISTS (
    SELECT 1 FROM role_capabilities rc
    WHERE rc.role_code = fgmr.role
      AND rc.action_code = 'release_version.notes.write'
)) AS can_edit_content

<!-- Unverändertes Field in admin_users_tab_repository.go Zeile 114: -->
bool_or(fgmr.role IS NOT NULL) AS can_view_members
-- NICHT ÄNDERN — kein Literal-Check, kein Capability-Bypass
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: leader_count auf role_capabilities-Join umstellen (beide Stellen)</name>
  <files>
    backend/internal/repository/admin_users_queries.go
  </files>
  <read_first>
    - backend/internal/repository/admin_users_queries.go vollständig lesen — beide SQL-Strings adminUsersListQuery (Zeile 65) und adminUsersOverviewQuery (Zeile 153) identifizieren; sicherstellen, dass der Kontext (LEFT JOIN LATERAL, COUNT FILTER WHERE) richtig gelesen wurde
    - backend/internal/repository/admin_users_queries.go — sicherstellen, dass keine weiteren Rollen-Literale vorhanden sind die umgestellt werden müssen (Research: nur Zeile 65 und 153)
  </read_first>
  <behavior>
    - Nach diesem Task: leader_count in adminUsersListQuery nutzt EXISTS (SELECT 1 FROM role_capabilities rc WHERE rc.role_code = fgmr.role AND rc.action_code = 'fansub_group.members.manage').
    - Nach diesem Task: leader_count in adminUsersOverviewQuery (Zeile 153) nutzt denselben JOIN-Pattern.
    - Verhalten: leader_count bleibt 1 für Nutzer mit fansub_lead-Rolle (da fansub_lead die einzige Rolle mit fansub_group.members.manage ist — Research A3 bestätigt).
    - go test ./internal/repository/... -run AdminUsers grün (bestehende Tests unverändert).
    - KEIN anderer SQL-Code in der Datei verändert.
  </behavior>
  <action>
    Lese admin_users_queries.go vollständig. Identifiziere beide SQL-Stellen mit leader_count.

    Stelle 1 (adminUsersListQuery, Zeile ~65):
    Ersetze COUNT(*) FILTER (WHERE fgmr.role IN ('leader')) AS leader_count
    durch:
    COUNT(*) FILTER (WHERE EXISTS (
        SELECT 1 FROM role_capabilities rc
        WHERE rc.role_code = fgmr.role
          AND rc.action_code = 'fansub_group.members.manage'
    )) AS leader_count

    Stelle 2 (adminUsersOverviewQuery, Zeile ~153):
    Exakt dieselbe Ersetzung. Beide Stellen müssen in einem Commit geändert werden (Pitfall 6 aus Research — divergierendes Verhalten wenn nur eine geändert wird).

    Füge oberhalb beider geänderten Stellen einen SQL-Kommentar ein:
    -- Capability-Join: fansub_group.members.manage (nur fansub_lead). Neues Recht = INSERT in role_capabilities, kein Edit hier nötig. (D-11)

    Datei-Zeilen-Limit CLAUDE.md <=450 prüfen nach Änderung (die Änderung ist neutral — EXISTS-Block ersetzt IN-Literal, leicht länger).
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go build ./internal/repository/... 2>&amp;1 | head -10 && go test ./internal/repository/... -run AdminUsers -count=1 -v 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    admin_users_queries.go: Beide leader_count-Stellen nutzen role_capabilities-JOIN.
    go build ./internal/repository/... kompiliert.
    go test -run AdminUsers grün (oder kein AdminUsers-Test vorhanden → build-only verifiziert).
    Keine anderen Änderungen an der Datei.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: can_edit_content auf role_capabilities-Join umstellen</name>
  <files>
    backend/internal/repository/admin_users_tab_repository.go
  </files>
  <read_first>
    - backend/internal/repository/admin_users_tab_repository.go Zeilen 100–140 — GetUserGroupRights Query vollständig lesen; Kontext: SELECT fg.id, fg.name, ARRAY_AGG, bool_or(fgmr.role IS NOT NULL), bool_or(fgmr.role IN (...)) — beide bool_or-Felder identifizieren
    - backend/internal/repository/admin_users_tab_repository.go Zeilen 1–20 — Imports prüfen (kein neues Import nötig; EXISTS ist reines SQL)
  </read_first>
  <behavior>
    - Nach diesem Task: can_edit_content nutzt EXISTS (SELECT 1 FROM role_capabilities rc WHERE rc.role_code = fgmr.role AND rc.action_code = 'release_version.notes.write').
    - can_view_members (bool_or(fgmr.role IS NOT NULL)) bleibt UNVERÄNDERT.
    - Verhalten: can_edit_content = true für Nutzer mit fansub_lead oder editor-Rolle (beide haben release_version.notes.write); contributor-Bug (kein gültiger Code) ist implizit bereinigt — niemand hatte diese Rolle je.
    - go test ./internal/repository/... -run GetUserGroupRights grün (oder build-pass wenn kein spezifischer Test existiert).
    - Datei <=450 Zeilen nach Änderung.
  </behavior>
  <action>
    Lese admin_users_tab_repository.go Zeilen 100–140 um die genaue Stelle zu identifizieren.

    Ersetze in GetUserGroupRights Query:
    bool_or(fgmr.role IN ('leader', 'editor', 'contributor')) AS can_edit_content
    durch:
    bool_or(EXISTS (
        SELECT 1 FROM role_capabilities rc
        WHERE rc.role_code = fgmr.role
          AND rc.action_code = 'release_version.notes.write'
    )) AS can_edit_content

    Action-Code-Begründung (im SQL-Kommentar dokumentieren):
    -- Capability-Join: release_version.notes.write (fansub_lead + editor besitzen das; 'contributor' war kein gültiger Rollen-Code — Bug bereinigt). D-07/D-09/D-11

    bool_or(fgmr.role IS NOT NULL) AS can_view_members: NICHT ändern.

    Datei <=450 Zeilen: Die EXISTS-Erweiterung fügt ~3 Zeilen hinzu. Falls Datei nach Änderung >450 Zeilen: Kommentar komprimieren oder Research-Notiz in DECISIONS.md statt inline.
  </action>
  <verify>
    <automated>cd /c/Users/admin/Documents/Team4s/backend && go build ./... 2>&amp;1 | head -10 && go test ./internal/repository/... -count=1 2>&amp;1 | tail -15</automated>
  </verify>
  <done>
    admin_users_tab_repository.go Zeile 115: EXISTS (... action_code = 'release_version.notes.write') statt IN ('leader', 'editor', 'contributor').
    go build ./... kompiliert.
    go test ./internal/repository/... grün.
    can_view_members unverändert.
    Datei <=450 Zeilen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL EXISTS Subquery → role_capabilities | Abfrage liest role_capabilities zur Laufzeit; Tabelle muss via Migration 0108 befüllt sein |
| app_user → leader_count/can_edit_content | Boolean-Felder werden im Detail-Drawer angezeigt; falscher Wert = fehlgeleitete Rechteanzeige |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-10 | Tampering | EXISTS JOIN ohne role_capabilities-Daten | mitigate | 0108.up.sql seedet die Matrix vor Backend-Start; main.go-LoadCache schlägt fehl wenn Tabelle leer → Backend startet nicht |
| T-86-11 | Elevation of Privilege | can_edit_content: 'contributor'-Bug bleibt | mitigate | EXISTS JOIN auf 'release_version.notes.write' matcht nur echte Rollen; 'contributor' kann nie matchen da er nicht in role_capabilities existiert |
| T-86-12 | Tampering | Nur eine leader_count-Stelle geändert | mitigate | Beide Stellen (Zeile 65 + 153) werden im selben Task geändert (Pitfall 6 aus Research explizit adressiert) |
| T-86-13 | Information Disclosure | can_view_members zeigt false wenn role_capabilities leer | mitigate | can_view_members nutzt bool_or(fgmr.role IS NOT NULL) — kein JOIN gegen role_capabilities; unabhängig vom Cache-Status |
| T-86-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Packages |
</threat_model>

<verification>
Nach Task 1:
- admin_users_queries.go: Beide leader_count-SQL-Stellen haben EXISTS (... action_code = 'fansub_group.members.manage').
- go build ./internal/repository/... kompiliert.

Nach Task 2:
- admin_users_tab_repository.go: can_edit_content nutzt EXISTS (... action_code = 'release_version.notes.write').
- go build ./... kompiliert.
- go test ./internal/repository/... -count=1 alle Tests grün.
- go test ./internal/permissions/... -count=1 alle Tests grün (Regression-Check).
- go test ./internal/... -count=1 → Full Suite grün vor /gsd:verify-work.
</verification>

<success_criteria>
- admin_users_queries.go: Kein fgmr.role IN ('leader') mehr — beide Stellen auf role_capabilities-JOIN.
- admin_users_tab_repository.go: Kein fgmr.role IN ('leader', 'editor', 'contributor') mehr — auf role_capabilities-JOIN.
- can_view_members bleibt bool_or(fgmr.role IS NOT NULL).
- go build ./... kompiliert ohne Fehler.
- go test ./internal/... -count=1 → alle Tests grün.
- D-11-Kommentare in beiden Dateien erklären, dass neues Recht = nur Daten-Insert.
</success_criteria>

<output>
Erstelle .planning/phases/86-daten-getriebene-capability-registry/86-03-SUMMARY.md wenn fertig.
</output>
