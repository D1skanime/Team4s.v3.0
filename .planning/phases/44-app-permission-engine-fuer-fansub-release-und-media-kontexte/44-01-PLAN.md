---
phase: 44-app-permission-engine-fuer-fansub-release-und-media-kontexte
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/cmd/server
  - backend/internal/handlers
  - backend/internal/middleware
  - backend/internal/models
  - backend/internal/permissions
  - backend/internal/repository
  - database/migrations
  - docs
autonomous: true
requirements:
  - AUTHZ-ENGINE-01
must_haves:
  truths:
    - "Phase 44 centralizes authorization; handlers and frontend must stop making standalone role decisions."
    - "Keycloak remains identity-only; business roles stay in Team4s data."
    - "Group-scope roles are evaluated from fansub-group membership data, not from JWT role claims."
    - "Only `fansub_group_member_roles` may grant app permissions; release/member credit tables never do."
    - "platform_admin short-circuits all permission checks, but normal users need resource-context evaluation."
    - "release_version_groups.fansub_group_id remains the canonical fansub-group column."
    - "If the Phase-43 auth foundation is missing in the executing branch, execution must stop before building Phase 44 on top of legacy auth seams."
    - "Context resolvers are central infrastructure; handlers must not compose their own authz context."
---

<objective>
Build the backend permission foundation for Phase 44: central action constants, static role matrix, sparse-but-resolved `PermissionContext`, and reusable `Can(...)` / `RequirePermission(...)` seams that future handlers and capability endpoints can share.

Purpose: Team4s needs one authoritative permission layer before it can safely expose fansub-, release-, release-version-, media-, and description-level self-service without reintroducing ad-hoc global-admin gates.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/44-app-permission-engine-fuer-fansub-release-und-media-kontexte/44-CONTEXT.md
@.planning/phases/44-app-permission-engine-fuer-fansub-release-und-media-kontexte/44-RESEARCH.md
@docs/architecture/db-schema-fansub-domain.md
@backend/cmd/server/main.go
@backend/internal/middleware/comment_auth.go
@backend/internal/repository/authz.go
@backend/internal/handlers/admin_content_authz.go
@backend/internal/handlers/fansub_admin.go
@backend/internal/repository/admin_content_fansub_releases.go
@backend/internal/repository/release_version_media_repository.go

<interfaces>
<!-- Expected upstream Phase-43 seams: CurrentUser/app-user/global-role/group-membership -->
<!-- Phase-44 foundation should fail fast if these seams are absent in the executing branch -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyse existing schema/code structure first and add the auth-foundation gate</name>
  <read_first>
    - docs/architecture/db-schema-fansub-domain.md
    - database/migrations
    - backend/cmd/server/main.go
    - backend/internal/middleware/comment_auth.go
    - backend/internal/repository/authz.go
    - .planning/phases/44-app-permission-engine-fuer-fansub-release-und-media-kontexte/44-CONTEXT.md
  </read_first>
  <action>
    Before writing permission logic, perform the mandatory bestandsanalyse and then verify that the branch actually contains the Phase-43 auth foundation the phase is supposed to build on.

    Concretely:
    - inspect existing user/app-user tables, Keycloak mapping columns, fansub membership/role tables, audit seams, release/media joins, and owner columns
    - inspect existing auth middleware, admin helpers, role checks, repository/service seams, response conventions, test layout, admin API clients, error handling, and UI entry points
    - write down which existing structures will be reused for Phase 44 and whether any migration is actually needed
    - identify the canonical authenticated principal type that Phase 44 will consume
    - verify that the branch exposes the expected app-user/global-role/fansub-membership seam
    - if it is missing, add an explicit blocker path (clear runtime/dev error and documentation note) instead of silently binding the new engine to legacy `users` + `user_roles`
    - if analysis reveals ambiguity around ownership or role source, stop with a concrete blocker question instead of inventing a parallel structure

    End state for this task:
    - there is a documented Ist-Analyse baseline for DB, backend, and frontend seams
    - there is one canonical actor input for the permission engine
    - Phase 44 does not proceed on an ambiguous auth seam
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./... -run "Test.*Auth|Test.*Permission" ./...</automated>
  </verify>
  <done>
    - Existing schema/code reality is documented before any new authz structure is introduced.
    - The executing branch either exposes the expected Phase-43 actor seam or fails fast with an explicit blocker.
    - The permission package knows which authenticated actor struct it accepts.
    - Legacy global-admin helpers are marked for replacement instead of being silently reused as-is.
  </done>
</task>

<task type="auto">
  <name>Task 2: Introduce central permission actions, role constants, and the static matrix</name>
  <read_first>
    - .planning/phases/44-app-permission-engine-fuer-fansub-release-und-media-kontexte/44-CONTEXT.md
    - .planning/phases/44-app-permission-engine-fuer-fansub-release-und-media-kontexte/44-RESEARCH.md
  </read_first>
  <action>
    Create a dedicated backend permission package that centrally defines:
    - typed action constants for the phase-44 actions
    - typed role constants for `platform_admin` plus all listed fansub roles
    - a static role matrix that maps roles to actions
    - a typed `PermissionResult` / decision object with `Allowed`, `ReasonCode`, `Reason`, and optional `MatchedRole` / `MatchedScope`
    - helper groupings only where needed for capabilities/tests/docs

    Be explicit that:
    - `platform_admin` bypasses matrix evaluation
    - `scope_type = group` is fully implemented
    - future scope types may exist as prepared constants but are not yet fully active
    - only `fansub_group_member_roles` may grant app permissions; `release_member_roles` and other credit tables are never consulted
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/permissions/...</automated>
  </verify>
  <done>
    - No priority handler needs to hardcode action strings itself.
    - The role matrix is readable and testable in one place.
    - The package documents which future scopes are only preparatory.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement PermissionContext resolution and `Can(...)` / `RequirePermission(...)`</name>
  <read_first>
    - backend/internal/repository/admin_content_fansub_releases.go
    - backend/internal/repository/release_version_media_repository.go
    - docs/architecture/db-schema-fansub-domain.md
  </read_first>
  <action>
    Implement the core evaluator:
    - sparse `PermissionContext`
    - repository-backed context resolvers
    - central resolver entrypoints for `fansub_group_id`, `release_id`, `release_version_id`, `release_media_id`, and description targets
    - group membership and role loading
    - coop/multi-group resolution for release-version and media contexts
    - disabled-user, missing-membership, wrong-group, and `delete_own` handling
    - `Can(...)` returning a structured decision
    - `RequirePermission(...)` translating that decision into reusable handler behavior

    The resolver layer must be the only place where IDs like `release_version_id` or `release_media_id` are expanded to fansub/release context.

    Additional invariants:
    - ignore or reject manipulated frontend context fields in favor of DB truth
    - `delete_own` must derive ownership from DB owner columns such as `uploaded_by_user_id` or `created_by_user_id`
    - `modified_by_user_id` must never be treated as ownership
    - if ownership cannot be determined reliably, `delete_own` is false
    - prefer extending existing repositories/queries for resource resolution; do not add a parallel permission-only data model unless the pre-analysis proves a gap
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/permissions/... ./internal/repository/...</automated>
  </verify>
  <done>
    - The engine can answer group-scoped permission questions from resource IDs.
    - `delete_own` works only when the resource owner matches the actor.
    - Permission failures can distinguish 401 vs 403 vs internal context-resolution errors.
  </done>
</task>

</tasks>

<verification>
Automated:
1. `cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/permissions/...`
2. `cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/repository/...`
3. `cd C:/Users/admin/Documents/Team4s/backend && go test ./...`
4. `cd C:/Users/admin/Documents/Team4s/backend && go build ./...`

Targeted:
1. Verify the phase blocks cleanly if the expected Phase-43 actor seam is absent.
2. Verify action and role constants compile without string duplication in touched handlers.
3. Verify context resolvers map `release_version_id` and media identifiers to the correct fansub group.
4. Verify mismatched request-vs-DB context is rejected or ignored in favor of DB truth.
5. Verify coop release-version resolution returns the participating-group set for later permission evaluation.
</verification>

<success_criteria>
- There is one backend package that owns actions, roles, matrix, contexts, and evaluation helpers.
- The evaluator supports group-scope membership and role checks with room for later scopes.
- The evaluator exposes structured permission results with stable reason codes.
- `Can(...)` and `RequirePermission(...)` exist and are ready for handler adoption.
- Phase 44 will not silently bind itself to the wrong auth seam if Phase 43 is still missing.
</success_criteria>

<output>
No summary file in this plan. Later plans should reuse the foundation from this slice.
</output>
