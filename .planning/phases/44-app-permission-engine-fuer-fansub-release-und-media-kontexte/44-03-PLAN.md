---
phase: 44-app-permission-engine-fuer-fansub-release-und-media-kontexte
plan: "03"
type: execute
wave: 3
depends_on:
  - 44-02-PLAN.md
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit
  - frontend/src/app/admin/fansubs/[id]/members
  - frontend/src/app/admin/episode-versions
  - frontend/src/components
  - frontend/src/lib
  - frontend/src/types
autonomous: true
requirements:
  - AUTHZ-ENGINE-01
must_haves:
  truths:
    - "Frontend capability checks only shape visibility and disabled states; they never replace backend enforcement."
    - "No touched frontend screen may keep role-name logic like role === fansub_lead."
    - "403 feedback must be understandable to operators and scoped to the failing surface."
---

<objective>
Wire the admin frontend to the new backend capability responses with the smallest useful UX change set: hide or disable forbidden actions, keep the existing UI structure, and show understandable 403 feedback when backend enforcement still rejects a request.

Purpose: Phase 44 should make the UI permission-aware without turning the client into a second policy engine.
</objective>

<context>
@.planning/phases/44-app-permission-engine-fuer-fansub-release-und-media-kontexte/44-02-PLAN.md
@frontend/src/lib/api.ts
@frontend/src/app/admin/fansubs/[id]/edit/page.tsx
@frontend/src/app/admin/fansubs/[id]/members/page.tsx
@frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseVersionMedia.ts
@frontend/src/app/admin/fansubs/[id]/edit/ReleaseVersionMediaDrawerSummary.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed capability DTOs and client helpers</name>
  <read_first>
    - frontend/src/lib/api.ts
    - frontend/src/types/fansub.ts
    - frontend/src/types/releaseVersionMedia.ts
  </read_first>
  <action>
    Add the frontend-facing DTOs and API helpers for the new capability endpoints.

    Requirements:
    - typed response shapes
    - small fetch helpers in `api.ts`
    - no new global auth state layer
    - reuse the current auth-token helper seam
    - capabilities are treated as resource-scoped and refreshable, not as globally cacheable user state
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck</automated>
  </verify>
  <done>
    - The frontend can request fansub-group and release-version capabilities explicitly.
    - Capability types are reusable across the touched admin screens.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply capability-driven visibility and disabled states to the MVP admin surfaces</name>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
    - frontend/src/app/admin/fansubs/[id]/members/page.tsx
    - frontend/src/app/admin/episode-versions/[versionId]/edit/useReleaseVersionMedia.ts
  </read_first>
  <action>
    Update the MVP surfaces to consume capabilities instead of hard assumptions:
    - fansub edit actions
    - members manage actions
    - release drawer CTAs
    - release-version media upload/edit/delete controls
    - description/note edit affordances on the touched screens

    Prefer:
    - hide actions that are fully unavailable
    - disable actions when showing the control adds useful context

    Ensure capability reloads are wired for:
    - context switch
    - drawer open
    - relevant successful mutations
    - known role/context refresh points

    Stay inside the existing project styling and page structure.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck && npm run lint</automated>
  </verify>
  <done>
    - MVP admin surfaces no longer rely on role-name assumptions in UI logic.
    - Users without capability do not see or cannot trigger the protected actions.
    - The visual changes stay minimal and local to permission affordances.
  </done>
</task>

<task type="auto">
  <name>Task 3: Surface understandable 403 feedback</name>
  <read_first>
    - frontend/src/lib/api.ts
    - touched admin pages/hooks from Task 2
  </read_first>
  <action>
    Make forbidden responses understandable:
    - preserve existing `ApiError` handling patterns
    - convert 403 responses into contextual, user-facing copy on the affected admin surfaces
    - keep loading/error scope local to the relevant drawer/page/panel

    Do not add a global modal or broad redesign for this.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck && npm run lint</automated>
  </verify>
  <done>
    - 403s show understandable, local feedback.
    - Operators are not left with silent failures when backend enforcement rejects an action.
  </done>
</task>

</tasks>

<verification>
Automated:
1. `cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck`
2. `cd C:/Users/admin/Documents/Team4s/frontend && npm run lint`

Behavioral:
1. Capability-false actions are hidden or disabled on the targeted screens.
2. No touched page uses direct role-name checks to decide UI affordances.
3. A forced 403 path surfaces readable feedback instead of generic failure noise.
</verification>

<success_criteria>
- The MVP admin UI consumes backend capabilities instead of hard role assumptions.
- Permission affordances are local, minimal, and aligned with backend enforcement.
- Forbidden responses are understandable to operators.
</success_criteria>
