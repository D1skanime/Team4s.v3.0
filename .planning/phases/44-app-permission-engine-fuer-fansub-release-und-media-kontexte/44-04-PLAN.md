---
phase: 44-app-permission-engine-fuer-fansub-release-und-media-kontexte
plan: "04"
type: execute
wave: 4
depends_on:
  - 44-03-PLAN.md
files_modified:
  - backend/internal/handlers
  - backend/internal/permissions
  - backend/internal/repository
  - frontend/src/app
  - frontend/src/lib
  - frontend/src/types
  - docs
autonomous: true
requirements:
  - AUTHZ-ENGINE-01
must_haves:
  truths:
    - "Permission correctness needs automated coverage on both engine and handler boundaries."
    - "Developer documentation must explain Keycloak-vs-app-role separation and the permission matrix clearly."
    - "Live verification should prove both backend enforcement and frontend capability behavior in the same flow."
---

<objective>
Close Phase 44 with durable verification: backend permission tests, handler and capability regression coverage, thin frontend tests, developer documentation, and a live-use checklist that proves release/media/description flows behave correctly under the new engine.

Purpose: This phase changes security behavior. It should not be considered complete without explicit tests and docs that future contributors can trust.
</objective>

<context>
@.planning/phases/44-app-permission-engine-fuer-fansub-release-und-media-kontexte/44-CONTEXT.md
@.planning/phases/44-app-permission-engine-fuer-fansub-release-und-media-kontexte/44-RESEARCH.md
@.planning/phases/44-app-permission-engine-fuer-fansub-release-und-media-kontexte/44-01-PLAN.md
@.planning/phases/44-app-permission-engine-fuer-fansub-release-und-media-kontexte/44-02-PLAN.md
@.planning/phases/44-app-permission-engine-fuer-fansub-release-und-media-kontexte/44-03-PLAN.md
@docs/architecture/db-schema-fansub-domain.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the required permission-engine and handler test matrix</name>
  <read_first>
    - backend/internal/permissions
    - backend/internal/handlers
    - backend/internal/repository
  </read_first>
  <action>
    Add automated coverage for the required scenarios:
    - `platform_admin` darf alles
    - normaler User darf keine Admin-Aktion
    - `fansub_lead` darf eigene Gruppe bearbeiten
    - `fansub_lead` darf fremde Gruppe nicht bearbeiten
    - `designer` darf Release-Media hochladen
    - `designer` darf keine Mitglieder verwalten
    - `editor` darf Beschreibungen bearbeiten
    - disabled User darf nichts
    - `delete_own` erlaubt nur eigene Ressource
    - `release_version_id` loest die korrekte Fansub-Gruppenmenge auf
    - `release_media_id` loest den korrekten Release-/Release-Version-/Fansub-Kontext auf
    - manipulierte `fansub_group_id` im Request wird ignoriert oder abgelehnt
    - User mit Rolle in Gruppe A darf keine Ressource von Gruppe B aendern
    - Coop-Release erlaubt berechtigte User aus beteiligten Gruppen
    - Coop-Release verweigert User aus nicht beteiligten Gruppen
    - `delete_own` funktioniert nur mit echtem DB-Owner und nie ueber `modified_by_user_id`

    Also add route/capability regression tests for the highest-priority handlers.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./... && go build ./...</automated>
  </verify>
  <done>
    - The required matrix scenarios are automated.
    - Capability and handler enforcement regressions are covered.
    - Backend build and test pass with the new security layer.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add thin frontend regression coverage for capability behavior</name>
  <read_first>
    - touched frontend tests/helpers from Plan 44-03
  </read_first>
  <action>
    Add the thinnest useful frontend coverage for:
    - capability-based CTA visibility/disabled states
    - 403 error presentation on the touched screens
    - absence of hard role-name branching in newly touched helpers where testable
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck && npm run lint</automated>
  </verify>
  <done>
    - Capability-based UI affordances have regression coverage.
    - 403 UX paths are exercised at least once in tests where practical.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write developer documentation and complete live verification</name>
  <read_first>
    - .planning/phases/44-app-permission-engine-fuer-fansub-release-und-media-kontexte/44-CONTEXT.md
    - .planning/phases/44-app-permission-engine-fuer-fansub-release-und-media-kontexte/44-RESEARCH.md
    - docs/architecture/db-schema-fansub-domain.md
  </read_first>
  <action>
    Add concise developer documentation that explains:
    - Keycloak roles vs Team4s app roles
    - the permission context model
    - the static role matrix
    - example: `designer` may upload media but not manage members
    - example: `fansub_lead` may manage only the own group

    Then complete a live verification checklist that exercises:
    - same-group success
    - cross-group denial
    - capability-driven UI affordances
    - media upload/delete
    - description edit
    - audit output presence

    The final implementation write-up for the phase must start with a short Ist-Analyse section that summarizes:
    - relevant DB tables found
    - relevant columns found
    - auth/user middleware found
    - existing roles/admin logic found
    - existing API/frontend conventions found
    - which existing structures were reused
    - whether new migrations were needed or not
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./... && go build ./...</automated>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck && npm run lint</automated>
    <automated>cd C:/Users/admin/Documents/Team4s && git diff --check</automated>
  </verify>
  <done>
    - Developer docs explain the new permission system clearly and concretely.
    - Live verification proves both backend and frontend behavior on the key flows.
    - The phase closeout starts with the requested Ist-Analyse summary.
    - The phase has a clean handoff into a narrower Phase-45 slice.
  </done>
</task>

</tasks>

<verification>
Automated:
1. `cd C:/Users/admin/Documents/Team4s/backend && go test ./...`
2. `cd C:/Users/admin/Documents/Team4s/backend && go build ./...`
3. `cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck`
4. `cd C:/Users/admin/Documents/Team4s/frontend && npm run lint`
5. `cd C:/Users/admin/Documents/Team4s && git diff --check`

Live:
1. Log in as `platform_admin` and verify capability-positive access to the protected fansub/release/media flows.
2. Log in as a same-group role holder and verify allowed actions only within the correct group.
3. Log in as a cross-group or under-privileged user and verify 403 on protected API calls plus capability-false UI affordances.
4. Verify media upload/delete and description edit write audit entries.
5. Verify denied critical mutations can be traced via audit/log reason codes where the new seam supports it.
</verification>

<success_criteria>
- The permission engine is covered by backend tests for the required role/action scenarios.
- The permission engine is also covered for resolver truth, manipulated context, coop participation, and DB-backed ownership.
- Thin frontend regressions protect capability-based affordances and 403 UX.
- Documentation explains the architecture and examples clearly.
- Live verification proves same-group success, cross-group denial, and audit behavior.
</success_criteria>
