---
phase: 87-sichtbarkeits-steuerung-per-rolle-capability-pflege-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 87-01
files_modified:
  - backend/internal/repository/authz_capability_mutations.go
  - backend/internal/handlers/admin_capability_handler.go
  - backend/internal/handlers/admin_capability_handler_test.go
  - backend/cmd/server/admin_routes.go
  - backend/cmd/server/main.go
  - backend/internal/handlers/fansub_hist_group_members_handler.go
  - backend/internal/handlers/fansub_anime_contributions_handler.go
  - backend/internal/handlers/fansub_anime_contributions_unified_handler.go
  - backend/internal/handlers/fansub_view_enforcement_test.go
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-06
  - D-07
  - D-08

must_haves:
  truths:
    - "D-01/D-02: GET /admin/fansubs/:id/group-members prüft ActionFansubGroupMembersView — 403 ohne View-Right, 200 mit"
    - "D-01/D-02: GET /admin/fansubs/:id/unified-members prüft ActionFansubGroupMembersView — 403 ohne, 200 mit"
    - "D-01/D-02: GET /admin/fansubs/:id/anime-coverage prüft ActionFansubGroupMembersView — 403 ohne, 200 mit"
    - "Alle drei gegateten Endpunkte (group-members, unified-members, anime-coverage) sind Mitglieder-/Beitrags-Ansichten derselben Fansub-Gruppe und nutzen daher einheitlich ActionFansubGroupMembersView; keine separate Action nötig (behavior-/scope-konsistent)"
    - "D-03: platform_admin-Bypass bleibt: Actor mit IsPlatformAdmin=true passiert alle drei Endpunkte"
    - "D-06: PUT /admin/role-capabilities/:roleCode/:actionCode ruft ReloadCache nach DB-INSERT — Änderung wirkt ohne Deploy"
    - "D-06 Fail-safe: schlägt ReloadCache nach einer Mutation fehl, bleibt der zuvor geladene Cache unverändert gültig — kein fail-open/leerer Cache"
    - "D-07: DELETE /admin/role-capabilities/:roleCode/:actionCode → HTTP 409 wenn Action danach in keiner Rolle mehr — vor DB-Mutation geprüft"
    - "D-08: requirePlatformAdminIdentity als erste Aktion aller drei Capability-Endpunkte"
    - "D-06: Jede Capability-Mutation schreibt AuditLogEntry mit EventType role_capability.granted/revoked"
    - "isStandaloneAction nutzt permissions.IsStandaloneAction(actionCode) — kein Hardcode von standalone-Codes im Handler"
  artifacts:
    - path: "backend/internal/repository/authz_capability_mutations.go"
      provides: "ListCapabilityMatrix, GrantRoleCapability, RevokeRoleCapability, CountRolesWithAction"
      exports: ["ListCapabilityMatrix", "GrantRoleCapability", "RevokeRoleCapability", "CountRolesWithAction"]
    - path: "backend/internal/handlers/admin_capability_handler.go"
      provides: "AdminCapabilityHandler mit ListCapabilityMatrix, GrantCapability, RevokeCapability"
    - path: "backend/internal/handlers/admin_capability_handler_test.go"
      provides: "GREEN-Tests fuer Lockout-Guard, Platform-Admin-Gate, Audit"
    - path: "backend/internal/handlers/fansub_view_enforcement_test.go"
      provides: "GREEN-Tests fuer View-Enforcement an 3 Endpunkten (befüllt aus Wave-0-RED-Stubs in Plan 87-01)"
  key_links:
    - from: "admin_capability_handler.go RevokeCapability"
      to: "authz_capability_mutations.go CountRolesWithAction"
      via: "Lockout-Guard vor DB-DELETE — D-07"
      pattern: "CountRolesWithAction"
    - from: "admin_capability_handler.go GrantCapability"
      to: "permissionSvc.ReloadCache"
      via: "Cache-Reload nach DB-INSERT — D-06"
      pattern: "ReloadCache"
    - from: "admin_capability_handler.go isStandaloneAction"
      to: "permissions.IsStandaloneAction"
      via: "Exportierte API aus Plan 87-01 — kein Hardcode"
      pattern: "permissions\\.IsStandaloneAction"
    - from: "fansub_hist_group_members_handler.go"
      to: "permissionSvc.CanForFansubGroup"
      via: "View-Enforcement — D-01/D-02"
      pattern: "CanForFansubGroup.*ActionFansubGroupMembersView"
---

<objective>
Backend-Kern: Repository-Methoden für Capability-CRUD, Handler mit Lockout-Guard + Audit, View-Enforcement an 3 ungated Admin-Lese-Endpunkten, Routing und main.go-Verdrahtung.

Purpose: Alle D-01..D-08-Backend-Anforderungen implementieren. Nach diesem Plan können Capability-Mutationen über HTTP ausgeführt werden und wirken sofort via Cache-Reload (D-06). Drei Admin-Lese-Pfade prüfen per D-01/D-02 die View-Capability. Der Lockout-Guard nutzt permissions.IsStandaloneAction statt hartkodierter Strings.
Output: authz_capability_mutations.go + admin_capability_handler.go (mit grünen Tests) + View-Checks an 3 Handler-Dateien + grüne View-Enforcement-Tests
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/87-sichtbarkeits-steuerung-per-rolle-capability-pflege-ui/87-CONTEXT.md
@.planning/phases/87-sichtbarkeits-steuerung-per-rolle-capability-pflege-ui/87-RESEARCH.md
@.planning/phases/87-sichtbarkeits-steuerung-per-rolle-capability-pflege-ui/87-VALIDATION.md

<interfaces>
<!-- Aus Plan 87-01 (muss existieren) -->

ReloadCache (permissions.go):
```go
func (s *Service) ReloadCache(ctx context.Context, loader CacheLoader) error
```

IsStandaloneAction (permissions.go — NEU in Plan 87-01):
```go
func IsStandaloneAction(a Action) bool
```

CacheLoader-Interface:
```go
type CacheLoader interface {
    LoadRoleCapabilities(ctx context.Context) (map[string][]Action, error)
}
```

Bekannte Rollen-Konstanten (permissions.go):
RoleFansubLead, RoleProjectLead, RoleTranslator, RoleTimer, RoleTypesetter,
RoleEditor, RoleEncoder, RoleRawProvider, RoleQualityChecker, RoleDesigner

<!-- Aus backend/internal/handlers/admin_users_mutations_handler.go (Audit-Vorlage) -->

Audit-Write-Pattern:
```go
_ = h.auditLogRepo.Write(c.Request.Context(), repository.AuditLogEntry{
    ActorAppUserID: &identity.AppUserID,
    EventType:      "role_capability.granted",
    TargetType:     "role_capability",
    Action:         "grant_capability",
    Outcome:        "allowed",
    Payload:        map[string]any{"role_code": roleCode, "action_code": actionCode},
})
```

Platform-Admin-Gate-Vorlage:
```go
identity, ok := requirePlatformAdminIdentity(c, h.authzRepo, "")
if !ok {
    return
}
```

<!-- Aus backend/internal/handlers/admin_content_fansub_releases_handlers.go (CanFor*-Vorlage) -->

View-Check-Pattern:
```go
result, err := h.permissionSvc.CanForFansubGroup(
    c.Request.Context(), actor, permissions.ActionFansubGroupMembersView, fansubID,
)
if err != nil {
    writePermissionInternalError(c, err, "Berechtigung konnte nicht geprüft werden.")
    return
}
if !result.Allowed {
    writePermissionDenied(c, result)
    return
}
```

<!-- Aus backend/cmd/server/main.go (Zeilen 125-228 — Verdrahtungs-Kontext) -->

Verfügbare Variablen:
- authzRepo: *repository.AuthzRepository (implementiert CacheLoader)
- permissionSvc: *permissions.Service
- auditLogRepo: *repository.AuditLogRepository
- adminContentHandler mit WithPermissionDeps(permissionSvc, auditLogRepo) bereits verdrahtet

<!-- Aus backend/internal/repository/authz_permissions.go -->

AuthzRepository.LoadRoleCapabilities existiert bereits (CacheLoader-Implementierung).

<!-- OpenAPI-Response-Shape (aus Plan 87-01 admin-capabilities.yaml) -->

RoleCapabilityMatrix: { roles: RoleEntry[], all_actions: ActionEntry[] }
RoleEntry: { role_code, label_de, actions: RoleActionState[] }
RoleActionState: { code, label_de, category, granted, standalone }
ActionEntry: { code, label_de, category, sort_order }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repository authz_capability_mutations.go + Handler + Tests</name>
  <files>backend/internal/repository/authz_capability_mutations.go, backend/internal/handlers/admin_capability_handler.go, backend/internal/handlers/admin_capability_handler_test.go</files>
  <behavior>
    - ListCapabilityMatrix(ctx): JOIN action_definitions + role_capabilities + role_definitions; gibt Map[roleCode][]ActionState zurück; liefert alle Rollen (auch ohne Eintraege) und alle Actions (mit granted=false wenn kein Eintrag); standalone-Actions haben standalone=true, granted=false
    - GrantRoleCapability(ctx, roleCode, actionCode): INSERT INTO role_capabilities(role_code, action_code) ON CONFLICT DO NOTHING; gibt Fehler bei ungültigem FK
    - RevokeRoleCapability(ctx, roleCode, actionCode): DELETE FROM role_capabilities WHERE role_code=$1 AND action_code=$2
    - CountRolesWithAction(ctx, actionCode): SELECT COUNT(DISTINCT role_code) FROM role_capabilities WHERE action_code=$1; gibt int64
    - AdminCapabilityHandler: Felder authzRepo *repository.AuthzRepository, permissionSvc *permissions.Service, auditLogRepo *repository.AuditLogRepository
    - ListCapabilityMatrix-Handler: GET, requirePlatformAdminIdentity, ruft repo.ListCapabilityMatrix, serialisiert zu RoleCapabilityMatrix-JSON
    - GrantCapability-Handler: PUT, requirePlatformAdminIdentity, roleCode+actionCode aus Params, GrantRoleCapability, danach permissionSvc.ReloadCache(ctx, h.authzRepo), Audit-Write, 200
    - RevokeCapability-Handler: DELETE, requirePlatformAdminIdentity, Lockout-Guard (CountRolesWithAction <= 1 UND NOT permissions.IsStandaloneAction(Action(actionCode)) → HTTP 409 mit deutscher Meldung), RevokeRoleCapability, danach permissionSvc.ReloadCache, Audit-Write, 200
    - TestGrantCapabilityRequiresPlatformAdmin: Mock ohne Platform-Admin → 403
    - TestRevokeCapabilityLastActionGuard: CountRolesWithAction=1, permissions.IsStandaloneAction=false → 409 mit "lockout_guard"-Code
    - TestCapabilityAuditOnGrant: Mock-AuditLogRepo prüft ob Write mit EventType "role_capability.granted" aufgerufen wurde
  </behavior>
  <action>
    Erstelle backend/internal/repository/authz_capability_mutations.go als neue Datei im repository-Package. Empfänger ist *AuthzRepository. Methoden:

    ListCapabilityMatrix(ctx context.Context) — verwendet 3-Wege-JOIN: SELECT ad.code, ad.label_de, ad.category, ad.sort_order, rd.code AS role_code, rd.label_de AS role_label, rc.action_code IS NOT NULL AS granted, CASE WHEN ad.code = ANY($1) THEN true ELSE false END AS standalone FROM action_definitions ad CROSS JOIN role_definitions rd LEFT JOIN role_capabilities rc ON rc.action_code = ad.code AND rc.role_code = rd.code ORDER BY rd.code, ad.sort_order. Übergebe standaloneActionCodes als $1-Parameter (String-Slice der standalone-Actions, aus permissions.standaloneActions nicht verfügbar — hardcode den einen Wert "fansub_group.invitations.accept" als repository-interner Parameter, da das Repository nicht vom permissions-Paket abhängen soll). Baue daraus die Go-DTO-Struktur auf (zwei Struct-Typen: CapabilityMatrixRoleRow und CapabilityMatrixActionEntry). Gib eine Go-Struct zurück die dem RoleCapabilityMatrix-JSON-Contract entspricht (roles-Array + all_actions-Array).

    GrantRoleCapability(ctx context.Context, roleCode, actionCode string) error — INSERT INTO role_capabilities(role_code, action_code) ON CONFLICT DO NOTHING. Validierung: beide Parameter nicht leer.

    RevokeRoleCapability(ctx context.Context, roleCode, actionCode string) error — DELETE FROM role_capabilities WHERE role_code=$1 AND action_code=$2.

    CountRolesWithAction(ctx context.Context, actionCode string) (int64, error) — SELECT COUNT(DISTINCT role_code) FROM role_capabilities WHERE action_code=$1.

    Erstelle backend/internal/handlers/admin_capability_handler.go — neues Handler-File. Struct AdminCapabilityHandler mit Feldern. Konstruktor NewAdminCapabilityHandler(authzRepo, permissionSvc, auditLogRepo). Drei Methoden (ListCapabilityMatrix, GrantCapability, RevokeCapability) wie in Behavior beschrieben.

    Lockout-Guard im RevokeCapability-Handler: Rufe permissions.IsStandaloneAction(permissions.Action(actionCode)) auf. Wenn CountRolesWithAction <= 1 UND NOT permissions.IsStandaloneAction → HTTP 409. Kein Hardcode-String und kein "sync with"-Kommentar — IsStandaloneAction ist der einzige Wahrheits-Ort (per D-01 in Plan 87-01).

    Fehler aus ReloadCache werden geloggt (log.Printf) und NICHT als HTTP-Fehler zurückgegeben — Mutation war erfolgreich, Cache-Reload-Fehlschlag ist nicht fatal. Der bestehende Cache bleibt gültig (Fail-safe per TestReloadCacheFailsafe).

    In admin_capability_handler_test.go: Package handlers_test. Befülle die drei RED-Stub-Tests aus Plan 87-01 mit echtem Inhalt. Nutze Struct-Literal-Test-Doubles (kein Interface-Mock-Framework). TestGrantCapabilityRequiresPlatformAdmin: erstelle Handler mit authzRepo-Stub der requirePlatformAdminIdentity scheitern lässt (kein Platform-Admin). TestRevokeCapabilityLastActionGuard: stub-CountRolesWithAction gibt 1 zurück, permissions.IsStandaloneAction("release.view")=false → erwarte HTTP 409 und body.error.code = "lockout_guard". TestCapabilityAuditOnGrant: prüfe AuditLogEntry-Felder via capturenden Audit-Stub.

    Achtung 450-Zeilen-Limit: Wenn admin_capability_handler.go > 450 Zeilen wird, Hilfsfunktionen in admin_capability_handler_helpers.go auslagern.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/permissions/... ./internal/repository/... ./internal/handlers/... -run "Capability|Reload|LastAction" -count=1 -v 2>&1 | tail -30</automated>
  </verify>
  <done>go build ./... fehlerfrei; alle drei Handler-Tests grün; ListCapabilityMatrix, GrantRoleCapability, RevokeRoleCapability, CountRolesWithAction compilieren ohne Fehler; kein Hardcode standalone-String im Handler (grep -n "invitations.accept" backend/internal/handlers/admin_capability_handler.go → kein Treffer)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: View-Enforcement an 3 Admin-Endpunkten + Routing + main.go-Verdrahtung</name>
  <files>backend/internal/handlers/fansub_hist_group_members_handler.go, backend/internal/handlers/fansub_anime_contributions_handler.go, backend/internal/handlers/fansub_anime_contributions_unified_handler.go, backend/cmd/server/admin_routes.go, backend/cmd/server/main.go, backend/internal/handlers/fansub_view_enforcement_test.go</files>
  <behavior>
    - TestViewCapabilityEnforcementGroupMembers (GREEN): Actor ohne ActionFansubGroupMembersView → 403; Actor mit ActionFansubGroupMembersView → 200
    - TestViewCapabilityEnforcementUnifiedMembers (GREEN): analog, anderer Endpunkt
    - TestViewCapabilityEnforcementAnimeCoverage (GREEN): analog, dritter Endpunkt
    - Alle drei Endpunkte: CanForFansubGroup(ActionFansubGroupMembersView) nach requireAdmin-Check
    - platform_admin (IsPlatformAdmin=true) passiert alle drei Checks (D-03)
  </behavior>
  <action>
    Per D-01/D-02: View-Capability-Check an drei heute nur-requireAdmin-gesicherten Admin-Endpunkten. Alle drei Endpunkte haben bereits authMiddleware + requireAdmin — der neue Check kommt danach.

    In fansub_hist_group_members_handler.go (GET /admin/fansubs/:id/group-members): Ergänze nach dem bestehenden requireAdmin-Check einen Aufruf von h.permissionSvc.CanForFansubGroup(c.Request.Context(), actor, permissions.ActionFansubGroupMembersView, fansubID). actor muss aus dem Gin-Context gelesen werden (analog zu bestehenden Handlern: getCurrentActor-Helper oder direkt aus c.Get("current_user")). Wenn !result.Allowed → writePermissionDenied(c, result); return. Per D-03: platform_admin-Actors passieren den Check (IsPlatformAdmin=true → result.Allowed=true in CanForFansubGroup).

    Analog in fansub_anime_contributions_handler.go (GET /admin/fansubs/:id/anime-coverage) und fansub_anime_contributions_unified_handler.go (GET /admin/fansubs/:id/unified-members).

    Alle drei Endpunkte nutzen einheitlich ActionFansubGroupMembersView — keine separate Action pro Endpunkt nötig, da alle drei Mitglieder-/Beitrags-Ansichten derselben Fansub-Gruppe sind (behavior-/scope-konsistent per must_haves.truths).

    Prüfe in jeder der drei Dateien ob permissionSvc bereits als Feld vorhanden ist. Falls nicht, ergänze das Feld und einen WithPermissionDeps-ähnlichen Setter oder erweitere den Konstruktor. Verwende als Vorlage die bestehende WithPermissionDeps-Methode in admin_content_handler.go.

    In backend/cmd/server/admin_routes.go (oder main.go, je nach Projektstruktur): Registriere die drei Capability-Routen:
    - GET /api/v1/admin/role-capabilities → adminCapabilityHandler.ListCapabilityMatrix
    - PUT /api/v1/admin/role-capabilities/:roleCode/:actionCode → adminCapabilityHandler.GrantCapability
    - DELETE /api/v1/admin/role-capabilities/:roleCode/:actionCode → adminCapabilityHandler.RevokeCapability
    Alle drei mit authMiddleware (kein separater Gate — die Handler-Methoden rufen requirePlatformAdminIdentity selbst auf).

    In main.go: Erstelle adminCapabilityHandler := handlers.NewAdminCapabilityHandler(authzRepo, permissionSvc, auditLogRepo) und rufe die drei View-Enforcement-Setter auf den drei Fansub-Handlern auf (sofern nötig). permissionSvc und auditLogRepo sind bereits ab Zeile 127 bzw. 130 in main.go verfügbar.

    Befülle fansub_view_enforcement_test.go: Ersetze die drei RED-Stubs aus Plan 87-01 mit echten Tests. Nutze Struct-Literal-Test-Doubles. Pro Test: (a) httptest.NewRecorder, (b) Actor ohne ActionFansubGroupMembersView → 403, (c) Actor mit → 200.

    Wichtig: Überprüfe vor dem Schreiben die Zeilenzahl jeder der drei Handler-Dateien. Wenn eine davon durch den neuen Check die 450-Zeilen-Grenze überschreitet, extrahiere eine Hilfsfunktion applyFansubViewGate(c, permissionSvc, fansubID) in eine gemeinsame Hilfsdatei.
  </action>
  <verify>
    <automated>cd backend && go build ./... && go test ./internal/handlers/... -run "ViewCapabilityEnforcement|ViewEnforcement|CanForFansub" -count=1 -v 2>&1 | tail -20</automated>
  </verify>
  <done>go build ./... grün; alle drei View-Enforcement-Tests grün; drei neue Routen registriert; alle drei Fansub-Handler-Dateien compilieren mit permissionSvc-Feld; grep -r "CanForFansubGroup" auf die drei Handler-Dateien liefert Treffer</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → PUT/DELETE /admin/role-capabilities | Nur Platform-Admin; requirePlatformAdminIdentity als erste Handler-Aktion |
| Handler → DB | roleCode/actionCode nur via parameterisierte Queries; DB-FK wirft Fehler bei ungültigem Code |
| Revoke → Cache-Reload | Reload erst nach erfolgreicher DB-Mutation; D-10-Check garantiert Konsistenz; Fail-safe: Fehler im Reload lässt alten Cache intakt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-03 | Elevation of Privilege | View-Check fehlt/falsch an Lese-Pfad | mitigate | CanForFansubGroup(ActionFansubGroupMembersView) an allen 3 neuen Gates; platform_admin-Bypass über IsPlatformAdmin=true in Actor |
| T-87-04 | Tampering | Lockout via letzten-Action-Entzug → Reload-Crash | mitigate | CountRolesWithAction-Guard VOR DB-DELETE; permissions.IsStandaloneAction-Check; HTTP 409 statt Crash |
| T-87-05 | Repudiation | Keine Audit-Attribution bei Capability-Mutation | mitigate | AuditLogRepo.Write nach jeder erfolgreichen Mutation (Grant/Revoke) mit ActorAppUserID + Payload |
| T-87-06 | Elevation of Privilege | Fehlender Platform-Admin-Gate auf Mutations-Endpunkten | mitigate | requirePlatformAdminIdentity als erste Aktion in GrantCapability und RevokeCapability |
| T-87-07 | Tampering | SQL-Injection über roleCode/actionCode | accept | Parametrisierte pgx-Queries; DB-FK wirft Fehler bei unbekanntem Code — kein Injection-Risiko |
| T-87-08 | Information Disclosure | IDOR: Lese-Endpunkt gibt Capabilities fremder Rollen | accept | GET /admin/role-capabilities: Platform-Admin sieht alles; kein Scope-Problem |
| T-87-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen externen Pakete |
</threat_model>

<verification>
Nach Plan 02 muss gelten:
1. `go build ./...` → fehlerfrei
2. `go test ./internal/handlers/... -run "Capability|LastAction|Audit|ViewCapabilityEnforcement" -count=1` → alle PASS
3. `grep -n "permissions.IsStandaloneAction" backend/internal/handlers/admin_capability_handler.go` → Treffer (kein Hardcode-String)
4. `grep -r "CanForFansubGroup" backend/internal/handlers/fansub_hist_group_members_handler.go` → Treffer (View-Check vorhanden)
5. `grep -r "CountRolesWithAction" backend/internal/handlers/admin_capability_handler.go` → Treffer (Lockout-Guard vorhanden)
</verification>

<success_criteria>
- Alle Handler-Tests grün (TestGrantCapabilityRequiresPlatformAdmin, TestRevokeCapabilityLastActionGuard, TestCapabilityAuditOnGrant)
- Alle View-Enforcement-Tests grün (TestViewCapabilityEnforcementGroupMembers, TestViewCapabilityEnforcementUnifiedMembers, TestViewCapabilityEnforcementAnimeCoverage)
- Drei Admin-Lese-Endpunkte haben View-Check via CanForFansubGroup(ActionFansubGroupMembersView)
- GrantCapability und RevokeCapability rufen ReloadCache nach erfolgreicher DB-Mutation auf
- RevokeCapability gibt HTTP 409 zurück wenn CountRolesWithAction <= 1 und permissions.IsStandaloneAction=false
- Alle Capabilities-Endpunkte sind in Routing registriert
- Kein Hardcode von standalone-Action-Strings im Handler
</success_criteria>

<output>
Erstelle `.planning/phases/87-sichtbarkeits-steuerung-per-rolle-capability-pflege-ui/87-02-SUMMARY.md` wenn fertig.
</output>
