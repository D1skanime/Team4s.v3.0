---
phase: 87-sichtbarkeits-steuerung-per-rolle-capability-pflege-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/permissions/permissions.go
  - backend/internal/permissions/permissions_reload_test.go
  - backend/internal/permissions/permissions_standalone_test.go
  - backend/internal/handlers/admin_capability_handler_test.go
  - backend/internal/handlers/fansub_view_enforcement_test.go
  - frontend/src/app/admin/role-capabilities/RoleCapabilityClient.test.tsx
  - shared/contracts/admin-capabilities.yaml
  - frontend/src/types/admin-capability.ts
autonomous: true
requirements:
  - D-06
  - D-07
  - D-08

must_haves:
  truths:
    - "D-06: ReloadCache-Funktion existiert in permissions.go und delegiert an LoadCache"
    - "D-06 Fail-safe: schlägt ReloadCache nach einer Mutation fehl, bleibt der zuvor geladene Cache unverändert gültig (LoadCache/ReloadCache setzt loadedCache erst NACH bestandenem D-10-Check) — kein fail-open/leerer Cache"
    - "D-07: TestLastActionGuard (RED) schlägt mit 'not implemented' fehl — bestätigt Lockout-Guard-Anforderung"
    - "D-08: OpenAPI-Contract admin-capabilities.yaml definiert GET/PUT/DELETE /api/v1/admin/role-capabilities"
    - "D-08: TypeScript-Typen RoleCapabilityMatrix + RoleEntry + ActionEntry exportiert in admin-capability.ts"
    - "IsStandaloneAction(a Action) bool ist als exportierte Funktion in permissions.go vorhanden und prüft gegen standaloneActions"
    - "TestViewCapabilityEnforcementGroupMembers, TestViewCapabilityEnforcementUnifiedMembers, TestViewCapabilityEnforcementAnimeCoverage sind als RED-Stubs in fansub_view_enforcement_test.go angelegt"
    - "TestReloadCacheFailsafe (RED) prüft dass der alte Cache bei Loader-Fehler erhalten bleibt"
    - "Wave-0-Tests sind RED (kompilieren, scheitern mit 'not implemented') — kein Produktionscode geändert"
  artifacts:
    - path: "backend/internal/permissions/permissions.go"
      provides: "ReloadCache-Funktion (delegiert an LoadCache) + IsStandaloneAction (exportiert)"
      contains: "func (s *Service) ReloadCache"
    - path: "backend/internal/permissions/permissions_reload_test.go"
      provides: "Wave-0 RED-Tests für ReloadCache + Fail-safe"
      exports: ["TestReloadCacheReplacesCacheAtomically", "TestReloadCacheFailsafe"]
    - path: "backend/internal/permissions/permissions_standalone_test.go"
      provides: "Wave-0 RED-Test für IsStandaloneAction"
      exports: ["TestIsStandaloneAction"]
    - path: "backend/internal/handlers/admin_capability_handler_test.go"
      provides: "Wave-0 RED-Tests für Handler-Verhalten"
      exports: ["TestGrantCapabilityRequiresPlatformAdmin", "TestRevokeCapabilityLastActionGuard", "TestCapabilityAuditOnGrant"]
    - path: "backend/internal/handlers/fansub_view_enforcement_test.go"
      provides: "Wave-0 RED-Tests für View-Enforcement an 3 Endpunkten"
      exports: ["TestViewCapabilityEnforcementGroupMembers", "TestViewCapabilityEnforcementUnifiedMembers", "TestViewCapabilityEnforcementAnimeCoverage"]
    - path: "frontend/src/app/admin/role-capabilities/RoleCapabilityClient.test.tsx"
      provides: "Wave-0 RED-Test für Pflege-UI-Komponente"
    - path: "shared/contracts/admin-capabilities.yaml"
      provides: "OpenAPI-Contract für alle 3 Capability-Endpunkte"
    - path: "frontend/src/types/admin-capability.ts"
      provides: "TypeScript-Typen für Capability-Matrix-API"
  key_links:
    - from: "permissions.go ReloadCache"
      to: "LoadCache"
      via: "Delegation — selbe Logik, selber D-10-Check; loadedCache wird erst NACH Check gesetzt"
      pattern: "func.*ReloadCache.*LoadCache"
    - from: "permissions.go IsStandaloneAction"
      to: "standaloneActions"
      via: "Exportierte API über paket-private Slice — kein Hardcode in Consumern"
      pattern: "func IsStandaloneAction.*standaloneActions"
    - from: "admin-capabilities.yaml"
      to: "frontend/src/types/admin-capability.ts"
      via: "OpenAPI-Contract → TypeScript-Typen (D-08)"
      pattern: "RoleCapabilityMatrix"
---

<objective>
Wave-0-Fundament: ReloadCache-Funktion nachrüsten, IsStandaloneAction exportieren, RED-Tests als Sicherheitsnetz anlegen, OpenAPI-Contract + TypeScript-Typen definieren — bevor ein einziger Handler-Byte Produktionscode berührt.

Purpose: D-06 (Cache-Reload ohne Deploy) benötigt eine ReloadCache-API. IsStandaloneAction verhindert, dass Consumer (Handler) standalone-Codes hardcoden müssen. Alle übrigen Tasks (Handler, Repository, UI) schreiben gegen das in diesem Plan fixierte Interface. Wave-0-RED-Tests belegen, dass das verlangte Verhalten noch nicht implementiert ist und verhindern False-Positives.
Output: permissions.go + 4 RED-Testdateien + OpenAPI-YAML + TypeScript-Typen
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-sichtbarkeits-steuerung-per-rolle-capability-pflege-ui/87-CONTEXT.md
@.planning/phases/87-sichtbarkeits-steuerung-per-rolle-capability-pflege-ui/87-RESEARCH.md
@.planning/phases/87-sichtbarkeits-steuerung-per-rolle-capability-pflege-ui/87-UI-SPEC.md
@.planning/phases/87-sichtbarkeits-steuerung-per-rolle-capability-pflege-ui/87-VALIDATION.md

<interfaces>
<!-- Aus backend/internal/permissions/permissions.go (Phase 86 — verifiziert) -->

Bestehende Service-Methode (unveraendert lassen):
```go
func (s *Service) LoadCache(ctx context.Context, loader CacheLoader) error
```

CacheLoader-Interface (bereits implementiert von AuthzRepository):
```go
type CacheLoader interface {
    LoadRoleCapabilities(ctx context.Context) (map[string][]Action, error)
}
```

Paket-globale Cache-Vars:
```go
var (
    cacheMu     sync.RWMutex
    loadedCache map[string][]Action
)
```

standaloneActions (unveraendert — paket-private, NUR über IsStandaloneAction zugreifen):
```go
var standaloneActions = []Action{ActionFansubGroupInvitationsAccept}
```

Bekannte Actions (alle 18, in allKnownActions registriert):
ActionFansubGroupEdit, ActionFansubGroupLinksManage, ActionFansubGroupMembersView,
ActionFansubGroupMembersManage, ActionFansubGroupInvitationsView,
ActionFansubGroupInvitationsCreate, ActionFansubGroupInvitationsCancel,
ActionFansubGroupInvitationsAccept, ActionFansubGroupNotesWrite,
ActionAnimeFansubProjectNotesWrite, ActionReleaseView, ActionReleaseVersionView,
ActionReleaseVersionMediaView, ActionReleaseVersionMediaUpload,
ActionReleaseVersionMediaUpdate, ActionReleaseVersionMediaDelete,
ActionReleaseVersionMediaDeleteOwn, ActionReleaseVersionNotesWrite
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ReloadCache + IsStandaloneAction in permissions.go + Wave-0-Backend-RED-Tests</name>
  <files>backend/internal/permissions/permissions.go, backend/internal/permissions/permissions_reload_test.go, backend/internal/permissions/permissions_standalone_test.go, backend/internal/handlers/admin_capability_handler_test.go, backend/internal/handlers/fansub_view_enforcement_test.go</files>
  <behavior>
    - ReloadCache(ctx, loader): delegiert an LoadCache — selber D-10-Check, selbes atomares Swap-Verhalten; gibt Fehler zurück (NICHT fatal) damit Handler HTTP 409 zurückgeben kann statt zu crashen
    - IsStandaloneAction(a Action) bool: exportierte Paket-Funktion (kein Methoden-Empfänger), prüft ob a in standaloneActions enthalten ist via slices.Contains; kein neues Feld, kein neues Struct
    - TestReloadCacheReplacesCacheAtomically (RED→GREEN): ruft ReloadCache mit einem Stub-Loader auf, prüft dass AllowedActionsForRole die neuen Werte zurückgibt — grün sobald ReloadCache implementiert
    - TestReloadCacheFailsafe (RED→GREEN): ruft erst LoadCache mit gültigem Stub, dann ReloadCache mit fehlendem Loader auf, prüft dass AllowedActionsForRole nach dem fehlgeschlagenen Reload immer noch den alten Cache zurückgibt — beweist D-06-Fail-safe (kein fail-open, kein leerer Cache)
    - TestIsStandaloneAction (RED→GREEN): prüft ActionFansubGroupInvitationsAccept → true; ActionReleaseView → false
    - TestGrantCapabilityRequiresPlatformAdmin (RED): schlägt mit "not implemented" fehl — in Plan 87-02 befüllt
    - TestRevokeCapabilityLastActionGuard (RED): schlägt mit "not implemented" fehl
    - TestCapabilityAuditOnGrant (RED): schlägt mit "not implemented" fehl
    - TestViewCapabilityEnforcementGroupMembers (RED): schlägt mit "not implemented" fehl
    - TestViewCapabilityEnforcementUnifiedMembers (RED): schlägt mit "not implemented" fehl
    - TestViewCapabilityEnforcementAnimeCoverage (RED): schlägt mit "not implemented" fehl
  </behavior>
  <action>
    In backend/internal/permissions/permissions.go zwei Ergänzungen nach der LoadCache-Funktion:

    1. ReloadCache hat die Signatur `func (s *Service) ReloadCache(ctx context.Context, loader CacheLoader) error` und gibt direkt `return s.LoadCache(ctx, loader)` zurück. Kein eigener Mutex, kein eigener D-10-Check — alles bereits in LoadCache vorhanden. Kommentar: "ReloadCache lädt die Capability-Matrix erneut aus der DB (D-06). Delegiert an LoadCache — selber atomarer Swap und D-10-Konsistenz-Check. Nach erfolgreicher Mutation aufrufen, nicht beim Startup (dafür LoadCache). Fail-safe: schlägt LoadCache fehl, bleibt loadedCache unverändert."

    2. IsStandaloneAction als exportierte Paket-Funktion (kein Empfänger): `func IsStandaloneAction(a Action) bool { return slices.Contains(standaloneActions, a) }`. Kommentar: "IsStandaloneAction meldet ob eine Action in standaloneActions deklariert ist. Exportierte API damit Handler die paket-private Slice nicht duplizieren müssen."

    In backend/internal/permissions/permissions_reload_test.go: Package permissions_test. Zwei Tests:

    TestReloadCacheReplacesCacheAtomically erstellt einen mockCacheLoader mit LoadRoleCapabilities-Stub der eine Map zurückgibt mit "fansub_lead" → allen 18 allKnownActions als Mindestdeckung (damit D-10-Check besteht). Nach ReloadCache prüft der Test via AllowedActionsForRole("fansub_lead") dass der Cache aktualisiert wurde.

    TestReloadCacheFailsafe: Schritt 1 — LoadCache mit gültigem Stub aufrufen (selbe 18-Action-Abdeckung). Schritt 2 — ReloadCache mit einem Stub aufrufen, dessen LoadRoleCapabilities einen Fehler zurückgibt (z.B. errors.New("db down")). Schritt 3 — AllowedActionsForRole("fansub_lead") aufrufen und prüfen, dass der Slice aus Schritt 1 unverändert vorhanden ist (len > 0). Dieser Test beweist den Fail-safe: ein fehlgeschlagener Reload überschreibt nie den gültigen Cache.

    In backend/internal/permissions/permissions_standalone_test.go: Package permissions_test. TestIsStandaloneAction: prüft IsStandaloneAction(ActionFansubGroupInvitationsAccept) == true und IsStandaloneAction(ActionReleaseView) == false.

    In backend/internal/handlers/admin_capability_handler_test.go: Package handlers_test. Drei Stub-Tests mit `t.Fatalf("not implemented — Plan 87-02")` — sie kompilieren, schlagen fehl. TestGrantCapabilityRequiresPlatformAdmin, TestRevokeCapabilityLastActionGuard, TestCapabilityAuditOnGrant. Diese Tests werden in Plan 87-02 mit echtem Inhalt befüllt; hier nur als rote Anker.

    In backend/internal/handlers/fansub_view_enforcement_test.go: Package handlers_test. Drei Stub-Tests mit `t.Fatalf("not implemented — Plan 87-02")`: TestViewCapabilityEnforcementGroupMembers, TestViewCapabilityEnforcementUnifiedMembers, TestViewCapabilityEnforcementAnimeCoverage. Diese Tests werden in Plan 87-02 befüllt wenn die View-Enforcement-Logik an den drei Endpunkten implementiert wird.

    Der mockCacheLoader in permissions_reload_test.go muss das CacheLoader-Interface erfüllen: eine Struct mit LoadRoleCapabilities-Methode. Für TestReloadCacheFailsafe: einen zweiten errorCacheLoader-Stub der immer einen Fehler zurückgibt.

    KEINE Änderungen an LoadCache oder anderen existierenden Funktionen in permissions.go.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/permissions/... -run "TestReloadCache|TestIsStandaloneAction" -count=1 -v && go test ./internal/handlers/... -run "TestGrantCapabilityRequiresPlatformAdmin|TestRevokeCapabilityLastActionGuard|TestCapabilityAuditOnGrant|TestViewCapabilityEnforcement" -count=1 2>&1 | grep -E "PASS|FAIL|not implemented"</automated>
  </verify>
  <done>ReloadCache-Funktion und IsStandaloneAction in permissions.go vorhanden; TestReloadCacheReplacesCacheAtomically + TestReloadCacheFailsafe + TestIsStandaloneAction grün; die sechs Handler-Stub-Tests schlagen fehl mit "not implemented" (das ist korrekt für Wave-0-RED)</done>
</task>

<task type="auto">
  <name>Task 2: OpenAPI-Contract + TypeScript-Typen</name>
  <files>shared/contracts/admin-capabilities.yaml, frontend/src/types/admin-capability.ts, frontend/src/app/admin/role-capabilities/RoleCapabilityClient.test.tsx</files>
  <action>
    Erstelle shared/contracts/admin-capabilities.yaml als eigenständiges OpenAPI-3.0.3-Dokument (per D-08). Es definiert drei Pfade:

    GET /api/v1/admin/role-capabilities: Gibt RoleCapabilityMatrix zurück. Response-Schema "RoleCapabilityMatrix" mit "roles" (Array von RoleEntry) und "all_actions" (Array von ActionEntry). RoleEntry: "role_code" (string), "label_de" (string), "actions" (Array von RoleActionState). RoleActionState: "code" (string), "label_de" (string), "category" (string), "granted" (boolean), "standalone" (boolean). ActionEntry: "code" (string), "label_de" (string), "category" (string), "sort_order" (integer).

    PUT /api/v1/admin/role-capabilities/{roleCode}/{actionCode}: Kein Request-Body. Response 200 mit "message" (string). Responses 401, 403, 404, 409 (Lockout-Guard), 500 dokumentiert.

    DELETE /api/v1/admin/role-capabilities/{roleCode}/{actionCode}: Kein Request-Body. Response 200 mit "message" (string). Response 409: "error.message" + "error.code" = "lockout_guard". Responses 401, 403, 404, 500 dokumentiert.

    Alle drei Endpunkte: security requirePlatformAdmin (Bearer), description auf Deutsch.

    Erstelle frontend/src/types/admin-capability.ts: Exportiere folgende TypeScript-Interfaces spiegelbildlich zum OpenAPI-Schema:
    - RoleActionState: { code: string; label_de: string; category: string; granted: boolean; standalone: boolean }
    - RoleEntry: { role_code: string; label_de: string; actions: RoleActionState[] }
    - ActionEntry: { code: string; label_de: string; category: string; sort_order: number }
    - RoleCapabilityMatrix: { roles: RoleEntry[]; all_actions: ActionEntry[] }

    Erstelle frontend/src/app/admin/role-capabilities/RoleCapabilityClient.test.tsx: Wave-0-RED-Test. Importiere RoleCapabilityClient (noch nicht existent). Der Test prüft: (1) Render mit leerem Matrix-Mock zeigt LoadingState, (2) nach resolvedData zeigt eine Tabelle mit Rollen. Beide Tests sollen zunächst mit import-Fehler scheitern (Datei existiert noch nicht) — das ist korrekt für Wave-0. Nutze @vitest-environment jsdom Direktive.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -i "admin-capability" | head -5; echo "--- yaml check ---"; python3 -c "import sys; data=open('$(pwd)/../shared/contracts/admin-capabilities.yaml').read(); print('yaml ok, len='+str(len(data)))" 2>/dev/null || node -e "const fs=require('fs'); const d=fs.readFileSync('../shared/contracts/admin-capabilities.yaml','utf8'); console.log('yaml ok, len='+d.length);"</automated>
  </verify>
  <done>admin-capabilities.yaml enthält alle 3 Endpunkte (GET/PUT/DELETE) mit RoleCapabilityMatrix-Schema; frontend/src/types/admin-capability.ts exportiert 4 Interfaces; RoleCapabilityClient.test.tsx existiert als RED-Test (schlägt mit Modul-nicht-gefunden fehl)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | Alle neuen Endpunkte erfordern gültigen Platform-Admin-Bearer-Token |
| API → permissions.Service | ReloadCache darf nur nach erfolgreicher DB-Mutation aufgerufen werden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01 | Elevation of Privilege | ReloadCache nach fehlgeschlagener Mutation | mitigate | ReloadCache wird nur aufgerufen wenn DB-Mutation erfolgreich — Handler-Reihenfolge: Guard → DB-Write → ReloadCache |
| T-87-02 | Tampering | D-10-Check-Bypass via ReloadCache | accept | ReloadCache delegiert an LoadCache inkl. vollem D-10-Check; kein Bypass möglich |
| T-87-02b | Tampering | Fail-open bei Reload-Fehler (leerer Cache) | mitigate | LoadCache setzt loadedCache erst nach bestandenem D-10-Check — fehlgeschlagener Reload lässt alten Cache intakt (TestReloadCacheFailsafe belegt dieses Verhalten) |
| T-87-03 | Tampering | Hardcode standalone-Codes in Consumern (drift-Risiko) | mitigate | IsStandaloneAction als exportierte API — ein einziger Wahrheits-Ort in standaloneActions |
| T-87-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen externen Pakete in Plan 01 |
</threat_model>

<verification>
Nach Plan 01 muss gelten:
1. `go test ./internal/permissions/... -run "TestReloadCache|TestIsStandaloneAction" -count=1` → alle PASS
2. `go test ./internal/handlers/... -run "TestGrantCapability|TestRevokeCapability|TestCapabilityAudit|TestViewCapabilityEnforcement" -count=1` → alle FAIL mit "not implemented" (Wave-0-RED bestätigt)
3. shared/contracts/admin-capabilities.yaml vorhanden mit GET/PUT/DELETE-Pfaden
4. frontend/src/types/admin-capability.ts exportiert RoleCapabilityMatrix, RoleEntry, ActionEntry, RoleActionState
5. `go build ./...` → erfolgreich (keine Kompilierfehler)
6. IsStandaloneAction in permissions.go: `grep -n "func IsStandaloneAction" backend/internal/permissions/permissions.go` → Treffer
</verification>

<success_criteria>
- ReloadCache-Funktion in permissions.go compiliert fehlerfrei und delegiert an LoadCache
- IsStandaloneAction als exportierte Funktion vorhanden (kein Hardcode standalone-Codes in Consumern nötig)
- TestReloadCacheFailsafe belegt: fehlgeschlagener Reload erhält alten Cache — kein fail-open
- Wave-0-RED-Tests für Handler und View-Enforcement schlagen mit "not implemented" fehl (erwartetes Verhalten)
- OpenAPI-Contract admin-capabilities.yaml ist strukturell valide (3 Pfade, RoleCapabilityMatrix-Schema)
- TypeScript-Interfaces kompilieren fehlerfrei (tsc --noEmit)
</success_criteria>

<output>
Erstelle `.planning/phases/87-sichtbarkeits-steuerung-per-rolle-capability-pflege-ui/87-01-SUMMARY.md` wenn fertig.
</output>
