---
phase: 68-badge-engine-archiv-entdeckung
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/handlers/fansub_group_history_handler.go
  - backend/cmd/server/admin_routes.go
  - frontend/src/components/groups/GroupHistorySection.tsx
  - frontend/src/components/groups/groups.module.css
  - frontend/src/app/admin/my-groups/[id]/page.tsx
  - frontend/src/lib/api.ts
autonomous: true
requirements:
  - P68-SC2

must_haves:
  truths:
    - "Leader kann einen Gruppen-Meilenstein anlegen (POST), bearbeiten (PATCH) und löschen (DELETE)."
    - "Nicht-Leader erhalten 403 bei Create/Update/Delete."
    - "Meilensteine erscheinen in einer chronologisch sortierten Inline-Timeline in manage/groups/[id]."
    - "Mehr als 5 Einträge werden per Progressive-Disclosure-Expander ausgeblendet."
    - "Titel ist Pflichtfeld; Jahr und Notiz sind optional."
    - "Neue Einträge werden sofort mit status='confirmed' gespeichert (D-11)."
    - "Löschen wird per Modal bestätigt; kein Page-Redirect."
    - "DELETE auf eine historyId, die zu einer anderen Gruppe gehört, gibt 404 zurück."
  artifacts:
    - path: backend/internal/handlers/fansub_group_history_handler.go
      provides: DeleteGroupHistory + permissionSvc-Feld + Leader-Auth-Check bei Create/Update/Delete + Cross-Group-Guard
    - path: backend/cmd/server/admin_routes.go
      provides: DELETE /admin/fansubs/:id/history/:historyId Route
    - path: frontend/src/components/groups/GroupHistorySection.tsx
      provides: Client-Komponente mit CRUD-Inline-Timeline, Progressive Disclosure, Formular, Lösch-Modal
    - path: frontend/src/lib/api.ts
      provides: deleteGroupHistory() + bestehende list/create/updateGroupHistory falls noch fehlend
  key_links:
    - from: frontend/src/app/admin/my-groups/[id]/page.tsx
      to: frontend/src/components/groups/GroupHistorySection.tsx
      via: "<GroupHistorySection fansubGroupId={groupId} />"
      pattern: "GroupHistorySection"
    - from: frontend/src/components/groups/GroupHistorySection.tsx
      to: frontend/src/lib/api.ts
      via: deleteGroupHistory / createGroupHistory / updateGroupHistory / listGroupHistory
      pattern: "deleteGroupHistory|createGroupHistory"
---

<objective>
Gruppen-Meilenstein-CRUD vollständig implementieren: fehlender DeleteGroupHistory-Handler, Leader-Auth-Check in fansub_group_history_handler.go, DELETE-Route in admin_routes.go, GroupHistorySection-Komponente mit Inline-Timeline + CRUD-Formular + Lösch-Modal, deleteGroupHistory() in api.ts.

Purpose: P68-SC2 — Leader kann Meilensteine für die Gruppe manuell eintragen; Meilensteine erscheinen in der Gruppen-Timeline (D-09 bis D-12).
Output: Vollständiger Leader-CRUD für fansub_group_history; Inline-Timeline-UI in manage/groups/[id].
</objective>

<execution_context>
@C:\Users\admin\Documents\Team4s\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\Documents\Team4s\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\admin\Documents\Team4s\.planning\ROADMAP.md
@C:\Users\admin\Documents\Team4s\.planning\phases\68-badge-engine-archiv-entdeckung\68-CONTEXT.md
@C:\Users\admin\Documents\Team4s\.planning\phases\68-badge-engine-archiv-entdeckung\68-RESEARCH.md
@C:\Users\admin\Documents\Team4s\.planning\phases\68-badge-engine-archiv-entdeckung\68-PATTERNS.md
@C:\Users\admin\Documents\Team4s\.planning\phases\68-badge-engine-archiv-entdeckung\68-UI-SPEC.md

<interfaces>
<!-- Schlüssel-Contracts aus Codebase. -->

From backend/internal/handlers/fansub_group_history_handler.go (195 lines):
```go
type FansubGroupHistoryHandler struct {
    historyRepo *repository.FansubGroupHistoryRepository
    // permissionSvc fehlt noch — muss ergänzt werden
}
func NewFansubGroupHistoryHandler(repo *repository.FansubGroupHistoryRepository) *FansubGroupHistoryHandler
func (h *FansubGroupHistoryHandler) ListGroupHistory(c *gin.Context)    // GET
func (h *FansubGroupHistoryHandler) CreateGroupHistory(c *gin.Context)  // POST — status default: "historical" → muss auf "confirmed" geändert werden (D-11)
func (h *FansubGroupHistoryHandler) UpdateGroupHistory(c *gin.Context)  // PATCH
// DeleteGroupHistory fehlt
```

From backend/internal/repository/fansub_group_history_repository.go:
```go
func (r *FansubGroupHistoryRepository) Delete(ctx context.Context, id int64) error  // existiert bereits (line 197)
func (r *FansubGroupHistoryRepository) ListByFansub(ctx context.Context, fansubID int64) ([]FansubGroupHistoryRow, error)
func (r *FansubGroupHistoryRepository) Create(ctx context.Context, fansubID int64, input FansubGroupHistoryInput) (*FansubGroupHistoryRow, error)
func (r *FansubGroupHistoryRepository) Update(ctx context.Context, id int64, input FansubGroupHistoryInput) (*FansubGroupHistoryRow, error)
```

From FansubAnimeContributionsHandler (Permission-Pattern — kopieren für History-Handler):
```go
identity, actor, ok := permissionActorFromContext(c)
if !ok { return }
result, err := h.permissionSvc.CanForFansubGroup(c.Request.Context(), actor,
    permissions.ActionFansubGroupMembersManage, fansubID)
if err != nil { writePermissionInternalError(c, err, "Berechtigung konnte nicht geprüft werden."); return }
if !result.Allowed { writePermissionDenied(c, result); return }
```

From backend/cmd/server/admin_routes.go (179 lines) — bestehende History-Routen:
```
v1.GET("/admin/fansubs/:id/history", auth, deps.groupHistoryHandler.ListGroupHistory)
v1.POST("/admin/fansubs/:id/history", auth, deps.groupHistoryHandler.CreateGroupHistory)
v1.PATCH("/admin/fansubs/:id/history/:historyId", auth, deps.groupHistoryHandler.UpdateGroupHistory)
// DELETE fehlt
```

From frontend/src/app/admin/my-groups/[id]/page.tsx — Client-Komponenten-Pattern:
```typescript
'use client'
import { useState, useCallback, useEffect } from 'react'
import { Button, Card, EmptyState, ErrorState, Modal, SectionHeader, Toolbar } from '@/components/ui'
import { ApiError } from '@/lib/api'
```

UI-SPEC Surface 2 — GroupHistorySection:
- SectionHeader eyebrow="Gruppen-Historie" title="Meilensteine"
- COLLAPSE_THRESHOLD = 5
- EVENT_TYPE_LABELS: founding→"Gründung", disbanding→"Auflösung", hiatus→"Pause", rebranding→"Umbenennung", milestone→"Meilenstein", other→"Sonstiges"
- Buttons: aria-label="Eintrag bearbeiten" / "Eintrag löschen" (Icon-only mit Pencil/Trash2)
- Formular-Felder: Titel (required), Ereignistyp (Select), Jahr (number), Notiz (Textarea)
- Toast: falls MessageToast vorhanden nutzen, sonst 3-Sekunden-Inline-Meldung
- Lösch-Modal: variant="danger" Button "Endgültig löschen" / "Nicht löschen"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DeleteGroupHistory-Handler + permissionSvc + Leader-Auth + Cross-Group-Guard + DELETE-Route</name>
  <files>
    backend/internal/handlers/fansub_group_history_handler.go,
    backend/cmd/server/admin_routes.go
  </files>
  <read_first>
    - backend/internal/handlers/fansub_group_history_handler.go (vollständig — UpdateGroupHistory als Pattern für DeleteGroupHistory, normalizeHistoricalContributionStatus)
    - backend/internal/repository/fansub_group_history_repository.go (vollständig — prüfe ob GetByID existiert; falls nicht, muss es ergänzt werden oder Delete-Repo-Methode muss WHERE id=$1 AND fansub_group_id=$2 unterstützen)
    - backend/internal/handlers/fansub_anime_contributions_handler.go (permissionActorFromContext + permissionSvc-Pattern, Zeilen 100–130)
    - backend/cmd/server/admin_routes.go (vollständig — bestehende History-Routen + wie groupHistoryHandler konstruiert wird)
    - backend/cmd/server/main.go (FansubGroupHistoryHandler-Instanziierung suchen)
    - .planning/phases/68-badge-engine-archiv-entdeckung/68-PATTERNS.md (Abschnitt fansub_group_history_handler.go)
    - .planning/phases/68-badge-engine-archiv-entdeckung/68-RESEARCH.md (Befund 3: Auth-Pattern, Status-Semantik D-11)
  </read_first>
  <behavior>
    - Handler-Test: POST ohne permissionSvc (Leader-Token) → 403
    - Handler-Test: DELETE mit gültigem Leader → 204
    - Handler-Test: DELETE auf nicht-existente ID → 404
    - Handler-Test: DELETE auf historyId, die zu einer anderen Gruppe gehört → 404
    - Handler-Test: POST ohne Titel → 400/422
    - Handler-Test: POST mit gültigem Payload → 201, status='confirmed' in Response
  </behavior>
  <action>
    Erweitere FansubGroupHistoryHandler in fansub_group_history_handler.go:
    - Füge Feld permissionSvc *permissions.Service zum Struct hinzu
    - Füge WithPermissionSvc(svc *permissions.Service) *FansubGroupHistoryHandler hinzu (gibt h zurück)
    - Ergänze permissionSvc-Check am Anfang von CreateGroupHistory, UpdateGroupHistory und dem neuen DeleteGroupHistory — exakt das Pattern aus FansubAnimeContributionsHandler (permissionActorFromContext + CanForFansubGroup(ActionFansubGroupMembersManage, fansubID))

    Status-Fix für Leader (D-11): In CreateGroupHistory: statt normalizeHistoricalContributionStatus aufzurufen, setze input.Status = "confirmed" direkt (oder falls normalizeHistoricalContributionStatus den Input nicht verändert: explizit nach dem Aufruf überschreiben). Ziel: Neueintrag landet immer mit status='confirmed'.

    CROSS-GROUP-GUARD (T-68-02-03): Neue Methode DeleteGroupHistory muss sicherstellen, dass der zu löschende Eintrag zur URL-Gruppe gehört. Implementiere einen der folgenden Ansätze (wähle den, der das Repository unterstützt):
    - Option A (bevorzugt): Erweitere FansubGroupHistoryRepository.Delete um eine Variante mit `WHERE id=$1 AND fansub_group_id=$2` — gibt 0 betroffene Zeilen zurück wenn kein Match → Handler gibt 404. Alternativ: Neuer Repository-Aufruf `DeleteByIDAndFansub(ctx, historyID, fansubID) error` mit scoped WHERE.
    - Option B (falls kein Repo-Change möglich): Rufe vor dem Delete `h.historyRepo.GetByID(ctx, historyID)` auf; wenn Zeile nicht gefunden → 404; wenn gefundene Zeile `FansubGroupID != fansubID` → 404 (kein Log, kein Infoleakage).
    Welcher Ansatz gewählt wird, muss in fansub_group_history_handler.go oder fansub_group_history_repository.go umgesetzt sein — die Prüfung darf NICHT fehlen.

    Neue Methode DeleteGroupHistory(c *gin.Context):
    - Parst fansubID aus c.Param("id") und historyID aus c.Param("historyId")
    - Permission-Check (permissionActorFromContext + CanForFansubGroup)
    - Cross-Group-Guard (s. oben) — gibt 404 wenn historyID nicht zu fansubID gehört
    - h.historyRepo.Delete(c.Request.Context(), historyID) [oder scoped Delete]
    - Bei ErrNotFound → 404; bei anderem Fehler → log + internalError
    - Erfolg: c.Status(http.StatusNoContent)

    UpdateGroupHistory erhält dieselbe Cross-Group-Prüfung: vor dem Update-Aufruf sicherstellen, dass der Eintrag zur URL-Gruppe gehört (gleiche Logik wie Delete). Auch UpdateGroupHistory kann einen fremden Eintrag überschreiben, wenn der fansubID-Check fehlt.

    In backend/cmd/server/admin_routes.go: Ergänze nach der PATCH-Route:
    v1.DELETE("/admin/fansubs/:id/history/:historyId", auth, deps.groupHistoryHandler.DeleteGroupHistory)

    In backend/cmd/server/main.go: Ergänze .WithPermissionSvc(permSvc) bei der FansubGroupHistoryHandler-Instanziierung (permSvc ist bereits vorhanden, da andere Handler es nutzen).

    DATEILIMIT: fansub_group_history_handler.go hat 195 Zeilen. DeleteGroupHistory ~30 Zeilen + WithPermissionSvc ~5 Zeilen + permissionSvc-Feld ~1 Zeile + 3 × Permission-Check-Block je ~8 Zeilen + Cross-Group-Guard ~10 Zeilen = ca. +70 Zeilen → ~265 Zeilen, unter dem 450-Zeilen-Limit.

    Handler-Tests in fansub_group_history_handler_test.go schreiben (falls Datei existiert) oder neue Testdatei:
    - TestDeleteGroupHistory_NotFound: repo.Delete gibt ErrNotFound → Handler gibt 404
    - TestDeleteGroupHistory_CrossGroupForbidden: historyID gehört zu Gruppe B, URL-Gruppe ist A → Handler gibt 404
    - TestCreateGroupHistory_StatusConfirmed: Handler setzt status='confirmed' im CreateInput
    - TestDeleteGroupHistory_PermissionDenied: permissionSvc gibt !Allowed → 403
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go build ./... 2>&1 | head -20 && go test ./internal/handlers/... -run "TestDeleteGroupHistory|TestCreateGroupHistory" -v -count=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - fansub_group_history_handler.go enthält DeleteGroupHistory und permissionSvc-Feld
    - admin_routes.go enthält DELETE-Route für /admin/fansubs/:id/history/:historyId
    - Handler-Tests für Delete (404, 403, 204) grün oder Source-Inspection belegt korrektes Error-Handling
    - CreateGroupHistory setzt status='confirmed' (D-11)
    - DeleteGroupHistory gibt 404 wenn historyId zu einer ANDEREN Gruppe gehört (kein fremder Eintrag löschbar)
    - UpdateGroupHistory gibt 404 wenn historyId zu einer ANDEREN Gruppe gehört
    - go build ./... fehlerfrei
  </acceptance_criteria>
  <done>DeleteGroupHistory implementiert, Leader-Auth in Create/Update/Delete, Cross-Group-Guard für Delete und Update, DELETE-Route registriert, status='confirmed' bei Neuerstellung</done>
</task>

<task type="auto">
  <name>Task 2: GroupHistorySection-Komponente + deleteGroupHistory in api.ts + Integration in manage/groups/[id]</name>
  <files>
    frontend/src/components/groups/GroupHistorySection.tsx,
    frontend/src/components/groups/groups.module.css,
    frontend/src/app/admin/my-groups/[id]/page.tsx,
    frontend/src/lib/api.ts
  </files>
  <read_first>
    - frontend/src/app/admin/my-groups/[id]/page.tsx (vollständig — Client-Komponenten-Pattern, State, API-Aufrufe, Fehlerbehandlung, bestehende Karten-Struktur)
    - frontend/src/lib/api.ts (bestehende listGroupHistory/createGroupHistory/updateGroupHistory falls vorhanden; sonst Muster von getMyFansubGroupDetail; delete-Pattern wie deleteMembership oder ähnlich)
    - frontend/src/components/ui/index.ts (exportierte Komponenten prüfen: Button, Card, EmptyState, ErrorState, Modal, SectionHeader, Toolbar, Select, Input)
    - .planning/phases/68-badge-engine-archiv-entdeckung/68-UI-SPEC.md (Surface 2 vollständig)
    - .planning/phases/68-badge-engine-archiv-entdeckung/68-PATTERNS.md (Abschnitt GroupHistorySection.tsx und api.ts)
  </read_first>
  <action>
    Erstelle frontend/src/components/groups/GroupHistorySection.tsx als 'use client' Komponente mit Props { fansubGroupId: number, authToken?: string }:

    State: entries (GroupHistoryRow[]), isLoading, error, isExpanded (false), editTarget (GroupHistoryRow|null), isFormOpen (false), deleteTarget (GroupHistoryRow|null), isSaving (false), successMessage (string|null).

    COLLAPSE_THRESHOLD = 5: const visibleEntries = isExpanded ? entries : entries.slice(0, COLLAPSE_THRESHOLD).

    EVENT_TYPE_LABELS Map: founding→"Gründung", disbanding→"Auflösung", hiatus→"Pause", rebranding→"Umbenennung", milestone→"Meilenstein", other→"Sonstiges".

    loadEntries(): ruft listGroupHistory(fansubGroupId, authToken) auf; sortiert nach year (null zuletzt) aufsteigend.

    Formular-Felder (inline, nur sichtbar wenn isFormOpen): Titel (Input, required), Ereignistyp (Select mit EVENT_TYPE_OPTIONS), Jahr (Input type="number" min=1990 max=2099), Notiz (Textarea rows=2). Submit: createGroupHistory oder updateGroupHistory. Buttons: "Meilenstein speichern" (primary) / "Bearbeitung abbrechen" (ghost).

    Zeilen-Layout per UI-SPEC: Jahr-Spalte (min-width:48px), EventType-Spalte (min-width:100px), Titel + Notiz (flex:1), Aktions-Buttons (Pencil/Trash2 mit aria-label).

    Lösch-Bestätigung: Modal (falls vorhanden aus @/components/ui) mit title="Eintrag löschen", Buttons "Endgültig löschen" (danger) und "Nicht löschen" (secondary). Falls Modal nicht verfügbar: window.confirm.

    Toast: Prüfe ob MessageToast aus '@/app/admin/anime/components/' importierbar ist; falls ja: nutze es für Erfolgs-Meldungen; sonst: setSuccessMessage(text) + setTimeout clearSuccess nach 3000ms.

    Validierung: Titel darf nicht leer sein → Fehlermeldung "Titel ist ein Pflichtfeld." inline unter dem Formular-Feld.

    Erstelle frontend/src/components/groups/groups.module.css mit den in UI-SPEC definierten Klassen: .historyRow (flex, gap:12px, padding:8px 12px), .historyYear, .historyEventType (font-size:13px, font-weight:700), .historyTitle (flex:1), .historyNote (font-size:13px, color:var(--text-faint)), .historyRowActions (flex, gap:8px), .historyForm (grid, gap:12px, padding:16px), .historyFormRow (grid, 2 Spalten, gap:12px; bei ≤480px 1 Spalte). Spacing: 4-Punkt-konform (8px, 12px, 16px); 12px ist dokumentierter Codebase-Token.

    In frontend/src/lib/api.ts: Füge deleteGroupHistory(fansubGroupId, historyId, authToken?) hinzu — DELETE-Aufruf gegen /api/v1/admin/fansubs/:id/history/:historyId über authorizedFetch; bei !response.ok: ApiError werfen. Stelle sicher dass listGroupHistory, createGroupHistory, updateGroupHistory ebenfalls exportiert sind (ergänze falls fehlend, analog zu bestehenden Gruppenaufrufen).

    In frontend/src/app/admin/my-groups/[id]/page.tsx: Importiere GroupHistorySection und rendere sie als neue Card nach den bestehenden Sections, mit fansubGroupId={resolvedGroupId} und authToken={authToken} (authToken ist in der Seite aus Session verfügbar — analog zu anderen API-Aufrufen auf der Seite).

    DATEILIMIT: GroupHistorySection.tsx soll ≤ 450 Zeilen bleiben. Falls das Formular zu viel Platz einnimmt, extrahiere es als GroupHistoryForm.tsx in denselben Ordner.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - GroupHistorySection.tsx existiert, compiliert im Frontend-Build fehlerfrei
    - api.ts exportiert deleteGroupHistory, listGroupHistory, createGroupHistory, updateGroupHistory
    - my-groups/[id]/page.tsx importiert und rendert GroupHistorySection
    - groups.module.css vorhanden mit .historyRow, .historyForm, .historyFormRow
    - npm run build fehlerfrei
    - GroupHistorySection.tsx bleibt ≤ 450 Zeilen (Formular ggf. in GroupHistoryForm.tsx ausgelagert)
  </acceptance_criteria>
  <done>Inline-Meilenstein-Timeline vollständig implementiert; Leader-CRUD via API verdrahtet; Frontend-Build grün</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Leader → /admin/fansubs/:id/history (Backend) | Auth-Middleware + CanForFansubGroup-Check — untrusted input vom Leader-Client |
| Frontend-Formular → deleteGroupHistory | authToken aus Session; fansubGroupId/historyId aus URL-Kontext |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-02-01 | Elevation of Privilege | DeleteGroupHistory ohne Gruppen-Mitgliedschaft | mitigate | permissionSvc.CanForFansubGroup(ActionFansubGroupMembersManage, fansubID) vor jedem Mutation-Handler (per D-12, RESEARCH.md Befund 3) |
| T-68-02-02 | Tampering | fansubID/historyID Integer-Injection | mitigate | strconv.ParseInt + Validierung ≤ 0 → badRequest; pgx parameterized queries |
| T-68-02-03 | Spoofing | Cross-Group History-Manipulation (Leader von Gruppe A löscht Eintrag von Gruppe B) | mitigate | DeleteGroupHistory und UpdateGroupHistory implementieren Cross-Group-Guard: Repository-Delete mit WHERE id=$1 AND fansub_group_id=$2 (scoped) ODER Handler liest GetByID vorher und vergleicht FansubGroupID → 404 auf Mismatch. Acceptance criteria: DELETE auf fremde historyId gibt 404. |
| T-68-02-SC | Tampering | npm install | mitigate | Keine neuen Packages — kein Audit nötig |
</threat_model>

<verification>
Backend: go build ./... fehlerfrei + Handler-Tests für Delete (204, 403, 404, Cross-Group-404) grün. Frontend: npm run build fehlerfrei.
</verification>

<success_criteria>
- DELETE /admin/fansubs/:id/history/:historyId gibt 204 bei gültigem Leader, 403 bei Nicht-Leader, 404 bei unbekannter ID, 404 wenn historyId zu anderer Gruppe gehört
- Neue History-Einträge haben status='confirmed' (D-11)
- GroupHistorySection rendert Timeline mit Progressive Disclosure (COLLAPSE_THRESHOLD=5)
- Lösch-Modal erscheint vor dem eigentlichen Delete
- api.ts exportiert deleteGroupHistory
- my-groups/[id]/page.tsx zeigt GroupHistorySection
</success_criteria>

<output>
Erstelle `.planning/phases/68-badge-engine-archiv-entdeckung/68-02-SUMMARY.md` wenn fertig.
</output>
