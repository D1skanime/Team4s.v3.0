---
phase: 68-badge-engine-archiv-entdeckung
plan: 04
type: execute
wave: 3
depends_on:
  - 68-01
  - 68-02
  - 68-03
files_modified: []
autonomous: false
requirements:
  - P68-SC1
  - P68-SC2
  - P68-SC3

must_haves:
  truths:
    - "Badge-Engine: ComputeAndStoreBadges vergibt und entzieht alle 7 Badge-Codes korrekt."
    - "Gruppen-Meilensteine: Leader kann anlegen, bearbeiten und löschen; Einträge erscheinen in GroupHistorySection."
    - "Archiv-Suche: /archiv zeigt öffentliche Member-Profile mit funktionierenden Filtern und Pagination."
    - "Alle user-facing Strings sind auf Deutsch mit korrekten Umlauten."
    - "go build ./... und npm run build laufen fehlerfrei."
  artifacts:
    - path: backend/internal/services/badge_service.go
      provides: computeFirstContribution, computeProductiveTiers, computeAllRounder, computeVerified integriert
    - path: backend/internal/services/badge_backfill_service.go
      provides: BadgeBackfillService.BackfillAll
    - path: backend/internal/handlers/fansub_group_history_handler.go
      provides: DeleteGroupHistory + permissionSvc + status='confirmed' für Leader
    - path: backend/internal/repository/member_archive_repository.go
      provides: SearchMembers mit Sichtbarkeits-Filter
    - path: frontend/src/app/archiv/page.tsx
      provides: öffentliche /archiv-Seite
    - path: frontend/src/components/groups/GroupHistorySection.tsx
      provides: Inline-Timeline mit CRUD
  key_links: []
---

<objective>
Abschluss-Checkpoint für Phase 68: Automatische Verifikation aller drei Teilfeatures durch Build-Tests, Unit-Tests und manuelle UAT-Schritte. Keine neuen Dateien werden erstellt.

Purpose: Alle P68-SC1, P68-SC2, P68-SC3 Anforderungen durch vollständige Verifikation abschließen.
Output: Phase 68 als vollständig verifikabel markiert; SUMMARY-Datei für Phase erstellt.
</objective>

<execution_context>
@C:\Users\admin\Documents\Team4s\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\Documents\Team4s\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\admin\Documents\Team4s\.planning\ROADMAP.md
@C:\Users\admin\Documents\Team4s\.planning\phases\68-badge-engine-archiv-entdeckung\68-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automatische Gesamtverifikation (Build + Tests)</name>
  <files></files>
  <read_first>
    - .planning/phases/68-badge-engine-archiv-entdeckung/68-VALIDATION.md (Test-Infrastruktur und Wave-0-Anforderungen)
    - .planning/phases/68-badge-engine-archiv-entdeckung/68-01-SUMMARY.md (was wurde in Plan 01 implementiert)
    - .planning/phases/68-badge-engine-archiv-entdeckung/68-02-SUMMARY.md (was wurde in Plan 02 implementiert)
    - .planning/phases/68-badge-engine-archiv-entdeckung/68-03-SUMMARY.md (was wurde in Plan 03 implementiert)
  </read_first>
  <action>
    Führe die vollständige automatisierte Verifikations-Suite aus:

    1. Backend Build: cd backend && go build ./...
    2. Backend Tests (P68-SC1 Badge-Engine): go test ./internal/services/... ./internal/repository/... -v -count=1
    3. Backend Tests (P68-SC2 Handler): go test ./internal/handlers/... -run "TestDeleteGroupHistory|TestCreateGroupHistory|TestGroupHistory" -v -count=1
    4. Backend Tests (P68-SC3 Archiv): go test ./internal/repository/... -run "TestArchive" -v -count=1
    5. Frontend Build: cd frontend && npm run build

    Wenn ein Test-Step fehlschlägt: Fehlerursache analysieren und beheben. Häufige Fix-Kategorien:
    - Missing import → ergänzen
    - Interface-Mismatch → Methoden-Signatur prüfen
    - SQL-Spalte nicht gefunden → Migration prüfen ob 0091 und 0092 up-Migrationen korrekt sind
    - Frontend Build-Fehler: TypeScript-Type fehlt → ArchiveMemberRow oder ArchiveSearchResponse in @/types/ ergänzen

    Dokumentiere das Ergebnis jedes Test-Runs in der SUMMARY.md.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go build ./... && go test ./internal/services/... ./internal/handlers/... ./internal/repository/... -v -count=1 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... exit 0
    - go test ./internal/services/... gibt PASS für Badge-Engine-Tests
    - go test ./internal/handlers/... gibt PASS für History-Handler-Tests
    - go test ./internal/repository/... gibt PASS für Archiv-Repository-Tests
    - npm run build exit 0
  </acceptance_criteria>
  <done>Alle automatisierten Tests grün; Backend und Frontend compilieren fehlerfrei</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manuelle UAT — alle drei Sub-Features live verifizieren</name>
  <what-built>
    Plan 01: Badge-Engine mit 4 neuen Badges (first_contribution, productive_bronze/silver/gold, all_rounder, verified), Recompute-Trigger in Contribution-Mutations, backfill-badges CLI-Subbefehl.
    Plan 02: Gruppen-Meilenstein-CRUD (Delete-Handler, Leader-Auth, GroupHistorySection-UI in manage/groups/[id]).
    Plan 03: Öffentliche /archiv-Seite mit Filtern (Rolle/Gruppe/Zeitraum), MemberSearchCard-Raster, Pagination, neue Badge-Chip-Labels.
  </what-built>
  <how-to-verify>
    Docker-Umgebung neu bauen (falls Backend geändert): docker-compose build backend && docker-compose up -d

    **P68-SC1 Badge-Engine:**
    1. Öffne Admin → Fansub-Gruppe → Anime-Beiträge → lege eine bestätigte Contribution an.
    2. Prüfe in der DB: SELECT badge_code, status FROM member_badges WHERE member_id = [ID]; expected: first_contribution mit status='active'.
    3. Lösche die Contribution. Prüfe: first_contribution hat status='revoked'.
    4. Führe im Container aus: ./migrate backfill-badges; erwarte Log "badge backfill complete: members_processed=N errors=0".

    **P68-SC2 Gruppen-Meilensteine:**
    1. Navigiere zu /manage/groups/[id] als Leader.
    2. Klicke "+ Meilenstein hinzufügen" → fülle Titel aus → klicke "Meilenstein speichern".
    3. Erwarte: Eintrag erscheint in der Timeline mit status='confirmed'.
    4. Klicke Bearbeiten-Button (Pencil-Icon) → ändere Titel → speichere.
    5. Klicke Löschen-Button (Trash2-Icon) → Modal erscheint → "Endgültig löschen" → Eintrag verschwindet.
    6. Lege 6 Einträge an → prüfe: nur 5 sichtbar, Expander-Button "Alle 6 Einträge anzeigen" erscheint.

    **P68-SC3 Archiv-Suche:**
    1. Navigiere zu /archiv (ohne Login).
    2. Seite lädt ohne Fehler; Heading "Archiv — Fansub-Mitwirkende entdecken" sichtbar.
    3. Setze Filter Rolle = "Übersetzung" → "Archiv durchsuchen" → Ergebnisse zeigen nur Member mit Translator-Contribution.
    4. Klicke "Filter zurücksetzen" → URL wird /archiv ohne Parameter.
    5. Bei > 20 Ergebnissen: Pagination mit "Weiter"-Link sichtbar.
    6. Prüfe: Member mit nicht-öffentlichem Profil erscheint nicht in der Liste.

    **Umlaut-Check:**
    - /archiv-Seite: alle Labels auf Deutsch korrekt (Übersetzung, Qualitätskontrolle, Projektleitung etc.)
    - Meilenstein-Formular: alle Labels mit korrekten Umlauten (Ereignistyp, Gründung, Auflösung)
  </how-to-verify>
  <resume-signal>Gib "bestätigt" ein wenn alle drei Sub-Features funktionieren, oder beschreibe die aufgetretenen Probleme.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| UAT-Tester → Live-Umgebung | Verifizierung in Docker-Compose-Dev-Umgebung; kein Produktions-Risiko |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-04-01 | Information Disclosure | UAT-Verifikation der Archiv-Suche | mitigate | Im UAT explizit prüfen: nicht-öffentliches Mitglied erscheint nicht (T-68-03-01 Nachweis) |
| T-68-04-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neuen Packages in Plan 04 |
</threat_model>

<verification>
Manuell: alle drei UAT-Schritte bestanden. Automatisch: go build + go test + npm run build grün.
</verification>

<success_criteria>
- go build ./... exit 0
- go test ./internal/... alle Phase-68-Tests grün
- npm run build exit 0
- UAT P68-SC1: Badge wird nach Contribution-Create vergeben und nach Delete entzogen
- UAT P68-SC2: Leader kann Meilensteine anlegen/bearbeiten/löschen; Progressive Disclosure bei > 5 Einträgen funktioniert
- UAT P68-SC3: /archiv lädt, Filter funktionieren, nicht-öffentliche Member erscheinen nicht
</success_criteria>

<output>
Erstelle `.planning/phases/68-badge-engine-archiv-entdeckung/68-04-SUMMARY.md` und aktualisiere ROADMAP.md Phase 68 Status auf "complete".
</output>
