---
phase: 68-badge-engine-archiv-entdeckung
plan: 03
type: execute
wave: 2
depends_on:
  - 68-01
  - 68-02
files_modified:
  - database/migrations/0092_archive_search_indexes.up.sql
  - database/migrations/0092_archive_search_indexes.down.sql
  - backend/internal/repository/member_archive_repository.go
  - backend/internal/handlers/member_archive_handler.go
  - backend/cmd/server/main.go
  - frontend/src/app/archiv/page.tsx
  - frontend/src/app/archiv/page.module.css
  - frontend/src/components/archive/MemberSearchCard.tsx
  - frontend/src/components/archive/archive.module.css
  - frontend/src/components/profile/MemberBadgeChips.tsx
  - frontend/src/lib/api.ts
autonomous: true
requirements:
  - P68-SC3
  - P68-SC1

must_haves:
  truths:
    - "GET /api/v1/archiv ohne Filter gibt alle öffentlichen Member zurück (profile_visibility='public', is_public_on_member_profile=true, hfgm.visibility='public')."
    - "Filter rolle/gruppe/von/bis sind UND-verknüpft und alle optional."
    - "Nicht-öffentliche Member (profile_visibility!='public') erscheinen niemals in den Ergebnissen."
    - "Offset-Pagination: 20 Ergebnisse pro Seite via ?page=N."
    - "Pagination-Params page<1 werden auf 1 normiert; keine negativen Offsets."
    - "Frontend /archiv rendert MemberSearchCard-Raster mit Filtern (Rolle, Zeitraum, Gruppe) und Pagination."
    - "Badge-Chip-Labels für die 6 neuen Badge-Codes sind in MemberBadgeChips.tsx eingetragen."
    - "Gruppe-Filter-Select rendert und filtert korrekt (per D-14)."
  artifacts:
    - path: database/migrations/0092_archive_search_indexes.up.sql
      provides: idx_members_profile_visibility + idx_member_claims_member_claim_status
    - path: backend/internal/repository/member_archive_repository.go
      provides: MemberArchiveRepository.SearchMembers(ctx, filters, page) → ArchiveSearchResult
      contains: "is_public_on_member_profile = true AND hfgm.visibility = 'public' AND m.profile_visibility = 'public'"
    - path: backend/internal/handlers/member_archive_handler.go
      provides: MemberArchiveHandler.SearchArchive — GET /api/v1/archiv, kein Auth-Gate
    - path: frontend/src/app/archiv/page.tsx
      provides: Server Component (force-dynamic), searchParams → Backend-Aufruf → MemberSearchCard-Raster
    - path: frontend/src/components/archive/MemberSearchCard.tsx
      provides: Props {id, nickname, displayName, slug, avatarPath, isVerified, topRoles, groups}
    - path: frontend/src/components/profile/MemberBadgeChips.tsx
      provides: BADGE_LABELS erweitert um 6 neue Codes (first_contribution, productive_bronze/silver/gold, all_rounder, verified)
  key_links:
    - from: backend/internal/handlers/member_archive_handler.go
      to: backend/internal/repository/member_archive_repository.go
      via: "archiveRepo.SearchMembers(ctx, filters, page)"
      pattern: "SearchMembers"
    - from: frontend/src/app/archiv/page.tsx
      to: frontend/src/lib/api.ts
      via: "searchArchive(params)"
      pattern: "searchArchive"
    - from: backend/cmd/server/main.go
      to: backend/internal/handlers/member_archive_handler.go
      via: "v1.GET(\"/archiv\", archiveHandler.SearchArchive)"
      pattern: "archiv"
---

<objective>
Archiv-Suche vollständig implementieren: Migration 0092 mit zwei neuen Indizes, neues member_archive_repository.go mit öffentlich-sicherer UND-Filter-Query + Offset-Pagination, neuer member_archive_handler.go, öffentliche Route /api/v1/archiv ohne Auth, Frontend /archiv-Seite mit Filtern (Rolle, Zeitraum, Gruppe) und MemberSearchCard-Raster, searchArchive() in api.ts. Außerdem: neue Badge-Chip-Labels in MemberBadgeChips.tsx (P68-SC1 Frontend-Teil).

Purpose: P68-SC3 — Archiv-Suche erlaubt Filtern nach Rolle, Zeitraum und Gruppe; P68-SC1 (Badge-Labels im Frontend, per D-03/D-17).
Output: /api/v1/archiv Backend-Endpunkt, /archiv Frontend-Seite, MemberBadgeChips-Erweiterung.
</objective>

<execution_context>
@C:\Users\admin\Documents\Team4s\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\Documents\Team4s\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\admin\Documents\Team4s\.planning\ROADMAP.md
@C:\Users\admin\Documents\Team4s\.planning\phases\68-badge-engine-archiv-entdeckung\68-CONTEXT.md
@C:\Users\admin\Documents\Team4s\.planning\phases\68-badge-engine-archiv-entdeckung\68-RESEARCH.md
@C:\Users\admin\Documents\Team4s\.planning\phases\68-badge-engine-archiv-entdeckung\68-PATTERNS.md
@C:\Users\admin\Documents\Team4s\.planning\phases\68-badge-engine-archiv-entdeckung\68-UI-SPEC.md

<interfaces>
<!-- Schlüssel-Contracts. Aus Codebase und PATTERNS.md extrahiert. -->

From backend/internal/repository/anime_contributions_public_repository.go — Sichtbarkeits-Pattern (verbindlich):
```go
// Diese WHERE-Klauseln MÜSSEN identisch in member_archive_repository.go verwendet werden:
// ac.is_public_on_member_profile = true
// hfgm.visibility = 'public'
// m.profile_visibility = 'public'
```

From backend/internal/handlers/member_badges_handler.go — Handler-Pattern:
```go
type MemberBadgesHandler struct { badgeRepo *repository.BadgeRepository }
func NewMemberBadgesHandler(repo *repository.BadgeRepository) *MemberBadgesHandler
// Handler gibt c.JSON(http.StatusOK, gin.H{...}) zurück; fehler: internalError(c, "interner serverfehler")
```

From backend/cmd/server/main.go — Public-Route-Pattern (KEIN authMiddleware):
```go
v1.GET("/fansubs/:id/contributions", memberContributionsHandler.ListByFansub)  // public — kein auth
// NEU analog:
v1.GET("/archiv", archiveHandler.SearchArchive)  // public — kein auth
```

DB-Tabellen (für die Query relevant):
- members: id, nickname, display_name, profile_visibility, avatar_media_id
- hist_fansub_group_members: id, member_id, fansub_group_id, visibility
- anime_contributions: id, fansub_group_member_id, anime_id, is_public_on_member_profile, status
- anime_contribution_roles: id, anime_contribution_id, role_code
- member_claims: id, member_id, claim_status
- media_assets: id, file_path (für Avatar)
- fansub_groups: id, name (für Gruppen-Filter und Gruppen-Namen)

Archive Result types (aus PATTERNS.md):
```go
type ArchiveSearchFilters struct {
    RoleCode      string
    FansubGroupID int64
    YearFrom      int
    YearUntil     int
}
type ArchiveMemberRow struct {
    ID          int64    `json:"id"`
    Nickname    string   `json:"nickname"`
    DisplayName string   `json:"display_name"`
    Slug        *string  `json:"slug"`
    AvatarPath  *string  `json:"avatar_path"`
    IsVerified  bool     `json:"is_verified"`
    TopRoles    []string `json:"top_roles"`
    Groups      []string `json:"groups"`
}
type ArchiveSearchResult struct {
    Members []ArchiveMemberRow
    Total   int
}
```

Frontend API-Response-Shape (Backend liefert):
```typescript
type ArchiveSearchResponse = {
  data: ArchiveMemberRow[]
  total: number
  page: number
}
```

Rolle-Labels für UI (UI-SPEC Surface 1 Filter-Bar):
translator→"Übersetzung", editor→"Editing", timer→"Timing", typesetter→"Typesetting",
encoder→"Encoding", raw_provider→"Raw Provider", quality_checker→"Qualitätskontrolle",
project_lead→"Projektleitung", designer→"Design", admin→"Administration", other→"Sonstiges"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0092 + member_archive_repository.go + member_archive_handler.go + Route</name>
  <files>
    database/migrations/0092_archive_search_indexes.up.sql,
    database/migrations/0092_archive_search_indexes.down.sql,
    backend/internal/repository/member_archive_repository.go,
    backend/internal/handlers/member_archive_handler.go,
    backend/cmd/server/main.go
  </files>
  <read_first>
    - backend/internal/repository/anime_contributions_public_repository.go (Sichtbarkeits-WHERE-Klauseln lesen — diese sind die verbindliche Vorlage)
    - backend/internal/repository/member_profile_repository.go (memberSlugExpr und memberDisplayExpr Konstanten suchen — wiederverwenden)
    - backend/internal/handlers/member_badges_handler.go (Handler-Struct-Pattern, internalError-Aufruf)
    - backend/cmd/server/main.go (public route Registrierung finden — analog zu /fansubs/:id/contributions)
    - database/migrations/0086_anime_contributions.up.sql (Index-Stil als Pattern)
    - .planning/phases/68-badge-engine-archiv-entdeckung/68-RESEARCH.md (Befund 4: vollständige SQL-Grundstruktur, Pitfall 6)
    - .planning/phases/68-badge-engine-archiv-entdeckung/68-PATTERNS.md (Abschnitte member_archive_repository.go, member_archive_handler.go)
  </read_first>
  <behavior>
    - Repository-Test: Member mit profile_visibility='members_only' → erscheint NICHT in SearchMembers
    - Repository-Test: 21 Member → Seite 1 hat 20, Seite 2 hat 1
    - Repository-Test: Filter rolle='translator' → nur Member mit Translator-Contribution in Ergebnissen
    - Repository-Test: Filter fansubGroupID → nur Member dieser Gruppe
    - Handler-Test: page<1 wird auf 1 normiert (kein negativer Offset)
  </behavior>
  <action>
    Erstelle database/migrations/0092_archive_search_indexes.up.sql:
    - CREATE INDEX IF NOT EXISTS idx_members_profile_visibility ON members (profile_visibility);
    - CREATE INDEX IF NOT EXISTS idx_member_claims_member_claim_status ON member_claims (member_id, claim_status);

    Erstelle database/migrations/0092_archive_search_indexes.down.sql:
    - DROP INDEX IF EXISTS idx_members_profile_visibility;
    - DROP INDEX IF EXISTS idx_member_claims_member_claim_status;

    Erstelle backend/internal/repository/member_archive_repository.go NEU (package repository):
    - Struct MemberArchiveRepository + NewMemberArchiveRepository(db *pgxpool.Pool)
    - Typen ArchiveSearchFilters, ArchiveMemberRow, ArchiveSearchResult (aus PATTERNS.md — exakt wie oben definiert)
    - Konstante archivePageSize = 20
    - Methode SearchMembers(ctx, filters, page):
      * offset = (page-1) * 20
      * COUNT-Query für Total: SELECT COUNT(DISTINCT m.id) mit denselben WHERE-Bedingungen
      * Haupt-Query: DISTINCT ON (m.id), m.id, m.nickname, m.display_name, avatar.file_path AS avatar_path
      * Zwingend: WHERE m.profile_visibility = 'public' AND ac.is_public_on_member_profile = true AND hfgm.visibility = 'public' AND ac.status = 'confirmed'
      * Rolle-Filter (nur wenn filters.RoleCode != ""): EXISTS-Subquery analog zu RESEARCH.md Befund 4
      * Zeitraum-Filter (nur wenn YearFrom>0): AND ac.started_year IS NOT NULL AND ac.started_year >= $N oder COALESCE-Variante
      * Gruppe-Filter (nur wenn FansubGroupID>0): AND hfgm.fansub_group_id = $N
      * ORDER BY m.id ASC LIMIT 20 OFFSET $N
      * Alle Filter über pgx parameterized args ($1, $2...) — KEINE String-Interpolation (Sicherheitsgrenze!)
      * IsVerified: EXISTS(SELECT 1 FROM member_claims mc WHERE mc.member_id = m.id AND mc.claim_status = 'verified') AS is_verified
      * TopRoles und Groups: In einem zweiten Query-Pass pro Member (ARRAY_AGG oder separater Batch-Aufruf — einfachste Variante: nach dem Member-Query für jeden Member die Rollen via SELECT DISTINCT acr.role_code ... LIMIT 5 und fansub_groups.name LIMIT 3 abfragen; oder via CTE wenn Komplexität vertretbar)
      * Gibt ArchiveSearchResult{Members, Total} zurück

    Erstelle backend/internal/handlers/member_archive_handler.go NEU:
    - Struct MemberArchiveHandler + NewMemberArchiveHandler(repo *repository.MemberArchiveRepository)
    - SearchArchive(c *gin.Context):
      * roleCode := c.Query("rolle")
      * gruppe := c.Query("gruppe") → ParseInt → fansubGroupID
      * von := c.Query("von") → Atoi → yearFrom
      * bis := c.Query("bis") → Atoi → yearUntil
      * page := c.DefaultQuery("page","1") → Atoi; if page < 1 { page = 1 }
      * Bounds-Check: page > 1000 → page = 1000 (Schutz gegen resource exhaustion, T-68-03-02)
      * Ruft archiveRepo.SearchMembers(ctx, filters, page) auf
      * Fehler → log + internalError(c, "interner serverfehler")
      * Erfolg → c.JSON(200, gin.H{"data": result.Members, "total": result.Total, "page": page})
    - KEIN Auth-Gate, KEIN authMiddleware (Pitfall 6 aus RESEARCH.md)

    In backend/cmd/server/main.go: Instanziiere archiveRepo + archiveHandler; registriere:
    v1.GET("/archiv", archiveHandler.SearchArchive)
    Stelle sicher, dass die Route VOR den authMiddleware-geschützten Routen steht (public section).

    Schreibe Repository-Tests (member_archive_repository_test.go) als Source-Inspection-Tests falls keine echte DB in Tests verfügbar:
    - TestArchiveVisibilityFilter: Prüfe, dass WHERE-Clause die drei Sichtbarkeits-Bedingungen enthält (grep auf SQL-String oder einfaches Query-String-Vergleich)
    - TestArchivePaginationBounds: page=0 → offset=0; page=2 → offset=20
    - TestArchiveRoleFilter: RoleCode!="" → EXISTS-Subquery in SQL enthalten
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go build ./... 2>&1 | head -20 && go test ./internal/repository/... -run "TestArchive" -v -count=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 0092_archive_search_indexes.up.sql erstellt beide Indizes
    - member_archive_repository.go enthält die drei Sichtbarkeits-WHERE-Bedingungen (is_public_on_member_profile, hfgm.visibility='public', m.profile_visibility='public')
    - Alle Filter (RoleCode, FansubGroupID, YearFrom, YearUntil) sind über pgx-Parameters übergeben — KEIN fmt.Sprintf mit User-Input
    - member_archive_handler.go registriert OHNE authMiddleware in main.go
    - page < 1 wird auf 1 normiert
    - go build ./... fehlerfrei
    - Repository-Tests für Sichtbarkeits-Filter vorhanden (Source-Inspection oder Integration)
  </acceptance_criteria>
  <done>Backend-Archiv-Suche vollständig implementiert; Migration 0092 erstellt; /api/v1/archiv öffentlich erreichbar</done>
</task>

<task type="auto">
  <name>Task 2: /archiv Frontend-Seite + MemberSearchCard + Badge-Chip-Labels</name>
  <files>
    frontend/src/app/archiv/page.tsx,
    frontend/src/app/archiv/page.module.css,
    frontend/src/components/archive/MemberSearchCard.tsx,
    frontend/src/components/archive/archive.module.css,
    frontend/src/components/profile/MemberBadgeChips.tsx,
    frontend/src/lib/api.ts
  </files>
  <read_first>
    - frontend/src/app/anime/page.tsx (Server Component mit searchParams — exakte Vorlage für /archiv/page.tsx)
    - frontend/src/app/members/[slug]/page.tsx (Avatar + Name + VerifiedBadge Pattern, error handling)
    - frontend/src/components/profile/MemberBadgeChips.tsx (BADGE_LABELS Map — erweitern)
    - frontend/src/components/profile/VerifiedBadge.tsx (wie importieren; erwartet keine Pflicht-Props)
    - frontend/src/lib/api.ts (getMemberContributions oder ähnliche Public-Fetch als Vorlage für searchArchive; getFansubs oder ähnliche Public-Fetch als Vorlage für Gruppen-Dropdown)
    - .planning/phases/68-badge-engine-archiv-entdeckung/68-UI-SPEC.md (Surface 1 vollständig, Copywriting-Tabelle)
    - .planning/phases/68-badge-engine-archiv-entdeckung/68-PATTERNS.md (Abschnitte archiv/page.tsx, MemberSearchCard.tsx, api.ts)
  </read_first>
  <action>
    Ergänze frontend/src/lib/api.ts um searchArchive(params: { rolle?, gruppe?, von?, bis?, page? }): Promise<ArchiveSearchResponse>:
    - PUBLIC Fetch (kein authToken) über resolveApiUrl (oder getApiBaseUrl) → /api/v1/archiv?...
    - URLSearchParams für alle filter (nur setzen wenn nicht leer/null)
    - Bei !response.ok: ApiError werfen (analog zu bestehenden public API-Calls)
    - KEIN { next: { revalidate: ... } } Cache-Hint — die Seite nutzt export const dynamic = 'force-dynamic', das macht revalidate wirkungslos; live fetch ohne Cache-Hint
    - Typen-Definition ArchiveSearchResponse und ArchiveMemberRow in der Datei oder @/types/

    Ergänze frontend/src/lib/api.ts um getFansubs() (falls noch nicht vorhanden) — PUBLIC Fetch gegen GET /api/v1/fansubs zum Befüllen des Gruppen-Filter-Dropdowns. Kein Auth nötig (öffentlicher Endpunkt laut RESEARCH.md Befund 6).

    Erstelle frontend/src/components/archive/MemberSearchCard.tsx (per UI-SPEC Surface 1):
    - Props-Type: { id: number; nickname: string; displayName: string; slug: string|null; avatarPath: string|null; isVerified: boolean; topRoles: string[]; groups: string[] }
    - ROLE_LABELS Map für lesbare Rollentexte (translator→"Übersetzung" etc. — aus UI-SPEC)
    - Avatar: img 48×48px, border-radius:50%; Fallback '/placeholder-avatar.png'
    - Rollen: topRoles.slice(0,3) als roleChip spans; bei >3: "+N weitere" span
    - Gruppen: groups.slice(0,2).join(', '); bei >2: " + N weitere" ergänzen
    - VerifiedBadge: import aus @/components/profile/VerifiedBadge; conditional {isVerified && <VerifiedBadge />}
    - Profil-Link: {slug && <Link href={`/members/${slug}`}>Profil ansehen</Link>}
    - CSS-Klassen aus archive.module.css: memberSearchCard, cardHeader, cardAvatar, cardMeta, cardName, cardRoles, roleChip, cardGroups, cardLink

    Erstelle frontend/src/components/archive/archive.module.css (per UI-SPEC):
    - .memberSearchCard { padding: 16px; display: grid; gap: 8px; background: var(--surface-card); border: 1px solid var(--border-subtle); border-radius: 8px; }
    - .cardHeader { display: grid; grid-template-columns: 48px 1fr; gap: 8px; align-items: center; }
    - .cardAvatar { width: 48px; height: 48px; border-radius: 50%; object-fit: cover; }
    - .cardName { font-size: 14px; font-weight: 700; color: var(--text-strong); }
    - .cardRoles { display: flex; flex-wrap: wrap; gap: 4px; }
    - .roleChip { font-size: 13px; font-weight: 700; background: rgba(95,132,221,0.1); border: 1px solid rgba(95,132,221,0.25); border-radius: 999px; padding: 4px 8px; color: var(--text-body); }
    - .cardGroups { font-size: 13px; color: var(--text-soft); }
    - .cardLink { font-size: 14px; color: var(--color-primary); text-decoration: none; }

    Erstelle frontend/src/app/archiv/page.tsx als async Server Component:
    - export const dynamic = 'force-dynamic' — die Seite ist immer live, kein SSG-Caching
    - searchParams: rolle, gruppe, von, bis, page (alle optional)
    - Await searchArchive(params) und await getFansubs() (für Gruppen-Dropdown) — KEIN revalidate-Option da force-dynamic
    - trycatch → fetchError string
    - Render: <main aria-label="Archiv — Fansub-Mitwirkende entdecken">
      * <header><h1>Archiv — Fansub-Mitwirkende entdecken</h1><p>Beschreibung</p></header>
      * Filter-Form (role="search"): Select Rolle (Optionen aus ROLE_OPTIONS), Select Gruppe (Optionen aus getFansubs()-Ergebnis — rendere Name-Optionen; bei Ladefehler leeres Select), zwei number-Inputs Von/Bis, Button "Archiv durchsuchen" (primär), Link/Button "Filter zurücksetzen" → /archiv
      * Filter-Submission: form action="/archiv" method="GET" (URL-State per searchParams — Server Component, kein JS nötig für Submit)
      * Ergebnisse: bei fetchError → ErrorState; bei leer → EmptyState (unterscheide mit/ohne Filter); sonst: .archivGrid mit MemberSearchCard pro Member
      * Pagination: zeige nur bei total > 20; "Zurück"-Link (page-1) und "Weiter"-Link (page+1); "Seite N von M"

    Erstelle frontend/src/app/archiv/page.module.css:
    - .archivPage { max-width: min(1280px, calc(100% - 48px)); margin: 0 auto; padding: 24px 0 48px; }
    - .archivFilters { padding: 16px 24px; background: var(--surface-card); border: 1px solid var(--border-subtle); border-radius: 8px; margin-bottom: 24px; }
    - .archivFilterRow { display: flex; gap: 16px; flex-wrap: wrap; align-items: flex-end; }
    - .archivGrid { display: grid; grid-template-columns: repeat(auto-fill, minmax(260px, 1fr)); gap: 16px; }
    - Responsive: @media (max-width: 600px) { .archivGrid { grid-template-columns: 1fr; } .archivFilterRow { flex-direction: column; } }
    - .archivPagination { display: flex; gap: 16px; align-items: center; justify-content: center; margin-top: 24px; }

    Erweitere frontend/src/components/profile/MemberBadgeChips.tsx (BADGE_LABELS Map ergänzen per UI-SPEC Surface 3):
    - first_contribution: '✦ Erster Beitrag'
    - productive_bronze:  '◈ Produktiv · 10+ Anime'
    - productive_silver:  '◈ Produktiv · 25+ Anime'
    - productive_gold:    '◈ Produktiv · 50+ Anime'
    - all_rounder:        '⬡ Allrounder'
    - verified:           '✓ Verifiziert'
    Kein CSS-Change an .badgeChip (brownfield-Constraint, UI-SPEC Surface 3).

    Alle user-facing Strings auf Deutsch mit korrekten Umlauten (D-17, CLAUDE.md): Übersetzung, Qualitätskontrolle, Projektleitung, Meilenstein etc.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/app/archiv/page.tsx existiert und ist ein async Server Component mit export const dynamic = 'force-dynamic'
    - searchArchive-Fetch enthält KEIN { next: { revalidate: ... } } (force-dynamic macht revalidate wirkungslos)
    - Gruppe-Filter-Select ist implementiert und befüllt sich via getFansubs() (GET /api/v1/fansubs); rendert Gruppen-Optionen (per D-14)
    - MemberSearchCard.tsx rendert Avatar, Name, isVerified-VerifiedBadge, topRoles (max 3), Gruppen (max 2), Profil-Link
    - MemberBadgeChips.tsx BADGE_LABELS enthält alle 6 neuen Badge-Codes mit deutschen Labels
    - api.ts exportiert searchArchive und getFansubs (oder bestehende Funktion für fansubs)
    - /archiv-Seite unterscheidet Leer-mit-Filter vs Leer-ohne-Filter EmptyState (D-14)
    - Alle Strings auf Deutsch ohne ASCII-Umlaut-Ersatzformen (D-17)
    - npm run build fehlerfrei
    - Keine neuen npm-Pakete installiert
  </acceptance_criteria>
  <done>/archiv-Seite vollständig implementiert mit allen drei Filtern (Rolle, Zeitraum, Gruppe); MemberSearchCard-Komponente fertig; Badge-Chip-Labels für 6 neue Codes eingetragen; Frontend-Build grün</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Internet → GET /api/v1/archiv (kein Auth) | Vollständig öffentlich — Sichtbarkeits-Filter ist Sicherheitsgrenze |
| Query-Params (rolle, gruppe, von, bis, page) | Untrusted user input — parametrized queries Pflicht |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-03-01 | Information Disclosure | SearchMembers ohne Sichtbarkeits-Filter | mitigate | WHERE m.profile_visibility='public' AND ac.is_public_on_member_profile=true AND hfgm.visibility='public' in jeder Query-Variante (auch mit gesetzten Filtern) — keine Bedingung darf optional sein; alle drei IMMER in der WHERE-Klausel |
| T-68-03-02 | Denial of Service | page/offset unbegrenzt | mitigate | page < 1 → page = 1; page > 1000 → page = 1000; LIMIT fest auf 20 (archivePageSize) — kein user-kontrollierbares LIMIT |
| T-68-03-03 | Injection | rolle/gruppe/von/bis als SQL-Parameter | mitigate | pgx parameterized queries ($1,$2...) — KEIN fmt.Sprintf mit User-Input; fansubGroupID via strconv.ParseInt (ungültig → 0 = kein Filter) |
| T-68-03-04 | Spoofing | /archiv hinter Admin-Route versehentlich | mitigate | Route in public section von main.go (kein authMiddleware) — Pitfall 6 aus RESEARCH.md; falsches Routing gibt 401 statt 200 |
| T-68-03-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neuen Pakete — kein Audit erforderlich |
</threat_model>

<verification>
Backend: go build ./... fehlerfrei; SearchMembers-Query enthält alle drei Sichtbarkeits-Bedingungen; /api/v1/archiv ohne Auth erreichbar (kein authMiddleware in main.go).
Frontend: npm run build fehlerfrei; /archiv-Seite rendert; MemberBadgeChips BADGE_LABELS hat 9 Einträge (3 bestehend + 6 neu); Gruppe-Filter-Select sichtbar und befüllt.
</verification>

<success_criteria>
- Migration 0092 erstellt beide Indizes (up + down vorhanden)
- GET /api/v1/archiv ohne Token gibt 200 + {data, total, page}
- member mit profile_visibility!='public' erscheint NIE in Response
- page-Param < 1 wird auf 1 normiert; > 1000 wird gekappt
- /archiv Frontend-Seite rendert Karten-Raster mit Filter-Form (Rolle, Zeitraum, Gruppe)
- Gruppe-Filter-Select befüllt sich aus GET /api/v1/fansubs (per D-14)
- MemberBadgeChips BADGE_LABELS enthält first_contribution, productive_bronze, productive_silver, productive_gold, all_rounder, verified
- npm run build + go build ./... beide fehlerfrei
- searchArchive-Fetch hat kein revalidate-Cache-Hint (force-dynamic macht es obsolet)
</success_criteria>

<output>
Erstelle `.planning/phases/68-badge-engine-archiv-entdeckung/68-03-SUMMARY.md` wenn fertig.
</output>

## Truths — Entscheidungs-Abdeckung (CONTEXT.md)

Dieser Plan setzt folgende Entscheidungen um:
- D-13: Neue öffentliche Route `/archiv` als eigenständige Entdeckungs-Seite — Task 1 (Route) + Task 2 (Seite).
- D-14: Filter Rolle/Zeitraum/Gruppe alle optional und UND-verknüpft — Task 1 (WHERE) + Task 2 (Filter-Bar inkl. Gruppe-Select).
- D-15: Suche öffentlich (kein Login), nur öffentlich sichtbare Member/Contributions (drei Visibility-WHERE-Bedingungen) — Task 1.
- D-16: Profil-Karten paginiert (Avatar, Name, verified-Badge, Top-Rollen/Gruppen) — Task 2 (`MemberSearchCard`, Pagination).
- D-17: Alle user-facing Strings auf Deutsch mit korrekten Umlauten; Badge-Chip-Labels der 6 neuen Codes — Task 2.
