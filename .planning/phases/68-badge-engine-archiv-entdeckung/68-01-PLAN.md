---
phase: 68-badge-engine-archiv-entdeckung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/repository/badge_repository.go
  - backend/internal/repository/anime_contributions_upsert_repository.go
  - backend/internal/services/badge_service.go
  - backend/internal/services/badge_backfill_service.go
  - backend/internal/handlers/fansub_anime_contributions_handler.go
  - backend/cmd/migrate/main.go
  - backend/cmd/server/main.go
autonomous: true
requirements:
  - P68-SC1

must_haves:
  truths:
    - "Nach einer bestätigten Contribution erhält der Member `first_contribution` (status=active) in member_badges."
    - "Bei 10/25/50 bestätigten distinct-Anime werden productive_bronze/silver/gold vergeben; sinkt die Zahl unter den Schwellenwert, wird der Badge revoked."
    - "Bei ≥ 3 verschiedenen Rollen in confirmed Contributions wird all_rounder vergeben."
    - "Bei einem verified member_claim wird verified vergeben."
    - "visibility='hidden' eines Badges bleibt nach jedem Recompute unverändert."
    - "`migrate backfill-badges` läuft durch alle Members und ruft ComputeAndStoreBadges auf."
    - "Contribution Delete löst einen Badge-Recompute aus, nachdem die member_id VOR dem Delete gelesen wurde."
  artifacts:
    - path: backend/internal/repository/badge_repository.go
      provides: RevokeMemberBadge(ctx, memberID, badgeCode) — setzt status=revoked nur für status=active
    - path: backend/internal/repository/anime_contributions_upsert_repository.go
      provides: GetMemberIDForContribution(ctx, contributionID) — löst member_id vor Delete auf
    - path: backend/internal/services/badge_service.go
      provides: computeFirstContribution, computeProductiveTiers (3 Stufen), computeAllRounder, computeVerified — alle in ComputeAndStoreBadges integriert
    - path: backend/internal/services/badge_backfill_service.go
      provides: BadgeBackfillService.BackfillAll(ctx) — iteriert alle members, ruft ComputeAndStoreBadges auf
    - path: backend/internal/handlers/fansub_anime_contributions_handler.go
      provides: WithBadgeService() + Badge-Trigger nach Create/Update/Delete
    - path: backend/cmd/migrate/main.go
      provides: case "backfill-badges" → runBackfillBadges(args)
  key_links:
    - from: backend/internal/handlers/fansub_anime_contributions_handler.go
      to: backend/internal/services/badge_service.go
      via: WithBadgeService() + ComputeAndStoreBadgesByMembership / ComputeAndStoreBadges
      pattern: "badgeService != nil"
    - from: backend/internal/services/badge_service.go
      to: backend/internal/repository/badge_repository.go
      via: UpsertMemberBadge / RevokeMemberBadge
      pattern: "s.repo.Upsert|Revoke"
---

<objective>
Badge-Engine vollständig ausbauen: vier neue Badge-Berechnungsfunktionen (first_contribution, productive_bronze/silver/gold, all_rounder, verified) in badge_service.go, RevokeMemberBadge im badge_repository, GetMemberIDForContribution im Contributions-Repository, Badge-Trigger in den Contribution-Handlern (Create/Update/Delete), BadgeBackfillService, und CLI-Subbefehl `backfill-badges`.

Purpose: P68-SC1 — Badge-Engine berechnet alle definierten Badges aus Contributions und aktualisiert member_badges bei Datenänderungen (D-01, D-03, D-06, D-07, D-08).
Output: Erweiterter badge_service, neuer badge_backfill_service, erweiterter badge_repository, neues Repository-Hilfsmittel GetMemberIDForContribution, Badge-Trigger im Contributions-Handler, CLI-Backfill-Subbefehl.
</objective>

<execution_context>
@C:\Users\admin\Documents\Team4s\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\Documents\Team4s\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\admin\Documents\Team4s\.planning\ROADMAP.md
@C:\Users\admin\Documents\Team4s\.planning\phases\68-badge-engine-archiv-entdeckung\68-CONTEXT.md
@C:\Users\admin\Documents\Team4s\.planning\phases\68-badge-engine-archiv-entdeckung\68-RESEARCH.md
@C:\Users\admin\Documents\Team4s\.planning\phases\68-badge-engine-archiv-entdeckung\68-PATTERNS.md

<interfaces>
<!-- Schlüssel-Contracts, die der Executor kennen muss. Aus Codebase extrahiert. -->

From backend/internal/services/badge_service.go (130 lines, vollständig):
```go
type BadgeService struct {
    db   *pgxpool.Pool
    repo *repository.BadgeRepository
}
func NewBadgeService(db *pgxpool.Pool, repo *repository.BadgeRepository) *BadgeService
func (s *BadgeService) ComputeAndStoreBadges(ctx context.Context, memberID int64) error
func (s *BadgeService) ComputeAndStoreBadgesByMembership(ctx context.Context, histMembershipID int64) error
// private: computeFoundingMember, computeHistoricalLeader, computeLongTermMember
```

From backend/internal/repository/badge_repository.go (134 lines):
```go
func (r *BadgeRepository) UpsertMemberBadge(ctx context.Context, memberID int64, badgeCode string, badgeCategory string, derivedFromType string, derivedFromID int64) error
// ON CONFLICT (member_id, badge_code) DO UPDATE SET status='active', awarded_at=NOW() — visibility wird NIE berührt
```

From backend/internal/handlers/fansub_anime_contributions_handler.go (427 lines):
```go
type FansubAnimeContributionsHandler struct {
    contributionsRepo *repository.AnimeContributionsRepository
    rolesRepo         *repository.HistGroupMemberRolesRepository
    permissionSvc     *permissions.Service
    auditLogRepo      *repository.AuditLogRepository
}
func NewFansubAnimeContributionsHandler(contributionsRepo, rolesRepo, permissionSvc, auditLogRepo) *FansubAnimeContributionsHandler
// CreateAnimeContribution (POST) — schreibt item.FansubGroupMemberID in DB
// UpdateAnimeContribution (PATCH) — schreibt item.FansubGroupMemberID in DB
// DeleteAnimeContribution (DELETE) — contributionID aus URL; löscht die Zeile
```

From backend/internal/repository/anime_contributions_upsert_repository.go:
```go
func (r *AnimeContributionsRepository) Delete(ctx context.Context, id int64) error
// Keine GetMemberIDForContribution — muss NEU hinzugefügt werden
```

From backend/cmd/migrate/main.go (185 lines) — Backfill-Pattern:
```go
case "backfill-phase-a-metadata":
    runBackfillPhaseAMetadata(os.Args[2:])
// runBackfillPhaseAMetadata: flag.NewFlagSet, config.Load, database.NewPool, services instanziieren, BackfillAll, report loggen
```

From backend/internal/services/anime_metadata_backfill.go — Service-Pattern:
```go
type AnimeMetadataBackfillReport struct { ... }
type AnimeMetadataBackfillService struct { db *pgxpool.Pool; ... }
func NewAnimeMetadataBackfillService(...) *AnimeMetadataBackfillService
func (s *AnimeMetadataBackfillService) BackfillAll(ctx context.Context) (*AnimeMetadataBackfillReport, error)
```

DB-Schema member_badges (migration 0087):
- CHECK (status IN ('active', 'revoked', 'pending'))
- CHECK (visibility IN ('public', 'internal', 'hidden'))
- UNIQUE (member_id, badge_code)

DB-Schema anime_contributions (migration 0086):
- fansub_group_member_id → hist_fansub_group_members.id
- anime_id, status IN ('draft','proposed','confirmed','disputed','hidden')

DB-Schema anime_contribution_roles (migration 0087):
- UNIQUE (anime_contribution_id, role_code)

DB-Schema member_claims (migration 0081):
- claim_status IN ('pending', 'verified', 'rejected') — KEINE category-Spalte
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RevokeMemberBadge + GetMemberIDForContribution + neue Badge-Compute-Funktionen (mit Tests)</name>
  <files>
    backend/internal/repository/badge_repository.go,
    backend/internal/repository/anime_contributions_upsert_repository.go,
    backend/internal/services/badge_service.go,
    backend/internal/services/badge_service_test.go
  </files>
  <read_first>
    - backend/internal/services/badge_service.go (vollständig lesen — bestehende computeFoundingMember als Vorlage)
    - backend/internal/repository/badge_repository.go (vollständig lesen — UpsertMemberBadge-Pattern für RevokeMemberBadge)
    - backend/internal/repository/anime_contributions_upsert_repository.go (Delete-Methode lesen — Pattern für GetMemberIDForContribution)
    - .planning/phases/68-badge-engine-archiv-entdeckung/68-PATTERNS.md (Abschnitte zu badge_repository und badge_service)
    - .planning/phases/68-badge-engine-archiv-entdeckung/68-RESEARCH.md (Befund 1: Schwellenwerte, Befund 2: Pitfall 1+2+3, Code Examples)
    - Suche nach vorhandenen *_test.go-Dateien in backend/internal/services/ und backend/internal/repository/ um das Testmuster des Projekts zu verstehen (pgxmock, testcontainers oder echte Test-DB via pgxpool)
  </read_first>
  <behavior>
    - RevokeMemberBadge: UPDATE member_badges SET status='revoked' WHERE member_id=$1 AND badge_code=$2 AND status='active' — visibility wird NICHT gesetzt (D-07)
    - GetMemberIDForContribution: SELECT hfgm.member_id FROM anime_contributions ac JOIN hist_fansub_group_members hfgm ON hfgm.id = ac.fansub_group_member_id WHERE ac.id = $1
    - computeFirstContribution: SELECT EXISTS(... WHERE hfgm.member_id=$1 AND ac.status='confirmed') → UpsertMemberBadge("first_contribution","historical_achievement","anime_contribution",rowID) oder RevokeMemberBadge
    - computeProductiveTiers: COUNT(DISTINCT ac.anime_id) WHERE confirmed → Schwellen 10/25/50; jede Stufe einzeln Upsert/Revoke (per D-05)
    - computeAllRounder: COUNT(DISTINCT acr.role_code) WHERE confirmed → Schwelle 3; Upsert/Revoke
    - computeVerified: EXISTS(SELECT 1 FROM member_claims WHERE member_id=$1 AND claim_status='verified') → badge_category='platform', Upsert/Revoke
    - ComputeAndStoreBadges ruft alle 7 compute-Funktionen auf (3 bestehende + 4 neue)
    - Test-Tabelle: member mit N Contributions → prüfe member_badges.status nach ComputeAndStoreBadges
    - Test: visibility='hidden' bleibt nach Recompute unverändert (Pitfall 1 / D-07)
    - Test: RevokeMemberBadge setzt nur active→revoked, lässt pending und hidden unberührt
  </behavior>
  <action>
    Füge RevokeMemberBadge(ctx context.Context, memberID int64, badgeCode string) error zu badge_repository.go hinzu — UPDATE member_badges SET status='revoked' WHERE member_id=$1 AND badge_code=$2 AND status='active'; visibility-Spalte NICHT im SET (per D-07, Pitfall 1 aus RESEARCH.md).

    Füge GetMemberIDForContribution(ctx context.Context, contributionID int64) (int64, error) zu anime_contributions_upsert_repository.go hinzu — SELECT hfgm.member_id JOIN hist_fansub_group_members. Gibt repository.ErrNotFound zurück wenn keine Zeile.

    Erweitere badge_service.go um vier private Methoden (analog zu computeFoundingMember aus PATTERNS.md):
    - computeFirstContribution(ctx, memberID): badge_code="first_contribution", badge_category="historical_achievement", derived_from_type="anime_contribution"
    - computeProductiveTiers(ctx, memberID): zählt COUNT(DISTINCT ac.anime_id); iteriert Tiers [{code:"productive_bronze",threshold:10},{code:"productive_silver",threshold:25},{code:"productive_gold",threshold:50}]; Upsert wenn animeCount>=threshold, Revoke sonst; derived_from_id=0 für gestufte Badges (kein einzelner Quell-Row)
    - computeAllRounder(ctx, memberID): COUNT(DISTINCT acr.role_code) über alle confirmed; Schwelle 3; badge_category="historical_achievement"
    - computeVerified(ctx, memberID): EXISTS(member_claims WHERE claim_status='verified'); badge_category="platform", derived_from_type="member_claim", derived_from_id=0

    In ComputeAndStoreBadges nach den drei bestehenden Aufrufen vier neue Aufrufe ergänzen: s.computeFirstContribution, s.computeProductiveTiers, s.computeAllRounder, s.computeVerified.

    Schreibe badge_service_test.go mit table-driven Tests (testify). Verwende das im Projekt etablierte Test-DB/pgxpool-Pattern: lies zunächst vorhandene *_test.go-Dateien in backend/internal/services/ und backend/internal/repository/, um zu ermitteln ob das Projekt pgxmock, testcontainers oder eine echte Test-DB-Verbindung nutzt. Folge exakt diesem Muster. Source-Inspection (grep auf SQL-Strings) ist kein Ersatz für automatisierte Tests.

    Testfälle:
    - TestComputeFirstContribution: 0 confirmed → revoked, 1 confirmed → active
    - TestComputeProductiveTiers: 9 anime → alle 3 revoked; 10 → bronze active; 25 → bronze+silver active; 50 → alle 3 active; dann 9 → alle 3 revoked
    - TestComputeAllRounder: 2 Rollen → revoked; 3 Rollen → active
    - TestComputeVerified: kein verified claim → revoked; claim_status='verified' → active
    - TestRecomputeKeepsHiddenVisibility: Badge mit visibility='hidden' direkt in DB setzen, dann recompute, prüfe dass visibility='hidden' bleibt
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go build ./internal/services/... ./internal/repository/... && go test ./internal/services/... -run "TestComputeFirstContribution|TestComputeProductiveTiers|TestComputeAllRounder|TestComputeVerified|TestRecomputeKeepsHiddenVisibility|TestRevokeBadge" -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - badge_repository.go enthält RevokeMemberBadge; SQL enthält "AND status = 'active'" und KEIN "visibility"
    - anime_contributions_upsert_repository.go enthält GetMemberIDForContribution
    - badge_service.go enthält computeFirstContribution, computeProductiveTiers, computeAllRounder, computeVerified; alle vier in ComputeAndStoreBadges aufgerufen
    - go build ./internal/services/... ./internal/repository/... läuft fehlerfrei
    - Tests für alle vier neuen Badge-Regeln vorhanden und grün (echte automatisierte Tests — kein Source-Inspection-Ersatz)
  </acceptance_criteria>
  <done>Vier neue Badge-Compute-Funktionen implementiert und in ComputeAndStoreBadges integriert; RevokeMemberBadge und GetMemberIDForContribution im Repository; Tests grün</done>
</task>

<task type="auto">
  <name>Task 2: BadgeBackfillService + CLI-Subbefehl + Contribution-Handler Badge-Trigger</name>
  <files>
    backend/internal/services/badge_backfill_service.go,
    backend/cmd/migrate/main.go,
    backend/internal/handlers/fansub_anime_contributions_handler.go,
    backend/cmd/server/main.go
  </files>
  <read_first>
    - backend/cmd/migrate/main.go (vollständig lesen — case "backfill-phase-a-metadata" + runBackfillPhaseAMetadata als exaktes Pattern)
    - backend/internal/services/anime_metadata_backfill.go (Struct + BackfillAll-Signatur als Vorlage für BadgeBackfillService)
    - backend/internal/handlers/fansub_anime_contributions_handler.go (vollständig lesen — struct, konstruktor, CreateAnimeContribution, UpdateAnimeContribution, DeleteAnimeContribution)
    - backend/cmd/server/main.go (Wiring-Abschnitt für FansubAnimeContributionsHandler lesen — wie er instanziiert wird)
    - .planning/phases/68-badge-engine-archiv-entdeckung/68-PATTERNS.md (Abschnitte badge_backfill_service, cmd/migrate/main.go, fansub_anime_contributions_handler.go)
    - .planning/phases/68-badge-engine-archiv-entdeckung/68-RESEARCH.md (Befund 2: Delete-Trigger-Pattern, Pitfall 2)
  </read_first>
  <action>
    Erstelle backend/internal/services/badge_backfill_service.go neu:
    - Struct BadgeBackfillService mit Feldern db *pgxpool.Pool, badgeService *BadgeService
    - Struct BadgeBackfillReport mit MembersProcessed int, Errors []string
    - NewBadgeBackfillService(db, badgeService) Konstruktor
    - BackfillAll(ctx): Query "SELECT id FROM members ORDER BY id", iteriere rows, rufe s.badgeService.ComputeAndStoreBadges(ctx, memberID) auf; Fehler in report.Errors sammeln, nie abbrechen; return report, nil (rows.Err() prüfen)

    Erweitere backend/cmd/migrate/main.go:
    - Im switch-Block neuen case "backfill-badges": runBackfillBadges(os.Args[2:]) einfügen (VOR dem default-case)
    - In der usage-Ausgabe Zeile "migrate backfill-badges [-database-url url]" ergänzen
    - Neue Funktion runBackfillBadges(args []string) nach dem Pattern von runBackfillPhaseAMetadata: flag.NewFlagSet, database-url, cfg, dbPool, badgeRepo := repository.NewBadgeRepository(dbPool), badgeSvc := services.NewBadgeService(dbPool, badgeRepo), backfillSvc := services.NewBadgeBackfillService(dbPool, badgeSvc), backfillSvc.BackfillAll(ctx), report loggen

    Erweitere FansubAnimeContributionsHandler in fansub_anime_contributions_handler.go:
    - Füge Feld badgeService *services.BadgeService zum Struct hinzu
    - Füge Methode WithBadgeService(svc *services.BadgeService) *FansubAnimeContributionsHandler hinzu (setzt h.badgeService = svc, gibt h zurück)
    - In CreateAnimeContribution: nach dem Audit-Log-Write, wenn h.badgeService != nil: go-Routine NICHT nötig — synchroner Aufruf _ = h.badgeService.ComputeAndStoreBadgesByMembership(c.Request.Context(), item.FansubGroupMemberID)
    - In UpdateAnimeContribution: analog nach dem Audit-Log-Write
    - In DeleteAnimeContribution: VOR dem Delete-Aufruf: memberID, _ := h.contributionsRepo.GetMemberIDForContribution(c.Request.Context(), contributionID); nach dem Delete: if memberID > 0 && h.badgeService != nil { _ = h.badgeService.ComputeAndStoreBadges(c.Request.Context(), memberID) }

    DATEILIMIT-Check: fansub_anime_contributions_handler.go hat aktuell 427 Zeilen. Die Erweiterungen (WithBadgeService ~5 Zeilen, Trigger-Calls je ~5 Zeilen) halten die Datei unter 450 Zeilen.

    Verdrahte in backend/cmd/server/main.go: suche die Instanziierung von FansubAnimeContributionsHandler (NewFansubAnimeContributionsHandler); ergänze .WithBadgeService(badgeService) direkt nach dem Konstruktor-Aufruf. badgeService muss an dieser Stelle bereits instanziiert sein (badge_service.go Konstruktor, badgeRepo bereits vorhanden in main.go laut Konvention).
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go build ./... 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - backend/internal/services/badge_backfill_service.go existiert und compiliert
    - backend/cmd/migrate/main.go enthält case "backfill-badges" und runBackfillBadges
    - fansub_anime_contributions_handler.go enthält WithBadgeService, badgeService-Feld, Trigger-Aufrufe nach Create/Update/Delete
    - DeleteAnimeContribution liest member_id VOR dem Delete-Aufruf (Pitfall 2 aus RESEARCH.md)
    - go build ./... fehlerfrei
    - cmd/server/main.go verdrahtet .WithBadgeService(badgeService)
  </acceptance_criteria>
  <done>BadgeBackfillService und CLI-Subbefehl implementiert; Contribution-Handler löst Badge-Recompute bei Create/Update/Delete aus; Gesamtes Backend compiliert</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Contribution-Handler → Badge-Service | Fehlende Badges sind kein Sicherheitsrisiko — Fehler werden geloggt, nicht an Client weitergegeben |
| CLI backfill-badges → Datenbank | Nur über DATABASE_URL erreichbar — keine HTTP-Oberfläche, kein Auth-Gate nötig |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01-01 | Tampering | RevokeMemberBadge SQL | mitigate | WHERE status='active' verhindert versehentliches Revoken von pending-Badges; kein visibility-UPDATE (D-07) |
| T-68-01-02 | Elevation of Privilege | backfill-badges CLI | accept | Nur für Admins mit DB-Zugriff; kein HTTP-Endpunkt — kein öffentlicher Angriffspfad |
| T-68-01-03 | Denial of Service | BackfillAll über große Members-Tabelle | accept | Community-Plattform-Skala; Timeout via context.WithTimeout(10min) in runBackfillBadges |
| T-68-01-SC | Tampering | npm/pip/cargo installs | mitigate | Keine neuen Pakete in Plan 01 — kein Package-Legitimacy-Audit erforderlich |
</threat_model>

<verification>
Backend compiliert fehlerfrei (go build ./...). Tests für alle vier neuen Badge-Regeln grün (echte automatisierte Tests). RevokeMemberBadge-SQL enthält kein visibility. DeleteAnimeContribution liest member_id vor dem Delete.
</verification>

<success_criteria>
- go build ./... fehlerfrei
- go test ./internal/services/... -run "TestCompute|TestRevoke|TestRecompute" grün
- fansub_anime_contributions_handler.go triggert Badge-Recompute bei Create/Update/Delete
- GetMemberIDForContribution in Repository vorhanden
- badge_backfill_service.go existiert
- migrate-Binary akzeptiert "backfill-badges" als Subbefehl (go build ./cmd/migrate fehlerfrei)
</success_criteria>

<output>
Erstelle `.planning/phases/68-badge-engine-archiv-entdeckung/68-01-SUMMARY.md` wenn fertig.
</output>
