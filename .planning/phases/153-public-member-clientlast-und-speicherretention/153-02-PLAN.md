---
phase: 153
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/profile/MemberStorySection.tsx
  - frontend/src/components/profile/MemberStorySection.test.tsx
  - frontend/src/components/profile/MemberGroupsHistorySection.tsx
  - frontend/src/components/public/PublicNoteCard.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNotesSection.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNotesSection.test.tsx
  - frontend/src/components/editor/index.ts
  - frontend/src/app/me/profile/components/ProfileStoryCard.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNoteWorkspace.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/NotesTab.helpers.tsx
autonomous: true
requirements: [P153-04, P153-05]

must_haves:
  truths:
    - "MemberStorySection, MemberGroupsHistorySection, PublicNoteCard, and AnimeProjectNotesSection import RichTextRenderer directly from its own module, never through the editor barrel."
    - "The editor barrel (index.ts) no longer re-exports RichTextRenderer, so a future accidental `import { RichTextRenderer } from '@/components/editor'` fails to compile instead of silently reintroducing the Tiptap import graph."
    - "The three legitimate dual-symbol consumers (ProfileStoryCard, AnimeProjectNoteWorkspace, NotesTab.helpers) still compile and render both RichTextEditor (via the barrel) and RichTextRenderer (via its direct module) with zero behavioral change."
    - "Rendered rich text output (HTML, sanitization, Phase 152 link contract) is byte-identical to before this change in all eight touched/verified consumers."
  artifacts:
    - path: "frontend/src/components/editor/index.ts"
      provides: "Editor barrel scoped to real editor consumers only (RichTextEditor, ColorTokenExtension, COLOR_TOKENS); documented B3 decision"
    - path: "frontend/src/components/profile/MemberStorySection.tsx"
      provides: "Direct RichTextRenderer import, no barrel dependency"
  key_links:
    - from: "frontend/src/components/profile/MemberStorySection.tsx"
      to: "frontend/src/components/editor/RichTextRenderer.tsx"
      via: "direct module import"
      pattern: "from '@/components/editor/RichTextRenderer'"
    - from: "frontend/src/app/me/profile/components/ProfileStoryCard.tsx"
      to: "frontend/src/components/editor/index.ts"
      via: "RichTextEditor import (barrel stays authoritative for real editor consumers)"
      pattern: "RichTextEditor.*from '@/components/editor'"
---

<objective>
Close half of RCA-02 (public import graph pulling the full Tiptap/ProseMirror editor into
renderer-only consumers). Per B1/B2 (locked, 153-USER-REQUEST.md), the four confirmed
renderer-only public/admin consumers import `RichTextRenderer` directly from its own module
instead of through `@/components/editor`'s barrel. Per B3 (locked — a decision must be made and
documented, not silently chosen), this plan additionally removes `RichTextRenderer` from the
barrel's export list entirely (a structural, compile-time guard against this defect recurring),
which requires updating the three legitimate dual-symbol consumers to import `RichTextRenderer`
directly alongside their unchanged barrel `RichTextEditor` import.

Purpose: `RichTextRenderer.tsx` itself has zero Tiptap/ProseMirror dependency — the only reason
renderer-only consumers pulled in 38 Tiptap modules (1.187 MB) + 11 ProseMirror modules (1.790 MB)
+ 10 editor modules (0.172 MB) was the shared barrel. This plan's fix, combined with Plan 03's
not-found.tsx loading boundary, is what the audit's A/B measurement showed saves 1.653 MB / 26.1%
of total public JS transfer.
Output: four renderer-only consumers on direct imports; barrel scoped to real editor consumers;
documented, reasoned B3 decision recorded in `editor/index.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-public-member-clientlast-und-speicherretention/153-USER-REQUEST.md
@.planning/phases/153-public-member-clientlast-und-speicherretention/153-RESEARCH.md
@.planning/phases/153-public-member-clientlast-und-speicherretention/153-PATTERNS.md

<interfaces>
Current barrel, full file, verified this session (`frontend/src/components/editor/index.ts`):

```ts
export { RichTextEditor } from './RichTextEditor'
export { RichTextRenderer } from './RichTextRenderer'
export { ColorTokenExtension, COLOR_TOKENS } from './ColorTokenExtension'
export type { ColorToken } from './ColorTokenExtension'
```

`RichTextRenderer.tsx` (full file, verified this session — has zero Tiptap import, never touched
by this plan):

```tsx
import styles from './RichTextRenderer.module.css'

type RichTextRendererProps = {
  bodyHtml?: string | null
  bodyJson?: unknown | null
  editorType?: string | null
  contentSchemaVersion?: number | null
}

// SICHERHEITSINVARIANTE: dangerouslySetInnerHTML NUR mit body_html (serverseitig sanitisiert).
// Niemals body_json direkt rendern ohne serverseitiges Sanitizing.
export function RichTextRenderer({ bodyHtml }: RichTextRendererProps) {
  if (!bodyHtml?.trim()) return null
  return (
    <div className={styles.richTextOutput} dangerouslySetInnerHTML={{ __html: bodyHtml }} />
  )
}
```

Confirmed this session (repo-wide grep) — exactly these ten consumers import from
`@/components/editor`, no others:

| Consumer | Imports | Role |
|---|---|---|
| `frontend/src/components/profile/MemberStorySection.tsx:5` | `RichTextRenderer` only | renderer-only — **fix** |
| `frontend/src/components/profile/MemberGroupsHistorySection.tsx:10` | `RichTextRenderer` only | renderer-only — **fix** |
| `frontend/src/components/public/PublicNoteCard.tsx:7` | `RichTextRenderer` only | renderer-only — **fix** |
| `frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNotesSection.tsx:6` | `RichTextRenderer` only | renderer-only — **fix** |
| `frontend/src/app/me/profile/components/ProfileStoryCard.tsx:1` | `RichTextEditor, RichTextRenderer` | dual — **split into 2 imports** |
| `frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNoteWorkspace.tsx:5` | `RichTextEditor, RichTextRenderer` | dual — **split into 2 imports** |
| `frontend/src/app/admin/fansubs/[id]/edit/NotesTab.helpers.tsx:5` | `RichTextEditor, RichTextRenderer` | dual — **split into 2 imports** |
| `frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNoteForm.tsx:6` | `RichTextEditor` only | editor-only — do not touch |
| `frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionNotesTab.tsx:6` | `RichTextEditor` only | editor-only — do not touch |
| `frontend/src/app/dev/ui-system/showcase/ProjectNoteShowcase.tsx:7` | `RichTextEditor` only | editor-only — do not touch |

Existing test mocks that target the barrel and must be extended (Pitfall 4 — a change to the
import specifier without a matching mock silently stops exercising the intended component):
- `frontend/src/components/profile/MemberStorySection.test.tsx:8-12` — `vi.mock('@/components/editor', () => ({ RichTextRenderer: ... }))`
- `frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNotesSection.test.tsx:13-33` — `vi.mock('@/components/editor', () => ({ RichTextEditor: ..., RichTextRenderer: ... }))`
`frontend/src/components/public/PublicNoteCard.test.tsx` has no `vi.mock('@/components/editor', ...)`
at all — it exercises the real component already, unaffected by the import-path change.
`MemberGroupsHistorySection.tsx` has no `.test.tsx` file at all — nothing to update there beyond
the source import.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Point the four renderer-only consumers at the direct RichTextRenderer module</name>
  <files>
    frontend/src/components/profile/MemberStorySection.tsx,
    frontend/src/components/profile/MemberStorySection.test.tsx,
    frontend/src/components/profile/MemberGroupsHistorySection.tsx,
    frontend/src/components/public/PublicNoteCard.tsx,
    frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNotesSection.tsx,
    frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNotesSection.test.tsx
  </files>
  <read_first>
    frontend/src/components/profile/MemberStorySection.tsx (full file — line 5 is the import to change)
    frontend/src/components/profile/MemberStorySection.test.tsx (full file — line 8-12 is the mock to extend)
    frontend/src/components/profile/MemberGroupsHistorySection.tsx (full file — line 10 is the import to change)
    frontend/src/components/public/PublicNoteCard.tsx (full file — line 7 is the import to change)
    frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNotesSection.tsx (full file — line 6 is the import to change)
    frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNotesSection.test.tsx (full file — lines 13-33 is the mock to extend)
    frontend/src/components/editor/RichTextRenderer.tsx (full file, 12 lines — confirm zero Tiptap dependency, do not edit)
  </read_first>
  <behavior>
    - `MemberStorySection`, `MemberGroupsHistorySection`, `PublicNoteCard`, `AnimeProjectNotesSection`
      each render identical output before and after the import-path change (same props in, same
      rendered HTML/text out) — this task changes only the import specifier, never the components'
      logic, JSX, or props.
    - `MemberStorySection.test.tsx` and `AnimeProjectNotesSection.test.tsx` continue to exercise a
      mocked `RichTextRenderer` (proving the mock still intercepts after the import-path change,
      not silently falling through to `undefined`).
  </behavior>
  <action>
    In each of the four consumer files, change `import { RichTextRenderer } from '@/components/editor'`
    to `import { RichTextRenderer } from '@/components/editor/RichTextRenderer'`. Do not touch any
    other import, prop, or JSX in these four files.
    In `MemberStorySection.test.tsx`, change the existing `vi.mock('@/components/editor', () => ({
    RichTextRenderer: ... }))` block's module specifier to `'@/components/editor/RichTextRenderer'`
    (keep the exact same mock factory body — only the string literal passed to `vi.mock` changes).
    In `AnimeProjectNotesSection.test.tsx`, the existing `vi.mock('@/components/editor', () => ({
    RichTextEditor: ..., RichTextRenderer: ... }))` mocks a symbol this component never imports
    (`RichTextEditor`) alongside the one it does (`RichTextRenderer`) — since `AnimeProjectNotesSection.tsx`
    only imports `RichTextRenderer`, move just the `RichTextRenderer` mock factory into a new
    `vi.mock('@/components/editor/RichTextRenderer', () => ({ RichTextRenderer: ... }))` block and
    remove the now-unused `RichTextEditor` mock entry from the barrel mock (or delete the barrel
    mock block entirely if nothing else in this file imports from the barrel — confirm via the
    read_first file which other imports exist before removing).
    `PublicNoteCard.tsx` has no matching test-file mock to update (confirmed this session);
    `MemberGroupsHistorySection.tsx` has no `.test.tsx` file at all — change only its source import.
  </action>
  <acceptance_criteria>
    - All four consumer files import `RichTextRenderer` from `'@/components/editor/RichTextRenderer'`,
      not `'@/components/editor'`.
    - `grep -rn "RichTextRenderer.*from '@/components/editor'$" frontend/src/components/profile/MemberStorySection.tsx frontend/src/components/profile/MemberGroupsHistorySection.tsx frontend/src/components/public/PublicNoteCard.tsx "frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNotesSection.tsx"` returns zero matches (i.e., none of the four still import the barrel).
    - `MemberStorySection.test.tsx` and `AnimeProjectNotesSection.test.tsx` both pass with the
      mocked `RichTextRenderer` still intercepted (assert via existing `data-testid="rich-text-renderer"`
      queries already present in those suites — confirm they still resolve, proving the mock is live,
      not silently bypassed).
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/profile/MemberStorySection.test.tsx src/app/admin/fansubs/\[id\]/edit/AnimeProjectNotesSection.test.tsx src/components/public/PublicNoteCard.test.tsx"</automated>
  </verify>
  <done>All four renderer-only consumers import directly; both dependent test suites pass with their mocks confirmed live.</done>
</task>

<task type="auto">
  <name>Task 2: Split RichTextRenderer out of the barrel (B3 decision) and update the three dual-symbol consumers</name>
  <files>
    frontend/src/components/editor/index.ts,
    frontend/src/app/me/profile/components/ProfileStoryCard.tsx,
    frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNoteWorkspace.tsx,
    frontend/src/app/admin/fansubs/[id]/edit/NotesTab.helpers.tsx
  </files>
  <read_first>
    frontend/src/components/editor/index.ts (full file, 4 lines)
    frontend/src/app/me/profile/components/ProfileStoryCard.tsx (line 1, the dual import)
    frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNoteWorkspace.tsx (line 5, the dual import)
    frontend/src/app/admin/fansubs/[id]/edit/NotesTab.helpers.tsx (line 5, the dual import)
  </read_first>
  <action>
    B3 decision (document verbatim as a comment block at the top of `index.ts`, above the export
    lines): the barrel is split — `RichTextRenderer` is removed from `index.ts`'s exports entirely.
    The barrel (`@/components/editor`) keeps only `RichTextEditor`, `ColorTokenExtension`, and
    `COLOR_TOKENS` — symbols that genuinely require the Tiptap/ProseMirror editor bundle. Rationale
    to record in the comment: RCA-02 showed that a shared barrel exporting both a heavy
    (Tiptap-dependent) and a light (dependency-free) symbol lets any future renderer-only import
    silently reintroduce the entire editor bundle with a single `import { RichTextRenderer } from
    '@/components/editor'` line; removing the light symbol from the barrel converts that mistake
    from a runtime bundle-size regression (invisible until measured) into a compile-time TypeScript
    error, which is a stronger and more durable regression guard than a lint rule or a periodic
    audit-script run. `RichTextRenderer.tsx` itself is not moved, renamed, or duplicated — only its
    barrel re-export is removed; direct imports from `@/components/editor/RichTextRenderer` are
    unaffected and remain the one and only public entry point to this component.
    Edit `frontend/src/components/editor/index.ts` to remove the
    `export { RichTextRenderer } from './RichTextRenderer'` line, keeping the other three export
    lines unchanged.
    In each of the three dual-symbol consumers (`ProfileStoryCard.tsx`,
    `AnimeProjectNoteWorkspace.tsx`, `NotesTab.helpers.tsx`), split the single
    `import { RichTextEditor, RichTextRenderer } from '@/components/editor'` line into two lines:
    `import { RichTextEditor } from '@/components/editor'` and
    `import { RichTextRenderer } from '@/components/editor/RichTextRenderer'`. Preserve the existing
    import-ordering convention used elsewhere in each file (place the new second import adjacent to
    the first, following whatever import-grouping style the file already uses). No other line in
    these three files changes.
  </action>
  <acceptance_criteria>
    - `frontend/src/components/editor/index.ts` no longer contains
      `export { RichTextRenderer } from './RichTextRenderer'`, still exports `RichTextEditor`,
      `ColorTokenExtension`, `COLOR_TOKENS`, and `ColorToken` (the type), and carries a comment
      documenting the B3 decision and its rationale.
    - `ProfileStoryCard.tsx`, `AnimeProjectNoteWorkspace.tsx`, `NotesTab.helpers.tsx` each import
      `RichTextEditor` from `'@/components/editor'` and `RichTextRenderer` from
      `'@/components/editor/RichTextRenderer'` on two separate import statements.
    - `docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit"` reports zero
      new TypeScript errors attributable to any of the ten consumers named in this plan's context
      (a missing-export error on `RichTextRenderer` from the barrel would appear here if any
      consumer was missed).
    - The three editor-only consumers (`AnimeProjectNoteForm.tsx`, `ReleaseVersionNotesTab.tsx`,
      `ProjectNoteShowcase.tsx`) are untouched and still compile (they never imported
      `RichTextRenderer`, so removing it from the barrel cannot affect them — confirm with a
      targeted grep, do not silently assume).
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit 2>&1 | grep -iE 'RichTextRenderer|components/editor' || echo NO_MATCHES"</automated>
  </verify>
  <done>Barrel scoped to editor-only symbols with a documented rationale; all three dual-symbol consumers compile against two explicit import lines; zero new typecheck errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Server-sanitized HTML rendering | `RichTextRenderer` renders `bodyHtml` (a server-sanitized string) via `dangerouslySetInnerHTML` in four public/admin surfaces; this plan changes only which module path resolves the symbol, never the sanitization contract itself. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153-02-01 | Tampering (XSS) | `RichTextRenderer.tsx`'s `dangerouslySetInnerHTML={{ __html: bodyHtml }}` | mitigate | `RichTextRenderer.tsx` itself is explicitly not edited by this plan (its `SICHERHEITSINVARIANTE` comment and its exclusive reliance on server-sanitized `bodyHtml`, never `bodyJson`, stay byte-identical) — verified via `git diff` showing zero changes to that file after this plan completes |
| T-153-02-02 | Tampering (regression reintroduction) | `frontend/src/components/editor/index.ts` | mitigate | Removing `RichTextRenderer` from the barrel converts a future accidental barrel-wide import back into a compile-time TypeScript error rather than a silent runtime bundle-size regression — this is the B3 decision's explicit purpose |
</threat_model>

<verification>
- `vitest run src/components/profile/MemberStorySection.test.tsx src/app/admin/fansubs/[id]/edit/AnimeProjectNotesSection.test.tsx src/components/public/PublicNoteCard.test.tsx` — 0 failures.
- `npx tsc --noEmit` — zero new errors on any of the ten named `@/components/editor` consumers.
- `git diff --stat frontend/src/components/editor/RichTextRenderer.tsx` shows no changes.
</verification>

<success_criteria>
- All four renderer-only consumers use the direct `RichTextRenderer` module path.
- The barrel no longer exports `RichTextRenderer`; the B3 decision and its rationale are documented
  in `index.ts`.
- The three dual-symbol consumers compile against two explicit import statements each, with zero
  behavioral change to what they render.
- `RichTextRenderer.tsx` itself is untouched.
</success_criteria>

<output>
Create `.planning/phases/153-public-member-clientlast-und-speicherretention/153-02-SUMMARY.md` when done.
</output>
