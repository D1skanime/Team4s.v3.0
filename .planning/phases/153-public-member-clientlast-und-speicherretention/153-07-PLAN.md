---
phase: 153
plan: 07
type: execute
wave: 3
depends_on: ["153-01", "153-02", "153-03", "153-04", "153-05", "153-06"]
files_modified:
  - frontend/src/components/editor/__tests__/publicImportGraph.test.ts
  - docs/audits/2026-09-09-public-member-performance/153-AFTER.md
autonomous: false
requirements: [P153-07, P153-11, P153-12, P153-13, P153-14]

must_haves:
  truths:
    - "No Tiptap, ProseMirror, or RichTextEditor module appears in the public DEV import graph for the member or group profile route, measured with audit-public-member-bundles.mjs."
    - "A new dated audit document under docs/audits/2026-09-09-public-member-performance/ records before/after measurements from all three committed audit scripts under identical report conditions, without modifying the existing REPORT.md."
    - "The full frontend regression suite, typecheck, and lint are green; docker compose build (not exec ... npm run build) exits 0."
    - "RCA-04 (the reported Chrome tab crash) is explicitly documented as still open and unreproduced; no document produced by this phase claims it is fixed."
    - "The three named pre-existing, phase-foreign defects (anime/page.tsx searchParams typecheck, admin/anime/[id]/edit/page.tsx formatEditLoadError build break, 13 ESLint errors/332 warnings) are named with file:line evidence and explicitly scoped out, not silently touched."
  artifacts:
    - path: "docs/audits/2026-09-09-public-member-performance/153-AFTER.md"
      provides: "Before/after measurement document for RCA-01/02/03, D4/D5 documentation, sibling to (not overwriting) REPORT.md"
    - path: "frontend/src/components/editor/__tests__/publicImportGraph.test.ts"
      provides: "Lightweight permanent regression guard for the two structural regressions (sizes=\"auto\" reintroduction, barrel-wide RichTextEditor import in renderer-only files)"
  key_links:
    - from: "docs/audits/2026-09-09-public-member-performance/153-AFTER.md"
      to: "docs/audits/2026-09-09-public-member-performance/REPORT.md"
      via: "sibling document referencing the same baseline measurements, never overwriting them"
      pattern: "REPORT.md"
---

<objective>
Close Workstream D (verification and explicit scope boundaries) once Plans 01-06 have landed the
three P1 root-cause corrections. Per D1-D5 (locked, 153-USER-REQUEST.md): measure the corrected
codebase under the exact same report conditions as the original audit, using the same committed
scripts, writing results into a **new** document rather than overwriting `REPORT.md`; prove the
full regression suite and production build are green; evaluate and add a lightweight permanent
regression guard (D2); and explicitly, honestly bound this phase's claims — RCA-04 stays open, and
the three pre-existing phase-foreign defects (D5) are named with evidence, not silently touched.

Purpose: this is the phase's honesty and completeness gate. Every prior plan's fix must now be
proven together, against the same measurement instruments that established the original baseline,
and the phase's scope boundaries (what it does NOT claim to have fixed) must be as explicit and
evidenced as what it does claim.
Output: a new dated audit document with before/after numbers for RCA-01/02/03; a lightweight
static regression guard; a fully green regression/build gate; explicit D4/D5 documentation; a
final live sanity checkpoint.
</objective>

<container_hygiene>
**Verbindlich vor jedem Playwright-Messlauf dieses Plans.** Zurueckgebliebene
Chromium-Prozesse im Frontend-Container haengen den naechsten Lauf, und ein kalter
Image-Cache verfaelscht die erste Messung:

```
docker restart team4sv30-frontend
# Warten bis der Dev-Server antwortet, dann den Image-Cache waermen:
curl -sS -o /dev/null "http://127.0.0.1:3000/members/timer"
curl -sS -o /dev/null "http://127.0.0.1:3000/members/kara"
curl -sS -o /dev/null "http://127.0.0.1:3000/fansubs/new-subs"
```

Ein Lauf, der ohne diesen Schritt haengt oder unplausible Zahlen liefert, ist zu
wiederholen und nicht als Messergebnis zu berichten.
</container_hygiene>


<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-public-member-clientlast-und-speicherretention/153-USER-REQUEST.md
@.planning/phases/153-public-member-clientlast-und-speicherretention/153-RESEARCH.md
@.planning/phases/153-public-member-clientlast-und-speicherretention/153-PATTERNS.md
@.planning/phases/153-public-member-clientlast-und-speicherretention/153-VALIDATION.md
@docs/audits/2026-09-09-public-member-performance/REPORT.md
@docs/audits/2026-09-09-public-member-performance/REPRODUCE.md

<interfaces>
Three committed audit scripts (all under `frontend/scripts/`), reused as-is, never rewritten:
- `audit-public-member-bundles.mjs` — categorizes `tiptap`/`prosemirror`/`editor` module buckets
  from `.next` compiled output; baseline (report conditions): 353 kB transferred / 1,155 kB
  decompressed for the member profile route vs 187/632 kB for the group route.
- `audit-public-member-navigation-retention.mjs` — supports `AUDIT_CYCLES` and `AUDIT_LABEL` env
  vars; already run at 12 and 50 cycles in Plan 01 Task 2 (reuse those recorded numbers as part of
  this plan's "before" reference where applicable, and re-run once more here against the fully
  merged state of all six plans for the authoritative combined after-measurement).
- `audit-public-member-visibility.mjs` — throttled (1.6 Mbit/s, CPU×4) visibility timing against
  `timer`, `kara`, and the group page (`new-subs`), compared against
  `visibility-members-timer.json`, `visibility-members-kara.json`, `visibility-fansubs-new-subs.json`.

Exact invocation commands (`REPRODUCE.md`, reused verbatim):
```
docker compose exec -T team4sv30-frontend node scripts/audit-public-member-bundles.mjs
docker compose exec -T -e AUDIT_LABEL=<label> -e AUDIT_CYCLES=<n> team4sv30-frontend node scripts/audit-public-member-navigation-retention.mjs
docker compose exec -T team4sv30-frontend node scripts/audit-public-member-visibility.mjs
```

D5's three confirmed pre-existing, phase-foreign defects (verified this session, cite exactly, do
not re-derive or "helpfully" fix):
- `frontend/src/app/anime/page.tsx:18,56-58` — `searchParams` typed as a union of a plain object
  and `Promise<...>` in a way that trips the Next 16 `PageProps` typecheck.
- `frontend/src/app/admin/anime/[id]/edit/page.tsx:26` — exports `formatEditLoadError` (a
  non-component, non-metadata named export) from a page file; Next.js's page-export validation
  rejects this at build time.
- ESLint: 13 errors, 332 warnings, 345 problems total in a full-repo lint run (confirmed this
  session, matches the audit's own count).
If any of these blocks a step below, treat it as a named, justified exception in the new audit
document — never work around it silently, never claim it is fixed.

D2 discretionary regression-guard shape (RESEARCH.md Open Question 3 recommendation, and the
legitimate "absence" exception to CLAUDE.md's Teststil rule — see PATTERNS.md "Shared Patterns →
Regression-guard convention"): a source-text **absence** check ("this identifier/string must never
appear in this specific file") is the one sanctioned use of `readFileSync`+string inspection under
CLAUDE.md's Teststil carve-out; asserting what a component *renders* still requires
`render`/`screen`, which is why this guard is scoped to pure absence checks only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full regression/build gate + lightweight permanent regression guard</name>
  <files>frontend/src/components/editor/__tests__/publicImportGraph.test.ts</files>
  <read_first>
    frontend/src/components/editor/index.ts (as landed by Plan 02 — confirm it no longer exports
    `RichTextRenderer`)
    frontend/src/components/profile/AchievementArtwork.tsx (as landed by Plan 01 — confirm the
    `sizes` computation)
    .planning/phases/153-public-member-clientlast-und-speicherretention/153-VALIDATION.md (Wave 0
    Requirements section, for the exact scope of this guard)
  </read_first>
  <action>
    Create `frontend/src/components/editor/__tests__/publicImportGraph.test.ts` as a static,
    fast, browser-independent Vitest guard using `node:fs`'s `readFileSync` under the CLAUDE.md
    Teststil "absence" carve-out (an identifier/string that must appear NOWHERE in a named file —
    this is the one legitimate use of source-text scanning, not a substitute for behavior
    assertions). It must assert two things, each against the same four renderer-only consumer file
    paths named in Plan 02 (`MemberStorySection.tsx`, `MemberGroupsHistorySection.tsx`,
    `PublicNoteCard.tsx`, `AnimeProjectNotesSection.tsx`): (1) none of the four files' source text
    contains the substring `from '@/components/editor'` (proving no renderer-only consumer has
    regressed back onto the barrel), and (2) `AchievementArtwork.tsx`'s source text does not
    contain the substring `` `auto, ` `` (proving the `sizes="auto"` defect has not been
    reintroduced). Do not assert anything about rendered output in this file — that is already
    covered by each component's own `.test.tsx` suite; this guard exists solely as a fast structural
    tripwire for the two specific regressions this phase fixed.
    Then run the full regression/build gate in this exact order, recording each command's outcome:
    `docker compose exec -T team4sv30-frontend sh -c "cd /app && npm test"`,
    `docker compose exec -T team4sv30-frontend sh -c "cd /app && npm run typecheck"`,
    `docker compose exec -T team4sv30-frontend sh -c "cd /app && npm run lint"`,
    `docker compose build` (run from the repository root on the host, per the operating
    environment's binding constraint — never `docker compose exec ... npm run build`, whose
    `.next` dev volume reports false prerender errors per this phase's own RESEARCH.md Pitfall 5).
    If `npm run typecheck`/`npm run lint` report the D5-named pre-existing failures (and only
    those), do not fix them — record their presence as confirmed-still-present, phase-foreign, and
    out of scope, citing this plan's own D5 evidence above. If any OTHER failure appears (i.e. one
    not already named in D5 and not attributable to one of this phase's six plans), stop and flag
    it — do not silently absorb an unexplained new failure into the "pre-existing" bucket.
  </action>
  <acceptance_criteria>
    - `publicImportGraph.test.ts` exists, uses `readFileSync`-based absence checks only (no
      behavior/render assertions), and passes.
    - `npm test` (full suite) exits 0 with all tests green except any explicitly-named pre-existing
      flaky/unrelated failures already documented in prior phases' STATE.md entries (cite which, if
      any, rather than silently accepting an unexplained red).
    - `npm run typecheck` and `npm run lint` report exactly the D5-named pre-existing issues (and no
      new ones attributable to this phase's six plans).
    - `docker compose build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/editor/__tests__/publicImportGraph.test.ts && npm test"</automated>
  </verify>
  <done>Regression guard created and passing; full suite, typecheck, lint, and docker compose build all confirmed green modulo the explicitly named D5 pre-existing exceptions.</done>
</task>

<task type="auto">
  <name>Task 2: Before/after audit document (D1) with explicit D4/D5 boundaries</name>
  <files>docs/audits/2026-09-09-public-member-performance/153-AFTER.md</files>
  <read_first>
    docs/audits/2026-09-09-public-member-performance/REPORT.md (structure/section-header convention
    to mirror — do not overwrite this file, do not edit it)
    docs/audits/2026-09-09-public-member-performance/REPRODUCE.md (exact invocation commands and env
    vars to reuse verbatim)
  </read_first>
  <action>
    Create `docs/audits/2026-09-09-public-member-performance/153-AFTER.md`, structurally mirroring
    `REPORT.md`'s own section-header conventions (read it directly for the exact convention; do not
    invent a new document shape). Re-run, under the exact same conditions as the original report
    (1440×900, DPR 1, anonymous context, cold and warm measured separately, 1.6 Mbit/s / CPU×4
    throttling where applicable) and record results from all three audit scripts:
    `audit-public-member-bundles.mjs` (compare against the 353/1155 kB member vs 187/632 kB group
    baseline — must show the Tiptap/ProseMirror/editor buckets at zero for both routes),
    `audit-public-member-navigation-retention.mjs` at both `AUDIT_CYCLES=12` and `AUDIT_CYCLES=50`
    (compare against the original 466→15,107 node / 347→1,100 listener baseline — cite Plan 01
    Task 2's already-recorded numbers plus this task's own fresh re-run against the fully merged
    six-plan state), and `audit-public-member-visibility.mjs` against `timer`, `kara`, and the
    group page (compare against `visibility-members-timer.json`, `visibility-members-kara.json`,
    `visibility-fansubs-new-subs.json`).
    Include two explicit, clearly labeled sections: "RCA-04 — Status" stating verbatim that the
    Chrome tab crash remains unreproduced and unfixed by this phase, with a one-line observation
    task for the user's own Chrome afterward (not a phase deliverable); and "Pre-existing,
    phase-foreign defects (D5)" listing the three named defects from this plan's context block with
    their exact file:line citations, explicitly stating none were modified by this phase.
    Include a "Badge-ladder scope (P153-09)" section citing the Auftraggeber's binding decision
    verbatim (see Plan 06's objective) and stating that `kara`'s badge-section DOM element count is
    deliberately unchanged, not a missed target.
    Do not modify `REPORT.md` in any way — confirm via `git diff --stat docs/audits/2026-09-09-public-member-performance/REPORT.md` showing zero changes.
  </action>
  <acceptance_criteria>
    - `153-AFTER.md` exists under the same audit directory as `REPORT.md`.
    - `git diff --stat docs/audits/2026-09-09-public-member-performance/REPORT.md` shows no changes.
    - The document reports all three audit scripts' before/after numbers for `timer`, `kara`, and
      the group page, under the same measurement conditions as `REPORT.md`.
    - The document contains an explicit "RCA-04 — Status" section stating the crash is unfixed and
      unreproduced, and an explicit D5 section naming all three pre-existing defects with citations.
    - `audit-public-member-bundles.mjs`'s reported Tiptap/ProseMirror/editor byte counts for both
      the member and group routes are zero.
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/audits/2026-09-09-public-member-performance/153-AFTER.md && git diff --stat docs/audits/2026-09-09-public-member-performance/REPORT.md | grep -q . && echo REPORT_MD_CHANGED || echo REPORT_MD_UNCHANGED</automated>
  </verify>
  <done>New before/after audit document exists with all three scripts' measurements, explicit RCA-04 and D5 sections, and zero Tiptap/ProseMirror/editor bytes in the public bundle audit; REPORT.md is byte-unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live sanity check of the corrected public member profile</name>
  <files>frontend/src/app/members/[slug]/page.tsx, frontend/src/app/members/[slug]/MemberProfileContent.tsx</files>
  <what-built>
    All three P1 root causes (RCA-01 DOM/listener retention, RCA-02 public import graph weight,
    RCA-03 hydration-masked SSR content) are corrected across Plans 01-06; the automated
    regression/build gate and a new before/after measurement document are complete.
  </what-built>
  <how-to-verify>
    Via the SSH tunnel at `http://127.0.0.1:3300`:
    1. Open `/members/timer` (a full profile) and `/members/kara` (a near-empty profile). Confirm
       content that is present in "View Source" (server-rendered HTML) appears visually immediately
       on page load, without a skeleton flash blocking it while the page hydrates.
    2. On `/members/kara`, confirm the full locked/gesperrte badge ladder (all badge families, all
       locked tiers) still renders exactly as fully as before this phase — this is a deliberate,
       unchanged product decision, not a regression to look for.
    3. Navigate between two member profiles several times in a row (SPA navigation, using in-app
       links, not full page reloads) and confirm the browser stays responsive with no obvious
       progressive slowdown.
    4. If you are logged in as the owner of a currently-hidden profile, open that profile's URL and
       confirm the private preview still renders correctly for you.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issue observed.</resume-signal>
  <action>Use the live tunnel and real user-visible navigation only; do not alter application code during this checkpoint. If an issue is found, describe it precisely (route, viewport, exact observation) rather than resolving it inline.</action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://192.168.235.196:3000/members/timer | grep -q 200</automated>
  </verify>
  <done>User types "approved" after completing all four live checks against the running stack.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Verification-only | This plan runs existing audit scripts and test suites; it introduces one new static test file and one new documentation file, neither of which touches a runtime trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153-07-01 | Tampering (silent scope creep) | D5's three named pre-existing defects | mitigate | Task 1 explicitly forbids fixing D5-named issues in this phase; any unexplained NEW failure must be flagged, not silently bucketed as pre-existing |
| T-153-07-02 | Repudiation (overclaiming fixed scope) | `153-AFTER.md` | mitigate | Explicit RCA-04 "still open" section and explicit P153-09 badge-ladder-unchanged section are mandatory content, preventing the document from implying a broader fix than what was actually done |
| T-153-07-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; Package Legitimacy Gate not applicable |
</threat_model>

<verification>
- `vitest run src/components/editor/__tests__/publicImportGraph.test.ts` — passes.
- `npm test`, `npm run typecheck`, `npm run lint` — green modulo named D5 exceptions.
- `docker compose build` — exit 0.
- `docs/audits/2026-09-09-public-member-performance/153-AFTER.md` exists; `REPORT.md` unchanged.
- Human checkpoint approved.
</verification>

<success_criteria>
- No Tiptap/ProseMirror/RichTextEditor module in the public DEV import graph (measured).
- Full regression suite, typecheck, lint, and `docker compose build` are green (D5 exceptions named,
  not fixed).
- A new, non-overwriting before/after audit document exists with all three scripts' measurements.
- RCA-04 is documented as open; no document claims it fixed.
- The badge-ladder scope decision is documented, not silently violated.
- A human has live-confirmed the fix on the running stack via the canonical SSH-tunnel path.
</success_criteria>

<output>
Create `.planning/phases/153-public-member-clientlast-und-speicherretention/153-07-SUMMARY.md` when done.
</output>
