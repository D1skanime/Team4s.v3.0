---
phase: 153
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/profile/MemberBadgeChain.tsx
  - frontend/src/components/profile/MemberBadgeChain.module.css
  - frontend/src/components/profile/MemberBadgeChain.test.tsx
autonomous: true
requirements: [P153-08, P153-09, P153-10]

must_haves:
  truths:
    - "The badge/role carousel's real, SSR-rendered content is visible immediately on first paint; the carouselSkeleton overlay no longer masks it behind an interactionEnabled/data-interaction-enabled gate."
    - "MemberBadgeChain's full locked/gesperrte badge ladder renders with exactly the same badge groups, families, and tier count as before this phase — zero content reduction. Binding decision, cited verbatim: \"Das in RCA-03 gemessene Problem ist der Zeitpunkt der Sichtbarkeit, nicht der Umfang der Darstellung. Der gesperrte Ladder hat einen fachlichen Zweck — er zeigt neuen Mitgliedern, was ueberhaupt erreichbar ist — und wird deshalb nicht als Nebenwirkung einer Performancekorrektur gekuerzt.\""
    - "buildMemberBadgeGroups and every rendering branch that decides WHICH badges/tiers to show are byte-identical to before this plan; only skeleton-visibility timing changed."
    - "MemberBadgeChain.test.tsx's images render the corrected (non-auto-prefixed) sizes string inherited from Plan 01's AchievementArtwork fix."
  artifacts:
    - path: "frontend/src/components/profile/MemberBadgeChain.tsx"
      provides: "carouselSkeleton overlay removed; badge/tier rendering content unchanged"
    - path: "frontend/src/components/profile/MemberBadgeChain.module.css"
      provides: "carouselSkeleton CSS rules and :has() masking selectors removed"
  key_links:
    - from: "frontend/src/components/profile/MemberBadgeChain.tsx"
      to: "frontend/src/components/profile/AchievementArtwork.tsx"
      via: "AchievementArtwork rendering inside badge chips, sizes string inherited from Plan 01"
      pattern: "HERO_SIZES|STAGE_SIZES|AchievementArtwork"
---

<objective>
Close RCA-03 (C1/C4 per UI-SPEC §2, §3, §5) for `MemberBadgeChain`, while enforcing the binding
P153-09/C2-C5 exception: the full locked/gesperrte badge ladder stays exactly as visually complete
on an empty profile as it is today. The Auftraggeber decided this directly during phase planning,
**before** research/pattern-mapping ran, and the decision is binding and non-negotiable:

> "Das in RCA-03 gemessene Problem ist der Zeitpunkt der Sichtbarkeit, nicht der Umfang der
> Darstellung. Der gesperrte Ladder hat einen fachlichen Zweck — er zeigt neuen Mitgliedern, was
> überhaupt erreichbar ist — und wird deshalb nicht als Nebenwirkung einer Performancekorrektur
> gekürzt."

The measured 606 DOM elements for member `kara` are **deliberately expected to NOT shrink** toward
the 172-element header-only control number by this plan — an unchanged badge-section element count
must never be treated as a missed target anywhere in this plan's verification language. This
requirement is satisfied once (a) this decision is documented in the plan (done, above) and
(b) the carouselSkeleton visibility-timing fix explicitly does not touch `buildMemberBadgeGroups`
or any rendering branch that decides which badges/tiers appear.

This plan also absorbs a test-file ripple from Plan 01 (Workstream A): `MemberBadgeChain.test.tsx`
asserts on the `sizes` attribute of `AchievementArtwork`-rendered `<img>` elements it contains,
which must be updated to the corrected non-`auto` strings alongside this plan's own
skeleton-assertion updates, since both changes land in the same test file.

Purpose: same throttled-load defect class as Plans 04/05 (skeleton overlaying already-SSR'd
content), applied via a `:has()`-selector variant instead of a sibling `data-visible` attribute.
Output: `carouselSkeleton` overlay and its CSS masking selectors removed; badge/tier content
100% unchanged; test suite fully updated for both this plan's and Plan 01's changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-public-member-clientlast-und-speicherretention/153-USER-REQUEST.md
@.planning/phases/153-public-member-clientlast-und-speicherretention/153-RESEARCH.md
@.planning/phases/153-public-member-clientlast-und-speicherretention/153-PATTERNS.md
@.planning/phases/153-public-member-clientlast-und-speicherretention/153-UI-SPEC.md

<interfaces>
Defect excerpt, verified this session (`MemberBadgeChain.tsx:210-219`):
```tsx
<div className={chainStyles.carouselShell}>
  <div className={chainStyles.carouselSkeleton} aria-hidden="true" data-badge-skeleton>
    <span className={chainStyles.skeletonControl} />
    <span className={chainStyles.skeletonCard} />
    <span className={chainStyles.skeletonControl} />
  </div>
  <FocalCarousel items={group.rows} /* ... */ deferInteractionUntilNearViewport /* ... */ />
</div>
```
CSS masking mechanism (`MemberBadgeChain.module.css:57-70`):
```css
.carouselSkeleton {
  position: absolute; inset: 0; z-index: 2;
  display: grid; grid-template-columns: 44px minmax(0, 1fr) 44px;
  align-items: center; gap: 8px; pointer-events: none;
}
.carouselShell:has([data-interaction-enabled="true"]) > .carouselSkeleton,
.carouselShell:has(.badgeGrid) > .carouselSkeleton {
  visibility: hidden;
}
```
`data-interaction-enabled` is set by `FocalCarouselInternals.tsx:296` on the carousel's default
single-item **track** view (the one that IS rendered in SSR HTML for `MemberBadgeChain`, since data
arrives fully via props, no client fetch). `.badgeGrid` (the `gridClassName` prop) is only applied
to `FocalCarouselInternals.tsx`'s separate **expanded grid** view (triggered by the "Alle
Auszeichnungen anzeigen" button) — confirmed via grep this session
(`gridClassName`/`badgeGrid` render only inside the expanded-grid render branch, not the default
track). This means the skeleton stays visible (masking the already-SSR'd default track content)
until `data-interaction-enabled="true"` fires post-hydration — the exact same defect class as
Plans 04/05, expressed via `:has()` instead of a sibling `data-visible` attribute.

`MemberBadgeChain`'s content-determining logic that must stay 100% byte-identical (lines 93-118,
120-189 — `buildMemberBadgeGroups`, `catalogWithEarnedBadges`, the `groups`/`collectionGroups`
construction, and every `renderItem` branch deciding which badge/tier chip to draw): **do not
modify any line in this range.**

Test assertions requiring updates this session, `MemberBadgeChain.test.tsx`:
- Skeleton-presence assertions (5 sites, grep-confirmed this session):
  `container.querySelectorAll('[data-badge-skeleton]')).toHaveLength(0)` at ~line 474 and ~1222
  (already-correct absence assertions for the specialized single-stage displays —
  `AnimeProjectAchievementStage`/`PointsAchievementStage`, which never had `carouselSkeleton` in
  the first place — these stay valid and need no change since the count stays 0).
  `container.querySelectorAll('[data-badge-skeleton][aria-hidden="true"]')).toHaveLength(1)` at
  ~line 1339 — **must change**, the skeleton no longer exists anywhere, so this becomes `0` or is
  removed entirely along with the surrounding test's now-inapplicable premise.
  `container.querySelector('[data-badge-skeleton="true"]')).not.toBeNull()` at ~line 1549 — **must
  change** to `.toBeNull()` or be removed, same reason.
  `stage.querySelectorAll('[data-badge-skeleton]')).toHaveLength(0)` at ~line 1726 — already-correct,
  no change needed (specialized stage display, same as the first two).
- `sizes` string assertions inherited from Plan 01's `AchievementArtwork.tsx` fix (grep-confirmed
  this session, 3 sites): `~line 1110`
  `expect(roleStageImages.every((image) => image.getAttribute('sizes') === 'auto, (min-width: 562px) 80px, 64px')).toBe(true)`;
  `~line 1335`
  `expect(compactImages.every((image) => image.getAttribute('sizes') === 'auto, (min-width: 562px) 80px, 64px')).toBe(true)`;
  `~line 1338`
  `expect(activeImages.every((image) => ['auto, (min-width: 562px) 80px, 64px', 'auto, (min-width: 658px) 240px, (min-width: 562px) 216px, 192px'].includes(image.getAttribute('sizes') ?? ''))).toBe(true)`.
  All three must drop the `auto, ` prefix from every string literal they compare against.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the carouselSkeleton overlay without touching badge/tier content</name>
  <files>
    frontend/src/components/profile/MemberBadgeChain.tsx,
    frontend/src/components/profile/MemberBadgeChain.module.css
  </files>
  <read_first>
    frontend/src/components/profile/MemberBadgeChain.tsx (full file, 395 lines — the skeleton
    block is at lines 210-219; the content-determining logic at lines 93-189 must stay untouched)
    frontend/src/components/profile/MemberBadgeChain.module.css (full file, 199 lines — skeleton
    rules at lines 57-77 and the container-query rules referencing `.carouselSkeleton` at lines
    169-193)
    frontend/src/components/ui/FocalCarouselInternals.tsx (lines 233-300 and 360-410 only — confirm
    `data-interaction-enabled` is set on the default track view and `gridClassName`/`.badgeGrid` is
    applied only to the separate expanded-grid view; read-only, not modified by this plan)
  </read_first>
  <action>
    Delete the `<div className={chainStyles.carouselSkeleton} aria-hidden="true" data-badge-skeleton>`
    block (current lines 211-219) entirely — `MemberBadgeChain`'s badge/role data arrives fully via
    props with no client-side fetch, so there is no genuine loading substate this overlay could ever
    legitimately represent (per UI-SPEC §2's rule: a skeleton is legitimate only for a genuine
    loading state with no content to show yet). The `<FocalCarousel>` element remains the sole child
    of `.carouselShell`.
    In `MemberBadgeChain.module.css`, remove the `.carouselSkeleton` rule (lines 57-66), the two
    `:has()` masking selectors (lines 68-71), the `.skeletonControl`/`.skeletonCard` declarations
    (lines 73-90) if grep confirms zero remaining references after the JSX change, and the
    `@container member-badge-carousel (max-width: 480px)` block's `.carouselSkeleton`/`.skeletonCard`/
    `.skeletonControl` rules (lines 169-193) under the same zero-reference condition. Do not remove
    `.chain`, `.badgeWindow`, `.badgeWindowActive`, `.badgeGrid`, `.currentChip`, or any other class
    still used by the real carousel/badge-chip rendering.
    Do not modify any line within `buildMemberBadgeGroups`, `catalogWithEarnedBadges`, the
    `groups`/`collectionGroups` construction, or any `renderItem` branch (lines 93-189 and 249-353) —
    these decide WHICH badges/tiers render and are explicitly out of scope; only the skeleton
    overlay's visibility-timing mechanism changes.
  </action>
  <acceptance_criteria>
    - `MemberBadgeChain.tsx` contains no `carouselSkeleton`/`data-badge-skeleton` reference.
    - `MemberBadgeChain.module.css` contains no `.carouselSkeleton` rule and no `:has([data-interaction-enabled="true"])`/`:has(.badgeGrid)` masking selector.
    - `git diff frontend/src/components/profile/MemberBadgeChain.tsx` shows zero changes within the
      line range spanning `buildMemberBadgeGroups` through the end of the `groups`/`collectionGroups`
      construction and every `renderItem` callback — only the skeleton `<div>` block is removed.
    - `FocalCarousel` remains the sole child element of `.carouselShell` in the rendered JSX.
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit 2>&1 | grep -i MemberBadgeChain || echo NO_MATCHES"</automated>
  </verify>
  <done>carouselSkeleton overlay and its masking CSS removed; badge/tier content-determining logic verified byte-identical via diff inspection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update MemberBadgeChain.test.tsx for both the removed skeleton and Plan 01's sizes-string ripple</name>
  <files>frontend/src/components/profile/MemberBadgeChain.test.tsx</files>
  <read_first>
    frontend/src/components/profile/MemberBadgeChain.test.tsx (full file — locate all five
    `data-badge-skeleton` sites and all three `sizes`-string sites named in this plan's context
    block by grepping `data-badge-skeleton` and `auto, (min-width` before editing, since exact line
    numbers may have shifted since this session's research pass)
  </read_first>
  <behavior>
    - Every assertion checking `[data-badge-skeleton]` presence (previously `toHaveLength(1)` or
      `.not.toBeNull()`) must be updated to assert absence (`toHaveLength(0)` / `.toBeNull()`) or be
      removed if the surrounding test's premise (e.g. "the skeleton is visible before X, hidden
      after Y") no longer applies at all — if removed, confirm the test still asserts something
      meaningful about the surrounding behavior (e.g. the expand/collapse state itself) rather than
      being deleted wholesale.
    - Every assertion checking `[data-badge-skeleton]` absence (already `toHaveLength(0)`, in the
      specialized single-stage-display tests) needs no change — these already correctly assert 0
      and remain 0.
    - Every `sizes`-string assertion inherited from Plan 01 must drop the `auto, ` prefix from each
      literal string compared against, matching `AchievementArtwork.tsx`'s corrected output exactly:
      `'(min-width: 562px) 80px, 64px'` and `'(min-width: 658px) 240px, (min-width: 562px) 216px, 192px'`.
    - Every other assertion in this file (badge labels, tier counts, role progress copy, carousel
      region labels, expand/collapse behavior, artwork geometry) stays exactly as today — this task
      touches only the skeleton-presence and sizes-string assertions named above.
  </behavior>
  <action>
    Grep this file for `data-badge-skeleton` and update each of the (up to five) matching
    assertions per the behavior above. Grep this file for `'auto, (min-width` and update each of
    the (up to three) matching assertions to drop the `auto, ` prefix. Run the full file's suite
    and fix any remaining failures that surface from either change (e.g. a test whose only
    assertion was skeleton presence may need a replacement assertion proving the real carousel
    content is present instead, to keep the test meaningful).
  </action>
  <acceptance_criteria>
    - `MemberBadgeChain.test.tsx` contains no assertion expecting `[data-badge-skeleton]` to be
      present (non-zero length or non-null).
    - `MemberBadgeChain.test.tsx` contains no `'auto, '`-prefixed `sizes` string literal.
    - `vitest run src/components/profile/MemberBadgeChain.test.tsx` passes, 0 failures.
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/profile/MemberBadgeChain.test.tsx"</automated>
  </verify>
  <done>All skeleton-presence and sizes-string assertions corrected; full test file green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | Purely a client-side skeleton-visibility-timing change on already-authorized, already-server-fetched badge/role data; no content, data, or input surface changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153-06-01 | Tampering (silent scope reduction) | `buildMemberBadgeGroups` / badge-ladder rendering | mitigate | Task 1's acceptance criteria requires a `git diff` inspection proving zero changes to the content-determining line range; this plan's success is explicitly NOT measured by any reduction in `kara`'s DOM element count (locked Auftraggeber decision, cited verbatim in this plan's objective) |
| T-153-06-02 | Information Disclosure | n/a | accept | No new data exposed; badge/role data shown is already public and already server-provided as props before the component mounts |
</threat_model>

<verification>
- `vitest run src/components/profile/MemberBadgeChain.test.tsx` — 0 failures.
- `git diff frontend/src/components/profile/MemberBadgeChain.tsx` — confirms zero changes to
  `buildMemberBadgeGroups`/`catalogWithEarnedBadges`/`groups`/`collectionGroups`/`renderItem`.
- `npx tsc --noEmit` — zero new errors on this file.
</verification>

<success_criteria>
- `carouselSkeleton` overlay and its CSS masking mechanism are removed.
- Real badge/role carousel content is visible immediately on mount.
- Badge/tier content-determining logic is byte-identical (verified via diff, not just tests).
- Both the skeleton-presence and Plan-01-inherited sizes-string test assertions are corrected.
- No verification step anywhere treats `kara`'s unchanged badge-section DOM element count as a
  regression or missed target.
</success_criteria>

<output>
Create `.planning/phases/153-public-member-clientlast-und-speicherretention/153-06-SUMMARY.md` when done.
</output>
