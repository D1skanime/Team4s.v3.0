---
phase: 153
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/profile/AchievementArtwork.tsx
  - frontend/src/components/profile/AchievementArtwork.test.tsx
autonomous: true
requirements: [P153-01, P153-02, P153-03]

must_haves:
  truths:
    - "A lazy-loaded achievement image renders sizes as the deterministic HERO_SIZES/STAGE_SIZES string, with no leading `auto,` token."
    - "A priority achievement image renders the identical deterministic sizes string (the priority/lazy branch on `sizes` is no longer differentiated)."
    - "Twelve and fifty simulated SPA navigation cycles against the public member profile show no linear DOM-node or listener growth, matching the audit's isolated-variable control result."
    - "Lazy loading (loading=\"lazy\" when not priority), reserved 1254x1254 intrinsic geometry, ResponsiveImage's optimizer usage, and srcset behavior remain byte-identical to before this change."
  artifacts:
    - path: "frontend/src/components/profile/AchievementArtwork.tsx"
      provides: "Deterministic, CSS-breakpoint-aligned sizes string for hero/stage achievement artwork, for both lazy and priority images"
    - path: "frontend/src/components/profile/AchievementArtwork.test.tsx"
      provides: "Regression coverage locking the corrected non-`auto` sizes strings"
  key_links:
    - from: "frontend/src/components/profile/AchievementArtwork.tsx"
      to: "frontend/src/components/profile/AchievementArtwork.module.css"
      via: "HERO_SIZES/STAGE_SIZES string constants matching @container achievement-card breakpoints"
      pattern: "HERO_SIZES|STAGE_SIZES"
---

<objective>
Eliminate RCA-01 (native `sizes="auto"` DOM/listener retention on SPA navigation) at its single
confirmed source: `AchievementArtwork.tsx:37`. Per D-01 (locked, 153-USER-REQUEST.md A1/A3/A4),
remove the `auto,` prefix so both lazy and priority achievement images use the already-correct,
already CSS-aligned deterministic `HERO_SIZES`/`STAGE_SIZES` strings. Per A2 (locked), do **not**
switch to global `eager` — lazy loading, reserved geometry, optimizer usage, `srcset`, and image
sharpness must remain provably unchanged; only the `sizes` attribute's `auto,` prefix is removed.

Purpose: this is the sole reproduced root cause of RCA-01's 466 → 15,107 DOM-node / 347 → 1,100
listener growth over twelve SPA navigation cycles (production audit). Removing it is confirmed
sufficient on its own (isolated-variable audit result: 464 → 1,192 nodes, same order of magnitude
as the `eager` control).
Output: `AchievementArtwork.tsx` renders a deterministic `sizes` string for every achievement
image; `AchievementArtwork.test.tsx` locks the corrected strings as a regression guard.
</objective>

<container_hygiene>
**Verbindlich vor jedem Playwright-Messlauf dieses Plans.** Zurueckgebliebene
Chromium-Prozesse im Frontend-Container haengen den naechsten Lauf, und ein kalter
Image-Cache verfaelscht die erste Messung:

```
docker restart team4sv30-frontend
# Warten bis der Dev-Server antwortet, dann den Image-Cache waermen:
curl -sS -o /dev/null "http://127.0.0.1:3000/members/timer"
curl -sS -o /dev/null "http://127.0.0.1:3000/members/kara"
curl -sS -o /dev/null "http://127.0.0.1:3000/fansubs/new-subs"
```

Ein Lauf, der ohne diesen Schritt haengt oder unplausible Zahlen liefert, ist zu
wiederholen und nicht als Messergebnis zu berichten.
</container_hygiene>


<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-public-member-clientlast-und-speicherretention/153-USER-REQUEST.md
@.planning/phases/153-public-member-clientlast-und-speicherretention/153-RESEARCH.md
@.planning/phases/153-public-member-clientlast-und-speicherretention/153-PATTERNS.md

<interfaces>
Current defect, verified this session at `frontend/src/components/profile/AchievementArtwork.tsx`
lines 20-21 and 35-38:

```
const HERO_SIZES = '(min-width: 658px) 240px, (min-width: 562px) 216px, 192px'
const STAGE_SIZES = '(min-width: 562px) 80px, 64px'
...
const fallbackSizes = size === 'hero' ? HERO_SIZES : STAGE_SIZES
// Native lazy-image auto sizes use the actual container layout without per-card observers.
const sizes = priority ? fallbackSizes : `auto, ${fallbackSizes}`
const loading = priority ? undefined : 'lazy'
```

Target shape (RESEARCH.md Pattern 1, verified byte-for-byte against
`AchievementArtwork.module.css`'s `.hero`/`.stage` base sizes 192px/64px and its two
`@container achievement-card` breakpoints at 562px/658px — **no CSS realignment work exists, A3 is
verify-only**):

```
const sizes = size === 'hero' ? HERO_SIZES : STAGE_SIZES
const loading = priority ? undefined : 'lazy'
```

`sizes` is forwarded verbatim by `ResponsiveImage.tsx` to `next/image` — that file needs no change.
Confirmed via direct `node_modules/next` inspection this session: Next.js has zero special-casing
of the literal string `"auto"` in `get-img-props.js` — the retention is a purely browser-native
(Chromium) mechanism, not a Next.js quirk.

Repo-wide grep this session (`grep -rln 'sizes="auto"\|`auto,'` frontend/src`) returns exactly one
file: `AchievementArtwork.tsx` itself. A4 ("treat any other `auto` call site the same way") is a
verify-only check — no second location exists.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the `auto,` sizes prefix and confirm no second call site exists (A1/A3/A4)</name>
  <files>frontend/src/components/profile/AchievementArtwork.tsx</files>
  <read_first>
    frontend/src/components/profile/AchievementArtwork.tsx (full file, 93 lines — HERO_SIZES/STAGE_SIZES
    at lines 20-21, the defect at lines 35-38)
    frontend/src/components/profile/AchievementArtwork.module.css (`.hero`/`.stage` base widths and
    the two `@container achievement-card` breakpoint rules — confirm they still read 192px/216px/240px
    and 64px/80px before touching source, per A3's verify-only nature)
  </read_first>
  <action>
    In `AchievementArtwork.tsx`, change line 37 from
    `const sizes = priority ? fallbackSizes : `auto, ${fallbackSizes}`` to a single unconditional
    assignment: `const sizes = size === 'hero' ? HERO_SIZES : STAGE_SIZES` (this both removes the
    `auto,` prefix and collapses the now-dead `priority` conditional on `sizes` — the `fallbackSizes`
    intermediate variable becomes redundant with this change and may be removed or kept as the
    right-hand side of the new `sizes` assignment, executor's choice, but do not leave an unused
    variable). Leave the `loading = priority ? undefined : 'lazy'` line and every other line in the
    file completely untouched — no other prop, no other branch, no JSX changes. Delete the now-stale
    comment `// Native lazy-image auto sizes use the actual container layout without per-card
    observers.` since it describes behavior that no longer exists in this file.
    Run `grep -rn "auto,\|sizes=\"auto\"" frontend/src` inside the container (or via a host-side
    grep against the mounted repo) to confirm zero remaining `auto`-prefixed `sizes` usages
    repo-wide (A4) — do not create a second variant if one is somehow found; if a second site is
    found (unexpected — RESEARCH.md found none), fix it identically in this same task.
    Do not touch `AchievementArtwork.module.css` — this session's research already confirmed
    `HERO_SIZES`/`STAGE_SIZES` byte-match the CSS breakpoints; A3 requires only a verification
    read (already done above), not a change.
  </action>
  <acceptance_criteria>
    - `AchievementArtwork.tsx` no longer contains the string `auto,` anywhere.
    - `sizes` is `HERO_SIZES` for `size === 'hero'` and `STAGE_SIZES` for `size === 'stage'`,
      identically for `priority={true}` and `priority={false}`/unset.
    - `loading` remains `undefined` when `priority` is true and `'lazy'` otherwise (unchanged).
    - Repo-wide grep for `auto,` or `sizes="auto"` under `frontend/src` returns zero matches.
    - `AchievementArtwork.module.css` is not modified (git diff shows zero changes to that file).
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i AchievementArtwork || true"</automated>
  </verify>
  <done>Source fix lands with the exact target shape above; no second `auto` call site exists anywhere in `frontend/src`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update the two stale test assertions and prove no linear retention over 12/50 cycles</name>
  <files>frontend/src/components/profile/AchievementArtwork.test.tsx</files>
  <read_first>
    frontend/src/components/profile/AchievementArtwork.test.tsx (full file, 150 lines — the two
    stale assertions are at line 40-42 and line 89; line 103's `.not.toContain('auto')` assertion
    for the priority case is already correct and needs no change; the `describe('shared achievement
    card geometry', ...)` block at lines 129-150 reads CSS files directly for a legitimate
    "declared once, in one module" structural assertion — do not touch that block)
  </read_first>
  <behavior>
    - Test "renders direct artwork through the bounded hero contract" (currently asserts
      `image.getAttribute('sizes')` equals `'auto, (min-width: 658px) 240px, (min-width: 562px) 216px, 192px'`
      at line 40-42): must assert `'(min-width: 658px) 240px, (min-width: 562px) 216px, 192px'`
      (the `auto, ` prefix removed, nothing else changed).
    - Test "makes the meaningful image decorative without changing marker dimensions" (currently
      asserts `image?.getAttribute('sizes')` equals `'auto, (min-width: 562px) 80px, 64px'` at line
      89): must assert `'(min-width: 562px) 80px, 64px'`.
    - Test "keeps eager priority artwork on a conservative image hint without invalid auto sizing"
      (line 92-105, asserts `.not.toContain('auto')`): unchanged, still passes (was already
      correct for the priority case pre-fix; now also matches the lazy case's new value).
  </behavior>
  <action>
    Update the two literal string assertions named above to remove the `auto, ` prefix, leaving
    every other character of each string identical (including the exact `(min-width: ...)`
    breakpoint ordering and px values). Do not add, remove, or reorder any other assertion in this
    file. After the edit, run the file's suite and confirm all tests pass, then run the phase's two
    Playwright retention audits against the running dev container to prove the audit-measured
    defect is closed for real (P153-03) — not just at the unit-test level:
    `docker compose exec -T -e AUDIT_LABEL=post153-cycles12 -e AUDIT_CYCLES=12 team4sv30-frontend node scripts/audit-public-member-navigation-retention.mjs`
    followed by
    `docker compose exec -T -e AUDIT_LABEL=post153-cycles50 -e AUDIT_CYCLES=50 team4sv30-frontend node scripts/audit-public-member-navigation-retention.mjs`.
    Record both scripts' reported node/listener counts in the task's completion notes (for
    SUMMARY.md and for Plan 07's D1 before/after document to cite) — do not discard the output.
    A linear or near-linear node/listener count across cycles (mirroring the original
    466 → 15,107 pattern) means the fix did not take; a flat/bounded count (mirroring the audit's
    own isolated-variable control, ~464 → ~1,192 nodes) is the expected passing result.
  </action>
  <acceptance_criteria>
    - `AchievementArtwork.test.tsx:40-42` asserts the exact string
      `'(min-width: 658px) 240px, (min-width: 562px) 216px, 192px'`.
    - `AchievementArtwork.test.tsx:89` asserts the exact string
      `'(min-width: 562px) 80px, 64px'`.
    - `vitest run src/components/profile/AchievementArtwork.test.tsx` passes with 0 failures.
    - Both retention-audit runs (12 and 50 cycles) complete and report a node/listener growth
      pattern that is flat or bounded, not linear across cycles — cite the actual before/after
      numbers in the SUMMARY, do not summarize as "fixed" without the numbers.
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/profile/AchievementArtwork.test.tsx"</automated>
  </verify>
  <done>Both stale assertions corrected and passing; both retention-audit runs (12 and 50 cycles) executed against the live dev container with recorded, non-linear node/listener counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | This plan changes a presentational `sizes` attribute string computed from a `size: 'hero' \| 'stage'` prop that is never derived from user input; no request, session, or persistence boundary is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153-01-01 | Denial of Service (client-side) | `AchievementArtwork.tsx` `sizes` computation | mitigate | This is the fix itself — the DOM/listener growth measured in RCA-01 is a genuine client-side resource-exhaustion pattern (uncapped heap/listener growth on repeated SPA navigation); removing `auto,` is the confirmed, reproduced mitigation, verified with the retention audit scripts in Task 2 |
| T-153-01-02 | Tampering / Information Disclosure | n/a | accept | No new data, endpoint, or user input surface is introduced; `sizes` only affects which `srcset` candidate the browser selects for an already-authorized, already-public achievement image |
</threat_model>

<verification>
- `vitest run src/components/profile/AchievementArtwork.test.tsx` — 0 failures.
- `docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit"` shows no new errors
  attributable to `AchievementArtwork.tsx` (pre-existing unrelated errors elsewhere are out of scope,
  see Plan 07's D5 documentation).
- `audit-public-member-navigation-retention.mjs` at `AUDIT_CYCLES=12` and `AUDIT_CYCLES=50` both
  report bounded, non-linear DOM-node/listener growth.
</verification>

<success_criteria>
- `sizes` is deterministic (no `auto,` prefix) for both lazy and priority achievement images.
- Zero other `sizes="auto"`/`` `auto,` `` call sites exist anywhere in `frontend/src`.
- The two stale test assertions are corrected and green.
- The navigation-retention audit script proves bounded growth at both 12 and 50 cycles.
- `loading`, reserved 1254x1254 geometry, `ResponsiveImage`'s optimizer usage, and `srcset`
  generation are unchanged (no lines touched besides the `sizes` computation).
</success_criteria>

<output>
Create `.planning/phases/153-public-member-clientlast-und-speicherretention/153-01-SUMMARY.md` when done, including the recorded before/after retention-audit numbers for both 12 and 50 cycles.
</output>
