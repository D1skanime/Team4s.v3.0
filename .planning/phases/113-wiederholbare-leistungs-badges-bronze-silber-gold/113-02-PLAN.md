---
phase: 113-wiederholbare-leistungs-badges-bronze-silber-gold
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/profile/memberBadgeLabels.ts
  - frontend/src/components/profile/memberBadgeLabels.test.ts
  - frontend/src/components/profile/MemberBadgeChain.test.tsx
autonomous: true
requirements: [GAM-04]
user_setup: []

must_haves:
  truths:
    - "D-05: Neue Gruppe „Beiträge" (Key `contributions`) in der „Auszeichnungen"-Sektion, positioniert zwischen `progress` und `membership`."
    - "D-05: Pro Familie erscheint nur die höchste erreichte Stufe als genau EIN Chip (kein roleCode → kein Ketten-Merge)."
    - "D-05: Die 9 Codes stehen bewusst NICHT im PUBLIC_MEMBER_BADGE_CATALOG (earned-only, kein Locked-/Schloss-Chip), analog point_milestone_*."
    - "D-05: Kein Sichtbarkeits-Toggle — die Gruppe wird ausschließlich über MemberBadgeChain (public /members/[slug]) gerendert, nicht in AchievementBadgesCard."
    - "GAM-04: Die abgeleiteten Badges fließen nur über den earned-Merge (catalogWithEarnedBadges) ein; keine persistierte Katalog-/member_badges-Repräsentation."
    - "CLAUDE.md: Alle 9 Chip-Labels + Gruppen-Label in korrektem Deutsch mit Umlauten („Beiträge", „Mitgetragene Projekte · Silber", „Bildarchivar · …")."
  artifacts:
    - path: "frontend/src/components/profile/memberBadgeLabels.ts"
      provides: "9 Presentation-Einträge (3 Familien × bronze/silver/gold), Gruppen-Key `contributions`, Gruppen-Label „Beiträge", Order-Position, Icon-Imports FolderCheck/ScrollText/Images"
      contains: "contribution_projects_bronze"
    - path: "frontend/src/components/profile/memberBadgeLabels.test.ts"
      provides: "Assertions: 9 Codes (Label/Icon/palette/group), Gruppen-Label + Order, Negativassertion (nicht im Katalog)"
      contains: "contribution_archivist_gold"
    - path: "frontend/src/components/profile/MemberBadgeChain.test.tsx"
      provides: "buildMemberBadgeGroups-Test: earned contribution_*-Badges → Gruppe erscheint mit je EINER Zeile pro Familie; ohne earned → Gruppe fehlt"
      contains: "contributions"
  key_links:
    - from: "MEMBER_BADGE_GROUP_ORDER"
      to: "buildMemberBadgeGroups"
      via: "contributions-Key wird iteriert und bei leeren rows ausgeblendet"
      pattern: "'contributions'"
    - from: "MEMBER_BADGE_PRESENTATIONS"
      to: "catalogWithEarnedBadges (MemberBadgeChain)"
      via: "earned-only-Merge, da Codes nicht im Katalog"
      pattern: "contribution_(projects|chronicle|archivist)_(bronze|silver|gold)"
---

<objective>
Ergänzt die Frontend-Präsentation für die drei abgeleiteten Badge-Familien: neue Gruppe „Beiträge"
(`contributions`) plus 9 statische Presentation-Einträge in `memberBadgeLabels.ts`, exakt nach dem
`point_milestone_*`-Muster (earned-only, NICHT im Katalog). Erweitert die bestehenden Vitest-Tests
additiv. `MemberBadgeChain.tsx` bleibt voraussichtlich unverändert — `buildMemberBadgeGroups` nimmt
die neue Gruppe ohne Umbau auf.

Purpose: Erfüllt D-05 (neue Gruppe, höchste Stufe pro Familie, kein Toggle, kein Locked-Chip) und
liefert die deutschen Labels/Icons/Paletten für die vom Backend (Plan 01) emittierten Badge-Codes.
Output: 9 Presentation-Einträge + Gruppen-Definition + erweiterte Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/113-wiederholbare-leistungs-badges-bronze-silber-gold/113-CONTEXT.md
@.planning/phases/113-wiederholbare-leistungs-badges-bronze-silber-gold/113-UI-SPEC.md
@.planning/phases/113-wiederholbare-leistungs-badges-bronze-silber-gold/113-PATTERNS.md

<interfaces>
<!-- Bestehende Struktur in memberBadgeLabels.ts — Executor adaptiert exakt dieses Muster. -->

Aktuelle Typen/Konstanten (memberBadgeLabels.ts:29-90):
```ts
export type MemberBadgeGroup = 'roles' | 'progress' | 'membership' | 'special'   // → 'contributions' ergänzen
export type MemberBadgePalette = 'gold' | 'indigo' | 'orange' | 'mint' | 'red' | 'bronze' | 'silver' | 'platinum'  // bronze/silver/gold existieren bereits

export type MemberBadgePresentation = {
  label: string; variant: MemberBadgeVariant; Icon: LucideIcon; palette: MemberBadgePalette; group: MemberBadgeGroup; roleCode?: string
}

// point_milestone_* (Vorbild: statisch in der Map, NICHT im Katalog):
point_milestone_first: { label: 'Erster Beitrag', variant: 'muted', Icon: Flag, palette: 'mint', group: 'progress' },

export const MEMBER_BADGE_GROUP_LABELS: Record<MemberBadgeGroup, string> = { roles: 'Rollen', progress: 'Fortschritt', membership: 'Mitgliedschaft', special: 'Besondere Auszeichnungen' }
export const MEMBER_BADGE_GROUP_ORDER: MemberBadgeGroup[] = ['roles', 'progress', 'membership', 'special']
```

Rendering-Konsument (MemberBadgeChain.tsx, unverändert): `buildMemberBadgeGroups` iteriert
`MEMBER_BADGE_GROUP_ORDER`, filtert leere Gruppen (`.filter(group => group.rows.length > 0)`);
`catalogWithEarnedBadges` merged earned-Codes, die nicht im Katalog stehen; der roleCode-Merge ist
roles-only (contributions setzt KEINEN roleCode → eine Zeile pro Badge).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gruppe `contributions` + 9 Presentation-Einträge + Icon-Imports in memberBadgeLabels.ts</name>
  <files>frontend/src/components/profile/memberBadgeLabels.ts</files>
  <read_first>
    - frontend/src/components/profile/memberBadgeLabels.ts (Icon-Import-Block :1-25; MemberBadgeGroup :34; point_milestone_*-Block :71-79; MEMBER_BADGE_GROUP_LABELS/ORDER :83-90; PUBLIC_MEMBER_BADGE_CATALOG :92-110)
    - .planning/phases/113-wiederholbare-leistungs-badges-bronze-silber-gold/113-UI-SPEC.md (Tabellen „Tier → Palette-Token" :103-111 und „Badge-Codes, Labels, Icons" :141-151)
  </read_first>
  <action>
    Erweitere `MemberBadgeGroup` um den Wert `'contributions'`. Ergänze in `MEMBER_BADGE_GROUP_LABELS` den Eintrag `contributions: 'Beiträge'` und setze `MEMBER_BADGE_GROUP_ORDER` auf `['roles', 'progress', 'contributions', 'membership', 'special']` (zwischen progress und membership).
    Importiere aus `lucide-react` zusätzlich `FolderCheck`, `ScrollText`, `Images` (alphabetisch in den bestehenden Import-Block einsortieren).
    Ergänze in `MEMBER_BADGE_PRESENTATIONS` die 9 Einträge nach dem `point_milestone_*`-Muster, alle mit `group: 'contributions'` und OHNE `roleCode`. Codes/Labels/Icons/palette/variant exakt aus UI-SPEC:
    `contribution_projects_bronze` (Label „Mitgetragene Projekte · Bronze", variant `muted`, Icon FolderCheck, palette `bronze`), `_silver` (variant `neutral`, palette `silver`), `_gold` (variant `warning`, palette `gold`);
    `contribution_chronicle_bronze/silver/gold` (Label „Chronist · Bronze/Silber/Gold", Icon ScrollText, palette/variant analog bronze=muted/silver=neutral/gold=warning);
    `contribution_archivist_bronze/silver/gold` (Label „Bildarchivar · Bronze/Silber/Gold", Icon Images, palette/variant analog).
    Die 9 Codes NICHT in `PUBLIC_MEMBER_BADGE_CATALOG` aufnehmen (earned-only, kein Locked-Chip). Korrekte Umlaute in allen Labels (Beiträge, Silber).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `MemberBadgeGroup` enthält `'contributions'`; `MEMBER_BADGE_GROUP_LABELS.contributions === 'Beiträge'`; `MEMBER_BADGE_GROUP_ORDER` ist genau `['roles','progress','contributions','membership','special']`.
    - Alle 9 `contribution_*`-Codes existieren in `MEMBER_BADGE_PRESENTATIONS` mit `group: 'contributions'` und ohne `roleCode`.
    - Keiner der 9 Codes taucht in `PUBLIC_MEMBER_BADGE_CATALOG` auf (grep: 0 Treffer im Katalog-Array).
    - `FolderCheck`, `ScrollText`, `Images` sind importiert; Typecheck (`tsc --noEmit`) grün.
    - Labels tragen korrekte Umlaute (keine ASCII-Ersetzung ae/oe/ue/ss).
  </acceptance_criteria>
  <done>Gruppe „Beiträge" + 9 earned-only Presentation-Einträge existieren, Typecheck grün.</done>
</task>

<task type="auto">
  <name>Task 2: Vitest-Tests additiv erweitern (Katalog-Assertions + Gruppenbildung)</name>
  <files>frontend/src/components/profile/memberBadgeLabels.test.ts, frontend/src/components/profile/MemberBadgeChain.test.tsx</files>
  <read_first>
    - frontend/src/components/profile/memberBadgeLabels.test.ts (bestehende point_milestone_*-Assertion + Katalog-Negativassertion als Muster)
    - frontend/src/components/profile/MemberBadgeChain.test.tsx (bestehende buildMemberBadgeGroups-/Render-Tests als Muster)
    - frontend/src/components/profile/memberBadgeLabels.ts (finaler Stand aus Task 1)
  </read_first>
  <action>
    In `memberBadgeLabels.test.ts`: additive Assertions, dass die 9 `contribution_*`-Codes in `MEMBER_BADGE_PRESENTATIONS` mit korrektem Label, Icon, `palette` (bronze/silver/gold) und `group: 'contributions'` liegen; dass `MEMBER_BADGE_GROUP_LABELS.contributions === 'Beiträge'` und die Order-Position zwischen `progress` und `membership` stimmt; Negativassertion, dass keiner der 9 Codes in `PUBLIC_MEMBER_BADGE_CATALOG` steht (Muster wie bestehende point_milestone_*-Assertion).
    In `MemberBadgeChain.test.tsx`: Test, dass `buildMemberBadgeGroups` mit earned `contribution_*`-Badges die Gruppe `contributions` mit je EINER Zeile pro Familie erzeugt (höchste Stufe, kein Ketten-Merge); und dass ohne earned contribution-Badges die Gruppe fehlt (leer-Filter). Nutze die vorhandenen Test-Helper/Fixtures der Datei.
    Keine Produktionslogik-Änderung an `MemberBadgeChain.tsx` — nur falls ein Test eine Signaturänderung zwingend erfordert, minimal anpassen und im SUMMARY begründen.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/profile/</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/profile/` läuft grün inkl. der neuen Assertions.
    - `memberBadgeLabels.test.ts` referenziert alle drei Familien-Codes (mind. `contribution_projects_*`, `contribution_chronicle_*`, `contribution_archivist_*`) und enthält die Katalog-Negativassertion.
    - `MemberBadgeChain.test.tsx` beweist: earned → Gruppe `contributions` mit einer Zeile pro Familie; nicht earned → keine `Beiträge`-Gruppe.
  </acceptance_criteria>
  <done>Vitest-Suite unter src/components/profile/ grün; neue Gruppe + earned-only-Verhalten testabgedeckt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server (SSR-Profil-Read) → MemberBadgeChain | public_badges-Array aus dem bereits gate-geschützten GetPublicMemberProfile; reine Anzeige, kein Input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-05 | Information Disclosure | Rendering abgeleiteter Contribution-Stufen im öffentlichen Profil | accept | Bewusst per D-05: nur die aggregierte Stufe (vom gate-geschützten Backend-Read) wird angezeigt; das Frontend führt keine eigene Zähllogik und keinen Rohdaten-Fetch aus |
| T-113-06 | Elevation of Privilege | Einblendung in der Toggle-Fläche (AchievementBadgesCard) würde persistierte Visibility-Semantik unterlaufen | mitigate | Rendering ausschließlich in MemberBadgeChain (public); AchievementBadgesCard bleibt unangetastet (Anti-Pattern D-05); earned-only ohne Katalog-Eintrag |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.json` → grün
- `cd frontend && npx vitest run src/components/profile/` → grün
- grep-Gate: 9 `contribution_*`-Codes in MEMBER_BADGE_PRESENTATIONS, 0 davon in PUBLIC_MEMBER_BADGE_CATALOG
</verification>

<success_criteria>
- Gruppe „Beiträge" (`contributions`) zwischen Fortschritt und Mitgliedschaft, earned-only.
- 9 Presentation-Einträge mit korrekten deutschen Umlaut-Labels, familien-identifizierenden Icons, tier-korrekter Palette/Variant.
- Kein Toggle, kein Locked-Chip, kein roleCode; Tests grün.
</success_criteria>

<output>
Create `.planning/phases/113-wiederholbare-leistungs-badges-bronze-silber-gold/113-02-SUMMARY.md` when done
</output>
