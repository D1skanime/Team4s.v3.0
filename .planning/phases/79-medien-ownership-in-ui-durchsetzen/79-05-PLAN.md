---
phase: 79-medien-ownership-in-ui-durchsetzen
plan: 05
type: execute
wave: 3
depends_on:
  - 79-03
  - 79-04
files_modified:
  - frontend/src/app/me/profile/components/MemberAvatarCard.tsx
  - frontend/src/app/me/profile/components/ProfileBackgroundCard.tsx
  - frontend/src/app/me/profile/page.tsx
autonomous: false
requirements:
  - G
  - I

user_setup: []

must_haves:
  truths:
    - "D-07: MemberAvatarCard.tsx zeigt MediaOwnershipContext-Owner-Chip mit ownerType='member' und owner_member_id als ownerID (Surface 5, Branding)"
    - "D-09: Member-Avatar- und Hintergrund-Upload senden visibilityCode='public', reviewStatusCode='approved' (Branding-Klasse)"
    - "D-06: Avatar-Upload in page.tsx blockiert wenn owner_member_id nicht auflösbar (profile.id oder profile.member_id fehlt)"
    - "D-07: ProfileBackgroundCard.tsx analog zu MemberAvatarCard.tsx eingebunden"
    - "D-05: Owner-Chip in beiden Cards zeigt Mitglied-Name read-only; ist nie editierbar"
    - "SC1: Alle 5 Surfaces haben MediaOwnershipContext eingebunden — kein Upload-Pfad ohne Owner-/Status-Kontext"
    - "SC5: Alle Surfaces nutzen dieselbe MediaOwnershipContext-Komponente — keine Surface-eigene Owner-/Status-Logik"
  artifacts:
    - path: "frontend/src/app/me/profile/components/MemberAvatarCard.tsx"
      provides: "Surface 5a mit MediaOwnershipContext (Branding-Slot, schlanker Flow)"
      contains: "MediaOwnershipContext"
    - path: "frontend/src/app/me/profile/components/ProfileBackgroundCard.tsx"
      provides: "Surface 5b mit MediaOwnershipContext (Branding-Slot)"
      contains: "MediaOwnershipContext"
    - path: "frontend/src/app/me/profile/page.tsx"
      provides: "Avatar/Hintergrund-Upload-Handler mit Status-Codes (visibilityCode='public', reviewStatusCode='approved')"
      contains: "visibilityCode"
  key_links:
    - from: "MemberAvatarCard.tsx"
      to: "MediaOwnershipContext"
      via: "ownerID={ownerMemberID}"
      pattern: "MediaOwnershipContext"
    - from: "page.tsx"
      to: "Avatar-Upload-Helfer in api.ts"
      via: "visibilityCode: 'public', reviewStatusCode: 'approved'"
      pattern: "visibilityCode"
---

<objective>
Surface 5 (Member Media — Avatar, Profil-Hintergrund, Story-Bilder) auf die gemeinsame MediaOwnershipContext-Komponente heben. Die Cards zeigen den Owner-Chip read-only; die eigentliche Upload-Logik liegt in page.tsx. Nach diesem Plan sind alle 5 Surfaces implementiert. Abschluss mit Human-Verify (visuelle/funktionale Prüfung im Browser).

Purpose: Letzter Surface-Schritt — nach diesem Plan erfüllt die Phase die Media-Ownership-Matrix vollständig (SC1). Human-Verify prüft alle 5 Surfaces auf Owner-Chip-Sichtbarkeit und Status-Verhalten.
Output: MemberAvatarCard.tsx + ProfileBackgroundCard.tsx mit Owner-Chip; page.tsx mit Status-Codes; Human-Verify der gesamten Phase.
</objective>

<execution_context>
@C:\Users\admin\Documents\Team4s\.planning\phases\79-medien-ownership-in-ui-durchsetzen\79-UI-SPEC.md
@C:\Users\admin\Documents\Team4s\.planning\phases\79-medien-ownership-in-ui-durchsetzen\79-RESEARCH.md
@C:\Users\admin\Documents\Team4s\.planning\phases\79-medien-ownership-in-ui-durchsetzen\79-PATTERNS.md
@C:\Users\admin\Documents\Team4s\.planning\phases\79-medien-ownership-in-ui-durchsetzen\79-CONTEXT.md
@C:\Users\admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\admin\Documents\Team4s\.planning\ROADMAP.md

<interfaces>
<!-- Aus Plan 79-01: MediaOwnershipContext -->
MediaOwnershipContextProps { ownerType, ownerID: number|null, ownerLabel, categoryMode, categoryValue?, statusPolicy, disabled?, onContextChange }
MediaOwnershipContextValue { ownerResolved: boolean; visibilityCode: string; reviewStatusCode: string; categoryValue: string }

<!-- Aus 79-PATTERNS.md MemberAvatarCard.tsx + ProfileBackgroundCard.tsx -->
MemberAvatarCard.tsx — bekannte Struktur (Z. 86–88, 112–124):
  const canUpload = profile.capabilities.can_upload_own_avatar
  onAvatarSelected: (payload: { sourceFile: File; croppedFile: File }) => Promise<void> | void
  Profile hat: profile.id oder profile.member_id (owner_member_id aus Migration 0090)

ProfileBackgroundCard.tsx — analog strukturiert:
  onBackgroundSelected: (payload: ...) => Promise<void> | void

page.tsx (me/profile/page.tsx) enthält die Upload-Handler die onAvatarSelected/onBackgroundSelected implementieren.
Avatar-Upload läuft über AvatarCropDialog → onAvatarSelected → page-level Handler → api.ts Helfer.
Relevanter api.ts Helfer: Avatar-Upload (suche nach 'uploadOwnMemberAvatar' oder ähnlichem in api.ts ≈Z. 8000+)

RESEARCH Assumption A3: Member-Avatar-Upload-Pfad läuft über page-level Handler.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface 5a/5b — MemberAvatarCard.tsx + ProfileBackgroundCard.tsx: Owner-Chip einbinden</name>
  <files>
    frontend/src/app/me/profile/components/MemberAvatarCard.tsx,
    frontend/src/app/me/profile/components/ProfileBackgroundCard.tsx
  </files>
  <read_first>
    - frontend/src/app/me/profile/components/MemberAvatarCard.tsx — vollständig lesen (Props-Interface, canUpload-Gate, onAvatarSelected-Callback-Nutzung, JSX-Struktur)
    - frontend/src/app/me/profile/components/ProfileBackgroundCard.tsx — vollständig lesen (analog zu MemberAvatarCard)
    - frontend/src/components/admin/media/MediaOwnershipContext.tsx — aus Plan 79-01 erzeugt; Props-Interface
    - .planning/phases/79-medien-ownership-in-ui-durchsetzen/79-UI-SPEC.md — Surface-Matrix Zeile 5: ownerType='member', categoryMode='slot', statusPolicy='immediate'
    - .planning/phases/79-medien-ownership-in-ui-durchsetzen/79-PATTERNS.md — Abschnitt MemberAvatarCard + ProfileBackgroundCard
  </read_first>
  <action>
    MemberAvatarCard.tsx und ProfileBackgroundCard.tsx erhalten den Owner-Chip als informative Anzeige.

    Da Upload-Logik in page.tsx liegt (Cards delegieren via Callback), ist MediaOwnershipContext hier ein reiner Anzeige-Block (kein onContextChange-Seiteneffekt nötig — der Kontext ist fix aus dem Profil bekannt).

    Für beide Cards:
    MediaOwnershipContext und MediaOwnershipContextValue aus '@/components/admin/media/MediaOwnershipContext' importieren.

    Owner-ID-Extraktion: Profile-Props prüfen auf das feld, das owner_member_id trägt (z.B. profile.id, profile.member_id — lese die tatsächliche Feldbezeichnung aus der Props-Typ-Definition der Card).

    Im JSX: MediaOwnershipContext VOR dem bestehenden Card-Inhalt einbinden:
    - ownerType="member"
    - ownerID: owner_member_id-Wert aus profile-Prop (Nummer oder null)
    - ownerLabel: Profil-Name aus profile-Prop (z.B. profile.name oder profile.display_name); Format entsprechend UI-SPEC: 'Upload für: Profil «{name}» · Owner-Typ: Mitglied'
    - categoryMode="slot" (D-08: Avatar-Slot ist read-only)
    - categoryValue: Slot-Bezeichnung — 'avatar' für MemberAvatarCard, 'hintergrund' für ProfileBackgroundCard
    - statusPolicy="immediate" (D-09: Member-Branding-Slots sofort sichtbar)
    - disabled={isUploading}
    - onContextChange: kann eine no-op-Funktion sein (der Kontext ist für die Anzeige; Upload-Entscheidung liegt in page.tsx)

    D-06-Guard in den Cards: Wenn profile-Prop vorhanden aber owner_member_id/profile.id = null → canUpload bleibt false (bestehende Guard-Logik); zusätzlich MediaOwnershipContext rendert ErrorState statt Chip (übernimmt das automatisch bei ownerID=null).

    Kein neues natives Form-Element. Alle Strings deutsch. Dateigröße ≤ 450 Zeilen je Datei.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    MemberAvatarCard.tsx importiert und rendert MediaOwnershipContext mit ownerType='member', statusPolicy='immediate'.
    ProfileBackgroundCard.tsx analog eingebunden.
    Owner-Chip zeigt Profil-Name read-only.
    Bei ownerID=null: ErrorState statt Owner-Chip (D-06 über MediaOwnershipContext).
    TypeScript kompiliert ohne Fehler. Dateigröße je ≤ 450 Zeilen.
  </done>
</task>

<task type="auto">
  <name>Task 2: Surface 5 — page.tsx: Avatar/Hintergrund-Upload mit Status-Codes</name>
  <files>
    frontend/src/app/me/profile/page.tsx
  </files>
  <read_first>
    - frontend/src/app/me/profile/page.tsx — vollständig lesen; Avatar-Upload-Handler finden (onAvatarSelected-Implementierung); API-Helfer-Aufruf identifizieren (z.B. uploadOwnMemberAvatar oder ähnliches); Profile-Datenstruktur (member_id, owner_member_id)
    - frontend/src/lib/api.ts — Avatar-Upload-Helfer (suche nach 'uploadOwnMemberAvatar' oder 'uploadMemberAvatar' oder 'avatar' in Z. 8000–8200); falls dieser Helfer noch keine Status-Felder hat: feststellen ob Plan 79-02 ihn hätte erweitern sollen (wenn nicht in den 4 Haupt-Helfern — dann hier im page.tsx direkt FormData ergänzen als Ausnahme)
    - .planning/phases/79-medien-ownership-in-ui-durchsetzen/79-RESEARCH.md — Surface 5 Befunde, Assumption A3
  </read_first>
  <action>
    In page.tsx die onAvatarSelected-Implementierung finden und erweitern.

    Strategie (Assumption A3 bestätigt — Upload läuft über page-level Handler):

    Falls Avatar-Upload-Helfer in api.ts (z.B. uploadOwnMemberAvatar) noch kein visibilityCode/reviewStatusCode-Feld hat:
    Den Helfer-Aufruf in page.tsx um visibilityCode: 'public' und reviewStatusCode: 'approved' ergänzen (Branding-Slot D-09).
    Falls der Helfer in api.ts die optionalen Felder noch nicht hat: api.ts ebenfalls analog zu Plan 79-02 um diese Felder erweitern (gleiche buildBody-if-Logik, Interface-Erweiterung).

    D-06-Guard in page.tsx: Vor dem Upload-Aufruf prüfen ob profile?.id (oder profile?.member_id) vorhanden ist. Wenn nicht: Fehlermeldung setzen ('Upload nicht möglich: Kein gültiger Member-Kontext.') und return.

    Analog für onBackgroundSelected-Handler (ProfileBackgroundCard-Callback).

    Falls Story-Bilder-Upload (uploadOwnProfileStoryImage) ebenfalls in page.tsx liegt: analog visibilityCode/reviewStatusCode ergänzen — Story-Bilder sind Prozessmedien (statusPolicy='in_review' = visibilityCode='private', reviewStatusCode='in_review').

    Alle Strings deutsch. Keine neuen nativen Form-Elemente.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20 && npx vitest run src/components/admin/media/ --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>
    page.tsx Avatar-Upload-Handler übergibt visibilityCode='public', reviewStatusCode='approved'.
    page.tsx Hintergrund-Upload-Handler übergibt visibilityCode='public', reviewStatusCode='approved'.
    D-06-Guard: Kein Upload wenn profile.id/member_id fehlt.
    TypeScript kompiliert ohne Fehler.
    Bestehende vitest-Tests grün.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Alle 5 Surfaces haben MediaOwnershipContext eingebunden:
    1. Surface 1 — Fansub Branding (MediaUpload.tsx): Owner-Chip 'Upload für: Gruppe «X»', sofort sichtbar.
    2. Surface 2 — Anime Assets (AnimeJellyfinAssetUploadControls.tsx): Owner-Chip 'Upload für: Anime «X»', sofort sichtbar.
    3. Surface 3 — Release Theme Assets (ReleaseThemeAssetsSection.tsx): Owner-Chip 'Upload für: Release «X»', sofort sichtbar; kein natives <select> mehr.
    4. Surface 4 — Release-Version Process Media (ReleaseVersionMediaSection.tsx): Owner-Chip + Status-Dropdown (6 Labels, Default 'in Prüfung') + Kategorie-Dropdown.
    5. Surface 5 — Member Media (MemberAvatarCard, ProfileBackgroundCard): Owner-Chip 'Upload für: Profil «X»', sofort sichtbar.
    Backend-Pfad: visibility_id + review_status_id werden bei neuen Uploads geschrieben.
  </what-built>
  <how-to-verify>
    Starte die Dev-Umgebung: http://localhost:3000 (Dev-Server, nicht Docker :3002).

    **Surface 1 — Fansub Branding:**
    1. Öffne /admin/fansubs/{id}/edit → Tab mit Logo/Banner-Upload.
    2. Prüfe: Owner-Chip 'Upload für: Gruppe «{name}»' ist sichtbar (read-only).
    3. Prüfe: Kein Status-Dropdown sichtbar (Branding = sofort sichtbar, D-09).
    4. Lade ein Test-Bild hoch; kein Fehler.
    5. Prüfe in DB: SELECT visibility_id, review_status_id FROM media_assets ORDER BY id DESC LIMIT 1; → beide Felder gesetzt (nicht NULL).

    **Surface 4 — Release-Version Process Media:**
    1. Öffne /admin/episode-versions/{id}/edit → Release-Version-Medien-Tab.
    2. Prüfe: Owner-Chip 'Upload für: Release-Version «{id}»' ist sichtbar.
    3. Prüfe: Kategorie-Dropdown mit 4 Optionen (Screenshot/Typesetting Karaoke/Fun Outtake/Sonstiges).
    4. Prüfe: Status-Dropdown mit 6 Optionen; Default 'in Prüfung' vorbefüllt.
    5. Prüfe: Hint 'Neue Uploads starten in «in Prüfung» und werden im Review freigegeben.' sichtbar.
    6. Upload testen → media_assets.review_status_id zeigt 'in_review'.

    **Surface 5 — Member Media:**
    1. Öffne /me/profile → Avatar-Bereich.
    2. Prüfe: Owner-Chip 'Upload für: Profil «{name}»' sichtbar.
    3. Prüfe: Kein Status-Dropdown (Branding-Slot).

    **Fehlerfall D-06 (optional prüfen):**
    Wenn ein Upload-Bereich ohne Owner-Kontext aufgerufen werden kann (z.B. direkte URL mit ungültiger ID), sollte 'Upload nicht möglich' statt des Formulars erscheinen.
  </how-to-verify>
  <verify>
    <human-check>
      Sind Owner-Chips auf allen getesteten Surfaces sichtbar?
      Zeigt Surface 4 das Status-Dropdown mit Default 'in Prüfung'?
      Sind Branding-Surfaces (1/2/3/5) ohne Status-Dropdown dargestellt?
      Sind neue media_assets.visibility_id + review_status_id in der DB gesetzt (nicht NULL)?
    </human-check>
  </verify>
  <resume-signal>
    Schreibe "bestanden" wenn alle Checks grün, oder beschreibe gefundene Abweichungen.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Grenze | Beschreibung |
|--------|-------------|
| profile.id / profile.member_id → ownerID | kommt aus server-side Profil-Session; Backend requireMeIdentity-Gate ist Authority |
| Frontend visibilityCode='public' → Member-Avatar-Upload | D-09 legt sofortige Sichtbarkeit fest; Backend-Handler setzt Default unabhängig |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-05-01 | Broken Access Control (Lock I) | Member-Avatar-Upload — owner_member_id | mitigate | Backend requireMeIdentity-Gate (existiert bereits) verhindert Upload unter fremdem Member-Kontext; phase 79 ändert kein Capability-System |
| T-79-05-02 | Elevation of Privilege | Sofort-Freigabe Member-Avatar (D-09) | accept | Member-Avatar/Hintergrund sind per Definition die öffentliche Identität des Mitglieds; sofortige Sichtbarkeit ist fachliches Soll (D-09); Owner-Kontext ist zwingend (D-06 Guard) |
| T-79-05-03 | Tampering | Story-Bilder — statusPolicy='in_review' im Frontend, aber Backend-Default falsch | mitigate | Story-Bilder-Handler in page.tsx übergibt explizit reviewStatusCode='in_review'; Backend-Prozessmedien-Default als Fallback |
  | T-79-05-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Pakete |
</threat_model>

<verification>
TypeScript:
cd frontend && npx tsc --noEmit

Test-Suite (vollständig, da letzter Plan):
cd frontend && npx vitest run --reporter=verbose 2>&1 | tail -30

Integritätsprüfung aller 5 Surfaces:
grep -rl "MediaOwnershipContext" \
  frontend/src/components/admin/MediaUpload.tsx \
  frontend/src/app/admin/anime/components/AnimeEditPage/AnimeJellyfinAssetUploadControls.tsx \
  frontend/src/app/admin/fansubs/ \
  frontend/src/app/admin/episode-versions/ \
  frontend/src/app/me/profile/components/
→ alle 5 Dateien gelistet

Natives-select-Audit (außerhalb @/components/ui):
grep -rn "<select" frontend/src/app/admin/ frontend/src/components/admin/
→ kein Treffer außerhalb der ui/-Dateien

Ownership-Check Backend (nach Fansub-Logo-Upload):
SELECT id, visibility_id, review_status_id FROM media_assets ORDER BY id DESC LIMIT 3;
→ beide Felder nicht NULL
</verification>

<success_criteria>
1. MemberAvatarCard.tsx und ProfileBackgroundCard.tsx rendert MediaOwnershipContext mit ownerType='member', statusPolicy='immediate'.
2. page.tsx Avatar/Hintergrund-Upload-Handler übergibt visibilityCode='public', reviewStatusCode='approved'.
3. D-06-Guard in page.tsx: Upload blockiert wenn profile.id/member_id fehlt.
4. npx tsc --noEmit ohne Fehler.
5. cd frontend && npx vitest run ohne Fehler (volle Suite grün).
6. Human-Verify: Owner-Chips auf allen 5 Surfaces sichtbar; Surface 4 zeigt Status-Dropdown mit Default 'in Prüfung'; DB-Check: visibility_id + review_status_id nicht NULL nach neuem Upload.
</success_criteria>

<output>
Erstelle .planning/phases/79-medien-ownership-in-ui-durchsetzen/79-05-SUMMARY.md wenn fertig.
</output>
