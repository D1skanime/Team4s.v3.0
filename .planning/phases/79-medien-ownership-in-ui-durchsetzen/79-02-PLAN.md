---
phase: 79-medien-ownership-in-ui-durchsetzen
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/contracts/openapi.yaml
  - backend/internal/models/media.go
  - backend/internal/repository/media_repository.go
  - backend/internal/handlers/fansub_media_upload.go
  - backend/internal/handlers/admin_content_release_version_media.go
  - backend/internal/handlers/admin_content_release_theme_assets.go
  - backend/internal/handlers/member_media_upload.go
  - frontend/src/lib/api.ts
autonomous: true
requirements:
  - G
  - K

must_haves:
  truths:
    - "Lock K: openapi.yaml Upload-Request-Bodies für POST /admin/fansubs/{id}/media, POST /admin/release-versions/{versionId}/media, POST /admin/fansubs/{fansubId}/anime/{animeId}/theme-assets und den Member-Media-Endpunkt (Avatar/Hintergrund/Story) enthalten optionale Felder visibility_code und review_status_code"
    - "Lock K: MediaAssetCreateInput in media.go enthält VisibilityCode *string und ReviewStatusCode *string"
    - "D-09: fansub_media_upload.go setzt Default visibility_code='public', review_status_code='approved' wenn Felder fehlen (Branding-Slot)"
    - "D-03: admin_content_release_version_media.go setzt Default visibility_code='private', review_status_code='in_review' wenn Felder fehlen (Prozessmedien)"
    - "D-09 (Surface 5): Member-Avatar/Hintergrund-Handler setzt Default visibility_code='public', review_status_code='approved' serverseitig (Identity-/Branding-Slot, Lock I — owner_member_id = angemeldeter User wird serverseitig erzwungen)"
    - "D-03 (Surface 5): Story-Bilder-Handler (uploadOwnProfileStoryImage) setzt Default visibility_code='private', review_status_code='in_review' (Prozessmedien)"
    - "Lock K: media_repository.go CreateMediaAsset INSERT nutzt Code-Sub-SELECT (SELECT id FROM visibilities WHERE name = $N) statt hardgecodeter IDs"
    - "Lock K: api.ts uploadFansubMedia, uploadReleaseVersionMedia, uploadAdminReleaseThemeAsset, uploadAdminAnimeMedia, uploadOwnProfileStoryImage und Avatar/Hintergrund-Helfer akzeptieren optionale visibilityCode und reviewStatusCode Parameter"
  artifacts:
    - path: "shared/contracts/openapi.yaml"
      provides: "Erweiterte Upload-Schemas mit visibility_code/review_status_code (alle 5 Surfaces inkl. Member-Media)"
      contains: "visibility_code"
    - path: "backend/internal/models/media.go"
      provides: "MediaAssetCreateInput mit VisibilityCode/ReviewStatusCode"
      contains: "VisibilityCode"
    - path: "backend/internal/repository/media_repository.go"
      provides: "CreateMediaAsset INSERT mit Sub-SELECT für Lookup-IDs"
      contains: "SELECT id FROM visibilities WHERE name"
    - path: "backend/internal/handlers/member_media_upload.go"
      provides: "Member-Avatar/Hintergrund-Handler mit Branding-Default (public/approved) und serverseitigem owner_member_id-Enforcement (Lock I)"
      contains: "ReviewStatusCode"
  key_links:
    - from: "fansub_media_upload.go"
      to: "media_repository.go CreateMediaAsset"
      via: "models.MediaAssetCreateInput{VisibilityCode, ReviewStatusCode}"
      pattern: "VisibilityCode"
    - from: "admin_content_release_version_media.go processOneRVMFile"
      to: "media_repository.go CreateMediaAssetWithStatusTx"
      via: "models.MediaAssetCreateInput{VisibilityCode, ReviewStatusCode}"
      pattern: "ReviewStatusCode"
    - from: "api.ts uploadFansubMedia"
      to: "POST /api/v1/admin/fansubs/{id}/media"
      via: "FormData visibility_code/review_status_code"
      pattern: "visibility_code"
    - from: "api.ts uploadOwnProfileStoryImage / Avatar-Helfer"
      to: "Member-Media-Endpunkt (Backend)"
      via: "FormData visibility_code/review_status_code"
      pattern: "visibility_code"
---

<objective>
Den vollständigen Lock-K-konformen Contract-Pfad für Sichtbarkeit und Reviewstatus bei Uploads implementieren: OpenAPI → Go-Models → Repository → Handler → api.ts. Nach diesem Plan überträgt jeder Upload-Helfer optionale `visibility_code`/`review_status_code`-Codes an das Backend, und das Backend löst die IDs per Sub-SELECT auf und schreibt sie in `media_assets.visibility_id`/`review_status_id`.

**Surface 5 (Member Media) ist vollständig eingeschlossen:** Der Member-Avatar/Hintergrund/Story-Handler erhält Whitelist + Sub-SELECT-Persistenz. `owner_member_id` wird serverseitig aus dem authentifizierten Session-Context gesetzt (Lock I — nie aus dem Frontend-Request). Plan 79-05 Task 2 ruft ausschließlich die hier erweiterten api.ts-Helfer auf.

Purpose: Ohne diesen Backend-Pfad können die Surface-Integrationen (Pläne 03–05) keine Owner-/Status-Daten persistieren. Lock K erzwingt, dass alle 5 Schichten gemeinsam bewegt werden.
Output: Erweiterte OpenAPI-Schemas (alle 5 Surfaces), Go-Model-Erweiterung, Repository-INSERT mit Code-Lookup, 4 erweiterte + 1 neuer/erweiterter Go-Handler, 6 erweiterte api.ts-Helfer.
</objective>

<execution_context>
@C:\Users\admin\Documents\Team4s\.planning\phases\79-medien-ownership-in-ui-durchsetzen\79-RESEARCH.md
@C:\Users\admin\Documents\Team4s\.planning\phases\79-medien-ownership-in-ui-durchsetzen\79-PATTERNS.md
@C:\Users\admin\Documents\Team4s\.planning\phases\79-medien-ownership-in-ui-durchsetzen\79-CONTEXT.md
@C:\Users\admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\admin\Documents\Team4s\.planning\ROADMAP.md
@C:\Users\admin\Documents\Team4s\.planning\STATE.md

<interfaces>
<!-- Bestehende Signaturen die erweitert werden — aus PATTERNS.md extrahiert -->

backend/internal/models/media.go (aktuell, Z. 28–37):
  type MediaAssetCreateInput struct {
    Kind        MediaKind
    Filename    string
    StoragePath string
    PublicURL   string
    MimeType    string
    SizeBytes   int64
    Width       *int
    Height      *int
  }

backend/internal/repository/media_repository.go — bestehender UPDATE-Lookup (Z. 522–527):
  UPDATE media_assets ma
  SET visibility_id = (SELECT id FROM visibilities WHERE name = $1 LIMIT 1),
      review_status_id = (SELECT id FROM review_statuses WHERE code = $2 LIMIT 1)
  WHERE ma.id = $3

backend/internal/handlers/fansub_media_upload.go — FormData-Parse (Z. 43–47):
  kind, err := parseMediaKind(c.PostForm("kind"))

backend/internal/handlers/admin_content_release_version_media.go — processOneRVMFile Signatur (Z. 265–272):
  func (h *AdminContentHandler) processOneRVMFile(
    c *gin.Context, fileHeader *multipart.FileHeader,
    versionID int64, category string, sortOrder int, uploadedByUserID int64,
  ) rvmFileResult

frontend/src/lib/api.ts — uploadFansubMedia Muster (Z. 2253–2273):
  interface FansubMediaUploadOptions {
    fansubID: number; kind: FansubMediaKind; file: File;
    onProgress?: (percent: number) => void;
  }
  buildBody: () => { body.set("kind", options.kind); body.set("file", options.file); return body; }

frontend/src/lib/api.ts — uploadOwnProfileStoryImage (≈Z. 8112):
  Helfer für Story-Bilder-Upload unter /me/profile — genaue Signatur beim Lesen ermitteln

shared/contracts/openapi.yaml — uploadFansubMedia requestBody (Z. 3488–3500):
  properties: { kind: {$ref: FansubMediaKind}, file: {type: string, format: binary} }

shared/contracts/openapi.yaml — uploadReleaseVersionMedia requestBody (Z. 5150–5174):
  properties: { category: {$ref: RVM_Category}, caption, is_preview_candidate, files[] }

Member-Media-Backend: Suche nach Avatar-Upload-Handler und Story-Bild-Upload-Handler in
  backend/internal/handlers/ (grep nach 'avatar' oder 'member_media' oder 'story') —
  ermittle den genauen Dateinamen und Funktionsnamen beim Lesen.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OpenAPI + Go-Model erweitern (Lock K, Schicht 1+2) — alle 5 Surfaces</name>
  <files>
    shared/contracts/openapi.yaml,
    backend/internal/models/media.go
  </files>
  <read_first>
    - shared/contracts/openapi.yaml — Z. 3473–3510 (uploadFansubMedia requestBody) und Z. 5139–5180 (uploadReleaseVersionMedia requestBody) und Theme-Asset-Endpunkt (suche nach 'theme-assets') und Member-Media/Avatar-Endpunkt (suche nach 'avatar' oder 'member' oder 'story')
    - backend/internal/models/media.go — Z. 1–60 (MediaAssetCreateInput, MediaKind-Konstanten)
    - .planning/phases/79-medien-ownership-in-ui-durchsetzen/79-PATTERNS.md — Abschnitt openapi.yaml + media.go
  </read_first>
  <behavior>
    - openapi.yaml: uploadFansubMedia request body erhält optionale Properties visibility_code (string, nullable) und review_status_code (string, nullable)
    - openapi.yaml: uploadReleaseVersionMedia request body analog erweitert
    - openapi.yaml: uploadAdminReleaseThemeAsset request body analog erweitert
    - openapi.yaml: uploadAdminAnimeMedia (POST /admin/upload) request body analog erweitert
    - openapi.yaml: Member-Media-Endpunkte (Avatar, Hintergrund, Story-Bilder — alle gefundenen) analog erweitert
    - models/media.go: MediaAssetCreateInput bekommt VisibilityCode *string und ReviewStatusCode *string als neue Felder
  </behavior>
  <action>
    In shared/contracts/openapi.yaml alle Upload-Request-Bodies (Fansub, RVM, Theme-Asset, Admin-Anime, Member-Avatar/Hintergrund/Story) um zwei optionale Properties erweitern:
    visibility_code (type: string, nullable: true, description: "Sichtbarkeits-Code (z.B. 'public', 'private'). Backend setzt Default wenn leer.") und
    review_status_code (type: string, nullable: true, description: "Reviewstatus-Code (z.B. 'approved', 'in_review'). Backend setzt Default wenn leer.").
    Nicht required — optionale Erweiterung. Kein bestehendes required-Array verändern.

    In backend/internal/models/media.go die Struct MediaAssetCreateInput um zwei Felder ergänzen:
    VisibilityCode *string (nil = Backend-Default) und ReviewStatusCode *string (nil = Backend-Default).
    Kein anderes Feld ändern. Keine Imports hinzufügen (nur primitive Typen).
  </action>
  <verify>
    <automated>cd backend && go build ./... 2>&1 | head -20</automated>
  </verify>
  <done>
    openapi.yaml Upload-Schemas (alle 5 Surfaces) enthalten visibility_code + review_status_code.
    media.go MediaAssetCreateInput hat VisibilityCode/ReviewStatusCode Felder.
    go build läuft durch.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repository INSERT mit Code-Sub-SELECT + Handler-Erweiterung inkl. Surface-5-Handler (Lock K, Schicht 3+4)</name>
  <files>
    backend/internal/repository/media_repository.go,
    backend/internal/handlers/fansub_media_upload.go,
    backend/internal/handlers/admin_content_release_version_media.go,
    backend/internal/handlers/admin_content_release_theme_assets.go,
    backend/internal/handlers/member_media_upload.go
  </files>
  <read_first>
    - backend/internal/repository/media_repository.go — vollständig lesen (CreateMediaAsset Z. 93–146, CreateMediaAssetWithStatusTx, UPDATE-Sub-SELECT-Muster Z. 522–527)
    - backend/internal/handlers/fansub_media_upload.go — vollständig lesen (Z. 1–160, inkl. storeFansubMediaUpload, saveFansubMediaUpload, parseMediaKind)
    - backend/internal/handlers/admin_content_release_version_media.go — Z. 260–430 (processOneRVMFile Signatur und Body, CreateMediaAssetWithStatusTx-Aufruf)
    - backend/internal/handlers/admin_content_release_theme_assets.go — vollständig lesen (UploadReleaseThemeAsset Handler, CreateMediaAsset-Aufruf)
    - backend/internal/handlers/member_media_upload.go — vollständig lesen (Avatar-Upload-Handler, Story-Upload-Handler, owner_member_id-Ableitung aus Session/Context, CreateMediaAsset-Aufruf). Falls Datei nicht existiert: grep -r 'avatar\|story\|member.*upload\|upload.*member' backend/internal/handlers/ um die richtige Datei zu finden.
    - .planning/phases/79-medien-ownership-in-ui-durchsetzen/79-PATTERNS.md — Abschnitte media_repository.go, fansub_media_upload.go, admin_content_release_version_media.go
  </read_first>
  <behavior>
    Repository:
    - CreateMediaAsset: wenn input.VisibilityCode != nil und input.ReviewStatusCode != nil → INSERT mit Sub-SELECT (analog zum bestehenden UPDATE-Pattern Z. 522–527); wenn nil → INSERT ohne diese Felder (Spalten bleiben NULL, Backend-Handler setzt Defaults vor dem Aufruf)
    - CreateMediaAssetWithStatusTx: analog erweitert

    fansub_media_upload.go (Branding, D-09):
    - Nach parseMediaKind: visibilityCode = strings.TrimSpace(c.PostForm("visibility_code")); reviewStatusCode = strings.TrimSpace(c.PostForm("review_status_code"))
    - Whitelist-Validierung für visibilityCode (valide: 'public','private','registered','fansubber','staff'; leer erlaubt); bei ungültigem Wert: badRequest mit 'ungültiger visibility_code'
    - Whitelist-Validierung für reviewStatusCode (valide: 'in_review','approved','rejected','archived','removed'; leer erlaubt); bei ungültig: badRequest
    - D-09 Branding-Default: wenn visibilityCode == "" → visibilityCode = "public"; wenn reviewStatusCode == "" → reviewStatusCode = "approved"
    - Diese Codes über die existierende Aufrufkette in models.MediaAssetCreateInput{VisibilityCode: &visibilityCode, ReviewStatusCode: &reviewStatusCode} eintragen

    admin_content_release_version_media.go (Prozessmedien, D-03):
    - processOneRVMFile: zwei neue Parameter visibilityCode string, reviewStatusCode string
    - Aufrufstelle liest c.PostForm("visibility_code") und c.PostForm("review_status_code"); Whitelist-Validierung analog
    - D-03 Prozessmedien-Default: wenn visibilityCode == "" → visibilityCode = "private"; wenn reviewStatusCode == "" → reviewStatusCode = "in_review"
    - createInput bekommt VisibilityCode: &visibilityCode, ReviewStatusCode: &reviewStatusCode

    admin_content_release_theme_assets.go (Theme-Assets, D-09 = Branding):
    - Analog zu fansub_media_upload.go: visibility_code/review_status_code aus FormData lesen, Branding-Defaults setzen (public/approved), an CreateMediaAsset weitergeben

    member_media_upload.go (Surface 5, Lock I + D-09/D-03):
    - Avatar- und Hintergrund-Handler (Branding-Slots, D-09): visibility_code/review_status_code aus FormData lesen; Whitelist analog; Branding-Default: wenn leer → visibilityCode = "public", reviewStatusCode = "approved"
    - KRITISCH (Lock I): owner_member_id wird NICHT aus dem Request gelesen — ausschließlich aus dem authentifizierten Session-Context (requireMeIdentity oder äquivalentes Gate) ableiten; kein Frontend-Wert wird akzeptiert
    - Story-Bilder-Handler (Prozessmedien, D-03): visibility_code/review_status_code aus FormData lesen; Prozessmedien-Default wenn leer: visibilityCode = "private", reviewStatusCode = "in_review"
    - Alle Handler: models.MediaAssetCreateInput erhält VisibilityCode/ReviewStatusCode; CreateMediaAsset/CreateMediaAssetWithStatusTx mit Sub-SELECT-Persistenz aufrufen (analog zu den anderen Handlers)
  </behavior>
  <action>
    media_repository.go: CreateMediaAsset-Funktion erweitern — wenn input.VisibilityCode != nil, erweiterten INSERT-SQL verwenden:
    INSERT INTO media_assets (media_type_id, file_path, mime_type, format, visibility_id, review_status_id, created_at)
    VALUES ($1, $2, $3, $4, (SELECT id FROM visibilities WHERE name = $5 LIMIT 1), (SELECT id FROM review_statuses WHERE code = $6 LIMIT 1), NOW())
    RETURNING id, file_path, mime_type, created_at
    Bei input.VisibilityCode == nil den bestehenden INSERT ohne diese Spalten lassen.
    CreateMediaAssetWithStatusTx analog erweitern.
    Achtung: Zeilen-Limit 450 prüfen; ggf. SQL als benannte Konstante auslagern.

    fansub_media_upload.go: Nach der existierenden parseMediaKind-Zeile visibilityCode/reviewStatusCode aus FormData lesen; Whitelist-Prüfung; Branding-Defaults (public/approved per D-09); Weitergabe an MediaAssetCreateInput.
    Imports: 'strings' ggf. hinzufügen.

    admin_content_release_version_media.go: processOneRVMFile-Signatur um visibilityCode string, reviewStatusCode string erweitern; Aufrufstelle liest FormData-Werte, prüft Whitelist, setzt Prozessmedien-Defaults (private/in_review per D-03); createInput erhält VisibilityCode/ReviewStatusCode.
    Alle Aufrufstellen von processOneRVMFile aktualisieren (neue Parameter übergeben).

    admin_content_release_theme_assets.go: UploadReleaseThemeAsset-Handler liest visibility_code/review_status_code; Branding-Defaults (public/approved per D-09); MediaAssetCreateInput erweitern.

    member_media_upload.go: Alle relevanten Upload-Handler (Avatar, Hintergrund, Story) erweitern:
    1. visibility_code/review_status_code aus FormData lesen (c.PostForm)
    2. Whitelist-Prüfung analog zu fansub_media_upload.go
    3. Avatar/Hintergrund-Default: public/approved (D-09); Story-Default: private/in_review (D-03)
    4. owner_member_id NUR aus Session-Context: z.B. identity.MemberID aus permissionActorFromContext — kein c.PostForm("owner_member_id") akzeptieren
    5. createInput erhält VisibilityCode, ReviewStatusCode; Sub-SELECT-Persistenz über Repository
  </action>
  <verify>
    <automated>cd backend && go build ./... 2>&1 | head -30</automated>
  </verify>
  <done>
    go build ohne Fehler.
    fansub_media_upload.go: Branding-Default 'public'/'approved' gesetzt wenn Felder fehlen.
    admin_content_release_version_media.go: Prozessmedien-Default 'private'/'in_review' gesetzt wenn Felder fehlen.
    media_repository.go CreateMediaAsset INSERT enthält Sub-SELECT für visibility_id und review_status_id.
    member_media_upload.go: Avatar/Hintergrund-Handler setzen 'public'/'approved'; Story-Handler setzt 'private'/'in_review'; owner_member_id kommt aus Session-Context (Lock I).
    Alle Handler-Dateien kompilieren ohne Warnungen.
  </done>
</task>

<task type="auto">
  <name>Task 3: api.ts Upload-Helfer um visibilityCode/reviewStatusCode erweitern — alle 5 Surfaces (Lock K, Schicht 5)</name>
  <files>
    frontend/src/lib/api.ts
  </files>
  <read_first>
    - frontend/src/lib/api.ts — Z. 2245–2280 (uploadFansubMedia inkl. FansubMediaUploadOptions Interface), Z. 4210–4250 (uploadAdminAnimeMedia), Z. 4415–4445 (uploadAdminReleaseThemeAsset), Z. 6245–6285 (uploadReleaseVersionMedia), Z. 8080–8160 (uploadOwnProfileStoryImage und Avatar-Upload-Helfer — genaue Zeilen per grep 'uploadOwnProfileStoryImage\|uploadOwnMemberAvatar\|avatar.*upload\|upload.*avatar' ermitteln)
    - .planning/phases/79-medien-ownership-in-ui-durchsetzen/79-PATTERNS.md — Abschnitt api.ts Upload-Helfer-Erweiterungen
  </read_first>
  <action>
    In frontend/src/lib/api.ts die Upload-Helfer aller 5 Surfaces erweitern:

    FansubMediaUploadOptions Interface: zwei optionale Felder hinzufügen: visibilityCode?: string und reviewStatusCode?: string.
    uploadFansubMedia buildBody: nach body.set("file", ...) folgende Zeilen einfügen:
    if (options.visibilityCode) body.set("visibility_code", options.visibilityCode);
    if (options.reviewStatusCode) body.set("review_status_code", options.reviewStatusCode);

    AdminAnimeMediaUploadOptions Interface: analog visibilityCode? und reviewStatusCode? hinzufügen.
    uploadAdminAnimeMedia buildBody: gleiche zwei if-Zeilen.

    AdminReleaseThemeAssetUploadOptions (oder entsprechendes Interface für uploadAdminReleaseThemeAsset): analog.
    uploadAdminReleaseThemeAsset buildBody: gleiche zwei if-Zeilen.

    ReleaseVersionMediaUploadOptions Interface: analog.
    uploadReleaseVersionMedia buildBody: gleiche zwei if-Zeilen.

    Surface-5-Helfer (uploadOwnProfileStoryImage + Avatar-Upload-Helfer):
    Beide Interfaces um visibilityCode? und reviewStatusCode? erweitern.
    buildBody beider Helfer: gleiche zwei if-Zeilen für visibility_code/review_status_code.
    (Plan 79-05 Task 2 ruft diese erweiterten Helfer auf — kein ad-hoc FormData-Ergänzen in page.tsx.)

    Nur Interface + buildBody ändern. Keine anderen Teile der Helfer anfassen.
    Alle bestehenden Aufrufstellen bleiben kompatibel (optionale Felder mit Default undefined).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    npx tsc --noEmit ohne Fehler für api.ts.
    Alle Upload-Helfer-Interfaces (Fansub, AnimeMedia, ReleaseThemeAsset, ReleaseVersionMedia, Story, Avatar) enthalten visibilityCode? und reviewStatusCode?.
    buildBody aller Helfer überträgt die Codes in FormData wenn gesetzt.
    Bestehende Aufrufstellen (ohne die neuen Felder) kompilieren weiterhin ohne Fehler.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Grenze | Beschreibung |
|--------|-------------|
| Frontend FormData → Backend Handler | visibility_code/review_status_code kommen als nicht-vertrauenswürdige Strings |
| Backend Handler → Repository | Codes werden per DB-Lookup aufgelöst — kein ID-Trust vom Frontend |
| Frontend → Member-Media-Handler (Surface 5) | owner_member_id kommt NIE aus dem Request — serverseitig aus Session erzwungen (Lock I) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-02-01 | Tampering | visibility_code/review_status_code in FormData | mitigate | Whitelist-Validierung im Handler vor Weiterleitung (valide Codes aus Migration 0037/0097); ungültige Codes → badRequest 400 |
| T-79-02-02 | Broken Access Control (Lock I) | Upload-Handler schreibt unter Owner-Kontext | mitigate | Existierende CanForFansubGroup / CanForReleaseVersion Capability-Gates bleiben unverändert; Phase 79 fügt nur Status-Felder hinzu, verändert keine Capability-Prüfung |
| T-79-02-03 | Elevation of Privilege | Sofort-Freigabe-Pfad (D-09) per manipuliertem reviewStatusCode='approved' | mitigate | Branding-Handler überschreiben mit hartem Default 'public'/'approved'; für Prozessmedien-Handler (RVM): wenn Frontend 'approved' schickt, nimmt Backend es an — serverseitige Leader-Capability-Prüfung bleibt Guard; explizite Freigabe via Review-Workflow (D-04) |
| T-79-02-04 | Tampering | Sub-SELECT mit nicht-existentem Code | mitigate | Whitelist im Handler vor DB-Aufruf (T-79-02-01 abgedeckt); LIMIT 1 liefert sonst NULL → INSERT-Fehler als Fallback |
| T-79-02-05 | Elevation of Privilege (Lock I) | Surface 5 — owner_member_id aus Request fälschen | mitigate | member_media_upload.go liest owner_member_id ausschließlich aus permissionActorFromContext/requireMeIdentity; kein c.PostForm("owner_member_id") wird akzeptiert |
| T-79-02-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Pakete — nur bestehender Go/TS-Stack |
</threat_model>

<verification>
Backend kompiliert:
cd backend && go build ./...

Frontend TypeCheck:
cd frontend && npx tsc --noEmit

Smoke-Test (manuell nach Worktree-Start):
curl -X POST http://localhost:8092/api/v1/admin/fansubs/1/media -H "Authorization: Bearer {token}" -F "kind=logo" -F "file=@test.png" -F "visibility_code=public" -F "review_status_code=approved"
→ Erwartete Antwort: 200 oder 201; media_assets.visibility_id und review_status_id in DB gesetzt.

Ungültige Codes:
curl ... -F "visibility_code=INVALID" → 400 Bad Request mit 'ungültiger visibility_code'

Surface-5-Helfer in api.ts:
grep -n "visibilityCode" frontend/src/lib/api.ts | grep -i "story\|avatar\|profile"
→ mindestens 2 Treffer (uploadOwnProfileStoryImage + Avatar-Helfer)
</verification>

<success_criteria>
1. cd backend && go build ./... läuft ohne Fehler.
2. openapi.yaml enthält visibility_code und review_status_code in den Upload-Request-Bodies (alle 5 Surfaces).
3. media_repository.go CreateMediaAsset enthält Sub-SELECT 'SELECT id FROM visibilities WHERE name'.
4. fansub_media_upload.go: Default visibilityCode='public', reviewStatusCode='approved' wenn Felder leer.
5. admin_content_release_version_media.go: Default visibilityCode='private', reviewStatusCode='in_review' wenn Felder leer.
6. member_media_upload.go: Avatar/Hintergrund-Handler setzt 'public'/'approved'; Story-Handler setzt 'private'/'in_review'; owner_member_id aus Session-Context (Lock I).
7. api.ts: alle Upload-Helfer-Interfaces (inkl. Story + Avatar) enthalten optionale visibilityCode/reviewStatusCode Felder.
8. cd frontend && npx tsc --noEmit ohne Fehler.
</success_criteria>

<output>
Erstelle .planning/phases/79-medien-ownership-in-ui-durchsetzen/79-02-SUMMARY.md wenn fertig.
</output>
