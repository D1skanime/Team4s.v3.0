---
phase: 56-cropper
plan: "02"
type: execute
wave: 2
depends_on:
  - 56-01
files_modified:
  - frontend/src/components/media/crop/AvatarCropDialog.tsx
  - frontend/src/components/media/crop/AvatarCropDialog.test.tsx
  - frontend/src/app/me/profile/components/MemberAvatarCard.tsx
  - frontend/src/app/me/profile/page.test.tsx
autonomous: true
requirements:
  - MEDIA-CROPPER-01
must_haves:
  truths:
    - "D-02: Profile avatar upload remains owned by uploadOwnProfileAvatar and POST /api/v1/me/profile/avatar."
    - "D-10: Avatar upload keeps the source-original plus cropped-display contract."
    - "D-11: The UI sends sourceFile and croppedFile; backend remains authoritative for type/dimension validation and storage."
    - "D-12: Profile/public avatar display must use the cropped display URL, never the retained source original."
    - "D-13: Existing-avatar recrop must continue loading the retained source original when available."
    - "No backend or API helper changes are expected in this plan."
---

<objective>
Migrate the own-profile avatar crop and recrop UX to the shared Team4s cropper while preserving the existing avatar upload contract and dirty-field isolation.
</objective>

<context>
@AGENTS.md
@.planning/phases/56-cropper/56-CONTEXT.md
@.planning/phases/56-cropper/56-01-PLAN.md
@docs/frontend/auth-api-client.md
@frontend/src/components/media/crop/Team4sCropper.tsx
@frontend/src/components/media/crop/AvatarCropDialog.tsx
@frontend/src/app/me/profile/components/MemberAvatarCard.tsx
@frontend/src/app/me/profile/page.tsx
@frontend/src/app/me/profile/page.test.tsx
@frontend/src/lib/api.ts
@frontend/src/types/profile.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace avatar crop internals with the shared adapter</name>
  <files>frontend/src/components/media/crop/AvatarCropDialog.tsx, frontend/src/components/media/crop/AvatarCropDialog.test.tsx</files>
  <read_first>
    - frontend/src/components/media/crop/Team4sCropper.tsx
    - frontend/src/components/media/crop/AvatarCropDialog.tsx
    - frontend/src/components/media/crop/AvatarCropDialog.test.tsx
  </read_first>
  <action>
    Keep `AvatarCropDialog` as a compatibility wrapper if that minimizes call-site churn, but remove its custom crop math/canvas implementation. Delegate crop UI/export to `Team4sCropper` with 1:1 aspect, circular preview, 512x512 PNG output, and the existing `onApply({ sourceFile, croppedFile })` callback shape.

    Preserve German strings with correct umlauts and keep cancel/apply/reset behavior visible and keyboard reachable.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm test -- --run src/components/media/crop/AvatarCropDialog.test.tsx src/components/media/crop/Team4sCropper.test.tsx</automated>
  </verify>
  <done>
    AvatarCropDialog no longer owns crop geometry/export and still satisfies its public prop contract.
  </done>
</task>

<task type="auto">
  <name>Task 2: Preserve avatar source-original recrop flow</name>
  <files>frontend/src/app/me/profile/components/MemberAvatarCard.tsx, frontend/src/app/me/profile/page.test.tsx</files>
  <read_first>
    - frontend/src/app/me/profile/components/MemberAvatarCard.tsx
    - frontend/src/app/me/profile/page.tsx
    - frontend/src/app/me/profile/page.test.tsx
    - frontend/src/lib/api.ts
  </read_first>
  <action>
    Confirm `MemberAvatarCard` still loads `sourceAvatarURL` for existing-avatar recrop and still passes the original `File` plus newly cropped output to `onAvatarSelected`.

    Do not change `uploadOwnProfileAvatar`, bearer handling, profile dirty-state, or profile save behavior. Avatar upload remains a separate action from profile-field save.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm test -- --run src/app/me/profile/page.test.tsx</automated>
  </verify>
  <done>
    Existing-avatar recrop uses the retained source original and avatar upload remains token-free through the central API helper.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add avatar crop parity regression coverage</name>
  <files>frontend/src/components/media/crop/AvatarCropDialog.test.tsx, frontend/src/app/me/profile/page.test.tsx</files>
  <read_first>
    - frontend/src/components/media/crop/Team4sCropper.test.tsx
    - frontend/src/app/me/profile/page.test.tsx
  </read_first>
  <action>
    Add or update tests so avatar apply passes a PNG `croppedFile`, keeps `sourceFile`, closes the dialog after success, preserves upload errors locally, and does not dirty or overwrite unrelated profile fields.

    If jsdom cannot validate visual crop parity, document that parity is covered by browser UAT in plan 56-04 rather than asserting false precision in unit tests.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm test -- --run src/components/media/crop/AvatarCropDialog.test.tsx src/app/me/profile/page.test.tsx</automated>
  </verify>
  <done>
    Avatar crop behavior is covered at the wrapper and profile integration levels.
  </done>
</task>

</tasks>

<acceptance_criteria>

- Avatar crop uses `Team4sCropper` and no longer duplicates crop math/export code.
- `onApply({ sourceFile, croppedFile })` behavior remains compatible with `uploadOwnProfileAvatar`.
- Existing-avatar recrop loads `source_original_url` when available.
- Profile dirty state and Keycloak-return refresh behavior are not changed.
- Tests cover avatar crop apply, recrop, success, and error paths.

</acceptance_criteria>

<recommended_checks>

- `cd C:/Users/admin/Documents/Team4s/frontend && npm test -- --run src/components/media/crop/AvatarCropDialog.test.tsx src/app/me/profile/page.test.tsx`
- `cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck`
- `cd C:/Users/admin/Documents/Team4s && git diff --check`

</recommended_checks>
