---
phase: 106-member-gamification-punktefundament
plan: "02"
type: execute
wave: 2
depends_on: ["106-01"]
files_modified:
  - database/migrations/0131_member_point_foundation.up.sql
  - database/migrations/0131_member_point_foundation.down.sql
autonomous: true
requirements: [GAM-01, GAM-02, GAM-03, GAM-04, GAM-05]
must_haves:
  truths:
    - "GAM-01 / D-01: Ledger-Begünstigte sind zwingend members; actor_app_user_id bleibt optional."
    - "GAM-02 / D-04: PostgreSQL erzwingt ein append-only Ledger und ausschließlich valide direkte Gegenbuchungen."
    - "GAM-03 / D-04: Rule-Code, Version, Kategorie und positiver Wert sind unveränderlich und konsistent im Ledger gesnapshottet."
    - "GAM-04 / D-02 / D-06 / D-07: Genau zwei stabile Kategorien existieren ohne Ranking, Profilpunkte oder produktive Werte."
    - "GAM-05 / D-03 / D-05 / D-08 / D-09 / D-10: Die Migration ist additiv, reversibel und berührt keine spätere Review-/Media-/Capability-Struktur."
  artifacts:
    - path: "database/migrations/0131_member_point_foundation.up.sql"
      provides: "Immutable point_rules und DB-erzwungen append-only point_ledger_entries"
      contains: "CREATE TABLE point_ledger_entries"
    - path: "database/migrations/0131_member_point_foundation.down.sql"
      provides: "Reversibler Rückbau ausschließlich der neuen Gamification-Strukturen"
      contains: "DROP TABLE IF EXISTS point_ledger_entries"
  key_links:
    - from: "point_ledger_entries.member_id"
      to: "members.id"
      via: "NOT NULL FK ohne Cascade-Löschung"
      pattern: "member_id.*REFERENCES members"
    - from: "point_ledger_entries.reverses_entry_id"
      to: "point_ledger_entries.id"
      via: "Constraint-Trigger validiert Originaltyp und alle geerbten Felder"
      pattern: "CREATE.*TRIGGER.*reversal"
    - from: "point_ledger_entries.point_rule_id"
      to: "point_rules.id"
      via: "BEFORE INSERT validiert Award-Snapshot ID/Code/Version/Kategorie/Wert gegen genau diese Regel"
      pattern: "CREATE.*TRIGGER.*ledger.*insert"
---

<objective>
Das additive und reversible PostgreSQL-Schema für einen unveränderlichen Regelkatalog und ein datenbankseitig append-only Punktebuch implementieren.

Purpose: PostgreSQL bleibt auch bei direktem SQL der letzte Arbiter für Identität, Snapshots, Stornoinvarianten, Idempotenz und Audit-Historie.
Output: Eine neue Up/Down-Migration, die die Wave-0-Verträge in einer disposable Testdatenbank erfüllt.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/106-member-gamification-punktefundament/106-CONTEXT.md
@.planning/phases/106-member-gamification-punktefundament/106-RESEARCH.md
@.planning/phases/106-member-gamification-punktefundament/106-PATTERNS.md
@.planning/phases/106-member-gamification-punktefundament/106-VALIDATION.md
@.planning/phases/106-member-gamification-punktefundament/106-01-SUMMARY.md
@.planning/notes/260722-member-gamification-DECISION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrationskette prüfen und beide unveränderlichen Tabellen anlegen</name>
  <read_first>
    - AGENTS.md
    - docs/engineering/implementation-contract.md
    - docs/architecture/db-schema-fansub-domain.md
    - .planning/phases/106-member-gamification-punktefundament/106-CONTEXT.md
    - .planning/phases/106-member-gamification-punktefundament/106-RESEARCH.md
    - .planning/phases/106-member-gamification-punktefundament/106-PATTERNS.md
    - backend/internal/migrations/phase106_member_points_test.go
    - database/migrations/0108_capability_registry.up.sql
    - database/migrations/0128_group_history_single_use_events.up.sql
    - database/migrations/0129_release_playback_entitlements.up.sql
    - database/migrations/0087_anime_contribution_roles_and_badges.up.sql
  </read_first>
  <files>database/migrations/0131_member_point_foundation.up.sql</files>
  <action>
Before creating a migration, run `git status --short`, determine the highest tracked numeric Up migration, and list untracked migration files. Proceed with 0131 only if tracked max is 0130 and none is untracked; otherwise renumber both planned files consistently when a tracked migration owns 0131, or stop on an untracked/ambiguous chain per AGENTS.md.

Create `point_rules` with immutable `(rule_code, rule_version)`, exact category allowlist `fansub_work`/`platform_contribution`, positive version/value, and no productive seed, activation, latest, schedule, update, or rollback semantics per D-04. Add a trigger/function rejecting every UPDATE and DELETE.

Create `point_ledger_entries` with award/reversal kind; mandatory Member; nullable Actor, fansub group, and release version using `ON DELETE SET NULL`; stable source type/key; unique server-generated idempotency key; rule identity plus code/version/category/value snapshot; independent effective/recorded timestamps; reversal reference/reason; coupled shape checks; self-reference rejection; and a partial unique index for one direct reversal. Member, rule, and original references use RESTRICT/default NO ACTION.

Add a ledger mutation guard that rejects every DELETE and every direct UPDATE. Its only UPDATE exception is a nested FK action with `pg_trigger_depth() > 1`: at least one of exactly `actor_app_user_id`, `fansub_group_id`, or `release_version_id` must transition non-null→null; each optional context column not being nulled and every other ledger column must remain `IS NOT DISTINCT FROM OLD`. Bind every changed-to-NULL column to actual parent disappearance in the same guard: Actor nulling requires `NOT EXISTS (SELECT 1 FROM app_users WHERE id = OLD.actor_app_user_id)`, group nulling requires `NOT EXISTS (SELECT 1 FROM fansub_groups WHERE id = OLD.fansub_group_id)`, and release-version nulling requires `NOT EXISTS (SELECT 1 FROM release_versions WHERE id = OLD.release_version_id)`. When multiple optional columns are nulled by one nested statement, require every corresponding OLD parent to be absent. If any changed column still has a live parent, raise the same append-only exception. Reject arbitrary nested mutation, null→value/value→different-value transitions, no-op updates, mixed context mutation, and direct `UPDATE ... SET ... = NULL`. Retain all existing depth, exact-transition, and null-safe unchanged-column checks; the added parent-existence proof preserves real A1 `ON DELETE SET NULL` behavior without allowing a foreign trigger to imitate it.

Add one BEFORE INSERT semantic-validation trigger (or equivalently named branch-specific BEFORE INSERT triggers) that makes PostgreSQL the final snapshot arbiter. For an award, load the referenced `point_rules` row and require exact agreement of `point_rule_id`, `rule_code`, `rule_version`, `category_code`, and the positive `point_value`; reject a missing rule or any mismatch even when the FK ID itself is valid. For a reversal, load the referenced row and require it to be an award; require identical Member, source type/key, rule ID/code/version/category, and null-safe fansub-group/release-version context; require the value to be the exact negative of the award. Reject missing originals, reversal-of-reversal, self-reference, or mismatched inherited data. Actor, reason, effective time, recorded time, and the deterministic reversal key remain properties of the reversal command and are not inherited. Do not touch existing review, capability, badge, media, upload, crop, thumbnail, relation, or cleanup objects per D-03/D-05/D-07/D-09/D-10.
  </action>
  <verify>
    <automated>cd backend; go test ./internal/migrations -run 'TestPhase106MigrationUpContract' -count=1</automated>
  </verify>
  <done>Die Up-Migration erzwingt unveränderliche Regeln, exakte Award-Rule-Snapshots, direkte Ledger-Unveränderlichkeit und erlaubt verschachtelte Kontextnullung nur bei nachgewiesenem Wegfall jedes betroffenen OLD-Parents; Cross-Row-Stornoinvarianten bleiben vollständig und es gibt keinen Produktionsseed.</done>
</task>

<task type="auto">
  <name>Task 2: Reversiblen Down-Pfad und disposable PostgreSQL-Wave-Gate abschließen</name>
  <read_first>
    - AGENTS.md
    - docs/engineering/implementation-contract.md
    - docs/architecture/db-schema-fansub-domain.md
    - .planning/phases/106-member-gamification-punktefundament/106-CONTEXT.md
    - .planning/phases/106-member-gamification-punktefundament/106-RESEARCH.md
    - .planning/phases/106-member-gamification-punktefundament/106-PATTERNS.md
    - database/migrations/0131_member_point_foundation.up.sql
    - database/migrations/0108_capability_registry.down.sql
    - backend/internal/migrations/phase106_member_points_test.go
    - backend/internal/testsupport/phase106_postgres.go
    - docker-compose.yml
  </read_first>
  <files>database/migrations/0131_member_point_foundation.down.sql</files>
  <action>
Create a transaction-wrapped Down migration that removes the insert-validation and guarded-mutation triggers/functions plus focused indexes, then drops `point_ledger_entries` before removing the rule immutability trigger/function and dropping `point_rules`. It must not alter any pre-existing table, data, sequence, function, or index.

Keep the per-task feedback fast with the named Down source contract. At the plan wave gate, start the real Compose service `team4sv30-db`, create a uniquely named database `team4s_phase106_test_<random>` through the container's `createdb`, set `TEAM4S_PHASE106_TEST_DSN` only for the test process, run the live Up -> Down -> Up and SQL invariant suite, then remove the environment variable and drop that exact database with `dropdb --force` in `finally`. Never point the tests at `team4s_v2`; do not reuse or reset the ordinary development database. A skipped live test is failure.
  </action>
  <verify>
    <automated>cd backend; go test ./internal/migrations -run 'TestPhase106MigrationDownContract' -count=1</automated>
  </verify>
  <done>Der Down-Vertrag ist grün; das Plan-Wave-Gate beweist Up -> Down -> Up und alle DB-Invarianten in einer anschließend gelöschten Testdatenbank.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Direct SQL -> point tables | DB-Trigger müssen Mutation und ungültige Cross-Row-Stornos unabhängig vom Go-Code abweisen. |
| Domain deletion -> ledger history | Nur optionale Kontext-FKs dürfen nullen; Identität und Auditkette bleiben erhalten. |
| Test runner -> PostgreSQL | Live-Tests laufen ausschließlich in einer pro Gate erzeugten und danach gelöschten Testdatenbank. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-05 | Tampering | point_rules | mitigate | PostgreSQL-Trigger verbietet UPDATE/DELETE. |
| T-106-06 | Tampering | point_ledger_entries | mitigate | PostgreSQL-Trigger verbietet DELETE und direkte UPDATEs; `pg_trigger_depth() > 1`, exakte non-null→null-Form und null-sicher unveränderte Restspalten genügen nur zusammen mit `NOT EXISTS` für jeden betroffenen OLD-Parent. Ein fremder Nested-Trigger bei lebendem Parent bleibt verboten, echte FK-Deletes bleiben erlaubt. |
| T-106-07 | Repudiation | Award/Reversal snapshots | mitigate | BEFORE-INSERT-Validierung gleicht Award-ID/Code/Version/Kategorie/Wert mit `point_rules` ab und validiert beim Storno Award-Typ, sämtliche geerbten Identitäts-/Snapshotfelder sowie die exakte Negation. |
| T-106-08 | Tampering | Test database | mitigate | Zufälliger Guard-Name, explizite DSN und garantiertes `dropdb --force` im `finally`. |
</threat_model>

<verification>
- Fast checks: `cd backend; go test ./internal/migrations -run 'TestPhase106Migration(Up|Down)Contract' -count=1`.
- Der Live-Gate-Teil `TestPhase106LedgerAppendOnly/nested_context_null_with_live_parent` installiert im disposable Schema einen fremden Nested-Trigger, nullt genau eine optionale Kontextspalte bei noch lebendem Parent und muss die Append-only-Exception erhalten; die drei realen Parent-Delete-Fälle müssen weiterhin erfolgreich nullen.
- Disposable DB gate: `docker compose up -d team4sv30-db; $dbName = 'team4s_phase106_test_' + [guid]::NewGuid().ToString('N'); try { docker compose exec -T team4sv30-db createdb -U team4s $dbName; if ($LASTEXITCODE -ne 0) { throw 'create test database failed' }; $port = if ($env:POSTGRES_PORT) { $env:POSTGRES_PORT } else { '5433' }; $env:TEAM4S_PHASE106_TEST_DSN = "postgres://team4s:team4s_dev_password@127.0.0.1:$port/$dbName?sslmode=disable"; Push-Location backend; try { go test ./internal/migrations -run 'TestPhase106(MigrationLive|LedgerAppendOnly|ReversalCrossRow)' -count=1; if ($LASTEXITCODE -ne 0) { throw 'phase 106 migration live tests failed' } } finally { Pop-Location } } finally { Remove-Item Env:TEAM4S_PHASE106_TEST_DSN -ErrorAction SilentlyContinue; if ($dbName -like 'team4s_phase106_test_*') { docker compose exec -T team4sv30-db dropdb -U team4s --force $dbName } }`.
- `git diff --check` besteht.
</verification>

<success_criteria>
- Rulezeilen sind vollständig append-only; Ledgerzeilen erlauben die drei eng begrenzten verschachtelten FK-non-null→null-Aktionen ausschließlich nach `NOT EXISTS`-Nachweis für jeden betroffenen OLD-Parent und weisen direkte oder nachgeahmte Nested-Mutationen bei lebendem Parent ab.
- Awards mit abweichendem Rule-Code, Version, Kategorie oder Wert sowie ungültige oder abweichend vererbte Stornos scheitern auch per direktem SQL.
- Up -> Down -> Up, erlaubte Parent-Delete→SET-NULL-Aktionen, abgewiesene direkte Kontext-UPDATEs und der abgewiesene exakt geformte Nested-Trigger-Versuch bei lebendem Parent sind auf PostgreSQL 16 bewiesen.
- Keine normale Entwicklungsdatenbank, produktive Regel, spätere Consumer-Logik oder Fremddomain wird verändert.
</success_criteria>

<output>
After completion, create `.planning/phases/106-member-gamification-punktefundament/106-02-SUMMARY.md`
</output>
