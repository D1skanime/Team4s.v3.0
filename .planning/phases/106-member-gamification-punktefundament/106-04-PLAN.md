---
phase: 106-member-gamification-punktefundament
plan: "04"
type: execute
wave: 4
depends_on: ["106-01", "106-02", "106-03"]
files_modified:
  - backend/internal/services/point_service.go
  - backend/internal/services/point_service_credit_test.go
  - backend/internal/services/point_service_reverse_test.go
  - backend/internal/services/point_service_boundary_test.go
autonomous: true
requirements: [GAM-01, GAM-02, GAM-03, GAM-04, GAM-05]
must_haves:
  truths:
    - "GAM-01 / D-01 / D-02: Credit benötigt MemberID, erlaubt Actor=nil und setzt keinen Claim, Account oder historischen Fallback voraus."
    - "GAM-02 / D-04: InTx-Methoden nutzen die Transaktion des Callers ohne Commit/Rollback; Convenience-Methoden besitzen ihre eigene Transaktion vollständig."
    - "GAM-03 / D-04: Credit akzeptiert RuleRef{Code, Version} plus explizite RewardKind-/Slot-Semantik; Wert und delimiter-sicherer Idempotenzschlüssel entstehen serverseitig und bleiben über Regelversionen stabil."
    - "GAM-04 / D-06 / D-07 / D-08: Die zwei Kategorien kommen nur aus Regeln; Profil-, Ranking-, Hash- oder Inhaltsbewertung existiert nicht."
    - "GAM-05 / D-03 / D-05 / D-09 / D-10: Kein HTTP-, Main-, Review-, Capability-, Badge-, Retention- oder Media-Wiring entsteht."
  artifacts:
    - path: "backend/internal/services/point_service.go"
      provides: "Tx-gebundene und optionale standalone Credit-/Reverse-Kommandos"
      exports: ["RewardKind", "RuleRef", "SourceRef", "CreditCommand", "ReverseCommand", "NewPointService"]
    - path: "backend/internal/services/point_service_credit_test.go"
      provides: "Credit-, RuleRef-, Idempotenz- und caller-owned-Tx-Verträge"
    - path: "backend/internal/services/point_service_reverse_test.go"
      provides: "Reverse-, Retry- und Fehlerverträge"
  key_links:
    - from: "PointService.CreditInTx"
      to: "PointRulesRepository.GetByRef / PointLedgerRepository.InsertAward"
      via: "dieselbe vom Caller übergebene repository.DBTX/pgx.Tx"
      pattern: "CreditInTx"
    - from: "PointService.ReverseInTx"
      to: "PointLedgerRepository.GetForUpdate / InsertReversal"
      via: "gesperrtes Original und serverseitiger Reversal-Key ohne Transaktionsbesitz"
      pattern: "ReverseInTx"
    - from: "CreditCommand.Source"
      to: "point_ledger_entries.idempotency_key"
      via: "validierte RewardKind/Type/Key/Slot-Komponenten im Format v1|reward-kind|source-type|stable-source|beneficiary:member|slot:slot"
      pattern: "v1\\|.*beneficiary:.*\\|slot:"
---

<objective>
Den internen PointService mit expliziten caller-owned-Transaction-Methoden sowie sicheren standalone Convenience-Pfaden implementieren.

Purpose: Phase-107/108-Adapter können Domainmutation und Award atomar in derselben Transaktion ausführen; unabhängige Backend-Jobs erhalten weiterhin einen vollständig verwalteten Convenience-Pfad.
Output: Getestete Credit-/Reverse-Services ohne produktive Consumer-Verdrahtung.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/106-member-gamification-punktefundament/106-CONTEXT.md
@.planning/phases/106-member-gamification-punktefundament/106-RESEARCH.md
@.planning/phases/106-member-gamification-punktefundament/106-PATTERNS.md
@.planning/phases/106-member-gamification-punktefundament/106-VALIDATION.md
@.planning/phases/106-member-gamification-punktefundament/106-03-SUMMARY.md
@.planning/notes/260722-member-gamification-DECISION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expliziten RuleRef-Credit mit caller-owned Transaktion implementieren</name>
  <read_first>
    - AGENTS.md
    - docs/engineering/implementation-contract.md
    - docs/architecture/db-schema-fansub-domain.md
    - .planning/phases/106-member-gamification-punktefundament/106-CONTEXT.md
    - .planning/phases/106-member-gamification-punktefundament/106-RESEARCH.md
    - .planning/phases/106-member-gamification-punktefundament/106-PATTERNS.md
    - backend/internal/services/asset_lifecycle_service.go
    - backend/internal/services/asset_lifecycle_service_test.go
    - backend/internal/repository/audit_logs.go
    - backend/internal/repository/errors.go
    - backend/internal/repository/point_rules_repository.go
    - backend/internal/repository/point_ledger_repository.go
    - backend/internal/repository/anime_contributions_proposal_repository.go
  </read_first>
  <files>backend/internal/services/point_service.go, backend/internal/services/point_service_credit_test.go</files>
  <behavior>
    - "CreditInTx requires MemberID, SourceRef, EffectiveAt, and explicit RuleRef; Actor is optional."
    - "RewardKind, Source Type, stable Source Key, and Slot are non-empty delimiter-safe tokens; the same source/member/slot keeps one key across Rule v1/v2, while different slots produce different keys."
    - "CreditInTx never begins, commits, or rolls back the caller's repository.DBTX/pgx.Tx."
    - "Standalone Credit begins through Begin(ctx), commits on success, and rolls back on every failure."
  </behavior>
  <action>
Define typed `RewardKind` with the only Phase-106 semantic values `work` and `review`, `RuleRef{Code, Version}`, `SourceRef{RewardKind, Type, Key, Slot}`, and `CreditCommand{MemberID, ActorAppUserID, Source, Rule, FansubGroupID, ReleaseVersionID, EffectiveAt}`. `RewardKind` distinguishes the two already specified reward semantics; `Slot` distinguishes legitimate roles/actions on the same stable source. Reject any unknown RewardKind. Validate each textual key component (`RewardKind`, `Type`, `Key`, `Slot`) after trimming: it must equal its trimmed form and match `^[^|]+$` (non-empty and no `|` delimiter). Require positive MemberID and render it as canonical base-10 digits in the key. Exclude PointValue, raw IdempotencyKey, CategoryCode, latest/active/schedule flags, review status, permission, media, hash, and content-length fields. Define `PointTxStarter` with `Begin(context.Context) (pgx.Tx, error)`, matching `pgxpool.Pool`. Existing caller transactions use the separate InTx path instead of acting as starters. Reuse `repository.DBTX` for bound repositories and a narrow internal repository factory; create no second DB abstraction.

Implement `CreditInTx(ctx, db repository.DBTX, cmd)` so it validates the command, exact-calls `GetByRef(code, version)`, then builds the key in exactly this format: `v1|{reward-kind}|{source-type}|{stable-source}|beneficiary:{member-id}|slot:{slot}`. Because all variable text components reject `|`, concatenation is delimiter-safe. Include version, RewardKind, source type, stable source, beneficiary Member and Slot; explicitly exclude rule version/value, hashes, and content metrics. Then call `InsertAward` through repositories bound to exactly `db`. It must not call Begin, Commit, or Rollback. Implement standalone `Credit(ctx, cmd)` as a wrapper that calls `PointTxStarter.Begin(ctx)`, delegates to CreditInTx, commits on success, and rolls back on all error paths while preserving `%w` chains.

Create only credit tests in this task. Fakes prove the exact RuleRef, catalog-derived code/version/category/value snapshot, optional Actor, exact key format, no caller value/key fields, no latest selection, identical DBTX passed to both repositories, and zero lifecycle calls from CreditInTx. Add validation cases for unknown/empty RewardKind plus empty, surrounding-whitespace, and `|`-containing RewardKind/Type/Key/Slot. Prove the same RewardKind/source/Member/Slot under Rule v1 and Rule v2 yields the identical key and therefore cannot create a second award after a catalog change, while two legitimate Slots on the same RewardKind/source/Member yield different keys; also prove `work` and `review` do not collide. Standalone tests prove Begin/Commit/Rollback behavior. Add an opt-in PostgreSQL test that creates a test-only domain marker table, performs a marker mutation plus CreditInTx in one caller-owned pgx transaction, and proves rollback removes both while commit preserves both; the PointService never commits that caller transaction.
  </action>
  <verify>
    <automated>cd backend; go test ./internal/services -run 'TestPointServiceCredit.*(Validation|RuleRef|Idempotency|InTx|Standalone)' -count=1</automated>
  </verify>
  <done>CreditInTx ist an eine vorhandene Domaintransaktion bindbar; Credit verwaltet optional eine eigene Transaktion, und der delimiter-sichere RewardKind-/Source-/Member-/Slot-Key bleibt über Regelversionen stabil und trennt legitime Slots.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tx-gebundenes idempotentes Reverse und enge Phase-106-Boundaries abschließen</name>
  <read_first>
    - AGENTS.md
    - docs/engineering/implementation-contract.md
    - docs/architecture/db-schema-fansub-domain.md
    - .planning/phases/106-member-gamification-punktefundament/106-CONTEXT.md
    - .planning/phases/106-member-gamification-punktefundament/106-RESEARCH.md
    - .planning/phases/106-member-gamification-punktefundament/106-PATTERNS.md
    - backend/internal/services/point_service.go
    - backend/internal/services/point_service_credit_test.go
    - backend/internal/repository/point_rules_repository.go
    - backend/internal/repository/point_ledger_repository.go
    - backend/internal/repository/point_ledger_repository_test.go
    - backend/internal/repository/anime_contributions_proposal_merge_repository.go
  </read_first>
  <files>backend/internal/services/point_service.go, backend/internal/services/point_service_reverse_test.go, backend/internal/services/point_service_boundary_test.go</files>
  <behavior>
    - "ReverseInTx locks an award, rejects reversal-of-reversal, and delegates one exact derived counterpart in the caller transaction."
    - "A repeated identical Reverse returns the existing reversal, including after a successful commit with lost response."
    - "Durable source scans inspect only Phase-106 artifacts and do not block future Phase-107/108 wiring."
  </behavior>
  <action>
Define `ReverseCommand{AwardEntryID, ActorAppUserID, Reason, EffectiveAt}` without inherited Member/source/rule/value/context fields or a raw key. Implement `ReverseInTx(ctx, db repository.DBTX, cmd)`: validate ID/reason/time; call `GetForUpdate`; require an award; derive the versioned reversal key solely from the original entry ID; call `InsertReversal` with only OriginalEntryID, Actor, generated key, effective time, and reason. Do not begin, commit, or rollback the caller DBTX. Repository insert-select plus the migration trigger remain the independent enforcement for inherited fields. Implement standalone `Reverse` using the same Begin/delegate/commit/rollback wrapper policy as Credit. Identical retries return the existing row; repository conflict/mismatch/invalid-reversal errors stay detectable with `errors.Is`.

Create reverse tests only now, after all referenced methods exist. Cover exact negation/inheritance returned by the repository, reversal-of-reversal, blank reason, deterministic key, identical lost-response retry, mismatch conflict, and transaction lifecycle errors. Create `point_service_boundary_test.go` that scans only Phase-106 production artifacts (`point_service.go`, both point repositories, and the two migration files) for forbidden imports/symbols from HTTP, handlers, review/capability, badges, profile, retention/cleanup, media/upload/crop/thumbnail/relation, hashing, ranking, or latest-rule selection. Do not scan `main.go`, global handler directories, or future consumer files. Verify the unchanged global diff surface as an execution acceptance check instead of a durable Go test. D-03/D-05/D-10 remain Phase 107, source adapters Phase 108, and D-02 import-time policy Phase 109.
  </action>
  <verify>
    <automated>cd backend; go test ./internal/services -run 'TestPointService(Reverse|Phase106Boundary).*(Validation|InTx|Standalone|Retry|Boundary|Error)' -count=1</automated>
  </verify>
  <done>Reverse ist tx-gebunden oder standalone nutzbar, bei identischen Retries idempotent und ohne vorzeitige Consumer-Kopplung vollständig getestet.</done>
</task>

</tasks>

<source_coverage_audit>

| Source | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Auditierbarer, idempotenter Member-Gamification-Kern ohne Fachsystemumbau | 01-04 | COVERED | Schema, Repository, Service, Isolation und DB-belegter Schutz gegen nachgeahmte FK-Nullung |
| REQ | GAM-01 | Member als Begünstigter, optionaler Actor | 01, 02, 04 | COVERED | DB- und Command-Vertrag |
| REQ | GAM-02 | Append-only Ledger, Quelle, Zeit, Storno, parallele Idempotenz | 01-04 | COVERED | DB- und Repository-Enforcement; Kontextnullung nur nach OLD-Parent-Wegfall, mit Live-Gegenprobe bei lebendem Parent |
| REQ | GAM-03 | Immutable versionierte Regeln, serverseitige Werte/Keys | 01-04 | COVERED | expliziter RuleRef, keine Latest-Semantik |
| REQ | GAM-04 | Stabile Kategorien; Profil/Badges getrennt | 01, 02, 04 | COVERED | exakt zwei Codes, keine Projektion |
| REQ | GAM-05 | Additiv, keine Fremdflows, Testabdeckung | 01-04 | COVERED | enge Artifact-Boundaries |
| RESEARCH | R-01 | Gemeinsame DBTX-Naht statt neuer DB-Abstraktion | 03, 04 | COVERED | Repositories und InTx-Service |
| RESEARCH | R-02 | Immutable Regelversionen plus Ledger-Snapshot | 01-04 | COVERED | DB-Trigger, exakter RuleRef |
| RESEARCH | R-03 | Semantischer Key aus Reward-Art, Source, Beneficiary und fachlichem Slot; insert-first und vollständiger Mismatch-Vergleich | 01, 03, 04 | COVERED | Exaktes delimiter-sicheres v1-Keyformat; Regelversion/Wert ausgeschlossen; unterschiedliche Slots getrennt |
| RESEARCH | R-04 | Append-only referenzielles Storno und genau ein direktes Storno | 01-04 | COVERED | DB-Trigger, Partial Unique, Insert-Select; Nested-UPDATE-Ausnahme beweist je genullter FK den OLD-Parent-Wegfall |
| RESEARCH | R-05 | `effective_at` getrennt von `recorded_at` | 01-04 | COVERED | explizite Wirkzeit, kein historischer Fallback |
| RESEARCH | R-06 | Domainmutation und Award in derselben Caller-Transaktion | 04 | COVERED | CreditInTx/ReverseInTx + Commit/Rollback-Test |
| RESEARCH | R-07 | Echte PostgreSQL-Concurrency in disposable Umgebung | 01-04 | COVERED | Guard + Create/Drop-Wave-Gates inklusive test-only Foreign-Trigger bei lebendem Parent und echten FK-Deletes |
| RESEARCH | R-08 | Kein HTTP, Review-Wiring, Badge-Umbau oder Media-Change | 01-04 | COVERED | Artifact-Boundary und Diff-Gate |
| CONTEXT | D-01 | Punkte gehören zum Member | 01, 02, 04 | COVERED | Member NOT NULL, Actor optional |
| CONTEXT | D-02 | Historische Leistung zählt später vollständig | 01, 04 | COVERED | Account-frei; Zeitfallback zu Phase 109 deferred |
| CONTEXT | D-03 | Nur bestätigte Beiträge zählen | 01, 02, 04 | COVERED | Review-Wiring bleibt Phase 107 |
| CONTEXT | D-04 | Feste, versionierte Punktwerte | 01-04 | COVERED | immutable Regeln und Snapshots |
| CONTEXT | D-05 | Vier-Augen-Prinzip | 01, 02, 04 | COVERED | kein vorzeitiger Consumer; Phase 107 |
| CONTEXT | D-06 | Review kann später Beitrag sein | 01, 02, 04 | COVERED | Kategorie vorbereitet, kein Review-Adapter |
| CONTEXT | D-07 | Profilpflege ohne Punkte | 01, 02, 04 | COVERED | Badge/Profile ausgeschlossen |
| CONTEXT | D-08 | Keine Inhaltsmengen-/Kopierbewertung | 01-04 | COVERED | Key/Commands ohne Hash oder Länge |
| CONTEXT | D-09 | Kein Medienumbau | 01, 02, 04 | COVERED | enge Phase-106-Artefakte |
| CONTEXT | D-10 | Retention erst Phase 107 | 01, 02, 04 | COVERED | kein Cleanup/Retention in Phase 106 |

</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Future domain adapter -> PointService | Adapter liefert Identität, Source, RuleRef und Zeit, aber nie Wert oder rohen Key. |
| Caller-owned pgx.Tx -> InTx methods | Service darf diese Transaktion weder beginnen noch committen/rollbacken. |
| Standalone caller -> transaction starter | Convenience-Pfad besitzt Begin/Commit/Rollback vollständig. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-13 | Tampering | CreditCommand | mitigate | Kein Caller-Wert/Key; exakter RuleRef/Katalogsnapshot; RewardKind/Source/Slot müssen non-empty, trim-stabil und `|`-frei sein, bevor das exakte v1-Keyformat erzeugt wird. |
| T-106-14 | Repudiation | Domainmutation plus Award | mitigate | Caller-owned InTx-Pfad und echter gemeinsamer Commit-/Rollback-Test. |
| T-106-15 | Tampering | ReverseCommand | mitigate | Nur Original-ID/Actor/Grund/Zeit; Repository/DB leiten restliche Felder ab. |
| T-106-16 | Elevation of privilege | Premature wiring | mitigate | Artifact-lokaler Boundary-Test und globaler Diff-Gate statt zukunftsblockierender Scans. |
</threat_model>

<verification>
- Fast checks: `cd backend; go test ./internal/services -run 'TestPointService(Credit|Reverse|Phase106Boundary)' -count=1`.
- Disposable DB gate: `docker compose up -d team4sv30-db; $dbName = 'team4s_phase106_test_' + [guid]::NewGuid().ToString('N'); try { docker compose exec -T team4sv30-db createdb -U team4s $dbName; if ($LASTEXITCODE -ne 0) { throw 'create test database failed' }; $port = if ($env:POSTGRES_PORT) { $env:POSTGRES_PORT } else { '5433' }; $env:TEAM4S_PHASE106_TEST_DSN = "postgres://team4s:team4s_dev_password@127.0.0.1:$port/$dbName?sslmode=disable"; Push-Location backend; try { go test ./internal/migrations ./internal/repository ./internal/services -run 'TestPhase106|TestPoint' -count=1; if ($LASTEXITCODE -ne 0) { throw 'phase 106 live suite failed' } } finally { Pop-Location } } finally { Remove-Item Env:TEAM4S_PHASE106_TEST_DSN -ErrorAction SilentlyContinue; if ($dbName -like 'team4s_phase106_test_*') { docker compose exec -T team4sv30-db dropdb -U team4s --force $dbName } }`.
- `cd backend; go test ./...`, `cd backend; go vet ./...`, and `git diff --check` pass.
- `git diff --name-only -- backend/cmd/server backend/internal/handlers shared/contracts frontend backend/internal/repository/badge_repository.go backend/internal/services/badge_service.go` returns no Phase-106 implementation change.
</verification>

<success_criteria>
- Future adapters can keep Domainmutation and Award/Reversal in one caller-owned transaction.
- Convenience methods correctly own their transaction; InTx methods never do.
- Explicit RuleRef, delimiter-safe RewardKind-/Source-/Slot-Keys mit Regelversionsstabilität und Slot-Trennung, server-side values, idempotent reversal retry, and all DB constraints are green.
- No HTTP endpoint, latest/activation semantics, production seed, review/source wiring, or media change exists.
</success_criteria>

<output>
After completion, create `.planning/phases/106-member-gamification-punktefundament/106-04-SUMMARY.md`
</output>
