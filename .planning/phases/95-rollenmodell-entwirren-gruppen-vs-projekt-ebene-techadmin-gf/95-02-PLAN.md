---
phase: 95-rollenmodell-entwirren-gruppen-vs-projekt-ebene-techadmin-gf
plan: 02
type: execute
wave: 2
depends_on:
  - 95-01
files_modified:
  - backend/internal/repository/authz_permissions.go
  - backend/internal/permissions/permissions.go
  - backend/internal/handlers/admin_group_roles_handler.go
  - backend/cmd/server/admin_routes.go
  - backend/cmd/server/main.go
  - backend/internal/handlers/admin_fansub_group_roles_handler_test.go
autonomous: true
requirements:
  - D-07
  - D-08
  - D-12

must_haves:
  truths:
    - "D-12: fansubGroupRoleCatalog wird beim Server-Start aus role_definitions (assignable=true) geladen statt aus einer hardkodierten Go-Slice"
    - "D-07: Neue Konstanten RoleTechadmin und RoleGfxler existieren in permissions.go"
    - "D-08: IsKnownFansubGroupRole gibt true für techadmin und gfxler zurück (nach DB-Laden)"
    - "D-12: Neuer Endpunkt GET /api/v1/admin/fansub-group-roles liefert JSON mit allen assignable Rollen inkl. techadmin+gfxler"
    - "Startup-Reihenfolge: LoadCache ZUERST, dann LoadFansubGroupCatalog (Fallstrick 5)"
  artifacts:
    - path: "backend/internal/repository/authz_permissions.go"
      provides: "LoadFansubGroupRoles()-Methode + CatalogLoader-Interface-Assertion"
    - path: "backend/internal/permissions/permissions.go"
      provides: "CatalogLoader-Interface, LoadFansubGroupCatalog(), catalogMu Mutex, RoleTechadmin/RoleGfxler"
    - path: "backend/internal/handlers/admin_group_roles_handler.go"
      provides: "ListFansubGroupRoles HTTP-Handler"
    - path: "backend/internal/handlers/admin_fansub_group_roles_handler_test.go"
      provides: "Wave-0-Test TestListFansubGroupRoles (D-12, RED→GREEN)"
  key_links:
    - from: "backend/cmd/server/main.go"
      to: "permissions.Service.LoadFansubGroupCatalog"
      via: "Startup nach permissionSvc.LoadCache"
      pattern: "LoadFansubGroupCatalog"
    - from: "backend/internal/handlers/admin_group_roles_handler.go"
      to: "permissions.FansubGroupRoles()"
      via: "ListFansubGroupRoles liest aus dynamisiertem Cache"
      pattern: "FansubGroupRoles"
---

<objective>
Plan 02 (Wave 2): Data-driven Catalog und neuer API-Endpunkt (D-12)

Der `fansubGroupRoleCatalog` (heute hardkodierte Go-Slice in permissions.go Z.200-211) wird dynamisiert:
AuthzRepository erhält `LoadFansubGroupRoles()`, permissions.Service bekommt `CatalogLoader`-Interface
und `LoadFansubGroupCatalog()`, und beim Server-Start wird der Catalog aus `role_definitions WHERE
assignable=true` geladen. Ein neuer Handler `admin_group_roles_handler.go` exponiert
`GET /api/v1/admin/fansub-group-roles` als Platform-Admin-Endpunkt.

Purpose: Nach Plan 01 (Migration) kennt die DB techadmin+gfxler. Dieser Plan macht sie automatisch
im Backend-Cache sichtbar, ohne einen Deploy pro neuer Rolle zu benötigen (D-12-Ziel).
Output: 5 Backend-Go-Dateien + Wave-0-Test-Datei.
</objective>

<execution_context>
@C:\Users\admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/95-rollenmodell-entwirren-gruppen-vs-projekt-ebene-techadmin-gf/95-CONTEXT.md
@.planning/phases/95-rollenmodell-entwirren-gruppen-vs-projekt-ebene-techadmin-gf/95-RESEARCH.md
@.planning/phases/95-rollenmodell-entwirren-gruppen-vs-projekt-ebene-techadmin-gf/95-PATTERNS.md
@.planning/phases/95-rollenmodell-entwirren-gruppen-vs-projekt-ebene-techadmin-gf/95-01-SUMMARY.md

<interfaces>
<!-- permissions.go (Z.163-287) — bestehende Cache-Architektur als Vorlage: -->
<!-- var cacheMu sync.RWMutex; var loadedCache map[string][]Action -->
<!-- type CacheLoader interface { LoadRoleCapabilities(ctx) (map[string][]Action, error) } -->
<!-- func (s *Service) LoadCache(ctx, CacheLoader) error { ... cacheMu.Lock(); loadedCache = m; ... } -->

<!-- fansubGroupRoleCatalog heute (Z.200-211): hardkodierte []string mit 10 Codes -->
<!-- FansubGroupRoles() (Z.299-305): liest fansubGroupRoleCatalog unter RWMutex -->
<!-- IsKnownFansubGroupRole(code string) (Z.307-314): prüft gegen fansubGroupRoleCatalog -->

<!-- authz_permissions.go LoadRoleCapabilities (Z.200-223): direkte Vorlage für LoadFansubGroupRoles -->

<!-- admin_capability_handler.go (Z.28-69): Struct/Konstruktor/ListCapabilityMatrix als Handler-Vorlage -->
<!-- requirePlatformAdminIdentity(c, h.authzRepo, "") → Z.51 -->

<!-- admin_routes.go Z.232-237: Muster für neue Route-Registrierung -->
<!-- main.go Z.127: permissionSvc.LoadCache → danach LoadFansubGroupCatalog einfügen -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0-Test für TestListFansubGroupRoles + LoadFansubGroupRoles Repository-Methode</name>
  <files>
    backend/internal/handlers/admin_fansub_group_roles_handler_test.go
    backend/internal/repository/authz_permissions.go
  </files>
  <read_first>
    - backend/internal/repository/authz_permissions.go (Z.200-223 — LoadRoleCapabilities als exakte Vorlage)
    - backend/internal/handlers/admin_capability_handler_test.go (Z.1-100 — Stub-Muster, Test-Setup)
    - 95-PATTERNS.md Abschnitt "authz_permissions.go" (vollständige Methodensignatur und Compile-Assertion)
    - 95-VALIDATION.md Zeile "D-12" (Testkommando: TestListFansubGroupRoles)
  </read_first>
  <behavior>
    - TestListFansubGroupRoles: Mock-Handler mit Stub der einen bekannten Catalog-Snapshot liefert; GET /admin/fansub-group-roles → 200 + JSON {data: [{code:"techadmin"},{code:"gfxler"},...]}
    - LoadFansubGroupRoles: Query `SELECT code FROM role_definitions WHERE assignable = true ORDER BY sort_order, code` → []string
    - Compile-Zeit-Assertion: `var _ permissions.CatalogLoader = (*AuthzRepository)(nil)` in authz_permissions.go nach der Methode
    - Test startet RED (Handler existiert noch nicht), wird GREEN nach Task 2
  </behavior>
  <action>
    In `backend/internal/repository/authz_permissions.go` nach LoadRoleCapabilities (Z.223) einfügen:
    Neue Methode `LoadFansubGroupRoles(ctx context.Context) ([]string, error)` analog zu LoadRoleCapabilities
    (Z.200-223 als Vorlage): Query `SELECT code FROM role_definitions WHERE assignable = true ORDER BY sort_order, code`,
    Row-Scan in []string, rows.Err()-Prüfung, fmt.Errorf-Wrapping.
    Danach Compile-Assertion: `var _ permissions.CatalogLoader = (*AuthzRepository)(nil)` (dieser Typ
    wird erst in permissions.go definiert — falls Compile-Fehler: Assertion temporär auskommentieren bis Task 2).

    Neue Testdatei `backend/internal/handlers/admin_fansub_group_roles_handler_test.go` erstellen:
    Test `TestListFansubGroupRoles` mit gin.New()-Router, Stub der FansubGroupRoles() als ["fansub_lead","techadmin","gfxler"]
    zurückliefert, GET /admin/fansub-group-roles mit reqPlatformAdmin-Header, erwarte 200 + data-Array.
    Zunächst RED (Handler-Typ referenziert), da admin_group_roles_handler.go noch nicht existiert.
  </action>
  <verify>
    <automated>
      cd "C:/Users/admin/Documents/Team4s/backend" && go build ./internal/repository/... 2>&1 | head -20
    </automated>
  </verify>
  <done>
    `authz_permissions.go` enthält `LoadFansubGroupRoles(ctx) ([]string, error)` nach `LoadRoleCapabilities`.
    Compile-Assertion `var _ permissions.CatalogLoader = (*AuthzRepository)(nil)` vorhanden (ggf. nach Task 2 aktiviert).
    Test-Datei existiert mit `TestListFansubGroupRoles`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CatalogLoader-Interface in permissions.go + neuer Handler + Route + main.go-Startup</name>
  <files>
    backend/internal/permissions/permissions.go
    backend/internal/handlers/admin_group_roles_handler.go
    backend/cmd/server/admin_routes.go
    backend/cmd/server/main.go
  </files>
  <read_first>
    - backend/internal/permissions/permissions.go (Z.163-314 — Cache-Architektur, FansubGroupRoles, fansubGroupRoleCatalog)
    - backend/internal/handlers/admin_capability_handler.go (Z.1-69 — Struct/Konstruktor/Auth-Guard-Muster)
    - backend/cmd/server/admin_routes.go (Z.232-237 — Muster für neue GET-Route)
    - backend/cmd/server/main.go (Z.125-130 — LoadCache-Aufruf, danach LoadFansubGroupCatalog)
    - 95-PATTERNS.md Abschnitt "permissions.go" (vollständige Interface-/Methoden-Muster)
    - 95-PATTERNS.md Abschnitt "admin_group_roles_handler.go" (Struct + ListFansubGroupRoles)
  </read_first>
  <behavior>
    - CatalogLoader-Interface in permissions.go: `type CatalogLoader interface { LoadFansubGroupRoles(ctx context.Context) ([]string, error) }`
    - Neue package-level vars: `var catalogMu sync.RWMutex` + `fansubGroupRoleCatalog []string` (ersetzt statische Slice Z.200-211)
    - Neue Konstanten: `RoleTechadmin = "techadmin"`, `RoleGfxler = "gfxler"` nach Z.54
    - LoadFansubGroupCatalog: lädt per CatalogLoader, lockt catalogMu, setzt fansubGroupRoleCatalog
    - FansubGroupRoles() und IsKnownFansubGroupRole() bleiben erhalten, lesen weiterhin aus fansubGroupRoleCatalog
    - Neuer Handler: `AdminGroupRolesHandler` mit `authzRepo capabilityAuthzRepo`; ListFansubGroupRoles: requirePlatformAdminIdentity → permissions.FansubGroupRoles() → JSON {data: [...]}
    - Route in admin_routes.go: GET /admin/fansub-group-roles
    - main.go: nach permissionSvc.LoadCache(ctx, authzRepo) einfügen: LoadFansubGroupCatalog(ctx, authzRepo) mit log.Fatalf bei Fehler
    - Startup-Reihenfolge: LoadCache ZUERST (Fallstrick 5), dann LoadFansubGroupCatalog
  </behavior>
  <action>
    In `permissions.go`:
    - Neue Konstanten `RoleTechadmin`, `RoleGfxler` nach dem bestehenden const-Block (Z.54).
    - CatalogLoader-Interface nach CacheLoader-Interface (Z.248 Bereich).
    - Neue vars `catalogMu sync.RWMutex` + `fansubGroupRoleCatalog []string` als package-level Vars
      (die bisherige statische `var fansubGroupRoleCatalog = []string{...}` Z.200-211 wird durch leere
      Deklaration ersetzt — DB-Laden übernimmt LoadFansubGroupCatalog).
    - `LoadFansubGroupCatalog(ctx, CatalogLoader) error`-Methode auf *Service analog zu LoadCache (Z.263-287).
    - `FansubGroupRoles()` und `IsKnownFansubGroupRole()` lesen weiterhin `fansubGroupRoleCatalog` —
      kein Body-Änderungsbedarf wenn var bereits oben deklariert. Mutex-Nutzung analog zu cacheMu.

    Neue Datei `backend/internal/handlers/admin_group_roles_handler.go`:
    Package handlers. Struct `AdminGroupRolesHandler` mit Feld `authzRepo capabilityAuthzRepo` (Interface
    aus admin_capability_handler.go nutzen, NICHT neu deklarieren). Konstruktor
    `NewAdminGroupRolesHandler(authzRepo capabilityAuthzRepo) *AdminGroupRolesHandler`.
    Methode `ListFansubGroupRoles(c *gin.Context)`: requirePlatformAdminIdentity → permissions.FansubGroupRoles()
    → struct roleItem{Code string `json:"code"`} → c.JSON(200, gin.H{"data": items}).
    Datei MUSS ≤ 80 Zeilen bleiben (schlanke Handler).

    In `admin_routes.go` nach Z.237 (RevokeCapability-Route):
    `v1.GET("/admin/fansub-group-roles", auth, deps.adminGroupRolesHandler.ListFansubGroupRoles)`
    Feld `adminGroupRolesHandler *AdminGroupRolesHandler` zur `adminRouteHandlers`-Struct hinzufügen.

    In `main.go`:
    Nach Z.129 (nach permissionSvc.LoadCache):
    `adminGroupRolesHandler := handlers.NewAdminGroupRolesHandler(authzRepo)`
    `if err := permissionSvc.LoadFansubGroupCatalog(ctx, authzRepo); err != nil { log.Fatalf(...) }`
    Feld bei adminRouteHandlers{...} eintragen.
  </action>
  <verify>
    <automated>
      cd "C:/Users/admin/Documents/Team4s/backend" && go build ./... 2>&1 | head -20 && go test ./internal/handlers/... -run TestListFansubGroupRoles -v 2>&1 | tail -10
    </automated>
  </verify>
  <done>
    `go build ./...` ohne Fehler. TestListFansubGroupRoles PASS.
    permissions.go enthält CatalogLoader-Interface, LoadFansubGroupCatalog, RoleTechadmin/RoleGfxler.
    admin_group_roles_handler.go ≤ 80 Zeilen. Route GET /admin/fansub-group-roles in admin_routes.go.
    main.go ruft LoadFansubGroupCatalog NACH LoadCache auf.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /admin/fansub-group-roles | Platform-Admin-Identität erforderlich (requirePlatformAdminIdentity) |
| DB → Cache | role_definitions.assignable steuert welche Rollen sichtbar sind |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-02-EoP | Elevation of Privilege | techadmin/gfxler in FansubGroupRoles() | mitigate | D-07/08: Neue Rollen starten mit assignable=true aber leeren role_capabilities; IsKnownFansubGroupRole-Gate bleibt aktiv; keine Capabilities ohne explizites Grant via /admin/role-capabilities |
| T-95-02-ID | Information Disclosure | GET /admin/fansub-group-roles | mitigate | requirePlatformAdminIdentity blockiert Nicht-Admins; gibt nur code-Codes zurück, keine sensiblen Daten |
| T-95-02-SC | Tampering | npm/pip/cargo installs | accept | Keine Paket-Installationen in diesem Plan |
</threat_model>

<verification>
Nach Abschluss beider Tasks:

1. `go build ./...` — grün, keine Kompilierfehler
2. `go test ./internal/handlers/... -run TestListFansubGroupRoles` — PASS
3. `go test ./internal/repository/... -run LoadFansubGroupRoles` — PASS (sofern separater Test vorhanden)
4. permissions.go enthält `CatalogLoader` Interface und `LoadFansubGroupCatalog` Methode
5. fansubGroupRoleCatalog ist keine statische Slice mehr sondern package-level var (leer bei Init)
6. `grep -n "RoleTechadmin\|RoleGfxler" backend/internal/permissions/permissions.go` — beide Konstanten gefunden
</verification>

<success_criteria>
- GET /api/v1/admin/fansub-group-roles liefert nach Rebuild HTTP 200 + JSON {data: [{code:"fansub_lead"},{code:"techadmin"},{code:"gfxler"},...]} (per D-12)
- fansubGroupRoleCatalog wird aus DB geladen, nicht mehr hardkodiert (TestListFansubGroupRoles PASS)
- RoleTechadmin + RoleGfxler als Konstanten in permissions.go (per D-07)
- Keine Go-Kompilierfehler, `go test ./...` grün
</success_criteria>

<output>
Erstelle `.planning/phases/95-rollenmodell-entwirren-gruppen-vs-projekt-ebene-techadmin-gf/95-02-SUMMARY.md` nach Abschluss.
</output>
