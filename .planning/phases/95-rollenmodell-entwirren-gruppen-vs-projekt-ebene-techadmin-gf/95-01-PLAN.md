---
phase: 95-rollenmodell-entwirren-gruppen-vs-projekt-ebene-techadmin-gf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/0112_role_model_cleanup.up.sql
  - database/migrations/0112_role_model_cleanup.down.sql
  - backend/internal/repository/admin_users_queries.go
  - backend/internal/repository/admin_users_tab_repository.go
  - backend/internal/repository/anime_contributions_public_repository.go
  - backend/internal/services/badge_service.go
  - backend/internal/repository/hist_group_member_roles_repository.go
  - backend/internal/repository/role_definitions_context_test.go
  - frontend/src/app/admin/fansubs/[id]/edit/GroupHistRoleDialog.test.tsx
autonomous: true
requirements:
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08

must_haves:
  truths:
    - "D-01: Das Zwei-Ebenen-Vokabular ist in role_definitions etabliert — Gruppen-Ebene (founder, fansub_lead, co_leader, project_lead, techadmin, gfxler) und Projekt-/Anime-Ebene (translator, editor, timer, typesetter, encoder, raw_provider, quality_checker, designer) teilen denselben role_code-Namespace."
    - "D-02: gfxler (Gruppen-Grafik) und designer (Projekt-Grafik) existieren als getrennte Codes in role_definitions — kein Merge."
    - "D-03: Gruppen-Rollen sind assignable=true und erscheinen dadurch in der Capability-Matrix."
    - "D-04: Nach Migration enthält hist_group_member_roles keine Zeilen mehr mit role_code IN ('leader','project_manager')"
    - "D-05: role_definitions.label_de für fansub_lead = 'Gruppenleitung', für project_lead = 'Fansub-Projektleitung'"
    - "D-06: groupHistoryDialogRoleWhitelist enthält 'fansub_lead','project_lead','techadmin','gfxler'; nicht mehr 'leader','project_manager'"
    - "D-07: 'techadmin' und 'gfxler' existieren als Zeilen in role_definitions mit assignable=true"
    - "D-08: Keine role_capabilities-Einträge für techadmin oder gfxler nach Migration"
    - "Backend-Code-Sync: Alle 5 SQL-Stellen mit hartkodiertem 'leader' + die Go-Slice groupHistoryDialogRoleWhitelist sind auf die kanonischen Codes ('fansub_lead'/'project_lead') umgestellt"
  artifacts:
    - path: "database/migrations/0112_role_model_cleanup.up.sql"
      provides: "Atomare Migration: assignable-Spalte, Label-Updates, hist-Datenmigration, FK-Delete, neue Rollen"
    - path: "database/migrations/0112_role_model_cleanup.down.sql"
      provides: "Rollback-Migration"
    - path: "backend/internal/repository/hist_group_member_roles_repository.go"
      provides: "Aktualisierte groupHistoryDialogRoleWhitelist (D-06)"
  key_links:
    - from: "database/migrations/0112_role_model_cleanup.up.sql"
      to: "hist_group_member_roles"
      via: "UPDATE role_code vor DELETE aus role_definitions (FK ON DELETE RESTRICT)"
      pattern: "UPDATE.*hist_group_member_roles.*SET role_code"
    - from: "backend/internal/repository/admin_users_queries.go"
      to: "fansub_group_member_roles"
      via: "SQL-Heuristik leader_count — muss 'fansub_lead' heißen"
      pattern: "fansub_lead"
---

<objective>
Plan 01 (Wave 1): DB-Migration und Go-Backend-Code-Sync

Legt das Fundament für das gesamte Phase-95-Rollenmodell: Die Datenbank wird durch
Migration 0112 bereinigt (neue `assignable`-Spalte, Label-Updates D-05, hist-Daten-Migration D-04,
Löschung alter Codes, neue Rollen techadmin+gfxler D-07/08, assignable-Flag für Gruppenrollen D-03).
Synchron werden alle 5 Go-Backend-SQL-Fundstellen mit hartkodiertem `'leader'` auf `'fansub_lead'`
umgestellt (Fallstrick 2 aus RESEARCH.md) und die Go-Whitelist (D-06) aktualisiert.

Dieser Plan muss vollständig abgeschlossen sein, bevor Plan 02, 03 und 04 starten können.

Purpose: FK-Constraint `ON DELETE RESTRICT` auf `hist_group_member_roles.role_code` macht die
Reihenfolge ZWINGEND: erst hist-Einträge migrieren, DANN alte role_definitions löschen.
Output: Migration 0112 (up+down), 5 Backend-SQL-Dateien, Whitelist-Go-Code, Tests aktualisiert.
</objective>

<execution_context>
@C:\Users\admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/95-rollenmodell-entwirren-gruppen-vs-projekt-ebene-techadmin-gf/95-CONTEXT.md
@.planning/phases/95-rollenmodell-entwirren-gruppen-vs-projekt-ebene-techadmin-gf/95-RESEARCH.md
@.planning/phases/95-rollenmodell-entwirren-gruppen-vs-projekt-ebene-techadmin-gf/95-PATTERNS.md

<interfaces>
<!-- Migration-Analogvorlage: database/migrations/0108_capability_registry.up.sql -->
<!-- Relevante DB-Schemastruktur: -->
<!-- hist_group_member_roles.role_code → FK auf role_definitions(code) ON DELETE RESTRICT (0085) -->
<!-- role_definitions Spalten: code, label_de, contexts[], sort_order -->
<!-- 0112 fügt hinzu: assignable BOOLEAN NOT NULL DEFAULT false -->
<!-- fansub_group_member_roles: FK statt CHECK seit Migration 0106 -->

<!-- Go-Whitelist (aktuelle Position, zu ersetzen): -->
<!-- hist_group_member_roles_repository.go:245-250 -->
<!-- var groupHistoryDialogRoleWhitelist = []string{"founder","leader","co_leader","project_manager"} -->

<!-- Backend-SQL-Fundstellen (alle mit 'leader' ersetzen durch 'fansub_lead'): -->
<!-- admin_users_queries.go:68   — leader_count FILTER (WHERE role IN ('leader')) -->
<!-- admin_users_queries.go:247  — role = 'leader' -->
<!-- admin_users_tab_repository.go:118 — role IN ('leader', ...) -->
<!-- anime_contributions_public_repository.go:243 — role_code IN ('leader', 'founder') -->
<!-- services/badge_service.go:93 — role_code IN ('leader', 'founder') -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0112 erstellen (assignable-Spalte, Label-Updates, hist-Datenmigration, FK-Delete, neue Rollen)</name>
  <files>
    database/migrations/0112_role_model_cleanup.up.sql
    database/migrations/0112_role_model_cleanup.down.sql
  </files>
  <read_first>
    - database/migrations/0108_capability_registry.up.sql (BEGIN/COMMIT-Muster, idempotente INSERTs)
    - database/migrations/0085_role_definitions_seed.up.sql (Baseline-Seed: leader/project_manager-Codes + labels)
    - database/migrations/0082_historical_fansub_group_members.up.sql (hist_fansub_group_members-Schema)
    - 95-PATTERNS.md Abschnitt "0112_role_model_cleanup.up.sql" (vollständige SQL-Vorlage mit Reihenfolge)
  </read_first>
  <behavior>
    - Vor dem DELETE aus role_definitions werden alle hist_group_member_roles-Zeilen migriert (FK-Reihenfolge)
    - Neue Spalte `assignable BOOLEAN NOT NULL DEFAULT false` mit ALTER TABLE IF NOT EXISTS
    - `techadmin`/`gfxler` werden mit assignable=true in role_definitions eingefügt
    - `fansub_lead`, `co_leader`, `founder`, `project_lead` bekommen assignable=true gesetzt
    - `fansub_group`-Kontext wird für Gruppenrollen ergänzt (WHERE NOT 'fansub_group' = ANY(contexts))
    - Down-Migration setzt label_de zurück, entfernt neue Rollen, setzt assignable=false (best-effort)
  </behavior>
  <action>
    Erstelle `database/migrations/0112_role_model_cleanup.up.sql` mit BEGIN/COMMIT-Wrapper und
    genau dieser Reihenfolge (Fallstrick 1 aus RESEARCH.md beachten):

    Schritt 1 — Spalte hinzufügen:
    `ALTER TABLE role_definitions ADD COLUMN IF NOT EXISTS assignable BOOLEAN NOT NULL DEFAULT false;`

    Schritt 2 — label_de aktualisieren (D-05):
    UPDATE für `fansub_lead` → 'Gruppenleitung', UPDATE für `project_lead` → 'Fansub-Projektleitung'.

    Schritt 3 — Historische Einträge migrieren VOR FK-Delete (D-04, ZWINGEND ZUERST):
    UPDATE hist_group_member_roles SET role_code = 'fansub_lead' WHERE role_code = 'leader';
    UPDATE hist_group_member_roles SET role_code = 'project_lead' WHERE role_code = 'project_manager';

    Schritt 4 — Alte role_definitions entfernen (erst nach Schritt 3):
    DELETE FROM role_definitions WHERE code IN ('leader', 'project_manager');

    Schritt 5 — Neue Rollen anlegen (D-07):
    INSERT INTO role_definitions (code, label_de, contexts, sort_order, assignable) VALUES
    ('techadmin', 'Techadmin', ARRAY['fansub_group'], 5, true),
    ('gfxler', 'GFX / Grafik', ARRAY['fansub_group'], 6, true)
    ON CONFLICT (code) DO NOTHING;

    Schritt 6 — assignable=true für bestehende Gruppenrollen (D-03/D-08):
    UPDATE role_definitions SET assignable = true
    WHERE code IN ('fansub_lead', 'co_leader', 'founder', 'project_lead');

    Schritt 7 — fansub_group-Kontext ergänzen:
    UPDATE role_definitions SET contexts = array_append(contexts, 'fansub_group')
    WHERE code IN ('fansub_lead', 'co_leader', 'founder', 'project_lead')
    AND NOT 'fansub_group' = ANY(contexts);

    Erstelle `database/migrations/0112_role_model_cleanup.down.sql` mit best-effort Rollback:
    label_de zurücksetzen, techadmin+gfxler löschen, assignable=false setzen, hist-Einträge zurück
    (soweit möglich: 'fansub_lead'→'leader', 'project_lead'→'project_manager' in hist-Tabelle,
    role_definitions-Zeilen für leader+project_manager wieder einfügen).
  </action>
  <verify>
    <automated>
      cd "C:/Users/admin/Documents/Team4s" && go test ./backend/internal/repository/... -run TestRoleDefinitionsContext -v 2>/dev/null || echo "Tests existieren noch nicht — Migration-Datei prüfen mit: cat database/migrations/0112_role_model_cleanup.up.sql"
    </automated>
  </verify>
  <done>
    Beide Migration-Dateien existieren. Die up.sql enthält in korrekter Reihenfolge: ALTER TABLE assignable,
    2× UPDATE label_de, 2× UPDATE hist-Einträge, DELETE role_definitions, INSERT techadmin+gfxler,
    UPDATE assignable=true, UPDATE contexts. Kein DELETE vor UPDATE der hist-Einträge.
  </done>
</task>

<task type="auto">
  <name>Task 2: Go-Backend-SQL-Fundstellen und Go-Whitelist auf kanonische Codes umstellen</name>
  <files>
    backend/internal/repository/admin_users_queries.go
    backend/internal/repository/admin_users_tab_repository.go
    backend/internal/repository/anime_contributions_public_repository.go
    backend/internal/services/badge_service.go
    backend/internal/repository/hist_group_member_roles_repository.go
    backend/internal/repository/role_definitions_context_test.go
    frontend/src/app/admin/fansubs/[id]/edit/GroupHistRoleDialog.test.tsx
  </files>
  <read_first>
    - backend/internal/repository/admin_users_queries.go (Z.68, Z.247 — 'leader' in SQL)
    - backend/internal/repository/admin_users_tab_repository.go (Z.118 — 'leader' in SQL)
    - backend/internal/repository/anime_contributions_public_repository.go (Z.243 — 'leader')
    - backend/internal/services/badge_service.go (Z.93 — 'leader')
    - backend/internal/repository/hist_group_member_roles_repository.go (Z.245-250 — Whitelist-Slice)
    - backend/internal/repository/role_definitions_context_test.go (Z.29-32, Z.115-116 — Fixtures)
    - frontend/src/app/admin/fansubs/[id]/edit/GroupHistRoleDialog.test.tsx (Z.15,17,68,70 — Fixtures)
    - 95-PATTERNS.md Abschnitt "Backend-Code-Fundstellen mit alten Codes" (vollständige Tabelle)
  </read_first>
  <action>
    Genau 5 Go-Backend-SQL-Stellen ersetzen ('leader' → 'fansub_lead'):
    - admin_users_queries.go Z.68: IN ('leader') → IN ('fansub_lead')
    - admin_users_queries.go Z.247: role = 'leader' → role = 'fansub_lead'
    - admin_users_tab_repository.go Z.118: IN ('leader', ...) → IN ('fansub_lead', ...)
    - anime_contributions_public_repository.go Z.243: IN ('leader','founder') → IN ('fansub_lead','founder')
    - badge_service.go Z.93: IN ('leader','founder') → IN ('fansub_lead','founder')

    Whitelist in hist_group_member_roles_repository.go (D-06) — die Slice auf Z.245-250 ersetzen:
    var groupHistoryDialogRoleWhitelist = []string{
        "founder", "fansub_lead", "co_leader", "project_lead", "techadmin", "gfxler",
    }
    Kommentare und Sortierung beibehalten.

    Test-Fixtures aktualisieren:
    - role_definitions_context_test.go Z.30,32: "leader" → "fansub_lead", Z.115,116: "project_manager" → "project_lead"
    - GroupHistRoleDialog.test.tsx Z.15,17: 'leader'/'project_manager' → 'fansub_lead'/'project_lead', Z.68,70: analog

    Keine Logikänderungen — nur String-Literale ersetzen. Keine neuen Funktionen hinzufügen.
    project_manager in frontend/src/components/contributions/contributionRoles.ts wird in Plan 05 entfernt (D-04).
  </action>
  <verify>
    <automated>
      cd "C:/Users/admin/Documents/Team4s/backend" && grep -rn "'leader'" internal/repository/admin_users_queries.go internal/repository/admin_users_tab_repository.go internal/repository/anime_contributions_public_repository.go internal/services/badge_service.go 2>/dev/null | grep -v "fansub_lead" | grep -c "leader" || echo "0 — alle 'leader'-Vorkommen ersetzt"
    </automated>
  </verify>
  <done>
    `grep -rn "'leader'" backend/internal/repository/admin_users_queries.go ... | grep -v fansub_lead`
    liefert 0 Treffer. groupHistoryDialogRoleWhitelist enthält "fansub_lead" und "project_lead" statt
    "leader" und "project_manager". Test-Fixtures kompilieren sauber.
    `go test ./internal/repository/... -run TestRoleDefinitionsContext` grün.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin-UI → Migration | Migrations werden nur von authorisierten Ops/Devs ausgeführt, kein Endnutzer-Zugriff |
| Go-Backend → DB | Alle SQL-Queries laufen über pgxpool mit App-Credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01-SC | Tampering | npm/pip/cargo installs | accept | Keine Paket-Installationen in diesem Plan |
| T-95-01-MIG | Tampering | 0112_role_model_cleanup.up.sql | mitigate | FK ON DELETE RESTRICT erzwingt korrekte Reihenfolge; Migration läuft in BEGIN/COMMIT-Transaktion — bei Fehler vollständiger Rollback |
| T-95-01-FIX | Tampering | SQL-Fundstellen mit 'leader' | mitigate | String-Ersetzung ohne Logikänderung; Tests (`TestRoleDefinitionsContext`) verifizieren Konsistenz nach Migration |
</threat_model>

<verification>
Nach Abschluss beider Tasks:

1. Migration-Datei existiert: `ls database/migrations/0112_role_model_cleanup.up.sql`
2. FK-Reihenfolge korrekt: grep in 0112.up.sql zeigt UPDATE hist_group_member_roles VOR DELETE FROM role_definitions
3. Kein 'leader' mehr in den 5 Backend-SQL-Dateien: `grep -rn "'leader'" backend/internal/repository/admin_users_queries.go backend/internal/repository/admin_users_tab_repository.go backend/internal/repository/anime_contributions_public_repository.go backend/internal/services/badge_service.go | grep -v fansub_lead | wc -l` → 0
4. Go-Tests: `cd backend && go test ./internal/repository/... -run TestRoleDefinitionsContext` → PASS
5. Frontend-Tests: `cd frontend && npm run test -- GroupHistRoleDialog --reporter=dot` → PASS
</verification>

<success_criteria>
- Migration 0112 up+down existieren und enthalten alle 7 SQL-Schritte in korrekter Reihenfolge (per D-04)
- Alle 5 Go-Backend-SQL-Fundstellen mit 'leader' zeigen 'fansub_lead' (per D-04, Fallstrick 2)
- groupHistoryDialogRoleWhitelist enthält 'fansub_lead', 'project_lead', 'techadmin', 'gfxler' (per D-06)
- Keine Go-Kompilierfehler: `go build ./...` grün
- TestRoleDefinitionsContext grün
</success_criteria>

<output>
Erstelle `.planning/phases/95-rollenmodell-entwirren-gruppen-vs-projekt-ebene-techadmin-gf/95-01-SUMMARY.md` nach Abschluss.
</output>
