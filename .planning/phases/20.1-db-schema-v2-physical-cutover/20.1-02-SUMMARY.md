---
phase: 20.1-db-schema-v2-physical-cutover
plan: 02
subsystem: database
tags: [postgres, migrations, schema-audit, db-schema-v2, release-graph]
requires:
  - phase: 20.1-db-schema-v2-physical-cutover
    provides: Plan 01 live-vs-target schema inventory and deletion boundary
provides:
  - Physical DB Schema v2 target tables
  - Filler metadata and combined-file release coverage structures
  - Enforceable DB Schema v2 contract check
affects: [phase-20.1, phase-20, episode-import, release-graph, schema-contracts]
tech-stack:
  added: []
  patterns:
    - Additive PostgreSQL forward migrations for documented target structures
    - PowerShell schema audit with failing contract-check mode
key-files:
  created:
    - database/migrations/0044_add_db_schema_v2_target_tables.up.sql
    - database/migrations/0044_add_db_schema_v2_target_tables.down.sql
    - database/migrations/0045_reconcile_db_schema_v2_columns.up.sql
    - database/migrations/0045_reconcile_db_schema_v2_columns.down.sql
    - scripts/schema-v2-contract-check.ps1
    - .planning/phases/20.1-db-schema-v2-physical-cutover/20.1-02-schema-audit.md
  modified:
    - scripts/schema-v2-audit.ps1
key-decisions:
  - "Documented target-named columns were added beside existing compatibility columns instead of renaming or dropping legacy columns in the additive Plan 02 slice."
  - "The schema contract check fails on missing target structures and unexpected divergences while allowing the old `streams` table and episode-version table family until their explicit cleanup plans."
patterns-established:
  - "Use `scripts/schema-v2-contract-check.ps1` for a loud DB Schema v2 contract failure before future release-graph work."
requirements-completed: [P20.1-SC2, P20.1-SC4]
duration: 6min
completed: 2026-04-21
---

# Phase 20.1 Plan 02: DB Schema v2 Physical Target Summary

**Physical DB Schema v2 tables, filler metadata, combined release coverage, and an enforceable schema contract check**

## Performance

- **Duration:** 6 min
- **Started:** 2026-04-21T13:27:13Z
- **Completed:** 2026-04-21T13:32:43Z
- **Tasks:** 3
- **Files modified:** 7

## Accomplishments

- Added the missing DB Schema v2 target tables, including `episode_filler_types`, `release_variant_episodes`, theme/opening/ending structures, contributor/member/note tables, and `users`.
- Reconciled target columns, constraints, and indexes while preserving compatibility columns for the later cleanup plan.
- Added a failing schema contract check and recorded a passing Plan 02 audit with zero missing target artifacts.

## Task Commits

1. **Task 1: Add missing target tables and lookup values** - `11daf51` (`feat`)
2. **Task 2: Reconcile divergent columns, constraints, and indexes** - `8d2fa96` (`feat`)
3. **Task 3: Add schema contract verification** - `186cb97` (`test`)

## Files Created/Modified

- `database/migrations/0044_add_db_schema_v2_target_tables.up.sql` - Creates missing target tables and seeds filler, theme type, and contributor role lookups.
- `database/migrations/0044_add_db_schema_v2_target_tables.down.sql` - Rolls back the target-table additions in dependency order.
- `database/migrations/0045_reconcile_db_schema_v2_columns.up.sql` - Adds target-named columns, foreign keys, unique constraints, and indexes without dropping compatibility columns.
- `database/migrations/0045_reconcile_db_schema_v2_columns.down.sql` - Rolls back the reconciliation migration.
- `scripts/schema-v2-audit.ps1` - Adds `-FailOnContractGaps` mode and removes the false FK expectation for `stream_sources.external_id`.
- `scripts/schema-v2-contract-check.ps1` - Runs the audit in failing contract-check mode.
- `.planning/phases/20.1-db-schema-v2-physical-cutover/20.1-02-schema-audit.md` - Passing Plan 02 audit artifact.

## Verification

- `cd backend; go run ./cmd/migrate up -database-url "postgres://team4s:team4s_dev_password@localhost:5433/team4s_v2?sslmode=disable" -dir ..\database\migrations` applied the two new migrations to the local DB.
- Fresh temporary DB verification applied all 45 migrations from scratch against `team4s_v2_plan02` and confirmed all 15 newly required target tables existed.
- `powershell -ExecutionPolicy Bypass -File .\scripts\schema-v2-contract-check.ps1 -OutputPath .\.planning\phases\20.1-db-schema-v2-physical-cutover\20.1-02-schema-audit.md` passed.
- `cd backend; go test ./internal/migrations -count=1` passed.

## Audit Result

| Category | Count |
|---|---:|
| Present artifacts | 398 |
| Missing artifacts | 0 |
| Divergent artifacts | 1 |
| Legacy deletion targets still present | 3 |
| Contract check | PASSED |

The remaining divergence is the old `streams` table existing beside target `release_streams`. The three legacy episode-version tables remain by design for Plan 03.

## Decisions Made

- Target-named compatibility columns such as `fansub_releases.source`, `release_version_groups.fansubgroup_id`, and `fansub_group_aliases.group_id` were added without removing the existing live columns.
- The contract check treats `streams` and the episode-version table family as explicitly deferred cleanup, not Plan 02 failures.

## Deviations from Plan

None - plan executed as written.

## Issues Encountered

- The original audit heuristic treated any `_id` column as requiring a foreign key, which incorrectly flagged `stream_sources.external_id`. The check now excludes `stream_sources` from that heuristic because the target document defines `external_id` as provider identity, not a local FK.

## Known Stubs

None found in files created or modified by this plan.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plan 03 can remove only the approved legacy episode-version table family from the already-physicalized target schema. Phase 20 can then build release-native episode import on top of `release_variant_episodes`, filler metadata, and the release/version/variant/stream graph.

## Self-Check: PASSED

- Verified created/modified files exist.
- Verified task commits exist: `11daf51`, `8d2fa96`, `186cb97`.

---
*Phase: 20.1-db-schema-v2-physical-cutover*
*Completed: 2026-04-21*
