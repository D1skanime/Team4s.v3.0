---
phase: 20.1-db-schema-v2-physical-cutover
plan: 02
type: database
wave: 2
depends_on:
  - 20.1-01-PLAN.md
files_modified:
  - database/migrations
  - docs/architecture/db-schema-v2.md
  - backend/internal/repository
autonomous: true
requirements:
  - P20.1-SC2
  - P20.1-SC4
must_haves:
  truths:
    - "All documented DB Schema v2 tables exist physically after migrations."
    - "Filler metadata and multi-episode release coverage are part of the physical schema."
    - "Schema migrations are additive until the explicit legacy-drop step."
---

<objective>
Build the full documented DB Schema v2 as real database tables.

Purpose: Stop future features from inventing partial schema slices by making the target model physically available.
Output: New migrations and schema checks that create/reconcile all missing target tables, columns, constraints, indexes, and lookup values.
</objective>

<context>
@docs/architecture/db-schema-v2.md
@.planning/phases/20.1-db-schema-v2-physical-cutover/20.1-CONTEXT.md
@.planning/phases/20.1-db-schema-v2-physical-cutover/20.1-01-SUMMARY.md
@database/migrations
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing target tables and lookup values</name>
  <files>database/migrations</files>
  <action>
    Add migrations for every missing table or lookup value from `docs/architecture/db-schema-v2.md`, including but not limited to `EpisodeFillerType`, `ReleaseVariantEpisode`, any missing theme/opening/ending/kara tables, contributor/note tables if they are documented target tables, and any missing media/release joins.
  </action>
  <verify>
    <automated>Migrate an empty local DB from scratch and verify all target table names exist.</automated>
  </verify>
  <done>Target schema tables exist physically.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile divergent columns, constraints, and indexes</name>
  <files>database/migrations</files>
  <action>
    Add migrations for missing columns, foreign keys, unique constraints, and indexes where live schema diverges from the target document. Keep compatibility columns only when explicitly documented as transitional.
  </action>
  <verify>
    <automated>Run schema audit and confirm no unhandled target-vs-live gaps remain except explicitly documented transitional columns.</automated>
  </verify>
  <done>The physical schema matches the architecture contract closely enough for feature work to rely on it.</done>
</task>

<task type="auto">
  <name>Task 3: Add schema contract verification</name>
  <files>scripts/schema-audit tooling or backend schema tests</files>
  <action>
    Add an automated schema contract check that compares critical DB Schema v2 structures against the migrated database, with special coverage for Episode, EpisodeFillerType, EpisodeTitle, FansubRelease, ReleaseVersion, ReleaseVariant, ReleaseVariantEpisode, StreamSource, Stream/ReleaseStream, and ReleaseVersionGroup.
  </action>
  <verify>
    <automated>The schema contract check fails before missing pieces are added and passes after migrations.</automated>
  </verify>
  <done>Future agents get a loud failure if the DB drifts away from the documented schema.</done>
</task>

</tasks>

<success_criteria>
- The complete documented target schema exists in the local DB.
- Filler and combined-file coverage are first-class physical structures.
- Automated schema audit guards the contract.
</success_criteria>

<output>
Create `20.1-02-SUMMARY.md`.
</output>

