---
phase: 157-projekt-memberseite-visuelles-referenzdesign
plan: 02
type: execute
wave: 2
depends_on: ["157-01"]
files_modified:
  - frontend/src/components/fansubs/projectMember/ProjectMemberHero.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberSummary.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberSummary.module.css
  - frontend/src/components/fansubs/projectMember/ProjectMemberStickyNav.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberStickyNav.module.css
  - frontend/src/components/fansubs/projectMember/ProjectMemberStickyNav.test.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberSummaryBand.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberSummaryBand.module.css
  - frontend/src/components/fansubs/projectMember/ProjectMemberPage.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberPage.module.css
autonomous: true
requirements: [P157-01, P157-02, P157-03, P157-04, P157-10]

must_haves:
  truths:
    - "The hero shows the avatar on the left with name/verified-badge/context/role-chip to its right, and both action buttons sit side by side (stacking only under 640px)"
    - "The statistics row is ONE card containing four icon+number+label entries in the order Rolle(n), Beiträge, Medien, Releases with correct German singular/plural, never four separate large boxes"
    - "The quick-nav row visibly highlights whichever of the three sections is currently in view, and never clips a pill at 390px width"
    - "A tinted summary band between the nav and the sections reads e.g. 'Typesetting für 13 Folgen · 12 dokumentierte Arbeitsnotizen · 2 Medien', omitting the Folgen clause entirely when episodes is 0"
  artifacts:
    - path: "frontend/src/components/fansubs/projectMember/ProjectMemberSummary.module.css"
      provides: "New, dedicated CSS module for the single-card stat strip (not stacked onto ProjectMemberPage.module.css)"
    - path: "frontend/src/components/fansubs/projectMember/ProjectMemberStickyNav.module.css"
      provides: "New, dedicated CSS module for the nav pills incl. active state, moved out of ProjectMemberPage.module.css"
    - path: "frontend/src/components/fansubs/projectMember/ProjectMemberSummaryBand.tsx"
      provides: "New component rendering the Beitragszusammenfassung band from summary.role_labels + summary.counts"
  key_links:
    - from: "frontend/src/components/fansubs/projectMember/ProjectMemberPage.tsx"
      to: "frontend/src/components/fansubs/projectMember/ProjectMemberSummaryBand.tsx"
      via: "<ProjectMemberSummaryBand summary={summary} /> rendered between ProjectMemberStickyNav and the three sections"
      pattern: "ProjectMemberSummaryBand"
    - from: "frontend/src/components/fansubs/projectMember/ProjectMemberStickyNav.tsx"
      to: "window.IntersectionObserver"
      via: "scrollspy observing #texte/#bilder/#releases, guarded by typeof IntersectionObserver !== 'undefined'"
      pattern: "IntersectionObserver"
---

<objective>
Rebuild the top of the Projekt-Member-Seite to match the written reference specification
(157-CONTEXT.md workstreams A, B, C, and D's frontend half): a compact two-column hero, a single
statistics card, an active-state-aware quick nav, and a new tinted "Beitragszusammenfassung" band
that reads the `episodes` count landed in 157-01.

Purpose: establish the correct information hierarchy (Person -> Rolle -> Umfang) above the detail
sections, per P157-01/02/03/04/10.
Output: Hero/Summary/StickyNav visually rebuilt with their own new CSS modules (per
mandatory_run_constraints #7 — no new rules stacked onto the 270-line ProjectMemberPage.module.css);
a new ProjectMemberSummaryBand component wired into ProjectMemberPage.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-CONTEXT.md
@.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-PATTERNS.md

<interfaces>
<!-- frontend/src/types/projectMember.ts, after 157-01 -->
export interface ProjectMemberCounts { roles: number; notes: number; media: number; releases: number; episodes: number }
export interface ProjectMemberSummary {
  member_id: number; member_slug: string | null; member_display_name: string
  member_avatar_url: string | null; is_verified: boolean; role_labels: string[]
  counts: ProjectMemberCounts
}

<!-- frontend/src/components/ui (barrel @/components/ui): Button (variant/size/leftIcon/href props),
     already used throughout this folder. Only global tokens allowed: --surface-canvas/-card/-sunken,
     --text-primary/-muted, --color-border, --accent-primary/-deep, --shadow-*, --radius-*. -->

<!-- Current ProjectMemberHero.tsx actions row (frontend/src/components/fansubs/projectMember/ProjectMemberHero.tsx:76-83) -->
<div className={styles.heroActions}>
  <Button href={`/members/${memberSlug}`} variant="secondary" size="sm">Vollständiges Memberprofil</Button>
  <Button href={projectPath} variant="ghost" size="sm">Zurück zum Projekt</Button>
</div>

<!-- Icon-in-button idiom (frontend/src/components/profile/MemberProfileHero.tsx:214-222) -->
<Button className={styles.heroActionButton} href={publicProfileHref} variant="secondary" leftIcon={<Eye size={16} />}>
  Öffentliches Profil ansehen
</Button>

<!-- IntersectionObserver graceful-degradation guard idiom (frontend/src/hooks/useNearViewportActivation.ts) -->
if (!target || (deferActivation && typeof IntersectionObserver !== 'undefined')) return
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero relayout — primary button + icon, secondary "Zurück zum Projekt"</name>
  <files>frontend/src/components/fansubs/projectMember/ProjectMemberHero.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberPage.module.css</files>
  <read_first>
    frontend/src/components/fansubs/projectMember/ProjectMemberHero.tsx (full file)
    frontend/src/components/fansubs/projectMember/ProjectMemberPage.module.css (`.hero`, `.heroAvatar`, `.heroBody`, `.heroActions` rules, lines 44-114 and the 640px media query)
    frontend/src/components/profile/MemberProfileHero.tsx (leftIcon idiom, lines ~214-222)
  </read_first>
  <action>
    In ProjectMemberHero.tsx: import `Users` from `lucide-react`. Change the "Vollständiges
    Memberprofil" Button to `variant="primary"` with `leftIcon={<Users size={16} />}` (per
    157-CONTEXT.md Workstream A: "links dunkles Primary ... mit Personen-Icon"). Change "Zurück zum
    Projekt" Button from `variant="ghost"` to `variant="secondary"` and prefix its label with
    `←&nbsp;` per the reference wording "← Zurück zum Projekt" (keep the href/projectPath logic
    unchanged — per D-16/mandatory_run_constraints #8, no navigation/data change). Do not touch
    `useRoleCatalog`, `presentationForRole`, or `getMemberInitials`. In
    ProjectMemberPage.module.css: this is an in-place adjustment of the EXISTING `.hero`/
    `.heroAvatar`/`.heroActions` rules (already `display:flex` with avatar-left/body-right at
    desktop and `flex-direction:column` stacking under 640px per the existing media query) — do not
    add new selectors here; only tune existing declared values if the primary-button visual weight
    needs more vertical breathing room (e.g. `.heroActions { margin-top }`). This keeps
    ProjectMemberPage.module.css net flat or smaller, per mandatory_run_constraints #7.
  </action>
  <acceptance_criteria>
    - `ProjectMemberHero.tsx` imports `Users` from `lucide-react` and passes `leftIcon={<Users size={16} />}` to the primary button
    - The "Vollständiges Memberprofil" button has `variant="primary"`; "Zurück zum Projekt" has `variant="secondary"`
    - No changes to `useRoleCatalog`/`presentationForRole`/`getMemberInitials` call sites or to `summary`/`memberSlug`/`projectPath` prop usage
    - `ProjectMemberPage.module.css` gains zero new class selectors from this task
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i ProjectMemberHero || echo OK"</automated>
  </verify>
  <done>Hero renders avatar-left/body-right with two side-by-side buttons (primary+icon, secondary), stacking only under 640px; no data/behavior regression.</done>
</task>

<task type="auto">
  <name>Task 2: Single-card Statistikleiste + active-state Tab-Nav</name>
  <files>frontend/src/components/fansubs/projectMember/ProjectMemberSummary.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberSummary.module.css, frontend/src/components/fansubs/projectMember/ProjectMemberStickyNav.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberStickyNav.module.css, frontend/src/components/fansubs/projectMember/ProjectMemberStickyNav.test.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberPage.module.css</files>
  <read_first>
    frontend/src/components/fansubs/projectMember/ProjectMemberSummary.tsx (full file)
    frontend/src/components/fansubs/projectMember/ProjectMemberStickyNav.tsx (full file — keep `scrollToSection`'s `prefers-reduced-motion` check verbatim)
    frontend/src/components/fansubs/projectMember/ProjectMemberPage.module.css (`.summary`/`.summaryCard`/`.summaryValue`/`.summaryLabel` at lines ~117-140, and `.stickyNav`/`.stickyNavItem`/`.stickyNavCount` at lines ~143-177 — both blocks are being extracted out of this file)
    frontend/src/components/ui/ui.module.css (`.tabButtonActive` around line 1258, for the filled/tinted-vs-outlined active-state visual convention — port the idea, not the class)
    frontend/src/hooks/useNearViewportActivation.ts and frontend/src/hooks/useNearViewportActivation.test.ts (IntersectionObserver graceful-degradation guard + vi.stubGlobal test pattern to copy)
  </read_first>
  <action>
    Statistikleiste: rewrite `ProjectMemberSummaryBar` in ProjectMemberSummary.tsx to render ONE
    container (`styles.summary` from a new `./ProjectMemberSummary.module.css`) holding four
    entries in order `roles, notes, media, releases`, each with a round icon chip
    (`Users`/`FileText`/`Image as ImageIcon`/`Package` from `lucide-react`) + bold number + small
    label, separated by thin vertical dividers (CSS `border-inline-start` on all but the first
    entry). Add a local pluralization helper (same terse inline-function style as `formatDate` in
    `ProjectMemberNoteCard.tsx`) producing: roles -> "Rolle"/"Rollen", notes -> "Beitrag"/"Beiträge",
    media -> "Medium"/"Medien", releases -> "Release"/"Releases" (0 and >1 both use the plural form
    per German grammar, e.g. "0 Releases"). Delete the old `SUMMARY_CARDS` array and its 4-card
    grid usage. Remove the now-dead `.summary`/`.summaryCard`/`.summaryValue`/`.summaryLabel` rules
    (and the `@media (max-width: 900px) { .summary { ... } }` block) from
    `ProjectMemberPage.module.css`. On mobile the new single card may wrap to two rows but must
    never regress to four separate large boxes.

    Tab-Nav active state: move `.stickyNav`/`.stickyNavItem`/`.stickyNavCount` out of
    `ProjectMemberPage.module.css` into a new `./ProjectMemberStickyNav.module.css`, replacing
    `overflow-x: auto` with `flex-wrap: wrap` (mandatory_run_constraints #10's "kein Abschneiden auf
    390 px" — three short pills must wrap or fit, never clip). Add an active-state class
    (`stickyNavItemActive`) using only global tokens (e.g. `background: var(--surface-sunken);
    border-color: var(--accent-primary)` for the filled/tinted look vs. plain white+border for
    inactive, matching the filled-vs-outlined convention referenced in `ui.module.css`'s
    `.tabButtonActive`). In ProjectMemberStickyNav.tsx: keep `scrollToSection`'s existing
    `prefers-reduced-motion` check verbatim; add `const [activeId, setActiveId] =
    useState<string>(NAV_ITEMS[0].id)`; in a `useEffect`, guard with `typeof IntersectionObserver
    === 'undefined'` (fall back to leaving `activeId` as whatever was last clicked — no observer
    wiring in that branch); otherwise create one `IntersectionObserver` watching
    `document.getElementById(item.id)` for each of the three `NAV_ITEMS`, and on each callback set
    `activeId` to the id of the entry with the largest `intersectionRatio` among currently
    intersecting entries; disconnect on unmount. On click, also set `activeId` immediately
    (last-clicked fallback) before calling `scrollToSection`. Apply
    `classNames`-equivalent conditional class (plain template-literal concatenation, matching this
    folder's existing style — no `classNames` import needed) to add `stickyNavItemActive` when
    `item.id === activeId`. Add `frontend/src/components/fansubs/projectMember/
    ProjectMemberStickyNav.test.tsx` (new, jsdom) that stubs `IntersectionObserver` via
    `vi.stubGlobal`, renders `ProjectMemberStickyNav`, invokes the captured callback with an
    intersecting entry for `bilder`, and asserts the `bilder` pill carries the active class while
    `texte` does not; add a second test asserting clicking the `releases` pill marks it active
    immediately (last-clicked fallback) without requiring an IntersectionObserver callback.
  </action>
  <acceptance_criteria>
    - `ProjectMemberSummary.module.css` is a new file; `ProjectMemberPage.module.css` no longer
      contains `.summaryCard`, `.stickyNavItem`, or the 900px `.summary` media query block
      (`grep -c "summaryCard\|stickyNavItem" ProjectMemberPage.module.css` == 0)
    - Statistikleiste order in source is `roles, notes, media, releases`
    - `ProjectMemberStickyNav.tsx` contains `typeof IntersectionObserver === 'undefined'` guard and
      no longer relies on `overflow-x: auto` for narrow-screen fit
    - New `ProjectMemberStickyNav.test.tsx` passes and exercises real component code (stubbed
      global + rendered DOM assertions, not source-substring checks)
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/fansubs/projectMember/ProjectMemberStickyNav.test.tsx"</automated>
  </verify>
  <done>Statistikleiste is one card with icon+number+label in the reference order and correct singular/plural; the nav row highlights the in-view section and never clips at 390px.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Beitragszusammenfassung band (episodes-count consumer)</name>
  <files>frontend/src/components/fansubs/projectMember/ProjectMemberSummaryBand.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberSummaryBand.module.css, frontend/src/components/fansubs/projectMember/ProjectMemberSummaryBand.test.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberPage.tsx</files>
  <read_first>
    frontend/src/components/fansubs/projectMember/ProjectMemberPage.tsx (full file — insertion point is between `<ProjectMemberStickyNav .../>` and the sections, only inside the non-empty branch)
    frontend/src/types/projectMember.ts (ProjectMemberSummary/ProjectMemberCounts, post-157-01: includes `counts.episodes`)
    frontend/src/components/fansubs/projectMember/ProjectMemberNoteCard.tsx (terse ternary style for conditional clause omission, e.g. `note.release_version_label ? ... : ''`)
  </read_first>
  <behavior>
    - Given `role_labels=['Typesetting']`, `counts={notes:12, media:2, episodes:13}` -> renders
      exactly "Typesetting für 13 Folgen · 12 dokumentierte Arbeitsnotizen · 2 Medien"
    - Given `role_labels=['Typesetting','Timing']`, same counts -> role clause is
      "Typesetting, Timing für 13 Folgen ..." (comma-joined, no "und")
    - Given `counts.episodes === 0` -> the "für N Folgen" clause is omitted entirely (never renders
      "für 0 Folgen"), remaining text still reads "<Rollen> · M dokumentierte Arbeitsnotizen · K Medien"
  </behavior>
  <action>
    Create `ProjectMemberSummaryBand.tsx` exporting `ProjectMemberSummaryBand({ summary }: { summary:
    ProjectMemberSummary })`. Render a tinted, rounded band (own CSS module, tokens only:
    `--surface-sunken`/`--accent-primary`-tinted background, `--radius-md`, `--text-primary`) with a
    small `BarChart3` icon (`lucide-react`) on the left and a sentence built as: `roles =
    summary.role_labels.join(', ')` (empty-role-labels edge case: if empty, omit the leading
    "<roles> für" segment entirely and start with the episode/notes clause), `episodesClause =
    summary.counts.episodes > 0 ? \` für ${summary.counts.episodes} Folgen\` : ''`, then
    ` · ${summary.counts.notes} dokumentierte Arbeitsnotizen · ${summary.counts.media} Medien`. Wire
    it into ProjectMemberPage.tsx: render `<ProjectMemberSummaryBand summary={summary} />`
    immediately after `<ProjectMemberStickyNav counts={counts} />` and before
    `<ProjectMemberNotesSection ... />`, inside the existing `isEmpty ? ... : (<>...</>)` non-empty
    branch only (do not render the band in the empty-state branch). Add
    `ProjectMemberSummaryBand.test.tsx` (jsdom) covering the three `<behavior>` cases with
    `screen.getByText(...)` on the exact composed sentence.
  </action>
  <acceptance_criteria>
    - `ProjectMemberSummaryBand.test.tsx` passes and covers single-role, multi-role, and
      zero-episodes cases with exact-string assertions
    - `ProjectMemberPage.tsx` renders the band only inside the non-empty branch, between StickyNav
      and NotesSection
    - No new design tokens introduced (grep the new CSS module for `var(--` and confirm every
      token is one of `--surface-canvas`, `--surface-card`, `--surface-sunken`, `--text-primary`,
      `--text-muted`, `--color-border`, `--accent-primary`, `--accent-deep`, `--shadow-*`, `--radius-*`)
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/fansubs/projectMember/ProjectMemberSummaryBand.test.tsx"</automated>
  </verify>
  <done>The summary band renders the exact reference sentence shape, correctly omitting the Folgen clause at 0, and is wired into the page between nav and sections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend JSON (`role_labels`, counts) -> rendered text | Member-controlled/curated data (role labels, display name already rendered elsewhere) rendered as plain text nodes, never `dangerouslySetInnerHTML` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-04 | Spoofing/Tampering (XSS) | ProjectMemberSummaryBand rendering `role_labels.join(', ')` | accept | Rendered via JSX text interpolation (auto-escaped by React), never raw HTML; identical risk profile to the existing Hero role-chip rendering already in production |
| T-157-05 | Denial of Service | IntersectionObserver in ProjectMemberStickyNav | mitigate | Observer is disconnected on unmount; guarded by `typeof IntersectionObserver === 'undefined'` so it never throws in environments without the API |
</threat_model>

<verification>
`docker compose exec -T team4sv30-frontend sh -c "cd /app && npm run typecheck"`
`docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/fansubs/projectMember/ProjectMemberStickyNav.test.tsx src/components/fansubs/projectMember/ProjectMemberSummaryBand.test.tsx"`
Manual spot-check (covered fully by 157-06's Live-UAT): hero/stat/nav/band order at 390px and 1440px.
</verification>

<success_criteria>
Hero, Statistikleiste, Tab-Nav, and Beitragszusammenfassung band all match the written reference
spec's structure and text; no new CSS was appended to the shared `ProjectMemberPage.module.css`
(net line count of that file should decrease, since `.summaryCard`/`.stickyNavItem` rules moved
out); `episodes` from 157-01 is consumed correctly including the 0-episodes edge case.
</success_criteria>

<output>
Create `.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-02-SUMMARY.md` when done
</output>
