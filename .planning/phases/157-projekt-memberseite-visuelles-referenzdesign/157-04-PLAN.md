---
phase: 157-projekt-memberseite-visuelles-referenzdesign
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.module.css
  - frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.test.tsx
autonomous: true
requirements: [P157-08, P157-07]

must_haves:
  truths:
    - "The Bilder & Medien header shows an icon next to the title, with the count on the right, matching the Texte/Releases section header shape"
    - "The gallery shows 2 columns on narrow screens and 2-3 columns on desktop, unchanged lightbox behavior"
    - "Once every medium is loaded (no more pages), the pager shows no 'Alle N angezeigt' text — the pager strip disappears entirely, matching the reference (\"Kein 'Alle 2 angezeigt'\")"
  artifacts:
    - path: "frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.tsx"
      provides: "Header icon + pagerInfo only rendered while canShowMore"
  key_links:
    - from: "frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.tsx"
      to: "frontend/src/components/fansubs/projectMember/useProjectMemberCollection.ts"
      via: "canShowMore/canShowLess/showMore/showLess consumed read-only, unmodified"
      pattern: "useProjectMemberCollection"
---

<objective>
Bring the Bilder & Medien section's header and pager copy in line with the reference (Workstream G,
P157-08) without touching the lightbox, the media-fetch wiring, or `useProjectMemberCollection`
(mandatory_run_constraints #3/#8).

Purpose: matches the reference's cleaner section header and removes the redundant "Alle 2
angezeigt" line called out explicitly in 157-CONTEXT.md's Ist-Zustand audit.
Output: Gallery header with icon; pager strip that disappears (not just empty-texts) once
everything is loaded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-CONTEXT.md
@.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-PATTERNS.md

<interfaces>
<!-- frontend/src/components/fansubs/projectMember/useProjectMemberCollection.ts (READ-ONLY, do not modify) -->
interface Result<T> { shown: T[]; loadedCount: number; loading: boolean; error: boolean; canShowMore: boolean; canShowLess: boolean; showMore: () => void; showLess: () => void }

<!-- Current header shape shared by Notes/Media/Releases sections, via ProjectMemberPage.module.css -->
<div className={pageStyles.sectionHead}>
  <h2 id="..." className={pageStyles.sectionTitle}>Bilder &amp; Medien</h2>
  <span className={pageStyles.sectionCount}>{count}</span>
</div>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header icon + pager "Alle N angezeigt" removal</name>
  <files>frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.module.css</files>
  <read_first>
    frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.tsx (full file)
    frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.module.css (full file, grid-template-columns breakpoints)
    frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.tsx (identical sectionHead/pager JSX shape, for parity reference)
  </read_first>
  <action>
    Import `Image as ImageIcon` from `lucide-react`. In the `pageStyles.sectionHead` block, add
    `<ImageIcon size={18} aria-hidden="true" />` as a sibling immediately before the `<h2>` text
    (wrap title+icon in a small flex span if needed so `sectionTitle` layout is unaffected;
    reuse `pageStyles.sectionTitle` class, do not create a new page-module class). Change the
    `pagerInfo` span from unconditionally rendering `canShowMore ? '${shown.length} von ${count}
    angezeigt' : 'Alle ${count} angezeigt'` to `canShowMore ? <span
    className={pageStyles.pagerInfo}>{...}</span> : null` — i.e. render nothing (not an empty
    string) once `!canShowMore`, so the pager row visually collapses when Media has nothing further
    to load. Verify (do not blindly rewrite) `ProjectMemberMediaGallery.module.css`'s
    grid-template-columns already satisfies "2 columns mobile, 2-3 desktop" per
    157-PATTERNS.md's note that it is "already close to spec" — only adjust breakpoints if visual
    inspection during Live-UAT (157-06) later flags a mismatch; do not preemptively rewrite working
    breakpoints.
  </action>
  <acceptance_criteria>
    - Gallery header renders an icon before the "Bilder & Medien" text
    - When `canShowMore` is false, no "Alle N angezeigt" text node exists in the rendered pager row
    - `ProjectMemberMediaViewer`, `useProjectMemberCollection` usage, and `ProjectMemberMediaCard` are unchanged
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/fansubs/projectMember/ProjectMemberMediaGallery.test.tsx"</automated>
  </verify>
  <done>Header matches the Notes/Releases header shape with an icon; the "Alle N angezeigt" line is gone once everything is loaded.</done>
</task>

<task type="auto">
  <name>Task 2: Adapt ProjectMemberMediaGallery.test.tsx for the removed pager text</name>
  <files>frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.test.tsx</files>
  <read_first>
    frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.test.tsx (full file, all 3 existing `it(...)` blocks)
  </read_first>
  <action>
    Adapt (do not delete) the existing 3 tests, all of which currently only assert on
    `cards()`/dialog behavior and are unaffected by the header/pager change. Add a new
    `it('renders no "Alle N angezeigt" pager text once every medium is loaded', ...)` that mocks
    `getProjectMemberMedia` to resolve a single page of e.g. 3 items with `has_more: false`, renders
    `ProjectMemberMediaGallery` with `count={3}`, waits for `cards()` to have length 3, and asserts
    `screen.queryByText(/Alle \d+ angezeigt/)` is `null`.
  </action>
  <acceptance_criteria>
    - New test passes and genuinely renders the component (RTL render + waitFor), matching this
      file's existing compliant Teststil pattern
    - All 4 tests in the file pass together
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/fansubs/projectMember/ProjectMemberMediaGallery.test.tsx"</automated>
  </verify>
  <done>Test suite proves the "Alle N angezeigt" text is gone once fully loaded, alongside the existing 3 passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Purely presentational change to already-fetched, already-authorized public media metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-06 | (none applicable) | ProjectMemberMediaGallery header/pager | accept | No new data source, no new user input, no new rendering of unescaped content introduced |
</threat_model>

<verification>
`docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/fansubs/projectMember/ProjectMemberMediaGallery.test.tsx"`
</verification>

<success_criteria>
Media section header matches the reference shape with an icon; "Alle N angezeigt" no longer renders
once fully loaded; all existing and new tests pass; lightbox and fetch wiring untouched.
</success_criteria>

<output>
Create `.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-04-SUMMARY.md` when done
</output>
