---
phase: 157-projekt-memberseite-visuelles-referenzdesign
plan: 16
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/DECISIONS.md
  - .planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-06-PLAN.md
  - frontend/src/components/fansubs/projectMember/ProjectMemberSummary.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberSummary.module.css
  - frontend/src/components/fansubs/projectMember/ProjectMemberSummaryBand.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberSummaryBand.module.css
  - frontend/src/components/fansubs/projectMember/ProjectMemberSummaryBand.test.tsx
autonomous: true
requirements: [P157-02, P157-04, P157-09, P157-12]

must_haves:
  truths:
    - "ROADMAP.md's Phase 157 requirements table legibly marks P157-02, P157-04, and P157-09 as replaced by explicit later user decisions -- not silently satisfied and not deleted"
    - "DECISIONS.md carries one new durable, dated entry documenting the Hero-inline-metrics replacement and, since no such entry exists today despite being cited by name elsewhere, retroactively records the 2026-09-13 Releases-section removal"
    - "The two orphaned components (ProjectMemberSummaryBar/ProjectMemberSummary.tsx and ProjectMemberSummaryBand.tsx) plus their CSS/test files no longer exist in the repository"
    - "The episodes count and '13 Folgen' hero text are completely unaffected by the deletion"
    - "157-06-PLAN.md's Task 4 stale Statistikleiste/Beitragszusammenfassung-band check instructions are flagged as superseded, not silently left looking current"
    - "The requirement that 157-VERIFICATION.md be regenerated (so its 2026-09-13 gaps_found/10-13 snapshot stops being read as current truth) is explicitly recorded as a required follow-up"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "P157-02/P157-04/P157-09 rows annotated as replaced-by-decision, plus a GAP-03 F5 explanatory paragraph"
      contains: "Nachtrag GAP-03 F5"
    - path: ".planning/DECISIONS.md"
      provides: "New dated entry documenting the Hero-metrics supersession and the retroactive Releases-removal record"
      contains: "## 2026-09-14"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/DECISIONS.md"
      via: "explicit cross-reference in the new ROADMAP paragraph pointing at the new DECISIONS.md entry"
      pattern: "DECISIONS.md"
    - from: "frontend/src (deletion)"
      to: "grep re-confirmation before delete"
      via: "grep -rln \"ProjectMemberSummaryBar\\|ProjectMemberSummaryBand\" frontend/src"
      pattern: "ProjectMemberSummary"
---

<objective>
Close GAP-03 finding F5 from `157-UAT.md` (independent post-GAP-02 check, 2026-09-14): reconcile
`157-VERIFICATION.md`'s `gaps_found` (10/13) status for P157-02/P157-04/P157-09 with the reality
that these three requirements were superseded by later, explicit user decisions -- not left unmet
by a bug -- and remove the two components (`ProjectMemberSummaryBar`, `ProjectMemberSummaryBand`)
that became dead code as a result.

Purpose: A stale `VERIFICATION.md`/`ROADMAP.md` pair currently reads as if three requirements
failed, when in fact the user explicitly redirected the product twice (2026-09-13: inline hero
metrics instead of a separate stat bar and a summary band; 2026-09-13: remove the Releases section
entirely). Leaving this undocumented risks a future planner or checker re-litigating already-settled
product decisions, and leaves two fully orphaned component files (confirmed via grep: zero
importers besides their own test file) sitting in the tree.
Output: An annotated `ROADMAP.md` requirements table, one new durable `DECISIONS.md` entry, a
superseded-flag on `157-06-PLAN.md`'s stale Task 4 wording, and a clean deletion of the two orphaned
components -- with the `episodes` count / "13 Folgen" hero text left completely untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-UAT.md
@.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-VERIFICATION.md

<scope_fence>
This plan closes ONLY GAP-03 finding F5 (see `157-UAT.md`'s "## GAP-03 — Nachkontrolle nach GAP-02"
heading). It is documentation + dead-code removal ONLY -- no backend change, no change to
`episodes` count logic, no change to any still-live component's rendered behavior. F1-F4 are a
separate plan, `157-15-PLAN.md` -- do not do any of that work here; the two plans touch disjoint
files and run in the same wave. This plan does NOT itself invoke `/gsd:verify-phase` or regenerate
`157-VERIFICATION.md` -- that remains an explicit required follow-up (see Task 2's acceptance
criteria and this plan's success criteria).
</scope_fence>

<known_discrepancy>
`157-UAT.md`'s F5 text and `157-VERIFICATION.md`'s P157-09 gap entry both cite
"DECISIONS.md, Eintrag 2026-09-13" for the Releases-section removal. A full read of
`.planning/DECISIONS.md` (83 lines, 3 headings, all dated 2026-06/2026-08) confirms NO such entry
actually exists -- it is cited by name in multiple places but was never written. Task 1 below does
not merely "cross-reference" a prior entry; it must ITSELF supply that missing durable record
(dated 2026-09-14, its actual authorship time, with an explicit note that it is a retroactive
record of an order first given 2026-09-13), alongside the Hero-metrics documentation F5 primarily
asks for.
</known_discrepancy>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile ROADMAP.md/DECISIONS.md with the Hero-metrics and Releases-removal decisions; flag 157-06 Task 4 as superseded</name>
  <files>.planning/ROADMAP.md, .planning/DECISIONS.md, .planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-06-PLAN.md</files>
  <read_first>
    .planning/ROADMAP.md lines 1671-1730 (Phase 157 goal, requirements table rows for P157-02/P157-04/P157-09 at lines ~1688/1690/1695, and the "Plans:" summary line at ~1723)
    .planning/DECISIONS.md full file (83 lines, 3 existing dated entries -- match their prose style: dated `## YYYY-MM-DD:` headings, paragraph prose, no bullet lists)
    .planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-VERIFICATION.md lines 1-30 and 90-110 (the P157-02/P157-09 gap entries this task reconciles)
    .planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-UAT.md's "### F5" subsection (bottom of file, under "## GAP-03")
    .planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-06-PLAN.md lines 180-205 (Task 4 checkpoint -- the stale "Statistikleiste"/"Beitragszusammenfassung band" wording to flag)
  </read_first>
  <action>
    In `ROADMAP.md`'s Phase 157 requirements table, append a short clause to the END of the
    "Inhalt" cell for the P157-02, P157-04, and P157-09 rows -- keep every word of the original
    requirement text intact and legible ahead of the appended clause. For P157-02 and P157-04
    append: " — **Ersetzt** durch Nutzerentscheid (Hero-Kennzahlen ersetzen Statistikleiste/
    Beitragszusammenfassung, siehe Aufträge vom 2026-09-13/2026-09-14 und `DECISIONS.md`)." For
    P157-09 append: " — **Entfällt**: Releases-Sektion auf ausdrücklichen Nutzerauftrag vom
    2026-09-13 vollständig entfernt (Plan 157-08), siehe `DECISIONS.md`." Directly after the
    table (before the "**Faktenbasis:**" paragraph), add one new paragraph starting
    "**Nachtrag GAP-03 F5 (2026-09-14):**" that states P157-02/P157-04/P157-09 are not to be read
    as silently satisfied, names both user orders, and points at the new `DECISIONS.md` entry this
    task adds. Do not alter the `**Plans:**` summary line's meaning about GAP-01/GAP-02 completion,
    only note that GAP-03 closure (157-15/157-16) is now also tracked there (this task only touches
    the requirements-table area; the Plans-list update itself is handled by the planning workflow,
    not this execution task). In `DECISIONS.md`, APPEND (never edit or reorder the existing three
    entries) one new entry titled `## 2026-09-14: Phase 157 Statistikleiste and
    Beitragszusammenfassung superseded by Hero-inline metrics; Releases-section removal recorded
    retroactively`. Its prose must: (a) state that the 2026-09-13 user order's binding starting
    point was "Kennzahlen direkt im Hero: Folgen / Beiträge / Medien", implemented by Plan 157-07
    (folding P157-02/P157-04 into `ProjectMemberHero`/`HeroMetrics`) and further confirmed/extended
    by the 2026-09-14 V6 decision (Plan 157-14, clickable hero metrics, tab-card removed); (b) name
    `ProjectMemberSummary.tsx` (`ProjectMemberSummaryBar`) and `ProjectMemberSummaryBand.tsx` as the
    resulting orphaned dead code, deleted by this same plan (157-16), with the `episodes` count
    (Plan 157-01) explicitly unaffected since it still feeds "13 Folgen"; (c) explicitly record,
    per the `<known_discrepancy>` above, that this is ALSO the first durable log entry for the
    2026-09-13 Releases-section removal (Plan 157-08) -- name the discrepancy (cited elsewhere by
    date, never actually written until now) and date this record honestly at 2026-09-14, not
    backdated to 2026-09-13; (d) close by noting `157-VERIFICATION.md`'s 2026-09-13 `gaps_found`
    snapshot predates this decision and is superseded by the next verification run. In
    `157-06-PLAN.md`, insert exactly one short XML comment (`<!-- ... -->`) immediately before Task
    4's `<how-to-verify>` opening tag, stating that its "Statistikleiste"/"Beitragszusammenfassung
    band" wording is superseded by the new `DECISIONS.md` 2026-09-14 entry and the annotated
    `ROADMAP.md` P157-02/P157-04 rows -- do not rewrite, remove, or reorder any of Task 4's existing
    text.
  </action>
  <acceptance_criteria>
    - `grep -n "Ersetzt\|Entfällt" .planning/ROADMAP.md` shows all three annotated rows, and
      `grep -n "Eine kompakte Statistikleiste mit Icon" .planning/ROADMAP.md` still matches
      (original requirement wording is preserved verbatim).
    - `grep -c "^## 2026-09-14" .planning/DECISIONS.md` reports exactly 1; `git diff
      .planning/DECISIONS.md` shows only an appended block at the end of the file, with all three
      pre-existing `## 2026-` headings and their bodies byte-identical to before.
    - `grep -c "superseded\|Nachtrag GAP-03" .planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-06-PLAN.md` reports >= 1; `git diff` on that file shows only the one inserted comment line/block, with Task 4's `<what-built>`/`<how-to-verify>`/`<resume-signal>` text byte-identical to before.
  </acceptance_criteria>
  <verify>
    <automated>git diff --stat .planning/ROADMAP.md .planning/DECISIONS.md ".planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-06-PLAN.md"</automated>
  </verify>
  <done>ROADMAP.md legibly annotates P157-02/P157-04/P157-09 as replaced-by-decision without deleting or silently passing them; DECISIONS.md carries the durable supersession record plus the retroactive Releases-removal record; 157-06-PLAN.md's stale Task 4 wording is flagged, not rewritten.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the orphaned Statistikleiste/Beitragszusammenfassung components; record the VERIFICATION.md regeneration requirement</name>
  <files>frontend/src/components/fansubs/projectMember/ProjectMemberSummary.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberSummary.module.css, frontend/src/components/fansubs/projectMember/ProjectMemberSummaryBand.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberSummaryBand.module.css, frontend/src/components/fansubs/projectMember/ProjectMemberSummaryBand.test.tsx</files>
  <read_first>
    frontend/src/components/fansubs/projectMember/ProjectMemberSummary.tsx (full file -- exports `ProjectMemberSummaryBar`)
    frontend/src/components/fansubs/projectMember/ProjectMemberSummaryBand.tsx (full file)
    frontend/src/components/fansubs/projectMember/ProjectMemberSummaryBand.test.tsx (full file -- confirm it only imports the sibling being deleted, nothing shared)
    frontend/src/components/fansubs/projectMember/ProjectMemberPage.tsx (confirm it does NOT import either component today)
    frontend/src/components/fansubs/projectMember/ProjectMemberHero.tsx (confirm where `episodes`/"13 Folgen" is actually wired today, so this task can prove it is untouched)
  </read_first>
  <action>
    Immediately before deleting anything, run
    `grep -rln "ProjectMemberSummaryBar\|ProjectMemberSummaryBand" frontend/src` and confirm the
    only hits are the 5 files this task deletes (already confirmed once at planning time: no test
    file exists for `ProjectMemberSummary.tsx` at all, and `ProjectMemberSummaryBand.tsx`'s only
    "importer" is its own `.test.tsx`) -- if grep surfaces any OTHER file, stop and report it
    instead of deleting. Then `git rm` all 5 files:
    `ProjectMemberSummary.tsx`, `ProjectMemberSummary.module.css`, `ProjectMemberSummaryBand.tsx`,
    `ProjectMemberSummaryBand.module.css`, `ProjectMemberSummaryBand.test.tsx`. Do not touch
    `episodes` count logic anywhere (`useProjectMemberCollection.ts`, `ProjectMemberHero.tsx`, the
    backend repository/handler, `shared/contracts/openapi.yaml`) -- it must keep feeding "13
    Folgen" in the hero exactly as before. Because this plan cannot itself invoke
    `/gsd:verify-phase`, do not attempt to regenerate `157-VERIFICATION.md` here -- instead record
    the requirement explicitly as an acceptance criterion below, so the next verification pass is
    not silently skipped.
  </action>
  <acceptance_criteria>
    - Pre-deletion: `grep -rln "ProjectMemberSummaryBar\|ProjectMemberSummaryBand" frontend/src`
      returns zero hits outside the 5 files targeted for deletion (the `.module.css` files contain
      no component-name string and are expected to be absent from the grep output; that is not a
      missing-file signal).
    - Post-deletion: the same grep command returns zero results.
    - `docker compose exec -T team4sv30-frontend sh -c "cd /app && npm run lint && npm run typecheck && npm test"` passes with zero NEW failures (STATE.md's documented baseline of 2 pre-existing CSS-guard failures and 3 todo is the only allowed non-green state).
    - `git diff frontend/src/components/fansubs/projectMember/ProjectMemberHero.tsx` is empty (the hero's `episodes`/"13 Folgen" wiring is untouched by this task).
    - This plan's own success criteria (below) explicitly record, as a required follow-up NOT executed by this task, that the next `/gsd:verify-phase 157` (or equivalent orchestrator verification step) run must regenerate `157-VERIFICATION.md` so its 2026-09-13 `gaps_found` (10/13) snapshot stops being read as current.
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npm run lint && npm run typecheck && npm test"</automated>
  </verify>
  <done>The 5 orphaned files are removed from the repository with zero remaining references anywhere in `frontend/src`; full frontend lint/typecheck/test suite is green with no new failures; `episodes`/"13 Folgen" wiring is provably untouched; the VERIFICATION.md-regeneration requirement is explicitly recorded as an open follow-up rather than silently left stale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan touches only planning documentation (`ROADMAP.md`, `DECISIONS.md`, a comment in `157-06-PLAN.md`) and deletes dead frontend component/CSS/test files that are not imported or reachable from any route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-16-01 | Tampering | `git rm` of orphaned components | accept | Pre-deletion grep re-confirmation is mandatory in Task 2's action/acceptance criteria before any file is removed; no live route, import, or test outside the 5 target files references these components |
| T-157-16-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no new packages |

No new attack surface: this plan is documentation plus dead-code removal only. No new data flow,
no new user input handling, no behavior change to any still-live component.
</threat_model>

<verification>
`docker compose exec -T team4sv30-frontend sh -c "cd /app && npm run lint && npm run typecheck && npm test"` must be green with no new failures beyond the documented baseline. `git diff --stat` on the three doc files must show only additive/annotating changes (no deletions of existing prose). Post-deletion grep for `ProjectMemberSummaryBar`/`ProjectMemberSummaryBand` across `frontend/src` must return zero results.
</verification>

<success_criteria>
- GAP-03 finding F5 from `157-UAT.md` is closed: `ROADMAP.md` legibly documents P157-02/P157-04/P157-09 as replaced-by-decision (never silently marked passed), `DECISIONS.md` carries the durable supersession record (plus the retroactive Releases-removal record that was previously only cited, never written), `157-06-PLAN.md`'s stale Task 4 wording is flagged, and the two orphaned components are deleted with zero regressions.
- `157-VERIFICATION.md`'s 2026-09-13 `gaps_found` (10/13) snapshot is explicitly recorded as superseded and requiring a fresh verification run -- this plan does not regenerate it itself, and does not pretend the snapshot is still current.
- This plan does NOT constitute live human UAT sign-off. The phase-level `157-06` Task 4 checkpoint and a full human re-run of `157-UAT.md`'s GAP-02 points 1-9 plus GAP-03 F1-F5 remain a separate, required, out-of-band human step.
</success_criteria>

<output>
Create `.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-16-SUMMARY.md` when done
</output>
