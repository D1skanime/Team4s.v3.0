---
phase: 157-projekt-memberseite-visuelles-referenzdesign
plan: 06
type: execute
wave: 4
depends_on: ["157-02", "157-03", "157-04", "157-05"]
files_modified:
  - frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/mitwirkende/[memberSlug]/page.test.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberPage.module.css
  - frontend/scripts/shot-projectmember.mjs
autonomous: false
requirements: [P157-10, P157-11, P157-12, P157-13]

must_haves:
  truths:
    - "The full frontend test suite (lint, typecheck, vitest) passes inside the frontend container"
    - "The full backend test suite passes, including the new episodes-count proof from 157-01"
    - "No horizontal scrollbar appears at 320/390/768/1024/1440px viewport widths"
    - "Live screenshots of the real member page (Type / Buddy Complex) show: no per-note role header, the new hero/stat/nav/band structure, the 13-episode summary, no 'Alle N angezeigt' in Media/Releases, and the Releases empty state — with an explicit before/after comparison against the 157-CONTEXT.md baseline numbers"
    - "Live screenshots also confirm role COLOR (not just structure) survived the redesign (P157-13): the Typesetting-authored notes visibly carry their catalog accent color (e.g. #7B3C4E), not a neutral/colorless treatment"
  artifacts:
    - path: "frontend/scripts/shot-projectmember.mjs"
      provides: "Extended fact-gathering covering hero/stat/nav/band structure, notes timeline, media, releases-empty-state, desktop+mobile viewports"
  key_links:
    - from: "frontend/scripts/shot-projectmember.mjs"
      to: "http://127.0.0.1:3300 reverse proxy -> http://127.0.0.1:3000 (frontend)"
      via: "CORS-compliant origin match, per the backend's single-allowed-origin config"
      pattern: "127.0.0.1:3300"
---

<objective>
Close out Phase 157 (Workstream I, P157-10/P157-11/P157-12): verify the full regression surface,
confirm responsive behavior across the required breakpoints, and produce concrete live-UAT evidence
(screenshots + before/after facts) comparing the rebuilt page against the written reference spec
and the 157-CONTEXT.md baseline measurements — not just an assertion that "it works".

Purpose: this is the phase's acceptance gate. Every prior plan's output gets exercised together for
the first time here (frontend Wave-2/3 plans never ran concurrently against each other or against
157-01's backend change until now).
Output: green full test suites (frontend + backend), a documented responsive check, an extended
Live-UAT script with before/after facts and an explicit deviation list, and a human sign-off
checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-CONTEXT.md
@.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize page.test.tsx fixture parity and responsive breakpoint audit</name>
  <files>frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/mitwirkende/[memberSlug]/page.test.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberPage.module.css</files>
  <read_first>
    frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/mitwirkende/[memberSlug]/page.test.tsx (full file — the `summary()` fixture's `counts` object at line ~43, and every `expect(html).toContain(...)` assertion for hero button labels/text)
    frontend/src/components/fansubs/projectMember/ProjectMemberPage.module.css (post 157-02/157-03 state — the `@media (max-width: 900px)` and `@media (max-width: 640px)` blocks)
  </read_first>
  <action>
    Add `episodes: <n>` to every `counts` object in the `summary()` fixture (e.g.
    `{ roles: 2, notes: 5, media: 8, releases: 3, episodes: 6 }` for the default case, and
    `{ roles: 2, notes: 0, media: 0, releases: 0, episodes: 0 }` for the empty-state case) so the
    fixture matches the post-157-01 `ProjectMemberSummary` type. Update the hero-button assertions
    to match 157-02 Task 1's changes: `expect(html).toContain('Vollständiges Memberprofil')` stays,
    but if the button label text changed (e.g. added a leading icon does not change text content,
    so no update needed there) verify "Zurück zum Projekt" still matches the rendered text exactly
    (157-02 prefixed it with an arrow character — update the assertion string to match exactly what
    157-02 produced). Add an assertion for the new summary band text fragment appearing only in the
    non-empty case (e.g. `expect(html).toContain('dokumentierte Arbeitsnotizen')`) and confirm it is
    ABSENT in the already-existing empty-state test case. Audit
    `ProjectMemberPage.module.css` against the required breakpoint set 320/390/768/1024/1440px: add
    or adjust `@media` rules only where a concrete overflow risk exists (e.g. verify the
    Statistikleiste and Tab-Nav's new modules — from 157-02 — behave at 320px, since 157-02 only
    explicitly handled 390/640/900px). Do not invent new breakpoints beyond what's needed to
    eliminate horizontal overflow; prefer `flex-wrap`/`min-width: 0` fixes over new fixed breakpoints.
  </action>
  <acceptance_criteria>
    - Every `counts` object in the fixture file includes `episodes`
    - `npx tsc --noEmit` reports no type errors for the fixture (proves it matches `ProjectMemberSummary`)
    - `page.test.tsx` passes with updated hero/band assertions
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run 'src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/mitwirkende/[memberSlug]/page.test.tsx'"</automated>
  </verify>
  <done>page.test.tsx matches the final component tree and type contract; no known horizontal-overflow gap remains at the five required breakpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Full frontend + backend regression suite</name>
  <files></files>
  <read_first>
    frontend/package.json (scripts: lint, test, typecheck)
  </read_first>
  <action>
    Run the complete frontend suite inside the running container (never on host, never `npm
    install` on host): lint, typecheck, and the full vitest run. Rebuild and run the complete
    backend suite: rebuild the backend container so 157-01's code is live
    (`docker compose up -d --build team4sv30-backend`), then run `go build ./...`, `go vet ./...`,
    and `go test ./...` in a `golang:1.25-alpine` container attached to the `team4s_default`
    network. If any `TEAM4S_PHASE117_TEST_DSN`-gated test (including 157-01's new episodes
    integration test) is to be exercised against real Postgres, derive the database password from
    the running backend container's own `DATABASE_URL`
    (`docker compose exec team4sv30-backend env | grep DATABASE_URL`), never from the repo's `.env`
    file (they can drift), and ensure the database name matches
    `^team4s_phase117_test_[a-z0-9]+$` EXACTLY — a mismatch here fails every gated test at 0.00s
    with an "unsafe ... must match" error that looks like a mass failure but is actually a
    naming-pattern rejection, not a real regression. Fix any failure found before proceeding to
    Task 3; do not proceed to Live-UAT with a red suite.
  </action>
  <acceptance_criteria>
    - `npm run lint`, `npm run typecheck`, `npm test` (vitest run) all exit 0 inside the frontend container
    - `go build ./...`, `go vet ./...`, `go test ./...` all exit 0 for the backend
    - The 157-01 episodes integration test, when run with a correctly-named DSN, reports `countEpisodes == 3` for its fixture (not a naming-pattern skip/failure)
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npm run lint && npm run typecheck && npm test"</automated>
  </verify>
  <done>Both stacks' full automated suites are green, with the DSN-naming gotcha explicitly checked (not silently misdiagnosed as a mass failure).</done>
</task>

<task type="auto">
  <name>Task 3: Extend shot-projectmember.mjs and capture before/after Live-UAT evidence</name>
  <files>frontend/scripts/shot-projectmember.mjs</files>
  <read_first>
    frontend/scripts/shot-projectmember.mjs (full file — the reverse-proxy setup on 127.0.0.1:3300, the two-viewport loop, and the existing `page.evaluate()` facts object)
    .planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-CONTEXT.md (the "Gemessener Ist-Zustand" table — the exact before-numbers to diff against: doc height 2996px mobile/2746px desktop, notes-section height 1649px for 5 notes, role name appearing 5x/12x, "Mitwirkung an Releases0Alle 0 angezeigt" literal text)
  </read_first>
  <action>
    Extend the existing `facts` object inside `page.evaluate()` (append fields, do not introduce a
    second output format) with: `heroButtonLabels` (texts of buttons inside a hero-region selector,
    to confirm both action buttons render side by side), `statBarEntryCount` (count of elements
    matching the new Statistikleiste's entry selector — confirms ONE card with 4 entries, not 4
    cards), `activeNavPill` (text content of whichever nav pill currently carries the active-state
    class/attribute), `summaryBandText` (text content of the new band container, to confirm the
    "Typesetting für 13 Folgen · ..." sentence appears with the live "13" value), `roleRepeatsInNotes`
    (keep the existing regex-based count — expect it to drop from 12/5 to 0, since the timeline no
    longer repeats the role per note for a single-role member), `mediaAllShownTextPresent` and
    `releasesEmptyStateText` (presence check for the dashed-border empty-state sentence);
    `noteAccentColorSamples` (per P157-13 — for each visible note entry, read the computed
    `border-inline-start-color`/`background-color` of its accent element via `getComputedStyle`
    inside `page.evaluate()`, confirming it resolves to a real catalog color, e.g. `rgb(123, 60,
    78)` for Typesetting's `#7B3C4E`, and NOT a neutral/gray or fully transparent value — this is
    the concrete, screenshot-independent proof that role color survived the redesign, not just an
    eyeballed screenshot check). Run the
    script twice — `SHOT_LABEL=before-baseline` is not re-runnable against the old code at this
    point (Waves 1-4 have already landed), so instead diff the newly captured `after` facts directly
    against the numeric baseline already recorded in 157-CONTEXT.md's table (2996/2746px doc height,
    1649/1637px notes-section height, 5x/12x role repeats) rather than re-shooting a live "before".
    Run: rebuild+start both containers fresh, then
    `docker compose exec -T team4sv30-frontend sh -c "cd /app && SHOT_LABEL=after node
    scripts/shot-projectmember.mjs"`. Collect both viewport JSON outputs and the two PNG files per
    viewport. Produce an explicit before/after comparison table (doc height delta, notes-section
    height delta at the same note count, role-repeat count old-vs-new, confirmation of the "13
    Folgen" summary band, confirmation the Releases literal-text bug is gone) plus a list of any
    remaining deviations from the written reference spec in 157-CONTEXT.md's "Referenz-Spezifikation
    in Worten" section.
  </action>
  <acceptance_criteria>
    - Script runs successfully against `http://127.0.0.1:3300` (never `:3000` directly) for both mobile (390px) and desktop (1440px) viewports
    - `consoleErrors` is empty for both viewport runs
    - `roleRepeatsInNotes` is 0 (single-role member "Type") in the after-capture, vs. 12 (desktop)/5 (mobile) documented as the before-baseline
    - `releasesEmptyStateText` contains "Noch keine öffentlichen Release-Einträge." and does NOT contain "Alle 0 angezeigt"
    - `noteAccentColorSamples` shows a real catalog color (matching Typesetting's `#7B3C4E` for member "Type"'s notes), never neutral gray/transparent, proving P157-13's role-color-preservation criterion live
    - A before/after comparison table and an explicit remaining-deviations list (even if empty) are produced as this task's evidence
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && SHOT_LABEL=after node scripts/shot-projectmember.mjs"</automated>
  </verify>
  <done>Concrete before/after screenshots and structured facts exist proving the rebuilt page against the reference spec and the documented baseline, with any residual deviations explicitly listed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live UAT sign-off against the reference spec</name>
  <files></files>
  <action>Present Task 2's green test-suite results and Task 3's before/after screenshot evidence and comparison table, then walk through the checks below via the 127.0.0.1:3300 tunnel and wait for explicit human sign-off before this phase is considered complete.</action>
  <what-built>
    The full Phase 157 redesign: compact hero, single-card Statistikleiste, active-state tab nav,
    Beitragszusammenfassung band with the real `episodes` count, a compact notes timeline with no
    per-note role header, cleaned-up Media/Releases sections with a proper Releases empty state —
    verified via green frontend+backend test suites and live screenshots against
    `/fansubs/new-subs/fansubprojekt/buddy-complex/mitwirkende/type`.
  </what-built>
  <!-- Nachtrag GAP-03 F5 (2026-09-14, superseded): the "Statistikleiste (one card, 4 entries,
       correct order/labels)" and "Beitragszusammenfassung band" wording in step 2 below is
       superseded by the DECISIONS.md 2026-09-14 entry ("Phase 157 Statistikleiste and
       Beitragszusammenfassung superseded by Hero-inline metrics...") and the annotated ROADMAP.md
       P157-02/P157-04 rows -- both surfaces were replaced by clickable Hero-inline metrics
       (Plan 157-07, confirmed by V6/Plan 157-14). Do not verify against this stale wording; verify
       against the hero-metrics behavior instead. -->
  <how-to-verify>
    1. Open `http://127.0.0.1:3300/fansubs/new-subs/fansubprojekt/buddy-complex/mitwirkende/type`
       through the Windows SSH tunnel (never `:3000` directly — CORS will silently break sections).
    2. Compare the page against 157-CONTEXT.md's "Referenz-Spezifikation in Worten" section point by
       point: Hero (avatar left, buttons side by side), Statistikleiste (one card, 4 entries,
       correct order/labels), Tab row (active state visible), Beitragszusammenfassung band ("...für
       13 Folgen · 12 dokumentierte Arbeitsnotizen · 2 Medien"), Notes timeline (no big role header,
       no "Notiz zu Folge X", chevron navigates to the release), Media gallery (no "Alle 2
       angezeigt"), Releases empty state ("Noch keine öffentlichen Release-Einträge.", dashed
       border).
    2b. Per P157-13 (binding mid-planning addendum): confirm each note entry visibly carries its
       role's accent color (e.g. Typesetting notes show the mauve `#7B3C4E` accent as a left edge/
       stripe) — the page must NOT read as entirely neutral/white despite the reference screenshot
       itself showing neutral rows; that one aspect of the reference image is explicitly overridden
       by the addendum.
    3. Resize/inspect at 320, 390, 768, 1024, and 1440px — confirm no horizontal scrollbar at any width.
    4. Review the before/after comparison table and remaining-deviations list produced in Task 3.
  </how-to-verify>
  <resume-signal>Type "approved" or list the specific deviations to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live-UAT script -> running frontend/backend containers | Local, operator-controlled network only (127.0.0.1); no external exposure introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-11 | Spoofing | Reverse proxy on 127.0.0.1:3300 in shot-projectmember.mjs | accept | Proxy binds to loopback only, forwards headers unmodified (no header forgery), matches the pre-existing, already-committed script's design exactly — this plan only extends its fact-gathering, not its network behavior |
| T-157-12 | Information Disclosure | Screenshots/JSON facts written to /tmp inside the container | accept | Ephemeral container-local output, not committed to the repo, not served publicly |
</threat_model>

<verification>
`docker compose exec -T team4sv30-frontend sh -c "cd /app && npm run lint && npm run typecheck && npm test"`
`cd /home/d1sk/team4s/backend && go build ./... && go vet ./... && go test ./...`
`docker compose exec -T team4sv30-frontend sh -c "cd /app && SHOT_LABEL=after node scripts/shot-projectmember.mjs"`
Manual breakpoint check at 320/390/768/1024/1440px against the live page.
</verification>

<success_criteria>
Both full test suites are green; no horizontal scroll at any required breakpoint; live screenshots
and structured facts prove the rebuilt page against the written reference spec and the documented
before-baseline, with any deviations explicitly listed rather than silently accepted; human sign-off
recorded.
</success_criteria>

<output>
Create `.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-06-SUMMARY.md` when done
</output>
