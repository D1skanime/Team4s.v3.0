---
phase: 157-projekt-memberseite-visuelles-referenzdesign
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/EmptyState.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberReleasesSection.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberReleasesSection.module.css
  - frontend/src/components/fansubs/projectMember/ProjectMemberReleasesSection.test.tsx
autonomous: true
requirements: [P157-09, P157-07]

must_haves:
  truths:
    - "When a member has 0 public releases, the section shows one compact, dashed-border empty state reading 'Noch keine öffentlichen Release-Einträge.' — not the current literal 'Mitwirkung an Releases0Alle 0 angezeigt' duplication"
    - "When releases > 0, the section renders exactly as it does today (unchanged list + pager)"
    - "Once every release is loaded (no more pages), no 'Alle N angezeigt' text renders for releases with count > 0, matching the Media section's rule"
  artifacts:
    - path: "frontend/src/components/ui/EmptyState.tsx"
      provides: "Backward-compatible optional icon/className props, defaulting to today's Inbox icon and no extra class"
    - path: "frontend/src/components/fansubs/projectMember/ProjectMemberReleasesSection.tsx"
      provides: "count === 0 branch rendering the EmptyState primitive with a Package icon and the reference empty-state sentence"
  key_links:
    - from: "frontend/src/components/fansubs/projectMember/ProjectMemberReleasesSection.tsx"
      to: "frontend/src/components/ui/EmptyState.tsx"
      via: "global primitive reuse per CLAUDE.md Frontend-UI rule, instead of a bespoke empty-state box"
      pattern: "EmptyState"
---

<objective>
Fix the most visually broken part of today's page (Workstream H, P157-09): the 0-releases section
currently renders the literal, un-spaced text "Mitwirkung an Releases0Alle 0 angezeigt". Replace it
with a compact empty state, reusing the existing global `EmptyState` primitive per CLAUDE.md's
Frontend-UI rule rather than hand-rolling a duplicate box (per mandatory_run_constraints #5).

Purpose: this is the concrete complaint in the user's original order ("Kein 'Mitwirkung an Releases
0' plus 'Alle 0 angezeigt' als unnötig doppelte Information").
Output: `EmptyState` gains small, additive `icon`/`className` props (backward compatible for all 58
existing call sites); Releases section uses it for the 0-count case; "Alle N angezeigt" removal
applied consistently for the >0 case too, mirroring 157-04's Media fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-CONTEXT.md
@.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-PATTERNS.md

<interfaces>
<!-- frontend/src/components/ui/EmptyState.tsx, current (pre-change) -->
export interface EmptyStateProps { title: string; description?: string; action?: ReactNode; variant?: 'default' | 'withAction' | 'compact' | 'inline' }
export function EmptyState({ title, description, action, variant = 'default' }: EmptyStateProps) { /* uses a hardcoded Inbox icon at size 20 strokeWidth 2 */ }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive EmptyState props — icon + className</name>
  <files>frontend/src/components/ui/EmptyState.tsx</files>
  <read_first>
    frontend/src/components/ui/EmptyState.tsx (full file)
    frontend/src/components/ui/classNames.ts (the `classNames` helper already imported by EmptyState.tsx)
  </read_first>
  <action>
    Add two optional props to `EmptyStateProps`: `icon?: ReactNode` and `className?: string`. In the
    non-`inline` render branch, replace the hardcoded Inbox icon element with a fallback expression
    that renders the provided `icon` when present and the original Inbox icon otherwise, inside the
    existing `styles.stateIcon` wrapper. Pass `className` through as the last argument of the
    existing `classNames(styles.stateCard, styles.stateNeutral, variant === 'compact' &&
    styles.stateCompact, ...)` call. Do not change default behavior for any existing caller: when
    `icon`/`className` are omitted, output must be byte-identical to today.
  </action>
  <acceptance_criteria>
    - `EmptyStateProps` has `icon?: ReactNode` and `className?: string`
    - Calling `<EmptyState title="x" />` with no new props renders identically to before (Inbox icon, no extra class)
    - None of the existing 58 call sites across `frontend/src/app` pass `icon` or `className` today (confirmed via prior audit), so none are broken by the additive change
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i EmptyState.tsx || echo OK"</automated>
  </verify>
  <done>EmptyState supports an optional custom icon and className without changing any existing caller's output.</done>
</task>

<task type="auto">
  <name>Task 2: Releases 0-count empty state + pager "Alle N angezeigt" parity with Media</name>
  <files>frontend/src/components/fansubs/projectMember/ProjectMemberReleasesSection.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberReleasesSection.module.css</files>
  <read_first>
    frontend/src/components/fansubs/projectMember/ProjectMemberReleasesSection.tsx (full file)
    frontend/src/components/fansubs/projectMember/ProjectMemberReleasesSection.module.css (full file)
    frontend/src/components/ui/ui.module.css (the `.stateCard`/`.stateCompact`/`.stateIcon` rule block, for the dashed-border override target)
  </read_first>
  <action>
    Import `EmptyState` from `@/components/ui` and `Package` from `lucide-react`. At the top of the
    render, branch on `count === 0`: return the section wrapper (`<section id="releases" ...>` with
    its existing `pageStyles.sectionHead` header showing "Mitwirkung an Releases" + the count `0`)
    containing ONLY one `EmptyState` element with `variant="compact"`, a `Package` icon sized to
    match the primitive's default (20px, strokeWidth 2), `className={styles.releasesEmpty}`, and
    `title="Noch keine öffentlichen Release-Einträge."` — no `<ul>`, no pager, no `error`/`loading`
    text (fetching an empty collection is not an error state worth surfacing). Add
    `.releasesEmpty { border-style: dashed; border-color: var(--color-border); }` to
    `ProjectMemberReleasesSection.module.css` (own module, overriding only the border style of the
    shared `.stateCard`, using only the already-allowed `--color-border` token — no new tokens). For
    the `count > 0` branch (unchanged list+pager path), apply the same fix as 157-04 Task 1: only
    render the `pagerInfo` span when `canShowMore` is true, so "Alle N angezeigt" disappears here
    too once fully loaded, mirroring Media's rule exactly (mandatory_run_constraints #6's F/G/H
    parity).
  </action>
  <acceptance_criteria>
    - With `count={0}`, the section renders `EmptyState` with the exact text "Noch keine
      öffentlichen Release-Einträge." and no `<ul>`/pager markup
    - With `count > 0`, rendering is otherwise unchanged except "Alle N angezeigt" no longer
      appears once `!canShowMore`
    - No native `<div>`-only hand-rolled empty-state box was added (verified: `EmptyState` is the
      only empty-state element used)
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/fansubs/projectMember/ProjectMemberReleasesSection.test.tsx"</automated>
  </verify>
  <done>Releases section shows the compact reference empty state at 0, unchanged list/pager above 0, and drops "Alle N angezeigt" once fully loaded.</done>
</task>

<task type="auto">
  <name>Task 3: Adapt ProjectMemberReleasesSection.test.tsx for the new empty state and pager rule</name>
  <files>frontend/src/components/fansubs/projectMember/ProjectMemberReleasesSection.test.tsx</files>
  <read_first>
    frontend/src/components/fansubs/projectMember/ProjectMemberReleasesSection.test.tsx (full file — keep the existing `ProjectMemberReleaseCard`/`ProjectMemberHero` describe blocks and both `ProjectMemberReleasesSection` tests exactly as-is, they are unaffected by the count===0 branch)
  </read_first>
  <action>
    Add a new `describe('ProjectMemberReleasesSection empty state', ...)` block with a test that
    mocks `getProjectMemberReleases` to resolve an empty page (`items: [], next_cursor: null,
    has_more: false`), renders `ProjectMemberReleasesSection` with `count={0}`, and asserts
    `screen.getByText('Noch keine öffentlichen Release-Einträge.')` is present while
    `screen.queryByRole('listitem')` is `null`. Add a second test mirroring the existing
    "loads the initial page" test but with a small fixture (e.g. 3 releases, `has_more: false`) that
    asserts `screen.queryByText(/Alle \d+ angezeigt/)` is `null` once loaded, proving the F/G/H
    pager-text parity.
  </action>
  <acceptance_criteria>
    - All tests in the file pass, including the two new ones, using real RTL render + waitFor
      (Teststil-compliant, matching this file's existing pattern)
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/fansubs/projectMember/ProjectMemberReleasesSection.test.tsx"</automated>
  </verify>
  <done>Test suite proves both the 0-count empty state and the "Alle N angezeigt" removal for the loaded case.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Purely presentational change to an already-fetched, already-authorized public releases count |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-07 | (none applicable) | EmptyState icon/className props | accept | Purely additive optional props; no new data source, no new user input, no new unescaped rendering |
</threat_model>

<verification>
`docker compose exec -T team4sv30-frontend sh -c "cd /app && npm run typecheck"`
`docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/fansubs/projectMember/ProjectMemberReleasesSection.test.tsx"`
Manual grep across `frontend/src/app` confirming no pre-existing `<EmptyState icon=` or `<EmptyState className=` call site exists that this change could conflict with.
</verification>

<success_criteria>
0-releases renders the compact reference empty state via the global `EmptyState` primitive (extended,
not duplicated); the "Alle N angezeigt" redundancy is gone for Releases exactly as it is for Media;
all existing and new tests pass.
</success_criteria>

<output>
Create `.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-05-SUMMARY.md` when done
</output>
