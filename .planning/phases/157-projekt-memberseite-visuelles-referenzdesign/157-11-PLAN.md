---
phase: 157-projekt-memberseite-visuelles-referenzdesign
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/scripts/shot-projectmember.mjs
autonomous: true
requirements: [P157-12]

must_haves:
  truths:
    - "The fullPage screenshot of the project-member page shows fully-loaded content (notes/media rendered), never a 'Wird geladen' placeholder, because the script waits for the page to scroll through and settle before capturing"
    - "A 200%-zoom-equivalent capture of the project-member page exists and proves no horizontal scrollbar/overflow, closing UAT checklist point 9 which was previously unverified"
    - "All existing script mechanics (127.0.0.1:3300 reverse proxy, three-viewport loop, release-request/CORS guard, note-toggle proof, hero SHOT_VERIFY_HERO block) keep working unchanged"
  artifacts:
    - path: "frontend/scripts/shot-projectmember.mjs"
      provides: "a waitForSectionsSettled(page) helper used before every fullPage screenshot, and an always-on (non-env-gated) 200%-zoom overflow check appended to the desktop viewport iteration"
  key_links:
    - from: "frontend/scripts/shot-projectmember.mjs (main viewport loop)"
      to: "frontend/scripts/shot-projectmember.mjs (waitForSectionsSettled helper)"
      via: "await waitForSectionsSettled(page) called after 'document.fonts.ready' and before the fullPage page.screenshot(...) call, for every viewport"
      pattern: "waitForSectionsSettled"
---

<objective>
Fix the live-UAT screenshot script (`frontend/scripts/shot-projectmember.mjs`) so it is trustworthy
evidence for the three content plans (157-12/13/14) that depend on it: today it takes the fullPage
screenshot immediately after `waitUntil: 'networkidle'` plus waiting for the hero `<h1>` and
fonts-ready, which is before the client-side `useProjectMemberCollection` fetches for Texte & Notizen
and Bilder & Medien have necessarily painted, and before any below-the-fold lazy-loaded images have
loaded — producing a misleading "Wird geladen" capture in the worst case. This plan also closes UAT
checklist point 9 (Browser-Zoom), which `157-UAT.md` explicitly marks as still unverified, by adding a
200%-zoom-equivalent overflow check.

Purpose: every subsequent GAP-02 plan (157-12, 157-13, 157-14) is required to produce live screenshots
via this exact script as part of its own verification — if the script itself is unreliable, all
downstream visual evidence is unreliable too. Fixing it first, in its own plan, keeps the fix isolated
and reusable.
Output: an updated `shot-projectmember.mjs` that scrolls the page fully, returns to the top, and waits
for content/images to settle before every fullPage capture, plus one additional always-on 200%-zoom
overflow assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-UAT.md

<interfaces>
Current structure of frontend/scripts/shot-projectmember.mjs (395 lines) the executor must read in
full before editing. Key anchors by current line number (may drift a few lines after small edits,
re-read after each task):
- Line 65-69: const viewports = [['mobile',390,844], ['tablet',768,1024], ['desktop',1440,900]]
- Line 73: for (const [name, width, height] of viewports) {
- Line 87-90: const resp = await page.goto(BASE + PATH_UNDER_TEST, { waitUntil: 'networkidle', timeout: 90000 })
- Line 91: await page.locator('section[aria-label="Projekt-Mitwirkung"] h1').waitFor()
- Line 92: await page.evaluate(() => document.fonts.ready)
- Line 94-95: const file = `${OUT}/${LABEL}-${name}.png`; await page.screenshot({ path: file, fullPage: true })
- Line 102-103: viewport-only screenshot (fullPage: false)
- Line 105-270: const facts = await page.evaluate(() => { ... }) — do not touch
- Line 291-306: expandableNote / expandedNote toggle proof, still inside the same viewport iteration
- Line 307-375: normalConsoleErrors + `if (process.env.SHOT_VERIFY_HERO === '1') { ... }` block,
  including the existing 720x450 "200% zoom equivalent" resize technique around line 369
  (`await page.setViewportSize({ width: 720, height: 450 })`) — reuse this exact equivalence (1440px
  desktop at 200% browser zoom reflows identically to 720 CSS px width), do not invent a different
  ratio. That existing usage is hero-scoped and env-gated; this plan's new check must be page-wide and
  always on (no env gate).
- Line 390: await ctx.close() — end of each viewport iteration
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a settle-before-screenshot helper and wire it into every viewport capture</name>
  <files>frontend/scripts/shot-projectmember.mjs</files>
  <read_first>
    frontend/scripts/shot-projectmember.mjs (full file, 395 lines)
  </read_first>
  <action>
    Add a new async function waitForSectionsSettled(page) placed after the viewports const
    declaration and before the "const browser = await chromium.launch()" line. Its body, in order:
    (1) read docHeight via page.evaluate(() => document.documentElement.scrollHeight); (2) read the
    current viewport height via page.viewportSize()?.height ?? 800; (3) loop from y=0 to docHeight in
    steps of viewportHeight, calling page.evaluate((scrollY) => window.scrollTo(0, scrollY), y) then
    page.waitForTimeout(120) on each iteration, to progressively scroll through the whole document and
    trigger any viewport-relative lazy image loading; (4) after the loop, scroll once more to the true
    bottom (window.scrollTo(0, document.documentElement.scrollHeight)) and await
    page.waitForLoadState('networkidle'); (5) await page.evaluate() that builds
    Promise.all(Array.from(document.images).filter((img) => !img.complete).map((img) => new
    Promise((resolve) => { img.addEventListener('load', resolve, { once: true });
    img.addEventListener('error', resolve, { once: true }) }))), so any still-loading img element is
    waited on explicitly rather than assumed complete; (6) scroll back to the top via
    page.evaluate(() => window.scrollTo(0, 0)); (7) settle layout with page.evaluate(() => new
    Promise((resolve) => requestAnimationFrame(() => requestAnimationFrame(resolve)))). Then call
    "await waitForSectionsSettled(page)" inside the main viewport loop, immediately after the existing
    "await page.evaluate(() => document.fonts.ready)" line and before the "const file = ..." /
    "await page.screenshot(...)" fullPage capture — this is the one call site; do not add a second call
    before the viewport-only screenshot (that capture is diagnostic only, not the primary evidence
    artifact, and running the settle routine twice would just slow the run without adding proof value).
    Do not touch the facts evaluate() block, the note-toggle proof, the release-request/CORS guard, or
    the SHOT_VERIFY_HERO block.
  </action>
  <acceptance_criteria>
    - grep -c "async function waitForSectionsSettled" frontend/scripts/shot-projectmember.mjs returns 1
    - grep -c "await waitForSectionsSettled(page)" frontend/scripts/shot-projectmember.mjs returns exactly 1
    - The single call site is positioned between the document.fonts.ready line and the fullPage
      page.screenshot(...) line for the same viewport iteration (verify by reading the file after
      editing, not by grep line-number arithmetic alone)
    - A Node syntax check of the file (see verify command) reports no syntax errors
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && node --check scripts/shot-projectmember.mjs"</automated>
  </verify>
  <done>waitForSectionsSettled exists, is called exactly once per viewport iteration in the correct position, and the script still parses.</done>
</task>

<task type="auto">
  <name>Task 2: Add an always-on 200%-zoom overflow check and run the corrected script against the live stack</name>
  <files>frontend/scripts/shot-projectmember.mjs</files>
  <read_first>
    frontend/scripts/shot-projectmember.mjs (re-read after Task 1's edit — line numbers have shifted)
  </read_first>
  <action>
    Inside the main viewport loop, in the name === 'desktop' iteration only, immediately before the
    existing "await ctx.close()" line (i.e. after the note-toggle proof and after the
    SHOT_VERIFY_HERO-gated block, which stays untouched and still only runs when that env var is set),
    add an unconditional block (no env-var gate) that: (1) resizes the same page via
    page.setViewportSize({ width: 720, height: 450 }) — reusing the exact 200%-zoom-equivalent ratio
    already established at the existing hero-only usage further up in the file, not a new ratio; (2)
    calls waitForSectionsSettled(page) again at this new size so lazy content settles post-resize; (3)
    takes a full-page screenshot to a path built from OUT/LABEL with a "-desktop-zoom200" suffix
    (mirroring the existing file-naming convention used for `file`/`viewportOnlyFile`); (4) evaluates
    document.documentElement.scrollWidth > document.documentElement.clientWidth on the whole document
    (not hero-scoped, unlike the existing SHOT_VERIFY_HERO hero-only check) and, if true, throws an
    Error naming the viewport ("desktop-zoom200") and the measured scrollWidth/clientWidth values; (5)
    stores the boolean result on facts.zoom200Overflow for the printed JSON output regardless of
    pass/fail. This directly closes 157-UAT.md's previously-unverified checklist point 9. Leave the
    pre-existing SHOT_VERIFY_HERO-gated 720x450 hero-only check completely alone — it stays env-gated
    and hero-scoped; this new check is the general, always-on, whole-page counterpart requested by
    GAP-02's verification requirements.
  </action>
  <acceptance_criteria>
    - grep -c "zoom200Overflow" frontend/scripts/shot-projectmember.mjs returns at least 2 (the fact assignment and the throw condition)
    - grep -c "desktop-zoom200" frontend/scripts/shot-projectmember.mjs returns at least 1
    - The new block is unconditional (not wrapped in "if (process.env.SHOT_VERIFY_HERO === '1')") — verify by reading the surrounding braces after editing
    - Line count of the file is 450 or fewer; if the addition would exceed 450, extract
      waitForSectionsSettled (and, if still needed, the zoom-check block) into a new co-located module
      frontend/scripts/lib/shotHelpers.mjs and import them, rather than exceeding the cap
    - Running the script end-to-end against the live stack (see verify command) exits 0 for all three
      viewports plus the new desktop-zoom200 pass, with no thrown error, and produces a
      "*-desktop-zoom200.png" file in SHOT_OUT
  </acceptance_criteria>
  <verify>
    <automated>docker restart team4sv30-frontend; sleep 3; docker compose exec -T team4sv30-frontend sh -c "cd /app && node scripts/shot-projectmember.mjs" 2>&1 | tail -150</automated>
  </verify>
  <done>The corrected script runs to completion for all three viewports plus the new zoom-200 pass, exits 0, and its evidence (screenshots + JSON facts, including zoom200Overflow: false) is available for the plans that depend on this one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Dev-only Playwright tooling script, not shipped to production; it navigates a locally-running dev instance through a local reverse proxy already required by the existing script |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-11-01 | Tampering | Local 127.0.0.1:3300 reverse proxy in the script | accept | Unchanged from the existing script: bound to 127.0.0.1 only, forwards to the local dev server, never exposed beyond the host; this plan adds no new listener or network surface |
| T-157-11-02 | Information Disclosure | Screenshots/JSON facts written to /tmp/pmshots (or SHOT_OUT) | accept | Local filesystem artifacts only, identical risk profile to the pre-existing script; no new data is captured beyond computed layout/style facts already gathered by the untouched facts() block |
| T-157-11-SC | Tampering | npm/pip/cargo installs | accept | No new package is installed by this plan (no package.json change); the script continues to use the already-installed playwright and axe-core dependencies |
</threat_model>

<verification>
1. `docker compose exec -T team4sv30-frontend sh -c "cd /app && node --check scripts/shot-projectmember.mjs"` — passes with no syntax errors.
2. `docker restart team4sv30-frontend` (always before a screenshot run, per project convention), then
   `docker compose exec -T team4sv30-frontend sh -c "cd /app && node scripts/shot-projectmember.mjs"` —
   runs to completion for mobile/tablet/desktop plus the new desktop-zoom200 pass, exit code 0, no
   thrown error, JSON facts include `zoom200Overflow: false`.
3. Manually inspect at least one of the produced fullPage screenshots (e.g. the desktop capture) and
   confirm it shows populated "Texte & Notizen"/"Bilder & Medien" content, not a "Wird geladen"
   placeholder — this is the concrete proof the fix worked, not just the absence of a thrown error.
4. This plan makes no functional/visual change to the application itself — there is no human Live-UAT
   sign-off implication from this plan alone. It exists solely so plans 157-12/13/14 can produce
   trustworthy evidence; those plans' own human Live-UAT sign-off remains separately pending.
</verification>

<success_criteria>
The screenshot script reliably captures fully-settled page content before every fullPage screenshot
(scroll-through-and-back-to-top-and-wait, not an immediate capture), and produces a 200%-zoom-equivalent
overflow check that closes 157-UAT.md's previously-unverified checklist point 9. All pre-existing script
mechanics (proxy, three viewports, CORS guard, note-toggle proof, SHOT_VERIFY_HERO block) are unchanged
and still pass. The file stays at or below 450 lines (or the helper is extracted to a co-located module
if it would not).
</success_criteria>

<output>
Create `.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-11-SUMMARY.md` when done
</output>
