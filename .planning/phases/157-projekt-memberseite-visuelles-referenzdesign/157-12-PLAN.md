---
phase: 157-projekt-memberseite-visuelles-referenzdesign
plan: 12
type: execute
wave: 2
depends_on: ["157-11"]
files_modified:
  - frontend/src/app/anime/[id]/group/[groupId]/sections/TeamSection.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/sections/TeamSection.test.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.module.css
  - frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.test.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.module.css
  - frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.test.tsx
  - frontend/scripts/shot-projectmember.mjs
autonomous: true
requirements: [P157-11, P157-07, P157-08]

must_haves:
  truths:
    - "The project page's 'Mitwirkende am Fansub-Projekt' header carries the same global wine-red underline as every other underlined SectionHeader on that page (V1)"
    - "The project-member page's 'Texte & Notizen' and 'Bilder & Medien' headers render through the global SectionHeader primitive with underline, not a locally hand-rolled h2 (V2)"
    - "Once every text contribution is already visible, the notes pager shows no 'Alle N angezeigt' text — it renders nothing, matching the already-correct Bilder & Medien behavior (V5)"
    - "'X von Y angezeigt' keeps rendering in the notes pager for as long as more contributions are actually loadable"
  artifacts:
    - path: "frontend/src/app/anime/[id]/group/[groupId]/sections/TeamSection.tsx"
      provides: "SectionHeader title='Mitwirkende am Fansub-Projekt' underline"
    - path: "frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.tsx"
      provides: "global SectionHeader (underline, actions=count badge) replacing the local h2, plus a pager that renders no text once canShowMore is false"
    - path: "frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.tsx"
      provides: "global SectionHeader (underline, actions=count badge) replacing the local h2"
  key_links:
    - from: "frontend/src/app/anime/[id]/group/[groupId]/sections/TeamSection.tsx"
      to: "frontend/src/components/ui/SectionHeader.tsx (underline -> ui.module.css .sectionHeaderUnderline)"
      via: "underline boolean prop"
      pattern: "underline"
    - from: "frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.tsx"
      to: "frontend/src/components/ui/SectionHeader.tsx"
      via: "import { SectionHeader } from '@/components/ui'; <SectionHeader title=\"Texte & Notizen\" underline actions={...} />"
      pattern: "SectionHeader"
---

<objective>
Close V1, V2 and V5 from `157-UAT.md`'s GAP-02 (Live-UAT-Befunde 2026-09-14): (V1) the project page's
"Mitwirkende am Fansub-Projekt" header is missing the global wine-red underline every sibling section
header already carries; (V2) the project-member page's "Texte & Notizen" and "Bilder & Medien" headers
each render a locally hand-built `<h2>` instead of the global `SectionHeader` primitive, so they never
get the underline either; (V5) the notes pager still shows a redundant "Alle N angezeigt" once
everything is already visible, unlike the already-correct Bilder & Medien pager which renders nothing
in that case.

Purpose: restore visual consistency with the rest of the site's already-established SectionHeader
convention (used on "Profil und Mitgliedschaft", "Beiträge", "Alle Releases", "An diesem Release
beteiligt", "Stimmen aus dem Team", "Karas", and the sibling `FansubTeamSection.tsx`), and remove
information-free pager text, per the operator's live-UAT findings.
Output: three header call sites converted or fixed to the global underline pattern, one pager text
fix, adapted/extended tests, and live-UAT evidence via the (now-corrected, per 157-11) screenshot
script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-UAT.md
@.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-PATTERNS.md

<interfaces>
From frontend/src/components/ui/SectionHeader.tsx (do not modify this file in this plan):
export interface SectionHeaderProps {
  eyebrow?: string
  title: string
  level?: 2 | 3
  description?: string
  actions?: ReactNode
  underline?: boolean
}
export function SectionHeader({ eyebrow, title, level, description, actions, underline }: SectionHeaderProps)
Renders <div class="sectionHeader[ sectionHeaderUnderline]"><div class="sectionHeaderContent">
[eyebrow][<Heading class="sectionTitle">title</Heading>][description]</div>[<div
class="sectionHeaderActions">actions</div>]</div>. There is no `id`/`className` prop on SectionHeader
— do not add one in this plan; use `aria-label` on the wrapping <section>, not `aria-labelledby`
pointed at a child id, to avoid needing to extend the primitive (see Task 2 action).

Existing, already-compliant sibling usage to mirror exactly, do not deviate from this shape:
frontend/src/components/fansubs/FansubTeamSection.tsx:39 — `<SectionHeader title="Team & Mitglieder"
underline />` inside `<section id="team" ...>` (no eyebrow/description/actions needed there).

Current TeamSection.tsx (frontend/src/app/anime/[id]/group/[groupId]/sections/TeamSection.tsx, full
file, 30 lines):
  <div id="team" className={styles.teamSection}>
    <SectionHeader title="Mitwirkende am Fansub-Projekt" />
    ...
  </div>

Current ProjectMemberNotesSection.tsx header block (lines 52-64) and pager block (lines 79-102, full
file 105 lines) — read the full current file before editing, both blocks change in this plan:
  <section id="texte" className={pageStyles.section} aria-labelledby="pm-texte-title">
    <div className={pageStyles.sectionHead}>
      <span className={styles.titleGroup}>
        <FileText size={18} aria-hidden="true" />
        <h2 id="pm-texte-title" className={pageStyles.sectionTitle}>Texte &amp; Notizen</h2>
      </span>
      <span className={pageStyles.sectionCount}>{count}</span>
    </div>
    ...
    <span className={pageStyles.pagerInfo}>
      {canShowMore ? `${shown.length} von ${count} angezeigt` : `Alle ${count} angezeigt`}
    </span>

Current ProjectMemberMediaGallery.tsx header block (lines 69-79, full file 117 lines) — its pager
(lines 87-92) is ALREADY correct (only renders text when canShowMore), used here as the reference
pattern for the Notes pager fix:
  <section id="bilder" className={pageStyles.section} aria-labelledby="pm-bilder-title">
    <div className={pageStyles.sectionHead}>
      <span className={styles.titleGroup}>
        <ImageIcon size={18} aria-hidden="true" />
        <h2 id="pm-bilder-title" className={pageStyles.sectionTitle}>Bilder &amp; Medien</h2>
      </span>
      <span className={pageStyles.sectionCount}>{count}</span>
    </div>
    ...
    {canShowMore ? (
      <span className={pageStyles.pagerInfo}>{`${shown.length} von ${count} angezeigt`}</span>
    ) : null}

Current ProjectMemberNotesSection.module.css / ProjectMemberMediaGallery.module.css both define an
identical local rule to be removed and replaced in this plan:
  .titleGroup { display: flex; align-items: center; gap: 8px; color: var(--text-primary); }

No test in this repo currently asserts on `id="pm-texte-title"`, `id="pm-bilder-title"`, or
`aria-labelledby` for these two sections (confirmed by search) — safe to drop in favor of a static
`aria-label` on the `<section>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write/extend failing tests for the underline, SectionHeader conversion, and pager fix</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/sections/TeamSection.test.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.test.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.test.tsx</files>
  <read_first>
    frontend/src/app/anime/[id]/group/[groupId]/sections/TeamSection.test.tsx (full file)
    frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.test.tsx (full file, note the existing "loads the initial page and appends more without duplicates" test at the bottom that already renders ProjectMemberNotesSection — reuse its render shape for the new pager test)
    frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.test.tsx (full file, especially the existing "renders no 'Alle N angezeigt' pager text once every medium is loaded" test at the bottom — mirror this exact shape for the new Notes-section pager test)
  </read_first>
  <behavior>
    - TeamSection: rendered output (renderToStaticMarkup) contains the literal class-name substring
      "sectionHeaderUnderline" (proves the underline prop reached the primitive).
    - ProjectMemberNotesSection: a render with jsdom exposes a `[class*="sectionHeaderUnderline"]`
      element inside the "texte" section, and the visible heading text is still exactly
      "Texte & Notizen" (queryable via `screen.getByRole('heading', { name: 'Texte & Notizen' })`).
    - ProjectMemberMediaGallery: same shape, heading text "Bilder & Medien".
    - ProjectMemberNotesSection pager: once all items are loaded (`has_more: false`, `shown.length ===
      count`), `screen.queryByText(/Alle \d+ angezeigt/)` is null — mirroring the existing Media test
      named "renders no 'Alle N angezeigt' pager text once every medium is loaded" almost verbatim,
      adapted to `getProjectMemberNotes`/`ProjectMemberNotesSection`.
  </behavior>
  <action>
    In TeamSection.test.tsx, add a new `it(...)` inside the existing `describe('TeamSection', ...)`
    block: `'renders the global underline on the section header (V1, 157-UAT GAP-02)'`, rendering the
    same `TeamSection` fixture already used by the first test, and asserting
    `html.includes('sectionHeaderUnderline')` is true. In ProjectMemberNotesSection.test.tsx, add a new
    `it(...)` inside the existing `describe('ProjectMemberNotesSection', ...)` block (the second
    describe in the file, not the `ProjectMemberNoteEntry` one): `'renders the section header through
    the global SectionHeader primitive with underline (V2, 157-UAT GAP-02)'`, rendering
    `ProjectMemberNotesSection` with a single resolved page (mock `getProjectMemberNotes` to resolve
    one item, `has_more: false`), awaiting the heading via
    `screen.findByRole('heading', { name: 'Texte & Notizen' })`, then asserting
    `container.querySelector('[class*="sectionHeaderUnderline"]')` is not null. In the same describe
    block, add a second new `it(...)`: `'renders no redundant "Alle N angezeigt" pager text once every
    contribution is loaded (V5, 157-UAT GAP-02)'`, following the exact structure of
    ProjectMemberMediaGallery.test.tsx's existing "renders no 'Alle N angezeigt' pager text once every
    medium is loaded" test (3 items, `has_more: false`, `count={3}`, wait for the items to render via
    `screen.getAllByRole('link')` reaching length 3, then assert `screen.queryByText(/Alle \d+
    angezeigt/)` is null). In ProjectMemberMediaGallery.test.tsx, add one new `it(...)` inside the
    existing `describe('ProjectMemberMediaGallery', ...)` block: `'renders the section header through
    the global SectionHeader primitive with underline (V2, 157-UAT GAP-02)'`, mirroring the Notes-side
    test above (resolve one media item, await `screen.findByRole('heading', { name: 'Bilder & Medien'
    })`, assert the `sectionHeaderUnderline` class-substring element exists). Run all three test files
    now and confirm the four new tests fail (SectionHeader/underline not yet wired, pager still shows
    "Alle N angezeigt") before starting Task 2 — do not proceed to implementation until you have
    observed the RED state.
  </action>
  <acceptance_criteria>
    - The four new tests exist, are named as specified, and fail when run against the current (pre-Task-2) code
    - No existing `it(...)` block in any of the three files is deleted or renamed
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run 'src/app/anime/[id]/group/[groupId]/sections/TeamSection.test.tsx' src/components/fansubs/projectMember/ProjectMemberNotesSection.test.tsx src/components/fansubs/projectMember/ProjectMemberMediaGallery.test.tsx" 2>&1 | tail -80</automated>
  </verify>
  <done>Four new tests exist and are observed failing (RED) before any implementation change.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the underline fix, SectionHeader conversion, and pager fix (GREEN)</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/sections/TeamSection.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.module.css, frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.module.css</files>
  <read_first>
    frontend/src/app/anime/[id]/group/[groupId]/sections/TeamSection.tsx (full file)
    frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.tsx (full file)
    frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.module.css (full file)
    frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.tsx (full file)
    frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.module.css (full file)
    frontend/src/components/fansubs/FansubTeamSection.tsx:39 (the exact underline call-site pattern to mirror for V1)
  </read_first>
  <action>
    V1 — TeamSection.tsx: change `<SectionHeader title="Mitwirkende am Fansub-Projekt" />` to
    `<SectionHeader title="Mitwirkende am Fansub-Projekt" underline />`. No other change to this file.

    V2 — ProjectMemberNotesSection.tsx: import `SectionHeader` alongside the existing `Button` import
    from `@/components/ui`. Replace the whole header block (the `<div className={pageStyles.sectionHead}>`
    through its closing `</div>`, containing the icon span, the local `<h2 id="pm-texte-title">`, and
    the count span) with: an outer `<div className={styles.sectionHeadRow}>` containing (a) the existing
    `<FileText size={18} aria-hidden="true" />` icon but with `className={styles.sectionHeadIcon}`
    added, and (b) a `<div className={styles.sectionHeadRowContent}>` wrapping a `<SectionHeader
    title="Texte & Notizen" underline actions={<span className={pageStyles.sectionCount}>{count}</span>}
    />`. Change the `<section id="texte" ... aria-labelledby="pm-texte-title">` opening tag to
    `<section id="texte" className={pageStyles.section} aria-label="Texte & Notizen">` (drop
    `aria-labelledby` and its now-removed target id entirely — do not add an `id` prop to
    `SectionHeader`, per the interfaces note above). Then fix the pager (V5): change the
    unconditionally-rendered `<span className={pageStyles.pagerInfo}>{canShowMore ? ... : \`Alle
    ${count} angezeigt\`}</span>` to the same conditional-render-or-nothing pattern already used by
    `ProjectMemberMediaGallery.tsx`'s pager: `{canShowMore ? (<span
    className={pageStyles.pagerInfo}>{\`${shown.length} von ${count} angezeigt\`}</span>) : null}`.

    ProjectMemberNotesSection.module.css: remove the `.titleGroup` rule entirely (no longer used in
    this file). Add three new rules: `.sectionHeadRow { display: flex; align-items: flex-start; gap:
    8px; }`, `.sectionHeadIcon { flex-shrink: 0; margin-top: 2px; color: var(--text-primary); }`,
    `.sectionHeadRowContent { flex: 1; min-width: 0; }`.

    V2 (Media) — ProjectMemberMediaGallery.tsx: apply the identical transformation: import
    `SectionHeader` alongside `Button`; replace the header block with `<div
    className={styles.sectionHeadRow}><ImageIcon size={18} aria-hidden="true"
    className={styles.sectionHeadIcon} /><div className={styles.sectionHeadRowContent}><SectionHeader
    title="Bilder & Medien" underline actions={<span
    className={pageStyles.sectionCount}>{count}</span>} /></div></div>`; change the `<section id="bilder"
    ... aria-labelledby="pm-bilder-title">` opening tag to `aria-label="Bilder & Medien"` (drop
    `aria-labelledby`). Its pager is already correct per V5 — do not touch it.

    ProjectMemberMediaGallery.module.css: remove `.titleGroup`, add the same three new rules
    (`.sectionHeadRow`/`.sectionHeadIcon`/`.sectionHeadRowContent`) with identical values as above.
  </action>
  <acceptance_criteria>
    - grep -c "underline" frontend/src/app/anime/[id]/group/[groupId]/sections/TeamSection.tsx returns 1
    - grep -c "SectionHeader" frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.tsx returns at least 2 (import + usage)
    - grep -c "SectionHeader" frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.tsx returns at least 2
    - grep -c "aria-labelledby" frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.tsx frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.tsx both return 0
    - grep -c "titleGroup" frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.module.css frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.module.css both return 0
    - grep -c "Alle \${count} angezeigt" frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.tsx returns 0
    - All four tests written in Task 1 now pass (GREEN)
    - `wc -l` on every modified file stays at or below 450
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit && npx vitest run 'src/app/anime/[id]/group/[groupId]/sections/TeamSection.test.tsx' src/components/fansubs/projectMember/ProjectMemberNotesSection.test.tsx src/components/fansubs/projectMember/ProjectMemberMediaGallery.test.tsx" 2>&1 | tail -120</automated>
  </verify>
  <done>All four Task-1 tests pass; TeamSection, ProjectMemberNotesSection, and ProjectMemberMediaGallery all use the global underlined SectionHeader; the notes pager no longer shows "Alle N angezeigt".</done>
</task>

<task type="auto">
  <name>Task 3: Full regression run and live-UAT evidence via the corrected screenshot script</name>
  <files>frontend/scripts/shot-projectmember.mjs</files>
  <read_first>
    frontend/scripts/shot-projectmember.mjs (as corrected by plan 157-11 — re-read current state, do not assume pre-157-11 line numbers)
  </read_first>
  <action>
    In the `page.evaluate(...)` facts block, add one new small diagnostic (do not restructure the
    existing block): a `sectionHeaderUnderlines` fact computed as an object mapping the three known
    header texts to a boolean of whether their nearest `[class*="sectionHeaderUnderline"]` ancestor
    exists — concretely, for each of `'Texte & Notizen'` and `'Bilder & Medien'`, find the heading via
    `Array.from(document.querySelectorAll('h2')).find((h) => h.textContent.trim() === label)`, then
    check `Boolean(heading?.closest('[class*="sectionHeaderUnderline"]'))`. Add a throwing check
    immediately after computing `facts` (same placement style as the existing `notePreviews` /
    `noteBorderUniformity` checks): if either boolean is false, throw an `Error` naming which header is
    missing its underline. Do not add a diagnostic for the project PAGE's "Mitwirkende am
    Fansub-Projekt" header — that page is not the one this script navigates to (it targets the
    project-MEMBER page only); V1's evidence for that separate page is the TeamSection unit test from
    Task 1 plus a manual screenshot noted in this plan's verification section, not this script.
  </action>
  <acceptance_criteria>
    - grep -c "sectionHeaderUnderlines" frontend/scripts/shot-projectmember.mjs returns at least 2
    - The script's line count stays at or below 450 after this addition
    - Running the script against the live stack (see verify command) exits 0 for all three viewports and the desktop-zoom200 pass, with `sectionHeaderUnderlines` showing both entries true
  </acceptance_criteria>
  <verify>
    <automated>docker restart team4sv30-frontend; sleep 3; docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit && npx eslint src/app/anime/[id]/group/[groupId]/sections/TeamSection.tsx src/components/fansubs/projectMember/ProjectMemberNotesSection.tsx src/components/fansubs/projectMember/ProjectMemberMediaGallery.tsx && npx vitest run" 2>&1 | tail -150</automated>
  </verify>
  <done>Full frontend regression suite passes with no new failures, and the live screenshot script confirms both project-member headers now render with the underline, using the settle-aware capture from plan 157-11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Purely presentational change to already-fetched, already-authorized public section headers and pager text; no new trust boundary is crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-12-01 | Information Disclosure | Notes pager text | accept | Removing "Alle N angezeigt" text removes information, not adds it; no new data is exposed |
| T-157-12-02 | Tampering | SectionHeader `actions` slot receiving a count span | accept | The count value already flowed to the page as `props.count`, sourced from the existing public summary endpoint; only its DOM location changes, not its trust origin |
| T-157-12-SC | Tampering | npm/pip/cargo installs | accept | No new package is installed by this plan (no package.json change) |
</threat_model>

<verification>
1. `docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit"` — passes with no type errors.
2. `docker compose exec -T team4sv30-frontend sh -c "cd /app && npx eslint src/app/anime/[id]/group/[groupId]/sections/TeamSection.tsx src/components/fansubs/projectMember/ProjectMemberNotesSection.tsx src/components/fansubs/projectMember/ProjectMemberMediaGallery.tsx"` — passes with no errors.
3. `docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run"` — full suite green, no new failures versus the documented 157-10 baseline.
4. `docker restart team4sv30-frontend`, then `docker compose exec -T team4sv30-frontend sh -c "cd /app && node scripts/shot-projectmember.mjs"` — exits 0, `sectionHeaderUnderlines` fact shows both project-member headers true, fullPage screenshots (mobile/tablet/desktop) show populated, settled content per plan 157-11's fix.
5. Manual, separate check for V1 (project page, not project-member page — outside this script's
   target path): open `http://127.0.0.1:3300/anime/{id}/group/{groupId}` through the Windows SSH
   tunnel and visually confirm "Mitwirkende am Fansub-Projekt" now carries the same wine-red underline
   as "Neuestes Release" above it.
6. Human Live-UAT sign-off remains a separate, subsequent step. This plan's own passing verification
   does NOT mark GAP-02 or Phase 157 as accepted — the operator re-runs live UAT after all GAP-02
   plans (157-11 through 157-14) are complete.
</verification>

<success_criteria>
TeamSection's "Mitwirkende am Fansub-Projekt" header, and both ProjectMemberNotesSection's "Texte &
Notizen" and ProjectMemberMediaGallery's "Bilder & Medien" headers, all render through the global
SectionHeader primitive with `underline`, matching the already-established site-wide convention. The
notes pager renders no text once everything is loaded, matching the already-correct media pager. All
new and pre-existing tests pass; the live screenshot script confirms both project-member headers carry
the underline in a real browser.
</success_criteria>

<output>
Create `.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-12-SUMMARY.md` when done
</output>
