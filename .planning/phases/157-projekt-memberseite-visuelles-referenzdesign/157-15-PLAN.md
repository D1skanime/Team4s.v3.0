---
phase: 157-projekt-memberseite-visuelles-referenzdesign
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/SectionHeader.tsx
  - frontend/src/components/ui/SectionHeader.test.tsx
  - frontend/src/components/ui/ui.module.css
  - frontend/src/components/ui/HeroMetrics.test.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.module.css
  - frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.test.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.module.css
  - frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.test.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberNoteEntry.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberNoteEntry.module.css
  - frontend/scripts/shot-projectmember.mjs
  - frontend/scripts/lib/shotHelpers.mjs
autonomous: true
requirements: [P157-05, P157-08, P157-11, P157-12]

must_haves:
  truths:
    - "Section header underline under 'Texte & Notizen' and 'Bilder & Medien' spans the full content width, matching the unchanged 'Karas' header on the Release page"
    - "The count next to those section titles stays on the same line as the title on mobile, tablet, desktop, and at 200% zoom -- it never wraps onto its own line"
    - "Hero metrics '12 Beiträge' / '2 Medien' show a permanent, hover-independent visual affordance that they are clickable, plus a visible keyboard focus ring; '13 Folgen' stays plain non-interactive text"
    - "A fully clipped link inside a collapsed note preview cannot be reached by keyboard Tab; the same link becomes reachable again once that note is expanded"
    - "Note-entry card click, toggle expand/collapse, and in-content links behave identically to before the isolation change (isolation is invisible functionally, only scopes z-index)"
    - "Every other existing SectionHeader consumer (profile sections, 'Alle Releases', 'An diesem Release beteiligt', 'Stimmen aus dem Team', 'Karas', 'Mitwirkende am Fansub-Projekt') renders byte-identical output, since none of them pass the new icon/counter props"
  artifacts:
    - path: "frontend/src/components/ui/SectionHeader.tsx"
      provides: "Optional icon/counter slots rendered inside the header's own title row, additive and backward-compatible"
      min_lines: 30
    - path: "frontend/src/components/ui/ui.module.css"
      provides: ".sectionHeaderTitleRow/.sectionHeaderIcon/.sectionHeaderCounter layout rules, a permanent (non-hover-only) .buttonText underline, and a .buttonText:focus-visible ring"
      contains: "sectionHeaderTitleRow"
    - path: "frontend/src/components/fansubs/projectMember/ProjectMemberNoteEntry.module.css"
      provides: "isolation: isolate scoping the card's stacking context"
      contains: "isolation: isolate"
    - path: "frontend/src/components/fansubs/projectMember/ProjectMemberNoteEntry.tsx"
      provides: "tabIndex containment toggling clamped-preview links out of (and back into) the tab order"
      contains: "isExpanded"
  key_links:
    - from: "frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.tsx"
      to: "frontend/src/components/ui/SectionHeader.tsx"
      via: "icon={<FileText .../>} counter={<span>{count}</span>}"
      pattern: "icon=\\{"
    - from: "frontend/scripts/shot-projectmember.mjs"
      to: "full-width underline + same-row counter + permanent hero-metric affordance"
      via: "DOM geometry + computed-style assertions per viewport, throwing on regression"
      pattern: "sectionHeaderTitleRow|buttonText"
---

<objective>
Close GAP-03 findings F1-F4 from `157-UAT.md` (independent post-GAP-02 check, 2026-09-14): the
section-header underline/counter regression left over from GAP-02's V2 fix (F1), the
not-recognizable-as-clickable hero metrics introduced by Plan 157-14 (F2), and two Review findings
against `ProjectMemberNoteEntry` from `157-REVIEW.md` -- WR-03's Tab-focusable clipped links (F3)
and WR-04's uncontained stacking context (F4).

Purpose: The Project Member page's section headers and hero metrics currently regress the visual
and keyboard-accessibility bar the rest of Team4s's design system holds everywhere else, and the
notes timeline has two small but real accessibility/CSS-hygiene gaps flagged by independent review.
None of this touches GAP-01/GAP-02's already-closed structure (article + stretched-link overlay,
timeline dot/line color) or any other phase's code.
Output: An additive `icon`/`counter` extension to the global `SectionHeader` primitive (used by
~70 existing call sites, all of which stay byte-identical), a permanent hover-independent
`.buttonText` affordance with a visible focus ring, contained tab order for clipped note links, and
an isolated stacking context on `.entry` -- all proven by unit tests plus an extended
`shot-projectmember.mjs` screenshot run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-CONTEXT.md
@.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-UAT.md
@.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-REVIEW.md

<scope_fence>
This plan closes ONLY GAP-03 findings F1, F2, F3, F4 (see `157-UAT.md`'s "## GAP-03 —
Nachkontrolle nach GAP-02" heading). GAP-01 and GAP-02 are already closed (157-10 through 157-14)
-- do NOT re-touch the `article`+stretched-link structure, the per-entry dot/line color derivation,
or the pager-text/tab-card removal already delivered. F5 (docs + orphan cleanup) is a separate
plan, `157-16-PLAN.md` -- do not do any of that work here; the two plans touch disjoint files and
run in the same wave.
</scope_fence>

<interfaces>
From `frontend/src/components/ui/SectionHeader.tsx` (current, before this plan):
```
export interface SectionHeaderProps {
  eyebrow?: string
  title: string
  level?: 2 | 3
  description?: string
  actions?: ReactNode
  underline?: boolean
}
export function SectionHeader({ eyebrow, title, level = 2, description, actions, underline }: SectionHeaderProps)
```
Renders `<div class="sectionHeader[ sectionHeaderUnderline]"><div class="sectionHeaderContent">...<Heading class="sectionTitle">{title}</Heading>...</div>{actions && <div class="sectionHeaderActions">{actions}</div>}</div>`.

From `frontend/src/components/ui/Button.tsx` (unchanged by this plan, for reference):
`variant="text"` applies `styles.button` + `styles.buttonText` together (never `buttonText` alone)
-- so `.button:focus-visible`'s existing box-shadow rule already competes with any `.buttonText`
rule at equal specificity; source order in `ui.module.css` decides the winner, which is why the new
`.buttonText:focus-visible` rule must be added, not assumed to be redundant.

From `frontend/src/hooks/useClampedOverflow.ts` (shared, do NOT modify -- also used elsewhere):
`export function useClampedOverflow(content: string): { contentRef: RefObject<HTMLDivElement>, isExpanded: boolean, setIsExpanded: Dispatch<SetStateAction<boolean>>, isOverflowing: boolean }`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend global SectionHeader with optional icon/counter slots (F1 contract)</name>
  <files>frontend/src/components/ui/SectionHeader.tsx, frontend/src/components/ui/SectionHeader.test.tsx, frontend/src/components/ui/ui.module.css</files>
  <read_first>
    frontend/src/components/ui/SectionHeader.tsx (current, 36 lines, full file)
    frontend/src/components/ui/SectionHeader.test.tsx (current 3 tests, full file)
    frontend/src/components/ui/ui.module.css lines 1011-1105 (.sectionHeader/.sectionHeaderUnderline/.sectionHeaderContent/.sectionHeaderActions/.sectionTitle rules)
    frontend/src/components/ui/ui.module.css lines 1923-1965 (the `@media (max-width: 767px)` block that switches `.sectionHeader` to `flex-direction: column` -- confirm this task does NOT need to touch it)
  </read_first>
  <behavior>
    - Test: `<SectionHeader title="X" />` and `<SectionHeader title="X" underline actions={<span>1</span>} />` (no `icon`/`counter` passed) render the exact same DOM shape as today -- the title `Heading` sits directly inside `.sectionHeaderContent` with no new wrapping element, and no element with a class matching `sectionHeaderTitleRow`/`sectionHeaderIcon`/`sectionHeaderCounter` exists anywhere in the output.
    - Test: `<SectionHeader title="Texte & Notizen" underline icon={<svg data-testid="icon" />} />` renders the icon immediately before the heading, both inside one `sectionHeaderTitleRow` element, and the OUTER `.sectionHeader`/`.sectionHeaderUnderline` classes remain on the same top-level element as before (proving the underline border now spans the full header width, since no external sibling row exists anymore).
    - Test: `<SectionHeader title="X" underline counter={<span data-testid="counter">12</span>} />` renders the counter immediately after the heading inside the SAME `sectionHeaderTitleRow` element -- not inside `.sectionHeaderActions`.
    - All 3 pre-existing tests (`renders an h2 by default...`, `renders the same global header styling...`, `keeps the underline color owned by...`) must still pass completely unmodified.
  </behavior>
  <action>
    Add `icon?: ReactNode` and `counter?: ReactNode` to `SectionHeaderProps`. In the component body,
    branch on `Boolean(icon || counter)`: when false, render the title exactly as today (the
    unchanged `<Heading className={styles.sectionTitle}>{title}</Heading>` line, verbatim, as the
    else-branch) so every existing call site's output is byte-identical; when true, wrap
    `icon` (if present, as a plain child, not forced `aria-hidden` -- callers already set that
    themselves the way `ProjectMemberNotesSection.tsx`/`ProjectMemberMediaGallery.tsx` currently
    do), the `Heading`, and `counter` (if present) inside a new
    `<div className={styles.sectionHeaderTitleRow}>`. Do not touch `eyebrow`/`description`
    rendering or the `actions` slot -- both keep working exactly as before, and `actions` remains
    available for consumers that still want a wrapping-allowed trailing slot. In `ui.module.css`,
    add three new rules directly after the existing `.sectionTitle` rule (~line 1085):
    `.sectionHeaderTitleRow { display: flex; align-items: center; gap: 8px; }`,
    `.sectionHeaderIcon { flex-shrink: 0; display: inline-flex; color: var(--text-primary); }`,
    `.sectionHeaderCounter { flex-shrink: 0; }`. Do not modify the `@media (max-width: 767px)`
    `.sectionHeader` column-wrap rule (~line 1953) -- the new title-row slot is deliberately outside
    that wrap path, which is the fix itself.
  </action>
  <acceptance_criteria>
    - `grep -c "icon?:" frontend/src/components/ui/SectionHeader.tsx` and
      `grep -c "counter?:" frontend/src/components/ui/SectionHeader.tsx` each report at least 1.
    - `grep -rc "<SectionHeader" frontend/src --include=*.tsx | awk -F: '{sum+=$2} END{print sum}'`
      matches the pre-change call-site count (no consumer file touched in this task).
    - All 3 pre-existing SectionHeader.test.tsx assertions plus the 3 new behavior tests above pass.
    - `docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/ui/SectionHeader.test.tsx"` is green.
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/ui/SectionHeader.test.tsx"</automated>
  </verify>
  <done>SectionHeader exports additive `icon`/`counter` props; every existing and new SectionHeader.test.tsx assertion passes; no consumer file outside SectionHeader.tsx/.test.tsx/ui.module.css is touched by this task.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire icon/counter into Texte & Notizen / Bilder & Medien headers, extend screenshot proof (F1 consumers)</name>
  <files>frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.module.css, frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.test.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.module.css, frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.test.tsx, frontend/scripts/shot-projectmember.mjs, frontend/scripts/lib/shotHelpers.mjs</files>
  <read_first>
    frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.tsx (current header markup, lines 52-62)
    frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.module.css (current `.sectionHeadRow`/`.sectionHeadIcon`/`.sectionHeadRowContent` rules, lines 1-21)
    frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.test.tsx lines 342-358 (existing V2 test to update)
    frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.tsx (current header markup, lines 69-80) and its `.module.css` (lines 1-17) and test (lines 121-128), same pattern
    frontend/scripts/shot-projectmember.mjs full file (currently 444/450 production lines)
    frontend/scripts/lib/shotHelpers.mjs full file (existing extraction pattern -- `runHeroVerification`)
  </read_first>
  <behavior>
    - Test: `ProjectMemberNotesSection` renders the `FileText` icon and the count inside the SAME
      title row as "Texte & Notizen" (assert no element anywhere in the container matches
      `[class*="sectionHeadRow"]`, `[class*="sectionHeadIcon"]`, or `[class*="sectionHeadRowContent"]`
      -- those classes must be fully gone -- and that the icon's SVG plus the heading plus the
      count text all live inside one `[class*="sectionHeaderTitleRow"]` ancestor).
    - Test: identical assertion shape for `ProjectMemberMediaGallery` with `ImageIcon` and
      "Bilder & Medien".
  </behavior>
  <action>
    In both components, replace the local
    `<div className={styles.sectionHeadRow}><Icon .../><div className={styles.sectionHeadRowContent}><SectionHeader ... actions={<span className={pageStyles.sectionCount}>{count}</span>} /></div></div>`
    wrapper with a single direct call:
    `<SectionHeader title="..." underline icon={<Icon size={18} aria-hidden="true" />} counter={<span className={pageStyles.sectionCount}>{count}</span>} />`
    -- swap the existing `actions=` prop for `counter=`, do not pass both. Delete the now-dead
    `.sectionHeadRow`/`.sectionHeadIcon`/`.sectionHeadRowContent` rules (and their explanatory
    comment blocks) from both `.module.css` files. In `shot-projectmember.mjs`, extend the
    `sectionHeaderUnderlines` fact block (or add a sibling `sectionHeaderTitleRowAlignment` fact)
    so that, per section ("Texte & Notizen", "Bilder & Medien"), it also captures the counter
    element's and the heading element's `getBoundingClientRect().top`, throwing if their
    difference exceeds 2px at any of mobile/tablet/desktop AND the existing `desktop-zoom200`
    720x450 pass -- proving same-row placement, not wrap. If adding this inline would push
    `shot-projectmember.mjs` over 450 production lines, extract the new check into a new exported
    function in `frontend/scripts/lib/shotHelpers.mjs` (same pattern `runHeroVerification` already
    established) and call it from the main per-viewport loop.
  </action>
  <acceptance_criteria>
    - `grep -rc "sectionHeadRow" frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.tsx frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.module.css frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.tsx frontend/src/components/fansubs/projectMember/ProjectMemberMediaGallery.module.css` returns 0 for every file.
    - Updated `ProjectMemberNotesSection.test.tsx`/`ProjectMemberMediaGallery.test.tsx` V2 tests (icon + same-row assertion) pass.
    - `wc -l frontend/scripts/shot-projectmember.mjs` reports <= 450.
    - A screenshot run (`docker restart team4sv30-frontend` then `node scripts/shot-projectmember.mjs` inside the container) shows the new counter/heading `top`-delta fact <= 2px for both sections at every checked viewport.
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/fansubs/projectMember/ProjectMemberNotesSection.test.tsx src/components/fansubs/projectMember/ProjectMemberMediaGallery.test.tsx"</automated>
  </verify>
  <done>Both section headers exclusively use the global icon/counter slots; no local wrapper markup or CSS remains in either component; the screenshot script proves same-row counter placement across all checked viewports while staying at or under 450 production lines.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Permanent hero-metric affordance + focus ring (F2), contain clamped-link focus (F3), isolate note stacking context (F4), full verification</name>
  <files>frontend/src/components/ui/ui.module.css, frontend/src/components/ui/HeroMetrics.test.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberNoteEntry.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberNoteEntry.module.css, frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.test.tsx, frontend/scripts/shot-projectmember.mjs, frontend/scripts/lib/shotHelpers.mjs</files>
  <read_first>
    frontend/src/components/ui/ui.module.css lines 1-60 (`.button`/`.buttonPrimary` focus-visible pattern) and lines 126-147 (`.buttonText`/`.buttonText:hover`, the exact block to change)
    frontend/src/styles/globals.css lines ~145-150 (`--focus-ring`/`--focus-outline` token definitions -- the ONLY tokens to use, no new hex values)
    frontend/src/components/ui/HeroMetrics.tsx (confirm `variant="text"` has exactly 2 usages, both here) and its full test file
    frontend/src/components/fansubs/projectMember/ProjectMemberNoteEntry.tsx (full file, 118 lines) and its `.module.css` (full file, 154 lines) -- specifically `.entry` (lines 6-16), `.bodyClamped` (lines 100-107), `.body a`/`.toggle` (lines 109-118), `.entryLink::after` (lines 131-136)
    frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.test.tsx lines 197-231 (existing GAP-01 nested-interactive-markup tests -- must not be weakened)
  </read_first>
  <behavior>
    - Test (HeroMetrics.test.tsx, CSS-content assertion -- this specific case is explicitly
      sanctioned by the GAP-03 F2 finding text itself, since jsdom cannot compute real pseudo-class
      styles): reading `ui.module.css`, the base `.buttonText { ... }` rule body (excluding the
      separate `:hover` block) contains a `text-decoration` declaration (the affordance is
      permanent, not hover-gated), and a separate `.buttonText:focus-visible { ... }` rule exists
      referencing `var(--focus-ring)`.
    - Test (in `ProjectMemberNotesSection.test.tsx`, extending the existing
      `describe('ProjectMemberNoteEntry', ...)` block): a note with a mocked overflowing body
      (`scrollHeight` > `clientHeight`) whose `body_html` contains a link renders that link with
      `tabindex="-1"` while collapsed; clicking "Mehr anzeigen" removes the `tabindex` attribute
      (link reachable again); collapsing back via "Weniger anzeigen" restores `tabindex="-1"`.
    - Test (source assertion, same rationale as the HeroMetrics CSS test above): the `.entry` rule
      in `ProjectMemberNoteEntry.module.css` contains `isolation: isolate`, and the pre-existing
      `.entryLink::after`/`.body a`/`.toggle` z-index declarations are unchanged (diff shows only
      one new declaration line inside `.entry`).
  </behavior>
  <action>
    In `ui.module.css`, move the underline out of `.buttonText:hover` into the base `.buttonText`
    rule: `text-decoration: underline; text-decoration-color: color-mix(in srgb, var(--accent-primary) 45%, transparent); text-underline-offset: 3px;`
    (existing tokens only, no new hex/custom properties). Keep `.buttonText:hover` for the
    color/decoration-color strengthening only (e.g. `text-decoration-color: currentColor;` plus the
    existing `color` change), and add a new `.buttonText:focus-visible { outline: none; box-shadow: var(--focus-ring); border-radius: 4px; }`
    rule directly below it, matching the existing `.switchRoot:focus-visible` pattern. Do not touch
    "13 Folgen" (plain text, not a Button) or any other `variant="text"` styling beyond this one
    block. In `ProjectMemberNoteEntry.tsx`, import `useEffect` alongside the existing `useId` import
    and add an effect keyed on `[isExpanded, isOverflowing]` that reads
    `contentRef.current?.querySelectorAll('a')` and sets `tabindex="-1"` on each anchor when
    `!isExpanded && isOverflowing`, removing the attribute otherwise -- do not modify
    `useClampedOverflow` itself (shared hook, used elsewhere, out of scope) or the GAP-01
    `article`/`.entryLink` overlay structure. In `ProjectMemberNoteEntry.module.css`, add exactly
    one declaration, `isolation: isolate;`, to the existing `.entry` rule -- do not alter
    `.entryLink::after`, `.body a`, or `.toggle` z-index values. Extend the screenshot script (or
    `shotHelpers.mjs`, following the same extraction rule as Task 2 if the 450-line cap would be
    exceeded) with a `desktop` keyboard-focus pass: Tab to a hero jump-metric button, assert a
    non-empty computed `boxShadow`/`outline` at that focus state, and assert the metric button's
    computed `textDecorationLine` is `"underline"` BEFORE any hover/mouseover interaction (proves
    the always-visible affordance live in a real browser, complementing the CSS-source unit test).
    Finish with the full lint/typecheck/test suite inside the container.
  </action>
  <acceptance_criteria>
    - `awk '/^\.buttonText \{/,/^\}/' frontend/src/components/ui/ui.module.css | grep -c "text-decoration"` reports >= 1 (permanent, not hover-only).
    - `grep -c "buttonText:focus-visible" frontend/src/components/ui/ui.module.css` reports 1.
    - `grep -c "isolation: isolate" frontend/src/components/fansubs/projectMember/ProjectMemberNoteEntry.module.css` reports 1; `git diff` on that file shows exactly one added declaration line inside `.entry`, with `.entryLink::after`/`.body a`/`.toggle` unchanged.
    - New collapsed/expanded `tabindex` tests pass; all pre-existing `ProjectMemberNoteEntry`/`ProjectMemberNotesSection` tests (nested-interactive-markup at lines 197-231, card click, toggle, per-entry role color) still pass unmodified.
    - `docker compose exec -T team4sv30-frontend sh -c "cd /app && npm run lint && npm run typecheck && npm test"` is green (matching STATE.md's documented baseline: 2 pre-existing CSS-guard failures and 3 todo are the only allowed non-green items, no NEW failures).
    - A `docker restart team4sv30-frontend` + `node scripts/shot-projectmember.mjs` run captures: full-width underline, counter in the title row at mobile/tablet/desktop/zoom200, a permanently visible hero-metric affordance, a visible keyboard focus ring, and an unchanged "Karas" sample (`ThemeTimeline.tsx:195`, `<SectionHeader title="Karas" underline />`, no icon/counter).
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npm run lint && npm run typecheck && npm test"</automated>
  </verify>
  <done>F2 (permanent hover-independent affordance + focus-visible ring), F3 (clamped-link tab containment), and F4 (isolated stacking context) are implemented exactly as specified; full frontend lint/typecheck/test suite is green with no new failures beyond the documented baseline; screenshot evidence is captured. Live human UAT sign-off is explicitly NOT claimed by this task -- it remains a separate, out-of-band human step (157-06 Task 4 plus a full 157-UAT.md GAP-02/GAP-03 re-run).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM (client-rendered React) | No new boundary crossed -- every change in this plan is presentational (CSS) or DOM-attribute-level (`tabindex`) on already-rendered, already-sanitized same-origin content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-15-01 | Tampering | `ProjectMemberNoteEntry.tsx` `tabindex` toggling | accept | Pure DOM-attribute manipulation of already-rendered, already-sanitized `RichTextRenderer` output; no new user input is parsed, no new markup is injected, `RichTextRenderer`'s own sanitization is untouched |
| T-157-15-02 | Information Disclosure | `SectionHeader` icon/counter props, hero-metric CSS affordance | accept | Purely presentational; icon/counter values are already-public section counts identical to what rendered before, just relocated |
| T-157-15-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no new packages |
</threat_model>

<verification>
`docker compose exec -T team4sv30-frontend sh -c "cd /app && npm run lint && npm run typecheck && npm test"` must be green (no new failures beyond the documented 2 CSS-guard + 3 todo baseline). Screenshot proof via
`docker restart team4sv30-frontend` then `docker compose exec -T team4sv30-frontend sh -c "cd /app && node scripts/shot-projectmember.mjs"` must show: full-width section-header underline, in-row counters at every viewport including 200% zoom, a permanently visible hero-metric affordance with a visible focus ring, contained tab order for clipped note links, and an unchanged "Karas" sample.
</verification>

<success_criteria>
- GAP-03 findings F1, F2, F3, F4 from `157-UAT.md` are closed exactly as specified, with no regression to any other `SectionHeader` consumer, to the GAP-01 note structure, or to the GAP-02 timeline dot/line work.
- All new and pre-existing automated tests pass; full lint/typecheck/test suite is green.
- Screenshot evidence exists proving the visual/keyboard-accessibility fixes across mobile, tablet, desktop, and 200% zoom.
- This plan does NOT constitute live human UAT sign-off. The phase-level `157-06` Task 4 checkpoint and a full human re-run of `157-UAT.md`'s GAP-02 points 1-9 plus GAP-03 F1-F5 remain a separate, required, out-of-band human step.
</success_criteria>

<output>
Create `.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-15-SUMMARY.md` when done
</output>
