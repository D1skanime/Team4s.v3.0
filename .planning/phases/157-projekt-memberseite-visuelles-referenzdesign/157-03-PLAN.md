---
phase: 157-projekt-memberseite-visuelles-referenzdesign
plan: 03
type: execute
wave: 3
depends_on: ["157-02"]
files_modified:
  - frontend/src/components/fansubs/projectMember/ProjectMemberNoteEntry.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberNoteEntry.module.css
  - frontend/src/components/fansubs/projectMember/ProjectMemberNoteCard.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.module.css
  - frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.test.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberPage.tsx
autonomous: true
requirements: [P157-05, P157-06, P157-07, P157-13]

must_haves:
  truths:
    - "Each note renders as a compact timeline row (meta-line Folge/Version/Datum, title, clamped body, chevron) with NO large colored role header and NO 'Notiz zu Folge X' context line and NO footer-link row — the whole row links to the release"
    - "Role NAME and role COLOR are independent axes (Nachtrag P157-13): the role NAME/text still appears only when the member has more than one project role (unchanged from the original Workstream E rule); the role COLOR is MANDATORY on every entry regardless of role count — a single-role member's notes are never colorless"
    - "Every entry's colored accent element carries data-color-key={boundedColorKey(note.role_color_key)}, feeding the existing central seam (globals.css [data-color-key] -> --role-chip-accent -> --role-accent) — no second/parallel role-color mapping exists anywhere in the projectMember code"
    - "The pager reads 'Weitere N Beiträge anzeigen' with the actual next-batch size (singular 'Weiteren 1 Beitrag anzeigen' when N=1), never the old generic 'Weitere Beiträge laden'"
  artifacts:
    - path: "frontend/src/components/fansubs/projectMember/ProjectMemberNoteEntry.tsx"
      provides: "New compact timeline-row component, replacing ProjectMemberNoteCard's use of the shared PublicNoteCard, carrying data-color-key on every render"
    - path: "frontend/src/components/fansubs/projectMember/ProjectMemberNoteEntry.module.css"
      provides: "New, dedicated timeline CSS (vertical line + dot + flat row + solid var(--role-accent) edge), not stacked onto ProjectMemberNotesSection.module.css's existing 30 lines, and containing zero hex-literal color values"
  key_links:
    - from: "frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.tsx"
      to: "frontend/src/components/fansubs/projectMember/ProjectMemberNoteEntry.tsx"
      via: "import replaces ProjectMemberNoteCard"
      pattern: "ProjectMemberNoteEntry"
    - from: "frontend/src/components/fansubs/projectMember/ProjectMemberNoteEntry.tsx"
      to: "release detail page"
      via: "whole entry is a Link to `${projectPath}/releases/${note.release_version_id}`"
      pattern: "releases/"
    - from: "frontend/src/components/fansubs/projectMember/ProjectMemberPage.tsx"
      to: "frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.tsx"
      via: "hasMultipleRoles={summary.counts.roles > 1} passed down"
      pattern: "hasMultipleRoles"
    - from: "frontend/src/components/fansubs/projectMember/ProjectMemberNoteEntry.tsx"
      to: "frontend/src/styles/globals.css"
      via: "data-color-key attribute consumed by the existing [data-color-key] -> --role-chip-accent -> --role-accent seam (lines 271-293) — the ONLY derivation site, per P157-13"
      pattern: "data-color-key"
---

<objective>
Rebuild the core of the phase (Workstream E, P157-05/P157-06): replace the large,
role-header-per-note cards with a compact timeline of flat rows, per the written reference spec.
`PublicNoteCard` (frontend/src/components/public/PublicNoteCard.tsx) is explicitly OFF LIMITS
(mandatory_run_constraints #1) — it also serves the release detail page's `ReleaseNotesList`, where
its role header is factually correct. Build a genuinely new component instead of adapting the
shared one, and delete the old `ProjectMemberNoteCard` adapter.

Also lands the concrete pager copy for this section (Workstream F, P157-07): "Weitere N Beiträge
anzeigen" with the real next-batch size, without touching `useProjectMemberCollection.ts`
(mandatory_run_constraints #3).

**Mid-planning addendum (P157-13, CONTEXT.md Workstream E2 — binding, overrides the original
single-role wording):** the reference screenshot's neutral white timeline rows are explicitly NOT
binding on role color. Role NAME text still only appears for multi-role members (unchanged), but
role COLOR must remain visible on every entry, single-role or multi-role, reusing the exact existing
central seam (`role_color_key` -> `boundedColorKey()` -> `data-color-key` -> `globals.css`'s
`--role-chip-accent`/`--role-accent`) — no parallel color table, no hardcoded hex in component CSS.

Purpose: this is the single biggest visual complaint in the user's order — the repeated large role
header on every note — while preserving the pre-existing, fachlich meaningful role-color identity
the operator explicitly insisted on retaining mid-planning.
Output: `ProjectMemberNoteEntry` (new, always color-accented via the central seam), wired into
`ProjectMemberNotesSection`; `ProjectMemberNoteCard` deleted; concrete pager label;
role-NAME-chip-only-when-multi-role logic threaded from the Hero's already-known role count, with
role COLOR applied unconditionally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-CONTEXT.md
@.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-PATTERNS.md

<interfaces>
<!-- frontend/src/types/projectMember.ts -->
export interface ProjectMemberNote {
  id: number; title: string | null; body_html: string; body_text: string
  role_label: string; role_code: string; role_color_key: string
  episode_label: string; release_version_label: string; release_version_id: number
  created_at: string
}

<!-- frontend/src/components/public/PublicNoteCard.tsx — DO NOT MODIFY. Read-only reference for the
     clamp/toggle algorithm, stripHtml helper, and the data-color-key idiom to PORT (copy the logic,
     not the component). PublicNoteCard.tsx:85 sets data-color-key on its <article> root — mirror
     that placement (root/accent element of the entry), not the removed role-header band: -->
function stripHtml(value: string): string { return value.replace(/<[^>]*>/g, '').trim() }
const [expanded, setExpanded] = useState(false)
const plainText = bodyText && bodyText.trim() !== '' ? bodyText : stripHtml(bodyHtml ?? '')
const expandable = plainText.length > clampThreshold
// hasRichBody ? <RichTextRenderer bodyHtml={bodyHtml} editorType="tiptap" contentSchemaVersion={1} /> : <p>{bodyText}</p>
// toggle button: aria-expanded={expanded}, onClick calls (e) => { e.preventDefault(); setExpanded(v => !v) }
// data-color-key={boundedColorKey(roleColorKey)} on the <article> root (PublicNoteCard.tsx:84-85)

<!-- frontend/src/lib/roleCatalog.ts -->
export function boundedColorKey(value: string | null | undefined): BoundedRoleColorKey
export const ROLE_COLOR_KEYS: readonly string[] // 15 catalog hexes, e.g. '#7b3c4e' (typesetter), '#6b7f2a' (quality_checker)
export const NEUTRAL_ROLE_COLOR_KEY = 'neutral'

<!-- frontend/src/styles/globals.css:271-293 — the ONLY derivation site, do not add a second one -->
// [data-color-key='#7b3c4e'] { --role-chip-accent: #7b3c4e; }  (one rule per catalog hex, plus 'neutral')
// [data-color-key] { --role-accent: var(--role-chip-accent, #596176); }
// -> any element carrying data-color-key, and its descendants (CSS custom properties inherit),
//    can consume var(--role-accent) directly. No hex literals belong in component CSS.

<!-- frontend/src/lib/roleCatalog.accessibility.test.ts — DO NOT MODIFY, DO NOT WEAKEN.
     Proven-safe existing pattern for a SOLID (non-text, non-color-mix) --role-accent stripe against
     --surface-card at the required 3:1 non-text contrast ratio for all 15 catalog hexes (see its
     "/me/projects role-row border-inline-start indicator" test: a solid `border-inline-start`/
     `border-color: var(--role-accent)` at 3px already passes 3:1 for every catalog hex). Prefer
     this exact solid-stripe/solid-dot approach over any color-mix or colored-text treatment, which
     would need new, unproven contrast math this plan does not have budget to derive. -->

<!-- frontend/src/components/fansubs/projectMember/useProjectMemberCollection.ts — READ-ONLY -->
interface Result<T> { shown: T[]; loadedCount: number; loading: boolean; error: boolean; canShowMore: boolean; canShowLess: boolean; showMore: () => void; showLess: () => void }

<!-- ProjectMemberReleaseCard.tsx — compact list-row-with-Link idiom to mirror structurally (not copy verbatim) -->
const href = `${projectPath}/releases/${release.release_version_id}`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProjectMemberNoteEntry — new compact timeline-row component, always role-color-accented</name>
  <files>frontend/src/components/fansubs/projectMember/ProjectMemberNoteEntry.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberNoteEntry.module.css</files>
  <read_first>
    frontend/src/components/public/PublicNoteCard.tsx (full file — clamp/toggle algorithm, stripHtml, and the `data-color-key={boundedColorKey(roleColorKey)}` root-attribute idiom at line 85 to port; DO NOT import or modify this file)
    frontend/src/components/fansubs/projectMember/ProjectMemberNoteCard.tsx (current adapter — its formatDate helper to copy verbatim, its props shape as a migration reference; this file gets deleted in Task 2)
    frontend/src/components/fansubs/projectMember/ProjectMemberReleaseCard.tsx (compact row-with-Link structural idiom)
    frontend/src/components/ui/DisclosureIndicator.tsx (chevron affordance primitive — check before hand-building a chevron)
    frontend/src/lib/roleCatalog.ts (boundedColorKey, ROLE_COLOR_KEYS, NEUTRAL_ROLE_COLOR_KEY)
    frontend/src/components/ui/Button.tsx (variant/size/onClick/aria-* props — native-button props pass through to the real <button> underneath; use this primitive for the Mehr/Weniger toggle, NOT a hand-built <button>, per CLAUDE.md's Frontend-UI rule)
    frontend/src/styles/globals.css (lines 271-293 — the single `[data-color-key]` -> `--role-chip-accent` -> `--role-accent` derivation seam; consume `var(--role-accent)` only, never a hex literal, never a second mapping)
    frontend/src/lib/roleCatalog.accessibility.test.ts (full file, read-only guard — note specifically the "/me/projects role-row border-inline-start indicator" test proving a SOLID `var(--role-accent)` border/stripe clears the 3:1 non-text contrast floor for all 15 catalog hexes; this is the safe pattern to copy for the new accent element, since it sidesteps the stricter 4.5:1 text-contrast math entirely)
  </read_first>
  <behavior>
    - Given ANY note (single-role or multi-role member) -> the entry's root/accent element carries
      `data-color-key={boundedColorKey(note.role_color_key)}` and a visibly colored solid edge (a
      left accent line or top edge using `border-color: var(--role-accent)` / `background:
      var(--role-accent)` directly, no color-mix) is present — role color is NEVER absent,
      independent of `hasMultipleRoles`
    - Given a note with `role_label` set and `hasMultipleRoles=false` -> no role NAME text/chip
      renders anywhere in the entry (only the color accent is present)
    - Given a note with `role_label='Typesetting'` and `hasMultipleRoles=true` -> the meta line reads
      exactly "Folge {episode_label} · {version} · {date} · Typesetting" (chip appended,
      comma/dot-separated per reference wording "Folge 10 · v1 · Typesetting") AND the same
      `data-color-key` accent from the first bullet is still present
    - Given `body_text` longer than the clamp threshold and no interaction -> body is visually
      clamped and a "Mehr anzeigen" toggle, rendered via the `Button` primitive from
      `@/components/ui` (never a hand-built native `<button>`), is present; clicking it does NOT
      navigate away (event.preventDefault on the toggle click) and switches the label to "Weniger
      anzeigen"
    - The whole entry (excluding the toggle button) is a single `next/link` Link to
      `${projectPath}/releases/${note.release_version_id}`, carrying `data-note-entry` (so
      `frontend/scripts/shot-projectmember.mjs`'s existing `article, [data-note-entry]` selector
      keeps working without script changes)
    - No "Notiz zu Folge X" context line and no separate footer link row are rendered (both removed
      vs. the old ProjectMemberNoteCard/PublicNoteCard shape)
  </behavior>
  <action>
    Create `ProjectMemberNoteEntry.tsx` exporting `ProjectMemberNoteEntry({ note, projectPath,
    hasMultipleRoles }: { note: ProjectMemberNote; projectPath: string; hasMultipleRoles: boolean })`.
    Copy `formatDate` verbatim from the current `ProjectMemberNoteCard.tsx`. Port (copy, do not
    import) the clamp/expand state and the `stripHtml` helper from `PublicNoteCard.tsx`, using
    `clampThreshold = 180` (matching the value the old adapter passed). Render structure: a
    `next/link` `Link` with `href={`${projectPath}/releases/${note.release_version_id}`}`,
    `className={styles.entry}`, `data-note-entry`, and — per P157-13, applied unconditionally,
    independent of `hasMultipleRoles` — `data-color-key={boundedColorKey(note.role_color_key)}` on
    this same root Link element (mirrors `PublicNoteCard.tsx:85`'s root-attribute placement exactly,
    just moved from an `<article>` to the entry's root `Link`). Inside: a meta line (`styles.meta`)
    joining `Folge ${note.episode_label}`, `note.release_version_label` (as the version segment),
    and `formatDate(note.created_at)` with ` · ` separators, PLUS `note.role_label` appended (also `
    · ` separated) ONLY when `hasMultipleRoles && note.role_label` is truthy, styled as a small
    NAME-only chip (`styles.roleChip`, plain neutral chip background/border from existing global
    tokens — the CHIP's background is not required to also be role-tinted, since the color signal
    already lives on the entry's accent edge; keep the chip simple and let the edge carry the color
    per the addendum's "Farbe darf nie der einzige Informationsträger sein" — color plus the
    optional name chip together satisfy that, exactly as today's design already separates "which
    role" (name, multi-role only) from "how does this look" (color, always)); a bold title
    (`note.title`, only if present); the body — if `note.body_html` is non-empty render via
    `RichTextRenderer` (`@/components/editor/RichTextRenderer`, `editorType="tiptap"`
    `contentSchemaVersion={1}`) inside the clamp wrapper, else a `<p>` with `note.body_text`; the
    "Mehr/Weniger anzeigen" toggle ONLY when the plain text exceeds `clampThreshold`, rendered as
    the `Button` primitive from `@/components/ui` (`variant="ghost"` `size="sm"`, its default
    `type="button"`) — explicitly NOT a hand-built native `<button>` JSX element, which is exactly
    what `PublicNoteCard.tsx`'s own toggle uses and which CLAUDE.md's Frontend-UI rule forbids
    porting into new code ("Lokale Datei-Konsistenz rechtfertigt KEIN Abweichen vom globalen
    Design-System"); port only the STATE LOGIC from `PublicNoteCard.tsx`, not its native-button
    markup. Pass `aria-expanded={expanded}` and
    `onClick={(e) => { e.preventDefault(); e.stopPropagation(); setExpanded((v) => !v) }}` to the
    `Button` (prevents the ancestor Link from navigating when the toggle, unavoidably nested
    inside the Link per the "whole entry links" requirement, is clicked); and a trailing chevron
    using `DisclosureIndicator`
    (check its `open`/`size`/`variant` props first — if it doesn't fit visually, a plain
    `ChevronRight` from `lucide-react` is an acceptable fallback, but prefer the primitive per the
    global-primitives rule).

    CSS (`ProjectMemberNoteEntry.module.css`, own module, zero hex literals): the timeline visual
    (vertical line + dot) uses `--color-border` for the connecting line; the dot AND a left accent
    edge on `.entry` (e.g. `border-inline-start: 3px solid var(--role-accent)`, matching the proven
    solid-stripe pattern from `roleCatalog.accessibility.test.ts`'s `/me/projects` case) both consume
    `var(--role-accent)` directly — no `color-mix`, no hex literal, since `[data-color-key]` on the
    entry root already resolves `--role-accent` for every descendant via CSS custom-property
    inheritance. `.roleChip` (the optional multi-role NAME chip) stays a plain, non-role-tinted chip
    using only `--surface-sunken`/`--color-border`/`--text-primary` (its job is naming the role, not
    re-signaling color — the edge already does that).
  </action>
  <acceptance_criteria>
    - `ProjectMemberNoteEntry.tsx` never imports from `@/components/public/PublicNoteCard`
    - Every rendered entry (regardless of `hasMultipleRoles`) has `data-color-key` on its root/accent
      element equal to `boundedColorKey(note.role_color_key)` — verified for BOTH a single-role and
      a multi-role fixture
    - `ProjectMemberNoteEntry.module.css` contains zero hex-literal color values and references
      `var(--role-accent)` for the accent edge/dot, with no `color-mix(...)` formula introduced (the
      accessibility guard file is not exercised against a new, unproven formula)
    - Rendered output contains `data-note-entry` on the link element
    - No text node containing "Notiz zu Folge" or a footer-link pattern (`→`-suffixed release link outside the main Link) is rendered
    - Role NAME text appears only when `hasMultipleRoles` is `true`; role COLOR (`data-color-key`) appears unconditionally
    - The "Mehr/Weniger anzeigen" toggle is rendered via the `Button` primitive from `@/components/ui`, never a hand-built native `<button>` JSX element — `grep -c "<button" frontend/src/components/fansubs/projectMember/ProjectMemberNoteEntry.tsx` returns `0` (only `<Button` usages are present)
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i ProjectMemberNoteEntry || echo OK"</automated>
  </verify>
  <done>A new, self-contained compact timeline-row component exists, fully independent of PublicNoteCard, matching the written reference structure, and every entry carries the correct role-color accent via the single existing central seam regardless of role count.</done>
</task>

<task type="auto">
  <name>Task 2: Wire into NotesSection, delete the old adapter, concrete pager label, thread hasMultipleRoles</name>
  <files>frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.module.css, frontend/src/components/fansubs/projectMember/ProjectMemberNoteCard.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberPage.tsx</files>
  <read_first>
    frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.tsx (full file)
    frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.module.css (full file — `.notesGrid`'s 2-column grid must become a single-column timeline list)
    frontend/src/components/fansubs/projectMember/ProjectMemberPage.tsx (post-157-02 state, incl. the `ProjectMemberSummaryBand` wiring — add the new prop alongside the existing `<ProjectMemberNotesSection .../>` call without disturbing 157-02's insertion)
  </read_first>
  <action>
    Delete `ProjectMemberNoteCard.tsx` (its only two importers — `ProjectMemberNotesSection.tsx` and
    its test file — are both updated in this plan). In `ProjectMemberNotesSection.tsx`: replace the
    `ProjectMemberNoteCard` import with `ProjectMemberNoteEntry`; add a `hasMultipleRoles: boolean`
    prop to `ProjectMemberNotesSectionProps`; replace the `styles.notesGrid` mapping to render
    `<ProjectMemberNoteEntry key={note.id} note={note} projectPath={projectPath}
    hasMultipleRoles={hasMultipleRoles} />` per note, wrapped as a single-column list (change
    `.notesGrid`'s `grid-template-columns` rule — remove the `repeat(2, 1fr)` desktop breakpoint
    rule entirely, since a timeline is inherently single-column). Compute the concrete pager label:
    `const nextBatch = Math.min(PAGE_LIMIT, count - shown.length)`, then render
    `nextBatch === 1 ? 'Weiteren 1 Beitrag anzeigen' : \`Weitere ${nextBatch} Beiträge anzeigen\``
    as the "load more" Button's children, replacing the current hardcoded "Weitere Beiträge laden".
    Leave "{shown} von {count} angezeigt" / "Alle {count} angezeigt" / "Weniger anzeigen" exactly as
    they are today (per Workstream F: these stay, only the notes load-more label becomes concrete).
    In `ProjectMemberPage.tsx`: add `hasMultipleRoles={counts.roles > 1}` to the existing
    `<ProjectMemberNotesSection ... />` call site (the `counts` destructure already exists from the
    original file; this is a one-line addition to that call, not a new destructure). Note:
    `hasMultipleRoles` only gates role NAME text (Task 1) — it has no bearing on role COLOR, which
    Task 1 already applies unconditionally.
  </action>
  <acceptance_criteria>
    - `ProjectMemberNoteCard.tsx` no longer exists in the repository
    - `ProjectMemberNotesSection.tsx` imports and renders `ProjectMemberNoteEntry`, not `ProjectMemberNoteCard`
    - The load-more button's text is computed from `Math.min(PAGE_LIMIT, count - shown.length)`, singular-correct at N=1
    - `ProjectMemberPage.tsx` passes `hasMultipleRoles={counts.roles > 1}` to `ProjectMemberNotesSection`
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i 'ProjectMemberNotesSection\\|ProjectMemberNoteCard\\|ProjectMemberPage' || echo OK"</automated>
  </verify>
  <done>NotesSection renders the new timeline component exclusively, with a concrete singular/plural-correct pager label; ProjectMemberPage threads the multi-role flag down; the old adapter file is gone.</done>
</task>

<task type="auto">
  <name>Task 3: Adapt ProjectMemberNotesSection.test.tsx for the new timeline markup and prove the role-color seam</name>
  <files>frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.test.tsx</files>
  <read_first>
    frontend/src/components/fansubs/projectMember/ProjectMemberNotesSection.test.tsx (full file, current `ProjectMemberNoteCard` describe block and the `ProjectMemberNotesSection` describe block)
    frontend/src/lib/roleCatalog.ts (import `boundedColorKey` directly in the test to compute the expected `data-color-key` value from the fixture's `role_color_key`, rather than hardcoding a possibly-stale literal)
  </read_first>
  <action>
    Rename the `describe('ProjectMemberNoteCard', ...)` block to `describe('ProjectMemberNoteEntry',
    ...)`, importing `ProjectMemberNoteEntry` instead. Update its three tests: (1) replace the "shows
    the role as heading" assertion — with `hasMultipleRoles={false}` the role NAME text must NOT be
    present (`expect(screen.queryByText('Qualitätsprüfung')).toBeNull()`), and "Notiz zu Folge 08"
    must also not be present (assert absence, not presence); still assert the row's single
    `getByRole('link')` has `href` ending in `/releases/41`; ADD (per P157-13) an assertion that the
    same link element's `data-color-key` attribute equals `boundedColorKey('#6b7f2a')` (the fixture's
    `role_color_key`) — role color must be present even though role name is not. Add a companion
    assertion (or a new `it`) rendering the same note with `hasMultipleRoles={true}` and asserting
    BOTH `screen.getByText('Qualitätsprüfung')` IS present AND the link's `data-color-key` is still
    `boundedColorKey('#6b7f2a')` (identical to the single-role case — color never changes with role
    count). (2) keep the "toggles Mehr/Weniger anzeigen" test, adjusting the render call to pass
    `hasMultipleRoles` (any value, irrelevant to this assertion) and `projectPath`. (3) keep the
    "renders the optional title" test similarly adjusted. In the
    `describe('ProjectMemberNotesSection', ...)` block: change `screen.getAllByRole('article')` to
    `screen.getAllByRole('link')` (the new entry is a `Link` wrapping the whole row, not an
    `<article>`) in both `waitFor` assertions, and change the clicked button text from `'Weitere
    Beiträge laden'` to the exact concrete label the fixture produces — with `PAGE_LIMIT=10` and
    after the first page of 15 shown, `nextBatch = min(10, 24 - 15) = 9`, so the button reads
    `'Weitere 9 Beiträge anzeigen'`. Pass `hasMultipleRoles={false}` to the
    `ProjectMemberNotesSection` render call in this block (existing fixture uses a single role's
    worth of notes conceptually — the value only needs to be consistent and does not change the
    load/dedup behavior under test).
  </action>
  <acceptance_criteria>
    - `npx vitest run .../ProjectMemberNotesSection.test.tsx` passes
    - The single-role case asserts `role_label` text is absent BUT `data-color-key` equals
      `boundedColorKey(note.role_color_key)`; the multi-role case asserts `role_label` text is
      present AND the same `data-color-key` value
    - No test still references `getAllByRole('article')` or the literal string `'Weitere Beiträge laden'`
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/fansubs/projectMember/ProjectMemberNotesSection.test.tsx"</automated>
  </verify>
  <done>Test suite fully covers the new timeline markup, the single-vs-multi-role NAME-chip rule, the always-on COLOR seam (P157-13), and the concrete pager label, with zero references to the deleted component's old semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Member-authored note body (`body_html`/`body_text`) -> rendered DOM | Same trust boundary PublicNoteCard already crosses today; unchanged sanitization path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-08 | Tampering (XSS) | ProjectMemberNoteEntry rendering `note.body_html` | mitigate | Reuses the exact same `RichTextRenderer` (`editorType="tiptap"`, `contentSchemaVersion={1}`) that `PublicNoteCard` already uses in production for the identical field — no new HTML-rendering path is introduced, only a new visual wrapper around the same sanitized renderer |
| T-157-09 | Tampering (XSS) | ProjectMemberNoteEntry rendering `note.body_text` (plain-text fallback) | accept | Rendered via JSX text interpolation (auto-escaped by React), identical to the existing `PublicNoteCard` fallback path |
| T-157-10 | Elevation of Privilege / broken navigation | Toggle button nested inside the whole-row Link | mitigate | Toggle's `onClick` calls `event.preventDefault()`/`stopPropagation()` before flipping expand state, so clicking "Mehr anzeigen" can never trigger unintended navigation |
| T-157-13 | Information Disclosure / Accessibility regression | `data-color-key`-driven `--role-accent` accent edge (P157-13) | mitigate | Reuses the single existing `globals.css` derivation seam verbatim (no new selector, no new mapping); uses only a SOLID `var(--role-accent)` stripe/dot (no color-mix, no colored text), matching the exact pattern `roleCatalog.accessibility.test.ts`'s `/me/projects` case already proves clears the 3:1 non-text contrast floor for all 15 catalog hexes — the accessibility guard file itself is neither modified nor weakened |
</threat_model>

<verification>
`docker compose exec -T team4sv30-frontend sh -c "cd /app && npm run typecheck"`
`docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/fansubs/projectMember/ProjectMemberNotesSection.test.tsx"`
`docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/lib/roleCatalog.accessibility.test.ts"` (must still pass, unmodified)
`grep -rn "ProjectMemberNoteCard" frontend/src` must return zero results after this plan.
`grep -n "#[0-9a-fA-F]\{6\}" frontend/src/components/fansubs/projectMember/ProjectMemberNoteEntry.module.css` must return zero results (no hex literals).
</verification>

<success_criteria>
Notes render as compact timeline rows with no role header, no "Notiz zu Folge X" line, and no
footer-link row; role NAME text appears only for multi-role members; role COLOR (via the single
existing central seam) appears on every entry regardless of role count (P157-13); the pager reads
the real next-batch count with correct singular/plural; `ProjectMemberNoteCard.tsx` is deleted; all
tests pass, including the unmodified `roleCatalog.accessibility.test.ts` guard.
</success_criteria>

<output>
Create `.planning/phases/157-projekt-memberseite-visuelles-referenzdesign/157-03-SUMMARY.md` when done
</output>
