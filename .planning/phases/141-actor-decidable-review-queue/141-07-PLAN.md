---
phase: 141-actor-decidable-review-queue
plan: 07
type: execute
wave: 5
depends_on: ["141-04"]
files_modified:
  - frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/page.tsx
  - frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/NextReviewControl.tsx
  - frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/page.test.tsx
autonomous: true
requirements: [RQUE-02, RQUE-05, RDEL-05]

must_haves:
  truths:
    - "RQUE-02/D04: a 403 response from GET .../release-reviews/:id renders the locked 'Nicht entscheidbar für dich' ErrorState with a return-to-list action, visibly distinct from the existing generic 404/network-failure message -- never Approve/Reject actions"
    - "RQUE-05/D05: 'Next' (both the post-decision button and the new standalone skip control) never silently disappears when exhausted -- it is replaced by the locked 'Keine weiteren Prüfungen für dich offen.' message in the same visual slot"
    - "D08: after a successful decision, the actor sees a real Next control (available-navigate or exhausted-message), not the previous silent omission when the backend returned no next item"
  artifacts:
    - path: "frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/NextReviewControl.tsx"
      provides: "shared resolving/available/exhausted/error presentational states for both post-decision and standalone 'next' navigation"
  key_links:
    - from: "frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/page.tsx"
      to: "frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/NextReviewControl.tsx"
      via: "<NextReviewControl mode=\"post-decision\" next={...} /> and <NextReviewControl mode=\"standalone\" fansubId={...} reviewId={...} />"
      pattern: "NextReviewControl"
---

<objective>
Give the review-detail page an honest, three-state "Next" experience (D05) and a real 403
branch (D04): today, a forbidden/own-submission review renders the same generic message as a
genuinely-missing one, and "no next item" silently omits the button with no explanation. Wire
the already-implemented-but-unused `getNextReleaseReview` API function into a new standalone
"Nächste Prüfung" skip control (UI-SPEC Component Contract 3, case B), sharing its
resolving/available/exhausted/error states with the existing post-decision "Nächste offene
Prüfung" button (case A) through one extracted `NextReviewControl` component.

Purpose: `page.tsx` is measured at 442/450 lines today; the 403 branch, the extended-parity
check for the "not pending" 409 case, and the new standalone control together would push it
past the cap without this extraction (per 141-UI-SPEC.md's File Size Note).

Output: `NextReviewControl.tsx`, `page.tsx` with a typed `loadError: unknown` and a real 403
branch, and new test coverage for both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-actor-decidable-review-queue/141-CONTEXT.md
@.planning/phases/141-actor-decidable-review-queue/141-UI-SPEC.md
@.planning/phases/141-actor-decidable-review-queue/141-PATTERNS.md

@frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/page.tsx
@frontend/src/app/admin/users/tabs/ReviewDelegationSection.tsx
@frontend/src/components/ui/ErrorState.tsx
@frontend/src/lib/api.ts
</context>

<interfaces>
<!-- Exact existing shapes/copy this plan extends. -->

From frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/page.tsx:68,82-109,208-217
(`loadError` -- boolean today, must become typed):
`const [loadError, setLoadError] = useState(false)` -> `useState<unknown>(null)`; the
`catch { if (!controller.signal.aborted) setLoadError(true) }` in `loadDetail` becomes
`catch (error: unknown) { if (!controller.signal.aborted) setLoadError(error) }`. The
`if (loadError || !detail)` render branch (lines 208-217) must split: `error instanceof
ApiError && error.status === 403` renders the NEW locked copy (below); everything else
(404/network/no status) renders the EXISTING generic message unchanged, byte-for-byte.

From `ApiError` (`frontend/src/lib/api.ts:414-436`, already imported in this file for the
existing 409 check): `.status: number`, `.code: string | null` -- reuse directly, no new
error-status helper needed.

Locked 403 copy (141-UI-SPEC.md Copywriting Contract, verbatim):
title `Nicht entscheidbar für dich`; description `Diese Prüfung ist entweder dein eigener
Beitrag oder du hast aktuell keine Berechtigung dafür.`; action `<Button href={returnHref}
variant="secondary">Zurück zur Prüfliste</Button>` (reuses the existing `returnHref` constant,
`page.tsx:80`). Render via `<ErrorState>` (`@/components/ui`) directly -- do NOT use
`getErrorStateCopy`'s built-in 403 branch (its default "Keine Berechtigung"/"wende dich an
einen Fansub-Admin" copy is misleading for a same-submission case an admin cannot fix).

**409 mapping -- already correct, verify, do not add new frontend branching**: confirmed by
reading `release_review_handler.go:351-365`, BOTH `services.ErrReviewAlreadyDecided` and
`services.ErrReviewTargetNotPending` already map to the SAME `"REVIEW_ALREADY_DECIDED"` code
server-side. The existing frontend check `error.code === 'REVIEW_ALREADY_DECIDED'`
(`page.tsx:165`) therefore ALREADY covers D11's "target no longer pending" case with zero
frontend change required -- add a regression test proving this (Task 3), do not add new
branching logic that would be redundant with what the server already guarantees.

Locked "Next" copy (141-UI-SPEC.md Copywriting Contract, verbatim): exhausted state (both
modes) `Keine weiteren Prüfungen für dich offen.`; standalone button label `Nächste Prüfung`;
post-decision button label (unchanged) `Nächste offene Prüfung`.

From frontend/src/lib/api.ts:10541-10550 (`getNextReleaseReview` -- already implemented,
currently called from nowhere): `async function getNextReleaseReview(fansubId: number,
reviewId: string): Promise<ReleaseReviewNextResponse>` where `ReleaseReviewNextResponse =
{data: ReleaseReviewQueueItem | null}`.

From frontend/src/app/admin/users/tabs/ReviewDelegationSection.tsx:5,24,34,56 (reference
pattern for `loadError: unknown` + `.catch((error: unknown) => ...)` -- mirror the storage
shape, not the render copy, which is phase-141-specific per above).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NextReviewControl (shared resolving/available/exhausted/error states)</name>
  <files>frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/NextReviewControl.tsx</files>
  <read_first>
    frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/page.tsx (full file, especially
    lines 357-368's current silent-omission bug and the existing decisionActions Button
    styling conventions), frontend/src/lib/api.ts lines 10541-10550
  </read_first>
  <behavior>
    - `mode="post-decision"` with `next` truthy renders a `Button href={.../reviews/{next.id}}`
      labeled `Nächste offene Prüfung`; with `next === null` renders the locked exhausted
      message in the same slot -- never renders nothing.
    - `mode="standalone"` idle state renders a `Button variant="secondary"` labeled `Nächste
      Prüfung`; clicking it calls `getNextReleaseReview`, shows the button's existing
      `loading` prop while in flight (a second click cannot double-fire); on success with a
      real item it navigates immediately (`router.push`) to that item's detail route; on
      success with `null` it replaces the button with the exhausted message and does not
      re-show the button until the component remounts (e.g. after navigating to a new review
      following a subsequent decision); on fetch failure it shows an inline `role="alert"`
      error with a secondary retry button, mirroring this component family's existing
      `styles.inlineError` convention.
  </behavior>
  <action>
    Create `NextReviewControl.tsx` (`'use client'`) exporting a component accepting either
    `{mode: 'post-decision', fansubId: number, next: ReleaseReviewQueueItem | null}` or
    `{mode: 'standalone', fansubId: number, reviewId: string}` (a discriminated union on
    `mode`). For `standalone`, own local state (`idle | loading | exhausted | error`) and an
    `onClick` handler calling `getNextReleaseReview(fansubId, reviewId)`; on a resolved item
    use `useRouter().push(...)` to navigate immediately (per UI-SPEC "Navigate immediately");
    on `null`, transition to the `exhausted` state and render the locked message in place of
    the button; on error, render the retry pattern. For `post-decision`, render synchronously
    from the passed `next` prop with no internal fetch. Reuse `Button` from `@/components/ui`
    and the existing `styles` module import path this directory already uses.
  </action>
  <acceptance_criteria>
    - `docker compose exec -T team4sv30-frontend npx tsc --noEmit` exits 0
    - `grep -n "Keine weiteren Prüfungen für dich offen" frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/NextReviewControl.tsx` matches exactly
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit"</automated>
  </verify>
  <done>NextReviewControl renders both call sites' three states consistently, never silently omitting the exhausted case.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: page.tsx 403 branch + Next control wiring</name>
  <files>frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/page.tsx</files>
  <read_first>
    frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/page.tsx (full file, especially
    lines 64-109 loadDetail, 161-184 submitDecision's error handling, 208-217 the render
    branch to split, 335-368 decisionActions/success rendering)
  </read_first>
  <behavior>
    - A 403 `ApiError` on load renders the locked "Nicht entscheidbar für dich" panel with a
      working "Zurück zur Prüfliste" link, no Approve/Reject controls rendered anywhere.
    - A 404/network load failure renders the existing generic message, unchanged.
    - `showDecisionActions === true` (pending, not own-or-forbidden, idle/pending state) always
      renders the standalone `NextReviewControl` alongside the existing Confirm/Reject
      buttons.
    - `decisionState.kind === 'success'` always renders `NextReviewControl` in post-decision
      mode (never the previous silent `? ... : null` omission).
  </behavior>
  <action>
    Change `loadError` to `useState<unknown>(null)` and its `catch` to store the raw error, as
    given in the interfaces block. Split the `if (loadError || !detail)` render branch: when
    `loadError instanceof ApiError && loadError.status === 403`, render `<ErrorState
    title="Nicht entscheidbar für dich" description="Diese Prüfung ist entweder dein eigener
    Beitrag oder du hast aktuell keine Berechtigung dafür." action={<Button href={returnHref}
    variant="secondary">Zurück zur Prüfliste</Button>} />`; otherwise keep the existing generic
    message block byte-for-byte. Replace the current conditional `{decisionState.response.data
    .next ? (<Button href={...}>Nächste offene Prüfung</Button>) : null}` with
    `<NextReviewControl mode="post-decision" fansubId={fansubId}
    next={decisionState.response.data.next ?? null} />`. Add `<NextReviewControl
    mode="standalone" fansubId={fansubId} reviewId={reviewId} />` next to the existing
    Confirm/Reject buttons inside the `showDecisionActions` block. Do NOT add any new 409
    error-code branching -- the existing `error.code === 'REVIEW_ALREADY_DECIDED'` check
    already covers both server-side sentinels (confirmed in the interfaces block); leave
    `submitDecision`'s catch block's conflict-detection logic unchanged.
  </action>
  <acceptance_criteria>
    - `docker compose exec -T team4sv30-frontend npx tsc --noEmit` exits 0
    - `grep -n "Nicht entscheidbar für dich" frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/page.tsx` matches exactly
    - `grep -n "NextReviewControl" frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/page.tsx` shows both the post-decision and standalone usages
    - `wc -l frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/page.tsx` stays at or below 450
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit"</automated>
  </verify>
  <done>The detail page distinguishes 403 from 404/network failures with the locked copy and always shows a real Next control, never a silent omission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Detail-page regression tests</name>
  <files>frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/page.test.tsx</files>
  <read_first>
    frontend/src/app/admin/fansubs/releaseReviews.test.tsx (existing mocking conventions for
    this component family to mirror -- `@/lib/api` mocks, `useAuthSession` mock, `useParams`
    mock)
  </read_first>
  <behavior>
    - A mocked `getReleaseReview` rejection shaped as `ApiError(403, ..., ..., 'REVIEW_FORBIDDEN')`
      renders the locked 403 title/description and a working return-to-list link, and renders
      no Confirm/Reject buttons.
    - A mocked `getReleaseReview` rejection shaped as a 404 renders the existing generic
      message unchanged (regression guard for the pre-existing behavior).
    - A mocked `decideReleaseReview` rejection with `code: 'REVIEW_ALREADY_DECIDED'` (used to
      simulate BOTH the already-decided and the not-pending server outcomes, since both share
      this code) renders the existing conflict panel -- proving the single existing frontend
      branch already covers both server-side causes without new code.
  </behavior>
  <action>
    Create `page.test.tsx` for the review-detail route, mocking `@/lib/api`'s
    `getReleaseReview`/`decideReleaseReview`/`getCurrentUser`/`getNextReleaseReview` and
    `next/navigation`'s `useParams`, following `releaseReviews.test.tsx`'s existing mocking
    style. Cover the three behaviors above as separate test cases.
  </action>
  <acceptance_criteria>
    - `docker compose exec -T team4sv30-frontend npx vitest run "reviews/\[reviewId\]/page" --reporter=basic` passes
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run page.test --reporter=basic"</automated>
  </verify>
  <done>The 403 branch, the pre-existing generic 404 behavior, and the shared 409 mapping are all proven by regression tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend 403/409 responses -> page.tsx rendering | frontend only renders what the server already decided (D03); no new client-side authorization inference |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-13 | Information Disclosure | 403 error copy | accept | The locked 403 message deliberately does not distinguish "own submission" from "wrong capability" to the client, matching the backend's single ErrForbidden sentinel (Plan 141-03) -- avoids a client-visible side channel for probing the specific reason |
| T-141-14 | Elevation of Privilege | showDecisionActions / Confirm-Reject rendering | accept | Purely presentational -- the actual decision request still goes through the server's authoritative transaction-scoped guard (review_service.go:147-248, untouched); hiding buttons client-side is UX only, never the security boundary |

No new package installs in this plan (T-{phase}-SC not applicable).
</threat_model>

<verification>
```
docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit && npx vitest run page.test --reporter=basic"
```
</verification>

<success_criteria>
- 403 responses render distinct, locked copy; 404/network failures render the unchanged generic message.
- Next never silently disappears in either mode.
- Regression tests prove all three behaviors.
</success_criteria>

<output>
Create `.planning/phases/141-actor-decidable-review-queue/141-07-SUMMARY.md` when done
</output>
