---
phase: 141-actor-decidable-review-queue
plan: 06
type: execute
wave: 5
depends_on: ["141-04"]
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/ReleaseReviewsSection.tsx
  - frontend/src/app/admin/fansubs/releaseReviews.test.tsx
autonomous: true
requirements: [RQUE-01, RQUE-04, RDEL-05]

must_haves:
  truths:
    - "RQUE-01/D10: the Typ filter renders an option only for review kinds present in allowed_types, and is omitted entirely (not disabled) when allowed_types has 0 or 1 entries; the Bildkategorie filter is omitted entirely when 'image' is not in allowed_types"
    - "RQUE-01/D06: the always-zero 'Mitwirkungen' badge is removed -- the counters row shows only Texte/Bilder, both accent-colored, with an aria-label stating the count is scoped to what the actor can decide"
    - "RDEL-05/D13: the empty state distinguishes 'no filters, genuinely empty' from 'filters active, zero matches' with the exact locked copy for each case -- never implying other reviews exist or that the actor lacks rights for more"
  artifacts:
    - path: "frontend/src/app/admin/fansubs/[id]/edit/ReleaseReviewsSection.tsx"
      provides: "D10-gated filters, D06-honest counters row, D13-locked empty-state copy"
  key_links:
    - from: "frontend/src/app/admin/fansubs/[id]/edit/ReleaseReviewsSection.tsx"
      to: "backend allowed_types field (Plan 141-02)"
      via: "lane.counts.allowed_types drives which Typ/Bildkategorie options render"
      pattern: "allowed_types"
---

<objective>
Finish the "Zu prüfen" tab's honesty contract (D06/D10/D13): remove the always-fake-zero
"Mitwirkungen" badge, gate the Typ and Bildkategorie filters on the backend's real
`allowed_types` signal (never inferred from a zero count, never shown as a disabled ghost
option), and replace the single generic empty-state message with the three D13-locked
variants that never imply broader work exists.

Purpose: this closes 141-RESEARCH.md's Pitfall 4 (a real, currently-shipping UI bug -- the
"Mitwirkungen 0" badge has never reflected real data) and is the concrete UI half of D06/D10's
"no leakage of unavailable review work" requirement.

Output: `ReleaseReviewsSection.tsx` with the badge removed, filters gated on `allowed_types`,
and locked empty-state copy for all three cases (no-filter, filters-active, history).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-actor-decidable-review-queue/141-CONTEXT.md
@.planning/phases/141-actor-decidable-review-queue/141-UI-SPEC.md
@.planning/phases/141-actor-decidable-review-queue/141-PATTERNS.md

@frontend/src/app/admin/fansubs/[id]/edit/ReleaseReviewsSection.tsx
@frontend/src/app/admin/fansubs/[id]/edit/useReleaseReviewLane.ts
@frontend/src/app/admin/fansubs/releaseReviews.test.tsx
</context>

<interfaces>
<!-- Locked copy and exact current lines to change (post-141-04 refactor -- line numbers
approximate, match by content). -->

Counters row (currently): `<Badge variant="info">Texte {counts.text}</Badge> <Badge
variant="info">Bilder {counts.image}</Badge> <Badge variant="muted">Mitwirkungen
{counts.contribution}</Badge>` with `aria-label="Offene Prüfungen nach Typ"` -> DELETE the
Mitwirkungen badge entirely (do not replace it with anything); change `aria-label` to
`"Prüfungen, die du entscheiden kannst, nach Typ"` (locked, verbatim).

`SectionHeader` description (currently): `"Release-Texte und Release-Bilder einzeln prüfen
und sicher entscheiden."` -> `"Release-Texte und Release-Bilder einzeln prüfen und sicher
entscheiden. Eigene Einreichungen zählen nicht mit — sie erscheinen unter „Wartet auf
Fremdprüfung"."` (locked, verbatim, exact umlauts and guillemets as written).

Typ filter (currently unconditional `<option value="text">Texte</option><option
value="image">Bilder</option>`): render `<option value="text">` only if
`lane.counts.allowed_types.includes('text')`; `<option value="image">` only if
`.includes('image')`; if `lane.counts.allowed_types.length <= 1`, omit the entire `FormField`
for Typ (not a disabled Select -- full omission).

Bildkategorie filter (currently always rendered, `disabled={type !== 'image'}`): omit the
entire `FormField` if `!lane.counts.allowed_types.includes('image')`; when rendered, keep the
existing `disabled={type !== 'image'}` behavior unchanged.

Empty state (currently one generic description regardless of case) -- 141-UI-SPEC.md
Copywriting Contract, locked verbatim:
| Case | Title | Description |
|---|---|---|
| view=open, no filters | `Keine offenen Prüfungen` | `Aktuell keine Prüfungen für dich offen.` |
| view=open, filters active | `Keine offenen Prüfungen` | `Für die gewählten Filter sind aktuell keine Prüfungen offen. Passe die Filter an oder setze sie zurück.` |
| view=history | (unchanged title) | `Für die gewählten Filter liegen keine vergangenen Entscheidungen vor.` |
`hasFilters` (existing local variable, unchanged) is the exact predicate to branch on for the
open-view two-way split.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Badge removal, D10 filter gating, D13 empty-state copy</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/ReleaseReviewsSection.tsx</files>
  <read_first>
    frontend/src/app/admin/fansubs/[id]/edit/ReleaseReviewsSection.tsx (full file, post-141-04
    refactor), frontend/src/app/admin/fansubs/[id]/edit/useReleaseReviewLane.ts (Plan 141-04,
    exposes `counts.allowed_types` via `lane.counts`)
  </read_first>
  <behavior>
    - An actor whose `allowed_types` is `['image']` never sees a `Texte` option in the Typ
      Select and never sees a disabled Typ Select showing only one real choice -- the whole
      FormField is absent.
    - An actor whose `allowed_types` is `['text', 'image']` sees both options exactly as
      today, and the Bildkategorie field behaves exactly as today (disabled unless
      `type==='image'`).
    - The rendered counters row never contains the word "Mitwirkungen".
    - Zero items with no active filters renders the D13-locked no-filter copy; zero items with
      an active filter renders the filters-active copy; these two cases render visibly
      different description text.
  </behavior>
  <action>
    Apply all four changes from the interfaces block: delete the Mitwirkungen badge and update
    the counters `aria-label`; update the `SectionHeader` description; gate the Typ FormField
    on `lane.counts.allowed_types.length > 1` and its options on `.includes('text')`/
    `.includes('image')`; gate the Bildkategorie FormField on
    `lane.counts.allowed_types.includes('image')`; replace the single `TableEmptyState`
    description with a computed value branching on `view === 'history'` vs (`view === 'open'`
    and `hasFilters`) vs (`view === 'open'` and `!hasFilters`), using the exact locked strings
    above for each of the three cases. Use correct German umlauts throughout (CLAUDE.md
    Sprachqualität) -- copy exactly as written, do not paraphrase or re-type from memory.
  </action>
  <acceptance_criteria>
    - `docker compose exec -T team4sv30-frontend npx tsc --noEmit` exits 0
    - `grep -n "Mitwirkungen" frontend/src/app/admin/fansubs/[id]/edit/ReleaseReviewsSection.tsx` shows zero matches
    - `grep -n "Aktuell keine Prüfungen für dich offen" frontend/src/app/admin/fansubs/[id]/edit/ReleaseReviewsSection.tsx` matches exactly
    - `grep -n "allowed_types" frontend/src/app/admin/fansubs/[id]/edit/ReleaseReviewsSection.tsx` shows at least two usages (Typ gating, Bildkategorie gating)
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit"</automated>
  </verify>
  <done>The Mitwirkungen badge is gone, filters are gated honestly on allowed_types, and the empty state uses the three D13-locked copy variants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test coverage for D10 gating and D13 copy</name>
  <files>frontend/src/app/admin/fansubs/releaseReviews.test.tsx</files>
  <read_first>frontend/src/app/admin/fansubs/releaseReviews.test.tsx (full file, existing mocking conventions)</read_first>
  <behavior>
    - A mocked `getReleaseReviewCounts` response with `allowed_types: ['image']` renders no
      `Texte` option in the Typ Select and no Typ FormField label text mismatch.
    - A mocked `getReleaseReviewCounts` response with `allowed_types: ['text', 'image']`
      renders both options.
    - A mocked empty `listReleaseReviews` response with no active filters renders "Aktuell
      keine Prüfungen für dich offen." A mocked empty response WITH an active filter (e.g.
      `type=text` set) renders the filters-active description instead.
  </behavior>
  <action>
    Extend `releaseReviews.test.tsx` with test cases covering the three behaviors above,
    mirroring this file's existing API-mocking and render/query conventions. Do not modify
    existing passing test cases' assertions unless they directly assert on the now-removed
    Mitwirkungen badge (update those specifically to assert its absence instead).
  </action>
  <acceptance_criteria>
    - `docker compose exec -T team4sv30-frontend npx vitest run releaseReviews --reporter=basic` passes with no new failures beyond the documented pre-existing baseline
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run releaseReviews --reporter=basic"</automated>
  </verify>
  <done>Test coverage proves D10's filter-gating and D13's two-way empty-state copy split.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend allowed_types -> Typ/Bildkategorie filter rendering | frontend renders filter options purely from server-provided data (D03); no client-side capability inference from counts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-12 | Information Disclosure | Typ/Bildkategorie filter rendering | mitigate | Filter options are gated strictly on the server-provided allowed_types field, never inferred from a zero count (which would incorrectly hide a real-but-currently-empty capability) nor shown disabled-but-visible for a capability the actor structurally lacks (D10) |

No new package installs in this plan (T-{phase}-SC not applicable).
</threat_model>

<verification>
```
docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit && npx vitest run releaseReviews --reporter=basic"
```
</verification>

<success_criteria>
- No fake-zero Mitwirkungen badge remains.
- Typ/Bildkategorie filters are gated on the real allowed_types signal, full omission not disabling.
- Empty state uses the three locked D13 copy variants correctly.
</success_criteria>

<output>
Create `.planning/phases/141-actor-decidable-review-queue/141-06-SUMMARY.md` when done
</output>
