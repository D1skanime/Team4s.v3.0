---
phase: 141-actor-decidable-review-queue
plan: 04
type: execute
wave: 4
depends_on: ["141-03"]
files_modified:
  - frontend/src/types/releaseReviews.ts
  - frontend/src/app/admin/fansubs/releaseReviewPresentation.ts
  - frontend/src/app/admin/fansubs/[id]/edit/useReleaseReviewLane.ts
  - frontend/src/app/admin/fansubs/[id]/edit/ReleaseReviewsSection.tsx
autonomous: true
requirements: [RQUE-01, RQUE-03, RQUE-04]

must_haves:
  truths:
    - "RQUE-03: ReleaseReviewView's type union includes 'own', matching the backend's third view value shipped in Plan 141-02"
    - "RQUE-01/D10: ReleaseReviewCounts carries allowed_types as a field distinct from the numeric text/image counts, so a zero count and 'not capable of this kind' are never conflated by later UI work"
    - "RQUE-04: the fetch/abort/sequence/pagination logic List+Counts already share is extracted into one hook (useReleaseReviewLane) so the queue lane and the new own-pending lane (Plan 141-05) consume identical data-fetching behavior instead of two independently-written copies"
  artifacts:
    - path: "frontend/src/app/admin/fansubs/[id]/edit/useReleaseReviewLane.ts"
      provides: "useReleaseReviewLane(options) -- shared fetch/abort/sequence-guard/pagination hook, extracted from ReleaseReviewsSection.tsx's existing inline logic"
      min_lines: 60
    - path: "frontend/src/types/releaseReviews.ts"
      provides: "ReleaseReviewView with 'own', ReleaseReviewCounts.allowed_types"
  key_links:
    - from: "frontend/src/app/admin/fansubs/[id]/edit/ReleaseReviewsSection.tsx"
      to: "frontend/src/app/admin/fansubs/[id]/edit/useReleaseReviewLane.ts"
      via: "const lane = useReleaseReviewLane({ fansubId, view, animeId, releaseVersionId, type, category, search, enabled })"
      pattern: "useReleaseReviewLane"
---

<objective>
Define the frontend contracts every later plan in this phase builds against: extend
`ReleaseReviewView`/`ReleaseReviewCounts` to match the backend's now-final contract (own
view, `allowed_types`), and extract `ReleaseReviewsSection.tsx`'s fetch/abort/
sequence-guard/pagination logic into a reusable `useReleaseReviewLane` hook -- both because
`OwnPendingReviewsSection.tsx` (Plan 141-05) needs the identical behavior against a different
`view`, and because `ReleaseReviewsSection.tsx` is measured at exactly 450 lines today (the
CLAUDE.md hard cap) and cannot absorb Plan 141-06's badge/filter/copy changes without first
shedding this logic into a sibling file.

Purpose: interface-first ordering -- this plan produces the contracts (types + hook) that
Plans 141-05, 141-06, and 141-07 consume; none of those plans re-derive fetch/pagination
behavior from scratch.

Output: updated `releaseReviews.ts` and `releaseReviewPresentation.ts`, a new
`useReleaseReviewLane.ts` hook, and `ReleaseReviewsSection.tsx` refactored to consume it
(behavior-preserving, verified against existing tests) with its line count back under the
450-line cap before any new content is added in Plan 141-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-actor-decidable-review-queue/141-CONTEXT.md
@.planning/phases/141-actor-decidable-review-queue/141-RESEARCH.md
@.planning/phases/141-actor-decidable-review-queue/141-PATTERNS.md
@.planning/phases/141-actor-decidable-review-queue/141-UI-SPEC.md

@frontend/src/types/releaseReviews.ts
@frontend/src/app/admin/fansubs/[id]/edit/ReleaseReviewsSection.tsx
@frontend/src/app/admin/fansubs/releaseReviewPresentation.ts
@frontend/src/lib/api.ts
</context>

<interfaces>
<!-- Exact existing logic to extract, byte-for-byte behavior preserved. -->

From frontend/src/types/releaseReviews.ts:1,46-51 (types to extend):
`export type ReleaseReviewView = 'open' | 'history'` -> add `| 'own'`.
`ReleaseReviewCounts { text, image, contribution, image_categories }` -> add
`allowed_types: ReleaseReviewType[]`.

From frontend/src/app/admin/fansubs/releaseReviewPresentation.ts:10-20
(`EMPTY_RELEASE_REVIEW_COUNTS` -- a literal of type `ReleaseReviewCounts`, will fail to
compile once the type above gains a required field unless updated in the SAME task): add
`allowed_types: []` to this literal. This is a required, mechanical follow-on of the type
change above -- do not leave it for a later plan to discover as a build failure.

From frontend/src/app/admin/fansubs/[id]/edit/ReleaseReviewsSection.tsx:62-196 (the exact
state/effects to extract into the hook, unchanged logic): `items`, `counts`, `nextCursor`,
`isLoading`, `isLoadingMore`, `error`, `pageError` state; `requestSequence`,
`initialAbortRef`, `loadMoreAbortRef` refs; `loadInitial` (aborts previous, bumps sequence,
`Promise.all([listReleaseReviews(...), getReleaseReviewCounts(...)])`, `dedupeReleaseReviews`
on success, generic German error copy on failure); the `useEffect` that re-runs `loadInitial`
keyed on a composite key; `loadMore` (cursor-based append, its own abort/sequence guard,
`dedupeReleaseReviews` on the appended array). `resetFilters`, `animeOptions`,
`releaseOptions`, `hasFilters`, `caption`, and all JSX/rendering stay in the component --
only the fetch/pagination mechanics move into the hook.

From frontend/src/app/admin/fansubs/releaseReviewPresentation.ts:32-53 (`dedupeReleaseReviews`,
`EMPTY_RELEASE_REVIEW_COUNTS` -- reused unchanged by the new hook, no duplication).

From frontend/src/lib/api.ts:10505-10527 (`listReleaseReviews`/`getReleaseReviewCounts` --
called unchanged by the new hook; `buildReleaseReviewQuery` already passes `params.view ??
"open"` through generically, so `view: 'own'` requires zero change to this file).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Type contracts</name>
  <files>frontend/src/types/releaseReviews.ts, frontend/src/app/admin/fansubs/releaseReviewPresentation.ts</files>
  <read_first>frontend/src/types/releaseReviews.ts (full file), frontend/src/app/admin/fansubs/releaseReviewPresentation.ts (full file)</read_first>
  <action>
    Add `'own'` to the `ReleaseReviewView` union (line 1). Add `allowed_types:
    ReleaseReviewType[]` to `ReleaseReviewCounts` (after `contribution`, before
    `image_categories`, matching the backend's field ordering intent from 141-UI-SPEC.md's
    Cross-Reference to Backend Contract section). Update `EMPTY_RELEASE_REVIEW_COUNTS` in
    `releaseReviewPresentation.ts` to include `allowed_types: []` so it keeps satisfying the
    extended `ReleaseReviewCounts` type. No other type in either file changes.
  </action>
  <acceptance_criteria>
    - `docker compose exec -T team4sv30-frontend npx tsc --noEmit` exits 0
    - `grep -n "'own'" frontend/src/types/releaseReviews.ts` shows the union extension
    - `grep -n "allowed_types" frontend/src/types/releaseReviews.ts frontend/src/app/admin/fansubs/releaseReviewPresentation.ts` shows both the new field and its default-empty literal
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit"</automated>
  </verify>
  <done>ReleaseReviewView and ReleaseReviewCounts match the backend's final contract from Plans 141-02/141-03, and the compiler stays green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract useReleaseReviewLane hook</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/useReleaseReviewLane.ts, frontend/src/app/admin/fansubs/[id]/edit/ReleaseReviewsSection.tsx</files>
  <read_first>
    frontend/src/app/admin/fansubs/[id]/edit/ReleaseReviewsSection.tsx (full file, currently
    exactly 450 lines -- at the CLAUDE.md cap), frontend/src/app/admin/fansubs/releaseReviewPresentation.ts
    (full file), frontend/src/app/admin/fansubs/releaseReviews.test.tsx (existing test
    coverage this refactor must not break)
  </read_first>
  <behavior>
    - `useReleaseReviewLane({fansubId, view: 'open', ...})` produces the exact same
      items/counts/nextCursor/isLoading/error sequence `ReleaseReviewsSection.tsx` produces
      today for the same inputs (behavior-preserving refactor, not a rewrite).
    - Changing any filter option (animeId, type, category, search) or `view` re-triggers
      `loadInitial` exactly once (no duplicate fetch, no stale-request race) -- same guarantee
      the existing `requestSequence`/`AbortController` mechanism already provides.
    - `loadMore()` appends deduped items and updates `nextCursor`, aborting any in-flight
      previous `loadMore` call, exactly as today.
  </behavior>
  <action>
    Create `useReleaseReviewLane.ts` exporting `UseReleaseReviewLaneOptions` (`fansubId:
    number`, `view: ReleaseReviewView`, `animeId: number | null`, `releaseVersionId: number |
    null`, `type: ReleaseReviewType | null`, `category: ReleaseReviewImageCategory | null`,
    `search: string`, `enabled: boolean`) and `UseReleaseReviewLaneResult` (`items:
    ReleaseReviewQueueItem[]`, `counts: ReleaseReviewCounts`, `nextCursor: string | null`,
    `isLoading: boolean`, `isLoadingMore: boolean`, `error: string | null`, `pageError: string
    | null`, `reload: () => void`, `loadMore: () => void`) and the hook function
    `useReleaseReviewLane(options): UseReleaseReviewLaneResult`. Move the exact
    state/refs/effects/functions listed in the interfaces block into this hook, parameterized
    by `options` instead of the component's own local state; `reload` is `loadInitial`
    re-exposed for the "Erneut versuchen" retry button and for D08's "re-derive count/list
    after a decision" requirement future plans need. Update `ReleaseReviewsSection.tsx` to
    call this hook with its existing filter state and `enabled: isClientInitialized &&
    hasActiveSession && !isMobile`, removing the now-duplicated inline state/effects/functions
    it previously owned -- the rendered JSX and all existing filter/URL-sync state
    (`view`/`animeId`/`releaseVersionId`/`type`/`category`/`search` useState hooks, the URL-sync
    effect, `resetFilters`, `animeOptions`, `releaseOptions`) stay in the component unchanged.
    Confirm the file's line count drops well below 450 after this extraction (verify with
    `wc -l`).
  </action>
  <acceptance_criteria>
    - `wc -l frontend/src/app/admin/fansubs/[id]/edit/ReleaseReviewsSection.tsx` reports a line count at least 80 lines below the pre-refactor 450
    - `docker compose exec -T team4sv30-frontend npx vitest run src/app/admin/fansubs --reporter=basic` passes with no new failures beyond the documented pre-existing baseline (139-BASELINE.md)
    - `grep -n "useReleaseReviewLane" frontend/src/app/admin/fansubs/[id]/edit/ReleaseReviewsSection.tsx` shows the hook call
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/app/admin/fansubs --reporter=basic"</automated>
  </verify>
  <done>useReleaseReviewLane exists with a stable, documented contract; ReleaseReviewsSection.tsx consumes it with identical observable behavior and is comfortably back under the 450-line cap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> backend API | this plan changes no request/response shape, only frontend-internal type/state organization -- no new trust boundary crossing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-09 | Tampering | none (pure refactor, no new input handling) | accept | This plan touches no authorization or input-validation logic; it is a behavior-preserving extraction |

No new package installs in this plan (T-{phase}-SC not applicable).
</threat_model>

<verification>
```
docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit && npx vitest run src/app/admin/fansubs --reporter=basic"
```
</verification>

<success_criteria>
- Types match the final backend contract (own view, allowed_types).
- Fetch/pagination logic lives in one reusable hook.
- ReleaseReviewsSection.tsx is under the 450-line cap with zero behavior regression.
</success_criteria>

<output>
Create `.planning/phases/141-actor-decidable-review-queue/141-04-SUMMARY.md` when done
</output>
