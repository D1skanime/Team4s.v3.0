---
phase: 122-ffentliche-projekt-member-seite-mit-beitr-gen-medien-und-rel
plan: 09
type: execute
wave: 3
depends_on: [07]
files_modified:
  - frontend/src/components/fansubs/projectMember/ProjectMemberMediaViewer.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberMediaViewer.module.css
  - frontend/src/components/fansubs/projectMember/ProjectMemberMediaViewer.test.tsx
autonomous: true
requirements: [D-11]

must_haves:
  truths:
    - "Desktop-Viewer nutzt Bild + Info-Sidebar (Bild ~65-75%, Sidebar ~25-35%), NICHT ein Bild auf volle Breite aufgeblasen (Brief 14, Akzeptanz 12)"
    - "Mobile-Viewer stapelt: Bild oben, Metadaten darunter (nicht ueber das Bild gelegt), vertikal scrollbar (Brief 16, Akzeptanz 13)"
    - "Tablet entscheidet per verfuegbarer Breite (Sidebar bei genug Breite, sonst Bottom-Info), nicht per starrem Geraetetyp (Brief 15)"
    - "Navigation: Prev/Next-Buttons, ArrowLeft/ArrowRight, Escape schliesst; Zaehler 'n / N' ueber die projektweite Galerie (Brief 17)"
    - "Viewer ist ein Dialog: Fokus wandert beim Oeffnen hinein, kehrt beim Schliessen auf das Ausgangs-Galerieelement zurueck (Brief 29)"
    - "Benachbarte Medien werden gezielt geprefetcht; bereits geladene Galerie-Medien werden wiederverwendet; Original nur bei expliziter Originalansicht (Brief 17/18)"
  artifacts:
    - path: "frontend/src/components/fansubs/projectMember/ProjectMemberMediaViewer.tsx"
      provides: "Responsiver Media Viewer (Desktop Sidebar / Mobile stacked), Prev/Next/Keyboard, Prefetch, Fokusmanagement"
  key_links:
    - from: "ProjectMemberMediaViewer"
      to: "Modal (@/components/ui) ODER FansubMediaLightbox-Basis"
      via: "Dialog/aria-modal + Fokusfalle wiederverwenden; Layout neu (Sidebar/stacked)"
      pattern: "role=\"dialog\"|aria-modal|Modal"
    - from: "Viewer Bildvarianten"
      to: "preview_url (Standard), thumbnail_url (Prefetch), Original (nur explizit)"
      via: "object-fit: contain, max-width ~1400-1600px"
      pattern: "object-fit|preview_url"
---

<objective>
Ein responsiver Media Viewer fuer die projektweite Galerie: Desktop mit Bild + Info-Sidebar, Mobile
mit Bild oben und Metadaten darunter, Tablet breitenabhaengig. Prev/Next + Tastatur + Escape,
Positionszaehler ueber die gesamte projektweite Galerie, Nachbar-Prefetch, korrektes Fokusmanagement.

Purpose: Brief 13-18/29 verlangen ausdruecklich unterschiedliche responsive Layouts (kein blosses
Skalieren), Ressourcenschonung (preview statt original, Prefetch, Wiederverwendung) und volle
A11y. FansubMediaLightbox liefert nur ein einfaches Overlay ohne Sidebar/Prefetch — Basis ok,
Layout+Prefetch neu.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-ffentliche-projekt-member-seite-mit-beitr-gen-medien-und-rel/122-CONTEXT.md
@frontend/src/components/fansubs/FansubMediaLightbox.tsx
@frontend/src/components/ui/Modal.tsx
@frontend/src/lib/fansubProjectRoutes.ts
</context>

<interfaces>
FansubMediaLightbox.tsx (Referenz): ArrowLeft/ArrowRight-Handler, Prev/Next-Buttons mit aria-label,
n/N-Zaehler, Modal liefert role=dialog/aria-modal + Fokusfalle + Fokus-Rueckgabe. NEU noetig:
Desktop-Grid (Bild-Spalte + Sidebar-Spalte via CSS Grid/Container-Query oder useBreakpoint),
Mobile-Stack, Nachbar-Prefetch (aktueller Index +/-1 preview vorladen), Wiederverwendung der bereits
in der Galerie geladenen items (aus 122-07 durchgereicht). ProjectMemberMedia-Felder fuer Sidebar:
release_version_label, media_type, uploader-Name (aus Summary/Media), created_at, caption,
release_version_id (-> "Release oeffnen").
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Viewer-Grundgeruest (Dialog, Prev/Next/Keyboard, Fokus)</name>
  <files>frontend/src/components/fansubs/projectMember/ProjectMemberMediaViewer.tsx</files>
  <read_first>
    frontend/src/components/fansubs/FansubMediaLightbox.tsx (kompletter Keyboard-/Nav-/Modal-Aufbau);
    frontend/src/components/ui/Modal.tsx (Fokusfalle, aria-modal, Fokus-Rueckgabe-Contract).
  </read_first>
  <action>
    Viewer als Dialog (Modal-Basis oder eigenes role=dialog/aria-modal). Props: items[], index,
    onIndexChange, onClose. Prev/Next-Buttons (aria-label) + ArrowLeft/ArrowRight + Escape. Zaehler
    "n / N" ueber items.length (projektweite Galerie). Bild object-fit: contain, kein Hochskalieren,
    max-width ~1400-1600px. Fokus beim Oeffnen in den Dialog; beim Schliessen zurueck auf das
    ausloesende Galerieelement (ref aus 122-07). Reduced-Motion respektieren.
  </action>
  <verify>cd frontend && npm run typecheck 2>&1 | tail -10</verify>
</task>

<task type="auto">
  <name>Task 2: Responsives Layout (Desktop Sidebar / Mobile stacked / Tablet breitenabhaengig)</name>
  <files>frontend/src/components/fansubs/projectMember/ProjectMemberMediaViewer.module.css, ProjectMemberMediaViewer.tsx</files>
  <read_first>
    frontend/src/components/fansubs/FansubMediaLightbox.module.css (bestehende Viewer-Styles als Startpunkt).
  </read_first>
  <action>
    Desktop (>= grosser Breakpoint): CSS-Grid Bild ~70% + Info-Sidebar ~30% (Release-Titel, Folge ·
    Version, "Von [Uploader]", Datum, Beschreibung, "Release oeffnen ->"). Mobile: Stack (Bild oben,
    Metadaten darunter, vertikal scrollbar; Metadaten NICHT ueber dem Bild). Tablet: per verfuegbarer
    Breite Sidebar oder Bottom-Info (Container-Query pruefen, sonst useBreakpoint/Breite). Kein
    horizontaler Overflow; Viewer nie abgeschnitten.
  </action>
  <verify>cd frontend && npm run typecheck 2>&1 | tail -10</verify>
</task>

<task type="auto">
  <name>Task 3: Bildvarianten + Nachbar-Prefetch + Wiederverwendung</name>
  <files>frontend/src/components/fansubs/projectMember/ProjectMemberMediaViewer.tsx</files>
  <read_first>frontend/src/components/ui/ResponsiveImage.tsx (variant/size-Handling).</read_first>
  <action>
    Viewer zeigt preview_url (nicht Original). Beim aktiven Index gezielt Nachbarn (index-1, index+1)
    preview prefetchen (z. B. new Image() oder link rel=prefetch). Bereits in der Galerie geladene
    items wiederverwenden (kein Re-Fetch). Original nur laden, falls eine explizite Originalansicht
    ausgeloest wird (optionaler Button). Requests bei schnellem Weiterklicken/Schliessen abbrechen.
  </action>
  <verify>cd frontend && npm run typecheck 2>&1 | tail -10</verify>
</task>

<task type="auto">
  <name>Task 4: Tests</name>
  <files>frontend/src/components/fansubs/projectMember/ProjectMemberMediaViewer.test.tsx</files>
  <read_first>frontend/src/components/fansubs/__tests__/FansubMediaLightbox.test.tsx (Keyboard/Close-Testmuster).</read_first>
  <action>
    Tests: (a) Klick oeffnet Viewer; (b) Close + Escape schliessen; (c) Prev/Next + ArrowLeft/
    ArrowRight navigieren; (d) Metadaten entsprechen dem aktuellen Medium; (e) "Release oeffnen"
    zeigt auf korrekten oeffentlichen Release; (f) Fokus wandert hinein und beim Schliessen zurueck.
  </action>
  <verify>cd frontend && npm run test -- ProjectMemberMediaViewer 2>&1 | tail -30</verify>
</task>

</tasks>

<acceptance>
- Desktop Bild+Sidebar; Mobile Bild oben + Info darunter; Tablet breitenabhaengig.
- Prev/Next/Arrow/Escape, n/N ueber projektweite Galerie, Nachbar-Prefetch, Fokusmanagement.
- typecheck + Viewer-Tests gruen.
</acceptance>
