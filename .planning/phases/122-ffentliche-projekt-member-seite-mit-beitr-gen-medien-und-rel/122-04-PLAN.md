---
phase: 122-ffentliche-projekt-member-seite-mit-beitr-gen-medien-und-rel
plan: 04
type: execute
wave: 2
depends_on: [03]
files_modified:
  - frontend/src/components/fansubs/ProjectMemberRows.tsx
  - frontend/src/components/fansubs/ProjectMemberRows.test.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/sections/TeamSection.tsx
autonomous: true
requirements: [D-03]

must_haves:
  truths:
    - "Interne Memberkarten INNERHALB der Fansub-Projektseite verlinken auf die Projekt-Member-Route, nicht mehr auf /members/[slug] (D-03, Brief 2)"
    - "Die AENDERUNG ist auf ProjectMemberRows.tsx (+ dessen einzigen Aufrufer TeamSection.tsx) beschraenkt; kein anderer Member-Link im Repo wird beruehrt (D-03)"
    - "Externe Mitwirkende ohne member_slug bleiben nicht klickbar (<div>, unveraendert)"
    - "Die ganze Karte ist der Link; die optionale Affordance 'Beitraege im Projekt ansehen ->' ist KEIN zweiter, konkurrierender Navigationsweg (Brief 2)"
  artifacts:
    - path: "frontend/src/components/fansubs/ProjectMemberRows.tsx"
      provides: "groupSlug+animeSlug-Props; interner Member -> buildPublicFansubProjectMemberPath; optionale In-Card-Affordance"
    - path: "frontend/src/components/fansubs/ProjectMemberRows.test.tsx"
      provides: "Regression: interner Member -> Projekt-Member-Route; externer ohne slug -> nicht klickbar"
  key_links:
    - from: "ProjectMemberRows (interner Member)"
      to: "buildPublicFansubProjectMemberPath (122-03)"
      via: "Link href statt '/members/' + slug"
      pattern: "buildPublicFansubProjectMemberPath"
    - from: "TeamSection.tsx"
      to: "ProjectMemberRows"
      via: "reicht groupSlug + animeSlug (aus dem Projekt-Kontext/Loader) als Props durch"
      pattern: "groupSlug=|animeSlug="
---

<objective>
Aendert das Linkverhalten der Memberkarten AUSSCHLIESSLICH innerhalb der Fansub-Projektseite: interne
Member (mit slug) fuehren auf die neue Projekt-Member-Route; alles andere bleibt unveraendert. Der
Rest des Repos (Ranking, Gruppenseite, Archiv, members/[slug]) wird nicht angefasst (D-03, Brief 2).

Purpose: Dies ist der praezise, risikoarme Kern von Brief 2. Weil ProjectMemberRows nur von
TeamSection benutzt wird, ist die Aenderung sauber isolierbar und per Regressionstest absicherbar,
ohne globale Membernavigation zu veraendern (Akzeptanzkriterium 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-ffentliche-projekt-member-seite-mit-beitr-gen-medien-und-rel/122-CONTEXT.md
@frontend/src/components/fansubs/ProjectMemberRows.tsx
@frontend/src/components/fansubs/ProjectMemberRows.test.tsx
@frontend/src/app/anime/[id]/group/[groupId]/sections/TeamSection.tsx
@frontend/src/lib/fansubProjectRoutes.ts
</context>

<interfaces>
ProjectMemberRows (heute): Props { teamMembers, externalContributors }. Interner Member ->
`<Link href={'/members/' + member.member_slug}>`; externer ohne slug -> `<div>`. Styles:
FansubTeamSection.module.css (memberRowLink, memberRow, chevron, memberNameLink...).
TeamSection.tsx: einziger Aufrufer; hat Zugriff auf den Projekt-Kontext (Gruppen-slug + anime-slug).
buildPublicFansubProjectMemberPath (122-03).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ProjectMemberRows auf Projekt-Member-Route umstellen</name>
  <files>frontend/src/components/fansubs/ProjectMemberRows.tsx</files>
  <read_first>
    frontend/src/components/fansubs/ProjectMemberRows.tsx (aktueller Link/div-Zweig);
    frontend/src/lib/fansubProjectRoutes.ts (buildPublicFansubProjectMemberPath).
  </read_first>
  <action>
    Props um `groupSlug: string` + `animeSlug: string` erweitern. Interner Member (member_slug !==
    null): `href = buildPublicFansubProjectMemberPath(groupSlug, animeSlug, member_slug)`. Externer
    ohne slug: unveraendert `<div>` (nicht klickbar). Optionale, dezente In-Card-Affordance
    "Beitraege im Projekt ansehen ->" INNERHALB des bestehenden Link-Elements (kein zweiter <a>/Link,
    kein separater onClick-Navigationspfad). Umlaute korrekt (Beitraege -> Beiträge).
  </action>
  <verify>cd frontend && npm run typecheck 2>&1 | tail -10</verify>
</task>

<task type="auto">
  <name>Task 2: TeamSection reicht Kontext durch</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/sections/TeamSection.tsx</files>
  <read_first>
    frontend/src/app/anime/[id]/group/[groupId]/sections/TeamSection.tsx (woher Gruppen-slug +
    anime-slug im Projekt-Loader kommen);
    frontend/src/app/anime/[id]/group/[groupId]/projectPageData.ts (group.slug, anime_slug).
  </read_first>
  <action>
    TeamSection liest Gruppen-slug + anime-slug aus dem vorhandenen Projekt-Datenkontext und reicht
    sie als groupSlug/animeSlug an ProjectMemberRows durch. Falls ein slug fehlt (Datenluecke),
    faellt die Karte auf nicht-verlinkt zurueck statt einen kaputten Link zu erzeugen.
  </action>
  <verify>cd frontend && npm run typecheck 2>&1 | tail -10</verify>
</task>

<task type="auto">
  <name>Task 3: Regressionstest</name>
  <files>frontend/src/components/fansubs/ProjectMemberRows.test.tsx</files>
  <read_first>
    frontend/src/components/fansubs/ProjectMemberRows.test.tsx (bestehende Assertions gegen /members/).
  </read_first>
  <action>
    Test anpassen/ergaenzen: (a) interner Member rendert Link auf
    /fansubs/[groupSlug]/fansubprojekt/[animeSlug]/mitwirkende/[memberSlug]; (b) externer Contributor
    ohne slug ist NICHT klickbar; (c) keine Verlinkung auf /members/[slug] mehr aus dieser Komponente.
    (Die Absicherung, dass Ranking/Gruppenseite/Archiv weiterhin /members/[slug] nutzen, gehoert zu
    122-10, da andere Komponenten.)
  </action>
  <verify>cd frontend && npm run test -- ProjectMemberRows 2>&1 | tail -25</verify>
</task>

</tasks>

<acceptance>
- Interne Memberkarte in der Projektseite -> Projekt-Member-Route; externe ohne slug nicht klickbar.
- Kein anderer Member-Link im Repo veraendert (nur ProjectMemberRows + TeamSection).
- typecheck + ProjectMemberRows-Test gruen.
</acceptance>

<discuss_lock source="122-CONTEXT.md D-14, Discuss 2026-08-10">
Affordance-Praezisierung: "Beitraege im Projekt ansehen ->" erscheint NUR bei Hover/Fokus der
Memberkarte (nicht permanent sichtbar); der Chevron bleibt die permanente Affordance. Weiterhin genau
EIN Link pro Karte, kein zweiter Navigationsweg.
</discuss_lock>
