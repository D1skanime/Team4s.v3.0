---
phase: 154-aggregator-duplikate-bildbudget-und-viewer-aufloesung
plan: 05
type: execute
wave: 2
depends_on: ["154-01", "154-02", "154-03", "154-04"]
files_modified:
  - docs/audits/2026-09-09-public-member-performance/154-D-MEASUREMENTS.md
autonomous: true
requirements: [P154-11, P154-12]
must_haves:
  truths:
    - "RCA-07 (empty React-root commits before first full client tree) is re-measured against the current, fully-merged codebase using the same committed script and a documented methodology; the result is recorded whether or not it justifies further investigation"
    - "The Phase-153 listener remainder (~14-15 per navigation cycle) is investigated for a second source; a documented negative outcome ('no further source found') is treated as a fully valid, complete result if that is what the evidence shows"
    - "No claim in the produced document attributes RCA-07 or the listener remainder to a specific framework function or a proven crash without direct evidence"
    - "If investigation surfaces a fixable root cause whose fix is out of this phase's scope, it is documented and proposed as a follow-up phase, not implemented here"
  artifacts:
    - path: "docs/audits/2026-09-09-public-member-performance/154-D-MEASUREMENTS.md"
      provides: "D1 (RCA-07) and D2 (listener remainder) re-measurement results and investigate-further-or-not decisions, each with rationale"
      contains: "RCA-07"
  key_links: []
---

<objective>
Re-run the two committed measurement scripts now that Phase 153's graph reduction has landed and this
phase's own Waves 1 changes are merged, to answer two open-ended questions with no fix commitment:
does RCA-07's empty-commit count now justify further investigation, and does a second listener source
exist beyond the auto-sizing cause Phase 153 already fixed? A documented negative outcome is a fully
valid, complete result for both (CONTEXT.md D1/D2) — do not force a fix or a positive finding.

Purpose: Investigation with an open outcome, not a fix commitment (per USER-REQUEST.md Workstream D).
Output: `154-D-MEASUREMENTS.md`, a new document recording both re-measurements, their comparison
against REPORT.md/153-AFTER.md baselines, and an explicit investigate-further-or-not decision with
rationale for each.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/154-aggregator-duplikate-bildbudget-und-viewer-aufloesung/154-CONTEXT.md
@docs/audits/2026-09-09-public-member-performance/REPORT.md
@docs/audits/2026-09-09-public-member-performance/153-AFTER.md
</context>

<interfaces>
Exact re-run commands (from 154-RESEARCH.md's "Code Examples", reuse the committed scripts verbatim,
do not rewrite):
```
AUDIT_LABEL=154-d1-after AUDIT_SLOW=1 AUDIT_CPU=4 AUDIT_ROUTES=members/timer,members/kara \
  node scripts/audit-public-member-performance.mjs
# Inspect /tmp/public-member-rca/<id>.json's evidence.commits array: count leading entries with
# changed===0 before the first entry with names!={}.

docker compose exec -T -e AUDIT_LABEL=154-d2-cycles50 -e AUDIT_CYCLES=50 team4sv30-frontend \
  node scripts/audit-public-member-navigation-retention.mjs
```
Baselines to compare against: REPORT.md's RCA-07 (1,664 empty commits `timer` / 257 `kara`, under
production throttling) and 153-AFTER.md's listener-remainder finding (634 -> 1,350 listeners over 50
cycles, ~14-15/cycle, while DOM nodes fell by a factor of 59-170). RESEARCH.md's Open Question 3 notes
153-AFTER.md used DEV-mode re-measurement rather than reconstructing REPORT.md's original isolated
production-diagnostic container — follow that same precedent for consistency unless a concrete reason
requires otherwise, and state explicitly which mode was used.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure RCA-07 (empty React-root commits) and document the result</name>
  <files>docs/audits/2026-09-09-public-member-performance/154-D-MEASUREMENTS.md</files>
  <read_first>
    docs/audits/2026-09-09-public-member-performance/REPORT.md (RCA-07 section),
    docs/audits/2026-09-09-public-member-performance/153-AFTER.md (methodology precedent),
    frontend/scripts/audit-public-member-performance.mjs (evidence.commits shape, AUDIT_SLOW/AUDIT_CPU/AUDIT_ROUTES env vars)
  </read_first>
  <action>
    Run the audit script per the exact invocation in `<interfaces>` against both `members/timer` and
    `members/kara`, under the SAME re-measurement mode 153-AFTER.md used (DEV + `docker compose build`
    as the separate release gate) unless investigation shows a reason to reconstruct the original
    production-diagnostic container — state which mode was used and why. Read the written
    `/tmp/public-member-rca/<id>.json` files' `evidence.commits` arrays; count leading entries with
    `changed===0` before the first entry with populated `names`. Compare the new counts against
    REPORT.md's baseline (1,664 `timer` / 257 `kara`). Write the exact new numbers, the delta, the
    methodology used, and an explicit decision — "numbers still large enough to warrant further
    investigation, recommend as a follow-up phase" OR "numbers have dropped enough that no further
    action is warranted" — into `154-D-MEASUREMENTS.md`. Do not attribute the cause to any specific
    React/framework function without direct evidence from the commit trace; do not claim a proven
    scroll-crash link.
  </action>
  <acceptance_criteria>
    <automated>test -f docs/audits/2026-09-09-public-member-performance/154-D-MEASUREMENTS.md &amp;&amp; grep -q "RCA-07" docs/audits/2026-09-09-public-member-performance/154-D-MEASUREMENTS.md</automated>
    - The document states concrete before/after numbers for both `timer` and `kara`, not rounded/estimated values.
    - The document contains an explicit "investigate further" or "no further action" decision sentence for D1, with its rationale.
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/audits/2026-09-09-public-member-performance/154-D-MEASUREMENTS.md &amp;&amp; grep -q "RCA-07" docs/audits/2026-09-09-public-member-performance/154-D-MEASUREMENTS.md</automated>
  </verify>
  <done>All acceptance criteria listed above for "Task 1: Re-measure RCA-07 (empty React-root commits) and document the result" are met.</done>
</task>

<task type="auto">
  <name>Task 2: Investigate the Phase-153 listener remainder and document the result</name>
  <files>docs/audits/2026-09-09-public-member-performance/154-D-MEASUREMENTS.md</files>
  <read_first>
    docs/audits/2026-09-09-public-member-performance/153-AFTER.md (RCA-01 listener-remainder section, exact baseline numbers and prior command),
    frontend/scripts/audit-public-member-navigation-retention.mjs
  </read_first>
  <action>
    Run the navigation-retention audit script per the exact invocation in `<interfaces>` (50 cycles,
    matching 153-AFTER.md's own methodology). Compare the new listener-growth-per-cycle against the
    153-AFTER.md baseline (634 -> 1,350 over 50 cycles, ~14-15/cycle). As part of the investigation,
    check whether this phase's OWN diff (Plans 154-02's locked-artwork branch, 154-03's bounded-fallback
    placeholder branch) introduces any new `addEventListener` call without a matching cleanup — grep
    the touched components for `addEventListener`/`useEffect` patterns as a plausible additional
    listener source distinct from the auto-sizing cause Phase 153 already fixed. Document the finding:
    if a second source is identified within this phase's own scope, note it (and whether it was
    already incidentally avoided by this phase's implementation choices, e.g. Task 1 of Plan 154-03
    deliberately using no `onError`-retry wiring). If NO second source is found, write this up as a
    complete, valid negative result — do not force a positive finding. If a fixable cause outside this
    phase's scope is found, document it and explicitly recommend it as a follow-up phase's scope, not
    implement a fix here.
  </action>
  <acceptance_criteria>
    <automated>grep -q "Listener" docs/audits/2026-09-09-public-member-performance/154-D-MEASUREMENTS.md</automated>
    - The document states the new listener-growth-per-cycle number compared against the 634->1,350/50-cycle baseline.
    - The document contains an explicit "second source found" or "no further source found" conclusion for D2, with its rationale documented at the same rigor as a positive finding would require.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Listener" docs/audits/2026-09-09-public-member-performance/154-D-MEASUREMENTS.md</automated>
  </verify>
  <done>All acceptance criteria listed above for "Task 2: Investigate the Phase-153 listener remainder and document the result" are met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Read-only instrumentation/measurement against the already-shipped public profile pages; no production code path is modified by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-D-01 | none applicable | n/a | accept | This plan produces documentation only; no new code surface is introduced |
</threat_model>

<verification>
- `docs/audits/2026-09-09-public-member-performance/154-D-MEASUREMENTS.md` exists and documents both
  D1 and D2 with concrete numbers, methodology, and an explicit decision for each.
- `REPORT.md` and `153-AFTER.md` remain byte-unmodified (`git diff --stat` shows no changes to either).
</verification>

<success_criteria>
Both re-measurements are complete, documented with concrete numbers and methodology, and each carries
an explicit, evidence-based decision — including a fully valid negative outcome if that is what the
evidence supports.
</success_criteria>

<output>
Create `.planning/phases/154-aggregator-duplikate-bildbudget-und-viewer-aufloesung/154-05-SUMMARY.md` when done
</output>
