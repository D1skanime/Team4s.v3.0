---
phase: 154-aggregator-duplikate-bildbudget-und-viewer-aufloesung
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/ResponsiveImage.tsx
  - frontend/src/components/ui/ResponsiveImage.test.tsx
  - frontend/src/components/ui/ResponsiveImage.config.test.ts
  - frontend/src/components/profile/MemberProfileHero.tsx
  - frontend/src/components/profile/MemberProfileHero.test.tsx
autonomous: true
requirements: [P154-06, P154-07]
must_haves:
  truths:
    - "When the Next.js image optimizer errors for a ResponsiveImage source, the fallback no longer transfers the unbounded original file; the worst-case transferred bytes stay at or below what the normal (non-error) optimized path already transfers for that slot"
    - "The fallback never changes width/height/fill or the wrapping CSS slot class -- no layout shift, regardless of optimizer success or failure"
    - "The fallback attempts exactly one state transition (normal -> bounded-fallback); it never retries indefinitely"
    - "timer's animated-WebP avatar (540x260px, 26 frames, 411,828 bytes) no longer transfers full, unresized bytes at the 140x140 avatar slot; the chosen mechanism and its justification are recorded in this plan's SUMMARY"
    - "No document produced by this plan attributes the fallback fix to a general PNG-crash narrative; REPORT.md's explicit finding of no crash and no oversized-original-PNG loading in baseline profiles is preserved"
  artifacts:
    - path: "frontend/src/components/ui/ResponsiveImage.tsx"
      provides: "Bounded, network-free fallback on optimizer error, replacing the unconditional unoptimized={true} original-bytes fallback"
      contains: "usingDisplayOriginal"
    - path: "frontend/src/components/profile/MemberProfileHero.tsx"
      provides: "Single animated-avatar code path covering both GIF and animated WebP, with a documented byte-budget mechanism"
      contains: "isAnimatedAvatar"
  key_links:
    - from: "frontend/src/components/ui/ResponsiveImage.tsx"
      to: "AchievementArtwork.module.css / MemberProfileHero.module.css fixed slot geometry"
      via: "identical width/height/fill props and CSS class regardless of fallback state"
      pattern: "width=\\{.*\\} height=\\{.*\\}"
---

<objective>
Close RCA-06's remaining two findings (P154-06, P154-07): bound `ResponsiveImage`'s optimizer-error
fallback so it never delivers multi-megabyte original bytes (currently up to 2.92MB combined for the
`kara` `first_contribution` pair, 9.49MB/2.93MB total under a deliberately blocked optimizer), and give
animated avatars (specifically animated WebP, which Next.js's built-in optimizer auto-detects and
bypasses resizing for regardless of the requested `w=`) an explicit, justified byte budget — extending
the existing GIF-only `isAnimatedAvatar` precedent in `MemberProfileHero.tsx` rather than inventing a
second parallel code path.

Purpose: Defense-in-depth against unbounded image transfer on optimizer failure or animated-format
auto-bypass; explicitly NOT a claimed fix for any observed crash (REPORT.md found none).
Output: `ResponsiveImage`'s fallback is network-free and geometry-preserving; `MemberProfileHero`'s
animated-avatar branch covers WebP as well as GIF behind one code path, with the chosen mechanism
documented and justified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/154-aggregator-duplikate-bildbudget-und-viewer-aufloesung/154-CONTEXT.md
@.planning/phases/154-aggregator-duplikate-bildbudget-und-viewer-aufloesung/154-UI-SPEC.md
@.planning/phases/154-aggregator-duplikate-bildbudget-und-viewer-aufloesung/154-RESEARCH.md
@.planning/phases/154-aggregator-duplikate-bildbudget-und-viewer-aufloesung/154-PATTERNS.md
</context>

<interfaces>
Current `ResponsiveImage.tsx` (full file, 31 lines, verified verbatim):
```
export function ResponsiveImage({ src, alt, onError, ...props }: ResponsiveImageProps) {
  const [failedOptimizedSource, setFailedOptimizedSource] = useState<string | null>(null)
  const usingDisplayOriginal = failedOptimizedSource === src
  return (
    <Image {...props} src={src} alt={alt} unoptimized={usingDisplayOriginal}
      onError={(event) => {
        onError?.(event)
        setFailedOptimizedSource((failedSource) => failedSource === src ? failedSource : src)
      }} />
  )
}
```
Every consumer always passes fixed numeric `width`/`height` (or `fill`) — `AchievementArtwork.tsx`:
`width={1254} height={1254}`; `MemberProfileHero.tsx` avatar: `width={140} height={140}`;
`MemberProfileHero.tsx` background: `fill`. The wrapping CSS slot (`.hero`/`.stage` in
`AchievementArtwork.module.css`, `.heroAvatar` in `MemberProfileHero.module.css`) already clips via
`object-fit: contain`/`overflow: hidden` regardless of the underlying image's native resolution — this
is the existing mechanism that already prevents layout shift; it must be preserved, not reinvented.

The regression script that must pass after this fix: `frontend/scripts/audit-public-member-performance.mjs`'s
`AUDIT_FAIL_BADGES=1` mode blocks `*://*/_next/image?url=%2Fmember-achievement-badges*` at the CDP
network layer — i.e. it blocks the ENTIRE `/_next/image` route for that URL prefix regardless of the
requested `w=`. A fallback design that merely requests a smaller width through the SAME `/_next/image`
route will NOT pass this specific regression check, because the whole route is blocked for that URL
pattern, not just one width. Verify this by reading `frontend/scripts/audit-public-member-performance.mjs`
lines 170-180 before choosing a mechanism.

Current `MemberProfileHero.tsx` animated-avatar branch (lines 107, 175-184, verified verbatim):
```
const isAnimatedAvatar = /\.gif(?:$|\?)/i.test(avatarURL)
...
{avatarURL && isAnimatedAvatar ? (
  <Image src={avatarURL} alt={...} width={140} height={140} sizes="..." loading="eager" unoptimized />
) : avatarURL ? (
  <ResponsiveImage src={avatarURL} alt={...} width={140} height={140} sizes="..." loading="eager" />
) : ( ... )}
```
No backend derivative-generation service exists for either asset class (badge PNGs under
`frontend/public/member-achievement-badges/`, or avatar/background uploads) —
`backend/internal/services/media_service.go` has no `imaging.Resize` call. Do not invent one; this
plan is frontend-only.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bound ResponsiveImage's optimizer-error fallback (P154-06)</name>
  <files>frontend/src/components/ui/ResponsiveImage.tsx, frontend/src/components/ui/ResponsiveImage.test.tsx</files>
  <behavior>
    - Test: on `onError` firing (optimizer failure simulated), the component no longer renders a
      `next/image` element with `unoptimized={true}` pointing at the raw `src` — no image network
      request for the original file is made in the fallback state.
    - Test: the fallback state's rendered box occupies the identical geometry as the success state —
      same numeric `width`/`height` (or `fill`) props and the same passed-through className/wrapper,
      asserted by comparing the rendered output's computed box between a normal render and a
      simulated-`onError` render.
    - Test: triggering `onError` a second time (simulating a second failure) does not change behavior
      further — no retry loop, state stabilizes after the first transition.
  </behavior>
  <read_first>
    frontend/src/components/ui/ResponsiveImage.tsx (full file),
    frontend/src/components/ui/ResponsiveImage.test.tsx (full file, existing test shape),
    frontend/src/components/ui/ResponsiveImage.config.test.ts,
    frontend/src/components/profile/AchievementArtwork.tsx (consumer, to confirm width/height contract),
    frontend/src/styles/globals.css (confirm `--surface-card-muted`/`--border-subtle` token names)
  </read_first>
  <action>
    Confirm via `frontend/scripts/audit-public-member-performance.mjs` (lines ~170-180) that
    `AUDIT_FAIL_BADGES` blocks the entire `/_next/image` route for the affected URL prefix, not a
    specific width — this rules out a "retry at a smaller `/_next/image` width" mechanism as the
    primary fix (any such retry would still hit the same block). Replace the `unoptimized={usingDisplayOriginal}`
    branch: when `usingDisplayOriginal` is true, render a bounded, network-free placeholder instead of
    an `<Image>` element — a `<span>`/`<div>` matching the exact reserved geometry (if `props.fill` is
    present, use `position: absolute; inset: 0`; otherwise set inline `width`/`height` in px equal to
    the numeric `width`/`height` props), styled with the existing `--surface-card-muted` background and
    `--border-subtle` outline tokens (no new custom CSS variable names), carrying `role="img"
    aria-label={alt}` for accessibility, and no `onError`/retry wiring (the state transition happens
    exactly once, driven by the underlying `<Image>`'s `onError` before the swap). Update the
    component's doc comment (currently "Uses the Next optimizer first, then retries the same public
    display URL once without optimization") to describe the new bounded-placeholder behavior instead.
    Do not change `AchievementArtwork.module.css`/`MemberProfileHero.module.css` slot dimensions.
  </action>
  <acceptance_criteria>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/ui/ResponsiveImage.test.tsx src/components/ui/ResponsiveImage.config.test.ts"</automated>
    - `grep -n "unoptimized={usingDisplayOriginal}" frontend/src/components/ui/ResponsiveImage.tsx` no longer matches (the unconditional-original-bytes branch is gone).
    - Manual/scripted byte-budget proof: `docker compose exec -T -e AUDIT_FAIL_BADGES=1 team4sv30-frontend node scripts/audit-public-member-performance.mjs` against `members/kara` shows total badge-image transfer near-zero additional bytes (well under the 2.92MB baseline) for the fallback path; record the before/after numbers in the plan SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/ui/ResponsiveImage.test.tsx src/components/ui/ResponsiveImage.config.test.ts"</automated>
  </verify>
  <done>All acceptance criteria listed above for "Task 1: Bound ResponsiveImage's optimizer-error fallback (P154-06)" are met.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Give animated avatars a single, budgeted code path (P154-07)</name>
  <files>frontend/src/components/profile/MemberProfileHero.tsx, frontend/src/components/profile/MemberProfileHero.test.tsx</files>
  <behavior>
    - Test: a `.gif` avatar URL still renders the existing eager, unoptimized 140x140 `<Image>` branch
      unchanged (no regression for the working GIF case).
    - Test: an avatar URL whose fetched bytes contain a WebP RIFF container with an `ANIM`/`ANMF`
      chunk is detected as animated and renders through the SAME branch as the GIF case (not a second,
      parallel branch) once detection resolves.
    - Test: a plain static `.webp` avatar (no ANIM chunk) stays on the normal `ResponsiveImage` branch
      throughout, with no flash/swap.
  </behavior>
  <read_first>
    frontend/src/components/profile/MemberProfileHero.tsx (lines 1-200),
    frontend/src/components/profile/MemberProfileHero.test.tsx (existing test shape),
    .planning/phases/154-.../154-RESEARCH.md (section "Pattern: animated-avatar budget (Workstream B3)")
  </read_first>
  <action>
    First, empirically confirm the root cause against the live dev container: request the SAME
    `/_next/image?url=<encoded-timer-avatar-path>&w=160` URL directly (e.g. via `curl` from inside the
    frontend container) and inspect the response `Content-Length`/headers to confirm Next.js's own
    optimizer route is auto-bypassing resize for the detected animated container (per
    nextjs.org/docs/app/api-reference/components/image's documented animated-format auto-detection),
    not a client-set `unoptimized` prop (the current `ResponsiveImage` branch for this avatar does not
    set `unoptimized`). Record this confirmation in the task's commit/SUMMARY. Then extend
    `isAnimatedAvatar` from a synchronous `.gif`-only regex into a piece of local state seeded
    synchronously `true` for `.gif` matches (unchanged behavior) and updated asynchronously for other
    extensions (specifically `.webp`) via a small helper (e.g. `isAnimatedWebpSource(url):
    Promise<boolean>`) that fetches a small byte range of the source (same-origin `/media/profile/**`)
    and checks for a RIFF container's `ANIM`/`ANMF` chunk signature, invoked from a `useEffect` keyed on
    `avatarURL`. While the async probe is pending for a `.webp` source, keep rendering the current
    `ResponsiveImage` branch (safe default — do not flash the unoptimized branch speculatively); once
    the probe resolves `true`, swap into the SAME existing `unoptimized` `<Image>` branch used for GIFs
    (do not create a second parallel animated-avatar branch — UI-SPEC's contract requires exactly one).
    Keep the avatar slot's rendered box at 140x140 (desktop, per existing `sizes` breakpoints)
    unconditionally. Document the chosen mechanism (client-side WebP container probe, no backend/
    upload-pipeline change) and its rationale in the task's commit message/SUMMARY, per CONTEXT.md B3's
    "muss nur begründet sein" requirement.
  </action>
  <acceptance_criteria>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/profile/MemberProfileHero.test.tsx"</automated>
    - `grep -n "isAnimatedAvatar" frontend/src/components/profile/MemberProfileHero.tsx` shows exactly one animated-avatar rendering branch (no second parallel `<Image unoptimized>` block added).
    - The task's SUMMARY states the confirmed root cause (client-set prop vs. Next's own auto-bypass) and the chosen mechanism's justification in prose.
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/profile/MemberProfileHero.test.tsx"</automated>
  </verify>
  <done>All acceptance criteria listed above for "Task 2: Give animated avatars a single, budgeted code path (P154-07)" are met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public browser -> Next.js `/_next/image` optimizer -> same-origin static/media assets | Existing boundary; this plan changes only what the CLIENT does when the optimizer errors or auto-bypasses, not what is served through the boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-B2-01 | Denial of Service (client-side, DoS-adjacent) | `ResponsiveImage.tsx` unbounded fallback | mitigate | Replace unconditional `unoptimized`-original fallback with a network-free bounded placeholder (Task 1) |
| T-154-B2-02 | Information Disclosure | Fallback logic accidentally serving a private-preview-sized image to an unauthenticated viewer | accept | Out of scope for this fix — `ResponsiveImage` only concerns already-public badge-artwork/avatar assets; no access-gated image path shares this component differently after this change (confirmed by reading every consumer's usage, unchanged by this plan) |
| T-154-B3-01 | Information Disclosure | Byte-range probe fetch to `/media/profile/**` for animated-WebP detection | accept | Same-origin, same-asset the browser is already loading for display; the probe reads no additional data beyond what the avatar `<img>` itself would fetch |
</threat_model>

<verification>
- `npx vitest run src/components/ui/ResponsiveImage.test.tsx src/components/ui/ResponsiveImage.config.test.ts src/components/profile/MemberProfileHero.test.tsx` all green.
- `AUDIT_FAIL_BADGES=1 node scripts/audit-public-member-performance.mjs` shows bounded (near-zero
  additional) byte transfer on optimizer failure, documented before/after in the SUMMARY.
- A direct `/_next/image?...&w=160` request against the animated avatar, re-checked after the fix,
  shows the chosen mechanism's effect (either bounded via the shared unoptimized-but-detected branch,
  or an explicitly documented residual limitation).
</verification>

<success_criteria>
`ResponsiveImage`'s optimizer-error fallback is bounded and network-free, preserving fixed geometry
with no retry loop; animated WebP avatars are detected and handled through the same single code path
GIFs already use, with the chosen mechanism justified in writing; no crash is claimed as fixed.
</success_criteria>

<output>
Create `.planning/phases/154-aggregator-duplikate-bildbudget-und-viewer-aufloesung/154-03-SUMMARY.md` when done
</output>
