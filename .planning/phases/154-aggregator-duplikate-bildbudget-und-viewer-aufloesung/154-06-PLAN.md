---
phase: 154-aggregator-duplikate-bildbudget-und-viewer-aufloesung
plan: 06
type: execute
wave: 3
depends_on: ["154-01", "154-02", "154-03", "154-04", "154-05"]
files_modified:
  - docs/audits/2026-09-09-public-member-performance/154-AFTER.md
autonomous: true
requirements: [P154-13, P154-14]
must_haves:
  truths:
    - "The full frontend suite, backend build/vet, the DSN-gated query-budget test, and docker compose build all pass, or every red result is explicitly checked against this phase's diff before being called pre-existing"
    - "A new audit document (154-AFTER.md) records before/after numbers for the query-count reduction, the image-byte-budget fixes, and cross-references Plan 154-05's D1/D2 findings -- REPORT.md and 153-AFTER.md remain byte-unmodified"
    - "RCA-04 (reported Chrome tab crash) is explicitly stated as open and unreproduced in this document -- never described as fixed"
    - "The three named pre-existing, phase-foreign defects are each either reproduced-and-confirmed or re-documented as an open discrepancy, not silently dropped"
    - "No claim in the produced document promises a perceptible page-load speed-up"
  artifacts:
    - path: "docs/audits/2026-09-09-public-member-performance/154-AFTER.md"
      provides: "Before/after measurement table, full-suite/build gate results, RCA-04 open statement, named pre-existing defects"
      contains: "RCA-04"
  key_links: []
---

<objective>
Run the phase's full verification gate and produce the mandated before/after audit document, closing
E1-E5 and E8 (P154-13, P154-14). Every red test result is checked against this phase's own diff before
being labeled pre-existing (E4 — lesson from Phase 152's mislabeled pre-existing failures). RCA-04
stays explicitly open in every document that touches it. The three named phase-foreign defects
(`anime/page.tsx` searchParams/Promise mismatch, `formatEditLoadError` page export, ESLint counts) are
named and scoped, and Phase 153's reproduction discrepancy for two of the three is reconciled or
re-documented as open, not silently dropped.

Purpose: Verification and scope boundary — prove the phase's changes are safe and complete without
overclaiming, and without reopening closed or explicitly-deferred findings.
Output: `154-AFTER.md`, a new audit document; a green (or fully explained) full verification gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/154-aggregator-duplikate-bildbudget-und-viewer-aufloesung/154-CONTEXT.md
@.planning/phases/154-aggregator-duplikate-bildbudget-und-viewer-aufloesung/154-USER-REQUEST.md
@docs/audits/2026-09-09-public-member-performance/REPORT.md
@docs/audits/2026-09-09-public-member-performance/153-AFTER.md
</context>

<interfaces>
Exact gate commands (per CONTEXT.md's Betriebsrahmen and 154-RESEARCH.md's Validation Architecture):
```
docker compose exec -T team4sv30-frontend sh -c "cd /app && npm test"          # full Vitest suite
docker compose exec -T team4sv30-frontend sh -c "cd /app && npm run typecheck"  # tsc --noEmit
docker compose exec -T team4sv30-frontend sh -c "cd /app && npm run lint"       # eslint .
docker run --rm --network team4s_default -v /home/d1sk/team4s:/workspace \
  -w /workspace/backend golang:1.25-alpine sh -c "go build ./... && go vet ./..."
docker run --rm --network team4s_default -v /home/d1sk/team4s:/workspace \
  -e TEAM4S_PHASE131_TEST_DSN='<derived-from-backend-container-DATABASE_URL, database name swapped>' \
  -w /workspace/backend golang:1.25-alpine go test ./internal/repository/... -run 'Phase131' -count=1
docker compose build   # NEVER `exec ... npm run build` (dirty .next volume -> false prerender errors)
```
The `.env` `POSTGRES_PASSWORD` value is confirmed WRONG for the Go test-container DSN (CONTEXT.md
E3) — derive the real password via `docker compose exec team4sv30-backend printenv DATABASE_URL` (or
equivalent) and swap only the database name.

Three named phase-foreign defects to name/scope (from 154-USER-REQUEST.md Workstream E / E8):
`frontend/src/app/anime/page.tsx` (synchronous `searchParams` alongside a `Promise` type),
`frontend/src/app/admin/anime/[id]/edit/page.tsx` (disallowed page export `formatEditLoadError`), and
13 ESLint errors / 331 warnings repo-wide. Phase 153 could not reproduce 2 of these 3 under its
mandated commands — this discrepancy must be clarified or re-documented as open, not silently dropped.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run the full verification gate and triage every red result</name>
  <files>docs/audits/2026-09-09-public-member-performance/154-AFTER.md</files>
  <read_first>
    .planning/phases/154-.../154-VALIDATION.md (Test Infrastructure / Sampling Rate sections),
    docs/audits/2026-09-09-public-member-performance/153-AFTER.md (prior phase's own gate-run + triage narrative, to match rigor)
  </read_first>
  <action>
    Run every command in `<interfaces>` in order: frontend `npm test`, `npm run typecheck`, `npm run
    lint`; backend `go build && go vet`; the DSN-gated `TestPhase131PublicProfileQueryBudgetIsConstant`
    (verify it actually ran, not skipped); `docker compose build`. For ANY red/failing result, apply
    E4: check whether Plans 154-01..05's diff caused it (e.g. by checking whether the failing
    file/test is in one of those plans' `files_modified` lists, or by reproducing the failure against
    the pre-phase commit if genuinely ambiguous) before calling it pre-existing. If confirmed
    pre-existing, cite the SPECIFIC prior commit/phase that introduced it (per CLAUDE.md's Teststil
    rule and E4's evidentiary bar) — a bare "pre-existing" label with no citation is not acceptable.
    Record every command's exit status and any triage narrative in `154-AFTER.md`.
  </action>
  <acceptance_criteria>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npm test" &amp;&amp; docker compose exec -T team4sv30-frontend sh -c "cd /app && npm run typecheck"</automated>
    - `docker run --rm --network team4s_default -v /home/d1sk/team4s:/workspace -w /workspace/backend golang:1.25-alpine sh -c "go build ./... && go vet ./..."` exits 0.
    - `docker compose build` exits 0.
    - `154-AFTER.md` lists every gate command's result; any red result is either resolved or cites a specific prior commit/phase as evidence of pre-existing status.
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npm test" &amp;&amp; docker compose exec -T team4sv30-frontend sh -c "cd /app && npm run typecheck"</automated>
  </verify>
  <done>All acceptance criteria listed above for "Task 1: Run the full verification gate and triage every red result" are met.</done>
</task>

<task type="auto">
  <name>Task 2: Write the before/after audit document and the scope-boundary narrative</name>
  <files>docs/audits/2026-09-09-public-member-performance/154-AFTER.md</files>
  <read_first>
    docs/audits/2026-09-09-public-member-performance/REPORT.md (original numbers to compare against),
    .planning/phases/154-.../154-05-SUMMARY.md (D1/D2 findings to cross-reference),
    frontend/scripts/shot.mjs (Playwright-in-container visual evidence tool)
  </read_first>
  <action>
    Write `docs/audits/2026-09-09-public-member-performance/154-AFTER.md` (new file — do not modify
    `REPORT.md` or `153-AFTER.md`) containing: (1) a before/after table for the query count
    (20/19 -> 16/15, cite the actual measured `TestPhase131PublicProfileQueryBudgetIsConstant` output),
    the badge-artwork byte transfer under `AUDIT_FAIL_BADGES=1` (before: 9.49MB timer / 2.93MB kara;
    after: measured value from Plan 154-03), and the `kara` zero-project hero-artwork transfer (before:
    2,922,646 bytes source; after: locked-hero, no image request); (2) the animated-avatar `w=160`
    transfer for `timer` (before: 412,249 bytes; after: measured value and the chosen mechanism's
    effect); (3) a cross-reference to Plan 154-05's `154-D-MEASUREMENTS.md` for D1/D2, not a
    restatement; (4) an explicit, standalone sentence: "RCA-04 (gemeldeter Chrome-Tab-Absturz) bleibt
    offen und unreproduziert; kein Dokument dieser Phase erklärt ihn als behoben."; (5) the three named
    phase-foreign defects from `<interfaces>`, each either reproduced-and-confirmed under this phase's
    mandated commands or re-documented as an open, unreproduced discrepancy (matching or updating Phase
    153's own finding); (6) an explicit statement that no page-load speed-up is claimed (HTTP medians
    already 4-6ms per REPORT.md's own framing). Capture Playwright visual evidence via
    `docker compose exec -T team4sv30-frontend node scripts/shot.mjs` for the `kara`/`timer` profiles
    if the document needs visual proof of the locked-hero/no-layout-shift claims — the embedded
    browser panel's white-screenshot-after-scroll artifact is explicitly NOT valid evidence (E7).
  </action>
  <acceptance_criteria>
    <automated>grep -c "RCA-04" docs/audits/2026-09-09-public-member-performance/154-AFTER.md</automated>
    - `git diff --stat docs/audits/2026-09-09-public-member-performance/REPORT.md docs/audits/2026-09-09-public-member-performance/153-AFTER.md` shows no changes to either file.
    - The document contains concrete before/after numbers (not rounded/estimated) for the query count and each measured byte budget.
    - The document names all three phase-foreign defects with their reproduction status stated explicitly (confirmed or open-discrepancy), and contains no sentence promising a perceptible page-load speed-up.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "RCA-04" docs/audits/2026-09-09-public-member-performance/154-AFTER.md</automated>
  </verify>
  <done>All acceptance criteria listed above for "Task 2: Write the before/after audit document and the scope-boundary narrative" are met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Verification/documentation only; no production code is modified by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-E-01 | Repudiation | Silently mislabeling a genuine regression as "pre-existing" | mitigate | E4 requirement enforced in Task 1: every red result must be checked against this phase's diff and cite a specific prior commit/phase if truly pre-existing |
</threat_model>

<verification>
- All gate commands in `<interfaces>` run and their results (pass, or triaged-and-explained fail) are
  recorded in `154-AFTER.md`.
- `154-AFTER.md` exists with all six required sections from Task 2's action.
- `REPORT.md`/`153-AFTER.md` remain byte-unmodified.
</verification>

<success_criteria>
Full verification gate green or fully explained; a new, honest before/after audit document exists;
RCA-04 stays open in every phase document; the three phase-foreign defects are named and scoped, not
silently carried over.
</success_criteria>

<output>
Create `.planning/phases/154-aggregator-duplikate-bildbudget-und-viewer-aufloesung/154-06-SUMMARY.md` when done
</output>
