---
phase: 154-aggregator-duplikate-bildbudget-und-viewer-aufloesung
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/profile/AnimeProjectAchievementStage.tsx
  - frontend/src/components/profile/MemberBadgeChain.test.tsx
autonomous: true
requirements: [P154-05]
must_haves:
  truths:
    - "A member profile with zero anime-project contributions (e.g. kara) no longer renders the unearned first_contribution hero artwork; it renders LockedStageArtwork's existing grey-medal/lock-icon/'Noch nicht freigeschaltet' state instead"
    - "The 'Anime-Projekte' card, its progress bar, count, and milestone list continue to render exactly as before — only the hero artwork slot changes"
    - "The other three sibling achievement-stage components (Contribution, Points, Membership) are untouched and their existing gating behavior is unaffected"
  artifacts:
    - path: "frontend/src/components/profile/AnimeProjectAchievementStage.tsx"
      provides: "currentCode-gated hero artwork matching the 3-sibling pattern"
      contains: "LockedStageArtwork hero"
  key_links:
    - from: "frontend/src/components/profile/AnimeProjectAchievementStage.tsx"
      to: "frontend/src/components/profile/achievementStageHelpers.tsx"
      via: "LockedStageArtwork hero rendered when currentCode is null"
      pattern: "currentCode \\? .* : .*LockedStageArtwork hero"
---

<objective>
Close RCA-06's locked-hero-artwork gap (P154-05): `AnimeProjectAchievementStage.tsx` currently
resolves its hero artwork via `selectedStage ?? family.heroStage` without checking `currentCode`,
so a member with zero anime-project contributions loads the unearned `first_contribution` motif/frame
pair at full 1254x1254 source resolution (2.92MB combined) as the profile hero. Apply the existing
3-of-4-sibling `LockedStageArtwork` gate pattern verbatim — this is the one family missing it.

Purpose: Defense against an ungated, unnecessarily large image load for a valid zero-progress state;
purely a presentational branch fix, no new component invented (CONTEXT.md B1: "keine dritte Variante").
Output: `AnimeProjectAchievementStage.tsx` matches its three siblings' hero-gating contract; a
regression test proves the zero-project state renders the locked hero.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/154-aggregator-duplikate-bildbudget-und-viewer-aufloesung/154-CONTEXT.md
@.planning/phases/154-aggregator-duplikate-bildbudget-und-viewer-aufloesung/154-UI-SPEC.md
@.planning/phases/154-aggregator-duplikate-bildbudget-und-viewer-aufloesung/154-PATTERNS.md
</context>

<interfaces>
Current bug (verified verbatim, `AnimeProjectAchievementStage.tsx` lines 17-62):
```
const currentCode = family.currentStage?.badge_code ?? null   // line 23, computed but never read below
const selectedStage = family.stages.find((stage) => stage.badge_code === selectedCode && stage.earned)
const heroStage = selectedStage ?? family.heroStage            // no currentCode check
...
<span data-anime-project-art={heroStage.badge_code}>
  {descriptor ? (
    <AchievementArtwork descriptor={descriptor} badgeCode={heroStage.badge_code} alt={heroStage.label}
      size="hero" className={...} />
  ) : (
    <presentation.Icon size={96} aria-label={heroStage.label} />
  )}
</span>
```
Analog gate to copy verbatim (`ContributionAchievementStage.tsx:65-81`, also
`PointsAchievementStage.tsx:50-67`, `MembershipStage.tsx:44-58`):
```
{currentCode ? (
  descriptor ? ( <AchievementArtwork .../> ) : ( <presentation.Icon .../> )
) : (
  <LockedStageArtwork hero />
)}
```
`LockedStageArtwork` and `resolveProgressArtworkDescriptor` are already imported from
`./achievementStageHelpers` at the top of `AnimeProjectAchievementStage.tsx` — no new import needed.
The milestone `<ol>` below (around line 100+) already correctly gates each marker via `stage.earned`
— do not touch it.

`MemberBadgeChain.tsx` renders this component only when `group.key === 'progress'`
(`<AnimeProjectAchievementStage family={group.families[0]} />`) — the badge-progress family key for
"Anime-Projekte" is `'progress'`, distinct from `'contribution_projects'` (a different family,
rendered by `ContributionAchievementStage`). `MemberBadgeChain.test.tsx` already defines
`FAMILY_STAGE_FIXTURES.progress` (line 15) but the existing "Quick 260812-bqs locked mystery heroes"
describe block's `lockedProgress` fixture array (lines 2029-2035) has NO `family: 'progress'` entry
today — this is exactly why the existing golden test does not catch this bug.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Apply the locked-hero gate to AnimeProjectAchievementStage</name>
  <files>frontend/src/components/profile/AnimeProjectAchievementStage.tsx</files>
  <read_first>
    frontend/src/components/profile/AnimeProjectAchievementStage.tsx (full file),
    frontend/src/components/profile/ContributionAchievementStage.tsx (lines 60-85),
    frontend/src/components/profile/PointsAchievementStage.tsx (lines 45-70),
    frontend/src/components/profile/MembershipStage.tsx (lines 40-60),
    frontend/src/components/profile/achievementStageHelpers.tsx (LockedStageArtwork definition)
  </read_first>
  <action>
    Wrap the existing hero `<span data-anime-project-art={heroStage.badge_code}>` block's
    `descriptor ? (...) : (...)` ternary in `{currentCode ? ( ... ) : <LockedStageArtwork hero /> }`,
    mirroring `ContributionAchievementStage.tsx:65-81` exactly — same `hero` prop, same fallback
    component, no new className, no new copy. Do not modify the milestone `<ol>` rendering below
    (already correct), the `Badge variant={presentation.variant}` chip, the progress bar, or the
    count/unit copy — this is a hero-artwork-slot-only change.
  </action>
  <acceptance_criteria>
    - `grep -n "currentCode ?" frontend/src/components/profile/AnimeProjectAchievementStage.tsx` shows the hero span now gated on `currentCode`.
    - `grep -n "LockedStageArtwork hero" frontend/src/components/profile/AnimeProjectAchievementStage.tsx` matches.
    - The file still imports `LockedStageArtwork` from `./achievementStageHelpers` (no new import line added).
    - `docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "currentCode ?" frontend/src/components/profile/AnimeProjectAchievementStage.tsx && grep -n "LockedStageArtwork hero" frontend/src/components/profile/AnimeProjectAchievementStage.tsx && test "$(grep -c "from './achievementStageHelpers'" frontend/src/components/profile/AnimeProjectAchievementStage.tsx)" = "1" && docker compose exec -T team4sv30-frontend sh -c "cd /app && npx tsc --noEmit"</automated>
  </verify>
  <done>All acceptance criteria listed above for "Task 1: Apply the locked-hero gate to AnimeProjectAchievementStage" are met.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a zero-progress regression test for the Anime-Projekte hero gate</name>
  <files>frontend/src/components/profile/MemberBadgeChain.test.tsx</files>
  <behavior>
    - Test: rendering `MemberBadgeChain` with a `badgeProgress` array whose `lockedProgress` fixture
      now includes a `family: 'progress'` entry at `current_count: 0` must show
      `[data-locked-stage-hero]` inside the `progress`/Anime-Projekte family's rendered output, exactly
      like the other 5 already-asserted families.
    - Test: the total locked-hero count and "Noch nicht freigeschaltet" text-node count both increase
      from 5 to 6 once the `progress` family is included at zero.
  </behavior>
  <read_first>
    frontend/src/components/profile/MemberBadgeChain.test.tsx (lines 1-30 for FAMILY_STAGE_FIXTURES,
    lines 2028-2067 for the 'Quick 260812-bqs locked mystery heroes' describe block to extend)
  </read_first>
  <action>
    Add one entry to the `lockedProgress` array (lines 2029-2035): `{ family: 'progress',
    current_count: 0, next_threshold: 1, remaining_count: 1, next_tier: 'first_contribution',
    complete: false, stages: FAMILY_STAGE_FIXTURES.progress }`. Update both length assertions from 5
    to 6: `heroes` (`toHaveLength(5)` -> `toHaveLength(6)`, line ~2042) and
    `screen.getAllByText('Noch nicht freigeschaltet')` (line ~2043). Add a
    `container.querySelector('[data-family="progress"] [data-locked-stage-hero]')).not.toBeNull()`
    assertion alongside the existing per-family checks (lines 2051-2055). In the second test in the
    same describe block ('keeps locked hero SSR free of future artwork identifiers and colors'),
    update both `toHaveLength(5)` SSR-string-count assertions (lines 2062-2063) to 6.
  </action>
  <acceptance_criteria>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/profile/MemberBadgeChain.test.tsx"</automated>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T team4sv30-frontend sh -c "cd /app && npx vitest run src/components/profile/MemberBadgeChain.test.tsx"</automated>
  </verify>
  <done>All acceptance criteria listed above for "Task 2: Add a zero-progress regression test for the Anime-Projekte hero gate" are met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure client-side presentational branch on already-fetched, already-public badge-progress data; no new trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-B1-01 | Information Disclosure | none applicable | accept | This change only alters which already-public artwork asset is requested for a zero-progress state; no new data is exposed or withheld beyond what was already public |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npx vitest run src/components/profile/MemberBadgeChain.test.tsx` passes with the extended
  zero-progress assertions.
- Manual/Playwright spot-check (folded into Plan 154-06's Workstream E visual evidence via
  `frontend/scripts/shot.mjs`, not required standalone in this plan) confirms `kara`'s "Anime-Projekte"
  hero renders the locked medal, not the `first_contribution` artwork.
</verification>

<success_criteria>
`AnimeProjectAchievementStage.tsx` matches the 3-sibling `currentCode`-gated hero pattern exactly; a
zero-project profile no longer loads the ungated `first_contribution` artwork; the extended
`MemberBadgeChain.test.tsx` proves it.
</success_criteria>

<output>
Create `.planning/phases/154-aggregator-duplikate-bildbudget-und-viewer-aufloesung/154-02-SUMMARY.md` when done
</output>
