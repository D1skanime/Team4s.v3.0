---
phase: 48-meine-gruppen-und-contributor-dashboard
plan: "03"
type: execute
wave: 3
depends_on:
  - 48-02-PLAN.md
files_modified:
  - frontend/src/app
  - frontend/src/lib
  - frontend/src/types
autonomous: true
requirements:
  - CONTRIBUTOR-DASHBOARD-01
must_haves:
  truths:
    - "The contributor dashboard should reuse existing UI surfaces rather than recreate them."
    - "Every visible action in the dashboard and contributor group page must be capability-driven."
    - "Navigation must make `Mein Profil` and `Meine Gruppen` first-class destinations."
---

<objective>
Implement the frontend contributor experience: `Meine Gruppen` overview, contributor group detail route, navigation entries, and safe wiring into existing notes/media/release/detail surfaces.

Purpose: Make the existing archive-maintenance features actually reachable for contributors in a secure, comprehensible way.
</objective>

<context>
@.planning/phases/48-meine-gruppen-und-contributor-dashboard/48-02-PLAN.md
@.planning/phases/48-meine-gruppen-und-contributor-dashboard/48-CONTEXT.md
@frontend/src/app/admin/fansubs/[id]/edit/page.tsx
@frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectNotesSection.tsx
@frontend/src/app/admin/fansubs/[id]/edit/NotesTab.tsx
@frontend/src/app/admin/fansubs/[id]/edit/ReleaseVersionMediaDrawerSummary.tsx
@frontend/src/lib/groupNavigation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add frontend API/types for `Meine Gruppen` overview and contributor group detail</name>
  <read_first>
    - frontend/src/lib/api.ts
    - frontend/src/types
  </read_first>
  <action>
    Add or extend frontend client/types for:
    - `GET /api/me/fansub-groups`
    - contributor group detail
    - contributor-scoped anime/release/release-version summaries
    - dashboard/group capability fields
    - optional lightweight contributions data if included in backend scope
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck</automated>
  </verify>
  <done>
    - The frontend has typed contracts for the overview and contributor group detail flows.
    - Capability fields are first-class in the dashboard DTOs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build `/admin/my-groups` and contributor group detail UI with safe reuse</name>
  <read_first>
    - frontend/src/app/admin/fansubs/[id]/edit/page.tsx
    - reusable notes/media/release components
  </read_first>
  <action>
    Implement:
    - `/admin/my-groups`
    - contributor group detail route, preferably `/admin/my-groups/:id`

    The overview should show:
    - user profile short card
    - own groups as cards/list
    - roles, status, active time, capabilities, and quick actions

    The detail route should:
    - reuse safe sections from existing fansub/release/media/note surfaces
    - hide or disable actions when capability is false
    - avoid exposing global-admin-only affordances
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck && npm run lint</automated>
  </verify>
  <done>
    - Contributors have a dedicated dashboard and group-detail experience.
    - Existing edit/media/note/drawer pieces are reused where safe.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend navigation/user menu and prepare `Meine Beteiligungen`</name>
  <read_first>
    - existing admin navigation/header/user-menu seams
    - profile page from Phase 47
  </read_first>
  <action>
    Extend navigation or user menu with:
    - `Mein Profil`
    - `Meine Gruppen`
    - Keycloak account action
    - Logout

    Also add `Meine Beteiligungen` as:
    - a lightweight read-only section if backend data is cheap
    - otherwise a clearly documented placeholder/prepared section

    Do not infer any rights from historical credits.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run lint</automated>
  </verify>
  <done>
    - The contributor navigation is first-class.
    - The dashboard can acknowledge historical contributions without overbuilding them.
  </done>
</task>

</tasks>

<verification>
Automated:
1. `cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck`
2. `cd C:/Users/admin/Documents/Team4s/frontend && npm run lint`

Behavioral:
1. `Meine Gruppen` shows only own groups and capability-driven actions.
2. Contributor group detail reuses existing sections safely.
3. Navigation exposes `Mein Profil` and `Meine Gruppen`.
</verification>

<success_criteria>
- The frontend has a dedicated contributor dashboard and group page.
- Existing release/media/notes/profile surfaces are reused safely rather than rebuilt.
- Navigation and quick actions make contributor workflows discoverable without role checks.
</success_criteria>
