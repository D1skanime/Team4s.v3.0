---
phase: 48-meine-gruppen-und-contributor-dashboard
plan: "04"
type: execute
wave: 4
depends_on:
  - 48-03-PLAN.md
files_modified:
  - backend/internal/handlers
  - backend/internal/repository
  - backend/internal/permissions
  - frontend/src/app
  - frontend/src/lib
  - frontend/src/types
  - docs
autonomous: true
requirements:
  - CONTRIBUTOR-DASHBOARD-01
must_haves:
  truths:
    - "Contributor scoping and URL-manipulation defense need explicit regression coverage."
    - "The final implementation write-up must begin with a short Ist-Analyse."
    - "The dashboard must document what was reused versus intentionally deferred."
---

<objective>
Close Phase 48 with regression coverage, concise developer documentation, live verification, and a clean handoff into the public archive phase.

Purpose: The contributor dashboard is mainly about safe reuse and routing, so verification and documentation are especially important.
</objective>

<context>
@.planning/phases/48-meine-gruppen-und-contributor-dashboard/48-CONTEXT.md
@.planning/phases/48-meine-gruppen-und-contributor-dashboard/48-RESEARCH.md
@.planning/phases/48-meine-gruppen-und-contributor-dashboard/48-01-PLAN.md
@.planning/phases/48-meine-gruppen-und-contributor-dashboard/48-02-PLAN.md
@.planning/phases/48-meine-gruppen-und-contributor-dashboard/48-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the required backend test matrix for contributor scoping</name>
  <read_first>
    - backend/internal/handlers
    - backend/internal/repository
    - backend/internal/permissions
  </read_first>
  <action>
    Add backend tests for at least:
    - authenticated user sees own groups
    - authenticated user does not see foreign groups
    - disabled user sees no groups
    - `platform_admin` sees all or the documented admin variant
    - contributor sees only own-group release versions
    - direct URL/API access to a foreign group is blocked
    - direct URL/API access to a foreign release version is blocked
    - coop release appears for a participating group only
    - capability booleans are correct per group
    - historical credits do not imply app rights
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./... && go build ./...</automated>
  </verify>
  <done>
    - The critical contributor scoping and coop cases are covered.
    - URL and API manipulation defenses have explicit tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add thin frontend regression coverage for dashboard actions and navigation</name>
  <read_first>
    - touched frontend dashboard tests/helpers
  </read_first>
  <action>
    Add the thinnest useful frontend coverage for:
    - `Meine Gruppen` action visibility from capabilities
    - absence of actions when capability is false
    - navigation entries for `Mein Profil` and `Meine Gruppen`
    - safe contributor routing into reused group/release sections
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck && npm run lint</automated>
  </verify>
  <done>
    - Dashboard and navigation affordances have basic regression protection.
    - Capability-based contributor actions are locked in by tests where practical.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write docs, verify live behavior, and prepare Phase 49 handoff</name>
  <read_first>
    - touched backend/frontend dashboard files
    - profile and contributor docs from earlier phases
  </read_first>
  <action>
    Add concise developer documentation that explains:
    - the `Meine Gruppen` overview model
    - which existing components/endpoints were reused
    - how contributor scoping differs from global-admin screens
    - how coop-release visibility is derived
    - what remains deferred for richer contributor workflows

    Then complete a live verification checklist that proves:
    - own-group overview
    - foreign-group denial
    - coop group visibility
    - capability-driven quick actions
    - safe routing into reused sections

    The final implementation write-up for the phase must start with a short Ist-Analyse section that summarizes:
    - relevant functions and endpoints found
    - reused components and handlers
    - relevant DB tables/joins used
    - existing API/frontend conventions found
    - which structures were reused
    - whether any new migration was needed or not

    Close with a narrow Phase-49 suggestion around Public Archive Pages.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./... && go build ./...</automated>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck && npm run lint</automated>
    <automated>cd C:/Users/admin/Documents/Team4s && git diff --check</automated>
  </verify>
  <done>
    - Docs explain the dashboard's reuse and scoping model clearly.
    - Live verification proves the contributor experience and security boundaries.
    - The final closeout starts with the requested Ist-Analyse and ends with a clean Phase-49 handoff.
  </done>
</task>

</tasks>

<verification>
Automated:
1. `cd C:/Users/admin/Documents/Team4s/backend && go test ./...`
2. `cd C:/Users/admin/Documents/Team4s/backend && go build ./...`
3. `cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck`
4. `cd C:/Users/admin/Documents/Team4s/frontend && npm run lint`
5. `cd C:/Users/admin/Documents/Team4s && git diff --check`

Live:
1. Verify a logged-in contributor sees only own groups.
2. Verify foreign group and foreign release-context routing are blocked.
3. Verify capability-driven quick actions into reused sections.
4. Verify navigation exposes `Mein Profil` and `Meine Gruppen`.
5. Verify historical credits remain context only, not permissions.
</verification>

<success_criteria>
- Backend and frontend regressions cover the contributor dashboard's critical scoping paths.
- Developer docs explain reuse, capability wiring, and contributor safety clearly.
- The phase ends with a clean handoff to Public Archive Pages.
</success_criteria>
