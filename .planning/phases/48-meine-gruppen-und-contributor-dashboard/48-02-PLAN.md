---
phase: 48-meine-gruppen-und-contributor-dashboard
plan: "02"
type: execute
wave: 2
depends_on:
  - 48-01-PLAN.md
files_modified:
  - backend/internal/handlers
  - backend/internal/models
  - backend/internal/permissions
  - backend/internal/repository
  - docs
autonomous: true
requirements:
  - CONTRIBUTOR-DASHBOARD-01
must_haves:
  truths:
    - "The backend must scope every dashboard and group-detail read to canonical membership and permission truth."
    - "Capabilities in the dashboard are aggregated from the central permission engine, never from handler-local role logic."
    - "Direct URL or ID manipulation for foreign groups/releases must be blocked by the backend."
---

<objective>
Implement the backend contributor dashboard reads: own groups overview, contributor group detail, scoped anime/release/release-version reads, and capability aggregation.

Purpose: This is where the contributor area becomes safe and useful without creating new domain features.
</objective>

<context>
@.planning/phases/48-meine-gruppen-und-contributor-dashboard/48-01-PLAN.md
@.planning/phases/48-meine-gruppen-und-contributor-dashboard/48-CONTEXT.md
@.planning/phases/48-meine-gruppen-und-contributor-dashboard/48-RESEARCH.md
@backend/cmd/server/main.go
@backend/cmd/server/admin_routes.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement `GET /api/me/fansub-groups` and contributor group detail reads</name>
  <read_first>
    - backend/internal/handlers
    - backend/internal/repository
    - backend/internal/permissions
  </read_first>
  <action>
    Add the backend read surfaces for:
    - `GET /api/me/fansub-groups`
    - contributor-safe group detail read, likely by group id under a contributor route namespace

    Requirements:
    - authenticated actor only
    - disabled users are blocked
    - only own groups are returned for normal users
    - `platform_admin` keeps full or explicitly documented broader visibility
    - response includes group metadata, own roles, status, active time, counts, and capability booleans
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/handlers/... -run "Test.*MyGroups|Test.*ContributorGroup"</automated>
  </verify>
  <done>
    - The dashboard overview and contributor group detail endpoints exist.
    - Responses are capability-rich and scoped to the actor.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add contributor-scoped anime/release/release-version reads and coop handling</name>
  <read_first>
    - backend/internal/repository
    - existing release/media handlers and repos
  </read_first>
  <action>
    Extend or wrap existing read surfaces so contributor group pages can load:
    - assigned anime
    - releases
    - release versions
    - relevant counts or summary status

    Requirements:
    - strict group scoping
    - coop release visibility when the actor's group participates
    - no leak of foreign release versions
    - DB truth wins over route parameters
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/handlers/... ./internal/repository/... -run "Test.*ReleaseScope|Test.*Coop"</automated>
  </verify>
  <done>
    - Contributors can load their own group's release contexts safely.
    - Coop cases are visible only when appropriate.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend capability aggregation for contributor quick actions</name>
  <read_first>
    - backend/internal/permissions
    - existing fansub-group capability handler seam
  </read_first>
  <action>
    Extend capability aggregation for the dashboard/group cards with fields such as:
    - `canOpenContributorGroup`
    - `canEditGroup`
    - `canViewGroupMedia`
    - `canUploadGroupMedia`
    - `canViewReleases`
    - `canEditReleaseDescriptions`
    - `canUploadReleaseMedia`
    - `canManageMembers`

    These must derive from central permission actions and the real group context.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/permissions/... ./internal/handlers/... -run "Test.*Capabilities|Test.*MyGroups"</automated>
  </verify>
  <done>
    - Dashboard quick-action capabilities come from the central permission engine.
    - No handler-local role mapping is needed for UI action visibility.
  </done>
</task>

</tasks>

<verification>
Automated:
1. `cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/handlers/...`
2. `cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/repository/...`
3. `cd C:/Users/admin/Documents/Team4s/backend && go build ./...`

Behavioral:
1. User sees own groups only.
2. Foreign group and foreign release-context access is blocked.
3. Coop release visibility matches actual participating groups.
</verification>

<success_criteria>
- Backend overview/detail endpoints exist for contributor groups.
- Release/anime reads are safely scoped to the actor's groups.
- Capability aggregation supports contributor quick actions from the central engine.
</success_criteria>
