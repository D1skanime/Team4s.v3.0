---
phase: 62-fansub-contributions-admin-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/repository/hist_group_members_repository.go
  - backend/internal/repository/hist_group_member_roles_repository.go
autonomous: true
requirements:
  - P62-SC1
  - P62-SC5

must_haves:
  truths:
    - "HistGroupMembersRepository kann Eintraege in hist_fansub_group_members auflisten, anlegen, aktualisieren und loeschen"
    - "HistGroupMemberRolesRepository kann Rollen aus hist_group_member_roles auflisten, anlegen, aktualisieren und loeschen"
    - "Beide Repositories werden mit NewXxx(db *pgxpool.Pool) instanziiert und folgen dem bestehenden Repository-Stil"
  artifacts:
    - path: "backend/internal/repository/hist_group_members_repository.go"
      provides: "CRUD fuer hist_fansub_group_members"
      exports: ["HistGroupMembersRepository", "NewHistGroupMembersRepository", "HistGroupMemberInput", "HistGroupMemberPatchInput"]
    - path: "backend/internal/repository/hist_group_member_roles_repository.go"
      provides: "CRUD fuer hist_group_member_roles"
      exports: ["HistGroupMemberRolesRepository", "NewHistGroupMemberRolesRepository", "HistGroupMemberRoleInput", "HistGroupMemberRolePatchInput"]
  key_links:
    - from: "HistGroupMembersRepository.Create"
      to: "hist_fansub_group_members"
      via: "INSERT mit fansub_group_id, member_id, joined_year, left_year, status, visibility, created_by"
    - from: "HistGroupMemberRolesRepository.Create"
      to: "hist_group_member_roles"
      via: "INSERT mit hist_fansub_group_member_id, role_code, started_year, ended_year, status, created_by"
---

<objective>
Legt die beiden Repositories fuer historische Gruppenmitglieder und deren Rollen an: HistGroupMembersRepository fuer hist_fansub_group_members und HistGroupMemberRolesRepository fuer hist_group_member_roles.

Purpose: Die Handler in Wave 2 brauchen diese Repositories als Dependency. Beide Repos sind in sich abgeschlossen und haben keine gegenseitige Abhaengigkeit.
Output: Zwei neue Go-Repository-Dateien nach bestehendem Team4s-Stil.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/admin/Documents/Team4s/.planning/PROJECT.md
@C:/Users/admin/Documents/Team4s/.planning/ROADMAP.md
@C:/Users/admin/Documents/Team4s/.planning/phases/62-fansub-contributions-admin-api/62-CONTEXT.md
@C:/Users/admin/Documents/Team4s/.planning/phases/61-fansub-contributions-datenmodell/61-CONTEXT.md
</context>

<interfaces>
<!-- Aus Phase 61: Tabellenschema (gesichert durch 61-01/61-02 Migrationen) -->

hist_fansub_group_members:
  id BIGSERIAL PRIMARY KEY
  fansub_group_id BIGINT NOT NULL REFERENCES fansub_groups(id) ON DELETE RESTRICT
  member_id BIGINT NOT NULL REFERENCES members(id) ON DELETE RESTRICT
  joined_year INT NULL
  left_year INT NULL
  status VARCHAR(20) NOT NULL DEFAULT 'historical' -- CHECK ('draft','historical','confirmed','disputed')
  visibility VARCHAR(20) NOT NULL DEFAULT 'internal' -- CHECK ('internal','public')
  created_by BIGINT NULL REFERENCES app_users(id) ON DELETE SET NULL
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
  UNIQUE (fansub_group_id, member_id)

hist_group_member_roles:
  id BIGSERIAL PRIMARY KEY
  hist_fansub_group_member_id BIGINT NOT NULL REFERENCES hist_fansub_group_members(id) ON DELETE CASCADE
  role_code TEXT NOT NULL REFERENCES role_definitions(code) ON DELETE RESTRICT
  started_year INT NULL
  ended_year INT NULL
  status VARCHAR(20) NOT NULL DEFAULT 'historical' -- CHECK ('draft','historical','confirmed','disputed')
  visibility VARCHAR(20) NOT NULL DEFAULT 'internal' -- CHECK ('internal','public')
  confirmed_by BIGINT NULL
  confirmed_at TIMESTAMPTZ NULL
  source_note TEXT NULL
  created_by BIGINT NULL
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()

<!-- Bestehender Repository-Stil (aus fansub_group_app_members_repository.go) -->
type XxxRepository struct { db *pgxpool.Pool }
func NewXxxRepository(db *pgxpool.Pool) *XxxRepository { return &XxxRepository{db: db} }
-- Query-Muster: r.db.Query(ctx, `SELECT ...`) -> pgx.CollectRows
-- Single-row: r.db.QueryRow(ctx, `SELECT ...`).Scan(...)
-- Insert returning: r.db.QueryRow(ctx, `INSERT ... RETURNING id, ...`).Scan(...)
-- Fehler: errors.Is(err, pgx.ErrNoRows) -> repository.ErrNotFound
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: HistGroupMembersRepository — CRUD fuer hist_fansub_group_members</name>

  <read_first>
    - backend/internal/repository/fansub_group_app_members_repository.go — vollstaendiger Repository-Stil: Struct, Constructor, Query-Muster, pgx.CollectRows, ErrNotFound-Handling
    - backend/internal/repository/errors.go — ErrNotFound und ggf. weitere Fehlerkonstanten
    - backend/internal/repository/sql_errors.go — UniqueViolation-Erkennung (falls vorhanden)
    - database/migrations/0082_historical_fansub_group_members.up.sql — exakte Spaltennamen und Constraints
  </read_first>

  <action>
  Erstelle backend/internal/repository/hist_group_members_repository.go.

  Package: repository

  Typen:
  - HistGroupMemberRow struct mit Feldern: ID int64, FansubGroupID int64, MemberID int64, JoinedYear *int, LeftYear *int, Status string, Visibility string, CreatedBy *int64, CreatedAt time.Time, UpdatedAt time.Time
  - HistGroupMemberInput struct (fuer Create): FansubGroupID int64, MemberID int64, JoinedYear *int, LeftYear *int, Status string, Visibility string, CreatedBy *int64
  - HistGroupMemberPatchInput struct (fuer Update): JoinedYear **int (double pointer fuer explizites Null-Setzen), LeftYear **int, Status *string, Visibility *string

  Repository-Struct: HistGroupMembersRepository mit db *pgxpool.Pool
  Constructor: NewHistGroupMembersRepository(db *pgxpool.Pool) *HistGroupMembersRepository

  Methoden (alle mit ctx context.Context als erstem Parameter):

  ListByFansubGroup(ctx, fansubGroupID int64) ([]HistGroupMemberRow, error)
  - SELECT alle Spalten FROM hist_fansub_group_members WHERE fansub_group_id = $1 ORDER BY COALESCE(joined_year, 9999), id
  - pgx.CollectRows mit pgx.RowToStructByPos oder manuelles Scan

  GetByID(ctx, id int64) (*HistGroupMemberRow, error)
  - SELECT FROM hist_fansub_group_members WHERE id = $1
  - errors.Is(pgx.ErrNoRows) -> ErrNotFound

  Create(ctx, input HistGroupMemberInput) (*HistGroupMemberRow, error)
  - INSERT INTO hist_fansub_group_members (fansub_group_id, member_id, joined_year, left_year, status, visibility, created_by) VALUES ($1...$7) RETURNING alle Spalten
  - Bei UNIQUE-Verletzung (fansub_group_id + member_id): 409-faehigen Fehler zurueckgeben (IsDuplicateMemberError o.ae. analog zu bestehendem Stil aus fansub_group_app_members_repository.go pruefen)

  Update(ctx, id int64, input HistGroupMemberPatchInput) (*HistGroupMemberRow, error)
  - Nur gesetzte Felder aktualisieren (nil-Pointer bedeutet "nicht aendern")
  - UPDATE hist_fansub_group_members SET ..., updated_at = NOW() WHERE id = $N RETURNING alle Spalten
  - Wenn keine Spalte gesetzt: sofortige GetByID-Rueckgabe ohne UPDATE
  - errors.Is(pgx.ErrNoRows) -> ErrNotFound

  Delete(ctx, id int64) error
  - DELETE FROM hist_fansub_group_members WHERE id = $1
  - Gibt ErrNotFound wenn RowsAffected == 0

  Datei darf max 450 Zeilen haben (CLAUDE.md Constraint). Kein Kommentar-Overhead, kein toter Code.
  </action>

  <verify>
    <automated>grep -c "func.*HistGroupMembersRepository" backend/internal/repository/hist_group_members_repository.go</automated>
  </verify>

  <acceptance_criteria>
    - Datei backend/internal/repository/hist_group_members_repository.go existiert
    - package repository am Anfang der Datei
    - NewHistGroupMembersRepository(db *pgxpool.Pool) *HistGroupMembersRepository exportiert
    - ListByFansubGroup, GetByID, Create, Update, Delete Methoden vorhanden
    - errors.Is(err, pgx.ErrNoRows) an passenden Stellen zu repository.ErrNotFound gewandelt
    - Datei bleibt unter 450 Zeilen (wc -l oder grep -c '' gibt <= 450)
    - go build ./backend/... gibt keinen Kompilierfehler aus
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: HistGroupMemberRolesRepository — CRUD fuer hist_group_member_roles</name>

  <read_first>
    - backend/internal/repository/hist_group_members_repository.go — gerade erstellter Stil (Vorlage fuer dieses Repository)
    - database/migrations/0083_hist_group_member_roles.up.sql — exakte Spaltennamen inkl. confirmed_by, source_note
    - database/migrations/0085_role_definitions_seed.up.sql — role_definitions-Tabelle fuer role_code-Validierungsabfrage
  </read_first>

  <action>
  Erstelle backend/internal/repository/hist_group_member_roles_repository.go.

  Package: repository

  Typen:
  - HistGroupMemberRoleRow struct: ID int64, HistFansubGroupMemberID int64, RoleCode string, StartedYear *int, EndedYear *int, Status string, Visibility string, ConfirmedBy *int64, ConfirmedAt *time.Time, SourceNote *string, CreatedBy *int64, CreatedAt time.Time, UpdatedAt time.Time
  - HistGroupMemberRoleInput struct (fuer Create): HistFansubGroupMemberID int64, RoleCode string, StartedYear *int, EndedYear *int, Status string, Visibility string, SourceNote *string, CreatedBy *int64
  - HistGroupMemberRolePatchInput struct (fuer Update): StartedYear **int, EndedYear **int, Status *string, Visibility *string, SourceNote **string

  Repository-Struct: HistGroupMemberRolesRepository mit db *pgxpool.Pool
  Constructor: NewHistGroupMemberRolesRepository(db *pgxpool.Pool) *HistGroupMemberRolesRepository

  Methoden:

  ListByMember(ctx, histFansubGroupMemberID int64) ([]HistGroupMemberRoleRow, error)
  - SELECT alle Spalten FROM hist_group_member_roles WHERE hist_fansub_group_member_id = $1 ORDER BY COALESCE(started_year, 9999), id

  GetByID(ctx, id int64) (*HistGroupMemberRoleRow, error)
  - SELECT FROM hist_group_member_roles WHERE id = $1
  - ErrNotFound bei pgx.ErrNoRows

  RoleCodeExistsForContext(ctx, roleCode string, contextName string) (bool, error)
  - SELECT EXISTS(SELECT 1 FROM role_definitions WHERE code = $1 AND $2 = ANY(contexts))
  - Wird von Handlern vor Create/Update aufgerufen, um role_code gegen den Kontext 'group_history' zu validieren

  Create(ctx, input HistGroupMemberRoleInput) (*HistGroupMemberRoleRow, error)
  - INSERT INTO hist_group_member_roles (...) VALUES (...) RETURNING alle Spalten

  Update(ctx, id int64, input HistGroupMemberRolePatchInput) (*HistGroupMemberRoleRow, error)
  - Analog zu HistGroupMembersRepository.Update: nur gesetzte Felder, updated_at = NOW()

  Delete(ctx, id int64) error
  - DELETE FROM hist_group_member_roles WHERE id = $1, ErrNotFound bei RowsAffected == 0

  Datei darf max 450 Zeilen haben.
  </action>

  <verify>
    <automated>grep -c "func.*HistGroupMemberRolesRepository" backend/internal/repository/hist_group_member_roles_repository.go</automated>
  </verify>

  <acceptance_criteria>
    - Datei backend/internal/repository/hist_group_member_roles_repository.go existiert
    - NewHistGroupMemberRolesRepository(db *pgxpool.Pool) exportiert
    - RoleCodeExistsForContext(ctx, roleCode, contextName string) (bool, error) vorhanden — wird fuer Handler-seitige Validierung benoetigt
    - ListByMember, GetByID, Create, Update, Delete Methoden vorhanden
    - ErrNotFound konsistent zurueckgegeben
    - Datei bleibt unter 450 Zeilen
    - go build ./backend/... gibt keinen Kompilierfehler aus
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Handler → Repository | Nur validierte Inputs erreichen SQL-Abfragen |
| Repository → DB | Parametrisierte Queries verhindern SQL-Injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-62-01 | Tampering | hist_group_member_roles.role_code | mitigate | RoleCodeExistsForContext prueft gegen role_definitions.contexts vor INSERT/UPDATE im Handler-Layer |
| T-62-02 | Information Disclosure | hist_fansub_group_members.visibility | accept | Sichtbarkeitsfilter liegt in Public-Handler (Wave 3); Repository gibt alle Eintraege zurueck — Admin-Kontext ist privilegiert |
| T-62-SC | Tampering | npm/pip/cargo installs | accept | Keine Paketinstallation in dieser Phase (nur Go-Quellcode) |
</threat_model>

<verification>
Nach Implementierung:

1. go build ./backend/... -- kein Fehler
2. grep -c "NewHistGroupMembersRepository" backend/internal/repository/hist_group_members_repository.go -- muss >= 1 sein
3. grep -c "NewHistGroupMemberRolesRepository" backend/internal/repository/hist_group_member_roles_repository.go -- muss >= 1
4. grep -c "RoleCodeExistsForContext" backend/internal/repository/hist_group_member_roles_repository.go -- muss >= 1
5. wc -l backend/internal/repository/hist_group_members_repository.go -- muss <= 450 sein
6. wc -l backend/internal/repository/hist_group_member_roles_repository.go -- muss <= 450 sein
</verification>

<success_criteria>
- Beide Repository-Dateien existieren und kompilieren ohne Fehler
- HistGroupMembersRepository hat ListByFansubGroup, GetByID, Create, Update, Delete
- HistGroupMemberRolesRepository hat ListByMember, GetByID, RoleCodeExistsForContext, Create, Update, Delete
- Beide Repositories folgen dem pgxpool.Pool-Stil der bestehenden Codebase
- Keine Datei ueberschreitet 450 Zeilen
</success_criteria>

<output>
Erstelle .planning/phases/62-fansub-contributions-admin-api/62-01-SUMMARY.md wenn fertig.
</output>
