---
phase: 115-globale-suche-postgres-fts
plan: 06
type: execute
wave: 5
depends_on: ["115-05"]
files_modified:
  - frontend/src/app/suche/page.tsx
  - frontend/src/app/suche/SearchField.tsx
  - frontend/src/app/suche/SuggestionList.tsx
  - frontend/src/app/suche/SearchField.test.tsx
autonomous: true
requirements: [D-08]
must_haves:
  truths:
    - "D-08: Zentrales Suchfeld nutzt @/components/ui Input; Search-as-you-type ab ≥2 Zeichen, 250ms Debounce über useDebouncedSearch"
    - "D-08: Vorschläge sind nach Anime/Fansubgruppen gruppiert (Card + SectionHeader + Badge) mit Trefferzahl je Gruppe und 'Alle Treffer anzeigen'-Zeile"
    - "D-08: Tastaturbedienung (↓/↑/Enter/Esc) über Combobox-Muster (role=listbox, aria-activedescendant); URL-Suchzustand teilbar"
    - "UI-SPEC: Nur @/components/ui-Primitives; korrekte Umlaute; Datei ≤450 Zeilen"
  artifacts:
    - path: "frontend/src/app/suche/page.tsx"
      provides: "Suchseiten-Shell (PageHeader + URL-Zustand-Bootstrap + SearchField/SuggestionList)"
    - path: "frontend/src/app/suche/SearchField.tsx"
      provides: "Zentrales Suchfeld (Input) mit Debounce/Tastatur/Combobox"
    - path: "frontend/src/app/suche/SuggestionList.tsx"
      provides: "Gruppierte Vorschlagsliste (Anime/Fansubgruppen)"
  key_links:
    - from: "frontend/src/app/suche/SearchField.tsx"
      to: "useDebouncedSearch"
      via: "search-as-you-type + AbortController"
      pattern: "useDebouncedSearch"
---

<objective>
Die interaktive Sucheingabefläche: die Route `/suche` (Page-Shell mit PageHeader + URL-Zustand-
Bootstrap), das zentrale Suchfeld (`@/components/ui` `Input`, visueller Anker der Seite) mit
Search-as-you-type und Tastaturbedienung, und die nach Anime/Fansubgruppen gruppierte Vorschlagsliste.

Purpose: Erfüllt den Suchfeld-/Vorschlags-Teil des UI-SPEC Interaction Contracts; verbindet den
useDebouncedSearch-Hook (Plan 115-05) mit sichtbaren Primitives. Die Ergebnis-/Filterfläche folgt in
Plan 115-07 (erweitert diese page.tsx).
Output: page.tsx (Shell) + SearchField + SuggestionList + SearchField-Test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-globale-suche-postgres-fts/115-UI-SPEC.md
@.planning/phases/115-globale-suche-postgres-fts/115-PATTERNS.md
@.planning/phases/115-globale-suche-postgres-fts/115-05-SUMMARY.md

<interfaces>
Page-Analog — frontend/src/app/anime/page.tsx (:17-58): Promise-fähige searchParams-Typisierung,
  toNumber-Defaults, `export const dynamic = 'force-dynamic'`. Abweichung: interaktive Such-UX
  (Debounce/Abbruch/Combobox) → Client-Component(en) unter der Page; Page bleibt schlank.

UI-Primitives (nur @/components/ui — Barrel index.ts): Input, Card, SectionHeader, Badge, PageHeader,
  EmptyState, LoadingState, Button. NICHT AnimeBrowserFilters.tsx kopieren (bestehende Verletzung).

Copywriting Contract (UI-SPEC — exakte Strings, korrekte Umlaute):
  Placeholder „Anime oder Fansubgruppe suchen …"; aria-label „Suchbegriff eingeben";
  Vorschlags-Gruppen „Anime" / „Fansubgruppen"; „Alle Treffer für „{query}" anzeigen";
  Trefferzahl „{n} Treffer" (Singular „1 Treffer"); Empty-Initial „Wonach suchst du?" +
  „Gib einen Anime-Titel oder eine Fansubgruppe ein, um loszulegen."; Empty-zu-kurz
  „Bitte gib mindestens 2 Zeichen ein.". PageHeader-Titel „Suche".

Interaction Contract (UI-SPEC): ≥2 Zeichen, 250ms Debounce; Combobox-Tastatur ↓/↑/Enter/Esc;
  Dropdown role=listbox + aria-activedescendant; Fokusrückgabe beim Schließen; aria-live=polite
  für Zustandswechsel. Accent (--color-primary) nur für aktives Feld/markierten Vorschlag.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route-Shell /suche (page.tsx) mit URL-Zustand-Bootstrap</name>
  <files>frontend/src/app/suche/page.tsx</files>
  <read_first>
    - frontend/src/app/anime/page.tsx (searchParams-Auflösung, dynamic-Export)
    - frontend/src/app/suche/useDebouncedSearch.ts (Hook aus Plan 05)
    - .planning/phases/115-globale-suche-postgres-fts/115-UI-SPEC.md (Copywriting/Color)
  </read_first>
  <action>
    Erstelle frontend/src/app/suche/page.tsx: schlanke Route-Shell mit `PageHeader` (Titel „Suche"),
    Promise-fähiger searchParams-Auflösung (q/type/Filter/page) als Initialzustand, und Einbettung der
    Client-Komponenten SearchField + SuggestionList. Seiten-Hintergrund `--surface-canvas`; das
    Suchfeld dominiert das obere Drittel (visueller Anker, UI-SPEC Color). Empty-Initialzustand
    (`EmptyState` „Wonach suchst du?") wenn q leer. Datei bleibt schlank (≤450 Z.); der Ergebnis-/
    Filterbereich wird in Plan 115-07 ergänzt (hier Platzhalter/Slot vorsehen, kein toter Markup).
    Nur @/components/ui-Primitives.
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `test -f frontend/src/app/suche/page.tsx`
    - `grep -c "PageHeader" frontend/src/app/suche/page.tsx` ≥ 1
    - `grep -Ec "<input|<button|<select" frontend/src/app/suche/page.tsx` == 0 (keine nativen Elemente)
    - `cd frontend && npm run typecheck && npm run lint` fehlerfrei
  </acceptance_criteria>
  <done>Route /suche existiert, bootstrappt den URL-Suchzustand, zeigt den Initial-Empty-State.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SearchField (Input) — Debounce, Tastatur, Combobox</name>
  <files>frontend/src/app/suche/SearchField.tsx, frontend/src/app/suche/SearchField.test.tsx</files>
  <read_first>
    - frontend/src/components/ui/Input (InputProps, invalid?)
    - frontend/src/app/suche/useDebouncedSearch.ts (Plan 05)
    - .planning/phases/115-globale-suche-postgres-fts/115-UI-SPEC.md (Interaction Contract + Accessibility)
  </read_first>
  <behavior>
    - Eingabe ab 2 Zeichen löst (debounced) einen Suggestions-Request aus; <2 Zeichen nicht
    - ↓/↑ navigiert Vorschläge, Enter aktiviert den markierten Vorschlag bzw. löst Vollsuche aus, Esc schließt
    - Das Feld trägt aria-label „Suchbegriff eingeben" und der Placeholder ist exakt „Anime oder Fansubgruppe suchen …"
  </behavior>
  <action>
    Erstelle frontend/src/app/suche/SearchField.tsx (Client-Component): zentrales `Input` mit inline
    `lucide-react` `Search`-Icon, Placeholder/aria-label gemäß Copy, Debounce via useDebouncedSearch.
    Combobox-Tastatur (↓/↑/Enter/Esc) + `aria-activedescendant` für die Markierung im Dropdown; der
    markierte Vorschlag nutzt Accent (--color-primary). Fokusrückgabe beim Schließen. Erstelle
    SearchField.test.tsx (Vitest jsdom): Placeholder/aria-label vorhanden, <2-Zeichen löst keinen
    Request aus, Tastaturnavigation markiert Vorschläge, Esc schließt. Datei ≤450 Z., nur Primitives.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/app/suche/SearchField.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/app/suche/SearchField.test.tsx` grün
    - `grep -c "Suchbegriff eingeben" frontend/src/app/suche/SearchField.tsx` ≥ 1 (aria-label)
    - `grep -Ec "<input|<button|<select" frontend/src/app/suche/SearchField.tsx` == 0
    - Test deckt Tastaturnavigation UND <2-Zeichen-Guard ab
  </acceptance_criteria>
  <done>Suchfeld nutzt Input-Primitiv mit Debounce/Combobox-Tastatur; korrekte Copy; getestet.</done>
</task>

<task type="auto">
  <name>Task 3: SuggestionList — gruppierte Vorschläge (Anime/Fansubgruppen)</name>
  <files>frontend/src/app/suche/SuggestionList.tsx</files>
  <read_first>
    - frontend/src/components/ui/Card, SectionHeader, Badge
    - frontend/src/app/suche/SearchField.tsx (Task 2 — geteilter Zustand/Callbacks)
    - .planning/phases/115-globale-suche-postgres-fts/115-UI-SPEC.md (Component Inventory Vorschlags-Dropdown)
  </read_first>
  <action>
    Erstelle frontend/src/app/suche/SuggestionList.tsx (Client-Component): `Card`-Container mit zwei
    Gruppen — `SectionHeader` „Anime" und „Fansubgruppen" —, je Gruppe serverseitig begrenzt (max ~5)
    mit Trefferzahl als `Badge variant="neutral"` und einer „Alle Treffer für „{query}" anzeigen"-Zeile
    (`Button variant="ghost"`, navigiert zur Vollsuche). Dropdown als `role="listbox"`; jede Zeile
    `role="option"` mit id für aria-activedescendant (Combobox aus Task 2). LoadingState während der
    Suggestions-Abfrage; kein Layout-Shift. Nur Primitives, korrekte Umlaute, ≤450 Z.
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Anime" frontend/src/app/suche/SuggestionList.tsx` ≥ 1 und `grep -c "Fansubgruppen" ...` ≥ 1 (zwei Gruppen)
    - `grep -c "role=\"listbox\"\|role='listbox'" frontend/src/app/suche/SuggestionList.tsx` ≥ 1
    - `grep -Ec "<input|<button|<select" frontend/src/app/suche/SuggestionList.tsx` == 0 (nur Primitives)
    - `cd frontend && npm run typecheck && npm run lint` fehlerfrei
  </acceptance_criteria>
  <done>Gruppierte, begrenzte Vorschläge mit Trefferzahl + „alle anzeigen"-Zeile als Listbox.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Suchbegriff → Rendering | q und Ergebnistext werden im DOM gerendert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-06-01 | Tampering | XSS über q/Ergebnistext | mitigate | Ausschließlich React-Textknoten (auto-escaped); kein dangerouslySetInnerHTML in SearchField/SuggestionList |
| T-115-06-02 | Denial of Service | Request-Flut bei Tippen | mitigate | Debounce/Abbruch über useDebouncedSearch (Plan 05); ≥2-Zeichen-Guard |
| T-115-06-03 | Information Disclosure | Client zeigt nicht-öffentliche Daten | accept | Sichtbarkeit serverseitig erzwungen (Plan 03/04); Client rendert nur gelieferte Items |
| T-115-06-SC | Tampering | npm-Installs | mitigate | Keine neuen Pakete |
</threat_model>

<verification>
- `cd frontend && npm run typecheck && npm run lint` fehlerfrei.
- `cd frontend && npx vitest run src/app/suche/` grün.
- Keine nativen `<input>/<button>/<select>` in den neuen Dateien (Grep-Gate + ESLint no-restricted-syntax).
- Visuelle/Interaktions-Abnahme (Fokusführung, Accent-Nutzung, mobile Touch-Targets) → Live-UAT Plan 115-08.
</verification>

<success_criteria>
`/suche` rendert das zentrale Suchfeld als visuellen Anker mit Search-as-you-type (≥2 Zeichen, 250ms),
Combobox-Tastaturbedienung und gruppierten Vorschlägen — ausschließlich mit @/components/ui-Primitives,
korrekten Umlauten und ≤450 Zeilen je Datei.
</success_criteria>

<output>
Create `.planning/phases/115-globale-suche-postgres-fts/115-06-SUMMARY.md` when done.
</output>
