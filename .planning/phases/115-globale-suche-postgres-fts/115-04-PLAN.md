---
phase: 115-globale-suche-postgres-fts
plan: 04
type: execute
wave: 3
depends_on: ["115-03"]
files_modified:
  - backend/internal/handlers/search.go
  - backend/internal/handlers/search_test.go
  - backend/internal/repository/search_suggestions.go
  - backend/cmd/server/main.go
  - shared/contracts/openapi.yaml
  - scripts/smoke-search.ps1
autonomous: true
requirements: [D-07, D-08, D-09]
must_haves:
  truths:
    - "D-07: GET /api/v1/search läuft über das Go-Backend mit Params q,type,year_from,year_to,genre,tag,format,status,fansub_group,page,per_page,sort; Frontend greift nie direkt auf PostgreSQL zu"
    - "D-07: Param-Validierung nach bestehenden Konventionen (parsePositiveInt, badRequest, maxLength q, Enum-Whitelist type/sort); q ist Bind-Parameter, nie interpoliert"
    - "D-08: Optionaler GET /api/v1/search/suggestions liefert gruppierte, serverseitig begrenzte Vorschläge (Anime/Fansubgruppen)"
    - "D-09: Mindestlänge q≥2 und per_page-Cap serverseitig erzwungen; Suggestions per LIMIT begrenzt"
  artifacts:
    - path: "backend/internal/handlers/search.go"
      provides: "SearchHandler.Search + SearchHandler.Suggestions, Param-Validierung, Pagination-Envelope"
      exports: ["Search", "Suggestions"]
    - path: "shared/contracts/openapi.yaml"
      provides: "/api/v1/search (+ /suggestions) Pfad + Schemas"
      contains: "/api/v1/search"
    - path: "scripts/smoke-search.ps1"
      provides: "Keycloak-Direct-Grant Smoke-Harness für D-04/D-05/D-11/D-12 End-to-End"
  key_links:
    - from: "backend/cmd/server/main.go"
      to: "v1.GET(\"/search\", searchHandler.Search)"
      via: "öffentliche Route (kein authMiddleware; authOptional für Admin-Override)"
      pattern: "/search"
---

<objective>
Der öffentliche Suchendpunkt `GET /api/v1/search` (+ optional `/search/suggestions`): Param-Validierung
nach bestehenden Handler-Konventionen, Pagination-Envelope, Admin-Override für disabled-Anime, Route-
Wiring in main.go und der OpenAPI-Kontrakt. Zusätzlich das Validierungs-Harness `scripts/smoke-search.ps1`
(Keycloak-Direct-Grant), das die DB-Verhaltens-Validierungen (D-04/D-05/D-11/D-12) end-to-end prüfbar macht.

Purpose: Der Handler ist die entkoppelte Grenze (D-07) — das Frontend (Wellen C) konsumiert nur diesen
Kontrakt. Das Smoke-Skript löst RESEARCH Open Question 1 (kein Live-DB-Go-Harness vorhanden).
Output: search.go + search_test.go + optionale search_suggestions.go + Route + OpenAPI + Smoke-Skript.

## Offene-Fragen-Auflösung (RESEARCH Open Question 1 — verbindlich)
Validierungsansatz = **PowerShell-Smoke-Skript `scripts/smoke-search.ps1`** mit Keycloak-Direct-Grant-
Token (analog vorhandener `smoke-*`-Skripte), NICHT ein neuer Docker-Postgres-Go-Integrationsharness.
Begründung: es existiert kein DB-gebundener Go-Testharness, `smoke-*` ist die Projektkonvention, die
Bash-Sandbox erreicht Host-Ports nicht (echtes Terminal nötig). Ranking-Determinismus (D-05) wird
ZUSÄTZLICH als Pure-Function-Test der ORDER-BY-Komposition abgedeckt (Plan 115-03). Das Skript wird
hier angelegt und in Plan 115-08 vollständig gegen Live-Daten gefahren.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-globale-suche-postgres-fts/115-RESEARCH.md
@.planning/phases/115-globale-suche-postgres-fts/115-PATTERNS.md
@.planning/phases/115-globale-suche-postgres-fts/115-03-SUMMARY.md

<interfaces>
Handler-Analog — backend/internal/handlers/anime.go:
  Struct+Konstruktor (:31-61, explizite DI), Param-Validierung (:64-138): parsePositiveInt,
  c.DefaultQuery, strings.TrimSpace, per_page>100→100, len(q)>100→badRequest,
  Enum-Whitelist allowedAnimeStatuses/allowedContentTypes (:18-29),
  Admin-Gate (:119-127) middleware.CommentAuthIdentityFromContext → identity.IsPlatformAdmin,
  Envelope (:152-172) gin.H{"data":items,"meta":models.PaginationMeta{...}}.
  ZUSÄTZLICH für Suche: Mindestlänge q≥2 serverseitig erzwingen (DoS-Mitigation).

Route-Analog — backend/cmd/server/main.go:73 (NewAnimeHandler), :361 (v1.GET("/anime", animeHandler.List)).
  Öffentlich lesend → kein authMiddleware; Admin-Override via authOptional + Identity-Check im Handler.

OpenAPI-Analog — shared/contracts/openapi.yaml /api/v1/anime-Block (:564-653): parameters
  (page/per_page mit minimum/maximum/default; q maxLength:100; Enum-$refs), responses 200/400/500
  mit examples. Neue Params: type(alle|anime|fansub), year_from, year_to, genre, tag, format, status,
  fansub_group, sort. Neue Schemas PaginatedSearchResponse/SearchResultItem analog PaginatedAnimeResponse.
  BROWNFIELD: /api/v1/anime + /api/v1/fansubs unangetastet lassen.

Suggestions-Analog — backend/internal/repository/admin_content.go:210-280 (ListGenreTokens/
  filterGenreTokens): Präfix-priorisiertes Ranking + LIMIT (D-09). max ~5/Gruppe (UI-SPEC).

Smoke-Analog — scripts/smoke-fansubs.ps1 + Memory „Live-API-Verifikation per Token"
  (Keycloak Direct-Grant, csubs-leader/123, client team4s-frontend; realer Port via `docker ps`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SearchHandler (Search + Suggestions) mit Param-Validierung + Envelope</name>
  <files>backend/internal/handlers/search.go, backend/internal/handlers/search_test.go, backend/internal/repository/search_suggestions.go</files>
  <read_first>
    - backend/internal/handlers/anime.go (List :18-172 — vollständiges Validierungs-/Envelope-Muster)
    - backend/internal/handlers/fansub_test.go + Anime-Handler-Tests (Test-Vorlage)
    - backend/internal/repository/search_repository.go (Search-Signatur aus Plan 03)
    - backend/internal/repository/admin_content.go (ListGenreTokens :210-280 — Suggestions-Ranking/LIMIT)
  </read_first>
  <behavior>
    - GET /search ohne q oder mit len(q)<2 → 400 badRequest (deutsche Meldung, korrekte Umlaute)
    - len(q)>100 → 400; ungültiger page/per_page → 400; per_page>100 wird auf 100 geklemmt
    - ungültiger type (nicht alle|anime|fansub) bzw. sort → 400 (Enum-Whitelist)
    - gültige Anfrage → 200 mit gin.H{"data":..., "meta":PaginationMeta{...}}
    - disabled-Anime nur mit Admin-Identität sichtbar; sonst ausgeschlossen
  </behavior>
  <action>
    Erstelle handlers/search.go: `SearchHandler{ repo *repository.SearchRepository }` +
    `NewSearchHandler(...)` (explizite DI wie AnimeHandler). `Search(c *gin.Context)`: alle Params
    via parsePositiveInt/DefaultQuery/TrimSpace parsen; q trimmen, `len(q)<2`→badRequest,
    `len(q)>100`→badRequest; Enum-Whitelist für type (alle|anime|fansub, Default alle) und sort;
    per_page-Cap 100; Admin-Gate für IncludeDisabled exakt wie anime.go:119-127; SearchQuery bauen,
    `h.repo.Search(...)` aufrufen, Envelope mit models.PaginationMeta zurückgeben; Fehler über
    writeInternalErrorResponse. `Suggestions(c *gin.Context)`: q≥2, ruft
    repository.SearchSuggestions (search_suggestions.go, Präfix-priorisiert, LIMIT ~5/Gruppe),
    liefert gruppierte Vorschläge. Erstelle search_suggestions.go analog ListGenreTokens.
    Erstelle handlers/search_test.go mit Param-Validierungs-/Envelope-/Status-Code-Tests
    (badRequest bei q<2, q>100, ungültigem type/sort; 200-Envelope-Form). Datei(en) ≤450 Zeilen.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/handlers -run TestSearch</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && go test ./internal/handlers -run TestSearch` grün (Validierung + Envelope + Status-Codes)
    - `grep -c "len(q)" backend/internal/handlers/search.go` ≥ 1 und Mindestlänge-Check auf q<2 vorhanden
    - `grep -c "fmt.Sprintf" backend/internal/handlers/search.go` zeigt KEINE SQL-String-Bildung mit q (q geht nur als Struct-Feld an das Repository)
    - `cd backend && go build ./...` fehlerfrei
  </acceptance_criteria>
  <done>Handler validiert alle Params, klemmt per_page, gated disabled, liefert PaginationMeta-Envelope; Suggestions begrenzt.</done>
</task>

<task type="auto">
  <name>Task 2: Route-Wiring (main.go) + OpenAPI-Kontrakt</name>
  <files>backend/cmd/server/main.go, shared/contracts/openapi.yaml</files>
  <read_first>
    - backend/cmd/server/main.go (:73 NewAnimeHandler, :361 v1.GET("/anime", ...))
    - shared/contracts/openapi.yaml (/api/v1/anime-Block :564-653; PaginatedAnimeResponse-Schema)
  </read_first>
  <action>
    Konstruiere in main.go `searchRepo` (mit shared *pgxpool.Pool wie AnimeRepository) und
    `searchHandler := handlers.NewSearchHandler(searchRepo)`. Registriere `v1.GET("/search",
    searchHandler.Search)` und `v1.GET("/search/suggestions", searchHandler.Suggestions)` — öffentlich
    (kein authMiddleware); für den Admin-Override authOptional-Middleware wie beim vergleichbaren
    öffentlichen Anime-Pfad. Ergänze in openapi.yaml den `/api/v1/search`- (+ `/suggestions`-) Pfad
    analog `/api/v1/anime`: parameters (q maxLength:100, type/sort Enums, year_from/year_to/genre/
    tag/format/status/fansub_group, page/per_page mit minimum/maximum/default), responses 200/400/500
    mit examples; neue Schemas PaginatedSearchResponse/SearchResultItem. `/api/v1/anime` und
    `/api/v1/fansubs` NICHT verändern.
  </action>
  <verify>
    <automated>cd backend && go build ./cmd/server</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/search" backend/cmd/server/main.go` ≥ 1 (Route registriert)
    - `grep -c "/api/v1/search" shared/contracts/openapi.yaml` ≥ 1
    - `grep -c "PaginatedSearchResponse" shared/contracts/openapi.yaml` ≥ 1
    - `cd backend && go build ./cmd/server` fehlerfrei
    - `git diff --stat shared/contracts/openapi.yaml` zeigt nur additive Änderungen (keine Zeilenentfernung an /anime- oder /fansubs-Blöcken)
  </acceptance_criteria>
  <done>Route öffentlich registriert; OpenAPI-Kontrakt additiv ergänzt; Bestands-Endpunkte unangetastet.</done>
</task>

<task type="auto">
  <name>Task 3: scripts/smoke-search.ps1 (Validierungs-Harness, Keycloak Direct-Grant)</name>
  <files>scripts/smoke-search.ps1</files>
  <read_first>
    - scripts/smoke-fansubs.ps1 (Struktur/Token-Beschaffung/Assertion-Muster)
    - .planning/phases/115-globale-suche-postgres-fts/115-VALIDATION.md (Manual-Only-Tabelle + Wave-0)
  </read_first>
  <action>
    Erstelle scripts/smoke-search.ps1 analog smoke-fansubs.ps1: realen Backend-Port via `docker ps`
    ermitteln; optional Keycloak-Direct-Grant-Token (csubs-leader/123, client team4s-frontend) für
    den Admin-Override-Fall. Prüfschritte als Assertions (mit deutschen Ausgabemeldungen):
    (a) D-12 „Koe no Katachi"/„Eiga Koe no Katachi" → Ziel-Anime im Ergebnis;
    (b) D-04 „Narotu" findet „Naruto", „T4S"/„team-4s" findet Team4s über Alias;
    (c) D-05 exakter Haupttitel Rang 1 trotz populärerem Teiltreffer;
    (d) D-11 dissolved-Gruppe erscheint, disabled-Anime fehlt (ohne Admin-Token), erscheint (mit Admin);
    (e) D-07 Param-Fehler: q<2 → 400, q>100 → 400.
    Das Skript darf bei fehlender Live-Umgebung sauber mit Hinweis abbrechen (kein Hard-Fail im CI).
    Es wird in Plan 115-08 vollständig gegen Live-Daten gefahren; hier zählt Existenz + Struktur.
  </action>
  <verify>
    <automated>test -f scripts/smoke-search.ps1 && grep -c "api/v1/search" scripts/smoke-search.ps1</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/smoke-search.ps1` (Datei existiert)
    - `grep -c "api/v1/search" scripts/smoke-search.ps1` ≥ 1
    - `grep -ci "Koe no Katachi\|Narotu\|T4S\|dissolved" scripts/smoke-search.ps1` ≥ 3 (deckt D-12/D-04/D-11 ab)
    - PowerShell-Syntaxprüfung: Skript parst ohne Fehler (`pwsh -NoProfile -Command "..."` oder Review)
  </acceptance_criteria>
  <done>Smoke-Harness existiert und deckt D-04/D-05/D-11/D-12-End-to-End-Fälle ab (Ausführung in 115-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymer/eingeloggter Client → GET /api/v1/search | Öffentlicher, unauthentifizierter Eingang mit q/Filter-Params |
| Handler → Admin-Identität | disabled-Anime nur mit Platform-Admin-Identität |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-04-01 | Tampering | q/Filter-Injection | mitigate | Params gehen nur als Struct-Felder ins Repository (parametrisierte $n-Queries); keine SQL-String-Bildung im Handler |
| T-115-04-02 | Denial of Service | unbegrenztes q/per_page | mitigate | q-Mindestlänge≥2 + maxLength 100; per_page-Cap 100; Suggestions-LIMIT ~5/Gruppe (D-09) |
| T-115-04-03 | Information Disclosure | disabled-Anime-Leak | mitigate | Admin-Gate (IsPlatformAdmin) für IncludeDisabled exakt wie anime.go:119-127; sonst status<>'disabled' |
| T-115-04-04 | Denial of Service | invalider Enum/Sort → teure Query | mitigate | Enum-Whitelist type/sort → 400 badRequest vor Repository-Aufruf |
| T-115-04-SC | Tampering | Paketinstalls | mitigate | Keine neuen Pakete; PowerShell-Skript nutzt nur Bordmittel |
</threat_model>

<verification>
- `cd backend && go test ./internal/handlers/... ` grün (Param-Validierung/Envelope/Status).
- `cd backend && go build ./cmd/server` fehlerfrei; Route + OpenAPI additiv.
- Nach `docker compose up -d --build team4sv30-backend` ist `/api/v1/search` live erreichbar
  (Container-Rebuild-Pflicht, Memory „Backend ist Docker auf :8092").
- Live-End-to-End (D-04/D-05/D-11/D-12) via smoke-search.ps1 → Plan 115-08 (Manual-Only, VALIDATION.md).
</verification>

<success_criteria>
`GET /api/v1/search` (+ `/suggestions`) ist öffentlich registriert, validiert alle Params nach
Bestandskonventionen (q≥2, maxLength, Enum-Whitelist, per_page-Cap), gated disabled-Anime, liefert den
PaginationMeta-Envelope; OpenAPI additiv gepflegt; smoke-search.ps1 als Validierungs-Harness vorhanden.
</success_criteria>

<output>
Create `.planning/phases/115-globale-suche-postgres-fts/115-04-SUMMARY.md` when done.
</output>
