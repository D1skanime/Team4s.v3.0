---
phase: 115-globale-suche-postgres-fts
plan: 03
type: execute
wave: 2
depends_on: ["115-01", "115-02"]
files_modified:
  - backend/internal/models/search.go
  - backend/internal/repository/search_repository.go
  - backend/internal/repository/search_anime.go
  - backend/internal/repository/search_fansub.go
  - backend/internal/repository/search_repository_test.go
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-11]
must_haves:
  truths:
    - "D-03: Anime-Suche deckt anime.title + anime_titles (alle Typen inkl. romaji/japanese) + slug + Jahr + Typ + Genre + Tag ab; Fansub-Suche deckt name + slug + Aliase ab"
    - "D-04: Matching nutzt f_unaccent(<col>) + pg_trgm (Tippfehler/Akzent/Bindestrich), nicht ILIKE '%q%'"
    - "D-05: Ranking ist deterministisch als ORDER-BY-CASE-Stufen; ein exakter Treffer wird NIE durch Popularität verdrängt (Popularität nur letzte Tie-Break-Ebene)"
    - "D-11(1): Aufgelöste Fansubgruppen (dissolved) ERSCHEINEN — kein impliziter Statusausschluss"
    - "D-11(Anime-Sichtbarkeit): Anime mit status='disabled' sind ohne Admin-Identität ausgeschlossen"
    - "D-02: SearchProvider bleibt ein schmales Interface mit genau einer Postgres-Impl (kein Factory/Registry/Multi-Provider-Switch)"
  artifacts:
    - path: "backend/internal/models/search.go"
      provides: "SearchQuery, SearchResult, SearchSuggestion, schmales SearchProvider-Interface"
      contains: "SearchQuery"
    - path: "backend/internal/repository/search_anime.go"
      provides: "Anime-Matching (unaccent+trgm), D-05-Ranking, Sichtbarkeitsfilter"
    - path: "backend/internal/repository/search_fansub.go"
      provides: "Fansub-Matching (name/slug/aliases), dissolved erscheint, D-05-Ranking"
  key_links:
    - from: "search_anime.go"
      to: "anime_titles / genres / tags"
      via: "f_unaccent(<col>) % f_unaccent($n) + ts_rank"
      pattern: "f_unaccent"
    - from: "search_fansub.go"
      to: "fansub_group_aliases.normalized_alias"
      via: "exakter Alias-Match als Ranking-Stufe 1 (D-05 Kürzel)"
      pattern: "normalized_alias"
---

<objective>
Kern der Suche: die Postgres-Repository-Schicht, die Anime und Fansubgruppen über die neuen
`f_unaccent`-Trigram-Indizes und tsvector-Spalten (Plan 115-02) durchsucht, mit deterministischem
D-05-Ranking und serverseitiger Sichtbarkeitsfilterung. Ersetzt die abzulösende ungeindexte
`ILIKE '%q%'`-Art durch `f_unaccent()+pg_trgm` (Match) + `ts_rank`/CASE-Stufen (Ranking).

Purpose: Der Handler (Plan 115-04) ruft nur `Search(...)` auf; die gesamte Query-Komposition,
Gewichtung und Sichtbarkeit liegt hier (Architectural Responsibility Map: Matching = DB, gefiltert
vom Backend).
Output: models/search.go + search_repository.go (Komposition/Envelope) + search_anime.go +
search_fansub.go + Pure-Function-Tests der Ranking-/WHERE-Komposition.

## Offene-Fragen-Auflösungen (verbindlich in diesem Plan)
- **D-02 SearchProvider:** Ein schmales `SearchProvider`-Interface in models/search.go mit genau
  einer Postgres-Implementierung (SearchRepository). Kein Factory/Registry/Multi-Provider-Switch.
  Das Interface ist der dokumentierte Andockpunkt für den späteren Meilisearch-Provider (D-10,
  Plan 115-08). Begründung: D-02 skizziert das Interface explizit und es bleibt pattern-konform
  (analog bestehender Repository-Interfaces), ohne Overengineering.
- **Open Question 4 (Fansub-Beschreibung):** In v1 AUSGESCHLOSSEN. `fansub_groups.description` wurde
  in Migration 0071 entfernt; Freitext lebt nur noch in `fansub_group_notes` (visibility/status-gated).
  Eine korrekte Sichtbarkeitsprüfung dieser Notes lohnt den v1-Aufwand nicht (D-03 „falls sinnvoll").
  Fansub-Ranking-Stufe 6 „Beschreibung" entfällt daher in v1 — explizit dokumentiert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-globale-suche-postgres-fts/115-RESEARCH.md
@.planning/phases/115-globale-suche-postgres-fts/115-PATTERNS.md
@.planning/phases/115-globale-suche-postgres-fts/115-02-SUMMARY.md

<interfaces>
WHERE-Builder-Analog — backend/internal/repository/anime.go:330-394 (buildAnimeListWhere):
  conditions []string + args []any + argPos; ALLE Werte als $n-Bind-Params.
  q-Teiltreffer (:370-377) ist die ABZULÖSENDE ILIKE-Art — Struktur/argPos beibehalten,
  `ILIKE '%q%'` durch `f_unaccent(<col>) % f_unaccent($n)` (Match) + ts_rank (Rang) ersetzen.

Titel-Rang-Analog — backend/internal/repository/anime.go:298-328 (primaryNormalizedTitleSQL):
  CASE l.code WHEN 'ja' THEN 0 WHEN 'romaji' THEN 1 ... / CASE tt.name WHEN 'main' THEN 0 ...
  → Vorlage für D-05-CASE-Stufen. tt.name='romaji' ist bereits vorgesehen (passt zu D-12).

Anime-Sichtbarkeit — backend/internal/repository/anime.go:342-348:
  if filter.Status != "" {explizit} else if !filter.IncludeDisabled { "status <> 'disabled'" }

Fansub-WHERE-Analog — backend/internal/repository/fansub_repository.go:1338-1374 (buildFansubGroupWhere):
  name/slug/alias-Match; KEIN impliziter Statusausschluss (dissolved erscheint, D-11(1));
  Status nur bei explizitem status=-Param.

Envelope/Pagination — models.PaginationMeta{Total,Page,PerPage,TotalPages} (models/anime.go);
  Modell-Feldbenennung analog AdminAnimeFilter (models/admin_content.go:75-95).

Anti-Patterns (RESEARCH): Popularität/view_count NIE in Primärsortierung (nur letzte Tie-Break-Ebene);
  kein `unaccent(...) ILIKE` ohne funktionalen Index; anime_schema.go/anime_v2.go NICHT kopieren.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suchmodelle + schmales SearchProvider-Interface (Contract zuerst)</name>
  <files>backend/internal/models/search.go</files>
  <read_first>
    - backend/internal/models/admin_content.go (AdminAnimeFilter :75-95 — Feldform/*ptr-Optionalität)
    - backend/internal/models/anime.go (PaginationMeta)
    - .planning/phases/115-globale-suche-postgres-fts/115-CONTEXT.md (D-02 Interface-Skizze)
  </read_first>
  <action>
    Erstelle models/search.go mit `SearchQuery{ Q, Type, YearFrom, YearTo, Genre, Tag, Format,
    Status, FansubGroup, Page, PerPage, Sort, IncludeDisabled }` (Optionalität via *string/*int16
    analog AdminAnimeFilter), `SearchResultItem` (Typ-Diskriminator anime|fansub, Titel/Slug/Meta,
    JSON-Tags snake_case), `SearchSuggestion`, und `SearchResult{ Items, Total }` je Entitätstyp.
    `PaginationMeta` NICHT neu definieren — bestehende wiederverwenden. Definiere ein schmales
    `SearchProvider`-Interface mit `Search(ctx, SearchQuery) (SearchResult, error)` und
    `Suggest(ctx, string) ([]SearchSuggestion, error)`; SearchRepository (Task 2/3) implementiert es.
    Kein Factory/Registry. Kommentiere das Interface als Meilisearch-Andockpunkt (D-02/D-10).
  </action>
  <verify>
    <automated>cd backend && go build ./internal/models/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "type SearchQuery struct" backend/internal/models/search.go` == 1
    - `grep -c "type SearchProvider interface" backend/internal/models/search.go` == 1
    - `grep -c "PaginationMeta" backend/internal/models/search.go` == 0 (keine Neudefinition; Wiederverwendung im Handler-Envelope)
    - `cd backend && go build ./internal/models/...` fehlerfrei
  </acceptance_criteria>
  <done>Contract-Datei existiert; SearchProvider ist ein schmales Ein-Impl-Interface.</done>
</task>

<task type="auto">
  <name>Task 2: search_anime.go — unaccent+trgm-Matching, D-05-Ranking, Sichtbarkeit</name>
  <files>backend/internal/repository/search_anime.go</files>
  <read_first>
    - backend/internal/repository/anime.go (buildAnimeListWhere :330-394, primaryNormalizedTitleSQL :298-328)
    - .planning/phases/115-globale-suche-postgres-fts/115-02-SUMMARY.md (Namen der 0140-Indizes/tsvector-Spalten)
    - backend/internal/repository/admin_content.go (Genre/Tag-Token-Aggregation :210-280)
  </read_first>
  <action>
    Erstelle search_anime.go: WHERE-Builder-Muster (conditions/args/argPos, alle Werte $n-Bind)
    für Anime. Matching über `f_unaccent(anime.title) % f_unaccent($n)` PLUS EXISTS-Subquery auf
    `anime_titles` (alle Typen main/de/en/official/japanese/romaji/synonym) mit `f_unaccent(at.title)
    % f_unaccent($n)`, PLUS slug-Match. Filter D-06: year_from/year_to, genre, tag, format/type,
    status, fansub_group (nur gesetzte anhängen). Sichtbarkeit: `status <> 'disabled'` außer bei
    explizitem status= oder IncludeDisabled (Admin). Ranking als deterministische `ORDER BY CASE`-
    Stufen exakt gemäß D-05 Anime: (1) exakter Haupttitel, (2) exakter alt. Titel, (3) Präfix,
    (4) trgm-Ähnlichkeit, (5) Genre/Tag-Treffer, (6) tsvector/ts_rank Beschreibung; Popularität nur
    ALS LETZTE Tie-Break-Ebene. Datei ≤450 Zeilen (sonst Query-Helfer auslagern).
  </action>
  <verify>
    <automated>cd backend && go build ./internal/repository/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "f_unaccent" backend/internal/repository/search_anime.go` ≥ 2 (title + anime_titles)
    - `grep -c "ILIKE '%" backend/internal/repository/search_anime.go` == 0 (keine ungeindexte ILIKE-Art)
    - `grep -ci "view_count\|popularit" backend/internal/repository/search_anime.go` zeigt Popularität nur in der letzten ORDER-BY-Ebene (Code-Review-Assertion; nicht in CASE-Stufe 1)
    - `cd backend && go build ./internal/repository/...` fehlerfrei
    - Pure-Function-Test der ORDER-BY-/CASE-Komposition grün (Task-übergreifend, siehe search_repository_test.go)
  </acceptance_criteria>
  <done>Anime-Matching indexgestützt; D-05-Rangstufen deterministisch; Sichtbarkeit serverseitig.</done>
</task>

<task type="auto">
  <name>Task 3: search_fansub.go + search_repository.go — Aliase, dissolved erscheint, Komposition</name>
  <files>backend/internal/repository/search_fansub.go, backend/internal/repository/search_repository.go, backend/internal/repository/search_repository_test.go</files>
  <read_first>
    - backend/internal/repository/fansub_repository.go (buildFansubGroupWhere :1338-1374, scanFansubGroup)
    - backend/internal/repository/search_anime.go (aus Task 2 — Kompositionsvertrag)
    - backend/internal/repository/anime.go (Pagination/Total-Count-Muster)
  </read_first>
  <action>
    Erstelle search_fansub.go: Matching über `f_unaccent(fansub_groups.name) % f_unaccent($n)`,
    slug-Match, und EXISTS auf `fansub_group_aliases` mit `f_unaccent(fga.normalized_alias) %
    f_unaccent($n)`. KEIN impliziter Statusausschluss — `dissolved` erscheint (D-11(1)); Status nur
    bei explizitem status=-Param. Ranking D-05 Fansub als CASE-Stufen: (1) exaktes Kürzel = exakter
    Treffer auf `fansub_group_aliases.normalized_alias`, (2) exakter Name, (3) exakter Slug,
    (4) alt./früherer Name (weitere Aliase), (5) Teiltreffer Name; Stufe 6 Beschreibung ENTFÄLLT in
    v1 (siehe Objective). KEINE neue `abbreviation`-Spalte (Pitfall 4).
    Erstelle search_repository.go: `SearchRepository` mit `*pgxpool.Pool` (Konstruktor wie
    AnimeRepository), implementiert `models.SearchProvider`; `Search` disptacht nach `Type`
    (alle|anime|fansub), aggregiert Items + Total je Entität und paginiert. Alle drei Dateien
    ≤450 Zeilen.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/repository -run TestSearch</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "normalized_alias" backend/internal/repository/search_fansub.go` ≥ 1
    - `grep -ci "dissolved" backend/internal/repository/search_fansub.go` zeigt KEINEN Ausschlussfilter (dissolved wird nicht herausgefiltert)
    - `grep -c "abbreviation" backend/internal/repository/search_fansub.go` == 0 (keine erfundene Spalte)
    - `grep -c "var _ models.SearchProvider" backend/internal/repository/search_repository.go` == 1 (Interface-Assertion)
    - `cd backend && go test ./internal/repository -run TestSearch` grün (Ranking-/WHERE-Komposition pure-function)
  </acceptance_criteria>
  <done>Fansub-Aliase gewichtet; dissolved erscheint; SearchRepository implementiert SearchProvider und komponiert die Ergebnisse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Handler → SearchRepository | q/Filter-Params (untrusted) fließen als Bind-Parameter in SQL |
| SearchRepository → PostgreSQL | Query-Ausführung; Sichtbarkeit muss hier durchgesetzt werden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-03-01 | Tampering | q/Filter → SQL | mitigate | Ausschließlich parametrisierte pgx-Queries ($n); q/Filter NIE in SQL-String konkateniert (grep: keine fmt.Sprintf mit q-Wert) |
| T-115-03-02 | Information Disclosure | disabled-Anime im Ergebnis | mitigate | `status <> 'disabled'` außer bei Admin-Identität (IncludeDisabled); Filter serverseitig, nie clientseitig (V4) |
| T-115-03-03 | Denial of Service | breite/kurze Trigram-Muster | mitigate | Match über funktionale GIN-Indizes (Plan 02); Mindestlänge q≥2 im Handler (Plan 04); page_size-Cap im Handler |
| T-115-03-04 | Information Disclosure | Fansub-Notes (visibility-gated) | mitigate | Fansub-Beschreibung/Notes in v1 NICHT durchsucht (Open-Question-4-Auflösung) → kein Leak visibility-gated Freitext |
| T-115-03-SC | Tampering | Paketinstalls | mitigate | Keine neuen Pakete (nur pgx/vorhandene Deps) |
</threat_model>

<verification>
- `cd backend && go build ./...` + `go test ./internal/repository/...` grün.
- Grep-Gates (f_unaccent statt ILIKE, keine abbreviation-Spalte, kein dissolved-Ausschluss,
  Interface-Assertion) erfüllt.
- Live-DB-Verhalten (findet „Koe no Katachi", Ranking-Reihenfolge mit echten Daten,
  dissolved erscheint) ist NICHT hier automatisiert beweisbar → Plan 115-08 Smoke/UAT (Manual-Only,
  VALIDATION.md). Die ORDER-BY-/WHERE-KOMPOSITION ist hier pure-function getestet.
</verification>

<success_criteria>
SearchRepository durchsucht Anime (anime.title + alle anime_titles-Typen + slug + Genre/Tag) und
Fansubgruppen (name/slug/Aliase) indexgestützt über f_unaccent+pg_trgm, mit deterministischem
D-05-Ranking (Popularität nur Tie-Break), serverseitiger Sichtbarkeit (disabled aus, dissolved rein),
und implementiert ein schmales SearchProvider-Interface. Dateien ≤450 Zeilen.
</success_criteria>

<output>
Create `.planning/phases/115-globale-suche-postgres-fts/115-03-SUMMARY.md` when done.
</output>
