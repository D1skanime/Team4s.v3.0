---
phase: 81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe
plan: 02
type: execute
wave: 2
depends_on:
  - 81-01
files_modified:
  - database/migrations/0101_expand_release_version_groups_from_collaborations.up.sql
  - database/migrations/0101_expand_release_version_groups_from_collaborations.down.sql
  - database/migrations/0102_drop_collaboration_schema.up.sql
  - database/migrations/0102_drop_collaboration_schema.down.sql
autonomous: true
requirements:
  - P81-SC5
  - P81-SC7

must_haves:
  truths:
    - "Migration 0101 expandiert release_version_groups und anime_fansub_groups per ON CONFLICT DO NOTHING (D-11)"
    - "Migration 0101 deaktiviert Kombigruppen via status='dissolved' wenn RESTRICT-Refs vorhanden (D-12)"
    - "Migration 0101 löscht Kombigruppen hart wenn keinerlei Fremdreferenzen (D-12)"
    - "Migration 0101 setzt theme_segments.fansub_group_id=NULL für Kombigruppen-IDs (Pitfall 1)"
    - "Migration 0102 enthält Guard: RAISE EXCEPTION wenn noch aktive Kombigruppen"
    - "Migration 0102 droppt fansub_collaboration_members-Tabelle und group_type-Spalte"
    - "Migrations-Tests aus Plan 01 drehen von RED auf GREEN"
  artifacts:
    - path: "database/migrations/0101_expand_release_version_groups_from_collaborations.up.sql"
      provides: "Datenmigration: Expansion + Deaktivierung"
      contains: "ON CONFLICT (release_version_id, fansub_group_id) DO NOTHING"
    - path: "database/migrations/0101_expand_release_version_groups_from_collaborations.down.sql"
      provides: "No-op Down mit RAISE WARNING"
    - path: "database/migrations/0102_drop_collaboration_schema.up.sql"
      provides: "Schema-Drop mit Guard"
      contains: "DROP TABLE IF EXISTS fansub_collaboration_members"
    - path: "database/migrations/0102_drop_collaboration_schema.down.sql"
      provides: "Restore-Down (ADD COLUMN + CREATE TABLE)"
  key_links:
    - from: "0101.up.sql"
      to: "release_version_groups"
      via: "INSERT … ON CONFLICT DO NOTHING"
      pattern: "ON CONFLICT \\(release_version_id, fansub_group_id\\) DO NOTHING"
    - from: "0101.up.sql"
      to: "fansub_groups"
      via: "DO $$ LOOP deactivate/delete"
      pattern: "status = 'dissolved'"
    - from: "0102.up.sql"
      to: "fansub_groups"
      via: "DO $$ Guard + ALTER TABLE"
      pattern: "drop column if exists group_type"
---

<objective>
SQL-Migrationen 0101 (Daten-Expansion) und 0102 (Schema-Drop)

Zweck: Bestandsdaten von Kombigruppen auf echte Member-IDs umschreiben und anschließend die
Kollaborations-Entität (Tabelle + Spalte) aus dem Schema entfernen. Diese Wave liefert die
forward-only SQL ohne Code-Änderungen; der Go-Code kompiliert weiterhin gegen das alte Schema
(0102 kann nach dem Code-Deployment ausgeführt werden, enthält Guard).

Output: Zwei .up.sql-Dateien + je eine .down.sql; Migrations-Contract-Tests aus Plan 01 drehen GREEN.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-CONTEXT.md
@.planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-RESEARCH.md
@.planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-PATTERNS.md
</context>

<interfaces>
<!-- Bestehende Schema-Fakten, direkt aus RESEARCH.md Q1 + Q6 extrahiert. -->
<!-- Migrationen verwenden diese Spalten/Tabellen direkt. -->

Tabellen (relevant):
  release_version_groups (release_version_id BIGINT, fansub_group_id BIGINT, PRIMARY KEY (release_version_id, fansub_group_id))
  anime_fansub_groups (anime_id BIGINT, fansub_group_id BIGINT, is_primary BOOLEAN, notes TEXT, UNIQUE(anime_id, fansub_group_id))
  fansub_groups (id BIGINT, status VARCHAR CHECK('active','inactive','dissolved'), group_type VARCHAR(20) CHECK('group','collaboration'), updated_at TIMESTAMPTZ)
  fansub_collaboration_members (collaboration_id BIGINT REFERENCES fansub_groups(id) ON DELETE CASCADE, member_group_id BIGINT, PRIMARY KEY(collaboration_id, member_group_id))
  hist_fansub_group_members (fansub_group_id BIGINT REFERENCES fansub_groups(id) ON DELETE RESTRICT)
  anime_contributions (fansub_group_id BIGINT REFERENCES fansub_groups(id) ON DELETE RESTRICT)
  theme_segments (fansub_group_id BIGINT REFERENCES fansub_groups(id) ON DELETE CASCADE)
  segment_library (fansub_group_id BIGINT REFERENCES fansub_groups(id) ON DELETE CASCADE)

Analogmuster: database/migrations/0036_backfill_releases_from_episode_versions.up.sql (INSERT SELECT ON CONFLICT)
Analogmuster: database/migrations/0057_drop_release_version_groups_fansubgroup_id.up.sql (DO $$ Guard + DROP)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0101 — Datenmigration (Expansion + Deaktivierung)</name>
  <files>
    database/migrations/0101_expand_release_version_groups_from_collaborations.up.sql,
    database/migrations/0101_expand_release_version_groups_from_collaborations.down.sql
  </files>
  <read_first>
    - database/migrations/0036_backfill_releases_from_episode_versions.up.sql (INSERT SELECT ON CONFLICT Muster)
    - database/migrations/0057_drop_release_version_groups_fansubgroup_id.up.sql (DO $$ Guard-Muster, vollständig)
    - database/migrations/0015_fansub_collaborations.up.sql (Original-Schema: fansub_collaboration_members Spalten)
    - .planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-RESEARCH.md §"Forschungsfrage 6" (komplettes SQL ausgearbeitet)
    - .planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-PATTERNS.md §"0101_expand_release_version_groups_from_collaborations.up.sql"
  </read_first>
  <behavior>
    - Nach 0101.up.sql läuft: SELECT COUNT(*) FROM release_version_groups rvg JOIN fansub_groups fg ON fg.id=rvg.fansub_group_id WHERE fg.group_type='collaboration' → 0
    - Nach 0101.up.sql läuft: SELECT COUNT(*) FROM anime_fansub_groups afg JOIN fansub_groups fg ON fg.id=afg.fansub_group_id WHERE fg.group_type='collaboration' → 0
    - Kombigruppen ohne RESTRICT-Refs sind gelöscht (kein Eintrag mehr in fansub_groups mit group_type='collaboration' ohne hist/contrib/theme/segment-Refs)
    - Kombigruppen MIT RESTRICT-Refs haben status='dissolved'
    - theme_segments.fansub_group_id-Zeilen mit Kombigruppen-IDs wurden auf NULL gesetzt
    - 0101.down.sql enthält RAISE WARNING (kein struktureller Rollback möglich)
  </behavior>
  <action>
    Erstelle database/migrations/0101_expand_release_version_groups_from_collaborations.up.sql.

    Schritt 1 — release_version_groups expandieren:
    INSERT INTO release_version_groups (release_version_id, fansub_group_id)
    SELECT rvg.release_version_id, fcm.member_group_id
    FROM release_version_groups rvg
    JOIN fansub_groups fg ON fg.id = rvg.fansub_group_id AND fg.group_type = 'collaboration'
    JOIN fansub_collaboration_members fcm ON fcm.collaboration_id = fg.id
    ON CONFLICT (release_version_id, fansub_group_id) DO NOTHING;

    Schritt 2 — anime_fansub_groups expandieren:
    INSERT INTO anime_fansub_groups (anime_id, fansub_group_id, is_primary, notes)
    SELECT afg.anime_id, fcm.member_group_id, false, NULL
    FROM anime_fansub_groups afg
    JOIN fansub_groups fg ON fg.id = afg.fansub_group_id AND fg.group_type = 'collaboration'
    JOIN fansub_collaboration_members fcm ON fcm.collaboration_id = fg.id
    ON CONFLICT (anime_id, fansub_group_id) DO NOTHING;

    Schritt 3 — theme_segments bereinigen (Pitfall 1 aus RESEARCH.md):
    UPDATE theme_segments SET fansub_group_id = NULL
    WHERE fansub_group_id IN (SELECT id FROM fansub_groups WHERE group_type = 'collaboration');

    Schritt 4 — segment_library bereinigen (analog):
    UPDATE segment_library SET fansub_group_id = NULL
    WHERE fansub_group_id IN (SELECT id FROM fansub_groups WHERE group_type = 'collaboration');

    Schritt 5 — Kombigruppen-Zeilen aus release_version_groups entfernen:
    DELETE FROM release_version_groups
    WHERE fansub_group_id IN (SELECT id FROM fansub_groups WHERE group_type = 'collaboration');

    Schritt 6 — Kombigruppen-Zeilen aus anime_fansub_groups entfernen:
    DELETE FROM anime_fansub_groups
    WHERE fansub_group_id IN (SELECT id FROM fansub_groups WHERE group_type = 'collaboration');

    Schritt 7 — Kombigruppen deaktivieren oder löschen (DO $$ Guard wie in RESEARCH.md Q1):
    DO $$ Block: FOR collab_id IN SELECT id FROM fansub_groups WHERE group_type = 'collaboration':
      Prüfe has_restrict_refs = EXISTS(hist_fansub_group_members) OR EXISTS(anime_contributions);
      IF has_restrict_refs: UPDATE fansub_groups SET status = 'dissolved', updated_at = NOW();
      ELSE: DELETE FROM fansub_groups WHERE id = collab_id;
    END $$;

    Note: theme_segments und segment_library wurden in Schritten 3+4 bereinigt, daher tauchen sie
    im has_restrict_refs-Check hier NICHT mehr auf (CASCADE statt RESTRICT; außerdem schon NULL gesetzt).
    Nur hist_fansub_group_members (RESTRICT) und anime_contributions (RESTRICT) werden geprüft.

    Erstelle database/migrations/0101_expand_release_version_groups_from_collaborations.down.sql:
    DO $$ BEGIN RAISE WARNING 'Migration 0101 down: Rollback nicht unterstuetzt — Datenmigration ist irreversibel'; END $$;
  </action>
  <verify>
    <automated>cd backend && go test ./internal/migrations/... -run TestPhase81CollaborationExpansionMigrationContract -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    TestPhase81CollaborationExpansionMigrationContract dreht GREEN; alle assertContainsAll-Strings im SQL vorhanden.
    Dateiinhalt enthält: "insert into release_version_groups", "on conflict (release_version_id, fansub_group_id) do nothing",
    "insert into anime_fansub_groups", "status = 'dissolved'", "has_restrict_refs".
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration 0102 — Schema-Drop (fansub_collaboration_members + group_type)</name>
  <files>
    database/migrations/0102_drop_collaboration_schema.up.sql,
    database/migrations/0102_drop_collaboration_schema.down.sql
  </files>
  <read_first>
    - database/migrations/0057_drop_release_version_groups_fansubgroup_id.up.sql (vollständig — Guard-Muster + DROP-Syntax)
    - database/migrations/0057_drop_release_version_groups_fansubgroup_id.down.sql (ADD COLUMN IF NOT EXISTS Muster)
    - database/migrations/0015_fansub_collaborations.up.sql (Original-CREATE TABLE fansub_collaboration_members — für Down-SQL)
    - database/migrations/0009_fansub_groups.up.sql (Original group_type CHECK-Constraint — für Down-SQL)
    - .planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-PATTERNS.md §"0102_drop_collaboration_schema.up.sql"
  </read_first>
  <behavior>
    - Nach 0102.up.sql: SELECT * FROM information_schema.tables WHERE table_name='fansub_collaboration_members' → 0 Zeilen
    - Nach 0102.up.sql: SELECT * FROM information_schema.columns WHERE table_name='fansub_groups' AND column_name='group_type' → 0 Zeilen
    - 0102.up.sql schlägt EXCEPTION wenn noch fansub_groups mit group_type='collaboration' AND status='active' vorhanden
    - 0102.down.sql stellt group_type-Spalte + fansub_collaboration_members-Tabelle wieder her
  </behavior>
  <action>
    Erstelle database/migrations/0102_drop_collaboration_schema.up.sql.

    Guard-Block (DO $$):
    DECLARE active_collabs BIGINT;
    SELECT COUNT(*) INTO active_collabs FROM fansub_groups WHERE group_type = 'collaboration' AND status = 'active';
    IF active_collabs > 0: RAISE EXCEPTION 'Cannot drop collaboration schema: % active collaboration groups remain', active_collabs;

    Nach Guard:
    DROP TABLE IF EXISTS fansub_collaboration_members;
    ALTER TABLE fansub_groups DROP COLUMN IF EXISTS group_type;

    Erstelle database/migrations/0102_drop_collaboration_schema.down.sql:
    ALTER TABLE fansub_groups ADD COLUMN IF NOT EXISTS group_type VARCHAR(20) NOT NULL DEFAULT 'group'
    CHECK (group_type IN ('group', 'collaboration'));
    CREATE TABLE IF NOT EXISTS fansub_collaboration_members (
      collaboration_id BIGINT NOT NULL REFERENCES fansub_groups(id) ON DELETE CASCADE,
      member_group_id  BIGINT NOT NULL REFERENCES fansub_groups(id) ON DELETE CASCADE,
      added_at         TIMESTAMPTZ NOT NULL DEFAULT NOW(),
      PRIMARY KEY (collaboration_id, member_group_id),
      CONSTRAINT no_self_reference CHECK (collaboration_id != member_group_id)
    );
  </action>
  <verify>
    <automated>cd backend && go test ./internal/migrations/... -run TestPhase81CollaborationSchemaDropMigrationContract -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    TestPhase81CollaborationSchemaDropMigrationContract dreht GREEN.
    0102.up.sql enthält: "active_collabs", "raise exception", "drop table if exists fansub_collaboration_members", "drop column if exists group_type".
    0102.down.sql enthält: "add column if not exists group_type", "create table if not exists fansub_collaboration_members".
    Beide Migrations-Contract-Tests aus Plan 01 jetzt GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration-Runner → fansub_groups | Schreibzugriff auf status-Spalte; Guard verhindert unbeabsichtigtes Löschen |
| Migration-Runner → theme_segments/segment_library | NULL-Setzung von fansub_group_id; vermeidet Orphan-Joins post-Migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-MIG-01 | Tampering | 0101.up.sql DELETE-Schritt | mitigate | DO $$ Guard prüft hist_fansub_group_members + anime_contributions (RESTRICT) vor DELETE; Deaktivierung als Fallback (D-12) |
| T-81-MIG-02 | Denial of Service | 0102.up.sql DROP TABLE | mitigate | Guard RAISE EXCEPTION wenn active_collabs > 0; Code muss vor 0102 deployed sein |
| T-81-MIG-03 | Tampering | theme_segments.fansub_group_id NULL-Setzung | accept | Defensiver Schritt — Segment-Quelle wird NULL statt auf gelöschte Gruppe zu zeigen; keine Datenverlust-Fläche |
</threat_model>

<verification>
1. go test ./backend/internal/migrations/... -run TestPhase81 — beide Tests GREEN
2. wc -l database/migrations/0101_expand_release_version_groups_from_collaborations.up.sql — SQL vollständig
3. grep -i "on conflict" database/migrations/0101_expand_release_version_groups_from_collaborations.up.sql — vorhanden
4. grep -i "raise exception" database/migrations/0102_drop_collaboration_schema.up.sql — vorhanden
5. grep -i "drop column" database/migrations/0102_drop_collaboration_schema.up.sql — group_type vorhanden
</verification>

<success_criteria>
- 4 SQL-Dateien (up + down je Migration) vorhanden
- Beide Migrations-Contract-Tests aus Plan 01 drehen von RED auf GREEN
- 0101.up.sql: Expansion + Theme-Segment-Bereinigung + Kombigruppen-Deaktivierung/-Löschen mit Guard
- 0102.up.sql: Active-Collabs-Guard + DROP TABLE + DROP COLUMN
- 0102.down.sql: Vollständige Restore-Struktur für fansub_collaboration_members + group_type
</success_criteria>

<output>
Erstelle `.planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-02-SUMMARY.md` nach Abschluss.
</output>
