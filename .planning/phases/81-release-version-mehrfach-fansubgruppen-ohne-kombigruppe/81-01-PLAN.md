---
phase: 81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/internal/migrations/phase81_collaboration_removal_test.go
  - backend/internal/repository/episode_version_repository_write_helpers_test.go
  - backend/internal/repository/episode_version_repository_read_helpers_test.go
  - backend/internal/repository/episode_import_repository_test.go
  - frontend/src/components/anime/ReleaseVersionFansubChips.test.tsx
  - backend/internal/repository/episode_version_repository.go
  - backend/internal/repository/episode_version_repository_read_helpers.go
  - backend/internal/repository/episode_version_repository_write_helpers.go
autonomous: true
requirements:
  - P81-SC1
  - P81-SC2
  - P81-SC6
  - P81-SC7
  - P81-SC8

must_haves:
  truths:
    - "Backend-Test schlägt ROT fehl für N-fach-Junction-Schreiben (P81-SC1)"
    - "Backend-Test schlägt ROT fehl für Aggregations-Lesepfad (P81-SC2)"
    - "Backend-Test schlägt ROT fehl für ID-Validierung gegen ErrNotFound (P81-SC6)"
    - "Migrations-Test schlägt ROT fehl für DO $$ Guard + ON CONFLICT-Muster (P81-SC7)"
    - "Frontend-Komponenten-Test schlägt ROT fehl für mehrere Badge-Chips (P81-SC4)"
    - "episode_version_repository.go ist nach Split ≤ 450 Zeilen"
    - "Bestehende Kollaborations-Tests aus episode_import_repository_test.go entfernt"
  artifacts:
    - path: "backend/internal/migrations/phase81_collaboration_removal_test.go"
      provides: "Migrations-Contract-Tests (RED) für 0101 + 0102"
    - path: "backend/internal/repository/episode_version_repository_write_helpers_test.go"
      provides: "Source-Scan-Tests (RED) für N-fach-Upsert + ID-Validierung"
    - path: "backend/internal/repository/episode_version_repository_read_helpers_test.go"
      provides: "Source-Scan-Test (RED) für Aggregations-Scan mit FansubGroups[]"
    - path: "frontend/src/components/anime/ReleaseVersionFansubChips.test.tsx"
      provides: "Vitest-Test (RED) für Mehrfach-Badge-Anzeige"
    - path: "backend/internal/repository/episode_version_repository.go"
      provides: "Schlankere Kerndatei nach Split (≤ 450 Z), enthält struct + GetByID-Stub"
    - path: "backend/internal/repository/episode_version_repository_read_helpers.go"
      provides: "Ausgelagerter Lesepfad-Stub (kompiliert, noch LIMIT 1 — erst Plan 04 ändert das)"
    - path: "backend/internal/repository/episode_version_repository_write_helpers.go"
      provides: "Ausgelagerter Schreibpfad-Stub (kompiliert, noch Kombigruppen-Logik — erst Plan 03 ändert das)"
  key_links:
    - from: "episode_version_repository_write_helpers_test.go"
      to: "episode_version_repository_write_helpers.go"
      via: "os.ReadFile source-scan"
      pattern: "episode_version_repository_write_helpers.go"
    - from: "phase81_collaboration_removal_test.go"
      to: "database/migrations/0101_*.up.sql"
      via: "readMigrationFile helper"
      pattern: "0101_expand_release_version_groups_from_collaborations.up.sql"
---

<objective>
Wave-0-Testgerüst (RED) + Pflicht-Split von episode_version_repository.go

Zweck: Nyquist-Compliance sicherstellen — alle P81-Kernbehaviors (Schreiben, Lesen, Migration, Chips) haben
RED-Tests bevor irgendein Produktionscode geändert wird. Parallel wird episode_version_repository.go
(1246 Z) in drei ≤450-Z-Dateien gesplittet, da Änderungen ohne Split den Zeilengrenzwert weiter verletzen.

Output: 5 neue Test-Dateien (alle ROT), 2 neue Helper-Stub-Dateien, eine verkleinerte Kern-Datei,
bereinigte episode_import_repository_test.go (alte Kollab-Tests entfernt).
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-CONTEXT.md
@.planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-RESEARCH.md
@.planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-PATTERNS.md
@.planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-VALIDATION.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: episode_version_repository.go in 3 Dateien splitten (Pflicht-Decomposition)</name>
  <files>
    backend/internal/repository/episode_version_repository.go,
    backend/internal/repository/episode_version_repository_read_helpers.go,
    backend/internal/repository/episode_version_repository_write_helpers.go
  </files>
  <read_first>
    - backend/internal/repository/episode_version_repository.go (vollständig lesen — 1246 Z)
    - backend/internal/repository/episode_import_repository_release_helpers.go (Package-Header + Import-Muster, Zeilen 1–20)
    - .planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-RESEARCH.md §"Pflicht-Split für episode_version_repository.go"
    - .planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-PATTERNS.md §"episode_version_repository_read_helpers.go" + §"episode_version_repository_write_helpers.go"
  </read_first>
  <action>
    Ziel: Pflicht-Split ohne Behavior-Änderung — alle Funktionen wandern, aber kein Algorithmus ändert sich.

    Neue Datei episode_version_repository_read_helpers.go (package repository):
    Verschiebe aus episode_version_repository.go: listReleaseVariantsByAnimeID, scanReleaseVariantAsEpisodeVersion,
    buildGroupedEpisodeCounts, countReleaseVariantsByEpisodeNumber.
    Verschiebe auch das rowScanner-Interface hierher (oder belasse es in der Kernddatei — einheitlich entscheiden).
    Die Funktionen bleiben inhaltlich unverändert (LIMIT 1 bleibt drin — wird erst in Plan 04 geändert).

    Neue Datei episode_version_repository_write_helpers.go (package repository):
    Verschiebe aus episode_version_repository.go: syncEpisodeVersionSelectedGroups,
    upsertReleaseVersionGroup (sofern vorhanden als separate Funktion),
    applyEpisodeVersionReleaseMetadata, applyEpisodeVersionVariantMetadata,
    loadEpisodeVersionStateForUpdate, ensureEpisodeVersionStream.
    Inhalt bleibt unverändert (EffectiveGroup-Logik bleibt drin — wird erst in Plan 03 geändert).

    Verbleibende Kerndatei episode_version_repository.go:
    EpisodeVersionRepository struct + NewEpisodeVersionRepository + GetByID + Create + Update + ListGroupedByAnimeID.
    Ziel ≤ 450 Zeilen.

    Sicherstellen: package repository in allen drei Dateien; alle Importe korrekt (context, fmt, models, pgx/v5, pgxpool).
    Nach dem Split: go build ./backend/internal/repository/... muss ohne Fehler kompilieren.
  </action>
  <behavior>
    - Alle drei Dateien sind im package repository
    - go build ./backend/internal/repository/... läuft fehlerfrei durch
    - episode_version_repository.go hat nach Split ≤ 450 Zeilen
    - episode_version_repository_read_helpers.go und ..._write_helpers.go haben je ≤ 300 Zeilen
    - Keine Funktion ist doppelt vorhanden
  </behavior>
  <verify>
    <automated>cd backend && go build ./internal/repository/...</automated>
  </verify>
  <done>
    Drei Dateien vorhanden, go build grün, episode_version_repository.go ≤ 450 Zeilen,
    jede der drei Dateien ≤ 450 Zeilen, kein Kompilierungsfehler.
  </done>
</task>

<task type="tdd">
  <name>Task 2: Wave-0-RED-Tests — Backend Repository (P81-SC1/SC2/SC6/SC7/SC8)</name>
  <files>
    backend/internal/migrations/phase81_collaboration_removal_test.go,
    backend/internal/repository/episode_version_repository_write_helpers_test.go,
    backend/internal/repository/episode_version_repository_read_helpers_test.go,
    backend/internal/repository/episode_import_repository_test.go
  </files>
  <read_first>
    - backend/internal/migrations/fansub_integrity_test.go (vollständig — readMigrationFile + assertContainsAll Helfer)
    - backend/internal/migrations/phase67_release_version_credits_test.go (assertContainsAll Nutzungsmuster)
    - backend/internal/repository/episode_import_repository_test.go (vollständig — Source-Scan-Muster L145–266, zu entfernende Tests)
    - .planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-PATTERNS.md §"phase81_collaboration_removal_test.go" + §"episode_version_repository_write_helpers_test.go"
    - .planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-RESEARCH.md §"Forschungsfrage 7: Test-Landschaft"
  </read_first>
  <action>
    Neue Datei backend/internal/migrations/phase81_collaboration_removal_test.go (package migrations):
    Nutzt readMigrationFile (bereits im Package vorhanden — KEIN Duplicate). Falls assertContainsAll ebenfalls
    bereits vorhanden ist, direkt nutzen; sonst lokal definieren.
    Test TestPhase81CollaborationExpansionMigrationContract: liest 0101_expand_release_version_groups_from_collaborations.up.sql
    (Datei existiert noch nicht → test MUSS fehlschlagen mit os.ReadFile-Fehler → RED-Zustand korrekt).
    Prüft strings.ToLower(content) auf: "insert into release_version_groups",
    "on conflict (release_version_id, fansub_group_id) do nothing",
    "insert into anime_fansub_groups", "delete from release_version_groups",
    "status = 'dissolved'", "has_restrict_refs".
    Test TestPhase81CollaborationSchemaDropMigrationContract: liest 0102_drop_collaboration_schema.up.sql + .down.sql.
    Prüft: "active_collabs", "raise exception", "drop table if exists fansub_collaboration_members",
    "drop column if exists group_type". Down prüft: "add column if not exists group_type",
    "create table if not exists fansub_collaboration_members".

    Neue Datei backend/internal/repository/episode_version_repository_write_helpers_test.go (package repository):
    Test TestSyncEpisodeVersionSelectedGroups_WritesNJunctionRows: os.ReadFile("episode_version_repository_write_helpers.go"),
    prüft strings.Contains auf "INSERT INTO release_version_groups" + "ON CONFLICT" + "DO NOTHING" + alle außer $2 Muster fehlen noch → RED.
    Test TestResolveImportFansubMemberGroups_RejectsUnknownGroupID: os.ReadFile("episode_import_repository_release_helpers.go"),
    prüft dass "ErrNotFound" vor "upsertImportFansubGroup" steht (Index-Check) — funktioniert schon eventuell → Prüfe ob RED.
    Test TestBuildAnimeFansubLinkGroupIDs_UsesOnlyMemberGroups: Struct-Test — ruft buildAnimeFansubLinkGroupIDs mit 3 resolvedImportFansubGroup-Einträgen auf
    (inkl. Duplikat, ID 3+4+3), erwartet Ergebnis-Länge 2. Schlägt fehl da Funktion noch EffectiveGroup erwartet → RED.

    Neue Datei backend/internal/repository/episode_version_repository_read_helpers_test.go (package repository):
    Test TestScanEpisodeVersion_ReturnsGroupsList: os.ReadFile("episode_version_repository_read_helpers.go"),
    prüft dass "FansubGroups" (Plural) im Source-String vorkommt — schlägt fehl da noch "FansubGroup" (Singular) → RED.

    Änderung episode_import_repository_test.go:
    Entferne vollständig: TestBuildImportCollaborationName_IsStableAcrossSelectionOrder (L145),
    TestEpisodeImportReleaseHelpers_SourceCreatesMembersBeforeCollaborationLink (L164).
    Behalte: TestBuildAnimeFansubLinkGroupIDs_IsIdempotentAcrossRepeatedApply (L183) — wird in diesem Schritt NICHT geändert
    (Rewrite erfolgt in Plan 03 gleichzeitig mit Funktionsänderung).
    Behalte: TestEpisodeImportApply_UsesReleaseNativeTablesOnly (L227) — wird in Plan 03 rewritten.
    Alle verbleibenden Tests müssen weiterhin kompilieren.
  </action>
  <behavior>
    - TestPhase81CollaborationExpansionMigrationContract schlägt RED fehl (Migrations-Datei noch nicht vorhanden)
    - TestSyncEpisodeVersionSelectedGroups_WritesNJunctionRows schlägt RED fehl (kein DO NOTHING im Write-Helper-Stub)
    - TestBuildAnimeFansubLinkGroupIDs_UsesOnlyMemberGroups schlägt RED fehl (Signatur stimmt noch nicht)
    - TestScanEpisodeVersion_ReturnsGroupsList schlägt RED fehl (noch "FansubGroup" Singular)
    - episode_import_repository_test.go kompiliert ohne die gelöschten Tests
    - go test ./backend/internal/repository/... und go test ./backend/internal/migrations/... kompilieren (auch wenn Tests faillen)
  </behavior>
  <verify>
    <automated>cd backend && go test ./internal/repository/... ./internal/migrations/... 2>&1 | grep -E "(FAIL|PASS|error)" | head -30</automated>
  </verify>
  <done>
    Alle 5 neuen Test-Funktionen vorhanden; Kompilierung ohne Fehler; die vier RED-Tests schlagen mit
    inhaltlichen Assertions fehl (nicht mit Kompilierungsfehlern); alte Kollab-Tests entfernt.
  </done>
</task>

<task type="tdd">
  <name>Task 3: Wave-0-RED-Test — Frontend Chip-Anzeige (P81-SC4)</name>
  <files>
    frontend/src/components/anime/ReleaseVersionFansubChips.test.tsx
  </files>
  <read_first>
    - frontend/src/app/admin/my-groups/[id]/page.test.tsx (vollständig — Vitest-Muster, jsdom-Direktive, Badge-Import)
    - frontend/src/components/anime/ReleaseVersionBreakdown.tsx (aktuelle Gruppen-Anzeige-Logik)
    - frontend/src/types/episodeVersion.ts (aktuelle FansubGroupSummary + episodeVersion Typen)
    - frontend/src/types/fansub.ts (FansubGroupSummary-Definition)
    - .planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-PATTERNS.md §"Chip-Anzeige"
  </read_first>
  <action>
    Neue Datei frontend/src/components/anime/ReleaseVersionFansubChips.test.tsx.
    Vitest-Direktive @vitest-environment jsdom an Dateianfang (Kommentar-Syntax: // @vitest-environment jsdom).
    Import: { render, screen } from '@testing-library/react', { describe, it, expect } from 'vitest'.
    Import der Zielkomponente: import ReleaseVersionFansubChips from './ReleaseVersionFansubChips' —
    Datei existiert noch nicht → Import schlägt fehl → RED-Zustand.

    Testfall 1 - rendert mehrere Chips für fansub_groups[]:
    Gibt zwei FansubGroupSummary-Objekte (id: 1, name: "FlameHazeSubs"; id: 2, name: "SubsRus") als Props.
    expect(screen.getByText("FlameHazeSubs")).toBeDefined()
    expect(screen.getByText("SubsRus")).toBeDefined()

    Testfall 2 - rendert keinen Chip wenn fansub_groups leer:
    Leeres Array übergeben, expect(screen.queryByRole("status")).toBeNull() oder ähnliche Prüfung.

    Testfall 3 - Chips sind alphabetisch sortiert (D-04):
    Übergibt [{id:2, name:"Zebra"}, {id:1, name:"Alpha"}].
    Erwartet: Alpha erscheint vor Zebra (queryAllByText + index-Check oder getByTestId-Reihenfolge).

    KEIN fenced code block im Action-Text. Testdatei schlägt RED fehl weil ReleaseVersionFansubChips.tsx
    noch nicht existiert.
  </action>
  <behavior>
    - Test-Datei kompiliert nicht (Importfehler weil ReleaseVersionFansubChips.tsx fehlt) → RED
    - Alle drei Testfälle beschreiben messbares Verhalten
    - Keine nativen select/input/button-Elemente im Test (nur Badge-getriebene Ausgabe prüfen)
  </behavior>
  <verify>
    <automated>cd frontend && npx vitest run src/components/anime/ReleaseVersionFansubChips.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <done>
    Test-Datei vorhanden; npx vitest run schlägt RED fehl mit Importfehler oder Assertion-Fehler;
    keine Kompilierungsfehler außerhalb der fehlenden Zieldatei.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-Runner → Quellcode | Source-Scan-Tests lesen Produktionscode-Dateien über os.ReadFile — kein Netzwerkzugriff, rein lokal |
| Test-Runner → Migrations-Dateien | readMigrationFile liest database/migrations/*.sql über Dateisystem — kein externe Eingabe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-01-SC | Tampering | Split-Aktion episode_version_repository.go | accept | Rein struktureller Refactor ohne Algorithmus-Änderung; go build verifiziert Korrektheit sofort |
| T-81-01-TEST | Information Disclosure | Test-Dateien lesen Quellcode via os.ReadFile | accept | Tests liegen im selben Repo; kein sensitiver Datenzugriff; Standardmuster im Projekt |
</threat_model>

<verification>
1. go build ./backend/internal/repository/... — grün (kein Kompilierungsfehler nach Split)
2. wc -l backend/internal/repository/episode_version_repository.go — ≤ 450
3. go test ./backend/internal/repository/... ./backend/internal/migrations/... — kompiliert, RED-Tests schlagen mit Assertions fehl (nicht panic)
4. cd frontend && npx vitest run src/components/anime/ReleaseVersionFansubChips.test.tsx — RED
5. git grep "TestBuildImportCollaborationName" backend/ — gibt keinen Treffer (Test gelöscht)
</verification>

<success_criteria>
- episode_version_repository.go ≤ 450 Zeilen; _read_helpers.go + _write_helpers.go neu vorhanden; go build grün
- 5 RED-Testfunktionen angelegt (4 Backend, 1 Frontend); kompilieren ohne Syntax-Fehler
- Alte Kollab-Tests (TestBuildImportCollaborationName, TestSourceCreatesMembersBeforeCollaborationLink) aus episode_import_repository_test.go entfernt
- Kein Produktionsalgorithmus geändert — rein strukturell
</success_criteria>

<output>
Erstelle `.planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-01-SUMMARY.md` nach Abschluss.
</output>
