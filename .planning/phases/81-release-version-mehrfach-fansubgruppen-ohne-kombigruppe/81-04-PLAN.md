---
phase: 81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe
plan: 04
type: execute
wave: 4
depends_on:
  - 81-03
files_modified:
  - backend/internal/repository/episode_version_repository.go
  - backend/internal/repository/episode_version_repository_read_helpers.go
  - backend/internal/repository/fansub_repository.go
  - backend/internal/handlers/fansub_collaborations.go
  - backend/cmd/server/main.go
  - shared/contracts/episode-versions.yaml
  - shared/contracts/admin-content.yaml
  - shared/contracts/fansubs.yaml
autonomous: true
requirements:
  - P81-SC2
  - P81-SC3

must_haves:
  truths:
    - "GetByID und listReleaseVariantsByAnimeID nutzen json_agg; kein LIMIT 1 mehr (D-08)"
    - "scanReleaseVariantAsEpisodeVersion scannt fansubGroupsJSON []byte und unmarshalt zu FansubGroups (D-08)"
    - "fansub_repository.go: Kollaborations-Zweig in GetPublicProfileBySlug entfernt (D-02)"
    - "fansub_repository.go: ListCollaborationMembers+AddCollaborationMember+RemoveCollaborationMember entfernt (D-02)"
    - "main.go: GET/POST/DELETE /fansubs/:id/collaboration-members Routen entfernt (D-02)"
    - "episode-versions.yaml: fansub_group → fansub_groups (Array-Schema) (D-08)"
    - "admin-content.yaml: EpisodeVersion.fansub_group → fansub_groups (Array); collaboration_group_id entfernt (D-08)"
    - "fansubs.yaml: Kollaborations-Endpoint-Definitionen entfernt (D-02)"
    - "Source-Scan-Test TestScanEpisodeVersion_ReturnsGroupsList dreht GREEN"
    - "go build ./backend/... grün"
  artifacts:
    - path: "backend/internal/repository/episode_version_repository_read_helpers.go"
      provides: "json_agg-Aggregation in GetByID+listReleaseVariantsByAnimeID; kein LIMIT 1"
    - path: "shared/contracts/episode-versions.yaml"
      provides: "fansub_groups als Array-Schema-Feld"
    - path: "shared/contracts/admin-content.yaml"
      provides: "EpisodeVersion.fansub_groups Array; collaboration_group_id entfernt"
    - path: "shared/contracts/fansubs.yaml"
      provides: "Kollaborations-Endpunkte entfernt"
  key_links:
    - from: "episode_version_repository_read_helpers.go"
      to: "release_version_groups JOIN fansub_groups"
      via: "json_agg(json_build_object(...)) FILTER (WHERE fg.id IS NOT NULL)"
      pattern: "json_agg"
    - from: "scanReleaseVariantAsEpisodeVersion"
      to: "models.EpisodeVersion.FansubGroups"
      via: "json.Unmarshal(fansubGroupsJSON, &item.FansubGroups)"
      pattern: "json.Unmarshal"
---

<objective>
Backend-Lesepfad: LIMIT 1 entfernen, json_agg einbauen, Kollaborations-Handler + Routen entfernen

Zweck: Der Lesepfad für Release-Versionen aggregiert nun alle Gruppen pro Version als JSON-Array
statt nur eine zurückzugeben. Gleichzeitig werden die drei Kollaborations-Endpunkte
(GET/POST/DELETE /fansubs/:id/collaboration-members) aus main.go und der Handler-Datei entfernt,
und der Kollaborations-Zweig in fansub_repository.go wird bereinigt.
OpenAPI-Contracts (episode-versions.yaml, admin-content.yaml, fansubs.yaml) werden auf das neue
Array-Schema aktualisiert.

Output: Kompiliertes Backend mit korrektem Mehrfach-Lesepfad; kein LIMIT 1 in Release-Version-Queries;
kein Kollaborations-HTTP-Endpunkt mehr erreichbar; Source-Scan-Test dreht GREEN.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-CONTEXT.md
@.planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-RESEARCH.md
@.planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-PATTERNS.md
</context>

<interfaces>
<!-- SQL-Aggregationsmuster aus PATTERNS.md §"episode_version_repository_read_helpers.go" -->

Neues SQL-Muster für SELECT (statt GROUP BY fg.*):
  COALESCE(
    json_agg(
      json_build_object('id', fg.id, 'slug', fg.slug, 'name', fg.name, 'logo_url', fg.logo_url)
      ORDER BY fg.name ASC, fg.id ASC
    ) FILTER (WHERE fg.id IS NOT NULL),
    '[]'::json
  ) AS fansub_groups

Neuer Scanner-Aufruf in Go (statt &groupID, &groupSlug...):
  var fansubGroupsJSON []byte
  dest = append(dest, &fansubGroupsJSON)
  // Nach scan:
  if err := json.Unmarshal(fansubGroupsJSON, &item.FansubGroups); err != nil { return error }

rowScanner interface (bleibt wie definiert in episode_version_repository.go oder read_helpers.go):
  interface { Scan(dest ...any) error }

Zu entfernende Routen in main.go (L455–465):
  v1.GET("/fansubs/:id/collaboration-members", fansubHandler.ListCollaborationMembers)
  v1.POST("/fansubs/:id/collaboration-members", authMiddleware, fansubHandler.AddCollaborationMember)
  v1.DELETE("/fansubs/:id/collaboration-members/:memberGroupId", authMiddleware, fansubHandler.RemoveCollaborationMember)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lesepfad auf json_agg umstellen — LIMIT 1 entfernen</name>
  <files>
    backend/internal/repository/episode_version_repository_read_helpers.go,
    backend/internal/repository/episode_version_repository.go
  </files>
  <read_first>
    - backend/internal/repository/episode_version_repository_read_helpers.go (vollständig — nach Split aus Plan 01)
    - backend/internal/repository/episode_version_repository.go (GetByID-Funktion + LIMIT 1-Zeilen; nach Split aus Plan 01)
    - .planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-PATTERNS.md §"episode_version_repository_read_helpers.go"
    - .planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-RESEARCH.md §"Architekturmuster: Aggregation im Lesepfad"
  </read_first>
  <behavior>
    - listReleaseVariantsByAnimeID: kein GROUP BY fg.id/slug/name/logo_url; stattdessen json_agg(json_build_object(...)) ORDER BY fg.name ASC, fg.id ASC FILTER (WHERE fg.id IS NOT NULL)
    - GetByID in episode_version_repository.go: LIMIT 1 aus der Group-Join-Query entfernen; json_agg-Muster analog einsetzen
    - scanReleaseVariantAsEpisodeVersion: var fansubGroupsJSON []byte; Unmarshal nach &item.FansubGroups
    - kein &groupID, &groupSlug, &groupName, &groupLogoURL mehr im Scan
    - go build ./backend/internal/repository/... kompiliert
    - Source-Scan-Test TestScanEpisodeVersion_ReturnsGroupsList dreht GREEN (FansubGroups im Source)
  </behavior>
  <action>
    In episode_version_repository_read_helpers.go:
    In der SQL-Query von listReleaseVariantsByAnimeID:
    Ersetze alle fg.id, fg.slug, fg.name, fg.logo_url im SELECT durch das json_agg-Muster.
    Entferne fg.id, fg.slug, fg.name, fg.logo_url aus dem GROUP BY.
    Prüfe ob LIMIT 1 am Query-Ende existiert — falls ja entfernen.
    Der LEFT JOIN auf release_version_groups und fansub_groups bleibt erhalten.

    Ändere scanReleaseVariantAsEpisodeVersion:
    Ersetze var groupID *int64, var groupSlug *string, var groupName *string, var groupLogoURL *string durch var fansubGroupsJSON []byte.
    Im dest-Slice: fansubGroupsJSON statt der vier Einzel-Pointer.
    Nach scanner.Scan: json.Unmarshal(fansubGroupsJSON, &item.FansubGroups) — nutze encoding/json-Import.
    item.FansubGroup = ... Zuweisung entfernen.

    In episode_version_repository.go (GetByID):
    Entferne LIMIT 1 aus der Group-Join-Query in GetByID. Hinweis laut RESEARCH Pitfall 4: bei
    zwei echten Gruppen entstehen nach Migration zwei Zeilen — LIMIT 1 würde nur eine zurückgeben.
    Setze auch hier das json_agg-Muster ein und passe den Scan analog zu scanReleaseVariantAsEpisodeVersion an.
    Stelle sicher: LIMIT 1 kommt nur noch aus episode_version_repository.go in einem anderen Kontext vor
    (z.B. für die Version selbst), nicht für den Gruppen-JOIN.

    Sicherstellen: FansubGroups im Source-String vorhanden → TestScanEpisodeVersion_ReturnsGroupsList dreht GREEN.
    Sicherstellen: Dateigröße episode_version_repository_read_helpers.go ≤ 450 Zeilen.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/repository/... -run TestScanEpisodeVersion -v 2>&1 | tail -10</automated>
  </verify>
  <done>
    TestScanEpisodeVersion_ReturnsGroupsList GREEN.
    grep -v "^//" backend/internal/repository/episode_version_repository_read_helpers.go | grep -c "FansubGroups" gibt ≥ 1.
    grep -n "LIMIT 1" backend/internal/repository/episode_version_repository.go zeigt kein Vorkommen im Gruppen-JOIN-Kontext.
    go build ./backend/internal/repository/... grün.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fansub_repository.go bereinigen + Kollaborations-Routen entfernen + Contracts aktualisieren</name>
  <files>
    backend/internal/repository/fansub_repository.go,
    backend/internal/handlers/fansub_collaborations.go,
    backend/cmd/server/main.go,
    shared/contracts/episode-versions.yaml,
    shared/contracts/admin-content.yaml,
    shared/contracts/fansubs.yaml
  </files>
  <read_first>
    - backend/internal/repository/fansub_repository.go Zeilen 1490–1560 (ListCollaborationMembers + GetPublicProfileBySlug-Kollaborations-Zweig)
    - backend/internal/handlers/fansub_collaborations.go (vollständig)
    - backend/cmd/server/main.go Zeilen 450–466 (Kollaborations-Routen)
    - shared/contracts/episode-versions.yaml (fansub_group-Feld suchen)
    - shared/contracts/admin-content.yaml (EpisodeVersion-Schema suchen)
    - shared/contracts/fansubs.yaml (Kollaborations-Endpunkte suchen)
    - .planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-RESEARCH.md §"Forschungsfrage 2: 2c fansub_repository.go" + §"Forschungsfrage 4: DTO/Contract-Kaskade"
  </read_first>
  <behavior>
    - fansub_repository.go: GetPublicProfileBySlug-Zweig für group_type == 'collaboration' entfernt
    - fansub_repository.go: ListCollaborationMembers Funktion entfernt (oder als TODO-stub ohne Inhalt belassen bis fansub_test.go bereinigt)
    - fansub_repository.go: AddCollaborationMember und RemoveCollaborationMember entfernt
    - fansub_repository.go: Verbleibende Dateigröße (2323 Z vor dieser Phase, nimmt durch Entfernung ab) liegt weiterhin über 450 Zeilen — technische Schuld, dokumentiert im SUMMARY (see done)
    - main.go: GET/POST/DELETE /fansubs/:id/collaboration-members-Zeilen entfernt (L455–465)
    - fansub_collaborations.go: Datei entfernt oder auf leere Handler-Stubs reduziert (sodass go build kompiliert)
    - episode-versions.yaml: fansub_group (Singular-Schema) → fansub_groups (Array von $ref FansubGroupSummary)
    - admin-content.yaml: EpisodeVersion.fansub_group → fansub_groups als Array; collaboration_group_id entfernt (per D-08, RESEARCH Q4)
    - fansubs.yaml: Kollaborations-Endpunkt-Definitionen entfernt oder mit deprecated-Note versehen
    - go build ./backend/... kompiliert
  </behavior>
  <action>
    In fansub_repository.go:
    Suche GetPublicProfileBySlug — entferne den Zweig der group.GroupType == FansubGroupTypeCollaboration prüft
    und CollaborationMembers setzt. Nach Entfernung bleibt ein einheitlicher Pfad für alle Gruppen.
    Entferne ListCollaborationMembers (L1497–1547) vollständig.
    Suche AddCollaborationMember und RemoveCollaborationMember — entferne beide vollständig.
    Hinweis: fansub_repository.go bleibt nach diesen Entfernungen weiterhin weit über 450 Zeilen.
    Dieser Split ist für Phase 81 nicht eingeplant (RESEARCH Q8: out-of-phase). Das SUMMARY-Dokument
    muss einen Eintrag für technische Schuld enthalten (fansub_repository.go CLAUDE.md-konformer
    Split in Folge-Phase, Verweis RESEARCH Q8).

    In fansub_collaborations.go:
    Da die Handler-Methoden ListCollaborationMembers, AddCollaborationMember, RemoveCollaborationMember
    aus fansub_repository entfernt wurden, kompiliert der Handler nicht mehr.
    Lösche die Datei vollständig ODER ersetze sie mit leeren Handler-Stubs die go build bestehen.
    Bevorzuge: Datei löschen (rm). Stelle sicher dass main.go die Methoden nicht mehr referenziert.

    In main.go:
    Entferne Zeilen 455–465:
    v1.GET("/fansubs/:id/collaboration-members", fansubHandler.ListCollaborationMembers)
    v1.POST("/fansubs/:id/collaboration-members", authMiddleware, fansubHandler.AddCollaborationMember)
    v1.DELETE("/fansubs/:id/collaboration-members/:memberGroupId", authMiddleware, fansubHandler.RemoveCollaborationMember)

    In episode-versions.yaml:
    Suche das Feld fansub_group (Singular, type object oder $ref FansubGroupSummary).
    Ersetze durch fansub_groups: type: array; items: $ref zum FansubGroupSummary-Schema.
    Entferne collaboration_group_id-Feld aus dem EpisodeVersionEditorContext-Schema (falls vorhanden).

    In admin-content.yaml:
    Suche das EpisodeVersion-Schema (oder EpisodeVersionSummary / ähnlich benannt).
    Ersetze fansub_group (Singular, $ref FansubGroupSummary oder type object) durch
    fansub_groups: type: array; items: $ref zum FansubGroupSummary-Schema (per D-08, RESEARCH Q4).
    Entferne collaboration_group_id aus demselben Schema (per D-08).

    In fansubs.yaml:
    Suche collaboration-members-Endpunkt-Definitionen (GET /fansubs/{id}/collaboration-members, POST, DELETE).
    Entferne diese Endpunkt-Definitionen vollständig.
    Entferne CollaborationMember-, AddCollaborationMemberRequest-Schemas falls in fansubs.yaml definiert.

    Nach allen Änderungen: go build ./backend/... kompiliert.
  </action>
  <verify>
    <automated>cd backend && go build ./... 2>&1 | head -20</automated>
  </verify>
  <done>
    go build ./backend/... grün.
    grep -r "collaboration-members" backend/ — 0 Treffer in Go-Code.
    grep "fansub_group:" shared/contracts/episode-versions.yaml | grep -v "fansub_groups" — 0 Treffer (Singular entfernt).
    grep "fansub_group:" shared/contracts/admin-content.yaml | grep -v "fansub_groups" — 0 Treffer (Singular entfernt in admin-content.yaml).
    fansub_collaborations.go existiert nicht mehr oder enthält keine Funktionsimplementierungen.
    SUMMARY-Datei enthält Eintrag: "Technische Schuld: fansub_repository.go (~2323 Z, nach Bereinigung ~2200+ Z) überschreitet CLAUDE.md-450-Zeilen-Limit. CLAUDE.md-konformer Split ist Folge-Phase-Aufgabe (Verweis: RESEARCH Q8)."
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP → fansub_repository.go | Kollaborations-Endpunkte entfernt; kein öffentlicher Schreibzugriff auf collaboration-members mehr möglich |
| DB → json_agg Aggregation | json_agg kann NULL-JSON liefern wenn keine Gruppen; COALESCE auf '[]' vermeidet Unmarshal-Fehler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-READ-01 | Information Disclosure | json_agg: JSON-Injection über Gruppen-Namen | accept | Gruppen-Namen kommen aus DB (eigenes Feld), nicht aus Benutzer-Input in dieser Abfrage |
| T-81-READ-02 | Denial of Service | json_agg bei sehr vielen Gruppen pro Version | accept | In der Praxis ≤ 5 Gruppen; kein pathologischer Fall zu erwarten |
| T-81-READ-03 | Elevation of Privilege | Gelöschte Kollaborations-Routen | mitigate | Routen aus main.go entfernt; fansubHandler-Methoden entfernt; keine Response mehr möglich |
</threat_model>

<verification>
1. cd backend && go build ./... — grün
2. grep -r "collaboration-members" backend/ — 0 Treffer in Go-Dateien
3. grep "fansub_group:" shared/contracts/episode-versions.yaml | grep -vc "fansub_groups" — 0 (Singular vollständig ersetzt)
4. grep "fansub_group:" shared/contracts/admin-content.yaml | grep -vc "fansub_groups" — 0 (Singular vollständig ersetzt in admin-content.yaml)
5. go test ./backend/internal/repository/... -run TestScanEpisodeVersion — GREEN
6. grep -c "LIMIT 1" backend/internal/repository/episode_version_repository_read_helpers.go — 0 (kein LIMIT 1 mehr im Gruppen-Kontext)
</verification>

<success_criteria>
- go build ./backend/... grün
- scanReleaseVariantAsEpisodeVersion nutzt json.Unmarshal auf FansubGroups []
- LIMIT 1 in listReleaseVariantsByAnimeID und GetByID entfernt (im Gruppen-JOIN-Kontext)
- Kollaborations-Routen aus main.go entfernt
- OpenAPI-Contracts aktualisiert: fansub_groups als Array in episode-versions.yaml UND admin-content.yaml; collaboration_group_id entfernt
- TestScanEpisodeVersion_ReturnsGroupsList GREEN
- SUMMARY enthält Eintrag für technische Schuld: fansub_repository.go Split als Folge-Phase
</success_criteria>

<output>
Erstelle `.planning/phases/81-release-version-mehrfach-fansubgruppen-ohne-kombigruppe/81-04-SUMMARY.md` nach Abschluss.
</output>
