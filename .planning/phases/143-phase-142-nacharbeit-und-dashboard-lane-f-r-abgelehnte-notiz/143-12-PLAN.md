---
phase: 143
plan: 12
type: execute
wave: 6
depends_on: ["143-11"]
files_modified:
  - "frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMetadataFields.tsx"
  - "frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectTimelineSection.tsx"
  - "frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectTimelineSection.module.css"
  - "frontend/src/app/me/releases/[versionId]/workspace/workspace.module.css"
  - frontend/eslint.config.mjs
autonomous: true
requirements: ["Criterion-6"]
must_haves:
  truths:
    - "ReleaseVersionMetadataFields.tsx uses Input/Select/FormField for Release-Name, Untertitel-Typ, Auflösung, Gesamtdauer, CRC32 -- zero native <input>/<select>"
    - "AnimeProjectTimelineSection.tsx uses a CSS module instead of inline style={{}} for all four inline-styled elements, and FormField instead of the duplicate native <label>"
    - "workspace.module.css's .metadataError/.metadataSuccess use design tokens, not raw hex"
    - "no-restricted-syntax is 'error' in eslint.config.mjs and npx eslint . exits 0 for that rule"
  artifacts:
    - path: "frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectTimelineSection.module.css"
      provides: ".timelineGrid, .errorText, .saveRow classes"
  key_links:
    - from: "frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMetadataFields.tsx"
      to: "@/components/ui"
      via: "import { FormField, Input, Select } from '@/components/ui'"
      pattern: "from '@/components/ui'"
---

<objective>
Retrofit the two Criterion-6 files onto the project's design-system primitives per
143-UI-SPEC.md's exact Retrofit Map (already fully specified, byte-precise field-by-field), fix
`workspace.module.css`'s hard-coded error/success colors, and flip ESLint's
`no-restricted-syntax` rule from `warn` to `error` once both files are clean.

Purpose: implement ROADMAP Success Criterion 6 and CLAUDE.md's mandatory
`@/components/ui`-primitives rule.
Output: zero native `<input>/<select>` in the two target files; zero inline `style={{}}` in
`AnimeProjectTimelineSection.tsx`; zero raw hex in `workspace.module.css`'s error/success classes;
`no-restricted-syntax: 'error'`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-phase-142-nacharbeit-und-dashboard-lane-f-r-abgelehnte-notiz/143-UI-SPEC.md
</context>

<interfaces>
`FormField` contract (`frontend/src/components/ui/FormField.tsx`, already exists, do not modify):
```ts
export interface FormFieldProps {
  label?: string; htmlFor?: string; hint?: string; error?: string
  required?: boolean; disabled?: boolean; children: ReactNode
}
```
`Select` is a thin wrapper over native `<select>` accepting all `SelectHTMLAttributes` plus
`invalid?: boolean` — `<option>` children are unchanged, only the wrapping element changes.

Sibling analog already using `FormField` + `Input` correctly in the SAME directory
(`SegmentEditPanel.tsx:414-432`):
```tsx
<FormField label="..." htmlFor="segment-override-start" hint="..." error={overrideDisplayError ?? undefined}>
  <Input id="segment-override-start" type="text" inputMode="numeric" value={...} onChange={...} onBlur={...} />
</FormField>
```

`Select` analog (`AnimeContributionModal.tsx:309-327`):
```tsx
<FormField label="Person" htmlFor="new-project-member-select">
  <Select id="new-project-member-select" value={...} onChange={...} disabled={...}>
    <option value="">...</option>
    {items.map((item) => <option key={item.id} value={item.id}>{item.label}</option>)}
  </Select>
</FormField>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Retrofit ReleaseVersionMetadataFields.tsx onto FormField/Input/Select</name>
  <files>frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMetadataFields.tsx</files>
  <read_first>frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMetadataFields.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/SegmentEditPanel.tsx, frontend/src/components/ui/Select.tsx, frontend/src/components/ui/FormField.tsx</read_first>
  <action>
    Add `FormField, Select` to the existing `import { DatePicker } from '@/components/ui/DatePicker'`
    / `import { Input } from '@/components/ui/Input'` lines (or consolidate into one
    `import { DatePicker, FormField, Input, Select } from '@/components/ui'` — check whichever
    import style the sibling `SegmentEditPanel.tsx` uses and match it).

    Replace field-by-field, preserving every existing value/onChange/placeholder/maxLength exactly:
    - Release-Name: `<label className={styles.field}><span>Release-Name</span><input .../></label>`
      -> `<FormField label="Release-Name" htmlFor="release-title"><Input id="release-title"
      value={formState.title} placeholder={defaultReleaseTitle(context)} onChange={...} /></FormField>`
    - Untertitel-Typ: native `<select>` -> `<FormField label="Untertitel-Typ" htmlFor="subtitle-type">
      <Select id="subtitle-type" value={formState.subtitleType} onChange={...}><option value="">keiner</option>
      <option value="softsub">softsub</option><option value="hardsub">hardsub</option></Select></FormField>`
      (identical 3 options, unchanged onChange body)
    - Auflösung: native `<input>` -> `<FormField label="Auflösung" htmlFor="video-quality">
      <Input id="video-quality" value={formState.videoQuality} onChange={...} /></FormField>`
    - Gesamtdauer: native `<input>` with its existing `onBlur` parse -> `<FormField label="Gesamtdauer"
      htmlFor="duration-seconds" hint="Akzeptiert m:ss, hh:mm:ss, rohe Sekunden sowie Kurzformen wie 2m
      oder 1m30s. Wird als Grenze für Segment-Endzeiten verwendet."><Input id="duration-seconds"
      value={formState.durationSeconds} placeholder="z. B. 24:10 oder 1450" onChange={...} onBlur={...}
      /></FormField>` — the `onChange`/`onBlur` bodies are unchanged, only moved inside `Input`. Remove
      the standalone `<p className={styles.helperText}>...</p>` below the grid since its exact
      sentence now lives in this field's `hint` (verbatim, do not reword).
    - CRC32: already `Input`, just wrap: `<FormField label="CRC32" htmlFor="crc32"><Input id="crc32"
      value={formState.crc32} maxLength={13} placeholder="1CC0A2E3" onChange={...} /></FormField>`
    - Bearbeitung begonnen/abgeschlossen am: LEAVE UNCHANGED (do not FormField-wrap these two
      `DatePicker` fields) — 143-UI-SPEC.md explicitly says their existing plain-`<span>` wrapper is
      NOT the label-duplication defect this criterion targets; wrapping them would manufacture a new
      double-label case.

    `styles.field` (the per-field raw wrapper class) can be removed from usage on the migrated
    fields since `FormField`/its own primitives now own that layout; `styles.grid` (the responsive
    grid container) stays unchanged around the whole field list.
  </action>
  <acceptance_criteria>
    <automated>docker compose exec team4sv30-frontend npx eslint "src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMetadataFields.tsx"</automated>
    - `grep -nE "<input|<select" "frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMetadataFields.tsx"` returns no matches
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/app/admin/episode-versions/[versionId]/edit/page.test.tsx"</automated>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npx eslint "src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionMetadataFields.tsx"</automated>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/app/admin/episode-versions/[versionId]/edit/page.test.tsx"</automated>
  </verify>
  <done>File uses only Input/Select/FormField/DatePicker for its form fields; the episode-version editor page test (which renders this component) still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Retrofit AnimeProjectTimelineSection.tsx onto a CSS module + FormField, and fix workspace.module.css colors</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectTimelineSection.tsx, frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectTimelineSection.module.css, frontend/src/app/me/releases/[versionId]/workspace/workspace.module.css</files>
  <read_first>frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectTimelineSection.tsx, frontend/src/app/admin/episode-versions/[versionId]/edit/ReleaseVersionNotesTab.module.css, "frontend/src/app/me/releases/[versionId]/workspace/workspace.module.css"</read_first>
  <action>
    Create `AnimeProjectTimelineSection.module.css` (new file, does not exist today) with three
    classes, following `ReleaseVersionNotesTab.module.css`'s `var(--space-*)` token idiom:
    ```
    .timelineGrid { display: grid; grid-template-columns: repeat(auto-fit, minmax(13rem, 1fr)); gap: var(--space-3); }
    .errorText { color: var(--color-error); margin-top: var(--space-2); }
    .saveRow { margin-top: var(--space-3); }
    ```
    In `AnimeProjectTimelineSection.tsx`: import `styles from './AnimeProjectTimelineSection.module.css'`
    plus `FormField` (add to the existing `import { Button, DatePicker } from '@/components/ui'`).
    Replace `style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(13rem, 1fr))',
    gap: 'var(--space-3)' }}` with `className={styles.timelineGrid}`. Replace both
    `<label htmlFor={...}>Projekt begonnen/abgeschlossen am</label>` + adjacent `<DatePicker
    id=... label=... .../>` pairs with `<FormField label="Projekt begonnen am"
    htmlFor={`project-start-${animeId}`}><DatePicker id={`project-start-${animeId}`} label="Projekt
    begonnen am" ... /></FormField>` (same for `-completed-`) — KEEP `DatePicker`'s own `label` prop
    on both (it drives the picker button's internal `aria-label` for "auswählen"/"Kalender"
    interactions; only the VISIBLE label now comes from `FormField`, not from a second native
    `<label>`). Replace `style={{ color: 'var(--color-error)', marginTop: 'var(--space-2)' }}` on the
    error `<p>` with `className={styles.errorText}`. Replace `style={{ marginTop: 'var(--space-3)' }}`
    on the save-button wrapper `<div>` with `className={styles.saveRow}`. Copy, field order,
    disabled/loading logic, and save behavior are unchanged — this is a pure markup/styling
    migration.

    In `workspace.module.css`, replace the current `.metadataError`/`.metadataSuccess` block
    (raw hex `#a4262c`/`#fff1f0`/`#176b44`/`#edfbf2`, `padding: 10px 12px; border-radius: 8px;`)
    with the exact token-based replacement already specified in 143-UI-SPEC.md's Color section:
    `padding: var(--space-2) var(--space-3); border-radius: var(--radius-sm); font-size: 0.9rem;`,
    `.metadataError { color: var(--color-error); background: color-mix(in srgb, var(--color-error)
    12%, var(--bg-card)); }`, `.metadataSuccess { color: var(--color-success); background:
    color-mix(in srgb, var(--color-success) 12%, var(--bg-card)); }` — matching the `color-mix()`
    idiom already used in `ReleaseVersionNotesTab.module.css` in the same directory tree.
  </action>
  <acceptance_criteria>
    <automated>docker compose exec team4sv30-frontend npx eslint "src/app/admin/fansubs/[id]/edit/AnimeProjectTimelineSection.tsx"</automated>
    - `grep -nE "style=\{\{" "frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectTimelineSection.tsx"` returns no matches
    - `grep -nE "#[0-9a-fA-F]{3,6}" "frontend/src/app/me/releases/[versionId]/workspace/workspace.module.css" "frontend/src/app/admin/fansubs/[id]/edit/AnimeProjectTimelineSection.module.css"` returns no matches
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/app/admin/fansubs/[id]/edit/page.test.tsx"</automated>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npx eslint "src/app/admin/fansubs/[id]/edit/AnimeProjectTimelineSection.tsx"</automated>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/app/admin/fansubs/[id]/edit/page.test.tsx"</automated>
  </verify>
  <done>No inline style={{}} remains in the component; no raw hex remains in either CSS module; copy and behavior unchanged; the fansub edit page test still passes.</done>
</task>

<task type="auto">
  <name>Task 3: Flip no-restricted-syntax to error and verify a clean lint</name>
  <files>frontend/eslint.config.mjs</files>
  <read_first>frontend/eslint.config.mjs</read_first>
  <action>
    Sequence this AFTER Tasks 1 and 2 land clean, so the `error` gate does not fire on files this
    phase does not touch. Change the `no-restricted-syntax: uiPrimitiveGuard` rule severity from
    `warn` to `error` in `frontend/eslint.config.mjs` (locate the exact rule config object — it may
    be a `[level, ...options]` array or a bare level string; change only the severity level, not the
    guard's option payload).
  </action>
  <acceptance_criteria>
    <automated>docker compose exec team4sv30-frontend npx eslint . --rule '{"no-restricted-syntax":"error"}' --quiet 2>&1 | grep -c "no-restricted-syntax"</automated>
    - The above command's output is `0` (zero violations of this specific rule across the whole repo, not the whole lint budget — the repo's other 11 pre-existing errors and 333 warnings from OTHER rules are explicitly out of scope per VALIDATION.md)
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npx eslint . --rule '{"no-restricted-syntax":"error"}' --quiet 2>&1 | grep -c "no-restricted-syntax"</automated>
  </verify>
  <done>no-restricted-syntax is 'error' in eslint.config.mjs; zero violations of that specific rule anywhere in frontend/src.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | Presentation-layer markup/styling retrofit only, no data-handling change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-12 | Tampering | Design-system primitive retrofit | accept | Pure markup/styling substitution with identical field order, labels, and validation behavior; the episode-version and fansub-edit page tests (run in verification) are the behavioral-parity proof. |
</threat_model>

<verification>
<automated>docker compose exec team4sv30-frontend npx eslint .</automated>
<automated>docker compose exec team4sv30-frontend npx vitest run "src/app/admin/episode-versions/[versionId]/edit/page.test.tsx" "src/app/admin/fansubs/[id]/edit/page.test.tsx"</automated>
</verification>

<success_criteria>
Both retrofitted files use only design-system primitives and CSS-module classes; workspace.module.css
uses design tokens; no-restricted-syntax is 'error' with zero violations; no rendering/behavior
regression in either page's existing tests.
</success_criteria>

<output>
Create `.planning/phases/143-phase-142-nacharbeit-und-dashboard-lane-f-r-abgelehnte-notiz/143-12-SUMMARY.md` when done
</output>
