---
phase: 143
plan: 11
type: execute
wave: 5
depends_on: ["143-10"]
files_modified:
  - backend/internal/repository/anime_contributions_member_project_repository.go
  - backend/internal/repository/anime_contributions_member_project_repository_test.go
  - "frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.test.tsx"
autonomous: true
requirements: ["Criterion-5"]
must_haves:
  truths:
    - "A rejected (review_state='rejected') release_version_notes row does not make has_own_notes true"
    - "A note with no lifecycle row (never entered review) still counts has_own_notes true"
    - "A tombstoned note stays excluded via the existing deleted_at IS NULL clause (no new special-casing needed)"
    - "isDone() in the frontend returns false for a release whose only note is rejected"
  artifacts: []
  key_links:
    - from: "backend/internal/repository/anime_contributions_member_project_repository.go"
      to: "release_version_note_review_lifecycle"
      via: "LEFT JOIN ... ON lifecycle.release_version_note_id = rvn.id"
      pattern: "release_version_note_review_lifecycle"
---

<objective>
Fix `has_own_notes` in `anime_contributions_member_project_repository.go` so a REJECTED release
note no longer counts as "done" (it currently has no review-state filter at all), and prove the
frontend consequence (`isDone()`) already picks this up correctly with zero code change.

Purpose: implement ROADMAP Success Criterion 5.
Output: one corrected `EXISTS` subquery + repository test; one new frontend test (no source change
needed in the frontend).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-phase-142-nacharbeit-und-dashboard-lane-f-r-abgelehnte-notiz/143-CONTEXT.md
</context>

<interfaces>
Exact current defect location (`anime_contributions_member_project_repository.go`, inside
`listMemberProjectReleaseVersions`, verified lines 139-145):
```sql
EXISTS (
	SELECT 1
	FROM release_version_notes rvn
	WHERE rvn.release_version_id = rv.id
	  AND rvn.member_id = $1
	  AND rvn.deleted_at IS NULL
) AS has_own_notes,
```
No review-state filter — a `rejected` note still counts as done today.

Correct lifecycle-join pattern to copy, from `release_version_notes_repository.go`'s
`ListReleaseVersionNotesForMember` (lines 162-164):
```sql
FROM release_version_notes rvn
LEFT JOIN release_version_note_review_lifecycle lifecycle
  ON lifecycle.release_version_note_id = rvn.id
```

Frontend consequence (`frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.tsx:53`,
NO code change needed — it already trusts `has_own_notes` as an opaque boolean):
```ts
function isDone(release: MeProjectReleaseVersion): boolean {
  return release.has_own_notes || release.has_own_media
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add the review-state filter to the has_own_notes EXISTS subquery</name>
  <files>backend/internal/repository/anime_contributions_member_project_repository.go, backend/internal/repository/anime_contributions_member_project_repository_test.go</files>
  <read_first>backend/internal/repository/anime_contributions_member_project_repository.go, backend/internal/repository/release_version_notes_repository.go, backend/internal/repository/anime_contributions_member_project_repository_test.go</read_first>
  <action>
    In `listMemberProjectReleaseVersions`'s `has_own_notes` `EXISTS` subquery (verified lines
    139-145, re-verify before editing), add a `LEFT JOIN release_version_note_review_lifecycle
    lifecycle ON lifecycle.release_version_note_id = rvn.id` inside the subquery and change the
    `WHERE` clause to additionally require
    `AND (lifecycle.review_state IS NULL OR lifecycle.review_state <> 'rejected')` — a `NULL`
    lifecycle (note never entered review) must still count as done; only an explicit `'rejected'`
    state must not. Do NOT add any `tombstoned` special-casing — the existing
    `rvn.deleted_at IS NULL` clause on `release_version_notes` already excludes tombstoned notes,
    since `release_review_cleanup_repository.go` sets `deleted_at` on tombstone (confirmed in
    CONTEXT.md Kriterium 5 and re-verify by reading that cleanup repository's tombstone-handling
    method before finalizing this task, to be certain no additional filter is needed).

    Add tests to `anime_contributions_member_project_repository_test.go` covering three cases for
    `listMemberProjectReleaseVersions` (or `GetMemberProjectDetail`, whichever is the public
    entrypoint this file's existing tests already call): (1) a release with a note whose lifecycle
    `review_state = 'rejected'` -> `has_own_notes` is `false`; (2) a release with a note that has NO
    lifecycle row at all (never submitted for review) -> `has_own_notes` is `true`; (3) a release
    with a note that was tombstoned (its `deleted_at` set by the cleanup path) -> `has_own_notes` is
    `false` via the pre-existing `deleted_at IS NULL` clause, asserting this stays true with no new
    logic (a regression guard, not a behavior change).
  </action>
  <acceptance_criteria>
    <automated>docker compose exec team4sv30-backend go test ./internal/repository/... -run "TestAnimeContributionsMemberProject|TestGetMemberProjectDetail" -v</automated>
    - `grep -n "release_version_note_review_lifecycle" backend/internal/repository/anime_contributions_member_project_repository.go` returns at least one match
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-backend go test ./internal/repository/... -run "TestAnimeContributionsMemberProject|TestGetMemberProjectDetail" -v</automated>
  </verify>
  <done>All three fixture cases pass; a rejected-only note no longer counts as done, a never-reviewed note still does, tombstoned stays excluded.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend test proving isDone() reflects the corrected has_own_notes</name>
  <files>frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.test.tsx</files>
  <read_first>frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.tsx, frontend/src/app/me/projects/[animeId]/group/[fansubGroupId]/page.test.tsx</read_first>
  <action>
    `isDone()` needs no source change — once the backend Task 1 fix ships, `has_own_notes` is
    already correct by the time it reaches the frontend and `isDone()` already trusts it opaquely.
    Add a new test case (do not touch existing passing cases) constructing a fixture release with
    `has_own_notes: false, has_own_media: false` (representing the corrected backend value for a
    release whose only note is rejected) and assert it is filtered into the "Offen" bucket / excluded
    from "Erledigt" wherever this page's existing filter/counter tests already exercise that logic
    (read the existing `filterReleases`/counter tests in this file first and follow the same
    assertion style — do not invent a new testing pattern for this one case).
  </action>
  <acceptance_criteria>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/app/me/projects/[animeId]/group/[fansubGroupId]/page.test.tsx"</automated>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/app/me/projects/[animeId]/group/[fansubGroupId]/page.test.tsx"</automated>
  </verify>
  <done>New test case passes; a release with a rejected-only note is treated as open, not done.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | Read-only projection correctness fix, no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-11 | Information Disclosure | has_own_notes projection | accept | Correcting a false-positive "done" signal only; no additional data is exposed, the fix narrows what counts as complete. |
</threat_model>

<verification>
<automated>docker compose exec team4sv30-backend go test ./internal/repository/...</automated>
<automated>docker compose exec team4sv30-frontend npx vitest run src/app/me/projects</automated>
</verification>

<success_criteria>
A rejected-only note no longer makes a release count as "erledigt" on the backend or frontend; the
"X offen · Y erledigt" counter and "Offen" filter both reflect the corrected state.
</success_criteria>

<output>
Create `.planning/phases/143-phase-142-nacharbeit-und-dashboard-lane-f-r-abgelehnte-notiz/143-11-SUMMARY.md` when done
</output>
