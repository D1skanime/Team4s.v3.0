---
phase: 143
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/handlers/app_auth.go
  - backend/internal/handlers/app_auth_invitations.go
  - backend/internal/handlers/app_auth_group_members.go
  - backend/internal/handlers/app_auth_capabilities.go
autonomous: true
requirements: ["Randbedingung-450-line-app_auth-split"]
must_haves:
  truths:
    - "app_auth.go is at or below 450 lines (was 1308)"
    - "No handler behavior, route, or exported signature changed by the split"
    - "app_auth_test.go passes unchanged"
  artifacts:
    - path: "backend/internal/handlers/app_auth.go"
      provides: "AppAuthHandler struct, constructor, store interfaces, GetCurrentUser, ListAppUsers, HandleKeycloakBackchannelLogout"
      min_lines: 180
    - path: "backend/internal/handlers/app_auth_invitations.go"
      provides: "Invitation request types + ListFansubGroupInvitations, CreateFansubGroupInvitation, CancelFansubGroupInvitation, AcceptFansubInvitation"
    - path: "backend/internal/handlers/app_auth_group_members.go"
      provides: "Member request types + ListFansubGroupAppMembers, SearchFansubGroupAppMemberCandidates, CreateFansubGroupAppMember, SetFansubGroupMemberRole, SetFansubLead, setFansubGroupMemberRole, UpdateFansubGroupMemberStatus, SetFansubGroupMemberMediaPermissions, normalizeRequestedFansubRoles"
    - path: "backend/internal/handlers/app_auth_capabilities.go"
      provides: "fansubGroupCapabilitiesResponse type + GetFansubGroupCapabilities"
  key_links:
    - from: "backend/internal/handlers/app_auth_invitations.go"
      to: "backend/internal/handlers/app_auth.go"
      via: "method receiver *AppAuthHandler (same package handlers, no import needed)"
      pattern: "func \\(h \\*AppAuthHandler\\)"
---

<objective>
Split the oversized `backend/internal/handlers/app_auth.go` (1308 lines, grew from 1254 during the
Phase 142 window) into four files of the same `handlers` package, per the EXACT breakdown mandated
in ROADMAP.md's Phase 143 Randbedingungen. This is a pure file move — no signature, route, or
behavior change — so `app_auth_test.go` must keep passing completely unchanged.

Purpose: bring the file under the project's 450-line production-file cap (CLAUDE.md Modularity
constraint) without destabilizing the existing, heavily-tested auth/invitation/member-management
surface.
Output: `app_auth.go` (~220 lines) + three new sibling files (`app_auth_invitations.go` ~455,
`app_auth_group_members.go` ~400, `app_auth_capabilities.go` ~230), all still `package handlers`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split app_auth.go into four files by exact line-range/function ownership</name>
  <files>backend/internal/handlers/app_auth.go, backend/internal/handlers/app_auth_invitations.go, backend/internal/handlers/app_auth_group_members.go, backend/internal/handlers/app_auth_capabilities.go</files>
  <read_first>backend/internal/handlers/app_auth.go, backend/internal/handlers/app_auth_test.go</read_first>
  <action>
    Read the full current `app_auth.go` (1308 lines, package `handlers`) first. Its current
    top-level members (verified by `grep -n "^func \|^type "`) are: `fansubGroupCapabilitiesResponse`
    (line 22), `fansubGroupAppMemberStore`/`fansubGroupInvitationStore`/`fansubGroupNameStore`/
    `auditLogWriter` interfaces (54-79), `AppAuthHandler` struct + `NewAppAuthHandler` constructor
    (81-136), `GetCurrentUser` (138), `ListAppUsers` (162), `HandleKeycloakBackchannelLogout` (176),
    request types `createFansubGroupAppMemberRequest`/`setFansubGroupMemberRoleRequest`/
    `setFansubGroupMemberStatusRequest`/`setFansubGroupMemberMediaPermissionsRequest`/
    `createFansubGroupInvitationRequest`/`acceptFansubGroupInvitationRequest` (220-250),
    `ListFansubGroupAppMembers` (251), `SearchFansubGroupAppMemberCandidates` (287),
    `ListFansubGroupInvitations` (332), `CreateFansubGroupInvitation` (368),
    `CancelFansubGroupInvitation` (554), `AcceptFansubInvitation` (613),
    `CreateFansubGroupAppMember` (696), `SetFansubGroupMemberRole` (782), `SetFansubLead` (786),
    `setFansubGroupMemberRole` (790), `UpdateFansubGroupMemberStatus` (890),
    `SetFansubGroupMemberMediaPermissions` (983), `GetFansubGroupCapabilities` (1060),
    `normalizeRequestedFansubRoles` (1290). Verify these exact boundaries yourself with a fresh
    `grep -n "^func \|^type "` before moving anything, since line numbers may have shifted slightly.

    Move code (cut, do not duplicate) into four files, all declaring `package handlers` and all
    keeping the SAME method receiver `(h *AppAuthHandler)` (methods do not need to live in the same
    file as their receiver's struct definition in Go — same package is sufficient):

    - `app_auth.go` KEEPS ONLY: imports actually still used, `fansubGroupAppMemberStore`,
      `fansubGroupInvitationStore`, `fansubGroupNameStore`, `auditLogWriter` interfaces,
      `AppAuthHandler` struct, `NewAppAuthHandler`, `GetCurrentUser`, `ListAppUsers`,
      `HandleKeycloakBackchannelLogout`. Target ~220 lines.
    - `app_auth_invitations.go` GETS: `createFansubGroupInvitationRequest`,
      `acceptFansubGroupInvitationRequest`, `ListFansubGroupInvitations`,
      `CreateFansubGroupInvitation`, `CancelFansubGroupInvitation`, `AcceptFansubInvitation`.
      Target ~455 lines.
    - `app_auth_group_members.go` GETS: `createFansubGroupAppMemberRequest`,
      `setFansubGroupMemberRoleRequest`, `setFansubGroupMemberStatusRequest`,
      `setFansubGroupMemberMediaPermissionsRequest`, `ListFansubGroupAppMembers`,
      `SearchFansubGroupAppMemberCandidates`, `CreateFansubGroupAppMember`,
      `SetFansubGroupMemberRole`, `SetFansubLead`, `setFansubGroupMemberRole`,
      `UpdateFansubGroupMemberStatus`, `SetFansubGroupMemberMediaPermissions`,
      `normalizeRequestedFansubRoles`. Target ~400 lines.
    - `app_auth_capabilities.go` GETS: `fansubGroupCapabilitiesResponse`,
      `GetFansubGroupCapabilities`. Target ~230 lines.

    Do NOT split `GetFansubGroupCapabilities` (229 lines) or `CreateFansubGroupInvitation` (185
    lines) internally — ROADMAP explicitly excludes that from this phase. Do not rename any
    exported or unexported identifier. Each new file needs its own `import (...)` block containing
    only what that file's code actually references (run goimports/gofmt after moving, see Task 2).
  </action>
  <acceptance_criteria>
    - `wc -l backend/internal/handlers/app_auth.go` reports <= 450 (target ~220)
    - `wc -l backend/internal/handlers/app_auth_invitations.go backend/internal/handlers/app_auth_group_members.go backend/internal/handlers/app_auth_capabilities.go` each report <= 450
    - `grep -c "^func (h \*AppAuthHandler)" backend/internal/handlers/app_auth*.go` across all four files sums to the same method count as `git show HEAD:backend/internal/handlers/app_auth.go | grep -c "^func (h \*AppAuthHandler)"`
    - No identifier renamed: `git diff --stat` shows only the four `app_auth*.go` files touched, no other file needs a changed import
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-backend go build ./...</automated>
  </verify>
  <done>All four files exist, each under 450 lines, containing exactly the functions/types listed above with zero behavior change.</done>
</task>

<task type="auto">
  <name>Task 2: Format, build, and verify app_auth_test.go passes unchanged</name>
  <files>backend/internal/handlers/app_auth.go, backend/internal/handlers/app_auth_invitations.go, backend/internal/handlers/app_auth_group_members.go, backend/internal/handlers/app_auth_capabilities.go</files>
  <read_first>backend/internal/handlers/app_auth_test.go</read_first>
  <action>
    Run `docker compose exec team4sv30-backend gofmt -l ./internal/handlers/` and
    `docker compose exec team4sv30-backend go vet ./internal/handlers/...` to catch unused/missing
    imports across the four split files (each file needs its own trimmed import block — do not
    leave an unused import from the original monolith, and do not miss an import a moved function
    actually needs). Fix import blocks per file until both commands report clean. Then run
    `docker compose exec team4sv30-backend go build ./...` to confirm the whole backend compiles.
    Confirm `app_auth_test.go` was NOT modified (`git diff --stat backend/internal/handlers/app_auth_test.go`
    must show no changes) — this is the hard behavioral-parity proof required by ROADMAP.
  </action>
  <acceptance_criteria>
    <automated>docker compose exec team4sv30-backend go build ./...</automated>
    <automated>docker compose exec team4sv30-backend go test ./internal/handlers/... -run TestAppAuth</automated>
    - `git diff --stat backend/internal/handlers/app_auth_test.go` shows zero changes
    - `gofmt -l` and `go vet ./internal/handlers/...` report no issues for the four split files
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-backend go build ./...</automated>
    <automated>docker compose exec team4sv30-backend go test ./internal/handlers/... -run TestAppAuth</automated>
  </verify>
  <done>Backend builds clean, all existing app_auth handler tests pass unmodified, no diff on the test file itself.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| admin client -> AppAuthHandler | Existing authenticated admin/member-management surface; unchanged by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-01 | Tampering | app_auth*.go split | accept | Pure code relocation, zero signature/route/permission-check change; `app_auth_test.go` left byte-identical and must pass, which is the concrete proof no auth logic shifted. |
</threat_model>

<verification>
`docker compose exec team4sv30-backend go build ./...` succeeds and
`docker compose exec team4sv30-backend go test ./internal/handlers/...` passes with the same
pass/fail set as before this plan (no new failures, no newly-skipped tests).
</verification>

<success_criteria>
`app_auth.go` and the three new sibling files each report <= 450 lines via `wc -l`; the backend
builds; `app_auth_test.go` is unmodified and green.
</success_criteria>

<output>
Create `.planning/phases/143-phase-142-nacharbeit-und-dashboard-lane-f-r-abgelehnte-notiz/143-01-SUMMARY.md` when done
</output>
