---
phase: 143-phase-142-nacharbeit-und-dashboard-lane-f-r-abgelehnte-notiz
plan: 16
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - "frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/page.tsx"
  - "frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/page.test.tsx"
autonomous: true
requirements: ["UAT-01"]
must_haves:
  truths:
    - "After a reviewer submits a decision (confirm or reject), the header status Badge immediately reflects the new status (Bestätigt / Öffentlich or Abgelehnt) without a page reload or manual refresh"
    - "The decision-panel message below the Badge and the Badge itself no longer contradict each other"
  artifacts: []
  key_links:
    - from: "frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/page.tsx"
      to: "frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/page.tsx"
      via: "setDetail(...) called inside submitDecision's success branch, immediately after setDecisionState({ kind: 'success', response })"
      pattern: "setDetail"
---

<objective>
Close UAT-01: the review-detail page's header status Badge stays on "In Prüfung" after a reviewer
successfully confirms or rejects a submission, because `submitDecision` only updates `decisionState`
and never the `detail` object the Badge's `status` variable is derived from. The decision itself
persists correctly server-side — this is purely a stale-display bug that makes the reviewer think
their decision was not registered.

Purpose: the header Badge (`<Badge variant={status.variant}>{status.label}</Badge>`, derived via
`releaseReviewDetailStatus(detail.status)`) and the decision-panel message directly below it
currently show contradictory information after a successful decision.
Output: `detail.status` is patched client-side immediately on decision success (no refetch, no new
API call), so the header Badge and the decision message agree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-phase-142-nacharbeit-und-dashboard-lane-f-r-abgelehnte-notiz/143-UAT.md
</context>

<interfaces>
Current `submitDecision` success branch in
`frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/page.tsx` (exact current text):
```tsx
      setRejectOpen(false)
      setDecisionState({ kind: 'success', response })
```
`detail` is `ReleaseReviewDetail | null` state (`useState<ReleaseReviewDetail | null>(null)`).
`ReleaseReviewDetail extends ReleaseReviewQueueItem`, whose `status` field is
`ReleaseReviewStatus = 'pending' | 'confirmed' | 'rejected' | 'tombstoned'`
(`frontend/src/types/releaseReviews.ts`). The `decision` parameter of `submitDecision(decision:
'confirm' | 'reject')` maps 1:1 onto the two non-pending statuses this page can ever transition to:
`'confirm' -> 'confirmed'`, `'reject' -> 'rejected'`.

Header Badge derivation (unchanged by this plan):
```tsx
const status = releaseReviewDetailStatus(detail.status)
// ...
<Badge variant={status.variant}>{status.label}</Badge>
```
`releaseReviewDetailStatus('pending')` -> `{ label: 'In Prüfung', variant: 'warning' }`;
`('confirmed')` -> `{ label: 'Bestätigt / Öffentlich', variant: 'success' }`;
`('rejected')` -> `{ label: 'Abgelehnt', variant: 'danger' }`
(`frontend/src/app/admin/fansubs/releaseReviewPresentation.ts`).

**File-size constraint:** `page.tsx` is currently 446 lines against this project's 450-line cap
(`CLAUDE.md`) — add the smallest possible number of new lines (ideally exactly one new statement) and
confirm the final line count with `wc -l` before finishing Task 1.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Patch detail.status on successful decision</name>
  <files>frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/page.tsx</files>
  <read_first>frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/page.tsx, frontend/src/types/releaseReviews.ts, frontend/src/app/admin/fansubs/releaseReviewPresentation.ts</read_first>
  <action>
    In `submitDecision`, immediately after the line `setDecisionState({ kind: 'success', response })`,
    add one call to `setDetail` using the functional-updater form (`setDetail((previous) => ...)`) so
    it never races a stale closure. Guard for `null` (return the previous value unchanged if `null`),
    otherwise return a shallow copy of the previous `detail` with `status` replaced by
    `decision === 'confirm' ? 'confirmed' : 'rejected'` (use the already-in-scope `decision` parameter,
    not `response.data.decision`, since it is the exact same value and avoids an extra property
    lookup). Do not touch `decided_at`, `last_activity_at`, or any other field — only `status` drives
    the header Badge this plan is fixing; do not add a refetch/reload call, since that would
    re-trigger `isLoading`/`LoadingState` and hide the just-shown decision message and
    `NextReviewControl` success actions. Keep the added statement as compact as this file's existing
    style allows (single-line arrow-function body) given the file's 446/450-line budget noted above.
  </action>
  <acceptance_criteria>
    <automated>grep -n "setDetail" "frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/page.tsx"</automated>
    - `wc -l "frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/page.tsx"` outputs a number `<= 450`
  </acceptance_criteria>
  <verify>
    <automated>wc -l "frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/page.tsx"</automated>
  </verify>
  <done>submitDecision's success branch calls setDetail to update the local status field; file stays at or below 450 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression tests proving the Badge updates after a decision</name>
  <files>frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/page.test.tsx</files>
  <read_first>frontend/src/app/admin/fansubs/[id]/reviews/[reviewId]/page.test.tsx</read_first>
  <action>
    Add two new tests (a new top-level `describe('header status Badge reflects the decision (UAT-01)',
    ...)` block, following this file's existing structure/mocks). Both tests render
    `ReleaseReviewPage`, wait for the initial "In Prüfung" Badge to appear
    (`await screen.findByText('In Prüfung')`), then:
    (a) **confirm case** — mock `api.decideReleaseReview` to resolve
    `{ data: { review_id: 'review-image', decision: 'confirm', next: null } }`, click the
    "Bestätigen und veröffentlichen" button, then `await waitFor` asserting
    `screen.getByText('Bestätigt / Öffentlich')` exists and `screen.queryByText('In Prüfung')` is
    `null`.
    (b) **reject case** — mock `api.decideReleaseReview` to resolve
    `{ data: { review_id: 'review-image', decision: 'reject', next: null } }`; open the reject modal
    (click "Ablehnen"), select a `rejection_category` via the "Ablehnungsgrund" `Select` (any
    `REJECTION_OPTIONS` value, e.g. `quality.insufficient`) and fill the "Begründung" `Textarea` with
    a string of at least 10 characters, then click the modal's "Beitrag ablehnen" submit button, then
    `await waitFor` asserting `screen.getByText('Abgelehnt')` exists and
    `screen.queryByText('In Prüfung')` is `null`. Both tests use this file's existing
    `api`/`navigation` `vi.hoisted` mocks and existing `item`/`beforeEach` fixture — no new mock
    infrastructure needed.
  </action>
  <acceptance_criteria>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/app/admin/fansubs/[id]/reviews/[reviewId]/page.test.tsx"</automated>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/app/admin/fansubs/[id]/reviews/[reviewId]/page.test.tsx"</automated>
  </verify>
  <done>Both new tests pass; all pre-existing tests in this file still pass (no regressions).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | Client-side display-state fix only; the authoritative decision is already persisted server-side via the existing, unchanged decideReleaseReview call and its existing authorization checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-16 | Tampering | Client-side detail.status patch | accept | The patched value is derived only from the decision the same authorized request already submitted and the server already accepted (decision === 'confirm'/'reject'), not from user-controlled input; no new write path, no new endpoint. A future page reload re-fetches the authoritative server state via the existing getReleaseReview call, so this is a display-only optimization, not a new source of truth. |
</threat_model>

<verification>
<automated>docker compose exec team4sv30-frontend npx vitest run "src/app/admin/fansubs/[id]/reviews/[reviewId]/page.test.tsx"</automated>
</verification>

<success_criteria>
After a successful confirm or reject decision, the header status Badge shows "Bestätigt / Öffentlich"
or "Abgelehnt" respectively, matching the decision message already shown below it; the file stays at
or below 450 lines.
</success_criteria>

<output>
Create `.planning/phases/143-phase-142-nacharbeit-und-dashboard-lane-f-r-abgelehnte-notiz/143-16-SUMMARY.md` when done
</output>
