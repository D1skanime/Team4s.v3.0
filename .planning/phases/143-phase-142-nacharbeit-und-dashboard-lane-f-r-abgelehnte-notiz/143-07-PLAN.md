---
phase: 143
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/profile/MemberBadgeChain.tsx
  - "frontend/src/app/members/[slug]/page.test.tsx"
  - frontend/next.config.mjs
  - "frontend/src/lib/api.no-token-boundary.test.ts"
  - frontend/src/components/profile/MembershipsSection.test.tsx
  - "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseGallery.test.tsx"
  - "frontend/src/app/admin/fansubs/[id]/edit/DefaultCrewManager.test.tsx"
autonomous: true
requirements: ["Criterion-1"]
must_haves:
  truths:
    - "MemberBadgeChain renders the roles progress group, founding-stage lock label, and anime-project-stage attribute its own test and the public member page test both require"
    - "next.config.mjs images.localPatterns allows every legitimate /media/** subpath used by ResponsiveImage today while excluding /media/admin/**"
    - "api.no-token-boundary.test.ts's docs allowlist points at files that actually exist post-milestone-archival"
    - "MembershipsSection.test.tsx's grid assertion matches the live, intentionally-responsive auto-fit CSS"
    - "ReleaseGallery.test.tsx's screenshot label/alt assertions match the live 'Fansub Screenshot' CATEGORY_LABELS value"
    - "DefaultCrewManager.test.tsx passes both standalone and inside the full suite run"
  artifacts: []
---

<objective>
Close the remaining six red frontend test files from CONTEXT.md Kriterium 1 that have no shared
root cause with each other or with the other Criterion-1 plans in this wave — each requires its own
small, targeted fix, per CONTEXT.md's own framing ("keine gemeinsames Muster... jede Datei braucht
eigene Triage").

Purpose: complete ROADMAP Success Criterion 1's remaining coverage.
Output: six files green, `next.config.mjs` image pattern narrowed, two test files corrected to match
already-live production behavior, one component bug fixed, one test-isolation bug fixed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix MemberBadgeChain's roles-progress group rendering</name>
  <files>frontend/src/components/profile/MemberBadgeChain.tsx</files>
  <read_first>frontend/src/components/profile/MemberBadgeChain.tsx, frontend/src/components/profile/MemberBadgeChain.test.tsx, frontend/src/app/members/[slug]/page.test.tsx</read_first>
  <action>
    Four related failures point at the same component: (1) MemberBadgeChain.test.tsx expects text
    "1 mitgetragenes Projekt · Noch 4 bis Silber" not found in the rendered `roles`-group progress
    card; (2) it expects a label "Gründungsmitglied · Gesperrt" not found for a non-founder locked
    founding stage; (3) it expects `container.querySelector('[data-anime-project-stage]')` to be
    non-null but gets null; (4) `members/[slug]/page.test.tsx` expects a level-3 heading
    "Rollenfortschritt" (rendered at `MemberBadgeChain.tsx:682` only `if (group.key === 'roles')`)
    among the page's headings, but it is missing — meaning the `roles` badge group is not reaching
    the render path at all under the fixtures both tests use. Investigate `MemberBadgeChain.tsx`'s
    group-building/gating logic (read the full file) against both test files' fixture data to find
    why the `roles` group (and its founding-stage/anime-project-stage sub-elements) is being
    filtered out or mis-computed. This is the one file in this plan requiring genuine
    root-cause debugging rather than a known 1-line fix — use the four concrete failure messages
    above as your starting hypotheses (all four look like symptoms of one gating/data-shape bug,
    not four separate bugs) and confirm with `git log --oneline -- frontend/src/components/profile/MemberBadgeChain.tsx`
    to see if a recent commit changed the gating condition.
  </action>
  <acceptance_criteria>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/components/profile/MemberBadgeChain.test.tsx"</automated>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/app/members/[slug]/page.test.tsx"</automated>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/components/profile/MemberBadgeChain.test.tsx"</automated>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/app/members/[slug]/page.test.tsx"</automated>
  </verify>
  <done>All four MemberBadgeChain.test.tsx cases and members/[slug]/page.test.tsx's heading-list case pass.</done>
</task>

<task type="auto">
  <name>Task 2: Narrow next.config.mjs media pattern, fix docs allowlist paths, align MembershipsSection grid assertion</name>
  <files>frontend/next.config.mjs, frontend/src/lib/api.no-token-boundary.test.ts, frontend/src/components/profile/MembershipsSection.test.tsx</files>
  <read_first>frontend/next.config.mjs, frontend/src/components/ui/ResponsiveImage.config.test.ts, frontend/src/lib/api.no-token-boundary.test.ts, frontend/src/components/profile/profile.module.css</read_first>
  <action>
    (a) `next.config.mjs`'s `images.localPatterns` currently has `{ pathname: '/media/**', search: '' }`,
    which is too broad: `ResponsiveImage.config.test.ts` requires
    `/media/admin/private/original.jpg` to be DENIED while `/media/release-version/1/asset/original.jpg`
    and `/media/profile/1/avatar/.../original.png` stay ALLOWED. Before narrowing, run
    `grep -rn "'/media/" frontend/src --include=*.ts --include=*.tsx | grep -v test` to enumerate
    every legitimate `/media/...` subpath the app actually constructs (at minimum
    `/media/profile/**` and `/media/release-version/**` are proven by the test file; there may be
    more, e.g. group/gallery namespaces — include all found). Replace the single `/media/**` entry
    with one explicit `{ pathname: '/media/<namespace>/**', search: '' }` entry per legitimate
    namespace found, deliberately excluding any `/media/admin/**` path.

    (b) `api.no-token-boundary.test.ts`'s `docsAllowlist` (near line 46) still points at
    `../.planning/phases/49-zentraler-auth-api-client-und-token-lifecycle-h-rtung/49-auth-api-client-boundaries.md`
    and the sibling `-inventory.md`, but these were relocated during a milestone archival to
    `.planning/milestones/pre-v1.3-recovery-2026-08-13/phases/49-zentraler-auth-api-client-und-token-lifecycle-h-rtung/`
    (confirmed present at that path via `find .planning -iname "*auth-api-client-boundaries*"`).
    Update both `docsAllowlist` entries to the new relative path
    (`../.planning/milestones/pre-v1.3-recovery-2026-08-13/phases/49-zentraler-auth-api-client-und-token-lifecycle-h-rtung/49-auth-api-client-boundaries.md`
    and `...-inventory.md`), keeping the two `auth-api-client.md`/`streaming-auth-handoff.md`
    entries untouched (they are separately exempted in the test body regardless of existence).

    (c) `MembershipsSection.test.tsx`'s "keeps membership cards bounded in a responsive overflow-safe
    grid" test asserts the base (non-media-query) `.membershipsList` rule contains
    `grid-template-columns: repeat(3, minmax(0, 360px));` and does NOT contain `auto-fit`. The live
    CSS at `profile.module.css:385-393` already uses
    `grid-template-columns: repeat(auto-fit, minmax(min(100%, 18rem), 1fr));` (a more modern,
    breakpoint-free responsive pattern) — the two `@media` overrides at 1100px/680px this same test
    also checks (lines 540-548 of profile.module.css) are UNCHANGED and already passing, so only
    the base-rule assertion is stale. Update the test's two failing expectations (`toContain('repeat(3,
    minmax(0, 360px));')` and `not.toContain('auto-fit')`) to assert the live auto-fit rule instead,
    preserving the test's overflow-safety intent (still assert `justify-content: start;` and
    `min-width: 0;`, which already pass unchanged).
  </action>
  <acceptance_criteria>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/components/ui/ResponsiveImage.config.test.ts"</automated>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/lib/api.no-token-boundary.test.ts"</automated>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/components/profile/MembershipsSection.test.tsx"</automated>
    - `docker compose exec team4sv30-frontend npx vitest run` full suite shows no NEW image-loading regressions in any test that renders profile/release-version media (spot check via full-suite pass/fail count from Task 3 of this plan)
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/components/ui/ResponsiveImage.config.test.ts"</automated>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/lib/api.no-token-boundary.test.ts"</automated>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/components/profile/MembershipsSection.test.tsx"</automated>
  </verify>
  <done>All three files pass; next.config.mjs still allows every legitimate /media/ namespace found via the grep audit.</done>
</task>

<task type="auto">
  <name>Task 3: Fix the stale screenshot label in ReleaseGallery.test.tsx and diagnose the DefaultCrewManager suite-order failure</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseGallery.test.tsx, frontend/src/app/admin/fansubs/[id]/edit/DefaultCrewManager.test.tsx</files>
  <read_first>"frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseGallery.tsx", frontend/src/types/releaseVersionMedia.ts, frontend/src/app/admin/fansubs/[id]/edit/DefaultCrewManager.test.tsx</read_first>
  <action>
    (a) `ReleaseGallery.tsx` derives its image title/alt/badge text from
    `CATEGORY_LABELS[image.category]` (`frontend/src/types/releaseVersionMedia.ts`), where
    `screenshot: 'Fansub Screenshot'` is the current, widely-used label (same root cause already
    fixed for `admin/fansubs/[id]/edit/page.test.tsx`'s DrawerSummary test in a sibling plan).
    `ReleaseGallery.test.tsx` currently expects "Release-Screenshot" text and alt attribute — update
    both assertions (`getByText`/`getByAltText` matching "Release-Screenshot") to expect
    "Fansub Screenshot" instead.

    (b) `DefaultCrewManager.test.tsx` passes when run standalone
    (`npx vitest run "src/app/admin/fansubs/[id]/edit/DefaultCrewManager.test.tsx"`) but fails with
    an uncaught `useRoleCatalog must be used within RoleCatalogProvider` error when the full suite
    runs — this is test-isolation pollution, not a fixture bug (this file's own fixtures already
    mock the provider correctly per its `vi.mock('@/providers/RoleCatalogProvider', ...)` call).
    Bisect: run `npx vitest run "src/app/admin/fansubs/[id]/edit/"` (whole directory) to see if it
    reproduces with a smaller file set than the full suite; if so, narrow further by running pairs/
    small groups from that directory until you find the specific neighboring test file whose
    module-level state (a `vi.mock` factory without proper reset, a shared singleton, or a
    `RoleCatalogProvider`-related import side effect) leaks into `DefaultCrewManager.test.tsx`'s
    worker. Once identified, fix at the source (typically: add `vi.resetModules()` or
    `vi.restoreAllMocks()` in that OTHER file's `afterEach`, or make its `vi.mock` factory
    self-contained rather than relying on shared hoisted state) — do not silence this by weakening
    DefaultCrewManager.test.tsx's own assertions.
  </action>
  <acceptance_criteria>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseGallery.test.tsx"</automated>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/app/admin/fansubs/[id]/edit/DefaultCrewManager.test.tsx"</automated>
    <automated>docker compose exec team4sv30-frontend npx vitest run</automated>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseGallery.test.tsx"</automated>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/app/admin/fansubs/[id]/edit/DefaultCrewManager.test.tsx"</automated>
    <automated>docker compose exec team4sv30-frontend npx vitest run</automated>
  </verify>
  <done>Both files pass standalone; the full-suite run (after all Criterion-1 plans in this wave land) no longer shows DefaultCrewManager.test.tsx failing due to cross-file pollution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser image loader -> `/media/**` local patterns | Next.js `localPatterns` config governs which local paths the image optimizer will fetch/serve |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-07-01 | Information Disclosure | next.config.mjs images.localPatterns | mitigate | Narrow the current `/media/**` wildcard to explicit legitimate namespaces so `/media/admin/**` (private/internal assets) can never be served through the public image optimizer, per the test's own security intent. |
| T-143-07-02 | Tampering | DefaultCrewManager.test.tsx suite-order pollution | accept | Test-infrastructure isolation bug only, no production code path affected; fix confined to test setup/mocks. |
</threat_model>

<verification>
<automated>docker compose exec team4sv30-frontend npx vitest run</automated>
</verification>

<success_criteria>
All six listed files pass individually; the full-suite run's total failed-file count drops by the
combined total of this plan and its sibling Criterion-1 plans (143-05, 143-06) in this wave.
</success_criteria>

<output>
Create `.planning/phases/143-phase-142-nacharbeit-und-dashboard-lane-f-r-abgelehnte-notiz/143-07-SUMMARY.md` when done
</output>
