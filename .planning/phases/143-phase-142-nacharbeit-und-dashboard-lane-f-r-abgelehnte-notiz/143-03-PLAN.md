---
phase: 143
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/repository/anime_contributions_proposal_repository.go
  - backend/internal/repository/anime_contributions_proposal_member_repository.go
  - backend/internal/repository/member_profile_projects_repository.go
  - backend/internal/repository/member_profile_projects_release_versions_repository.go
autonomous: true
requirements: ["Criterion-6-Randbedingung-450-line-repos"]
must_haves:
  truths:
    - "anime_contributions_proposal_repository.go is at or below 450 lines (was 461)"
    - "member_profile_projects_repository.go is at or below 450 lines (was 458)"
    - "No proposal-review or member-project-listing behavior changed"
  artifacts:
    - path: "backend/internal/repository/anime_contributions_proposal_repository.go"
      provides: "ProposalInput, GroupProposalRow, CreateProposal, ListProposedByGroup, Confirm, Reject"
    - path: "backend/internal/repository/anime_contributions_proposal_member_repository.go"
      provides: "MemberContributionWithProposalRow, ListByMemberIDWithProposalFields, hasAnimeContributionReviewNoteColumn, SelfPublish"
    - path: "backend/internal/repository/member_profile_projects_repository.go"
      provides: "loadCurrentProjects, countCurrentProjects, loadKnownFor, GetPublicMemberProjects, GetPublicMemberProjectsByID"
    - path: "backend/internal/repository/member_profile_projects_release_versions_repository.go"
      provides: "loadCurrentProjectReleaseVersionsBatch, sameMemberRoleSets, decodeMemberRoles"
---

<objective>
Split two more oversized repository files past the 450-line cap: `anime_contributions_proposal_repository.go`
(461 lines) and `member_profile_projects_repository.go` (458 lines). Both are pure moves within
the same `repository` package — no behavior change.

Purpose: complete the ROADMAP-mandated 450-line remediation for the four files named in Phase 143's
Randbedingungen (this plan covers the two NOT related to the dashboard/claims work covered
elsewhere in this phase).
Output: four files, all <= 450 lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split anime_contributions_proposal_repository.go</name>
  <files>backend/internal/repository/anime_contributions_proposal_repository.go, backend/internal/repository/anime_contributions_proposal_member_repository.go</files>
  <read_first>backend/internal/repository/anime_contributions_proposal_repository.go</read_first>
  <action>
    Read the full current file (461 lines, `package repository`, receiver `*AnimeContributionsRepository`).
    Confirmed current boundaries via `grep -n "^func \|^type "`: `ProposalInput` (15), `GroupProposalRow`
    (31), `CreateProposal` (46), `ListProposedByGroup` (153), `Confirm` (207), `Reject` (231),
    `MemberContributionWithProposalRow` (258), `ListByMemberIDWithProposalFields` (276),
    `hasAnimeContributionReviewNoteColumn` (402), `SelfPublish` (424). Re-verify with a fresh grep
    before cutting since line numbers may have shifted.

    Move (cut) into `anime_contributions_proposal_member_repository.go`: the type
    `MemberContributionWithProposalRow` and the functions `ListByMemberIDWithProposalFields`,
    `hasAnimeContributionReviewNoteColumn`, `SelfPublish` (lines ~258-461, ~204 lines). Keep in
    the original file: `ProposalInput`, `GroupProposalRow`, `CreateProposal`, `ListProposedByGroup`,
    `Confirm`, `Reject` (~257 lines remaining). Both files `package repository`, receiver
    `(r *AnimeContributionsRepository)`.
  </action>
  <acceptance_criteria>
    - `wc -l backend/internal/repository/anime_contributions_proposal_repository.go` reports <= 450
    - `wc -l backend/internal/repository/anime_contributions_proposal_member_repository.go` reports <= 450
    - `grep -c "^func (r \*AnimeContributionsRepository) SelfPublish" backend/internal/repository/anime_contributions_proposal_member_repository.go` equals 1
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-backend go build ./...</automated>
  </verify>
  <done>Both files exist, each under 450 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Split member_profile_projects_repository.go</name>
  <files>backend/internal/repository/member_profile_projects_repository.go, backend/internal/repository/member_profile_projects_release_versions_repository.go</files>
  <read_first>backend/internal/repository/member_profile_projects_repository.go</read_first>
  <action>
    Read the full current file (458 lines, `package repository`, receiver `*MemberProfileRepository`).
    Confirmed current boundaries via `grep -n "^func \|^type "`: `loadCurrentProjects` (19),
    `countCurrentProjects` (157), `loadKnownFor` (190), `GetPublicMemberProjects` (290),
    `GetPublicMemberProjectsByID` (305), `memberProjectKey` type (324),
    `loadCurrentProjectReleaseVersionsBatch` (336), `sameMemberRoleSets` (430), `decodeMemberRoles`
    (449). Re-verify with a fresh grep before cutting.

    Move (cut) into `member_profile_projects_release_versions_repository.go`: the type
    `memberProjectKey` and the functions `loadCurrentProjectReleaseVersionsBatch`,
    `sameMemberRoleSets`, `decodeMemberRoles` (lines ~324-458, ~135 lines). Keep in the original
    file: `loadCurrentProjects`, `countCurrentProjects`, `loadKnownFor`, `GetPublicMemberProjects`,
    `GetPublicMemberProjectsByID` (~323 lines remaining). Both files `package repository`, receiver
    `(r *MemberProfileRepository)` where applicable (the two moved standalone helper functions
    `sameMemberRoleSets`/`decodeMemberRoles` have no receiver — keep them as plain package functions).
  </action>
  <acceptance_criteria>
    - `wc -l backend/internal/repository/member_profile_projects_repository.go` reports <= 450
    - `wc -l backend/internal/repository/member_profile_projects_release_versions_repository.go` reports <= 450
    - `grep -c "^func (r \*MemberProfileRepository) loadCurrentProjectReleaseVersionsBatch" backend/internal/repository/member_profile_projects_release_versions_repository.go` equals 1
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-backend go build ./...</automated>
  </verify>
  <done>Both files exist, each under 450 lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | Pure repository-layer file relocation, no new trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-03 | Tampering | anime_contributions_proposal_*.go / member_profile_projects_*.go split | accept | Pure relocation, zero SQL/logic change; existing test suites for both structs (run in verification) are the parity proof. |
</threat_model>

<verification>
<automated>docker compose exec team4sv30-backend go build ./...</automated>
<automated>docker compose exec team4sv30-backend go test ./internal/repository/... -run "TestAnimeContributionsRepository|TestMemberProfileRepository|TestGetPublicMemberProjects"</automated>
</verification>

<success_criteria>
All four target files (from Tasks 1 and 2) report <= 450 lines via `wc -l`; backend builds; existing
proposal and member-project repository tests pass unmodified.
</success_criteria>

<output>
Create `.planning/phases/143-phase-142-nacharbeit-und-dashboard-lane-f-r-abgelehnte-notiz/143-03-SUMMARY.md` when done
</output>
