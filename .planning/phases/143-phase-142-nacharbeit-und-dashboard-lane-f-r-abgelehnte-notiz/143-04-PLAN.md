---
phase: 143
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - "frontend/src/app/me/releases/[versionId]/workspace/page.tsx"
  - "frontend/src/app/me/releases/[versionId]/workspace/workspaceHelpers.ts"
autonomous: true
requirements: ["Criterion-6-Randbedingung-workspace-page-450"]
must_haves:
  truths:
    - "workspace/page.tsx is at or below 450 lines (was 469)"
    - "Criterion 7 (a later wave) must find this file already under budget and must not need to touch it again"
    - "No rendering/behavior change to the release workspace page"
  artifacts:
    - path: "frontend/src/app/me/releases/[versionId]/workspace/workspaceHelpers.ts"
      provides: "parsePositiveInt, readErrorMessage, formatEpisodeNumber, formatEpisodeLabel, getProjectReturnPath, formatAdjacentReleaseLabel, parseWorkspaceTab, buildWorkspaceHref, WorkspaceTab, AdjacentReleases, NavigationState"
  key_links:
    - from: "frontend/src/app/me/releases/[versionId]/workspace/page.tsx"
      to: "frontend/src/app/me/releases/[versionId]/workspace/workspaceHelpers.ts"
      via: "named import"
      pattern: "from './workspaceHelpers'"
---

<objective>
Extract the pure helper functions and shared types out of
`frontend/src/app/me/releases/[versionId]/workspace/page.tsx` (469 lines) into a new
`workspaceHelpers.ts` sibling file, bringing `page.tsx` under the 450-line cap BEFORE Criterion 7
(a later wave in this phase) touches the dashboard lane — ROADMAP explicitly forbids Criterion 7
from growing this file further, so it must already be compliant.

Purpose: satisfy CLAUDE.md's Modularity constraint without changing any rendered output.
Output: `workspaceHelpers.ts` (new, ~60 lines) + `page.tsx` shrunk to ~415 lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract pure helpers/types into workspaceHelpers.ts</name>
  <files>frontend/src/app/me/releases/[versionId]/workspace/page.tsx, frontend/src/app/me/releases/[versionId]/workspace/workspaceHelpers.ts</files>
  <read_first>frontend/src/app/me/releases/[versionId]/workspace/page.tsx</read_first>
  <action>
    Read the full current `page.tsx` (469 lines). Move (cut) these currently module-scope,
    zero-hook, zero-JSX pure functions and types (lines 39-92 in the current file — re-verify exact
    lines before cutting) into a new `workspaceHelpers.ts`: `parsePositiveInt`, `readErrorMessage`,
    `formatEpisodeNumber`, `formatEpisodeLabel`, `getProjectReturnPath`, `formatAdjacentReleaseLabel`,
    `parseWorkspaceTab`, `buildWorkspaceHref`, plus the type aliases `WorkspaceTab`,
    `AdjacentReleases`, `NavigationState`. `readErrorMessage` needs `ApiError` from `@/lib/api`;
    `formatAdjacentReleaseLabel` needs `MeProjectReleaseVersion` from `@/types/contributions` — carry
    both imports into the new file. Export every moved identifier. In `page.tsx`, replace the moved
    definitions with a single named import:
    `import { AdjacentReleases, NavigationState, WorkspaceTab, buildWorkspaceHref, formatAdjacentReleaseLabel, formatEpisodeLabel, formatEpisodeNumber, getProjectReturnPath, parsePositiveInt, parseWorkspaceTab, readErrorMessage } from './workspaceHelpers'`.
    Do not change any call site's argument order or behavior — this is a pure relocation.
  </action>
  <acceptance_criteria>
    - `wc -l "frontend/src/app/me/releases/[versionId]/workspace/page.tsx"` reports <= 450
    - `wc -l "frontend/src/app/me/releases/[versionId]/workspace/workspaceHelpers.ts"` reports a new file with all 8 functions + 3 types exported
    - `grep -c "^function \|^export function \|^type " "frontend/src/app/me/releases/[versionId]/workspace/page.tsx"` shows none of the 8 moved function names remain defined locally
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npx tsc --noEmit</automated>
  </verify>
  <done>workspaceHelpers.ts exists with all 8 helpers + 3 types; page.tsx imports them and is under 450 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Typecheck and confirm no behavior change</name>
  <files>frontend/src/app/me/releases/[versionId]/workspace/page.tsx, frontend/src/app/me/releases/[versionId]/workspace/workspaceHelpers.ts</files>
  <read_first>frontend/vitest.config.ts</read_first>
  <action>
    Run the frontend TypeScript build/typecheck and ESLint against just these two files to confirm
    no unused imports or missing exports. There is no existing dedicated test file for this page
    (route-level manual/E2E coverage only per prior phases) — do not invent one; confirm via
    typecheck + lint only, which is sufficient proof for a pure extraction.
  </action>
  <acceptance_criteria>
    <automated>docker compose exec team4sv30-frontend npx tsc --noEmit</automated>
    <automated>docker compose exec team4sv30-frontend npx eslint "src/app/me/releases/[versionId]/workspace/page.tsx" "src/app/me/releases/[versionId]/workspace/workspaceHelpers.ts"</automated>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npx tsc --noEmit</automated>
    <automated>docker compose exec team4sv30-frontend npx eslint "src/app/me/releases/[versionId]/workspace/page.tsx" "src/app/me/releases/[versionId]/workspace/workspaceHelpers.ts"</automated>
  </verify>
  <done>Typecheck and lint pass clean for both files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | Pure client-side helper extraction, no new trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-04 | Tampering | workspace/page.tsx extraction | accept | Pure relocation of presentation-only helpers; typecheck + lint are sufficient parity proof for this risk level. |
</threat_model>

<verification>
<automated>docker compose exec team4sv30-frontend npx tsc --noEmit</automated>
</verification>

<success_criteria>
`page.tsx` reports <= 450 lines via `wc -l`; typecheck and lint pass; no rendered output changes.
</success_criteria>

<output>
Create `.planning/phases/143-phase-142-nacharbeit-und-dashboard-lane-f-r-abgelehnte-notiz/143-04-SUMMARY.md` when done
</output>
