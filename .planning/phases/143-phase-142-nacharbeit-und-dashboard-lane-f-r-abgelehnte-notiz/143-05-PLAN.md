---
phase: 143
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/contracts/openapi.yaml
  - "frontend/src/types/__tests__/v12-projection-contract.test.ts"
  - "frontend/src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.test.tsx"
  - "frontend/src/app/admin/fansubs/[id]/edit/page.test.tsx"
  - "frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/mitwirkende/[memberSlug]/page.test.tsx"
  - "frontend/src/app/admin/episode-versions/[versionId]/edit/page.test.tsx"
autonomous: true
requirements: ["Criterion-1"]
must_haves:
  truths:
    - "v12-projection-contract.test.ts's PublicMemberBadge.next_tier assertion agrees with openapi.yaml"
    - "FansubAppMembersOverview-rendering test trees resolve useRoleCatalog without throwing"
    - "admin/fansubs/[id]/edit/page.test.tsx's ReleaseVersionMediaDrawerSummary category label assertion matches the current CATEGORY_LABELS value"
    - "episode-versions edit page.test.tsx's 14 currently-failing cases resolve the missing api mock export"
  artifacts: []
  key_links:
    - from: "frontend/src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.test.tsx"
      to: "frontend/src/providers/RoleCatalogProvider.tsx"
      via: "vi.mock('@/providers/RoleCatalogProvider', ...)"
      pattern: "vi\\.mock\\('@/providers/RoleCatalogProvider'"
---

<objective>
Close four of the 17 red frontend test files from CONTEXT.md Kriterium 1 that share exactly-known,
already-diagnosed root causes: the `PublicMemberBadge.next_tier` contract drift, the missing
`RoleCatalogProvider` in three component test trees, a stale category-label string in one of those
same three files, and a missing `@/lib/api` mock export in a fourth, unrelated file.

Purpose: implement ROADMAP Success Criterion 1 for this specific, fully-diagnosed slice (5 of the
17 files: `v12-projection-contract.test.ts`, `FansubAppMembersSection.test.tsx`,
`admin/fansubs/[id]/edit/page.test.tsx`, `mitwirkende/[memberSlug]/page.test.tsx`,
`admin/episode-versions/[versionId]/edit/page.test.tsx`).
Output: five test files green, `shared/contracts/openapi.yaml` unchanged (it is already correct —
see Task 1's reasoning).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-phase-142-nacharbeit-und-dashboard-lane-f-r-abgelehnte-notiz/143-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the PublicMemberBadge.next_tier contract-drift decision</name>
  <files>frontend/src/types/__tests__/v12-projection-contract.test.ts</files>
  <read_first>shared/contracts/openapi.yaml, frontend/src/types/__tests__/v12-projection-contract.test.ts</read_first>
  <action>
    This is the CONTEXT.md-flagged "decide which side is correct" item. Verified state: at
    `shared/contracts/openapi.yaml` around line 11988, `PublicMemberBadge.next_tier` already has
    `enum: [bronze, silver, gold, platinum]`, and `current_tier` (line 11978) already has
    `enum: [entry, bronze, silver, gold, platinum]` — the openapi.yaml side is correct and needs no
    edit. The test at `v12-projection-contract.test.ts:276` asserts
    `expect(block).toContain("enum: [bronze, silver, gold]")` (missing `platinum`) — this is the
    stale side. Resolution: `next_tier` is "the tier a member is progressing TOWARD"; a member at
    `current_tier: 'gold'` has `next_tier: 'platinum'` (see the same test file's own badge fixture
    at line ~255-262, which sets `current_tier: 'platinum'` and correspondingly `next_tier: null`
    at the ceiling — proving `platinum` IS a valid reachable `next_tier` value for a gold member).
    Change ONLY line 276 to `expect(block).toContain("enum: [bronze, silver, gold, platinum]")`. Do
    not touch line 275 (`current_tier` assertion) or any other line — that one already matches.
  </action>
  <acceptance_criteria>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/types/__tests__/v12-projection-contract.test.ts"</automated>
    - `git diff shared/contracts/openapi.yaml` is empty (no change needed on the contract side)
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/types/__tests__/v12-projection-contract.test.ts"</automated>
  </verify>
  <done>v12-projection-contract.test.ts's "keeps PublicMemberBadge role-progress metadata aligned" test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add RoleCatalogProvider mocks and fix the stale screenshot-category label</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.test.tsx, frontend/src/app/admin/fansubs/[id]/edit/page.test.tsx, frontend/src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/mitwirkende/[memberSlug]/page.test.tsx</files>
  <read_first>frontend/src/app/admin/fansubs/[id]/edit/DefaultCrewManager.test.tsx, frontend/src/providers/RoleCatalogProvider.tsx, frontend/src/app/admin/fansubs/[id]/edit/FansubAppMembersOverview.tsx, frontend/src/types/releaseVersionMedia.ts</read_first>
  <action>
    Root cause (confirmed via live `npx vitest run`): `FansubAppMembersOverview.tsx:154` calls
    `useRoleCatalog('fansub_group')`, which throws `Error: useRoleCatalog must be used within
    RoleCatalogProvider` in any test tree that renders it without a provider. This crashes ALL
    tests in `FansubAppMembersSection.test.tsx` (7 tests), the "AdminFansubEditPage token-free
    wiring" describe block in `admin/fansubs/[id]/edit/page.test.tsx` (9 of its 10 failures), and
    (via `ProjectMemberHero.tsx:31` calling `useRoleCatalog` during SSR) the
    `mitwirkende/[memberSlug]/page.test.tsx` render. Add the exact `vi.mock` already used by
    `DefaultCrewManager.test.tsx` (and `ReleaseContributionDrawer.test.tsx`/`AdminGroupsClient.test.tsx`/
    `UserContributionsTab.test.tsx`) to the top of each of the three files:
    `vi.mock('@/providers/RoleCatalogProvider', () => ({ useRoleCatalog: () => ({ roles: catalogRoles, error: null }) }))`
    — substitute `catalogRoles` with whatever locally-defined role-fixture array each file already
    declares for other purposes (or a minimal new one if none exists), matching that file's own
    existing test data conventions.

    SEPARATELY in `admin/fansubs/[id]/edit/page.test.tsx`: the `ReleaseVersionMediaDrawerSummary`
    describe block's "treats a null media payload like an empty drawer summary instead of crashing"
    failure is UNRELATED to the provider issue — it expects text matching `/Release-Screenshot:/i`,
    but `frontend/src/app/admin/fansubs/[id]/edit/ReleaseVersionMediaDrawerSummary.tsx:57` renders
    `CATEGORY_LABELS[cat]` from `frontend/src/types/releaseVersionMedia.ts`, where
    `screenshot: 'Fansub Screenshot'` is the current, widely-used label (15+ other call sites across
    the codebase already use "Fansub Screenshot", confirmed via
    `grep -rln "Fansub Screenshot" frontend/src`). The test's expectation is stale, not the shared
    label. Update this test's assertion from matching `/Release-Screenshot:/i` to matching
    `/Fansub Screenshot:/i` (find the exact assertion via
    `grep -n "Release-Screenshot" "frontend/src/app/admin/fansubs/[id]/edit/page.test.tsx"`).
  </action>
  <acceptance_criteria>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.test.tsx"</automated>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/app/admin/fansubs/[id]/edit/page.test.tsx"</automated>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/mitwirkende/[memberSlug]/page.test.tsx"</automated>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.test.tsx"</automated>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/app/admin/fansubs/[id]/edit/page.test.tsx"</automated>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/mitwirkende/[memberSlug]/page.test.tsx"</automated>
  </verify>
  <done>All three files pass; the RoleCatalogProvider mock is present in each; the drawer-summary label assertion matches the live "Fansub Screenshot" label.</done>
</task>

<task type="auto">
  <name>Task 3: Add the missing getAnimeFansubProjectTimeline mock export</name>
  <files>frontend/src/app/admin/episode-versions/[versionId]/edit/page.test.tsx</files>
  <read_first>frontend/src/app/admin/episode-versions/[versionId]/edit/EpisodeVersionEditorPage.tsx</read_first>
  <action>
    Root cause (confirmed via live `npx vitest run`, single error repeated across all 14 failing
    cases in this file): `EpisodeVersionEditorPage.tsx:157` calls `getAnimeFansubProjectTimeline`
    (an effect added for the Criterion-6/UI-SPEC project-timeline integration), but this test
    file's `vi.mock('@/lib/api', ...)` factory does not export it, so vitest throws
    `No "getAnimeFansubProjectTimeline" export is defined on the "@/lib/api" mock`. Locate the
    `vi.mock('@/lib/api', ...)` block at the top of this test file and add
    `getAnimeFansubProjectTimeline: vi.fn().mockResolvedValue(null)` (or the file's established
    pattern for optional/absent related-data mocks — match whatever neighboring mock entries in the
    same object literal already do for shape, e.g. if others return `{ data: ... }` envelopes,
    match that; `getAnimeFansubProjectTimeline` in `@/lib/api` returns the timeline object directly,
    not envelope-wrapped — confirm the real function's return type before choosing the mock's
    resolved value).
  </action>
  <acceptance_criteria>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/app/admin/episode-versions/[versionId]/edit/page.test.tsx"</automated>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npx vitest run "src/app/admin/episode-versions/[versionId]/edit/page.test.tsx"</automated>
  </verify>
  <done>All 14 previously-failing cases in this file pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | Test-file and contract-documentation fixes only, no runtime code path changed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-05 | Information Disclosure | openapi.yaml enum documentation | accept | Documentation-only decision (which enum values are valid); no runtime behavior affected since the backend already emits `platinum` and the frontend types already accept it. |
</threat_model>

<verification>
<automated>docker compose exec team4sv30-frontend npx vitest run "src/types/__tests__/v12-projection-contract.test.ts" "src/app/admin/fansubs/[id]/edit/FansubAppMembersSection.test.tsx" "src/app/admin/fansubs/[id]/edit/page.test.tsx" "src/app/fansubs/[slug]/fansubprojekt/[animeSlug]/mitwirkende/[memberSlug]/page.test.tsx" "src/app/admin/episode-versions/[versionId]/edit/page.test.tsx"</automated>
</verification>

<success_criteria>
All five listed test files pass with zero failures; no other previously-green test regresses.
</success_criteria>

<output>
Create `.planning/phases/143-phase-142-nacharbeit-und-dashboard-lane-f-r-abgelehnte-notiz/143-05-SUMMARY.md` when done
</output>
