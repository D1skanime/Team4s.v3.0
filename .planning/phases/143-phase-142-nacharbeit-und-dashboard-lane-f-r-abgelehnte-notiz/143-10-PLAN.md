---
phase: 143
plan: 10
type: execute
wave: 4
depends_on: ["143-09"]
files_modified:
  - backend/internal/services/release_metadata_credit_service_test.go
  - backend/internal/repository/anime_fansub_project_timeline_repository_test.go
  - backend/internal/handlers/admin_content_anime_project_timeline_test.go
autonomous: true
requirements: ["Criterion-4"]
must_haves:
  truths:
    - "ReleaseMetadataCreditService.AwardIfCompleted has a first test covering the ambiguous rv.id/rev.id lookup"
    - "FansubNotesRepository.UpdateAnimeFansubProjectTimeline's 'end before completed release' date rule has a fixture-based test"
    - "TestUpdateAnimeFansubProjectTimelineDeniesQualityChecker posts to /timeline, not /project-timeline"
  artifacts:
    - path: "backend/internal/services/release_metadata_credit_service_test.go"
      provides: "first test file for ReleaseMetadataCreditService"
      exports: ["TestReleaseMetadataCreditServiceAwardIfCompleted"]
    - path: "backend/internal/repository/anime_fansub_project_timeline_repository_test.go"
      provides: "date-validation fixture test for UpdateAnimeFansubProjectTimeline"
      exports: ["TestUpdateAnimeFansubProjectTimelineRejectsEndBeforeCompletedRelease"]
---

<objective>
Add focus tests for two pieces of untested new logic named in ROADMAP Success Criterion 4, and fix
the one known test-route bug alongside them (same handler, same file family, cheap to bundle).

Purpose: implement ROADMAP Success Criterion 4.
Output: two new test files with first-ever coverage of `AwardIfCompleted`'s ambiguous ID lookup and
`UpdateAnimeFansubProjectTimeline`'s date-validation rule; one corrected route string in an existing
test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-phase-142-nacharbeit-und-dashboard-lane-f-r-abgelehnte-notiz/143-PATTERNS.md
</context>

<interfaces>
Constructor under test: `services.NewReleaseMetadataCreditService(starter PointTxStarter) *ReleaseMetadataCreditService`
(`backend/internal/services/release_metadata_credit_service.go:26-28` — takes ONLY `starter`, unlike
the analog `NewProjectNoteCreditService`'s two-argument signature; do not add a second argument).

The ambiguous query under test (`release_metadata_credit_service.go:43-51`):
```sql
SELECT rev.id, rev.production_started_on, COALESCE(rev.release_date, fr.release_date),
       (SELECT rvg.fansub_group_id FROM release_version_groups rvg WHERE rvg.release_version_id = rev.id ORDER BY rvg.fansub_group_id LIMIT 1)
FROM release_variants rv
JOIN release_versions rev ON rev.id = rv.release_version_id
JOIN fansub_releases fr ON fr.id = rev.release_id
WHERE rv.id = $1 OR rev.id = $1
ORDER BY rv.id LIMIT 1
FOR UPDATE OF rv, rev, fr
```
Same ID space is checked against BOTH `release_variants.id` and `release_versions.id` — construct a
fixture where a `release_variants` row and a `release_versions` row have colliding/adjacent IDs and
assert (via `point_ledger_entries.source_key`, which embeds the resolved `releaseVersionID` as
`"release-version:<id>:metadata-complete"`) which row is actually credited. Document the resolved
behavior; the query does not need to change unless your fixture proves it silently credits the
wrong release version for a real (non-synthetic) ID collision.

Pool + fixture-seeding pattern to copy from `backend/internal/services/project_note_credit_service_test.go`
(full file, same package `services`): `testsupport.OpenPhase107Postgres(t)` to get a real ephemeral
Postgres pool, then apply the specific migrations that create `release_variants`/`release_versions`/
`fansub_releases`/`episodes`/`anime`/`release_version_groups`/`member_claims`/`point_ledger_entries`/
`point_rules` (read that test file to see its exact migration-application loop and adapt the
migration filename list to what this service's tables actually need), then seed rows directly.
Balance-check helper pattern to copy (`project_note_credit_service_test.go:196-209`):
```go
func projectNotePointBalance(t testing.TB, pool *pgxpool.Pool, memberID int64) int64 {
	t.Helper()
	var value int64
	require.NoError(t, pool.QueryRow(context.Background(), `
SELECT COALESCE(SUM(point_value), 0) FROM point_ledger_entries WHERE member_id = $1`, memberID).Scan(&value))
	return value
}
```

Date-validation rule under test (`anime_fansub_project_timeline_repository.go:83-85`):
```go
if completedOn != nil && latestReleaseCompletion != nil && completedOn.Before(*latestReleaseCompletion) {
	return nil, ErrInvalidProjectTimeline
}
```
`latestReleaseCompletion` comes from `MAX(COALESCE(rev.release_date, fr.release_date))` joined
through `release_version_groups` -> `release_versions` -> `fansub_releases` -> `episodes` for the
same `(fansubGroupID, animeID)` (query at lines 71-79 of the same file — read it for the exact join
tree before seeding fixture rows).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: First test for ReleaseMetadataCreditService.AwardIfCompleted's ambiguous ID lookup</name>
  <files>backend/internal/services/release_metadata_credit_service_test.go</files>
  <read_first>backend/internal/services/release_metadata_credit_service.go, backend/internal/services/project_note_credit_service_test.go</read_first>
  <action>
    Create `release_metadata_credit_service_test.go` in `package services`. Build a pool-seeding
    helper mirroring `project_note_credit_service_test.go`'s pattern (real ephemeral Postgres via
    `testsupport.OpenPhase107Postgres`, real migration application, not mocks). Seed: an `anime`
    row, a `fansub_releases`/`episodes` chain, a `release_versions` row with
    `production_started_on` and `release_date` both set (so `AwardIfCompleted`'s completeness gate
    passes), a `release_variants` row, a `release_version_groups` row linking them to a
    `fansub_group_id`, and a verified `member_claims` row for the actor. Deliberately choose the
    `release_variants.id` and `release_versions.id` to collide or be adjacent (e.g. seed so
    `release_variants.id == release_versions.id + 1` or force an explicit ID collision if your test
    DB allows manual PK assignment) so `WHERE rv.id = $1 OR rev.id = $1` has more than one candidate
    row to prove which one `ORDER BY rv.id LIMIT 1` actually picks. Call
    `service.AwardIfCompleted(ctx, <the colliding ID>, actorAppUserID)`. Assert via
    `point_ledger_entries.source_key` (format `"release-version:<releaseVersionID>:metadata-complete"`)
    which `releaseVersionID` was actually credited, and assert this matches (or explicitly documents
    a mismatch with) the release version you intended to award. Also add a straightforward
    happy-path case (no ID collision) proving the award is created exactly once and is idempotent on
    a second call (source_key uniqueness check already in the service, per
    `alreadyAwarded` short-circuit at line 63-69 of the service file).
  </action>
  <acceptance_criteria>
    <automated>docker compose exec team4sv30-backend go test ./internal/services/... -run TestReleaseMetadataCreditServiceAwardIfCompleted -v</automated>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-backend go test ./internal/services/... -run TestReleaseMetadataCreditServiceAwardIfCompleted -v</automated>
  </verify>
  <done>New test file exists, passes, and documents the resolved behavior of the ambiguous rv.id/rev.id lookup.</done>
</task>

<task type="auto">
  <name>Task 2: Fixture-based test for the project-timeline date-validation rule</name>
  <files>backend/internal/repository/anime_fansub_project_timeline_repository_test.go</files>
  <read_first>backend/internal/repository/anime_fansub_project_timeline_repository.go, backend/internal/repository/fansub_notes_repository_test.go</read_first>
  <action>
    Create `anime_fansub_project_timeline_repository_test.go` in `package repository`. Using the
    same real-Postgres fixture-seeding approach as Task 1 (this repository shares the
    `FansubNotesRepository` struct/receiver with the note repository, so reuse whatever ephemeral-DB
    helper that repository's own tests already use — check for an existing pool helper in this
    package before adding a new one), seed a fansub_group + anime + a release chain where the
    LATEST release's completion date (`MAX(COALESCE(rev.release_date, fr.release_date))`) is a known
    date, e.g. 2026-06-01. Call `UpdateAnimeFansubProjectTimeline(ctx, animeID, fansubGroupID,
    startedOn, completedOn)` with `completedOn` set BEFORE that date (e.g. 2026-05-01) and assert it
    returns `ErrInvalidProjectTimeline` without mutating the row. Call it again with `completedOn`
    set ON OR AFTER that date (e.g. 2026-06-01 or 2026-07-01) and assert it succeeds and the
    persisted row reflects the new value. Also cover the `completedOn == nil` case (rule must not
    apply when no completion date is being set) as a passing case.
  </action>
  <acceptance_criteria>
    <automated>docker compose exec team4sv30-backend go test ./internal/repository/... -run TestUpdateAnimeFansubProjectTimelineRejectsEndBeforeCompletedRelease -v</automated>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-backend go test ./internal/repository/... -run TestUpdateAnimeFansubProjectTimelineRejectsEndBeforeCompletedRelease -v</automated>
  </verify>
  <done>New test file exists, passes, and proves both the rejection and acceptance paths of the date rule against real fixture data.</done>
</task>

<task type="auto">
  <name>Task 3: Fix the stale /project-timeline route in the existing 403 test</name>
  <files>backend/internal/handlers/admin_content_anime_project_timeline_test.go</files>
  <read_first>backend/internal/handlers/admin_content_anime_project_timeline_test.go</read_first>
  <action>
    In `TestUpdateAnimeFansubProjectTimelineDeniesQualityChecker`, the `makeAppAuthTestContext` call
    builds a request against `/api/v1/admin/fansubs/88/anime/9/project-timeline` — the registered
    route (`backend/cmd/server/admin_routes.go`) is
    `/admin/fansubs/:id/anime/:animeId/timeline`. Change ONLY the path string to
    `/api/v1/admin/fansubs/88/anime/9/timeline`. Do not change the assertion logic (still expects
    403 for a quality-checker actor) or any other part of the test.
  </action>
  <acceptance_criteria>
    <automated>docker compose exec team4sv30-backend go test ./internal/handlers/... -run TestUpdateAnimeFansubProjectTimelineDeniesQualityChecker -v</automated>
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-backend go test ./internal/handlers/... -run TestUpdateAnimeFansubProjectTimelineDeniesQualityChecker -v</automated>
  </verify>
  <done>The route string matches the registered route; the test still asserts 403.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | Test-only additions; no production code path changed by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-10 | Repudiation | ReleaseMetadataCreditService ambiguous ID lookup | accept | Test-only phase; if the fixture proves the wrong row is credited, that finding must be handed to a future phase/quick-task as a real fix (VALIDATION.md explicitly scopes this phase to documenting, not necessarily changing, the query) — record the finding plainly in the test's comments and in SUMMARY.md. |
</threat_model>

<verification>
<automated>docker compose exec team4sv30-backend go test ./internal/services/... ./internal/repository/... ./internal/handlers/...</automated>
</verification>

<success_criteria>
Both new test files exist and pass; the route-string fix is applied; no other test regresses.
</success_criteria>

<output>
Create `.planning/phases/143-phase-142-nacharbeit-und-dashboard-lane-f-r-abgelehnte-notiz/143-10-SUMMARY.md` when done
</output>
