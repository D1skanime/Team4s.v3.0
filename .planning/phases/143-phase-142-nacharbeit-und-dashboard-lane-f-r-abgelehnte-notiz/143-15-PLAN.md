---
phase: 143-phase-142-nacharbeit-und-dashboard-lane-f-r-abgelehnte-notiz
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/me/dashboard/components/AttentionSection.tsx
  - frontend/src/app/me/dashboard/components/AttentionSection.module.css
  - frontend/src/app/me/dashboard/components/AttentionSection.test.tsx
autonomous: true
requirements: ["UAT-03", "UAT-04"]
must_haves:
  truths:
    - "A rejected-own-notes group with exactly one item renders a visibly shorter noteRevisionList than the default (multi-item) spacing, without losing the grouped header/badge"
    - "A rejected-own-notes group with 2+ items keeps today's spacing and grouping behavior unchanged"
    - "AttentionSection.module.css contains zero raw hex fallback values on var(--color-primary, ...) or var(--text-soft, ...) anywhere in the file, including the 4 pre-existing occurrences not introduced by this phase"
  artifacts:
    - path: "frontend/src/app/me/dashboard/components/AttentionSection.module.css"
      provides: "noteRevisionListSingle single-entry spacing override class; zero hardcoded color fallback hex values"
  key_links:
    - from: "frontend/src/app/me/dashboard/components/AttentionSection.tsx"
      to: "frontend/src/app/me/dashboard/components/AttentionSection.module.css"
      via: "conditional className applying styles.noteRevisionListSingle when group.items.length === 1"
      pattern: "noteRevisionListSingle"
---

<objective>
Close UAT-03 (the rejected-own-notes dashboard lane renders visibly taller/emptier than sibling
Attention-lane cards when a group has exactly one item) and UAT-04 (that same lane's CSS introduced
2 new hardcoded color fallback values with wrong hex, on top of 4 pre-existing ones in the same file)
found during live UAT of the already-closed Phase 143.

Purpose: both defects live in the same two files shipped by plan 143-14 (`b7ac77cc feat(143-14): add
rejected-own-notes lane to AttentionSection`) and its sibling `.module.css`; fixing them together
avoids a second round of edits to the same small file pair.
Output: a single-entry spacing override class wired conditionally in the lane's markup, and a fully
token-only color declaration in `AttentionSection.module.css` (no raw hex fallback anywhere in the
file, old or new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-phase-142-nacharbeit-und-dashboard-lane-f-r-abgelehnte-notiz/143-UAT.md
@.planning/phases/143-phase-142-nacharbeit-und-dashboard-lane-f-r-abgelehnte-notiz/143-14-SUMMARY.md
</context>

<interfaces>
Current `AttentionSection.tsx` rejected-notes lane (unchanged parts omitted), the exact `<ul>` this
plan must apply a conditional class to:
```tsx
<ul className={styles.noteRevisionList}>
  {group.items.map((item) => { /* ... */ })}
</ul>
```

Current `AttentionSection.module.css` classes this plan touches (verbatim, current state):
```css
.itemAction { color: var(--color-primary, #2f5fe3); }
.taskChip {
  border: 1px solid color-mix(in srgb, var(--color-primary, #2f5fe3) 24%, transparent);
  color: var(--color-primary, #2f5fe3);
  background: color-mix(in srgb, var(--color-primary, #2f5fe3) 8%, white);
}
.noteRevisionList { display: grid; gap: var(--space-1); margin: var(--space-3) 0 0; padding: 0; list-style: none; }
.noteRevisionRow { display: flex; align-items: center; gap: var(--space-3); padding: var(--space-2) var(--space-3); border-radius: var(--radius-sm); color: inherit; text-decoration: none; }
.noteRevisionRow:hover, .noteRevisionRow:focus-visible { background: color-mix(in srgb, var(--color-primary, #2f5fe3) 8%, transparent); }
.noteRevisionEpisode { flex: 0 0 auto; font-weight: 600; color: var(--text-soft, #6b6b70); }
```
Confirmed real token values (`frontend/src/styles/globals.css`): `--color-primary: #5f84dd;` (line 5),
`--text-soft: #625c56;` (line 135) — both DIFFER from the fallback hex currently hardcoded above
(`#2f5fe3`, `#6b6b70`). `--radius-sm: 6px` and `--space-1: 4px` already exist and are already used
verbatim elsewhere in this same file.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix UAT-03 — reduce the single-entry rejected-notes card footprint</name>
  <files>frontend/src/app/me/dashboard/components/AttentionSection.tsx, frontend/src/app/me/dashboard/components/AttentionSection.module.css, frontend/src/app/me/dashboard/components/AttentionSection.test.tsx</files>
  <read_first>frontend/src/app/me/dashboard/components/AttentionSection.tsx, frontend/src/app/me/dashboard/components/AttentionSection.module.css, frontend/src/app/me/dashboard/components/AttentionSection.test.tsx</read_first>
  <action>
    In `AttentionSection.tsx`, inside the `pendingOwnNoteRevisions.map((group) => ...)` block, compute
    whether the group has exactly one item (`group.items.length === 1`) and apply a second CSS-module
    class to the existing `<ul className={styles.noteRevisionList}>` element only in that case — e.g.
    `` `${styles.noteRevisionList} ${styles.noteRevisionListSingle}` `` when true, `styles.noteRevisionList`
    unchanged otherwise. Do not change the `<div className={styles.noteGroupHeader}>` markup, the
    `Badge`, or anything for the multi-item (2+) case — those must render byte-identical to today.

    In `AttentionSection.module.css`, add a new `.noteRevisionListSingle` class immediately after
    `.noteRevisionList` that overrides `margin-top` down from `var(--space-3)` to `var(--space-1)`
    (keep the `0` right/bottom/left values and `gap`/`padding`/`list-style` from `.noteRevisionList`
    unchanged — only override `margin-top`), plus a compound selector
    `.noteRevisionListSingle .noteRevisionRow` that overrides `.noteRevisionRow`'s vertical padding to
    `0` while keeping the existing horizontal padding (`var(--space-3)`) so the row's click target
    width and icon alignment are unchanged — only the row's own top/bottom padding is removed for the
    single-entry case. Do not modify `.noteRevisionList`, `.noteRevisionRow`, `.noteGroupHeader`, or
    any other existing rule — append only.

    Extend `AttentionSection.test.tsx`'s existing "gruppiert abgelehnte eigene Notizen..." describe
    block (or add a new `it(...)` immediately after it) with two assertions: (a) render a
    `pendingOwnNoteRevisions` group with exactly one `items` entry and assert the rendered
    `<ul>` element (query via `container.querySelector('ul[class*="noteRevisionList"]')`) has a
    `className` containing the substring `"noteRevisionListSingle"`; (b) re-render (or use the
    existing multi-item test case already in this file, which has 2 items) and assert that same
    query's `className` does NOT contain `"noteRevisionListSingle"` — proving the multi-entry
    grouping/spacing is untouched.
  </action>
  <acceptance_criteria>
    <automated>docker compose exec team4sv30-frontend npx vitest run src/app/me/dashboard/components/AttentionSection.test.tsx</automated>
    - `grep -n "noteRevisionListSingle" frontend/src/app/me/dashboard/components/AttentionSection.tsx frontend/src/app/me/dashboard/components/AttentionSection.module.css` shows the class applied conditionally in the .tsx and defined exactly once (plus its compound-selector override) in the .css
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-frontend npx vitest run src/app/me/dashboard/components/AttentionSection.test.tsx</automated>
  </verify>
  <done>A single-item rejected-notes group's list carries the noteRevisionListSingle class with reduced margin/padding; a 2+-item group does not; both proven by passing tests; no other lane or class changed.</done>
</task>

<task type="auto">
  <name>Task 2: Fix UAT-04 — remove all hardcoded color fallback values from AttentionSection.module.css</name>
  <files>frontend/src/app/me/dashboard/components/AttentionSection.module.css</files>
  <read_first>frontend/src/app/me/dashboard/components/AttentionSection.module.css, frontend/src/styles/globals.css</read_first>
  <action>
    Remove the raw-hex fallback argument from every `var(--color-primary, #2f5fe3)` and
    `var(--text-soft, #6b6b70)` occurrence in this file, replacing each with the bare token form
    `var(--color-primary)` / `var(--text-soft)` — no fallback argument at all. This covers all 6
    occurrences: `.itemAction` (1), `.taskChip`'s `border`/`color`/`background` (3), the new
    `.noteRevisionRow:hover, .noteRevisionRow:focus-visible` rule (1, introduced by plan 143-14), and
    `.noteRevisionEpisode` (1, introduced by plan 143-14). Do not change any other part of these
    rules (selectors, other property values, `color-mix()` percentages) — this is a token-fallback
    removal only, not a redesign. Do not touch any other CSS module file in the repo; CLAUDE.md's
    hardcoded-fallback rule is a real pattern beyond this file, but this plan's scope is exactly the
    file UAT-04 flagged.
  </action>
  <acceptance_criteria>
    <automated>grep -nE "#2f5fe3|#6b6b70|var\(--color-primary, #|var\(--text-soft, #" frontend/src/app/me/dashboard/components/AttentionSection.module.css | wc -l</automated>
    - The above command must output `0`
    <automated>docker compose exec team4sv30-frontend npx vitest run src/app/me/dashboard/components/AttentionSection.test.tsx</automated>
  </acceptance_criteria>
  <verify>
    <automated>grep -c "var(--color-primary)" frontend/src/app/me/dashboard/components/AttentionSection.module.css</automated>
    <automated>docker compose exec team4sv30-frontend npx vitest run src/app/me/dashboard/components/AttentionSection.test.tsx</automated>
  </verify>
  <done>Zero raw hex fallback values remain in AttentionSection.module.css (grep proof); all 13 existing AttentionSection.test.tsx cases plus Task 1's 2 new assertions still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | Presentation-only CSS/markup fix inside an already-authorized, already-shipped dashboard lane; no new data, no new endpoint, no new input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-15 | Information Disclosure | AttentionSection.module.css color tokens | accept | No new attack surface: purely visual spacing/color-token fix on data the caller already owns (own rejected notes); no new inputs, no new endpoints, no behavior change to what data renders. |
</threat_model>

<verification>
<automated>docker compose exec team4sv30-frontend npx vitest run src/app/me/dashboard/components/AttentionSection.test.tsx</automated>
<automated>grep -nE "#2f5fe3|#6b6b70|var\(--color-primary, #|var\(--text-soft, #" frontend/src/app/me/dashboard/components/AttentionSection.module.css | wc -l</automated>
</verification>

<success_criteria>
A single-entry rejected-notes group renders with reduced vertical footprint (noteRevisionListSingle
class, reduced margin/padding) while a multi-entry group is pixel-identical to before; zero hardcoded
color fallback hex values remain anywhere in AttentionSection.module.css (both the 2 newly-introduced
and 4 pre-existing occurrences); all existing and new AttentionSection.test.tsx cases pass.
</success_criteria>

<output>
Create `.planning/phases/143-phase-142-nacharbeit-und-dashboard-lane-f-r-abgelehnte-notiz/143-15-SUMMARY.md` when done
</output>
