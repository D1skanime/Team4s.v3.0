---
phase: 143
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/repository/member_claims_repository.go
  - backend/internal/repository/member_claims_submit_repository.go
  - backend/internal/repository/member_claims_queries_repository.go
autonomous: true
requirements: ["Randbedingung-450-line-member_claims_repository"]
must_haves:
  truths:
    - "member_claims_repository.go is at or below 450 lines (was 516)"
    - "No SubmitClaim/ListPendingClaimsForGroup/GetMyClaim/UpdateNoindex behavior changed"
  artifacts:
    - path: "backend/internal/repository/member_claims_repository.go"
      provides: "constructor, ListPendingClaimAttentionCandidates, SearchHistoricalMembers, VerifyClaim, RejectClaim, scan helpers"
    - path: "backend/internal/repository/member_claims_submit_repository.go"
      provides: "SubmitClaim"
    - path: "backend/internal/repository/member_claims_queries_repository.go"
      provides: "ListPendingClaimsForGroup, GetMyClaim, UpdateNoindex"
  key_links:
    - from: "backend/internal/repository/member_claims_submit_repository.go"
      to: "backend/internal/repository/member_claims_repository.go"
      via: "method receiver *MemberClaimsRepository (same package repository)"
      pattern: "func \\(r \\*MemberClaimsRepository\\)"
---

<objective>
Split the oversized `backend/internal/repository/member_claims_repository.go` (516 lines) into
three files under 450 lines each, following the SAME naming/splitting convention this file's
sibling files already use in this codebase (`member_claims_list_repository.go`,
`member_claims_activate_repository.go`, `member_claims_role_activation_repository.go` already
exist as prior splits of the same `MemberClaimsRepository` struct). This is a pure move, no
behavior change.

Purpose: bring the file under the 450-line cap (CLAUDE.md Modularity constraint) using the exact
precedent pattern already established in this package.
Output: `member_claims_repository.go` shrunk to ~345 lines, plus two new sibling files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
Existing sibling split files in the SAME package to match style/imports against:
- `backend/internal/repository/member_claims_list_repository.go` (holds `ClaimListRow`,
  `ClaimListFilter`, `ClampAdminListPage`, `ListClaims` — all on `*MemberClaimsRepository`)
- `backend/internal/repository/member_claims_activate_repository.go` (holds `ActivateClaimedMember`,
  `PreviewActivatableRoles`, `collectActivatableHistoricalRoles`)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Move SubmitClaim and the three read-query methods into two new sibling files</name>
  <files>backend/internal/repository/member_claims_repository.go, backend/internal/repository/member_claims_submit_repository.go, backend/internal/repository/member_claims_queries_repository.go</files>
  <read_first>backend/internal/repository/member_claims_repository.go, backend/internal/repository/member_claims_list_repository.go</read_first>
  <action>
    Read the full current `member_claims_repository.go` (516 lines, `package repository`). Confirm
    current function boundaries with `grep -n "^func " backend/internal/repository/member_claims_repository.go`
    (previously verified: `SubmitClaim` at line 164, `ListPendingClaimsForGroup` at 364, `GetMyClaim`
    at 393, `UpdateNoindex` at 424 — re-verify exact current line numbers before cutting).

    Move (cut, do not duplicate), all keeping `package repository` and receiver `(r *MemberClaimsRepository)`:
    - `member_claims_submit_repository.go` GETS: `SubmitClaim` only (~79 lines). Give it its own
      import block for whatever it needs (`context`, `fmt`, `errors`, `time`, `github.com/jackc/pgx/v5`
      as applicable — check actual usage, don't guess).
    - `member_claims_queries_repository.go` GETS: `ListPendingClaimsForGroup`, `GetMyClaim`,
      `UpdateNoindex` (~90 lines combined).

    KEEP in `member_claims_repository.go`: `ClaimMutationError`/`AsClaimMutationError` (used by
    `VerifyClaim`/`RejectClaim`, which stay), `NewMemberClaimsRepository`, `WithAuditLog`,
    `ListPendingClaimAttentionCandidates`, `SearchHistoricalMembers`, `VerifyClaim`, `RejectClaim`,
    `scanMemberClaim`, `scanMemberClaimWithMember`, `scanMemberClaims`, `nullableStringPtr`.
    Target ~345 lines remaining.

    Do not touch `member_claims_repository_test.go`, `member_claims_list_repository.go`,
    `member_claims_activate_repository.go`, or `member_claims_role_activation_repository.go` in
    this task — only the three files listed above.
  </action>
  <acceptance_criteria>
    - `wc -l backend/internal/repository/member_claims_repository.go` reports <= 450
    - `wc -l backend/internal/repository/member_claims_submit_repository.go backend/internal/repository/member_claims_queries_repository.go` each report <= 450
    - `grep -c "^func (r \*MemberClaimsRepository) SubmitClaim" backend/internal/repository/member_claims_submit_repository.go` equals 1, and 0 in `member_claims_repository.go`
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-backend go build ./...</automated>
  </verify>
  <done>Both new files exist, `member_claims_repository.go` under 450 lines, all moved methods behaviorally identical.</done>
</task>

<task type="auto">
  <name>Task 2: Build and run existing MemberClaimsRepository tests unchanged</name>
  <files>backend/internal/repository/member_claims_repository.go, backend/internal/repository/member_claims_submit_repository.go, backend/internal/repository/member_claims_queries_repository.go</files>
  <read_first>backend/internal/repository/member_claims_repository_test.go</read_first>
  <action>
    Run `docker compose exec team4sv30-backend gofmt -l ./internal/repository/` and
    `docker compose exec team4sv30-backend go vet ./internal/repository/...` to fix per-file import
    blocks (unused/missing imports after the split). Then `docker compose exec team4sv30-backend go build ./...`.
    Confirm `member_claims_repository_test.go` was not modified.
  </action>
  <acceptance_criteria>
    <automated>docker compose exec team4sv30-backend go build ./...</automated>
    <automated>docker compose exec team4sv30-backend go test ./internal/repository/... -run TestMemberClaimsRepository</automated>
    - `git diff --stat backend/internal/repository/member_claims_repository_test.go` shows zero changes
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec team4sv30-backend go build ./...</automated>
    <automated>docker compose exec team4sv30-backend go test ./internal/repository/... -run TestMemberClaimsRepository</automated>
  </verify>
  <done>Backend builds clean; all existing MemberClaimsRepository tests (SubmitClaim/VerifyClaim/RejectClaim/etc.) pass unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | Pure repository-layer file relocation, no new trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-02 | Tampering | member_claims_*_repository.go split | accept | Pure relocation matching an already-established package convention; existing test suite (unmodified) is the behavioral-parity proof. |
</threat_model>

<verification>
`docker compose exec team4sv30-backend go build ./...` succeeds and the existing
`member_claims_repository_test.go` suite passes with an identical pass/fail set to before this plan.
</verification>

<success_criteria>
All three files report <= 450 lines via `wc -l`; backend builds; existing claims tests unmodified and green.
</success_criteria>

<output>
Create `.planning/phases/143-phase-142-nacharbeit-und-dashboard-lane-f-r-abgelehnte-notiz/143-02-SUMMARY.md` when done
</output>
