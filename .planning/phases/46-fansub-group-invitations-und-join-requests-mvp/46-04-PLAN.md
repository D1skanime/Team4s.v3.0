---
phase: 46-fansub-group-invitations-und-join-requests-mvp
plan: "04"
type: execute
wave: 4
depends_on:
  - 46-03-PLAN.md
files_modified:
  - backend/internal/handlers
  - backend/internal/repository
  - backend/internal/permissions
  - frontend/src/app
  - frontend/src/lib
  - frontend/src/types
  - docs
autonomous: true
requirements:
  - FANSUB-INVITES-01
must_haves:
  truths:
    - "Invitation security needs explicit regression coverage for token, status, and permission edge cases."
    - "The final implementation write-up must begin with a short Ist-Analyse."
    - "Join requests should remain a narrow follow-up unless the MVP naturally proved otherwise."
---

<objective>
Close Phase 46 with regression coverage, concise developer documentation, live verification, and a narrow handoff to the next self-service slice.

Purpose: Invitation workflows affect permissions and membership creation, so they need explicit proof and operational guidance.
</objective>

<context>
@.planning/phases/46-fansub-group-invitations-und-join-requests-mvp/46-CONTEXT.md
@.planning/phases/46-fansub-group-invitations-und-join-requests-mvp/46-RESEARCH.md
@.planning/phases/46-fansub-group-invitations-und-join-requests-mvp/46-01-PLAN.md
@.planning/phases/46-fansub-group-invitations-und-join-requests-mvp/46-02-PLAN.md
@.planning/phases/46-fansub-group-invitations-und-join-requests-mvp/46-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the required backend invitation test matrix</name>
  <read_first>
    - backend/internal/handlers
    - backend/internal/repository
    - backend/internal/permissions
  </read_first>
  <action>
    Add backend tests for at least:
    - user without permission cannot view invitations
    - group manager can create invitation for own group
    - same actor cannot create/cancel invitations for another group
    - `designer` cannot create invitation
    - unknown role codes are rejected
    - at least one role code is required
    - invitation for already-active member is rejected
    - duplicate pending invitation is rejected
    - token is not stored in plaintext
    - expired invitation cannot be accepted
    - cancelled invitation cannot be accepted
    - accepted invitation cannot be accepted twice
    - valid invitation creates or activates membership with invited roles
    - email mismatch is rejected
    - conflicting or duplicate membership acceptance is handled cleanly
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./... && go build ./...</automated>
  </verify>
  <done>
    - The critical invitation positive and negative paths are covered.
    - Token and terminal-state rules have explicit regressions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add thin frontend regression coverage for invitation capabilities and acceptance UX</name>
  <read_first>
    - touched frontend invitation tests/helpers
  </read_first>
  <action>
    Add the thinnest useful frontend coverage for:
    - invitation capability gating
    - create invitation token display
    - cancel action availability
    - `401`, `403`, `404`, `409`, and `410` acceptance/error presentation when applicable
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck && npm run lint</automated>
  </verify>
  <done>
    - Invitation UI affordances have basic regression protection.
    - Acceptance and error UX is covered where practical.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write docs, verify live behavior, and prepare the next phase handoff</name>
  <read_first>
    - touched backend/frontend invitation files
    - docs/architecture/db-schema-fansub-domain.md
  </read_first>
  <action>
    Add concise developer documentation that explains:
    - invitation token model and hash-only storage
    - Keycloak identity vs Team4s invitation/member logic
    - invitation permissions and capability mapping
    - acceptance flow into Phase-45 membership state
    - what is intentionally deferred for join requests
    - why Join Requests remain Phase-47 material unless the public/user context is already solid

    Then complete a live verification checklist that proves:
    - own-group invitation creation/list/cancel
    - cross-group denial
    - valid accept flow
    - invalid/expired/used token behavior
    - audit output presence
    - duplicate pending invitation prevention
    - already-active member invite rejection

    The final implementation write-up for the phase must start with a short Ist-Analyse section that summarizes:
    - relevant DB tables found
    - relevant columns found
    - auth/user middleware found
    - permission/member seams found
    - existing API/frontend conventions found
    - which structures were reused
    - whether new migrations were needed or not

    Close with a narrow next-phase suggestion, preferably around richer join requests, resend/reminder flows, or acceptance UX polish.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./... && go build ./...</automated>
    <automated>cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck && npm run lint</automated>
    <automated>cd C:/Users/admin/Documents/Team4s && git diff --check</automated>
  </verify>
  <done>
    - Docs explain the invitation MVP and its boundaries clearly.
    - Live verification proves both operator and invitee paths.
    - The final closeout starts with the requested Ist-Analyse and ends with a clean next-phase handoff.
  </done>
</task>

</tasks>

<verification>
Automated:
1. `cd C:/Users/admin/Documents/Team4s/backend && go test ./...`
2. `cd C:/Users/admin/Documents/Team4s/backend && go build ./...`
3. `cd C:/Users/admin/Documents/Team4s/frontend && npm run typecheck`
4. `cd C:/Users/admin/Documents/Team4s/frontend && npm run lint`
5. `cd C:/Users/admin/Documents/Team4s && git diff --check`

Live:
1. Verify a same-group manager can create, list, and cancel invitations.
2. Verify a cross-group or under-privileged user gets capability-false UI and 403 API behavior.
3. Verify a valid invitation can be accepted by an authenticated app user.
4. Verify invalid, expired, cancelled, or already used tokens fail cleanly.
5. Verify audit entries exist for the required invitation events.
</verification>

<success_criteria>
- Backend and frontend regressions cover the invitation MVP's critical paths.
- Developer docs explain token handling, permission mapping, and membership acceptance clearly.
- The phase ends with a clean handoff to the next narrow self-service slice.
</success_criteria>
