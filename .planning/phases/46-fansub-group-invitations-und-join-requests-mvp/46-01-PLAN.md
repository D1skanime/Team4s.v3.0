---
phase: 46-fansub-group-invitations-und-join-requests-mvp
plan: "01"
type: execute
wave: 1
depends_on:
  - ../45-fansub-member-management-mvp/45-04-PLAN.md
files_modified:
  - backend/cmd/server
  - backend/internal/handlers
  - backend/internal/middleware
  - backend/internal/models
  - backend/internal/permissions
  - backend/internal/repository
  - database/migrations
  - docs
autonomous: true
requirements:
  - FANSUB-INVITES-01
must_haves:
  truths:
    - "Phase 46 builds on the app-user membership model from Phase 45 and the permission engine from Phase 44."
    - "Invitation tokens must never be stored in plaintext."
    - "If a suitable invitation or membership seam already exists in the executing branch, it must be reused instead of replaced."
---

<objective>
Do the mandatory pre-analysis and establish the minimal canonical invitation foundation: validate the app-user/member-management seam, confirm whether invitation/join-request structures already exist, and introduce only the smallest schema and repository additions truly needed for the invitation MVP.

Purpose: Phase 46 should not create a second membership or audit system, and it should not guess at token or status conventions.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/46-fansub-group-invitations-und-join-requests-mvp/46-CONTEXT.md
@.planning/phases/46-fansub-group-invitations-und-join-requests-mvp/46-RESEARCH.md
@.planning/phases/45-fansub-member-management-mvp/45-CONTEXT.md
@backend/internal/handlers/fansub_group_members.go
@backend/internal/repository/fansub_repository.go
@frontend/src/app/admin/fansubs/[id]/members/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyse existing invite, membership, audit, and token conventions first</name>
  <read_first>
    - database/migrations
    - backend/internal/permissions
    - backend/internal/repository
    - frontend/src/lib/api.ts
    - frontend/src/app/admin/fansubs/[id]/members/page.tsx
  </read_first>
  <action>
    Perform the required Ist-Analyse and document:
    - whether invitation or join-request tables already exist
    - how `app_users`, `fansub_groups`, `fansub_group_members`, and `fansub_group_member_roles` are actually modeled
    - how statuses, soft-delete fields, `created_by`/`updated_at`, and audit are modeled
    - whether there are existing token/hash/expiry conventions in the project
    - how Phase-45 member-management and Phase-44 capabilities are exposed in runtime code
    - whether there is already a project convention for returning one-time tokens/links in create responses

    Then decide:
    - which structures can be reused directly
    - whether a new `fansub_group_invitations` table is truly needed
    - whether join requests remain preparatory only

    If the execution branch still lacks the app-user membership seam or the permission engine, stop with a blocker.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s && git diff --check</automated>
  </verify>
  <done>
    - There is a documented Ist-Analyse baseline for invitations.
    - The canonical membership and acceptance target seam is explicit.
    - Any required migration is justified by visible gaps, not assumptions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Establish the minimal invitation data model and token policy</name>
  <read_first>
    - results of Task 1
    - backend/internal/models
    - backend/internal/repository
  </read_first>
  <action>
    Reuse an existing invitation structure if present. Otherwise add the smallest schema needed for:
    - one invitation row per invite
    - normalized email
    - invited role list
    - hashed token
    - status and expiry
    - accepted/cancelled actor and timestamps
    - audit-friendly created/updated timestamps
    - uniqueness or repository-level conflict protection for one active pending invitation per group + normalized email

    Also define the token policy:
    - cryptographically secure generation
    - hash-only persistence
    - raw token returned once at creation time
    - expiry handling rules
    - no token or hash leakage through list/read endpoints
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/models/... ./internal/repository/...</automated>
  </verify>
  <done>
    - The canonical invitation model is explicit and minimal.
    - No plaintext token storage remains possible in the chosen shape.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add repository/service seams for invitation lifecycle and membership acceptance handoff</name>
  <read_first>
    - backend/internal/repository
    - backend/internal/permissions
  </read_first>
  <action>
    Introduce or extend repository/service helpers for:
    - list invitations by group
    - create invitation and return the raw token once
    - cancel invitation
    - resolve invitation by token
    - accept invitation by routing into the canonical Phase-45 membership creation/activation path
    - lazily recognize expired invitations when read or accepted
    - reject unknown role codes and empty role lists during invitation creation
    - reject invitations for already-active group members
    - reject duplicate pending invitations for the same group + normalized email

    Keep acceptance logic centralized so handlers do not each rebuild status, expiry, and membership-conflict rules.
  </action>
  <verify>
    <automated>cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/repository/... ./internal/permissions/...</automated>
  </verify>
  <done>
    - Repository/service seams exist for the complete invitation lifecycle.
    - Acceptance has a clear handoff into the member-management model.
  </done>
</task>

</tasks>

<verification>
Automated:
1. `cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/repository/...`
2. `cd C:/Users/admin/Documents/Team4s/backend && go test ./internal/permissions/...`
3. `cd C:/Users/admin/Documents/Team4s && git diff --check`

Targeted:
1. Verify the executing branch exposes the membership and permission seams Phase 46 depends on.
2. Verify any new invitation table is minimal and justified.
3. Verify there is no plaintext token persistence path.
</verification>

<success_criteria>
- Phase 46 begins with a documented invitation-focused Ist-Analyse.
- The invitation model is minimal, explicit, and hash-only for tokens.
- Acceptance is designed to reuse Phase-45 membership seams.
</success_criteria>
