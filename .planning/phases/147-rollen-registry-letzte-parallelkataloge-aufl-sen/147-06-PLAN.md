---
phase: 147-rollen-registry-letzte-parallelkataloge-aufl-sen
plan: 06
type: execute
wave: 3
depends_on: ["147-01", "147-02", "147-03", "147-04", "147-05"]
files_modified: []
autonomous: false
requirements: [HC-01, HC-02, HC-03, HC-09]
must_haves:
  truths:
    - "Backend go build/vet/test, frontend lint/typecheck/test/build, and the OpenAPI contract are all green after all four findings are closed"
    - "A live UAT on :3000 confirms public note rendering (release-detail and project-member notes) is visually unchanged despite the role_code plumbing change"
  artifacts: []
  key_links: []
---

<objective>
Prove the whole phase together: run every automated gate across backend, frontend, and contracts
after HC-01/HC-02/HC-03/HC-09 have all landed, then get a live human sign-off that public note
rendering is unchanged — per ROADMAP.md Success Criterion 8.

Purpose: Plans 147-01 through 147-05 each verify their own slice in isolation. This plan is the
integration gate — it catches any cross-plan interaction (e.g. a frontend type mismatch against the
now-extended OpenAPI schema, or a stale build artifact) before the phase is considered complete, and
closes the loop with the one criterion no automated test can fully replace: does the live page still
look and behave the same to a real admin/visitor.
Output: a full green run of every affected test suite plus a signed-off live UAT confirming rendering
parity for both public note surfaces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/147-rollen-registry-letzte-parallelkataloge-aufl-sen/147-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated regression sweep across backend, frontend, and contracts</name>
  <files></files>
  <read_first>
    .planning/phases/147-rollen-registry-letzte-parallelkataloge-aufl-sen/147-01-PLAN.md
    .planning/phases/147-rollen-registry-letzte-parallelkataloge-aufl-sen/147-02-PLAN.md
    .planning/phases/147-rollen-registry-letzte-parallelkataloge-aufl-sen/147-03-PLAN.md
    .planning/phases/147-rollen-registry-letzte-parallelkataloge-aufl-sen/147-04-PLAN.md
    .planning/phases/147-rollen-registry-letzte-parallelkataloge-aufl-sen/147-05-PLAN.md
  </read_first>
  <action>
    Run, in order, and record pass/fail with output for each: (1) `docker exec team4sv30-backend go
    build ./...`; (2) `docker exec team4sv30-backend go vet ./...`; (3)
    `docker exec team4sv30-backend go test ./internal/repository/... ./internal/handlers/...
    ./internal/permissions/... ./internal/migrations/... ./internal/models/... -v` (include
    `TEAM4S_PHASE117_TEST_DSN` in the environment if a Phase-117 test database already exists on
    `team4sv30-db`, matching Plan 147-01's Task 3 setup — mirror how prior phases resolved this DSN
    via `docker exec team4sv30-db psql`, per the Phase-146 SUMMARY precedent, if the database does not
    yet exist); (4) `cd frontend && npx tsc --noEmit`; (5) `cd frontend && npx eslint .`; (6)
    `cd frontend && npx vitest run`; (7) `cd frontend && npm run build`; (8) `git diff --check` (no
    trailing-whitespace/conflict-marker regressions across the whole phase's diff). For any
    pre-existing failures unrelated to this phase's four findings (i.e. present on `main` before this
    phase's plans started), document them as pre-existing per the Phase-133/146 precedent of
    confirming via a baseline run — do not attempt to fix out-of-scope failures.
  </action>
  <verify>
    <automated>docker exec team4sv30-backend go build ./... && docker exec team4sv30-backend go vet ./... && cd frontend && npx tsc --noEmit && npx eslint . && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - All 8 commands in the action above either exit 0, or any non-zero exit is explicitly shown (via a baseline run without this phase's changes) to be a pre-existing failure unrelated to HC-01/HC-02/HC-03/HC-09
    - `grep -rn "roleColorCode\|ROLE_CODE_BY_LABEL" frontend/src` returns no matches (confirms Plan 147-02's deletion is complete repo-wide)
    - `grep -rn "RoleTranslator\|RoleTypesetter\|RoleTechadmin\|RoleGfxler" backend/internal --include="*.go" | grep -v "_test.go" | grep -v "/\* "` returns no matches outside the inert comment block (confirms Plan 147-05's removal is complete repo-wide)
  </acceptance_criteria>
  <done>Every automated gate for this phase's touched surfaces is green, or documented as a pre-existing unrelated failure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live UAT — public note rendering and admin role surfaces are unchanged</name>
  <files></files>
  <what-built>
    All four hardcoding-audit findings (HC-01, HC-02, HC-03, HC-09) are closed: public note cards now
    derive their role color from the stable `role_code` field instead of a broken label→code map; the
    admin group-members historical-role summary resolves labels through the shared catalog path
    instead of a second stale label map; the three global App-Rollen have one Go source
    (`models.AppGlobalRoles`) instead of four independent literal copies; and four unreferenced role
    constants were removed from `permissions.go`.
  </what-built>
  <action>
    Do not alter application code during this checkpoint. Use the live browser tunnel and real
    user-visible navigation only, exactly per `<how-to-verify>` below, and record the observed
    outcome of each of the 4 steps in this plan's SUMMARY.
  </action>
  <how-to-verify>
    1. Open `http://127.0.0.1:3300` (Windows SSH tunnel to the Linux frontend at
       `http://192.168.235.196:3000`) and navigate to a release-detail page with published public
       release notes (`/anime/{id}/group/{groupId}/releases/{releaseVersionId}`, "Stimmen aus dem
       Team" section). Confirm each note card still shows the correct role label text and the same
       visual header-band styling as before this phase (note: per CONTEXT.md's documented side
       finding, the `--role-accent-*` CSS tokens are already undefined repo-wide, so no visible color
       change is expected either before or after this phase — only the underlying `data-role-code`
       attribute value changes, which you can confirm via browser DevTools if desired).
    2. Navigate to a project-member public page with published text contributions and confirm its
       note cards render identically (role label text, layout, toggle behavior for long notes).
    3. Open `/admin/fansubs/{id}/edit` (Historische Mitglieder tab) and confirm the historical-member
       role summary still shows correct, readable German role labels for members with open historical
       roles (e.g. "Gründer/in", not a raw code or a wrong/stale label).
    4. Open `/admin/users/{id}` (Globale Rechte tab) and confirm the three global role toggles
       (Plattform-Admin, Content-Admin, Benutzer) still list, assign, and revoke correctly, and that
       attempting an invalid role via a raw API call still returns the German
       "Ungültige Rolle. Erlaubte Werte: platform_admin, content_admin, user." message.
  </how-to-verify>
  <verify>
    <automated>MISSING — no automated substitute exists for live visual/behavioral parity confirmation; this is the phase's designated manual checkpoint (Nyquist exception: human-check only)</automated>
  </verify>
  <resume-signal>Type "approved" if all four checks render/behave identically to before, or describe what regressed.</resume-signal>
  <done>User types "approved" after confirming all 4 steps render and behave identically to before this phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Full phase surface (public unauthenticated note reads, authenticated admin group/user management) | Aggregates the boundaries already assessed in Plans 147-01 through 147-05; no new boundary introduced by this integration/verification plan itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-147-12 | Repudiation / Tampering (regression-class) | Cross-plan integration surface (e.g. a frontend build consuming an OpenAPI contract that a backend plan changed) | mitigate | Task 1's full sweep (`tsc --noEmit`, `eslint`, `vitest run`, `npm run build`, full backend `go build`/`go vet`/`go test`, `git diff --check`) is the explicit gate that would catch any cross-plan contract drift before this phase is marked complete. |
| T-147-13 | Information Disclosure (aggregate re-check) | Combined effect of exposing `role_code` (Plan 147-01/02) — re-confirmed here at the integration level | accept | Re-affirms Plan 147-01's T-147-01 conclusion: `role_code` remains strictly less sensitive than the already-public `role_label` it sits beside on the same public, unauthenticated endpoints; no new field beyond what Plans 147-01/147-02 already assessed is introduced by this plan. |
| T-147-SC | Tampering | No new package-manager installs in this plan | n/a | No npm/pip/cargo install tasks exist in this plan; gate not applicable. |
</threat_model>

<verification>
- Task 1's full command sweep is green (or pre-existing failures are documented)
- Task 2's live UAT checkpoint is explicitly approved by the user
</verification>

<success_criteria>
- All backend, frontend, and contract gates pass (or documented pre-existing failures only).
- A human confirms live rendering parity for both public note surfaces and the two admin surfaces this phase touched.
</success_criteria>

<output>
Create `.planning/phases/147-rollen-registry-letzte-parallelkataloge-aufl-sen/147-06-SUMMARY.md` when done
</output>
