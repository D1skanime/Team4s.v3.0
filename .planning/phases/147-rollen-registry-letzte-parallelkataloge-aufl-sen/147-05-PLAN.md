---
phase: 147-rollen-registry-letzte-parallelkataloge-aufl-sen
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/permissions/permissions.go
  - backend/internal/permissions/permissions_test.go
  - backend/internal/permissions/effective_rights_test.go
  - backend/internal/permissions/effective_rights_capability_impact_preview_test.go
  - backend/internal/permissions/capability_registry_test.go
autonomous: true
requirements: [HC-09]
must_haves:
  truths:
    - "RoleTranslator, RoleTypesetter, RoleTechadmin and RoleGfxler no longer exist as Go constants in permissions.go"
    - "The four package-internal test fixtures that referenced these constants now use the equivalent raw string literals and still pass"
    - "The remaining role-constant block carries a comment clarifying it is not an authoritative role list, that role_definitions is the catalog, and that it holds only codes directly referenced in Go comparisons"
    - "All other constants in the same block (RolePlatformAdmin, RoleFansubLead, RoleProjectLead, RoleTimer, RoleEditor, RoleEncoder, RoleRawProvider, RoleQualityChecker, RoleDesigner) remain unchanged"
  artifacts:
    - path: backend/internal/permissions/permissions.go
      provides: "role-constant block with the 4 unreferenced constants removed and a clarifying comment"
  key_links: []
---

<objective>
Remove the four Go role constants (`RoleTranslator`, `RoleTypesetter`, `RoleTechadmin`,
`RoleGfxler`) that have zero production references and are only used, unqualified, inside four
package-internal test fixtures — per CONTEXT.md's HC-09 decision (a correction of the original
audit, which missed these package-internal test usages) and ROADMAP.md Success Criterion 7.

Purpose: these four constants exist purely to feed test fixtures; the only place they appear in
production code is a fully commented-out `roleMatrix` block (`/* ... */`, lines 103+) that does not
compile. Removing them and switching the four test files to raw string literals eliminates dead
production surface without introducing a new Go role list — `role_definitions` remains the sole
catalog.
Output: `permissions.go`'s role-constant block with the 4 dead constants removed and a clarifying
comment; the 4 affected test files converted to literals; all other constants untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/147-rollen-registry-letzte-parallelkataloge-aufl-sen/147-CONTEXT.md
@.planning/phases/147-rollen-registry-letzte-parallelkataloge-aufl-sen/147-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the four unreferenced constants and clarify the remaining block</name>
  <files>backend/internal/permissions/permissions.go</files>
  <read_first>backend/internal/permissions/permissions.go</read_first>
  <action>
    In the first `const (...)` block (lines 65-77), delete the two lines
    `RoleTranslator     = "translator"` and `RoleTypesetter     = "typesetter"`, leaving
    `RolePlatformAdmin`, `RoleFansubLead`, `RoleProjectLead`, `RoleTimer`, `RoleEditor`,
    `RoleEncoder`, `RoleRawProvider`, `RoleQualityChecker`, `RoleDesigner` exactly as they are today
    (values and alignment unchanged, gofmt will realign padding). Delete the entire second
    `const (...)` block (lines 79-83: the comment `// Neue Gruppenrollen (D-07): in Migration 0112
    mit assignable=true angelegt.` plus `RoleTechadmin = "techadmin"` and `RoleGfxler = "gfxler"`) —
    both its members and its comment are removed together since the comment described exactly these
    two constants.

    Add a new comment directly above the first (now sole) `const (...)` block stating: this is not
    an authoritative role list; the canonical catalog is the `role_definitions` table; this block
    holds only codes that are directly referenced in Go comparisons (e.g.
    `result.MatchedRole == permissions.RoleFansubLead`).

    Do NOT touch the inert `/* Historical bootstrap grants retained as documentation only. ... */`
    block (starting around line 103) — it is fully commented out, does not compile, and its
    remaining textual references to `RoleTranslator`/`RoleTypesetter` inside the comment are
    explicitly out of scope per CONTEXT.md (dead text, not a compiled reference).
  </action>
  <verify>
    <automated>docker exec team4sv30-backend go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RoleTranslator\|RoleTypesetter\|RoleTechadmin\|RoleGfxler" backend/internal/permissions/permissions.go` reports at most the count of occurrences inside the `/* ... */` inert block only (verify by inspection that any remaining hits are inside that comment, not in a compiled `const` declaration)
    - `grep -q "RolePlatformAdmin  = \"platform_admin\"" backend/internal/permissions/permissions.go` (untouched constants still present)
    - `docker exec team4sv30-backend go build ./...` exits 0 (fails at this point if Task 2's test-file literal conversion has not happened yet — run Task 1 and Task 2 together before verifying build)
  </acceptance_criteria>
  <done>The two dead constants and the two-member dead-Gruppenrollen block are removed; the remaining block carries a clarifying non-authoritative comment; all other constants are byte-identical to before.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert the four package-internal test fixtures to string literals</name>
  <files>backend/internal/permissions/permissions_test.go, backend/internal/permissions/effective_rights_test.go, backend/internal/permissions/effective_rights_capability_impact_preview_test.go, backend/internal/permissions/capability_registry_test.go</files>
  <read_first>
    backend/internal/permissions/permissions_test.go
    backend/internal/permissions/effective_rights_test.go
    backend/internal/permissions/effective_rights_capability_impact_preview_test.go
    backend/internal/permissions/capability_registry_test.go
  </read_first>
  <behavior>
    - Every test that previously referenced `RoleTranslator`, `RoleTypesetter`, `RoleTechadmin`, or
      `RoleGfxler` continues to exercise the exact same role-string values (`"translator"`,
      `"typesetter"`, `"techadmin"`, `"gfxler"`) and passes unchanged in outcome.
  </behavior>
  <action>
    In `permissions_test.go` at line 466 (`contributionRolesByUser: map[int64][]string{2:
    {RoleTranslator}}`), replace `RoleTranslator` with the raw string literal `"translator"`.

    In `effective_rights_test.go` at line 325 (`roles: []string{RoleTranslator, RoleFansubLead}`),
    replace `RoleTranslator` with `"translator"`; leave `RoleFansubLead` untouched (its constant
    still exists).

    In `effective_rights_capability_impact_preview_test.go` at line 183 (`{appUserID: 102,
    fansubGroupID: 6}: {roles: []string{RoleTranslator}, activeMembership: true}`), replace
    `RoleTranslator` with `"translator"`.

    In `capability_registry_test.go`: replace the map keys `RoleTranslator:` (line 186),
    `RoleTypesetter:` (line 197), `RoleGfxler:` (line 227), and `RoleTechadmin:` (line 233) with
    `"translator":`, `"typesetter":`, `"gfxler":`, `"techadmin":` respectively — mirror the
    raw-string-literal map-key style the adjacent `"founder":` key (immediately following
    `RoleGfxler`/`RoleTechadmin` in the same map literal) already uses. Optionally correct the
    stale comment immediately above the `RoleGfxler`/`RoleTechadmin` entries ("gfxler/techadmin sind
    Go-Konstanten...") to reflect that these are now raw literals — not a required success
    criterion, local consistency only.
  </action>
  <verify>
    <automated>docker exec team4sv30-backend go build ./... && docker exec team4sv30-backend go test ./internal/permissions/... -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "RoleTranslator\|RoleTypesetter\|RoleTechadmin\|RoleGfxler" backend/internal/permissions/permissions_test.go backend/internal/permissions/effective_rights_test.go backend/internal/permissions/effective_rights_capability_impact_preview_test.go backend/internal/permissions/capability_registry_test.go` returns no match in any of the four files
    - `docker exec team4sv30-backend go build ./...` exits 0
    - `docker exec team4sv30-backend go test ./internal/permissions/... -v` passes with 0 failures
  </acceptance_criteria>
  <done>All four test files use raw string literals instead of the removed constants; the full permissions package test suite passes unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed — pure dead-code removal in a non-runtime-reachable constant block plus test-fixture literal substitution | N/A |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-147-10 | Elevation of Privilege | Removing `RoleTechadmin`/`RoleGfxler`/`RoleTranslator`/`RoleTypesetter` could theoretically break a hidden production comparison if the audit's "zero production references" claim were wrong | mitigate | `docker exec team4sv30-backend go build ./...` is a compile-time proof: if any production file still referenced these identifiers, the build would fail immediately. CONTEXT.md's verification (grep-confirmed: the only non-test production occurrence is inside the fully commented-out `/* ... */` `roleMatrix` block, which does not compile) is re-validated by this plan's own build-gate acceptance criterion. |
| T-147-11 | Tampering | Test fixtures switching from named constants to raw string literals could silently typo a role value (e.g. `"techadmn"`) | mitigate | `docker exec team4sv30-backend go test ./internal/permissions/... -v` must pass with 0 failures after the substitution — any typo changes the map key/slice value and breaks the specific test case that exercises that role, since these tests assert on role-keyed capability sets. |
| T-147-SC | Tampering | No new package-manager installs in this plan | n/a | No npm/pip/cargo install tasks exist in this plan; gate not applicable. |
</threat_model>

<verification>
- `docker exec team4sv30-backend go build ./...` succeeds
- `docker exec team4sv30-backend go vet ./internal/permissions/...` succeeds
- `docker exec team4sv30-backend go test ./internal/permissions/... -v` passes
</verification>

<success_criteria>
- The four unreferenced constants are removed from `permissions.go` with a clarifying comment left behind.
- All other constants in the same block are untouched.
- The four affected test files compile and pass using raw string literals.
</success_criteria>

<output>
Create `.planning/phases/147-rollen-registry-letzte-parallelkataloge-aufl-sen/147-05-SUMMARY.md` when done
</output>
