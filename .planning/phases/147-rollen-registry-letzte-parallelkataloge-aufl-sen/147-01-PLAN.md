---
phase: 147-rollen-registry-letzte-parallelkataloge-aufl-sen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/repository/release_detail_public_repository.go
  - backend/internal/repository/release_detail_public_repository_helpers.go
  - backend/internal/repository/project_member_public_repository.go
  - shared/contracts/openapi.yaml
  - backend/internal/testsupport/phase117_postgres.go
  - backend/internal/repository/public_note_role_code_integration_test.go
autonomous: true
requirements: [HC-01]
must_haves:
  truths:
    - "role_code is selected and scanned alongside role_label at all three note query sites (release cursor query, release loadNotes, project-member ListNotes)"
    - "shared/contracts/openapi.yaml documents role_code as a non-required string property on both PublicReleaseNote and ProjectMemberNote"
    - "A real-Postgres repository test proves RoleCode on actual query results for both note types, and proves changing label_de does not change RoleCode"
  artifacts:
    - path: backend/internal/repository/release_detail_public_repository.go
      provides: "PublicReleaseNote.RoleCode field + role_code SELECT/Scan in ListReleaseVersionNotesCursor"
    - path: backend/internal/repository/release_detail_public_repository_helpers.go
      provides: "role_code SELECT/Scan in loadNotes (2nd PublicReleaseNote query site, used by GetPublicReleaseDetail)"
    - path: backend/internal/repository/project_member_public_repository.go
      provides: "ProjectMemberNote.RoleCode field + role_code SELECT/Scan in ListNotes"
    - path: backend/internal/repository/public_note_role_code_integration_test.go
      provides: "real-DB proof of RoleCode for both note types via testsupport.OpenPhase117Postgres"
  key_links:
    - from: backend/internal/repository/release_detail_public_repository.go
      to: role_definitions
      via: "LEFT JOIN role_definitions rd ON rd.code = cr.name"
      pattern: "COALESCE\\(rd\\.code"
    - from: backend/internal/repository/project_member_public_repository.go
      to: role_definitions
      via: "LEFT JOIN role_definitions rd ON rd.code = cr.name"
      pattern: "COALESCE\\(rd\\.code"
---

<objective>
Thread the stable `role_code` (already joined via `role_definitions rd ON rd.code = cr.name` at all
three note query sites) from Postgres into the two public note DTOs and the OpenAPI contract, and
prove it with a real-Postgres test — per CONTEXT.md's HC-01 decision and ROADMAP.md Success
Criterion 2.

Purpose: `PublicNoteCard`'s role color currently derives from a broken label→code reverse map
(`roleColors.ts`, HC-01). The fix requires the stable `role_code` to actually reach the frontend;
today only `role_label` is selected even though the join already exists at every site.
Output: `PublicReleaseNote.RoleCode` / `ProjectMemberNote.RoleCode` populated from real queries,
`shared/contracts/openapi.yaml` documenting `role_code`, and a passing real-Postgres regression test
that survives a `label_de` change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/147-rollen-registry-letzte-parallelkataloge-aufl-sen/147-CONTEXT.md
@.planning/phases/147-rollen-registry-letzte-parallelkataloge-aufl-sen/147-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add role_code to both PublicReleaseNote query sites and ProjectMemberNote</name>
  <files>backend/internal/repository/release_detail_public_repository.go, backend/internal/repository/release_detail_public_repository_helpers.go, backend/internal/repository/project_member_public_repository.go</files>
  <read_first>
    backend/internal/repository/release_detail_public_repository.go
    backend/internal/repository/release_detail_public_repository_helpers.go
    backend/internal/repository/project_member_public_repository.go
  </read_first>
  <behavior>
    - `PublicReleaseNote.RoleCode` is populated with the `role_definitions.code` value for a note
      whose `role_id` resolves through `contributor_roles.name` to a real `role_definitions` row.
    - `ProjectMemberNote.RoleCode` behaves identically for the project-member note path.
    - A note whose `role_id` has no matching `role_definitions` row (or is NULL) yields
      `RoleCode == ""` (mirrors `RoleLabel`'s existing `COALESCE(..., '')` empty-string behavior,
      not NULL).
  </behavior>
  <action>
    In `backend/internal/repository/release_detail_public_repository.go`: add
    `RoleCode string \`json:"role_code"\`` to the `PublicReleaseNote` struct (lines 57-67)
    immediately after the `RoleLabel string \`json:"role_label"\`` field. In the
    `ListReleaseVersionNotesCursor` cursor query (the `SELECT ... FROM release_version_notes rvn`
    block around line 462), add `COALESCE(rd.code, '') AS role_code,` on its own line directly after
    `COALESCE(rd.label_de, '') AS role_label,`. In the corresponding `Scan(...)` call (around line
    489), add `&item.RoleCode` directly after `&item.RoleLabel`. The existing
    `LEFT JOIN role_definitions rd ON rd.code = cr.name` (line 470) already provides `rd` — do not
    add a second join.

    In `backend/internal/repository/release_detail_public_repository_helpers.go`: apply the
    identical addition to the second, independent query implementation for the same
    `PublicReleaseNote` struct inside `loadNotes` (around lines 400-423) — `COALESCE(rd.code, '') AS
    role_code,` after the `role_label` SELECT line, and `&item.RoleCode` after `&item.RoleLabel` in
    its `Scan(...)` call. This second site is used by `GetPublicReleaseDetail`'s full aggregate read
    and must change together with the cursor query in the other file — missing it is the most likely
    regression per CONTEXT.md.

    In `backend/internal/repository/project_member_public_repository.go`: add
    `RoleCode string \`json:"role_code"\`` to the `ProjectMemberNote` struct (lines 44-54)
    immediately after `RoleLabel`. In `ListNotes`'s query (around lines 247-257), add
    `COALESCE(rd.code, '') AS role_code,` after the `COALESCE(rd.label_de, '') AS role_label,` line.
    In the `Scan(...)` call (around line 270), add `&n.RoleCode` after `&n.RoleLabel`. Only one query
    site exists here (unlike the release-note path).
  </action>
  <verify>
    <automated>docker exec team4sv30-backend go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RoleCode" backend/internal/repository/release_detail_public_repository.go` reports at least 3 (struct field, SELECT-adjacent COALESCE, Scan arg)
    - `grep -c "RoleCode" backend/internal/repository/release_detail_public_repository_helpers.go` reports at least 2 (SELECT-adjacent COALESCE, Scan arg)
    - `grep -c "RoleCode" backend/internal/repository/project_member_public_repository.go` reports at least 3
    - `docker exec team4sv30-backend go build ./...` exits 0
  </acceptance_criteria>
  <done>Both PublicReleaseNote query sites and the ProjectMemberNote query site select, scan, and expose role_code; the backend builds cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Document role_code in the OpenAPI contract</name>
  <files>shared/contracts/openapi.yaml</files>
  <read_first>shared/contracts/openapi.yaml</read_first>
  <action>
    In the `PublicReleaseNote` schema (around line 14894-14919), add a new property
    `role_code: {type: string}` (matching the file's existing multi-line block style for this
    schema — mirror the `role_label:` / `type: string` two-line pattern) directly after the existing
    `role_label:` property. Do NOT add `role_code` to the schema's `required:` array (line 14896) —
    notes without a resolvable role yield an empty string, same non-required treatment as
    `role_label`.

    In the `ProjectMemberNote` schema (around line 15152-15164), add `role_code: {type: string}`
    in the same single-line compact style this schema already uses (mirror
    `role_label: {type: string}`), directly after the `role_label:` property. Do NOT add it to this
    schema's `required:` array either.
  </action>
  <verify>
    <automated>test $(grep -c "role_code" shared/contracts/openapi.yaml) -ge 2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A2 "PublicReleaseNote:" shared/contracts/openapi.yaml | grep -q "role_label"` succeeds AND a `role_code` property line exists within the same schema block
    - `grep -n "role_code" shared/contracts/openapi.yaml | wc -l` reports at least 2 (one per schema)
    - `required:` lines for both schemas remain unchanged (no `role_code` token inside either `required: [...]` array)
  </acceptance_criteria>
  <done>Both schemas document role_code as an optional string property, matching each schema's existing formatting style.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Real-Postgres proof that role_code is stable and label_de-independent</name>
  <files>backend/internal/testsupport/phase117_postgres.go, backend/internal/repository/public_note_role_code_integration_test.go</files>
  <read_first>
    backend/internal/testsupport/phase117_postgres.go
    backend/internal/repository/release_detail_public_segments_integration_test.go
  </read_first>
  <behavior>
    - Given a `release_version_notes` row whose `role_id` resolves to `role_definitions.code =
      'phase147_test_role'`, both `ListReleaseVersionNotesCursor` (release_detail_public_repository.go)
      and the unexported `loadNotes` (release_detail_public_repository_helpers.go) return a
      `PublicReleaseNote` with `RoleCode == "phase147_test_role"`.
    - Given the analogous fixture threaded through a project-member note,
      `ProjectMemberPublicRepository.ListNotes` returns a `ProjectMemberNote` with
      `RoleCode == "phase147_test_role"`.
    - After `UPDATE role_definitions SET label_de = 'Testrolle Zwei' WHERE code =
      'phase147_test_role'`, re-running the same three calls yields `RoleLabel == "Testrolle Zwei"`
      but `RoleCode` UNCHANGED at `"phase147_test_role"` — proves role_code is derived from the
      stable code column, not recomputed from the label.
  </behavior>
  <action>
    Extend `createPhase117Prerequisites` in `backend/internal/testsupport/phase117_postgres.go`:
    add a `name TEXT` column to the existing `fansub_groups` table definition, add a
    `release_date TIMESTAMPTZ NULL` column to the existing `release_versions` and `fansub_releases`
    table definitions (needed by `loadReleaseHeader`'s `publicReleaseTitleSQL`/`COALESCE(rv.release_date,
    fr.release_date)`), and append four new tables after the existing `theme_segments` table: `members
    (id BIGINT PRIMARY KEY, nickname TEXT, display_name TEXT, avatar_media_id BIGINT REFERENCES
    media_assets(id))`; `contributor_roles (id BIGINT PRIMARY KEY, name VARCHAR(80) NOT NULL)`;
    `role_definitions (code TEXT PRIMARY KEY, label_de TEXT NOT NULL, contexts TEXT[] NOT NULL DEFAULT
    '{}', sort_order INT NOT NULL DEFAULT 0)`; `release_version_notes (id BIGINT PRIMARY KEY,
    release_version_id BIGINT NOT NULL REFERENCES release_versions(id), fansub_group_id BIGINT
    REFERENCES fansub_groups(id), member_id BIGINT NOT NULL REFERENCES members(id), role_id BIGINT
    REFERENCES contributor_roles(id), title TEXT, body_html TEXT NOT NULL DEFAULT '', body_text TEXT
    NOT NULL DEFAULT '', visibility VARCHAR(20) NOT NULL DEFAULT 'internal', status VARCHAR(20) NOT
    NULL DEFAULT 'draft', sort_order INT NOT NULL DEFAULT 0, created_at TIMESTAMPTZ NOT NULL DEFAULT
    NOW(), deleted_at TIMESTAMPTZ NULL)`. This is purely additive to the shared Phase-117 stub schema
    (existing `TestReleaseDetailPublicSegments` does not reference the new tables/columns).

    Create `backend/internal/repository/public_note_role_code_integration_test.go` (package
    `repository`, same package as the analog `release_detail_public_segments_integration_test.go` so
    the unexported `loadNotes` is reachable), function `TestPublicNoteRoleCode`. Call
    `testsupport.OpenPhase117Postgres(t)`. Insert one row each into `anime`, `episodes` (with
    `anime_id`, `episode_number`), `fansub_groups` (with `name`), `fansub_releases` (`episode_id`),
    `release_versions` (`release_id`, `version`), `release_version_groups`
    (`release_version_id`+`fansub_group_id`), `members`, `contributor_roles` (`name =
    'phase147_test_role'`), `role_definitions` (`code = 'phase147_test_role'`, `label_de = 'Testrolle
    Eins'`), and `release_version_notes` (`visibility = 'public'`, `status = 'published'`,
    `role_id` = the inserted `contributor_roles.id`). Construct `repo :=
    NewReleaseDetailPublicRepository(pool, "")` and `pmRepo := NewProjectMemberPublicRepository(pool)`
    (verify the exact constructor name/signature for `ProjectMemberPublicRepository` by reading
    `project_member_public_repository.go` before writing the call). Also insert a second
    `release_version_notes` row (or reuse the same member/role) so `ListNotes`'s
    member/anime/group scoping matches. In `t.Run` sub-tests: (1) call `repo.ListReleaseVersionNotesCursor`,
    `repo.loadNotes` and `pmRepo.ListNotes`, assert `RoleCode == "phase147_test_role"` on each
    result via `require.Equal`; (2) `UPDATE role_definitions SET label_de = 'Testrolle Zwei' WHERE
    code = 'phase147_test_role'`, re-call all three, assert `RoleLabel == "Testrolle Zwei"` AND
    `RoleCode` still `== "phase147_test_role"`.
  </action>
  <verify>
    <automated>docker exec team4sv30-backend go test ./internal/repository/... -run "TestPublicNoteRoleCode|TestReleaseDetailPublicSegments" -v</automated>
  </verify>
  <acceptance_criteria>
    - `TEAM4S_PHASE117_TEST_DSN=&lt;dsn&gt; docker exec team4sv30-backend go test ./internal/repository/... -run TestPublicNoteRoleCode -v` passes with 0 failures (skips cleanly if the env var is unset, matching `OpenPhase117Postgres`'s existing fail-closed/skip contract)
    - Test file contains no `os.ReadFile`/`strings.Contains` assertion against its own package's `.go` source (verified by inspection — this is the CLAUDE.md Teststil real-call requirement)
    - `TestReleaseDetailPublicSegments` (the existing Phase-117 analog test) still passes after the stub-schema extension, proving the addition is non-breaking
  </acceptance_criteria>
  <done>A real-Postgres test proves role_code on actual query results for both note types and proves it survives a label_de change, without any source-substring assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| anonymous client -> `/anime/{id}/group/{groupId}/releases/{id}` and project-member public endpoints | Unauthenticated public read; response body is fully attacker-visible |
| repository -> Postgres | Trusted internal boundary, parameterized queries only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-147-01 | Information Disclosure | `PublicReleaseNote.RoleCode` / `ProjectMemberNote.RoleCode` newly exposed on two already-public, unauthenticated endpoints | accept | `role_code` (e.g. `translator`, `fansub_lead`) is strictly less sensitive than the already-public `role_label` (e.g. "Übersetzung", "Fansub-Leitung") it sits beside — both describe the same publicly-known contribution role for a publicly-visible note; `role_code` is a stable machine identifier for a fact (which role contributed this note) that is already disclosed in prose form. No new fact is disclosed, no internal/private role catalog entries are exposed (the join is scoped to `role_definitions` rows that already resolve via the existing public `contributor_roles`/`role_definitions` join), and no PII, ownership ID, or internal status accompanies it. Verified: the two touched schemas (`PublicReleaseNote`, `ProjectMemberNote`) carry no other new fields in this plan. |
| T-147-02 | Tampering | SQL query text (`COALESCE(rd.code, '')` addition) | mitigate | Addition is a static SELECT-list literal with no new user input; existing parameterized `$1`/`$2`/`$3` placeholders for `releaseVersionID`/`animeID`/`groupID` are unchanged — no new injection surface. |
| T-147-SC | Tampering | No new package-manager installs in this plan | n/a | No npm/pip/cargo install tasks exist in this plan; gate not applicable. |

Note on the phase-wide HC-03 authorization-adjacent regression risk (global-role derivation): not
applicable to this plan — Plan 147-04 owns `models.AppGlobalRoles` and its four consumers; this
plan touches only public, read-only note projections with no role-assignment authority.
</threat_model>

<verification>
- `docker exec team4sv30-backend go build ./...` succeeds
- `docker exec team4sv30-backend go vet ./internal/repository/...` succeeds
- `TEAM4S_PHASE117_TEST_DSN=&lt;dsn&gt; docker exec team4sv30-backend go test ./internal/repository/... -run "TestPublicNoteRoleCode|TestReleaseDetailPublicSegments" -v` passes
- `grep -n "role_code" shared/contracts/openapi.yaml` shows exactly the two new properties added (no accidental duplicate insertions)
</verification>

<success_criteria>
- `PublicReleaseNote.RoleCode` and `ProjectMemberNote.RoleCode` are populated from real `role_definitions.code` values at all three query sites.
- `shared/contracts/openapi.yaml` documents `role_code` as an optional string on both schemas.
- A real-Postgres test proves the field on actual results and its independence from `label_de`.
</success_criteria>

<output>
Create `.planning/phases/147-rollen-registry-letzte-parallelkataloge-aufl-sen/147-01-SUMMARY.md` when done
</output>
