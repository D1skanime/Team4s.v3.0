---
phase: 147-rollen-registry-letzte-parallelkataloge-aufl-sen
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/admin/fansubs/[id]/edit/useGroupMembersTab.ts
  - frontend/src/app/admin/fansubs/[id]/edit/GroupMembersTab.tsx
  - frontend/src/app/admin/fansubs/[id]/edit/useGroupMembersTab.test.ts
autonomous: true
requirements: [HC-02]
must_haves:
  truths:
    - "ROLE_LABELS and roleLabelForCode are removed from useGroupMembersTab.ts"
    - "roleSummary resolves role labels via the catalog-driven labelForRole(historyRoleOptions, code) path, the same path GroupMembersTab.tsx already uses for GroupHistRoleDialog/GroupMembersHistTable/GroupMemberFormModals"
    - "A test proves label resolution flows through the real historicalIdentityOptions/roleSummary computation with a RoleDefinitionOption[] fixture, not a standalone removed function"
  artifacts:
    - path: frontend/src/app/admin/fansubs/[id]/edit/useGroupMembersTab.ts
      provides: "historyRoleOptions option field + labelForRole-driven roleSummary"
    - path: frontend/src/app/admin/fansubs/[id]/edit/GroupMembersTab.tsx
      provides: "historyRoleOptions state declared before the useGroupMembersTab(...) call and passed into it"
  key_links:
    - from: frontend/src/app/admin/fansubs/[id]/edit/useGroupMembersTab.ts
      to: frontend/src/lib/roleCatalog.ts
      via: "labelForRole(historyRoleOptions, role.role_code)"
      pattern: "labelForRole\\(historyRoleOptions"
---

<objective>
Remove the second parallel role-label registry (`ROLE_LABELS`/`roleLabelForCode`) from
`useGroupMembersTab.ts` and switch `roleSummary` onto the already-existing catalog-driven
`labelForRole(...)` path, per CONTEXT.md's HC-02 decision and ROADMAP.md Success Criterion 4.

Purpose: `GroupMembersTab.tsx` already resolves role labels via `labelForRole(historyRoleOptions,
code)` from `@/lib/roleCatalog` for three other UI surfaces in the same file. `useGroupMembersTab.ts`
still carries an independent, stale 20-entry label map with 5 codes that never existed in
`role_definitions` and no `karaoke_fx` entry — this plan converges the hook onto the one already-live
catalog path instead of introducing anything new.
Output: `useGroupMembersTab.ts` with `ROLE_LABELS`/`roleLabelForCode` deleted and
`historicalIdentityOptions` sourcing labels from `historyRoleOptions` via `labelForRole`;
`GroupMembersTab.tsx` reordered so that state exists before the hook call that now consumes it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/147-rollen-registry-letzte-parallelkataloge-aufl-sen/147-CONTEXT.md
@.planning/phases/147-rollen-registry-letzte-parallelkataloge-aufl-sen/147-PATTERNS.md

<interfaces>
From frontend/src/lib/roleCatalog.ts (existing, unchanged — the target pattern):
```typescript
export function getRole(rows: readonly RoleDefinitionOption[], code: string): RoleDefinitionOption | undefined
export function labelForRole(rows: readonly RoleDefinitionOption[], code: string): string
```
From frontend/src/types/admin-capability.ts (existing, unchanged):
```typescript
export interface RoleDefinitionOption {
  code: string;
  label_de: string;
  contexts?: string[];
  sort_order: number;
  assignable?: boolean;
  color_key?: string;
  icon_key?: string;
  operative_capability_count?: number;
  has_operative_capabilities?: boolean;
}
```
From frontend/src/types/fansub.ts (existing, unchanged — the shape `openRoles` items already have):
```typescript
export interface HistGroupMemberRole {
  id: number;
  fansub_group_member_id: number;
  member_display_name: string;
  role_code: string;
  role_label: string | null;
  started_date: string | null;
  ended_date: string | null;
  note: string | null;
  status: "historical" | "confirmed";
  created_at: string;
}
```
GroupMembersTab.tsx's existing calling convention to mirror (lines 170, 239):
```tsx
roleLabelForCode={(code) => labelForRole(historyRoleOptions, code)}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate useGroupMembersTab.ts onto labelForRole and thread historyRoleOptions from GroupMembersTab.tsx</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/useGroupMembersTab.ts, frontend/src/app/admin/fansubs/[id]/edit/GroupMembersTab.tsx</files>
  <read_first>
    frontend/src/app/admin/fansubs/[id]/edit/useGroupMembersTab.ts
    frontend/src/app/admin/fansubs/[id]/edit/GroupMembersTab.tsx
    frontend/src/lib/roleCatalog.ts
  </read_first>
  <behavior>
    - `historicalIdentityOptions`'s `roleSummary` field resolves each open role's label via
      `labelForRole(historyRoleOptions, role.role_code)` when `role.role_label` is falsy, using
      the `historyRoleOptions` passed into the hook (not a hardcoded map).
    - Passing an empty `historyRoleOptions` array and an unknown code falls back to
      `labelForRole`'s own `readableCodeLabel(code)` behavior (Title-Cased code), matching
      `labelForRole`'s existing contract — this is an intentional behavior change from the removed
      `roleLabelForCode`'s raw-code fallback, since it converges onto the shared catalog helper's
      contract rather than inventing a second fallback rule.
  </behavior>
  <action>
    In `frontend/src/app/admin/fansubs/[id]/edit/useGroupMembersTab.ts`: delete the `ROLE_LABELS`
    constant (lines 41-65, including its explanatory comment) and the exported function
    `roleLabelForCode` (lines 67-69) in their entirety. Add
    `import { labelForRole } from '@/lib/roleCatalog'` and
    `import type { RoleDefinitionOption } from '@/types/admin-capability'` to this file's import
    block. Extend `UseGroupMembersTabOptions` (lines 169-173) with a new required field
    `historyRoleOptions: RoleDefinitionOption[]`, and destructure it in
    `useGroupMembersTab({ fansubId, onActionsChange, onActiveAppMembersChanged, historyRoleOptions })`
    (line 175). In `historicalIdentityOptions`'s `useMemo` (lines 275-287), change
    `roleSummary: openRoles.map((role) => role.role_label ?? roleLabelForCode(role.role_code)).join(', ')`
    to `roleSummary: openRoles.map((role) => role.role_label ?? labelForRole(historyRoleOptions,
    role.role_code)).join(', ')`, and add `historyRoleOptions` to the `useMemo`'s dependency array
    (currently `[members, rolesByMember]`).

    In `frontend/src/app/admin/fansubs/[id]/edit/GroupMembersTab.tsx`: move the existing
    `const [historyRoleOptions, setHistoryRoleOptions] = useState<RoleDefinitionOption[]>([])` /
    `const [historyRoleLoadError, setHistoryRoleLoadError] = useState<string | null>(null)` /
    `useEffect(...)` block (currently declared right after the `useGroupMembersTab(...)` call, around
    lines 84-105) to BEFORE the `const tab = useGroupMembersTab({ fansubId, onActionsChange,
    onActiveAppMembersChanged })` call (currently line 82). Update that call to
    `const tab = useGroupMembersTab({ fansubId, onActionsChange, onActiveAppMembersChanged,
    historyRoleOptions })`. Do not change the `useEffect`'s body, dependency array, or the existing
    two call sites of `roleLabelForCode={(code) => labelForRole(historyRoleOptions, code)}` (lines
    170, 239, which are an unrelated prop name on `GroupHistRoleDialog`/`GroupMembersHistTable` and
    stay exactly as-is).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "ROLE_LABELS" frontend/src/app/admin/fansubs/[id]/edit/useGroupMembersTab.ts` returns no match
    - `grep -q "export function roleLabelForCode" frontend/src/app/admin/fansubs/[id]/edit/useGroupMembersTab.ts` returns no match
    - `grep -q "labelForRole(historyRoleOptions, role.role_code)" frontend/src/app/admin/fansubs/[id]/edit/useGroupMembersTab.ts`
    - `grep -q "historyRoleOptions: RoleDefinitionOption\[\]" frontend/src/app/admin/fansubs/[id]/edit/useGroupMembersTab.ts`
    - In `GroupMembersTab.tsx`, the line number of `const [historyRoleOptions, setHistoryRoleOptions]` is LOWER than the line number of `const tab = useGroupMembersTab(` (verifiable via `grep -n`)
    - `cd frontend && npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>useGroupMembersTab.ts no longer contains a parallel label registry; roleSummary resolves labels via the shared catalog path fed by GroupMembersTab.tsx's already-loaded historyRoleOptions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the removed-function unit test with a real historicalIdentityOptions call</name>
  <files>frontend/src/app/admin/fansubs/[id]/edit/useGroupMembersTab.test.ts</files>
  <read_first>
    frontend/src/app/admin/fansubs/[id]/edit/useGroupMembersTab.test.ts
    frontend/src/app/admin/fansubs/[id]/edit/useGroupMembersTab.ts
    frontend/src/lib/useMemberViewer.test.ts
  </read_first>
  <behavior>
    - A `historyRoleOptions` fixture containing a `RoleDefinitionOption` for `founder` (label_de
      `'Gründer/in'`) drives a historical member's `roleSummary` to include `'Gründer/in'` when that
      member has an open `founder` role with `role_label: null`.
    - The behavior is proven via a real call into the hook's returned `historicalIdentityOptions`
      (through `renderHook`), not via a direct call to the removed `roleLabelForCode` function.
  </behavior>
  <action>
    Remove `roleLabelForCode` from the import on line 5 (keep `findDuplicateMemberMatches`). Delete
    the entire `describe('roleLabelForCode', ...)` block (lines 7-16).

    Add `import { act, cleanup, renderHook, waitFor } from '@testing-library/react'` and
    `import { afterEach, ... } from 'vitest'` (merge into the existing vitest import), plus
    `import type { RoleDefinitionOption } from '@/types/admin-capability'`. Mock `@/lib/api` with
    `vi.mock('@/lib/api', () => ({ ... }))` (mirror `frontend/src/lib/useMemberViewer.test.ts`'s
    `vi.hoisted`/`vi.mock` pattern) providing at minimum: `listGroupMembers` resolving to `{ data:
    [buildMember({ id: 1, display_name: 'Sora', app_user_id: null })] }`, `listMemberRoles`
    resolving to `{ data: [{ id: 1, fansub_group_member_id: 1, member_display_name: 'Sora',
    role_code: 'founder', role_label: null, started_date: '2020-01-01', ended_date: null, note: null,
    status: 'historical', created_at: '2020-01-01T00:00:00Z' }] }`, `listClaimInvitations` resolving
    to `[]`, `listPendingMemberClaims` resolving to `[]`, `listMemberRequests` resolving to `[]`, and
    `ApiError` as a minimal error class (mirror `useMemberViewer.test.ts`'s `MockApiError`). Stub the
    remaining functions this hook and `useGroupMembersClaimActions` import from `@/lib/api`
    (`createGroupMember`, `createMemberRole`, `deleteGroupMember`, `deleteMemberRole`,
    `updateGroupMember`, `updateMemberRole`, `approveMemberRequest`, `cancelClaimInvitation`,
    `generateClaimInvitation`, `rejectMemberClaim`, `rejectMemberRequest`, `verifyMemberClaim`,
    `activateClaimedMember`) as `vi.fn()` no-ops so the module import does not throw.

    Add a new test: `renderHook(() => useGroupMembersTab({ fansubId: 10, historyRoleOptions:
    [{ code: 'founder', label_de: 'Gründer/in', sort_order: 1 } as RoleDefinitionOption] }))`, `await
    waitFor(...)` for `result.current.loading` to become `false`, then assert
    `result.current.historicalIdentityOptions.find((o) =&gt; o.displayName === 'Sora')?.roleSummary`
    equals `'Gründer/in'`. Add `afterEach(() =&gt; { cleanup(); vi.clearAllMocks() })` if not already
    present at file scope.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run "src/app/admin/fansubs/[id]/edit/useGroupMembersTab.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "describe('roleLabelForCode'" frontend/src/app/admin/fansubs/[id]/edit/useGroupMembersTab.test.ts` returns no match
    - `grep -q "renderHook" frontend/src/app/admin/fansubs/[id]/edit/useGroupMembersTab.test.ts`
    - `cd frontend && npx vitest run src/app/admin/fansubs/\[id\]/edit/useGroupMembersTab.test.ts` passes with 0 failures
    - The existing `findDuplicateMemberMatches` describe block (lines 39-74 pre-edit) still passes unchanged
  </acceptance_criteria>
  <done>The removed-function test is replaced by a real renderHook-driven proof that roleSummary resolves labels through the catalog path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| admin browser -> `useGroupMembersTab` hook -> `@/lib/api` | Authenticated admin-only surface (fansub group edit page); no new trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-147-05 | Information Disclosure | `roleSummary` label resolution | accept | Purely a display-string change (which German label text appears for a historical role) on an already-authenticated admin-only page; no new data crosses the client/server boundary and no new field is exposed. |
| T-147-06 | Tampering | None — pure refactor of an in-memory label lookup, no new user input surface | accept | No new input parsing, no new SQL, no new HTTP handler introduced. |
| T-147-SC | Tampering | No new package-manager installs in this plan | n/a | No npm/pip/cargo install tasks exist in this plan; gate not applicable. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` reports no new errors
- `cd frontend && npx eslint "src/app/admin/fansubs/[id]/edit/useGroupMembersTab.ts" "src/app/admin/fansubs/[id]/edit/GroupMembersTab.tsx" "src/app/admin/fansubs/[id]/edit/useGroupMembersTab.test.ts"` reports no new errors
- `cd frontend && npx vitest run "src/app/admin/fansubs/[id]/edit/useGroupMembersTab.test.ts"` passes
</verification>

<success_criteria>
- `ROLE_LABELS`/`roleLabelForCode` are gone from `useGroupMembersTab.ts`.
- `roleSummary` resolves through `labelForRole(historyRoleOptions, code)`.
- A real-call test (via `renderHook`) proves this behavior.
</success_criteria>

<output>
Create `.planning/phases/147-rollen-registry-letzte-parallelkataloge-aufl-sen/147-03-SUMMARY.md` when done
</output>
