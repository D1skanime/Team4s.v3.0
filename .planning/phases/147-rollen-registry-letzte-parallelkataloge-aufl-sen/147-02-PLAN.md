---
phase: 147-rollen-registry-letzte-parallelkataloge-aufl-sen
plan: 02
type: execute
wave: 2
depends_on: ["147-01"]
files_modified:
  - frontend/src/types/releaseDetail.ts
  - frontend/src/types/projectMember.ts
  - frontend/src/components/public/PublicNoteCard.tsx
  - frontend/src/components/public/PublicNoteCard.test.tsx
  - frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseNotesList.tsx
  - frontend/src/components/fansubs/projectMember/ProjectMemberNoteCard.tsx
  - frontend/src/lib/roleColors.ts
autonomous: true
requirements: [HC-01]
must_haves:
  truths:
    - "PublicNoteCard's role-color data-role-code attribute is set from a stable roleCode prop, not from a label->code reverse lookup"
    - "ROLE_CODE_BY_LABEL and roleColorCode in frontend/src/lib/roleColors.ts are removed with no remaining consumer"
    - "A frontend regression test proves fansub_lead, founder, co_leader, techadmin, gfxler, karaoke_fx, editor and typesetter each get their own data-role-code directly from the prop, and that changing roleLabel alone does not change data-role-code"
  artifacts:
    - path: frontend/src/components/public/PublicNoteCard.tsx
      provides: "roleCode prop driving data-role-code"
    - path: frontend/src/types/releaseDetail.ts
      provides: "PublicReleaseNote.role_code TS field"
    - path: frontend/src/types/projectMember.ts
      provides: "ProjectMemberNote.role_code TS field"
  key_links:
    - from: frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseNotesList.tsx
      to: frontend/src/components/public/PublicNoteCard.tsx
      via: "roleCode={note.role_code} prop"
      pattern: "roleCode=\\{note\\.role_code\\}"
    - from: frontend/src/components/fansubs/projectMember/ProjectMemberNoteCard.tsx
      to: frontend/src/components/public/PublicNoteCard.tsx
      via: "roleCode={note.role_code} prop"
      pattern: "roleCode=\\{note\\.role_code\\}"
---

<objective>
Consume the `role_code` field Plan 147-01 added to both backend DTOs/OpenAPI schemas all the way
into `PublicNoteCard`'s `data-role-code` attribute, replacing the broken `roleColors.ts` label→code
reverse map, per CONTEXT.md's HC-01 decision and ROADMAP.md Success Criteria 1 and 3.

Purpose: `role_code` now exists on the wire (Plan 147-01); this plan is the frontend half — typing
it, threading it through both note-card consumers, switching `PublicNoteCard` to read it directly,
and deleting the now-dead reverse-map utility.
Output: `PublicNoteCard` with a `roleCode` prop as the sole `data-role-code` source; `roleColors.ts`
deleted; a regression test covering all 8 currently-relevant role codes plus label-independence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/147-rollen-registry-letzte-parallelkataloge-aufl-sen/147-CONTEXT.md
@.planning/phases/147-rollen-registry-letzte-parallelkataloge-aufl-sen/147-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Type role_code and add the roleCode prop to PublicNoteCard</name>
  <files>frontend/src/types/releaseDetail.ts, frontend/src/types/projectMember.ts, frontend/src/components/public/PublicNoteCard.tsx</files>
  <read_first>
    frontend/src/types/releaseDetail.ts
    frontend/src/types/projectMember.ts
    frontend/src/components/public/PublicNoteCard.tsx
    frontend/src/lib/roleColors.ts
  </read_first>
  <behavior>
    - `PublicNoteCard` renders `data-role-code` equal to the `roleCode` prop value when provided.
    - When `roleCode` is `undefined`/`null`/empty, `data-role-code` falls back to `'other'` (preserves
      `roleColorCode`'s existing unknown->`'other'` fallback semantics for callers that have not yet
      migrated, and for notes whose backend-resolved `role_code` is an empty string).
    - `roleLabel` continues to render as pure display text (header band text, role-variant title) and
      no longer influences `data-role-code` in any way.
  </behavior>
  <action>
    In `frontend/src/types/releaseDetail.ts`'s `PublicReleaseNote` interface (lines 26-34), add
    `role_code: string;` immediately after `role_label: string;` (this file uses semicolons —
    preserve that convention).

    In `frontend/src/types/projectMember.ts`'s `ProjectMemberNote` interface (lines 21-29), add
    `role_code: string` immediately after `role_label: string` (this file has NO semicolons —
    preserve that convention).

    In `frontend/src/components/public/PublicNoteCard.tsx`: remove the import
    `import { roleColorCode } from '@/lib/roleColors'` (line 9). In the `PublicNoteCardProps`
    interface (lines 23-25), add a new optional prop directly after `roleLabel`:
    `roleCode?: string | null` with a doc comment `/** Stabiler Rollen-Code
    (role_definitions.code) für data-role-code; unabhängig vom Anzeigelabel. */`. Destructure
    `roleCode` alongside `roleLabel` in the function signature (line 54). Change the `<article>`
    element (line 76) from `data-role-code={roleColorCode(roleLabel ?? '')}` to
    `data-role-code={roleCode || 'other'}`. Do not change any other usage of `roleLabel` in this
    file (header text at lines 97 and 103 stay untouched — pure display text).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "roleCode?: string | null" frontend/src/components/public/PublicNoteCard.tsx`
    - `grep -q "data-role-code={roleCode || 'other'}" frontend/src/components/public/PublicNoteCard.tsx`
    - `grep -q "roleColorCode" frontend/src/components/public/PublicNoteCard.tsx` returns no match (import and usage both removed)
    - `grep -q "role_code: string" frontend/src/types/releaseDetail.ts`
    - `grep -q "role_code: string" frontend/src/types/projectMember.ts`
    - `cd frontend && npx tsc --noEmit` reports no new errors in these three files
  </acceptance_criteria>
  <done>PublicNoteCard reads data-role-code exclusively from the new roleCode prop; both note DTOs are typed with role_code.</done>
</task>

<task type="auto">
  <name>Task 2: Thread roleCode through both PublicNoteCard consumers and delete roleColors.ts</name>
  <files>frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseNotesList.tsx, frontend/src/components/fansubs/projectMember/ProjectMemberNoteCard.tsx, frontend/src/lib/roleColors.ts</files>
  <read_first>
    frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseNotesList.tsx
    frontend/src/components/fansubs/projectMember/ProjectMemberNoteCard.tsx
  </read_first>
  <action>
    In `ReleaseNotesList.tsx`'s `PublicNoteCard` usage (around line 71), add
    `roleCode={note.role_code}` immediately after `roleLabel={note.role_label}` (same
    one-prop-per-line style as the surrounding props).

    In `ProjectMemberNoteCard.tsx`'s `PublicNoteCard` usage (around line 25), add
    `roleCode={note.role_code}` immediately after `roleLabel={note.role_label}`.

    Before deleting `frontend/src/lib/roleColors.ts`, run
    `grep -rn "roleColors" frontend/src --include="*.ts*"` to reconfirm `PublicNoteCard.tsx` was the
    sole consumer (already verified during phase mapping — re-verify here since this task deletes the
    file). Delete `frontend/src/lib/roleColors.ts` entirely (its exported `ROLE_CODE_BY_LABEL` map and
    `roleColorCode` function have no remaining consumer after Task 1).
  </action>
  <verify>
    <automated>test ! -f frontend/src/lib/roleColors.ts && grep -rL "roleColors" frontend/src --include="*.ts*" | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "roleCode={note.role_code}" "frontend/src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseNotesList.tsx"`
    - `grep -q "roleCode={note.role_code}" frontend/src/components/fansubs/projectMember/ProjectMemberNoteCard.tsx`
    - `test ! -f frontend/src/lib/roleColors.ts` (file no longer exists)
    - `grep -rln "roleColors" frontend/src --include="*.ts*"` returns no results (no dangling import)
    - `cd frontend && npx tsc --noEmit` reports no errors
  </acceptance_criteria>
  <done>Both consumers pass roleCode through; roleColors.ts is deleted with zero remaining references.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Frontend regression test — 8 roles get their own code, label change is inert</name>
  <files>frontend/src/components/public/PublicNoteCard.test.tsx</files>
  <read_first>frontend/src/components/public/PublicNoteCard.test.tsx</read_first>
  <behavior>
    - Each render call in the existing 4 test cases passes `roleCode` explicitly, and
      `data-role-code` is asserted to equal that `roleCode` value directly (not a value derived from
      `roleLabel`).
    - A new parametrized test proves each of the 8 roles `fansub_lead`, `founder`, `co_leader`,
      `techadmin`, `gfxler`, `karaoke_fx`, `editor`, `typesetter` renders its own `data-role-code`
      equal to its own code (no `'other'` fallback for any of these 8).
    - A new test proves that re-rendering with the same `roleCode` but a different `roleLabel` leaves
      `data-role-code` unchanged.
  </behavior>
  <action>
    Update all 4 existing `render(&lt;PublicNoteCard roleLabel="..." .../&gt;)` calls (lines
    11-28, 30-45, 47-64, 66-68) to also pass a `roleCode` prop (e.g. `roleCode="quality_checker"` for
    the first case, `roleCode="timer"` for the second, `roleCode="editor"` for the third and fourth —
    match each case's existing `roleLabel` German text to its corresponding code from
    `role_definitions`), and change each `data-role-code` assertion from the current label-derived
    expectation to `expect(card.getAttribute('data-role-code')).toBe(&lt;the roleCode value passed
    in&gt;)`.

    Add a new `describe('data-role-code (role_code-driven, Phase 147)', ...)` block with: (1) an
    `it.each` or loop-based test rendering `PublicNoteCard` once per code in
    `['fansub_lead', 'founder', 'co_leader', 'techadmin', 'gfxler', 'karaoke_fx', 'editor',
    'typesetter']` with `roleCode={code}` and a fixed `roleLabel="Beliebiges Label"`, asserting
    `screen.getByRole('article').getAttribute('data-role-code')` equals that exact `code` (never
    `'other'`); (2) one test rendering with `roleCode="editor"` and `roleLabel="Editing"`, then
    re-rendering (via RTL's `rerender`) the same component with `roleCode="editor"` but
    `roleLabel="Etwas ganz anderes"`, asserting `data-role-code` stays `"editor"` across both renders.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/public/PublicNoteCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/public/PublicNoteCard.test.tsx` passes with 0 failures
    - Test file contains 8 distinct assertions (or one parametrized assertion covering 8 cases) for `fansub_lead`, `founder`, `co_leader`, `techadmin`, `gfxler`, `karaoke_fx`, `editor`, `typesetter`, each expecting `data-role-code` to equal its own code
    - Test file contains a `rerender`-based (or equivalent two-render) assertion proving a `roleLabel` change alone does not change `data-role-code`
  </acceptance_criteria>
  <done>The test suite proves role_code drives data-role-code directly for all 8 named roles and is immune to label changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `PublicNoteCard` props -> rendered DOM attribute | `roleCode` is server-supplied data rendered as a `data-*` attribute value, not interpreted as markup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-147-03 | Information Disclosure | `data-role-code` DOM attribute now reflects the real `role_code` instead of a mis-mapped/broken value | accept | Same conclusion as Plan 147-01's T-147-01: `role_code` is a strict subset of information already disclosed via the adjacent, already-public `roleLabel` text node in the same component. No new fact reaches the DOM that a user could not already read from the visible role label. |
| T-147-04 | Tampering | React renders `roleCode` as a plain string attribute value (`data-role-code={roleCode || 'other'}`) | accept | React's JSX attribute binding auto-escapes; no `dangerouslySetInnerHTML` or raw HTML injection path is introduced. `roleCode` values originate from the backend's `role_definitions.code` catalog (a controlled, small enum), not free-form user input. |
| T-147-SC | Tampering | No new package-manager installs in this plan | n/a | No npm/pip/cargo install tasks exist in this plan; gate not applicable. |

**Disclaimer on the phase-wide HC-03 authorization-regression question (AppGlobalRoles
derivation):** not applicable to this plan — this plan's diff touches only frontend note-card
types/props/rendering (`PublicNoteCard`, `ReleaseNotesList.tsx`, `ProjectMemberNoteCard.tsx`,
`roleColors.ts`) and does not touch `models.AppGlobalRoles` or any global-role assignment path.
Plan 147-04 owns that assessment.
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` reports no new errors
- `cd frontend && npx eslint src/components/public/PublicNoteCard.tsx src/types/releaseDetail.ts src/types/projectMember.ts "src/app/anime/[id]/group/[groupId]/releases/[releaseVersionId]/ReleaseNotesList.tsx" src/components/fansubs/projectMember/ProjectMemberNoteCard.tsx` reports no new errors
- `cd frontend && npx vitest run src/components/public/PublicNoteCard.test.tsx` passes
- `test ! -f frontend/src/lib/roleColors.ts`
</verification>

<success_criteria>
- `PublicNoteCard`'s `data-role-code` is sourced solely from the `roleCode` prop.
- `roleColors.ts` is deleted; no dangling imports remain.
- A regression test proves 8 named roles each get their own code and that label changes are inert.
</success_criteria>

<output>
Create `.planning/phases/147-rollen-registry-letzte-parallelkataloge-aufl-sen/147-02-SUMMARY.md` when done
</output>
